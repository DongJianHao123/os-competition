# 🛡️ OS 内核赛自动化代码级架构审计报告

**审计时间**: `2026-07-02 03:19:07`
**参赛作品名称**: `2026-T202610112999587-XX`
**年份**: `2026`
**学校**: `XX`
**队伍名称**: `T202610112999587`
**比赛名字**: `OS-Competition`
**比赛子赛道**: `Kernel-Implementation`
**仓库地址**: `https://gitlab.eduxiji.net/educg-group-43501-3132633/T202610112999587-3708`
**推测开发语言**: `Rust / C (由AST探针自动识别)`
**特征函数量**: `10776`

## ⚔️ 底层代码同源性对比 (AST Homology)
> 本部分由专门的代码审查智能体，对该项目与符合年份条件的历届最高相似度项目进行控制流与底层调用对比。新版报告会先给出确定性的全模块覆盖矩阵，再给出 Agent 的分模块解释，避免只围绕少量抽样函数展开。

### 🏆 Top 5 底层对标: vs `2025-AstranciA-哈尔滨工业大学深圳`（哈尔滨工业大学（深圳） / AstranciA）

#### 对方作品信息

**对方年份**: `2025`
**对方学校**: `哈尔滨工业大学（深圳）`
**对方队伍名称**: `AstranciA`
**对方比赛名字**: `OS-Competition`
**对方比赛子赛道**: `Kernel-Implementation`
**对方仓库地址**: `https://gitlab.eduxiji.net/educg-group-36002-2710490/T202518123995667-1436`

> 双向覆盖率：共同 AST 指纹: 76；唯一 AST 指纹: 目标项目 8484，参考项目 3750；覆盖率: 目标项目 0.9%，参考项目 2.03%

## 全模块对比总览（确定性证据）

> 总体覆盖率：共同 AST 指纹: 76；唯一 AST 指纹: 目标项目 8484，参考项目 3750；覆盖率: 目标项目 0.9%，参考项目 2.03%

### 1. 模块覆盖矩阵
| 模块 | 目标函数数 | 参考函数数 | 共同唯一 AST 指纹 | Target 覆盖 | Base 覆盖 | 状态 |
| :--- | ---: | ---: | ---: | ---: | ---: | :--- |
| 进程管理 | 696 | 112 | 5 | 0.88% | 5.43% | 双方存在且有结构重合 |
| 任务调度/异步运行时 | 1070 | 455 | 5 | 0.58% | 1.56% | 双方存在且有结构重合 |
| 内存管理 | 1729 | 343 | 2 | 0.13% | 0.65% | 双方存在且有结构重合 |
| 中断/异常处理 | 226 | 71 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |
| 系统调用 | 37 | 136 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |
| 文件系统 | 2220 | 620 | 5 | 0.28% | 1.03% | 双方存在且有结构重合 |
| 设备驱动 | 808 | 142 | 2 | 0.31% | 1.59% | 双方存在且有结构重合 |
| 网络协议栈 | 439 | 61 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |
| 文件系统/第三方库 | 0 | 775 | 0 | 0.0% | 0.0% | 仅参考项目体现 |
| 网络协议栈/第三方库 | 2456 | 217 | 3 | 0.16% | 1.55% | 双方存在且有结构重合 |
| 第三方库 | 7 | 1155 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |
| 通用模块/其他 | 1088 | 673 | 8 | 0.9% | 1.47% | 双方存在且有结构重合 |

### 2. 分模块证据清单

#### 进程管理
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `kernel/src/syscalls/process.rs#sys_futex` | `AstrancE/ulib/axmono/src/syscall/process.rs#sys_futex` | 同板块同名 | Err, Ok, ctx.task, futex_cmd, futex_key, futex_lock_pi ...(+7) | task::sys_exit |
| `kernel/src/syscalls/signal.rs#sys_rt_sigtimedwait` | `AstrancE/ulib/axmono/src/syscall/signal.rs#sys_rt_sigtimedwait` | 同板块同名 | (sec as u64)
                .saturating_mul, (sec as u64)
                .saturating_mul(1_000_000_000)
                .saturating_add, Err, Ok, SigSet::from_raw, Some ...(+15) | crate::task::signal::sys_sigtimedwait, crate::task::signal::sys_sigtimedwait(set as _, info as _, timeout as _).map |
| `kernel/src/syscalls/process.rs#sys_clone` | `AstrancE/ulib/axmono/src/syscall/process.rs#sys_clone` | 同板块同名 | Arc::clone, CloneFlags::from_raw, Err, Ok, UserContextRef::new, ctx.tf.as_usize ...(+5) | CloneFlags::from_bits_retain, Ok, Some, child_task.task_ext, child_task.task_ext().thread.process, child_task.task_ext().thread.process().pid ...(+1) |
| `kernel/src/syscalls/signal.rs#sys_rt_sigprocmask` | `AstrancE/ulib/axmono/src/syscall/signal.rs#sys_rt_sigprocmask` | 同板块同名 | Err, Ok, SigSet::from_raw, copy_from_user, copy_from_user(set_user, &mut raw).map_err, copy_to_user ...(+8) | crate::task::signal::sys_sigprocmask |

#### 任务调度/异步运行时
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `kernel/src/syscalls/signal.rs#sys_rt_sigsuspend` | `AstrancE/ulib/axmono/src/task/signal.rs#sys_rt_sigsuspend` | 同板块同名 | Err, SigSet::from_raw, copy_from_user, copy_from_user(set_user, &mut raw).map_err, ctx
            .task, ctx
            .task()
            .cas_state ...(+8) | (*mask_ref).into, Err, core::mem::size_of::<sigset_t>, curr.task_ext, curr.task_ext().process_data, curr.task_ext().process_data().signal.lock ...(+7) |
| `libs/sched/src/mutex.rs#lock` | `AstrancE/ulib/axstd/src/sync/mutex.rs#lock` | 同板块同名 | current_task, now_ns_public, schedule_once, self
                .locked
                .compare_exchange_weak, self
                .locked
                .compare_exchange_weak(false, true, Ordering::Acquire, Ordering::Relaxed)
                .is_ok, self.waiters.finish_wait ...(+1) | api::ax_current_task_id, api::ax_wait_queue_wait_until, assert_ne!, self.data.get, self.is_locked, self.owner_id.compare_exchange_weak |
| `libs/sched/src/rlimit.rs#name` | `AstrancE/ulib/axstd/src/thread/multi.rs#name` | 同板块同名 | 内部闭环逻辑 | Some |
| `kernel/src/syscalls/signal.rs#sys_rt_sigaction` | `AstrancE/ulib/axmono/src/syscall/signal.rs#sys_rt_sigaction` | 同板块同名 | Err, Ok, SignalNumber::from_raw, SignalNumber::from_raw(ctx.args[0] as i32).ok_or, ctx.task, ctx.task().shared_signal ...(+4) | crate::task::signal::sys_sigaction, crate::task::signal::sys_sigaction(signum, act as _, oldact as _).map_err, panic! |

#### 内存管理
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `libs/sched/src/spawn.rs#clone_task` | `AstrancE/ulib/axmono/src/task/process.rs#clone_task` | 同板块同名 | Arc::clone, Arc::downgrade, Arc::new, SchedAttr::fair, SignalNumber::SIGCHLD.raw, Task::new ...(+48) | Arc::new, CURRENT_DIR
                .deref_from, CURRENT_DIR
                .deref_from(&process_data.ns)
                .init_new, CURRENT_DIR
                .deref_from(&process_data.ns)
                .init_shared, CURRENT_DIR.copy_inner, CURRENT_DIR.share ...(+72) |
| `libs/allocator/src/slab.rs#alloc` | `AstrancE/modules/axalloc/src/lib.rs#alloc` | 同板块同名 | CACHE_CAPACITY
                .saturating_sub, CACHE_CAPACITY
                .saturating_sub(cache_guard.count)
                .min, CacheEntry::empty, SlabAllocation::null, Some, allocate_slab_node ...(+17) | Ok, balloc.add_memory, balloc.alloc, balloc.total_bytes, debug!, layout.size ...(+5) |
| `general/src/mm/vm_space.rs#mremap` | `AstrancE/ulib/axlibc/c/mmap.c#mremap` | 同板块同名 | Err, Ok, Self::collect_file_backings, Self::notify_file_unmapped, Self::notify_files_mapped, Some ...(+30) | unimplemented |
| `libs/allocator/src/lib.rs#realloc` | `AstrancE/ulib/axlibc/c/stdlib.c#realloc` | 同板块同名 | AllocationRecord::new, AllocationRecord::new(AllocationKind::Boot, MemoryDomain::Kernel, ptr as usize)
                        .with_sizes, AllocationRecord::new(AllocationKind::Boot, MemoryDomain::Kernel, ptr as usize)
                    .with_sizes, Layout::from_size_align, Some, core::ptr::copy_nonoverlapping ...(+19) | free, malloc |

#### 中断/异常处理
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `general/src/dev/drivers/loongson_irq.rs#translate_source` | `AstrancE/ulib/axsyscall/src/syscall_imp/errno.rs#try_from` | 同模块上下文对齐/差异观察 | Some, drop, entry.is_some_and, inner
                .slots
                .iter, inner
                .slots
                .iter()
                .enumerate, inner
                .slots
                .iter()
                .enumerate()
                .find ...(+13) | Err, Ok |
| `libs/errno/src/lib.rs#from_i32` | `AstrancE/modules/axhal/src/arch/loongarch64/unaligned.rs#emulate_load_store_insn` | 同模块上下文对齐/差异观察 | Errno::Other | asm!, badv::read, badv::read().raw, core::slice::from_raw_parts_mut, panic!, read_fpr ...(+5) |
| `libs/errno/src/lib.rs#as_i32` | `AstrancE/ulib/axlibc/c/pow.c#exp_inline` | 同模块上下文对齐/差异观察 | 内部闭环逻辑 | __math_oflow, __math_uflow, asdouble, asuint64, converttoint, eval_as_double ...(+4) |
| `kernel/src/dtb/pci.rs#install_irq_routing` | `AstrancE/modules/axhal/src/arch/aarch64/trap.rs#handle_sync_exception` | 同模块上下文对齐/差异观察 | PCI_IRQ_ROUTING.lock, Some, Vec::new, child_key.extend_from_slice, child_key.into_boxed_slice, entry.child_address.len ...(+10) | ESR_EL1.extract, crate::trap::handle_syscall, crate::trap::post_trap_callback, debug!, esr.read, esr.read_as_enum ...(+8) |

#### 系统调用
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `kernel/src/syscalls/ipc.rs#encode_shmid64_ds` | `AstrancE/modules/axsignal/src/siginfo.rs#fill_raw_siginfo` | 同模块上下文对齐/差异观察 | meta.perm.mode.bits, write_i32, write_i64, write_u16, write_u32, write_u64 | addr.as_ptr, call_addr.as_ptr, core::mem::size_of::<siginfo_t>, i32::from, ptr.as_ptr, ptr::write_bytes ...(+1) |
| `kernel/src/syscalls/signal.rs#read_stack_t` | `AstrancE/api/arceos_posix_api/src/imp/net.rs#sys_getaddrinfo` | 同模块上下文对齐/差异观察 | Err, Ok, SigAltStack::disabled, copy_from_user, copy_from_user(user, &mut raw).map_err, e.as_errno ...(+10) | char_ptr_to_str, debug!, syscall_body! |
| `hal/src/user.rs#patch_loongarch64_enosys_stub` | `AstrancE/ulib/axmono/src/syscall/time.rs#sys_setitimer` | 同模块上下文对齐/差异观察 | ENOSYS_STUB_PREFIX.len, bytes.get, bytes[off..off + patch.len()].copy_from_slice, patch.len, write_u32 | Err, ItimerConfig::default, Ok, Some, new_value.is_null, old_value.is_null ...(+1) |
| `kernel/src/syscalls/syslog.rs#set_console_level` | `AstrancE/api/arceos_posix_api/src/imp/fd_ops.rs#sys_ppoll` | 同模块上下文对齐/差异观察 | (1..=8).contains, Err, Ok, log::set_console_level, unreachable! | syscall_body! |

#### 文件系统
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `libs/extfs/src/inode.rs#rename` | `AstrancE/modules/axfs/src/fs/fatfs.rs#rename` | 同板块同名 | Arc::ptr_eq, Err, Ok, copy_i_block, core::ptr::eq, crate::dir::is_dir_empty ...(+59) | debug!, self.0
            .rename, self.0
            .rename(src_path, &self.0, dst_path)
            .map_err |
| `general/src/vfs/tmpfs.rs#rename` | `AstrancE/modules/axfs/src/root.rs#rename` | 同板块同名 | Err, Ok, Some, dir.dec_nlink, dir.fs_id, dir.ino ...(+30) | ax_err!, fs.root_dir, fs.root_dir().rename, rest_path.is_empty, self.lookup_mounted_fs |
| `libs/extfs/src/file.rs#read_at` | `AstrancE/modules/axmm/src/shm/posix.rs#read_at` | 同板块同名 | (offset + remaining as u64).min, (size - offset).min, (start + remaining).min, Ok, Vec::new, buf.is_empty ...(+25) | (self.size - offset).min, Ok, buf.len, buf[bytes_read..bytes_read + bytes_to_copy].copy_from_slice, self
                .get_slice_at, self
                .get_slice_at(current_offset)
                .ok_or ...(+2) |
| `libs/fatfs/src/inode.rs#rename` | `AstrancE/modules/axfs/src/root.rs#rename` | 同板块同名 | Arc::ptr_eq, Err, Ok, Vec::new, dir::build_entries_for_name, dir::build_sfn_entry ...(+17) | parent_node_of, parent_node_of(None, new).lookup, parent_node_of(None, new).lookup(new).is_ok, parent_node_of(None, old).rename, remove_file, warn! |

#### 设备驱动
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `general/src/dev/drivers/loongson_irq.rs#probe` | `AstrancE/modules/axdriver/src/lib.rs#probe` | 同板块同名 | Arc::clone, Arc::new, EioIntc::new, EioIntcRouteConfig::from_platform, EioIntcRouteConfig::from_platform(parent_hwi, info).ok_or, Err ...(+19) | for_each_drivers!, info!, self.probe_bus_devices |
| `general/src/dev/function.rs#push` | `AstrancE/modules/axdriver/src/structs/static.rs#push` | 同板块同名 | Arc::clone, Err, Ok, Vec::new, core::mem::replace, drop ...(+22) | Some, self.0.is_none |
| `arch/src/riscv64/early_console.rs#write_bytes` | `AstrancE/modules/axhal/src/platform/loongarch64_qemu_virt/console.rs#write_bytes` | 同板块同名 | AtomicBool::new, BASE.load, INITED.load, INITED.store, INITIALIZING
                .compare_exchange, INITIALIZING
                .compare_exchange(false, true, Ordering::Acquire, Ordering::Relaxed)
                .is_ok ...(+9) | UART.lock, uart.put |
| `general/src/dev/drivers/random.rs#block` | `AstrancE/modules/axsignal/src/lib.rs#block` | 同板块同名 | Self::quarter_round, out[i * 4..(i + 1) * 4].copy_from_slice, s[12].wrapping_add, s[i].to_le_bytes, s[i].wrapping_add | self.blocked.union |

#### 网络协议栈
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `libs/socket/src/state.rs#send` | `AstrancE/api/arceos_posix_api/src/imp/net.rs#send` | 同板块同名 | Err, effective_send_deadline, io::send_datagram, io::send_seqpacket, io::send_stream, options.sender_identity.unwrap_or ...(+2) | Ok, tcpsocket.lock, tcpsocket.lock().send, udpsocket.lock, udpsocket.lock().send |
| `libs/socket/src/state.rs#accept` | `AstrancE/api/arceos_posix_api/src/imp/net.rs#accept` | 同板块同名 | Err, connection::accept_connection_socket, effective_recv_deadline, self.finish_result | Err, Ok, tcpsocket.lock, tcpsocket.lock().accept |
| `libs/socket/src/state.rs#connect` | `AstrancE/ulib/axstd/src/net/tcp.rs#connect` | 同板块同名 | connection::connect_datagram, connection::connect_seqpacket, connection::connect_stream, effective_send_deadline, self.finish_result | Ok, TcpStream, api::ax_tcp_connect, api::ax_tcp_socket, super::each_addr |
| `libs/socket/src/state.rs#bind` | `AstrancE/ulib/axstd/src/net/tcp.rs#bind` | 同板块同名 | Err, Ok, Some, address.binding_key, address.binding_key().ok_or, current.is_some ...(+3) | Ok, TcpListener, api::ax_tcp_bind, api::ax_tcp_listen, api::ax_tcp_socket, super::each_addr |

#### 文件系统/第三方库
仅参考项目中观察到该模块代表函数，目标项目没有同模块可对齐证据：
- 参考项目：`AstrancE/crates/lwext4_rust/c/lwext4/src/ext4_balloc.c#ext4_balloc_alloc_block`，调用摘要：ext4_balloc_get_bgid_of_block, ext4_balloc_get_block_of_bgid, ext4_balloc_set_bitmap_csum, ext4_balloc_verify_bitmap_csum, ext4_bg_get_block_bitmap, ext4_bg_get_free_blocks_count ...(+19)
- 参考项目：`AstrancE/crates/lwext4_rust/c/lwext4/src/ext4_journal.c#jbd_journal_prepare`，调用摘要：EXT4_BLOCK_ZERO, RB_FIND, TAILQ_FOREACH_REVERSE_SAFE, TAILQ_FOREACH_SAFE, TAILQ_REMOVE, ext4_bcache_clear_dirty ...(+17)
- 参考项目：`AstrancE/crates/lwext4_rust/c/lwext4/src/ext4.c#ext4_dir_rm`，调用摘要：EXT4_MP_LOCK, EXT4_MP_UNLOCK, ext4_block_cache_write_back, ext4_dir_en_get_inode, ext4_dir_iterator_fini, ext4_dir_iterator_init ...(+19)
- 参考项目：`AstrancE/crates/lwext4_rust/c/lwext4/src/ext4_dir_idx.c#ext4_dir_dx_split_index`，调用摘要：ext4_block_set, ext4_dir_dx_climit_get_count, ext4_dir_dx_climit_get_limit, ext4_dir_dx_climit_set_count, ext4_dir_dx_climit_set_limit, ext4_dir_dx_entry_get_hash ...(+10)

#### 网络协议栈/第三方库
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `libs/mygo-smoltcp/src/storage/assembler.rs#add` | `AstrancE/crates/virtio-drivers/src/device/socket/connectionmanager.rs#add` | 同板块同名 | Contig::hole_and_data, Err, Ok, contig.has_data, contig.shrink_hole_to, contig.total_size ...(+11) | bytes.get, bytes.len, bytes_after_wraparound.len, min, self.buffer.len, self.buffer[0..bytes_after_wraparound.len()].copy_from_slice ...(+2) |
| `libs/mygo-smoltcp/src/socket/tcp.rs#connect` | `AstrancE/modules/axnet/src/smoltcp_impl/tcp.rs#connect` | 同板块同名 | Err, Ok, Self::random_seq_no, Some, addr.is_unspecified, cx
                    .get_source_address ...(+9) | Err, Ok, Ok::<(IpEndpoint, IpEndpoint), AxError>, SOCKET_SET
                .with_socket_mut::<tcp::Socket, _, _>, SOCKET_SET.add, SocketSetWrapper::new_tcp_socket ...(+26) |
| `libs/mygo-smoltcp/src/socket/tcp.rs#send_impl` | `AstrancE/modules/axnet/src/smoltcp_impl/udp.rs#send_impl` | 同板块同名 | Err, Ok, f, self.may_send, self.rtte.retransmission_timeout, self.timer.is_idle ...(+3) | Err, Ok, SOCKET_SET.with_socket_mut::<udp::Socket, _, _>, ax_err!, ax_err_type!, buf.len ...(+6) |
| `libs/mygo-smoltcp/src/phy/fault_injector.rs#receive` | `AstrancE/crates/virtio-drivers/src/device/net/dev.rs#receive` | 同板块同名 | <D::RxToken<'_> as phy::RxToken>::meta, Some, buffer.len, net_trace!, self.inner.receive, self.rx_buf.len ...(+5) | Err, Ok, rx_buf.as_bytes_mut, rx_buf.set_packet_len, self.inner.poll_receive, self.inner.receive_complete ...(+2) |

#### 第三方库
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `libs/efi/src/c/efi_global.c#efi_system_table_firmware_vendor` | `AstrancE/crates/axdriver_crates/axdriver_block/src/dw_mshc/mmc.rs#card_init` | 同模块上下文对齐/差异观察 | efi_system_table_is_valid | CID::from, CLKDIV::offset, CMD::data_cmd, CMD::no_data_cmd, CMD::no_data_cmd_no_crc, CMD::no_data_cmd_no_crc(card_idx, 2).with_response_length ...(+33) |
| `libs/efi/src/lib.rs#firmware_vendor` | `AstrancE/crates/virtio-drivers/src/queue.rs#fake_read_write_queue` | 同模块上下文对齐/差异观察 | Some, core::ptr::null, core::slice::from_raw_parts, efi_system_table_firmware_vendor, ptr.is_null | (*available_ring).idx.load, (*used_ring).idx.fetch_add, (*used_ring).idx.load, Vec::new, assert!, assert_eq! ...(+18) |
| `libs/efi/src/lib.rs#firmware_vendor_cstr16` | `AstrancE/crates/virtio-drivers/src/device/sound/fake.rs#handle_control_request` | 同模块上下文对齐/差异观察 | firmware_vendor | CommandCode::SOk.into, Some, Vec::new, VirtIOSndHdr {
                            command_code: CommandCode::SOk.into(),
                        }
                        .as_bytes, VirtIOSndHdr::read_from_prefix, VirtIOSndHdr::read_from_prefix(&request)
                .expect ...(+16) |
| `general/src/vfs/procfs.rs#cpuinfo_vendor_from_compatible` | `AstrancE/crates/virtio-drivers/src/transport/pci.rs#new` | 同模块上下文对齐/差异观察 | compatible
        .split_once, compatible
        .split_once(',')
        .map, compatible
        .split_once(',')
        .map(|(vendor, _)| vendor)
        .unwrap_or | Err, Ok, Some, UniqueMmioPointer::new, VirtioPciError::InvalidDeviceId, VirtioPciError::InvalidNotifyOffMultiplier ...(+15) |

#### 通用模块/其他
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `libs/log/src/lib.rs#write_str` | `AstrancE/modules/axlog/src/lib.rs#write_str` | 同板块同名 | Ok, ch.len_utf8, s.as_bytes, s.char_indices, s.len, self.buf[self.len..self.len + copy_len].copy_from_slice | Ok, cfg_if::cfg_if! |
| `general/src/ipc/shm.rs#remove` | `AstrancE/ulib/axlibc/c/stdio.c#remove` | 同板块同名 | Ok, check_control_owner, remove_entry_locked, self.state.lock, state.by_id.get_mut, state.by_id.get_mut(&id).ok_or ...(+1) | unimplemented |
| `general/src/dev/char.rs#write_all` | `AstrancE/ulib/axlibc/src/strftime.rs#write_all` | 同板块同名 | Err, Ok, core::hint::spin_loop, remaining.is_empty, self.write | Err, Ok, buf.len, self.inner.write_all |
| `arch/src/loongarch64/loader.rs#time` | `AstrancE/ulib/axlibc/c/time.c#time` | 同板块同名 | STABLE_TIMER_HZ.load, core::arch::asm! | clock_gettime |


好的，我将遵循审计指令，基于提供的证据材料，对本案进行最终的独立定性仲裁。

### 1. 宏观判定
- **定性结论**: **独立开发**
- **核心依据**: 客观 AST 相似度仅为 0.63%，双向覆盖率均低于 3%。该量化指标远低于认定代码复用的合理阈值，属于领域噪声水平。控方提出的“复用指控”均基于单条原子操作、标准容器初始化或 POSIX 系统调用入口等不具备独创性的 AST 节点，无法构成系统性抄袭的证据链。辩方提供的架构差异分析（如任务抽象、同步原语、系统调用实现路径）与客观调用图谱高度吻合，支持独立开发的结论。

### 2. 全模块覆盖矩阵解读
- **进程管理**:
  1. 双方均存在该模块。
  2. 存在 5 个共同 AST 指纹。
  3. 目标覆盖率 0.88%，参考覆盖率 5.43%。覆盖率极低，表明双方在进程管理的庞大函数群中，仅有极少数的底层原子操作节点重合。
  4. 证据强度：**弱**。重合点均为 `load`/`store` 原子操作或标准容器初始化，是 Rust 内核编程的通用惯用法，不构成独创性表达的重合。
- **任务调度/异步运行时**:
  1. 双方均存在该模块。
  2. 存在 5 个共同 AST 指纹。
  3. 目标覆盖率 0.58%，参考覆盖率 1.56%。覆盖率极低，与进程管理模块情况类似。
  4. 证据强度：**弱**。重合点如 `sys_rt_sigsuspend` 等，其内部调用图谱（目标项目操作 `ctx.task`，参考项目操作 `curr.task_ext().process_data()`）展示了完全不同的任务和信号抽象模型。
- **内存管理**:
  1. 双方均存在该模块。
  2. 存在 2 个共同 AST 指纹。
  3. 目标覆盖率 0.13%，参考覆盖率 0.65%。覆盖率可忽略不计。
  4. 证据强度：**未形成**。例如 `clone_task` 函数，目标项目调用图谱包含 `SchedAttr::fair`、`Task::new` 等，参考项目则包含 `CURRENT_DIR.deref_from`、`Arc::new` 等，外部对象和资源管理路径完全不同。
- **中断/异常处理**:
  1. 双方均存在该模块。
  2. 共同 AST 指纹为 0。
  3. 覆盖率为 0.0%。
  4. 证据强度：**未形成**。双方在此模块无任何可对齐的底层结构指纹。
- **系统调用**:
  1. 双方均存在该模块。
  2. 共同 AST 指纹为 0。
  3. 覆盖率为 0.0%。
  4. 证据强度：**未形成**。尽管存在大量同名系统调用入口，但其内部实现 AST 结构无重合，表明实现路径独立。
- **文件系统**:
  1. 双方均存在该模块。
  2. 存在 5 个共同 AST 指纹。
  3. 目标覆盖率 0.28%，参考覆盖率 1.03%。覆盖率极低。
  4. 证据强度：**弱**。例如 `rename` 函数，目标项目（`extfs`）调用 `copy_i_block`、`is_dir_empty` 等 ext 特定操作，参考项目（`fatfs`）则直接委托给底层 `self.0.rename`，逻辑层次和依赖对象截然不同。
- **设备驱动**:
  1. 双方均存在该模块。
  2. 存在 2 个共同 AST 指纹。
  3. 目标覆盖率 0.31%，参考覆盖率 1.59%。覆盖率极低。
  4. 证据强度：**弱**。例如 `probe` 函数，目标项目初始化 `EioIntc` 中断控制器，参考项目调用 `for_each_drivers!` 宏，两者面向的硬件和驱动模型完全不同。
- **网络协议栈**:
  1. 双方均存在该模块。
  2. 共同 AST 指纹为 0。
  3. 覆盖率为 0.0%。
  4. 证据强度：**未形成**。双方在此模块无任何可对齐的底层结构指纹。
- **第三方库/公共依赖**:
  1. 双方均存在该模块。
  2. 存在 3 个共同 AST 指纹，均位于 `网络协议栈/第三方库` 子模块。
  3. 目标覆盖率 0.16%，参考覆盖率 1.55%。覆盖率极低。
  4. 证据强度：**未形成**。重合点如 `connect`、`add` 等，均是对 `smoltcp` 或 `virtio-drivers` 等公共第三方库的标准 API 调用，是集成第三方库的必然结果，不反映项目本身的原创逻辑。

### 3. 底层 AST 关键证据指纹表
| 所属板块 | 目标项目路径 | 参考项目路径 | 证据类型 | 技术说明 |
| :--- | :--- | :--- | :--- | :--- |
| 进程管理 | `libs/sched/src/task.rs#clear_child_tid` | `AstrancE/ulib/axmono/src/task/process.rs#clear_child_tid` | 单节点原子操作 | 两者均为 `self.clear_child_tid.load`。这是 Linux `futex` 机制要求的标准原子读取操作，无独创性。 |
| 进程管理 | `libs/sched/src/task.rs#set_clear_child_tid` | `AstrancE/ulib/axmono/src/task/process.rs#set_clear_child_tid` | 单节点原子操作 | 两者均为 `self.clear_child_tid.store`。同上，是原子写操作的标准表达。 |
| 进程管理 | `libs/sched/src/wait.rs#new` | `AstrancE/modules/axtask/src/wait_queue.rs#new` | 标准容器初始化 | 目标为 `Spinlock::new, VecDeque::new`，参考为 `SpinNoIrq::new, VecDeque::new`。锁类型不同，且该组合是 Rust 并发编程的通行模式。 |
| 进程管理 | `libs/sched/src/wait.rs#len_hint` | `AstrancE/modules/axtask/src/wait_queue.rs#len` | 通用方法调用 | 目标调用 `self.waiters.lock().len()`，参考调用 `self.queue.lock().len()`。成员名和函数名均不同，语义可能有别（hint vs 精确）。 |
| 进程管理 | `libs/sched/src/task.rs#exit_code` | `AstrancE/modules/axtask/src/task.rs#exit_code` | 单节点原子操作 | 目标增加了 `has_exit_code` 状态判断，参考仅 `self.exit_code.load`。目标项目做出了独立的设计决策。 |
| 任务调度 | `kernel/src/syscalls/signal.rs#sys_rt_sigsuspend` | `AstrancE/ulib/axmono/src/task/signal.rs#sys_rt_sigsuspend` | 同名系统调用 | 目标操作 `ctx.task` 和 `SigSet::from_raw`，参考操作 `curr.task_ext().process_data().signal.lock`。任务和信号抽象模型完全不同。 |
| 内存管理 | `libs/sched/src/spawn.rs#clone_task` | `AstrancE/ulib/axmono/src/task/process.rs#clone_task` | 同名内核函数 | 目标调用图谱含 `SchedAttr::fair`、`Task::new`，参考含 `CURRENT_DIR.deref_from`、`Arc::new`。外部依赖对象和资源管理路径完全不同。 |
| 文件系统 | `libs/extfs/src/inode.rs#rename` | `AstrancE/modules/axfs/src/fs/fatfs.rs#rename` | 同名文件系统操作 | 目标实现 ext 特定逻辑（`copy_i_block`），参考直接委托给 `self.0.rename`。面向的具体文件系统类型和实现层次均不同。 |
| 设备驱动 | `general/src/dev/drivers/loongson_irq.rs#probe` | `AstrancE/modules/axdriver/src/lib.rs#probe` | 同名驱动函数 | 目标初始化 `EioIntc` 中断控制器，参考调用 `for_each_drivers!` 宏。面向的硬件平台和驱动模型完全不同。 |
| 第三方库 | `libs/mygo-smoltcp/src/socket/tcp.rs#connect` | `AstrancE/modules/axnet/src/smoltcp_impl/tcp.rs#connect` | 第三方库 API 调用 | 两者均是对 `smoltcp` 库 `tcp::Socket` 的 `connect` 方法的封装调用，是集成公共第三方库的必然结果。 |

### 4. 核心代码调用流对比剖析
* **【进程管理/任务退出与清理路径】**
* **底层技术析理**: 控方核心指控集中于 `clear_child_tid` 和 `set_clear_child_tid` 的原子操作。然而，这两个函数仅是庞大进程管理子系统中的两个叶子节点。从调用流来看，目标项目通过 `kernel/src/syscalls/process.rs` 中的 `sys_set_tid_address` 调用 `ctx.task().set_clear_child_tid`，其任务抽象为 `ctx.task()`，并通过 `sched::operation::gettid` 获取 tid。参考项目则在 `AstrancE/ulib/axmono/src/syscall/process.rs` 中通过 `current().task_ext().thread_data().set_clear_child_tid` 实现，其任务抽象为 `current().task_ext()`，tid 直接从 `thread.tid` 字段获取。两者在**任务模型**（`ctx.task` vs `task_ext`）、**同步策略**（`Spinlock` vs `SpinNoIrq`）和**子系统划分**上存在根本性架构差异。单点原子操作的重合是 Linux ABI 规范和 Rust 语言惯用法共同作用下的必然结果，无法掩盖上层调用链路的异构性。
* **代码溯源链接**:
  - 目标项目: `kernel/src/syscalls/process.rs#sys_set_tid_address` -> `libs/sched/src/task.rs#set_clear_child_tid`
  - 参考项目: `AstrancE/ulib/axmono/src/syscall/process.rs#sys_set_tid_address` -> `AstrancE/ulib/axmono/src/task/process.rs#set_clear_child_tid`

* **【内存管理/任务克隆路径】**
* **底层技术析理**: `clone_task` 是创建新任务的核心函数。目标项目 (`libs/sched/src/spawn.rs`) 的实现围绕 `SchedAttr::fair`、`Task::new` 等自有调度和任务对象展开，构建了一个完整的任务创建流程。参考项目 (`AstrancE/ulib/axmono/src/task/process.rs`) 的实现则深度依赖 `CURRENT_DIR` 的 `deref_from`、`share` 等方法，其核心在于文件系统命名空间的复制与共享。两者虽然函数名相同，但**内存模型**（任务结构体构成）和**资源克隆策略**（调度属性 vs 文件系统上下文）截然不同，表明它们是为完全不同的内核架构独立设计的。
* **代码溯源链接**:
  - 目标项目: `libs/sched/src/spawn.rs#clone_task`
  - 参考项目: `AstrancE/ulib/axmono/src/task/process.rs#clone_task`

### 5. 单方模块与降权项
* **单方模块**: `文件系统/第三方库` 模块仅在参考项目中体现，包含 `lwext4_rust` 等第三方 ext4 文件系统库的封装函数。目标项目无此模块，其文件系统实现（如 `extfs`）是独立编写的。此为双方项目在组件选型上的根本差异，不构成同源证据。
* **降权项说明**:
  * **跨板块同名函数**: 如 `parent`（目标在 ACPI 命名空间，参考在文件系统路径）、`wait`（目标在块设备完成量，参考在任务等待队列）等，因所属业务领域完全不同，其内部逻辑无任何可比性，已从主证据链中排除。
  * **POSIX 系统调用入口**: `sys_clone`、`sys_execve`、`sys_wait4`、`sys_getpid` 等大量同名函数，其名称由 Linux/POSIX ABI 标准强制规定。控辩双方证据均显示，这些函数在内部实现、错误处理、外部对象依赖上存在显著差异，同名是接口规范的必然要求，而非代码复用。
  * **第三方库/公共接口**: 网络协议栈中对 `smoltcp` 库的调用、设备驱动中对 `virtio-drivers` 的调用，以及 `unimplemented` 宏等，均属于对公共第三方库或标准语言特性的使用，不构成项目间的独创性逻辑复用。

### 6. 保守结论
综合全模块覆盖矩阵、底层 AST 指纹和核心调用流对比，目标项目 `[2026-T202610112999587-XX]` 与参考项目 `[2025-AstranciA-哈尔滨工业大学深圳]` 的客观 AST 相似度仅为 0.63%，远低于认定代码抄袭的合理阈值。控方提出的“复用指控”均聚焦于单条原子操作、标准容器初始化或 POSIX 系统调用入口等不具备独创性的底层节点，未能提供连续、成片的中层逻辑结构重合证据。相反，辩方论证的双方在任务抽象、同步原语、内存模型和子系统划分上的系统性架构差异，与客观调用图谱高度一致。因此，现有证据无法支持代码复用或抄袭的指控，应认定目标项目为独立开发，整体同源性风险极低。

------------------------------

