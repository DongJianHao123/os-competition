# 🛡️ OS 内核赛自动化代码级架构审计报告

**审计时间**: `2026-07-02 03:19:07`
**参赛作品名称**: `2026-T202610112999587-XX`
**年份**: `2026`
**学校**: `XX`
**队伍名称**: `T202610112999587`
**比赛名字**: `OS-Competition`
**比赛子赛道**: `Kernel-Implementation`
**仓库地址**: `https://gitlab.eduxiji.net/educg-group-43501-3132633/T202610112999587-3708`
**推测开发语言**: `Rust / C (由AST探针自动识别)`
**特征函数量**: `10776`

## ⚔️ 底层代码同源性对比 (AST Homology)
> 本部分由专门的代码审查智能体，对该项目与符合年份条件的历届最高相似度项目进行控制流与底层调用对比。新版报告会先给出确定性的全模块覆盖矩阵，再给出 Agent 的分模块解释，避免只围绕少量抽样函数展开。

### 🏆 Top 4 底层对标: vs `2023-Starry-清华大学`（清华大学 / Starry）

#### 对方作品信息

**对方年份**: `2023`
**对方学校**: `清华大学`
**对方队伍名称**: `Starry`
**对方比赛名字**: `OS-Competition`
**对方比赛子赛道**: `Kernel-Implementation`
**对方仓库地址**: `https://gitlab.eduxiji.net/202310003101649/starry`

> 双向覆盖率：共同 AST 指纹: 103；唯一 AST 指纹: 目标项目 8484，参考项目 6206；覆盖率: 目标项目 1.21%，参考项目 1.66%

## 全模块对比总览（确定性证据）

> 总体覆盖率：共同 AST 指纹: 103；唯一 AST 指纹: 目标项目 8484，参考项目 6206；覆盖率: 目标项目 1.21%，参考项目 1.66%

### 1. 模块覆盖矩阵
| 模块 | 目标函数数 | 参考函数数 | 共同唯一 AST 指纹 | Target 覆盖 | Base 覆盖 | 状态 |
| :--- | ---: | ---: | ---: | ---: | ---: | :--- |
| 进程管理 | 696 | 128 | 1 | 0.18% | 0.98% | 双方存在且有结构重合 |
| 任务调度/异步运行时 | 1070 | 296 | 3 | 0.35% | 1.24% | 双方存在且有结构重合 |
| 内存管理 | 1729 | 257 | 1 | 0.06% | 0.43% | 双方存在且有结构重合 |
| 中断/异常处理 | 226 | 40 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |
| 系统调用 | 37 | 64 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |
| 文件系统 | 2220 | 379 | 5 | 0.28% | 1.62% | 双方存在且有结构重合 |
| 设备驱动 | 808 | 57 | 1 | 0.16% | 1.96% | 双方存在且有结构重合 |
| 网络协议栈 | 439 | 73 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |
| 文件系统/第三方库 | 0 | 487 | 0 | 0.0% | 0.0% | 仅参考项目体现 |
| 网络协议栈/第三方库 | 2456 | 110 | 4 | 0.21% | 3.81% | 双方存在且有结构重合 |
| 第三方库 | 7 | 5822 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |
| 通用模块/其他 | 1088 | 341 | 6 | 0.68% | 2.12% | 双方存在且有结构重合 |

### 2. 分模块证据清单

#### 进程管理
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `libs/sched/src/operation.rs#exit` | `ulib/starry_libax/src/trap.rs#exit` | 同板块同名 | ExitCode, current_task, drop, exit_task, panic!, schedule_once | axlog::error!, current, current().id, current().id().as_u64, syscall_tkill |
| `libs/sched/src/operation.rs#getpid` | `ulib/c_libax/src/unistd.c#getpid` | 同板块同名 | current_task, l.pid_root, me.pid_root, me.thread_group, me.thread_group()
        .leader, me.thread_group()
        .leader()
        .and_then ...(+2) | ax_getpid |
| `general/src/dev/completion.rs#wait` | `modules/axtask/src/wait_queue.rs#wait` | 同板块同名 | sched::is_ready, self.wait_blocking, self.wait_spinning | RUN_QUEUE.lock, RUN_QUEUE.lock().block_current, crate::current, self.cancel_events, self.queue.lock, self.queue.lock().push_back ...(+1) |
| `libs/sched/src/task.rs#set_clear_child_tid` | `modules/axtask/src/monolithic_task/task.rs#set_clear_child_tid` | 同板块同名 | self.clear_child_tid.store | self.clear_child_tid.store |

#### 任务调度/异步运行时
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `libs/sched/src/mutex.rs#lock` | `ulib/libax/src/cbindings/pthread/mutex.rs#lock` | 同板块同名 | current_task, now_ns_public, schedule_once, self
                .locked
                .compare_exchange_weak, self
                .locked
                .compare_exchange_weak(false, true, Ordering::Acquire, Ordering::Relaxed)
                .is_ok, self.waiters.finish_wait ...(+1) | ManuallyDrop::new, Ok, self.0.lock |
| `libs/sched/src/rlimit.rs#name` | `ulib/libax/src/thread/mod.rs#name` | 同板块同名 | 内部闭环逻辑 | Some |
| `general/src/dev/drivers/goldfish_rtc.rs#create` | `ulib/libax/src/cbindings/pthread/mod.rs#create` | 同板块同名 | Arc::new, GoldfishRtcPlatformDriver::new, Ok | Arc::new, Box::into_raw, Box::new, ForceSendSync, Ok, TID_TO_PTHREAD.write ...(+10) |
| `libs/sched/src/scheduler.rs#on_timer_tick` | `modules/axtask/src/api.rs#on_timer_tick` | 同板块同名 | INIT_READY.load, RUNQUEUES[cpu_id].tick, cpu, fire_expired_realtime_itimers, request_resched, wake_expired_sleepers | RUN_QUEUE.lock, RUN_QUEUE.lock().scheduler_timer_tick, crate::timers::check_events |

#### 内存管理
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `libs/sched/src/spawn.rs#clone_task` | `modules/axprocess/src/process.rs#clone_task` | 同板块同名 | Arc::clone, Arc::downgrade, Arc::new, SchedAttr::fair, SignalNumber::SIGCHLD.raw, Task::new ...(+48) | Arc::clone, Arc::new, Err, FutexRobustList::default, MemorySet::clone, Ok ...(+65) |
| `libs/allocator/src/slab.rs#alloc` | `modules/axalloc/src/lib.rs#alloc` | 同板块同名 | CACHE_CAPACITY
                .saturating_sub, CACHE_CAPACITY
                .saturating_sub(cache_guard.count)
                .min, CacheEntry::empty, SlabAllocation::null, Some, allocate_slab_node ...(+17) | Ok, balloc.add_memory, balloc.alloc, balloc.total_bytes, debug!, old_size.max ...(+4) |
| `general/src/mm/vm_space.rs#mremap` | `ulib/c_libax/src/mmap.c#mremap` | 同板块同名 | Err, Ok, Self::collect_file_backings, Self::notify_file_unmapped, Self::notify_files_mapped, Some ...(+30) | unimplemented |
| `libs/allocator/src/lib.rs#realloc` | `ulib/c_libax/src/stdlib.c#realloc` | 同板块同名 | AllocationRecord::new, AllocationRecord::new(AllocationKind::Boot, MemoryDomain::Kernel, ptr as usize)
                        .with_sizes, AllocationRecord::new(AllocationKind::Boot, MemoryDomain::Kernel, ptr as usize)
                    .with_sizes, Layout::from_size_align, Some, core::ptr::copy_nonoverlapping ...(+19) | ax_free, ax_malloc, malloc |

#### 中断/异常处理
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `general/src/dev/drivers/loongson_irq.rs#translate_source` | `ulib/libax/src/cbindings/time.rs#ax_nanosleep` | 同模块上下文对齐/差异观察 | Some, drop, entry.is_some_and, inner
                .slots
                .iter, inner
                .slots
                .iter()
                .enumerate, inner
                .slots
                .iter()
                .enumerate()
                .find ...(+13) | ax_call_body! |
| `libs/errno/src/lib.rs#from_i32` | `modules/axhal/src/arch/x86_64/idt.rs#new` | 同模块上下文对齐/差异观察 | Errno::Other | InterruptDescriptorTable::new, core::mem::transmute, core::slice::from_raw_parts_mut, entries[i].set_handler_fn |
| `libs/errno/src/lib.rs#as_i32` | `modules/axhal/src/platform/qemu_virt_riscv/irq.rs#register_handler` | 同模块上下文对齐/差异观察 | 内部闭环逻辑 | with_cause! |
| `kernel/src/dtb/pci.rs#install_irq_routing` | `modules/axhal/src/irq.rs#register_handler_common` | 同模块上下文对齐/差异观察 | PCI_IRQ_ROUTING.lock, Some, Vec::new, child_key.extend_from_slice, child_key.into_boxed_slice, entry.child_address.len ...(+10) | IRQ_HANDLER_TABLE.register_handler, set_enable, warn! |

#### 系统调用
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `kernel/src/syscalls/ipc.rs#encode_shmid64_ds` | `ulib/starry_libax/src/syscall/socket.rs#get` | 同模块上下文对齐/差异观察 | meta.perm.mode.bits, write_i32, write_i64, write_u16, write_u32, write_u64 | copy_nonoverlapping, keep_alive.to_ne_bytes, panic!, s.keep_alive, s.keep_alive().is_some, s.with_socket ...(+4) |
| `kernel/src/syscalls/signal.rs#read_stack_t` | `ulib/starry_libax/src/syscall/socket.rs#set` | 同模块上下文对齐/差异观察 | Err, Ok, SigAltStack::disabled, copy_from_user, copy_from_user(user, &mut raw).map_err, e.as_errno ...(+10) | <[u8; 4]>::try_from, <[u8; 4]>::try_from(&opt[0..4]).unwrap, Some, axnet::Duration::from_secs, i32::from_ne_bytes, opt.as_ptr ...(+6) |
| `hal/src/user.rs#patch_loongarch64_enosys_stub` | `ulib/starry_libax/src/syscall/socket.rs#get` | 同模块上下文对齐/差异观察 | ENOSYS_STUB_PREFIX.len, bytes.get, bytes[off..off + patch.len()].copy_from_slice, patch.len, write_u32 | bytes.as_ptr, bytes.len, copy_nonoverlapping, panic!, raw_socket.congestion.as_bytes, size_of::<usize> ...(+2) |
| `kernel/src/syscalls/syslog.rs#set_console_level` | `ulib/starry_libax/src/syscall/socket.rs#accept` | 同模块上下文对齐/差异观察 | (1..=8).contains, Err, Ok, log::set_console_level, unreachable! | Err, Ok, SocketInner::Tcp, String::from, new_socket.peer_addr, s.accept ...(+2) |

#### 文件系统
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `general/src/vfs/device_files/rtc.rs#ioctl` | `modules/axfs/src/monolithic_fs/file_io.rs#ioctl` | 同板块同名 | Err, Ok, RtcControlRequest::SetAlarm, RtcControlRequest::SetAlarmIrqEnabled, RtcControlRequest::SetEpoch, RtcControlRequest::SetPeriodicIrqEnabled ...(+23) | Err |
| `libs/extfs/src/file.rs#read_at` | `modules/axfs/src/fs/fatfs.rs#read_at` | 同板块同名 | (offset + remaining as u64).min, (size - offset).min, (start + remaining).min, Ok, Vec::new, buf.is_empty ...(+25) | Ok, SeekFrom::Start, ans.is_err, ans.unwrap, buf.len, buf.to_vec ...(+7) |
| `general/src/dev/block.rs#control` | `ulib/libax/src/cbindings/io_mpx/epoll.rs#control` | 同板块同名 | BlockControlResponse::Bool, BlockControlResponse::IoHints, BlockControlResponse::U32, Err, Ok, blocks.get ...(+23) | Err, Ok, e.insert, events.entry, get_file_like, ocp.insert ...(+3) |
| `general/src/vfs/devtmpfs.rs#mount` | `modules/axfs/src/root.rs#mount` | 同板块同名 | Arc::clone, Arc::new, AtomicU64::new, Box::new, DEVTMPFS_INSTANCE_COUNTER.fetch_add, Dentry::new_positive ...(+19) | MountPoint::new, Ok, ax_err!, fs.mount, path.starts_with, self.main_fs.root_dir ...(+5) |

#### 设备驱动
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `general/src/dev/drivers/loongson_irq.rs#probe` | `modules/axdriver/src/lib.rs#probe` | 同板块同名 | Arc::clone, Arc::new, EioIntc::new, EioIntcRouteConfig::from_platform, EioIntcRouteConfig::from_platform(parent_hwi, info).ok_or, Err ...(+19) | for_each_drivers! |
| `general/src/dev/function.rs#push` | `modules/axdriver/src/structs/static.rs#push` | 同板块同名 | Arc::clone, Err, Ok, Vec::new, core::mem::replace, drop ...(+22) | Some, self.0.is_none |
| `general/src/dev/drivers/random.rs#deref` | `modules/axdriver/src/structs/dyn.rs#deref` | 同板块同名 | self.lock.state.get | 内部闭环逻辑 |
| `general/src/dev/virtio.rs#is_empty` | `modules/axdriver/src/structs/dyn.rs#is_empty` | 同板块同名同结构 | self.len | self.len |

#### 网络协议栈
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `libs/socket/src/state.rs#send` | `ulib/libax/src/cbindings/socket.rs#send` | 同板块同名 | Err, effective_send_deadline, io::send_datagram, io::send_seqpacket, io::send_stream, options.sender_identity.unwrap_or ...(+2) | Ok, tcpsocket.lock, tcpsocket.lock().send, udpsocket.lock, udpsocket.lock().send |
| `libs/socket/src/state.rs#accept` | `ulib/libax/src/cbindings/socket.rs#accept` | 同板块同名 | Err, connection::accept_connection_socket, effective_recv_deadline, self.finish_result | Err, Ok, tcpsocket.lock, tcpsocket.lock().accept |
| `libs/socket/src/state.rs#connect` | `ulib/libax/src/cbindings/socket.rs#connect` | 同板块同名 | connection::connect_datagram, connection::connect_seqpacket, connection::connect_stream, effective_send_deadline, self.finish_result | Ok, tcpsocket.lock, tcpsocket.lock().connect, udpsocket.lock, udpsocket.lock().connect |
| `libs/socket/src/state.rs#bind` | `ulib/libax/src/cbindings/socket.rs#bind` | 同板块同名 | Err, Ok, Some, address.binding_key, address.binding_key().ok_or, current.is_some ...(+3) | Ok, tcpsocket.lock, tcpsocket.lock().bind, udpsocket.lock, udpsocket.lock().bind |

#### 文件系统/第三方库
仅参考项目中观察到该模块代表函数，目标项目没有同模块可对齐证据：
- 参考项目：`extern_crates/libz-sys-1.1.8/src/zlib/contrib/minizip/zip.c#LoadCentralDirectoryRecord`，调用摘要：ALLOC, TRYFREE, ZCLOSE64, ZREAD64, ZSEEK64, add_data_in_datablock ...(+5)
- 参考项目：`extern_crates/rust-fatfs-a3a834e/src/fs.rs#format_volume`，调用摘要：DirFileEntryData::new, Err, Ok, SeekFrom::End, SeekFrom::Start, alloc_cluster ...(+28)
- 参考项目：`extern_crates/rust-fatfs-a3a834e/src/table.rs#test_fat`，调用摘要：ClusterIterator::<&mut S, S::Error, S>::new, [0xA_u32, 0x14_u32, 0x15_u32, 0x16_u32, 0x19_u32, 0x1A_u32]
                .iter, [0xA_u32, 0x14_u32, 0x15_u32, 0x16_u32, 0x19_u32, 0x1A_u32]
                .iter()
                .cloned, [0xA_u32, 0x14_u32, 0x15_u32, 0x16_u32, 0x19_u32, 0x1A_u32]
                .iter()
                .cloned()
                .map, [0xA_u32, 0x14_u32, 0x15_u32, 0x16_u32, 0x19_u32, 0x1A_u32]
                .iter()
                .cloned()
                .map(Some)
                .collect::<Vec<_>>, assert! ...(+3)
- 参考项目：`extern_crates/rust-fatfs-a3a834e/src/boot_sector.rs#format_bpb`，调用摘要：Err, FatType::from_clusters, Ok, assert!, bpb.total_clusters, debug_assert! ...(+15)

#### 网络协议栈/第三方库
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `libs/mygo-smoltcp/src/storage/assembler.rs#add` | `modules/axnet/src/smoltcp_impl/mod.rs#add` | 同板块同名 | Contig::hole_and_data, Err, Ok, contig.has_data, contig.shrink_hole_to, contig.total_size ...(+11) | debug!, self.0.lock, self.0.lock().add |
| `libs/mygo-smoltcp/src/socket/tcp.rs#connect` | `modules/axnet/src/smoltcp_impl/tcp.rs#connect` | 同板块同名 | Err, Ok, Self::random_seq_no, Some, addr.is_unspecified, cx
                    .get_source_address ...(+9) | Err, LOOPBACK.try_get, LOOPBACK.try_get().unwrap, Ok, SOCKET_SET.add, SOCKET_SET.with_socket_mut::<tcp::Socket, _, _> ...(+27) |
| `libs/mygo-smoltcp/src/phy/fault_injector.rs#receive` | `modules/axnet/src/smoltcp_impl/mod.rs#receive` | 同板块同名 | <D::RxToken<'_> as phy::RxToken>::meta, Some, buffer.len, net_trace!, self.inner.receive, self.rx_buf.len ...(+5) | AxNetRxToken, AxNetTxToken, Some, dev.can_transmit, dev.receive, dev.recycle_tx_buffers ...(+3) |
| `libs/mygo-smoltcp/src/socket/tcp.rs#listen` | `modules/axnet/src/smoltcp_impl/tcp.rs#listen` | 同板块同名 | Err, Ok, local_endpoint.into, matches!, self.is_open, self.reset ...(+1) | LISTEN_TABLE.listen, Ok, debug!, self.bound_addr, self.local_addr.get, self.update_state ...(+1) |

#### 第三方库
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `libs/efi/src/c/efi_global.c#efi_system_table_firmware_vendor` | `extern_crates/libz-sys-1.1.8/src/zlib/contrib/inflate86/inffas86.c#inflate_fast` | 同模块上下文对齐/差异观察 | efi_system_table_is_valid | 内部闭环逻辑 |
| `libs/efi/src/lib.rs#firmware_vendor` | `extern_crates/libz-sys-1.1.8/src/zlib-ng/inflate.c#PREFIX` | 同模块上下文对齐/差异观察 | Some, core::ptr::null, core::slice::from_raw_parts, efi_system_table_firmware_vendor, ptr.is_null | BITS, BYTEBITS, CRC2, CRC4, DROPBITS, INFLATE_MARK_HOOK ...(+38) |
| `libs/efi/src/lib.rs#firmware_vendor_cstr16` | `extern_crates/miniz_oxide-0.6.2/src/inflate/core.rs#decompress` | 同模块上下文对齐/差异观察 | firmware_vendor | OutputBuffer::from_slice_and_pos, cfg!, generate_state!, in_buf.iter, in_buf.len, in_iter.len ...(+9) |
| `general/src/vfs/procfs.rs#cpuinfo_vendor_from_compatible` | `extern_crates/cc-1.0.79/src/lib.rs#add_default_flags` | 同模块上下文对齐/差异观察 | compatible
        .split_once, compatible
        .split_once(',')
        .map, compatible
        .split_once(',')
        .map(|(vendor, _)| vendor)
        .unwrap_or | "--target=aarch64-unknown-windows-gnu".into, "--target=i686-unknown-windows-gnu".into, "--target=x86_64-unknown-windows-gnu".into, "-Brepro".into, "-DANDROID".into, "-D_ARM_WINAPI_PARTITION_DESKTOP_SDK_AVAILABLE=1".into ...(+103) |

#### 通用模块/其他
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `libs/log/src/lib.rs#write_str` | `modules/axlog/src/lib.rs#write_str` | 同板块同名 | Ok, ch.len_utf8, s.as_bytes, s.char_indices, s.len, self.buf[self.len..self.len + copy_len].copy_from_slice | Ok, cfg_if::cfg_if! |
| `arch/src/loongarch64/loader.rs#time` | `ulib/c_libax/src/time.c#time` | 同板块同名 | STABLE_TIMER_HZ.load, core::arch::asm! | ax_clock_gettime |
| `libs/acpi/src/aml/object.rs#deref` | `modules/axsync/src/mutex.rs#deref` | 同板块同名 | self.0.get | 内部闭环逻辑 |
| `general/src/dev/loopdev.rs#flush` | `ulib/libax/src/io/stdio.rs#flush` | 同板块同名 | backing.sync, backing.sync().map_err, self.begin_flush, self.begin_flush().map_err, self.finish_io | self.inner.lock, self.inner.lock().flush |


#### 1. 宏观判定
- **定性结论**: 底层架构显著差异 / 独立开发 (相似度: 0.71%，共同 AST 指纹: 103；唯一 AST 指纹: 目标项目 8484，参考项目 6206；覆盖率: 目标项目 1.21%，参考项目 1.66%)

#### 2. 全模块覆盖矩阵解读
- **进程管理**: 双方均存在该模块。存在 1 个共同 AST 指纹。目标覆盖率 0.18%，参考覆盖率 0.98%，表明双方在数百个函数中仅有极个别结构重合，远低于系统性复用阈值。证据强度：**弱**。
- **任务调度/异步运行时**: 双方均存在该模块。存在 3 个共同 AST 指纹。目标覆盖率 0.35%，参考覆盖率 1.24%，重合度极低。证据强度：**弱**。
- **内存管理**: 双方均存在该模块。存在 1 个共同 AST 指纹。目标覆盖率 0.06%，参考覆盖率 0.43%，几乎无结构重合。证据强度：**弱**。
- **中断/异常处理**: 双方均存在该模块。共同 AST 指纹为 0，覆盖率均为 0.0%，未观察到任何结构重合。证据强度：**未形成**。
- **系统调用**: 双方均存在该模块。共同 AST 指纹为 0，覆盖率均为 0.0%，未观察到任何结构重合。证据强度：**未形成**。
- **文件系统**: 双方均存在该模块。存在 5 个共同 AST 指纹。目标覆盖率 0.28%，参考覆盖率 1.62%，重合度极低。证据强度：**弱**。
- **设备驱动**: 双方均存在该模块。存在 1 个共同 AST 指纹。目标覆盖率 0.16%，参考覆盖率 1.96%，重合度极低。证据强度：**弱**。
- **网络协议栈**: 双方均存在该模块。共同 AST 指纹为 0，覆盖率均为 0.0%，未观察到任何结构重合。证据强度：**未形成**。
- **第三方库/公共依赖**: 双方均存在该模块。共同 AST 指纹为 0，覆盖率均为 0.0%，未观察到任何结构重合。证据强度：**未形成**。

#### 3. 底层 AST 关键证据指纹表
| 所属板块 | 目标项目路径 | 参考项目路径 | 证据类型 | 技术说明 |
| :--- | :--- | :--- | :--- | :--- |
| 进程管理 | `libs/sched/src/task.rs#set_clear_child_tid` | `modules/axtask/src/monolithic_task/task.rs#set_clear_child_tid` | 同板块同名 | 均调用 `self.clear_child_tid.store`。此为 Linux `CLONE_CHILD_CLEARTID` 语义的标准实现，属于功能决定表达，不构成独创性复用。 |
| 进程管理 | `libs/sched/src/wait.rs#new` | `modules/axtask/src/wait_queue.rs#new` | 同板块同名同结构 | 均调用 `VecDeque::new` 初始化内部队列。`VecDeque` 是 Rust 标准库中实现 FIFO 等待队列的惯用选择，属于通用数据结构调用。 |
| 进程管理 | `libs/sched/src/operation.rs#exit` | `ulib/starry_libax/src/trap.rs#exit` | 同板块同名 | 目标调用 `exit_task, schedule_once`，参考调用 `syscall_tkill, axlog::error!`。两者外部调用对象完全不同，仅函数名相同。 |
| 进程管理 | `libs/sched/src/operation.rs#getpid` | `ulib/c_libax/src/unistd.c#getpid` | 同板块同名 | 目标通过 `thread_group().leader().pid_root()` 链式调用获取 PID，参考直接调用 `ax_getpid`。实现路径和抽象层次完全不同。 |
| 任务调度 | `libs/sched/src/mutex.rs#lock` | `ulib/libax/src/cbindings/pthread/mutex.rs#lock` | 同板块同名 | 目标使用 `compare_exchange_weak` 自旋，参考使用 `self.0.lock`。底层同步原语和锁策略不同。 |
| 任务调度 | `libs/sched/src/scheduler.rs#on_timer_tick` | `modules/axtask/src/api.rs#on_timer_tick` | 同板块同名 | 目标调用 `RUNQUEUES[cpu_id].tick`，参考调用 `RUN_QUEUE.lock().scheduler_timer_tick`。调度器数据结构与锁粒度不同。 |
| 内存管理 | `libs/sched/src/spawn.rs#clone_task` | `modules/axprocess/src/process.rs#clone_task` | 同板块同名 | 目标调用 `SchedAttr::fair, Task::new`，参考调用 `MemorySet::clone, FutexRobustList::default`。克隆流程与资源管理模型不同。 |
| 文件系统 | `general/src/vfs/device_files/rtc.rs#ioctl` | `modules/axfs/src/monolithic_fs/file_io.rs#ioctl` | 同板块同名 | 目标处理 `RtcControlRequest::SetAlarm` 等多种 RTC 命令，参考直接返回 `Err`。功能复杂度完全不同。 |
| 文件系统 | `libs/extfs/src/file.rs#read_at` | `modules/axfs/src/fs/fatfs.rs#read_at` | 同板块同名 | 目标实现 ext 文件系统读取，参考实现 FAT 文件系统读取。底层文件系统语义和实现逻辑不同。 |
| 设备驱动 | `general/src/dev/drivers/loongson_irq.rs#probe` | `modules/axdriver/src/lib.rs#probe` | 同板块同名 | 目标初始化龙芯中断控制器，参考使用 `for_each_drivers!` 宏。驱动模型和硬件平台完全不同。 |
| 网络协议栈/第三方库 | `libs/mygo-smoltcp/src/socket/tcp.rs#connect` | `modules/axnet/src/smoltcp_impl/tcp.rs#connect` | 同板块同名 | 目标调用 `Self::random_seq_no`，参考调用 `SOCKET_SET.add`。TCP 连接管理的外部对象和状态机实现不同。 |
| 网络协议栈/第三方库 | `libs/mygo-smoltcp/src/socket/tcp.rs#listen` | `modules/axnet/src/smoltcp_impl/tcp.rs#listen` | 同板块同名 | 目标调用 `self.reset`，参考调用 `LISTEN_TABLE.listen`。监听状态的内部管理机制不同。 |

#### 4. 核心代码调用流对比剖析
* **【进程管理/exit】**
* **底层技术析理**: 目标项目 `exit` 函数调用 `exit_task` 进行任务资源回收，随后调用 `schedule_once` 触发调度；参考项目 `exit` 函数调用 `syscall_tkill` 向自身发送终止信号，并通过 `axlog::error!` 记录日志。两者在进程终止的底层机制上存在根本差异：目标采用“直接回收+主动调度”模型，参考采用“信号驱动”模型。
* **代码溯源链接**:
  - 目标项目: `libs/sched/src/operation.rs#exit`
  - 参考项目: `ulib/starry_libax/src/trap.rs#exit`

* **【任务调度/on_timer_tick】**
* **底层技术析理**: 目标项目 `on_timer_tick` 直接访问 per-CPU 的 `RUNQUEUES` 数组并调用其 `tick` 方法，同时处理实时定时器；参考项目通过 `RUN_QUEUE.lock()` 获取全局锁后再调用 `scheduler_timer_tick`。这揭示了调度器架构的核心差异：目标采用“无锁 per-CPU 数据”设计，参考采用“全局锁”设计。
* **代码溯源链接**:
  - 目标项目: `libs/sched/src/scheduler.rs#on_timer_tick`
  - 参考项目: `modules/axtask/src/api.rs#on_timer_tick`

* **【内存管理/clone_task】**
* **底层技术析理**: 目标项目 `clone_task` 的核心是构造 `SchedAttr::fair` 调度属性并调用 `Task::new` 创建内核任务对象；参考项目 `clone_task` 的核心是调用 `MemorySet::clone` 复制地址空间并初始化 `FutexRobustList`。两者在资源克隆的侧重点上完全不同：目标侧重调度上下文，参考侧重内存上下文。
* **代码溯源链接**:
  - 目标项目: `libs/sched/src/spawn.rs#clone_task`
  - 参考项目: `modules/axprocess/src/process.rs#clone_task`

* **【文件系统/read_at】**
* **底层技术析理**: 目标项目 `read_at` 实现的是 ext 系列文件系统的块读取逻辑，涉及 `(offset + remaining as u64).min` 等块边界计算；参考项目 `read_at` 实现的是 FAT 文件系统的读取，使用 `SeekFrom::Start` 进行定位。两者服务于完全不同的文件系统，其内部数据结构和寻址逻辑无任何重合。
* **代码溯源链接**:
  - 目标项目: `libs/extfs/src/file.rs#read_at`
  - 参考项目: `modules/axfs/src/fs/fatfs.rs#read_at`

#### 5. 单方模块与降权项
- **目标项目独有模块/功能**:
  - `libs/acpi/src/aml/mod.rs#do_execute_method`: ACPI AML 方法执行引擎，参考项目无 ACPI 子系统。
  - `general/src/vfs/sysfs.rs#lookup_child`: sysfs CPU 拓扑节点，参考项目无对应实现。
  - `kernel/src/syscalls/process.rs#sys_mygo_sched_info`: 自定义调度信息 syscall，参考项目无此接口。
  - `general/src/vfs/procfs.rs#render_pnp`: 即插即用设备信息渲染，参考项目无 PNP 子系统。
  - `kernel/src/syscalls/process.rs#exit_robust_list`: futex robust list 清理，参考项目无 robust list 支持。
- **参考项目独有模块/功能**:
  - `modules/axprocess/src/link.rs`: 动态链接路径映射，目标项目无此机制。
  - `extern_crates/rust-fatfs-a3a834e/`: FAT 文件系统实现，目标项目使用自研 extfs。
  - `extern_crates/libz-sys-1.1.8/`: zlib 压缩库，目标项目未集成。
- **降权项说明**:
  - 跨板块同名函数（如 `new`, `ioctl`, `read`, `push`, `deref`, `is_empty` 等）属于 Rust 标准 trait 方法或领域通用术语，其出现是语言规范和操作系统惯例使然，不构成同源证据。
  - 第三方库/公共依赖（如 smoltcp 协议栈）的相似调用源于对同一上游开源项目的分叉或集成，属于公共依赖，不能作为项目间抄袭的证据。

#### 6. 保守结论
本次审计基于 0.71% 的 AST 相似度及最高仅 1.66% 的模块覆盖率，认定目标项目与参考项目在底层架构上存在显著差异。控方所指控的 `set_clear_child_tid`、等待队列初始化等相似片段，经查均属 Linux ABI 兼容性要求或 Rust 标准库惯用法所决定的必然实现，不构成独创性表达的复用。双方在调度器设计（per-CPU 无锁 vs 全局锁）、进程模型、文件系统实现及硬件平台支持上展现出完全独立的架构选择。目标项目包含大量参考项目不具备的子系统（ACPI、sysfs、PNP、robust list 等），进一步佐证其独立开发性质。综合判定，现有证据无法支持“实质性相似”或“未授权复用”的指控，该项目原创性风险极低。

------------------------------

