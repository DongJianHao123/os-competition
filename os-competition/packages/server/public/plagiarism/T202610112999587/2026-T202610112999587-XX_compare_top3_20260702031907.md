# 🛡️ OS 内核赛自动化代码级架构审计报告

**审计时间**: `2026-07-02 03:19:07`
**参赛作品名称**: `2026-T202610112999587-XX`
**年份**: `2026`
**学校**: `XX`
**队伍名称**: `T202610112999587`
**比赛名字**: `OS-Competition`
**比赛子赛道**: `Kernel-Implementation`
**仓库地址**: `https://gitlab.eduxiji.net/educg-group-43501-3132633/T202610112999587-3708`
**推测开发语言**: `Rust / C (由AST探针自动识别)`
**特征函数量**: `10776`

## ⚔️ 底层代码同源性对比 (AST Homology)
> 本部分由专门的代码审查智能体，对该项目与符合年份条件的历届最高相似度项目进行控制流与底层调用对比。新版报告会先给出确定性的全模块覆盖矩阵，再给出 Agent 的分模块解释，避免只围绕少量抽样函数展开。

### 🏆 Top 3 底层对标: vs `2025-StarryX-杭州电子科技大学`（杭州电子科技大学 / StarryX）

#### 对方作品信息

**对方年份**: `2025`
**对方学校**: `杭州电子科技大学`
**对方队伍名称**: `StarryX`
**对方比赛名字**: `OS-Competition`
**对方比赛子赛道**: `Kernel-Implementation`
**对方仓库地址**: `https://gitlab.eduxiji.net/educg-group-36002-2710490/T202510336995486-3037`

> 双向覆盖率：共同 AST 指纹: 82；唯一 AST 指纹: 目标项目 8484，参考项目 2897；覆盖率: 目标项目 0.97%，参考项目 2.83%

## 全模块对比总览（确定性证据）

> 总体覆盖率：共同 AST 指纹: 82；唯一 AST 指纹: 目标项目 8484，参考项目 2897；覆盖率: 目标项目 0.97%，参考项目 2.83%

### 1. 模块覆盖矩阵
| 模块 | 目标函数数 | 参考函数数 | 共同唯一 AST 指纹 | Target 覆盖 | Base 覆盖 | 状态 |
| :--- | ---: | ---: | ---: | ---: | ---: | :--- |
| 进程管理 | 696 | 172 | 7 | 1.24% | 4.4% | 双方存在且有结构重合 |
| 任务调度/异步运行时 | 1070 | 480 | 4 | 0.47% | 1.14% | 双方存在且有结构重合 |
| 内存管理 | 1729 | 430 | 6 | 0.39% | 1.54% | 双方存在且有结构重合 |
| 中断/异常处理 | 226 | 70 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |
| 系统调用 | 37 | 89 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |
| 文件系统 | 2220 | 801 | 6 | 0.34% | 0.97% | 双方存在且有结构重合 |
| 设备驱动 | 808 | 139 | 2 | 0.31% | 1.59% | 双方存在且有结构重合 |
| 网络协议栈 | 439 | 77 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |
| 文件系统/第三方库 | 0 | 164 | 0 | 0.0% | 0.0% | 仅参考项目体现 |
| 网络协议栈/第三方库 | 2456 | 166 | 5 | 0.27% | 3.47% | 双方存在且有结构重合 |
| 第三方库 | 7 | 349 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |
| 通用模块/其他 | 1088 | 665 | 10 | 1.13% | 1.89% | 双方存在且有结构重合 |

### 2. 分模块证据清单

#### 进程管理
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `general/src/vfs/procfs.rs#lookup` | `xcore/src/fs/vfs/proc/pid.rs#lookup` | 同板块同名 | Err, Ok, proc_fd_dir_inode, proc_task_file_inode, proc_task_link_inode, proc_task_list_dir_inode | VirtNodeOps::Dir, create_tid_root, get_task_by_id, get_task_by_id(tid.into())
            .and_then, get_task_by_id(tid.into())
            .and_then(|weak| weak.upgrade())
            .map, name.parse::<u32> ...(+5) |
| `kernel/src/syscalls/signal.rs#sys_rt_sigtimedwait` | `xapi/src/task/signal.rs#sys_rt_sigtimedwait` | 同板块同名 | (sec as u64)
                .saturating_mul, (sec as u64)
                .saturating_mul(1_000_000_000)
                .saturating_add, Err, Ok, SigSet::from_raw, Some ...(+15) | Err, Ok, XProcess::from_thread, XProcess::from_thread(thread).uspace, XThread::from_thread, check_sigset_size ...(+6) |
| `kernel/src/syscalls/process.rs#exit_robust_list` | `xapi/src/task/futex.rs#exit_robust_list` | 同板块同名 | Vec::new, handle_robust_node, log::debug!, log::warning!, read_robust_isize, read_robust_usize ...(+10) | Err, Ok, UserPtr::from, axtask::yield_now, core::ptr::eq, handle_futex_death ...(+2) |
| `kernel/src/syscalls/signal.rs#sys_rt_sigprocmask` | `xapi/src/task/signal.rs#sys_rt_sigprocmask` | 同板块同名 | Err, Ok, SigSet::from_raw, copy_from_user, copy_from_user(set_user, &mut raw).map_err, copy_to_user ...(+8) | Err, Ok, XProcess::from_thread, XProcess::from_thread(thread).uspace, XThread::from_thread, check_sigset_size ...(+3) |

#### 任务调度/异步运行时
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `kernel/src/syscalls/fs.rs#sys_pselect6` | `xapi/src/iomux/select.rs#sys_pselect6` | 同板块同名 | Err, Fd::from_raw, Ok, PollEvents::POLLIN
                        .with, PollEvents::POLLIN
                        .with(PollEvents::POLLHUP)
                        .with, PollEvents::POLLOUT.with ...(+31) | do_select, nullable!, nullable!(uspace.read(timeout))?
                .map, nullable!(uspace.read(timeout))?
                .map(timespec::to_time_value)
                .transpose, with_uspace |
| `kernel/src/syscalls/signal.rs#sys_rt_sigsuspend` | `xapi/src/task/signal.rs#sys_rt_sigsuspend` | 同板块同名 | Err, SigSet::from_raw, copy_from_user, copy_from_user(set_user, &mut raw).map_err, ctx
            .task, ctx
            .task()
            .cas_state ...(+8) | Err, Some, check_signals, check_sigset_size, mem::replace, set.remove ...(+7) |
| `libs/sched/src/mutex.rs#lock` | `arceos/ulib/axstd/src/sync/mutex.rs#lock` | 同板块同名 | current_task, now_ns_public, schedule_once, self
                .locked
                .compare_exchange_weak, self
                .locked
                .compare_exchange_weak(false, true, Ordering::Acquire, Ordering::Relaxed)
                .is_ok, self.waiters.finish_wait ...(+1) | api::ax_current_task_id, api::ax_wait_queue_wait_until, assert_ne!, self.data.get, self.is_locked, self.owner_id.compare_exchange_weak |
| `kernel/src/syscalls/fs.rs#sys_fchdir` | `xapi/src/fs/ctl.rs#sys_fchdir` | 同板块同名 | Dirfd::Fd, Err, FileMode::new, Gid, Ok, Uid ...(+15) | Directory::from_fd, Directory::from_fd(fd, FileFlags::empty(), FileFlags::PATH)?
        .inner, Directory::from_fd(fd, FileFlags::empty(), FileFlags::PATH)?
        .inner()
        .clone, FS_CONTEXT.lock, FS_CONTEXT.lock().set_current_dir, FileFlags::empty ...(+2) |

#### 内存管理
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `libs/extfs/src/file.rs#write_at` | `xmodules/xcache/src/lib.rs#write_at` | 同板块同名 | ((block_size - in_block as u64) as usize).min, (old_blocks_lo as u64 + added_sectors).min, Err, Ok, Some, Vec::new ...(+58) | (buf.len() - buf_offset).min, Ok, P::read_page, P::write_page, buf.is_empty, buf.len ...(+10) |
| `libs/allocator/src/slab.rs#alloc` | `arceos/modules/axalloc/src/lib.rs#alloc` | 同板块同名 | CACHE_CAPACITY
                .saturating_sub, CACHE_CAPACITY
                .saturating_sub(cache_guard.count)
                .min, CacheEntry::empty, SlabAllocation::null, Some, allocate_slab_node ...(+17) | Err, Ok, PAGE_SIZE.max, balloc.add_memory, balloc.alloc, balloc.total_bytes ...(+7) |
| `general/src/mm/vm_space.rs#mremap` | `arceos/ulib/axlibc/c/mmap.c#mremap` | 同板块同名 | Err, Ok, Self::collect_file_backings, Self::notify_file_unmapped, Self::notify_files_mapped, Some ...(+30) | unimplemented |
| `libs/allocator/src/lib.rs#realloc` | `arceos/ulib/axlibc/c/stdlib.c#realloc` | 同板块同名 | AllocationRecord::new, AllocationRecord::new(AllocationKind::Boot, MemoryDomain::Kernel, ptr as usize)
                        .with_sizes, AllocationRecord::new(AllocationKind::Boot, MemoryDomain::Kernel, ptr as usize)
                    .with_sizes, Layout::from_size_align, Some, core::ptr::copy_nonoverlapping ...(+19) | free, malloc |

#### 中断/异常处理
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `general/src/dev/drivers/loongson_irq.rs#translate_source` | `arceos/ulib/axlibc/c/pow.c#exp_inline` | 同模块上下文对齐/差异观察 | Some, drop, entry.is_some_and, inner
                .slots
                .iter, inner
                .slots
                .iter()
                .enumerate, inner
                .slots
                .iter()
                .enumerate()
                .find ...(+13) | __math_oflow, __math_uflow, asdouble, asuint64, converttoint, eval_as_double ...(+4) |
| `libs/errno/src/lib.rs#from_i32` | `xmodules/xsignal/src/types.rs#default_action` | 同模块上下文对齐/差异观察 | Errno::Other | 内部闭环逻辑 |
| `libs/errno/src/lib.rs#as_i32` | `arceos/modules/axhal/src/arch/aarch64/trap.rs#handle_sync_exception` | 同模块上下文对齐/差异观察 | 内部闭环逻辑 | ESR_EL1.extract, crate::trap::handle_syscall, crate::trap::post_trap_callback, debug!, esr.read, esr.read_as_enum ...(+6) |
| `kernel/src/dtb/pci.rs#install_irq_routing` | `xcore/src/fs/vfs/dev/tty.rs#default_termios` | 同模块上下文对齐/差异观察 | PCI_IRQ_ROUTING.lock, Some, Vec::new, child_key.extend_from_slice, child_key.into_boxed_slice, entry.child_address.len ...(+10) | 内部闭环逻辑 |

#### 系统调用
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `kernel/src/syscalls/ipc.rs#encode_shmid64_ds` | `arceos/api/arceos_posix_api/src/imp/net.rs#sys_getaddrinfo` | 同模块上下文对齐/差异观察 | meta.perm.mode.bits, write_i32, write_i64, write_u16, write_u32, write_u64 | char_ptr_to_str, debug!, syscall_body! |
| `kernel/src/syscalls/signal.rs#read_stack_t` | `xapi/src/ipc/sem.rs#sys_semget` | 同模块上下文对齐/差异观察 | Err, Ok, SigAltStack::disabled, copy_from_user, copy_from_user(user, &mut raw).map_err, e.as_errno ...(+10) | Err, IPC_MANAGER.with_sem, Ok, current_pid, sem_manager.create_semset, sem_manager.get_semid_by_key ...(+3) |
| `hal/src/user.rs#patch_loongarch64_enosys_stub` | `xapi/src/ipc/shm.rs#sys_shmctl` | 同模块上下文对齐/差异观察 | ENOSYS_STUB_PREFIX.len, bytes.get, bytes[off..off + patch.len()].copy_from_slice, patch.len, write_u32 | Err, IPC_MANAGER.with_shm, Ok, buf.is_null, nullable!, segment.lock ...(+5) |
| `kernel/src/syscalls/syslog.rs#set_console_level` | `arceos/api/arceos_posix_api/src/imp/net.rs#sys_recvfrom` | 同模块上下文对齐/差异观察 | (1..=8).contains, Err, Ok, log::set_console_level, unreachable! | debug!, syscall_body! |

#### 文件系统
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `libs/extfs/src/inode.rs#rename` | `arceos/modules/axfs-ng/src/fs/fat/dir.rs#rename` | 同板块同名 | Arc::ptr_eq, Err, Ok, copy_i_block, core::ptr::eq, crate::dir::is_dir_empty ...(+59) | Err, dir.rename, dir.rename(src_name, dst_dir.inner.borrow(&fs), dst_name)
            .map_err, dst_dir.downcast, dst_dir.downcast().map_err, dst_dir.inner.borrow ...(+4) |
| `general/src/vfs/device_files/rtc.rs#ioctl` | `xcore/src/fs/vfs/dev/loopx.rs#ioctl` | 同板块同名 | Err, Ok, RtcControlRequest::SetAlarm, RtcControlRequest::SetAlarmIrqEnabled, RtcControlRequest::SetEpoch, RtcControlRequest::SetPeriodicIrqEnabled ...(+23) | Err, Ok, Some, argp.address, argp.address().as_usize, argp.cast ...(+24) |
| `general/src/vfs/tmpfs.rs#rename` | `xcore/src/fs/vfs/tmp/mod.rs#rename` | 同板块同名 | Err, Ok, Some, dir.dec_nlink, dir.fs_id, dir.ino ...(+30) | Ok, dst_dir.downcast::<Self>, dst_dir.lookup, dst_name.into, dst_node
            .inode
            .as_dir, dst_node
            .inode
            .as_dir()?
            .lock ...(+8) |
| `libs/extfs/src/file.rs#read_at` | `arceos/modules/axfs-ng/src/fs/fat/file.rs#read_at` | 同板块同名 | (offset + remaining as u64).min, (size - offset).min, (start + remaining).min, Ok, Vec::new, buf.is_empty ...(+25) | Ok, SeekFrom::Start, file.read, file.read(buf).map_err, file.seek, file.seek(SeekFrom::Start(offset)).map_err ...(+2) |

#### 设备驱动
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `general/src/dev/drivers/loongson_irq.rs#probe` | `arceos/modules/axdriver/src/lib.rs#probe` | 同板块同名 | Arc::clone, Arc::new, EioIntc::new, EioIntcRouteConfig::from_platform, EioIntcRouteConfig::from_platform(parent_hwi, info).ok_or, Err ...(+19) | for_each_drivers!, self.probe_bus_devices |
| `general/src/dev/function.rs#push` | `arceos/modules/axdriver/src/structs/static.rs#push` | 同板块同名 | Arc::clone, Err, Ok, Vec::new, core::mem::replace, drop ...(+22) | Some, self.0.is_none |
| `arch/src/riscv64/early_console.rs#write_bytes` | `arceos/modules/axhal/src/platform/loongarch64_qemu_virt/console.rs#write_bytes` | 同板块同名 | AtomicBool::new, BASE.load, INITED.load, INITED.store, INITIALIZING
                .compare_exchange, INITIALIZING
                .compare_exchange(false, true, Ordering::Acquire, Ordering::Relaxed)
                .is_ok ...(+9) | UART.lock, uart.put |
| `general/src/dev/drivers/uart16550.rs#flush` | `arceos/tools/raspi4/chainloader/src/bsp/device_driver/bcm/bcm2xxx_pl011_uart.rs#flush` | 同板块同名 | Err, Ok, core::hint::spin_loop, guard.state_mut, self.kick_tx_locked, self.transmitter_empty ...(+1) | cpu::nop, self.registers.FR.matches_all |

#### 网络协议栈
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `libs/socket/src/state.rs#send` | `arceos/api/arceos_posix_api/src/imp/net.rs#send` | 同板块同名 | Err, effective_send_deadline, io::send_datagram, io::send_seqpacket, io::send_stream, options.sender_identity.unwrap_or ...(+2) | Ok, tcpsocket.lock, tcpsocket.lock().send, udpsocket.lock, udpsocket.lock().send |
| `libs/socket/src/state.rs#accept` | `xcore/src/net/socket.rs#accept` | 同板块同名 | Err, connection::accept_connection_socket, effective_recv_deadline, self.finish_result | Err, Mutex::new, Socket::Tcp, Socket::Unix, tcpsocket
                .lock, tcpsocket
                .lock()
                .accept ...(+4) |
| `libs/socket/src/state.rs#connect` | `xcore/src/net/socket.rs#connect` | 同板块同名 | connection::connect_datagram, connection::connect_seqpacket, connection::connect_stream, effective_send_deadline, self.finish_result | Err, Ok, tcpsocket.lock, tcpsocket.lock().connect, udpsocket.lock, udpsocket.lock().connect |
| `libs/socket/src/state.rs#bind` | `arceos/ulib/axstd/src/net/tcp.rs#bind` | 同板块同名 | Err, Ok, Some, address.binding_key, address.binding_key().ok_or, current.is_some ...(+3) | Ok, TcpListener, api::ax_tcp_bind, api::ax_tcp_listen, api::ax_tcp_socket, super::each_addr |

#### 文件系统/第三方库
仅参考项目中观察到该模块代表函数，目标项目没有同模块可对齐证据：
- 参考项目：`arceos/crates/lwext4_rust/src/inode/file.rs#read_at`，调用摘要：(inode as *const _ as *const u8).add, (pos + buf.len() as u64).min, Ok, WritebackGuard::new, assert!, buf.as_mut_ptr ...(+22)
- 参考项目：`arceos/crates/lwext4_rust/src/inode/file.rs#write_at`，调用摘要：Ok, assert!, assert_eq!, buf.as_ptr, buf.is_empty, buf.len ...(+15)
- 参考项目：`arceos/crates/lwext4_rust/src/blockdev.rs#new`，调用摘要：Box::new, Ok, Some, block_buf.as_mut_ptr, block_cache_buf.as_mut, block_dev_iface.as_mut ...(+10)
- 参考项目：`arceos/modules/axfs-ng/src/fs/ext4/inode.rs#create`，调用摘要：DirEntry::new_dir, DirEntry::new_file, DirNode::new, Err, FileNode::new, Inode::new ...(+11)

#### 网络协议栈/第三方库
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `libs/mygo-smoltcp/src/storage/assembler.rs#add` | `arceos/modules/axnet/src/smoltcp_impl/mod.rs#add` | 同板块同名 | Contig::hole_and_data, Err, Ok, contig.has_data, contig.shrink_hole_to, contig.total_size ...(+11) | debug!, self.0.lock, self.0.lock().add |
| `libs/mygo-smoltcp/src/socket/tcp.rs#connect` | `arceos/modules/axnet/src/smoltcp_impl/tcp.rs#connect` | 同板块同名 | Err, Ok, Self::random_seq_no, Some, addr.is_unspecified, cx
                    .get_source_address ...(+9) | Err, Ok, Ok::<(IpEndpoint, IpEndpoint), NetError>, SOCKET_SET
                .with_socket_mut::<tcp::Socket, _, _>, SOCKET_SET.add, SocketSetWrapper::new_tcp_socket ...(+33) |
| `libs/mygo-smoltcp/src/socket/tcp.rs#send_impl` | `arceos/modules/axnet/src/smoltcp_impl/udp.rs#send_impl` | 同板块同名 | Err, Ok, f, self.may_send, self.rtte.retransmission_timeout, self.timer.is_idle ...(+3) | Err, Ok, SOCKET_SET.with_socket_mut::<udp::Socket, _, _>, buf.len, self.block_on, self.local_addr.read ...(+5) |
| `libs/mygo-smoltcp/src/phy/fault_injector.rs#receive` | `arceos/modules/axnet/src/smoltcp_impl/mod.rs#receive` | 同板块同名 | <D::RxToken<'_> as phy::RxToken>::meta, Some, buffer.len, net_trace!, self.inner.receive, self.rx_buf.len ...(+5) | AxNetRxToken, AxNetTxToken, Some, dev.can_transmit, dev.receive, dev.recycle_tx_buffers ...(+3) |

#### 第三方库
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `libs/efi/src/c/efi_global.c#efi_system_table_firmware_vendor` | `arceos/crates/page_table_multiarch/page_table_multiarch/src/bits64.rs#map_region` | 同模块上下文对齐/差异观察 | efi_system_table_is_valid | Err, M::flush_tlb, Ok, PageSize::Size1G.is_aligned, PageSize::Size2M.is_aligned, PageSize::Size4K.is_aligned ...(+12) |
| `libs/efi/src/lib.rs#firmware_vendor` | `arceos/crates/axdriver_crates/axdriver_virtio/src/net.rs#try_new` | 同模块上下文对齐/差异观察 | Some, core::ptr::null, core::slice::from_raw_parts, efi_system_table_firmware_vendor, ptr.is_null | Err, InnerDev::new, InnerDev::new(transport).map_err, NetBufPool::new, Ok, Some ...(+14) |
| `libs/efi/src/lib.rs#firmware_vendor_cstr16` | `arceos/crates/page_table_multiarch/page_table_multiarch/src/bits64.rs#protect_region` | 同模块上下文对齐/差异观察 | firmware_vendor | Err, Ok, TlbFlushAll::new, assert!, error!, self.protect ...(+5) |
| `general/src/vfs/procfs.rs#cpuinfo_vendor_from_compatible` | `arceos/crates/page_table_multiarch/page_table_multiarch/src/arch/loongarch64.rs#flush_tlb` | 同模块上下文对齐/差异观察 | compatible
        .split_once, compatible
        .split_once(',')
        .map, compatible
        .split_once(',')
        .map(|(vendor, _)| vendor)
        .unwrap_or | asm! |

#### 通用模块/其他
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `libs/acpi/src/aml/namespace.rs#insert` | `xmodules/xutils/src/collections/btreemap.rs#insert` | 同板块同名 | ObjectFlags::new, Ok, assert!, emergency!, level
            .values
            .insert, path.normalize ...(+1) | key.clone, self.forward.insert, self.forward.remove, self.reverse.insert, self.reverse.remove, value.clone |
| `libs/log/src/lib.rs#write_str` | `arceos/modules/axlog/src/lib.rs#write_str` | 同板块同名 | Ok, ch.len_utf8, s.as_bytes, s.char_indices, s.len, self.buf[self.len..self.len + copy_len].copy_from_slice | Ok, cfg_if::cfg_if! |
| `general/src/ipc/shm.rs#remove` | `xmodules/xsignal/src/types.rs#remove` | 同板块同名 | Ok, check_control_owner, remove_entry_locked, self.state.lock, state.by_id.get_mut, state.by_id.get_mut(&id).ok_or ...(+1) | Self::signo_bit |
| `general/src/dev/char.rs#write_all` | `arceos/ulib/axlibc/src/strftime.rs#write_all` | 同板块同名 | Err, Ok, core::hint::spin_loop, remaining.is_empty, self.write | Err, Ok, buf.len, self.inner.write_all |


好的，审计官。以下是基于您提供的全息证据、控辩双方论点及客观 AST 数据的最终定性仲裁报告。

### 1. 宏观判定
- **定性结论**: **独立开发 (底层架构显著差异)**
- **核心依据**: 客观 AST 相似度仅为 **0.73%**，处于统计噪声水平。目标项目覆盖率 **0.97%**，参考项目覆盖率 **2.83%**，均远低于可认定代码复用的阈值。双方在核心子系统（进程管理、内存管理、文件系统等）的实现路径、抽象层次和依赖库上存在根本性架构差异。

### 2. 全模块覆盖矩阵解读
| 模块 | 双方存在 | 共同指纹 | 目标覆盖 | 参考覆盖 | 证据强度 | 解读 |
| :--- | :---: | :---: | :---: | :---: | :---: | :--- |
| **进程管理** | 是 | 7 | 1.24% | 4.40% | **弱** | 共同指纹均为 Linux 标准字段访问器或通用容器初始化，属规范趋同。 |
| **任务调度/异步运行时** | 是 | 4 | 0.47% | 1.14% | **弱** | 共同指纹为 POSIX 系统调用入口，内部实现路径（如 `sched::operation::wait4` vs `child.data::<XProcess>`）完全不同。 |
| **内存管理** | 是 | 6 | 0.39% | 1.54% | **弱** | 共同指纹为通用读写操作，目标项目有自研 `mremap` 实现，参考项目为 `unimplemented` 桩。 |
| **中断/异常处理** | 是 | 0 | 0.0% | 0.0% | **未形成** | 无共同指纹，双方在此模块无任何可关联证据。 |
| **系统调用** | 是 | 0 | 0.0% | 0.0% | **未形成** | 无共同指纹，双方在此模块无任何可关联证据。 |
| **文件系统** | 是 | 6 | 0.34% | 0.97% | **弱** | 共同指纹为通用文件操作（`rename`, `read_at`），底层文件系统实现（extfs vs FAT/ext4）不同。 |
| **设备驱动** | 是 | 2 | 0.31% | 1.59% | **弱** | 共同指纹为通用 `probe` 和 `push` 操作，驱动对象（Loongson IRQ vs VirtIO）完全不同。 |
| **网络协议栈** | 是 | 0 | 0.0% | 0.0% | **未形成** | 无共同指纹，双方在此模块无任何可关联证据。 |
| **第三方库/公共依赖** | 是 | 5 | 0.27% | 3.47% | **弱** | 共同指纹源于对 `smoltcp` 等公共第三方库的封装，属公共依赖，非项目间复用。 |

### 3. 底层 AST 关键证据指纹表
| 所属板块 | 目标项目路径 | 参考项目路径 | 证据类型 | 技术说明 |
| :--- | :--- | :--- | :--- | :--- |
| **进程管理** | `libs/sched/src/task.rs#clear_child_tid` | `xcore/src/task/proc.rs#clear_child_tid` | 同板块底层逻辑一致 | 均为 `self.clear_child_tid.load` 单语句 getter。`clear_child_tid` 是 Linux 标准字段，此为规范趋同。 |
| **进程管理** | `libs/sched/src/task.rs#set_clear_child_tid` | `xcore/src/task/proc.rs#set_clear_child_tid` | 同板块同名同结构 | 均为 `self.clear_child_tid.store` 单语句 setter。同上，属 Linux ABI 强制的标准实现。 |
| **进程管理** | `libs/sched/src/wait.rs#new` | `arceos/modules/axtask/src/wait_queue.rs#new` | 同板块同名同结构 | 均为 `Spinlock::new, VecDeque::new` 初始化。目标用 `Spinlock`，参考用 `SpinNoIrq`，并发策略不同。 |
| **进程管理** | `libs/sched/src/wait.rs#len_hint` | `arceos/modules/axtask/src/wait_queue.rs#len` | 同板块证据 | 目标为 `self.waiters.lock().len`，参考为 `self.queue.lock().len`。字段名、函数名、语义均不同。 |
| **进程管理** | `libs/sched/src/task.rs#exit_code` | `arceos/modules/axtask/src/task.rs#exit_code` | 同板块同名 | 目标含 `has_exit_code` 状态判断返回 `Option`，参考直接返回 `ExitCode`，状态机设计不同。 |
| **任务调度** | `kernel/src/syscalls/fs.rs#sys_pselect6` | `xapi/src/iomux/select.rs#sys_pselect6` | 同板块同名 | POSIX 系统调用。目标用 `PollEvents`，参考用 `do_select` 和 `nullable!`，实现路径异构。 |
| **内存管理** | `general/src/mm/vm_space.rs#mremap` | `arceos/ulib/axlibc/c/mmap.c#mremap` | 同板块同名 | 目标有完整自研实现，参考为 `unimplemented` 桩，证明目标独立开发。 |
| **文件系统** | `libs/extfs/src/inode.rs#rename` | `arceos/modules/axfs-ng/src/fs/fat/dir.rs#rename` | 同板块同名 | 目标基于自研 extfs，参考基于 FAT，底层文件系统逻辑完全不同。 |
| **设备驱动** | `general/src/dev/drivers/loongson_irq.rs#probe` | `arceos/modules/axdriver/src/lib.rs#probe` | 同板块同名 | 目标驱动 Loongson 中断控制器，参考为 VirtIO 驱动框架，硬件域完全不同。 |
| **网络协议栈/第三方库** | `libs/mygo-smoltcp/src/socket/tcp.rs#connect` | `arceos/modules/axnet/src/smoltcp_impl/tcp.rs#connect` | 同板块同名 | 双方均封装 `smoltcp` 库，调用链相似源于对同一公共库的依赖，非彼此抄袭。 |

### 4. 核心代码调用流对比剖析
*   **【进程管理/系统调用路径】`sys_wait4`**
    *   **底层技术析理**: 目标项目通过自研调度子系统 `sched::operation::wait4` 实现核心等待逻辑，并使用 `WaitOptions::from_raw` 和 `copy_to_user` 进行参数与结果传递。参考项目则深度依赖其自有的 `XProcess` 抽象，通过 `child.data::<XProcess>()` 获取进程扩展数据并进行状态检查。两者在进程对象模型、等待队列实现和用户态交互机制上存在根本性架构差异。
    *   **代码溯源链接**:
        -   目标项目: `kernel/src/syscalls/process.rs#sys_wait4`
        -   参考项目: `xapi/src/task/wait.rs#sys_wait4`

*   **【内存管理/系统调用路径】`mremap`**
    *   **底层技术析理**: 目标项目在 `general/src/mm/vm_space.rs` 中实现了完整的 `mremap` 逻辑，涉及文件 backing、内存通知等复杂操作。参考项目在 `arceos/ulib/axlibc/c/mmap.c` 中仅提供一个 `unimplemented` 桩。这直接证明目标项目在该功能上进行了完全独立的开发。
    *   **代码溯源链接**:
        -   目标项目: `general/src/mm/vm_space.rs#mremap`
        -   参考项目: `arceos/ulib/axlibc/c/mmap.c#mremap`

*   **【文件系统/核心操作】`rename`**
    *   **底层技术析理**: 目标项目在 `libs/extfs/src/inode.rs` 中操作自研的 ext 系列文件系统，涉及 `copy_i_block` 等底层 inode 操作。参考项目在 `arceos/modules/axfs-ng/src/fs/fat/dir.rs` 中操作 FAT 文件系统，通过 `dir.rename` 进行目录项重命名。两者面向完全不同的文件系统元数据结构。
    *   **代码溯源链接**:
        -   目标项目: `libs/extfs/src/inode.rs#rename`
        -   参考项目: `arceos/modules/axfs-ng/src/fs/fat/dir.rs#rename`

### 5. 单方模块与降权项
*   **单方模块**: “文件系统/第三方库”模块仅在参考项目中体现（如 `lwext4_rust`），目标项目无此模块，不构成证据。
*   **跨板块同名降权**: 控辩双方均提及的 `parent` (ACPI vs 进程)、`children` (设备树 vs 进程)、`fork` (VFS vs 进程) 等函数，虽同名但分属完全不同的功能领域和抽象层次，其 AST 相似源于通用命名，不构成同板块主证据，已全部降权为弱信号或排除。
*   **第三方库/公共依赖**: 网络协议栈中 `smoltcp` 相关的相似调用链，以及 `Spinlock`、`VecDeque` 等标准库容器的使用，均属于对公共第三方库或语言标准库的正常依赖，不能作为项目间代码复用的证据。

### 6. 保守结论
本次审计的客观 AST 相似度极低 (0.73%)，处于背景噪声水平，是独立开发的强证据。控方提出的“核心铁证”经技术剖析，均为 Linux 标准字段访问器、通用设计模式或 POSIX 规范下的必然趋同实现，不具备可保护的代码表达独创性。双方在进程模型、内存管理、文件系统、设备驱动等核心子系统上展现出截然不同的架构选型、抽象层次和第三方库依赖。跨板块的同名函数已被排除。综上，目标项目 `2026-T202610112999587-XX` 相对于参考项目 `2025-StarryX-杭州电子科技大学` 在代码实现层面是独立开发的，不存在违规复用行为，整体风险极低。

------------------------------

