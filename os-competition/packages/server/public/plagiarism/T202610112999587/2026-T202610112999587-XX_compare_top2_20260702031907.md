# 🛡️ OS 内核赛自动化代码级架构审计报告

**审计时间**: `2026-07-02 03:19:07`
**参赛作品名称**: `2026-T202610112999587-XX`
**年份**: `2026`
**学校**: `XX`
**队伍名称**: `T202610112999587`
**比赛名字**: `OS-Competition`
**比赛子赛道**: `Kernel-Implementation`
**仓库地址**: `https://gitlab.eduxiji.net/educg-group-43501-3132633/T202610112999587-3708`
**推测开发语言**: `Rust / C (由AST探针自动识别)`
**特征函数量**: `10776`

## ⚔️ 底层代码同源性对比 (AST Homology)
> 本部分由专门的代码审查智能体，对该项目与符合年份条件的历届最高相似度项目进行控制流与底层调用对比。新版报告会先给出确定性的全模块覆盖矩阵，再给出 Agent 的分模块解释，避免只围绕少量抽样函数展开。

### 🏆 Top 2 底层对标: vs `2024-萌新-南开大学`（南开大学 / 萌新）

#### 对方作品信息

**对方年份**: `2024`
**对方学校**: `南开大学`
**对方队伍名称**: `萌新`
**对方比赛名字**: `OS-Competition`
**对方比赛子赛道**: `Kernel-Implementation`
**对方仓库地址**: `https://gitlab.eduxiji.net/educg-group-26010-2376550/T202410055992606-2876`

> 双向覆盖率：共同 AST 指纹: 87；唯一 AST 指纹: 目标项目 8484，参考项目 2483；覆盖率: 目标项目 1.03%，参考项目 3.5%

## 全模块对比总览（确定性证据）

> 总体覆盖率：共同 AST 指纹: 87；唯一 AST 指纹: 目标项目 8484，参考项目 2483；覆盖率: 目标项目 1.03%，参考项目 3.5%

### 1. 模块覆盖矩阵
| 模块 | 目标函数数 | 参考函数数 | 共同唯一 AST 指纹 | Target 覆盖 | Base 覆盖 | 状态 |
| :--- | ---: | ---: | ---: | ---: | ---: | :--- |
| 进程管理 | 696 | 39 | 1 | 0.18% | 3.12% | 双方存在且有结构重合 |
| 任务调度/异步运行时 | 1070 | 272 | 4 | 0.47% | 1.78% | 双方存在且有结构重合 |
| 内存管理 | 1729 | 174 | 1 | 0.06% | 0.6% | 双方存在且有结构重合 |
| 中断/异常处理 | 226 | 29 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |
| 系统调用 | 37 | 67 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |
| 文件系统 | 2220 | 285 | 4 | 0.22% | 1.72% | 双方存在且有结构重合 |
| 设备驱动 | 808 | 42 | 1 | 0.16% | 2.5% | 双方存在且有结构重合 |
| 网络协议栈 | 439 | 0 | 0 | 0.0% | 0.0% | 仅目标项目体现 |
| 文件系统/第三方库 | 0 | 730 | 0 | 0.0% | 0.0% | 仅参考项目体现 |
| 网络协议栈/第三方库 | 2456 | 118 | 3 | 0.16% | 2.7% | 双方存在且有结构重合 |
| 第三方库 | 7 | 1374 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |
| 通用模块/其他 | 1088 | 106 | 5 | 0.57% | 5.68% | 双方存在且有结构重合 |

### 2. 分模块证据清单

#### 进程管理
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `libs/sched/src/operation.rs#exit` | `modules/axtask/src/api.rs#exit` | 同板块同名 | ExitCode, current_task, drop, exit_task, panic!, schedule_once | RUN_QUEUE.lock, RUN_QUEUE.lock().exit_current |
| `libs/sched/src/task.rs#sched_reset_on_fork` | `modules/axtask/src/task.rs#get_zombie` | 同板块同结构 | self.sched_reset_on_fork.load | self.is_zombie.load |
| `libs/acpi/src/aml/mod.rs#do_execute_method` | `api/linux_syscall_api/src/syscall_task/imp/task.rs#syscall_exec` | 同模块上下文对齐/差异观察 | AmlName::from_str, AmlName::from_str(&object.as_string().unwrap())?
                                .resolve, Arc::new, Argument::Object, AtomicU64::new, Err ...(+214) | Err, Ok, Some, Vec::new, args_vec.len, args_vec.push ...(+20) |
| `general/src/vfs/sysfs.rs#lookup_child` | `modules/axfs/src/mounts.rs#procfs` | 同模块上下文对齐/差异观察 | Arc::new, CpuSlot::Online.to_u64, CpuSlot::Possible.to_u64, CpuSlot::Present.to_u64, CpuSlot::TopoDir.to_u64, Err ...(+109) | Arc::new, Ok, file_over.write_at, file_somaxconn.write_at, fs::ramfs::RamFileSystem::new, proc_root.clone ...(+3) |

#### 任务调度/异步运行时
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `libs/sched/src/mutex.rs#lock` | `modules/axtask/src/sync/mutex.rs#lock` | 同板块同名 | current_task, now_ns_public, schedule_once, self
                .locked
                .compare_exchange_weak, self
                .locked
                .compare_exchange_weak(false, true, Ordering::Acquire, Ordering::Relaxed)
                .is_ok, self.waiters.finish_wait ...(+1) | assert_ne!, current, current().tid, panic!, self.data.get, self.owner_id.compare_exchange_weak |
| `libs/sched/src/rlimit.rs#name` | `modules/axtask/src/task.rs#name` | 同板块同名 | 内部闭环逻辑 | self.inner.lock, self.inner.lock().name, self.inner.lock().name().to_string |
| `libs/sched/src/sync.rs#try_lock` | `modules/axtask/src/sync/mutex.rs#try_lock` | 同板块同名 | self.locked
            .compare_exchange, self.locked
            .compare_exchange(false, true, Ordering::Acquire, Ordering::Relaxed)
            .ok, self.locked
            .compare_exchange(false, true, Ordering::Acquire, Ordering::Relaxed)
            .ok()
            .map | Some, current, current().inner.lock, current().inner.lock().id, current().inner.lock().id().as_u64, self
            .owner_id
            .compare_exchange ...(+2) |
| `libs/sched/src/scheduler.rs#on_timer_tick` | `modules/axtask/src/api.rs#on_timer_tick` | 同板块同名 | INIT_READY.load, RUNQUEUES[cpu_id].tick, cpu, fire_expired_realtime_itimers, request_resched, wake_expired_sleepers | RUN_QUEUE.lock, RUN_QUEUE.lock().scheduler_timer_tick |

#### 内存管理
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `libs/sched/src/spawn.rs#clone_task` | `modules/axtask/src/task.rs#clone_task` | 同板块同名 | Arc::clone, Arc::downgrade, Arc::new, SchedAttr::fair, SignalNumber::SIGCHLD.raw, Task::new ...(+48) | (axconfig::SIGNAL_TRAMPOLINE).into, (start_signal_trampoline as usize).into, Arc::clone, Arc::new, AxTask::new, Err ...(+73) |
| `libs/allocator/src/slab.rs#alloc` | `modules/axalloc/src/lib.rs#alloc` | 同板块同名 | CACHE_CAPACITY
                .saturating_sub, CACHE_CAPACITY
                .saturating_sub(cache_guard.count)
                .min, CacheEntry::empty, SlabAllocation::null, Some, allocate_slab_node ...(+17) | Ok, balloc.add_memory, balloc.alloc, balloc.total_bytes, debug!, layout.size ...(+5) |
| `general/src/mm/vm_space.rs#mremap` | `modules/axmem/src/lib.rs#mremap` | 同板块同名 | Err, Ok, Self::collect_file_backings, Self::notify_file_unmapped, Self::notify_files_mapped, Some ...(+30) | (old_page_start..=old_page_end).step_by, VirtAddr::from, assert!, check_page_table_entry_validity, core::slice::from_raw_parts, core::slice::from_raw_parts_mut ...(+18) |
| `libs/allocator/src/metadata.rs#alloc` | `modules/axhal/src/tls.rs#alloc` | 同板块同名 | base.checked_add, boot.alloc, boot_source, core::ptr::write_bytes, drop, layout.align ...(+14) | Layout::from_size_align, Layout::from_size_align(tls_area_size(), TLS_ALIGN).unwrap, NonNull::new, NonNull::new(area_base).unwrap, alloc::alloc::alloc_zeroed, area_base.add ...(+5) |

#### 中断/异常处理
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `general/src/dev/drivers/ls7a_rtc.rs#handle_irq` | `modules/axhal/src/platform/VisionFive2/rtc.rs#handle_irq` | 同板块同名 | RtcIrqData::new, rtc_dev.record_irq, self.rtc.acknowledge_alarm_irq, self.rtc_dev.upgrade | self.gold_fish_rtc
            .set_alarm, self.gold_fish_rtc.clear_irq, self.gold_fish_rtc.read_alarm_fmt, self.gold_fish_rtc.read_time_fmt, self.read_time_u64, trace! |
| `general/src/dev/drivers/plic.rs#handle_irq` | `api/linux_syscall_api/src/trap.rs#handle_irq` | 同板块同名 | irq::dispatch_irq_line, self.plic.claim, self.plic.complete | axhal::irq::dispatch_irq, time_stat_from_kernel_to_user, time_stat_from_user_to_kernel |
| `general/src/dev/drivers/loongson_irq.rs#translate_source` | `modules/axhal/src/platform/VisionFive2/irq.rs#register_handler` | 同模块上下文对齐/差异观察 | Some, drop, entry.is_some_and, inner
                .slots
                .iter, inner
                .slots
                .iter()
                .enumerate, inner
                .slots
                .iter()
                .enumerate()
                .find ...(+13) | with_cause! |
| `libs/errno/src/lib.rs#from_i32` | `modules/axhal/src/platform/riscv64_qemu_virt/irq.rs#register_handler` | 同模块上下文对齐/差异观察 | Errno::Other | with_cause! |

#### 系统调用
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `kernel/src/syscalls/ipc.rs#encode_shmid64_ds` | `api/linux_syscall_api/src/syscall_net/socket.rs#get` | 同模块上下文对齐/差异观察 | meta.perm.mode.bits, write_i32, write_i64, write_u16, write_u32, write_u64 | copy_nonoverlapping, drop, keep_alive.to_ne_bytes, panic!, s.keep_alive, s.keep_alive().is_some ...(+10) |
| `kernel/src/syscalls/signal.rs#read_stack_t` | `api/linux_syscall_api/src/syscall_net/socket.rs#set` | 同模块上下文对齐/差异观察 | Err, Ok, SigAltStack::disabled, copy_from_user, copy_from_user(user, &mut raw).map_err, e.as_errno ...(+10) | <[u8; 4]>::try_from, <[u8; 4]>::try_from(&opt[0..4]).unwrap, Err, Ok, Some, axnet::Duration::from_secs ...(+14) |
| `hal/src/user.rs#patch_loongarch64_enosys_stub` | `api/linux_syscall_api/src/syscall_mem/imp.rs#syscall_shmget` | 同模块上下文对齐/差异观察 | ENOSYS_STUB_PREFIX.len, bytes.get, bytes[off..off + patch.len()].copy_from_slice, patch.len, write_u32 | Err, MemorySet::add_shared_mem, MemorySet::create_shared_mem, Ok, ShmFlags::from_bits, axmem::KEY_TO_SHMID.lock ...(+8) |
| `kernel/src/syscalls/syslog.rs#set_console_level` | `api/linux_syscall_api/src/syscall_net/socket.rs#get` | 同模块上下文对齐/差异观察 | (1..=8).contains, Err, Ok, log::set_console_level, unreachable! | bytes.as_bytes, bytes.as_ptr, bytes.len, copy_nonoverlapping, panic!, raw_socket.get_congestion ...(+4) |

#### 文件系统
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `libs/extfs/src/inode.rs#rename` | `modules/axfs/src/fs/fatfs.rs#rename` | 同板块同名 | Arc::ptr_eq, Err, Ok, copy_i_block, core::ptr::eq, crate::dir::is_dir_empty ...(+59) | debug!, dst_path.trim_matches, self.0
            .rename, self.0
            .rename(src_path, &self.0, dst_path)
            .map_err |
| `general/src/vfs/device_files/rtc.rs#ioctl` | `modules/axfs/src/api/port.rs#ioctl` | 同板块同名 | Err, Ok, RtcControlRequest::SetAlarm, RtcControlRequest::SetAlarmIrqEnabled, RtcControlRequest::SetEpoch, RtcControlRequest::SetPeriodicIrqEnabled ...(+23) | Err |
| `libs/fatfs/src/fat.rs#set` | `api/linux_syscall_api/src/syscall_fs/imp/poll.rs#set` | 同板块同名 | (value as u16).to_le_bytes, Ok, Self::ensure_slot_present, Self::ensure_slot_present_mut, new.to_le_bytes, raw.to_le_bytes ...(+7) | self.addr.add |
| `general/src/vfs/tmpfs.rs#rename` | `modules/axfs/src/root.rs#rename` | 同板块同名 | Err, Ok, Some, dir.dec_nlink, dir.fs_id, dir.ino ...(+30) | ax_err!, fs.root_dir, fs.root_dir().rename, rest_path.is_empty, self.lookup_mounted_fs |

#### 设备驱动
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `general/src/dev/drivers/loongson_irq.rs#probe` | `modules/axdriver/src/lib.rs#probe` | 同板块同名 | Arc::clone, Arc::new, EioIntc::new, EioIntcRouteConfig::from_platform, EioIntcRouteConfig::from_platform(parent_hwi, info).ok_or, Err ...(+19) | for_each_drivers!, self.probe_bus_devices |
| `general/src/dev/function.rs#push` | `modules/axdriver/src/structs/static.rs#push` | 同板块同名 | Arc::clone, Err, Ok, Vec::new, core::mem::replace, drop ...(+22) | Some, self.0.is_none |
| `general/src/dev/drivers/random.rs#deref` | `modules/axdriver/src/structs/dyn.rs#deref` | 同板块同名 | self.lock.state.get | 内部闭环逻辑 |
| `general/src/dev/virtio.rs#is_empty` | `modules/axdriver/src/structs/dyn.rs#is_empty` | 同板块同名同结构 | self.len | self.len |

#### 网络协议栈
仅目标项目中观察到该模块代表函数，参考项目没有同模块可对齐证据：
- 目标项目：`libs/socket/src/state.rs#socket_inflight_reaches_socket`，调用摘要：Arc::clone, SOCKETS.lock, Some, Vec::new, dgram.state.lock, drop ...(+18)
- 目标项目：`libs/net/src/stack.rs#raw_send`，调用摘要：Err, Ok, Some, data.len, endpoint_to_smoltcp, iface_lock.lock ...(+22)
- 目标项目：`libs/socket/src/state.rs#push_socket_edges`，调用摘要：Arc::clone, dgram.state.lock, drop, push_handle, push_packet_queue, push_stream_queue ...(+3)
- 目标项目：`libs/net/src/stack.rs#socket_recv_queue_reports_raw_and_icmp_datagram_bytes`，调用摘要：IfConfig::static_v4, Ipv4Addr::new, NetStack::new, assert_eq!, attach_test_iface_with_driver, b"icmp-queued".len ...(+11)

#### 文件系统/第三方库
仅参考项目中观察到该模块代表函数，目标项目没有同模块可对齐证据：
- 参考项目：`crates/rust-fatfs/src/fs.rs#format_volume`，调用摘要：DirFileEntryData::new, Err, Ok, SeekFrom::End, SeekFrom::Start, alloc_cluster ...(+27)
- 参考项目：`crates/rust-fatfs/src/table.rs#test_fat`，调用摘要：ClusterIterator::<&mut S, S::Error, S>::new, [0xA_u32, 0x14_u32, 0x15_u32, 0x16_u32, 0x19_u32, 0x1A_u32]
                .iter, [0xA_u32, 0x14_u32, 0x15_u32, 0x16_u32, 0x19_u32, 0x1A_u32]
                .iter()
                .copied, [0xA_u32, 0x14_u32, 0x15_u32, 0x16_u32, 0x19_u32, 0x1A_u32]
                .iter()
                .copied()
                .map, [0xA_u32, 0x14_u32, 0x15_u32, 0x16_u32, 0x19_u32, 0x1A_u32]
                .iter()
                .copied()
                .map(Some)
                .collect::<Vec<_>>, assert! ...(+3)
- 参考项目：`crates/another_ext4/src/ext4_defs/xattr.rs#insert`，调用摘要：XattrEntry::new, XattrEntry::required_size, entry.compare_name, entry.compare_name(name).is_gt, entry.used_size, self.0
            .data
            .copy_within ...(+7)
- 参考项目：`crates/rust-fatfs/src/boot_sector.rs#determine_sectors_per_fat`，调用摘要：fat_type.bits_per_fat_entry, u32::from, u64::from

#### 网络协议栈/第三方库
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `libs/mygo-smoltcp/src/storage/assembler.rs#add` | `modules/axnet/src/smoltcp_impl/mod.rs#add` | 同板块同名 | Contig::hole_and_data, Err, Ok, contig.has_data, contig.shrink_hole_to, contig.total_size ...(+11) | debug!, self.0.lock, self.0.lock().add |
| `libs/mygo-smoltcp/src/socket/tcp.rs#connect` | `modules/axnet/src/smoltcp_impl/tcp.rs#connect` | 同板块同名 | Err, Ok, Self::random_seq_no, Some, addr.is_unspecified, cx
                    .get_source_address ...(+9) | Ok, Ok::<(IpEndpoint, IpEndpoint), AxError>, SOCKET_SET
                .with_socket_mut::<tcp::Socket, _, _>, SOCKET_SET.add, SocketSetWrapper::new_tcp_socket, Some ...(+23) |
| `libs/mygo-smoltcp/src/socket/tcp.rs#send_impl` | `modules/axnet/src/smoltcp_impl/udp.rs#send_impl` | 同板块同名 | Err, Ok, f, self.may_send, self.rtte.retransmission_timeout, self.timer.is_idle ...(+3) | Err, Ok, SOCKET_SET.with_socket_mut::<udp::Socket, _, _>, ax_err!, ax_err_type!, buf.len ...(+8) |
| `libs/mygo-smoltcp/src/phy/fault_injector.rs#receive` | `modules/axnet/src/smoltcp_impl/mod.rs#receive` | 同板块同名 | <D::RxToken<'_> as phy::RxToken>::meta, Some, buffer.len, net_trace!, self.inner.receive, self.rx_buf.len ...(+5) | AxNetRxToken, AxNetTxToken, Some, dev.can_transmit, dev.receive, dev.recycle_tx_buffers ...(+3) |

#### 第三方库
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `libs/efi/src/c/efi_global.c#efi_system_table_firmware_vendor` | `crates/bcm2835-sdhci/src/Bcm2835SDhci.rs#sd_card_init` | 同模块上下文对齐/差异观察 | efi_system_table_is_valid | ACMD!, byte_swap, debug!, info!, self.cmd_timeout, self.dumpregs ...(+26) |
| `libs/efi/src/lib.rs#firmware_vendor` | `crates/elf_parser/src/arch/x86_64.rs#get_relocate_pairs` | 同模块上下文对齐/差异观察 | Some, core::ptr::null, core::slice::from_raw_parts, efi_system_table_firmware_vendor, ptr.is_null | Ok, Vec::new, VirtAddr::from, assert_eq!, dyn_sym.get_name, dyn_sym.get_name(elf).unwrap ...(+23) |
| `libs/efi/src/lib.rs#firmware_vendor_cstr16` | `crates/elf_parser/src/arch/aarch64.rs#get_relocate_pairs` | 同模块上下文对齐/差异观察 | firmware_vendor | Ok, Vec::new, VirtAddr::from, assert_eq!, dyn_sym.get_name, dyn_sym.get_name(elf).unwrap ...(+22) |
| `general/src/vfs/procfs.rs#cpuinfo_vendor_from_compatible` | `crates/elf_parser/src/arch/riscv.rs#get_relocate_pairs` | 同模块上下文对齐/差异观察 | compatible
        .split_once, compatible
        .split_once(',')
        .map, compatible
        .split_once(',')
        .map(|(vendor, _)| vendor)
        .unwrap_or | Ok, Vec::new, VirtAddr::from, assert_eq!, dyn_sym.get_name, dyn_sym.get_name(elf).unwrap ...(+23) |

#### 通用模块/其他
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `libs/log/src/lib.rs#write_str` | `modules/axlog/src/lib.rs#write_str` | 同板块同名 | Ok, ch.len_utf8, s.as_bytes, s.char_indices, s.len, self.buf[self.len..self.len + copy_len].copy_from_slice | Ok, cfg_if::cfg_if! |
| `general/src/dev/loopdev.rs#flush` | `modules/axlog/src/lib.rs#flush` | 同板块同名 | backing.sync, backing.sync().map_err, self.begin_flush, self.begin_flush().map_err, self.finish_io | 内部闭环逻辑 |
| `general/src/dev/bio.rs#as_slice` | `modules/axmem/src/area.rs#as_slice` | 同板块同名 | core::slice::from_raw_parts, self.ptr.as_ptr | core::slice::from_raw_parts, self.size, self.vaddr.as_ptr |
| `arch/src/lib.rs#clear_bss` | `modules/axhal/src/mem.rs#clear_bss` | 同板块同名 | clear_bss_for_loop | core::slice::from_raw_parts_mut, core::slice::from_raw_parts_mut(_sbss as usize as *mut u8, _ebss as usize - _sbss as usize)
            .fill |


好的，审计官。基于提供的客观 AST 证据、双方完整的模块画像以及控辩双方的论点，我将进行最终的深度析理与定性仲裁。

### 1. 宏观判定
- **定性结论**: **独立开发，底层架构存在显著差异**
- **核心依据**: 客观 AST 相似度仅为 **0.8%**。在总计 10967 个唯一 AST 指纹中，双方仅有 87 个共同指纹，属于典型的**领域噪声级**重合。目标项目对参考项目的覆盖率仅为 **1.03%**，参考项目对目标项目的覆盖率仅为 **3.5%**。此数据从根本上否定了任何形式的“深度复用”或“骨架抄袭”指控。控方提出的“底层逻辑复用”指控，本质上是将操作系统领域的通用设计范式与 Linux ABI 的强制性约束，误判为代码同源。

### 2. 全模块覆盖矩阵解读
| 模块 | 双方存在 | 共同指纹 | 目标覆盖 | 参考覆盖 | 证据强度 | 解读 |
| :--- | :---: | :---: | :---: | :---: | :---: | :--- |
| **进程管理** | 是 | 1 | 0.18% | 3.12% | **弱** | 双方均实现了进程管理，但规模差异巨大（696 vs 39）。唯一的共同指纹无法支撑控方提出的 5 项复用指控。目标项目实现了 `sys_clone3`、`sys_ptrace` 等复杂系统调用，远超参考项目范畴。 |
| **任务调度/异步运行时** | 是 | 4 | 0.47% | 1.78% | **弱** | 双方均有调度器实现，但模型迥异。目标项目是宏内核的抢占式调度器，参考项目是协作式异步运行时。同名函数如 `exit`、`lock` 的调用图谱完全不同，体现了设计哲学的根本差异。 |
| **内存管理** | 是 | 1 | 0.06% | 0.6% | **未形成** | 覆盖率极低。目标项目拥有 1729 个函数，是参考项目（174）的近 10 倍，表明其内存管理子系统极其复杂，包含自研的 `slab` 分配器等，与参考项目无实质结构重合。 |
| **中断/异常处理** | 是 | 0 | 0.0% | 0.0% | **未形成** | 双方均存在该模块，但 AST 指纹无任何交集。目标项目面向 LoongArch 等平台，参考项目面向 RISC-V，硬件架构差异导致底层处理逻辑完全不同。 |
| **系统调用** | 是 | 0 | 0.0% | 0.0% | **未形成** | 双方均实现了 Linux 兼容的系统调用层，但 AST 指纹无交集。这强有力地证明，即使遵循同一 ABI 规范，独立实现的代码结构也可以完全不同。 |
| **文件系统** | 是 | 4 | 0.22% | 1.72% | **弱** | 双方均有 VFS 和具体 FS 实现。目标项目包含自研的 `extfs`、`fatfs`，而参考项目依赖第三方 `rust-fatfs`。同名函数如 `rename` 的调用图谱差异巨大，目标项目直接操作 inode 和 block，参考项目则封装了第三方库调用。 |
| **设备驱动** | 是 | 1 | 0.16% | 2.5% | **弱** | 双方均有驱动模型，但面向的硬件平台完全不同。唯一的共同指纹 `is_empty` 是极其通用的容器方法，不具备特异性。 |
| **网络协议栈** | 仅目标 | 0 | 0.0% | 0.0% | **未形成** | 目标项目拥有 439 个函数的独立网络协议栈，参考项目无此模块。这是目标项目独立架构设计的有力证据。 |
| **第三方库/公共依赖** | 是 | 3 | 0.16% | 2.7% | **弱** | 双方均集成了 smoltcp 等第三方库。共同指纹源于对同一上游开源库的集成，属于**公共依赖**，不能作为抄袭证据。 |

### 3. 底层 AST 关键证据指纹表
| 所属板块 | 目标项目路径 | 参考项目路径 | 证据类型 | 技术说明 |
| :--- | :--- | :--- | :--- | :--- |
| 进程管理 | `libs/sched/src/operation.rs#exit` | `modules/axtask/src/api.rs#exit` | 同板块同名 | 函数名相同，但调用图谱完全不同。目标调用 `exit_task`、`schedule_once`；参考调用 `RUN_QUEUE.lock().exit_current`。体现了同步调度器与异步运行时的根本差异。 |
| 任务调度/异步运行时 | `libs/sched/src/mutex.rs#lock` | `modules/axtask/src/sync/mutex.rs#lock` | 同板块同名 | 目标使用 `compare_exchange_weak` 自旋，参考使用 `owner_id` 比较。锁的实现策略和依赖对象完全不同。 |
| 内存管理 | `libs/sched/src/spawn.rs#clone_task` | `modules/axtask/src/task.rs#clone_task` | 同板块同名 | 目标项目涉及 `SchedAttr`、`SignalNumber` 等复杂调度属性，参考项目则处理 `SIGNAL_TRAMPOLINE` 等。任务克隆的上下文完全不同。 |
| 文件系统 | `libs/extfs/src/inode.rs#rename` | `modules/axfs/src/fs/fatfs.rs#rename` | 同板块同名 | 目标直接操作 `copy_i_block` 等底层 inode 结构，参考则调用 `self.0.rename` 封装了第三方 `fatfs` 库。实现层次和依赖关系截然不同。 |
| 设备驱动 | `general/src/dev/virtio.rs#is_empty` | `modules/axdriver/src/structs/dyn.rs#is_empty` | 同板块同名同结构 | 双方均返回 `self.len`。这是 Rust 集合类型的通用惯用法，属于语言级噪声，无特异性。 |
| 网络协议栈/第三方库 | `libs/mygo-smoltcp/src/socket/tcp.rs#connect` | `modules/axnet/src/smoltcp_impl/tcp.rs#connect` | 同板块同名 | 双方均基于 `smoltcp` 库。目标项目直接调用 `smoltcp` 内部 API，参考项目通过 `SOCKET_SET` 封装。这是对同一公共依赖的不同集成方式。 |

### 4. 核心代码调用流对比剖析
*   **【进程管理 / Futex 等待队列管理】**
    *   **底层技术析理**: 控方指控双方在 Futex 实现上存在“底层控制流同构”。然而，AST 证据揭示了截然不同的同步和内存管理模型。目标项目 (`sys_futex_waitv`) 采用基于 `Arc::clone`/`Arc::downgrade` 的引用计数生命周期管理，配合 `FUTEX_TABLE` 全局表，是典型的无 GC 语言中安全共享状态的宏内核设计。参考项目 (`clear_wait`) 则使用简单的 `Vec` 和 `retain` 操作，其生命周期依赖于更简单的任务模型。两者在内存安全策略和对象所有权模型上存在根本性差异，所谓的“加锁全局表 -> 遍历操作”是任何 Futex 实现都不可避免的通用步骤，而非特定代码骨架的复用。
    *   **代码溯源链接**:
        - 目标项目: `kernel/src/syscalls/process.rs#sys_futex_waitv`
        - 参考项目: `modules/axtask/src/futex.rs#clear_wait`

*   **【进程管理 / Execve 系统调用】**
    *   **底层技术析理**: 目标项目 (`sys_execveat`) 的调用流展示了高度结构化的设计：`copy_cstr_from_user` → `ExecRequest::from_kernel_path` → `UserContextRef::new` → `ctx.finalize_frame`。这是一个围绕 `ExecRequest` 和 `UserTrapFrame` 等复杂抽象构建的、支持 `execveat` 的完整流程。参考项目 (`syscall_exec`) 的调用流则简单得多：`args_vec.push` → `argv.add` → `clear_wait`，是手动的参数收集和状态清理。目标项目的抽象层次和复杂度远超参考项目，其“参数收集 -> 上下文重置”的宏观流程是 POSIX `exec` 语义的必然要求，而非代码复用。
    *   **代码溯源链接**:
        - 目标项目: `kernel/src/syscalls/process.rs#sys_execveat`
        - 参考项目: `api/linux_syscall_api/src/syscall_task/imp/task.rs#syscall_exec`

*   **【任务调度/异步运行时 / 任务退出】**
    *   **底层技术析理**: 目标项目 (`exit_task`) 的退出流程涉及复杂的进程树管理：`Arc::ptr_eq` 检查父子关系，`SignalNumber::SIGCHLD` 发送信号，`children.iter` 遍历子进程进行 reparent。参考项目 (`exit_current`) 则极为简单：`EXITED_TASKS.lock().clear`，`axhal::misc::terminate`。目标项目实现了完整的 POSIX 进程组和信号语义，而参考项目仅实现了最基础的任务终止。两者在功能完整性和复杂度上不在一个量级，不存在骨架复用。
    *   **代码溯源链接**:
        - 目标项目: `libs/sched/src/spawn.rs#exit_task`
        - 参考项目: `modules/axtask/src/run_queue.rs#exit_current`

### 5. 单方模块与降权项
*   **单方模块**:
    *   **网络协议栈 (目标项目)**: 目标项目拥有 `libs/net`、`libs/socket` 等构成的完整独立网络协议栈（439 个函数），而参考项目无此模块。这是目标项目原创性的强有力证据。
    *   **文件系统/第三方库 (参考项目)**: 参考项目在 `crates/` 目录下包含 `rust-fatfs`、`another_ext4` 等第三方 FS 实现（730 个函数），目标项目则自研了 `libs/extfs` 和 `libs/fatfs`。这表明双方在文件系统实现上选择了完全不同的技术路线。
*   **降权项**:
    *   **通用语法与公共依赖**: `Arc::new`、`Vec::new`、`Ok`、`Err`、`.lock()`、`copy_from_user`、`core::hint::spin_loop` 等 AST 节点，属于 Rust 语言标准库和操作系统内核开发中的通用惯用法。将它们作为抄袭指纹是方法论上的错误。
    *   **跨板块对齐**: 控方将目标项目的 ACPI AML 解释器 (`do_execute_method`) 与参考项目的 `syscall_exec` 进行“同模块上下文对齐”，二者分属固件接口和系统调用，领域完全不同，此类硬凑的证据必须降权。
    *   **第三方库集成**: 双方在 `smoltcp` 网络栈上的共同指纹，源于对同一开源项目的集成，属于公共依赖，不构成相互抄袭的证据。

### 6. 保守结论
本次审计的客观 AST 覆盖率极低（0.8%），从根本上排除了系统性代码复用或架构抄袭的可能性。控方提出的“底层逻辑复用”指控，混淆了“领域共性”与“代码同源”。Futex、Exit、Execve 等系统调用的实现流程受 POSIX/Linux ABI 的严格约束，任何兼容内核都必然遵循相似的高层逻辑。真正的代码同源性应体现在具体的、非规范的 AST 结构重合上，而本案中 87 个共同指纹多为 `Arc::clone`、`Vec::new` 等语言级通用构造，不具备特异性。目标项目在进程管理、内存管理、文件系统等方面的规模和复杂度远超参考项目，并拥有独立的网络协议栈，符合独立开发的典型特征。综合判定，无证据表明存在违规复用。

------------------------------

