# 🛡️ OS 内核赛自动化代码级架构审计报告

**审计时间**: `2026-07-02 03:19:07`
**参赛作品名称**: `2026-T202610112999587-XX`
**年份**: `2026`
**学校**: `XX`
**队伍名称**: `T202610112999587`
**比赛名字**: `OS-Competition`
**比赛子赛道**: `Kernel-Implementation`
**仓库地址**: `https://gitlab.eduxiji.net/educg-group-43501-3132633/T202610112999587-3708`
**推测开发语言**: `Rust / C (由AST探针自动识别)`
**特征函数量**: `10776`

## ⚔️ 底层代码同源性对比 (AST Homology)
> 本部分由专门的代码审查智能体，对该项目与符合年份条件的历届最高相似度项目进行控制流与底层调用对比。新版报告会先给出确定性的全模块覆盖矩阵，再给出 Agent 的分模块解释，避免只围绕少量抽样函数展开。

### 🏆 Top 1 底层对标: vs `2025-SubsToKernel-北京科技大学`（北京科技大学 / SubsToKernel）

#### 对方作品信息

**对方年份**: `2025`
**对方学校**: `北京科技大学`
**对方队伍名称**: `SubsToKernel`
**对方比赛名字**: `OS-Competition`
**对方比赛子赛道**: `Kernel-Implementation`
**对方仓库地址**: `https://gitlab.eduxiji.net/educg-group-36002-2710490/T202510008995695-2720`

> 双向覆盖率：共同 AST 指纹: 1587；唯一 AST 指纹: 目标项目 8484，参考项目 3437；覆盖率: 目标项目 18.71%，参考项目 46.17%

## 全模块对比总览（确定性证据）

> 总体覆盖率：共同 AST 指纹: 1587；唯一 AST 指纹: 目标项目 8484，参考项目 3437；覆盖率: 目标项目 18.71%，参考项目 46.17%

### 1. 模块覆盖矩阵
| 模块 | 目标函数数 | 参考函数数 | 共同唯一 AST 指纹 | Target 覆盖 | Base 覆盖 | 状态 |
| :--- | ---: | ---: | ---: | ---: | ---: | :--- |
| 进程管理 | 696 | 86 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |
| 任务调度/异步运行时 | 1070 | 141 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |
| 内存管理 | 1729 | 329 | 3 | 0.19% | 1.03% | 双方存在且有结构重合 |
| 中断/异常处理 | 226 | 8 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |
| 系统调用 | 37 | 108 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |
| 文件系统 | 2220 | 256 | 3 | 0.17% | 1.6% | 双方存在且有结构重合 |
| 设备驱动 | 808 | 57 | 2 | 0.31% | 4.26% | 双方存在且有结构重合 |
| 网络协议栈 | 439 | 124 | 1 | 0.26% | 0.84% | 双方存在且有结构重合 |
| 文件系统/第三方库 | 0 | 704 | 0 | 0.0% | 0.0% | 仅参考项目体现 |
| 网络协议栈/第三方库 | 2456 | 2388 | 1568 | 84.03% | 86.87% | 双方存在且有结构重合 |
| 第三方库 | 7 | 0 | 0 | 0.0% | 0.0% | 仅目标项目体现 |
| 通用模块/其他 | 1088 | 139 | 3 | 0.34% | 2.33% | 双方存在且有结构重合 |

### 2. 分模块证据清单

#### 进程管理
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `kernel/src/syscalls/process.rs#futex_wait` | `os/src/task/futex.rs#futex_wait` | 同板块同名 | Arc::clone, Arc::downgrade, Arc::new, Err, FUTEX_TABLE.lock, FutexWaitState::new ...(+18) | Arc::downgrade, FUTEX_QUEUE.lock, VecDeque::new, block_current_and_run_next, current_task, current_task().unwrap ...(+9) |
| `kernel/src/syscalls/process.rs#sys_futex` | `os/src/syscall/process.rs#sys_futex` | 同板块同名 | Err, Ok, ctx.task, futex_cmd, futex_key, futex_lock_pi ...(+7) | FutexCmd::from_bits, FutexCmd::from_bits(futex_op & 0x7f).unwrap, FutexKey::new, FutexOpt::from_bits_truncate, VirtAddr::from, add_timer ...(+29) |
| `kernel/src/syscalls/process.rs#sys_sysinfo` | `os/src/syscall/process.rs#sys_sysinfo` | 同板块同名 | Ok, copy_to_user, copy_to_user(info, &out).map_err, e.as_errno, put_i64, put_u16 ...(+3) | SysInfo::new, current_process, current_process().inner_exclusive_access, current_process().inner_exclusive_access().get_user_token, get_time_ms, process_num ...(+1) |
| `kernel/src/syscalls/process.rs#sys_sched_getaffinity` | `os/src/syscall/process.rs#sys_sched_getaffinity` | 同板块同名 | Err, Ok, Vec::new, copy_to_user, copy_to_user(mask_user, &mask).map_err, ctx.task ...(+6) | 内部闭环逻辑 |

#### 任务调度/异步运行时
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `kernel/src/syscalls/fs.rs#sys_pselect6` | `os/src/syscall/fs.rs#sys_pselect6` | 同板块同名 | Err, Fd::from_raw, Ok, PollEvents::POLLIN
                        .with, PollEvents::POLLIN
                        .with(PollEvents::POLLHUP)
                        .with, PollEvents::POLLOUT.with ...(+31) | Some, current_process, current_task, current_task().unwrap, current_task().unwrap().set_sig_mask, drop ...(+24) |
| `kernel/src/syscalls/fs.rs#sys_ppoll` | `os/src/syscall/fs.rs#sys_ppoll` | 同板块同名 | 0u16.to_le_bytes, Err, Fd::from_raw, Ok, PollEvents, PollEvents::POLLNVAL.0.to_le_bytes ...(+38) | PollEvents::empty, Vec::new, current_process, debug!, drop, events.is_empty ...(+11) |
| `libs/vfs/src/net_socket.rs#connect` | `os/src/net/socket/tcp.rs#connect` | 同板块同名 | Err, Ok, Some, addr::parse_inet_sockaddr, endpoint_addr_is_unspecified, handle.socket_type ...(+17) | Err, Ok, Ok::<(IpEndpoint, IpEndpoint), SysErrNo>, SOCKET_SET
                .with_socket_mut::<tcp::Socket, _, _>, SOCKET_SET
                .with_socket_mut::<tcp::Socket, _, _>(handle, |socket| {
                    socket
                        .connect(iface.lock().context(), remote_endpoint, bound_endpoint)
                        .or_else(|e| match e {
                            ConnectError::InvalidState => {
                                ax_err!(BadState, "socket connect() failed")
                            }
                            ConnectError::Unaddressable => {
                                ax_err!(ConnectionRefused, "socket connect() failed")
                            }
                        })?;
                    Ok::<(IpEndpoint, IpEndpoint), SysErrNo>((
                        socket.local_endpoint().unwrap(),
                        socket.remote_endpoint().unwrap(),
                    ))
                })
                .unwrap, SOCKET_SET.add ...(+30) |
| `kernel/src/syscalls/fs.rs#sys_getdents64` | `os/src/syscall/fs.rs#sys_getdents64` | 同板块同名 | ControlFlow::Break, ControlFlow::Continue, Err, Ok, Some, align_up ...(+17) | UserBuffer::new, buffer.write, check_off.unwrap, current_process, de.as_slice, de.len ...(+14) |

#### 内存管理
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `libs/extfs/src/file.rs#write_at` | `os/src/mm/page_table.rs#write_at` | 同板块同名 | ((block_size - in_block as u64) as usize).min, (old_blocks_lo as u64 + added_sectors).min, Err, Ok, Some, Vec::new ...(+58) | (*sub_buff).len, buff.len, self.buffers.iter_mut, self.len |
| `libs/allocator/src/slab.rs#alloc` | `os/src/sync/banker_algo.rs#alloc` | 同板块同名 | CACHE_CAPACITY
                .saturating_sub, CACHE_CAPACITY
                .saturating_sub(cache_guard.count)
                .min, CacheEntry::empty, SlabAllocation::null, Some, allocate_slab_node ...(+17) | assert!, self.available.get_mut, self.available.get_mut(&resource).unwrap, self.task_state
            .get_mut, self.task_state
            .get_mut(&tid)
            .unwrap, self.task_state
            .get_mut(&tid)
            .unwrap()
            .get_mut ...(+1) |
| `libs/allocator/src/metadata.rs#alloc` | `os/src/task/alloc.rs#alloc` | 同板块同名 | base.checked_add, boot.alloc, boot_source, core::ptr::write_bytes, drop, layout.align ...(+14) | info!, self.recycled.pop |
| `kernel/src/syscalls/mm.rs#sys_mmap` | `os/src/syscall/mem.rs#sys_mmap` | 同板块同名 | Err, Fd::from_raw, Ok, align_up, align_up(ctx.args[1], page_size).ok_or, current_fdtable ...(+19) | MapPermission::empty, MmapFlags::from_bits, MmapFlags::from_bits(flags).unwrap, MmapProt::from_bits, MmapProt::from_bits(port).unwrap, Some ...(+16) |

#### 中断/异常处理
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `general/src/dev/drivers/loongson_irq.rs#translate_source` | `os/src/signal/signal.rs#default_op` | 同模块上下文对齐/差异观察 | Some, drop, entry.is_some_and, inner
                .slots
                .iter, inner
                .slots
                .iter()
                .enumerate, inner
                .slots
                .iter()
                .enumerate()
                .find ...(+13) | continue_signals.contains, dump_signals.contains, ignore_signals.contains, stop_signals.contains, terminate_signals.contains |
| `libs/errno/src/lib.rs#from_i32` | `os/src/hal/trap/mod.rs#trap_handler_kernel` | 同模块上下文对齐/差异观察 | Errno::Other | crmd.plv, crmd::read, era::read, estat.cause, estat::read, panic! ...(+1) |
| `libs/errno/src/lib.rs#as_i32` | `os/src/hal/trap/mod.rs#enable_timer_interrupt` | 同模块上下文对齐/差异观察 | 内部闭环逻辑 | crmd::set_ie, ecfg::set_lie, get_timer_freq, sie::set_stimer, tcfg::set_en, tcfg::set_init_val ...(+2) |
| `kernel/src/dtb/pci.rs#install_irq_routing` | `os/src/hal/trap/mod.rs#trap_from_kernel` | 同模块上下文对齐/差异观察 | PCI_IRQ_ROUTING.lock, Some, Vec::new, child_key.extend_from_slice, child_key.into_boxed_slice, entry.child_address.len ...(+10) | era::read, estat::read, panic!, warn! |

#### 系统调用
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `kernel/src/syscalls/ipc.rs#encode_shmid64_ds` | `os/src/syscall/mem.rs#sys_shmget` | 同模块上下文对齐/差异观察 | meta.perm.mode.bits, write_i32, write_i64, write_u16, write_u32, write_u64 | ShmFlags::from_bits, ShmFlags::from_bits(shmflag & !0x1ff).unwrap, flags.contains, shm_create, shm_find |
| `kernel/src/syscalls/signal.rs#read_stack_t` | `user/src/syscall.rs#sys_basicsh` | 同模块上下文对齐/差异观察 | Err, Ok, SigAltStack::disabled, copy_from_user, copy_from_user(user, &mut raw).map_err, e.as_errno ...(+10) | "basic_testcode.sh\0".as_ptr, "busybox\0".as_ptr, "sh\0".as_ptr, [
                "busybox\0".as_ptr() as isize,
                "sh\0".as_ptr() as isize,
                "basic_testcode.sh\0".as_ptr() as isize,
                //"busybox_testcode.sh\0".as_ptr() as isize,
                //"./test-ltp.sh\0".as_ptr() as isize,
                //"ltp/testcases/bin/abort01\0".as_ptr() as isize,
                // "libctest_testcode.sh\0".as_ptr() as isize,
                0,
            ]
            .as_ptr, syscall6 |
| `hal/src/user.rs#patch_loongarch64_enosys_stub` | `os/src/syscall/mem.rs#sys_shmat` | 同模块上下文对齐/差异观察 | ENOSYS_STUB_PREFIX.len, bytes.get, bytes[off..off + patch.len()].copy_from_slice, patch.len, write_u32 | ShmFlags::from_bits, ShmFlags::from_bits(shmflag).unwrap, shm_attach, shmflg.contains |
| `kernel/src/syscalls/syslog.rs#set_console_level` | `user/src/syscall.rs#sys_busyboxsh` | 同模块上下文对齐/差异观察 | (1..=8).contains, Err, Ok, log::set_console_level, unreachable! | "sh\0".as_ptr, [
                test.as_ptr() as isize,
                "sh\0".as_ptr() as isize,
                cwd.as_ptr() as isize,
                //"./test-ltp.sh\0".as_ptr() as isize,
                //"ltp/testcases/bin/abort01\0".as_ptr() as isize,
                // "libctest_testcode.sh\0".as_ptr() as isize,
                0,
            ]
            .as_ptr, cwd.as_ptr, syscall6, test.as_ptr |

#### 文件系统
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `libs/vfs/src/net_socket.rs#recvfrom` | `os/src/fs/vfs/mod.rs#recvfrom` | 同板块同名 | Err, Ok, Some, buf.len, datagram_msg_flags, handle.interface_id ...(+18) | unimplemented! |
| `libs/extfs/src/inode.rs#rename` | `os/src/fs/vfs/mod.rs#rename` | 同板块同名 | Arc::ptr_eq, Err, Ok, copy_i_block, core::ptr::eq, crate::dir::is_dir_empty ...(+59) | unimplemented! |
| `libs/fatfs/src/fat.rs#set` | `os/src/fs/fstruct.rs#set` | 同板块同名 | (value as u16).to_le_bytes, Ok, Self::ensure_slot_present, Self::ensure_slot_present_mut, new.to_le_bytes, raw.to_le_bytes ...(+7) | Some, self.get_mut |
| `libs/extfs/src/file.rs#read_at` | `os/src/fs/vfs/mod.rs#read_at` | 同板块同名 | (offset + remaining as u64).min, (size - offset).min, (start + remaining).min, Ok, Vec::new, buf.is_empty ...(+25) | unimplemented! |

#### 设备驱动
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `general/src/dev/drivers/uart16550.rs#flush` | `os/src/drivers/virtio/blk.rs#flush` | 同板块同名 | Err, Ok, core::hint::spin_loop, guard.state_mut, self.kick_tx_locked, self.transmitter_empty ...(+1) | 内部闭环逻辑 |
| `general/src/dev/syscon.rs#get` | `os/src/hal/arch/uart.rs#get` | 同板块同名 | Arc::clone, SYSCONS
        .lock, SYSCONS
        .lock()
        .devices
        .iter, SYSCONS
        .lock()
        .devices
        .iter()
        .find, SYSCONS
        .lock()
        .devices
        .iter()
        .find(|registered| registered.handle.phandle == phandle)
        .map | Some, ptr.add, ptr.add(0).read_volatile, ptr.add(5).read_volatile |
| `general/src/dev/drivers/cfi_flash.rs#capabilities` | `os/src/net/socket/mod.rs#capabilities` | 同板块同名 | 内部闭环逻辑 | DeviceCapabilities::default |
| `general/src/dev/drivers/loopback.rs#mac_address` | `os/src/drivers/virtio/net.rs#mac_address` | 同板块同名 | 内部闭环逻辑 | self.dev.inner.lock, self.dev.inner.lock().mac_address |

#### 网络协议栈
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `libs/socket/src/state.rs#send` | `os/src/net/socket/tcp.rs#send` | 同板块同名 | Err, effective_send_deadline, io::send_datagram, io::send_seqpacket, io::send_stream, options.sender_identity.unwrap_or ...(+2) | Err, Ok, SOCKET_SET.with_socket_mut::<tcp::Socket, _, _>, ax_err!, ax_err_type!, self.block_on ...(+10) |
| `libs/socket/src/state.rs#accept` | `os/src/net/socket/listen_table.rs#accept` | 同板块同名 | Err, connection::accept_connection_socket, effective_recv_deadline, self.finish_result | Ok, ax_err!, get_addr_tuple, is_connected, is_connected(handle).then, self.tcp[port as usize].lock ...(+8) |
| `libs/socket/src/state.rs#connect` | `os/src/net/socket_impl.rs#connect` | 同板块同名 | connection::connect_datagram, connection::connect_seqpacket, connection::connect_stream, effective_send_deadline, self.finish_result | Err, Ok, SysErrNo::EOPNOTSUPP.into, crate::net::AxError::from, debug!, tcpsocket.lock ...(+1) |
| `libs/socket/src/state.rs#receive` | `os/src/net/socket/mod.rs#receive` | 同板块同名 | effective_recv_deadline, io::recv_datagram, io::recv_seqpacket, io::recv_stream, self.finish_result, self.passcred_enabled | AxNetRxToken, AxNetTxToken, Some, dev.can_transmit, dev.receive, dev.recycle_tx_buffers ...(+3) |

#### 文件系统/第三方库
仅参考项目中观察到该模块代表函数，目标项目没有同模块可对齐证据：
- 参考项目：`os/libs/lwext4_rsut/c/lwext4/src/ext4_balloc.c#ext4_balloc_alloc_block`，调用摘要：ext4_balloc_get_bgid_of_block, ext4_balloc_get_block_of_bgid, ext4_balloc_set_bitmap_csum, ext4_balloc_verify_bitmap_csum, ext4_bg_get_block_bitmap, ext4_bg_get_free_blocks_count ...(+19)
- 参考项目：`os/libs/lwext4_rsut/c/lwext4/src/ext4_journal.c#jbd_journal_prepare`，调用摘要：EXT4_BLOCK_ZERO, RB_FIND, TAILQ_FOREACH_REVERSE_SAFE, TAILQ_FOREACH_SAFE, TAILQ_REMOVE, ext4_bcache_clear_dirty ...(+17)
- 参考项目：`os/libs/lwext4_rsut/c/lwext4/src/ext4.c#ext4_dir_rm`，调用摘要：EXT4_MP_LOCK, EXT4_MP_UNLOCK, ext4_block_cache_write_back, ext4_dir_en_get_inode, ext4_dir_iterator_fini, ext4_dir_iterator_init ...(+19)
- 参考项目：`os/libs/lwext4_rsut/c/lwext4/src/ext4_dir_idx.c#ext4_dir_dx_split_index`，调用摘要：ext4_block_set, ext4_dir_dx_climit_get_count, ext4_dir_dx_climit_get_limit, ext4_dir_dx_climit_set_count, ext4_dir_dx_climit_set_limit, ext4_dir_dx_entry_get_hash ...(+10)

#### 网络协议栈/第三方库
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `libs/mygo-smoltcp/src/socket/tcp.rs#process` | `os/libs/smoltcp/src/socket/tcp.rs#process` | 同板块同名 | AckDelayTimer::Waiting, IpEndpoint::new, Self::random_seq_no, Self::rst_reply, Some, control.quash_psh ...(+49) | AckDelayTimer::Waiting, IpEndpoint::new, Self::random_seq_no, Self::rst_reply, Some, control.quash_psh ...(+42) |
| `libs/mygo-smoltcp/src/socket/tcp.rs#dispatch` | `os/libs/smoltcp/src/socket/tcp.rs#dispatch` | 同板块同名 | IpRepr::new, Ok, Some, TcpTimestampRepr::generate_reply_with_tsval, crate::phy::PacketMeta::default, cx.ip_mtu ...(+50) | IpRepr::new, Ok, Some, cx.ip_mtu, cx.now, emit ...(+45) |
| `libs/mygo-smoltcp/src/iface/interface/mod.rs#dispatch_ip` | `os/libs/smoltcp/src/iface/interface/mod.rs#dispatch_ip` | 同板块同名 | EthernetAddress, EthernetFrame::<&[u8]>::buffer_len, EthernetFrame::<&[u8]>::header_len, EthernetFrame::new_unchecked, Ipv4Packet::new_unchecked, Ok ...(+35) | EthernetAddress, EthernetFrame::<&[u8]>::buffer_len, EthernetFrame::<&[u8]>::header_len, EthernetFrame::new_unchecked, Ipv4Packet::new_unchecked, Ok ...(+35) |
| `libs/mygo-smoltcp/src/socket/dhcpv4.rs#dispatch` | `os/libs/smoltcp/src/socket/dhcpv4.rs#dispatch` | 同板块同名同结构 | Ok, Self::random_transaction_id, Some, cx.hardware_addr, cx.ip_mtu, cx.now ...(+12) | Ok, Self::random_transaction_id, Some, cx.hardware_addr, cx.ip_mtu, cx.now ...(+12) |

#### 第三方库
仅目标项目中观察到该模块代表函数，参考项目没有同模块可对齐证据：
- 目标项目：`libs/efi/src/c/efi_global.c#efi_system_table_firmware_vendor`，调用摘要：efi_system_table_is_valid
- 目标项目：`libs/efi/src/lib.rs#firmware_vendor`，调用摘要：Some, core::ptr::null, core::slice::from_raw_parts, efi_system_table_firmware_vendor, ptr.is_null
- 目标项目：`libs/efi/src/lib.rs#firmware_vendor_cstr16`，调用摘要：firmware_vendor
- 目标项目：`general/src/vfs/procfs.rs#cpuinfo_vendor_from_compatible`，调用摘要：compatible
        .split_once, compatible
        .split_once(',')
        .map, compatible
        .split_once(',')
        .map(|(vendor, _)| vendor)
        .unwrap_or

#### 通用模块/其他
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `arch/src/loongarch64/boot.rs#_start` | `user/src/lib.rs#_start` | 同板块同名 | naked_asm! | ((argv + i * core::mem::size_of::<usize>()) as *const usize).read_volatile, ((str_start + *i) as *const u8).read_volatile, (0usize..)
            .find, (0usize..)
            .find(|i| unsafe { ((str_start + *i) as *const u8).read_volatile() == 0 })
            .unwrap, HEAP.lock, HEAP.lock()
            .init ...(+11) |
| `libs/log/src/lib.rs#write_str` | `user/src/console.rs#write_str` | 同板块同名 | Ok, ch.len_utf8, s.as_bytes, s.char_indices, s.len, self.buf[self.len..self.len + copy_len].copy_from_slice | Err, Ok, s.as_bytes, s.as_bytes().iter, self.0.len, self.0.push_back ...(+1) |
| `arch/src/riscv64/loader.rs#write_str` | `os/src/hal/utils/console.rs#write_str` | 同板块同名 | Ok, s.as_bytes, s.len, s.len().min, self.buf.len, self.buf[self.len - 3..self.len].copy_from_slice ...(+1) | Ok, console_putchar, s.chars |
| `libs/acpi/src/aml/namespace.rs#get` | `os/src/system.rs#get` | 同板块同名 | AmlError::ObjectDoesNotExist, Err, Ok, assert!, level.values.get, object.clone ...(+3) | change_to_c, dn.as_bytes, ma.as_bytes, nn.as_bytes, re.as_bytes, sn.as_bytes ...(+1) |


好的，我将遵循审计指令，基于提供的证据材料，对本案进行最终同源性定性仲裁。

### 1. 宏观判定
- **定性结论**: **独立开发，底层架构显著差异** (客观 AST 相似度: 15.36%，共同 AST 指纹: 1587；唯一 AST 指纹: 目标项目 8484，参考项目 3437；覆盖率: 目标项目 18.71%，参考项目 46.17%)。高覆盖率主要由双方共用的第三方开源网络协议栈贡献，与核心内核逻辑无关。在核心系统板块（如进程管理），双方未检出任何共同 AST 指纹，表明实现路径存在根本性差异。

### 2. 全模块覆盖矩阵解读
- **进程管理**: 双方均存在。共同 AST 指纹为 0，双方覆盖率均为 0.0%。这表明双方在进程创建、调度、退出、信号处理等核心逻辑上，代码实现结构完全不同。证据强度：**未形成**。
- **任务调度/异步运行时**: 双方均存在。共同 AST 指纹为 0，双方覆盖率均为 0.0%。目标项目拥有更复杂的调度域和异步运行时抽象，与参考项目的简单调度器在结构上无重合。证据强度：**未形成**。
- **内存管理**: 双方均存在。共同 AST 指纹为 3，覆盖率极低（目标 0.19%，参考 1.03%）。重合点源于通用函数名（如 `alloc`），但调用图谱完全不同，属于领域内偶然同名。证据强度：**弱**。
- **中断/异常处理**: 双方均存在。共同 AST 指纹为 0，双方覆盖率均为 0.0%。目标项目的中断处理与驱动框架深度绑定，参考项目则相对独立，结构无重合。证据强度：**未形成**。
- **系统调用**: 双方均存在。共同 AST 指纹为 0，双方覆盖率均为 0.0%。尽管存在大量同名系统调用入口，但内部实现路径（参数解析、权限校验、内核对象访问）的 AST 结构无重合。证据强度：**未形成**。
- **文件系统**: 双方均存在。共同 AST 指纹为 3，覆盖率极低（目标 0.17%，参考 1.6%）。重合点源于 `read_at`、`write_at` 等通用接口名，但底层操作的对象（ExtFS/FAT vs. VFS 抽象）完全不同。证据强度：**弱**。
- **设备驱动**: 双方均存在。共同 AST 指纹为 2，覆盖率低（目标 0.31%，参考 4.26%）。重合点源于 `flush`、`get` 等通用驱动方法名，但操作的硬件和数据结构（UART/CFI Flash vs. VirtIO）完全不同。证据强度：**弱**。
- **网络协议栈**: 双方均存在。共同 AST 指纹为 1，覆盖率低（目标 0.26%，参考 0.84%）。该重合点为 `send` 等通用接口，但底层 Socket 状态管理模型差异巨大。证据强度：**弱**。
- **第三方库/公共依赖**: 此类别是双方高覆盖率的唯一来源。`网络协议栈/第三方库` 板块共同 AST 指纹高达 1568，覆盖率（目标 84.03%，参考 86.87%）。这明确指向双方均复用了开源 `smoltcp` 协议栈或其衍生版本，属于合法的公共依赖。证据强度：**强（但属于第三方库复用，非核心逻辑抄袭）**。

### 3. 底层 AST 关键证据指纹表
| 所属板块 | 目标项目路径 | 参考项目路径 | 证据类型 | 技术说明 |
| :--- | :--- | :--- | :--- | :--- |
| 第三方库/公共依赖 | `libs/mygo-smoltcp/src/socket/tcp.rs#process` | `os/libs/smoltcp/src/socket/tcp.rs#process` | 同源指纹 | 双方均基于开源 smoltcp 协议栈。调用图谱高度一致（如 `AckDelayTimer::Waiting`, `IpEndpoint::new`, `Self::random_seq_no`），属于对同一开源代码的修改或引用。 |
| 第三方库/公共依赖 | `libs/mygo-smoltcp/src/iface/interface/mod.rs#dispatch_ip` | `os/libs/smoltcp/src/iface/interface/mod.rs#dispatch_ip` | 同源指纹 | 同上。`EthernetFrame::new_unchecked`, `Ipv4Packet::new_unchecked` 等调用序列完全一致，确证同源。 |
| 第三方库/公共依赖 | `libs/mygo-smoltcp/src/socket/dhcpv4.rs#dispatch` | `os/libs/smoltcp/src/socket/dhcpv4.rs#dispatch` | 同源指纹 | 同上。`Self::random_transaction_id`, `cx.hardware_addr` 等调用序列完全一致，确证同源。 |
| 内存管理 | `libs/allocator/src/slab.rs#alloc` | `os/src/sync/banker_algo.rs#alloc` | 弱信号 | 同名函数 `alloc`，但目标项目实现 Slab 分配器，参考项目实现银行家算法死锁检测，调用图谱完全不同。 |
| 文件系统 | `libs/extfs/src/file.rs#read_at` | `os/src/fs/vfs/mod.rs#read_at` | 弱信号 | 同名函数 `read_at`，但目标项目操作 ExtFS 块设备，参考项目返回 `unimplemented!`，无实质重合。 |
| 设备驱动 | `general/src/dev/drivers/uart16550.rs#flush` | `os/src/drivers/virtio/blk.rs#flush` | 弱信号 | 同名函数 `flush`，但目标项目刷新 UART 硬件，参考项目刷新 VirtIO 块设备，硬件抽象与操作逻辑完全不同。 |

### 4. 核心代码调用流对比剖析
* **【进程管理/futex 机制】**
* **底层技术析理**: 控方核心指控点。目标项目 `futex_wait` 基于 `FutexWaitState` 状态机和 `FUTEX_TABLE` 全局表，通过 `Arc::downgrade` 创建弱引用管理等待队列，并支持 PI 锁。参考项目 `futex_wait` 基于 `FUTEX_QUEUE` 和 `VecDeque`，通过 `block_current_and_run_next` 直接切换任务。两者在**同步原语**（状态机 vs. 直接阻塞）、**内存模型**（弱引用计数 vs. 强引用升级）和**调度策略**上存在根本性架构差异，并非简单的变量重命名。
* **代码溯源链接**:
  - 目标项目: `kernel/src/syscalls/process.rs#futex_wait`
  - 参考项目: `os/src/task/futex.rs#futex_wait`

* **【进程管理/进程退出】**
* **底层技术析理**: 目标项目 `sys_exit` 通过 `SyscallContext` 获取任务引用，调用 `sched::operation::exit` 进入调度器模块的退出逻辑，体现了清晰的**分层架构**。参考项目 `sys_exit` 直接调用 `exit_current_and_run_next` 函数，该函数耦合了进程退出和任务切换，是典型的**单体式设计**。两者在**对象管理模型**和**控制流分发**上截然不同。
* **代码溯源链接**:
  - 目标项目: `kernel/src/syscalls/process.rs#sys_exit`
  - 参考项目: `os/src/syscall/process.rs#sys_exit`

* **【进程管理/进程标识符获取】**
* **底层技术析理**: 目标项目 `sys_getpid` 实现了复杂的 PID 回退逻辑（`tgid_cached` -> `pid_root_cached` -> `thread_group().tgid()`），体现了对 Linux PID namespace 和线程组语义的深度兼容。参考项目 `sys_getpid` 通过简单的指针链 `current_task().unwrap().process.upgrade().unwrap().getpid()` 获取 PID。两者在**语义复杂度**和**抽象层次**上存在巨大差异。
* **代码溯源链接**:
  - 目标项目: `kernel/src/syscalls/process.rs#sys_getpid`
  - 参考项目: `os/src/syscall/process.rs#sys_getpid`

### 5. 单方模块与降权项
- **单方模块**: `文件系统/第三方库` (如 lwext4) 仅存在于参考项目；`第三方库` (如 efi) 仅存在于目标项目。这些模块是双方独立引入的外部依赖，不构成相互抄袭证据。
- **降权项**: 控方提出的 `sys_umask`, `sys_getuid` 等大量同名系统调用，以及 `wait_for_file_readiness` vs `sys_clock_nano_sleep` 等跨板块对比，均属于**标准 POSIX 接口名称**或**跨领域硬凑**。同名源于 Linux ABI 规范，而非代码复用。`Arc::clone`, `drop`, `debug!` 等是 Rust 语言的标准惯用法，不具备独创性。这些项均降权为弱信号或直接排除。

### 6. 保守结论
综合全模块 AST 指纹分析，目标项目与参考项目的整体相似度仅为 15.36%，且高覆盖率完全由双方共用的开源第三方网络协议栈（smoltcp）贡献。在操作系统核心板块，如进程管理、任务调度、中断处理等，双方共同 AST 指纹为零，覆盖率均为 0.0%，表明不存在结构级的代码复用。控方基于标准系统调用同名、Rust 语言惯用法和跨板块硬凑提出的指控，在底层 AST 证据上不成立。双方在内存模型、同步策略、抽象分层等关键架构设计上展现出显著差异，符合独立开发的特征。因此，本案的同源性风险极低，不构成抄袭或违规复用。

------------------------------

