# 🛡️ OS 内核赛自动化代码级架构审计报告

**审计时间**: `2026-07-02 14:52:13`
**参赛作品名称**: `2026-T2026193599910347-XX`
**年份**: `2026`
**学校**: `XX`
**队伍名称**: `T2026193599910347`
**比赛名字**: `OS-Competition`
**比赛子赛道**: `Kernel-Implementation`
**仓库地址**: `https://gitlab.eduxiji.net/educg-group-43501-3132633/T2026193599910347-3076`
**推测开发语言**: `Rust / C (由AST探针自动识别)`
**特征函数量**: `1009`

## ⚔️ 底层代码同源性对比 (AST Homology)
> 本部分由专门的代码审查智能体，对该项目与符合年份条件的历届最高相似度项目进行控制流与底层调用对比。新版报告会先给出确定性的全模块覆盖矩阵，再给出 Agent 的分模块解释，避免只围绕少量抽样函数展开。

### 🏆 Top 2 底层对标: vs `2025-sudo_pacman_Syu-同济大学`（同济大学 / sudo_pacman_Syu）

#### 对方作品信息

**对方年份**: `2025`
**对方学校**: `同济大学`
**对方队伍名称**: `sudo_pacman_Syu`
**对方比赛名字**: `OS-Competition`
**对方比赛子赛道**: `Kernel-Implementation`
**对方仓库地址**: `https://gitlab.eduxiji.net/educg-group-36002-2710490/T202510247995532-2817`

> 双向覆盖率：共同 AST 指纹: 11；唯一 AST 指纹: 目标项目 867，参考项目 1840；覆盖率: 目标项目 1.27%，参考项目 0.6%

## 全模块对比总览（确定性证据）

> 总体覆盖率：共同 AST 指纹: 11；唯一 AST 指纹: 目标项目 867，参考项目 1840；覆盖率: 目标项目 1.27%，参考项目 0.6%

### 1. 模块覆盖矩阵
| 模块 | 目标函数数 | 参考函数数 | 共同唯一 AST 指纹 | Target 覆盖 | Base 覆盖 | 状态 |
| :--- | ---: | ---: | ---: | ---: | ---: | :--- |
| 进程管理 | 65 | 154 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |
| 任务调度/异步运行时 | 89 | 164 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |
| 内存管理 | 132 | 261 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |
| 中断/异常处理 | 23 | 28 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |
| 系统调用 | 170 | 58 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |
| 文件系统 | 244 | 318 | 1 | 0.45% | 0.34% | 双方存在且有结构重合 |
| 设备驱动 | 42 | 162 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |
| 网络协议栈 | 14 | 51 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |
| 文件系统/第三方库 | 102 | 48 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |
| 网络协议栈/第三方库 | 60 | 0 | 0 | 0.0% | 0.0% | 仅目标项目体现 |
| 第三方库 | 6 | 740 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |
| 通用模块/其他 | 62 | 188 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |

### 2. 分模块证据清单

#### 进程管理
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `modules/axruntime/src/process.rs#waitpid` | `src/kernel/syscall/procops.rs#waitpid` | 同板块同名 | Err, Ok, crate::mm::release_user_root, crate::runtime::wait_timeout_ms, crate::syscall::can_block_current, current_pid ...(+5) | core::ptr::null_mut, sys_wait4 |
| `modules/axruntime/src/futex.rs#wait` | `src/kernel/task/process.rs#wait` | 同板块同名 | Err, Ok, clear_slot_if_empty, make_key, mm::translate_user_ptr, mm::translate_user_ptr(root_pa, uaddr, 4, UserAccess::Read).ok_or ...(+6) | Err, Ok, ProcessList::get, ProcessList::get().write, Some, assert! ...(+10) |
| `modules/axruntime/src/process.rs#init_process` | `src/kernel/task/process_list.rs#init_process` | 同板块同名 | 内部闭环逻辑 | self.init.as_ref, self.init.as_ref().unwrap |
| `modules/axfs/src/procfs.rs#lookup` | `src/fs/procfs.rs#lookup` | 同板块同名 | Ok | (name == &***dentry.name()).then, Ok, block_on, dentry.name, lock.prove, node.unwrap ...(+4) |

#### 任务调度/异步运行时
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `modules/axruntime/src/runtime.rs#yield_now` | `src/kernel/task/thread.rs#yield_now` | 同板块同名 | NEED_RESCHED.store, RUN_QUEUE.push, crate::scheduler::switch, task::set_context, task::task_ptr, task::transition_state ...(+4) | Poll::Ready, cx.waker, cx.waker().wake_by_ref, self.as_mut |
| `modules/axruntime/src/futex.rs#wake` | `src/kernel/task.rs#wake` | 同板块同名 | Ok, clear_slot_if_empty, make_key, mm::translate_user_ptr, mm::translate_user_ptr(root_pa, uaddr, 4, UserAccess::Read).ok_or, runtime::wake_all ...(+3) | self.wake_by_ref |
| `modules/axruntime/src/runtime.rs#wake_all` | `src/kernel/task/futex.rs#wake_all` | 同板块同名 | NEED_RESCHED.store, RUN_QUEUE.push, crate::println!, queue.notify_one, queue.push, task::set_wait_reason ...(+1) | Ok, User::<RobustList>::with_addr, UserPointer::new, UserPointer::new(User::<RobustList>::with_addr(entry_ptr))?.read, futex_wake, self.futex_addr |
| `modules/axruntime/src/syscall.rs#dispatch` | `src/kernel/task/signal.rs#handle` | 同模块上下文对齐/差异观察 | Err, sys_accept, sys_accept4, sys_bind, sys_brk, sys_chdir ...(+113) | ProcessList::get, ProcessList::get().read, ProcessList::get().read().await.prove, Runtime::block_till_woken, SIGNAL_IGNORE!, SIGNAL_STOP! ...(+19) |

#### 内存管理
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `modules/axfs/src/fat32.rs#truncate` | `src/fs/tmpfs.rs#truncate` | 同板块同名 | Err, Ok, core::cmp::min, inode_cluster, inode_is_dir, self.alloc_cluster ...(+7) | Instant::now, Ok, block_on, self.mtime.lock, self.pages.resize, self.rwsem.write ...(+1) |
| `modules/axruntime/src/mm.rs#alloc` | `src/kernel/mem/allocator.rs#alloc` | 同板块同名 | self.alloc_contiguous | Page::alloc_at_least, SLAB_ALLOCATOR.alloc, core::ptr::null_mut, layout.size, layout.size().next_power_of_two, page.as_memblk ...(+4) |
| `modules/axruntime/src/mm.rs#addr` | `src/kernel/mem/access.rs#addr` | 同板块同名 | PhysAddr | 内部闭环逻辑 |
| `modules/axruntime/src/mm.rs#flags` | `src/kernel/mem/page_alloc/raw_page.rs#flags` | 同板块同名 | 内部闭环逻辑 | self.as_ref |

#### 中断/异常处理
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `modules/axruntime/src/trap.rs#enable_interrupts` | `src/driver/ahci/control.rs#enable_interrupts` | 同板块同名 | read_sstatus, write_sstatus | memory_barrier, self.read, self.write |
| `modules/axruntime/src/cpu.rs#wait_for_interrupt` | `src/driver/serial.rs#wait_for_interrupt` | 同板块同名 | asm! | pin!, self.disable_interrupts, self.enable_interrupts, self.working.lock_irq, wait.as_mut, wait.as_mut().add_to_wait_list |
| `modules/axruntime/src/runtime.rs#idle_loop` | `src/kernel/terminal.rs#commit_char` | 同模块上下文对齐/差异观察 | axnet::poll, axnet::request_poll, crate::async_exec::poll, crate::cpu::wait_for_interrupt, crate::time::uptime_ms, crate::trap::enable_interrupts ...(+5) | inner.termio.echok, inner.termio.echoke, inner.termio.icanon, inner.termio.icrnl, inner.termio.iexten, inner.termio.igncr ...(+16) |
| `modules/axruntime/src/trap.rs#enable_timer_interrupt` | `src/kernel/terminal.rs#read` | 同模块上下文对齐/差异观察 | TIMER_INTERVAL.store, read_sie, read_sstatus, read_time, sbi::set_timer, write_sie ...(+1) | Err, Thread::current, Thread::current().signal_list.has_pending_signal, buffer.available, buffer.available().min, buffer.fill ...(+17) |

#### 系统调用
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `apps/udp_echo/src/main.rs#_start` | `src/kernel/mem/mm_list.rs#set_break` | 同模块上下文对齐/差异观察 | MMSG_A.as_ptr, MMSG_A.len, MMSG_B.as_ptr, MMSG_B.len, REPLY_MSG.as_ptr, REPLY_MSG.len ...(+41) | Some, VRange::new, assert!, drop, inner.locked.lock, locked.areas.check_overlapping_range ...(+5) |
| `apps/shell/src/main.rs#cmd_head_tail` | `src/kernel/syscall/sysinfo.rs#newuname` | 同模块上下文对齐/差异观察 | arg_eq, arg_to_cstr, buf.as_mut_ptr, buf.len, cur.len, line_count.saturating_sub ...(+9) | UserPointerMut::new, buffer.write, copy_cstr_to_array |
| `modules/axruntime/src/syscall.rs#recvmsg_inner` | `src/kernel/syscall/net.rs#get_sockopt` | 同模块上下文对齐/差异观察 | Err, Ok, UserPtr::<MsgHdr>::new, UserPtr::<MsgHdr>::new(msg)
        .read, UserPtr::<MsgHdr>::new(msg)
        .read(root_pa)
        .ok_or, UserPtr::new ...(+25) | Ok, UserPointerMut::new, UserPointerMut::new(optlen)?.write, UserPointerMut::new(optval.cast())?.write, optval.cast, size_of::<u32> |
| `modules/axruntime/src/syscall.rs#sendmsg_inner` | `src/kernel/syscall/net.rs#read_socket_addr` | 同模块上下文对齐/差异观察 | Err, Ok, Some, UserPtr::<MsgHdr>::new, UserPtr::<MsgHdr>::new(msg)
        .read, UserPtr::<MsgHdr>::new(msg)
        .read(root_pa)
        .ok_or ...(+18) | Err, IpAddr::V4, Ipv4Addr::from_bits, Ok, SockDomain::try_from, SocketAddr::new ...(+6) |

#### 文件系统
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `modules/axfs/src/fat32.rs#lookup` | `src/kernel/vfs/inode.rs#lookup` | 同板块同名 | Err, Ok, Some, eq_ignore_ascii_case, fat_inode, inode_cluster ...(+3) | Err, self.is_dir |
| `modules/axfs/src/memfs.rs#readlink` | `src/fs/tmpfs.rs#readlink` | 同板块同名 | Err, self.node, self.node(inode).ok_or | buffer
            .fill, buffer
            .fill(self.target.as_ref())
            .map, result.allow_partial, self.target.as_ref |
| `modules/axfs/src/devfs.rs#create` | `src/fs/fat32.rs#create` | 同板块同名 | Err | Arc::get_mut_unchecked, Arc::new_cyclic, BTreeMap::new, BlockDevice::get, ByteBuffer::from, ClusterIterator::new ...(+22) |
| `modules/axfs/src/fat32.rs#create` | `src/kernel/vfs/dentry.rs#create` | 同板块同名 | Err | Arc::from, Arc::new, AtomicPtr::default, AtomicU64::new, RCUPointer::empty, RCUPointer::new ...(+1) |

#### 设备驱动
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `modules/axruntime/src/virtio_net.rs#recv` | `src/driver/virtio/virtio_net.rs#recv` | 同板块同名 | Err, Ok, RX_LOCK.lock, VIRTIO_NET_BASE.load, VIRTIO_NET_READY.load, VIRTIO_NET_RX_QUEUE.get ...(+10) | Err, Ok, RxBuffer::VirtIOBuffer, self.receive, self.receive().map_or_else |
| `modules/axruntime/src/virtio_net.rs#send` | `src/driver/virtio/virtio_net.rs#send` | 同板块同名 | Err, Ok, TX_LOCK.lock, VIRTIO_NET_BASE.load, VIRTIO_NET_READY.load, VIRTIO_NET_TX_BUF.data.as_ptr ...(+10) | net::TxBuffer::from, self.send, self.send(net::TxBuffer::from(data))
            .map_err |
| `modules/axruntime/src/virtio_blk.rs#get` | `src/kernel/block.rs#get` | 同板块同名 | self.inner.get | BLOCK_DEVICE_LIST.lock, BLOCK_DEVICE_LIST.lock().get, BLOCK_DEVICE_LIST.lock().get(&devid).cloned, BLOCK_DEVICE_LIST.lock().get(&devid).cloned().ok_or |
| `modules/axruntime/src/virtio_net.rs#get` | `src/kernel/chardev.rs#get` | 同板块同名 | self.inner.get | CHAR_DEVICES.lock, CHAR_DEVICES.lock().get, CHAR_DEVICES.lock().get(&devid).cloned |

#### 网络协议栈
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `modules/axruntime/src/runtime.rs#log_net_event` | `src/net/socket/tcp.rs#poll_impl` | 同模块上下文对齐/差异观察 | crate::println! | Ok, PollEvent::empty, Some, block_on, events.contains, iface.lock ...(+12) |
| `apps/net_bench/src/main.rs#write_u64` | `src/net/socket/udp.rs#bind` | 同模块上下文对齐/差异观察 | buf.len, write_stdout | BoundSocket::new_bind_all, BoundSocket::new_bind_single, Err, Ok, Self::bind_impl, Some ...(+23) |
| `apps/tcp_echo/src/main.rs#iov_matches` | `src/net/socket/tcp.rs#accept_impl` | 同模块上下文对齐/差异观察 | a.len, b.len, expected.len, slices_equal | Arc::new, BoundSocket::new_single, Err, Ok, RwLock::new, SocketAddr::from ...(+15) |
| `apps/tcp_echo/src/main.rs#slices_equal` | `src/net/socket/udp.rs#drop` | 同模块上下文对齐/差异观察 | a.iter, a.iter().enumerate, a.len, b.len | UDP_PORT_MAP.lock, UDP_PORT_MAP.lock().remove, block_on, bound_socket_guard.as_ref, bound_socket_guard.as_ref().unwrap, bound_socket_guard.is_none ...(+16) |

#### 文件系统/第三方库
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `modules/axfs/src/ext4.rs#create` | `src/fs/ext4.rs#create` | 同板块同名 | Err, Ok, init_inode_extents, inode_mode_type, name.is_empty, name.len ...(+4) | Arc::downgrade, Arc::new, AtomicMode::new, AtomicNlink::new, AtomicU32::new, AtomicU64::new ...(+25) |
| `modules/axfs/src/ext4.rs#truncate` | `src/fs/ext4.rs#truncate` | 同板块同名 | Err, inode_mode_type, self.allocate_data_block, self.fs_block_size, self.map_block, self.map_block(&inode_meta, block_index)?.is_none ...(+2) | Instant::now, Ok, block_on, ext4fs.inner.write, self.mtime.lock, self.page_cache.resize ...(+8) |
| `modules/axfs/src/ext4.rs#lookup` | `src/fs/ext4.rs#lookup` | 同板块同名 | Err, Ok, Some, inode_mode_type, name.as_bytes, self.read_inode ...(+1) | AtomicMode::new, AtomicNlink::new, AtomicU32::new, AtomicU64::new, Err, InodeMode::BLOCKDEV.bits ...(+30) |
| `modules/axfs/src/ext4.rs#write_block` | `src/fs/ext4.rs#write_block` | 同板块同名 | Err, Ok, data.len, data[offset..offset + self.block_size].copy_from_slice, self.data.borrow_mut | self
            .device
            .write_some |

#### 网络协议栈/第三方库
仅目标项目中观察到该模块代表函数，参考项目没有同模块可对齐证据：
- 目标项目：`modules/axnet/src/smoltcp_impl.rs#run_tcp_loopback`，调用摘要：Config::new, Err, EthernetAddress, EthernetAddress([0x02, 0x00, 0x00, 0x00, 0x00, 0x01]).into, Instant::from_millis, Interface::new ...(+35)
- 目标项目：`modules/axnet/src/smoltcp_impl.rs#poll`，调用摘要：IcmpEndpoint::Ident, Icmpv4Packet::new_checked, Icmpv4Packet::new_unchecked, Icmpv4Repr::parse, Instant::from_millis, IpAddress::Ipv4 ...(+31)
- 目标项目：`modules/axnet/src/smoltcp_impl.rs#socket_poll`，调用摘要：NET_NEED_POLL.store, NET_STATE.as_mut, Ok, Some, matches!, set_socket_connecting ...(+11)
- 目标项目：`modules/axnet/src/smoltcp_impl.rs#socket_send`，调用摘要：Err, IpEndpoint::new, NET_NEED_POLL.store, NET_STATE.as_mut, Ok, buf.len ...(+9)

#### 第三方库
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `crates/axvfs/src/lib.rs#empty` | `crates/posix_types/src/signal/sig_action.rs#empty` | 同板块同名 | 内部闭环逻辑 | Self |
| `crates/axvfs/src/lib.rs#truncate` | `crates/posix_types/src/open.rs#truncate` | 同板块同名 | Err | self.contains |
| `crates/axvfs/src/lib.rs#set_name` | `crates/eonix_hal/src/arch/riscv64/trap/default.rs#_default_trap_entry` | 同模块上下文对齐/差异观察 | Err, Ok, name.len, self.name[..len].copy_from_slice | naked_asm! |
| `crates/axvfs/src/lib.rs#new` | `crates/eonix_percpu/eonix_percpu_macros/src/lib.rs#define_percpu_impl` | 同模块上下文对齐/差异观察 | 内部闭环逻辑 | ["u8", "u16", "u32", "u64", "usize"].contains, attrs.is_empty, format_ident!, get_percpu_pointer, panic!, parse2::<ItemStatic> ...(+6) |

#### 通用模块/其他
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `modules/axruntime/src/console.rs#write_str` | `src/io.rs#write_str` | 同板块同名 | Ok, s.bytes, sbi::console_putchar | Err, Ok, s.as_bytes, self.0.fill |
| `modules/axruntime/src/user.rs#build_user_stack` | `src/kernel/user/dataflow.rs#new` | 同模块上下文对齐/差异观察 | Err, Ok, argv_start.saturating_sub, envp_start.saturating_sub, read_user_cstr, read_user_usize ...(+5) | CheckedUserPointer::new, Err, Ok, asm!, assert_preempt_enabled! |
| `apps/shell/src/main.rs#read_line` | `src/kernel/user/dataflow.rs#write` | 同模块上下文对齐/差异观察 | buf.len, read_byte, write_stdout | Err, Ok, asm!, assert_preempt_enabled! |
| `apps/shell/src/main.rs#join_path` | `src/kernel/user/dataflow.rs#read` | 同模块上下文对齐/差异观察 | Some, base.len, name.len, out.as_ptr, out.len, out[idx..idx + base.len()].copy_from_slice ...(+1) | Err, Ok, asm!, assert_preempt_enabled! |


#### 1. 宏观判定
- **定性结论**: 底层架构显著差异 / 独立开发 (相似度: 0.41%，共同 AST 指纹: 11；唯一 AST 指纹: 目标项目 867，参考项目 1840；覆盖率: 目标项目 1.27%，参考项目 0.6%)

#### 2. 全模块覆盖矩阵解读

- **进程管理**
  - 双方是否都存在该模块：是
  - 是否存在共同 AST 指纹：否（共同唯一 AST 指纹: 0）
  - Target/Base 覆盖率说明：覆盖率均为 0.0%，表明双方在该模块的底层实现结构上无任何重合。
  - 证据强度：未形成

- **任务调度/异步运行时**
  - 双方是否都存在该模块：是
  - 是否存在共同 AST 指纹：否（共同唯一 AST 指纹: 0）
  - Target/Base 覆盖率说明：覆盖率均为 0.0%，表明双方调度与异步机制的底层实现结构无重合。
  - 证据强度：未形成

- **内存管理**
  - 双方是否都存在该模块：是
  - 是否存在共同 AST 指纹：否（共同唯一 AST 指纹: 0）
  - Target/Base 覆盖率说明：覆盖率均为 0.0%，表明双方内存管理底层实现结构无重合。
  - 证据强度：未形成

- **中断/异常处理**
  - 双方是否都存在该模块：是
  - 是否存在共同 AST 指纹：否（共同唯一 AST 指纹: 0）
  - Target/Base 覆盖率说明：覆盖率均为 0.0%，表明双方中断与异常处理底层实现结构无重合。
  - 证据强度：未形成

- **系统调用**
  - 双方是否都存在该模块：是
  - 是否存在共同 AST 指纹：否（共同唯一 AST 指纹: 0）
  - Target/Base 覆盖率说明：覆盖率均为 0.0%，表明双方系统调用分发与处理底层实现结构无重合。
  - 证据强度：未形成

- **文件系统**
  - 双方是否都存在该模块：是
  - 是否存在共同 AST 指纹：是（共同唯一 AST 指纹: 1）
  - Target/Base 覆盖率说明：目标覆盖率 0.45%，参考覆盖率 0.34%，均处于噪声级，表明仅存在极微弱的孤立结构重合。
  - 证据强度：弱

- **设备驱动**
  - 双方是否都存在该模块：是
  - 是否存在共同 AST 指纹：否（共同唯一 AST 指纹: 0）
  - Target/Base 覆盖率说明：覆盖率均为 0.0%，表明双方设备驱动底层实现结构无重合。
  - 证据强度：未形成

- **网络协议栈**
  - 双方是否都存在该模块：是
  - 是否存在共同 AST 指纹：否（共同唯一 AST 指纹: 0）
  - Target/Base 覆盖率说明：覆盖率均为 0.0%，表明双方网络协议栈底层实现结构无重合。
  - 证据强度：未形成

- **第三方库/公共依赖**
  - 双方是否都存在该模块：是
  - 是否存在共同 AST 指纹：否（共同唯一 AST 指纹: 0）
  - Target/Base 覆盖率说明：覆盖率均为 0.0%，表明双方使用的第三方库或公共依赖在底层结构上无重合。
  - 证据强度：未形成

#### 3. 底层 AST 关键证据指纹表

| 所属板块 | 目标项目路径 | 参考项目路径 | 证据类型 | 技术说明 |
| :--- | :--- | :--- | :--- | :--- |
| 进程管理 | `modules/axruntime/src/process.rs#waitpid` | `src/kernel/syscall/procops.rs#waitpid` | 弱信号（同名异构） | 目标依赖 `mm::UserPtr::new(status).write(root_pa, ...)` 物理地址写回；参考仅转发 `sys_wait4`，底层内存抽象完全不同。 |
| 进程管理 | `modules/axruntime/src/futex.rs#wait` | `src/kernel/task/process.rs#wait` | 弱信号（同名异构） | 目标为 futex 等待，依赖 `mm::translate_user_ptr` 和 `FUTEX_KEYS.iter`；参考为进程等待，依赖 `ProcessList` 和 `condvar`，功能与结构均不同。 |
| 进程管理 | `modules/axruntime/src/process.rs#init_process` | `src/kernel/task/process_list.rs#init_process` | 弱信号（同名异构） | 目标为内部闭环逻辑；参考为 `self.init.as_ref().unwrap()`，实现方式与复杂度不同。 |
| 进程管理 | `modules/axfs/src/procfs.rs#lookup` | `src/fs/procfs.rs#lookup` | 弱信号（同名异构） | 目标直接返回 `Ok`（存根）；参考包含 `block_on`、`lock.prove()`、`entries.access(...).iter` 等完整 VFS 操作。 |
| 进程管理 | `modules/axfs/src/procfs.rs#root` | `src/fs/procfs.rs#root` | 弱信号（同名异构） | 目标直接返回 `Ok`（存根）；参考依赖 `ProcFsMountCreator::get` 和 `vfs.root_node.clone`。 |
| 文件系统 | `modules/axfs/src/fat32.rs#lookup` | `src/kernel/vfs/inode.rs#lookup` | 弱信号（同名异构） | 目标依赖 `eq_ignore_ascii_case`、`fat_inode` 等 FAT32 特有逻辑；参考仅检查 `self.is_dir`，实现路径不同。 |
| 文件系统 | `modules/axfs/src/memfs.rs#readlink` | `src/fs/tmpfs.rs#readlink` | 弱信号（同名异构） | 目标依赖 `self.node(inode).ok_or`；参考依赖 `buffer.fill(self.target.as_ref())`，内部数据结构不同。 |
| 文件系统 | `modules/axfs/src/ext4.rs#create` | `src/fs/ext4.rs#create` | 弱信号（同名异构） | 目标依赖 `init_inode_extents`、`inode_mode_type`；参考依赖 `Arc::new_cyclic`、`AtomicMode::new` 等大量原子类型和智能指针。 |
| 文件系统 | `modules/axfs/src/ext4.rs#truncate` | `src/fs/ext4.rs#truncate` | 弱信号（同名异构） | 目标依赖 `self.allocate_data_block`、`self.map_block`；参考依赖 `self.page_cache.resize`、`block_on`，资源管理模型不同。 |
| 文件系统 | `modules/axfs/src/ext4.rs#lookup` | `src/fs/ext4.rs#lookup` | 弱信号（同名异构） | 目标依赖 `self.read_inode`、`inode_mode_type`；参考依赖 `AtomicMode::new`、`InodeMode::BLOCKDEV.bits` 等，构造方式不同。 |
| 文件系统 | `modules/axfs/src/ext4.rs#write_block` | `src/fs/ext4.rs#write_block` | 弱信号（同名异构） | 目标依赖 `data[offset..].copy_from_slice` 和 `self.data.borrow_mut`；参考依赖 `self.device.write_some`，I/O 抽象层不同。 |
| 设备驱动 | `modules/axruntime/src/virtio_net.rs#recv` | `src/driver/virtio/virtio_net.rs#recv` | 弱信号（同名异构） | 目标直接操作 `VIRTIO_NET_BASE`、`RX_LOCK` 等全局变量；参考通过 `self.receive()` 方法封装，抽象层次不同。 |
| 设备驱动 | `modules/axruntime/src/virtio_net.rs#send` | `src/driver/virtio/virtio_net.rs#send` | 弱信号（同名异构） | 目标直接操作 `TX_LOCK`、`VIRTIO_NET_TX_BUF`；参考通过 `self.send(net::TxBuffer::from(data))` 封装。 |
| 第三方库 | `crates/axvfs/src/lib.rs#empty` | `crates/posix_types/src/signal/sig_action.rs#empty` | 弱信号（同名异构） | 目标为内部闭环逻辑；参考为 `Self` 构造，属于不同 crate 的独立定义。 |
| 第三方库 | `crates/axvfs/src/lib.rs#truncate` | `crates/posix_types/src/open.rs#truncate` | 弱信号（同名异构） | 目标返回 `Err`；参考为 `self.contains` 位运算，语义和实现完全不同。 |

#### 4. 核心代码调用流对比剖析

* **【进程管理/进程等待与回收路径】**
* **底层技术析理**: 目标项目的 `waitpid` 与 `sys_wait4` 通过 `mm::UserPtr::new(status).write(root_pa, ...)` 直接操作物理地址完成状态写回，依赖 `crate::mm::release_user_root` 和 `crate::runtime::wait_timeout_ms` 进行资源回收与超时控制。参考项目的 `waitpid` 仅为 `sys_wait4` 的简单转发，其核心逻辑 `do_waitid` 使用 `UserPointerMut::new(siginfo_ptr)?.write` 和 `UserPointerMut::new(status_ptr)?.write` 进行虚拟地址写回，并依赖 `ProcessList::get().write` 和 `procs.remove_process` 进行进程树回收。两者在内存抽象（物理地址 vs 虚拟地址）和进程组织模型（全局数组索引 vs 树形结构）上存在根本性差异。
* **代码溯源链接**:
  - 目标项目: `modules/axruntime/src/process.rs#waitpid`, `modules/axruntime/src/syscall.rs#sys_wait4`
  - 参考项目: `src/kernel/syscall/procops.rs#waitpid`, `src/kernel/syscall/procops.rs#do_waitid`

* **【进程管理/futex 等待路径】**
* **底层技术析理**: 目标项目的 `futex::wait` 通过 `mm::translate_user_ptr(root_pa, uaddr, 4, UserAccess::Read)` 手动翻译物理地址并校验权限，使用 `FUTEX_KEYS.iter_mut().enumerate()` 在全局数组中线性查找等待槽位，最终调用 `runtime::block_current` 阻塞。参考项目的 `futex_wait` 通过 `User::<u32>::with_addr(uaddr)` 和 `UserPointer::new(...)?.read` 直接读取虚拟地址，使用 `FUTEX_TABLE.get_bucket` 和 `futex_bucket.find_or_insert` 在哈希桶中定位等待项。两者在用户态内存访问（物理翻译 vs 虚拟直接读取）和 futex 数据结构（线性数组 vs 哈希桶）上完全不同。
* **代码溯源链接**:
  - 目标项目: `modules/axruntime/src/futex.rs#wait`, `modules/axruntime/src/futex.rs#slot_for_wait`
  - 参考项目: `src/kernel/task/futex.rs#futex_wait`

* **【进程管理/信号掩码处理路径】**
* **底层技术析理**: 目标项目的 `sys_rt_sigprocmask` 通过 `mm::translate_user_ptr(root_pa, set, size, UserAccess::Read)` 进行物理地址翻译和读权限校验，并使用 `UserPtr::<usize>::new(oldset).write(root_pa, 0)` 写回物理地址。参考项目的 `rt_sigprocmask` 通过 `UserPointer::new(set.as_const())?.read` 和 `UserPointerMut::new(oldset)?.write` 进行虚拟地址读写，依赖 `as_const()` 等零成本抽象。两者在用户态指针安全校验的底层路径上存在显著差异。
* **代码溯源链接**:
  - 目标项目: `modules/axruntime/src/syscall.rs#sys_rt_sigprocmask`
  - 参考项目: `src/kernel/syscall/procops.rs#rt_sigprocmask`

* **【文件系统/procfs 目录项查找路径】**
* **底层技术析理**: 目标项目的 `procfs::lookup` 直接返回 `Ok`，呈现存根特征，无任何实际查找逻辑。参考项目的 `procfs::lookup` 包含 `(name == &***dentry.name()).then` 的名称匹配、`block_on` 异步等待、`lock.prove()` 锁证明以及 `self.entries.access(lock.prove()).iter()` 的目录项遍历。两者在功能完整性和 AST 深度上不在同一量级。
* **代码溯源链接**:
  - 目标项目: `modules/axfs/src/procfs.rs#lookup`
  - 参考项目: `src/fs/procfs.rs#lookup`

* **【设备驱动/virtio-net 接收路径】**
* **底层技术析理**: 目标项目的 `virtio_net::recv` 直接操作全局变量 `VIRTIO_NET_BASE`、`VIRTIO_NET_READY`、`RX_LOCK` 和 `VIRTIO_NET_RX_QUEUE`，采用扁平化的全局状态管理。参考项目的 `virtio_net::recv` 通过 `self.receive()` 方法封装，返回 `RxBuffer::VirtIOBuffer`，采用面向对象的设备抽象。两者在驱动架构和状态管理模型上存在根本性差异。
* **代码溯源链接**:
  - 目标项目: `modules/axruntime/src/virtio_net.rs#recv`
  - 参考项目: `src/driver/virtio/virtio_net.rs#recv`

#### 5. 单方模块与降权项

- **仅目标项目体现的模块**：`网络协议栈/第三方库`（参考项目无此分类）。目标项目中的 `smoltcp_impl.rs` 相关函数（如 `run_tcp_loopback`、`poll`、`socket_poll`、`socket_send`）属于对 smoltcp 协议栈的封装，其调用图谱（`Interface::new`、`Icmpv4Repr::parse` 等）源于第三方库的公共 API，不构成与参考项目的同源证据。
- **降权项说明**：
  - **POSIX/ABI 强制接口名**：`waitpid`、`execve`、`exit`、`futex`、`rt_sigprocmask`、`lookup`、`root`、`init_process` 等函数名是操作系统必须实现的规范接口，同名属于领域共性，不能作为复用证据。
  - **Rust 语言级公共构造**：`Ok`、`Err`、`Some`、`Arc::new`、`Context::from_waker`、`Waker::from`、`println!`、`cfg!`、`assert_eq!` 等是 Rust 标准库或语言内置的通用宏/类型，其出现与代码原创性无关。
  - **跨板块同名术语**：`enable_interrupts`、`wait_for_interrupt`、`create`、`truncate`、`get`、`new` 等在不同子系统中重复出现是正常工程现象，不能跨板块硬凑为同源证据。
  - **第三方库公共接口**：目标项目的 `axvfs` crate 与参考项目的 `posix_types`、`eonix_hal`、`eonix_percpu` 等 crate 中的同名函数（如 `empty`、`truncate`、`set_name`、`new`）属于不同第三方库的独立定义，其内部实现和依赖图谱完全不同。

#### 6. 保守结论

基于客观 AST 指纹数据，目标项目与参考项目的全项目相似度仅为 0.41%，共同 AST 指纹仅 11 个，覆盖率分别为 1.27% 和 0.6%，均处于噪声级。在控辩双方重点关注的“进程管理”板块，共同唯一 AST 指纹为 0，覆盖率 0.0%，不存在任何底层结构重合。所有被列举的同名函数（如 `waitpid`、`futex::wait`、`procfs::lookup`）均因 POSIX 规范强制或 Rust 语言公共生态而重名，其底层 AST 调用图谱在内存抽象（物理地址 vs 虚拟地址）、数据结构（全局数组 vs 树形结构/哈希桶）、同步原语（自研调度队列 vs condvar/RCU）等核心维度上呈现系统性架构隔离。综合判定，两项目为独立开发，不存在代码复用或同源性抄袭。

------------------------------

