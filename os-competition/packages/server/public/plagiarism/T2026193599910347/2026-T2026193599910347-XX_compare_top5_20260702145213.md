# 🛡️ OS 内核赛自动化代码级架构审计报告

**审计时间**: `2026-07-02 14:52:13`
**参赛作品名称**: `2026-T2026193599910347-XX`
**年份**: `2026`
**学校**: `XX`
**队伍名称**: `T2026193599910347`
**比赛名字**: `OS-Competition`
**比赛子赛道**: `Kernel-Implementation`
**仓库地址**: `https://gitlab.eduxiji.net/educg-group-43501-3132633/T2026193599910347-3076`
**推测开发语言**: `Rust / C (由AST探针自动识别)`
**特征函数量**: `1009`

## ⚔️ 底层代码同源性对比 (AST Homology)
> 本部分由专门的代码审查智能体，对该项目与符合年份条件的历届最高相似度项目进行控制流与底层调用对比。新版报告会先给出确定性的全模块覆盖矩阵，再给出 Agent 的分模块解释，避免只围绕少量抽样函数展开。

### 🏆 Top 5 底层对标: vs `2025-广告位招租-西安电子科技大学`（西安电子科技大学 / 广告位招租）

#### 对方作品信息

**对方年份**: `2025`
**对方学校**: `西安电子科技大学`
**对方队伍名称**: `广告位招租`
**对方比赛名字**: `OS-Competition`
**对方比赛子赛道**: `Kernel-Implementation`
**对方仓库地址**: `https://gitlab.eduxiji.net/educg-group-36002-2710490/T202510701995284-398`

> 双向覆盖率：共同 AST 指纹: 10；唯一 AST 指纹: 目标项目 867，参考项目 1946；覆盖率: 目标项目 1.15%，参考项目 0.51%

## 全模块对比总览（确定性证据）

> 总体覆盖率：共同 AST 指纹: 10；唯一 AST 指纹: 目标项目 867，参考项目 1946；覆盖率: 目标项目 1.15%，参考项目 0.51%

### 1. 模块覆盖矩阵
| 模块 | 目标函数数 | 参考函数数 | 共同唯一 AST 指纹 | Target 覆盖 | Base 覆盖 | 状态 |
| :--- | ---: | ---: | ---: | ---: | ---: | :--- |
| 进程管理 | 65 | 131 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |
| 任务调度/异步运行时 | 89 | 139 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |
| 内存管理 | 132 | 630 | 1 | 0.85% | 0.22% | 双方存在且有结构重合 |
| 中断/异常处理 | 23 | 47 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |
| 系统调用 | 170 | 64 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |
| 文件系统 | 244 | 530 | 3 | 1.35% | 0.87% | 双方存在且有结构重合 |
| 设备驱动 | 42 | 47 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |
| 网络协议栈 | 14 | 91 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |
| 文件系统/第三方库 | 102 | 366 | 2 | 2.08% | 0.7% | 双方存在且有结构重合 |
| 网络协议栈/第三方库 | 60 | 21 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |
| 第三方库 | 6 | 869 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |
| 通用模块/其他 | 62 | 226 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |

### 2. 分模块证据清单

#### 进程管理
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `modules/axruntime/src/syscall.rs#sys_execve` | `os/src/syscall/process.rs#sys_execve` | 同板块同名 | Err, Ok, cfg!, close_cloexec_fds, crate::mm::release_user_root, crate::println! ...(+12) | Box::<[u8; 4]>::new, DEFAULT_SHELL.to_string, Some, Vec::with_capacity, arg_ptr.is_null, argv.add ...(+22) |
| `modules/axruntime/src/syscall.rs#sys_exit` | `os/src/syscall/process.rs#sys_exit` | 同板块同名 | crate::process::current_pid, crate::process::current_pid().unwrap_or, crate::process::exit_current, crate::runtime::exit_current, crate::sbi::shutdown | exit_current_and_run_next |
| `modules/axruntime/src/process.rs#waitpid` | `os/src/syscall/process.rs#sys_prlimit` | 同模块上下文对齐/差异观察 | Err, Ok, crate::mm::release_user_root, crate::runtime::wait_timeout_ms, crate::syscall::can_block_current, current_pid ...(+5) | Resource::from_primitive, assert!, copy_from_user, copy_from_user(token, new_limit, rlimit).is_err, copy_to_user, copy_to_user(
                        token,
                        &(RLimit {
                            rlim_cur: MAX_FILE_SIZE,
                            rlim_max: MAX_FILE_SIZE,
                        }),
                        old_limit,
                    )
                    .is_err ...(+19) |
| `modules/axruntime/src/runtime.rs#wait_timeout_ms` | `os/src/syscall/process.rs#sys_shmctl` | 同模块上下文对齐/差异观察 | NEED_RESCHED.store, SLEEP_QUEUE.push, SLEEP_QUEUE.remove, crate::scheduler::switch, queue.pop, queue.push ...(+9) | buf.is_null, info!, warn! |

#### 任务调度/异步运行时
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `modules/axruntime/src/main.rs#rust_main` | `os/src/main.rs#rust_main` | 同板块同名 | axnet::arp_probe_gateway_once, axnet::init, axnet::init(dev).is_ok, axnet::tcp_loopback_test_once, axnet::tcp_loopback_test_once().is_ok, crate::println! ...(+22) | bootstrap_init, console::log_init, fs::directory_tree::init_fs, fs::flush_preload, machine_init, mem_clear ...(+7) |
| `modules/axruntime/src/futex.rs#wake` | `os/src/task/threads.rs#wake` | 同板块同名 | Ok, clear_slot_if_empty, make_key, mm::translate_user_ptr, mm::translate_user_ptr(root_pa, uaddr, 4, UserAccess::Read).ok_or, runtime::wake_all ...(+3) | self.inner.insert, self.inner.remove, wait_queue.is_empty, wait_queue.wake_at_most |
| `modules/axruntime/src/runtime.rs#wake_all` | `os/src/task/manager.rs#wake_all` | 同板块同名 | NEED_RESCHED.store, RUN_QUEUE.push, crate::println!, queue.notify_one, queue.push, task::set_wait_reason ...(+1) | self.wake_at_most |
| `modules/axruntime/src/syscall.rs#dispatch` | `os/src/syscall/fs.rs#sys_copy_file_range` | 同模块上下文对齐/差异观察 | Err, sys_accept, sys_accept4, sys_bind, sys_brk, sys_chdir ...(+113) | Some, Vec::new, Vec::with_capacity, buf.set_len, current_task, current_task().unwrap ...(+18) |

#### 内存管理
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `modules/axruntime/src/trap.rs#trap_handler` | `os/src/hal/arch/loongarch64/trap/mod.rs#trap_handler` | 同板块同名 | TIMER_INTERVAL.load, TRAP_LOG_ONCE.swap, crate::mm::current_root_pa, crate::mm::handle_cow_fault, crate::plic::claim, crate::plic::complete ...(+16) | (0..sz).rev, BadV::read, BadV::read().get_vaddr, ERA::read, ERA::read().next_ins, ERA::read().next_ins().write ...(+55) |
| `modules/axfs/src/fat32.rs#write_at` | `os/src/mm/page_table.rs#write_at` | 同板块同名 | Err, Ok, _buf.is_empty, _buf.len, core::cmp::max, core::cmp::min ...(+9) | buffer.len, buffer[copy_buffer_start..copy_buffer_end]
                .copy_from_slice, dst_end.min, dst_start.max, self.buffers.iter_mut, src.len |
| `modules/axruntime/src/mm.rs#init_frame_allocator` | `os/src/mm/frame_allocator.rs#init_frame_allocator` | 同板块同名 | BumpFrameAllocator::new, FRAME_ALLOC.write, FRAME_ALLOC_READY.store, FRAME_BASE.store, FRAME_COUNT.store, PhysAddr::new ...(+6) | FRAME_ALLOCATOR.write, FRAME_ALLOCATOR.write().init, PhysAddr::from, PhysAddr::from(MEMORY_END).floor, PhysAddr::from(ekernel as usize).ceil |
| `modules/axruntime/src/syscall.rs#sys_socket` | `os/src/syscall/net.rs#sys_socket` | 同板块同名 | FdObject::Socket, alloc_fd, alloc_fd(entry).ok_or, axnet::socket_create, axnet::socket_create(domain as i32, sock_type_base as i32, protocol as i32)
        .map_err | <dyn Socket>::alloc, info! |

#### 中断/异常处理
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `modules/axruntime/src/trap.rs#enable_timer_interrupt` | `os/src/hal/arch/loongarch64/trap/mod.rs#enable_timer_interrupt` | 同板块同名 | TIMER_INTERVAL.store, read_sie, read_sstatus, read_time, sbi::set_timer, write_sie ...(+1) | ECfg::empty, ECfg::empty()
        .set_line_based_interrupt_vector, ECfg::empty()
        .set_line_based_interrupt_vector(LineBasedInterrupt::TIMER)
        .write, TCfg::read, TCfg::read()
        .set_enable, TCfg::read()
        .set_enable(true)
        .set_periodic ...(+3) |
| `modules/axruntime/src/runtime.rs#idle_loop` | `os/src/hal/arch/loongarch64/trap/mod.rs#trap_from_kernel` | 同模块上下文对齐/差异观察 | axnet::poll, axnet::request_poll, crate::async_exec::poll, crate::cpu::wait_for_interrupt, crate::time::uptime_ms, crate::trap::enable_interrupts ...(+5) | ((addr + i) as *mut u8).read_unaligned, ((addr + i) as *mut u8).write_unaligned, (0..sz).rev, BadV::read, BadV::read().get_vaddr, EStat::read ...(+14) |
| `modules/axruntime/src/mm.rs#with_no_irq` | `os/src/hal/arch/loongarch64/trap/mem_access.rs#get_size` | 同模块上下文对齐/差异观察 | asm!, f | 内部闭环逻辑 |
| `modules/axruntime/src/trap.rs#return_to_user` | `os/src/fs/dev/tty.rs#default` | 同模块上下文对齐/差异观察 | asm! | 内部闭环逻辑 |

#### 系统调用
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `modules/axruntime/src/syscall.rs#sys_connect` | `os/src/syscall/net.rs#sys_connect` | 同板块同名 | Err, Ok, TCP_CONNECT_LOGGED.swap, axnet::socket_connect, axnet::socket_connecting, axnet::socket_connecting(socket_id).map_err ...(+13) | get_socket!, socket.connect, socket.connect(addr_buf).unwrap, trans_ref! |
| `modules/axruntime/src/syscall.rs#sys_getpeername` | `os/src/syscall/net.rs#sys_getpeername` | 同板块同名 | Err, Ok, Some, axnet::socket_remote_endpoint, axnet::socket_remote_endpoint(socket_id).map_err, mm::current_root_pa ...(+2) | get_socket!, socket.peer_addr, socket.peer_addr(addr, addrlen).unwrap |
| `modules/axruntime/src/syscall.rs#sys_getsockname` | `os/src/syscall/net.rs#sys_getsockname` | 同板块同名 | Ok, Some, axnet::socket_local_endpoint, axnet::socket_local_endpoint(socket_id).map_err, mm::current_root_pa, resolve_socket_fd ...(+1) | get_socket!, socket.addr, socket.addr(addr, addrlen).unwrap |
| `modules/axruntime/src/syscall.rs#sys_read` | `user/src/syscall.rs#sys_read` | 同板块同名 | Err, Ok, mm::current_root_pa, read_from_entry, resolve_fd, resolve_fd(fd).ok_or | buffer.as_mut_ptr, buffer.len, syscall |

#### 文件系统
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `modules/axfs/src/block.rs#read_block` | `os/src/fs/cache.rs#read_block` | 同板块同名 | Err, Ok, buf.len, buf[..self.block_size].copy_from_slice, guard.state_mut, self.cache.lock ...(+3) | block_device.read_block, self.buffer.as_mut |
| `modules/axruntime/src/syscall.rs#sys_utimensat` | `os/src/syscall/fs.rs#sys_utimensat` | 同板块同名 | Err, Ok, mm::current_root_pa, size_of::<Timespec>, validate_at_dirfd, validate_user_path ...(+2) | Some, String::new, TimeSpec::now, UtimensatFlags::from_bits, __openat, copy_from_user ...(+10) |
| `modules/axfs/src/block.rs#flush` | `os/src/drivers/serial/ns16550a.rs#flush` | 同板块同名 | guard.state_mut, self.cache.lock, self.device
                        .write_block, self.device.flush, state.entries.iter_mut | Err, Ok, read_volatile |
| `modules/axfs/src/devfs.rs#create` | `os/src/fs/fat32/fat_osinode.rs#create` | 同板块同名 | Err | Arc::new, Mutex::new, Ok, Weak::new, name.to_string, panic! ...(+2) |

#### 设备驱动
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `modules/axruntime/src/virtio_blk.rs#try_init_device` | `os/src/drivers/block/sata_blk.rs#pci_init` | 同模块上下文对齐/差异观察 | VIRTIO_BLK_BASE.store, VIRTIO_BLK_IRQ.store, VIRTIO_BLK_QUEUE_SIZE.store, VIRTIO_BLK_USED_IDX.store, VIRTIO_QUEUE.get, core::cmp::min ...(+10) | AHCI::new, Some, Status::empty, dev.bars.iter, dev.bars.iter().enumerate, dev.bars.iter().enumerate().for_each ...(+3) |
| `modules/axruntime/src/virtio_net.rs#try_init_device` | `os/src/net/config.rs#new` | 同模块上下文对齐/差异观察 | VIRTIO_NET_BASE.store, VIRTIO_NET_IRQ.store, VIRTIO_NET_RX_QUEUE.get, VIRTIO_NET_RX_QUEUE_SIZE.store, VIRTIO_NET_RX_USED.store, VIRTIO_NET_TX_QUEUE.get ...(+11) | Config::new, EthernetAddress, EthernetAddress([0x02, 0x00, 0x00, 0x00, 0x00, 0x01]).into, Instant::from_millis, Interface::new, IpAddress::v4 ...(+12) |
| `modules/axruntime/src/virtio_net.rs#recv` | `os/src/net/address.rs#from` | 同模块上下文对齐/差异观察 | Err, Ok, RX_LOCK.lock, VIRTIO_NET_BASE.load, VIRTIO_NET_READY.load, VIRTIO_NET_RX_QUEUE.get ...(+10) | IpAddress::Ipv6, Ipv6Address, RNG.positive_u32, Some, addr.is_unspecified, u16::from_be_bytes |
| `modules/axruntime/src/virtio_net.rs#send` | `os/src/net/address.rs#from` | 同模块上下文对齐/差异观察 | Err, Ok, TX_LOCK.lock, VIRTIO_NET_BASE.load, VIRTIO_NET_READY.load, VIRTIO_NET_TX_BUF.data.as_ptr ...(+10) | IpAddress::Ipv4, Ipv4Address, RNG.positive_u32, Some, addr.is_unspecified, u16::from_be_bytes |

#### 网络协议栈
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `modules/axruntime/src/runtime.rs#log_net_event` | `os/src/net/tcp.rs#new` | 同模块上下文对齐/差异观察 | crate::println! | Mutex::new, NET_INTERFACE.add_socket, NET_INTERFACE.poll, RNG.positive_u32, info!, socket::tcp::Socket::new ...(+2) |
| `apps/net_bench/src/main.rs#write_u64` | `os/src/net/udp.rs#new` | 同模块上下文对齐/差异观察 | buf.len, write_stdout | Mutex::new, NET_INTERFACE.add_socket, NET_INTERFACE.poll, log::info!, socket::udp::PacketBuffer::new, socket::udp::Socket::new ...(+1) |
| `apps/tcp_echo/src/main.rs#iov_matches` | `os/src/net/tcp.rs#drop` | 同模块上下文对齐/差异观察 | a.len, b.len, expected.len, slices_equal | NET_INTERFACE.poll, NET_INTERFACE.remove, NET_INTERFACE.tcp_socket, info!, socket.close, socket.is_open |
| `apps/tcp_echo/src/main.rs#slices_equal` | `os/src/net/mod.rs#can_bind` | 同模块上下文对齐/差异观察 | a.iter, a.iter().enumerate, a.len, b.len | Some, log::info!, self.0.clone, socket.loacl_endpoint, socket.loacl_endpoint().eq, socket.socket_type ...(+1) |

#### 文件系统/第三方库
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `modules/axfs/src/ext4.rs#write_at` | `os/src/fs/ext4/file.rs#write_at` | 同板块同名 | Err, Ok, buf.is_empty, buf.len, core::cmp::min, inode_mode_type ...(+6) | Block::load_offset, Ok, block.sync_blk_to_disk, block.write_offset, drop, inode_ref.inode.set_size ...(+10) |
| `modules/axfs/src/ext4.rs#create` | `os/src/fs/ext4/layout.rs#create` | 同板块同名 | Err, Ok, init_inode_extents, inode_mode_type, name.is_empty, name.len ...(+4) | (InodePerm::S_IREAD | InodePerm::S_IWRITE).bits, Arc::new, InodeFileType::S_IFDIR.bits, InodeFileType::S_IFREG.bits, Mutex::new, Ok ...(+12) |
| `modules/axfs/src/ext4.rs#read_at` | `os/src/fs/ext4/file.rs#read_at` | 同板块同名 | Err, inode_mode_type, self.read_from_inode, self.read_inode | Ok, core::cmp::min, inode_ref.inode.size, min, read_buf.len, read_buf[cursor..cursor + adjust_read_size].copy_from_slice ...(+4) |
| `modules/axfs/src/ext4.rs#block_size` | `os/src/fs/ext4/superblock.rs#block_size` | 同板块同名同结构 | 内部闭环逻辑 | 内部闭环逻辑 |

#### 网络协议栈/第三方库
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `modules/axnet/src/smoltcp_impl.rs#receive` | `dependency/dep_iso/isomorphic_drivers/src/net/ethernet/intel/e1000.rs#receive` | 同板块同名 | LOOPBACK_QUEUE.pop, NET_RX_SEEN.compare_exchange, Some, record_arp_reply, self.dev.poll, self.dev.recv ...(+1) | Some, buffer.to_vec, recv_desc.status.get_bit, recv_desc.status.set_bit, self.recv_queue.len, self.registers[E1000_RDT].read ...(+5) |
| `modules/axnet/src/smoltcp_impl.rs#run_tcp_loopback` | `dependency/dep_iso/isomorphic_drivers/src/net/ethernet/intel/ixgbe.rs#new` | 同模块上下文对齐/差异观察 | Config::new, Err, EthernetAddress, EthernetAddress([0x02, 0x00, 0x00, 0x00, 0x00, 0x01]).into, Instant::from_millis, Interface::new ...(+35) | (IXGBE_FCRTH..IXGBE_FCRTH_END).step_by, (IXGBE_FCRTL..IXGBE_FCRTL_END).step_by, (IXGBE_FCTTV..IXGBE_FCTTV_END).step_by, EthernetAddress::from_bytes, MaybeUninit::uninit, MaybeUninit::uninit().assume_init ...(+56) |
| `modules/axnet/src/smoltcp_impl.rs#poll` | `dependency/dep_iso/isomorphic_drivers/src/net/ethernet/intel/e1000.rs#new` | 同模块上下文对齐/差异观察 | IcmpEndpoint::Ident, Icmpv4Packet::new_checked, Icmpv4Packet::new_unchecked, Icmpv4Repr::parse, Instant::from_millis, IpAddress::Ipv4 ...(+31) | P::alloc_dma, Vec::with_capacity, assert_eq!, debug!, e1000[E1000_ICR].read, e1000[E1000_ICR].write ...(+25) |
| `modules/axnet/src/smoltcp_impl.rs#socket_poll` | `dependency/dep_iso/isomorphic_drivers/src/net/ethernet/intel/ixgbe.rs#msend` | 同模块上下文对齐/差异观察 | NET_NEED_POLL.store, NET_STATE.as_mut, Ok, Some, matches!, set_socket_connecting ...(+11) | data.len, data[data_index].len, fence, ixgbe[IXGBE_TDT + IXGBE_TDT_GAP * queue].read, ixgbe[IXGBE_TDT + IXGBE_TDT_GAP * queue].write, send_desc.status.get_bit ...(+2) |

#### 第三方库
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `crates/axvfs/src/lib.rs#flush` | `dependency/virtio-drivers/src/gpu.rs#flush` | 同板块同名 | Ok | Ok, self.resource_flush, self.transfer_to_host_2d |
| `crates/axvfs/src/lib.rs#set_name` | `dependency/dep_pci/pci/src/lib.rs#probe_function` | 同模块上下文对齐/差异观察 | Err, Ok, name.len, self.name[..len].copy_from_slice | BAR::decode, BridgeControl::from_bits_truncate, Command::from_bits_truncate, DeviceKind::CardbusBridge, DeviceKind::Device, DeviceKind::PciBridge ...(+6) |
| `crates/axvfs/src/lib.rs#empty` | `dependency/dep_iso/isomorphic_drivers/src/block/ahci.rs#new` | 同模块上下文对齐/差异观察 | 内部闭环逻辑 | (0..ghc.num_ports()).find, P::alloc_dma, Some, c.set_bit, debug!, ghc.enable ...(+16) |
| `crates/axvfs/src/lib.rs#new` | `dependency/riscv/descriptor/generator.rs#generate` | 同模块上下文对齐/差异观察 | 内部闭环逻辑 | String::new, bf.generate_enum, format!, self.bfs.iter, write!, write!(&mut bit_sets, "{}", bf.generate_bitops()).unwrap ...(+2) |

#### 通用模块/其他
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `modules/axruntime/src/sbi.rs#sbi_call` | `os/src/hal/arch/riscv/sbi.rs#sbi_call` | 同板块同名 | asm! | asm! |
| `modules/axruntime/src/sbi.rs#shutdown` | `os/src/hal/arch/loongarch64/sbi.rs#shutdown` | 同板块同名 | asm!, sbi_call, sbi_call_legacy | (0x100E_001C as *mut u8).write_volatile, Pm1Cnt::empty, pm1_cnt.set_s5, pm1_cnt.set_s5().write |
| `modules/axruntime/src/sbi.rs#console_getchar` | `os/src/hal/arch/riscv/sbi.rs#console_getchar` | 同板块同名 | Some, sbi_call_legacy | sbi_call |
| `modules/axruntime/src/sbi.rs#set_timer` | `os/src/hal/arch/riscv/sbi.rs#set_timer` | 同板块同名 | sbi_call, sbi_call_legacy | sbi_call |


### 审计结论：最终同源性定性仲裁

#### 1. 宏观判定
- **定性结论**: **独立开发，无实质性代码复用**
- **核心依据**: 客观 AST 相似度仅为 0.36%，共同 AST 指纹仅 10 个，双向覆盖率均低于 1.15%。在【进程管理】这一核心指控板块，共同唯一 AST 指纹为 0。双方在内存模型、调度模型、同步原语等核心架构层面存在根本性、系统性分歧，非代码片段级修改可以达成。

#### 2. 全模块覆盖矩阵解读
| 模块 | 双方存在 | 共同指纹 | 覆盖率解读 | 证据强度 |
| :--- | :---: | :---: | :--- | :---: |
| **进程管理** | 是 | 0 | 0.0% 覆盖率，双方在该板块无任何可识别的同源结构。 | **未形成** |
| **任务调度/异步运行时** | 是 | 0 | 0.0% 覆盖率，调度模型（异步 vs 同步）完全不同。 | **未形成** |
| **内存管理** | 是 | 1 | 覆盖率极低（0.85% vs 0.22%），单一指纹不足以构成同源证据。 | **弱** |
| **中断/异常处理** | 是 | 0 | 0.0% 覆盖率，Trap 处理逻辑与架构强相关，双方实现独立。 | **未形成** |
| **系统调用** | 是 | 0 | 0.0% 覆盖率，同名函数均为 POSIX 标准接口，底层实现异构。 | **未形成** |
| **文件系统** | 是 | 3 | 覆盖率低（1.35% vs 0.87%），指纹源于 ext4/fat32 等标准磁盘结构的通用解析逻辑。 | **弱** |
| **设备驱动** | 是 | 0 | 0.0% 覆盖率，驱动模型（virtio vs 具体硬件）和实现完全不同。 | **未形成** |
| **网络协议栈** | 是 | 0 | 0.0% 覆盖率，协议栈实现（smoltcp vs 自研）和集成方式不同。 | **未形成** |
| **第三方库/公共依赖** | 是 | 0 | 0.0% 覆盖率，双方依赖的第三方库集合不同，无共同指纹。 | **未形成** |

#### 3. 底层 AST 关键证据指纹表
| 所属板块 | 目标项目路径 | 参考项目路径 | 证据类型 | 技术说明 |
| :--- | :--- | :--- | :--- | :--- |
| 进程管理 | `modules/axruntime/src/syscall.rs#sys_execve` | `os/src/syscall/process.rs#sys_execve` | 同板块同名 | POSIX 标准系统调用。目标项目关注 fd 清理与根页表释放，参考项目关注参数解析，调用图谱无交集。 |
| 进程管理 | `modules/axruntime/src/syscall.rs#sys_exit` | `os/src/syscall/process.rs#sys_exit` | 同板块同名 | POSIX 标准系统调用。目标项目拆分进程/运行时退出并调用 SBI，参考项目为单一函数调用。 |
| 内存管理 | `modules/axruntime/src/mm.rs#init_frame_allocator` | `os/src/mm/frame_allocator.rs#init_frame_allocator` | 同板块同名 | 通用物理内存分配器初始化。均使用 `FRAME_ALLOCATOR` 全局变量和 `PhysAddr`，属于常规实现模式。 |
| 文件系统 | `modules/axfs/src/ext4.rs#block_size` | `os/src/fs/ext4/superblock.rs#block_size` | 同板块同名同结构 | 均为“内部闭环逻辑”，即从 ext4 超级块读取并返回块大小的常量函数，属于标准磁盘布局的必然实现。 |
| 文件系统 | `modules/axfs/src/block.rs#read_block` | `os/src/fs/cache.rs#read_block` | 同板块同名 | 通用块缓存读取。均涉及 `buf.copy_from_slice` 和缓存状态管理，属于块设备驱动的常规操作。 |
| 文件系统 | `modules/axfs/src/ext4.rs#create` | `os/src/fs/ext4/layout.rs#create` | 同板块同名 | ext4 文件系统 inode 创建。均涉及权限、文件类型等标准 POSIX 语义，但底层布局操作不同。 |
| 通用/其他 | `modules/axruntime/src/sbi.rs#sbi_call` | `os/src/hal/arch/riscv/sbi.rs#sbi_call` | 同板块同名 | RISC-V SBI 标准调用规范，所有 RISC-V 内核均使用 `asm!` 实现，属于架构强制约定。 |

#### 4. 核心代码调用流对比剖析
* **【进程管理 / 内存与用户态交互模型】**
* **底层技术析理**: 这是双方最根本的架构分歧点。目标项目采用“根页表切换”模型，通过 `mm::current_root_pa` 获取物理根页表，并使用 `mm::UserPtr` 和 `mm::translate_user_ptr` 进行用户态地址转换与读写。参考项目则采用经典的“用户 Token + 显式拷贝”模型，通过 `current_user_token` 和 `copy_from_user`/`copy_to_user` 进行数据交换。这两种模型在地址空间抽象、权限验证和数据拷贝机制上完全不同，导致所有涉及用户态交互的系统调用（如 `sys_wait4`, `sys_rt_sigprocmask`）的 AST 调用链彻底异构。
* **代码溯源链接**:
  - 目标项目: `modules/axruntime/src/syscall.rs#sys_wait4`
  - 参考项目: `os/src/syscall/process.rs#sys_prlimit`

* **【任务调度/异步运行时 / 调度与等待模型】**
* **底层技术析理**: 目标项目实现了基于 `Waker`/`poll_fn` 的异步执行器 (`async_exec.rs`)，任务等待通过 `SLEEP_QUEUE` 和 `NEED_RESCHED` 标志进行协作式管理。参考项目采用传统的同步阻塞模型，任务通过 `block_current_and_run_next` 主动让出 CPU，并使用 `interruptible_queue` 管理等待队列。前者是异步运行时模型，后者是同步内核模型，调度实体的状态转换和等待机制完全不同。
* **代码溯源链接**:
  - 目标项目: `modules/axruntime/src/runtime.rs#wait_timeout_ms`
  - 参考项目: `os/src/syscall/process.rs#sys_clock_nanosleep`

* **【进程管理 / Futex 同步原语】**
* **底层技术析理**: 目标项目的 Futex 实现基于全局 `FUTEX_KEYS` 数组和 `make_key` 函数进行键值管理，等待操作直接调用 `runtime::block_current`。参考项目则将 Futex 状态内嵌于任务控制块 `task.futex.lock` 中，并使用独立的 `WaitQueue` 进行等待管理。两者在数据结构设计、锁粒度和等待队列实现上均无共同点。
* **代码溯源链接**:
  - 目标项目: `modules/axruntime/src/futex.rs#wait`
  - 参考项目: `os/src/task/threads.rs#do_futex_wait`

#### 5. 单方模块与降权项
- **单方模块**: 目标项目的 `async_exec` 异步运行时模块在参考项目中无对应实现，参考项目的 `task/signal.rs` 等复杂信号处理模块在目标项目中仅为桩实现。这体现了双方设计重心的不同。
- **第三方库/公共依赖**: 双方均依赖不同的第三方库集合（如目标项目依赖 `smoltcp`，参考项目有大量 `dependency/dep_iso` 下的自研或移植驱动）。任何跨项目的第三方库对比，因来源不同，均不构成同源证据。
- **降权项**: 所有同名系统调用（如 `sys_execve`, `sys_exit`, `sys_read`）均为 POSIX/Linux 标准接口，其名称由规范决定，不代表实现复用。`asm!`, `println!`, `Ok/Err`, `copy_from_slice` 等为 Rust 语言内建宏、标准库函数或社区惯用法，属于公共知识领域。

#### 6. 保守结论
本次审计基于 0.36% 的极低 AST 相似度和 1.15% 以下的覆盖率，结合对核心板块调用图谱的深度剖析，认定目标项目 `[2026-T2026193599910347-XX]` 与参考项目 `[2025-广告位招租-西安电子科技大学]` 在底层架构上存在显著差异。双方在内存管理、任务调度、同步原语等操作系统内核的核心设计决策上选择了完全不同的技术路径，这种系统性的架构分歧无法通过简单的代码重命名或逻辑等价变换达成。现有证据强力支持双方为独立开发，不存在实质性代码复用行为，整体代码原创性风险极低。

------------------------------

