# 🛡️ OS 内核赛自动化代码级架构审计报告

**审计时间**: `2026-07-02 13:22:45`
**参赛作品名称**: `2026-T2026106109910526-XX`
**年份**: `2026`
**学校**: `XX`
**队伍名称**: `T2026106109910526`
**比赛名字**: `OS-Competition`
**比赛子赛道**: `Kernel-Implementation`
**仓库地址**: `https://gitlab.eduxiji.net/educg-group-43501-3132633/T2026106109910526-568`
**推测开发语言**: `Rust / C (由AST探针自动识别)`
**特征函数量**: `2758`

## ⚔️ 底层代码同源性对比 (AST Homology)
> 本部分由专门的代码审查智能体，对该项目与符合年份条件的历届最高相似度项目进行控制流与底层调用对比。新版报告会先给出确定性的全模块覆盖矩阵，再给出 Agent 的分模块解释，避免只围绕少量抽样函数展开。

### 🏆 Top 4 底层对标: vs `2025-Del0n1x-哈尔滨工业大学深圳`（哈尔滨工业大学（深圳） / Del0n1x）

#### 对方作品信息

**对方年份**: `2025`
**对方学校**: `哈尔滨工业大学（深圳）`
**对方队伍名称**: `Del0n1x`
**对方比赛名字**: `OS-Competition`
**对方比赛子赛道**: `Kernel-Implementation`
**对方仓库地址**: `https://gitlab.eduxiji.net/educg-group-36002-2710490/T202518123995600-2523`

> 双向覆盖率：共同 AST 指纹: 138；唯一 AST 指纹: 目标项目 2284，参考项目 1681；覆盖率: 目标项目 6.04%，参考项目 8.21%

## 全模块对比总览（确定性证据）

> 总体覆盖率：共同 AST 指纹: 138；唯一 AST 指纹: 目标项目 2284，参考项目 1681；覆盖率: 目标项目 6.04%，参考项目 8.21%

### 1. 模块覆盖矩阵
| 模块 | 目标函数数 | 参考函数数 | 共同唯一 AST 指纹 | Target 覆盖 | Base 覆盖 | 状态 |
| :--- | ---: | ---: | ---: | ---: | ---: | :--- |
| 进程管理 | 287 | 239 | 6 | 2.41% | 3.3% | 双方存在且有结构重合 |
| 任务调度/异步运行时 | 393 | 254 | 6 | 1.61% | 2.67% | 双方存在且有结构重合 |
| 内存管理 | 623 | 396 | 7 | 1.28% | 2.11% | 双方存在且有结构重合 |
| 中断/异常处理 | 77 | 113 | 1 | 1.92% | 1.22% | 双方存在且有结构重合 |
| 系统调用 | 174 | 97 | 12 | 8.33% | 16.0% | 双方存在且有结构重合 |
| 文件系统 | 488 | 496 | 21 | 5.83% | 5.71% | 双方存在且有结构重合 |
| 设备驱动 | 229 | 155 | 30 | 15.62% | 22.73% | 双方存在且有结构重合 |
| 网络协议栈 | 163 | 88 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |
| 文件系统/第三方库 | 66 | 44 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |
| 通用模块/其他 | 258 | 265 | 36 | 15.65% | 15.65% | 双方存在且有结构重合 |

### 2. 分模块证据清单

#### 进程管理
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `os/src/syscall/process.rs#sys_clone` | `os/src/syscall/process.rs#sys_clone` | 同板块同名 | CloneFlags::LEGACY_FLAGS.bits, CloneFlags::from_bits, CloneFlags::from_bits((flags & CloneFlags::LEGACY_FLAGS.bits()) & !0xff)
        .ok_or, Err, Ok, Some ...(+17) | CloneFlags::from_bits, CloneFlags::from_bits(flags as u32).ok_or, Err, Ok, add_task, child_trap_cx.set_sp ...(+19) |
| `os/src/executor/mod.rs#spawn` | `os/src/task/executor.rs#spawn` | 同板块同名 | <Arc<TaskControlBlock> as Clone>::clone, <Arc<TaskControlBlock> as Clone>::clone(&(&future.task.clone())).turn_cpu_mask_id, TASK_QUEUE.push, TASK_QUEUE.push_preempt, WithInfo, async_task::spawn ...(+3) | TASK_QUEUE.push_normal, TASK_QUEUE.push_prior, WithInfo, async_task::spawn, runnable.schedule, task.detach |
| `os/src/sync/mutex/spin_mutex.rs#wait_unlock` | `os/src/sync/mutex/spin.rs#wait_unlock` | 同板块同名 | core::hint::spin_loop, panic!, self.owner.load | core::hint::spin_loop, panic!, self.lock.load |
| `os/src/task/task.rs#do_group_exit` | `os/src/signal/do_signal.rs#do_group_exit` | 同板块同名 | Default::default, Some, drop, self.do_exit, self.pid, self.thread_group.lock ...(+5) | info!, task.kill_all_thread, task.set_exit_code |

#### 任务调度/异步运行时
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `os/src/syscall/io.rs#sys_ppoll` | `os/src/syscall/io.rs#sys_ppoll` | 同板块同名 | Err, Ok, PollEvents::empty, Select2Futures::new, Some, TimedTaskFuture::new ...(+38) | Duration::from, Err, IoFutrue::new, Ok, SigMask::from_bits, SigMask::from_bits(unsafe { *sigmask }).ok_or ...(+9) |
| `os/src/syscall/net.rs#get` | `os/src/task/manager.rs#get` | 同板块同名 | 0u8.to_ne_bytes, Err, Ok, SOCKET_SET.with_socket::<smoltcp::socket::tcp::Socket, _, _>, TimeSpec::_as_bytes, UserSliceRaw::new ...(+26) | self.0.get, self.0.get(&pid).and_then, weak.upgrade |
| `os/src/syscall/fs.rs#sys_renameat2` | `os/src/syscall/fs.rs#sys_renameat2` | 同板块同名 | Arc::ptr_eq, Err, Ok, RenameFlags::from_bits, RenameFlags::from_bits(flags).ok_or, UserPtrRaw::new ...(+47) | Dentry::get_dentry_from_path, Err, Ok, RenameFlags::from_bits, RenameFlags::from_bits(flags).ok_or, current_task ...(+25) |
| `os/src/syscall/fs.rs#sys_splice` | `os/src/syscall/fs.rs#sys_splice` | 同板块同名 | (size - copied).min, Err, Ok, UserPtrRaw::new, UserPtrRaw::new(in_off_ptr as *const usize)
            .ensure_read, UserPtrRaw::new(in_off_ptr as *const usize)
            .ensure_read(&mut task.get_vm_space().lock())
            .ok_or ...(+33) | Err, Ok, core::ptr::write, current_task, current_task().unwrap, debug_point! ...(+24) |

#### 内存管理
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `os/src/syscall/mod.rs#syscall` | `os/src/syscall/mod.rs#syscall` | 同板块同名 | SendWrapper, SysError::EINTR.code, SysError::ENOSYS.code, SyscallId::from_repr, UserPtrRaw::new, VirtAddr::from ...(+248) | Ok, SysCode::from, sys_accept, sys_accept4, sys_bind, sys_brk ...(+121) |
| `os/src/trap/mod.rs#user_trap_handler` | `os/src/hal/la64/trap/user_trap.rs#user_trap_handler` | 同板块同名 | Default::default, IRQ_COUNTER.lock, IRQ_COUNTER.lock().add_timer_irq_cnt, Instruction::enable_interrupt, SumGuard::new, TrapType::get_debug ...(+26) | DEVICE_MANAGER.read, DEVICE_MANAGER.read().handle_irq, TIMER_QUEUE.handle_expired, badv::read, badv::read().vaddr, crmd.plv ...(+23) |
| `os/src/drivers/block/mmc/mod.rs#send_cmd` | `os/src/drivers/vf2/dw/vf2_sdio.rs#send_cmd` | 同板块同名 | CMD::offset, CMDARG::offset, RESP::offset, RINSTS::offset, Some, arg.into ...(+22) | CMD::offset, CMDARG::offset, RESP::offset, RINSTS::offset, Some, arg.into ...(+27) |
| `os/src/syscall/fs.rs#sys_openat` | `os/src/syscall/fs.rs#sys_openat` | 同板块同名 | AtFlags::from_bits_truncate, Err, Ok, OpenFlags::from_bits, OpenFlags::from_bits(flags as i32).ok_or, UserPtrRaw::new ...(+42) | Err, FdInfo::new, Ok, OpenFlags::from_bits, OpenFlags::from_bits(flags as i32).ok_or, SYS_OPENAT_MODE.store ...(+19) |

#### 中断/异常处理
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `hal/src/component/irq/loongarch64/eiointc.rs#claim_irq` | `os/src/drivers/irqchip/la64/loongson_eiointc.rs#claim_irq` | 同板块同名 | Some, flags0.leading_zeros, flags1.leading_zeros, flags2.leading_zeros, flags3.leading_zeros, iocsr_read_d | Some, pendings.trailing_zeros, self.get_core_pendings, self.get_core_pendings(hart_id as u8)
            .expect |
| `hal/src/component/irq/loongarch64/mod.rs#enable_irq` | `os/src/drivers/irqchip/la64/loongson_liointc.rs#enable_irq` | 同板块同名 | Eiointc::enable_irq, log::warn!, self.platic.read_w, self.platic.write_b, self.platic.write_w | ConfigVector::new, self.enable, self.route |
| `hal/src/component/irq/loongarch64/eiointc.rs#enable_irq` | `os/src/drivers/irqchip/la64/loongson_pch_pic.rs#enable_irq` | 同板块同名 | iocsr_read_d, iocsr_write_d, log::warn! | self.enable, self.route, self.set_ht_msi_vector |
| `hal/src/component/irq/loongarch64/mod.rs#disable_irq` | `os/src/drivers/irqchip/la64/loongson_eiointc.rs#disable_irq` | 同板块同名 | Eiointc::disable_irq, log::warn!, self.platic.read_w, self.platic.write_w | self.next_icu.disable_irq |

#### 系统调用
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `user/src/lib.rs#execve` | `user/src/lib.rs#execve` | 同板块同名 | CString::new, CString::new(*s).unwrap, CString::new(path).unwrap, argv.as_ptr, argv.iter, argv.iter().map ...(+12) | (*s).as_ptr, argv.as_ptr, argv.iter, argv.iter().map, argv.iter().map(|s| (*s).as_ptr() as usize).collect, argv.push ...(+6) |
| `os/src/syscall/mm.rs#sys_membarrier` | `os/src/syscall/mm.rs#sys_membarrier` | 同板块同名 | Err, Ok, fence | Ok, info! |
| `user/src/syscall.rs#sys_sendto` | `user/src/syscall.rs#sys_sendto` | 同板块同名 | println!, syscall | syscall |
| `user/src/syscall.rs#syscall` | `user/src/syscall.rs#syscall` | 同板块同名 | asm! | asm! |

#### 文件系统
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `os/src/fs/vfs/dentry.rs#walk` | `os/src/fs/tmp/inode.rs#walk` | 同板块同名 | DCACHE.lock, DCACHE.lock().insert, Ok, current_dentry.clone, current_dentry.clone().lookup_child_dentry, current_dentry.get_child ...(+9) | file.check_inode_exist, self.file.lock |
| `os/src/fs/devfs/tty.rs#ioctl` | `os/src/fs/devfs/tty.rs#ioctl` | 同板块同名 | Err, Ok, TtyIoctlCmd::from_repr, copy_in_ioctl_value, copy_out_ioctl_value, log::debug! ...(+2) | Err, Ok, TtyIoctlCmd::try_from, TtyIoctlCmd::try_from(op).map_err, arg.into, info! ...(+7) |
| `os/src/fs/vfs/file.rs#seek` | `os/src/drivers/disk.rs#seek` | 同板块同名 | Ok, off.unsigned_abs, pos.checked_add, pos.checked_add(off).ok_or, pos.checked_sub, pos.checked_sub(off).ok_or ...(+10) | Ok, Some, debug!, dev
                .position, dev
                .position()
                .checked_add_signed, dev
                .position()
                .checked_add_signed(off as isize)
                .map ...(+7) |
| `os/src/drivers/block/mmc/mod.rs#reset_clock` | `os/src/drivers/vf2/dw/vf2_sdio.rs#reset_clock` | 同板块同名 | CLKDIV::new, CLKDIV::new().with_clk_divider0, CLKDIV::offset, CLKENA::new, CLKENA::new().with_cclk_enable, CLKENA::offset ...(+8) | CLKDIV::new, CLKDIV::new().with_clk_divider0, CLKDIV::offset, CLKENA::new, CLKENA::new().with_cclk_enable, CLKENA::offset ...(+8) |

#### 设备驱动
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `os/src/drivers/serial/uart.rs#init_u32` | `os/src/drivers/tty/serial/ns16550a.rs#init_u32` | 同板块同名 | log::info!, reg.byte_add, reg.byte_add(0 << self.reg_shift)
                .write_volatile, reg.byte_add(1 << self.reg_shift)
                .write_volatile, reg.byte_add(FCR << self.reg_shift).write_volatile, reg.byte_add(IER << self.reg_shift).write_volatile ...(+2) | info!, reg.byte_add, reg.byte_add(0 << self.reg_shift)
                    .write_volatile, reg.byte_add(1 << self.reg_shift)
                    .write_volatile, reg.byte_add(FCR << self.reg_shift).write_volatile, reg.byte_add(IER << self.reg_shift).write_volatile ...(+2) |
| `os/src/drivers/serial/uart.rs#init_u8` | `os/src/drivers/tty/serial/ns16550a.rs#init_u8` | 同板块同名 | reg.byte_add, reg.byte_add(0 << self.reg_shift)
                .write_volatile, reg.byte_add(1 << self.reg_shift)
                .write_volatile, reg.byte_add(IER << self.reg_shift).write_volatile, reg.byte_add(LCR << self.reg_shift).write_volatile, reg.byte_add(MCR << self.reg_shift).write_volatile | reg.byte_add, reg.byte_add(0 << self.reg_shift)
                    .write_volatile, reg.byte_add(1 << self.reg_shift)
                    .write_volatile, reg.byte_add(IER << self.reg_shift).write_volatile, reg.byte_add(LCR << self.reg_shift).write_volatile, reg.byte_add(MCR << self.reg_shift).write_volatile |
| `os/src/drivers/serial/uart.rs#send_u8` | `os/src/drivers/tty/serial/ns16550a.rs#send_u8` | 同板块同名 | ptr.byte_add, ptr.byte_add(THR << self.reg_shift).write_volatile, wait_for! | ptr.byte_add, ptr.byte_add(THR << self.reg_shift).write_volatile, wait_for! |
| `os/src/drivers/serial/uart.rs#send_u32` | `os/src/drivers/tty/serial/ns16550a.rs#send_u32` | 同板块同名 | b' '.into, c.into, ptr.byte_add, ptr.byte_add(THR << self.reg_shift)
                        .write_volatile, ptr.byte_add(THR << self.reg_shift).write_volatile, wait_for! | b' '.into, c.into, ptr.byte_add, ptr.byte_add(THR << self.reg_shift)
                        .write_volatile, ptr.byte_add(THR << self.reg_shift).write_volatile, wait_for! |

#### 网络协议栈
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `os/src/net/tcp.rs#shutdown` | `os/src/net/tcp.rs#shutdown` | 同板块同名 | Err, LISTEN_TABLE.unlisten, Ok, SOCKET_SET.check_poll, SOCKET_SET.poll_interfaces, SOCKET_SET.with_socket_mut::<tcp::Socket, _, _> ...(+11) | NET_DEV.lock, NET_DEV.lock().poll, Ok, SOCKET_SET.lock, Some, binding.get_mut::<tcp::Socket> ...(+5) |
| `os/src/net/tcp.rs#bind` | `os/src/net/tcp.rs#bind` | 同板块同名 | Err, Ok, get_ephemeral_port, info!, self.local_endpoint, self.local_endpoint().ok_or ...(+4) | Err, IpEndpoint::try_from, Ok, addr.clone, info!, self.do_bind ...(+2) |
| `os/src/net/udp.rs#bind` | `os/src/net/unix.rs#bind` | 同板块同名 | Err, Ok, SOCKET_SET.with_socket_mut::<smoltcp::socket::udp::Socket, _, _>, Some, local_addr.is_some, log::info! ...(+5) | unimplemented! |
| `os/src/net/listen_table.rs#listen` | `os/src/net/tcp.rs#listen` | 同板块同名 | Box::new, Err, ListenEntry::new, Ok, Some, entry.is_none ...(+1) | Err, Ok, info!, self.set_state, self.sockmeta.lock, self.with_socket ...(+5) |

#### 文件系统/第三方库
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `os/src/fs/ext4/inode.rs#rename` | `os/src/fs/ext4/inode.rs#rename` | 同板块同名 | Err, InodeMode::from_inode_type, InodeMode::from_inode_type(ty).get_type, Ok, file
                .file_rename, file
                .file_rename(old_path, target)
                .map_err ...(+14) | Err, Ext4Inode::new, Ok, block_on, ext4file.file_rename, new_dentry.bind ...(+18) |
| `os/src/fs/ext4/file.rs#read_all` | `os/src/fs/ext4/inode.rs#read_all` | 同板块同名 | SeekFrom::Current, Vec::new, i64::try_from, inode.clone, inode.clone().cache_read_at, self.inode ...(+4) | Ok, debug!, self.read_at, vec! |
| `os/src/fs/ext4/inode.rs#truncate` | `os/src/fs/ext4/inode.rs#truncate` | 同板块同名 | Ok, SysError::from_i32, file
            .file_truncate, file
            .file_truncate(size as _)
            .map_err, file.file_close, file.file_open ...(+8) | Errno::EIO.into, file.file_close, file.file_open, file.file_truncate, file.get_path, info! ...(+11) |
| `os/src/fs/ext4/inode.rs#read_at` | `os/src/fs/ext4/inode.rs#read_at` | 同板块同名 | debug!, file.file_close, file.file_open, file.file_read, file.file_seek, file.get_path ...(+5) | buf.len, cache.read, info!, self.get_size, self.read_dirctly |

#### 通用模块/其他
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `utils/range-map/src/lib.rs#find_free_range` | `os/src/utils/container/range_map.rs#find_free_range` | 同板块同名 | Some, debug_assert!, self.0.range, self.0.range(..=start).next_back, start.min | Some, debug_assert!, self.0.range, self.0.range(..=start).next_back, start.min |
| `os/src/utils/ring_buffer.rs#peek` | `os/src/utils/cache.rs#peek` | 同板块同名 | buf.is_empty, buf.len, buf[..ret_len].copy_from_slice, buf[..right_len].copy_from_slice, buf[right_len..ret_len].copy_from_slice, cmp::min ...(+2) | todo! |
| `utils/range-map/src/lib.rs#try_insert` | `os/src/utils/container/range_map.rs#try_insert` | 同板块同名同结构 | Err, Ok, debug_assert!, self.0.range, self.0.range(..range.end).next_back, self.0.try_insert ...(+2) | Err, Ok, debug_assert!, self.0.range, self.0.range(..range.end).next_back, self.0.try_insert ...(+2) |
| `hal/src/util/mutex.rs#lock` | `os/src/sync/mutex/spin.rs#lock` | 同板块同名 | SieGuard::new, core::hint::spin_loop, panic!, self.mutex.compare_exchange, self.mutex.compare_exchange(false, true, Ordering::Acquire, Ordering::Relaxed).is_ok | S::before_lock, self
                .lock
                .compare_exchange, self
                .lock
                .compare_exchange(false, true, Ordering::Acquire, Ordering::Relaxed)
                .is_ok, self.wait_unlock |


#### 1. 宏观判定
- **定性结论**: 独立开发 (相似度: 3.61%，共同 AST 指纹: 138；唯一 AST 指纹: 目标项目 2284，参考项目 1681；覆盖率: 目标项目 6.04%，参考项目 8.21%)

#### 2. 全模块覆盖矩阵解读
- **进程管理**: 双方均存在。共同 AST 指纹 6 个，目标覆盖率 2.41%，参考覆盖率 3.3%。覆盖率极低，表明双方在进程模型、身份管理、调度集成上存在根本性架构差异。证据强度：**弱**。
- **任务调度/异步运行时**: 双方均存在。共同 AST 指纹 6 个，目标覆盖率 1.61%，参考覆盖率 2.67%。覆盖率极低，双方执行器与调度队列设计（如多核负载均衡 vs 双优先级队列）完全不同。证据强度：**弱**。
- **内存管理**: 双方均存在。共同 AST 指纹 7 个，目标覆盖率 1.28%，参考覆盖率 2.11%。覆盖率极低，表明内存分配、地址空间及页表管理策略高度异构。证据强度：**弱**。
- **中断/异常处理**: 双方均存在。共同 AST 指纹 1 个，目标覆盖率 1.92%，参考覆盖率 1.22%。覆盖率极低，中断控制器驱动与 Trap 分发逻辑独立实现。证据强度：**弱**。
- **系统调用**: 双方均存在。共同 AST 指纹 12 个，目标覆盖率 8.33%，参考覆盖率 16.0%。覆盖率相对较高，但主要源于 POSIX 标准系统调用名及 `async-task` 等公共 crate 的强制 API 收敛。证据强度：**中**（经分析，为规范与公共依赖驱动）。
- **文件系统**: 双方均存在。共同 AST 指纹 21 个，目标覆盖率 5.83%，参考覆盖率 5.71%。覆盖率低，VFS 结构、Ext4 驱动集成方式及设备文件实现存在显著差异。证据强度：**弱**。
- **设备驱动**: 双方均存在。共同 AST 指纹 30 个，目标覆盖率 15.62%，参考覆盖率 22.73%。覆盖率较高，但集中于 NS16550A UART 和 DW MMC 控制器，这些相似性由硬件寄存器手册和第三方 `dw-apb-uart`/`dw_mmc` 驱动模板强制规定。证据强度：**中**（经分析，为硬件规范驱动）。
- **网络协议栈**: 双方均存在。共同 AST 指纹 0 个，覆盖率 0.0%。双方虽均集成 smoltcp，但套接字管理层与监听表实现完全异构。证据强度：**未形成**。
- **第三方库/公共依赖**: 双方均存在。共同 AST 指纹 0 个，覆盖率 0.0%。双方依赖的 `async-task`、`smoltcp`、`ext4_rs` 等库以不同方式被封装和调用，未产生共同指纹。证据强度：**未形成**。

#### 3. 底层 AST 关键证据指纹表
| 所属板块 | 目标项目路径 | 参考项目路径 | 证据类型 | 技术说明 |
| :--- | :--- | :--- | :--- | :--- |
| 进程管理 | `os/src/task/task.rs#add_child` | `os/src/task/task.rs#add_child` | 同板块同名同结构 | 均采用 `self.children.lock().insert` 维护子进程集合，但目标使用 `child.gettid`，参考使用 `child.get_pid`，反映线程 vs 进程模型差异。 |
| 进程管理 | `os/src/sync/mutex/spin_mutex.rs#wait_unlock` | `os/src/sync/mutex/spin.rs#wait_unlock` | 同板块同名 | 自旋原语 `spin_loop` + `panic!` 为 Rust no_std 环境通用惯用模式。目标锁基于 `owner`，参考基于 `lock` 布尔标志。 |
| 进程管理 | `os/src/processor/processor.rs#current_task` | `os/src/task/processor.rs#current_task` | 同板块同名 | 获取 per-CPU 当前任务指针的惯用模式 `get_xxx().current`，辅助函数命名不同。 |
| 进程管理 | `os/src/syscall/process.rs#sys_getpgid` | `os/src/syscall/process.rs#sys_getpgid` | 同板块同名 | 实现 POSIX `getpgid` 语义，均使用 `ok_or` 转换 `Option`。目标用字段 `task.pgid`，参考用方法 `task.get_pgid()`。 |
| 任务调度/异步运行时 | `os/src/executor/mod.rs#spawn` | `os/src/task/executor.rs#spawn` | 同板块同名 | 均调用第三方 crate `async_task::spawn` 和 `WithInfo`。目标集成 SMP 调度逻辑，参考使用双优先级队列。 |
| 系统调用 | `os/src/syscall/mod.rs#syscall` | `os/src/syscall/mod.rs#syscall` | 同板块同名 | 系统调用分发器，均基于 `SyscallId` 进行模式匹配。分发数量、具体实现及错误处理架构完全不同。 |
| 文件系统 | `os/src/fs/vfs/dentry.rs#walk` | `os/src/fs/tmp/inode.rs#walk` | 同板块同名 | 路径解析遍历，目标基于 Dentry 缓存，参考基于 Inode 文件检查，VFS 模型异构。 |
| 设备驱动 | `os/src/drivers/serial/uart.rs#init_u32` | `os/src/drivers/tty/serial/ns16550a.rs#init_u32` | 同板块同名 | NS16550A UART 初始化序列，由硬件数据手册规定，寄存器操作序列必然一致。 |
| 设备驱动 | `os/src/drivers/block/mmc/mod.rs#send_cmd` | `os/src/drivers/vf2/dw/vf2_sdio.rs#send_cmd` | 同板块同名 | DW MMC 控制器命令发送流程，由硬件手册和第三方 `dw_mmc` 驱动模板强制规定。 |
| 第三方库/公共依赖 | `utils/range-map/src/lib.rs#try_insert` | `os/src/utils/container/range_map.rs#try_insert` | 同板块同名同结构 | 区间映射插入逻辑，均基于 `BTreeMap` 的 `range` 方法，属于 Rust 标准库惯用模式。 |

#### 4. 核心代码调用流对比剖析
* **【进程管理/任务退出路径】**
* **底层技术析理**: 目标项目将进程退出逻辑封装在 `TaskControlBlock::do_exit` 和 `do_group_exit` 方法中，通过 `thread_group.lock()` 遍历线程组并逐一调用 `do_exit`，体现了线程组为中心的设计。参考项目则在系统调用层内联处理，直接调用 `task.set_exit_code`、`task.set_zombie` 或 `task.kill_all_thread`，其进程模型未显式区分线程组操作。两者在退出逻辑的抽象层次、错误处理路径及状态管理上存在根本性架构差异。
* **代码溯源链接**:
  - 目标项目: `os/src/syscall/process.rs#sys_exit`
  - 参考项目: `os/src/syscall/process.rs#sys_exit`

* **【任务调度/异步运行时/任务生成】**
* **底层技术析理**: 两者均依赖第三方 `async-task` crate 的 `spawn` 和 `WithInfo`，这是社区标准 API。目标项目在此基础上集成了复杂的 SMP 调度逻辑，调用 `select_run_queue_index`、`enqueue_on_processor` 和 `turn_cpu_mask_id` 以实现多核负载均衡。参考项目则采用简单的双优先级队列设计，通过 `push_normal` 和 `push_prior` 管理任务。两者在调度策略和并发模型上截然不同。
* **代码溯源链接**:
  - 目标项目: `os/src/executor/mod.rs#spawn`
  - 参考项目: `os/src/task/executor.rs#spawn`

* **【设备驱动/NS16550A UART 初始化】**
* **底层技术析理**: 两者对 NS16550A UART 的初始化序列（`init_u32`/`init_u8`）在寄存器操作上高度一致，均包含对 FCR、IER、LCR、MCR 等标准寄存器的特定值写入。这种相似性由 NS16550A 芯片的数据手册和标准初始化流程强制规定，属于硬件规范驱动的必然收敛，而非代码复用。
* **代码溯源链接**:
  - 目标项目: `os/src/drivers/serial/uart.rs#init_u32`
  - 参考项目: `os/src/drivers/tty/serial/ns16550a.rs#init_u32`

#### 5. 单方模块与降权项
- **跨板块同名函数**: 控方原始证据中的 `initproc` (目标 `task` vs 参考 `fs`)、`do_group_exit` (目标 `task` vs 参考 `signal`)、`sys_sigprocmask` (目标 `user` vs 参考 `os`) 等，因模块归属、抽象层级和语义完全不同，属于跨板块硬凑，不构成同源证据，已全部降权或排除。
- **未实现桩函数**: 参考项目中 `sys_setresuid`、`sys_setgid`、`sys_getegid` 等函数仅返回 `Ok` 和日志，为未实现桩。目标项目对这些系统调用进行了完整的权限设置逻辑实现。将完整实现与空桩进行比对无意义，已降权。
- **内部闭环逻辑**: `child_time_pair` 与 `get_child_ustime` 等函数无外部调用图谱支撑，无法进行有效的结构比对，已降权。
- **第三方库/公共依赖**: `async_task::spawn`、`WithInfo` 来自 `async-task` crate；`CLKDIV::new().with_clk_divider0` 等来自硬件驱动模板。这些相似性源于共享依赖，不视为项目间复用。

#### 6. 保守结论
基于 3.61% 的极低 AST 相似度及双方海量唯一指纹，目标项目与参考项目在底层架构上存在显著差异，判定为独立开发。所有观察到的相似性均可被 POSIX 系统调用规范、Rust no_std 惯用模式、第三方公共 crate API 及硬件寄存器手册充分解释。特别是在进程管理、任务调度等核心子系统上，两者展现出截然不同的抽象层次、同步机制和并发模型。现有 AST 证据无法支持代码复用或抄袭的指控，目标项目具备充分的原创合理性。

------------------------------

