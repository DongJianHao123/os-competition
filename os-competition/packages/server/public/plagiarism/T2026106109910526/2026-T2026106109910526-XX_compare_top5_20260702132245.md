# 🛡️ OS 内核赛自动化代码级架构审计报告

**审计时间**: `2026-07-02 13:22:45`
**参赛作品名称**: `2026-T2026106109910526-XX`
**年份**: `2026`
**学校**: `XX`
**队伍名称**: `T2026106109910526`
**比赛名字**: `OS-Competition`
**比赛子赛道**: `Kernel-Implementation`
**仓库地址**: `https://gitlab.eduxiji.net/educg-group-43501-3132633/T2026106109910526-568`
**推测开发语言**: `Rust / C (由AST探针自动识别)`
**特征函数量**: `2758`

## ⚔️ 底层代码同源性对比 (AST Homology)
> 本部分由专门的代码审查智能体，对该项目与符合年份条件的历届最高相似度项目进行控制流与底层调用对比。新版报告会先给出确定性的全模块覆盖矩阵，再给出 Agent 的分模块解释，避免只围绕少量抽样函数展开。

### 🏆 Top 5 底层对标: vs `2023-MankorOS-哈尔滨工业大学深圳`（哈尔滨工业大学（深圳） / MankorOS）

#### 对方作品信息

**对方年份**: `2023`
**对方学校**: `哈尔滨工业大学（深圳）`
**对方队伍名称**: `MankorOS`
**对方比赛名字**: `OS-Competition`
**对方比赛子赛道**: `Kernel-Implementation`
**对方仓库地址**: `https://gitlab.eduxiji.net/MankorOS/OSKernel2023-MankorOS`

> 双向覆盖率：共同 AST 指纹: 99；唯一 AST 指纹: 目标项目 2284，参考项目 1233；覆盖率: 目标项目 4.33%，参考项目 8.03%

## 全模块对比总览（确定性证据）

> 总体覆盖率：共同 AST 指纹: 99；唯一 AST 指纹: 目标项目 2284，参考项目 1233；覆盖率: 目标项目 4.33%，参考项目 8.03%

### 1. 模块覆盖矩阵
| 模块 | 目标函数数 | 参考函数数 | 共同唯一 AST 指纹 | Target 覆盖 | Base 覆盖 | 状态 |
| :--- | ---: | ---: | ---: | ---: | ---: | :--- |
| 进程管理 | 287 | 264 | 6 | 2.41% | 2.5% | 双方存在且有结构重合 |
| 任务调度/异步运行时 | 393 | 79 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |
| 内存管理 | 623 | 320 | 2 | 0.37% | 0.71% | 双方存在且有结构重合 |
| 中断/异常处理 | 77 | 28 | 1 | 1.92% | 3.85% | 双方存在且有结构重合 |
| 系统调用 | 174 | 12 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |
| 文件系统 | 488 | 475 | 19 | 5.28% | 4.95% | 双方存在且有结构重合 |
| 设备驱动 | 229 | 123 | 31 | 16.15% | 28.7% | 双方存在且有结构重合 |
| 网络协议栈 | 163 | 0 | 0 | 0.0% | 0.0% | 仅目标项目体现 |
| 文件系统/第三方库 | 66 | 0 | 0 | 0.0% | 0.0% | 仅目标项目体现 |
| 通用模块/其他 | 258 | 159 | 11 | 4.78% | 8.09% | 双方存在且有结构重合 |

### 2. 分模块证据清单

#### 进程管理
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `os/src/syscall/process.rs#sys_execve` | `src/syscall/process.rs#sys_execve` | 同板块同名 | "busybox".to_string, "sh".to_string, AtFlags::AT_FDCWD.bits, AtFlags::empty, Err, FileReader::new ...(+71) | Ok, Path::from_string, Some, String::from, UserReadPtr::<u8>::from, UserReadPtr::<usize>::from ...(+24) |
| `os/src/syscall/process.rs#sys_clone` | `src/syscall/process.rs#sys_clone` | 同板块同名 | CloneFlags::LEGACY_FLAGS.bits, CloneFlags::from_bits, CloneFlags::from_bits((flags & CloneFlags::LEGACY_FLAGS.bits()) & !0xff)
        .ok_or, Err, Ok, Some ...(+17) | CloneFlags::from_bits_truncate, Err, Into::<usize>::into, Ok, Some, VirtAddr::from ...(+18) |
| `os/src/task/task.rs#do_exit` | `src/process/lproc.rs#do_exit` | 同板块同名 | TASK_MANAGER.remove_task, drop, log::error!, log::info!, os_is_shutting_down, os_send_shutdown ...(+14) | Arc::downgrade, GlobalLProcManager::get, GlobalLProcManager::get(Pid::from(1)).unwrap, Pid::from, Some, c.parent.lock ...(+19) |
| `os/src/executor/mod.rs#run_until_idle` | `src/executor/mod.rs#run_until_idle` | 同板块同名 | TASK_QUEUE.fetch, current_processor, current_processor()
            .unwrap_with_mut_task_queue, current_processor()
            .unwrap_with_mut_task_queue(|task_queue| task_queue.pop_front())
            .flatten, current_processor().need_migrate_check, enqueue_on_processor ...(+9) | TASK_QUEUE.fetch, task.run |

#### 任务调度/异步运行时
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `os/src/syscall/io.rs#sys_ppoll` | `src/syscall/io.rs#sys_ppoll` | 同板块同名 | Err, Ok, PollEvents::empty, Select2Futures::new, Some, TimedTaskFuture::new ...(+38) | AnyFuture::new_with, BTreeMap::<usize, (usize, i16)>::new, Ok, UserInOutPtr::<PollFd>::from_usize, Vec::<Async<SysResult<usize>>>::new, add_func_call ...(+24) |
| `os/src/syscall/fs.rs#sys_renameat2` | `src/syscall/fs.rs#sys_renameat2` | 同板块同名 | Arc::ptr_eq, Err, Ok, RenameFlags::from_bits, RenameFlags::from_bits(flags).ok_or, UserPtrRaw::new ...(+47) | Err, Ok, UserReadPtr::<u8>::from, new_dir.attach, new_dir.clone, new_dir.kind ...(+11) |
| `os/src/syscall/fs.rs#sys_mount` | `src/syscall/fs.rs#sys_mount` | 同板块同名 | Err, MountFlags::from_bits, MountFlags::from_bits(flags).ok_or, Ok, Some, String::new ...(+50) | Ok, Path::from_string, UserReadPtr::<u8>::from, VfsFileRef::new, cwd.to_string, debug_assert! ...(+16) |
| `os/src/syscall/fs.rs#sys_utimensat` | `src/syscall/fs.rs#sys_utimensat` | 同板块同名 | AtFlags::AT_FDCWD.bits, AtFlags::from_bits_truncate, Err, Ok, String::new, TimeSpec::default ...(+38) | Err, Ok, TimeChange::new_now, TimeChange::new_omit, TimeChange::new_time, TimeInfoChange::new ...(+18) |

#### 内存管理
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `os/src/drivers/block/mmc/mod.rs#card_init` | `src/drivers/blk/dw_mshc/mmc.rs#card_init` | 同板块同名 | CID::from, CLKDIV::offset, CMD::data_cmd, CMD::no_data_cmd, CMD::no_data_cmd_no_crc, CMD::no_data_cmd_no_crc(card_idx, 2).with_response_length ...(+39) | CID::from, CLKDIV::offset, CMD::data_cmd, CMD::no_data_cmd, CMD::no_data_cmd_no_crc, CMD::no_data_cmd_no_crc(card_idx, 2).with_response_length ...(+38) |
| `os/src/drivers/block/mmc/mod.rs#send_cmd` | `src/drivers/blk/dw_mshc/mmc.rs#send_cmd` | 同板块同名 | CMD::offset, CMDARG::offset, RESP::offset, RINSTS::offset, Some, arg.into ...(+22) | CMD::offset, CMDARG::offset, RESP::offset, RINSTS::offset, Some, arg.into ...(+27) |
| `os/src/devices/manager.rs#map_devices` | `src/drivers/manager.rs#map_devices` | 同板块同名 | "virtio-net".to_string, Arc::new, IrqCtrl::from_dt, MmioManager::scan_mmio_root, PciManager::scan_pcie_root, Some ...(+22) | PTEFlags::rw, VirtAddr::from, boot::boot_pagetable_paddr, boot::boot_pagetable_paddr().into, core::mem::forget, dev.mmio_base ...(+11) |
| `os/src/syscall/fs.rs#sys_openat` | `src/syscall/io.rs#sys_openat` | 同板块同名 | AtFlags::from_bits_truncate, Err, Ok, OpenFlags::from_bits, OpenFlags::from_bits(flags as i32).ok_or, UserPtrRaw::new ...(+42) | Box::new, Err, Ok, OpenFlags::from_bits_truncate, Path::from_str, Path::from_string ...(+27) |

#### 中断/异常处理
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `hal/src/component/irq/loongarch64/eiointc.rs#claim_irq` | `src/drivers/plic.rs#claim_irq` | 同板块同名 | Some, flags0.leading_zeros, flags1.leading_zeros, flags2.leading_zeros, flags3.leading_zeros, iocsr_read_d | (*plic).claim, PLICCtxWrapper::new, irq.get, irq.map, kernel_phys_dev_to_virt |
| `hal/src/component/irq/loongarch64/mod.rs#enable_irq` | `src/drivers/plic.rs#enable_irq` | 同板块同名 | Eiointc::enable_irq, log::warn!, self.platic.read_w, self.platic.write_b, self.platic.write_w | (*plic).enable, (*plic).set_priority, (*plic).set_threshold, PLICCtxWrapper::new, PLICSrcWrapper::new, kernel_phys_dev_to_virt |
| `hal/src/component/irq/loongarch64/eiointc.rs#enable_irq` | `src/trap/trap.rs#enable_irq` | 同板块同名 | iocsr_read_d, iocsr_write_d, log::warn! | sstatus::set_sie |
| `hal/src/component/irq/riscv64.rs#complete_irq` | `src/drivers/plic.rs#complete_irq` | 同板块同名 | (*plic).complete, PLICCtxWrapper::new, PLICSrcWrapper::new, log::warn! | (*plic).complete, PLICCtxWrapper::new, PLICSrcWrapper::new, kernel_phys_dev_to_virt |

#### 系统调用
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `os/src/syscall/net.rs#set` | `src/syscall/io.rs#set` | 同板块同名 | Duration::from_secs, Err, Ok, SOCKET_SET.with_socket_mut::<smoltcp::socket::tcp::Socket, _, _>, Some, opt.as_ptr ...(+12) | 内部闭环逻辑 |
| `os/src/syscall/io.rs#is_set` | `src/syscall/io.rs#is_set` | 同板块同名 | 内部闭环逻辑 | 内部闭环逻辑 |
| `os/src/syscall/misc.rs#from_str` | `src/syscall/misc.rs#from_str` | 同板块同名 | data.len, data[..copy_len].copy_from_slice, info.as_bytes, info.len, info.len().min | data[..info.len()].copy_from_slice, info.as_bytes, info.len |
| `os/src/syscall/io.rs#clear` | `src/syscall/io.rs#clear` | 同板块同名 | self.fds_bits.fill | self.fds_bits.len |

#### 文件系统
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `os/src/fs/vfs/dentry.rs#find` | `src/fs/nfat32/fs.rs#find` | 同板块同名 | DCACHE.lock, Ok, Some, Vec::new, cache.get, compoents.join ...(+14) | Some, self.next |
| `os/src/fs/devfs/tty.rs#ioctl` | `src/fs/new_vfs/top.rs#ioctl` | 同板块同名 | Err, Ok, TtyIoctlCmd::from_repr, copy_in_ioctl_value, copy_out_ioctl_value, log::debug! ...(+2) | Ok, dyn_future |
| `os/src/fs/fat32/inode.rs#create` | `src/fs/memfs/tmpdir.rs#create` | 同板块同名 | Arc::new, Err, InodeInner::new, String::from, UPSafeCell::new, dir.inner
                    .create_dir ...(+9) | Err, Ok, Self::new, TmpFile::new, VfsFileRef::new, children.contains_key ...(+8) |
| `os/src/fs/fat32/inode.rs#lookup` | `src/fs/new_vfs/path_cache.rs#lookup` | 同板块同名 | Arc::new, InodeInner::new, Some, String::from, UPSafeCell::new, dir.inner
        .iter ...(+12) | Err, Ok, dyn_future, file.clone, here!, name.to_string ...(+8) |

#### 设备驱动
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `os/src/drivers/serial/uart.rs#init_u32` | `src/drivers/serial/uart8250.rs#init_u32` | 同板块同名 | log::info!, reg.byte_add, reg.byte_add(0 << self.reg_shift)
                .write_volatile, reg.byte_add(1 << self.reg_shift)
                .write_volatile, reg.byte_add(FCR << self.reg_shift).write_volatile, reg.byte_add(IER << self.reg_shift).write_volatile ...(+2) | info!, reg.byte_add, reg.byte_add(0 << self.reg_shift).write_volatile, reg.byte_add(1 << self.reg_shift).write_volatile, reg.byte_add(FCR << self.reg_shift).write_volatile, reg.byte_add(IER << self.reg_shift).write_volatile ...(+2) |
| `os/src/drivers/serial/uart.rs#init_u8` | `src/drivers/serial/uart8250.rs#init_u8` | 同板块同名 | reg.byte_add, reg.byte_add(0 << self.reg_shift)
                .write_volatile, reg.byte_add(1 << self.reg_shift)
                .write_volatile, reg.byte_add(IER << self.reg_shift).write_volatile, reg.byte_add(LCR << self.reg_shift).write_volatile, reg.byte_add(MCR << self.reg_shift).write_volatile | reg.byte_add, reg.byte_add(0 << self.reg_shift).write_volatile, reg.byte_add(1 << self.reg_shift).write_volatile, reg.byte_add(FCR << self.reg_shift).write_volatile, reg.byte_add(IER << self.reg_shift).write_volatile, reg.byte_add(LCR << self.reg_shift).write_volatile ...(+1) |
| `os/src/drivers/block/pci_blk.rs#try_new` | `src/drivers/blk/virtio.rs#try_new` | 同板块同名 | Arc::new, BLK_ID.fetch_add, BufferCache::new, Err, Ok, UPSafeCell::new ...(+7) | InnerDev::new, InnerDev::new(transport).map_err, Ok, UnsafeCell::new |
| `os/src/drivers/serial/uart.rs#send_u8` | `src/drivers/serial/uart8250.rs#send_u8` | 同板块同名同结构 | ptr.byte_add, ptr.byte_add(THR << self.reg_shift).write_volatile, wait_for! | ptr.byte_add, ptr.byte_add(THR << self.reg_shift).write_volatile, wait_for! |

#### 网络协议栈
仅目标项目中观察到该模块代表函数，参考项目没有同模块可对齐证据：
- 目标项目：`os/src/net/tcp.rs#recv`，调用摘要：Err, Ok, SOCKET_SET.with_socket_mut::<tcp::Socket,_,_>, get_current_time_duration, get_waker, info! ...(+18)
- 目标项目：`os/src/net/udp.rs#send_to`，调用摘要：Err, Ok, SOCKET_SET.with_socket_mut::<smoltcp::socket::udp::Socket,_,_>, data.len, get_waker, log::debug! ...(+14)
- 目标项目：`os/src/net/udp.rs#recv`，调用摘要：Err, Ok, SOCKET_SET.with_socket_mut::<smoltcp::socket::udp::Socket, _, _>, get_waker, log::info!, log::warn! ...(+10)
- 目标项目：`os/src/net/udp.rs#send`，调用摘要：Err, Ok, SOCKET_SET.with_socket_mut::<smoltcp::socket::udp::Socket,_,_>, data.len, get_waker, log::warn! ...(+12)

#### 文件系统/第三方库
仅目标项目中观察到该模块代表函数，参考项目没有同模块可对齐证据：
- 目标项目：`os/src/fs/ext4/dentry.rs#load_child_dentry`，调用摘要：Arc::new, DCACHE
                    .lock, DCACHE
                    .lock()
                    .insert, Err, Ext4Inode::new, Ext4Inode::new_with_size ...(+29)
- 目标项目：`os/src/fs/ext4/inode.rs#getxattr`，调用摘要：XstatMask::from_bits_truncate, cache.end, cmp::max, file.get_type, inner.atime, inner.ctime ...(+8)
- 目标项目：`os/src/fs/ext4/inode.rs#cache_read_at`，调用摘要：Arc::get_mut, Arc::get_mut(&mut page)
                    .ok_or, Arc::get_mut(&mut page)
                    .ok_or(SysError::EIO as i32)?
                    .read_from, Ok, Page::new, Page::new(page_offset).map_err ...(+14)
- 目标项目：`os/src/fs/ext4/inode.rs#cache_write_at`，调用摘要：Arc::get_mut, Arc::get_mut(&mut page)
                        .ok_or, Arc::get_mut(&mut page)
                        .ok_or(SysError::EIO as i32)?
                        .read_from, Ok, Page::new, Page::new(page_offset).map_err ...(+18)

#### 通用模块/其他
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `hal/src/util/mutex.rs#lock` | `src/sync/mutex.rs#lock` | 同板块同名 | SieGuard::new, core::hint::spin_loop, panic!, self.mutex.compare_exchange, self.mutex.compare_exchange(false, true, Ordering::Acquire, Ordering::Relaxed).is_ok | S::before_lock, self.ensure_support, self.obtain_lock |
| `utils/range-map/src/lib.rs#get_mut` | `src/lazy_init.rs#get_mut` | 同板块同名 | Some, self.0.range_mut, self.0.range_mut(..=key).next_back | self.check_init, self.get_mut_unchecked |
| `os/src/sync/lazy.rs#deref_mut` | `src/sync/sleep.rs#deref_mut` | 同板块同名 | panic!, self.once_flag.load, self.payload.get_mut | self.mutex.data.get |
| `utils/range-map/src/lib.rs#get` | `src/tools/hash.rs#get` | 同板块同名 | Some, self.0.range, self.0.range(..=key).next_back | Some, bucket.load, key.hash, node.value.equal, np.as_mut, np.is_null ...(+1) |


好的，我将遵循审计指令，基于提供的客观证据、控辩双方论点及原始AST抽样，进行最终的同源性定性仲裁。

### 1. 宏观判定
- **定性结论**: **独立开发 (底层架构显著差异)**
- **核心依据**: 客观AST相似度仅为 **2.9%**，远低于可认定同源的阈值。目标项目覆盖率 **4.33%**，参考项目覆盖率 **8.03%**，表明双方在代码实现层面存在巨大差异。控方所指控的“复用”点，经AST剖析，均为操作系统内核开发中实现标准接口或使用通用库时不可避免的语法与范式重合，不构成实质性抄袭证据。

### 2. 全模块覆盖矩阵解读
| 模块 | 双方存在 | 共同AST指纹 | 覆盖率 (目标/参考) | 证据强度 | 解读 |
| :--- | :---: | :---: | :---: | :---: | :--- |
| **进程管理** | 是 | 6 | 2.41% / 2.5% | **弱** | 双方均实现了Linux标准的进程管理接口，同名函数众多。但极低的覆盖率表明，即使在同一板块，底层实现（如TCB结构、调度器交互）也截然不同。控方证据多为`sys_*`标准系统调用名和`lock().clone()`等通用模式。 |
| **任务调度/异步运行时** | 是 | 0 | 0.0% / 0.0% | **未形成** | 这是操作系统最核心的模块之一。**零共同指纹**是独立开发的最强铁证。目标项目实现了复杂的SMP多核调度，而参考项目为单核轮询，架构设计完全不同。 |
| **内存管理** | 是 | 2 | 0.37% / 0.71% | **弱** | 覆盖率极低。共同指纹可能源于对相同硬件寄存器（如MMC）的操作或标准的内存分配模式，不代表核心内存管理算法（如页表、分配器）同源。 |
| **中断/异常处理** | 是 | 1 | 1.92% / 3.85% | **弱** | 双方都需要处理硬件中断，但目标项目覆盖LoongArch64和RISC-V多架构，参考项目架构不同。共同指纹源于对相似硬件机制（如PLIC）的驱动编写范式，而非核心Trap处理流程。 |
| **系统调用** | 是 | 0 | 0.0% / 0.0% | **未形成** | **零共同指纹**。尽管双方都提供Linux ABI，但系统调用分发、参数处理、用户态内存访问的底层实现路径完全不同。目标项目使用`UserPtrRaw`/`UserSliceRaw`，参考项目使用`UserReadPtr`/`UserWritePtr`。 |
| **文件系统** | 是 | 19 | 5.28% / 4.95% | **中** | 覆盖率相对较高，但证据多为`find`、`lookup`、`create`等通用VFS操作名。目标项目实现了FAT32和Ext4，参考项目实现了NFAT32和MemFS，具体文件系统实现差异巨大。 |
| **设备驱动** | 是 | 31 | 16.15% / 28.7% | **中** | 覆盖率最高，但证据高度集中在UART、MMC等**硬件标准协议驱动**上。对特定硬件寄存器的操作序列（如`reg.byte_add(0 << self.reg_shift).write_volatile`）由硬件手册规定，不同驱动实现必然趋同。 |
| **网络协议栈** | 仅目标 | 0 | 0.0% / 0.0% | **未形成** | 目标项目拥有完整的TCP/UDP协议栈，参考项目缺失此模块。这是目标项目独立扩展的显著证据。 |
| **第三方库/公共依赖** | 仅目标 | 0 | 0.0% / 0.0% | **未形成** | 目标项目包含Ext4等第三方文件系统实现，参考项目缺失。这进一步证明双方在功能范围和代码来源上的独立性。 |

### 3. 底层 AST 关键证据指纹表
| 所属板块 | 目标项目路径 | 参考项目路径 | 证据类型 | 技术说明 |
| :--- | :--- | :--- | :--- | :--- |
| 设备驱动 | `os/src/drivers/serial/uart.rs#init_u32` | `src/drivers/serial/uart8250.rs#init_u32` | 硬件标准范式 | 对UART 8250兼容芯片的初始化序列，如设置FCR、IER寄存器，由芯片手册规定，不同驱动的代码流必然高度相似。 |
| 设备驱动 | `os/src/drivers/block/mmc/mod.rs#card_init` | `src/drivers/blk/dw_mshc/mmc.rs#card_init` | 硬件标准范式 | MMC/SD卡初始化命令序列（CMD0, CMD2, CMD3等）是行业标准，调用图谱重合是遵循协议的结果。 |
| 进程管理 | `os/src/executor/mod.rs#new` | `src/executor/task_queue.rs#new` | 通用数据结构初始化 | `SpinNoIrqLock::new, VecDeque::new` 是Rust内核中初始化任务队列的标准组合，属于语言级通用模式。 |
| 进程管理 | `os/src/task/task.rs#parent` | `src/process/lproc.rs#parent` | 通用访问器模式 | `self.parent.lock().clone()` 是对`Mutex<Weak<...>>`类型父进程指针的标准访问模式，无独创性。 |
| 文件系统 | `os/src/fs/fat32/inode.rs#lookup` | `src/fs/new_vfs/path_cache.rs#lookup` | 通用VFS操作 | `lookup`是VFS的标准操作，双方均需实现，但底层文件系统（FAT32 vs NFAT32）和缓存机制完全不同。 |

### 4. 核心代码调用流对比剖析
* **【进程管理/系统调用路径】**
* **底层技术析理**: 以`sys_execve`为例，目标项目的调用流围绕`FileReader::new`、`AtFlags`等自研抽象展开，用于从文件系统加载可执行文件。参考项目则使用`UserReadPtr::<u8>::from`、`Path::from_string`等不同的用户态指针和路径处理抽象。两者从用户态获取参数、解析路径、加载程序的全过程在AST层面无重合，证明其系统调用实现框架是独立构建的。
* **代码溯源链接**:
  - 目标项目: `os/src/syscall/process.rs#sys_execve`
  - 参考项目: `src/syscall/process.rs#sys_execve`

* **【任务调度/异步运行时】**
* **底层技术析理**: 这是差异最大的核心板块。目标项目的`run_until_idle`调用流包含`current_processor`、`need_migrate_check`、`enqueue_on_processor`等复杂的多核负载均衡与任务迁移逻辑。参考项目的`run_until_idle`仅包含`TASK_QUEUE.fetch`和`task.run`，是典型的单核轮询模型。两者在调度算法、并发模型、数据结构上完全不同，**共同AST指纹为0**，是独立开发的决定性证据。
* **代码溯源链接**:
  - 目标项目: `os/src/executor/mod.rs#run_until_idle`
  - 参考项目: `src/executor/mod.rs#run_until_idle`

* **【内存管理/用户态访问】**
* **底层技术析理**: 在`sys_process_vm_readv`系统调用中，目标项目使用`UserSliceRaw::new(...).ensure_read(...)`模式进行跨进程内存拷贝，其核心是`UserSliceRaw`和`ensure_read`的组合。参考项目未提供此系统调用的证据，但其通用的用户态内存访问模式为`UserReadPtr::from(ptr).read_cstr()`。两者在用户态内存的抽象、安全检查和访问方法上采用了完全不同的设计哲学和实现路径。
* **代码溯源链接**:
  - 目标项目: `os/src/syscall/mm.rs#sys_process_vm_readv`
  - 参考项目: `src/syscall/process.rs#sys_execve` (作为其通用用户态访问模式的代表)

### 5. 单方模块与降权项
- **单方模块**:
  - **网络协议栈**: 仅存在于目标项目 (`os/src/net/`)，包含完整的TCP/UDP socket实现。这是目标项目独立开发的重大功能模块。
  - **文件系统/第三方库**: 目标项目包含独立的Ext4文件系统实现 (`os/src/fs/ext4/`)，参考项目未包含。
- **降权项说明**:
  - **Linux标准系统调用名 (`sys_*`)**: 如`sys_clone`, `sys_getpid`等，函数名由POSIX/Linux标准规定，任何兼容该ABI的操作系统都必须使用，不能作为同源证据。
  - **通用领域术语**: 如`do_exit`, `add_child`, `parent`等，是描述进程管理操作的通用词汇，命名空间高度收敛。
  - **标准库/第三方Crate API**: 如`VecDeque::new`, `SpinNoIrqLock::new`, `async_task::spawn`, `AtomicUsize::load/store`等，是Rust生态的公共基础设施，其调用图谱在所有项目中一致。
  - **跨板块同名**: 控方将目标项目`fs/procfs`中的`set_pid_max`与参考项目`process/lproc`中的`set_dents_progress`进行对比，属于跨模块硬凑，已降权为弱信号。

### 6. 保守结论
综合客观AST覆盖率（2.9%）、全模块矩阵分析及核心调用流对比，目标项目与参考项目在底层架构上存在显著差异。双方在任务调度、内存管理、系统调用等核心板块的共同指纹为零或极低，其重合点主要源于对标准硬件协议（UART、MMC）的驱动实现、Linux标准系统调用命名以及Rust语言标准库的通用范式。目标项目独立实现了SMP多核调度、网络协议栈、Ext4文件系统等参考项目不具备的复杂模块，进一步佐证了其独立开发的本质。因此，本次审计判定，无证据表明目标项目对参考项目构成代码抄袭或同源性复用，整体风险极低。

------------------------------

