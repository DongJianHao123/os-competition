# 🛡️ OS 内核赛自动化代码级架构审计报告

**审计时间**: `2026-07-02 04:21:13`
**参赛作品名称**: `2026-T202610336999947-XX`
**年份**: `2026`
**学校**: `XX`
**队伍名称**: `T202610336999947`
**比赛名字**: `OS-Competition`
**比赛子赛道**: `Kernel-Implementation`
**仓库地址**: `https://gitlab.eduxiji.net/educg-group-43501-3132633/T202610336999947-671`
**推测开发语言**: `Rust / C (由AST探针自动识别)`
**特征函数量**: `1489`

## ⚔️ 底层代码同源性对比 (AST Homology)
> 本部分由专门的代码审查智能体，对该项目与符合年份条件的历届最高相似度项目进行控制流与底层调用对比。新版报告会先给出确定性的全模块覆盖矩阵，再给出 Agent 的分模块解释，避免只围绕少量抽样函数展开。

### 🏆 Top 5 底层对标: vs `2025-undefined-清华大学`（清华大学 / undefined）

#### 对方作品信息

**对方年份**: `2025`
**对方学校**: `清华大学`
**对方队伍名称**: `undefined`
**对方比赛名字**: `OS-Competition`
**对方比赛子赛道**: `Kernel-Implementation`
**对方仓库地址**: `https://gitlab.eduxiji.net/educg-group-36002-2710490/T202510003995291-2331`

> 双向覆盖率：共同 AST 指纹: 816；唯一 AST 指纹: 目标项目 1187，参考项目 2820；覆盖率: 目标项目 68.74%，参考项目 28.94%

## 全模块对比总览（确定性证据）

> 总体覆盖率：共同 AST 指纹: 816；唯一 AST 指纹: 目标项目 1187，参考项目 2820；覆盖率: 目标项目 68.74%，参考项目 28.94%

### 1. 模块覆盖矩阵
| 模块 | 目标函数数 | 参考函数数 | 共同唯一 AST 指纹 | Target 覆盖 | Base 覆盖 | 状态 |
| :--- | ---: | ---: | ---: | ---: | ---: | :--- |
| 进程管理 | 35 | 135 | 24 | 77.42% | 19.83% | 双方存在且有结构重合 |
| 任务调度/异步运行时 | 171 | 415 | 114 | 74.03% | 38.0% | 双方存在且有结构重合 |
| 内存管理 | 181 | 450 | 93 | 59.24% | 24.35% | 双方存在且有结构重合 |
| 中断/异常处理 | 3 | 53 | 2 | 66.67% | 4.26% | 双方存在且有结构重合 |
| 系统调用 | 66 | 109 | 31 | 54.39% | 36.05% | 双方存在且有结构重合 |
| 文件系统 | 357 | 999 | 121 | 46.36% | 18.11% | 双方存在且有结构重合 |
| 设备驱动 | 102 | 133 | 80 | 85.11% | 65.57% | 双方存在且有结构重合 |
| 网络协议栈 | 61 | 65 | 50 | 98.04% | 90.91% | 双方存在且有结构重合 |
| 文件系统/第三方库 | 6 | 643 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |
| 网络协议栈/第三方库 | 98 | 130 | 62 | 69.66% | 53.45% | 双方存在且有结构重合 |
| 通用模块/其他 | 409 | 555 | 251 | 80.97% | 57.44% | 双方存在且有结构重合 |

### 2. 分模块证据清单

#### 进程管理
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `arceos/modules/axtask/src/run_queue.rs#exit_current` | `arceos/modules/axtask/src/run_queue.rs#exit_current` | 同板块同名同结构 | EXITED_TASKS.current_ref_mut_raw, EXITED_TASKS.current_ref_mut_raw().clear, EXITED_TASKS.current_ref_mut_raw().push_back, WAIT_FOR_EXIT.current_ref_mut_raw, WAIT_FOR_EXIT.current_ref_mut_raw().notify_one, assert! ...(+8) | EXITED_TASKS.current_ref_mut_raw, EXITED_TASKS.current_ref_mut_raw().clear, EXITED_TASKS.current_ref_mut_raw().push_back, WAIT_FOR_EXIT.current_ref_mut_raw, WAIT_FOR_EXIT.current_ref_mut_raw().notify_one, assert! ...(+8) |
| `arceos/api/arceos_posix_api/src/imp/io_mpx/epoll.rs#sys_epoll_wait` | `api/src/interface/fs/epoll.rs#sys_epoll_wait` | 同板块同名 | debug!, syscall_body! | (!timeout.is_negative()).then, Duration::from_millis, EpollInstance::from_fd, Err, Ok, axnet::poll_interfaces ...(+7) |
| `arceos/modules/axtask/src/wait_queue.rs#cancel_events` | `arceos/modules/axtask/src/wait_queue.rs#cancel_events` | 同板块同名同结构 | curr.in_wait_queue, curr.ptr_eq, curr.set_in_wait_queue, curr.timer_ticket_expired, self.queue.lock, self.queue.lock().retain | curr.in_wait_queue, curr.ptr_eq, curr.set_in_wait_queue, curr.timer_ticket_expired, self.queue.lock, self.queue.lock().retain |
| `arceos/modules/axtask/src/wait_queue.rs#wait_timeout_until` | `arceos/modules/axtask/src/wait_queue.rs#wait_timeout_until` | 同板块同名同结构 | axhal::time::wall_time, condition, crate::current, crate::timers::set_alarm_wakeup, curr.clone, current_run_queue::<NoPreemptIrqSave> ...(+4) | axhal::time::wall_time, condition, crate::current, crate::timers::set_alarm_wakeup, curr.clone, current_run_queue::<NoPreemptIrqSave> ...(+4) |

#### 任务调度/异步运行时
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `arceos/ulib/axlibc/c/printf.c#format_string_loop` | `arceos/ulib/axlibc/c/printf.c#format_string_loop` | 同板块同名同结构 | ABS_FOR_PRINTING, ADVANCE_IN_FORMAT_STRING, atou_, is_digit_, out_rev_, parse_flags ...(+5) | ABS_FOR_PRINTING, ADVANCE_IN_FORMAT_STRING, atou_, is_digit_, out_rev_, parse_flags ...(+5) |
| `arceos/modules/axlog/src/lib.rs#log` | `arceos/modules/axlog/src/lib.rs#log` | 同板块同名同结构 | cfg_if::cfg_if!, record.level, record.line, record.line().unwrap_or, record.metadata, record.target ...(+1) | cfg_if::cfg_if!, record.level, record.line, record.line().unwrap_or, record.metadata, record.target ...(+1) |
| `arceos/modules/axtask/src/run_queue.rs#switch_to` | `arceos/modules/axtask/src/run_queue.rs#switch_to` | 同板块同名同结构 | (*prev_ctx_ptr).switch_to, Arc::downgrade, CurrentTask::set_current, PREV_TASK.current_ref_mut_raw, assert!, clear_prev_task_on_cpu ...(+8) | (*prev_ctx_ptr).switch_to, Arc::downgrade, CurrentTask::set_current, PREV_TASK.current_ref_mut_raw, assert!, clear_prev_task_on_cpu ...(+8) |
| `arceos/modules/axtask/src/run_queue.rs#put_task_with_state` | `arceos/modules/axtask/src/run_queue.rs#put_task_with_state` | 同板块同名 | core::hint::spin_loop, self.scheduler.lock, self.scheduler.lock().put_prev_task, task.is_idle, task.on_cpu, task.set_cpu_id ...(+1) | core::hint::spin_loop, self.scheduler.lock, self.scheduler.lock().put_prev_task, task.is_idle, task.on_cpu, task.transition_state |

#### 内存管理
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `arceos/ulib/axlibc/src/strftime.rs#strftime_inner` | `arceos/ulib/axlibc/src/strftime.rs#strftime_inner` | 同板块同名同结构 | CountingWriter::new, format.offset, inner_strftime, w! | CountingWriter::new, format.offset, inner_strftime, w! |
| `arceos/ulib/axlibc/src/strftime.rs#inner_strftime` | `arceos/ulib/axlibc/src/strftime.rs#inner_strftime` | 同板块同名同结构 | format.offset, w! | format.offset, w! |
| `arceos/modules/axruntime/src/lib.rs#rust_main` | `arceos/modules/axruntime/src/lib.rs#rust_main` | 同板块同名 | INITED_CPUS.fetch_add, ax_println!, axdisplay::init_display, axdriver::init_drivers, axfs::init_filesystems, axhal::init_early ...(+24) | INITED_CPUS.fetch_add, Mountpoint::new_root, all_devices
                .block
                .take_one, all_devices
                .block
                .take_one()
                .expect, ax_println!, axdisplay::init_display ...(+28) |
| `arceos/modules/axdriver/src/bus/pci.rs#config_pci_device` | `arceos/modules/axdriver/src/bus/pci.rs#config_pci_device` | 同板块同名 | Ok, allocator
                    .as_mut, allocator
                    .as_mut()
                    .expect, allocator
                    .as_mut()
                    .expect("No memory ranges available for PCI BARs!")
                    .alloc, allocator
                    .as_mut()
                    .expect("No memory ranges available for PCI BARs!")
                    .alloc(size as _)
                    .ok_or, debug! ...(+8) | Ok, allocator
                    .as_mut, allocator
                    .as_mut()
                    .expect, allocator
                    .as_mut()
                    .expect("No memory ranges available for PCI BARs!")
                    .alloc, allocator
                    .as_mut()
                    .expect("No memory ranges available for PCI BARs!")
                    .alloc(size as _)
                    .ok_or, debug! ...(+7) |

#### 中断/异常处理
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `arceos/ulib/axlibc/c/pow.c#exp_inline` | `arceos/ulib/axlibc/c/pow.c#exp_inline` | 同板块同名同结构 | __math_oflow, __math_uflow, asdouble, asuint64, converttoint, eval_as_double ...(+4) | __math_oflow, __math_uflow, asdouble, asuint64, converttoint, eval_as_double ...(+4) |
| `arceos/modules/axsync/src/mutex.rs#may_interrupt` | `arceos/modules/axsync/src/mutex.rs#may_interrupt` | 同板块同名同结构 | rand::random::<u32>, thread::yield_now | rand::random::<u32>, thread::yield_now |
| `compat/oscomp-runner/src/main.rs#main` | `arceos/modules/axhal/src/arch/aarch64/trap.rs#handle_sync_exception` | 同模块上下文对齐/差异观察 | bootstrap_ext4, print_root_entries, println! | ESR_EL1.extract, crate::trap::handle_syscall, crate::trap::post_trap_callback, debug!, esr.read, esr.read_as_enum ...(+6) |

#### 系统调用
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `arceos/api/arceos_posix_api/src/imp/net.rs#sys_getaddrinfo` | `api/src/imp/net/socket.rs#sys_getaddrinfo` | 同板块同名 | char_ptr_to_str, debug!, syscall_body! | Err, Ok, SocketAddrV4::new, SocketAddrV4::new(ip, port).into, Vec::with_capacity, axnet::dns_query ...(+21) |
| `arceos/api/arceos_posix_api/src/imp/net.rs#sys_recvfrom` | `api/src/imp/net/socket.rs#sys_recvfrom` | 同板块同名 | debug!, syscall_body! | Err, Ok, Socket::from_fd, addrlen.is_null, buf_ptr.is_null, core::slice::from_raw_parts_mut ...(+4) |
| `arceos/api/arceos_posix_api/src/imp/net.rs#sys_accept` | `api/src/imp/net/socket.rs#sys_accept` | 同板块同名 | debug!, syscall_body! | Err, Mutex::new, Ok, Socket::Tcp, Socket::add_to_fd_table, Socket::from_fd ...(+6) |
| `arceos/api/arceos_posix_api/src/imp/net.rs#sys_sendto` | `api/src/imp/net/socket.rs#sys_sendto` | 同板块同名 | debug!, syscall_body! | Err, Socket::from_fd, Socket::from_fd(socket_fd)?
        .sendto, Socket::from_fd(socket_fd)?
        .sendto(buf, addr)
        .map, Socket::from_fd(socket_fd)?
        .sendto(buf, addr)
        .map(|res| res as isize)
        .map_err, buf_ptr.is_null ...(+3) |

#### 文件系统
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `arceos/ulib/axlibc/c/glob.c#do_glob` | `arceos/ulib/axlibc/c/glob.c#do_glob` | 同板块同名同结构 | S_ISDIR, append, closedir, do_glob, errfunc, fnmatch ...(+7) | S_ISDIR, append, closedir, do_glob, errfunc, fnmatch ...(+7) |
| `arceos/ulib/axlibc/src/setjmp.rs#longjmp` | `arceos/ulib/axlibc/src/setjmp.rs#longjmp` | 同板块同名 | core::arch::naked_asm! | core::arch::naked_asm! |
| `arceos/ulib/axlibc/src/setjmp.rs#setjmp` | `arceos/ulib/axlibc/src/setjmp.rs#setjmp` | 同板块同名 | core::arch::naked_asm! | core::arch::naked_asm! |
| `arceos/api/arceos_posix_api/src/imp/io_mpx/select.rs#poll_all` | `api/src/core/file/epoll.rs#poll_all` | 同板块同名 | Ok, debug!, execpt_bits_ptr.add, get_file_like, get_file_like(fd as _)?.poll, read_bits_ptr.add ...(+3) | Ok, fd_lookup, fd_lookup(*infd as c_int)?.poll, ready_list.iter, self.events.lock |

#### 设备驱动
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `arceos/tools/bwbench_client/src/main.rs#transmit_benchmark` | `arceos/tools/bwbench_client/src/main.rs#transmit_benchmark` | 同板块同名同结构 | Local::now, NetDevice::new, NetDevice::new(interface).unwrap, current_time.signed_duration_since, current_time.signed_duration_since(past_time).num_seconds, dev.send ...(+2) | Local::now, NetDevice::new, NetDevice::new(interface).unwrap, current_time.signed_duration_since, current_time.signed_duration_since(past_time).num_seconds, dev.send ...(+2) |
| `arceos/tools/bwbench_client/src/device.rs#bind_interface` | `arceos/tools/bwbench_client/src/device.rs#bind_interface` | 同板块同名同结构 | ETH_P_ALL.to_be, Err, Ok, libc::bind, self.ifreq.ioctl, std::io::Error::last_os_error ...(+2) | ETH_P_ALL.to_be, Err, Ok, libc::bind, self.ifreq.ioctl, std::io::Error::last_os_error ...(+2) |
| `arceos/modules/axdriver/src/lib.rs#init_drivers` | `arceos/modules/axdriver/src/lib.rs#init_drivers` | 同板块同名同结构 | AllDevices::default, all_devs.block.iter, all_devs.block.iter().enumerate, all_devs.display.iter, all_devs.display.iter().enumerate, all_devs.net.iter ...(+5) | AllDevices::default, all_devs.block.iter, all_devs.block.iter().enumerate, all_devs.display.iter, all_devs.display.iter().enumerate, all_devs.net.iter ...(+5) |
| `arceos/tools/bwbench_client/src/main.rs#receive_benchmark` | `arceos/tools/bwbench_client/src/main.rs#receive_benchmark` | 同板块同名同结构 | Local::now, NetDevice::new, NetDevice::new(interface).unwrap, current_time.signed_duration_since, current_time.signed_duration_since(past_time).num_seconds, dev.recv ...(+1) | Local::now, NetDevice::new, NetDevice::new(interface).unwrap, current_time.signed_duration_since, current_time.signed_duration_since(past_time).num_seconds, dev.recv ...(+1) |

#### 网络协议栈
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `arceos/ulib/axlibc/c/network.c#inet_pton` | `arceos/ulib/axlibc/c/network.c#inet_pton` | 同板块同名同结构 | hexval, inet_pton, isdigit, memmove | hexval, inet_pton, isdigit, memmove |
| `arceos/api/arceos_posix_api/src/imp/net.rs#shutdown` | `api/src/imp/net/socket.rs#shutdown` | 同板块同名同结构 | Ok, tcpsocket.lock, tcpsocket.peer_addr, tcpsocket.shutdown, udpsocket.lock, udpsocket.peer_addr ...(+1) | Ok, tcpsocket.lock, tcpsocket.peer_addr, tcpsocket.shutdown, udpsocket.lock, udpsocket.peer_addr ...(+1) |
| `arceos/ulib/axlibc/c/socket.c#accept4` | `arceos/ulib/axlibc/c/socket.c#accept4` | 同板块同名同结构 | accept, fcntl | accept, fcntl |
| `arceos/api/arceos_posix_api/src/imp/net.rs#recvfrom` | `api/src/imp/net/socket.rs#recvfrom` | 同板块同名同结构 | Ok, Some, tcpsocket.lock, tcpsocket.lock().recv, tcpsocket.lock().recv(buf).map, udpsocket
                .lock ...(+2) | Ok, Some, tcpsocket.lock, tcpsocket.lock().recv, tcpsocket.lock().recv(buf).map, udpsocket
                .lock ...(+2) |

#### 文件系统/第三方库
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `compat/oscomp-runner/src/ext4_import.rs#import_dir` | `modules/lwext4_rust/c/lwext4/src/ext4_balloc.c#ext4_balloc_alloc_block` | 同模块上下文对齐/差异观察 | Ok, entry
            .metadata, entry
            .metadata()
            .map_err, entry.map_err, ext4
                .read, ext4
                .read(path.as_str())
                .map_err ...(+15) | ext4_balloc_get_bgid_of_block, ext4_balloc_get_block_of_bgid, ext4_balloc_set_bitmap_csum, ext4_balloc_verify_bitmap_csum, ext4_bg_get_block_bitmap, ext4_bg_get_free_blocks_count ...(+19) |
| `compat/oscomp-runner/src/ext4_import.rs#read` | `modules/lwext4_rust/c/lwext4/src/ext4_journal.c#jbd_journal_prepare` | 同模块上下文对齐/差异观察 | Box::new, DiskReadError, Err, Ok, disk.read_one, disk.read_one(&mut dst[filled..]).map_err ...(+5) | EXT4_BLOCK_ZERO, RB_FIND, TAILQ_FOREACH_REVERSE_SAFE, TAILQ_FOREACH_SAFE, TAILQ_REMOVE, ext4_bcache_clear_dirty ...(+17) |
| `compat/oscomp-runner/src/ext4_import.rs#import_boot_disk` | `modules/lwext4_rust/c/lwext4/src/ext4.c#ext4_dir_rm` | 同模块上下文对齐/差异观察 | Box::new, Ext4::load, Ext4::load(Box::new(reader)).map_err, ImportStats::default, Ok, format! ...(+3) | EXT4_MP_LOCK, EXT4_MP_UNLOCK, ext4_block_cache_write_back, ext4_dir_en_get_inode, ext4_dir_iterator_fini, ext4_dir_iterator_init ...(+19) |
| `compat/oscomp-runner/src/ext4_import.rs#join_path` | `modules/lwext4_rust/c/lwext4/src/ext4_dir_idx.c#ext4_dir_dx_split_index` | 同模块上下文对齐/差异观察 | format! | ext4_block_set, ext4_dir_dx_climit_get_count, ext4_dir_dx_climit_get_limit, ext4_dir_dx_climit_set_count, ext4_dir_dx_climit_set_limit, ext4_dir_dx_entry_get_hash ...(+10) |

#### 网络协议栈/第三方库
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `arceos/modules/axnet/src/smoltcp_impl/tcp.rs#connect` | `arceos/modules/axnet/src/smoltcp_impl/tcp.rs#connect` | 同板块同名 | Err, Ok, SOCKET_SET
                .with_socket_mut::<tcp::Socket, _, _>, SOCKET_SET.add, SocketSetWrapper::new_tcp_socket, Some ...(+24) | Err, Ok, Ok::<(IpEndpoint, IpEndpoint), AxError>, SOCKET_SET
                .with_socket_mut::<tcp::Socket, _, _>, SOCKET_SET.add, SocketSetWrapper::new_tcp_socket ...(+32) |
| `arceos/modules/axnet/src/smoltcp_impl/dns.rs#query` | `arceos/modules/axnet/src/smoltcp_impl/dns.rs#query` | 同板块同名 | Err, IpAddr::from, Ok, SOCKET_SET
            .with_socket_mut::<dns::Socket, _, _>, SOCKET_SET
            .with_socket_mut::<dns::Socket, _, _>(handle, |socket| {
                socket.start_query(iface.lock().context(), name, query_type)
            })
            .map_err, SOCKET_SET.poll_interfaces ...(+12) | Err, Ok, SOCKET_SET
            .with_socket_mut::<dns::Socket, _, _>, SOCKET_SET
            .with_socket_mut::<dns::Socket, _, _>(handle, |socket| {
                socket.start_query(iface.lock().context(), name, query_type)
            })
            .map_err, SOCKET_SET.poll_interfaces, SOCKET_SET.with_socket_mut::<dns::Socket, _, _> ...(+12) |
| `arceos/modules/axnet/src/smoltcp_impl/bench.rs#bench_transmit_bandwidth` | `arceos/modules/axnet/src/smoltcp_impl/bench.rs#bench_transmit_bandwidth` | 同板块同名同结构 | (current_time - past_time).secs, AxNetTxToken::consume, InterfaceWrapper::current_time, info!, self.transmit, tx_buf[0..12].fill ...(+2) | (current_time - past_time).secs, AxNetTxToken::consume, InterfaceWrapper::current_time, info!, self.transmit, tx_buf[0..12].fill ...(+2) |
| `arceos/modules/axnet/src/smoltcp_impl/tcp.rs#shutdown` | `arceos/modules/axnet/src/smoltcp_impl/tcp.rs#shutdown` | 同板块同名同结构 | LISTEN_TABLE.unlisten, Ok, SOCKET_SET.poll_interfaces, SOCKET_SET.with_socket_mut::<tcp::Socket, _, _>, debug!, self.handle.get ...(+9) | LISTEN_TABLE.unlisten, Ok, SOCKET_SET.poll_interfaces, SOCKET_SET.with_socket_mut::<tcp::Socket, _, _>, debug!, self.handle.get ...(+9) |

#### 通用模块/其他
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `arceos/ulib/axlibc/c/printf.c#print_exponential_number` | `arceos/ulib/axlibc/c/printf.c#print_exponential_number` | 同板块同名同结构 | ABS_FOR_PRINTING, PRINTF_ABS, bastardized_floor, get_components, get_normalized_components, get_sign_bit ...(+5) | ABS_FOR_PRINTING, PRINTF_ABS, bastardized_floor, get_components, get_normalized_components, get_sign_bit ...(+5) |
| `arceos/ulib/axlibc/c/pow.c#pow` | `arceos/ulib/axlibc/c/pow.c#pow` | 同板块同名同结构 | __builtin_fma, __math_invalid, __math_oflow, __math_uflow, asdouble, asuint64 ...(+8) | __builtin_fma, __math_invalid, __math_oflow, __math_uflow, asdouble, asuint64 ...(+8) |
| `arceos/ulib/axlibc/c/string.c#twoway_strstr` | `arceos/ulib/axlibc/c/string.c#twoway_strstr` | 同板块同名同结构 | BITOP, MAX, memchr, memcmp | BITOP, MAX, memchr, memcmp |
| `arceos/ulib/axlibc/c/printf.c#print_broken_up_decimal` | `arceos/ulib/axlibc/c/printf.c#print_broken_up_decimal` | 同板块同名同结构 | out_rev_ | out_rev_ |


#### 1. 宏观判定
- **定性结论**: 独立开发，存在共同上游依赖 (相似度: 25.57%，共同 AST 指纹: 816；唯一 AST 指纹: 目标项目 1187，参考项目 2820；覆盖率: 目标项目 68.74%，参考项目 28.94%)

#### 2. 全模块覆盖矩阵解读
- **进程管理**: 双方均存在该模块。存在共同 AST 指纹。目标项目覆盖率 77.42%，参考项目覆盖率 19.83%。高目标覆盖率源于双方共同继承了 ArceOS 的 `axtask` 核心调度与等待队列组件，而参考项目在此之上构建了大量独立的高级进程管理逻辑，导致其覆盖率极低。该模块证据强度为**弱**，共同指纹均为上游公共代码。
- **任务调度/异步运行时**: 双方均存在该模块。存在共同 AST 指纹。目标项目覆盖率 74.03%，参考项目覆盖率 38.0%。共同指纹集中于 `axlibc` 的 C 库实现（如 `printf`）和 `axtask` 调度原语。该模块证据强度为**弱**，共同指纹可归因于公共 C 库和调度框架。
- **内存管理**: 双方均存在该模块。存在共同 AST 指纹。目标项目覆盖率 59.24%，参考项目覆盖率 24.35%。覆盖率差异显著，表明参考项目有大量独立的内存管理实现。共同指纹主要来自 `axruntime`、`axdriver` 等框架初始化代码。该模块证据强度为**弱**。
- **中断/异常处理**: 双方均存在该模块。存在共同 AST 指纹。目标项目覆盖率 66.67%，参考项目覆盖率 4.26%。目标项目仅含 3 个函数，覆盖率数值波动大，无统计意义。共同指纹为 `pow.c` 等数学库函数，与中断处理无关，属于跨板块误分类。该模块证据强度为**未形成**。
- **系统调用**: 双方均存在该模块。存在共同 AST 指纹。目标项目覆盖率 54.39%，参考项目覆盖率 36.05%。共同指纹多位于 POSIX 接口层（如 `sys_accept`、`sys_recvfrom`），但底层实现完全不同。目标项目多用 `syscall_body!` 宏包装，参考项目有具体实现。该模块证据强度为**弱**，同名由 POSIX 规范导致。
- **文件系统**: 双方均存在该模块。存在共同 AST 指纹。目标项目覆盖率 46.36%，参考项目覆盖率 18.11%。共同指纹集中在 `axlibc` 的 C 库函数（如 `glob`、`setjmp`）。参考项目有庞大的独立文件系统实现。该模块证据强度为**弱**。
- **设备驱动**: 双方均存在该模块。存在共同 AST 指纹。目标项目覆盖率 85.11%，参考项目覆盖率 65.57%。高覆盖率源于双方均包含 ArceOS 的设备驱动框架（`axdriver`）和相同的基准测试工具（`bwbench_client`）。该模块证据强度为**弱**，均为上游框架与工具代码。
- **网络协议栈**: 双方均存在该模块。存在共同 AST 指纹。目标项目覆盖率 98.04%，参考项目覆盖率 90.91%。覆盖率极高，因为双方均完整集成了 `smoltcp` 协议栈和 `axlibc` 的 socket 兼容层。该模块证据强度为**弱**，共同指纹均为第三方库和其 Rust 封装。
- **第三方库/公共依赖**: 双方均存在该模块。文件系统/第三方库部分未观察到共同指纹，网络协议栈/第三方库部分覆盖率较高。这进一步证实共同指纹源自双方共同依赖的第三方开源库（如 lwext4、smoltcp、musl 等）。该模块证据强度为**未形成**（针对抄袭指控）。

#### 3. 底层 AST 关键证据指纹表
| 所属板块 | 目标项目路径 | 参考项目路径 | 证据类型 | 技术说明 |
| :--- | :--- | :--- | :--- | :--- |
| 第三方库/公共依赖 | `arceos/ulib/axlibc/c/printf.c#format_string_loop` | `arceos/ulib/axlibc/c/printf.c#format_string_loop` | 公共上游库 | 双方均使用相同的 musl 或类似开源 C 库的 `printf` 内核实现，调用链一致是继承行为。 |
| 第三方库/公共依赖 | `arceos/modules/axnet/src/smoltcp_impl/tcp.rs#connect` | `arceos/modules/axnet/src/smoltcp_impl/tcp.rs#connect` | 公共上游库 | 双方均封装 `smoltcp` 协议栈，其复杂的 socket 操作调用链由 `smoltcp` API 决定，必然一致。 |
| 第三方库/公共依赖 | `arceos/modules/axtask/src/wait_queue.rs#wait_timeout_until` | `arceos/modules/axtask/src/wait_queue.rs#wait_timeout_until` | 公共上游框架 | 双方均继承 ArceOS 的 `axtask` 调度框架，`WaitQueue` 的阻塞/唤醒机制是框架核心逻辑，非自行开发。 |
| 进程管理 | `arceos/modules/axtask/src/run_queue.rs#exit_current` | `arceos/modules/axtask/src/run_queue.rs#exit_current` | 公共上游框架 | 同为 ArceOS 任务调度器核心函数。目标项目调用 `axhal::power::system_off`，参考项目调用 `axhal::misc::terminate`，差异源于 HAL 版本/配置不同。 |
| 设备驱动 | `arceos/tools/bwbench_client/src/main.rs#transmit_benchmark` | `arceos/tools/bwbench_client/src/main.rs#transmit_benchmark` | 公共上游工具 | 双方均包含 ArceOS 自带的网络带宽基准测试工具，代码完全一致是工具复用的结果。 |
| 系统调用 | `arceos/api/arceos_posix_api/src/imp/io_mpx/epoll.rs#sys_epoll_wait` | `api/src/interface/fs/epoll.rs#sys_epoll_wait` | 接口同名，实现异构 | 函数名由 POSIX 规范决定。目标项目为 `syscall_body!` 宏包装的空壳，参考项目有完整的 `EpollInstance` 管理逻辑，底层 AST 完全不同。 |
| 进程管理 | `arceos/api/arceos_posix_api/src/imp/task.rs#sys_exit` | `api/src/interface/task/exit.rs#sys_exit` | 接口同名，实现异构 | 目标项目直接调用 `axtask::exit` 和 `system_off`，参考项目则调用 `sys_exit_impl` 进行复杂的信号处理和内存清理，调用链深度和逻辑完全不同。 |

#### 4. 核心代码调用流对比剖析
* **【进程管理/任务退出路径】**
* **底层技术析理**: 目标项目的 `sys_exit` 实现极其简单，调用流为 `sys_exit` -> `axtask::exit` -> `axhal::power::system_off`，直接终止硬件。参考项目的 `sys_exit` 则是一个复杂流程的入口，调用流为 `sys_exit` -> `sys_exit_impl` -> `close_all_file_like` / `SignalInfo::new` / `addr_clear_child_tid` 等，涉及文件描述符清理、信号发送、子线程 tid 清除等完整的 POSIX 进程退出语义。两者在抽象层次上完全不同：一个是硬件级终止，一个是软件级进程资源回收。
* **代码溯源链接**:
  - 目标项目: `arceos/api/arceos_posix_api/src/imp/task.rs#sys_exit`
  - 参考项目: `api/src/interface/task/exit.rs#sys_exit`

* **【系统调用/epoll 等待】**
* **底层技术析理**: 目标项目的 `sys_epoll_wait` 仅包含 `debug!` 和 `syscall_body!` 宏，表明其可能是一个未完成的桩实现或简单的转发。参考项目的 `sys_epoll_wait` 则包含完整的超时管理 (`Duration::from_millis`)、事件循环 (`axnet::poll_interfaces`) 和就绪列表处理逻辑。两者的对象管理模型完全不同：目标项目无实质逻辑，参考项目管理了 `EpollInstance` 的生命周期和网络轮询。
* **代码溯源链接**:
  - 目标项目: `arceos/api/arceos_posix_api/src/imp/io_mpx/epoll.rs#sys_epoll_wait`
  - 参考项目: `api/src/interface/fs/epoll.rs#sys_epoll_wait`

* **【内存管理/内核初始化】**
* **底层技术析理**: 在 `rust_main` 初始化流程中，双方都调用了 `axhal::init_early`、`axdriver::init_drivers`、`axfs::init_filesystems` 等 ArceOS 标准初始化序列。然而，参考项目在此序列中插入了 `Mountpoint::new_root` 和 `all_devices.block.take_one` 等独立的存储设备挂载逻辑，显示出其在初始化阶段就构建了更复杂的 VFS 抽象。目标项目则遵循了更标准的 ArceOS 初始化流程。
* **代码溯源链接**:
  - 目标项目: `arceos/modules/axruntime/src/lib.rs#rust_main`
  - 参考项目: `arceos/modules/axruntime/src/lib.rs#rust_main`

#### 5. 单方模块与降权项
- **单方模块**: 参考项目在 `api/src/core/fs/imp/proc.rs` 中实现的 `builder` 函数，用于构建 procfs 的动态目录结构，在目标项目中无对应实现。目标项目的 `procfs` 实现位于 `arceos/modules/axfs/src/mounts.rs`，采用基于 ramfs 的静态文件写入方式。两者实现路径完全不同，是架构差异的体现。
- **降权项**: 大量标记为 `[🚨同板块底层逻辑一致]` 的证据，如 `wait_queue.rs` 中的 `new`、`len`、`is_empty` 等函数，以及 `unimplemented` 桩函数。这些函数或为 Rust 标准库惯用法（`VecDeque::new`），或为表达“功能未实现”的通用占位符，不具备独创性，不能作为抄袭证据。跨板块同名函数（如将 `printf.c` 中的函数归入“任务调度”）亦因分类错误而降权。

#### 6. 保守结论
综合评估，目标项目与参考项目的 AST 相似度为 25.57%，处于较低水平。所有强相似信号均指向双方共同依赖的 ArceOS 内核框架、smoltcp 网络栈、axlibc C 库以及 POSIX 标准接口。在剔除这些公共上游代码后，双方在核心系统调用实现、进程管理模型、文件系统架构上展现出显著差异。目标项目倾向于简单的硬件抽象和桩实现，而参考项目则构建了更完整的 POSIX 兼容层。现有证据充分支持“基于共同开源底座的独立开发”这一结论，不存在针对参考项目的系统性代码抄袭或逻辑复用。

------------------------------

