# 🛡️ OS 内核赛自动化代码级架构审计报告

**审计时间**: `2026-07-02 04:21:13`
**参赛作品名称**: `2026-T202610336999947-XX`
**年份**: `2026`
**学校**: `XX`
**队伍名称**: `T202610336999947`
**比赛名字**: `OS-Competition`
**比赛子赛道**: `Kernel-Implementation`
**仓库地址**: `https://gitlab.eduxiji.net/educg-group-43501-3132633/T202610336999947-671`
**推测开发语言**: `Rust / C (由AST探针自动识别)`
**特征函数量**: `1489`

## ⚔️ 底层代码同源性对比 (AST Homology)
> 本部分由专门的代码审查智能体，对该项目与符合年份条件的历届最高相似度项目进行控制流与底层调用对比。新版报告会先给出确定性的全模块覆盖矩阵，再给出 Agent 的分模块解释，避免只围绕少量抽样函数展开。

### 🏆 Top 3 底层对标: vs `2025-狗剩-海南大学`（海南大学 / 狗剩）

#### 对方作品信息

**对方年份**: `2025`
**对方学校**: `海南大学`
**对方队伍名称**: `狗剩`
**对方比赛名字**: `OS-Competition`
**对方比赛子赛道**: `Kernel-Implementation`
**对方仓库地址**: `https://gitlab.eduxiji.net/educg-group-36002-2710490/T202510589995148-2209`

> 双向覆盖率：共同 AST 指纹: 995；唯一 AST 指纹: 目标项目 1187，参考项目 3020；覆盖率: 目标项目 83.82%，参考项目 32.95%

## 全模块对比总览（确定性证据）

> 总体覆盖率：共同 AST 指纹: 995；唯一 AST 指纹: 目标项目 1187，参考项目 3020；覆盖率: 目标项目 83.82%，参考项目 32.95%

### 1. 模块覆盖矩阵
| 模块 | 目标函数数 | 参考函数数 | 共同唯一 AST 指纹 | Target 覆盖 | Base 覆盖 | 状态 |
| :--- | ---: | ---: | ---: | ---: | ---: | :--- |
| 进程管理 | 35 | 93 | 28 | 90.32% | 33.33% | 双方存在且有结构重合 |
| 任务调度/异步运行时 | 171 | 399 | 127 | 82.47% | 44.1% | 双方存在且有结构重合 |
| 内存管理 | 181 | 326 | 100 | 63.69% | 34.36% | 双方存在且有结构重合 |
| 中断/异常处理 | 3 | 62 | 2 | 66.67% | 4.08% | 双方存在且有结构重合 |
| 系统调用 | 66 | 153 | 56 | 98.25% | 43.41% | 双方存在且有结构重合 |
| 文件系统 | 357 | 747 | 223 | 85.44% | 39.4% | 双方存在且有结构重合 |
| 设备驱动 | 102 | 142 | 84 | 89.36% | 68.29% | 双方存在且有结构重合 |
| 网络协议栈 | 61 | 77 | 51 | 100.0% | 96.23% | 双方存在且有结构重合 |
| 文件系统/第三方库 | 6 | 689 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |
| 网络协议栈/第三方库 | 98 | 107 | 71 | 79.78% | 71.0% | 双方存在且有结构重合 |
| 第三方库 | 0 | 386 | 0 | 0.0% | 0.0% | 仅参考项目体现 |
| 通用模块/其他 | 409 | 665 | 273 | 88.06% | 55.6% | 双方存在且有结构重合 |

### 2. 分模块证据清单

#### 进程管理
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `arceos/modules/axtask/src/run_queue.rs#exit_current` | `arceos/modules/axtask/src/run_queue.rs#exit_current` | 同板块同名同结构 | EXITED_TASKS.current_ref_mut_raw, EXITED_TASKS.current_ref_mut_raw().clear, EXITED_TASKS.current_ref_mut_raw().push_back, WAIT_FOR_EXIT.current_ref_mut_raw, WAIT_FOR_EXIT.current_ref_mut_raw().notify_one, assert! ...(+8) | EXITED_TASKS.current_ref_mut_raw, EXITED_TASKS.current_ref_mut_raw().clear, EXITED_TASKS.current_ref_mut_raw().push_back, WAIT_FOR_EXIT.current_ref_mut_raw, WAIT_FOR_EXIT.current_ref_mut_raw().notify_one, assert! ...(+8) |
| `arceos/api/arceos_posix_api/src/imp/io_mpx/epoll.rs#sys_epoll_wait` | `arceos/api/arceos_posix_api/src/imp/io_mpx/epoll.rs#sys_epoll_wait` | 同板块同名同结构 | debug!, syscall_body! | debug!, syscall_body! |
| `arceos/modules/axtask/src/wait_queue.rs#cancel_events` | `arceos/modules/axtask/src/wait_queue.rs#cancel_events` | 同板块同名同结构 | curr.in_wait_queue, curr.ptr_eq, curr.set_in_wait_queue, curr.timer_ticket_expired, self.queue.lock, self.queue.lock().retain | curr.in_wait_queue, curr.ptr_eq, curr.set_in_wait_queue, curr.timer_ticket_expired, self.queue.lock, self.queue.lock().retain |
| `arceos/modules/axfs/src/mounts.rs#procfs` | `arceos/modules/axfs/src/mounts.rs#procfs` | 同板块同名 | Arc::new, Ok, file_over.write_at, file_somaxconn.write_at, fs::ramfs::RamFileSystem::new, proc_root.clone ...(+3) | Arc::new, Ok, file_over.write_at, file_somaxconn.write_at, fs::ramfs::RamFileSystem::new, proc_root.clone ...(+3) |

#### 任务调度/异步运行时
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `arceos/ulib/axlibc/c/printf.c#format_string_loop` | `arceos/ulib/axlibc/c/printf.c#format_string_loop` | 同板块同名同结构 | ABS_FOR_PRINTING, ADVANCE_IN_FORMAT_STRING, atou_, is_digit_, out_rev_, parse_flags ...(+5) | ABS_FOR_PRINTING, ADVANCE_IN_FORMAT_STRING, atou_, is_digit_, out_rev_, parse_flags ...(+5) |
| `arceos/modules/axlog/src/lib.rs#log` | `arceos/modules/axlog/src/lib.rs#log` | 同板块同名同结构 | cfg_if::cfg_if!, record.level, record.line, record.line().unwrap_or, record.metadata, record.target ...(+1) | cfg_if::cfg_if!, record.level, record.line, record.line().unwrap_or, record.metadata, record.target ...(+1) |
| `arceos/modules/axtask/src/run_queue.rs#switch_to` | `arceos/modules/axtask/src/run_queue.rs#switch_to` | 同板块同名 | (*prev_ctx_ptr).switch_to, Arc::downgrade, CurrentTask::set_current, PREV_TASK.current_ref_mut_raw, assert!, clear_prev_task_on_cpu ...(+8) | (*prev_ctx_ptr).switch_to, Arc::downgrade, CurrentTask::set_current, PREV_TASK.current_ref_mut_raw, assert!, clear_prev_task_on_cpu ...(+11) |
| `arceos/modules/axtask/src/run_queue.rs#put_task_with_state` | `arceos/modules/axtask/src/run_queue.rs#put_task_with_state` | 同板块同名 | core::hint::spin_loop, self.scheduler.lock, self.scheduler.lock().put_prev_task, task.is_idle, task.on_cpu, task.set_cpu_id ...(+1) | core::hint::spin_loop, self.scheduler.lock, self.scheduler.lock().put_prev_task, task.is_idle, task.on_cpu, task.transition_state |

#### 内存管理
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `arceos/ulib/axlibc/src/strftime.rs#strftime_inner` | `arceos/ulib/axlibc/src/strftime.rs#strftime_inner` | 同板块同名同结构 | CountingWriter::new, format.offset, inner_strftime, w! | CountingWriter::new, format.offset, inner_strftime, w! |
| `arceos/ulib/axlibc/src/strftime.rs#inner_strftime` | `arceos/ulib/axlibc/src/strftime.rs#inner_strftime` | 同板块同名同结构 | format.offset, w! | format.offset, w! |
| `arceos/modules/axruntime/src/lib.rs#rust_main` | `arceos/modules/axruntime/src/lib.rs#rust_main` | 同板块同名 | INITED_CPUS.fetch_add, ax_println!, axdisplay::init_display, axdriver::init_drivers, axfs::init_filesystems, axhal::init_early ...(+24) | INITED_CPUS.fetch_add, all_devices
                .block
                .take_one, all_devices
                .block
                .take_one()
                .expect, ax_println!, axdisplay::init_display, axdriver::init_drivers ...(+28) |
| `arceos/modules/axdriver/src/bus/pci.rs#config_pci_device` | `arceos/modules/axdriver/src/bus/pci.rs#config_pci_device` | 同板块同名 | Ok, allocator
                    .as_mut, allocator
                    .as_mut()
                    .expect, allocator
                    .as_mut()
                    .expect("No memory ranges available for PCI BARs!")
                    .alloc, allocator
                    .as_mut()
                    .expect("No memory ranges available for PCI BARs!")
                    .alloc(size as _)
                    .ok_or, debug! ...(+8) | Ok, allocator
                    .as_mut, allocator
                    .as_mut()
                    .expect, allocator
                    .as_mut()
                    .expect("No memory ranges available for PCI BARs!")
                    .alloc, allocator
                    .as_mut()
                    .expect("No memory ranges available for PCI BARs!")
                    .alloc(size as _)
                    .ok_or, debug! ...(+7) |

#### 中断/异常处理
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `arceos/ulib/axlibc/c/pow.c#exp_inline` | `arceos/ulib/axlibc/c/pow.c#exp_inline` | 同板块同名同结构 | __math_oflow, __math_uflow, asdouble, asuint64, converttoint, eval_as_double ...(+4) | __math_oflow, __math_uflow, asdouble, asuint64, converttoint, eval_as_double ...(+4) |
| `arceos/modules/axsync/src/mutex.rs#may_interrupt` | `arceos/modules/axsync/src/mutex.rs#may_interrupt` | 同板块同名同结构 | rand::random::<u32>, thread::yield_now | rand::random::<u32>, thread::yield_now |
| `compat/oscomp-runner/src/main.rs#main` | `arceos/modules/axhal/src/arch/aarch64/trap.rs#handle_sync_exception` | 同模块上下文对齐/差异观察 | bootstrap_ext4, print_root_entries, println! | ESR_EL1.extract, crate::trap::handle_syscall, crate::trap::post_trap_callback, debug!, esr.read, esr.read_as_enum ...(+6) |

#### 系统调用
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `arceos/api/arceos_posix_api/src/imp/net.rs#sys_getaddrinfo` | `arceos/api/arceos_posix_api/src/imp/net.rs#sys_getaddrinfo` | 同板块同名同结构 | char_ptr_to_str, debug!, syscall_body! | char_ptr_to_str, debug!, syscall_body! |
| `arceos/api/arceos_posix_api/src/imp/net.rs#sys_recvfrom` | `arceos/api/arceos_posix_api/src/imp/net.rs#sys_recvfrom` | 同板块同名同结构 | debug!, syscall_body! | debug!, syscall_body! |
| `arceos/api/arceos_posix_api/src/imp/net.rs#sys_socket` | `arceos/api/arceos_posix_api/src/imp/net.rs#sys_socket` | 同板块同名同结构 | debug!, syscall_body! | debug!, syscall_body! |
| `arceos/api/arceos_posix_api/src/imp/io.rs#sys_writev` | `api/src/imp/io.rs#sys_writev` | 同板块同名 | debug!, syscall_body! | (0..=1024).contains, Err, Ok, core::slice::from_raw_parts, debug!, iov.get_as_bytes ...(+2) |

#### 文件系统
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `arceos/ulib/axlibc/c/glob.c#do_glob` | `arceos/ulib/axlibc/c/glob.c#do_glob` | 同板块同名同结构 | S_ISDIR, append, closedir, do_glob, errfunc, fnmatch ...(+7) | S_ISDIR, append, closedir, do_glob, errfunc, fnmatch ...(+7) |
| `arceos/ulib/axlibc/src/setjmp.rs#longjmp` | `arceos/ulib/axlibc/src/setjmp.rs#longjmp` | 同板块同名 | core::arch::naked_asm! | core::arch::naked_asm! |
| `arceos/ulib/axlibc/src/setjmp.rs#setjmp` | `arceos/ulib/axlibc/src/setjmp.rs#setjmp` | 同板块同名 | core::arch::naked_asm! | core::arch::naked_asm! |
| `arceos/api/arceos_posix_api/src/imp/io_mpx/select.rs#poll_all` | `arceos/api/arceos_posix_api/src/imp/io_mpx/select.rs#poll_all` | 同板块同名同结构 | Ok, debug!, execpt_bits_ptr.add, get_file_like, get_file_like(fd as _)?.poll, read_bits_ptr.add ...(+3) | Ok, debug!, execpt_bits_ptr.add, get_file_like, get_file_like(fd as _)?.poll, read_bits_ptr.add ...(+3) |

#### 设备驱动
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `arceos/tools/bwbench_client/src/main.rs#transmit_benchmark` | `arceos/tools/bwbench_client/src/main.rs#transmit_benchmark` | 同板块同名同结构 | Local::now, NetDevice::new, NetDevice::new(interface).unwrap, current_time.signed_duration_since, current_time.signed_duration_since(past_time).num_seconds, dev.send ...(+2) | Local::now, NetDevice::new, NetDevice::new(interface).unwrap, current_time.signed_duration_since, current_time.signed_duration_since(past_time).num_seconds, dev.send ...(+2) |
| `arceos/tools/bwbench_client/src/device.rs#bind_interface` | `arceos/tools/bwbench_client/src/device.rs#bind_interface` | 同板块同名同结构 | ETH_P_ALL.to_be, Err, Ok, libc::bind, self.ifreq.ioctl, std::io::Error::last_os_error ...(+2) | ETH_P_ALL.to_be, Err, Ok, libc::bind, self.ifreq.ioctl, std::io::Error::last_os_error ...(+2) |
| `arceos/modules/axdriver/src/lib.rs#init_drivers` | `arceos/modules/axdriver/src/lib.rs#init_drivers` | 同板块同名同结构 | AllDevices::default, all_devs.block.iter, all_devs.block.iter().enumerate, all_devs.display.iter, all_devs.display.iter().enumerate, all_devs.net.iter ...(+5) | AllDevices::default, all_devs.block.iter, all_devs.block.iter().enumerate, all_devs.display.iter, all_devs.display.iter().enumerate, all_devs.net.iter ...(+5) |
| `arceos/tools/bwbench_client/src/main.rs#receive_benchmark` | `arceos/tools/bwbench_client/src/main.rs#receive_benchmark` | 同板块同名同结构 | Local::now, NetDevice::new, NetDevice::new(interface).unwrap, current_time.signed_duration_since, current_time.signed_duration_since(past_time).num_seconds, dev.recv ...(+1) | Local::now, NetDevice::new, NetDevice::new(interface).unwrap, current_time.signed_duration_since, current_time.signed_duration_since(past_time).num_seconds, dev.recv ...(+1) |

#### 网络协议栈
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `arceos/ulib/axlibc/c/network.c#inet_pton` | `arceos/ulib/axlibc/c/network.c#inet_pton` | 同板块同名同结构 | hexval, inet_pton, isdigit, memmove | hexval, inet_pton, isdigit, memmove |
| `arceos/api/arceos_posix_api/src/imp/net.rs#shutdown` | `api/src/fd/net.rs#shutdown` | 同板块同名同结构 | Ok, tcpsocket.lock, tcpsocket.peer_addr, tcpsocket.shutdown, udpsocket.lock, udpsocket.peer_addr ...(+1) | Ok, tcpsocket.lock, tcpsocket.peer_addr, tcpsocket.shutdown, udpsocket.lock, udpsocket.peer_addr ...(+1) |
| `arceos/ulib/axlibc/c/socket.c#accept4` | `arceos/ulib/axlibc/c/socket.c#accept4` | 同板块同名同结构 | accept, fcntl | accept, fcntl |
| `arceos/api/arceos_posix_api/src/imp/net.rs#recvfrom` | `api/src/fd/net.rs#recvfrom` | 同板块同名同结构 | Ok, Some, tcpsocket.lock, tcpsocket.lock().recv, tcpsocket.lock().recv(buf).map, udpsocket
                .lock ...(+2) | Ok, Some, tcpsocket.lock, tcpsocket.lock().recv, tcpsocket.lock().recv(buf).map, udpsocket
                .lock ...(+2) |

#### 文件系统/第三方库
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `compat/oscomp-runner/src/ext4_import.rs#import_dir` | `crates/lwext4_rust/c/lwext4/src/ext4_balloc.c#ext4_balloc_alloc_block` | 同模块上下文对齐/差异观察 | Ok, entry
            .metadata, entry
            .metadata()
            .map_err, entry.map_err, ext4
                .read, ext4
                .read(path.as_str())
                .map_err ...(+15) | ext4_balloc_get_bgid_of_block, ext4_balloc_get_block_of_bgid, ext4_balloc_set_bitmap_csum, ext4_balloc_verify_bitmap_csum, ext4_bg_get_block_bitmap, ext4_bg_get_free_blocks_count ...(+19) |
| `compat/oscomp-runner/src/ext4_import.rs#read` | `crates/lwext4_rust/c/lwext4/src/ext4_journal.c#jbd_journal_prepare` | 同模块上下文对齐/差异观察 | Box::new, DiskReadError, Err, Ok, disk.read_one, disk.read_one(&mut dst[filled..]).map_err ...(+5) | EXT4_BLOCK_ZERO, RB_FIND, TAILQ_FOREACH_REVERSE_SAFE, TAILQ_FOREACH_SAFE, TAILQ_REMOVE, ext4_bcache_clear_dirty ...(+17) |
| `compat/oscomp-runner/src/ext4_import.rs#import_boot_disk` | `crates/lwext4_rust/c/lwext4/src/ext4.c#ext4_dir_rm` | 同模块上下文对齐/差异观察 | Box::new, Ext4::load, Ext4::load(Box::new(reader)).map_err, ImportStats::default, Ok, format! ...(+3) | EXT4_MP_LOCK, EXT4_MP_UNLOCK, ext4_block_cache_write_back, ext4_dir_en_get_inode, ext4_dir_iterator_fini, ext4_dir_iterator_init ...(+19) |
| `compat/oscomp-runner/src/ext4_import.rs#join_path` | `crates/lwext4_rust/c/lwext4/src/ext4_dir_idx.c#ext4_dir_dx_split_index` | 同模块上下文对齐/差异观察 | format! | ext4_block_set, ext4_dir_dx_climit_get_count, ext4_dir_dx_climit_get_limit, ext4_dir_dx_climit_set_count, ext4_dir_dx_climit_set_limit, ext4_dir_dx_entry_get_hash ...(+10) |

#### 网络协议栈/第三方库
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `arceos/modules/axnet/src/smoltcp_impl/tcp.rs#connect` | `arceos/modules/axnet/src/smoltcp_impl/tcp.rs#connect` | 同板块同名 | Err, Ok, SOCKET_SET
                .with_socket_mut::<tcp::Socket, _, _>, SOCKET_SET.add, SocketSetWrapper::new_tcp_socket, Some ...(+24) | Err, Ok, SOCKET_SET
                .with_socket_mut::<tcp::Socket, _, _>, SOCKET_SET.add, SocketSetWrapper::new_tcp_socket, Some ...(+29) |
| `arceos/modules/axnet/src/smoltcp_impl/dns.rs#query` | `arceos/modules/axnet/src/smoltcp_impl/dns.rs#query` | 同板块同名 | Err, IpAddr::from, Ok, SOCKET_SET
            .with_socket_mut::<dns::Socket, _, _>, SOCKET_SET
            .with_socket_mut::<dns::Socket, _, _>(handle, |socket| {
                socket.start_query(iface.lock().context(), name, query_type)
            })
            .map_err, SOCKET_SET.poll_interfaces ...(+12) | Err, Ok, SOCKET_SET
            .with_socket_mut::<dns::Socket, _, _>, SOCKET_SET
            .with_socket_mut::<dns::Socket, _, _>(handle, |socket| {
                socket.start_query(iface.lock().context(), name, query_type)
            })
            .map_err, SOCKET_SET.poll_interfaces, SOCKET_SET.with_socket_mut::<dns::Socket, _, _> ...(+12) |
| `arceos/modules/axnet/src/smoltcp_impl/bench.rs#bench_transmit_bandwidth` | `arceos/modules/axnet/src/smoltcp_impl/bench.rs#bench_transmit_bandwidth` | 同板块同名同结构 | (current_time - past_time).secs, AxNetTxToken::consume, InterfaceWrapper::current_time, info!, self.transmit, tx_buf[0..12].fill ...(+2) | (current_time - past_time).secs, AxNetTxToken::consume, InterfaceWrapper::current_time, info!, self.transmit, tx_buf[0..12].fill ...(+2) |
| `arceos/modules/axnet/src/smoltcp_impl/tcp.rs#shutdown` | `arceos/modules/axnet/src/smoltcp_impl/tcp.rs#shutdown` | 同板块同名同结构 | LISTEN_TABLE.unlisten, Ok, SOCKET_SET.poll_interfaces, SOCKET_SET.with_socket_mut::<tcp::Socket, _, _>, debug!, self.handle.get ...(+9) | LISTEN_TABLE.unlisten, Ok, SOCKET_SET.poll_interfaces, SOCKET_SET.with_socket_mut::<tcp::Socket, _, _>, debug!, self.handle.get ...(+9) |

#### 第三方库
仅参考项目中观察到该模块代表函数，目标项目没有同模块可对齐证据：
- 参考项目：`crates/visionfive2-sd/src/lib.rs#send_cmd`，调用摘要：RawInterrupt::from, RawInterruptStatusReg::from, S::sleep_ms_until, Some, arg.into, assert! ...(+24)
- 参考项目：`crates/visionfive2-sd/src/lib.rs#init_sdcard`，调用摘要：CardTypeReg::from, CardTypeReg::from(0).with_card_width4_1, CmdArg::new, CmdReg::from, ControlReg::from, RawInterruptStatusReg::from ...(+20)
- 参考项目：`crates/page_table_multiarch/page_table_multiarch/src/bits64.rs#map_region`，调用摘要：Err, M::flush_tlb, Ok, PageSize::Size1G.is_aligned, PageSize::Size2M.is_aligned, PageSize::Size4K.is_aligned ...(+12)
- 参考项目：`crates/axdriver_crates/axdriver_virtio/src/net.rs#try_new`，调用摘要：Err, InnerDev::new, InnerDev::new(transport).map_err, NetBufPool::new, Ok, Some ...(+14)

#### 通用模块/其他
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `arceos/ulib/axlibc/c/printf.c#print_exponential_number` | `arceos/ulib/axlibc/c/printf.c#print_exponential_number` | 同板块同名同结构 | ABS_FOR_PRINTING, PRINTF_ABS, bastardized_floor, get_components, get_normalized_components, get_sign_bit ...(+5) | ABS_FOR_PRINTING, PRINTF_ABS, bastardized_floor, get_components, get_normalized_components, get_sign_bit ...(+5) |
| `arceos/ulib/axlibc/c/pow.c#pow` | `arceos/ulib/axlibc/c/pow.c#pow` | 同板块同名同结构 | __builtin_fma, __math_invalid, __math_oflow, __math_uflow, asdouble, asuint64 ...(+8) | __builtin_fma, __math_invalid, __math_oflow, __math_uflow, asdouble, asuint64 ...(+8) |
| `arceos/ulib/axlibc/c/string.c#twoway_strstr` | `arceos/ulib/axlibc/c/string.c#twoway_strstr` | 同板块同名同结构 | BITOP, MAX, memchr, memcmp | BITOP, MAX, memchr, memcmp |
| `arceos/ulib/axlibc/c/printf.c#print_broken_up_decimal` | `arceos/ulib/axlibc/c/printf.c#print_broken_up_decimal` | 同板块同名同结构 | out_rev_ | out_rev_ |


#### 1. 宏观判定
- **定性结论**: 独立开发 / 基于共同上游框架的合法衍生 (相似度: 30.98%，共同 AST 指纹: 995；唯一 AST 指纹: 目标项目 1187，参考项目 3020；覆盖率: 目标项目 83.82%，参考项目 32.95%)

#### 2. 全模块覆盖矩阵解读
- **进程管理**: 双方均存在该模块。存在共同 AST 指纹。目标覆盖率 90.32%，参考覆盖率 33.33%。高目标覆盖率源于双方均实现了基于 ArceOS 框架的标准等待队列、任务状态原子操作及 POSIX 兼容存根，这些代码在框架约束下高度同构。证据强度：**弱**。
- **任务调度/异步运行时**: 双方均存在该模块。存在共同 AST 指纹。目标覆盖率 82.47%，参考覆盖率 44.1%。重合部分主要为 ArceOS 调度器核心（`run_queue`）及公共 C 库（`printf`），属于框架基础设施。证据强度：**弱**。
- **内存管理**: 双方均存在该模块。存在共同 AST 指纹。目标覆盖率 63.69%，参考覆盖率 34.36%。重合集中在 PCI 设备配置、格式化字符串等公共逻辑，未涉及核心页表算法或分配器策略的深度同源。证据强度：**未形成**。
- **中断/异常处理**: 双方均存在该模块。存在共同 AST 指纹。目标覆盖率 66.67%，参考覆盖率 4.08%。极低的参考覆盖率表明双方中断处理架构完全不同。重合的 `exp_inline` 为数学库函数，`may_interrupt` 为测试辅助代码，与核心中断逻辑无关。证据强度：**未形成**。
- **系统调用**: 双方均存在该模块。存在共同 AST 指纹。目标覆盖率 98.25%，参考覆盖率 43.41%。高目标覆盖率源于 `syscall_body!` 宏及 POSIX 接口的标准化封装，这是 ArceOS 框架的固定模式，无独立创造性。证据强度：**弱**。
- **文件系统**: 双方均存在该模块。存在共同 AST 指纹。目标覆盖率 85.44%，参考覆盖率 39.4%。重合部分为 `glob`、`setjmp` 等 C 库函数及 `select` 机制，属于标准接口实现。证据强度：**未形成**。
- **设备驱动**: 双方均存在该模块。存在共同 AST 指纹。目标覆盖率 89.36%，参考覆盖率 68.29%。重合集中在 ArceOS 驱动框架初始化及带宽测试工具，属于框架代码和通用测试逻辑。证据强度：**弱**。
- **网络协议栈**: 双方均存在该模块。存在共同 AST 指纹。目标覆盖率 100.0%，参考覆盖率 96.23%。极高的覆盖率源于双方均完整集成了 `smoltcp` 第三方网络栈，所有 socket 操作、DNS 查询等均为对该库 API 的直接调用。证据强度：**弱**。
- **第三方库/公共依赖**: 参考项目存在大量目标项目未包含的第三方库（如 `visionfive2-sd`、`page_table_multiarch`），目标项目覆盖率 0.0%。这直接证明目标项目并非对参考项目的全盘复制，而是有选择地集成了不同的组件。证据强度：**未形成**。

#### 3. 底层 AST 关键证据指纹表
| 所属板块 | 目标项目路径 | 参考项目路径 | 证据类型 | 技术说明 |
| :--- | :--- | :--- | :--- | :--- |
| 进程管理 | `arceos/modules/axtask/src/run_queue.rs#exit_current` | `arceos/modules/axtask/src/run_queue.rs#exit_current` | 同源框架/API迁移 | 核心调度退出逻辑。调用流高度一致，但目标项目将底层关机调用从 `axhal::misc::terminate` 替换为 `axhal::power::system_off`，属于 HAL 层 API 的正常演进或分支差异，非抄袭伪装。 |
| 进程管理 | `arceos/modules/axtask/src/wait_queue.rs#cancel_events` | `arceos/modules/axtask/src/wait_queue.rs#cancel_events` | 同源框架/领域约束 | 等待队列事件取消。调用流 `curr.in_wait_queue` -> `curr.ptr_eq` -> `self.queue.lock().retain` 是操作 `VecDeque` 和任务状态标志的标准模式，在 ArceOS 框架内无其他实现路径。 |
| 进程管理 | `arceos/api/arceos_posix_api/src/imp/task.rs#sys_exit` | `arceos/api/arceos_posix_api/src/imp/task.rs#sys_exit` | 同源框架/API迁移 | POSIX `_exit` 系统调用封装。目标项目使用 `axhal::power::system_off`，参考项目使用 `axhal::misc::terminate`，差异点明确，其余 `axtask::exit` 和 `debug!` 为框架固定调用。 |
| 进程管理 | `arceos/ulib/axlibc/c/unistd.c#fork` | `arceos/ulib/axlibc/c/unistd.c#fork` | 公共接口/Trivial | POSIX `fork` 存根。双方均返回 `unimplemented`，这是未实现功能的行业标准做法，AST 仅含单一宏，无原创性。 |
| 任务调度/异步运行时 | `arceos/ulib/axlibc/c/printf.c#format_string_loop` | `arceos/ulib/axlibc/c/printf.c#format_string_loop` | 第三方库/公共依赖 | `printf` 格式化核心循环。调用图谱中的 `ABS_FOR_PRINTING`、`print_floating_point` 等均为 musl/newlib 等开源 C 库的标准内部实现，非任何一方原创。 |
| 网络协议栈 | `arceos/modules/axnet/src/smoltcp_impl/tcp.rs#connect` | `arceos/modules/axnet/src/smoltcp_impl/tcp.rs#connect` | 第三方库/公共依赖 | TCP 连接操作。调用流围绕 `SOCKET_SET.with_socket_mut::<tcp::Socket, _, _>` 展开，这是对 `smoltcp` 库的标准封装，所有基于该库的项目代码必然相似。 |
| 设备驱动 | `arceos/modules/axdriver/src/lib.rs#init_drivers` | `arceos/modules/axdriver/src/lib.rs#init_drivers` | 同源框架 | 驱动初始化。遍历 `AllDevices` 并枚举各类设备是 ArceOS 驱动框架规定的标准流程，代码结构由框架决定。 |
| 系统调用 | `arceos/api/arceos_posix_api/src/imp/io_mpx/epoll.rs#sys_epoll_wait` | `arceos/api/arceos_posix_api/src/imp/io_mpx/epoll.rs#sys_epoll_wait` | 同源框架/Trivial | `epoll_wait` 系统调用。函数体仅包含 `debug!` 和 `syscall_body!` 宏，是 ArceOS POSIX 兼容层的标准模板，无业务逻辑。 |

#### 4. 核心代码调用流对比剖析
* **【进程管理/任务退出路径】**
* **底层技术析理**: 双方 `exit_current` 函数均遵循“取出当前任务 -> 清理退出任务队列 -> 通知等待者 -> 关机”的流程。然而，在最终的关机操作上，目标项目调用 `axhal::power::system_off`，而参考项目调用 `axhal::misc::terminate`。这并非简单的“换名”，而是反映了 ArceOS 硬件抽象层（HAL）从通用的 `misc` 模块向更专用的 `power` 模块的架构演进。目标项目采用了更新或不同的 HAL 分支，其调用流末梢的差异是独立演化的证据。
* **代码溯源链接**:
  - 目标项目: `arceos/modules/axtask/src/run_queue.rs#exit_current`
  - 参考项目: `arceos/modules/axtask/src/run_queue.rs#exit_current`

* **【网络协议栈/TCP连接】**
* **底层技术析理**: 双方 `connect` 函数的调用流完全围绕 `smoltcp` 库的 `SOCKET_SET` 和 `tcp::Socket` 展开。从创建套接字、添加到集合、调用 `connect` 到轮询接口，每一步都是对第三方库 API 的直接、必要调用。这种一致性并非相互抄袭，而是共同依赖于同一个开源网络协议栈的必然结果。任何集成 `smoltcp` 的项目，其 TCP 连接代码的 AST 结构都将与此高度相似。
* **代码溯源链接**:
  - 目标项目: `arceos/modules/axnet/src/smoltcp_impl/tcp.rs#connect`
  - 参考项目: `arceos/modules/axnet/src/smoltcp_impl/tcp.rs#connect`

#### 5. 单方模块与降权项
- **单方模块**: 参考项目独有的 `第三方库` 模块（如 `visionfive2-sd`、`page_table_multiarch`）在目标项目中完全缺失。这是证明双方项目独立选型、非“全盘抄袭”的关键证据。
- **降权项**:
  - **跨板块硬凑**: 控方将文件系统模块的 `arceos/modules/axfs/src/mounts.rs#procfs` 归入进程管理，将 C 标准库的 `printf.c#format_string_loop` 归入任务调度，属于分类错误，其证据效力已被降权。
  - **公共接口/Trivial 函数**: `sys_getpid`、`sys_epoll_wait` 等仅包含 `syscall_body!` 宏的函数，以及 `fork`、`execve` 等返回 `unimplemented` 的存根函数，因其无创造性表达，不构成强同源证据。
  - **第三方库调用**: 网络协议栈、`printf` 等对 `smoltcp`、开源 C 库的调用，属于公共依赖，不能作为抄袭证据。

#### 6. 保守结论
综合评估，目标项目与参考项目的 AST 相似度为 30.98%，参考项目覆盖率仅 32.95%，且目标项目存在大量唯一 AST 指纹（1187）。双方代码的重合部分高度集中于 ArceOS 操作系统框架的公共基础设施、POSIX 标准接口封装以及 `smoltcp` 等第三方开源库的调用。这些重合是共同上游依赖和操作系统领域固有约束下的自然结果，而非一方对另一方的抄袭。目标项目缺失参考项目中的多个第三方库，并在 HAL 层接口上存在独立演化痕迹。因此，现有证据不足以支持抄袭指控，应认定为基于同一开源框架的独立开发作品。

------------------------------

