# 🛡️ OS 内核赛自动化代码级架构审计报告

**审计时间**: `2026-07-02 02:13:49`
**参赛作品名称**: `2026-T2026100069910232-XX`
**年份**: `2026`
**学校**: `XX`
**队伍名称**: `T2026100069910232`
**比赛名字**: `OS-Competition`
**比赛子赛道**: `Kernel-Implementation`
**仓库地址**: `https://gitlab.eduxiji.net/educg-group-43501-3132633/T2026100069910232-2532`
**推测开发语言**: `Rust / C (由AST探针自动识别)`
**特征函数量**: `1400`

## ⚔️ 底层代码同源性对比 (AST Homology)
> 本部分由专门的代码审查智能体，对该项目与符合年份条件的历届最高相似度项目进行控制流与底层调用对比。新版报告会先给出确定性的全模块覆盖矩阵，再给出 Agent 的分模块解释，避免只围绕少量抽样函数展开。

### 🏆 Top 5 底层对标: vs `2025-九莲宝灯一向听-天津理工大学`（天津理工大学 / 九莲宝灯一向听）

#### 对方作品信息

**对方年份**: `2025`
**对方学校**: `天津理工大学`
**对方队伍名称**: `九莲宝灯一向听`
**对方比赛名字**: `OS-Competition`
**对方比赛子赛道**: `Kernel-Implementation`
**对方仓库地址**: `https://gitlab.eduxiji.net/educg-group-36002-2710490/T202510060995409-3585`

> 双向覆盖率：共同 AST 指纹: 61；唯一 AST 指纹: 目标项目 1227，参考项目 2610；覆盖率: 目标项目 4.97%，参考项目 2.34%

## 全模块对比总览（确定性证据）

> 总体覆盖率：共同 AST 指纹: 61；唯一 AST 指纹: 目标项目 1227，参考项目 2610；覆盖率: 目标项目 4.97%，参考项目 2.34%

### 1. 模块覆盖矩阵
| 模块 | 目标函数数 | 参考函数数 | 共同唯一 AST 指纹 | Target 覆盖 | Base 覆盖 | 状态 |
| :--- | ---: | ---: | ---: | ---: | ---: | :--- |
| 进程管理 | 74 | 79 | 3 | 4.41% | 4.11% | 双方存在且有结构重合 |
| 任务调度/异步运行时 | 295 | 385 | 6 | 2.24% | 2.18% | 双方存在且有结构重合 |
| 内存管理 | 205 | 301 | 11 | 5.7% | 4.04% | 双方存在且有结构重合 |
| 中断/异常处理 | 9 | 60 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |
| 系统调用 | 153 | 130 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |
| 文件系统 | 564 | 616 | 5 | 1.03% | 1.1% | 双方存在且有结构重合 |
| 设备驱动 | 0 | 156 | 0 | 0.0% | 0.0% | 仅参考项目体现 |
| 网络协议栈 | 0 | 61 | 0 | 0.0% | 0.0% | 仅参考项目体现 |
| 文件系统/第三方库 | 0 | 577 | 0 | 0.0% | 0.0% | 仅参考项目体现 |
| 网络协议栈/第三方库 | 0 | 130 | 0 | 0.0% | 0.0% | 仅参考项目体现 |
| 第三方库 | 0 | 209 | 0 | 0.0% | 0.0% | 仅参考项目体现 |
| 通用模块/其他 | 100 | 630 | 2 | 2.41% | 0.42% | 双方存在且有结构重合 |

### 2. 分模块证据清单

#### 进程管理
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `src/kernel/src/task/ops.rs#do_exit` | `api/src/imp/task/exit.rs#do_exit` | 同板块同名 | Arc::downgrade, FutexKey::new_current, SignalInfo::new_kernel, Some, axtask::yield_now, child.free ...(+43) | FD_TABLE.clear, SignalInfo::new, UserPtr::<Pid>::from, axtask::exit, axtask::yield_now, clear_child_tid.get_as_mut ...(+22) |
| `src/kernel/src/syscall/signal.rs#sys_rt_sigprocmask` | `api/src/imp/signal.rs#sys_rt_sigprocmask` | 同板块同名 | Err, Ok, check_sigset_size, curr.as_thread, current, debug! ...(+7) | Err, Ok, check_sigset_size, current, current()
        .task_ext, current()
        .task_ext()
        .thread_data ...(+2) |
| `src/kernel/src/syscall/ipc/shm.rs#clear_proc_shm` | `api/src/imp/ipc/shm.rs#clear_proc_shm` | 同板块同名 | self.get_inner_by_shmid, self.get_shmids_by_pid, self.remove_pid, self.remove_shmid, shm_inner.attach_count, shm_inner.detach_process ...(+1) | SHM_MANAGER.lock, shm_inner.attach_count, shm_inner.detach_process, shm_inner.lock, shm_manager.get_inner_by_shmid, shm_manager.get_inner_by_shmid(shmid).unwrap ...(+3) |
| `src/kernel/src/file/pidfd.rs#process_data` | `core/src/task.rs#process_data` | 同板块同名 | Err, self.proc_data.upgrade, self.proc_data.upgrade().ok_or, thread_exit.load | self.thread.process, self.thread.process().data, self.thread.process().data().unwrap |

#### 任务调度/异步运行时
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `src/kernel/src/syscall/sync/futex.rs#sys_futex` | `api/src/imp/futex.rs#sys_futex` | 同板块同名 | (deadline - now).min, AxError::from, Err, FutexKey::new_current, Ok, Some ...(+23) | Err, Ok, axtask::yield_now, curr.task_ext, curr.task_ext().process_data, current ...(+16) |
| `src/kernel/src/syscall/signal.rs#sys_kill` | `api/src/imp/signal.rs#sys_kill` | 同板块同名 | Err, Ok, Some, check_kill_permission, check_kill_permission(proc.pid(), &sig).is_err, check_kill_permission(proc_data.proc.pid(), &Some(sig.clone())).is_err ...(+16) | Err, Ok, curr.task_ext, curr.task_ext().thread.process, curr.task_ext().thread.process().group, current ...(+9) |
| `src/kernel/src/syscall/fs/stat.rs#sys_statx` | `api/src/imp/fs/stat.rs#sys_statx` | 同板块同名 | Err, Ok, Some, check_stat_path_search, debug!, path.as_str ...(+8) | Err, Ok, debug!, f.stat, f.stat()?.into, get_file_like ...(+9) |
| `src/kernel/src/task/signal.rs#check_signals` | `api/src/signal.rs#check_signals` | 同板块同名 | Poll::Ready, block_on, cx.waker, data.child_exit_event.wake, do_exit, get_process_data ...(+11) | current, current()
        .task_ext, current()
        .task_ext()
        .thread_data, current()
        .task_ext()
        .thread_data()
        .signal
        .check_signals, do_exit, sig.signo |

#### 内存管理
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `src/kernel/src/syscall/mm/mmap.rs#sys_mmap` | `api/src/imp/mm/mmap.rs#sys_mmap` | 同板块同名 | (addr + length).align_up, Arc::new, Backend::new_alloc, Backend::new_cow, Backend::new_file, Backend::new_linear ...(+51) | Err, File::from_fd, MmapFlags::from_bits_truncate, MmapProt::from_bits_truncate, Ok, VirtAddr::from ...(+27) |
| `src/kernel/src/mm/loader.rs#load_user_app` | `core/src/mm.rs#load_user_app` | 同板块同名 | (ustack_top - user_sp_aligned).align_up_4k, Backend::new_alloc, ELF_LOADER.lock, ELF_LOADER.lock().load, Err, FS_CONTEXT.lock ...(+54) | CStr::from_bytes_with_nul, CStr::from_bytes_with_nul(interp)
                .map_err, CStr::from_bytes_with_nul(interp)
                .map_err(|_| AxError::InvalidData)?
                .to_str, CStr::from_bytes_with_nul(interp)
                .map_err(|_| AxError::InvalidData)?
                .to_str()
                .map_err, ElfFile::new, ElfFile::new(&file_data).map_err ...(+41) |
| `src/kernel/src/syscall/ipc/shm.rs#sys_shmat` | `api/src/imp/ipc/shm.rs#sys_shmat` | 同板块同名 | Arc::new, Backend::new_shared, Err, Ok, SHM_MANAGER.lock, SharedPages::new ...(+29) | Err, Ok, SHM_MANAGER.lock, ShmAtFlags::from_bits_truncate, Some, VirtAddr::from ...(+28) |
| `src/kernel/src/mm/access.rs#check_null_terminated` | `api/src/ptr.rs#check_null_terminated` | 同板块同名 | Err, Layout::new::<T>, Layout::new::<T>().align, Ok, T::default, access_user_memory ...(+10) | Err, Layout::new::<T>, Layout::new::<T>().align, Ok, T::default, VirtAddrRange::from_start_size ...(+12) |

#### 中断/异常处理
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `src/tools/cargo-axplat-0.3.0/template/src/irq.rs#set_enable` | `arceos/modules/axhal/src/platform/loongarch64_qemu_virt/irq.rs#set_enable` | 同板块同名 | todo! | ecfg::read, ecfg::read().lie, ecfg::set_lie |
| `src/kernel/src/pseudofs/dev/tty/terminal/termios.rs#default` | `arceos/ulib/axlibc/c/pow.c#exp_inline` | 同模块上下文对齐/差异观察 | ctl | __math_oflow, __math_uflow, asdouble, asuint64, converttoint, eval_as_double ...(+4) |
| `src/kernel/src/file/signalfd.rs#from_signal_info` | `arceos/modules/axhal/src/arch/aarch64/trap.rs#handle_sync_exception` | 同模块上下文对齐/差异观察 | sig_info.code, sig_info.errno, sig_info.signo | ESR_EL1.extract, crate::trap::handle_syscall, crate::trap::post_trap_callback, debug!, esr.read, esr.read_as_enum ...(+6) |
| `src/kernel/src/pseudofs/dev/tty/terminal/termios.rs#signo_for` | `arceos/modules/axhal/src/arch/x86_64/idt.rs#new` | 同模块上下文对齐/差异观察 | Some, self.special_char | InterruptDescriptorTable::new, core::mem::transmute, core::slice::from_raw_parts_mut, entries[i].set_handler_fn, opt.set_privilege_level |

#### 系统调用
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `src/kernel/src/syscall/io_mpx/epoll.rs#sys_epoll_ctl` | `arceos/api/arceos_posix_api/src/imp/io_mpx/epoll.rs#sys_epoll_ctl` | 同板块同名 | Epoll::from_fd, EpollFlags::from_bits, EpollFlags::from_bits(event.events & !events.bits()).ok_or, Err, IoEvents::from_bits_truncate, Ok ...(+7) | debug!, syscall_body! |
| `src/kernel/src/syscall/ipc/shm.rs#sys_shmctl` | `api/src/imp/ipc/shm.rs#sys_shmctl` | 同板块同名 | Err, Ok, SHM_MANAGER.lock, buf.get_as_mut, monotonic_time_nanos, nullable! ...(+3) | Err, Ok, SHM_MANAGER.lock, buf.get_as_mut, monotonic_time_nanos, nullable! ...(+3) |
| `src/kernel/src/syscall/net/socket.rs#sys_connect` | `arceos/api/arceos_posix_api/src/imp/net.rs#sys_connect` | 同板块同名 | Ok, Socket::from_fd, Socket::from_fd(fd)?.connect, Socket::from_fd(fd)?.connect(addr).map_err, SocketAddrEx::read_from_user, debug! | debug!, syscall_body! |
| `src/kernel/src/syscall/net/socket.rs#sys_shutdown` | `arceos/api/arceos_posix_api/src/imp/net.rs#sys_shutdown` | 同板块同名 | Err, Socket::from_fd, debug!, socket.shutdown, socket.shutdown(how).map | debug!, syscall_body! |

#### 文件系统
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `src/kernel/src/syscall/fs/ctl.rs#sys_utimensat` | `api/src/imp/fs/ctl.rs#sys_utimensat` | 同板块同名 | Err, Ok, Some, atime.is_none, check_access, check_access(ptr_addr, core::mem::size_of::<[timespec; 2]>())
            .map_err ...(+13) | Ok, Some, atime.into, atime.is_none, file.atime.lock, file.downcast_ref::<File> ...(+16) |
| `src/kernel/src/syscall/fs/ctl.rs#sys_linkat` | `api/src/imp/fs/ctl.rs#sys_linkat` | 同板块同名 | Err, Ok, Path::new, VfsCredentials::effective, check_parent_permission, check_path_len ...(+18) | HARDLINK_MANAGER.create_link, Ok, debug!, handle_file_path, new_path.get_as_str, old_path.get_as_str ...(+1) |
| `src/kernel/src/syscall/fs/stat.rs#sys_faccessat2` | `api/src/imp/fs/stat.rs#sys_faccessat2` | 同板块同名 | AccessMode::empty, Err, Ok, VfsCredentials::effective, VfsCredentials::real, check_path_len ...(+13) | AccessFlags::from_bits, AccessFlags::from_bits(mode).ok_or, Err, Ok, OpenOptions::new, axfs::fops::Directory::open_dir ...(+13) |
| `src/kernel/src/pseudofs/dev/event.rs#read_at` | `arceos/modules/axfs/src/readonly_ramfs.rs#read_at` | 同板块同名 | Err, Ok, buf.chunks_exact_mut, buf.is_empty, buf.len, inner.has_event ...(+8) | Ok, buf.len, buf[..src.len()].copy_from_slice, self.content.len, self.content.len().min, src.len |

#### 设备驱动
仅参考项目中观察到该模块代表函数，目标项目没有同模块可对齐证据：
- 参考项目：`arceos/modules/axdriver/src/dwmac.rs#set_clocks_uboot`，调用摘要：<Self as DwmacHal>::mmio_phys_to_virt, <Self as DwmacHal>::mmio_phys_to_virt(pac::AONCRG::ptr() as usize, 0x1000).as_ptr, <Self as DwmacHal>::mmio_phys_to_virt(pac::SYSCRG::ptr() as usize, 0x1000).as_ptr, DwmacHalImpl::wait_until, Self::check_diff, aoncrg.clk_ahb_gmac5 ...(+67)
- 参考项目：`arceos/modules/axdriver/src/dwmac.rs#print_preserved_status`，调用摘要：<Self as DwmacHal>::mmio_phys_to_virt, <Self as DwmacHal>::mmio_phys_to_virt(pac::AONCRG::ptr() as usize, 0x1000).as_ptr, <Self as DwmacHal>::mmio_phys_to_virt(pac::SYSCRG::ptr() as usize, 0x1000).as_ptr, log::info!, pac::AONCRG::ptr, pac::SYSCRG::ptr ...(+24)
- 参考项目：`arceos/modules/axdriver/src/dwmac.rs#print_clocks`，调用摘要：<Self as DwmacHal>::mmio_phys_to_virt, <Self as DwmacHal>::mmio_phys_to_virt(pac::AONCRG::ptr() as usize, 0x1000).as_ptr, <Self as DwmacHal>::mmio_phys_to_virt(pac::SYSCRG::ptr() as usize, 0x1000).as_ptr, aoncrg.clk_axi_gmac5, aoncrg.clk_axi_gmac5().read, aoncrg.clk_axi_gmac5().read().clk_icg ...(+27)
- 参考项目：`arceos/tools/raspi4/chainloader/src/bsp/device_driver/bcm/bcm2xxx_pl011_uart.rs#init`，调用摘要：FBRD::BAUD_DIVFRAC.val, IBRD::BAUD_DIVINT.val, self.flush, self.registers
            .CR
            .write, self.registers
            .LCR_H
            .write, self.registers.CR.set ...(+3)

#### 网络协议栈
仅参考项目中观察到该模块代表函数，目标项目没有同模块可对齐证据：
- 参考项目：`arceos/ulib/axlibc/c/network.c#inet_pton`，调用摘要：hexval, inet_pton, isdigit, memmove
- 参考项目：`arceos/api/arceos_posix_api/src/imp/net.rs#shutdown`，调用摘要：Ok, tcpsocket.lock, tcpsocket.peer_addr, tcpsocket.shutdown, udpsocket.lock, udpsocket.peer_addr ...(+1)
- 参考项目：`arceos/ulib/axlibc/c/socket.c#accept4`，调用摘要：accept, fcntl
- 参考项目：`arceos/api/arceos_posix_api/src/imp/net.rs#recvfrom`，调用摘要：Ok, Some, tcpsocket.lock, tcpsocket.lock().recv, tcpsocket.lock().recv(buf).map, udpsocket
                .lock ...(+2)

#### 文件系统/第三方库
仅参考项目中观察到该模块代表函数，目标项目没有同模块可对齐证据：
- 参考项目：`crates/lwext4_rust/c/lwext4/src/ext4_balloc.c#ext4_balloc_alloc_block`，调用摘要：ext4_balloc_get_bgid_of_block, ext4_balloc_get_block_of_bgid, ext4_balloc_set_bitmap_csum, ext4_balloc_verify_bitmap_csum, ext4_bg_get_block_bitmap, ext4_bg_get_free_blocks_count ...(+19)
- 参考项目：`crates/lwext4_rust/c/lwext4/src/ext4_journal.c#jbd_journal_prepare`，调用摘要：EXT4_BLOCK_ZERO, RB_FIND, TAILQ_FOREACH_REVERSE_SAFE, TAILQ_FOREACH_SAFE, TAILQ_REMOVE, ext4_bcache_clear_dirty ...(+17)
- 参考项目：`crates/lwext4_rust/c/lwext4/src/ext4.c#ext4_dir_rm`，调用摘要：EXT4_MP_LOCK, EXT4_MP_UNLOCK, ext4_block_cache_write_back, ext4_dir_en_get_inode, ext4_dir_iterator_fini, ext4_dir_iterator_init ...(+19)
- 参考项目：`crates/lwext4_rust/c/lwext4/src/ext4_dir_idx.c#ext4_dir_dx_split_index`，调用摘要：ext4_block_set, ext4_dir_dx_climit_get_count, ext4_dir_dx_climit_get_limit, ext4_dir_dx_climit_set_count, ext4_dir_dx_climit_set_limit, ext4_dir_dx_entry_get_hash ...(+10)

#### 网络协议栈/第三方库
仅参考项目中观察到该模块代表函数，目标项目没有同模块可对齐证据：
- 参考项目：`arceos/modules/axnet/src/smoltcp_impl/tcp.rs#connect`，调用摘要：Err, Ok, Ok::<(IpEndpoint, IpEndpoint), AxError>, SOCKET_SET
                .with_socket_mut::<tcp::Socket, _, _>, SOCKET_SET.add, SocketSetWrapper::new_tcp_socket ...(+32)
- 参考项目：`arceos/modules/axnet/src/smoltcp_impl/dns.rs#query`，调用摘要：Err, Ok, SOCKET_SET
            .with_socket_mut::<dns::Socket, _, _>, SOCKET_SET
            .with_socket_mut::<dns::Socket, _, _>(handle, |socket| {
                socket.start_query(iface.lock().context(), name, query_type)
            })
            .map_err, SOCKET_SET.poll_interfaces, SOCKET_SET.with_socket_mut::<dns::Socket, _, _> ...(+12)
- 参考项目：`arceos/modules/axnet/src/smoltcp_impl/tcp.rs#recv_timeout`，调用摘要：Err, Ok, SOCKET_SET.with_socket_mut::<tcp::Socket, _, _>, ax_err!, ax_err_type!, current_ticks ...(+11)
- 参考项目：`arceos/modules/axnet/src/smoltcp_impl/mod.rs#init`，调用摘要：Config::new, ETH0.init_once, EthernetAddress, GATEWAY.parse, GATEWAY.parse().expect, IP.parse ...(+22)

#### 第三方库
仅参考项目中观察到该模块代表函数，目标项目没有同模块可对齐证据：
- 参考项目：`crates/axdriver_net/src/dwmac/mod.rs#init_phy`，调用摘要：Err, H::wait_until, Ok, Yt8531cPhy::<H>::new, core::time::Duration::from_millis, log::error! ...(+18)
- 参考项目：`crates/axdriver_net/src/dwmac/mod.rs#init`，调用摘要：AtomicBool::new, DescriptorRing::<RX_DESC_COUNT, H>::new, DescriptorRing::<TX_DESC_COUNT, H>::new, H::configure_platform, Ok, log::error! ...(+20)
- 参考项目：`crates/visionfive2-sd/src/lib.rs#send_cmd`，调用摘要：RawInterrupt::from, RawInterruptStatusReg::from, S::sleep_ms_until, Some, arg.into, assert! ...(+22)
- 参考项目：`crates/visionfive2-sd/src/lib.rs#init_sdcard`，调用摘要：CardTypeReg::from, CardTypeReg::from(0).with_card_width4_1, CmdArg::new, CmdReg::from, ControlReg::from, RawInterruptStatusReg::from ...(+20)

#### 通用模块/其他
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `src/kernel/src/time.rs#from_time_value` | `api/src/time.rs#from_time_value` | 同板块同名同结构 | tv.as_secs, tv.subsec_micros | tv.as_secs, tv.subsec_micros |
| `src/kernel/src/time.rs#from_time_value` | `api/src/time.rs#from_time_value` | 同板块同名同结构 | tv.as_secs, tv.subsec_micros | tv.as_secs, tv.subsec_micros |
| `src/kernel/src/time.rs#from_time_value` | `api/src/time.rs#from_time_value` | 同板块同名同结构 | tv.as_secs, tv.subsec_micros | tv.as_secs, tv.subsec_micros |
| `src/kernel/src/time.rs#from_time_value` | `api/src/time.rs#from_time_value` | 同板块同名同结构 | tv.as_secs, tv.subsec_nanos | tv.as_secs, tv.subsec_nanos |


#### 1. 宏观判定
- **定性结论**: 独立开发 (相似度: 1.62%，共同 AST 指纹: 61；唯一 AST 指纹: 目标项目 1227，参考项目 2610；覆盖率: 目标项目 4.97%，参考项目 2.34%)

#### 2. 全模块覆盖矩阵解读

*   **进程管理**
    *   双方均存在该模块。
    *   存在 3 个共同唯一 AST 指纹。
    *   Target 覆盖 4.41%，Base 覆盖 4.11%。覆盖率极低，表明双方在进程管理模块的实现上存在根本性差异，重合部分仅限于极少数由规范或语言惯用法决定的原子操作。
    *   证据强度：**弱**。共同指纹均为 Linux ABI 规范字段的原子读写操作 (`store`, `load`) 或标准库惯用法 (`RwLock` 查询)，不构成实质性代码复用证据。

*   **任务调度/异步运行时**
    *   双方均存在该模块。
    *   存在 6 个共同唯一 AST 指纹。
    *   Target 覆盖 2.24%，Base 覆盖 2.18%。覆盖率极低，表明双方在调度与运行时框架上采用了不同的设计。
    *   证据强度：**弱**。共同指纹多为系统调用入口的同名函数，其内部调用链差异巨大，例如 `sys_futex` 在目标项目中直接操作 `FutexKey`，而参考项目则通过 `task_ext` 间接访问。

*   **内存管理**
    *   双方均存在该模块。
    *   存在 11 个共同唯一 AST 指纹。
    *   Target 覆盖 5.7%，Base 覆盖 4.04%。覆盖率低，表明双方的内存管理后端、分配器或抽象层存在显著差异。
    *   证据强度：**弱**。共同指纹多为 `sys_mmap`、`sys_shmat` 等标准系统调用入口，但其内部实现调用的后端对象完全不同（目标项目使用 `Backend::new_*` 系列，参考项目使用 `VirtAddr`、`MmapFlags` 等），属于接口同名但实现异构。

*   **中断/异常处理**
    *   双方均存在该模块。
    *   共同唯一 AST 指纹为 0。
    *   Target 覆盖 0.0%，Base 覆盖 0.0%。双方在此模块无任何可观测的结构重合。
    *   证据强度：**未形成**。

*   **系统调用**
    *   双方均存在该模块。
    *   共同唯一 AST 指纹为 0。
    *   Target 覆盖 0.0%，Base 覆盖 0.0%。双方在此模块无任何可观测的结构重合。
    *   证据强度：**未形成**。

*   **文件系统**
    *   双方均存在该模块。
    *   存在 5 个共同唯一 AST 指纹。
    *   Target 覆盖 1.03%，Base 覆盖 1.1%。覆盖率极低，表明双方的文件系统实现（VFS、伪文件系统、具体文件系统驱动）是独立的。
    *   证据强度：**弱**。共同指纹多为 `sys_utimensat`、`sys_linkat` 等 POSIX 系统调用入口，内部实现调用的 VFS 抽象层完全不同（目标项目使用 `VfsCredentials`、`Path::new`，参考项目使用 `HARDLINK_MANAGER`、`handle_file_path`）。

*   **设备驱动**
    *   仅参考项目体现，目标项目无此模块。
    *   不存在共同 AST 指纹。
    *   证据强度：**未形成**。此模块为单方特有，是架构差异的体现。

*   **网络协议栈**
    *   仅参考项目体现，目标项目无此模块。
    *   不存在共同 AST 指纹。
    *   证据强度：**未形成**。此模块为单方特有，是架构差异的体现。

*   **第三方库/公共依赖**
    *   参考项目包含大量第三方库代码（如 `lwext4_rust`、`smoltcp` 等），目标项目未体现。
    *   不存在共同 AST 指纹。
    *   证据强度：**未形成**。此模块为单方特有，是架构差异的体现。

#### 3. 底层 AST 关键证据指纹表

| 所属板块 | 目标项目路径 | 参考项目路径 | 证据类型 | 技术说明 |
| :--- | :--- | :--- | :--- | :--- |
| 进程管理 | `src/kernel/src/task/mod.rs#set_clear_child_tid` | `core/src/task.rs#set_clear_child_tid` | 同板块同名，底层逻辑一致 | 均为 `self.clear_child_tid.store`。`clear_child_tid` 是 Linux `clone` 系统调用的标准 ABI 字段，原子存储是该字段的唯一操作方式，属于规范约束下的必然实现。 |
| 进程管理 | `src/kernel/src/task/mod.rs#clear_child_tid` | `core/src/task.rs#clear_child_tid` | 同板块同名，底层逻辑一致 | 均为 `self.clear_child_tid.load`。与上一条配对，是同一标准字段的原子读取操作，同样属于规范约束下的必然实现。 |
| 进程管理 | `src/kernel/src/task/mod.rs#is_clone_child` | `core/src/task.rs#is_clone_child` | 同板块同名，底层逻辑一致 | 调用图谱仅显示 `Some`。这是 Rust 语言中 `Option` 枚举的构造器，任何返回 `Option` 类型的函数都可能产生此 AST 节点，属于语言级噪声，无实际比对意义。 |
| 进程管理 | `src/kernel/src/task/ops.rs#get_process_group` | `core/src/task.rs#get_process_group` | 同板块同名 | 均为 `PROCESS_GROUP_TABLE.read().get(&pgid).ok_or`。这是对全局 `RwLock<HashMap>` 进行查询并转换错误的 Rust 标准惯用法，`PROCESS_GROUP_TABLE` 是描述性命名，非独创标识。 |
| 进程管理 | `src/kernel/src/task/ops.rs#processes` | `core/src/task.rs#processes` | 同板块同名 | 均为 `PROCESS_TABLE.read().values().collect`。这是遍历全局 `RwLock<HashMap>` 所有值的 Rust 标准惯用法，任何管理进程集合的内核都可能采用此模式。 |
| 进程管理 | `src/kernel/src/syscall/task/wait.rs#apply` | `api/src/imp/task/wait.rs#apply` | 同板块同名 | 均为 `child.group().pgid, child.pid`。这是 POSIX `waitid` 系统调用填充 `siginfo_t` 结构体时必须获取的进程组 ID 和进程 ID，属于领域规范必然。 |
| 进程管理 | `src/kernel/src/syscall/task/clone.rs#sys_fork` | `api/src/imp/task/clone.rs#sys_fork` | 同板块同名 | 均为 `sys_clone`。Linux 内核中 `fork` 本质上是 `clone(SIGCHLD, 0)` 的特例，将其实现为对 `sys_clone` 的透传调用是教科书式标准实现。 |
| 进程管理 | `src/kernel/src/syscall/task/exit.rs#sys_exit_group` | `api/src/imp/task/exit.rs#sys_exit_group` | 同板块同名 | 均调用 `do_exit`。`exit_group` 系统调用需执行与 `exit` 相同的核心退出逻辑，委托给同一内部函数是符合语义的标准做法。目标项目额外返回 `Ok`，存在细节差异。 |
| 进程管理 | `src/kernel/src/syscall/ipc/shm.rs#attach_process` | `api/src/imp/ipc/shm.rs#attach_process` | 同板块同名 | 均包含 `monotonic_time_nanos, self.va_range.insert`。共享内存挂载必须记录映射时间与虚拟地址范围，这是领域标准操作。目标项目缺少参考项目中的 `assert!`，存在实现差异。 |
| 进程管理 | `src/kernel/src/syscall/ipc/shm.rs#detach_process` | `api/src/imp/ipc/shm.rs#detach_process` | 同板块同名 | 均包含 `monotonic_time_nanos, self.va_range.remove`。共享内存卸载必须移除地址映射并记录时间，属于领域标准操作。目标项目额外使用 `is_some` 判断返回值，存在实现差异。 |

#### 4. 核心代码调用流对比剖析

*   **【进程管理/进程退出】**
    *   **底层技术析理**: 双方均实现了 `do_exit` 核心函数，但进程退出时的资源清理模型截然不同。目标项目采用基于 `FutexKey` 和 `SignalInfo` 的细粒度清理机制，并直接操作子进程状态 (`child.free`, `child.is_zombie`)；参考项目则采用更宏观的清理方式，直接清空文件描述符表 (`FD_TABLE.clear`)，并通过 `UserPtr` 和 `axtask::exit` 完成任务退出。这表明双方在进程生命周期管理和资源回收策略上存在根本性架构差异。
    *   **代码溯源链接**:
        - 目标项目: `src/kernel/src/task/ops.rs#do_exit`
        - 参考项目: `api/src/imp/task/exit.rs#do_exit`

*   **【进程管理/信号处理】**
    *   **底层技术析理**: 双方均实现了 `sys_rt_sigprocmask` 系统调用，但访问当前任务信号掩码的数据结构路径完全不同。目标项目通过 `curr.as_thread` 直接访问线程内部结构，并使用 `vm_write` 写入用户态内存；参考项目则通过 `current().task_ext().thread_data().signal.with_blocked_mut` 的长调用链，并借助 `nullable!` 宏处理用户指针。这揭示了双方在任务扩展、线程数据结构以及用户态内存访问抽象上的独立设计。
    *   **代码溯源链接**:
        - 目标项目: `src/kernel/src/syscall/signal.rs#sys_rt_sigprocmask`
        - 参考项目: `api/src/imp/signal.rs#sys_rt_sigprocmask`

*   **【内存管理/内存映射】**
    *   **底层技术析理**: 双方均实现了 `sys_mmap` 系统调用，但内存映射的后端抽象层完全不同。目标项目使用一套丰富的 `Backend` 枚举（`new_alloc`, `new_cow`, `new_file`, `new_linear`）来封装不同的内存来源，体现了策略模式的设计；参考项目则直接使用 `VirtAddr`、`MmapFlags`、`MmapProt` 等底层原语进行映射操作。这表明双方在内存管理模型上，一个采用了高层抽象封装，另一个则更贴近底层硬件原语。
    *   **代码溯源链接**:
        - 目标项目: `src/kernel/src/syscall/mm/mmap.rs#sys_mmap`
        - 参考项目: `api/src/imp/mm/mmap.rs#sys_mmap`

*   **【文件系统/路径解析】**
    *   **底层技术析理**: 双方均实现了 `sys_linkat` 系统调用，但文件系统操作的抽象层不同。目标项目使用 `VfsCredentials` 进行权限检查，并通过 `Path::new` 创建路径对象，体现了独立的 VFS 层设计；参考项目则使用 `HARDLINK_MANAGER` 集中管理硬链接，并通过 `handle_file_path` 辅助函数处理路径。这显示双方在文件系统命名空间管理和操作分发上采用了不同的架构模式。
    *   **代码溯源链接**:
        - 目标项目: `src/kernel/src/syscall/fs/ctl.rs#sys_linkat`
        - 参考项目: `api/src/imp/fs/ctl.rs#sys_linkat`

#### 5. 单方模块与降权项

*   **单方模块**: **设备驱动**、**网络协议栈**、**文件系统/第三方库**、**网络协议栈/第三方库**、**第三方库** 均为参考项目独有模块。目标项目未包含这些模块的实现，这构成了双方在操作系统功能范围上的重大差异，是独立开发的强有力证据。
*   **降权项说明**: 控辩双方材料中大量出现的跨板块同名函数（如将参考项目的 `wait_queue.rs` 与目标项目的 `futex.rs` 配对，或将用户态测试程序 `apps/` 与内核系统调用配对）均不能作为强同源证据。这些配对仅基于函数名或通用语言构造（如 `debug!`, `println!`, `assert!`）的巧合，违反了“同板块”比对原则，其证据价值已被降权处理。

#### 6. 保守结论

综合全模块覆盖矩阵、底层AST指纹分析及核心调用流对比，目标项目与参考项目在宏观架构上存在显著差异。1.62%的极低AST相似度、1227 vs 2610的海量独立指纹，以及进程管理、内存管理、文件系统等核心板块中抽象层次与实现策略的根本性不同，共同排除了系统性代码复用的可能。双方共有的极少数AST指纹，经技术析理，均为Linux标准ABI约束、POSIX规范必然或Rust语言标准惯用法所导致的巧合，不构成抄袭证据。整体风险评级为：**极低**。

------------------------------

