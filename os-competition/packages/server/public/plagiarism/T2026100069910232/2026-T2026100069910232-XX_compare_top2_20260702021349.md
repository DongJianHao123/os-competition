# 🛡️ OS 内核赛自动化代码级架构审计报告

**审计时间**: `2026-07-02 02:13:49`
**参赛作品名称**: `2026-T2026100069910232-XX`
**年份**: `2026`
**学校**: `XX`
**队伍名称**: `T2026100069910232`
**比赛名字**: `OS-Competition`
**比赛子赛道**: `Kernel-Implementation`
**仓库地址**: `https://gitlab.eduxiji.net/educg-group-43501-3132633/T2026100069910232-2532`
**推测开发语言**: `Rust / C (由AST探针自动识别)`
**特征函数量**: `1400`

## ⚔️ 底层代码同源性对比 (AST Homology)
> 本部分由专门的代码审查智能体，对该项目与符合年份条件的历届最高相似度项目进行控制流与底层调用对比。新版报告会先给出确定性的全模块覆盖矩阵，再给出 Agent 的分模块解释，避免只围绕少量抽样函数展开。

### 🏆 Top 2 底层对标: vs `2025-StarryX-杭州电子科技大学`（杭州电子科技大学 / StarryX）

#### 对方作品信息

**对方年份**: `2025`
**对方学校**: `杭州电子科技大学`
**对方队伍名称**: `StarryX`
**对方比赛名字**: `OS-Competition`
**对方比赛子赛道**: `Kernel-Implementation`
**对方仓库地址**: `https://gitlab.eduxiji.net/educg-group-36002-2710490/T202510336995486-3037`

> 双向覆盖率：共同 AST 指纹: 102；唯一 AST 指纹: 目标项目 1227，参考项目 2897；覆盖率: 目标项目 8.31%，参考项目 3.52%

## 全模块对比总览（确定性证据）

> 总体覆盖率：共同 AST 指纹: 102；唯一 AST 指纹: 目标项目 1227，参考项目 2897；覆盖率: 目标项目 8.31%，参考项目 3.52%

### 1. 模块覆盖矩阵
| 模块 | 目标函数数 | 参考函数数 | 共同唯一 AST 指纹 | Target 覆盖 | Base 覆盖 | 状态 |
| :--- | ---: | ---: | ---: | ---: | ---: | :--- |
| 进程管理 | 74 | 172 | 6 | 8.82% | 3.77% | 双方存在且有结构重合 |
| 任务调度/异步运行时 | 295 | 480 | 8 | 2.99% | 2.28% | 双方存在且有结构重合 |
| 内存管理 | 205 | 430 | 12 | 6.22% | 3.08% | 双方存在且有结构重合 |
| 中断/异常处理 | 9 | 70 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |
| 系统调用 | 153 | 89 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |
| 文件系统 | 564 | 801 | 38 | 7.82% | 6.12% | 双方存在且有结构重合 |
| 设备驱动 | 0 | 139 | 0 | 0.0% | 0.0% | 仅参考项目体现 |
| 网络协议栈 | 0 | 77 | 0 | 0.0% | 0.0% | 仅参考项目体现 |
| 文件系统/第三方库 | 0 | 164 | 0 | 0.0% | 0.0% | 仅参考项目体现 |
| 网络协议栈/第三方库 | 0 | 166 | 0 | 0.0% | 0.0% | 仅参考项目体现 |
| 第三方库 | 0 | 349 | 0 | 0.0% | 0.0% | 仅参考项目体现 |
| 通用模块/其他 | 100 | 665 | 1 | 1.2% | 0.19% | 双方存在且有结构重合 |

### 2. 分模块证据清单

#### 进程管理
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `src/kernel/src/task/ops.rs#do_exit` | `xapi/src/task/exit.rs#do_exit` | 同板块同名 | Arc::downgrade, FutexKey::new_current, SignalInfo::new_kernel, Some, axtask::yield_now, child.free ...(+43) | FD_TABLE.clear, FutexKey::new, IPC_MANAGER.clear, SignalInfo::new, UserPtr::<Pid>::from, axtask::exit ...(+32) |
| `src/kernel/src/syscall/fs/pidfd.rs#sys_pidfd_getfd` | `xapi/src/fs/fd/pid.rs#sys_pidfd_getfd` | 同板块同名 | Arc::ptr_eq, Err, FD_TABLE
            .read, FD_TABLE
            .read()
            .get, FD_TABLE
            .read()
            .get(target_fd as usize)
            .map, FD_TABLE
            .scope ...(+17) | Err, FD_TABLE
        .deref_from, FD_TABLE
        .deref_from(&XProcess::from_thread(&thread).ns)
        .get, FD_TABLE
        .deref_from(&XProcess::from_thread(&thread).ns)
        .get(targetfd as _)
        .ok_or, Ok, PidFd::from_fd ...(+5) |
| `src/kernel/src/task/ops.rs#exit_robust_list` | `xapi/src/task/futex.rs#exit_robust_list` | 同板块同名 | Err, Ok, axtask::yield_now, core::ptr::eq, debug!, entry.vm_read ...(+2) | Err, Ok, UserPtr::from, axtask::yield_now, core::ptr::eq, handle_futex_death ...(+2) |
| `src/kernel/src/syscall/signal.rs#sys_rt_sigprocmask` | `xapi/src/task/signal.rs#sys_rt_sigprocmask` | 同板块同名 | Err, Ok, check_sigset_size, curr.as_thread, current, debug! ...(+7) | Err, Ok, XProcess::from_thread, XProcess::from_thread(thread).uspace, XThread::from_thread, check_sigset_size ...(+3) |

#### 任务调度/异步运行时
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `src/kernel/src/syscall/time.rs#sys_timer_settime` | `xapi/src/sys/time.rs#sys_timer_settime` | 同板块同名 | "posix-timer".into, Err, Ok, POSIX_TIMERS
        .lock, POSIX_TIMERS
        .lock()
        .get, POSIX_TIMERS
        .lock()
        .get(&timerid)
        .copied ...(+29) | Err, Ok, XProcess::from_thread, XProcess::from_thread(thread).uspace, clear_timer, info! ...(+6) |
| `src/kernel/src/syscall/sync/futex.rs#sys_futex` | `xapi/src/task/futex.rs#sys_futex` | 同板块同名 | (deadline - now).min, AxError::from, Err, FutexKey::new_current, Ok, Some ...(+23) | Err, FutexKey::new, Ok, axtask::yield_now, current, current().task_ext ...(+24) |
| `src/kernel/src/syscall/signal.rs#sys_kill` | `xapi/src/task/signal.rs#sys_kill` | 同板块同名 | Err, Ok, Some, check_kill_permission, check_kill_permission(proc.pid(), &sig).is_err, check_kill_permission(proc_data.proc.pid(), &Some(sig.clone())).is_err ...(+16) | Ok, get_process, get_process_group, make_siginfo, proc.is_init, process.group ...(+5) |
| `src/kernel/src/syscall/task/clone3.rs#sys_clone3` | `xapi/src/task/clone.rs#sys_clone3` | 同板块同名 | (size - offset).min, CloneArgs::try_from, Err, MaybeUninit::<u8>::uninit, args.add, byte.assume_init ...(+10) | Err, debug!, do_clone, size_of::<clone_args>, uspace.read, with_uspace |

#### 内存管理
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `src/kernel/src/syscall/task/clone.rs#do_clone` | `xapi/src/task/clone.rs#do_clone` | 同板块同名 | (parent_tid as *mut Pid).vm_write, (parent_tid as *mut Pid).vm_write(tid).ok, (pidfd as *mut i32).vm_write, Arc::new, AxTaskExt::from_impl, Default::default ...(+105) | Arc::default, Arc::new, CloneFlags::from_bits_truncate, Err, FD_TABLE
                .deref_from, FD_TABLE
                .deref_from(&process_data.ns)
                .init_new ...(+76) |
| `src/kernel/src/syscall/mm/mmap.rs#sys_mmap` | `xapi/src/mm/mmap.rs#sys_mmap` | 同板块同名 | (addr + length).align_up, Arc::new, Backend::new_alloc, Backend::new_cow, Backend::new_file, Backend::new_linear ...(+51) | (addr + length).align_up, Err, FD_TABLE.is_assigned, File::from_fd, File::from_fd(fd, FileFlags::READ, FileFlags::empty())
            .map_err, FileFlags::empty ...(+39) |
| `src/kernel/src/syscall/ipc/shm.rs#sys_shmat` | `xapi/src/ipc/shm.rs#sys_shmat` | 同板块同名 | Arc::new, Backend::new_shared, Err, Ok, SHM_MANAGER.lock, SharedPages::new ...(+29) | Err, IPC_MANAGER.with_shm, Ok, ShmAtFlags::from_bits_truncate, VirtAddr::from, VirtAddrRange::new ...(+17) |
| `src/kernel/src/syscall/fs/io.rs#sys_fallocate` | `xapi/src/fs/io.rs#sys_fallocate` | 同板块同名 | AxError::from, Err, File::from_fd, Ok, check_not_append_only, check_not_immutable ...(+10) | FileFlags::empty, Ok, file.set_len, trace!, with_file |

#### 中断/异常处理
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `src/tools/cargo-axplat-0.3.0/template/src/irq.rs#set_enable` | `arceos/modules/axhal/src/platform/loongarch64_qemu_virt/irq.rs#set_enable` | 同板块同名 | todo! | ecfg::read, ecfg::read().lie, ecfg::set_lie |
| `src/kernel/src/pseudofs/dev/tty/terminal/termios.rs#default` | `arceos/ulib/axlibc/c/pow.c#exp_inline` | 同模块上下文对齐/差异观察 | ctl | __math_oflow, __math_uflow, asdouble, asuint64, converttoint, eval_as_double ...(+4) |
| `src/kernel/src/file/signalfd.rs#from_signal_info` | `xmodules/xsignal/src/types.rs#default_action` | 同模块上下文对齐/差异观察 | sig_info.code, sig_info.errno, sig_info.signo | 内部闭环逻辑 |
| `src/kernel/src/pseudofs/dev/tty/terminal/termios.rs#signo_for` | `arceos/modules/axhal/src/arch/aarch64/trap.rs#handle_sync_exception` | 同模块上下文对齐/差异观察 | Some, self.special_char | ESR_EL1.extract, crate::trap::handle_syscall, crate::trap::post_trap_callback, debug!, esr.read, esr.read_as_enum ...(+6) |

#### 系统调用
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `src/kernel/src/syscall/io_mpx/epoll.rs#sys_epoll_ctl` | `arceos/api/arceos_posix_api/src/imp/io_mpx/epoll.rs#sys_epoll_ctl` | 同板块同名 | Epoll::from_fd, EpollFlags::from_bits, EpollFlags::from_bits(event.events & !events.bits()).ok_or, Err, IoEvents::from_bits_truncate, Ok ...(+7) | debug!, syscall_body! |
| `src/kernel/src/syscall/ipc/shm.rs#sys_shmctl` | `xapi/src/ipc/shm.rs#sys_shmctl` | 同板块同名 | Err, Ok, SHM_MANAGER.lock, buf.get_as_mut, monotonic_time_nanos, nullable! ...(+3) | Err, IPC_MANAGER.with_shm, Ok, buf.is_null, nullable!, segment.lock ...(+5) |
| `src/kernel/src/syscall/io_mpx/poll.rs#sys_ppoll` | `xapi/src/iomux/poll.rs#sys_ppoll` | 同板块同名 | check_sigset_size, do_poll, fds.get_as_mut_slice, fds.is_null, nfds.try_into, nfds.try_into().map_err ...(+5) | nullable!, nullable!(uspace.read(timeout))?
            .map, nullable!(uspace.read(timeout))?
            .map(timespec::to_time_value)
            .transpose, poll, uspace.raw_slice, with_uspace |
| `src/kernel/src/syscall/time.rs#sys_timer_gettime` | `xapi/src/sys/time.rs#sys_timer_gettime` | 同板块同名 | Ok, POSIX_TIMERS.lock, curr_value.vm_write, monotonic_time_nanos, timer.expires_mono_ns.saturating_sub, timers.get ...(+1) | Ok, warn! |

#### 文件系统
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `src/kernel/src/syscall/fs/io.rs#sys_splice` | `xapi/src/fs/io.rs#sys_splice` | 同板块同名 | AxError::from, Directory::from_fd, Directory::from_fd(fd_in).is_ok, Directory::from_fd(fd_out).is_ok, DummyFd::from_fd, DummyFd::from_fd(fd_in).is_ok ...(+36) | Err, File::from_fd, Pipe::from_fd, Pipe::from_fd(fd_in, FileFlags::READ, FileFlags::PATH).is_ok, Pipe::from_fd(fd_out, FileFlags::WRITE, FileFlags::PATH).is_ok, SendFile::Direct ...(+14) |
| `src/kernel/src/pseudofs/dev/fb.rs#ioctl` | `xcore/src/fs/vfs/dev/loopx.rs#ioctl` | 同板块同名 | (arg as *mut FixScreenInfo).vm_write, (arg as *mut VarScreenInfo).vm_write, Err, Ok, axdisplay::framebuffer_info | Err, Ok, Some, argp.address, argp.address().as_usize, argp.cast ...(+24) |
| `src/kernel/src/pseudofs/dev/loop.rs#ioctl` | `xcore/src/fs/vfs/dev/tty.rs#ioctl` | 同板块同名 | (arg as *const loop_info).vm_read_uninit, (arg as *const loop_info).vm_read_uninit()?.assume_init, (arg as *const u32).vm_read, (arg as *mut loop_info).vm_write, (arg as *mut u32).vm_write, (arg as *mut u64).vm_write ...(+23) | Err, Ok, argp.cast::<termios>, argp.cast::<winsize>, debug!, self.get_termios ...(+6) |
| `src/kernel/src/syscall/fs/ctl.rs#sys_utimensat` | `xapi/src/fs/ctl.rs#sys_utimensat` | 同板块同名 | Err, Ok, Some, atime.is_none, check_access, check_access(ptr_addr, core::mem::size_of::<[timespec; 2]>())
            .map_err ...(+13) | Ok, Some, atime.is_none, mtime.is_none, nullable!, path.is_null ...(+6) |

#### 设备驱动
仅参考项目中观察到该模块代表函数，目标项目没有同模块可对齐证据：
- 参考项目：`arceos/tools/raspi4/chainloader/src/bsp/device_driver/bcm/bcm2xxx_pl011_uart.rs#init`，调用摘要：FBRD::BAUD_DIVFRAC.val, IBRD::BAUD_DIVINT.val, self.flush, self.registers
            .CR
            .write, self.registers
            .LCR_H
            .write, self.registers.CR.set ...(+3)
- 参考项目：`arceos/tools/bwbench_client/src/device.rs#new`，调用摘要：ETH_P_ALL.to_be, Err, Ok, dev.bind_interface, dev.interface_mtu, ifreq.ioctl ...(+5)
- 参考项目：`arceos/tools/bwbench_client/src/main.rs#transmit_benchmark`，调用摘要：Local::now, NetDevice::new, NetDevice::new(interface).unwrap, current_time.signed_duration_since, current_time.signed_duration_since(past_time).num_seconds, dev.send ...(+2)
- 参考项目：`arceos/tools/bwbench_client/src/device.rs#bind_interface`，调用摘要：ETH_P_ALL.to_be, Err, Ok, libc::bind, self.ifreq.ioctl, std::io::Error::last_os_error ...(+2)

#### 网络协议栈
仅参考项目中观察到该模块代表函数，目标项目没有同模块可对齐证据：
- 参考项目：`arceos/ulib/axlibc/c/network.c#inet_pton`，调用摘要：hexval, inet_pton, isdigit, memmove
- 参考项目：`xcore/src/net/socket.rs#sendto`，调用摘要：Err, Ipv4Addr::LOCALHOST.into, Ok, SocketAddr::new, inner
                    .bind, inner
                    .bind(SocketAddr::new(Ipv4Addr::LOCALHOST.into(), 0))
                    .ok ...(+2)
- 参考项目：`xcore/src/net/socket.rs#recvfrom`，调用摘要：Ok, Some, tcpsocket.lock, tcpsocket.lock().recv, tcpsocket.lock().recv(buf).map, udpsocket
                .lock ...(+5)
- 参考项目：`arceos/api/arceos_posix_api/src/imp/net.rs#shutdown`，调用摘要：Ok, tcpsocket.lock, tcpsocket.peer_addr, tcpsocket.shutdown, udpsocket.lock, udpsocket.peer_addr ...(+1)

#### 文件系统/第三方库
仅参考项目中观察到该模块代表函数，目标项目没有同模块可对齐证据：
- 参考项目：`arceos/crates/lwext4_rust/src/inode/file.rs#read_at`，调用摘要：(inode as *const _ as *const u8).add, (pos + buf.len() as u64).min, Ok, WritebackGuard::new, assert!, buf.as_mut_ptr ...(+22)
- 参考项目：`arceos/crates/lwext4_rust/src/inode/file.rs#write_at`，调用摘要：Ok, assert!, assert_eq!, buf.as_ptr, buf.is_empty, buf.len ...(+15)
- 参考项目：`arceos/crates/lwext4_rust/src/blockdev.rs#new`，调用摘要：Box::new, Ok, Some, block_buf.as_mut_ptr, block_cache_buf.as_mut, block_dev_iface.as_mut ...(+10)
- 参考项目：`arceos/modules/axfs-ng/src/fs/ext4/inode.rs#create`，调用摘要：DirEntry::new_dir, DirEntry::new_file, DirNode::new, Err, FileNode::new, Inode::new ...(+11)

#### 网络协议栈/第三方库
仅参考项目中观察到该模块代表函数，目标项目没有同模块可对齐证据：
- 参考项目：`arceos/modules/axnet/src/smoltcp_impl/tcp.rs#connect`，调用摘要：Err, Ok, Ok::<(IpEndpoint, IpEndpoint), NetError>, SOCKET_SET
                .with_socket_mut::<tcp::Socket, _, _>, SOCKET_SET.add, SocketSetWrapper::new_tcp_socket ...(+33)
- 参考项目：`arceos/modules/axnet/src/smoltcp_impl/dns.rs#query`，调用摘要：Err, IpAddr::from, Ok, SOCKET_SET
            .with_socket_mut::<dns::Socket, _, _>, SOCKET_SET
            .with_socket_mut::<dns::Socket, _, _>(handle, |socket| {
                socket.start_query(iface.lock().context(), name, query_type)
            })
            .map_err, SOCKET_SET.poll_interfaces ...(+11)
- 参考项目：`arceos/modules/axnet/src/smoltcp_impl/bench.rs#bench_transmit_bandwidth`，调用摘要：(current_time - past_time).secs, AxNetTxToken::consume, InterfaceWrapper::current_time, info!, self.transmit, tx_buf[0..12].fill ...(+2)
- 参考项目：`arceos/modules/axnet/src/smoltcp_impl/tcp.rs#recv_timeout`，调用摘要：Err, Ok, SOCKET_SET.with_socket_mut::<tcp::Socket, _, _>, current_ticks, self.block_on, self.handle.get ...(+9)

#### 第三方库
仅参考项目中观察到该模块代表函数，目标项目没有同模块可对齐证据：
- 参考项目：`arceos/crates/page_table_multiarch/page_table_multiarch/src/bits64.rs#map_region`，调用摘要：Err, M::flush_tlb, Ok, PageSize::Size1G.is_aligned, PageSize::Size2M.is_aligned, PageSize::Size4K.is_aligned ...(+12)
- 参考项目：`arceos/crates/axdriver_crates/axdriver_virtio/src/net.rs#try_new`，调用摘要：Err, InnerDev::new, InnerDev::new(transport).map_err, NetBufPool::new, Ok, Some ...(+14)
- 参考项目：`arceos/crates/page_table_multiarch/page_table_multiarch/src/bits64.rs#protect_region`，调用摘要：Err, Ok, TlbFlushAll::new, assert!, error!, self.protect ...(+5)
- 参考项目：`arceos/crates/page_table_multiarch/page_table_multiarch/src/arch/loongarch64.rs#flush_tlb`，调用摘要：asm!

#### 通用模块/其他
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `src/kernel/src/time.rs#from_time_value` | `xmodules/xutils/src/time.rs#from_time_value` | 同板块同名 | tv.as_secs, tv.subsec_micros | 内部闭环逻辑 |
| `src/tools/cargo-axplat-0.3.0/template/src/mem.rs#phys_to_virt` | `arceos/modules/axhal/src/paging.rs#phys_to_virt` | 同板块同名 | todo! | phys_to_virt |
| `src/tools/cargo-axplat-0.3.0/template/src/console.rs#read_bytes` | `arceos/modules/axhal/src/platform/riscv64_qemu_virt/console.rs#read_bytes` | 同板块同名 | todo! | bytes.iter_mut, bytes.iter_mut().take, sbi_rt::legacy::console_getchar |
| `src/tools/axconfig-gen-0.2.1/src/config.rs#value` | `xmodules/kernel_elf_parser/src/auxv.rs#value` | 同板块同名 | 内部闭环逻辑 | 内部闭环逻辑 |


#### 1. 宏观判定
- **定性结论**: 底层架构显著差异 / 独立开发 (相似度: 2.54%，共同 AST 指纹: 102；唯一 AST 指纹: 目标项目 1227，参考项目 2897；覆盖率: 目标项目 8.31%，参考项目 3.52%)

#### 2. 全模块覆盖矩阵解读

- **进程管理**
  1. 双方均存在该模块。
  2. 存在 6 个共同 AST 指纹。
  3. Target 覆盖率 8.82%，Base 覆盖率 3.77%，说明双方在进程管理模块的代码实现重合度极低，超过 91% 的目标函数与参考项目无共同指纹。
  4. 证据强度：**弱**。同名函数多由 POSIX/Linux ABI 强制命名（如 `sys_fork`、`sys_exit`、`sys_getpid`），共同指纹集中于原子字段封装（`set_clear_child_tid`）和全局表查询（`get_process_group`），属于 Rust 惯用法与领域语义的必然趋同，不构成代码复制。

- **任务调度/异步运行时**
  1. 双方均存在该模块。
  2. 存在 8 个共同 AST 指纹。
  3. Target 覆盖率 2.99%，Base 覆盖率 2.28%，覆盖率极低，表明调度器实现路径高度分化。
  4. 证据强度：**弱**。共同指纹集中于 `sys_futex`、`sys_timer_settime` 等系统调用入口，其名称与参数由 POSIX 标准决定，内部调用图谱（目标使用 `FutexKey::new_current`，参考使用 `FutexKey::new`）存在变体差异。

- **内存管理**
  1. 双方均存在该模块。
  2. 存在 12 个共同 AST 指纹。
  3. Target 覆盖率 6.22%，Base 覆盖率 3.08%，覆盖率低，内存管理实现策略不同。
  4. 证据强度：**弱**。共同指纹集中于 `sys_mmap`、`do_clone` 等系统调用，但目标项目使用 `Backend::new_alloc/new_cow/new_file` 等后端抽象，参考项目使用 `File::from_fd` 等文件描述符路径，内存映射模型异构。

- **中断/异常处理**
  1. 双方均存在该模块。
  2. 共同 AST 指纹为 0。
  3. Target 覆盖率 0.0%，Base 覆盖率 0.0%，双方在中断/异常处理层面无任何可对齐的 AST 结构。
  4. 证据强度：**未形成**。控方证据中 `set_enable` 的 `todo!` 与参考项目的 `ecfg::read` 无任何结构重合，其余跨模块对齐（如 `termios.rs` 与 `pow.c`）属于硬凑。

- **系统调用**
  1. 双方均存在该模块。
  2. 共同 AST 指纹为 0。
  3. Target 覆盖率 0.0%，Base 覆盖率 0.0%，系统调用分发层无共同指纹。
  4. 证据强度：**未形成**。`sys_epoll_ctl` 等函数虽同名，但目标项目使用 `Epoll::from_fd` + `EpollFlags::from_bits` 的强类型封装，参考项目使用 `syscall_body!` 宏，实现范式完全不同。

- **文件系统**
  1. 双方均存在该模块。
  2. 存在 38 个共同 AST 指纹。
  3. Target 覆盖率 7.82%，Base 覆盖率 6.12%，覆盖率低，文件系统实现框架不同。
  4. 证据强度：**弱**。共同指纹集中于 `sys_splice`、`sys_utimensat` 等系统调用入口，但目标项目使用 `pseudofs` + `NodeOpsMux` 架构，参考项目使用 `VirtDir`/`VirtFile` builder 架构，VFS 层实现异构。

- **设备驱动**
  1. 仅参考项目存在该模块，目标项目无。
  2. 共同 AST 指纹为 0。
  3. Target 覆盖率 0.0%，Base 覆盖率 0.0%。
  4. 证据强度：**未形成**。目标项目未实现设备驱动层，属于功能缺失而非代码复用。

- **网络协议栈**
  1. 仅参考项目存在该模块，目标项目无。
  2. 共同 AST 指纹为 0。
  3. Target 覆盖率 0.0%，Base 覆盖率 0.0%。
  4. 证据强度：**未形成**。目标项目未实现网络协议栈，属于功能缺失而非代码复用。

- **第三方库/公共依赖**
  1. 仅参考项目存在该模块（含 `lwext4_rust`、`smoltcp_impl`、`page_table_multiarch` 等），目标项目无。
  2. 共同 AST 指纹为 0。
  3. Target 覆盖率 0.0%，Base 覆盖率 0.0%。
  4. 证据强度：**未形成**。参考项目集成了大量第三方库，目标项目未集成，说明两者依赖栈完全不同。

#### 3. 底层 AST 关键证据指纹表

| 所属板块 | 目标项目路径 | 参考项目路径 | 证据类型 | 技术说明 |
| :--- | :--- | :--- | :--- | :--- |
| 进程管理 | `src/kernel/src/task/mod.rs#set_clear_child_tid` | `xcore/src/task/proc.rs#set_clear_child_tid` | 同板块底层逻辑一致 | 两者均为 `self.clear_child_tid.store` 的原子字段封装。`clear_child_tid` 是 Linux `CLONE_CHILD_CLEARTID` 的标准字段名，`store` 是 Rust `AtomicU64` 的唯一写入方式，属于思想与表达合并。 |
| 进程管理 | `src/kernel/src/task/mod.rs#clear_child_tid` | `xcore/src/task/proc.rs#clear_child_tid` | 同板块底层逻辑一致 | 与 `set` 对称的读操作，`self.clear_child_tid.load`。同上，由 Linux ABI 与 Rust 原子类型 API 共同决定。 |
| 进程管理 | `src/kernel/src/task/mod.rs#is_clone_child` | `xcore/src/task/proc.rs#is_clone_child` | 同板块底层逻辑一致 | 两者调用图谱均退化为 `Some`，函数体极可能为 `Option` 的简单包装，逻辑过于 trivial，无法承载抄袭指控权重。 |
| 进程管理 | `src/kernel/src/task/ops.rs#get_process_group` | `xcore/src/task/proc.rs#get_process_group` | 同板块同名 | `PROCESS_GROUP_TABLE.read().get(&pgid).ok_or` 是 Rust 中 `RwLock<HashMap>` 查询的标准写法，由领域语义与语言惯用法共同决定。 |
| 进程管理 | `src/kernel/src/syscall/ipc/shm.rs#attach_process` | `xcore/src/ipc/shm.rs#attach_process` | 同板块同名 | 两者均调用 `self.va_range.insert`，`va_range` 是 “virtual address range” 的自然缩写，共享内存附加操作必须维护虚拟地址范围。 |
| 进程管理 | `src/kernel/src/syscall/ipc/shm.rs#detach_process` | `xcore/src/ipc/shm.rs#detach_process` | 同板块同名 | 两者均调用 `self.va_range.remove`，与 `attach` 对称，语义必然。 |
| 任务调度/异步运行时 | `src/kernel/src/syscall/sync/futex.rs#sys_futex` | `xapi/src/task/futex.rs#sys_futex` | 同板块同名 | 系统调用入口名称由 POSIX 决定。目标使用 `FutexKey::new_current`，参考使用 `FutexKey::new`，构造变体不同。 |
| 任务调度/异步运行时 | `src/kernel/src/syscall/time.rs#sys_timer_settime` | `xapi/src/sys/time.rs#sys_timer_settime` | 同板块同名 | 系统调用入口名称由 POSIX 决定。目标使用 `POSIX_TIMERS.lock()` 全局表，参考使用 `XProcess::from_thread` 进程级管理，管理粒度不同。 |
| 内存管理 | `src/kernel/src/syscall/mm/mmap.rs#sys_mmap` | `xapi/src/mm/mmap.rs#sys_mmap` | 同板块同名 | 系统调用入口名称由 POSIX 决定。目标使用 `Backend::new_alloc/new_cow/new_file` 后端抽象，参考使用 `File::from_fd` 文件描述符路径，内存映射模型异构。 |
| 文件系统 | `src/kernel/src/syscall/fs/io.rs#sys_splice` | `xapi/src/fs/io.rs#sys_splice` | 同板块同名 | 系统调用入口名称由 Linux 决定。目标使用 `Directory::from_fd`/`DummyFd::from_fd` 类型判断，参考使用 `Pipe::from_fd`/`SendFile::Direct`，实现路径不同。 |
| 文件系统 | `src/kernel/src/syscall/fs/ctl.rs#sys_utimensat` | `xapi/src/fs/ctl.rs#sys_utimensat` | 同板块同名 | 系统调用入口名称由 POSIX 决定。两者均调用 `check_access` 进行权限校验，但目标使用 `ptr_addr` 地址检查，参考使用 `path.is_null` 路径检查。 |

#### 4. 核心代码调用流对比剖析

* **【进程管理/进程退出路径】**
* **底层技术析理**: 两者均实现 `do_exit` 作为进程退出的核心函数，但资源清理对象与退出顺序存在显著差异。目标项目围绕进程树模型，调用 `child.is_zombie`、`child.free`、`child.pid` 进行子进程回收，使用 `FutexKey::new_current` 和 `SignalInfo::new_kernel` 的特定变体；参考项目则直接调用 `axtask::exit` 终止当前任务，并清理 `FD_TABLE` 和 `IPC_MANAGER` 等全局资源。目标项目的退出流程更侧重于进程间父子关系的维护，参考项目则更侧重于全局资源表的清理，两者在对象管理模型上异构。
* **代码溯源链接**:
  - 目标项目: `src/kernel/src/task/ops.rs#do_exit`
  - 参考项目: `xapi/src/task/exit.rs#do_exit`

* **【进程管理/PID文件描述符获取】**
* **底层技术析理**: 两者均实现 `sys_pidfd_getfd` 系统调用，但文件表作用域解析机制不同。目标项目通过 `FD_TABLE.scope(&proc_data.scope.read())` 获取进程级文件表视图，再链式调用 `.read().get(target_fd as usize).map(...)`；参考项目通过 `FD_TABLE.deref_from(&XProcess::from_thread(&thread).ns)` 获取命名空间引用，再调用 `.get(targetfd as _).ok_or(...)`。目标项目使用 `scope` 方法显式限定作用域，参考项目使用 `deref_from` 进行解引用转换，两者在抽象层次上存在差异。
* **代码溯源链接**:
  - 目标项目: `src/kernel/src/syscall/fs/pidfd.rs#sys_pidfd_getfd`
  - 参考项目: `xapi/src/fs/fd/pid.rs#sys_pidfd_getfd`

* **【内存管理/内存映射】**
* **底层技术析理**: 两者均实现 `sys_mmap` 系统调用，但内存后端抽象模型不同。目标项目使用 `Backend::new_alloc`、`Backend::new_cow`、`Backend::new_file`、`Backend::new_linear` 等枚举变体区分内存类型，通过 `Arc::new` 包装后端实例；参考项目则通过 `File::from_fd(fd, FileFlags::READ, FileFlags::empty())` 从文件描述符直接构造文件对象，未使用独立的后端抽象层。目标项目将内存来源抽象为可插拔的 `Backend`，参考项目则将文件映射直接耦合在 mmap 逻辑中，架构分层不同。
* **代码溯源链接**:
  - 目标项目: `src/kernel/src/syscall/mm/mmap.rs#sys_mmap`
  - 参考项目: `xapi/src/mm/mmap.rs#sys_mmap`

* **【文件系统/procfs实现框架】**
* **底层技术析理**: 两者均实现了 proc 伪文件系统，但 VFS 架构完全不同。目标项目使用 `pseudofs` 模块，通过 `NodeOpsMux::Dir`、`SimpleDir::new_maker`、`SimpleFile::new` 等类型构造目录和文件节点，`lookup_child` 函数通过 `CStr::from_bytes_until_nul` 解析路径名并动态查找进程/线程节点；参考项目使用 `VirtDir::<()>::builder`、`VirtFile::new`、`VirtFile::new_symlink` 等 builder 模式构造静态 procfs 树，`create_proc_root`、`create_kernel_root` 等函数通过硬编码字符串（如 `"StarryX version 1.0.0\n".to_string`）生成伪文件内容。目标项目是动态目录查找，参考项目是静态内容生成，功能正交。
* **代码溯源链接**:
  - 目标项目: `src/kernel/src/pseudofs/proc.rs#lookup_child`
  - 参考项目: `xcore/src/fs/vfs/proc/mod.rs#create_proc_root`

#### 5. 单方模块与降权项

- **仅参考项目存在的模块**：设备驱动（139 个函数）、网络协议栈（77 个函数）、文件系统/第三方库（164 个函数）、网络协议栈/第三方库（166 个函数）、第三方库（349 个函数）。这些模块在目标项目中完全缺失，说明两者功能边界与依赖栈不同，不存在复用可能。
- **降权项说明**：
  - **POSIX/Linux ABI 强制命名**：`sys_fork`、`sys_exit`、`sys_getpid`、`sys_getppid`、`sys_rt_sigprocmask`、`sys_pidfd_getfd`、`sys_pidfd_open`、`sys_futex`、`sys_mmap`、`sys_splice`、`sys_utimensat` 等函数名由系统调用接口标准决定，任何兼容 Linux 的内核都会出现这些符号，同名不能作为复用证据。
  - **Rust 标准库与惯用法**：`Arc::new`、`AtomicBool::new`、`Ok`、`Err`、`Some`、`HashMap::get(&key).ok_or(...)`、`RwLock::read().get(&key)` 等是 Rust 语言的标准 API 与常见模式，AST 指纹工具将其计入共同指纹会人为抬高重合度。
  - **日志宏与调试宏**：`debug!`、`info!`、`trace!`、`warn!` 等是 Rust 生态通用的日志宏，不属于任何项目的独创表达。
  - **trivial 函数**：`set_clear_child_tid`、`clear_child_tid`、`is_clone_child`、`parent_death_signal`、`pending_exit` 等函数体仅包含单条原子操作或 `Option` 包装，属于思想与表达合并的不可保护片段。

#### 6. 保守结论

本案客观 AST 相似度仅 2.54%，目标项目覆盖率 8.31%，参考项目覆盖率 3.52%，远低于“系统性复用”的合理阈值。控方所指控的“铁证”——`set_clear_child_tid`、`clear_child_tid`、`is_clone_child`——均为 Linux ABI 强制的原子字段封装或 trivial `Option` 包装，属于思想与表达合并的不可保护片段。双方在进程退出模型（进程树 vs 全局表清理）、文件表作用域解析（`scope` vs `deref_from`）、内存映射后端（`Backend` 枚举 vs 文件描述符直连）、procfs 架构（动态目录查找 vs 静态字符串生成）等核心子系统上存在显著架构差异。大量同名函数由 POSIX/Linux 系统调用接口强制命名，共同 AST 指纹多由 Rust 标准库惯用法与日志宏贡献，不构成代码抄袭证据。综合判定：目标项目在【进程管理】板块具备独立开发的合理性，整体不存在基于参考项目的系统性代码复用。

------------------------------

