# 🛡️ OS 内核赛自动化代码级架构审计报告

**审计时间**: `2026-07-02 02:13:49`
**参赛作品名称**: `2026-T2026100069910232-XX`
**年份**: `2026`
**学校**: `XX`
**队伍名称**: `T2026100069910232`
**比赛名字**: `OS-Competition`
**比赛子赛道**: `Kernel-Implementation`
**仓库地址**: `https://gitlab.eduxiji.net/educg-group-43501-3132633/T2026100069910232-2532`
**推测开发语言**: `Rust / C (由AST探针自动识别)`
**特征函数量**: `1400`

## ⚔️ 底层代码同源性对比 (AST Homology)
> 本部分由专门的代码审查智能体，对该项目与符合年份条件的历届最高相似度项目进行控制流与底层调用对比。新版报告会先给出确定性的全模块覆盖矩阵，再给出 Agent 的分模块解释，避免只围绕少量抽样函数展开。

### 🏆 Top 3 底层对标: vs `2025-狗剩-海南大学`（海南大学 / 狗剩）

#### 对方作品信息

**对方年份**: `2025`
**对方学校**: `海南大学`
**对方队伍名称**: `狗剩`
**对方比赛名字**: `OS-Competition`
**对方比赛子赛道**: `Kernel-Implementation`
**对方仓库地址**: `https://gitlab.eduxiji.net/educg-group-36002-2710490/T202510589995148-2209`

> 双向覆盖率：共同 AST 指纹: 98；唯一 AST 指纹: 目标项目 1227，参考项目 3020；覆盖率: 目标项目 7.99%，参考项目 3.25%

## 全模块对比总览（确定性证据）

> 总体覆盖率：共同 AST 指纹: 98；唯一 AST 指纹: 目标项目 1227，参考项目 3020；覆盖率: 目标项目 7.99%，参考项目 3.25%

### 1. 模块覆盖矩阵
| 模块 | 目标函数数 | 参考函数数 | 共同唯一 AST 指纹 | Target 覆盖 | Base 覆盖 | 状态 |
| :--- | ---: | ---: | ---: | ---: | ---: | :--- |
| 进程管理 | 74 | 93 | 3 | 4.41% | 3.57% | 双方存在且有结构重合 |
| 任务调度/异步运行时 | 295 | 399 | 2 | 0.75% | 0.69% | 双方存在且有结构重合 |
| 内存管理 | 205 | 326 | 8 | 4.15% | 2.75% | 双方存在且有结构重合 |
| 中断/异常处理 | 9 | 62 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |
| 系统调用 | 153 | 153 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |
| 文件系统 | 564 | 747 | 49 | 10.08% | 8.66% | 双方存在且有结构重合 |
| 设备驱动 | 0 | 142 | 0 | 0.0% | 0.0% | 仅参考项目体现 |
| 网络协议栈 | 0 | 77 | 0 | 0.0% | 0.0% | 仅参考项目体现 |
| 文件系统/第三方库 | 0 | 689 | 0 | 0.0% | 0.0% | 仅参考项目体现 |
| 网络协议栈/第三方库 | 0 | 107 | 0 | 0.0% | 0.0% | 仅参考项目体现 |
| 第三方库 | 0 | 386 | 0 | 0.0% | 0.0% | 仅参考项目体现 |
| 通用模块/其他 | 100 | 665 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |

### 2. 分模块证据清单

#### 进程管理
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `src/kernel/src/task/ops.rs#do_exit` | `api/src/imp/task.rs#do_exit` | 同板块同名 | Arc::downgrade, FutexKey::new_current, SignalInfo::new_kernel, Some, axtask::yield_now, child.free ...(+43) | Layout::new::<Tid>, SignalInfo::new, UserPtr::<Tid>::from, axtask::exit, axtask::yield_now, clear_child_tid.get_as ...(+18) |
| `src/kernel/src/syscall/signal.rs#sys_rt_sigprocmask` | `api/src/imp/signal.rs#sys_rt_sigprocmask` | 同板块同名 | Err, Ok, check_sigset_size, curr.as_thread, current, debug! ...(+7) | Err, Ok, check_sigset, current, current()
        .task_ext, current()
        .task_ext()
        .thread
        .signal
        .with_blocked_mut::<LinuxResult<_>> ...(+2) |
| `src/kernel/src/syscall/ipc/shm.rs#clear_proc_shm` | `api/src/imp/ipc/shm.rs#clear_proc_shm` | 同板块同名 | self.get_inner_by_shmid, self.get_shmids_by_pid, self.remove_pid, self.remove_shmid, shm_inner.attach_count, shm_inner.detach_process ...(+1) | SHM_MANAGER.lock, shm_inner.detach_process, shm_inner.lock, shm_manager.get_inner_by_shmid, shm_manager.get_inner_by_shmid(shmid).unwrap, shm_manager.get_shmids_by_pid ...(+1) |
| `src/kernel/src/syscall/ipc/shm.rs#attach_process` | `api/src/imp/ipc/shm.rs#attach_process` | 同板块同名 | monotonic_time_nanos, self.va_range.insert | assert!, self.va_range.insert |

#### 任务调度/异步运行时
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `src/kernel/src/syscall/sync/futex.rs#sys_futex` | `api/src/imp/futex.rs#sys_futex` | 同板块同名 | (deadline - now).min, AxError::from, Err, FutexKey::new_current, Ok, Some ...(+23) | Err, Ok, TimeValue::new, axtask::yield_now, futex_table.get, futex_table.get_or_insert ...(+14) |
| `src/kernel/src/syscall/signal.rs#sys_kill` | `api/src/imp/signal.rs#sys_kill` | 同板块同名 | Err, Ok, Some, check_kill_permission, check_kill_permission(proc.pid(), &sig).is_err, check_kill_permission(proc_data.proc.pid(), &Some(sig.clone())).is_err ...(+16) | Err, Ok, current, find_process, get_all_processes, make_siginfo ...(+3) |
| `src/kernel/src/pseudofs/mod.rs#mount_all` | `core/src/vfs/mod.rs#mount_all` | 同板块同名 | FS_CONTEXT.lock, Ok, Path::new, Path::new("/sys/class/graphics/fb0/device").components, PathBuf::new, comp.as_str ...(+16) | Ok, dev::new_devfs, mount_at, proc::new_procfs, tmp::MemoryFs::new |
| `src/kernel/src/syscall/fs/ctl.rs#sys_getcwd` | `api/src/imp/fd/ctl.rs#sys_getcwd` | 同板块同名 | CString::new, CString::new(cwd_path.as_str()).map_err, Err, FS_CONTEXT.lock, Ok, cur.absolute_path ...(+9) | CString::new, CString::new(cwd.as_str()).map_err, Err, FS_CONTEXT.lock, FS_CONTEXT.lock().current_dir, FS_CONTEXT.lock().current_dir().absolute_path ...(+10) |

#### 内存管理
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `src/kernel/src/syscall/mm/mmap.rs#sys_mmap` | `src/syscall_imp/mm/mmap.rs#sys_mmap` | 同板块同名 | (addr + length).align_up, Arc::new, Backend::new_alloc, Backend::new_cow, Backend::new_file, Backend::new_linear ...(+51) | (addr + length).align_up, Err, MmapFlags::MAP_PRIVATE.bits, MmapFlags::MAP_SHARED.bits, MmapFlags::MAP_SHARED_VALIDATE.bits, MmapFlags::from_bits_truncate ...(+32) |
| `src/kernel/src/mm/loader.rs#load_user_app` | `core/src/mm.rs#load_user_app` | 同板块同名 | (ustack_top - user_sp_aligned).align_up_4k, Backend::new_alloc, ELF_LOADER.lock, ELF_LOADER.lock().load, Err, FS_CONTEXT.lock ...(+54) | CStr::from_bytes_with_nul, CStr::from_bytes_with_nul(interp)
                    .ok, CStr::from_bytes_with_nul(interp)
                    .ok()
                    .and_then, CStr::from_bytes_with_nul(interp)
                    .ok()
                    .and_then(|it| it.to_str().ok())
                    .ok_or, ElfFile::new, ElfFile::new(&file_data).map_err ...(+53) |
| `src/kernel/src/syscall/ipc/shm.rs#sys_shmat` | `api/src/imp/ipc/shm.rs#sys_shmat` | 同板块同名 | Arc::new, Backend::new_shared, Err, Ok, SHM_MANAGER.lock, SharedPages::new ...(+29) | Err, MappingFlags::all, Ok, SHM_MANAGER.lock, ShmAtFlags::from_bits_truncate, VirtAddr::from ...(+26) |
| `src/kernel/src/syscall/fs/ctl.rs#sys_ioctl` | `arceos/api/arceos_posix_api/src/imp/io.rs#sys_ioctl` | 同板块同名 | (arg as *const u32).vm_read, (arg as *const u8).vm_read, (arg as *mut u32).vm_write, Err, Ok, VfsCredentials::effective ...(+18) | debug!, syscall_body! |

#### 中断/异常处理
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `src/tools/cargo-axplat-0.3.0/template/src/irq.rs#set_enable` | `arceos/modules/axhal/src/platform/loongarch64_2k1000la/irq.rs#set_enable` | 同板块同名 | todo! | ecfg::read, ecfg::read().lie, ecfg::set_lie |
| `src/kernel/src/pseudofs/dev/tty/terminal/termios.rs#default` | `arceos/ulib/axlibc/c/pow.c#exp_inline` | 同模块上下文对齐/差异观察 | ctl | __math_oflow, __math_uflow, asdouble, asuint64, converttoint, eval_as_double ...(+4) |
| `src/kernel/src/file/signalfd.rs#from_signal_info` | `arceos/modules/axhal/src/arch/aarch64/trap.rs#handle_sync_exception` | 同模块上下文对齐/差异观察 | sig_info.code, sig_info.errno, sig_info.signo | ESR_EL1.extract, crate::trap::handle_syscall, crate::trap::post_trap_callback, debug!, esr.read, esr.read_as_enum ...(+6) |
| `src/kernel/src/pseudofs/dev/tty/terminal/termios.rs#signo_for` | `arceos/modules/axhal/src/arch/x86_64/idt.rs#new` | 同模块上下文对齐/差异观察 | Some, self.special_char | InterruptDescriptorTable::new, core::mem::transmute, core::slice::from_raw_parts_mut, entries[i].set_handler_fn, opt.set_privilege_level |

#### 系统调用
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `src/kernel/src/syscall/io_mpx/epoll.rs#sys_epoll_ctl` | `api/src/imp/io_mpx/epoll.rs#sys_epoll_ctl` | 同板块同名 | Epoll::from_fd, EpollFlags::from_bits, EpollFlags::from_bits(event.events & !events.bits()).ok_or, Err, IoEvents::from_bits_truncate, Ok ...(+7) | EpollInstance::from_fd, EpollInstance::from_fd(epfd)?.control, Err, Ok, debug!, event.get_as_mut |
| `src/kernel/src/syscall/ipc/shm.rs#sys_shmctl` | `api/src/imp/ipc/shm.rs#sys_shmctl` | 同板块同名 | Err, Ok, SHM_MANAGER.lock, buf.get_as_mut, monotonic_time_nanos, nullable! ...(+3) | Err, Ok, SHM_MANAGER.lock, shm_inner.lock, shm_manager
            .get_inner_by_shmid, shm_manager
            .get_inner_by_shmid(shmid)
            .ok_or |
| `src/kernel/src/syscall/io_mpx/poll.rs#sys_ppoll` | `api/src/imp/io_mpx/poll.rs#sys_ppoll` | 同板块同名 | check_sigset_size, do_poll, fds.get_as_mut_slice, fds.is_null, nfds.try_into, nfds.try_into().map_err ...(+5) | Err, core::slice::from_raw_parts_mut, fds.get_as_array, poll_impl, timeout.is_none, timeout.nullable ...(+2) |
| `src/kernel/src/syscall/io_mpx/select.rs#sys_pselect6` | `api/src/imp/io_mpx/poll.rs#sys_pselect6` | 同板块同名 | do_select, nullable!, nullable!(timeout.get_as_ref())?
            .map, nullable!(timeout.get_as_ref())?
            .map(|ts| ts.try_into_time_value())
            .transpose, ts.try_into_time_value | Err, do_select, timeout.is_none, timeout.nullable, timeout.unwrap |

#### 文件系统
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `src/kernel/src/syscall/fs/io.rs#sys_splice` | `api/src/imp/io.rs#sys_splice` | 同板块同名 | AxError::from, Directory::from_fd, Directory::from_fd(fd_in).is_ok, Directory::from_fd(fd_out).is_ok, DummyFd::from_fd, DummyFd::from_fd(fd_in).is_ok ...(+36) | (len - total_copied).min, Err, Ok, SeekFrom::Current, SeekFrom::Start, buf.len ...(+43) |
| `src/kernel/src/syscall/fs/ctl.rs#sys_utimensat` | `api/src/imp/fd/ctl.rs#sys_utimensat` | 同板块同名 | Err, Ok, Some, atime.is_none, check_access, check_access(ptr_addr, core::mem::size_of::<[timespec; 2]>())
            .map_err ...(+13) | Default::default, Ok, Some, TimeValue::new, atime.is_none, mtime.is_none ...(+11) |
| `src/kernel/src/file/fs.rs#resolve_at` | `api/src/fd/fs.rs#resolve_at` | 同板块同名 | Err, Ok, ResolveAtResult::File, ResolveAtResult::Other, busybox_applet, busybox_applet(path).map ...(+16) | Err, Ok, ResolveAtResult::File, ResolveAtResult::Other, dir.inner, dir.inner().clone ...(+12) |
| `src/kernel/src/syscall/fs/ctl.rs#sys_linkat` | `api/src/imp/fd/ctl.rs#sys_linkat` | 同板块同名 | Err, Ok, Path::new, VfsCredentials::effective, check_parent_permission, check_path_len ...(+18) | Err, Ok, debug!, fs.resolve_nonexistent, new_dir.link, new_path.get_as_str ...(+9) |

#### 设备驱动
仅参考项目中观察到该模块代表函数，目标项目没有同模块可对齐证据：
- 参考项目：`arceos/tools/raspi4/chainloader/src/bsp/device_driver/bcm/bcm2xxx_pl011_uart.rs#init`，调用摘要：FBRD::BAUD_DIVFRAC.val, IBRD::BAUD_DIVINT.val, self.flush, self.registers
            .CR
            .write, self.registers
            .LCR_H
            .write, self.registers.CR.set ...(+3)
- 参考项目：`arceos/tools/bwbench_client/src/device.rs#new`，调用摘要：ETH_P_ALL.to_be, Err, Ok, dev.bind_interface, dev.interface_mtu, ifreq.ioctl ...(+5)
- 参考项目：`arceos/tools/bwbench_client/src/main.rs#transmit_benchmark`，调用摘要：Local::now, NetDevice::new, NetDevice::new(interface).unwrap, current_time.signed_duration_since, current_time.signed_duration_since(past_time).num_seconds, dev.send ...(+2)
- 参考项目：`arceos/tools/bwbench_client/src/device.rs#bind_interface`，调用摘要：ETH_P_ALL.to_be, Err, Ok, libc::bind, self.ifreq.ioctl, std::io::Error::last_os_error ...(+2)

#### 网络协议栈
仅参考项目中观察到该模块代表函数，目标项目没有同模块可对齐证据：
- 参考项目：`arceos/ulib/axlibc/c/network.c#inet_pton`，调用摘要：hexval, inet_pton, isdigit, memmove
- 参考项目：`api/src/fd/net.rs#sendto`，调用摘要：Err, IpAddr::V4, Ipv4Addr::new, Ok, SocketAddr::new, socket
                    .bind ...(+3)
- 参考项目：`api/src/fd/net.rs#shutdown`，调用摘要：Ok, tcpsocket.lock, tcpsocket.peer_addr, tcpsocket.shutdown, udpsocket.lock, udpsocket.peer_addr ...(+1)
- 参考项目：`arceos/api/arceos_posix_api/src/imp/net.rs#shutdown`，调用摘要：Ok, tcpsocket.lock, tcpsocket.peer_addr, tcpsocket.shutdown, udpsocket.lock, udpsocket.peer_addr ...(+1)

#### 文件系统/第三方库
仅参考项目中观察到该模块代表函数，目标项目没有同模块可对齐证据：
- 参考项目：`crates/lwext4_rust/c/lwext4/src/ext4_balloc.c#ext4_balloc_alloc_block`，调用摘要：ext4_balloc_get_bgid_of_block, ext4_balloc_get_block_of_bgid, ext4_balloc_set_bitmap_csum, ext4_balloc_verify_bitmap_csum, ext4_bg_get_block_bitmap, ext4_bg_get_free_blocks_count ...(+19)
- 参考项目：`crates/lwext4_rust/c/lwext4/src/ext4_journal.c#jbd_journal_prepare`，调用摘要：EXT4_BLOCK_ZERO, RB_FIND, TAILQ_FOREACH_REVERSE_SAFE, TAILQ_FOREACH_SAFE, TAILQ_REMOVE, ext4_bcache_clear_dirty ...(+17)
- 参考项目：`crates/lwext4_rust/c/lwext4/src/ext4.c#ext4_dir_rm`，调用摘要：EXT4_MP_LOCK, EXT4_MP_UNLOCK, ext4_block_cache_write_back, ext4_dir_en_get_inode, ext4_dir_iterator_fini, ext4_dir_iterator_init ...(+19)
- 参考项目：`crates/lwext4_rust/c/lwext4/src/ext4_dir_idx.c#ext4_dir_dx_split_index`，调用摘要：ext4_block_set, ext4_dir_dx_climit_get_count, ext4_dir_dx_climit_get_limit, ext4_dir_dx_climit_set_count, ext4_dir_dx_climit_set_limit, ext4_dir_dx_entry_get_hash ...(+10)

#### 网络协议栈/第三方库
仅参考项目中观察到该模块代表函数，目标项目没有同模块可对齐证据：
- 参考项目：`arceos/modules/axnet/src/smoltcp_impl/tcp.rs#connect`，调用摘要：Err, Ok, SOCKET_SET
                .with_socket_mut::<tcp::Socket, _, _>, SOCKET_SET.add, SocketSetWrapper::new_tcp_socket, Some ...(+29)
- 参考项目：`arceos/modules/axnet/src/smoltcp_impl/dns.rs#query`，调用摘要：Err, Ok, SOCKET_SET
            .with_socket_mut::<dns::Socket, _, _>, SOCKET_SET
            .with_socket_mut::<dns::Socket, _, _>(handle, |socket| {
                socket.start_query(iface.lock().context(), name, query_type)
            })
            .map_err, SOCKET_SET.poll_interfaces, SOCKET_SET.with_socket_mut::<dns::Socket, _, _> ...(+12)
- 参考项目：`arceos/modules/axnet/src/smoltcp_impl/bench.rs#bench_transmit_bandwidth`，调用摘要：(current_time - past_time).secs, AxNetTxToken::consume, InterfaceWrapper::current_time, info!, self.transmit, tx_buf[0..12].fill ...(+2)
- 参考项目：`arceos/modules/axnet/src/smoltcp_impl/tcp.rs#shutdown`，调用摘要：LISTEN_TABLE.unlisten, Ok, SOCKET_SET.poll_interfaces, SOCKET_SET.with_socket_mut::<tcp::Socket, _, _>, debug!, self.handle.get ...(+9)

#### 第三方库
仅参考项目中观察到该模块代表函数，目标项目没有同模块可对齐证据：
- 参考项目：`crates/visionfive2-sd/src/lib.rs#send_cmd`，调用摘要：RawInterrupt::from, RawInterruptStatusReg::from, S::sleep_ms_until, Some, arg.into, assert! ...(+24)
- 参考项目：`crates/visionfive2-sd/src/lib.rs#init_sdcard`，调用摘要：CardTypeReg::from, CardTypeReg::from(0).with_card_width4_1, CmdArg::new, CmdReg::from, ControlReg::from, RawInterruptStatusReg::from ...(+20)
- 参考项目：`crates/page_table_multiarch/page_table_multiarch/src/bits64.rs#map_region`，调用摘要：Err, M::flush_tlb, Ok, PageSize::Size1G.is_aligned, PageSize::Size2M.is_aligned, PageSize::Size4K.is_aligned ...(+12)
- 参考项目：`crates/axdriver_crates/axdriver_virtio/src/net.rs#try_new`，调用摘要：Err, InnerDev::new, InnerDev::new(transport).map_err, NetBufPool::new, Ok, Some ...(+14)

#### 通用模块/其他
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `src/tools/axconfig-gen-0.2.1/src/output.rs#from_str` | `api/src/imp/sys.rs#from_str` | 同板块同名 | Err, Ok, s.into | data[..info.len()].copy_from_slice, info.as_bytes, info.len |
| `src/tools/cargo-axplat-0.3.0/template/src/mem.rs#phys_to_virt` | `arceos/modules/axhal/src/paging.rs#phys_to_virt` | 同板块同名 | todo! | phys_to_virt |
| `src/tools/cargo-axplat-0.3.0/template/src/console.rs#read_bytes` | `arceos/modules/axhal/src/platform/riscv64_qemu_virt/console.rs#read_bytes` | 同板块同名 | todo! | (bytes.len() - total_read).min, VirtAddr::from_mut_ptr_of, bytes.as_mut_ptr, bytes.as_ptr, bytes.len, bytes.len().min ...(+8) |
| `src/tools/cargo-axplat-0.3.0/template/src/time.rs#set_oneshot_timer` | `arceos/modules/axhal/src/platform/x86_pc/time.rs#set_oneshot_timer` | 同板块同名 | todo! | NANOS_TO_LAPIC_TICKS_RATIO.mul_trunc, apic_ticks.max, assert!, crate::time::monotonic_time_nanos, lapic.set_timer_initial, super::apic::local_apic |


### 1. 宏观判定
- **定性结论**: **独立开发，底层架构显著差异**
- **相似度**: 2.36%
- **共同 AST 指纹**: 98
- **唯一 AST 指纹**: 目标项目 1227，参考项目 3020
- **覆盖率**: 目标项目 7.99%，参考项目 3.25%

**判定依据**: 双方项目的客观 AST 相似度仅为 2.36%，远低于构成“抄袭”或“非独立开发”的阈值。在 98 个共同 AST 指纹中，绝大多数为 `Ok`、`Err`、`Some`、`None`、`debug!`、`assert!`、`.map()`、`.collect()` 等 Rust 语言通用范式或标准库惯用法，不具备项目间排他性。双方在核心模块（如进程管理、文件系统）的覆盖率均低于 10%，表明在实现层面存在根本性的架构差异。控方提出的“铁证”经技术析理，实为 Linux 标准 ABI 语义下的领域必然同形，而非代码复用。

### 2. 全模块覆盖矩阵解读

| 模块 | 目标函数数 | 参考函数数 | 共同唯一 AST 指纹 | Target 覆盖 | Base 覆盖 | 证据强度 | 解读 |
| :--- | ---: | ---: | ---: | ---: | ---: | :--- | :--- |
| **进程管理** | 74 | 93 | 3 | 4.41% | 3.57% | **弱** | 双方均实现进程管理，但共同指纹极少。`clear_child_tid` 等“一致”项为 Linux ABI 标准字段的原子操作，属领域必然同形。`clear_proc_shm` 等函数在管理器抽象（`self` vs 全局 `SHM_MANAGER`）上存在结构性差异。 |
| **任务调度/异步运行时** | 295 | 399 | 2 | 0.75% | 0.69% | **未形成** | 覆盖率极低，接近随机噪声。共同指纹 `sys_futex` 和 `sys_kill` 均为 POSIX 标准系统调用，其高层调用流由接口语义强制，但内部实现（如 FutexKey 管理、信号权限检查）差异显著。 |
| **内存管理** | 205 | 326 | 8 | 4.15% | 2.75% | **弱** | 共同指纹集中在 `sys_mmap`、`load_user_app` 等标准内存操作入口。两者在内存后端（`Backend` 类型体系）、地址空间管理（`VirtAddrRange` vs `kernel_aspace`）上采用了不同的抽象与同步策略。 |
| **中断/异常处理** | 9 | 62 | 0 | 0.0% | 0.0% | **未形成** | 目标项目该模块体量极小（9个函数），与参考项目（62个函数）规模悬殊，且未观察到任何共同结构指纹，表明双方在底层架构（如 Trap 处理、中断控制器驱动）上完全不同。 |
| **系统调用** | 153 | 153 | 0 | 0.0% | 0.0% | **未形成** | 函数数量巧合相同，但共同指纹为零。双方对系统调用分发、参数校验、错误处理等核心路径的实现方式完全不同，无任何同源迹象。 |
| **文件系统** | 564 | 747 | 49 | 10.08% | 8.66% | **中** | 覆盖率最高的模块，但共同指纹主要源于双方均实现了 Linux 兼容的 procfs、devfs、sysfs 等虚拟文件系统，以及 `sys_splice`、`sys_utimensat` 等标准系统调用。底层文件系统后端（`SimpleFs` vs `DynamicFs`/`RamFileSystem`）和 VFS 抽象层设计存在根本差异。 |
| **设备驱动** | 0 | 142 | 0 | 0.0% | 0.0% | **未形成** | 仅参考项目存在该模块，目标项目无任何设备驱动代码，无法形成对比。 |
| **网络协议栈** | 0 | 77 | 0 | 0.0% | 0.0% | **未形成** | 仅参考项目存在该模块，目标项目无任何网络协议栈代码，无法形成对比。 |
| **第三方库/公共依赖** | 0 | 1182 | 0 | 0.0% | 0.0% | **未形成** | 参考项目包含大量第三方/上游库（如 `lwext4_rust`、`smoltcp_impl`、`visionfive2-sd`），目标项目未包含。这证明双方均复用了公开上游生态（ArceOS），而非互相复用。 |

### 3. 底层 AST 关键证据指纹表

| 所属板块 | 目标项目路径 | 参考项目路径 | 证据类型 | 技术说明 |
| :--- | :--- | :--- | :--- | :--- |
| 进程管理 | `src/kernel/src/task/mod.rs#set_clear_child_tid` | `core/src/task/thread.rs#set_clear_child_tid` | 领域必然同形 | `self.clear_child_tid.store` 是 Linux `set_tid_address(2)` ABI 标准字段的原子写操作，任何实现该语义的内核都会生成此 AST。 |
| 进程管理 | `src/kernel/src/task/mod.rs#clear_child_tid` | `core/src/task/thread.rs#clear_child_tid` | 领域必然同形 | `self.clear_child_tid.load` 同上，为标准原子读操作。 |
| 进程管理 | `src/kernel/src/syscall/ipc/shm.rs#clear_proc_shm` | `api/src/imp/ipc/shm.rs#clear_proc_shm` | 架构差异 | 目标使用 `self` 方法调用（面向对象封装），参考使用全局 `SHM_MANAGER.lock()` 调用。管理器抽象与所有权模型不同。 |
| 进程管理 | `src/kernel/src/task/ops.rs#processes` | `core/src/task/process_group.rs#processes` | 架构差异 | 目标使用全局 `PROCESS_TABLE.read()`（RwLock），参考使用 `self.processes.lock()`（SpinNoIrq）。同步原语与容器归属不同。 |
| 进程管理 | `src/kernel/src/syscall/ipc/shm.rs#attach_process` | `api/src/imp/ipc/shm.rs#attach_process` | 上下文差异 | 目标在 `va_range.insert` 前调用 `monotonic_time_nanos`（时间戳记录），参考调用 `assert!`（前置断言）。附加语义不同。 |
| 进程管理 | `src/kernel/src/syscall/task/thread.rs#sys_getppid` | `src/syscall_imp/task/thread.rs#sys_getppid` | 架构差异 | 目标任务抽象路径为 `current().as_thread().proc_data.proc.parent()`，参考为 `axtask::current().task_ext().thread.process().parent()`。任务-进程关系建模不同。 |
| 文件系统 | `src/kernel/src/pseudofs/proc.rs#new_procfs` | `core/src/vfs/proc.rs#new_procfs` | 架构差异 | 目标使用 `SimpleFs::new_with`，参考使用 `DynamicFs::new_with`。底层文件系统实现框架不同。 |
| 文件系统 | `src/kernel/src/syscall/fs/ctl.rs#sys_getcwd` | `api/src/imp/fd/ctl.rs#sys_getcwd` | 架构差异 | 目标通过 `FS_CONTEXT.lock` 获取当前目录，参考通过 `FS_CONTEXT.lock().current_dir()`。VFS 上下文管理模型不同。 |
| 内存管理 | `src/kernel/src/syscall/mm/mmap.rs#sys_mmap` | `src/syscall_imp/mm/mmap.rs#sys_mmap` | 架构差异 | 目标 `Backend` 体系包含 `new_cow`、`new_linear` 等特有变体，参考 `MmapFlags` 处理逻辑不同。内存映射后端设计存在差异。 |
| 任务调度 | `src/kernel/src/syscall/sync/futex.rs#sys_futex` | `api/src/imp/futex.rs#sys_futex` | 架构差异 | 目标使用 `FutexKey::new_current` 和 `(deadline - now).min` 超时计算，参考使用 `futex_table.get_or_insert` 和 `TimeValue::new`。Futex 管理模型不同。 |

### 4. 核心代码调用流对比剖析

#### **【进程管理 / 进程退出路径】**
*   **底层技术析理**: 双方均实现了 POSIX `exit` 系统调用，最终汇聚到 `do_exit` 函数。然而，目标项目的 `do_exit` 侧重于进程级资源清理（`child.free`、`child.is_zombie`、`FutexKey::new_current`），而参考项目的 `do_exit` 侧重于线程级资源清理（`Layout::new::<Tid>`、`clear_child_tid.get_as`、`close_all_filelike`）。目标项目在退出时处理 Futex 健壮列表（`exit_robust_list`），参考项目则无此逻辑。两者在清理粒度、内存回收策略和同步机制上存在根本性架构差异。
*   **代码溯源链接**:
    - 目标项目: `src/kernel/src/task/ops.rs#do_exit`
    - 参考项目: `api/src/imp/task.rs#do_exit`

#### **【文件系统 / procfs 实现】**
*   **底层技术析理**: 双方均实现了 Linux 兼容的 `proc` 虚拟文件系统。目标项目采用 `SimpleFs` 框架，通过 `NodeOpsMux::Dir` 和 `SimpleDir::new_maker` 构建目录树，其 `lookup_child` 函数直接处理 `CStr` 解析和 `FD_TABLE` 遍历。参考项目采用 `DynamicFs` 框架，通过 `DynamicDir::builder` 和 `RamFileSystem` 构建，其 `builder` 函数使用 `Device::new` 和 `DeviceId` 抽象。两者在 VFS 后端、节点操作分发和设备抽象层上完全不同。
*   **代码溯源链接**:
    - 目标项目: `src/kernel/src/pseudofs/proc.rs#lookup_child`
    - 参考项目: `core/src/vfs/proc.rs#builder`

#### **【内存管理 / 应用加载】**
*   **底层技术析理**: 双方均实现了 ELF 加载器。目标项目的 `load_user_app` 使用 `ELF_LOADER.lock().load` 进行加载，并显式计算用户栈对齐（`(ustack_top - user_sp_aligned).align_up_4k`）。参考项目的 `load_user_app` 则包含动态链接器解析逻辑（`CStr::from_bytes_with_nul(interp)`），并直接使用 `ElfFile::new`。两者在 ELF 解析方式、动态链接支持和地址空间布局计算上存在显著差异。
*   **代码溯源链接**:
    - 目标项目: `src/kernel/src/mm/loader.rs#load_user_app`
    - 参考项目: `core/src/mm.rs#load_user_app`

### 5. 单方模块与降权项

*   **仅参考项目存在的模块**: 设备驱动、网络协议栈、文件系统/第三方库、网络协议栈/第三方库、第三方库。这些模块在目标项目中完全缺失，表明双方项目的功能范围和设计目标存在巨大差异。参考项目中的 `lwext4_rust`、`smoltcp_impl`、`visionfive2-sd` 等均为公开的第三方或上游组件，不能作为指控目标项目复用的证据。
*   **降权项说明**: 控辩双方材料中出现的 `println!`、`debug!`、`assert!`、`syscall_body!` 等宏，以及 `Ok`、`Err`、`Some`、`None`、`.map()`、`.collect()` 等 Rust 标准库范式，均为语言或框架提供的公共接口，不具备任何项目特异性，已从强证据中全部降权排除。跨板块同名函数（如将参考项目的 `sys_epoll_wait` 与目标项目的进程管理函数对比）不构成同板块主证据，已予排除。

### 6. 保守结论

综合客观 AST 相似度（2.36%）、极低的模块覆盖率以及核心代码调用流的架构差异分析，本次审计未发现任何足以支持“抄袭”或“非独立开发”指控的技术证据。双方项目在进程管理、文件系统、内存管理等核心模块的实现上，于抽象层次、内存模型、同步策略及第三方库依赖等方面均表现出根本性的异构特征。控方提出的“铁证”经技术析理，实为遵循 POSIX/Linux 标准 ABI 或 Rust 语言惯用法而产生的领域必然同形，不具备项目间排他性。整体代码同源性风险极低，应认定为独立开发作品。

------------------------------

