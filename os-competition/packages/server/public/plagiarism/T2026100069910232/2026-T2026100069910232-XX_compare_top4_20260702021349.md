# 🛡️ OS 内核赛自动化代码级架构审计报告

**审计时间**: `2026-07-02 02:13:49`
**参赛作品名称**: `2026-T2026100069910232-XX`
**年份**: `2026`
**学校**: `XX`
**队伍名称**: `T2026100069910232`
**比赛名字**: `OS-Competition`
**比赛子赛道**: `Kernel-Implementation`
**仓库地址**: `https://gitlab.eduxiji.net/educg-group-43501-3132633/T2026100069910232-2532`
**推测开发语言**: `Rust / C (由AST探针自动识别)`
**特征函数量**: `1400`

## ⚔️ 底层代码同源性对比 (AST Homology)
> 本部分由专门的代码审查智能体，对该项目与符合年份条件的历届最高相似度项目进行控制流与底层调用对比。新版报告会先给出确定性的全模块覆盖矩阵，再给出 Agent 的分模块解释，避免只围绕少量抽样函数展开。

### 🏆 Top 4 底层对标: vs `2025-undefined-清华大学`（清华大学 / undefined）

#### 对方作品信息

**对方年份**: `2025`
**对方学校**: `清华大学`
**对方队伍名称**: `undefined`
**对方比赛名字**: `OS-Competition`
**对方比赛子赛道**: `Kernel-Implementation`
**对方仓库地址**: `https://gitlab.eduxiji.net/educg-group-36002-2710490/T202510003995291-2331`

> 双向覆盖率：共同 AST 指纹: 82；唯一 AST 指纹: 目标项目 1227，参考项目 2820；覆盖率: 目标项目 6.68%，参考项目 2.91%

## 全模块对比总览（确定性证据）

> 总体覆盖率：共同 AST 指纹: 82；唯一 AST 指纹: 目标项目 1227，参考项目 2820；覆盖率: 目标项目 6.68%，参考项目 2.91%

### 1. 模块覆盖矩阵
| 模块 | 目标函数数 | 参考函数数 | 共同唯一 AST 指纹 | Target 覆盖 | Base 覆盖 | 状态 |
| :--- | ---: | ---: | ---: | ---: | ---: | :--- |
| 进程管理 | 74 | 135 | 3 | 4.41% | 2.48% | 双方存在且有结构重合 |
| 任务调度/异步运行时 | 295 | 415 | 2 | 0.75% | 0.67% | 双方存在且有结构重合 |
| 内存管理 | 205 | 450 | 9 | 4.66% | 2.36% | 双方存在且有结构重合 |
| 中断/异常处理 | 9 | 53 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |
| 系统调用 | 153 | 109 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |
| 文件系统 | 564 | 999 | 47 | 9.67% | 7.04% | 双方存在且有结构重合 |
| 设备驱动 | 0 | 133 | 0 | 0.0% | 0.0% | 仅参考项目体现 |
| 网络协议栈 | 0 | 65 | 0 | 0.0% | 0.0% | 仅参考项目体现 |
| 文件系统/第三方库 | 0 | 643 | 0 | 0.0% | 0.0% | 仅参考项目体现 |
| 网络协议栈/第三方库 | 0 | 130 | 0 | 0.0% | 0.0% | 仅参考项目体现 |
| 通用模块/其他 | 100 | 555 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |

### 2. 分模块证据清单

#### 进程管理
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `src/kernel/src/task/ops.rs#do_exit` | `arceos/examples/shell/src/cmd.rs#do_exit` | 同板块同名 | Arc::downgrade, FutexKey::new_current, SignalInfo::new_kernel, Some, axtask::yield_now, child.free ...(+43) | println!, std::process::exit |
| `src/kernel/src/syscall/signal.rs#sys_rt_sigprocmask` | `api/src/imp/task/signal.rs#sys_rt_sigprocmask` | 同板块同名 | Err, Ok, check_sigset_size, curr.as_thread, current, debug! ...(+7) | Err, Ok, check_sigset_size, current_thread_data, current_thread_data()
        .signal
        .with_blocked_mut::<LinuxResult<_>>, oldset.nullable ...(+1) |
| `src/kernel/src/syscall/task/execve.rs#sys_execve` | `api/src/interface/task/execve.rs#sys_execve` | 同板块同名 | execve_inner, load_exec_args_envs, vm_load_string | BUSYBOX.to_string, Some, abs_path.to_string, args[1..].iter, args[1..].iter().cloned, argv.clone ...(+10) |
| `src/kernel/src/syscall/task/wait.rs#apply` | `api/src/imp/task/wait.rs#apply` | 同板块同名 | child.group, child.group().pgid, child.pid | child.get_group, child.get_group().get_pgid, child.get_pid |

#### 任务调度/异步运行时
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `src/kernel/src/syscall/sync/futex.rs#sys_futex` | `api/src/imp/task/futex.rs#sys_futex` | 同板块同名 | (deadline - now).min, AxError::from, Err, FutexKey::new_current, Ok, Some ...(+23) | Err, Ok, axtask::yield_now, current_process_data, drop, futex_table
                .entry ...(+27) |
| `src/kernel/src/syscall/signal.rs#sys_kill` | `api/src/imp/task/signal.rs#sys_kill` | 同板块同名 | Err, Ok, Some, check_kill_permission, check_kill_permission(proc.pid(), &sig).is_err, check_kill_permission(proc_data.proc.pid(), &Some(sig.clone())).is_err ...(+16) | Err, Ok, current_process, current_process().get_group, debug!, get_all_processes ...(+8) |
| `src/kernel/src/syscall/task/job.rs#sys_setpgid` | `api/src/imp/task/thread.rs#sys_setpgid` | 同板块同名 | Err, Ok, curr.as_thread, curr.as_thread().proc_data.clone, curr_proc.group, curr_proc.group().session ...(+24) | Err, Ok, current_process, current_process().get_child, current_process().get_child(pid).ok_or, current_process().get_pid ...(+2) |
| `src/kernel/src/task/signal.rs#check_signals` | `api/src/imp/task/signal.rs#check_signals` | 同板块同名 | Poll::Ready, block_on, cx.waker, data.child_exit_event.wake, do_exit, get_process_data ...(+11) | current_thread_data, debug!, sig.signo, signal.check_signals, sys_exit_impl |

#### 内存管理
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `src/kernel/src/syscall/mod.rs#handle_syscall` | `src/syscall.rs#handle_syscall` | 同板块同名 | Err, LinuxError::ENOSYS.code, LinuxError::from, LinuxError::from(err).code, Sysno::new, debug! ...(+266) | Err, Sysno::new, err.code, info!, result.unwrap_or_else, set_trap_frame ...(+151) |
| `src/kernel/src/syscall/mm/mmap.rs#sys_mmap` | `api/src/imp/mm/mmap.rs#sys_mmap` | 同板块同名 | (addr + length).align_up, Arc::new, Backend::new_alloc, Backend::new_cow, Backend::new_file, Backend::new_linear ...(+51) | Err, File::from_fd, MmapFlags::from_bits_truncate, MmapProt::from_bits_truncate, Ok, PhysAddr::from ...(+34) |
| `src/kernel/src/mm/loader.rs#load_user_app` | `core/src/mm.rs#load_user_app` | 同板块同名 | (ustack_top - user_sp_aligned).align_up_4k, Backend::new_alloc, ELF_LOADER.lock, ELF_LOADER.lock().load, Err, FS_CONTEXT.lock ...(+54) | CStr::from_bytes_with_nul, CStr::from_bytes_with_nul(interp)
            .map_err, CStr::from_bytes_with_nul(interp)
            .map_err(|_| AxError::InvalidData)?
            .to_str, CStr::from_bytes_with_nul(interp)
            .map_err(|_| AxError::InvalidData)?
            .to_str()
            .map_err, CStr::from_bytes_with_nul(interp)
            .map_err(|_| AxError::InvalidData)?
            .to_str()
            .map_err(|_| AxError::InvalidData)?
            .to_string, ElfFile::new ...(+34) |
| `src/kernel/src/syscall/ipc/shm.rs#sys_shmat` | `api/src/interface/mm/shm.rs#sys_shmat` | 同板块同名 | Arc::new, Backend::new_shared, Err, Ok, SHM_MANAGER.lock, SharedPages::new ...(+29) | Err, Ok, SHARED_MEMORY_MANAGER.get, SHARED_MEMORY_MANAGER.get(key).ok_or, ShmFlags::from_bits_truncate, Some ...(+16) |

#### 中断/异常处理
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `src/tools/cargo-axplat-0.3.0/template/src/irq.rs#set_enable` | `arceos/modules/axhal/src/platform/loongarch64_qemu_virt/irq.rs#set_enable` | 同板块同名 | todo! | ecfg::read, ecfg::read().lie, ecfg::set_lie |
| `src/kernel/src/pseudofs/dev/tty/terminal/termios.rs#default` | `arceos/ulib/axlibc/c/pow.c#exp_inline` | 同模块上下文对齐/差异观察 | ctl | __math_oflow, __math_uflow, asdouble, asuint64, converttoint, eval_as_double ...(+4) |
| `src/kernel/src/file/signalfd.rs#from_signal_info` | `arceos/modules/axhal/src/arch/aarch64/trap.rs#handle_sync_exception` | 同模块上下文对齐/差异观察 | sig_info.code, sig_info.errno, sig_info.signo | ESR_EL1.extract, crate::trap::handle_syscall, crate::trap::post_trap_callback, debug!, esr.read, esr.read_as_enum ...(+6) |
| `src/kernel/src/pseudofs/dev/tty/terminal/termios.rs#signo_for` | `arceos/modules/axhal/src/arch/x86_64/idt.rs#new` | 同模块上下文对齐/差异观察 | Some, self.special_char | InterruptDescriptorTable::new, core::mem::transmute, core::slice::from_raw_parts_mut, entries[i].set_handler_fn, opt.set_privilege_level |

#### 系统调用
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `src/kernel/src/syscall/net/opt.rs#sys_setsockopt` | `api/src/imp/net/socket.rs#sys_setsockopt` | 同板块同名 | Err, Ok, Socket::from_fd, call_dispatch!, debug!, size_of::<T> ...(+2) | Err, Ok, Socket::from_fd, Socket::from_fd(sockfd)?.setsockopt, core::slice::from_raw_parts, debug! ...(+1) |
| `src/kernel/src/syscall/net/socket.rs#sys_connect` | `api/src/imp/net/socket.rs#sys_connect` | 同板块同名 | Ok, Socket::from_fd, Socket::from_fd(fd)?.connect, Socket::from_fd(fd)?.connect(addr).map_err, SocketAddrEx::read_from_user, debug! | Ok, Socket::from_fd, Socket::from_fd(socket_fd)?.connect, debug!, from_sockaddr |
| `src/kernel/src/syscall/time.rs#sys_clock_getres` | `api/src/imp/utils/time.rs#sys_clock_getres` | 同板块同名 | Err, Ok, TimeValue::from_nanos, res.nullable, res.vm_write, timespec::from_time_value ...(+2) | Err, Ok, nullable! |
| `src/kernel/src/syscall/net/socket.rs#sys_shutdown` | `api/src/imp/net/socket.rs#sys_shutdown` | 同板块同名 | Err, Socket::from_fd, debug!, socket.shutdown, socket.shutdown(how).map | Ok, Socket::from_fd, Socket::from_fd(socket_fd)?.shutdown, debug! |

#### 文件系统
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `src/kernel/src/syscall/fs/io.rs#sys_splice` | `api/src/interface/fs/io.rs#sys_splice` | 同板块同名 | AxError::from, Directory::from_fd, Directory::from_fd(fd_in).is_ok, Directory::from_fd(fd_out).is_ok, DummyFd::from_fd, DummyFd::from_fd(fd_in).is_ok ...(+36) | Err, Ok, fd_lookup, file_like_as::<Pipe>, file_like_as::<Pipe>(in_file.clone()).is_none, file_like_as::<Pipe>(out_file.clone()).is_none ...(+6) |
| `src/kernel/src/pseudofs/dev/fb.rs#ioctl` | `api/src/core/fs/imp/dev/framebuffer.rs#ioctl` | 同板块同名 | (arg as *mut FixScreenInfo).vm_write, (arg as *mut VarScreenInfo).vm_write, Err, Ok, axdisplay::framebuffer_info | Default::default, Err, Ok, arg.into, framebuffer_flush, user_info.get_as_mut_ref |
| `src/kernel/src/pseudofs/dev/loop.rs#ioctl` | `api/src/core/fs/pseudo/file.rs#ioctl` | 同板块同名 | (arg as *const loop_info).vm_read_uninit, (arg as *const loop_info).vm_read_uninit()?.assume_init, (arg as *const u32).vm_read, (arg as *mut loop_info).vm_write, (arg as *mut u32).vm_write, (arg as *mut u64).vm_write ...(+23) | Err, warn! |
| `src/kernel/src/syscall/fs/ctl.rs#sys_utimensat` | `api/src/imp/fs/ctl.rs#sys_utimensat` | 同板块同名 | Err, Ok, Some, atime.is_none, check_access, check_access(ptr_addr, core::mem::size_of::<[timespec; 2]>())
            .map_err ...(+13) | Default::default, Duration::from, Ok, ResolveFlags::from_bits_truncate, Some, into_duration ...(+8) |

#### 设备驱动
仅参考项目中观察到该模块代表函数，目标项目没有同模块可对齐证据：
- 参考项目：`arceos/tools/raspi4/chainloader/src/bsp/device_driver/bcm/bcm2xxx_pl011_uart.rs#init`，调用摘要：FBRD::BAUD_DIVFRAC.val, IBRD::BAUD_DIVINT.val, self.flush, self.registers
            .CR
            .write, self.registers
            .LCR_H
            .write, self.registers.CR.set ...(+3)
- 参考项目：`arceos/tools/bwbench_client/src/device.rs#new`，调用摘要：ETH_P_ALL.to_be, Err, Ok, dev.bind_interface, dev.interface_mtu, ifreq.ioctl ...(+5)
- 参考项目：`arceos/tools/bwbench_client/src/main.rs#transmit_benchmark`，调用摘要：Local::now, NetDevice::new, NetDevice::new(interface).unwrap, current_time.signed_duration_since, current_time.signed_duration_since(past_time).num_seconds, dev.send ...(+2)
- 参考项目：`arceos/tools/bwbench_client/src/device.rs#bind_interface`，调用摘要：ETH_P_ALL.to_be, Err, Ok, libc::bind, self.ifreq.ioctl, std::io::Error::last_os_error ...(+2)

#### 网络协议栈
仅参考项目中观察到该模块代表函数，目标项目没有同模块可对齐证据：
- 参考项目：`arceos/ulib/axlibc/c/network.c#inet_pton`，调用摘要：hexval, inet_pton, isdigit, memmove
- 参考项目：`api/src/imp/net/socket.rs#setsockopt`，调用摘要：Ok, SocketOptionLevel::try_from, error!, udpsocket.lock, unimplemented!, warn!
- 参考项目：`api/src/imp/net/socket.rs#sendto`，调用摘要：Err, IpAddr::V4, Ipv4Addr::new, SocketAddr::new, udpsocket
                    .bind, udpsocket
                    .bind(SocketAddr::new(IpAddr::V4(Ipv4Addr::new(127, 0, 0, 1)), 0))
                    .map_err ...(+3)
- 参考项目：`api/src/imp/net/socket.rs#shutdown`，调用摘要：Ok, tcpsocket.lock, tcpsocket.peer_addr, tcpsocket.shutdown, udpsocket.lock, udpsocket.peer_addr ...(+1)

#### 文件系统/第三方库
仅参考项目中观察到该模块代表函数，目标项目没有同模块可对齐证据：
- 参考项目：`modules/lwext4_rust/c/lwext4/src/ext4_balloc.c#ext4_balloc_alloc_block`，调用摘要：ext4_balloc_get_bgid_of_block, ext4_balloc_get_block_of_bgid, ext4_balloc_set_bitmap_csum, ext4_balloc_verify_bitmap_csum, ext4_bg_get_block_bitmap, ext4_bg_get_free_blocks_count ...(+19)
- 参考项目：`modules/lwext4_rust/c/lwext4/src/ext4_journal.c#jbd_journal_prepare`，调用摘要：EXT4_BLOCK_ZERO, RB_FIND, TAILQ_FOREACH_REVERSE_SAFE, TAILQ_FOREACH_SAFE, TAILQ_REMOVE, ext4_bcache_clear_dirty ...(+17)
- 参考项目：`modules/lwext4_rust/c/lwext4/src/ext4.c#ext4_dir_rm`，调用摘要：EXT4_MP_LOCK, EXT4_MP_UNLOCK, ext4_block_cache_write_back, ext4_dir_en_get_inode, ext4_dir_iterator_fini, ext4_dir_iterator_init ...(+19)
- 参考项目：`modules/lwext4_rust/c/lwext4/src/ext4_dir_idx.c#ext4_dir_dx_split_index`，调用摘要：ext4_block_set, ext4_dir_dx_climit_get_count, ext4_dir_dx_climit_get_limit, ext4_dir_dx_climit_set_count, ext4_dir_dx_climit_set_limit, ext4_dir_dx_entry_get_hash ...(+10)

#### 网络协议栈/第三方库
仅参考项目中观察到该模块代表函数，目标项目没有同模块可对齐证据：
- 参考项目：`arceos/modules/axnet/src/smoltcp_impl/tcp.rs#connect`，调用摘要：Err, Ok, Ok::<(IpEndpoint, IpEndpoint), AxError>, SOCKET_SET
                .with_socket_mut::<tcp::Socket, _, _>, SOCKET_SET.add, SocketSetWrapper::new_tcp_socket ...(+32)
- 参考项目：`arceos/modules/axnet/src/smoltcp_impl/dns.rs#query`，调用摘要：Err, Ok, SOCKET_SET
            .with_socket_mut::<dns::Socket, _, _>, SOCKET_SET
            .with_socket_mut::<dns::Socket, _, _>(handle, |socket| {
                socket.start_query(iface.lock().context(), name, query_type)
            })
            .map_err, SOCKET_SET.poll_interfaces, SOCKET_SET.with_socket_mut::<dns::Socket, _, _> ...(+12)
- 参考项目：`arceos/modules/axnet/src/smoltcp_impl/tcp.rs#recv_timeout`，调用摘要：Err, Ok, SOCKET_SET.with_socket_mut::<tcp::Socket, _, _>, ax_err!, ax_err_type!, current_ticks ...(+11)
- 参考项目：`arceos/modules/axnet/src/smoltcp_impl/bench.rs#bench_transmit_bandwidth`，调用摘要：(current_time - past_time).secs, AxNetTxToken::consume, InterfaceWrapper::current_time, info!, self.transmit, tx_buf[0..12].fill ...(+2)

#### 通用模块/其他
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `src/tools/axconfig-gen-0.2.1/src/output.rs#from_str` | `api/src/imp/sys.rs#from_str` | 同板块同名 | Err, Ok, s.into | data[..info.len()].copy_from_slice, info.as_bytes, info.len |
| `src/tools/cargo-axplat-0.3.0/template/src/mem.rs#phys_to_virt` | `arceos/modules/axhal/src/paging.rs#phys_to_virt` | 同板块同名 | todo! | phys_to_virt |
| `src/tools/cargo-axplat-0.3.0/template/src/console.rs#read_bytes` | `arceos/modules/axhal/src/platform/riscv64_qemu_virt/console.rs#read_bytes` | 同板块同名 | todo! | VirtAddr::from_mut_ptr_of, bytes.as_mut_ptr, bytes.len, bytes.len().min, sbi_rt::Physical::new, sbi_rt::console_read ...(+2) |
| `src/tools/cargo-axplat-0.3.0/template/src/time.rs#set_oneshot_timer` | `arceos/modules/axhal/src/platform/x86_pc/time.rs#set_oneshot_timer` | 同板块同名 | todo! | NANOS_TO_LAPIC_TICKS_RATIO.mul_trunc, apic_ticks.max, assert!, crate::time::monotonic_time_nanos, lapic.set_timer_initial, super::apic::local_apic |


# 最终同源性定性仲裁报告

**审计对象**: [2026-T2026100069910232-XX] vs [2025-undefined-清华大学]
**审计模式**: 深度析理模式
**仲裁依据**: 客观 AST 相似度、双向覆盖率、同板块底层调用图谱、架构差异分析

---

## 1. 宏观判定

- **定性结论**: **独立开发，底层架构显著差异**
- **客观相似度**: 2.07%
- **共同 AST 指纹**: 82
- **唯一 AST 指纹**: 目标项目 1227，参考项目 2820
- **双向覆盖率**: 目标项目 6.68%，参考项目 2.91%

**判定依据**: 双方在进程管理、内存管理、文件系统等核心板块的覆盖率均低于 10%，且共同 AST 指纹中大量为 Rust 标准库泛型构造（`Some`、`Ok`、`Err`、`Arc::new`）和 POSIX 标准接口命名（`sys_getpid`、`sys_fork`、`sys_exit`）。在可对齐的核心子系统（进程退出、内存映射、procfs 构造）中，双方在同步原语选择、错误处理模型、伪文件系统框架、进程表访问路径上存在实质性架构差异。82 个共同指纹不足以支撑代码抄袭或逻辑复用指控。

---

## 2. 全模块覆盖矩阵解读

### 进程管理
- **双方均存在**: 是（目标 74 函数，参考 135 函数）
- **共同 AST 指纹**: 3
- **覆盖率**: 目标 4.41%，参考 2.48%
- **证据强度**: **弱**
- **说明**: 共同指纹仅 3 个，覆盖率极低。所谓“铁证”`is_clone_child` 的 AST 指纹仅为 `Some`，属于 Rust `Option<T>` 泛型构造，无法区分任何涉及 `Option` 判断的实现。`apply`、`get_process_group`、`new_procfs` 等函数在同步原语（`read()` vs `lock()`）、错误处理（`ok_or` vs `cloned`）、文件系统框架（`SimpleFs` vs `DynamicFs`）上存在明确差异，属于 POSIX 语义约束下的独立实现。

### 任务调度/异步运行时
- **双方均存在**: 是（目标 295 函数，参考 415 函数）
- **共同 AST 指纹**: 2
- **覆盖率**: 目标 0.75%，参考 0.67%
- **证据强度**: **未形成**
- **说明**: 覆盖率不足 1%，共同指纹仅为 `sys_futex` 和 `sys_kill` 等 POSIX 标准系统调用入口。双方在调度器实现（`axtask::yield_now` vs `axtask::exit`）、等待队列机制上使用不同的上游框架抽象，无同源迹象。

### 内存管理
- **双方均存在**: 是（目标 205 函数，参考 450 函数）
- **共同 AST 指纹**: 9
- **覆盖率**: 目标 4.66%，参考 2.36%
- **证据强度**: **弱**
- **说明**: 共同指纹集中在 `sys_mmap`、`sys_shmat`、`load_user_app` 等标准内存管理系统调用。目标项目使用 `Backend::new_alloc`/`Backend::new_cow`/`Backend::new_file` 等多态后端模型，参考项目使用 `MmapFlags::from_bits_truncate`/`PhysAddr::from` 等直接物理地址操作，内存抽象模型不同。

### 中断/异常处理
- **双方均存在**: 是（目标 9 函数，参考 53 函数）
- **共同 AST 指纹**: 0
- **覆盖率**: 目标 0.0%，参考 0.0%
- **证据强度**: **未形成**
- **说明**: 无任何共同 AST 指纹。目标项目中断处理函数多为 `todo!` 占位，参考项目有完整的架构相关 Trap 处理（`handle_sync_exception`、`set_enable`），双方在此模块无任何可对齐的实现。

### 系统调用
- **双方均存在**: 是（目标 153 函数，参考 109 函数）
- **共同 AST 指纹**: 0
- **覆盖率**: 目标 0.0%，参考 0.0%
- **证据强度**: **未形成**
- **说明**: 虽然双方均有系统调用分发机制（`handle_syscall`），但 AST 指纹匹配算法未在此模块捕获任何共同指纹。目标项目使用 `LinuxError::ENOSYS.code` 错误模型，参考项目使用 `err.code` 直接传播，分发路径不同。

### 文件系统
- **双方均存在**: 是（目标 564 函数，参考 999 函数）
- **共同 AST 指纹**: 47
- **覆盖率**: 目标 9.67%，参考 7.04%
- **证据强度**: **中**
- **说明**: 文件系统是共同指纹最多的板块（47 个），但覆盖率仍低于 10%。共同指纹集中在 `sys_splice`、`ioctl`、`sys_utimensat` 等标准文件系统系统调用。目标项目使用 `SimpleFs`/`NodeOpsMux::Dir`/`VfsError` 伪文件系统框架，参考项目使用 `DynamicFs`/`DynamicDir`/`AxError`，框架选型不同。`new_procfs` 中 `"proc".into` 是 Linux procfs 标准挂载名，不构成抄袭证据。

### 设备驱动
- **仅参考项目存在**: 参考项目 133 函数，目标项目 0 函数
- **共同 AST 指纹**: 0
- **覆盖率**: 目标 N/A，参考 N/A
- **证据强度**: **未形成**
- **说明**: 目标项目无设备驱动模块，参考项目包含 bcm PL011 UART、网络设备驱动等。此模块为单方持有，不构成同源证据。

### 网络协议栈
- **仅参考项目存在**: 参考项目 65 函数，目标项目 0 函数
- **共同 AST 指纹**: 0
- **覆盖率**: 目标 N/A，参考 N/A
- **证据强度**: **未形成**
- **说明**: 目标项目无网络协议栈模块，参考项目包含 smoltcp TCP/DNS 实现。此模块为单方持有，不构成同源证据。

### 第三方库/公共依赖
- **仅参考项目存在**: 参考项目 643 函数（文件系统/第三方库）+ 130 函数（网络协议栈/第三方库）
- **共同 AST 指纹**: 0
- **覆盖率**: 目标 N/A，参考 N/A
- **证据强度**: **未形成**
- **说明**: 参考项目包含 lwext4 嵌入式文件系统库（`ext4_balloc_alloc_block`、`jbd_journal_prepare` 等）和 smoltcp 网络栈，目标项目无此类第三方库依赖。此模块为单方持有，且属于上游开源组件，不构成同源证据。

---

## 3. 底层 AST 关键证据指纹表

| 所属板块 | 目标项目路径 | 参考项目路径 | 证据类型 | 技术说明 |
| :--- | :--- | :--- | :--- | :--- |
| 进程管理 | `src/kernel/src/task/mod.rs#is_clone_child` | `core/src/process.rs#is_clone_child` | 泛型构造误判 | AST 指纹仅为 `Some`，属于 Rust `Option<T>` 标准库变体构造器，无法区分任何涉及 `Option` 判断的实现。函数名源于 Linux `clone` 语义，非独创命名。 |
| 进程管理 | `src/kernel/src/syscall/task/wait.rs#apply` | `api/src/imp/task/wait.rs#apply` | POSIX 语义收敛 | 两者均需向 `waitid` 返回子进程 PID 和 PGID，调用链 `取组→取PGID→取PID` 是 POSIX 规范决定的必然路径。目标使用直接字段访问（`.pgid`/`.pid`），参考使用 getter 封装（`.get_pgid()`/`.get_pid()`），数据模型不同。 |
| 进程管理 | `src/kernel/src/task/ops.rs#get_process_group` | `process/src/process_group.rs#get_process_group` | 同步策略差异 | 目标使用 `PROCESS_GROUP_TABLE.read()`（RwLock 读锁），参考使用 `PROCESS_GROUP_TABLE.lock()`（Mutex 互斥锁），同步原语选择不同。目标返回 `ok_or`（错误转换），参考返回 `cloned`（克隆），错误处理语义不同。 |
| 进程管理 | `src/kernel/src/pseudofs/proc.rs#new_procfs` | `api/src/core/fs/imp/proc.rs#new_procfs` | 框架选型差异 | `"proc".into` 是 Linux procfs 标准挂载名，非独创字符串。目标使用 `SimpleFs::new_with`，参考使用 `DynamicFs::new_with`，伪文件系统框架不同。 |
| 进程管理 | `src/kernel/src/syscall/task/exit.rs#sys_exit` | `api/src/interface/task/exit.rs#sys_exit` | 系统调用命名碰撞 | 两者均委托给内部实现（`do_exit` vs `sys_exit_impl`），但内部实现路径完全不同：目标涉及 `FutexKey`/`SignalInfo`/`axtask::yield_now`，参考涉及 `close_all_file_like`/`axtask::exit`。 |
| 内存管理 | `src/kernel/src/syscall/mm/mmap.rs#sys_mmap` | `api/src/imp/mm/mmap.rs#sys_mmap` | 内存抽象差异 | 目标使用 `Backend::new_alloc`/`Backend::new_cow`/`Backend::new_file` 多态后端模型，参考使用 `MmapFlags::from_bits_truncate`/`PhysAddr::from` 直接物理地址操作。内存抽象层次不同。 |
| 内存管理 | `src/kernel/src/mm/loader.rs#load_user_app` | `core/src/mm.rs#load_user_app` | ELF 加载路径差异 | 目标使用 `ELF_LOADER.lock().load` 集中式加载器，参考使用 `ElfFile::new` 直接解析。目标涉及 `Backend::new_alloc` 内存后端分配，参考涉及 `CStr::from_bytes_with_nul` 解释器路径解析，加载流程不同。 |
| 文件系统 | `src/kernel/src/pseudofs/dev/fb.rs#ioctl` | `api/src/core/fs/imp/dev/framebuffer.rs#ioctl` | 设备控制路径差异 | 目标使用 `axdisplay::framebuffer_info` 直接获取帧缓冲信息，参考使用 `framebuffer_flush` 刷新操作。ioctl 命令处理路径不同。 |
| 文件系统 | `src/kernel/src/syscall/fs/ctl.rs#sys_utimensat` | `api/src/imp/fs/ctl.rs#sys_utimensat` | 时间处理差异 | 目标使用 `check_access` 权限校验 + `timespec` 结构体写入，参考使用 `ResolveFlags::from_bits_truncate` + `Duration::from` 时间转换。权限模型和时间抽象不同。 |
| 任务调度 | `src/kernel/src/syscall/sync/futex.rs#sys_futex` | `api/src/imp/task/futex.rs#sys_futex` | POSIX 系统调用收敛 | 两者均为 Linux futex 系统调用的标准实现入口。目标使用 `FutexKey::new_current` 键值模型，参考使用 `futex_table.entry` 表项模型，内部数据结构不同。 |

---

## 4. 核心代码调用流对比剖析

### 【进程管理/进程退出路径】

* **底层技术析理**: 
  目标项目进程退出路径为 `sys_exit` → `do_exit`，在 `do_exit` 中涉及 `FutexKey::new_current`（futex 键值生成）、`SignalInfo::new_kernel`（内核信号构造）、`child.free`/`child.is_zombie`（子进程回收）、`axtask::yield_now`（协作式调度让出）。参考项目退出路径为 `sys_exit` → `sys_exit_impl`，涉及 `close_all_file_like`（文件描述符批量关闭）、`axtask::exit`（任务终止）、`addr_clear_child_tid`（清除子线程 ID）。双方在退出流程的**资源回收顺序、信号发送机制、调度原语**上存在实质性差异。目标项目采用协作式 `yield_now` 让出 CPU，参考项目采用 `exit` 直接终止，调度语义不同。

* **代码溯源链接**:
  - 目标项目: `src/kernel/src/task/ops.rs#do_exit`
  - 参考项目: `api/src/imp/task/exit.rs#sys_exit_impl`

---

### 【内存管理/mmap 系统调用】

* **底层技术析理**:
  目标项目 `sys_mmap` 使用 `Backend` 多态后端抽象：`Backend::new_alloc`（匿名分配）、`Backend::new_cow`（写时复制）、`Backend::new_file`（文件映射）、`Backend::new_linear`（线性映射），通过 `Arc::new` 包装后端实例。参考项目 `sys_mmap` 使用 `MmapFlags::from_bits_truncate` 位标志解析 + `PhysAddr::from` 物理地址转换，直接操作物理页。目标项目采用**后端策略模式**隔离内存来源，参考项目采用**位标志 + 物理地址**的直接映射模型，内存抽象层次和扩展性设计不同。

* **代码溯源链接**:
  - 目标项目: `src/kernel/src/syscall/mm/mmap.rs#sys_mmap`
  - 参考项目: `api/src/imp/mm/mmap.rs#sys_mmap`

---

### 【文件系统/procfs 构造】

* **底层技术析理**:
  目标项目 `new_procfs` 使用 `SimpleFs::new_with("proc".into)` 构造 procfs 实例，后续通过 `SimpleDir::new_maker` 和 `NodeOpsMux::Dir` 构建目录节点。参考项目 `new_procfs` 使用 `DynamicFs::new_with("proc".into)` 构造，后续通过 `DynamicDir::builder` 链式构建。`"proc"` 字符串是 Linux procfs 标准挂载名，不构成独创证据。双方在伪文件系统框架选型（`SimpleFs` vs `DynamicFs`）、目录节点构造方式（`new_maker` vs `builder` 模式）上存在框架级差异。

* **代码溯源链接**:
  - 目标项目: `src/kernel/src/pseudofs/proc.rs#new_procfs`
  - 参考项目: `api/src/core/fs/imp/proc.rs#new_procfs`

---

### 【任务调度/futex 系统调用】

* **底层技术析理**:
  目标项目 `sys_futex` 使用 `FutexKey::new_current` 生成 futex 键值，通过 `(deadline - now).min` 计算超时时间。参考项目 `sys_futex` 使用 `futex_table.entry` 表项模型管理 futex 等待队列，通过 `axtask::yield_now` 让出 CPU。双方在 futex 的**键值生成策略、等待队列数据结构、超时计算方式**上存在差异。目标项目使用时间戳差值计算，参考项目使用协作式让出，等待机制不同。

* **代码溯源链接**:
  - 目标项目: `src/kernel/src/syscall/sync/futex.rs#sys_futex`
  - 参考项目: `api/src/imp/task/futex.rs#sys_futex`

---

## 5. 单方模块与降权项

### 仅参考项目存在的模块
| 模块 | 函数数 | 说明 |
| :--- | ---: | :--- |
| 设备驱动 | 133 | 包含 bcm PL011 UART、Raspi4 链式加载器、网络设备驱动等，目标项目无此模块 |
| 网络协议栈 | 65 | 包含 smoltcp TCP/DNS/bench 实现，目标项目无此模块 |
| 文件系统/第三方库 | 643 | 包含 lwext4 嵌入式 ext4 文件系统库，属于上游开源组件 |
| 网络协议栈/第三方库 | 130 | 包含 smoltcp 网络栈实现，属于上游开源组件 |

### 降权项说明
以下 AST 指纹在控方证据中被错误计入共同指纹，必须降权处理：

| 降权项 | 降权原因 |
| :--- | :--- |
| `Some`、`Ok`、`Err` | Rust 标准库 `Option`/`Result` 泛型变体构造器，与业务领域无关 |
| `Arc::new`、`Arc::downgrade` | Rust 标准库原子引用计数构造器，属于内存管理惯用法 |
| `Default::default` | Rust 标准库默认值 trait，属于语言级泛型构造 |
| `println!`、`writeln!`、`vec!` | Rust 标准库宏，属于语言基础设施 |
| `"proc".into` | Linux procfs 标准挂载名，属于 POSIX/行业标准字符串 |
| `sys_getpid`、`sys_fork`、`sys_exit`、`sys_execve` | POSIX/Linux 标准系统调用名，属于接口命名收敛 |
| `do_exit`（跨板块） | 目标为内核态退出逻辑，参考为用户态 shell 示例，分属不同抽象层级 |
| `sys_rt_sigprocmask`（跨板块） | 目标使用 `curr.as_thread` 路径，参考使用 `current_thread_data().signal.with_blocked_mut` 路径，访问路径完全不同 |

---

## 6. 保守结论

本次审计基于客观 AST 指纹比对，双方总体相似度仅 2.07%，双向覆盖率均低于 7%。82 个共同 AST 指纹中，大量为 Rust 标准库泛型构造（`Some`、`Ok`、`Err`、`Arc::new`）、POSIX 标准系统调用命名（`sys_getpid`、`sys_fork`、`sys_exit`）和行业标准字符串（`"proc".into`），这些元素在操作系统内核开发中具有高度必然性，不构成代码抄袭或逻辑复用的有效证据。

在可对齐的核心子系统（进程退出、内存映射、procfs 构造、futex 等待）中，双方在同步原语选择（RwLock vs Mutex）、内存抽象模型（Backend 策略模式 vs 物理地址直接操作）、伪文件系统框架（SimpleFs vs DynamicFs）、错误处理模型（VfsError vs AxError）上存在实质性架构差异。参考项目独有的设备驱动、网络协议栈、第三方库（lwext4、smoltcp）模块进一步证明双方项目范围和目标平台不同。

综合判定：**目标项目 [2026-T2026100069910232-XX] 与参考项目 [2025-undefined-清华大学] 属于独立开发，底层架构显著差异，不存在代码抄袭或逻辑复用行为。** 建议后续审查以源代码级语义比对为准，而非依赖粗粒度 AST 外部调用名指纹。

------------------------------

