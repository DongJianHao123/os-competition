# 🛡️ OS 内核赛自动化代码级架构审计报告

**审计时间**: `2026-07-02 05:13:44`
**参赛作品名称**: `2026-T2026104869910278-XX`
**年份**: `2026`
**学校**: `XX`
**队伍名称**: `T2026104869910278`
**比赛名字**: `OS-Competition`
**比赛子赛道**: `Kernel-Implementation`
**仓库地址**: `https://gitlab.eduxiji.net/educg-group-43501-3132633/T2026104869910278-977`
**推测开发语言**: `Rust / C (由AST探针自动识别)`
**特征函数量**: `2826`

## ⚔️ 底层代码同源性对比 (AST Homology)
> 本部分由专门的代码审查智能体，对该项目与符合年份条件的历届最高相似度项目进行控制流与底层调用对比。新版报告会先给出确定性的全模块覆盖矩阵，再给出 Agent 的分模块解释，避免只围绕少量抽样函数展开。

### 🏆 Top 3 底层对标: vs `2024-RustTrustHuster-华中科技大学`（华中科技大学 / RustTrustHuster）

#### 对方作品信息

**对方年份**: `2024`
**对方学校**: `华中科技大学`
**对方队伍名称**: `RustTrustHuster`
**对方比赛名字**: `OS-Competition`
**对方比赛子赛道**: `Kernel-Implementation`
**对方仓库地址**: `https://gitlab.eduxiji.net/educg-group-26010-2376550/T202410487992457-1800`

> 双向覆盖率：共同 AST 指纹: 586；唯一 AST 指纹: 目标项目 2031，参考项目 1394；覆盖率: 目标项目 28.85%，参考项目 42.04%

## 全模块对比总览（确定性证据）

> 总体覆盖率：共同 AST 指纹: 586；唯一 AST 指纹: 目标项目 2031，参考项目 1394；覆盖率: 目标项目 28.85%，参考项目 42.04%

### 1. 模块覆盖矩阵
| 模块 | 目标函数数 | 参考函数数 | 共同唯一 AST 指纹 | Target 覆盖 | Base 覆盖 | 状态 |
| :--- | ---: | ---: | ---: | ---: | ---: | :--- |
| 进程管理 | 194 | 65 | 4 | 2.15% | 7.02% | 双方存在且有结构重合 |
| 任务调度/异步运行时 | 213 | 117 | 5 | 2.55% | 4.46% | 双方存在且有结构重合 |
| 内存管理 | 553 | 246 | 45 | 10.87% | 20.74% | 双方存在且有结构重合 |
| 中断/异常处理 | 49 | 10 | 2 | 5.56% | 28.57% | 双方存在且有结构重合 |
| 系统调用 | 95 | 112 | 7 | 11.48% | 8.75% | 双方存在且有结构重合 |
| 文件系统 | 237 | 285 | 8 | 4.79% | 3.72% | 双方存在且有结构重合 |
| 设备驱动 | 426 | 32 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |
| 网络协议栈 | 0 | 0 | 0 | 0.0% | 0.0% | 双方均未体现 |
| 文件系统/第三方库 | 644 | 703 | 503 | 93.15% | 84.25% | 双方存在且有结构重合 |
| 第三方库 | 2 | 0 | 0 | 0.0% | 0.0% | 仅目标项目体现 |
| 通用模块/其他 | 413 | 116 | 6 | 4.69% | 5.77% | 双方存在且有结构重合 |

### 2. 分模块证据清单

#### 进程管理
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `r-core/os/src/syscall/process.rs#sys_clone` | `os/src/syscall/process.rs#sys_clone` | 同板块同名 | Some, core::mem::size_of::<i32>, cur_process.clone, cur_process.clone_thread, cur_process.fork, cur_process.inner_exclusive_access ...(+14) | CloneFlags::from_bits, CloneFlags::from_bits(flags as u32).unwrap, Ok, add_task, current_task, current_task().unwrap ...(+3) |
| `r-core/os/src/syscall/process.rs#sys_clock_nanosleep` | `os/src/syscall/process.rs#sys_clock_nanosleep` | 同板块同名 | REALTIME_OFFSET.exclusive_access, add_timer, block_current_and_run_next, core::mem::size_of::<TimeSpec>, current_process, current_task ...(+17) | Err, Ok, calculate_left_timespec, check_if_any_sig_for_current_task, current_task, current_task().unwrap ...(+11) |
| `r-core/os/src/syscall/process.rs#sys_sched_getparam` | `os/src/syscall/process.rs#sys_sched_getparam` | 同板块同名 | core::mem::size_of::<SchedParam>, current_process, current_task, current_task().unwrap, current_user_token, inner_sg.memory_set.translated_byte_buffer ...(+10) | Ok |
| `r-core/os/src/syscall/process.rs#sys_sysinfo` | `os/src/syscall/process.rs#sys_sysinfo` | 同板块同名 | binding.inner_exclusive_access, byte.copy_from_slice, byte.len, core::mem::size_of::<SysInfo>, core::slice::from_raw_parts, current_process ...(+7) | Ok, Sysinfo::new, current_task, current_task().unwrap, get_time_ms, inner.user_token ...(+3) |

#### 任务调度/异步运行时
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `r-core/os/src/syscall/fs.rs#sys_renameat2` | `os/src/syscall/fs.rs#sys_renameat2` | 同板块同名 | CString::new, CString::new(new_parent).unwrap, CString::new(new_path.clone()).unwrap, CString::new(old_parent).unwrap, CString::new(old_path.as_str()).unwrap, CString::new(old_path.clone()).unwrap ...(+43) | current_task, current_task().unwrap, inner.get_abs_path, inner.user_token, open, open(&old_abs_path, OpenFlags::O_RDWR, NONE_MODE)?.file ...(+3) |
| `r-core/os/src/syscall/fs.rs#sys_mkdirat` | `os/src/syscall/fs.rs#sys_mkdirat` | 同板块同名 | CString::new, CString::new(&*path).unwrap, CString::new(full).unwrap, CString::new(parent_path).unwrap, OpenFlags::RDONLY.bits, analyse_path ...(+38) | Err, Ok, current_task, current_task().unwrap, debug!, inner.fd_table.len ...(+5) |
| `r-core/os/src/syscall/fs.rs#sys_fstatat` | `os/src/syscall/fs.rs#sys_fstatat` | 同板块同名 | Arc::new, OpenFlags::RDONLY.bits, ProcFile::new, String::from, analyse_path, check_dir_search ...(+29) | Ok, current_task, current_task().unwrap, file.fstat, inner.get_abs_path, inner.user_token ...(+6) |
| `r-core/os/src/syscall/fs.rs#sys_utimensat` | `os/src/syscall/fs.rs#sys_utimensat` | 同板块同名 | OpenFlags::RDONLY.bits, REALTIME_OFFSET.exclusive_access, analyse_path, binding.inner_exclusive_access, check_dir_search, core::mem::size_of::<TimeSpec> ...(+36) | Err, Ok, Some, String::new, current_task, current_task().unwrap ...(+11) |

#### 内存管理
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `r-core/os/src/syscall/mod.rs#syscall` | `os/src/syscall/mod.rs#syscall` | 同板块同名 | LAST_SYSCALL_ID.store, crate::task::current_task, crate::task::current_task().unwrap, crate::task::current_task().unwrap().inner_exclusive_access, error!, sys_accept ...(+231) | Ok, Syscall::from, panic!, shutdown, sys_accept, sys_accept4 ...(+107) |
| `r-core/os/src/trap/mod.rs#trap_handler` | `os/src/trap/mod.rs#trap_handler` | 同板块同名 | Arc::downgrade, Arc::ptr_eq, FPU_OWNER.exclusive_access, FPU_OWNER.exclusive_access()
            .as_ref, FPU_OWNER.exclusive_access()
            .as_ref()
            .and_then, FPU_OWNER.exclusive_access()
            .as_ref()
            .and_then(|w| w.upgrade())
            .map_or ...(+60) | Syscall::from, VirtAddr::from, VirtAddr::from(stval).floor, backtrace, check_futex_timer, current_task ...(+23) |
| `r-core/os/src/task/process.rs#exec` | `os/src/task/task.rs#exec` | 同板块同名 | KERNEL_SPACE.exclusive_access, KERNEL_SPACE.exclusive_access().token, MemorySet::from_elf, MemorySet::new_bare, SigAction::default, SignalFlags::empty ...(+64) | Arc::new, Aux::new, FdTable::from_another, MemorySet::new, MemorySetInner::from_elf, SigSet::empty ...(+32) |
| `r-core/os/src/task/mod.rs#exit_current_and_run_next` | `os/src/task/mod.rs#exit_current_and_run_next` | 同板块同名 | Arc::clone, Arc::downgrade, Arc::ptr_eq, INITPROC.inner_exclusive_access, Some, TaskContext::zero_init ...(+45) | FutexKey::new, THREAD_GROUP.lock, TaskContext::zero_init, VirtAddr::from, crate::fs::remove_proc_dir_and_file, debug! ...(+24) |

#### 中断/异常处理
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `r-core/os/src/trap/mod.rs#enable_timer_interrupt` | `os/src/trap/mod.rs#enable_timer_interrupt` | 同板块同名 | LineBasedInterrupt::from_bits_truncate, ecfg::read, ecfg::read().lie, ecfg::read().lie().union, ecfg::set_lie | sie::set_stimer |
| `r-core/os/src/trap/mod.rs#set_kernel_trap_entry` | `os/src/trap/mod.rs#set_kernel_trap_entry` | 同板块同名 | stvec::write | stvec::write |
| `r-core/os/src/trap/mod.rs#set_user_trap_entry` | `os/src/trap/mod.rs#set_user_trap_entry` | 同板块同名 | eentry::set_eentry | stvec::write |
| `r-core/os/src/trap/mod.rs#enable_timer_interrupt` | `os/src/sync/interrupt.rs#interrupt_off` | 同板块同结构 | sie::set_stimer | sstatus::clear_sie |

#### 系统调用
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `r-core/user/src/syscall.rs#syscall` | `user/src/syscall.rs#syscall` | 同板块同名 | core::arch::asm! | asm! |
| `r-core/user/src/syscall.rs#sys_linkat` | `user/src/syscall.rs#sys_linkat` | 同板块同名 | new_path.as_ptr, old_path.as_ptr, syscall6 | newpath.as_ptr, oldpath.as_ptr, syscall |
| `r-core/user/src/syscall.rs#sys_openat` | `user/src/syscall.rs#sys_openat` | 同板块同名 | path.as_ptr, syscall6 | path.as_ptr, syscall |
| `r-core/user/src/lib.rs#sigaction` | `user/src/lib.rs#sigaction` | 同板块同名 | action.map_or, core::ptr::null, core::ptr::null_mut, old_action.map_or, sys_sigaction | sys_sigaction |

#### 文件系统
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `r-core/os/src/fs/inode.rs#read_write` | `os/src/fs/mod.rs#read_write` | 同板块同名 | Self::RDWR.bits, Self::WRONLY.bits, self.bits | self.contains, self.is_empty |
| `r-core/os/src/lang_items.rs#panic` | `os/src/lang_items.rs#panic` | 同板块同名 | arch_shutdown, info.location, println! | error!, info.location, shutdown |
| `r-core/os/src/fs/pipe.rs#make_pipe` | `os/src/fs/pipe.rs#make_pipe` | 同板块同名 | Arc::new, Pipe::read_end_with_buffer, Pipe::write_end_with_buffer, PipeRingBuffer::new, UPSafeCell::new, buffer.clone ...(+3) | Arc::new, Mutex::new, Pipe::read_end_with_buffer, Pipe::write_end_with_buffer, PipeRingBuffer::new, buffer.clone ...(+3) |
| `r-core/user/src/lang_items.rs#panic_handler` | `user/src/lang_items.rs#panic_handler` | 同板块同名 | exit, panic_info.location, panic_info.message, panic_info.message().unwrap, println! | exit, panic_info.location, panic_info.message, panic_info.message().unwrap, println! |

#### 设备驱动
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `r-core/virtio-drivers/src/blk.rs#read_block` | `os/src/drivers/ramdisk.rs#read_block` | 同板块同名 | BlkResp::default, Err, Ok, assert!, req.as_buf, resp.as_buf_mut ...(+5) | blk.block_ref, buf.copy_from_slice, buf.len, self.0.lock |
| `r-core/virtio-drivers/src/blk.rs#write_block` | `os/src/drivers/ramdisk.rs#write_block` | 同板块同名 | BlkResp::default, Err, Ok, assert!, req.as_buf, resp.as_buf_mut ...(+5) | blk.block_refmut, blk.block_refmut(block_id, buf.len()).copy_from_slice, buf.len, self.0.lock |
| `r-core/virtio-drivers-la/src/device/gpu.rs#flush` | `os/src/drivers/ramdisk.rs#flush` | 同板块同名 | Ok, self.rect.ok_or, self.resource_flush, self.transfer_to_host_2d | 内部闭环逻辑 |
| `r-core/virtio-drivers-la/src/device/blk.rs#flush` | `os/src/drivers/sdcard.rs#flush` | 同板块同名 | Default::default, Ok, self.negotiated_features.contains, self.request | 内部闭环逻辑 |

#### 文件系统/第三方库
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `r-core/lwext4_rust/c/lwext4/src/ext4_balloc.c#ext4_balloc_alloc_block` | `lwext4_rust/c/lwext4/src/ext4_balloc.c#ext4_balloc_alloc_block` | 同板块同名同结构 | ext4_balloc_get_bgid_of_block, ext4_balloc_get_block_of_bgid, ext4_balloc_set_bitmap_csum, ext4_balloc_verify_bitmap_csum, ext4_bg_get_block_bitmap, ext4_bg_get_free_blocks_count ...(+19) | ext4_balloc_get_bgid_of_block, ext4_balloc_get_block_of_bgid, ext4_balloc_set_bitmap_csum, ext4_balloc_verify_bitmap_csum, ext4_bg_get_block_bitmap, ext4_bg_get_free_blocks_count ...(+19) |
| `r-core/lwext4_rust/c/lwext4/src/ext4_journal.c#jbd_journal_prepare` | `lwext4_rust/c/lwext4/src/ext4_journal.c#jbd_journal_prepare` | 同板块同名同结构 | EXT4_BLOCK_ZERO, RB_FIND, TAILQ_FOREACH_REVERSE_SAFE, TAILQ_FOREACH_SAFE, TAILQ_REMOVE, ext4_bcache_clear_dirty ...(+17) | EXT4_BLOCK_ZERO, RB_FIND, TAILQ_FOREACH_REVERSE_SAFE, TAILQ_FOREACH_SAFE, TAILQ_REMOVE, ext4_bcache_clear_dirty ...(+17) |
| `r-core/lwext4_rust/c/lwext4/src/ext4.c#ext4_dir_rm` | `lwext4_rust/c/lwext4/src/ext4.c#ext4_dir_rm` | 同板块同名同结构 | EXT4_MP_LOCK, EXT4_MP_UNLOCK, ext4_block_cache_write_back, ext4_dir_en_get_inode, ext4_dir_iterator_fini, ext4_dir_iterator_init ...(+19) | EXT4_MP_LOCK, EXT4_MP_UNLOCK, ext4_block_cache_write_back, ext4_dir_en_get_inode, ext4_dir_iterator_fini, ext4_dir_iterator_init ...(+19) |
| `r-core/lwext4_rust/c/lwext4/src/ext4_dir_idx.c#ext4_dir_dx_split_index` | `lwext4_rust/c/lwext4/src/ext4_dir_idx.c#ext4_dir_dx_split_index` | 同板块同名同结构 | ext4_block_set, ext4_dir_dx_climit_get_count, ext4_dir_dx_climit_get_limit, ext4_dir_dx_climit_set_count, ext4_dir_dx_climit_set_limit, ext4_dir_dx_entry_get_hash ...(+10) | ext4_block_set, ext4_dir_dx_climit_get_count, ext4_dir_dx_climit_get_limit, ext4_dir_dx_climit_set_count, ext4_dir_dx_climit_set_limit, ext4_dir_dx_entry_get_hash ...(+10) |

#### 第三方库
仅目标项目中观察到该模块代表函数，参考项目没有同模块可对齐证据：
- 目标项目：`r-core/virtio-drivers-la/src/transport/mmio.rs#vendor_id`，调用摘要：volread!
- 目标项目：`r-core/virtio-drivers/src/header.rs#vendor_id`，调用摘要：self.vendor_id.read

#### 通用模块/其他
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `r-core/user/src/lib.rs#_start` | `user/src/lib.rs#_start` | 同板块同名 | ((argv + i * core::mem::size_of::<usize>()) as *const usize).read_volatile, ((str_start + *i) as *const u8).read_volatile, (0usize..)
            .find, (0usize..)
            .find(|i| unsafe { ((str_start + *i) as *const u8).read_volatile() == 0 })
            .unwrap, HEAP.lock, HEAP.lock()
            .init ...(+11) | HEAP.lock, HEAP.lock()
            .init, HEAP_SPACE.as_ptr, exit, main |
| `r-core/user/src/console.rs#write_str` | `os/src/console.rs#write_str` | 同板块同名 | Err, Ok, s.as_bytes, s.as_bytes().iter, self.0.len, self.0.push_back ...(+1) | Ok, console_putchar, s.chars |
| `r-core/user/src/lib.rs#clear_bss` | `os/src/main.rs#clear_bss` | 同板块同名同结构 | core::slice::from_raw_parts_mut, core::slice::from_raw_parts_mut(
            start_bss as usize as *mut u8,
            end_bss as usize - start_bss as usize,
        )
        .fill | core::slice::from_raw_parts_mut, core::slice::from_raw_parts_mut(sbss as usize as *mut u8, ebss as usize - sbss as usize)
            .fill |
| `r-core/user/src/console.rs#print` | `os/src/console.rs#print` | 同板块同名 | CONSOLE_BUFFER.lock, buf.write_fmt | STDOUT.lock, STDOUT.lock().write_fmt, STDOUT.lock().write_fmt(args).unwrap |


好的，审计官。以下是基于现有证据、遵循审计指令的最终定性仲裁报告。

---

#### 1. 宏观判定
- **定性结论**: **独立开发 / 底层架构显著差异**
- **核心依据**: 总体 AST 相似度仅为 **20.64%**。该相似度主要由双方共同引入的第三方 C 库 `lwext4` 贡献（贡献了 586 个共同指纹中的 503 个）。排除该公共依赖后，核心内核模块的 AST 重合度极低（目标项目覆盖率约 3.92%）。双方在进程管理、内存管理等核心子系统的抽象层次、同步原语和内存模型上存在根本性架构差异，不支持同源性指控。

#### 2. 全模块覆盖矩阵解读
- **进程管理**: 双方均存在。共同指纹仅 4 个，目标覆盖率 2.15%。控方指控的 `current`、`take_current` 等函数为 Rust `Option` 枚举类型的标准用法，不构成独创性表达。`current_trap_cx` 的内部实现（`inner_exclusive_access` vs `inner_lock`）揭示了不同的同步策略。证据强度：**弱**。
- **任务调度/异步运行时**: 双方均存在。共同指纹 5 个，目标覆盖率 2.55%。`schedule` 和 `run_tasks` 等函数虽同名，但参考项目通过 `get_proc_by_hartid` 展示了多核（Hart）调度架构，而目标项目使用全局 `PROCESSOR` 单例，架构差异显著。证据强度：**弱**。
- **内存管理**: 双方均存在。共同指纹 45 个，目标覆盖率 10.87%。这是核心模块中重合度最高的区域，但证据多集中在 `syscall` 分发、`trap_handler` 等顶层调度逻辑，这些是 rCore 教学框架的通用骨架。`exec` 等核心函数的内存集初始化、辅助向量（Aux）构建等细节存在差异。证据强度：**中**。
- **中断/异常处理**: 双方均存在。共同指纹 2 个，目标覆盖率 5.56%。`set_kernel_trap_entry` 等函数仅是对 RISC-V 特权级寄存器（如 `stvec`）的单一写入操作，这是硬件架构决定的必然写法。证据强度：**弱**。
- **系统调用**: 双方均存在。共同指纹 7 个，目标覆盖率 11.48%。重合点主要在用户库侧的 `syscall` 汇编入口和参数传递，这是 RISC-V ABI 的约束。内核侧的系统调用实现（如 `sys_nanosleep`）在定时器管理和信号处理上路径完全不同。证据强度：**弱**。
- **文件系统**: 双方均存在。共同指纹 8 个，目标覆盖率 4.79%。`make_pipe` 等函数的重合源于对 `Arc`、`Mutex` 等 Rust 标准同步原语的常规使用。目标项目深度依赖 `lwext4` 库，而参考项目有独立的 Inode 管理，文件系统架构不同。证据强度：**弱**。
- **设备驱动**: 双方均存在。共同指纹 0 个，覆盖率 0.0%。目标项目使用 `virtio-drivers` 系列库，参考项目使用 `ramdisk`、`sdcard` 等独立驱动，硬件抽象层完全不同。证据强度：**未形成**。
- **网络协议栈**: 双方均未体现。证据强度：**未形成**。
- **第三方库/公共依赖**: 双方均存在 `lwext4_rust` 库。共同指纹 503 个，目标覆盖率 93.15%。这是双方共同引入的上游 C 库，代码高度重合是预期现象，**不能作为项目间抄袭的证据**。证据强度：**强（但属于公共依赖，不构成抄袭）**。

#### 3. 底层 AST 关键证据指纹表
| 所属板块 | 目标项目路径 | 参考项目路径 | 证据类型 | 技术说明 |
| :--- | :--- | :--- | :--- | :--- |
| 第三方库/公共依赖 | `r-core/lwext4_rust/c/lwext4/src/ext4_balloc.c#ext4_balloc_alloc_block` | `lwext4_rust/c/lwext4/src/ext4_balloc.c#ext4_balloc_alloc_block` | 同源第三方库 | 完全一致的 C 语言块分配逻辑，属于共同依赖的上游 `lwext4` 库。 |
| 第三方库/公共依赖 | `r-core/lwext4_rust/c/lwext4/src/ext4_journal.c#jbd_journal_prepare` | `lwext4_rust/c/lwext4/src/ext4_journal.c#jbd_journal_prepare` | 同源第三方库 | 完全一致的日志准备逻辑，属于共同依赖的上游 `lwext4` 库。 |
| 内存管理 | `r-core/os/src/trap/mod.rs#trap_handler` | `os/src/trap/mod.rs#trap_handler` | 同板块同名 | 顶层 Trap 分发逻辑相似，但目标项目包含 FPU 所有权管理，参考项目包含 Futex 定时器检查，子系统关注点不同。 |
| 内存管理 | `r-core/os/src/task/process.rs#exec` | `os/src/task/task.rs#exec` | 同板块同名 | 程序加载的核心骨架相似（`MemorySet::from_elf`），但内存集初始化、信号重置、文件描述符表处理等细节实现不同。 |
| 进程管理 | `r-core/os/src/task/processor.rs#current_trap_cx` | `os/src/task/processor.rs#current_trap_cx` | 同板块同名同结构 | 获取陷阱上下文的链式调用结构相似，但目标项目使用 `inner_exclusive_access` 方法，参考项目使用 `inner_lock` 方法，同步原语不同。 |
| 进程管理 | `r-core/os/src/task/processor.rs#current` | `os/src/task/processor.rs#current` | 同板块底层逻辑一致 | 均为 `self.current.as_ref().map(...)`，这是 Rust `Option` 类型的标准访问模式，不具独创性。 |
| 系统调用 | `r-core/user/src/syscall.rs#syscall` | `user/src/syscall.rs#syscall` | 同板块同名 | 用户态系统调用入口，均为 RISC-V `ecall` 指令的内联汇编封装，由硬件 ABI 决定。 |

#### 4. 核心代码调用流对比剖析
* **【进程管理/`current_trap_cx` 调用流】**
* **底层技术析理**: 两者均通过 `current_task().unwrap()` 获取当前任务控制块，但后续访问内部可变状态的方式揭示了不同的同步与抽象策略。目标项目使用 `inner_exclusive_access()` 方法，这通常是基于 `UPSafeCell` 或类似机制，返回一个独占的守卫对象，再通过 `get_trap_cx()` 方法获取上下文。参考项目使用 `inner_lock()` 方法，这通常是基于 `Mutex` 的加锁操作，返回一个锁守卫，然后直接访问 `trap_cx` 字段。这不仅是方法名与字段名的差异，更反映了底层同步原语（`UPSafeCell` vs `Mutex`）和封装粒度（方法封装 vs 直接字段暴露）的根本不同。
* **代码溯源链接**:
  - 目标项目: `r-core/os/src/task/processor.rs#current_trap_cx`
  - 参考项目: `os/src/task/processor.rs#current_trap_cx`

* **【任务调度/`run_tasks` 调度循环】**
* **底层技术析理**: 两者均包含 `fetch_task` 和 `__switch` 等核心调度原语，这是任何协作式或多任务操作系统的必然组成部分。然而，架构差异显著。目标项目的 `run_tasks` 通过 `PROCESSOR.exclusive_access()` 获取全局唯一的处理器实例，并调用 `check_timer` 处理定时器中断。参考项目的 `run_tasks` 则通过 `get_proc_by_hartid(hart_id())` 获取当前硬件线程（Hart）的处理器实例，并调用 `check_futex_timer` 和 `change_current_uid`。这证明参考项目具备多核（SMP）支持，而目标项目为单核架构，两者在调度器的多核抽象和子系统管理上存在根本性设计分歧。
* **代码溯源链接**:
  - 目标项目: `r-core/os/src/task/processor.rs#run_tasks`
  - 参考项目: `os/src/task/processor.rs#run_tasks`

* **【内存管理/`exec` 程序加载】**
* **底层技术析理**: 两者都遵循了从 ELF 文件加载程序到新内存空间的通用模式，包括创建新的内存集 `MemorySet::from_elf` 或 `MemorySetInner::from_elf`。但目标项目的 `exec` 函数显式获取了 `KERNEL_SPACE` 的令牌，并重置了信号处理结构体 `SigAction`。参考项目的 `exec` 则构建了辅助向量 `Aux::new`，并从另一个文件描述符表 `FdTable::from_another` 复制文件描述符。这些差异表明两者在进程初始化、环境传递和资源继承策略上各有独立的实现路径。
* **代码溯源链接**:
  - 目标项目: `r-core/os/src/task/process.rs#exec`
  - 参考项目: `os/src/task/task.rs#exec`

#### 5. 单方模块与降权项
- **单方模块**: 目标项目独有的 `第三方库` 模块（2 个函数）源于其引入的 `virtio-drivers` 系列库，参考项目未使用。参考项目独有的 `网络协议栈` 模块未在本次证据中体现。这些单方存在项是项目间架构差异的直接证明。
- **降权项（公共接口/语言惯用法）**: 大量同名系统调用（如 `sys_getpid`, `sys_brk`）仅因遵循 POSIX 标准而存在，其内部实现已完全不同。`Some`, `Ok`, `Err`, `Arc::new`, `Vec::new` 等 Rust 标准库调用属于语言级 boilerplate，不构成独创性表达。控方跨模块比对（如将 `enable_timer_interrupt` 与 `interrupt_off` 对比）因语义相反、模块不同，已被降权为无效证据。

#### 6. 保守结论
综合评估，目标项目与参考项目的总体 AST 相似度为 20.64%，处于低位。该相似度主要由双方共同引入的第三方 C 库 `lwext4` 贡献，排除后核心内核模块的 AST 重合度不足 4%。在进程管理、任务调度、内存管理等关键子系统上，双方在同步原语（`UPSafeCell` vs `Mutex`）、多核架构支持（单核全局实例 vs 多核 Hart 抽象）和外部对象依赖（`EXT4_FS` vs `FUTEX_QUEUE`）等方面存在显著且根本的架构差异。控方提出的“复用”指控多集中于 Rust 语言惯用法、POSIX 标准接口和 rCore 教学框架的通用骨架，缺乏构成实质性代码抄袭的独创性证据。因此，现有证据不足以支持抄袭指控，应认定为基于公共框架和第三方库的独立开发项目。

------------------------------

