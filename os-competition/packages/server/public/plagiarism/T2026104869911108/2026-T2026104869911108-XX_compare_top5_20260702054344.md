# 🛡️ OS 内核赛自动化代码级架构审计报告

**审计时间**: `2026-07-02 05:43:44`
**参赛作品名称**: `2026-T2026104869911108-XX`
**年份**: `2026`
**学校**: `XX`
**队伍名称**: `T2026104869911108`
**比赛名字**: `OS-Competition`
**比赛子赛道**: `Kernel-Implementation`
**仓库地址**: `https://gitlab.eduxiji.net/educg-group-43501-3132633/T2026104869911108-2514`
**推测开发语言**: `Rust / C (由AST探针自动识别)`
**特征函数量**: `1836`

## ⚔️ 底层代码同源性对比 (AST Homology)
> 本部分由专门的代码审查智能体，对该项目与符合年份条件的历届最高相似度项目进行控制流与底层调用对比。新版报告会先给出确定性的全模块覆盖矩阵，再给出 Agent 的分模块解释，避免只围绕少量抽样函数展开。

### 🏆 Top 5 底层对标: vs `2025-AstranciA-哈尔滨工业大学深圳`（哈尔滨工业大学（深圳） / AstranciA）

#### 对方作品信息

**对方年份**: `2025`
**对方学校**: `哈尔滨工业大学（深圳）`
**对方队伍名称**: `AstranciA`
**对方比赛名字**: `OS-Competition`
**对方比赛子赛道**: `Kernel-Implementation`
**对方仓库地址**: `https://gitlab.eduxiji.net/educg-group-36002-2710490/T202518123995667-1436`

> 双向覆盖率：共同 AST 指纹: 872；唯一 AST 指纹: 目标项目 1534，参考项目 3750；覆盖率: 目标项目 56.84%，参考项目 23.25%

## 全模块对比总览（确定性证据）

> 总体覆盖率：共同 AST 指纹: 872；唯一 AST 指纹: 目标项目 1534，参考项目 3750；覆盖率: 目标项目 56.84%，参考项目 23.25%

### 1. 模块覆盖矩阵
| 模块 | 目标函数数 | 参考函数数 | 共同唯一 AST 指纹 | Target 覆盖 | Base 覆盖 | 状态 |
| :--- | ---: | ---: | ---: | ---: | ---: | :--- |
| 进程管理 | 75 | 112 | 31 | 41.89% | 33.7% | 双方存在且有结构重合 |
| 任务调度/异步运行时 | 356 | 455 | 162 | 63.28% | 50.47% | 双方存在且有结构重合 |
| 内存管理 | 270 | 343 | 150 | 58.37% | 48.7% | 双方存在且有结构重合 |
| 中断/异常处理 | 53 | 71 | 42 | 89.36% | 73.68% | 双方存在且有结构重合 |
| 系统调用 | 79 | 136 | 1 | 1.67% | 0.86% | 双方存在且有结构重合 |
| 文件系统 | 458 | 620 | 172 | 44.68% | 35.46% | 双方存在且有结构重合 |
| 设备驱动 | 76 | 142 | 59 | 85.51% | 46.83% | 双方存在且有结构重合 |
| 网络协议栈 | 10 | 61 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |
| 文件系统/第三方库 | 22 | 775 | 10 | 45.45% | 1.57% | 双方存在且有结构重合 |
| 网络协议栈/第三方库 | 99 | 217 | 90 | 98.9% | 46.63% | 双方存在且有结构重合 |
| 第三方库 | 74 | 1155 | 2 | 3.08% | 0.21% | 双方存在且有结构重合 |
| 通用模块/其他 | 264 | 673 | 144 | 60.76% | 26.52% | 双方存在且有结构重合 |

### 2. 分模块证据清单

#### 进程管理
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `api/src/imp/task/exit.rs#do_exit` | `AstrancE/ulib/axmono/src/task/exit.rs#do_exit` | 同板块同名 | Arc::strong_count, FD_TABLE.clear, SignalInfo::new, UserPtr::<Pid>::from, axtask::exit, axtask::yield_now ...(+31) | FD_TABLE.clear, SigInfo_::Child, SigStatus::ExitCode, UserPtr::<Pid>::from, axtask::exit, clear_child_tid.get ...(+21) |
| `api/src/imp/task/execve.rs#sys_execve` | `AstrancE/ulib/axmono/src/syscall/process.rs#sys_execve` | 同板块同名 | Err, Ok, arg.get_as_str, arg.get_as_str().map, args.is_empty, args.push ...(+40) | Err, char_ptr_to_str, err.into, str_vec_ptr_to_str, str_vec_ptr_to_str(argv as *const *const c_char)?
        .into_iter, str_vec_ptr_to_str(argv as *const *const c_char)?
        .into_iter()
        .map ...(+6) |
| `api/src/imp/task/wait.rs#sys_waitpid` | `AstrancE/ulib/axmono/src/task/wait.rs#sys_waitpid` | 同板块同名 | Err, Ok, WaitOptions::from_bits_truncate, WaitPid::Pgid, WaitPid::Pid, child.data::<ProcessData> ...(+25) | Err, Ok, WaitOptions::from_bits_truncate, WaitPid::Pgid, WaitPid::Pid, child.data::<ProcessData> ...(+27) |
| `arceos/modules/axtask/src/run_queue.rs#exit_current` | `AstrancE/modules/axtask/src/run_queue.rs#exit_current` | 同板块同名同结构 | EXITED_TASKS.current_ref_mut_raw, EXITED_TASKS.current_ref_mut_raw().clear, EXITED_TASKS.current_ref_mut_raw().push_back, WAIT_FOR_EXIT.current_ref_mut_raw, WAIT_FOR_EXIT.current_ref_mut_raw().notify_one, assert! ...(+8) | EXITED_TASKS.current_ref_mut_raw, EXITED_TASKS.current_ref_mut_raw().clear, EXITED_TASKS.current_ref_mut_raw().push_back, WAIT_FOR_EXIT.current_ref_mut_raw, WAIT_FOR_EXIT.current_ref_mut_raw().notify_one, assert! ...(+8) |

#### 任务调度/异步运行时
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `arceos/modules/axlog/src/lib.rs#log` | `AstrancE/modules/axlog/src/lib.rs#log` | 同板块同名同结构 | cfg_if::cfg_if!, record.level, record.line, record.line().unwrap_or, record.metadata, record.target ...(+1) | cfg_if::cfg_if!, record.level, record.line, record.line().unwrap_or, record.metadata, record.target ...(+1) |
| `arceos/modules/axtask/src/run_queue.rs#switch_to` | `AstrancE/modules/axtask/src/run_queue.rs#switch_to` | 同板块同名 | (*prev_ctx_ptr).switch_to, Arc::downgrade, CurrentTask::set_current, PREV_TASK.current_ref_mut_raw, assert!, clear_prev_task_on_cpu ...(+8) | (*prev_ctx_ptr).switch_to, Arc::downgrade, CurrentTask::set_current, PREV_TASK.current_ref_mut_raw, assert!, clear_prev_task_on_cpu ...(+7) |
| `arceos/modules/axhal/src/arch/aarch64/context.rs#fpstate_switch` | `AstrancE/modules/axhal/src/arch/aarch64/context.rs#fpstate_switch` | 同板块同名同结构 | naked_asm! | naked_asm! |
| `arceos/modules/axfs/src/root.rs#init_rootfs` | `AstrancE/modules/axfs/src/root.rs#init_rootfs` | 同板块同名 | "/".into, Arc::new, CURRENT_DIR.init_new, CURRENT_DIR_PATH.init_new, Mutex::new, ROOT_DIR.clone ...(+16) | "/".into, Arc::new, CURRENT_DIR.init_new, CURRENT_DIR_PATH.init_new, Mutex::new, PROC_ROOT.init_once ...(+20) |

#### 内存管理
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `arceos/modules/axmm/src/aspace.rs#populate_area` | `AstrancE/modules/axmm/src/aspace/mod.rs#populate_area` | 同板块同名 | Err, Ok, PageIterWrapper::new, PageIterWrapper::new(
                    start.align_down(align),
                    end.align_up(align).min(area.end()),
                    align,
                )
                .unwrap, Self::handle_cow_fault, _access_flags.contains ...(+14) | Err, Ok, PageIter4K::new, PageIter4K::new(start, area.end().min(end)).unwrap, area.backend, area.backend().clone ...(+9) |
| `arceos/modules/axruntime/src/lib.rs#rust_main` | `AstrancE/modules/axruntime/src/lib.rs#rust_main` | 同板块同名 | INITED_CPUS.fetch_add, ax_println!, axdisplay::init_display, axdriver::init_drivers, axfs::init_filesystems, axhal::mem::memory_regions ...(+20) | INITED_CPUS.fetch_add, ax_println!, axdisplay::init_display, axdriver::init_drivers, axfs::init_filesystems, axhal::mem::memory_regions ...(+24) |
| `arceos/modules/axdriver/src/bus/pci.rs#config_pci_device` | `AstrancE/modules/axdriver/src/bus/pci.rs#config_pci_device` | 同板块同名同结构 | Ok, allocator
                    .as_mut, allocator
                    .as_mut()
                    .expect, allocator
                    .as_mut()
                    .expect("No memory ranges available for PCI BARs!")
                    .alloc, allocator
                    .as_mut()
                    .expect("No memory ranges available for PCI BARs!")
                    .alloc(size as _)
                    .ok_or, debug! ...(+7) | Ok, allocator
                    .as_mut, allocator
                    .as_mut()
                    .expect, allocator
                    .as_mut()
                    .expect("No memory ranges available for PCI BARs!")
                    .alloc, allocator
                    .as_mut()
                    .expect("No memory ranges available for PCI BARs!")
                    .alloc(size as _)
                    .ok_or, debug! ...(+7) |
| `arceos/modules/axhal/src/arch/riscv/context.rs#switch_to` | `AstrancE/modules/axhal/src/arch/riscv/context.rs#switch_to` | 同板块同名同结构 | clear_fp_registers, context_switch, panic!, restore_fp_registers, save_fp_registers, sstatus::read ...(+5) | clear_fp_registers, context_switch, panic!, restore_fp_registers, save_fp_registers, sstatus::read ...(+5) |

#### 中断/异常处理
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `arceos/modules/axhal/src/arch/aarch64/trap.rs#handle_sync_exception` | `AstrancE/modules/axhal/src/arch/aarch64/trap.rs#handle_sync_exception` | 同板块同名 | ESR_EL1.extract, crate::trap::handle_syscall, crate::trap::post_trap_callback, debug!, esr.read, esr.read_as_enum ...(+6) | ESR_EL1.extract, crate::trap::handle_syscall, crate::trap::post_trap_callback, debug!, esr.read, esr.read_as_enum ...(+8) |
| `arceos/modules/axhal/src/platform/riscv64_qemu_virt/irq.rs#register_handler` | `AstrancE/modules/axhal/src/platform/riscv64_qemu_virt/irq.rs#register_handler` | 同板块同名同结构 | with_cause! | with_cause! |
| `arceos/modules/axhal/src/platform/loongarch64_qemu_virt/irq.rs#set_enable` | `AstrancE/modules/axhal/src/platform/loongarch64_2k1000/irq.rs#set_enable` | 同板块同名同结构 | ecfg::read, ecfg::read().lie, ecfg::set_lie | ecfg::read, ecfg::read().lie, ecfg::set_lie |
| `arceos/modules/axhal/src/irq.rs#register_handler_common` | `AstrancE/modules/axhal/src/irq.rs#register_handler_common` | 同板块同名同结构 | IRQ_HANDLER_TABLE.register_handler, set_enable, warn! | IRQ_HANDLER_TABLE.register_handler, set_enable, warn! |

#### 系统调用
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `api/src/imp/net.rs#sys_socket` | `AstrancE/api/arceos_posix_api/src/imp/net.rs#sys_socket` | 同板块同名 | Err, Mutex::new, Ok, Socket::Tcp, Socket::Udp, TcpSocket::new ...(+4) | debug!, syscall_body! |
| `api/src/imp/net.rs#sys_recvfrom` | `AstrancE/api/arceos_posix_api/src/imp/net.rs#sys_recvfrom` | 同板块同名 | Ok, Socket::from_fd, buf.get_as_mut_slice, socket.recvfrom, socket.set_nonblocking, write_sockaddr | debug!, syscall_body! |
| `api/src/imp/mm/shm.rs#sys_shmctl` | `AstrancE/ulib/axmono/src/syscall/ipc.rs#sys_shmctl` | 同板块同名 | Err, Ok, SHM_MANAGER.lock, Some, debug!, manager.by_id.remove ...(+3) | axmm_shm_ctl, axmm_shm_ctl(shmid as usize, cmd, buf as usize)
        .map, axmm_shm_ctl(shmid as usize, cmd, buf as usize)
        .map(|_| 0 as isize) // Return 0 on success
        .map_err, debug! |
| `api/src/imp/net.rs#sys_sendto` | `AstrancE/api/arceos_posix_api/src/imp/net.rs#sys_sendto` | 同板块同名 | Ok, Socket::from_fd, SocketAddr::read_from_user, buf.get_as_slice, dest_addr.is_null, socket.send ...(+1) | debug!, syscall_body! |

#### 文件系统
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `arceos/modules/axfs/src/fops.rs#_open_at` | `AstrancE/modules/axfs/src/fops.rs#_open_at` | 同板块同名 | Err, Ok, WithCap::new, attr.is_dir, attr.perm, ax_err! ...(+10) | Err, Ok, WithCap::new, attr.is_dir, attr.perm, ax_err! ...(+10) |
| `api/src/imp/fs/fd_ops.rs#sys_openat` | `AstrancE/api/arceos_posix_api/src/imp/fs.rs#sys_openat` | 同板块同名 | Directory::from_fd, Directory::new, Directory::new(
        dir.map_or_else(
            || axfs::fops::Directory::open_dir(path, &opts),
            |dir| dir.inner().open_dir_at(path, &opts),
        )?,
        real_path.to_string(),
    )
    .add_to_fd_table, File::new, File::new(r?, real_path.to_string()).add_to_fd_table, Ok ...(+21) | Directory::from_fd, Directory::from_fd(dirfd)
        .and_then, Directory::from_fd(dirfd)
        .and_then(|dir| {
            add_file_or_directory_fd(
                |filename, options| dir.inner.lock().open_file_at(filename, options),
                |filename, options| dir.inner.lock().open_dir_at(filename, options),
                filename,
                &flags_to_options(flags, mode),
            )
        })
        .unwrap_or_else, add_file_or_directory_fd, char_ptr_to_str, debug! ...(+7) |
| `arceos/modules/axfs/src/dev.rs#write_one` | `AstrancE/modules/axfs/src/dev.rs#write_one` | 同板块同名 | Ok, buf.len, buf.len().min, buf[0..BLOCK_SIZE].to_vec, data[start..start + count].copy_from_slice, self.dev.read_block ...(+1) | Ok, buf.len, buf.len().min, data[start..start + count].copy_from_slice, self.dev.read_block, self.dev.write_block |
| `api/src/imp/fs/mount.rs#sys_mount` | `AstrancE/api/arceos_posix_api/src/imp/fs.rs#sys_mount` | 同板块同名 | Err, Ok, check_mounted, debug!, fs_type.get_as_str, handle_file_path ...(+5) | Ok, debug! |

#### 设备驱动
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `arceos/modules/axdriver/src/lib.rs#init_drivers` | `AstrancE/modules/axdriver/src/lib.rs#init_drivers` | 同板块同名 | AllDevices::default, all_devs.block.iter, all_devs.block.iter().enumerate, all_devs.display.iter, all_devs.display.iter().enumerate, all_devs.net.iter ...(+5) | AllDevices::default, all_devs.block.iter_mut, all_devs.block.iter_mut().enumerate, all_devs.display.iter_mut, all_devs.display.iter_mut().enumerate, all_devs.net.iter_mut ...(+14) |
| `arceos/modules/axdriver/src/virtio.rs#probe_pci` | `AstrancE/modules/axdriver/src/virtio.rs#probe_pci` | 同板块同名同结构 | D::try_new, Some, axdriver_virtio::probe_pci_device::<VirtIoHalImpl>, warn! | D::try_new, Some, axdriver_virtio::probe_pci_device::<VirtIoHalImpl>, warn! |
| `arceos/modules/axdriver/src/virtio.rs#probe_mmio` | `AstrancE/modules/axdriver/src/virtio.rs#probe_mmio` | 同板块同名 | D::try_new, Some, axdriver_virtio::probe_mmio_device, base_vaddr.as_mut_ptr, mmio_base.into, phys_to_virt ...(+1) | D::try_new, Some, axdriver_virtio::probe_mmio_device, base_vaddr.as_mut_ptr, mmio_base.into, phys_to_virt ...(+1) |
| `arceos/modules/axdriver/src/bus/mmio.rs#probe_bus_devices` | `AstrancE/modules/axdriver/src/bus/mmio.rs#probe_bus_devices` | 同板块同名同结构 | for_each_drivers! | for_each_drivers! |

#### 网络协议栈
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `api/src/socket.rs#write_to_user` | `AstrancE/ulib/axlibc/c/network.c#inet_pton` | 同模块上下文对齐/差异观察 | Err, Ok, addr.get_as_mut, addr.is_null, core::ptr::copy_nonoverlapping, self.flowinfo ...(+7) | hexval, inet_pton, isdigit, memmove |
| `api/src/socket.rs#write_to_user` | `AstrancE/api/arceos_posix_api/src/imp/net.rs#shutdown` | 同模块上下文对齐/差异观察 | Err, Ok, addr.get_as_mut, addr.is_null, core::ptr::copy_nonoverlapping, self.ip ...(+5) | Ok, tcpsocket.lock, tcpsocket.peer_addr, tcpsocket.shutdown, udpsocket.lock, udpsocket.peer_addr ...(+1) |
| `api/src/imp/net.rs#gather_iovs` | `AstrancE/ulib/axlibc/c/socket.c#accept4` | 同模块上下文对齐/差异观察 | (0..=1024).contains, Err, Ok, UserConstPtr::<iovec>::from, UserConstPtr::<iovec>::from(iov).get_as_slice, UserConstPtr::<u8>::from ...(+6) | accept, fcntl |
| `api/src/imp/net.rs#write_int_opt` | `AstrancE/api/arceos_posix_api/src/imp/net.rs#recvfrom` | 同模块上下文对齐/差异观察 | Err, Ok, UserPtr::<c_int>::from, UserPtr::<c_int>::from(optval.address().as_usize()).get_as_mut, optlen.get_as_mut, optlen.is_null ...(+4) | Ok, Some, tcpsocket.lock, tcpsocket.lock().recv, tcpsocket.lock().recv(buf).map, udpsocket
                .lock ...(+2) |

#### 文件系统/第三方库
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `arceos/modules/axfs/src/fs/lwext4_rust.rs#get_attr` | `AstrancE/modules/axfs/src/fs/lwext4_rust.rs#get_attr` | 同板块同名 | <i32 as TryInto<AxError>>::try_into, <i32 as TryInto<AxError>>::try_into(e).unwrap, EXT4_LOCK.lock, Ok, VfsNodeAttr::new, VfsNodePerm::from_bits_truncate ...(+13) | <i32 as TryInto<AxError>>::try_into, <i32 as TryInto<AxError>>::try_into(e).unwrap, Ok, VfsNodeAttr::new, VfsNodePerm::from_bits_truncate, file.file_close ...(+25) |
| `arceos/modules/axfs/src/fs/lwext4_rust.rs#create` | `AstrancE/modules/axfs/src/fs/lwext4_rust.rs#create` | 同板块同名 | EXT4_LOCK.lock, Ok, e.try_into, e.try_into().unwrap, file.check_inode_exist, file.dir_mk ...(+13) | Ok, e.try_into, e.try_into().unwrap, file.check_inode_exist, file.dir_mk, file.dir_mk(fpath)
                    .map ...(+12) |
| `arceos/modules/axfs/src/fs/lwext4_rust.rs#read_dir` | `AstrancE/crates/axfs_crates/axfs_procfs/src/dir.rs#read_dir` | 同板块同名 | EXT4_LOCK.lock, Ok, VfsDirEntry::new, core::str::from_utf8, core::str::from_utf8(iname.unwrap()).unwrap, dirents.iter_mut ...(+14) | BTreeMap::new, Ok, VfsDirEntry::new, all_children.insert, all_children.iter, all_children.iter().skip ...(+10) |
| `arceos/modules/axfs/src/fs/lwext4_rust.rs#lookup` | `AstrancE/modules/axfs/src/fs/lwext4_rust.rs#lookup` | 同板块同名 | Arc::new, EXT4_LOCK.lock, Err, Ok, Self::new, file.check_inode_exist ...(+6) | Arc::new, Err, Ok, Self::new, debug!, file.check_inode_exist ...(+6) |

#### 网络协议栈/第三方库
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `arceos/modules/axnet/src/smoltcp_impl/tcp.rs#connect` | `AstrancE/modules/axnet/src/smoltcp_impl/tcp.rs#connect` | 同板块同名 | Err, Ok, SOCKET_SET
                .with_socket_mut::<tcp::Socket, _, _>, SOCKET_SET.add, SocketSetWrapper::new_tcp_socket, Some ...(+25) | Err, Ok, Ok::<(IpEndpoint, IpEndpoint), AxError>, SOCKET_SET
                .with_socket_mut::<tcp::Socket, _, _>, SOCKET_SET.add, SocketSetWrapper::new_tcp_socket ...(+26) |
| `arceos/modules/axnet/src/smoltcp_impl/dns.rs#query` | `AstrancE/modules/axnet/src/smoltcp_impl/dns.rs#query` | 同板块同名同结构 | Err, Ok, SOCKET_SET
            .with_socket_mut::<dns::Socket, _, _>, SOCKET_SET
            .with_socket_mut::<dns::Socket, _, _>(handle, |socket| {
                socket.start_query(iface.lock().context(), name, query_type)
            })
            .map_err, SOCKET_SET.poll_interfaces, SOCKET_SET.with_socket_mut::<dns::Socket, _, _> ...(+12) | Err, Ok, SOCKET_SET
            .with_socket_mut::<dns::Socket, _, _>, SOCKET_SET
            .with_socket_mut::<dns::Socket, _, _>(handle, |socket| {
                socket.start_query(iface.lock().context(), name, query_type)
            })
            .map_err, SOCKET_SET.poll_interfaces, SOCKET_SET.with_socket_mut::<dns::Socket, _, _> ...(+12) |
| `arceos/modules/axnet/src/smoltcp_impl/bench.rs#bench_transmit_bandwidth` | `AstrancE/modules/axnet/src/smoltcp_impl/bench.rs#bench_transmit_bandwidth` | 同板块同名同结构 | (current_time - past_time).secs, AxNetTxToken::consume, InterfaceWrapper::current_time, info!, self.transmit, tx_buf[0..12].fill ...(+2) | (current_time - past_time).secs, AxNetTxToken::consume, InterfaceWrapper::current_time, info!, self.transmit, tx_buf[0..12].fill ...(+2) |
| `arceos/modules/axnet/src/smoltcp_impl/tcp.rs#shutdown` | `AstrancE/modules/axnet/src/smoltcp_impl/tcp.rs#shutdown` | 同板块同名同结构 | LISTEN_TABLE.unlisten, Ok, SOCKET_SET.poll_interfaces, SOCKET_SET.with_socket_mut::<tcp::Socket, _, _>, debug!, self.handle.get ...(+9) | LISTEN_TABLE.unlisten, Ok, SOCKET_SET.poll_interfaces, SOCKET_SET.with_socket_mut::<tcp::Socket, _, _>, debug!, self.handle.get ...(+9) |

#### 第三方库
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `crates/axprocess/src/lib.rs#data` | `AstrancE/crates/virtio-drivers/src/device/sound.rs#data` | 同板块同名 | Box::new, Some | 内部闭环逻辑 |
| `crates/axprocess/src/lib.rs#parent` | `AstrancE/crates/axfs_crates/axfs_vfs/src/lib.rs#parent` | 同板块同名 | self.parent.lock, self.parent.lock().as_ref, self.parent.lock().as_ref().and_then | 内部闭环逻辑 |
| `crates/axsignal/src/lib.rs#try_from` | `AstrancE/crates/virtio-drivers/src/transport/mod.rs#try_from` | 同板块同名 | Ok | DeviceTypeError::InvalidDeviceType, Err, Ok |
| `crates/axsignal/src/lib.rs#contains` | `AstrancE/crates/axmm_crates/memory_addr/src/range.rs#contains` | 同板块同名 | signo.index | 内部闭环逻辑 |

#### 通用模块/其他
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `arceos/modules/axhal/src/mem.rs#kernel_image_regions` | `AstrancE/modules/axhal/src/mem.rs#kernel_image_regions` | 同板块同名 | (_sbss as usize).into, (_sdata as usize).into, (_srodata as usize).into, (_stext as usize).into, (boot_stack as usize).into, [
        MemRegion {
            paddr: virt_to_phys((_stext as usize).into()),
            size: _etext as usize - _stext as usize,
            flags: MemRegionFlags::RESERVED | MemRegionFlags::READ | MemRegionFlags::EXECUTE,
            name: ".text",
        },
        MemRegion {
            paddr: virt_to_phys((_srodata as usize).into()),
            size: _erodata as usize - _srodata as usize,
            flags: MemRegionFlags::RESERVED | MemRegionFlags::READ,
            name: ".rodata",
        },
        MemRegion {
            paddr: virt_to_phys((_sdata as usize).into()),
            size: _edata as usize - _sdata as usize,
            flags: MemRegionFlags::RESERVED | MemRegionFlags::READ | MemRegionFlags::WRITE,
            name: ".data .tdata .tbss .percpu",
        },
        MemRegion {
            paddr: virt_to_phys((boot_stack as usize).into()),
            size: boot_stack_top as usize - boot_stack as usize,
            flags: MemRegionFlags::RESERVED | MemRegionFlags::READ | MemRegionFlags::WRITE,
            name: "boot stack",
        },
        MemRegion {
            paddr: virt_to_phys((_sbss as usize).into()),
            size: _ebss as usize - _sbss as usize,
            flags: MemRegionFlags::RESERVED | MemRegionFlags::READ | MemRegionFlags::WRITE,
            name: ".bss",
        },
    ]
    .into_iter ...(+1) | (_sbss as usize).into, (_sdata as usize).into, (_srodata as usize).into, (_stext as usize).into, (_strampoline as usize).into, (boot_stack as usize).into ...(+2) |
| `arceos/modules/axhal/src/platform/x86_pc/apic.rs#init_primary` | `AstrancE/modules/axhal/src/platform/x86_pc/apic.rs#init_primary` | 同板块同名同结构 | IO_APIC.init_once, IoApic::new, LOCAL_APIC.get, LOCAL_APIC.get().as_mut, LOCAL_APIC.get().as_mut().unwrap, LOCAL_APIC.get().as_mut().unwrap().write ...(+18) | IO_APIC.init_once, IoApic::new, LOCAL_APIC.get, LOCAL_APIC.get().as_mut, LOCAL_APIC.get().as_mut().unwrap, LOCAL_APIC.get().as_mut().unwrap().write ...(+18) |
| `arceos/modules/axsync/src/mutex.rs#lock` | `AstrancE/modules/axsync/src/mutex.rs#lock` | 同板块同名同结构 | assert_ne!, current, current().id, current().id().as_u64, self.is_locked, self.owner_id.compare_exchange_weak ...(+1) | assert_ne!, current, current().id, current().id().as_u64, self.is_locked, self.owner_id.compare_exchange_weak ...(+1) |
| `arceos/modules/axhal/src/platform/riscv64_qemu_virt/console.rs#write_bytes` | `AstrancE/modules/axhal/src/platform/riscv64_qemu_virt/console.rs#write_bytes` | 同板块同名同结构 | bytes.as_ptr, bytes.len, bytes.to_vec, kernel_bytes.len, try_write_bytes | bytes.as_ptr, bytes.len, bytes.to_vec, kernel_bytes.len, try_write_bytes |


### 1. 宏观判定
- **定性结论**: 独立开发 (相似度: 19.76%，共同 AST 指纹: 872；唯一 AST 指纹: 目标项目 1534，参考项目 3750；覆盖率: 目标项目 56.84%，参考项目 23.25%)

### 2. 全模块覆盖矩阵解读
- **进程管理**: 双方均存在该模块。存在共同 AST 指纹。目标覆盖率 41.89%，参考覆盖率 33.7%，说明双方在进程生命周期管理上有部分结构重合，但均未过半。证据强度：中。
- **任务调度/异步运行时**: 双方均存在该模块。存在共同 AST 指纹。目标覆盖率 63.28%，参考覆盖率 50.47%，是重合度最高的核心模块，主要源于共享的 ArceOS 调度器框架。证据强度：中。
- **内存管理**: 双方均存在该模块。存在共同 AST 指纹。目标覆盖率 58.37%，参考覆盖率 48.7%，重合集中于页表操作与物理内存分配等底层抽象。证据强度：中。
- **中断/异常处理**: 双方均存在该模块。存在共同 AST 指纹。目标覆盖率 89.36%，参考覆盖率 73.68%，覆盖率极高，但几乎全部位于 `axhal` 等硬件抽象层，属于架构强约束代码。证据强度：弱。
- **系统调用**: 双方均存在该模块。存在共同 AST 指纹。目标覆盖率 1.67%，参考覆盖率 0.86%，覆盖率极低，说明系统调用层的实现几乎完全不同。证据强度：未形成。
- **文件系统**: 双方均存在该模块。存在共同 AST 指纹。目标覆盖率 44.68%，参考覆盖率 35.46%，重合集中于 VFS 接口与 RamFS 实现。证据强度：中。
- **设备驱动**: 双方均存在该模块。存在共同 AST 指纹。目标覆盖率 85.51%，参考覆盖率 46.83%，目标覆盖率极高，但主要源于 VirtIO 和 PCI 等总线驱动的公共框架代码。证据强度：弱。
- **网络协议栈**: 双方均存在该模块。未观察到共同结构指纹，覆盖率均为 0.0%，说明双方的网络实现完全独立。证据强度：未形成。
- **第三方库/公共依赖**: 双方均存在大量第三方库代码。存在共同 AST 指纹。目标覆盖率 3.08%，参考覆盖率 0.21%，覆盖率极低，说明双方引用的第三方库或对其的封装方式差异巨大。证据强度：未形成。

### 3. 底层 AST 关键证据指纹表
| 所属板块 | 目标项目路径 | 参考项目路径 | 证据类型 | 技术说明 |
| :--- | :--- | :--- | :--- | :--- |
| 任务调度/异步运行时 | `arceos/modules/axtask/src/run_queue.rs#exit_current` | `AstrancE/modules/axtask/src/run_queue.rs#exit_current` | 同板块同名同结构 | 完全一致的 `EXITED_TASKS` 清理、`WAIT_FOR_EXIT` 唤醒与 `axhal::misc::terminate` 调用流，源于 ArceOS 公共调度器基线。 |
| 任务调度/异步运行时 | `arceos/modules/axtask/src/wait_queue.rs#wait_timeout_until` | `AstrancE/modules/axtask/src/wait_queue.rs#wait_timeout_until` | 同板块同名同结构 | 基于 `axhal::time::wall_time` 与 `crate::timers::set_alarm_wakeup` 的超时等待逻辑完全同构，是 ArceOS 定时器 API 的标准用法。 |
| 任务调度/异步运行时 | `arceos/modules/axtask/src/wait_queue.rs#requeue` | `AstrancE/modules/axtask/src/wait_queue.rs#requeue` | 同板块同名同结构 | `VecDeque` 的 `drain`、`extend` 等队列迁移操作与锁机制的组合方式一致，属于 Rust 标准库与 ArceOS 同步原语下的自然表达。 |
| 内存管理 | `arceos/modules/axmm/src/aspace.rs#populate_area` | `AstrancE/modules/axmm/src/aspace/mod.rs#populate_area` | 同板块同名 | 地址空间填充逻辑均包含缺页处理与 COW 故障处理，但目标使用 `PageIterWrapper`，参考使用 `PageIter4K`，迭代器抽象不同。 |
| 中断/异常处理 | `arceos/modules/axhal/src/arch/aarch64/trap.rs#handle_sync_exception` | `AstrancE/modules/axhal/src/arch/aarch64/trap.rs#handle_sync_exception` | 同板块同名同结构 | 完全一致的 `ESR_EL1` 异常解析与 `handle_syscall` 分发逻辑，是 AArch64 架构异常处理的固定模式。 |
| 文件系统 | `arceos/modules/axfs/src/fops.rs#_open_at` | `AstrancE/modules/axfs/src/fops.rs#_open_at` | 同板块同名同结构 | 文件打开路径的权限检查与 `WithCap` 创建流程完全一致，是 ArceOS VFS 层的标准实现。 |
| 设备驱动 | `arceos/modules/axdriver/src/virtio.rs#probe_pci` | `AstrancE/modules/axdriver/src/virtio.rs#probe_pci` | 同板块同名同结构 | VirtIO PCI 设备探测逻辑完全一致，是 `axdriver_virtio` 公共库的标准调用模式。 |
| 进程管理 | `api/src/imp/task/exit.rs#do_exit` | `AstrancE/ulib/axmono/src/task/exit.rs#do_exit` | 跨板块同名 | 目标使用 `SignalInfo::new` 与 `axtask::yield_now`，参考使用 `SigInfo_::Child` 与 `clear_tid.write`，信号结构与调度行为不同。 |
| 进程管理 | `api/src/imp/task/execve.rs#sys_execve` | `AstrancE/ulib/axmono/src/syscall/process.rs#sys_execve` | 跨板块同名 | 目标使用 `argv.get_as_null_terminated` 高层封装，参考使用 `str_vec_ptr_to_str` 直接操作 C 指针，抽象层次完全不同。 |
| 第三方库/公共依赖 | `crates/axsignal/src/lib.rs#try_from` | `AstrancE/crates/virtio-drivers/src/transport/mod.rs#try_from` | 跨板块同名 | 目标为信号量的 `TryFrom` 实现，参考为 VirtIO 设备类型的 `TryFrom` 实现，功能与上下文完全不同。 |

### 4. 核心代码调用流对比剖析
* **【任务调度/异步运行时 / 等待队列机制】**
* **底层技术析理**: 双方均实现了基于 `SpinNoIrq` 自旋锁保护的 `VecDeque` 等待队列。在 `wait_until` 等阻塞函数中，调用流均为 `condition` 检查 -> `current_run_queue::<NoPreemptIrqSave>` 获取当前运行队列 -> `rq.blocked_resched` 阻塞并重调度 -> `self.cancel_events` 取消定时器事件。这种高度一致的调用拓扑是 ArceOS 调度器框架的固有约束，任何基于该框架的实现都必然遵循此模式。
* **代码溯源链接**:
  - 目标项目: `arceos/modules/axtask/src/wait_queue.rs#wait_until`
  - 参考项目: `AstrancE/modules/axtask/src/wait_queue.rs#wait_until`

* **【内存管理 / 地址空间填充】**
* **底层技术析理**: 两者在 `populate_area` 函数中均处理了缺页异常和写时复制故障，核心流程相似。但目标项目使用自定义的 `PageIterWrapper` 进行地址迭代，而参考项目使用 `PageIter4K`。这反映了双方在内存管理迭代器抽象上的独立设计选择，并非代码的直接搬运。
* **代码溯源链接**:
  - 目标项目: `arceos/modules/axmm/src/aspace.rs#populate_area`
  - 参考项目: `AstrancE/modules/axmm/src/aspace/mod.rs#populate_area`

* **【文件系统 / 文件打开】**
* **底层技术析理**: `_open_at` 函数是 ArceOS VFS 的核心路径解析与打开逻辑。双方实现完全同构，包括 `WithCap::new` 的权限封装、`attr.is_dir` 与 `attr.perm` 的检查顺序，以及 `ax_err!` 宏的错误处理。这证明双方的文件系统基础均直接构建于同一版本的 ArceOS VFS 库之上。
* **代码溯源链接**:
  - 目标项目: `arceos/modules/axfs/src/fops.rs#_open_at`
  - 参考项目: `AstrancE/modules/axfs/src/fops.rs#_open_at`

### 5. 单方模块与降权项
- **网络协议栈**: 目标项目仅有 10 个函数，参考项目有 61 个函数，且共同 AST 指纹为 0。双方在此模块的实现完全独立，是证明项目独立性的强有力证据。
- **系统调用层**: 覆盖率极低（目标 1.67%，参考 0.86%），说明双方在用户接口层的实现架构完全不同。例如，目标项目倾向于在 `api/src/imp/` 中内联实现系统调用逻辑，而参考项目则大量使用 `syscall_body!` 宏或将逻辑下沉到 `ulib/axmono/src/task/` 模块。
- **跨板块同名函数**: 如 `do_exit`、`sys_execve`、`sys_getppid` 等，虽然函数名相同，但分属 `api/`、`core/` 与 `ulib/axmono/` 等不同板块，且 AST 调用图谱存在显著差异（如信号结构体、指针访问方式、错误处理风格），不能作为强同源证据。
- **第三方库/公共依赖**: 如 `crates/axsignal` 与 `crates/virtio-drivers` 中的同名函数，其功能上下文完全不同，属于 Rust 语言 Trait 实现的常见命名，不构成抄袭证据。

### 6. 保守结论
综合评估，目标项目与参考项目的整体 AST 相似度为 19.76%，处于较低水平。高度重合的代码几乎全部集中于 `arceos/modules/` 目录下的 `axtask`、`axhal`、`axfs` 等模块，这些模块是 ArceOS 开源框架的核心基础设施，其 API 与实现模式具有强约束性，导致衍生项目在此层面必然产生结构同构。在系统调用、网络协议栈、第三方库封装等体现项目独创性的模块上，双方覆盖率极低或为零，架构设计与实现路径差异显著。因此，现有证据不支持目标项目对参考项目构成代码抄袭或底座级复用的指控，双方更可能是基于同一上游开源基线的独立演进作品。

------------------------------

