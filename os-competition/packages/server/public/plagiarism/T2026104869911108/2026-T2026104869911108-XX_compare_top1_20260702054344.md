# 🛡️ OS 内核赛自动化代码级架构审计报告

**审计时间**: `2026-07-02 05:43:44`
**参赛作品名称**: `2026-T2026104869911108-XX`
**年份**: `2026`
**学校**: `XX`
**队伍名称**: `T2026104869911108`
**比赛名字**: `OS-Competition`
**比赛子赛道**: `Kernel-Implementation`
**仓库地址**: `https://gitlab.eduxiji.net/educg-group-43501-3132633/T2026104869911108-2514`
**推测开发语言**: `Rust / C (由AST探针自动识别)`
**特征函数量**: `1836`

## ⚔️ 底层代码同源性对比 (AST Homology)
> 本部分由专门的代码审查智能体，对该项目与符合年份条件的历届最高相似度项目进行控制流与底层调用对比。新版报告会先给出确定性的全模块覆盖矩阵，再给出 Agent 的分模块解释，避免只围绕少量抽样函数展开。

### 🏆 Top 1 底层对标: vs `2025-模仿游戏-燕山大学`（燕山大学 / 模仿游戏）

#### 对方作品信息

**对方年份**: `2025`
**对方学校**: `燕山大学`
**对方队伍名称**: `模仿游戏`
**对方比赛名字**: `OS-Competition`
**对方比赛子赛道**: `Kernel-Implementation`
**对方仓库地址**: `https://gitlab.eduxiji.net/educg-group-36002-2710490/T202510216995249-4014`

> 双向覆盖率：共同 AST 指纹: 1170；唯一 AST 指纹: 目标项目 1534，参考项目 1942；覆盖率: 目标项目 76.27%，参考项目 60.25%

## 全模块对比总览（确定性证据）

> 总体覆盖率：共同 AST 指纹: 1170；唯一 AST 指纹: 目标项目 1534，参考项目 1942；覆盖率: 目标项目 76.27%，参考项目 60.25%

### 1. 模块覆盖矩阵
| 模块 | 目标函数数 | 参考函数数 | 共同唯一 AST 指纹 | Target 覆盖 | Base 覆盖 | 状态 |
| :--- | ---: | ---: | ---: | ---: | ---: | :--- |
| 进程管理 | 75 | 76 | 48 | 64.86% | 68.57% | 双方存在且有结构重合 |
| 任务调度/异步运行时 | 356 | 383 | 208 | 81.25% | 76.47% | 双方存在且有结构重合 |
| 内存管理 | 270 | 302 | 215 | 83.66% | 77.9% | 双方存在且有结构重合 |
| 中断/异常处理 | 53 | 56 | 47 | 100.0% | 94.0% | 双方存在且有结构重合 |
| 系统调用 | 79 | 121 | 35 | 58.33% | 35.71% | 双方存在且有结构重合 |
| 文件系统 | 458 | 631 | 249 | 64.68% | 53.66% | 双方存在且有结构重合 |
| 设备驱动 | 76 | 135 | 65 | 94.2% | 53.28% | 双方存在且有结构重合 |
| 网络协议栈 | 10 | 67 | 5 | 55.56% | 8.93% | 双方存在且有结构重合 |
| 文件系统/第三方库 | 22 | 25 | 11 | 50.0% | 45.83% | 双方存在且有结构重合 |
| 网络协议栈/第三方库 | 99 | 99 | 91 | 100.0% | 100.0% | 双方存在且有结构重合 |
| 第三方库 | 74 | 0 | 0 | 0.0% | 0.0% | 仅目标项目体现 |
| 通用模块/其他 | 264 | 611 | 214 | 90.3% | 45.05% | 双方存在且有结构重合 |

### 2. 分模块证据清单

#### 进程管理
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `api/src/imp/task/exit.rs#do_exit` | `api/src/imp/task/exit.rs#do_exit` | 同板块同名 | Arc::strong_count, FD_TABLE.clear, SignalInfo::new, UserPtr::<Pid>::from, axtask::exit, axtask::yield_now ...(+31) | FD_TABLE.clear, SignalInfo::new, UserPtr::<Pid>::from, axtask::exit, axtask::yield_now, clear_child_tid.get_as_mut ...(+22) |
| `api/src/imp/task/execve.rs#sys_execve` | `api/src/imp/task/execve.rs#sys_execve` | 同板块同名 | Err, Ok, arg.get_as_str, arg.get_as_str().map, args.is_empty, args.push ...(+40) | Err, Ok, arg.get_as_str, arg.get_as_str().map, argv
        .get_as_null_terminated, argv
        .get_as_null_terminated()?
        .iter ...(+37) |
| `api/src/imp/task/wait.rs#sys_waitpid` | `api/src/imp/task/wait.rs#sys_waitpid` | 同板块同名同结构 | Err, Ok, WaitOptions::from_bits_truncate, WaitPid::Pgid, WaitPid::Pid, child.data::<ProcessData> ...(+25) | Err, Ok, WaitOptions::from_bits_truncate, WaitPid::Pgid, WaitPid::Pid, child.data::<ProcessData> ...(+25) |
| `arceos/modules/axtask/src/run_queue.rs#exit_current` | `arceos/modules/axtask/src/run_queue.rs#exit_current` | 同板块同名同结构 | EXITED_TASKS.current_ref_mut_raw, EXITED_TASKS.current_ref_mut_raw().clear, EXITED_TASKS.current_ref_mut_raw().push_back, WAIT_FOR_EXIT.current_ref_mut_raw, WAIT_FOR_EXIT.current_ref_mut_raw().notify_one, assert! ...(+8) | EXITED_TASKS.current_ref_mut_raw, EXITED_TASKS.current_ref_mut_raw().clear, EXITED_TASKS.current_ref_mut_raw().push_back, WAIT_FOR_EXIT.current_ref_mut_raw, WAIT_FOR_EXIT.current_ref_mut_raw().notify_one, assert! ...(+8) |

#### 任务调度/异步运行时
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `src/syscall.rs#handle_syscall` | `src/syscall.rs#handle_syscall` | 同板块同名 | Err, Sysno::from, err.code, info!, result.unwrap_or_else, sys_accept ...(+178) | Err, Sysno::from, err.code, info!, result.unwrap_or_else, sys_access ...(+109) |
| `arceos/modules/axlog/src/lib.rs#log` | `arceos/modules/axlog/src/lib.rs#log` | 同板块同名同结构 | cfg_if::cfg_if!, record.level, record.line, record.line().unwrap_or, record.metadata, record.target ...(+1) | cfg_if::cfg_if!, record.level, record.line, record.line().unwrap_or, record.metadata, record.target ...(+1) |
| `api/src/imp/futex.rs#sys_futex` | `api/src/imp/futex.rs#sys_futex` | 同板块同名同结构 | Err, Ok, axtask::yield_now, curr.task_ext, curr.task_ext().process_data, current ...(+16) | Err, Ok, axtask::yield_now, curr.task_ext, curr.task_ext().process_data, current ...(+16) |
| `api/src/imp/fs/stat.rs#sys_statx` | `api/src/imp/fs/stat.rs#sys_statx` | 同板块同名同结构 | Err, Ok, debug!, f.stat, f.stat()?.into, get_file_like ...(+9) | Err, Ok, debug!, f.stat, f.stat()?.into, get_file_like ...(+9) |

#### 内存管理
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `api/src/imp/task/clone.rs#sys_clone` | `api/src/imp/task/clone.rs#sys_clone` | 同板块同名 | Arc::default, Arc::new, CURRENT_DIR
                .deref_from, CURRENT_DIR
                .deref_from(&process_data.ns)
                .init_new, CURRENT_DIR
                .deref_from(&process_data.ns)
                .init_shared, CURRENT_DIR.copy_inner ...(+73) | Arc::default, Arc::new, CURRENT_DIR
                .deref_from, CURRENT_DIR
                .deref_from(&process_data.ns)
                .init_new, CURRENT_DIR
                .deref_from(&process_data.ns)
                .init_shared, CURRENT_DIR.copy_inner ...(+72) |
| `arceos/modules/axmm/src/aspace.rs#try_clone` | `arceos/modules/axmm/src/aspace.rs#try_clone` | 同板块同名 | Backend::new_alloc, Err, MemoryArea::new, Ok, PageIterWrapper::new, PageIterWrapper::new(area.start(), area.end(), align)
                .expect ...(+29) | Backend::new_alloc, Err, MemoryArea::new, Ok, PageIterWrapper::new, PageIterWrapper::new(area.start(), area.end(), align)
                .expect ...(+29) |
| `arceos/modules/axmm/src/aspace.rs#populate_area` | `arceos/modules/axmm/src/aspace.rs#populate_area` | 同板块同名 | Err, Ok, PageIterWrapper::new, PageIterWrapper::new(
                    start.align_down(align),
                    end.align_up(align).min(area.end()),
                    align,
                )
                .unwrap, Self::handle_cow_fault, _access_flags.contains ...(+14) | Err, Ok, PageIterWrapper::new, PageIterWrapper::new(
                    start.align_down(align),
                    end.align_up(align).min(area.end()),
                    align,
                )
                .unwrap, Self::handle_cow_fault, _access_flags.contains ...(+14) |
| `api/src/imp/mm/mmap.rs#sys_mmap` | `api/src/imp/mm/mmap.rs#sys_mmap` | 同板块同名 | (addr + length).align_up, Err, MmapFlags::from_bits_truncate, MmapProt::from_bits_truncate, Ok, VirtAddr::from ...(+26) | (addr + length).align_up, Err, File::from_fd, MmapFlags::from_bits_truncate, MmapProt::from_bits_truncate, Ok ...(+27) |

#### 中断/异常处理
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `arceos/modules/axhal/src/arch/aarch64/trap.rs#handle_sync_exception` | `arceos/modules/axhal/src/arch/aarch64/trap.rs#handle_sync_exception` | 同板块同名同结构 | ESR_EL1.extract, crate::trap::handle_syscall, crate::trap::post_trap_callback, debug!, esr.read, esr.read_as_enum ...(+6) | ESR_EL1.extract, crate::trap::handle_syscall, crate::trap::post_trap_callback, debug!, esr.read, esr.read_as_enum ...(+6) |
| `arceos/modules/axhal/src/platform/riscv64_qemu_virt/irq.rs#register_handler` | `arceos/modules/axhal/src/platform/riscv64_qemu_virt/irq.rs#register_handler` | 同板块同名同结构 | with_cause! | with_cause! |
| `arceos/modules/axhal/src/platform/loongarch64_qemu_virt/irq.rs#set_enable` | `arceos/modules/axhal/src/platform/loongarch64_qemu_virt/irq.rs#set_enable` | 同板块同名同结构 | ecfg::read, ecfg::read().lie, ecfg::set_lie | ecfg::read, ecfg::read().lie, ecfg::set_lie |
| `arceos/modules/axhal/src/irq.rs#register_handler_common` | `arceos/modules/axhal/src/irq.rs#register_handler_common` | 同板块同名同结构 | IRQ_HANDLER_TABLE.register_handler, set_enable, warn! | IRQ_HANDLER_TABLE.register_handler, set_enable, warn! |

#### 系统调用
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `api/src/imp/net.rs#sys_socket` | `arceos/api/arceos_posix_api/src/imp/net.rs#sys_socket` | 同板块同名 | Err, Mutex::new, Ok, Socket::Tcp, Socket::Udp, TcpSocket::new ...(+4) | debug!, syscall_body! |
| `apps/nimbos/rust/src/arch/riscv.rs#sys_clone` | `apps/nimbos/rust/src/arch/riscv.rs#sys_clone` | 同板块同名同结构 | naked_asm! | naked_asm! |
| `apps/nimbos/rust/src/arch/x86_64.rs#sys_clone` | `apps/nimbos/rust/src/arch/x86_64.rs#sys_clone` | 同板块同名同结构 | naked_asm! | naked_asm! |
| `apps/nimbos/rust/src/arch/aarch64.rs#sys_clone` | `apps/nimbos/rust/src/arch/aarch64.rs#sys_clone` | 同板块同名同结构 | naked_asm! | naked_asm! |

#### 文件系统
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `arceos/modules/axfs/src/fops.rs#_open_at` | `arceos/modules/axfs/src/fops.rs#_open_at` | 同板块同名 | Err, Ok, WithCap::new, attr.is_dir, attr.perm, ax_err! ...(+10) | Err, Ok, WithCap::new, attr.is_dir, attr.perm, ax_err! ...(+10) |
| `api/src/imp/fs/eventpoll.rs#sys_epoll_ctl` | `api/src/imp/fs/io_mpx/epoll.rs#sys_epoll_ctl` | 同板块同名 | Err, Ok, epoll.events.lock, event.get_as_ref, events.contains_key, events.get ...(+8) | EpollInstance::from_fd, EpollInstance::from_fd(epfd)?.control, Ok, debug!, event.get_as_mut, event.is_null |
| `api/src/imp/fs/fd_ops.rs#sys_openat` | `api/src/imp/fs/fd_ops.rs#sys_openat` | 同板块同名 | Directory::from_fd, Directory::new, Directory::new(
        dir.map_or_else(
            || axfs::fops::Directory::open_dir(path, &opts),
            |dir| dir.inner().open_dir_at(path, &opts),
        )?,
        real_path.to_string(),
    )
    .add_to_fd_table, File::new, File::new(r?, real_path.to_string()).add_to_fd_table, Ok ...(+21) | Directory::from_fd, Directory::new, Directory::new(
        dir.map_or_else(
            || axfs::fops::Directory::open_dir(path, &opts),
            |dir| dir.inner().open_dir_at(path, &opts),
        )?,
        real_path.to_string(),
    )
    .add_to_fd_table, File::new, File::new(r?, real_path.to_string()).add_to_fd_table, Ok ...(+16) |
| `arceos/modules/axfs/src/dev.rs#write_one` | `arceos/modules/axfs/src/dev.rs#write_one` | 同板块同名同结构 | Ok, buf.len, buf.len().min, buf[0..BLOCK_SIZE].to_vec, data[start..start + count].copy_from_slice, self.dev.read_block ...(+1) | Ok, buf.len, buf.len().min, buf[0..BLOCK_SIZE].to_vec, data[start..start + count].copy_from_slice, self.dev.read_block ...(+1) |

#### 设备驱动
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `arceos/modules/axdriver/src/lib.rs#init_drivers` | `arceos/modules/axdriver/src/lib.rs#init_drivers` | 同板块同名同结构 | AllDevices::default, all_devs.block.iter, all_devs.block.iter().enumerate, all_devs.display.iter, all_devs.display.iter().enumerate, all_devs.net.iter ...(+5) | AllDevices::default, all_devs.block.iter, all_devs.block.iter().enumerate, all_devs.display.iter, all_devs.display.iter().enumerate, all_devs.net.iter ...(+5) |
| `arceos/modules/axdriver/src/virtio.rs#probe_pci` | `arceos/modules/axdriver/src/virtio.rs#probe_pci` | 同板块同名同结构 | D::try_new, Some, axdriver_virtio::probe_pci_device::<VirtIoHalImpl>, warn! | D::try_new, Some, axdriver_virtio::probe_pci_device::<VirtIoHalImpl>, warn! |
| `api/src/socket.rs#read_from_user` | `api/src/socket.rs#read_from_user` | 同板块同名同结构 | Err, SocketAddrV4::read_from_user, SocketAddrV4::read_from_user(addr, addrlen).map, SocketAddrV6::read_from_user, SocketAddrV6::read_from_user(addr, addrlen).map, addr.get_as_ref ...(+2) | Err, SocketAddrV4::read_from_user, SocketAddrV4::read_from_user(addr, addrlen).map, SocketAddrV6::read_from_user, SocketAddrV6::read_from_user(addr, addrlen).map, addr.get_as_ref ...(+2) |
| `arceos/modules/axdriver/src/virtio.rs#probe_mmio` | `arceos/modules/axdriver/src/virtio.rs#probe_mmio` | 同板块同名同结构 | D::try_new, Some, axdriver_virtio::probe_mmio_device, base_vaddr.as_mut_ptr, mmio_base.into, phys_to_virt ...(+1) | D::try_new, Some, axdriver_virtio::probe_mmio_device, base_vaddr.as_mut_ptr, mmio_base.into, phys_to_virt ...(+1) |

#### 网络协议栈
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `api/src/socket.rs#write_to_user` | `api/src/socket.rs#write_to_user` | 同板块同名同结构 | Err, Ok, addr.get_as_mut, addr.is_null, core::ptr::copy_nonoverlapping, self.flowinfo ...(+7) | Err, Ok, addr.get_as_mut, addr.is_null, core::ptr::copy_nonoverlapping, self.flowinfo ...(+7) |
| `api/src/socket.rs#write_to_user` | `api/src/socket.rs#write_to_user` | 同板块同名同结构 | Err, Ok, addr.get_as_mut, addr.is_null, core::ptr::copy_nonoverlapping, self.ip ...(+5) | Err, Ok, addr.get_as_mut, addr.is_null, core::ptr::copy_nonoverlapping, self.ip ...(+5) |
| `api/src/socket.rs#write_to_user` | `api/src/socket.rs#write_to_user` | 同板块同名同结构 | Err, addr.is_null, v4.write_to_user, v6.write_to_user | Err, addr.is_null, v4.write_to_user, v6.write_to_user |
| `api/src/socket.rs#family` | `api/src/socket.rs#family` | 同板块同名同结构 | v4.family, v6.family | v4.family, v6.family |

#### 文件系统/第三方库
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `arceos/modules/axfs/src/fs/lwext4_rust.rs#get_attr` | `arceos/modules/axfs/src/fs/lwext4_rust.rs#get_attr` | 同板块同名 | <i32 as TryInto<AxError>>::try_into, <i32 as TryInto<AxError>>::try_into(e).unwrap, EXT4_LOCK.lock, Ok, VfsNodeAttr::new, VfsNodePerm::from_bits_truncate ...(+13) | <i32 as TryInto<AxError>>::try_into, <i32 as TryInto<AxError>>::try_into(e).unwrap, Ok, VfsNodeAttr::new, VfsNodePerm::from_bits_truncate, file.file_close ...(+12) |
| `arceos/modules/axfs/src/fs/lwext4_rust.rs#create` | `arceos/modules/axfs/src/fs/lwext4_rust.rs#create` | 同板块同名 | EXT4_LOCK.lock, Ok, e.try_into, e.try_into().unwrap, file.check_inode_exist, file.dir_mk ...(+13) | Ok, e.try_into, e.try_into().unwrap, file.check_inode_exist, file.dir_mk, file.dir_mk(fpath)
                    .map ...(+12) |
| `arceos/modules/axfs/src/fs/lwext4_rust.rs#read_dir` | `arceos/modules/axfs/src/fs/lwext4_rust.rs#read_dir` | 同板块同名 | EXT4_LOCK.lock, Ok, VfsDirEntry::new, core::str::from_utf8, core::str::from_utf8(iname.unwrap()).unwrap, dirents.iter_mut ...(+14) | Ok, VfsDirEntry::new, core::str::from_utf8, core::str::from_utf8(iname.unwrap()).unwrap, dirents.iter_mut, dirents.iter_mut().enumerate ...(+13) |
| `arceos/modules/axfs/src/fs/lwext4_rust.rs#lookup` | `arceos/modules/axfs/src/fs/lwext4_rust.rs#lookup` | 同板块同名 | Arc::new, EXT4_LOCK.lock, Err, Ok, Self::new, file.check_inode_exist ...(+6) | Arc::new, Err, Ok, Self::new, file.check_inode_exist, fpath.as_str ...(+5) |

#### 网络协议栈/第三方库
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `arceos/modules/axnet/src/smoltcp_impl/tcp.rs#connect` | `arceos/modules/axnet/src/smoltcp_impl/tcp.rs#connect` | 同板块同名同结构 | Err, Ok, SOCKET_SET
                .with_socket_mut::<tcp::Socket, _, _>, SOCKET_SET.add, SocketSetWrapper::new_tcp_socket, Some ...(+25) | Err, Ok, SOCKET_SET
                .with_socket_mut::<tcp::Socket, _, _>, SOCKET_SET.add, SocketSetWrapper::new_tcp_socket, Some ...(+25) |
| `arceos/modules/axnet/src/smoltcp_impl/dns.rs#query` | `arceos/modules/axnet/src/smoltcp_impl/dns.rs#query` | 同板块同名同结构 | Err, Ok, SOCKET_SET
            .with_socket_mut::<dns::Socket, _, _>, SOCKET_SET
            .with_socket_mut::<dns::Socket, _, _>(handle, |socket| {
                socket.start_query(iface.lock().context(), name, query_type)
            })
            .map_err, SOCKET_SET.poll_interfaces, SOCKET_SET.with_socket_mut::<dns::Socket, _, _> ...(+12) | Err, Ok, SOCKET_SET
            .with_socket_mut::<dns::Socket, _, _>, SOCKET_SET
            .with_socket_mut::<dns::Socket, _, _>(handle, |socket| {
                socket.start_query(iface.lock().context(), name, query_type)
            })
            .map_err, SOCKET_SET.poll_interfaces, SOCKET_SET.with_socket_mut::<dns::Socket, _, _> ...(+12) |
| `arceos/modules/axnet/src/smoltcp_impl/bench.rs#bench_transmit_bandwidth` | `arceos/modules/axnet/src/smoltcp_impl/bench.rs#bench_transmit_bandwidth` | 同板块同名同结构 | (current_time - past_time).secs, AxNetTxToken::consume, InterfaceWrapper::current_time, info!, self.transmit, tx_buf[0..12].fill ...(+2) | (current_time - past_time).secs, AxNetTxToken::consume, InterfaceWrapper::current_time, info!, self.transmit, tx_buf[0..12].fill ...(+2) |
| `arceos/modules/axnet/src/smoltcp_impl/tcp.rs#shutdown` | `arceos/modules/axnet/src/smoltcp_impl/tcp.rs#shutdown` | 同板块同名同结构 | LISTEN_TABLE.unlisten, Ok, SOCKET_SET.poll_interfaces, SOCKET_SET.with_socket_mut::<tcp::Socket, _, _>, debug!, self.handle.get ...(+9) | LISTEN_TABLE.unlisten, Ok, SOCKET_SET.poll_interfaces, SOCKET_SET.with_socket_mut::<tcp::Socket, _, _>, debug!, self.handle.get ...(+9) |

#### 第三方库
仅目标项目中观察到该模块代表函数，参考项目没有同模块可对齐证据：
- 目标项目：`crates/axprocess/src/lib.rs#build`，调用摘要：Arc::new, AtomicBool::new, AtomicI32::new, INIT_PROC.call_once, Mutex::new, Once::new ...(+8)
- 目标项目：`crates/axsignal/src/lib.rs#from_repr`，调用摘要：Some
- 目标项目：`crates/axsignal/src/lib.rs#check_signals`，调用摘要：Some, self.blocked.lock, self.take_unblocked_signal, sig.signo
- 目标项目：`crates/axprocess/src/lib.rs#build`，调用摘要：Arc::downgrade, Arc::new, AtomicBool::new, AtomicI32::new, Once::new, thread.data.call_once ...(+2)

#### 通用模块/其他
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `arceos/modules/axhal/src/mem.rs#kernel_image_regions` | `arceos/modules/axhal/src/mem.rs#kernel_image_regions` | 同板块同名同结构 | (_sbss as usize).into, (_sdata as usize).into, (_srodata as usize).into, (_stext as usize).into, (boot_stack as usize).into, [
        MemRegion {
            paddr: virt_to_phys((_stext as usize).into()),
            size: _etext as usize - _stext as usize,
            flags: MemRegionFlags::RESERVED | MemRegionFlags::READ | MemRegionFlags::EXECUTE,
            name: ".text",
        },
        MemRegion {
            paddr: virt_to_phys((_srodata as usize).into()),
            size: _erodata as usize - _srodata as usize,
            flags: MemRegionFlags::RESERVED | MemRegionFlags::READ,
            name: ".rodata",
        },
        MemRegion {
            paddr: virt_to_phys((_sdata as usize).into()),
            size: _edata as usize - _sdata as usize,
            flags: MemRegionFlags::RESERVED | MemRegionFlags::READ | MemRegionFlags::WRITE,
            name: ".data .tdata .tbss .percpu",
        },
        MemRegion {
            paddr: virt_to_phys((boot_stack as usize).into()),
            size: boot_stack_top as usize - boot_stack as usize,
            flags: MemRegionFlags::RESERVED | MemRegionFlags::READ | MemRegionFlags::WRITE,
            name: "boot stack",
        },
        MemRegion {
            paddr: virt_to_phys((_sbss as usize).into()),
            size: _ebss as usize - _sbss as usize,
            flags: MemRegionFlags::RESERVED | MemRegionFlags::READ | MemRegionFlags::WRITE,
            name: ".bss",
        },
    ]
    .into_iter ...(+1) | (_sbss as usize).into, (_sdata as usize).into, (_srodata as usize).into, (_stext as usize).into, (boot_stack as usize).into, [
        MemRegion {
            paddr: virt_to_phys((_stext as usize).into()),
            size: _etext as usize - _stext as usize,
            flags: MemRegionFlags::RESERVED | MemRegionFlags::READ | MemRegionFlags::EXECUTE,
            name: ".text",
        },
        MemRegion {
            paddr: virt_to_phys((_srodata as usize).into()),
            size: _erodata as usize - _srodata as usize,
            flags: MemRegionFlags::RESERVED | MemRegionFlags::READ,
            name: ".rodata",
        },
        MemRegion {
            paddr: virt_to_phys((_sdata as usize).into()),
            size: _edata as usize - _sdata as usize,
            flags: MemRegionFlags::RESERVED | MemRegionFlags::READ | MemRegionFlags::WRITE,
            name: ".data .tdata .tbss .percpu",
        },
        MemRegion {
            paddr: virt_to_phys((boot_stack as usize).into()),
            size: boot_stack_top as usize - boot_stack as usize,
            flags: MemRegionFlags::RESERVED | MemRegionFlags::READ | MemRegionFlags::WRITE,
            name: "boot stack",
        },
        MemRegion {
            paddr: virt_to_phys((_sbss as usize).into()),
            size: _ebss as usize - _sbss as usize,
            flags: MemRegionFlags::RESERVED | MemRegionFlags::READ | MemRegionFlags::WRITE,
            name: ".bss",
        },
    ]
    .into_iter ...(+1) |
| `arceos/modules/axhal/src/platform/x86_pc/apic.rs#init_primary` | `arceos/modules/axhal/src/platform/x86_pc/apic.rs#init_primary` | 同板块同名同结构 | IO_APIC.init_once, IoApic::new, LOCAL_APIC.get, LOCAL_APIC.get().as_mut, LOCAL_APIC.get().as_mut().unwrap, LOCAL_APIC.get().as_mut().unwrap().write ...(+18) | IO_APIC.init_once, IoApic::new, LOCAL_APIC.get, LOCAL_APIC.get().as_mut, LOCAL_APIC.get().as_mut().unwrap, LOCAL_APIC.get().as_mut().unwrap().write ...(+18) |
| `apps/libc/c/signal/signal.c#test_sigsuspend` | `apps/libc/c/signal/signal.c#test_sigsuspend` | 同板块同名同结构 | exit, fork, kill, puts, sigaction, sigaddset ...(+4) | exit, fork, kill, puts, sigaction, sigaddset ...(+4) |
| `arceos/modules/axsync/src/mutex.rs#lock` | `arceos/modules/axsync/src/mutex.rs#lock` | 同板块同名同结构 | assert_ne!, current, current().id, current().id().as_u64, self.is_locked, self.owner_id.compare_exchange_weak ...(+1) | assert_ne!, current, current().id, current().id().as_u64, self.is_locked, self.owner_id.compare_exchange_weak ...(+1) |


好的，审计官。基于提供的证据底座、控辩双方陈词及AST抽样数据，我将依据审计指令进行最终定性仲裁。

---

### 1. 宏观判定
- **定性结论**: **部分逻辑借鉴与架构改动并存 (相似度: 50.74%)**
  目标项目与参考项目在宏观层面共享了显著的代码指纹（共同AST指纹1170），但双方均保有大量独立代码（唯一指纹：目标1534，参考1942）。这种相似度水平表明，双方存在明确的共同技术源头或架构参考，但并非简单的“复制粘贴”式抄袭。目标项目在继承公共基线的基础上，进行了独立的模块扩展与架构调整。

### 2. 全模块覆盖矩阵解读
- **进程管理**：双方均存在。共同AST指纹48个，目标覆盖率64.86%，参考覆盖率68.57%。双方在进程退出、等待、信号处理等核心系统调用实现上存在结构重合，证据强度为**强**。
- **任务调度/异步运行时**：双方均存在。共同AST指纹208个，目标覆盖率81.25%，参考覆盖率76.47%。作为操作系统核心，双方在等待队列、调度器原语等底层机制上高度重合，是相似度的主要贡献者，证据强度为**强**。
- **内存管理**：双方均存在。共同AST指纹215个，目标覆盖率83.66%，参考覆盖率77.9%。在地址空间克隆、缺页处理、mmap等复杂逻辑上表现出高度一致性，证据强度为**强**。
- **中断/异常处理**：双方均存在。共同AST指纹47个，目标覆盖率100%，参考覆盖率94.0%。此模块几乎完全重合，但因其多为架构定义的标准模板代码（如读取ESR寄存器、分发处理），其作为独创性证据的强度为**中**。
- **系统调用**：双方均存在。共同AST指纹35个，目标覆盖率58.33%，参考覆盖率35.71%。覆盖率差异显著，参考项目拥有更多独立系统调用实现。重合部分集中在少数核心调用上，证据强度为**中**。
- **文件系统**：双方均存在。共同AST指纹249个，目标覆盖率64.68%，参考覆盖率53.66%。在VFS、RamFS、DevFS及Ext4封装层存在大量重合，但双方各自拥有大量独立文件系统逻辑，证据强度为**中**。
- **设备驱动**：双方均存在。共同AST指纹65个，目标覆盖率94.2%，参考覆盖率53.28%。目标项目高度依赖参考项目的驱动框架，特别是VirtIO驱动探针逻辑几乎完全一致，证据强度为**强**。
- **网络协议栈**：双方均存在。共同AST指纹5个，目标覆盖率55.56%，参考覆盖率8.93%。参考项目拥有更完整的独立协议栈实现。重合点极少，仅存在于Socket地址处理等工具函数，证据强度为**弱**。
- **第三方库/公共依赖**：此类别包含`文件系统/第三方库`、`网络协议栈/第三方库`及`第三方库`模块。`网络协议栈/第三方库`（smoltcp封装）双方完全重合（100%/100%），是明确的公共依赖。`文件系统/第三方库`（lwext4封装）高度重合。目标项目独有的`第三方库`模块（74个函数，如`axprocess`， `axsignal`）是独立开发的关键证据。此类别证据强度为**弱（公共依赖）** 与**强（独立开发）** 并存。

### 3. 底层 AST 关键证据指纹表
| 所属板块 | 目标项目路径 | 参考项目路径 | 证据类型 | 技术说明 |
| :--- | :--- | :--- | :--- | :--- |
| 任务调度/异步运行时 | `arceos/modules/axtask/src/wait_queue.rs#wait_timeout_until` | `arceos/modules/axtask/src/wait_queue.rs#wait_timeout_until` | 同源实现 | 带超时的等待逻辑完全一致，包含`wall_time`、`set_alarm_wakeup`、`NoPreemptIrqSave`、`blocked_resched`等非标准调用组合。 |
| 任务调度/异步运行时 | `arceos/modules/axtask/src/run_queue.rs#exit_current` | `arceos/modules/axtask/src/run_queue.rs#exit_current` | 同源实现 | 任务退出清理流程一致，涉及`EXITED_TASKS`和`WAIT_FOR_EXIT`两个全局队列的特定操作序列。 |
| 进程管理 | `api/src/imp/signal.rs#sys_rt_sigprocmask` | `api/src/imp/signal.rs#sys_rt_sigprocmask` | 同源实现 | 信号掩码处理的数据访问路径完全一致：`current().task_ext().thread_data().signal.with_blocked_mut`。 |
| 进程管理 | `api/src/imp/task/wait.rs#sys_waitpid` | `api/src/imp/task/wait.rs#sys_waitpid` | 同源实现 | `waitpid`的选项解析与子进程状态检查逻辑高度一致，包括`WaitOptions::from_bits_truncate`和`is_clone_child`的调用。 |
| 内存管理 | `arceos/modules/axmm/src/aspace.rs#try_clone` | `arceos/modules/axmm/src/aspace.rs#try_clone` | 同源实现 | 地址空间克隆的复杂流程一致，包括`Backend::new_alloc`、`MemoryArea::new`、`PageIterWrapper::new`的特定组合。 |
| 内存管理 | `api/src/imp/mm/mmap.rs#sys_mmap` | `api/src/imp/mm/mmap.rs#sys_mmap` | 同源实现 | `mmap`系统调用的参数解析与标志位处理逻辑高度重合，如`MmapFlags::from_bits_truncate`和地址对齐操作。 |
| 设备驱动 | `arceos/modules/axdriver/src/virtio.rs#probe_pci` | `arceos/modules/axdriver/src/virtio.rs#probe_pci` | 同源实现 | VirtIO PCI设备探针逻辑完全一致，包含`axdriver_virtio::probe_pci_device::<VirtIoHalImpl>`这一特定调用。 |
| 中断/异常处理 | `arceos/modules/axhal/src/arch/aarch64/trap.rs#handle_sync_exception` | `arceos/modules/axhal/src/arch/aarch64/trap.rs#handle_sync_exception` | 同源实现 | AArch64同步异常处理流程一致，包括`ESR_EL1.extract`、`handle_syscall`、`post_trap_callback`的调用序列。 |
| 文件系统 | `arceos/modules/axfs/src/fops.rs#_open_at` | `arceos/modules/axfs/src/fops.rs#_open_at` | 同源实现 | 文件打开的核心逻辑一致，包含`WithCap::new`、`attr.is_dir`、`attr.perm`等权限和属性检查的特定顺序。 |
| 第三方库/公共依赖 | `arceos/modules/axnet/src/smoltcp_impl/tcp.rs#connect` | `arceos/modules/axnet/src/smoltcp_impl/tcp.rs#connect` | 公共依赖 | 完全一致的smoltcp库封装，调用`SOCKET_SET.add`、`SocketSetWrapper::new_tcp_socket`等。 |
| 第三方库/公共依赖 | `crates/axprocess/src/lib.rs#build` | (无) | 独立开发 | 目标项目独有的进程构建逻辑，参考项目中不存在。 |
| 第三方库/公共依赖 | `crates/axsignal/src/lib.rs#check_signals` | (无) | 独立开发 | 目标项目独有的信号检查与分发逻辑，参考项目中不存在。 |

### 4. 核心代码调用流对比剖析
* **【进程管理/信号处理路径】**
* **底层技术析理**: 双方均采用在任务扩展（`task_ext`）中内嵌信号处理结构体的设计。`sys_rt_sigprocmask`的实现完全遵循了`current() -> task_ext() -> thread_data() -> signal -> with_blocked_mut`的调用链，这表明两者不仅共享了ArceOS的线程模型，连信号掩码的加锁修改逻辑（`with_blocked_mut`）都如出一辙，是典型的同源实现。
* **代码溯源链接**:
  - 目标项目: `api/src/imp/signal.rs#sys_rt_sigprocmask`
  - 参考项目: `api/src/imp/signal.rs#sys_rt_sigprocmask`

* **【任务调度/等待队列机制】**
* **底层技术析理**: 等待队列是内核调度最核心的同步原语之一。双方在`wait_timeout_until`函数中，从获取当前时间(`wall_time`)、设置唤醒闹钟(`set_alarm_wakeup`)、获取特定调度策略的运行队列(`current_run_queue::<NoPreemptIrqSave>`)到最终执行阻塞调度(`blocked_resched`)，整个带超时的阻塞流程在AST层面完全一致。这种非标准的、特定泛型参数（`NoPreemptIrqSave`）的调用组合，是强有力的同源指纹。
* **代码溯源链接**:
  - 目标项目: `arceos/modules/axtask/src/wait_queue.rs#wait_timeout_until`
  - 参考项目: `arceos/modules/axtask/src/wait_queue.rs#wait_timeout_until`

* **【内存管理/地址空间克隆】**
* **底层技术析理**: 进程创建（`fork`/`clone`）的核心是地址空间克隆。双方在`try_clone`函数中，对内存区域（`MemoryArea`）的遍历、为新区域分配物理页后端（`Backend::new_alloc`）、以及通过`PageIterWrapper`进行页对齐迭代的整个流程完全一致。这不仅是算法相似，更是对同一套内存管理抽象（`Backend`， `MemoryArea`）的具体操作序列的复用。
* **代码溯源链接**:
  - 目标项目: `arceos/modules/axmm/src/aspace.rs#try_clone`
  - 参考项目: `arceos/modules/axmm/src/aspace.rs#try_clone`

### 5. 单方模块与降权项
* **目标项目独有模块**: `第三方库`模块下的`crates/axprocess`和`crates/axsignal`等是目标项目独立开发的强证据，在参考项目中无任何对应。这表明目标项目在进程和信号管理上进行了超越参考项目的架构抽象。
* **公共依赖/第三方库**: `网络协议栈/第三方库`（smoltcp）和`文件系统/第三方库`（lwext4）的封装代码高度重合，应被视为对共同开源上游的依赖，不构成项目间的抄袭。
* **跨板块弱信号**: 控方将`arceos/modules/axhal/src/time.rs#busy_wait_until`（HAL层）和`apps/nimbos/rust/src/bin/forktree.rs`（示例程序）等归入【进程管理】板块进行指控，属于跨板块硬凑证据。这些项仅能作为弱信号，不构成同板块主证据。
* **同名但异构项**: `do_exit`和`sys_execve`等函数虽同名且在同一文件，但其AST调用图谱存在差异（如参数处理方式不同），已被控辩双方共同认定为弱信号或存在重构，不能作为强同源证据。

### 6. 保守结论
本次审计的目标项目与参考项目之间存在明确的、系统性的代码同源性，尤其在任务调度、内存管理、进程控制和设备驱动等核心内核模块，大量非标准的AST指纹高度重合。然而，50.74%的总体相似度及双方各自拥有的上千个唯一AST指纹表明，这并非一次完全的、无脑的代码抄袭。更准确的定性是：目标项目在ArceOS/NimbOS等公共开源基线上进行了二次开发，其大量底层机制和驱动框架直接继承或借鉴了与参考项目相同的上游源码或设计。同时，目标项目通过引入`axprocess`、`axsignal`等独立crate，展现了其自身的架构设计与创新能力。因此，整体风险评定为**中等**，建议对重合度超过80%的核心模块（如任务调度、内存管理）进行重点人工审查，以明确其借鉴边界是否合规。

------------------------------

