# 🛡️ OS 内核赛自动化代码级架构审计报告

**审计时间**: `2026-07-02 05:43:44`
**参赛作品名称**: `2026-T2026104869911108-XX`
**年份**: `2026`
**学校**: `XX`
**队伍名称**: `T2026104869911108`
**比赛名字**: `OS-Competition`
**比赛子赛道**: `Kernel-Implementation`
**仓库地址**: `https://gitlab.eduxiji.net/educg-group-43501-3132633/T2026104869911108-2514`
**推测开发语言**: `Rust / C (由AST探针自动识别)`
**特征函数量**: `1836`

## ⚔️ 底层代码同源性对比 (AST Homology)
> 本部分由专门的代码审查智能体，对该项目与符合年份条件的历届最高相似度项目进行控制流与底层调用对比。新版报告会先给出确定性的全模块覆盖矩阵，再给出 Agent 的分模块解释，避免只围绕少量抽样函数展开。

### 🏆 Top 4 底层对标: vs `2025-undefined-清华大学`（清华大学 / undefined）

#### 对方作品信息

**对方年份**: `2025`
**对方学校**: `清华大学`
**对方队伍名称**: `undefined`
**对方比赛名字**: `OS-Competition`
**对方比赛子赛道**: `Kernel-Implementation`
**对方仓库地址**: `https://gitlab.eduxiji.net/educg-group-36002-2710490/T202510003995291-2331`

> 双向覆盖率：共同 AST 指纹: 834；唯一 AST 指纹: 目标项目 1534，参考项目 2820；覆盖率: 目标项目 54.37%，参考项目 29.57%

## 全模块对比总览（确定性证据）

> 总体覆盖率：共同 AST 指纹: 834；唯一 AST 指纹: 目标项目 1534，参考项目 2820；覆盖率: 目标项目 54.37%，参考项目 29.57%

### 1. 模块覆盖矩阵
| 模块 | 目标函数数 | 参考函数数 | 共同唯一 AST 指纹 | Target 覆盖 | Base 覆盖 | 状态 |
| :--- | ---: | ---: | ---: | ---: | ---: | :--- |
| 进程管理 | 75 | 135 | 30 | 40.54% | 24.79% | 双方存在且有结构重合 |
| 任务调度/异步运行时 | 356 | 415 | 174 | 67.97% | 58.0% | 双方存在且有结构重合 |
| 内存管理 | 270 | 450 | 189 | 73.54% | 49.48% | 双方存在且有结构重合 |
| 中断/异常处理 | 53 | 53 | 46 | 97.87% | 97.87% | 双方存在且有结构重合 |
| 系统调用 | 79 | 109 | 29 | 48.33% | 33.72% | 双方存在且有结构重合 |
| 文件系统 | 458 | 999 | 65 | 16.88% | 9.73% | 双方存在且有结构重合 |
| 设备驱动 | 76 | 133 | 62 | 89.86% | 50.82% | 双方存在且有结构重合 |
| 网络协议栈 | 10 | 65 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |
| 文件系统/第三方库 | 22 | 643 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |
| 网络协议栈/第三方库 | 99 | 130 | 73 | 80.22% | 62.93% | 双方存在且有结构重合 |
| 第三方库 | 74 | 0 | 0 | 0.0% | 0.0% | 仅目标项目体现 |
| 通用模块/其他 | 264 | 555 | 171 | 72.15% | 39.13% | 双方存在且有结构重合 |

### 2. 分模块证据清单

#### 进程管理
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `api/src/imp/task/exit.rs#do_exit` | `arceos/examples/shell/src/cmd.rs#do_exit` | 同板块同名 | Arc::strong_count, FD_TABLE.clear, SignalInfo::new, UserPtr::<Pid>::from, axtask::exit, axtask::yield_now ...(+31) | println!, std::process::exit |
| `api/src/imp/task/execve.rs#sys_execve` | `api/src/interface/task/execve.rs#sys_execve` | 同板块同名 | Err, Ok, arg.get_as_str, arg.get_as_str().map, args.is_empty, args.push ...(+40) | BUSYBOX.to_string, Some, abs_path.to_string, args[1..].iter, args[1..].iter().cloned, argv.clone ...(+10) |
| `arceos/modules/axtask/src/run_queue.rs#exit_current` | `arceos/modules/axtask/src/run_queue.rs#exit_current` | 同板块同名同结构 | EXITED_TASKS.current_ref_mut_raw, EXITED_TASKS.current_ref_mut_raw().clear, EXITED_TASKS.current_ref_mut_raw().push_back, WAIT_FOR_EXIT.current_ref_mut_raw, WAIT_FOR_EXIT.current_ref_mut_raw().notify_one, assert! ...(+8) | EXITED_TASKS.current_ref_mut_raw, EXITED_TASKS.current_ref_mut_raw().clear, EXITED_TASKS.current_ref_mut_raw().push_back, WAIT_FOR_EXIT.current_ref_mut_raw, WAIT_FOR_EXIT.current_ref_mut_raw().notify_one, assert! ...(+8) |
| `arceos/modules/axtask/src/wait_queue.rs#cancel_events` | `arceos/modules/axtask/src/wait_queue.rs#cancel_events` | 同板块同名同结构 | curr.in_wait_queue, curr.ptr_eq, curr.set_in_wait_queue, curr.timer_ticket_expired, self.queue.lock, self.queue.lock().retain | curr.in_wait_queue, curr.ptr_eq, curr.set_in_wait_queue, curr.timer_ticket_expired, self.queue.lock, self.queue.lock().retain |

#### 任务调度/异步运行时
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `arceos/modules/axlog/src/lib.rs#log` | `arceos/modules/axlog/src/lib.rs#log` | 同板块同名同结构 | cfg_if::cfg_if!, record.level, record.line, record.line().unwrap_or, record.metadata, record.target ...(+1) | cfg_if::cfg_if!, record.level, record.line, record.line().unwrap_or, record.metadata, record.target ...(+1) |
| `api/src/imp/futex.rs#sys_futex` | `api/src/imp/task/futex.rs#sys_futex` | 同板块同名 | Err, Ok, axtask::yield_now, curr.task_ext, curr.task_ext().process_data, current ...(+16) | Err, Ok, axtask::yield_now, current_process_data, drop, futex_table
                .entry ...(+27) |
| `arceos/modules/axtask/src/run_queue.rs#switch_to` | `arceos/modules/axtask/src/run_queue.rs#switch_to` | 同板块同名同结构 | (*prev_ctx_ptr).switch_to, Arc::downgrade, CurrentTask::set_current, PREV_TASK.current_ref_mut_raw, assert!, clear_prev_task_on_cpu ...(+8) | (*prev_ctx_ptr).switch_to, Arc::downgrade, CurrentTask::set_current, PREV_TASK.current_ref_mut_raw, assert!, clear_prev_task_on_cpu ...(+8) |
| `arceos/modules/axhal/src/arch/aarch64/context.rs#fpstate_switch` | `arceos/modules/axhal/src/arch/aarch64/context.rs#fpstate_switch` | 同板块同名 | naked_asm! | naked_asm! |

#### 内存管理
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `arceos/modules/axmm/src/aspace.rs#try_clone` | `arceos/modules/axmm/src/aspace.rs#try_clone` | 同板块同名同结构 | Backend::new_alloc, Err, MemoryArea::new, Ok, PageIterWrapper::new, PageIterWrapper::new(area.start(), area.end(), align)
                .expect ...(+29) | Backend::new_alloc, Err, MemoryArea::new, Ok, PageIterWrapper::new, PageIterWrapper::new(area.start(), area.end(), align)
                .expect ...(+29) |
| `arceos/modules/axmm/src/aspace.rs#populate_area` | `arceos/modules/axmm/src/aspace.rs#populate_area` | 同板块同名 | Err, Ok, PageIterWrapper::new, PageIterWrapper::new(
                    start.align_down(align),
                    end.align_up(align).min(area.end()),
                    align,
                )
                .unwrap, Self::handle_cow_fault, _access_flags.contains ...(+14) | Err, Ok, PageIterWrapper::new, PageIterWrapper::new(
                    start.align_down(align),
                    end.align_up(align).min(area.end()),
                    align,
                )
                .unwrap, Self::handle_cow_fault, access_flags.contains ...(+14) |
| `api/src/imp/mm/mmap.rs#sys_mmap` | `api/src/imp/mm/mmap.rs#sys_mmap` | 同板块同名 | (addr + length).align_up, Err, MmapFlags::from_bits_truncate, MmapProt::from_bits_truncate, Ok, VirtAddr::from ...(+26) | Err, File::from_fd, MmapFlags::from_bits_truncate, MmapProt::from_bits_truncate, Ok, PhysAddr::from ...(+34) |
| `arceos/modules/axruntime/src/lib.rs#rust_main` | `arceos/modules/axruntime/src/lib.rs#rust_main` | 同板块同名 | INITED_CPUS.fetch_add, ax_println!, axdisplay::init_display, axdriver::init_drivers, axfs::init_filesystems, axhal::mem::memory_regions ...(+20) | INITED_CPUS.fetch_add, Mountpoint::new_root, all_devices
                .block
                .take_one, all_devices
                .block
                .take_one()
                .expect, ax_println!, axdisplay::init_display ...(+28) |

#### 中断/异常处理
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `arceos/modules/axhal/src/arch/aarch64/trap.rs#handle_sync_exception` | `arceos/modules/axhal/src/arch/aarch64/trap.rs#handle_sync_exception` | 同板块同名同结构 | ESR_EL1.extract, crate::trap::handle_syscall, crate::trap::post_trap_callback, debug!, esr.read, esr.read_as_enum ...(+6) | ESR_EL1.extract, crate::trap::handle_syscall, crate::trap::post_trap_callback, debug!, esr.read, esr.read_as_enum ...(+6) |
| `arceos/modules/axhal/src/platform/riscv64_qemu_virt/irq.rs#register_handler` | `arceos/modules/axhal/src/platform/riscv64_qemu_virt/irq.rs#register_handler` | 同板块同名同结构 | with_cause! | with_cause! |
| `arceos/modules/axhal/src/platform/loongarch64_qemu_virt/irq.rs#set_enable` | `arceos/modules/axhal/src/platform/loongarch64_qemu_virt/irq.rs#set_enable` | 同板块同名同结构 | ecfg::read, ecfg::read().lie, ecfg::set_lie | ecfg::read, ecfg::read().lie, ecfg::set_lie |
| `arceos/modules/axhal/src/irq.rs#register_handler_common` | `arceos/modules/axhal/src/irq.rs#register_handler_common` | 同板块同名同结构 | IRQ_HANDLER_TABLE.register_handler, set_enable, warn! | IRQ_HANDLER_TABLE.register_handler, set_enable, warn! |

#### 系统调用
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `apps/nimbos/rust/src/arch/riscv.rs#sys_clone` | `apps/nimbos/rust/src/arch/riscv.rs#sys_clone` | 同板块同名同结构 | naked_asm! | naked_asm! |
| `apps/nimbos/rust/src/arch/x86_64.rs#sys_clone` | `apps/nimbos/rust/src/arch/x86_64.rs#sys_clone` | 同板块同名同结构 | naked_asm! | naked_asm! |
| `apps/nimbos/rust/src/arch/aarch64.rs#sys_clone` | `apps/nimbos/rust/src/arch/aarch64.rs#sys_clone` | 同板块同名同结构 | naked_asm! | naked_asm! |
| `apps/nimbos/rust/src/lib.rs#thread_spawn` | `apps/nimbos/rust/src/lib.rs#thread_spawn` | 同板块同名同结构 | AtomicUsize::new, THREAD_COUNT.fetch_add, THREAD_STACKS[thread_id].as_ptr_range, sys_clone | AtomicUsize::new, THREAD_COUNT.fetch_add, THREAD_STACKS[thread_id].as_ptr_range, sys_clone |

#### 文件系统
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `api/src/imp/fs/eventpoll.rs#sys_epoll_ctl` | `api/src/interface/fs/epoll.rs#sys_epoll_ctl` | 同板块同名 | Err, Ok, epoll.events.lock, event.get_as_ref, events.contains_key, events.get ...(+8) | EpollInstance::from_fd, Ok, event.get_as_ref, instance.control |
| `api/src/imp/fs/fd_ops.rs#sys_openat` | `api/src/interface/fs/fd.rs#sys_openat` | 同板块同名 | Directory::from_fd, Directory::new, Directory::new(
        dir.map_or_else(
            || axfs::fops::Directory::open_dir(path, &opts),
            |dir| dir.inner().open_dir_at(path, &opts),
        )?,
        real_path.to_string(),
    )
    .add_to_fd_table, File::new, File::new(r?, real_path.to_string()).add_to_fd_table, Ok ...(+21) | Ok, path.as_ref, path.get_as_str, sys_open_impl |
| `api/src/imp/fs/io.rs#sys_sendfile` | `api/src/interface/fs/io.rs#sys_sendfile` | 同板块同名 | Err, File::from_fd, Ok, SeekFrom::Current, SeekFrom::Start, Some ...(+9) | Ok, fd_lookup, nullable!, sys_copy_file_range_impl |
| `api/src/imp/fs/mount.rs#sys_mount` | `api/src/imp/fs/mount.rs#sys_mount` | 同板块同名 | Err, Ok, check_mounted, debug!, fs_type.get_as_str, handle_file_path ...(+5) | Ok |

#### 设备驱动
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `arceos/modules/axdriver/src/lib.rs#init_drivers` | `arceos/modules/axdriver/src/lib.rs#init_drivers` | 同板块同名同结构 | AllDevices::default, all_devs.block.iter, all_devs.block.iter().enumerate, all_devs.display.iter, all_devs.display.iter().enumerate, all_devs.net.iter ...(+5) | AllDevices::default, all_devs.block.iter, all_devs.block.iter().enumerate, all_devs.display.iter, all_devs.display.iter().enumerate, all_devs.net.iter ...(+5) |
| `arceos/modules/axdriver/src/virtio.rs#probe_pci` | `arceos/modules/axdriver/src/virtio.rs#probe_pci` | 同板块同名同结构 | D::try_new, Some, axdriver_virtio::probe_pci_device::<VirtIoHalImpl>, warn! | D::try_new, Some, axdriver_virtio::probe_pci_device::<VirtIoHalImpl>, warn! |
| `arceos/modules/axdriver/src/virtio.rs#probe_mmio` | `arceos/modules/axdriver/src/virtio.rs#probe_mmio` | 同板块同名同结构 | D::try_new, Some, axdriver_virtio::probe_mmio_device, base_vaddr.as_mut_ptr, mmio_base.into, phys_to_virt ...(+1) | D::try_new, Some, axdriver_virtio::probe_mmio_device, base_vaddr.as_mut_ptr, mmio_base.into, phys_to_virt ...(+1) |
| `arceos/modules/axdriver/src/bus/mmio.rs#probe_bus_devices` | `arceos/modules/axdriver/src/bus/mmio.rs#probe_bus_devices` | 同板块同名同结构 | for_each_drivers! | for_each_drivers! |

#### 网络协议栈
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `api/src/socket.rs#write_to_user` | `arceos/ulib/axlibc/c/network.c#inet_pton` | 同模块上下文对齐/差异观察 | Err, Ok, addr.get_as_mut, addr.is_null, core::ptr::copy_nonoverlapping, self.flowinfo ...(+7) | hexval, inet_pton, isdigit, memmove |
| `api/src/socket.rs#write_to_user` | `api/src/imp/net/socket.rs#setsockopt` | 同模块上下文对齐/差异观察 | Err, Ok, addr.get_as_mut, addr.is_null, core::ptr::copy_nonoverlapping, self.ip ...(+5) | Ok, SocketOptionLevel::try_from, error!, udpsocket.lock, unimplemented!, warn! |
| `api/src/imp/net.rs#gather_iovs` | `api/src/imp/net/socket.rs#sendto` | 同模块上下文对齐/差异观察 | (0..=1024).contains, Err, Ok, UserConstPtr::<iovec>::from, UserConstPtr::<iovec>::from(iov).get_as_slice, UserConstPtr::<u8>::from ...(+6) | Err, IpAddr::V4, Ipv4Addr::new, SocketAddr::new, udpsocket
                    .bind, udpsocket
                    .bind(SocketAddr::new(IpAddr::V4(Ipv4Addr::new(127, 0, 0, 1)), 0))
                    .map_err ...(+3) |
| `api/src/imp/net.rs#write_int_opt` | `api/src/imp/net/socket.rs#shutdown` | 同模块上下文对齐/差异观察 | Err, Ok, UserPtr::<c_int>::from, UserPtr::<c_int>::from(optval.address().as_usize()).get_as_mut, optlen.get_as_mut, optlen.is_null ...(+4) | Ok, tcpsocket.lock, tcpsocket.peer_addr, tcpsocket.shutdown, udpsocket.lock, udpsocket.peer_addr ...(+1) |

#### 文件系统/第三方库
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `arceos/modules/axfs/src/fs/lwext4_rust.rs#get_attr` | `modules/lwext4_rust/src/inode/attr.rs#get_attr` | 同板块同名 | <i32 as TryInto<AxError>>::try_into, <i32 as TryInto<AxError>>::try_into(e).unwrap, EXT4_LOCK.lock, Ok, VfsNodeAttr::new, VfsNodePerm::from_bits_truncate ...(+13) | decode_time, ext4_inode_get_blocks_count, get_block_size, self.gid, self.inode_type, self.mode ...(+6) |
| `arceos/modules/axfs/src/fs/lwext4_rust.rs#create` | `arceos/modules/axfs-ng/src/fs/ext4/inode.rs#create` | 同板块同名 | EXT4_LOCK.lock, Ok, e.try_into, e.try_into().unwrap, file.check_inode_exist, file.dir_mk ...(+13) | DirEntry::new_dir, DirEntry::new_file, DirNode::new, Err, FileNode::new, Inode::new ...(+11) |
| `arceos/modules/axfs/src/fs/lwext4_rust.rs#read_dir` | `arceos/modules/axfs-ng/src/fs/ext4/inode.rs#read_dir` | 同板块同名 | EXT4_LOCK.lock, Ok, VfsDirEntry::new, core::str::from_utf8, core::str::from_utf8(iname.unwrap()).unwrap, dirents.iter_mut ...(+14) | Ok, core::str::from_utf8, core::str::from_utf8(entry.name())
                .map_err, core::str::from_utf8(entry.name())
                .map_err(|_| VfsError::EINVAL)?
                .to_owned, entry.ino, entry.inode_type ...(+10) |
| `arceos/modules/axfs/src/fs/lwext4_rust.rs#lookup` | `modules/lwext4_rust/src/inode/dir.rs#lookup` | 同板块同名 | Arc::new, EXT4_LOCK.lock, Err, Ok, Self::new, file.check_inode_exist ...(+6) | Ok, ext4_dir_find_entry, ext4_dir_find_entry(
                &mut result,
                self.inner.as_mut(),
                name.as_ptr() as *const _,
                name.len() as _,
            )
            .context, mem::zeroed, name.as_ptr, name.len ...(+1) |

#### 网络协议栈/第三方库
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `arceos/modules/axnet/src/smoltcp_impl/tcp.rs#connect` | `arceos/modules/axnet/src/smoltcp_impl/tcp.rs#connect` | 同板块同名 | Err, Ok, SOCKET_SET
                .with_socket_mut::<tcp::Socket, _, _>, SOCKET_SET.add, SocketSetWrapper::new_tcp_socket, Some ...(+25) | Err, Ok, Ok::<(IpEndpoint, IpEndpoint), AxError>, SOCKET_SET
                .with_socket_mut::<tcp::Socket, _, _>, SOCKET_SET.add, SocketSetWrapper::new_tcp_socket ...(+32) |
| `arceos/modules/axnet/src/smoltcp_impl/dns.rs#query` | `arceos/modules/axnet/src/smoltcp_impl/dns.rs#query` | 同板块同名 | Err, Ok, SOCKET_SET
            .with_socket_mut::<dns::Socket, _, _>, SOCKET_SET
            .with_socket_mut::<dns::Socket, _, _>(handle, |socket| {
                socket.start_query(iface.lock().context(), name, query_type)
            })
            .map_err, SOCKET_SET.poll_interfaces, SOCKET_SET.with_socket_mut::<dns::Socket, _, _> ...(+12) | Err, Ok, SOCKET_SET
            .with_socket_mut::<dns::Socket, _, _>, SOCKET_SET
            .with_socket_mut::<dns::Socket, _, _>(handle, |socket| {
                socket.start_query(iface.lock().context(), name, query_type)
            })
            .map_err, SOCKET_SET.poll_interfaces, SOCKET_SET.with_socket_mut::<dns::Socket, _, _> ...(+12) |
| `arceos/modules/axnet/src/smoltcp_impl/bench.rs#bench_transmit_bandwidth` | `arceos/modules/axnet/src/smoltcp_impl/bench.rs#bench_transmit_bandwidth` | 同板块同名同结构 | (current_time - past_time).secs, AxNetTxToken::consume, InterfaceWrapper::current_time, info!, self.transmit, tx_buf[0..12].fill ...(+2) | (current_time - past_time).secs, AxNetTxToken::consume, InterfaceWrapper::current_time, info!, self.transmit, tx_buf[0..12].fill ...(+2) |
| `arceos/modules/axnet/src/smoltcp_impl/tcp.rs#shutdown` | `arceos/modules/axnet/src/smoltcp_impl/tcp.rs#shutdown` | 同板块同名同结构 | LISTEN_TABLE.unlisten, Ok, SOCKET_SET.poll_interfaces, SOCKET_SET.with_socket_mut::<tcp::Socket, _, _>, debug!, self.handle.get ...(+9) | LISTEN_TABLE.unlisten, Ok, SOCKET_SET.poll_interfaces, SOCKET_SET.with_socket_mut::<tcp::Socket, _, _>, debug!, self.handle.get ...(+9) |

#### 第三方库
仅目标项目中观察到该模块代表函数，参考项目没有同模块可对齐证据：
- 目标项目：`crates/axprocess/src/lib.rs#build`，调用摘要：Arc::new, AtomicBool::new, AtomicI32::new, INIT_PROC.call_once, Mutex::new, Once::new ...(+8)
- 目标项目：`crates/axsignal/src/lib.rs#from_repr`，调用摘要：Some
- 目标项目：`crates/axsignal/src/lib.rs#check_signals`，调用摘要：Some, self.blocked.lock, self.take_unblocked_signal, sig.signo
- 目标项目：`crates/axprocess/src/lib.rs#build`，调用摘要：Arc::downgrade, Arc::new, AtomicBool::new, AtomicI32::new, Once::new, thread.data.call_once ...(+2)

#### 通用模块/其他
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `arceos/modules/axhal/src/mem.rs#kernel_image_regions` | `arceos/modules/axhal/src/mem.rs#kernel_image_regions` | 同板块同名同结构 | (_sbss as usize).into, (_sdata as usize).into, (_srodata as usize).into, (_stext as usize).into, (boot_stack as usize).into, [
        MemRegion {
            paddr: virt_to_phys((_stext as usize).into()),
            size: _etext as usize - _stext as usize,
            flags: MemRegionFlags::RESERVED | MemRegionFlags::READ | MemRegionFlags::EXECUTE,
            name: ".text",
        },
        MemRegion {
            paddr: virt_to_phys((_srodata as usize).into()),
            size: _erodata as usize - _srodata as usize,
            flags: MemRegionFlags::RESERVED | MemRegionFlags::READ,
            name: ".rodata",
        },
        MemRegion {
            paddr: virt_to_phys((_sdata as usize).into()),
            size: _edata as usize - _sdata as usize,
            flags: MemRegionFlags::RESERVED | MemRegionFlags::READ | MemRegionFlags::WRITE,
            name: ".data .tdata .tbss .percpu",
        },
        MemRegion {
            paddr: virt_to_phys((boot_stack as usize).into()),
            size: boot_stack_top as usize - boot_stack as usize,
            flags: MemRegionFlags::RESERVED | MemRegionFlags::READ | MemRegionFlags::WRITE,
            name: "boot stack",
        },
        MemRegion {
            paddr: virt_to_phys((_sbss as usize).into()),
            size: _ebss as usize - _sbss as usize,
            flags: MemRegionFlags::RESERVED | MemRegionFlags::READ | MemRegionFlags::WRITE,
            name: ".bss",
        },
    ]
    .into_iter ...(+1) | (_sbss as usize).into, (_sdata as usize).into, (_srodata as usize).into, (_stext as usize).into, (boot_stack as usize).into, [
        MemRegion {
            paddr: virt_to_phys((_stext as usize).into()),
            size: _etext as usize - _stext as usize,
            flags: MemRegionFlags::RESERVED | MemRegionFlags::READ | MemRegionFlags::EXECUTE,
            name: ".text",
        },
        MemRegion {
            paddr: virt_to_phys((_srodata as usize).into()),
            size: _erodata as usize - _srodata as usize,
            flags: MemRegionFlags::RESERVED | MemRegionFlags::READ,
            name: ".rodata",
        },
        MemRegion {
            paddr: virt_to_phys((_sdata as usize).into()),
            size: _edata as usize - _sdata as usize,
            flags: MemRegionFlags::RESERVED | MemRegionFlags::READ | MemRegionFlags::WRITE,
            name: ".data .tdata .tbss .percpu",
        },
        MemRegion {
            paddr: virt_to_phys((boot_stack as usize).into()),
            size: boot_stack_top as usize - boot_stack as usize,
            flags: MemRegionFlags::RESERVED | MemRegionFlags::READ | MemRegionFlags::WRITE,
            name: "boot stack",
        },
        MemRegion {
            paddr: virt_to_phys((_sbss as usize).into()),
            size: _ebss as usize - _sbss as usize,
            flags: MemRegionFlags::RESERVED | MemRegionFlags::READ | MemRegionFlags::WRITE,
            name: ".bss",
        },
    ]
    .into_iter ...(+1) |
| `arceos/modules/axhal/src/platform/x86_pc/apic.rs#init_primary` | `arceos/modules/axhal/src/platform/x86_pc/apic.rs#init_primary` | 同板块同名同结构 | IO_APIC.init_once, IoApic::new, LOCAL_APIC.get, LOCAL_APIC.get().as_mut, LOCAL_APIC.get().as_mut().unwrap, LOCAL_APIC.get().as_mut().unwrap().write ...(+18) | IO_APIC.init_once, IoApic::new, LOCAL_APIC.get, LOCAL_APIC.get().as_mut, LOCAL_APIC.get().as_mut().unwrap, LOCAL_APIC.get().as_mut().unwrap().write ...(+18) |
| `arceos/modules/axsync/src/mutex.rs#lock` | `arceos/modules/axsync/src/mutex.rs#lock` | 同板块同名同结构 | assert_ne!, current, current().id, current().id().as_u64, self.is_locked, self.owner_id.compare_exchange_weak ...(+1) | assert_ne!, current, current().id, current().id().as_u64, self.is_locked, self.owner_id.compare_exchange_weak ...(+1) |
| `arceos/modules/axhal/src/platform/riscv64_qemu_virt/console.rs#write_bytes` | `arceos/modules/axhal/src/platform/riscv64_qemu_virt/console.rs#write_bytes` | 同板块同名同结构 | bytes.as_ptr, bytes.len, bytes.to_vec, kernel_bytes.len, try_write_bytes | bytes.as_ptr, bytes.len, bytes.to_vec, kernel_bytes.len, try_write_bytes |


好的，我将遵循审计指令，以仲裁专家身份，基于客观AST证据与双方画像，对本次同源性争议进行最终定性。

### 1. 宏观判定
- **定性结论**: **独立开发，底层架构存在显著差异**
  本次审计的客观AST相似度为23.69%，处于较低水平。目标项目与参考项目共同AST指纹为834个，而双方各自拥有大量唯一指纹（目标1534，参考2820），表明双方代码库主体结构独立。控方指控主要集中在`arceos/modules/axtask`等上游框架模块，未能有效证明目标项目对参考项目的独创性表达构成抄袭。辩方关于“共同依赖开源上游框架”的辩护，与AST证据中大量同路径、同结构的框架代码高度吻合，构成更合理的解释。

### 2. 全模块覆盖矩阵解读
- **进程管理**
  - 双方均存在该模块。
  - 存在共同AST指纹（30个）。
  - 目标覆盖率40.54%，参考覆盖率24.79%。覆盖率中等，但重合指纹集中于`arceos/modules/axtask`等公共框架路径。
  - **证据强度: 弱**。控方主证据均来自上游框架，双方原创部分（如`do_exit`）调用图谱完全不同，不构成同源。

- **任务调度/异步运行时**
  - 双方均存在该模块。
  - 存在大量共同AST指纹（174个）。
  - 目标覆盖率67.97%，参考覆盖率58.0%。覆盖率较高，但证据清单显示重合项几乎全部位于`arceos/modules/axtask`、`arceos/modules/axhal`等框架路径。
  - **证据强度: 弱**。高覆盖率由共同依赖的ArceOS内核框架导致，非项目间抄袭。

- **内存管理**
  - 双方均存在该模块。
  - 存在大量共同AST指纹（189个）。
  - 目标覆盖率73.54%，参考覆盖率49.48%。覆盖率较高。
  - **证据强度: 弱**。证据清单中的`try_clone`、`populate_area`等函数均位于`arceos/modules/axmm`框架内。而双方原创的`sys_mmap`调用图谱差异巨大（目标操作虚拟地址，参考操作物理地址和文件描述符），证明顶层设计独立。

- **中断/异常处理**
  - 双方均存在该模块。
  - 存在大量共同AST指纹（46个）。
  - 目标覆盖率97.87%，参考覆盖率97.87%。覆盖率极高。
  - **证据强度: 弱**。证据清单中的`handle_sync_exception`、`register_handler`等函数均位于`arceos/modules/axhal`框架内，是硬件架构强相关的标准实现，几乎无独创空间。

- **系统调用**
  - 双方均存在该模块。
  - 存在共同AST指纹（29个）。
  - 目标覆盖率48.33%，参考覆盖率33.72%。
  - **证据强度: 弱**。重合项如`sys_clone`是架构相关的汇编实现（`naked_asm!`），`thread_spawn`是NimbOS示例代码，均属公共模板或上游代码。

- **文件系统**
  - 双方均存在该模块。
  - 存在共同AST指纹（65个）。
  - 目标覆盖率16.88%，参考覆盖率9.73%。覆盖率低。
  - **证据强度: 未形成**。低覆盖率表明双方文件系统实现差异巨大。证据清单中的同名函数（如`sys_openat`）调用图谱完全不同，目标采用`Directory::open_dir`路径，参考则封装为`sys_open_impl`。

- **设备驱动**
  - 双方均存在该模块。
  - 存在大量共同AST指纹（62个）。
  - 目标覆盖率89.86%，参考覆盖率50.82%。覆盖率较高。
  - **证据强度: 弱**。证据清单中的`init_drivers`、`probe_pci`等函数均位于`arceos/modules/axdriver`框架内，是VirtIO等标准总线驱动的初始化模板。

- **网络协议栈**
  - 双方均存在该模块。
  - 共同AST指纹为0。
  - 目标覆盖率0.0%，参考覆盖率0.0%。
  - **证据强度: 未形成**。双方在该模块未观察到任何共同结构指纹，是完全独立实现或使用了不同版本的第三方库。

- **第三方库/公共依赖**
  - 双方均存在`文件系统/第三方库`和`网络协议栈/第三方库`子模块。
  - `文件系统/第三方库`共同指纹为0，`网络协议栈/第三方库`共同指纹为73个。
  - `网络协议栈/第三方库`目标覆盖率80.22%，参考覆盖率62.93%。
  - **证据强度: 弱**。`网络协议栈/第三方库`的高覆盖率源于双方共同依赖的`smoltcp`库。`文件系统/第三方库`的共同指纹为0，说明双方使用了不同版本或不同库（如`lwext4_rust` vs `axfs-ng`）。目标项目独有的`第三方库`模块（`crates/axprocess`等）是独立开发的直接证据。

### 3. 底层 AST 关键证据指纹表
| 所属板块 | 目标项目路径 | 参考项目路径 | 证据类型 | 技术说明 |
| :--- | :--- | :--- | :--- | :--- |
| 进程管理 | `api/src/imp/task/exit.rs#do_exit` | `arceos/examples/shell/src/cmd.rs#do_exit` | 架构差异 | 目标为内核进程退出逻辑，调用`FD_TABLE.clear`等内核对象；参考为用户态Shell示例，调用`println!`。领域完全不同。 |
| 进程管理 | `core/src/task.rs#get_process_group` | `process/src/process_group.rs#get_process_group` | 架构差异 | 目标使用`PROCESS_GROUP_TABLE.read().get().ok_or`（读锁+Result）；参考使用`PROCESS_GROUP_TABLE.lock().get().cloned`（互斥锁+Option）。同步原语与错误处理模型不同。 |
| 内存管理 | `api/src/imp/mm/mmap.rs#sys_mmap` | `api/src/imp/mm/mmap.rs#sys_mmap` | 架构差异 | 目标核心操作`VirtAddr::from`、`(addr + length).align_up`，面向虚拟内存；参考核心操作`File::from_fd`、`PhysAddr::from`，面向文件与物理内存。 |
| 系统调用 | `api/src/imp/task/thread.rs#sys_getppid` | `api/src/imp/task/thread.rs#sys_getppid` | 架构差异 | 目标调用链为`axtask::current().task_ext().thread.process().parent().unwrap().pid`；参考封装为`current_process().get_parent().get_pid`。抽象层次与错误处理（`unwrap` vs 内部处理）不同。 |
| 文件系统 | `api/src/imp/fs/fd_ops.rs#sys_openat` | `api/src/interface/fs/fd.rs#sys_openat` | 架构差异 | 目标实现`Directory::open_dir`或`open_dir_at`的路径分发；参考封装为`sys_open_impl`。文件系统操作模型不同。 |
| 任务调度/异步运行时 | `arceos/modules/axtask/src/wait_queue.rs#cancel_events` | `arceos/modules/axtask/src/wait_queue.rs#cancel_events` | 公共框架 | 路径与调用图谱完全一致，是双方共同依赖的ArceOS上游框架代码，非项目间抄袭。 |
| 中断/异常处理 | `arceos/modules/axhal/src/arch/aarch64/trap.rs#handle_sync_exception` | `arceos/modules/axhal/src/arch/aarch64/trap.rs#handle_sync_exception` | 公共框架 | 路径与调用图谱完全一致，是双方共同依赖的ArceOS上游框架代码。 |
| 设备驱动 | `arceos/modules/axdriver/src/lib.rs#init_drivers` | `arceos/modules/axdriver/src/lib.rs#init_drivers` | 公共框架 | 路径与调用图谱完全一致，是双方共同依赖的ArceOS上游框架代码。 |
| 第三方库 | `crates/axsignal/src/lib.rs#check_signals` | (无) | 独立原创 | 目标项目独有的`axsignal`模块，参考项目无对应证据，是独立开发的直接证明。 |

### 4. 核心代码调用流对比剖析
* **【进程管理/进程退出】**
* **底层技术析理**: 目标项目的`do_exit`是一个完整的内核进程退出路径，涉及文件描述符表清理（`FD_TABLE.clear`）、信号发送（`SignalInfo::new`）、子进程清理（`clear_child_tid`）等复杂的内核资源回收逻辑。参考项目的`do_exit`位于用户态Shell示例中，仅调用标准库的`println!`和`std::process::exit`。两者在抽象层次（内核态 vs 用户态）、操作对象（内核数据结构 vs 标准库）和功能复杂度上完全不同，不存在同源性。
* **代码溯源链接**:
  - 目标项目: `api/src/imp/task/exit.rs#do_exit`
  - 参考项目: `arceos/examples/shell/src/cmd.rs#do_exit`

* **【内存管理/mmap系统调用】**
* **底层技术析理**: 目标项目的`sys_mmap`实现围绕虚拟内存操作展开，核心调用涉及`VirtAddr::from`、`(addr + length).align_up`等地址计算与对齐，映射标志通过`MmapFlags::from_bits_truncate`处理。参考项目的实现则耦合了文件系统，核心调用为`File::from_fd`和`PhysAddr::from`，表明其设计更侧重于文件映射到物理内存的场景。两者在内存模型（虚拟地址 vs 物理地址）和核心对象（纯地址 vs 文件描述符）上存在根本性架构差异。
* **代码溯源链接**:
  - 目标项目: `api/src/imp/mm/mmap.rs#sys_mmap`
  - 参考项目: `api/src/imp/mm/mmap.rs#sys_mmap`

* **【进程管理/获取进程组】**
* **底层技术析理**: 目标项目在`core/src/task.rs`中实现`get_process_group`，使用读写锁（`RwLock`）的`read`方法进行并发保护，并通过`ok_or`将`Option`转换为`Result`类型，体现了Rust的错误处理范式。参考项目在`process/src/process_group.rs`中实现，使用互斥锁（`Mutex`）的`lock`方法，并通过`cloned`直接返回`Option`。两者在同步原语的选择（`RwLock` vs `Mutex`）和错误处理模型（`Result` vs `Option`）上做出了不同的设计决策，表明其进程管理子系统是独立构建的。
* **代码溯源链接**:
  - 目标项目: `core/src/task.rs#get_process_group`
  - 参考项目: `process/src/process_group.rs#get_process_group`

### 5. 单方模块与降权项
- **目标项目独有模块**: `第三方库`模块（包含`crates/axprocess`、`crates/axsignal`等）仅在目标项目中作为独立模块被识别，参考项目无此分类。这是目标项目进行独立架构设计与开发的直接证据。
- **公共框架/第三方库代码**: 所有位于`arceos/modules/*`、`apps/nimbos/*`路径下的函数，以及`lwext4_rust`、`smoltcp`等库的接口调用，均属于双方共同依赖的上游代码。控方基于此类代码提出的“同板块底层逻辑一致”指控，不能作为项目间抄袭的证据，应予降权。
- **跨板块同名函数**: 如`do_exit`（目标位于内核`api/`，参考位于用户态`examples/`），因所属领域和实现逻辑完全不同，不构成同源证据。
- **Trivial函数**: 如`is_clone_child`（仅返回`Some`）、`busy_wait_until`（仅包含标准轮询循环）等，因逻辑过于简单，独创性空间极低，不能作为强证据。

### 6. 保守结论
本次审计的宏观AST相似度仅为23.69%，远低于可判定为抄袭的阈值。控方提出的“100%镜像复用”指控，其证据几乎全部集中于`arceos/modules/axtask`等双方共同依赖的ArceOS开源内核框架路径。这一现象最合理的解释是双方均基于同一上游项目进行开发，而非一方抄袭另一方。在双方存在差异的原创模块中，如进程管理的`do_exit`、内存管理的`sys_mmap`，AST证据清晰地揭示了它们在抽象层次、内存模型、同步原语和错误处理等核心架构决策上的显著差异。此外，目标项目存在`crates/axprocess`等独有的原创模块。综上，现有证据强力支持双方为独立开发，整体代码同源性风险极低。

------------------------------

