# 🛡️ OS 内核赛自动化代码级架构审计报告

**审计时间**: `2026-07-02 05:43:44`
**参赛作品名称**: `2026-T2026104869911108-XX`
**年份**: `2026`
**学校**: `XX`
**队伍名称**: `T2026104869911108`
**比赛名字**: `OS-Competition`
**比赛子赛道**: `Kernel-Implementation`
**仓库地址**: `https://gitlab.eduxiji.net/educg-group-43501-3132633/T2026104869911108-2514`
**推测开发语言**: `Rust / C (由AST探针自动识别)`
**特征函数量**: `1836`

## ⚔️ 底层代码同源性对比 (AST Homology)
> 本部分由专门的代码审查智能体，对该项目与符合年份条件的历届最高相似度项目进行控制流与底层调用对比。新版报告会先给出确定性的全模块覆盖矩阵，再给出 Agent 的分模块解释，避免只围绕少量抽样函数展开。

### 🏆 Top 2 底层对标: vs `2025-九莲宝灯一向听-天津理工大学`（天津理工大学 / 九莲宝灯一向听）

#### 对方作品信息

**对方年份**: `2025`
**对方学校**: `天津理工大学`
**对方队伍名称**: `九莲宝灯一向听`
**对方比赛名字**: `OS-Competition`
**对方比赛子赛道**: `Kernel-Implementation`
**对方仓库地址**: `https://gitlab.eduxiji.net/educg-group-36002-2710490/T202510060995409-3585`

> 双向覆盖率：共同 AST 指纹: 1074；唯一 AST 指纹: 目标项目 1534，参考项目 2610；覆盖率: 目标项目 70.01%，参考项目 41.15%

## 全模块对比总览（确定性证据）

> 总体覆盖率：共同 AST 指纹: 1074；唯一 AST 指纹: 目标项目 1534，参考项目 2610；覆盖率: 目标项目 70.01%，参考项目 41.15%

### 1. 模块覆盖矩阵
| 模块 | 目标函数数 | 参考函数数 | 共同唯一 AST 指纹 | Target 覆盖 | Base 覆盖 | 状态 |
| :--- | ---: | ---: | ---: | ---: | ---: | :--- |
| 进程管理 | 75 | 79 | 48 | 64.86% | 65.75% | 双方存在且有结构重合 |
| 任务调度/异步运行时 | 356 | 385 | 199 | 77.73% | 72.36% | 双方存在且有结构重合 |
| 内存管理 | 270 | 301 | 168 | 65.37% | 61.76% | 双方存在且有结构重合 |
| 中断/异常处理 | 53 | 60 | 43 | 91.49% | 84.31% | 双方存在且有结构重合 |
| 系统调用 | 79 | 130 | 35 | 58.33% | 32.71% | 双方存在且有结构重合 |
| 文件系统 | 458 | 616 | 245 | 63.64% | 53.96% | 双方存在且有结构重合 |
| 设备驱动 | 76 | 156 | 58 | 84.06% | 40.85% | 双方存在且有结构重合 |
| 网络协议栈 | 10 | 61 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |
| 文件系统/第三方库 | 22 | 577 | 11 | 50.0% | 2.28% | 双方存在且有结构重合 |
| 网络协议栈/第三方库 | 99 | 130 | 73 | 80.22% | 62.93% | 双方存在且有结构重合 |
| 第三方库 | 74 | 209 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |
| 通用模块/其他 | 264 | 630 | 212 | 89.45% | 44.07% | 双方存在且有结构重合 |

### 2. 分模块证据清单

#### 进程管理
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `api/src/imp/task/exit.rs#do_exit` | `api/src/imp/task/exit.rs#do_exit` | 同板块同名 | Arc::strong_count, FD_TABLE.clear, SignalInfo::new, UserPtr::<Pid>::from, axtask::exit, axtask::yield_now ...(+31) | FD_TABLE.clear, SignalInfo::new, UserPtr::<Pid>::from, axtask::exit, axtask::yield_now, clear_child_tid.get_as_mut ...(+22) |
| `api/src/imp/task/execve.rs#sys_execve` | `api/src/imp/task/execve.rs#sys_execve` | 同板块同名 | Err, Ok, arg.get_as_str, arg.get_as_str().map, args.is_empty, args.push ...(+40) | Err, Ok, arg.get_as_str, arg.get_as_str().map, args.to_vec, argv
        .get_as_null_terminated ...(+38) |
| `api/src/imp/task/wait.rs#sys_waitpid` | `api/src/imp/task/wait.rs#sys_waitpid` | 同板块同名同结构 | Err, Ok, WaitOptions::from_bits_truncate, WaitPid::Pgid, WaitPid::Pid, child.data::<ProcessData> ...(+25) | Err, Ok, WaitOptions::from_bits_truncate, WaitPid::Pgid, WaitPid::Pid, child.data::<ProcessData> ...(+25) |
| `arceos/modules/axtask/src/run_queue.rs#exit_current` | `arceos/modules/axtask/src/run_queue.rs#exit_current` | 同板块同名同结构 | EXITED_TASKS.current_ref_mut_raw, EXITED_TASKS.current_ref_mut_raw().clear, EXITED_TASKS.current_ref_mut_raw().push_back, WAIT_FOR_EXIT.current_ref_mut_raw, WAIT_FOR_EXIT.current_ref_mut_raw().notify_one, assert! ...(+8) | EXITED_TASKS.current_ref_mut_raw, EXITED_TASKS.current_ref_mut_raw().clear, EXITED_TASKS.current_ref_mut_raw().push_back, WAIT_FOR_EXIT.current_ref_mut_raw, WAIT_FOR_EXIT.current_ref_mut_raw().notify_one, assert! ...(+8) |

#### 任务调度/异步运行时
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `src/syscall.rs#handle_syscall` | `src/syscall.rs#handle_syscall` | 同板块同名 | Err, Sysno::from, err.code, info!, result.unwrap_or_else, sys_accept ...(+178) | Ok, Sysno::from, err.code, info!, result.unwrap_or_else, stub_bypass ...(+115) |
| `arceos/modules/axlog/src/lib.rs#log` | `arceos/modules/axlog/src/lib.rs#log` | 同板块同名同结构 | cfg_if::cfg_if!, record.level, record.line, record.line().unwrap_or, record.metadata, record.target ...(+1) | cfg_if::cfg_if!, record.level, record.line, record.line().unwrap_or, record.metadata, record.target ...(+1) |
| `api/src/imp/futex.rs#sys_futex` | `api/src/imp/futex.rs#sys_futex` | 同板块同名同结构 | Err, Ok, axtask::yield_now, curr.task_ext, curr.task_ext().process_data, current ...(+16) | Err, Ok, axtask::yield_now, curr.task_ext, curr.task_ext().process_data, current ...(+16) |
| `api/src/imp/fs/stat.rs#sys_statx` | `api/src/imp/fs/stat.rs#sys_statx` | 同板块同名同结构 | Err, Ok, debug!, f.stat, f.stat()?.into, get_file_like ...(+9) | Err, Ok, debug!, f.stat, f.stat()?.into, get_file_like ...(+9) |

#### 内存管理
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `api/src/imp/task/clone.rs#sys_clone` | `api/src/imp/task/clone.rs#sys_clone` | 同板块同名 | Arc::default, Arc::new, CURRENT_DIR
                .deref_from, CURRENT_DIR
                .deref_from(&process_data.ns)
                .init_new, CURRENT_DIR
                .deref_from(&process_data.ns)
                .init_shared, CURRENT_DIR.copy_inner ...(+73) | Arc::default, Arc::new, CURRENT_DIR
                .deref_from, CURRENT_DIR
                .deref_from(&process_data.ns)
                .init_new, CURRENT_DIR
                .deref_from(&process_data.ns)
                .init_shared, CURRENT_DIR.copy_inner ...(+72) |
| `arceos/modules/axmm/src/aspace.rs#populate_area` | `arceos/modules/axmm/src/aspace.rs#populate_area` | 同板块同名 | Err, Ok, PageIterWrapper::new, PageIterWrapper::new(
                    start.align_down(align),
                    end.align_up(align).min(area.end()),
                    align,
                )
                .unwrap, Self::handle_cow_fault, _access_flags.contains ...(+14) | Err, Ok, PageIter4K::new, PageIter4K::new(start, area.end().min(end)).unwrap, area.backend, area.end ...(+8) |
| `api/src/imp/mm/mmap.rs#sys_mmap` | `api/src/imp/mm/mmap.rs#sys_mmap` | 同板块同名 | (addr + length).align_up, Err, MmapFlags::from_bits_truncate, MmapProt::from_bits_truncate, Ok, VirtAddr::from ...(+26) | Err, File::from_fd, MmapFlags::from_bits_truncate, MmapProt::from_bits_truncate, Ok, VirtAddr::from ...(+27) |
| `arceos/modules/axruntime/src/lib.rs#rust_main` | `arceos/modules/axruntime/src/lib.rs#rust_main` | 同板块同名 | INITED_CPUS.fetch_add, ax_println!, axdisplay::init_display, axdriver::init_drivers, axfs::init_filesystems, axhal::mem::memory_regions ...(+20) | INITED_CPUS.fetch_add, ax_println!, axdisplay::init_display, axdriver::init_drivers, axfs::init_filesystems, axhal::mem::memory_regions ...(+20) |

#### 中断/异常处理
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `arceos/modules/axhal/src/arch/aarch64/trap.rs#handle_sync_exception` | `arceos/modules/axhal/src/arch/aarch64/trap.rs#handle_sync_exception` | 同板块同名同结构 | ESR_EL1.extract, crate::trap::handle_syscall, crate::trap::post_trap_callback, debug!, esr.read, esr.read_as_enum ...(+6) | ESR_EL1.extract, crate::trap::handle_syscall, crate::trap::post_trap_callback, debug!, esr.read, esr.read_as_enum ...(+6) |
| `arceos/modules/axhal/src/platform/riscv64_qemu_virt/irq.rs#register_handler` | `arceos/modules/axhal/src/platform/riscv64_qemu_virt/irq.rs#register_handler` | 同板块同名同结构 | with_cause! | with_cause! |
| `arceos/modules/axhal/src/platform/loongarch64_qemu_virt/irq.rs#set_enable` | `arceos/modules/axhal/src/platform/loongarch64_qemu_virt/irq.rs#set_enable` | 同板块同名同结构 | ecfg::read, ecfg::read().lie, ecfg::set_lie | ecfg::read, ecfg::read().lie, ecfg::set_lie |
| `arceos/modules/axhal/src/irq.rs#register_handler_common` | `arceos/modules/axhal/src/irq.rs#register_handler_common` | 同板块同名同结构 | IRQ_HANDLER_TABLE.register_handler, set_enable, warn! | IRQ_HANDLER_TABLE.register_handler, set_enable, warn! |

#### 系统调用
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `api/src/imp/net.rs#sys_socket` | `arceos/api/arceos_posix_api/src/imp/net.rs#sys_socket` | 同板块同名 | Err, Mutex::new, Ok, Socket::Tcp, Socket::Udp, TcpSocket::new ...(+4) | debug!, syscall_body! |
| `apps/nimbos/rust/src/arch/riscv.rs#sys_clone` | `apps/nimbos/rust/src/arch/riscv.rs#sys_clone` | 同板块同名同结构 | naked_asm! | naked_asm! |
| `apps/nimbos/rust/src/arch/x86_64.rs#sys_clone` | `apps/nimbos/rust/src/arch/x86_64.rs#sys_clone` | 同板块同名同结构 | naked_asm! | naked_asm! |
| `apps/nimbos/rust/src/arch/aarch64.rs#sys_clone` | `apps/nimbos/rust/src/arch/aarch64.rs#sys_clone` | 同板块同名同结构 | naked_asm! | naked_asm! |

#### 文件系统
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `arceos/modules/axfs/src/fops.rs#_open_at` | `arceos/modules/axfs/src/fops.rs#_open_at` | 同板块同名 | Err, Ok, WithCap::new, attr.is_dir, attr.perm, ax_err! ...(+10) | Err, Ok, WithCap::new, attr.is_dir, attr.perm, ax_err! ...(+10) |
| `api/src/imp/fs/fd_ops.rs#sys_openat` | `api/src/imp/fs/fd_ops.rs#sys_openat` | 同板块同名 | Directory::from_fd, Directory::new, Directory::new(
        dir.map_or_else(
            || axfs::fops::Directory::open_dir(path, &opts),
            |dir| dir.inner().open_dir_at(path, &opts),
        )?,
        real_path.to_string(),
    )
    .add_to_fd_table, File::new, File::new(r?, real_path.to_string()).add_to_fd_table, Ok ...(+21) | Directory::from_fd, Directory::new, Directory::new(
        dir.map_or_else(
            || axfs::fops::Directory::open_dir(path, &opts),
            |dir| dir.inner().open_dir_at(path, &opts),
        )?,
        real_path.to_string(),
    )
    .add_to_fd_table, File::new, File::new(r?, real_path.to_string()).add_to_fd_table, Ok ...(+16) |
| `api/src/imp/fs/io.rs#sys_sendfile` | `api/src/imp/fs/io.rs#sys_sendfile` | 同板块同名 | Err, File::from_fd, Ok, SeekFrom::Current, SeekFrom::Start, Some ...(+9) | Ok, debug!, dest.as_ref, do_sendfile, get_file_like, if let Some(offset) = offset {
        let src = src
            .into_any()
            .downcast::<File>()
            .map_err(|_| LinuxError::ESPIPE)?;

        do_sendfile(
            |buf| {
                let bytes_read = src.inner().read_at(*offset, buf)?;
                *offset += bytes_read as u64;
                Ok(bytes_read)
            },
            dest.as_ref(),
        )
    } else {
        do_sendfile(|buf| src.read(buf), dest.as_ref())
    }
    .map ...(+7) |
| `arceos/modules/axfs/src/dev.rs#write_one` | `arceos/modules/axfs/src/dev.rs#write_one` | 同板块同名同结构 | Ok, buf.len, buf.len().min, buf[0..BLOCK_SIZE].to_vec, data[start..start + count].copy_from_slice, self.dev.read_block ...(+1) | Ok, buf.len, buf.len().min, buf[0..BLOCK_SIZE].to_vec, data[start..start + count].copy_from_slice, self.dev.read_block ...(+1) |

#### 设备驱动
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `arceos/modules/axdriver/src/lib.rs#init_drivers` | `arceos/modules/axdriver/src/lib.rs#init_drivers` | 同板块同名 | AllDevices::default, all_devs.block.iter, all_devs.block.iter().enumerate, all_devs.display.iter, all_devs.display.iter().enumerate, all_devs.net.iter ...(+5) | AllDevices::default, all_devs.block.iter, all_devs.block.iter().enumerate, all_devs.display.iter, all_devs.display.iter().enumerate, all_devs.net.iter ...(+5) |
| `arceos/modules/axdriver/src/virtio.rs#probe_pci` | `arceos/modules/axdriver/src/virtio.rs#probe_pci` | 同板块同名同结构 | D::try_new, Some, axdriver_virtio::probe_pci_device::<VirtIoHalImpl>, warn! | D::try_new, Some, axdriver_virtio::probe_pci_device::<VirtIoHalImpl>, warn! |
| `arceos/modules/axdriver/src/virtio.rs#probe_mmio` | `arceos/modules/axdriver/src/virtio.rs#probe_mmio` | 同板块同名 | D::try_new, Some, axdriver_virtio::probe_mmio_device, base_vaddr.as_mut_ptr, mmio_base.into, phys_to_virt ...(+1) | D::try_new, Some, axdriver_virtio::probe_mmio_device, base_vaddr.as_mut_ptr, mmio_base.into, phys_to_virt ...(+1) |
| `arceos/modules/axdriver/src/bus/mmio.rs#probe_bus_devices` | `arceos/modules/axdriver/src/bus/mmio.rs#probe_bus_devices` | 同板块同名同结构 | for_each_drivers! | for_each_drivers! |

#### 网络协议栈
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `api/src/socket.rs#write_to_user` | `arceos/ulib/axlibc/c/network.c#inet_pton` | 同模块上下文对齐/差异观察 | Err, Ok, addr.get_as_mut, addr.is_null, core::ptr::copy_nonoverlapping, self.flowinfo ...(+7) | hexval, inet_pton, isdigit, memmove |
| `api/src/socket.rs#write_to_user` | `arceos/api/arceos_posix_api/src/imp/net.rs#shutdown` | 同模块上下文对齐/差异观察 | Err, Ok, addr.get_as_mut, addr.is_null, core::ptr::copy_nonoverlapping, self.ip ...(+5) | Ok, tcpsocket.lock, tcpsocket.peer_addr, tcpsocket.shutdown, udpsocket.lock, udpsocket.peer_addr ...(+1) |
| `api/src/imp/net.rs#gather_iovs` | `arceos/ulib/axlibc/c/socket.c#accept4` | 同模块上下文对齐/差异观察 | (0..=1024).contains, Err, Ok, UserConstPtr::<iovec>::from, UserConstPtr::<iovec>::from(iov).get_as_slice, UserConstPtr::<u8>::from ...(+6) | accept, fcntl |
| `api/src/imp/net.rs#write_int_opt` | `arceos/api/arceos_posix_api/src/imp/net.rs#recvfrom` | 同模块上下文对齐/差异观察 | Err, Ok, UserPtr::<c_int>::from, UserPtr::<c_int>::from(optval.address().as_usize()).get_as_mut, optlen.get_as_mut, optlen.is_null ...(+4) | Ok, Some, tcpsocket.lock, tcpsocket.lock().recv, tcpsocket.lock().recv(buf).map, udpsocket
                .lock ...(+2) |

#### 文件系统/第三方库
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `arceos/modules/axfs/src/fs/lwext4_rust.rs#get_attr` | `arceos/modules/axfs/src/fs/lwext4_rust.rs#get_attr` | 同板块同名 | <i32 as TryInto<AxError>>::try_into, <i32 as TryInto<AxError>>::try_into(e).unwrap, EXT4_LOCK.lock, Ok, VfsNodeAttr::new, VfsNodePerm::from_bits_truncate ...(+13) | <i32 as TryInto<AxError>>::try_into, <i32 as TryInto<AxError>>::try_into(e).unwrap, Ok, VfsNodeAttr::new, VfsNodePerm::from_bits_truncate, file.file_close ...(+12) |
| `arceos/modules/axfs/src/fs/lwext4_rust.rs#create` | `arceos/modules/axfs/src/fs/lwext4_rust.rs#create` | 同板块同名 | EXT4_LOCK.lock, Ok, e.try_into, e.try_into().unwrap, file.check_inode_exist, file.dir_mk ...(+13) | Ok, e.try_into, e.try_into().unwrap, file.check_inode_exist, file.dir_mk, file.dir_mk(fpath)
                    .map ...(+12) |
| `arceos/modules/axfs/src/fs/lwext4_rust.rs#read_dir` | `arceos/modules/axfs/src/fs/lwext4_rust.rs#read_dir` | 同板块同名 | EXT4_LOCK.lock, Ok, VfsDirEntry::new, core::str::from_utf8, core::str::from_utf8(iname.unwrap()).unwrap, dirents.iter_mut ...(+14) | Ok, VfsDirEntry::new, core::str::from_utf8, core::str::from_utf8(iname.unwrap()).unwrap, dirents.iter_mut, dirents.iter_mut().enumerate ...(+13) |
| `arceos/modules/axfs/src/fs/lwext4_rust.rs#lookup` | `arceos/modules/axfs/src/fs/lwext4_rust.rs#lookup` | 同板块同名 | Arc::new, EXT4_LOCK.lock, Err, Ok, Self::new, file.check_inode_exist ...(+6) | Arc::new, Err, Ok, Self::new, file.check_inode_exist, fpath.as_str ...(+5) |

#### 网络协议栈/第三方库
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `arceos/modules/axnet/src/smoltcp_impl/tcp.rs#connect` | `arceos/modules/axnet/src/smoltcp_impl/tcp.rs#connect` | 同板块同名 | Err, Ok, SOCKET_SET
                .with_socket_mut::<tcp::Socket, _, _>, SOCKET_SET.add, SocketSetWrapper::new_tcp_socket, Some ...(+25) | Err, Ok, Ok::<(IpEndpoint, IpEndpoint), AxError>, SOCKET_SET
                .with_socket_mut::<tcp::Socket, _, _>, SOCKET_SET.add, SocketSetWrapper::new_tcp_socket ...(+32) |
| `arceos/modules/axnet/src/smoltcp_impl/dns.rs#query` | `arceos/modules/axnet/src/smoltcp_impl/dns.rs#query` | 同板块同名 | Err, Ok, SOCKET_SET
            .with_socket_mut::<dns::Socket, _, _>, SOCKET_SET
            .with_socket_mut::<dns::Socket, _, _>(handle, |socket| {
                socket.start_query(iface.lock().context(), name, query_type)
            })
            .map_err, SOCKET_SET.poll_interfaces, SOCKET_SET.with_socket_mut::<dns::Socket, _, _> ...(+12) | Err, Ok, SOCKET_SET
            .with_socket_mut::<dns::Socket, _, _>, SOCKET_SET
            .with_socket_mut::<dns::Socket, _, _>(handle, |socket| {
                socket.start_query(iface.lock().context(), name, query_type)
            })
            .map_err, SOCKET_SET.poll_interfaces, SOCKET_SET.with_socket_mut::<dns::Socket, _, _> ...(+12) |
| `arceos/modules/axnet/src/smoltcp_impl/bench.rs#bench_transmit_bandwidth` | `arceos/modules/axnet/src/smoltcp_impl/bench.rs#bench_transmit_bandwidth` | 同板块同名同结构 | (current_time - past_time).secs, AxNetTxToken::consume, InterfaceWrapper::current_time, info!, self.transmit, tx_buf[0..12].fill ...(+2) | (current_time - past_time).secs, AxNetTxToken::consume, InterfaceWrapper::current_time, info!, self.transmit, tx_buf[0..12].fill ...(+2) |
| `arceos/modules/axnet/src/smoltcp_impl/tcp.rs#shutdown` | `arceos/modules/axnet/src/smoltcp_impl/tcp.rs#shutdown` | 同板块同名同结构 | LISTEN_TABLE.unlisten, Ok, SOCKET_SET.poll_interfaces, SOCKET_SET.with_socket_mut::<tcp::Socket, _, _>, debug!, self.handle.get ...(+9) | LISTEN_TABLE.unlisten, Ok, SOCKET_SET.poll_interfaces, SOCKET_SET.with_socket_mut::<tcp::Socket, _, _>, debug!, self.handle.get ...(+9) |

#### 第三方库
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `crates/axprocess/src/lib.rs#free` | `crates/axdriver_net/src/dwmac/mempool.rs#free` | 同板块同名 | parent.children.lock, parent.children.lock().retain, self.freed.store, self.parent | self.free.borrow_mut, self.free.borrow_mut().push_back |
| `crates/axprocess/src/lib.rs#build` | `crates/axdriver_net/src/dwmac/mod.rs#init_phy` | 同模块上下文对齐/差异观察 | Arc::new, AtomicBool::new, AtomicI32::new, INIT_PROC.call_once, Mutex::new, Once::new ...(+8) | Err, H::wait_until, Ok, Yt8531cPhy::<H>::new, core::time::Duration::from_millis, log::error! ...(+18) |
| `crates/axsignal/src/lib.rs#from_repr` | `crates/axdriver_net/src/dwmac/mod.rs#init` | 同模块上下文对齐/差异观察 | Some | AtomicBool::new, DescriptorRing::<RX_DESC_COUNT, H>::new, DescriptorRing::<TX_DESC_COUNT, H>::new, H::configure_platform, Ok, log::error! ...(+20) |
| `crates/axsignal/src/lib.rs#check_signals` | `crates/visionfive2-sd/src/lib.rs#send_cmd` | 同模块上下文对齐/差异观察 | Some, self.blocked.lock, self.take_unblocked_signal, sig.signo | RawInterrupt::from, RawInterruptStatusReg::from, S::sleep_ms_until, Some, arg.into, assert! ...(+22) |

#### 通用模块/其他
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `arceos/modules/axhal/src/mem.rs#kernel_image_regions` | `arceos/modules/axhal/src/mem.rs#kernel_image_regions` | 同板块同名同结构 | (_sbss as usize).into, (_sdata as usize).into, (_srodata as usize).into, (_stext as usize).into, (boot_stack as usize).into, [
        MemRegion {
            paddr: virt_to_phys((_stext as usize).into()),
            size: _etext as usize - _stext as usize,
            flags: MemRegionFlags::RESERVED | MemRegionFlags::READ | MemRegionFlags::EXECUTE,
            name: ".text",
        },
        MemRegion {
            paddr: virt_to_phys((_srodata as usize).into()),
            size: _erodata as usize - _srodata as usize,
            flags: MemRegionFlags::RESERVED | MemRegionFlags::READ,
            name: ".rodata",
        },
        MemRegion {
            paddr: virt_to_phys((_sdata as usize).into()),
            size: _edata as usize - _sdata as usize,
            flags: MemRegionFlags::RESERVED | MemRegionFlags::READ | MemRegionFlags::WRITE,
            name: ".data .tdata .tbss .percpu",
        },
        MemRegion {
            paddr: virt_to_phys((boot_stack as usize).into()),
            size: boot_stack_top as usize - boot_stack as usize,
            flags: MemRegionFlags::RESERVED | MemRegionFlags::READ | MemRegionFlags::WRITE,
            name: "boot stack",
        },
        MemRegion {
            paddr: virt_to_phys((_sbss as usize).into()),
            size: _ebss as usize - _sbss as usize,
            flags: MemRegionFlags::RESERVED | MemRegionFlags::READ | MemRegionFlags::WRITE,
            name: ".bss",
        },
    ]
    .into_iter ...(+1) | (_sbss as usize).into, (_sdata as usize).into, (_srodata as usize).into, (_stext as usize).into, (boot_stack as usize).into, [
        MemRegion {
            paddr: virt_to_phys((_stext as usize).into()),
            size: _etext as usize - _stext as usize,
            flags: MemRegionFlags::RESERVED | MemRegionFlags::READ | MemRegionFlags::EXECUTE,
            name: ".text",
        },
        MemRegion {
            paddr: virt_to_phys((_srodata as usize).into()),
            size: _erodata as usize - _srodata as usize,
            flags: MemRegionFlags::RESERVED | MemRegionFlags::READ,
            name: ".rodata",
        },
        MemRegion {
            paddr: virt_to_phys((_sdata as usize).into()),
            size: _edata as usize - _sdata as usize,
            flags: MemRegionFlags::RESERVED | MemRegionFlags::READ | MemRegionFlags::WRITE,
            name: ".data .tdata .tbss .percpu",
        },
        MemRegion {
            paddr: virt_to_phys((boot_stack as usize).into()),
            size: boot_stack_top as usize - boot_stack as usize,
            flags: MemRegionFlags::RESERVED | MemRegionFlags::READ | MemRegionFlags::WRITE,
            name: "boot stack",
        },
        MemRegion {
            paddr: virt_to_phys((_sbss as usize).into()),
            size: _ebss as usize - _sbss as usize,
            flags: MemRegionFlags::RESERVED | MemRegionFlags::READ | MemRegionFlags::WRITE,
            name: ".bss",
        },
    ]
    .into_iter ...(+1) |
| `arceos/modules/axhal/src/platform/x86_pc/apic.rs#init_primary` | `arceos/modules/axhal/src/platform/x86_pc/apic.rs#init_primary` | 同板块同名同结构 | IO_APIC.init_once, IoApic::new, LOCAL_APIC.get, LOCAL_APIC.get().as_mut, LOCAL_APIC.get().as_mut().unwrap, LOCAL_APIC.get().as_mut().unwrap().write ...(+18) | IO_APIC.init_once, IoApic::new, LOCAL_APIC.get, LOCAL_APIC.get().as_mut, LOCAL_APIC.get().as_mut().unwrap, LOCAL_APIC.get().as_mut().unwrap().write ...(+18) |
| `apps/libc/c/signal/signal.c#test_sigsuspend` | `apps/libc/c/signal/signal.c#test_sigsuspend` | 同板块同名同结构 | exit, fork, kill, puts, sigaction, sigaddset ...(+4) | exit, fork, kill, puts, sigaction, sigaddset ...(+4) |
| `arceos/modules/axsync/src/mutex.rs#lock` | `arceos/modules/axsync/src/mutex.rs#lock` | 同板块同名同结构 | assert_ne!, current, current().id, current().id().as_u64, self.is_locked, self.owner_id.compare_exchange_weak ...(+1) | assert_ne!, current, current().id, current().id().as_u64, self.is_locked, self.owner_id.compare_exchange_weak ...(+1) |


好的，仲裁官。基于提供的证据底座、双方模块画像及控辩意见，现出具最终同源性定性仲裁报告。

---

### 1. 宏观判定
- **定性结论**: **独立开发，存在共享开源框架基底**
  双方项目在底层架构上存在显著差异，整体 AST 相似度仅为 34.98%。目标项目拥有 1534 个唯一 AST 指纹，远多于双方共同的 1074 个指纹，表明其主体实现为独立开发。观察到的相似性高度集中于 `arceos/modules/` 路径下的公共框架代码，属于共同上游依赖，不构成项目间的抄袭或系统性复用。

### 2. 全模块覆盖矩阵解读
- **进程管理**: 双方均存在该模块。存在共同 AST 指纹。目标覆盖率 64.86%，参考覆盖率 65.75%，覆盖率接近，说明双方在进程管理上共享了相似的底层模型，这主要源于对 ArceOS 框架 `axtask` 等核心组件的共同依赖。证据强度：**中**。
- **任务调度/异步运行时**: 双方均存在该模块。存在共同 AST 指纹。目标覆盖率 77.73%，参考覆盖率 72.36%，高覆盖率指向双方均大量使用了 ArceOS 的调度与运行时框架。证据强度：**中**。
- **内存管理**: 双方均存在该模块。存在共同 AST 指纹。目标覆盖率 65.37%，参考覆盖率 61.76%，表明双方在内存管理上共享了 ArceOS 的 `axmm` 等基础组件，但各自有独立扩展。证据强度：**中**。
- **中断/异常处理**: 双方均存在该模块。存在共同 AST 指纹。目标覆盖率高达 91.49%，参考覆盖率 84.31%，这是典型的硬件架构相关代码（如 `axhal`），高度一致源于对同一硬件平台和框架底层原语的必然实现。证据强度：**弱**。
- **系统调用**: 双方均存在该模块。存在共同 AST 指纹。目标覆盖率 58.33%，参考覆盖率 32.71%，覆盖率差异显著。目标项目实现了更完整的系统调用层，而参考项目有大量未匹配代码，说明双方在此模块的独立开发程度较高。证据强度：**弱**。
- **文件系统**: 双方均存在该模块。存在共同 AST 指纹。目标覆盖率 63.64%，参考覆盖率 53.96%，共享部分主要源于 ArceOS 的 `axfs` 框架，但双方在具体文件系统实现（如 `procfs`）上存在明显差异。证据强度：**中**。
- **设备驱动**: 双方均存在该模块。存在共同 AST 指纹。目标覆盖率 84.06%，参考覆盖率 40.85%，覆盖率差异巨大。目标项目复用了大量 VirtIO 等驱动框架代码，而参考项目拥有更多独立或不同的驱动实现。证据强度：**弱**。
- **网络协议栈**: 双方均存在该模块。**未观察到共同 AST 指纹**。这是双方架构差异最显著的模块之一，表明目标项目未从参考项目复用任何网络协议栈代码。证据强度：**未形成**。
- **第三方库/公共依赖**: 双方均存在该模块。在 `文件系统/第三方库` 和 `网络协议栈/第三方库` 中观察到共同指纹，这源于对 `lwext4_rust`、`smoltcp` 等开源库的共同依赖。在 `第三方库` 模块中未观察到共同指纹，说明双方引入的独立第三方库完全不同。证据强度：**弱**。

### 3. 底层 AST 关键证据指纹表
| 所属板块 | 目标项目路径 | 参考项目路径 | 证据类型 | 技术说明 |
| :--- | :--- | :--- | :--- | :--- |
| 进程管理 | `arceos/modules/axtask/src/wait_queue.rs` | `arceos/modules/axtask/src/wait_queue.rs` | 同源框架 | 多个函数（如 `cancel_events`, `wait_timeout_until`）的 AST 一致，源于双方共同依赖的 ArceOS 任务等待队列实现。 |
| 进程管理 | `arceos/modules/axtask/src/run_queue.rs#exit_current` | `arceos/modules/axtask/src/run_queue.rs#exit_current` | 同源框架 | 任务退出清理逻辑完全一致，调用 `EXITED_TASKS` 和 `WAIT_FOR_EXIT` 等 ArceOS 内部全局变量。 |
| 进程管理 | `api/src/imp/signal.rs#sys_rt_sigprocmask` | `api/src/imp/signal.rs#sys_rt_sigprocmask` | 同源框架 | 深层链式调用 `current().task_ext().thread_data().signal.with_blocked_mut` 一致，源于 ArceOS 的 Task 扩展和信号模块设计。 |
| 任务调度/异步运行时 | `api/src/imp/futex.rs#sys_futex` | `api/src/imp/futex.rs#sys_futex` | 同源框架 | `sys_futex` 的实现逻辑完全一致，均基于 `axtask::yield_now` 和 `curr.task_ext().process_data` 构建。 |
| 内存管理 | `arceos/modules/axruntime/src/lib.rs#rust_main` | `arceos/modules/axruntime/src/lib.rs#rust_main` | 同源框架 | 内核初始化序列完全一致，调用 `axdriver::init_drivers`、`axfs::init_filesystems` 等 ArceOS 标准初始化流程。 |
| 中断/异常处理 | `arceos/modules/axhal/src/arch/aarch64/trap.rs#handle_sync_exception` | `arceos/modules/axhal/src/arch/aarch64/trap.rs#handle_sync_exception` | 同源框架 | 异常处理逻辑完全一致，包括读取 `ESR_EL1` 并调用 `handle_syscall`，这是 ArceOS AArch64 架构的标准陷阱处理流程。 |
| 文件系统 | `arceos/modules/axfs/src/fops.rs#_open_at` | `arceos/modules/axfs/src/fops.rs#_open_at` | 同源框架 | 文件打开的核心逻辑一致，均使用 `WithCap::new` 和 `attr.perm` 进行权限检查，属于 ArceOS VFS 框架。 |
| 文件系统 | `arceos/modules/axfs/src/mounts.rs#procfs` | `arceos/modules/axfs/src/mounts.rs#procfs` | 同名异构 | 函数名相同，但内部实现完全不同。目标项目使用 `RamFileSystem`，参考项目使用 `ReadOnlyRamFileSystem` 并挂载了不同文件。 |
| 设备驱动 | `arceos/modules/axdriver/src/virtio.rs#probe_pci` | `arceos/modules/axdriver/src/virtio.rs#probe_pci` | 同源框架 | VirtIO PCI 设备探测逻辑一致，调用 `axdriver_virtio::probe_pci_device`，属于 ArceOS 驱动框架。 |
| 网络协议栈/第三方库 | `arceos/modules/axnet/src/smoltcp_impl/tcp.rs#connect` | `arceos/modules/axnet/src/smoltcp_impl/tcp.rs#connect` | 公共第三方库 | TCP 连接逻辑高度一致，均基于 `smoltcp` 库的 `SocketSet` 和 `tcp::Socket` API 构建。 |
| 文件系统/第三方库 | `arceos/modules/axfs/src/fs/lwext4_rust.rs#get_attr` | `arceos/modules/axfs/src/fs/lwext4_rust.rs#get_attr` | 公共第三方库 | 文件属性获取逻辑一致，均是对 `lwext4_rust` 库的 `file_attr_get` 等函数的封装调用。 |

### 4. 核心代码调用流对比剖析
* **【进程管理/任务退出】**
* **底层技术析理**: 双方 `do_exit` 函数的调用流均遵循 ArceOS 的退出模型：清理文件描述符表 -> 构造信号信息 -> 唤醒等待者 -> 调用 `axtask::exit` 进入调度器。目标项目额外调用了 `Arc::strong_count` 进行引用计数检查，而参考项目则通过 `curr.task_ext().process_data` 访问进程数据，显示出在框架之上的不同处理细节。
* **代码溯源链接**:
  - 目标项目: `api/src/imp/task/exit.rs#do_exit`
  - 参考项目: `api/src/imp/task/exit.rs#do_exit`

* **【内存管理/内核初始化】**
* **底层技术析理**: 双方 `rust_main` 函数的初始化序列完全一致，均为：初始化显示 -> 初始化驱动 -> 初始化文件系统 -> 初始化网络 -> 启动调度器。这是 ArceOS 内核启动的标准范式，其调用流由框架定义，不具备项目独创性。
* **代码溯源链接**:
  - 目标项目: `arceos/modules/axruntime/src/lib.rs#rust_main`
  - 参考项目: `arceos/modules/axruntime/src/lib.rs#rust_main`

* **【文件系统/procfs 挂载】**
* **底层技术析理**: 双方均有 `procfs` 函数用于挂载 proc 文件系统，但底层实现异构。目标项目基于可写的 `ramfs::RamFileSystem` 创建，并手动写入 `file_over` 等文件；参考项目则基于只读的 `ReadOnlyRamFileSystem` 创建，并挂载了 `ProcInterruptsFile` 等自定义节点。这表明双方在 procfs 的具体实现上采用了完全不同的设计策略。
* **代码溯源链接**:
  - 目标项目: `arceos/modules/axfs/src/mounts.rs#procfs`
  - 参考项目: `arceos/modules/axfs/src/mounts.rs#procfs`

### 5. 单方模块与降权项
- **单方模块/强异构模块**:
  - **网络协议栈**: 双方虽均有此模块，但共同 AST 指纹为 0，表明是完全独立的实现，是证明双方独立开发的关键证据。
  - **第三方库**: 双方引入的独立第三方库（非公共依赖）完全不同，共同指纹为 0。
- **降权项说明**:
  - **`arceos/modules/` 路径下的所有代码**: 这些属于 ArceOS 开源操作系统框架的公共组件。任何基于此框架的项目都必然包含并极少修改这些核心模块，其 AST 一致性不能作为项目间抄袭的证据。
  - **短函数/薄封装**: 如 `notify_all`、`set_in_wait_queue` 等单行函数，其 AST 形态有限，独立开发也必然相同，证据强度极弱。
  - **跨板块同名函数**: 如将 `path.rs#parent` 归入进程管理板块，此类归类错误产生的同名证据不构成有效指控。
  - **用户态测试程序**: `apps/` 路径下的 `forktree`、`signal.c` 等测试用例，其功能是验证标准 POSIX 接口，调用序列由接口规范决定，不具备原创性。

### 6. 保守结论
本次审计的目标项目与参考项目整体 AST 相似度为 34.98%，处于较低水平。所有强相似信号均指向双方共同依赖的 ArceOS 开源框架及 `smoltcp`、`lwext4` 等第三方公共库。目标项目拥有 1534 个唯一 AST 指纹，且在网络协议栈、设备驱动、文件系统细节（如 procfs 实现）等关键子系统上与参考项目存在显著架构差异或完全独立实现。综合判断，目标项目是在 ArceOS 框架基础上的独立开发作品，未发现对参考项目进行系统性代码复用或抄袭的证据。

------------------------------

