# 🛡️ OS 内核赛自动化代码级架构审计报告

**审计时间**: `2026-07-02 05:43:44`
**参赛作品名称**: `2026-T2026104869911108-XX`
**年份**: `2026`
**学校**: `XX`
**队伍名称**: `T2026104869911108`
**比赛名字**: `OS-Competition`
**比赛子赛道**: `Kernel-Implementation`
**仓库地址**: `https://gitlab.eduxiji.net/educg-group-43501-3132633/T2026104869911108-2514`
**推测开发语言**: `Rust / C (由AST探针自动识别)`
**特征函数量**: `1836`

## ⚔️ 底层代码同源性对比 (AST Homology)
> 本部分由专门的代码审查智能体，对该项目与符合年份条件的历届最高相似度项目进行控制流与底层调用对比。新版报告会先给出确定性的全模块覆盖矩阵，再给出 Agent 的分模块解释，避免只围绕少量抽样函数展开。

### 🏆 Top 3 底层对标: vs `2025-狗剩-海南大学`（海南大学 / 狗剩）

#### 对方作品信息

**对方年份**: `2025`
**对方学校**: `海南大学`
**对方队伍名称**: `狗剩`
**对方比赛名字**: `OS-Competition`
**对方比赛子赛道**: `Kernel-Implementation`
**对方仓库地址**: `https://gitlab.eduxiji.net/educg-group-36002-2710490/T202510589995148-2209`

> 双向覆盖率：共同 AST 指纹: 995；唯一 AST 指纹: 目标项目 1534，参考项目 3020；覆盖率: 目标项目 64.86%，参考项目 32.95%

## 全模块对比总览（确定性证据）

> 总体覆盖率：共同 AST 指纹: 995；唯一 AST 指纹: 目标项目 1534，参考项目 3020；覆盖率: 目标项目 64.86%，参考项目 32.95%

### 1. 模块覆盖矩阵
| 模块 | 目标函数数 | 参考函数数 | 共同唯一 AST 指纹 | Target 覆盖 | Base 覆盖 | 状态 |
| :--- | ---: | ---: | ---: | ---: | ---: | :--- |
| 进程管理 | 75 | 93 | 33 | 44.59% | 39.29% | 双方存在且有结构重合 |
| 任务调度/异步运行时 | 356 | 399 | 170 | 66.41% | 59.03% | 双方存在且有结构重合 |
| 内存管理 | 270 | 326 | 191 | 74.32% | 65.64% | 双方存在且有结构重合 |
| 中断/异常处理 | 53 | 62 | 47 | 100.0% | 95.92% | 双方存在且有结构重合 |
| 系统调用 | 79 | 153 | 29 | 48.33% | 22.48% | 双方存在且有结构重合 |
| 文件系统 | 458 | 747 | 183 | 47.53% | 32.33% | 双方存在且有结构重合 |
| 设备驱动 | 76 | 142 | 61 | 88.41% | 49.59% | 双方存在且有结构重合 |
| 网络协议栈 | 10 | 77 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |
| 文件系统/第三方库 | 22 | 689 | 11 | 50.0% | 1.87% | 双方存在且有结构重合 |
| 网络协议栈/第三方库 | 99 | 107 | 85 | 93.41% | 85.0% | 双方存在且有结构重合 |
| 第三方库 | 74 | 386 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |
| 通用模块/其他 | 264 | 665 | 182 | 76.79% | 37.07% | 双方存在且有结构重合 |

### 2. 分模块证据清单

#### 进程管理
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `api/src/imp/task/exit.rs#do_exit` | `api/src/imp/task.rs#do_exit` | 同板块同名 | Arc::strong_count, FD_TABLE.clear, SignalInfo::new, UserPtr::<Pid>::from, axtask::exit, axtask::yield_now ...(+31) | Layout::new::<Tid>, SignalInfo::new, UserPtr::<Tid>::from, axtask::exit, axtask::yield_now, clear_child_tid.get_as ...(+18) |
| `arceos/modules/axtask/src/run_queue.rs#exit_current` | `arceos/modules/axtask/src/run_queue.rs#exit_current` | 同板块同名同结构 | EXITED_TASKS.current_ref_mut_raw, EXITED_TASKS.current_ref_mut_raw().clear, EXITED_TASKS.current_ref_mut_raw().push_back, WAIT_FOR_EXIT.current_ref_mut_raw, WAIT_FOR_EXIT.current_ref_mut_raw().notify_one, assert! ...(+8) | EXITED_TASKS.current_ref_mut_raw, EXITED_TASKS.current_ref_mut_raw().clear, EXITED_TASKS.current_ref_mut_raw().push_back, WAIT_FOR_EXIT.current_ref_mut_raw, WAIT_FOR_EXIT.current_ref_mut_raw().notify_one, assert! ...(+8) |
| `arceos/modules/axtask/src/wait_queue.rs#cancel_events` | `arceos/modules/axtask/src/wait_queue.rs#cancel_events` | 同板块同名同结构 | curr.in_wait_queue, curr.ptr_eq, curr.set_in_wait_queue, curr.timer_ticket_expired, self.queue.lock, self.queue.lock().retain | curr.in_wait_queue, curr.ptr_eq, curr.set_in_wait_queue, curr.timer_ticket_expired, self.queue.lock, self.queue.lock().retain |
| `arceos/modules/axfs/src/mounts.rs#procfs` | `arceos/modules/axfs/src/mounts.rs#procfs` | 同板块同名 | Arc::new, Ok, file_over.write_at, file_somaxconn.write_at, fs::ramfs::RamFileSystem::new, proc_root.clone ...(+3) | Arc::new, Ok, file_over.write_at, file_somaxconn.write_at, fs::ramfs::RamFileSystem::new, proc_root.clone ...(+3) |

#### 任务调度/异步运行时
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `arceos/modules/axlog/src/lib.rs#log` | `arceos/modules/axlog/src/lib.rs#log` | 同板块同名同结构 | cfg_if::cfg_if!, record.level, record.line, record.line().unwrap_or, record.metadata, record.target ...(+1) | cfg_if::cfg_if!, record.level, record.line, record.line().unwrap_or, record.metadata, record.target ...(+1) |
| `api/src/imp/futex.rs#sys_futex` | `api/src/imp/futex.rs#sys_futex` | 同板块同名 | Err, Ok, axtask::yield_now, curr.task_ext, curr.task_ext().process_data, current ...(+16) | Err, Ok, TimeValue::new, axtask::yield_now, futex_table.get, futex_table.get_or_insert ...(+14) |
| `arceos/modules/axtask/src/run_queue.rs#switch_to` | `arceos/modules/axtask/src/run_queue.rs#switch_to` | 同板块同名 | (*prev_ctx_ptr).switch_to, Arc::downgrade, CurrentTask::set_current, PREV_TASK.current_ref_mut_raw, assert!, clear_prev_task_on_cpu ...(+8) | (*prev_ctx_ptr).switch_to, Arc::downgrade, CurrentTask::set_current, PREV_TASK.current_ref_mut_raw, assert!, clear_prev_task_on_cpu ...(+11) |
| `arceos/modules/axhal/src/arch/aarch64/context.rs#fpstate_switch` | `arceos/modules/axhal/src/arch/aarch64/context.rs#fpstate_switch` | 同板块同名同结构 | naked_asm! | naked_asm! |

#### 内存管理
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `api/src/imp/task/clone.rs#sys_clone` | `src/syscall_imp/task/thread.rs#sys_clone` | 同板块同名 | Arc::default, Arc::new, CURRENT_DIR
                .deref_from, CURRENT_DIR
                .deref_from(&process_data.ns)
                .init_new, CURRENT_DIR
                .deref_from(&process_data.ns)
                .init_shared, CURRENT_DIR.copy_inner ...(+73) | Arc::default, Arc::downgrade, Arc::new, CloneFlags::from_bits, CloneFlags::from_bits((flags & !FLAG_MASK) as u32).unwrap, Err ...(+57) |
| `arceos/modules/axmm/src/aspace.rs#try_clone` | `arceos/modules/axmm/src/aspace.rs#try_clone` | 同板块同名 | Backend::new_alloc, Err, MemoryArea::new, Ok, PageIterWrapper::new, PageIterWrapper::new(area.start(), area.end(), align)
                .expect ...(+29) | Backend::new_alloc, Err, MemoryArea::new, Ok, PageIterWrapper::new, PageIterWrapper::new(area.start(), area.end(), align)
                .expect ...(+29) |
| `arceos/modules/axmm/src/aspace.rs#populate_area` | `arceos/modules/axmm/src/aspace.rs#populate_area` | 同板块同名 | Err, Ok, PageIterWrapper::new, PageIterWrapper::new(
                    start.align_down(align),
                    end.align_up(align).min(area.end()),
                    align,
                )
                .unwrap, Self::handle_cow_fault, _access_flags.contains ...(+14) | Err, Ok, PageIterWrapper::new, PageIterWrapper::new(
                    start.align_down(align),
                    end.align_up(align).min(area.end()),
                    align,
                )
                .unwrap, Self::handle_cow_fault, _access_flags.contains ...(+14) |
| `api/src/imp/mm/mmap.rs#sys_mmap` | `src/syscall_imp/mm/mmap.rs#sys_mmap` | 同板块同名 | (addr + length).align_up, Err, MmapFlags::from_bits_truncate, MmapProt::from_bits_truncate, Ok, VirtAddr::from ...(+26) | (addr + length).align_up, Err, MmapFlags::MAP_PRIVATE.bits, MmapFlags::MAP_SHARED.bits, MmapFlags::MAP_SHARED_VALIDATE.bits, MmapFlags::from_bits_truncate ...(+32) |

#### 中断/异常处理
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `arceos/modules/axhal/src/arch/aarch64/trap.rs#handle_sync_exception` | `arceos/modules/axhal/src/arch/aarch64/trap.rs#handle_sync_exception` | 同板块同名同结构 | ESR_EL1.extract, crate::trap::handle_syscall, crate::trap::post_trap_callback, debug!, esr.read, esr.read_as_enum ...(+6) | ESR_EL1.extract, crate::trap::handle_syscall, crate::trap::post_trap_callback, debug!, esr.read, esr.read_as_enum ...(+6) |
| `arceos/modules/axhal/src/platform/riscv64_qemu_virt/irq.rs#register_handler` | `arceos/modules/axhal/src/platform/riscv64_qemu_virt/irq.rs#register_handler` | 同板块同名同结构 | with_cause! | with_cause! |
| `arceos/modules/axhal/src/platform/loongarch64_qemu_virt/irq.rs#set_enable` | `arceos/modules/axhal/src/platform/loongarch64_2k1000la/irq.rs#set_enable` | 同板块同名同结构 | ecfg::read, ecfg::read().lie, ecfg::set_lie | ecfg::read, ecfg::read().lie, ecfg::set_lie |
| `arceos/modules/axhal/src/irq.rs#register_handler_common` | `arceos/modules/axhal/src/irq.rs#register_handler_common` | 同板块同名同结构 | IRQ_HANDLER_TABLE.register_handler, set_enable, warn! | IRQ_HANDLER_TABLE.register_handler, set_enable, warn! |

#### 系统调用
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `api/src/imp/net.rs#sys_socket` | `arceos/api/arceos_posix_api/src/imp/net.rs#sys_socket` | 同板块同名 | Err, Mutex::new, Ok, Socket::Tcp, Socket::Udp, TcpSocket::new ...(+4) | debug!, syscall_body! |
| `apps/nimbos/rust/src/arch/riscv.rs#sys_clone` | `apps/nimbos/rust/src/arch/riscv.rs#sys_clone` | 同板块同名同结构 | naked_asm! | naked_asm! |
| `apps/nimbos/rust/src/arch/x86_64.rs#sys_clone` | `apps/nimbos/rust/src/arch/x86_64.rs#sys_clone` | 同板块同名同结构 | naked_asm! | naked_asm! |
| `apps/nimbos/rust/src/arch/aarch64.rs#sys_clone` | `apps/nimbos/rust/src/arch/aarch64.rs#sys_clone` | 同板块同名同结构 | naked_asm! | naked_asm! |

#### 文件系统
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `arceos/modules/axfs/src/fops.rs#_open_at` | `arceos/modules/axfs/src/fops.rs#_open_at` | 同板块同名 | Err, Ok, WithCap::new, attr.is_dir, attr.perm, ax_err! ...(+10) | Err, Ok, WithCap::new, attr.is_dir, attr.perm, ax_err! ...(+10) |
| `api/src/imp/fs/fd_ops.rs#sys_openat` | `arceos/api/arceos_posix_api/src/imp/fs.rs#sys_openat` | 同板块同名 | Directory::from_fd, Directory::new, Directory::new(
        dir.map_or_else(
            || axfs::fops::Directory::open_dir(path, &opts),
            |dir| dir.inner().open_dir_at(path, &opts),
        )?,
        real_path.to_string(),
    )
    .add_to_fd_table, File::new, File::new(r?, real_path.to_string()).add_to_fd_table, Ok ...(+21) | Directory::from_fd, Directory::from_fd(dirfd).and_then, add_file_or_directory_fd, char_ptr_to_str, debug!, dir.inner.lock ...(+6) |
| `arceos/modules/axfs/src/dev.rs#write_one` | `arceos/modules/axfs/src/dev.rs#write_one` | 同板块同名同结构 | Ok, buf.len, buf.len().min, buf[0..BLOCK_SIZE].to_vec, data[start..start + count].copy_from_slice, self.dev.read_block ...(+1) | Ok, buf.len, buf.len().min, buf[0..BLOCK_SIZE].to_vec, data[start..start + count].copy_from_slice, self.dev.read_block ...(+1) |
| `arceos/modules/axfs/src/fops.rs#_open_dir_at` | `arceos/modules/axfs/src/fops.rs#_open_dir_at` | 同板块同名 | Err, Ok, WithCap::new, attr.is_dir, attr.perm, ax_err! ...(+7) | Ok, WithCap::new, attr.is_dir, attr.perm, ax_err!, cap.contains ...(+6) |

#### 设备驱动
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `arceos/modules/axdriver/src/lib.rs#init_drivers` | `arceos/modules/axdriver/src/lib.rs#init_drivers` | 同板块同名同结构 | AllDevices::default, all_devs.block.iter, all_devs.block.iter().enumerate, all_devs.display.iter, all_devs.display.iter().enumerate, all_devs.net.iter ...(+5) | AllDevices::default, all_devs.block.iter, all_devs.block.iter().enumerate, all_devs.display.iter, all_devs.display.iter().enumerate, all_devs.net.iter ...(+5) |
| `arceos/modules/axdriver/src/virtio.rs#probe_pci` | `arceos/modules/axdriver/src/virtio.rs#probe_pci` | 同板块同名同结构 | D::try_new, Some, axdriver_virtio::probe_pci_device::<VirtIoHalImpl>, warn! | D::try_new, Some, axdriver_virtio::probe_pci_device::<VirtIoHalImpl>, warn! |
| `arceos/modules/axdriver/src/virtio.rs#probe_mmio` | `arceos/modules/axdriver/src/virtio.rs#probe_mmio` | 同板块同名同结构 | D::try_new, Some, axdriver_virtio::probe_mmio_device, base_vaddr.as_mut_ptr, mmio_base.into, phys_to_virt ...(+1) | D::try_new, Some, axdriver_virtio::probe_mmio_device, base_vaddr.as_mut_ptr, mmio_base.into, phys_to_virt ...(+1) |
| `arceos/modules/axdriver/src/bus/mmio.rs#probe_bus_devices` | `arceos/modules/axdriver/src/bus/mmio.rs#probe_bus_devices` | 同板块同名同结构 | for_each_drivers! | for_each_drivers! |

#### 网络协议栈
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `api/src/socket.rs#write_to_user` | `arceos/ulib/axlibc/c/network.c#inet_pton` | 同模块上下文对齐/差异观察 | Err, Ok, addr.get_as_mut, addr.is_null, core::ptr::copy_nonoverlapping, self.flowinfo ...(+7) | hexval, inet_pton, isdigit, memmove |
| `api/src/socket.rs#write_to_user` | `api/src/fd/net.rs#sendto` | 同模块上下文对齐/差异观察 | Err, Ok, addr.get_as_mut, addr.is_null, core::ptr::copy_nonoverlapping, self.ip ...(+5) | Err, IpAddr::V4, Ipv4Addr::new, Ok, SocketAddr::new, socket
                    .bind ...(+3) |
| `api/src/imp/net.rs#gather_iovs` | `api/src/fd/net.rs#shutdown` | 同模块上下文对齐/差异观察 | (0..=1024).contains, Err, Ok, UserConstPtr::<iovec>::from, UserConstPtr::<iovec>::from(iov).get_as_slice, UserConstPtr::<u8>::from ...(+6) | Ok, tcpsocket.lock, tcpsocket.peer_addr, tcpsocket.shutdown, udpsocket.lock, udpsocket.peer_addr ...(+1) |
| `api/src/imp/net.rs#write_int_opt` | `arceos/api/arceos_posix_api/src/imp/net.rs#shutdown` | 同模块上下文对齐/差异观察 | Err, Ok, UserPtr::<c_int>::from, UserPtr::<c_int>::from(optval.address().as_usize()).get_as_mut, optlen.get_as_mut, optlen.is_null ...(+4) | Ok, tcpsocket.lock, tcpsocket.peer_addr, tcpsocket.shutdown, udpsocket.lock, udpsocket.peer_addr ...(+1) |

#### 文件系统/第三方库
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `arceos/modules/axfs/src/fs/lwext4_rust.rs#get_attr` | `arceos/modules/axfs/src/fs/lwext4_rust.rs#get_attr` | 同板块同名 | <i32 as TryInto<AxError>>::try_into, <i32 as TryInto<AxError>>::try_into(e).unwrap, EXT4_LOCK.lock, Ok, VfsNodeAttr::new, VfsNodePerm::from_bits_truncate ...(+13) | <i32 as TryInto<AxError>>::try_into, <i32 as TryInto<AxError>>::try_into(e).unwrap, Ok, VfsNodeAttr::new, VfsNodePerm::from_bits_truncate, file.file_close ...(+12) |
| `arceos/modules/axfs/src/fs/lwext4_rust.rs#create` | `arceos/modules/axfs-ng/src/fs/ext4/inode.rs#create` | 同板块同名 | EXT4_LOCK.lock, Ok, e.try_into, e.try_into().unwrap, file.check_inode_exist, file.dir_mk ...(+13) | DirEntry::new_dir, DirEntry::new_file, DirNode::new, Err, FileNode::new, Inode::new ...(+11) |
| `arceos/modules/axfs/src/fs/lwext4_rust.rs#read_dir` | `arceos/modules/axfs/src/fs/lwext4_rust.rs#read_dir` | 同板块同名 | EXT4_LOCK.lock, Ok, VfsDirEntry::new, core::str::from_utf8, core::str::from_utf8(iname.unwrap()).unwrap, dirents.iter_mut ...(+14) | Ok, VfsDirEntry::new, core::str::from_utf8, core::str::from_utf8(iname.unwrap()).unwrap, dirents.iter_mut, dirents.iter_mut().enumerate ...(+13) |
| `arceos/modules/axfs/src/fs/lwext4_rust.rs#lookup` | `arceos/modules/axfs/src/fs/lwext4_rust.rs#lookup` | 同板块同名 | Arc::new, EXT4_LOCK.lock, Err, Ok, Self::new, file.check_inode_exist ...(+6) | Arc::new, Err, Ok, Self::new, file.check_inode_exist, fpath.as_str ...(+5) |

#### 网络协议栈/第三方库
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `arceos/modules/axnet/src/smoltcp_impl/tcp.rs#connect` | `arceos/modules/axnet/src/smoltcp_impl/tcp.rs#connect` | 同板块同名 | Err, Ok, SOCKET_SET
                .with_socket_mut::<tcp::Socket, _, _>, SOCKET_SET.add, SocketSetWrapper::new_tcp_socket, Some ...(+25) | Err, Ok, SOCKET_SET
                .with_socket_mut::<tcp::Socket, _, _>, SOCKET_SET.add, SocketSetWrapper::new_tcp_socket, Some ...(+29) |
| `arceos/modules/axnet/src/smoltcp_impl/dns.rs#query` | `arceos/modules/axnet/src/smoltcp_impl/dns.rs#query` | 同板块同名同结构 | Err, Ok, SOCKET_SET
            .with_socket_mut::<dns::Socket, _, _>, SOCKET_SET
            .with_socket_mut::<dns::Socket, _, _>(handle, |socket| {
                socket.start_query(iface.lock().context(), name, query_type)
            })
            .map_err, SOCKET_SET.poll_interfaces, SOCKET_SET.with_socket_mut::<dns::Socket, _, _> ...(+12) | Err, Ok, SOCKET_SET
            .with_socket_mut::<dns::Socket, _, _>, SOCKET_SET
            .with_socket_mut::<dns::Socket, _, _>(handle, |socket| {
                socket.start_query(iface.lock().context(), name, query_type)
            })
            .map_err, SOCKET_SET.poll_interfaces, SOCKET_SET.with_socket_mut::<dns::Socket, _, _> ...(+12) |
| `arceos/modules/axnet/src/smoltcp_impl/bench.rs#bench_transmit_bandwidth` | `arceos/modules/axnet/src/smoltcp_impl/bench.rs#bench_transmit_bandwidth` | 同板块同名同结构 | (current_time - past_time).secs, AxNetTxToken::consume, InterfaceWrapper::current_time, info!, self.transmit, tx_buf[0..12].fill ...(+2) | (current_time - past_time).secs, AxNetTxToken::consume, InterfaceWrapper::current_time, info!, self.transmit, tx_buf[0..12].fill ...(+2) |
| `arceos/modules/axnet/src/smoltcp_impl/tcp.rs#shutdown` | `arceos/modules/axnet/src/smoltcp_impl/tcp.rs#shutdown` | 同板块同名同结构 | LISTEN_TABLE.unlisten, Ok, SOCKET_SET.poll_interfaces, SOCKET_SET.with_socket_mut::<tcp::Socket, _, _>, debug!, self.handle.get ...(+9) | LISTEN_TABLE.unlisten, Ok, SOCKET_SET.poll_interfaces, SOCKET_SET.with_socket_mut::<tcp::Socket, _, _>, debug!, self.handle.get ...(+9) |

#### 第三方库
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `crates/axprocess/src/lib.rs#parent` | `crates/axfs-ng-vfs/src/path.rs#parent` | 同板块同名 | self.parent.lock, self.parent.lock().as_ref, self.parent.lock().as_ref().and_then | Some, comp.and_then, comps.as_path, comps.next_back, self.components |
| `crates/axsignal/src/lib.rs#bits` | `crates/page_table_multiarch/page_table_entry/src/arch/aarch64.rs#bits` | 同板块同名 | 内部闭环逻辑 | 内部闭环逻辑 |
| `crates/axprocess/src/lib.rs#build` | `crates/visionfive2-sd/src/lib.rs#send_cmd` | 同模块上下文对齐/差异观察 | Arc::new, AtomicBool::new, AtomicI32::new, INIT_PROC.call_once, Mutex::new, Once::new ...(+8) | RawInterrupt::from, RawInterruptStatusReg::from, S::sleep_ms_until, Some, arg.into, assert! ...(+24) |
| `crates/axsignal/src/lib.rs#from_repr` | `crates/visionfive2-sd/src/lib.rs#init_sdcard` | 同模块上下文对齐/差异观察 | Some | CardTypeReg::from, CardTypeReg::from(0).with_card_width4_1, CmdArg::new, CmdReg::from, ControlReg::from, RawInterruptStatusReg::from ...(+20) |

#### 通用模块/其他
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `arceos/modules/axhal/src/mem.rs#kernel_image_regions` | `arceos/modules/axhal/src/mem.rs#kernel_image_regions` | 同板块同名同结构 | (_sbss as usize).into, (_sdata as usize).into, (_srodata as usize).into, (_stext as usize).into, (boot_stack as usize).into, [
        MemRegion {
            paddr: virt_to_phys((_stext as usize).into()),
            size: _etext as usize - _stext as usize,
            flags: MemRegionFlags::RESERVED | MemRegionFlags::READ | MemRegionFlags::EXECUTE,
            name: ".text",
        },
        MemRegion {
            paddr: virt_to_phys((_srodata as usize).into()),
            size: _erodata as usize - _srodata as usize,
            flags: MemRegionFlags::RESERVED | MemRegionFlags::READ,
            name: ".rodata",
        },
        MemRegion {
            paddr: virt_to_phys((_sdata as usize).into()),
            size: _edata as usize - _sdata as usize,
            flags: MemRegionFlags::RESERVED | MemRegionFlags::READ | MemRegionFlags::WRITE,
            name: ".data .tdata .tbss .percpu",
        },
        MemRegion {
            paddr: virt_to_phys((boot_stack as usize).into()),
            size: boot_stack_top as usize - boot_stack as usize,
            flags: MemRegionFlags::RESERVED | MemRegionFlags::READ | MemRegionFlags::WRITE,
            name: "boot stack",
        },
        MemRegion {
            paddr: virt_to_phys((_sbss as usize).into()),
            size: _ebss as usize - _sbss as usize,
            flags: MemRegionFlags::RESERVED | MemRegionFlags::READ | MemRegionFlags::WRITE,
            name: ".bss",
        },
    ]
    .into_iter ...(+1) | (_sbss as usize).into, (_sdata as usize).into, (_srodata as usize).into, (_stext as usize).into, (boot_stack as usize).into, [
        MemRegion {
            paddr: virt_to_phys((_stext as usize).into()),
            size: _etext as usize - _stext as usize,
            flags: MemRegionFlags::RESERVED | MemRegionFlags::READ | MemRegionFlags::EXECUTE,
            name: ".text",
        },
        MemRegion {
            paddr: virt_to_phys((_srodata as usize).into()),
            size: _erodata as usize - _srodata as usize,
            flags: MemRegionFlags::RESERVED | MemRegionFlags::READ,
            name: ".rodata",
        },
        MemRegion {
            paddr: virt_to_phys((_sdata as usize).into()),
            size: _edata as usize - _sdata as usize,
            flags: MemRegionFlags::RESERVED | MemRegionFlags::READ | MemRegionFlags::WRITE,
            name: ".data .tdata .tbss .percpu",
        },
        MemRegion {
            paddr: virt_to_phys((boot_stack as usize).into()),
            size: boot_stack_top as usize - boot_stack as usize,
            flags: MemRegionFlags::RESERVED | MemRegionFlags::READ | MemRegionFlags::WRITE,
            name: "boot stack",
        },
        MemRegion {
            paddr: virt_to_phys((_sbss as usize).into()),
            size: _ebss as usize - _sbss as usize,
            flags: MemRegionFlags::RESERVED | MemRegionFlags::READ | MemRegionFlags::WRITE,
            name: ".bss",
        },
    ]
    .into_iter ...(+1) |
| `arceos/modules/axhal/src/platform/x86_pc/apic.rs#init_primary` | `arceos/modules/axhal/src/platform/x86_pc/apic.rs#init_primary` | 同板块同名同结构 | IO_APIC.init_once, IoApic::new, LOCAL_APIC.get, LOCAL_APIC.get().as_mut, LOCAL_APIC.get().as_mut().unwrap, LOCAL_APIC.get().as_mut().unwrap().write ...(+18) | IO_APIC.init_once, IoApic::new, LOCAL_APIC.get, LOCAL_APIC.get().as_mut, LOCAL_APIC.get().as_mut().unwrap, LOCAL_APIC.get().as_mut().unwrap().write ...(+18) |
| `arceos/modules/axsync/src/mutex.rs#lock` | `arceos/modules/axsync/src/mutex.rs#lock` | 同板块同名 | assert_ne!, current, current().id, current().id().as_u64, self.is_locked, self.owner_id.compare_exchange_weak ...(+1) | current, current().id, current().id().as_u64, self.is_locked, self.owner_id.compare_exchange_weak, self.wq.wait_until |
| `arceos/modules/axhal/src/platform/riscv64_qemu_virt/console.rs#write_bytes` | `arceos/modules/axhal/src/platform/riscv64_qemu_virt/console.rs#write_bytes` | 同板块同名同结构 | bytes.as_ptr, bytes.len, bytes.to_vec, kernel_bytes.len, try_write_bytes | bytes.as_ptr, bytes.len, bytes.to_vec, kernel_bytes.len, try_write_bytes |


好的，审判长。本庭已收到控辩双方的全部材料及原始 AST 证据，现依据审计指令，对本案进行最终定性仲裁。

### 1. 宏观判定
- **定性结论**: **独立开发 (存在共享上游框架)**
- **核心依据**: 总体 AST 相似度仅为 27.96%，远低于构成系统性抄袭的阈值。双向覆盖率指标（目标项目 64.86%，参考项目 32.95%）呈现显著不对称性，且目标项目拥有 1534 个唯一 AST 指纹，表明其存在大量独立设计与实现。双方代码的相似性高度集中在 `arceos/modules` 等明确可追溯的开源框架路径中，属于典型的“共同基于上游 ArceOS/NimbOS 项目进行开发”的特征，而非一方对另一方的抄袭。

### 2. 全模块覆盖矩阵解读
| 模块 | 双方存在 | 共同指纹 | 目标覆盖 | 参考覆盖 | 证据强度 | 解读 |
| :--- | :---: | :---: | ---: | ---: | :---: | :--- |
| **进程管理** | 是 | 有 | 44.59% | 39.29% | **弱** | 双方覆盖率均未过半，且共同指纹集中在 `axtask` 等上游框架的调度与等待队列原语中，属于框架强制 API 调用。 |
| **任务调度/异步运行时** | 是 | 有 | 66.41% | 59.03% | **中** | 覆盖率较高，但证据清单显示共同指纹多为 `axlog`、`axtask` 内部实现，是 ArceOS 核心运行时，双方均未做深度修改。 |
| **内存管理** | 是 | 有 | 74.32% | 65.64% | **中** | 高覆盖率源于 `axmm` 等上游内存管理框架。然而，在 `sys_clone`、`sys_mmap` 等关键系统调用上，双方调用图谱存在显著差异，体现了独立设计。 |
| **中断/异常处理** | 是 | 有 | 100.0% | 95.92% | **强 (上游)** | 覆盖率极高，但证据指纹（如 `handle_sync_exception`）完全位于 `axhal` 架构相关代码中，是硬件规定的处理流程，原创空间极小。 |
| **系统调用** | 是 | 有 | 48.33% | 22.48% | **弱** | 目标覆盖率不足一半，参考覆盖率极低。这表明被告实现了大量原告未涉及的系统调用，双方在 POSIX 兼容层的实现范围和方式上差异巨大。 |
| **文件系统** | 是 | 有 | 47.53% | 32.33% | **弱** | 共同指纹主要在 `axfs` 的 VFS 接口和 `lwext4_rust` 第三方库封装上。`sys_openat` 等关键路径的调用流完全不同，展现了不同的资源管理模型。 |
| **设备驱动** | 是 | 有 | 88.41% | 49.59% | **强 (上游)** | 高覆盖率完全集中在 `axdriver` 框架的 VirtIO、PCI 等总线枚举与驱动初始化逻辑上，是 ArceOS 官方驱动模型的直接复用。 |
| **网络协议栈** | 是 | 无 | 0.0% | 0.0% | **未形成** | 双方虽均有该模块，但未观察到任何共同 AST 指纹，是彻底的独立实现或使用了完全不同的协议栈。 |
| **第三方库/公共依赖** | 是 | 无/有 | 0.0%~93.41% | 0.0%~85.0% | **弱/未形成** | 相似度呈现两极分化。`网络协议栈/第三方库` 高相似度源于共同依赖的 `smoltcp` 库；`第三方库` 零相似度则说明双方独立选择了不同的第三方库。 |

### 3. 底层 AST 关键证据指纹表
| 所属板块 | 目标项目路径 | 参考项目路径 | 证据类型 | 技术说明 |
| :--- | :--- | :--- | :--- | :--- |
| 中断/异常处理 | `arceos/modules/axhal/src/arch/aarch64/trap.rs#handle_sync_exception` | `arceos/modules/axhal/src/arch/aarch64/trap.rs#handle_sync_exception` | **强-上游框架** | 100% 同构。AArch64 同步异常入口，读取 `ESR_EL1` 并分发到系统调用或错误处理。硬件架构决定的固定流程。 |
| 设备驱动 | `arceos/modules/axdriver/src/lib.rs#init_drivers` | `arceos/modules/axdriver/src/lib.rs#init_drivers` | **强-上游框架** | 100% 同构。遍历所有 `AllDevices` 并初始化块设备、显示设备、网络设备。ArceOS 驱动模型的标准初始化范式。 |
| 任务调度/异步运行时 | `arceos/modules/axtask/src/run_queue.rs#exit_current` | `arceos/modules/axtask/src/run_queue.rs#exit_current` | **强-上游框架** | 100% 同构。将当前任务推入 `EXITED_TASKS` 并通知 `WAIT_FOR_EXIT`。ArceOS `axtask` 模块的任务退出生命周期管理。 |
| 进程管理 | `arceos/modules/axtask/src/wait_queue.rs#cancel_events` | `arceos/modules/axtask/src/wait_queue.rs#cancel_events` | **强-上游框架** | 100% 同构。检查并清除等待队列中任务的事件状态。`SpinNoIrq` + `VecDeque` 的典型 Rust 无锁结构操作。 |
| 内存管理 | `arceos/modules/axmm/src/aspace.rs#try_clone` | `arceos/modules/axmm/src/aspace.rs#try_clone` | **强-上游框架** | 100% 同构。地址空间的克隆逻辑，遍历 `MemoryArea` 并处理 `Backend`。ArceOS `axmm` 的核心地址空间管理。 |
| 文件系统 | `arceos/modules/axfs/src/fops.rs#_open_at` | `arceos/modules/axfs/src/fops.rs#_open_at` | **强-上游框架** | 100% 同构。文件打开的核心权限检查与能力创建逻辑。ArceOS VFS 的标准实现。 |
| 网络协议栈/第三方库 | `arceos/modules/axnet/src/smoltcp_impl/tcp.rs#connect` | `arceos/modules/axnet/src/smoltcp_impl/tcp.rs#connect` | **强-公共依赖** | 高度同构。对 `smoltcp` 库 `tcp::Socket` 的 `connect` 操作的封装，调用 `SOCKET_SET.add` 和 `with_socket_mut`。 |
| 进程管理 | `api/src/imp/task/exit.rs#do_exit` | `api/src/imp/task.rs#do_exit` | **弱-独立设计** | 核心退出逻辑。目标使用 `UserPtr::<Pid>`、`FD_TABLE.clear`；参考使用 `UserPtr::<Tid>`、`close_all_filelike`。进程模型与资源清理策略根本不同。 |
| 系统调用 | `api/src/imp/fs/fd_ops.rs#sys_openat` | `arceos/api/arceos_posix_api/src/imp/fs.rs#sys_openat` | **弱-独立设计** | 文件打开系统调用。目标通过 `Directory::new` 构建路径并显式管理 `real_path`；参考通过 `add_file_or_directory_fd` 闭包处理。资源管理模型异构。 |

### 4. 核心代码调用流对比剖析
* **【进程管理/任务退出】**
* **底层技术析理**: 双方均实现了 `do_exit` 函数，但展现了截然不同的进程模型。目标项目以 `Pid`（进程ID）为核心，使用 `Arc::strong_count` 管理进程资源引用，并通过全局 `FD_TABLE.clear` 清理文件描述符。参考项目则以 `Tid`（线程ID）为核心，使用 `Layout::new::<Tid>` 进行内存布局，并通过 `close_all_filelike` 逐个关闭文件。这不仅是命名差异，更是“进程级退出”与“线程级退出”两种不同抽象模型的对立。
* **代码溯源链接**:
  - 目标项目: `api/src/imp/task/exit.rs#do_exit`
  - 参考项目: `api/src/imp/task.rs#do_exit`

* **【内存管理/地址空间克隆】**
* **底层技术析理**: 在 `sys_clone` 的核心路径中，双方都调用了 `axmm/src/aspace.rs#try_clone`，这是 ArceOS 框架的标准操作，AST 完全一致。然而，在上层系统调用 `sys_clone` 中，目标项目通过 `CURRENT_DIR.deref_from(...).init_new` 等复杂逻辑处理命名空间和当前目录的继承；参考项目则侧重于 `CloneFlags::from_bits` 的位掩码解析。这表明双方在遵循相同上游内存管理原语的同时，对 POSIX 兼容层的实现策略是独立设计的。
* **代码溯源链接**:
  - 目标项目: `api/src/imp/task/clone.rs#sys_clone`
  - 参考项目: `src/syscall_imp/task/thread.rs#sys_clone`

* **【文件系统/文件打开】**
* **底层技术析理**: `sys_openat` 的实现揭示了双方在文件系统资源管理上的根本分歧。目标项目采用 RAII 风格，显式构造 `Directory` 或 `File` 对象，并携带 `real_path` 字符串用于路径追踪，最后通过 `.add_to_fd_table()` 注册。参考项目则采用函数式风格，通过 `add_file_or_directory_fd` 闭包统一处理文件描述符的分配。前者是面向对象的资源封装，后者是过程式的资源注册。
* **代码溯源链接**:
  - 目标项目: `api/src/imp/fs/fd_ops.rs#sys_openat`
  - 参考项目: `arceos/api/arceos_posix_api/src/imp/fs.rs#sys_openat`

### 5. 单方模块与降权项
*   **单方模块/强异构模块**:
    *   **网络协议栈**: 双方虽均有此模块，但共同 AST 指纹为 0。这表明双方在协议栈的实现或选型上完全不同，是独立开发的有力证据。
    *   **第三方库**: 同样为 0 共同指纹，说明双方依赖的第三方库集合几乎没有交集。
*   **降权项说明**:
    *   **`arceos/modules/` 路径下的所有函数**: 这些函数（如 `exit_current`, `cancel_events`, `handle_sync_exception`）均属于 ArceOS 开源框架。控辩双方项目都基于此框架构建，因此这些函数的高度同构是使用同一上游代码的必然结果，**不能作为被告抄袭原告的证据**。
    *   **跨板块同名函数**: 如 `parent`、`processes`、`wait_timeout`，它们在不同模块（文件系统 vs 进程管理）中承担不同职责，其相似性源于通用算法或 Rust 标准库惯用法，不构成同板块主证据。
    *   **单行封装函数**: 如 `sys_exit` 调用 `do_exit`，`wait` 调用 `waitpid`，`busy_wait` 调用 `busy_wait_until`。这种单行转发是软件工程中的标准实践，无原创性，不能作为抄袭证据。

### 6. 保守结论
本庭裁定，被告项目 `[2026-T2026104869911108-XX]` 与参考项目 `[2025-狗剩-海南大学]` 之间**不存在构成抄袭或严重代码复用的同源性**。总体 27.96% 的 AST 相似度及双向覆盖率的不对称性，清晰地指向了“基于共享开源框架（ArceOS/NimbOS）的独立演进”模式。控方提出的“铁证”几乎全部位于 `arceos/modules` 等上游框架目录中，这些代码的原创性归属于 ArceOS 社区，而非原告。而在真正体现项目独创性的系统调用、进程模型、文件系统资源管理等核心领域，双方 AST 证据揭示了在抽象层次、内存模型和同步策略上的根本性架构差异。因此，现有证据不足以支持抄袭指控，被告项目的原创性辩护成立。

------------------------------

