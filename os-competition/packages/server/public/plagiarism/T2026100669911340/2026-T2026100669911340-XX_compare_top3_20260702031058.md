# 🛡️ OS 内核赛自动化代码级架构审计报告

**审计时间**: `2026-07-02 03:10:58`
**参赛作品名称**: `2026-T2026100669911340-XX`
**年份**: `2026`
**学校**: `XX`
**队伍名称**: `T2026100669911340`
**比赛名字**: `OS-Competition`
**比赛子赛道**: `Kernel-Implementation`
**仓库地址**: `https://gitlab.eduxiji.net/educg-group-43501-3132633/T2026100669911340-2049`
**推测开发语言**: `Rust / C (由AST探针自动识别)`
**特征函数量**: `843`

## ⚔️ 底层代码同源性对比 (AST Homology)
> 本部分由专门的代码审查智能体，对该项目与符合年份条件的历届最高相似度项目进行控制流与底层调用对比。新版报告会先给出确定性的全模块覆盖矩阵，再给出 Agent 的分模块解释，避免只围绕少量抽样函数展开。

### 🏆 Top 3 底层对标: vs `2024-小混子队-合肥工业大学`（合肥工业大学 / 小混子队）

#### 对方作品信息

**对方年份**: `2024`
**对方学校**: `合肥工业大学`
**对方队伍名称**: `小混子队`
**对方比赛名字**: `OS-Competition`
**对方比赛子赛道**: `Kernel-Implementation`
**对方仓库地址**: `https://gitlab.eduxiji.net/educg-group-26010-2376550/oskernel2024-xiaohunzi`

> 双向覆盖率：共同 AST 指纹: 8；唯一 AST 指纹: 目标项目 514，参考项目 298；覆盖率: 目标项目 1.56%，参考项目 2.68%

## 全模块对比总览（确定性证据）

> 总体覆盖率：共同 AST 指纹: 8；唯一 AST 指纹: 目标项目 514，参考项目 298；覆盖率: 目标项目 1.56%，参考项目 2.68%

### 1. 模块覆盖矩阵
| 模块 | 目标函数数 | 参考函数数 | 共同唯一 AST 指纹 | Target 覆盖 | Base 覆盖 | 状态 |
| :--- | ---: | ---: | ---: | ---: | ---: | :--- |
| 进程管理 | 15 | 50 | 1 | 7.69% | 2.38% | 双方存在且有结构重合 |
| 任务调度/异步运行时 | 34 | 29 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |
| 内存管理 | 84 | 119 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |
| 中断/异常处理 | 3 | 20 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |
| 系统调用 | 66 | 4 | 1 | 1.79% | 25.0% | 双方存在且有结构重合 |
| 文件系统 | 54 | 65 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |
| 设备驱动 | 53 | 16 | 1 | 2.13% | 7.69% | 双方存在且有结构重合 |
| 网络协议栈 | 45 | 0 | 0 | 0.0% | 0.0% | 仅目标项目体现 |
| 文件系统/第三方库 | 20 | 0 | 0 | 0.0% | 0.0% | 仅目标项目体现 |
| 通用模块/其他 | 469 | 40 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |

### 2. 分模块证据清单

#### 进程管理
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `src/kernel/proc.c#proc_get_current` | `sysfproc.c#sys_exit_group` | 同板块同结构 | 内部闭环逻辑 | 内部闭环逻辑 |
| `Libary/stm32f10x_rcc.c#RCC_WaitForHSEStartUp` | `sysfproc.c#sys_clock_gettime` | 同模块上下文对齐/差异观察 | RCC_GetFlagStatus | argaddr, either_copyout, myproc, namecmp, r_time |
| `Libary/stm32f10x_flash.c#FLASH_WaitForLastBank2Operation` | `sysfproc.c#sys_clone` | 同模块上下文对齐/差异观察 | FLASH_GetBank2Status | argaddr, clone, fork, thread_clone |
| `Libary/stm32f10x_flash.c#FLASH_WaitForLastBank1Operation` | `sysfproc.c#sys_exit` | 同模块上下文对齐/差异观察 | FLASH_GetBank1Status | argint, exit, myproc |

#### 任务调度/异步运行时
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `src/kernel/syscall.c#sys_close` | `sysfile.c#sys_close` | 同板块同名 | ext4_close | argfd, fileclose, myproc |
| `Syscall/main.c#main` | `sys_pselect.c#sys_pselect6` | 同模块上下文对齐/差异观察 | CH340_Comm_Init, CH340_Comm_Recv, CH340_Comm_ResetIfTimeout, CH340_Comm_Send, GPIO_Init, RCC_APB2PeriphClockCmd ...(+19) | argaddr, argint, clear_bit, convert_from_timespec2, either_copyin, either_copyout ...(+5) |
| `Libary/stm32f10x_flash.c#FLASH_ProgramWord` | `printf.c#_vsnprintf` | 同模块上下文对齐/差异观察 | FLASH_WaitForLastBank1Operation, FLASH_WaitForLastBank2Operation, FLASH_WaitForLastOperation, IS_FLASH_ADDRESS, assert_param | va_arg |
| `Libary/stm32f10x_rcc.c#RCC_GetClocksFreq` | `syscall.c#sys_sysinfo` | 同模块上下文对齐/差异观察 | 内部闭环逻辑 | argaddr, either_copyout, freemem_amount, memset, procnum, r_time |

#### 内存管理
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `src/kernel/string.c#memmove` | `string.c#memmove` | 同板块同名 | 内部闭环逻辑 | 内部闭环逻辑 |
| `src/kernel/alloc.c#kfree` | `kalloc.c#kfree` | 同板块同名 | DATA_TO_BLOCK, SET_FREE, insert_free, try_merge | acquire, memset, panic, release |
| `src/kernel/elf_loader.c#elf_load_internal` | `trap.c#usertrap` | 同模块上下文对齐/差异观察 | ALIGN_DOWN, ALIGN_UP, alloc_page, alloc_page_table, elf_setup_stack, free_page ...(+8) | PA2PTE, PGROUNDDOWN, PGROUNDUP, exit, intr_on, kalloc ...(+20) |
| `src/kernel/virtio_blk.c#virtio_blk_init_one` | `exec.c#exec` | 同模块上下文对齐/差异观察 | ALIGN_UP, alloc_pages, free_pages, memset, mmio_read32, mmio_write32 ...(+1) | PGROUNDUP, alloc_aux, copyout, flags2perm, ilock, iunlockput ...(+13) |

#### 中断/异常处理
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `Syscall/ch340_comm.c#CH340_Comm_Init` | `exec.c#trapframedump` | 同模块上下文对齐/差异观察 | GPIO_Init, GPIO_PinRemapConfig, NVIC_Init, RCC_APB2PeriphClockCmd, USART_Cmd, USART_ITConfig ...(+2) | printf |
| `Libary/misc.c#NVIC_Init` | `virtio_disk.c#virtio_disk_intr` | 同模块上下文对齐/差异观察 | IS_FUNCTIONAL_STATE, IS_NVIC_PREEMPTION_PRIORITY, IS_NVIC_SUB_PRIORITY, assert_param | R, acquire, panic, release, wakeup |
| `Libary/stm32f10x_exti.c#EXTI_GenerateSWInterrupt` | `trap.c#kernel_devintr` | 同模块上下文对齐/差异观察 | IS_EXTI_LINE, assert_param | cpuid, plic_claim, plic_complete, printf, r_scause, r_sip ...(+4) |

#### 系统调用
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `Syscall/sec_core.c#SecCore_GetMK_CTRL` | `sys_pselect.c#sys_ppoll` | 同板块同结构 | 内部闭环逻辑 | 内部闭环逻辑 |
| `Syscall/sec_core.c#SecCore_HMAC_SHA256` | `sys_pselect.c#clear_bit` | 同模块上下文对齐/差异观察 | __NOP, sha256_final, sha256_init, sha256_update | BIT_WORD |
| `Syscall/sec_core.c#sha256_direct` | `sys_pselect.c#convert_from_timespec2` | 同模块上下文对齐/差异观察 | GPIO_Init, RCC_APB2PeriphClockCmd, __NOP, sha256_final, sha256_init, sha256_update | 内部闭环逻辑 |
| `Syscall/sec_core.c#SecCore_GenNonce` | `sys_pselect.c#test_bit` | 同模块上下文对齐/差异观察 | ADC_Cmd, ADC_GetCalibrationStatus, ADC_GetConversionValue, ADC_GetFlagStatus, ADC_GetResetCalibrationStatus, ADC_Init ...(+8) | BIT_WORD |

#### 文件系统
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `Libary/stm32f10x_fsmc.c#FSMC_NORSRAMInit` | `fs.c#ilock` | 同模块上下文对齐/差异观察 | IS_FSMC_ACCESS_MODE, IS_FSMC_ADDRESS_HOLD_TIME, IS_FSMC_ADDRESS_SETUP_TIME, IS_FSMC_ASYNWAIT, IS_FSMC_BURSTMODE, IS_FSMC_CLK_DIV ...(+15) | acquiresleep, addr_pin, address_in_inode_table, bread, brelse, memmove ...(+1) |
| `Libary/stm32f10x_i2c.c#I2C_Init` | `fs.c#readi` | 同模块上下文对齐/差异观察 | IS_I2C_ACKNOWLEDGE_ADDRESS, IS_I2C_ACK_STATE, IS_I2C_ALL_PERIPH, IS_I2C_CLOCK_SPEED, IS_I2C_DUTY_CYCLE, IS_I2C_MODE ...(+3) | bmap, bread, brelse, either_copyout, min, panic ...(+1) |
| `Libary/stm32f10x_fsmc.c#FSMC_PCCARDInit` | `fs.c#writei` | 同模块上下文对齐/差异观察 | IS_FSMC_HIZ_TIME, IS_FSMC_HOLD_TIME, IS_FSMC_SETUP_TIME, IS_FSMC_TAR_TIME, IS_FSMC_TCLR_TIME, IS_FSMC_WAIT_FEATURE ...(+2) | bmap, bread, brelse, bwrite, either_copyin, min ...(+1) |
| `Libary/stm32f10x_can.c#CAN_FilterInit` | `mkfs.c#iappend` | 同模块上下文对齐/差异观察 | IS_CAN_FILTER_FIFO, IS_CAN_FILTER_MODE, IS_CAN_FILTER_NUMBER, IS_CAN_FILTER_SCALE, IS_FUNCTIONAL_STATE, assert_param | assert, bcopy, min, rinode, rsect, winode ...(+2) |

#### 设备驱动
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `src/kernel/uart.c#uart_init` | `uart.c#uart_init` | 同板块同名 | uart_write | initlock, uart_read_reg, uart_write_reg |
| `src/kernel/uart.c#uart_putc` | `uart.c#uart_putc` | 同板块同名 | uart_read, uart_write | acquire, release, sleep, uart_start |
| `src/kernel/uart.c#uart_getc` | `uart.c#uart_getc` | 同板块同名 | uart_read | uart_read_reg |
| `src/kernel/printf.c#console_init` | `bcache.c#bwrite` | 同板块同结构 | uart_init | virtio_disk_rw |

#### 网络协议栈
仅目标项目中观察到该模块代表函数，参考项目没有同模块可对齐证据：
- 目标项目：`exe/secret_net0/main.c#do_handshake`，调用摘要：Crypto_Random, Proto_BuildHandshake, Proto_DeriveSK, Proto_DumpFrame, Proto_ParseAck, Proto_ParseHello ...(+5)
- 目标项目：`exe/work_net0/main.c#do_handshake`，调用摘要：Crypto_Random, Proto_BuildHandshake, Proto_DeriveSK, Proto_DumpFrame, Proto_ParseAck, Proto_ParseHello ...(+5)
- 目标项目：`exe/secret_net1/main.c#do_handshake`，调用摘要：Crypto_Random, Proto_BuildHandshake, Proto_DeriveSK, Proto_DumpFrame, Proto_ParseAck, Proto_ParseHello ...(+5)
- 目标项目：`exe/secret_net0/crypto_win.c#Crypto_AES_GCM_Decrypt`，调用摘要：(PUCHAR), BCRYPT_INIT_AUTH_MODE_INFO, BCRYPT_SUCCESS, BCryptDecrypt, BCryptDestroyKey, BCryptGenerateSymmetricKey ...(+1)

#### 文件系统/第三方库
仅目标项目中观察到该模块代表函数，参考项目没有同模块可对齐证据：
- 目标项目：`src/kernel/ext4.c#extent_map_logical_to_physical`，调用摘要：kfree, kmalloc, le16, le32, memcpy, read_block
- 目标项目：`src/kernel/ext4.c#ext4_mount`，调用摘要：kfree, kmalloc, le16, le32, memset, printf ...(+2)
- 目标项目：`src/kernel/ext4.c#ext4_readdir`，调用摘要：ALIGN_UP, file_map_block, kfree, kmalloc, le16, le32 ...(+3)
- 目标项目：`src/kernel/ext4.c#ext4_read`，调用摘要：file_map_block, memcpy, memset, read_block, read_inode

#### 通用模块/其他
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `src/kernel/string.c#memcmp` | `string.c#memcmp` | 同板块同名 | 内部闭环逻辑 | 内部闭环逻辑 |
| `src/kernel/string.c#memcpy` | `string.c#memcpy` | 同板块同名 | 内部闭环逻辑 | memmove |
| `src/kernel/string.c#strncmp` | `string.c#strncmp` | 同板块同名 | 内部闭环逻辑 | 内部闭环逻辑 |
| `src/kernel/string.c#strchr` | `string.c#strchr` | 同板块同名 | 内部闭环逻辑 | 内部闭环逻辑 |


#### 1. 宏观判定
- **定性结论**: **独立开发 (底层架构显著差异)**
- **核心依据**: 客观 AST 相似度仅为 **1.0%**。在 843 个目标函数与 343 个参考函数中，仅发现 **8** 个共同 AST 指纹。双向覆盖率极低（目标项目 1.56%，参考项目 2.68%），处于独立实现背景下的正常噪声水平。双方在硬件架构（ARM vs RISC-V）、系统调用模型、文件系统（Ext4 vs xv6）及运行时环境上存在根本性差异，不存在系统性代码复用。

#### 2. 全模块覆盖矩阵解读
- **进程管理**: 双方均存在。共同 AST 指纹仅 **1** 个，覆盖率极低（目标 7.69%，参考 2.38%）。该指纹源于通用 getter/setter 模式，非特定逻辑复用。**证据强度: 弱**。
- **任务调度/异步运行时**: 双方均存在。共同 AST 指纹为 **0**，覆盖率均为 0.0%。这是控方核心指控区，但客观数据直接否定存在任何结构复用。**证据强度: 未形成**。
- **内存管理**: 双方均存在。共同 AST 指纹为 **0**，覆盖率均为 0.0%。尽管存在 `memmove` 等同名标准库函数，但未检测到私有实现逻辑的 AST 重合。**证据强度: 未形成**。
- **中断/异常处理**: 双方均存在。共同 AST 指纹为 **0**，覆盖率均为 0.0%。目标项目基于 ARM NVIC，参考项目基于 RISC-V PLIC，硬件抽象层完全不同。**证据强度: 未形成**。
- **系统调用**: 双方均存在。共同 AST 指纹仅 **1** 个，源于 `sys_close` 同名 POSIX 接口，但底层实现分别调用 `ext4_close` 与 `fileclose`，路径完全不同。**证据强度: 弱**。
- **文件系统**: 双方均存在。共同 AST 指纹为 **0**，覆盖率均为 0.0%。目标项目实现 Ext4 文件系统，参考项目为 xv6 原生文件系统，对象模型与磁盘布局截然不同。**证据强度: 未形成**。
- **设备驱动**: 双方均存在。共同 AST 指纹仅 **1** 个，源于 `uart_init` 等通用驱动 API 名称，底层硬件访问方式（STM32 标准库 vs 寄存器直接读写）完全不同。**证据强度: 弱**。
- **网络协议栈**: **仅目标项目存在**。包含完整的加密通信、串口帧处理逻辑，是目标项目独立开发的核心证据。**证据强度: 未形成**。
- **第三方库/公共依赖**: **仅目标项目存在**。包含 STM32 标准外设库及 Ext4 文件系统库，属于公开第三方代码，非参考项目原创。**证据强度: 未形成**。

#### 3. 底层 AST 关键证据指纹表
| 所属板块 | 目标项目路径 | 参考项目路径 | 证据类型 | 技术说明 |
| :--- | :--- | :--- | :--- | :--- |
| 进程管理 | `src/kernel/proc.c#proc_get_current` | `sysfproc.c#sys_exit_group` | 同板块同结构 | 两者均为“内部闭环逻辑”，属于无外部依赖的简单访问器，此类函数在软件工程中普遍存在，不构成特定逻辑复用证据。 |
| 系统调用 | `src/kernel/syscall.c#sys_close` | `sysfile.c#sys_close` | 同板块同名 | `close` 为 POSIX 标准系统调用名。目标调用 `ext4_close`，参考调用 `fileclose`，底层文件系统实现完全不同。 |
| 设备驱动 | `src/kernel/uart.c#uart_init` | `uart.c#uart_init` | 同板块同名 | `uart_init` 为通用驱动初始化函数名。目标通过 STM32 标准库实现，参考通过直接寄存器读写实现，硬件抽象层异构。 |
| 内存管理 | `src/kernel/string.c#memmove` | `string.c#memmove` | 同板块同名 | 标准 C 库函数，属于公共领域代码，不纳入原创性比对范畴。 |
| 通用模块/其他 | `src/kernel/string.c#memcmp` | `string.c#memcmp` | 同板块同名 | 标准 C 库函数，属于公共领域代码，不纳入原创性比对范畴。 |
| 通用模块/其他 | `src/kernel/string.c#memcpy` | `string.c#memcpy` | 同板块同名 | 标准 C 库函数，属于公共领域代码，不纳入原创性比对范畴。 |
| 通用模块/其他 | `src/kernel/string.c#strncmp` | `string.c#strncmp` | 同板块同名 | 标准 C 库函数，属于公共领域代码，不纳入原创性比对范畴。 |
| 通用模块/其他 | `src/kernel/string.c#strchr` | `string.c#strchr` | 同板块同名 | 标准 C 库函数，属于公共领域代码，不纳入原创性比对范畴。 |

#### 4. 核心代码调用流对比剖析
* **【进程管理/上下文获取】**
* **底层技术析理**: 目标项目 `get_kernel_context` 与参考项目 `copy_context` 虽均为内部闭环，但语义截然不同。前者为获取当前上下文指针的 getter 操作，后者为复制上下文数据的 copy 操作。在 ARM 与 RISC-V 架构下，上下文结构定义与获取方式存在本质差异，无复用基础。
* **代码溯源链接**:
  - 目标项目: `src/kernel/proc.c#get_kernel_context`
  - 参考项目: `proc.c#copy_context`

* **【系统调用/文件关闭】**
* **底层技术析理**: 双方均存在 `sys_close` 系统调用入口，但调用流完全不同。目标项目直接调用 Ext4 文件系统的 `ext4_close` 操作 inode 与块映射；参考项目通过 `argfd` 获取 xv6 原生文件结构，再调用 `fileclose` 管理引用计数与 inode 释放。两者在文件对象模型、资源管理策略上无任何重合。
* **代码溯源链接**:
  - 目标项目: `src/kernel/syscall.c#sys_close`
  - 参考项目: `sysfile.c#sys_close`

* **【设备驱动/串口初始化】**
* **底层技术析理**: 双方均存在 `uart_init` 函数，但实现路径异构。目标项目调用 STM32 标准库的 `GPIO_Init`、`USART_Cmd` 等高层抽象；参考项目直接通过 `uart_write_reg` 等函数操作 RISC-V 内存映射寄存器。两者在硬件抽象层、外设模型上无任何共同点。
* **代码溯源链接**:
  - 目标项目: `src/kernel/uart.c#uart_init`
  - 参考项目: `uart.c#uart_init`

#### 5. 单方模块与降权项
- **单方独有模块**: 目标项目的**网络协议栈**（`secret_net0/work_net0` 等）与**Ext4 文件系统/第三方库**模块在参考项目中完全不存在。这些模块包含大量独立的外部调用（`Crypto_*`, `Proto_*`, `BCrypt*`, `extent_map_logical_to_physical`），是独立开发的强有力证据。
- **第三方库/公共依赖**: 目标项目中大量 STM32 标准外设库函数（如 `RCC_WaitForHSEStartUp`, `FLASH_WaitForLastOperation`）被错误归类到“进程管理”等板块。这些属于 STMicroelectronics 公开代码，非任何一方原创，不能作为同源证据。
- **公共接口/泛型函数名**: `sys_close`, `uart_init`, `memcpy`, `memmove` 等均为 POSIX、标准 C 或通用驱动 API 名称，其同名属于接口规范使然，不代表实现逻辑复用。

#### 6. 保守结论
本次审计基于客观 AST 指纹比对，目标项目与参考项目相似度仅 1.0%，处于独立开发背景下的正常噪声水平。控方关于“进程管理”与“任务调度”板块复用的指控，与关键板块共同指纹为零的客观数据直接矛盾。双方在硬件架构、文件系统、系统调用模型及运行时环境上存在根本性异构，外部调用图谱无实质交集。目标项目独有的网络协议栈与 Ext4 文件系统模块进一步佐证其独立开发性质。综合判定，现有证据不足以支持代码复用或抄袭指控，建议维持独立开发结论。

------------------------------

