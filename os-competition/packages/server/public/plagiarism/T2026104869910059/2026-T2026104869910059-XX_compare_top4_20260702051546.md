# 🛡️ OS 内核赛自动化代码级架构审计报告

**审计时间**: `2026-07-02 05:15:46`
**参赛作品名称**: `2026-T2026104869910059-XX`
**年份**: `2026`
**学校**: `XX`
**队伍名称**: `T2026104869910059`
**比赛名字**: `OS-Competition`
**比赛子赛道**: `Kernel-Implementation`
**仓库地址**: `https://gitlab.eduxiji.net/educg-group-43501-3132633/T2026104869910059-3729`
**推测开发语言**: `Rust / C (由AST探针自动识别)`
**特征函数量**: `3667`

## ⚔️ 底层代码同源性对比 (AST Homology)
> 本部分由专门的代码审查智能体，对该项目与符合年份条件的历届最高相似度项目进行控制流与底层调用对比。新版报告会先给出确定性的全模块覆盖矩阵，再给出 Agent 的分模块解释，避免只围绕少量抽样函数展开。

### 🏆 Top 4 底层对标: vs `2024-cabbageOS-华中科技大学`（华中科技大学 / cabbageOS）

#### 对方作品信息

**对方年份**: `2024`
**对方学校**: `华中科技大学`
**对方队伍名称**: `cabbageOS`
**对方比赛名字**: `OS-Competition`
**对方比赛子赛道**: `Kernel-Implementation`
**对方仓库地址**: `https://gitlab.eduxiji.net/educg-group-26010-2376550/T202410487992456-422`

> 双向覆盖率：共同 AST 指纹: 720；唯一 AST 指纹: 目标项目 2712，参考项目 1579；覆盖率: 目标项目 26.55%，参考项目 45.6%

## 全模块对比总览（确定性证据）

> 总体覆盖率：共同 AST 指纹: 720；唯一 AST 指纹: 目标项目 2712，参考项目 1579；覆盖率: 目标项目 26.55%，参考项目 45.6%

### 1. 模块覆盖矩阵
| 模块 | 目标函数数 | 参考函数数 | 共同唯一 AST 指纹 | Target 覆盖 | Base 覆盖 | 状态 |
| :--- | ---: | ---: | ---: | ---: | ---: | :--- |
| 进程管理 | 142 | 95 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |
| 任务调度/异步运行时 | 225 | 56 | 1 | 0.5% | 1.85% | 双方存在且有结构重合 |
| 内存管理 | 727 | 290 | 33 | 5.83% | 11.58% | 双方存在且有结构重合 |
| 中断/异常处理 | 126 | 28 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |
| 系统调用 | 168 | 76 | 6 | 5.56% | 27.27% | 双方存在且有结构重合 |
| 文件系统 | 396 | 176 | 16 | 5.23% | 10.88% | 双方存在且有结构重合 |
| 设备驱动 | 462 | 446 | 267 | 65.93% | 70.45% | 双方存在且有结构重合 |
| 网络协议栈 | 0 | 0 | 0 | 0.0% | 0.0% | 双方均未体现 |
| 文件系统/第三方库 | 748 | 522 | 389 | 60.5% | 89.22% | 双方存在且有结构重合 |
| 第三方库 | 2 | 1 | 1 | 50.0% | 100.0% | 双方存在且有结构重合 |
| 通用模块/其他 | 671 | 228 | 7 | 2.13% | 3.8% | 双方存在且有结构重合 |

### 2. 分模块证据清单

#### 进程管理
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `os-la/src/syscall/process.rs#sys_wait4` | `kernel/src/syscall/sysproc.c#sys_wait4` | 同板块同名 | Err, Ok, child.getpid, child.inner_exclusive_access, current_task, current_task().unwrap ...(+19) | arg_addr, arg_int, waitpid |
| `os/src/syscall/sync.rs#sys_rt_sigtimedwait` | `kernel/src/syscall/sysproc.c#sys_rt_sigtimedwait` | 同板块同名 | Some, b.fill, child.inner_exclusive_access, crate::mm::try_translated_byte_buffer, crate::timer::check_timer, current_process ...(+18) | 内部闭环逻辑 |
| `os-la/src/syscall/process.rs#sys_clone` | `kernel/src/syscall/sysproc.c#sys_clone` | 同板块同名 | CloneFlags::from_bits, CloneFlags::from_bits(flags as u32).ok_or_else, Ok, add_task, current_task, current_task().unwrap ...(+5) | arg_addr, arg_int, do_clone |
| `os/src/syscall/process.rs#sys_getppid` | `kernel/src/syscall/sysproc.c#sys_getppid` | 同板块同名 | current_process, p.getpid, parent.upgrade, process.inner_exclusive_access, trace! | proc_current |

#### 任务调度/异步运行时
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `os/src/syscall/fs.rs#sys_chdir` | `kernel/src/syscall/sysfile.c#sys_chdir` | 同板块同名 | Ext4File::new, canonicalize_path, core::str::from_utf8, core::str::from_utf8(&n[..len]).ok, core::str::from_utf8(&n[..len]).ok().map, current_process ...(+21) | arg_str, get_absolute_path, get_fs_from_path, ip->i_op->lock, ip->i_op->unlock, ip->i_op->unlock_put ...(+4) |
| `os-la/src/syscall/fs.rs#sys_linkat` | `kernel/src/syscall/sysfile.c#sys_linkat` | 同板块同名 | Err, Ok, current_task, current_task().unwrap, current_user_token, debug! ...(+10) | ASSERT, arg_int, arg_str, find_inode, get_absolute_path, get_fs_from_path ...(+3) |
| `os-la/src/syscall/fs.rs#sys_unlinkat` | `kernel/src/syscall/sysfile.c#sys_unlinkat` | 同板块同名 | Ok, UnlinkatFlags::from_bits_truncate, abs_path.as_str, current_task, current_task().unwrap, current_user_token ...(+14) | __namecmp, arg_int, arg_str, dp->i_op->delete, dp->i_op->dirlookup, dp->i_op->lock ...(+13) |
| `os/src/syscall/fs.rs#sys_dup3` | `kernel/src/syscall/sysfile.c#sys_dup3` | 同板块同名 | Arc::clone, Some, current_process, inner.fd_table.len, inner.fd_table.push, inner.fd_table[newfd].take ...(+5) | ASSERT, acquire, arg_fd, arg_int, f->f_op->dup, generic_fileclose ...(+2) |

#### 内存管理
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `os-la-patch/virtio-drivers/src/device/socket/connectionmanager.rs#send_recv` | `kernel/dep/virtio-drivers/src/device/socket/connectionmanager.rs#send_recv` | 同板块同名 | "Hello from guest".as_bytes, (hello_from_guest.len() as u32).into, (hello_from_host.len() as u32).into, 0.into, 50.into, Arc::new ...(+44) | "Hello from guest".as_bytes, (hello_from_guest.len() as u32).into, (hello_from_host.len() as u32).into, 0.into, 50.into, Arc::new ...(+43) |
| `os-la-patch/virtio-drivers/src/device/socket/connectionmanager.rs#incoming_connection` | `kernel/dep/virtio-drivers/src/device/socket/connectionmanager.rs#incoming_connection` | 同板块同名同结构 | 0.into, 50.into, Arc::new, Default::default, Mutex::new, NonNull::from ...(+25) | 0.into, 50.into, Arc::new, Default::default, Mutex::new, NonNull::from ...(+25) |
| `os/src/syscall/process.rs#sys_mmap` | `kernel/src/mm/mmap.c#sys_mmap` | 同板块同名 | (PAGE_SIZE - page_off).min, MapArea::new, VirtAddr::from, VirtAddr::from(start_va + alloc_len).ceil, VirtAddr::from(start_va).floor, asm! ...(+23) | acquire, arg_addr, arg_fd, arg_int, arg_long, arg_ulong ...(+3) |
| `os-la-patch/virtio-drivers/src/queue.rs#recycle_descriptors` | `kernel/dep/virtio-drivers/src/queue.rs#recycle_descriptors` | 同板块同名同结构 | Box::from_raw, H::unshare, InputOutputIter::new, InputOutputIter::new(inputs, outputs).enumerate, Some, assert_eq! ...(+17) | Box::from_raw, H::unshare, InputOutputIter::new, InputOutputIter::new(inputs, outputs).enumerate, Some, assert_eq! ...(+17) |

#### 中断/异常处理
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `os-la-patch/polyhal-trap/src/trap/loongarch64/unaligned.rs#emulate_load_store_insn` | `kernel/platform/visionfive/src/trap.c#tf_flrestore` | 同模块上下文对齐/差异观察 | badv::read, badv::read().vaddr, core::arch::asm!, panic!, unaligned_read, unaligned_write | 内部闭环逻辑 |
| `os-la/src/signal/sigflags.rs#default_op` | `kernel/platform/visionfive/src/trap.c#tf_flstore` | 同模块上下文对齐/差异观察 | continue_signals.contains, dump_signals.contains, ignore_signals.contains, stop_signals.contains, terminate_signals.contains | 内部闭环逻辑 |
| `os-la-patch/polyhal-trap/src/trapframe/x86_64.rs#index_mut` | `kernel/platform/qemu/src/virtio_disk.c#virtio_disk_intr` | 同模块上下文对齐/差异观察 | (self.rsp as *mut usize).as_mut, (self.rsp as *mut usize).as_mut().unwrap, log::warn!, unreachable! | R, __sync_synchronize, acquire, panic, release, sem_v |
| `os-la-patch/polyhal-trap/src/trap/riscv64.rs#uservec` | `kernel/platform/qemu/src/trap.c#kerneltrap` | 同模块上下文对齐/差异观察 | naked_asm! | backtrace, devintr, intr_get, panic, printf, r_scause ...(+7) |

#### 系统调用
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `os-la/src/syscall/other.rs#sys_uname` | `kernel/src/syscall/sysinfo.c#sys_uname` | 同板块同名 | Ok, UserBuffer::new, buf[..len].copy_from_slice, bytes.len, bytes.len().min, copy_str_to_buf ...(+8) | arg_addr, either_copy_out |
| `os-la/src/syscall/mm.rs#sys_shmat` | `kernel/src/syscall/sysipc.c#sys_shmat` | 同板块同名 | Err, ShmFlags::from_bits_truncate, shm_attach, shmflg.contains | arg_addr, arg_int, do_shmat |
| `user/src/lib.rs#waitpid` | `user/deps/syscall.c#waitpid` | 同板块同名 | sys_waitpid, yield_ | syscall |
| `user/src/lib.rs#wait` | `user/deps/syscall.c#wait` | 同板块同名 | sys_waitpid, yield_ | waitpid |

#### 文件系统
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `os-la-patch/virtio-drivers/src/device/blk.rs#device_id` | `kernel/dep/virtio-drivers/src/device/blk.rs#device_id` | 同板块同名同结构 | Arc::new, BlkFeature::RING_INDIRECT_DESC.bits, Default::default, Mutex::new, NonNull::from, QUEUE_SIZE.into ...(+14) | Arc::new, BlkFeature::RING_INDIRECT_DESC.bits, Default::default, Mutex::new, NonNull::from, QUEUE_SIZE.into ...(+14) |
| `os-la-patch/virtio-drivers/src/device/blk.rs#flush` | `kernel/dep/virtio-drivers/src/device/blk.rs#flush` | 同板块同名同结构 | (BlkFeature::RING_INDIRECT_DESC | BlkFeature::FLUSH).bits, Arc::new, Default::default, Mutex::new, NonNull::from, QUEUE_SIZE.into ...(+13) | (BlkFeature::RING_INDIRECT_DESC | BlkFeature::FLUSH).bits, Arc::new, Default::default, Mutex::new, NonNull::from, QUEUE_SIZE.into ...(+13) |
| `os-la-patch/virtio-drivers/src/transport/pci/bus.rs#bar_info` | `kernel/dep/virtio-drivers/src/transport/pci/bus.rs#bar_info` | 同板块同名 | (!(size_mask & 0xfffffff0)).wrapping_add, Err, MemoryBarType::try_from, Ok, self
                    .configuration_access
                    .read_word, self
            .configuration_access
            .read_word ...(+2) | (!(size_mask & 0xfffffff0)).wrapping_add, Err, MemoryBarType::try_from, Ok, self.config_read_word, self.config_write_word ...(+1) |
| `user/basic/user/lib/syscall.c#fstat` | `user/deps/syscall.c#fstat` | 同板块同名 | makedev, syscall | syscall |

#### 设备驱动
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `os-la-patch/virtio-drivers/src/queue.rs#fake_read_write_queue` | `kernel/dep/virtio-drivers/src/queue.rs#fake_read_write_queue` | 同板块同名 | (*available_ring).idx.load, (*used_ring).idx.fetch_add, (*used_ring).idx.load, Vec::new, assert!, assert_eq! ...(+18) | (*available_ring).idx.load, (*used_ring).idx.fetch_add, (*used_ring).idx.load, Vec::new, assert!, assert_eq! ...(+18) |
| `os-la-patch/virtio-drivers/src/device/sound/fake.rs#handle_control_request` | `kernel/dep/virtio-drivers/src/device/sound/fake.rs#handle_control_request` | 同板块同名 | CommandCode::SOk.into, Some, Vec::new, VirtIOSndHdr {
                            command_code: CommandCode::SOk.into(),
                        }
                        .as_bytes, VirtIOSndHdr::read_from_prefix, VirtIOSndHdr::read_from_prefix(&request)
                .expect ...(+16) | CommandCode::SOk.into, Some, Vec::new, VirtIOSndHdr {
                            command_code: CommandCode::SOk.into(),
                        }
                        .as_bytes, VirtIOSndHdr::read_from_prefix, VirtIOSndHdr::read_from_prefix(&request).expect ...(+16) |
| `os-la-patch/virtio-drivers/src/device/sound.rs#play` | `kernel/dep/virtio-drivers/src/device/sound.rs#play` | 同板块同名同结构 | FakeSoundDevice::new, PcmFeatures::empty, VirtIOSound::<FakeHal, FakeTransport<VirtIOSoundConfig>>::new, VirtIOSound::<FakeHal, FakeTransport<VirtIOSoundConfig>>::new(transport).unwrap, assert_eq!, expected_sound.extend ...(+22) | FakeSoundDevice::new, PcmFeatures::empty, VirtIOSound::<FakeHal, FakeTransport<VirtIOSoundConfig>>::new, VirtIOSound::<FakeHal, FakeTransport<VirtIOSoundConfig>>::new(transport).unwrap, assert_eq!, expected_sound.extend ...(+22) |
| `os-la-patch/virtio-drivers/src/device/sound.rs#pcm_xfer` | `kernel/dep/virtio-drivers/src/device/sound.rs#pcm_xfer` | 同板块同名同结构 | CommandCode::SOk.into, Default::default, Err, Ok, Some, array::from_fn ...(+17) | CommandCode::SOk.into, Default::default, Err, Ok, Some, array::from_fn ...(+17) |

#### 文件系统/第三方库
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `lwext4_rust/c/lwext4/src/ext4_balloc.c#ext4_balloc_alloc_block` | `kernel/src/fs/ext4/lwext4/ext4_balloc.c#ext4_balloc_alloc_block` | 同板块同名同结构 | ext4_balloc_get_bgid_of_block, ext4_balloc_get_block_of_bgid, ext4_balloc_set_bitmap_csum, ext4_balloc_verify_bitmap_csum, ext4_bg_get_block_bitmap, ext4_bg_get_free_blocks_count ...(+19) | ext4_balloc_get_bgid_of_block, ext4_balloc_get_block_of_bgid, ext4_balloc_set_bitmap_csum, ext4_balloc_verify_bitmap_csum, ext4_bg_get_block_bitmap, ext4_bg_get_free_blocks_count ...(+19) |
| `lwext4_rust/c/lwext4/src/ext4_journal.c#jbd_journal_prepare` | `kernel/src/fs/ext4/lwext4/ext4_journal.c#jbd_journal_prepare` | 同板块同名同结构 | EXT4_BLOCK_ZERO, RB_FIND, TAILQ_FOREACH_REVERSE_SAFE, TAILQ_FOREACH_SAFE, TAILQ_REMOVE, ext4_bcache_clear_dirty ...(+17) | EXT4_BLOCK_ZERO, RB_FIND, TAILQ_FOREACH_REVERSE_SAFE, TAILQ_FOREACH_SAFE, TAILQ_REMOVE, ext4_bcache_clear_dirty ...(+17) |
| `lwext4_rust/c/lwext4/src/ext4.c#ext4_dir_rm` | `kernel/src/fs/ext4/lwext4/ext4.c#ext4_dir_rm` | 同板块同名同结构 | EXT4_MP_LOCK, EXT4_MP_UNLOCK, ext4_block_cache_write_back, ext4_dir_en_get_inode, ext4_dir_iterator_fini, ext4_dir_iterator_init ...(+19) | EXT4_MP_LOCK, EXT4_MP_UNLOCK, ext4_block_cache_write_back, ext4_dir_en_get_inode, ext4_dir_iterator_fini, ext4_dir_iterator_init ...(+19) |
| `lwext4_rust/c/lwext4/src/ext4_dir_idx.c#ext4_dir_dx_split_index` | `kernel/src/fs/ext4/lwext4/ext4_dir_idx.c#ext4_dir_dx_split_index` | 同板块同名同结构 | ext4_block_set, ext4_dir_dx_climit_get_count, ext4_dir_dx_climit_get_limit, ext4_dir_dx_climit_set_count, ext4_dir_dx_climit_set_limit, ext4_dir_dx_entry_get_hash ...(+10) | ext4_block_set, ext4_dir_dx_climit_get_count, ext4_dir_dx_climit_get_limit, ext4_dir_dx_climit_set_count, ext4_dir_dx_climit_set_limit, ext4_dir_dx_entry_get_hash ...(+10) |

#### 第三方库
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `os-la-patch/virtio-drivers/src/transport/mmio.rs#vendor_id` | `kernel/dep/virtio-drivers/src/transport/mmio.rs#vendor_id` | 同板块同名同结构 | volread! | volread! |

#### 通用模块/其他
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `user/basic/user/lib/string.c#strncpy` | `kernel/src/lib/string.c#strncpy` | 同板块同名 | HASZERO, memset | 内部闭环逻辑 |
| `user/basic/user/lib/string.c#strcpy` | `user/deps/ulib.c#strcpy` | 同板块同名 | HASZERO | 内部闭环逻辑 |
| `user/basic/user/lib/stdio.c#printint` | `user/deps/printf.c#printint` | 同板块同名 | out, puts | putc |
| `os/src/lang_items.rs#backtrace` | `kernel/src/lib/printf.c#backtrace` | 同板块同名 | asm!, current_kstack_top, println! | Log, PGROUNDDOWN, PGROUNDUP, printf, r_fp |


#### 1. 宏观判定
- **定性结论**: **独立开发** (相似度: 20.16%，共同 AST 指纹: 720；唯一 AST 指纹: 目标项目 2712，参考项目 1579；覆盖率: 目标项目 26.55%，参考项目 45.6%)
- **核心依据**: 总体 AST 相似度处于低位。共同指纹高度集中于双方共用的第三方开源库（`virtio-drivers`、`lwext4`），而在操作系统核心板块（进程管理、中断/异常处理）中，共同 AST 指纹为零。这表明两项目在核心逻辑上不存在可被客观 AST 比对识别的结构复用。

#### 2. 全模块覆盖矩阵解读
- **进程管理**: 双方均存在。共同 AST 指纹为 0，覆盖率均为 0.0%。双方虽实现了 POSIX 标准要求的同名系统调用，但在 AST 层面未观察到任何共同结构指纹，表明底层实现逻辑完全异构。**证据强度: 未形成**。
- **任务调度/异步运行时**: 双方均存在。共同 AST 指纹为 1，覆盖率极低（目标 0.5%，参考 1.85%）。唯一的共同指纹源于文件系统相关的 `sys_chdir` 调用，属于跨板块弱关联，不构成调度器核心逻辑的同源证据。**证据强度: 弱**。
- **内存管理**: 双方均存在。共同 AST 指纹为 33，覆盖率较低（目标 5.83%，参考 11.58%）。共同指纹主要来自 `virtio-drivers` 库中的内存操作和 `sys_mmap` 等标准系统调用接口，未涉及核心分配器算法的同源。**证据强度: 弱**。
- **中断/异常处理**: 双方均存在。共同 AST 指纹为 0，覆盖率均为 0.0%。双方在 Trap 处理、信号默认操作等核心逻辑上无任何共同结构指纹，架构差异显著。**证据强度: 未形成**。
- **系统调用**: 双方均存在。共同 AST 指纹为 6，覆盖率（目标 5.56%，参考 27.27%）差异大。共同指纹均为 POSIX 标准系统调用名（如 `sys_uname`, `sys_shmat`），其内部调用图谱完全不同，属于接口撞车。**证据强度: 弱**。
- **文件系统**: 双方均存在。共同 AST 指纹为 16，覆盖率较低（目标 5.23%，参考 10.88%）。共同指纹主要来自 `virtio-drivers` 的块设备驱动和 `lwext4` 文件系统库，属于第三方库共用。**证据强度: 弱**。
- **设备驱动**: 双方均存在。共同 AST 指纹为 267，覆盖率极高（目标 65.93%，参考 70.45%）。共同指纹几乎全部来自双方共用的 `virtio-drivers` 上游 crate，其内部测试函数（如 `fake_read_write_queue`）和核心数据结构完全一致。**证据强度: 强（第三方库）**。
- **网络协议栈**: 双方均未体现。**证据强度: 未形成**。
- **第三方库/公共依赖**: 双方均存在。共同 AST 指纹为 390（文件系统/第三方库 389 + 第三方库 1），覆盖率极高（目标 60.5%/50.0%，参考 89.22%/100.0%）。共同指纹覆盖了 `lwext4` 和 `virtio-drivers` 的核心逻辑，是整体相似度的主要来源。**证据强度: 强（第三方库）**。

#### 3. 底层 AST 关键证据指纹表
| 所属板块 | 目标项目路径 | 参考项目路径 | 证据类型 | 技术说明 |
| :--- | :--- | :--- | :--- | :--- |
| 第三方库/公共依赖 | `os-la-patch/virtio-drivers/src/device/socket/connectionmanager.rs#incoming_connection` | `kernel/dep/virtio-drivers/src/device/socket/connectionmanager.rs#incoming_connection` | 同源代码 | 函数体 AST 结构完全一致，包含相同的 `Arc::new`, `Mutex::new`, `NonNull::from` 等调用序列，系共用上游 crate。 |
| 第三方库/公共依赖 | `os-la-patch/virtio-drivers/src/device/blk.rs#device_id` | `kernel/dep/virtio-drivers/src/device/blk.rs#device_id` | 同源代码 | VirtIO 块设备初始化逻辑的 AST 拓扑完全同构，包含相同的特性位掩码和队列配置。 |
| 第三方库/公共依赖 | `lwext4_rust/c/lwext4/src/ext4_balloc.c#ext4_balloc_alloc_block` | `kernel/src/fs/ext4/lwext4/ext4_balloc.c#ext4_balloc_alloc_block` | 同源代码 | ext4 块分配器的核心算法 AST 完全一致，包含相同的 bitmap 校验、块组查找等调用序列。 |
| 第三方库/公共依赖 | `lwext4_rust/c/lwext4/src/ext4_journal.c#jbd_journal_prepare` | `kernel/src/fs/ext4/lwext4/ext4_journal.c#jbd_journal_prepare` | 同源代码 | JBD 日志准备阶段的 AST 结构完全一致，包含相同的 `TAILQ_FOREACH_SAFE` 等宏调用和事务状态机。 |
| 设备驱动 | `os-la-patch/virtio-drivers/src/queue.rs#fake_read_write_queue` | `kernel/dep/virtio-drivers/src/queue.rs#fake_read_write_queue` | 同源代码 | VirtIO 队列模拟测试函数的 AST 完全一致，包含相同的 `available_ring` 和 `used_ring` 操作序列。 |
| 设备驱动 | `os-la-patch/virtio-drivers/src/device/sound.rs#play` | `kernel/dep/virtio-drivers/src/device/sound.rs#play` | 同源代码 | VirtIO 音频设备播放测试的 AST 完全同构，包含相同的 `FakeSoundDevice::new` 和 `expected_sound.extend` 调用流。 |
| 文件系统 | `os-la-patch/virtio-drivers/src/device/blk.rs#flush` | `kernel/dep/virtio-drivers/src/device/blk.rs#flush` | 同源代码 | VirtIO 块设备刷新操作的 AST 完全一致，包含相同的特性位组合和队列初始化。 |
| 内存管理 | `os-la-patch/virtio-drivers/src/queue.rs#recycle_descriptors` | `kernel/dep/virtio-drivers/src/queue.rs#recycle_descriptors` | 同源代码 | VirtIO 队列描述符回收逻辑的 AST 完全同构，包含相同的 `Box::from_raw` 和迭代器操作。 |
| 系统调用 | `user/src/lib.rs#waitpid` | `user/deps/syscall.c#waitpid` | 弱信号 | 用户态系统调用封装，函数名和功能遵循 POSIX 标准，但内部实现（Rust `sys_waitpid` vs C `syscall`）完全不同。 |
| 通用模块/其他 | `user/basic/user/lib/string.c#strncpy` | `kernel/src/lib/string.c#strncpy` | 弱信号 | C 标准库函数，目标项目使用 `HASZERO` 宏优化，参考项目为闭环逻辑，实现路径不同。 |

#### 4. 核心代码调用流对比剖析
* **【进程管理/调度器核心循环】**
* **底层技术析理**: 目标项目 `run_tasks` 采用全局 `PROCESSOR` 单例加 RAII 锁 (`exclusive_access`) 管理任务队列，通过 `fetch_task` 获取任务，`check_timer` 处理定时器，最后调用 `__switch` 进行上下文切换。参考项目 `thread_sched` 基于 per-CPU `mycpu` 结构，通过 `Queue_provide_atomic` 获取任务，手动调用 `add_timer_atomic`/`delete_timer_atomic` 管理定时器，最后调用 `swtch`。两者虽在功能流上相似，但抽象层次（全局单例 vs per-CPU）、同步模型（RAII vs 手动锁）和具体调用对象完全不同，AST 无共同指纹。
* **代码溯源链接**:
  - 目标项目: `os/src/task/processor.rs#run_tasks`
  - 参考项目: `kernel/src/proc/sched.c#thread_sched`

* **【进程管理/信号掩码管理】**
* **底层技术析理**: 目标项目 `sys_rt_sigprocmask` 通过 `current_process` 获取进程结构，使用 `inner_exclusive_access` 加锁，然后通过 `try_translated_refmut` 操作用户态内存来读取和设置信号掩码。参考项目 `do_sigprocmask` 通过 `thread_current` 获取线程结构，使用 `acquire`/`release` 手动加解锁，通过 `sig_and`/`sig_or` 进行位运算。两者在作用对象（进程 vs 线程）、用户态交互方式（内存翻译 vs 无）和掩码操作（空指针判断 vs 位运算）上存在根本差异，AST 无共同指纹。
* **代码溯源链接**:
  - 目标项目: `os/src/syscall/process.rs#sys_rt_sigprocmask`
  - 参考项目: `kernel/src/ipc/signal.c#do_sigprocmask`

* **【内存管理/内存映射】**
* **底层技术析理**: 目标项目 `sys_mmap` 的 AST 中包含 Rust 特有的 `VirtAddr::from(start_va).floor()`、`MapArea::new` 等对象构造和方法调用，以及内联汇编 `asm!`。参考项目 `sys_mmap` 则是典型的 C 系统调用处理，通过 `arg_addr`, `arg_fd` 等函数解析参数，然后调用 `acquire` 加锁。两者除了系统调用名相同外，底层的内存抽象（对象化 vs 过程化）和参数处理方式完全不同。
* **代码溯源链接**:
  - 目标项目: `os/src/syscall/process.rs#sys_mmap`
  - 参考项目: `kernel/src/mm/mmap.c#sys_mmap`

* **【文件系统/第三方库】**
* **底层技术析理**: 目标项目 `ext4_balloc_alloc_block` 与参考项目对应函数在 AST 层面完全同构，均包含 `ext4_balloc_get_bgid_of_block` -> `ext4_balloc_verify_bitmap_csum` -> `ext4_bg_set_free_blocks_count` 等完整的调用序列。这是双方共用 `lwext4` 库的直接证据，属于第三方公共依赖，不反映项目原创性。
* **代码溯源链接**:
  - 目标项目: `lwext4_rust/c/lwext4/src/ext4_balloc.c#ext4_balloc_alloc_block`
  - 参考项目: `kernel/src/fs/ext4/lwext4/ext4_balloc.c#ext4_balloc_alloc_block`

#### 5. 单方模块与降权项
- **单方模块**: 无。所有核心模块双方均有涉及。
- **降权项**:
  - **第三方库/公共依赖**: `virtio-drivers` (Rust) 和 `lwext4` (C) 是双方共用的上游开源库，贡献了绝大部分共同 AST 指纹（设备驱动、文件系统/第三方库板块）。这些代码的同源性不能作为项目间抄袭的证据。
  - **POSIX 标准系统调用接口**: `sys_wait4`, `sys_clone`, `sys_exit`, `sys_getpid` 等函数名由 Linux ABI 标准定义，双方实现这些接口是功能需求所致。其内部 AST 调用图谱完全不同，属于接口撞车，不构成逻辑复用。
  - **通用 C 库函数**: `strncpy`, `strcpy`, `printint` 等函数是标准 C 库的常见实现，其功能逻辑具有领域必然性，且双方实现路径存在差异（如目标项目使用 `HASZERO` 宏），不能作为强同源证据。

#### 6. 保守结论
本次审计的总体 AST 相似度为 20.16%，处于低位。高相似度模块（设备驱动、文件系统/第三方库）的共同指纹经核实，均源于双方对 `virtio-drivers`、`lwext4` 等上游开源库的正常引用，属于公共依赖。在操作系统核心板块（进程管理、中断/异常处理），共同 AST 指纹为零，表明两项目在调度、同步、异常处理等关键逻辑上采用了完全异构的底层架构和实现路径。目标项目基于 Rust 语言，其 RAII 锁、`Option`/`Result` 错误处理、对象化内存管理等范式与参考项目的 C 语言手动锁、过程式调用存在根本性差异。综合判定，无证据表明目标项目对参考项目存在核心逻辑的抄袭或翻译性复用，整体风险为低。

------------------------------

