# 🛡️ OS 内核赛自动化代码级架构审计报告

**审计时间**: `2026-07-02 05:15:46`
**参赛作品名称**: `2026-T2026104869910059-XX`
**年份**: `2026`
**学校**: `XX`
**队伍名称**: `T2026104869910059`
**比赛名字**: `OS-Competition`
**比赛子赛道**: `Kernel-Implementation`
**仓库地址**: `https://gitlab.eduxiji.net/educg-group-43501-3132633/T2026104869910059-3729`
**推测开发语言**: `Rust / C (由AST探针自动识别)`
**特征函数量**: `3667`

## ⚔️ 底层代码同源性对比 (AST Homology)
> 本部分由专门的代码审查智能体，对该项目与符合年份条件的历届最高相似度项目进行控制流与底层调用对比。新版报告会先给出确定性的全模块覆盖矩阵，再给出 Agent 的分模块解释，避免只围绕少量抽样函数展开。

### 🏆 Top 3 底层对标: vs `2025-塔特林设计局-华中科技大学`（华中科技大学 / 塔特林设计局）

#### 对方作品信息

**对方年份**: `2025`
**对方学校**: `华中科技大学`
**对方队伍名称**: `塔特林设计局`
**对方比赛名字**: `OS-Competition`
**对方比赛子赛道**: `Kernel-Implementation`
**对方仓库地址**: `https://gitlab.eduxiji.net/educg-group-36002-2710490/T202510487995221-883`

> 双向覆盖率：共同 AST 指纹: 710；唯一 AST 指纹: 目标项目 2712，参考项目 1476；覆盖率: 目标项目 26.18%，参考项目 48.1%

## 全模块对比总览（确定性证据）

> 总体覆盖率：共同 AST 指纹: 710；唯一 AST 指纹: 目标项目 2712，参考项目 1476；覆盖率: 目标项目 26.18%，参考项目 48.1%

### 1. 模块覆盖矩阵
| 模块 | 目标函数数 | 参考函数数 | 共同唯一 AST 指纹 | Target 覆盖 | Base 覆盖 | 状态 |
| :--- | ---: | ---: | ---: | ---: | ---: | :--- |
| 进程管理 | 142 | 78 | 4 | 3.17% | 5.48% | 双方存在且有结构重合 |
| 任务调度/异步运行时 | 225 | 148 | 8 | 3.98% | 6.5% | 双方存在且有结构重合 |
| 内存管理 | 727 | 284 | 52 | 9.19% | 20.88% | 双方存在且有结构重合 |
| 中断/异常处理 | 126 | 21 | 2 | 2.08% | 10.0% | 双方存在且有结构重合 |
| 系统调用 | 168 | 105 | 8 | 7.41% | 9.88% | 双方存在且有结构重合 |
| 文件系统 | 396 | 263 | 41 | 13.4% | 20.81% | 双方存在且有结构重合 |
| 设备驱动 | 462 | 47 | 4 | 0.99% | 12.12% | 双方存在且有结构重合 |
| 网络协议栈 | 0 | 0 | 0 | 0.0% | 0.0% | 双方均未体现 |
| 文件系统/第三方库 | 748 | 704 | 556 | 86.47% | 92.98% | 双方存在且有结构重合 |
| 第三方库 | 2 | 0 | 0 | 0.0% | 0.0% | 仅目标项目体现 |
| 通用模块/其他 | 671 | 136 | 21 | 6.38% | 17.21% | 双方存在且有结构重合 |

### 2. 分模块证据清单

#### 进程管理
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `os-la/src/syscall/process.rs#sys_wait4` | `os/src/syscall/process/wait4.rs#sys_wait4` | 同板块同名 | Err, Ok, child.getpid, child.inner_exclusive_access, current_task, current_task().unwrap ...(+19) | Arc::clone, Err, Ok, Process::remove_from_global_map, child.inner_lock, child.inner_lock().get_locked_sigtable ...(+25) |
| `os/src/task/processor.rs#run_tasks` | `os/src/task/processor.rs#run_tasks` | 同板块同名 | PROCESSOR.exclusive_access, Some, __switch, crate::board::QEMU_EXIT_HANDLE.exit_success, crate::timer::check_timer, crate::timer::has_timer ...(+5) | Some, check_futex_timer, cur_task.inner_lock, drop, get_proc_by_hartid, hart_id ...(+7) |
| `os/src/syscall/process.rs#sys_uname` | `os/src/syscall/process/others.rs#sys_uname` | 同板块同名 | buffers.iter_mut, bytes.len, core::cmp::min, crate::config::utsname_domainname, crate::config::utsname_nodename, current_user_token ...(+5) | Ok, b[0..s.len()].copy_from_slice, cfg!, current_task, current_task().unwrap, memory_set.put_data ...(+5) |
| `os/src/syscall/process.rs#sys_rt_sigprocmask` | `os/src/syscall/signal.rs#sys_rt_sigprocmask` | 同板块同名 | current_process, current_user_token, oldset.is_null, process.inner_exclusive_access, set.is_null, try_translated_ref ...(+1) | Err, Ok, SignalMaskFlag::from_bits, SignalMaskFlag::from_bits(how).ok_or, current_task, current_task().unwrap ...(+6) |

#### 任务调度/异步运行时
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `os-la/src/syscall/fs.rs#sys_pselect6` | `os/src/syscall/fs.rs#sys_pselect6` | 同板块同名 | Ok, Some, current_task, current_task().unwrap, current_user_token, drop ...(+21) | Ok, Some, current_task, current_task().unwrap, drop, event.contains ...(+21) |
| `os-la/src/syscall/fs.rs#sys_copy_file_range` | `os/src/syscall/fs.rs#sys_copy_file_range` | 同板块同名 | Err, Ok, UserBuffer::new, Vec::new, buf.as_mut_slice, buf.as_mut_slice().as_mut_ptr ...(+27) | Err, Ok, Some, UserBuffer::new, Vec::new, buf.as_mut_slice ...(+30) |
| `os/src/syscall/fs.rs#sys_utimensat` | `os/src/syscall/fs.rs#sys_utimensat` | 同板块同名 | String::from, String::new, canonicalize_path, current_process, current_timespec_sec, current_user_token ...(+16) | Err, Ok, Some, String::new, current_task, current_task().unwrap ...(+13) |
| `os/src/syscall/fs.rs#sys_chdir` | `os/src/syscall/fs.rs#sys_chdir` | 同板块同名 | Ext4File::new, canonicalize_path, core::str::from_utf8, core::str::from_utf8(&n[..len]).ok, core::str::from_utf8(&n[..len]).ok().map, current_process ...(+21) | Err, Ok, current_task, current_task().unwrap, debug!, get_abs_path ...(+13) |

#### 内存管理
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `os/src/syscall/mod.rs#syscall` | `os/src/syscall/mod.rs#syscall` | 同板块同名 | buf.is_empty, buf.iter_mut, buf.iter_mut().skip, crate::mm::try_translated_byte_buffer, crate::task::current_user_token, error! ...(+101) | Err, Ok, Syscall::from, cfg!, error!, log::debug! ...(+115) |
| `os-la/src/utils/error.rs#str` | `os/src/utils/error.rs#str` | 同板块同名同结构 | panic! | panic! |
| `os-la/src/mm/memory_set.rs#from_existed_user` | `os/src/mm/memory_set/inner.rs#from_existed_user` | 同板块同名 | GROUP_SHARE.lock, GROUP_SHARE.lock().add_frame, MapArea::from_another, Self::new_bare, a.vaddr_range.range, area.data_frames.clone ...(+32) | GROUP_SHARE.lock, GROUP_SHARE.lock().add_area, MapArea::from_another, Self::new_from_kernel, area.data_frames.clone, area.data_frames.iter ...(+19) |
| `os-la/src/mm/memory_set.rs#from_elf` | `os/src/mm/memory_set/inner.rs#from_elf` | 同板块同名 | ((ph.virtual_addr() + ph.mem_size()) as usize).into, (ph.virtual_addr() as usize).into, Aux::new, MapArea::new, Self::new_bare, Some ...(+31) | Aux::new, MapArea::new, Self::new_from_kernel, Vec::new, VirtAddr, assert_eq! ...(+15) |

#### 中断/异常处理
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `os-la/src/signal/sigflags.rs#default_op` | `os/src/signal/signal.rs#default_op` | 同板块同名 | continue_signals.contains, dump_signals.contains, ignore_signals.contains, stop_signals.contains, terminate_signals.contains | continue_signals.contains, dump_signals.contains, ignore_signals.contains, realtime_signals.contains, stop_signals.contains, terminate_signals.contains ...(+1) |
| `os/src/trap/mod.rs#trap_from_kernel` | `os/src/trap/mod.rs#trap_from_kernel` | 同板块同名 | panic!, println! | Trap::Interrupt, VirtAddr::from, VirtAddr::from(stval).floor, backtrace, error!, get_trap_cause ...(+1) |
| `os/src/trap/mod.rs#set_kernel_trap_entry` | `os/src/arch/riscv64/qemu/trap_interface.rs#set_kernel_trap_entry` | 同板块同名 | stvec::write | stvec::write |
| `os/src/trap/mod.rs#set_user_trap_entry` | `os/src/arch/riscv64/qemu/trap_interface.rs#set_user_trap_entry` | 同板块同名 | stvec::write | stvec::write |

#### 系统调用
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `os-la/src/syscall/other.rs#sys_getrandom` | `user/src/syscall.rs#sys_getrandom` | 同板块同名 | Err, Ok, UserBuffer::new, Vec::new, all_bytes.extend_from_slice, buf_ptr.is_null ...(+7) | buf.as_mut_ptr, syscall |
| `os-la/src/syscall/mm.rs#sys_shmget` | `os/src/syscall/memory.rs#sys_shmget` | 同板块同名 | Err, Ok, ShmFlags::from_bits_truncate, flags.contains, shm_create, shm_find ...(+1) | Err, Ok, ShmFlags::from_bits, ShmFlags::from_bits(shmflag & !0x1ff).unwrap, flags.contains, shm_create ...(+1) |
| `os/src/syscall/socket.rs#sys_getsockname` | `os/src/syscall/net.rs#sys_getsockname` | 同板块同名 | SOCKET_TABLE.exclusive_access, Some, addr.clone, local.len, local.len().min, read_socklen ...(+7) | Ok, debug! |
| `os-la/src/syscall/other.rs#sys_clock_nanosleep` | `user/src/syscall.rs#sys_clock_nanosleep` | 同板块同名 | Ok, current_user_token, get_data, get_time_ms, suspend_current_and_run_next, trace! | rem.as_mut_ptr, req.as_ptr, syscall |

#### 文件系统
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `os-la/src/mm/memory_set.rs#load_dl_interp_if_needed` | `os/src/mm/memory_set/inner.rs#load_dl_interp_if_needed` | 同板块同名 | "/glibc/lib/ld-linux-loongarch-lp64d.so.1".to_string, "/glibc/lib/libc.so.6".to_string, "/musl/lib/libc.so".to_string, "ld-musl-riscv64.so.1".to_string, Arc::new, DL_INTERP_OFFSET.into ...(+39) | DL_INTERP_OFFSET.into, Some, String::from_utf8, String::from_utf8(section.raw_data(&elf).to_vec()).unwrap, debug!, elf.find_section_by_name ...(+24) |
| `user/basic/user/lib/syscall.c#fstat` | `os/src/fs/files/devfs.rs#fstat` | 同板块同名 | makedev, syscall | Kstat::default, StMode::FCHR.bits, get_devno |
| `os-la/src/fs/inode.rs#lseek` | `os/src/fs/files/os_file.rs#lseek` | 同板块同名 | Err, Ok, debug!, self.inner.lock, self.inode.size, trace! | Err, Ok, self.inner.lock, self.inode.size |
| `os-la/src/drivers/disk.rs#read_one` | `os/src/drivers/disk.rs#read_one` | 同板块同名同结构 | Ok, buf.len, buf.len().min, buf[..count].copy_from_slice, info!, self.dev
                .read_block ...(+1) | Ok, buf.len, buf.len().min, buf[..count].copy_from_slice, info!, self.dev
                .read_block ...(+1) |

#### 设备驱动
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `deps/virtio-drivers/src/blk.rs#read_block` | `os/src/drivers/ramdisk/mod.rs#read_block` | 同板块同名 | BlkResp::default, Err, Ok, assert_eq!, req.as_buf, resp.as_buf_mut ...(+5) | Ok, assert!, buf.copy_from_slice, core::slice::from_raw_parts |
| `deps/virtio-drivers/src/blk.rs#write_block` | `os/src/drivers/cache.rs#write_block` | 同板块同名 | BlkResp::default, Err, Ok, assert_eq!, req.as_buf, resp.as_buf_mut ...(+5) | Ok, buf.len, buf.to_vec, buffer.copy_from_slice, panic!, self.block_size ...(+2) |
| `os-la/src/drivers/virtio_blk.rs#read_block` | `os/src/drivers/cache.rs#read_block` | 同板块同名 | self.0
            .exclusive_access, self.0
            .exclusive_access()
            .read_blocks, self.0
            .exclusive_access()
            .read_blocks(block_id as _, buf)
            .map_err | Ok, buf.copy_from_slice, self.buffers.get, self.inner.lock, self.inner.lock().read_block |
| `os-la/src/drivers/virtio_blk.rs#write_block` | `os/src/drivers/ramdisk/mod.rs#write_block` | 同板块同名 | self.0
            .exclusive_access, self.0
            .exclusive_access()
            .write_blocks, self.0
            .exclusive_access()
            .write_blocks(block_id as _, buf)
            .map_err | Ok, assert!, block.copy_from_slice, core::slice::from_raw_parts_mut |

#### 文件系统/第三方库
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `lwext4_rust/c/lwext4/src/ext4_balloc.c#ext4_balloc_alloc_block` | `lwext4_rust/c/lwext4/src/ext4_balloc.c#ext4_balloc_alloc_block` | 同板块同名同结构 | ext4_balloc_get_bgid_of_block, ext4_balloc_get_block_of_bgid, ext4_balloc_set_bitmap_csum, ext4_balloc_verify_bitmap_csum, ext4_bg_get_block_bitmap, ext4_bg_get_free_blocks_count ...(+19) | ext4_balloc_get_bgid_of_block, ext4_balloc_get_block_of_bgid, ext4_balloc_set_bitmap_csum, ext4_balloc_verify_bitmap_csum, ext4_bg_get_block_bitmap, ext4_bg_get_free_blocks_count ...(+19) |
| `lwext4_rust/c/lwext4/src/ext4_journal.c#jbd_journal_prepare` | `lwext4_rust/c/lwext4/src/ext4_journal.c#jbd_journal_prepare` | 同板块同名同结构 | EXT4_BLOCK_ZERO, RB_FIND, TAILQ_FOREACH_REVERSE_SAFE, TAILQ_FOREACH_SAFE, TAILQ_REMOVE, ext4_bcache_clear_dirty ...(+17) | EXT4_BLOCK_ZERO, RB_FIND, TAILQ_FOREACH_REVERSE_SAFE, TAILQ_FOREACH_SAFE, TAILQ_REMOVE, ext4_bcache_clear_dirty ...(+17) |
| `lwext4_rust/c/lwext4/src/ext4.c#ext4_dir_rm` | `lwext4_rust/c/lwext4/src/ext4.c#ext4_dir_rm` | 同板块同名同结构 | EXT4_MP_LOCK, EXT4_MP_UNLOCK, ext4_block_cache_write_back, ext4_dir_en_get_inode, ext4_dir_iterator_fini, ext4_dir_iterator_init ...(+19) | EXT4_MP_LOCK, EXT4_MP_UNLOCK, ext4_block_cache_write_back, ext4_dir_en_get_inode, ext4_dir_iterator_fini, ext4_dir_iterator_init ...(+19) |
| `lwext4_rust/c/lwext4/src/ext4_dir_idx.c#ext4_dir_dx_split_index` | `lwext4_rust/c/lwext4/src/ext4_dir_idx.c#ext4_dir_dx_split_index` | 同板块同名同结构 | ext4_block_set, ext4_dir_dx_climit_get_count, ext4_dir_dx_climit_get_limit, ext4_dir_dx_climit_set_count, ext4_dir_dx_climit_set_limit, ext4_dir_dx_entry_get_hash ...(+10) | ext4_block_set, ext4_dir_dx_climit_get_count, ext4_dir_dx_climit_get_limit, ext4_dir_dx_climit_set_count, ext4_dir_dx_climit_set_limit, ext4_dir_dx_entry_get_hash ...(+10) |

#### 第三方库
仅目标项目中观察到该模块代表函数，参考项目没有同模块可对齐证据：
- 目标项目：`os-la-patch/virtio-drivers/src/transport/mmio.rs#vendor_id`，调用摘要：volread!
- 目标项目：`deps/virtio-drivers/src/header.rs#vendor_id`，调用摘要：self.vendor_id.read

#### 通用模块/其他
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `user/src/lib.rs#_start` | `user/src/lib.rs#_start` | 同板块同名 | ((argv + i * core::mem::size_of::<usize>()) as *const usize).read_volatile, ((str_start + *i) as *const u8).read_volatile, (0usize..)
            .find, (0usize..)
            .find(|i| unsafe { ((str_start + *i) as *const u8).read_volatile() == 0 })
            .unwrap, HEAP.lock, HEAP.lock()
            .init ...(+10) | HEAP.lock, HEAP.lock()
            .init, HEAP_SPACE.as_ptr, exit, main |
| `os-la/src/timer.rs#new_from_ms` | `os/src/timer/mod.rs#new_from_ms` | 同板块同名同结构 | Timespec::new | TimeVal::new |
| `os-la/src/utils/string.rs#get_abs_path` | `os/src/utils/string.rs#get_abs_path` | 同板块同名 | Vec::with_capacity, debug!, is_abs_path, path.to_string, path2abs, path2vec | Vec::with_capacity, is_abs_path, path.to_string, path2abs, path2vec |
| `os-la/src/utils/string.rs#path2abs` | `os/src/utils/string.rs#path2abs` | 同板块同名同结构 | String::from, abs_path.push_str, cwdv.join, cwdv.pop, cwdv.push, pathv.iter | String::from, abs_path.push_str, cwdv.join, cwdv.pop, cwdv.push, pathv.iter |


好的，审计官。基于提供的AST相似度数据、模块画像、控辩双方证据及辩护意见，现出具最终定性仲裁报告。

### 📝 最终同源性定性仲裁报告

#### 1. 宏观判定
- **定性结论**: **独立开发，底层架构存在显著差异**
  本案目标项目 `[2026-T2026104869910059-XX]` 与参考项目 `[2025-塔特林设计局-华中科技大学]` 的客观 AST 相似度仅为 **20.41%**。目标项目覆盖率 26.18%，参考项目覆盖率 48.1%，表明双方代码库主体均为独立实现。控方提出的“进程管理板块抄袭”指控，其证据多为 Rust 语言惯用法、POSIX 标准接口约束及操作系统领域通用设计模式，未能穿透至足以证明逻辑复用的底层架构层。辩方关于处理器抽象、锁机制、上下文切换原语及多架构支持等方面存在实质性差异的辩护成立。

#### 2. 全模块覆盖矩阵解读
| 模块 | 双方存在 | 共同指纹 | 目标覆盖 | 参考覆盖 | 证据强度 | 解读 |
| :--- | :---: | :---: | :---: | :---: | :---: | :--- |
| **进程管理** | 是 | 4 | 3.17% | 5.48% | **弱** | 重合度极低。同名源于 POSIX 规范，相似调用流（如 `current_task`）是内核开发的通用范式。控方聚焦的 `Option::take` 等为 Rust 语言标准惯用法。 |
| **任务调度/异步运行时** | 是 | 8 | 3.98% | 6.5% | **弱** | 覆盖率极低。调度器核心逻辑（如 `run_tasks`）的外部调用图谱差异巨大，目标项目使用 `__switch` 和 `PROCESSOR` 大锁，参考项目使用 `switch` 和 `get_proc_by_hartid` 多核查找，架构截然不同。 |
| **内存管理** | 是 | 52 | 9.19% | 20.88% | **中** | 存在一定结构重合，如 `from_elf`、`from_existed_user`。这主要源于对 ELF 文件格式解析和内存区域映射的通用算法，但具体实现（如共享内存管理 `GROUP_SHARE` 的用法）仍有差异。 |
| **中断/异常处理** | 是 | 2 | 2.08% | 10.0% | **弱** | 覆盖率极低。`set_kernel_trap_entry` 等函数直接写入 `stvec` 寄存器，这是 RISC-V 架构的硬件规定动作，不具备原创性。 |
| **系统调用** | 是 | 8 | 7.41% | 9.88% | **弱** | 重合度低。系统调用分发（`syscall` 函数）的 `match` 结构是 Rust 处理此类问题的标准模式。同名函数由 Linux ABI 强制规定。 |
| **文件系统** | 是 | 41 | 13.4% | 20.81% | **中** | 存在结构重合，但主要集中于对 `lwext4` 库的封装调用和通用 inode 操作。核心文件系统逻辑（如路径解析、挂载）的外部依赖图谱差异明显。 |
| **设备驱动** | 是 | 4 | 0.99% | 12.12% | **弱** | 覆盖率极低。重合点 `read_block`/`write_block` 是对 `virtio-drivers` 公共库的封装，属于第三方库依赖，非原创代码复用。 |
| **网络协议栈** | 否 | 0 | 0.0% | 0.0% | **未形成** | 双方均未实现该模块。 |
| **文件系统/第三方库** | 是 | 556 | 86.47% | 92.98% | **强（第三方库）** | 覆盖率极高，但证据明确指向双方共同依赖的 `lwext4_rust` 上游开源库。这是公共依赖，**不能作为双方互相抄袭的证据**，应在分析中予以剔除。 |
| **第三方库** | 仅目标 | 0 | 0.0% | 0.0% | **未形成** | 目标项目独有的 `virtio-drivers` 等依赖，参考项目未使用。 |
| **通用模块/其他** | 是 | 21 | 6.38% | 17.21% | **弱** | 重合度低。相似点如 `path2abs` 是字符串处理的通用算法，`_start` 是用户态程序的通用入口模板。 |

#### 3. 底层 AST 关键证据指纹表
| 所属板块 | 目标项目路径 | 参考项目路径 | 证据类型 | 技术说明 |
| :--- | :--- | :--- | :--- | :--- |
| **第三方库/公共依赖** | `lwext4_rust/c/lwext4/src/ext4_balloc.c#ext4_balloc_alloc_block` | `lwext4_rust/c/lwext4/src/ext4_balloc.c#ext4_balloc_alloc_block` | 同源文件/同函数 | 双方共同依赖的上游 C 语言库 `lwext4`，AST 指纹完全一致，是公共代码，非抄袭。 |
| **第三方库/公共依赖** | `lwext4_rust/c/lwext4/src/ext4_journal.c#jbd_journal_prepare` | `lwext4_rust/c/lwext4/src/ext4_journal.c#jbd_journal_prepare` | 同源文件/同函数 | 同上，`lwext4` 公共库代码。 |
| **第三方库/公共依赖** | `lwext4_rust/c/lwext4/src/ext4.c#ext4_dir_rm` | `lwext4_rust/c/lwext4/src/ext4.c#ext4_dir_rm` | 同源文件/同函数 | 同上，`lwext4` 公共库代码。 |
| **进程管理** | `os-la/src/task/processor.rs#current` | `os/src/task/processor.rs#current` | 语言惯用法 | `self.current.as_ref().map(...)` 是 Rust `Option` 类型的标准操作链，任何内核调度器都可能自然写出。 |
| **进程管理** | `os-la/src/task/processor.rs#take_current` | `os/src/task/processor.rs#take_current` | 语言惯用法 | `self.current.take()` 是 Rust `Option` 类型转移所有权的唯一标准方法。 |
| **进程管理** | `os/src/task/processor.rs#current_trap_cx` | `os/src/task/processor.rs#current_trap_cx` | 领域通用逻辑 | `current_task -> unwrap -> 获取锁 -> 访问trap_cx` 是获取当前陷阱上下文的强制逻辑链路，但目标用 `get_trap_cx()` 方法，参考用 `trap_cx` 字段，实现细节不同。 |
| **内存管理** | `os-la/src/mm/memory_set.rs#from_elf` | `os/src/mm/memory_set/inner.rs#from_elf` | 领域通用逻辑 | 解析 ELF 文件并建立内存映射的流程相似，源于 ELF 标准的强制性，但具体 API 调用（如 `MapArea::new` 的参数）存在差异。 |
| **文件系统** | `os-la/src/drivers/disk.rs#read_one` | `os/src/drivers/disk.rs#read_one` | 领域通用逻辑 | 从块设备读取数据的标准流程：计算长度、`copy_from_slice`、调用底层 `read_block`。这是块设备驱动的通用模式。 |

#### 4. 核心代码调用流对比剖析
* **【进程管理/调度器核心】**
* **底层技术析理**: 双方均实现了 `run_tasks` 函数作为调度入口，但体现了完全不同的处理器架构抽象。目标项目采用单核大锁模型，通过 `PROCESSOR.exclusive_access()` 获取全局处理器控制权，并使用 `__switch` 进行上下文切换。参考项目则采用多核架构，通过 `get_proc_by_hartid(hart_id())` 为每个硬件线程获取独立的处理器结构体，并使用 `switch` 进行切换。这种在并发模型和底层原语上的根本性差异，是独立架构设计的有力证明。
* **代码溯源链接**:
  - 目标项目: `os/src/task/processor.rs#run_tasks`
  - 参考项目: `os/src/task/processor.rs#run_tasks`

* **【内存管理/地址空间初始化】**
* **底层技术析理**: 双方都有 `from_elf` 函数来根据 ELF 文件初始化进程地址空间。虽然解析 ELF 头的流程（遍历 Program Header，映射 `MapArea`）是标准操作，但目标项目在构建 `MapArea` 时直接使用了 `ph.virtual_addr()` 和 `ph.mem_size()`，而参考项目则显式引入了 `VirtAddr` 类型并进行了断言检查。这反映了在内存地址抽象和安全性处理上的不同设计风格。
* **代码溯源链接**:
  - 目标项目: `os-la/src/mm/memory_set.rs#from_elf`
  - 参考项目: `os/src/mm/memory_set/inner.rs#from_elf`

#### 5. 单方模块与降权项
*   **单方模块/函数**:
    *   **目标项目独有**: `user/src/bin/initproc.rs` 中的 `run_busybox_group`、`run_iozone_group` 等复杂测试框架函数，以及 `os-la-patch/polyhal/src/components/timer/x86_64.rs` 中的 x86_64 架构定时器驱动，在参考项目中无对应实现，是目标项目独立开发的直接证据。
    *   **参考项目独有**: `os/src/task/futex.rs` 中的 `futex_wait_bitset` 等 futex 相关实现，在目标项目中无对应证据。
*   **降权项**:
    *   **POSIX 系统调用同名**: `sys_clone`、`sys_exit`、`sys_wait4` 等所有以 `sys_` 开头的函数名，均由 Linux 系统调用接口规范强制定义，不能作为抄袭证据。
    *   **第三方公共库**: `lwext4_rust` 和 `virtio-drivers` 目录下的所有代码均属于上游开源项目，其高 AST 覆盖率是公共依赖所致，必须在同源性判定中予以剔除。
    *   **Rust 语言惯用法**: `Option::take`、`Option::as_ref`、`match` 分支、`Result::Err/Ok` 等是 Rust 编程的标准模式，不具备原创性。
    *   **硬件规定操作**: `stvec::write` 等直接操作特权寄存器的代码，是 RISC-V 架构的硬件编程规范，无创造性可言。

#### 6. 保守结论
综合全模块证据，目标项目与参考项目的整体 AST 相似度处于低位（20.41%）。扣除 `lwext4` 等第三方公共库后，双方在核心模块（进程管理、内存管理、任务调度）的实质性代码重合度极低。控方提出的“进程管理板块抄袭”指控，其证据均可被 Rust 语言惯用法、POSIX 标准接口约束及操作系统领域通用设计模式所解释。辩方关于双方在处理器抽象（单核大锁 vs. 多核按核查找）、上下文切换原语（`__switch` vs. `switch`）、锁机制命名及多架构支持（LoongArch/x86_64 vs. RISC-V）等方面存在根本性架构差异的论证成立。因此，本案无证据表明目标项目对参考项目构成代码抄袭，应认定为独立开发。

------------------------------

