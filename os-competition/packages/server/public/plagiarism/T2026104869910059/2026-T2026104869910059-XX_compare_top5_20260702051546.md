# 🛡️ OS 内核赛自动化代码级架构审计报告

**审计时间**: `2026-07-02 05:15:46`
**参赛作品名称**: `2026-T2026104869910059-XX`
**年份**: `2026`
**学校**: `XX`
**队伍名称**: `T2026104869910059`
**比赛名字**: `OS-Competition`
**比赛子赛道**: `Kernel-Implementation`
**仓库地址**: `https://gitlab.eduxiji.net/educg-group-43501-3132633/T2026104869910059-3729`
**推测开发语言**: `Rust / C (由AST探针自动识别)`
**特征函数量**: `3667`

## ⚔️ 底层代码同源性对比 (AST Homology)
> 本部分由专门的代码审查智能体，对该项目与符合年份条件的历届最高相似度项目进行控制流与底层调用对比。新版报告会先给出确定性的全模块覆盖矩阵，再给出 Agent 的分模块解释，避免只围绕少量抽样函数展开。

### 🏆 Top 5 底层对标: vs `2025-KernalCraft-中山大学`（中山大学 / KernalCraft）

#### 对方作品信息

**对方年份**: `2025`
**对方学校**: `中山大学`
**对方队伍名称**: `KernalCraft`
**对方比赛名字**: `OS-Competition`
**对方比赛子赛道**: `Kernel-Implementation`
**对方仓库地址**: `https://gitlab.eduxiji.net/educg-group-36002-2710490/T202510558995240-2536`

> 双向覆盖率：共同 AST 指纹: 667；唯一 AST 指纹: 目标项目 2712，参考项目 1853；覆盖率: 目标项目 24.59%，参考项目 36.0%

## 全模块对比总览（确定性证据）

> 总体覆盖率：共同 AST 指纹: 667；唯一 AST 指纹: 目标项目 2712，参考项目 1853；覆盖率: 目标项目 24.59%，参考项目 36.0%

### 1. 模块覆盖矩阵
| 模块 | 目标函数数 | 参考函数数 | 共同唯一 AST 指纹 | Target 覆盖 | Base 覆盖 | 状态 |
| :--- | ---: | ---: | ---: | ---: | ---: | :--- |
| 进程管理 | 142 | 71 | 16 | 12.7% | 25.4% | 双方存在且有结构重合 |
| 任务调度/异步运行时 | 225 | 115 | 17 | 8.46% | 15.18% | 双方存在且有结构重合 |
| 内存管理 | 727 | 741 | 236 | 41.7% | 52.68% | 双方存在且有结构重合 |
| 中断/异常处理 | 126 | 128 | 22 | 22.92% | 22.0% | 双方存在且有结构重合 |
| 系统调用 | 168 | 21 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |
| 文件系统 | 396 | 147 | 32 | 10.46% | 25.0% | 双方存在且有结构重合 |
| 设备驱动 | 462 | 555 | 291 | 71.85% | 62.18% | 双方存在且有结构重合 |
| 网络协议栈 | 0 | 53 | 0 | 0.0% | 0.0% | 仅参考项目体现 |
| 文件系统/第三方库 | 748 | 340 | 2 | 0.31% | 0.76% | 双方存在且有结构重合 |
| 第三方库 | 2 | 2 | 1 | 50.0% | 50.0% | 双方存在且有结构重合 |
| 通用模块/其他 | 671 | 669 | 71 | 21.58% | 26.79% | 双方存在且有结构重合 |

### 2. 分模块证据清单

#### 进程管理
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `os-la/src/syscall/process.rs#sys_exec` | `os/src/syscall/process.rs#sys_exec` | 同板块同名 | "LD_LIBRARY_PATH=/:/lib:/usr/lib:/lib64:/glibc/lib:/musl/lib:".to_string, "PATH=/:/bin:".to_string, Err, Ok, String::from, Vec::<String>::new ...(+33) | BUSYBOX_BIN.to_vec, String::from, Vec::new, all_data.as_slice, app_inode.read_all, args.add ...(+15) |
| `os-la/src/syscall/process.rs#sys_wait4` | `os/src/syscall/process.rs#sys_wait4` | 同板块同名 | Err, Ok, child.getpid, child.inner_exclusive_access, current_task, current_task().unwrap ...(+19) | current_process, current_process().getpid, log::trace!, sys_waitpid, sys_yield |
| `os/src/task/processor.rs#run_tasks` | `os/src/task/processor.rs#run_tasks` | 同板块同名 | PROCESSOR.exclusive_access, Some, __switch, crate::board::QEMU_EXIT_HANDLE.exit_success, crate::timer::check_timer, crate::timer::has_timer ...(+5) | PROCESSOR.exclusive_access, Some, __switch, drop, fetch_task, println! ...(+2) |
| `os/src/syscall/process.rs#sys_uname` | `os/src/syscall/process.rs#sys_uname` | 同板块同名 | buffers.iter_mut, bytes.len, core::cmp::min, crate::config::utsname_domainname, crate::config::utsname_nodename, current_user_token ...(+5) | "#1 PREEMPT Thu May 29 00:00:00 UTC".as_bytes, "0.1.0".as_bytes, "KernelCraft".as_bytes, "None".as_bytes, "localhost".as_bytes, "riscv64".as_bytes ...(+14) |

#### 任务调度/异步运行时
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `os-la/src/syscall/mod.rs#syscall` | `os/src/syscall/mod.rs#syscall` | 同板块同名 | Err, Ok, debug!, mask_ptr.add, mask_ptr.is_null, param_ptr.is_null ...(+84) | MmapFlags::from_bits_truncate, exit_current_and_run_next, log::error!, sys_accept, sys_brk, sys_chdir ...(+82) |
| `os/src/syscall/fs.rs#sys_utimensat` | `os/src/syscall/fs.rs#sys_utimensat` | 同板块同名 | String::from, String::new, canonicalize_path, current_process, current_timespec_sec, current_user_token ...(+16) | (*atime_ptr).to_ms, (*mtime_ptr).to_ms, Some, TimeSpec::now, TimeSpec::now().to_ms, current_process ...(+15) |
| `os/src/syscall/fs.rs#sys_chdir` | `os/src/syscall/fs.rs#sys_chdir` | 同板块同名 | Ext4File::new, canonicalize_path, core::str::from_utf8, core::str::from_utf8(&n[..len]).ok, core::str::from_utf8(&n[..len]).ok().map, current_process ...(+21) | current_process, current_user_token, drop, get_dirinode, get_dirinode_at, path_return.as_str ...(+3) |
| `os-la/src/syscall/fs.rs#sys_ppoll` | `os/src/syscall/ppoll.rs#sys_ppoll` | 同板块同名 | Err, Ok, PollEvents::empty, Vec::new, current_task, current_task().unwrap ...(+14) | Box::from_raw, Box::into_raw, Box::new, SignalFlags::empty, curr_process.inner_exclusive_access, current_process ...(+16) |

#### 内存管理
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `os-la-patch/virtio-drivers/src/device/socket/connectionmanager.rs#send_recv` | `virtio-drivers/src/device/socket/connectionmanager.rs#send_recv` | 同板块同名 | "Hello from guest".as_bytes, (hello_from_guest.len() as u32).into, (hello_from_host.len() as u32).into, 0.into, 50.into, Arc::new ...(+44) | "Hello from guest".as_bytes, (hello_from_guest.len() as u32).into, (hello_from_host.len() as u32).into, 0.into, 50.into, Arc::new ...(+43) |
| `os-la/src/task/task.rs#exec` | `os/src/task/process.rs#exec` | 同板块同名 | Arc::new, Aux::new, MemorySet::new, MemorySetInner::from_elf, String::from, TrapFrame::new ...(+34) | KERNEL_SPACE.exclusive_access, KERNEL_SPACE.exclusive_access().token, MemorySet::from_elf, TrapContextImpl::app_init_ctx, assert_eq!, core::mem::size_of::<usize> ...(+12) |
| `os-la-patch/virtio-drivers/src/device/socket/connectionmanager.rs#incoming_connection` | `virtio-drivers/src/device/socket/connectionmanager.rs#incoming_connection` | 同板块同名 | 0.into, 50.into, Arc::new, Default::default, Mutex::new, NonNull::from ...(+25) | 0.into, 50.into, Arc::new, Mutex::new, ReadOnly::new, SocketType::Stream.into ...(+24) |
| `os-la/src/mm/memory_set.rs#from_elf` | `os/src/mm/memory_set.rs#from_elf` | 同板块同名 | ((ph.virtual_addr() + ph.mem_size()) as usize).into, (ph.virtual_addr() as usize).into, Aux::new, MapArea::new, Self::new_bare, Some ...(+31) | AuxHeader::new, MapArea::new, MmapManager::new, Self::new_bare, Some, VirtAddr ...(+13) |

#### 中断/异常处理
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `os-la-patch/polyhal-trap/src/trapframe/x86_64.rs#index` | `os/src/hal/irq/riscv64.rs#index` | 同板块同名 | unimplemented! | 内部闭环逻辑 |
| `deps/riscv/src/interrupt.rs#free` | `riscv/src/interrupt.rs#free` | 同板块同名同结构 | CriticalSection::new, disable, enable, f, mstatus.mie, mstatus::read | CriticalSection::new, disable, enable, f, mstatus.mie, mstatus::read |
| `deps/riscv/src/register/mcause.rs#is_interrupt` | `riscv/src/register/mcause.rs#is_interrupt` | 同板块同名同结构 | 内部闭环逻辑 | 内部闭环逻辑 |
| `deps/riscv/src/register/mtvec.rs#trap_mode` | `riscv/src/register/mtvec.rs#trap_mode` | 同板块同名同结构 | Some | Some |

#### 系统调用
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `os-la/src/syscall/other.rs#sys_syslog` | `os/src/syscall/info.rs#sys_syslog` | 同板块同名 | Ok, warn! | 内部闭环逻辑 |
| `os/src/syscall/socket.rs#sys_recvfrom` | `os/src/syscall/net.rs#sys_socket` | 同模块上下文对齐/差异观察 | SOCKET_TABLE.exclusive_access, packet.addr.len, packet.addr.len().min, packet.data.len, packet.data.len().min, read_socklen ...(+6) | create_tcp_socket, create_udp_socket |
| `os/src/syscall/socket.rs#sys_recvmsg` | `os/src/syscall/input.rs#sys_event_get` | 同模块上下文对齐/差异观察 | SOCKET_TABLE.exclusive_access, current_user_token, packet.addr.len, packet.addr.len().min, sock.recvq.is_empty, sock.recvq.pop_front ...(+6) | KEYBOARD_DEVICE.clone, MOUSE_DEVICE.clone, kb.is_empty, kb.read_event, mouse.is_empty, mouse.read_event |
| `user/src/bin/ls.rs#ls_dir` | `os/src/syscall/info.rs#fmt` | 同模块上下文对齐/差异观察 | buf.as_mut_ptr, buf.len, close, core::str::from_utf8, core::str::from_utf8(&buf[name_start..name_start + name_len]).unwrap_or, get_u16_le ...(+4) | writeln! |

#### 文件系统
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `os-la/src/mm/memory_set.rs#load_dl_interp_if_needed` | `os/src/mm/memory_set.rs#load_dl_interp_if_needed` | 同板块同名 | "/glibc/lib/ld-linux-loongarch-lp64d.so.1".to_string, "/glibc/lib/libc.so.6".to_string, "/musl/lib/libc.so".to_string, "ld-musl-riscv64.so.1".to_string, Arc::new, DL_INTERP_OFFSET.into ...(+39) | "/lib/libc.so".to_string, "/libc.so".to_string, EXT4.generic_open, EXT4.generic_open(&interp, &mut 2, false, InodeFileType::S_IFREG.bits(), &mut 0).ok, Inode::new, InodeFileType::S_IFREG.bits ...(+28) |
| `os-la-patch/virtio-drivers/src/device/blk.rs#device_id` | `virtio-drivers/src/device/blk.rs#device_id` | 同板块同名 | Arc::new, BlkFeature::RING_INDIRECT_DESC.bits, Default::default, Mutex::new, NonNull::from, QUEUE_SIZE.into ...(+14) | Arc::new, BlkFeature::RING_INDIRECT_DESC.bits, Mutex::new, QUEUE_SIZE.into, ReadOnly::new, State::new ...(+13) |
| `os-la-patch/virtio-drivers/src/device/blk.rs#flush` | `virtio-drivers/src/device/blk.rs#flush` | 同板块同名 | (BlkFeature::RING_INDIRECT_DESC | BlkFeature::FLUSH).bits, Arc::new, Default::default, Mutex::new, NonNull::from, QUEUE_SIZE.into ...(+13) | (BlkFeature::RING_INDIRECT_DESC | BlkFeature::FLUSH).bits, Arc::new, Mutex::new, QUEUE_SIZE.into, ReadOnly::new, State::new ...(+12) |
| `deps/riscv/descriptor/generator.rs#generate` | `riscv/descriptor/generator.rs#generate` | 同板块同名同结构 | String::new, bf.generate_enum, format!, self.bfs.iter, write!, write!(&mut bit_sets, "{}", bf.generate_bitops()).unwrap ...(+2) | String::new, bf.generate_enum, format!, self.bfs.iter, write!, write!(&mut bit_sets, "{}", bf.generate_bitops()).unwrap ...(+2) |

#### 设备驱动
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `os-la-patch/virtio-drivers/src/queue.rs#fake_read_write_queue` | `virtio-drivers/src/queue.rs#fake_read_write_queue` | 同板块同名同结构 | (*available_ring).idx.load, (*used_ring).idx.fetch_add, (*used_ring).idx.load, Vec::new, assert!, assert_eq! ...(+18) | (*available_ring).idx.load, (*used_ring).idx.fetch_add, (*used_ring).idx.load, Vec::new, assert!, assert_eq! ...(+18) |
| `os-la-patch/virtio-drivers/src/device/sound/fake.rs#handle_control_request` | `virtio-drivers/src/device/sound/fake.rs#handle_control_request` | 同板块同名同结构 | CommandCode::SOk.into, Some, Vec::new, VirtIOSndHdr {
                            command_code: CommandCode::SOk.into(),
                        }
                        .as_bytes, VirtIOSndHdr::read_from_prefix, VirtIOSndHdr::read_from_prefix(&request)
                .expect ...(+16) | CommandCode::SOk.into, Some, Vec::new, VirtIOSndHdr {
                            command_code: CommandCode::SOk.into(),
                        }
                        .as_bytes, VirtIOSndHdr::read_from_prefix, VirtIOSndHdr::read_from_prefix(&request)
                .expect ...(+16) |
| `os-la-patch/virtio-drivers/src/device/sound.rs#play` | `virtio-drivers/src/device/sound.rs#play` | 同板块同名同结构 | FakeSoundDevice::new, PcmFeatures::empty, VirtIOSound::<FakeHal, FakeTransport<VirtIOSoundConfig>>::new, VirtIOSound::<FakeHal, FakeTransport<VirtIOSoundConfig>>::new(transport).unwrap, assert_eq!, expected_sound.extend ...(+22) | FakeSoundDevice::new, PcmFeatures::empty, VirtIOSound::<FakeHal, FakeTransport<VirtIOSoundConfig>>::new, VirtIOSound::<FakeHal, FakeTransport<VirtIOSoundConfig>>::new(transport).unwrap, assert_eq!, expected_sound.extend ...(+22) |
| `os-la-patch/virtio-drivers/src/device/sound.rs#pcm_xfer` | `virtio-drivers/src/device/sound.rs#pcm_xfer` | 同板块同名同结构 | CommandCode::SOk.into, Default::default, Err, Ok, Some, array::from_fn ...(+17) | CommandCode::SOk.into, Default::default, Err, Ok, Some, array::from_fn ...(+17) |

#### 网络协议栈
仅参考项目中观察到该模块代表函数，目标项目没有同模块可对齐证据：
- 参考项目：`lose-net-stack/src/packets/udp.rs#build_data`，调用摘要：((self.data_len + UDP_LEN + IP_LEN) as u16).to_be, ((self.data_len + UDP_LEN) as u16).to_be, ETH_RTYPE_IP.to_be, IP_PROTOCAL_UDP.to_be, UnsafeRefIter::new, check_sum ...(+17)
- 参考项目：`lose-net-stack/src/packets/arp.rs#build_data`，调用摘要：ARP_HRD_ETHER.to_be, ETH_RTYPE_ARP.to_be, ETH_RTYPE_IP.to_be, UnsafeRefIter::new, data_ptr_iter.next_mut::<Arp>, data_ptr_iter.next_mut::<Eth> ...(+11)
- 参考项目：`lose-net-stack/src/packets/tcp.rs#ack`，调用摘要：flags.contains, flags.remove, self.flags.contains
- 参考项目：`lose-net-stack/src/lib.rs#analysis_udp`，调用摘要：IPv4::from_u32, MacAddress::new, Packet::UDP, data_ptr_iter.get_curr_arr, data_ptr_iter.next::<UDP>, ip_header.dst.to_be ...(+5)

#### 文件系统/第三方库
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `os-la/src/fs/ext4_lw/inode.rs#write_at` | `ext4_rs/src/ext4_impls/file.rs#write_at` | 同板块同名 | Err, SysErrNo::from, file.file_close, file.file_open, file.file_open(path, O_RDWR)
            .map_err, file.file_seek ...(+12) | Block::load, Ok, block.sync_blk_to_disk, block.write_offset, drop, inode_ref
                .inode
                .set_size ...(+9) |
| `lwext4_rust/src/file.rs#file_type` | `ext4_rs/src/ext4_defs/inode.rs#file_type` | 同板块同名 | self.file_mode, self.file_mode().unwrap, warn! | InodeFileType::from_bits_truncate |
| `lwext4_rust/c/lwext4/src/ext4_bitmap.c#ext4_bmap_bit_find_clr` | `ext4_rs/src/utils/bitmap.rs#ext4_bmap_bit_find_clr` | 同板块同名 | ext4_bmap_is_bit_clr | ext4_bmap_is_bit_clr |
| `lwext4_rust/c/lwext4/src/ext4.c#ext4_parse_flags` | `ext4_rs/src/simple_interface/mod.rs#ext4_parse_flags` | 同板块同名 | strcmp | Err, Ext4Error::new, Ok |

#### 第三方库
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `os-la-patch/virtio-drivers/src/transport/mmio.rs#vendor_id` | `virtio-drivers/src/transport/mmio.rs#vendor_id` | 同板块同名 | volread! | field_shared!, field_shared!(self.header, vendor_id).read |
| `deps/virtio-drivers/src/header.rs#vendor_id` | `virtio-drivers-old/src/header.rs#vendor_id` | 同板块同名同结构 | self.vendor_id.read | self.vendor_id.read |

#### 通用模块/其他
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `os/src/boards/qemu.rs#exit` | `os/src/hal/up/riscv.rs#exit` | 同板块同名 | asm!, exit_code_encode | sstatus::set_sie |
| `deps/riscv/descriptor/generator.rs#generate_enum` | `riscv/descriptor/generator.rs#generate_enum` | 同板块同名同结构 | String::new, self.enumerations.iter, write!, write!(
            &mut ret,
            "#[derive(Copy, Clone, Debug)]
#[repr(usize)]
"
        )
        .unwrap, write!(
            &mut ret,
            "}}
impl {}{{
    fn from(x: usize)->Self{{
        match x{{
{}            _ => unreachable!()
        }}
    }}
}}
",
            name, branches
        )
        .unwrap, write!(&mut branches, "            {} => Self::{},\n", e.1, e.0).unwrap ...(+2) | String::new, self.enumerations.iter, write!, write!(
            &mut ret,
            "#[derive(Copy, Clone, Debug)]
#[repr(usize)]
"
        )
        .unwrap, write!(
            &mut ret,
            "}}
impl {}{{
    fn from(x: usize)->Self{{
        match x{{
{}            _ => unreachable!()
        }}
    }}
}}
",
            name, branches
        )
        .unwrap, write!(&mut branches, "            {} => Self::{},\n", e.1, e.0).unwrap ...(+2) |
| `deps/riscv/descriptor/generator.rs#parse` | `riscv/descriptor/generator.rs#parse` | 同板块同名同结构 | EnumerationDescriptor::parse, Some, as_str_polyfill!, desc.split, parts.next, parts.next().unwrap ...(+2) | EnumerationDescriptor::parse, Some, as_str_polyfill!, desc.split, parts.next, parts.next().unwrap ...(+2) |
| `user/src/lib.rs#_start` | `os/src/hal/boot/loongarch.rs#_start` | 同板块同名 | ((argv + i * core::mem::size_of::<usize>()) as *const usize).read_volatile, ((str_start + *i) as *const u8).read_volatile, (0usize..)
            .find, (0usize..)
            .find(|i| unsafe { ((str_start + *i) as *const u8).read_volatile() == 0 })
            .unwrap, HEAP.lock, HEAP.lock()
            .init ...(+10) | core::arch::naked_asm! |


好的，我将以审计专家的身份，基于提供的证据、控辩双方论点及审计指令，对本案进行最终的定性仲裁。

### 1. 宏观判定
- **定性结论**: **独立开发，底层架构存在显著差异** (相似度: 17.11%，共同 AST 指纹: 667；唯一 AST 指纹: 目标项目 2712，参考项目 1853；覆盖率: 目标项目 24.59%，参考项目 36.0%)

### 2. 全模块覆盖矩阵解读
- **进程管理**: 双方均存在。存在共同 AST 指纹。目标覆盖率 12.7%，参考覆盖率 25.4%，表明参考项目在该模块有更高比例的通用模式，而目标项目大部分实现独立。证据强度：**弱**。控方所指的“核心骨架”均为基于同一教学框架（rCore）的短小标准访问器函数，其调用链由数据结构强制决定，不构成特异性表达。
- **任务调度/异步运行时**: 双方均存在。存在共同 AST 指纹。目标覆盖率 8.46%，参考覆盖率 15.18%，重合度极低。证据强度：**弱**。重合点集中在 `syscall` 分发等通用入口，具体调度逻辑（如 `run_tasks`）的外部调用（定时器、退出处理）存在明显差异。
- **内存管理**: 双方均存在。存在共同 AST 指纹。目标覆盖率 41.7%，参考覆盖率 52.68%，是重合度最高的模块。证据强度：**中**。高重合度主要由 `virtio-drivers` 等公共第三方依赖贡献，核心内存管理逻辑（如 `from_elf`）虽同名，但调用链（如 `Aux::new` vs `AuxHeader::new`）和实现细节存在差异。
- **中断/异常处理**: 双方均存在。存在共同 AST 指纹。目标覆盖率 22.92%，参考覆盖率 22.0%，覆盖率相近。证据强度：**弱**。重合点完全集中在 `riscv` 公共 crate 的寄存器定义与中断控制函数，属于对开源硬件的标准描述，非原创逻辑。
- **系统调用**: 双方均存在。未观察到共同结构指纹（覆盖率 0.0%）。证据强度：**未形成**。尽管存在同名函数，但 AST 指纹无重合，表明系统调用层的实现逻辑和依赖关系完全不同。
- **文件系统**: 双方均存在。存在共同 AST 指纹。目标覆盖率 10.46%，参考覆盖率 25.0%。证据强度：**弱**。重合点主要源于 `ext4` 文件系统规范的标准算法实现（如 `ext4_bmap_bit_find_clr`）和 `virtio-drivers` 公共依赖，双方采用了不同的绑定方式（C绑定 vs 纯Rust）。
- **设备驱动**: 双方均存在。存在共同 AST 指纹。目标覆盖率 71.85%，参考覆盖率 62.18%，重合度极高。证据强度：**强（但指向公共依赖）**。证据清单中的函数（如 `fake_read_write_queue`）呈现“同板块同名同结构”，路径均指向 `virtio-drivers`，这是典型的引用同一开源项目代码，不构成相互抄袭。
- **网络协议栈**: 仅参考项目体现。目标项目无此模块。证据强度：**未形成**。此差异是证明两个项目独立设计的强有力反证。
- **第三方库/公共依赖**: 双方均存在。存在共同 AST 指纹。证据强度：**强（指向公共依赖）**。`riscv` 代码生成器、`virtio-drivers` 等模块的完全一致，明确指向共享上游开源代码。

### 3. 底层 AST 关键证据指纹表
| 所属板块 | 目标项目路径 | 参考项目路径 | 证据类型 | 技术说明 |
| :--- | :--- | :--- | :--- | :--- |
| 设备驱动 | `os-la-patch/virtio-drivers/src/queue.rs#fake_read_write_queue` | `virtio-drivers/src/queue.rs#fake_read_write_queue` | 同板块同名同结构 | 双方均完整复制了 `virtio-drivers` 开源项目的测试函数，调用图谱完全一致，属于引用公共第三方库代码。 |
| 设备驱动 | `os-la-patch/virtio-drivers/src/device/sound.rs#play` | `virtio-drivers/src/device/sound.rs#play` | 同板块同名同结构 | 同上，为 `virtio-drivers` 公共依赖的测试代码，非原创逻辑。 |
| 第三方库 | `deps/riscv/descriptor/generator.rs#generate` | `riscv/descriptor/generator.rs#generate` | 同板块同名同结构 | 双方均使用了开源的 `riscv` crate 的寄存器描述生成器，代码完全一致，属于公共依赖。 |
| 内存管理 | `os-la-patch/virtio-drivers/src/device/socket/connectionmanager.rs#send_recv` | `virtio-drivers/src/device/socket/connectionmanager.rs#send_recv` | 同板块同名 | 调用图谱高度相似，源于对 `virtio-drivers` 公共依赖的适配性修改，核心逻辑来自上游。 |
| 文件系统 | `lwext4_rust/c/lwext4/src/ext4_bitmap.c#ext4_bmap_bit_find_clr` | `ext4_rs/src/utils/bitmap.rs#ext4_bmap_bit_find_clr` | 同板块同名 | 双方均实现了 ext4 规范中的位图查找算法，函数名和核心调用（`ext4_bmap_is_bit_clr`）由规范决定。 |
| 进程管理 | `os/src/task/processor.rs#current_trap_cx_user_va` | `os/src/task/processor.rs#current_trap_cx_user_va` | 同板块底层逻辑一致 | 6层链式调用由 `TaskControlBlock` 嵌套数据结构强制决定，是 rCore 教学框架下的标准访问模式。 |
| 进程管理 | `os/src/task/manager.rs#remove_from_pid2process` | `os/src/task/manager.rs#remove_from_pid2process` | 同板块底层逻辑一致 | `Mutex<BTreeMap>` 的删除并断言操作是 Rust 内核开发中的常见防御性编程范式。 |

### 4. 核心代码调用流对比剖析
* **【进程管理/`sys_exec` 系统调用】**
* **底层技术析理**: 两者均实现了 `exec` 系统调用，但底层对象模型完全不同。目标项目 (`os-la`) 构造 `LD_LIBRARY_PATH` 等环境变量字符串，旨在支持动态链接器的路径解析，服务于 glibc/musl 等动态库。参考项目则加载 `BUSYBOX_BIN` 静态二进制数据，通过 `app_inode.read_all` 读取并执行。一个面向动态链接的进程模型，一个面向静态二进制应用，在可执行文件加载这一核心语义上存在根本性架构差异。
* **代码溯源链接**:
  - 目标项目: `os-la/src/syscall/process.rs#sys_exec`
  - 参考项目: `os/src/syscall/process.rs#sys_exec`

* **【进程管理/`sys_clone` 系统调用】**
* **底层技术析理**: 两者均实现 `clone`，但任务派生模型截然不同。目标项目 (`os-la`) 调用 `current_task.clone_task` 和 `add_task`，表明其模型是基于“任务/线程”的克隆。参考项目则调用 `current_process.fork` 和 `new_process.exec`，是典型的“进程”派生模型。这反映了双方在操作系统核心抽象（线程 vs 进程）上的根本性设计分歧。
* **代码溯源链接**:
  - 目标项目: `os-la/src/syscall/process.rs#sys_clone`
  - 参考项目: `os/src/syscall/process.rs#sys_clone`

* **【任务调度/`run_tasks` 调度器主循环】**
* **底层技术析理**: 两者均实现调度器主循环，但集成逻辑不同。目标项目 (`os/src`) 在循环中集成了 `crate::timer::check_timer` 和 `crate::board::QEMU_EXIT_HANDLE.exit_success`，表明其调度器与定时器管理和平台退出机制紧密耦合。参考项目则使用 `println!` 进行调试输出，并通过 `task.inner.exclusive_session` 管理任务状态，未见平台退出逻辑。两者在调度器的外设集成和调试策略上存在差异。
* **代码溯源链接**:
  - 目标项目: `os/src/task/processor.rs#run_tasks`
  - 参考项目: `os/src/task/processor.rs#run_tasks`

### 5. 单方模块与降权项
- **单方模块**: **网络协议栈**模块仅在参考项目中存在（53个函数），目标项目无此模块。这是证明两个项目独立开发、架构设计迥异的最有力证据，直接驳斥了“全面抄袭”的指控。
- **降权项（第三方库/公共依赖）**: `virtio-drivers` 和 `riscv` crate 中的同名同结构函数，是双方引用公共开源代码的直接证据，不构成相互抄袭。`ext4` 相关的同名函数源于文件系统规范的标准算法。所有跨板块（如将目标项目的文件系统函数与参考项目的进程管理函数对比）的证据，均依规降级为弱信号，不作为主证据。

### 6. 保守结论
综合全模块证据，本案整体相似度低（17.11%），75.41% 的目标项目 AST 指纹为独有。控方所指控的“像素级复制”函数，经技术析理，均为受限于数据结构、Rust 语言 idioms 或公共教学框架（rCore）的短小标准访问器，其调用链的趋同是领域约束下的自然结果，而非创造性表达的抄袭。双方在进程/线程模型、可执行文件加载、网络协议栈等核心架构上存在根本性差异。高重合度模块（设备驱动、第三方库）的相似性可明确归因于对 `virtio-drivers`、`riscv` 等开源项目的共同依赖。因此，现有证据无法支持抄袭指控，项目间观察到的相似性属于独立开发过程中的正常技术趋同与公共资源引用。

------------------------------

