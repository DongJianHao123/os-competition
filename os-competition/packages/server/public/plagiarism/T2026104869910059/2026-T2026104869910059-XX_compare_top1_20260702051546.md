# 🛡️ OS 内核赛自动化代码级架构审计报告

**审计时间**: `2026-07-02 05:15:46`
**参赛作品名称**: `2026-T2026104869910059-XX`
**年份**: `2026`
**学校**: `XX`
**队伍名称**: `T2026104869910059`
**比赛名字**: `OS-Competition`
**比赛子赛道**: `Kernel-Implementation`
**仓库地址**: `https://gitlab.eduxiji.net/educg-group-43501-3132633/T2026104869910059-3729`
**推测开发语言**: `Rust / C (由AST探针自动识别)`
**特征函数量**: `3667`

## ⚔️ 底层代码同源性对比 (AST Homology)
> 本部分由专门的代码审查智能体，对该项目与符合年份条件的历届最高相似度项目进行控制流与底层调用对比。新版报告会先给出确定性的全模块覆盖矩阵，再给出 Agent 的分模块解释，避免只围绕少量抽样函数展开。

### 🏆 Top 1 底层对标: vs `2025-如有名字队-南开大学`（南开大学 / 如有名字队）

#### 对方作品信息

**对方年份**: `2025`
**对方学校**: `南开大学`
**对方队伍名称**: `如有名字队`
**对方比赛名字**: `OS-Competition`
**对方比赛子赛道**: `Kernel-Implementation`
**对方仓库地址**: `https://gitlab.eduxiji.net/educg-group-36002-2710490/nonix`

> 双向覆盖率：共同 AST 指纹: 1675；唯一 AST 指纹: 目标项目 2712，参考项目 1745；覆盖率: 目标项目 61.76%，参考项目 95.99%

## 全模块对比总览（确定性证据）

> 总体覆盖率：共同 AST 指纹: 1675；唯一 AST 指纹: 目标项目 2712，参考项目 1745；覆盖率: 目标项目 61.76%，参考项目 95.99%

### 1. 模块覆盖矩阵
| 模块 | 目标函数数 | 参考函数数 | 共同唯一 AST 指纹 | Target 覆盖 | Base 覆盖 | 状态 |
| :--- | ---: | ---: | ---: | ---: | ---: | :--- |
| 进程管理 | 142 | 36 | 29 | 23.02% | 82.86% | 双方存在且有结构重合 |
| 任务调度/异步运行时 | 225 | 92 | 62 | 30.85% | 74.7% | 双方存在且有结构重合 |
| 内存管理 | 727 | 297 | 265 | 46.82% | 94.64% | 双方存在且有结构重合 |
| 中断/异常处理 | 126 | 43 | 33 | 34.38% | 100.0% | 双方存在且有结构重合 |
| 系统调用 | 168 | 63 | 35 | 32.41% | 68.63% | 双方存在且有结构重合 |
| 文件系统 | 396 | 252 | 204 | 66.67% | 96.68% | 双方存在且有结构重合 |
| 设备驱动 | 462 | 372 | 336 | 82.96% | 100.0% | 双方存在且有结构重合 |
| 网络协议栈 | 0 | 0 | 0 | 0.0% | 0.0% | 双方均未体现 |
| 文件系统/第三方库 | 748 | 696 | 591 | 91.91% | 99.66% | 双方存在且有结构重合 |
| 第三方库 | 2 | 1 | 1 | 50.0% | 100.0% | 双方存在且有结构重合 |
| 通用模块/其他 | 671 | 155 | 136 | 41.34% | 97.14% | 双方存在且有结构重合 |

### 2. 分模块证据清单

#### 进程管理
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `os-la/src/syscall/process.rs#sys_exec` | `os/src/syscall/process.rs#sys_exec` | 同板块同名 | "LD_LIBRARY_PATH=/:/lib:/usr/lib:/lib64:/glibc/lib:/musl/lib:".to_string, "PATH=/:/bin:".to_string, Err, Ok, String::from, Vec::<String>::new ...(+33) | "LD_LIBRARY_PATH=/:/lib:/usr/lib:".to_string, "PATH=/:/bin:".to_string, Ok, String::from, Vec::<String>::new, Vec::new ...(+28) |
| `os-la/src/syscall/signal.rs#sys_sig_proc_mask` | `os/src/syscall/signal.rs#sys_sig_proc_mask` | 同板块同名 | Ok, SignalMaskFlag::from_bits, current_task, current_task().unwrap, current_user_token, debug! ...(+5) | Err, Ok, SignalMaskFlag::from_bits, SignalMaskFlag::from_bits(how).unwrap, current_task, current_task().unwrap ...(+7) |
| `os-la-patch/polyhal/src/components/timer/x86_64.rs#timer_wait` | `patch/polyhal/src/components/timer/x86_64.rs#timer_wait` | 同板块同名同结构 | PC_SPEAKER.read, PC_SPEAKER.write, PIT_CH2.write, PIT_CMD.write, duration.as_millis, spin_loop | PC_SPEAKER.read, PC_SPEAKER.write, PIT_CH2.write, PIT_CMD.write, duration.as_millis, spin_loop |
| `os-la/src/syscall/process.rs#sys_clone` | `os/src/syscall/process.rs#sys_clone` | 同板块同名同结构 | CloneFlags::from_bits, CloneFlags::from_bits(flags as u32).ok_or_else, Ok, add_task, current_task, current_task().unwrap ...(+5) | CloneFlags::from_bits, CloneFlags::from_bits(flags as u32).ok_or_else, Ok, add_task, current_task, current_task().unwrap ...(+5) |

#### 任务调度/异步运行时
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `os-la/src/syscall/mod.rs#syscall` | `os/src/syscall/mod.rs#syscall` | 同板块同名 | Err, Ok, debug!, mask_ptr.add, mask_ptr.is_null, param_ptr.is_null ...(+84) | Err, error!, sys_brk, sys_chdir, sys_clock_gettime, sys_clock_nanosleep ...(+72) |
| `os-la/src/syscall/fs.rs#sys_pselect6` | `os/src/syscall/fs.rs#sys_pselect6` | 同板块同名 | Ok, Some, current_task, current_task().unwrap, current_user_token, drop ...(+21) | Ok, Some, current_task, current_task().unwrap, current_user_token, drop ...(+21) |
| `os-la/src/syscall/fs.rs#sys_copy_file_range` | `os/src/syscall/fs.rs#sys_copy_file_range` | 同板块同名同结构 | Err, Ok, UserBuffer::new, Vec::new, buf.as_mut_slice, buf.as_mut_slice().as_mut_ptr ...(+27) | Err, Ok, UserBuffer::new, Vec::new, buf.as_mut_slice, buf.as_mut_slice().as_mut_ptr ...(+27) |
| `os-la/src/syscall/fs.rs#sys_utimesat` | `os/src/syscall/fs.rs#sys_utimesat` | 同板块同名 | (times as *const Timespec).add, Err, Ok, Some, current_task, current_task().unwrap ...(+18) | (times as *const Timespec).add, Ok, Some, current_task, current_task().unwrap, current_user_token ...(+11) |

#### 内存管理
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `os-la-patch/virtio-drivers/src/device/socket/connectionmanager.rs#send_recv` | `patch/virtio-drivers/src/device/socket/connectionmanager.rs#send_recv` | 同板块同名同结构 | "Hello from guest".as_bytes, (hello_from_guest.len() as u32).into, (hello_from_host.len() as u32).into, 0.into, 50.into, Arc::new ...(+44) | "Hello from guest".as_bytes, (hello_from_guest.len() as u32).into, (hello_from_host.len() as u32).into, 0.into, 50.into, Arc::new ...(+44) |
| `os-la/src/task/task.rs#exec` | `os/src/task/task.rs#exec` | 同板块同名 | Arc::new, Aux::new, MemorySet::new, MemorySetInner::from_elf, String::from, TrapFrame::new ...(+34) | Arc::new, Aux::new, MemorySet::new, MemorySetInner::from_elf, String::from, TrapFrame::new ...(+34) |
| `os-la/src/utils/error.rs#str` | `os/src/utils/error.rs#str` | 同板块同名同结构 | panic! | panic! |
| `os-la/src/trap/mod.rs#kernel_interrupt` | `os/src/trap/mod.rs#kernel_interrupt` | 同板块同名 | LAST_SYSCALL.store, VirtAddr::new, VirtAddr::new(_paddr).floor, check_signals_error_of_current, crate::timer::get_time_ms, ctx.args ...(+20) | VirtAddr::new, VirtAddr::new(_paddr).floor, check_signals_error_of_current, ctx.args, ctx.syscall_ok, current_add_signal ...(+18) |

#### 中断/异常处理
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `os-la/src/signal/sigflags.rs#default_op` | `os/src/signal/sigflags.rs#default_op` | 同板块同名同结构 | continue_signals.contains, dump_signals.contains, ignore_signals.contains, stop_signals.contains, terminate_signals.contains | continue_signals.contains, dump_signals.contains, ignore_signals.contains, stop_signals.contains, terminate_signals.contains |
| `os-la-patch/polyhal/src/utils/mutex_no_irq.rs#try_lock` | `patch/polyhal/src/utils/mutex_no_irq.rs#try_lock` | 同板块同名同结构 | IRQ::int_disable, IRQ::int_enabled, self.lock
            .try_lock, self.lock
            .try_lock()
            .map, self.lock.is_locked | IRQ::int_disable, IRQ::int_enabled, self.lock
            .try_lock, self.lock
            .try_lock()
            .map, self.lock.is_locked |
| `os-la/src/trap/interrupts.rs#inc_irq_count` | `os/src/trap/interrupts.rs#inc_irq_count` | 同板块同名同结构 | IRQ_COUNTS.lock, counts[irq].saturating_add | IRQ_COUNTS.lock, counts[irq].saturating_add |
| `os-la-patch/polyhal/src/components/irq/aarch64.rs#irq_enable` | `patch/polyhal/src/components/irq/aarch64.rs#irq_enable` | 同板块同名同结构 | GICD.lock, GICD.lock().set_enable | GICD.lock, GICD.lock().set_enable |

#### 系统调用
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `os-la/src/syscall/other.rs#sys_uname` | `os/src/syscall/other.rs#sys_uname` | 同板块同名同结构 | Ok, UserBuffer::new, buf[..len].copy_from_slice, bytes.len, bytes.len().min, copy_str_to_buf ...(+8) | Ok, UserBuffer::new, buf[..len].copy_from_slice, bytes.len, bytes.len().min, copy_str_to_buf ...(+8) |
| `os-la/src/syscall/other.rs#sys_getrandom` | `os/src/syscall/other.rs#sys_getrandom` | 同板块同名同结构 | Err, Ok, UserBuffer::new, Vec::new, all_bytes.extend_from_slice, buf_ptr.is_null ...(+7) | Err, Ok, UserBuffer::new, Vec::new, all_bytes.extend_from_slice, buf_ptr.is_null ...(+7) |
| `os-la/src/syscall/mm.rs#sys_shmget` | `os/src/syscall/mm.rs#sys_shmget` | 同板块同名 | Err, Ok, ShmFlags::from_bits_truncate, flags.contains, shm_create, shm_find ...(+1) | Err, Ok, ShmFlags::from_bits, ShmFlags::from_bits(flags & !0x1ff).unwrap, flags.contains, shm_create ...(+2) |
| `os-la/src/syscall/other.rs#sys_clock_nanosleep` | `os/src/syscall/other.rs#sys_clock_nanosleep` | 同板块同名同结构 | Ok, current_user_token, get_data, get_time_ms, suspend_current_and_run_next, trace! | Ok, current_user_token, get_data, get_time_ms, suspend_current_and_run_next, trace! |

#### 文件系统
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `os-la/src/mm/memory_set.rs#split_area_by_range` | `os/src/mm/memory_set.rs#split_area_by_range` | 同板块同名同结构 | BTreeMap::new, MapArea::from_another, VAddrRange::new, Vec::new, area.clone, area.data_frames.retain ...(+19) | BTreeMap::new, MapArea::from_another, VAddrRange::new, Vec::new, area.clone, area.data_frames.retain ...(+19) |
| `os-la/src/mm/memory_set.rs#load_dl_interp_if_needed` | `os/src/mm/memory_set.rs#load_dl_interp_if_needed` | 同板块同名 | "/glibc/lib/ld-linux-loongarch-lp64d.so.1".to_string, "/glibc/lib/libc.so.6".to_string, "/musl/lib/libc.so".to_string, "ld-musl-riscv64.so.1".to_string, Arc::new, DL_INTERP_OFFSET.into ...(+39) | "/glibc/lib/libc.so.6".to_string, "ld-musl-riscv64.so.1".to_string, DL_INTERP_OFFSET.into, Some, String::from_utf8, String::from_utf8(section.raw_data(&elf).to_vec()).unwrap ...(+29) |
| `os-la-patch/polyhal/src/debug_console/x86_64/graphic.rs#put_char` | `patch/polyhal/src/debug_console/x86_64/graphic.rs#put_char` | 同板块同名同结构 | BIT_FONTS.len, self.backspace, self.scroll_up, self.write_pixel | BIT_FONTS.len, self.backspace, self.scroll_up, self.write_pixel |
| `os-la-patch/virtio-drivers/src/device/blk.rs#device_id` | `patch/virtio-drivers/src/device/blk.rs#device_id` | 同板块同名同结构 | Arc::new, BlkFeature::RING_INDIRECT_DESC.bits, Default::default, Mutex::new, NonNull::from, QUEUE_SIZE.into ...(+14) | Arc::new, BlkFeature::RING_INDIRECT_DESC.bits, Default::default, Mutex::new, NonNull::from, QUEUE_SIZE.into ...(+14) |

#### 设备驱动
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `os-la-patch/virtio-drivers/src/queue.rs#fake_read_write_queue` | `patch/virtio-drivers/src/queue.rs#fake_read_write_queue` | 同板块同名同结构 | (*available_ring).idx.load, (*used_ring).idx.fetch_add, (*used_ring).idx.load, Vec::new, assert!, assert_eq! ...(+18) | (*available_ring).idx.load, (*used_ring).idx.fetch_add, (*used_ring).idx.load, Vec::new, assert!, assert_eq! ...(+18) |
| `os-la-patch/virtio-drivers/src/device/sound/fake.rs#handle_control_request` | `patch/virtio-drivers/src/device/sound/fake.rs#handle_control_request` | 同板块同名同结构 | CommandCode::SOk.into, Some, Vec::new, VirtIOSndHdr {
                            command_code: CommandCode::SOk.into(),
                        }
                        .as_bytes, VirtIOSndHdr::read_from_prefix, VirtIOSndHdr::read_from_prefix(&request)
                .expect ...(+16) | CommandCode::SOk.into, Some, Vec::new, VirtIOSndHdr {
                            command_code: CommandCode::SOk.into(),
                        }
                        .as_bytes, VirtIOSndHdr::read_from_prefix, VirtIOSndHdr::read_from_prefix(&request)
                .expect ...(+16) |
| `os-la-patch/virtio-drivers/src/device/sound.rs#play` | `patch/virtio-drivers/src/device/sound.rs#play` | 同板块同名同结构 | FakeSoundDevice::new, PcmFeatures::empty, VirtIOSound::<FakeHal, FakeTransport<VirtIOSoundConfig>>::new, VirtIOSound::<FakeHal, FakeTransport<VirtIOSoundConfig>>::new(transport).unwrap, assert_eq!, expected_sound.extend ...(+22) | FakeSoundDevice::new, PcmFeatures::empty, VirtIOSound::<FakeHal, FakeTransport<VirtIOSoundConfig>>::new, VirtIOSound::<FakeHal, FakeTransport<VirtIOSoundConfig>>::new(transport).unwrap, assert_eq!, expected_sound.extend ...(+22) |
| `os-la-patch/virtio-drivers/src/device/sound.rs#pcm_xfer` | `patch/virtio-drivers/src/device/sound.rs#pcm_xfer` | 同板块同名同结构 | CommandCode::SOk.into, Default::default, Err, Ok, Some, array::from_fn ...(+17) | CommandCode::SOk.into, Default::default, Err, Ok, Some, array::from_fn ...(+17) |

#### 文件系统/第三方库
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `lwext4_rust/c/lwext4/src/ext4_balloc.c#ext4_balloc_alloc_block` | `lwext4_rust/c/lwext4/src/ext4_balloc.c#ext4_balloc_alloc_block` | 同板块同名同结构 | ext4_balloc_get_bgid_of_block, ext4_balloc_get_block_of_bgid, ext4_balloc_set_bitmap_csum, ext4_balloc_verify_bitmap_csum, ext4_bg_get_block_bitmap, ext4_bg_get_free_blocks_count ...(+19) | ext4_balloc_get_bgid_of_block, ext4_balloc_get_block_of_bgid, ext4_balloc_set_bitmap_csum, ext4_balloc_verify_bitmap_csum, ext4_bg_get_block_bitmap, ext4_bg_get_free_blocks_count ...(+19) |
| `lwext4_rust/c/lwext4/src/ext4_journal.c#jbd_journal_prepare` | `lwext4_rust/c/lwext4/src/ext4_journal.c#jbd_journal_prepare` | 同板块同名同结构 | EXT4_BLOCK_ZERO, RB_FIND, TAILQ_FOREACH_REVERSE_SAFE, TAILQ_FOREACH_SAFE, TAILQ_REMOVE, ext4_bcache_clear_dirty ...(+17) | EXT4_BLOCK_ZERO, RB_FIND, TAILQ_FOREACH_REVERSE_SAFE, TAILQ_FOREACH_SAFE, TAILQ_REMOVE, ext4_bcache_clear_dirty ...(+17) |
| `lwext4_rust/c/lwext4/src/ext4.c#ext4_dir_rm` | `lwext4_rust/c/lwext4/src/ext4.c#ext4_dir_rm` | 同板块同名同结构 | EXT4_MP_LOCK, EXT4_MP_UNLOCK, ext4_block_cache_write_back, ext4_dir_en_get_inode, ext4_dir_iterator_fini, ext4_dir_iterator_init ...(+19) | EXT4_MP_LOCK, EXT4_MP_UNLOCK, ext4_block_cache_write_back, ext4_dir_en_get_inode, ext4_dir_iterator_fini, ext4_dir_iterator_init ...(+19) |
| `lwext4_rust/c/lwext4/src/ext4_dir_idx.c#ext4_dir_dx_split_index` | `lwext4_rust/c/lwext4/src/ext4_dir_idx.c#ext4_dir_dx_split_index` | 同板块同名同结构 | ext4_block_set, ext4_dir_dx_climit_get_count, ext4_dir_dx_climit_get_limit, ext4_dir_dx_climit_set_count, ext4_dir_dx_climit_set_limit, ext4_dir_dx_entry_get_hash ...(+10) | ext4_block_set, ext4_dir_dx_climit_get_count, ext4_dir_dx_climit_get_limit, ext4_dir_dx_climit_set_count, ext4_dir_dx_climit_set_limit, ext4_dir_dx_entry_get_hash ...(+10) |

#### 第三方库
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `os-la-patch/virtio-drivers/src/transport/mmio.rs#vendor_id` | `patch/virtio-drivers/src/transport/mmio.rs#vendor_id` | 同板块同名同结构 | volread! | volread! |

#### 通用模块/其他
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `os-la-patch/polyhal/src/mem.rs#parse_system_info` | `patch/polyhal/src/mem.rs#parse_system_info` | 同板块同名同结构 | CPU_NUM.init_once, display_info!, fdt.chosen, fdt.chosen().inspect, fdt.find_nodes, fdt.find_nodes("/cpus/cpu").count ...(+8) | CPU_NUM.init_once, display_info!, fdt.chosen, fdt.chosen().inspect, fdt.find_nodes, fdt.find_nodes("/cpus/cpu").count ...(+8) |
| `os-la/src/signal/sigflags.rs#check_error` | `os/src/signal/sigflags.rs#check_error` | 同板块同名同结构 | Some, self.contains | Some, self.contains |
| `user/src/lib.rs#_start` | `user/src/lib.rs#_start` | 同板块同名 | ((argv + i * core::mem::size_of::<usize>()) as *const usize).read_volatile, ((str_start + *i) as *const u8).read_volatile, (0usize..)
            .find, (0usize..)
            .find(|i| unsafe { ((str_start + *i) as *const u8).read_volatile() == 0 })
            .unwrap, HEAP.lock, HEAP.lock()
            .init ...(+10) | ((argv + i * core::mem::size_of::<usize>()) as *const usize).read_volatile, ((str_start + *i) as *const u8).read_volatile, (0usize..)
            .find, (0usize..)
            .find(|i| unsafe { ((str_start + *i) as *const u8).read_volatile() == 0 })
            .unwrap, HEAP.lock, HEAP.lock()
            .init ...(+10) |
| `os-la-patch/polyhal/src/components/percpu/mod.rs#get_percpu_ptr` | `patch/polyhal/src/components/percpu/mod.rs#get_percpu_ptr` | 同板块同名同结构 | cfg_if::cfg_if! | cfg_if::cfg_if! |


好的，审计官。基于提供的证据底座、控辩双方陈述及AST抽样数据，现出具最终同源性定性仲裁报告。

---

### 📝 最终同源性定性仲裁报告

#### 1. 宏观判定
- **定性结论**: **部分逻辑借鉴，存在显著架构改动与独立演化** (相似度: 60.21%，共同 AST 指纹: 1675；唯一 AST 指纹: 目标项目 2712，参考项目 1745；覆盖率: 目标项目 61.76%，参考项目 95.99%)

**判定依据**：总体相似度处于中等偏上水平，但覆盖率指标揭示了关键的非对称性。参考项目覆盖率高达95.99%，表明其代码几乎完全被目标项目所涵盖的公共模式所覆盖，这符合其作为较早的、基于公共教学框架的竞赛作品的定位。目标项目覆盖率仅为61.76%，且拥有远超共同指纹数量的唯一AST指纹（2712），这强有力地证明了目标项目在参考项目的基础上进行了大规模的功能扩展、架构重构和独立代码编写，并非简单的整体抄袭或“换皮”。

#### 2. 全模块覆盖矩阵解读

| 模块 | 目标函数数 | 参考函数数 | 共同唯一 AST 指纹 | Target 覆盖 | Base 覆盖 | 状态 | 证据强度 |
| :--- | ---: | ---: | ---: | ---: | ---: | :--- | :--- |
| **进程管理** | 142 | 36 | 29 | 23.02% | 82.86% | 双方存在且有结构重合 | **强** |
| **任务调度/异步运行时** | 225 | 92 | 62 | 30.85% | 74.7% | 双方存在且有结构重合 | **强** |
| **内存管理** | 727 | 297 | 265 | 46.82% | 94.64% | 双方存在且有结构重合 | **中** |
| **中断/异常处理** | 126 | 43 | 33 | 34.38% | 100.0% | 双方存在且有结构重合 | **中** |
| **系统调用** | 168 | 63 | 35 | 32.41% | 68.63% | 双方存在且有结构重合 | **中** |
| **文件系统** | 396 | 252 | 204 | 66.67% | 96.68% | 双方存在且有结构重合 | **中** |
| **设备驱动** | 462 | 372 | 336 | 82.96% | 100.0% | 双方存在且有结构重合 | **弱** |
| **网络协议栈** | 0 | 0 | 0 | 0.0% | 0.0% | 双方均未体现 | 未形成 |
| **文件系统/第三方库** | 748 | 696 | 591 | 91.91% | 99.66% | 双方存在且有结构重合 | **弱** |
| **第三方库** | 2 | 1 | 1 | 50.0% | 100.0% | 双方存在且有结构重合 | **弱** |
| **通用模块/其他** | 671 | 155 | 136 | 41.34% | 97.14% | 双方存在且有结构重合 | **中** |

**解读摘要**：
- **进程管理 & 任务调度**：证据强度为“强”。这是控方指控的核心区域。目标项目函数体量远超参考项目（142 vs 36， 225 vs 92），但覆盖率极低（23.02%， 30.85%），说明目标项目在此模块有大量独立扩展。然而，双方重合的29个和62个AST指纹中，包含了如`timer_wait`、`sys_clone`等底层逻辑完全一致的“铁证”，表明存在对参考项目核心骨架的直接复用。
- **内存管理 & 中断/异常处理**：证据强度为“中”。参考项目覆盖率极高（94.64%， 100.0%），表明其实现基本是公共框架的完整映射。目标项目覆盖率较低（46.82%， 34.38%），说明其进行了大量替换或重构，但重合部分仍揭示了共同的底层起源。
- **设备驱动 & 第三方库**：证据强度为“弱”。双方覆盖率均极高（>80%），且证据大量集中于`polyhal`、`virtio-drivers`、`lwext4_rust`等目录。这明确指向双方共同依赖了相同的第三方开源代码或硬件抽象层补丁，不能作为一方抄袭另一方的证据。
- **文件系统**：证据强度为“中”。目标覆盖率66.67%，参考覆盖率96.68%。目标项目在文件系统层面有显著的自研部分，但仍有约三分之二的代码与参考项目共享相同的AST指纹，表明其实现建立在与参考项目相同的底层文件系统框架之上。

#### 3. 底层 AST 关键证据指纹表

| 所属板块 | 目标项目路径 | 参考项目路径 | 证据类型 | 技术说明 |
| :--- | :--- | :--- | :--- | :--- |
| **进程管理** | `os-la-patch/polyhal/src/components/timer/x86_64.rs#timer_wait` | `patch/polyhal/src/components/timer/x86_64.rs#timer_wait` | 同板块同名同结构 | **硬件端口操作流完全一致**。对`PC_SPEAKER`， `PIT_CH2`， `PIT_CMD`的读写序列及`spin_loop`等待模式构成硬件编程的特定“指纹”，独立开发极难重合。 |
| **进程管理** | `os-la/src/syscall/process.rs#sys_clone` | `os/src/syscall/process.rs#sys_clone` | 同板块同名同结构 | **特定闭包调用链一致**。`CloneFlags::from_bits(flags as u32).ok_or_else(...)` 这一包含类型转换和错误处理的特定组合，是高度特征化的实现细节。 |
| **进程管理** | `os-la/src/task/manager.rs#remove_from_pid2task` | `os/src/task/manager.rs#remove_from_pid2task` | 同板块同名同结构 | **错误处理模式一致**。`map.remove(&pid).is_none()` 后接 `panic!` 的防御性编程组合，是特定设计选择的体现。 |
| **进程管理** | `os-la/src/syscall/process.rs#sys_getpgid` | `os/src/syscall/process.rs#sys_getpgid` | 同板块同名同结构 | **存根函数模式一致**。`Ok(…)` -> `trace!` -> `warn!` 的调用序列，是未完整实现系统调用时的特定日志记录模式。 |
| **任务调度** | `os-la/src/syscall/fs.rs#sys_pselect6` | `os/src/syscall/fs.rs#sys_pselect6` | 同板块同名同结构 | **复杂同步调用流一致**。`current_task` -> `current_user_token` -> `get_data` -> `suspend_current_and_run_next` 的完整调用链高度一致。 |
| **内存管理** | `os-la/src/task/task.rs#exec` | `os/src/task/task.rs#exec` | 同板块同名 | **核心加载流程一致**。`MemorySet::new` -> `MemorySetInner::from_elf` -> `TrapFrame::new` 的程序加载核心骨架相同。 |
| **系统调用** | `os-la/src/syscall/other.rs#sys_uname` | `os/src/syscall/other.rs#sys_uname` | 同板块同名同结构 | **数据拷贝模式一致**。`UserBuffer::new` -> `bytes.len().min` -> `buf[..len].copy_from_slice` 的特定用户态数据读取序列完全一致。 |
| **文件系统** | `os-la/src/mm/memory_set.rs#split_area_by_range` | `os/src/mm/memory_set.rs#split_area_by_range` | 同板块同名同结构 | **复杂数据结构操作一致**。`MapArea::from_another` -> `BTreeMap::new` -> `area.data_frames.retain` 的内存区域分割算法实现细节高度一致。 |
| **第三方库** | `os-la-patch/virtio-drivers/src/device/blk.rs#device_id` | `patch/virtio-drivers/src/device/blk.rs#device_id` | 同板块同名同结构 | **公共上游依赖**。`BlkFeature::RING_INDIRECT_DESC.bits` 等VirtIO规范定义及驱动初始化序列，源于同一开源项目。 |

#### 4. 核心代码调用流对比剖析

* **【进程管理/进程创建 (`sys_clone`)】**
* **底层技术析理**: 双方均采用了基于`CloneFlags`标志位的任务克隆模型。调用流均从解析标志位开始，通过`current_task`获取当前任务控制块，调用`clone_task`创建新的任务控制块副本，最后通过`add_task`将新任务加入全局调度队列。这种对象管理模型（Task作为核心对象，通过Manager进行全局管理）和同步策略（`exclusive_access`）的一致性，揭示了共同的架构起源。目标项目在此基础上的独立演化体现在其更庞大的进程管理函数集（142个 vs 36个）中。
* **代码溯源链接**:
  - 目标项目: `os-la/src/syscall/process.rs#sys_clone`
  - 参考项目: `os/src/syscall/process.rs#sys_clone`

* **【任务调度/同步等待 (`sys_pselect6`)】**
* **底层技术析理**: 该调用流展示了典型的同步I/O多路复用或睡眠流程。双方均遵循“获取当前任务 -> 获取用户态令牌 -> 从用户态拷贝数据 -> 挂起当前任务并运行下一个”的模式。`UserBuffer`作为内存抽象，`suspend_current_and_run_next`作为调度原语，其组合方式高度一致。这表明目标项目在实现此复杂系统调用时，直接复用了参考项目的整体流程设计。
* **代码溯源链接**:
  - 目标项目: `os-la/src/syscall/fs.rs#sys_pselect6`
  - 参考项目: `os/src/syscall/fs.rs#sys_pselect6`

* **【内存管理/程序加载 (`exec`)】**
* **底层技术析理**: 双方的程序加载入口函数`exec`均遵循相同的核心流程：创建新的内存地址空间(`MemorySet`)，从ELF文件解析并填充该空间(`MemorySetInner::from_elf`)，最后构造用户态陷阱帧(`TrapFrame`)以准备返回用户态执行。这种将内存管理与任务上下文创建耦合的抽象层次设计是共同的。目标项目后续在`parse_interp_and_load`等函数中实现了对动态链接器的独立支持，体现了在此骨架上的演化。
* **代码溯源链接**:
  - 目标项目: `os-la/src/task/task.rs#exec`
  - 参考项目: `os/src/task/task.rs#exec`

#### 5. 单方模块与降权项

- **单方模块/功能**：
    - **目标项目特有**：`run_busybox_group` (BusyBox测试框架)、`parse_interp_and_load` (动态链接器加载)、`sys_setitimer` (间隔定时器)、`DevNullZero` 设备等。这些是目标项目独立扩展的明确证据。
    - **参考项目特有**：无明显独有模块，其代码基本被目标项目覆盖。
- **降权项（第三方库/公共依赖）**：
    - **`polyhal`、`virtio-drivers`、`lwext4_rust`**：这些目录下的代码在双方项目中表现出极高的相似度（“同板块同名同结构”），是双方共同依赖的第三方开源代码或硬件抽象层补丁。其一致性不能作为目标项目抄袭参考项目的证据，只能证明双方使用了相同的上游依赖。
    - **极简存根函数**：如 `sys_getuid`、`sys_getgid` 等仅包含 `Ok -> trace! -> warn!` 的函数，其逻辑过于简单，独立编写的收敛性极高，作为强同源证据的权重被降级。
    - **跨板块证据**：控辩双方材料中未出现强行将不同板块证据进行关联的情况，符合审计要求。

#### 6. 保守结论

综合来看，目标项目 `[2026-T2026104869910059-XX]` 并非对参考项目 `[2025-如有名字队-南开大学]` 的简单整体抄袭。60.21%的AST相似度及参考项目高达95.99%的覆盖率，清晰地表明目标项目**确实在早期开发阶段系统性地借鉴了参考项目的核心骨架与设计模式**，尤其是在进程管理、任务调度和内存管理等核心内核子系统中，存在多处底层逻辑完全一致的“铁证”。

然而，目标项目拥有远超共同指纹数量的唯一AST指纹（2712），且在进程管理等关键模块的函数体量是参考项目的数倍，覆盖率却显著偏低。这有力地证明了目标项目在借鉴的基础上，进行了**大规模的独立开发、功能扩展和架构演化**，如引入多架构动态链接器支持、BusyBox测试框架等。因此，整体风险定性为**“存在明确的结构性借鉴与局部逻辑复用，但已进行显著的二次开发和独立演化，不属于无脑抄袭”**。审计建议关注其独立演化部分的价值，同时承认其与参考项目在核心骨架上的同源性。

------------------------------

