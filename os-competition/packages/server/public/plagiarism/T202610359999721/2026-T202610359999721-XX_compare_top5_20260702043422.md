# 🛡️ OS 内核赛自动化代码级架构审计报告

**审计时间**: `2026-07-02 04:34:22`
**参赛作品名称**: `2026-T202610359999721-XX`
**年份**: `2026`
**学校**: `XX`
**队伍名称**: `T202610359999721`
**比赛名字**: `OS-Competition`
**比赛子赛道**: `Kernel-Implementation`
**仓库地址**: `https://gitlab.eduxiji.net/educg-group-43501-3132633/T202610359999721-2135`
**推测开发语言**: `Rust / C (由AST探针自动识别)`
**特征函数量**: `5518`

## ⚔️ 底层代码同源性对比 (AST Homology)
> 本部分由专门的代码审查智能体，对该项目与符合年份条件的历届最高相似度项目进行控制流与底层调用对比。新版报告会先给出确定性的全模块覆盖矩阵，再给出 Agent 的分模块解释，避免只围绕少量抽样函数展开。

### 🏆 Top 5 底层对标: vs `2025-西北工业大学一二三队-西北工业大学`（西北工业大学 / 西北工业大学一二三队）

#### 对方作品信息

**对方年份**: `2025`
**对方学校**: `西北工业大学`
**对方队伍名称**: `西北工业大学一二三队`
**对方比赛名字**: `OS-Competition`
**对方比赛子赛道**: `Kernel-Implementation`
**对方仓库地址**: `https://gitlab.eduxiji.net/educg-group-36002-2710490/T202510699995276-827`

> 双向覆盖率：共同 AST 指纹: 59；唯一 AST 指纹: 目标项目 4367，参考项目 2317；覆盖率: 目标项目 1.35%，参考项目 2.55%

## 全模块对比总览（确定性证据）

> 总体覆盖率：共同 AST 指纹: 59；唯一 AST 指纹: 目标项目 4367，参考项目 2317；覆盖率: 目标项目 1.35%，参考项目 2.55%

### 1. 模块覆盖矩阵
| 模块 | 目标函数数 | 参考函数数 | 共同唯一 AST 指纹 | Target 覆盖 | Base 覆盖 | 状态 |
| :--- | ---: | ---: | ---: | ---: | ---: | :--- |
| 进程管理 | 597 | 74 | 2 | 0.43% | 2.99% | 双方存在且有结构重合 |
| 任务调度/异步运行时 | 538 | 139 | 3 | 0.6% | 2.38% | 双方存在且有结构重合 |
| 内存管理 | 1321 | 593 | 9 | 0.75% | 2.16% | 双方存在且有结构重合 |
| 中断/异常处理 | 51 | 47 | 2 | 4.0% | 5.0% | 双方存在且有结构重合 |
| 系统调用 | 247 | 65 | 2 | 0.87% | 3.85% | 双方存在且有结构重合 |
| 文件系统 | 1450 | 552 | 6 | 0.66% | 1.65% | 双方存在且有结构重合 |
| 设备驱动 | 116 | 47 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |
| 网络协议栈 | 179 | 88 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |
| 文件系统/第三方库 | 71 | 369 | 2 | 3.08% | 0.69% | 双方存在且有结构重合 |
| 网络协议栈/第三方库 | 4 | 138 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |
| 第三方库 | 0 | 1228 | 0 | 0.0% | 0.0% | 仅参考项目体现 |
| 通用模块/其他 | 944 | 230 | 7 | 0.88% | 5.93% | 双方存在且有结构重合 |

### 2. 分模块证据清单

#### 进程管理
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `kernel/src/syscall/process_exec.rs#sys_execve` | `os/src/syscall/process.rs#sys_execve` | 同板块同名 | Vec::new, args.is_empty, args.push, argv_ptr.is_null, core::mem::size_of::<usize>, crate::exec::execve_from_kernel_args ...(+6) | Box::<[u8; 4]>::new, DEFAULT_SHELL.to_string, Some, Vec::with_capacity, arg_ptr.is_null, argv.add ...(+22) |
| `kernel/src/syscall/process_group.rs#sys_setpgid` | `os/src/syscall/process.rs#sys_setpgid` | 同板块同名 | caller_proc.children.contains, crate::sched::process::PROCESS_TABLE.lock, guard.as_mut, table.get, table.get_mut, table.iter ...(+1) | crate::task::find_task_by_tgid, task.setpgid |
| `kernel/src/syscall/process_info.rs#sys_setsid` | `os/src/syscall/process.rs#sys_setsid` | 同板块同名 | PROCESS_TABLE.lock, ns_view, table.as_mut, table.get, table.get_mut, table.iter ...(+1) | current_task, current_task().unwrap, parent
            .acquire_inner_lock, parent
            .acquire_inner_lock()
            .children
            .retain, task.acquire_inner_lock, task.acquire_inner_lock().parent.as_ref ...(+2) |
| `kernel/src/syscall/process_group.rs#sys_getpgid` | `os/src/syscall/process.rs#sys_getpgid` | 同板块同名 | crate::sched::process::PROCESS_TABLE.lock, super::process_info::ns_view_leader_or_zero, table.as_ref, table.current.and_then, table.current.and_then(|c| table.get(c)).map, table.current.and_then(|c| table.get(c)).map(|p| p.pidns_root).unwrap_or ...(+1) | crate::task::find_task_by_tgid, task.getpgid |

#### 任务调度/异步运行时
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `kernel/src/syscall/fcntl.rs#sys_fcntl` | `os/src/syscall/fs.rs#sys_fcntl` | 同板块同名 | (0..=2).contains, (0..=64).contains, Arc::as_ptr, F_UNLCK.to_le_bytes, LockOwner::Ofd, LockOwner::Pid ...(+74) | Fcntl_Command::from_primitive, OpenFlags::O_NONBLOCK.bits, OpenFlags::O_RDWR.bits, current_task, current_task().unwrap, fd_table.get_ref ...(+10) |
| `kernel/src/syscall/dir.rs#sys_unlinkat` | `os/src/syscall/fs.rs#sys_unlinkat` | 同板块同名 | access_fs_errno, crate::fs::dentry_cache_remove, crate::fs::dentry_cache_remove_tree, crate::fs::is_mount_point, crate::fs::is_readonly_path, crate::fs::lookup ...(+32) | UnlinkatFlags::from_bits, current_task, current_task().unwrap, fd_table.get_ref, file_descriptor.clone, file_descriptor.delete ...(+9) |
| `kernel/src/syscall/mod.rs#syscall` | `os/src/syscall/mod.rs#syscall` | 同板块同名 | SCDBG_DYNAMIC.load, Some, crate::arch::asm_helpers::rdtime, crate::println!, crate::sched::process::ExitReason::killed, crate::sched::process::ExitReason::signaled_with_dump ...(+12) | Errno::try_from, [
            //black list
            SYSCALL_YIELD,
            // SYSCALL_READ,
            SYSCALL_WRITE,
            SYSCALL_GETDENTS64,
            SYSCALL_READV,
            SYSCALL_WRITEV,
            SYSCALL_PSELECT6,
            SYSCALL_SIGACTION,
            SYSCALL_SIGPROCMASK,
            // SYSCALL_WAIT4,
            // SYSCALL_GETPPID,
            SYSCALL_CLOCK_GETTIME,
        ]
        .contains, args.len, crate::task::current_task, crate::task::current_task()
                .unwrap, crate::task::current_task()
                .unwrap()
                .acquire_inner_lock ...(+107) |
| `kernel/src/fs/write_io.rs#sys_writev` | `os/src/syscall/fs.rs#sys_writev` | 同板块同名 | crate::arch::asm_helpers::rdtime, crate::arch::asm_helpers::rdtime().saturating_add, crate::sched::process::PROCESS_TABLE.lock, crate::sched::process::account_io_write, crate::sched::process::enter_wait_channel, crate::sched::process::suspend_current_for_io ...(+24) | UserBuffer::new, Vec::<IOVec>::with_capacity, Vec::with_capacity, copy_from_user_array, copy_from_user_array(token, iov as *const IOVec, iovecs.as_mut_ptr(), iovcnt).is_err, current_task ...(+11) |

#### 内存管理
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `kernel/src/syscall/process_clone.rs#sys_clone` | `os/src/task/task.rs#sys_clone` | 同板块同名 | PROCESS_TABLE.lock, Some, addr_space.handle_page_fault, addr_space.page_table, addr_space.page_table().get_pte_flags, addr_space.page_table().translate ...(+48) | Arc::downgrade, Arc::new, Futex::new, ITimerVal::new, MemorySet::from_existing_user, Mutex::new ...(+55) |
| `kernel/src/syscall/socket.rs#sys_socket` | `os/src/syscall/net.rs#sys_socket` | 同板块同名 | crate::dprintln!, crate::sched::process::PROCESS_TABLE.lock, install_socket_fd, socket::create_socket_file, socket::override_record_domain, t.current.and_then ...(+5) | <dyn Socket>::alloc, info! |
| `kernel/src/syscall/ipc.rs#sys_socketpair` | `os/src/syscall/net.rs#sys_socketpair` | 同板块同名 | Arc::new, FileDescriptor::new, core::mem::size_of::<[i32; 2]>, core::slice::from_raw_parts, crate::sched::process::PROCESS_TABLE.lock, fd_table::alloc ...(+6) | FileDescriptor::new, core::mem::size_of::<u32>, core::slice::from_raw_parts_mut, current_task, current_task().unwrap, current_task().unwrap().files.lock ...(+5) |
| `kernel/src/syscall/system.rs#sys_sysinfo` | `os/src/syscall/process.rs#sys_sysinfo` | 同板块同名 | (base as i64 + boot_off / 1_000_000_000).max, UserSlice::<u8>::new_mut, UserSlice::<u8>::new_mut(info_ptr, info_bytes.len()).write_bytes, core::mem::size_of::<KernelSysInfo>, core::slice::from_raw_parts, crate::arch::asm_helpers::rdtime ...(+9) | copy_to_user, copy_to_user(
        token,
        &Sysinfo {
            uptime: get_time_sec(),
            // Use only current sample (as average) to evaluate
            loads: [
                procs as usize * LINUX_SYSINFO_LOADS_SCALE / SEC_1_MIN,
                procs as usize * LINUX_SYSINFO_LOADS_SCALE / SEC_5_MIN,
                procs as usize * LINUX_SYSINFO_LOADS_SCALE / SEC_15_MIN,
            ],
            totalram: crate::config::MEMORY_END - crate::config::MEMORY_START,
            freeram: crate::mm::unallocated_frames() * PAGE_SIZE,
            sharedram: UNIMPLEMENT,
            bufferram: UNIMPLEMENT,
            totalswap: 0,
            freeswap: 0,
            procs,
            totalhigh: 0,
            freehigh: 0,
            mem_unit: 1,
        },
        info,
    )
    .is_err, crate::mm::unallocated_frames, current_user_token, get_time_sec, log::error! ...(+1) |

#### 中断/异常处理
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `kernel/src/arch/mod.rs#is_interrupt_enabled` | `os/src/hal/arch/loongarch64/register/base/crmd.rs#is_interrupt_enabled` | 同板块同名 | asm_helpers::read_sstatus, core::arch::asm! | self.bits.get_bit |
| `kernel/src/arch/loongarch64/trap.rs#init_secondary` | `os/src/hal/arch/riscv/mod.rs#bootstrap_init` | 同板块同结构 | 内部闭环逻辑 | 内部闭环逻辑 |
| `kernel/src/arch/riscv64/trap.rs#rearm_timer` | `os/src/hal/arch/loongarch64/trap/mod.rs#init` | 同板块同结构 | set_next_timer | set_kernel_trap_entry |
| `kernel/src/kernel/coredump.rs#build_prstatus_note` | `os/src/hal/arch/loongarch64/trap/mod.rs#trap_from_kernel` | 同模块上下文对齐/差异观察 | Vec::new, align_up, name.len, note.extend_from_slice, note.push, push_le16 ...(+2) | ((addr + i) as *mut u8).read_unaligned, ((addr + i) as *mut u8).write_unaligned, (0..sz).rev, BadV::read, BadV::read().get_vaddr, EStat::read ...(+14) |

#### 系统调用
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `kernel/src/syscall/socket.rs#sys_connect` | `os/src/syscall/net.rs#sys_connect` | 同板块同名 | (addrlen as usize).min, addr.is_null, dest[..len].copy_from_slice, read_user_buffer, socket::connect_fd, socket::connect_fd(fd, dest).map_or_else ...(+3) | get_socket!, socket.connect, socket.connect(addr_buf).unwrap, trans_ref! |
| `user/src/syscall.rs#syscall6` | `user/src/syscall.rs#syscall6` | 同板块同名 | asm! | __syscall, core::arch::asm!, unimplemented! |
| `kernel/src/syscall/socket.rs#sys_getpeername` | `os/src/syscall/net.rs#sys_getpeername` | 同板块同名 | copy_socket_addr, crate::syscall::ipc::fd_is_unix_socketpair, socket::getpeername_fd, socket_errno, unnamed_unix_addr | get_socket!, socket.peer_addr, socket.peer_addr(addr, addrlen).unwrap |
| `kernel/src/syscall/socket.rs#sys_getsockname` | `os/src/syscall/net.rs#sys_getsockname` | 同板块同名 | copy_socket_addr, crate::syscall::ipc::fd_is_unix_socketpair, socket::getsockname_fd, socket_errno, unnamed_unix_addr | get_socket!, socket.addr, socket.addr(addr, addrlen).unwrap |

#### 文件系统
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `kernel/src/lang_items.rs#panic` | `os/src/lang_items.rs#panic` | 同板块同名 | capture_backtrace_with_symbols, crate::arch::disable_interrupts, crate::arch::wfi, crate::config::is_gdb_enabled, crate::gdb::get_stub, get_hart_id ...(+9) | info.location, println!, shutdown |
| `kernel/src/syscall/mount.rs#sys_umount2` | `os/src/syscall/fs.rs#sys_umount2` | 同板块同名 | crate::fs::inotify::INOTIFY_REGISTRY.notify_unmount, crate::fs::is_mount_point, crate::fs::lookup, crate::fs::mark_mount_expired, crate::fs::mount_info_remove, crate::fs::remove_expired_mark ...(+8) | UmountFlags::from_bits, current_user_token, info!, target.is_null, translated_str, warn! |
| `kernel/src/fs/ramfs.rs#rename` | `os/src/fs/directory_tree.rs#rename` | 同板块同名 | Err, Ok, d.read_dir, drop, dst.as_dir, dst.file_type ...(+15) | Arc::downgrade, Arc::new, Err, Mutex::new, Ok, ROOT.cd_comp ...(+41) |
| `kernel/src/fs/devfs.rs#ioctl` | `os/src/fs/file_descriptor.rs#ioctl` | 同板块同名 | LOOP_STATES.lock, Some, crate::syscall::user::UserSlice::<u8>::new, crate::syscall::user::UserSlice::<u8>::new(arg as *const u8, 4).read_bytes, crate::syscall::user::UserSlice::<u8>::new_mut, crate::syscall::user::UserSlice::<u8>::new_mut(arg as *mut u8, 4)
                    .write_bytes ...(+18) | self.file.ioctl |

#### 设备驱动
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `kernel/src/driver/virtio/hal_impl.rs#mmio_phys_to_virt` | `os/src/drivers/block/virtio_blk.rs#mmio_phys_to_virt` | 同板块同名 | NonNull::new, NonNull::new(paddr as usize as *mut u8)
                .expect, NonNull::new(vaddr as *mut u8)
                .expect | NonNull::new, NonNull::new(vaddr.0 as *mut u8)
            .expect, PhysAddr, virtio_phys_to_virt |
| `kernel/src/net/socket.rs#setsockopt_fd` | `os/src/drivers/block/sata_blk.rs#pci_init` | 同模块上下文对齐/差异观察 | (0..=255).contains, (0..=5).contains, (1..=255).contains, (224..=239).contains, (88..=65535).contains, Err ...(+44) | AHCI::new, Some, Status::empty, dev.bars.iter, dev.bars.iter().enumerate, dev.bars.iter().enumerate().for_each ...(+3) |
| `kernel/src/net/socket.rs#getsockopt_fd` | `os/src/net/config.rs#new` | 同模块上下文对齐/差异观察 | Err, Ok, drop, i32_opt, linger_opt, out[..n].copy_from_slice ...(+17) | Config::new, EthernetAddress, EthernetAddress([0x02, 0x00, 0x00, 0x00, 0x00, 0x01]).into, Instant::from_millis, Interface::new, IpAddress::v4 ...(+12) |
| `kernel/src/driver/mod.rs#self_test` | `os/src/drivers/block/virtio_blk.rs#read_block` | 同模块上下文对齐/差异观察 | DRIVER_MANAGER.lock, DeviceCapManager::new, InterruptForwarder::new, MmioManager::new, Ok, PhysAddr ...(+17) | assert!, buf.chunks_mut, buf.chunks_mut(VIRT_IO_BLOCK_SZ).enumerate, self.0
                .lock, self.0
                .lock()
                .read_blocks, self.0
                .lock()
                .read_blocks(virtio_block_id as usize, chunk)
                .expect |

#### 网络协议栈
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `kernel/src/net/tcp.rs#tcp_state_transition` | `os/src/net/tcp.rs#new` | 同模块上下文对齐/差异观察 | Err, Ok, tcb.irs.wrapping_add, tcb.iss.wrapping_add, tcb.rcv_nxt.wrapping_add | Mutex::new, NET_INTERFACE.add_socket, NET_INTERFACE.poll, RNG.positive_u32, info!, socket::tcp::Socket::new ...(+2) |
| `kernel/src/net/dns.rs#poll_dns_response` | `os/src/net/udp.rs#new` | 同模块上下文对齐/差异观察 | Err, Ok, addrs.iter, core::hint::spin_loop, dns_socket.get_query_result, log::error! ...(+5) | Mutex::new, NET_INTERFACE.add_socket, NET_INTERFACE.poll, log::info!, socket::udp::PacketBuffer::new, socket::udp::Socket::new ...(+1) |
| `kernel/src/net/af_netlink.rs#build_getlink_dump` | `os/src/net/tcp.rs#drop` | 同模块上下文对齐/差异观察 | (iface.index as i32).to_ne_bytes, 0u32.to_ne_bytes, 0xFFFFFFFFu32.to_ne_bytes, 1000u32.to_ne_bytes, Vec::with_capacity, buf.extend_from_slice ...(+14) | NET_INTERFACE.poll, NET_INTERFACE.remove, NET_INTERFACE.tcp_socket, info!, socket.close, socket.is_open |
| `kernel/src/net/tcp_cong.rs#cubic_cong_avoid` | `os/src/net/mod.rs#can_bind` | 同模块上下文对齐/差异观察 | cong_clock_now, cubic_root, cubic_target, diff.max, now.saturating_sub, self.cubic.tcp_cwnd.max ...(+1) | Some, log::info!, self.0.clone, socket.loacl_endpoint, socket.loacl_endpoint().eq, socket.socket_type ...(+1) |

#### 文件系统/第三方库
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `kernel/src/fs/ext4.rs#create` | `os/src/fs/ext4/layout.rs#create` | 同板块同名 | overlay_dir.as_dir, overlay_dir.as_dir().unwrap, overlay_dir.as_dir().unwrap().create, overlay_get_or_create_dir | (InodePerm::S_IREAD | InodePerm::S_IWRITE).bits, Arc::new, InodeFileType::S_IFDIR.bits, InodeFileType::S_IFREG.bits, Mutex::new, Ok ...(+12) |
| `kernel/src/fs/ext4.rs#unlink` | `os/src/fs/ext4/layout.rs#unlink` | 同板块同名 | overlay_dir.as_dir, overlay_dir.as_dir().unwrap, overlay_dir.as_dir().unwrap().unlink, overlay_get_or_create_dir | Arc::downcast::<Ext4OSInode>, Arc::downcast::<Ext4OSInode>(parent_osinode.clone())
            .map_err, Err, Ok, dir_node.father_arc, dir_node.name.as_str ...(+14) |
| `kernel/src/fs/ext4.rs#file_type` | `os/src/fs/ext4/ext4_inode.rs#file_type` | 同板块同名 | 内部闭环逻辑 | InodeFileType::from_bits_truncate |
| `kernel/src/fs/ext4.rs#is_immutable` | `os/src/fs/ext4/layout.rs#r_ready` | 同板块同结构 | 内部闭环逻辑 | 内部闭环逻辑 |

#### 网络协议栈/第三方库
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `kernel/src/net/backend.rs#smoltcp` | `dependency/dep_iso/src/net/ethernet/intel/ixgbe.rs#new` | 同模块上下文对齐/差异观察 | Some | (IXGBE_FCRTH..IXGBE_FCRTH_END).step_by, (IXGBE_FCRTL..IXGBE_FCRTL_END).step_by, (IXGBE_FCTTV..IXGBE_FCTTV_END).step_by, EthernetAddress::from_bytes, MaybeUninit::uninit, MaybeUninit::uninit().assume_init ...(+56) |
| `kernel/src/net/backend.rs#swap_smoltcp_handle` | `dependency/virtio-drivers/src/device/socket/connectionmanager.rs#send_recv` | 同模块上下文对齐/差异观察 | SmoltcpSocketHandle::Tcp, SmoltcpSocketHandle::Udp, Some, self.is_tcp, self.is_udp | "Hello from guest".as_bytes, (hello_from_guest.len() as u32).into, (hello_from_host.len() as u32).into, 0.into, 50.into, Arc::new ...(+43) |
| `kernel/src/net/backend.rs#set_smoltcp_handle` | `dependency/virtio-drivers/src/device/socket/connectionmanager.rs#incoming_connection` | 同模块上下文对齐/差异观察 | SmoltcpSocketHandle::Tcp, Some | 0.into, 50.into, Arc::new, Mutex::new, ReadOnly::new, SocketType::Stream.into ...(+24) |
| `kernel/src/net/backend.rs#take_smoltcp` | `dependency/dep_iso/src/net/ethernet/intel/e1000.rs#new` | 同模块上下文对齐/差异观察 | self.smoltcp.take | P::alloc_dma, Vec::with_capacity, assert_eq!, debug!, e1000[E1000_ICR].read, e1000[E1000_ICR].write ...(+25) |

#### 第三方库
仅参考项目中观察到该模块代表函数，目标项目没有同模块可对齐证据：
- 参考项目：`dependency/virtio-drivers/src/queue.rs#fake_read_write_queue`，调用摘要：(*available_ring).idx.load, (*used_ring).idx.fetch_add, (*used_ring).idx.load, Vec::new, assert!, assert_eq! ...(+18)
- 参考项目：`dependency/dep_pci/pci/src/lib.rs#probe_function`，调用摘要：BAR::decode, BridgeControl::from_bits_truncate, Command::from_bits_truncate, DeviceKind::CardbusBridge, DeviceKind::Device, DeviceKind::PciBridge ...(+6)
- 参考项目：`dependency/virtio-drivers/src/device/sound/fake.rs#handle_control_request`，调用摘要：CommandCode::SOk.into, Some, Vec::new, VirtIOSndHdr {
                            command_code: CommandCode::SOk.into(),
                        }
                        .as_bytes, VirtIOSndHdr::read_from_prefix, VirtIOSndHdr::read_from_prefix(&request)
                .expect ...(+16)
- 参考项目：`dependency/dep_iso/src/block/ahci.rs#new`，调用摘要：(0..ghc.num_ports()).find, P::alloc_dma, Some, c.set_bit, debug!, ghc.enable ...(+16)

#### 通用模块/其他
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `kernel/src/random.rs#fill_bytes` | `os/src/utils/random.rs#fill_bytes` | 同板块同名 | RANDOM_STATE.lock, buf.iter_mut, crate::arch::asm_helpers::rdtime, crate::arch::asm_helpers::read_cpu_id, mix, next | dest.len, self.next_u32 |
| `kernel/src/seccomp/mod.rs#as_bytes` | `os/src/lang_items.rs#as_bytes` | 同板块同名 | buf[0..4].copy_from_slice, buf[4..8].copy_from_slice, buf[8..16].copy_from_slice, buf[off..off + 8].copy_from_slice, self.arch.to_le_bytes, self.args[i].to_le_bytes ...(+2) | core::mem::size_of::<T>, core::slice::from_raw_parts |
| `kernel/src/arch/loongarch64/timer.rs#get_time` | `os/src/hal/arch/loongarch64/time.rs#get_time` | 同板块同名 | core::arch::asm! | asm! |
| `kernel/src/benchmark/mod.rs#get_time_ns` | `os/src/timer.rs#get_time_ns` | 同板块同名 | crate::arch::asm_helpers::rdtime | get_clock_freq, get_time |


好的，审判长。基于提交的客观 AST 证据、双方模块画像及控辩双方意见，本庭现对目标项目 `[2026-T202610359999721-XX]` 与参考项目 `[2025-西北工业大学一二三队-西北工业大学]` 的同源性争议作出最终定性仲裁。

### 1. 宏观判定
- **定性结论**: **独立开发**
- **核心依据**: 客观 AST 相似度仅为 0.89%，双向覆盖率均低于 3%。双方在进程管理、内存管理等核心板块的底层架构范式存在根本性差异，所有同名函数均因调用图谱的彻底背离而被降级为弱信号，不构成代码复用或逻辑照搬的证据。

### 2. 全模块覆盖矩阵解读
| 模块 | 双方存在 | 共同指纹 | 覆盖率 (目标/参考) | 证据强度 | 解读 |
| :--- | :---: | :---: | :---: | :---: | :--- |
| **进程管理** | 是 | 2 | 0.43% / 2.99% | **弱** | 双方均实现了POSIX标准进程接口，但底层架构完全不同。目标项目基于全局 `PROCESS_TABLE` 和命名空间视图，参考项目基于处理器局部 `current_task` 和 `Arc` 任务树。同名函数调用链无重合。 |
| **任务调度/异步运行时** | 是 | 3 | 0.6% / 2.38% | **弱** | 共同指纹源于 `sys_fcntl`、`sys_unlinkat` 等系统调用，属于POSIX标准接口。目标项目调度器包含复杂的优先级提升和cgroup判定，参考项目为简单的队列调度，实现逻辑无重合。 |
| **内存管理** | 是 | 9 | 0.75% / 2.16% | **弱** | 共同指纹集中在 `sys_clone`、`sys_socket` 等跨模块系统调用。目标项目内存管理涉及页表操作和物理帧分配，参考项目基于 `MemorySet` 抽象，底层内存模型和分配策略异构。 |
| **中断/异常处理** | 是 | 2 | 4.0% / 5.0% | **弱** | 覆盖率相对较高，但共同指纹为 `is_interrupt_enabled` 等架构相关函数，其实现依赖于具体的硬件架构（RISC-V/LoongArch），属于硬件规范决定的必然相似，非逻辑复用。 |
| **系统调用** | 是 | 2 | 0.87% / 3.85% | **弱** | 共同指纹为 `sys_connect`、`syscall6` 等。`syscall6` 是用户态陷入内核的汇编封装，由指令集架构决定。其他同名系统调用均为POSIX标准，底层调用路径完全不同。 |
| **文件系统** | 是 | 6 | 0.66% / 1.65% | **弱** | 共同指纹包括 `panic`、`sys_umount2`、`rename` 等。`panic` 是语言级机制。`rename` 等VFS操作双方均调用各自独立的文件系统抽象层（`ramfs` vs `directory_tree`），无内部逻辑重合。 |
| **设备驱动** | 是 | 0 | 0.0% / 0.0% | **未形成** | 双方均有设备驱动模块，但未观察到任何共同AST指纹，表明驱动模型和硬件交互方式完全不同。 |
| **网络协议栈** | 是 | 0 | 0.0% / 0.0% | **未形成** | 双方均有网络协议栈，但未观察到任何共同AST指纹。目标项目包含netlink、DNS等复杂实现，参考项目为简单的TCP/UDP socket封装，实现规模和路径无重合。 |
| **文件系统/第三方库** | 是 | 2 | 3.08% / 0.69% | **弱** | 共同指纹为 `ext4.rs` 中的 `create`、`unlink` 等函数。目标项目通过 `overlay` 层操作，参考项目直接操作 `Ext4OSInode`，实现路径不同。此模块在参考项目中占比大，属于第三方库集成。 |
| **网络协议栈/第三方库** | 是 | 0 | 0.0% / 0.0% | **未形成** | 双方均存在，但无共同指纹。目标项目使用 `smoltcp` 协议栈，参考项目使用 `virtio-drivers` 中的网络设备驱动，属于不同第三方库的集成。 |
| **第三方库** | 仅参考 | 0 | 0.0% / 0.0% | **未形成** | 参考项目包含大量第三方库代码（如 `virtio-drivers`, `dep_pci`），目标项目无此独立模块，属于项目依赖差异。 |
| **通用模块/其他** | 是 | 7 | 0.88% / 5.93% | **弱** | 共同指纹为 `fill_bytes`、`as_bytes`、`get_time` 等基础工具函数。这些函数实现简单，多依赖于硬件架构或Rust标准库惯用法，属于公共领域逻辑。 |

### 3. 底层 AST 关键证据指纹表
| 所属板块 | 目标项目路径 | 参考项目路径 | 证据类型 | 技术说明 |
| :--- | :--- | :--- | :--- | :--- |
| 进程管理 | `kernel/src/syscall/process_exec.rs#sys_execve` | `os/src/syscall/process.rs#sys_execve` | 同板块同名 | POSIX标准接口。目标项目调用 `execve_from_kernel_args` 进行内核态参数重组；参考项目调用 `DEFAULT_SHELL.to_string` 进行用户态默认壳插入。调用流完全不同。 |
| 进程管理 | `kernel/src/syscall/process.rs#sys_exit` | `os/src/syscall/process.rs#sys_exit` | 同板块同名 | POSIX标准接口。目标项目调用 `do_clear_child_tid` 等实现复杂的futex清理；参考项目仅调用 `exit_current_and_run_next`。复杂度差异显著。 |
| 进程管理 | `kernel/src/sched/process.rs#current` | `os/src/task/processor.rs#current` | 同板块同名 | 基础函数。目标项目通过 `self.current.and_then` 配合 `self.get` 从全局表获取；参考项目通过 `self.current.as_ref().map` 从处理器局部结构获取。 |
| 内存管理 | `kernel/src/syscall/process_clone.rs#sys_clone` | `os/src/task/task.rs#sys_clone` | 同板块同名 | POSIX标准接口。目标项目调用 `addr_space.page_table` 进行页表操作；参考项目调用 `MemorySet::from_existing_user` 进行地址空间复制。内存模型不同。 |
| 系统调用 | `user/src/syscall.rs#syscall6` | `user/src/syscall.rs#syscall6` | 同板块同名 | 用户态陷入内核的汇编封装。由RISC-V/LoongArch等指令集架构的 `ecall` 指令决定，属于硬件规范决定的必然相似。 |
| 文件系统 | `kernel/src/fs/ramfs.rs#rename` | `os/src/fs/directory_tree.rs#rename` | 同板块同名 | VFS操作。目标项目操作 `ramfs` 的 `read_dir` 和 `dst.as_dir`；参考项目操作自定义 `DirectoryTreeNode` 和 `ROOT.cd_comp`。文件系统抽象层不同。 |
| 文件系统/第三方库 | `kernel/src/fs/ext4.rs#create` | `os/src/fs/ext4/layout.rs#create` | 同板块同名 | 第三方库集成。目标项目通过 `overlay_dir` 层创建；参考项目直接构造 `InodeFileType` 和 `Arc::new`。集成方式不同。 |
| 通用模块/其他 | `kernel/src/random.rs#fill_bytes` | `os/src/utils/random.rs#fill_bytes` | 同板块同名 | 基础工具函数。目标项目调用 `RANDOM_STATE.lock` 和硬件辅助函数；参考项目调用 `self.next_u32`。随机数生成策略不同。 |

### 4. 核心代码调用流对比剖析
* **【进程管理/进程创建】**
* **底层技术析理**: 双方均实现了 `sys_clone` 系统调用，但底层进程创建与内存管理模型截然不同。目标项目采用全局进程表 `PROCESS_TABLE` 管理所有进程，克隆时涉及 `addr_space.page_table` 的直接页表操作和物理帧分配，体现了Linux式的全局资源管理。参考项目则基于 `Arc` 引用计数的任务树，克隆时调用 `MemorySet::from_existing_user` 进行地址空间的逻辑复制，体现了教学OS的局部资源管理。两者在同步策略（全局锁 vs 局部锁）、内存抽象（页表 vs 地址空间）上存在根本性架构差异。
* **代码溯源链接**:
  - 目标项目: `kernel/src/syscall/process_clone.rs#sys_clone`
  - 参考项目: `os/src/task/task.rs#sys_clone`

* **【内存管理/内存映射】**
* **底层技术析理**: 双方均实现了 `sys_mmap` 系统调用，但内存映射的底层实现路径完全不同。目标项目通过 `memory_set.mmap` 进行映射，其内部依赖于复杂的物理内存分配器和页表管理。参考项目同样调用 `memory_set.mmap`，但其 `MemorySet` 是基于 `MapArea` 和 `MapPermission` 的教学OS抽象。尽管顶层函数名相似，但 `memory_set` 对象的类型、内部数据结构和映射算法在AST图谱中无任何重合，证明双方独立实现了该POSIX接口。
* **代码溯源链接**:
  - 目标项目: `kernel/src/syscall/process.rs#sys_mmap`
  - 参考项目: `os/src/syscall/process.rs#sys_mmap`

* **【系统调用/陷入内核】**
* **底层技术析理**: 双方用户态的 `syscall6` 函数均用于发起系统调用，其核心是内联汇编指令 `asm!` 或 `ecall`。这是由RISC-V/LoongArch等RISC架构的指令集规范决定的，用户态必须通过特定指令陷入内核，其汇编模板和寄存器使用约定由硬件ABI强制规定，属于公共领域知识，不构成项目间的逻辑复用。
* **代码溯源链接**:
  - 目标项目: `user/src/syscall.rs#syscall6`
  - 参考项目: `user/src/syscall.rs#syscall6`

### 5. 单方模块与降权项
*   **单方模块**: 参考项目中的 `第三方库` 模块（包含 `virtio-drivers`, `dep_pci` 等）在目标项目中无对应独立模块。这属于项目依赖和架构选择的差异，不能作为同源性证据。
*   **降权项说明**: 所有 `[同板块同名]` 函数，如 `sys_getpid`, `sys_execve`, `sys_exit` 等，其函数名由 POSIX/Linux 标准强制规定。双方调用图谱中共同出现的 `Vec::new`, `String::from`, `Some`, `Ok`, `Err`, `println!`, `format!`, `Arc::new`, `Box::new` 等均为 Rust 语言标准库或核心语法的公共领域调用。这些项在审计中已被降权，不视为项目间复用的特异性指纹。

### 6. 保守结论
经 AST 结构级深度仲裁，目标项目与参考项目的客观相似度仅为 0.89%，远低于同源性判定阈值。双方在进程管理、内存管理等核心板块展现出截然不同的架构范式：目标项目采用全局进程表、命名空间视图和复杂调度策略的 Linux 兼容内核设计；参考项目则采用处理器局部任务管理和 `Arc` 引用计数的教学 OS 设计。所有同名函数均源于 POSIX 标准或 Rust 语言惯用法，底层调用图谱无实质性重合。综合判定，目标项目为独立开发，不存在对参考项目的代码复用或逻辑抄袭，整体风险极低。

------------------------------

