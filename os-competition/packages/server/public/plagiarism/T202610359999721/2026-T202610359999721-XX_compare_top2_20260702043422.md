# 🛡️ OS 内核赛自动化代码级架构审计报告

**审计时间**: `2026-07-02 04:34:22`
**参赛作品名称**: `2026-T202610359999721-XX`
**年份**: `2026`
**学校**: `XX`
**队伍名称**: `T202610359999721`
**比赛名字**: `OS-Competition`
**比赛子赛道**: `Kernel-Implementation`
**仓库地址**: `https://gitlab.eduxiji.net/educg-group-43501-3132633/T202610359999721-2135`
**推测开发语言**: `Rust / C (由AST探针自动识别)`
**特征函数量**: `5518`

## ⚔️ 底层代码同源性对比 (AST Homology)
> 本部分由专门的代码审查智能体，对该项目与符合年份条件的历届最高相似度项目进行控制流与底层调用对比。新版报告会先给出确定性的全模块覆盖矩阵，再给出 Agent 的分模块解释，避免只围绕少量抽样函数展开。

### 🏆 Top 2 底层对标: vs `2025-火箭队-哈尔滨工业大学`（哈尔滨工业大学 / 火箭队）

#### 对方作品信息

**对方年份**: `2025`
**对方学校**: `哈尔滨工业大学`
**对方队伍名称**: `火箭队`
**对方比赛名字**: `OS-Competition`
**对方比赛子赛道**: `Kernel-Implementation`
**对方仓库地址**: `https://gitlab.eduxiji.net/educg-group-36002-2710490/T202510213995926-2475`

> 双向覆盖率：共同 AST 指纹: 62；唯一 AST 指纹: 目标项目 4367，参考项目 2341；覆盖率: 目标项目 1.42%，参考项目 2.65%

## 全模块对比总览（确定性证据）

> 总体覆盖率：共同 AST 指纹: 62；唯一 AST 指纹: 目标项目 4367，参考项目 2341；覆盖率: 目标项目 1.42%，参考项目 2.65%

### 1. 模块覆盖矩阵
| 模块 | 目标函数数 | 参考函数数 | 共同唯一 AST 指纹 | Target 覆盖 | Base 覆盖 | 状态 |
| :--- | ---: | ---: | ---: | ---: | ---: | :--- |
| 进程管理 | 597 | 534 | 1 | 0.21% | 0.67% | 双方存在且有结构重合 |
| 任务调度/异步运行时 | 538 | 552 | 5 | 1.0% | 1.07% | 双方存在且有结构重合 |
| 内存管理 | 1321 | 666 | 14 | 1.17% | 3.05% | 双方存在且有结构重合 |
| 中断/异常处理 | 51 | 41 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |
| 系统调用 | 247 | 85 | 2 | 0.87% | 2.63% | 双方存在且有结构重合 |
| 文件系统 | 1450 | 813 | 5 | 0.55% | 1.03% | 双方存在且有结构重合 |
| 设备驱动 | 116 | 185 | 1 | 0.9% | 0.69% | 双方存在且有结构重合 |
| 网络协议栈 | 179 | 156 | 2 | 1.17% | 1.47% | 双方存在且有结构重合 |
| 文件系统/第三方库 | 71 | 313 | 2 | 3.08% | 1.05% | 双方存在且有结构重合 |
| 网络协议栈/第三方库 | 4 | 0 | 0 | 0.0% | 0.0% | 仅目标项目体现 |
| 通用模块/其他 | 944 | 454 | 13 | 1.63% | 4.47% | 双方存在且有结构重合 |

### 2. 分模块证据清单

#### 进程管理
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `kernel/src/kernel/signal_syscall.rs#sys_rt_sigtimedwait` | `os/src/syscall/signal.rs#sys_rt_sigtimedwait` | 同板块同名 | (0..1_000_000_000).contains, (nsec as u64).saturating_mul, (sec as u64)
                .saturating_mul, (sec as u64)
                .saturating_mul(CLOCK_FREQ)
                .saturating_add, AtomicU32::new, Err ...(+44) | Err, Ok, SigSet::empty, TimeSpec::default, copy_from_user, copy_to_user ...(+18) |
| `kernel/src/sched/process.rs#switch_to` | `os/src/task/processor.rs#switch_to` | 同板块同名 | Some, crate::arch::asm_helpers::rdtime, crate::fs::mount::set_active_mnt_ns, crate::sched::process_sched::HART_CURRENT_PID
            .store, crate::sched::trace::record_switch, prev.cpu_kernel_ticks.saturating_add ...(+10) | self.current.time_stat, self.current.time_stat().record_switch_out, task.set_running, task.time_stat, task.time_stat().record_switch_in |
| `kernel/src/fs/procfs.rs#lookup` | `os/src/fs/proc/fd.rs#lookup` | 同板块同名 | Arc::new, Box::new, ProcInode::file, Some, component.parse::<usize>, component.parse::<usize>().is_ok ...(+18) | Dentry::negative, FdInode::new, Some, current_task, dentry
                    .flags
                    .write, dentry
                    .flags
                    .write()
                    .update_type_from_negative ...(+9) |
| `kernel/src/kernel/signal_syscall.rs#sys_rt_sigprocmask` | `os/src/syscall/signal.rs#sys_rt_sigprocmask` | 同板块同名 | Some, buf[0..8].try_into, buf[0..8].try_into().unwrap, core::mem::size_of::<u64>, crate::sched::process::PROCESS_TABLE.lock, old_mask_val.to_le_bytes ...(+5) | Err, Ok, SigSet::empty, copy_from_user, copy_to_user, core::mem::size_of::<SigSet> ...(+7) |

#### 任务调度/异步运行时
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `kernel/src/syscall/fcntl.rs#sys_fcntl` | `os/src/syscall/fs.rs#sys_fcntl` | 同板块同名 | (0..=2).contains, (0..=64).contains, Arc::as_ptr, F_UNLCK.to_le_bytes, LockOwner::Ofd, LockOwner::Pid ...(+74) | current_task, log::info!, task.fd_table, task.fd_table().fcntl |
| `kernel/src/syscall/dir.rs#sys_unlinkat` | `os/src/syscall/fs.rs#sys_unlinkat` | 同板块同名 | access_fs_errno, crate::fs::dentry_cache_remove, crate::fs::dentry_cache_remove_tree, crate::fs::is_mount_point, crate::fs::is_readonly_path, crate::fs::lookup ...(+32) | Err, Nameidata::new, Ok, c_str_to_string, debug_assert!, delete_dentry ...(+10) |
| `kernel/src/syscall/time.rs#sys_setitimer` | `os/src/syscall/util.rs#sys_setitimer` | 同板块同名 | (ITIMER_REAL..=ITIMER_PROF).contains, (it_sec as u64).saturating_mul, (it_sec as u64).saturating_mul(CLOCK_FREQ)
            .saturating_add, (it_usec as u64).saturating_mul, (old_interval_ticks % CLOCK_FREQ).saturating_mul, (old_remaining_ticks % CLOCK_FREQ).saturating_mul ...(+40) | Err, ITimerVal::default, Ok, Sig::SIGALRM.raw, Sig::SIGPROF.raw, Sig::SIGVTALRM.raw ...(+18) |
| `kernel/src/syscall/link.rs#sys_linkat` | `os/src/syscall/fs.rs#sys_linkat` | 同板块同名 | access_allowed, check_prefix_search_effective, crate::fs::is_readonly_path, crate::fs::lookup, crate::fs::lookup(&full_new).is_ok, crate::fs::lookup_no_follow ...(+27) | Err, Nameidata::new, Ok, c_str_to_string, dentry_check_access, filename_create ...(+8) |

#### 内存管理
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `kernel/src/syscall/memory.rs#sys_mmap` | `os/src/syscall/mm.rs#sys_mmap` | 同板块同名 | (PAGE_SIZE - page_offset).min, Some, VirtAddr, VirtAddr(map_addr + aligned_len - 1).floor, VirtAddr(map_addr).floor, VirtAddr(map_end - 1).floor ...(+106) | Err, MapArea::new, MmapFlags::from_bits, MmapProt::from_bits, MmapProt::from_bits(prot as u32).unwrap, Ok ...(+37) |
| `kernel/src/syscall/open.rs#sys_openat` | `os/src/syscall/fs.rs#sys_openat` | 同板块同名 | Arc::new, AtomicUsize::new, PROCESS_TABLE.lock, Some, TMP_SEQ.fetch_add, alloc::format! ...(+96) | Err, FdFlags::from, OpenFlags::from_bits, OpenFlags::from_bits(flags).ok_or, c_str_to_string, current_task ...(+8) |
| `kernel/src/sched/process_exit.rs#wait` | `os/src/task/manager.rs#wait` | 同板块同名 | PROCESS_TABLE.lock, Some, build_rusage_buf_from_ticks, c.maxrss_kb.max, candidates.is_empty, candidates.iter ...(+61) | WAIT_MANAGER.add, current_task, log::trace!, log::warn!, schedule, task.is_interrupted ...(+2) |
| `kernel/src/mm/mman.rs#sys_mremap` | `os/src/syscall/mm.rs#sys_mremap` | 同板块同名 | CHUNK.min, VirtAddr, addr_space
                            .vmas_iter, addr_space
                            .vmas_iter()
                            .any, addr_space
                        .range_fully_mapped, addr_space.range_has_mapped_page ...(+22) | Err, MapArea::new, Ok, VPNRange::new, VirtPageNum::from, area.backend_file.clone ...(+32) |

#### 中断/异常处理
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `kernel/src/arch/mod.rs#is_interrupt_enabled` | `os/src/arch/la64/register/base/crmd.rs#is_interrupt_enabled` | 同板块同名 | asm_helpers::read_sstatus, core::arch::asm! | self.bits.get_bit |
| `kernel/src/kernel/coredump.rs#build_prstatus_note` | `os/src/net/tcp.rs#accept` | 同模块上下文对齐/差异观察 | Vec::new, align_up, name.len, note.extend_from_slice, note.push, push_le16 ...(+2) | Err, LISTEN_TABLE.lock, LISTEN_TABLE.lock().get, LISTEN_TABLE.lock().get().unwrap, LISTEN_TABLE.lock().get().unwrap().accept, Ok ...(+7) |
| `kernel/src/exec/mod.rs#execve_from_kernel_args` | `os/src/signal/sig_struct copy.rs#name` | 同模块上下文对齐/差异观察 | crate::println!, crate::syscall::path::may_exec_errno, drop, exec_error_to_errno, image::do_exec_from_fs_path, matches! ...(+4) | 内部闭环逻辑 |
| `kernel/src/syscall/ioctl.rs#default_termios2` | `os/src/signal/sig_struct.rs#name` | 同模块上下文对齐/差异观察 | B38400.to_le_bytes, buf[0..4].copy_from_slice, buf[12..16].copy_from_slice, buf[17 + NCCS + 4..17 + NCCS + 8].copy_from_slice, buf[17 + NCCS..17 + NCCS + 4].copy_from_slice, buf[4..8].copy_from_slice ...(+5) | 内部闭环逻辑 |

#### 系统调用
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `user/src/syscall.rs#sys_execve` | `user/src/syscall.rs#sys_execve` | 同板块同名 | argv.len, argv.len().min, argv_ptrs.as_ptr, buf.as_ptr, buf[..len].copy_from_slice, envp.len ...(+6) | argv.as_ptr, envp.as_ptr, path.as_ptr, syscall |
| `user/src/syscall.rs#sys_open` | `user/src/syscall.rs#sys_open` | 同板块同名 | buf.as_ptr, buf[..len].copy_from_slice, path.as_bytes, path.len, path.len().min, syscall4 | path.as_ptr, syscall |
| `user/src/syscall.rs#sys_chdir` | `user/src/syscall.rs#sys_chdir` | 同板块同名 | buf.as_ptr, buf[..len].copy_from_slice, path.as_bytes, path.len, path.len().min, syscall2 | path.as_ptr, syscall |
| `kernel/src/syscall/resource.rs#sys_setrlimit` | `os/src/syscall/util.rs#sys_setrlimit` | 同板块同名 | core::ptr::null_mut, sys_prlimit64 | core::ptr::null_mut, sys_prlimit64 |

#### 文件系统
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `kernel/src/syscall/link.rs#sys_mknodat` | `os/src/syscall/fs.rs#sys_mknodat` | 同板块同名 | access_allowed, check_prefix_search_effective, crate::fs::dentry_cache_insert, crate::fs::is_readonly_path, crate::fs::lookup, crate::fs::lookup(&full).is_ok ...(+14) | DevT::new, Err, Nameidata::new, Ok, c_str_to_string, convert_old_dev_to_new ...(+9) |
| `kernel/src/syscall/dir.rs#sys_mkdirat` | `os/src/syscall/fs.rs#sys_mkdirat` | 同板块同名 | access_allowed, access_fs_errno, crate::fs::dentry_cache_insert, crate::fs::is_readonly_path, crate::fs::lookup, crate::fs::lookup_no_follow ...(+14) | Err, Nameidata::new, Ok, c_str_to_string, filename_create, log::info! ...(+2) |
| `kernel/src/lang_items.rs#panic` | `os/src/arch/la64/lang_items.rs#panic` | 同板块同名 | capture_backtrace_with_symbols, crate::arch::disable_interrupts, crate::arch::wfi, crate::config::is_gdb_enabled, crate::gdb::get_stub, get_hart_id ...(+9) | dump_backtrace, info.location, println!, shutdown |
| `kernel/src/syscall/mount.rs#sys_umount2` | `os/src/syscall/fs.rs#sys_umount2` | 同板块同名 | crate::fs::inotify::INOTIFY_REGISTRY.notify_unmount, crate::fs::is_mount_point, crate::fs::lookup, crate::fs::mark_mount_expired, crate::fs::mount_info_remove, crate::fs::remove_expired_mark ...(+8) | Ok, log::info! |

#### 设备驱动
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `kernel/src/driver/virtio/hal_impl.rs#mmio_phys_to_virt` | `os/src/arch/la64/virtio_blk.rs#mmio_phys_to_virt` | 同板块同名 | NonNull::new, NonNull::new(paddr as usize as *mut u8)
                .expect, NonNull::new(vaddr as *mut u8)
                .expect | NonNull::new, NonNull::new(paddr as _).unwrap |
| `kernel/src/net/smol_device.rs#capabilities` | `os/src/drivers/net/mod.rs#capabilities` | 同板块同名 | DeviceCapabilities::default, smoltcp::phy::ChecksumCapabilities::default | DeviceCapabilities::default |
| `kernel/src/net/smol_device.rs#receive` | `os/src/net/mod.rs#receive` | 同板块同名 | Some, crate::driver::virtio::net::recv_packet | NetRecvToken, NetSendToken, Some, dev.isok_recv, dev.isok_send, dev.recv ...(+4) |
| `kernel/src/driver/virtio/net.rs#recv` | `os/src/drivers/net/mod.rs#recv` | 同板块同名 | NetPacket::new_tx, core::mem::size_of::<VirtioNetHdr>, packet.data[..data_len].to_vec, self.rx_buffers.get, self.rx_pending.pop_front, self.rx_pending.pop_front().map ...(+3) | Some, log::error!, recv_buf.get_raw_mut_buf, recv_buf.into_buf_ptr, recv_buf.set_header_len, recv_buf.set_packet_len ...(+5) |

#### 网络协议栈
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `kernel/src/net/af_packet.rs#to_bytes` | `os/src/net/unix.rs#to_bytes` | 同板块同名 | buf[0..2].copy_from_slice, buf[12..20].copy_from_slice, buf[2..4].copy_from_slice, buf[4..8].copy_from_slice, buf[8..10].copy_from_slice, self.sll_family.to_ne_bytes ...(+3) | (off_pw_dir as u32).to_le_bytes, (off_pw_gecos as u32).to_le_bytes, (off_pw_name as u32).to_le_bytes, (off_pw_passwd as u32).to_le_bytes, (off_pw_shell as u32).to_le_bytes, (total_len as u32).to_le_bytes ...(+15) |
| `kernel/src/net/zerocopy.rs#add` | `os/src/net/mod.rs#add` | 同板块同名 | Err, Ok, self.entries.len, self.entries.push | log::error!, self.0.lock, self.0.lock().add |
| `kernel/src/net/tcp_cong.rs#name` | `os/src/net/socket.rs#name` | 同板块同名 | 内部闭环逻辑 | Err, Ok, from_ipendpoint_to_socketaddr, tcp_socket.local_addr, udp_socket.local_addr |
| `kernel/src/net/zerocopy.rs#total_len` | `os/src/net/alg.rs#total_len` | 同板块同名 | 内部闭环逻辑 | 内部闭环逻辑 |

#### 文件系统/第三方库
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `kernel/src/fs/ext4.rs#lookup` | `os/src/ext4/mod.rs#lookup` | 同板块同名 | Arc::new, AtomicBool::new, Ok, String::from, alloc::format!, busybox_path.as_str ...(+19) | Arc::downgrade, Dentry::negative, Dentry::new, PipeInode::new, SocketInode::new, Some ...(+19) |
| `kernel/src/fs/ext4.rs#create` | `os/src/ext4/mod.rs#create` | 同板块同名 | overlay_dir.as_dir, overlay_dir.as_dir().unwrap, overlay_dir.as_dir().unwrap().create, overlay_get_or_create_dir | Ext4Inode::new, Some, current_task, debug_assert!, dentry
            .flags
            .write, dentry
            .flags
            .write()
            .update_type_from_negative ...(+15) |
| `kernel/src/fs/ext4.rs#truncate` | `os/src/ext4/inode.rs#truncate` | 同板块同名 | Ok, cow.as_file, cow.as_file().unwrap, cow.as_file().unwrap().truncate, dirty_file_mark, self.promote_cow | Err, Ok, log::warn!, self.get_flags, self.get_seals, self.get_size ...(+5) |
| `kernel/src/fs/ext4.rs#rename` | `os/src/ext4/mod.rs#rename` | 同板块同名 | overlay_dir.as_dir, overlay_dir.as_dir().unwrap, overlay_dir.as_dir().unwrap().rename, overlay_get_or_create_dir | Err, Ok, Some, TimeSpec::new_wall_time, debug_assert!, ext4_inode.set_ctime ...(+30) |

#### 网络协议栈/第三方库
仅目标项目中观察到该模块代表函数，参考项目没有同模块可对齐证据：
- 目标项目：`kernel/src/net/backend.rs#smoltcp`，调用摘要：Some
- 目标项目：`kernel/src/net/backend.rs#swap_smoltcp_handle`，调用摘要：SmoltcpSocketHandle::Tcp, SmoltcpSocketHandle::Udp, Some, self.is_tcp, self.is_udp
- 目标项目：`kernel/src/net/backend.rs#set_smoltcp_handle`，调用摘要：SmoltcpSocketHandle::Tcp, Some
- 目标项目：`kernel/src/net/backend.rs#take_smoltcp`，调用摘要：self.smoltcp.take

#### 通用模块/其他
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `kernel/src/ebpf/maps.rs#update` | `os/src/signal/sig_handler.rs#update` | 同板块同名 | Err, Ok, key.len, key.to_vec, self.data.contains_key, self.data.insert ...(+3) | debug_assert!, sig.index |
| `kernel/src/livepatch/mod.rs#revert` | `os/src/signal/sig_struct copy.rs#revert` | 同板块同名 | Err, Ok, PatchError::RevertFailed, revert_func_patch, self.patches.get_mut, self.patches.get_mut(&patch_id)
            .ok_or ...(+2) | 内部闭环逻辑 |
| `kernel/src/lockfree/mod.rs#get` | `os/src/index_list/mod.rs#get` | 同板块同名 | Some, current.is_null, node.next.load, self.buckets[bucket].load, self.hash | index.get, index.get().unwrap_or, self.elems.get, self.elems.get(ndx)?.as_ref |
| `kernel/src/arch/loongarch64/timer.rs#get_time` | `os/src/arch/la64/timer.rs#get_time` | 同板块同名 | core::arch::asm! | asm! |


#### 1. 宏观判定
- **定性结论**: **独立开发** (客观 AST 相似度: 0.93%，共同 AST 指纹: 62；唯一 AST 指纹: 目标项目 4367，参考项目 2341；覆盖率: 目标项目 1.42%，参考项目 2.65%)

#### 2. 全模块覆盖矩阵解读
- **进程管理**: 双方均存在该模块。共同唯一 AST 指纹仅 1 条，目标覆盖率 0.21%，参考覆盖率 0.67%。极低的覆盖率表明双方在进程管理的数据结构、同步机制和调度逻辑上存在根本性差异。证据强度：**弱**。
- **任务调度/异步运行时**: 双方均存在该模块。共同唯一 AST 指纹 5 条，目标覆盖率 1.0%，参考覆盖率 1.07%。尽管存在少量同名系统调用，但底层调用图谱显示调度对象、时间统计模型完全不同。证据强度：**弱**。
- **内存管理**: 双方均存在该模块。共同唯一 AST 指纹 14 条，目标覆盖率 1.17%，参考覆盖率 3.05%。覆盖率差异显著，且同名函数（如 `sys_mmap`）的调用链在地址对齐、区域管理对象上完全不同。证据强度：**弱**。
- **中断/异常处理**: 双方均存在该模块。共同唯一 AST 指纹为 0，覆盖率均为 0.0%。双方在此模块无任何可对齐的底层结构指纹。证据强度：**未形成**。
- **系统调用**: 双方均存在该模块。共同唯一 AST 指纹 2 条，目标覆盖率 0.87%，参考覆盖率 2.63%。同名函数（如 `sys_setrlimit`）仅共享标准库空指针调用，核心逻辑无重合。证据强度：**弱**。
- **文件系统**: 双方均存在该模块。共同唯一 AST 指纹 5 条，目标覆盖率 0.55%，参考覆盖率 1.03%。双方在 VFS 核心对象（`ProcInode` vs `Dentry`）和挂载管理上采用完全不同的架构。证据强度：**弱**。
- **设备驱动**: 双方均存在该模块。共同唯一 AST 指纹 1 条，目标覆盖率 0.9%，参考覆盖率 0.69%。仅 `mmio_phys_to_virt` 等极少数硬件适配函数存在同名，但实现细节不同。证据强度：**弱**。
- **网络协议栈**: 双方均存在该模块。共同唯一 AST 指纹 2 条，目标覆盖率 1.17%，参考覆盖率 1.47%。同名函数（如 `to_bytes`）的序列化对象和字段完全不同。证据强度：**弱**。
- **第三方库/公共依赖**: 双方均存在该模块。共同唯一 AST 指纹 2 条，目标覆盖率 3.08%，参考覆盖率 1.05%。证据集中于 `ext4` 文件系统操作，但底层依赖的 VFS 接口和 Inode 模型完全不同。证据强度：**弱**。

#### 3. 底层 AST 关键证据指纹表
| 所属板块 | 目标项目路径 | 参考项目路径 | 证据类型 | 技术说明 |
| :--- | :--- | :--- | :--- | :--- |
| 进程管理 | `kernel/src/fs/procfs.rs#writable` | `os/src/fs/proc/interrupts.rs#writable` | 同板块底层逻辑一致 | 双方均返回布尔常量，属于 POSIX procfs 只读语义下的平凡实现，无自定义逻辑复用。 |
| 进程管理 | `kernel/src/sched/process.rs#switch_to` | `os/src/task/processor.rs#switch_to` | 同板块同名 | 目标依赖 `rdtime`、`HART_CURRENT_PID` 等硬件相关调度；参考依赖 `time_stat` 状态机。调度模型异构。 |
| 进程管理 | `kernel/src/kernel/signal_syscall.rs#sys_rt_sigtimedwait` | `os/src/syscall/signal.rs#sys_rt_sigtimedwait` | 同板块同名 | 目标采用纳秒饱和运算与原子计数器；参考采用 `SigSet` 强类型与 `copy_from_user`。时间与同步机制不同。 |
| 任务调度/异步运行时 | `kernel/src/syscall/time.rs#sys_setitimer` | `os/src/syscall/util.rs#sys_setitimer` | 同板块同名 | 目标直接进行 Tick 饱和运算；参考使用 `ITimerVal` 结构体与信号发送。定时器管理模型不同。 |
| 内存管理 | `kernel/src/syscall/memory.rs#sys_mmap` | `os/src/syscall/mm.rs#sys_mmap` | 同板块同名 | 目标基于 `VirtAddr` 进行页对齐与范围检查；参考基于 `MapArea` 与 `MmapFlags` 构建。内存区域抽象不同。 |
| 内存管理 | `kernel/src/mm/mman.rs#sys_mremap` | `os/src/syscall/mm.rs#sys_mremap` | 同板块同名 | 目标通过 `addr_space.vmas_iter()` 遍历；参考通过 `MapArea::new` 与 `VPNRange` 操作。地址空间管理模型不同。 |
| 系统调用 | `kernel/src/syscall/resource.rs#sys_setrlimit` | `os/src/syscall/util.rs#sys_setrlimit` | 同板块同名 | 双方均直接转发至 `sys_prlimit64`，并传入 `core::ptr::null_mut`，属于标准库调用。 |
| 文件系统 | `kernel/src/syscall/link.rs#sys_mknodat` | `os/src/syscall/fs.rs#sys_mknodat` | 同板块同名 | 目标依赖 `dentry_cache_insert` 等自定义缓存；参考依赖 `Nameidata` 与 `DevT`。路径解析与节点创建模型不同。 |
| 设备驱动 | `kernel/src/driver/virtio/hal_impl.rs#mmio_phys_to_virt` | `os/src/arch/la64/virtio_blk.rs#mmio_phys_to_virt` | 同板块同名 | 双方均使用 `NonNull::new` 进行指针转换，属于 Rust 标准库的不安全代码惯用法。 |
| 网络协议栈 | `kernel/src/net/af_packet.rs#to_bytes` | `os/src/net/unix.rs#to_bytes` | 同板块同名 | 目标序列化 `sockaddr_ll` 结构；参考序列化 `passwd` 相关字段。序列化对象完全不同。 |
| 第三方库/公共依赖 | `kernel/src/fs/ext4.rs#lookup` | `os/src/ext4/mod.rs#lookup` | 同板块同名 | 目标通过 `overlay_dir` 进行层叠查找；参考直接构造 `Dentry` 与 `Inode`。VFS 集成方式不同。 |

#### 4. 核心代码调用流对比剖析
* **【进程管理/调度路径】**
* **底层技术析理**: 目标项目的 `switch_to` 函数直接依赖 `crate::arch::asm_helpers::rdtime` 读取硬件时间，并操作 `HART_CURRENT_PID` 进行多核调度，同时调用 `set_active_mnt_ns` 切换挂载命名空间，其上下文切换与硬件和内核命名空间强耦合。参考项目的 `switch_to` 则完全基于 `time_stat` 对象的方法调用（`record_switch_out`/`record_switch_in`）来记录时间，并通过 `task.set_running` 改变任务状态，是一个纯粹基于软件状态机的切换模型。两者在同步原语、时间源和调度上下文管理上存在根本性架构差异。
* **代码溯源链接**:
  - 目标项目: `kernel/src/sched/process.rs#switch_to`
  - 参考项目: `os/src/task/processor.rs#switch_to`

* **【内存管理/内存映射路径】**
* **底层技术析理**: 目标项目的 `sys_mmap` 函数直接操作 `VirtAddr` 进行页对齐计算（`.floor`）和范围检查，其地址空间管理是基于裸虚拟地址的算术运算。参考项目的 `sys_mmap` 则通过 `MapArea::new` 创建区域对象，并依赖 `MmapProt::from_bits` 和 `MmapFlags::from_bits` 进行权限和标志位转换，其内存抽象层次更高，完全封装了地址操作。这表明两者在内存管理的核心抽象模型上截然不同。
* **代码溯源链接**:
  - 目标项目: `kernel/src/syscall/memory.rs#sys_mmap`
  - 参考项目: `os/src/syscall/mm.rs#sys_mmap`

* **【文件系统/路径查找路径】**
* **底层技术析理**: 目标项目的 `lookup` 函数（`procfs.rs`）通过 `component.parse::<usize>()` 解析 PID，并构造 `ProcInode::file` 或 `ProcVfsInode::new` 对象，其核心是围绕自定义的 `ProcInode` 类型。参考项目的 `lookup` 函数（`fd.rs`）则通过 `Dentry::negative` 创建负目录项，并操作 `dentry.flags.write().update_type_from_negative` 来更新缓存状态，其核心是围绕 `Dentry` 缓存和 `FdInode` 类型。两者在 VFS 节点对象和缓存策略上完全不同。
* **代码溯源链接**:
  - 目标项目: `kernel/src/fs/procfs.rs#lookup`
  - 参考项目: `os/src/fs/proc/fd.rs#lookup`

#### 5. 单方模块与降权项
- **单方模块**: `网络协议栈/第三方库` 模块仅存在于目标项目，包含 `kernel/src/net/backend.rs` 中的 `smoltcp` 相关函数。此模块为第三方库（smoltcp）的封装，其存在与否取决于项目需求，不构成同源证据。
- **降权项**: 所有 `[同板块同名]` 的系统调用（如 `sys_execve`, `sys_getpid`, `sys_exit`）均为标准 POSIX 接口，其函数名由规范定义，不能作为复用证据。所有 `[🚨同板块底层逻辑一致]` 且调用图谱为“内部闭环逻辑”的函数（如大量 `writable`, `readable`），均为返回布尔常量的平凡实现，是 POSIX 语义约束下的必然收敛，已降级为弱信号。`Arc::new`, `Box::new`, `format!` 等 Rust 标准库和语言宏的调用，属于通用工具，不构成项目间复用证据。

#### 6. 保守结论
本次审计的客观 AST 相似度仅为 0.93%，覆盖率低于 3%，这在操作系统内核级项目中属于极低水平，直接否定了“架构级克隆”或“大规模复用”的可能性。控方核心指控的 `procfs writable` 函数群，经查均为无自定义逻辑的平凡常量实现，其相似性源于 POSIX 规范约束，而非代码抄袭。所有可对齐的同名系统调用，其底层调用图谱均揭示了双方在进程模型（全局表 vs 当前任务）、内存抽象（裸地址 vs 区域对象）和文件系统（ProcInode vs Dentry）等核心架构上的根本性差异。因此，综合判定目标项目为独立开发，不构成对参考项目的代码复用。

------------------------------

