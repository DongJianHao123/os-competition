# 🛡️ OS 内核赛自动化代码级架构审计报告

**审计时间**: `2026-07-02 04:34:22`
**参赛作品名称**: `2026-T202610359999721-XX`
**年份**: `2026`
**学校**: `XX`
**队伍名称**: `T202610359999721`
**比赛名字**: `OS-Competition`
**比赛子赛道**: `Kernel-Implementation`
**仓库地址**: `https://gitlab.eduxiji.net/educg-group-43501-3132633/T202610359999721-2135`
**推测开发语言**: `Rust / C (由AST探针自动识别)`
**特征函数量**: `5518`

## ⚔️ 底层代码同源性对比 (AST Homology)
> 本部分由专门的代码审查智能体，对该项目与符合年份条件的历届最高相似度项目进行控制流与底层调用对比。新版报告会先给出确定性的全模块覆盖矩阵，再给出 Agent 的分模块解释，避免只围绕少量抽样函数展开。

### 🏆 Top 3 底层对标: vs `2025-NoAxiom-杭州电子科技大学`（杭州电子科技大学 / NoAxiom）

#### 对方作品信息

**对方年份**: `2025`
**对方学校**: `杭州电子科技大学`
**对方队伍名称**: `NoAxiom`
**对方比赛名字**: `OS-Competition`
**对方比赛子赛道**: `Kernel-Implementation`
**对方仓库地址**: `https://gitlab.eduxiji.net/educg-group-36002-2710490/T202510336995214-148`

> 双向覆盖率：共同 AST 指纹: 61；唯一 AST 指纹: 目标项目 4367，参考项目 2349；覆盖率: 目标项目 1.4%，参考项目 2.6%

## 全模块对比总览（确定性证据）

> 总体覆盖率：共同 AST 指纹: 61；唯一 AST 指纹: 目标项目 4367，参考项目 2349；覆盖率: 目标项目 1.4%，参考项目 2.6%

### 1. 模块覆盖矩阵
| 模块 | 目标函数数 | 参考函数数 | 共同唯一 AST 指纹 | Target 覆盖 | Base 覆盖 | 状态 |
| :--- | ---: | ---: | ---: | ---: | ---: | :--- |
| 进程管理 | 597 | 207 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |
| 任务调度/异步运行时 | 538 | 434 | 5 | 1.0% | 1.27% | 双方存在且有结构重合 |
| 内存管理 | 1321 | 382 | 4 | 0.33% | 1.13% | 双方存在且有结构重合 |
| 中断/异常处理 | 51 | 107 | 1 | 2.0% | 1.49% | 双方存在且有结构重合 |
| 系统调用 | 247 | 48 | 2 | 0.87% | 4.76% | 双方存在且有结构重合 |
| 文件系统 | 1450 | 877 | 5 | 0.55% | 0.69% | 双方存在且有结构重合 |
| 设备驱动 | 116 | 227 | 1 | 0.9% | 0.47% | 双方存在且有结构重合 |
| 网络协议栈 | 179 | 53 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |
| 文件系统/第三方库 | 71 | 49 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |
| 网络协议栈/第三方库 | 4 | 0 | 0 | 0.0% | 0.0% | 仅目标项目体现 |
| 通用模块/其他 | 944 | 552 | 16 | 2.01% | 3.9% | 双方存在且有结构重合 |

### 2. 分模块证据清单

#### 进程管理
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `kernel/src/gdb/protocol.rs#process` | `NoAxiom/lib/fatfs/src/dir.rs#process` | 同板块同名 | RspPacket::calculate_checksum, RspPacket::parse_hex_digit, Some, self.buffer.clear, self.buffer.len, self.buffer.push ...(+1) | 内部闭环逻辑 |
| `kernel/src/syscall/process_exec.rs#sys_execve` | `NoAxiom/kernel/src/syscall/process.rs#sys_execve` | 同板块同名 | Vec::new, args.is_empty, args.push, argv_ptr.is_null, core::mem::size_of::<usize>, crate::exec::execve_from_kernel_args ...(+6) | BUSYBOX.to_string, FileFlags::empty, Ok, SearchFlags::empty, UserPtr::<UserPtr<u8>>::new, UserPtr::<UserPtr<u8>>::new(argv).get_string_vec ...(+17) |
| `kernel/src/syscall/process_group.rs#sys_setpgid` | `NoAxiom/kernel/src/syscall/process.rs#sys_setpgid` | 同板块同名 | caller_proc.children.contains, crate::sched::process::PROCESS_TABLE.lock, guard.as_mut, table.get, table.get_mut, table.iter ...(+1) | Ok, PROCESS_GROUP_MANAGER
                .lock, PROCESS_GROUP_MANAGER
                .lock()
                .create_new_group_by, PROCESS_GROUP_MANAGER.lock, PROCESS_GROUP_MANAGER.lock().modify_pgid, TASK_MANAGER.get ...(+3) |
| `kernel/src/syscall/process_info.rs#sys_setsid` | `NoAxiom/kernel/src/syscall/process.rs#sys_setsid` | 同板块同名 | PROCESS_TABLE.lock, ns_view, table.as_mut, table.get, table.get_mut, table.iter ...(+1) | Ok, task.tgid, warn! |

#### 任务调度/异步运行时
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `kernel/src/syscall/time.rs#sys_setitimer` | `NoAxiom/kernel/src/syscall/time.rs#sys_setitimer` | 同板块同名 | (ITIMER_REAL..=ITIMER_PROF).contains, (it_sec as u64).saturating_mul, (it_sec as u64).saturating_mul(CLOCK_FREQ)
            .saturating_add, (it_usec as u64).saturating_mul, (old_interval_ticks % CLOCK_FREQ).saturating_mul, (old_remaining_ticks % CLOCK_FREQ).saturating_mul ...(+40) | Box::new, ITimer::register, ITimerReal::new, ITimerType::from_repr, ITimerType::from_repr(which).ok_or, Ok ...(+12) |
| `kernel/src/syscall/mod.rs#syscall` | `NoAxiom/kernel/src/syscall/syscall.rs#syscall` | 同板块同名 | SCDBG_DYNAMIC.load, Some, crate::arch::asm_helpers::rdtime, crate::println!, crate::sched::process::ExitReason::killed, crate::sched::process::ExitReason::signaled_with_dump ...(+12) | Syscall::new, Syscall::new(self)
            .syscall, cx.get_syscall_args, cx.get_syscall_id |
| `kernel/src/syscall/link.rs#sys_linkat` | `NoAxiom/kernel/src/syscall/fs.rs#sys_linkat` | 同板块同名 | access_allowed, check_prefix_search_effective, crate::fs::is_readonly_path, crate::fs::lookup, crate::fs::lookup(&full_new).is_ok, crate::fs::lookup_no_follow ...(+27) | AtFlags::AT_EMPTY_PATH.bits, AtFlags::AT_SYMLINK_FOLLOW.bits, AtFlags::from_bits_truncate, Err, Ok, SearchFlags::empty ...(+13) |
| `kernel/src/fs/write_io.rs#sys_writev` | `NoAxiom/kernel/src/syscall/fs.rs#sys_writev` | 同板块同名 | crate::arch::asm_helpers::rdtime, crate::arch::asm_helpers::rdtime().saturating_add, crate::sched::process::PROCESS_TABLE.lock, crate::sched::process::account_io_write, crate::sched::process::enter_wait_channel, crate::sched::process::suspend_current_for_io ...(+24) | Err, Iovec::size, Ok, UserPtr::<Iovec>::new, UserPtr::<u8>::new, buf_ptr.as_slice_const_checked ...(+10) |

#### 内存管理
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `kernel/src/syscall/mod.rs#syscall_inner` | `NoAxiom/kernel/src/syscall/syscall.rs#syscall_inner` | 同板块同名 | aio::sys_io_cancel, aio::sys_io_destroy, aio::sys_io_getevents, aio::sys_io_setup, aio::sys_io_submit, core::arch::asm! ...(+317) | Ok, Self::empty_syscall, Self::sys_gettimeofday, Self::sys_madvise, Self::sys_syslog, Self::sys_systemshutdown ...(+156) |
| `kernel/src/syscall/memory.rs#sys_mmap` | `NoAxiom/kernel/src/syscall/mm.rs#sys_mmap` | 同板块同名 | (PAGE_SIZE - page_offset).min, Some, VirtAddr, VirtAddr(map_addr + aligned_len - 1).floor, VirtAddr(map_addr).floor, VirtAddr(map_end - 1).floor ...(+106) | MmapFlags::from_bits, MmapFlags::from_bits(flags).ok_or, MmapProts::from_bits_truncate, Ok, align_ceil, flags.contains ...(+3) |
| `kernel/src/syscall/open.rs#sys_openat` | `NoAxiom/kernel/src/syscall/fs.rs#sys_openat` | 同板块同名 | Arc::new, AtomicUsize::new, PROCESS_TABLE.lock, Some, TMP_SEQ.fetch_add, alloc::format! ...(+96) | Err, FdFlags::from, FileFlags::from_bits, FileFlags::from_bits(flags).ok_or, InodeMode::from_bits, InodeMode::from_bits(mode).ok_or ...(+36) |
| `kernel/src/syscall/process_clone.rs#sys_clone` | `NoAxiom/kernel/src/syscall/process.rs#sys_clone` | 同板块同名 | PROCESS_TABLE.lock, Some, addr_space.handle_page_fault, addr_space.page_table, addr_space.page_table().get_pte_flags, addr_space.page_table().translate ...(+48) | CloneArgs::from_legacy, self.task.do_fork |

#### 中断/异常处理
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `kernel/src/arch/mod.rs#is_interrupt_enabled` | `NoAxiom/lib/arch/src/rv64/interrupt.rs#is_interrupt_enabled` | 同板块同名 | asm_helpers::read_sstatus, core::arch::asm! | riscv::register::sstatus::read, riscv::register::sstatus::read().sie |
| `kernel/src/arch/loongarch64/trap.rs#is_interrupt` | `NoAxiom/lib/arch/src/common/trap.rs#is_interrupt` | 同板块同名同结构 | matches! | matches! |
| `kernel/src/arch/loongarch64/timer.rs#clear_timer_interrupt` | `NoAxiom/lib/arch/src/rv64/time.rs#clear_timer_interrupt` | 同板块同名 | core::arch::asm! | set_timer, set_timer(u64::MAX).expect |
| `kernel/src/arch/loongarch64/mod.rs#is_interrupt_enabled` | `NoAxiom/lib/arch/src/la64/interrupt.rs#is_interrupt_enabled` | 同板块同名 | csrrd! | is_interrupt_enabled |

#### 系统调用
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `kernel/src/syscall/system.rs#sys_uname` | `NoAxiom/kernel/src/syscall/system.rs#sys_uname` | 同板块同名 | MACHINE.len, UserSlice::<u8>::new_mut, UserSlice::<u8>::new_mut(buf, utsname.len()).write_bytes, buf.is_null, get_uts_from_current_ns, release.len ...(+9) | Ok, UserPtr::<Utsname>::new, Utsname::get, buf.write |
| `kernel/src/syscall/socket.rs#sys_getsockopt` | `NoAxiom/kernel/src/syscall/net.rs#sys_getsockopt` | 同板块同名 | (value_len as u32).to_ne_bytes, len.min, optval.is_null, read_optlen, socket::getsockopt_fd, socket::validate_socket_fd ...(+4) | CONGESTION.as_bytes, CONGESTION.len, Ok, PosixSocketOption::from_repr, PosixTcpSocketOptions::from_repr, SocketLevel::try_from ...(+12) |
| `kernel/src/syscall/time.rs#sys_clock_settime` | `NoAxiom/kernel/src/syscall/time.rs#sys_clock_settime` | 同板块同名 | (0..1_000_000_000).contains, UserSlice::<u8>::new, UserSlice::<u8>::new(ts, 16).read_bytes, buf[0..8].try_into, buf[0..8].try_into().unwrap, buf[8..16].try_into ...(+6) | CLOCK_MANAGER.lock, CLOCK_MANAGER.lock().0.get_mut, ClockId::from_repr, ClockId::from_repr(clockid).ok_or, Err, Ok ...(+9) |
| `kernel/src/syscall/system.rs#sys_syslog` | `NoAxiom/kernel/src/syscall/system.rs#sys_syslog` | 同板块同名 | (1..=8).contains, buf.is_null, current_euid | Err, Ok, SyslogAction::from_repr, SyslogAction::from_repr(log_type).ok_or, UserPtr::<u8>::new, user_ptr.as_slice_const_checked |

#### 文件系统
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `kernel/src/arch/loongarch64/mod.rs#shutdown` | `NoAxiom/kernel/src/net/socketfile.rs#shutdown` | 同板块同名 | core::arch::asm! | Err, socket.shutdown |
| `kernel/src/syscall/mount.rs#sys_umount2` | `NoAxiom/kernel/src/syscall/fs.rs#sys_umount2` | 同板块同名 | crate::fs::inotify::INOTIFY_REGISTRY.notify_unmount, crate::fs::is_mount_point, crate::fs::lookup, crate::fs::mark_mount_expired, crate::fs::mount_info_remove, crate::fs::remove_expired_mark ...(+8) | Ok, UserPtr::<u8>::new, info!, ptr.get_string_from_ptr |
| `kernel/src/fs/ramfs.rs#rename` | `NoAxiom/lib/fatfs/src/dir.rs#rename` | 同板块同名 | Err, Ok, d.read_dir, drop, dst.as_dir, dst.file_type ...(+15) | Box::pin, Some, dst_dir.find_entry, e.to_dir, e.to_dir().rename, self.find_entry ...(+4) |
| `kernel/src/fs/devfs.rs#ioctl` | `NoAxiom/kernel/src/fs/vfs/impls/devfs/tty/file.rs#ioctl` | 同板块同名 | LOOP_STATES.lock, Some, crate::syscall::user::UserSlice::<u8>::new, crate::syscall::user::UserSlice::<u8>::new(arg as *const u8, 4).read_bytes, crate::syscall::user::UserSlice::<u8>::new_mut, crate::syscall::user::UserSlice::<u8>::new_mut(arg as *mut u8, 4)
                    .write_bytes ...(+18) | Ok, TtyIoctlCmd::from_repr, log::error!, log::info!, self.inner.lock, todo! ...(+1) |

#### 设备驱动
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `kernel/src/arch/loongarch64/eiointc.rs#handle_irq` | `NoAxiom/lib/driver/src/impls/interrupt/plic.rs#handle_irq` | 同板块同名 | iocsr_read32, iocsr_write32, pending.trailing_zeros | Arch::get_hartid, entry.get, entry.get().unwrap, self.handle_irq_with_dev |
| `kernel/src/console.rs#getc` | `NoAxiom/lib/driver/src/impls/char/uart8250.rs#getc` | 同板块同名 | Some, lsr.read_volatile, rbr.read_volatile | self.receive |
| `kernel/src/console.rs#putc` | `NoAxiom/lib/driver/src/impls/char/uart8250.rs#putc` | 同板块同名 | lsr.read_volatile, thr.write_volatile | self.send |
| `kernel/src/driver/virtio/mod.rs#write_reg` | `NoAxiom/lib/driver/src/impls/block/vf2_sdcard/utils.rs#write_reg` | 同板块同名 | write_volatile | get_sdio_phys_base, io.write_reg_at |

#### 网络协议栈
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `kernel/src/net/af_alg.rs#setsockopt` | `NoAxiom/kernel/src/net/socket.rs#setsockopt` | 同板块同名 | Err, crate::dprintln!, set_key | Ok |
| `kernel/src/net/tcp.rs#tcp_state_transition` | `NoAxiom/kernel/src/net/mod.rs#net_udp_test` | 同模块上下文对齐/差异观察 | Err, Ok, tcb.irs.wrapping_add, tcb.iss.wrapping_add, tcb.rcv_nxt.wrapping_add | Instant::from_millis, LoopBackDev::new, SocketSet::new, SpinLock::new, client.bind, client.bind(233).unwrap ...(+26) |
| `kernel/src/net/dns.rs#poll_dns_response` | `NoAxiom/kernel/src/net/tcpsocket.rs#poll` | 同模块上下文对齐/差异观察 | Err, Ok, addrs.iter, core::hint::spin_loop, dns_socket.get_query_result, log::error! ...(+5) | PollEvent::empty, SOCKET_SET.lock, debug!, poll_ifaces, req.contains, socket.can_recv ...(+6) |
| `kernel/src/net/af_netlink.rs#build_getlink_dump` | `NoAxiom/kernel/src/net/tcpsocket.rs#accept` | 同模块上下文对齐/差异观察 | (iface.index as i32).to_ne_bytes, 0u32.to_ne_bytes, 0xFFFFFFFFu32.to_ne_bytes, 1000u32.to_ne_bytes, Vec::with_capacity, buf.extend_from_slice ...(+14) | Err, Ok, SOCKET_SET.lock, Self::new_socket, TcpSocket::from_handle, core::mem::replace ...(+15) |

#### 文件系统/第三方库
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `kernel/src/fs/ext4.rs#create` | `NoAxiom/kernel/src/fs/vfs/impls/ext4/dentry.rs#create` | 同板块同名 | overlay_dir.as_dir, overlay_dir.as_dir().unwrap, overlay_dir.as_dir().unwrap().create, overlay_get_or_create_dir | Arc::new, Err, Ext4DirInode::new, Ext4FileInode::new, Ok, assert! ...(+42) |
| `kernel/src/fs/ext4.rs#truncate` | `NoAxiom/kernel/src/fs/vfs/impls/ext4/inode.rs#truncate` | 同板块同名 | Ok, cow.as_file, cow.as_file().unwrap, cow.as_file().unwrap().truncate, dirty_file_mark, self.promote_cow | Ok, assert!, assert_no_lock!, debug!, ext4.truncate_inode, ext4.truncate_inode(&mut inode, new as u64)
                .await
                .map_err ...(+7) |
| `kernel/src/fs/ext4.rs#root` | `NoAxiom/kernel/src/fs/vfs/impls/ext4/filesystem.rs#root` | 同板块同名 | Arc::new, String::from, self.inner.clone | Arc::new, AsyncMutex::new, DiskCursor::new, Ext4Dentry::new, Ext4DirInode::new, Ext4SuperBlock::new ...(+15) |
| `kernel/src/fs/ext4.rs#name` | `NoAxiom/kernel/src/fs/vfs/impls/ext4/filesystem.rs#name` | 同板块同名 | 内部闭环逻辑 | 内部闭环逻辑 |

#### 网络协议栈/第三方库
仅目标项目中观察到该模块代表函数，参考项目没有同模块可对齐证据：
- 目标项目：`kernel/src/net/backend.rs#smoltcp`，调用摘要：Some
- 目标项目：`kernel/src/net/backend.rs#swap_smoltcp_handle`，调用摘要：SmoltcpSocketHandle::Tcp, SmoltcpSocketHandle::Udp, Some, self.is_tcp, self.is_udp
- 目标项目：`kernel/src/net/backend.rs#set_smoltcp_handle`，调用摘要：SmoltcpSocketHandle::Tcp, Some
- 目标项目：`kernel/src/net/backend.rs#take_smoltcp`，调用摘要：self.smoltcp.take

#### 通用模块/其他
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `kernel/src/lockfree/mod.rs#push` | `NoAxiom/kernel/src/signal/sig_manager.rs#push` | 同板块同名 | QueueNode::new, Some, next.is_null, ptr::null_mut, self.len.fetch_add, self.tail.compare_exchange ...(+4) | self.pending_set.contains_signal, self.pending_set.enable, self.queue.push_back |
| `kernel/src/gdb/protocol.rs#decode` | `user/libd/src/lib_request.rs#decode` | 同板块同名 | Self::calculate_checksum, Self::parse_hex_digit, Some, Vec::new, data.iter, data.iter().position ...(+4) | Point::new, RawU24::new, RawU24::new(val).into, RequestArgType::Bool, RequestArgType::Color, RequestArgType::Id ...(+14) |
| `kernel/src/gdb/protocol.rs#encode` | `user/libd/src/lib_request.rs#encode` | 同板块同名 | Self::hex_digit, Vec::with_capacity, result.push, self.data.len | (((point.x as i16 as u32) << 16)
                | (point.y as i16 as u32) & 0xFFFF)
                .to_be_bytes, (((point.x as i16 as u32) << 16)
                | (point.y as i16 as u32) & 0xFFFF)
                .to_be_bytes()
                .to_vec, (((size.width as u32) << 16) | (size.height as u32))
                .to_be_bytes, (((size.width as u32) << 16) | (size.height as u32))
                .to_be_bytes()
                .to_vec, (if val.clone() { 1u32 } else { 0u32 })
                .to_be_bytes, (if val.clone() { 1u32 } else { 0u32 })
                .to_be_bytes()
                .to_vec ...(+13) |
| `kernel/src/ipc/mod.rs#receive` | `user/libd/src/lib_request.rs#receive` | 同板块同名 | Err, self.recv_queue.push_back, self.send_queue.is_empty, self.send_queue.pop_front, self.send_queue.pop_front().unwrap, self.update_state ...(+2) | Request::decode, Request::decode(&self.buffer[index..index + len])
                    .expect, println!, read, self.requests.pop_front, self.requests.push_back |


好的，我将依据客观 AST 证据与审计指令，对本案进行最终定性仲裁。

### 1. 宏观判定
- **定性结论**: **独立开发 / 底层架构显著差异**
- **核心依据**: 客观 AST 相似度仅为 **0.92%**，共同 AST 指纹仅 **61** 个，而双方项目体量庞大（目标项目 4367 唯一指纹，参考项目 2349 唯一指纹），双向覆盖率均低于 3%。控方集中指控的“进程管理”板块，其模块级共同 AST 指纹为 **0**，覆盖率 **0.0%**，直接否定了该板块存在结构性复用的指控。整体证据强度不足以支持代码抄袭的认定，更符合两个独立团队遵循 POSIX 规范、使用 Rust 语言开发操作系统内核时产生的领域共性噪声。

### 2. 全模块覆盖矩阵解读
- **进程管理**: 双方均存在。共同 AST 指纹为 0，覆盖率 0.0%。控方所有“强指控”均与此客观数据矛盾，辩方关于 POSIX 语义共性的解释成立。证据强度：**未形成**。
- **任务调度/异步运行时**: 双方均存在。共同 AST 指纹 5，覆盖率约 1%。重合点集中在 `sys_setitimer`、`syscall` 分发等标准系统调用路径，属于接口规范决定的相似性。证据强度：**弱**。
- **内存管理**: 双方均存在。共同 AST 指纹 4，覆盖率 < 1.2%。重合点如 `sys_mmap`、`sys_clone` 等，其底层外部调用图谱（目标项目直接操作页表，参考项目使用 `MmapFlags` 等抽象）差异显著。证据强度：**弱**。
- **中断/异常处理**: 双方均存在。共同 AST 指纹 1，覆盖率约 1.5%-2.0%。`is_interrupt` 等函数使用 Rust `matches!` 宏是标准惯用法，`is_interrupt_enabled` 等函数因 CPU 架构不同（LoongArch vs RISC-V）导致底层指令和内联汇编完全不同。证据强度：**弱**。
- **系统调用**: 双方均存在。共同 AST 指纹 2，覆盖率 0.87%-4.76%。`sys_uname`、`sys_clock_settime` 等重合点均为 Linux ABI 强制规定的同名函数，其内部实现调用的对象（`UserSlice` vs `UserPtr`）和逻辑路径完全不同。证据强度：**弱**。
- **文件系统**: 双方均存在。共同 AST 指纹 5，覆盖率 < 0.7%。`sys_umount2`、`rename` 等重合点均为 VFS 标准接口，底层文件系统实现（如 ext4、ramfs）的架构和外部依赖完全不同。证据强度：**弱**。
- **设备驱动**: 双方均存在。共同 AST 指纹 1，覆盖率 < 1%。`handle_irq`、`putc` 等函数同名但驱动硬件不同（LoongArch eiointc vs RISC-V PLIC，不同 UART 型号），底层寄存器操作完全不同。证据强度：**弱**。
- **网络协议栈**: 双方均存在。共同 AST 指纹为 0，覆盖率 0.0%。双方在网络协议栈的实现上无任何可观测的结构性重合。证据强度：**未形成**。
- **第三方库/公共依赖**: 目标项目存在 `文件系统/第三方库` 和 `网络协议栈/第三方库` 模块，参考项目存在 `文件系统/第三方库` 模块。共同 AST 指纹为 0，覆盖率 0.0%。双方依赖的第三方库或公共组件无重合。证据强度：**未形成**。

### 3. 底层 AST 关键证据指纹表
| 所属板块 | 目标项目路径 | 参考项目路径 | 证据类型 | 技术说明 |
| :--- | :--- | :--- | :--- | :--- |
| 进程管理 | `kernel/src/syscall/process_exec.rs#sys_execve` | `NoAxiom/kernel/src/syscall/process.rs#sys_execve` | 同板块同名 | Linux ABI 标准系统调用。目标项目使用 `argv_ptr.is_null` 和 `args.push` 手动解析；参考项目使用 `UserPtr::<UserPtr<u8>>::new(argv).get_string_vec` 封装解析。调用流和抽象层次完全不同。 |
| 进程管理 | `kernel/src/syscall/process_group.rs#sys_setpgid` | `NoAxiom/kernel/src/syscall/process.rs#sys_setpgid` | 同板块同名 | Linux ABI 标准系统调用。目标项目通过 `PROCESS_TABLE.lock` 和 `table.iter().any` 遍历全局表实现；参考项目通过 `PROCESS_GROUP_MANAGER.lock().create_new_group_by` 等专用管理器实现。架构设计迥异。 |
| 进程管理 | `kernel/src/fs/proc/vfs.rs#root` | `NoAxiom/kernel/src/fs/vfs/impls/proc/filesystem.rs#root` | 同板块同名 | 文件系统 trait 强制方法。目标项目是轻量代理 `ProcVfsInode::new` + `self.procfs.clone`；参考项目是重量级构造 `RamFsDentry::new` + `SuperBlockMeta::new` + 异步 `init`。实现复杂度与架构完全不同。 |
| 任务调度/异步运行时 | `kernel/src/syscall/time.rs#sys_setitimer` | `NoAxiom/kernel/src/syscall/time.rs#sys_setitimer` | 同板块同名 | Linux ABI 标准系统调用。目标项目直接进行 `saturating_mul` 等时间算术运算；参考项目通过 `ITimer::register` 和 `ITimerReal::new` 等抽象对象管理定时器。同步模型不同。 |
| 内存管理 | `kernel/src/syscall/memory.rs#sys_mmap` | `NoAxiom/kernel/src/syscall/mm.rs#sys_mmap` | 同板块同名 | Linux ABI 标准系统调用。目标项目直接操作 `VirtAddr` 和页对齐；参考项目使用 `MmapFlags::from_bits` 和 `MmapProts` 等类型安全抽象。内存管理模型不同。 |
| 系统调用 | `kernel/src/syscall/system.rs#sys_uname` | `NoAxiom/kernel/src/syscall/system.rs#sys_uname` | 同板块同名 | Linux ABI 标准系统调用。目标项目使用 `UserSlice::<u8>::new_mut` 写入；参考项目使用 `UserPtr::<Utsname>::new` 和 `buf.write`。用户态数据拷贝机制不同。 |
| 中断/异常处理 | `kernel/src/arch/loongarch64/mod.rs#is_interrupt_enabled` | `NoAxiom/lib/arch/src/la64/interrupt.rs#is_interrupt_enabled` | 同板块同名 | 目标项目使用 LoongArch `csrrd!` 宏；参考项目使用 RISC-V `riscv::register::sstatus::read`。底层 CPU 架构和指令集完全不同。 |
| 文件系统 | `kernel/src/fs/ext4.rs#root` | `NoAxiom/kernel/src/fs/vfs/impls/ext4/filesystem.rs#root` | 同板块同名 | 文件系统 trait 强制方法。目标项目是 `Arc::new` + `self.inner.clone` 的轻量封装；参考项目是 `AsyncMutex::new` + `DiskCursor::new` + `Ext4SuperBlock::new` 的完整异步构造。架构差异巨大。 |
| 设备驱动 | `kernel/src/console.rs#putc` | `NoAxiom/lib/driver/src/impls/char/uart8250.rs#putc` | 同板块同名 | 目标项目直接 `lsr.read_volatile` / `thr.write_volatile` 操作内存映射寄存器；参考项目通过 `self.send` 方法封装。硬件驱动模型不同。 |
| 网络协议栈 | `kernel/src/net/af_alg.rs#setsockopt` | `NoAxiom/kernel/src/net/socket.rs#setsockopt` | 同板块同名 | 目标项目仅返回 `Err` 或调用 `set_key`；参考项目返回 `Ok`。实现完整度与逻辑完全不同，无共同指纹。 |

### 4. 核心代码调用流对比剖析
* **【进程管理/进程创建】**
* **底层技术析理**: 双方均实现 `sys_clone` / `do_fork`，但进程创建的核心路径完全不同。目标项目 `do_fork_clone` 直接操作 `PROCESS_TABLE.lock`、`c.mm.clone`、`child_ctx.set_sp` 和 `core::ptr::write_volatile`，属于手动管理进程控制块和上下文的传统内核风格。参考项目 `do_fork` 则通过 `CloneFlags::from_bits`、`Signal::from_repr` 和 `UserPtr::<usize>::new` 等类型安全抽象进行参数解析和校验，体现了不同的设计哲学和抽象层次。
* **代码溯源链接**:
  - 目标项目: `kernel/src/syscall/process_clone.rs#do_fork_clone`
  - 参考项目: `NoAxiom/kernel/src/task/fork.rs#do_fork`

* **【内存管理/内存映射】**
* **底层技术析理**: 双方均实现 `sys_mmap`，但内存映射的实现路径截然不同。目标项目 `sys_mmap` 的调用流围绕 `VirtAddr` 的算术运算展开，如 `VirtAddr(map_addr + aligned_len - 1).floor`，直接进行地址对齐和范围检查。参考项目 `sys_mmap` 则依赖 `MmapFlags::from_bits` 和 `MmapProts::from_bits_truncate` 等位标志类型系统，将权限和标志解析与核心映射逻辑分离，展现了更强的类型安全抽象。
* **代码溯源链接**:
  - 目标项目: `kernel/src/syscall/memory.rs#sys_mmap`
  - 参考项目: `NoAxiom/kernel/src/syscall/mm.rs#sys_mmap`

* **【系统调用/分发机制】**
* **底层技术析理**: 双方的系统调用总入口 `syscall` / `syscall_inner` 虽然功能相同，但分发机制完全不同。目标项目 `syscall_inner` 是一个巨大的 `match` 语句，直接内联了数百个系统调用处理函数（如 `aio::sys_io_cancel` 等），属于扁平化分发。参考项目 `syscall_inner` 则通过 `Syscall::new(self).syscall` 将分发逻辑委托给一个 `Syscall` 对象，体现了面向对象的策略模式，架构设计迥异。
* **代码溯源链接**:
  - 目标项目: `kernel/src/syscall/mod.rs#syscall_inner`
  - 参考项目: `NoAxiom/kernel/src/syscall/syscall.rs#syscall_inner`

### 5. 单方模块与降权项
- **单方模块**: `网络协议栈/第三方库` 模块仅存在于目标项目，参考项目无此模块。这属于目标项目的独立功能扩展，不构成同源证据。
- **跨板块同名降权**: 控辩双方均提及的 `add_child`（目标项目 `kernel/src/fs/procfs.rs` vs 参考项目 `user/libd/src/lib_widget.rs`）属于内核文件系统与用户态 UI 库的跨板块同名，功能与上下文完全不同，已依规降权为弱信号，不纳入主证据。
- **通用接口降权**: `root`、`name`、`wait`、`process`、`signaled` 等函数名是文件系统 trait、同步原语、状态机的通用接口名，在大型项目中同名概率极高。其实现路径（如 `kernel/src/ipc/mod.rs#wait` vs `NoAxiom/lib/ksync/src/async_lock/barrier.rs#wait`）完全不同，不能作为同源证据。

### 6. 保守结论
本次仲裁的核心客观指标——0.92% 的 AST 相似度和 61 个共同指纹——在两个体量庞大的独立内核项目中，远未达到认定代码抄袭的阈值。控方对“进程管理”板块的指控，与“该板块共同指纹为 0”的客观事实直接矛盾，其主张的“底层 AST 同构”缺乏数据支撑。双方在进程模型、内存管理、文件系统、设备驱动等核心子系统上，展现出截然不同的架构风格、抽象层次和外部依赖，例如目标项目偏向传统宏内核的全局表遍历，而参考项目则大量采用类型安全的管理器模式。这些根本性的设计差异，是“独立开发”的有力证据。因此，综合判定两个项目为独立开发，不存在代码抄袭或结构性复用。

------------------------------

