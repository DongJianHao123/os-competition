# 🛡️ OS 内核赛自动化代码级架构审计报告

**审计时间**: `2026-07-02 04:34:22`
**参赛作品名称**: `2026-T202610359999721-XX`
**年份**: `2026`
**学校**: `XX`
**队伍名称**: `T202610359999721`
**比赛名字**: `OS-Competition`
**比赛子赛道**: `Kernel-Implementation`
**仓库地址**: `https://gitlab.eduxiji.net/educg-group-43501-3132633/T202610359999721-2135`
**推测开发语言**: `Rust / C (由AST探针自动识别)`
**特征函数量**: `5518`

## ⚔️ 底层代码同源性对比 (AST Homology)
> 本部分由专门的代码审查智能体，对该项目与符合年份条件的历届最高相似度项目进行控制流与底层调用对比。新版报告会先给出确定性的全模块覆盖矩阵，再给出 Agent 的分模块解释，避免只围绕少量抽样函数展开。

### 🏆 Top 1 底层对标: vs `2024-萌新-南开大学`（南开大学 / 萌新）

#### 对方作品信息

**对方年份**: `2024`
**对方学校**: `南开大学`
**对方队伍名称**: `萌新`
**对方比赛名字**: `OS-Competition`
**对方比赛子赛道**: `Kernel-Implementation`
**对方仓库地址**: `https://gitlab.eduxiji.net/educg-group-26010-2376550/T202410055992606-2876`

> 双向覆盖率：共同 AST 指纹: 65；唯一 AST 指纹: 目标项目 4367，参考项目 2483；覆盖率: 目标项目 1.49%，参考项目 2.62%

## 全模块对比总览（确定性证据）

> 总体覆盖率：共同 AST 指纹: 65；唯一 AST 指纹: 目标项目 4367，参考项目 2483；覆盖率: 目标项目 1.49%，参考项目 2.62%

### 1. 模块覆盖矩阵
| 模块 | 目标函数数 | 参考函数数 | 共同唯一 AST 指纹 | Target 覆盖 | Base 覆盖 | 状态 |
| :--- | ---: | ---: | ---: | ---: | ---: | :--- |
| 进程管理 | 597 | 39 | 1 | 0.21% | 3.12% | 双方存在且有结构重合 |
| 任务调度/异步运行时 | 538 | 272 | 3 | 0.6% | 1.33% | 双方存在且有结构重合 |
| 内存管理 | 1321 | 174 | 1 | 0.08% | 0.6% | 双方存在且有结构重合 |
| 中断/异常处理 | 51 | 29 | 1 | 2.0% | 4.35% | 双方存在且有结构重合 |
| 系统调用 | 247 | 67 | 1 | 0.44% | 1.61% | 双方存在且有结构重合 |
| 文件系统 | 1450 | 285 | 2 | 0.22% | 0.86% | 双方存在且有结构重合 |
| 设备驱动 | 116 | 42 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |
| 网络协议栈 | 179 | 0 | 0 | 0.0% | 0.0% | 仅目标项目体现 |
| 文件系统/第三方库 | 71 | 730 | 1 | 1.54% | 0.16% | 双方存在且有结构重合 |
| 网络协议栈/第三方库 | 4 | 118 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |
| 第三方库 | 0 | 1374 | 0 | 0.0% | 0.0% | 仅参考项目体现 |
| 通用模块/其他 | 944 | 106 | 3 | 0.38% | 3.41% | 双方存在且有结构重合 |

### 2. 分模块证据清单

#### 进程管理
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `kernel/src/fs/proc/system.rs#writable` | `modules/axtask/src/stdio.rs#executable` | 同板块同结构 | 内部闭环逻辑 | 内部闭环逻辑 |
| `kernel/src/fs/procfs.rs#writable` | `modules/axtask/src/stdio.rs#executable` | 同板块同结构 | 内部闭环逻辑 | 内部闭环逻辑 |
| `kernel/src/fs/proc/pid.rs#writable` | `modules/axtask/src/stdio.rs#executable` | 同板块同结构 | 内部闭环逻辑 | 内部闭环逻辑 |
| `kernel/src/fs/proc/pid.rs#writable` | `modules/axtask/src/stdio.rs#executable` | 同板块同结构 | 内部闭环逻辑 | 内部闭环逻辑 |

#### 任务调度/异步运行时
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `kernel/src/sched/pi_mutex.rs#lock` | `modules/axtask/src/sync/mutex.rs#lock` | 同板块同名 | core::hint::spin_loop, self.check_priority_inheritance, self.try_lock, self.waiters.lock, waiters.push_back, waiters.retain | assert_ne!, current, current().tid, panic!, self.data.get, self.owner_id.compare_exchange_weak |
| `kernel/src/fs/devfs.rs#ioctl` | `modules/axtask/src/stdio.rs#ioctl` | 同板块同名 | Some, cnt.to_le_bytes, crate::syscall::user::UserSlice::<u8>::new_mut, crate::syscall::user::UserSlice::<u8>::new_mut(arg as *mut u8, 4)
                    .write_bytes | ConsoleWinSize::default, Err, Ok, warn! |
| `kernel/src/mm/memcg.rs#add_task` | `modules/axtask/src/run_queue.rs#add_task` | 同板块同名 | MEMBERS_TOTAL.fetch_add, MEMBERS_TOTAL.fetch_sub, MEMCG.lock, c.members.iter, c.members.iter().position, c.members.push ...(+4) | debug!, self.scheduler.add_task |
| `kernel/src/sched/pi_mutex.rs#try_lock` | `modules/axtask/src/sync/mutex.rs#try_lock` | 同板块同名 | Some, self.owner.compare_exchange, self.owner.compare_exchange(
            0, tid, Ordering::Acquire, Ordering::Relaxed
        ).is_ok, self.owner_current_priority.store, self.owner_original_priority.store | Some, current, current().inner.lock, current().inner.lock().id, current().inner.lock().id().as_u64, self
            .owner_id
            .compare_exchange ...(+2) |

#### 内存管理
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `kernel/src/mm/address_space.rs#handle_page_fault` | `modules/axmem/src/area.rs#handle_page_fault` | 同板块同名 | Some, VirtAddr, best.map, crate::arch::asm_helpers::flush_tlb_va, self.lazy_free.remove, self.page_table.get_swap ...(+20) | PhysPage::alloc, PhysPage::alloc().expect, SeekFrom::Current, Some, addr.align_down_4k, addr.align_down_4k().into ...(+17) |
| `kernel/src/io_uring/ops.rs#name` | `modules/axalloc/src/lib.rs#name` | 同板块同名 | 内部闭环逻辑 | cfg_if::cfg_if! |
| `kernel/src/driver/virtio/hal_impl.rs#dma_alloc` | `modules/axdriver/src/virtio.rs#dma_alloc` | 同板块同名 | NonNull::new, NonNull::new(vaddr as *mut u8)
            .expect, alloc_frames, alloc_frames(order)
            .expect, core::ptr::write_volatile, crate::mm::phys_to_kernel_vaddr ...(+3) | NonNull::dangling, NonNull::new, NonNull::new(vaddr as _).unwrap, global_allocator, global_allocator().alloc_pages, paddr.as_usize ...(+2) |
| `kernel/src/mm/slab.rs#dealloc` | `modules/axmem/src/area.rs#dealloc` | 同板块同名 | (*header).dealloc_slot, (*header).is_empty, (*header).is_full, Some, kernel_vaddr_to_paddr, self.add_to_partial ...(+3) | page_table.unmap_region, page_table.unmap_region(self.vaddr, self.size()).unwrap, self.pages.clear, self.size |

#### 中断/异常处理
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `kernel/src/arch/riscv64/trap.rs#handle_breakpoint` | `modules/axtrap/src/arch/riscv/mod.rs#handle_breakpoint` | 同板块同名 | crate::config::is_gdb_enabled, crate::println!, gdb::get_stub, stub.breakpoint_hit, stub.is_connected | axlog::debug! |
| `kernel/src/driver/mod.rs#handle_irq` | `modules/axhal/src/platform/VisionFive2/rtc.rs#handle_irq` | 同板块同名 | self.handlers.contains_key, self.pending.lock, self.pending.lock().push | self.gold_fish_rtc
            .set_alarm, self.gold_fish_rtc.clear_irq, self.gold_fish_rtc.read_alarm_fmt, self.gold_fish_rtc.read_time_fmt, self.read_time_u64, trace! |
| `kernel/src/arch/riscv64/trap.rs#rearm_timer` | `modules/axtrap/src/arch/riscv/mod.rs#init_interrupt` | 同板块同结构 | set_next_timer | set_kernel_trap_entry |
| `kernel/src/kernel/coredump.rs#build_prstatus_note` | `modules/axhal/src/platform/VisionFive2/irq.rs#register_handler` | 同模块上下文对齐/差异观察 | Vec::new, align_up, name.len, note.extend_from_slice, note.push, push_le16 ...(+2) | with_cause! |

#### 系统调用
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `kernel/src/syscall/ipc.rs#is_unix_socketpair` | `api/linux_syscall_api/src/syscall_net/socket.rs#executable` | 同板块同结构 | 内部闭环逻辑 | 内部闭环逻辑 |
| `kernel/src/security/keyring.rs#sys_keyctl` | `api/linux_syscall_api/src/syscall_net/socket.rs#get` | 同模块上下文对齐/差异观察 | (id as i32).to_ne_bytes, (id as i32).to_ne_bytes().to_vec, KEY_ENTRIES.lock, KEY_ENTRIES.lock().remove, KeyEntry::new_keyring, REQKEY_KEYRING.lock ...(+41) | copy_nonoverlapping, drop, keep_alive.to_ne_bytes, panic!, s.keep_alive, s.keep_alive().is_some ...(+10) |
| `kernel/src/ipc/sysv_msg.rs#sys_msgrcv` | `api/linux_syscall_api/src/syscall_net/socket.rs#set` | 同模块上下文对齐/差异观察 | (msg.mtype as i64).to_le_bytes, (mtype as i64).to_le_bytes, MSG_REMOVED_IDS.lock, MSG_REMOVED_IDS.lock().contains, MSG_TABLE.lock, Some ...(+26) | <[u8; 4]>::try_from, <[u8; 4]>::try_from(&opt[0..4]).unwrap, Err, Ok, Some, axnet::Duration::from_secs ...(+14) |
| `user/src/bin/io_uring_demo.rs#print_json_output` | `api/linux_syscall_api/src/syscall_mem/imp.rs#syscall_shmget` | 同模块上下文对齐/差异观察 | get_time_ns, println! | Err, MemorySet::add_shared_mem, MemorySet::create_shared_mem, Ok, ShmFlags::from_bits, axmem::KEY_TO_SHMID.lock ...(+8) |

#### 文件系统
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `kernel/src/fs/ramfs.rs#rename` | `modules/axfs/src/fs/fatfs.rs#rename` | 同板块同名 | Err, Ok, d.read_dir, drop, dst.as_dir, dst.file_type ...(+15) | debug!, dst_path.trim_matches, self.0
            .rename, self.0
            .rename(src_path, &self.0, dst_path)
            .map_err |
| `kernel/src/fs/devfs.rs#ioctl` | `modules/axfs/src/api/port.rs#ioctl` | 同板块同名 | LOOP_STATES.lock, Some, crate::syscall::user::UserSlice::<u8>::new, crate::syscall::user::UserSlice::<u8>::new(arg as *const u8, 4).read_bytes, crate::syscall::user::UserSlice::<u8>::new_mut, crate::syscall::user::UserSlice::<u8>::new_mut(arg as *mut u8, 4)
                    .write_bytes ...(+18) | Err |
| `kernel/src/fs/ramfs.rs#create` | `modules/axfs/src/fs/fatfs.rs#create` | 同板块同名 | Arc::new, AtomicUsize::new, Err, NEXT_INO.fetch_add, Ok, RamInode::new_typed ...(+8) | Err, Ok, debug!, path.is_empty, path.strip_prefix, path.trim_matches ...(+5) |
| `kernel/src/fs/fat32.rs#mount` | `modules/axfs/src/root.rs#mount` | 同板块同名 | Box::new, Err, Ok, Vec::new, boot_data.as_ptr, boot_data.len ...(+4) | MountPoint::new, Ok, ax_err!, fs.mount, path.starts_with, self.main_fs.root_dir ...(+5) |

#### 设备驱动
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `kernel/src/driver/virtio/mod.rs#probe` | `modules/axdriver/src/lib.rs#probe` | 同板块同名 | Some, VirtioDeviceType::from, crate::println!, read_volatile | for_each_drivers!, self.probe_bus_devices |
| `kernel/src/driver/virtio/hal_impl.rs#mmio_phys_to_virt` | `modules/axdriver/src/ixgbe.rs#mmio_phys_to_virt` | 同板块同名 | NonNull::new, NonNull::new(paddr as usize as *mut u8)
                .expect, NonNull::new(vaddr as *mut u8)
                .expect | NonNull::new, NonNull::new(phys_to_virt(paddr.into()).as_mut_ptr()).unwrap, paddr.into, phys_to_virt, phys_to_virt(paddr.into()).as_mut_ptr |
| `kernel/src/driver/virtio/hal_impl.rs#share` | `modules/axdriver/src/virtio.rs#share` | 同板块同名 | buffer.as_ptr | buffer.as_ptr, vaddr.into, virt_to_phys, virt_to_phys(vaddr.into()).into |
| `kernel/src/driver/virtio/mod.rs#device_type` | `modules/axdriver/src/structs/mod.rs#device_type` | 同板块同名 | 内部闭环逻辑 | unreachable! |

#### 网络协议栈
仅目标项目中观察到该模块代表函数，参考项目没有同模块可对齐证据：
- 目标项目：`kernel/src/net/tcp.rs#tcp_state_transition`，调用摘要：Err, Ok, tcb.irs.wrapping_add, tcb.iss.wrapping_add, tcb.rcv_nxt.wrapping_add
- 目标项目：`kernel/src/net/dns.rs#poll_dns_response`，调用摘要：Err, Ok, addrs.iter, core::hint::spin_loop, dns_socket.get_query_result, log::error! ...(+5)
- 目标项目：`kernel/src/net/af_netlink.rs#build_getlink_dump`，调用摘要：(iface.index as i32).to_ne_bytes, 0u32.to_ne_bytes, 0xFFFFFFFFu32.to_ne_bytes, 1000u32.to_ne_bytes, Vec::with_capacity, buf.extend_from_slice ...(+14)
- 目标项目：`kernel/src/net/tcp_cong.rs#cubic_cong_avoid`，调用摘要：cong_clock_now, cubic_root, cubic_target, diff.max, now.saturating_sub, self.cubic.tcp_cwnd.max ...(+1)

#### 文件系统/第三方库
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `kernel/src/fs/ext4.rs#lookup` | `crates/axfs_devfs/src/dir.rs#lookup` | 同板块同名 | Arc::new, AtomicBool::new, Ok, String::from, alloc::format!, busybox_path.as_str ...(+19) | Ok, node.lookup, self
                .children
                .read, self
                .children
                .read()
                .get, self
                .children
                .read()
                .get(name)
                .cloned, self
                .children
                .read()
                .get(name)
                .cloned()
                .ok_or ...(+4) |
| `kernel/src/fs/ext4.rs#read_dir` | `crates/axfs_devfs/src/dir.rs#read_dir` | 同板块同名 | Ok, String::from, Vec::new, alloc::collections::BTreeSet::new, alloc_ino, drop ...(+19) | Ok, VfsDirEntry::new, children.iter, children.iter().skip, children.next, dirents.iter_mut ...(+7) |
| `kernel/src/fs/ext4.rs#create` | `crates/axfs_ramfs/src/dir.rs#create` | 同板块同名 | overlay_dir.as_dir, overlay_dir.as_dir().unwrap, overlay_dir.as_dir().unwrap().create, overlay_get_or_create_dir | Ok, log::debug!, name.is_empty, self
                        .children
                        .read, self
                        .children
                        .read()
                        .get, self
                        .children
                        .read()
                        .get(name)
                        .ok_or ...(+8) |
| `kernel/src/fs/ext4.rs#truncate` | `crates/rust-fatfs/src/file.rs#truncate` | 同板块同名 | Ok, cow.as_file, cow.as_file().unwrap, cow.as_file().unwrap().truncate, dirty_file_mark, self.promote_cow | Ok, debug_assert!, e.set_first_cluster, e.set_size, panic!, self.fs.fat_type ...(+3) |

#### 网络协议栈/第三方库
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `kernel/src/net/backend.rs#smoltcp` | `modules/axnet/src/smoltcp_impl/tcp.rs#connect` | 同模块上下文对齐/差异观察 | Some | Ok, Ok::<(IpEndpoint, IpEndpoint), AxError>, SOCKET_SET
                .with_socket_mut::<tcp::Socket, _, _>, SOCKET_SET.add, SocketSetWrapper::new_tcp_socket, Some ...(+23) |
| `kernel/src/net/backend.rs#swap_smoltcp_handle` | `modules/axnet/src/smoltcp_impl/tcp.rs#recv_timeout` | 同模块上下文对齐/差异观察 | SmoltcpSocketHandle::Tcp, SmoltcpSocketHandle::Udp, Some, self.is_tcp, self.is_udp | Err, Ok, SOCKET_SET.with_socket_mut::<tcp::Socket, _, _>, ax_err!, ax_err_type!, current_ticks ...(+11) |
| `kernel/src/net/backend.rs#set_smoltcp_handle` | `modules/axnet/src/smoltcp_impl/mod.rs#init` | 同模块上下文对齐/差异观察 | SmoltcpSocketHandle::Tcp, Some | Config::new, ETH0.init_by, EthernetAddress, GATEWAY.parse, GATEWAY.parse().expect, IP.parse ...(+22) |
| `kernel/src/net/backend.rs#take_smoltcp` | `modules/axnet/src/smoltcp_impl/tcp.rs#shutdown` | 同模块上下文对齐/差异观察 | self.smoltcp.take | LISTEN_TABLE.unlisten, Ok, SOCKET_SET.poll_interfaces, SOCKET_SET.with_socket_mut::<tcp::Socket, _, _>, debug!, self.handle.get ...(+9) |

#### 第三方库
仅参考项目中观察到该模块代表函数，目标项目没有同模块可对齐证据：
- 参考项目：`crates/bcm2835-sdhci/src/Bcm2835SDhci.rs#sd_card_init`，调用摘要：ACMD!, byte_swap, debug!, info!, self.cmd_timeout, self.dumpregs ...(+26)
- 参考项目：`crates/elf_parser/src/arch/x86_64.rs#get_relocate_pairs`，调用摘要：Ok, Vec::new, VirtAddr::from, assert_eq!, dyn_sym.get_name, dyn_sym.get_name(elf).unwrap ...(+23)
- 参考项目：`crates/elf_parser/src/arch/aarch64.rs#get_relocate_pairs`，调用摘要：Ok, Vec::new, VirtAddr::from, assert_eq!, dyn_sym.get_name, dyn_sym.get_name(elf).unwrap ...(+22)
- 参考项目：`crates/elf_parser/src/arch/riscv.rs#get_relocate_pairs`，调用摘要：Ok, Vec::new, VirtAddr::from, assert_eq!, dyn_sym.get_name, dyn_sym.get_name(elf).unwrap ...(+23)

#### 通用模块/其他
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `kernel/src/main.rs#clear_bss` | `modules/axhal/src/mem.rs#clear_bss` | 同板块同名 | core::slice::from_raw_parts_mut, core::slice::from_raw_parts_mut(
            sbss as usize as *mut u8,
            ebss as usize - sbss as usize,
        ).fill | core::slice::from_raw_parts_mut, core::slice::from_raw_parts_mut(_sbss as usize as *mut u8, _ebss as usize - _sbss as usize)
            .fill |
| `kernel/src/console.rs#write_str` | `modules/axlog/src/lib.rs#write_str` | 同板块同名 | Ok, s.bytes, self.putc | Ok, cfg_if::cfg_if! |
| `kernel/src/formal/mod.rs#contains` | `modules/axmem/src/area.rs#contains` | 同板块同名 | addr.checked_add, addr.checked_add(size).map_or | self.end_va |
| `kernel/src/secure/mod.rs#clear` | `modules/axsignal/src/lib.rs#clear` | 同板块同名 | 内部闭环逻辑 | self.handlers.iter_mut |


好的，我将严格遵循审计指令，基于客观 AST 证据与调用图谱，对本案进行最终同源性定性仲裁。

### 1. 宏观判定
- **定性结论**: **独立开发**
- **核心依据**: 客观 AST 相似度仅为 **0.96%**，共同 AST 指纹仅 65 个，远低于判定抄袭的阈值。双方在核心系统架构（宏内核 vs 模块化微内核）、进程调度模型、内存管理抽象、文件系统实现及高级特性集上存在根本性差异。控方指控的“底层逻辑复用”混淆了功能语义与 AST 事实，将 POSIX/Linux 强制接口约束和 Rust 语言通用范式误判为抄袭证据。

### 2. 全模块覆盖矩阵解读

| 模块 | 双方存在 | 共同 AST 指纹 | 覆盖率解读 | 证据强度 |
| :--- | :--- | :--- | :--- | :--- |
| **进程管理** | 是 | 1 | 覆盖率极低（0.21% / 3.12%）。唯一共同指纹是 `writable`/`executable` 这类零外部调用的内部闭环函数，属于信息熵极低的退化碰撞，无证明力。 | **弱** |
| **任务调度/异步运行时** | 是 | 3 | 覆盖率低（0.6% / 1.33%）。共同指纹为 `lock`/`try_lock` 等通用同步原语，以及 `add_task` 这类通用函数名。双方调度模型（全局表遍历 vs 异步队列）完全不同。 | **弱** |
| **内存管理** | 是 | 1 | 覆盖率极低（0.08% / 0.6%）。共同指纹 `handle_page_fault` 是缺页中断处理的必然命名，其内部调用流（`VirtAddr`/`PhysPage` vs `page_table`/`swap`）完全不同。 | **弱** |
| **中断/异常处理** | 是 | 1 | 覆盖率低（2.0% / 4.35%）。共同指纹 `handle_breakpoint` 是调试异常处理的通用命名，内部逻辑（GDB 对接 vs 日志打印）完全不同。 | **弱** |
| **系统调用** | 是 | 1 | 覆盖率极低（0.44% / 1.61%）。共同指纹 `is_unix_socketpair`/`executable` 为内部闭环逻辑，无外部调用图谱支撑。 | **弱** |
| **文件系统** | 是 | 2 | 覆盖率极低（0.22% / 0.86%）。共同指纹 `rename`/`create` 是 VFS 接口的通用命名，底层实现（`RamInode` vs `fatfs`）完全不同。 | **弱** |
| **设备驱动** | 是 | 0 | 覆盖率均为 0.0%。双方虽都有驱动模块，但未观察到任何共同 AST 指纹，表明驱动实现完全独立。 | **未形成** |
| **网络协议栈** | 仅目标项目 | 0 | 目标项目有 179 个函数，参考项目无此模块。这是目标项目独立开发的重要宏观证据。 | **未形成** |
| **第三方库/公共依赖** | 部分 | 1 | 覆盖率低（1.54% / 0.16%）。共同指纹 `lookup` 是文件系统查找的通用命名，底层实现（`ext4` vs `devfs`）完全不同。 | **弱** |

### 3. 底层 AST 关键证据指纹表
所有共同指纹均为语言级通用范式、POSIX 强制接口或零外部调用的退化碰撞，不具备证明同源性的强度。

| 所属板块 | 目标项目路径 | 参考项目路径 | 证据类型 | 技术说明 |
| :--- | :--- | :--- | :--- | :--- |
| 进程管理 | `kernel/src/fs/proc/pid.rs#writable` | `modules/axtask/src/stdio.rs#executable` | 退化碰撞 | 均为零外部调用的内部闭环逻辑，AST 指纹信息熵极低，无法证明语义复用。 |
| 任务调度/异步运行时 | `kernel/src/sched/pi_mutex.rs#lock` | `modules/axtask/src/sync/mutex.rs#lock` | 通用同步原语 | `lock` 是互斥锁的通用命名，内部调用流（`waiters` 队列 vs `owner_id` CAS）完全不同。 |
| 任务调度/异步运行时 | `kernel/src/mm/memcg.rs#add_task` | `modules/axtask/src/run_queue.rs#add_task` | 通用函数名 | `add_task` 是任务管理的通用命名，操作对象（`MEMCG` 内存控制组 vs `scheduler` 调度器）完全不同。 |
| 内存管理 | `kernel/src/mm/address_space.rs#handle_page_fault` | `modules/axmem/src/area.rs#handle_page_fault` | 强制接口 | `handle_page_fault` 是 MMU 缺页中断的必然处理入口，内部逻辑（`page_table.get_swap` vs `PhysPage::alloc`）完全不同。 |
| 中断/异常处理 | `kernel/src/arch/riscv64/trap.rs#handle_breakpoint` | `modules/axtrap/src/arch/riscv/mod.rs#handle_breakpoint` | 强制接口 | `handle_breakpoint` 是 RISC-V 架构调试异常的必然处理入口，内部逻辑（GDB 对接 vs 日志打印）完全不同。 |
| 系统调用 | `kernel/src/syscall/ipc.rs#is_unix_socketpair` | `api/linux_syscall_api/src/syscall_net/socket.rs#executable` | 退化碰撞 | 均为零外部调用的内部闭环逻辑，AST 指纹信息熵极低，无法证明语义复用。 |
| 文件系统 | `kernel/src/fs/ramfs.rs#rename` | `modules/axfs/src/fs/fatfs.rs#rename` | 强制接口 | `rename` 是 VFS 的通用接口，底层实现（`RamInode` 操作 vs `fatfs` 库调用）完全不同。 |
| 文件系统 | `kernel/src/fs/ramfs.rs#create` | `modules/axfs/src/fs/fatfs.rs#create` | 强制接口 | `create` 是 VFS 的通用接口，底层实现（`RamInode::new_typed` vs `fatfs` 库调用）完全不同。 |
| 文件系统/第三方库 | `kernel/src/fs/ext4.rs#lookup` | `crates/axfs_devfs/src/dir.rs#lookup` | 强制接口 | `lookup` 是 VFS 的通用接口，底层实现（`ext4` 路径解析 vs `devfs` 哈希表查找）完全不同。 |

### 4. 核心代码调用流对比剖析

* **【进程管理/进程退出与回收路径】**
* **底层技术析理**: 目标项目采用宏内核经典的全局进程表轮询模型。`waitid_reap` 函数锁定全局 `PROCESS_TABLE`，遍历 `candidates` 列表，通过 `c.exit_status.unwrap_or_else` 匹配 `ExitReason::killed` 等状态来回收僵尸进程。参考项目则采用异步运行时模型，`syscall_wait4` 通过 `WaitFuture::new` 创建一个异步等待任务，并调用 `axtask::block_task` 挂起当前任务，依赖调度器而非轮询。两者在同步/异步模型、等待机制和核心数据结构上存在根本性架构差异。
* **代码溯源链接**:
  - 目标项目: `kernel/src/sched/process_exit.rs#waitid_reap`
  - 参考项目: `api/linux_syscall_api/src/syscall_task/imp/task.rs#syscall_wait4`

* **【文件系统/Procfs 构建路径】**
* **底层技术析理**: 目标项目使用自有的 `ProcInode` 体系（`ProcInode::directory`、`ProcInode::file`）并通过 `dir.add_child` 构建目录树，其 `lookup` 函数直接调用 `component.parse::<usize>()` 来解析 `/proc/<pid>` 路径，这是 Linux procfs 接口的强制要求。参考项目则基于 `RamFileSystem` 构建，通过 `proc_root.create` 和 `proc_root.clone().lookup` 进行操作。两者底层文件系统实现、Inode 抽象和挂载方式完全不同。
* **代码溯源链接**:
  - 目标项目: `kernel/src/fs/procfs.rs#lookup`
  - 参考项目: `modules/axfs/src/mounts.rs#procfs`

* **【内存管理/缺页中断处理路径】**
* **底层技术析理**: 目标项目的 `handle_page_fault` 直接操作 `page_table` 对象，调用 `get_swap` 处理换页，并涉及 `lazy_free` 等复杂的内存回收机制。参考项目的 `handle_page_fault` 则通过 `PhysPage::alloc` 分配物理页，并调用 `page_table.map` 建立映射。两者在物理内存分配策略、页表操作抽象和错误处理路径上完全不同。
* **代码溯源链接**:
  - 目标项目: `kernel/src/mm/address_space.rs#handle_page_fault`
  - 参考项目: `modules/axmem/src/area.rs#handle_page_fault`

### 5. 单方模块与降权项
*   **网络协议栈**: 目标项目包含完整的 TCP/IP 协议栈实现（`kernel/src/net/`），拥有 179 个函数，而参考项目无此模块。这是目标项目独立开发的强有力证据。
*   **第三方库**: 参考项目包含大量第三方库（如 `bcm2835-sdhci`、`elf_parser`、`rust-fatfs`），共 1374 个函数，目标项目无此模块。这体现了双方在项目构建和依赖管理上的根本差异。
*   **降权项说明**: 所有共同指纹，如 `Arc::new`、`Box::new`、`Some`、`Ok`、`Err`、`Vec::new`、`String::from`、`saturating_add` 等，均为 Rust 语言标准库的通用构造，在任何 Rust 项目中都会高频出现，不能作为同源性证据。`writable`、`executable` 等零外部调用的 trait 空实现，其 AST 指纹信息熵极低，属于退化碰撞，同样不具备证明力。

### 6. 保守结论
综合客观 AST 证据，双方项目相似度仅为 0.96%，处于噪声水平。所有共同指纹均可被解释为 Rust 语言通用范式、POSIX/Linux 强制接口约束或零外部调用的退化碰撞。双方在核心架构（宏内核 vs 模块化OS）、进程调度模型（全局表轮询 vs 异步队列）、内存管理抽象、文件系统实现及高级特性集（如目标项目独有的网络协议栈）上存在根本性差异。控方基于功能语义推断的指控缺乏 AST 级证据支撑。因此，本庭裁定被告项目 `[2026-T202610359999721-XX]` 为独立开发，不构成对参考项目 `[2024-萌新-南开大学]` 的抄袭或底层逻辑复用。

------------------------------

