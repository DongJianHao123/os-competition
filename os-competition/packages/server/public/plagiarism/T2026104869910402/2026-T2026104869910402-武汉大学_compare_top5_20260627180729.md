# 🛡️ OS 内核赛自动化代码级架构审计报告

**审计时间**: `2026-06-27 18:07:29`
**参赛作品名称**: `2026-T2026104869910402-武汉大学`
**年份**: `2026`
**学校**: `武汉大学`
**队伍名称**: `T2026104869910402`
**比赛名字**: `OS-Competition`
**比赛子赛道**: `Kernel-Implementation`
**仓库地址**: `https://gitlab.eduxiji.net/educg-group-43501-3132633/T2026104869910402-3085`
**推测开发语言**: `Rust / C (由AST探针自动识别)`
**特征函数量**: `2458`

## ⚔️ 底层代码同源性对比 (AST Homology)
> 本部分由专门的代码审查智能体，对该项目与符合年份条件的历届最高相似度项目进行控制流与底层调用对比。新版报告会先给出确定性的全模块覆盖矩阵，再给出 Agent 的分模块解释，避免只围绕少量抽样函数展开。

### 🏆 Top 5 底层对标: vs `2025-西北工业大学一二三队-西北工业大学`（西北工业大学 / 西北工业大学一二三队）

#### 对方作品信息

**对方年份**: `2025`
**对方学校**: `西北工业大学`
**对方队伍名称**: `西北工业大学一二三队`
**对方比赛名字**: `OS-Competition`
**对方比赛子赛道**: `Kernel-Implementation`
**对方仓库地址**: `https://gitlab.eduxiji.net/educg-group-36002-2710490/T202510699995276-827`

> 双向覆盖率：共同 AST 指纹: 173；唯一 AST 指纹: 目标项目 2043，参考项目 2317；覆盖率: 目标项目 8.47%，参考项目 7.47%

## 全模块对比总览（确定性证据）

> 总体覆盖率：共同 AST 指纹: 173；唯一 AST 指纹: 目标项目 2043，参考项目 2317；覆盖率: 目标项目 8.47%，参考项目 7.47%

### 1. 模块覆盖矩阵
| 模块 | 目标函数数 | 参考函数数 | 共同唯一 AST 指纹 | Target 覆盖 | Base 覆盖 | 状态 |
| :--- | ---: | ---: | ---: | ---: | ---: | :--- |
| 进程管理 | 115 | 74 | 4 | 3.96% | 5.97% | 双方存在且有结构重合 |
| 任务调度/异步运行时 | 224 | 139 | 4 | 2.02% | 3.17% | 双方存在且有结构重合 |
| 内存管理 | 551 | 593 | 22 | 4.35% | 5.29% | 双方存在且有结构重合 |
| 中断/异常处理 | 356 | 47 | 1 | 0.34% | 2.5% | 双方存在且有结构重合 |
| 系统调用 | 153 | 65 | 2 | 2.13% | 3.85% | 双方存在且有结构重合 |
| 文件系统 | 289 | 552 | 4 | 1.68% | 1.1% | 双方存在且有结构重合 |
| 设备驱动 | 109 | 47 | 1 | 1.01% | 2.13% | 双方存在且有结构重合 |
| 网络协议栈 | 73 | 88 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |
| 文件系统/第三方库 | 410 | 369 | 118 | 36.09% | 40.83% | 双方存在且有结构重合 |
| 网络协议栈/第三方库 | 0 | 138 | 0 | 0.0% | 0.0% | 仅参考项目体现 |
| 第三方库 | 0 | 1228 | 0 | 0.0% | 0.0% | 仅参考项目体现 |
| 通用模块/其他 | 178 | 230 | 6 | 3.59% | 5.08% | 双方存在且有结构重合 |

### 2. 分模块证据清单

#### 进程管理
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `os/src/syscall/process.rs#sys_clone` | `os/src/syscall/process.rs#sys_clone` | 同板块同名 | Ok, clone_exit_signal, clone_flags_from_raw, current_process, current_process.clone_process | CloneFlags::from_bits, CloneFlags::from_bits(flags & !0xff).unwrap, Signals::empty, Signals::from_signum, add_task, current_task ...(+7) |
| `os/src/syscall/process.rs#sys_brk` | `os/src/syscall/process.rs#sys_brk` | 同板块同名 | Ok, VirtAddr::new, current_process, inner.memory_set.get_mut, inner.memory_set.get_mut().expand_heap, inner.memory_set.get_mut().shrink_heap ...(+1) | current_task, current_task().unwrap, info!, memory_set.sbrk, task.acquire_inner_lock, task.vm.lock |
| `user/src/signal.rs#sigprocmask` | `os/src/task/signal.rs#sigprocmask` | 同板块同名 | core::ptr::null, core::ptr::null_mut, oldset
        .map, oldset
        .map(|s| s as *mut SigSet as *mut u8)
        .unwrap_or, set
        .map, set
        .map(|s| s as *const SigSet as *const u8)
        .unwrap_or ...(+1) | SigMaskHow::from_bits, current_task, current_task().unwrap, inner.sigmask.insert, inner.sigmask.remove, task.acquire_inner_lock ...(+4) |
| `os/src/syscall/process.rs#sys_getppid` | `os/src/syscall/process.rs#sys_getppid` | 同板块同名 | Ok, current_process, parent.getpid, process.inner_exclusive_access, weak.upgrade | current_task, current_task().unwrap, inner.parent.as_ref, inner.parent.as_ref().unwrap, inner.parent.as_ref().unwrap().upgrade, inner.parent.as_ref().unwrap().upgrade().unwrap ...(+1) |

#### 任务调度/异步运行时
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `os/src/syscall/fs.rs#sys_fstatat` | `os/src/syscall/fs.rs#sys_fstatat` | 同板块同名 | (&attr as *const Kstat).cast::<u8>, Err, Ok, core::mem::size_of::<Kstat>, core::slice::from_raw_parts, current_process ...(+24) | FstatatFlags::from_bits, copy_to_user, copy_to_user(token, &file_descriptor.get_stat(), buf as *mut Stat).is_err, current_task, current_task().unwrap, current_user_token ...(+12) |
| `os/src/syscall/fs.rs#sys_fcntl` | `os/src/syscall/fs.rs#sys_fcntl` | 同板块同名 | Err, Ok, OpenFlags::from_bits_truncate, current_process, file.flags, file.flags().bits ...(+11) | Fcntl_Command::from_primitive, OpenFlags::O_NONBLOCK.bits, OpenFlags::O_RDWR.bits, current_task, current_task().unwrap, fd_table.get_ref ...(+10) |
| `os/src/syscall/fs.rs#sys_mkdirat` | `os/src/syscall/fs.rs#sys_mkdirat` | 同板块同名 | Err, Ok, SyscallErrNo::from, VfsFile::open, apply_umask_to_mode, current_process ...(+11) | current_task, current_task().unwrap, fd_table.get_ref, file_descriptor.clone, file_descriptor.mkdir, info! ...(+6) |
| `os/src/syscall/fs.rs#sys_write` | `os/src/syscall/fs.rs#sys_write` | 同板块同名 | Err, Ok, SyscallErrNo::from, crate::task::check_signal_for_eintr, current_process, drop ...(+11) | UserBuffer::new, current_task, current_task().unwrap, fd_table.get_ref, file_descriptor.writable, file_descriptor.write_user ...(+3) |

#### 内存管理
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `os/src/syscall/process.rs#sys_wait4` | `os/src/syscall/process.rs#sys_wait4` | 同板块同名 | Arc::strong_count, Err, Ok, block_current_task, build_rusage, child.getpid ...(+36) | WaitOption::from_bits, WaitOption::from_bits(option).unwrap, assert_eq!, block_current_and_run_next, child.acquire_inner_lock, child.getpid ...(+23) |
| `os/src/mm/memory_set.rs#new_kernel` | `os/src/mm/memory_set.rs#new_kernel` | 同板块同名 | (_ebss as usize).into, (_edata as usize).into, (_end as usize).into, (_load_end as usize).into, (_sdata as usize).into, (erodata as usize).into ...(+11) | Self::new_bare_kern, anonymous_identical_map!, memory_set.map_trampoline, println!, should_map_trampoline! |
| `os/src/mm/memory_set.rs#mprotect` | `os/src/mm/memory_set.rs#mprotect` | 同板块同名 | Err, Ok, VirtPageNum, area.vpn_range.get_end, area.vpn_range.get_start, self
                .areas
                .iter ...(+7) | Err, MapPermission::from_bits, MapPermission::from_bits(((prot as u8) << 1) | (1 << 4)).unwrap, Ok, VirtAddr::from, area.get_end::<T> ...(+18) |
| `os/src/syscall/stub.rs#sys_sysinfo` | `os/src/syscall/process.rs#sys_sysinfo` | 同板块同名 | Ok, crate::mm::frame_allocator_stats, crate::task::process_count, crate::timer::get_time_ms, current_process, info.is_null ...(+4) | copy_to_user, copy_to_user(
        token,
        &Sysinfo {
            uptime: get_time_sec(),
            // Use only current sample (as average) to evaluate
            loads: [
                procs as usize * LINUX_SYSINFO_LOADS_SCALE / SEC_1_MIN,
                procs as usize * LINUX_SYSINFO_LOADS_SCALE / SEC_5_MIN,
                procs as usize * LINUX_SYSINFO_LOADS_SCALE / SEC_15_MIN,
            ],
            totalram: crate::config::MEMORY_END - crate::config::MEMORY_START,
            freeram: crate::mm::unallocated_frames() * PAGE_SIZE,
            sharedram: UNIMPLEMENT,
            bufferram: UNIMPLEMENT,
            totalswap: 0,
            freeswap: 0,
            procs,
            totalhigh: 0,
            freehigh: 0,
            mem_unit: 1,
        },
        info,
    )
    .is_err, crate::mm::unallocated_frames, current_user_token, get_time_sec, log::error! ...(+1) |

#### 中断/异常处理
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `os/src/arch/riscv64/board/qemu_virt.rs#irq_handler` | `os/src/hal/platform/riscv/k210.rs#irq_handler` | 同板块同名 | PLIC::new, UART.handle_irq, crate::arch::block::handle_claimed_irq, crate::arch::block::handle_claimed_irq_vdb, crate::arch::virtio::block_irq, crate::arch::virtio::block_irq2 ...(+4) | unimplemented! |
| `filesystem/src/vfs_defs/inode.rs#get_size` | `os/src/hal/arch/loongarch64/trap/mem_access.rs#get_size` | 同板块同名 | Ok, self.meta.size_if_valid, self.meta.update_size, self.ops.get_size | 内部闭环逻辑 |
| `os/src/trap/mod.rs#init` | `os/src/hal/arch/riscv/mod.rs#bootstrap_init` | 同板块同结构 | 内部闭环逻辑 | 内部闭环逻辑 |
| `filesystem/src/vfs/dentry.rs#resolve_or_create_child` | `os/src/hal/arch/loongarch64/trap/mod.rs#trap_from_kernel` | 同模块上下文对齐/差异观察 | Err, Ok, child.bind_inode, child.is_negative_dentry, dcache_lookup, parent_inode.create ...(+5) | ((addr + i) as *mut u8).read_unaligned, ((addr + i) as *mut u8).write_unaligned, (0..sz).rev, BadV::read, BadV::read().get_vaddr, EStat::read ...(+14) |

#### 系统调用
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `user/src/syscall.rs#syscall` | `user/src/syscall.rs#syscall` | 同板块同名 | core::arch::asm! | __syscall, core::arch::asm!, unimplemented! |
| `user/src/syscall.rs#sys_read` | `user/src/syscall.rs#sys_read` | 同板块同名 | buffer.as_mut_ptr, buffer.len, syscall | buffer.as_mut_ptr, buffer.len, syscall |
| `user/src/syscall.rs#sys_exec` | `user/src/syscall.rs#sys_exec` | 同板块同名 | syscall | args.as_ptr, envp.as_ptr, path.as_ptr, syscall |
| `user/src/syscall.rs#sys_getpeername` | `os/src/syscall/net.rs#sys_getpeername` | 同板块同名 | syscall | get_socket!, socket.peer_addr, socket.peer_addr(addr, addrlen).unwrap |

#### 文件系统
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `filesystem/src/devfs/loop_dev.rs#read_block` | `os/src/fs/cache.rs#read_block` | 同板块同名 | buf.fill, buf[n..to_read].fill, buf[to_read..].fill, core::cmp::min, file.read_at, self.state.exclusive_access | block_device.read_block, self.buffer.as_mut |
| `os/src/fs/mod.rs#make_pipe` | `os/src/fs/dev/pipe.rs#make_pipe` | 同板块同名 | Arc::new, Inode::from_arc_ops, VfsFile::new_dev_with_flags, crate::pipe::create_pipe, flags.contains | Arc::new, Mutex::new, Pipe::read_end_with_buffer, Pipe::write_end_with_buffer, PipeRingBuffer::new, buffer.clone ...(+3) |
| `os/src/mm/memory_set.rs#from_elf` | `os/src/mm/memory_set.rs#from_elf` | 同板块同名 | (offset + buf.len() as u64).min, Self::from_elf_file, buf.len, buf[..len].copy_from_slice, elf_data.len, end.saturating_sub | Ok, Self::new_bare, memory_set.map_elf, memory_set.map_signaltrampoline, memory_set.map_trampoline, should_map_trampoline! ...(+2) |
| `os/src/lang_items.rs#panic` | `os/src/lang_items.rs#panic` | 同板块同名 | PANICKED.swap, backtrace, info.location, polyhal::println!, shutdown | info.location, println!, shutdown |

#### 设备驱动
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `os/src/drivers/block/virtio_blk.rs#read_block` | `os/src/drivers/block/virtio_blk.rs#read_block` | 同板块同名 | DEV_NON_BLOCKING_ACCESS.load, self.read_block_nb, self.virtio_blk
                .exclusive_access, self.virtio_blk
                .exclusive_access()
                .read_blocks, self.virtio_blk
                .exclusive_access()
                .read_blocks(block_id, buf)
                .expect | assert!, buf.chunks_mut, buf.chunks_mut(VIRT_IO_BLOCK_SZ).enumerate, self.0
                .lock, self.0
                .lock()
                .read_blocks, self.0
                .lock()
                .read_blocks(virtio_block_id as usize, chunk)
                .expect |
| `os/src/drivers/block/virtio_blk.rs#write_block` | `os/src/drivers/block/virtio_blk.rs#write_block` | 同板块同名 | DEV_NON_BLOCKING_ACCESS.load, self.virtio_blk
                .exclusive_access, self.virtio_blk
                .exclusive_access()
                .write_blocks, self.virtio_blk
                .exclusive_access()
                .write_blocks(block_id, buf)
                .expect, self.write_block_nb | assert!, buf.chunks, buf.chunks(VIRT_IO_BLOCK_SZ).enumerate, self.0
                .lock, self.0
                .lock()
                .write_blocks, self.0
                .lock()
                .write_blocks(virtio_block_id as usize, chunk)
                .expect |
| `os/src/arch/loongarch64/board/qemu_virt.rs#device_init` | `os/src/hal/platform/riscv/k210.rs#device_init` | 同板块同名 | enable_external_irq_line, extioi_route_all_to_ip1, polyhal::irq::IRQ::irq_enable | unimplemented! |
| `os/src/drivers/bus/virtio.rs#mmio_phys_to_virt` | `os/src/drivers/block/virtio_blk.rs#mmio_phys_to_virt` | 同板块同名 | NonNull::new, NonNull::new((paddr | polyhal::consts::VIRT_ADDR_START) as *mut u8)
            .expect | NonNull::new, NonNull::new(vaddr.0 as *mut u8)
            .expect, PhysAddr, virtio_phys_to_virt |

#### 网络协议栈
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `patches/lose-net-stack/src/connection/tcp.rs#readable` | `os/src/net/udp.rs#readable` | 同板块同名 | Ok, conn.readable, self.active_client, self.is_client.read, self.is_client.read().clone, self.wait_queue.lock ...(+1) | todo! |
| `patches/lose-net-stack/src/connection/tcp.rs#writable` | `os/src/net/unix.rs#writable` | 同板块同名 | Ok, conn.writable, self.active_client, self.is_client.read, self.is_client.read().clone | todo! |
| `patches/lose-net-stack/src/connection/tcp.rs#writable` | `os/src/net/tcp.rs#writable` | 同板块同名 | Ok, matches!, self.peer_writable, self.remote_closed.read, self.status.read | 内部闭环逻辑 |
| `patches/lose-net-stack/src/connection/tcp.rs#readable` | `os/src/net/unix.rs#readable` | 同板块同名 | Ok, self.queue_has_data, self.remote_closed.read | todo! |

#### 文件系统/第三方库
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `filesystem/ext4_rs/src/ext4_impls/file.rs#write_at` | `os/src/fs/ext4/file.rs#write_at` | 同板块同名 | Block::load, Err, Ok, alloc::collections::BTreeSet::new, alloc::vec!, allocated_blocks.iter ...(+24) | Block::load_offset, Ok, block.sync_blk_to_disk, block.write_offset, drop, inode_ref.inode.set_size ...(+10) |
| `filesystem/ext4_rs/src/ext4_impls/extents.rs#ext_remove_leaf` | `os/src/fs/ext4/extent.rs#ext_remove_leaf` | 同板块同名 | Block::load, Block::load_inode_root_block, Ext4Extent::default, Ok, Vec::new, core::slice::from_raw_parts ...(+25) | Block::load_inode_root_block, Block::load_offset, Ext4Extent::default, Ok, ex.get_actual_len, ex.get_pblock ...(+14) |
| `filesystem/ext4_rs/src/ext4_impls/extents.rs#extent_remove_space` | `os/src/fs/ext4/extent.rs#extent_remove_space` | 同板块同名 | Block::load, Ext4Extent::default, Ok, ex.get_actual_len, ex.get_first_block, ex.get_pblock ...(+13) | Block::load_offset, Ext4Extent::default, Ok, ex.get_actual_len, ex.get_first_block, ex.get_pblock ...(+14) |
| `filesystem/ext4_rs/src/ext4_impls/extents.rs#insert_new_extent` | `os/src/fs/ext4/extent.rs#insert_new_extent` | 同板块同名 | Block::load, Ok, core::mem::size_of::<Ext4Extent>, core::mem::size_of::<Ext4ExtentHeader>, ext4block.read_offset_as_mut, ext4block.sync_blk_to_disk ...(+10) | Block::load_offset, Ok, core::mem::size_of::<Ext4Extent>, core::mem::size_of::<Ext4ExtentHeader>, ext4block.read_offset_as_mut, ext4block.sync_blk_to_disk ...(+5) |

#### 网络协议栈/第三方库
仅参考项目中观察到该模块代表函数，目标项目没有同模块可对齐证据：
- 参考项目：`dependency/dep_iso/src/net/ethernet/intel/ixgbe.rs#new`，调用摘要：(IXGBE_FCRTH..IXGBE_FCRTH_END).step_by, (IXGBE_FCRTL..IXGBE_FCRTL_END).step_by, (IXGBE_FCTTV..IXGBE_FCTTV_END).step_by, EthernetAddress::from_bytes, MaybeUninit::uninit, MaybeUninit::uninit().assume_init ...(+56)
- 参考项目：`dependency/virtio-drivers/src/device/socket/connectionmanager.rs#send_recv`，调用摘要："Hello from guest".as_bytes, (hello_from_guest.len() as u32).into, (hello_from_host.len() as u32).into, 0.into, 50.into, Arc::new ...(+43)
- 参考项目：`dependency/virtio-drivers/src/device/socket/connectionmanager.rs#incoming_connection`，调用摘要：0.into, 50.into, Arc::new, Mutex::new, ReadOnly::new, SocketType::Stream.into ...(+24)
- 参考项目：`dependency/dep_iso/src/net/ethernet/intel/e1000.rs#new`，调用摘要：P::alloc_dma, Vec::with_capacity, assert_eq!, debug!, e1000[E1000_ICR].read, e1000[E1000_ICR].write ...(+25)

#### 第三方库
仅参考项目中观察到该模块代表函数，目标项目没有同模块可对齐证据：
- 参考项目：`dependency/virtio-drivers/src/queue.rs#fake_read_write_queue`，调用摘要：(*available_ring).idx.load, (*used_ring).idx.fetch_add, (*used_ring).idx.load, Vec::new, assert!, assert_eq! ...(+18)
- 参考项目：`dependency/dep_pci/pci/src/lib.rs#probe_function`，调用摘要：BAR::decode, BridgeControl::from_bits_truncate, Command::from_bits_truncate, DeviceKind::CardbusBridge, DeviceKind::Device, DeviceKind::PciBridge ...(+6)
- 参考项目：`dependency/virtio-drivers/src/device/sound/fake.rs#handle_control_request`，调用摘要：CommandCode::SOk.into, Some, Vec::new, VirtIOSndHdr {
                            command_code: CommandCode::SOk.into(),
                        }
                        .as_bytes, VirtIOSndHdr::read_from_prefix, VirtIOSndHdr::read_from_prefix(&request)
                .expect ...(+16)
- 参考项目：`dependency/dep_iso/src/block/ahci.rs#new`，调用摘要：(0..ghc.num_ports()).find, P::alloc_dma, Some, c.set_bit, debug!, ghc.enable ...(+16)

#### 通用模块/其他
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `user/src/lib.rs#_start` | `user/src/lib.rs#_start` | 同板块同名 | ((argv + i * core::mem::size_of::<usize>()) as *const usize).read_volatile, ((str_start + *i) as *const u8).read_volatile, (0usize..)
            .find, (0usize..)
            .find(|i| unsafe { ((str_start + *i) as *const u8).read_volatile() == 0 })
            .unwrap, HEAP.lock, HEAP.lock()
            .init ...(+10) | _parameter, asm!, core::arch::asm!, unreachable! |
| `patches/polyhal-boot/src/arch/loongarch64.rs#_start` | `os/src/hal/arch/loongarch64/boot.rs#_start` | 同板块同名 | naked_asm! | naked_asm! |
| `os/src/timer.rs#set_next_trigger` | `os/src/hal/arch/riscv/time.rs#set_next_trigger` | 同板块同名 | Duration::from_nanos, current_time, set_next_timer | get_time, set_timer |
| `patches/polyhal/src/debug_console/mod.rs#log_init` | `os/src/console.rs#log_init` | 同板块同名 | log::set_logger, log::set_logger(&DebugConsole).unwrap, log::set_max_level, option_env! | log::set_logger, log::set_logger(&LOGGER).unwrap, log::set_max_level, option_env! |


#### 1. 宏观判定
- **定性结论**: 独立开发 (相似度: 4.13%，共同 AST 指纹: 173；唯一 AST 指纹: 目标项目 2043，参考项目 2317；覆盖率: 目标项目 8.47%，参考项目 7.47%)

#### 2. 全模块覆盖矩阵解读

- **进程管理**
  1. 双方均存在该模块。
  2. 存在 4 个共同 AST 指纹。
  3. 目标覆盖率 3.96%，参考覆盖率 5.97%，均处于极低水平。双方在进程抽象（Process vs Task）、同步原语（exclusive_access vs lock）及用户态初始化路径上存在系统性差异。
  4. 证据强度：弱。

- **任务调度/异步运行时**
  1. 双方均存在该模块。
  2. 存在 4 个共同 AST 指纹。
  3. 目标覆盖率 2.02%，参考覆盖率 3.17%。共同指纹集中于 `sys_fstatat`、`sys_fcntl` 等标准系统调用，其内部实现逻辑（如文件描述符获取、标志位处理）差异显著。
  4. 证据强度：弱。

- **内存管理**
  1. 双方均存在该模块。
  2. 存在 22 个共同 AST 指纹。
  3. 目标覆盖率 4.35%，参考覆盖率 5.29%。共同指纹多源于 `memory_set` 等标准内核概念，但堆管理接口（`expand_heap`/`shrink_heap` vs `sbrk`）和内存映射实现完全不同。
  4. 证据强度：弱。

- **中断/异常处理**
  1. 双方均存在该模块。
  2. 存在 1 个共同 AST 指纹。
  3. 目标覆盖率 0.34%，参考覆盖率 2.5%。双方硬件抽象层（`polyhal` vs 自研 `hal`）和 Trap 处理框架完全不同，共同指纹仅为极简初始化函数。
  4. 证据强度：未形成。

- **系统调用**
  1. 双方均存在该模块。
  2. 存在 2 个共同 AST 指纹。
  3. 目标覆盖率 2.13%，参考覆盖率 3.85%。共同指纹为用户态 syscall 封装，其内核实现路径（如 `sys_clone`、`sys_brk`）在对象模型上完全不同。
  4. 证据强度：未形成。

- **文件系统**
  1. 双方均存在该模块。
  2. 存在 4 个共同 AST 指纹。
  3. 目标覆盖率 1.68%，参考覆盖率 1.1%。双方均实现了 ext4 支持，但被告使用独立的 `ext4_rs` crate，原告为自研模块，架构来源不同。
  4. 证据强度：弱。

- **设备驱动**
  1. 双方均存在该模块。
  2. 存在 1 个共同 AST 指纹。
  3. 目标覆盖率 1.01%，参考覆盖率 2.13%。共同指纹为 `virtio_blk` 的 `read_block`/`write_block`，但设备访问模型（非阻塞 vs 阻塞）和锁粒度完全不同。
  4. 证据强度：弱。

- **网络协议栈**
  1. 双方均存在该模块。
  2. 不存在共同 AST 指纹。
  3. 目标覆盖率 0.0%，参考覆盖率 0.0%。被告使用 `lose-net-stack` 补丁，原告为自研模块，无任何结构重合。
  4. 证据强度：未形成。

- **第三方库/公共依赖**
  1. 双方均存在该模块（文件系统/第三方库）。
  2. 存在 118 个共同 AST 指纹，覆盖率高达 36.09%/40.83%。
  3. 此高重合度源于双方均合法引用了相同的开源 ext4 实现（`ext4_rs`）及 Rust 标准库，属于公共依赖，与核心内核原创性无关。
  4. 证据强度：中（但属于公共依赖，不构成抄袭证据）。

#### 3. 底层 AST 关键证据指纹表

| 所属板块 | 目标项目路径 | 参考项目路径 | 证据类型 | 技术说明 |
| :--- | :--- | :--- | :--- | :--- |
| 进程管理 | `os/src/task/processor.rs#take_current` | `os/src/task/processor.rs#take_current` | 同板块底层逻辑一致 | 双方均使用 `self.current.take`，这是 `Option<T>` 的标准方法调用，无原创性表达空间。 |
| 进程管理 | `os/src/task/processor.rs#current` | `os/src/task/processor.rs#current` | 同板块底层逻辑一致 | 双方均使用 `self.current.as_ref().map`，是获取 `Option` 内部引用的 Rust 语言惯用法。 |
| 进程管理 | `os/src/task/processor.rs#take_current_task` | `os/src/task/processor.rs#take_current_task` | 同板块同名同结构 | 均为 `PROCESSOR.<锁>().take_current()`，但锁机制不同（`exclusive_access` vs `lock`）。 |
| 进程管理 | `os/src/task/processor.rs#current_task` | `os/src/task/processor.rs#current_task` | 同板块同名同结构 | 均为 `PROCESSOR.<锁>().current`，但锁机制不同，属于单例模式的标准实现。 |
| 进程管理 | `os/src/task/processor.rs#current_trap_cx` | `os/src/task/processor.rs#current_trap_cx` | 同板块同名 | 获取 Trap 上下文的标准四步流程，但内部锁接口不同（`inner_exclusive_access` vs `acquire_inner_lock`）。 |
| 进程管理 | `os/src/syscall/process.rs#sys_clone` | `os/src/syscall/process.rs#sys_clone` | 同板块同名 | 同名系统调用，但进程抽象模型完全不同（`Process.clone_process` vs `CloneFlags::from_bits`）。 |
| 进程管理 | `os/src/syscall/process.rs#sys_brk` | `os/src/syscall/process.rs#sys_brk` | 同板块同名 | 同名系统调用，但堆管理接口完全不同（`expand_heap`/`shrink_heap` vs `sbrk`）。 |
| 内存管理 | `os/src/mm/memory_set.rs#new_kernel` | `os/src/mm/memory_set.rs#new_kernel` | 同板块同名 | 内核内存集初始化，但映射方式（手动映射段 vs `anonymous_identical_map!` 宏）不同。 |
| 内存管理 | `os/src/mm/memory_set.rs#mprotect` | `os/src/mm/memory_set.rs#mprotect` | 同板块同名 | 内存保护修改，但权限位处理逻辑和区域迭代方式不同。 |
| 文件系统/第三方库 | `filesystem/ext4_rs/src/ext4_impls/file.rs#write_at` | `os/src/fs/ext4/file.rs#write_at` | 同板块同名 | 双方均使用 ext4 文件系统实现，属于公共开源依赖。 |
| 文件系统/第三方库 | `filesystem/ext4_rs/src/ext4_impls/extents.rs#ext_remove_leaf` | `os/src/fs/ext4/extent.rs#ext_remove_leaf` | 同板块同名 | 同上，ext4 extent 操作逻辑源于同一开源代码库。 |

#### 4. 核心代码调用流对比剖析

* **【进程管理/进程调度】**
* **底层技术析理**: 被告以 `Process` 为调度核心，通过 `current_process` 获取进程，再调用 `process.inner_exclusive_access()` 获取内部锁；原告以 `Task` 为调度核心，通过 `current_task` 获取任务，再调用 `task.acquire_inner_lock()` 获取内部锁。两者在抽象层次（进程 vs 任务）和同步原语（`exclusive_access` vs `lock`）上存在根本性架构差异。
* **代码溯源链接**:
  - 目标项目: `os/src/task/processor.rs#schedule`
  - 参考项目: `os/src/task/processor.rs#schedule`

* **【内存管理/堆管理】**
* **底层技术析理**: 被告的 `sys_brk` 通过 `memory_set.get_mut().expand_heap/shrink_heap` 直接操作内存集；原告的 `sys_brk` 通过 `memory_set.sbrk` 进行堆调整。两者在内存模型（直接操作 vs 封装函数）和接口设计上完全不同。
* **代码溯源链接**:
  - 目标项目: `os/src/syscall/process.rs#sys_brk`
  - 参考项目: `os/src/syscall/process.rs#sys_brk`

* **【设备驱动/virtio块设备】**
* **底层技术析理**: 被告的 `read_block` 支持非阻塞访问（`DEV_NON_BLOCKING_ACCESS`），通过 `virtio_blk.exclusive_access()` 获取设备锁；原告的 `read_block` 为同步阻塞实现，通过 `self.0.lock()` 获取互斥锁。两者在 I/O 模型和并发控制上存在显著差异。
* **代码溯源链接**:
  - 目标项目: `os/src/drivers/block/virtio_blk.rs#read_block`
  - 参考项目: `os/src/drivers/block/virtio_blk.rs#read_block`

#### 5. 单方模块与降权项

- **仅参考项目体现的模块**：`网络协议栈/第三方库`、`第三方库`。这些模块在目标项目中无对应实现，属于原告独有的第三方依赖（如 `virtio-drivers`、`dep_iso`、`dep_pci`），无法形成同源证据。
- **降权项**：
  - **跨板块同名函数**：如 `sigprocmask`（目标位于用户态 `user/src/signal.rs`，参考位于内核态 `os/src/task/signal.rs`），功能层次不同，不构成同板块主证据。
  - **极简标准 syscall 转发**：如 `sys_yield`、`sys_shutdown`、`sys_getpid`，其实现仅为单行标准接口调用，无原创性，不能作为抄袭证据。
  - **公共教学模板函数**：如 `processor.rs` 中的 `take_current_task`、`current_task` 等，其函数名和文件布局高度符合 rCore 等公共教学框架，属于公共知识领域。

#### 6. 保守结论

综合全模块 AST 指纹分析，双方全局相似度仅 4.13%，覆盖率均不足 9%。控方所指控的“铁证”均为 Rust 语言标准库惯用法（`Option::take`、`as_ref().map`）或操作系统公共模板下的极简封装函数，其 AST 重合是功能必然性与语言特性的结果，而非代码复用。双方在进程抽象（Process vs Task）、同步原语（`exclusive_access` vs `lock`）、内存管理接口（`expand_heap` vs `sbrk`）及硬件抽象层（`polyhal` vs 自研 `hal`）等核心架构上存在系统性差异。高重合度仅出现在双方合法引用的公共第三方库（ext4），与内核原创性无关。整体证据不支持抄袭指控，应认定为独立开发。

------------------------------

