# 🛡️ OS 内核赛自动化代码级架构审计报告

**审计时间**: `2026-06-27 18:07:29`
**参赛作品名称**: `2026-T2026104869910402-武汉大学`
**年份**: `2026`
**学校**: `武汉大学`
**队伍名称**: `T2026104869910402`
**比赛名字**: `OS-Competition`
**比赛子赛道**: `Kernel-Implementation`
**仓库地址**: `https://gitlab.eduxiji.net/educg-group-43501-3132633/T2026104869910402-3085`
**推测开发语言**: `Rust / C (由AST探针自动识别)`
**特征函数量**: `2458`

## ⚔️ 底层代码同源性对比 (AST Homology)
> 本部分由专门的代码审查智能体，对该项目与符合年份条件的历届最高相似度项目进行控制流与底层调用对比。新版报告会先给出确定性的全模块覆盖矩阵，再给出 Agent 的分模块解释，避免只围绕少量抽样函数展开。

### 🏆 Top 2 底层对标: vs `2025-如有名字队-南开大学`（南开大学 / 如有名字队）

#### 对方作品信息

**对方年份**: `2025`
**对方学校**: `南开大学`
**对方队伍名称**: `如有名字队`
**对方比赛名字**: `OS-Competition`
**对方比赛子赛道**: `Kernel-Implementation`
**对方仓库地址**: `https://gitlab.eduxiji.net/educg-group-36002-2710490/nonix`

> 双向覆盖率：共同 AST 指纹: 279；唯一 AST 指纹: 目标项目 2043，参考项目 1745；覆盖率: 目标项目 13.66%，参考项目 15.99%

## 全模块对比总览（确定性证据）

> 总体覆盖率：共同 AST 指纹: 279；唯一 AST 指纹: 目标项目 2043，参考项目 1745；覆盖率: 目标项目 13.66%，参考项目 15.99%

### 1. 模块覆盖矩阵
| 模块 | 目标函数数 | 参考函数数 | 共同唯一 AST 指纹 | Target 覆盖 | Base 覆盖 | 状态 |
| :--- | ---: | ---: | ---: | ---: | ---: | :--- |
| 进程管理 | 115 | 36 | 5 | 4.95% | 14.29% | 双方存在且有结构重合 |
| 任务调度/异步运行时 | 224 | 92 | 18 | 9.09% | 21.69% | 双方存在且有结构重合 |
| 内存管理 | 551 | 297 | 103 | 20.36% | 36.79% | 双方存在且有结构重合 |
| 中断/异常处理 | 356 | 43 | 29 | 9.93% | 87.88% | 双方存在且有结构重合 |
| 系统调用 | 153 | 63 | 2 | 2.13% | 3.92% | 双方存在且有结构重合 |
| 文件系统 | 289 | 252 | 16 | 6.72% | 7.58% | 双方存在且有结构重合 |
| 设备驱动 | 109 | 372 | 6 | 6.06% | 1.79% | 双方存在且有结构重合 |
| 网络协议栈 | 73 | 0 | 0 | 0.0% | 0.0% | 仅目标项目体现 |
| 文件系统/第三方库 | 410 | 696 | 2 | 0.61% | 0.34% | 双方存在且有结构重合 |
| 第三方库 | 0 | 1 | 0 | 0.0% | 0.0% | 仅参考项目体现 |
| 通用模块/其他 | 178 | 155 | 87 | 52.1% | 62.14% | 双方存在且有结构重合 |

### 2. 分模块证据清单

#### 进程管理
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `patches/polyhal/src/components/timer/x86_64.rs#timer_wait` | `patch/polyhal/src/components/timer/x86_64.rs#timer_wait` | 同板块同名 | Port::new, duration.as_millis, pc_speaker.read, pc_speaker.write, pit_ch2.write, pit_cmd.write ...(+1) | PC_SPEAKER.read, PC_SPEAKER.write, PIT_CH2.write, PIT_CMD.write, duration.as_millis, spin_loop |
| `os/src/syscall/process.rs#sys_clone` | `os/src/syscall/process.rs#sys_clone` | 同板块同名 | Ok, clone_exit_signal, clone_flags_from_raw, current_process, current_process.clone_process | CloneFlags::from_bits, CloneFlags::from_bits(flags as u32).ok_or_else, Ok, add_task, current_task, current_task().unwrap ...(+5) |
| `user/src/signal.rs#sigprocmask` | `user/src/lib.rs#sigprocmask` | 同板块同名 | core::ptr::null, core::ptr::null_mut, oldset
        .map, oldset
        .map(|s| s as *mut SigSet as *mut u8)
        .unwrap_or, set
        .map, set
        .map(|s| s as *const SigSet as *const u8)
        .unwrap_or ...(+1) | sys_sigprocmask |
| `os/src/syscall/process.rs#sys_getppid` | `os/src/syscall/process.rs#sys_getppid` | 同板块同名 | Ok, current_process, parent.getpid, process.inner_exclusive_access, weak.upgrade | Ok, current_task, current_task().unwrap, inner.parent.as_ref, inner.parent.as_ref().ok_or, parent.upgrade ...(+3) |

#### 任务调度/异步运行时
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `os/src/task/mod.rs#check_pending_signals` | `os/src/task/mod.rs#check_pending_signals` | 同板块同名 | Some, call_kernel_signal_handler, call_user_signal_handler, current_task, drop, p_inner.shared_signal_queue.len ...(+9) | SignalFlags::from_bits, SignalFlags::from_bits(1 << sig).unwrap, call_kernel_signal_handler, call_user_signal_handler, current_task, current_task().unwrap ...(+6) |
| `os/src/syscall/fs.rs#sys_fstatat` | `os/src/syscall/fs.rs#sys_fstatat` | 同板块同名 | (&attr as *const Kstat).cast::<u8>, Err, Ok, core::mem::size_of::<Kstat>, core::slice::from_raw_parts, current_process ...(+24) | FileClass::Abs, FstatatFlags::empty, FstatatFlags::from_bits, FstatatFlags::from_bits(flags as u32).unwrap_or, Ok, UserBuffer::new ...(+20) |
| `os/src/syscall/fs.rs#sys_mkdirat` | `os/src/syscall/fs.rs#sys_mkdirat` | 同板块同名 | Err, Ok, SyscallErrNo::from, VfsFile::open, apply_umask_to_mode, current_process ...(+11) | Err, Ok, abs_path.as_str, created.is_ok, current_task, current_task().unwrap ...(+8) |
| `os/src/syscall/fs.rs#sys_write` | `os/src/syscall/fs.rs#sys_write` | 同板块同名 | Err, Ok, SyscallErrNo::from, crate::task::check_signal_for_eintr, current_process, drop ...(+11) | Err, Ok, UserBuffer::new, current_task, current_task().unwrap, current_user_token ...(+11) |

#### 内存管理
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `os/src/task/process.rs#exec` | `os/src/task/task.rs#exec` | 同板块同名 | (0..=args.len())
                .map, (0..=args.len())
                .map(|i| translated_refmut(pt, (argv_base + i * core::mem::size_of::<usize>()) as *mut usize))
                .collect::<Result<Vec<_>, _>>, (0..=envs.len())
                .map, (0..=envs.len())
                .map(|i| translated_refmut(pt, (envp_base + i * core::mem::size_of::<usize>()) as *mut usize))
                .collect::<Result<Vec<_>, _>>, (1..inner.tasks.len()).rev, (user_sp as u64).to_le_bytes ...(+73) | Arc::new, Aux::new, MemorySet::new, MemorySetInner::from_elf, String::from, TrapFrame::new ...(+34) |
| `os/src/task/mod.rs#exit_current_and_run_next` | `os/src/task/mod.rs#exit_current_and_run_next` | 同板块同名 | Arc::downgrade, SigInfo::kernel_signal, Some, Vec::<TaskUserRes>::new, add_signal_to_process, child.clone ...(+48) | Arc::downgrade, INITPROC.inner_exclusive_access, KContext::blank, Some, child.clone, child.inner_exclusive_access ...(+15) |
| `os/src/syscall/process.rs#sys_wait4` | `os/src/syscall/process.rs#sys_wait4` | 同板块同名 | Arc::strong_count, Err, Ok, block_current_task, build_rusage, child.getpid ...(+36) | Err, Ok, assert_eq!, child.getpid, child.inner_exclusive_access, current_task ...(+20) |
| `os/src/syscall/process.rs#sys_mmap` | `os/src/syscall/mm.rs#sys_mmap` | 同板块同名 | Err, Ok, VirtAddr::new, addr.checked_add, addr.checked_add(len_aligned).ok_or, current_process ...(+29) | Err, MapPermission::from, MmapFlags::from_bits, MmapFlags::from_bits(flags as u32).unwrap, MmapProt::from_bits, MmapProt::from_bits(prot as u32).unwrap ...(+15) |

#### 中断/异常处理
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `patches/polyhal/src/utils/mutex_no_irq.rs#try_lock` | `patch/polyhal/src/utils/mutex_no_irq.rs#try_lock` | 同板块同名同结构 | IRQ::int_disable, IRQ::int_enabled, self.lock
            .try_lock, self.lock
            .try_lock()
            .map, self.lock.is_locked | IRQ::int_disable, IRQ::int_enabled, self.lock
            .try_lock, self.lock
            .try_lock()
            .map, self.lock.is_locked |
| `patches/polyhal/src/components/irq/loongarch64.rs#irq_disable` | `patch/polyhal/src/components/irq/x86_64.rs#irq_disable` | 同板块同名 | LineBasedInterrupt::from_bits_truncate, ecfg::read, ecfg::read().lie, ecfg::set_lie | io_apic, io_apic().lock, io_apic().lock().disable_irq |
| `patches/polyhal/src/components/irq/loongarch64.rs#irq_enable` | `patch/polyhal/src/components/irq/aarch64.rs#irq_enable` | 同板块同名 | LineBasedInterrupt::from_bits_truncate, ecfg::read, ecfg::read().lie, ecfg::set_lie | GICD.lock, GICD.lock().set_enable |
| `patches/polyhal/src/components/irq/aarch64.rs#irq_enable` | `patch/polyhal/src/components/irq/x86_64.rs#irq_enable` | 同板块同名 | GICD.lock, GICD.lock().set_enable | io_apic, io_apic().lock, io_apic().lock().enable_irq |

#### 系统调用
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `user/src/syscall.rs#syscall` | `user/src/syscall.rs#syscall` | 同板块同名 | core::arch::asm! | asm! |
| `user/src/signal.rs#sigaction` | `user/src/lib.rs#sigaction` | 同板块同名 | act
        .map, act
        .map(|a| a as *const SigAction as *const u8)
        .unwrap_or, core::ptr::null, core::ptr::null_mut, old_act
        .map, old_act
        .map(|a| a as *mut SigAction as *mut u8)
        .unwrap_or ...(+1) | action.map_or, core::ptr::null, core::ptr::null_mut, old_action.map_or, sys_sigaction |
| `os/src/syscall/stub.rs#sys_syslog` | `os/src/syscall/other.rs#sys_syslog` | 同板块同名 | Ok | Ok, warn! |
| `user/src/syscall.rs#sys_read` | `user/src/syscall.rs#sys_read` | 同板块同名 | buffer.as_mut_ptr, buffer.len, syscall | buffer.as_mut_ptr, buffer.len, syscall |

#### 文件系统
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `patches/polyhal/src/debug_console/x86_64/graphic.rs#put_char` | `patch/polyhal/src/debug_console/x86_64/graphic.rs#put_char` | 同板块同名同结构 | BIT_FONTS.len, self.backspace, self.scroll_up, self.write_pixel | BIT_FONTS.len, self.backspace, self.scroll_up, self.write_pixel |
| `patches/polyhal/src/debug_console/mod.rs#log` | `patch/polyhal/src/debug_console/mod.rs#log` | 同板块同名同结构 | DebugConsole::set_color, println!, record.level, record.line, record.module_path | DebugConsole::set_color, println!, record.level, record.line, record.module_path |
| `os/src/fs/fdtable.rs#new_with_stdio` | `os/src/fs/fstruct.rs#new_with_stdio` | 同板块同名 | Self::new, open_stdio, open_stdio("/dev/stderr", OpenFlags::O_WRONLY)
            .map, open_stdio("/dev/stderr", OpenFlags::O_WRONLY)
            .map(FdEntry::new)
            .map, open_stdio("/dev/stderr", OpenFlags::O_WRONLY)
            .map(FdEntry::new)
            .map(FdSlot::Occupied)
            .unwrap_or, open_stdio("/dev/stdin", OpenFlags::O_RDONLY)
            .map ...(+6) | Arc::new, FdTableInner::new, FileClass::Abs, FileDescriptor::default, Some, UPSafeCell::new ...(+2) |
| `patches/polyhal/src/debug_console/x86_64/vga_text.rs#putchar` | `patch/polyhal/src/debug_console/x86_64/vga_text.rs#putchar` | 同板块同名同结构 | FChar, self.move_cursor, self.offset, self.scroll_up, vga_buffer.add, vga_buffer.add(self.offset()).write_volatile | FChar, self.move_cursor, self.offset, self.scroll_up, vga_buffer.add, vga_buffer.add(self.offset()).write_volatile |

#### 设备驱动
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `patches/lose-net-stack/src/connection/tcp.rs#connect` | `patch/virtio-drivers/src/device/socket/connectionmanager.rs#connect` | 同板块同名 | Err, Ok, debug!, drop, remote.ip, remote.ip().is_loopback ...(+15) | Connection::new, Err, Ok, SocketError::ConnectionExists.into, debug!, self.connections.iter ...(+3) |
| `os/src/net/mod.rs#send` | `patch/virtio-drivers/src/device/console.rs#send` | 同板块同名 | NET_DEVICE.transmit, NET_SERVER.analysis_net_data, arp_info_from_frame, build_arp_reply, ipv4_dst_from_frame, is_loopback_ip | Arc::new, Default::default, Mutex::new, NonNull::from, ReadOnly::new, State::wait_until_queue_notified ...(+13) |
| `patches/lose-net-stack/src/connection/tcp.rs#connect` | `patch/virtio-drivers/src/device/socket/vsock.rs#connect` | 同板块同名 | Arc::new, Mutex::new, Ok, RwLock::new, SocketAddrV4::new, TcpQueue::new ...(+18) | VirtioVsockOp::Request.into, connection_info.new_header, self.send_packet_to_tx_queue |
| `patches/polyhal/src/mem.rs#init_dtb_once` | `patch/polyhal/src/mem.rs#init_dtb_once` | 同板块同名同结构 | DTB_INFO.init_once, Fdt::from_ptr, NonNull::new, Ok, add_memory_region, dtb_ptr.get_mut_ptr ...(+6) | DTB_INFO.init_once, Fdt::from_ptr, NonNull::new, Ok, add_memory_region, dtb_ptr.get_mut_ptr ...(+6) |

#### 网络协议栈
仅目标项目中观察到该模块代表函数，参考项目没有同模块可对齐证据：
- 目标项目：`patches/lose-net-stack/src/connection/tcp.rs#close`，调用摘要：Ok, conn.close, conn.queue_push_owned, conn.remote.read, conn.remote_closed.write, conn.status.write ...(+18)
- 目标项目：`os/src/net/socket.rs#poll`，调用摘要：PollEvents::empty, events.contains, self.buf.exclusive_access, self.buf.exclusive_access().is_empty, self.inner.get_remote, self.inner.get_remote().is_ok ...(+7)
- 目标项目：`os/src/net/mod.rs#build_arp_reply`，调用摘要：reply[0..6].copy_from_slice, reply[12..14].copy_from_slice, reply[14..16].copy_from_slice, reply[16..18].copy_from_slice, reply[20..22].copy_from_slice, reply[22..28].copy_from_slice ...(+4)
- 目标项目：`patches/lose-net-stack/src/packets/arp.rs#build_data`，调用摘要：ARP_HRD_ETHER.to_be, EthRtype::ARP.into, EthRtype::IP.into, UnsafeRefIter::new, data_ptr_iter.next_mut::<Arp>, data_ptr_iter.next_mut::<Eth> ...(+5)

#### 文件系统/第三方库
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `filesystem/ext4_rs/src/ext4_impls/file.rs#write_at` | `os/src/fs/ext4_lw/inode.rs#write_at` | 同板块同名 | Block::load, Err, Ok, alloc::collections::BTreeSet::new, alloc::vec!, allocated_blocks.iter ...(+24) | Err, SysErrNo::from, file.file_close, file.file_open, file.file_open(path, O_RDWR)
            .map_err, file.file_seek ...(+12) |
| `filesystem/src/ext4fs/inode.rs#rename` | `lwext4_rust/examples/src/ext4fs.rs#rename` | 同板块同名 | Block::load, Err, Ext4DirEntry::default, Ext4DirSearchResult::new, Ok, Some ...(+34) | file.file_rename, info!, self.0.borrow_mut |
| `filesystem/ext4_rs/src/ext4_impls/file.rs#read_at` | `os/src/fs/ext4_lw/inode.rs#read_at` | 同板块同名 | Ok, inode_ref.inode.size, log::error!, log::trace!, min, read_buf.len ...(+6) | SysErrNo::from, file.file_close, file.file_open, file.file_open(path, O_RDONLY)
            .map_err, file.file_read, file.file_seek ...(+6) |
| `filesystem/src/ext4fs/inode.rs#unlink` | `os/src/fs/ext4_lw/inode.rs#unlink` | 同板块同名 | Ext4DirEntry::default, Ext4DirSearchResult::new, Ok, child_inode_ref.inode.is_dir, child_inode_ref.inode.links_count, err.error ...(+14) | SysErrNo::from, file.file_remove, file.file_remove(path).map_err, self.inner.exclusive_access |

#### 第三方库
仅参考项目中观察到该模块代表函数，目标项目没有同模块可对齐证据：
- 参考项目：`patch/virtio-drivers/src/transport/mmio.rs#vendor_id`，调用摘要：volread!

#### 通用模块/其他
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `patches/polyhal/src/mem.rs#parse_system_info` | `patch/polyhal/src/mem.rs#parse_system_info` | 同板块同名同结构 | CPU_NUM.init_once, display_info!, fdt.chosen, fdt.chosen().inspect, fdt.find_nodes, fdt.find_nodes("/cpus/cpu").count ...(+8) | CPU_NUM.init_once, display_info!, fdt.chosen, fdt.chosen().inspect, fdt.find_nodes, fdt.find_nodes("/cpus/cpu").count ...(+8) |
| `user/src/lib.rs#_start` | `user/src/lib.rs#_start` | 同板块同名 | ((argv + i * core::mem::size_of::<usize>()) as *const usize).read_volatile, ((str_start + *i) as *const u8).read_volatile, (0usize..)
            .find, (0usize..)
            .find(|i| unsafe { ((str_start + *i) as *const u8).read_volatile() == 0 })
            .unwrap, HEAP.lock, HEAP.lock()
            .init ...(+10) | ((argv + i * core::mem::size_of::<usize>()) as *const usize).read_volatile, ((str_start + *i) as *const u8).read_volatile, (0usize..)
            .find, (0usize..)
            .find(|i| unsafe { ((str_start + *i) as *const u8).read_volatile() == 0 })
            .unwrap, HEAP.lock, HEAP.lock()
            .init ...(+10) |
| `patches/polyhal/src/components/percpu/mod.rs#get_percpu_ptr` | `patch/polyhal/src/components/percpu/mod.rs#get_percpu_ptr` | 同板块同名同结构 | cfg_if::cfg_if! | cfg_if::cfg_if! |
| `os/src/timer.rs#set_next_trigger` | `os/src/timer.rs#set_next_trigger` | 同板块同名 | Duration::from_nanos, current_time, set_next_timer | Time::get_freq, Time::now, Time::now().raw |


好的，我将遵循审计指令，以操作系统内核代码同源性审计专家的身份，对本案进行最终定性仲裁。

### 1. 宏观判定
- **定性结论**: **独立开发 (底层架构显著差异)**
  客观 AST 相似度仅为 7.95%，共同指纹 279 个，远低于目标项目（2043）和参考项目（1745）的唯一指纹数。13.66% 与 15.99% 的覆盖率表明，双方超过 84% 的代码库是彼此唯一的，不存在系统性的代码复用。控方所指控的“核心逻辑直接搬用”与宏观数据严重不符，其证据集中在极少数因领域共性和语言惯用法导致重合的微型函数上，无法构成有效的抄袭证明。

### 2. 全模块覆盖矩阵解读
- **进程管理**
  - **双方均存在**: 是
  - **共同 AST 指纹**: 5
  - **覆盖率解读**: 目标覆盖率 4.95%，参考覆盖率 14.29%。覆盖率极低，表明双方在进程管理的核心实现上差异巨大。目标项目函数数量（115）远超参考项目（36），说明其功能复杂度更高。
  - **证据强度**: **弱**。控方提出的“铁证”均为 `Option` 的标准取值或硬件规范操作，属于公共模式。

- **任务调度/异步运行时**
  - **双方均存在**: 是
  - **共同 AST 指纹**: 18
  - **覆盖率解读**: 目标覆盖率 9.09%，参考覆盖率 21.69%。覆盖率低，且参考项目覆盖率相对较高是因为其模块函数总数（92）较少，共同指纹占比被放大。
  - **证据强度**: **弱**。共同指纹多为 `sys_write`、`sys_mkdirat` 等系统调用入口，其内部调用流（如 `current_process` vs `current_task`）存在根本性架构差异。

- **内存管理**
  - **双方均存在**: 是
  - **共同 AST 指纹**: 103
  - **覆盖率解读**: 目标覆盖率 20.36%，参考覆盖率 36.79%。这是共同指纹最多的模块，但覆盖率仍属较低水平。内存管理是 OS 核心，大量数据结构（页表、分配器）和算法（ELF加载）有成熟范式。
  - **证据强度**: **中**。`exec`、`sys_mmap` 等函数同名且调用流有相似之处，但深入分析可见内存抽象（`MemorySet` vs `MemorySetInner`）和同步策略的差异，属于遵循共同理论的不同实现。

- **中断/异常处理**
  - **双方均存在**: 是
  - **共同 AST 指纹**: 29
  - **覆盖率解读**: 目标覆盖率 9.93%，参考覆盖率 87.88%。参考项目覆盖率极高，因其模块函数总数（43）很少，且共同指纹多集中在 `polyhal` 公共硬件抽象层。
  - **证据强度**: **弱**。证据集中在 `mutex_no_irq`、`irq_enable/disable` 等公共组件，这些是底层硬件架构决定的通用实现，且双方文件路径（`patches/polyhal` vs `patch/polyhal`）和具体操作对象（`ecfg` vs `io_apic`）存在差异。

- **系统调用**
  - **双方均存在**: 是
  - **共同 AST 指纹**: 2
  - **覆盖率解读**: 目标覆盖率 2.13%，参考覆盖率 3.92%。覆盖率极低，表明双方的系统调用层实现几乎完全不同。
  - **证据强度**: **未形成**。唯一的同名证据是 `syscall` 内联汇编指令和 `sys_read` 等极简封装，这是系统调用 ABI 的必然要求，不构成抄袭。

- **文件系统**
  - **双方均存在**: 是
  - **共同 AST 指纹**: 16
  - **覆盖率解读**: 目标覆盖率 6.72%，参考覆盖率 7.58%。覆盖率极低，双方文件系统实现差异显著。
  - **证据强度**: **弱**。证据集中在 `debug_console` 的 `put_char`、`log` 等极简函数，这些是 VGA 文本模式或日志宏的标准实现，无独创性。

- **设备驱动**
  - **双方均存在**: 是
  - **共同 AST 指纹**: 6
  - **覆盖率解读**: 目标覆盖率 6.06%，参考覆盖率 1.79%。覆盖率极低，双方驱动模型和覆盖硬件完全不同。
  - **证据强度**: **弱**。`connect`、`send` 等网络驱动函数同名，但分属 `lose-net-stack` 和 `virtio-drivers` 两个完全不同的驱动框架，调用流差异巨大。

- **网络协议栈**
  - **双方均存在**: 否。仅目标项目体现。
  - **共同 AST 指纹**: 0
  - **覆盖率解读**: 目标项目拥有独立的网络协议栈实现，参考项目无此模块。
  - **证据强度**: **未形成**。这是目标项目独有的原创模块，是独立开发的强有力证据。

- **第三方库/公共依赖**
  - **双方均存在**: 是
  - **共同 AST 指纹**: 2
  - **覆盖率解读**: 目标覆盖率 0.61%，参考覆盖率 0.34%。覆盖率极低。
  - **证据强度**: **弱**。证据为 `ext4` 文件系统的 `write_at`、`read_at` 等操作，但目标项目使用 `ext4_rs`，参考项目使用 `ext4_lw`，是不同的第三方库封装，内部实现完全不同。

### 3. 底层 AST 关键证据指纹表
| 所属板块 | 目标项目路径 | 参考项目路径 | 证据类型 | 技术说明 |
| :--- | :--- | :--- | :--- | :--- |
| 进程管理 | `os/src/task/processor.rs#current_task` | `os/src/task/processor.rs#current_task` | 同板块同名，底层逻辑一致 | 均为 `PROCESSOR.exclusive_access().current`，是获取当前任务指针的 Rust 标准惯用法，无独创性。 |
| 进程管理 | `patches/polyhal/src/components/timer/x86_64.rs#timer_wait` | `patch/polyhal/src/components/timer/x86_64.rs#timer_wait` | 同板块同名 | 均操作 PIT 硬件端口实现忙等，调用时序由 x86_64 硬件规范决定。目标项目使用 `Port::new` 抽象，与参考项目直接使用全局常量不同。 |
| 内存管理 | `os/src/task/process.rs#exec` | `os/src/task/task.rs#exec` | 同板块同名 | 均实现 `exec` 系统调用，涉及 ELF 加载、内存集替换、栈初始化等。但目标项目以 `Process` 为中心，参考项目以 `Task` 为中心，架构不同。 |
| 内存管理 | `os/src/syscall/process.rs#sys_mmap` | `os/src/syscall/mm.rs#sys_mmap` | 同板块同名 | 均实现 `mmap` 系统调用，但目标项目直接操作 `MemorySet`，参考项目使用 `MmapFlags` 等标志位解析，抽象层次不同。 |
| 中断/异常处理 | `patches/polyhal/src/utils/mutex_no_irq.rs#try_lock` | `patch/polyhal/src/utils/mutex_no_irq.rs#try_lock` | 同板块同名同结构 | 完全一致的关中断自旋锁实现，是 `polyhal` 公共硬件抽象层的标准组件，属于第三方公共依赖。 |
| 通用模块/其他 | `patches/polyhal/src/mem.rs#parse_system_info` | `patch/polyhal/src/mem.rs#parse_system_info` | 同板块同名同结构 | 完全一致的设备树解析逻辑，是 `polyhal` 公共硬件抽象层的标准组件，属于第三方公共依赖。 |
| 文件系统/第三方库 | `filesystem/ext4_rs/src/ext4_impls/file.rs#write_at` | `os/src/fs/ext4_lw/inode.rs#write_at` | 同板块同名 | 均封装 `ext4` 文件写入，但底层依赖库不同（`ext4_rs` vs `lwext4_rust`），调用流和错误处理完全不同。 |

### 4. 核心代码调用流对比剖析
* **【进程管理/系统调用路径】**
* **底层技术析理**: 双方均实现了 `sys_getpid` 系统调用，但获取进程ID的调用流揭示了根本性的架构差异。目标项目采用 `Process` 为中心的两级模型，通过 `current_task() -> process.upgrade() -> getpid()` 的路径获取，体现了任务和进程的解耦。参考项目则采用 `Task` 为中心的扁平模型，直接从 `current_task().unwrap()` 获取 PID。这种核心抽象层次的不同，是独立设计的有力证明，而非简单的“洗稿”。
* **代码溯源链接**:
  - 目标项目: `os/src/syscall/process.rs#sys_getpid`
  - 参考项目: `os/src/syscall/process.rs#sys_getpid`

* **【任务调度/异步运行时/退出与调度路径】**
* **底层技术析理**: 在 `exit_current_and_run_next` 这一核心调度函数中，双方均处理了进程退出、子进程托管给 `initproc`、唤醒父进程等标准 POSIX 语义。然而，目标项目的实现更为复杂，涉及 `SigInfo::kernel_signal` 的信号发送、`TaskUserRes` 的资源回收，以及显式的 `add_signal_to_process` 调用。参考项目实现相对简洁，直接操作 `INITPROC` 和 `child` 的内部状态。两者在信号处理、资源回收的具体对象和调用序列上存在显著差异。
* **代码溯源链接**:
  - 目标项目: `os/src/task/mod.rs#exit_current_and_run_next`
  - 参考项目: `os/src/task/mod.rs#exit_current_and_run_next`

* **【内存管理/内存映射路径】**
* **底层技术析理**: 双方均实现了 `sys_mmap`，但内存映射的入口抽象完全不同。目标项目直接通过 `current_process` 获取进程的内存管理单元 `inner.memory_set` 并调用其 `mmap` 方法，体现了以进程为内存管理主体的设计。参考项目则通过解析 `MmapFlags`、`MmapProt` 等标志位，再调用底层 `map` 函数，其抽象层次和参数传递方式与目标项目存在结构性差异。
* **代码溯源链接**:
  - 目标项目: `os/src/syscall/process.rs#sys_mmap`
  - 参考项目: `os/src/syscall/mm.rs#sys_mmap`

### 5. 单方模块与降权项
- **单方模块**:
  - **网络协议栈**: 仅存在于目标项目，是其原创性贡献，是独立开发的强证据。
  - **第三方库**: 参考项目独有的 `patch/virtio-drivers` 模块，是其项目依赖。
- **降权项**:
  - **`polyhal` 公共组件**: `mutex_no_irq`、`parse_system_info`、`get_percpu_ptr` 等函数，双方完全一致。这些属于 `polyhal` 公共硬件抽象层，是双方共同依赖的第三方基础库，不能作为抄袭证据。
  - **泛型/标准库函数**: `Option::take`、`Option::as_ref`、`BTreeMap::new`、`Vec::new` 等 Rust 标准库的泛型函数调用，是任何 Rust 项目的通用语言基础设施，必须从同源证据中剔除。
  - **硬件规范驱动**: `timer_wait` 对 PIT 端口的操作序列，由 x86_64 硬件编程规范决定，属于领域共性，独创性极低。

### 6. 保守结论
综合宏观覆盖率（7.95%）、全模块矩阵分析和核心调用流对比，本庭裁定被告项目 [2026-T2026104869910402-武汉大学] 与原告项目 [2025-如有名字队-南开大学] 不存在系统性的代码抄袭或同源复用。双方超过 84% 的代码库是彼此唯一的，在进程/任务抽象模型、内存管理接口、系统调用实现和功能模块（如网络协议栈）上存在根本性架构差异。控方提出的“铁证”均能被解释为 Rust 语言惯用法、硬件规范约束或对公共第三方库（polyhal）的依赖，其指控的“核心逻辑直接搬用”与宏观数据相悖。因此，被告项目的原创性得以确认，复用指控不成立。

------------------------------

