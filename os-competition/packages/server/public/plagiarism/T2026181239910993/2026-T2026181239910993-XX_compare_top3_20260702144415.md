# 🛡️ OS 内核赛自动化代码级架构审计报告

**审计时间**: `2026-07-02 14:44:15`
**参赛作品名称**: `2026-T2026181239910993-XX`
**年份**: `2026`
**学校**: `XX`
**队伍名称**: `T2026181239910993`
**比赛名字**: `OS-Competition`
**比赛子赛道**: `Kernel-Implementation`
**仓库地址**: `https://gitlab.eduxiji.net/educg-group-43501-3132633/T2026181239910993-1468`
**推测开发语言**: `Rust / C (由AST探针自动识别)`
**特征函数量**: `5274`

## ⚔️ 底层代码同源性对比 (AST Homology)
> 本部分由专门的代码审查智能体，对该项目与符合年份条件的历届最高相似度项目进行控制流与底层调用对比。新版报告会先给出确定性的全模块覆盖矩阵，再给出 Agent 的分模块解释，避免只围绕少量抽样函数展开。

### 🏆 Top 3 底层对标: vs `2025-塔特林设计局-华中科技大学`（华中科技大学 / 塔特林设计局）

#### 对方作品信息

**对方年份**: `2025`
**对方学校**: `华中科技大学`
**对方队伍名称**: `塔特林设计局`
**对方比赛名字**: `OS-Competition`
**对方比赛子赛道**: `Kernel-Implementation`
**对方仓库地址**: `https://gitlab.eduxiji.net/educg-group-36002-2710490/T202510487995221-883`

> 双向覆盖率：共同 AST 指纹: 568；唯一 AST 指纹: 目标项目 3566，参考项目 1476；覆盖率: 目标项目 15.93%，参考项目 38.48%

## 全模块对比总览（确定性证据）

> 总体覆盖率：共同 AST 指纹: 568；唯一 AST 指纹: 目标项目 3566，参考项目 1476；覆盖率: 目标项目 15.93%，参考项目 38.48%

### 1. 模块覆盖矩阵
| 模块 | 目标函数数 | 参考函数数 | 共同唯一 AST 指纹 | Target 覆盖 | Base 覆盖 | 状态 |
| :--- | ---: | ---: | ---: | ---: | ---: | :--- |
| 进程管理 | 697 | 78 | 4 | 1.51% | 5.48% | 双方存在且有结构重合 |
| 任务调度/异步运行时 | 381 | 148 | 4 | 1.16% | 3.25% | 双方存在且有结构重合 |
| 内存管理 | 879 | 284 | 28 | 3.67% | 11.24% | 双方存在且有结构重合 |
| 中断/异常处理 | 116 | 21 | 3 | 3.19% | 15.0% | 双方存在且有结构重合 |
| 系统调用 | 176 | 105 | 6 | 4.2% | 7.41% | 双方存在且有结构重合 |
| 文件系统 | 1327 | 263 | 8 | 1.05% | 4.06% | 双方存在且有结构重合 |
| 设备驱动 | 111 | 47 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |
| 网络协议栈 | 339 | 0 | 0 | 0.0% | 0.0% | 仅目标项目体现 |
| 文件系统/第三方库 | 781 | 704 | 498 | 75.91% | 83.28% | 双方存在且有结构重合 |
| 通用模块/其他 | 467 | 136 | 9 | 2.65% | 7.38% | 双方存在且有结构重合 |

### 2. 分模块证据清单

#### 进程管理
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `os/src/task/processor.rs#run_tasks` | `os/src/task/processor.rs#run_tasks` | 同板块同名 | Arc::clone, PROCESSORS[id].as_mut, PROCESSORS[id].as_mut().unwrap, PROCESSORS[id].as_mut().unwrap().lock, Some, check_timers ...(+15) | Some, check_futex_timer, cur_task.inner_lock, drop, get_proc_by_hartid, hart_id ...(+7) |
| `os/src/syscall/process.rs#sys_wait4` | `os/src/syscall/process/wait4.rs#sys_wait4` | 同板块同名 | Err, Ok, Some, _set_sum_bit, block_current_and_run_next, buf.fill ...(+18) | Arc::clone, Err, Ok, Process::remove_from_global_map, child.inner_lock, child.inner_lock().get_locked_sigtable ...(+25) |
| `os/src/syscall/process.rs#sys_sched_getaffinity` | `os/src/syscall/process/others.rs#sys_sched_getaffinity` | 同板块同名 | Err, Ok, current_user_token, log::info!, log::warn!, size_of::<u64> ...(+1) | Ok, warn! |
| `os/src/syscall/process.rs#sys_sched_getparam` | `os/src/syscall/process/others.rs#sys_sched_getparam` | 同板块同名 | Err, Ok, copy_struct_to_user, core::mem::size_of::<SchedParam>, current_user_token, error! ...(+3) | Ok, warn! |

#### 任务调度/异步运行时
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `os/src/syscall/net.rs#sys_sendto` | `os/src/syscall/net.rs#sys_sendto` | 同板块同名 | Err, Ok, SOCKET_MANAGER.lock, Some, _set_sum_bit, addr_ptr.is_null ...(+32) | Ok, SocketDomain::from, UDP_QUEUE.try_lock, UDP_QUEUE.try_lock().unwrap, UDP_QUEUE.try_lock().unwrap().push_back, Vec::new ...(+8) |
| `os/src/syscall/net.rs#sys_recvfrom` | `os/src/syscall/net.rs#sys_recvfrom` | 同板块同名 | 0x7F000001u32.to_be, Err, Ok, SOCKET_MANAGER.lock, Some, _set_sum_bit ...(+30) | Ok, SocketDomain::from, UDP_QUEUE.try_lock, UDP_QUEUE.try_lock().unwrap, UDP_QUEUE.try_lock().unwrap().pop_front, UDP_QUEUE.try_lock().unwrap().pop_front().unwrap ...(+10) |
| `os/src/syscall/fs.rs#sys_copy_file_range` | `os/src/syscall/fs.rs#sys_copy_file_range` | 同板块同名 | (len - total_copied).min, Err, Ok, UserBuffer::new, buffer.as_mut_ptr, check_write_size_limit ...(+46) | Err, Ok, Some, UserBuffer::new, Vec::new, buf.as_mut_slice ...(+30) |
| `os/src/syscall/fs.rs#sys_pselect6` | `os/src/syscall/fs.rs#sys_pselect6` | 同板块同名 | Err, Ok, Some, block_current_and_run_next, check_fd_ready, copy_fd_set_from_user ...(+23) | Ok, Some, current_task, current_task().unwrap, drop, event.contains ...(+21) |

#### 内存管理
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `os/src/syscall/mod.rs#syscall` | `os/src/syscall/mod.rs#syscall` | 同板块同名 | Err, error!, info!, polyhal::instruction::shutdown, sys_accept, sys_bind ...(+193) | Err, Ok, Syscall::from, cfg!, error!, log::debug! ...(+115) |
| `os/src/task/mod.rs#exit_current_and_run_next` | `os/src/task/mod.rs#exit_current_and_run_next` | 同板块同名 | Arc::downgrade, INITPROC
                        .inner_exclusive_access, INITPROC
                        .inner_exclusive_access()
                        .children
                        .extend, KContext::blank, Some, Vec::new ...(+76) | VirtAddr::from, VirtAddr::from(inner.trap_cx_bottom).floor, VirtAddr::from(inner.user_stack_top - USER_STACK_SIZE).floor, abandon, activate_kernel_space, debug! ...(+33) |
| `os/src/mm/vm_set.rs#from_elf` | `os/src/mm/memory_set/inner.rs#from_elf` | 同板块同名 | ((ph.virtual_addr() + ph.mem_size()) as usize).into, (interp_base + (ph.virtual_addr() + ph.mem_size()) as usize).into, (interp_base + ph.virtual_addr() as usize).into, (ph.virtual_addr() as usize).into, GLOBAL_DCACHE.get, KERNEL_VMSET.lock ...(+45) | Aux::new, MapArea::new, Self::new_from_kernel, Vec::new, VirtAddr, assert_eq! ...(+15) |
| `os/src/syscall/fs.rs#sys_openat` | `os/src/syscall/fs.rs#sys_openat` | 同板块同名 | Err, InodeMode::FILE.bits, InodeMode::from_bits_truncate, NotifyTarget::new, Ok, OpenFlags::from_bits_truncate ...(+70) | Err, FileDescriptor::new, Ok, OpenFlags::from_bits, OpenFlags::from_bits(flags).unwrap, assert! ...(+18) |

#### 中断/异常处理
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `polyhal/polyhal/src/components/timer/loongarch64.rs#enable_timer_interrupt` | `os/src/arch/loongarch64/qemu/trap_interface.rs#enable_timer_interrupt` | 同板块同名 | ecfg::read, ecfg::read().lie, ecfg::set_lie | ecfg::set_lie, ticlr::clear_timer_interrupt |
| `os/src/trap/mod.rs#enable_timer_interrupt` | `os/src/arch/riscv64/qemu/trap_interface.rs#enable_timer_interrupt` | 同板块同名 | polyhal::timer::enable_timer_interrupt | sie::set_stimer |
| `polyhal/polyhal-trap/src/trap/mod.rs#init_trap` | `os/src/trap/mod.rs#init` | 同板块同结构 | init | trap_init |
| `os/src/trap/mod.rs#_set_sum_bit` | `os/src/arch/loongarch64/qemu/trap_interface.rs#trap_init` | 同板块同结构 | 内部闭环逻辑 | 内部闭环逻辑 |

#### 系统调用
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `user/src/lib.rs#sigaction` | `user/src/lib.rs#sigaction` | 同板块同名 | KernelSigAction::default, act.map, core::mem::size_of::<SignalSet>, core::ptr::null, core::ptr::null_mut, from_kernel_sigaction ...(+4) | sys_sigaction |
| `user/src/lib.rs#execve` | `user/src/lib.rs#execve` | 同板块同名 | CString::new, CString::new(*s).unwrap, CString::new(path).unwrap, argv.as_ptr, argv.iter, argv.iter().map ...(+12) | sys_execve |
| `os/src/syscall/misc.rs#sys_getrandom` | `user/src/syscall.rs#sys_getrandom` | 同板块同名 | Err, Ok, Vec::with_capacity, buf.is_null, copy_to_user, current_user_token ...(+2) | buf.as_mut_ptr, syscall |
| `os/src/syscall/time.rs#sys_clock_gettime` | `user/src/syscall.rs#sys_clock_gettime` | 同板块同名 | Err, Ok, _set_sum_bit, current_time, current_time().as_nanos, current_user_token ...(+2) | syscall, tp.as_mut_ptr |

#### 文件系统
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `os/src/fs/tmpfs/dentry.rs#rename` | `os/src/fs/vfs.rs#rename` | 同板块同名 | Err, GLOBAL_DCACHE.insert, GLOBAL_DCACHE.remove_subtree, Ok, Self::clone_subtree, Some ...(+47) | unimplemented! |
| `os/src/syscall/fs.rs#sys_fstatat` | `user/src/syscall.rs#sys_fstatat` | 同板块同名 | Err, Kstat::new, Ok, core::mem::size_of::<LinuxStat>, core::slice::from_raw_parts, crate::mm::copy_to_user ...(+15) | kst.as_mut_ptr, path.as_ptr, syscall |
| `os/src/fs/vfs/kstat.rs#kstat_to_statx` | `os/src/fs/stat.rs#kstat_to_statx` | 同板块同名 | Default::default, linux_major, linux_minor, statx_attributes_from_fs_flags | 内部闭环逻辑 |
| `os/src/fs/tmpfs/dentry.rs#unlink` | `os/src/fs/vfs.rs#unlink` | 同板块同名 | Err, GLOBAL_DCACHE.remove, GLOBAL_DCACHE.remove_subtree, Ok, c.clone, child.get_dentryinner ...(+16) | unimplemented! |

#### 设备驱动
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `os/src/drivers/block/virtio_blk.rs#read_block` | `os/src/drivers/ramdisk/mod.rs#read_block` | 同板块同名 | BLK_IO_BOUNCE.lock, BlkResp::default, Some, assert_eq!, assert_ne!, blk.capacity ...(+13) | Ok, assert!, buf.copy_from_slice, core::slice::from_raw_parts |
| `os/src/drivers/block/virtio_blk.rs#write_block` | `os/src/drivers/cache.rs#write_block` | 同板块同名 | BLK_IO_BOUNCE.lock, BlkResp::default, Some, assert_eq!, assert_ne!, blk.capacity ...(+13) | Ok, buf.len, buf.to_vec, buffer.copy_from_slice, panic!, self.block_size ...(+2) |
| `os/src/drivers/block/virtio_blk.rs#size` | `os/src/drivers/disk.rs#size` | 同板块同名 | self.0.lock, self.0.lock().capacity | self.dev.num_blocks |
| `os/src/drivers/block/virtio_blk.rs#block_size` | `os/src/drivers/cache.rs#block_size` | 同板块同名 | 内部闭环逻辑 | self.inner.lock, self.inner.lock().block_size |

#### 网络协议栈
仅目标项目中观察到该模块代表函数，参考项目没有同模块可对齐证据：
- 目标项目：`netperf-2.7.0/src/netrt_rtnetlink.c#find_egress_interface`，调用摘要：NLMSG_DATA, NLMSG_LENGTH, NLMSG_NEXT, NLMSG_OK, RTA_DATA, RTA_NEXT ...(+15)
- 目标项目：`os/src/net/tcp.rs#dispatch_kernel_tcp_service`，调用摘要：KERNEL_NEXT_ISS.fetch_add, KERNEL_TCP_CONNS.lock, Vec::new, conn.snd_nxt.wrapping_add, conns.push, conns.swap_remove ...(+9)
- 目标项目：`os/src/socket/mod.rs#shutdown`，调用摘要：Err, Ok, crate::net::tcp::tcp_send_segment, matches!, tcp.lock, tcp.lock().receive_queue.lock ...(+5)
- 目标项目：`netperf-2.7.0/src/netlib.c#calculate_confidence`，调用摘要：confid, fflush, fprintf, min, printf, sqrt

#### 文件系统/第三方库
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `lwext4_rust/c/lwext4/src/ext4_balloc.c#ext4_balloc_alloc_block` | `lwext4_rust/c/lwext4/src/ext4_balloc.c#ext4_balloc_alloc_block` | 同板块同名同结构 | ext4_balloc_get_bgid_of_block, ext4_balloc_get_block_of_bgid, ext4_balloc_set_bitmap_csum, ext4_balloc_verify_bitmap_csum, ext4_bg_get_block_bitmap, ext4_bg_get_free_blocks_count ...(+19) | ext4_balloc_get_bgid_of_block, ext4_balloc_get_block_of_bgid, ext4_balloc_set_bitmap_csum, ext4_balloc_verify_bitmap_csum, ext4_bg_get_block_bitmap, ext4_bg_get_free_blocks_count ...(+19) |
| `lwext4_rust/c/lwext4/src/ext4_journal.c#jbd_journal_prepare` | `lwext4_rust/c/lwext4/src/ext4_journal.c#jbd_journal_prepare` | 同板块同名同结构 | EXT4_BLOCK_ZERO, RB_FIND, TAILQ_FOREACH_REVERSE_SAFE, TAILQ_FOREACH_SAFE, TAILQ_REMOVE, ext4_bcache_clear_dirty ...(+17) | EXT4_BLOCK_ZERO, RB_FIND, TAILQ_FOREACH_REVERSE_SAFE, TAILQ_FOREACH_SAFE, TAILQ_REMOVE, ext4_bcache_clear_dirty ...(+17) |
| `lwext4_rust/c/lwext4/src/ext4.c#ext4_dir_rm` | `lwext4_rust/c/lwext4/src/ext4.c#ext4_dir_rm` | 同板块同名同结构 | EXT4_MP_LOCK, EXT4_MP_UNLOCK, ext4_block_cache_write_back, ext4_dir_en_get_inode, ext4_dir_iterator_fini, ext4_dir_iterator_init ...(+19) | EXT4_MP_LOCK, EXT4_MP_UNLOCK, ext4_block_cache_write_back, ext4_dir_en_get_inode, ext4_dir_iterator_fini, ext4_dir_iterator_init ...(+19) |
| `lwext4_rust/c/lwext4/src/ext4_dir_idx.c#ext4_dir_dx_split_index` | `lwext4_rust/c/lwext4/src/ext4_dir_idx.c#ext4_dir_dx_split_index` | 同板块同名同结构 | ext4_block_set, ext4_dir_dx_climit_get_count, ext4_dir_dx_climit_get_limit, ext4_dir_dx_climit_set_count, ext4_dir_dx_climit_set_limit, ext4_dir_dx_entry_get_hash ...(+10) | ext4_block_set, ext4_dir_dx_climit_get_count, ext4_dir_dx_climit_get_limit, ext4_dir_dx_climit_set_count, ext4_dir_dx_climit_set_limit, ext4_dir_dx_entry_get_hash ...(+10) |

#### 通用模块/其他
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `polyhal/polyhal-boot/src/arch/loongarch64.rs#_start` | `user/src/lib.rs#_start` | 同板块同名 | naked_asm! | HEAP.lock, HEAP.lock()
            .init, HEAP_SPACE.as_ptr, exit, main |
| `polyhal/polyhal/src/debug_console/x86_64.rs#putchar` | `os/src/arch/loongarch64/qemu/console.rs#putchar` | 同板块同名 | com::putchar, graphic::putchar, vga_text::putchar | ptr.add, ptr.add(0).write_volatile, ptr.add(5).read_volatile |
| `polyhal/polyhal-boot/src/arch/mod.rs#clear_bss` | `os/src/main.rs#clear_bss` | 同板块同名 | core::slice::from_raw_parts_mut, core::slice::from_raw_parts_mut(
            _sbss as usize as *mut u128,
            (_ebss as usize - _sbss as usize) / size_of::<u128>(),
        )
        .fill, size_of::<u128> | core::slice::from_raw_parts_mut, core::slice::from_raw_parts_mut(sbss as usize as *mut u8, ebss as usize - sbss as usize)
            .fill |
| `polyhal/polyhal/src/components/instruction/loongarch64.rs#shutdown` | `os/src/sbi.rs#shutdown` | 同板块同名 | PhysAddr, ged_addr.get_mut_ptr::<u8>, ged_addr.get_mut_ptr::<u8>().write_volatile, log::info!, log::warn!, loongArch64::asm::idle ...(+1) | system_reset, unreachable! |


好的，我将以操作系统内核架构审计专家的身份，基于提供的所有材料，对本案进行最终的定性仲裁。

### 1. 宏观判定
- **定性结论**: **独立开发 (底层架构显著差异)**
- **核心依据**: 客观 AST 相似度仅为 **12.7%**，远低于可认定同源性的阈值。全项目共同 AST 指纹仅 568 个，且主要由第三方公共 C 库 `lwext4_rust` 贡献。在操作系统内核的核心模块（进程管理、内存管理、任务调度等），双方共同指纹极少，且均为语言标准库惯用法或 POSIX 标准接口名，不构成独创性表达。双方在核心调度、内存模型、系统调用实现上展现出根本性的架构异构。

### 2. 全模块覆盖矩阵解读

| 模块 | 双方存在 | 共同指纹 | 目标覆盖 | 参考覆盖 | 证据强度 | 解读 |
| :--- | :---: | :---: | :---: | :---: | :---: | :--- |
| **进程管理** | 是 | 4 | 1.51% | 5.48% | **弱** | 共同指纹为 `Option::as_ref().map`、`Option::take` 等 Rust 标准库极简惯用法。双方在 `run_tasks`、`sys_clone`、`sys_wait4` 等核心调度与系统调用上调用链显著分叉，证明架构设计独立。 |
| **任务调度/异步运行时** | 是 | 4 | 1.16% | 3.25% | **弱** | 共同指纹来自 `sys_sendto`、`sys_recvfrom` 等网络系统调用，其相似性源于 POSIX 标准接口定义，而非内部实现逻辑。 |
| **内存管理** | 是 | 28 | 3.67% | 11.24% | **中** | 共同指纹数量相对较多，但主要源于 `syscall` 分发、`from_elf` 等通用功能。深入分析 `from_elf` 等函数可发现双方在内存抽象（如 `MapArea` vs 直接地址计算）上存在本质差异。 |
| **中断/异常处理** | 是 | 3 | 3.19% | 15.0% | **弱** | 共同指纹为 `enable_timer_interrupt` 和 `init_trap`，这是任何 OS 都必须实现的硬件相关操作，且调用路径（`polyhal` 抽象层 vs 直接架构相关代码）不同。 |
| **系统调用** | 是 | 6 | 4.2% | 7.41% | **弱** | 共同指纹多为用户库侧的 `syscall` 包装函数或标准 POSIX 接口（如 `execve`），内核侧实现差异巨大。 |
| **文件系统** | 是 | 8 | 1.05% | 4.06% | **弱** | 共同指纹极少。参考项目的 `rename`、`unlink` 等核心 VFS 函数为 `unimplemented!`，而目标项目有完整实现，表明目标项目在该模块的工作量和原创性远超参考项目。 |
| **设备驱动** | 是 | 0 | 0.0% | 0.0% | **未形成** | 双方虽都有驱动模块，但未检测到任何共同 AST 指纹，表明驱动模型和实现完全独立。 |
| **网络协议栈** | 否 | 0 | 0.0% | 0.0% | **未形成** | 该模块仅存在于目标项目，是目标项目独立开发的显著特征。 |
| **第三方库/公共依赖** | 是 | 498 | 75.91% | 83.28% | **强（但属公共代码）** | 这是全案高参考覆盖率的唯一来源。双方均引用了 `lwext4_rust` 公共 C 库，其 AST 指纹完全一致是必然的，**不能作为抄袭证据**。 |

### 3. 底层 AST 关键证据指纹表
> 表格按模块优先级排序，第三方库证据后置。

| 所属板块 | 目标项目路径 | 参考项目路径 | 证据类型 | 技术说明 |
| :--- | :--- | :--- | :--- | :--- |
| 进程管理 | `os/src/task/processor.rs#current` | `os/src/task/processor.rs#current` | 弱证据 | 两者均为 `self.current.as_ref().map(...)`。这是 Rust `Option<T>` 类型的标准 getter 模式，无独创性。 |
| 进程管理 | `os/src/task/processor.rs#take_current` | `os/src/task/processor.rs#take_current` | 弱证据 | 两者均为 `self.current.take()`。这是 Rust `Option<T>` 类型的标准所有权取出操作，是唯一且必然的写法。 |
| 进程管理 | `os/src/task/manager.rs#num_processes` | `os/src/task/manager.rs#ready_procs_num` | 弱证据 | 调用图谱均为 `Mutex<Collection>.lock().len()`。这是对互斥锁保护的集合取长度的通用模式，且操作的全局对象（`PID2PCB` vs `READY_QUEUE`）语义不同。 |
| 进程管理 | `os/src/task/process.rs#get_user_token` | `os/src/task/task/task.rs#ppid` | 弱证据 | 调用图谱均为 `self.field.nested_field`。这是访问嵌套结构体字段的唯一语法形式，且访问的字段（`vm_set.token` vs `process.ppid`）分属内存管理和进程关系两个不同领域。 |
| 内存管理 | `os/src/mm/vm_set.rs#from_elf` | `os/src/mm/memory_set/inner.rs#from_elf` | 中证据 | 双方均实现了从 ELF 文件加载内存映射的功能。目标项目使用 `GLOBAL_DCACHE` 和直接地址计算，参考项目使用 `MapArea::new` 和 `Aux::new` 等抽象，内存布局策略不同。 |
| 中断/异常处理 | `polyhal/.../loongarch64.rs#enable_timer_interrupt` | `os/src/arch/loongarch64/.../trap_interface.rs#enable_timer_interrupt` | 弱证据 | 目标项目通过 `polyhal` 抽象层操作 `ecfg` 寄存器，参考项目直接操作 `ecfg` 和 `ticlr` 寄存器。架构分层不同。 |
| 系统调用 | `os/src/syscall/mod.rs#syscall` | `os/src/syscall/mod.rs#syscall` | 中证据 | 两者均为系统调用总入口，包含大量 `sys_*` 函数调用。这是任何 OS 的必要结构，但具体分发的系统调用号和内部实现差异巨大。 |
| 第三方库 | `lwext4_rust/c/lwext4/src/ext4_balloc.c#ext4_balloc_alloc_block` | `lwext4_rust/c/lwext4/src/ext4_balloc.c#ext4_balloc_alloc_block` | 强证据（公共代码） | 函数体、调用图谱完全一致。双方均引用了相同的开源 C 库 `lwext4`，此部分相似度与内核原创性无关。 |

### 4. 核心代码调用流对比剖析

* **【进程管理/核心调度】**
* **底层技术析理**: 双方 `run_tasks` 函数是调度器的核心循环，但其实现揭示了完全不同的 CPU 抽象与同步模型。目标项目采用全局 `PROCESSORS` 数组管理多核，通过 `PROCESSORS[id].as_mut().unwrap().lock()` 获取核的独占锁，并调用 `check_timers` 和 `context_switch`。参考项目则通过 `get_proc_by_hartid(hart_id())` 函数获取 Hart 结构体，无显式锁操作，并调用 `check_futex_timer` 和 `switch`。这表明双方在多核管理、定时器机制和上下文切换的函数命名与实现上均独立发展。
* **代码溯源链接**:
  - 目标项目: `os/src/task/processor.rs#run_tasks`
  - 参考项目: `os/src/task/processor.rs#run_tasks`

* **【内存管理/ELF加载】**
* **底层技术析理**: 在解析 ELF 文件构建进程地址空间时，目标项目的 `from_elf` 函数直接使用 `ph.virtual_addr()` 进行算术运算来计算映射地址，并依赖 `GLOBAL_DCACHE` 进行缓存。参考项目则引入了 `MapArea::new`、`Aux::new` 等更高层次的抽象来构建内存区域，并调用 `Self::new_from_kernel`。这体现了双方在内存管理上“过程式直接计算”与“对象化抽象构建”的架构风格差异。
* **代码溯源链接**:
  - 目标项目: `os/src/mm/vm_set.rs#from_elf`
  - 参考项目: `os/src/mm/memory_set/inner.rs#from_elf`

* **【系统调用/进程创建】**
* **底层技术析理**: `sys_clone` 是创建进程的核心系统调用。目标项目的实现通过 `process._clone` 方法进行内部克隆，错误处理使用 `SysError::try_from`。参考项目则显式地调用 `MemorySet::new`、`FdTable::from_another`、`FsInfo::from_another` 来分别复制内存、文件描述符和文件系统信息。目标项目将复制逻辑封装在 `_clone` 方法中，而参考项目在主调函数中显式组合，二者在职责划分和实现路径上存在根本不同。
* **代码溯源链接**:
  - 目标项目: `os/src/syscall/process.rs#sys_clone`
  - 参考项目: `os/src/syscall/process/clone.rs#sys_clone`

### 5. 单方模块与降权项

*   **单方模块**:
    *   **网络协议栈**: 仅存在于目标项目，包含 TCP、Socket 等完整实现，是目标项目独立开发、功能更强的显著标志。
*   **降权项**:
    *   **第三方库 `lwext4_rust`**: 双方共同引用的开源 C 库，贡献了全案绝大部分共同 AST 指纹（498/568）。此部分相似度是公共代码复用，**不构成同源证据**。
    *   **POSIX/Linux 标准接口名**: `sys_clone`、`sys_wait4`、`sys_getpid` 等函数名是 Linux ABI 的强制要求，任何兼容 Linux 的操作系统课程项目都会使用，**不具备独创性**。
    *   **Rust 语言惯用法与泛型宏**: `Option::take`、`as_ref().map`、`Mutex::lock()`、`Err`、`Ok`、`Some`、`log::info!` 等是 Rust 编程的标准元素，其 AST 指纹相同是语言必然性，**不能作为抄袭证据**。
    *   **跨板块/跨语言证据**: `netperf-2.7.0` 是 C 语言编写的第三方网络测试工具，与参考项目的 Rust 内核无任何可比性，**完全不能作为证据**。
    *   **证据抽取失败**: 外部证据抽取 API 返回 `401 Unauthorized`，表明部分证据的生成过程存在可靠性风险，基于此得出的结论应审慎对待。

### 6. 保守结论

综合全案证据，目标项目 [2026-T2026181239910993-XX] 与参考项目 [2025-塔特林设计局-华中科技大学] 的客观 AST 相似度仅为 12.7%，处于极低水平。全案共同指纹高度集中于双方引用的同一开源第三方 C 库 `lwext4_rust`，剔除该公共代码后，内核核心模块的共同指纹覆盖率极低。控方所指控的“核心铁证”，经技术分析，均为 Rust 语言的标准惯用法或 POSIX 标准接口名，不具备可保护的独创性。相反，在进程调度、内存管理、系统调用等核心领域，双方在 CPU 抽象、内存模型、同步策略和实现路径上展现出根本性的架构差异。特别是目标项目独立实现了完整的网络协议栈，进一步佐证了其开发工作的独立性。因此，本案整体风险极低，无任何证据表明目标项目对参考项目存在系统性的代码抄袭或架构复用。

------------------------------

