# 🛡️ OS 内核赛自动化代码级架构审计报告

**审计时间**: `2026-07-02 13:08:16`
**参赛作品名称**: `2026-T2026105619910101-XX`
**年份**: `2026`
**学校**: `XX`
**队伍名称**: `T2026105619910101`
**比赛名字**: `OS-Competition`
**比赛子赛道**: `Kernel-Implementation`
**仓库地址**: `https://gitlab.eduxiji.net/educg-group-43501-3132633/T2026105619910101-357`
**推测开发语言**: `Rust / C (由AST探针自动识别)`
**特征函数量**: `2860`

## ⚔️ 底层代码同源性对比 (AST Homology)
> 本部分由专门的代码审查智能体，对该项目与符合年份条件的历届最高相似度项目进行控制流与底层调用对比。新版报告会先给出确定性的全模块覆盖矩阵，再给出 Agent 的分模块解释，避免只围绕少量抽样函数展开。

### 🏆 Top 5 底层对标: vs `2024-SCUCCS-CST-SRA-四川大学`（四川大学 / SCUCCS-CST-SRA）

#### 对方作品信息

**对方年份**: `2024`
**对方学校**: `四川大学`
**对方队伍名称**: `SCUCCS-CST-SRA`
**对方比赛名字**: `OS-Competition`
**对方比赛子赛道**: `Kernel-Implementation`
**对方仓库地址**: `https://gitlab.eduxiji.net/educg-group-26011-2376549/T202410610993313-2141`

> 双向覆盖率：共同 AST 指纹: 395；唯一 AST 指纹: 目标项目 2421，参考项目 667；覆盖率: 目标项目 16.32%，参考项目 59.22%

## 全模块对比总览（确定性证据）

> 总体覆盖率：共同 AST 指纹: 395；唯一 AST 指纹: 目标项目 2421，参考项目 667；覆盖率: 目标项目 16.32%，参考项目 59.22%

### 1. 模块覆盖矩阵
| 模块 | 目标函数数 | 参考函数数 | 共同唯一 AST 指纹 | Target 覆盖 | Base 覆盖 | 状态 |
| :--- | ---: | ---: | ---: | ---: | ---: | :--- |
| 进程管理 | 48 | 15 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |
| 任务调度/异步运行时 | 220 | 35 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |
| 内存管理 | 158 | 97 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |
| 中断/异常处理 | 3 | 17 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |
| 系统调用 | 159 | 26 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |
| 文件系统 | 492 | 54 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |
| 设备驱动 | 0 | 9 | 0 | 0.0% | 0.0% | 仅参考项目体现 |
| 网络协议栈 | 0 | 0 | 0 | 0.0% | 0.0% | 双方均未体现 |
| 文件系统/第三方库 | 684 | 501 | 395 | 68.7% | 96.58% | 双方存在且有结构重合 |
| 网络协议栈/第三方库 | 67 | 0 | 0 | 0.0% | 0.0% | 仅目标项目体现 |
| 第三方库 | 1014 | 0 | 0 | 0.0% | 0.0% | 仅目标项目体现 |
| 通用模块/其他 | 15 | 49 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |

### 2. 分模块证据清单

#### 进程管理
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `kernel/src/syscall/task/execve.rs#sys_execve` | `src/core/syscall.c#sys_execve` | 同板块同名 | Arc::new, Default::default, Err, FD_TABLE.write, FS_CONTEXT.lock, FS_CONTEXT.lock().resolve ...(+42) | argraw, exec, get_path, kill, my_proc, strcpy |
| `kernel/src/pseudofs/proc.rs#lookup_child` | `src/core/proc.c#switch_to` | 同模块上下文对齐/差异观察 | Arc::downgrade, Arc::new, CStr::from_bytes_until_nul, CStr::from_bytes_until_nul(&input)
                                    .map_err, CStr::from_bytes_until_nul(&input)
                                    .map_err(|_| VfsError::InvalidInput)?
                                    .to_str, CStr::from_bytes_until_nul(&input)
                                    .map_err(|_| VfsError::InvalidInput)?
                                    .to_str()
                                    .map_err ...(+55) | invtlb_all, swtch, w_csr_pgdl |
| `kernel/src/task/signal.rs#send_signal_to_process` | `src/core/proc.c#in_kernel_memory` | 同模块上下文对齐/差异观察 | Ok, get_process_data, get_task, info!, proc_data.proc.threads, proc_data.signal.send_signal ...(+5) | 内部闭环逻辑 |
| `kernel/src/syscall/signal.rs#sys_rt_sigprocmask` | `src/core/proc.c#yield` | 同模块上下文对齐/差异观察 | Err, Ok, check_sigset_size, curr.as_thread, current, debug! ...(+7) | my_proc, swtch |

#### 任务调度/异步运行时
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `kernel/src/syscall/task/clone.rs#sys_clone` | `src/core/syscall.c#sys_clone` | 同板块同名 | CloneFlags::from_bits_truncate, Err, args.do_clone, clone_flags.contains | fork, my_proc |
| `kernel/src/syscall/fs/ctl.rs#sys_chdir` | `src/core/syscall.c#sys_chdir` | 同板块同名 | AxError::from, Err, FS_CONTEXT.lock, Ok, curr.as_thread, curr.as_thread().proc_data.gid ...(+8) | argstr, my_proc, printf, strcat, strcmp, strcpy ...(+1) |
| `kernel/src/syscall/signal.rs#sys_rt_sigaction` | `src/core/syscall.c#sys_rt_sigaction` | 同板块同名 | Err, Ok, act.nullable, act.vm_read_uninit, act.vm_read_uninit()?.assume_init, actions[signo].clone ...(+11) | argraw, assert, my_proc |
| `kernel/src/syscall/fs/fd_ops.rs#sys_openat` | `src/core/syscall.c#sys_openat` | 同板块同名 | Some, add_to_fd, check_text_file_busy, current, current().as_thread, current().as_thread().proc_data.umask ...(+11) | argraw, get_path, my_proc, openat |

#### 内存管理
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `kernel/src/syscall/mm/mmap.rs#sys_mmap` | `src/core/syscall.c#sys_mmap` | 同板块同名 | Arc::new, Backend::new_alloc, Backend::new_cow, Backend::new_file, Backend::new_linear, Backend::new_shared ...(+52) | argraw, kmalloc |
| `kernel/src/syscall/mm/brk.rs#sys_brk` | `src/core/syscall.c#sys_brk` | 同板块同名 | Backend::new_alloc, Ok, VirtAddr::from, align_up_4k, curr.as_thread, current ...(+12) | argraw, assert, my_proc |
| `kernel/src/syscall/mod.rs#handle_syscall` | `src/fs/fat32.c#readDirectory` | 同模块上下文对齐/差异观察 | Err, LinuxError::ENOSYS.code, LinuxError::from, LinuxError::from(err).code, Sysno::new, a0.into ...(+229) | fat_to_unix_time, free, getNextCluster, get_offset, malloc, memcpy ...(+4) |
| `kernel/src/syscall/ipc/msg.rs#sys_msgctl` | `src/core/exec.c#exec` | 同模块上下文对齐/差异观察 | AxError::from, Err, MSG_MANAGER.lock, MSG_MANAGER.lock().remove_msqid, Ok, Pid::from ...(+19) | PA2VA, alloc_vm, copy_to_va, elf_loader, hexdump, panic ...(+7) |

#### 中断/异常处理
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `kernel/src/pseudofs/dev/tty/terminal/termios.rs#default` | `src/core/kdb.c#get_trapframe_reg` | 同模块上下文对齐/差异观察 | ctl | assert, memcmp |
| `kernel/src/file/signalfd.rs#from_signal_info` | `src/core/kdb.c#info_registers` | 同模块上下文对齐/差异观察 | sig_info.code, sig_info.errno, sig_info.signo | info_csrs, printf |
| `kernel/src/pseudofs/dev/tty/terminal/termios.rs#signo_for` | `src/core/trap.c#trap_init` | 同模块上下文对齐/差异观察 | Some, self.special_char | timer_init, w_csr_ecfg, w_csr_eentry, w_csr_tlbrentry |

#### 系统调用
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `kernel/src/syscall/bpf.rs#bpf_map_update` | `src/core/syscall.c#sys_newuname` | 同模块上下文对齐/差异观察 | AxError::from, BPF_STATE.lock, Err, Ok, attr.cast::<BpfElemAttr>, attr.cast::<BpfElemAttr>().get_as_ref ...(+12) | argraw, strcpy |
| `kernel/src/syscall/time.rs#sys_setitimer` | `src/core/syscall.c#sys_writev` | 同模块上下文对齐/差异观察 | ITimerType::from_repr, ITimerType::from_repr(which).ok_or, Ok, curr
        .as_thread, curr
        .as_thread()
        .time
        .try_borrow_mut, curr
        .as_thread()
        .time
        .try_borrow_mut()
        .map_err ...(+13) | argraw, write |
| `kernel/src/syscall/bpf.rs#bpf_map_lookup` | `src/core/syscall.c#sys_statx` | 同模块上下文对齐/差异观察 | AxError::from, BPF_STATE.lock, Err, Ok, attr.cast::<BpfElemAttr>, attr.cast::<BpfElemAttr>().get_as_ref ...(+16) | argraw |
| `kernel/src/syscall/io_mpx/epoll.rs#sys_epoll_ctl` | `src/core/syscall.c#sys_splice` | 同模块上下文对齐/差异观察 | Epoll::from_fd, EpollFlags::from_bits, EpollFlags::from_bits(event.events & !events.bits()).ok_or, Err, IoEvents::from_bits_truncate, Ok ...(+7) | argraw, splice |

#### 文件系统
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `kernel/src/syscall/fs/pipe.rs#sys_pipe2` | `src/core/syscall.c#sys_pipe2` | 同板块同名 | Ok, Pipe::new, PipeFlags::from_bits_truncate, close_file_like, close_file_like(read_fd).unwrap, debug! ...(+9) | argraw, pipe |
| `kernel/src/syscall/fs/io.rs#sys_sendfile` | `src/core/syscall.c#sys_sendfile` | 同板块同名 | Err, File::from_fd, SendFile::Direct, SendFile::Offset, debug!, do_send ...(+4) | argraw, panic, read, write |
| `kernel/src/syscall/fs/io.rs#sys_lseek` | `src/core/syscall.c#sys_lseek` | 同板块同名 | Err, File::from_fd, File::from_fd(fd)?.inner, File::from_fd(fd)?.inner().seek, Ok, SeekFrom::Current ...(+3) | argraw, lseek |
| `kernel/src/syscall/fs/stat.rs#sys_fstat` | `src/core/syscall.c#sys_fstat` | 同板块同名 | core::ptr::null, sys_fstatat | argraw, argstr, printf |

#### 设备驱动
仅参考项目中观察到该模块代表函数，目标项目没有同模块可对齐证据：
- 参考项目：`src/driver/ahci.c#ahic_probe_port`，调用摘要：printf
- 参考项目：`src/driver/ahci.c#find_cmdslot`，调用摘要：Log, get_bits_range
- 参考项目：`src/driver/ahci.c#ahci_write`，调用摘要：do_ahci_write, memcpy
- 参考项目：`src/driver/ahci.c#ahci_read`，调用摘要：do_ahci_read, memcpy

#### 文件系统/第三方库
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `third_party/lwext4_rust/c/lwext4/src/ext4_balloc.c#ext4_balloc_alloc_block` | `src/fs/lwext4/src/ext4_balloc.c#ext4_balloc_alloc_block` | 同板块同名同结构 | ext4_balloc_get_bgid_of_block, ext4_balloc_get_block_of_bgid, ext4_balloc_set_bitmap_csum, ext4_balloc_verify_bitmap_csum, ext4_bg_get_block_bitmap, ext4_bg_get_free_blocks_count ...(+19) | ext4_balloc_get_bgid_of_block, ext4_balloc_get_block_of_bgid, ext4_balloc_set_bitmap_csum, ext4_balloc_verify_bitmap_csum, ext4_bg_get_block_bitmap, ext4_bg_get_free_blocks_count ...(+19) |
| `third_party/lwext4_rust/c/lwext4/src/ext4_journal.c#jbd_journal_prepare` | `src/fs/lwext4/src/ext4_journal.c#jbd_journal_prepare` | 同板块同名同结构 | EXT4_BLOCK_ZERO, RB_FIND, TAILQ_FOREACH_REVERSE_SAFE, TAILQ_FOREACH_SAFE, TAILQ_REMOVE, ext4_bcache_clear_dirty ...(+17) | EXT4_BLOCK_ZERO, RB_FIND, TAILQ_FOREACH_REVERSE_SAFE, TAILQ_FOREACH_SAFE, TAILQ_REMOVE, ext4_bcache_clear_dirty ...(+17) |
| `third_party/lwext4_rust/c/lwext4/src/ext4.c#ext4_dir_rm` | `src/fs/lwext4/src/ext4.c#ext4_dir_rm` | 同板块同名同结构 | EXT4_MP_LOCK, EXT4_MP_UNLOCK, ext4_block_cache_write_back, ext4_dir_en_get_inode, ext4_dir_iterator_fini, ext4_dir_iterator_init ...(+19) | EXT4_MP_LOCK, EXT4_MP_UNLOCK, ext4_block_cache_write_back, ext4_dir_en_get_inode, ext4_dir_iterator_fini, ext4_dir_iterator_init ...(+19) |
| `third_party/lwext4_rust/c/lwext4/src/ext4_dir_idx.c#ext4_dir_dx_split_index` | `src/fs/lwext4/src/ext4_dir_idx.c#ext4_dir_dx_split_index` | 同板块同名同结构 | ext4_block_set, ext4_dir_dx_climit_get_count, ext4_dir_dx_climit_get_limit, ext4_dir_dx_climit_set_count, ext4_dir_dx_climit_set_limit, ext4_dir_dx_entry_get_hash ...(+10) | ext4_block_set, ext4_dir_dx_climit_get_count, ext4_dir_dx_climit_get_limit, ext4_dir_dx_climit_set_count, ext4_dir_dx_climit_set_limit, ext4_dir_dx_entry_get_hash ...(+10) |

#### 网络协议栈/第三方库
仅目标项目中观察到该模块代表函数，参考项目没有同模块可对齐证据：
- 目标项目：`third_party/axnet-ng/src/device/ethernet.rs#process_arp`，调用摘要：ArpPacket::new_checked, ArpPacket::new_checked(payload).and_then, ArpPacket::new_unchecked, ArpRepr::parse, IpAddress::Ipv4, Self::send_to ...(+21)
- 目标项目：`third_party/axnet-ng/src/udp.rs#recv`，调用摘要：Err, ExpectedRemote::Any, ExpectedRemote::Expecting, Ok, SocketAddrEx::Ip, Some ...(+20)
- 目标项目：`third_party/axnet-ng/src/tcp.rs#connect`，调用摘要：Err, IpEndpoint::from, Ok, Some, ax_err_type!, axtask::yield_now ...(+22)
- 目标项目：`third_party/axnet-ng/src/udp.rs#send`，调用摘要：Err, IpAddr::V4, IpEndpoint::from, Ok, PacketMeta::default, SocketAddr::new ...(+22)

#### 第三方库
仅目标项目中观察到该模块代表函数，参考项目没有同模块可对齐证据：
- 目标项目：`third_party/axio/src/read/mod.rs#default_read_to_end`，调用摘要：Err, Ok, Some, buf.capacity, buf.extend_from_slice, buf.len ...(+24)
- 目标项目：`third_party/arceos/axruntime/src/lib.rs#rust_main`，调用摘要：INITED_CPUS.fetch_add, _edata.as_ptr, _etext.as_ptr, _stext.as_ptr, ax_println!, axbacktrace::init ...(+28)
- 目标项目：`third_party/axio/src/buffered/linewriter/shim.rs#write`，调用摘要：Ok, memchr::memrchr, self.buffer.capacity, self.buffer.flush_buf, self.buffer.write, self.buffer.write_to_buf ...(+4)
- 目标项目：`third_party/arceos/axfs/src/highlevel/file.rs#set_len`，调用摘要：(len - page_start).min, Ok, dirty_evicted.clear, dirty_evicted.get_mut, dirty_evicted.retain, file.set_len ...(+23)

#### 通用模块/其他
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `kernel/src/time.rs#try_into_time_value` | `src/lolibc/kstruct/list.c#test_kstruct_list` | 同模块上下文对齐/差异观察 | Err, Ok, TimeValue::new | init_list_head, list_del, list_for_each_entry, list_for_each_entry_safe, printf |
| `kernel/src/time.rs#try_into_time_value` | `src/core/kdb.c#info_csrs` | 同模块上下文对齐/差异观察 | Err, Ok, TimeValue::new | printf, r_csr_badv, r_csr_dmw0, r_csr_dmw1, r_csr_dmw2, r_csr_dmw3 ...(+7) |
| `kernel/src/time.rs#try_into_time_value` | `src/lolibc/stdio.c#_uitoa` | 同模块上下文对齐/差异观察 | Err, Ok, TimeValue::new | panic_on |
| `kernel/src/time.rs#try_into_time_value` | `src/lolibc/struct/mlist.c#mlist_remove` | 同模块上下文对齐/差异观察 | Err, Ok, TimeValue::new | assert, free, mlist_is_empty |


#### 1. 宏观判定
- **定性结论**: 独立开发 (相似度: 14.67%，共同 AST 指纹: 395；唯一 AST 指纹: 目标项目 2421，参考项目 667；覆盖率: 目标项目 16.32%，参考项目 59.22%)

#### 2. 全模块覆盖矩阵解读
- **进程管理**: 双方均存在该模块。共同 AST 指纹为 0。目标项目覆盖率 0.0%，参考项目覆盖率 0.0%。该模块证据强度为**未形成**。
- **任务调度/异步运行时**: 双方均存在该模块。共同 AST 指纹为 0。目标项目覆盖率 0.0%，参考项目覆盖率 0.0%。该模块证据强度为**未形成**。
- **内存管理**: 双方均存在该模块。共同 AST 指纹为 0。目标项目覆盖率 0.0%，参考项目覆盖率 0.0%。该模块证据强度为**未形成**。
- **中断/异常处理**: 双方均存在该模块。共同 AST 指纹为 0。目标项目覆盖率 0.0%，参考项目覆盖率 0.0%。该模块证据强度为**未形成**。
- **系统调用**: 双方均存在该模块。共同 AST 指纹为 0。目标项目覆盖率 0.0%，参考项目覆盖率 0.0%。该模块证据强度为**未形成**。
- **文件系统**: 双方均存在该模块。共同 AST 指纹为 0。目标项目覆盖率 0.0%，参考项目覆盖率 0.0%。该模块证据强度为**未形成**。
- **设备驱动**: 仅参考项目存在该模块。共同 AST 指纹为 0。该模块证据强度为**未形成**。
- **网络协议栈**: 双方均未体现该模块。共同 AST 指纹为 0。该模块证据强度为**未形成**。
- **第三方库/公共依赖**: 双方均存在该模块。共同 AST 指纹为 395。目标项目覆盖率 68.7%，参考项目覆盖率 96.58%。该模块证据强度为**强**，但属于双方共同引用的开源第三方库 `lwext4`，不构成项目间原创性复用。

#### 3. 底层 AST 关键证据指纹表
| 所属板块 | 目标项目路径 | 参考项目路径 | 证据类型 | 技术说明 |
| :--- | :--- | :--- | :--- | :--- |
| 第三方库/公共依赖 | `third_party/lwext4_rust/c/lwext4/src/ext4_balloc.c#ext4_balloc_alloc_block` | `src/fs/lwext4/src/ext4_balloc.c#ext4_balloc_alloc_block` | 同源指纹 | 双方调用图谱完全一致，均调用 `ext4_balloc_get_bgid_of_block`、`ext4_balloc_set_bitmap_csum` 等内部函数。 |
| 第三方库/公共依赖 | `third_party/lwext4_rust/c/lwext4/src/ext4_journal.c#jbd_journal_prepare` | `src/fs/lwext4/src/ext4_journal.c#jbd_journal_prepare` | 同源指纹 | 双方调用图谱完全一致，均使用 `TAILQ_FOREACH_SAFE`、`ext4_bcache_clear_dirty` 等宏和函数。 |
| 第三方库/公共依赖 | `third_party/lwext4_rust/c/lwext4/src/ext4.c#ext4_dir_rm` | `src/fs/lwext4/src/ext4.c#ext4_dir_rm` | 同源指纹 | 双方调用图谱完全一致，均调用 `ext4_dir_iterator_init`、`ext4_dir_en_get_inode` 等函数。 |
| 第三方库/公共依赖 | `third_party/lwext4_rust/c/lwext4/src/ext4_dir_idx.c#ext4_dir_dx_split_index` | `src/fs/lwext4/src/ext4_dir_idx.c#ext4_dir_dx_split_index` | 同源指纹 | 双方调用图谱完全一致，均调用 `ext4_dir_dx_entry_get_hash`、`ext4_block_set` 等函数。 |
| 进程管理 | `kernel/src/syscall/task/execve.rs#sys_execve` | `src/core/syscall.c#sys_execve` | 同名异构 | 目标项目使用 `Arc::new`、`FS_CONTEXT.lock().resolve`；参考项目使用 `argraw`、`get_path`、`exec`。 |
| 任务调度/异步运行时 | `kernel/src/syscall/task/clone.rs#sys_clone` | `src/core/syscall.c#sys_clone` | 同名异构 | 目标项目使用 `CloneFlags::from_bits_truncate`、`args.do_clone`；参考项目使用 `fork`、`my_proc`。 |
| 内存管理 | `kernel/src/syscall/mm/mmap.rs#sys_mmap` | `src/core/syscall.c#sys_mmap` | 同名异构 | 目标项目使用 `Backend::new_alloc`、`Backend::new_file` 等；参考项目使用 `argraw`、`kmalloc`。 |
| 系统调用 | `kernel/src/syscall/fs/pipe.rs#sys_pipe2` | `src/core/syscall.c#sys_pipe2` | 同名异构 | 目标项目使用 `Pipe::new`、`PipeFlags::from_bits_truncate`；参考项目使用 `argraw`、`pipe`。 |

#### 4. 核心代码调用流对比剖析
* **【进程管理/execve 系统调用路径】**
* **底层技术析理**: 目标项目 `sys_execve` 的调用流为 `FS_CONTEXT.lock().resolve` -> `Arc::new` -> `Default::default`，体现了基于 VFS 抽象、所有权管理和 `Result` 错误传播的 Rust 异步内核范式。参考项目 `sys_execve` 的调用流为 `argraw` -> `get_path` -> `exec` -> `kill`，体现了基于裸指针、全局变量 `my_proc` 和线性控制流的 C 宏内核范式。两者在内存模型、错误处理机制和对象生命周期管理上存在根本性架构差异。
* **代码溯源链接**:
  - 目标项目: `kernel/src/syscall/task/execve.rs#sys_execve`
  - 参考项目: `src/core/syscall.c#sys_execve`

* **【内存管理/mmap 系统调用路径】**
* **底层技术析理**: 目标项目 `sys_mmap` 的调用流为 `Backend::new_alloc` / `Backend::new_file` / `Backend::new_shared` 等多态后端分发，依赖 Rust 的 `Arc` 共享所有权和页面对齐抽象 `align_up_4k`。参考项目 `sys_mmap` 的调用流为 `argraw` -> `kmalloc`，是典型的 C 语言裸内存分配。目标项目展现了现代内核的 VMA 管理和多种映射后端策略，而参考项目仅实现了简单的内存分配，两者在内存管理子系统上的抽象层次和复杂度完全不同。
* **代码溯源链接**:
  - 目标项目: `kernel/src/syscall/mm/mmap.rs#sys_mmap`
  - 参考项目: `src/core/syscall.c#sys_mmap`

* **【任务调度/clone 系统调用路径】**
* **底层技术析理**: 目标项目 `sys_clone` 的调用流为 `CloneFlags::from_bits_truncate` -> `args.do_clone`，将标志位解析后委托给复杂的 `clone3` 参数处理逻辑。参考项目 `sys_clone` 的调用流为 `fork` -> `my_proc`，直接调用传统的 `fork` 机制。目标项目实现了 Linux 兼容的 `clone3` 语义，支持细粒度的资源共享控制，而参考项目仅实现了基础的进程复制，两者在任务创建的原语和并发模型上存在代际差异。
* **代码溯源链接**:
  - 目标项目: `kernel/src/syscall/task/clone.rs#sys_clone`
  - 参考项目: `src/core/syscall.c#sys_clone`

#### 5. 单方模块与降权项
- **设备驱动**: 仅参考项目存在该模块，目标项目无此模块。此为单方模块，不构成同源证据。
- **网络协议栈/第三方库**: 仅目标项目存在该模块，参考项目无此模块。此为单方模块，不构成同源证据。
- **第三方库**: 仅目标项目存在该模块，参考项目无此模块。此为单方模块，不构成同源证据。
- **第三方库/公共依赖 (`lwext4`)**: 双方均存在，且贡献了全部 395 个共同 AST 指纹。此为双方共同引用的开源 ext4 文件系统实现，属于公共依赖，不构成项目间的原创性复用。
- **同名系统调用**: `sys_clone`、`sys_chdir`、`sys_openat`、`sys_execve` 等函数名由 Linux ABI 强制规定，任何兼容 Linux 的内核都必须实现。同名不代表同实现，在 AST 结构完全不同的情况下，不能作为复用证据。

#### 6. 保守结论
本次审计的客观 AST 相似度仅为 14.67%，且全部 395 个共同指纹均源于双方共同引用的开源第三方库 `lwext4`。在进程管理、内存管理、任务调度、系统调用等全部核心内核模块中，共同 AST 指纹为 0。目标项目基于 Rust 的所有权系统、VFS 抽象层和异步运行时构建，与参考项目的 C 语言裸指针、全局变量和线性控制流在架构上存在系统性差异。控方提出的“降级弱信号”本质上是 Linux 标准系统调用语义的体现，而非代码复用。综合判定，目标项目对参考项目不构成代码复用，建议驳回相关指控。

------------------------------

