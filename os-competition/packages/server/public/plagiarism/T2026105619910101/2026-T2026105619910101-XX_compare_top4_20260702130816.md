# 🛡️ OS 内核赛自动化代码级架构审计报告

**审计时间**: `2026-07-02 13:08:16`
**参赛作品名称**: `2026-T2026105619910101-XX`
**年份**: `2026`
**学校**: `XX`
**队伍名称**: `T2026105619910101`
**比赛名字**: `OS-Competition`
**比赛子赛道**: `Kernel-Implementation`
**仓库地址**: `https://gitlab.eduxiji.net/educg-group-43501-3132633/T2026105619910101-357`
**推测开发语言**: `Rust / C (由AST探针自动识别)`
**特征函数量**: `2860`

## ⚔️ 底层代码同源性对比 (AST Homology)
> 本部分由专门的代码审查智能体，对该项目与符合年份条件的历届最高相似度项目进行控制流与底层调用对比。新版报告会先给出确定性的全模块覆盖矩阵，再给出 Agent 的分模块解释，避免只围绕少量抽样函数展开。

### 🏆 Top 4 底层对标: vs `2025-狗剩-海南大学`（海南大学 / 狗剩）

#### 对方作品信息

**对方年份**: `2025`
**对方学校**: `海南大学`
**对方队伍名称**: `狗剩`
**对方比赛名字**: `OS-Competition`
**对方比赛子赛道**: `Kernel-Implementation`
**对方仓库地址**: `https://gitlab.eduxiji.net/educg-group-36002-2710490/T202510589995148-2209`

> 双向覆盖率：共同 AST 指纹: 716；唯一 AST 指纹: 目标项目 2421，参考项目 3020；覆盖率: 目标项目 29.57%，参考项目 23.71%

## 全模块对比总览（确定性证据）

> 总体覆盖率：共同 AST 指纹: 716；唯一 AST 指纹: 目标项目 2421，参考项目 3020；覆盖率: 目标项目 29.57%，参考项目 23.71%

### 1. 模块覆盖矩阵
| 模块 | 目标函数数 | 参考函数数 | 共同唯一 AST 指纹 | Target 覆盖 | Base 覆盖 | 状态 |
| :--- | ---: | ---: | ---: | ---: | ---: | :--- |
| 进程管理 | 48 | 93 | 3 | 6.82% | 3.57% | 双方存在且有结构重合 |
| 任务调度/异步运行时 | 220 | 399 | 2 | 1.01% | 0.69% | 双方存在且有结构重合 |
| 内存管理 | 158 | 326 | 8 | 5.44% | 2.75% | 双方存在且有结构重合 |
| 中断/异常处理 | 3 | 62 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |
| 系统调用 | 159 | 153 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |
| 文件系统 | 492 | 747 | 52 | 12.32% | 9.19% | 双方存在且有结构重合 |
| 设备驱动 | 0 | 142 | 0 | 0.0% | 0.0% | 仅参考项目体现 |
| 网络协议栈 | 0 | 77 | 0 | 0.0% | 0.0% | 仅参考项目体现 |
| 文件系统/第三方库 | 684 | 689 | 495 | 86.09% | 84.33% | 双方存在且有结构重合 |
| 网络协议栈/第三方库 | 67 | 107 | 1 | 1.54% | 1.0% | 双方存在且有结构重合 |
| 第三方库 | 1014 | 386 | 5 | 0.58% | 1.53% | 双方存在且有结构重合 |
| 通用模块/其他 | 15 | 665 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |

### 2. 分模块证据清单

#### 进程管理
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `kernel/src/task/ops.rs#do_exit` | `api/src/imp/task.rs#do_exit` | 同板块同名 | FutexKey::new_current, SignalInfo::new_kernel, Some, axtask::yield_now, clear_child_tid.vm_write, clear_child_tid.vm_write(0).is_ok ...(+33) | Layout::new::<Tid>, SignalInfo::new, UserPtr::<Tid>::from, axtask::exit, axtask::yield_now, clear_child_tid.get_as ...(+18) |
| `kernel/src/syscall/signal.rs#sys_rt_sigprocmask` | `api/src/imp/signal.rs#sys_rt_sigprocmask` | 同板块同名 | Err, Ok, check_sigset_size, curr.as_thread, current, debug! ...(+7) | Err, Ok, check_sigset, current, current()
        .task_ext, current()
        .task_ext()
        .thread
        .signal
        .with_blocked_mut::<LinuxResult<_>> ...(+2) |
| `kernel/src/syscall/ipc/shm.rs#clear_proc_shm` | `api/src/imp/ipc/shm.rs#clear_proc_shm` | 同板块同名 | self.get_inner_by_shmid, self.get_shmids_by_pid, self.remove_pid, self.remove_shmid, shm_inner.attach_count, shm_inner.detach_process ...(+1) | SHM_MANAGER.lock, shm_inner.detach_process, shm_inner.lock, shm_manager.get_inner_by_shmid, shm_manager.get_inner_by_shmid(shmid).unwrap, shm_manager.get_shmids_by_pid ...(+1) |
| `kernel/src/syscall/ipc/shm.rs#attach_process` | `api/src/imp/ipc/shm.rs#attach_process` | 同板块同名 | assert!, monotonic_time_nanos, self.va_range.insert | assert!, self.va_range.insert |

#### 任务调度/异步运行时
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `kernel/src/syscall/sync/futex.rs#sys_futex` | `api/src/imp/futex.rs#sys_futex` | 同板块同名 | AxError::from, Err, FutexKey::new_current, Ok, Some, assert_unsigned ...(+18) | Err, Ok, TimeValue::new, axtask::yield_now, futex_table.get, futex_table.get_or_insert ...(+14) |
| `kernel/src/syscall/signal.rs#sys_kill` | `api/src/imp/signal.rs#sys_kill` | 同板块同名 | Ok, Some, current, current().as_thread, current().as_thread().proc_data.proc.group, current().as_thread().proc_data.proc.group().pgid ...(+9) | Err, Ok, current, find_process, get_all_processes, make_siginfo ...(+3) |
| `kernel/src/syscall/ipc/shm.rs#sys_shmdt` | `api/src/imp/ipc/shm.rs#sys_shmdt` | 同板块同名 | Ok, SHM_MANAGER.lock, VirtAddr::from, aspace.unmap, curr.as_thread, current ...(+14) | Ok, SHM_MANAGER.lock, VirtAddr::from, aspace.unmap, axhal::arch::flush_tlb, curr.task_ext ...(+17) |
| `kernel/src/syscall/fs/ctl.rs#sys_getcwd` | `api/src/imp/fd/ctl.rs#sys_getcwd` | 同板块同名 | CString::new, CString::new(cwd.as_str()).map_err, Err, FS_CONTEXT.lock, Ok, buf.is_null ...(+12) | CString::new, CString::new(cwd.as_str()).map_err, Err, FS_CONTEXT.lock, FS_CONTEXT.lock().current_dir, FS_CONTEXT.lock().current_dir().absolute_path ...(+10) |

#### 内存管理
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `kernel/src/syscall/mm/mmap.rs#sys_mmap` | `src/syscall_imp/mm/mmap.rs#sys_mmap` | 同板块同名 | Arc::new, Backend::new_alloc, Backend::new_cow, Backend::new_file, Backend::new_linear, Backend::new_shared ...(+52) | (addr + length).align_up, Err, MmapFlags::MAP_PRIVATE.bits, MmapFlags::MAP_SHARED.bits, MmapFlags::MAP_SHARED_VALIDATE.bits, MmapFlags::from_bits_truncate ...(+32) |
| `kernel/src/mm/loader.rs#load_user_app` | `core/src/mm.rs#load_user_app` | 同板块同名 | "/bin/sh".to_owned, (ustack_top - user_sp_aligned).align_up_4k, Backend::new_alloc, ELF_LOADER.lock, ELF_LOADER.lock().load, Err ...(+43) | CStr::from_bytes_with_nul, CStr::from_bytes_with_nul(interp)
                    .ok, CStr::from_bytes_with_nul(interp)
                    .ok()
                    .and_then, CStr::from_bytes_with_nul(interp)
                    .ok()
                    .and_then(|it| it.to_str().ok())
                    .ok_or, ElfFile::new, ElfFile::new(&file_data).map_err ...(+53) |
| `kernel/src/syscall/ipc/shm.rs#sys_shmat` | `api/src/imp/ipc/shm.rs#sys_shmat` | 同板块同名 | Arc::new, Backend::new_shared, Ok, SHM_MANAGER.lock, SharedPages::new, ShmAtFlags::from_bits_truncate ...(+27) | Err, MappingFlags::all, Ok, SHM_MANAGER.lock, ShmAtFlags::from_bits_truncate, VirtAddr::from ...(+26) |
| `kernel/src/mm/access.rs#check_null_terminated` | `api/src/ptr.rs#check_null_terminated` | 同板块同名 | Err, Layout::new::<T>, Layout::new::<T>().align, Ok, T::default, access_user_memory ...(+10) | Err, Layout::new::<T>, Layout::new::<T>().align, Ok, T::default, VirtAddrRange::from_start_size ...(+14) |

#### 中断/异常处理
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `kernel/src/pseudofs/dev/tty/terminal/termios.rs#default` | `arceos/ulib/axlibc/c/pow.c#exp_inline` | 同模块上下文对齐/差异观察 | ctl | __math_oflow, __math_uflow, asdouble, asuint64, converttoint, eval_as_double ...(+4) |
| `kernel/src/file/signalfd.rs#from_signal_info` | `arceos/modules/axhal/src/arch/aarch64/trap.rs#handle_sync_exception` | 同模块上下文对齐/差异观察 | sig_info.code, sig_info.errno, sig_info.signo | ESR_EL1.extract, crate::trap::handle_syscall, crate::trap::post_trap_callback, debug!, esr.read, esr.read_as_enum ...(+6) |
| `kernel/src/pseudofs/dev/tty/terminal/termios.rs#signo_for` | `arceos/modules/axhal/src/arch/x86_64/idt.rs#new` | 同模块上下文对齐/差异观察 | Some, self.special_char | InterruptDescriptorTable::new, core::mem::transmute, core::slice::from_raw_parts_mut, entries[i].set_handler_fn, opt.set_privilege_level |

#### 系统调用
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `kernel/src/syscall/io_mpx/epoll.rs#sys_epoll_ctl` | `api/src/imp/io_mpx/epoll.rs#sys_epoll_ctl` | 同板块同名 | Epoll::from_fd, EpollFlags::from_bits, EpollFlags::from_bits(event.events & !events.bits()).ok_or, Err, IoEvents::from_bits_truncate, Ok ...(+7) | EpollInstance::from_fd, EpollInstance::from_fd(epfd)?.control, Err, Ok, debug!, event.get_as_mut |
| `kernel/src/syscall/ipc/shm.rs#sys_shmctl` | `api/src/imp/ipc/shm.rs#sys_shmctl` | 同板块同名 | Err, Ok, SHM_MANAGER.lock, buf.get_as_mut, monotonic_time_nanos, nullable! ...(+3) | Err, Ok, SHM_MANAGER.lock, shm_inner.lock, shm_manager
            .get_inner_by_shmid, shm_manager
            .get_inner_by_shmid(shmid)
            .ok_or |
| `kernel/src/syscall/io_mpx/poll.rs#sys_ppoll` | `api/src/imp/io_mpx/poll.rs#sys_ppoll` | 同板块同名 | check_sigset_size, do_poll, fds.get_as_mut_slice, nfds.try_into, nfds.try_into().map_err, nullable! ...(+4) | Err, core::slice::from_raw_parts_mut, fds.get_as_array, poll_impl, timeout.is_none, timeout.nullable ...(+2) |
| `kernel/src/syscall/io_mpx/select.rs#sys_pselect6` | `api/src/imp/io_mpx/poll.rs#sys_pselect6` | 同板块同名 | do_select, nullable!, nullable!(timeout.get_as_ref())?
            .map, nullable!(timeout.get_as_ref())?
            .map(|ts| ts.try_into_time_value())
            .transpose, ts.try_into_time_value | Err, do_select, timeout.is_none, timeout.nullable, timeout.unwrap |

#### 文件系统
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `kernel/src/syscall/fs/io.rs#sys_splice` | `api/src/imp/io.rs#sys_splice` | 同板块同名 | DummyFd::from_fd, DummyFd::from_fd(fd_in).is_ok, DummyFd::from_fd(fd_out).is_ok, Err, File::from_fd, Pipe::from_fd ...(+20) | (len - total_copied).min, Err, Ok, SeekFrom::Current, SeekFrom::Start, buf.len ...(+43) |
| `kernel/src/pseudofs/dev/event.rs#read_at` | `arceos/modules/axfs-ng/src/fs/fat/file.rs#read_at` | 同板块同名 | Err, Ok, buf.chunks_exact_mut, buf.is_empty, buf.len, inner.has_event ...(+8) | Ok, SeekFrom::Start, file.read, file.read(buf).map_err, file.seek, file.seek(SeekFrom::Start(offset)).map_err ...(+2) |
| `kernel/src/syscall/fs/ctl.rs#sys_utimensat` | `api/src/imp/fd/ctl.rs#sys_utimensat` | 同板块同名 | Ok, Some, atime.is_none, mtime.is_none, path.is_null, time.try_into_time_value ...(+8) | Default::default, Ok, Some, TimeValue::new, atime.is_none, mtime.is_none ...(+11) |
| `kernel/src/pseudofs/dir.rs#read_dir` | `core/src/vfs/dynamic.rs#read_dir` | 同板块同名 | Ok, [DOT, DOTDOT]
            .into_iter, [DOT, DOTDOT]
            .into_iter()
            .map, [DOT, DOTDOT]
            .into_iter()
            .map(Cow::Borrowed)
            .chain, children.enumerate, children.enumerate().skip ...(+12) | Ok, [DOT, DOTDOT]
            .into_iter, [DOT, DOTDOT]
            .into_iter()
            .chain, children.enumerate, children.enumerate().skip, entry.metadata ...(+12) |

#### 设备驱动
仅参考项目中观察到该模块代表函数，目标项目没有同模块可对齐证据：
- 参考项目：`arceos/tools/raspi4/chainloader/src/bsp/device_driver/bcm/bcm2xxx_pl011_uart.rs#init`，调用摘要：FBRD::BAUD_DIVFRAC.val, IBRD::BAUD_DIVINT.val, self.flush, self.registers
            .CR
            .write, self.registers
            .LCR_H
            .write, self.registers.CR.set ...(+3)
- 参考项目：`arceos/tools/bwbench_client/src/device.rs#new`，调用摘要：ETH_P_ALL.to_be, Err, Ok, dev.bind_interface, dev.interface_mtu, ifreq.ioctl ...(+5)
- 参考项目：`arceos/tools/bwbench_client/src/main.rs#transmit_benchmark`，调用摘要：Local::now, NetDevice::new, NetDevice::new(interface).unwrap, current_time.signed_duration_since, current_time.signed_duration_since(past_time).num_seconds, dev.send ...(+2)
- 参考项目：`arceos/tools/bwbench_client/src/device.rs#bind_interface`，调用摘要：ETH_P_ALL.to_be, Err, Ok, libc::bind, self.ifreq.ioctl, std::io::Error::last_os_error ...(+2)

#### 网络协议栈
仅参考项目中观察到该模块代表函数，目标项目没有同模块可对齐证据：
- 参考项目：`arceos/ulib/axlibc/c/network.c#inet_pton`，调用摘要：hexval, inet_pton, isdigit, memmove
- 参考项目：`api/src/fd/net.rs#sendto`，调用摘要：Err, IpAddr::V4, Ipv4Addr::new, Ok, SocketAddr::new, socket
                    .bind ...(+3)
- 参考项目：`api/src/fd/net.rs#shutdown`，调用摘要：Ok, tcpsocket.lock, tcpsocket.peer_addr, tcpsocket.shutdown, udpsocket.lock, udpsocket.peer_addr ...(+1)
- 参考项目：`arceos/api/arceos_posix_api/src/imp/net.rs#shutdown`，调用摘要：Ok, tcpsocket.lock, tcpsocket.peer_addr, tcpsocket.shutdown, udpsocket.lock, udpsocket.peer_addr ...(+1)

#### 文件系统/第三方库
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `third_party/lwext4_rust/c/lwext4/src/ext4_balloc.c#ext4_balloc_alloc_block` | `crates/lwext4_rust/c/lwext4/src/ext4_balloc.c#ext4_balloc_alloc_block` | 同板块同名同结构 | ext4_balloc_get_bgid_of_block, ext4_balloc_get_block_of_bgid, ext4_balloc_set_bitmap_csum, ext4_balloc_verify_bitmap_csum, ext4_bg_get_block_bitmap, ext4_bg_get_free_blocks_count ...(+19) | ext4_balloc_get_bgid_of_block, ext4_balloc_get_block_of_bgid, ext4_balloc_set_bitmap_csum, ext4_balloc_verify_bitmap_csum, ext4_bg_get_block_bitmap, ext4_bg_get_free_blocks_count ...(+19) |
| `third_party/lwext4_rust/c/lwext4/src/ext4_journal.c#jbd_journal_prepare` | `crates/lwext4_rust/c/lwext4/src/ext4_journal.c#jbd_journal_prepare` | 同板块同名同结构 | EXT4_BLOCK_ZERO, RB_FIND, TAILQ_FOREACH_REVERSE_SAFE, TAILQ_FOREACH_SAFE, TAILQ_REMOVE, ext4_bcache_clear_dirty ...(+17) | EXT4_BLOCK_ZERO, RB_FIND, TAILQ_FOREACH_REVERSE_SAFE, TAILQ_FOREACH_SAFE, TAILQ_REMOVE, ext4_bcache_clear_dirty ...(+17) |
| `third_party/lwext4_rust/c/lwext4/src/ext4.c#ext4_dir_rm` | `crates/lwext4_rust/c/lwext4/src/ext4.c#ext4_dir_rm` | 同板块同名同结构 | EXT4_MP_LOCK, EXT4_MP_UNLOCK, ext4_block_cache_write_back, ext4_dir_en_get_inode, ext4_dir_iterator_fini, ext4_dir_iterator_init ...(+19) | EXT4_MP_LOCK, EXT4_MP_UNLOCK, ext4_block_cache_write_back, ext4_dir_en_get_inode, ext4_dir_iterator_fini, ext4_dir_iterator_init ...(+19) |
| `third_party/lwext4_rust/c/lwext4/src/ext4_dir_idx.c#ext4_dir_dx_split_index` | `crates/lwext4_rust/c/lwext4/src/ext4_dir_idx.c#ext4_dir_dx_split_index` | 同板块同名同结构 | ext4_block_set, ext4_dir_dx_climit_get_count, ext4_dir_dx_climit_get_limit, ext4_dir_dx_climit_set_count, ext4_dir_dx_climit_set_limit, ext4_dir_dx_entry_get_hash ...(+10) | ext4_block_set, ext4_dir_dx_climit_get_count, ext4_dir_dx_climit_get_limit, ext4_dir_dx_climit_set_count, ext4_dir_dx_climit_set_limit, ext4_dir_dx_entry_get_hash ...(+10) |

#### 网络协议栈/第三方库
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `third_party/axnet-ng/src/udp.rs#recv` | `arceos/modules/axnet/src/smoltcp_impl/tcp.rs#recv` | 同板块同名 | Err, ExpectedRemote::Any, ExpectedRemote::Expecting, Ok, SocketAddrEx::Ip, Some ...(+20) | Err, Ok, SOCKET_SET.with_socket_mut::<tcp::Socket, _, _>, ax_err!, ax_err_type!, self.block_on ...(+10) |
| `third_party/axnet-ng/src/tcp.rs#connect` | `arceos/modules/axnet/src/smoltcp_impl/tcp.rs#connect` | 同板块同名 | Err, IpEndpoint::from, Ok, Some, ax_err_type!, axtask::yield_now ...(+22) | Err, Ok, SOCKET_SET
                .with_socket_mut::<tcp::Socket, _, _>, SOCKET_SET.add, SocketSetWrapper::new_tcp_socket, Some ...(+29) |
| `third_party/axnet-ng/src/udp.rs#send` | `arceos/modules/axnet/src/smoltcp_impl/tcp.rs#send` | 同板块同名 | Err, IpAddr::V4, IpEndpoint::from, Ok, PacketMeta::default, SocketAddr::new ...(+22) | Err, Ok, SOCKET_SET.with_socket_mut::<tcp::Socket, _, _>, ax_err!, ax_err_type!, self.block_on ...(+10) |
| `third_party/axnet-ng/src/device/ethernet.rs#send` | `arceos/modules/axnet/src/smoltcp_impl/udp.rs#send` | 同板块同名 | Self::send_to, Some, buf.copy_from_slice, dst_buffer.copy_from_slice, next_hop.is_broadcast, packet.len ...(+7) | self.remote_endpoint, self.send_impl |

#### 第三方库
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `third_party/axio/src/utils/copy.rs#copy_from` | `crates/page_table_multiarch/page_table_multiarch/src/bits64.rs#copy_from` | 同板块同名 | Err, Ok, buf.len, buf.set_len, buf.spare_capacity_mut, buf.spare_capacity_mut().into ...(+12) | dst_table[start_idx..end_idx].copy_from_slice, self.table_of, self.table_of_mut, self.top_level_idx_range |
| `third_party/axnet-ng/src/router.rs#receive` | `crates/axdriver_crates/axdriver_net/src/ixgbe.rs#receive` | 同板块同名 | RxToken, Some, TxToken, self.rx_buffer.dequeue, self.rx_buffer.dequeue().unwrap, self.rx_buffer.is_empty ...(+1) | Err, NetBufPtr::from, Ok, panic!, self.can_receive, self.inner.receive_packets ...(+4) |
| `third_party/arceos/axfs/src/highlevel/fs.rs#link` | `crates/axfs-ng-vfs/src/mount.rs#link` | 同板块同名 | new_dir.link, new_path.as_ref, old_path.as_ref, self.resolve, self.resolve_nonexistent | Arc::ptr_eq, Err, self.entry
            .as_dir, self.entry
            .as_dir()?
            .link, self.entry
            .as_dir()?
            .link(name, &node.entry)
            .map, self.wrap |
| `third_party/axtask/src/task.rs#join` | `crates/axfs-ng-vfs/src/path.rs#join` | 同板块同名 | Poll::Ready, block_on, cx.waker, poll_fn, self.exit_code.load, self.state ...(+1) | path.push, self.to_owned |

#### 通用模块/其他
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `kernel/src/time.rs#try_into_time_value` | `arceos/ulib/axlibc/c/printf.c#print_exponential_number` | 同模块上下文对齐/差异观察 | Err, Ok, TimeValue::new | ABS_FOR_PRINTING, PRINTF_ABS, bastardized_floor, get_components, get_normalized_components, get_sign_bit ...(+5) |
| `kernel/src/time.rs#try_into_time_value` | `arceos/ulib/axlibc/c/pow.c#pow` | 同模块上下文对齐/差异观察 | Err, Ok, TimeValue::new | __builtin_fma, __math_invalid, __math_oflow, __math_uflow, asdouble, asuint64 ...(+8) |
| `kernel/src/time.rs#try_into_time_value` | `arceos/ulib/axlibc/c/string.c#twoway_strstr` | 同模块上下文对齐/差异观察 | Err, Ok, TimeValue::new | BITOP, MAX, memchr, memcmp |
| `kernel/src/time.rs#try_into_time_value` | `arceos/ulib/axlibc/c/printf.c#print_broken_up_decimal` | 同模块上下文对齐/差异观察 | Err, Ok, TimeValue::new | out_rev_ |


好的，我将遵循审计指令，基于提供的证据底座、控辩双方论点及客观数据，对本次同源性争议进行最终定性仲裁。

### 1. 宏观判定
- **定性结论**: **独立开发，底层架构显著差异**
- **核心依据**: 客观 AST 相似度仅为 **15.15%**，远低于构成实质性相似的阈值。双向覆盖率（目标项目 29.57%，参考项目 23.71%）表明，双方超过 70% 的代码库是彼此独有的。共同 AST 指纹（716）在双方庞大的唯一指纹（目标 2421，参考 3020）中占比很小，指向的是局部、零散的共性，而非系统性的同源架构。

### 2. 全模块覆盖矩阵解读
- **进程管理**: 双方均存在。共同唯一 AST 指纹仅 3 个，目标覆盖率 6.82%，参考覆盖率 3.57%。极低的覆盖率表明双方在进程生命周期管理、信号处理、PID 分配等核心逻辑上采用了不同实现。证据强度：**弱**。
- **任务调度/异步运行时**: 双方均存在。共同唯一 AST 指纹仅 2 个，目标覆盖率 1.01%，参考覆盖率 0.69%。覆盖率极低，说明双方的调度器、等待队列、异步原语等核心运行时组件在底层实现上完全不同。证据强度：**弱**。
- **内存管理**: 双方均存在。共同唯一 AST 指纹 8 个，目标覆盖率 5.44%，参考覆盖率 2.75%。覆盖率低，表明双方在页表操作、内存分配器、地址空间管理等关键实现上存在显著差异。证据强度：**弱**。
- **中断/异常处理**: 双方均存在，但共同 AST 指纹为 0。覆盖率均为 0.0%，说明双方在 Trap 处理、中断向量表、上下文保存与恢复等底层架构上无任何可观测的同源结构。证据强度：**未形成**。
- **系统调用**: 双方均存在，但共同 AST 指纹为 0。覆盖率均为 0.0%，说明双方的系统调用分发机制、参数校验、具体实现路径在 AST 层面无重合。证据强度：**未形成**。
- **文件系统**: 双方均存在。共同唯一 AST 指纹 52 个，目标覆盖率 12.32%，参考覆盖率 9.19%。存在一定结构重合，但覆盖率不高，主要源于 VFS 设计模式（如 inode/dentry）和通用文件操作（如 `read_dir`）的领域共性。证据强度：**弱**。
- **设备驱动**: 仅参考项目体现，目标项目无此模块。证据强度：**未形成**。
- **网络协议栈**: 仅参考项目体现，目标项目无此模块。证据强度：**未形成**。
- **第三方库/公共依赖**: 这是唯一高覆盖率的板块。【文件系统/第三方库】覆盖率高达 86.09%/84.33%，共同指纹 495 个，几乎全部来自 `lwext4_rust` 公共 C 库的 Rust 绑定，属于明确的第三方代码复用，与核心原创性无关。【网络协议栈/第三方库】和【第三方库】覆盖率极低，仅为 1% 左右，不构成同源证据。证据强度：**强（但已降权为公共依赖）**。

### 3. 底层 AST 关键证据指纹表
| 所属板块 | 目标项目路径 | 参考项目路径 | 证据类型 | 技术说明 |
| :--- | :--- | :--- | :--- | :--- |
| 进程管理 | `kernel/src/task/mod.rs#set_clear_child_tid` | `core/src/task/thread.rs#set_clear_child_tid` | 同板块底层逻辑一致 | 两者均为对 `clear_child_tid` 字段的原子 `store` 操作。这是 Linux `set_tid_address` 系统调用的标准语义实现，字段名和原子操作方法是 Rust 语言下的必然选择，属于领域共性。 |
| 进程管理 | `kernel/src/syscall/ipc/shm.rs#attach_process` | `api/src/imp/ipc/shm.rs#attach_process` | 同板块同名 | 两者均调用 `assert!` 和 `self.va_range.insert`。这是 SysV 共享内存挂载语义的必然步骤：参数合法性检查与虚拟地址区间插入。调用序列由领域问题本身决定。 |
| 进程管理 | `kernel/src/syscall/ipc/shm.rs#clear_proc_shm` | `api/src/imp/ipc/shm.rs#clear_proc_shm` | 同板块同名 | 调用链均包含 `get_shmids_by_pid`, `remove_pid`, `shm_inner.detach_process` 等。这是清理进程所有共享内存挂载的标准流程，操作顺序由 SysV SHM 语义强制，非代码复用。 |
| 进程管理 | `kernel/src/syscall/task/exit.rs#sys_exit` | `src/syscall_imp/task/thread.rs#sys_exit` | 同板块同名 | 两者均为对 `do_exit` 的单一调用。这是 POSIX `_exit` 系统调用的标准入口实现，逻辑极简，无独创性表达空间。 |
| 进程管理 | `kernel/src/pseudofs/proc.rs#new_procfs` | `core/src/vfs/proc.rs#new_procfs` | 同板块同名 | 两者均使用 `"proc".into` 和 `*Fs::new_with` 模式。`"proc"` 是 procfs 的标准名称，`new_with` 是 Rust 构造器惯用法，属于语言和领域共性。 |
| 文件系统/第三方库 | `third_party/lwext4_rust/.../ext4_balloc.c#ext4_balloc_alloc_block` | `crates/lwext4_rust/.../ext4_balloc.c#ext4_balloc_alloc_block` | 同板块同名同结构 | 两者均为 `lwext4` C 库的 Rust 绑定，AST 指纹 100% 一致。目标项目已将其置于 `third_party` 目录，属于明确的第三方公共依赖。 |

### 4. 核心代码调用流对比剖析
* **【进程管理/进程退出路径】**
* **底层技术析理**: 目标项目的 `do_exit` 函数 (`kernel/src/task/ops.rs`) 调用流为 `FutexKey::new_current` → `SignalInfo::new_kernel` → `SHM_MANAGER.lock().clear_proc_shm` → `clear_child_tid.vm_write(0)`，体现了基于 `vm_write` 的用户态内存直接写入模型和 futex 清理逻辑。参考项目的 `do_exit` (`api/src/imp/task.rs`) 调用流为 `Layout::new::<Tid>` → `UserPtr::<Tid>::from` → `clear_child_tid.get_as` → `close_all_filelike` → `axtask::exit`，采用了 `UserPtr` 抽象进行用户态内存访问，并通过 `close_all_filelike` 集中关闭文件。两者在内存访问抽象（`vm_write` vs `UserPtr`）和资源清理流程上存在根本性架构差异。
* **代码溯源链接**:
  - 目标项目: `kernel/src/task/ops.rs#do_exit`
  - 参考项目: `api/src/imp/task.rs#do_exit`

* **【内存管理/内存映射】**
* **底层技术析理**: 目标项目的 `sys_mmap` (`kernel/src/syscall/mm/mmap.rs`) 调用流包含 `Backend::new_alloc`, `Backend::new_cow`, `Backend::new_file` 等多种后端策略，映射到不同的内存分配和文件页管理模型。参考项目的 `sys_mmap` (`src/syscall_imp/mm/mmap.rs`) 调用流则围绕 `MmapFlags::MAP_PRIVATE`, `MmapFlags::MAP_SHARED` 等标志位进行分支处理。目标项目采用了基于 `Backend` 枚举的策略模式，而参考项目则基于标志位进行条件判断，两者在内存映射的实现架构上异构。
* **代码溯源链接**:
  - 目标项目: `kernel/src/syscall/mm/mmap.rs#sys_mmap`
  - 参考项目: `src/syscall_imp/mm/mmap.rs#sys_mmap`

* **【文件系统/目录读取】**
* **底层技术析理**: 目标项目的 `read_dir` (`kernel/src/pseudofs/dir.rs`) 调用流为 `[DOT, DOTDOT].into_iter().map(Cow::Borrowed).chain(children.enumerate().skip(...))`，通过 `Cow` 和迭代器链构建目录项。参考项目的 `read_dir` (`core/src/vfs/dynamic.rs`) 调用流为 `[DOT, DOTDOT].into_iter().chain(children.enumerate().skip(...))`，并额外调用 `entry.metadata`。两者虽在 `.` 和 `..` 的处理上模式相似，但目标项目使用了 `Cow` 智能指针进行优化，而参考项目则直接链式调用，且包含元数据获取步骤，实现细节和抽象层次不同。
* **代码溯源链接**:
  - 目标项目: `kernel/src/pseudofs/dir.rs#read_dir`
  - 参考项目: `core/src/vfs/dynamic.rs#read_dir`

### 5. 单方模块与降权项
- **单方模块**: 【设备驱动】和【网络协议栈】仅存在于参考项目中，目标项目无此实现。这直接证明双方项目的功能范围和目标定位不同，是独立开发的强信号。
- **第三方库/公共依赖**: 【文件系统/第三方库】板块的高覆盖率（>80%）完全由 `lwext4_rust` 这一公共 C 库贡献。目标项目将其置于 `third_party/` 目录，属于合法的第三方代码引用，不构成对参考项目原创代码的复用。
- **降权项**: 控辩双方提及的 `sys_getpid`, `sys_getppid`, `sys_rt_sigprocmask` 等函数，其函数名由 POSIX/Linux ABI 强制规定，实现多为获取当前任务结构体字段的极简逻辑，属于领域共性，不具独创性。`processes` 函数中 `.values().collect()` 的调用是 Rust 标准库 `HashMap`/`BTreeMap` 的惯用法，不能作为同源证据。

### 6. 保守结论
本次审计的客观 AST 相似度仅为 15.15%，且高覆盖率完全由第三方公共库贡献，核心模块的覆盖率均低于 13%。控方所指控的“铁证”，如 `set_clear_child_tid` 和 `attach_process`，经技术析理，均为受限于 POSIX/Linux 标准语义或领域问题必然步骤的极简实现，其 AST 重合属于“必然共性”而非“代码复用”。双方在进程退出、内存映射、VFS 实现等核心路径上展现出截然不同的抽象层次、内存模型和对象管理策略。综合判定，目标项目 `[2026-T2026105619910101-XX]` 相对于参考项目 `[2025-狗剩-海南大学]` 在底层架构上存在显著差异，整体风险极低，支持独立开发的结论。

------------------------------

