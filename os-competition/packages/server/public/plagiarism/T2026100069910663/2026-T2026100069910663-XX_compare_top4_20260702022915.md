# 🛡️ OS 内核赛自动化代码级架构审计报告

**审计时间**: `2026-07-02 02:29:15`
**参赛作品名称**: `2026-T2026100069910663-XX`
**年份**: `2026`
**学校**: `XX`
**队伍名称**: `T2026100069910663`
**比赛名字**: `OS-Competition`
**比赛子赛道**: `Kernel-Implementation`
**仓库地址**: `https://gitlab.eduxiji.net/educg-group-43501-3132633/T2026100069910663-3263`
**推测开发语言**: `Rust / C (由AST探针自动识别)`
**特征函数量**: `2566`

## ⚔️ 底层代码同源性对比 (AST Homology)
> 本部分由专门的代码审查智能体，对该项目与符合年份条件的历届最高相似度项目进行控制流与底层调用对比。新版报告会先给出确定性的全模块覆盖矩阵，再给出 Agent 的分模块解释，避免只围绕少量抽样函数展开。

### 🏆 Top 4 底层对标: vs `2025-rustflyer-哈尔滨工业大学深圳`（哈尔滨工业大学（深圳） / rustflyer）

#### 对方作品信息

**对方年份**: `2025`
**对方学校**: `哈尔滨工业大学（深圳）`
**对方队伍名称**: `rustflyer`
**对方比赛名字**: `OS-Competition`
**对方比赛子赛道**: `Kernel-Implementation`
**对方仓库地址**: `https://gitlab.eduxiji.net/educg-group-36002-2710490/T202518123995755-2974`

> 双向覆盖率：共同 AST 指纹: 24；唯一 AST 指纹: 目标项目 2244，参考项目 2184；覆盖率: 目标项目 1.07%，参考项目 1.1%

## 全模块对比总览（确定性证据）

> 总体覆盖率：共同 AST 指纹: 24；唯一 AST 指纹: 目标项目 2244，参考项目 2184；覆盖率: 目标项目 1.07%，参考项目 1.1%

### 1. 模块覆盖矩阵
| 模块 | 目标函数数 | 参考函数数 | 共同唯一 AST 指纹 | Target 覆盖 | Base 覆盖 | 状态 |
| :--- | ---: | ---: | ---: | ---: | ---: | :--- |
| 进程管理 | 516 | 336 | 3 | 0.65% | 1.35% | 双方存在且有结构重合 |
| 任务调度/异步运行时 | 158 | 428 | 1 | 0.67% | 0.26% | 双方存在且有结构重合 |
| 内存管理 | 592 | 436 | 1 | 0.19% | 0.25% | 双方存在且有结构重合 |
| 中断/异常处理 | 105 | 43 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |
| 系统调用 | 63 | 36 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |
| 文件系统 | 855 | 1065 | 2 | 0.26% | 0.29% | 双方存在且有结构重合 |
| 设备驱动 | 80 | 217 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |
| 网络协议栈 | 18 | 88 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |
| 文件系统/第三方库 | 34 | 89 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |
| 通用模块/其他 | 145 | 224 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |

### 2. 分模块证据清单

#### 进程管理
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `kernel/src/syscall/process/signal/sys.rs#sys_rt_sigtimedwait` | `kernel/src/syscall/signal.rs#sys_rt_sigtimedwait` | 同板块同名 | USER_PROCESSES.lock, err, read_optional_timeout, read_sigset_from_task, signal_bit, table.current_index ...(+5) | Err, Ok, Some, UserReadPtr::<SigSet>::new, UserReadPtr::<TimeSpec>::new, UserWritePtr::<SigInfo>::new ...(+22) |
| `kernel/src/syscall/process/resource.rs#sys_prlimit64` | `kernel/src/syscall/process.rs#sys_prlimit64` | 同板块同名 | USER_PROCESSES.lock, err, read_rlimit_from_task, table.current_index, table.index_of, validate_resource_limit ...(+2) | Ok, RLimit::one, Resource::from_repr, Resource::from_repr(resource).ok_or, TASK_MANAGER.get_task, TASK_MANAGER.get_task(pid).ok_or ...(+14) |
| `kernel/src/syscall/process/sys/info.rs#sys_capset` | `kernel/src/syscall/process.rs#sys_capset` | 同板块同名 | Err, Ok, cap_data_slots_or_write_preferred, cap_pid_matches_current, err, read_cap_data ...(+3) | Err, Ok, UserReadPtr::<CapUserData>::new, UserReadPtr::<CapUserHeader>::new, current_task, data_ptr.try_into_slice ...(+6) |
| `kernel/src/syscall/process/sys/info.rs#sys_capget` | `kernel/src/syscall/process.rs#sys_capget` | 同板块同名 | Err, Ok, cap_data_slots_or_write_preferred, cap_pid_matches_current, err, read_cap_header ...(+3) | Err, Ok, UserReadPtr::<CapUserHeader>::new, UserWritePtr::<CapUserData>::new, UserWritePtr::<CapUserHeader>::new, current_task ...(+7) |

#### 任务调度/异步运行时
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `kernel/src/syscall/select.rs#sys_pselect6` | `kernel/src/syscall/fs.rs#sys_pselect6` | 同板块同名 | Err, block_pselect, current.take_any_signal_wake, current.take_wake_reason, current.task, err ...(+10) | Err, IntrBySignalFuture::new, Ok, PSelectFuture::new, PollEvents::empty, Select2Futures::new ...(+46) |
| `kernel/src/syscall/select.rs#sys_ppoll` | `kernel/src/syscall/fs.rs#sys_ppoll` | 同板块同名 | Err, block_ppoll, core::mem::size_of::<u64>, current.take_any_signal_wake, current.take_wake_reason, current.task ...(+10) | Duration::from_micros, Err, Ok, PollEvents::from_bits, PollEvents::from_bits(poll_fd.events).unwrap, Select2Futures::new ...(+25) |
| `kernel/src/syscall/fs/path.rs#sys_utimensat` | `kernel/src/syscall/fs.rs#sys_utimensat` | 同板块同名 | Err, Ok, Some, dir_base, err, file.as_inode ...(+9) | AtFd::from, Err, Ok, OpenFlags::from_bits, OpenFlags::from_bits(flags).ok_or, TimeSpec::from ...(+20) |
| `kernel/src/syscall/fs/metadata.rs#sys_statx` | `kernel/src/syscall/fs.rs#sys_statx` | 同板块同名 | Err, Ok, dir_base, err, metadata_for_open_file, path.is_empty ...(+7) | AtFd::from, Err, Ok, Path::resolve_symlink_through, StatxFlags::from_bits_truncate, StatxTimestamp::default ...(+25) |

#### 内存管理
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `kernel/src/syscall/mm.rs#sys_mmap` | `kernel/src/syscall/mm.rs#sys_mmap` | 同板块同名 | Err, Ok, Vec::new, VirtAddr, bytes.resize, bytes.truncate ...(+36) | Err, MappingFlags::from, MmapFlags::from_bits_truncate, MmapProt::from_bits_truncate, Some, VirtAddr::new ...(+13) |
| `tools/user-smoke.rs#_start` | `kernel/src/entry/riscv64.rs#_start` | 同板块同名 | ((grown_brk - 1) as *mut u8).write_volatile, dup_message.as_ptr, dup_message.len, exercise_clock_nanosleep_syscall, exercise_clone_stack_syscall, exercise_clone_wait_syscalls ...(+46) | naked_asm! |
| `kernel/src/mm/memory_set/mmap.rs#unmap_range` | `kernel/src/vm/page_table.rs#unmap_range` | 同板块同名 | Err, Ok, VirtAddr, VirtAddr(end)
            .checked_ceil, VirtAddr(end)
            .checked_ceil()
            .ok_or, area.kind.is_mmap_area ...(+11) | VirtPageNum::new, fence, self.unmap_page, start_vpn.address, start_vpn.address().to_usize, start_vpn.to_usize ...(+1) |
| `kernel/src/syscall/socket.rs#sys_socketpair` | `kernel/src/syscall/net.rs#sys_socketpair` | 同板块同名 | (read_fd as i32).to_le_bytes, (write_fd as i32).to_le_bytes, Err, Ok, UserBuffer::new, err ...(+12) | Ok, OpenFlags::empty, UserWritePtr::<[u32; 2]>::new, current_task, new_pipe, sv.write ...(+3) |

#### 中断/异常处理
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `kernel/src/arch/loongarch64/trap.rs#init_trap_vector` | `lib/net/src/tcp/tcpstate.rs#block_on` | 同模块上下文对齐/差异观察 | core::arch::asm!, debug_assert_eq!, init_address_translation_control, trap_entry_addr, trap_stack_top_addr | Err, Ok, SOCKET_SET.check_poll, SOCKET_SET.poll_interfaces, f, has_signal ...(+4) |
| `kernel/src/arch/riscv64/trap.rs#init_trap_vector` | `lib/osfs/src/dev/tty/ioctl.rs#new` | 同模块上下文对齐/差异观察 | core::arch::asm!, core::ptr::addr_of! | 内部闭环逻辑 |
| `kernel/src/arch/loongarch64/trap.rs#trigger_kernel_syscall_probe` | `lib/net/src/tcp/tcpstate.rs#block_on_async` | 同模块上下文对齐/差异观察 | core::arch::asm! | Err, Ok, SOCKET_SET.check_poll, SOCKET_SET.poll_interfaces, f, has_signal ...(+3) |
| `kernel/src/main.rs#rust_main` | `lib/net/src/udp.rs#block_on` | 同模块上下文对齐/差异观察 | arch::init_trap_vector, arch::shutdown, drivers::init, fs::mount::init_root_from_first_block_device, logging::banner, logging::shutdown_requested ...(+6) | Err, Ok, SOCKET_SET.check_poll, SOCKET_SET.poll_interfaces, f, has_signal ...(+3) |

#### 系统调用
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `tools/user-smoke-loongarch64.rs#exit` | `user/src/lib.rs#exit` | 同板块同名 | core::hint::spin_loop, syscall1 | panic!, sys_exit |
| `tools/user-smoke-loongarch64.rs#exercise_libcbench_arena_reuse_after_old_mapping` | `user/src/lib.rs#execve` | 同模块上下文对齐/差异观察 | (ARENA as *mut u64).write_volatile, (OLD as *mut u64).write_volatile, exit, message.as_ptr, message.len, syscall2 ...(+2) | CString::new, CString::new(*s).unwrap, CString::new(path).unwrap, argv.as_ptr, argv.iter, argv.iter().map ...(+12) |
| `tools/user-smoke.rs#exercise_mount_syscalls` | `kernel/src/syscall/misc.rs#default` | 同模块上下文对齐/差异观察 | b"/dev/missing\0".as_ptr, b"/dev/vda2\0".as_ptr, b"/dev\0".as_ptr, b"missing_mnt\0".as_ptr, b"vfat\0".as_ptr, exit ...(+5) | Self::from_str |
| `tools/user-smoke.rs#exercise_utimensat_syscall` | `kernel/src/syscall/key.rs#add_key` | 同模块上下文对齐/差异观察 | b"/tmp/utime_missing.txt\0".as_ptr, exit, is_error, path.as_ptr, syscall1, syscall4 ...(+1) | description.into, payload.as_ref, payload.as_ref().to_vec, self.keys.insert |

#### 文件系统
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `kernel/src/fs/ramfs/io.rs#rename` | `lib/vfs/src/dentry.rs#rename` | 同板块同名 | Err, Ok, String::from, child_prefix, child_prefix(old)?.as_str, new.starts_with ...(+9) | <dyn File>::open, <dyn File>::open(Arc::clone(new_dentry))?.load_dir, Arc::clone, Arc::ptr_eq, Err, FanEventMask::empty ...(+26) |
| `kernel/src/fs/vfs/file.rs#ioctl` | `lib/osfs/src/dev/loopx/file.rs#ioctl` | 同板块同名 | Err, Ok, flags.to_ne_bytes, ops.ioctl, out.len, out[..FS_IOC_FLAGS_LEN].copy_from_slice ...(+7) | BlkIoctlCmd::from_repr, Err, LoopInfo64::default, LoopIoctlCmd::from_repr, Ok, Some ...(+18) |
| `kernel/src/fs/devfs/mod.rs#metadata` | `lib/vfs/src/fanotify/mod.rs#metadata` | 同板块同名 | NodeId::devfs, matches!, node.inode, rdev_for_node | panic! |
| `kernel/src/fs/socket/mod.rs#remove` | `lib/osfs/src/fd_table.rs#remove` | 同板块同名 | entry.as_ref, entry.as_ref().is_some_and, pending_ids[..pending_count].iter, pending_ids[..pending_count].iter().copied, self
            .entries
            .iter, self
            .entries
            .iter()
            .position ...(+4) | Err, Ok, crate::special::inotify::vfs_delete_notify, fdinfo.fanotify_close, fdinfo.file.clone, log::debug! ...(+3) |

#### 设备驱动
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `kernel/src/drivers/block/mod.rs#devices` | `kernel/src/osdriver/manager.rs#devices` | 同板块同名 | out.iter_mut, out.len, self.state.lock | 内部闭环逻辑 |
| `kernel/src/net/loopback.rs#capabilities` | `lib/driver/src/net/virtnet.rs#capabilities` | 同板块同名 | ChecksumCapabilities::ignored, DeviceCapabilities::default | DeviceCapabilities::default |
| `kernel/src/drivers/block/mod.rs#empty` | `lib/driver/src/block/dw_mshc/registers.rs#empty` | 同板块同名 | 内部闭环逻辑 | CMDARG::new |
| `kernel/src/drivers/bus/pci.rs#align_up` | `kernel/src/osdriver/probe.rs#align_up` | 同板块同名 | 内部闭环逻辑 | 内部闭环逻辑 |

#### 网络协议栈
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `kernel/src/net/loopback.rs#consume` | `lib/net/src/rttoken.rs#consume` | 同板块同名 | f, self.queue.push_back, vec! | crate::tcp::snoop_tcp_packet, crate::tcp::snoop_tcp_packet(self.1.packet(), is_ethernet).ok, f, rx_buf.packet_mut, self.0.borrow, self.0.borrow().capabilities ...(+4) |
| `kernel/src/net/mod.rs#add_udp_socket` | `lib/net/src/tcp/tcpsr.rs#send` | 同模块上下文对齐/差异观察 | self.sockets.add, udp::PacketBuffer::new, udp::Socket::new, vec! | Err, Ok, SOCKET_SET.poll_interfaces, SOCKET_SET.with_socket_mut::<tcp::Socket, _, _>, log::error!, log::info! ...(+17) |
| `kernel/src/net/mod.rs#udp_send` | `lib/net/src/udp.rs#send_to` | 同模块上下文对齐/差异观察 | IpEndpoint::from, bytes.len, self.sockets
            .get_mut::<udp::Socket>, self.sockets
            .get_mut::<udp::Socket>(handle)
            .send_slice, self.sockets
            .get_mut::<udp::Socket>(handle)
            .send_slice(bytes, endpoint)
            .expect | Err, Ok, SOCKET_SET.with_socket_mut::<udp::Socket, _, _>, buf.len, log::debug!, log::info! ...(+12) |
| `kernel/src/net/mod.rs#tcp_listen` | `lib/net/src/tcp/tcpstate.rs#shutdown` | 同模块上下文对齐/差异观察 | Some, self.sockets
            .get_mut::<tcp::Socket>, self.sockets
            .get_mut::<tcp::Socket>(handle)
            .listen, self.sockets
            .get_mut::<tcp::Socket>(handle)
            .listen(endpoint)
            .expect | Err, LISTEN_TABLE.unlisten, Ok, SOCKET_SET.check_poll, SOCKET_SET.poll_interfaces, SOCKET_SET.with_socket_mut::<tcp::Socket, _, _> ...(+12) |

#### 文件系统/第三方库
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `kernel/src/fs/ext4/mod.rs#seek` | `lib/ext4/src/disk.rs#seek` | 同板块同名 | Err, Ok, base.checked_add, base.checked_add(offset as i128).ok_or, self.stat | Ok, Some, dev.pos, dev.pos().checked_add_signed, dev.pos().checked_add_signed(off as isize).map, dev.set_pos ...(+6) |
| `kernel/src/fs/ext4/mod.rs#name` | `lib/ext4/src/ext/dir.rs#name` | 同板块同名 | 内部闭环逻辑 | String::from_utf8, String::from_utf8(name_bytes).map_err, self.0.name[..self.0.name_length as usize].to_vec |
| `kernel/src/fs/ext4/mod.rs#map_extent_node` | `lib/ext4/src/dentry.rs#base_rename` | 同模块上下文对齐/差异观察 | Err, MappedBlock::Data, Ok, STATE.lock, Some, chosen.ok_or ...(+7) | CString::new, CString::new(dentry.path()).unwrap, CString::new(new_dentry.path()).unwrap, Err, ExtDir::open, ExtDir::open(&new_path)?.next ...(+18) |
| `kernel/src/fs/ext4/mod.rs#read_inode` | `lib/ext4/src/ext/file.rs#open_by_ino` | 同模块上下文对齐/差异观察 | Err, Ok, STATE.lock, block::read_bytes, block::read_bytes(gd_offset, &mut gd[..state.desc_size as usize]).map_err, block::read_bytes(inode_offset, &mut raw[..read_len]).map_err ...(+13) | CString::new, CString::new(mount_point).unwrap, Err, ExtDir::open, ExtDir::open(mp_path.as_c_str()).unwrap, MaybeUninit::uninit ...(+14) |

#### 通用模块/其他
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `kernel/src/sync/spin.rs#lock` | `lib/mutex/src/mutex/spin_mutex.rs#lock` | 同板块同名 | self
            .locked
            .compare_exchange, self
            .locked
            .compare_exchange(false, true, Ordering::Acquire, Ordering::Relaxed)
            .is_err, self.locked.load, spin_loop | S::before_lock, self
                .lock
                .compare_exchange, self
                .lock
                .compare_exchange(false, true, Ordering::SeqCst, Ordering::Relaxed)
                .is_ok, self.wait_unlock |
| `kernel/src/sync/spin.rs#deref_mut` | `lib/mutex/src/mutex/sleep_mutex.rs#deref_mut` | 同板块同名 | self.lock.value.get | self.mutex.data.get |
| `kernel/src/sync/spin.rs#deref` | `lib/mutex/src/mutex/sleep_mutex.rs#deref` | 同板块同名 | self.lock.value.get | self.mutex.data.get |
| `tools/user-smoke.rs#contains` | `kernel/src/vm/vm_area.rs#contains` | 同板块同名 | haystack
        .windows, haystack
        .windows(needle.len())
        .any, needle.is_empty, needle.len | 内部闭环逻辑 |


#### 1. 宏观判定
- **定性结论**: 底层架构显著差异 / 独立开发 (相似度: 0.54%，共同 AST 指纹: 24；唯一 AST 指纹: 目标项目 2244，参考项目 2184；覆盖率: 目标项目 1.07%，参考项目 1.1%)

#### 2. 全模块覆盖矩阵解读
- **进程管理**: 双方均存在该模块。存在 3 个共同 AST 指纹，目标覆盖率 0.65%，参考覆盖率 1.35%。覆盖率极低，共同指纹源于 Linux 标准系统调用名及 Rust 通用惯用法。证据强度：**弱**。
- **任务调度/异步运行时**: 双方均存在该模块。存在 1 个共同 AST 指纹，目标覆盖率 0.67%，参考覆盖率 0.26%。覆盖率极低，共同指纹为通用宏或标准库调用。证据强度：**弱**。
- **内存管理**: 双方均存在该模块。存在 1 个共同 AST 指纹，目标覆盖率 0.19%，参考覆盖率 0.25%。覆盖率极低，共同指纹为通用错误处理或标准库调用。证据强度：**弱**。
- **中断/异常处理**: 双方均存在该模块。共同 AST 指纹为 0，覆盖率均为 0.0%。未观察到任何共同结构指纹。证据强度：**未形成**。
- **系统调用**: 双方均存在该模块。共同 AST 指纹为 0，覆盖率均为 0.0%。未观察到任何共同结构指纹。证据强度：**未形成**。
- **文件系统**: 双方均存在该模块。存在 2 个共同 AST 指纹，目标覆盖率 0.26%，参考覆盖率 0.29%。覆盖率极低，共同指纹为通用标准库调用。证据强度：**弱**。
- **设备驱动**: 双方均存在该模块。共同 AST 指纹为 0，覆盖率均为 0.0%。未观察到任何共同结构指纹。证据强度：**未形成**。
- **网络协议栈**: 双方均存在该模块。共同 AST 指纹为 0，覆盖率均为 0.0%。未观察到任何共同结构指纹。证据强度：**未形成**。
- **文件系统/第三方库**: 双方均存在该模块。共同 AST 指纹为 0，覆盖率均为 0.0%。未观察到任何共同结构指纹。证据强度：**未形成**。
- **通用模块/其他**: 双方均存在该模块。共同 AST 指纹为 0，覆盖率均为 0.0%。未观察到任何共同结构指纹。证据强度：**未形成**。

#### 3. 底层 AST 关键证据指纹表
| 所属板块 | 目标项目路径 | 参考项目路径 | 证据类型 | 技术说明 |
| :--- | :--- | :--- | :--- | :--- |
| 进程管理 | `kernel/src/syscall/process/sys/identity.rs#sys_getpid` | `kernel/src/syscall/process.rs#sys_getpid` | 同板块同名 | 均为 Linux 标准系统调用。目标项目通过 `with_current_user` 和 `process_view()` 获取，参考项目通过 `current_task()` 获取，底层调用链完全不同。 |
| 进程管理 | `kernel/src/syscall/process/sys/exit.rs#sys_exit` | `kernel/src/syscall/process.rs#sys_exit` | 同板块同名 | 均为 Linux 标准系统调用。目标项目调用 `terminate_current_user`，参考项目内联操作 `set_exit_code` 和 `set_state`，状态管理路径不同。 |
| 进程管理 | `kernel/src/task/process.rs#add_child` | `kernel/src/task/task.rs#add_child` | 同板块同名 | 目标项目使用 `self.children.push` (Vec)，参考项目使用 `self.children.lock().insert` (带锁集合)，数据结构和同步策略完全不同。 |
| 任务调度/异步运行时 | `kernel/src/syscall/select.rs#sys_pselect6` | `kernel/src/syscall/fs.rs#sys_pselect6` | 同板块同名 | 均为 Linux 标准系统调用。目标项目调用 `block_pselect`，参考项目使用 `PSelectFuture::new` 等异步组合子，实现范式不同。 |
| 内存管理 | `kernel/src/syscall/mm.rs#sys_mmap` | `kernel/src/syscall/mm.rs#sys_mmap` | 同板块同名 | 均为 Linux 标准系统调用。目标项目直接操作 `Vec<u8>` 和 `VirtAddr`，参考项目使用 `MmapFlags`、`MmapProt` 等类型化参数，抽象层次不同。 |
| 文件系统 | `kernel/src/fs/ramfs/io.rs#rename` | `lib/vfs/src/dentry.rs#rename` | 同板块同名 | 目标项目基于字符串前缀检查实现，参考项目基于 `Arc` 指针比较和 `FanEventMask` 事件通知，实现机制完全不同。 |
| 文件系统 | `kernel/src/fs/vfs/file.rs#ioctl` | `lib/osfs/src/dev/loopx/file.rs#ioctl` | 同板块同名 | 目标项目处理通用文件系统标志，参考项目处理 `LoopIoctlCmd` 等特定设备命令，功能域不同。 |
| 设备驱动 | `kernel/src/drivers/block/mod.rs#devices` | `kernel/src/osdriver/manager.rs#devices` | 同板块同名 | 目标项目通过 `self.state.lock` 访问，参考项目为内部闭环逻辑，实现方式不同。 |
| 网络协议栈 | `kernel/src/net/loopback.rs#consume` | `lib/net/src/rttoken.rs#consume` | 同板块同名 | 目标项目操作 `self.queue.push_back`，参考项目调用 `snoop_tcp_packet` 等复杂网络处理逻辑，功能完全不同。 |
| 第三方库/公共依赖 | `kernel/src/fs/ext4/mod.rs#seek` | `lib/ext4/src/disk.rs#seek` | 同板块同名 | 目标项目基于 `base.checked_add` 实现，参考项目通过 `dev.pos()` 和 `checked_add_signed` 实现，底层设备抽象不同。 |

#### 4. 核心代码调用流对比剖析
* **【进程管理/进程标识获取】**
* **底层技术析理**: 目标项目采用“用户上下文视图”模型，通过 `with_current_user` 闭包获取 `process_view` 或 `thread_view` 对象，再从中读取 `tgid`、`tid` 等字段。参考项目采用“全局当前任务”模型，直接调用 `current_task()` 函数获取任务结构体，并访问其 `pid`、`tid` 字段。两者在任务上下文访问、权限隔离和对象封装上存在根本性架构差异。
* **代码溯源链接**:
  - 目标项目: `kernel/src/syscall/process/sys/identity.rs#sys_getpid`
  - 参考项目: `kernel/src/syscall/process.rs#sys_getpid`

* **【进程管理/进程退出】**
* **底层技术析理**: 目标项目将进程退出逻辑封装在 `terminate_current_user` 和 `terminate_current_user_group` 等高阶函数中，系统调用层仅负责错误传递。参考项目则在 `sys_exit` 函数内部直接操作任务状态，包括调用 `set_exit_code`、`set_state` 以及获取 `thread_group` 锁进行线程组遍历。两者在职责分离和抽象层次上设计不同。
* **代码溯源链接**:
  - 目标项目: `kernel/src/syscall/process/sys/exit.rs#sys_exit`
  - 参考项目: `kernel/src/syscall/process.rs#sys_exit`

* **【内存管理/内存映射】**
* **底层技术析理**: 目标项目的 `sys_mmap` 直接操作 `Vec<u8>` 字节数组和 `VirtAddr` 虚拟地址，通过 `resize`、`truncate` 等方法管理内存。参考项目则使用 `MmapFlags::from_bits_truncate`、`MmapProt::from_bits_truncate` 等类型化参数解析，并基于 `VirtAddr::new` 创建地址对象。两者在用户参数解析和内存抽象上采用不同策略。
* **代码溯源链接**:
  - 目标项目: `kernel/src/syscall/mm.rs#sys_mmap`
  - 参考项目: `kernel/src/syscall/mm.rs#sys_mmap`

#### 5. 单方模块与降权项
- **单方模块**: 目标项目独有的 `tools/user-smoke-loongarch64.rs` 等 LoongArch64 架构支持代码，在参考项目中无对应项。参考项目独有的 `lib/osfs`、`lib/net` 等独立库，在目标项目中无对应项。这些单方模块是独立开发的直接证据。
- **降权项**: 所有跨板块配对（如目标项目中断处理与参考项目网络协议栈的配对）均因违反“同板块”原则而降权。所有 `Ok`、`Err`、`Some`、`None`、`Vec::new`、`println!`、`format!`、`env!`、`matches!`、`asm!` 等 Rust 标准库或语言内建元素，均作为通用惯用法降权，不计入复用证据。

#### 6. 保守结论
本案客观 AST 相似度仅 0.54%，共同指纹 24 个，均为 Rust 语言通用惯用法或 Linux 标准系统调用名，处于噪声水平。全模块覆盖矩阵显示，所有模块的覆盖率均低于 1.5%，中断处理、系统调用、设备驱动、网络协议栈等核心模块未发现任何共同结构指纹。控方已自行撤回直接抄袭指控，其提出的 8 类“弱信号”均可被 Linux ABI 规范、独立架构设计（如 `USER_PROCESSES` vs `TASK_MANAGER`、`Vec` vs 带锁集合、LoongArch64 支持）充分解释。综合判定，两个项目在底层架构上存在显著差异，不存在代码复用或系统性洗稿的证据，应认定为独立开发。

------------------------------

