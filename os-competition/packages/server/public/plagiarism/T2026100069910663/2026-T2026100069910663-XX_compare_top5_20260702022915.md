# 🛡️ OS 内核赛自动化代码级架构审计报告

**审计时间**: `2026-07-02 02:29:15`
**参赛作品名称**: `2026-T2026100069910663-XX`
**年份**: `2026`
**学校**: `XX`
**队伍名称**: `T2026100069910663`
**比赛名字**: `OS-Competition`
**比赛子赛道**: `Kernel-Implementation`
**仓库地址**: `https://gitlab.eduxiji.net/educg-group-43501-3132633/T2026100069910663-3263`
**推测开发语言**: `Rust / C (由AST探针自动识别)`
**特征函数量**: `2566`

## ⚔️ 底层代码同源性对比 (AST Homology)
> 本部分由专门的代码审查智能体，对该项目与符合年份条件的历届最高相似度项目进行控制流与底层调用对比。新版报告会先给出确定性的全模块覆盖矩阵，再给出 Agent 的分模块解释，避免只围绕少量抽样函数展开。

### 🏆 Top 5 底层对标: vs `2024-Phoenix-哈尔滨工业大学深圳`（哈尔滨工业大学（深圳） / Phoenix）

#### 对方作品信息

**对方年份**: `2024`
**对方学校**: `哈尔滨工业大学（深圳）`
**对方队伍名称**: `Phoenix`
**对方比赛名字**: `OS-Competition`
**对方比赛子赛道**: `Kernel-Implementation`
**对方仓库地址**: `local-upload://T202418123993075-2940`

> 双向覆盖率：共同 AST 指纹: 19；唯一 AST 指纹: 目标项目 2244，参考项目 1389；覆盖率: 目标项目 0.85%，参考项目 1.37%

## 全模块对比总览（确定性证据）

> 总体覆盖率：共同 AST 指纹: 19；唯一 AST 指纹: 目标项目 2244，参考项目 1389；覆盖率: 目标项目 0.85%，参考项目 1.37%

### 1. 模块覆盖矩阵
| 模块 | 目标函数数 | 参考函数数 | 共同唯一 AST 指纹 | Target 覆盖 | Base 覆盖 | 状态 |
| :--- | ---: | ---: | ---: | ---: | ---: | :--- |
| 进程管理 | 516 | 138 | 2 | 0.43% | 1.87% | 双方存在且有结构重合 |
| 任务调度/异步运行时 | 158 | 190 | 1 | 0.67% | 0.54% | 双方存在且有结构重合 |
| 内存管理 | 592 | 369 | 6 | 1.12% | 1.75% | 双方存在且有结构重合 |
| 中断/异常处理 | 105 | 33 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |
| 系统调用 | 63 | 37 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |
| 文件系统 | 855 | 443 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |
| 设备驱动 | 80 | 179 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |
| 网络协议栈 | 18 | 98 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |
| 文件系统/第三方库 | 34 | 70 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |
| 第三方库 | 0 | 52 | 0 | 0.0% | 0.0% | 仅参考项目体现 |
| 通用模块/其他 | 145 | 115 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |

### 2. 分模块证据清单

#### 进程管理
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `kernel/src/syscall/process/sys/wait.rs#sys_wait4` | `kernel/src/syscall/process.rs#sys_wait4` | 同板块同名 | (status as i32).to_ne_bytes, USER_PROCESSES.lock, WaitTarget::from_pid, child.cpu_times, child.exit_code.unwrap_or, encode_wait_status ...(+15) | Err, Ok, PROCESS_GROUP_MANAGER.remove, Some, TASK_MANAGER.remove, WaitFor::PGid ...(+43) |
| `kernel/src/syscall/process/signal/sys.rs#sys_rt_sigtimedwait` | `kernel/src/syscall/signal.rs#sys_rt_sigtimedwait` | 同板块同名 | USER_PROCESSES.lock, err, read_optional_timeout, read_sigset_from_task, signal_bit, table.current_index ...(+5) | Err, Ok, Some, info.not_null, info.write, log::warn! ...(+15) |
| `kernel/src/syscall/process/sys/sched.rs#sys_setpgid` | `kernel/src/syscall/process.rs#sys_setpgid` | 同板块同名 | USER_PROCESSES.lock, err, table
        .tasks
        .iter, table
        .tasks
        .iter()
        .any, table.current_index, table.index_of ...(+1) | Ok, PROCESS_GROUP_MANAGER.add_group, PROCESS_GROUP_MANAGER.add_process, PROCESS_GROUP_MANAGER.get_group, PROCESS_GROUP_MANAGER.get_group(pgid).is_none, TASK_MANAGER.get ...(+2) |
| `kernel/src/syscall/process/sys/sched.rs#sys_setsid` | `kernel/src/syscall/process.rs#sys_setsid` | 同板块同名 | USER_PROCESSES.lock, err, table.current_index, table.tasks.iter, table.tasks.iter().any, table.tasks.iter_mut | Ok, task.pid |

#### 任务调度/异步运行时
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `kernel/src/syscall/select.rs#sys_pselect6` | `kernel/src/syscall/io.rs#sys_pselect6` | 同板块同名 | Err, block_pselect, current.take_any_signal_wake, current.take_wake_reason, current.task, err ...(+10) | Err, Ok, PollEvents::empty, Select2Futures::new, Some, TimeLimitedTaskFuture::new ...(+40) |
| `kernel/src/syscall/select.rs#sys_ppoll` | `kernel/src/syscall/io.rs#sys_ppoll` | 同板块同名 | Err, block_ppoll, core::mem::size_of::<u64>, current.take_any_signal_wake, current.take_wake_reason, current.task ...(+10) | Err, Ok, Select2Futures::new, Some, TimeLimitedTaskFuture::new, UserSlice::<PollFd>::new_unchecked ...(+24) |
| `kernel/src/syscall/fs/path.rs#sys_utimensat` | `kernel/src/syscall/fs.rs#sys_utimensat` | 同板块同名 | Err, Ok, Some, dir_base, err, file.as_inode ...(+9) | Err, Ok, OpenFlags::from_bits, OpenFlags::from_bits(flags).ok_or, TimeSpec::from, dentry.inode ...(+12) |
| `kernel/src/syscall/fs/io.rs#sys_readv` | `kernel/src/syscall/fs.rs#sys_readv` | 同板块同名 | Err, Ok, err, process::with_current_user_context, read_iovec_entry, read_to_user_buffer ...(+3) | Ok, SeekFrom::Current, UserWritePtr::<u8>::from, f.get_file, file.pos, file.read_at ...(+7) |

#### 内存管理
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `kernel/src/syscall/mm.rs#sys_mmap` | `kernel/src/syscall/mm.rs#sys_mmap` | 同板块同名 | Err, Ok, Vec::new, VirtAddr, bytes.resize, bytes.truncate ...(+36) | (addr + length).round_up, Err, MapPerm::from, MmapFlags::from_bits_truncate, MmapProt::from_bits_truncate, Ok ...(+16) |
| `tools/user-smoke.rs#_start` | `arch/src/riscv64/entry.rs#_start` | 同板块同名 | ((grown_brk - 1) as *mut u8).write_volatile, dup_message.as_ptr, dup_message.len, exercise_clock_nanosleep_syscall, exercise_clone_stack_syscall, exercise_clone_wait_syscalls ...(+46) | core::arch::asm! |
| `kernel/src/fs/ramfs/regular.rs#write_at` | `modules/vfs-core/src/file.rs#write_at` | 同板块同名 | Ok, core::cmp::min, end.checked_add, end.checked_add(PAGE_SIZE - 1).ok_or, input.is_empty, input.len ...(+8) | (buf_it.len()).min, Ok, Page::new_file, align_offset_to_page, buf.len, buf_it.is_empty ...(+21) |
| `kernel/src/syscall/socket.rs#sys_socketpair` | `kernel/src/syscall/net.rs#sys_socketpair` | 同板块同名 | (read_fd as i32).to_le_bytes, (write_fd as i32).to_le_bytes, Err, Ok, UserBuffer::new, err ...(+12) | Ok, OpenFlags::empty, new_pipe, sv.write, table.alloc, task.with_mut_fd_table |

#### 中断/异常处理
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `kernel/src/arch/riscv64/trap.rs#enable_timer_interrupt` | `arch/src/riscv64/interrupts.rs#enable_timer_interrupt` | 同板块同名 | core::arch::asm! | sie::set_stimer |
| `kernel/src/arch/loongarch64/trap.rs#init_trap_vector` | `modules/vfs/src/devfs/tty.rs#new` | 同模块上下文对齐/差异观察 | core::arch::asm!, debug_assert_eq!, init_address_translation_control, trap_entry_addr, trap_stack_top_addr | 内部闭环逻辑 |
| `kernel/src/arch/riscv64/trap.rs#init_trap_vector` | `modules/net/src/tcp.rs#block_on_async` | 同模块上下文对齐/差异观察 | core::arch::asm!, core::ptr::addr_of! | Err, Ok, SOCKET_SET.check_poll, SOCKET_SET.poll_interfaces, f, has_signal ...(+3) |
| `kernel/src/arch/loongarch64/trap.rs#trigger_kernel_syscall_probe` | `modules/net/src/tcp.rs#block_on` | 同模块上下文对齐/差异观察 | core::arch::asm! | Err, Ok, SOCKET_SET.check_poll, SOCKET_SET.poll_interfaces, f, has_signal ...(+3) |

#### 系统调用
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `tools/user-smoke-loongarch64.rs#exit` | `user/src/lib.rs#exit` | 同板块同名 | core::hint::spin_loop, syscall1 | sys_exit |
| `tools/user-smoke-loongarch64.rs#exercise_libcbench_arena_reuse_after_old_mapping` | `kernel/src/ipc/futex.rs#requeue_waiters` | 同模块上下文对齐/差异观察 | (ARENA as *mut u64).write_volatile, (OLD as *mut u64).write_volatile, exit, message.as_ptr, message.len, syscall2 ...(+2) | Ok, Vec::with_capacity, log::info!, min, new_waiters.push, old_waiters.is_empty ...(+7) |
| `tools/user-smoke.rs#exercise_mount_syscalls` | `kernel/src/ipc/futex.rs#wake` | 同模块上下文对齐/差异观察 | b"/dev/missing\0".as_ptr, b"/dev/vda2\0".as_ptr, b"/dev\0".as_ptr, b"missing_mnt\0".as_ptr, b"vfat\0".as_ptr, exit ...(+5) | Err, Ok, drop, log::debug!, log::info!, min ...(+5) |
| `tools/user-smoke.rs#exercise_utimensat_syscall` | `user/src/lib.rs#execve` | 同模块上下文对齐/差异观察 | b"/tmp/utime_missing.txt\0".as_ptr, exit, is_error, path.as_ptr, syscall1, syscall4 ...(+1) | CString::new, CString::new(*s).unwrap, CString::new(path).unwrap, argv.as_ptr, argv.iter, argv.iter().map ...(+12) |

#### 文件系统
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `kernel/src/fs/vfs/file.rs#ioctl` | `modules/vfs/src/devfs/tty.rs#ioctl` | 同板块同名 | Err, Ok, flags.to_ne_bytes, ops.ioctl, out.len, out[..FS_IOC_FLAGS_LEN].copy_from_slice ...(+7) | Ok, TtyIoctlCmd::from_repr, log::error!, log::info!, self.inner.lock, todo! ...(+1) |
| `kernel/src/fs/socket/mod.rs#remove` | `modules/vfs/src/fd_table.rs#remove` | 同板块同名 | entry.as_ref, entry.as_ref().is_some_and, pending_ids[..pending_count].iter, pending_ids[..pending_count].iter().copied, self
            .entries
            .iter, self
            .entries
            .iter()
            .position ...(+4) | Err, Ok, self.table.len, self.table[fd].is_none |
| `kernel/src/fs/socket/mod.rs#create` | `modules/vfs-core/src/dentry.rs#create` | 同板块同名 | Err, SOCKET_TABLE.lock, SOCKET_TABLE.lock().create | Err, Ok, child.is_negetive, self.clone, self.clone().base_create, self.get_child_or_create ...(+3) |
| `kernel/src/fs/devfs/mod.rs#ioctl` | `modules/vfs/src/devfs/rtc.rs#ioctl` | 同板块同名 | Err, Ok, VDA2_SIZE_BYTES.to_ne_bytes, node_from_inode, out.len, out[..8].copy_from_slice ...(+2) | Ok, RtcTime::default |

#### 设备驱动
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `kernel/src/drivers/block/mod.rs#devices` | `driver/src/manager.rs#devices` | 同板块同名 | out.iter_mut, out.len, self.state.lock | 内部闭环逻辑 |
| `kernel/src/net/loopback.rs#capabilities` | `driver/src/net/virtio.rs#capabilities` | 同板块同名 | ChecksumCapabilities::ignored, DeviceCapabilities::default | DeviceCapabilities::default |
| `kernel/src/drivers/block/mod.rs#empty` | `driver/src/blk/dw_mshc/registers.rs#empty` | 同板块同名 | 内部闭环逻辑 | CMDARG::new |
| `kernel/src/drivers/block/mod.rs#get` | `driver/src/manager.rs#get` | 同板块同名 | self.state.lock, self.state.lock().devices.get, self.state.lock().devices.get(index).copied, self.state.lock().devices.get(index).copied().flatten | self.devices.get |

#### 网络协议栈
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `kernel/src/net/loopback.rs#receive` | `modules/net/src/lib.rs#receive` | 同板块同名 | self.queue.pop_front, self.queue.pop_front().map | NetRxToken, NetTxToken, Some, dev.can_transmit, dev.receive, dev.recycle_tx_buffers ...(+3) |
| `kernel/src/net/loopback.rs#consume` | `modules/net/src/lib.rs#consume` | 同板块同名 | f, self.queue.push_back, vec! | f, rx_buf.packet_mut, self.0.borrow_mut, self.0.borrow_mut().recycle_rx_buffer, self.0.borrow_mut().recycle_rx_buffer(rx_buf).unwrap, warn! |
| `kernel/src/net/loopback.rs#transmit` | `modules/net/src/lib.rs#transmit` | 同板块同名 | Some | NetTxToken, Some, dev.can_transmit, dev.recycle_tx_buffers, self.inner.borrow_mut, warn! |
| `kernel/src/net/mod.rs#add_udp_socket` | `modules/net/src/tcp.rs#shutdown` | 同模块上下文对齐/差异观察 | self.sockets.add, udp::PacketBuffer::new, udp::Socket::new, vec! | Err, LISTEN_TABLE.unlisten, Ok, SOCKET_SET.check_poll, SOCKET_SET.poll_interfaces, SOCKET_SET.with_socket_mut::<tcp::Socket, _, _> ...(+12) |

#### 文件系统/第三方库
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `kernel/src/fs/ext4/mod.rs#seek` | `modules/ext4/src/disk.rs#seek` | 同板块同名 | Err, Ok, base.checked_add, base.checked_add(offset as i128).ok_or, self.stat | Ok, Some, dev.position, dev.position().checked_add_signed, dev.position().checked_add_signed(off).map, dev.set_position ...(+7) |
| `kernel/src/fs/ext4/mod.rs#map_extent_node` | `modules/ext4/src/dentry.rs#base_rename_to` | 同模块上下文对齐/差异观察 | Err, MappedBlock::Data, Ok, STATE.lock, Some, chosen.ok_or ...(+7) | Err, Ok, flags.contains, lwext4_mvdir, lwext4_mvfile, lwext4_rmdir ...(+13) |
| `kernel/src/fs/ext4/mod.rs#read_inode` | `modules/ext4/src/file/dir.rs#base_load_dir` | 同模块上下文对齐/差异观察 | Err, Ok, STATE.lock, block::read_bytes, block::read_bytes(gd_offset, &mut gd[..state.desc_size as usize]).map_err, block::read_bytes(inode_offset, &mut raw[..read_len]).map_err ...(+13) | CString::new, CString::new(dirent.name).map_err, Ext4DirInode::new, Ext4DirInode::new(self.super_block(), ext4_dir).clone, Ext4FileInode::new, Ext4FileInode::new(self.super_block(), ext4_file).clone ...(+25) |
| `kernel/src/fs/ext4/mod.rs#mount_root` | `modules/ext4/src/dentry.rs#base_create` | 同模块上下文对齐/差异观察 | 1024u32
        .checked_shl, 1024u32
        .checked_shl(log_block_size)
        .ok_or, Err, Ok, STATE.lock, block::read_bytes ...(+3) | (OpenFlags::O_RDWR | OpenFlags::O_CREAT | OpenFlags::O_TRUNC).bits, Ext4DirInode::new, Ext4FileInode::new, LwExt4Dir::create, LwExt4Dir::create(&path).map_err, LwExt4File::open ...(+15) |

#### 第三方库
仅参考项目中观察到该模块代表函数，目标项目没有同模块可对齐证据：
- 参考项目：`crates/ring-buffer/src/lib.rs#read`，调用摘要：buf.is_empty, buf.len, buf[..ret_len].copy_from_slice, buf[..right_len].copy_from_slice, buf[right_len..ret_len].copy_from_slice, cmp::min ...(+1)
- 参考项目：`crates/ring-buffer/src/lib.rs#write`，调用摘要：buf.is_empty, buf.len, cmp::min, self.arr.len, self.arr[..(ret_len - (n - self.tail))]
                    .copy_from_slice, self.arr[self.tail..].copy_from_slice ...(+1)
- 参考项目：`crates/range-map/src/lib.rs#find_free_range`，调用摘要：Some, debug_assert!, self.0.range, self.0.range(..=start).next_back, start.min
- 参考项目：`crates/range-map/src/lib.rs#try_insert`，调用摘要：Err, Ok, debug_assert!, log::error!, self.0.range, self.0.range(..range.end).next_back ...(+3)

#### 通用模块/其他
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `kernel/src/sync/spin.rs#lock` | `modules/sync/src/mutex/spin_mutex.rs#lock` | 同板块同名 | self
            .locked
            .compare_exchange, self
            .locked
            .compare_exchange(false, true, Ordering::Acquire, Ordering::Relaxed)
            .is_err, self.locked.load, spin_loop | S::before_lock, self
                .lock
                .compare_exchange, self
                .lock
                .compare_exchange(false, true, Ordering::Acquire, Ordering::Relaxed)
                .is_ok, self.wait_unlock |
| `kernel/src/sync/spin.rs#deref_mut` | `modules/sync/src/mutex/sleep_mutex.rs#deref_mut` | 同板块同名 | self.lock.value.get | self.mutex.data.get |
| `kernel/src/sync/spin.rs#deref` | `modules/sync/src/mutex/sleep_mutex.rs#deref` | 同板块同名 | self.lock.value.get | self.mutex.data.get |
| `kernel/src/console/mod.rs#write_str` | `user/src/console.rs#write_str` | 同板块同名 | Ok, put_byte, s.bytes | Ok, s.as_bytes, write |


好的，我将遵循审计指令，基于提供的客观 AST 证据，对本次同源性定性仲裁给出最终结论。

### 1. 宏观判定
- **定性结论**: **独立开发**
  基于客观 AST 指纹数据，目标项目与参考项目的整体相似度为 0.53%，共同 AST 指纹仅 19 个，远低于可认定为“代码复用”的阈值。该相似度水平完全处于两个独立项目因实现相同领域规范（如 POSIX/Linux 系统调用接口）而产生的“领域同构噪声”范围内。双方在核心架构设计、数据结构和同步策略上存在根本性差异，不构成抄袭或非合规复用。

### 2. 全模块覆盖矩阵解读
- **进程管理**
  1. 双方均存在该模块。
  2. 存在 2 个共同 AST 指纹。
  3. 目标覆盖率 0.43%，参考覆盖率 1.87%，表明在函数数量众多的进程管理模块中，结构重合度极低。
  4. 证据强度：**弱**。共同指纹源于 POSIX 标准系统调用同名，底层实现路径和数据结构完全不同。

- **任务调度/异步运行时**
  1. 双方均存在该模块。
  2. 存在 1 个共同 AST 指纹。
  3. 目标覆盖率 0.67%，参考覆盖率 0.54%，覆盖率极低。
  4. 证据强度：**弱**。共同指纹 `sys_pselect6` 等为 Linux 标准接口，双方在异步模型（如目标项目的 `block_pselect` vs 参考项目的 `Select2Futures`）上存在显著架构差异。

- **内存管理**
  1. 双方均存在该模块。
  2. 存在 6 个共同 AST 指纹。
  3. 目标覆盖率 1.12%，参考覆盖率 1.75%，覆盖率极低。
  4. 证据强度：**弱**。共同指纹如 `sys_mmap` 为通用系统调用，但参数处理、权限模型（如 `MapPerm` vs `MmapProt`）和内部实现路径不同。

- **中断/异常处理**
  1. 双方均存在该模块。
  2. 未观察到共同 AST 指纹。
  3. 目标覆盖率 0.0%，参考覆盖率 0.0%。
  4. 证据强度：**未形成**。双方在该模块无任何结构重合。

- **系统调用**
  1. 双方均存在该模块。
  2. 未观察到共同 AST 指纹。
  3. 目标覆盖率 0.0%，参考覆盖率 0.0%。
  4. 证据强度：**未形成**。双方在该模块无任何结构重合。

- **文件系统**
  1. 双方均存在该模块。
  2. 未观察到共同 AST 指纹。
  3. 目标覆盖率 0.0%，参考覆盖率 0.0%。
  4. 证据强度：**未形成**。双方在该模块无任何结构重合。

- **设备驱动**
  1. 双方均存在该模块。
  2. 未观察到共同 AST 指纹。
  3. 目标覆盖率 0.0%，参考覆盖率 0.0%。
  4. 证据强度：**未形成**。双方在该模块无任何结构重合。

- **网络协议栈**
  1. 双方均存在该模块。
  2. 未观察到共同 AST 指纹。
  3. 目标覆盖率 0.0%，参考覆盖率 0.0%。
  4. 证据强度：**未形成**。双方在该模块无任何结构重合。

- **第三方库/公共依赖**
  1. 仅参考项目体现该模块。
  2. 未观察到共同 AST 指纹。
  3. 目标覆盖率 0.0%，参考覆盖率 0.0%。
  4. 证据强度：**未形成**。目标项目无此模块，无法对比。

### 3. 底层 AST 关键证据指纹表
| 所属板块 | 目标项目路径 | 参考项目路径 | 证据类型 | 技术说明 |
| :--- | :--- | :--- | :--- | :--- |
| 进程管理 | `kernel/src/syscall/process/sys/wait.rs#sys_wait4` | `kernel/src/syscall/process.rs#sys_wait4` | 同板块同名 | 均为 Linux 标准系统调用。目标使用 `USER_PROCESSES.lock` 和 `WaitTarget`，参考使用 `TASK_MANAGER` 和 `WaitFor`，底层管理模型不同。 |
| 进程管理 | `kernel/src/syscall/process/signal/sys.rs#sys_rt_sigtimedwait` | `kernel/src/syscall/signal.rs#sys_rt_sigtimedwait` | 同板块同名 | 均为 POSIX 标准信号接口。目标通过 `table.tasks.iter` 遍历任务表处理信号，参考通过 `pending.dequeue_expect` 操作信号队列，实现机制不同。 |
| 进程管理 | `kernel/src/syscall/process/sys/sched.rs#sys_setpgid` | `kernel/src/syscall/process.rs#sys_setpgid` | 同板块同名 | 均为标准进程组管理调用。目标内联遍历 `table.tasks` 实现，参考调用独立的 `PROCESS_GROUP_MANAGER` 模块，架构设计相反。 |
| 进程管理 | `kernel/src/syscall/process/sys/sched.rs#sys_setsid` | `kernel/src/syscall/process.rs#sys_setsid` | 同板块同名 | 均为标准会话管理调用。目标遍历 `table.tasks` 进行校验，参考直接操作 `task.pid`，实现逻辑不同。 |
| 任务调度/异步运行时 | `kernel/src/syscall/select.rs#sys_pselect6` | `kernel/src/syscall/io.rs#sys_pselect6` | 同板块同名 | 均为 Linux I/O 多路复用接口。目标使用 `block_pselect` 阻塞模型，参考使用 `Select2Futures` 异步模型，调度策略不同。 |
| 内存管理 | `kernel/src/syscall/mm.rs#sys_mmap` | `kernel/src/syscall/mm.rs#sys_mmap` | 同板块同名 | 均为标准内存映射调用。目标使用 `VirtAddr` 和 `Vec` 管理映射，参考使用 `MapPerm` 和 `MmapFlags` 位标志，参数处理模型不同。 |
| 通用模块/其他 | `kernel/src/sync/spin.rs#lock` | `modules/sync/src/mutex/spin_mutex.rs#lock` | 同板块同名 | 均为自旋锁实现。两者都使用了 `compare_exchange` 循环，这是实现自旋锁的标准 Rust 模式，属于领域通用写法。 |

### 4. 核心代码调用流对比剖析
* **【进程管理 / `sys_wait4` 调用路径】**
* **底层技术析理**: 目标项目通过 `USER_PROCESSES.lock` 获取全局进程表锁，然后使用 `WaitTarget::from_pid` 构造等待目标，并通过 `encode_wait_status` 和 `to_ne_bytes` 处理子进程退出状态。参考项目则通过 `TASK_MANAGER.remove` 直接管理任务生命周期，并使用 `WaitFor::Pid` 和 `WaitOptions::from_bits_truncate` 处理等待选项。两者在进程生命周期管理上存在根本差异：目标项目采用基于锁和状态编码的集中式管理，参考项目采用基于任务管理器的移除式管理。
* **代码溯源链接**:
  - 目标项目: `kernel/src/syscall/process/sys/wait.rs#sys_wait4`
  - 参考项目: `kernel/src/syscall/process.rs#sys_wait4`

* **【进程管理 / `sys_exit_group` 调用路径】**
* **底层技术析理**: 目标项目将线程组退出逻辑完全委托给 `super::super::table::terminate_current_user_group` 函数，这是一个高度封装的抽象。参考项目则显式地通过 `task.with_thread_group` 获取线程组，然后调用 `tg.iter` 遍历并逐一设置 `t.set_terminated` 和 `task.set_exit_code`。目标项目倾向于高层抽象封装，而参考项目倾向于显式的迭代器操作，体现了不同的代码组织哲学。
* **代码溯源链接**:
  - 目标项目: `kernel/src/syscall/process/sys/exit.rs#sys_exit_group`
  - 参考项目: `kernel/src/syscall/process.rs#sys_exit_group`

* **【进程管理 / `add_child` / `remove_child` 调用路径】**
* **底层技术析理**: 目标项目使用 `Vec` 的 `push` 和 `iter().position().remove` 方法管理子进程，这是一种基于线性表的无锁或外部加锁实现。参考项目使用 `HashMap` 的 `try_insert` 和 `remove` 方法，并内部通过 `self.children.lock()` 进行细粒度加锁。数据结构（`Vec` vs `HashMap`）和同步策略（外部锁 vs 内部锁）的差异导致两者的 AST 调用图谱完全不同，是架构设计层面的根本分歧。
* **代码溯源链接**:
  - 目标项目: `kernel/src/task/process.rs#add_child` / `kernel/src/task/process.rs#remove_child`
  - 参考项目: `kernel/src/task/task.rs#add_child` / `kernel/src/task/task.rs#remove_child`

* **【内存管理 / `sys_mmap` 调用路径】**
* **底层技术析理**: 目标项目的 `sys_mmap` 调用流涉及 `VirtAddr`、`Vec::new`、`bytes.resize` 等操作，表明其倾向于使用 Rust 标准库的向量和虚拟地址抽象进行内存管理。参考项目则使用 `MapPerm::from`、`MmapFlags::from_bits_truncate`、`MmapProt::from_bits_truncate` 等位标志和权限结构体，更贴近传统 C 风格的内存管理模型。两者在内存抽象和参数处理模型上存在显著差异。
* **代码溯源链接**:
  - 目标项目: `kernel/src/syscall/mm.rs#sys_mmap`
  - 参考项目: `kernel/src/syscall/mm.rs#sys_mmap`

### 5. 单方模块与降权项
- **单方模块**: 参考项目中的 `第三方库` 模块（如 `crates/ring-buffer`、`crates/range-map`）在目标项目中无对应实现，无法形成证据。
- **降权项**:
  - **POSIX/Linux 标准系统调用名**: `sys_wait4`、`sys_execve`、`sys_getpid` 等函数名是遵循 Linux ABI 的必然结果，同名不能作为代码复用的强证据。
  - **跨板块同名函数**: 如目标项目 `kernel/src/fs/procfs/mod.rs#readlink` 与参考项目 `modules/vfs/src/procfs/self_.rs#readlink`，虽同名但分属不同子系统，实现逻辑和调用对象完全不同，不构成同板块主证据。
  - **通用编程模式**: 如自旋锁实现中的 `compare_exchange` 循环，是 Rust 语言中实现该同步原语的标准范式，属于领域公共知识，不能作为抄袭证据。

### 6. 保守结论
本次审计基于客观 AST 指纹数据，目标项目与参考项目的整体相似度仅为 0.53%，共同指纹 19 个，覆盖率极低。该水平的相似度完全可由两个项目独立实现同一套 POSIX/Linux 系统调用接口来解释，属于“领域同构噪声”。在进程管理、内存管理等核心模块的深度剖析中，双方在数据结构（`Vec` vs `HashMap`）、同步策略（外部锁 vs 内部锁）、对象管理模型（`USER_PROCESSES` vs `TASK_MANAGER`）和抽象层次（闭包封装 vs 直接字段访问）上均表现出根本性的架构差异。所有共同指纹均可归因于标准接口同名或通用编程范式，未发现任何非标准的、私有的“同源指纹”。因此，综合判定目标项目为独立开发，不存在对参考项目的代码抄袭或非合规复用行为，整体风险极低。

------------------------------

