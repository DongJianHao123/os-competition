# 🛡️ OS 内核赛自动化代码级架构审计报告

**审计时间**: `2026-07-02 03:59:48`
**参赛作品名称**: `2026-T2026103369910030-XX`
**年份**: `2026`
**学校**: `XX`
**队伍名称**: `T2026103369910030`
**比赛名字**: `OS-Competition`
**比赛子赛道**: `Kernel-Implementation`
**仓库地址**: `https://gitlab.eduxiji.net/educg-group-43501-3132633/T2026103369910030-2019`
**推测开发语言**: `Rust / C (由AST探针自动识别)`
**特征函数量**: `5091`

## ⚔️ 底层代码同源性对比 (AST Homology)
> 本部分由专门的代码审查智能体，对该项目与符合年份条件的历届最高相似度项目进行控制流与底层调用对比。新版报告会先给出确定性的全模块覆盖矩阵，再给出 Agent 的分模块解释，避免只围绕少量抽样函数展开。

### 🏆 Top 4 底层对标: vs `2024-10183_BOOT-吉林大学`（吉林大学 / 10183_BOOT）

#### 对方作品信息

**对方年份**: `2024`
**对方学校**: `吉林大学`
**对方队伍名称**: `10183_BOOT`
**对方比赛名字**: `OS-Competition`
**对方比赛子赛道**: `Kernel-Implementation`
**对方仓库地址**: `https://gitlab.eduxiji.net/educg-group-26010-2376550/T202410183992516-775`

> 双向覆盖率：共同 AST 指纹: 393；唯一 AST 指纹: 目标项目 4055，参考项目 743；覆盖率: 目标项目 9.69%，参考项目 52.89%

## 全模块对比总览（确定性证据）

> 总体覆盖率：共同 AST 指纹: 393；唯一 AST 指纹: 目标项目 4055，参考项目 743；覆盖率: 目标项目 9.69%，参考项目 52.89%

### 1. 模块覆盖矩阵
| 模块 | 目标函数数 | 参考函数数 | 共同唯一 AST 指纹 | Target 覆盖 | Base 覆盖 | 状态 |
| :--- | ---: | ---: | ---: | ---: | ---: | :--- |
| 进程管理 | 533 | 39 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |
| 任务调度/异步运行时 | 429 | 34 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |
| 内存管理 | 667 | 138 | 4 | 0.68% | 3.01% | 双方存在且有结构重合 |
| 中断/异常处理 | 57 | 15 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |
| 系统调用 | 154 | 7 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |
| 文件系统 | 1298 | 85 | 2 | 0.2% | 2.82% | 双方存在且有结构重合 |
| 设备驱动 | 288 | 29 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |
| 网络协议栈 | 278 | 0 | 0 | 0.0% | 0.0% | 仅目标项目体现 |
| 文件系统/第三方库 | 888 | 485 | 391 | 51.65% | 97.75% | 双方存在且有结构重合 |
| 通用模块/其他 | 499 | 52 | 1 | 0.22% | 2.27% | 双方存在且有结构重合 |

### 2. 分模块证据清单

#### 进程管理
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `kernelx/src/kernel/task/pcb/lifecycle.rs#exit` | `kernel/proc.c#exit` | 同板块同名 | KSiFields::SigChld, Some, State::Exited, c.parent.lock, c.set_wait_parent_tid, child.is_exited ...(+54) | acquire, fileclose, myproc, mythread, panic, release ...(+3) |
| `kernelx/src/kernel/syscall/task.rs#waitid` | `ipc/signal.c#sigprocmask` | 同模块上下文对齐/差异观察 | ChildWaitOptions::new, ChildWaitOptions::new(!options.contains(WaitIdOptions::WNOHANG))
        .wait_parent_tid, ChildWaitOptions::new(!options.contains(WaitIdOptions::WNOHANG))
        .wait_parent_tid(wait_parent_tid)
        .wait_exited, ChildWaitOptions::new(!options.contains(WaitIdOptions::WNOHANG))
        .wait_parent_tid(wait_parent_tid)
        .wait_exited(wait_exited)
        .wait_stopped, ChildWaitOptions::new(!options.contains(WaitIdOptions::WNOHANG))
        .wait_parent_tid(wait_parent_tid)
        .wait_exited(wait_exited)
        .wait_stopped(wait_stopped)
        .wait_ptrace_stopped, ChildWaitOptions::new(!options.contains(WaitIdOptions::WNOHANG))
        .wait_parent_tid(wait_parent_tid)
        .wait_exited(wait_exited)
        .wait_stopped(wait_stopped)
        .wait_ptrace_stopped(wait_stopped)
        .wait_continued ...(+32) | mythread |
| `kernelx/src/kernel/task/pcb/lifecycle.rs#new` | `kernel/futex.c#futexWait` | 同模块上下文对齐/差异观察 | Arc::new, PendingSignalQueue::new, SleepLock::new, Some, SpinLock::new, TimerTable::new ...(+32) | 内部闭环逻辑 |
| `kernelx/src/kernel/syscall/futex.rs#futex_waitv` | `user/sh.c#parseexec` | 同模块上下文对齐/差异观察 | Err, FutexWaitvClockId::try_from, FutexWaitvClockId::try_from(clockid).map_err, FutexWaitvFlags::from_bits, FutexWaitvFlags::from_bits(waiter.flags).ok_or, Ok ...(+33) | execcmd, gettoken, panic, parseblock, parseredirs, peek |

#### 任务调度/异步运行时
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `kernelx/src/kernel/trap.rs#syscall` | `kernel/syscall/syscall.c#syscall` | 同板块同名 | Err, Some, current::tcb, syscall::should_restart_on_eintr, syscall::syscall, tcb.push_ucontext_syscall_retreg ...(+2) | NELEM, myproc, mythread, printf, syscalls[num] |
| `kernelx/src/kernel/syscall/event.rs#select` | `fs/fat32/fat.c#emake` | 同模块上下文对齐/差异观察 | Err, FileEvent::empty, Ok, Some, Vec::new, current::fdtable ...(+53) | (uint16), cal_checksum, generate_shortname, memset, panic, reloc_clus ...(+3) |
| `kernelx/src/kernel/ipc/signal/handle.rs#handle_signal` | `fs/fat32/fat.c#eput` | 同模块上下文对齐/差异观察 | (*user_context).into, Into::<u32>::into, SigFrame::empty, SignalStack::from_state, Some, action.flags.contains ...(+47) | acquire, acquiresleep, elock, eput, etrunc, eunlock ...(+3) |
| `kernelx/src/fs/vfs/mount.rs#move_mount` | `user/sh.c#runcmd` | 同模块上下文对齐/差异观察 | Arc::ptr_eq, Err, Ok, Self::check_bind_target_type, Self::join_mounts_to_group, Self::mount_chain_contains ...(+46) | close, dup, exec, exit, fork1, fprintf ...(+5) |

#### 内存管理
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `kernelx/src/kernel/syscall/mm.rs#mmap` | `kernel/proc.c#mmap` | 同板块同名 | Box::new, Err, MMapFlags::from_bits, MMapFlags::from_bits(flags).ok_or, MMapProt::from_bits, MMapProt::from_bits(prot).ok_or ...(+23) | PGROUNDDOWN, filedup, myproc, panic, printf |
| `kernelx/src/kernel/mm/maparea/filemap/private.rs#fork` | `kernel/proc.c#fork` | 同板块同名 | Box::new, FrameState::Cow, core::mem::replace, f.get_page, frame.clone, matches! ...(+11) | acquire, allocproc, filedup, freeproc, memmove, mykthread ...(+4) |
| `kernelx/src/kernel/mm/swappable/nofile/swap.rs#free` | `user/umalloc.c#free` | 同板块同名 | SWAPPER.free_slot, allocated.frame.get_page, self.state.lock, swapper::remove_lru | 内部闭环逻辑 |
| `kernelx/src/arch/riscv/pagetable/pagetable.rs#munmap` | `kernel/proc.c#munmap` | 同板块同名 | Err, Ok, PTEFlags::empty, pte.set_flags, pte.write_back, pte.write_back().expect ...(+1) | fileclose, filewrite, myproc, panic, uvmunmap, walkaddr |

#### 中断/异常处理
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `kernelx/src/driver/pci.rs#from_fdt` | `kernel/trap.c#kerneltrapframedump` | 同模块上下文对齐/差异观察 | BTreeMap::new, chunk[0..4].try_into, chunk[0..4].try_into().unwrap, chunk[12..16].try_into, chunk[12..16].try_into().unwrap, chunk[20..24].try_into ...(+11) | printf |
| `kernelx/src/fs/devfs/inode/pty/inner.rs#read_buffer` | `kernel/trap.c#trapframedump` | 同模块上下文对齐/差异观察 | Err, Ok, buf.iter_mut, current::block, hung_up, ring.lock ...(+5) | printf |
| `kernelx/src/arch/loongarch/eiointc.rs#dump_irq` | `kernel/thread.c#threadinit` | 同模块上下文对齐/差异观察 | crate::kinfo!, iocsr::iocsr_read_d | initlock |
| `kernelx/src/driver/char/tty.rs#termios` | `kernel/futex.c#futexRequeue` | 同模块上下文对齐/差异观察 | Termios::default, self.attr | 内部闭环逻辑 |

#### 系统调用
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `kernelx/src/kernel/syscall/ipc.rs#msgctl` | `kernel/syscall/sysmem.c#sys_mmap` | 同模块上下文对齐/差异观察 | Err, IpcCtlFlags::IPC_64.bits, MsgCtlCmd::try_from, MsgCtlCmd::try_from(cmd & !IpcCtlFlags::IPC_64.bits()).map_err, Ok, UPtr::<UserMsgInfo>::from_uaddr ...(+14) | argaddr, argint, mmap |
| `kernelx/src/kernel/syscall/event.rs#setitimer` | `kernel/syscall/syssig.c#sys_tgkill` | 同模块上下文对齐/差异观察 | Box::new, Err, ITimerWhich::try_from, ITimerWhich::try_from(which).map_err, Ok, Some ...(+21) | argint, tkill |
| `kernelx/src/kernel/syscall/socket.rs#socketpair` | `kernel/syscall/syscall.c#argstr` | 同模块上下文对齐/差异观察 | AddressFamily::try_from, AddressFamily::try_from(domain).map_err, Arc::new, Err, Ok, SocketKind::try_from ...(+9) | argaddr, fetchstr |
| `kernelx/src/kernel/syscall/ipc.rs#sigaltstack` | `kernel/syscall/syssig.c#sys_rt_sigreturn` | 同模块上下文对齐/差异观察 | Err, Ok, SignalStackFlags::SS_DISABLE.bits, SignalStackFlags::empty, SignalStackFlags::from_bits, SignalStackFlags::from_bits(stack.ss_flags).ok_or ...(+9) | rt_sigreturn |

#### 文件系统
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `kernelx/src/fs/memtreefs/inode.rs#readlink` | `fs/vfs/fs.c#readlink` | 同板块同名 | Ok, Some, buf.len, buf[..len].copy_from_slice, core::cmp::min, kclock::now ...(+4) | ext4_readlink |
| `kernelx/clib/src/klib/panic.c#kpanic` | `fs/fat32/fat.c#free_clus` | 同板块同结构 | rust_kpanic | write_fat |
| `kernelx/clib/lib/libfdt/fdt_sw.c#fdt_create` | `fs/fat32/fat.c#enameparent` | 同板块同结构 | fdt_create_with_flags | lookup_path |
| `kernelx/src/kernel/syscall/fs.rs#do_openat_with_lookup_flags` | `fs/fat32/FileSystem.c#fatInit` | 同模块上下文对齐/差异观察 | Err, Mode::from_bits, Mode::from_bits(mode as u32 & 0o7777 & !current::umask()).ok_or, Ok, OpenFlags::O_RDWR.bits, OpenFlags::O_WRONLY.bits ...(+51) | brelse, fs->read, initsleeplock, memmove, memset, panic ...(+2) |

#### 设备驱动
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `kernelx/src/driver/char/tty.rs#ioctl` | `block/bio.c#bget` | 同模块上下文对齐/差异观察 | IoctlReq::try_from, Ok, TtyIoctlResult::FlushInput, TtyIoctlResult::Handled, addrspace.copy_from_user::<Termios2>, addrspace.copy_from_user::<Termios> ...(+10) | acquire, acquiresleep, panic, release |
| `kernelx/src/driver/virtio/matcher.rs#try_match` | `drivers/console.c#consputc` | 同模块上下文对齐/差异观察 | Arc::new, MsixPciTransport::new, PciTransport::new::<VirtIOHal>, Some, VirtIOBlockDriver::new, device.device_function ...(+9) | uart8250_putc, uartputc_sync |
| `kernelx/src/arch/loongarch/fdt.rs#load_device_tree` | `platform/qemu/uart.c#uartinit` | 同模块上下文对齐/差异观察 | Err, Fdt::new, Fdt::new(data).map_err, Ok, arch::paddr_to_kaddr, chosen.bootargs ...(+14) | WriteReg, initlock |
| `kernelx/src/driver/char/serial/ns16550a.rs#try_match` | `platform/qemu/uart.c#uartstart` | 同模块上下文对齐/差异观察 | Arc::new, Box::new, Serial::new, Some, Stty::new, arch::enable_device_interrupt_irq ...(+18) | ReadReg, WriteReg, wakeup |

#### 网络协议栈
仅目标项目中观察到该模块代表函数，参考项目没有同模块可对齐证据：
- 目标项目：`kernelx/src/net/interface/dispatch.rs#on_receive`，调用摘要：EthernetFrame::parse, SocketAddr::new, eth.payload, flags.intersects, ip.header_len, ip.payload ...(+21)
- 目标项目：`kernelx/src/net/socket/netlink.rs#handle_getlink`，调用摘要：NlMsgFlags::MULTI.bits, Vec::new, as_bytes, attrs.len, core::mem::size_of::<IfInfoMsg>, flags.bits ...(+16)
- 目标项目：`kernelx/src/net/interface/dhcp.rs#dhcp_configure`，调用摘要：DHCPBuilder::new, DHCPBuilder::new()
                    .op, DHCPBuilder::new()
                    .op(DhcpOp::Request)
                    .xid, DHCPBuilder::new()
                    .op(DhcpOp::Request)
                    .xid(xid)
                    .chaddr, DHCPBuilder::new()
                    .op(DhcpOp::Request)
                    .xid(xid)
                    .chaddr(mac)
                    .message_type, DHCPBuilder::new()
                    .op(DhcpOp::Request)
                    .xid(xid)
                    .chaddr(mac)
                    .message_type(DhcpMessageType::Request)
                    .add_option ...(+15)
- 目标项目：`kernelx/src/net/protocol/ipv4.rs#build`，调用摘要：0u16.to_be_bytes, Ok, Self::compute_checksum, cksum.to_be_bytes, data.len, data[10..12].copy_from_slice ...(+15)

#### 文件系统/第三方库
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `kernelx/clib/lib/lwext4/lwext4/src/ext4_balloc.c#ext4_balloc_alloc_block` | `fs/lwext4/ext4_balloc.c#ext4_balloc_alloc_block` | 同板块同名同结构 | ext4_balloc_get_bgid_of_block, ext4_balloc_get_block_of_bgid, ext4_balloc_set_bitmap_csum, ext4_balloc_verify_bitmap_csum, ext4_bg_get_block_bitmap, ext4_bg_get_free_blocks_count ...(+19) | ext4_balloc_get_bgid_of_block, ext4_balloc_get_block_of_bgid, ext4_balloc_set_bitmap_csum, ext4_balloc_verify_bitmap_csum, ext4_bg_get_block_bitmap, ext4_bg_get_free_blocks_count ...(+19) |
| `kernelx/clib/lib/lwext4/lwext4/src/ext4_journal.c#jbd_journal_prepare` | `fs/lwext4/ext4_journal.c#jbd_journal_prepare` | 同板块同名同结构 | EXT4_BLOCK_ZERO, RB_FIND, TAILQ_FOREACH_REVERSE_SAFE, TAILQ_FOREACH_SAFE, TAILQ_REMOVE, ext4_bcache_clear_dirty ...(+17) | EXT4_BLOCK_ZERO, RB_FIND, TAILQ_FOREACH_REVERSE_SAFE, TAILQ_FOREACH_SAFE, TAILQ_REMOVE, ext4_bcache_clear_dirty ...(+17) |
| `kernelx/clib/lib/lwext4/lwext4/src/ext4.c#ext4_dir_rm` | `fs/lwext4/ext4.c#ext4_dir_rm` | 同板块同名同结构 | EXT4_MP_LOCK, EXT4_MP_UNLOCK, ext4_block_cache_write_back, ext4_dir_en_get_inode, ext4_dir_iterator_fini, ext4_dir_iterator_init ...(+19) | EXT4_MP_LOCK, EXT4_MP_UNLOCK, ext4_block_cache_write_back, ext4_dir_en_get_inode, ext4_dir_iterator_fini, ext4_dir_iterator_init ...(+19) |
| `kernelx/clib/lib/lwext4/lwext4/src/ext4_dir_idx.c#ext4_dir_dx_split_index` | `fs/lwext4/ext4_dir_idx.c#ext4_dir_dx_split_index` | 同板块同名同结构 | ext4_block_set, ext4_dir_dx_climit_get_count, ext4_dir_dx_climit_get_limit, ext4_dir_dx_climit_set_count, ext4_dir_dx_climit_set_limit, ext4_dir_dx_entry_get_hash ...(+10) | ext4_block_set, ext4_dir_dx_climit_get_count, ext4_dir_dx_climit_get_limit, ext4_dir_dx_climit_set_count, ext4_dir_dx_climit_set_limit, ext4_dir_dx_entry_get_hash ...(+10) |

#### 通用模块/其他
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `kernelx/src/klib/ksync/lockdep.rs#release` | `kernel/atomic/spinlock.c#release` | 同板块同名 | core::ptr::eq, held.iter, held.iter().rposition, held.remove, panic!, self.held.get | __sync_lock_release, __sync_synchronize, holding, panic, pop_off |
| `kernelx/clib/src/klib/string.c#strncmp` | `lib/string.c#strncmp` | 同板块同名 | 内部闭环逻辑 | 内部闭环逻辑 |
| `kernelx/clib/src/klib/string.c#strcmp` | `lib/string.c#strcmp` | 同板块同名 | 内部闭环逻辑 | 内部闭环逻辑 |
| `kernelx/clib/src/klib/string.c#strcpy` | `lib/string.c#strcpy` | 同板块同名 | 内部闭环逻辑 | 内部闭环逻辑 |


好的，审判长。本庭已收到控辩双方的全部陈述、客观AST证据及模块画像。现依据《审计指令》，对目标项目 `[2026-T2026103369910030-XX]` 与参考项目 `[2024-10183_BOOT-吉林大学]` 进行最终同源性定性仲裁。

### 1. 宏观判定
- **定性结论**: **独立开发，底层架构显著差异**
- **核心依据**:
  - **整体相似度极低**: 客观 AST 相似度仅为 **8.92%**。目标项目覆盖率 9.69%，参考项目覆盖率 52.89%。
  - **高覆盖率源于第三方库**: 参考项目 52.89% 的高覆盖率几乎全部由 `文件系统/第三方库` 模块贡献（该模块参考覆盖率达 97.75%），其共同指纹对应 `lwext4` 库和标准 C 函数，属于公开代码，与内核核心逻辑无关。
  - **核心板块零重合**: 控方指控的核心板块【进程管理】，其共同唯一 AST 指纹为 **0**，目标与参考覆盖率均为 0.0%，是“底层架构显著差异”的最强有力证据。

### 2. 全模块覆盖矩阵解读
| 模块 | 双方存在 | 共同AST指纹 | 覆盖率 (目标/参考) | 证据强度 | 解读 |
| :--- | :---: | :---: | :---: | :---: | :--- |
| **进程管理** | 是 | **0** | 0.0% / 0.0% | **未形成** | 控方指控的核心板块。533 vs 39 个函数中无任何共同结构指纹，彻底否定“语义复用”指控。双方均实现 `exit`、`wait` 等 POSIX 接口，但底层 AST 拓扑完全不同。 |
| **任务调度/异步运行时** | 是 | **0** | 0.0% / 0.0% | **未形成** | 双方均存在调度模块，但无共同指纹。参考项目为经典 `sched` 协程切换，目标项目为 Rust 异步运行时，架构范式根本不同。 |
| **内存管理** | 是 | 4 | 0.68% / 3.01% | **弱** | 存在极少量共同指纹。同名函数 `mmap`、`munmap`、`fork` 源于 POSIX 标准接口名，其内部调用图谱（目标用 `MMapFlags::from_bits`，参考用 `argint`）完全不同。 |
| **中断/异常处理** | 是 | **0** | 0.0% / 0.0% | **未形成** | 双方均存在该模块，但无共同指纹。目标项目包含多架构（RISC-V, LoongArch）中断控制器驱动，参考项目为简单的 `trap.c`，实现层次差异巨大。 |
| **系统调用** | 是 | **0** | 0.0% / 0.0% | **未形成** | 双方均存在系统调用分发机制，但无共同指纹。同名函数（如 `syscall`）是操作系统通用概念，其内部调用链无任何重合。 |
| **文件系统** | 是 | 2 | 0.2% / 2.82% | **弱** | 共同指纹极少。同名函数 `readlink` 源于 POSIX 接口。其余证据为跨板块硬凑或第三方库函数，不构成核心逻辑复用。 |
| **设备驱动** | 是 | **0** | 0.0% / 0.0% | **未形成** | 双方均存在驱动模型，但无共同指纹。目标项目包含 VirtIO、PCI、NS16550A 等现代驱动，参考项目为简易 UART，无 AST 重合。 |
| **网络协议栈** | **仅目标** | 0 | 0.0% / 0.0% | **未形成** | 参考项目完全缺失该模块。目标项目自研了完整的 TCP/IP、DHCP、Netlink 协议栈，是独立开发的强正向证据。 |
| **第三方库/公共依赖** | 是 | **391** | 51.65% / 97.75% | **强（但无关）** | **这是双方 AST 重合的唯一主要来源。** 共同指纹集中于 `lwext4` 文件系统库和 `string.c` 等标准 C 函数。这些是公开的第三方代码，与内核原创性无关，必须从核心逻辑指控中剥离。 |

### 3. 底层 AST 关键证据指纹表
| 所属板块 | 目标项目路径 | 参考项目路径 | 证据类型 | 技术说明 |
| :--- | :--- | :--- | :--- | :--- |
| **第三方库/公共依赖** | `kernelx/clib/lib/lwext4/lwext4/src/ext4_balloc.c#ext4_balloc_alloc_block` | `fs/lwext4/ext4_balloc.c#ext4_balloc_alloc_block` | 同源公开库 | 双方均使用了开源的 `lwext4` 库，函数名、调用链完全一致，贡献了绝大多数共同 AST 指纹。 |
| **第三方库/公共依赖** | `kernelx/clib/lib/lwext4/lwext4/src/ext4_journal.c#jbd_journal_prepare` | `fs/lwext4/ext4_journal.c#jbd_journal_prepare` | 同源公开库 | 同上，属于 `lwext4` 库内部实现，非任何一方原创逻辑。 |
| **第三方库/公共依赖** | `kernelx/clib/src/klib/string.c#strncmp` | `lib/string.c#strncmp` | 标准C函数 | 标准 C 库函数，实现逻辑固定，不构成抄袭。 |
| **第三方库/公共依赖** | `kernelx/clib/src/klib/string.c#strcmp` | `lib/string.c#strcmp` | 标准C函数 | 同上。 |
| **第三方库/公共依赖** | `kernelx/clib/src/klib/string.c#strcpy` | `lib/string.c#strcpy` | 标准C函数 | 同上。 |
| 通用模块/其他 | `kernelx/src/klib/ksync/lockdep.rs#release` | `kernel/atomic/spinlock.c#release` | 同名不同构 | 函数名 `release` 是通用操作。目标为 Rust 的锁依赖检查，参考为 C 的自旋锁释放，内部逻辑完全不同。 |
| 内存管理 | `kernelx/src/kernel/syscall/mm.rs#mmap` | `kernel/proc.c#mmap` | 同名不同构 | POSIX 同名系统调用。目标用 `MMapFlags::from_bits` 解析标志，参考用 `argint`，内部实现路径无重合。 |
| 内存管理 | `kernelx/src/kernel/mm/maparea/filemap/private.rs#fork` | `kernel/proc.c#fork` | 同名不同构 | POSIX 同名系统调用。目标处理 COW 页表，参考处理进程结构体复制，调用链完全不同。 |
| 文件系统 | `kernelx/src/fs/memtreefs/inode.rs#readlink` | `fs/vfs/fs.c#readlink` | 同名不同构 | POSIX 同名系统调用。目标操作内存文件系统，参考调用 `ext4_readlink`，实现无关。 |

### 4. 核心代码调用流对比剖析
#### 【进程管理 / `exit` 系统调用路径】
- **底层技术析理**: 这是控方指控的核心。参考项目采用经典 C 宏内核模式：手动获取进程锁（`acquire`），显式关闭所有文件（`fileclose`），调用 `reparent` 将子进程过继给 `init`，最后调用 `sched` 让出 CPU。目标项目则呈现现代 Rust 内核的 RAII 风格：通过 `Arc::strong_count` 和 `drop` 管理资源生命周期，调用 `self.addrspace.cleanup` 清理地址空间，父子关系通过 `self.parent.exit` 和 `c.parent.lock` 维护，调度由异步运行时隐式处理。两者在**同步原语、资源管理模型、调度方式**上存在根本性架构差异，无 AST 结构重合。
- **代码溯源链接**:
  - 目标项目: `kernelx/src/kernel/task/tcb.rs#exit`
  - 参考项目: `kernel/proc.c#exit`

#### 【内存管理 / `mmap` 系统调用路径】
- **底层技术析理**: 双方均实现 `mmap`，但抽象层次完全不同。参考项目直接操作页表（`PGROUNDDOWN`），使用 `filedup` 增加文件引用。目标项目则构建了多层抽象：`MMapFlags::from_bits` 进行标志解析，`MMapProt::from_bits` 处理保护模式，最终通过 `Box::new` 创建管理对象。调用链中无任何重合的原子操作或内存分配模式。
- **代码溯源链接**:
  - 目标项目: `kernelx/src/kernel/syscall/mm.rs#mmap`
  - 参考项目: `kernel/proc.c#mmap`

#### 【系统调用 / 分发路径】
- **底层技术析理**: 参考项目通过函数指针表 `syscalls[num]` 直接调用，目标项目通过 `syscall::syscall` 进行分发，并包含 `should_restart_on_eintr` 等复杂逻辑。两者除了函数名 `syscall` 相同外，分发机制、错误处理、上下文保存（`tcb.push_ucontext_syscall_retreg` vs `myproc()`）均无共同点。
- **代码溯源链接**:
  - 目标项目: `kernelx/src/kernel/trap.rs#syscall`
  - 参考项目: `kernel/syscall/syscall.c#syscall`

### 5. 单方模块与降权项
- **网络协议栈**: 目标项目独有的完整模块，包含 `kernelx/src/net/` 下的 TCP/IP、DHCP、Netlink 等实现。参考项目完全缺失，是目标项目独立开发的强正向证据。
- **跨板块硬凑项**: 控方证据中，将 `net/socket/tcp.rs`、`driver/char/tty.rs`、`fs/procfs/` 等目标项目证据强行归入【进程管理】板块与参考项目对比。这些证据实际分属网络、驱动、文件系统模块，与参考项目的进程管理函数无任何可比性，**不构成同板块主证据**，已全部降权。
- **POSIX 标准接口名**: `exit`、`wait4`、`fork`、`mmap`、`sigprocmask` 等函数名是 POSIX/Linux 标准系统调用名，任何实现 Linux 兼容的内核都必须使用。同名不能作为抄袭证据，必须考察其内部 AST 结构。

### 6. 保守结论
本庭裁定，目标项目 `[2026-T2026103369910030-XX]` 与参考项目 `[2024-10183_BOOT-吉林大学]` 在底层架构上存在显著差异，应认定为**独立开发**。整体 8.92% 的 AST 相似度处于极低水平，且几乎全部由双方共用的开源第三方库 `lwext4` 和标准 C 函数贡献。在操作系统内核的核心板块，如进程管理、内存管理、系统调用等，双方共同 AST 指纹为零或接近于零，其调用图谱、同步策略、资源管理模型均呈现出 C 宏内核与 Rust 现代宏内核的本质区别。控方基于 POSIX 公共知识、语言惯用表达和跨板块硬凑的“语义复用”指控，缺乏客观 AST 结构证据支持，不予采信。目标项目包含的自研网络协议栈、多架构支持等模块，进一步佐证了其独立开发的完整性。

------------------------------

