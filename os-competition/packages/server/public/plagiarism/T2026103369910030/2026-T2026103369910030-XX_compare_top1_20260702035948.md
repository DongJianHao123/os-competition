# 🛡️ OS 内核赛自动化代码级架构审计报告

**审计时间**: `2026-07-02 03:59:48`
**参赛作品名称**: `2026-T2026103369910030-XX`
**年份**: `2026`
**学校**: `XX`
**队伍名称**: `T2026103369910030`
**比赛名字**: `OS-Competition`
**比赛子赛道**: `Kernel-Implementation`
**仓库地址**: `https://gitlab.eduxiji.net/educg-group-43501-3132633/T2026103369910030-2019`
**推测开发语言**: `Rust / C (由AST探针自动识别)`
**特征函数量**: `5091`

## ⚔️ 底层代码同源性对比 (AST Homology)
> 本部分由专门的代码审查智能体，对该项目与符合年份条件的历届最高相似度项目进行控制流与底层调用对比。新版报告会先给出确定性的全模块覆盖矩阵，再给出 Agent 的分模块解释，避免只围绕少量抽样函数展开。

### 🏆 Top 1 底层对标: vs `2024-undefinedType-南京航空航天大学`（南京航空航天大学 / undefinedType）

#### 对方作品信息

**对方年份**: `2024`
**对方学校**: `南京航空航天大学`
**对方队伍名称**: `undefinedType`
**对方比赛名字**: `OS-Competition`
**对方比赛子赛道**: `Kernel-Implementation`
**对方仓库地址**: `https://gitlab.eduxiji.net/educg-group-26010-2376550/T202410287992755-2176`

> 双向覆盖率：共同 AST 指纹: 382；唯一 AST 指纹: 目标项目 4055，参考项目 398；覆盖率: 目标项目 9.42%，参考项目 95.98%

## 全模块对比总览（确定性证据）

> 总体覆盖率：共同 AST 指纹: 382；唯一 AST 指纹: 目标项目 4055，参考项目 398；覆盖率: 目标项目 9.42%，参考项目 95.98%

### 1. 模块覆盖矩阵
| 模块 | 目标函数数 | 参考函数数 | 共同唯一 AST 指纹 | Target 覆盖 | Base 覆盖 | 状态 |
| :--- | ---: | ---: | ---: | ---: | ---: | :--- |
| 进程管理 | 533 | 0 | 0 | 0.0% | 0.0% | 仅目标项目体现 |
| 任务调度/异步运行时 | 429 | 0 | 0 | 0.0% | 0.0% | 仅目标项目体现 |
| 内存管理 | 667 | 0 | 0 | 0.0% | 0.0% | 仅目标项目体现 |
| 中断/异常处理 | 57 | 0 | 0 | 0.0% | 0.0% | 仅目标项目体现 |
| 系统调用 | 154 | 0 | 0 | 0.0% | 0.0% | 仅目标项目体现 |
| 文件系统 | 1298 | 0 | 0 | 0.0% | 0.0% | 仅目标项目体现 |
| 设备驱动 | 288 | 0 | 0 | 0.0% | 0.0% | 仅目标项目体现 |
| 网络协议栈 | 278 | 0 | 0 | 0.0% | 0.0% | 仅目标项目体现 |
| 文件系统/第三方库 | 888 | 478 | 382 | 50.46% | 95.98% | 双方存在且有结构重合 |
| 通用模块/其他 | 499 | 0 | 0 | 0.0% | 0.0% | 仅目标项目体现 |

### 2. 分模块证据清单

#### 进程管理
仅目标项目中观察到该模块代表函数，参考项目没有同模块可对齐证据：
- 目标项目：`kernelx/src/net/socket/tcp.rs#process_segment`，调用摘要：Some, TCPPacket::parse, crate::kwarn!, flags.contains, payload.is_empty, pkt.ack_num ...(+10)
- 目标项目：`kernelx/src/kernel/syscall/task.rs#waitid`，调用摘要：ChildWaitOptions::new, ChildWaitOptions::new(!options.contains(WaitIdOptions::WNOHANG))
        .wait_parent_tid, ChildWaitOptions::new(!options.contains(WaitIdOptions::WNOHANG))
        .wait_parent_tid(wait_parent_tid)
        .wait_exited, ChildWaitOptions::new(!options.contains(WaitIdOptions::WNOHANG))
        .wait_parent_tid(wait_parent_tid)
        .wait_exited(wait_exited)
        .wait_stopped, ChildWaitOptions::new(!options.contains(WaitIdOptions::WNOHANG))
        .wait_parent_tid(wait_parent_tid)
        .wait_exited(wait_exited)
        .wait_stopped(wait_stopped)
        .wait_ptrace_stopped, ChildWaitOptions::new(!options.contains(WaitIdOptions::WNOHANG))
        .wait_parent_tid(wait_parent_tid)
        .wait_exited(wait_exited)
        .wait_stopped(wait_stopped)
        .wait_ptrace_stopped(wait_stopped)
        .wait_continued ...(+32)
- 目标项目：`kernelx/src/kernel/task/pcb/lifecycle.rs#exit`，调用摘要：KSiFields::SigChld, Some, State::Exited, c.parent.lock, c.set_wait_parent_tid, child.is_exited ...(+54)
- 目标项目：`kernelx/src/kernel/task/pcb/lifecycle.rs#new`，调用摘要：Arc::new, PendingSignalQueue::new, SleepLock::new, Some, SpinLock::new, TimerTable::new ...(+32)

#### 任务调度/异步运行时
仅目标项目中观察到该模块代表函数，参考项目没有同模块可对齐证据：
- 目标项目：`kernelx/src/kernel/syscall/event.rs#select`，调用摘要：Err, FileEvent::empty, Ok, Some, Vec::new, current::fdtable ...(+53)
- 目标项目：`kernelx/src/kernel/ipc/signal/handle.rs#handle_signal`，调用摘要：(*user_context).into, Into::<u32>::into, SigFrame::empty, SignalStack::from_state, Some, action.flags.contains ...(+47)
- 目标项目：`kernelx/src/fs/vfs/mount.rs#move_mount`，调用摘要：Arc::ptr_eq, Err, Ok, Self::check_bind_target_type, Self::join_mounts_to_group, Self::mount_chain_contains ...(+46)
- 目标项目：`kernelx/src/kernel/syscall/event.rs#do_poll`，调用摘要：Err, Ok, PollEventSet::empty, Some, cancel_all, current::fdtable ...(+42)

#### 内存管理
仅目标项目中观察到该模块代表函数，参考项目没有同模块可对齐证据：
- 目标项目：`kernelx/src/kernel/syscall/misc.rs#prlimit64`，调用摘要：Err, Ok, RLimitResource::try_from, RLimitResource::try_from(resource).map_err, crate::kwarn!, current::fdtable ...(+10)
- 目标项目：`kernelx/src/kernel/task/tcb.rs#new_exec`，调用摘要：(|| {
            let mut addrspace = AddrSpace::new();
            let exec_perm = Perm::current(PermFlags::X);
            let root = self.parent.root();
            let (user_entry, dyn_info) = elf::loader::load_elf(&root, &file, &mut addrspace, &exec_perm)?;

            let mut auxv = Auxv::new();
            if let Some(dyn_info) = dyn_info {
                auxv.push(AuxKey::BASE, dyn_info.interpreter_base);
                auxv.push(AuxKey::PHDR, dyn_info.phdr_addr);
                auxv.push(AuxKey::PHENT, dyn_info.phent as usize);
                auxv.push(AuxKey::PHNUM, dyn_info.phnum as usize);
                auxv.push(AuxKey::ENTRY, dyn_info.user_entry);
            }

            auxv.push(AuxKey::PAGESZ, arch::PGSIZE);
            auxv.push(AuxKey::RANDOM, config::USER_RANDOM_ADDR_BASE);

            let usetstack_top = addrspace.create_user_stack(argv, envp, &auxv)?;

            let mut new_user_context = UserContext::new();
            new_user_context.set_user_stack_top(usetstack_top);
            new_user_context.set_user_entry(user_entry);
            // Provide a non-zero initial tp so early TLS accesses in the
            // dynamic linker (tp + negative offset) land in the read-only
            // random page instead of faulting at kernel-range addresses.
            // The real tp is set later via set_tid_address / set_thread_area.
            // new_user_context.set_tls(config::USER_RANDOM_ADDR_BASE + arch::PGSIZE / 2);

            let fdtable = self.fdtable();
            fdtable.lock().cloexec();

            let new_tcb = TCB::new(self.tid, &self.parent, new_user_context, addrspace, fdtable);
            new_tcb.set_signal_mask(self.get_signal_mask());
            let tracer = self.tracer.lock().clone();
            *new_tcb.tracer.lock() = tracer;

            Ok((new_tcb, exec_path, exec_inode.clone()))
        }), AddrSpace::new, Auxv::new, FileFlags::readonly, Ok, Perm::current ...(+48)
- 目标项目：`kernelx/src/kernel/task/tcb.rs#new_inittask`，调用摘要：AddrSpace::new, Arc::new, Auxv::new, FDFlags::empty, FDTable::new, FileFlags::readonly ...(+51)
- 目标项目：`kernelx/src/fs/procfs/superblock.rs#get_inode`，调用摘要：Arc::new, Err, Ok, inode::TaskDirInode::from_ino, inode::TaskDirInode::from_ino(i).ok_or, inode::TaskExeInode::from_ino ...(+19)

#### 中断/异常处理
仅目标项目中观察到该模块代表函数，参考项目没有同模块可对齐证据：
- 目标项目：`kernelx/src/driver/pci.rs#from_fdt`，调用摘要：BTreeMap::new, chunk[0..4].try_into, chunk[0..4].try_into().unwrap, chunk[12..16].try_into, chunk[12..16].try_into().unwrap, chunk[20..24].try_into ...(+11)
- 目标项目：`kernelx/src/fs/devfs/inode/pty/inner.rs#read_buffer`，调用摘要：Err, Ok, buf.iter_mut, current::block, hung_up, ring.lock ...(+5)
- 目标项目：`kernelx/src/arch/loongarch/eiointc.rs#dump_irq`，调用摘要：crate::kinfo!, iocsr::iocsr_read_d
- 目标项目：`kernelx/src/driver/char/tty.rs#termios`，调用摘要：Termios::default, self.attr

#### 系统调用
仅目标项目中观察到该模块代表函数，参考项目没有同模块可对齐证据：
- 目标项目：`kernelx/src/kernel/syscall/ipc.rs#msgctl`，调用摘要：Err, IpcCtlFlags::IPC_64.bits, MsgCtlCmd::try_from, MsgCtlCmd::try_from(cmd & !IpcCtlFlags::IPC_64.bits()).map_err, Ok, UPtr::<UserMsgInfo>::from_uaddr ...(+14)
- 目标项目：`kernelx/src/kernel/syscall/event.rs#setitimer`，调用摘要：Box::new, Err, ITimerWhich::try_from, ITimerWhich::try_from(which).map_err, Ok, Some ...(+21)
- 目标项目：`kernelx/src/kernel/syscall/socket.rs#socketpair`，调用摘要：AddressFamily::try_from, AddressFamily::try_from(domain).map_err, Arc::new, Err, Ok, SocketKind::try_from ...(+9)
- 目标项目：`kernelx/src/kernel/syscall/ipc.rs#sigaltstack`，调用摘要：Err, Ok, SignalStackFlags::SS_DISABLE.bits, SignalStackFlags::empty, SignalStackFlags::from_bits, SignalStackFlags::from_bits(stack.ss_flags).ok_or ...(+9)

#### 文件系统
仅目标项目中观察到该模块代表函数，参考项目没有同模块可对齐证据：
- 目标项目：`kernelx/src/kernel/syscall/fs.rs#do_openat_with_lookup_flags`，调用摘要：Err, Mode::from_bits, Mode::from_bits(mode as u32 & 0o7777 & !current::umask()).ok_or, Ok, OpenFlags::O_RDWR.bits, OpenFlags::O_WRONLY.bits ...(+51)
- 目标项目：`kernelx/src/kernel/syscall/fs.rs#do_openat`，调用摘要：Err, Mode::from_bits, Mode::from_bits(mode as u32 & 0o7777 & !current::umask()).ok_or, Ok, OpenFlags::O_RDWR.bits, OpenFlags::O_WRONLY.bits ...(+52)
- 目标项目：`kernelx/src/kernel/syscall/fs.rs#fanotify_mark`，调用摘要：Err, FanotifyEventMask::from_bits, FanotifyFdinfoKey::new, FanotifyMarkFlags::from_bits, Ok, Some ...(+53)
- 目标项目：`kernelx/src/fs/memtreefs/inode.rs#rename`，调用摘要："..".into, Arc::ptr_eq, Err, Ok, T::check_filename, ancestor.downcast_ref::<Self> ...(+34)

#### 设备驱动
仅目标项目中观察到该模块代表函数，参考项目没有同模块可对齐证据：
- 目标项目：`kernelx/src/driver/char/tty.rs#ioctl`，调用摘要：IoctlReq::try_from, Ok, TtyIoctlResult::FlushInput, TtyIoctlResult::Handled, addrspace.copy_from_user::<Termios2>, addrspace.copy_from_user::<Termios> ...(+10)
- 目标项目：`kernelx/src/driver/virtio/matcher.rs#try_match`，调用摘要：Arc::new, MsixPciTransport::new, PciTransport::new::<VirtIOHal>, Some, VirtIOBlockDriver::new, device.device_function ...(+9)
- 目标项目：`kernelx/src/arch/loongarch/fdt.rs#load_device_tree`，调用摘要：Err, Fdt::new, Fdt::new(data).map_err, Ok, arch::paddr_to_kaddr, chosen.bootargs ...(+14)
- 目标项目：`kernelx/src/driver/char/serial/ns16550a.rs#try_match`，调用摘要：Arc::new, Box::new, Serial::new, Some, Stty::new, arch::enable_device_interrupt_irq ...(+18)

#### 网络协议栈
仅目标项目中观察到该模块代表函数，参考项目没有同模块可对齐证据：
- 目标项目：`kernelx/src/net/interface/dispatch.rs#on_receive`，调用摘要：EthernetFrame::parse, SocketAddr::new, eth.payload, flags.intersects, ip.header_len, ip.payload ...(+21)
- 目标项目：`kernelx/src/net/socket/netlink.rs#handle_getlink`，调用摘要：NlMsgFlags::MULTI.bits, Vec::new, as_bytes, attrs.len, core::mem::size_of::<IfInfoMsg>, flags.bits ...(+16)
- 目标项目：`kernelx/src/net/interface/dhcp.rs#dhcp_configure`，调用摘要：DHCPBuilder::new, DHCPBuilder::new()
                    .op, DHCPBuilder::new()
                    .op(DhcpOp::Request)
                    .xid, DHCPBuilder::new()
                    .op(DhcpOp::Request)
                    .xid(xid)
                    .chaddr, DHCPBuilder::new()
                    .op(DhcpOp::Request)
                    .xid(xid)
                    .chaddr(mac)
                    .message_type, DHCPBuilder::new()
                    .op(DhcpOp::Request)
                    .xid(xid)
                    .chaddr(mac)
                    .message_type(DhcpMessageType::Request)
                    .add_option ...(+15)
- 目标项目：`kernelx/src/net/protocol/ipv4.rs#build`，调用摘要：0u16.to_be_bytes, Ok, Self::compute_checksum, cksum.to_be_bytes, data.len, data[10..12].copy_from_slice ...(+15)

#### 文件系统/第三方库
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `kernelx/clib/lib/lwext4/lwext4/src/ext4_balloc.c#ext4_balloc_alloc_block` | `Kernel/File/lwext4/ext4_balloc.c#ext4_balloc_alloc_block` | 同板块同名同结构 | ext4_balloc_get_bgid_of_block, ext4_balloc_get_block_of_bgid, ext4_balloc_set_bitmap_csum, ext4_balloc_verify_bitmap_csum, ext4_bg_get_block_bitmap, ext4_bg_get_free_blocks_count ...(+19) | EXT4_Debug_u32, ext4_balloc_get_bgid_of_block, ext4_balloc_get_block_of_bgid, ext4_balloc_set_bitmap_csum, ext4_balloc_verify_bitmap_csum, ext4_bg_get_block_bitmap ...(+19) |
| `kernelx/clib/lib/lwext4/lwext4/src/ext4_journal.c#jbd_journal_prepare` | `Kernel/File/lwext4/ext4_journal.c#jbd_journal_prepare` | 同板块同名同结构 | EXT4_BLOCK_ZERO, RB_FIND, TAILQ_FOREACH_REVERSE_SAFE, TAILQ_FOREACH_SAFE, TAILQ_REMOVE, ext4_bcache_clear_dirty ...(+17) | EXT4_BLOCK_ZERO, RB_FIND, TAILQ_FOREACH_REVERSE_SAFE, TAILQ_FOREACH_SAFE, TAILQ_REMOVE, ext4_bcache_clear_dirty ...(+17) |
| `kernelx/clib/lib/lwext4/lwext4/src/ext4.c#ext4_dir_rm` | `Kernel/File/lwext4/ext4.c#ext4_dir_rm` | 同板块同名同结构 | EXT4_MP_LOCK, EXT4_MP_UNLOCK, ext4_block_cache_write_back, ext4_dir_en_get_inode, ext4_dir_iterator_fini, ext4_dir_iterator_init ...(+19) | EXT4_MP_LOCK, EXT4_MP_UNLOCK, ext4_block_cache_write_back, ext4_dir_en_get_inode, ext4_dir_iterator_fini, ext4_dir_iterator_init ...(+19) |
| `kernelx/clib/lib/lwext4/lwext4/src/ext4_dir_idx.c#ext4_dir_dx_split_index` | `Kernel/File/lwext4/ext4_dir_idx.c#ext4_dir_dx_split_index` | 同板块同名同结构 | ext4_block_set, ext4_dir_dx_climit_get_count, ext4_dir_dx_climit_get_limit, ext4_dir_dx_climit_set_count, ext4_dir_dx_climit_set_limit, ext4_dir_dx_entry_get_hash ...(+10) | ext4_block_set, ext4_dir_dx_climit_get_count, ext4_dir_dx_climit_get_limit, ext4_dir_dx_climit_set_count, ext4_dir_dx_climit_set_limit, ext4_dir_dx_entry_get_hash ...(+10) |

#### 通用模块/其他
仅目标项目中观察到该模块代表函数，参考项目没有同模块可对齐证据：
- 目标项目：`kernelx/src/kernel/ipc/sem/manager.rs#begin_semop`，调用摘要：Err, Ok, SemOpFlags::from_bits, SemOpFlags::from_bits(op.sem_flg as u16).ok_or, flags.contains, has_sem_perm ...(+18)
- 目标项目：`kernelx/src/klib/ksync/lockdep.rs#on_acquire`，调用摘要：LOCK_DEP_GRAPH.lock, can_reach, core::ptr::eq, crate::klib::backtrace::backtrace_chain, crate::println!, edges.iter ...(+12)
- 目标项目：`kernelx/src/kernel/ipc/msg/manager.rs#get_or_create`，调用摘要：Err, Ok, VecDeque::new, WaitQueue::new, flags.contains, has_msg_get_perm ...(+9)
- 目标项目：`kernelx/clib/lib/tlsf/tlsf.c#tlsf_check`，调用摘要：block_is_free, block_is_prev_free, block_next, block_size, mapping_insert, tlsf_cast ...(+1)


好的，我将遵循审计指令，以仲裁专家身份，基于提供的证据底座、控辩双方陈述及原始AST抽样证据，对本案进行最终定性分析。

### 1. 宏观判定
- **定性结论**: **独立开发 (第三方库重合)**
  目标项目与参考项目在整体架构上存在根本性差异。尽管在“文件系统/第三方库”模块存在高度重合，但该重合完全集中于双方共同引入的开源库 `lwext4`。目标项目其余 90.58% 的内核代码（进程、内存、调度、驱动、网络等）与参考项目零重合，且技术栈（Rust vs C）截然不同。因此，双方相似度由共同依赖的第三方库导致，不构成项目间的抄袭或复用。

### 2. 全模块覆盖矩阵解读
- **进程管理**: 仅目标项目存在。参考项目无此模块，共同 AST 指纹为 0。目标项目覆盖率 0%，参考项目覆盖率 0%。证据强度：**未形成**。
- **任务调度/异步运行时**: 仅目标项目存在。参考项目无此模块，共同 AST 指纹为 0。目标项目覆盖率 0%，参考项目覆盖率 0%。证据强度：**未形成**。
- **内存管理**: 仅目标项目存在。参考项目无此模块，共同 AST 指纹为 0。目标项目覆盖率 0%，参考项目覆盖率 0%。证据强度：**未形成**。
- **中断/异常处理**: 仅目标项目存在。参考项目无此模块，共同 AST 指纹为 0。目标项目覆盖率 0%，参考项目覆盖率 0%。证据强度：**未形成**。
- **系统调用**: 仅目标项目存在。参考项目无此模块，共同 AST 指纹为 0。目标项目覆盖率 0%，参考项目覆盖率 0%。证据强度：**未形成**。
- **文件系统**: 仅目标项目存在自研部分。参考项目无此模块，共同 AST 指纹为 0。目标项目覆盖率 0%，参考项目覆盖率 0%。证据强度：**未形成**。
- **设备驱动**: 仅目标项目存在。参考项目无此模块，共同 AST 指纹为 0。目标项目覆盖率 0%，参考项目覆盖率 0%。证据强度：**未形成**。
- **网络协议栈**: 仅目标项目存在。参考项目无此模块，共同 AST 指纹为 0。目标项目覆盖率 0%，参考项目覆盖率 0%。证据强度：**未形成**。
- **第三方库/公共依赖**: 双方均存在。共同 AST 指纹 382，目标项目覆盖率 50.46%，参考项目覆盖率 95.98%。该模块是双方唯一的相似度来源，且均为对开源库 `lwext4` 的引入。证据强度：**强（但指向共同上游依赖）**。
- **通用模块/其他**: 仅目标项目存在。参考项目无此模块，共同 AST 指纹为 0。目标项目覆盖率 0%，参考项目覆盖率 0%。证据强度：**未形成**。

### 3. 底层 AST 关键证据指纹表
| 所属板块 | 目标项目路径 | 参考项目路径 | 证据类型 | 技术说明 |
| :--- | :--- | :--- | :--- | :--- |
| **第三方库/公共依赖** | `kernelx/clib/lib/lwext4/lwext4/src/ext4_balloc.c#ext4_balloc_alloc_block` | `Kernel/File/lwext4/ext4_balloc.c#ext4_balloc_alloc_block` | 同源指纹 | 函数名、核心调用链（如 `ext4_balloc_get_bgid_of_block`）完全一致，均源自 `lwext4` 库。 |
| **第三方库/公共依赖** | `kernelx/clib/lib/lwext4/lwext4/src/ext4_journal.c#jbd_journal_prepare` | `Kernel/File/lwext4/ext4_journal.c#jbd_journal_prepare` | 同源指纹 | 红黑树（`RB_FIND`）与链表（`TAILQ_FOREACH_SAFE`）操作序列一致，为 `lwext4` 内部标准事务准备流程。 |
| **第三方库/公共依赖** | `kernelx/clib/lib/lwext4/lwext4/src/ext4_extent.c#ext4_extent_remove_space` | `Kernel/File/lwext4/ext4_extent.c#ext4_extent_remove_space` | 同源指纹 | `EXT_FIRST_EXTENT`, `EXT_LAST_EXTENT` 等宏的嵌套调用逻辑一致，是 `lwext4` 实现 ext4 规范的标准 extent 树操作。 |
| **第三方库/公共依赖** | `kernelx/clib/lib/lwext4/lwext4/src/ext4_mkfs.c#fill_sb` | `Kernel/File/lwext4/ext4_mkfs.c#fill_sb` | 同源指纹 | `ext4_sb_set_blocks_cnt` 后接 `log_2` 等数学计算，是 ext4 规范强制要求的超级块填充步骤。 |
| **第三方库/公共依赖** | `kernelx/clib/lib/lwext4/lwext4/src/ext4_dir_idx.c#ext4_dir_dx_init` | `Kernel/File/lwext4/ext4_dir_idx.c#ext4_dir_dx_init` | 同源指纹 | `ext4_dir_dx_climit_set_count` 等初始化序列一致，是 `lwext4` 中 HTree 索引的标准初始化流程。 |

### 4. 核心代码调用流对比剖析
* **【第三方库/公共依赖 - ext4 块分配器】**
* **底层技术析理**: 双方在 `ext4_balloc_alloc_block` 函数中，均遵循 `获取块组ID -> 获取块组内偏移 -> 校验位图校验和 -> 分配块 -> 设置位图校验和 -> 更新空闲块计数` 的调用流。此流程由 ext4 文件系统 on-disk 格式和 `lwext4` 库的内部 API 强制规定，是功能实现的必然路径，而非逻辑抄袭。
* **代码溯源链接**:
  - 目标项目: `kernelx/clib/lib/lwext4/lwext4/src/ext4_balloc.c#ext4_balloc_alloc_block`
  - 参考项目: `Kernel/File/lwext4/ext4_balloc.c#ext4_balloc_alloc_block`

* **【第三方库/公共依赖 - ext4 日志事务提交】**
* **底层技术析理**: 在 `__jbd_journal_commit_trans` 函数中，双方均使用 `RB_FOREACH_SAFE` 遍历红黑树管理的事务缓冲区，并使用 `TAILQ_INSERT_TAIL` 等宏操作链表。这是 `lwext4` 库利用 BSD 风格宏实现 JBD2 事务提交的标准模式，其数据结构操作序列由库的内部实现决定。
* **代码溯源链接**:
  - 目标项目: `kernelx/clib/lib/lwext4/lwext4/src/ext4_journal.c#__jbd_journal_commit_trans`
  - 参考项目: `Kernel/File/lwext4/ext4_journal.c#__jbd_journal_commit_trans`

* **【进程管理 - 进程退出】**
* **底层技术析理**: 目标项目使用 Rust 的 `Arc`、`SpinLock`、`SleepLock` 等高级抽象管理进程控制块（PCB）的生命周期和同步，调用流涉及 `State::Exited` 枚举、`PendingSignalQueue` 等自研结构。参考项目无此模块，无法对比。这体现了目标项目在核心架构上的独立设计与实现。
* **代码溯源链接**:
  - 目标项目: `kernelx/src/kernel/task/pcb/lifecycle.rs#exit`
  - 参考项目: 无

### 5. 单方模块与降权项
- **单方模块**: 进程管理、任务调度/异步运行时、内存管理、中断/异常处理、系统调用、文件系统（自研部分）、设备驱动、网络协议栈、通用模块/其他。这些模块仅在目标项目中存在，拥有 4055 个唯一 AST 指纹，是目标项目独立开发的铁证。
- **降权项**: 控方指控的“调试宏替换”（如 `EXT4_Debug_u32` 替换为 `ext4_dbg`）发生在第三方库 `lwext4` 内部，属于不同版本或移植配置间的正常差异，不构成“洗稿”证据。双方在 `lwext4` 库之外的所有跨模块同名函数（如 `ext4_fread` 调用 `ext4_fs_get_inode_ref`）均为 `lwext4` 库的内部 API 调用，已降级为弱信号，不作为项目间抄袭的证据。

### 6. 保守结论
本案中 9.38% 的 AST 相似度完全由双方共同引入的开源库 `lwext4` 贡献。该库的代码结构、函数命名与调用逻辑由 ext4 文件系统规范和库自身实现决定，属于公共技术依赖。目标项目其余超过 90% 的代码库，特别是构成操作系统核心的进程管理、内存管理、驱动及网络等模块，与参考项目在技术栈（Rust vs C）、抽象层次和实现上均无任何重合。因此，现有证据强力支持目标项目为独立开发，整体代码原创性风险极低。

------------------------------

