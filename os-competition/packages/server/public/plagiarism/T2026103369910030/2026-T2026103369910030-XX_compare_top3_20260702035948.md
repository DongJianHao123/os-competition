# 🛡️ OS 内核赛自动化代码级架构审计报告

**审计时间**: `2026-07-02 03:59:48`
**参赛作品名称**: `2026-T2026103369910030-XX`
**年份**: `2026`
**学校**: `XX`
**队伍名称**: `T2026103369910030`
**比赛名字**: `OS-Competition`
**比赛子赛道**: `Kernel-Implementation`
**仓库地址**: `https://gitlab.eduxiji.net/educg-group-43501-3132633/T2026103369910030-2019`
**推测开发语言**: `Rust / C (由AST探针自动识别)`
**特征函数量**: `5091`

## ⚔️ 底层代码同源性对比 (AST Homology)
> 本部分由专门的代码审查智能体，对该项目与符合年份条件的历届最高相似度项目进行控制流与底层调用对比。新版报告会先给出确定性的全模块覆盖矩阵，再给出 Agent 的分模块解释，避免只围绕少量抽样函数展开。

### 🏆 Top 3 底层对标: vs `2024-学到东西就算胜利队-南京航空航天大学`（南京航空航天大学 / 学到东西就算胜利队）

#### 对方作品信息

**对方年份**: `2024`
**对方学校**: `南京航空航天大学`
**对方队伍名称**: `学到东西就算胜利队`
**对方比赛名字**: `OS-Competition`
**对方比赛子赛道**: `Kernel-Implementation`
**对方仓库地址**: `local-upload://T202410287992637-1454`

> 双向覆盖率：共同 AST 指纹: 393；唯一 AST 指纹: 目标项目 4055，参考项目 721；覆盖率: 目标项目 9.69%，参考项目 54.51%

## 全模块对比总览（确定性证据）

> 总体覆盖率：共同 AST 指纹: 393；唯一 AST 指纹: 目标项目 4055，参考项目 721；覆盖率: 目标项目 9.69%，参考项目 54.51%

### 1. 模块覆盖矩阵
| 模块 | 目标函数数 | 参考函数数 | 共同唯一 AST 指纹 | Target 覆盖 | Base 覆盖 | 状态 |
| :--- | ---: | ---: | ---: | ---: | ---: | :--- |
| 进程管理 | 533 | 42 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |
| 任务调度/异步运行时 | 429 | 28 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |
| 内存管理 | 667 | 112 | 2 | 0.34% | 1.85% | 双方存在且有结构重合 |
| 中断/异常处理 | 57 | 18 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |
| 系统调用 | 154 | 16 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |
| 文件系统 | 1298 | 64 | 1 | 0.1% | 1.89% | 双方存在且有结构重合 |
| 设备驱动 | 288 | 15 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |
| 网络协议栈 | 278 | 18 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |
| 文件系统/第三方库 | 888 | 485 | 392 | 51.78% | 98.0% | 双方存在且有结构重合 |
| 通用模块/其他 | 499 | 58 | 1 | 0.22% | 1.85% | 双方存在且有结构重合 |

### 2. 分模块证据清单

#### 进程管理
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `kernelx/src/net/socket/tcp.rs#process_segment` | `kernel/sysproc.c#sys_prlimit64` | 同模块上下文对齐/差异观察 | Some, TCPPacket::parse, crate::kwarn!, flags.contains, payload.is_empty, pkt.ack_num ...(+10) | argaddr, argint, copyin2, copyout2, myproc, printf |
| `kernelx/src/kernel/syscall/task.rs#waitid` | `kernel/proc.c#waitpid` | 同模块上下文对齐/差异观察 | ChildWaitOptions::new, ChildWaitOptions::new(!options.contains(WaitIdOptions::WNOHANG))
        .wait_parent_tid, ChildWaitOptions::new(!options.contains(WaitIdOptions::WNOHANG))
        .wait_parent_tid(wait_parent_tid)
        .wait_exited, ChildWaitOptions::new(!options.contains(WaitIdOptions::WNOHANG))
        .wait_parent_tid(wait_parent_tid)
        .wait_exited(wait_exited)
        .wait_stopped, ChildWaitOptions::new(!options.contains(WaitIdOptions::WNOHANG))
        .wait_parent_tid(wait_parent_tid)
        .wait_exited(wait_exited)
        .wait_stopped(wait_stopped)
        .wait_ptrace_stopped, ChildWaitOptions::new(!options.contains(WaitIdOptions::WNOHANG))
        .wait_parent_tid(wait_parent_tid)
        .wait_exited(wait_exited)
        .wait_stopped(wait_stopped)
        .wait_ptrace_stopped(wait_stopped)
        .wait_continued ...(+32) | acquire, copyout2, freeproc, myproc, release, sleep |
| `kernelx/src/kernel/task/pcb/lifecycle.rs#exit` | `kernel/proc.c#wakeup2` | 同模块上下文对齐/差异观察 | KSiFields::SigChld, Some, State::Exited, c.parent.lock, c.set_wait_parent_tid, child.is_exited ...(+54) | acquire, release |
| `kernelx/src/kernel/task/pcb/lifecycle.rs#new` | `kernel/proc.c#reparent` | 同模块上下文对齐/差异观察 | Arc::new, PendingSignalQueue::new, SleepLock::new, Some, SpinLock::new, TimerTable::new ...(+32) | acquire, release |

#### 任务调度/异步运行时
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `kernelx/src/kernel/syscall/event.rs#select` | `kernel/sysfile.c#sys_pselect6` | 同模块上下文对齐/差异观察 | Err, FileEvent::empty, Ok, Some, Vec::new, current::fdtable ...(+53) | TEST_FD, argaddr, argint, copyin2, copyout2, memset ...(+2) |
| `kernelx/src/kernel/ipc/signal/handle.rs#handle_signal` | `kernel/syslog.c#sys_syslog` | 同模块上下文对齐/差异观察 | (*user_context).into, Into::<u32>::into, SigFrame::empty, SignalStack::from_state, Some, action.flags.contains ...(+47) | argint, argstr |
| `kernelx/src/fs/vfs/mount.rs#move_mount` | `kernel/sdcard.c#SDIO_WakeEvent` | 同模块上下文对齐/差异观察 | Arc::ptr_eq, Err, Ok, Self::check_bind_target_type, Self::join_mounts_to_group, Self::mount_chain_contains ...(+46) | printf |
| `kernelx/src/kernel/syscall/event.rs#do_poll` | `kernel/console.c#consoleintr` | 同模块上下文对齐/差异观察 | Err, Ok, PollEventSet::empty, Some, cancel_all, current::fdtable ...(+42) | C, acquire, consputc, release, wakeup |

#### 内存管理
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `kernelx/src/kernel/mm/maparea/filemap/private.rs#fork` | `kernel/proc.c#fork` | 同板块同名 | Box::new, FrameState::Cow, core::mem::replace, f.get_page, frame.clone, matches! ...(+11) | acquire, allocproc, alloctd, attachtd, filedup, freeproc ...(+4) |
| `kernelx/src/net/socket/udp.rs#sendto` | `kernel/socket.c#sendto` | 同板块同名 | Ok, Self::route_iface, SocketAddr::new, Some, buf.len, dst.or ...(+10) | MIN, TAILQ_INSERT_TAIL, acquire, copyin2, fd_socket_write, find_udp_remote_socket ...(+5) |
| `kernelx/src/kernel/mm/swappable/nofile/swap.rs#free` | `user/umalloc.c#free` | 同板块同名 | SWAPPER.free_slot, allocated.frame.get_page, self.state.lock, swapper::remove_lru | 内部闭环逻辑 |
| `kernelx/clib/src/klib/malloc.c#malloc` | `user/umalloc.c#malloc` | 同板块同名 | tlsf_malloc | morecore |

#### 中断/异常处理
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `kernelx/src/driver/pci.rs#from_fdt` | `kernel/sdcard.c#SD_IRQHandler` | 同模块上下文对齐/差异观察 | BTreeMap::new, chunk[0..4].try_into, chunk[0..4].try_into().unwrap, chunk[12..16].try_into, chunk[12..16].try_into().unwrap, chunk[20..24].try_into ...(+11) | sdioif->wake_evt |
| `kernelx/src/fs/devfs/inode/pty/inner.rs#read_buffer` | `kernel/trap.c#kerneltrap` | 同模块上下文对齐/差异观察 | Err, Ok, buf.iter_mut, current::block, hung_up, ring.lock ...(+5) | devintr, intr_get, mythread, panic, printf, r_scause ...(+7) |
| `kernelx/src/arch/loongarch/eiointc.rs#dump_irq` | `kernel/trap.c#trapframedump` | 同模块上下文对齐/差异观察 | crate::kinfo!, iocsr::iocsr_read_d | printf |
| `kernelx/src/driver/char/tty.rs#termios` | `kernel/plic.c#plicinithart` | 同模块上下文对齐/差异观察 | Termios::default, self.attr | PLIC_SENABLE, PLIC_SPRIORITY, printf |

#### 系统调用
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `kernelx/src/kernel/syscall/ipc.rs#msgctl` | `kernel/sysothers.c#sys_uname` | 同模块上下文对齐/差异观察 | Err, IpcCtlFlags::IPC_64.bits, MsgCtlCmd::try_from, MsgCtlCmd::try_from(cmd & !IpcCtlFlags::IPC_64.bits()).map_err, Ok, UPtr::<UserMsgInfo>::from_uaddr ...(+14) | argaddr, copyout2 |
| `kernelx/src/kernel/syscall/event.rs#setitimer` | `kernel/sysothers.c#sys_nanosleep` | 同模块上下文对齐/差异观察 | Box::new, Err, ITimerWhich::try_from, ITimerWhich::try_from(which).map_err, Ok, Some ...(+21) | acquire, argaddr, copyin2, mythread, release, sleep |
| `kernelx/src/kernel/syscall/socket.rs#socketpair` | `kernel/sysothers.c#sys_gettimeofday` | 同模块上下文对齐/差异观察 | AddressFamily::try_from, AddressFamily::try_from(domain).map_err, Arc::new, Err, Ok, SocketKind::try_from ...(+9) | argaddr, copyout2, r_time |
| `kernelx/src/kernel/syscall/ipc.rs#sigaltstack` | `kernel/sysothers.c#sys_secsleep` | 同模块上下文对齐/差异观察 | Err, Ok, SignalStackFlags::SS_DISABLE.bits, SignalStackFlags::empty, SignalStackFlags::from_bits, SignalStackFlags::from_bits(stack.ss_flags).ok_or ...(+9) | acquire, argint, mythread, r_time, release, sleep |

#### 文件系统
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `kernelx/src/fs/memtreefs/inode.rs#readlink` | `kernel/fs.c#readlink` | 同板块同名 | Ok, Some, buf.len, buf[..len].copy_from_slice, core::cmp::min, kclock::now ...(+4) | ext4_readlink |
| `kernelx/clib/lib/libfdt/fdt_sw.c#fdt_create` | `kernel/fs.c#fseek` | 同板块同结构 | fdt_create_with_flags | ext4_fseek |
| `kernelx/src/kernel/syscall/fs.rs#do_openat_with_lookup_flags` | `kernel/socket.c#fd_socket_write` | 同模块上下文对齐/差异观察 | Err, Mode::from_bits, Mode::from_bits(mode as u32 & 0o7777 & !current::umask()).ok_or, Ok, OpenFlags::O_RDWR.bits, OpenFlags::O_WRONLY.bits ...(+51) | (uint64), MIN, SOCK_IS_UDP, acquire, copyin2, printf ...(+6) |
| `kernelx/src/kernel/syscall/fs.rs#do_openat` | `kernel/fs.c#fkstatat` | 同模块上下文对齐/差异观察 | Err, Mode::from_bits, Mode::from_bits(mode as u32 & 0o7777 & !current::umask()).ok_or, Ok, OpenFlags::O_RDWR.bits, OpenFlags::O_WRONLY.bits ...(+52) | ext4_raw_inode_fill, is_relative, printf, strcmp, strncmp, to_abspath |

#### 设备驱动
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `kernelx/src/driver/char/tty.rs#ioctl` | `kernel/uart.c#uartinit` | 同模块上下文对齐/差异观察 | IoctlReq::try_from, Ok, TtyIoctlResult::FlushInput, TtyIoctlResult::Handled, addrspace.copy_from_user::<Termios2>, addrspace.copy_from_user::<Termios> ...(+10) | WriteReg, initlock |
| `kernelx/src/driver/virtio/matcher.rs#try_match` | `kernel/uart.c#uartstart` | 同模块上下文对齐/差异观察 | Arc::new, MsixPciTransport::new, PciTransport::new::<VirtIOHal>, Some, VirtIOBlockDriver::new, device.device_function ...(+9) | ReadReg, WriteReg, wakeup |
| `kernelx/src/arch/loongarch/fdt.rs#load_device_tree` | `kernel/console.c#consolewritev` | 同模块上下文对齐/差异观察 | Err, Fdt::new, Fdt::new(data).map_err, Ok, arch::paddr_to_kaddr, chosen.bootargs ...(+14) | acquire, either_copyin, release, sbi_console_putchar, uart8250_putc |
| `kernelx/src/driver/char/serial/ns16550a.rs#try_match` | `kernel/uart.c#uartputc` | 同模块上下文对齐/差异观察 | Arc::new, Box::new, Serial::new, Some, Stty::new, arch::enable_device_interrupt_irq ...(+18) | acquire, release, sleep, uartstart |

#### 网络协议栈
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `kernelx/src/net/socket/tcp.rs#shutdown` | `kernel/socket.c#shutdown` | 同板块同名 | Ok, self.send_segment, self.snd_nxt.wrapping_add | acquire, release, remote_find_peer_socket, wakeup |
| `kernelx/src/net/socket/inet.rs#getsockopt` | `kernel/socket.c#getsockopt` | 同板块同名 | Ok, SocketOption::try_from, matches!, self.options.lock, self.socket_type, usize::from | copyout2 |
| `kernelx/src/net/socket/inet.rs#setsockopt` | `kernel/socket.c#setsockopt` | 同板块同名 | Ok, SocketOption::try_from, self.options.lock, value.max | 内部闭环逻辑 |
| `kernelx/src/net/socket/raw.rs#bind` | `kernel/socket.c#bind` | 同板块同名 | Err, Ok, SocketAddr::new, Some, addr.ip.is_unspecified, iface.bind_raw ...(+5) | acquire, memmove, release |

#### 文件系统/第三方库
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `kernelx/clib/lib/lwext4/lwext4/src/ext4_balloc.c#ext4_balloc_alloc_block` | `kernel/lwext4/ext4_balloc.c#ext4_balloc_alloc_block` | 同板块同名同结构 | ext4_balloc_get_bgid_of_block, ext4_balloc_get_block_of_bgid, ext4_balloc_set_bitmap_csum, ext4_balloc_verify_bitmap_csum, ext4_bg_get_block_bitmap, ext4_bg_get_free_blocks_count ...(+19) | ext4_balloc_get_bgid_of_block, ext4_balloc_get_block_of_bgid, ext4_balloc_set_bitmap_csum, ext4_balloc_verify_bitmap_csum, ext4_bg_get_block_bitmap, ext4_bg_get_free_blocks_count ...(+19) |
| `kernelx/clib/lib/lwext4/lwext4/src/ext4_journal.c#jbd_journal_prepare` | `kernel/lwext4/ext4_journal.c#jbd_journal_prepare` | 同板块同名同结构 | EXT4_BLOCK_ZERO, RB_FIND, TAILQ_FOREACH_REVERSE_SAFE, TAILQ_FOREACH_SAFE, TAILQ_REMOVE, ext4_bcache_clear_dirty ...(+17) | EXT4_BLOCK_ZERO, RB_FIND, TAILQ_FOREACH_REVERSE_SAFE, TAILQ_FOREACH_SAFE, TAILQ_REMOVE, ext4_bcache_clear_dirty ...(+17) |
| `kernelx/clib/lib/lwext4/lwext4/src/ext4.c#ext4_dir_rm` | `kernel/lwext4/ext4.c#ext4_dir_rm` | 同板块同名同结构 | EXT4_MP_LOCK, EXT4_MP_UNLOCK, ext4_block_cache_write_back, ext4_dir_en_get_inode, ext4_dir_iterator_fini, ext4_dir_iterator_init ...(+19) | EXT4_MP_LOCK, EXT4_MP_UNLOCK, ext4_block_cache_write_back, ext4_dir_en_get_inode, ext4_dir_iterator_fini, ext4_dir_iterator_init ...(+19) |
| `kernelx/clib/lib/lwext4/lwext4/src/ext4_dir_idx.c#ext4_dir_dx_split_index` | `kernel/lwext4/ext4_dir_idx.c#ext4_dir_dx_split_index` | 同板块同名同结构 | ext4_block_set, ext4_dir_dx_climit_get_count, ext4_dir_dx_climit_get_limit, ext4_dir_dx_climit_set_count, ext4_dir_dx_climit_set_limit, ext4_dir_dx_entry_get_hash ...(+10) | ext4_block_set, ext4_dir_dx_climit_get_count, ext4_dir_dx_climit_get_limit, ext4_dir_dx_climit_set_count, ext4_dir_dx_climit_set_limit, ext4_dir_dx_entry_get_hash ...(+10) |

#### 通用模块/其他
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `kernelx/src/klib/ksync/lockdep.rs#release` | `kernel/spinlock.c#release` | 同板块同名 | core::ptr::eq, held.iter, held.iter().rposition, held.remove, panic!, self.held.get | __sync_lock_release, __sync_synchronize, holding, panic, pop_off |
| `kernelx/clib/src/klib/string.c#strncmp` | `kernel/string.c#strncmp` | 同板块同名 | 内部闭环逻辑 | 内部闭环逻辑 |
| `kernelx/clib/src/klib/string.c#strcmp` | `kernel/string.c#strcmp` | 同板块同名 | 内部闭环逻辑 | 内部闭环逻辑 |
| `kernelx/clib/src/klib/string.c#strcpy` | `kernel/string.c#strcpy` | 同板块同名 | 内部闭环逻辑 | 内部闭环逻辑 |


#### 1. 宏观判定
- **定性结论**: 底层架构显著差异 / 独立开发 (相似度: 8.97%，共同 AST 指纹: 393；唯一 AST 指纹: 目标项目 4055，参考项目 721；覆盖率: 目标项目 9.69%，参考项目 54.51%)

#### 2. 全模块覆盖矩阵解读
- **进程管理**: 双方均存在该模块。共同 AST 指纹为 0，目标覆盖率 0.0%，参考覆盖率 0.0%。该模块证据强度为**未形成**。尽管双方都实现了进程等待、退出、信号等机制，但 AST 层面无任何结构重合，表明实现路径完全不同。
- **任务调度/异步运行时**: 双方均存在该模块。共同 AST 指纹为 0，目标覆盖率 0.0%，参考覆盖率 0.0%。该模块证据强度为**未形成**。目标项目采用基于 `WaitQueue` 和 `FileEvent` 的异步阻塞模型，参考项目使用基于 `sleep`/`sched` 的自旋锁调度原语，无结构同源性。
- **内存管理**: 双方均存在该模块。共同 AST 指纹为 2，目标覆盖率 0.34%，参考覆盖率 1.85%。该模块证据强度为**弱**。重合指纹仅来自 `fork` 和 `free` 等通用概念的同名函数，其内部调用图谱（如目标项目的 `FrameState::Cow` 与参考项目的 `allocproc`）截然不同。
- **中断/异常处理**: 双方均存在该模块。共同 AST 指纹为 0，目标覆盖率 0.0%，参考覆盖率 0.0%。该模块证据强度为**未形成**。目标项目包含 LoongArch 架构特定的中断控制器驱动，参考项目为 RISC-V 的 PLIC 处理，硬件抽象层完全不同。
- **系统调用**: 双方均存在该模块。共同 AST 指纹为 0，目标覆盖率 0.0%，参考覆盖率 0.0%。该模块证据强度为**未形成**。目标项目实现了 `futex`、`epoll`、`process_vm` 等复杂系统调用，参考项目仅包含基础的 `sleep`、`exit` 等，功能集和实现结构无重合。
- **文件系统**: 双方均存在该模块。共同 AST 指纹为 1，目标覆盖率 0.1%，参考覆盖率 1.89%。该模块证据强度为**弱**。重合点仅为 `readlink` 这一 POSIX 标准接口，且目标项目实现于 `memtreefs`，参考项目直接调用 `ext4_readlink`，上下文完全不同。
- **设备驱动**: 双方均存在该模块。共同 AST 指纹为 0，目标覆盖率 0.0%，参考覆盖率 0.0%。该模块证据强度为**未形成**。目标项目包含 VirtIO、PCI、NS16550A 等复杂驱动框架，参考项目仅有基础的 UART 和 SD 卡驱动，无结构重合。
- **网络协议栈**: 双方均存在该模块。共同 AST 指纹为 0，目标覆盖率 0.0%，参考覆盖率 0.0%。该模块证据强度为**未形成**。目标项目实现了完整的 TCP 状态机、乱序段合并等，参考项目仅有基础的 UDP socket 包装，无结构重合。
- **第三方库/公共依赖**: 双方均存在该模块。共同 AST 指纹为 392，目标覆盖率 51.78%，参考覆盖率 98.0%。该模块证据强度为**强**，但性质为**第三方库引用**。所有重合指纹均来自 `lwext4` 文件系统库和标准 C 字符串函数，属于公共依赖，不构成原创代码复用。

#### 3. 底层 AST 关键证据指纹表
| 所属板块 | 目标项目路径 | 参考项目路径 | 证据类型 | 技术说明 |
| :--- | :--- | :--- | :--- | :--- |
| 第三方库/公共依赖 | kernelx/clib/lib/lwext4/lwext4/src/ext4_balloc.c#ext4_balloc_alloc_block | kernel/lwext4/ext4_balloc.c#ext4_balloc_alloc_block | 同源文件/同函数 | 双方均完整引用了开源库 lwext4 的块分配函数，AST 指纹完全一致，属于第三方公共代码。 |
| 第三方库/公共依赖 | kernelx/clib/lib/lwext4/lwext4/src/ext4_journal.c#jbd_journal_prepare | kernel/lwext4/ext4_journal.c#jbd_journal_prepare | 同源文件/同函数 | 双方均完整引用了开源库 lwext4 的日志准备函数，AST 指纹完全一致，属于第三方公共代码。 |
| 第三方库/公共依赖 | kernelx/clib/lib/lwext4/lwext4/src/ext4.c#ext4_dir_rm | kernel/lwext4/ext4.c#ext4_dir_rm | 同源文件/同函数 | 双方均完整引用了开源库 lwext4 的目录删除函数，AST 指纹完全一致，属于第三方公共代码。 |
| 第三方库/公共依赖 | kernelx/clib/lib/lwext4/lwext4/src/ext4_dir_idx.c#ext4_dir_dx_split_index | kernel/lwext4/ext4_dir_idx.c#ext4_dir_dx_split_index | 同源文件/同函数 | 双方均完整引用了开源库 lwext4 的目录索引分裂函数，AST 指纹完全一致，属于第三方公共代码。 |
| 第三方库/公共依赖 | kernelx/clib/src/klib/string.c#strncmp | kernel/string.c#strncmp | 同源文件/同函数 | 双方均实现了标准 C 库的字符串比较函数，属于公共标准算法。 |
| 第三方库/公共依赖 | kernelx/clib/src/klib/string.c#strcmp | kernel/string.c#strcmp | 同源文件/同函数 | 双方均实现了标准 C 库的字符串比较函数，属于公共标准算法。 |
| 第三方库/公共依赖 | kernelx/clib/src/klib/string.c#strcpy | kernel/string.c#strcpy | 同源文件/同函数 | 双方均实现了标准 C 库的字符串拷贝函数，属于公共标准算法。 |
| 内存管理 | kernelx/src/kernel/mm/maparea/filemap/private.rs#fork | kernel/proc.c#fork | 同名函数/不同结构 | 同名但调用图谱完全不同。目标项目使用 `FrameState::Cow` 和 `Box::new`，参考项目使用 `allocproc` 和 `filedup`，内存模型异构。 |
| 内存管理 | kernelx/src/kernel/mm/swappable/nofile/swap.rs#free | user/umalloc.c#free | 同名函数/不同结构 | 同名但实现完全不同。目标项目涉及 `SWAPPER.free_slot` 和 LRU 链表，参考项目为简单的用户态内存释放。 |
| 文件系统 | kernelx/src/fs/memtreefs/inode.rs#readlink | kernel/fs.c#readlink | 同名函数/不同结构 | 同名但实现上下文不同。目标项目在内存文件系统中实现，参考项目直接封装 ext4 的 `readlink`。 |

#### 4. 核心代码调用流对比剖析
* **【进程管理/进程等待与回收】**
* **底层技术析理**: 目标项目 `waitid` 采用 Rust 的 Builder 模式构建等待选项，通过 `ChildWaitOptions` 链式调用配置 `wait_exited`、`wait_stopped` 等状态，最终调用 `consume` 消费结果。其底层依赖 `Arc` 引用计数的 PCB 管理和 `WaitQueue` 同步原语。参考项目 `waitpid` 则采用 C 语言的过程式调用，通过 `acquire` 获取自旋锁保护进程表，循环检查子进程状态后调用 `freeproc` 直接释放进程结构，最后 `release` 解锁。两者在内存模型（引用计数 vs 直接释放）、同步策略（等待队列 vs 自旋锁）和抽象层次（Builder 模式 vs 过程式循环）上存在根本性架构差异。
* **代码溯源链接**:
  - 目标项目: `kernelx/src/kernel/syscall/task.rs#waitid`
  - 参考项目: `kernel/proc.c#waitpid`

* **【内存管理/进程地址空间复制】**
* **底层技术析理**: 目标项目 `fork` 函数位于 `filemap/private.rs`，专门处理文件映射的写时复制 (Copy-on-Write)。其调用流为 `f.get_page` -> `FrameState::Cow` -> `Box::new`，体现了基于页帧状态机和堆分配的对象管理。参考项目 `fork` 函数在 `proc.c` 中，负责整个进程结构的复制，调用流为 `allocproc` -> `alloctd` -> `filedup`，体现了基于结构体分配和文件表指针复制的传统 Unix 风格。两者虽同名，但一个聚焦于内存映射子系统的 CoW 策略，一个负责进程结构的整体复制，职责范围和实现技术截然不同。
* **代码溯源链接**:
  - 目标项目: `kernelx/src/kernel/mm/maparea/filemap/private.rs#fork`
  - 参考项目: `kernel/proc.c#fork`

* **【系统调用/信号掩码管理】**
* **底层技术析理**: 目标项目 `rt_sigprocmask` 通过 `SigProcmaskHow::try_from(how).map_err` 进行类型安全的命令解析，然后获取 `current::tcb().signal_mask.lock` 操作线程控制块中的信号掩码字段，最后通过 `uptr_oldset.write` 写回用户空间。参考项目 `sigprocmask` 直接通过 `mythread` 宏获取当前线程指针并修改其信号掩码，异常处理仅调用 `panic`。目标项目展现了 Rust 内核典型的错误处理传播（`Result` 类型）和细粒度锁保护，而参考项目是传统的宏式上下文获取和简单错误处理，两者在错误处理模型和同步粒度上存在显著差异。
* **代码溯源链接**:
  - 目标项目: `kernelx/src/kernel/syscall/ipc.rs#rt_sigprocmask`
  - 参考项目: `kernel/signal.c#sigprocmask`

#### 5. 单方模块与降权项
- **目标项目独有模块**: `procfs` 虚拟文件系统（`kernelx/src/fs/procfs/`）、`futex` 系统调用（`kernelx/src/kernel/syscall/futex.rs`）、完整的 TCP/IP 协议栈（`kernelx/src/net/socket/tcp.rs`）、VirtIO 驱动框架（`kernelx/src/driver/virtio/`）。这些模块在参考项目中无任何对应实现，是目标项目独立开发的强有力证据。
- **参考项目独有模块**: 简单的 SD 卡驱动等待（`kernel/sdcard.c#SDIO_WaitEvent`）、RISC-V 架构特定的 PLIC 中断控制器初始化（`kernel/plic.c`）。这些模块在目标项目中无对应实现。
- **降权项说明**: 所有同名函数（如 `fork`、`exit`、`bind`、`sendto`）均为 POSIX/Linux 标准 ABI 或操作系统通用概念，其存在本身不能作为复用证据。控方将目标项目的 TCP 处理函数（`process_segment`）与参考项目的资源限制系统调用（`sys_prlimit64`）进行跨板块配对，属于无效证据，已予排除。

#### 6. 保守结论
本次审计的客观 AST 相似度仅为 8.97%，且几乎所有共同指纹（392/393）均源于双方引用的同一开源文件系统库 `lwext4` 及标准 C 字符串函数。在操作系统核心子系统（进程管理、调度、内存管理、系统调用、驱动、网络）中，双方共同 AST 指纹为零或接近零，表明不存在任何可量化的代码结构复用。目标项目作为 Rust 语言实现的宏内核，在内存模型（基于 `Arc` 和 `Box` 的引用计数与所有权）、同步原语（`SpinLock`/`SleepLock`/`WaitQueue`）、错误处理（`Result` 传播）和驱动框架（VirtIO/PCI）等方面，与参考项目的 C 语言内核存在根本性架构差异。尽管双方均实现了 POSIX 兼容接口，但这属于操作系统领域的规范共性，而非代码复用。综合判定，目标项目为独立开发，无同源性违规风险。

------------------------------

