# 🛡️ OS 内核赛自动化代码级架构审计报告

**审计时间**: `2026-07-02 03:59:48`
**参赛作品名称**: `2026-T2026103369910030-XX`
**年份**: `2026`
**学校**: `XX`
**队伍名称**: `T2026103369910030`
**比赛名字**: `OS-Competition`
**比赛子赛道**: `Kernel-Implementation`
**仓库地址**: `https://gitlab.eduxiji.net/educg-group-43501-3132633/T2026103369910030-2019`
**推测开发语言**: `Rust / C (由AST探针自动识别)`
**特征函数量**: `5091`

## ⚔️ 底层代码同源性对比 (AST Homology)
> 本部分由专门的代码审查智能体，对该项目与符合年份条件的历届最高相似度项目进行控制流与底层调用对比。新版报告会先给出确定性的全模块覆盖矩阵，再给出 Agent 的分模块解释，避免只围绕少量抽样函数展开。

### 🏆 Top 5 底层对标: vs `2024-蕾姆赛高队-扬州大学`（扬州大学 / 蕾姆赛高队）

#### 对方作品信息

**对方年份**: `2024`
**对方学校**: `扬州大学`
**对方队伍名称**: `蕾姆赛高队`
**对方比赛名字**: `OS-Competition`
**对方比赛子赛道**: `Kernel-Implementation`
**对方仓库地址**: `https://gitlab.eduxiji.net/educg-group-26010-2376550/T202411117993110-198`

> 双向覆盖率：共同 AST 指纹: 394；唯一 AST 指纹: 目标项目 4055，参考项目 754；覆盖率: 目标项目 9.72%，参考项目 52.25%

## 全模块对比总览（确定性证据）

> 总体覆盖率：共同 AST 指纹: 394；唯一 AST 指纹: 目标项目 4055，参考项目 754；覆盖率: 目标项目 9.72%，参考项目 52.25%

### 1. 模块覆盖矩阵
| 模块 | 目标函数数 | 参考函数数 | 共同唯一 AST 指纹 | Target 覆盖 | Base 覆盖 | 状态 |
| :--- | ---: | ---: | ---: | ---: | ---: | :--- |
| 进程管理 | 533 | 45 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |
| 任务调度/异步运行时 | 429 | 43 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |
| 内存管理 | 667 | 125 | 2 | 0.34% | 1.68% | 双方存在且有结构重合 |
| 中断/异常处理 | 57 | 17 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |
| 系统调用 | 154 | 6 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |
| 文件系统 | 1298 | 68 | 2 | 0.2% | 3.57% | 双方存在且有结构重合 |
| 设备驱动 | 288 | 43 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |
| 网络协议栈 | 278 | 0 | 0 | 0.0% | 0.0% | 仅目标项目体现 |
| 文件系统/第三方库 | 888 | 503 | 393 | 51.92% | 94.93% | 双方存在且有结构重合 |
| 通用模块/其他 | 499 | 51 | 1 | 0.22% | 2.44% | 双方存在且有结构重合 |

### 2. 分模块证据清单

#### 进程管理
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `kernelx/src/kernel/event/fanotify/file/permission.rs#wait` | `src/proc/proc.c#wait` | 同板块同名 | Err, current::schedule, current::task, current::task().take_wakeup_event, current::task().take_wakeup_event().unwrap, drop ...(+5) | acquire, copyout2, freeproc, myproc, release, sleep |
| `kernelx/src/kernel/syscall/task.rs#exit_group` | `src/proc/proc.c#exit_group` | 同板块同名 | ExitStatus::Normal, Ok, current::pcb, current::tcb, current::tcb().wake_parent_waiting_vfork, pcb.exit | exit |
| `kernelx/src/kernel/syscall/task.rs#waitid` | `src/proc/proc.c#waitpid` | 同模块上下文对齐/差异观察 | ChildWaitOptions::new, ChildWaitOptions::new(!options.contains(WaitIdOptions::WNOHANG))
        .wait_parent_tid, ChildWaitOptions::new(!options.contains(WaitIdOptions::WNOHANG))
        .wait_parent_tid(wait_parent_tid)
        .wait_exited, ChildWaitOptions::new(!options.contains(WaitIdOptions::WNOHANG))
        .wait_parent_tid(wait_parent_tid)
        .wait_exited(wait_exited)
        .wait_stopped, ChildWaitOptions::new(!options.contains(WaitIdOptions::WNOHANG))
        .wait_parent_tid(wait_parent_tid)
        .wait_exited(wait_exited)
        .wait_stopped(wait_stopped)
        .wait_ptrace_stopped, ChildWaitOptions::new(!options.contains(WaitIdOptions::WNOHANG))
        .wait_parent_tid(wait_parent_tid)
        .wait_exited(wait_exited)
        .wait_stopped(wait_stopped)
        .wait_ptrace_stopped(wait_stopped)
        .wait_continued ...(+32) | acquire, copyout2, freeproc, myproc, release, sleep |
| `kernelx/src/kernel/task/pcb/lifecycle.rs#exit` | `src/kernel/signal.c#sigprocmask` | 同模块上下文对齐/差异观察 | KSiFields::SigChld, Some, State::Exited, c.parent.lock, c.set_wait_parent_tid, child.is_exited ...(+54) | myproc |

#### 任务调度/异步运行时
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `kernelx/src/kernel/trap.rs#syscall` | `src/kernel/syscall.c#syscall` | 同板块同名 | Err, Some, current::tcb, syscall::should_restart_on_eintr, syscall::syscall, tcb.push_ucontext_syscall_retreg ...(+2) | NELEM, myproc, printf, syscalls[num] |
| `kernelx/src/kernel/syscall/event.rs#select` | `src/kernel/sysctl.c#sysctl_clock_device_en` | 同模块上下文对齐/差异观察 | Err, FileEvent::empty, Ok, Some, Vec::new, current::fdtable ...(+53) | 内部闭环逻辑 |
| `kernelx/src/kernel/ipc/signal/handle.rs#handle_signal` | `src/driver/spi.c#spi_receive_data_standard` | 同模块上下文对齐/差异观察 | (*user_context).into, Into::<u32>::into, SigFrame::empty, SignalStack::from_state, Some, action.flags.contains ...(+47) | (uint32), spi_get_frame_size, spi_set_tmod |
| `kernelx/src/fs/vfs/mount.rs#move_mount` | `src/driver/spi.c#spi_send_data_normal` | 同模块上下文对齐/差异观察 | Arc::ptr_eq, Err, Ok, Self::check_bind_target_type, Self::join_mounts_to_group, Self::mount_chain_contains ...(+46) | memmove, spi_get_frame_size, spi_set_tmod |

#### 内存管理
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `kernelx/src/kernel/mm/maparea/filemap/private.rs#fork` | `src/proc/proc.c#fork` | 同板块同名 | Box::new, FrameState::Cow, core::mem::replace, f.get_page, frame.clone, matches! ...(+11) | acquire, allocproc, filedup, freeproc, myproc, release ...(+2) |
| `kernelx/src/kernel/mm/swappable/nofile/swap.rs#free` | `user/umalloc.c#free` | 同板块同名 | SWAPPER.free_slot, allocated.frame.get_page, self.state.lock, swapper::remove_lru | 内部闭环逻辑 |
| `kernelx/clib/src/klib/malloc.c#malloc` | `user/umalloc.c#malloc` | 同板块同名 | tlsf_malloc | morecore |
| `kernelx/clib/src/klib/malloc.c#free` | `src/mm/dmac.c#dmac_wait_done` | 同板块同结构 | tlsf_free | dmac_wait_idle |

#### 中断/异常处理
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `kernelx/src/driver/pci.rs#from_fdt` | `src/kernel/trap.c#trapframedump` | 同模块上下文对齐/差异观察 | BTreeMap::new, chunk[0..4].try_into, chunk[0..4].try_into().unwrap, chunk[12..16].try_into, chunk[12..16].try_into().unwrap, chunk[20..24].try_into ...(+11) | printf |
| `kernelx/src/fs/devfs/inode/pty/inner.rs#read_buffer` | `src/kernel/plic.c#plicinithart` | 同模块上下文对齐/差异观察 | Err, Ok, buf.iter_mut, current::block, hung_up, ring.lock ...(+5) | PLIC_MENABLE, PLIC_SENABLE, PLIC_SPRIORITY, cpuid, printf, readd |
| `kernelx/src/arch/loongarch/eiointc.rs#dump_irq` | `src/mm/dmac.c#dmac_enable_common_interrupt_signal` | 同模块上下文对齐/差异观察 | crate::kinfo!, iocsr::iocsr_read_d | readq, writeq |
| `kernelx/src/driver/char/tty.rs#termios` | `src/mm/dmac.c#dmac_enable_common_interrupt_status` | 同模块上下文对齐/差异观察 | Termios::default, self.attr | readq, writeq |

#### 系统调用
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `kernelx/src/kernel/syscall/ipc.rs#msgctl` | `src/kernel/sysothers.c#sys_clock_gettime` | 同模块上下文对齐/差异观察 | Err, IpcCtlFlags::IPC_64.bits, MsgCtlCmd::try_from, MsgCtlCmd::try_from(cmd & !IpcCtlFlags::IPC_64.bits()).map_err, Ok, UPtr::<UserMsgInfo>::from_uaddr ...(+14) | argaddr, argint, copyout2, r_time |
| `kernelx/src/kernel/syscall/event.rs#setitimer` | `src/kernel/sysothers.c#sys_gettimeofday` | 同模块上下文对齐/差异观察 | Box::new, Err, ITimerWhich::try_from, ITimerWhich::try_from(which).map_err, Ok, Some ...(+21) | argaddr, copyout2, r_time |
| `kernelx/src/kernel/syscall/socket.rs#socketpair` | `src/kernel/syscall.c#argstr` | 同模块上下文对齐/差异观察 | AddressFamily::try_from, AddressFamily::try_from(domain).map_err, Arc::new, Err, Ok, SocketKind::try_from ...(+9) | argaddr, fetchstr |
| `kernelx/src/kernel/syscall/ipc.rs#sigaltstack` | `src/kernel/sysmem.c#sys_munmap` | 同模块上下文对齐/差异观察 | Err, Ok, SignalStackFlags::SS_DISABLE.bits, SignalStackFlags::empty, SignalStackFlags::from_bits, SignalStackFlags::from_bits(stack.ss_flags).ok_or ...(+9) | argaddr, do_munmap |

#### 文件系统
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `kernelx/src/kernel/syscall/fs.rs#mount` | `src/fs/fat32/fat32.c#mount` | 同板块同名 | Err, MountFlags::from_bits_truncate, MountOptions::new, Ok, Some, allowed.contains ...(+29) | bread, brelse, initlock, initsleeplock, memmove, memset ...(+2) |
| `kernelx/src/kernel/syscall/fs.rs#umount2` | `src/fs/fat32/fat32.c#umount2` | 同板块同名 | Err, Ok, current::euid, current::with_root_cwd, uptr_target.read_path, vfs::unmount | memset |
| `kernelx/clib/src/klib/panic.c#kpanic` | `src/lib/utils.c#set_gpio_bit` | 同板块同结构 | rust_kpanic | set_bit_offset |
| `kernelx/clib/lib/libfdt/fdt_sw.c#fdt_create` | `src/lib/utils.c#get_gpio_bit` | 同板块同结构 | fdt_create_with_flags | get_bit |

#### 设备驱动
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `kernelx/src/driver/char/tty.rs#ioctl` | `src/driver/spi.c#spi_receive_data_normal_dma` | 同模块上下文对齐/差异观察 | IoctlReq::try_from, Ok, TtyIoctlResult::FlushInput, TtyIoctlResult::Handled, addrspace.copy_from_user::<Termios2>, addrspace.copy_from_user::<Termios> ...(+10) | (uint32), dmac_set_single_mode, dmac_wait_done, spi_get_frame_format, spi_set_tmod, sysctl_dma_select |
| `kernelx/src/driver/virtio/matcher.rs#try_match` | `src/driver/sdcard.c#sdcard_write_sector` | 同模块上下文对齐/差异观察 | Arc::new, MsixPciTransport::new, PciTransport::new::<VirtIOHal>, Some, VirtIOBlockDriver::new, device.device_function ...(+9) | acquiresleep, panic, printf, releasesleep, sd_end_cmd, sd_get_response_R1 ...(+4) |
| `kernelx/src/arch/loongarch/fdt.rs#load_device_tree` | `src/driver/fpioa.c#fpioa_set_function_raw` | 同模块上下文对齐/差异观察 | Err, Fdt::new, Fdt::new(data).map_err, Ok, arch::paddr_to_kaddr, chosen.bootargs ...(+14) | 内部闭环逻辑 |
| `kernelx/src/driver/char/serial/ns16550a.rs#try_match` | `src/driver/sdcard.c#check_block_size` | 同模块上下文对齐/差异观察 | Arc::new, Box::new, Serial::new, Some, Stty::new, arch::enable_device_interrupt_irq ...(+18) | printf, sd_end_cmd, sd_get_response_R1, sd_get_response_R3_rest, sd_send_cmd |

#### 网络协议栈
仅目标项目中观察到该模块代表函数，参考项目没有同模块可对齐证据：
- 目标项目：`kernelx/src/net/interface/dispatch.rs#on_receive`，调用摘要：EthernetFrame::parse, SocketAddr::new, eth.payload, flags.intersects, ip.header_len, ip.payload ...(+21)
- 目标项目：`kernelx/src/net/socket/netlink.rs#handle_getlink`，调用摘要：NlMsgFlags::MULTI.bits, Vec::new, as_bytes, attrs.len, core::mem::size_of::<IfInfoMsg>, flags.bits ...(+16)
- 目标项目：`kernelx/src/net/interface/dhcp.rs#dhcp_configure`，调用摘要：DHCPBuilder::new, DHCPBuilder::new()
                    .op, DHCPBuilder::new()
                    .op(DhcpOp::Request)
                    .xid, DHCPBuilder::new()
                    .op(DhcpOp::Request)
                    .xid(xid)
                    .chaddr, DHCPBuilder::new()
                    .op(DhcpOp::Request)
                    .xid(xid)
                    .chaddr(mac)
                    .message_type, DHCPBuilder::new()
                    .op(DhcpOp::Request)
                    .xid(xid)
                    .chaddr(mac)
                    .message_type(DhcpMessageType::Request)
                    .add_option ...(+15)
- 目标项目：`kernelx/src/net/protocol/ipv4.rs#build`，调用摘要：0u16.to_be_bytes, Ok, Self::compute_checksum, cksum.to_be_bytes, data.len, data[10..12].copy_from_slice ...(+15)

#### 文件系统/第三方库
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `kernelx/clib/lib/lwext4/lwext4/src/ext4_balloc.c#ext4_balloc_alloc_block` | `src/fs/ext4/ext4_balloc.c#ext4_balloc_alloc_block` | 同板块同名同结构 | ext4_balloc_get_bgid_of_block, ext4_balloc_get_block_of_bgid, ext4_balloc_set_bitmap_csum, ext4_balloc_verify_bitmap_csum, ext4_bg_get_block_bitmap, ext4_bg_get_free_blocks_count ...(+19) | ext4_balloc_get_bgid_of_block, ext4_balloc_get_block_of_bgid, ext4_balloc_set_bitmap_csum, ext4_balloc_verify_bitmap_csum, ext4_bg_get_block_bitmap, ext4_bg_get_free_blocks_count ...(+19) |
| `kernelx/clib/lib/lwext4/lwext4/src/ext4_journal.c#jbd_journal_prepare` | `src/fs/ext4/ext4_journal.c#jbd_journal_prepare` | 同板块同名同结构 | EXT4_BLOCK_ZERO, RB_FIND, TAILQ_FOREACH_REVERSE_SAFE, TAILQ_FOREACH_SAFE, TAILQ_REMOVE, ext4_bcache_clear_dirty ...(+17) | EXT4_BLOCK_ZERO, RB_FIND, TAILQ_FOREACH_REVERSE_SAFE, TAILQ_FOREACH_SAFE, TAILQ_REMOVE, ext4_bcache_clear_dirty ...(+17) |
| `kernelx/clib/lib/lwext4/lwext4/src/ext4.c#ext4_dir_rm` | `src/fs/ext4/ext4.c#ext4_dir_rm` | 同板块同名同结构 | EXT4_MP_LOCK, EXT4_MP_UNLOCK, ext4_block_cache_write_back, ext4_dir_en_get_inode, ext4_dir_iterator_fini, ext4_dir_iterator_init ...(+19) | EXT4_MP_LOCK, EXT4_MP_UNLOCK, ext4_block_cache_write_back, ext4_dir_en_get_inode, ext4_dir_iterator_fini, ext4_dir_iterator_init ...(+19) |
| `kernelx/clib/lib/lwext4/lwext4/src/ext4_dir_idx.c#ext4_dir_dx_split_index` | `src/fs/ext4/ext4_dir_idx.c#ext4_dir_dx_split_index` | 同板块同名同结构 | ext4_block_set, ext4_dir_dx_climit_get_count, ext4_dir_dx_climit_get_limit, ext4_dir_dx_climit_set_count, ext4_dir_dx_climit_set_limit, ext4_dir_dx_entry_get_hash ...(+10) | ext4_block_set, ext4_dir_dx_climit_get_count, ext4_dir_dx_climit_get_limit, ext4_dir_dx_climit_set_count, ext4_dir_dx_climit_set_limit, ext4_dir_dx_entry_get_hash ...(+10) |

#### 通用模块/其他
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `kernelx/src/klib/ksync/lockdep.rs#release` | `src/atomic/spinlock.c#release` | 同板块同名 | core::ptr::eq, held.iter, held.iter().rposition, held.remove, panic!, self.held.get | __sync_lock_release, __sync_synchronize, holding, panic, pop_off |
| `kernelx/clib/src/klib/string.c#strncmp` | `src/lib/string.c#strncmp` | 同板块同名 | 内部闭环逻辑 | 内部闭环逻辑 |
| `kernelx/clib/src/klib/string.c#strcmp` | `src/lib/string.c#strcmp` | 同板块同名 | 内部闭环逻辑 | 内部闭环逻辑 |
| `kernelx/clib/src/klib/string.c#strcpy` | `src/lib/string.c#strcpy` | 同板块同名 | 内部闭环逻辑 | 内部闭环逻辑 |


#### 1. 宏观判定
- **定性结论**: **底层架构显著差异 / 独立开发** (相似度: 8.92%，共同 AST 指纹: 394；唯一 AST 指纹: 目标项目 4055，参考项目 754；覆盖率: 目标项目 9.72%，参考项目 52.25%)

#### 2. 全模块覆盖矩阵解读
- **进程管理**: 双方均存在该模块。共同 AST 指纹为 0，目标覆盖率 0%，参考覆盖率 0%。双方在进程管理核心逻辑上无任何可被检测器识别的结构重合，证据强度为**未形成**。
- **任务调度/异步运行时**: 双方均存在该模块。共同 AST 指纹为 0，目标覆盖率 0%，参考覆盖率 0%。双方在调度器实现上无结构重合，证据强度为**未形成**。
- **内存管理**: 双方均存在该模块。共同 AST 指纹为 2，目标覆盖率 0.34%，参考覆盖率 1.68%。重合度极低，且重合点位于通用内存分配器（`malloc`/`free`）和进程创建（`fork`）等标准语义函数上，证据强度为**弱**。
- **中断/异常处理**: 双方均存在该模块。共同 AST 指纹为 0，目标覆盖率 0%，参考覆盖率 0%。双方在中断处理流程上无结构重合，证据强度为**未形成**。
- **系统调用**: 双方均存在该模块。共同 AST 指纹为 0，目标覆盖率 0%，参考覆盖率 0%。双方在系统调用分发与实现上无结构重合，证据强度为**未形成**。
- **文件系统**: 双方均存在该模块。共同 AST 指纹为 2，目标覆盖率 0.2%，参考覆盖率 3.57%。重合度极低，重合点位于 `mount`/`umount2` 等标准系统调用接口，证据强度为**弱**。
- **设备驱动**: 双方均存在该模块。共同 AST 指纹为 0，目标覆盖率 0%，参考覆盖率 0%。双方在驱动模型上无结构重合，证据强度为**未形成**。
- **网络协议栈**: 仅目标项目体现。参考项目无此模块，无法进行同板块比对，证据强度为**未形成**。
- **第三方库/公共依赖**: 双方均存在该模块。共同 AST 指纹为 393，目标覆盖率 51.92%，参考覆盖率 94.93%。重合度极高，但经核实，重合指纹均指向双方共同引用的开源 `lwext4` 文件系统库，属于合法公共依赖，证据强度为**强（但非原创代码复用）**。

#### 3. 底层 AST 关键证据指纹表
| 所属板块 | 目标项目路径 | 参考项目路径 | 证据类型 | 技术说明 |
| :--- | :--- | :--- | :--- | :--- |
| 第三方库/公共依赖 | kernelx/clib/lib/lwext4/lwext4/src/ext4_balloc.c#ext4_balloc_alloc_block | src/fs/ext4/ext4_balloc.c#ext4_balloc_alloc_block | 同板块同名同结构 | 双方均直接引用了开源库 lwext4 的同一函数，调用图谱完全一致，属于公共依赖。 |
| 第三方库/公共依赖 | kernelx/clib/lib/lwext4/lwext4/src/ext4_journal.c#jbd_journal_prepare | src/fs/ext4/ext4_journal.c#jbd_journal_prepare | 同板块同名同结构 | 同上，为 lwext4 库内部函数，非任何一方原创。 |
| 第三方库/公共依赖 | kernelx/clib/lib/lwext4/lwext4/src/ext4.c#ext4_dir_rm | src/fs/ext4/ext4.c#ext4_dir_rm | 同板块同名同结构 | 同上，为 lwext4 库内部函数，非任何一方原创。 |
| 第三方库/公共依赖 | kernelx/clib/lib/lwext4/lwext4/src/ext4_dir_idx.c#ext4_dir_dx_split_index | src/fs/ext4/ext4_dir_idx.c#ext4_dir_dx_split_index | 同板块同名同结构 | 同上，为 lwext4 库内部函数，非任何一方原创。 |
| 内存管理 | kernelx/src/kernel/mm/maparea/filemap/private.rs#fork | src/proc/proc.c#fork | 同板块同名 | 双方均实现了 `fork` 调用，但目标项目使用 Rust 的 `Box::new`、`FrameState::Cow` 等内存安全抽象，参考项目使用 `allocproc`、`filedup` 等 C 语言原语，AST 无重合。 |
| 内存管理 | kernelx/src/kernel/mm/swappable/nofile/swap.rs#free | user/umalloc.c#free | 同板块同名 | 同名函数 `free`，但目标项目操作交换槽（`SWAPPER.free_slot`），参考项目是用户态内存释放，实现机制完全不同。 |
| 文件系统 | kernelx/src/kernel/syscall/fs.rs#mount | src/fs/fat32/fat32.c#mount | 同板块同名 | 同名系统调用 `mount`，目标项目使用 `MountFlags`、`MountOptions` 等 Rust 抽象，参考项目直接操作 FAT32 文件系统的 `bread`/`brelse`，实现完全不同。 |
| 文件系统 | kernelx/src/kernel/syscall/fs.rs#umount2 | src/fs/fat32/fat32.c#umount2 | 同板块同名 | 同名系统调用 `umount2`，目标项目调用 `vfs::unmount`，参考项目调用 `memset` 清理内存，实现路径无重合。 |

#### 4. 核心代码调用流对比剖析
* **【进程管理/进程退出路径】**
* **底层技术析理**: 目标项目 `exit` 函数基于 Rust 的所有权与引用计数模型，通过 `Arc::strong_count` 检查引用，使用 `futex::wake` 唤醒等待者，并调用 `self.addrspace.cleanup` 和 `manager::remove` 进行资源回收。参考项目 `exit_group` 仅调用 `exit`，其内部依赖全局进程表和 `acquire`/`release` 自旋锁保护。两者在内存安全模型、同步原语和资源管理策略上存在根本性架构差异。
* **代码溯源链接**:
  - 目标项目: `kernelx/src/kernel/task/tcb.rs#exit`
  - 参考项目: `src/proc/proc.c#exit_group`

* **【进程管理/子进程等待路径】**
* **底层技术析理**: 目标项目通过 `ChildWaitOptions` 构建器模式配置等待选项，最终调用 `matching_child`、`waitable_child` 等内部方法进行状态匹配。参考项目 `waitpid` 直接操作进程表，通过 `sleep` 挂起当前进程，被唤醒后调用 `freeproc` 回收资源。目标项目将状态机逻辑封装在类型系统中，而参考项目是典型的过程式C代码，两者在抽象层次和对象管理模型上完全不同。
* **代码溯源链接**:
  - 目标项目: `kernelx/src/kernel/syscall/task.rs#waitid`
  - 参考项目: `src/proc/proc.c#waitpid`

* **【内存管理/内存分配路径】**
* **底层技术析理**: 目标项目 `malloc` 直接调用 `tlsf_malloc`，使用 TLSF（Two-Level Segregated Fit）算法。参考项目 `malloc` 调用 `morecore`，基于传统 Unix `sbrk` 机制扩展堆。两者在内存分配算法和底层系统调用上完全不同。
* **代码溯源链接**:
  - 目标项目: `kernelx/clib/src/klib/malloc.c#malloc`
  - 参考项目: `user/umalloc.c#malloc`

#### 5. 单方模块与降权项
- **网络协议栈**: 目标项目拥有完整的 TCP/IP 网络协议栈（`kernelx/src/net/`），而参考项目无此模块。此为单方原创功能，不构成同源证据。
- **第三方库/公共依赖**: 全项目 394 条共同 AST 指纹中，393 条来自 `lwext4` 库。该库为公开的 ext4 文件系统实现，双方均直接引用，属于合法公共依赖，必须从原创性比对中排除。
- **跨板块同名函数**: 如目标项目 `fanotify` 中的 `wait` 与参考项目 `proc.c` 中的 `wait`，虽同名但分属文件事件通知与进程管理子系统，AST 调用图谱无重合，仅作为弱信号，不构成同板块主证据。
- **POSIX/标准接口**: `exit_group`、`waitid`、`mount`、`sigprocmask` 等均为 POSIX/Linux 标准系统调用。任何实现这些接口的内核都必然包含相似的高层语义流程，此为领域共性，不能作为代码复用的证据。

#### 6. 保守结论
本次审计基于客观 AST 调用图谱比对，目标项目与参考项目的整体相似度仅为 8.92%。在核心原创模块（进程管理、任务调度、内存管理、系统调用等）中，双方共同 AST 指纹为零或极低，且外部调用对象（Rust 高级抽象 vs. C 裸原语）完全异构，表明两者在架构设计、编程范式和安全模型上存在根本性差异。全项目 99.7% 的共同指纹（393/394）集中于双方共同引用的开源第三方库 `lwext4`，属于合法公共依赖，与原创代码抄袭无关。控方基于高层语义相似性提出的复用指控，缺乏底层 AST 结构证据支持，混淆了领域公共知识与代码实现。综合判定，目标项目为独立开发，整体同源性风险极低。

------------------------------

