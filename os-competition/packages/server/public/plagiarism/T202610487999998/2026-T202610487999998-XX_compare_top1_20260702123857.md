# 🛡️ OS 内核赛自动化代码级架构审计报告

**审计时间**: `2026-07-02 12:38:57`
**参赛作品名称**: `2026-T202610487999998-XX`
**年份**: `2026`
**学校**: `XX`
**队伍名称**: `T202610487999998`
**比赛名字**: `OS-Competition`
**比赛子赛道**: `Kernel-Implementation`
**仓库地址**: `https://gitlab.eduxiji.net/educg-group-43501-3132633/T202610487999998-2931`
**推测开发语言**: `Rust / C (由AST探针自动识别)`
**特征函数量**: `227`

## ⚔️ 底层代码同源性对比 (AST Homology)
> 本部分由专门的代码审查智能体，对该项目与符合年份条件的历届最高相似度项目进行控制流与底层调用对比。新版报告会先给出确定性的全模块覆盖矩阵，再给出 Agent 的分模块解释，避免只围绕少量抽样函数展开。

### 🏆 Top 1 底层对标: vs `2022-w以忽略所有警告-南京航空航天大学`（南京航空航天大学 / -w以忽略所有警告）

#### 对方作品信息

**对方年份**: `2022`
**对方学校**: `南京航空航天大学`
**对方队伍名称**: `-w以忽略所有警告`
**对方比赛名字**: `OS-Competition`
**对方比赛子赛道**: `Kernel-Implementation`
**对方仓库地址**: `https://gitlab.eduxiji.net/qianpinyi/pal_operatingsystem`

> 双向覆盖率：共同 AST 指纹: 3；唯一 AST 指纹: 目标项目 204，参考项目 64；覆盖率: 目标项目 1.47%，参考项目 4.69%

## 全模块对比总览（确定性证据）

> 总体覆盖率：共同 AST 指纹: 3；唯一 AST 指纹: 目标项目 204，参考项目 64；覆盖率: 目标项目 1.47%，参考项目 4.69%

### 1. 模块覆盖矩阵
| 模块 | 目标函数数 | 参考函数数 | 共同唯一 AST 指纹 | Target 覆盖 | Base 覆盖 | 状态 |
| :--- | ---: | ---: | ---: | ---: | ---: | :--- |
| 进程管理 | 75 | 1 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |
| 任务调度/异步运行时 | 4 | 12 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |
| 内存管理 | 57 | 6 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |
| 中断/异常处理 | 0 | 11 | 0 | 0.0% | 0.0% | 仅参考项目体现 |
| 系统调用 | 8 | 0 | 0 | 0.0% | 0.0% | 仅目标项目体现 |
| 文件系统 | 12 | 0 | 0 | 0.0% | 0.0% | 仅目标项目体现 |
| 设备驱动 | 37 | 47 | 1 | 3.45% | 2.78% | 双方存在且有结构重合 |
| 网络协议栈 | 0 | 0 | 0 | 0.0% | 0.0% | 双方均未体现 |
| 文件系统/第三方库 | 31 | 0 | 0 | 0.0% | 0.0% | 仅目标项目体现 |
| 通用模块/其他 | 3 | 0 | 0 | 0.0% | 0.0% | 仅目标项目体现 |

### 2. 分模块证据清单

#### 进程管理
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `kernel/proc/user.c#fd_write_bytes` | `Kernel/HAL/Drivers/_dmac.c#dmac_wait_idle` | 同模块上下文对齐/差异观察 | fd_pipe, fd_sync_mem_file, memcpy, memset, user_puts | dmacSemWait, dmac_is_idle |

#### 任务调度/异步运行时
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `kernel/common/main.c#run_iozone_group` | `Kernel/HAL/Drivers/_sysctl.c#sysctl_clock_device_en` | 同模块上下文对齐/差异观察 | ext4_dir_exists, force_synthetic_lib, kprintf, kputs, lib_file_exists, make_lib_path | 内部闭环逻辑 |
| `kernel/common/main.c#run_ltp_group` | `Kernel/HAL/Drivers/_spi.c#spi_receive_data_standard` | 同模块上下文对齐/差异观察 | emit_ltp_pass, ext4_dir_exists, ext4_file_exists, kprintf, kputs, make_lib_path ...(+2) | (uint32), spi_get_frame_size, spi_set_tmod |
| `kernel/common/main.c#ltp_expected_passes` | `Kernel/HAL/Drivers/_spi.c#spi_send_data_normal` | 同模块上下文对齐/差异观察 | str_eq | memmove, spi_get_frame_size, spi_set_tmod |
| `kernel/common/print.c#kprintf` | `Kernel/HAL/Drivers/_sysctl.c#sysctl_clock_bus_en` | 同模块上下文对齐/差异观察 | kputs, print_u64, put_char, va_arg, va_end, va_start | 内部闭环逻辑 |

#### 内存管理
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `kernel/proc/user.c#user_trap_handler` | `Kernel/HAL/Drivers/_spi.c#spi_receive_data_standard_dma` | 同模块上下文对齐/差异观察 | apply_loader_path_alias, compat_fill_rlimit, compat_fill_stat, compat_fill_stat_owner, compat_fill_statfs, compat_fill_statx ...(+66) | kalloc, kfree, spi_get_frame_size, spi_receive_data_normal_dma |
| `kernel/common/main.c#emit_basic_result` | `Kernel/HAL/Drivers/_spi.c#spi_send_data_standard_dma` | 同模块上下文对齐/差异观察 | basic_end, basic_start, kprintf, kputs, str_eq | kalloc, kfree, spi_get_frame_size, spi_send_data_normal_dma |
| `kernel/proc/user.c#user_run_elf_args` | `Kernel/HAL/Drivers/_spi.c#spi_init` | 同模块上下文对齐/差异观察 | build_user_stack, copy_kernel_cstr, ext4_lookup_path, ext4_read_inode_all, load_elf, map_range_identity ...(+16) | 内部闭环逻辑 |
| `kernel/common/main.c#run_libctest_group` | `Kernel/HAL/Drivers/_spi.c#spi_send_data_normal_dma` | 同模块上下文对齐/差异观察 | ext4_dir_exists, force_synthetic_lib, kprintf, kputs, lib_file_exists, make_lib_path | dmac_set_single_mode, dmac_wait_done, kalloc, kfree, spi_set_tmod, sysctl_dma_select |

#### 中断/异常处理
仅参考项目中观察到该模块代表函数，目标项目没有同模块可对齐证据：
- 参考项目：`Kernel/HAL/Drivers/_plic.c#plicinithart`，调用摘要：PLIC_MENABLE, PLIC_SENABLE, PLIC_SPRIORITY, cpuid, printf, readd
- 参考项目：`Kernel/HAL/Drivers/_dmac.c#dmac_enable_common_interrupt_signal`，调用摘要：readq, writeq
- 参考项目：`Kernel/HAL/Drivers/_dmac.c#dmac_enable_common_interrupt_status`，调用摘要：readq, writeq
- 参考项目：`Kernel/HAL/Drivers/_dmac.c#dmac_enable_channel_interrupt`，调用摘要：writeq

#### 系统调用
仅目标项目中观察到该模块代表函数，参考项目没有同模块可对齐证据：
- 目标项目：`kernel/syscall/linux_compat.c#compat_fill_statx`，调用摘要：memset
- 目标项目：`kernel/syscall/linux_compat.c#compat_fill_statx_special`，调用摘要：compat_fill_statx
- 目标项目：`kernel/arch/riscv64/arch.c#sbi_call`，调用摘要：内部闭环逻辑
- 目标项目：`kernel/syscall/linux_compat.c#compat_fill_stat`，调用摘要：memset

#### 文件系统
仅目标项目中观察到该模块代表函数，参考项目没有同模块可对齐证据：
- 目标项目：`kernel/common/main.c#run_busybox_group`，调用摘要：ext4_file_exists, force_synthetic_lib, kprintf, kputs, make_lib_path, str_eq ...(+2)
- 目标项目：`kernel/common/main.c#kmain`，调用摘要：arch_shutdown, block_init, ext4_discover_basic_tests_in, ext4_mount, kprintf, kputs ...(+5)
- 目标项目：`kernel/common/main.c#run_cyclictest_group`，调用摘要：force_synthetic_lib, kprintf, kputs, lib_file_exists
- 目标项目：`kernel/common/main.c#run_iperf_group`，调用摘要：force_synthetic_lib, kprintf, kputs, lib_file_exists

#### 设备驱动
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `kernel/common/virtio.c#virtio_notify_queue` | `Kernel/HAL/Drivers/_sdcard.c#sd_lowlevel_init` | 同板块同结构 | vpci_write16 | gpiohs_set_drive_mode |
| `kernel/common/virtio.c#virtio_notify_queue` | `Kernel/HAL/Drivers/_dmac.c#dmac_wait_done` | 同板块同结构 | mmio_write | dmac_wait_idle |
| `kernel/common/virtio.c#fence_io` | `Kernel/HAL/Drivers/_sdcard.c#sd_get_response_R3_rest` | 同板块同结构 | __sync_synchronize | sd_read_data |
| `kernel/common/virtio.c#block_read_sector` | `Kernel/HAL/Drivers/_dmac.c#dmac_set_channel_param` | 同模块上下文对齐/差异观察 | fence_io, kprintf, memcpy, virtio_ack_used, virtio_cpu_dma_ptr, virtio_dma_addr ...(+3) | is_memory, readq, writeq |

#### 文件系统/第三方库
仅目标项目中观察到该模块代表函数，参考项目没有同模块可对齐证据：
- 目标项目：`kernel/fs/ext4.c#ext4_lookup_path`，调用摘要：inode_size, lookup_child_typed, read_inode, root_or_cwd, str_eq_local
- 目标项目：`kernel/fs/ext4.c#discover_basic_tests_from_dir`，调用摘要：append_named_test, copy_name, ext4_read_inode_prefix, inode_read_at, inode_size, kputs ...(+4)
- 目标项目：`kernel/fs/ext4.c#ext4_extent_physical_block`，调用摘要：block_read, ext4_extent_physical_block, extent_idx_leaf, extent_start
- 目标项目：`kernel/fs/ext4.c#ext4_read_dirent`，调用摘要：copy_name, inode_read_at, inode_size, read_inode

#### 通用模块/其他
仅目标项目中观察到该模块代表函数，参考项目没有同模块可对齐证据：
- 目标项目：`kernel/arch/loongarch64/arch.c#arch_shutdown`，调用摘要：mmio_write8
- 目标项目：`kernel/arch/riscv64/arch.c#arch_shutdown`，调用摘要：sbi_call
- 目标项目：`kernel/arch/riscv64/arch.c#arch_putchar`，调用摘要：sbi_call


#### 1. 宏观判定
- **定性结论**: 底层架构显著差异 / 独立开发 (相似度: 1.13%，共同 AST 指纹: 3；唯一 AST 指纹: 目标项目 204，参考项目 64；覆盖率: 目标项目 1.47%，参考项目 4.69%)

#### 2. 全模块覆盖矩阵解读
- **进程管理**: 双方均存在该模块。共同 AST 指纹为 0。目标项目覆盖率 0.0%，参考项目覆盖率 0.0%。目标项目拥有 75 个函数，而参考项目仅 1 个，体量悬殊。双方在该模块未形成任何有效结构重合，证据强度为**未形成**。
- **任务调度/异步运行时**: 双方均存在该模块。共同 AST 指纹为 0。目标项目覆盖率 0.0%，参考项目覆盖率 0.0%。目标项目侧重测试套件调度（`run_ltp_group`），参考项目侧重底层外设时钟控制（`sysctl_clock_device_en`），功能语义完全不同，证据强度为**未形成**。
- **内存管理**: 双方均存在该模块。共同 AST 指纹为 0。目标项目覆盖率 0.0%，参考项目覆盖率 0.0%。目标项目涉及 ELF 加载、页表映射等复杂虚拟内存管理，参考项目仅为 SPI DMA 传输中的 `kalloc/kfree` 缓冲区分配，抽象层次差异巨大，证据强度为**未形成**。
- **中断/异常处理**: 仅参考项目体现。共同 AST 指纹为 0。目标项目无此独立模块，证据强度为**未形成**。
- **系统调用**: 仅目标项目体现。共同 AST 指纹为 0。参考项目无此模块，证据强度为**未形成**。
- **文件系统**: 仅目标项目体现。共同 AST 指纹为 0。参考项目无此模块，证据强度为**未形成**。
- **设备驱动**: 双方均存在该模块。共同 AST 指纹为 1。目标项目覆盖率 3.45%，参考项目覆盖率 2.78%。这是唯一存在结构重合的模块，但重合度极低。目标项目为 VirtIO/PCI 驱动栈，参考项目为 SPI/SDCard/DMAC 驱动栈，硬件抽象层完全不同，证据强度为**弱**。
- **网络协议栈**: 双方均未体现。证据强度为**未形成**。
- **第三方库/公共依赖**: 仅目标项目体现（文件系统/第三方库 31 个函数）。共同 AST 指纹为 0。参考项目无此模块，证据强度为**未形成**。

#### 3. 底层 AST 关键证据指纹表
| 所属板块 | 目标项目路径 | 参考项目路径 | 证据类型 | 技术说明 |
| :--- | :--- | :--- | :--- | :--- |
| 设备驱动 | kernel/common/virtio.c#fence_io | Kernel/HAL/Drivers/_sdcard.c#sd_get_response_R3_rest | 同板块同结构 | 双方均存在一个调用单一底层函数的简单包装函数。目标调用 `__sync_synchronize` (编译器内置屏障)，参考调用 `sd_read_data` (SD卡数据读取)。这是通用函数包装模式，而非具体逻辑同源。 |
| 设备驱动 | kernel/common/virtio.c#virtio_notify_queue | Kernel/HAL/Drivers/_sdcard.c#sd_lowlevel_init | 同板块同结构 | 双方均存在一个调用单一底层函数的简单包装函数。目标调用 `vpci_write16` (PCI IO 写)，参考调用 `gpiohs_set_drive_mode` (GPIO 模式设置)。这是通用函数包装模式，而非具体逻辑同源。 |
| 设备驱动 | kernel/common/virtio.c#virtio_notify_queue | Kernel/HAL/Drivers/_dmac.c#dmac_wait_done | 同板块同结构 | 双方均存在一个调用单一底层函数的简单包装函数。目标调用 `mmio_write` (MMIO 写)，参考调用 `dmac_wait_idle` (等待 DMA 空闲)。这是通用函数包装模式，而非具体逻辑同源。 |

#### 4. 核心代码调用流对比剖析
* **【设备驱动/块设备读写路径】**
* **底层技术析理**: 目标项目的 `block_read_sector` 与参考项目的 `sdcard_read_sector` 虽然都实现了扇区读取，但底层驱动模型完全不同。目标项目基于 VirtIO 协议，通过 `virtio_queue`、`virtio_notify_queue`、`virtio_ack_used` 等函数操作虚拟队列和通知机制，并使用 `fence_io` 进行内存屏障同步。参考项目基于 SPI 协议的 SD 卡驱动，通过 `sd_send_cmd`、`sd_end_cmd`、`sd_read_data` 等函数实现命令-响应式的硬件交互，并使用信号量 `semSDCard` 进行任务同步。两者在同步原语（内存屏障 vs 信号量）、数据传输模型（VirtIO 队列 vs SPI 命令序列）、硬件抽象对象（PCI/VirtIO vs SPI/SDCard）上均存在本质差异。
* **代码溯源链接**:
  - 目标项目: `kernel/common/virtio.c#block_read_sector`
  - 参考项目: `Kernel/HAL/Drivers/_sdcard.c#sdcard_read_sector`

* **【内存管理/程序加载路径】**
* **底层技术析理**: 目标项目的 `user_run_elf_args` 实现了一个完整的 ELF 加载器，其调用流涉及文件系统（`ext4_lookup_path`, `ext4_read_inode_all`）、内存映射（`map_range_identity`, `map_user_range`）和用户栈构建（`build_user_stack`），管理的是虚拟地址空间和页表。参考项目的 `spi_send_data_standard_dma` 是 SPI 外设的 DMA 发送函数，其调用流涉及动态内存分配（`kalloc`, `kfree`）和 DMA 控制器配置（`spi_get_frame_size`, `spi_send_data_normal_dma`），管理的是物理内存缓冲区。两者管理的对象模型（虚拟内存页表 vs 物理DMA缓冲区）和抽象层次（进程加载器 vs 外设驱动）完全不同。
* **代码溯源链接**:
  - 目标项目: `kernel/proc/user.c#user_run_elf_args`
  - 参考项目: `Kernel/HAL/Drivers/_spi.c#spi_send_data_standard_dma`

#### 5. 单方模块与降权项
- **单方模块**: 目标项目独有的【系统调用】（如 `compat_fill_statx`）、【文件系统】（如 `ext4_lookup_path`）模块，以及参考项目独有的【中断/异常处理】（如 `plicinithart`）模块，是双方架构定位差异的直接体现，不构成同源证据。
- **第三方库/公共依赖**: 目标项目中存在 31 个被归类为“文件系统/第三方库”的函数，参考项目中无此分类。这些函数属于目标项目的独立依赖。
- **通用原语降权**: 双方证据中出现的 `memcpy`、`memset`、`kprintf`/`printf`、`__sync_synchronize`、`mmio_read`/`write`、`readq`/`writeq` 等均为操作系统内核或 C 语言标准库中的通用原语，其调用模式不能作为抄袭的强证据。

#### 6. 保守结论
本次审计的目标项目与参考项目在 AST 层面的相似度仅为 1.13%，属于噪声级重合。双方在硬件平台（RISC-V/LoongArch vs K210）、驱动模型（VirtIO/PCI vs SPI/SDCard）、系统架构（宏内核/Linux兼容 vs 嵌入式HAL）上存在根本性差异。目标项目具备完整的文件系统、系统调用和测试框架，而参考项目则侧重特定 SoC 的外设驱动。控方提出的“协议层替换式洗稿”指控缺乏客观的 AST 结构证据支持，其列举的函数对在调用图谱、同步机制和对象模型上均无实质同构性。综合判定，目标项目为独立开发，不存在对参考项目的违规复用。

------------------------------

