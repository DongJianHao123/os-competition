# 🛡️ OS 内核赛自动化代码级架构审计报告

**审计时间**: `2026-07-02 12:38:57`
**参赛作品名称**: `2026-T202610487999998-XX`
**年份**: `2026`
**学校**: `XX`
**队伍名称**: `T202610487999998`
**比赛名字**: `OS-Competition`
**比赛子赛道**: `Kernel-Implementation`
**仓库地址**: `https://gitlab.eduxiji.net/educg-group-43501-3132633/T202610487999998-2931`
**推测开发语言**: `Rust / C (由AST探针自动识别)`
**特征函数量**: `227`

## ⚔️ 底层代码同源性对比 (AST Homology)
> 本部分由专门的代码审查智能体，对该项目与符合年份条件的历届最高相似度项目进行控制流与底层调用对比。新版报告会先给出确定性的全模块覆盖矩阵，再给出 Agent 的分模块解释，避免只围绕少量抽样函数展开。

### 🏆 Top 2 底层对标: vs `2021-TeamNameNotFound-重庆邮电大学`（重庆邮电大学 / TeamNameNotFound）

#### 对方作品信息

**对方年份**: `2021`
**对方学校**: `重庆邮电大学`
**对方队伍名称**: `TeamNameNotFound`
**对方比赛名字**: `OS-Competition`
**对方比赛子赛道**: `Kernel-Implementation`
**对方仓库地址**: `https://gitlab.eduxiji.net/qq976243638/xos`

> 双向覆盖率：共同 AST 指纹: 3；唯一 AST 指纹: 目标项目 204，参考项目 78；覆盖率: 目标项目 1.47%，参考项目 3.85%

## 全模块对比总览（确定性证据）

> 总体覆盖率：共同 AST 指纹: 3；唯一 AST 指纹: 目标项目 204，参考项目 78；覆盖率: 目标项目 1.47%，参考项目 3.85%

### 1. 模块覆盖矩阵
| 模块 | 目标函数数 | 参考函数数 | 共同唯一 AST 指纹 | Target 覆盖 | Base 覆盖 | 状态 |
| :--- | ---: | ---: | ---: | ---: | ---: | :--- |
| 进程管理 | 75 | 14 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |
| 任务调度/异步运行时 | 4 | 4 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |
| 内存管理 | 57 | 34 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |
| 中断/异常处理 | 0 | 2 | 0 | 0.0% | 0.0% | 仅参考项目体现 |
| 系统调用 | 8 | 0 | 0 | 0.0% | 0.0% | 仅目标项目体现 |
| 文件系统 | 12 | 9 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |
| 设备驱动 | 37 | 4 | 1 | 3.45% | 25.0% | 双方存在且有结构重合 |
| 网络协议栈 | 0 | 0 | 0 | 0.0% | 0.0% | 双方均未体现 |
| 文件系统/第三方库 | 31 | 0 | 0 | 0.0% | 0.0% | 仅目标项目体现 |
| 第三方库 | 0 | 18 | 0 | 0.0% | 0.0% | 仅参考项目体现 |
| 通用模块/其他 | 3 | 0 | 0 | 0.0% | 0.0% | 仅目标项目体现 |

### 2. 分模块证据清单

#### 进程管理
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `kernel/proc/user.c#fd_write_bytes` | `kernel/proc/proc.c#proc_wait` | 同模块上下文对齐/差异观察 | fd_pipe, fd_sync_mem_file, memcpy, memset, user_puts | free_proc, kfree, list_rm, proc_switch |
| `kernel/proc/user.c#fd_read_bytes` | `kernel/proc/proc.c#proc_init` | 同模块上下文对齐/差异观察 | ext4_read_inode_at, fd_pipe, memcpy, memset | init_fd_table, sche_ptr->init |
| `kernel/proc/user.c#proc_self_status_data` | `kernel/proc/proc.c#init_fd_table` | 同模块上下文对齐/差异观察 | str_append, str_append_u64 | 内部闭环逻辑 |
| `kernel/proc/user.c#resolve_loader_path` | `kernel/proc/sche_fifo.c#dequeue` | 同模块上下文对齐/差异观察 | copy_kernel_cstr, str_contains, str_eq | isempty |

#### 任务调度/异步运行时
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `kernel/common/main.c#run_iozone_group` | `kernel/trap/syscall.c#syscall` | 同模块上下文对齐/差异观察 | ext4_dir_exists, force_synthetic_lib, kprintf, kputs, lib_file_exists, make_lib_path | fs_close, fs_openat, fs_read, proc_clone, proc_execve, proc_exit ...(+2) |
| `kernel/common/main.c#run_ltp_group` | `kernel/trap/trap.c#trap_ret_user` | 同模块上下文对齐/差异观察 | emit_ltp_pass, ext4_dir_exists, ext4_file_exists, kprintf, kputs, make_lib_path ...(+2) | csrr, csrw, trap_end |
| `kernel/common/main.c#ltp_expected_passes` | `kernel/fs/fat32.c#fat32_read` | 同模块上下文对齐/差异观察 | str_eq | kfree, load_file, memcpy |
| `kernel/common/print.c#kprintf` | `kernel/fs/fs.c#fs_close` | 同模块上下文对齐/差异观察 | kputs, print_u64, put_char, va_arg, va_end, va_start | 内部闭环逻辑 |

#### 内存管理
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `kernel/proc/user.c#load_elf` | `kernel/fs/elf.c#load_elf` | 同板块同名 | align_up_u64, elf_interp_path, elf_is_valid, load_elf_image, read_ext4_file_path, resolve_loader_path | (uint64), ROUNDDOWN, flags_trans, kalloc, list_add_back, magic_test ...(+4) |
| `kernel/common/main.c#emit_basic_result` | `kernel/batch/app.c#app_init` | 同模块上下文对齐/差异观察 | basic_end, basic_start, kprintf, kputs, str_eq | alloc_proc, fs_close, fs_openat, fs_read, init_fd_table, kalloc ...(+6) |
| `kernel/proc/user.c#user_run_elf_args` | `kernel/proc/proc.c#proc_clone` | 同模块上下文对齐/差异观察 | build_user_stack, copy_kernel_cstr, ext4_lookup_path, ext4_read_inode_all, load_elf, map_range_identity ...(+16) | ROUNDDOWN, alloc_proc, copy_segments, copy_trapframe, init_fd_table, kalloc ...(+3) |
| `kernel/common/main.c#run_libctest_group` | `kernel/fs/fat32.c#fat32_openat` | 同模块上下文对齐/差异观察 | ext4_dir_exists, force_synthetic_lib, kprintf, kputs, lib_file_exists, make_lib_path | alloc_fd, alloc_file_info, dentry2file_info, dir_search, fat32_openat, fs_close ...(+3) |

#### 中断/异常处理
仅参考项目中观察到该模块代表函数，目标项目没有同模块可对齐证据：
- 参考项目：`kernel/trap/syscall.c#sys_write`，调用摘要：console_putchar
- 参考项目：`kernel/trap/trap.c#trap_init`，调用摘要：csrr, csrw

#### 系统调用
仅目标项目中观察到该模块代表函数，参考项目没有同模块可对齐证据：
- 目标项目：`kernel/syscall/linux_compat.c#compat_fill_statx`，调用摘要：memset
- 目标项目：`kernel/syscall/linux_compat.c#compat_fill_statx_special`，调用摘要：compat_fill_statx
- 目标项目：`kernel/arch/riscv64/arch.c#sbi_call`，调用摘要：内部闭环逻辑
- 目标项目：`kernel/syscall/linux_compat.c#compat_fill_stat`，调用摘要：memset

#### 文件系统
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `kernel/common/main.c#run_busybox_group` | `kernel/fs/fat32.c#fname2short` | 同模块上下文对齐/差异观察 | ext4_file_exists, force_synthetic_lib, kprintf, kputs, make_lib_path, str_eq ...(+2) | strcpy, strlen, toupper |
| `kernel/common/main.c#kmain` | `kernel/fs/elf.c#magic_test` | 同模块上下文对齐/差异观察 | arch_shutdown, block_init, ext4_discover_basic_tests_in, ext4_mount, kprintf, kputs ...(+5) | panic |
| `kernel/common/main.c#run_cyclictest_group` | `kernel/fs/fat32.c#dentry2file_info` | 同模块上下文对齐/差异观察 | force_synthetic_lib, kprintf, kputs, lib_file_exists | 内部闭环逻辑 |
| `kernel/common/main.c#run_iperf_group` | `kernel/fs/fs.c#dev_init` | 同模块上下文对齐/差异观察 | force_synthetic_lib, kprintf, kputs, lib_file_exists | strcpy |

#### 设备驱动
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `kernel/common/virtio.c#virtio_cpu_dma_ptr` | `kernel/driver/virt_disk.c#disk_write_sector` | 同板块同结构 | 内部闭环逻辑 | 内部闭环逻辑 |
| `kernel/common/virtio.c#block_read_sector` | `kernel/driver/virt_disk.c#disk_read_sector` | 同模块上下文对齐/差异观察 | fence_io, kprintf, memcpy, virtio_ack_used, virtio_cpu_dma_ptr, virtio_dma_addr ...(+3) | memcpy |
| `kernel/common/virtio.c#block_init` | `kernel/driver/clock.c#clock_init` | 同模块上下文对齐/差异观察 | kputs, la_init_legacy_pci, pci_read16 | csrr, csrw, set_next_timer |
| `kernel/arch/loongarch64/arch.c#arch_putchar` | `kernel/driver/clock.c#set_next_timer` | 同模块上下文对齐/差异观察 | mmio_read8, mmio_write8 | csrr, set_timer |

#### 文件系统/第三方库
仅目标项目中观察到该模块代表函数，参考项目没有同模块可对齐证据：
- 目标项目：`kernel/fs/ext4.c#ext4_lookup_path`，调用摘要：inode_size, lookup_child_typed, read_inode, root_or_cwd, str_eq_local
- 目标项目：`kernel/fs/ext4.c#discover_basic_tests_from_dir`，调用摘要：append_named_test, copy_name, ext4_read_inode_prefix, inode_read_at, inode_size, kputs ...(+4)
- 目标项目：`kernel/fs/ext4.c#ext4_extent_physical_block`，调用摘要：block_read, ext4_extent_physical_block, extent_idx_leaf, extent_start
- 目标项目：`kernel/fs/ext4.c#ext4_read_dirent`，调用摘要：copy_name, inode_read_at, inode_size, read_inode

#### 第三方库
仅参考项目中观察到该模块代表函数，目标项目没有同模块可对齐证据：
- 参考项目：`kernel/libs/sbi.c#sbi_ecall`，调用摘要：内部闭环逻辑
- 参考项目：`kernel/libs/list.c#list_add_back`，调用摘要：内部闭环逻辑
- 参考项目：`kernel/libs/list.c#list_rm_front`，调用摘要：panic
- 参考项目：`kernel/libs/list.c#list_rm`，调用摘要：list_rm_front

#### 通用模块/其他
仅目标项目中观察到该模块代表函数，参考项目没有同模块可对齐证据：
- 目标项目：`kernel/arch/loongarch64/arch.c#arch_shutdown`，调用摘要：mmio_write8
- 目标项目：`kernel/arch/riscv64/arch.c#arch_shutdown`，调用摘要：sbi_call
- 目标项目：`kernel/arch/riscv64/arch.c#arch_putchar`，调用摘要：sbi_call


# 最终同源性定性仲裁报告

**案号**: 2026-T202610487999998-XX vs 2021-TeamNameNotFound-重庆邮电大学
**审计模式**: 深度析理模式
**裁定日期**: 2026年

---

## 1. 宏观判定

- **定性结论**: **独立开发** — 双方在底层架构、核心抽象与系统调用链路上存在根本性差异，不构成代码复用或抄袭。
- **客观指标**: AST 相似度 1.08%；共同 AST 指纹仅 3 个；目标项目唯一指纹 204 个，参考项目唯一指纹 78 个；双向覆盖率均低于 4%。
- **裁定依据**: 共同指纹集中于设备驱动模块的 trivial 内部闭环逻辑；进程管理、内存管理、任务调度等核心模块的共同 AST 指纹均为 0；文件系统、内存模型、同步策略存在不可约化的架构鸿沟。

---

## 2. 全模块覆盖矩阵解读

### 进程管理
- **双方存在情况**: 双方均存在（目标 75 函数，参考 14 函数）。
- **共同 AST 指纹**: 0。
- **覆盖率解读**: 目标覆盖率 0.0%，参考覆盖率 0.0%。双方在进程管理模块无任何可检测的结构同源。
- **证据强度**: **未形成**。控方指控的 `proc_clone_current` vs `proc_clone`、`user_execve_current` vs `proc_execve` 均基于功能语义类比，而非 AST 结构一致。目标采用页表级复制 + ext4 文件系统，参考采用段式复制 + FAT32 文件系统，底层数据流不可通约。

### 任务调度/异步运行时
- **双方存在情况**: 双方均存在（各 4 函数）。
- **共同 AST 指纹**: 0。
- **覆盖率解读**: 目标覆盖率 0.0%，参考覆盖率 0.0%。
- **证据强度**: **未形成**。目标项目为测试集调度框架（`run_iozone_group`、`run_ltp_group`），参考项目为基础系统调用分发（`syscall`）与陷阱返回（`trap_ret_user`），功能层级与调用图谱完全不同。控方已主动降级此板块。

### 内存管理
- **双方存在情况**: 双方均存在（目标 57 函数，参考 34 函数）。
- **共同 AST 指纹**: 0。
- **覆盖率解读**: 目标覆盖率 0.0%，参考覆盖率 0.0%。
- **证据强度**: **未形成**。控方指控的 `load_elf` 拆分、`copy_user_page_table_level` vs `copy_segments`、`pte_walk` vs `va2pa` 等，均忽略了一个根本事实：目标采用**递归页表遍历 + 用户态范围映射**，参考采用**段式迭代 + 单页映射 + VMA 链表**。两者在地址空间抽象上属于不同代际的设计。同名函数 `load_elf` 的调用图谱无重合（目标依赖 `elf_interp_path`/`resolve_loader_path`，参考依赖 `flags_trans`/`list_add_back`）。

### 中断/异常处理
- **双方存在情况**: 仅参考项目存在（2 函数）。
- **共同 AST 指纹**: 0。
- **覆盖率解读**: 目标项目无此模块独立体现，其异常处理整合于 `user_trap_handler`（归类于内存管理）。
- **证据强度**: **未形成**。单方模块，无法比对。

### 系统调用
- **双方存在情况**: 仅目标项目存在（8 函数）。
- **共同 AST 指纹**: 0。
- **覆盖率解读**: 参考项目无独立系统调用模块。
- **证据强度**: **未形成**。目标项目实现了完整的 Linux 兼容层（`compat_fill_statx`、`compat_fill_rlimit` 等），参考项目无对应代码。

### 文件系统
- **双方存在情况**: 双方均存在（目标 12 函数，参考 9 函数）。
- **共同 AST 指纹**: 0。
- **覆盖率解读**: 目标覆盖率 0.0%，参考覆盖率 0.0%。
- **证据强度**: **未形成**。目标使用 ext4 文件系统（`ext4_lookup_path`、`ext4_read_inode_all`），参考使用 FAT32（`fat32_openat`、`dir_search`）。两者在目录项解析、inode 管理、扇区读取路径上无任何 AST 重合。

### 设备驱动
- **双方存在情况**: 双方均存在（目标 37 函数，参考 4 函数）。
- **共同 AST 指纹**: 1（`virtio_cpu_dma_ptr` vs `disk_write_sector`，均为内部闭环逻辑）。
- **覆盖率解读**: 目标覆盖率 3.45%，参考覆盖率 25.0%。唯一共同指纹为 trivial 空操作/内部闭环函数，不携带任何领域逻辑。
- **证据强度**: **弱**。共同指纹属于无意义的边界重合。目标项目实现了完整的 VirtIO-MMIO + PCI 遗留模式初始化（`la_init_legacy_pci`、`block_init`），参考项目仅有简单的 `disk_read_sector`/`disk_write_sector` 封装。控方指控 5 中声称的“参考 `block_init`”在参考证据中不存在，属于事实错误。

### 网络协议栈
- **双方存在情况**: 双方均不存在。
- **共同 AST 指纹**: 0。
- **证据强度**: **未形成**。

### 第三方库/公共依赖
- **双方存在情况**: 目标项目含 31 函数（文件系统/第三方库混合），参考项目含 18 函数（纯第三方库）。
- **共同 AST 指纹**: 0。
- **覆盖率解读**: 双方第三方库无重合。
- **证据强度**: **未形成**。目标项目依赖 ext4 实现与合成库路径管理（`force_synthetic_lib`、`make_lib_path`），参考项目依赖 sbi 封装与链表库（`sbi_ecall`、`list_add_back`），无共同依赖。

---

## 3. 底层 AST 关键证据指纹表

| 所属板块 | 目标项目路径 | 参考项目路径 | 证据类型 | 技术说明 |
| :--- | :--- | :--- | :--- | :--- |
| 设备驱动 | `kernel/common/virtio.c#virtio_cpu_dma_ptr` | `kernel/driver/virt_disk.c#disk_write_sector` | 弱信号 — trivial 闭环 | 双方均为内部闭环逻辑，无外部调用链，不携带领域语义。属于无意义的边界重合。 |
| 进程管理 | `kernel/proc/user.c#proc_clone_current` | `kernel/proc/proc.c#proc_clone` | 未形成 — 功能类比 | 目标：页表复制 + `proc_save_globals`；参考：段复制 + `copy_trapframe`。调用图谱无重合。 |
| 内存管理 | `kernel/proc/user.c#load_elf` | `kernel/fs/elf.c#load_elf` | 未形成 — 同名异构 | 目标：`elf_interp_path` + `load_elf_image`；参考：`flags_trans` + `list_add_back`。AST 路径不可通约。 |
| 内存管理 | `kernel/proc/user.c#copy_user_page_table_level` | `kernel/proc/proc.c#copy_segments` | 未形成 — 算法异构 | 目标：递归页表遍历 + `pte_is_leaf`；参考：段迭代 + `ROUNDDOWN`。数据结构根本不同。 |
| 文件系统 | `kernel/common/main.c#kmain` | `kernel/fs/elf.c#magic_test` | 未形成 — 跨模块硬凑 | 目标：ext4 挂载 + 块设备初始化；参考：ELF 魔数校验。无功能关联。 |

---

## 4. 核心代码调用流对比剖析

### 【进程管理 — execve 执行流】
- **底层技术析理**: 
  - **目标项目**: `user_execve_current` → `free_user_page_table`（页表级释放）→ `ext4_lookup_basic_file` / `ext4_lookup_path`（ext4 inode 查找）→ `ext4_read_inode_all`（inode 级读取）→ `load_elf`（含解释器路径解析）→ `build_user_stack`（显式用户栈构建）→ `map_range_identity`（身份映射）。
  - **参考项目**: `proc_execve` → `free_segments` / `free_vma_list`（段/VMA 释放）→ `fs_openat`（通用文件打开）→ `fs_read`（通用读取）→ `load_elf`（无解释器支持）→ 无显式栈构建。
  - **异构性**: 目标项目在文件系统层使用 ext4 原生 inode 接口，参考使用 FAT32 之上的通用 fs 抽象层；目标项目支持动态链接解释器路径解析，参考不支持；目标项目显式构建用户栈并映射用户地址空间，参考依赖隐式栈。两者在抽象层次上存在代际差异。
- **代码溯源链接**:
  - 目标项目: `kernel/proc/user.c#user_execve_current`
  - 参考项目: `kernel/proc/proc.c#proc_execve`

### 【内存管理 — ELF 加载器】
- **底层技术析理**:
  - **目标项目**: `load_elf` → `elf_is_valid`（魔数+结构校验）→ `elf_interp_path`（提取解释器路径）→ `resolve_loader_path`（解释器路径解析）→ `read_ext4_file_path`（ext4 文件读取）→ `load_elf_image` → `align_up_u64`（向上对齐）→ `map_user_range`（用户态范围映射）。
  - **参考项目**: `load_elf` → `magic_test`（仅魔数校验）→ `flags_trans`（标志位转换）→ `ROUNDDOWN`（向下对齐）→ `kalloc`（内核分配）→ `map_a_page`（单页映射）→ `list_add_back`（VMA 链表维护）。
  - **异构性**: 目标加载器支持解释器路径解析与用户态范围映射，参考仅支持静态单页映射与内核 VMA 链表管理。对齐方向相反（`align_up_u64` vs `ROUNDDOWN`），分配器不同（`page_alloc` vs `kalloc`），映射粒度不同（范围映射 vs 单页映射）。
- **代码溯源链接**:
  - 目标项目: `kernel/proc/user.c#load_elf`
  - 参考项目: `kernel/fs/elf.c#load_elf`

### 【文件系统 — 目录查找】
- **底层技术析理**:
  - **目标项目**: `ext4_lookup_path` → `root_or_cwd`（根/当前目录判定）→ `read_inode`（inode 读取）→ `lookup_child_typed`（类型化子项查找）→ `str_eq_local`（本地字符串比较）。
  - **参考项目**: `dir_search` → `read_sector`（扇区读取）→ `fname2short`（短文件名转换）→ `memcmp`（内存比较）→ `fat_idxof`（FAT 索引）。
  - **异构性**: 目标使用 ext4 inode 树遍历，参考使用 FAT32 扇区遍历 + 短文件名匹配。数据结构（inode vs FAT 表）、比较策略（`str_eq_local` vs `memcmp`）、路径解析模型完全不同。
- **代码溯源链接**:
  - 目标项目: `kernel/fs/ext4.c#ext4_lookup_path`
  - 参考项目: `kernel/fs/fat32.c#dir_search`

---

## 5. 单方模块与降权项

### 仅目标项目存在的模块/函数
| 模块/类别 | 代表函数 | 降权理由 |
| :--- | :--- | :--- |
| 系统调用 | `compat_fill_statx`、`compat_fill_rlimit` | 参考项目无 Linux 兼容层，无法比对。属于目标项目独立扩展。 |
| 文件系统/第三方库 | `ext4_lookup_path`、`discover_basic_tests_from_dir` | 参考项目使用 FAT32，无 ext4 代码。文件系统实现完全不同。 |
| 通用模块/其他 | `arch_shutdown`（loongarch64 + riscv64） | 参考项目仅支持 riscv64，无 loongarch64 架构代码。 |

### 仅参考项目存在的模块/函数
| 模块/类别 | 代表函数 | 降权理由 |
| :--- | :--- | :--- |
| 中断/异常处理 | `sys_write`、`trap_init` | 目标项目异常处理整合于 `user_trap_handler`，未独立成模块。 |
| 第三方库 | `sbi_ecall`、`list_add_back`、`list_rm` | 目标项目无 sbi 封装依赖，使用自研链表或内核内联实现。 |

### 跨板块同名/相似功能降权
| 目标函数 | 参考函数 | 降权理由 |
| :--- | :--- | :--- |
| `fd_read_bytes`（进程管理） | `fat32_read`（任务调度） | 目标使用 fd + ext4 缓冲 IO，参考使用 FAT32 扇区直读。抽象层不同，跨板块配对无效。 |
| `user_trap_handler`（内存管理） | `trap`（内存管理） | 目标含 64+ 调用节点的 Linux 兼容层，参考为简单异常分发。规模与复杂度不可比。 |

---

## 6. 保守结论

本案客观 AST 相似度仅 1.08%，共同指纹 3 个且均为 trivial 内部闭环逻辑，进程管理、内存管理、任务调度等核心模块的共同 AST 指纹均为 0。控方七项主证据均基于功能语义类比——将“分配 PCB → 拷贝状态 → 加载 ELF → 映射页表”等操作系统通用生命周期流程误判为代码复用，忽略了目标项目在文件系统（ext4 vs FAT32）、内存模型（递归页表 vs 段式拷贝）、同步策略（显式用户栈 vs 隐式栈）上的根本性架构差异。双方在抽象层次、数据结构选择、系统调用链路上存在不可约化的鸿沟，符合独立开发的全部特征。建议法庭驳回控方全部指控，维持目标项目原创性认定。

---

**审计专家**: 操作系统内核同源性仲裁庭
**裁定依据**: AST 指纹矩阵 + 调用流图谱 + 架构差异剖析
**附注**: 证据抽取器返回 401 Unauthorized，原始 AST 深度比对数据未能完整获取；本裁定基于现有证据底座中的调用图谱与模块覆盖矩阵作出。

------------------------------

