# 🛡️ OS 内核赛自动化代码级架构审计报告

**审计时间**: `2026-07-02 12:38:57`
**参赛作品名称**: `2026-T202610487999998-XX`
**年份**: `2026`
**学校**: `XX`
**队伍名称**: `T202610487999998`
**比赛名字**: `OS-Competition`
**比赛子赛道**: `Kernel-Implementation`
**仓库地址**: `https://gitlab.eduxiji.net/educg-group-43501-3132633/T202610487999998-2931`
**推测开发语言**: `Rust / C (由AST探针自动识别)`
**特征函数量**: `227`

## ⚔️ 底层代码同源性对比 (AST Homology)
> 本部分由专门的代码审查智能体，对该项目与符合年份条件的历届最高相似度项目进行控制流与底层调用对比。新版报告会先给出确定性的全模块覆盖矩阵，再给出 Agent 的分模块解释，避免只围绕少量抽样函数展开。

### 🏆 Top 5 底层对标: vs `2021-电脑配件队-北京大学`（北京大学 / 电脑配件队）

#### 对方作品信息

**对方年份**: `2021`
**对方学校**: `北京大学`
**对方队伍名称**: `电脑配件队`
**对方比赛名字**: `OS-Competition`
**对方比赛子赛道**: `Kernel-Implementation`
**对方仓库地址**: `https://gitlab.eduxiji.net/ckf104/test_commit`

> 双向覆盖率：共同 AST 指纹: 3；唯一 AST 指纹: 目标项目 204，参考项目 105；覆盖率: 目标项目 1.47%，参考项目 2.86%

## 全模块对比总览（确定性证据）

> 总体覆盖率：共同 AST 指纹: 3；唯一 AST 指纹: 目标项目 204，参考项目 105；覆盖率: 目标项目 1.47%，参考项目 2.86%

### 1. 模块覆盖矩阵
| 模块 | 目标函数数 | 参考函数数 | 共同唯一 AST 指纹 | Target 覆盖 | Base 覆盖 | 状态 |
| :--- | ---: | ---: | ---: | ---: | ---: | :--- |
| 进程管理 | 75 | 2 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |
| 任务调度/异步运行时 | 4 | 15 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |
| 内存管理 | 57 | 19 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |
| 中断/异常处理 | 0 | 9 | 0 | 0.0% | 0.0% | 仅参考项目体现 |
| 系统调用 | 8 | 0 | 0 | 0.0% | 0.0% | 仅目标项目体现 |
| 文件系统 | 12 | 10 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |
| 设备驱动 | 37 | 17 | 1 | 3.45% | 8.33% | 双方存在且有结构重合 |
| 网络协议栈 | 0 | 0 | 0 | 0.0% | 0.0% | 双方均未体现 |
| 文件系统/第三方库 | 31 | 0 | 0 | 0.0% | 0.0% | 仅目标项目体现 |
| 通用模块/其他 | 3 | 58 | 1 | 33.33% | 2.17% | 双方存在且有结构重合 |

### 2. 分模块证据清单

#### 进程管理
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `kernel/proc/user.c#fd_write_bytes` | `kernel/src/dmac.c#dmac_wait_idle` | 同模块上下文对齐/差异观察 | fd_pipe, fd_sync_mem_file, memcpy, memset, user_puts | dmac_is_idle, get_proc_lock, proc_lock, rls_proc_lock, sleep |
| `kernel/proc/user.c#fd_read_bytes` | `kernel/src/dmac.c#dmac_wait_done` | 同模块上下文对齐/差异观察 | ext4_read_inode_at, fd_pipe, memcpy, memset | dmac_wait_idle |

#### 任务调度/异步运行时
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `kernel/common/main.c#run_iozone_group` | `kernel/src/sysctl.c#sysctl_clock_device_en` | 同模块上下文对齐/差异观察 | ext4_dir_exists, force_synthetic_lib, kprintf, kputs, lib_file_exists, make_lib_path | 内部闭环逻辑 |
| `kernel/common/main.c#run_ltp_group` | `bootloader/SBI/rustsbi-qemu/src/main.rs#start_trap_rust` | 同模块上下文对齐/差异观察 | emit_ltp_pass, ext4_dir_exists, ext4_file_exists, kprintf, kputs, make_lib_path ...(+2) | clint.get_mtime, get_vaddr_u32, hal::Clint::new, llvm_asm!, mcause::read, mcause::read().cause ...(+9) |
| `kernel/common/main.c#ltp_expected_passes` | `kernel/src/spi.c#spi_receive_data_standard` | 同模块上下文对齐/差异观察 | str_eq | (uint32), spi_get_frame_size, spi_set_tmod |
| `kernel/common/print.c#kprintf` | `kernel/src/spi.c#spi_send_data_normal` | 同模块上下文对齐/差异观察 | kputs, print_u64, put_char, va_arg, va_end, va_start | memmove, spi_get_frame_size, spi_set_tmod |

#### 内存管理
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `kernel/proc/user.c#user_trap_handler` | `bootloader/SBI/rustsbi-k210/src/main.rs#start_trap_rust` | 同模块上下文对齐/差异观察 | apply_loader_path_alias, compat_fill_rlimit, compat_fill_stat, compat_fill_stat_owner, compat_fill_statfs, compat_fill_statx ...(+66) | get_vaddr_u32, k210_hal::clint::mtime::read, llvm_asm!, mcause::read, mcause::read().cause, mepc::read ...(+18) |
| `kernel/common/main.c#emit_basic_result` | `bootloader/SBI/rustsbi-k210/src/main.back.rs#start_trap_rust` | 同模块上下文对齐/差异观察 | basic_end, basic_start, kprintf, kputs, str_eq | devintr, get_vaddr_u32, k210_hal::clint::mtime::read, llvm_asm!, mcause::read, mcause::read().cause ...(+16) |
| `kernel/proc/user.c#user_run_elf_args` | `bootloader/SBI/rustsbi-k210/src/main.back.rs#main` | 同模块上下文对齐/差异观察 | build_user_stack, copy_kernel_cstr, ext4_lookup_path, ext4_read_inode_all, load_elf, map_range_identity ...(+16) | 115_200.bps, ALLOCATOR.lock, ALLOCATOR.lock().init, Clocks::new, boot.clear_interrupt_pending_bits, boot.trigger_on_edge ...(+46) |
| `kernel/common/main.c#run_libctest_group` | `bootloader/SBI/rustsbi-k210/src/main.rs#main` | 同模块上下文对齐/差异观察 | ext4_dir_exists, force_synthetic_lib, kprintf, kputs, lib_file_exists, make_lib_path | 115_200.bps, ALLOCATOR.lock, ALLOCATOR.lock().init, Clocks::new, boot.clear_interrupt_pending_bits, boot.trigger_on_edge ...(+45) |

#### 中断/异常处理
仅参考项目中观察到该模块代表函数，目标项目没有同模块可对齐证据：
- 参考项目：`bootloader/SBI/rustsbi-k210/src/main.back.rs#set_rd`，调用摘要：panic!
- 参考项目：`bootloader/SBI/rustsbi-k210/src/main.rs#set_rd`，调用摘要：panic!
- 参考项目：`kernel/src/dmac.c#dmac_enable_common_interrupt_signal`，调用摘要：readq, writeq
- 参考项目：`kernel/src/dmac.c#dmac_enable_common_interrupt_status`，调用摘要：readq, writeq

#### 系统调用
仅目标项目中观察到该模块代表函数，参考项目没有同模块可对齐证据：
- 目标项目：`kernel/syscall/linux_compat.c#compat_fill_statx`，调用摘要：memset
- 目标项目：`kernel/syscall/linux_compat.c#compat_fill_statx_special`，调用摘要：compat_fill_statx
- 目标项目：`kernel/arch/riscv64/arch.c#sbi_call`，调用摘要：内部闭环逻辑
- 目标项目：`kernel/syscall/linux_compat.c#compat_fill_stat`，调用摘要：memset

#### 文件系统
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `kernel/common/main.c#run_busybox_group` | `bootloader/SBI/rustsbi-qemu/src/main.rs#count_harts` | 同模块上下文对齐/差异观察 | ext4_file_exists, force_synthetic_lib, kprintf, kputs, make_lib_path, str_eq ...(+2) | DeviceTree::load, cluster_node.children.iter, cluster_node.children.iter().count, compiled_max_hartid, core::slice::from_raw_parts, cpu_map_node.children.iter ...(+4) |
| `kernel/common/main.c#kmain` | `bootloader/SBI/rustsbi-qemu/src/main.rs#enumerate_cpu_map` | 同模块上下文对齐/差异观察 | arch_shutdown, block_init, ext4_discover_basic_tests_in, ext4_mount, kprintf, kputs ...(+5) | cluster_node.children.iter, cluster_node.children.iter().count, cpu_map_node.children.iter, println! |
| `kernel/common/main.c#run_cyclictest_group` | `bootloader/SBI/rustsbi-qemu/src/hal/ns16550a.rs#try_read` | 同模块上下文对齐/差异观察 | force_synthetic_lib, kprintf, kputs, lib_file_exists | Err, Ok, read_volatile |
| `kernel/common/main.c#run_iperf_group` | `bootloader/SBI/rustsbi-qemu/src/hal/ns16550a.rs#try_flush` | 同模块上下文对齐/差异观察 | force_synthetic_lib, kprintf, kputs, lib_file_exists | Err, Ok, read_volatile |

#### 设备驱动
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `kernel/common/virtio.c#virtio_notify_queue` | `kernel/src/sdcard.c#sd_lowlevel_init` | 同板块同结构 | vpci_write16 | gpiohs_set_drive_mode |
| `kernel/common/virtio.c#virtio_notify_queue` | `kernel/src/sdcard.c#SD_CS_HIGH` | 同板块同结构 | mmio_write | gpiohs_set_pin |
| `kernel/common/virtio.c#fence_io` | `kernel/src/sdcard.c#SD_CS_LOW` | 同板块同结构 | __sync_synchronize | gpiohs_set_pin |
| `kernel/common/virtio.c#block_read_sector` | `kernel/src/spi.c#spi_receive_data_normal_dma` | 同模块上下文对齐/差异观察 | fence_io, kprintf, memcpy, virtio_ack_used, virtio_cpu_dma_ptr, virtio_dma_addr ...(+3) | (uint32), dmac_set_single_mode, dmac_wait_done, spi_get_frame_format, spi_set_tmod, sysctl_dma_select |

#### 文件系统/第三方库
仅目标项目中观察到该模块代表函数，参考项目没有同模块可对齐证据：
- 目标项目：`kernel/fs/ext4.c#ext4_lookup_path`，调用摘要：inode_size, lookup_child_typed, read_inode, root_or_cwd, str_eq_local
- 目标项目：`kernel/fs/ext4.c#discover_basic_tests_from_dir`，调用摘要：append_named_test, copy_name, ext4_read_inode_prefix, inode_read_at, inode_size, kputs ...(+4)
- 目标项目：`kernel/fs/ext4.c#ext4_extent_physical_block`，调用摘要：block_read, ext4_extent_physical_block, extent_idx_leaf, extent_start
- 目标项目：`kernel/fs/ext4.c#ext4_read_dirent`，调用摘要：copy_name, inode_read_at, inode_size, read_inode

#### 通用模块/其他
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `kernel/arch/riscv64/arch.c#arch_putchar` | `kernel/src/sdcard.c#sd_get_response_R3_rest` | 同板块同结构 | sbi_call | sd_read_data |
| `kernel/arch/loongarch64/arch.c#arch_shutdown` | `kernel/src/dmac.c#dmac_set_channel_param` | 同模块上下文对齐/差异观察 | mmio_write8 | is_memory, readq, writeq |
| `kernel/arch/riscv64/arch.c#arch_shutdown` | `kernel/src/sdcard.c#sdcard_write_sector` | 同模块上下文对齐/差异观察 | sbi_call | acquiresleep, panic, printf, releasesleep, sd_end_cmd, sd_get_response_R1 ...(+4) |


好的，审计官。以下是基于您提供的全量证据、遵循严格审计指令生成的最终同源性定性仲裁报告。

---

#### 1. 宏观判定
- **定性结论**: **独立开发 / 底层架构显著差异**
- **核心依据**: 客观 AST 相似度仅为 0.98%，共同 AST 指纹仅有 3 个，且均位于非核心的通用惯用法层面。目标项目与参考项目在系统架构、功能完整性、核心抽象与依赖生态上存在根本性、系统性的差异，未发现任何足以构成“同板块底层逻辑复用”的强证据。

#### 2. 全模块覆盖矩阵解读
- **进程管理**
  - 双方存在性：双方均存在。
  - 共同 AST 指纹：0。
  - 覆盖率解读：目标项目覆盖率 0.0%，参考项目覆盖率 0.0%。目标项目实现了基于文件描述符的字节流读写（`fd_write_bytes`），而参考项目仅包含 DMA 控制器的等待与锁原语（`dmac_wait_idle`），两者在抽象层次和实现路径上完全不同。
  - 证据强度：**未形成**。

- **任务调度/异步运行时**
  - 双方存在性：双方均存在。
  - 共同 AST 指纹：0。
  - 覆盖率解读：目标项目覆盖率 0.0%，参考项目覆盖率 0.0%。目标项目实现了复杂的用户态测试集调度（LTP, iozone），依赖 ext4 文件系统；参考项目则为裸机中断处理（`start_trap_rust`）和 SPI 设备通信，两者在调度对象和运行环境上毫无交集。
  - 证据强度：**未形成**。

- **内存管理**
  - 双方存在性：双方均存在。
  - 共同 AST 指纹：0。
  - 覆盖率解读：目标项目覆盖率 0.0%，参考项目覆盖率 0.0%。目标项目具备完整的用户态页表管理、ELF 加载和进程克隆机制（`copy_user_page_table_level`）；参考项目仅停留在 SBI 启动期的物理内存分配（`kalloc`）和内联汇编读取虚拟地址阶段。
  - 证据强度：**未形成**。

- **中断/异常处理**
  - 双方存在性：仅参考项目体现。
  - 共同 AST 指纹：0。
  - 覆盖率解读：目标项目无此独立模块证据，参考项目有 9 个函数。目标项目的中断处理逻辑已融入 `user_trap_handler` 等内存管理/系统调用路径中。
  - 证据强度：**未形成**。

- **系统调用**
  - 双方存在性：仅目标项目体现。
  - 共同 AST 指纹：0。
  - 覆盖率解读：目标项目有 8 个函数，实现了 Linux 兼容系统调用层（`compat_fill_statx`）；参考项目无此模块。
  - 证据强度：**未形成**。

- **文件系统**
  - 双方存在性：双方均存在。
  - 共同 AST 指纹：0。
  - 覆盖率解读：目标项目覆盖率 0.0%，参考项目覆盖率 0.0%。目标项目已对接 ext4 文件系统并支持 busybox 等复杂应用；参考项目仅涉及设备树解析和 UART 寄存器读写，无文件系统逻辑。
  - 证据强度：**未形成**。

- **设备驱动**
  - 双方存在性：双方均存在。
  - 共同 AST 指纹：1。
  - 覆盖率解读：目标项目覆盖率 3.45%，参考项目覆盖率 8.33%。共同指纹源于极浅层的单条寄存器写入或内存屏障操作（`vpci_write16` vs `gpiohs_set_drive_mode`），属于硬件驱动领域的通用惯用法。
  - 证据强度：**弱**。

- **网络协议栈**
  - 双方存在性：双方均未体现。
  - 共同 AST 指纹：0。
  - 覆盖率解读：无。
  - 证据强度：**未形成**。

- **第三方库/公共依赖**
  - 双方存在性：仅目标项目体现（`文件系统/第三方库` 模块，31个函数）。
  - 共同 AST 指纹：0。
  - 覆盖率解读：目标项目包含 ext4 等第三方库代码，参考项目无此模块。
  - 证据强度：**未形成**。

#### 3. 底层 AST 关键证据指纹表
| 所属板块 | 目标项目路径 | 参考项目路径 | 证据类型 | 技术说明 |
| :--- | :--- | :--- | :--- | :--- |
| 设备驱动 | `kernel/common/virtio.c#virtio_notify_queue` | `kernel/src/sdcard.c#sd_lowlevel_init` | 弱信号-通用模式 | 均为单次寄存器写入调用（`vpci_write16` vs `gpiohs_set_drive_mode`），是底层硬件操作的通用封装，不构成独创逻辑。 |
| 通用模块/其他 | `kernel/arch/riscv64/arch.c#arch_putchar` | `kernel/src/sdcard.c#sd_get_response_R3_rest` | 弱信号-通用模式 | 均为单函数调用包装器（`sbi_call` vs `sd_read_data`），是底层操作封装的常见模式。 |
| 设备驱动 | `kernel/common/virtio.c#fence_io` | `kernel/src/sdcard.c#SD_CS_LOW` | 弱信号-通用模式 | 编译器内建函数/宏（`__sync_synchronize`）与 GPIO 引脚操作，无逻辑关联。 |

#### 4. 核心代码调用流对比剖析
* **【内存管理/进程地址空间】**
* **底层技术析理**: 目标项目 `user_execve_current` 的调用流展示了现代内核的完整 `execve` 路径：从 ext4 文件系统查找和读取 ELF 文件，到构建用户栈、映射内存区域、加载 ELF 镜像，最后释放旧页表。其核心依赖是 `ext4_lookup_path`、`load_elf`、`map_user_range` 等高级抽象。参考项目 `main` 函数则是一个典型的裸机启动流：初始化全局分配器锁、配置系统时钟、设置 GPIO 功能引脚、使能中断，最后进入特权模式。其核心依赖是 `ALLOCATOR.lock().init`、`Clocks::new`、`fpioa.io16.into_function` 等 SoC 特定硬件抽象。两者在内存模型（虚拟内存 vs 物理内存）、抽象层次（文件系统与 ELF vs 寄存器与外设）和同步策略上完全不同。
* **代码溯源链接**:
  - 目标项目: `kernel/proc/user.c#user_execve_current`
  - 参考项目: `bootloader/SBI/rustsbi-k210/src/main.rs#main`

* **【文件系统/内核主路径】**
* **底层技术析理**: 目标项目 `kmain` 函数展示了挂载 ext4 文件系统、发现并运行测试用例的完整初始化流程，其调用流涉及 `ext4_mount`、`ext4_discover_basic_tests_in` 等文件系统操作。参考项目 `enumerate_cpu_map` 函数则是通过解析设备树（DeviceTree）来枚举 CPU 核心，其调用流围绕 `DeviceTree::load` 和迭代器展开。一个是面向持久化存储的文件系统操作，一个是面向硬件描述的数据结构解析，两者解决的问题域和依赖对象完全不同。
* **代码溯源链接**:
  - 目标项目: `kernel/common/main.c#kmain`
  - 参考项目: `bootloader/SBI/rustsbi-qemu/src/main.rs#enumerate_cpu_map`

#### 5. 单方模块与降权项
- **单方模块**：目标项目的【系统调用】和【文件系统/第三方库】模块，参考项目的【中断/异常处理】模块，均为对方所无。这直接证明两个项目处于完全不同的开发阶段和架构目标，不存在代码复用基础。
- **降权项说明**：
  - **跨板块硬凑**：控方证据中将目标项目【内存管理】的 `user_trap_handler` 与参考项目【任务调度/异步运行时】的 `start_trap_rust` 进行关联，仅因字面均含“trap”。两者一个处理用户态系统调用，一个处理 M-mode 异常，属于跨抽象层级的无效关联，已降级为弱信号。
  - **通用模式**：`kprintf`、`memcpy`、`memset`、`str_eq`、`__sync_synchronize`、单条寄存器读写等属于 C 标准库、编译器内建函数或硬件交互的通用惯用法，不具备项目特异性，不能作为同源证据。
  - **第三方库**：目标项目中的 ext4 文件系统实现属于第三方库/公共依赖范畴，其存在本身不能作为与其他项目比对的独创性证据。

#### 6. 保守结论
本次审计基于 0.98% 的极低 AST 相似度和 3 个仅存在于通用惯用法层面的共同指纹，认定 [2026-T202610487999998-XX] 与 [2021-电脑配件队-北京大学] 不存在代码复用或抄袭。两个项目在架构目标、功能完整性和核心依赖上存在根本性差异：目标项目是一个面向现代虚拟化环境、具备完整文件系统和用户态支持的多架构内核；参考项目则是一个面向特定 SoC 的裸机启动与外设驱动集合。所有核心模块的共同 AST 指纹均为零，控辩双方的分析也一致指向独立开发。因此，整体抄袭风险极低，应驳回指控。

------------------------------

