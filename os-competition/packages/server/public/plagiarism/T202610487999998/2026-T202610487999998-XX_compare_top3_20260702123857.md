# 🛡️ OS 内核赛自动化代码级架构审计报告

**审计时间**: `2026-07-02 12:38:57`
**参赛作品名称**: `2026-T202610487999998-XX`
**年份**: `2026`
**学校**: `XX`
**队伍名称**: `T202610487999998`
**比赛名字**: `OS-Competition`
**比赛子赛道**: `Kernel-Implementation`
**仓库地址**: `https://gitlab.eduxiji.net/educg-group-43501-3132633/T202610487999998-2931`
**推测开发语言**: `Rust / C (由AST探针自动识别)`
**特征函数量**: `227`

## ⚔️ 底层代码同源性对比 (AST Homology)
> 本部分由专门的代码审查智能体，对该项目与符合年份条件的历届最高相似度项目进行控制流与底层调用对比。新版报告会先给出确定性的全模块覆盖矩阵，再给出 Agent 的分模块解释，避免只围绕少量抽样函数展开。

### 🏆 Top 3 底层对标: vs `2023-接下来我要表扬一款游戏-广东工业大学`（广东工业大学 / 接下来我要表扬一款游戏）

#### 对方作品信息

**对方年份**: `2023`
**对方学校**: `广东工业大学`
**对方队伍名称**: `接下来我要表扬一款游戏`
**对方比赛名字**: `OS-Competition`
**对方比赛子赛道**: `Kernel-Implementation`
**对方仓库地址**: `https://gitlab.eduxiji.net/202311845101096/oskernel2023-gdut`

> 双向覆盖率：共同 AST 指纹: 4；唯一 AST 指纹: 目标项目 204，参考项目 179；覆盖率: 目标项目 1.96%，参考项目 2.23%

## 全模块对比总览（确定性证据）

> 总体覆盖率：共同 AST 指纹: 4；唯一 AST 指纹: 目标项目 204，参考项目 179；覆盖率: 目标项目 1.96%，参考项目 2.23%

### 1. 模块覆盖矩阵
| 模块 | 目标函数数 | 参考函数数 | 共同唯一 AST 指纹 | Target 覆盖 | Base 覆盖 | 状态 |
| :--- | ---: | ---: | ---: | ---: | ---: | :--- |
| 进程管理 | 75 | 29 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |
| 任务调度/异步运行时 | 4 | 21 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |
| 内存管理 | 57 | 67 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |
| 中断/异常处理 | 0 | 20 | 0 | 0.0% | 0.0% | 仅参考项目体现 |
| 系统调用 | 8 | 12 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |
| 文件系统 | 12 | 49 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |
| 设备驱动 | 37 | 7 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |
| 网络协议栈 | 0 | 0 | 0 | 0.0% | 0.0% | 双方均未体现 |
| 文件系统/第三方库 | 31 | 0 | 0 | 0.0% | 0.0% | 仅目标项目体现 |
| 通用模块/其他 | 3 | 11 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |

### 2. 分模块证据清单

#### 进程管理
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `kernel/proc/user.c#fd_write_bytes` | `kernel/process/fork.c#copy_thread` | 同模块上下文对齐/差异观察 | fd_pipe, fd_sync_mem_file, memcpy, memset, user_puts | get_current_task, get_pt_reg, memset |
| `kernel/proc/user.c#fd_read_bytes` | `kernel/process/schedule.c#__schedule` | 同模块上下文对齐/差异观察 | ext4_read_inode_at, fd_pipe, memcpy, memset | get_current_task, irq_disable, schedule_debug, schedule_tail, simple_sched_class.dequeue_task, simple_sched_class.pick_next_task ...(+1) |
| `kernel/proc/user.c#proc_self_status_data` | `kernel/process/schedule.c#pick_next_task_simple` | 同模块上下文对齐/差异观察 | str_append, str_append_u64 | container_of, reset_score |
| `kernel/proc/user.c#resolve_loader_path` | `kernel/process/spinlock.c#spin_lock` | 同模块上下文对齐/差异观察 | copy_kernel_cstr, str_contains, str_eq | csr_clr, csr_set |

#### 任务调度/异步运行时
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `kernel/common/main.c#run_iozone_group` | `lib/printk.c#vsprintf` | 同模块上下文对齐/差异观察 | ext4_dir_exists, force_synthetic_lib, kprintf, kputs, lib_file_exists, make_lib_path | is_digit, number, skip_atoi, strlen, va_arg |
| `kernel/common/main.c#run_ltp_group` | `kernel/driver/virtio.c#virtio_dev_init` | 同模块上下文对齐/差异观察 | emit_ltp_pass, ext4_dir_exists, ext4_file_exists, kprintf, kputs, make_lib_path ...(+2) | printk, readword, refresh_cache, virtio_blk_init_legacy, writeword |
| `kernel/common/main.c#ltp_expected_passes` | `kernel/fs/loader.c#initElf` | 同模块上下文对齐/差异观察 | str_eq | LOADER_SEEK_SET, filp->f_ops->lseek, filp->f_ops->read, get_current_task, memcpy, printk ...(+1) |
| `kernel/common/print.c#kprintf` | `kernel/trap/trap.c#do_exception` | 同模块上下文对齐/差异观察 | kputs, print_u64, put_char, va_arg, va_end, va_start | do_trap_error, handle_plic_irq, handle_timer, irq_enable, is_interrupt_fault, printk ...(+1) |

#### 内存管理
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `kernel/proc/user.c#load_elf` | `kernel/fs/loader.c#load_elf` | 同板块同名 | align_up_u64, elf_interp_path, elf_is_valid, load_elf_image, read_ext4_file_path, resolve_loader_path | get_current_task, initElf, loadSymbols, load_segment, memset, printf ...(+1) |
| `kernel/proc/user.c#page_alloc` | `kernel/memory/page_alloc.c#page_alloc` | 同板块同名 | memset | 内部闭环逻辑 |
| `kernel/proc/user.c#page_free` | `kernel/memory/page_alloc.c#page_free` | 同板块同名 | 内部闭环逻辑 | 内部闭环逻辑 |
| `kernel/proc/user.c#user_trap_handler` | `kernel/fs/fat32.c#FAT32_lookup` | 同模块上下文对齐/差异观察 | apply_loader_path_alias, compat_fill_rlimit, compat_fill_stat, compat_fill_stat_owner, compat_fill_statfs, compat_fill_statx ...(+66) | alloc_pgtable, memset, page_free_addr, printk, read_FAT_Entry, read_more_sector |

#### 中断/异常处理
仅参考项目中观察到该模块代表函数，目标项目没有同模块可对齐证据：
- 参考项目：`kernel/trap/trap.c#show_regs`，调用摘要：printk
- 参考项目：`kernel/trap/stacktrace.c#search_stackframe`，调用摘要：__builtin_frame_address, kernel_text, printk
- 参考项目：`kernel/trap/plic.c#handle_uart_irq`，调用摘要：printk, uart_get, uart_send
- 参考项目：`kernel/driver/virtio.c#virtio_disk_intr`，调用摘要：__sync_synchronize, printk, readword, writeword

#### 系统调用
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `kernel/syscall/linux_compat.c#compat_fill_statx` | `usr/user_syscall.c#internal_syscall` | 同模块上下文对齐/差异观察 | memset | 内部闭环逻辑 |
| `kernel/syscall/linux_compat.c#compat_fill_statx_special` | `usr/user_syscall.c#printf` | 同模块上下文对齐/差异观察 | compat_fill_statx | SYSCALL_1, va_end, va_start, vsprintf |
| `kernel/arch/riscv64/arch.c#sbi_call` | `usr/user_syscall.c#print` | 同模块上下文对齐/差异观察 | 内部闭环逻辑 | SYSCALL_1, va_end, va_start, vsprintf |
| `kernel/syscall/linux_compat.c#compat_fill_stat` | `usr/user_syscall.c#clone` | 同模块上下文对齐/差异观察 | memset | __clone |

#### 文件系统
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `kernel/common/main.c#run_busybox_group` | `kernel/fs/loader.c#placeInfo` | 同模块上下文对齐/差异观察 | ext4_file_exists, force_synthetic_lib, kprintf, kputs, make_lib_path, str_eq ...(+2) | loadSecData, strcmp |
| `kernel/common/main.c#kmain` | `kernel/fs/bufio.c#bget` | 同模块上下文对齐/差异观察 | arch_shutdown, block_init, ext4_discover_basic_tests_in, ext4_mount, kprintf, kputs ...(+5) | acquire_sleeplock, container_of, printk, spin_lock, spin_unlock |
| `kernel/common/main.c#run_cyclictest_group` | `kernel/fs/fat32.c#write_FAT_Entry_test` | 同模块上下文对齐/差异观察 | force_synthetic_lib, kprintf, kputs, lib_file_exists | bread, brelease, bwrite |
| `kernel/common/main.c#run_iperf_group` | `kernel/fs/log.c#end_log` | 同模块上下文对齐/差异观察 | force_synthetic_lib, kprintf, kputs, lib_file_exists | commit, printk, spin_lock, spin_unlock, wakeup |

#### 设备驱动
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `kernel/common/virtio.c#block_read_sector` | `kernel/urat.c#uart_init` | 同模块上下文对齐/差异观察 | fence_io, kprintf, memcpy, virtio_ack_used, virtio_cpu_dma_ptr, virtio_dma_addr ...(+3) | writebyte |
| `kernel/common/virtio.c#block_init` | `kernel/driver/virtio.c#free_chain` | 同模块上下文对齐/差异观察 | kputs, la_init_legacy_pci, pci_read16 | free_desc |
| `kernel/arch/loongarch64/arch.c#arch_putchar` | `lib/printk.c#print_f` | 同模块上下文对齐/差异观察 | mmio_read8, mmio_write8 | uart_send_string, va_end, va_start, vsprintf |
| `kernel/common/virtio.c#pci_cfg_addr` | `kernel/driver/virtio.c#free_desc` | 同模块上下文对齐/差异观察 | 内部闭环逻辑 | 内部闭环逻辑 |

#### 文件系统/第三方库
仅目标项目中观察到该模块代表函数，参考项目没有同模块可对齐证据：
- 目标项目：`kernel/fs/ext4.c#ext4_lookup_path`，调用摘要：inode_size, lookup_child_typed, read_inode, root_or_cwd, str_eq_local
- 目标项目：`kernel/fs/ext4.c#discover_basic_tests_from_dir`，调用摘要：append_named_test, copy_name, ext4_read_inode_prefix, inode_read_at, inode_size, kputs ...(+4)
- 目标项目：`kernel/fs/ext4.c#ext4_extent_physical_block`，调用摘要：block_read, ext4_extent_physical_block, extent_idx_leaf, extent_start
- 目标项目：`kernel/fs/ext4.c#ext4_read_dirent`，调用摘要：copy_name, inode_read_at, inode_size, read_inode

#### 通用模块/其他
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `kernel/arch/loongarch64/arch.c#arch_shutdown` | `lib/printk.c#number` | 同模块上下文对齐/差异观察 | mmio_write8 | do_div |
| `kernel/arch/riscv64/arch.c#arch_shutdown` | `lib/lib.c#strcmp` | 同模块上下文对齐/差异观察 | sbi_call | 内部闭环逻辑 |
| `kernel/arch/riscv64/arch.c#arch_putchar` | `lib/lib.c#strcpy` | 同模块上下文对齐/差异观察 | sbi_call | 内部闭环逻辑 |


# 最终同源性定性仲裁报告

**案号**: 2026-T202610487999998-XX vs 2023-接下来我要表扬一款游戏-广东工业大学
**审计日期**: 2026年
**审计依据**: 客观AST相似度、双向覆盖率、模块覆盖矩阵、原始证据底座

---

## 1. 宏观判定

- **定性结论**: **独立开发** (相似度: 1.06%，共同 AST 指纹: 4；唯一 AST 指纹: 目标项目 204，参考项目 179；覆盖率: 目标项目 1.96%，参考项目 2.23%)

**判定依据**: 双方共同 AST 指纹仅 4 个，占各自全部指纹的比例均不足 2.5%。全模块覆盖矩阵显示，所有核心模块（进程管理、任务调度、内存管理、系统调用、文件系统、设备驱动）的共同唯一 AST 指纹数均为 0。目标项目拥有 204 个独立 AST 指纹，参考项目拥有 179 个，表明两套代码在底层结构上具有压倒性的独立性。控方提出的三项“核心铁证”均因源文件归属错位、调用图谱差异显著、或依赖行业通用范式而被降权或否定。

---

## 2. 全模块覆盖矩阵解读

### 进程管理
- **双方存在性**: 双方均存在（目标 75 函数，参考 29 函数）
- **共同 AST 指纹**: 0
- **覆盖率**: 目标 0.0%，参考 0.0%
- **证据强度**: **未形成**

目标项目的进程管理围绕 `fd_*`（文件描述符操作）、`mem_*`（内存文件系统）、`proc_self_*`（procfs 兼容层）构建，重度依赖 ext4 文件系统交互；参考项目则是传统的 `schedule`、`spinlock`、`sleeplock`、`fork` 体系。两者在进程管理的抽象层次和实现路径上完全不同，未观察到任何共同结构指纹。

### 任务调度/异步运行时
- **双方存在性**: 双方均存在（目标 4 函数，参考 21 函数）
- **共同 AST 指纹**: 0
- **覆盖率**: 目标 0.0%，参考 0.0%
- **证据强度**: **未形成**

目标项目的调度相关函数集中在 `kernel/common/main.c` 的测试组运行器（`run_iozone_group`、`run_ltp_group`），属于测试编排逻辑；参考项目则是完整的内核调度器实现（`__schedule`、`pick_next_task_simple`、`preempt_schedule`）。两者在功能定位和调用图谱上无交集。

### 内存管理
- **双方存在性**: 双方均存在（目标 57 函数，参考 67 函数）
- **共同 AST 指纹**: 0
- **覆盖率**: 目标 0.0%，参考 0.0%
- **证据强度**: **弱**（仅存在同名函数，但调用图谱不同且源文件归属不同）

控方指控的 `page_free`、`page_alloc`、`load_elf` 均位于此模块。然而：
- `page_free` 和 `page_alloc` 在目标项目中位于 `kernel/proc/user.c`（进程用户态交互），在参考项目中位于 `kernel/memory/page_alloc.c`（物理页分配器），源文件归属不同子系统。
- `load_elf` 在目标项目中调用 `read_ext4_file_path`、`elf_interp_path` 等 ext4 相关函数，在参考项目中调用 `initElf`、`loadSymbols`、`filp->f_ops->read` 等通用文件接口，调用图谱完全不同。
- 模块矩阵显示共同唯一 AST 指纹为 0，不支持底层逻辑复用的指控。

### 中断/异常处理
- **双方存在性**: 仅参考项目存在（20 函数），目标项目无此模块
- **共同 AST 指纹**: 0
- **覆盖率**: 目标 0.0%，参考 0.0%
- **证据强度**: **未形成**（单方模块）

目标项目未体现独立的中断/异常处理模块，参考项目拥有 `show_regs`、`search_stackframe`、`handle_uart_irq` 等函数。此为架构差异，不构成同源证据。

### 系统调用
- **双方存在性**: 双方均存在（目标 8 函数，参考 12 函数）
- **共同 AST 指纹**: 0
- **覆盖率**: 目标 0.0%，参考 0.0%
- **证据强度**: **未形成**

目标项目的系统调用层围绕 Linux 兼容（`compat_fill_statx`、`compat_fill_stat`）和跨架构抽象（`sbi_call`）构建；参考项目则是简单的 `internal_syscall`、`clone`、`sleep_proc`。调用图谱无交集。

### 文件系统
- **双方存在性**: 双方均存在（目标 12 函数，参考 49 函数）
- **共同 AST 指纹**: 0
- **覆盖率**: 目标 0.0%，参考 0.0%
- **证据强度**: **未形成**

目标项目使用 ext4 文件系统（`ext4_lookup_path`、`ext4_read_inode_at`），参考项目使用 FAT32（`fat32.c`）和 bufio 层。两者在文件系统选型和实现上完全不同。

### 设备驱动
- **双方存在性**: 双方均存在（目标 37 函数，参考 7 函数）
- **共同 AST 指纹**: 0
- **覆盖率**: 目标 0.0%，参考 0.0%
- **证据强度**: **未形成**

目标项目拥有完整的 Virtio PCI/legacy 驱动（`virtio.c`、`pci_cfg_addr`、`block_read_sector`），参考项目仅有简单的 UART 和 Virtio blk legacy 初始化。调用图谱无交集。

### 网络协议栈
- **双方存在性**: 双方均不存在
- **共同 AST 指纹**: 0
- **覆盖率**: 目标 0.0%，参考 0.0%
- **证据强度**: **未形成**

### 第三方库/公共依赖
- **双方存在性**: 仅目标项目存在（31 函数），参考项目无此独立模块
- **共同 AST 指纹**: 0
- **覆盖率**: 目标 0.0%，参考 0.0%
- **证据强度**: **未形成**（单方模块）

目标项目拥有独立的 ext4 文件系统实现（`ext4.c`），参考项目无此模块。此为架构差异。

---

## 3. 底层 AST 关键证据指纹表

| 所属板块 | 目标项目路径 | 参考项目路径 | 证据类型 | 技术说明 |
| :--- | :--- | :--- | :--- | :--- |
| 内存管理 | `kernel/proc/user.c#page_free` | `kernel/memory/page_alloc.c#page_free` | 弱信号-同名函数 | 同为“内部闭环逻辑”，但源文件分属进程管理与内存管理子系统；无外部依赖是页释放的常见实现形态，非独特指纹 |
| 内存管理 | `kernel/proc/user.c#page_alloc` | `kernel/memory/page_alloc.c#page_alloc` | 弱信号-同名函数 | 目标调用 `memset`，参考为内部闭环；`memset` 是页分配后清零的标准操作，体现功能差异而非伪装 |
| 内存管理 | `kernel/proc/user.c#load_elf` | `kernel/fs/loader.c#load_elf` | 弱信号-同名函数 | 目标依赖 ext4（`read_ext4_file_path`），参考依赖通用文件指针（`filp->f_ops->read`）；调用图谱完全不同 |
| 进程管理 | `kernel/proc/user.c#proc_exit_current` | `kernel/trap/syscall.c#callback_sys_exit` | 弱信号-跨文件映射 | 同属进程管理但调用图谱无交集（目标: `copy_to_user, proc_save_globals`；参考: `get_current_task, printk`） |
| 任务调度 | `kernel/common/print.c#kprintf` | `lib/printk.c#vsprintf` | 弱信号-通用范式 | 可变参数解析（`va_arg`）是 C 标准库范式，属不可保护的表达 |

**说明**: 上表所列均为弱信号证据。双方不存在任何满足“同板块+同调用图谱+同源文件归属”三重条件的强同源证据。所有同名函数均因调用图谱差异或源文件归属错位而被降权。

---

## 4. 核心代码调用流对比剖析

### 【进程管理】进程退出路径

- **底层技术析理**: 目标项目的进程退出（`proc_exit_current`）通过 `copy_to_user` 向用户空间回传状态、`proc_save_globals` 保存全局状态、`proc_wake_waiter_for` 唤醒等待者，呈现用户态兼容层的设计特征。参考项目的进程退出（`callback_sys_exit`）仅调用 `get_current_task` 获取当前任务、`printk` 输出日志，属于最小教学内核的简单实现。两者在内存模型（用户态交互 vs 内核态日志）、同步策略（等待者唤醒 vs 无同步）上完全不同。

- **代码溯源链接**:
  - 目标项目: `kernel/proc/user.c#proc_exit_current`
  - 参考项目: `kernel/trap/syscall.c#callback_sys_exit`

### 【内存管理】ELF 加载路径

- **底层技术析理**: 目标项目的 `load_elf` 调用链为 `elf_is_valid` → `elf_interp_path` → `resolve_loader_path` → `load_elf_image` → `read_ext4_file_path`，完全围绕 ext4 文件系统和解释器路径解析构建。参考项目的 `load_elf` 调用链为 `initElf` → `load_segment` → `loadSymbols` → `filp->f_ops->read`，基于通用文件指针和符号加载。两者在文件系统抽象层（ext4 专有 vs 通用 VFS）、加载粒度（段加载+解释器解析 vs 段加载+符号加载）上存在根本性差异。

- **代码溯源链接**:
  - 目标项目: `kernel/proc/user.c#load_elf`
  - 参考项目: `kernel/fs/loader.c#load_elf`

### 【系统调用】统计信息填充路径

- **底层技术析理**: 目标项目的 `compat_fill_statx` 调用 `memset` 清零结构体后填充，属于 Linux statx 系统调用的兼容实现。参考项目的 `internal_syscall` 为内部闭环逻辑，是简单的系统调用分发器。两者在系统调用语义（Linux 兼容 vs 自定义）和实现复杂度上完全不同。

- **代码溯源链接**:
  - 目标项目: `kernel/syscall/linux_compat.c#compat_fill_statx`
  - 参考项目: `usr/user_syscall.c#internal_syscall`

### 【设备驱动】块设备读取路径

- **底层技术析理**: 目标项目的 `block_read_sector` 调用 `virtio_ack_used`、`virtio_cpu_dma_ptr`、`virtio_dma_addr`、`fence_io` 等 Virtio PCI 驱动接口，呈现完整的 DMA 和内存屏障管理。参考项目的 `virtio_dev_init` 调用 `readword`、`writeword`、`refresh_cache`、`virtio_blk_init_legacy`，属于 Virtio legacy MMIO 初始化。两者在驱动模型（PCI DMA vs MMIO legacy）和操作粒度（扇区读取 vs 设备初始化）上完全不同。

- **代码溯源链接**:
  - 目标项目: `kernel/common/virtio.c#block_read_sector`
  - 参考项目: `kernel/driver/virtio.c#virtio_dev_init`

---

## 5. 单方模块与降权项

### 仅目标项目存在的模块
- **文件系统/第三方库**（31 函数）: 包含完整的 ext4 文件系统实现（`ext4_lookup_path`、`ext4_read_inode_at`、`ext4_extent_physical_block`、`discover_basic_tests_from_dir` 等）。参考项目使用 FAT32，无 ext4 支持。此为独立架构选择，不能作为同源证据。

### 仅参考项目存在的模块
- **中断/异常处理**（20 函数）: 包含 `show_regs`、`search_stackframe`、`handle_uart_irq`、`virtio_disk_intr` 等。目标项目未体现独立的中断处理模块。此为架构差异。

### 降权项说明
以下元素因属于行业通用范式或跨板块映射，已降权处理，不构成强同源证据：

1. **同名函数**（`page_free`、`page_alloc`、`load_elf`）: 函数名由领域规范决定（页分配器、ELF 加载器），且调用图谱均不同。
2. **“内部闭环逻辑”**: 在双方项目中均大量出现（参考项目: `find_pid`、`get_pt_reg`、`task_tick_simple`、`spin_init`、`wakeup`；目标项目: `canonicalize_abs_path`、`mmap_track`、`path_has_proc_fdinfo_shape`），属于基础操作的常见形态，不具备指纹特异性。
3. **通用原语**（`memcpy`、`memset`、`strlen`、`va_arg`、`printk`/`kprintf`）: 属于 C 语言标准库或内核开发的基础设施，不可作为抄袭证据。
4. **跨板块映射**: 控方将目标项目 `kernel/proc/user.c` 中的函数与参考项目 `kernel/memory/page_alloc.c`、`kernel/fs/loader.c` 中的函数对齐，忽视了源文件路径所反映的真实子系统归属，此类跨文件映射不得作为强证据。

---

## 6. 保守结论

基于客观 AST 证据，目标项目 `[2026-T202610487999998-XX]` 与参考项目 `[2023-接下来我要表扬一款游戏-广东工业大学]` 的相似度为 1.06%，共同 AST 指纹仅 4 个，覆盖率均不足 2.5%。全模块覆盖矩阵显示所有核心模块的共同唯一 AST 指纹数均为 0。控方提出的三项“核心铁证”——`page_free` 的“幽灵闭环”、`page_alloc` 的“伪装性复用”、`load_elf` 的“外壳重构”——均因源文件归属错位、调用图谱显著差异、或依赖行业通用范式而被否定。目标项目在文件系统（ext4 vs FAT32）、进程管理（procfs 兼容层 vs 简单调度器）、架构支持（多架构 vs 单 RISC-V）、测试基础设施（LTP/busybox 测试组 vs 无）等维度上呈现独立演化痕迹，无法通过“在参考项目基础上混淆重构”解释。综合判定：**双方为独立开发，不存在应追究的同源性复用**。

------------------------------

