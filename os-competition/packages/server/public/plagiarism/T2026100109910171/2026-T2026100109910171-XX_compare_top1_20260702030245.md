# 🛡️ OS 内核赛自动化代码级架构审计报告

**审计时间**: `2026-07-02 03:02:45`
**参赛作品名称**: `2026-T2026100109910171-XX`
**年份**: `2026`
**学校**: `XX`
**队伍名称**: `T2026100109910171`
**比赛名字**: `OS-Competition`
**比赛子赛道**: `Kernel-Implementation`
**仓库地址**: `https://gitlab.eduxiji.net/educg-group-43501-3132633/T2026100109910171-3181`
**推测开发语言**: `Rust / C (由AST探针自动识别)`
**特征函数量**: `1073`

## ⚔️ 底层代码同源性对比 (AST Homology)
> 本部分由专门的代码审查智能体，对该项目与符合年份条件的历届最高相似度项目进行控制流与底层调用对比。新版报告会先给出确定性的全模块覆盖矩阵，再给出 Agent 的分模块解释，避免只围绕少量抽样函数展开。

### 🏆 Top 1 底层对标: vs `2025-StarryMix-清华大学`（清华大学 / Starry Mix）

#### 对方作品信息

**对方年份**: `2025`
**对方学校**: `清华大学`
**对方队伍名称**: `Starry Mix`
**对方比赛名字**: `OS-Competition`
**对方比赛子赛道**: `Kernel-Implementation`
**对方仓库地址**: `local-upload://starry-mix`

> 双向覆盖率：共同 AST 指纹: 369；唯一 AST 指纹: 目标项目 940，参考项目 1503；覆盖率: 目标项目 39.26%，参考项目 24.55%

## 全模块对比总览（确定性证据）

> 总体覆盖率：共同 AST 指纹: 369；唯一 AST 指纹: 目标项目 940，参考项目 1503；覆盖率: 目标项目 39.26%，参考项目 24.55%

### 1. 模块覆盖矩阵
| 模块 | 目标函数数 | 参考函数数 | 共同唯一 AST 指纹 | Target 覆盖 | Base 覆盖 | 状态 |
| :--- | ---: | ---: | ---: | ---: | ---: | :--- |
| 进程管理 | 87 | 56 | 22 | 27.85% | 42.31% | 双方存在且有结构重合 |
| 任务调度/异步运行时 | 225 | 282 | 43 | 20.19% | 16.23% | 双方存在且有结构重合 |
| 内存管理 | 156 | 273 | 54 | 37.24% | 21.6% | 双方存在且有结构重合 |
| 中断/异常处理 | 3 | 8 | 2 | 66.67% | 25.0% | 双方存在且有结构重合 |
| 系统调用 | 144 | 78 | 10 | 7.46% | 13.33% | 双方存在且有结构重合 |
| 文件系统 | 443 | 562 | 174 | 46.28% | 37.18% | 双方存在且有结构重合 |
| 设备驱动 | 0 | 120 | 0 | 0.0% | 0.0% | 仅参考项目体现 |
| 网络协议栈 | 0 | 138 | 0 | 0.0% | 0.0% | 仅参考项目体现 |
| 文件系统/第三方库 | 0 | 37 | 0 | 0.0% | 0.0% | 仅参考项目体现 |
| 通用模块/其他 | 15 | 158 | 2 | 33.33% | 1.49% | 双方存在且有结构重合 |

### 2. 分模块证据清单

#### 进程管理
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `kernel/src/pseudofs/proc.rs#lookup_child` | `api/src/vfs/proc.rs#lookup_child` | 同板块同名同结构 | Arc::downgrade, Arc::new, CStr::from_bytes_until_nul, CStr::from_bytes_until_nul(&input)
                                    .map_err, CStr::from_bytes_until_nul(&input)
                                    .map_err(|_| VfsError::InvalidInput)?
                                    .to_str, CStr::from_bytes_until_nul(&input)
                                    .map_err(|_| VfsError::InvalidInput)?
                                    .to_str()
                                    .map_err ...(+55) | Arc::downgrade, Arc::new, CStr::from_bytes_until_nul, CStr::from_bytes_until_nul(&input)
                                    .map_err, CStr::from_bytes_until_nul(&input)
                                    .map_err(|_| VfsError::EINVAL)?
                                    .to_str, CStr::from_bytes_until_nul(&input)
                                    .map_err(|_| VfsError::EINVAL)?
                                    .to_str()
                                    .map_err ...(+55) |
| `kernel/src/syscall/task/execve.rs#sys_execve` | `api/src/syscall/task/execve.rs#sys_execve` | 同板块同名 | Arc::new, Default::default, Err, FD_TABLE.write, FS_CONTEXT.lock, FS_CONTEXT.lock().resolve ...(+43) | Arc::new, Default::default, Err, FD_TABLE.write, FS_CONTEXT.lock, FS_CONTEXT.lock().resolve ...(+37) |
| `kernel/src/syscall/task/wait.rs#sys_waitpid` | `api/src/syscall/task/wait.rs#sys_waitpid` | 同板块同名 | AxError::from, Err, Ok, WaitOptions::from_bits_truncate, WaitPid::Pgid, WaitPid::Pid ...(+22) | Err, Ok, Poll::Ready, WaitOptions::from_bits_truncate, WaitPid::Pgid, WaitPid::Pid ...(+28) |
| `kernel/src/task/ops.rs#do_exit` | `api/src/task.rs#do_exit` | 同板块同名 | FutexKey::new_current, SignalInfo::new_kernel, Some, axtask::yield_now, clear_child_tid.vm_write, clear_child_tid.vm_write(0).is_ok ...(+32) | FutexKey::new_current, SHM_MANAGER.lock, SHM_MANAGER.lock().clear_proc_shm, SignalInfo::new_kernel, Some, axtask::yield_now ...(+30) |

#### 任务调度/异步运行时
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `kernel/src/pseudofs/dev/event.rs#ioctl` | `api/src/vfs/dev/event.rs#ioctl` | 同板块同名 | Err, EventType::Led.bits_count, EventType::Sound.bits_count, EventType::Switch.bits_count, Ok, UserPtr::<InputDeviceId>::from ...(+16) | Err, EventType::Led.bits_count, EventType::Sound.bits_count, EventType::Switch.bits_count, Ok, UserPtr::<InputDeviceId>::from ...(+16) |
| `kernel/src/pseudofs/dev/mod.rs#builder` | `api/src/vfs/dev/mod.rs#builder` | 同板块同名 | Arc::new, Device::new, DeviceId::new, DirMapping::new, Ok, Random::new ...(+11) | Arc::new, Device::new, DeviceId::new, DirMapping::new, Ok, Random::new ...(+11) |
| `kernel/src/syscall/sync/futex.rs#sys_futex` | `api/src/syscall/sync/futex.rs#sys_futex` | 同板块同名 | AxError::from, Err, FutexKey::new_current, Ok, Some, assert_unsigned ...(+18) | Err, FutexKey::new_current, Ok, Some, assert_unsigned, axtask::yield_now ...(+19) |
| `kernel/src/pseudofs/dev/tty/mod.rs#ioctl` | `api/src/vfs/dev/tty.rs#ioctl` | 同板块同名 | (arg as *const Termios).vm_read, (arg as *const Termios2).vm_read, (arg as *const WindowSize).vm_read, (arg as *mut Termios).vm_write, (arg as *mut Termios2).vm_write, (arg as *mut WindowSize).vm_write ...(+30) | (arg as *const Termios).vm_read, (arg as *const Termios2).vm_read, (arg as *const WindowSize).vm_read, (arg as *mut Termios).vm_write, (arg as *mut Termios2).vm_write, (arg as *mut WindowSize).vm_write ...(+30) |

#### 内存管理
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `kernel/src/syscall/mod.rs#handle_syscall` | `api/src/syscall/mod.rs#handle_syscall` | 同板块同名 | Err, LinuxError::ENOSYS.code, LinuxError::from, LinuxError::from(err).code, Sysno::new, debug! ...(+223) | Err, LinuxError::ENOSYS.code, Ok, Sysno::new, debug!, err.code ...(+201) |
| `kernel/src/syscall/mm/mmap.rs#sys_mmap` | `api/src/syscall/mm/mmap.rs#sys_mmap` | 同板块同名 | (addr + length).align_up, Arc::new, Backend::new_alloc, Backend::new_cow, Backend::new_file, Backend::new_linear ...(+50) | (addr + length).align_up, Arc::new, Backend::new_alloc, Backend::new_cow, Backend::new_file, Backend::new_linear ...(+50) |
| `kernel/src/mm/loader.rs#load` | `core/src/mm.rs#load` | 同板块同名 | AuxEntry::new, CStr::from_bytes_with_nul, CStr::from_bytes_with_nul(&data)
                    .ok, CStr::from_bytes_with_nul(&data)
                    .ok()
                    .and_then, CStr::from_bytes_with_nul(&data)
                    .ok()
                    .and_then(|cstr| cstr.to_str().ok())
                    .ok_or, ElfCacheEntry::load ...(+52) | CStr::from_bytes_with_nul, CStr::from_bytes_with_nul(&data)
                .ok, CStr::from_bytes_with_nul(&data)
                .ok()
                .and_then, CStr::from_bytes_with_nul(&data)
                .ok()
                .and_then(|cstr| cstr.to_str().ok())
                .ok_or, ElfCacheEntry::load, ElfCacheEntry::load(loc)?.map_err ...(+42) |
| `kernel/src/task/user.rs#new_user_task` | `api/src/task.rs#new_user_task` | 同板块同名 | (set_child_tid as *mut Pid).nullable, SignalInfo::new_kernel, TaskInner::new, axtask::current, check_signals, curr.as_thread ...(+22) | SignalInfo::new_kernel, TaskInner::new, access_user_memory, axtask::current, check_signals, curr.as_thread ...(+20) |

#### 中断/异常处理
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `kernel/src/pseudofs/dev/tty/terminal/termios.rs#signo_for` | `api/src/terminal/termios.rs#signo_for` | 同板块同名同结构 | Some, self.special_char | Some, self.special_char |
| `kernel/src/pseudofs/dev/tty/terminal/termios.rs#default` | `api/src/terminal/termios.rs#default` | 同板块同名同结构 | ctl | ctl |

#### 系统调用
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `kernel/src/syscall/net/opt.rs#sys_getsockopt` | `api/src/syscall/net/opt.rs#sys_getsockopt` | 同板块同名 | Err, Ok, Socket::from_fd, call_dispatch!, debug!, optlen.get_as_mut ...(+3) | Err, Ok, Socket::from_fd, call_dispatch!, debug!, optlen.get_as_mut ...(+3) |
| `kernel/src/syscall/time.rs#sys_setitimer` | `api/src/syscall/time.rs#sys_setitimer` | 同板块同名 | ITimerType::from_repr, ITimerType::from_repr(which).ok_or, Ok, curr
        .as_thread, curr
        .as_thread()
        .time
        .borrow_mut, curr
        .as_thread()
        .time
        .borrow_mut()
        .set_itimer ...(+12) | ITimerType::from_repr, ITimerType::from_repr(which).ok_or, Ok, curr
        .as_thread, curr
        .as_thread()
        .time
        .borrow_mut, curr
        .as_thread()
        .time
        .borrow_mut()
        .set_itimer ...(+12) |
| `kernel/src/syscall/io_mpx/epoll.rs#sys_epoll_ctl` | `api/src/syscall/io_mpx/epoll.rs#sys_epoll_ctl` | 同板块同名 | Epoll::from_fd, EpollFlags::from_bits, EpollFlags::from_bits(event.events & !events.bits()).ok_or, Err, IoEvents::from_bits_truncate, Ok ...(+7) | Epoll::from_fd, EpollFlags::from_bits, EpollFlags::from_bits(event.events & !(events.bits() as u32))
            .ok_or, Err, IoEvents::from_bits_truncate, Ok ...(+7) |
| `kernel/src/syscall/net/opt.rs#sys_setsockopt` | `api/src/syscall/net/opt.rs#sys_setsockopt` | 同板块同名 | Err, Ok, Socket::from_fd, call_dispatch!, debug!, size_of::<T> ...(+2) | Err, Ok, Socket::from_fd, call_dispatch!, debug!, size_of::<T> ...(+2) |

#### 文件系统
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `kernel/src/pseudofs/dev/fb.rs#ioctl` | `api/src/vfs/dev/fb.rs#ioctl` | 同板块同名 | (arg as *mut FixScreenInfo).vm_write, (arg as *mut VarScreenInfo).vm_write, Err, Ok, axdisplay::framebuffer_info | (arg as *mut FixScreenInfo).vm_write, (arg as *mut VarScreenInfo).vm_write, Err, Ok, axdisplay::main_display, axdisplay::main_display().info |
| `kernel/src/pseudofs/dev/loop.rs#ioctl` | `api/src/vfs/dev/loop.rs#ioctl` | 同板块同名 | (arg as *const loop_info).vm_read_uninit, (arg as *const loop_info).vm_read_uninit()?.assume_init, (arg as *const u32).vm_read, (arg as *mut loop_info).vm_write, (arg as *mut u32).vm_write, (arg as *mut u64).vm_write ...(+22) | (arg as *const loop_info).vm_read_uninit, (arg as *const loop_info).vm_read_uninit()?.assume_init, (arg as *const u32).vm_read, (arg as *mut loop_info).vm_write, (arg as *mut u32).vm_write, (arg as *mut u64).vm_write ...(+22) |
| `kernel/src/file/epoll.rs#poll_events` | `api/src/file/epoll.rs#poll_events` | 同板块同名 | Arc::downgrade, Err, Ok, event.events.bits, file.as_ref, interest.consume ...(+13) | Arc::downgrade, Err, Ok, event.events.bits, file.as_ref, interest.enabled.store ...(+14) |
| `kernel/src/syscall/io_mpx/poll.rs#do_poll` | `api/src/syscall/io_mpx/poll.rs#do_poll` | 同板块同名 | Err, FdPollSet, IoEvents::empty, IoEvents::from_bits, IoEvents::from_bits(fd.events as _).ok_or, Ok ...(+17) | Err, FdPollSet, IoEvents::empty, IoEvents::from_bits, IoEvents::from_bits(fd.events as _).ok_or, Ok ...(+17) |

#### 设备驱动
仅参考项目中观察到该模块代表函数，目标项目没有同模块可对齐证据：
- 参考项目：`arceos/modules/axnet/src/router.rs#dispatch`，调用摘要：IpAddress::Ipv4, IpAddress::Ipv6, IpVersion::of_packet, IpVersion::of_packet(packet).expect, assert_eq!, dev.send ...(+12)
- 参考项目：`arceos/modules/axnet/src/device/ethernet.rs#send`，调用摘要：Self::send_to, Some, buf.copy_from_slice, dst_buffer.copy_from_slice, next_hop.is_broadcast, packet.len ...(+7)
- 参考项目：`arceos/modules/axnet/src/lib.rs#init_network`，调用摘要："eth0".to_owned, Box::new, EthernetAddress, EthernetDevice::new, GATEWAY.parse, GATEWAY.parse().expect ...(+32)
- 参考项目：`arceos/tools/raspi4/chainloader/src/bsp/device_driver/bcm/bcm2xxx_pl011_uart.rs#init`，调用摘要：FBRD::BAUD_DIVFRAC.val, IBRD::BAUD_DIVINT.val, self.flush, self.registers
            .CR
            .write, self.registers
            .LCR_H
            .write, self.registers.CR.set ...(+3)

#### 网络协议栈
仅参考项目中观察到该模块代表函数，目标项目没有同模块可对齐证据：
- 参考项目：`arceos/modules/axnet/src/udp.rs#recv`，调用摘要：Err, ExpectedRemote::Any, ExpectedRemote::Expecting, Ok, SocketAddrEx::Ip, ax_err! ...(+20)
- 参考项目：`arceos/modules/axnet/src/udp.rs#send`，调用摘要：Err, IpAddr::V4, IpEndpoint::from, Ok, PacketMeta::default, SERVICE.lock ...(+23)
- 参考项目：`arceos/modules/axnet/src/tcp.rs#recv`，调用摘要：Err, Ok, ax_err!, dst.remaining_mut, dst.write, options.flags.contains ...(+13)
- 参考项目：`arceos/modules/axnet/src/unix/dgram.rs#recv`，调用摘要：Err, Ok, SocketAddrEx::Unix, data.len, dst.extend, dst.write ...(+9)

#### 文件系统/第三方库
仅参考项目中观察到该模块代表函数，目标项目没有同模块可对齐证据：
- 参考项目：`arceos/modules/axfs-ng/src/fs/ext4/inode.rs#create`，调用摘要：DirEntry::new_dir, DirEntry::new_file, DirNode::new, Err, FileNode::new, Inode::new ...(+14)
- 参考项目：`arceos/modules/axfs-ng/src/fs/ext4/inode.rs#metadata`，调用摘要：DeviceId::default, FileAttr::default, NodePermission::from_bits_truncate, Ok, into_vfs_type, self.fs
            .lock ...(+2)
- 参考项目：`arceos/modules/axfs-ng/src/fs/ext4/inode.rs#update_metadata`，调用摘要：Ok, fs.with_inode_ref, fs.with_inode_ref(self.ino, |inode| {
            if let Some(mode) = update.mode {
                inode.set_mode((inode.mode() & !0xfff) | (mode.bits() as u32));
            }
            if let Some((uid, gid)) = update.owner {
                inode.set_owner(uid as _, gid as _);
            }
            if let Some(atime) = update.atime {
                inode.set_atime(&atime);
            }
            if let Some(mtime) = update.mtime {
                inode.set_mtime(&mtime);
            }
            inode.update_ctime();
            Ok(())
        })
        .map_err, inode.mode, inode.set_atime, inode.set_mode ...(+5)
- 参考项目：`arceos/modules/axfs-ng/src/fs/ext4/inode.rs#read_dir`，调用摘要：Ok, core::str::from_utf8, core::str::from_utf8(entry.name())
                .map_err, core::str::from_utf8(entry.name())
                .map_err(|_| VfsError::EINVAL)?
                .to_owned, entry.ino, entry.inode_type ...(+10)

#### 通用模块/其他
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `kernel/src/time.rs#try_into_time_value` | `api/src/time.rs#try_into_time_value` | 同板块同名 | Err, Ok, TimeValue::new | Err, Ok, TimeValue::new |
| `kernel/src/time.rs#try_into_time_value` | `api/src/time.rs#try_into_time_value` | 同板块同名 | Err, Ok, TimeValue::new | Err, Ok, TimeValue::new |
| `kernel/src/time.rs#try_into_time_value` | `api/src/time.rs#try_into_time_value` | 同板块同名 | Err, Ok, TimeValue::new | Err, Ok, TimeValue::new |
| `kernel/src/time.rs#try_into_time_value` | `api/src/time.rs#try_into_time_value` | 同板块同名 | Err, Ok, TimeValue::new | Err, Ok, TimeValue::new |


### 1. 宏观判定
- **定性结论**: 独立开发 (相似度: 17.79%，共同 AST 指纹: 369；唯一 AST 指纹: 目标项目 940，参考项目 1503；覆盖率: 目标项目 39.26%，参考项目 24.55%)

### 2. 全模块覆盖矩阵解读
- **进程管理**
  1. 双方均存在该模块。
  2. 存在共同 AST 指纹。
  3. 目标覆盖率 27.85%，参考覆盖率 42.31%。覆盖率均未过半，且参考项目因函数基数较小导致覆盖率虚高，说明共同指纹集中在少数受 ABI 强约束的函数上。
  4. 证据强度：弱。相似性源于 POSIX 系统调用命名、同一框架（ArceOS/Starry）的全局对象（如 `PROCESS_TABLE`）及 Rust 标准惯用法，而非核心逻辑的独创性表达。

- **任务调度/异步运行时**
  1. 双方均存在该模块。
  2. 存在共同 AST 指纹。
  3. 目标覆盖率 20.19%，参考覆盖率 16.23%。双向覆盖率均很低，表明调度器、异步运行时等核心逻辑高度异构。
  4. 证据强度：弱。共同指纹主要来自设备虚拟文件系统（如 `/dev/event`、`/dev/tty`）的 `ioctl` 实现，其结构由 Linux ABI 和框架 VFS 接口决定。

- **内存管理**
  1. 双方均存在该模块。
  2. 存在共同 AST 指纹。
  3. 目标覆盖率 37.24%，参考覆盖率 21.6%。覆盖率差异显著，目标项目在内存管理上有更多独立实现。
  4. 证据强度：弱。共同指纹集中在 `sys_mmap` 的后端构造（`Backend::new_*`）和 ELF 加载流程，这些是框架提供的内存原语和标准 ELF 解析步骤，不涉及内部分配器或页表管理的独创逻辑。

- **中断/异常处理**
  1. 双方均存在该模块。
  2. 存在共同 AST 指纹。
  3. 目标覆盖率 66.67%，参考覆盖率 25.0%。目标项目函数基数极小（3个），导致覆盖率波动大，无统计意义。
  4. 证据强度：未形成。共同指纹仅为终端控制字符的简单字段访问，属于数据结构定义，非逻辑实现。

- **系统调用**
  1. 双方均存在该模块。
  2. 存在共同 AST 指纹。
  3. 目标覆盖率 7.46%，参考覆盖率 13.33%。双向覆盖率极低，表明系统调用分发和实现高度异构。
  4. 证据强度：弱。共同指纹集中在少数网络和时间的系统调用，其调用链由标准 Socket API 和 `itimer` 结构定义决定。

- **文件系统**
  1. 双方均存在该模块。
  2. 存在共同 AST 指纹。
  3. 目标覆盖率 46.28%，参考覆盖率 37.18%。覆盖率相对较高，但未超过 50%。
  4. 证据强度：中。共同指纹较多，但主要源于对 Linux `/proc`、`/dev` 文件系统接口的模拟以及同一 VFS 框架的使用。例如 `lookup_child`、`child_names` 等函数的结构由 `/proc` 的 ABI 和框架的 `SimpleDir` API 共同决定。

- **设备驱动**
  1. 仅参考项目存在该模块。
  2. 不存在共同 AST 指纹。
  3. 目标覆盖率 0.0%，参考覆盖率 0.0%。
  4. 证据强度：未形成。目标项目完全未实现此模块，是架构差异的铁证。

- **网络协议栈**
  1. 仅参考项目存在该模块。
  2. 不存在共同 AST 指纹。
  3. 目标覆盖率 0.0%，参考覆盖率 0.0%。
  4. 证据强度：未形成。目标项目完全未实现此模块，是架构差异的铁证。

- **第三方库/公共依赖**
  1. 仅参考项目存在该模块（ext4 文件系统）。
  2. 不存在共同 AST 指纹。
  3. 目标覆盖率 0.0%，参考覆盖率 0.0%。
  4. 证据强度：未形成。目标项目未集成该第三方库，表明项目依赖和功能选型独立。

### 3. 底层 AST 关键证据指纹表
| 所属板块 | 目标项目路径 | 参考项目路径 | 证据类型 | 技术说明 |
| :--- | :--- | :--- | :--- | :--- |
| 进程管理 | `kernel/src/syscall/ipc/shm.rs#clear_proc_shm` | `core/src/shm.rs#clear_proc_shm` | 同板块底层逻辑一致 | SysV 共享内存管理器的标准生命周期：按 shmid 取对象 -> 按 pid 取关联 -> 解绑 -> 清理。调用链由数据结构 API 决定，属领域必然。 |
| 进程管理 | `kernel/src/pseudofs/proc.rs#lookup_child` | `api/src/vfs/proc.rs#lookup_child` | 同板块底层逻辑一致 | 基于同一 VFS 框架构建 proc 目录节点的标准模板：`Arc::downgrade` -> `Arc::new` -> `NodeOpsMux::Dir` -> `SimpleDir::new_maker` -> `get_task`。 |
| 进程管理 | `kernel/src/pseudofs/proc.rs#child_names` | `api/src/vfs/proc.rs#child_names` | 同板块底层逻辑一致 | 获取任务 ID 并转换为字符串的 Rust 惯用链式调用：`task.id().as_u64().to_string().into()`。 |
| 进程管理 | `kernel/src/syscall/task/wait.rs#apply` | `api/src/syscall/task/wait.rs#apply` | 同板块底层逻辑一致 | `waitpid` 系统调用解析 `pid` 参数的必然字段访问：`child.group().pgid` 和 `child.pid`。 |
| 进程管理 | `kernel/src/file/pidfd.rs#register` | `api/src/file/pidfd.rs#register` | 同板块底层逻辑一致 | Rust 异步 `Poll` 模型下事件注册的标准模板：获取 waker -> 检查事件 -> 注册到退出事件。 |
| 文件系统 | `kernel/src/pseudofs/dev/fb.rs#ioctl` | `api/src/vfs/dev/fb.rs#ioctl` | 同板块同名 | 帧缓冲设备 `ioctl` 的标准处理：将参数强转为 `FixScreenInfo` 或 `VarScreenInfo` 指针并进行 `vm_write`。由 Linux ABI 定义。 |
| 文件系统 | `kernel/src/pseudofs/dev/loop.rs#ioctl` | `api/src/vfs/dev/loop.rs#ioctl` | 同板块同名 | 回环设备 `ioctl` 的标准处理：根据命令字将参数强转为 `loop_info` 或整型指针并进行读写。由 Linux ABI 定义。 |
| 任务调度/异步运行时 | `kernel/src/pseudofs/dev/event.rs#ioctl` | `api/src/vfs/dev/event.rs#ioctl` | 同板块同名 | 输入设备 `ioctl` 的标准处理：处理 `InputDeviceId` 等标准结构体。由 Linux ABI 和框架 VFS 接口决定。 |
| 内存管理 | `kernel/src/syscall/mm/mmap.rs#sys_mmap` | `api/src/syscall/mm/mmap.rs#sys_mmap` | 同板块同名 | `mmap` 系统调用的标准后端构造流程：`Backend::new_alloc`、`Backend::new_file` 等。由框架内存抽象接口决定。 |
| 系统调用 | `kernel/src/syscall/time.rs#sys_setitimer` | `api/src/syscall/time.rs#sys_setitimer` | 同板块同名 | `setitimer` 系统调用的标准实现：解析 `ITimerType`，获取当前线程的时间结构并调用 `set_itimer`。由 POSIX ABI 定义。 |

### 4. 核心代码调用流对比剖析
* **【进程管理/进程退出】**
* **底层技术析理**: 两者均遵循 Linux 进程退出的标准流程：发送信号、清理共享内存、处理 robust list、清除子线程 ID、最终让出 CPU。然而，目标项目在 `do_exit` 中通过显式路径 `crate::syscall::SHM_MANAGER.lock().clear_proc_shm` 调用共享内存清理，而参考项目使用 `SHM_MANAGER.lock().clear_proc_shm`，表明两者在模块组织和依赖管理上存在独立设计。此外，目标项目的 `exit_robust_list` 增加了 `debug!` 日志埋点，是独立的调试策略。
* **代码溯源链接**:
  - 目标项目: `kernel/src/task/ops.rs#do_exit`
  - 参考项目: `api/src/task.rs#do_exit`

* **【内存管理/内存映射】**
* **底层技术析理**: 两者在 `sys_mmap` 的实现上都遵循了标准的 Linux `mmap` 语义：对齐地址、根据标志位选择后端（匿名、文件、CoW 等）、创建 `Arc` 包装的内存区域。调用流高度相似是因为它们都受限于同一套框架（`Backend` trait）和 POSIX 标准。真正的内存管理核心——物理页分配、页表操作、缺页中断处理——并未在这层调用图谱中体现，而这些通常是区分内核原创性的关键。
* **代码溯源链接**:
  - 目标项目: `kernel/src/syscall/mm/mmap.rs#sys_mmap`
  - 参考项目: `api/src/syscall/mm/mmap.rs#sys_mmap`

* **【文件系统/伪文件系统构建】**
* **底层技术析理**: 在构建 `/proc/<pid>/fd` 目录项时，两者都遵循了相同的 VFS 框架模式：获取文件描述符表的作用域 -> 读取 -> 查找文件描述符 -> 获取路径 -> 创建 `SimpleFile` 节点。目标项目使用 `VfsError::NotFound`，而参考项目使用 `VfsError::ENOENT`，这是错误码抽象层的独立设计。调用流的相似性源于框架 API 的强制约束，而非核心逻辑的抄袭。
* **代码溯源链接**:
  - 目标项目: `kernel/src/pseudofs/proc.rs#lookup_child`
  - 参考项目: `api/src/vfs/proc.rs#lookup_child`

### 5. 单方模块与降权项
- **设备驱动**、**网络协议栈**、**文件系统/第三方库（ext4）** 仅存在于参考项目中。目标项目完全没有这些模块的实现，这是两者架构设计和功能范围存在根本性差异的直接证据，排除了系统性代码搬运的可能性。
- 控辩双方提及的 `sys_waitpid`、`sys_execve`、`sys_fork` 等大量同名函数，其函数名由 Linux/POSIX 系统调用 ABI 强制规定，属于接口约束，不能作为同源性证据。
- `format!`、`assert!` 等 Rust 内置宏的 AST 指纹一致，属于语言层面的必然，不具备任何独创性，已降权处理。
- 错误码的差异（如 `LinuxError::EBADF` vs `AxError::BadFileDescriptor`）是目标项目进行独立错误抽象和类型系统设计的直接证据，而非“机械式障眼法”。

### 6. 保守结论
综合全模块 AST 指纹分析，目标项目与参考项目的整体相似度仅为 17.79%，远未达到“实质性相似”的阈值。双方共有的 AST 指纹高度集中在受 Linux ABI/POSIX 标准、Rust 语言惯用法以及共同技术框架（ArceOS/Starry）强制约束的接口层和标准操作流程上。在决定内核原创性的核心领域，如设备驱动、网络协议栈、第三方库集成等方面，目标项目与参考项目存在根本性架构差异。目标项目拥有 940 个唯一 AST 指纹，并在错误处理、模块路径组织上展现出独立设计。因此，现有证据充分支持目标项目为独立开发，不存在对参考项目的非法代码复用。

------------------------------

