# 🛡️ OS 内核赛自动化代码级架构审计报告

**审计时间**: `2026-07-02 12:48:36`
**参赛作品名称**: `2026-T202610558999978-XX`
**年份**: `2026`
**学校**: `XX`
**队伍名称**: `T202610558999978`
**比赛名字**: `OS-Competition`
**比赛子赛道**: `Kernel-Implementation`
**仓库地址**: `https://gitlab.eduxiji.net/educg-group-43501-3132633/T202610558999978-3790`
**推测开发语言**: `Rust / C (由AST探针自动识别)`
**特征函数量**: `382`

## ⚔️ 底层代码同源性对比 (AST Homology)
> 本部分由专门的代码审查智能体，对该项目与符合年份条件的历届最高相似度项目进行控制流与底层调用对比。新版报告会先给出确定性的全模块覆盖矩阵，再给出 Agent 的分模块解释，避免只围绕少量抽样函数展开。

### 🏆 Top 2 底层对标: vs `2021-小白闯江湖队-国防科技大学`（国防科技大学 / 小白闯江湖队）

#### 对方作品信息

**对方年份**: `2021`
**对方学校**: `国防科技大学`
**对方队伍名称**: `小白闯江湖队`
**对方比赛名字**: `OS-Competition`
**对方比赛子赛道**: `Kernel-Implementation`
**对方仓库地址**: `https://gitlab.eduxiji.net/Misure/oskernel2021-x2`

> 双向覆盖率：共同 AST 指纹: 144；唯一 AST 指纹: 目标项目 367，参考项目 420；覆盖率: 目标项目 39.24%，参考项目 34.29%

## 全模块对比总览（确定性证据）

> 总体覆盖率：共同 AST 指纹: 144；唯一 AST 指纹: 目标项目 367，参考项目 420；覆盖率: 目标项目 39.24%，参考项目 34.29%

### 1. 模块覆盖矩阵
| 模块 | 目标函数数 | 参考函数数 | 共同唯一 AST 指纹 | Target 覆盖 | Base 覆盖 | 状态 |
| :--- | ---: | ---: | ---: | ---: | ---: | :--- |
| 进程管理 | 55 | 42 | 24 | 45.28% | 72.73% | 双方存在且有结构重合 |
| 任务调度/异步运行时 | 31 | 39 | 10 | 32.26% | 26.32% | 双方存在且有结构重合 |
| 内存管理 | 122 | 128 | 41 | 33.88% | 33.06% | 双方存在且有结构重合 |
| 中断/异常处理 | 12 | 21 | 3 | 25.0% | 15.79% | 双方存在且有结构重合 |
| 系统调用 | 2 | 7 | 2 | 100.0% | 50.0% | 双方存在且有结构重合 |
| 文件系统 | 75 | 71 | 11 | 15.28% | 16.18% | 双方存在且有结构重合 |
| 设备驱动 | 12 | 28 | 7 | 58.33% | 31.82% | 双方存在且有结构重合 |
| 网络协议栈 | 19 | 0 | 0 | 0.0% | 0.0% | 仅目标项目体现 |
| 通用模块/其他 | 54 | 137 | 41 | 83.67% | 34.45% | 双方存在且有结构重合 |

### 2. 分模块证据清单

#### 进程管理
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `kernel/proc/proc.c#scheduler` | `kernel/proc.c#scheduler` | 同板块同名 | acquire, intr_on, mycpu, release, swtch | acquire, intr_on, mycpu, release, swtch |
| `kernel/proc/proc.c#reparent` | `kernel/proc.c#reparent` | 同板块同名同结构 | acquire, release | acquire, release |
| `user/program/sh.c#parseexec` | `xv6-user/sh.c#parseexec` | 同板块同名同结构 | execcmd, gettoken, panic, parseblock, parseredirs, peek | execcmd, gettoken, panic, parseblock, parseredirs, peek |
| `kernel/proc/proc.c#sleep` | `kernel/proc.c#sleep` | 同板块同名同结构 | acquire, myproc, release, sched | acquire, myproc, release, sched |

#### 任务调度/异步运行时
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `user/program/sh.c#runcmd` | `xv6-user/sh.c#runcmd` | 同板块同名 | close, dup, exec, exit, fork1, fprintf ...(+5) | close, dup, exec, exit, fork1, fprintf ...(+5) |
| `kernel/driver/console.c#consoleread` | `kernel/console.c#consoleread` | 同板块同名同结构 | C, acquire, either_copyout, myproc, release, sleep | C, acquire, either_copyout, myproc, release, sleep |
| `kernel/driver/console.c#consoleintr` | `kernel/console.c#consoleintr` | 同板块同名 | C, acquire, consputc, procdump, release, wakeup | C, acquire, consputc, procdump, release, wakeup |
| `kernel/lib/printf.c#printf` | `kernel/printf.c#printf` | 同板块同名 | acquire, consputc, panic, printint, printptr, release ...(+2) | acquire, consputc, panic, printint, printptr, release ...(+2) |

#### 内存管理
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `kernel/proc/exec.c#exec` | `kernel/exec.c#exec` | 同板块同名 | PGROUNDUP, begin_op, copyout, end_op, ilock, iunlockput ...(+12) | CopyString2Stack, CreateUserStack, PGROUNDUP, __debug_info, copyout, elock ...(+12) |
| `kernel/interrupt/trap.c#usertrap` | `kernel/trap.c#usertrap` | 同板块同名 | PGROUNDDOWN, PGROUNDUP, cowalloc, cowpage, devintr, exit ...(+17) | cowcopy, devintr, exit, intr_on, lazy_alloc, myproc ...(+12) |
| `kernel/driver/virtio_disk.c#virtio_disk_rw` | `kernel/virtio_disk.c#virtio_disk_rw` | 同板块同名 | R, __sync_synchronize, acquire, alloc3_desc, free_chain, kvmpa ...(+2) | R, __sync_synchronize, acquire, alloc3_desc, free_chain, kvmpa ...(+2) |
| `kernel/proc/proc.c#exit` | `kernel/proc.c#exit` | 同板块同名 | acquire, begin_op, end_op, fileclose, filewrite, iput ...(+7) | __debug_info, acquire, eput, fileclose, myproc, panic ...(+4) |

#### 中断/异常处理
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `kernel/driver/virtio_disk.c#virtio_disk_intr` | `kernel/virtio_disk.c#virtio_disk_intr` | 同板块同名同结构 | R, acquire, panic, release, wakeup | R, acquire, panic, release, wakeup |
| `kernel/interrupt/plic.c#plicinithart` | `kernel/plic.c#plicinithart` | 同板块同名 | PLIC_SENABLE, PLIC_SPRIORITY, cpuid | PLIC_MENABLE, PLIC_SENABLE, PLIC_SPRIORITY, cpuid, printf, readd |
| `kernel/interrupt/plic.c#plicinit` | `kernel/plic.c#plicinit` | 同板块同名 | 内部闭环逻辑 | printf, writed |
| `kernel/lock/spinlock.c#pop_off` | `kernel/intr.c#pop_off` | 同板块同名 | intr_get, intr_on, mycpu, panic | intr_get, intr_on, mycpu, panic |

#### 系统调用
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `kernel/syscall.c#argstr` | `kernel/syscall.c#argstr` | 同板块同名同结构 | argaddr, fetchstr | argaddr, fetchstr |
| `kernel/syscall.c#argint` | `kernel/syscall.c#argint` | 同板块同名同结构 | argraw | argraw |

#### 文件系统
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `user/program/grind.c#go` | `xv6-user/grind.c#go` | 同板块同名同结构 | chdir, close, dup, exec, exit, fork ...(+16) | chdir, close, dup, exec, exit, fork ...(+16) |
| `kernel/filesystem/fs.c#dirlookup` | `kernel/fat32.c#dirlookup` | 同板块同名 | iget, namecmp, panic, readi | edup, eget, enext, eput, panic, reloc_clus ...(+2) |
| `kernel/filesystem/file.c#fileclose` | `kernel/file.c#fileclose` | 同板块同名 | acquire, begin_op, end_op, iput, panic, pipeclose ...(+2) | acquire, eput, panic, pipeclose, release |
| `kernel/filesystem/fs.c#skipelem` | `kernel/fat32.c#skipelem` | 同板块同名 | memmove | memmove |

#### 设备驱动
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `kernel/driver/uart.c#uartinit` | `kernel/uart.c#uartinit` | 同板块同名 | WriteReg, initlock | WriteReg, initlock |
| `kernel/driver/uart.c#uartstart` | `kernel/uart.c#uartstart` | 同板块同名同结构 | ReadReg, WriteReg, wakeup | ReadReg, WriteReg, wakeup |
| `kernel/driver/uart.c#uartputc` | `kernel/uart.c#uartputc` | 同板块同名同结构 | acquire, release, sleep, uartstart | acquire, release, sleep, uartstart |
| `user/program/sh.c#parseblock` | `xv6-user/sh.c#parseblock` | 同板块同名同结构 | gettoken, panic, parseline, parseredirs, peek | gettoken, panic, parseline, parseredirs, peek |

#### 网络协议栈
仅目标项目中观察到该模块代表函数，参考项目没有同模块可对齐证据：
- 目标项目：`kernel/network/net.c#in_cksum`，调用摘要：内部闭环逻辑
- 目标项目：`kernel/network/net.c#net_rx_arp`，调用摘要：mbuffree, mbufpullhdr, memmove, net_tx_arp, ntohl, ntohs
- 目标项目：`kernel/network/net.c#net_rx_ip`，调用摘要：htonl, htons, in_cksum, mbuffree, mbufpullhdr, net_rx_udp ...(+1)
- 目标项目：`kernel/network/net.c#net_rx_udp`，调用摘要：mbuffree, mbufpullhdr, mbuftrim, ntohl, ntohs, sockrecvudp

#### 通用模块/其他
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `user/program/printf.c#vprintf` | `xv6-user/printf.c#vprintf` | 同板块同名同结构 | printint, printptr, putc, va_arg | printint, printptr, putc, va_arg |
| `kernel/lock/spinlock.c#release` | `kernel/spinlock.c#release` | 同板块同名同结构 | __sync_lock_release, __sync_synchronize, holding, panic, pop_off | __sync_lock_release, __sync_synchronize, holding, panic, pop_off |
| `user/program/grind.c#iter` | `xv6-user/grind.c#iter` | 同板块同名同结构 | exit, fork, go, kill, printf, unlink ...(+1) | exit, fork, go, kill, printf, unlink ...(+1) |
| `user/program/wc.c#wc` | `xv6-user/wc.c#wc` | 同板块同名同结构 | exit, printf, read, strchr | exit, printf, read, strchr |


好的，审计官。基于提供的证据底座、控辩双方主张及全模块确定性对比上下文，我将进行最终的同源性定性仲裁。

### 1. 宏观判定
- **定性结论**: **独立开发，底层架构显著差异**
  - **客观 AST 相似度**: 22.4%
  - **双向覆盖率**: 目标项目 39.24%，参考项目 34.29%
  - **核心依据**: 双向覆盖率均未过半，表明双方项目的大部分代码实现是唯一的。目标项目存在大量独立模块（如网络协议栈、消息队列），且在与参考项目共有的核心模块中，关键实现路径（如文件系统后端、系统调用封装）存在实质性架构差异。重合部分可被“共同源于MIT xv6教学内核及RISC-V硬件ABI约束”的公共知识框架所解释。

### 2. 全模块覆盖矩阵解读
| 模块 | 双方存在 | 共同指纹 | 目标覆盖 | 参考覆盖 | 证据强度 | 解读 |
| :--- | :---: | :---: | :---: | :---: | :---: | :--- |
| **进程管理** | 是 | 24 | 45.28% | 72.73% | **中** | 参考项目覆盖率极高，说明目标项目实现了xv6标准进程模型的核心子集。重合集中在调度、锁、生命周期等基础原语，属于公共知识。目标项目独有的`messagequeue.c`等扩展功能是独立开发的有力证明。 |
| **任务调度/异步运行时** | 是 | 10 | 32.26% | 26.32% | **弱** | 双方覆盖率均低，表明调度策略或实现细节差异大。重合部分如`consoleintr`、`printf`等属于通用基础设施，非调度核心逻辑。 |
| **内存管理** | 是 | 41 | 33.88% | 33.06% | **中** | 双方覆盖率接近且均不高，说明在内存管理这一复杂子系统上实现路径分歧显著。例如`exec`、`usertrap`等函数调用图谱存在差异，体现了不同的内存布局或写时复制策略。 |
| **中断/异常处理** | 是 | 3 | 25.0% | 15.79% | **弱** | 覆盖率低，重合仅限`virtio_disk_intr`、`plicinit`等少量标准设备中断处理函数，这是硬件规范决定的，不构成强同源证据。 |
| **系统调用** | 是 | 2 | 100.0% | 50.0% | **弱** | 目标项目仅2个函数，100%覆盖是因其仅实现了最基础的参数获取函数`argint`和`argstr`，这是系统调用ABI的必然要求，无独创性。 |
| **文件系统** | 是 | 11 | 15.28% | 16.18% | **弱** | 双方覆盖率极低，是架构差异最大的模块之一。参考项目基于FAT32，目标项目路径`kernel/filesystem/fs.c`暗示了不同的文件系统实现，`dirlookup`等函数的调用图谱完全不同。 |
| **设备驱动** | 是 | 7 | 58.33% | 31.82% | **中** | 目标项目覆盖率较高，但重合集中在`uart.c`、`virtio_disk.c`等标准硬件驱动，其寄存器操作和初始化流程由硬件手册规定，属于公共领域知识。 |
| **网络协议栈** | **否** | 0 | 0.0% | 0.0% | **未形成** | 目标项目独有模块，包含`net_rx_arp`、`net_rx_ip`等完整协议栈实现，是证明其独立开发能力与架构差异的**决定性证据**。 |
| **通用模块/其他** | 是 | 41 | 83.67% | 34.45% | **弱** | 目标项目覆盖率畸高，但此模块混杂了用户态测试程序（`grind`、`wc`、`sh`）和内核基础库（`printf`、`spinlock`）。这些多为xv6官方公开示例或标准C库实现，不能作为抄袭证据。 |

### 3. 底层 AST 关键证据指纹表
| 所属板块 | 目标项目路径 | 参考项目路径 | 证据类型 | 技术说明 |
| :--- | :--- | :--- | :--- | :--- |
| 进程管理 | `kernel/proc/proc.c#forkret` | `kernel/proc.c#forkret` | 同名不同构 | 核心调用流一致，但文件系统初始化调用不同（`fsinit` vs `fat32_init`），揭示了底层文件系统架构的根本性差异。 |
| 进程管理 | `kernel/sysproc.c#sys_clone` | `kernel/sysproc.c#sys_clone` | 同名不同构 | 目标项目调用`clone`，参考项目调用`argint, fetchaddr, fork`，表明线程创建的系统调用封装与实现路径完全不同。 |
| 内存管理 | `kernel/proc/exec.c#exec` | `kernel/exec.c#exec` | 同名不同构 | 参考项目调用`CreateUserStack`, `CopyString2Stack`等特有函数，目标项目无此调用，表明用户栈构建方式存在差异。 |
| 内存管理 | `kernel/interrupt/trap.c#usertrap` | `kernel/trap.c#usertrap` | 同名不同构 | 目标项目调用`cowalloc`, `cowpage`，参考项目调用`lazy_alloc`, `cowcopy`，表明写时复制或惰性分配的具体实现策略不同。 |
| 文件系统 | `kernel/filesystem/fs.c#dirlookup` | `kernel/fat32.c#dirlookup` | 同名不同构 | 参考项目调用`edup, eget, enext, eput, reloc_clus`等FAT32簇链操作函数，目标项目调用`iget, namecmp, readi`，文件系统模型完全不同。 |
| 网络协议栈 | `kernel/network/net.c#net_rx_ip` | (无) | 单方独有 | 目标项目独有模块，实现了完整的IP层接收逻辑，调用`in_cksum`, `net_rx_udp`等，是独立架构设计的铁证。 |
| 进程管理 | `kernel/proc/proc.c#scheduler` | `kernel/proc.c#scheduler` | 同源弱证据 | 调用图谱完全一致，但这是xv6标准调度器循环的经典实现，属于公共知识。目标项目另有`scheduler1`、`scheduler2`变体。 |
| 通用模块/其他 | `kernel/lock/spinlock.c#release` | `kernel/spinlock.c#release` | 同源弱证据 | 调用图谱一致，但这是RISC-V架构下自旋锁释放的标准内存屏障与检查序列，由硬件内存模型决定。 |

### 4. 核心代码调用流对比剖析
* **【进程管理/系统调用路径】**
* **底层技术析理**: 在`forkret`这一进程创建后首次返回用户态的关键路径上，两者都遵循`myproc -> release -> usertrapret`的锁释放与陷阱返回流程，这是xv6的标准模式。然而，在文件系统初始化环节，目标项目调用`fsinit`，而参考项目调用`fat32_init`。这不仅是函数名的替换，更揭示了双方选择了完全不同的持久化存储架构，导致进程地址空间加载文件可执行映像的底层逻辑截然不同。
* **代码溯源链接**:
  - 目标项目: `kernel/proc/proc.c#forkret`
  - 参考项目: `kernel/proc.c#forkret`

* **【内存管理/陷阱处理路径】**
* **底层技术析理**: 在`usertrap`这一用户态异常/中断总入口，两者都处理了缺页异常。但目标项目调用`cowalloc`和`cowpage`，暗示其可能采用了一种基于分配时机的写时复制策略。而参考项目调用`lazy_alloc`和`cowcopy`，表明其可能混合了惰性分配与写时复制。这两种内存管理策略在实现复杂度、性能和内存占用上存在显著权衡，其内部逻辑流不可能通过简单“换皮”实现。
* **代码溯源链接**:
  - 目标项目: `kernel/interrupt/trap.c#usertrap`
  - 参考项目: `kernel/trap.c#usertrap`

### 5. 单方模块与降权项
*   **单方独有模块**:
    *   **网络协议栈** (目标项目): 包含 `kernel/network/net.c` 等文件，实现了完整的 ARP、IP、UDP 协议处理，是目标项目独立于参考项目的重大功能扩展，是**独立开发的决定性证据**。
    *   **DMA 管理** (参考项目): 包含 `kernel/dmac.c` 等文件，实现了 DMA 等待与状态检查逻辑，目标项目未体现此模块。
    *   **高级信号处理** (参考项目): 包含 `sys_rt_sigprocmask`, `sys_rt_sigaction` 等 POSIX 实时信号接口，目标项目未实现。
*   **降权项**:
    *   **第三方库/公共依赖**: `xv6-user/sh.c`, `xv6-user/grind.c`, `xv6-user/zombie.c` 等用户态程序，以及 `printf`, `memmove`, `strchr` 等函数，均源自 MIT xv6 公开发布的代码或标准 C 库，其相似性不构成抄袭证据。
    *   **硬件驱动**: `uart.c`, `virtio_disk.c`, `plic.c` 中的函数，其实现由硬件规范和数据手册严格约束，调用图谱相似是技术必然。
    *   **微函数与薄封装**: `sys_getpid`, `cpuid`, `mycpu`, `kill`, `reparent` 等函数代码量极小，其实现由内核 API 或硬件 ABI 唯一确定，不具备可版权性。

### 6. 保守结论
经综合仲裁，目标项目 `[2026-T202610558999978-XX]` 与参考项目 `[2021-小白闯江湖队-国防科技大学]` 的客观 AST 相似度仅为 22.4%，双向覆盖率均低于 40%，表明两者在代码实现层面存在显著差异。尽管在进程管理、内存管理等基础内核模块上存在调用图谱重合，但这完全可归因于双方共同遵循的 MIT xv6 教学内核基线、RISC-V 硬件 ABI 约束以及操作系统领域的标准实践。目标项目独立实现了网络协议栈、消息队列等参考项目不具备的复杂子系统，并在文件系统、内存管理策略等核心路径上与参考项目存在根本性架构分歧。因此，现有证据无法支持“抄袭”或“系统性复用”的指控，应判定为**独立开发**。

------------------------------

