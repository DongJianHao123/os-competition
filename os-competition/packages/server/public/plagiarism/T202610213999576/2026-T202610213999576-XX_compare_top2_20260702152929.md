# 🛡️ OS 内核赛自动化代码级架构审计报告

**审计时间**: `2026-07-02 15:29:29`
**参赛作品名称**: `2026-T202610213999576-XX`
**年份**: `2026`
**学校**: `XX`
**队伍名称**: `T202610213999576`
**比赛名字**: `OS-Competition`
**比赛子赛道**: `Kernel-Implementation`
**仓库地址**: `https://gitlab.eduxiji.net/educg-group-43501-3132633/T202610213999576-845`
**推测开发语言**: `Rust / C (由AST探针自动识别)`
**特征函数量**: `4747`

## ⚔️ 底层代码同源性对比 (AST Homology)
> 本部分由专门的代码审查智能体，对该项目与符合年份条件的历届最高相似度项目进行控制流与底层调用对比。新版报告会先给出确定性的全模块覆盖矩阵，再给出 Agent 的分模块解释，避免只围绕少量抽样函数展开。

### 🏆 Top 2 底层对标: vs `2022-健康向上好青年-哈尔滨工业大学深圳`（哈尔滨工业大学(深圳) / 健康向上好青年）

#### 对方作品信息

**对方年份**: `2022`
**对方学校**: `哈尔滨工业大学(深圳)`
**对方队伍名称**: `健康向上好青年`
**对方比赛名字**: `OS-Competition`
**对方比赛子赛道**: `Kernel-Implementation`
**对方仓库地址**: `local-upload://oskernel2022`

> 双向覆盖率：共同 AST 指纹: 47；唯一 AST 指纹: 目标项目 4164，参考项目 1324；覆盖率: 目标项目 1.13%，参考项目 3.55%

## 全模块对比总览（确定性证据）

> 总体覆盖率：共同 AST 指纹: 47；唯一 AST 指纹: 目标项目 4164，参考项目 1324；覆盖率: 目标项目 1.13%，参考项目 3.55%

### 1. 模块覆盖矩阵
| 模块 | 目标函数数 | 参考函数数 | 共同唯一 AST 指纹 | Target 覆盖 | Base 覆盖 | 状态 |
| :--- | ---: | ---: | ---: | ---: | ---: | :--- |
| 进程管理 | 692 | 72 | 1 | 0.16% | 1.54% | 双方存在且有结构重合 |
| 任务调度/异步运行时 | 331 | 92 | 2 | 0.73% | 2.25% | 双方存在且有结构重合 |
| 内存管理 | 827 | 269 | 21 | 2.71% | 9.81% | 双方存在且有结构重合 |
| 中断/异常处理 | 24 | 5 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |
| 系统调用 | 219 | 52 | 8 | 4.17% | 20.0% | 双方存在且有结构重合 |
| 文件系统 | 1960 | 361 | 6 | 0.35% | 2.33% | 双方存在且有结构重合 |
| 设备驱动 | 61 | 308 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |
| 网络协议栈 | 269 | 0 | 0 | 0.0% | 0.0% | 仅目标项目体现 |
| 文件系统/第三方库 | 210 | 0 | 0 | 0.0% | 0.0% | 仅目标项目体现 |
| 通用模块/其他 | 154 | 1728 | 2 | 1.47% | 0.33% | 双方存在且有结构重合 |

### 2. 分模块证据清单

#### 进程管理
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `os/src/syscall/process/misc.rs#sys_setpgid` | `os/src/syscall/process.rs#sys_setpgid` | 同板块同名 | PROCESS_MANAGER.lock, Some, current.group.session_id, current.tgid, pcb.group.pgrp_id, pcb.group.session_id ...(+10) | gdb_println! |
| `os/src/syscall/process/misc.rs#sys_syslog` | `os/src/syscall/process.rs#sys_syslog` | 同板块同名 | bufp.is_null, core::cmp::min, current_memory_set, group.credentials, message.len, with_current_group ...(+2) | UserBuffer::new, clear_log_buf, current_user_token, gdb_println!, read_all_log_buf, read_clear_log_buf ...(+5) |
| `os/src/syscall/process/clone.rs#sys_set_tid_address` | `os/src/syscall/process.rs#sys_set_tid_address` | 同板块同名 | PROCESS_MANAGER.lock, current_pid, log::trace!, pm.current_process_mut, probe_optional_tid_ptr | current_task, current_task().unwrap, current_user_token, gdb_println!, task.acquire_inner_lock, task_inner.gettid ...(+1) |
| `os/src/syscall/process/misc.rs#sys_getpgid` | `os/src/syscall/process.rs#sys_getpgid` | 同板块同名 | PROCESS_MANAGER.lock, pm.current_process, pm.get_process, process
        .map, process
        .map(|process| process.group.pgrp_id() as isize)
        .unwrap_or, process.group.pgrp_id | gdb_println! |

#### 任务调度/异步运行时
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `os/src/syscall/fs/io.rs#sys_write` | `os/src/syscall/fs.rs#sys_write` | 同板块同名 | (fsize_limit != crate::process::RLIM_INFINITY).then_some, PROCESS_MANAGER.lock, STDIO_WRITE_LOCK.lock, Some, Vec::new, chunk.len ...(+45) | UserBuffer::new, current_process, current_user_token, drop, f.writable, f.write ...(+5) |
| `os/src/syscall/fs/meta.rs#sys_getdents64` | `os/src/syscall/fs.rs#sys_getdents64` | 同板块同名 | PROCESS_MANAGER.lock, buf.is_null, crate::fs::vfs_core::api::vfs_getdents_commit, crate::fs::vfs_core::api::vfs_getdents_prepare, dirent64_reclen, dirent_cookie ...(+17) | UserBuffer::new, current_process, current_user_token, gdb_println!, getdents64_inner, inner.cwd.clone ...(+6) |
| `os/src/syscall/fs/poll.rs#sys_ppoll` | `os/src/syscall/fs.rs#sys_ppoll` | 同板块同名 | block_ppoll, crate::process::current_pid, crate::signal::has_unmasked_pending_signal, crate::signal::install_temporary_signal_mask, crate::signal::restore_temporary_signal_mask, get_time_ns ...(+4) | current_process, current_user_token, f.is_some, fds.add, gdb_println!, inner.fd_table.get ...(+2) |
| `os/src/syscall/fs/io.rs#sys_pread64` | `os/src/syscall/fs.rs#sys_pread64` | 同板块同名 | (buf as usize).checked_add, PROCESS_MANAGER.lock, Some, core::cmp::min, crate::fs::vfs_core::api::vfs_api_prepare_positioned_read_target_descriptor, crate::fs::vfs_core::api::vfs_api_read_positioned_chunk ...(+10) | UserBuffer::new, current_process, current_user_token, fi.offset, fi.read, fi.set_offset ...(+4) |

#### 内存管理
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `os/src/arch/riscv/trap/mod.rs#trap_handler` | `os/src/trap/mod.rs#trap_handler` | 同板块同名 | Some, __restore, arm_next_timer_interrupt, crate::arch::sbi::shutdown, crate::arch::set_user_fpu_state, crate::process::PROCESS_MANAGER.lock ...(+28) | Trap::Exception, asm!, current_add_signal, current_process, current_trap_cx, cx.x.iter ...(+15) |
| `os/src/mm/memory_set/loader.rs#from_elf` | `os/src/mm/memory_set.rs#from_elf` | 同板块同名 | MapArea::new, MapArea::new(
                TRAMPOLINE_BASE.into(),
                TRAMPOLINE_BASE.wrapping_add(PAGE_SIZE).into(),
                MapType::Linear(Self::trampoline_map_offset()),
                MapPermission::R | MapPermission::X,
            )
            .with_kind, MapArea::new(
                TRAP_CONTEXT_BASE.into(),
                TRAMPOLINE_BASE.into(),
                MapType::Framed,
                MapPermission::R | MapPermission::W,
            )
            .with_kind, MapArea::new(
                layout.stack_bottom.into(),
                layout.stack_top.into(),
                MapType::Framed,
                MapPermission::R | MapPermission::W | MapPermission::U,
            )
            .with_kind, MapArea::new(
                layout.tls_bottom.into(),
                layout.tls_top.into(),
                MapType::Framed,
                MapPermission::R | MapPermission::W | MapPermission::U,
            )
            .with_kind, MapArea::new(start_va, end_va, MapType::Framed, permission)
                .with_kind ...(+27) | ((ph.virtual_addr() + ph.mem_size()) as usize).into, (ph.virtual_addr() as usize).into, MapArea::new, Self::new_bare, Some, Vec::with_capacity ...(+33) |
| `os/src/syscall/fs/fd.rs#sys_pipe2` | `os/src/syscall/fs.rs#sys_pipe2` | 同板块同名 | (read_fd as u32).to_le_bytes, (write_fd as u32).to_le_bytes, Some, bytes.len, bytes[..4].copy_from_slice, bytes[4..].copy_from_slice ...(+20) | FileClass::Abs, OpenFlags::from_bits, OpenFlags::from_bits(flags).unwrap, Some, current_process, current_user_token ...(+6) |
| `os/src/mm/frame_allocator.rs#frame_alloc` | `os/src/mm/frame_allocator.rs#frame_alloc` | 同板块同名 | FRAME_ALLOCATOR.lock, FRAME_ALLOCATOR.lock().alloc, FRAME_ALLOC_FAIL_STREAK.fetch_add, FRAME_ALLOC_FAIL_STREAK.store, FrameTracker::new, Some ...(+8) | FRAME_ALLOCATOR.write, FRAME_ALLOCATOR.write().alloc, FRAME_ALLOCATOR.write().alloc().map |

#### 中断/异常处理
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `os/src/syscall/fs/poll.rs#sys_pselect6` | `os/src/trap/mod.rs#trap_from_kernel` | 同模块上下文对齐/差异观察 | block_pselect, fast_ready_write_only_select, get_time_ns, is_short_empty_wait, parse_timeout_mode, pre_snapshot_fast_write_only_select ...(+6) | fatal!, panic! |
| `os/src/arch/loongarch/trap/mod.rs#loongarch_kernel_trap_handler` | `os/src/trap/mod.rs#set_kernel_trap_entry` | 同模块上下文对齐/差异观察 | IN_KERNEL_TRAP
        .compare_exchange, IN_KERNEL_TRAP
        .compare_exchange(false, true, Ordering::SeqCst, Ordering::SeqCst)
        .is_err, ecode_name, raw_console_putln, raw_console_puts, shutdown | stvec::write |
| `os/src/arch/riscv/trap/mod.rs#kernel_trap_handler` | `os/src/trap/mod.rs#set_user_trap_entry` | 同模块上下文对齐/差异观察 | IN_KERNEL_TRAP
        .compare_exchange, IN_KERNEL_TRAP
        .compare_exchange(false, true, Ordering::SeqCst, Ordering::SeqCst)
        .is_err, raw_console_putln, raw_console_puts, shutdown | stvec::write |
| `os/src/arch/loongarch/trap/mod.rs#init` | `os/src/trap/mod.rs#enable_timer_interrupt` | 同模块上下文对齐/差异观察 | arm_next_timer_interrupt, core::arch::asm!, crate::mm::kernel_symbol_to_phys_addr, usize::from | sie::set_stimer |

#### 系统调用
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `os/src/syscall/net.rs#sys_shutdown` | `user/src/syscall.rs#sys_shutdown` | 同板块同名 | (0..=2).contains, crate::net::local::unix_socket_shutdown, e.errno, get_any_socket_handle, net::shutdown, trace_net_lookup_err ...(+1) | panic!, syscall |
| `user/src/bin/tools/tool_common/mod.rs#syscall` | `user/src/syscall.rs#syscall` | 同板块同名 | asm! | asm! |
| `user/src/syscall.rs#sys_clone` | `user/src/syscall.rs#sys_clone` | 同板块同名 | syscall | syscall |
| `user/src/lib.rs#waitpid` | `user/src/lib.rs#waitpid` | 同板块同名 | sys_wait4, yield_ | sys_waitpid |

#### 文件系统
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `os/src/syscall/fs/mount.rs#sys_mount` | `os/src/syscall/fs.rs#sys_mount` | 同板块同名 | String::new, build_vfs_path_at_request, crate::fs::vfs_core::api::vfs_mountat, fstype.is_null, read_user_string_checked, src.is_null ...(+1) | gdb_println! |
| `os/src/syscall/fs/meta.rs#sys_statfs` | `os/src/syscall/fs.rs#sys_statfs` | 同板块同名 | buf.is_null, build_vfs_path_at_request, crate::fs::vfs_core::api::vfs_statfsat, path.is_null, path_str.is_empty, read_user_string_checked ...(+2) | Statfs::new, UserBuffer::new, current_user_token, gdb_println!, size_of::<Statfs>, statfs.as_bytes ...(+2) |
| `os/src/syscall/fs/mount.rs#sys_umount` | `os/src/syscall/fs.rs#sys_umount` | 同板块同名 | build_vfs_path_at_request, crate::fs::vfs_core::api::vfs_umountat, read_user_string_checked | gdb_println! |
| `os/src/fs/vfs_core/dentry.rs#empty` | `fat32_fs/src/layout.rs#empty` | 同板块同名 | 内部闭环逻辑 | 内部闭环逻辑 |

#### 设备驱动
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `os/src/drivers/block.rs#read_block` | `os/src/drivers/block/sdcard.rs#read_block` | 同板块同名 | self.inner
            .read_blocks, self.inner
            .read_blocks(block_id, buf)
            .expect | self.0
            .lock, self.0
            .lock()
            .read_sector, self.0
            .lock()
            .read_sector(buf, block_id as u32 + 10274)
            .unwrap |
| `os/src/drivers/block.rs#write_block` | `os/src/drivers/block/sdcard.rs#write_block` | 同板块同名 | self.inner
            .write_blocks, self.inner
            .write_blocks(block_id, buf)
            .expect | println!, self.0
                    .lock, self.0
                    .lock()
                    .write_sector, self.0
                    .lock()
                    .write_sector(buf, block_id as u32 + 10274)
                    .unwrap, self.0.lock, self.0.lock().write_sector |
| `os/src/net/mod.rs#socket_ioctl` | `os/src/drivers/block/sdcard.rs#write_sector` | 同模块上下文对齐/差异观察 | ETH0_DEVICE.lock, LOOPBACK_DEVICE.lock, bytes.len, dev.addr, dev.broadcast, dev.flags ...(+26) | (data_buf.len() / SEC_LEN).try_into, (data_buf.len() / SEC_LEN).try_into().unwrap, Err, Ok, assert!, chunk.iter ...(+10) |
| `os/src/net/mod.rs#ioctl_gifconf` | `os/src/drivers/block/sdcard.rs#read_sector` | 同模块上下文对齐/差异观察 | (needed as i32).to_le_bytes, (written as i32).to_le_bytes, ETH0_DEVICE.lock, LOOPBACK_DEVICE.lock, NetIfDevice::Eth0, NetIfDevice::Loopback ...(+18) | Err, Ok, assert!, chunk.iter_mut, chunk.iter_mut().zip, data_buf.chunks_mut ...(+7) |

#### 网络协议栈
仅目标项目中观察到该模块代表函数，参考项目没有同模块可对齐证据：
- 目标项目：`os/src/net/mod.rs#setsockopt`，调用摘要：Err, Ok, alg::setsockopt, core::cmp::min, i32::from_le_bytes, matches! ...(+12)
- 目标项目：`os/src/net/mod.rs#getsockopt`，调用摘要：(err as u32).to_le_bytes, 0u32.to_le_bytes, Err, Ok, alg::getsockopt, build_tcp_info_bytes ...(+21)
- 目标项目：`user/src/bin/net/ip.rs#main`，调用摘要：Err, IfReq::new, Ok, Some, argc, argv ...(+16)
- 目标项目：`os/src/net/socket.rs#new`，调用摘要：InetEndpoint::unspecified, SocketData::Alg, SocketData::Icmp, SocketData::Netlink, SocketData::Packet, SocketData::Tcp ...(+6)

#### 文件系统/第三方库
仅目标项目中观察到该模块代表函数，参考项目没有同模块可对齐证据：
- 目标项目：`os/src/fs/ext4/ext4.rs#rename_raw`，调用摘要：Err, Ext4FileSystem::file_type_from_inode, Ok, Some, adjust_disk_inode_nlink, cache_dir_entry ...(+33)
- 目标项目：`os/src/fs/ext4/ext4.rs#add_dir_entry`，调用摘要：(actual as u16).to_le_bytes, (rec_len as u16).to_le_bytes, (self.block_size as u16).to_le_bytes, (slack as u16).to_le_bytes, Err, Ok ...(+32)
- 目标项目：`os/src/fs/ext4/ext4.rs#write_inode_at`，调用摘要：((data.len() - done) / self.block_size).min, (self.block_size - in_block_off).min, Err, Ok, Vec::with_capacity, data.is_empty ...(+23)
- 目标项目：`os/src/fs/ext4/ext4.rs#read_inode_at_with_kind`，调用摘要：(first_phys as usize).checked_mul, (self.block_size - in_block_off).min, inode.size, out.is_empty, out.len, out.len().min ...(+9)

#### 通用模块/其他
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `os/src/arch/riscv/sbi.rs#sbi_call` | `os/src/sbi.rs#sbi_call` | 同板块同名 | asm! | asm! |
| `os/src/arch/loongarch/sbi.rs#set_timer` | `os/src/sbi.rs#set_timer` | 同板块同名 | _timer.saturating_sub, _timer.saturating_sub(now).max, core::arch::asm!, crate::arch::timer::get_time | sbi_call |
| `os/src/main.rs#clear_bss` | `os/src/main.rs#clear_bss` | 同板块同名 | ptr::write_bytes | core::slice::from_raw_parts_mut, core::slice::from_raw_parts_mut(sbss as usize as *mut u8, ebss as usize - sbss as usize)
            .fill |
| `os/src/arch/loongarch/sbi.rs#shutdown` | `os/src/sbi.rs#shutdown` | 同板块同名 | (GED_POWER_REG as *mut u8).write_volatile, core::arch::asm! | panic!, sbi_call |


#### 1. 宏观判定
- **定性结论**: **独立开发 / 底层架构显著差异**
- **核心依据**: 客观 AST 相似度仅为 **0.86%**，共同 AST 指纹仅 47 个。目标项目覆盖率 1.13%，参考项目覆盖率 3.55%。在核心争议板块“进程管理”中，双方仅存在 **1** 条共同唯一 AST 指纹，目标覆盖率低至 **0.16%**。该数据量级远不足以支撑“系统性复用”或“同源性”指控，指向两条完全独立的演化路径。

#### 2. 全模块覆盖矩阵解读
- **进程管理**: 双方均存在。共同唯一 AST 指纹仅 1 条，目标覆盖率 0.16%。极低的覆盖率表明双方在进程抽象、同步机制上存在根本性架构差异。证据强度：**弱**。
- **任务调度/异步运行时**: 双方均存在。共同唯一 AST 指纹 2 条，目标覆盖率 0.73%。双方调度器实现（全局管理器 vs. 多核处理器数组）和上下文切换路径完全不同。证据强度：**弱**。
- **内存管理**: 双方均存在。共同唯一 AST 指纹 21 条，目标覆盖率 2.71%。这是覆盖率最高的板块，但指纹多集中于 `from_elf`、`frame_alloc` 等由硬件规范或通用算法强约束的函数，属于领域收敛。证据强度：**中**。
- **中断/异常处理**: 双方均存在。共同唯一 AST 指纹为 0。双方 Trap 处理入口、分发逻辑和内核态 Trap 处理机制无任何结构重合。证据强度：**未形成**。
- **系统调用**: 双方均存在。共同唯一 AST 指纹 8 条，目标覆盖率 4.17%。指纹集中于用户态 `syscall` 汇编指令和少数同名 POSIX 入口，是 ABI 强制约束的必然结果。证据强度：**弱**。
- **文件系统**: 双方均存在。共同唯一 AST 指纹 6 条，目标覆盖率 0.35%。目标项目实现了完整的 VFS、ext4 等复杂子系统，而参考项目为简易文件抽象，规模与复杂度不在一个量级。证据强度：**弱**。
- **设备驱动**: 双方均存在。共同唯一 AST 指纹为 0。双方驱动模型和具体硬件操作逻辑无重合。证据强度：**未形成**。
- **网络协议栈**: **仅目标项目体现**。参考项目无此模块，目标项目实现了完整的 TCP/UDP/Netlink 等协议栈。此为单方独有模块，不构成同源证据。
- **文件系统/第三方库**: **仅目标项目体现**。参考项目无此分类，目标项目包含 ext4 等第三方文件系统实现。此为单方独有模块，不构成同源证据。
- **通用模块/其他**: 双方均存在。共同唯一 AST 指纹 2 条，目标覆盖率 1.47%。指纹为 `sbi_call` 和 `clear_bss`，属于 RISC-V SBI 标准调用和内核启动的必要初始化步骤，是平台强制约束。证据强度：**弱**。

#### 3. 底层 AST 关键证据指纹表
| 所属板块 | 目标项目路径 | 参考项目路径 | 证据类型 | 技术说明 |
| :--- | :--- | :--- | :--- | :--- |
| 内存管理 | `os/src/mm/memory_set/loader.rs#from_elf` | `os/src/mm/memory_set.rs#from_elf` | 领域收敛 | 均遵循 ELF 标准格式解析，调用 `MapArea::new` 映射各段。目标项目映射了更多段（如 Trampoline），且权限与映射类型管理更复杂。 |
| 内存管理 | `os/src/mm/frame_allocator.rs#frame_alloc` | `os/src/mm/frame_allocator.rs#frame_alloc` | 领域收敛 | 均实现基于全局分配器的物理页帧分配。目标项目增加了分配失败计数器等监控逻辑，但核心的 `alloc` 调用流是通用模式。 |
| 系统调用 | `user/src/bin/tools/tool_common/mod.rs#syscall` | `user/src/syscall.rs#syscall` | 语言/ABI约束 | 用户态发起系统调用的底层 `asm!` 指令，由 RISC-V `ecall` 指令和 ABI 规范强制定义，无独创性。 |
| 系统调用 | `user/src/syscall.rs#sys_clone` | `user/src/syscall.rs#sys_clone` | 语言/ABI约束 | 均为对 `syscall` 函数的简单包装，传递系统调用号，是标准用户库实现方式。 |
| 通用模块/其他 | `os/src/arch/riscv/sbi.rs#sbi_call` | `os/src/sbi.rs#sbi_call` | 平台强制 | RISC-V SBI 标准调用规范，通过 `asm!` 执行 `ecall`，是固件接口的强制实现方式。 |
| 通用模块/其他 | `os/src/main.rs#clear_bss` | `os/src/main.rs#clear_bss` | 平台强制 | 内核启动时清零 BSS 段的必要操作，是链接脚本和 C/Rust 运行时要求的通用初始化步骤。 |

#### 4. 核心代码调用流对比剖析
* **【进程管理 / 进程退出与回收路径】**
* **底层技术析理**: 参考项目采用简化的教学模型，`sys_exit` 直接调用 `exit_current_and_run_next` 并打印调试信息，无复杂的资源清理链。目标项目则构建了完整的多阶段清理流程，`sys_exit` 调用 `perform_exit`，进而触发 `cleanup_exiting_threads`，该函数通过 `PROCESS_MANAGER.lock` 同步，并依次调用 `vfs_api_cleanup_fd_table_on_exit`、`vfs_api_remove_all_pipe_read_waiters_for_pid` 等 VFS 层接口进行文件描述符和管道资源的回收，最后操作 `WAIT4_WAITERS` 唤醒父进程。两者在**抽象层次**（直接退出 vs. 多阶段资源清理）、**同步策略**（任务内部锁 vs. 全局进程管理器锁）和**对象管理模型**（无 VFS 清理 vs. 调用独立 VFS API）上存在根本性异构。
* **代码溯源链接**:
  - 目标项目: `os/src/syscall/process/exit.rs#sys_exit` -> `os/src/syscall/process/exit.rs#cleanup_exiting_threads`
  - 参考项目: `os/src/syscall/process.rs#sys_exit`

* **【进程管理 / Futex 同步机制】**
* **底层技术析理**: 参考项目使用 `FUTEX_QUEUE` 配合 `FutexWaiter` 结构体实现简单的等待队列，通过 `block_current_and_run_next` 阻塞任务。目标项目则实现了更接近 Linux 的完整 Futex 模型，使用全局 `FUTEX_WAITERS` 映射管理等待者，并提供 `futex_wake_matching` 进行匹配唤醒，同时额外实现了 `robust_list_release_on_exit` 等健壮性特性。两者在**内存模型**（队列 vs. 全局映射）、**同步原语**（`FUTEX_QUEUE.write` vs. `FUTEX_WAITERS.lock`）和**功能完整性**（无 robust 支持 vs. 有 robust 支持）上差异显著。
* **代码溯源链接**:
  - 目标项目: `os/src/syscall/process/futex.rs#sys_futex`
  - 参考项目: `os/src/syscall/sync.rs#futex_wait`

* **【任务调度 / 上下文切换】**
* **底层技术析理**: 参考项目采用多核处理器数组 `PROCESSORS[get_hartid()]` 管理调度上下文，通过 `__switch` 函数进行任务切换。目标项目则采用全局 `PROCESS_MANAGER` 统一管理所有核的进程，上下文切换路径为 `switch_to_process` -> `__restore`，并在切换前后增加了 `prepare_user_trap_return`、`set_user_fpu_state` 等更复杂的用户态现场准备与恢复逻辑，以及 `drain_recycled_processes_if_pressure` 等回收压力控制机制。两者在**调度架构**（per-HART vs. Global Manager）和**上下文切换流程**上完全不同。
* **代码溯源链接**:
  - 目标项目: `os/src/syscall/process/sched.rs#switch_to_process`
  - 参考项目: `os/src/task/processor.rs#schedule`

#### 5. 单方模块与降权项
- **单方独有模块**:
  - **网络协议栈**: 目标项目 `os/src/net/mod.rs` 等文件实现了完整的网络协议栈，参考项目无此模块。此为全新增功能，无法构成同源证据。
  - **文件系统/第三方库**: 目标项目 `os/src/fs/ext4/` 等文件实现了 ext4 文件系统，参考项目无此模块。此为全新增功能，无法构成同源证据。
- **降权项说明**:
  - **同名 POSIX 接口**: 如 `sys_getpid`、`sys_yield` 等，其函数名由 Linux 系统调用 ABI 强制规定，任何兼容内核都必须使用。目标项目与参考项目在这些函数上的实现路径（如 `current_tgid` vs. `current_task().unwrap()...`）完全不同，不能作为同源证据。
  - **跨板块配对**: 控方证据中出现的 `sys_pselect6` 与 `trap_from_kernel`、`socket_ioctl` 与 `write_sector` 等配对，属于跨模块硬凑，功能领域无逻辑关联，应依法排除。
  - **第三方库/公共依赖**: 双方共同使用的 Rust 标准库核心 API（如 `Some`、`Vec::new`、`unwrap_or`）和 RISC-V 汇编指令，属于语言和平台公共依赖，不具独创性，不能作为侵权证据。

#### 6. 保守结论
本次审计基于 0.86% 的客观 AST 相似度，该数值远低于任何可认定为“代码复用”的合理阈值。控方提出的“系统性复用”指控，实质上是将 POSIX 标准接口、Rust 语言惯用法和 CPU 硬件强制约束等“领域收敛”现象，误读为抄袭证据。目标项目在进程管理、内存管理、调度器等核心子系统上，与参考项目在抽象层次、同步策略和内存模型上均表现出根本性的架构差异，并独立实现了网络协议栈、ext4 文件系统等参考项目完全不具备的复杂模块。因此，综合判定目标项目 `[2026-T202610213999576-XX]` 为独立开发作品，整体代码同源性风险极低。

------------------------------

