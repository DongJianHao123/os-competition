# 🛡️ OS 内核赛自动化代码级架构审计报告

**审计时间**: `2026-07-02 15:29:29`
**参赛作品名称**: `2026-T202610213999576-XX`
**年份**: `2026`
**学校**: `XX`
**队伍名称**: `T202610213999576`
**比赛名字**: `OS-Competition`
**比赛子赛道**: `Kernel-Implementation`
**仓库地址**: `https://gitlab.eduxiji.net/educg-group-43501-3132633/T202610213999576-845`
**推测开发语言**: `Rust / C (由AST探针自动识别)`
**特征函数量**: `4747`

## ⚔️ 底层代码同源性对比 (AST Homology)
> 本部分由专门的代码审查智能体，对该项目与符合年份条件的历届最高相似度项目进行控制流与底层调用对比。新版报告会先给出确定性的全模块覆盖矩阵，再给出 Agent 的分模块解释，避免只围绕少量抽样函数展开。

### 🏆 Top 3 底层对标: vs `2022-caporustos-浙江大学`（浙江大学 / caporustos）

#### 对方作品信息

**对方年份**: `2022`
**对方学校**: `浙江大学`
**对方队伍名称**: `caporustos`
**对方比赛名字**: `OS-Competition`
**对方比赛子赛道**: `Kernel-Implementation`
**对方仓库地址**: `https://gitlab.eduxiji.net/yhwu_is/oskernel2022-caporustos`

> 双向覆盖率：共同 AST 指纹: 52；唯一 AST 指纹: 目标项目 4164，参考项目 1959；覆盖率: 目标项目 1.25%，参考项目 2.65%

## 全模块对比总览（确定性证据）

> 总体覆盖率：共同 AST 指纹: 52；唯一 AST 指纹: 目标项目 4164，参考项目 1959；覆盖率: 目标项目 1.25%，参考项目 2.65%

### 1. 模块覆盖矩阵
| 模块 | 目标函数数 | 参考函数数 | 共同唯一 AST 指纹 | Target 覆盖 | Base 覆盖 | 状态 |
| :--- | ---: | ---: | ---: | ---: | ---: | :--- |
| 进程管理 | 692 | 161 | 1 | 0.16% | 0.76% | 双方存在且有结构重合 |
| 任务调度/异步运行时 | 331 | 34 | 2 | 0.73% | 6.06% | 双方存在且有结构重合 |
| 内存管理 | 827 | 232 | 23 | 2.97% | 10.85% | 双方存在且有结构重合 |
| 中断/异常处理 | 24 | 10 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |
| 系统调用 | 219 | 49 | 8 | 4.17% | 25.0% | 双方存在且有结构重合 |
| 文件系统 | 1960 | 259 | 3 | 0.17% | 1.42% | 双方存在且有结构重合 |
| 设备驱动 | 61 | 36 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |
| 网络协议栈 | 269 | 0 | 0 | 0.0% | 0.0% | 仅目标项目体现 |
| 文件系统/第三方库 | 210 | 2 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |
| 第三方库 | 0 | 2951 | 0 | 0.0% | 0.0% | 仅参考项目体现 |
| 通用模块/其他 | 154 | 41 | 4 | 2.94% | 10.81% | 双方存在且有结构重合 |

### 2. 分模块证据清单

#### 进程管理
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `os/src/syscall/process/clone.rs#sys_set_tid_address` | `codes/os/src/syscall/process.rs#sys_set_tid_address` | 同板块同名 | PROCESS_MANAGER.lock, current_pid, log::trace!, pm.current_process_mut, probe_optional_tid_ptr | current_task, current_task().unwrap, current_task().unwrap().get_parent, current_task().unwrap().get_parent().unwrap, current_task().unwrap().get_parent().unwrap().acquire_inner_lock, sys_gettid |
| `os/src/process/current.rs#current_user_token` | `codes/os/src/process/processor/processor.rs#current_user_token` | 同板块同名 | PROCESS_MANAGER
        .lock, PROCESS_MANAGER
        .lock()
        .current_process, PROCESS_MANAGER
        .lock()
        .current_process()
        .map, PROCESS_MANAGER
        .lock()
        .current_process()
        .map(|p| p.get_token())
        .unwrap_or, p.get_token | current_task, current_task().unwrap, task.get_parent, task.get_parent().unwrap, task.get_parent().unwrap().acquire_inner_lock, task.get_parent().unwrap().acquire_inner_lock().get_user_token |
| `os/src/syscall/process/sched.rs#sys_yield` | `codes/os/src/syscall/process.rs#sys_yield` | 同板块同名 | maybe_schedule_after_syscall | suspend_current_and_run_next |
| `os/src/syscall/process/misc.rs#sys_getegid` | `codes/os/src/syscall/process.rs#sys_getegid` | 同板块同名 | group.credentials, with_current_group, with_current_group(|group| group.credentials().egid as isize).unwrap_or | 内部闭环逻辑 |

#### 任务调度/异步运行时
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `os/src/syscall/fs/meta.rs#sys_getdents64` | `codes/os/src/syscall/fs.rs#sys_getdents64` | 同板块同名 | PROCESS_MANAGER.lock, buf.is_null, crate::fs::vfs_core::api::vfs_getdents_commit, crate::fs::vfs_core::api::vfs_getdents_prepare, dirent64_reclen, dirent_cookie ...(+17) | UserBuffer::new, current_task, current_task().unwrap, current_user_token, getdents64_inner, inner.current_path.clone ...(+9) |
| `os/src/syscall/fs/fd.rs#sys_ioctl` | `codes/os/src/syscall/fs.rs#sys_ioctl` | 同板块同名 | crate::fs::vfs_core::api::vfs_api_fd_ioctl_target_descriptor, crate::fs::vfs_core::api::vfs_api_fd_ioctl_target_is_loop_control, crate::fs::vfs_core::api::vfs_api_fd_ioctl_target_is_proc_namespace_or_unsupported, crate::fs::vfs_core::api::vfs_api_fd_ioctl_target_is_rtc, crate::fs::vfs_core::api::vfs_api_fd_ioctl_target_is_socket, crate::fs::vfs_core::api::vfs_api_fd_ioctl_target_is_tty ...(+18) | current_task, current_task().unwrap, current_user_token, drop, f.clone, file.ioctl ...(+4) |
| `os/src/syscall/fs/io.rs#sys_pread64` | `codes/os/src/syscall/fs.rs#sys_pread64` | 同板块同名 | (buf as usize).checked_add, PROCESS_MANAGER.lock, Some, core::cmp::min, crate::fs::vfs_core::api::vfs_api_prepare_positioned_read_target_descriptor, crate::fs::vfs_core::api::vfs_api_read_positioned_chunk ...(+10) | UserBuffer::new, current_task, current_task().unwrap, current_user_token, file.get_offset, file.read ...(+9) |
| `os/src/syscall/fs/path.rs#sys_getcwd` | `codes/os/src/syscall/fs.rs#sys_getcwd` | 同板块同名 | Some, String::from, Vec::with_capacity, buf.is_null, cwd.as_bytes, cwd.as_bytes().get ...(+13) | UserBuffer::new, current_task, current_task().unwrap, current_user_token, inner.current_path.as_bytes, task.acquire_inner_lock ...(+4) |

#### 内存管理
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `os/src/syscall/mod.rs#syscall` | `codes/os/src/syscall/mod.rs#syscall` | 同板块同名 | Some, alloc::string::String::new, core::ptr::null, core::ptr::null_mut, crate::process::PROCESS_MANAGER.lock, crate::process::current_pid ...(+236) | core::arch::asm!, fcntl, gdb_print!, reset_time_int, shutdown, sys_brk ...(+59) |
| `os/src/syscall/mm.rs#sys_mmap` | `codes/os/src/syscall/process.rs#sys_mmap` | 同板块同名 | PROCESS_MANAGER.lock, Some, VirtAddr::from, align_up_len, align_up_to, fd_table.get_mut ...(+36) | current_task, current_task().unwrap, gdb_println!, task.mmap, thread.get_parent, thread.get_parent().unwrap |
| `os/src/arch/riscv/trap/mod.rs#trap_handler` | `codes/os/src/trap/mod.rs#trap_handler` | 同板块同名 | Some, __restore, arm_next_timer_interrupt, crate::arch::sbi::shutdown, crate::arch::set_user_fpu_state, crate::process::PROCESS_MANAGER.lock ...(+28) | (stval as usize).into, TRAMPOLINE.into, Trap::Exception, core::arch::asm!, current_task, current_task().unwrap ...(+22) |
| `os/src/mm/memory_set/loader.rs#from_elf` | `codes/os/src/mm/memory_set.rs#from_elf` | 同板块同名 | MapArea::new, MapArea::new(
                TRAMPOLINE_BASE.into(),
                TRAMPOLINE_BASE.wrapping_add(PAGE_SIZE).into(),
                MapType::Linear(Self::trampoline_map_offset()),
                MapPermission::R | MapPermission::X,
            )
            .with_kind, MapArea::new(
                TRAP_CONTEXT_BASE.into(),
                TRAMPOLINE_BASE.into(),
                MapType::Framed,
                MapPermission::R | MapPermission::W,
            )
            .with_kind, MapArea::new(
                layout.stack_bottom.into(),
                layout.stack_top.into(),
                MapType::Framed,
                MapPermission::R | MapPermission::W | MapPermission::U,
            )
            .with_kind, MapArea::new(
                layout.tls_bottom.into(),
                layout.tls_top.into(),
                MapType::Framed,
                MapPermission::R | MapPermission::W | MapPermission::U,
            )
            .with_kind, MapArea::new(start_va, end_va, MapType::Framed, permission)
                .with_kind ...(+27) | ((ph.virtual_addr() + ph.mem_size()) as usize).into, (TRAP_CONTEXT+PAGE_SIZE).into, (ph.virtual_addr() as usize).into, MapArea::new, Self::new_bare, Some ...(+37) |

#### 中断/异常处理
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `os/src/syscall/fs/poll.rs#sys_pselect6` | `codes/os/src/trap/mod.rs#trap_from_kernel` | 同模块上下文对齐/差异观察 | block_pselect, fast_ready_write_only_select, get_time_ns, is_short_empty_wait, parse_timeout_mode, pre_snapshot_fast_write_only_select ...(+6) | panic! |
| `os/src/arch/loongarch/trap/mod.rs#loongarch_kernel_trap_handler` | `codes/os/src/syscall/mod.rs#reset_time_int` | 同模块上下文对齐/差异观察 | IN_KERNEL_TRAP
        .compare_exchange, IN_KERNEL_TRAP
        .compare_exchange(false, true, Ordering::SeqCst, Ordering::SeqCst)
        .is_err, ecode_name, raw_console_putln, raw_console_puts, shutdown | crate::timer::set_next_trigger, crate::trap::enable_timer_interrupt |
| `os/src/arch/riscv/trap/mod.rs#kernel_trap_handler` | `codes/os/src/trap/mod.rs#set_kernel_trap_entry` | 同模块上下文对齐/差异观察 | IN_KERNEL_TRAP
        .compare_exchange, IN_KERNEL_TRAP
        .compare_exchange(false, true, Ordering::SeqCst, Ordering::SeqCst)
        .is_err, raw_console_putln, raw_console_puts, shutdown | stvec::write |
| `os/src/arch/loongarch/trap/mod.rs#init` | `codes/os/src/trap/mod.rs#set_user_trap_entry` | 同模块上下文对齐/差异观察 | arm_next_timer_interrupt, core::arch::asm!, crate::mm::kernel_symbol_to_phys_addr, usize::from | stvec::write |

#### 系统调用
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `os/src/syscall/net.rs#sys_shutdown` | `codes/user/src/syscall.rs#sys_shutdown` | 同板块同名 | (0..=2).contains, crate::net::local::unix_socket_shutdown, e.errno, get_any_socket_handle, net::shutdown, trace_net_lookup_err ...(+1) | syscall |
| `user/src/lib.rs#execve` | `codes/user/src/lib.rs#execve` | 同板块同名 | argv.iter, argv.iter().enumerate, argv.len, argv_p.as_ptr, envp.iter, envp.iter().enumerate ...(+6) | sys_execve |
| `user/src/bin/tools/tool_common/mod.rs#syscall` | `codes/user/src/syscall.rs#syscall` | 同板块同名 | asm! | core::arch::asm! |
| `user/src/lib.rs#waitpid` | `codes/user/src/lib.rs#waitpid` | 同板块同名 | sys_wait4, yield_ | sys_waitpid, yield_ |

#### 文件系统
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `os/src/syscall/fs/mount.rs#sys_mount` | `codes/os/src/syscall/fs.rs#sys_mount` | 同板块同名 | String::new, build_vfs_path_at_request, crate::fs::vfs_core::api::vfs_mountat, fstype.is_null, read_user_string_checked, src.is_null ...(+1) | MNT_TABLE.lock, MNT_TABLE.lock().mount, current_user_token, translated_str |
| `os/src/syscall/fs/meta.rs#sys_statfs` | `codes/os/src/syscall/fs.rs#sys_statfs` | 同板块同名 | buf.is_null, build_vfs_path_at_request, crate::fs::vfs_core::api::vfs_statfsat, path.is_null, path_str.is_empty, read_user_string_checked ...(+2) | Statfs::new, UserBuffer::new, current_user_token, gdb_println!, size_of::<Statfs>, statfs.as_bytes ...(+2) |
| `os/src/syscall/fs/mount.rs#sys_umount` | `codes/os/src/syscall/fs.rs#sys_umount` | 同板块同名 | build_vfs_path_at_request, crate::fs::vfs_core::api::vfs_umountat, read_user_string_checked | MNT_TABLE.lock, MNT_TABLE.lock().umount, current_user_token, translated_str |
| `os/src/fs/vfs_core/dentry.rs#empty` | `codes/fat32_fs/src/layout.rs#empty` | 同板块同名 | 内部闭环逻辑 | 内部闭环逻辑 |

#### 设备驱动
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `os/src/drivers/block.rs#read_block` | `codes/os/src/drivers/block/sdcard.rs#read_block` | 同板块同名 | self.inner
            .read_blocks, self.inner
            .read_blocks(block_id, buf)
            .expect | self.0
            .lock, self.0
            .lock()
            .read_sector, self.0
            .lock()
            .read_sector(buf, block_id as u32 + 10274)
            .unwrap |
| `os/src/drivers/block.rs#write_block` | `codes/os/src/drivers/block/sdcard.rs#write_block` | 同板块同名 | self.inner
            .write_blocks, self.inner
            .write_blocks(block_id, buf)
            .expect | println!, self.0
                    .lock, self.0
                    .lock()
                    .write_sector, self.0
                    .lock()
                    .write_sector(buf, block_id as u32 + 10274)
                    .unwrap, self.0.lock, self.0.lock().write_sector |
| `os/src/net/mod.rs#socket_ioctl` | `codes/os/src/drivers/block/sdcard.rs#write_sector` | 同模块上下文对齐/差异观察 | ETH0_DEVICE.lock, LOOPBACK_DEVICE.lock, bytes.len, dev.addr, dev.broadcast, dev.flags ...(+26) | (data_buf.len() / SEC_LEN).try_into, (data_buf.len() / SEC_LEN).try_into().unwrap, Err, Ok, assert!, chunk.iter ...(+10) |
| `os/src/net/mod.rs#ioctl_gifconf` | `codes/os/src/drivers/block/sdcard.rs#read_sector` | 同模块上下文对齐/差异观察 | (needed as i32).to_le_bytes, (written as i32).to_le_bytes, ETH0_DEVICE.lock, LOOPBACK_DEVICE.lock, NetIfDevice::Eth0, NetIfDevice::Loopback ...(+18) | Err, Ok, assert!, chunk.iter_mut, chunk.iter_mut().zip, data_buf.chunks_mut ...(+7) |

#### 网络协议栈
仅目标项目中观察到该模块代表函数，参考项目没有同模块可对齐证据：
- 目标项目：`os/src/net/mod.rs#setsockopt`，调用摘要：Err, Ok, alg::setsockopt, core::cmp::min, i32::from_le_bytes, matches! ...(+12)
- 目标项目：`os/src/net/mod.rs#getsockopt`，调用摘要：(err as u32).to_le_bytes, 0u32.to_le_bytes, Err, Ok, alg::getsockopt, build_tcp_info_bytes ...(+21)
- 目标项目：`user/src/bin/net/ip.rs#main`，调用摘要：Err, IfReq::new, Ok, Some, argc, argv ...(+16)
- 目标项目：`os/src/net/socket.rs#new`，调用摘要：InetEndpoint::unspecified, SocketData::Alg, SocketData::Icmp, SocketData::Netlink, SocketData::Packet, SocketData::Tcp ...(+6)

#### 文件系统/第三方库
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `os/src/fs/ext4/ext4.rs#rename_raw` | `codes/dependency/fu740-pac/src/spi0/fmt.rs#dir` | 同模块上下文对齐/差异观察 | Err, Ext4FileSystem::file_type_from_inode, Ok, Some, adjust_disk_inode_nlink, cache_dir_entry ...(+33) | DIR_R::new |
| `os/src/fs/ext4/ext4.rs#add_dir_entry` | `codes/dependency/fu740-pac/src/spi0/fmt.rs#dir` | 同模块上下文对齐/差异观察 | (actual as u16).to_le_bytes, (rec_len as u16).to_le_bytes, (self.block_size as u16).to_le_bytes, (slack as u16).to_le_bytes, Err, Ok ...(+32) | 内部闭环逻辑 |

#### 第三方库
仅参考项目中观察到该模块代表函数，目标项目没有同模块可对齐证据：
- 参考项目：`codes/dependency/num-derive/src/lib.rs#float`，调用摘要：NumTraits::new, import.wrap, import.wrap("Float", &name, impl_).into, newtype_inner, newtype_inner(&ast.data).expect, parse! ...(+1)
- 参考项目：`codes/dependency/num-derive/src/lib.rs#to_primitive`，调用摘要：NumTraits::new, import.wrap, import.wrap("ToPrimitive", &name, impl_).into, newtype_inner, panic!, parse! ...(+5)
- 参考项目：`codes/dependency/num-derive/src/lib.rs#from_primitive`，调用摘要：NumTraits::new, clauses.is_empty, import.wrap, import.wrap("FromPrimitive", &name, impl_).into, newtype_inner, panic! ...(+5)
- 参考项目：`codes/dependency/log/src/kv/value.rs#visit`，调用摘要：Visitor, self.0
                    .visit_any, self.0
                    .visit_any(Value { inner: value })
                    .map_err, self.0.visit_bool, self.0.visit_bool(value).map_err, self.0.visit_borrowed_error ...(+21)

#### 通用模块/其他
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `os/src/arch/riscv/sbi.rs#sbi_call` | `codes/os/src/sbi.rs#sbi_call` | 同板块同名 | asm! | core::arch::asm! |
| `os/src/arch/loongarch/sbi.rs#set_timer` | `codes/os/src/sbi.rs#set_timer` | 同板块同名 | _timer.saturating_sub, _timer.saturating_sub(now).max, core::arch::asm!, crate::arch::timer::get_time | sbi_call |
| `os/src/main.rs#clear_bss` | `codes/os/src/main.rs#clear_bss` | 同板块同名 | ptr::write_bytes | (a as *mut u8).write_volatile, (sbss as usize..ebss as usize).for_each |
| `os/src/arch/loongarch/sbi.rs#shutdown` | `codes/os/src/sbi.rs#shutdown` | 同板块同名 | (GED_POWER_REG as *mut u8).write_volatile, core::arch::asm! | panic!, sbi_call |


好的，审判长。本庭已对双方提交的全部证据、控辩意见及底层 AST 指纹进行了独立、深度的析理。现做出如下最终裁决。

### 1. 宏观判定
- **定性结论**: **独立开发 / 底层架构显著差异**
- **核心依据**: 双方项目整体 AST 相似度仅为 **0.86%**，共同 AST 指纹仅 52 个。在争议焦点的【进程管理】板块，共同指纹仅 1 个，目标项目覆盖率低至 **0.16%**。该量级远低于可认定代码复用的阈值，属于噪声级巧合。双方在进程模型、内存管理、系统调用路径上的架构实现存在根本性异构。

### 2. 全模块覆盖矩阵解读
- **进程管理**:
  1. 双方均存在。
  2. 存在 1 个共同 AST 指纹。
  3. Target 覆盖 0.16%，Base 覆盖 0.76%，表明在 692 vs 161 个函数的庞大体量下，结构重合几乎为零。
  4. **证据强度: 未形成**。控方四项指控均因调用对象、数据结构、功能层级不同而被驳回。
- **任务调度/异步运行时**:
  1. 双方均存在。
  2. 存在 2 个共同 AST 指纹。
  3. Target 覆盖 0.73%，Base 覆盖 6.06%。共同指纹为 `sys_getdents64`、`sys_ioctl` 等跨板块函数，非调度核心逻辑。
  4. **证据强度: 未形成**。调度器实现完全不同（`PROCESS_MANAGER` 全局模型 vs `current_task` 线程局部模型）。
- **内存管理**:
  1. 双方均存在。
  2. 存在 23 个共同 AST 指纹。
  3. Target 覆盖 2.97%，Base 覆盖 10.85%。共同指纹集中在 `syscall` 分发、`sys_mmap`、`trap_handler`、`from_elf` 等通用模式。
  4. **证据强度: 弱**。`from_elf` 等函数受 ELF 标准格式约束，且双方在 `MapArea` 构造、地址空间管理上存在差异。
- **中断/异常处理**:
  1. 双方均存在。
  2. 共同 AST 指纹为 0。
  3. Target 覆盖 0.0%，Base 覆盖 0.0%。
  4. **证据强度: 未形成**。双方 Trap 处理逻辑无任何结构重合。
- **系统调用**:
  1. 双方均存在。
  2. 存在 8 个共同 AST 指纹。
  3. Target 覆盖 4.17%，Base 覆盖 25.0%。共同指纹多为用户态库函数（如 `execve`、`waitpid`）或系统调用分发入口。
  4. **证据强度: 弱**。用户态库函数受 POSIX 接口强约束，内核态分发入口为通用 `match` 模式。
- **文件系统**:
  1. 双方均存在。
  2. 存在 3 个共同 AST 指纹。
  3. Target 覆盖 0.17%，Base 覆盖 1.42%。共同指纹为 `sys_mount`、`sys_statfs` 等系统调用入口。
  4. **证据强度: 未形成**。被告为 ext4/VFS 架构，原告为简单 FAT 架构，实现完全不同。
- **设备驱动**:
  1. 双方均存在。
  2. 共同 AST 指纹为 0。
  3. Target 覆盖 0.0%，Base 覆盖 0.0%。
  4. **证据强度: 未形成**。双方驱动模型无结构重合。
- **网络协议栈**:
  1. 仅目标项目体现。
  2. 不存在共同 AST 指纹。
  3. 参考项目无此模块。
  4. **证据强度: 未形成**。属于被告独有功能集。
- **第三方库/公共依赖**:
  1. 仅参考项目体现（2951 个函数）。
  2. 不存在共同 AST 指纹。
  3. 目标项目无此模块。
  4. **证据强度: 未形成**。属于原告独有依赖。

### 3. 底层 AST 关键证据指纹表
| 所属板块 | 目标项目路径 | 参考项目路径 | 证据类型 | 技术说明 |
| :--- | :--- | :--- | :--- | :--- |
| 进程管理 | `os/src/syscall/process/clone.rs#sys_set_tid_address` | `codes/os/src/syscall/process.rs#sys_set_tid_address` | 同板块同名 | 函数名由 Linux ABI 强制规定。被告调用 `probe_optional_tid_ptr`，原告调用 `acquire_inner_lock`，实现路径完全不同。 |
| 进程管理 | `os/src/process/current.rs#current_user_token` | `codes/os/src/process/processor/processor.rs#current_user_token` | 同板块同名 | 被告通过全局 `PROCESS_MANAGER.lock()` 获取，原告通过线程局部 `current_task().unwrap().get_parent()` 获取，架构根本不同。 |
| 进程管理 | `os/src/syscall/process/sched.rs#sys_yield` | `codes/os/src/syscall/process.rs#sys_yield` | 同板块同名 | 被告调用 `maybe_schedule_after_syscall`，原告调用 `suspend_current_and_run_next`，AST 无交集。 |
| 内存管理 | `os/src/syscall/mod.rs#syscall` | `codes/os/src/syscall/mod.rs#syscall` | 同板块同名 | 系统调用分发入口，均为基于调用号的 `match` 分发模式，属于通用实现范式。 |
| 内存管理 | `os/src/syscall/mm.rs#sys_mmap` | `codes/os/src/syscall/process.rs#sys_mmap` | 同板块同名 | 被告使用 `VirtAddr`、`fd_table`，原告使用 `task.mmap`，内存映射路径不同。 |
| 内存管理 | `os/src/arch/riscv/trap/mod.rs#trap_handler` | `codes/os/src/trap/mod.rs#trap_handler` | 同板块同名 | Trap 入口，均包含上下文保存/恢复，但被告包含 `arm_next_timer_interrupt` 等特有逻辑。 |
| 内存管理 | `os/src/mm/memory_set/loader.rs#from_elf` | `codes/os/src/mm/memory_set.rs#from_elf` | 同板块同名 | ELF 加载入口，均构造 `MapArea`，但被告包含 `TRAMPOLINE`、`TLS` 等特有布局，原告无。 |
| 系统调用 | `user/src/lib.rs#execve` | `codes/user/src/lib.rs#execve` | 同板块同名 | 用户态库函数，受 POSIX 标准约束，参数传递模式相似但实现不同。 |
| 系统调用 | `user/src/lib.rs#waitpid` | `codes/user/src/lib.rs#waitpid` | 同板块同名 | 用户态库函数，受 POSIX 标准约束，均调用 `yield_` 但内核实现不同。 |
| 文件系统 | `os/src/syscall/fs/mount.rs#sys_mount` | `codes/os/src/syscall/fs.rs#sys_mount` | 同板块同名 | 被告调用 `vfs_mountat`，原告调用 `MNT_TABLE.lock().mount`，VFS 架构完全不同。 |
| 通用模块/其他 | `os/src/arch/riscv/sbi.rs#sbi_call` | `codes/os/src/sbi.rs#sbi_call` | 同板块同名 | SBI 调用封装，均为 `asm!` 宏，属于 RISC-V 架构标准调用约定。 |
| 通用模块/其他 | `os/src/main.rs#clear_bss` | `codes/os/src/main.rs#clear_bss` | 同板块同名 | BSS 段清零，均为标准启动流程，实现方式不同（`write_bytes` vs `write_volatile`）。 |

### 4. 核心代码调用流对比剖析
* **【进程管理 / execve 系统调用】**
* **底层技术析理**: 被告采用全局 `PROCESS_MANAGER` 锁与 VFS API 进行权限预检查，参数通过 `Vec` 与迭代器收集；原告直接操作 `KERNEL_SPACE` 与 `MemorySet::from_elf` 加载 ELF，并初始化 `TrapContext`。两者在抽象层次（VFS vs 直接内存操作）、权限模型（credentials vs 无）和调用对象上完全不同。
* **代码溯源链接**:
  - 目标项目: `os/src/syscall/process/exec.rs#sys_execve`
  - 参考项目: `codes/os/src/process/process/core/pcb.rs#execve`

* **【进程管理 / sigtimedwait 系统调用】**
* **底层技术析理**: 被告基于全局 `SIGTIMEDWAIT_WAITERS` 哈希表实现阻塞等待，与调度器 `switch_to_process` 深度耦合；原告基于线程局部 `inner.lock` 与 `pending.mask` 操作，通过 `translated_ref` 回写用户态。两者在同步策略（全局等待表 vs 线程局部锁）和调度交互上存在根本性架构差异。
* **代码溯源链接**:
  - 目标项目: `os/src/signal/mod.rs#rt_sigtimedwait`
  - 参考项目: `codes/os/src/syscall/signal.rs#sys_sigtimedwait`

* **【内存管理 / mmap 系统调用】**
* **底层技术析理**: 被告使用 `VirtAddr::from`、`align_up_len` 进行地址对齐，并通过 `fd_table` 处理文件映射；原告通过 `current_task().unwrap().mmap` 委托给任务对象。两者在地址空间操作和对象管理模型上存在差异。
* **代码溯源链接**:
  - 目标项目: `os/src/syscall/mm.rs#sys_mmap`
  - 参考项目: `codes/os/src/syscall/process.rs#sys_mmap`

### 5. 单方模块与降权项
- **单方模块**:
  - **网络协议栈**: 仅目标项目体现，包含 `setsockopt`、`getsockopt` 等完整实现，原告项目无此模块。
  - **第三方库**: 仅参考项目体现，包含 `num-derive`、`log` 等依赖库，目标项目无此模块。
- **降权项说明**:
  - 所有同名函数（如 `sys_getpid`、`sys_exit`、`sys_yield` 等）均因 POSIX/Linux ABI 强制命名而降权。其内部 AST 调用图谱已发生根本性偏移，不构成同源证据。
  - 跨板块硬凑证据（如将 `block_epoll_wait` 归入进程管理）已被排除，因原告项目无 epoll、futex、seccomp 等子系统。

### 6. 保守结论
本庭认定，被告项目 `2026-T202610213999576-XX` 与参考项目 `2022-caporustos-浙江大学` 在整体架构与核心模块实现上存在根本性差异。0.86% 的 AST 相似度与 1.25% 的覆盖率属于噪声级巧合，主要源于 POSIX 标准约束、Rust 标准库调用及 RISC-V 架构规范。控方提出的“全局管理器重构洗稿”指控缺乏客观 AST 证据支持，其列举的四项核心指控均因调用对象、数据结构、同步策略及功能层级的不同而被推翻。被告项目具备原告项目所没有的网络协议栈、ext4 文件系统、LoongArch 架构支持及高级安全机制，进一步佐证了其独立开发的本质。综上，本庭裁决被告在【进程管理】及所有其他板块均不存在基于原告代码的实质性复用。

------------------------------

