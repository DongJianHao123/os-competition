# 🛡️ OS 内核赛自动化代码级架构审计报告

**审计时间**: `2026-07-02 15:29:29`
**参赛作品名称**: `2026-T202610213999576-XX`
**年份**: `2026`
**学校**: `XX`
**队伍名称**: `T202610213999576`
**比赛名字**: `OS-Competition`
**比赛子赛道**: `Kernel-Implementation`
**仓库地址**: `https://gitlab.eduxiji.net/educg-group-43501-3132633/T202610213999576-845`
**推测开发语言**: `Rust / C (由AST探针自动识别)`
**特征函数量**: `4747`

## ⚔️ 底层代码同源性对比 (AST Homology)
> 本部分由专门的代码审查智能体，对该项目与符合年份条件的历届最高相似度项目进行控制流与底层调用对比。新版报告会先给出确定性的全模块覆盖矩阵，再给出 Agent 的分模块解释，避免只围绕少量抽样函数展开。

### 🏆 Top 4 底层对标: vs `2022-S0S-哈尔滨工业大学`（哈尔滨工业大学 / S0S）

#### 对方作品信息

**对方年份**: `2022`
**对方学校**: `哈尔滨工业大学`
**对方队伍名称**: `S0S`
**对方比赛名字**: `OS-Competition`
**对方比赛子赛道**: `Kernel-Implementation`
**对方仓库地址**: `https://gitlab.eduxiji.net/educg-group-14158-894147/oskernel2022-s0s`

> 双向覆盖率：共同 AST 指纹: 39；唯一 AST 指纹: 目标项目 4164，参考项目 447；覆盖率: 目标项目 0.94%，参考项目 8.72%

## 全模块对比总览（确定性证据）

> 总体覆盖率：共同 AST 指纹: 39；唯一 AST 指纹: 目标项目 4164，参考项目 447；覆盖率: 目标项目 0.94%，参考项目 8.72%

### 1. 模块覆盖矩阵
| 模块 | 目标函数数 | 参考函数数 | 共同唯一 AST 指纹 | Target 覆盖 | Base 覆盖 | 状态 |
| :--- | ---: | ---: | ---: | ---: | ---: | :--- |
| 进程管理 | 692 | 51 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |
| 任务调度/异步运行时 | 331 | 41 | 1 | 0.36% | 2.5% | 双方存在且有结构重合 |
| 内存管理 | 827 | 139 | 20 | 2.58% | 15.75% | 双方存在且有结构重合 |
| 中断/异常处理 | 24 | 5 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |
| 系统调用 | 219 | 35 | 6 | 3.12% | 25.0% | 双方存在且有结构重合 |
| 文件系统 | 1960 | 166 | 2 | 0.12% | 1.34% | 双方存在且有结构重合 |
| 设备驱动 | 61 | 42 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |
| 网络协议栈 | 269 | 0 | 0 | 0.0% | 0.0% | 仅目标项目体现 |
| 文件系统/第三方库 | 210 | 0 | 0 | 0.0% | 0.0% | 仅目标项目体现 |
| 通用模块/其他 | 154 | 31 | 4 | 2.94% | 13.79% | 双方存在且有结构重合 |

### 2. 分模块证据清单

#### 进程管理
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `os/src/syscall/process/clone.rs#sys_set_tid_address` | `os/src/syscall/process.rs#sys_set_tid_address` | 同板块同名 | PROCESS_MANAGER.lock, current_pid, log::trace!, pm.current_process_mut, probe_optional_tid_ptr | current_task, current_task().unwrap, current_task().unwrap().getpid, current_user_token, translated_refmut |
| `os/src/process/current.rs#current_user_token` | `os/src/task/processor.rs#current_user_token` | 同板块同名 | PROCESS_MANAGER
        .lock, PROCESS_MANAGER
        .lock()
        .current_process, PROCESS_MANAGER
        .lock()
        .current_process()
        .map, PROCESS_MANAGER
        .lock()
        .current_process()
        .map(|p| p.get_token())
        .unwrap_or, p.get_token | current_task, current_task().unwrap, task.inner_exclusive_access, task.inner_exclusive_access().get_user_token |
| `os/src/syscall/process/sched.rs#sys_yield` | `os/src/syscall/process.rs#sys_yield` | 同板块同名 | maybe_schedule_after_syscall | suspend_current_and_run_next |
| `os/src/syscall/process/misc.rs#sys_getuid` | `os/src/syscall/process.rs#sys_getuid` | 同板块同名 | group.credentials, with_current_group, with_current_group(|group| group.credentials().uid as isize).unwrap_or | 内部闭环逻辑 |

#### 任务调度/异步运行时
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `os/src/syscall/fs/io.rs#sys_write` | `os/src/syscall/fs.rs#sys_write` | 同板块同名 | (fsize_limit != crate::process::RLIM_INFINITY).then_some, PROCESS_MANAGER.lock, STDIO_WRITE_LOCK.lock, Some, Vec::new, chunk.len ...(+45) | UserBuffer::new, current_task, current_task().unwrap, current_user_token, drop, f.clone ...(+5) |
| `os/src/syscall/fs/meta.rs#sys_getdents64` | `os/src/syscall/fs.rs#sys_getdents64` | 同板块同名 | PROCESS_MANAGER.lock, buf.is_null, crate::fs::vfs_core::api::vfs_getdents_commit, crate::fs::vfs_core::api::vfs_getdents_prepare, dirent64_reclen, dirent_cookie ...(+17) | UserBuffer::new, buf.read, current_task, current_task().unwrap, current_user_token, inner.fd_table.len ...(+4) |
| `user/src/bin/tools/gzip.rs#rust_main` | `os/src/main.rs#rust_main` | 同板块同名 | arg, argc, basename, copy_stdin_stdout, eq, exit ...(+2) | LOCK.load, LOCK.store, clear_bss, fs::add_initproc_shell, fs::list_apps, hartid ...(+11) |
| `os/src/syscall/fs/path.rs#sys_chdir` | `os/src/syscall/fs.rs#sys_chdir` | 同板块同名 | crate::fs::vfs_core::api::vfs_api_resolve_chdir_target_text, crate::fs::vfs_core::api::vfs_prepare_directory_context_path, path_str.is_empty, read_user_string_checked, with_current_fs_access_subject, with_fs_context ...(+1) | current_task, current_task().unwrap, current_user_token, open_file_at, open_file_at(AT_FDCWD, &path, OpenFlags::RDWR).unwrap, remove_from_file_table ...(+2) |

#### 内存管理
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `os/src/syscall/mm.rs#sys_mmap` | `os/src/syscall/process.rs#sys_mmap` | 同板块同名 | PROCESS_MANAGER.lock, Some, VirtAddr::from, align_up_len, align_up_to, fd_table.get_mut ...(+36) | MapFlags::from_bits, MapFlags::from_bits(flags).unwrap, ProtFlags::from_bits, ProtFlags::from_bits(prot).unwrap, UserBuffer::new, Vec::new ...(+20) |
| `os/src/arch/riscv/trap/mod.rs#trap_handler` | `os/src/trap/mod.rs#trap_handler` | 同板块同名 | Some, __restore, arm_next_timer_interrupt, crate::arch::sbi::shutdown, crate::arch::set_user_fpu_state, crate::process::PROCESS_MANAGER.lock ...(+28) | check_signals_error_of_current, current_add_signal, current_trap_cx, exit_current_and_run_next, handle_signals, panic! ...(+9) |
| `os/src/mm/memory_set/loader.rs#from_elf` | `os/src/mm/memory_set.rs#from_elf` | 同板块同名 | MapArea::new, MapArea::new(
                TRAMPOLINE_BASE.into(),
                TRAMPOLINE_BASE.wrapping_add(PAGE_SIZE).into(),
                MapType::Linear(Self::trampoline_map_offset()),
                MapPermission::R | MapPermission::X,
            )
            .with_kind, MapArea::new(
                TRAP_CONTEXT_BASE.into(),
                TRAMPOLINE_BASE.into(),
                MapType::Framed,
                MapPermission::R | MapPermission::W,
            )
            .with_kind, MapArea::new(
                layout.stack_bottom.into(),
                layout.stack_top.into(),
                MapType::Framed,
                MapPermission::R | MapPermission::W | MapPermission::U,
            )
            .with_kind, MapArea::new(
                layout.tls_bottom.into(),
                layout.tls_top.into(),
                MapType::Framed,
                MapPermission::R | MapPermission::W | MapPermission::U,
            )
            .with_kind, MapArea::new(start_va, end_va, MapType::Framed, permission)
                .with_kind ...(+27) | ((ph.virtual_addr() + ph.mem_size()) as usize).into, (ph.virtual_addr() as usize).into, Aux::new, MapArea::new, Self::new_bare, Some ...(+33) |
| `os/src/syscall/fs/fd.rs#sys_pipe2` | `os/src/syscall/fs.rs#sys_pipe2` | 同板块同名 | (read_fd as u32).to_le_bytes, (write_fd as u32).to_le_bytes, Some, bytes.len, bytes[..4].copy_from_slice, bytes[4..].copy_from_slice ...(+20) | FileClass::PIPE, Some, current_task, current_task().unwrap, current_user_token, inner.alloc_fd ...(+4) |

#### 中断/异常处理
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `os/src/syscall/fs/poll.rs#sys_pselect6` | `os/src/trap/mod.rs#trap_from_kernel` | 同模块上下文对齐/差异观察 | block_pselect, fast_ready_write_only_select, get_time_ns, is_short_empty_wait, parse_timeout_mode, pre_snapshot_fast_write_only_select ...(+6) | panic!, println! |
| `os/src/arch/loongarch/trap/mod.rs#loongarch_kernel_trap_handler` | `os/src/trap/mod.rs#set_kernel_trap_entry` | 同模块上下文对齐/差异观察 | IN_KERNEL_TRAP
        .compare_exchange, IN_KERNEL_TRAP
        .compare_exchange(false, true, Ordering::SeqCst, Ordering::SeqCst)
        .is_err, ecode_name, raw_console_putln, raw_console_puts, shutdown | stvec::write |
| `os/src/arch/riscv/trap/mod.rs#kernel_trap_handler` | `os/src/trap/mod.rs#set_user_trap_entry` | 同模块上下文对齐/差异观察 | IN_KERNEL_TRAP
        .compare_exchange, IN_KERNEL_TRAP
        .compare_exchange(false, true, Ordering::SeqCst, Ordering::SeqCst)
        .is_err, raw_console_putln, raw_console_puts, shutdown | stvec::write |
| `os/src/arch/loongarch/trap/mod.rs#init` | `os/src/trap/mod.rs#enable_timer_interrupt` | 同模块上下文对齐/差异观察 | arm_next_timer_interrupt, core::arch::asm!, crate::mm::kernel_symbol_to_phys_addr, usize::from | sie::set_stimer |

#### 系统调用
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `os/src/syscall/time/sleep.rs#sys_nanosleep` | `user/src/syscall.rs#sys_nanosleep` | 同板块同名 | (ts.sec as usize)
        .saturating_mul, (ts.sec as usize)
        .saturating_mul(1_000_000_000)
        .saturating_add, deadline_ns.saturating_sub, get_time_ns, get_time_ns().saturating_add, ns_to_timespec ...(+5) | syscall |
| `os/src/syscall/net.rs#sys_shutdown` | `user/src/syscall.rs#sys_shutdown` | 同板块同名 | (0..=2).contains, crate::net::local::unix_socket_shutdown, e.errno, get_any_socket_handle, net::shutdown, trace_net_lookup_err ...(+1) | syscall |
| `user/src/bin/tools/tool_common/mod.rs#syscall` | `user/src/syscall.rs#syscall` | 同板块同名 | asm! | asm! |
| `user/src/lib.rs#waitpid` | `user/src/lib.rs#waitpid` | 同板块同名 | sys_wait4, yield_ | sys_waitpid |

#### 文件系统
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `os/src/syscall/fs/path.rs#sys_renameat2` | `os/src/syscall/fs.rs#sys_renameat2` | 同板块同名 | build_vfs_path_at_request, crate::fs::vfs_core::api::vfs_renameat2, newpath.is_null, notify_dnotify_rename, oldpath.is_null, read_user_string_checked | current_user_token, rename, translated_str |
| `os/src/syscall/fs/path.rs#sys_faccessat` | `os/src/syscall/fs.rs#sys_faccessat` | 同板块同名 | build_vfs_path_at_request, crate::fs::vfs_core::api::vfs_accessat, crate::fs::vfs_core::api::vfs_fd_access, crate::fs::vfs_core::api::vfs_fd_access(
                    crate::fs::vfs_core::api::VfsFdAccessRequest {
                        file: &file,
                        mode: mode as u32,
                        subject,
                    },
                )
                .err, crate::fs::vfs_core::api::vfs_fd_access(
                    crate::fs::vfs_core::api::VfsFdAccessRequest {
                        file: &file,
                        mode: mode as u32,
                        subject,
                    },
                )
                .err()
                .unwrap_or, fd_table.get ...(+5) | current_user_token, open_file_at, open_file_at(dirfd, path.as_str(), OpenFlags::RDWR).is_some, path.as_str, translated_str |
| `os/src/syscall/fs/mount.rs#sys_mount` | `os/src/syscall/fs.rs#sys_mount` | 同板块同名 | String::new, build_vfs_path_at_request, crate::fs::vfs_core::api::vfs_mountat, fstype.is_null, read_user_string_checked, src.is_null ...(+1) | 内部闭环逻辑 |
| `os/src/syscall/fs/path.rs#sys_utimensat` | `os/src/syscall/fs.rs#sys_utimensat` | 同板块同名 | build_vfs_path_at_request, crate::fs::vfs_core::api::vfs_utimensat, parse_utime_request, path.is_null, path_str.is_empty, read_user_string_checked ...(+3) | current_user_token, open_file_at, open_file_at(dirfd, path.as_str(), flags).is_some, path.as_str, translated_str |

#### 设备驱动
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `os/src/drivers/block.rs#read_block` | `os/src/drivers/block/virtio_blk.rs#read_block` | 同板块同名 | self.inner
            .read_blocks, self.inner
            .read_blocks(block_id, buf)
            .expect | self.0
            .lock, self.0
            .lock()
            .read_block, self.0
            .lock()
            .read_block(block_id, buf)
            .expect |
| `os/src/drivers/block.rs#write_block` | `os/src/drivers/block/virtio_blk.rs#write_block` | 同板块同名 | self.inner
            .write_blocks, self.inner
            .write_blocks(block_id, buf)
            .expect | self.0
            .lock, self.0
            .lock()
            .write_block, self.0
            .lock()
            .write_block(block_id, buf)
            .expect |
| `os/src/net/mod.rs#socket_ioctl` | `os/src/drivers/block/sdcard.rs#read_sector` | 同模块上下文对齐/差异观察 | ETH0_DEVICE.lock, LOOPBACK_DEVICE.lock, bytes.len, dev.addr, dev.broadcast, dev.flags ...(+26) | Err, Ok, assert!, chunk.iter_mut, chunk.iter_mut().zip, data_buf.chunks_mut ...(+6) |
| `os/src/net/mod.rs#ioctl_gifconf` | `os/src/drivers/block/sdcard.rs#write_sector` | 同模块上下文对齐/差异观察 | (needed as i32).to_le_bytes, (written as i32).to_le_bytes, ETH0_DEVICE.lock, LOOPBACK_DEVICE.lock, NetIfDevice::Eth0, NetIfDevice::Loopback ...(+18) | (data_buf.len() / SEC_LEN).try_into, (data_buf.len() / SEC_LEN).try_into().unwrap, Err, Ok, assert!, chunk.iter ...(+9) |

#### 网络协议栈
仅目标项目中观察到该模块代表函数，参考项目没有同模块可对齐证据：
- 目标项目：`os/src/net/mod.rs#setsockopt`，调用摘要：Err, Ok, alg::setsockopt, core::cmp::min, i32::from_le_bytes, matches! ...(+12)
- 目标项目：`os/src/net/mod.rs#getsockopt`，调用摘要：(err as u32).to_le_bytes, 0u32.to_le_bytes, Err, Ok, alg::getsockopt, build_tcp_info_bytes ...(+21)
- 目标项目：`user/src/bin/net/ip.rs#main`，调用摘要：Err, IfReq::new, Ok, Some, argc, argv ...(+16)
- 目标项目：`os/src/net/socket.rs#new`，调用摘要：InetEndpoint::unspecified, SocketData::Alg, SocketData::Icmp, SocketData::Netlink, SocketData::Packet, SocketData::Tcp ...(+6)

#### 文件系统/第三方库
仅目标项目中观察到该模块代表函数，参考项目没有同模块可对齐证据：
- 目标项目：`os/src/fs/ext4/ext4.rs#rename_raw`，调用摘要：Err, Ext4FileSystem::file_type_from_inode, Ok, Some, adjust_disk_inode_nlink, cache_dir_entry ...(+33)
- 目标项目：`os/src/fs/ext4/ext4.rs#add_dir_entry`，调用摘要：(actual as u16).to_le_bytes, (rec_len as u16).to_le_bytes, (self.block_size as u16).to_le_bytes, (slack as u16).to_le_bytes, Err, Ok ...(+32)
- 目标项目：`os/src/fs/ext4/ext4.rs#write_inode_at`，调用摘要：((data.len() - done) / self.block_size).min, (self.block_size - in_block_off).min, Err, Ok, Vec::with_capacity, data.is_empty ...(+23)
- 目标项目：`os/src/fs/ext4/ext4.rs#read_inode_at_with_kind`，调用摘要：(first_phys as usize).checked_mul, (self.block_size - in_block_off).min, inode.size, out.is_empty, out.len, out.len().min ...(+9)

#### 通用模块/其他
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `os/src/arch/riscv/sbi.rs#sbi_call` | `os/src/sbi.rs#sbi_call` | 同板块同名 | asm! | asm! |
| `os/src/arch/loongarch/sbi.rs#set_timer` | `os/src/sbi.rs#set_timer` | 同板块同名 | _timer.saturating_sub, _timer.saturating_sub(now).max, core::arch::asm!, crate::arch::timer::get_time | sbi_call |
| `os/src/main.rs#clear_bss` | `os/src/main.rs#clear_bss` | 同板块同名 | ptr::write_bytes | core::slice::from_raw_parts_mut, core::slice::from_raw_parts_mut(sbss as usize as *mut u8, ebss as usize - sbss as usize)
            .fill |
| `os/src/global_logger.rs#test` | `user/src/bin/user_shell.rs#test` | 同板块同名 | log::debug!, log::error!, log::info!, log::trace!, log::warn!, println! | busybox_test, lua_test, println!, shutdown |


#### 1. 宏观判定
- **定性结论**: 底层架构显著差异 / 独立开发 (相似度: 0.85%，共同 AST 指纹: 39；唯一 AST 指纹: 目标项目 4164，参考项目 447；覆盖率: 目标项目 0.94%，参考项目 8.72%)

#### 2. 全模块覆盖矩阵解读
- **进程管理**: 双方均存在该模块。共同 AST 指纹为 0，目标覆盖率 0.0%，参考覆盖率 0.0%。双方在进程管理领域不存在可被检测器识别的共同代码结构，证据强度为**未形成**。
- **任务调度/异步运行时**: 双方均存在该模块。存在 1 个共同 AST 指纹，目标覆盖率 0.36%，参考覆盖率 2.5%。该指纹经核验为 `sys_write` 等系统调用入口的通用范式，非特异性复用，证据强度为**弱**。
- **内存管理**: 双方均存在该模块。存在 20 个共同 AST 指纹，目标覆盖率 2.58%，参考覆盖率 15.75%。共同指纹集中在 `sys_mmap`、`from_elf` 等函数，但外部调用对象（如 `PROCESS_MANAGER` vs `current_task`）和内存模型差异显著，证据强度为**中**。
- **中断/异常处理**: 双方均存在该模块。共同 AST 指纹为 0，目标覆盖率 0.0%，参考覆盖率 0.0%。双方 Trap 处理逻辑完全不同，证据强度为**未形成**。
- **系统调用**: 双方均存在该模块。存在 6 个共同 AST 指纹，目标覆盖率 3.12%，参考覆盖率 25.0%。共同指纹多为 `asm!`、`syscall` 等底层指令或用户态封装，属于架构强制范式，证据强度为**弱**。
- **文件系统**: 双方均存在该模块。存在 2 个共同 AST 指纹，目标覆盖率 0.12%，参考覆盖率 1.34%。共同指纹为 `sys_renameat2` 等 POSIX 标准接口，外部调用对象完全不同，证据强度为**弱**。
- **设备驱动**: 双方均存在该模块。共同 AST 指纹为 0，目标覆盖率 0.0%，参考覆盖率 0.0%。双方驱动模型和硬件抽象层完全不同，证据强度为**未形成**。
- **网络协议栈**: 仅目标项目存在该模块，参考项目无对应模块。目标项目独立实现了完整的网络协议栈，证据强度为**未形成**。
- **第三方库/公共依赖**: 仅目标项目存在该模块，参考项目无对应模块。目标项目独立集成了 ext4 等第三方库，证据强度为**未形成**。

#### 3. 底层 AST 关键证据指纹表
| 所属板块 | 目标项目路径 | 参考项目路径 | 证据类型 | 技术说明 |
| :--- | :--- | :--- | :--- | :--- |
| 内存管理 | os/src/syscall/mm.rs#sys_mmap | os/src/syscall/process.rs#sys_mmap | 同板块同名 | 双方均实现 mmap，但目标使用 `PROCESS_MANAGER.lock` 全局锁，参考使用 `current_task` 局部变量，内存抽象不同。 |
| 内存管理 | os/src/mm/memory_set/loader.rs#from_elf | os/src/mm/memory_set.rs#from_elf | 同板块同名 | 双方均实现 ELF 加载，但目标使用 `MapArea::new` 构建器模式，参考使用 `Self::new_bare`，对象管理模型不同。 |
| 系统调用 | user/src/bin/tools/tool_common/mod.rs#syscall | user/src/syscall.rs#syscall | 同板块同名 | 双方均使用 `asm!` 宏进行系统调用，这是 RISC-V/LoongArch 架构的底层指令，属于硬件强制范式。 |
| 通用模块/其他 | os/src/arch/riscv/sbi.rs#sbi_call | os/src/sbi.rs#sbi_call | 同板块同名 | 双方均使用 `asm!` 宏进行 SBI 调用，这是 RISC-V 架构的底层指令，属于硬件强制范式。 |
| 通用模块/其他 | os/src/main.rs#clear_bss | os/src/main.rs#clear_bss | 同板块同名 | 双方均实现 BSS 段清零，但目标使用 `ptr::write_bytes`，参考使用 `from_raw_parts_mut`，实现方式不同。 |
| 任务调度/异步运行时 | os/src/syscall/fs/io.rs#sys_write | os/src/syscall/fs.rs#sys_write | 同板块同名 | 双方均实现 write 系统调用，但目标使用 `STDIO_WRITE_LOCK` 等复杂锁机制，参考使用简单的 `UserBuffer`，同步策略不同。 |
| 文件系统 | os/src/syscall/fs/path.rs#sys_renameat2 | os/src/syscall/fs.rs#sys_renameat2 | 同板块同名 | 双方均实现 renameat2，但目标使用 `vfs_core::api` 虚拟文件系统接口，参考直接操作 `inode`，抽象层次不同。 |
| 进程管理 | os/src/syscall/process/clone.rs#sys_set_tid_address | os/src/syscall/process.rs#sys_set_tid_address | 同板块同名 | 双方均实现 set_tid_address，但目标使用 `PROCESS_MANAGER.lock`，参考使用 `current_task`，进程管理模型不同。 |
| 进程管理 | os/src/process/current.rs#current_user_token | os/src/task/processor.rs#current_user_token | 同板块同名 | 双方均实现获取用户 Token，但目标使用 `PROCESS_MANAGER.lock().current_process()`，参考使用 `current_task().unwrap()`，对象访问路径不同。 |
| 进程管理 | os/src/syscall/process/sched.rs#sys_yield | os/src/syscall/process.rs#sys_yield | 同板块同名 | 双方均实现 yield，但目标委托给 `maybe_schedule_after_syscall`，参考委托给 `suspend_current_and_run_next`，调度器接口不同。 |

#### 4. 核心代码调用流对比剖析
* **【进程管理/上下文获取】**
* **底层技术析理**: 目标项目通过全局 `PROCESS_MANAGER` 锁获取当前进程，再通过 `Option::map` 提取 Token，体现了全局进程表 + 防御性编程的抽象层次。参考项目通过 `current_task()` 获取线程局部变量，再通过 `inner_exclusive_access()` 获取互斥访问，体现了任务局部存储 + 内部可变性的设计。两者在内存模型和同步策略上存在根本性差异。
* **代码溯源链接**:
  - 目标项目: `os/src/process/current.rs#current_user_token`
  - 参考项目: `os/src/task/processor.rs#current_user_token`

* **【内存管理/ELF加载】**
* **底层技术析理**: 目标项目使用 `MapArea::new` 构建器模式，为每个段（如 Trampoline、Trap Context、Stack、TLS）显式创建 `MapArea` 并配置 `MapType` 和 `MapPermission`，体现了细粒度的内存区域管理。参考项目使用 `Self::new_bare` 创建空的 `MemorySet`，然后通过 `push` 方法逐步添加区域，体现了更简单的聚合式内存管理。两者在对象创建和管理模型上异构。
* **代码溯源链接**:
  - 目标项目: `os/src/mm/memory_set/loader.rs#from_elf`
  - 参考项目: `os/src/mm/memory_set.rs#from_elf`

* **【系统调用/底层指令】**
* **底层技术析理**: 双方在用户态系统调用封装中均使用 `asm!` 宏，这是 RISC-V/LoongArch 指令集架构进行特权级切换的唯一方式，属于硬件强制范式，不构成软件层面的复用证据。
* **代码溯源链接**:
  - 目标项目: `user/src/bin/tools/tool_common/mod.rs#syscall`
  - 参考项目: `user/src/syscall.rs#syscall`

#### 5. 单方模块与降权项
- **网络协议栈**: 目标项目独有，包含 `setsockopt`、`getsockopt`、`socket_ioctl` 等完整实现，参考项目无任何对应。这是目标项目独立开发的核心子系统，不能作为同源证据。
- **文件系统/第三方库**: 目标项目独有，包含 `ext4` 文件系统的完整实现（如 `rename_raw`、`add_dir_entry`、`write_inode_at` 等），参考项目无任何对应。这是目标项目独立集成的第三方库，不能作为同源证据。
- **公共接口与泛型函数名**: 双方共有的 `sys_getpid`、`sys_execve`、`sys_exit` 等函数名是 POSIX/Linux ABI 的强制标准名称，任何兼容 Linux 的操作系统都必须使用。`Vec::new`、`.lock()`、`asm!`、`println!` 等是 Rust 语言和标准库的通用构造，频繁出现在任何 Rust 项目中。这些均降级为弱信号，不构成特异性复用证据。

#### 6. 保守结论
本次审计基于 0.85% 的极低 AST 相似度，认定目标项目与参考项目在底层架构上存在显著差异，属于独立开发。39 个共同 AST 指纹在 4000+ 唯一指纹中占比不足 1%，且经核验均为 POSIX 标准接口名、Rust 语言惯用法或硬件强制指令，不构成实质性代码复用。目标项目在进程管理、内存管理、文件系统等核心模块上采用了与参考项目完全不同的全局锁、虚拟文件系统接口和构建器模式，并独立实现了网络协议栈、ext4 文件系统等参考项目完全缺失的复杂子系统。整体来看，目标项目不存在对参考项目的系统性抄袭或换皮复用，原创性应予认定。

------------------------------

