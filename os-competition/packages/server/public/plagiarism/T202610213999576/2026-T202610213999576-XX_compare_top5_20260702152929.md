# 🛡️ OS 内核赛自动化代码级架构审计报告

**审计时间**: `2026-07-02 15:29:29`
**参赛作品名称**: `2026-T202610213999576-XX`
**年份**: `2026`
**学校**: `XX`
**队伍名称**: `T202610213999576`
**比赛名字**: `OS-Competition`
**比赛子赛道**: `Kernel-Implementation`
**仓库地址**: `https://gitlab.eduxiji.net/educg-group-43501-3132633/T202610213999576-845`
**推测开发语言**: `Rust / C (由AST探针自动识别)`
**特征函数量**: `4747`

## ⚔️ 底层代码同源性对比 (AST Homology)
> 本部分由专门的代码审查智能体，对该项目与符合年份条件的历届最高相似度项目进行控制流与底层调用对比。新版报告会先给出确定性的全模块覆盖矩阵，再给出 Agent 的分模块解释，避免只围绕少量抽样函数展开。

### 🏆 Top 5 底层对标: vs `2022-OopS-哈尔滨工业大学深圳`（哈尔滨工业大学（深圳） / OopS）

#### 对方作品信息

**对方年份**: `2022`
**对方学校**: `哈尔滨工业大学（深圳）`
**对方队伍名称**: `OopS`
**对方比赛名字**: `OS-Competition`
**对方比赛子赛道**: `Kernel-Implementation`
**对方仓库地址**: `https://gitlab.eduxiji.net/ZYF_2001/oskernel2022-oops.git`

> 双向覆盖率：共同 AST 指纹: 41；唯一 AST 指纹: 目标项目 4164，参考项目 784；覆盖率: 目标项目 0.98%，参考项目 5.23%

## 全模块对比总览（确定性证据）

> 总体覆盖率：共同 AST 指纹: 41；唯一 AST 指纹: 目标项目 4164，参考项目 784；覆盖率: 目标项目 0.98%，参考项目 5.23%

### 1. 模块覆盖矩阵
| 模块 | 目标函数数 | 参考函数数 | 共同唯一 AST 指纹 | Target 覆盖 | Base 覆盖 | 状态 |
| :--- | ---: | ---: | ---: | ---: | ---: | :--- |
| 进程管理 | 692 | 188 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |
| 任务调度/异步运行时 | 331 | 49 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |
| 内存管理 | 827 | 215 | 20 | 2.58% | 10.47% | 双方存在且有结构重合 |
| 中断/异常处理 | 24 | 8 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |
| 系统调用 | 219 | 74 | 10 | 5.21% | 16.67% | 双方存在且有结构重合 |
| 文件系统 | 1960 | 310 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |
| 设备驱动 | 61 | 24 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |
| 网络协议栈 | 269 | 22 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |
| 文件系统/第三方库 | 210 | 0 | 0 | 0.0% | 0.0% | 仅目标项目体现 |
| 通用模块/其他 | 154 | 52 | 3 | 2.21% | 6.0% | 双方存在且有结构重合 |

### 2. 分模块证据清单

#### 进程管理
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `os/src/syscall/process/futex.rs#sys_futex` | `kernel/src/syscall/process.rs#sys_futex` | 同板块同名 | FUTEX_WAITERS.lock, PROCESS_MANAGER.lock, Some, cancel_timeout, consume_early_shared_wake, consume_pending_wake ...(+44) | do_futex_requeue, do_futex_wait, do_futex_wake, trace!, unimplemented! |
| `os/src/syscall/process/wait.rs#sys_wait4` | `kernel/src/syscall/process.rs#sys_wait4` | 同板块同名 | IOZONE_FORCE_TIMER_LOG.store, IOZONE_TIMER_LOG_BUDGET.store, PROCESS_MANAGER.lock, WAIT4_WAITERS.lock, WAIT4_WAITERS.lock().insert, WAIT4_WAITERS.lock().remove ...(+31) | Err, Ok, Some, child.get_pid, child.is_zombie, child_task.get_exit_code ...(+22) |
| `os/src/syscall/process/exec.rs#sys_execve` | `kernel/src/syscall/process.rs#sys_execve` | 同板块同名 | PROCESS_MANAGER.lock, Vec::new, arg.clone, argv_vec.iter, argv_vec.iter().skip, crate::fs::vfs_core::api::vfs_precheck_exec_access ...(+28) | "busybox".into, Err, Ok, Path::from_string, String::from, Vec::new ...(+22) |
| `os/src/syscall/process/misc.rs#sys_setpgid` | `kernel/src/syscall/process.rs#sys_setpgid` | 同板块同名 | PROCESS_MANAGER.lock, Some, current.group.session_id, current.tgid, pcb.group.pgrp_id, pcb.group.session_id ...(+10) | Ok |

#### 任务调度/异步运行时
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `os/src/syscall/fs/io.rs#sys_write` | `kernel/src/syscall/fs.rs#sys_write` | 同板块同名 | (fsize_limit != crate::process::RLIM_INFINITY).then_some, PROCESS_MANAGER.lock, STDIO_WRITE_LOCK.lock, Some, Vec::new, chunk.len ...(+45) | Ok, copyin_vec, file.write, get_current_task, get_current_task().unwrap, task.get_file ...(+2) |
| `os/src/syscall/fs/poll.rs#sys_ppoll` | `kernel/src/syscall/fs.rs#sys_ppoll` | 同板块同名 | block_ppoll, crate::process::current_pid, crate::signal::has_unmasked_pending_signal, crate::signal::install_temporary_signal_mask, crate::signal::restore_temporary_signal_mask, get_time_ns ...(+4) | Err, Ok, Pollfd::default, STDIO_BUF.lock, Vec::new, buf.append ...(+10) |
| `os/src/syscall/net.rs#sys_connect` | `kernel/src/syscall/net.rs#sys_connect` | 同板块同名 | crate::net::local::unix_socket_connect, crate::syscall::process::yield_and_restart_interruptible_syscall, e.errno, fd_is_nonblock, get_any_socket_handle, net::connect ...(+5) | get_current_task, get_current_task().unwrap, socket.as_socket, socket.as_socket()?.connect, socket.as_socket()?.connect().map, task.get_fd_table ...(+1) |
| `os/src/syscall/time/clock.rs#sys_getrusage` | `kernel/src/syscall/other.rs#sys_getrusage` | 同板块同名 | PROCESS_MANAGER.lock, TimeVal::default, group.rusage_children_maxrss_kb, group.update_rusage_self_maxrss_kb, memory_set.resident_user_memory_kb_fast, next_rusage_cpu_time_for_key ...(+7) | Default::default, Ok, Timeval::from_tick, copyout, get_current_task, get_current_task().unwrap ...(+2) |

#### 内存管理
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `os/src/syscall/mm.rs#sys_mmap` | `kernel/src/syscall/memory.rs#sys_mmap` | 同板块同名 | PROCESS_MANAGER.lock, Some, VirtAddr::from, align_up_len, align_up_to, fd_table.get_mut ...(+36) | (start + len).into, Err, MapArea::new_pro, MapProt::from_mmap_prot, MmapFlag::from_bits, MmapFlag::from_bits(flags).ok_or ...(+18) |
| `os/src/arch/riscv/trap/mod.rs#trap_handler` | `kernel/src/trap/mod.rs#trap_handler` | 同板块同名 | Some, __restore, arm_next_timer_interrupt, crate::arch::sbi::shutdown, crate::arch::set_user_fpu_state, crate::process::PROCESS_MANAGER.lock ...(+28) | get_current_trap_context, pagefault_handler, panic!, reset_sstatus, scause.cause, scause::read ...(+7) |
| `os/src/mm/frame_allocator.rs#frame_alloc` | `kernel/src/memory/frame_allocator.rs#frame_alloc` | 同板块同名 | FRAME_ALLOCATOR.lock, FRAME_ALLOCATOR.lock().alloc, FRAME_ALLOC_FAIL_STREAK.fetch_add, FRAME_ALLOC_FAIL_STREAK.store, FrameTracker::new, Some ...(+8) | FRAME_ALLOCATOR.lock, FRAME_ALLOCATOR.lock().alloc, FRAME_ALLOCATOR.lock().alloc().map |
| `os/src/mm/vmalloc.rs#alloc` | `kernel/src/utils/allocator.rs#alloc` | 同板块同名 | PROCESS_MANAGER.lock, Some, VMALLOC_PENDING_FREE.lock, VMALLOC_REGIONS.lock, Vec::with_capacity, VirtAddr::from ...(+16) | Some, self.recycled.pop |

#### 中断/异常处理
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `os/src/arch/riscv/trap/mod.rs#kernel_trap_handler` | `kernel/src/trap/mod.rs#kernel_trap_handler` | 同板块同名 | IN_KERNEL_TRAP
        .compare_exchange, IN_KERNEL_TRAP
        .compare_exchange(false, true, Ordering::SeqCst, Ordering::SeqCst)
        .is_err, raw_console_putln, raw_console_puts, shutdown | panic!, println! |
| `os/src/arch/loongarch/trap/mod.rs#loongarch_kernel_trap_handler` | `kernel/src/trap/mod.rs#reset_sstatus` | 同模块上下文对齐/差异观察 | IN_KERNEL_TRAP
        .compare_exchange, IN_KERNEL_TRAP
        .compare_exchange(false, true, Ordering::SeqCst, Ordering::SeqCst)
        .is_err, ecode_name, raw_console_putln, raw_console_puts, shutdown | asm! |
| `os/src/arch/loongarch/trap/mod.rs#init` | `kernel/src/trap/mod.rs#set_user_trap_entry` | 同模块上下文对齐/差异观察 | arm_next_timer_interrupt, core::arch::asm!, crate::mm::kernel_symbol_to_phys_addr, usize::from | stvec::write |
| `os/src/arch/riscv/trap/mod.rs#init` | `kernel/src/memory/memory_set.rs#is_trapframe` | 同模块上下文对齐/差异观察 | core::arch::asm!, crate::mm::kernel_symbol_to_virt_addr, sstatus::set_sum, stvec::write, usize::from | self.map_prot.contains |

#### 系统调用
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `os/src/syscall/net.rs#sys_bind` | `kernel/src/syscall/net.rs#sys_bind` | 同板块同名 | crate::net::alg::bind, crate::net::local::unix_socket_bind, crate::net::netlink::bind, current_euid, e.errno, get_any_socket_handle ...(+8) | Ok |
| `os/src/syscall/time/sleep.rs#sys_nanosleep` | `kernel/src/syscall/time.rs#sys_nanosleep` | 同板块同名 | (ts.sec as usize)
        .saturating_mul, (ts.sec as usize)
        .saturating_mul(1_000_000_000)
        .saturating_add, deadline_ns.saturating_sub, get_time_ns, get_time_ns().saturating_add, ns_to_timespec ...(+5) | Err, Ok, copyin, get_current_user_token, get_time_ms, suspend_current ...(+2) |
| `os/src/syscall/net.rs#sys_shutdown` | `user/src/syscall.rs#sys_shutdown` | 同板块同名 | (0..=2).contains, crate::net::local::unix_socket_shutdown, e.errno, get_any_socket_handle, net::shutdown, trace_net_lookup_err ...(+1) | syscall |
| `os/src/syscall/system/uts.rs#sys_uname` | `kernel/src/syscall/other.rs#sys_uname` | 同板块同名 | _compat_hostname_len.saturating_add, buf.is_null, current_uts, uts_field_len, write_user_data | Ok, Utsname::default_init, copyout, get_current_user_token |

#### 文件系统
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `os/src/syscall/fs/path.rs#sys_faccessat` | `kernel/src/syscall/fs.rs#sys_faccessat` | 同板块同名 | build_vfs_path_at_request, crate::fs::vfs_core::api::vfs_accessat, crate::fs::vfs_core::api::vfs_fd_access, crate::fs::vfs_core::api::vfs_fd_access(
                    crate::fs::vfs_core::api::VfsFdAccessRequest {
                        file: &file,
                        mode: mode as u32,
                        subject,
                    },
                )
                .err, crate::fs::vfs_core::api::vfs_fd_access(
                    crate::fs::vfs_core::api::VfsFdAccessRequest {
                        file: &file,
                        mode: mode as u32,
                        subject,
                    },
                )
                .err()
                .unwrap_or, fd_table.get ...(+5) | Ok, get_current_user_token, get_file, open_at, trace!, translate_str |
| `os/src/syscall/fs/mount.rs#sys_mount` | `kernel/src/syscall/fs.rs#sys_mount` | 同板块同名 | String::new, build_vfs_path_at_request, crate::fs::vfs_core::api::vfs_mountat, fstype.is_null, read_user_string_checked, src.is_null ...(+1) | Ok, Path::from_string, fstype.as_str, get_current_user_token, info!, mount ...(+1) |
| `os/src/syscall/fs/meta.rs#sys_newfstatat` | `kernel/src/syscall/fs.rs#sys_newfstatat` | 同板块同名 | buf.is_null, build_vfs_core_kstat, build_vfs_path_at_request, crate::fs::vfs_core::api::vfs_statat, path.is_null, path_str.is_empty ...(+3) | Ok, copyout, file.read_stat, get_current_user_token, get_file, open_at ...(+2) |
| `os/src/syscall/fs/path.rs#sys_linkat` | `kernel/src/syscall/fs.rs#sys_linkat` | 同板块同名 | build_vfs_path_at_request, crate::fs::vfs_core::api::vfs_linkat2, read_user_string_checked | todo! |

#### 设备驱动
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `os/src/drivers/block.rs#read_block` | `kernel/src/driver/block_device/k210_sdcard.rs#read_block` | 同板块同名 | self.inner
            .read_blocks, self.inner
            .read_blocks(block_id, buf)
            .expect | self.inner
            .lock, self.inner
            .lock()
            .read_sector, self.inner
            .lock()
            .read_sector(buf, block_id as u32)
            .unwrap |
| `os/src/drivers/block.rs#write_block` | `kernel/src/driver/block_device/k210_sdcard.rs#write_block` | 同板块同名 | self.inner
            .write_blocks, self.inner
            .write_blocks(block_id, buf)
            .expect | self.inner
            .lock, self.inner
            .lock()
            .write_sector, self.inner
            .lock()
            .write_sector(buf, block_id as u32)
            .unwrap |
| `os/src/net/mod.rs#socket_ioctl` | `kernel/src/driver/block_device/k210_sdcard.rs#read_sector` | 同模块上下文对齐/差异观察 | ETH0_DEVICE.lock, LOOPBACK_DEVICE.lock, bytes.len, dev.addr, dev.broadcast, dev.flags ...(+26) | Err, Ok, assert!, chunk.iter_mut, chunk.iter_mut().zip, data_buf.chunks_mut ...(+6) |
| `os/src/net/mod.rs#ioctl_gifconf` | `kernel/src/driver/block_device/k210_sdcard.rs#write_sector` | 同模块上下文对齐/差异观察 | (needed as i32).to_le_bytes, (written as i32).to_le_bytes, ETH0_DEVICE.lock, LOOPBACK_DEVICE.lock, NetIfDevice::Eth0, NetIfDevice::Loopback ...(+18) | (data_buf.len() / SEC_LEN).try_into, (data_buf.len() / SEC_LEN).try_into().unwrap, Err, Ok, assert!, chunk.iter ...(+9) |

#### 网络协议栈
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `os/src/net/mod.rs#setsockopt` | `kernel/src/net/socket.rs#setsockopt` | 同板块同名 | Err, Ok, alg::setsockopt, core::cmp::min, i32::from_le_bytes, matches! ...(+12) | self.setsockopt |
| `os/src/net/mod.rs#sendto` | `kernel/src/net/socket.rs#sendto` | 同板块同名 | Err, addr.is_some, alg::write, drop, icmp::icmp_sendto, netlink::send ...(+5) | self.sendto |
| `os/src/net/mod.rs#recvfrom` | `kernel/src/net/socket.rs#recvfrom` | 同板块同名 | alg::read, drop, icmp::icmp_recvfrom, netlink::recv, packet::packet_recvfrom, sock.lock ...(+2) | Ok, Vec::new, a.push |
| `os/src/net/mod.rs#connect` | `kernel/src/net/socket.rs#connect` | 同板块同名 | Err, disconnect, drop, encode_socket_addr, icmp::icmp_connect, matches! ...(+3) | self.connect |

#### 文件系统/第三方库
仅目标项目中观察到该模块代表函数，参考项目没有同模块可对齐证据：
- 目标项目：`os/src/fs/ext4/ext4.rs#rename_raw`，调用摘要：Err, Ext4FileSystem::file_type_from_inode, Ok, Some, adjust_disk_inode_nlink, cache_dir_entry ...(+33)
- 目标项目：`os/src/fs/ext4/ext4.rs#add_dir_entry`，调用摘要：(actual as u16).to_le_bytes, (rec_len as u16).to_le_bytes, (self.block_size as u16).to_le_bytes, (slack as u16).to_le_bytes, Err, Ok ...(+32)
- 目标项目：`os/src/fs/ext4/ext4.rs#write_inode_at`，调用摘要：((data.len() - done) / self.block_size).min, (self.block_size - in_block_off).min, Err, Ok, Vec::with_capacity, data.is_empty ...(+23)
- 目标项目：`os/src/fs/ext4/ext4.rs#read_inode_at_with_kind`，调用摘要：(first_phys as usize).checked_mul, (self.block_size - in_block_off).min, inode.size, out.is_empty, out.len, out.len().min ...(+9)

#### 通用模块/其他
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `os/src/arch/riscv/sbi.rs#sbi_call` | `kernel/src/sbi.rs#sbi_call` | 同板块同名 | asm! | asm! |
| `os/src/arch/loongarch/sbi.rs#set_timer` | `kernel/src/sbi.rs#set_timer` | 同板块同名 | _timer.saturating_sub, _timer.saturating_sub(now).max, core::arch::asm!, crate::arch::timer::get_time | sbi_call |
| `os/src/global_logger.rs#test` | `kernel/src/utils/mem_buffer.rs#test` | 同板块同名 | log::debug!, log::error!, log::info!, log::trace!, log::warn!, println! | 内部闭环逻辑 |
| `os/src/arch/loongarch/sbi.rs#shutdown` | `kernel/src/sbi.rs#shutdown` | 同板块同名 | (GED_POWER_REG as *mut u8).write_volatile, core::arch::asm! | panic!, sbi_call |


### 🛡️ 审计任务：最终同源性定性仲裁

#### 1. 宏观判定
- **定性结论**: **底层架构显著差异 / 独立开发**
- **核心依据**: 客观 AST 相似度仅为 0.84%，双向覆盖率极低（目标项目 0.98%，参考项目 5.23%）。全项目仅发现 41 个共同 AST 指纹，且全部分布在内存管理、系统调用分发等通用底层机制上，未在核心业务逻辑（如进程管理、文件系统）中发现任何结构复用。双方在进程管理、任务调度、文件系统等关键模块的共同 AST 指纹均为 0，表明两者采用了完全不同的架构设计、抽象层次和同步策略。

#### 2. 全模块覆盖矩阵解读

| 模块 | 双方存在 | 共同 AST 指纹 | 覆盖率解读 | 证据强度 |
| :--- | :---: | :---: | :--- | :---: |
| **进程管理** | 是 | **0** | 目标 0.0%，参考 0.0%。双方虽实现了同名系统调用，但底层调用图谱无任何交集，架构完全不同。 | **未形成** |
| **任务调度/异步运行时** | 是 | **0** | 目标 0.0%，参考 0.0%。调度器、等待队列、超时管理等核心机制无共同指纹。 | **未形成** |
| **内存管理** | 是 | **20** | 目标 2.58%，参考 10.47%。重合集中在 `frame_alloc`、`sys_mmap` 等通用内存分配与映射接口，属于操作系统底层共性，非项目特有逻辑。 | **弱** |
| **中断/异常处理** | 是 | **0** | 目标 0.0%，参考 0.0%。Trap 处理入口、上下文保存/恢复等关键路径无共同指纹。 | **未形成** |
| **系统调用** | 是 | **10** | 目标 5.21%，参考 16.67%。重合集中在 `sys_bind`、`sys_nanosleep` 等简单系统调用的分发与参数校验模式，属于 POSIX 标准的必然实现。 | **弱** |
| **文件系统** | 是 | **0** | 目标 0.0%，参考 0.0%。目标项目实现了完整的 ext4 支持，参考项目为简单的 in-memory 或 procfs，两者在 VFS 抽象、磁盘布局、路径解析上无任何共同指纹。 | **未形成** |
| **设备驱动** | 是 | **0** | 目标 0.0%，参考 0.0%。双方驱动模型和硬件交互方式完全不同。 | **未形成** |
| **网络协议栈** | 是 | **0** | 目标 0.0%，参考 0.0%。目标项目具备完整的 socket 层、协议族分发，参考项目仅为简单的 socket 骨架，无共同指纹。 | **未形成** |
| **文件系统/第三方库** | 仅目标 | **0** | 目标项目包含完整的 ext4 文件系统实现，参考项目无此模块。 | **未形成** |
| **通用模块/其他** | 是 | **3** | 目标 2.21%，参考 6.0%。重合在 `sbi_call`、`shutdown` 等极简的架构相关底层操作，属于硬件规范决定的必然实现。 | **弱** |

#### 3. 底层 AST 关键证据指纹表
> 表格按模块优先级排序，仅列出存在共同指纹的模块。

| 所属板块 | 目标项目路径 | 参考项目路径 | 证据类型 | 技术说明 |
| :--- | :--- | :--- | :--- | :--- |
| 内存管理 | `os/src/mm/frame_allocator.rs#frame_alloc` | `kernel/src/memory/frame_allocator.rs#frame_alloc` | 同板块同名 | 两者均使用全局 `FRAME_ALLOCATOR.lock().alloc()` 进行物理页框分配，这是操作系统内核中极其通用的伙伴或栈式分配器模式，属于标准实现范式。 |
| 内存管理 | `os/src/syscall/mm.rs#sys_mmap` | `kernel/src/syscall/memory.rs#sys_mmap` | 同板块同名 | 两者均包含 `MapProt`/`MmapFlag` 位解析和 `MapArea` 创建流程，这是实现 POSIX `mmap` 系统调用的标准步骤，调用流虽相似但具体对象（如 `PROCESS_MANAGER` vs 直接操作）完全不同。 |
| 系统调用 | `os/src/syscall/net.rs#sys_bind` | `kernel/src/syscall/net.rs#sys_bind` | 同板块同名 | 目标项目通过 `get_any_socket_handle` 分发到不同协议族，参考项目直接返回 `Ok`。共同指纹仅在于系统调用入口的函数签名和极简的参数校验模式。 |
| 系统调用 | `os/src/syscall/time/sleep.rs#sys_nanosleep` | `kernel/src/syscall/time.rs#sys_nanosleep` | 同板块同名 | 两者均包含时间单位转换和 `get_time_ms`/`get_time_ns` 调用，这是实现 POSIX `nanosleep` 的通用数学逻辑，不构成特异性证据。 |
| 通用模块/其他 | `os/src/arch/riscv/sbi.rs#sbi_call` | `kernel/src/sbi.rs#sbi_call` | 同板块同名 | 两者均使用 `asm!` 宏执行 SBI 生态调用，这是 RISC-V 架构下与 SEE 固件交互的唯一标准方式，由硬件规范强制定义。 |
| 通用模块/其他 | `os/src/arch/loongarch/sbi.rs#shutdown` | `kernel/src/sbi.rs#shutdown` | 同板块同名 | 目标项目通过 ACPI GED 寄存器或 `asm!` 实现关机，参考项目通过 `sbi_call` 实现，共同点仅在于最终调用架构相关的关机指令，属于必然实现。 |

#### 4. 核心代码调用流对比剖析

*   **【内存管理 / 物理页框分配】**
    *   **底层技术析理**: 两者均采用了全局锁保护的分配器模式（`FRAME_ALLOCATOR.lock().alloc()`），这是操作系统内核管理物理内存的经典方法。然而，目标项目的分配器返回 `FrameTracker` 并集成了失败计数统计（`FRAME_ALLOC_FAIL_STREAK`），显示出更复杂的监控与追踪机制；参考项目则直接映射 `Option`。这种差异表明目标项目在通用模式上进行了独立的工程增强。
    *   **代码溯源链接**:
        - 目标项目: `os/src/mm/frame_allocator.rs#frame_alloc`
        - 参考项目: `kernel/src/memory/frame_allocator.rs#frame_alloc`

*   **【系统调用 / 内存映射 (mmap)】**
    *   **底层技术析理**: 两者在 `sys_mmap` 入口都执行了标志位解析、权限检查和区域创建，这是 POSIX 标准的必然要求。但目标项目通过 `PROCESS_MANAGER.lock` 和 `fd_table` 进行统一资源管理，而参考项目直接操作 `memory_set`。目标项目的调用流更复杂，涉及 `align_up` 系列函数和文件描述符检查，显示出独立的内存管理抽象层设计。
    *   **代码溯源链接**:
        - 目标项目: `os/src/syscall/mm.rs#sys_mmap`
        - 参考项目: `kernel/src/syscall/memory.rs#sys_mmap`

*   **【进程管理 / 退出与清理 (exit)】**
    *   **底层技术析理**: 这是控辩双方争论的焦点，但 AST 证据明确显示**共同指纹为 0**。目标项目的 `sys_exit` 调用 `perform_exit`，进而触发 `cleanup_exiting_threads`，其调用流充满了 `vfs_api_*` 系列高级抽象和 `WAIT4_WAITERS` 等复杂的等待队列操作。参考项目的 `sys_exit` 直接调用 `exit_current`，其调用流围绕 `current_task.free_resource`、`INITPROC.adopt_orphan` 等直接的任务结构体操作。两者在资源回收的抽象层次、孤儿进程处理机制和同步模型上完全不同，不存在结构复用。
    *   **代码溯源链接**:
        - 目标项目: `os/src/syscall/process/exit.rs#sys_exit`
        - 参考项目: `kernel/src/syscall/process.rs#sys_exit`

#### 5. 单方模块与降权项

*   **单方模块**:
    *   **文件系统/第三方库 (ext4)**: 目标项目 `os/src/fs/ext4/` 实现了完整的 ext4 文件系统，包含 `rename_raw`、`add_dir_entry`、`write_inode_at` 等复杂逻辑。参考项目完全没有此模块。这是目标项目独立开发的强有力证据。
*   **降权项**:
    *   **极简 POSIX 系统调用**: `sys_getpid`、`sys_getuid`、`sys_getegid`、`sys_membarrier` 等函数，在双方项目中均仅包含 `Ok` 或极简单的字段访问。这些是实现 POSIX 标准所必需的，其调用图谱过于简单，不具备作为抄袭证据的鉴别力，已从主证据中降权。
    *   **跨板块/跨层级配对**: 控方将目标项目的 `sys_execve` 与参考项目的 `init_user_stack` 配对，将目标项目的 `cleanup_exiting_threads` 与参考项目的 `exit_current` 配对。这种跨函数、跨职责的配对方式不符合“同板块底层逻辑一致”的查重原则，已作为弱信号处理。
    *   **泛型与标准库调用**: `Vec::new`、`Some`、`Ok`、`Err`、`iter`、`clone` 等 Rust 标准库的泛型函数调用，在双方项目中大量出现。这些是 Rust 语言的通用模式，不能作为项目间代码复用的特异性指纹。

#### 6. 保守结论

综合全模块 AST 指纹分析，目标项目 `[2026-T202610213999576-XX]` 与参考项目 `[2022-OopS-哈尔滨工业大学深圳]` 的客观相似度为 0.84%，远低于同源性阈值。双方在进程管理、文件系统、网络协议栈等核心业务逻辑模块的共同 AST 指纹均为 0，表明两者在架构设计、抽象层次和同步策略上存在根本性差异。现有 41 个共同指纹均落在内存分配、系统调用分发、硬件交互等操作系统底层通用机制上，属于领域共性或硬件规范决定的必然实现。控方基于函数名和板块标签提出的复用指控，缺乏 AST 结构层面的特异性证据支持。因此，综合判定两个项目为**独立开发**，不存在违规复用行为。建议将审查重点转向 41 个共同指纹所在的通用模块，以确认是否存在未声明的第三方库引用，而非项目间的抄袭。

------------------------------

