# 🛡️ OS 内核赛自动化代码级架构审计报告

**审计时间**: `2026-07-02 13:55:21`
**参赛作品名称**: `2026-T202610617999569-XX`
**年份**: `2026`
**学校**: `XX`
**队伍名称**: `T202610617999569`
**比赛名字**: `OS-Competition`
**比赛子赛道**: `Kernel-Implementation`
**仓库地址**: `https://gitlab.eduxiji.net/educg-group-43501-3132633/T202610617999569-509`
**推测开发语言**: `Rust / C (由AST探针自动识别)`
**特征函数量**: `7345`

## ⚔️ 底层代码同源性对比 (AST Homology)
> 本部分由专门的代码审查智能体，对该项目与符合年份条件的历届最高相似度项目进行控制流与底层调用对比。新版报告会先给出确定性的全模块覆盖矩阵，再给出 Agent 的分模块解释，避免只围绕少量抽样函数展开。

### 🏆 Top 5 底层对标: vs `2024-蕾姆赛高队-扬州大学`（扬州大学 / 蕾姆赛高队）

#### 对方作品信息

**对方年份**: `2024`
**对方学校**: `扬州大学`
**对方队伍名称**: `蕾姆赛高队`
**对方比赛名字**: `OS-Competition`
**对方比赛子赛道**: `Kernel-Implementation`
**对方仓库地址**: `https://gitlab.eduxiji.net/educg-group-26010-2376550/T202411117993110-198`

> 双向覆盖率：共同 AST 指纹: 401；唯一 AST 指纹: 目标项目 5879，参考项目 754；覆盖率: 目标项目 6.82%，参考项目 53.18%

## 全模块对比总览（确定性证据）

> 总体覆盖率：共同 AST 指纹: 401；唯一 AST 指纹: 目标项目 5879，参考项目 754；覆盖率: 目标项目 6.82%，参考项目 53.18%

### 1. 模块覆盖矩阵
| 模块 | 目标函数数 | 参考函数数 | 共同唯一 AST 指纹 | Target 覆盖 | Base 覆盖 | 状态 |
| :--- | ---: | ---: | ---: | ---: | ---: | :--- |
| 进程管理 | 270 | 45 | 1 | 0.41% | 2.33% | 双方存在且有结构重合 |
| 任务调度/异步运行时 | 763 | 43 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |
| 内存管理 | 731 | 125 | 2 | 0.29% | 1.68% | 双方存在且有结构重合 |
| 中断/异常处理 | 27 | 17 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |
| 系统调用 | 2577 | 6 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |
| 文件系统 | 2101 | 68 | 2 | 0.11% | 3.57% | 双方存在且有结构重合 |
| 设备驱动 | 0 | 43 | 0 | 0.0% | 0.0% | 仅参考项目体现 |
| 网络协议栈 | 0 | 0 | 0 | 0.0% | 0.0% | 双方均未体现 |
| 文件系统/第三方库 | 629 | 503 | 392 | 74.38% | 94.69% | 双方存在且有结构重合 |
| 第三方库 | 232 | 0 | 0 | 0.0% | 0.0% | 仅目标项目体现 |
| 通用模块/其他 | 15 | 51 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |

### 2. 分模块证据清单

#### 进程管理
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `kernel/src/syscall/task/execve.rs#sys_execve` | `src/kernel/sysproc.c#sys_execve` | 同板块同名 | FS_CONTEXT.lock, FS_CONTEXT.lock().resolve, FS_CONTEXT.lock().resolve(&path).ok, exec_loc.clone, execve_common, path.ends_with ...(+1) | argaddr, argstr, execve |
| `kernel/src/syscall/task/thread.rs#sys_getppid` | `src/kernel/sysproc.c#sys_getppid` | 同板块同名 | current, current()
        .as_thread, current()
        .as_thread()
        .proc_data
        .proc
        .parent, current()
        .as_thread()
        .proc_data
        .proc
        .parent()
        .ok_or, current()
        .as_thread()
        .proc_data
        .proc
        .parent()
        .ok_or(AxError::NoSuchProcess)
        .map, p.pid | myproc |
| `kernel/src/syscall/task/exit.rs#sys_exit_group` | `src/kernel/sysproc.c#sys_exit_group` | 同板块同名 | Ok, do_exit | argint, exit |
| `kernel/src/syscall/task/thread.rs#sys_getpid` | `src/kernel/sysproc.c#sys_getpid` | 同板块同名 | Ok, current, current().as_thread, current().as_thread().proc_data.visible_pid | myproc |

#### 任务调度/异步运行时
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `kernel/src/syscall/time.rs#sys_times` | `src/kernel/sysothers.c#sys_times` | 同板块同名 | Ok, curr.as_thread, curr.as_thread().proc_data.child_cpu_time.lock, curr.as_thread().time.borrow, curr.as_thread().time.borrow().output, current ...(+6) | argaddr, copyout2, myproc, r_time |
| `kernel/src/syscall/task/schedule.rs#sys_sched_yield` | `src/kernel/sysothers.c#sys_sched_yield` | 同板块同名 | Ok, axtask::yield_now | yield |
| `syscalls_code/nftw/nftw64.c#main` | `src/kernel/sysctl.c#sysctl_clock_device_en` | 同模块上下文对齐/差异观察 | ARRAY_SIZE, anyfail, blenter, blexit, cleanup_function, fprintf ...(+33) | 内部闭环逻辑 |
| `syscalls_code/nftw/nftw.c#main` | `src/driver/spi.c#spi_receive_data_standard` | 同模块上下文对齐/差异观察 | ARRAY_SIZE, anyfail, blenter, blexit, cleanup_function, fprintf ...(+33) | (uint32), spi_get_frame_size, spi_set_tmod |

#### 内存管理
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `kernel/src/syscall/sys.rs#sys_sysinfo` | `src/kernel/syscall.c#sys_sysinfo` | 同板块同名 | Ok, allocator.available_pages, allocator.usages, allocator.used_pages, axalloc::global_allocator, core::mem::zeroed ...(+4) | argaddr, copyout2, freemem_amount, procnum |
| `syscalls_code/move_pages/move_pages_support.c#get_page_size` | `src/mm/vm.c#kvmpa` | 同板块同结构 | sysconf | kwalkaddr |
| `syscalls_code/fallocate/fallocate05.c#cleanup` | `src/mm/dmac.c#dmac_wait_done` | 同板块同结构 | free | dmac_wait_idle |
| `kernel/src/syscall/mod.rs#handle_syscall` | `src/proc/exec.c#exec` | 同模块上下文对齐/差异观察 | AxError::from, Err, LinuxError::ENOSYS.code, LinuxError::from, LinuxError::from(err).code, Sysno::new ...(+277) | ADD_AUXV, EXT4close, EXT4read, MAKE_SATP, PGROUNDDOWN, PGROUNDUP ...(+17) |

#### 中断/异常处理
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `syscalls_code/connect/rewritten/connect01_simple.c#server_loop` | `src/kernel/trap.c#trapframedump` | 同模块上下文对齐/差异观察 | FD_ISSET, FD_SET, FD_ZERO, _exit, accept, close ...(+4) | printf |
| `syscalls_code/sigaltstack/sigaltstack01.c#main` | `src/kernel/plic.c#plicinithart` | 同模块上下文对齐/差异观察 | TEST, TEST_LOOPING, cleanup, kill, setup, sigaction ...(+6) | PLIC_MENABLE, PLIC_SENABLE, PLIC_SPRIORITY, cpuid, printf, readd |
| `syscalls_code/recvmsg/recvmsg01.c#do_child` | `src/mm/dmac.c#dmac_enable_common_interrupt_signal` | 同模块上下文对齐/差异观察 | FD_CLR, FD_ISSET, FD_SET, FD_ZERO, MAX, SAFE_ACCEPT ...(+9) | readq, writeq |
| `syscalls_code/sendmsg/sendmsg01.c#do_child` | `src/mm/dmac.c#dmac_enable_common_interrupt_status` | 同模块上下文对齐/差异观察 | FD_CLR, FD_ISSET, FD_SET, FD_ZERO, MAX, accept ...(+5) | readq, writeq |

#### 系统调用
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `kernel/src/syscall/time.rs#sys_gettimeofday` | `src/kernel/sysothers.c#sys_gettimeofday` | 同板块同名 | Ok, realtime_now, timeval::from_time_value, tv.nullable, tv.vm_write, tz.nullable ...(+1) | argaddr, copyout2, r_time |
| `syscalls_code/string/string01.c#main` | `src/kernel/sysothers.c#sys_clock_gettime` | 同模块上下文对齐/差异观察 | anyfail, blexit, fprintf, memset, setup, sign ...(+11) | argaddr, argint, copyout2, r_time |
| `syscalls_code/ipc/semctl/semctl07.c#verify_semctl` | `src/kernel/syscall.c#argstr` | 同模块上下文对齐/差异观察 | TST_EXP_PASS, getgid, getpid, getuid, semctl, tst_res | argaddr, fetchstr |
| `syscalls_code/ipc/msgctl/msgctl01.c#verify_msgctl` | `src/kernel/sysmem.c#sys_munmap` | 同模块上下文对齐/差异观察 | TEST, memset, msgctl, tst_res | argaddr, do_munmap |

#### 文件系统
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `kernel/src/syscall/fs/ctl.rs#sys_unlinkat` | `src/kernel/sysfile.c#sys_unlinkat` | 同板块同名 | (target.metadata()?.nlink <= 1).then_some, Err, Ok, Path::new, check_remove_target_access, debug! ...(+28) | argint, argstr, fname, funlink, get_inode_type |
| `kernel/src/syscall/fs/ctl.rs#sys_getdents64` | `src/kernel/sysfile.c#sys_getdents64` | 同板块同名 | DirBuffer::new, Directory::from_fd, Err, Ok, buffer.write_entry, debug! ...(+7) | argaddr, argfd, argint, direntnext |
| `kernel/src/syscall/fs/io.rs#sys_write` | `src/kernel/sysfile.c#sys_write` | 同板块同名 | Ok, VmBytes::new, check_memfd_write, debug!, f.as_ref, f.downcast_ref::<File> ...(+5) | argaddr, argfd, argint, filewrite |
| `kernel/src/syscall/fs/io.rs#sys_read` | `src/kernel/sysfile.c#sys_read` | 同板块同名 | Ok, VmBytesMut::new, check_fanotify_access_permission, debug!, f.as_ref, get_file_like ...(+3) | argaddr, argfd, argint, fileread |

#### 设备驱动
仅参考项目中观察到该模块代表函数，目标项目没有同模块可对齐证据：
- 参考项目：`src/driver/spi.c#spi_receive_data_normal_dma`，调用摘要：(uint32), dmac_set_single_mode, dmac_wait_done, spi_get_frame_format, spi_set_tmod, sysctl_dma_select
- 参考项目：`src/driver/sdcard.c#sdcard_write_sector`，调用摘要：acquiresleep, panic, printf, releasesleep, sd_end_cmd, sd_get_response_R1 ...(+4)
- 参考项目：`src/driver/fpioa.c#fpioa_set_function_raw`，调用摘要：内部闭环逻辑
- 参考项目：`src/driver/sdcard.c#check_block_size`，调用摘要：printf, sd_end_cmd, sd_get_response_R1, sd_get_response_R3_rest, sd_send_cmd

#### 文件系统/第三方库
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `third_party/lwext4_rust/c/lwext4/src/ext4_balloc.c#ext4_balloc_alloc_block` | `src/fs/ext4/ext4_balloc.c#ext4_balloc_alloc_block` | 同板块同名同结构 | ext4_balloc_get_bgid_of_block, ext4_balloc_get_block_of_bgid, ext4_balloc_set_bitmap_csum, ext4_balloc_verify_bitmap_csum, ext4_bg_get_block_bitmap, ext4_bg_get_free_blocks_count ...(+19) | ext4_balloc_get_bgid_of_block, ext4_balloc_get_block_of_bgid, ext4_balloc_set_bitmap_csum, ext4_balloc_verify_bitmap_csum, ext4_bg_get_block_bitmap, ext4_bg_get_free_blocks_count ...(+19) |
| `third_party/lwext4_rust/c/lwext4/src/ext4_journal.c#jbd_journal_prepare` | `src/fs/ext4/ext4_journal.c#jbd_journal_prepare` | 同板块同名同结构 | EXT4_BLOCK_ZERO, RB_FIND, TAILQ_FOREACH_REVERSE_SAFE, TAILQ_FOREACH_SAFE, TAILQ_REMOVE, ext4_bcache_clear_dirty ...(+17) | EXT4_BLOCK_ZERO, RB_FIND, TAILQ_FOREACH_REVERSE_SAFE, TAILQ_FOREACH_SAFE, TAILQ_REMOVE, ext4_bcache_clear_dirty ...(+17) |
| `third_party/lwext4_rust/c/lwext4/src/ext4.c#ext4_dir_rm` | `src/fs/ext4/ext4.c#ext4_dir_rm` | 同板块同名同结构 | EXT4_MP_LOCK, EXT4_MP_UNLOCK, ext4_block_cache_write_back, ext4_dir_en_get_inode, ext4_dir_iterator_fini, ext4_dir_iterator_init ...(+19) | EXT4_MP_LOCK, EXT4_MP_UNLOCK, ext4_block_cache_write_back, ext4_dir_en_get_inode, ext4_dir_iterator_fini, ext4_dir_iterator_init ...(+19) |
| `third_party/lwext4_rust/c/lwext4/src/ext4_dir_idx.c#ext4_dir_dx_split_index` | `src/fs/ext4/ext4_dir_idx.c#ext4_dir_dx_split_index` | 同板块同名同结构 | ext4_block_set, ext4_dir_dx_climit_get_count, ext4_dir_dx_climit_get_limit, ext4_dir_dx_climit_set_count, ext4_dir_dx_climit_set_limit, ext4_dir_dx_entry_get_hash ...(+10) | ext4_block_set, ext4_dir_dx_climit_get_count, ext4_dir_dx_climit_get_limit, ext4_dir_dx_climit_set_count, ext4_dir_dx_climit_set_limit, ext4_dir_dx_entry_get_hash ...(+10) |

#### 第三方库
仅目标项目中观察到该模块代表函数，参考项目没有同模块可对齐证据：
- 目标项目：`third_party/syscalls/src/syscall/mips.rs#syscall7`，调用摘要：asm!, ret.wrapping_neg
- 目标项目：`third_party/syscalls/src/syscall/mips.rs#syscall6`，调用摘要：asm!, ret.wrapping_neg
- 目标项目：`third_party/syscalls/src/syscall/mips.rs#syscall5`，调用摘要：asm!, ret.wrapping_neg
- 目标项目：`third_party/syscalls/src/set.rs#next`，调用摘要：MASK.rotate_left, NonZeroUsize::new, Some, Sysno::first, Sysno::first().id, Sysno::from ...(+6)

#### 通用模块/其他
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `src/main.rs#main` | `src/lib/printf.c#print_logo` | 同模块上下文对齐/差异观察 | "STARRY_SHELL_MODE=1".to_owned, CMDLINE
        .iter, CMDLINE
        .iter()
        .copied, CMDLINE
        .iter()
        .copied()
        .map, CMDLINE
        .iter()
        .copied()
        .map(str::to_owned)
        .collect::<Vec<_>>, Vec::new ...(+3) | printf |
| `kernel/src/time.rs#try_into_time_value` | `user/printf.c#vprintf` | 同模块上下文对齐/差异观察 | Err, Ok, TimeValue::new | printint, printptr, putc, va_arg |
| `kernel/src/time.rs#try_into_time_value` | `src/atomic/spinlock.c#release` | 同模块上下文对齐/差异观察 | Err, Ok, TimeValue::new | __sync_lock_release, __sync_synchronize, holding, panic, pop_off |
| `kernel/src/time.rs#try_into_time_value` | `src/lib/printf.c#print_logo` | 同模块上下文对齐/差异观察 | Err, Ok, TimeValue::new | printf |


#### 1. 宏观判定
- **定性结论**: 独立开发 (相似度: 6.43%，共同 AST 指纹: 401；唯一 AST 指纹: 目标项目 5879，参考项目 754；覆盖率: 目标项目 6.82%，参考项目 53.18%)
- **核心依据**: 全项目 AST 相似度极低（6.43%），目标项目覆盖率仅 6.82%。参考项目 53.18% 的高覆盖率主要由双方共用的开源 `lwext4` 第三方库贡献（392/401 共同指纹）。在排除第三方库后，双方原创内核代码的共同 AST 指纹仅 9 个，且均为 POSIX 标准系统调用同名函数签名节点，不涉及函数体控制流或数据依赖结构的复用。双方在内存模型、并发原语、错误处理、进程抽象等核心架构层面存在本质差异。

#### 2. 全模块覆盖矩阵解读

- **进程管理**
  - 双方均存在该模块。
  - 共同 AST 指纹: 1。
  - 目标覆盖率 0.41%，参考覆盖率 2.33%。覆盖率极低，表明双方在该模块的实现结构几乎完全独立。唯一的共同指纹为 POSIX 标准系统调用同名函数签名节点（如 `sys_exit`），不构成实现复用。
  - 证据强度: 未形成。

- **任务调度/异步运行时**
  - 双方均存在该模块。
  - 共同 AST 指纹: 0。
  - 目标覆盖率 0.0%，参考覆盖率 0.0%。双方在该模块无任何共同 AST 结构。目标项目基于 Rust 异步运行时（axtask），参考项目基于经典 xv6 调度器（`sched`, `swtch`），架构完全不同。
  - 证据强度: 未形成。

- **内存管理**
  - 双方均存在该模块。
  - 共同 AST 指纹: 2。
  - 目标覆盖率 0.29%，参考覆盖率 1.68%。覆盖率极低。共同指纹为 `sys_sysinfo` 同名函数签名及 `get_page_size`/`kwalkaddr` 等通用模式，不构成实现复用。目标项目使用 Rust 全局分配器（`axalloc`），参考项目使用 C 手动页表管理（`kwalkaddr`）。
  - 证据强度: 未形成。

- **中断/异常处理**
  - 双方均存在该模块。
  - 共同 AST 指纹: 0。
  - 目标覆盖率 0.0%，参考覆盖率 0.0%。双方在该模块无任何共同 AST 结构。目标项目的中断处理与 Rust 异步运行时集成，参考项目使用经典 PLIC 驱动与 trapframe 操作。
  - 证据强度: 未形成。

- **系统调用**
  - 双方均存在该模块。
  - 共同 AST 指纹: 0。
  - 目标覆盖率 0.0%，参考覆盖率 0.0%。双方在该模块无任何共同 AST 结构。目标项目有 2577 个函数（含大量 LTP 测试用例），参考项目仅 6 个函数，规模与实现完全不同。
  - 证据强度: 未形成。

- **文件系统**
  - 双方均存在该模块。
  - 共同 AST 指纹: 2。
  - 目标覆盖率 0.11%，参考覆盖率 3.57%。覆盖率极低。共同指纹为 `sys_unlinkat`、`sys_getdents64` 等 POSIX 标准系统调用同名函数签名，不构成实现复用。目标项目使用 Rust VFS 抽象（`Directory::from_fd`），参考项目使用 C 手动 inode 操作（`get_inode_type`）。
  - 证据强度: 未形成。

- **设备驱动**
  - 仅参考项目存在该模块（43 个函数），目标项目无此模块。
  - 共同 AST 指纹: 0。
  - 目标覆盖率 0.0%，参考覆盖率 0.0%。双方在该模块无交集。
  - 证据强度: 未形成。

- **网络协议栈**
  - 双方均未体现该模块。
  - 共同 AST 指纹: 0。
  - 证据强度: 未形成。

- **第三方库/公共依赖**
  - 双方均存在该模块（目标项目 629 个函数，参考项目 503 个函数）。
  - 共同 AST 指纹: 392。
  - 目标覆盖率 74.38%，参考覆盖率 94.69%。覆盖率极高，共同指纹占全项目共同指纹的 97.8%。双方均使用了开源 `lwext4`（轻量级 ext4 文件系统实现）库，属于合法第三方代码复用，与抄袭指控无关。
  - 证据强度: 强（但属于合法公共依赖，不作为同源抄袭证据）。

#### 3. 底层 AST 关键证据指纹表

| 所属板块 | 目标项目路径 | 参考项目路径 | 证据类型 | 技术说明 |
| :--- | :--- | :--- | :--- | :--- |
| 第三方库/公共依赖 | `third_party/lwext4_rust/c/lwext4/src/ext4_balloc.c#ext4_balloc_alloc_block` | `src/fs/ext4/ext4_balloc.c#ext4_balloc_alloc_block` | 同板块同名同结构 | 双方均使用开源 lwext4 库，函数体 AST 完全一致，属于合法第三方代码复用。 |
| 第三方库/公共依赖 | `third_party/lwext4_rust/c/lwext4/src/ext4_journal.c#jbd_journal_prepare` | `src/fs/ext4/ext4_journal.c#jbd_journal_prepare` | 同板块同名同结构 | 同上，lwext4 库内部函数。 |
| 第三方库/公共依赖 | `third_party/lwext4_rust/c/lwext4/src/ext4.c#ext4_dir_rm` | `src/fs/ext4/ext4.c#ext4_dir_rm` | 同板块同名同结构 | 同上，lwext4 库内部函数。 |
| 第三方库/公共依赖 | `third_party/lwext4_rust/c/lwext4/src/ext4_dir_idx.c#ext4_dir_dx_split_index` | `src/fs/ext4/ext4_dir_idx.c#ext4_dir_dx_split_index` | 同板块同名同结构 | 同上，lwext4 库内部函数。 |
| 进程管理 | `kernel/src/syscall/task/exit.rs#sys_exit` | `src/kernel/sysproc.c#sys_exit` | 同板块同名 | POSIX 标准系统调用同名函数签名。目标实现为 `Ok(do_exit(...))`，参考实现为 `argint, exit`，函数体 AST 完全不同。 |
| 进程管理 | `kernel/src/syscall/task/thread.rs#sys_getpid` | `src/kernel/sysproc.c#sys_getpid` | 同板块同名 | POSIX 标准系统调用同名函数签名。目标使用 `current().as_thread().proc_data.visible_pid`，参考使用 `myproc`，数据模型完全不同。 |
| 进程管理 | `kernel/src/syscall/task/clone.rs#sys_fork` | `src/kernel/sysproc.c#sys_fork` | 同板块同名 | POSIX 标准系统调用同名函数签名。目标委托 `sys_clone`，参考委托 `fork`，实现机制不同。 |
| 进程管理 | `kernel/src/syscall/task/execve.rs#sys_execve` | `src/kernel/sysproc.c#sys_execve` | 同板块同名 | POSIX 标准系统调用同名函数签名。目标使用 `FS_CONTEXT.lock().resolve`，参考使用 `argstr`，路径解析机制不同。 |
| 内存管理 | `kernel/src/syscall/sys.rs#sys_sysinfo` | `src/kernel/syscall.c#sys_sysinfo` | 同板块同名 | POSIX 标准系统调用同名函数签名。目标使用 `axalloc::global_allocator`，参考使用 `freemem_amount`，内存管理模型不同。 |
| 文件系统 | `kernel/src/syscall/fs/ctl.rs#sys_unlinkat` | `src/kernel/sysfile.c#sys_unlinkat` | 同板块同名 | POSIX 标准系统调用同名函数签名。目标使用 `Path::new` + `check_remove_target_access`，参考使用 `fname` + `funlink`，实现不同。 |
| 文件系统 | `kernel/src/syscall/fs/io.rs#sys_write` | `src/kernel/sysfile.c#sys_write` | 同板块同名 | POSIX 标准系统调用同名函数签名。目标使用 `VmBytes::new` + `check_memfd_write`，参考使用 `filewrite`，实现不同。 |

#### 4. 核心代码调用流对比剖析

* **【进程管理/exit 系统调用路径】**
* **底层技术析理**: 目标项目 `sys_exit` 直接调用 `do_exit`，`do_exit` 内部涉及 Rust 异步运行时（`axtask::yield_now`）、Futex 机制（`FutexKey::new_current`）、信号处理（`SignalInfo::new_kernel`）及子进程时间统计（`child_time.0.saturating_add`）等现代内核特性，外部调用图谱包含 56 个以上对象。参考项目 `sys_exit` 通过 `argint` 提取参数后调用 `exit`，`exit` 内部使用经典 xv6 自旋锁（`acquire`/`release`）、睡眠/唤醒（`sleep`/`wakeup`）及进程表遍历。两者在同步原语、进程抽象、资源回收策略上完全不同，无共同 AST 子树。
* **代码溯源链接**:
  - 目标项目: `kernel/src/syscall/task/exit.rs#sys_exit`
  - 参考项目: `src/kernel/sysproc.c#sys_exit`

* **【进程管理/getpid 系统调用路径】**
* **底层技术析理**: 目标项目 `sys_getpid` 通过 `current()` 获取当前 Task，再通过 `.as_thread().proc_data.visible_pid` 链式访问进程 PID。这反映了目标项目的 Task → Thread → ProcessData 三层进程抽象模型。参考项目 `sys_getpid` 通过 `myproc()` 宏直接获取当前 CPU 的 `struct proc*` 指针，然后访问 `p->pid`。这是扁平的单层进程模型。两者在数据模型、上下文获取机制上完全不同。
* **代码溯源链接**:
  - 目标项目: `kernel/src/syscall/task/thread.rs#sys_getpid`
  - 参考项目: `src/kernel/sysproc.c#sys_getpid`

* **【文件系统/unlinkat 系统调用路径】**
* **底层技术析理**: 目标项目 `sys_unlinkat` 使用 Rust 的 `Path::new` 进行路径解析，通过 `check_remove_target_access` 进行权限检查，并检查 `target.metadata()?.nlink <= 1` 条件。参考项目 `sys_unlinkat` 通过 `argstr` 手动拷贝用户态路径字符串，调用 `fname` 进行路径解析，使用 `get_inode_type` 检查 inode 类型，最后调用 `funlink` 执行删除。两者在路径解析、权限检查、inode 操作等环节的实现机制完全不同。
* **代码溯源链接**:
  - 目标项目: `kernel/src/syscall/fs/ctl.rs#sys_unlinkat`
  - 参考项目: `src/kernel/sysfile.c#sys_unlinkat`

#### 5. 单方模块与降权项

- **设备驱动**: 仅参考项目存在（43 个函数），目标项目无此模块。参考项目的 SPI、SD 卡、FPIOA 等驱动与目标项目无任何关联。
- **第三方库**: 仅目标项目存在（232 个函数），参考项目无此模块。目标项目的 `third_party/syscalls` 等为独立引入的第三方库。
- **LTP 测试代码**: 目标项目 `syscalls_code/` 目录下的大量 C 语言测试用例（如 `vfork01.c`、`kill02.c`）属于 Linux Test Project 标准开源测试套件，与参考项目无关。控方已主动将其降级为弱信号。
- **公共接口/泛型函数名**: 双方共有的 `sys_exit`、`sys_getpid`、`sys_fork`、`sys_execve` 等函数名均为 POSIX/Linux 系统调用 ABI 强制规定的标准名称，任何实现兼容 Linux 的内核都必须使用这些名称。名称相同属于接口一致性要求，不构成代码复用证据。

#### 6. 保守结论

本次审计基于客观 AST 指纹比对，全项目相似度仅 6.43%，目标项目覆盖率仅 6.82%。在排除双方共用的开源 `lwext4` 第三方库（贡献 392/401 共同指纹）后，双方原创内核代码的共同 AST 指纹仅 9 个，且均为 POSIX 标准系统调用同名函数签名节点，不涉及函数体实现复用。目标项目基于 Rust + axtask + ArceOS 架构，参考项目基于 C + xv6 风格，两者在进程抽象（Task/Thread/ProcessData vs 单层 proc）、同步原语（Futex/锁 vs acquire/release/sleep/wakeup）、内存管理（全局分配器 vs 手动页表）、错误处理（Result/Option vs 裸指针）等核心架构层面存在本质差异。综合判断，目标项目为独立开发，不存在对参考项目的代码抄袭或直接逻辑复用。

------------------------------

