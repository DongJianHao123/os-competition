# 🛡️ OS 内核赛自动化代码级架构审计报告

**审计时间**: `2026-07-02 14:34:16`
**参赛作品名称**: `2026-T2026181239910974-XX`
**年份**: `2026`
**学校**: `XX`
**队伍名称**: `T2026181239910974`
**比赛名字**: `OS-Competition`
**比赛子赛道**: `Kernel-Implementation`
**仓库地址**: `https://gitlab.eduxiji.net/educg-group-43501-3132633/T2026181239910974-166`
**推测开发语言**: `Rust / C (由AST探针自动识别)`
**特征函数量**: `2543`

## ⚔️ 底层代码同源性对比 (AST Homology)
> 本部分由专门的代码审查智能体，对该项目与符合年份条件的历届最高相似度项目进行控制流与底层调用对比。新版报告会先给出确定性的全模块覆盖矩阵，再给出 Agent 的分模块解释，避免只围绕少量抽样函数展开。

### 🏆 Top 3 底层对标: vs `2022-S0S-哈尔滨工业大学`（哈尔滨工业大学 / S0S）

#### 对方作品信息

**对方年份**: `2022`
**对方学校**: `哈尔滨工业大学`
**对方队伍名称**: `S0S`
**对方比赛名字**: `OS-Competition`
**对方比赛子赛道**: `Kernel-Implementation`
**对方仓库地址**: `https://gitlab.eduxiji.net/educg-group-14158-894147/oskernel2022-s0s`

> 双向覆盖率：共同 AST 指纹: 73；唯一 AST 指纹: 目标项目 2010，参考项目 447；覆盖率: 目标项目 3.63%，参考项目 16.33%

## 全模块对比总览（确定性证据）

> 总体覆盖率：共同 AST 指纹: 73；唯一 AST 指纹: 目标项目 2010，参考项目 447；覆盖率: 目标项目 3.63%，参考项目 16.33%

### 1. 模块覆盖矩阵
| 模块 | 目标函数数 | 参考函数数 | 共同唯一 AST 指纹 | Target 覆盖 | Base 覆盖 | 状态 |
| :--- | ---: | ---: | ---: | ---: | ---: | :--- |
| 进程管理 | 237 | 51 | 1 | 0.65% | 2.13% | 双方存在且有结构重合 |
| 任务调度/异步运行时 | 546 | 41 | 1 | 0.21% | 2.5% | 双方存在且有结构重合 |
| 内存管理 | 420 | 139 | 35 | 8.77% | 27.56% | 双方存在且有结构重合 |
| 中断/异常处理 | 32 | 5 | 1 | 3.57% | 25.0% | 双方存在且有结构重合 |
| 系统调用 | 244 | 35 | 9 | 4.76% | 37.5% | 双方存在且有结构重合 |
| 文件系统 | 685 | 166 | 5 | 0.98% | 3.36% | 双方存在且有结构重合 |
| 设备驱动 | 18 | 42 | 3 | 17.65% | 7.89% | 双方存在且有结构重合 |
| 网络协议栈 | 5 | 0 | 0 | 0.0% | 0.0% | 仅目标项目体现 |
| 文件系统/第三方库 | 158 | 0 | 0 | 0.0% | 0.0% | 仅目标项目体现 |
| 通用模块/其他 | 198 | 31 | 7 | 4.24% | 24.14% | 双方存在且有结构重合 |

### 2. 分模块证据清单

#### 进程管理
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `os/src/syscall/task.rs#sys_exit` | `os/src/syscall/process.rs#sys_exit` | 同板块同名 | core::hint::spin_loop, current_task, kernel_exit, log::debug!, remove_task, switch_to_next_task ...(+2) | exit_current_and_run_next, panic! |
| `os/src/syscall/task.rs#sys_exit_group` | `os/src/syscall/process.rs#sys_exit_group` | 同板块同名 | core::hint::spin_loop, current_task, kernel_exit_group, log::debug!, remove_task, switch_to_next_task ...(+1) | exit_current_and_run_next, panic! |
| `os/src/syscall/task.rs#sys_getppid` | `os/src/syscall/process.rs#sys_getppid` | 同板块同名 | current_task, p.tgid, p.upgrade, parent
            .as_ref, parent
            .as_ref()
            .and_then, parent
            .as_ref()
            .and_then(|p| p.upgrade())
            .map ...(+2) | current_task, current_task().unwrap |
| `os/src/task/mod.rs#add_initproc` | `os/src/task/mod.rs#add_initproc` | 同板块同名 | Arc::as_ptr, crate::arch::set_kernel_thread_pointer | INITPROC.clone, add_task |

#### 任务调度/异步运行时
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `os/src/syscall/fs.rs#sys_pselect6` | `os/src/syscall/fs.rs#sys_pselect6` | 同板块同名 | FutexWaiter::new, Some, blocking_and_run_next, check_all_real_timers, copy_to_user, core::mem::size_of::<crate::timer::TimeSpec> ...(+40) | FdSet, check_readfds.is_set, check_writefds.is_set, current_task, current_task().unwrap, current_user_token ...(+13) |
| `os/src/signal/delivery.rs#check_pending_signals` | `os/src/task/mod.rs#check_pending_signals` | 同板块同名 | RtSigFrame::build_on_user_stack, available.first_unblocked, core::mem::size_of::<super::sigframe::SigInfo>, handle_default_action, log::debug!, log::error! ...(+21) | SignalFlags::from_bits, SignalFlags::from_bits(1 << sig).unwrap, call_kernel_signal_handler, call_user_signal_handler, current_task, current_task().unwrap ...(+5) |
| `os/src/syscall/fs.rs#sys_getdents64` | `os/src/syscall/fs.rs#sys_getdents64` | 同板块同名 | buf.as_ptr, copy_to_user, core::cmp::min, crate::fs::perf::now, crate::fs::perf::record_sys_getdents64_regular, current_task ...(+19) | UserBuffer::new, buf.read, current_task, current_task().unwrap, current_user_token, inner.fd_table.len ...(+4) |
| `os/src/syscall/fs.rs#sys_chdir` | `os/src/syscall/fs.rs#sys_chdir` | 同板块同名 | Nameidata::new, Path::new, copy_path_from_user, current_task, current_task().set_pwd, dentry.get_inode ...(+18) | current_task, current_task().unwrap, current_user_token, open_file_at, open_file_at(AT_FDCWD, &path, OpenFlags::RDWR).unwrap, remove_from_file_table ...(+2) |

#### 内存管理
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `os/src/trap/mod.rs#trap_handler` | `os/src/trap/mod.rs#trap_handler` | 同板块同名 | alloc::sync::Arc::as_ptr, check_all_real_timers, check_current_real_timer_signal, check_pending_signals, crate::arch::activate_user_memory_access, crate::arch::clear_timer_interrupt ...(+33) | check_signals_error_of_current, current_add_signal, current_trap_cx, exit_current_and_run_next, handle_signals, panic! ...(+9) |
| `os/src/syscall/fs.rs#sys_openat` | `os/src/syscall/fs.rs#sys_openat` | 同板块同名 | Nameidata::new, Pipe::new_fifo, Some, StaticFile::new, alloc::sync::Arc::new, copy_pathname ...(+51) | FileClass::OSInode, OpenFlags::from_bits, OpenFlags::from_bits(flags).unwrap, Some, current_task, current_task().unwrap ...(+6) |
| `os/src/syscall/mm.rs#sys_mmap` | `os/src/syscall/process.rs#sys_mmap` | 同板块同名 | MMAPFLAGS::from_bits_retain, MMAPPROT::from_bits, VPNRange::new, VirtAddr::from, VirtAddr::from(va).floor, VirtPageNum::from ...(+43) | MapFlags::from_bits, MapFlags::from_bits(flags).unwrap, ProtFlags::from_bits, ProtFlags::from_bits(prot).unwrap, UserBuffer::new, Vec::new ...(+20) |
| `os/src/mm/memory_set.rs#new_kernel` | `os/src/mm/memory_set.rs#new_kernel` | 同板块同名 | ((*pair).0 + (*pair).1 + KERNEL_BASE).into, ((*pair).0 + KERNEL_BASE).into, (KERNEL_BASE + MEMORY_END).into, (ebss as usize).into, (edata as usize).into, (ekernel as usize).into ...(+15) | ((*pair).0 + (*pair).1).into, (*pair).0.into, (ebss as usize).into, (edata as usize).into, (ekernel as usize).into, (erodata as usize).into ...(+11) |

#### 中断/异常处理
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `os/src/arch/loongarch64.rs#enable_timer_interrupt` | `os/src/trap/mod.rs#enable_timer_interrupt` | 同板块同名 | asm! | sie::set_stimer |
| `os/src/trap/mod.rs#set_kernel_trap_entry` | `os/src/trap/mod.rs#set_kernel_trap_entry` | 同板块同名 | crate::arch::init_trap_entry | stvec::write |
| `os/src/signal/sigaction.rs#default_action` | `os/src/trap/mod.rs#trap_from_kernel` | 同模块上下文对齐/差异观察 | 内部闭环逻辑 | panic!, println! |
| `os/src/trap/mod.rs#read_user_insn_word` | `os/src/trap/mod.rs#set_user_trap_entry` | 同模块上下文对齐/差异观察 | Some, core::mem::size_of::<u32>, core::ptr::read_unaligned, pa.checked_add | stvec::write |

#### 系统调用
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `user/src/syscall.rs#syscall` | `user/src/syscall.rs#syscall` | 同板块同名 | asm! | asm! |
| `os/src/syscall/socket.rs#sys_shutdown` | `user/src/syscall.rs#sys_shutdown` | 同板块同名 | matches!, socket.is_connected, socket.is_stream_like, socket.mark_read_closed, socket.mark_send_closed, socket_from_fd | syscall |
| `user/src/lib.rs#waitpid` | `user/src/lib.rs#waitpid` | 同板块同名 | sys_waitpid | sys_waitpid |
| `user/src/lib.rs#wait` | `user/src/lib.rs#wait` | 同板块同名 | sys_waitpid | sys_waitpid, yield_ |

#### 文件系统
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `os/src/fs/fdtable.rs#clear` | `fat32/src/dir_entry.rs#clear` | 同板块同名 | file.before_close, file.on_fd_remove, self.cloexec.lock, self.cloexec.lock().clear, self.free_fds.lock, self.free_fds.lock().clear ...(+6) | self.set_first_cluster |
| `os/src/fs/old/os_inode_old.rs#read_all` | `os/src/fs/inode.rs#read_all` | 同板块同名 | Vec::new, inner.inode.clone, inode.read, self.get_offset, self.inner_handler, self.set_offset ...(+1) | Vec::new, inner.inode.read, inner.inode.read().read_at, self.inner_exclusive_access, v.extend_from_slice |
| `os/src/drivers/block/block_cache.rs#get_ref` | `fat32/src/block_cache.rs#get_ref` | 同板块同名 | assert!, core::mem::size_of::<T>, self.addr_of_offset | assert!, core::mem::size_of::<T>, self.addr_of_offset |
| `os/src/fs/old/inode_trait.rs#delete` | `fat32/src/dir_entry.rs#delete` | 同板块同名 | children.remove, parent
                .get_meta, parent
                .get_meta()
                .children_handler, parent.clone, parent.upgrade, parent.upgrade().unwrap ...(+4) | self.set_first_cluster |

#### 设备驱动
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `os/src/drivers/block/mod.rs#block_device_test` | `os/src/drivers/block/mod.rs#block_device_test` | 同板块同名 | BLOCK_DEVICE.clone, assert_eq!, block_device.read_blocks, block_device.write_blocks, println!, write_buffer.iter_mut | BLOCK_DEVICE.clone, assert_eq!, block_device.read_block, block_device.write_block, println!, write_buffer.iter_mut |
| `os/src/drivers/block/block_cache.rs#sync` | `fat32/src/block_cache.rs#sync` | 同板块同名同结构 | self.block_device.write_blocks | self.block_device.write_block |
| `os/src/drivers/block/virtio_blk.rs#read_blocks` | `os/src/drivers/block/virtio_blk.rs#read_block` | 同板块同结构 | self.0
            .lock, self.0
            .lock()
            .read_blocks, self.0
            .lock()
            .read_blocks(block_id, buf)
            .expect | self.0
            .lock, self.0
            .lock()
            .read_block, self.0
            .lock()
            .read_block(block_id, buf)
            .expect |
| `os/src/drivers/block/virtio_blk.rs#write_blocks` | `os/src/drivers/block/virtio_blk.rs#write_block` | 同板块同结构 | self.0
            .lock, self.0
            .lock()
            .write_blocks, self.0
            .lock()
            .write_blocks(block_id, buf)
            .expect | self.0
            .lock, self.0
            .lock()
            .write_block, self.0
            .lock()
            .write_block(block_id, buf)
            .expect |

#### 网络协议栈
仅目标项目中观察到该模块代表函数，参考项目没有同模块可对齐证据：
- 目标项目：`os/src/net/options.rs#default_sol_socket_value`，调用摘要：Some
- 目标项目：`os/src/net/options.rs#accepts_sol_socket_noop`，调用摘要：matches!
- 目标项目：`os/src/net/options.rs#default_tcp_value`，调用摘要：Some
- 目标项目：`os/src/net/options.rs#accepts_ip_noop`，调用摘要：matches!

#### 文件系统/第三方库
仅目标项目中观察到该模块代表函数，参考项目没有同模块可对齐证据：
- 目标项目：`os/src/ext4/inode.rs#read`，调用摘要：(rbuf_len - current_read)
                        .min, (rbuf_len - current_read)
                        .min(PAGE_SIZE - page_offset_in_page)
                        .min, (rbuf_len - current_read)
                .min, (rbuf_len - current_read)
                .min(PAGE_SIZE - page_offset_in_page)
                .min, Ok, Some ...(+22)
- 目标项目：`os/src/ext4/block_op.rs#add_entry`，调用摘要：Err, Ext4DirEntry::default, Ext4DirEntry::try_from, Ext4DirEntry::try_from(
                &self.content[rec_len_total..rec_len_total + rec_len as usize],
            )
            .expect, Ok, assert! ...(+10)
- 目标项目：`os/src/ext4/inode.rs#update_extent`，调用摘要：Err, Ext4Extent::new, Ext4InodeDisk::leaf_max_entries, Ext4InodeDisk::make_extent_idx, Ext4InodeDisk::normalize_extents, Ext4InodeDisk::read_leaf_extents_from_block ...(+26)
- 目标项目：`os/src/ext4/inode.rs#getdents_at`，调用摘要：Dentry::new, Ext4DirEntry::try_from, Some, Vec::new, assert!, buf.len ...(+26)

#### 通用模块/其他
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `os/src/sbi.rs#sbi_call` | `os/src/sbi.rs#sbi_call` | 同板块同名 | asm! | asm! |
| `os/src/sbi.rs#set_timer` | `os/src/sbi.rs#set_timer` | 同板块同名 | asm!, crate::arch::timer_count, timer.saturating_sub, timer.saturating_sub(now).max | sbi_call |
| `os/src/logging.rs#test` | `user/src/bin/user_shell.rs#test` | 同板块同名 | log::debug!, log::error!, log::info!, log::trace!, log::warn!, println! | busybox_test, lua_test, println!, shutdown |
| `os/src/main.rs#clear_bss` | `os/src/main.rs#clear_bss` | 同板块同名 | ptr::write_bytes | core::slice::from_raw_parts_mut, core::slice::from_raw_parts_mut(sbss as usize as *mut u8, ebss as usize - sbss as usize)
            .fill |


#### 1. 宏观判定
- **定性结论**: **独立开发，底层架构显著差异**
- **核心依据**: 客观 AST 相似度仅为 **3.06%**，远低于代码复用或抄袭的判定阈值。目标项目 96.37% 的 AST 指纹为独立所有，且拥有参考项目完全不具备的网络协议栈、ext4 文件系统、futex、cgroup 等复杂子系统。双方在进程管理、内存管理等核心板块的调用图谱、抽象层次与同步模型上存在实质性架构分歧。

#### 2. 全模块覆盖矩阵解读
- **进程管理**: 双方均存在。共同 AST 指纹仅 1 个，目标覆盖率 0.65%，参考覆盖率 2.13%。极低的覆盖率表明双方在进程模型（Linux 兼容 vs. 教学级）上存在根本差异。证据强度：**弱**。
- **任务调度/异步运行时**: 双方均存在。共同 AST 指纹仅 1 个，目标覆盖率 0.21%，参考覆盖率 2.5%。目标项目实现了复杂的 futex/child waiter 调度，参考项目为简单任务管理器。证据强度：**弱**。
- **内存管理**: 双方均存在。共同 AST 指纹 35 个，目标覆盖率 8.77%，参考覆盖率 27.56%。共同指纹多源于 `new_kernel`、`mmap` 等公共惯用模式或 POSIX 标准接口，非私有逻辑复用。证据强度：**中**。
- **中断/异常处理**: 双方均存在。共同 AST 指纹 1 个，目标覆盖率 3.57%，参考覆盖率 25.0%。`trap_handler` 等核心路径调用图谱完全不同，目标项目信号处理路径更复杂。证据强度：**弱**。
- **系统调用**: 双方均存在。共同 AST 指纹 9 个，目标覆盖率 4.76%，参考覆盖率 37.5%。共同点集中在 `syscall` 汇编指令、`waitpid` 等 POSIX 强制同名函数，内部实现差异巨大。证据强度：**弱**。
- **文件系统**: 双方均存在。共同 AST 指纹 5 个，目标覆盖率 0.98%，参考覆盖率 3.36%。目标项目实现了 ext4、procfs 等复杂文件系统，参考项目为简易 FAT32/OSInode。证据强度：**弱**。
- **设备驱动**: 双方均存在。共同 AST 指纹 3 个，目标覆盖率 17.65%，参考覆盖率 7.89%。共同点源于 `virtio_blk` 驱动的公共硬件交互模式，但接口粒度不同（`read_blocks` vs `read_block`）。证据强度：**弱**。
- **网络协议栈**: 仅目标项目存在。无共同指纹，是目标项目独立开发的强有力证据。证据强度：**未形成**。
- **第三方库/公共依赖**: 仅目标项目存在（ext4、第三方库适配层）。无共同指纹，属于独立集成或开发。证据强度：**未形成**。

#### 3. 底层 AST 关键证据指纹表
| 所属板块 | 目标项目路径 | 参考项目路径 | 证据类型 | 技术说明 |
| :--- | :--- | :--- | :--- | :--- |
| 进程管理 | `os/src/syscall/task.rs#sys_exit` | `os/src/syscall/process.rs#sys_exit` | 同板块同名 | 目标调用 `kernel_exit`, `remove_task`；参考调用 `exit_current_and_run_next`。退出逻辑的拆分粒度与调度接口完全不同。 |
| 进程管理 | `os/src/task/task.rs#is_zombie` | `os/src/task/task.rs#is_zombie` | 同板块同名同结构 | 目标为字段访问 `self.status`；参考为方法调用 `self.get_status()`。AST 节点类型不同，属于极简访问器的独立实现。 |
| 进程管理 | `os/src/task/processor.rs#current_task` | `os/src/task/processor.rs#current_task` | 同板块同名 | 目标通过 `self.current.clone()` 获取；参考通过 `PROCESSORS[hartid()].lock().current` 获取。处理器架构与同步模型迥异。 |
| 内存管理 | `os/src/mm/memory_set.rs#new_kernel` | `os/src/mm/memory_set.rs#new_kernel` | 同板块同名 | 共同点在于对内核各段（`ekernel`, `edata` 等）的地址转换，这是链接脚本定义的公共惯用操作。 |
| 内存管理 | `os/src/syscall/mm.rs#sys_mmap` | `os/src/syscall/process.rs#sys_mmap` | 同板块同名 | 目标使用 `MMAPFLAGS`, `VPNRange` 等独立抽象；参考使用 `MapFlags`, `ProtFlags`。内存映射标志位与区域管理模型不同。 |
| 系统调用 | `user/src/syscall.rs#syscall` | `user/src/syscall.rs#syscall` | 同板块同名 | 共同点仅为 `asm!` 宏，这是 RISC-V/LoongArch 架构下触发系统调用的唯一标准方式，不可保护。 |
| 设备驱动 | `os/src/drivers/block/virtio_blk.rs#read_blocks` | `os/src/drivers/block/virtio_blk.rs#read_block` | 同板块同结构 | 共同点源于 VirtIO 协议标准，但目标封装为批量 `read_blocks`，参考为单块 `read_block`，接口设计不同。 |
| 文件系统 | `os/src/fs/old/os_inode_old.rs#read_all` | `os/src/fs/inode.rs#read_all` | 同板块同名 | 目标通过 `inner.inode.clone()` 后读取；参考通过 `inner.inode.read()` 链式调用。内部 inode 的所有权与访问模式不同。 |

#### 4. 核心代码调用流对比剖析
* **【进程管理/进程退出路径】**
* **底层技术析理**: 目标项目将进程退出拆分为 `kernel_exit`（内核态清理）、`remove_task`（数据结构移除）、`switch_to_next_task`（显式调度）三个阶段，体现了更细粒度的资源管理与调度解耦。参考项目则将所有逻辑封装在 `exit_current_and_run_next` 单一函数中，并直接 `panic!`，属于教学级简化的同步退出模型。两者在错误处理、日志记录（`log::debug!` vs `panic!`）和调度接口上存在根本性设计差异。
* **代码溯源链接**:
  - 目标项目: `os/src/syscall/task.rs#sys_exit`
  - 参考项目: `os/src/syscall/process.rs#sys_exit`

* **【内存管理/内存映射】**
* **底层技术析理**: 目标项目 `sys_mmap` 使用自有的 `MMAPFLAGS::from_bits_retain` 和 `VPNRange::new` 等抽象来构建虚拟内存区域，其标志位处理与地址范围计算自成体系。参考项目则使用 `MapFlags::from_bits` 和 `ProtFlags::from_bits`，并直接操作 `UserBuffer` 和 `MemorySet`。两者在内存权限位定义、映射标志解析以及用户态数据拷贝机制上采用了完全不同的抽象层和实现路径。
* **代码溯源链接**:
  - 目标项目: `os/src/syscall/mm.rs#sys_mmap`
  - 参考项目: `os/src/syscall/process.rs#sys_mmap`

* **【中断/异常处理/Trap 入口】**
* **底层技术析理**: 目标项目的 `trap_handler` 调用图谱包含 `check_all_real_timers`、`check_pending_signals`、`crate::arch::activate_user_memory_access` 等复杂调用，显示其实现了完整的实时定时器、信号递送和用户态内存访问控制机制。参考项目的 `trap_handler` 则主要依赖 `handle_signals` 和 `exit_current_and_run_next`，其信号处理和错误恢复路径相对简单。两者在 Trap 处理流程的复杂度和功能完整性上差异显著。
* **代码溯源链接**:
  - 目标项目: `os/src/trap/mod.rs#trap_handler`
  - 参考项目: `os/src/trap/mod.rs#trap_handler`

#### 5. 单方模块与降权项
- **网络协议栈 (`os/src/net/`)**: 仅目标项目具备，包含 `default_sol_socket_value`、`accepts_ip_noop` 等函数。这是目标项目独立于参考项目的重大功能扩展，属于**强原创性证据**。
- **文件系统/第三方库 (`os/src/ext4/`, `os/src/fs/old/`)**: 仅目标项目具备，包含 ext4 文件系统的 `read`、`add_entry`、`update_extent` 等复杂实现。这些模块的 AST 指纹均为目标项目唯一，属于**强原创性证据**。
- **跨板块同名函数**: 如 `fdtable.rs#clear` 与 `fat32/dir_entry.rs#clear`，虽函数名相同，但操作对象分别为文件描述符表和 FAT32 目录项，功能与数据结构完全不同。此类证据已按审计指令降权，**不构成同板块主证据**。
- **公共惯用与强制同名**: `sys_fork`、`sys_getpid`、`syscall` 等函数名由 POSIX/Linux ABI 或硬件架构强制规定，`INITPROC.clone`、`asm!` 等为 Rust 内核开发的公共惯用模式。这些不能作为代码复用的特异性指纹。

#### 6. 保守结论
综合评估，目标项目 `[2026-T2026181239910974-XX]` 与参考项目 `[2022-S0S-哈尔滨工业大学]` 的客观 AST 相似度仅为 3.06%，处于极低水平。双方在进程管理、内存管理等核心板块的共同指纹多源于操作系统领域的公共惯用模式、POSIX 标准强制接口或极简访问器，缺乏私有逻辑或非标准实现上的特异性重合。目标项目在架构上展现出显著的独立演进特征，包括但不限于 Linux 兼容的线程组模型、futex/pidfd 等复杂同步机制、ext4/procfs 等独立文件系统以及完整的网络协议栈，这些均是参考项目所不具备的。现有 AST 证据链清晰指向两个项目为遵循相同领域规范但独立开发的不同作品，不存在足以判定代码抄袭或系统性复用的同源性风险。

------------------------------

