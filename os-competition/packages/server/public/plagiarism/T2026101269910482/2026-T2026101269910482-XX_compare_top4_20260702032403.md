# 🛡️ OS 内核赛自动化代码级架构审计报告

**审计时间**: `2026-07-02 03:24:03`
**参赛作品名称**: `2026-T2026101269910482-XX`
**年份**: `2026`
**学校**: `XX`
**队伍名称**: `T2026101269910482`
**比赛名字**: `OS-Competition`
**比赛子赛道**: `Kernel-Implementation`
**仓库地址**: `https://gitlab.eduxiji.net/educg-group-43501-3132633/T2026101269910482-2169`
**推测开发语言**: `Rust / C (由AST探针自动识别)`
**特征函数量**: `450`

## ⚔️ 底层代码同源性对比 (AST Homology)
> 本部分由专门的代码审查智能体，对该项目与符合年份条件的历届最高相似度项目进行控制流与底层调用对比。新版报告会先给出确定性的全模块覆盖矩阵，再给出 Agent 的分模块解释，避免只围绕少量抽样函数展开。

### 🏆 Top 4 底层对标: vs `2024-练习时长两年半-哈尔滨工业大学`（哈尔滨工业大学 / 练习时长两年半）

#### 对方作品信息

**对方年份**: `2024`
**对方学校**: `哈尔滨工业大学`
**对方队伍名称**: `练习时长两年半`
**对方比赛名字**: `OS-Competition`
**对方比赛子赛道**: `Kernel-Implementation`
**对方仓库地址**: `https://gitlab.eduxiji.net/educg-group-26010-2376550/T202410213992712-3123`

> 双向覆盖率：共同 AST 指纹: 5；唯一 AST 指纹: 目标项目 406，参考项目 969；覆盖率: 目标项目 1.23%，参考项目 0.52%

## 全模块对比总览（确定性证据）

> 总体覆盖率：共同 AST 指纹: 5；唯一 AST 指纹: 目标项目 406，参考项目 969；覆盖率: 目标项目 1.23%，参考项目 0.52%

### 1. 模块覆盖矩阵
| 模块 | 目标函数数 | 参考函数数 | 共同唯一 AST 指纹 | Target 覆盖 | Base 覆盖 | 状态 |
| :--- | ---: | ---: | ---: | ---: | ---: | :--- |
| 进程管理 | 17 | 160 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |
| 任务调度/异步运行时 | 55 | 144 | 2 | 5.13% | 1.46% | 双方存在且有结构重合 |
| 内存管理 | 135 | 228 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |
| 中断/异常处理 | 2 | 20 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |
| 系统调用 | 51 | 112 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |
| 文件系统 | 62 | 293 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |
| 设备驱动 | 22 | 83 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |
| 网络协议栈 | 1 | 17 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |
| 文件系统/第三方库 | 13 | 29 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |
| 通用模块/其他 | 92 | 82 | 3 | 3.37% | 4.11% | 双方存在且有结构重合 |

### 2. 分模块证据清单

#### 进程管理
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `kernel/src/syscall/mod.rs#sys_execve` | `kernel/src/syscall/process.rs#sys_execve` | 同板块同名 | Vec::with_capacity, args.is_empty, args.push, crate::println!, crate::runner::enter_user_elf, ctx.lookup ...(+8) | CString::new, CString::new("LD_LIBRARY_PATH=/:/lib:/lib/glibc:/lib/musl").unwrap, CString::new("PATH=/:/bin").unwrap, CString::new("busybox").unwrap, CString::new("sh").unwrap, CString::new(arg).unwrap ...(+27) |
| `kernel/src/syscall/mod.rs#sys_wait4` | `kernel/src/syscall/process.rs#sys_wait4` | 同板块同名 | crate::task::scheduler::WAIT_STATUS_PTR.lock, scheduler::try_run_child, scheduler::wait_for_child, unreachable!, user_write_u32 | Err, Event::all, Event::all().difference, WaitOptions::from_bits, WaitOptions::from_bits(options).ok_or, WaitPidFuture::new ...(+6) |
| `kernel/src/task/scheduler.rs#current_process` | `kernel/src/processor/mod.rs#current_process` | 同板块同名 | CURRENT.lock, CURRENT.lock().clone | current_thread |
| `kernel/src/syscall/context.rs#install_minimal_procfs` | `kernel/src/process/mod.rs#clone_thread` | 同模块上下文对齐/差异观察 | Arc::new, RamDir::new, RamFile::from_bytes, proc.add, proc_1.add, root.add | Arc::downgrade, MONITORS.lock, MONITORS.lock().thread.add, Ok, Some, Thread::new ...(+26) |

#### 任务调度/异步运行时
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `kernel/src/syscall/mod.rs#syscall` | `kernel/src/syscall/mod.rs#syscall` | 同板块同名 | LAST_SYSCALL.lock, crate::arch::arch_shutdown, crate::println!, crate::runner::RUNNER_JB.is_some, crate::runner::record_la_lua_syscall, crate::task::scheduler::PARENT_TF.lock ...(+58) | Err, SyscallCode::from, async_syscall!, local_hart, strace!, syscall! ...(+1) |
| `kernel/src/syscall/mod.rs#sys_write` | `kernel/src/syscall/fs.rs#sys_write` | 同板块同名 | Vec::new, crate::arch::putchar, current_fd_table, data.as_mut_slice, data.as_slice, data.resize ...(+6) | Err, Ok, current_process, current_process().inner.lock, current_process().inner.lock().fd_table.get, fd_impl.file.metadata ...(+4) |
| `kernel/src/syscall/mod.rs#sys_read` | `kernel/src/syscall/fs.rs#sys_read` | 同板块同名 | current_fd_table, fd_table.get, fh.inode.read_at, fh.lock, user_write_bytes | Err, Ok, current_process, current_process().inner.lock, current_process().inner.lock().fd_table.get, fd_impl.file.metadata ...(+4) |
| `kernel/src/syscall/mod.rs#sys_chdir` | `kernel/src/syscall/fs.rs#sys_chdir` | 同板块同名 | ctx.chdir, ctx.lookup, ctx.lookup(&path).is_some, read_user_str, trace_rv_chdir | Err, Ok, current_process, current_process().inner.lock, current_thread, current_thread().inner ...(+5) |

#### 内存管理
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `kernel/src/syscall/mod.rs#sys_mmap` | `kernel/src/syscall/mm.rs#sys_mmap` | 同板块同名 | (page_addr as *mut u8).write_bytes, (pg.as_usize() as *mut u8).write_bytes, BumpFrameAllocator::new, PAGE_SIZE.min, PAGE_SIZE.min(remaining_file).min, PhysAddr::new ...(+28) | ASPerms::from, Err, MapFlags::from_bits_truncate, MapProt::from_bits_truncate, Some, VirtAddr ...(+17) |
| `kernel/src/memory/heap.rs#alloc` | `kernel/src/net/socket.rs#alloc` | 同板块同名 | align_up_to, alloc_start.saturating_add, core::mem::align_of::<usize>, heap.heap_start.saturating_add, heap.next.saturating_sub, layout.align ...(+6) | Arc::new, Err, FileDescriptor::new, Ok, SocketType::from_bits, SocketType::from_bits(socket_type).ok_or ...(+7) |
| `kernel/src/memory/pagetable.rs#translate` | `kernel/src/mm/page_table.rs#translate` | 同板块同名 | (0..SV39_LEVELS).rev, PhysAddr::new, Some, paddr.as_usize, sv39_vpn, vaddr.as_usize | PhysAddr::from, VirtPageNum::from, panic!, pt.slot_type, vaddr.floor, vaddr.page_offset ...(+3) |
| `kernel/src/memory/pagetable.rs#unmap` | `kernel/src/mm/region/direct.rs#unmap` | 同板块同名 | (0..SV39_LEVELS).rev, PhysAddr::new, paddr.as_usize, sv39_vpn | VirtPageNum, VirtPageNum(offset).step_lv1, next_vpn.index, self.unmap_one |

#### 中断/异常处理
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `kernel/src/trap/mod.rs#recover_loongarch_user_exception` | `kernel/src/trap/user.rs#trap_return` | 同模块上下文对齐/差异观察 | crate::arch::arch_shutdown, crate::println!, crate::syscall::is_runner_active, crate::syscall::recover_user_fault_to_runner | __restore_to_user, current_thread, current_thread().event_bus.reset, current_thread().inner, inner.rusage.trap_in, inner.rusage.trap_out ...(+7) |
| `kernel/src/arch/riscv64.rs#init_trap` | `kernel/src/fs/devfs/tty.rs#default` | 同模块上下文对齐/差异观察 | core::arch::asm!, crate::println! | 内部闭环逻辑 |

#### 系统调用
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `kernel/src/syscall/mod.rs#sys_uname` | `kernel/src/syscall/system.rs#sys_uname` | 同板块同名 | user_write_bytes, user_zero_bytes, value.len, value.len().min, write_field | Ok, UTS_NAME.as_bytes, UTS_NAME.as_bytes().len, user_buf.copy_from_slice, user_slice_w |
| `kernel/src/syscall/mod.rs#sys_nanosleep` | `kernel/src/syscall/time.rs#sys_nanosleep` | 同板块同名 | FAKE_TIME_MS.lock, core::ptr::read_unaligned, now.saturating_add, sec.saturating_mul, sec.saturating_mul(1000).saturating_add | Duration::from, Ok, req.check_forward, sleep_for, user_transmute_r::<TimeSpec>, user_transmute_r::<TimeSpec>(req)?.ok_or |
| `kernel/user/hello.c#_start` | `user/src/lib.rs#_start` | 同板块同名 | 内部闭环逻辑 | CStr::from_ptr, HEAP.lock, HEAP.lock().init, HEAP_SPACE.as_ptr, Vec::new, argv.add ...(+7) |
| `kernel/src/syscall/mod.rs#sys_getrandom` | `kernel/src/syscall/misc.rs#sys_getrandom` | 同板块同名 | (len - done).min, 0x5ausize.wrapping_add, user_write_bytes | KRNG.lock, KRNG.lock().fill, Ok, RandomFlags::from_bits, RandomFlags::from_bits(flags).ok_or, size_of::<u8> ...(+1) |

#### 文件系统
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `kernel/src/syscall/mod.rs#sys_statfs` | `kernel/src/syscall/fs.rs#sys_statfs` | 同板块同名 | user_write_u64 | KernelStatfs::default, Ok, current_thread, current_thread().inner, fs.flags, fs.flags().bits ...(+9) |
| `kernel/src/fs/vfs.rs#seek` | `kernel/src/fs/file.rs#seek` | 同板块同名 | ((self.offset as i64) + d).max, ((size as i64) + d).max, self.inode.stat | Err, Ok, pos.checked_add, self.metadata.inode.as_ref, self.metadata.inode.as_ref().unwrap, self.metadata.inode.as_ref().unwrap().metadata ...(+3) |
| `kernel/src/fs/vfs.rs#get` | `kernel/src/fs/inode_cache.rs#get` | 同板块同名 | s.clone, self.fds.lock, self.fds.lock().get, self.fds.lock().get(fd).and_then | Cow::Borrowed, HashKey::new, cache.get, cache.get(&hash_key).and_then, inode.upgrade, p.metadata ...(+3) |
| `kernel/src/fs/ramfs.rs#read_dir` | `kernel/src/fs/fat32/mod.rs#read_dir` | 同板块同名 | d.name.clone, self.items.lock, self.items.lock().iter, self.items.lock().iter().map, self.items.lock().iter().map(|d| d.name.clone()).collect | (0..BLOCK_SIZE).step_by, FAT32ChildExt::new, FAT32Dirent::default, FAT32Dirent::is_empty, FAT32Dirent::is_end, FAT32Dirent::is_long_dirent ...(+13) |

#### 设备驱动
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `kernel/src/driver/virtio_mmio.rs#init_pci` | `kernel/src/driver/serial/dw_apb_uart.rs#init` | 同模块上下文对齐/差异观察 | Some, VirtIoBlk::init_at, VirtIoBlk::init_pci_legacy_io, crate::println!, pci_io_addr, pci_r16 ...(+3) | self.regs, self.regs().dlf.set, self.regs().fcr.set, self.regs().ier.set, self.regs().lcr.get, self.regs().lcr.set ...(+3) |
| `kernel/src/driver/virtio_mmio.rs#read_sector_mmio` | `kernel/src/driver/mod.rs#parse_reg` | 同模块上下文对齐/差异观察 | crate::println!, self.read_sector_mmio_once, self.reset_mmio_queue | (0..reg.len()).step_by, Ok, Vec::new, bytemuck::cast_slice, node
        .props, node
        .props()
        .find ...(+10) |
| `kernel/src/driver/virtio_mmio.rs#read_sector_pci_legacy_io` | `kernel/src/driver/mmc/regs.rs#data_cmd` | 同模块上下文对齐/差异观察 | crate::println!, self.read_sector_pci_legacy_io_once, self.reset_pci_legacy_queue | CMD::new, CMD::new()
            .with_start_cmd, CMD::new()
            .with_start_cmd(true)
            .with_use_hold_reg, CMD::new()
            .with_start_cmd(true)
            .with_use_hold_reg(true) // TODO: check different mode of correct value
            .with_data_expected, CMD::new()
            .with_start_cmd(true)
            .with_use_hold_reg(true) // TODO: check different mode of correct value
            .with_data_expected(true)
            .with_response_expected, CMD::new()
            .with_start_cmd(true)
            .with_use_hold_reg(true) // TODO: check different mode of correct value
            .with_data_expected(true)
            .with_response_expected(true)
            .with_wait_prvdata_complete ...(+3) |
| `kernel/src/driver/virtio_mmio.rs#reset_pci_legacy_queue` | `kernel/src/driver/virtio/net.rs#receive` | 同模块上下文对齐/差异观察 | core::mem::size_of::<VirtIoPciDma>, core::ptr::write_bytes, dma_fence, io_w16, io_w32, io_w8 | Some, VirtioRxToken, VirtioTxToken, panic!, self.dev.clone, self.dev.lock ...(+1) |

#### 网络协议栈
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `kernel/src/runner.rs#print_compat_netperf` | `kernel/src/net/tcp.rs#drop` | 同模块上下文对齐/差异观察 | put_str | NET_INTERFACE.lock, info!, net.poll, net.sockets.get_mut::<tcp::Socket>, net.sockets.remove, self.inner.lock ...(+2) |

#### 文件系统/第三方库
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `kernel/src/fs/ext4.rs#extent_lookup` | `kernel/src/fs/ext4/inode.rs#load_children` | 同模块上下文对齐/差异观察 | Some, alloc_buf, cbuf.as_ptr, cbuf.as_ptr().add, cp.wrapping_add, inode.block.as_ptr ...(+3) | Arc::new, Duration::from_secs, Duration::from_secs(inode_ref.access_time as u64).into, Duration::from_secs(inode_ref.change_inode_time as u64).into, Duration::from_secs(inode_ref.modification_time as u64).into, Ext4File::open_dir ...(+33) |
| `kernel/src/main.rs#mount_ext4` | `kernel/src/fs/ext4/inode.rs#do_create` | 同模块上下文对齐/差异观察 | Ext4Fs::open, Some, VirtIoBlk::init, VirtIoBlk::init_pci, crate::println!, entries.is_empty ...(+2) | Arc::new, Duration::from_secs, Duration::from_secs(inode_ref.access_time as u64).into, Duration::from_secs(inode_ref.change_inode_time as u64).into, Duration::from_secs(inode_ref.modification_time as u64).into, Ext4File::open_dir ...(+29) |
| `kernel/src/fs/ext4.rs#read_dir` | `kernel/src/fs/ext4/inode.rs#do_symlink` | 同模块上下文对齐/差异观察 | String::from, Vec::new, alloc_buf, buf.as_ptr, buf.as_ptr().add, core::slice::from_raw_parts ...(+5) | Arc::new, Duration::from_secs, Duration::from_secs(inode_ref.access_time as u64).into, Duration::from_secs(inode_ref.change_inode_time as u64).into, Duration::from_secs(inode_ref.modification_time as u64).into, Ext4InodeInner::new ...(+21) |
| `kernel/src/fs/ext4.rs#read_file` | `kernel/src/fs/ext4/inode.rs#do_movein` | 同模块上下文对齐/差异观察 | (offset..offset + rlen).step_by, (rlen - done).min, alloc_buf, bbuf.len, buf.len, buf.len().min ...(+4) | Arc::new, InodeMeta::movein, Ok, format!, fs.driver_lock.lock, inode.as_ref ...(+18) |

#### 通用模块/其他
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `kernel/src/sync.rs#try_lock` | `kernel/src/sync/mutex/reentrant.rs#try_lock` | 同板块同名 | Some, self.locked.swap | S::new_guard, Some, local_hart, self
            .owner
            .compare_exchange, self
            .owner
            .compare_exchange(NOBODY, local_hart().id, Ordering::Acquire, Ordering::Relaxed)
            .is_ok, self.lock.get ...(+2) |
| `kernel/src/sync.rs#lock` | `kernel/src/sync/mutex/reentrant.rs#lock` | 同板块同名 | core::hint::spin_loop, self.locked.swap | Duration::from_secs, core::hint::spin_loop, cpu_time, local_hart, panic!, self.locked_by ...(+1) |
| `kernel/src/sync.rs#deref_mut` | `kernel/src/sync/mutex/reentrant.rs#deref_mut` | 同板块同名同结构 | self.mutex.data.get | self.mutex.data.get |
| `kernel/src/sync.rs#deref` | `kernel/src/sync/mutex/reentrant.rs#deref` | 同板块同名同结构 | self.mutex.data.get | self.mutex.data.get |


好的，审判长。本庭已收到控辩双方的全部陈述、底层AST证据及模块覆盖矩阵。现在，我将依据客观的AST调用图谱数据，对本案进行最终的独立审计与定性仲裁。

---

### 1. 宏观判定

- **定性结论**: **独立开发**
- **核心依据**:
  - **极低的结构相似度**: 客观AST相似度仅为 **0.36%**，处于统计噪声水平。
  - **微弱的指纹重合**: 在双方总计 **1375** 个唯一AST指纹中，仅有 **5** 个共同指纹。
  - **可忽略的覆盖率**: 目标项目覆盖率 **1.23%**，参考项目覆盖率 **0.52%**，表明双方代码库在抽象语法树层面几乎完全正交。
  - **关键模块零重合**: 控方指控最激烈的“进程管理”、“中断/异常处理”、“文件系统”等核心模块，共同AST指纹均为 **0**，从根本上否定了“直接搬运”或“重构洗稿”的指控。

### 2. 全模块覆盖矩阵解读

| 模块 | 双方均存在 | 共同AST指纹 | 目标覆盖率 | 参考覆盖率 | 证据强度 | 解读 |
| :--- | :---: | :---: | :---: | :---: | :---: | :--- |
| **进程管理** | 是 | **0** | 0.0% | 0.0% | **未形成** | 控方“铁证”所在模块，但AST证据为零。同名函数（如`sys_execve`）的底层调用图谱完全不同，表明双方采用了截然不同的进程模型与实现路径。 |
| **任务调度/异步运行时** | 是 | **2** | 5.13% | 1.46% | **弱** | 共同指纹源于`syscall`分发函数中的通用Rust模式（如`println!`）。双方在调度模型（同步Runner vs. 异步Future）、文件描述符抽象（`fh.inode` vs. `fd_impl.file.metadata`）上存在根本性架构差异。 |
| **内存管理** | 是 | **0** | 0.0% | 0.0% | **未形成** | 同名函数（如`sys_mmap`, `translate`）的调用图谱无任何重合。目标项目使用`BumpFrameAllocator`，参考项目使用`ASPerms::from`等，物理/虚拟内存管理实现完全异构。 |
| **中断/异常处理** | 是 | **0** | 0.0% | 0.0% | **未形成** | 控方指控的`trap/context.rs`复用不成立。目标项目为LoongArch Runner的同步异常恢复，参考项目为通用RISC-V的异步线程返回，调用链无交集。 |
| **系统调用** | 是 | **0** | 0.0% | 0.0% | **未形成** | 同名POSIX调用（如`sys_uname`）由接口规范强制决定。底层实现中，目标项目使用`user_write_bytes`，参考项目使用`user_buf.copy_from_slice`，无结构复用。 |
| **文件系统** | 是 | **0** | 0.0% | 0.0% | **未形成** | 控方“POSIX全家桶洗稿”指控的核心模块，但AST重合率为零。双方在VFS、Ext4、RamFS等实现上使用了完全不同的内部对象和方法调用链。 |
| **设备驱动** | 是 | **0** | 0.0% | 0.0% | **未形成** | 控方证据均为跨板块硬凑（如VirtIO块设备 vs. 串口/MMC驱动），不构成同板块证据。 |
| **网络协议栈** | 是 | **0** | 0.0% | 0.0% | **未形成** | 目标项目仅有1个网络相关函数（测试输出），参考项目有完整TCP栈，双方在该领域无任何可比性。 |
| **文件系统/第三方库** | 是 | **0** | 0.0% | 0.0% | **未形成** | 双方虽均有Ext4相关实现，但调用图谱无重合，属于对同一公开标准的独立实现。 |
| **通用模块/其他** | 是 | **3** | 3.37% | 4.11% | **弱** | 共同指纹为`self.mutex.data.get`、`core::hint::spin_loop`等Rust语言级惯用法，是自旋锁等基础同步原语的通用实现模式，不构成项目级复用证据。 |

### 3. 底层 AST 关键证据指纹表

| 所属板块 | 目标项目路径 | 参考项目路径 | 证据类型 | 技术说明 |
| :--- | :--- | :--- | :--- | :--- |
| 通用模块/其他 | `kernel/src/sync.rs#deref_mut` | `kernel/src/sync/mutex/reentrant.rs#deref_mut` | 同板块同名同结构 | 双方均使用 `self.mutex.data.get` 实现 `DerefMut`。这是Rust中基于 `UnsafeCell` 实现互斥锁的标准、通用模式，属于语言惯用法。 |
| 通用模块/其他 | `kernel/src/sync.rs#deref` | `kernel/src/sync/mutex/reentrant.rs#deref` | 同板块同名同结构 | 同上，`Deref` trait的标准实现，`self.mutex.data.get` 是访问内部数据的必然选择。 |
| 通用模块/其他 | `kernel/src/sync.rs#lock` | `kernel/src/sync/mutex/reentrant.rs#lock` | 同板块同名 | 双方均使用 `core::hint::spin_loop` 实现自旋等待。这是Rust标准库提供的自旋锁核心原语，任何自旋锁实现都会使用。 |
| 任务调度/异步运行时 | `kernel/src/syscall/mod.rs#syscall` | `kernel/src/syscall/mod.rs#syscall` | 同板块同名 | 双方均使用了 `println!` 等调试宏。这是Rust语言的标准输出机制，不构成逻辑复用。 |
| 任务调度/异步运行时 | `kernel/src/syscall/mod.rs#syscall` | `kernel/src/syscall/mod.rs#syscall` | 同板块同名 | 双方均使用了 `Option` 枚举的 `is_some` 方法。这是Rust中最基础的枚举操作，无独创性。 |

### 4. 核心代码调用流对比剖析

*   **【进程管理 / `fork` 语义实现路径】**
    *   **底层技术析理**: 控方指控目标项目“直接搬运”了参考项目的 `fork_process`。然而，AST图谱揭示了完全不同的实现路径。目标项目的 `sys_fork` 调用链为 `scheduler::current_process` → `scheduler::fork_process`，其内部依赖 `PROCESSES.lock` 等全局锁。参考项目的 `fork_process` 则是一个包含 `Arc::downgrade`, `BTreeMap::new`, `MONITORS.lock` 等数十个调用的复杂函数。目标项目的调用图谱中**完全没有**参考项目 `fork_process` 的任何特征调用，表明其 `scheduler::fork_process` 是一个独立实现的、更轻量级的封装，而非搬运。
    *   **代码溯源链接**:
        -   目标项目: `kernel/src/syscall/mod.rs#sys_fork`
        -   参考项目: `kernel/src/process/mod.rs#fork_process`

*   **【任务调度/异步运行时 / 文件系统调用实现路径】**
    *   **底层技术析理**: 控方指控目标项目对POSIX调用进行了“API重命名洗稿”。以 `sys_write` 为例，AST图谱揭示了双方在文件描述符抽象上的根本差异。目标项目通过 `current_fd_table` 获取 `fd_table`，然后操作 `fh.inode.stat` 和 `fh.lock`。参考项目则通过 `current_process().inner.lock().fd_table.get` 获取 `fd_impl`，然后操作 `fd_impl.file.metadata().flags.lock().writable`。这是两套完全不同的对象模型和访问路径，不是简单的“重命名”可以解释的，而是独立设计的VFS和文件描述符系统。
    *   **代码溯源链接**:
        -   目标项目: `kernel/src/syscall/mod.rs#sys_write`
        -   参考项目: `kernel/src/syscall/fs.rs#sys_write`

*   **【中断/异常处理 / Trap上下文操作】**
    *   **底层技术析理**: 控方指控双方在 `trap/context.rs` 中的“内部闭环逻辑”函数存在复用。这是对简单 getter/setter 函数的误读。`syscall_return`、`get_sp` 等函数通常仅包含对结构体字段的读写操作，在AST上自然表现为无外部调用的“闭环”。双方在Trap处理的核心路径上完全不同：目标项目是 `recover_loongarch_user_exception` → `arch_shutdown` / `recover_user_fault_to_runner`，服务于测试Runner；参考项目是 `trap_return` → `__restore_to_user` / `event_bus.reset`，服务于通用异步调度。两者毫无结构重合。
    *   **代码溯源链接**:
        -   目标项目: `kernel/src/trap/mod.rs#recover_loongarch_user_exception`
        -   参考项目: `kernel/src/trap/user.rs#trap_return`

### 5. 单方模块与降权项

*   **单方特有模块/功能**:
    *   **目标项目**: 拥有大量与特定测试框架/比赛相关的代码，如 `runner.rs` 中的 `lua_exec_strategy`, `busybox_exec_strategy`, `case_exec_strategy` 以及 `option_env!("LA_REAL_LIBCBENCH")` 等编译时特性开关。这些是目标项目独有的业务逻辑。
    *   **参考项目**: 拥有完整的网络协议栈（`kernel/src/net/tcp.rs`）、Capability安全模型（`sys_capset`, `sys_capget`）、资源限制（`sys_prlimit`, `sys_getrusage`）等通用操作系统特性，这些在目标项目中完全缺失。
*   **降权项说明**:
    *   **跨板块硬凑**: 控方将目标项目的 `virtio_mmio::init_pci`（块设备驱动）与参考项目的 `serial/dw_apb_uart::init`（串口驱动）进行对比，属于跨设备类型的强行配对，不构成同板块证据，予以排除。
    *   **领域强制同名**: 所有 `sys_write`, `sys_fork`, `sys_execve` 等POSIX系统调用名称，以及 `new`, `default`, `read`, `write` 等通用方法名，均由操作系统接口规范或Rust语言惯例决定，不能作为抄袭证据。
    *   **通用Rust惯用法**: `println!`, `Some`, `Option`, `Vec::new`, `core::hint::spin_loop`, `self.mutex.data.get` 等是Rust语言和标准库提供的通用机制，任何Rust项目都可能使用，不构成可保护的独创性表达。

### 6. 保守结论

本庭基于客观AST调用图谱证据作出最终裁决。双方项目在抽象语法树层面的相似度仅为0.36%，共同指纹仅5个，且均为Rust语言通用惯用法或调试宏。控方提出的“直接搬运”与“重构洗稿”等指控，在“进程管理”、“文件系统”、“中断/异常处理”等核心模块的共同AST指纹均为零，缺乏底层结构证据支持。双方在进程模型（同步全局锁 vs. 异步Future）、文件描述符抽象（`fh.inode` vs. `fd_impl.file.metadata`）、异常处理路径（测试Runner回跳 vs. 通用线程返回）等关键架构设计上存在根本性、系统性的差异。这些差异无法通过简单的“宏替换”或“API重命名”来实现，清晰地指向了两个独立的设计与开发过程。因此，本庭认定，目标项目 `[2026-T2026101269910482-XX]` 相对于参考项目 `[2024-练习时长两年半-哈尔滨工业大学]` 构成**独立开发**，驳回控方所有指控。

------------------------------

