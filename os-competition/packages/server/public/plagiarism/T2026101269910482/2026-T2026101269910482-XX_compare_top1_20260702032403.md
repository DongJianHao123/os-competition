# 🛡️ OS 内核赛自动化代码级架构审计报告

**审计时间**: `2026-07-02 03:24:03`
**参赛作品名称**: `2026-T2026101269910482-XX`
**年份**: `2026`
**学校**: `XX`
**队伍名称**: `T2026101269910482`
**比赛名字**: `OS-Competition`
**比赛子赛道**: `Kernel-Implementation`
**仓库地址**: `https://gitlab.eduxiji.net/educg-group-43501-3132633/T2026101269910482-2169`
**推测开发语言**: `Rust / C (由AST探针自动识别)`
**特征函数量**: `450`

## ⚔️ 底层代码同源性对比 (AST Homology)
> 本部分由专门的代码审查智能体，对该项目与符合年份条件的历届最高相似度项目进行控制流与底层调用对比。新版报告会先给出确定性的全模块覆盖矩阵，再给出 Agent 的分模块解释，避免只围绕少量抽样函数展开。

### 🏆 Top 1 底层对标: vs `2024-Phoenix-哈尔滨工业大学深圳`（哈尔滨工业大学（深圳） / Phoenix）

#### 对方作品信息

**对方年份**: `2024`
**对方学校**: `哈尔滨工业大学（深圳）`
**对方队伍名称**: `Phoenix`
**对方比赛名字**: `OS-Competition`
**对方比赛子赛道**: `Kernel-Implementation`
**对方仓库地址**: `local-upload://T202418123993075-2940`

> 双向覆盖率：共同 AST 指纹: 13；唯一 AST 指纹: 目标项目 406，参考项目 1389；覆盖率: 目标项目 3.2%，参考项目 0.94%

## 全模块对比总览（确定性证据）

> 总体覆盖率：共同 AST 指纹: 13；唯一 AST 指纹: 目标项目 406，参考项目 1389；覆盖率: 目标项目 3.2%，参考项目 0.94%

### 1. 模块覆盖矩阵
| 模块 | 目标函数数 | 参考函数数 | 共同唯一 AST 指纹 | Target 覆盖 | Base 覆盖 | 状态 |
| :--- | ---: | ---: | ---: | ---: | ---: | :--- |
| 进程管理 | 17 | 138 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |
| 任务调度/异步运行时 | 55 | 190 | 2 | 5.13% | 1.08% | 双方存在且有结构重合 |
| 内存管理 | 135 | 369 | 8 | 6.25% | 2.33% | 双方存在且有结构重合 |
| 中断/异常处理 | 2 | 33 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |
| 系统调用 | 51 | 37 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |
| 文件系统 | 62 | 443 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |
| 设备驱动 | 22 | 179 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |
| 网络协议栈 | 1 | 98 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |
| 文件系统/第三方库 | 13 | 70 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |
| 第三方库 | 0 | 52 | 0 | 0.0% | 0.0% | 仅参考项目体现 |
| 通用模块/其他 | 92 | 115 | 2 | 2.25% | 1.85% | 双方存在且有结构重合 |

### 2. 分模块证据清单

#### 进程管理
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `kernel/src/syscall/mod.rs#sys_execve` | `kernel/src/syscall/process.rs#sys_execve` | 同板块同名 | Vec::with_capacity, args.is_empty, args.push, crate::println!, crate::runner::enter_user_elf, ctx.lookup ...(+8) | "/busybox".to_string, "busybox".to_string, "sh".to_string, Ok, Vec::new, argv.insert ...(+15) |
| `kernel/src/syscall/mod.rs#sys_wait4` | `kernel/src/syscall/process.rs#sys_wait4` | 同板块同名 | crate::task::scheduler::WAIT_STATUS_PTR.lock, scheduler::try_run_child, scheduler::wait_for_child, unreachable!, user_write_u32 | Err, Ok, PROCESS_GROUP_MANAGER.remove, Some, TASK_MANAGER.remove, WaitFor::PGid ...(+43) |
| `kernel/src/syscall/context.rs#install_minimal_procfs` | `kernel/src/task/task.rs#do_exit` | 同模块上下文对齐/差异观察 | Arc::new, RamDir::new, RamFile::from_bytes, proc.add, proc_1.add, root.add | Arc::downgrade, SHARED_MEMORY_MANAGER.detach, Some, TASK_MANAGER.init_proc, TASK_MANAGER.remove, UserWritePtr::from ...(+42) |
| `kernel/src/syscall/context.rs#parent_dir_and_leaf` | `kernel/src/syscall/process.rs#sys_clone` | 同模块上下文对齐/差异观察 | Some, Vec::new, dir.lookup, node.file_type, normalize_path, normalized.split ...(+7) | CloneFlags::from_bits, CloneFlags::from_bits(flags as u64 & !0xff).ok_or_else, Ok, Some, UserWritePtr::from, UserWritePtr::from(child_tid.bits()).write ...(+17) |

#### 任务调度/异步运行时
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `kernel/src/syscall/mod.rs#sys_getdents64` | `kernel/src/syscall/fs.rs#sys_getdents64` | 同板块同名 | align_up_usize, current_fd_table, entries.iter, entries.iter().enumerate, fd_table.get, fh
                    .inode
                    .lookup ...(+12) | UserWritePtr::<u8>::from, UserWritePtr::<u8>::from(buf).into_mut_slice, file.read_dir, table.get_file, task.with_fd_table |
| `kernel/src/syscall/mod.rs#sys_write` | `kernel/src/syscall/fs.rs#sys_write` | 同板块同名 | Vec::new, crate::arch::putchar, current_fd_table, data.as_mut_slice, data.as_slice, data.resize ...(+6) | Err, Select2Futures::new, buf.into_slice, file.write, log::info!, table.get_file ...(+6) |
| `kernel/src/syscall/mod.rs#sys_read` | `kernel/src/syscall/fs.rs#sys_read` | 同板块同名 | current_fd_table, fd_table.get, fh.inode.read_at, fh.lock, user_write_bytes | Err, Select2Futures::new, buf.into_mut_slice, file.read, log::info!, table.get_file ...(+6) |
| `kernel/src/syscall/mod.rs#sys_chdir` | `kernel/src/syscall/fs.rs#sys_chdir` | 同板块同名 | ctx.chdir, ctx.lookup, ctx.lookup(&path).is_some, read_user_str, trace_rv_chdir | Err, Ok, dentry.inode, dentry.inode()?.itype, dentry.inode()?.itype().is_dir, log::debug! ...(+3) |

#### 内存管理
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `kernel/src/syscall/mod.rs#sys_mmap` | `kernel/src/syscall/mm.rs#sys_mmap` | 同板块同名 | (page_addr as *mut u8).write_bytes, (pg.as_usize() as *mut u8).write_bytes, BumpFrameAllocator::new, PAGE_SIZE.min, PAGE_SIZE.min(remaining_file).min, PhysAddr::new ...(+28) | (addr + length).round_up, Err, MapPerm::from, MmapFlags::from_bits_truncate, MmapProt::from_bits_truncate, Ok ...(+16) |
| `kernel/src/syscall/mod.rs#sys_openat` | `kernel/src/syscall/fs.rs#sys_openat` | 同板块同名 | FileHandle::new, ctx.create_file, ctx.lookup, current_fd_table, fd_table.alloc, inode.truncate ...(+1) | Err, InodeMode::from_bits_truncate, OpenFlags::from_bits, OpenFlags::from_bits(flags).ok_or, dentry.inode, dentry.is_negetive ...(+14) |
| `kernel/src/syscall/mod.rs#sys_pipe2` | `kernel/src/syscall/fs.rs#sys_pipe2` | 同板块同名 | Arc::new, FileHandle::new, RamFile::from_bytes, crate::println!, current_fd_table, fd_table.alloc ...(+2) | Ok, OpenFlags::from_bits, OpenFlags::from_bits(flags)
            .unwrap_or_else, log::info!, new_pipe, pipefd.write ...(+3) |
| `kernel/src/memory/heap.rs#alloc` | `driver/src/net/mod.rs#alloc` | 同板块同名 | align_up_to, alloc_start.saturating_add, core::mem::align_of::<usize>, heap.heap_start.saturating_add, heap.next.saturating_sub, layout.align ...(+6) | Arc::clone, NonNull::new, NonNull::new(self.pool.as_ptr().add(pool_offset) as *mut u8).unwrap, Some, self.free_list.lock, self.free_list.lock().pop ...(+2) |

#### 中断/异常处理
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `kernel/src/trap/mod.rs#recover_loongarch_user_exception` | `modules/vfs/src/devfs/tty.rs#new` | 同模块上下文对齐/差异观察 | crate::arch::arch_shutdown, crate::println!, crate::syscall::is_runner_active, crate::syscall::recover_user_fault_to_runner | 内部闭环逻辑 |
| `kernel/src/arch/riscv64.rs#init_trap` | `modules/net/src/tcp.rs#block_on_async` | 同模块上下文对齐/差异观察 | core::arch::asm!, crate::println! | Err, Ok, SOCKET_SET.check_poll, SOCKET_SET.poll_interfaces, f, has_signal ...(+3) |

#### 系统调用
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `kernel/src/syscall/mod.rs#clear` | `kernel/src/syscall/io.rs#clear` | 同板块同名 | 内部闭环逻辑 | self.fds_bits.len |
| `kernel/src/runner.rs#run_elf` | `kernel/src/ipc/futex.rs#requeue_waiters` | 同模块上下文对齐/差异观察 | MaybeUninit::<JumpBuf>::uninit, Some, Vec::new, argv.first, core::hint::black_box, core::ptr::addr_of! ...(+14) | Ok, Vec::with_capacity, log::info!, min, new_waiters.push, old_waiters.is_empty ...(+7) |
| `kernel/src/runner.rs#print_la_lua_syscall_trace_if_needed` | `kernel/src/ipc/futex.rs#wake` | 同模块上下文对齐/差异观察 | la_real_lua_trace_enabled, label.starts_with, put_str, put_usize_dec, put_usize_signed, total.saturating_sub | Err, Ok, drop, log::debug!, log::info!, min ...(+5) |
| `kernel/src/runner.rs#trace_rv_basic_case` | `user/src/lib.rs#execve` | 同模块上下文对齐/差异观察 | crate::syscall::debug_fd_present, crate::syscall::debug_last_syscall, name.is_empty, put_str, put_usize_dec | CString::new, CString::new(*s).unwrap, CString::new(path).unwrap, argv.as_ptr, argv.iter, argv.iter().map ...(+12) |

#### 文件系统
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `kernel/src/fs/vfs.rs#seek` | `modules/vfs-core/src/file.rs#seek` | 同板块同名 | ((self.offset as i64) + d).max, ((size as i64) + d).max, self.inode.stat | Err, Ok, off.abs, self.pos, self.set_pos, self.size |
| `kernel/src/fs/ramfs.rs#read_at` | `kernel/src/net/socket.rs#read_at` | 同板块同名 | buf.len, buf.len().min, buf[..n].copy_from_slice, d.len, self.data.lock | Ok, buf.len, self.sk.recvfrom, self.sk.recvfrom(buf).await.map, warn! |
| `kernel/src/fs/ramfs.rs#write_at` | `kernel/src/net/socket.rs#write_at` | 同板块同名 | buf.len, d.len, d.resize, d[offset..offset + buf.len()].copy_from_slice, self.data.lock | Ok, buf.len, self.sk.sendto, warn! |
| `kernel/src/panic.rs#panic` | `kernel/src/panic.rs#panic` | 同板块同名 | crate::arch::arch_shutdown, crate::println!, info.location | LOG_INITIALIZED.store, PANIC_CNT.fetch_add, backtrace, disable_interrupt, info.location, info.message ...(+5) |

#### 设备驱动
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `kernel/src/driver/virtio_mmio.rs#init_pci` | `driver/src/serial/mod.rs#probe_char_device` | 同模块上下文对齐/差异观察 | Some, VirtIoBlk::init_at, VirtIoBlk::init_pci_legacy_io, crate::println!, pci_io_addr, pci_r16 ...(+3) | Arc::new, Some, bytes.iter, chosen
            .properties, chosen
            .properties()
            .find, chosen
            .properties()
            .find(|n| n.name == "stdout-path")
            .and_then ...(+16) |
| `kernel/src/driver/virtio_mmio.rs#read_sector_mmio` | `kernel/src/net/mod.rs#try_from` | 同模块上下文对齐/差异观察 | crate::println!, self.read_sector_mmio_once, self.reset_mmio_queue | Ok, log::warn! |
| `kernel/src/driver/virtio_mmio.rs#read_sector_pci_legacy_io` | `driver/src/blk/virtio.rs#try_new` | 同模块上下文对齐/差异观察 | crate::println!, self.read_sector_pci_legacy_io_once, self.reset_pci_legacy_queue | "virtio-blk".to_string, Arc::new, BufferCache::new, Some, SpinNoIrqLock::new, VirtIOBlk::<VirtioHalImpl, MmioTransport>::new ...(+4) |
| `kernel/src/driver/virtio_mmio.rs#reset_pci_legacy_queue` | `driver/src/manager.rs#probe` | 同模块上下文对齐/差异观察 | core::mem::size_of::<VirtIoPciDma>, core::ptr::write_bytes, dma_fence, io_w16, io_w32, io_w8 | Some, config::board::set_harts, dev.clone, dev.dev_id, dev.irq_no, device_tree.chosen ...(+15) |

#### 网络协议栈
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `kernel/src/runner.rs#print_compat_netperf` | `modules/net/src/tcp.rs#shutdown` | 同模块上下文对齐/差异观察 | put_str | Err, LISTEN_TABLE.unlisten, Ok, SOCKET_SET.check_poll, SOCKET_SET.poll_interfaces, SOCKET_SET.with_socket_mut::<tcp::Socket, _, _> ...(+12) |

#### 文件系统/第三方库
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `kernel/src/fs/ext4.rs#size` | `modules/ext4/src/disk.rs#size` | 同板块同名 | 内部闭环逻辑 | self.dev.size |
| `kernel/src/fs/ext4.rs#extent_lookup` | `modules/ext4/src/dentry.rs#base_rename_to` | 同模块上下文对齐/差异观察 | Some, alloc_buf, cbuf.as_ptr, cbuf.as_ptr().add, cp.wrapping_add, inode.block.as_ptr ...(+3) | Err, Ok, flags.contains, lwext4_mvdir, lwext4_mvfile, lwext4_rmdir ...(+13) |
| `kernel/src/main.rs#mount_ext4` | `modules/ext4/src/file/dir.rs#base_load_dir` | 同模块上下文对齐/差异观察 | Ext4Fs::open, Some, VirtIoBlk::init, VirtIoBlk::init_pci, crate::println!, entries.is_empty ...(+2) | CString::new, CString::new(dirent.name).map_err, Ext4DirInode::new, Ext4DirInode::new(self.super_block(), ext4_dir).clone, Ext4FileInode::new, Ext4FileInode::new(self.super_block(), ext4_file).clone ...(+25) |
| `kernel/src/fs/ext4.rs#read_dir` | `modules/ext4/src/dentry.rs#base_create` | 同模块上下文对齐/差异观察 | String::from, Vec::new, alloc_buf, buf.as_ptr, buf.as_ptr().add, core::slice::from_raw_parts ...(+5) | (OpenFlags::O_RDWR | OpenFlags::O_CREAT | OpenFlags::O_TRUNC).bits, Ext4DirInode::new, Ext4FileInode::new, LwExt4Dir::create, LwExt4Dir::create(&path).map_err, LwExt4File::open ...(+15) |

#### 第三方库
仅参考项目中观察到该模块代表函数，目标项目没有同模块可对齐证据：
- 参考项目：`crates/ring-buffer/src/lib.rs#read`，调用摘要：buf.is_empty, buf.len, buf[..ret_len].copy_from_slice, buf[..right_len].copy_from_slice, buf[right_len..ret_len].copy_from_slice, cmp::min ...(+1)
- 参考项目：`crates/ring-buffer/src/lib.rs#write`，调用摘要：buf.is_empty, buf.len, cmp::min, self.arr.len, self.arr[..(ret_len - (n - self.tail))]
                    .copy_from_slice, self.arr[self.tail..].copy_from_slice ...(+1)
- 参考项目：`crates/range-map/src/lib.rs#find_free_range`，调用摘要：Some, debug_assert!, self.0.range, self.0.range(..=start).next_back, start.min
- 参考项目：`crates/range-map/src/lib.rs#try_insert`，调用摘要：Err, Ok, debug_assert!, log::error!, self.0.range, self.0.range(..range.end).next_back ...(+3)

#### 通用模块/其他
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `kernel/src/sync.rs#lock` | `modules/sync/src/mutex/spin_mutex.rs#lock` | 同板块同名 | core::hint::spin_loop, self.locked.swap | S::before_lock, self
                .lock
                .compare_exchange, self
                .lock
                .compare_exchange(false, true, Ordering::Acquire, Ordering::Relaxed)
                .is_ok, self.wait_unlock |
| `kernel/src/console.rs#write_str` | `user/src/console.rs#write_str` | 同板块同名 | Ok, crate::arch::putchar, s.bytes | Ok, s.as_bytes, write |
| `kernel/src/sync.rs#deref_mut` | `modules/sync/src/mutex/sleep_mutex.rs#deref_mut` | 同板块同名同结构 | self.mutex.data.get | self.mutex.data.get |
| `kernel/src/sync.rs#deref` | `modules/sync/src/mutex/sleep_mutex.rs#deref` | 同板块同名同结构 | self.mutex.data.get | self.mutex.data.get |


#### 1. 宏观判定
- **定性结论**: **独立开发**。双方客观 AST 相似度仅 0.73%，共同 AST 指纹仅 13 个，且均归因于 Linux 系统调用 ABI 强制同名、Rust 标准库通用 API 或跨项目普遍存在的惯用写法。目标项目覆盖率 3.2%，参考项目覆盖率 0.94%，远低于通常认定的复用阈值。双方在核心架构（同步 vs 异步运行时）、文件系统抽象（RamFS vs Dentry/Inode 树）、进程模型（全局锁调度 vs 进程组管理）上存在根本性分化，不存在底层逻辑复用。

#### 2. 全模块覆盖矩阵解读
- **进程管理**: 双方均存在该模块。共同 AST 指纹为 0，目标覆盖率 0.0%，参考覆盖率 0.0%。所有同名函数（如 `sys_wait4`、`sys_execve`）均因系统调用 ABI 导致，底层调用图谱完全不同。证据强度：**未形成**。
- **任务调度/异步运行时**: 双方均存在该模块。共同 AST 指纹为 2，目标覆盖率 5.13%，参考覆盖率 1.08%。共同指纹来自 `SeekFrom` 等 Rust 标准库枚举，非独创逻辑。目标为同步阻塞模型，参考为异步运行时模型，I/O 路径维度不同。证据强度：**弱**。
- **内存管理**: 双方均存在该模块。共同 AST 指纹为 8，目标覆盖率 6.25%，参考覆盖率 2.33%。共同指纹涉及 `sys_mmap`、`sys_openat` 等系统调用，但底层分配器（Bump vs 权限位解析）和对象管理模型完全不同。证据强度：**弱**。
- **中断/异常处理**: 双方均存在该模块。共同 AST 指纹为 0，目标覆盖率 0.0%，参考覆盖率 0.0%。目标处理 LoongArch 用户态异常，参考为 TTY 设备创建，功能领域无关。证据强度：**未形成**。
- **系统调用**: 双方均存在该模块。共同 AST 指纹为 0，目标覆盖率 0.0%，参考覆盖率 0.0%。目标通过 `ctx` 代理隐藏底层，参考直接操作 Dentry 树，架构完全不同。证据强度：**未形成**。
- **文件系统**: 双方均存在该模块。共同 AST 指纹为 0，目标覆盖率 0.0%，参考覆盖率 0.0%。目标使用简化 RamFS，参考使用完整 Dentry/Inode VFS，抽象层次迥异。证据强度：**未形成**。
- **设备驱动**: 双方均存在该模块。共同 AST 指纹为 0，目标覆盖率 0.0%，参考覆盖率 0.0%。目标专注 VirtIO-BLK 的 PCI/MMIO 路径，参考分散于串口/网络/块设备管理。证据强度：**未形成**。
- **网络协议栈**: 双方均存在该模块。共同 AST 指纹为 0，目标覆盖率 0.0%，参考覆盖率 0.0%。目标仅含一个输出字符串的函数，参考为完整 TCP 协议栈，规模与复杂度不在同一量级。证据强度：**未形成**。
- **第三方库/公共依赖**: 仅参考项目存在该模块（`ring-buffer`、`range-map` 等 crate），目标项目无对应模块。共同 AST 指纹为 0。证据强度：**未形成**。

#### 3. 底层 AST 关键证据指纹表
| 所属板块 | 目标项目路径 | 参考项目路径 | 证据类型 | 技术说明 |
| :--- | :--- | :--- | :--- | :--- |
| 进程管理 | `kernel/src/syscall/mod.rs#sys_wait4` | `kernel/src/syscall/process.rs#sys_wait4` | 同名系统调用 | Linux ABI 强制命名。目标使用全局状态锁与简单子进程调度；参考使用进程组与任务管理器。底层状态机完全不同。 |
| 进程管理 | `kernel/src/syscall/mod.rs#sys_execve` | `kernel/src/syscall/process.rs#sys_execve` | 同名系统调用 | Linux ABI 强制命名。目标通过上下文查找 Inode 并进入 ELF 加载器；参考硬编码 Busybox 路径并全量读取。加载路径完全不同。 |
| 任务调度/异步运行时 | `kernel/src/syscall/mod.rs#sys_read` | `kernel/src/syscall/fs.rs#sys_read` | 同名系统调用 | Linux ABI 强制命名。目标为同步阻塞模型，直接操作扁平 FD 表与 Inode；参考为异步运行时，使用 Future 选择器与任务中断。 |
| 任务调度/异步运行时 | `kernel/src/syscall/mod.rs#sys_lseek` | `kernel/src/syscall/fs.rs#sys_lseek` | 标准库 API 使用 | 共同使用 Rust 标准库 `SeekFrom` 枚举。后续句柄获取与锁定逻辑完全不同。 |
| 内存管理 | `kernel/src/syscall/mod.rs#sys_mmap` | `kernel/src/syscall/mm.rs#sys_mmap` | 同名系统调用 | Linux ABI 强制命名。目标使用 bump 物理页分配器；参考使用权限/标志位解析与地址对齐。分配策略迥异。 |
| 通用模块/其他 | `kernel/src/sync.rs#deref` | `modules/sync/src/mutex/sleep_mutex.rs#deref` | 标准库 Trait 实现 | 共同一行 `self.mutex.data.get` 是 Rust `Deref` for `MutexGuard` 的标准惯用写法，不具独创性。 |
| 文件系统 | `kernel/src/fs/ramfs.rs#read_at` | `kernel/src/net/socket.rs#read_at` | 跨板块同名 | 目标操作内存 buffer (`Vec<u8>`)，参考操作网络 socket (`sk.recvfrom`)。外部对象与领域完全不同，属硬凑证据。 |
| 设备驱动 | `kernel/src/driver/virtio_mmio.rs#init_pci` | `driver/src/serial/mod.rs#probe_char_device` | 跨板块硬凑 | 目标初始化 VirtIO 块设备 PCI 路径；参考从设备树探测串口设备。功能无关。 |

#### 4. 核心代码调用流对比剖析
* **【进程管理 / `sys_wait4` 调用路径】**
* **底层技术析理**: 目标项目采用全局状态锁 `WAIT_STATUS_PTR` 配合简单的子进程调度函数 `try_run_child` 与 `wait_for_child`，模型为主动轮询与直接状态写入。参考项目则构建了完整的进程组管理器 `PROCESS_GROUP_MANAGER` 和任务管理器 `TASK_MANAGER`，通过 `WaitFor` 枚举和 `WaitOptions` 位标志实现复杂的等待策略。两者在进程生命周期管理与等待语义的实现上存在架构级差异。
* **代码溯源链接**:
  - 目标项目: `kernel/src/syscall/mod.rs#sys_wait4`
  - 参考项目: `kernel/src/syscall/process.rs#sys_wait4`

* **【任务调度/异步运行时 / `sys_read` 调用路径】**
* **底层技术析理**: 目标项目的调用流为 `current_fd_table` -> `fd_table.get` -> `fh.inode.read_at` -> `fh.lock`，是典型的同步阻塞式操作，直接获取文件句柄并锁定 Inode 进行读取。参考项目的调用流为 `task.with_fd_table` -> `table.get_file` -> `file.read`，并引入 `Select2Futures::new` 和 `task.set_interruptable`，体现了异步运行时的核心特征。两者在 I/O 模型上存在同步与异步的根本性分歧。
* **代码溯源链接**:
  - 目标项目: `kernel/src/syscall/mod.rs#sys_read`
  - 参考项目: `kernel/src/syscall/fs.rs#sys_read`

* **【内存管理 / `sys_mmap` 调用路径】**
* **底层技术析理**: 目标项目使用 `BumpFrameAllocator::new` 和 `PhysAddr::new` 进行物理内存分配，并通过裸指针 `write_bytes` 操作页面，属于简单的 bump 分配策略。参考项目则专注于解析内存映射标志位，如 `MmapFlags::from_bits_truncate`、`MmapProt::from_bits_truncate`，并进行地址对齐计算 `(addr + length).round_up`。两者在内存管理的核心抽象（物理分配器 vs 权限与标志解析）上完全不同。
* **代码溯源链接**:
  - 目标项目: `kernel/src/syscall/mod.rs#sys_mmap`
  - 参考项目: `kernel/src/syscall/mm.rs#sys_mmap`

#### 5. 单方模块与降权项
- **单方模块**: 参考项目独有的 `第三方库` 模块（含 `ring-buffer`、`range-map` 等自研 crate），目标项目无任何对应实现，不构成同源证据。
- **降权项**: 所有因 Linux 系统调用 ABI 导致的同名函数（如 `sys_write`、`sys_close`、`sys_chdir` 等），以及 Rust 标准库通用 API（如 `SeekFrom`、`Arc::new`、`Vec::new`、`copy_from_slice`）和惯用模式（如 `MutexGuard` 的 `deref` 实现），均降级为弱信号，不构成同板块主证据。跨板块的同名或同哈希配对（如 `ramfs::read_at` vs `socket::read_at`）因操作对象和领域完全不同，予以排除。

#### 6. 保守结论
本案客观 AST 相似度仅 0.73%，共同指纹 13 个，且均可被 Linux ABI、Rust 标准库或通用编程习惯所解释，无任何底层逻辑复用痕迹。双方在进程管理、任务调度、文件系统等核心板块的架构设计上存在根本性分歧：目标项目为面向 LoongArch 真实机测试的同步、扁平化内核；参考项目为基于异步运行时与完整 VFS 树的复杂内核。96% 以上的 AST 结构互不相同，符合独立开发的统计特征。综合判定，目标项目不存在对参考项目的代码复用，原创性成立。

------------------------------

