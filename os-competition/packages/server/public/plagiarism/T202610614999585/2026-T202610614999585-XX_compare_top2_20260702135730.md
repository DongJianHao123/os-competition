# 🛡️ OS 内核赛自动化代码级架构审计报告

**审计时间**: `2026-07-02 13:57:30`
**参赛作品名称**: `2026-T202610614999585-XX`
**年份**: `2026`
**学校**: `XX`
**队伍名称**: `T202610614999585`
**比赛名字**: `OS-Competition`
**比赛子赛道**: `Kernel-Implementation`
**仓库地址**: `https://gitlab.eduxiji.net/educg-group-43501-3132633/T202610614999585-464`
**推测开发语言**: `Rust / C (由AST探针自动识别)`
**特征函数量**: `2880`

## ⚔️ 底层代码同源性对比 (AST Homology)
> 本部分由专门的代码审查智能体，对该项目与符合年份条件的历届最高相似度项目进行控制流与底层调用对比。新版报告会先给出确定性的全模块覆盖矩阵，再给出 Agent 的分模块解释，避免只围绕少量抽样函数展开。

### 🏆 Top 2 底层对标: vs `2025-StarryX-杭州电子科技大学`（杭州电子科技大学 / StarryX）

#### 对方作品信息

**对方年份**: `2025`
**对方学校**: `杭州电子科技大学`
**对方队伍名称**: `StarryX`
**对方比赛名字**: `OS-Competition`
**对方比赛子赛道**: `Kernel-Implementation`
**对方仓库地址**: `https://gitlab.eduxiji.net/educg-group-36002-2710490/T202510336995486-3037`

> 双向覆盖率：共同 AST 指纹: 1483；唯一 AST 指纹: 目标项目 2330，参考项目 2897；覆盖率: 目标项目 63.65%，参考项目 51.19%

## 全模块对比总览（确定性证据）

> 总体覆盖率：共同 AST 指纹: 1483；唯一 AST 指纹: 目标项目 2330，参考项目 2897；覆盖率: 目标项目 63.65%，参考项目 51.19%

### 1. 模块覆盖矩阵
| 模块 | 目标函数数 | 参考函数数 | 共同唯一 AST 指纹 | Target 覆盖 | Base 覆盖 | 状态 |
| :--- | ---: | ---: | ---: | ---: | ---: | :--- |
| 进程管理 | 118 | 172 | 43 | 40.19% | 27.04% | 双方存在且有结构重合 |
| 任务调度/异步运行时 | 482 | 480 | 182 | 49.86% | 51.85% | 双方存在且有结构重合 |
| 内存管理 | 362 | 430 | 200 | 59.7% | 51.41% | 双方存在且有结构重合 |
| 中断/异常处理 | 58 | 70 | 47 | 90.38% | 75.81% | 双方存在且有结构重合 |
| 系统调用 | 99 | 89 | 59 | 65.56% | 73.75% | 双方存在且有结构重合 |
| 文件系统 | 802 | 801 | 336 | 51.45% | 54.11% | 双方存在且有结构重合 |
| 设备驱动 | 135 | 139 | 118 | 96.72% | 93.65% | 双方存在且有结构重合 |
| 网络协议栈 | 90 | 77 | 51 | 66.23% | 83.61% | 双方存在且有结构重合 |
| 文件系统/第三方库 | 31 | 164 | 31 | 100.0% | 19.75% | 双方存在且有结构重合 |
| 网络协议栈/第三方库 | 146 | 166 | 59 | 47.97% | 40.97% | 双方存在且有结构重合 |
| 第三方库 | 0 | 349 | 0 | 0.0% | 0.0% | 仅参考项目体现 |
| 通用模块/其他 | 557 | 665 | 375 | 85.62% | 70.75% | 双方存在且有结构重合 |

### 2. 分模块证据清单

#### 进程管理
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `api/src/imp/task/exit.rs#do_exit` | `xapi/src/task/exit.rs#do_exit` | 同板块同名 | 0i32.to_ne_bytes, Arc::strong_count, FD_CLOEXEC_TABLE.write, FD_CLOEXEC_TABLE.write().clear, FD_TABLE.write, FutexKey::new_current ...(+54) | FD_TABLE.clear, FutexKey::new, IPC_MANAGER.clear, SignalInfo::new, UserPtr::<Pid>::from, axtask::exit ...(+32) |
| `arceos/modules/axtask/src/run_queue.rs#exit_current` | `arceos/modules/axtask/src/run_queue.rs#exit_current` | 同板块同名同结构 | EXITED_TASKS.current_ref_mut_raw, EXITED_TASKS.current_ref_mut_raw().clear, EXITED_TASKS.current_ref_mut_raw().push_back, WAIT_FOR_EXIT.current_ref_mut_raw, WAIT_FOR_EXIT.current_ref_mut_raw().notify_one, assert! ...(+8) | EXITED_TASKS.current_ref_mut_raw, EXITED_TASKS.current_ref_mut_raw().clear, EXITED_TASKS.current_ref_mut_raw().push_back, WAIT_FOR_EXIT.current_ref_mut_raw, WAIT_FOR_EXIT.current_ref_mut_raw().notify_one, assert! ...(+8) |
| `arceos/api/arceos_posix_api/src/imp/io_mpx/epoll.rs#sys_epoll_wait` | `arceos/api/arceos_posix_api/src/imp/io_mpx/epoll.rs#sys_epoll_wait` | 同板块同名同结构 | debug!, syscall_body! | debug!, syscall_body! |
| `arceos/modules/axtask/src/wait_queue.rs#cancel_events` | `arceos/modules/axtask/src/wait_queue.rs#cancel_events` | 同板块同名同结构 | curr.in_wait_queue, curr.ptr_eq, curr.set_in_wait_queue, curr.timer_ticket_expired, self.queue.lock, self.queue.lock().retain | curr.in_wait_queue, curr.ptr_eq, curr.set_in_wait_queue, curr.timer_ticket_expired, self.queue.lock, self.queue.lock().retain |

#### 任务调度/异步运行时
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `arceos/ulib/axlibc/c/printf.c#format_string_loop` | `arceos/ulib/axlibc/c/printf.c#format_string_loop` | 同板块同名同结构 | ABS_FOR_PRINTING, ADVANCE_IN_FORMAT_STRING, atou_, is_digit_, out_rev_, parse_flags ...(+5) | ABS_FOR_PRINTING, ADVANCE_IN_FORMAT_STRING, atou_, is_digit_, out_rev_, parse_flags ...(+5) |
| `api/src/imp/futex.rs#sys_futex` | `xapi/src/task/futex.rs#sys_futex` | 同板块同名 | Err, FutexKey::new_current, Ok, StarryTaskExt::of, StarryTaskExt::of(&curr)
                    .thread_data, StarryTaskExt::of(&curr)
                    .thread_data()
                    .futex_bitset
                    .store ...(+27) | Err, FutexKey::new, Ok, axtask::yield_now, current, current().task_ext ...(+24) |
| `arceos/modules/axlog/src/lib.rs#log` | `arceos/modules/axlog/src/lib.rs#log` | 同板块同名同结构 | cfg_if::cfg_if!, record.level, record.line, record.line().unwrap_or, record.metadata, record.target ...(+1) | cfg_if::cfg_if!, record.level, record.line, record.line().unwrap_or, record.metadata, record.target ...(+1) |
| `arceos/modules/axtask/src/run_queue.rs#switch_to` | `arceos/modules/axtask/src/run_queue.rs#switch_to` | 同板块同名 | (*prev_ctx_ptr).switch_to, Arc::downgrade, CurrentTask::set_current, PREV_TASK.current_ref_mut_raw, assert!, clear_prev_task_on_cpu ...(+14) | (*prev_ctx_ptr).switch_to, Arc::downgrade, CurrentTask::set_current, PREV_TASK.current_ref_mut_raw, assert!, clear_prev_task_on_cpu ...(+11) |

#### 内存管理
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `src/syscall.rs#handle_syscall_impl` | `src/syscall.rs#handle_syscall_impl` | 同板块同名 | Err, Ok, sys_accept, sys_accept4, sys_access, sys_arch_prctl ...(+189) | Err, Ok, sys_accept, sys_accept4, sys_access, sys_arch_prctl ...(+203) |
| `api/src/imp/mm/mmap.rs#sys_mmap` | `xapi/src/mm/mmap.rs#sys_mmap` | 同板块同名 | (addr + length).align_up, (offset as usize).align_offset_4k, (starry_config::USER_HEAP_BASE + starry_config::USER_HEAP_SIZE).align_up, Err, File::from_fd, MmapFlags::from_bits_truncate ...(+64) | (addr + length).align_up, Err, FD_TABLE.is_assigned, File::from_fd, File::from_fd(fd, FileFlags::READ, FileFlags::empty())
            .map_err, FileFlags::empty ...(+39) |
| `arceos/ulib/axlibc/src/strftime.rs#strftime_inner` | `arceos/ulib/axlibc/src/strftime.rs#strftime_inner` | 同板块同名同结构 | CountingWriter::new, format.offset, inner_strftime, w! | CountingWriter::new, format.offset, inner_strftime, w! |
| `arceos/ulib/axlibc/src/strftime.rs#inner_strftime` | `arceos/ulib/axlibc/src/strftime.rs#inner_strftime` | 同板块同名同结构 | format.offset, w! | format.offset, w! |

#### 中断/异常处理
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `arceos/ulib/axlibc/c/pow.c#exp_inline` | `arceos/ulib/axlibc/c/pow.c#exp_inline` | 同板块同名同结构 | __math_oflow, __math_uflow, asdouble, asuint64, converttoint, eval_as_double ...(+4) | __math_oflow, __math_uflow, asdouble, asuint64, converttoint, eval_as_double ...(+4) |
| `arceos/modules/axhal/src/arch/aarch64/trap.rs#handle_sync_exception` | `arceos/modules/axhal/src/arch/aarch64/trap.rs#handle_sync_exception` | 同板块同名同结构 | ESR_EL1.extract, crate::trap::handle_syscall, crate::trap::post_trap_callback, debug!, esr.read, esr.read_as_enum ...(+6) | ESR_EL1.extract, crate::trap::handle_syscall, crate::trap::post_trap_callback, debug!, esr.read, esr.read_as_enum ...(+6) |
| `src/syscall.rs#handle_syscall` | `src/syscall.rs#handle_syscall` | 同板块同名同结构 | Sysno::new, debug!, err.code, handle_syscall_impl, result.unwrap_or_else, sysno
        .ok_or ...(+4) | Sysno::new, debug!, err.code, handle_syscall_impl, result.unwrap_or_else, sysno
        .ok_or ...(+4) |
| `arceos/modules/axhal/src/platform/riscv64_qemu_virt/irq.rs#register_handler` | `arceos/modules/axhal/src/platform/riscv64_qemu_virt/irq.rs#register_handler` | 同板块同名同结构 | with_cause! | with_cause! |

#### 系统调用
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `arceos/api/arceos_posix_api/src/imp/net.rs#sys_getaddrinfo` | `arceos/api/arceos_posix_api/src/imp/net.rs#sys_getaddrinfo` | 同板块同名同结构 | char_ptr_to_str, debug!, syscall_body! | char_ptr_to_str, debug!, syscall_body! |
| `api/src/imp/net/socket.rs#sys_socket` | `arceos/api/arceos_posix_api/src/imp/net.rs#sys_socket` | 同板块同名 | Err, Mutex::new, Ok, Socket::Tcp, Socket::Udp, TcpSocket::new ...(+6) | debug!, syscall_body! |
| `api/src/imp/net/io.rs#sys_sendto` | `arceos/api/arceos_posix_api/src/imp/net.rs#sys_sendto` | 同板块同名 | Ok, Socket::from_fd, UnixAddr::read_from_user, UnixSocket::from_fd, addr.is_null, buf.get_as_slice ...(+9) | debug!, syscall_body! |
| `api/src/imp/net/io.rs#sys_recvfrom` | `arceos/api/arceos_posix_api/src/imp/net.rs#sys_recvfrom` | 同板块同名 | Ok, Socket::from_fd, UnixSocket::from_fd, UserPtr::from, addr.is_null, buf.write_bytes ...(+8) | debug!, syscall_body! |

#### 文件系统
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `arceos/ulib/axlibc/c/glob.c#do_glob` | `arceos/ulib/axlibc/c/glob.c#do_glob` | 同板块同名同结构 | S_ISDIR, append, closedir, do_glob, errfunc, fnmatch ...(+7) | S_ISDIR, append, closedir, do_glob, errfunc, fnmatch ...(+7) |
| `api/src/imp/fs/io.rs#sys_splice` | `xapi/src/fs/io.rs#sys_splice` | 同板块同名 | 0x4000.min, Err, File::from_fd, Ok, Pipe::from_fd, Pipe::from_fd(fd_in).is_ok ...(+33) | Err, File::from_fd, Pipe::from_fd, Pipe::from_fd(fd_in, FileFlags::READ, FileFlags::PATH).is_ok, Pipe::from_fd(fd_out, FileFlags::WRITE, FileFlags::PATH).is_ok, SendFile::Direct ...(+14) |
| `arceos/ulib/axlibc/src/setjmp.rs#longjmp` | `arceos/ulib/axlibc/src/setjmp.rs#longjmp` | 同板块同名同结构 | core::arch::naked_asm! | core::arch::naked_asm! |
| `arceos/ulib/axlibc/src/setjmp.rs#setjmp` | `arceos/ulib/axlibc/src/setjmp.rs#setjmp` | 同板块同名同结构 | core::arch::naked_asm! | core::arch::naked_asm! |

#### 设备驱动
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `arceos/tools/bwbench_client/src/main.rs#transmit_benchmark` | `arceos/tools/bwbench_client/src/main.rs#transmit_benchmark` | 同板块同名同结构 | Local::now, NetDevice::new, NetDevice::new(interface).unwrap, current_time.signed_duration_since, current_time.signed_duration_since(past_time).num_seconds, dev.send ...(+2) | Local::now, NetDevice::new, NetDevice::new(interface).unwrap, current_time.signed_duration_since, current_time.signed_duration_since(past_time).num_seconds, dev.send ...(+2) |
| `arceos/tools/bwbench_client/src/device.rs#bind_interface` | `arceos/tools/bwbench_client/src/device.rs#bind_interface` | 同板块同名同结构 | ETH_P_ALL.to_be, Err, Ok, libc::bind, self.ifreq.ioctl, std::io::Error::last_os_error ...(+2) | ETH_P_ALL.to_be, Err, Ok, libc::bind, self.ifreq.ioctl, std::io::Error::last_os_error ...(+2) |
| `arceos/modules/axdriver/src/lib.rs#init_drivers` | `arceos/modules/axdriver/src/lib.rs#init_drivers` | 同板块同名同结构 | AllDevices::default, all_devs.block.iter, all_devs.block.iter().enumerate, all_devs.display.iter, all_devs.display.iter().enumerate, all_devs.net.iter ...(+5) | AllDevices::default, all_devs.block.iter, all_devs.block.iter().enumerate, all_devs.display.iter, all_devs.display.iter().enumerate, all_devs.net.iter ...(+5) |
| `arceos/modules/axdriver/src/virtio.rs#probe_pci` | `arceos/modules/axdriver/src/virtio.rs#probe_pci` | 同板块同名同结构 | D::try_new, Some, axdriver_virtio::probe_pci_device::<VirtIoHalImpl>, warn! | D::try_new, Some, axdriver_virtio::probe_pci_device::<VirtIoHalImpl>, warn! |

#### 网络协议栈
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `arceos/ulib/axlibc/c/network.c#inet_pton` | `arceos/ulib/axlibc/c/network.c#inet_pton` | 同板块同名同结构 | hexval, inet_pton, isdigit, memmove | hexval, inet_pton, isdigit, memmove |
| `arceos/api/arceos_posix_api/src/imp/net.rs#shutdown` | `arceos/api/arceos_posix_api/src/imp/net.rs#shutdown` | 同板块同名同结构 | Ok, tcpsocket.lock, tcpsocket.peer_addr, tcpsocket.shutdown, udpsocket.lock, udpsocket.peer_addr ...(+1) | Ok, tcpsocket.lock, tcpsocket.peer_addr, tcpsocket.shutdown, udpsocket.lock, udpsocket.peer_addr ...(+1) |
| `arceos/ulib/axlibc/c/socket.c#accept4` | `arceos/ulib/axlibc/c/socket.c#accept4` | 同板块同名同结构 | accept, fcntl | accept, fcntl |
| `arceos/api/arceos_posix_api/src/imp/net.rs#recvfrom` | `arceos/api/arceos_posix_api/src/imp/net.rs#recvfrom` | 同板块同名同结构 | Ok, Some, tcpsocket.lock, tcpsocket.lock().recv, tcpsocket.lock().recv(buf).map, udpsocket
                .lock ...(+2) | Ok, Some, tcpsocket.lock, tcpsocket.lock().recv, tcpsocket.lock().recv(buf).map, udpsocket
                .lock ...(+2) |

#### 文件系统/第三方库
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `arceos/modules/axfs-ng/src/fs/ext4/inode.rs#create` | `arceos/modules/axfs-ng/src/fs/ext4/inode.rs#create` | 同板块同名同结构 | DirEntry::new_dir, DirEntry::new_file, DirNode::new, Err, FileNode::new, Inode::new ...(+11) | DirEntry::new_dir, DirEntry::new_file, DirNode::new, Err, FileNode::new, Inode::new ...(+11) |
| `arceos/modules/axfs-ng/src/fs/ext4/inode.rs#metadata` | `arceos/modules/axfs-ng/src/fs/ext4/inode.rs#metadata` | 同板块同名同结构 | DeviceId::default, FileAttr::default, NodePermission::from_bits_truncate, Ok, into_vfs_type, self.fs
            .lock ...(+2) | DeviceId::default, FileAttr::default, NodePermission::from_bits_truncate, Ok, into_vfs_type, self.fs
            .lock ...(+2) |
| `arceos/modules/axfs-ng/src/fs/ext4/inode.rs#read_dir` | `arceos/modules/axfs-ng/src/fs/ext4/inode.rs#read_dir` | 同板块同名同结构 | Ok, core::str::from_utf8, core::str::from_utf8(entry.name())
                .map_err, core::str::from_utf8(entry.name())
                .map_err(|_| VfsError::EINVAL)?
                .to_owned, entry.ino, entry.inode_type ...(+10) | Ok, core::str::from_utf8, core::str::from_utf8(entry.name())
                .map_err, core::str::from_utf8(entry.name())
                .map_err(|_| VfsError::EINVAL)?
                .to_owned, entry.ino, entry.inode_type ...(+10) |
| `arceos/modules/axfs-ng/src/fs/ext4/inode.rs#update_metadata` | `arceos/modules/axfs-ng/src/fs/ext4/inode.rs#update_metadata` | 同板块同名同结构 | Ok, fs.with_inode_ref, fs.with_inode_ref(self.ino, |inode| {
            if let Some(mode) = update.mode {
                inode.set_mode((inode.mode() & !0xfff) | (mode.bits() as u32));
            }
            if let Some((uid, gid)) = update.owner {
                inode.set_owner(uid as _, gid as _);
            }
            if let Some(atime) = update.atime {
                inode.set_atime(&atime);
            }
            if let Some(mtime) = update.mtime {
                inode.set_mtime(&mtime);
            }
            Ok(())
        })
        .map_err, inode.mode, inode.set_atime, inode.set_mode ...(+4) | Ok, fs.with_inode_ref, fs.with_inode_ref(self.ino, |inode| {
            if let Some(mode) = update.mode {
                inode.set_mode((inode.mode() & !0xfff) | (mode.bits() as u32));
            }
            if let Some((uid, gid)) = update.owner {
                inode.set_owner(uid as _, gid as _);
            }
            if let Some(atime) = update.atime {
                inode.set_atime(&atime);
            }
            if let Some(mtime) = update.mtime {
                inode.set_mtime(&mtime);
            }
            Ok(())
        })
        .map_err, inode.mode, inode.set_atime, inode.set_mode ...(+4) |

#### 网络协议栈/第三方库
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `arceos/modules/axnet/src/smoltcp_impl/tcp.rs#connect` | `arceos/modules/axnet/src/smoltcp_impl/tcp.rs#connect` | 同板块同名 | Err, Ok, Ok::<(IpEndpoint, IpEndpoint), AxError>, SOCKET_SET
                .with_socket_mut::<tcp::Socket, _, _>, SOCKET_SET.add, SOCKET_SET.poll_interfaces ...(+31) | Err, Ok, Ok::<(IpEndpoint, IpEndpoint), NetError>, SOCKET_SET
                .with_socket_mut::<tcp::Socket, _, _>, SOCKET_SET.add, SocketSetWrapper::new_tcp_socket ...(+33) |
| `arceos/modules/axnet/src/smoltcp_impl/tcp.rs#recv_timeout` | `arceos/modules/axnet/src/smoltcp_impl/tcp.rs#recv_timeout` | 同板块同名 | Duration::from_nanos, Err, Ok, SOCKET_SET.poll_interfaces, SOCKET_SET.with_socket_mut::<tcp::Socket, _, _>, ax_err! ...(+17) | Err, Ok, SOCKET_SET.with_socket_mut::<tcp::Socket, _, _>, current_ticks, self.block_on, self.handle.get ...(+9) |
| `arceos/modules/axnet/src/smoltcp_impl/udp.rs#recv_from_timeout` | `arceos/modules/axnet/src/smoltcp_impl/udp.rs#recv_from_timeout` | 同板块同名 | Duration::from_nanos, Err, Ok, SOCKET_SET.poll_interfaces, SOCKET_SET.with_socket_mut::<udp::Socket, _, _>, ax_err! ...(+16) | Err, Ok, current_ticks, meta.endpoint.into, self.recv_impl, socket.recv_slice |
| `arceos/modules/axnet/src/smoltcp_impl/dns.rs#query` | `arceos/modules/axnet/src/smoltcp_impl/dns.rs#query` | 同板块同名 | Err, Ok, SOCKET_SET
            .with_socket_mut::<dns::Socket, _, _>, SOCKET_SET
            .with_socket_mut::<dns::Socket, _, _>(handle, |socket| {
                socket.start_query(iface.lock().context(), name, query_type)
            })
            .map_err, SOCKET_SET.poll_interfaces, SOCKET_SET.with_socket_mut::<dns::Socket, _, _> ...(+12) | Err, IpAddr::from, Ok, SOCKET_SET
            .with_socket_mut::<dns::Socket, _, _>, SOCKET_SET
            .with_socket_mut::<dns::Socket, _, _>(handle, |socket| {
                socket.start_query(iface.lock().context(), name, query_type)
            })
            .map_err, SOCKET_SET.poll_interfaces ...(+11) |

#### 第三方库
仅参考项目中观察到该模块代表函数，目标项目没有同模块可对齐证据：
- 参考项目：`arceos/crates/page_table_multiarch/page_table_multiarch/src/bits64.rs#map_region`，调用摘要：Err, M::flush_tlb, Ok, PageSize::Size1G.is_aligned, PageSize::Size2M.is_aligned, PageSize::Size4K.is_aligned ...(+12)
- 参考项目：`arceos/crates/axdriver_crates/axdriver_virtio/src/net.rs#try_new`，调用摘要：Err, InnerDev::new, InnerDev::new(transport).map_err, NetBufPool::new, Ok, Some ...(+14)
- 参考项目：`arceos/crates/page_table_multiarch/page_table_multiarch/src/bits64.rs#protect_region`，调用摘要：Err, Ok, TlbFlushAll::new, assert!, error!, self.protect ...(+5)
- 参考项目：`arceos/crates/page_table_multiarch/page_table_multiarch/src/arch/loongarch64.rs#flush_tlb`，调用摘要：asm!

#### 通用模块/其他
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `arceos/ulib/axlibc/c/printf.c#print_exponential_number` | `arceos/ulib/axlibc/c/printf.c#print_exponential_number` | 同板块同名同结构 | ABS_FOR_PRINTING, PRINTF_ABS, bastardized_floor, get_components, get_normalized_components, get_sign_bit ...(+5) | ABS_FOR_PRINTING, PRINTF_ABS, bastardized_floor, get_components, get_normalized_components, get_sign_bit ...(+5) |
| `arceos/ulib/axlibc/c/pow.c#pow` | `arceos/ulib/axlibc/c/pow.c#pow` | 同板块同名同结构 | __builtin_fma, __math_invalid, __math_oflow, __math_uflow, asdouble, asuint64 ...(+8) | __builtin_fma, __math_invalid, __math_oflow, __math_uflow, asdouble, asuint64 ...(+8) |
| `arceos/ulib/axlibc/c/string.c#twoway_strstr` | `arceos/ulib/axlibc/c/string.c#twoway_strstr` | 同板块同名同结构 | BITOP, MAX, memchr, memcmp | BITOP, MAX, memchr, memcmp |
| `arceos/ulib/axlibc/c/printf.c#print_broken_up_decimal` | `arceos/ulib/axlibc/c/printf.c#print_broken_up_decimal` | 同板块同名同结构 | out_rev_ | out_rev_ |


好的，我将遵循审计指令，基于提供的所有证据，进行最终的深度析理与定性仲裁。

### 1. 宏观判定
- **定性结论**: **独立开发，存在共享上游基线**
  综合客观 AST 相似度（39.61%）、双向覆盖率指标及双方模块画像，判定目标项目 `[2026-T202610614999585-XX]` 与参考项目 `[2025-StarryX-杭州电子科技大学]` 在整体上构成**独立开发**。两者在自研核心层（`api/` vs `xapi/`, `core/` vs `xcore/`）的抽象模型、资源管理策略和实现路径上存在系统性差异。控方所指控的“复用”证据，经核实，几乎全部位于双方共用的 `arceos/` 上游公共代码基线内，属于共享框架、标准接口及通用同步原语的必然收敛，不构成对参考项目原创部分的抄袭。

### 2. 全模块覆盖矩阵解读
| 模块 | 双方存在 | 共同指纹 | 目标覆盖 | 参考覆盖 | 证据强度 | 解读 |
| :--- | :---: | :---: | :---: | :---: | :---: | :--- |
| **进程管理** | 是 | 43 | 40.19% | 27.04% | **弱** | 覆盖率偏低，共同指纹主要来自 `arceos/` 公共任务与等待队列。自研层（`api/` vs `xapi/`）在信号、退出、IPC 等核心逻辑上抽象模型不同（`StarryTaskExt` vs `XProcess`），差异显著。 |
| **任务调度/异步运行时** | 是 | 182 | 49.86% | 51.85% | **中** | 覆盖率中等，高度重合部分集中在 `arceos/modules/axtask` 调度器、`axlibc` 等公共运行时库，属于共享基线的核心。 |
| **内存管理** | 是 | 200 | 59.7% | 51.41% | **中** | 覆盖率较高，共同指纹多源于 `axlibc` 通用库函数（如 `strftime`）及系统调用分发框架。自研层 `sys_mmap` 等函数调用图谱存在差异。 |
| **中断/异常处理** | 是 | 47 | 90.38% | 75.81% | **中** | 覆盖率极高，但证据均指向 `arceos/modules/axhal` 等公共硬件抽象层和系统调用入口，这是所有基于 ArceOS 项目的标准实现，无可争议。 |
| **系统调用** | 是 | 59 | 65.56% | 73.75% | **弱** | 共同指纹主要来自 `arceos_posix_api` 的 POSIX 兼容层包装函数（如 `syscall_body!` 宏），属于接口强制收敛。自研层网络系统调用实现路径不同。 |
| **文件系统** | 是 | 336 | 51.45% | 54.11% | **中** | 覆盖率中等，共同指纹大量来自 `axlibc` 的 C 库函数（`glob`, `setjmp`）和 `axfs-ng` 公共文件系统框架。 |
| **设备驱动** | 是 | 118 | 96.72% | 93.65% | **中** | 覆盖率极高，证据完全落在 `arceos/modules/axdriver` 公共驱动框架和 `bwbench` 等公共工具内，属于共享基线的直接使用。 |
| **网络协议栈** | 是 | 51 | 66.23% | 83.61% | **中** | 共同指纹来自 `axlibc` 网络函数和 `arceos_posix_api` 标准 Socket 封装。自研层 `smoltcp_impl` 中的实现细节（如超时处理）存在差异。 |
| **文件系统/第三方库** | 是 | 31 | 100.0% | 19.75% | **弱** | 目标项目 100% 覆盖了参考项目在该分类下的部分函数，说明目标项目集成了参考项目所用第三方库（如 ext4）的一个子集，是独立裁剪，而非全量照搬。 |
| **网络协议栈/第三方库** | 是 | 59 | 47.97% | 40.97% | **弱** | 双方均集成了 `smoltcp` 等第三方网络栈，共同指纹源于此。覆盖率不高，说明各自封装和调用方式有差异。 |
| **第三方库** | 否 | 0 | 0.0% | 0.0% | **未形成** | 参考项目独有的 `page_table_multiarch` 等库在目标项目中完全缺失，是独立选型的直接证据。 |
| **通用模块/其他** | 是 | 375 | 85.62% | 70.75% | **中** | 覆盖率很高，证据几乎全部来自 `axlibc` 的 C 标准库实现（`printf`, `pow`, `string` 等），属于公共第三方代码。 |

### 3. 底层 AST 关键证据指纹表
| 所属板块 | 目标项目路径 | 参考项目路径 | 证据类型 | 技术说明 |
| :--- | :--- | :--- | :--- | :--- |
| 进程管理 | `arceos/modules/axtask/src/wait_queue.rs#cancel_events` | `arceos/modules/axtask/src/wait_queue.rs#cancel_events` | 公共基线 | 完全相同的 ArceOS 上游 `WaitQueue` 同步原语实现，非任何一方原创。 |
| 进程管理 | `api/src/imp/task/exit.rs#do_exit` | `xapi/src/task/exit.rs#do_exit` | 自研差异 | 同名函数，但目标项目使用 `FD_CLOEXEC_TABLE` / `StarryTaskExt`，参考项目使用 `FD_TABLE.clear` / `IPC_MANAGER`，核心资源管理模型不同。 |
| 进程管理 | `api/src/imp/signal.rs#sys_rt_sigprocmask` | `xapi/src/task/signal.rs#sys_rt_sigprocmask` | 自研差异 | 同名系统调用，目标项目基于 `StarryTaskExt` 线程数据模型，参考项目基于 `XProcess`/`XThread` 抽象，调用链完全不同。 |
| 任务调度 | `arceos/modules/axtask/src/run_queue.rs#switch_to` | `arceos/modules/axtask/src/run_queue.rs#switch_to` | 公共基线 | ArceOS 调度器核心上下文切换逻辑，调用图谱高度一致，属于公共上游代码。 |
| 内存管理 | `api/src/imp/mm/mmap.rs#sys_mmap` | `xapi/src/mm/mmap.rs#sys_mmap` | 自研差异 | 同名系统调用，目标项目包含 `USER_HEAP_BASE` 等特有地址空间检查，参考项目则侧重 `FD_TABLE.is_assigned` 检查，实现细节不同。 |
| 系统调用 | `arceos/api/arceos_posix_api/src/imp/io_mpx/epoll.rs#sys_epoll_wait` | `arceos/api/arceos_posix_api/src/imp/io_mpx/epoll.rs#sys_epoll_wait` | 公共基线 | 完全相同的 ArceOS POSIX 兼容层封装，仅包含 `debug!` 和 `syscall_body!` 宏，无原创逻辑。 |
| 文件系统 | `arceos/modules/axfs-ng/src/fs/ext4/inode.rs#create` | `arceos/modules/axfs-ng/src/fs/ext4/inode.rs#create` | 第三方库 | 完全相同的 ext4 文件系统第三方库代码，非项目原创。 |
| 设备驱动 | `arceos/modules/axdriver/src/virtio.rs#probe_pci` | `arceos/modules/axdriver/src/virtio.rs#probe_pci` | 公共基线 | 完全相同的 ArceOS VirtIO 驱动框架实现。 |
| 网络协议栈 | `arceos/modules/axnet/src/smoltcp_impl/tcp.rs#connect` | `arceos/modules/axnet/src/smoltcp_impl/tcp.rs#connect` | 自研差异 | 同名函数，目标项目使用 `AxError`，参考项目使用 `NetError`，错误处理类型不同，超时机制实现细节存在差异。 |
| 第三方库 | `arceos/ulib/axlibc/c/pow.c#pow` | `arceos/ulib/axlibc/c/pow.c#pow` | 第三方库 | 完全相同的 musl libc 数学库实现，属于公共第三方代码。 |

### 4. 核心代码调用流对比剖析
* **【进程管理/进程退出路径】**
* **底层技术析理**: 对比 `do_exit` 函数，两者均执行进程退出清理，但资源管理模型截然不同。目标项目采用细粒度的文件描述符表（`FD_CLOEXEC_TABLE`, `FD_TABLE`）分别清理，并通过 `StarryTaskExt` 扩展任务获取线程数据；参考项目则调用 `FD_TABLE.clear` 统一清理，并通过 `IPC_MANAGER` 清理进程间通信资源，任务扩展方式为 `curr.task_ext`。这体现了在进程资源管理和任务扩展机制上的独立设计。
* **代码溯源链接**:
  - 目标项目: `api/src/imp/task/exit.rs#do_exit`
  - 参考项目: `xapi/src/task/exit.rs#do_exit`

* **【进程管理/信号处理路径】**
* **底层技术析理**: 对比 `sys_rt_sigprocmask` 系统调用，两者实现信号屏蔽位设置，但核心抽象完全不同。目标项目通过 `StarryTaskExt::of(&current()).thread_data().signal.with_blocked_mut` 直接操作与任务绑定的线程信号数据；参考项目则通过 `XThread::from_thread` 和 `XProcess::from_thread` 进行多层用户空间抽象转换后再操作。这显示了在任务与信号数据组织方式上的根本性架构差异。
* **代码溯源链接**:
  - 目标项目: `api/src/imp/signal.rs#sys_rt_sigprocmask`
  - 参考项目: `xapi/src/task/signal.rs#sys_rt_sigprocmask`

* **【网络协议栈/TCP连接路径】**
* **底层技术析理**: 对比 `smoltcp_impl` 中的 `connect` 函数，两者均基于 `smoltcp` 库实现 TCP 连接，但在错误处理和轮询机制上存在差异。目标项目使用自定义的 `AxError` 类型，而参考项目使用 `NetError`。在等待连接建立的轮询逻辑中，双方的超时处理和状态检查调用序列也不尽相同，表明在第三方库之上的封装层是各自独立编写的。
* **代码溯源链接**:
  - 目标项目: `arceos/modules/axnet/src/smoltcp_impl/tcp.rs#connect`
  - 参考项目: `arceos/modules/axnet/src/smoltcp_impl/tcp.rs#connect`

### 5. 单方模块与降权项
* **单方模块**: 参考项目独有的 **第三方库** 模块（349个函数，如 `page_table_multiarch`），目标项目完全没有对应实现。这强烈证明双方在第三方库选型和项目构建上是独立的，目标项目并未全量复制参考项目的依赖。
* **降权项说明**:
  - **跨板块归类**: 控方将 `arceos/modules/axtask/src/wait_queue.rs`（通用同步原语）、`arceos/api/arceos_posix_api/src/imp/io_mpx/epoll.rs`（I/O多路复用）等函数归入【进程管理】板块，属于不当归类。这些通用组件在操作系统内核中服务于多个模块，其一致性源于共享的 ArceOS 基线，不能作为特定业务板块的复用证据。
  - **微小包装与公共接口**: `sys_getpid`、`sys_exit`、`ax_exit` 等函数仅包含对底层 API 的1-2行调用，`sys_epoll_wait` 仅包含宏调用。这些函数因 POSIX 标准或 ArceOS 框架接口而强制收敛，表达空间极其有限，其 AST 一致性不具备抄袭指征意义，已降级为弱信号。

### 6. 保守结论
本次审计的核心发现是，目标项目与参考项目在 `arceos/` 上游公共基线代码上存在大量必然的 AST 指纹重合，这是双方共同使用 ArceOS 内核框架、POSIX 兼容层及第三方库的直接结果。然而，在真正体现项目原创性的自研层（`api/` vs `xapi/`, `core/` vs `xcore/`），两者在进程资源管理、信号处理抽象、IPC 管理器设计等关键系统调用路径上，展现出 `StarryTaskExt` vs `XProcess`、`SHM_MANAGER` vs `IPC_MANAGER` 等系统性架构差异。整体 39.61% 的 AST 相似度主要由公共基线贡献，自研层重合度低。因此，现有证据不足以支持“抄袭”或“同源”指控，应认定为基于同一开源基线的独立开发项目。

------------------------------

