# 🛡️ OS 内核赛自动化代码级架构审计报告

**审计时间**: `2026-07-02 13:57:30`
**参赛作品名称**: `2026-T202610614999585-XX`
**年份**: `2026`
**学校**: `XX`
**队伍名称**: `T202610614999585`
**比赛名字**: `OS-Competition`
**比赛子赛道**: `Kernel-Implementation`
**仓库地址**: `https://gitlab.eduxiji.net/educg-group-43501-3132633/T202610614999585-464`
**推测开发语言**: `Rust / C (由AST探针自动识别)`
**特征函数量**: `2880`

## ⚔️ 底层代码同源性对比 (AST Homology)
> 本部分由专门的代码审查智能体，对该项目与符合年份条件的历届最高相似度项目进行控制流与底层调用对比。新版报告会先给出确定性的全模块覆盖矩阵，再给出 Agent 的分模块解释，避免只围绕少量抽样函数展开。

### 🏆 Top 1 底层对标: vs `2025-模仿游戏-燕山大学`（燕山大学 / 模仿游戏）

#### 对方作品信息

**对方年份**: `2025`
**对方学校**: `燕山大学`
**对方队伍名称**: `模仿游戏`
**对方比赛名字**: `OS-Competition`
**对方比赛子赛道**: `Kernel-Implementation`
**对方仓库地址**: `https://gitlab.eduxiji.net/educg-group-36002-2710490/T202510216995249-4014`

> 双向覆盖率：共同 AST 指纹: 1347；唯一 AST 指纹: 目标项目 2330，参考项目 1942；覆盖率: 目标项目 57.81%，参考项目 69.36%

## 全模块对比总览（确定性证据）

> 总体覆盖率：共同 AST 指纹: 1347；唯一 AST 指纹: 目标项目 2330，参考项目 1942；覆盖率: 目标项目 57.81%，参考项目 69.36%

### 1. 模块覆盖矩阵
| 模块 | 目标函数数 | 参考函数数 | 共同唯一 AST 指纹 | Target 覆盖 | Base 覆盖 | 状态 |
| :--- | ---: | ---: | ---: | ---: | ---: | :--- |
| 进程管理 | 118 | 76 | 40 | 37.38% | 57.14% | 双方存在且有结构重合 |
| 任务调度/异步运行时 | 482 | 383 | 203 | 55.62% | 74.63% | 双方存在且有结构重合 |
| 内存管理 | 362 | 302 | 217 | 64.78% | 78.62% | 双方存在且有结构重合 |
| 中断/异常处理 | 58 | 56 | 47 | 90.38% | 94.0% | 双方存在且有结构重合 |
| 系统调用 | 99 | 121 | 61 | 67.78% | 62.24% | 双方存在且有结构重合 |
| 文件系统 | 802 | 631 | 208 | 31.85% | 44.83% | 双方存在且有结构重合 |
| 设备驱动 | 135 | 135 | 121 | 99.18% | 99.18% | 双方存在且有结构重合 |
| 网络协议栈 | 90 | 67 | 54 | 70.13% | 96.43% | 双方存在且有结构重合 |
| 文件系统/第三方库 | 31 | 25 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |
| 网络协议栈/第三方库 | 146 | 99 | 64 | 52.03% | 70.33% | 双方存在且有结构重合 |
| 通用模块/其他 | 557 | 611 | 376 | 85.84% | 79.16% | 双方存在且有结构重合 |

### 2. 分模块证据清单

#### 进程管理
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `api/src/imp/task/exit.rs#do_exit` | `api/src/imp/task/exit.rs#do_exit` | 同板块同名 | 0i32.to_ne_bytes, Arc::strong_count, FD_CLOEXEC_TABLE.write, FD_CLOEXEC_TABLE.write().clear, FD_TABLE.write, FutexKey::new_current ...(+54) | FD_TABLE.clear, SignalInfo::new, UserPtr::<Pid>::from, axtask::exit, axtask::yield_now, clear_child_tid.get_as_mut ...(+22) |
| `api/src/imp/task/wait.rs#sys_waitpid` | `api/src/imp/task/wait.rs#sys_waitpid` | 同板块同名 | Err, Ok, StarryTaskExt::of, WaitOptions::from_bits, WaitOptions::from_bits(options).ok_or, WaitPid::Pgid ...(+29) | Err, Ok, WaitOptions::from_bits_truncate, WaitPid::Pgid, WaitPid::Pid, child.data::<ProcessData> ...(+25) |
| `arceos/modules/axtask/src/run_queue.rs#exit_current` | `arceos/modules/axtask/src/run_queue.rs#exit_current` | 同板块同名同结构 | EXITED_TASKS.current_ref_mut_raw, EXITED_TASKS.current_ref_mut_raw().clear, EXITED_TASKS.current_ref_mut_raw().push_back, WAIT_FOR_EXIT.current_ref_mut_raw, WAIT_FOR_EXIT.current_ref_mut_raw().notify_one, assert! ...(+8) | EXITED_TASKS.current_ref_mut_raw, EXITED_TASKS.current_ref_mut_raw().clear, EXITED_TASKS.current_ref_mut_raw().push_back, WAIT_FOR_EXIT.current_ref_mut_raw, WAIT_FOR_EXIT.current_ref_mut_raw().notify_one, assert! ...(+8) |
| `arceos/api/arceos_posix_api/src/imp/io_mpx/epoll.rs#sys_epoll_wait` | `arceos/api/arceos_posix_api/src/imp/io_mpx/epoll.rs#sys_epoll_wait` | 同板块同名同结构 | debug!, syscall_body! | debug!, syscall_body! |

#### 任务调度/异步运行时
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `arceos/ulib/axlibc/c/printf.c#format_string_loop` | `arceos/ulib/axlibc/c/printf.c#format_string_loop` | 同板块同名同结构 | ABS_FOR_PRINTING, ADVANCE_IN_FORMAT_STRING, atou_, is_digit_, out_rev_, parse_flags ...(+5) | ABS_FOR_PRINTING, ADVANCE_IN_FORMAT_STRING, atou_, is_digit_, out_rev_, parse_flags ...(+5) |
| `api/src/imp/futex.rs#sys_futex` | `api/src/imp/futex.rs#sys_futex` | 同板块同名 | Err, FutexKey::new_current, Ok, StarryTaskExt::of, StarryTaskExt::of(&curr)
                    .thread_data, StarryTaskExt::of(&curr)
                    .thread_data()
                    .futex_bitset
                    .store ...(+27) | Err, Ok, axtask::yield_now, curr.task_ext, curr.task_ext().process_data, current ...(+16) |
| `arceos/modules/axlog/src/lib.rs#log` | `arceos/modules/axlog/src/lib.rs#log` | 同板块同名同结构 | cfg_if::cfg_if!, record.level, record.line, record.line().unwrap_or, record.metadata, record.target ...(+1) | cfg_if::cfg_if!, record.level, record.line, record.line().unwrap_or, record.metadata, record.target ...(+1) |
| `api/src/imp/fs/stat.rs#sys_statx` | `api/src/imp/fs/stat.rs#sys_statx` | 同板块同名 | Err, Ok, check_parent_search_perm_stat, debug!, nullable!, path.is_null ...(+4) | Err, Ok, debug!, f.stat, f.stat()?.into, get_file_like ...(+9) |

#### 内存管理
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `api/src/imp/task/clone.rs#sys_clone` | `api/src/imp/task/clone.rs#sys_clone` | 同板块同名 | Arc::default, Arc::downgrade, Arc::new, AtomicU32::new, CloneFlags::NEWNS
        .union, CloneFlags::NEWNS
        .union(CloneFlags::NEWUTS)
        .union ...(+123) | Arc::default, Arc::new, CURRENT_DIR
                .deref_from, CURRENT_DIR
                .deref_from(&process_data.ns)
                .init_new, CURRENT_DIR
                .deref_from(&process_data.ns)
                .init_shared, CURRENT_DIR.copy_inner ...(+72) |
| `api/src/imp/mm/mmap.rs#sys_mmap` | `api/src/imp/mm/mmap.rs#sys_mmap` | 同板块同名 | (addr + length).align_up, (offset as usize).align_offset_4k, (starry_config::USER_HEAP_BASE + starry_config::USER_HEAP_SIZE).align_up, Err, File::from_fd, MmapFlags::from_bits_truncate ...(+64) | (addr + length).align_up, Err, File::from_fd, MmapFlags::from_bits_truncate, MmapProt::from_bits_truncate, Ok ...(+27) |
| `core/src/mm.rs#load_user_app` | `core/src/mm.rs#load_user_app` | 同板块同名 | "/bin/sh".to_owned, Arc::new, Box::into_raw, CACHED_ELF
                    .write, CACHED_ELF
                    .write()
                    .insert, CACHED_ELF.read ...(+88) | CStr::from_bytes_with_nul, CStr::from_bytes_with_nul(interp)
                .map_err, CStr::from_bytes_with_nul(interp)
                .map_err(|_| AxError::InvalidData)?
                .to_str, CStr::from_bytes_with_nul(interp)
                .map_err(|_| AxError::InvalidData)?
                .to_str()
                .map_err, ElfFile::new, ElfFile::new(&file_data).map_err ...(+45) |
| `arceos/ulib/axlibc/src/strftime.rs#strftime_inner` | `arceos/ulib/axlibc/src/strftime.rs#strftime_inner` | 同板块同名同结构 | CountingWriter::new, format.offset, inner_strftime, w! | CountingWriter::new, format.offset, inner_strftime, w! |

#### 中断/异常处理
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `arceos/ulib/axlibc/c/pow.c#exp_inline` | `arceos/ulib/axlibc/c/pow.c#exp_inline` | 同板块同名同结构 | __math_oflow, __math_uflow, asdouble, asuint64, converttoint, eval_as_double ...(+4) | __math_oflow, __math_uflow, asdouble, asuint64, converttoint, eval_as_double ...(+4) |
| `arceos/modules/axhal/src/arch/aarch64/trap.rs#handle_sync_exception` | `arceos/modules/axhal/src/arch/aarch64/trap.rs#handle_sync_exception` | 同板块同名同结构 | ESR_EL1.extract, crate::trap::handle_syscall, crate::trap::post_trap_callback, debug!, esr.read, esr.read_as_enum ...(+6) | ESR_EL1.extract, crate::trap::handle_syscall, crate::trap::post_trap_callback, debug!, esr.read, esr.read_as_enum ...(+6) |
| `src/syscall.rs#handle_syscall` | `arceos/modules/axhal/src/arch/x86_64/syscall.rs#handle_syscall` | 同板块同名 | Sysno::new, debug!, err.code, handle_syscall_impl, result.unwrap_or_else, sysno
        .ok_or ...(+4) | crate::trap::handle_syscall |
| `arceos/modules/axhal/src/platform/riscv64_qemu_virt/irq.rs#register_handler` | `arceos/modules/axhal/src/platform/riscv64_qemu_virt/irq.rs#register_handler` | 同板块同名同结构 | with_cause! | with_cause! |

#### 系统调用
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `arceos/api/arceos_posix_api/src/imp/net.rs#sys_getaddrinfo` | `arceos/api/arceos_posix_api/src/imp/net.rs#sys_getaddrinfo` | 同板块同名同结构 | char_ptr_to_str, debug!, syscall_body! | char_ptr_to_str, debug!, syscall_body! |
| `api/src/imp/net/socket.rs#sys_socket` | `arceos/api/arceos_posix_api/src/imp/net.rs#sys_socket` | 同板块同名 | Err, Mutex::new, Ok, Socket::Tcp, Socket::Udp, TcpSocket::new ...(+6) | debug!, syscall_body! |
| `api/src/imp/net/io.rs#sys_sendto` | `arceos/api/arceos_posix_api/src/imp/net.rs#sys_sendto` | 同板块同名 | Ok, Socket::from_fd, UnixAddr::read_from_user, UnixSocket::from_fd, addr.is_null, buf.get_as_slice ...(+9) | debug!, syscall_body! |
| `api/src/imp/net/io.rs#sys_recvfrom` | `arceos/api/arceos_posix_api/src/imp/net.rs#sys_recvfrom` | 同板块同名 | Ok, Socket::from_fd, UnixSocket::from_fd, UserPtr::from, addr.is_null, buf.write_bytes ...(+8) | debug!, syscall_body! |

#### 文件系统
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `arceos/ulib/axlibc/c/glob.c#do_glob` | `arceos/ulib/axlibc/c/glob.c#do_glob` | 同板块同名同结构 | S_ISDIR, append, closedir, do_glob, errfunc, fnmatch ...(+7) | S_ISDIR, append, closedir, do_glob, errfunc, fnmatch ...(+7) |
| `api/src/imp/fs/io.rs#sys_splice` | `api/src/imp/fs/io.rs#sys_splice` | 同板块同名 | 0x4000.min, Err, File::from_fd, Ok, Pipe::from_fd, Pipe::from_fd(fd_in).is_ok ...(+33) | Err, File::from_fd, Ok, Pipe::from_fd, Pipe::from_fd(fd_in).ok, Pipe::from_fd(fd_out).ok ...(+10) |
| `arceos/ulib/axlibc/src/setjmp.rs#longjmp` | `arceos/ulib/axlibc/src/setjmp.rs#longjmp` | 同板块同名同结构 | core::arch::naked_asm! | core::arch::naked_asm! |
| `arceos/ulib/axlibc/src/setjmp.rs#setjmp` | `arceos/ulib/axlibc/src/setjmp.rs#setjmp` | 同板块同名同结构 | core::arch::naked_asm! | core::arch::naked_asm! |

#### 设备驱动
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `arceos/tools/bwbench_client/src/main.rs#transmit_benchmark` | `arceos/tools/bwbench_client/src/main.rs#transmit_benchmark` | 同板块同名同结构 | Local::now, NetDevice::new, NetDevice::new(interface).unwrap, current_time.signed_duration_since, current_time.signed_duration_since(past_time).num_seconds, dev.send ...(+2) | Local::now, NetDevice::new, NetDevice::new(interface).unwrap, current_time.signed_duration_since, current_time.signed_duration_since(past_time).num_seconds, dev.send ...(+2) |
| `arceos/tools/bwbench_client/src/device.rs#bind_interface` | `arceos/tools/bwbench_client/src/device.rs#bind_interface` | 同板块同名同结构 | ETH_P_ALL.to_be, Err, Ok, libc::bind, self.ifreq.ioctl, std::io::Error::last_os_error ...(+2) | ETH_P_ALL.to_be, Err, Ok, libc::bind, self.ifreq.ioctl, std::io::Error::last_os_error ...(+2) |
| `arceos/modules/axdriver/src/lib.rs#init_drivers` | `arceos/modules/axdriver/src/lib.rs#init_drivers` | 同板块同名同结构 | AllDevices::default, all_devs.block.iter, all_devs.block.iter().enumerate, all_devs.display.iter, all_devs.display.iter().enumerate, all_devs.net.iter ...(+5) | AllDevices::default, all_devs.block.iter, all_devs.block.iter().enumerate, all_devs.display.iter, all_devs.display.iter().enumerate, all_devs.net.iter ...(+5) |
| `arceos/modules/axdriver/src/virtio.rs#probe_pci` | `arceos/modules/axdriver/src/virtio.rs#probe_pci` | 同板块同名同结构 | D::try_new, Some, axdriver_virtio::probe_pci_device::<VirtIoHalImpl>, warn! | D::try_new, Some, axdriver_virtio::probe_pci_device::<VirtIoHalImpl>, warn! |

#### 网络协议栈
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `arceos/ulib/axlibc/c/network.c#inet_pton` | `arceos/ulib/axlibc/c/network.c#inet_pton` | 同板块同名同结构 | hexval, inet_pton, isdigit, memmove | hexval, inet_pton, isdigit, memmove |
| `arceos/api/arceos_posix_api/src/imp/net.rs#shutdown` | `arceos/api/arceos_posix_api/src/imp/net.rs#shutdown` | 同板块同名同结构 | Ok, tcpsocket.lock, tcpsocket.peer_addr, tcpsocket.shutdown, udpsocket.lock, udpsocket.peer_addr ...(+1) | Ok, tcpsocket.lock, tcpsocket.peer_addr, tcpsocket.shutdown, udpsocket.lock, udpsocket.peer_addr ...(+1) |
| `arceos/ulib/axlibc/c/socket.c#accept4` | `arceos/ulib/axlibc/c/socket.c#accept4` | 同板块同名同结构 | accept, fcntl | accept, fcntl |
| `arceos/api/arceos_posix_api/src/imp/net.rs#recvfrom` | `arceos/api/arceos_posix_api/src/imp/net.rs#recvfrom` | 同板块同名同结构 | Ok, Some, tcpsocket.lock, tcpsocket.lock().recv, tcpsocket.lock().recv(buf).map, udpsocket
                .lock ...(+2) | Ok, Some, tcpsocket.lock, tcpsocket.lock().recv, tcpsocket.lock().recv(buf).map, udpsocket
                .lock ...(+2) |

#### 文件系统/第三方库
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `arceos/modules/axfs-ng/src/fs/ext4/inode.rs#create` | `arceos/modules/axfs/src/fs/lwext4_rust.rs#create` | 同板块同名 | DirEntry::new_dir, DirEntry::new_file, DirNode::new, Err, FileNode::new, Inode::new ...(+11) | Ok, e.try_into, e.try_into().unwrap, file.check_inode_exist, file.dir_mk, file.dir_mk(fpath)
                    .map ...(+12) |
| `arceos/modules/axfs-ng/src/fs/ext4/inode.rs#read_dir` | `arceos/modules/axfs/src/fs/lwext4_rust.rs#read_dir` | 同板块同名 | Ok, core::str::from_utf8, core::str::from_utf8(entry.name())
                .map_err, core::str::from_utf8(entry.name())
                .map_err(|_| VfsError::EINVAL)?
                .to_owned, entry.ino, entry.inode_type ...(+10) | Ok, VfsDirEntry::new, core::str::from_utf8, core::str::from_utf8(iname.unwrap()).unwrap, dirents.iter_mut, dirents.iter_mut().enumerate ...(+13) |
| `arceos/modules/axfs-ng/src/fs/ext4/inode.rs#rename` | `arceos/modules/axfs/src/fs/lwext4_rust.rs#rename` | 同板块同名 | dst_dir.downcast, dst_dir.downcast().map_err, self.fs
            .lock, self.fs
            .lock()
            .rename, self.fs
            .lock()
            .rename(self.ino, src_name, dst_dir.ino, dst_name)
            .map_err | e.try_into, e.try_into().unwrap, file.file_rename, file.file_rename(src_path, dst_path)
            .map, file.file_rename(src_path, dst_path)
            .map(|_v| ())
            .map_err, self.0.lock |
| `arceos/modules/axfs-ng/src/fs/ext4/inode.rs#read_at` | `arceos/modules/axfs/src/fs/lwext4_rust.rs#read_at` | 同板块同名 | self.fs
            .lock, self.fs
            .lock()
            .read_at, self.fs
            .lock()
            .read_at(self.ino, buf, offset)
            .map_err | <i32 as TryInto<AxError>>::try_into, <i32 as TryInto<AxError>>::try_into(e).unwrap, e.try_into, e.try_into().unwrap, file.file_close, file.file_open ...(+9) |

#### 网络协议栈/第三方库
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `arceos/modules/axnet/src/smoltcp_impl/tcp.rs#connect` | `arceos/modules/axnet/src/smoltcp_impl/tcp.rs#connect` | 同板块同名 | Err, Ok, Ok::<(IpEndpoint, IpEndpoint), AxError>, SOCKET_SET
                .with_socket_mut::<tcp::Socket, _, _>, SOCKET_SET.add, SOCKET_SET.poll_interfaces ...(+31) | Err, Ok, SOCKET_SET
                .with_socket_mut::<tcp::Socket, _, _>, SOCKET_SET.add, SocketSetWrapper::new_tcp_socket, Some ...(+25) |
| `arceos/modules/axnet/src/smoltcp_impl/dns.rs#query` | `arceos/modules/axnet/src/smoltcp_impl/dns.rs#query` | 同板块同名 | Err, Ok, SOCKET_SET
            .with_socket_mut::<dns::Socket, _, _>, SOCKET_SET
            .with_socket_mut::<dns::Socket, _, _>(handle, |socket| {
                socket.start_query(iface.lock().context(), name, query_type)
            })
            .map_err, SOCKET_SET.poll_interfaces, SOCKET_SET.with_socket_mut::<dns::Socket, _, _> ...(+12) | Err, Ok, SOCKET_SET
            .with_socket_mut::<dns::Socket, _, _>, SOCKET_SET
            .with_socket_mut::<dns::Socket, _, _>(handle, |socket| {
                socket.start_query(iface.lock().context(), name, query_type)
            })
            .map_err, SOCKET_SET.poll_interfaces, SOCKET_SET.with_socket_mut::<dns::Socket, _, _> ...(+12) |
| `arceos/modules/axnet/src/smoltcp_impl/tcp.rs#bind` | `arceos/modules/axnet/src/smoltcp_impl/udp.rs#bind` | 同板块同名 | Ok, SOCKET_SET.add, SOCKET_SET.bind_check, SOCKET_SET.with_socket_mut::<tcp::Socket, _, _>, SocketAddr::from, SocketSetWrapper::new_tcp_socket ...(+18) | (!is_unspecified(local_endpoint.addr)).then_some, Ok, SOCKET_SET.with_socket_mut::<udp::Socket, _, _>, Some, ax_err!, debug! ...(+9) |
| `arceos/modules/axnet/src/smoltcp_impl/tcp.rs#poll_connect` | `arceos/modules/axnet/src/smoltcp_impl/tcp.rs#poll_connect` | 同板块同名 | Ok, SOCKET_SET.with_socket::<tcp::Socket, _, _>, debug!, self.handle.get, self.handle.get().read, self.handle.get().read().unwrap ...(+6) | Ok, SOCKET_SET.with_socket::<tcp::Socket, _, _>, debug!, self.handle.get, self.handle.get().read, self.handle.get().read().unwrap ...(+6) |

#### 通用模块/其他
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `arceos/ulib/axlibc/c/printf.c#print_exponential_number` | `arceos/ulib/axlibc/c/printf.c#print_exponential_number` | 同板块同名同结构 | ABS_FOR_PRINTING, PRINTF_ABS, bastardized_floor, get_components, get_normalized_components, get_sign_bit ...(+5) | ABS_FOR_PRINTING, PRINTF_ABS, bastardized_floor, get_components, get_normalized_components, get_sign_bit ...(+5) |
| `arceos/ulib/axlibc/c/pow.c#pow` | `arceos/ulib/axlibc/c/pow.c#pow` | 同板块同名同结构 | __builtin_fma, __math_invalid, __math_oflow, __math_uflow, asdouble, asuint64 ...(+8) | __builtin_fma, __math_invalid, __math_oflow, __math_uflow, asdouble, asuint64 ...(+8) |
| `arceos/ulib/axlibc/c/string.c#twoway_strstr` | `arceos/ulib/axlibc/c/string.c#twoway_strstr` | 同板块同名同结构 | BITOP, MAX, memchr, memcmp | BITOP, MAX, memchr, memcmp |
| `arceos/ulib/axlibc/c/printf.c#print_broken_up_decimal` | `arceos/ulib/axlibc/c/printf.c#print_broken_up_decimal` | 同板块同名同结构 | out_rev_ | out_rev_ |


好的，我将遵循审计指令，以仲裁专家身份，基于客观AST证据与双方陈述，对本案进行最终定性。

### 1. 宏观判定
- **定性结论**: **部分逻辑借鉴与架构改动** (相似度: 46.05%，共同 AST 指纹: 1347；唯一 AST 指纹: 目标项目 2330，参考项目 1942；覆盖率: 目标项目 57.81%，参考项目 69.36%)

本案的核心争议在于如何界定“对上游开源框架的合法复用”与“对参考项目的抄袭”。宏观数据显示，目标项目拥有更多的唯一AST指纹（2330 > 1942），表明其存在大量独立代码。覆盖率指标（目标57.81%，参考69.36%）更符合“目标项目在共享上游基础上进行了扩展和修改”的模式，而非单向抄袭。控方指控的“穿透式抄袭”混淆了位于`arceos/`目录下的上游框架代码与参考项目原创代码，其核心证据效力不足。辩方关于“共同祖先”和“独立架构抽象”的辩护在技术上是成立的。

### 2. 全模块覆盖矩阵解读

- **进程管理**
  1. 双方均存在该模块。
  2. 存在共同AST指纹（40个）。
  3. 目标覆盖率37.38%，参考覆盖率57.14%。覆盖率较低，说明目标项目在该模块有大量独立实现，与参考项目重合度不高。
  4. 证据强度：**弱**。控方列举的“铁证”多为ArceOS上游框架代码或简短包装函数，原创层代码（如`do_exit`）存在显著外部对象差异。

- **任务调度/异步运行时**
  1. 双方均存在该模块。
  2. 存在共同AST指纹（203个）。
  3. 目标覆盖率55.62%，参考覆盖率74.63%。高覆盖率源于双方共享ArceOS的`axtask`核心调度框架。
  4. 证据强度：**弱**。共同指纹集中在`arceos/modules/axtask`等上游目录，属于框架共性，不能作为对参考项目抄袭的证据。

- **内存管理**
  1. 双方均存在该模块。
  2. 存在共同AST指纹（217个）。
  3. 目标覆盖率64.78%，参考覆盖率78.62%。高覆盖率同样源于共享ArceOS的内存管理框架及通用算法。
  4. 证据强度：**弱**。证据显示双方在`sys_clone`、`sys_mmap`等关键系统调用上存在独立实现（如目标项目使用`StarryTaskExt`、`CloneFlags`联合等），并非照搬。

- **中断/异常处理**
  1. 双方均存在该模块。
  2. 存在共同AST指纹（47个）。
  3. 目标覆盖率90.38%，参考覆盖率94.0%。覆盖率极高，因为该模块几乎完全由硬件架构定义，且双方均使用ArceOS的`axhal`硬件抽象层。
  4. 证据强度：**未形成**。这是典型的硬件强相关与上游框架共享代码，不构成抄袭证据。

- **系统调用**
  1. 双方均存在该模块。
  2. 存在共同AST指纹（61个）。
  3. 目标覆盖率67.78%，参考覆盖率62.24%。覆盖率中等，共同部分多为POSIX标准接口的声明或简单分发宏（如`syscall_body!`）。
  4. 证据强度：**弱**。控方证据多为`arceos_posix_api`下的宏调用包装，缺乏实质性业务逻辑。

- **文件系统**
  1. 双方均存在该模块。
  2. 存在共同AST指纹（208个）。
  3. 目标覆盖率31.85%，参考覆盖率44.83%。覆盖率较低，说明双方在文件系统实现上差异巨大。
  4. 证据强度：**弱**。证据显示双方使用了不同的文件系统实现（`axfs-ng` vs `axfs`），底层调用逻辑完全不同。

- **设备驱动**
  1. 双方均存在该模块。
  2. 存在共同AST指纹（121个）。
  3. 目标覆盖率99.18%，参考覆盖率99.18%。覆盖率极高，因为双方都完整复用了ArceOS的设备驱动框架（`axdriver`）和VirtIO实现。
  4. 证据强度：**未形成**。这是对上游框架的完整复用，与参考项目无关。

- **网络协议栈**
  1. 双方均存在该模块。
  2. 存在共同AST指纹（54个）。
  3. 目标覆盖率70.13%，参考覆盖率96.43%。高覆盖率源于共享`axnet`及`smoltcp`协议栈实现。
  4. 证据强度：**未形成**。共同指纹均位于`arceos/modules/axnet`等上游目录，属于第三方库和框架代码。

- **第三方库/公共依赖**
  1. 双方均存在该模块。
  2. 文件系统/第三方库未观察到共同指纹（0%），网络协议栈/第三方库覆盖率52.03%/70.33%。
  3. 覆盖率差异说明双方可能使用了不同版本或配置的第三方库。
  4. 证据强度：**未形成**。第三方库代码的相似性不能作为项目间抄袭的证据。

### 3. 底层 AST 关键证据指纹表

| 所属板块 | 目标项目路径 | 参考项目路径 | 证据类型 | 技术说明 |
| :--- | :--- | :--- | :--- | :--- |
| 进程管理 | `api/src/imp/task/exit.rs#do_exit` | `api/src/imp/task/exit.rs#do_exit` | 强证据（差异） | 目标项目使用`FD_CLOEXEC_TABLE`/`FD_TABLE`分离设计，并通过`StarryTaskExt::of`访问任务扩展；参考项目使用`FD_TABLE.clear`和`curr.task_ext`。两者在文件描述符管理和任务抽象上存在根本性架构差异。 |
| 进程管理 | `api/src/imp/task/wait.rs#sys_waitpid` | `api/src/imp/task/wait.rs#sys_waitpid` | 强证据（差异） | 目标项目使用`WaitOptions::from_bits`（严格校验）和`StarryTaskExt::of`；参考项目使用`WaitOptions::from_bits_truncate`（截断）和`child.data::<ProcessData>`。两者在错误处理和对象访问模型上不同。 |
| 进程管理 | `core/src/task.rs#get_process` | `core/src/task.rs#get_process` | 强证据（差异） | 目标项目引入了`find_linux_init_process_alias`等独立逻辑以兼容Linux init进程别名查找，参考项目无此实现。这是功能层面的独立扩展。 |
| 进程管理 | `api/src/imp/signal.rs#sys_rt_sigprocmask` | `api/src/imp/signal.rs#sys_rt_sigprocmask` | 中证据（差异） | 目标项目通过`StarryTaskExt::of(&current())`获取任务扩展，参考项目通过`current().task_ext()`。这是贯穿整个原创层的系统性抽象差异，非简单重命名。 |
| 进程管理 | `arceos/modules/axtask/src/wait_queue.rs#cancel_events` | `arceos/modules/axtask/src/wait_queue.rs#cancel_events` | 弱证据（上游） | 调用图谱完全一致。该文件属于ArceOS上游任务调度框架，非参考项目原创。双方基于同一上游，相似是必然的。 |
| 进程管理 | `arceos/modules/axtask/src/run_queue.rs#exit_current` | `arceos/modules/axtask/src/run_queue.rs#exit_current` | 弱证据（上游） | 调用图谱完全一致。属于ArceOS上游任务调度框架的标准退出路径实现。 |
| 进程管理 | `arceos/api/arceos_posix_api/src/imp/task.rs#sys_exit` | `arceos/api/arceos_posix_api/src/imp/task.rs#sys_exit` | 弱证据（上游） | 调用图谱完全一致。属于ArceOS上游POSIX API适配层的标准包装，逻辑仅为调用`axtask::exit`。 |
| 进程管理 | `arceos/modules/axhal/src/time.rs#busy_wait_until` | `arceos/modules/axhal/src/time.rs#busy_wait_until` | 弱证据（上游） | 调用图谱完全一致。属于ArceOS上游硬件抽象层的标准忙等待实现，是此类操作的唯一合理写法。 |
| 进程管理 | `arceos/ulib/axlibc/c/pthread.c#pthread_cond_wait` | `arceos/ulib/axlibc/c/pthread.c#pthread_cond_wait` | 弱证据（上游） | 调用图谱完全一致，仅含`unimplemented`宏。属于上游C库的未实现占位符，无任何原创逻辑。 |
| 进程管理 | `core/src/task.rs#set_clear_child_tid` | `core/src/task.rs#set_clear_child_tid` | 弱证据（常规） | 调用图谱完全一致，仅包含单行原子存储操作`self.clear_child_tid.store`。过于简短，缺乏创造性。 |

### 4. 核心代码调用流对比剖析

* **【进程管理 / 任务退出路径】**
* **底层技术析理**: 对比`do_exit`函数，两者在进程退出时的资源清理策略和任务抽象模型上存在本质区别。目标项目采用`FD_CLOEXEC_TABLE`和`FD_TABLE`分离管理文件描述符，并通过`StarryTaskExt::of`这一统一的扩展Trait来访问任务私有数据，体现了更清晰的关注点分离和抽象。参考项目则直接操作`FD_TABLE`并通过`curr.task_ext`内嵌方法访问。此外，目标项目在退出时关注`FutexKey`清理，而参考项目关注`clear_child_tid`，表明两者在同步与子线程管理上的侧重点不同。这些差异不是简单的API重命名，而是反映了不同的设计决策和架构风格。
* **代码溯源链接**:
  - 目标项目: `api/src/imp/task/exit.rs#do_exit`
  - 参考项目: `api/src/imp/task/exit.rs#do_exit`

* **【进程管理 / 等待子进程】**
* **底层技术析理**: 对比`sys_waitpid`函数，两者在等待选项的解析上采用了不同语义的API。目标项目使用`WaitOptions::from_bits(options).ok_or`，对非法标志位采取严格报错策略；参考项目使用`WaitOptions::from_bits_truncate`，对非法位进行静默截断。这体现了不同的健壮性设计哲学。同时，两者获取子进程数据的方式也延续了`StarryTaskExt::of`与`child.data::<ProcessData>`的抽象差异，进一步印证了目标项目在原创层进行了系统性的独立架构设计。
* **代码溯源链接**:
  - 目标项目: `api/src/imp/task/wait.rs#sys_waitpid`
  - 参考项目: `api/src/imp/task/wait.rs#sys_waitpid`

### 5. 单方模块与降权项

- **单方模块**: 无。所有宏观模块在双方项目中均存在。
- **降权项**:
  - **上游框架代码**: 所有位于`arceos/modules/axtask`、`arceos/modules/axhal`、`arceos/api/arceos_posix_api`、`arceos/api/arceos_api`、`arceos/ulib/axlibc`、`arceos/tools/`等路径下的代码，均为ArceOS开源框架或第三方库的组成部分。双方共同复用这些代码导致的AST相似，不能作为指控抄袭的强证据。
  - **简短/常规代码**: 如`set_clear_child_tid`（单行原子存储）、`sys_getpid`（仅含宏）、`pthread_cond_wait`（仅含`unimplemented`）等函数，因其逻辑过于简单或属于标准范式，缺乏足够的创造性来表达独立思想，其相似性证明力极弱。
  - **跨板块证据**: 控方将`arceos/modules/axtask`（任务调度）、`arceos/modules/axhal`（硬件抽象层）等模块的代码强行归入“进程管理”板块进行指控，属于跨板块硬凑，依规降权处理。

### 6. 保守结论

本案整体风险较低。46.05%的宏观AST相似度主要由双方共同依赖的ArceOS开源框架及第三方库贡献，这在基于同一内核框架的衍生项目中是正常现象。控方对“进程管理”板块“穿透式抄袭”的指控，经AST调用图谱深度比对，其核心证据均指向`arceos/`上游目录或过于简短的常规代码，证明力不足。相反，在双方原创的`api/src/imp/`和`core/src/`层，关键系统调用（如`do_exit`、`sys_waitpid`）的AST证据揭示了`StarryTaskExt`与`task_ext`、`FD_CLOEXEC_TABLE`与`FD_TABLE`等系统性的架构抽象差异，以及`WaitOptions`解析策略的不同。这些差异表明目标项目并非对参考项目的简单照搬或伪装，而是在共享上游的基础上进行了独立的架构设计与功能扩展。因此，不能认定构成实质性代码抄袭。

------------------------------

