# 🛡️ OS 内核赛自动化代码级架构审计报告

**审计时间**: `2026-07-02 12:20:43`
**参赛作品名称**: `2026-T202610487999805-XX`
**年份**: `2026`
**学校**: `XX`
**队伍名称**: `T202610487999805`
**比赛名字**: `OS-Competition`
**比赛子赛道**: `Kernel-Implementation`
**仓库地址**: `https://gitlab.eduxiji.net/educg-group-43501-3132633/T202610487999805-1289`
**推测开发语言**: `Rust / C (由AST探针自动识别)`
**特征函数量**: `2279`

## ⚔️ 底层代码同源性对比 (AST Homology)
> 本部分由专门的代码审查智能体，对该项目与符合年份条件的历届最高相似度项目进行控制流与底层调用对比。新版报告会先给出确定性的全模块覆盖矩阵，再给出 Agent 的分模块解释，避免只围绕少量抽样函数展开。

### 🏆 Top 1 底层对标: vs `2025-Chronix-哈尔滨工业大学深圳`（哈尔滨工业大学（深圳） / Chronix）

#### 对方作品信息

**对方年份**: `2025`
**对方学校**: `哈尔滨工业大学（深圳）`
**对方队伍名称**: `Chronix`
**对方比赛名字**: `OS-Competition`
**对方比赛子赛道**: `Kernel-Implementation`
**对方仓库地址**: `https://gitlab.eduxiji.net/educg-group-36002-2710490/T202518123995568-675`

> 双向覆盖率：共同 AST 指纹: 1873；唯一 AST 指纹: 目标项目 1873，参考项目 1873；覆盖率: 目标项目 100.0%，参考项目 100.0%

## 全模块对比总览（确定性证据）

> 总体覆盖率：共同 AST 指纹: 1873；唯一 AST 指纹: 目标项目 1873，参考项目 1873；覆盖率: 目标项目 100.0%，参考项目 100.0%

### 1. 模块覆盖矩阵
| 模块 | 目标函数数 | 参考函数数 | 共同唯一 AST 指纹 | Target 覆盖 | Base 覆盖 | 状态 |
| :--- | ---: | ---: | ---: | ---: | ---: | :--- |
| 进程管理 | 176 | 176 | 158 | 100.0% | 100.0% | 双方存在且有结构重合 |
| 任务调度/异步运行时 | 275 | 275 | 266 | 100.0% | 100.0% | 双方存在且有结构重合 |
| 内存管理 | 562 | 562 | 489 | 100.0% | 100.0% | 双方存在且有结构重合 |
| 中断/异常处理 | 89 | 89 | 58 | 100.0% | 100.0% | 双方存在且有结构重合 |
| 系统调用 | 115 | 115 | 99 | 100.0% | 100.0% | 双方存在且有结构重合 |
| 文件系统 | 430 | 430 | 309 | 100.0% | 100.0% | 双方存在且有结构重合 |
| 设备驱动 | 200 | 200 | 164 | 100.0% | 100.0% | 双方存在且有结构重合 |
| 网络协议栈 | 144 | 144 | 135 | 100.0% | 100.0% | 双方存在且有结构重合 |
| 文件系统/第三方库 | 60 | 60 | 59 | 100.0% | 100.0% | 双方存在且有结构重合 |
| 通用模块/其他 | 228 | 228 | 201 | 100.0% | 100.0% | 双方存在且有结构重合 |

### 2. 分模块证据清单

#### 进程管理
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `os/src/syscall/process.rs#sys_waitpid` | `os/src/syscall/process.rs#sys_waitpid` | 同板块同名同结构 | Err, Ok, PROCESS_GROUP_MANAGER.remove, Some, TASK_MANAGER.remove_task, UserPtrRaw::new ...(+45) | Err, Ok, PROCESS_GROUP_MANAGER.remove, Some, TASK_MANAGER.remove_task, UserPtrRaw::new ...(+45) |
| `os/src/syscall/io.rs#sys_epoll_pwait2` | `os/src/syscall/io.rs#sys_epoll_pwait2` | 同板块同名同结构 | Err, Ok, Select2Futures::new, Some, TimedTaskFuture::new, UserPtrRaw::new ...(+29) | Err, Ok, Select2Futures::new, Some, TimedTaskFuture::new, UserPtrRaw::new ...(+29) |
| `os/src/syscall/process.rs#sys_execve` | `os/src/syscall/process.rs#sys_execve` | 同板块同名同结构 | "/loongarch/musl/busybox".to_string, "/riscv/musl/busybox".to_string, "busybox".to_string, "sh".to_string, AtFlags::AT_FDCWD.bits, AtFlags::empty ...(+52) | "/loongarch/musl/busybox".to_string, "/riscv/musl/busybox".to_string, "busybox".to_string, "sh".to_string, AtFlags::AT_FDCWD.bits, AtFlags::empty ...(+52) |
| `os/src/syscall/io.rs#sys_epoll_pwait` | `os/src/syscall/io.rs#sys_epoll_pwait` | 同板块同名同结构 | Err, Ok, Select2Futures::new, Some, TimeSpec::from_ms, TimedTaskFuture::new ...(+27) | Err, Ok, Select2Futures::new, Some, TimeSpec::from_ms, TimedTaskFuture::new ...(+27) |

#### 任务调度/异步运行时
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `os/src/syscall/io.rs#sys_pselect6` | `os/src/syscall/io.rs#sys_pselect6` | 同板块同名同结构 | (*timeout.to_ref()).into, (*timeout.to_ref()).is_valid, Err, Ok, PollEvents::empty, Select2Futures::new ...(+52) | (*timeout.to_ref()).into, (*timeout.to_ref()).is_valid, Err, Ok, PollEvents::empty, Select2Futures::new ...(+52) |
| `os/src/syscall/net.rs#get` | `os/src/syscall/net.rs#get` | 同板块同名同结构 | 0u8.to_ne_bytes, Err, Ok, SOCKET_SET.with_socket::<smoltcp::socket::tcp::Socket,_,_>, TimeSpec::_as_bytes, UserSliceRaw::new ...(+26) | 0u8.to_ne_bytes, Err, Ok, SOCKET_SET.with_socket::<smoltcp::socket::tcp::Socket,_,_>, TimeSpec::_as_bytes, UserSliceRaw::new ...(+26) |
| `os/src/syscall/io.rs#sys_ppoll` | `os/src/syscall/io.rs#sys_ppoll` | 同板块同名同结构 | Err, Ok, Select2Futures::new, Some, TimedTaskFuture::new, UserPtrRaw::new ...(+37) | Err, Ok, Select2Futures::new, Some, TimedTaskFuture::new, UserPtrRaw::new ...(+37) |
| `os/src/task/signal.rs#check_and_handle` | `os/src/task/signal.rs#check_and_handle` | 同板块同名同结构 | LinuxSigInfo::default, SigActionFlag::from_bits_truncate, UContext::save_current_context, UserPtrRaw::new, UserPtrRaw::new(new_sp as *mut LinuxSigInfo).ensure_write, UserPtrRaw::new(new_sp as *mut LinuxSigInfo).ensure_write(&mut self.get_vm_space().lock()).unwrap ...(+28) | LinuxSigInfo::default, SigActionFlag::from_bits_truncate, UContext::save_current_context, UserPtrRaw::new, UserPtrRaw::new(new_sp as *mut LinuxSigInfo).ensure_write, UserPtrRaw::new(new_sp as *mut LinuxSigInfo).ensure_write(&mut self.get_vm_space().lock()).unwrap ...(+28) |

#### 内存管理
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `os/src/syscall/mod.rs#syscall` | `os/src/syscall/mod.rs#syscall` | 同板块同名同结构 | SendWrapper, SysError::ENOSYS.code, SyscallId::from_repr, UserPtrRaw::new, VirtAddr::from, args[0].into ...(+171) | SendWrapper, SysError::ENOSYS.code, SyscallId::from_repr, UserPtrRaw::new, VirtAddr::from, args[0].into ...(+171) |
| `os/src/syscall/futex.rs#sys_futex` | `os/src/syscall/futex.rs#sys_futex` | 同板块同名同结构 | (uaddr2 as *const _ as usize).into, Err, FutexOp::from, Ok, SumGuard::new, VirtAddr::from ...(+37) | (uaddr2 as *const _ as usize).into, Err, FutexOp::from, Ok, SumGuard::new, VirtAddr::from ...(+37) |
| `os/src/drivers/block/mmc/mod.rs#card_init` | `os/src/drivers/block/mmc/mod.rs#card_init` | 同板块同名同结构 | CID::from, CLKDIV::offset, CMD::data_cmd, CMD::no_data_cmd, CMD::no_data_cmd_no_crc, CMD::no_data_cmd_no_crc(card_idx, 2).with_response_length ...(+38) | CID::from, CLKDIV::offset, CMD::data_cmd, CMD::no_data_cmd, CMD::no_data_cmd_no_crc, CMD::no_data_cmd_no_crc(card_idx, 2).with_response_length ...(+38) |
| `os/src/net/crypto.rs#encode_raw` | `os/src/net/crypto.rs#encode_raw` | 同板块同名同结构 | (&alg_instance).get_key, Aes128::new, AlgType::from_bytes, Err, GenericArray::clone_from_slice, GenericArray::from_slice ...(+31) | (&alg_instance).get_key, Aes128::new, AlgType::from_bytes, Err, GenericArray::clone_from_slice, GenericArray::from_slice ...(+31) |

#### 中断/异常处理
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `hal/src/component/trap/loongarch64/mod.rs#restore` | `hal/src/component/trap/loongarch64/mod.rs#restore` | 同板块同名同结构 | core::arch::asm!, register::euen::read, register::euen::read().fpe, register::euen::set_fpe | core::arch::asm!, register::euen::read, register::euen::read().fpe, register::euen::set_fpe |
| `hal/src/component/trap/loongarch64/mod.rs#save` | `hal/src/component/trap/loongarch64/mod.rs#save` | 同板块同名同结构 | core::arch::asm!, register::euen::read, register::euen::read().fpe, register::euen::set_fpe | core::arch::asm!, register::euen::read, register::euen::read().fpe, register::euen::set_fpe |
| `hal/src/component/trap/riscv64/mod.rs#restore` | `hal/src/component/trap/riscv64/mod.rs#restore` | 同板块同名同结构 | asm! | asm! |
| `hal/src/component/trap/riscv64/mod.rs#save` | `hal/src/component/trap/riscv64/mod.rs#save` | 同板块同名同结构 | asm! | asm! |

#### 系统调用
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `os/src/syscall/net.rs#set` | `os/src/syscall/net.rs#set` | 同板块同名同结构 | <[u8; 4]>::try_from, <[u8; 4]>::try_from(&opt[0..4]).unwrap, Duration::from_secs, Err, Ok, SOCKET_SET.with_socket_mut::<smoltcp::socket::tcp::Socket,_,_> ...(+15) | <[u8; 4]>::try_from, <[u8; 4]>::try_from(&opt[0..4]).unwrap, Duration::from_secs, Err, Ok, SOCKET_SET.with_socket_mut::<smoltcp::socket::tcp::Socket,_,_> ...(+15) |
| `os/src/syscall/net.rs#try_from` | `os/src/syscall/net.rs#try_from` | 同板块同名同结构 | Err, Ok, log::warn! | Err, Ok, log::warn! |
| `os/src/syscall/net.rs#set` | `os/src/syscall/net.rs#set` | 同板块同名同结构 | <[u8; 4]>::try_from, <[u8; 4]>::try_from(&opt[0..4]).unwrap, Err, Ok, SOCKET_SET.with_socket_mut::<smoltcp::socket::tcp::Socket,_,_>, String::from_utf8 ...(+8) | <[u8; 4]>::try_from, <[u8; 4]>::try_from(&opt[0..4]).unwrap, Err, Ok, SOCKET_SET.with_socket_mut::<smoltcp::socket::tcp::Socket,_,_>, String::from_utf8 ...(+8) |
| `os/src/syscall/net.rs#set` | `os/src/syscall/net.rs#set` | 同板块同名同结构 | <[u8; 1]>::try_from, <[u8; 1]>::try_from(&opt[0..1]).unwrap, Err, Ok, u8::from_be_bytes, udp.set_ttl | <[u8; 1]>::try_from, <[u8; 1]>::try_from(&opt[0..1]).unwrap, Err, Ok, u8::from_be_bytes, udp.set_ttl |

#### 文件系统
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `os/src/net/raw.rs#send` | `os/src/net/raw.rs#send` | 同板块同名同结构 | 0u16.to_be_bytes, Err, Ok, SOCKET_SET.poll_interfaces, SOCKET_SET.with_socket_mut::<smoltcp::socket::raw::Socket, _, _>, [ipv4_header.as_ref(), data].concat ...(+25) | 0u16.to_be_bytes, Err, Ok, SOCKET_SET.poll_interfaces, SOCKET_SET.with_socket_mut::<smoltcp::socket::raw::Socket, _, _>, [ipv4_header.as_ref(), data].concat ...(+25) |
| `os/src/fs/devfs/mod.rs#init_devfs` | `os/src/fs/devfs/mod.rs#init_devfs` | 同板块同名同结构 | CpuDmaLatencyInode::new, DCACHE.lock, DCACHE.lock().insert, LoopDevDentry::new, LoopDevInode::new, NullDentry::new ...(+43) | CpuDmaLatencyInode::new, DCACHE.lock, DCACHE.lock().insert, LoopDevDentry::new, LoopDevInode::new, NullDentry::new ...(+43) |
| `os/src/net/crypto.rs#encode` | `os/src/net/crypto.rs#encode` | 同板块同名同结构 | AlgType::from_bytes, CmsgLevel::from, CmsgType::parse, Err, Ok, Vec::new ...(+20) | AlgType::from_bytes, CmsgLevel::from, CmsgType::parse, Err, Ok, Vec::new ...(+20) |
| `os/src/mm/vm/uvm.rs#load_dl_interp_if_needed` | `os/src/mm/vm/uvm.rs#load_dl_interp_if_needed` | 同板块同名同结构 | "/lib/libc.so".to_string, Constant::DL_INTERP_OFFSET.into, Err, FileReader::new, FileReader::new(interp_file.clone()).map_err, Ok ...(+32) | "/lib/libc.so".to_string, Constant::DL_INTERP_OFFSET.into, Err, FileReader::new, FileReader::new(interp_file.clone()).map_err, Ok ...(+32) |

#### 设备驱动
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `os/src/devices/serial.rs#get_serial` | `os/src/devices/serial.rs#get_serial` | 同板块同名同结构 | Box::new, Serial::new, Uart::new, log::info!, panic!, reg.size.unwrap ...(+21) | Box::new, Serial::new, Uart::new, log::info!, panic!, reg.size.unwrap ...(+21) |
| `os/src/drivers/serial/uart.rs#init_u32` | `os/src/drivers/serial/uart.rs#init_u32` | 同板块同名同结构 | log::info!, reg.byte_add, reg.byte_add(0 << self.reg_shift)
                .write_volatile, reg.byte_add(1 << self.reg_shift)
                .write_volatile, reg.byte_add(FCR << self.reg_shift).write_volatile, reg.byte_add(IER << self.reg_shift).write_volatile ...(+2) | log::info!, reg.byte_add, reg.byte_add(0 << self.reg_shift)
                .write_volatile, reg.byte_add(1 << self.reg_shift)
                .write_volatile, reg.byte_add(FCR << self.reg_shift).write_volatile, reg.byte_add(IER << self.reg_shift).write_volatile ...(+2) |
| `os/src/net/mod.rs#init_network` | `os/src/net/mod.rs#init_network` | 同板块同名同结构 | "".parse, "".parse().unwrap, "127.0.0.1".parse, "127.0.0.1".parse().unwrap, ETH0.call_once, EthernetAddress ...(+15) | "".parse, "".parse().unwrap, "127.0.0.1".parse, "127.0.0.1".parse().unwrap, ETH0.call_once, EthernetAddress ...(+15) |
| `os/src/devices/serial.rs#scan_char_device` | `os/src/devices/serial.rs#scan_char_device` | 同板块同名同结构 | Arc::new, bytes.iter, chosen
        .properties, chosen
        .properties()
        .find, chosen
        .properties()
        .find(|n| n.name == "stdout-path")
        .and_then, chosen
        .properties()
        .find(|n| n.name == "stdout-path")
        .and_then(|n| {
            let bytes = unsafe {
                core::slice::from_raw_parts_mut((n.value.as_ptr()) as *mut u8, n.value.len())
            };
            let mut len = 0;
            for byte in bytes.iter() {
                if *byte == b':' {
                    return core::str::from_utf8(&n.value[..len]).ok();
                }
                len += 1;
            }
            core::str::from_utf8(&n.value[..n.value.len() - 1]).ok()
        })
        .unwrap ...(+13) | Arc::new, bytes.iter, chosen
        .properties, chosen
        .properties()
        .find, chosen
        .properties()
        .find(|n| n.name == "stdout-path")
        .and_then, chosen
        .properties()
        .find(|n| n.name == "stdout-path")
        .and_then(|n| {
            let bytes = unsafe {
                core::slice::from_raw_parts_mut((n.value.as_ptr()) as *mut u8, n.value.len())
            };
            let mut len = 0;
            for byte in bytes.iter() {
                if *byte == b':' {
                    return core::str::from_utf8(&n.value[..len]).ok();
                }
                len += 1;
            }
            core::str::from_utf8(&n.value[..n.value.len() - 1]).ok()
        })
        .unwrap ...(+13) |

#### 网络协议栈
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `os/src/net/tcp.rs#recv` | `os/src/net/tcp.rs#recv` | 同板块同名同结构 | Err, Ok, SOCKET_SET.with_socket_mut::<tcp::Socket,_,_>, get_current_time_duration, get_waker, info! ...(+15) | Err, Ok, SOCKET_SET.with_socket_mut::<tcp::Socket,_,_>, get_current_time_duration, get_waker, info! ...(+15) |
| `os/src/net/udp.rs#send_to` | `os/src/net/udp.rs#send_to` | 同板块同名同结构 | Err, Ok, SOCKET_SET.with_socket_mut::<smoltcp::socket::udp::Socket,_,_>, data.len, get_waker, log::info! ...(+11) | Err, Ok, SOCKET_SET.with_socket_mut::<smoltcp::socket::udp::Socket,_,_>, data.len, get_waker, log::info! ...(+11) |
| `user/src/bin/tcp.rs#server` | `user/src/bin/tcp.rs#server` | 同板块同名同结构 | TEST_ADDR.to_be, TEST_PORT.to_be, accept, bind, buf.as_mut_slice, close ...(+11) | TEST_ADDR.to_be, TEST_PORT.to_be, accept, bind, buf.as_mut_slice, close ...(+11) |
| `os/src/net/tcp.rs#send` | `os/src/net/tcp.rs#send` | 同板块同名同结构 | Duration::from_millis, Err, Ok, SOCKET_SET.poll_interfaces, SOCKET_SET.with_socket_mut::<tcp::Socket,_,_>, get_waker ...(+15) | Duration::from_millis, Err, Ok, SOCKET_SET.poll_interfaces, SOCKET_SET.with_socket_mut::<tcp::Socket,_,_>, get_waker ...(+15) |

#### 文件系统/第三方库
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `os/src/fs/ext4/inode.rs#getxattr` | `os/src/fs/ext4/inode.rs#getxattr` | 同板块同名同结构 | XstatMask::from_bits_truncate, cache.end, cmp::max, file.file_close, file.file_close().expect, file.get_type ...(+10) | XstatMask::from_bits_truncate, cache.end, cmp::max, file.file_close, file.file_close().expect, file.get_type ...(+10) |
| `os/src/fs/ext4/inode.rs#cache_read_at` | `os/src/fs/ext4/inode.rs#cache_read_at` | 同板块同名同结构 | Arc::get_mut, Arc::get_mut(&mut page).unwrap, Arc::get_mut(&mut page).unwrap()
                    .read_from, Ok, Page::new, buf.len ...(+16) | Arc::get_mut, Arc::get_mut(&mut page).unwrap, Arc::get_mut(&mut page).unwrap()
                    .read_from, Ok, Page::new, buf.len ...(+16) |
| `os/src/fs/ext4/inode.rs#cache_write_at` | `os/src/fs/ext4/inode.rs#cache_write_at` | 同板块同名同结构 | Arc::get_mut, Arc::get_mut(&mut page).unwrap, Arc::get_mut(&mut page).unwrap().read_from, Ok, Page::new, buf.len ...(+14) | Arc::get_mut, Arc::get_mut(&mut page).unwrap, Arc::get_mut(&mut page).unwrap().read_from, Ok, Page::new, buf.len ...(+14) |
| `os/src/fs/ext4/dentry.rs#load_child_dentry` | `os/src/fs/ext4/dentry.rs#load_child_dentry` | 同板块同名同结构 | DCACHE.lock, DCACHE.lock().insert, Ok, Some, Vec::new, child.clone ...(+19) | DCACHE.lock, DCACHE.lock().insert, Ok, Some, Vec::new, child.clone ...(+19) |

#### 通用模块/其他
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `os/src/main.rs#pre_main` | `os/src/main.rs#pre_main` | 同板块同名同结构 | naked_asm! | naked_asm! |
| `os/src/timer/timer.rs#check` | `os/src/timer/timer.rs#check` | 同板块同名同结构 | Reverse, drop, get_current_time_duration, log::trace!, self.timers.lock, self.timers.lock().push ...(+4) | Reverse, drop, get_current_time_duration, log::trace!, self.timers.lock, self.timers.lock().push ...(+4) |
| `utils/range-map/src/lib.rs#find_free_range` | `utils/range-map/src/lib.rs#find_free_range` | 同板块同名同结构 | Some, debug_assert!, self.0.range, self.0.range(..=start).next_back, start.min | Some, debug_assert!, self.0.range, self.0.range(..=start).next_back, start.min |
| `utils/segment-tree/src/lib.rs#intersect` | `utils/segment-tree/src/lib.rs#intersect` | 同板块同名同结构 | op_range.clone, range.end.min, range.start.max, self.left.as_ref, self.left.as_ref().unwrap, self.left.as_ref().unwrap().intersect ...(+3) | op_range.clone, range.end.min, range.start.max, self.left.as_ref, self.left.as_ref().unwrap, self.left.as_ref().unwrap().intersect ...(+3) |


### 1. 宏观判定
- **定性结论**: 架构高度同构 / 代码直接复用 (相似度: 100.0%，共同 AST 指纹: 1873；唯一 AST 指纹: 目标项目 1873，参考项目 1873；覆盖率: 目标项目 100.0%，参考项目 100.0%)

### 2. 全模块覆盖矩阵解读
- **进程管理**: 双方均存在该模块。存在大量共同 AST 指纹。目标与参考覆盖率均为 100.0%，说明双方在该模块的函数级 AST 结构完全重合。证据强度：**强**。
- **任务调度/异步运行时**: 双方均存在该模块。存在大量共同 AST 指纹。目标与参考覆盖率均为 100.0%，说明双方在该模块的函数级 AST 结构完全重合。证据强度：**强**。
- **内存管理**: 双方均存在该模块。存在大量共同 AST 指纹。目标与参考覆盖率均为 100.0%，说明双方在该模块的函数级 AST 结构完全重合。证据强度：**强**。
- **中断/异常处理**: 双方均存在该模块。存在共同 AST 指纹。目标与参考覆盖率均为 100.0%，说明双方在该模块的函数级 AST 结构完全重合。证据强度：**强**。
- **系统调用**: 双方均存在该模块。存在大量共同 AST 指纹。目标与参考覆盖率均为 100.0%，说明双方在该模块的函数级 AST 结构完全重合。证据强度：**强**。
- **文件系统**: 双方均存在该模块。存在大量共同 AST 指纹。目标与参考覆盖率均为 100.0%，说明双方在该模块的函数级 AST 结构完全重合。证据强度：**强**。
- **设备驱动**: 双方均存在该模块。存在共同 AST 指纹。目标与参考覆盖率均为 100.0%，说明双方在该模块的函数级 AST 结构完全重合。证据强度：**强**。
- **网络协议栈**: 双方均存在该模块。存在共同 AST 指纹。目标与参考覆盖率均为 100.0%，说明双方在该模块的函数级 AST 结构完全重合。证据强度：**强**。
- **文件系统/第三方库**: 双方均存在该模块。存在共同 AST 指纹。目标与参考覆盖率均为 100.0%，说明双方在该模块的函数级 AST 结构完全重合。证据强度：**中**（因第三方库代码本身具有公共性）。
- **通用模块/其他**: 双方均存在该模块。存在共同 AST 指纹。目标与参考覆盖率均为 100.0%，说明双方在该模块的函数级 AST 结构完全重合。证据强度：**强**。

### 3. 底层 AST 关键证据指纹表
| 所属板块 | 目标项目路径 | 参考项目路径 | 证据类型 | 技术说明 |
| :--- | :--- | :--- | :--- | :--- |
| 进程管理 | `os/src/syscall/process.rs#sys_waitpid` | `os/src/syscall/process.rs#sys_waitpid` | 同板块同名同结构 | 用户态指针写入的链式调用 `UserPtrRaw::new(exit_code_ptr as *mut i32).ensure_write(vm.deref_mut()).ok_or` 完全一致，是高度特异性的代码指纹。 |
| 进程管理 | `os/src/syscall/process.rs#sys_clone` | `os/src/syscall/process.rs#sys_clone` | 同板块同名同结构 | `CloneFlags::from_bits(flags & !0xff).ok_or` 与后续 `child_tid`/`parent_tid` 的 `UserPtrRaw` 链式写入逻辑完全一致。 |
| 进程管理 | `os/src/task/task.rs#exit_robust_list` | `os/src/task/task.rs#exit_robust_list` | 同板块同名同结构 | 针对 robust futex 的裸指针偏移计算 `(entry.cast::<u8>() + futex_offset).cast` 与 `(pending.cast::<u8>() + futex_offset).cast` 完全一致。 |
| 进程管理 | `os/src/task/task.rs#handle_zombie` | `os/src/task/task.rs#handle_zombie` | 同板块同名同结构 | 僵尸进程回收逻辑 `assert!(child.is_zombie)` -> `child.parent.lock` -> `children.clear` -> `TASK_MANAGER.remove_task` 调用序列完全一致。 |
| 任务调度/异步运行时 | `os/src/executor/mod.rs#spawn` | `os/src/executor/mod.rs#spawn` | 同板块同名同结构 | 任务派发逻辑 `<Arc<TaskControlBlock> as Clone>::clone(&(&future.task.clone())).turn_cpu_mask_id` 及后续 `PROCESSORS` 操作序列完全一致。 |
| 任务调度/异步运行时 | `os/src/processor/schedule.rs#load_balance` | `os/src/processor/schedule.rs#load_balance` | 同板块同名同结构 | CFS 负载均衡算法中 `loads.iter().max_by_key(|(_, l)| l).unwrap` 和 `min_by_key` 的调用模式完全一致。 |
| 内存管理 | `os/src/syscall/mm.rs#sys_process_vm_writev` | `os/src/syscall/mm.rs#sys_process_vm_writev` | 同板块同名同结构 | 跨进程内存写入的 `UserSliceRaw::new(iov.base as *const u8, iov.len).ensure_read(&mut caller_task.get_vm_space().lock()).ok_or` 链式调用完全一致。 |
| 内存管理 | `os/src/syscall/mm.rs#sys_process_vm_readv` | `os/src/syscall/mm.rs#sys_process_vm_readv` | 同板块同名同结构 | 跨进程内存读取的 `UserSliceRaw::new(iov.base as *const u8, iov.len).ensure_read(&mut target_task.get_vm_space().lock()).ok_or` 链式调用完全一致。 |
| 中断/异常处理 | `hal/src/component/trap/loongarch64/mod.rs#save` | `hal/src/component/trap/loongarch64/mod.rs#save` | 同板块同名同结构 | 龙芯架构下涉及 FPU 状态保存的 `register::euen::read().fpe` 和 `register::euen::set_fpe` 调用序列完全一致。 |
| 系统调用 | `os/src/syscall/signal.rs#sys_rt_sigprocmask` | `os/src/syscall/signal.rs#sys_rt_sigprocmask` | 同板块同名同结构 | 信号掩码操作的深层嵌套错误处理 `SigSet::from_bits(*UserPtrRaw::new(set).ensure_read(...).ok_or(SysError::EINVAL)?.to_ref() as usize).ok_or` 完全一致。 |
| 文件系统 | `os/src/fs/procfs/meminfo.rs#serialize` | `os/src/fs/procfs/meminfo.rs#serialize` | 同板块同名同结构 | `/proc/meminfo` 的字符串序列 `"MemTotal:\t".to_string` 等 8 个以上特定字面量的创建顺序完全一致。 |
| 文件系统 | `os/src/fs/procfs/selfdir/exe.rs#getattr` | `os/src/fs/procfs/selfdir/exe.rs#getattr` | 同板块同名同结构 | inode 属性填充顺序 `inner.atime` -> `inner.ctime` -> `inner.mode().bits` -> `inner.mtime` -> `inner.nlink` -> `inner.size` 完全一致。 |
| 设备驱动 | `os/src/devices/serial.rs#get_serial` | `os/src/devices/serial.rs#get_serial` | 同板块同名同结构 | 串口设备初始化流程 `Box::new(Serial::new(Uart::new(...)))` 及错误处理 `reg.size.unwrap` 调用完全一致。 |
| 网络协议栈 | `os/src/net/tcp.rs#send` | `os/src/net/tcp.rs#send` | 同板块同名同结构 | TCP 发送路径中 `SOCKET_SET.poll_interfaces` -> `SOCKET_SET.with_socket_mut::<tcp::Socket,_,_>` -> `get_waker` 的调用序列完全一致。 |
| 文件系统/第三方库 | `os/src/fs/ext4/inode.rs#cache_read_at` | `os/src/fs/ext4/inode.rs#cache_read_at` | 同板块同名同结构 | ext4 页缓存读取逻辑 `Arc::get_mut(&mut page).unwrap().read_from` 的调用模式完全一致。 |

### 4. 核心代码调用流对比剖析
* **【进程管理/进程创建与退出】**
* **底层技术析理**: 双方在进程创建 (`sys_clone`) 和退出 (`do_exit`, `handle_zombie`) 的调用流上表现出完全的同构性。`sys_clone` 中，两者均采用 `CloneFlags::from_bits(flags & !0xff).ok_or` 解析标志位，随后通过 `UserPtrRaw::new(child_tid.0 as *mut u32).ensure_write(&mut task.get_vm_space().lock()).ok_or` 这一高度特异性的链式调用来处理用户态指针。在进程退出时，`do_exit` 遍历子进程并修改其父进程的逻辑 (`child.parent.lock`, `children.clear`) 以及 `handle_zombie` 中通过 `assert!(child.is_zombie)` 断言后调用 `TASK_MANAGER.remove_task` 进行回收的序列完全一致。这不仅是算法层面的相似，更是对同一套内部抽象 (`UserPtrRaw`, `TASK_MANAGER`) 的相同调用编排。
* **代码溯源链接**:
  - 目标项目: `os/src/syscall/process.rs#sys_clone`, `os/src/task/task.rs#do_exit`, `os/src/task/task.rs#handle_zombie`
  - 参考项目: `os/src/syscall/process.rs#sys_clone`, `os/src/task/task.rs#do_exit`, `os/src/task/task.rs#handle_zombie`

* **【任务调度/异步运行时】**
* **底层技术析理**: 在任务派发 (`spawn`) 和负载均衡 (`load_balance`) 上，双方调用流完全一致。`spawn` 函数中，两者均使用了 `<Arc<TaskControlBlock> as Clone>::clone(&(&future.task.clone())).turn_cpu_mask_id` 这种冗长且特定的强制类型转换与链式调用，随后根据 CPU 亲和性通过 `PROCESSORS[cpu_mask_id].unwrap_with_mut_task_queue` 或 `PROCESSORS[crate::processor::schedule::select_run_queue_index()].unwrap_with_mut_task_queue` 将任务推入队列。`load_balance` 中，两者均通过 `loads.iter().max_by_key(|(_, l)| l).unwrap` 和 `min_by_key` 寻找最忙/最闲 CPU，其 CFS 负载计算逻辑 (`update_task_load` 中的 `_add_segment`, `decay_load`) 也完全一致。
* **代码溯源链接**:
  - 目标项目: `os/src/executor/mod.rs#spawn`, `os/src/processor/schedule.rs#load_balance`
  - 参考项目: `os/src/executor/mod.rs#spawn`, `os/src/processor/schedule.rs#load_balance`

* **【文件系统/procfs 伪文件系统】**
* **底层技术析理**: 在 `/proc` 文件系统的实现上，双方在 `meminfo` 的 `serialize` 函数中，连 `"MemTotal:\t".to_string` 等 8 个以上的特定字符串字面量创建顺序都与参考项目一致。在 `exe.rs` 和 `fd.rs` 的 `getattr` 函数中，对 inode 属性的提取逻辑 `XstatMask::from_bits_truncate` -> `inner.atime` -> `inner.ctime` -> `inner.mode().bits` -> `inner.mtime` -> `inner.nlink` -> `inner.size` 形成了完全相同的 AST 序列。这种属性排列顺序的完全一致，是典型的复制粘贴遗留特征。
* **代码溯源链接**:
  - 目标项目: `os/src/fs/procfs/meminfo.rs#serialize`, `os/src/fs/procfs/selfdir/exe.rs#getattr`
  - 参考项目: `os/src/fs/procfs/meminfo.rs#serialize`, `os/src/fs/procfs/selfdir/exe.rs#getattr`

### 5. 单方模块与降权项
- **第三方库/公共依赖**: `os/src/fs/ext4/` 下的部分代码（如 `inode.rs`, `dentry.rs`）与 `utils/` 下的 `range-map`、`segment-tree` 等模块，其函数签名和调用模式高度一致。虽然这些模块可能源自公共的 ext4 实现参考或通用数据结构库，但在两个项目中以完全相同的 AST 指纹出现，仍构成强同源信号。然而，考虑到第三方库代码的公共属性，其作为独立抄袭指控的证据强度应适当降权。
- **跨板块同名函数**: 如 `getattr`、`getxattr`、`serialize`、`load_child_dentry` 等函数在 `procfs` 和 `ext4` 等多个文件系统子模块中出现，且 AST 指纹相同。这源于它们实现了统一的文件系统 trait 接口，属于接口契约导致的同构，不能直接作为跨模块抄袭的证据，但其在各自板块内部的完全一致性仍可作为该板块内的强证据。

### 6. 保守结论
本次审计发现，目标项目与参考项目在全部 10 个核心模块中存在 100% 的 AST 指纹覆盖率，证据显示双方在系统调用、进程管理、任务调度、文件系统等关键路径上的调用图谱完全一致，达到了“像素级”复用程度。然而，必须指出证据底座存在严重缺陷：证据抽取 API 返回 401 未授权错误，导致无法验证所提供证据是否真实源自两个独立代码库，且 100% 覆盖率指标与提供的唯一指纹数据存在逻辑矛盾。因此，虽然现有材料强烈指向代码直接复用，但鉴于证据链的完整性和可靠性存疑，无法在程序上做出终局性抄袭判定。建议在修复证据抽取工具并排除共同上游基线后，进行源码级逐行比对，以获取更具法律效力的结论。

------------------------------

