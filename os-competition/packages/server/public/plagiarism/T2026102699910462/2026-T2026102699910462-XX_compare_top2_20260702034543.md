# 🛡️ OS 内核赛自动化代码级架构审计报告

**审计时间**: `2026-07-02 03:45:43`
**参赛作品名称**: `2026-T2026102699910462-XX`
**年份**: `2026`
**学校**: `XX`
**队伍名称**: `T2026102699910462`
**比赛名字**: `OS-Competition`
**比赛子赛道**: `Kernel-Implementation`
**仓库地址**: `https://gitlab.eduxiji.net/educg-group-43501-3132633/T2026102699910462-2452`
**推测开发语言**: `Rust / C (由AST探针自动识别)`
**特征函数量**: `2495`

## ⚔️ 底层代码同源性对比 (AST Homology)
> 本部分由专门的代码审查智能体，对该项目与符合年份条件的历届最高相似度项目进行控制流与底层调用对比。新版报告会先给出确定性的全模块覆盖矩阵，再给出 Agent 的分模块解释，避免只围绕少量抽样函数展开。

### 🏆 Top 2 底层对标: vs `2024-萌新-南开大学`（南开大学 / 萌新）

#### 对方作品信息

**对方年份**: `2024`
**对方学校**: `南开大学`
**对方队伍名称**: `萌新`
**对方比赛名字**: `OS-Competition`
**对方比赛子赛道**: `Kernel-Implementation`
**对方仓库地址**: `https://gitlab.eduxiji.net/educg-group-26010-2376550/T202410055992606-2876`

> 双向覆盖率：共同 AST 指纹: 39；唯一 AST 指纹: 目标项目 2114，参考项目 2483；覆盖率: 目标项目 1.84%，参考项目 1.57%

## 全模块对比总览（确定性证据）

> 总体覆盖率：共同 AST 指纹: 39；唯一 AST 指纹: 目标项目 2114，参考项目 2483；覆盖率: 目标项目 1.84%，参考项目 1.57%

### 1. 模块覆盖矩阵
| 模块 | 目标函数数 | 参考函数数 | 共同唯一 AST 指纹 | Target 覆盖 | Base 覆盖 | 状态 |
| :--- | ---: | ---: | ---: | ---: | ---: | :--- |
| 进程管理 | 173 | 39 | 1 | 0.65% | 3.12% | 双方存在且有结构重合 |
| 任务调度/异步运行时 | 347 | 272 | 1 | 0.31% | 0.44% | 双方存在且有结构重合 |
| 内存管理 | 938 | 174 | 2 | 0.25% | 1.2% | 双方存在且有结构重合 |
| 中断/异常处理 | 96 | 29 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |
| 系统调用 | 29 | 67 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |
| 文件系统 | 348 | 285 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |
| 设备驱动 | 123 | 42 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |
| 网络协议栈 | 7 | 0 | 0 | 0.0% | 0.0% | 仅目标项目体现 |
| 文件系统/第三方库 | 39 | 730 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |
| 网络协议栈/第三方库 | 0 | 118 | 0 | 0.0% | 0.0% | 仅参考项目体现 |
| 第三方库 | 0 | 1374 | 0 | 0.0% | 0.0% | 仅参考项目体现 |
| 通用模块/其他 | 395 | 106 | 2 | 0.55% | 2.27% | 双方存在且有结构重合 |

### 2. 分模块证据清单

#### 进程管理
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `kernel/src/process.rs#exit` | `modules/axtask/src/api.rs#exit` | 同板块同名 | Ok, self.exit_status.store, self.exited.complete_all, self.lifecycle
            .compare_exchange, self.lifecycle
            .compare_exchange(
                THREAD_RUNNING,
                THREAD_EXITING,
                Ordering::AcqRel,
                Ordering::Acquire,
            )
            .map_err, self.lifecycle.store | RUN_QUEUE.lock, RUN_QUEUE.lock().exit_current |
| `kernel/src/process.rs#close_on_exec` | `modules/axtask/src/fd_manager.rs#close_on_exec` | 同板块同名 | Ok, Vec::new, drop, files
            .try_reserve, files
            .try_reserve(PROCESS_MAX_FDS)
            .map_err, self.table.lock ...(+1) | Arc::new, Mutex::new, OpenFlags::empty, Some, f.get_status, f.get_status().is_close_on_exec ...(+7) |
| `kernel/src/process.rs#set_parent` | `modules/axtask/src/task.rs#set_parent` | 同板块同名 | Ok, Some, self.relations.lock | self.parent.store |
| `kernel/src/process.rs#set_close_on_exec` | `modules/axtask/src/stdio.rs#set_close_on_exec` | 同板块同名 | self.table.lock, self.table.lock().set_close_on_exec | self.flags.lock |

#### 任务调度/异步运行时
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `kernel/src/tty.rs#ioctl` | `modules/axtask/src/stdio.rs#ioctl` | 同板块同名 | (pgrp as i32).to_ne_bytes, CONSOLE_TTY.lock, Err, KernelTermios::from_tty, KernelTermios::zeroed, KernelWinsize::zeroed ...(+17) | ConsoleWinSize::default, Err, Ok, warn! |
| `kernel/src/tracked_spin.rs#try_lock` | `modules/axtask/src/sync/mutex.rs#try_lock` | 同板块同名 | IrqSaveGuard::new, LockInstanceId::of, MigrationGuard::new, Some, cpu.get, crate::context::assert_interrupts_enabled ...(+8) | Some, current, current().inner.lock, current().inner.lock().id, current().inner.lock().id().as_u64, self
            .owner_id
            .compare_exchange ...(+2) |
| `kernel/src/tracked_spin.rs#lock` | `modules/axtask/src/sync/mutex.rs#lock` | 同板块同名 | IrqSaveGuard::new, LockInstanceId::of, MigrationGuard::new, Some, cpu.get, crate::context::assert_interrupts_enabled ...(+8) | assert_ne!, current, current().tid, panic!, self.data.get, self.owner_id.compare_exchange_weak |
| `kernel/src/fs/mod.rs#seek` | `modules/axtask/src/stdio.rs#seek` | 同板块同名 | Err, Some, d.len, data.len, file.seek_position, generator.generate ...(+3) | Err |

#### 内存管理
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `kernel/src/virtio.rs#dma_alloc` | `modules/axdriver/src/virtio.rs#dma_alloc` | 同板块同名 | DMA_ALLOCATIONS.lock, NonNull::dangling, NonNull::new, PageAllocationOptions::dma32_zeroed, allocation.range, allocation.range().start ...(+16) | NonNull::dangling, NonNull::new, NonNull::new(vaddr as _).unwrap, global_allocator, global_allocator().alloc_pages, paddr.as_usize ...(+2) |
| `kernel/src/user.rs#exec` | `modules/axtask/src/task.rs#exec` | 同板块同名 | Ok, VirtAddr::new, VirtRange::from_bounds, VmArea::new, VmAreaFlags::user_rw, crate::elf::build_static_exec ...(+4) | (axconfig::SIGNAL_TRAMPOLINE).into, (start_signal_trampoline as usize).into, Arc::new, Arc::strong_count, Err, FutexRobustList::default ...(+38) |
| `kernel/src/virtio.rs#dma_dealloc` | `modules/axdriver/src/ixgbe.rs#dma_dealloc` | 同板块同名 | DMA_ALLOCATIONS.lock, allocations
                .iter, allocations
                .iter()
                .position, allocations.swap_remove, compiler_fence, crate::println! ...(+2) | Layout::from_size_align, Layout::from_size_align(size, 8).unwrap, global_allocator, global_allocator().dealloc |
| `kernel/src/fault.rs#handle_page_fault` | `modules/axmem/src/area.rs#handle_page_fault` | 同板块同名 | fault.source, panic!, record_fault | PhysPage::alloc, PhysPage::alloc().expect, SeekFrom::Current, Some, addr.align_down_4k, addr.align_down_4k().into ...(+17) |

#### 中断/异常处理
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `kernel/src/trap.rs#handle_breakpoint` | `modules/axtrap/src/arch/riscv/mod.rs#handle_breakpoint` | 同板块同名 | crate::user::handle_exception, frame.advance_pc, frame.exception_code, frame.previous_mode_was_user, mark_breakpoint_reached | axlog::debug! |
| `kernel/src/user.rs#sys_clone3` | `modules/axhal/src/platform/VisionFive2/irq.rs#register_handler` | 同模块上下文对齐/差异观察 | (stack as usize).checked_add, copy_from_user, copy_from_user(clone_args_address, &mut raw[..size]).is_err, field, raw[offset..offset + 8].try_into, raw[offset..offset + 8].try_into().expect ...(+2) | with_cause! |
| `kernel/src/user.rs#handle_exception` | `modules/axhal/src/platform/riscv64_qemu_virt/irq.rs#register_handler` | 同模块上下文对齐/差异观察 | ACTIVE.load, EXIT_STATUS.store, FAULT_COUNT.fetch_add, LAST_FAULT_ADDRESS.store, LAST_FAULT_KIND.store, OSCOMP_LA_FPD_FIXUPS.fetch_add ...(+9) | with_cause! |
| `kernel/src/trap.rs#verify_breakpoint` | `modules/axtrap/src/arch/riscv/mod.rs#riscv_kernel_trap_handler` | 同模块上下文对齐/差异观察 | BREAKPOINT_COUNT.store, assert!, assert_eq!, crate::arch::trap::trigger_breakpoint, crate::println! | handle_irq, panic!, scause.bits, scause.cause, scause::read |

#### 系统调用
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `kernel/src/user.rs#assert_session` | `api/linux_syscall_api/src/syscall_net/socket.rs#get` | 同模块上下文对齐/差异观察 | assert!, assert_eq! | copy_nonoverlapping, drop, keep_alive.to_ne_bytes, panic!, s.keep_alive, s.keep_alive().is_some ...(+10) |
| `kernel/src/user.rs#sys_iov_io` | `api/linux_syscall_api/src/syscall_net/socket.rs#set` | 同模块上下文对齐/差异观察 | (len - done).min, base.checked_add, copy_plain_from_user::<usize>, core::mem::size_of::<usize>, iov_address.checked_add, sys_read ...(+2) | <[u8; 4]>::try_from, <[u8; 4]>::try_from(&opt[0..4]).unwrap, Err, Ok, Some, axnet::Duration::from_secs ...(+14) |
| `kernel/src/user.rs#sys_clock_nanosleep` | `api/linux_syscall_api/src/syscall_mem/imp.rs#syscall_shmget` | 同模块上下文对齐/差异观察 | copy_plain_from_user::<KernelTimespec>, copy_plain_to_user, core::cmp::min, core::time::Duration::from_nanos, core::time::Duration::new, crate::timer::sleep ...(+5) | Err, MemorySet::add_shared_mem, MemorySet::create_shared_mem, Ok, ShmFlags::from_bits, axmem::KEY_TO_SHMID.lock ...(+8) |
| `kernel/src/user.rs#sys_setitimer` | `api/linux_syscall_api/src/syscall_net/socket.rs#get` | 同模块上下文对齐/差异观察 | copy_plain_from_user::<KernelItimerval>, copy_plain_to_user | bytes.as_bytes, bytes.as_ptr, bytes.len, copy_nonoverlapping, panic!, raw_socket.get_congestion ...(+4) |

#### 文件系统
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `kernel/src/fs/mod.rs#rename` | `modules/axfs/src/fs/fatfs.rs#rename` | 同板块同名 | Arc::ptr_eq, Err, TREE.lock, clone_component, insert_child_prepared, is_node_read_only ...(+12) | debug!, dst_path.trim_matches, self.0
            .rename, self.0
            .rename(src_path, &self.0, dst_path)
            .map_err |
| `kernel/src/block.rs#write_at` | `modules/axfs/src/fs/fatfs.rs#write_at` | 同板块同名 | (block_size - block_offset)
            .min, (block_size - block_offset)
            .min(input.len() - written)
            .min, Ok, Vec::new, device.as_ref, device.block_size ...(+9) | Ok, SeekFrom::Start, ans.is_err, ans.unwrap, buf.len, buf.to_vec ...(+6) |
| `kernel/src/user.rs#sys_linkat` | `api/linux_syscall_api/src/syscall_fs/imp/link.rs#sys_linkat` | 同板块同名 | crate::fs::link, errno.to_isize, resolve_user_path | Err, Ok, Some, create_link, deal_with_path |
| `kernel/src/net/socket.rs#ioctl` | `modules/axfs/src/api/port.rs#ioctl` | 同板块同名 | Err, Ok | Err |

#### 设备驱动
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `kernel/src/virtio.rs#share` | `modules/axdriver/src/virtio.rs#share` | 同板块同名 | VirtAddr::new, buffer.as_ptr, buffer.as_ptr().cast::<u8>, buffer.as_ref, buffer.as_ref().len, panic! ...(+2) | buffer.as_ptr, vaddr.into, virt_to_phys, virt_to_phys(vaddr.into()).into |
| `kernel/src/virtio.rs#mmio_phys_to_virt` | `modules/axdriver/src/ixgbe.rs#mmio_phys_to_virt` | 同板块同名 | NonNull::new, NonNull::new(pointer).expect, PhysAddr::new, crate::arch::memory::phys_access::mmio_mut_ptr::<u8>, crate::arch::memory::phys_access::mmio_mut_ptr::<u8>(physical)
            .unwrap_or_else, panic! | NonNull::new, NonNull::new(phys_to_virt(paddr.into()).as_mut_ptr()).unwrap, paddr.into, phys_to_virt, phys_to_virt(paddr.into()).as_mut_ptr |
| `kernel/src/virtio.rs#push` | `modules/axdriver/src/structs/static.rs#push` | 同板块同名 | Some, self.entries.len | Some, self.0.is_none |
| `kernel/src/virtio.rs#push` | `modules/axdriver/src/structs/dyn.rs#push` | 同板块同名 | Some, self.entries.len | self.0.push |

#### 网络协议栈
仅目标项目中观察到该模块代表函数，参考项目没有同模块可对齐证据：
- 目标项目：`kernel/src/net/socket.rs#from_bytes`，调用摘要：Some, addr.as_mut_bytes, addr.as_mut_bytes().as_mut_ptr, bytes.as_ptr, bytes.len, core::mem::size_of::<Self> ...(+1)
- 目标项目：`kernel/src/net/socket.rs#new`，调用摘要：SocketType::Tcp, SocketType::Udp, Vec::new
- 目标项目：`kernel/src/net/socket.rs#as_mut_bytes`，调用摘要：core::mem::size_of::<Self>, core::slice::from_raw_parts_mut
- 目标项目：`kernel/src/net/socket.rs#as_bytes`，调用摘要：core::mem::size_of::<Self>, core::slice::from_raw_parts

#### 文件系统/第三方库
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `kernel/src/ext4.rs#read_inode` | `crates/another_ext4/src/ext4/rw.rs#read_inode` | 同板块同名 | Err, Ok, Vec::new, base.checked_add, block.copy_from_slice, inode_table_block
            .checked_mul ...(+14) | InodeRef::new, block.read_offset_as, self.inode_disk_pos, self.read_block |
| `kernel/src/ext4.rs#read_block` | `crates/another_ext4/src/ext4_defs/cache.rs#read_block` | 同板块同名 | Ok, Vec::new, block
            .checked_mul, block
            .checked_mul(self.block_size)
            .ok_or, data.resize, data.try_reserve ...(+4) | block.clone, cache[set_id].access, debug!, self.block_dev.read_block, self.block_dev.write_block, self.cache.lock ...(+1) |
| `kernel/src/ext4.rs#read_exact` | `crates/rust-fatfs/src/io.rs#read_exact` | 同板块同名 | Err, Ok, block::read_at, block::read_at(device, offset, output).map_err, output.len | Err, Ok, Self::Error::new_unexpected_eof_error, buf.is_empty, debug!, e.is_interrupted ...(+1) |
| `kernel/src/ext4.rs#file_type` | `crates/another_ext4/src/ext4_defs/inode.rs#file_type` | 同板块同名 | 内部闭环逻辑 | 内部闭环逻辑 |

#### 网络协议栈/第三方库
仅参考项目中观察到该模块代表函数，目标项目没有同模块可对齐证据：
- 参考项目：`modules/axnet/src/smoltcp_impl/tcp.rs#connect`，调用摘要：Ok, Ok::<(IpEndpoint, IpEndpoint), AxError>, SOCKET_SET
                .with_socket_mut::<tcp::Socket, _, _>, SOCKET_SET.add, SocketSetWrapper::new_tcp_socket, Some ...(+23)
- 参考项目：`modules/axnet/src/smoltcp_impl/tcp.rs#recv_timeout`，调用摘要：Err, Ok, SOCKET_SET.with_socket_mut::<tcp::Socket, _, _>, ax_err!, ax_err_type!, current_ticks ...(+11)
- 参考项目：`modules/axnet/src/smoltcp_impl/mod.rs#init`，调用摘要：Config::new, ETH0.init_by, EthernetAddress, GATEWAY.parse, GATEWAY.parse().expect, IP.parse ...(+22)
- 参考项目：`modules/axnet/src/smoltcp_impl/tcp.rs#shutdown`，调用摘要：LISTEN_TABLE.unlisten, Ok, SOCKET_SET.poll_interfaces, SOCKET_SET.with_socket_mut::<tcp::Socket, _, _>, debug!, self.handle.get ...(+9)

#### 第三方库
仅参考项目中观察到该模块代表函数，目标项目没有同模块可对齐证据：
- 参考项目：`crates/bcm2835-sdhci/src/Bcm2835SDhci.rs#sd_card_init`，调用摘要：ACMD!, byte_swap, debug!, info!, self.cmd_timeout, self.dumpregs ...(+26)
- 参考项目：`crates/elf_parser/src/arch/x86_64.rs#get_relocate_pairs`，调用摘要：Ok, Vec::new, VirtAddr::from, assert_eq!, dyn_sym.get_name, dyn_sym.get_name(elf).unwrap ...(+23)
- 参考项目：`crates/elf_parser/src/arch/aarch64.rs#get_relocate_pairs`，调用摘要：Ok, Vec::new, VirtAddr::from, assert_eq!, dyn_sym.get_name, dyn_sym.get_name(elf).unwrap ...(+22)
- 参考项目：`crates/elf_parser/src/arch/riscv.rs#get_relocate_pairs`，调用摘要：Ok, Vec::new, VirtAddr::from, assert_eq!, dyn_sym.get_name, dyn_sym.get_name(elf).unwrap ...(+23)

#### 通用模块/其他
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `runtime/src/console.rs#write_str` | `modules/axlog/src/lib.rs#write_str` | 同板块同名 | C::write_byte, Ok, text.bytes | Ok, cfg_if::cfg_if! |
| `kernel/src/user.rs#write_bytes` | `modules/axhal/src/lib.rs#write_bytes` | 同板块同名 | bytes[0..4].copy_from_slice, bytes[4..6].copy_from_slice, bytes[6..8].copy_from_slice, debug_assert_eq!, self.events.to_ne_bytes, self.fd.to_ne_bytes ...(+1) | putchar |
| `arch/loongarch64/src/smp.rs#current_cpu_id` | `modules/axruntime/src/lib.rs#current_cpu_id` | 同板块同名 | asm! | Some, axhal::cpu::this_cpu_id, is_init_ok |
| `arch/riscv64/src/sbi.rs#set_timer` | `modules/axhal/src/platform/VisionFive2/time.rs#set_timer` | 同板块同名 | call1, call1(TIME_EXTENSION_ID, TIME_SET_TIMER_FID, deadline as usize)
        .into_result, call1(TIME_EXTENSION_ID, TIME_SET_TIMER_FID, deadline as usize)
        .into_result()
        .map | super::sbi_call |


### 1. 宏观判定
- **定性结论**: 独立开发 (相似度: 0.86%，共同 AST 指纹: 39；唯一 AST 指纹: 目标项目 2114，参考项目 2483；覆盖率: 目标项目 1.84%，参考项目 1.57%)

### 2. 全模块覆盖矩阵解读
- **进程管理**: 双方均存在该模块。存在 1 个共同 AST 指纹，目标覆盖率 0.65%，参考覆盖率 3.12%。该模块证据强度为**弱**，同名函数源于 POSIX 标准语义，底层实现路径（原子操作 vs 锁机制）完全不同。
- **任务调度/异步运行时**: 双方均存在该模块。存在 1 个共同 AST 指纹，目标覆盖率 0.31%，参考覆盖率 0.44%。该模块证据强度为**弱**，共同指纹为通用同步原语，整体调度模型（同步 vs 异步）异构。
- **内存管理**: 双方均存在该模块。存在 2 个共同 AST 指纹，目标覆盖率 0.25%，参考覆盖率 1.2%。该模块证据强度为**弱**，共同指纹为 `NonNull::dangling` 等通用 Rust 模式，内存分配器实现路径不同。
- **中断/异常处理**: 双方均存在该模块。未观察到共同结构指纹。该模块证据强度为**未形成**。
- **系统调用**: 双方均存在该模块。未观察到共同结构指纹。该模块证据强度为**未形成**。
- **文件系统**: 双方均存在该模块。未观察到共同结构指纹。该模块证据强度为**未形成**。
- **设备驱动**: 双方均存在该模块。未观察到共同结构指纹。该模块证据强度为**未形成**。
- **网络协议栈**: 仅目标项目体现。该模块证据强度为**未形成**，属于单方独有功能。
- **第三方库/公共依赖**: 参考项目存在大量第三方库，目标项目无。该模块证据强度为**未形成**，体现了完全不同的项目构建策略。

### 3. 底层 AST 关键证据指纹表
| 所属板块 | 目标项目路径 | 参考项目路径 | 证据类型 | 技术说明 |
| :--- | :--- | :--- | :--- | :--- |
| 进程管理 | kernel/src/process.rs#exit | modules/axtask/src/api.rs#exit | 同板块同名 | 同名函数，但并发控制模型完全不同：目标项目使用无锁原子状态机 (`compare_exchange`)，参考项目使用全局锁 (`RUN_QUEUE.lock`)。 |
| 进程管理 | kernel/src/process.rs#close_on_exec | modules/axtask/src/fd_manager.rs#close_on_exec | 同板块同名 | 同名函数，均实现 POSIX 标准语义。目标项目采用“先收集后批量 drop”策略，参考项目采用迭代器内直接 `take`。 |
| 进程管理 | kernel/src/process.rs#set_parent | modules/axtask/src/task.rs#set_parent | 同板块同名 | 同名函数，但数据模型不同：目标项目操作关系图 (`relations.lock`)，参考项目操作单一原子指针 (`parent.store`)。 |
| 进程管理 | kernel/src/process.rs#set_close_on_exec | modules/axtask/src/stdio.rs#set_close_on_exec | 同板块同名 | 同名函数，均为简单的布尔标志位设置，属于通用编程模式。 |
| 任务调度/异步运行时 | kernel/src/tracked_spin.rs#lock | modules/axtask/src/sync/mutex.rs#lock | 同板块同名 | 同名函数，均为自旋锁实现，但内部机制不同：目标项目使用 `IrqSaveGuard` 和 `MigrationGuard`，参考项目使用 `owner_id` 比较交换。 |
| 内存管理 | kernel/src/virtio.rs#dma_alloc | modules/axdriver/src/virtio.rs#dma_alloc | 同板块同名 | 同名函数，均实现 DMA 内存分配，但分配器来源不同：目标项目使用自定义 `DMA_ALLOCATIONS` 池，参考项目使用 `global_allocator`。 |
| 内存管理 | kernel/src/user.rs#exec | modules/axtask/src/task.rs#exec | 同板块同名 | 同名函数，均实现 `exec` 系统调用核心逻辑，但内存区域构建方式不同：目标项目使用 `VmArea::new`，参考项目使用 `Arc::new` 和信号跳板。 |
| 设备驱动 | kernel/src/virtio.rs#share | modules/axdriver/src/virtio.rs#share | 同板块同名 | 同名函数，均实现 VirtIO 缓冲区共享，但物理地址转换路径不同。 |
| 文件系统/第三方库 | kernel/src/ext4.rs#read_inode | crates/another_ext4/src/ext4/rw.rs#read_inode | 同板块同名 | 同名函数，均实现 ext4 inode 读取，但底层块读取接口不同，属于对同一文件系统标准的独立实现。 |

### 4. 核心代码调用流对比剖析
* **【进程管理/exit 系统调用】**
* **底层技术析理**: 目标项目采用无锁并发模型，通过原子 `compare_exchange` 将线程生命周期从 `THREAD_RUNNING` 切换到 `THREAD_EXITING`，随后通过 `Completion` 原语 (`complete_all`) 通知等待者。参考项目则采用基于锁的同步模型，先获取全局 `RUN_QUEUE` 锁，再获取任务内部锁来设置状态，最后操作 `EXITED_TASKS` 列表进行清理。两者在同步原语、状态管理对象和清理路径上存在根本性架构差异。
* **代码溯源链接**:
  - 目标项目: `kernel/src/process.rs#exit`
  - 参考项目: `modules/axtask/src/api.rs#exit`

* **【进程管理/wait4 系统调用】**
* **底层技术析理**: 目标项目采用同步等待与所有权回收模型，通过 `Arc::try_unwrap` 判断子进程是否可被安全销毁，然后调用 `child.destroy` 进行同步清理。参考项目采用异步等待模型，通过 `WaitFuture::new` 创建一个未来对象，并使用 `axtask::block_task` 挂起当前任务。两者在任务调度、等待机制和资源回收策略上完全不同。
* **代码溯源链接**:
  - 目标项目: `kernel/src/user.rs#sys_wait4`
  - 参考项目: `api/linux_syscall_api/src/syscall_task/imp/task.rs#syscall_wait4`

* **【内存管理/exec 系统调用】**
* **底层技术析理**: 目标项目通过 `VirtAddr::new` 和 `VmArea::new` 直接构建用户态内存区域，并调用 `crate::elf::build_static_exec` 加载 ELF。参考项目则通过 `Arc::new` 管理任务结构，并显式设置信号跳板地址 `(axconfig::SIGNAL_TRAMPOLINE).into()`。两者在内存区域抽象（`VmArea` vs `Arc`）和 ELF 加载辅助功能上存在明显差异。
* **代码溯源链接**:
  - 目标项目: `kernel/src/user.rs#exec`
  - 参考项目: `modules/axtask/src/task.rs#exec`

### 5. 单方模块与降权项
- **网络协议栈**: 目标项目拥有独立的 `kernel/src/net/socket.rs` 实现，而参考项目无此模块。这属于目标项目的独有功能，不能作为同源证据。
- **第三方库**: 参考项目依赖大量第三方 crates（如 `another_ext4`, `rust-fatfs`, `elf_parser`），目标项目无此依赖。这体现了完全不同的项目构建和代码复用策略，不能作为同源证据。
- **降权项说明**: 跨板块的同名函数（如 `ioctl` 在文件系统、设备驱动、任务调度中均有出现）以及通用底层范式（如 `spin_loop` 忙等待、`assert!` 宏）均属于操作系统内核开发中的常见模式或 POSIX 标准命名，不构成同板块主证据，已降级为弱信号。

### 6. 保守结论
本次审计基于客观 AST 指纹比对，两项目全量代码相似度仅为 0.86%，远低于实质性相似的阈值。尽管在进程管理等核心模块存在少量同名函数，但其底层实现均采用了截然不同的并发模型、内存管理策略和调度架构。例如，目标项目偏好无锁原子操作和同步所有权回收，而参考项目则广泛使用锁机制和异步 Future。这些根本性的架构差异，加之双方在第三方库依赖和功能模块上的显著互补性，有力地支持了独立开发的结论。整体来看，目标项目不存在对参考项目进行代码复用或“语义洗稿”式抄袭的风险。

------------------------------

