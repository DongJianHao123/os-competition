# 🛡️ OS 内核赛自动化代码级架构审计报告

**审计时间**: `2026-07-02 03:45:43`
**参赛作品名称**: `2026-T2026102699910462-XX`
**年份**: `2026`
**学校**: `XX`
**队伍名称**: `T2026102699910462`
**比赛名字**: `OS-Competition`
**比赛子赛道**: `Kernel-Implementation`
**仓库地址**: `https://gitlab.eduxiji.net/educg-group-43501-3132633/T2026102699910462-2452`
**推测开发语言**: `Rust / C (由AST探针自动识别)`
**特征函数量**: `2495`

## ⚔️ 底层代码同源性对比 (AST Homology)
> 本部分由专门的代码审查智能体，对该项目与符合年份条件的历届最高相似度项目进行控制流与底层调用对比。新版报告会先给出确定性的全模块覆盖矩阵，再给出 Agent 的分模块解释，避免只围绕少量抽样函数展开。

### 🏆 Top 1 底层对标: vs `2025-sudo_pacman_Syu-同济大学`（同济大学 / sudo_pacman_Syu）

#### 对方作品信息

**对方年份**: `2025`
**对方学校**: `同济大学`
**对方队伍名称**: `sudo_pacman_Syu`
**对方比赛名字**: `OS-Competition`
**对方比赛子赛道**: `Kernel-Implementation`
**对方仓库地址**: `https://gitlab.eduxiji.net/educg-group-36002-2710490/T202510247995532-2817`

> 双向覆盖率：共同 AST 指纹: 34；唯一 AST 指纹: 目标项目 2114，参考项目 1840；覆盖率: 目标项目 1.61%，参考项目 1.85%

## 全模块对比总览（确定性证据）

> 总体覆盖率：共同 AST 指纹: 34；唯一 AST 指纹: 目标项目 2114，参考项目 1840；覆盖率: 目标项目 1.61%，参考项目 1.85%

### 1. 模块覆盖矩阵
| 模块 | 目标函数数 | 参考函数数 | 共同唯一 AST 指纹 | Target 覆盖 | Base 覆盖 | 状态 |
| :--- | ---: | ---: | ---: | ---: | ---: | :--- |
| 进程管理 | 173 | 154 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |
| 任务调度/异步运行时 | 347 | 164 | 1 | 0.31% | 0.65% | 双方存在且有结构重合 |
| 内存管理 | 938 | 261 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |
| 中断/异常处理 | 96 | 28 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |
| 系统调用 | 29 | 58 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |
| 文件系统 | 348 | 318 | 2 | 0.69% | 0.69% | 双方存在且有结构重合 |
| 设备驱动 | 123 | 162 | 1 | 0.93% | 0.78% | 双方存在且有结构重合 |
| 网络协议栈 | 7 | 51 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |
| 文件系统/第三方库 | 39 | 48 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |
| 第三方库 | 0 | 740 | 0 | 0.0% | 0.0% | 仅参考项目体现 |
| 通用模块/其他 | 395 | 188 | 3 | 0.82% | 1.92% | 双方存在且有结构重合 |

### 2. 分模块证据清单

#### 进程管理
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `kernel/src/process.rs#exit` | `src/kernel/syscall/procops.rs#exit` | 同板块同名 | Ok, self.exit_status.store, self.exited.complete_all, self.lifecycle
            .compare_exchange, self.lifecycle
            .compare_exchange(
                THREAD_RUNNING,
                THREAD_EXITING,
                Ordering::AcqRel,
                Ordering::Acquire,
            )
            .map_err, self.lifecycle.store | ProcessList::get, ProcessList::get().write, WaitType::Exited, procs
            .do_exit |
| `kernel/src/process.rs#add_child` | `src/kernel/task/process.rs#add_child` | 同板块同名 | Ok, relations
            .children
            .try_reserve, relations
            .children
            .try_reserve(1)
            .map_err, relations.children.push, self.relations.lock | assert! |
| `kernel/src/task/wait_queue.rs#wait` | `src/kernel/task/process.rs#wait` | 同板块同名 | self.done.load, self.try_wait, self.waiters
                .wait_until | Err, Ok, ProcessList::get, ProcessList::get().write, Some, assert! ...(+10) |
| `kernel/src/process.rs#setsid` | `src/kernel/task/process.rs#setsid` | 同板块同名 | self.id.get, self.process_group.store, self.session.store | Err, Ok, ProcessGroupBuilder::new, ProcessGroupBuilder::new()
            .leader, ProcessGroupBuilder::new()
            .leader(self)
            .session, ProcessGroupBuilder::new()
            .leader(self)
            .session(session.clone())
            .build ...(+16) |

#### 任务调度/异步运行时
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `kernel/src/tty.rs#ioctl` | `src/kernel/terminal.rs#ioctl` | 同板块同名 | (pgrp as i32).to_ne_bytes, CONSOLE_TTY.lock, Err, KernelTermios::from_tty, KernelTermios::zeroed, KernelWinsize::zeroed ...(+17) | Err, Ok, ProcessList::get, ProcessList::get().read, TermioIFlags::from_bits_truncate, TermioLFlags::from_bits_truncate ...(+12) |
| `kernel/src/smp.rs#name` | `src/kernel/task/thread.rs#name` | 同板块同名 | 内部闭环逻辑 | Some |
| `kernel/src/fs/mod.rs#truncate` | `src/kernel/syscall/file_rw.rs#truncate` | 同板块同名 | Err, matches!, truncate_node | Dentry::open, Path::new, UserString::new, dentry.truncate, path.as_cstr, path.as_cstr().to_bytes |
| `kernel/src/task/mod.rs#current` | `src/kernel/task/thread.rs#current` | 同板块同名 | cpu.get, self.cpus[cpu.get()]
            .current
            .expect | BorrowedArc::from_raw, CURRENT_THREAD.get, CURRENT_THREAD.get().expect |

#### 内存管理
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `mm/src/heap/large.rs#allocate` | `src/kernel/pcie/device.rs#allocate` | 同板块同名 | Err, HeapError::Provider, ManuallyDrop::new, NonNull::new, NonNull::new(user_address as *mut u8).ok_or, Ok ...(+36) | NonZero::new, eonix_log::println_trace!, self.start.align_up, size.next_power_of_two, size.next_power_of_two().try_into, size.next_power_of_two().try_into().ok |
| `kernel/src/virtio.rs#dma_alloc` | `src/driver/virtio/hal.rs#dma_alloc` | 同板块同名 | DMA_ALLOCATIONS.lock, NonNull::dangling, NonNull::new, PageAllocationOptions::dma32_zeroed, allocation.range, allocation.range().start ...(+16) | Page::alloc_at_least, page.as_memblk, page.as_memblk().as_byte_ptr, page.into_raw, page.start, page.start().addr |
| `mm/src/slab/cache.rs#deallocate` | `src/kernel/task/thread.rs#deallocate` | 同板块同名 | (&*slab_pointer.as_ptr()).class::<P::Error>, (&mut *slab_pointer.as_ptr()).free_object::<P::Error>, (&mut *slab_pointer.as_ptr()).take_allocation, Err, Ok, SlabHeader::from_object::<P::Error> ...(+11) | self.0.deallocate |
| `arch/loongarch64/src/memory/paging/hardware.rs#activate` | `src/kernel/mem/mm_list.rs#activate` | 同板块同名 | Err, Ok, data_barrier, instruction_barrier, invalidate_all, read_capabilities ...(+13) | PFN::from, inner.page_table.addr, inner.user_count.fetch_add, self.inner.try_borrow, set_root_page_table_pfn |

#### 中断/异常处理
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `kernel/src/timer.rs#handle_interrupt` | `src/driver/ahci/mod.rs#handle_interrupt` | 同板块同名 | (firing.callback), assert_initialized, base.queue.lock, base.queue.lock().earliest, cpu.get, crate::smp::current_cpu_id ...(+2) | debug_assert!, port.handle_interrupt, port.interrupt_status, port.interrupt_status().read_once, port.interrupt_status().write_once, ports[nport as usize].as_ref ...(+5) |
| `arch/riscv64/src/cpu.rs#wait_for_interrupt` | `src/driver/serial.rs#wait_for_interrupt` | 同板块同名 | asm! | pin!, self.disable_interrupts, self.enable_interrupts, self.working.lock_irq, wait.as_mut, wait.as_mut().add_to_wait_list |
| `arch/loongarch64/src/trap/frame.rs#pending_interrupts` | `src/driver/ahci/control.rs#pending_interrupts` | 同板块同名 | 内部闭环逻辑 | BitsIterator::new, self.read |
| `kernel/src/user.rs#sys_clone3` | `src/kernel/terminal.rs#commit_char` | 同模块上下文对齐/差异观察 | (stack as usize).checked_add, copy_from_user, copy_from_user(clone_args_address, &mut raw[..size]).is_err, field, raw[offset..offset + 8].try_into, raw[offset..offset + 8].try_into().expect ...(+2) | inner.termio.echok, inner.termio.echoke, inner.termio.icanon, inner.termio.icrnl, inner.termio.iexten, inner.termio.igncr ...(+16) |

#### 系统调用
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `kernel/src/user.rs#assert_session` | `src/kernel/mem/mm_list.rs#set_break` | 同模块上下文对齐/差异观察 | assert!, assert_eq! | Some, VRange::new, assert!, drop, inner.locked.lock, locked.areas.check_overlapping_range ...(+5) |
| `kernel/src/user.rs#sys_iov_io` | `src/kernel/syscall/sysinfo.rs#newuname` | 同模块上下文对齐/差异观察 | (len - done).min, base.checked_add, copy_plain_from_user::<usize>, core::mem::size_of::<usize>, iov_address.checked_add, sys_read ...(+2) | UserPointerMut::new, buffer.write, copy_cstr_to_array |
| `kernel/src/user.rs#sys_clock_nanosleep` | `src/kernel/syscall/net.rs#get_sockopt` | 同模块上下文对齐/差异观察 | copy_plain_from_user::<KernelTimespec>, copy_plain_to_user, core::cmp::min, core::time::Duration::from_nanos, core::time::Duration::new, crate::timer::sleep ...(+5) | Ok, UserPointerMut::new, UserPointerMut::new(optlen)?.write, UserPointerMut::new(optval.cast())?.write, optval.cast, size_of::<u32> |
| `kernel/src/user.rs#sys_setitimer` | `src/kernel/syscall/net.rs#read_socket_addr` | 同模块上下文对齐/差异观察 | copy_plain_from_user::<KernelItimerval>, copy_plain_to_user | Err, IpAddr::V4, Ipv4Addr::from_bits, Ok, SockDomain::try_from, SocketAddr::new ...(+6) |

#### 文件系统
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `kernel/src/fs/mod.rs#readdir` | `src/kernel/vfs/dentry.rs#readdir` | 同板块同名 | Err, Ok, buf.len, child.mode.file_type, children.len, emit_dirent64 ...(+6) | dir.do_readdir, self.get_inode |
| `kernel/src/fs/mod.rs#rename` | `src/fs/tmpfs.rs#rename` | 同板块同名 | Arc::ptr_eq, Err, TREE.lock, clone_component, insert_child_prepared, is_node_read_only ...(+12) | (new_parent_inode.as_ref() as &dyn Any)
                .downcast_ref::<DirectoryInode>, (new_parent_inode.as_ref() as &dyn Any)
                .downcast_ref::<DirectoryInode>()
                .expect, Arc::as_ptr, Err, Instant::now, Ok ...(+53) |
| `kernel/src/fs/mod.rs#link` | `src/fs/tmpfs.rs#link` | 同板块同名 | Arc::clone, Err, Ok, TREE.lock, insert_child, is_node_read_only ...(+5) | Instant::now, file.nlink.fetch_add, self.ctime.lock, self.entries.access_mut, self.entries.access_mut(dlock).push, self.mtime.lock ...(+1) |
| `kernel/src/fs/mod.rs#mkdir` | `src/fs/tmpfs.rs#mkdir` | 同板块同名 | Err, FileMode::from_bits, TREE.lock, directory, insert_child, is_node_read_only ...(+2) | DirectoryInode::new, acquire, astmp, at.get_name, at.save_dir, block_on ...(+6) |

#### 设备驱动
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `kernel/src/device.rs#register_driver` | `src/kernel/pcie/driver.rs#register_driver` | 同板块同名 | Arc::clone, DEVICES.lock, DRIVERS.lock, Ok, Some, crate::println! ...(+10) | Arc::new, Err, Ok, PCIE_DEVICES.lock, PCIE_DEVICES.lock().get, PCIE_DEVICES.lock().get(&index).cloned ...(+7) |
| `kernel/src/virtio.rs#share` | `src/driver/virtio/hal.rs#share` | 同板块同名 | VirtAddr::new, buffer.as_ptr, buffer.as_ptr().cast::<u8>, buffer.as_ref, buffer.as_ref().len, panic! ...(+2) | ArchPhysAccess::from_ptr, buffer.cast::<u8>, paddr.addr |
| `kernel/src/net/virtio_net.rs#receive` | `src/net/device.rs#receive` | 同板块同名 | Err, Ok, buffer.len, buffer[..copy_len].copy_from_slice, driver.poll_receive, driver.poll_receive().is_none ...(+4) | RxToken, Some, TxToken, self.can_receive, self.can_send, self.recv ...(+1) |
| `kernel/src/virtio.rs#mmio_phys_to_virt` | `src/driver/virtio/hal.rs#mmio_phys_to_virt` | 同板块同名 | NonNull::new, NonNull::new(pointer).expect, PhysAddr::new, crate::arch::memory::phys_access::mmio_mut_ptr::<u8>, crate::arch::memory::phys_access::mmio_mut_ptr::<u8>(physical)
            .unwrap_or_else, panic! | ArchPhysAccess::as_ptr, PAddr::from |

#### 网络协议栈
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `kernel/src/net/socket.rs#from_bytes` | `src/net/socket/tcp.rs#poll_impl` | 同模块上下文对齐/差异观察 | Some, addr.as_mut_bytes, addr.as_mut_bytes().as_mut_ptr, bytes.as_ptr, bytes.len, core::mem::size_of::<Self> ...(+1) | Ok, PollEvent::empty, Some, block_on, events.contains, iface.lock ...(+12) |
| `kernel/src/net/socket.rs#new` | `src/net/socket/udp.rs#bind` | 同模块上下文对齐/差异观察 | SocketType::Tcp, SocketType::Udp, Vec::new | BoundSocket::new_bind_all, BoundSocket::new_bind_single, Err, Ok, Self::bind_impl, Some ...(+23) |
| `kernel/src/net/socket.rs#as_mut_bytes` | `src/net/socket/tcp.rs#accept_impl` | 同模块上下文对齐/差异观察 | core::mem::size_of::<Self>, core::slice::from_raw_parts_mut | Arc::new, BoundSocket::new_single, Err, Ok, RwLock::new, SocketAddr::from ...(+15) |
| `kernel/src/net/socket.rs#as_bytes` | `src/net/socket/udp.rs#drop` | 同模块上下文对齐/差异观察 | core::mem::size_of::<Self>, core::slice::from_raw_parts | UDP_PORT_MAP.lock, UDP_PORT_MAP.lock().remove, block_on, bound_socket_guard.as_ref, bound_socket_guard.as_ref().unwrap, bound_socket_guard.is_none ...(+16) |

#### 文件系统/第三方库
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `kernel/src/ext4.rs#read_block` | `src/fs/ext4.rs#read_block` | 同板块同名 | Ok, Vec::new, block
            .checked_mul, block
            .checked_mul(self.block_size)
            .ok_or, data.resize, data.try_reserve ...(+4) | ByteBuffer::new, buffer.as_mut_slice, self
            .device
            .read_some, self
            .device
            .read_some((block_id as usize) * 4096, &mut byte_buffer)
            .expect |
| `kernel/src/user.rs#sdcard_install_ext4_dir_files` | `src/fs/ext4.rs#lookup` | 同模块上下文对齐/差异观察 | alloc::format!, alloc::string::String::from, crate::block::open_device, crate::ext4::list_directory, crate::fs::install_ext4_path, crate::fs::install_ext4_path("/dev/vda", &vfs_path, &ext4_path).is_ok ...(+6) | AtomicMode::new, AtomicNlink::new, AtomicU32::new, AtomicU64::new, Err, InodeMode::BLOCKDEV.bits ...(+30) |
| `kernel/src/ext4.rs#open` | `src/fs/ext4.rs#rename` | 同模块上下文对齐/差异观察 | (32..=1024).contains, 1024_u64
            .checked_shl, 1024_u64
            .checked_shl(log_block_size)
            .ok_or, Err, Ok, block_size.is_power_of_two ...(+10) | Arc::as_ptr, Err, Instant::now, Ok, block_on, core::str::from_utf8 ...(+27) |
| `kernel/src/main.rs#oscomp_materialize_ext4_dir_flat` | `src/fs/ext4.rs#create` | 同模块上下文对齐/差异观察 | alloc::format!, alloc::sync::Arc::clone, crate::block::open_device, crate::ext4::list_directory, crate::println!, fs::mkdir ...(+5) | Arc::downgrade, Arc::new, AtomicMode::new, AtomicNlink::new, AtomicU32::new, AtomicU64::new ...(+25) |

#### 第三方库
仅参考项目中观察到该模块代表函数，目标项目没有同模块可对齐证据：
- 参考项目：`crates/eonix_hal/src/arch/riscv64/trap/default.rs#_default_trap_entry`，调用摘要：naked_asm!
- 参考项目：`crates/eonix_hal/src/arch/loongarch64/trap/mod.rs#_raw_trap_entry`，调用摘要：naked_asm!
- 参考项目：`crates/eonix_percpu/eonix_percpu_macros/src/lib.rs#define_percpu_impl`，调用摘要：["u8", "u16", "u32", "u64", "usize"].contains, attrs.is_empty, format_ident!, get_percpu_pointer, panic!, parse2::<ItemStatic> ...(+6)
- 参考项目：`crates/eonix_hal/src/arch/riscv64/trap/captured.rs#_captured_trap_entry`，调用摘要：naked_asm!

#### 通用模块/其他
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `sync/src/spin_lock.rs#lock` | `src/rcu.rs#lock` | 同板块同名 | self
                .locked
                .compare_exchange_weak, self
                .locked
                .compare_exchange_weak(false, true, Ordering::Acquire, Ordering::Relaxed)
                .is_ok, self.locked.load, spin_loop | GLOBAL_RCU_SEM.read, block_on |
| `runtime/src/console.rs#write_str` | `src/io.rs#write_str` | 同板块同名 | C::write_byte, Ok, text.bytes | Err, Ok, s.as_bytes, self.0.fill |
| `kernel/src/tty.rs#as_bytes` | `src/kernel/mem/access.rs#as_bytes` | 同板块同名 | core::mem::size_of::<Self>, core::ptr::addr_of!, core::slice::from_raw_parts | core::slice::from_raw_parts, self.as_ptr_unchecked, self.as_ptr_unchecked().as_ptr |
| `kernel/src/tracked_spin.rs#deref` | `src/kernel/pcie/header.rs#deref` | 同板块同名 | self.guard
            .as_ref, self.guard
            .as_ref()
            .expect, self.guard
            .as_ref()
            .expect("tracked spin-lock guard was already released")
            .deref | 内部闭环逻辑 |


#### 1. 宏观判定
- **定性结论**: 独立开发 (相似度: 0.87%，共同 AST 指纹: 34；唯一 AST 指纹: 目标项目 2114，参考项目 1840；覆盖率: 目标项目 1.61%，参考项目 1.85%)

#### 2. 全模块覆盖矩阵解读

- **进程管理**
  - 双方均存在该模块（目标 173 函数，参考 154 函数）。
  - 共同唯一 AST 指纹: 0。
  - 覆盖率: 目标 0.0%，参考 0.0%。表明双方在进程管理的底层实现上无任何可识别的共同结构片段。
  - 证据强度: 未形成。同名函数（如 `exit`、`setsid`、`add_child`）的调用图谱完全不同，属于 POSIX 语义下的独立实现。

- **任务调度/异步运行时**
  - 双方均存在该模块（目标 347 函数，参考 164 函数）。
  - 共同唯一 AST 指纹: 1。
  - 覆盖率: 目标 0.31%，参考 0.65%。极低的覆盖率表明双方调度器设计基本独立。
  - 证据强度: 弱。唯一的共同指纹可能源于通用的同步原语或 Rust 惯用法，不构成结构性复用。

- **内存管理**
  - 双方均存在该模块（目标 938 函数，参考 261 函数）。
  - 共同唯一 AST 指纹: 0。
  - 覆盖率: 目标 0.0%，参考 0.0%。双方内存管理子系统在 AST 层面完全异构。
  - 证据强度: 未形成。同名函数（如 `allocate`、`deallocate`、`activate`）操作的对象和抽象层级完全不同。

- **中断/异常处理**
  - 双方均存在该模块（目标 96 函数，参考 28 函数）。
  - 共同唯一 AST 指纹: 0。
  - 覆盖率: 目标 0.0%，参考 0.0%。无共同结构指纹。
  - 证据强度: 未形成。同名函数（如 `handle_interrupt`、`wait_for_interrupt`）处理的具体设备和机制完全不同。

- **系统调用**
  - 双方均存在该模块（目标 29 函数，参考 58 函数）。
  - 共同唯一 AST 指纹: 0。
  - 覆盖率: 目标 0.0%，参考 0.0%。无共同结构指纹。
  - 证据强度: 未形成。系统调用实现依赖各自的内核抽象，无复用痕迹。

- **文件系统**
  - 双方均存在该模块（目标 348 函数，参考 318 函数）。
  - 共同唯一 AST 指纹: 2。
  - 覆盖率: 目标 0.69%，参考 0.69%。覆盖率极低，表明文件系统核心逻辑独立。
  - 证据强度: 弱。共同指纹可能源于 VFS 通用操作（如 `readdir`、`rename`）的接口相似性，而非实现复用。

- **设备驱动**
  - 双方均存在该模块（目标 123 函数，参考 162 函数）。
  - 共同唯一 AST 指纹: 1。
  - 覆盖率: 目标 0.93%，参考 0.78%。覆盖率极低。
  - 证据强度: 弱。共同指纹可能源于 VirtIO 等标准驱动框架的通用交互模式。

- **网络协议栈**
  - 双方均存在该模块（目标 7 函数，参考 51 函数）。
  - 共同唯一 AST 指纹: 0。
  - 覆盖率: 目标 0.0%，参考 0.0%。无共同结构指纹。
  - 证据强度: 未形成。目标项目网络栈规模极小，与参考项目无结构重合。

- **第三方库/公共依赖**
  - 仅参考项目体现（740 函数），目标项目无此独立模块。
  - 共同唯一 AST 指纹: 0。
  - 覆盖率: 目标 0.0%，参考 0.0%。
  - 证据强度: 未形成。参考项目包含大量第三方库（如 `eonix_hal`），目标项目未使用，属于单方特征。

#### 3. 底层 AST 关键证据指纹表
| 所属板块 | 目标项目路径 | 参考项目路径 | 证据类型 | 技术说明 |
| :--- | :--- | :--- | :--- | :--- |
| 任务调度/异步运行时 | `kernel/src/tty.rs#ioctl` | `src/kernel/terminal.rs#ioctl` | 弱信号 | 同名函数，但目标操作 `KernelTermios`/`KernelWinsize`，参考操作 `TermioIFlags`/`ProcessList`，外部调用对象完全不同。 |
| 任务调度/异步运行时 | `kernel/src/smp.rs#name` | `src/kernel/task/thread.rs#name` | 弱信号 | 同名函数，目标为内部闭环逻辑，参考返回 `Some`，语义和实现完全不同。 |
| 任务调度/异步运行时 | `kernel/src/fs/mod.rs#truncate` | `src/kernel/syscall/file_rw.rs#truncate` | 弱信号 | 同名函数，目标直接调用 `truncate_node`，参考通过 `Dentry::open`/`Path::new` 解析路径，调用链完全不同。 |
| 任务调度/异步运行时 | `kernel/src/task/mod.rs#current` | `src/kernel/task/thread.rs#current` | 弱信号 | 同名函数，目标通过 `cpu.get` 从 PerCPU 数组获取，参考通过 `CURRENT_THREAD.get` 从线程局部存储获取，机制不同。 |
| 文件系统 | `kernel/src/fs/mod.rs#readdir` | `src/kernel/vfs/dentry.rs#readdir` | 弱信号 | 同名函数，目标直接操作 `children.len`/`emit_dirent64`，参考委托给 `dir.do_readdir`，架构分层不同。 |
| 文件系统 | `kernel/src/fs/mod.rs#rename` | `src/fs/tmpfs.rs#rename` | 弱信号 | 同名函数，目标操作全局 `TREE.lock`，参考通过 `downcast_ref` 获取具体文件系统实现，多态机制不同。 |
| 设备驱动 | `kernel/src/device.rs#register_driver` | `src/kernel/pcie/driver.rs#register_driver` | 弱信号 | 同名函数，目标操作 `DEVICES`/`DRIVERS` 全局表，参考操作 `PCIE_DEVICES` 专用表，设备模型不同。 |

#### 4. 核心代码调用流对比剖析

* **【进程管理/进程退出路径】**
* **底层技术析理**: 目标项目 `exit` 函数通过原子 CAS 操作 (`compare_exchange`) 将线程生命周期从 `THREAD_RUNNING` 切换到 `THREAD_EXITING`，随后调用 `complete_all` 唤醒等待者，并存储退出状态。参考项目 `exit` 函数则获取全局 `ProcessList` 写锁，调用 `do_exit` 进行进程表清理。前者是**基于原子状态机的无锁线程退出模型**，后者是**基于全局锁的进程表维护模型**，同步策略和资源回收路径完全不同。
* **代码溯源链接**:
  - 目标项目: `kernel/src/process.rs#exit`
  - 参考项目: `src/kernel/syscall/procops.rs#exit`

* **【内存管理/物理内存分配】**
* **底层技术析理**: 目标项目 `allocate` 函数通过 `ManuallyDrop` 和 `NonNull` 直接操作裸指针，从自定义堆分配器获取内存，错误处理返回 `HeapError::Provider`。参考项目 `allocate` 函数通过 `size.next_power_of_two` 对齐后调用 `self.start.align_up` 进行边界检查。前者是**基于裸指针和手动管理的底层分配器接口**，后者是**基于对齐运算和范围检查的分配策略**，内存抽象模型不同。
* **代码溯源链接**:
  - 目标项目: `mm/src/heap/large.rs#allocate`
  - 参考项目: `src/kernel/pcie/device.rs#allocate`

* **【系统调用/信号掩码管理】**
* **底层技术析理**: 目标项目 `sys_rt_sigprocmask` 使用 `copy_from_user`/`copy_to_user` 配合 `to_ne_bytes` 进行用户态内存交互，并调用 `crate::signal::update_mask` 和 `crate::task::current_user_thread` 更新内核状态。参考项目 `rt_sigprocmask` 使用自有的 `UserPointer`/`UserPointerMut` 抽象进行读写，并通过 `oldset.is_null` 进行空指针检查。前者是**基于经典 Linux 内核原语的用户态访问模型**，后者是**基于自建安全指针封装的访问模型**，用户态交互层完全不同。
* **代码溯源链接**:
  - 目标项目: `kernel/src/user.rs#sys_rt_sigprocmask`
  - 参考项目: `src/kernel/syscall/procops.rs#rt_sigprocmask`

* **【文件系统/路径重命名】**
* **底层技术析理**: 目标项目 `rename` 函数操作全局 `TREE.lock`，通过 `Arc::ptr_eq` 判断源和目标是否相同，并调用 `clone_component`/`insert_child_prepared` 进行树节点操作。参考项目 `rename` 函数通过 `downcast_ref::<DirectoryInode>` 将通用 inode 转换为具体文件系统类型，然后操作 `DirectoryInode` 的内部条目。前者是**基于全局树锁和 Arc 指针比较的统一 VFS 模型**，后者是**基于向下转型和具体文件系统实现的分离模型**，VFS 架构设计不同。
* **代码溯源链接**:
  - 目标项目: `kernel/src/fs/mod.rs#rename`
  - 参考项目: `src/fs/tmpfs.rs#rename`

#### 5. 单方模块与降权项
- **第三方库/公共依赖**: 参考项目包含 `crates/eonix_hal`、`crates/eonix_percpu` 等第三方库模块（740 函数），目标项目无此独立模块。这些库属于参考项目的自建基础设施，目标项目未使用，不能作为同源证据。
- **同名弱信号降权说明**: 双方在多个模块中存在同名函数（如 `wait`、`get`、`entry`、`add_child`、`session`、`process`），但其 AST 调用图谱完全不同。例如，目标 `wait` 基于 `Completion`/`WaitQueue` 机制，参考 `wait` 基于 `Condvar`/`ProcessList` 机制。这些同名源于 POSIX/内核领域的通用术语，不构成复用证据，已降权为弱信号。
- **跨板块证据排除**: 控方将目标项目 `kernel/src/process.rs#drop` 与参考项目 `src/kernel/task/process_list.rs#remove_process` 配对，两者分属线程析构和全局进程表维护，生命周期阶段和调用上下文完全不同，属于跨生命周期硬凑，已排除。

#### 6. 保守结论
双方项目整体 AST 相似度仅 0.87%，共同指纹 34 个，覆盖率均低于 2%，在宏观层面已排除系统性抄袭可能。进程管理、内存管理等核心模块的共同指纹为零，表明底层实现完全异构。目标项目采用基于原子状态机、裸指针操作和全局树锁的扁平化设计，参考项目则采用基于 Builder 模式、安全指针封装和向下转型的分层抽象设计，两者在同步策略、内存模型和 VFS 架构上存在根本性差异。现有弱信号均源于 POSIX 语义约束或 Rust 通用惯用法，不构成同源证据。综合判定，目标项目为独立开发，无违规复用风险。

------------------------------

