# 🛡️ OS 内核赛自动化代码级架构审计报告

**审计时间**: `2026-07-02 03:45:43`
**参赛作品名称**: `2026-T2026102699910462-XX`
**年份**: `2026`
**学校**: `XX`
**队伍名称**: `T2026102699910462`
**比赛名字**: `OS-Competition`
**比赛子赛道**: `Kernel-Implementation`
**仓库地址**: `https://gitlab.eduxiji.net/educg-group-43501-3132633/T2026102699910462-2452`
**推测开发语言**: `Rust / C (由AST探针自动识别)`
**特征函数量**: `2495`

## ⚔️ 底层代码同源性对比 (AST Homology)
> 本部分由专门的代码审查智能体，对该项目与符合年份条件的历届最高相似度项目进行控制流与底层调用对比。新版报告会先给出确定性的全模块覆盖矩阵，再给出 Agent 的分模块解释，避免只围绕少量抽样函数展开。

### 🏆 Top 3 底层对标: vs `2025-StarryX-杭州电子科技大学`（杭州电子科技大学 / StarryX）

#### 对方作品信息

**对方年份**: `2025`
**对方学校**: `杭州电子科技大学`
**对方队伍名称**: `StarryX`
**对方比赛名字**: `OS-Competition`
**对方比赛子赛道**: `Kernel-Implementation`
**对方仓库地址**: `https://gitlab.eduxiji.net/educg-group-36002-2710490/T202510336995486-3037`

> 双向覆盖率：共同 AST 指纹: 32；唯一 AST 指纹: 目标项目 2114，参考项目 2897；覆盖率: 目标项目 1.51%，参考项目 1.1%

## 全模块对比总览（确定性证据）

> 总体覆盖率：共同 AST 指纹: 32；唯一 AST 指纹: 目标项目 2114，参考项目 2897；覆盖率: 目标项目 1.51%，参考项目 1.1%

### 1. 模块覆盖矩阵
| 模块 | 目标函数数 | 参考函数数 | 共同唯一 AST 指纹 | Target 覆盖 | Base 覆盖 | 状态 |
| :--- | ---: | ---: | ---: | ---: | ---: | :--- |
| 进程管理 | 173 | 172 | 3 | 1.95% | 1.89% | 双方存在且有结构重合 |
| 任务调度/异步运行时 | 347 | 480 | 1 | 0.31% | 0.28% | 双方存在且有结构重合 |
| 内存管理 | 938 | 430 | 3 | 0.37% | 0.77% | 双方存在且有结构重合 |
| 中断/异常处理 | 96 | 70 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |
| 系统调用 | 29 | 89 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |
| 文件系统 | 348 | 801 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |
| 设备驱动 | 123 | 139 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |
| 网络协议栈 | 7 | 77 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |
| 文件系统/第三方库 | 39 | 164 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |
| 网络协议栈/第三方库 | 0 | 166 | 0 | 0.0% | 0.0% | 仅参考项目体现 |
| 第三方库 | 0 | 349 | 0 | 0.0% | 0.0% | 仅参考项目体现 |
| 通用模块/其他 | 395 | 665 | 6 | 1.65% | 1.13% | 双方存在且有结构重合 |

### 2. 分模块证据清单

#### 进程管理
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `kernel/src/user.rs#sys_wait4` | `xapi/src/task/wait.rs#sys_wait4` | 同板块同名 | ACTIVE.load, Arc::try_unwrap, child.destroy, child.destroy().is_err, child.id, child.id().get ...(+10) | Err, Ok, WaitOptions::from_bits, WaitPid::Pgid, WaitPid::Pid, child.data::<XProcess> ...(+26) |
| `kernel/src/user.rs#sys_rt_sigtimedwait` | `xapi/src/task/signal.rs#sys_rt_sigtimedwait` | 同板块同名 | copy_from_user, copy_from_user(set_address, &mut set_bytes).is_err, copy_plain_from_user::<KernelTimespec>, copy_to_user, copy_to_user(info_address, unsafe {
                core::slice::from_raw_parts(
                    &info as *const KernelSigInfo as *const u8,
                    core::mem::size_of::<KernelSigInfo>(),
                )
            })
            .is_err, core::cmp::min ...(+13) | Err, Ok, XProcess::from_thread, XProcess::from_thread(thread).uspace, XThread::from_thread, check_sigset_size ...(+6) |
| `kernel/src/task/m4c_verify.rs#wait_until` | `arceos/modules/axtask/src/wait_queue.rs#wait_until` | 同板块同名 | SWITCH_RACE_QUEUE.debug_state, WAIT_QUEUE.debug_state, condition, crate::arch::time::counter, panic!, spin_loop ...(+4) | condition, crate::current, current_run_queue::<NoPreemptIrqSave>, rq.blocked_resched, self.cancel_events, self.queue.lock |
| `kernel/src/user.rs#sys_rt_sigprocmask` | `xapi/src/task/signal.rs#sys_rt_sigprocmask` | 同板块同名 | Some, copy_from_user, copy_from_user(set_address, &mut bytes).is_err, copy_to_user, copy_to_user(oldset_address, &old.to_ne_bytes()).is_err, core::mem::size_of::<u64> ...(+9) | Err, Ok, XProcess::from_thread, XProcess::from_thread(thread).uspace, XThread::from_thread, check_sigset_size ...(+3) |

#### 任务调度/异步运行时
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `kernel/src/user.rs#sys_clone` | `xapi/src/task/clone.rs#sys_clone` | 同板块同名 | child.create_initial_thread, child.id, child.id().get, child_thread.id, child_thread.id().get, child_thread.save_trap_frame ...(+19) | do_clone |
| `kernel/src/user.rs#sys_pselect6` | `xapi/src/iomux/select.rs#sys_pselect6` | 同板块同名 | copy_fdset_from_user, copy_fdset_from_user(exceptfds_address, &mut exceptfds[..bytes_len]).is_err, copy_fdset_from_user(readfds_address, &mut readfds[..bytes_len]).is_err, copy_fdset_from_user(writefds_address, &mut writefds[..bytes_len]).is_err, copy_fdset_to_user, copy_fdset_to_user(exceptfds_address, &out_exceptfds[..bytes_len]).is_err ...(+15) | do_select, nullable!, nullable!(uspace.read(timeout))?
                .map, nullable!(uspace.read(timeout))?
                .map(timespec::to_time_value)
                .transpose, with_uspace |
| `kernel/src/user.rs#sys_futex` | `xapi/src/task/futex.rs#sys_futex` | 同板块同名 | capped.is_zero, copy_plain_from_user::<KernelTimespec>, copy_plain_from_user::<u32>, core::cmp::min, core::time::Duration::from_millis, core::time::Duration::new ...(+6) | Err, FutexKey::new, Ok, axtask::yield_now, current, current().task_ext ...(+24) |
| `kernel/src/user.rs#sys_getcwd` | `arceos/api/arceos_posix_api/src/imp/fs.rs#sys_getcwd` | 同板块同名 | bytes[..chosen.len()].copy_from_slice, chosen.as_bytes, chosen.len, copy_to_user, copy_to_user(address, &bytes[..chosen.len() + 1]).is_err, current_process ...(+6) | debug!, syscall_body! |

#### 内存管理
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `kernel/src/user.rs#sys_execve` | `xapi/src/task/execve.rs#sys_execve` | 同板块同名 | Arc::clone, Arc::try_unwrap, Some, Vec::new, VirtAddr::new, VirtRange::from_bounds ...(+60) | Err, FD_TABLE.close_on_exec, FS_CONTEXT.lock, FS_CONTEXT.lock().canonicalize, FS_CONTEXT.lock().canonicalize(path)?.to_string, Ok ...(+35) |
| `kernel/src/user.rs#sys_mmap` | `xapi/src/mm/mmap.rs#sys_mmap` | 同板块同名 | ACTIVE.load, MMAP_COUNT.fetch_add, VirtAddr::new, VirtRange::from_bounds, VirtRange::new, crate::println! ...(+10) | (addr + length).align_up, Err, FD_TABLE.is_assigned, File::from_fd, File::from_fd(fd, FileFlags::READ, FileFlags::empty())
            .map_err, FileFlags::empty ...(+39) |
| `mm/src/slab/slab.rs#create` | `arceos/modules/axfs-ng/src/fs/fat/dir.rs#create` | 同板块同名 | Err, Ok, SlabError::Provider, allocation.order, base.as_ptr, base.cast::<Self> ...(+15) | DirEntry::new_dir, DirEntry::new_file, Err, FatDirNode::new, FatFileNode::new, Reference::new ...(+12) |
| `kernel/src/virtio.rs#dma_alloc` | `arceos/modules/axdriver/src/virtio.rs#dma_alloc` | 同板块同名 | DMA_ALLOCATIONS.lock, NonNull::dangling, NonNull::new, PageAllocationOptions::dma32_zeroed, allocation.range, allocation.range().start ...(+16) | NonNull::dangling, NonNull::new, NonNull::new(vaddr as _).unwrap, global_allocator, global_allocator().alloc_pages, paddr.as_usize ...(+2) |

#### 中断/异常处理
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `arch/riscv64/src/interrupt.rs#restore` | `xmodules/xsignal/src/arch/x86_64.rs#restore` | 同板块同名 | asm!, state.was_enabled | 内部闭环逻辑 |
| `kernel/src/trap.rs#handle_breakpoint` | `arceos/modules/axhal/src/arch/riscv/trap.rs#handle_breakpoint` | 同板块同名 | crate::user::handle_exception, frame.advance_pc, frame.exception_code, frame.previous_mode_was_user, mark_breakpoint_reached | debug! |
| `kernel/src/trap.rs#handle_breakpoint` | `arceos/modules/axhal/src/arch/loongarch64/trap.rs#handle_breakpoint` | 同板块同名 | crate::user::handle_exception, frame.advance_pc, frame.cause_code, frame.previous_mode_was_user, mark_breakpoint_reached | debug! |
| `arch/loongarch64/src/interrupt.rs#restore` | `xmodules/xsignal/src/arch/loongarch64.rs#restore` | 同板块同名 | exchange_interrupt_enable, state.was_enabled | mem::transmute::<[u64; 32], GeneralRegisters> |

#### 系统调用
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `kernel/src/net/socket.rs#sys_bind` | `arceos/api/arceos_posix_api/src/imp/net.rs#sys_bind` | 同板块同名 | SOCKET_TABLE.lock, SocketType::Tcp, SocketType::Udp, copy_addr_from_user, core::mem::size_of::<SockAddrIn>, get_socket_id_from_fd ...(+1) | debug!, syscall_body! |
| `kernel/src/user.rs#sys_syslog` | `xapi/src/sys/common.rs#sys_syslog` | 同板块同名 | 内部闭环逻辑 | Ok |
| `kernel/src/net/socket.rs#sys_connect` | `arceos/api/arceos_posix_api/src/imp/net.rs#sys_connect` | 同板块同名 | SOCKET_TABLE.lock, SockAddrIn::new, SocketType::Tcp, copy_addr_from_user, core::mem::size_of::<SockAddrIn>, get_socket_id_from_fd ...(+1) | debug!, syscall_body! |
| `kernel/src/net/socket.rs#sys_listen` | `arceos/api/arceos_posix_api/src/imp/net.rs#sys_listen` | 同板块同名 | SOCKET_TABLE.lock, SocketType::Tcp, get_socket_id_from_fd, table.get_mut | debug!, syscall_body! |

#### 文件系统
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `kernel/src/fs/mod.rs#readdir` | `arceos/ulib/axlibc/c/dirent.c#readdir` | 同板块同名 | Err, Ok, buf.len, child.mode.file_type, children.len, emit_dirent64 ...(+6) | unimplemented |
| `kernel/src/fs/mod.rs#rename` | `arceos/modules/axfs-ng/src/fs/fat/dir.rs#rename` | 同板块同名 | Arc::ptr_eq, Err, TREE.lock, clone_component, insert_child_prepared, is_node_read_only ...(+12) | Err, dir.rename, dir.rename(src_name, dst_dir.inner.borrow(&fs), dst_name)
            .map_err, dst_dir.downcast, dst_dir.downcast().map_err, dst_dir.inner.borrow ...(+4) |
| `kernel/src/user.rs#sys_renameat2` | `xapi/src/fs/ctl.rs#sys_renameat2` | 同板块同名 | copy_user_c_string, crate::fs::stat, crate::fs::stat(&new_resolved).is_ok, resolve_path_from_user, sys_renameat | Ok, Path::new, fs.resolve_nonexistent, fs.resolve_parent, new_path.into, old_dir.rename ...(+4) |
| `kernel/src/block.rs#write_at` | `arceos/modules/axfs-ng/src/fs/fat/file.rs#write_at` | 同板块同名 | (block_size - block_offset)
            .min, (block_size - block_offset)
            .min(input.len() - written)
            .min, Ok, Vec::new, device.as_ref, device.block_size ...(+9) | Ok, SeekFrom::Start, file.seek, file.seek(SeekFrom::Start(offset)).map_err, file.write, file.write(buf).map_err ...(+2) |

#### 设备驱动
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `kernel/src/device.rs#register_driver` | `arceos/tools/raspi4/chainloader/src/driver.rs#register_driver` | 同板块同名 | Arc::clone, DEVICES.lock, DRIVERS.lock, Ok, Some, crate::println! ...(+10) | Some, self.inner.lock |
| `kernel/src/virtio.rs#share` | `arceos/modules/axdriver/src/virtio.rs#share` | 同板块同名 | VirtAddr::new, buffer.as_ptr, buffer.as_ptr().cast::<u8>, buffer.as_ref, buffer.as_ref().len, panic! ...(+2) | buffer.as_ptr, vaddr.into, virt_to_phys, virt_to_phys(vaddr.into()).into |
| `kernel/src/virtio.rs#mmio_phys_to_virt` | `arceos/modules/axdriver/src/ixgbe.rs#mmio_phys_to_virt` | 同板块同名 | NonNull::new, NonNull::new(pointer).expect, PhysAddr::new, crate::arch::memory::phys_access::mmio_mut_ptr::<u8>, crate::arch::memory::phys_access::mmio_mut_ptr::<u8>(physical)
            .unwrap_or_else, panic! | NonNull::new, NonNull::new(phys_to_virt(paddr.into()).as_mut_ptr()).unwrap, paddr.into, phys_to_virt, phys_to_virt(paddr.into()).as_mut_ptr |
| `kernel/src/block.rs#flush` | `arceos/tools/raspi4/chainloader/src/bsp/device_driver/bcm/bcm2xxx_pl011_uart.rs#flush` | 同板块同名 | Ok, self.device.flush, self.device.write_block, self.state.lock | cpu::nop, self.registers.FR.matches_all |

#### 网络协议栈
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `kernel/src/net/socket.rs#from_bytes` | `arceos/ulib/axlibc/c/network.c#inet_pton` | 同模块上下文对齐/差异观察 | Some, addr.as_mut_bytes, addr.as_mut_bytes().as_mut_ptr, bytes.as_ptr, bytes.len, core::mem::size_of::<Self> ...(+1) | hexval, inet_pton, isdigit, memmove |
| `kernel/src/net/socket.rs#new` | `xcore/src/net/socket.rs#sendto` | 同模块上下文对齐/差异观察 | SocketType::Tcp, SocketType::Udp, Vec::new | Err, Ipv4Addr::LOCALHOST.into, Ok, SocketAddr::new, inner
                    .bind, inner
                    .bind(SocketAddr::new(Ipv4Addr::LOCALHOST.into(), 0))
                    .ok ...(+2) |
| `kernel/src/net/socket.rs#as_mut_bytes` | `xcore/src/net/socket.rs#recvfrom` | 同模块上下文对齐/差异观察 | core::mem::size_of::<Self>, core::slice::from_raw_parts_mut | Ok, Some, tcpsocket.lock, tcpsocket.lock().recv, tcpsocket.lock().recv(buf).map, udpsocket
                .lock ...(+5) |
| `kernel/src/net/socket.rs#as_bytes` | `arceos/api/arceos_posix_api/src/imp/net.rs#shutdown` | 同模块上下文对齐/差异观察 | core::mem::size_of::<Self>, core::slice::from_raw_parts | Ok, tcpsocket.lock, tcpsocket.peer_addr, tcpsocket.shutdown, udpsocket.lock, udpsocket.peer_addr ...(+1) |

#### 文件系统/第三方库
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `kernel/src/user.rs#sdcard_install_ext4_dir_files` | `arceos/crates/lwext4_rust/src/inode/file.rs#read_at` | 同模块上下文对齐/差异观察 | alloc::format!, alloc::string::String::from, crate::block::open_device, crate::ext4::list_directory, crate::fs::install_ext4_path, crate::fs::install_ext4_path("/dev/vda", &vfs_path, &ext4_path).is_ok ...(+6) | (inode as *const _ as *const u8).add, (pos + buf.len() as u64).min, Ok, WritebackGuard::new, assert!, buf.as_mut_ptr ...(+22) |
| `kernel/src/ext4.rs#open` | `arceos/crates/lwext4_rust/src/inode/file.rs#write_at` | 同模块上下文对齐/差异观察 | (32..=1024).contains, 1024_u64
            .checked_shl, 1024_u64
            .checked_shl(log_block_size)
            .ok_or, Err, Ok, block_size.is_power_of_two ...(+10) | Ok, assert!, assert_eq!, buf.as_ptr, buf.is_empty, buf.len ...(+15) |
| `kernel/src/main.rs#oscomp_materialize_ext4_dir_flat` | `arceos/crates/lwext4_rust/src/blockdev.rs#new` | 同模块上下文对齐/差异观察 | alloc::format!, alloc::sync::Arc::clone, crate::block::open_device, crate::ext4::list_directory, crate::println!, fs::mkdir ...(+5) | Box::new, Ok, Some, block_buf.as_mut_ptr, block_cache_buf.as_mut, block_dev_iface.as_mut ...(+10) |
| `kernel/src/ext4.rs#read_root_dir_entries` | `arceos/modules/axfs-ng/src/fs/ext4/inode.rs#create` | 同模块上下文对齐/差异观察 | Err, Ok, String::from_utf8, String::from_utf8(name_bytes.to_vec())
                        .map_err, data.get, data.get(offset + 6).ok_or ...(+14) | DirEntry::new_dir, DirEntry::new_file, DirNode::new, Err, FileNode::new, Inode::new ...(+11) |

#### 网络协议栈/第三方库
仅参考项目中观察到该模块代表函数，目标项目没有同模块可对齐证据：
- 参考项目：`arceos/modules/axnet/src/smoltcp_impl/tcp.rs#connect`，调用摘要：Err, Ok, Ok::<(IpEndpoint, IpEndpoint), NetError>, SOCKET_SET
                .with_socket_mut::<tcp::Socket, _, _>, SOCKET_SET.add, SocketSetWrapper::new_tcp_socket ...(+33)
- 参考项目：`arceos/modules/axnet/src/smoltcp_impl/dns.rs#query`，调用摘要：Err, IpAddr::from, Ok, SOCKET_SET
            .with_socket_mut::<dns::Socket, _, _>, SOCKET_SET
            .with_socket_mut::<dns::Socket, _, _>(handle, |socket| {
                socket.start_query(iface.lock().context(), name, query_type)
            })
            .map_err, SOCKET_SET.poll_interfaces ...(+11)
- 参考项目：`arceos/modules/axnet/src/smoltcp_impl/bench.rs#bench_transmit_bandwidth`，调用摘要：(current_time - past_time).secs, AxNetTxToken::consume, InterfaceWrapper::current_time, info!, self.transmit, tx_buf[0..12].fill ...(+2)
- 参考项目：`arceos/modules/axnet/src/smoltcp_impl/tcp.rs#recv_timeout`，调用摘要：Err, Ok, SOCKET_SET.with_socket_mut::<tcp::Socket, _, _>, current_ticks, self.block_on, self.handle.get ...(+9)

#### 第三方库
仅参考项目中观察到该模块代表函数，目标项目没有同模块可对齐证据：
- 参考项目：`arceos/crates/page_table_multiarch/page_table_multiarch/src/bits64.rs#map_region`，调用摘要：Err, M::flush_tlb, Ok, PageSize::Size1G.is_aligned, PageSize::Size2M.is_aligned, PageSize::Size4K.is_aligned ...(+12)
- 参考项目：`arceos/crates/axdriver_crates/axdriver_virtio/src/net.rs#try_new`，调用摘要：Err, InnerDev::new, InnerDev::new(transport).map_err, NetBufPool::new, Ok, Some ...(+14)
- 参考项目：`arceos/crates/page_table_multiarch/page_table_multiarch/src/bits64.rs#protect_region`，调用摘要：Err, Ok, TlbFlushAll::new, assert!, error!, self.protect ...(+5)
- 参考项目：`arceos/crates/page_table_multiarch/page_table_multiarch/src/arch/loongarch64.rs#flush_tlb`，调用摘要：asm!

#### 通用模块/其他
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `kernel/src/rtc.rs#ioctl` | `arceos/ulib/axlibc/c/ioctl.c#ioctl` | 同板块同名 | (tm_year - 1900).to_le_bytes, Err, Ok, buf[0..4].copy_from_slice, buf[12..16].copy_from_slice, buf[16..20].copy_from_slice ...(+22) | unimplemented |
| `sync/src/spin_lock.rs#lock` | `arceos/modules/axsync/src/mutex.rs#lock` | 同板块同名 | self
                .locked
                .compare_exchange_weak, self
                .locked
                .compare_exchange_weak(false, true, Ordering::Acquire, Ordering::Relaxed)
                .is_ok, self.locked.load, spin_loop | assert_ne!, current, current().id, current().id().as_u64, self.is_locked, self.owner_id.compare_exchange_weak ...(+1) |
| `kernel/src/ipi.rs#reset` | `xcore/src/sys/time.rs#reset` | 同板块同名 | self.coalesced.store, self.doorbells.store, self.handled_batches.store, self.interrupts.store, self.pending.store, self.spurious_interrupts.store | 内部闭环逻辑 |
| `runtime/src/console.rs#write_str` | `arceos/modules/axlog/src/lib.rs#write_str` | 同板块同名 | C::write_byte, Ok, text.bytes | Ok, cfg_if::cfg_if! |


好的，审计官。以下是基于您提供的证据底座、双方画像及控辩主张，进行的最终同源性定性仲裁。

### 1. 宏观判定
- **定性结论**: **独立开发，底层架构显著差异**
- **核心依据**: 客观 AST 相似度仅为 **0.64%**，共同 AST 指纹仅 32 个，远低于判定同源性的阈值。目标项目与参考项目在进程管理、内存管理、任务调度等核心板块的覆盖率均低于 2%，属于因 POSIX 系统调用同名、Rust 语言标准库调用及通用编程惯例产生的背景噪声，不构成系统性复用或抄袭。

### 2. 全模块覆盖矩阵解读
| 模块 | 双方存在 | 共同指纹 | 覆盖率 (目标/参考) | 证据强度 | 解读 |
| :--- | :--- | :--- | :--- | :--- | :--- |
| **进程管理** | 是 | 3 | 1.95% / 1.89% | **弱** | 覆盖率极低。共同指纹源于 `sys_wait4` 等 POSIX 强制同名系统调用入口及 `set_clear_child_tid` 等单行原子操作，属于接口规范收敛与通用 getter/setter 模式，非逻辑复用。 |
| **任务调度/异步运行时** | 是 | 1 | 0.31% / 0.28% | **未形成** | 覆盖率可忽略不计。双方调度器实现（目标基于 `WaitQueue`/`Completion`，参考基于 `axtask`/`run_queue`）在底层机制上完全不同。 |
| **内存管理** | 是 | 3 | 0.37% / 0.77% | **弱** | 覆盖率极低。共同指纹如 `sys_mmap` 为系统调用入口，`dma_alloc` 为设备驱动通用需求，内部实现（目标用 `PageAllocationOptions`，参考用 `global_allocator`）完全不同。 |
| **中断/异常处理** | 是 | 0 | 0.0% / 0.0% | **未形成** | 无共同 AST 指纹。双方 Trap 处理框架（目标 `kernel/src/trap.rs`，参考 `axhal/src/arch/.../trap.rs`）无结构重合。 |
| **系统调用** | 是 | 0 | 0.0% / 0.0% | **未形成** | 无共同 AST 指纹。目标项目直接在内核实现系统调用，参考项目大量通过 `arceos_posix_api` 宏封装转发，架构分层完全不同。 |
| **文件系统** | 是 | 0 | 0.0% / 0.0% | **未形成** | 无共同 AST 指纹。目标项目有自研 VFS (`kernel/src/fs/mod.rs`)，参考项目使用 `axfs-ng` 及 `lwext4_rust` 等第三方库，实现路径迥异。 |
| **设备驱动** | 是 | 0 | 0.0% / 0.0% | **未形成** | 无共同 AST 指纹。双方驱动模型（目标 `kernel/src/device.rs`，参考 `axdriver` 框架）及具体驱动实现无重合。 |
| **网络协议栈** | 是 | 0 | 0.0% / 0.0% | **未形成** | 无共同 AST 指纹。目标项目有自研 Socket 实现，参考项目基于 `smoltcp` 并封装，底层协议栈完全不同。 |
| **第三方库/公共依赖** | 部分 | 0 | 0.0% / 0.0% | **未形成** | 参考项目大量使用 `arceos` 生态库，目标项目无此依赖。无共同指纹，表明无共享第三方代码库复用。 |

### 3. 底层 AST 关键证据指纹表
| 所属板块 | 目标项目路径 | 参考项目路径 | 证据类型 | 技术说明 |
| :--- | :--- | :--- | :--- | :--- |
| 进程管理 | `kernel/src/process.rs#set_clear_child_tid` | `xcore/src/task/proc.rs#set_clear_child_tid` | 同板块同名同结构 | 均为 `self.clear_child_tid.store` 单行赋值。这是 Linux `set_tid_address` ABI 的必然实现，属于原子操作的标准用法，不构成可保护的创造性表达。 |
| 进程管理 | `kernel/src/process.rs#clear_child_tid_address` | `xcore/src/task/proc.rs#clear_child_tid` | 同板块证据 | 均为 `self.clear_child_tid.load` 单行读取。是 getter 的最简形式，命名差异表明独立封装。 |
| 进程管理 | `kernel/src/user.rs#sys_wait4` | `xapi/src/task/wait.rs#sys_wait4` | 同板块同名 | POSIX 强制系统调用入口。目标使用 `Arc::try_unwrap`/`ACTIVE` 全局表，参考使用 `XProcess`/`WaitOptions`，对象模型与错误处理路径完全不同。 |
| 进程管理 | `kernel/src/user.rs#sys_rt_sigtimedwait` | `xapi/src/task/signal.rs#sys_rt_sigtimedwait` | 同板块同名 | POSIX 强制系统调用入口。目标直接在内核态构造 `KernelSigInfo` 并拷贝，参考通过 `uspace` 高层抽象读取，调用对象与抽象层次完全不同。 |
| 进程管理 | `kernel/src/user.rs#sys_rt_sigprocmask` | `xapi/src/task/signal.rs#sys_rt_sigprocmask` | 同板块同名 | POSIX 强制系统调用入口。目标直接操作用户缓冲区，参考围绕 `XProcess`/`XThread` 封装，架构分层不同。 |
| 任务调度 | `kernel/src/user.rs#sys_clone` | `xapi/src/task/clone.rs#sys_clone` | 同板块同名 | POSIX 强制系统调用入口。目标调用 `child.create_initial_thread`，参考调用 `do_clone`，内部进程创建逻辑完全不同。 |
| 内存管理 | `kernel/src/user.rs#sys_mmap` | `xapi/src/mm/mmap.rs#sys_mmap` | 同板块同名 | POSIX 强制系统调用入口。目标使用 `VirtRange`/`MMAP_COUNT`，参考使用 `File::from_fd`/`align_up`，内存管理模型不同。 |
| 内存管理 | `kernel/src/virtio.rs#dma_alloc` | `arceos/modules/axdriver/src/virtio.rs#dma_alloc` | 同板块同名 | 设备驱动通用需求。目标使用 `PageAllocationOptions::dma32_zeroed`，参考使用 `global_allocator().alloc_pages`，底层分配器实现不同。 |
| 设备驱动 | `kernel/src/virtio.rs#share` | `arceos/modules/axdriver/src/virtio.rs#share` | 同板块同名 | virtio 设备驱动通用操作。目标使用 `VirtAddr::new`，参考使用 `virt_to_phys`，地址转换机制不同。 |
| 通用模块 | `sync/src/spin_lock.rs#lock` | `arceos/modules/axsync/src/mutex.rs#lock` | 同板块同名 | 自旋锁通用原语。目标使用 `compare_exchange_weak` 自旋，参考使用 `owner_id` 比较并记录持有者，实现策略不同。 |

### 4. 核心代码调用流对比剖析
* **【进程管理/进程退出与回收】**
* **底层技术析理**: 目标项目的进程退出 (`exit`/`drop`) 基于自定义的 `lifecycle` 原子状态机 (`THREAD_RUNNING` -> `THREAD_EXITING`) 和 `Completion` 通知机制，通过 `mark_child_zombie` 将子进程移入 `zombie_children` 列表。参考项目 (`exit`) 则基于 `Arc::downgrade` 和 `reaper` 父进程引用，通过操作 `thread group` (`tg.threads.remove`) 和 `children.lock` 来管理。两者在**状态管理模型、同步原语选择、父子关系维护的数据结构**上存在根本性架构差异。
* **代码溯源链接**:
  - 目标项目: `kernel/src/process.rs#exit`，`kernel/src/process.rs#drop`，`kernel/src/process.rs#mark_child_zombie`
  - 参考项目: `xmodules/xprocess/src/process.rs#exit`，`xmodules/xprocess/src/thread.rs#exit`

* **【进程管理/等待队列机制】**
* **底层技术析理**: 目标项目的等待队列 (`wait_timeout`/`wait`) 基于 `WaitQueue` 和 `Completion` 模型，通过 `wait_until_deadline` 和 `try_wait` 实现带超时的阻塞。参考项目 (`wait_timeout`/`wait`) 则深度集成 `axtask` 调度器，通过 `current_run_queue::<NoPreemptIrqSave>().blocked_resched` 和 `set_alarm_wakeup` 定时器机制实现。两者在**调度器集成方式、超时实现机制、底层同步原语**上完全不同。
* **代码溯源链接**:
  - 目标项目: `kernel/src/task/wait_queue.rs#wait_timeout`，`kernel/src/task/wait_queue.rs#wait`
  - 参考项目: `arceos/modules/axtask/src/wait_queue.rs#wait_timeout`，`arceos/modules/axtask/src/wait_queue.rs#wait`

### 5. 单方模块与降权项
*   **单方模块**: “网络协议栈/第三方库”与“第三方库”模块仅存在于参考项目中，如 `smoltcp_impl`、`page_table_multiarch` 等。这恰恰证明双方在第三方库选型上无任何重叠，是独立技术栈选型的强证据。
*   **降权项说明**:
    *   **跨板块同名函数**: 如目标 `kernel/src/task/m4c_verify.rs#wait_until` 与参考 `arceos/modules/axtask/src/wait_queue.rs#wait_until`，虽同名但分属测试验证代码与核心调度模块，职责完全不同，不构成同板块主证据。
    *   **通用 API 与宏**: 证据中大量出现的 `Arc::new`、`Atomic*::new`、`copy_from_user`、`assert!`、`println!` 等均为 Rust 语言标准库或内核开发的通用元素，其出现频率极高，不能作为代码复用的特异性指纹。
    *   **单行访问器**: `session`、`pending`、`process` 等函数均为对内部字段的简单 getter/setter，其 AST 结构单一，是面向对象编程的通用模式，不具原创性。

### 6. 保守结论
本次审计基于 0.64% 的极低 AST 相似度，认定目标项目 `[2026-T2026102699910462-XX]` 与参考项目 `[2025-StarryX-杭州电子科技大学]` 为**独立开发**，不存在系统性代码复用或抄袭。双方在进程管理、内存管理、任务调度等核心板块的少量共同指纹，均可归因于 POSIX 系统调用接口的同名强制收敛、Rust 语言标准原子操作及通用编程惯例。深入对比核心调用流（如进程退出、等待队列）揭示，双方在状态机模型、同步原语、调度器集成等关键架构决策上存在根本性差异。控方提出的“铁证”与“强证据”均系对单行访问器、跨板块函数及通用 API 调用的误读，不构成实质性同源指控。整体抄袭风险极低。

------------------------------

