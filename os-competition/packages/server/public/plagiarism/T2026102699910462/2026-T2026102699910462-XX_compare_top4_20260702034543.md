# 🛡️ OS 内核赛自动化代码级架构审计报告

**审计时间**: `2026-07-02 03:45:43`
**参赛作品名称**: `2026-T2026102699910462-XX`
**年份**: `2026`
**学校**: `XX`
**队伍名称**: `T2026102699910462`
**比赛名字**: `OS-Competition`
**比赛子赛道**: `Kernel-Implementation`
**仓库地址**: `https://gitlab.eduxiji.net/educg-group-43501-3132633/T2026102699910462-2452`
**推测开发语言**: `Rust / C (由AST探针自动识别)`
**特征函数量**: `2495`

## ⚔️ 底层代码同源性对比 (AST Homology)
> 本部分由专门的代码审查智能体，对该项目与符合年份条件的历届最高相似度项目进行控制流与底层调用对比。新版报告会先给出确定性的全模块覆盖矩阵，再给出 Agent 的分模块解释，避免只围绕少量抽样函数展开。

### 🏆 Top 4 底层对标: vs `2025-AstranciA-哈尔滨工业大学深圳`（哈尔滨工业大学（深圳） / AstranciA）

#### 对方作品信息

**对方年份**: `2025`
**对方学校**: `哈尔滨工业大学（深圳）`
**对方队伍名称**: `AstranciA`
**对方比赛名字**: `OS-Competition`
**对方比赛子赛道**: `Kernel-Implementation`
**对方仓库地址**: `https://gitlab.eduxiji.net/educg-group-36002-2710490/T202518123995667-1436`

> 双向覆盖率：共同 AST 指纹: 37；唯一 AST 指纹: 目标项目 2114，参考项目 3750；覆盖率: 目标项目 1.75%，参考项目 0.99%

## 全模块对比总览（确定性证据）

> 总体覆盖率：共同 AST 指纹: 37；唯一 AST 指纹: 目标项目 2114，参考项目 3750；覆盖率: 目标项目 1.75%，参考项目 0.99%

### 1. 模块覆盖矩阵
| 模块 | 目标函数数 | 参考函数数 | 共同唯一 AST 指纹 | Target 覆盖 | Base 覆盖 | 状态 |
| :--- | ---: | ---: | ---: | ---: | ---: | :--- |
| 进程管理 | 173 | 112 | 3 | 1.95% | 3.26% | 双方存在且有结构重合 |
| 任务调度/异步运行时 | 347 | 455 | 2 | 0.63% | 0.62% | 双方存在且有结构重合 |
| 内存管理 | 938 | 343 | 2 | 0.25% | 0.65% | 双方存在且有结构重合 |
| 中断/异常处理 | 96 | 71 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |
| 系统调用 | 29 | 136 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |
| 文件系统 | 348 | 620 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |
| 设备驱动 | 123 | 142 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |
| 网络协议栈 | 7 | 61 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |
| 文件系统/第三方库 | 39 | 775 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |
| 网络协议栈/第三方库 | 0 | 217 | 0 | 0.0% | 0.0% | 仅参考项目体现 |
| 第三方库 | 0 | 1155 | 0 | 0.0% | 0.0% | 仅参考项目体现 |
| 通用模块/其他 | 395 | 673 | 6 | 1.65% | 1.1% | 双方存在且有结构重合 |

### 2. 分模块证据清单

#### 进程管理
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `kernel/src/user.rs#sys_wait4` | `AstrancE/ulib/axsyscall/src/syscall_imp/process.rs#sys_wait4` | 同板块同名 | ACTIVE.load, Arc::try_unwrap, child.destroy, child.destroy().is_err, child.id, child.id().get ...(+10) | axmono::syscall::process::sys_wait4 |
| `kernel/src/user.rs#sys_rt_sigtimedwait` | `AstrancE/ulib/axmono/src/syscall/signal.rs#sys_rt_sigtimedwait` | 同板块同名 | copy_from_user, copy_from_user(set_address, &mut set_bytes).is_err, copy_plain_from_user::<KernelTimespec>, copy_to_user, copy_to_user(info_address, unsafe {
                core::slice::from_raw_parts(
                    &info as *const KernelSigInfo as *const u8,
                    core::mem::size_of::<KernelSigInfo>(),
                )
            })
            .is_err, core::cmp::min ...(+13) | crate::task::signal::sys_sigtimedwait, crate::task::signal::sys_sigtimedwait(set as _, info as _, timeout as _).map |
| `kernel/src/task/m4c_verify.rs#wait_until` | `AstrancE/modules/axtask/src/wait_queue.rs#wait_until` | 同板块同名 | SWITCH_RACE_QUEUE.debug_state, WAIT_QUEUE.debug_state, condition, crate::arch::time::counter, panic!, spin_loop ...(+4) | condition, crate::current, current_run_queue::<NoPreemptIrqSave>, rq.blocked_resched, self.cancel_events, self.queue.lock |
| `kernel/src/user.rs#sys_rt_sigprocmask` | `AstrancE/ulib/axmono/src/syscall/signal.rs#sys_rt_sigprocmask` | 同板块同名 | Some, copy_from_user, copy_from_user(set_address, &mut bytes).is_err, copy_to_user, copy_to_user(oldset_address, &old.to_ne_bytes()).is_err, core::mem::size_of::<u64> ...(+9) | crate::task::signal::sys_sigprocmask |

#### 任务调度/异步运行时
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `kernel/src/user.rs#sys_rt_sigsuspend` | `AstrancE/ulib/axmono/src/task/signal.rs#sys_rt_sigsuspend` | 同板块同名 | copy_from_user, copy_from_user(mask_address, &mut mask_bytes).is_err, crate::signal::unblockable_mask, crate::task::current_user_thread, crate::task::current_user_thread()
        .expect, crate::task::yield_now ...(+7) | (*mask_ref).into, Err, core::mem::size_of::<sigset_t>, curr.task_ext, curr.task_ext().process_data, curr.task_ext().process_data().signal.lock ...(+7) |
| `kernel/src/user.rs#sys_rt_sigaction` | `AstrancE/ulib/axmono/src/syscall/signal.rs#sys_rt_sigaction` | 同板块同名 | copy_plain_from_user::<crate::signal::KernelSigAction>, copy_plain_to_user, crate::signal::signal_bit, crate::signal::signal_bit(signal).is_none, crate::signal::unblockable_mask, current_process ...(+6) | crate::task::signal::sys_sigaction, crate::task::signal::sys_sigaction(signum, act as _, oldact as _).map_err, panic! |
| `kernel/src/tracked_spin.rs#try_lock` | `AstrancE/ulib/axstd/src/sync/mutex.rs#try_lock` | 同板块同名 | IrqSaveGuard::new, LockInstanceId::of, MigrationGuard::new, Some, cpu.get, crate::context::assert_interrupts_enabled ...(+8) | Some, api::ax_current_task_id, self
            .owner_id
            .compare_exchange, self
            .owner_id
            .compare_exchange(0, current_id, Ordering::Acquire, Ordering::Relaxed)
            .is_ok, self.data.get |
| `kernel/src/user.rs#sys_kill` | `AstrancE/ulib/axmono/src/task/signal.rs#sys_kill` | 同板块同名 | crate::process::ProcessId::from_raw_for_kernel, crate::process::lookup_process, crate::signal::send_signal, current_process, current_process().id, errno.to_isize | Ok, SigInfo_::Generic, Signal::from_u32, current, current().task_ext, current().task_ext().thread.process ...(+8) |

#### 内存管理
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `kernel/src/virtio.rs#dma_alloc` | `AstrancE/modules/axdriver/src/virtio.rs#dma_alloc` | 同板块同名 | DMA_ALLOCATIONS.lock, NonNull::dangling, NonNull::new, PageAllocationOptions::dma32_zeroed, allocation.range, allocation.range().start ...(+16) | NonNull::dangling, NonNull::new, NonNull::new(vaddr as _).unwrap, global_allocator, global_allocator().alloc_pages, paddr.as_usize ...(+2) |
| `kernel/src/page_alloc.rs#free` | `AstrancE/ulib/axlibc/src/malloc.rs#free` | 同板块同名 | Err, Ok, PAGE_ALLOCATOR.lock, allocator.begin_free, allocator.cancel_free, allocator.finish_free ...(+5) | Layout::from_size_align, Layout::from_size_align(size + CTRL_BLK_SIZE, 8).unwrap, assert!, dealloc, ptr.cast, ptr.cast::<MemoryControlBlock> ...(+3) |
| `kernel/src/virtio.rs#dma_dealloc` | `AstrancE/modules/axdriver/src/ixgbe.rs#dma_dealloc` | 同板块同名 | DMA_ALLOCATIONS.lock, allocations
                .iter, allocations
                .iter()
                .position, allocations.swap_remove, compiler_fence, crate::println! ...(+2) | BusAddr::from, Layout::from_size_align, Layout::from_size_align(size, 8).unwrap, dealloc_coherent |
| `kernel/src/fault.rs#handle_page_fault` | `AstrancE/modules/axmm/src/aspace/mod.rs#handle_page_fault` | 同板块同名 | fault.source, panic!, record_fault | area
                    .backend, area
                    .backend()
                    .clone, area
                    .backend()
                    .clone()
                    .handle_page_fault, area.flags, debug!, orig_flags.contains ...(+2) |

#### 中断/异常处理
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `kernel/src/trap.rs#handle_breakpoint` | `AstrancE/modules/axhal/src/arch/riscv/trap.rs#handle_breakpoint` | 同板块同名 | crate::user::handle_exception, frame.advance_pc, frame.exception_code, frame.previous_mode_was_user, mark_breakpoint_reached | debug! |
| `kernel/src/trap.rs#handle_breakpoint` | `AstrancE/modules/axhal/src/arch/loongarch64/trap.rs#handle_breakpoint` | 同板块同名 | crate::user::handle_exception, frame.advance_pc, frame.cause_code, frame.previous_mode_was_user, mark_breakpoint_reached | debug! |
| `kernel/src/user.rs#sys_clone3` | `AstrancE/ulib/axsyscall/src/syscall_imp/errno.rs#try_from` | 同模块上下文对齐/差异观察 | (stack as usize).checked_add, copy_from_user, copy_from_user(clone_args_address, &mut raw[..size]).is_err, field, raw[offset..offset + 8].try_into, raw[offset..offset + 8].try_into().expect ...(+2) | Err, Ok |
| `kernel/src/user.rs#handle_exception` | `AstrancE/modules/axhal/src/arch/loongarch64/unaligned.rs#emulate_load_store_insn` | 同模块上下文对齐/差异观察 | ACTIVE.load, EXIT_STATUS.store, FAULT_COUNT.fetch_add, LAST_FAULT_ADDRESS.store, LAST_FAULT_KIND.store, OSCOMP_LA_FPD_FIXUPS.fetch_add ...(+9) | asm!, badv::read, badv::read().raw, core::slice::from_raw_parts_mut, panic!, read_fpr ...(+5) |

#### 系统调用
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `kernel/src/user.rs#sys_setitimer` | `AstrancE/ulib/axmono/src/syscall/time.rs#sys_setitimer` | 同板块同名 | copy_plain_from_user::<KernelItimerval>, copy_plain_to_user | Err, ItimerConfig::default, Ok, Some, new_value.is_null, old_value.is_null ...(+1) |
| `kernel/src/net/socket.rs#sys_bind` | `AstrancE/api/arceos_posix_api/src/imp/net.rs#sys_bind` | 同板块同名 | SOCKET_TABLE.lock, SocketType::Tcp, SocketType::Udp, copy_addr_from_user, core::mem::size_of::<SockAddrIn>, get_socket_id_from_fd ...(+1) | debug!, syscall_body! |
| `kernel/src/net/socket.rs#sys_connect` | `AstrancE/api/arceos_posix_api/src/imp/net.rs#sys_connect` | 同板块同名 | SOCKET_TABLE.lock, SockAddrIn::new, SocketType::Tcp, copy_addr_from_user, core::mem::size_of::<SockAddrIn>, get_socket_id_from_fd ...(+1) | debug!, syscall_body! |
| `kernel/src/user.rs#sys_nanosleep` | `AstrancE/ulib/axsyscall/src/syscall_imp/time.rs#sys_nanosleep` | 同板块同名 | copy_plain_from_user::<KernelTimespec>, copy_plain_to_user, core::time::Duration::new, crate::timer::sleep, duration.is_zero | api::sys_nanosleep, unsafe { api::sys_nanosleep(req, rem) }.to_linux_result |

#### 文件系统
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `kernel/src/fs/mod.rs#readdir` | `AstrancE/ulib/axlibc/c/dirent.c#readdir` | 同板块同名 | Err, Ok, buf.len, child.mode.file_type, children.len, emit_dirent64 ...(+6) | unimplemented |
| `kernel/src/fs/mod.rs#rename` | `AstrancE/modules/axfs/src/fs/fatfs.rs#rename` | 同板块同名 | Arc::ptr_eq, Err, TREE.lock, clone_component, insert_child_prepared, is_node_read_only ...(+12) | debug!, self.0
            .rename, self.0
            .rename(src_path, &self.0, dst_path)
            .map_err |
| `kernel/src/block.rs#write_at` | `AstrancE/modules/axmm/src/shm/posix.rs#write_at` | 同板块同名 | (block_size - block_offset)
            .min, (block_size - block_offset)
            .min(input.len() - written)
            .min, Ok, Vec::new, device.as_ref, device.block_size ...(+9) | (self.size - offset).min, Err, Ok, buf.is_empty, buf.len, dst_part.len ...(+5) |
| `kernel/src/user.rs#sys_mount` | `AstrancE/api/arceos_posix_api/src/imp/fs.rs#sys_mount` | 同板块同名 | Some, copy_user_c_string, crate::fs::mount, errno.to_isize, resolve_user_path, source.as_deref | Ok, debug! |

#### 设备驱动
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `kernel/src/device.rs#register_driver` | `AstrancE/tools/raspi4/chainloader/src/driver.rs#register_driver` | 同板块同名 | Arc::clone, DEVICES.lock, DRIVERS.lock, Ok, Some, crate::println! ...(+10) | Some, self.inner.lock |
| `kernel/src/virtio.rs#share` | `AstrancE/modules/axdriver/src/virtio.rs#share` | 同板块同名 | VirtAddr::new, buffer.as_ptr, buffer.as_ptr().cast::<u8>, buffer.as_ref, buffer.as_ref().len, panic! ...(+2) | buffer.as_ptr, vaddr.into, virt_to_phys, virt_to_phys(vaddr.into()).into |
| `kernel/src/virtio.rs#mmio_phys_to_virt` | `AstrancE/modules/axdriver/src/ixgbe.rs#mmio_phys_to_virt` | 同板块同名 | NonNull::new, NonNull::new(pointer).expect, PhysAddr::new, crate::arch::memory::phys_access::mmio_mut_ptr::<u8>, crate::arch::memory::phys_access::mmio_mut_ptr::<u8>(physical)
            .unwrap_or_else, panic! | NonNull::new, NonNull::new(phys_to_virt(paddr.into()).as_mut_ptr()).unwrap, paddr.into, phys_to_virt, phys_to_virt(paddr.into()).as_mut_ptr |
| `kernel/src/block.rs#flush` | `AstrancE/tools/raspi4/chainloader/src/bsp/device_driver/bcm/bcm2xxx_pl011_uart.rs#flush` | 同板块同名 | Ok, self.device.flush, self.device.write_block, self.state.lock | cpu::nop, self.registers.FR.matches_all |

#### 网络协议栈
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `kernel/src/net/socket.rs#from_bytes` | `AstrancE/ulib/axlibc/c/network.c#inet_pton` | 同模块上下文对齐/差异观察 | Some, addr.as_mut_bytes, addr.as_mut_bytes().as_mut_ptr, bytes.as_ptr, bytes.len, core::mem::size_of::<Self> ...(+1) | hexval, inet_pton, isdigit, memmove |
| `kernel/src/net/socket.rs#new` | `AstrancE/api/arceos_posix_api/src/imp/net.rs#shutdown` | 同模块上下文对齐/差异观察 | SocketType::Tcp, SocketType::Udp, Vec::new | Ok, tcpsocket.lock, tcpsocket.peer_addr, tcpsocket.shutdown, udpsocket.lock, udpsocket.peer_addr ...(+1) |
| `kernel/src/net/socket.rs#as_mut_bytes` | `AstrancE/ulib/axlibc/c/socket.c#accept4` | 同模块上下文对齐/差异观察 | core::mem::size_of::<Self>, core::slice::from_raw_parts_mut | accept, fcntl |
| `kernel/src/net/socket.rs#as_bytes` | `AstrancE/api/arceos_posix_api/src/imp/net.rs#recvfrom` | 同模块上下文对齐/差异观察 | core::mem::size_of::<Self>, core::slice::from_raw_parts | Ok, Some, tcpsocket.lock, tcpsocket.lock().recv, tcpsocket.lock().recv(buf).map, udpsocket
                .lock ...(+2) |

#### 文件系统/第三方库
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `kernel/src/user.rs#sdcard_install_ext4_dir_files` | `AstrancE/crates/lwext4_rust/c/lwext4/src/ext4_balloc.c#ext4_balloc_alloc_block` | 同模块上下文对齐/差异观察 | alloc::format!, alloc::string::String::from, crate::block::open_device, crate::ext4::list_directory, crate::fs::install_ext4_path, crate::fs::install_ext4_path("/dev/vda", &vfs_path, &ext4_path).is_ok ...(+6) | ext4_balloc_get_bgid_of_block, ext4_balloc_get_block_of_bgid, ext4_balloc_set_bitmap_csum, ext4_balloc_verify_bitmap_csum, ext4_bg_get_block_bitmap, ext4_bg_get_free_blocks_count ...(+19) |
| `kernel/src/ext4.rs#open` | `AstrancE/crates/lwext4_rust/c/lwext4/src/ext4_journal.c#jbd_journal_prepare` | 同模块上下文对齐/差异观察 | (32..=1024).contains, 1024_u64
            .checked_shl, 1024_u64
            .checked_shl(log_block_size)
            .ok_or, Err, Ok, block_size.is_power_of_two ...(+10) | EXT4_BLOCK_ZERO, RB_FIND, TAILQ_FOREACH_REVERSE_SAFE, TAILQ_FOREACH_SAFE, TAILQ_REMOVE, ext4_bcache_clear_dirty ...(+17) |
| `kernel/src/main.rs#oscomp_materialize_ext4_dir_flat` | `AstrancE/crates/lwext4_rust/c/lwext4/src/ext4.c#ext4_dir_rm` | 同模块上下文对齐/差异观察 | alloc::format!, alloc::sync::Arc::clone, crate::block::open_device, crate::ext4::list_directory, crate::println!, fs::mkdir ...(+5) | EXT4_MP_LOCK, EXT4_MP_UNLOCK, ext4_block_cache_write_back, ext4_dir_en_get_inode, ext4_dir_iterator_fini, ext4_dir_iterator_init ...(+19) |
| `kernel/src/ext4.rs#read_root_dir_entries` | `AstrancE/crates/lwext4_rust/c/lwext4/src/ext4_dir_idx.c#ext4_dir_dx_split_index` | 同模块上下文对齐/差异观察 | Err, Ok, String::from_utf8, String::from_utf8(name_bytes.to_vec())
                        .map_err, data.get, data.get(offset + 6).ok_or ...(+14) | ext4_block_set, ext4_dir_dx_climit_get_count, ext4_dir_dx_climit_get_limit, ext4_dir_dx_climit_set_count, ext4_dir_dx_climit_set_limit, ext4_dir_dx_entry_get_hash ...(+10) |

#### 网络协议栈/第三方库
仅参考项目中观察到该模块代表函数，目标项目没有同模块可对齐证据：
- 参考项目：`AstrancE/crates/virtio-drivers/src/device/socket/connectionmanager.rs#send_recv`，调用摘要："Hello from guest".as_bytes, (hello_from_guest.len() as u32).into, (hello_from_host.len() as u32).into, 0.into, 50.into, Arc::new ...(+43)
- 参考项目：`AstrancE/crates/virtio-drivers/src/device/socket/connectionmanager.rs#incoming_connection`，调用摘要：0.into, 50.into, Arc::new, Mutex::new, ReadOnly::new, SocketType::Stream.into ...(+24)
- 参考项目：`AstrancE/crates/virtio-drivers/src/device/socket/connectionmanager.rs#poll`，调用摘要：Connection::new, Err, Ok, SocketError::OutputBufferTooShort, SocketError::OutputBufferTooShort(length).into, Some ...(+16)
- 参考项目：`AstrancE/crates/virtio-drivers/examples/x86_64/src/tcp.rs#test_echo_server`，调用摘要：Config::new, DeviceWrapper::new, Instant::from_micros_const, Interface::new, IpAddress::from_str, IpAddress::from_str(IP).unwrap ...(+44)

#### 第三方库
仅参考项目中观察到该模块代表函数，目标项目没有同模块可对齐证据：
- 参考项目：`AstrancE/crates/axdriver_crates/axdriver_block/src/dw_mshc/mmc.rs#card_init`，调用摘要：CID::from, CLKDIV::offset, CMD::data_cmd, CMD::no_data_cmd, CMD::no_data_cmd_no_crc, CMD::no_data_cmd_no_crc(card_idx, 2).with_response_length ...(+33)
- 参考项目：`AstrancE/crates/virtio-drivers/src/queue.rs#fake_read_write_queue`，调用摘要：(*available_ring).idx.load, (*used_ring).idx.fetch_add, (*used_ring).idx.load, Vec::new, assert!, assert_eq! ...(+18)
- 参考项目：`AstrancE/crates/virtio-drivers/src/device/sound/fake.rs#handle_control_request`，调用摘要：CommandCode::SOk.into, Some, Vec::new, VirtIOSndHdr {
                            command_code: CommandCode::SOk.into(),
                        }
                        .as_bytes, VirtIOSndHdr::read_from_prefix, VirtIOSndHdr::read_from_prefix(&request)
                .expect ...(+16)
- 参考项目：`AstrancE/crates/virtio-drivers/src/transport/pci.rs#new`，调用摘要：Err, Ok, Some, UniqueMmioPointer::new, VirtioPciError::InvalidDeviceId, VirtioPciError::InvalidNotifyOffMultiplier ...(+15)

#### 通用模块/其他
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `kernel/src/rtc.rs#ioctl` | `AstrancE/api/arceos_posix_api/src/imp/fd_ops.rs#ioctl` | 同板块同名 | (tm_year - 1900).to_le_bytes, Err, Ok, buf[0..4].copy_from_slice, buf[12..16].copy_from_slice, buf[16..20].copy_from_slice ...(+22) | Ok, warn! |
| `sync/src/spin_lock.rs#lock` | `AstrancE/modules/axsync/src/mutex.rs#lock` | 同板块同名 | self
                .locked
                .compare_exchange_weak, self
                .locked
                .compare_exchange_weak(false, true, Ordering::Acquire, Ordering::Relaxed)
                .is_ok, self.locked.load, spin_loop | assert_ne!, current, current().id, current().id().as_u64, self.is_locked, self.owner_id.compare_exchange_weak ...(+1) |
| `kernel/src/ipi.rs#reset` | `AstrancE/ulib/axmono/src/ctypes.rs#reset` | 同板块同名 | self.coalesced.store, self.doorbells.store, self.handled_batches.store, self.interrupts.store, self.pending.store, self.spurious_interrupts.store | 内部闭环逻辑 |
| `runtime/src/console.rs#write_str` | `AstrancE/modules/axlog/src/lib.rs#write_str` | 同板块同名 | C::write_byte, Ok, text.bytes | Ok, cfg_if::cfg_if! |


#### 1. 宏观判定
- **定性结论**: 底层架构显著差异 / 独立开发 (相似度: 0.63%，共同 AST 指纹: 37；唯一 AST 指纹: 目标项目 2114，参考项目 3750；覆盖率: 目标项目 1.75%，参考项目 0.99%)

#### 2. 全模块覆盖矩阵解读
- **进程管理**: 双方均存在该模块。存在 3 个共同 AST 指纹，目标覆盖率 1.95%，参考覆盖率 3.26%。共同指纹集中在 Linux ABI 强制的 `set_clear_child_tid` 等单节点操作及系统调用同名项上，未形成跨函数的连续调用链重合。证据强度：弱。
- **任务调度/异步运行时**: 双方均存在该模块。存在 2 个共同 AST 指纹，目标覆盖率 0.63%，参考覆盖率 0.62%。共同指纹为 `sys_rt_sigsuspend` 等系统调用同名项，但底层调度原语（如目标使用 `yield_now`，参考使用 `blocked_resched`）完全不同。证据强度：弱。
- **内存管理**: 双方均存在该模块。存在 2 个共同 AST 指纹，目标覆盖率 0.25%，参考覆盖率 0.65%。共同指纹为 `dma_alloc` 和 `handle_page_fault` 等通用操作，但内存分配器实现（目标使用 `PageAllocationOptions`，参考使用 `global_allocator`）和缺页处理流程异构。证据强度：弱。
- **中断/异常处理**: 双方均存在该模块。共同 AST 指纹为 0，覆盖率均为 0.0%。尽管存在 `handle_breakpoint` 等同名函数，但内部调用图谱无重合，目标项目包含复杂的用户态异常转发逻辑，参考项目仅为简单的 `debug!` 宏。证据强度：未形成。
- **系统调用**: 双方均存在该模块。共同 AST 指纹为 0，覆盖率均为 0.0%。存在 `sys_setitimer`、`sys_nanosleep` 等大量同名系统调用，但实现方式差异巨大，目标项目倾向于直接操作内核对象，参考项目多通过 `syscall_body!` 宏或转发函数实现。证据强度：未形成。
- **文件系统**: 双方均存在该模块。共同 AST 指纹为 0，覆盖率均为 0.0%。尽管存在 `readdir`、`rename` 等同名函数，但目标项目基于自定义的 `TREE` 锁和节点模型，参考项目则基于 `lwext4` 或 `fatfs` 等第三方库的封装。证据强度：未形成。
- **设备驱动**: 双方均存在该模块。共同 AST 指纹为 0，覆盖率均为 0.0%。`register_driver` 和 `dma_alloc` 等函数虽同名，但驱动模型和 DMA 分配策略完全不同。证据强度：未形成。
- **网络协议栈**: 双方均存在该模块。共同 AST 指纹为 0，覆盖率均为 0.0%。目标项目仅有 7 个函数，实现极为精简；参考项目有 61 个函数，并包含完整的 `virtio-drivers` 网络设备驱动。双方在规模和实现上差异巨大。证据强度：未形成。
- **第三方库/公共依赖**: 参考项目包含大量第三方库代码（如 `lwext4`、`virtio-drivers`），目标项目无对应模块。这些属于公共依赖或参考项目独有的外部代码，不构成同源证据。证据强度：未形成。

#### 3. 底层 AST 关键证据指纹表
| 所属板块 | 目标项目路径 | 参考项目路径 | 证据类型 | 技术说明 |
| :--- | :--- | :--- | :--- | :--- |
| 进程管理 | kernel/src/process.rs#set_clear_child_tid | AstrancE/ulib/axmono/src/task/process.rs#set_clear_child_tid | 同板块同名同结构 | 两者均为对 `clear_child_tid` 字段的单节点原子 store 操作，源自 Linux `set_tid_address` ABI 语义，属于接口强制表达，非逻辑复用。 |
| 进程管理 | kernel/src/process.rs#clear_child_tid_address | AstrancE/ulib/axmono/src/task/process.rs#clear_child_tid | 同板块证据 | 两者均为对 `clear_child_tid` 字段的单节点原子 load 操作，属于 ABI 强制字段访问。目标函数名加入 `address` 以明确语义，与参考函数职责不同。 |
| 进程管理 | kernel/src/user.rs#sys_wait4 | AstrancE/ulib/axsyscall/src/syscall_imp/process.rs#sys_wait4 | 同板块同名 | 参考为转发包装，目标为完整实现。目标调用 `ACTIVE.load`、`Arc::try_unwrap` 等，参考实际实现位于 `task/wait.rs` 并调用 `WaitOptions` 等，外部对象完全不同。 |
| 进程管理 | kernel/src/user.rs#sys_rt_sigtimedwait | AstrancE/ulib/axmono/src/syscall/signal.rs#sys_rt_sigtimedwait | 同板块同名 | 参考为转发包装。目标使用 `KernelSigInfo` 和手动字节切片拷贝，参考实际实现使用 `task_ext/process_data/signal` 锁和 `consume_one_in`，数据流模型异构。 |
| 进程管理 | kernel/src/user.rs#sys_rt_sigprocmask | AstrancE/ulib/axmono/src/syscall/signal.rs#sys_rt_sigprocmask | 同板块同名 | 参考为转发包装。目标使用 `update_mask` 辅助函数和 `to_ne_bytes`，参考直接操作 `process_data().signal` 锁和 `old_mask.bits`，掩码处理路径不同。 |
| 任务调度/异步运行时 | kernel/src/user.rs#sys_rt_sigsuspend | AstrancE/ulib/axmono/src/task/signal.rs#sys_rt_sigsuspend | 同板块同名 | 目标调用 `yield_now` 主动让出，参考调用 `blocked_resched` 阻塞调度，底层同步原语异构。 |
| 内存管理 | kernel/src/virtio.rs#dma_alloc | AstrancE/modules/axdriver/src/virtio.rs#dma_alloc | 同板块同名 | 目标使用 `PageAllocationOptions::dma32_zeroed` 分配，参考使用 `global_allocator().alloc_pages`，分配器后端不同。 |
| 内存管理 | kernel/src/fault.rs#handle_page_fault | AstrancE/modules/axmm/src/aspace/mod.rs#handle_page_fault | 同板块同名 | 目标直接 `panic!` 或记录故障，参考通过 `area.backend().handle_page_fault` 进行复杂处理，缺页处理策略完全不同。 |

#### 4. 核心代码调用流对比剖析
* **【进程管理/sys_wait4 系统调用】**
* **底层技术析理**: 目标项目在 `kernel/src/user.rs` 中直接实现了完整的进程等待逻辑，通过 `ACTIVE.load` 查找进程、`Arc::try_unwrap` 管理引用计数、`child.destroy` 销毁子进程，并调用 `copy_to_user` 写回 `rusage` 信息。参考项目在 `axsyscall` 层仅为一层转发包装，其真实逻辑位于 `ulib/axmono/src/task/wait.rs`，通过 `WaitOptions`、`WaitPid` 枚举和 `child.data::<ProcessData>()` 动态类型转换实现。两者在进程查找、状态管理和用户态数据拷贝的调用链上无重合。
* **代码溯源链接**:
  - 目标项目: `kernel/src/user.rs#sys_wait4`
  - 参考项目: `AstrancE/ulib/axsyscall/src/syscall_imp/process.rs#sys_wait4`

* **【进程管理/sys_rt_sigtimedwait 系统调用】**
* **底层技术析理**: 目标项目采用底层字节操作方式，通过 `copy_from_user` 读取原始 `set_bytes`，使用 `core::slice::from_raw_parts` 将 `KernelSigInfo` 转换为字节流，并通过 `copy_to_user` 写回。参考项目则通过高层抽象 `curr.task_ext().process_data().signal.lock().consume_one_in` 消费信号。目标项目直接操作内核结构体与用户态内存，参考项目则依赖任务扩展和进程数据的层级抽象，两者在信号获取与传递的调用流上存在本质差异。
* **代码溯源链接**:
  - 目标项目: `kernel/src/user.rs#sys_rt_sigtimedwait`
  - 参考项目: `AstrancE/ulib/axmono/src/syscall/signal.rs#sys_rt_sigtimedwait`

* **【内存管理/dma_alloc 操作】**
* **底层技术析理**: 目标项目在 `kernel/src/virtio.rs` 中实现 DMA 分配，通过 `DMA_ALLOCATIONS.lock` 管理分配记录，并使用 `PageAllocationOptions::dma32_zeroed` 指定分配策略。参考项目在 `modules/axdriver/src/virtio.rs` 中直接调用 `global_allocator().alloc_pages` 进行页面分配，并通过 `virt_to_phys` 转换地址。两者在分配器后端、物理地址获取方式和元数据管理上采用完全不同的调用路径。
* **代码溯源链接**:
  - 目标项目: `kernel/src/virtio.rs#dma_alloc`
  - 参考项目: `AstrancE/modules/axdriver/src/virtio.rs#dma_alloc`

#### 5. 单方模块与降权项
- **仅参考项目存在的模块**: `网络协议栈/第三方库`、`第三方库`。这些模块包含 `virtio-drivers`、`lwext4` 等外部开源库的封装或代码，目标项目未包含对应模块，不构成同源证据。
- **降权项说明**:
  - **公共接口/宏**: `Arc::clone`、`assert!`、`copy_from_user`、`core::mem::size_of` 等属于 Rust 标准库或操作系统内核通用原语，在任何同类项目中均会高频出现，不能作为特异性复用证据。
  - **跨板块同名函数**: 如 `wait_until`（目标位于验证桩，参考位于调度原语）、`drop`（目标处理进程生命周期，参考处理地址空间）等，虽在同板块下并列，但分属不同子领域，调用对象和数据结构完全不同，已降权为弱信号。
  - **Linux ABI 强制项**: `sys_wait4`、`sys_rt_sigprocmask` 等系统调用名及 `clear_child_tid` 等字段名由 Linux ABI 强制规定，同名不能作为复用证据。

#### 6. 保守结论
综合全模块 AST 指纹比对，目标项目与参考项目的整体相似度仅为 0.63%，共同指纹仅 37 个，覆盖率均低于 2%。控方提出的“复用指控”集中于进程管理模块的少数几个 Linux ABI 强制接口和单节点原子操作，这些证据在剔除语言级惯用法和公共接口后，无法形成跨函数的连续调用链重合。双方在内存管理、任务调度、文件系统等核心子系统的实现上，从抽象层次、同步原语到对象管理模型均表现出显著差异。因此，现有证据无法支持代码复用或抄袭的结论，目标项目应认定为独立开发。

------------------------------

