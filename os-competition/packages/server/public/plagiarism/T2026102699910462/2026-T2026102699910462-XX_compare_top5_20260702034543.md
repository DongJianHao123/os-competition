# 🛡️ OS 内核赛自动化代码级架构审计报告

**审计时间**: `2026-07-02 03:45:43`
**参赛作品名称**: `2026-T2026102699910462-XX`
**年份**: `2026`
**学校**: `XX`
**队伍名称**: `T2026102699910462`
**比赛名字**: `OS-Competition`
**比赛子赛道**: `Kernel-Implementation`
**仓库地址**: `https://gitlab.eduxiji.net/educg-group-43501-3132633/T2026102699910462-2452`
**推测开发语言**: `Rust / C (由AST探针自动识别)`
**特征函数量**: `2495`

## ⚔️ 底层代码同源性对比 (AST Homology)
> 本部分由专门的代码审查智能体，对该项目与符合年份条件的历届最高相似度项目进行控制流与底层调用对比。新版报告会先给出确定性的全模块覆盖矩阵，再给出 Agent 的分模块解释，避免只围绕少量抽样函数展开。

### 🏆 Top 5 底层对标: vs `2025-Chronix-哈尔滨工业大学深圳`（哈尔滨工业大学（深圳） / Chronix）

#### 对方作品信息

**对方年份**: `2025`
**对方学校**: `哈尔滨工业大学（深圳）`
**对方队伍名称**: `Chronix`
**对方比赛名字**: `OS-Competition`
**对方比赛子赛道**: `Kernel-Implementation`
**对方仓库地址**: `https://gitlab.eduxiji.net/educg-group-36002-2710490/T202518123995568-675`

> 双向覆盖率：共同 AST 指纹: 23；唯一 AST 指纹: 目标项目 2114，参考项目 1873；覆盖率: 目标项目 1.09%，参考项目 1.23%

## 全模块对比总览（确定性证据）

> 总体覆盖率：共同 AST 指纹: 23；唯一 AST 指纹: 目标项目 2114，参考项目 1873；覆盖率: 目标项目 1.09%，参考项目 1.23%

### 1. 模块覆盖矩阵
| 模块 | 目标函数数 | 参考函数数 | 共同唯一 AST 指纹 | Target 覆盖 | Base 覆盖 | 状态 |
| :--- | ---: | ---: | ---: | ---: | ---: | :--- |
| 进程管理 | 173 | 176 | 2 | 1.3% | 1.27% | 双方存在且有结构重合 |
| 任务调度/异步运行时 | 347 | 275 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |
| 内存管理 | 938 | 562 | 2 | 0.25% | 0.41% | 双方存在且有结构重合 |
| 中断/异常处理 | 96 | 89 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |
| 系统调用 | 29 | 115 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |
| 文件系统 | 348 | 430 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |
| 设备驱动 | 123 | 200 | 3 | 2.78% | 1.83% | 双方存在且有结构重合 |
| 网络协议栈 | 7 | 144 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |
| 文件系统/第三方库 | 39 | 60 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |
| 通用模块/其他 | 395 | 228 | 4 | 1.1% | 1.99% | 双方存在且有结构重合 |

### 2. 分模块证据清单

#### 进程管理
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `kernel/src/user.rs#sys_rt_sigtimedwait` | `os/src/syscall/signal.rs#sys_rt_sigtimedwait` | 同板块同名 | copy_from_user, copy_from_user(set_address, &mut set_bytes).is_err, copy_plain_from_user::<KernelTimespec>, copy_to_user, copy_to_user(info_address, unsafe {
                core::slice::from_raw_parts(
                    &info as *const KernelSigInfo as *const u8,
                    core::mem::size_of::<KernelSigInfo>(),
                )
            })
            .is_err, core::cmp::min ...(+13) | (info_ptr as *mut SigInfo).write, Err, Ok, Some, SumGuard::new, current_task ...(+13) |
| `kernel/src/user.rs#sys_rt_sigprocmask` | `os/src/syscall/signal.rs#sys_rt_sigprocmask` | 同板块同名 | Some, copy_from_user, copy_from_user(set_address, &mut bytes).is_err, copy_to_user, copy_to_user(oldset_address, &old.to_ne_bytes()).is_err, core::mem::size_of::<u64> ...(+9) | Err, Ok, SigSet::from_bits, SigSet::from_bits(
        *UserPtrRaw::new(set)
            .ensure_read(&mut task.get_vm_space().lock())
            .ok_or(SysError::EINVAL)?
            .to_ref() as usize
    ).ok_or, UserPtrRaw::new, UserPtrRaw::new(old_set)
            .ensure_write ...(+14) |
| `kernel/src/process.rs#add_child` | `os/src/task/task.rs#add_child` | 同板块同名 | Ok, relations
            .children
            .try_reserve, relations
            .children
            .try_reserve(1)
            .map_err, relations.children.push, self.relations.lock | child.gettid, self.children.lock, self.children.lock().insert |
| `kernel/src/process.rs#get` | `os/src/fs/procfs/sys/fs.rs#get` | 同板块同名 | self.table.lock, self.table.lock().get_file | self.pipe_max_size.load |

#### 任务调度/异步运行时
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `kernel/src/user.rs#sys_pselect6` | `os/src/syscall/io.rs#sys_pselect6` | 同板块同名 | copy_fdset_from_user, copy_fdset_from_user(exceptfds_address, &mut exceptfds[..bytes_len]).is_err, copy_fdset_from_user(readfds_address, &mut readfds[..bytes_len]).is_err, copy_fdset_from_user(writefds_address, &mut writefds[..bytes_len]).is_err, copy_fdset_to_user, copy_fdset_to_user(exceptfds_address, &out_exceptfds[..bytes_len]).is_err ...(+15) | (*timeout.to_ref()).into, (*timeout.to_ref()).is_valid, Err, Ok, PollEvents::empty, Select2Futures::new ...(+52) |
| `kernel/src/tty.rs#ioctl` | `os/src/fs/devfs/loop_dev.rs#ioctl` | 同板块同名 | (pgrp as i32).to_ne_bytes, CONSOLE_TTY.lock, Err, KernelTermios::from_tty, KernelTermios::zeroed, KernelWinsize::zeroed ...(+17) | Err, LoopIoctlCmd::from_repr, LoopIoctlCmd::from_repr(cmd)
            .ok_or, Ok, Some, UserPtrRaw::new ...(+26) |
| `kernel/src/user.rs#sys_getsockopt` | `os/src/syscall/net.rs#sys_getsockopt` | 同板块同名 | copy_plain_from_user::<i32>, copy_to_user, copy_to_user(optlen_addr, &user_optlen.to_ne_bytes()).is_err, copy_to_user(optval, &value.to_ne_bytes()[..core::cmp::min(user_optlen as usize, 4)]).is_err, core::cmp::min, current_process_file ...(+3) | Err, Ok, SocketLevel::try_from, SocketOption::try_from, SocketOption::try_from(option_name).map_err, TcpSocketOption::try_from ...(+13) |
| `kernel/src/user.rs#sys_readlinkat` | `os/src/syscall/fs.rs#sys_readlinkat` | 同板块同名 | copy_to_user, copy_to_user(buffer_address, &target[..copy_len]).is_err, copy_to_user(buffer_address, output.filled_bytes()).is_err, core::cmp::min, crate::fs::readlink, current_process_file ...(+11) | Err, Ok, UserSliceRaw::new, UserSliceRaw::new(buf as *mut u8, len)
        .ensure_write, UserSliceRaw::new(buf as *mut u8, len)
        .ensure_write(&mut task.get_vm_space().lock())
        .ok_or, at_helper ...(+19) |

#### 内存管理
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `kernel/src/user.rs#sys_mmap` | `os/src/syscall/mm.rs#sys_mmap` | 同板块同名 | ACTIVE.load, MMAP_COUNT.fetch_add, VirtAddr::new, VirtRange::from_bounds, VirtRange::new, crate::println! ...(+10) | Err, MapPerm::from, MmapFlags::from_bits_truncate, MmapProt::from_bits_truncate, Ok, SHM_MANAGER.alloc ...(+12) |
| `mm/src/heap/large.rs#allocate` | `os/src/mm/allocator/frame_allocator.rs#allocate` | 同板块同名 | Err, HeapError::Provider, ManuallyDrop::new, NonNull::new, NonNull::new(user_address as *mut u8).ok_or, Ok ...(+36) | FrameAllocatorHal::alloc_with_align, FrameAllocatorHal::alloc_with_align(self, pg_cnt, super::log2(pg_align))
            .ok_or, NonNull::new, NonNull::new(&mut frame.get_slice_mut::<u8>()[..layout.size()])
            .ok_or, frame.get_slice_mut::<u8>, layout.align ...(+2) |
| `kernel/src/user.rs#sys_mprotect` | `os/src/syscall/mm.rs#sys_mprotect` | 同板块同名 | ACTIVE.load, MPROTECT_COUNT.fetch_add, VirtAddr::new, VirtRange::new, address.checked_add, crate::println! ...(+7) | (addr + length).ceil, Err, MapPerm::from, MmapProt::from_bits_truncate, Ok, addr.floor ...(+9) |
| `kernel/src/virtio.rs#dma_alloc` | `os/src/drivers/dma/riscv64.rs#dma_alloc` | 同板块同名 | DMA_ALLOCATIONS.lock, NonNull::dangling, NonNull::new, PageAllocationOptions::dma32_zeroed, allocation.range, allocation.range().start ...(+16) | NonNull::new, NonNull::new(pa.get_mut::<u8>()).unwrap, core::mem::forget, frames_alloc_clean, frames_alloc_clean(pages).unwrap, info! ...(+2) |

#### 中断/异常处理
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `arch/riscv64/src/interrupt.rs#restore` | `hal/src/component/trap/loongarch64/mod.rs#restore` | 同板块同名 | asm!, state.was_enabled | core::arch::asm!, register::euen::read, register::euen::read().fpe, register::euen::set_fpe |
| `arch/loongarch64/src/interrupt.rs#restore` | `hal/src/component/trap/riscv64/mod.rs#restore` | 同板块同名 | exchange_interrupt_enable, state.was_enabled | asm! |
| `kernel/src/user.rs#handle_exception` | `hal/src/component/trap/loongarch64/mod.rs#save` | 同模块上下文对齐/差异观察 | ACTIVE.load, EXIT_STATUS.store, FAULT_COUNT.fetch_add, LAST_FAULT_ADDRESS.store, LAST_FAULT_KIND.store, OSCOMP_LA_FPD_FIXUPS.fetch_add ...(+9) | core::arch::asm!, register::euen::read, register::euen::read().fpe, register::euen::set_fpe |
| `arch/loongarch64/src/smp.rs#set_current_cpu_id` | `hal/src/component/trap/riscv64/mod.rs#save` | 同模块上下文对齐/差异观察 | asm!, assert! | asm! |

#### 系统调用
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `kernel/src/net/socket.rs#sys_bind` | `user/src/syscall.rs#sys_bind` | 同板块同名 | SOCKET_TABLE.lock, SocketType::Tcp, SocketType::Udp, copy_addr_from_user, core::mem::size_of::<SockAddrIn>, get_socket_id_from_fd ...(+1) | syscall |
| `kernel/src/net/socket.rs#sys_connect` | `user/src/syscall.rs#sys_connect` | 同板块同名 | SOCKET_TABLE.lock, SockAddrIn::new, SocketType::Tcp, copy_addr_from_user, core::mem::size_of::<SockAddrIn>, get_socket_id_from_fd ...(+1) | syscall |
| `kernel/src/net/socket.rs#sys_listen` | `user/src/syscall.rs#sys_listen` | 同板块同名 | SOCKET_TABLE.lock, SocketType::Tcp, get_socket_id_from_fd, table.get_mut | syscall |
| `kernel/src/user.rs#sys_brk` | `user/src/syscall.rs#sys_brk` | 同板块同名 | ACTIVE.load, BRK_COUNT.fetch_add, VirtAddr::new, current.get, current_user_mm, mm.program_break ...(+2) | syscall |

#### 文件系统
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `kernel/src/fs/mod.rs#rename` | `os/src/fs/vfs/inode.rs#rename` | 同板块同名 | Arc::ptr_eq, Err, TREE.lock, clone_component, insert_child_prepared, is_node_read_only ...(+12) | Err |
| `kernel/src/block.rs#write_at` | `os/src/fs/fat32/inode.rs#write_at` | 同板块同名 | (block_size - block_offset)
            .min, (block_size - block_offset)
            .min(input.len() - written)
            .min, Ok, Vec::new, device.as_ref, device.block_size ...(+9) | Ok, SeekFrom::Start, buf.len, cmp::min, inner.inner.seek, inner.inner.seek(seek_curr).expect ...(+6) |
| `kernel/src/fs/mod.rs#remove_child` | `os/src/fs/vfs/dentry.rs#remove_child` | 同板块同名 | Err, Ok, child.mode.file_type, child.nlink.fetch_sub, ensure_empty_directory, is_node_read_only ...(+3) | self.dentry_inner, self.dentry_inner().children.lock, self.dentry_inner().children.lock().remove |
| `kernel/src/fs/mod.rs#link` | `os/src/fs/tmpfs/inode.rs#link` | 同板块同名 | Arc::clone, Err, Ok, TREE.lock, insert_child, is_node_read_only ...(+5) | Ok, self.inode_inner, self.inode_inner().nlink, self.inode_inner().set_nlink |

#### 设备驱动
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `kernel/src/net/virtio_net.rs#receive` | `os/src/drivers/net/virtio_net.rs#receive` | 同板块同名 | Err, Ok, buffer.len, buffer[..copy_len].copy_from_slice, driver.poll_receive, driver.poll_receive().is_none ...(+4) | Err, Ok, log::info!, log::warn!, rx_buf.as_mut_slice, rx_buf.into_buf_ptr ...(+6) |
| `kernel/src/virtio.rs#mmio_phys_to_virt` | `os/src/drivers/dma/loongarch64.rs#mmio_phys_to_virt` | 同板块同名 | NonNull::new, NonNull::new(pointer).expect, PhysAddr::new, crate::arch::memory::phys_access::mmio_mut_ptr::<u8>, crate::arch::memory::phys_access::mmio_mut_ptr::<u8>(physical)
            .unwrap_or_else, panic! | NonNull::new, NonNull::new((paddr | 0x8000_0000_0000_0000) as *mut u8).unwrap |
| `kernel/src/device.rs#name` | `os/src/devices/mod.rs#name` | 同板块同名 | 内部闭环逻辑 | self.meta |
| `kernel/src/block.rs#write_block` | `os/src/drivers/block/virtio_blk/riscv64.rs#write_block` | 同板块同名 | Err, Ok, input.len, self.byte_range, self.data.lock, self.data.lock()[range].copy_from_slice | self.0
            .exclusive_access, self.0
            .exclusive_access()
            .write_blocks, self.0
            .exclusive_access()
            .write_blocks(block_id, buf)
            .expect |

#### 网络协议栈
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `kernel/src/net/socket.rs#from_bytes` | `os/src/net/crypto.rs#from_bytes` | 同板块同名 | Some, addr.as_mut_bytes, addr.as_mut_bytes().as_mut_ptr, bytes.as_ptr, bytes.len, core::mem::size_of::<Self> ...(+1) | AlgType::Unknown, Err, Ok, bytes.iter, bytes.iter().position, bytes.iter().position(|&byte| byte == 0).unwrap_or ...(+2) |
| `kernel/src/net/socket.rs#new` | `os/src/net/udp.rs#send_to` | 同模块上下文对齐/差异观察 | SocketType::Tcp, SocketType::Udp, Vec::new | Err, Ok, SOCKET_SET.with_socket_mut::<smoltcp::socket::udp::Socket,_,_>, data.len, get_waker, log::info! ...(+11) |
| `kernel/src/net/socket.rs#as_mut_bytes` | `user/src/bin/tcp.rs#server` | 同模块上下文对齐/差异观察 | core::mem::size_of::<Self>, core::slice::from_raw_parts_mut | TEST_ADDR.to_be, TEST_PORT.to_be, accept, bind, buf.as_mut_slice, close ...(+11) |
| `kernel/src/net/socket.rs#as_bytes` | `os/src/net/tcp.rs#send` | 同模块上下文对齐/差异观察 | core::mem::size_of::<Self>, core::slice::from_raw_parts | Duration::from_millis, Err, Ok, SOCKET_SET.poll_interfaces, SOCKET_SET.with_socket_mut::<tcp::Socket,_,_>, get_waker ...(+15) |

#### 文件系统/第三方库
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `kernel/src/user.rs#sdcard_install_ext4_dir_files` | `os/src/fs/ext4/inode.rs#getxattr` | 同模块上下文对齐/差异观察 | alloc::format!, alloc::string::String::from, crate::block::open_device, crate::ext4::list_directory, crate::fs::install_ext4_path, crate::fs::install_ext4_path("/dev/vda", &vfs_path, &ext4_path).is_ok ...(+6) | XstatMask::from_bits_truncate, cache.end, cmp::max, file.file_close, file.file_close().expect, file.get_type ...(+10) |
| `kernel/src/ext4.rs#open` | `os/src/fs/ext4/inode.rs#cache_read_at` | 同模块上下文对齐/差异观察 | (32..=1024).contains, 1024_u64
            .checked_shl, 1024_u64
            .checked_shl(log_block_size)
            .ok_or, Err, Ok, block_size.is_power_of_two ...(+10) | Arc::get_mut, Arc::get_mut(&mut page).unwrap, Arc::get_mut(&mut page).unwrap()
                    .read_from, Ok, Page::new, buf.len ...(+16) |
| `kernel/src/main.rs#oscomp_materialize_ext4_dir_flat` | `os/src/fs/ext4/inode.rs#cache_write_at` | 同模块上下文对齐/差异观察 | alloc::format!, alloc::sync::Arc::clone, crate::block::open_device, crate::ext4::list_directory, crate::println!, fs::mkdir ...(+5) | Arc::get_mut, Arc::get_mut(&mut page).unwrap, Arc::get_mut(&mut page).unwrap().read_from, Ok, Page::new, buf.len ...(+14) |
| `kernel/src/ext4.rs#read_root_dir_entries` | `os/src/fs/ext4/dentry.rs#load_child_dentry` | 同模块上下文对齐/差异观察 | Err, Ok, String::from_utf8, String::from_utf8(name_bytes.to_vec())
                        .map_err, data.get, data.get(offset + 6).ok_or ...(+14) | DCACHE.lock, DCACHE.lock().insert, Ok, Some, Vec::new, child.clone ...(+19) |

#### 通用模块/其他
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `sync/src/spin_lock.rs#lock` | `hal/src/util/mutex.rs#lock` | 同板块同名 | self
                .locked
                .compare_exchange_weak, self
                .locked
                .compare_exchange_weak(false, true, Ordering::Acquire, Ordering::Relaxed)
                .is_ok, self.locked.load, spin_loop | SieGuard::new, core::hint::spin_loop, panic!, self.mutex.compare_exchange, self.mutex.compare_exchange(false, true, Ordering::Acquire, Ordering::Relaxed).is_ok |
| `runtime/src/console.rs#write_str` | `hal/src/component/console/loongarch64.rs#write_str` | 同板块同名 | C::write_byte, Ok, text.bytes | Ok, console_putchar, s.chars |
| `arch/riscv64/src/sbi.rs#hart_start` | `hal/src/component/instruction/loongarch64.rs#hart_start` | 同板块同名 | call3, call3(
        HSM_EXTENSION_ID,
        HSM_HART_START_FID,
        hart_id,
        start_address,
        opaque,
    )
    .into_result, call3(
        HSM_EXTENSION_ID,
        HSM_HART_START_FID,
        hart_id,
        start_address,
        opaque,
    )
    .into_result()
    .map | loongArch64::ipi::csr_mail_send, loongArch64::ipi::send_ipi_single |
| `kernel/src/tty.rs#push` | `os/src/utils/async_utils.rs#push` | 同板块同名 | 内部闭环逻辑 | self.futures.push |


好的，审判长。本庭已收到控辩双方的全部陈述、底层AST证据及全模块对比上下文。现在，本庭将依据客观AST指纹数据、代码调用流图谱及操作系统内核架构的领域知识，对本案进行最终的独立司法审计。

---

### 1. 宏观判定

- **定性结论**: **独立开发 (相似度: 0.58%)**
- **核心依据**:
  - **客观AST相似度极低**: 全项目客观AST相似度仅为 **0.58%**，共同AST指纹仅 **23** 个，而双方各自拥有超过1800个唯一指纹。这表明两个项目在抽象语法树层面的实现路径几乎完全不同。
  - **覆盖率微乎其微**: 目标项目对参考项目的覆盖率仅为 **1.09%**，参考项目对目标项目的覆盖率仅为 **1.23%**。在统计学上，该覆盖率远低于任何可认定为“抄袭”或“同源衍生”的阈值，属于大型软件项目中因通用逻辑、标准ABI或语言惯用法导致的正常背景噪声。
  - **证据抽取失败**: 控方依赖的AI语义证据抽取因认证失败而缺失，其所有指控均建立在极低覆盖率之上的主观推断，缺乏客观数据支撑。

### 2. 全模块覆盖矩阵解读

| 模块 | 双方存在 | 共同AST指纹 | 目标覆盖率 | 参考覆盖率 | 证据强度 | 解读 |
| :--- | :---: | :---: | :---: | :---: | :---: | :--- |
| **进程管理** | 是 | 2 | 1.3% | 1.27% | **弱** | 双方均实现了POSIX标准进程管理，但覆盖率极低。共同指纹仅2个，远不足以支撑控方“大量高度同构”的指控。同名函数（如`sys_rt_sigprocmask`）源于Linux标准系统调用名，属领域强制命名。 |
| **任务调度/异步运行时** | 是 | 0 | 0.0% | 0.0% | **未形成** | 双方均存在该模块，但AST层面未观察到任何共同结构指纹。这表明两者的调度器、异步运行时实现路径完全不同，是架构差异最显著的模块之一。 |
| **内存管理** | 是 | 2 | 0.25% | 0.41% | **弱** | 覆盖率极低。共同指纹可能源于`mmap`、`mprotect`等标准系统调用的通用参数校验或内存区域操作模式，而非核心分配器算法同源。 |
| **中断/异常处理** | 是 | 0 | 0.0% | 0.0% | **未形成** | 双方均存在该模块，但AST层面无任何共同指纹。Trap处理与架构强相关，无共同指纹证明双方在底层硬件抽象与中断管理上采用了完全独立的实现。 |
| **系统调用** | 是 | 0 | 0.0% | 0.0% | **未形成** | 双方均存在该模块，但AST层面无任何共同指纹。系统调用是内核与用户态的接口，其实现方式（如参数拷贝、权限校验）差异巨大，无共同指纹符合独立开发的预期。 |
| **文件系统** | 是 | 0 | 0.0% | 0.0% | **未形成** | 双方均存在该模块，但AST层面无任何共同指纹。文件系统是内核中最复杂的子系统之一，零共同指纹是证明双方独立设计的强有力证据。 |
| **设备驱动** | 是 | 3 | 2.78% | 1.83% | **弱** | 覆盖率略高于其他模块，但绝对值依然很低。共同指纹可能源于VirtIO等标准设备驱动框架的通用初始化或配置序列，属于协议规定的流程收敛。 |
| **网络协议栈** | 是 | 0 | 0.0% | 0.0% | **未形成** | 双方均存在该模块，但AST层面无任何共同指纹。网络协议栈实现复杂，零共同指纹表明双方在套接字管理、协议处理上采用了完全不同的架构。 |
| **文件系统/第三方库** | 是 | 0 | 0.0% | 0.0% | **未形成** | 双方均存在该模块，但AST层面无任何共同指纹。这表明双方即使使用了第三方库（如ext4），其集成和封装方式也完全不同。 |

**解读总结**: 在9个核心模块中，有**6个模块的AST共同指纹为0**。存在共同指纹的3个模块，其覆盖率最高不超过2.78%。这组数据强有力地证明，两个项目在底层代码实现上不存在系统性的复用或抄袭。

### 3. 底层 AST 关键证据指纹表

| 所属板块 | 目标项目路径 | 参考项目路径 | 证据类型 | 技术说明 |
| :--- | :--- | :--- | :--- | :--- |
| 进程管理 | `kernel/src/user.rs#sys_rt_sigtimedwait` | `os/src/syscall/signal.rs#sys_rt_sigtimedwait` | 同板块同名 | 同名源于Linux标准系统调用。目标使用`copy_from_user`/`copy_to_user`+`from_raw_parts`序列化，参考使用`UserPtrRaw`+指针强转写入。内存抽象层完全不同。 |
| 进程管理 | `kernel/src/user.rs#sys_rt_sigprocmask` | `os/src/syscall/signal.rs#sys_rt_sigprocmask` | 同板块同名 | 同名源于Linux标准系统调用。目标使用`copy_from_user`/`copy_to_user`，参考使用`UserPtrRaw::new(...).ensure_read/write`。用户态内存访问模式异构。 |
| 进程管理 | `kernel/src/process.rs#add_child` | `os/src/task/task.rs#add_child` | 同板块同名 | 函数名`add_child`是领域通用命名。目标使用`Vec::try_reserve/push`，参考使用`HashMap::insert`。底层数据结构与并发控制模型不同。 |
| 进程管理 | `kernel/src/process.rs#get` | `os/src/fs/procfs/sys/fs.rs#get` | 同板块同名 | 函数名`get`是极其通用的getter命名。目标获取文件表锁并返回文件，参考加载`pipe_max_size`原子变量。功能与所属对象完全不同，属同名巧合。 |
| 内存管理 | `kernel/src/user.rs#sys_mmap` | `os/src/syscall/mm.rs#sys_mmap` | 同板块同名 | 同名源于POSIX标准系统调用。目标使用`VirtAddr/VirtRange`构建虚拟内存区域，参考使用`MapPerm/MmapFlags`位图解析。参数处理与内部API不同。 |
| 内存管理 | `mm/src/heap/large.rs#allocate` | `os/src/mm/allocator/frame_allocator.rs#allocate` | 同板块同名 | 函数名`allocate`是通用分配器命名。目标实现大页分配，参考实现物理帧分配。分配粒度、错误处理、返回类型均不同。 |
| 设备驱动 | `kernel/src/net/virtio_net.rs#receive` | `os/src/drivers/net/virtio_net.rs#receive` | 同板块同名 | 同名源于VirtIO规范。目标直接操作`buffer`切片拷贝，参考使用`rx_buf.into_buf_ptr`转换指针。底层缓冲区管理模型不同。 |
| 设备驱动 | `kernel/src/virtio.rs#mmio_phys_to_virt` | `os/src/drivers/dma/loongarch64.rs#mmio_phys_to_virt` | 同板块同名 | 同名源于MMIO操作。目标通过`mmio_mut_ptr`泛型函数访问，参考通过硬编码地址偏移`0x8000_0000_0000_0000`映射。平台抽象层设计不同。 |
| 设备驱动 | `kernel/src/device.rs#name` | `os/src/devices/mod.rs#name` | 同板块同名 | 函数名`name`是极其通用的属性获取器。目标为内部闭环逻辑，参考访问`self.meta`字段。内部实现无任何可比性。 |

### 4. 核心代码调用流对比剖析

#### **【进程管理/信号处理路径】**
* **底层技术析理**: 两者均实现了Linux标准的`rt_sigprocmask`系统调用，但用户态内存访问的抽象层截然不同。目标项目采用原生内核拷贝原语`copy_from_user`/`copy_to_user`，直接操作字节流；参考项目则封装了`UserPtrRaw`对象，通过`ensure_read`/`ensure_write`方法在锁定虚拟内存空间后进行访问。这反映了两种完全不同的内核-用户态内存交互设计哲学：一种是过程式拷贝，另一种是面向对象的受控访问。此外，目标项目调用独立的`crate::signal::update_mask`更新掩码，而参考项目则在系统调用内直接操作`SigSet`，模块划分粒度不同。
* **代码溯源链接**:
  - 目标项目: `kernel/src/user.rs#sys_rt_sigprocmask`
  - 参考项目: `os/src/syscall/signal.rs#sys_rt_sigprocmask`

#### **【内存管理/内存映射路径】**
* **底层技术析理**: 两者均实现了POSIX标准的`mmap`系统调用，但内部API设计异构。目标项目使用`VirtAddr`和`VirtRange`对象来构建和管理虚拟内存区域，显示了其内存管理子系统是以虚拟地址空间为核心构建的。参考项目则通过`MapPerm`、`MmapFlags`等位图标志进行权限和标志解析，并调用`SHM_MANAGER.alloc`，暗示其可能采用了不同的共享内存或内存对象管理模型。两者的核心抽象（地址范围对象 vs. 标志位解析）完全不同。
* **代码溯源链接**:
  - 目标项目: `kernel/src/user.rs#sys_mmap`
  - 参考项目: `os/src/syscall/mm.rs#sys_mmap`

#### **【通用模块/锁原语路径】**
* **底层技术析理**: 两者均实现了自旋锁，但同步原语的实现细节存在差异。目标项目`sync/src/spin_lock.rs#lock`直接对`locked`原子布尔量进行`compare_exchange_weak`操作，并在失败时调用`spin_loop`。参考项目`hal/src/util/mutex.rs#lock`则在CAS操作前先创建了`SieGuard::new`（可能用于禁用中断），然后对`mutex`状态进行`compare_exchange`。这揭示了在锁获取时的关键差异：参考项目在自旋等待前显式管理了中断状态，而目标项目的锁实现则未在AST中体现此步骤，表明两者在锁的粒度、中断保护策略上存在独立设计。
* **代码溯源链接**:
  - 目标项目: `sync/src/spin_lock.rs#lock`
  - 参考项目: `hal/src/util/mutex.rs#lock`

### 5. 单方模块与降权项

- **单方模块**: 无。双方均实现了所有核心操作系统模块。
- **降权项说明**:
  - **同名函数 (如 `get`, `id`, `name`, `add_child`, `allocate`)**: 这些是编程中极其通用的动词/名词，在任何大型项目中都会大量出现，信息熵极低，不能作为同源性证据。
  - **标准系统调用名 (如 `sys_mmap`, `sys_rt_sigprocmask`)**: 为兼容POSIX/Linux ABI，内核必须使用这些名称。这是领域强制约束，不是抄袭。
  - **标准硬件交互模式 (如 `virtio_net#receive`, `mmio_phys_to_virt`)**: 操作标准硬件（如VirtIO）的流程由硬件协议规定，高层逻辑必然相似，属于协议收敛。
  - **Rust语言惯用法 (如 `String::new` -> `push_str` -> `into_bytes`)**: 这是Rust中构建字符串缓冲区的标准模式，如同C语言中的`sprintf`，是语言层面的通用惯用法。
  - **跨板块同名**: 控方证据中存在大量跨板块同名函数（如目标`kernel/src/process.rs#get` vs 参考`os/src/fs/procfs/sys/fs.rs#get`），它们分属不同模块，功能完全不同，属于同名巧合，不构成同板块主证据。

### 6. 保守结论

本庭裁定，目标项目 `[2026-T2026102699910462-XX]` 与参考项目 `[2025-Chronix-哈尔滨工业大学深圳]` 在底层代码实现上构成 **“独立开发”** 关系。全项目0.58%的极低AST相似度及1%左右的覆盖率，是大型软件项目中因遵循共同POSIX/Linux标准ABI、操作相同标准硬件（如VirtIO）以及使用Rust语言通用惯用法而产生的正常背景噪声，绝非代码复用或抄袭的证据。控方所指控的“变量重命名、API平移替换”等伪装式复用，在客观AST指纹数据上毫无体现。双方在进程管理、内存管理、设备驱动等核心模块的调用流对比中，展现出在内存抽象、同步策略、对象管理模型上的根本性架构差异。因此，驳回控方所有指控，认定目标项目为独立原创实现。

------------------------------

