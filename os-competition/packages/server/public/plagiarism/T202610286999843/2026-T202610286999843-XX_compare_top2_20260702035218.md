# 🛡️ OS 内核赛自动化代码级架构审计报告

**审计时间**: `2026-07-02 03:52:18`
**参赛作品名称**: `2026-T202610286999843-XX`
**年份**: `2026`
**学校**: `XX`
**队伍名称**: `T202610286999843`
**比赛名字**: `OS-Competition`
**比赛子赛道**: `Kernel-Implementation`
**仓库地址**: `https://gitlab.eduxiji.net/educg-group-43501-3132633/T202610286999843-322`
**推测开发语言**: `Rust / C (由AST探针自动识别)`
**特征函数量**: `572`

## ⚔️ 底层代码同源性对比 (AST Homology)
> 本部分由专门的代码审查智能体，对该项目与符合年份条件的历届最高相似度项目进行控制流与底层调用对比。新版报告会先给出确定性的全模块覆盖矩阵，再给出 Agent 的分模块解释，避免只围绕少量抽样函数展开。

### 🏆 Top 2 底层对标: vs `2021-小白闯江湖队-国防科技大学`（国防科技大学 / 小白闯江湖队）

#### 对方作品信息

**对方年份**: `2021`
**对方学校**: `国防科技大学`
**对方队伍名称**: `小白闯江湖队`
**对方比赛名字**: `OS-Competition`
**对方比赛子赛道**: `Kernel-Implementation`
**对方仓库地址**: `https://gitlab.eduxiji.net/Misure/oskernel2021-x2`

> 双向覆盖率：共同 AST 指纹: 103；唯一 AST 指纹: 目标项目 454，参考项目 420；覆盖率: 目标项目 22.69%，参考项目 24.52%

## 全模块对比总览（确定性证据）

> 总体覆盖率：共同 AST 指纹: 103；唯一 AST 指纹: 目标项目 454，参考项目 420；覆盖率: 目标项目 22.69%，参考项目 24.52%

### 1. 模块覆盖矩阵
| 模块 | 目标函数数 | 参考函数数 | 共同唯一 AST 指纹 | Target 覆盖 | Base 覆盖 | 状态 |
| :--- | ---: | ---: | ---: | ---: | ---: | :--- |
| 进程管理 | 47 | 42 | 13 | 33.33% | 39.39% | 双方存在且有结构重合 |
| 任务调度/异步运行时 | 34 | 39 | 8 | 23.53% | 21.05% | 双方存在且有结构重合 |
| 内存管理 | 134 | 128 | 25 | 19.84% | 20.16% | 双方存在且有结构重合 |
| 中断/异常处理 | 18 | 21 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |
| 系统调用 | 91 | 7 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |
| 文件系统 | 98 | 71 | 9 | 10.98% | 13.24% | 双方存在且有结构重合 |
| 设备驱动 | 40 | 28 | 3 | 8.82% | 13.64% | 双方存在且有结构重合 |
| 网络协议栈 | 0 | 0 | 0 | 0.0% | 0.0% | 双方均未体现 |
| 文件系统/第三方库 | 33 | 0 | 0 | 0.0% | 0.0% | 仅目标项目体现 |
| 通用模块/其他 | 77 | 137 | 40 | 57.14% | 33.61% | 双方存在且有结构重合 |

### 2. 分模块证据清单

#### 进程管理
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `kernel/proc.c#scheduler` | `kernel/proc.c#scheduler` | 同板块同名 | acquire, intr_off, intr_on, mycpu, release, swtch ...(+1) | acquire, intr_on, mycpu, release, swtch |
| `user/sh.c#parseexec` | `xv6-user/sh.c#parseexec` | 同板块同名同结构 | execcmd, gettoken, panic, parseblock, parseredirs, peek | execcmd, gettoken, panic, parseblock, parseredirs, peek |
| `kernel/proc.c#procdump` | `kernel/proc.c#procdump` | 同板块同名 | NELEM, printf | NELEM, printf |
| `kernel/proc.c#sleep` | `kernel/proc.c#sleep` | 同板块同名 | acquire, myproc, release, sched | acquire, myproc, release, sched |

#### 任务调度/异步运行时
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `user/sh.c#runcmd` | `xv6-user/sh.c#runcmd` | 同板块同名 | close, dup, exec, exit, fork1, fprintf ...(+9) | close, dup, exec, exit, fork1, fprintf ...(+5) |
| `kernel/console.c#consoleintr` | `kernel/console.c#consoleintr` | 同板块同名 | C, acquire, consputc, procdump, release, wakeup | C, acquire, consputc, procdump, release, wakeup |
| `kernel/console.c#consoleread` | `kernel/console.c#consoleread` | 同板块同名 | C, acquire, either_copyout, killed, myproc, release ...(+1) | C, acquire, either_copyout, myproc, release, sleep |
| `user/sh.c#nulterminate` | `xv6-user/sh.c#nulterminate` | 同板块同名同结构 | nulterminate | nulterminate |

#### 内存管理
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `kernel/virtio_disk-la.c#virtio_disk_init` | `kernel/virtio_disk.c#virtio_disk_init` | 同板块同名 | __sync_synchronize, initlock, kalloc, memset, panic, pci_read16 ...(+13) | R, initlock, memset, panic, printf |
| `kernel/virtio_disk.c#virtio_disk_rw` | `kernel/virtio_disk.c#virtio_disk_rw` | 同板块同名 | R_DEV, __sync_synchronize, acquire, alloc3_desc, free_chain, panic ...(+3) | R, __sync_synchronize, acquire, alloc3_desc, free_chain, kvmpa ...(+2) |
| `kernel/sysfile.c#sys_exec` | `kernel/sysfile.c#sys_exec` | 同板块同名 | NELEM, argaddr, argstr, begin_op, end_op, fetchaddr ...(+11) | NELEM, argaddr, argstr, exec, fetchaddr, fetchstr ...(+3) |
| `kernel/sysfile.c#sys_open` | `kernel/sysfile.c#sys_open` | 同板块同名 | argint, argstr, begin_op, createat, end_op, fdalloc ...(+7) | argint, argstr, create, elock, ename, eput ...(+4) |

#### 中断/异常处理
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `kernel/virtio_disk-la.c#virtio_disk_intr` | `kernel/virtio_disk.c#virtio_disk_intr` | 同板块同名 | __sync_synchronize, acquire, panic, release, wakeup | R, acquire, panic, release, wakeup |
| `kernel/plic.c#plicinithart` | `kernel/plic.c#plicinithart` | 同板块同名 | PLIC_SENABLE, PLIC_SPRIORITY, cpuid | PLIC_MENABLE, PLIC_SENABLE, PLIC_SPRIORITY, cpuid, printf, readd |
| `kernel/trap-la.c#trapinit` | `kernel/trap.c#trapinit` | 同板块同名 | initlock, intr_on, w_csr_ecfg, w_csr_eentry, w_csr_merrentry, w_csr_tcfg ...(+1) | initlock, printf |
| `kernel/plic.c#plicinit` | `kernel/plic.c#plicinit` | 同板块同名 | 内部闭环逻辑 | printf, writed |

#### 系统调用
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `kernel/syscall.c#argstr` | `kernel/syscall.c#argstr` | 同板块同名 | argaddr, fetchstr | argaddr, fetchstr |
| `kernel/syscall.c#argaddr` | `kernel/syscall.c#argaddr` | 同板块同名 | argraw | argraw |
| `kernel/syscall.c#argint` | `kernel/syscall.c#argint` | 同板块同名 | argraw | argraw |
| `codex-basic-syscall-bundle/references/basic-src/getdents.c#test_getdents` | `kernel/syscall.c#sys_syslog` | 同模块上下文对齐/差异观察 | TEST_END, TEST_START, assert, close, getdents, open ...(+1) | __debug_error |

#### 文件系统
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `user/grind.c#go` | `xv6-user/grind.c#go` | 同板块同名 | chdir, close, dup, exec, exit, fork ...(+16) | chdir, close, dup, exec, exit, fork ...(+16) |
| `kernel/sysfile.c#sys_unlinkat` | `kernel/sysfile.c#sys_unlinkat` | 同板块同名 | argint, argstr, begin_op, dirlookup, end_op, ext4_is_ext4 ...(+11) | argint, argstr, elock, ename, eput, eunlock ...(+1) |
| `kernel/fs.c#dirlookup` | `kernel/fat32.c#dirlookup` | 同板块同名 | ext4_is_ext4, ext4_lookup, iget, namecmp, panic, readi | edup, eget, enext, eput, panic, reloc_clus ...(+2) |
| `kernel/sysfile.c#filewrite_kernel` | `kernel/file.c#filewrite_kernel` | 同板块同名 | begin_op, devsw[f->major].write, end_op, ilock, iunlock, pipewrite_kernel ...(+1) | devsw[f->major].write, elock, eunlock, ewrite, panic, pipewrite |

#### 设备驱动
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `kernel/uart-la.c#uartinit` | `kernel/uart.c#uartinit` | 同板块同名 | WriteReg, initlock | WriteReg, initlock |
| `kernel/uart-la.c#uartstart` | `kernel/uart.c#uartstart` | 同板块同名 | ReadReg, WriteReg, wakeup | ReadReg, WriteReg, wakeup |
| `kernel/uart-la.c#uartputc` | `kernel/uart.c#uartputc` | 同板块同名 | acquire, release, sleep, uartstart | acquire, release, sleep, uartstart |
| `user/sh.c#parseblock` | `xv6-user/sh.c#parseblock` | 同板块同名同结构 | gettoken, panic, parseline, parseredirs, peek | gettoken, panic, parseline, parseredirs, peek |

#### 文件系统/第三方库
仅目标项目中观察到该模块代表函数，参考项目没有同模块可对齐证据：
- 目标项目：`kernel/ext4.c#ext4_dirlink`，调用摘要：ext4_alloc_block, ext4_append_extent_block, ext4_bmap, ext4_free_block, ext4_iread, ext4_iwrite ...(+11)
- 目标项目：`kernel/ext4.c#ext4_extent_bmap`，调用摘要：ext4_extent_len, ext4_iread, ext4_read_full_block, kalloc, kfree
- 目标项目：`kernel/ext4.c#ext4_writei`，调用摘要：either_copyin, ext4_alloc_block, ext4_bmap, ext4_iread, ext4_iwrite, ext4_read_full_block ...(+6)
- 目标项目：`kernel/ext4.c#ext4_readdir`，调用摘要：either_copyout, ext4_bmap, ext4_read_full_block, kalloc, kfree, le16_to_cpu ...(+4)

#### 通用模块/其他
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `user/printf.c#vprintf` | `xv6-user/printf.c#vprintf` | 同板块同名 | printint, printptr, putc, va_arg | printint, printptr, putc, va_arg |
| `kernel/printf.c#printf` | `xv6-user/printf.c#printf` | 同板块同名 | acquire, consputc, printint, printptr, release, va_arg ...(+2) | va_start, vprintf |
| `kernel/spinlock.c#release` | `kernel/spinlock.c#release` | 同板块同名同结构 | __sync_lock_release, __sync_synchronize, holding, panic, pop_off | __sync_lock_release, __sync_synchronize, holding, panic, pop_off |
| `kernel/bio.c#bget` | `kernel/bio.c#bget` | 同板块同名同结构 | acquire, acquiresleep, panic, release | acquire, acquiresleep, panic, release |


好的，审计官。基于提供的证据底座、控辩双方陈述及原始AST抽样数据，本庭现对“[2026-T202610286999843-XX]”与“[2021-小白闯江湖队-国防科技大学]”的同源性争议进行最终定性仲裁。

### 1. 宏观判定
- **定性结论**: **独立开发，底层架构显著差异**
- **核心指标**: 客观 AST 相似度仅为 13.36%，远低于同源性阈值。共同 AST 指纹仅 103 个，而双方各自拥有 454 和 420 个唯一指纹，覆盖率均不足 25%。该数据从根本上不支持“系统性复用”或“抄袭”的指控，指向两个项目在庞大的代码库中仅共享了极小部分的公共模式。

### 2. 全模块覆盖矩阵解读
| 模块 | 双方存在 | 共同指纹 | 覆盖率 (目标/参考) | 证据强度 | 解读 |
| :--- | :---: | :---: | :---: | :---: | :--- |
| **进程管理** | 是 | 13 | 33.33% / 39.39% | **弱** | 覆盖率中等，但重合点集中在 xv6 标准函数（如 `myproc`, `yield`）和 thin-wrapper（如 `sys_getpid`）。控方所指的“铁证”均为领域通用实现或公共遗产继承，缺乏私有、非标准的结构指纹。 |
| **任务调度/异步运行时** | 是 | 8 | 23.53% / 21.05% | **弱** | 重合点同样为 xv6 经典实现（如 `consoleintr`, `runcmd`），属于共享同一教学操作系统祖先的正常现象，非定向抄袭证据。 |
| **内存管理** | 是 | 25 | 19.84% / 20.16% | **弱** | 覆盖率低。证据多为 `sys_exec`, `sys_open` 等标准 POSIX 系统调用路径，其调用链相似是接口规范所致。目标项目独有的 ext4 文件系统实现需要大量独立的内存管理逻辑，进一步削弱了同源性。 |
| **中断/异常处理** | 是 | 0 | 0.0% / 0.0% | **未形成** | 尽管双方均有此模块，但 AST 指纹覆盖率为 0。这强烈暗示双方在底层硬件抽象和 Trap 处理流程上存在根本性差异，与目标项目进行 LoongArch 架构移植的事实相符。 |
| **系统调用** | 是 | 0 | 0.0% / 0.0% | **未形成** | 覆盖率 0%。目标项目拥有 91 个系统调用函数，而参考项目仅 7 个，规模差异巨大。目标项目独立实现了庞大的 syscall 层，与参考项目无结构重合。 |
| **文件系统** | 是 | 9 | 10.98% / 13.24% | **弱** | 覆盖率极低。核心差异在于目标项目实现了完整的 **ext4** 文件系统，而参考项目基于 **FAT32**。这是完全不同的文件系统架构，是独立开发的有力证明。少量重合点（如 `dirlookup`）仅为通用 VFS 接口层的相似。 |
| **设备驱动** | 是 | 3 | 8.82% / 13.64% | **弱** | 覆盖率极低。重合点仅为 UART 等简单字符设备的通用初始化/读写模式。目标项目包含针对 LoongArch 的 `-la` 后缀驱动文件，体现了平台级独立开发。 |
| **网络协议栈** | 否 | 0 | 0.0% / 0.0% | **未形成** | 双方均未体现此模块。 |
| **文件系统/第三方库** | 仅目标 | 0 | 0.0% / 0.0% | **未形成** | 目标项目独有的 ext4 等第三方库代码，在参考项目中无任何对应，是独立开发的直接证据。 |
| **通用模块/其他** | 是 | 40 | 57.14% / 33.61% | **中** | 覆盖率较高，但证据集中在 `printf` 系列、`spinlock`、`bio` 等从 xv6 继承或标准库风格的代码。这些是构成操作系统骨架的公共基础组件，其相似性是可预期的。 |

### 3. 底层 AST 关键证据指纹表
| 所属板块 | 目标项目路径 | 参考项目路径 | 证据类型 | 技术说明 |
| :--- | :--- | :--- | :--- | :--- |
| 通用模块/其他 | `kernel/spinlock.c#release` | `kernel/spinlock.c#release` | 同板块同名同结构 | 自旋锁释放逻辑完全一致，包含 `__sync_lock_release` 等特定原语。这是 xv6 的标准实现，属于公共遗产。 |
| 通用模块/其他 | `kernel/bio.c#bget` | `kernel/bio.c#bget` | 同板块同名同结构 | 缓冲区缓存获取逻辑一致。这是 xv6 文件系统 Buffer Cache 层的经典实现，属于公共遗产。 |
| 进程管理 | `user/sh.c#parseexec` | `xv6-user/sh.c#parseexec` | 同板块同名同结构 | Shell 解析器调用链一致。这是 xv6 用户态 Shell 的标准递归下降解析器，属于公共遗产。 |
| 进程管理 | `kernel/proc.c#myproc` | `kernel/proc.c#myproc` | 同板块底层逻辑一致 | 获取当前进程指针的逻辑一致。这是 xv6 内核的经典 thin-wrapper，调用链 `mycpu, push_off, pop_off` 是架构约束下的必然实现。 |
| 进程管理 | `kernel/proc.c#yield` | `kernel/proc.c#yield` | 同板块底层逻辑一致 | 进程让出 CPU 的调用链一致。`acquire, myproc, release, sched` 是 xv6 定义的标准调度入口模式。 |
| 任务调度/异步运行时 | `kernel/console.c#consoleintr` | `kernel/console.c#consoleintr` | 同板块同名 | 控制台中断处理调用链一致。这是 xv6 的标准 TTY 驱动逻辑，属于公共遗产。 |
| 内存管理 | `kernel/sysfile.c#sys_exec` | `kernel/sysfile.c#sys_exec` | 同板块同名 | `exec` 系统调用实现路径相似。这是 POSIX 标准接口，其调用链（如 `begin_op`, `end_op`）是 xv6 文件系统事务模型的标准用法。 |
| 设备驱动 | `kernel/uart-la.c#uartputc` | `kernel/uart.c#uartputc` | 同板块同名 | UART 发送字符的同步逻辑一致。`acquire, sleep, uartstart, release` 是 xv6 UART 驱动的标准生产者-消费者模式。 |
| 文件系统 | `kernel/fs.c#dirlookup` | `kernel/fat32.c#dirlookup` | 同板块同名 | 目录项查找函数同名，但内部实现完全不同。目标项目调用 `ext4_*` 系列函数，参考项目调用 `eget`, `enext` 等 FAT32 函数，是 VFS 接口下的异构实现。 |

### 4. 核心代码调用流对比剖析
* **【进程管理/调度路径】**
* **底层技术析理**: 双方均遵循 xv6 的调度模型，即在 `yield` 中通过 `acquire` 持有进程锁，修改状态后调用 `sched` 进行上下文切换，最后由 `release` 释放锁。这是 xv6 教科书定义的标准算法流程，任何基于 xv6 的项目都会呈现此调用图谱。目标项目在 `scheduler` 函数中额外调用了 `intr_off` 和 `wfi`，这是针对 LoongArch 架构的独立电源管理与中断控制适配，体现了与参考项目（RISC-V）在硬件抽象上的差异。
* **代码溯源链接**:
  - 目标项目: `kernel/proc.c#yield`
  - 参考项目: `kernel/proc.c#yield`

* **【文件系统/路径解析】**
* **底层技术析理**: 双方都提供了 `dirlookup` 函数用于在目录中查找文件，这是虚拟文件系统（VFS）层的标准接口。然而，其底层实现揭示了根本性的架构分歧。目标项目的 `dirlookup` 直接调用 `ext4_lookup` 和 `ext4_is_ext4` 等 ext4 专用函数，操作的是 ext4 的 inode 和 extent 结构。参考项目的 `dirlookup` 则调用 `eget`, `enext`, `reloc_clus` 等函数，操作的是 FAT32 的目录项和簇链。这表明双方仅在 POSIX 兼容的 VFS 上层共享了函数名，其内部实现属于完全不同的文件系统世界。
* **代码溯源链接**:
  - 目标项目: `kernel/fs.c#dirlookup`
  - 参考项目: `kernel/fat32.c#dirlookup`

* **【系统调用/进程创建】**
* **底层技术析理**: 双方都实现了 `sys_clone` 系统调用，但调用路径截然不同。目标项目通过 `argaddr` 解析参数后，调用其独立的内核封装函数 `kclone`。参考项目则调用 `argaddr, argint, fetchaddr` 后，直接调用 `fork`。目标项目引入的 `k`-prefixed 内核函数族（`kfork`, `kexit`, `kkill` 等）是其独立设计的系统调用分发与内核内部实现之间的抽象层，这与参考项目直接将系统调用指向内核函数（如 `fork`, `exit`）的架构形成鲜明对比。
* **代码溯源链接**:
  - 目标项目: `kernel/sysproc.c#sys_clone`
  - 参考项目: `kernel/sysproc.c#sys_clone`

### 5. 单方模块与降权项
- **单方模块/第三方库**:
  - **目标项目独有**: `文件系统/第三方库` 模块下的 ext4 文件系统实现（`kernel/ext4.c` 中的 `ext4_dirlink`, `ext4_extent_bmap` 等）是目标项目独立开发的强有力证据，在参考项目中无任何对应。
  - **目标项目独有**: `codex_busy_box` 和 `codex-basic-syscall-bundle` 等用户态测试框架，属于独立的测试套件，与参考项目内核源码无同源关系。
  - **参考项目独有**: `kernel/dmac.c` 中的 DMA 控制器驱动，在目标项目中无对应，体现了硬件平台差异。
- **降权项说明**:
  - **跨板块硬凑**: 控方将目标项目的用户态测试代码（如 `codex-basic-syscall-bundle/.../waitpid.c`）与参考项目内核态的系统调用实现（如 `kernel/syscall.c#sys_syslog`）强行对齐，属于典型的跨板块、跨层次硬凑证据，不构成有效同源指控，已予降权。
  - **公共接口与泛型函数名**: 大量同名函数如 `printf`, `panic`, `memset`, `initlock` 等，属于 C 标准库或操作系统内核的通用基础设施，其存在和调用模式不能作为特定项目间抄袭的证据。`sys_getpid`, `sys_getuid` 等 thin-wrapper 的调用图谱由 POSIX 接口语义唯一确定，不具备指纹特异性。

### 6. 保守结论
本次仲裁的核心发现是，13.36% 的 AST 相似度与约 23% 的双向覆盖率，在统计学上显著不支持“抄袭”或“系统性复用”的指控。所有被控方标记为“铁证”的重合点，均可被“共同继承自 MIT xv6 教学操作系统”这一事实所合理解释，属于领域内基于公共遗产进行独立开发的正常现象。更重要的是，目标项目在**硬件架构（LoongArch 移植）、文件系统（ext4 实现）和内核抽象层（k-prefixed 函数族）**这三个核心维度上，展现了与参考项目（RISC-V, FAT32）根本性的、系统级的架构差异。这些差异无法通过简单的“洗稿”或“套壳”来实现，构成了独立开发的压倒性证据。因此，本庭裁定目标项目原创性成立，复用指控不成立。

------------------------------

