# 🛡️ OS 内核赛自动化代码级架构审计报告

**审计时间**: `2026-07-02 04:52:01`
**参赛作品名称**: `2026-T2026104239911569-XX`
**年份**: `2026`
**学校**: `XX`
**队伍名称**: `T2026104239911569`
**比赛名字**: `OS-Competition`
**比赛子赛道**: `Kernel-Implementation`
**仓库地址**: `https://gitlab.eduxiji.net/educg-group-43501-3132633/T2026104239911569-1510`
**推测开发语言**: `Rust / C (由AST探针自动识别)`
**特征函数量**: `1440`

## ⚔️ 底层代码同源性对比 (AST Homology)
> 本部分由专门的代码审查智能体，对该项目与符合年份条件的历届最高相似度项目进行控制流与底层调用对比。新版报告会先给出确定性的全模块覆盖矩阵，再给出 Agent 的分模块解释，避免只围绕少量抽样函数展开。

### 🏆 Top 4 底层对标: vs `2023-Runable-中山大学`（中山大学 / Runable）

#### 对方作品信息

**对方年份**: `2023`
**对方学校**: `中山大学`
**对方队伍名称**: `Runable`
**对方比赛名字**: `OS-Competition`
**对方比赛子赛道**: `Kernel-Implementation`
**对方仓库地址**: `https://gitlab.eduxiji.net/runable/oskernel2023-Runable`

> 双向覆盖率：共同 AST 指纹: 359；唯一 AST 指纹: 目标项目 1187，参考项目 2950；覆盖率: 目标项目 30.24%，参考项目 12.17%

## 全模块对比总览（确定性证据）

> 总体覆盖率：共同 AST 指纹: 359；唯一 AST 指纹: 目标项目 1187，参考项目 2950；覆盖率: 目标项目 30.24%，参考项目 12.17%

### 1. 模块覆盖矩阵
| 模块 | 目标函数数 | 参考函数数 | 共同唯一 AST 指纹 | Target 覆盖 | Base 覆盖 | 状态 |
| :--- | ---: | ---: | ---: | ---: | ---: | :--- |
| 进程管理 | 76 | 47 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |
| 任务调度/异步运行时 | 143 | 87 | 1 | 0.88% | 1.22% | 双方存在且有结构重合 |
| 内存管理 | 197 | 201 | 9 | 5.56% | 4.81% | 双方存在且有结构重合 |
| 中断/异常处理 | 15 | 6 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |
| 系统调用 | 13 | 66 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |
| 文件系统 | 234 | 345 | 2 | 1.17% | 0.63% | 双方存在且有结构重合 |
| 设备驱动 | 123 | 9 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |
| 网络协议栈 | 0 | 0 | 0 | 0.0% | 0.0% | 双方均未体现 |
| 文件系统/第三方库 | 446 | 443 | 343 | 82.45% | 84.9% | 双方存在且有结构重合 |
| 第三方库 | 1 | 6558 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |
| 通用模块/其他 | 192 | 115 | 1 | 0.63% | 1.1% | 双方存在且有结构重合 |

### 2. 分模块证据清单

#### 进程管理
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `kernel/src/syscall/process.rs#sys_exec` | `os/src/syscall/process.rs#sys_exec` | 同板块同名 | crate::task::exec_current | Vec::new, all_data.as_slice, app_inode.read_all, args.add, args_vec.len, args_vec.push ...(+7) |
| `kernel/src/syscall/process.rs#sys_munmap` | `os/src/syscall/process.rs#sys_munmap` | 同板块同名 | crate::task::munmap_current | align_down, align_up, current_process, current_process().inner_exclusive_access, current_process().inner_exclusive_access().memory_set.munmap |
| `kernel/src/syscall/process.rs#sys_exit` | `os/src/syscall/process.rs#sys_exit` | 同板块同名 | crate::task::exit_current_and_run_next | exit_current_and_run_next, panic!, println! |
| `kernel/src/syscall/process.rs#sys_mmap` | `os/src/syscall/process.rs#sys_mmap` | 同板块同名 | crate::task::mmap_current | align_down, align_up, current_process, inner.memory_set.mmap, process.inner_exclusive_access |

#### 任务调度/异步运行时
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `kernel/src/syscall/fs.rs#sys_chdir` | `os/src/syscall/fs.rs#sys_chdir` | 同板块同名 | core::str::from_utf8, crate::fs::lookup, crate::fs::normalize_path, crate::task::current_cwd, crate::task::read_current_user_bytes, crate::task::set_current_cwd ...(+4) | Arc::new, FileDescriptor::File, current_process, current_user_token, inner
        .work_dir
        .as_ref, inner
        .work_dir
        .as_ref()
        .open ...(+3) |
| `kernel/src/syscall/fs.rs#sys_read` | `os/src/syscall/fs.rs#sys_read` | 同板块同名 | crate::task::current_file, crate::task::write_current_user_bytes, crate::task::write_current_user_bytes(user_buf, &kbuf[..n]).is_none, file.read, file.readable, vec! | UserBuffer::new, current_process, current_user_token, drop, file.clone, file.read ...(+4) |
| `kernel/src/syscall/fs.rs#sys_getcwd` | `os/src/syscall/fs.rs#sys_getcwd` | 同板块同名 | bytes.len, crate::task::current_cwd, crate::task::write_current_user_bytes, crate::task::write_current_user_bytes(user_buf + bytes.len(), &[0]).is_none, crate::task::write_current_user_bytes(user_buf, bytes).is_none, cwd.as_bytes | UserBuffer::new, b[0..dir.len()].copy_from_slice, current_process, current_user_token, dir.len, inner.work_dir.name ...(+4) |
| `kernel/src/syscall/fs.rs#sys_write` | `os/src/syscall/fs.rs#sys_write` | 同板块同名 | crate::task::current_file, crate::task::read_current_user_bytes, file.writable, file.write | UserBuffer::new, current_process, current_user_token, drop, file.clone, file.writable ...(+5) |

#### 内存管理
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `kernel/src/mm/memory_set.rs#from_elf` | `os/src/mm/memory_set.rs#from_elf` | 同板块同名 | Elf64Header::parse, MapArea::new, MapPermission::R
                .union, MapPermission::R
                .union(MapPermission::W)
                .union, MemorySet::new_bare, Some ...(+14) | ((ph.virtual_addr() + ph.mem_size()) as usize).into, (ph.virtual_addr() as usize).into, MapArea::new, Self::new_bare, Some, USER_MMAP_BOTTOM.into ...(+24) |
| `kernel/src/mm/page_table/loongarch64.rs#find_pte_create` | `os/src/mm/page_table.rs#find_pte_create` | 同板块同名 | FrameTracker::new, PhysPageNum, Some, alloc_frame, raw_entry_array, self.frames.push ...(+1) | PageTableEntry::new, Some, frame_alloc, frame_alloc().unwrap, idxs.iter, idxs.iter().enumerate ...(+5) |
| `kernel/src/mm/frame_allocator.rs#init_frame_allocator` | `os/src/mm/frame_allocator.rs#init_frame_allocator` | 同板块同名 | FRAME_ALLOCATOR.init, FRAME_ALLOCATOR_LOCK.lock, FRAME_ALLOCATOR_LOCK.unlock, PhysAddr, PhysAddr(heap_end_pa).ceil, assert! ...(+5) | FRAME_ALLOCATOR.exclusive_access, FRAME_ALLOCATOR.exclusive_access().init, PhysAddr::from, PhysAddr::from(MEMORY_END).floor, PhysAddr::from(ekernel as usize).ceil |
| `kernel/src/mm/memory_set.rs#copy_data` | `os/src/mm/memory_set.rs#copy_data` | 同板块同名 | VirtAddr, VirtAddr(va).floor, core::cmp::min, core::ptr::copy_nonoverlapping, crate::mm::kernel_phys_to_virt, data.as_ptr ...(+5) | assert_eq!, current_vpn.step, data.len, dst.copy_from_slice, len.min, page_table
                .translate ...(+5) |

#### 中断/异常处理
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `kernel/src/trap/loongarch64/mod.rs#init` | `os/src/trap/mod.rs#trap_from_kernel` | 同模块上下文对齐/差异观察 | core::arch::asm!, log::info! | panic!, println! |
| `kernel/src/arch/loongarch64/mod.rs#disable_interrupt` | `user/src/bin/priv_inst.rs#main` | 同模块上下文对齐/差异观察 | core::arch::asm! | asm!, println! |
| `kernel/src/arch/loongarch64/mod.rs#enable_interrupt` | `os/src/trap/mod.rs#set_kernel_trap_entry` | 同模块上下文对齐/差异观察 | core::arch::asm! | stvec::write |
| `kernel/src/trap/riscv64/mod.rs#init` | `os/src/trap/mod.rs#set_user_trap_entry` | 同模块上下文对齐/差异观察 | asm!, trap_println! | stvec::write |

#### 系统调用
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `kernel/src/syscall/thread.rs#sys_thread_create` | `user/src/syscall.rs#sys_thread_create` | 同板块同名 | thread_create_current | syscall |
| `user/rust/ulib/src/syscall.rs#syscall6` | `os/src/syscall/sync.rs#sys_sleep` | 同模块上下文对齐/差异观察 | core::arch::asm! | current_user_token, get_time_us, println!, rem.add, req.add, suspend_current_and_run_next ...(+2) |
| `user/rust/ulib/src/syscall.rs#syscall6` | `os/src/syscall/sys_info.rs#new` | 同模块上下文对齐/差异观察 | core::arch::asm! | utsname::str2u8 |
| `user/rust/ulib/src/syscall.rs#syscall3` | `user/src/syscall.rs#syscall` | 同模块上下文对齐/差异观察 | core::arch::asm! | asm! |

#### 文件系统
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `kernel/src/fs/fatfs.rs#getdents` | `os/src/fatfs/file.rs#getdents` | 同板块同名 | DirEntry::new, Vec::new, dir.iter, e.file_name, e.is_dir, entries.push ...(+5) | SeekFrom::Start, core::slice::from_raw_parts_mut, d_name.clone_from_slice, dir.file_name, dir.file_name().as_bytes, dir.file_name().len ...(+3) |
| `kernel/src/fs/initramfs.rs#getdents` | `os/src/fs/inode.rs#getdents` | 同板块同名 | DirEntry::new, Vec::new, crate::loader::get_app_name, crate::loader::num_apps, entries.push, strip_numeric_prefix | self.inner
            .exclusive_access, self.inner
            .exclusive_access()
            .inode
            .exclusive_access, self.inner
            .exclusive_access()
            .inode
            .exclusive_access()
            .getdents |
| `kernel/src/fs/fatfs.rs#create` | `os/src/fatfs/file.rs#create` | 同板块同名 | Arc::new, Some, fs.root_dir, join_path, root.create_file, self.fs.clone ...(+3) | Some, dir.create_dir, dir.create_file |
| `kernel/src/fs/common_file.rs#getdents` | `os/src/fs/mod.rs#getdents` | 同板块同名 | buf.len, buf[start..end].copy_from_slice, entry.as_bytes, self.entries.len, self.offset.lock | 内部闭环逻辑 |

#### 设备驱动
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `kernel/src/block/ramdisk.rs#read_block` | `os/src/drivers/block/virtio_blk.rs#read_block` | 同板块同名 | block_id.checked_mul, buf.copy_from_slice, buf.len, self.data.len, start.checked_add | self.0
            .exclusive_access, self.0
            .exclusive_access()
            .read_block, self.0
            .exclusive_access()
            .read_block(block_id, buf)
            .expect |
| `kernel/src/block/virtio_blk.rs#write_block` | `os/src/drivers/block/virtio_blk.rs#write_block` | 同板块同名 | buf.len, self.write_sector_sync | self.0
            .exclusive_access, self.0
            .exclusive_access()
            .write_block, self.0
            .exclusive_access()
            .write_block(block_id, buf)
            .expect |
| `kernel/src/block/virtio_blk.rs#read_sector_sync` | `user/src/bin/adder_mutex_blocking.rs#main` | 同模块上下文对齐/差异观察 | core::hint::spin_loop, core::mem::size_of::<VirtioBlkReq>, core::ptr::write_bytes, core::slice::from_raw_parts, core::sync::atomic::fence, inner.last_used_idx.wrapping_add ...(+17) | Vec::new, argv[1].parse, argv[1].parse().unwrap, argv[2].parse, argv[2].parse().unwrap, assert_eq! ...(+6) |
| `kernel/src/block/virtio_blk.rs#write_sector_sync` | `user/src/bin/barrier_condvar.rs#block` | 同模块上下文对齐/差异观察 | core::hint::spin_loop, core::mem::size_of::<VirtioBlkReq>, core::slice::from_raw_parts_mut, core::sync::atomic::fence, dst.copy_from_slice, in_buf.len ...(+16) | condvar_signal, condvar_wait, mutex_lock, mutex_unlock, self.count.get |

#### 文件系统/第三方库
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `third_party/fatfs/src/fs.rs#format_volume` | `modified_dependencies/rust-fatfs/src/fs.rs#format_volume` | 同板块同名 | DirFileEntryData::new, Err, Ok, SeekFrom::End, SeekFrom::Start, Some ...(+28) | DirFileEntryData::new, Err, Ok, SeekFrom::End, SeekFrom::Start, alloc_cluster ...(+28) |
| `third_party/fatfs/src/table.rs#test_fat` | `modified_dependencies/rust-fatfs/src/table.rs#test_fat` | 同板块同名同结构 | ClusterIterator::<&mut S, S::Error, S>::new, [0xA_u32, 0x14_u32, 0x15_u32, 0x16_u32, 0x19_u32, 0x1A_u32]
                .iter, [0xA_u32, 0x14_u32, 0x15_u32, 0x16_u32, 0x19_u32, 0x1A_u32]
                .iter()
                .copied, [0xA_u32, 0x14_u32, 0x15_u32, 0x16_u32, 0x19_u32, 0x1A_u32]
                .iter()
                .copied()
                .map, [0xA_u32, 0x14_u32, 0x15_u32, 0x16_u32, 0x19_u32, 0x1A_u32]
                .iter()
                .copied()
                .map(Some)
                .collect::<Vec<_>>, assert! ...(+3) | ClusterIterator::<&mut S, S::Error, S>::new, [0xA_u32, 0x14_u32, 0x15_u32, 0x16_u32, 0x19_u32, 0x1A_u32]
                .iter, [0xA_u32, 0x14_u32, 0x15_u32, 0x16_u32, 0x19_u32, 0x1A_u32]
                .iter()
                .cloned, [0xA_u32, 0x14_u32, 0x15_u32, 0x16_u32, 0x19_u32, 0x1A_u32]
                .iter()
                .cloned()
                .map, [0xA_u32, 0x14_u32, 0x15_u32, 0x16_u32, 0x19_u32, 0x1A_u32]
                .iter()
                .cloned()
                .map(Some)
                .collect::<Vec<_>>, assert! ...(+3) |
| `third_party/fatfs/src/boot_sector.rs#determine_sectors_per_fat` | `modified_dependencies/rust-fatfs/src/boot_sector.rs#determine_sectors_per_fat` | 同板块同名同结构 | fat_type.bits_per_fat_entry, u32::from, u64::from | fat_type.bits_per_fat_entry, u32::from, u64::from |
| `third_party/fatfs/src/boot_sector.rs#format_bpb` | `modified_dependencies/rust-fatfs/src/boot_sector.rs#format_bpb` | 同板块同名 | Err, FatType::from_clusters, Ok, bpb.total_clusters, determine_fs_layout, error! ...(+5) | Err, FatType::from_clusters, Ok, assert!, bpb.total_clusters, debug_assert! ...(+15) |

#### 第三方库
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `kernel/src/block/virtio_mmio.rs#vendor_id` | `dependencies/virtio-drivers/src/header.rs#vendor_id` | 同板块同名 | self.read32 | self.vendor_id.read |

#### 通用模块/其他
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `user/rust/programs/library_system/src/main.rs#_start` | `user/src/lib.rs#_start` | 同板块同名 | LibraryManager::new, auth::login_or_register, exit, menu_loop, puts, storage::load_books ...(+3) | ((argv + i * core::mem::size_of::<usize>()) as *const usize).read_volatile, ((str_start + *i) as *const u8).read_volatile, (0usize..)
            .find, (0usize..)
            .find(|i| unsafe { ((str_start + *i) as *const u8).read_volatile() == 0 })
            .unwrap, HEAP.lock, HEAP.lock()
            .init ...(+10) |
| `kernel/src/sync/spin.rs#lock` | `user/src/bin/adder_peterson_spin.rs#lock` | 同板块同名 | self
            .locked
            .compare_exchange, self
            .locked
            .compare_exchange(false, true,
                core::sync::atomic::Ordering::Acquire,
                core::sync::atomic::Ordering::Relaxed)
            .is_err, spin_loop | compiler_fence, vload! |
| `kernel/src/sync/sync.rs#lock` | `os/src/sync/mutex.rs#lock` | 同板块同名 | core::hint::spin_loop, self
            .locked
            .compare_exchange, self
            .locked
            .compare_exchange(false, true, Ordering::Acquire, Ordering::Relaxed)
            .is_err | drop, self.locked.exclusive_access, suspend_current_and_run_next |
| `kernel/src/shutdown.rs#shutdown` | `os/src/sbi.rs#shutdown` | 同板块同名 | core::hint::spin_loop, core::ptr::write_volatile, crate::mm::kernel_phys_to_virt | system_reset, unreachable! |


#### 1. 宏观判定
- **定性结论**: 底层架构显著差异 / 独立开发 (相似度: 9.5%，共同 AST 指纹: 359；唯一 AST 指纹: 目标项目 1187，参考项目 2950；覆盖率: 目标项目 30.24%，参考项目 12.17%)

#### 2. 全模块覆盖矩阵解读
- **进程管理**: 双方均存在该模块。共同 AST 指纹为 0，双向覆盖率均为 0.0%。这表明两个项目在进程生命周期管理、调度实体抽象上采用了完全不同的代码结构，未观察到任何可被检测器识别的共同结构片段。证据强度：**未形成**。
- **任务调度/异步运行时**: 双方均存在该模块。共同 AST 指纹为 1，覆盖率极低（目标 0.88%，参考 1.22%）。这唯一的指纹源于文件系统系统调用中对路径处理的通用逻辑，而非调度器核心算法。双方调度器（Stride vs 简单调度）在 AST 层面无结构重合。证据强度：**弱**。
- **内存管理**: 双方均存在该模块。共同 AST 指纹为 9，覆盖率中等（目标 5.56%，参考 4.81%）。指纹集中在 ELF 加载、页表创建、帧分配器等基础操作上，这些是遵循硬件规范（如 RISC-V/LoongArch 页表格式）和通用内核模式的必然结果，属于领域共性实现。证据强度：**弱**。
- **中断/异常处理**: 双方均存在该模块。共同 AST 指纹为 0，双向覆盖率均为 0.0%。目标项目支持 LoongArch64 和 RISC-V 双架构，参考项目主要为 RISC-V，底层汇编及 trap 处理逻辑完全不同。证据强度：**未形成**。
- **系统调用**: 双方均存在该模块。共同 AST 指纹为 0，双向覆盖率均为 0.0%。尽管存在大量同名 POSIX 接口（如 `sys_fork`, `sys_exec`），但目标项目多为空壳转发，参考项目为直接实现，AST 调用链无交集。证据强度：**未形成**。
- **文件系统**: 双方均存在该模块。共同 AST 指纹为 2，覆盖率极低（目标 1.17%，参考 0.63%）。指纹源于 `getdents` 等通用目录操作，其核心的 FAT 文件系统实现均依赖于第三方库 `rust-fatfs`，自研部分未发现结构重合。证据强度：**弱**。
- **设备驱动**: 双方均存在该模块。共同 AST 指纹为 0，双向覆盖率均为 0.0%。目标项目包含 `ramdisk` 和 `virtio` 驱动，参考项目主要为 `virtio`，但实现路径和抽象层次不同。证据强度：**未形成**。
- **网络协议栈**: 双方均未体现该模块。
- **第三方库/公共依赖**: 双方均存在该模块。共同 AST 指纹为 343，覆盖率极高（目标 82.45%，参考 84.9%）。这明确指向双方都使用了 `rust-fatfs` 等相同的第三方库，这部分代码属于公共依赖，不应计入原创性比对。证据强度：**强（但属于公共依赖，非项目间复用）**。

#### 3. 底层 AST 关键证据指纹表
| 所属板块 | 目标项目路径 | 参考项目路径 | 证据类型 | 技术说明 |
| :--- | :--- | :--- | :--- | :--- |
| 第三方库/公共依赖 | `third_party/fatfs/src/fs.rs#format_volume` | `modified_dependencies/rust-fatfs/src/fs.rs#format_volume` | 同源库 | 双方均使用了相同的第三方 FAT 文件系统库 `rust-fatfs`，此部分 AST 高度重合是公共依赖所致。 |
| 第三方库/公共依赖 | `third_party/fatfs/src/table.rs#test_fat` | `modified_dependencies/rust-fatfs/src/table.rs#test_fat` | 同源库 | 同上，属于 `rust-fatfs` 库内部的单元测试代码，结构完全一致。 |
| 第三方库/公共依赖 | `third_party/fatfs/src/boot_sector.rs#determine_sectors_per_fat` | `modified_dependencies/rust-fatfs/src/boot_sector.rs#determine_sectors_per_fat` | 同源库 | 同上，属于 `rust-fatfs` 库的引导扇区计算逻辑。 |
| 内存管理 | `kernel/src/mm/memory_set.rs#from_elf` | `os/src/mm/memory_set.rs#from_elf` | 领域共性 | 双方均实现了 ELF 文件加载，需解析 ELF 头并建立内存映射，此流程由 ELF 标准定义，非项目独创。 |
| 内存管理 | `kernel/src/mm/page_table/loongarch64.rs#find_pte_create` | `os/src/mm/page_table.rs#find_pte_create` | 领域共性 | 多级页表创建和遍历逻辑，遵循 RISC-V/LoongArch 硬件规范，是内核开发的通用模式。 |
| 内存管理 | `kernel/src/mm/frame_allocator.rs#init_frame_allocator` | `os/src/mm/frame_allocator.rs#init_frame_allocator` | 领域共性 | 物理帧分配器的初始化，涉及物理地址计算和分配器结构初始化，是内核基础组件。 |
| 任务调度/异步运行时 | `kernel/src/syscall/fs.rs#sys_chdir` | `os/src/syscall/fs.rs#sys_chdir` | 弱信号 | 文件系统路径操作的系统调用实现，涉及路径解析和当前工作目录切换，属于 POSIX 标准操作。 |
| 文件系统 | `kernel/src/fs/fatfs.rs#getdents` | `os/src/fatfs/file.rs#getdents` | 弱信号 | 目录项读取操作，均依赖于 `rust-fatfs` 库提供的接口，自研部分无结构重合。 |

#### 4. 核心代码调用流对比剖析
* **【进程管理/进程创建与调度】**
* **底层技术析理**: 目标项目采用**进程-线程二级模型**与**Stride 步长调度**，参考项目采用**单级任务模型**与**简单调度**。目标项目的 `fork_from` 调用流为 `KernelStack::new` -> `kernel_stack.push_context` -> `TaskContext::goto_task_entry`，并包含 `parent_tickets.max` 的调度参数继承；参考项目的 `sys_fork` 调用流为 `current_process.fork` -> `task.inner_exclusive_access().get_trap_cx`。两者在调度实体的抽象层次（线程 vs 任务）、上下文构造方式（独立内核栈 vs 直接操作 trap 上下文）和调度算法（Stride vs 无）上存在根本性架构差异。
* **代码溯源链接**:
  - 目标项目: `kernel/src/task/task.rs#fork_from`
  - 参考项目: `os/src/syscall/process.rs#sys_fork`

* **【内存管理/物理内存分配】**
* **底层技术析理**: 两者都实现了物理帧分配器，但内存模型不同。目标项目 `init_frame_allocator` 使用 `FRAME_ALLOCATOR.init` 配合 `FRAME_ALLOCATOR_LOCK.lock/unlock` 进行初始化，显式管理锁；参考项目使用 `FRAME_ALLOCATOR.exclusive_access().init`，通过 `UPSafeCell` 封装内部可变性。目标项目直接操作 `PhysAddr` 进行计算，参考项目则通过 `PhysAddr::from` 构造。这体现了在内部可变性实现模式和物理地址抽象上的不同选择。
* **代码溯源链接**:
  - 目标项目: `kernel/src/mm/frame_allocator.rs#init_frame_allocator`
  - 参考项目: `os/src/mm/frame_allocator.rs#init_frame_allocator`

#### 5. 单方模块与降权项
- **单方模块**: 目标项目独有的 `user/src/shell.c` 等 C 语言用户态程序，参考项目独有的 `os/src/sync/condvar.rs` 条件变量实现、`user/src/bin/forktree.rs` 等 Rust 用户态测试程序。这些模块在对方项目中无对应实现，是独立开发的体现。
- **降权项**: 所有同名系统调用（如 `sys_exec`, `sys_fork`, `sys_mmap` 等）均为 POSIX/教学 OS 标准接口，其函数签名和功能由规范定义，不能作为复用证据。`Vec::new`, `Some`, `unwrap` 等 Rust 标准库泛型调用属于语言共性。第三方库 `rust-fatfs` 的代码重合属于公共依赖，已从原创性比对中排除。

#### 6. 保守结论
综合评估，目标项目与参考项目的总体 AST 相似度仅为 9.5%，且绝大部分重合源于共享的第三方库 `rust-fatfs`。在核心的进程管理、任务调度、中断处理等模块，共同 AST 指纹为零，覆盖率极低。双方在进程/线程模型、调度算法、同步机制、内存管理抽象上存在根本性架构差异。控方指控的“复用痕迹”均是基于功能相似性的主观推断，缺乏 AST 结构层面的客观证据支持。因此，目标项目 `[2026-T2026104239911569-XX]` 应被认定为独立开发，不存在对参考项目 `[2023-Runable-中山大学]` 的代码抄袭或结构性复用。

------------------------------

