# 🛡️ OS 内核赛自动化代码级架构审计报告

**审计时间**: `2026-07-02 05:19:25`
**参赛作品名称**: `2026-T2026104869910383-XX`
**年份**: `2026`
**学校**: `XX`
**队伍名称**: `T2026104869910383`
**比赛名字**: `OS-Competition`
**比赛子赛道**: `Kernel-Implementation`
**仓库地址**: `https://gitlab.eduxiji.net/educg-group-43501-3132633/T2026104869910383-3620`
**推测开发语言**: `Rust / C (由AST探针自动识别)`
**特征函数量**: `1349`

## ⚔️ 底层代码同源性对比 (AST Homology)
> 本部分由专门的代码审查智能体，对该项目与符合年份条件的历届最高相似度项目进行控制流与底层调用对比。新版报告会先给出确定性的全模块覆盖矩阵，再给出 Agent 的分模块解释，避免只围绕少量抽样函数展开。

### 🏆 Top 2 底层对标: vs `2025-StarryX-杭州电子科技大学`（杭州电子科技大学 / StarryX）

#### 对方作品信息

**对方年份**: `2025`
**对方学校**: `杭州电子科技大学`
**对方队伍名称**: `StarryX`
**对方比赛名字**: `OS-Competition`
**对方比赛子赛道**: `Kernel-Implementation`
**对方仓库地址**: `https://gitlab.eduxiji.net/educg-group-36002-2710490/T202510336995486-3037`

> 双向覆盖率：共同 AST 指纹: 181；唯一 AST 指纹: 目标项目 1188，参考项目 2897；覆盖率: 目标项目 15.24%，参考项目 6.25%

## 全模块对比总览（确定性证据）

> 总体覆盖率：共同 AST 指纹: 181；唯一 AST 指纹: 目标项目 1188，参考项目 2897；覆盖率: 目标项目 15.24%，参考项目 6.25%

### 1. 模块覆盖矩阵
| 模块 | 目标函数数 | 参考函数数 | 共同唯一 AST 指纹 | Target 覆盖 | Base 覆盖 | 状态 |
| :--- | ---: | ---: | ---: | ---: | ---: | :--- |
| 进程管理 | 71 | 172 | 5 | 7.46% | 3.14% | 双方存在且有结构重合 |
| 任务调度/异步运行时 | 245 | 480 | 8 | 3.46% | 2.28% | 双方存在且有结构重合 |
| 内存管理 | 308 | 430 | 12 | 4.41% | 3.08% | 双方存在且有结构重合 |
| 中断/异常处理 | 5 | 70 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |
| 系统调用 | 181 | 89 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |
| 文件系统 | 496 | 801 | 39 | 9.2% | 6.28% | 双方存在且有结构重合 |
| 设备驱动 | 0 | 139 | 0 | 0.0% | 0.0% | 仅参考项目体现 |
| 网络协议栈 | 0 | 77 | 0 | 0.0% | 0.0% | 仅参考项目体现 |
| 文件系统/第三方库 | 0 | 164 | 0 | 0.0% | 0.0% | 仅参考项目体现 |
| 网络协议栈/第三方库 | 0 | 166 | 0 | 0.0% | 0.0% | 仅参考项目体现 |
| 第三方库 | 0 | 349 | 0 | 0.0% | 0.0% | 仅参考项目体现 |
| 通用模块/其他 | 43 | 665 | 2 | 5.88% | 0.38% | 双方存在且有结构重合 |

### 2. 分模块证据清单

#### 进程管理
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `kernel/src/task/ops.rs#do_exit` | `xapi/src/task/exit.rs#do_exit` | 同板块同名 | FutexKey::new_current, SignalInfo::new_kernel, Some, axtask::yield_now, clear_child_tid.is_null, clear_child_tid.vm_write ...(+33) | FD_TABLE.clear, FutexKey::new, IPC_MANAGER.clear, SignalInfo::new, UserPtr::<Pid>::from, axtask::exit ...(+32) |
| `kernel/src/task/ops.rs#exit_robust_list` | `xapi/src/task/futex.rs#exit_robust_list` | 同板块同名 | axtask::yield_now, core::ptr::eq, entry.vm_read, entry.vm_read().map, handle_futex_death, head.vm_read ...(+2) | Err, Ok, UserPtr::from, axtask::yield_now, core::ptr::eq, handle_futex_death ...(+2) |
| `kernel/src/syscall/fs/pidfd.rs#sys_pidfd_getfd` | `xapi/src/fs/fd/pid.rs#sys_pidfd_getfd` | 同板块同名 | Err, FD_TABLE.scope, Ok, PidFd::from_fd, add_file_like, debug! ...(+7) | Err, FD_TABLE
        .deref_from, FD_TABLE
        .deref_from(&XProcess::from_thread(&thread).ns)
        .get, FD_TABLE
        .deref_from(&XProcess::from_thread(&thread).ns)
        .get(targetfd as _)
        .ok_or, Ok, PidFd::from_fd ...(+5) |
| `kernel/src/syscall/signal.rs#sys_rt_sigprocmask` | `xapi/src/task/signal.rs#sys_rt_sigprocmask` | 同板块同名 | Err, Ok, check_sigset_size, curr.as_thread, current, debug! ...(+7) | Err, Ok, XProcess::from_thread, XProcess::from_thread(thread).uspace, XThread::from_thread, check_sigset_size ...(+3) |

#### 任务调度/异步运行时
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `kernel/src/syscall/sync/futex.rs#sys_futex` | `xapi/src/task/futex.rs#sys_futex` | 同板块同名 | AxError::from, Err, FutexKey::new_current, Ok, Some, assert_unsigned ...(+22) | Err, FutexKey::new, Ok, axtask::yield_now, current, current().task_ext ...(+24) |
| `kernel/src/syscall/timer.rs#sys_timer_settime` | `xapi/src/sys/time.rs#sys_timer_settime` | 同板块同名 | Arc::new, AtomicBool::new, Duration::from_nanos, Err, Ok, POSIX_TIMERS.lock ...(+36) | Err, Ok, XProcess::from_thread, XProcess::from_thread(thread).uspace, clear_timer, info! ...(+6) |
| `kernel/src/syscall/signal.rs#sys_rt_sigsuspend` | `xapi/src/task/signal.rs#sys_rt_sigsuspend` | 同板块同名 | LinuxError::EINTR.code, Ok, Poll::Ready, Some, block_on, check_signals ...(+14) | Err, Some, check_signals, check_sigset_size, mem::replace, set.remove ...(+7) |
| `kernel/src/syscall/signal.rs#sys_kill` | `xapi/src/task/signal.rs#sys_kill` | 同板块同名 | Ok, Some, current, current().as_thread, current().as_thread().proc_data.proc.group, current().as_thread().proc_data.proc.group().pgid ...(+9) | Ok, get_process, get_process_group, make_siginfo, proc.is_init, process.group ...(+5) |

#### 内存管理
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `kernel/src/syscall/mm/mmap.rs#sys_mmap` | `xapi/src/mm/mmap.rs#sys_mmap` | 同板块同名 | (addr + length).align_up, Arc::new, Backend::new_alloc, Backend::new_cow, Backend::new_file, Backend::new_linear ...(+56) | (addr + length).align_up, Err, FD_TABLE.is_assigned, File::from_fd, File::from_fd(fd, FileFlags::READ, FileFlags::empty())
            .map_err, FileFlags::empty ...(+39) |
| `kernel/src/syscall/task/clone.rs#do_clone` | `xapi/src/task/clone.rs#do_clone` | 同板块同名 | (parent_tid as *mut Pid).vm_write, (parent_tid as *mut Pid).vm_write(tid).ok, (pidfd as *mut i32).vm_write, Arc::new, AxTaskExt::from_impl, Default::default ...(+81) | Arc::default, Arc::new, CloneFlags::from_bits_truncate, Err, FD_TABLE
                .deref_from, FD_TABLE
                .deref_from(&process_data.ns)
                .init_new ...(+76) |
| `kernel/src/syscall/ipc/shm.rs#sys_shmat` | `xapi/src/ipc/shm.rs#sys_shmat` | 同板块同名 | Arc::new, Backend::new_shared, Ok, SHM_MANAGER.lock, SharedPages::new, ShmAtFlags::from_bits_truncate ...(+32) | Err, IPC_MANAGER.with_shm, Ok, ShmAtFlags::from_bits_truncate, VirtAddr::from, VirtAddrRange::new ...(+17) |
| `kernel/src/mm/aspace/mod.rs#handle_page_fault` | `src/mm.rs#handle_page_fault` | 同板块同名 | VirtAddrRange::from_start_size, area.backend, area.backend().page_size, area.backend().populate, area.flags, axtask::yield_now ...(+8) | (xcore::config::USER_STACK_TOP - xcore::config::USER_STACK_SIZE
        ..xcore::config::USER_STACK_TOP)
        .contains, SignalInfo::new, Signo::from_repr, Signo::from_repr(SIGSEGV as u8).unwrap, XTaskExt::from_task, current ...(+17) |

#### 中断/异常处理
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `kernel/src/pseudofs/dev/tty/terminal/termios.rs#default` | `arceos/ulib/axlibc/c/pow.c#exp_inline` | 同模块上下文对齐/差异观察 | ctl | __math_oflow, __math_uflow, asdouble, asuint64, converttoint, eval_as_double ...(+4) |
| `kernel/src/file/signalfd.rs#from_signal_info` | `xmodules/xsignal/src/types.rs#default_action` | 同模块上下文对齐/差异观察 | sig_info.code, sig_info.errno, sig_info.signo | 内部闭环逻辑 |
| `kernel/src/syscall/signal.rs#is_fatal_signal` | `arceos/modules/axhal/src/arch/aarch64/trap.rs#handle_sync_exception` | 同模块上下文对齐/差异观察 | matches! | ESR_EL1.extract, crate::trap::handle_syscall, crate::trap::post_trap_callback, debug!, esr.read, esr.read_as_enum ...(+6) |
| `tools/ltp-runner.c#drain_child_output` | `xcore/src/fs/vfs/dev/tty.rs#default_termios` | 同模块上下文对齐/差异观察 | output_count_feed, read | 内部闭环逻辑 |

#### 系统调用
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `kernel/src/syscall/ipc/shm.rs#sys_shmctl` | `xapi/src/ipc/shm.rs#sys_shmctl` | 同板块同名 | Err, Ok, SHM_MANAGER.lock, buf.get_as_mut, crate::syscall::sys::sys_getuid, monotonic_time_nanos ...(+4) | Err, IPC_MANAGER.with_shm, Ok, buf.is_null, nullable!, segment.lock ...(+5) |
| `kernel/src/syscall/ipc/sem.rs#sys_semget` | `xapi/src/ipc/sem.rs#sys_semget` | 同板块同名 | Err, NEXT_SEM_ID.fetch_add, Ok, SEM_STATE.lock, has_ipc_permission, new_sem_set ...(+5) | Err, IPC_MANAGER.with_sem, Ok, current_pid, sem_manager.create_semset, sem_manager.get_semid_by_key ...(+3) |
| `kernel/src/syscall/io_mpx/epoll.rs#sys_epoll_ctl` | `arceos/api/arceos_posix_api/src/imp/io_mpx/epoll.rs#sys_epoll_ctl` | 同板块同名 | Epoll::from_fd, EpollFlags::from_bits, EpollFlags::from_bits(event.events & !events.bits()).ok_or, Err, IoEvents::from_bits_truncate, Ok ...(+7) | debug!, syscall_body! |
| `kernel/src/syscall/timer.rs#sys_timer_gettime` | `xapi/src/sys/time.rs#sys_timer_gettime` | 同板块同名 | Err, Ok, POSIX_TIMERS.lock, curr_value.vm_write, current_pid, drop ...(+8) | Ok, warn! |

#### 文件系统
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `kernel/src/pseudofs/dev/fb.rs#ioctl` | `xcore/src/fs/vfs/dev/loopx.rs#ioctl` | 同板块同名 | (arg as *mut FixScreenInfo).vm_write, (arg as *mut VarScreenInfo).vm_write, Err, Ok, axdisplay::framebuffer_info | Err, Ok, Some, argp.address, argp.address().as_usize, argp.cast ...(+24) |
| `kernel/src/pseudofs/dev/loop.rs#ioctl` | `xcore/src/fs/vfs/dev/tty.rs#ioctl` | 同板块同名 | (arg as *const loop_info).vm_read_uninit, (arg as *const loop_info).vm_read_uninit()?.assume_init, (arg as *const u32).vm_read, (arg as *mut loop_info).vm_write, (arg as *mut u32).vm_write, (arg as *mut u64).vm_write ...(+22) | Err, Ok, argp.cast::<termios>, argp.cast::<winsize>, debug!, self.get_termios ...(+6) |
| `kernel/src/syscall/fs/fd_ops.rs#sys_fcntl` | `xapi/src/fs/fd_ops.rs#sys_fcntl` | 同板块同名 | FD_TABLE
                .read, FD_TABLE
                .read()
                .get, FD_TABLE
                .read()
                .get(fd as _)
                .ok_or, FD_TABLE.write, FD_TABLE.write().set_cloexec, NodePermission::from_bits_truncate ...(+16) | Err, FD_TABLE.has_cloexec, FD_TABLE.is_assigned, FD_TABLE.set_cloexec, NodePermission::from_bits_truncate, Ok ...(+8) |
| `kernel/src/syscall/fs/io.rs#sys_splice` | `xapi/src/fs/io.rs#sys_splice` | 同板块同名 | DummyFd::from_fd, DummyFd::from_fd(fd_in).is_ok, DummyFd::from_fd(fd_out).is_ok, Err, File::from_fd, Pipe::from_fd ...(+20) | Err, File::from_fd, Pipe::from_fd, Pipe::from_fd(fd_in, FileFlags::READ, FileFlags::PATH).is_ok, Pipe::from_fd(fd_out, FileFlags::WRITE, FileFlags::PATH).is_ok, SendFile::Direct ...(+14) |

#### 设备驱动
仅参考项目中观察到该模块代表函数，目标项目没有同模块可对齐证据：
- 参考项目：`arceos/tools/raspi4/chainloader/src/bsp/device_driver/bcm/bcm2xxx_pl011_uart.rs#init`，调用摘要：FBRD::BAUD_DIVFRAC.val, IBRD::BAUD_DIVINT.val, self.flush, self.registers
            .CR
            .write, self.registers
            .LCR_H
            .write, self.registers.CR.set ...(+3)
- 参考项目：`arceos/tools/bwbench_client/src/device.rs#new`，调用摘要：ETH_P_ALL.to_be, Err, Ok, dev.bind_interface, dev.interface_mtu, ifreq.ioctl ...(+5)
- 参考项目：`arceos/tools/bwbench_client/src/main.rs#transmit_benchmark`，调用摘要：Local::now, NetDevice::new, NetDevice::new(interface).unwrap, current_time.signed_duration_since, current_time.signed_duration_since(past_time).num_seconds, dev.send ...(+2)
- 参考项目：`arceos/tools/bwbench_client/src/device.rs#bind_interface`，调用摘要：ETH_P_ALL.to_be, Err, Ok, libc::bind, self.ifreq.ioctl, std::io::Error::last_os_error ...(+2)

#### 网络协议栈
仅参考项目中观察到该模块代表函数，目标项目没有同模块可对齐证据：
- 参考项目：`arceos/ulib/axlibc/c/network.c#inet_pton`，调用摘要：hexval, inet_pton, isdigit, memmove
- 参考项目：`xcore/src/net/socket.rs#sendto`，调用摘要：Err, Ipv4Addr::LOCALHOST.into, Ok, SocketAddr::new, inner
                    .bind, inner
                    .bind(SocketAddr::new(Ipv4Addr::LOCALHOST.into(), 0))
                    .ok ...(+2)
- 参考项目：`xcore/src/net/socket.rs#recvfrom`，调用摘要：Ok, Some, tcpsocket.lock, tcpsocket.lock().recv, tcpsocket.lock().recv(buf).map, udpsocket
                .lock ...(+5)
- 参考项目：`arceos/api/arceos_posix_api/src/imp/net.rs#shutdown`，调用摘要：Ok, tcpsocket.lock, tcpsocket.peer_addr, tcpsocket.shutdown, udpsocket.lock, udpsocket.peer_addr ...(+1)

#### 文件系统/第三方库
仅参考项目中观察到该模块代表函数，目标项目没有同模块可对齐证据：
- 参考项目：`arceos/crates/lwext4_rust/src/inode/file.rs#read_at`，调用摘要：(inode as *const _ as *const u8).add, (pos + buf.len() as u64).min, Ok, WritebackGuard::new, assert!, buf.as_mut_ptr ...(+22)
- 参考项目：`arceos/crates/lwext4_rust/src/inode/file.rs#write_at`，调用摘要：Ok, assert!, assert_eq!, buf.as_ptr, buf.is_empty, buf.len ...(+15)
- 参考项目：`arceos/crates/lwext4_rust/src/blockdev.rs#new`，调用摘要：Box::new, Ok, Some, block_buf.as_mut_ptr, block_cache_buf.as_mut, block_dev_iface.as_mut ...(+10)
- 参考项目：`arceos/modules/axfs-ng/src/fs/ext4/inode.rs#create`，调用摘要：DirEntry::new_dir, DirEntry::new_file, DirNode::new, Err, FileNode::new, Inode::new ...(+11)

#### 网络协议栈/第三方库
仅参考项目中观察到该模块代表函数，目标项目没有同模块可对齐证据：
- 参考项目：`arceos/modules/axnet/src/smoltcp_impl/tcp.rs#connect`，调用摘要：Err, Ok, Ok::<(IpEndpoint, IpEndpoint), NetError>, SOCKET_SET
                .with_socket_mut::<tcp::Socket, _, _>, SOCKET_SET.add, SocketSetWrapper::new_tcp_socket ...(+33)
- 参考项目：`arceos/modules/axnet/src/smoltcp_impl/dns.rs#query`，调用摘要：Err, IpAddr::from, Ok, SOCKET_SET
            .with_socket_mut::<dns::Socket, _, _>, SOCKET_SET
            .with_socket_mut::<dns::Socket, _, _>(handle, |socket| {
                socket.start_query(iface.lock().context(), name, query_type)
            })
            .map_err, SOCKET_SET.poll_interfaces ...(+11)
- 参考项目：`arceos/modules/axnet/src/smoltcp_impl/bench.rs#bench_transmit_bandwidth`，调用摘要：(current_time - past_time).secs, AxNetTxToken::consume, InterfaceWrapper::current_time, info!, self.transmit, tx_buf[0..12].fill ...(+2)
- 参考项目：`arceos/modules/axnet/src/smoltcp_impl/tcp.rs#recv_timeout`，调用摘要：Err, Ok, SOCKET_SET.with_socket_mut::<tcp::Socket, _, _>, current_ticks, self.block_on, self.handle.get ...(+9)

#### 第三方库
仅参考项目中观察到该模块代表函数，目标项目没有同模块可对齐证据：
- 参考项目：`arceos/crates/page_table_multiarch/page_table_multiarch/src/bits64.rs#map_region`，调用摘要：Err, M::flush_tlb, Ok, PageSize::Size1G.is_aligned, PageSize::Size2M.is_aligned, PageSize::Size4K.is_aligned ...(+12)
- 参考项目：`arceos/crates/axdriver_crates/axdriver_virtio/src/net.rs#try_new`，调用摘要：Err, InnerDev::new, InnerDev::new(transport).map_err, NetBufPool::new, Ok, Some ...(+14)
- 参考项目：`arceos/crates/page_table_multiarch/page_table_multiarch/src/bits64.rs#protect_region`，调用摘要：Err, Ok, TlbFlushAll::new, assert!, error!, self.protect ...(+5)
- 参考项目：`arceos/crates/page_table_multiarch/page_table_multiarch/src/arch/loongarch64.rs#flush_tlb`，调用摘要：asm!

#### 通用模块/其他
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `kernel/src/time.rs#from_time_value` | `xmodules/xutils/src/time.rs#from_time_value` | 同板块同名 | tv.as_secs, tv.subsec_micros | 内部闭环逻辑 |
| `tools/ltp-runner.c#exists` | `arceos/ulib/axlibc/c/log.c#top16` | 同板块同结构 | access | asuint64 |
| `tools/ltp-runner.c#output_write_legacy_result` | `arceos/ulib/axlibc/c/printf.c#print_exponential_number` | 同模块上下文对齐/差异观察 | fputc, fputs, fwrite, is_horizontal_space, is_ltp_status_token | ABS_FOR_PRINTING, PRINTF_ABS, bastardized_floor, get_components, get_normalized_components, get_sign_bit ...(+5) |
| `tools/ltp-runner.c#output_count_line` | `arceos/ulib/axlibc/c/pow.c#pow` | 同模块上下文对齐/差异观察 | is_horizontal_space, is_ltp_status_token, is_status_token_boundary, is_summary_start, strncmp | __builtin_fma, __math_invalid, __math_oflow, __math_uflow, asdouble, asuint64 ...(+8) |


好的，审判长。本庭已收到控辩双方的全部陈述、证据底座及原始AST抽样数据。现依据审计指令，对目标项目 `[2026-T2026104869910383-XX]` 与参考项目 `[2025-StarryX-杭州电子科技大学]` 进行最终同源性定性仲裁。

### 1. 宏观判定
- **定性结论**: **独立开发 (相似度: 4.64%，共同 AST 指纹: 181；唯一 AST 指纹: 目标项目 1188，参考项目 2897；覆盖率: 目标项目 15.24%，参考项目 6.25%)**
- **裁决摘要**: 两项目在宏观 AST 结构上表现出**压倒性的差异**。4.64% 的客观相似度与极低的双向覆盖率（15.24% / 6.25%）表明，双方代码库的主体是各自独立构建的。控方提出的“底层逻辑一致”指控，经技术析理，均指向受 Linux ABI、POSIX 标准及 Rust 语言惯用法强约束的极简访问器（accessor）函数，其 AST 节点的必然收敛不具备区分原创与复用的效力。双方在核心架构抽象（如用户态内存访问、文件描述符表隔离、进程/线程模型）上存在显著且无法用“洗码”解释的差异。因此，本庭判定，**无证据表明目标项目对参考项目构成实质性代码复用**。

### 2. 全模块覆盖矩阵解读
| 模块 | 双方均存在 | 共同 AST 指纹 | Target 覆盖 | Base 覆盖 | 证据强度 | 解读 |
| :--- | :---: | :---: | :---: | :---: | :---: | :--- |
| **进程管理** | 是 | 5 | 7.46% | 3.14% | **弱** | 共同指纹均为 `clear_child_tid` 等极简 getter/setter 或 `sys_getpid` 等 POSIX 标准入口，是领域强约束下的必然收敛，非独创性逻辑。 |
| **任务调度/异步运行时** | 是 | 8 | 3.46% | 2.28% | **弱** | 共同指纹集中在 `sys_futex`、`sys_kill` 等系统调用入口，其内部调用链（如 futex 键生成、信号发送路径）差异显著，表明底层运行时实现不同。 |
| **内存管理** | 是 | 12 | 4.41% | 3.08% | **弱** | 共同指纹如 `sys_mmap`、`do_clone`，虽同名同板块，但内存后端（`Backend` vs `File`）和用户指针模型（`vm_read` vs `UserPtr`）截然不同，架构异构。 |
| **中断/异常处理** | 是 | 0 | 0.0% | 0.0% | **未形成** | 双方均有该模块，但 AST 层面未发现任何共同结构指纹，表明 trap 处理、信号分发等底层逻辑完全独立实现。 |
| **系统调用** | 是 | 0 | 0.0% | 0.0% | **未形成** | 尽管存在大量同名 syscall 函数，但 AST 指纹无重合，说明系统调用分发、参数校验、返回路径等框架代码无同源关系。 |
| **文件系统** | 是 | 39 | 9.2% | 6.28% | **中** | 共同指纹数量最多，但主要集中在 `ioctl`、`sys_fcntl` 等标准接口。证据底座显示，底层 VFS 抽象（`NodeOpsMux` vs `VirtDir`）和伪文件系统实现方式不同。 |
| **设备驱动** | 否 | 0 | 0.0% | 0.0% | **未形成** | 仅参考项目存在该模块，目标项目无此模块，无比对基础。 |
| **网络协议栈** | 否 | 0 | 0.0% | 0.0% | **未形成** | 仅参考项目存在该模块，目标项目无此模块，无比对基础。 |
| **第三方库/公共依赖** | 否 | 0 | 0.0% | 0.0% | **未形成** | 参考项目包含大量第三方库（ext4, smoltcp, page_table 等），目标项目未体现。此差异是模块有无的根本区别，而非同源问题。 |

### 3. 底层 AST 关键证据指纹表
| 所属板块 | 目标项目路径 | 参考项目路径 | 证据类型 | 技术说明 |
| :--- | :--- | :--- | :--- | :--- |
| **进程管理** | `kernel/src/task/mod.rs#set_clear_child_tid` | `xcore/src/task/proc.rs#set_clear_child_tid` | 同板块底层逻辑一致 | 均为 `self.clear_child_tid.store`。`clear_child_tid` 是 Linux ABI 强制字段，`store` 是 Rust `Atomic` 类型唯一写入方法，属领域与语言双重约束下的必然收敛。 |
| **进程管理** | `kernel/src/task/mod.rs#clear_child_tid` | `xcore/src/task/proc.rs#clear_child_tid` | 同板块底层逻辑一致 | 均为 `self.clear_child_tid.load`。同上，`load` 是 Rust `Atomic` 类型唯一读取方法，不具独创性。 |
| **进程管理** | `kernel/src/task/mod.rs#is_clone_child` | `xcore/src/task/proc.rs#is_clone_child` | 同板块底层逻辑一致 | AST 图谱仅含 `Some`。函数体为对 `Option<Pid>` 的极简模式匹配，是 Rust 处理可空值的标准惯用法。 |
| **进程管理** | `kernel/src/file/pidfd.rs#process` | `xmodules/xprocess/src/thread.rs#process` | 同板块底层逻辑一致 | AST 图谱为“内部闭环逻辑”。两者均为返回内部字段引用的极简 accessor，无外部调用是此类函数的正常特征。 |
| **进程管理** | `kernel/src/task/ops.rs#do_exit` | `xapi/src/task/exit.rs#do_exit` | 同板块同名 | 同名函数，但退出清理路径不同。目标项目清理 `clear_elf_cache`，参考项目清理 `FD_TABLE` 和 `IPC_MANAGER`，反映不同资源管理架构。 |
| **进程管理** | `kernel/src/syscall/fs/pidfd.rs#sys_pidfd_getfd` | `xapi/src/fs/fd/pid.rs#sys_pidfd_getfd` | 同板块同名 | 均操作 `FD_TABLE`，但访问路径不同。目标项目用 `FD_TABLE.scope`，参考项目用 `FD_TABLE.deref_from`，表明命名空间隔离机制实现各异。 |
| **任务调度** | `kernel/src/syscall/sync/futex.rs#sys_futex` | `xapi/src/task/futex.rs#sys_futex` | 同板块同名 | Futex 键生成方式不同：目标项目 `FutexKey::new_current`，参考项目 `FutexKey::new`，表明 futex 内核对象模型不同。 |
| **内存管理** | `kernel/src/syscall/mm/mmap.rs#sys_mmap` | `xapi/src/mm/mmap.rs#sys_mmap` | 同板块同名 | 内存映射后端抽象不同：目标项目使用 `Backend::new_*` 系列，参考项目使用 `File::from_fd`，表明 VMA 与文件系统的集成方式异构。 |
| **内存管理** | `kernel/src/syscall/task/clone.rs#do_clone` | `xapi/src/task/clone.rs#do_clone` | 同板块同名 | 用户态内存写入方式不同：目标项目用 `(ptr).vm_write()`，参考项目用 `UserPtr::from` + `uspace.write`，用户空间访问抽象层完全不同。 |
| **文件系统** | `kernel/src/pseudofs/dev/fb.rs#ioctl` | `xcore/src/fs/vfs/dev/loopx.rs#ioctl` | 同板块同名 | 均实现 `ioctl`，但用户指针转换方式不同：目标项目用 `(arg as *mut T).vm_write`，参考项目用 `argp.cast::<T>`，表明用户态数据拷贝机制不同。 |
| **文件系统** | `kernel/src/syscall/fs/fd_ops.rs#sys_fcntl` | `xapi/src/fs/fd_ops.rs#sys_fcntl` | 同板块同名 | 均操作 `FD_TABLE`，但加锁粒度不同：目标项目显式 `FD_TABLE.read/write`，参考项目使用 `has_cloexec`/`set_cloexec` 等原子检查方法。 |

### 4. 核心代码调用流对比剖析
* **【进程管理/进程退出路径】**
* **底层技术析理**: 两者均实现 `do_exit`，但资源清理策略和同步原语使用存在架构级差异。目标项目采用协作式退出，调用 `axtask::yield_now` 主动让权，并清理 ELF 缓存；参考项目则调用 `axtask::exit` 直接终止，并显式清理全局 `FD_TABLE` 和 `IPC_MANAGER`。这反映了不同的任务生命周期管理和资源所有权模型。
* **代码溯源链接**:
  - 目标项目: `kernel/src/task/ops.rs#do_exit`
  - 参考项目: `xapi/src/task/exit.rs#do_exit`

* **【内存管理/用户空间访问】**
* **底层技术析理**: 在 `do_clone` 系统调用中，向用户空间写入子线程 TID 的方式揭示了根本性的抽象层差异。目标项目直接为原始指针扩展了 `vm_write` 方法，属于侵入式扩展；参考项目则定义了 `UserPtr` 包装类型，并通过 `uspace` 对象进行读写，属于非侵入式的类型安全抽象。这两种设计哲学无法通过简单重命名转换。
* **代码溯源链接**:
  - 目标项目: `kernel/src/syscall/task/clone.rs#do_clone`
  - 参考项目: `xapi/src/task/clone.rs#do_clone`

* **【文件系统/文件描述符表隔离】**
* **底层技术析理**: 在 `sys_pidfd_getfd` 中，两者都需要跨进程访问 FD 表，但隔离机制不同。目标项目通过 `proc_data.scope` 实现基于作用域的访问控制；参考项目通过 `XProcess::from_thread(&thread).ns` 获取命名空间。`scope` 与 `ns` 是不同的命名空间隔离概念，其内部实现和生命周期管理逻辑必然不同。
* **代码溯源链接**:
  - 目标项目: `kernel/src/syscall/fs/pidfd.rs#sys_pidfd_getfd`
  - 参考项目: `xapi/src/fs/fd/pid.rs#sys_pidfd_getfd`

### 5. 单方模块与降权项
*   **单方模块**: **设备驱动**、**网络协议栈**、**第三方库/公共依赖** 仅存在于参考项目中。这是项目功能范围和依赖选择上的根本差异，不构成同源性证据。
*   **降权项**: 所有标记为 `[同板块同名]` 的 POSIX 标准系统调用入口（如 `sys_getpid`、`sys_fork`、`sys_exit`、`sys_mmap` 等），其函数签名和存在性由标准强制规定，同名同板块是合规的必然结果。其内部实现路径的差异（如前述 `do_exit`、`do_clone`）才是判定原创性的关键。控方将其作为弱信号处理，本庭予以确认并彻底排除其作为主证据的效力。

### 6. 保守结论
本庭基于 4.64% 的极低 AST 相似度及对全模块证据的技术析理，认定目标项目 `[2026-T2026104869910383-XX]` 与参考项目 `[2025-StarryX-杭州电子科技大学]` 为独立开发。控方所依赖的“底层逻辑一致”证据，经鉴定均为受 Linux ABI、POSIX 标准或 Rust 语言惯用法强约束的极简代码模式，其 AST 收敛是必然的，不具备区分原创与复用的效力。相反，双方在用户态内存访问抽象、文件描述符表隔离机制、进程退出资源清理策略等核心架构决策上存在显著且根本的差异。综合裁定，**无证据支持代码复用指控，目标项目原创性成立**。

------------------------------

