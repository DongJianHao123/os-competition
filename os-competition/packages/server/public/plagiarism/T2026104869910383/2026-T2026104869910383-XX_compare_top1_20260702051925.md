# 🛡️ OS 内核赛自动化代码级架构审计报告

**审计时间**: `2026-07-02 05:19:25`
**参赛作品名称**: `2026-T2026104869910383-XX`
**年份**: `2026`
**学校**: `XX`
**队伍名称**: `T2026104869910383`
**比赛名字**: `OS-Competition`
**比赛子赛道**: `Kernel-Implementation`
**仓库地址**: `https://gitlab.eduxiji.net/educg-group-43501-3132633/T2026104869910383-3620`
**推测开发语言**: `Rust / C (由AST探针自动识别)`
**特征函数量**: `1349`

## ⚔️ 底层代码同源性对比 (AST Homology)
> 本部分由专门的代码审查智能体，对该项目与符合年份条件的历届最高相似度项目进行控制流与底层调用对比。新版报告会先给出确定性的全模块覆盖矩阵，再给出 Agent 的分模块解释，避免只围绕少量抽样函数展开。

### 🏆 Top 1 底层对标: vs `2025-StarryMix-清华大学`（清华大学 / Starry Mix）

#### 对方作品信息

**对方年份**: `2025`
**对方学校**: `清华大学`
**对方队伍名称**: `Starry Mix`
**对方比赛名字**: `OS-Competition`
**对方比赛子赛道**: `Kernel-Implementation`
**对方仓库地址**: `local-upload://starry-mix`

> 双向覆盖率：共同 AST 指纹: 327；唯一 AST 指纹: 目标项目 1188，参考项目 1503；覆盖率: 目标项目 27.53%，参考项目 21.76%

## 全模块对比总览（确定性证据）

> 总体覆盖率：共同 AST 指纹: 327；唯一 AST 指纹: 目标项目 1188，参考项目 1503；覆盖率: 目标项目 27.53%，参考项目 21.76%

### 1. 模块覆盖矩阵
| 模块 | 目标函数数 | 参考函数数 | 共同唯一 AST 指纹 | Target 覆盖 | Base 覆盖 | 状态 |
| :--- | ---: | ---: | ---: | ---: | ---: | :--- |
| 进程管理 | 71 | 56 | 18 | 26.87% | 34.62% | 双方存在且有结构重合 |
| 任务调度/异步运行时 | 245 | 282 | 36 | 15.58% | 13.58% | 双方存在且有结构重合 |
| 内存管理 | 308 | 273 | 47 | 17.28% | 18.8% | 双方存在且有结构重合 |
| 中断/异常处理 | 5 | 8 | 2 | 40.0% | 25.0% | 双方存在且有结构重合 |
| 系统调用 | 181 | 78 | 7 | 4.14% | 9.33% | 双方存在且有结构重合 |
| 文件系统 | 496 | 562 | 159 | 37.5% | 33.97% | 双方存在且有结构重合 |
| 设备驱动 | 0 | 120 | 0 | 0.0% | 0.0% | 仅参考项目体现 |
| 网络协议栈 | 0 | 138 | 0 | 0.0% | 0.0% | 仅参考项目体现 |
| 文件系统/第三方库 | 0 | 37 | 0 | 0.0% | 0.0% | 仅参考项目体现 |
| 通用模块/其他 | 43 | 158 | 2 | 5.88% | 1.49% | 双方存在且有结构重合 |

### 2. 分模块证据清单

#### 进程管理
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `kernel/src/pseudofs/proc.rs#lookup_child` | `api/src/vfs/proc.rs#lookup_child` | 同板块同名 | Arc::downgrade, Arc::new, CStr::from_bytes_until_nul, CStr::from_bytes_until_nul(&input)
                                    .map_err, CStr::from_bytes_until_nul(&input)
                                    .map_err(|_| VfsError::InvalidInput)?
                                    .to_str, CStr::from_bytes_until_nul(&input)
                                    .map_err(|_| VfsError::InvalidInput)?
                                    .to_str()
                                    .map_err ...(+54) | Arc::downgrade, Arc::new, CStr::from_bytes_until_nul, CStr::from_bytes_until_nul(&input)
                                    .map_err, CStr::from_bytes_until_nul(&input)
                                    .map_err(|_| VfsError::EINVAL)?
                                    .to_str, CStr::from_bytes_until_nul(&input)
                                    .map_err(|_| VfsError::EINVAL)?
                                    .to_str()
                                    .map_err ...(+55) |
| `kernel/src/task/futex.rs#wait_if` | `core/src/futex.rs#wait_if` | 同板块同名 | Arc::downgrade, NEXT_WAITER_ID.fetch_add, Ok, Poll::Ready, block_on, condition ...(+11) | Ok, Poll::Ready, Some, block_on_interruptible, cond, condition.take ...(+8) |
| `kernel/src/task/ops.rs#do_exit` | `api/src/task.rs#do_exit` | 同板块同名 | FutexKey::new_current, SignalInfo::new_kernel, Some, axtask::yield_now, clear_child_tid.is_null, clear_child_tid.vm_write ...(+33) | FutexKey::new_current, SHM_MANAGER.lock, SHM_MANAGER.lock().clear_proc_shm, SignalInfo::new_kernel, Some, axtask::yield_now ...(+30) |
| `kernel/src/pseudofs/proc.rs#lookup_child` | `api/src/vfs/proc.rs#lookup_child` | 同板块同名 | Arc::downgrade, Arc::new, NodeOpsMux::Dir, Ok, SimpleDir::new_maker, current ...(+26) | Arc::downgrade, Arc::new, Err, NodeOpsMux::Dir, Ok, SimpleDir::new_maker ...(+10) |

#### 任务调度/异步运行时
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `kernel/src/syscall/task/ctl.rs#sys_prctl` | `api/src/syscall/task/ctl.rs#sys_prctl` | 同板块同名 | (arg3 as *const u8).vm_read, Err, Ok, buf[..len].copy_from_slice, current, current().as_thread ...(+11) | Err, Ok, buf[..len].copy_from_slice, current, current().name, current().set_name ...(+7) |
| `kernel/src/pseudofs/dev/event.rs#ioctl` | `api/src/vfs/dev/event.rs#ioctl` | 同板块同名 | Err, EventType::Led.bits_count, EventType::Sound.bits_count, EventType::Switch.bits_count, Ok, UserPtr::<InputDeviceId>::from ...(+16) | Err, EventType::Led.bits_count, EventType::Sound.bits_count, EventType::Switch.bits_count, Ok, UserPtr::<InputDeviceId>::from ...(+16) |
| `kernel/src/pseudofs/dev/mod.rs#builder` | `api/src/vfs/dev/mod.rs#builder` | 同板块同名 | Arc::new, Device::new, DeviceId::new, DirMapping::new, Ok, Random::new ...(+11) | Arc::new, Device::new, DeviceId::new, DirMapping::new, Ok, Random::new ...(+11) |
| `kernel/src/syscall/sync/futex.rs#sys_futex` | `api/src/syscall/sync/futex.rs#sys_futex` | 同板块同名 | AxError::from, Err, FutexKey::new_current, Ok, Some, assert_unsigned ...(+22) | Err, FutexKey::new_current, Ok, Some, assert_unsigned, axtask::yield_now ...(+19) |

#### 内存管理
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `kernel/src/syscall/mod.rs#handle_syscall` | `api/src/syscall/mod.rs#handle_syscall` | 同板块同名 | Err, LinuxError::ENOSYS.code, LinuxError::from, LinuxError::from(err).code, Ok, PERSONALITY.load ...(+239) | Err, LinuxError::ENOSYS.code, Ok, Sysno::new, debug!, err.code ...(+201) |
| `kernel/src/syscall/mm/mmap.rs#sys_mmap` | `api/src/syscall/mm/mmap.rs#sys_mmap` | 同板块同名 | (addr + length).align_up, Arc::new, Backend::new_alloc, Backend::new_cow, Backend::new_file, Backend::new_linear ...(+56) | (addr + length).align_up, Arc::new, Backend::new_alloc, Backend::new_cow, Backend::new_file, Backend::new_linear ...(+50) |
| `kernel/src/mm/aspace/backend/file.rs#populate` | `arceos/modules/axmm/src/backend/file.rs#populate` | 同板块同名 | (start + FILE_FAULT_AROUND_PAGES * PAGE_SIZE_4K).min, AxResult::Ok, Box::new, Err, Ok, Some ...(+20) | Box::new, Err, Ok, Some, Vec::new, access_flags.contains ...(+18) |
| `kernel/src/task/user.rs#new_user_task` | `api/src/task.rs#new_user_task` | 同板块同名 | (set_child_tid as *mut Pid).nullable, SignalInfo::new_kernel, TaskInner::new, axtask::current, axtask::resched_if_pending, check_signals ...(+24) | SignalInfo::new_kernel, TaskInner::new, access_user_memory, axtask::current, check_signals, curr.as_thread ...(+20) |

#### 中断/异常处理
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `kernel/src/pseudofs/dev/tty/terminal/termios.rs#signo_for` | `api/src/terminal/termios.rs#signo_for` | 同板块同名同结构 | Some, self.special_char | Some, self.special_char |
| `kernel/src/pseudofs/dev/tty/terminal/termios.rs#default` | `api/src/terminal/termios.rs#default` | 同板块同名同结构 | ctl | ctl |
| `kernel/src/syscall/signal.rs#is_fatal_signal` | `api/src/syscall/signal.rs#sys_rt_sigreturn` | 同模块上下文对齐/差异观察 | matches! | Ok, block_next_signal, current, current().as_thread, current().as_thread().signal.restore, tf.retval |
| `tools/ltp-runner.c#drain_child_output` | `arceos/modules/axhal/src/irq.rs#register_irq_waker` | 同模块上下文对齐/差异观察 | output_count_feed, read | POLL_TABLE[irq as usize].register, axplat::irq::register |

#### 系统调用
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `kernel/src/syscall/net/opt.rs#sys_getsockopt` | `api/src/syscall/net/opt.rs#sys_getsockopt` | 同板块同名 | Err, GetSocketOption::TcpInfo, Ok, Socket::from_fd, call_dispatch!, debug! ...(+6) | Err, Ok, Socket::from_fd, call_dispatch!, debug!, optlen.get_as_mut ...(+3) |
| `kernel/src/syscall/ipc/shm.rs#sys_shmctl` | `api/src/syscall/ipc/shm.rs#sys_shmctl` | 同板块同名 | Err, Ok, SHM_MANAGER.lock, buf.get_as_mut, crate::syscall::sys::sys_getuid, monotonic_time_nanos ...(+4) | Err, Ok, SHM_MANAGER.lock, buf.get_as_mut, monotonic_time_nanos, nullable! ...(+3) |
| `kernel/src/syscall/time.rs#sys_setitimer` | `api/src/syscall/time.rs#sys_setitimer` | 同板块同名 | ITimerType::from_repr, ITimerType::from_repr(which).ok_or, Ok, TimeValue::default, curr
        .as_thread, curr
        .as_thread()
        .time
        .try_borrow_mut ...(+15) | ITimerType::from_repr, ITimerType::from_repr(which).ok_or, Ok, curr
        .as_thread, curr
        .as_thread()
        .time
        .borrow_mut, curr
        .as_thread()
        .time
        .borrow_mut()
        .set_itimer ...(+12) |
| `kernel/src/syscall/io_mpx/epoll.rs#sys_epoll_ctl` | `api/src/syscall/io_mpx/epoll.rs#sys_epoll_ctl` | 同板块同名 | Epoll::from_fd, EpollFlags::from_bits, EpollFlags::from_bits(event.events & !events.bits()).ok_or, Err, IoEvents::from_bits_truncate, Ok ...(+7) | Epoll::from_fd, EpollFlags::from_bits, EpollFlags::from_bits(event.events & !(events.bits() as u32))
            .ok_or, Err, IoEvents::from_bits_truncate, Ok ...(+7) |

#### 文件系统
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `kernel/src/pseudofs/dev/fb.rs#ioctl` | `api/src/vfs/dev/fb.rs#ioctl` | 同板块同名 | (arg as *mut FixScreenInfo).vm_write, (arg as *mut VarScreenInfo).vm_write, Err, Ok, axdisplay::framebuffer_info | (arg as *mut FixScreenInfo).vm_write, (arg as *mut VarScreenInfo).vm_write, Err, Ok, axdisplay::main_display, axdisplay::main_display().info |
| `kernel/src/pseudofs/dev/loop.rs#ioctl` | `api/src/vfs/dev/loop.rs#ioctl` | 同板块同名 | (arg as *const loop_info).vm_read_uninit, (arg as *const loop_info).vm_read_uninit()?.assume_init, (arg as *const u32).vm_read, (arg as *mut loop_info).vm_write, (arg as *mut u32).vm_write, (arg as *mut u64).vm_write ...(+22) | (arg as *const loop_info).vm_read_uninit, (arg as *const loop_info).vm_read_uninit()?.assume_init, (arg as *const u32).vm_read, (arg as *mut loop_info).vm_write, (arg as *mut u32).vm_write, (arg as *mut u64).vm_write ...(+22) |
| `kernel/src/file/epoll.rs#poll_events` | `api/src/file/epoll.rs#poll_events` | 同板块同名 | Arc::downgrade, Err, Ok, event.events.bits, file.as_ref, interest.consume ...(+13) | Arc::downgrade, Err, Ok, event.events.bits, file.as_ref, interest.enabled.store ...(+14) |
| `kernel/src/syscall/fs/fd_ops.rs#sys_fcntl` | `api/src/syscall/fs/fd_ops.rs#sys_fcntl` | 同板块同名 | FD_TABLE
                .read, FD_TABLE
                .read()
                .get, FD_TABLE
                .read()
                .get(fd as _)
                .ok_or, FD_TABLE.write, FD_TABLE.write().set_cloexec, NodePermission::from_bits_truncate ...(+16) | FD_TABLE
                .read, FD_TABLE
                .read()
                .get, FD_TABLE
                .read()
                .get(fd as _)
                .ok_or, FD_TABLE
                .write, FD_TABLE
                .write()
                .get_mut, FD_TABLE
                .write()
                .get_mut(fd as _)
                .ok_or ...(+15) |

#### 设备驱动
仅参考项目中观察到该模块代表函数，目标项目没有同模块可对齐证据：
- 参考项目：`arceos/modules/axnet/src/router.rs#dispatch`，调用摘要：IpAddress::Ipv4, IpAddress::Ipv6, IpVersion::of_packet, IpVersion::of_packet(packet).expect, assert_eq!, dev.send ...(+12)
- 参考项目：`arceos/modules/axnet/src/device/ethernet.rs#send`，调用摘要：Self::send_to, Some, buf.copy_from_slice, dst_buffer.copy_from_slice, next_hop.is_broadcast, packet.len ...(+7)
- 参考项目：`arceos/modules/axnet/src/lib.rs#init_network`，调用摘要："eth0".to_owned, Box::new, EthernetAddress, EthernetDevice::new, GATEWAY.parse, GATEWAY.parse().expect ...(+32)
- 参考项目：`arceos/tools/raspi4/chainloader/src/bsp/device_driver/bcm/bcm2xxx_pl011_uart.rs#init`，调用摘要：FBRD::BAUD_DIVFRAC.val, IBRD::BAUD_DIVINT.val, self.flush, self.registers
            .CR
            .write, self.registers
            .LCR_H
            .write, self.registers.CR.set ...(+3)

#### 网络协议栈
仅参考项目中观察到该模块代表函数，目标项目没有同模块可对齐证据：
- 参考项目：`arceos/modules/axnet/src/udp.rs#recv`，调用摘要：Err, ExpectedRemote::Any, ExpectedRemote::Expecting, Ok, SocketAddrEx::Ip, ax_err! ...(+20)
- 参考项目：`arceos/modules/axnet/src/udp.rs#send`，调用摘要：Err, IpAddr::V4, IpEndpoint::from, Ok, PacketMeta::default, SERVICE.lock ...(+23)
- 参考项目：`arceos/modules/axnet/src/tcp.rs#recv`，调用摘要：Err, Ok, ax_err!, dst.remaining_mut, dst.write, options.flags.contains ...(+13)
- 参考项目：`arceos/modules/axnet/src/unix/dgram.rs#recv`，调用摘要：Err, Ok, SocketAddrEx::Unix, data.len, dst.extend, dst.write ...(+9)

#### 文件系统/第三方库
仅参考项目中观察到该模块代表函数，目标项目没有同模块可对齐证据：
- 参考项目：`arceos/modules/axfs-ng/src/fs/ext4/inode.rs#create`，调用摘要：DirEntry::new_dir, DirEntry::new_file, DirNode::new, Err, FileNode::new, Inode::new ...(+14)
- 参考项目：`arceos/modules/axfs-ng/src/fs/ext4/inode.rs#metadata`，调用摘要：DeviceId::default, FileAttr::default, NodePermission::from_bits_truncate, Ok, into_vfs_type, self.fs
            .lock ...(+2)
- 参考项目：`arceos/modules/axfs-ng/src/fs/ext4/inode.rs#update_metadata`，调用摘要：Ok, fs.with_inode_ref, fs.with_inode_ref(self.ino, |inode| {
            if let Some(mode) = update.mode {
                inode.set_mode((inode.mode() & !0xfff) | (mode.bits() as u32));
            }
            if let Some((uid, gid)) = update.owner {
                inode.set_owner(uid as _, gid as _);
            }
            if let Some(atime) = update.atime {
                inode.set_atime(&atime);
            }
            if let Some(mtime) = update.mtime {
                inode.set_mtime(&mtime);
            }
            inode.update_ctime();
            Ok(())
        })
        .map_err, inode.mode, inode.set_atime, inode.set_mode ...(+5)
- 参考项目：`arceos/modules/axfs-ng/src/fs/ext4/inode.rs#read_dir`，调用摘要：Ok, core::str::from_utf8, core::str::from_utf8(entry.name())
                .map_err, core::str::from_utf8(entry.name())
                .map_err(|_| VfsError::EINVAL)?
                .to_owned, entry.ino, entry.inode_type ...(+10)

#### 通用模块/其他
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `kernel/src/time.rs#try_into_time_value` | `api/src/time.rs#try_into_time_value` | 同板块同名 | Err, Ok, TimeValue::new | Err, Ok, TimeValue::new |
| `kernel/src/time.rs#try_into_time_value` | `api/src/time.rs#try_into_time_value` | 同板块同名 | Err, Ok, TimeValue::new | Err, Ok, TimeValue::new |
| `kernel/src/time.rs#try_into_time_value` | `api/src/time.rs#try_into_time_value` | 同板块同名 | Err, Ok, TimeValue::new | Err, Ok, TimeValue::new |
| `kernel/src/time.rs#try_into_time_value` | `api/src/time.rs#try_into_time_value` | 同板块同名 | Err, Ok, TimeValue::new | Err, Ok, TimeValue::new |


#### 1. 宏观判定
- **定性结论**: 独立开发 (相似度: 13.83%，共同 AST 指纹: 327；唯一 AST 指纹: 目标项目 1188，参考项目 1503；覆盖率: 目标项目 27.53%，参考项目 21.76%)

#### 2. 全模块覆盖矩阵解读
- **进程管理**: 双方均存在该模块。存在 18 个共同 AST 指纹，目标覆盖率 26.87%，参考覆盖率 34.62%。相似性集中于 Linux ABI 强制语义（如 `sys_getpid`、`sys_exit`）和共享框架 API（如 `FD_TABLE`、`SimpleFs`）。证据强度：**弱**。
- **任务调度/异步运行时**: 双方均存在该模块。存在 36 个共同 AST 指纹，目标覆盖率 15.58%，参考覆盖率 13.58%。相似性源于 `axtask` 等共享异步运行时框架的固定调用模式。证据强度：**弱**。
- **内存管理**: 双方均存在该模块。存在 47 个共同 AST 指纹，目标覆盖率 17.28%，参考覆盖率 18.8%。相似性集中在 `sys_mmap` 等标准系统调用的参数校验与后端分发流程。证据强度：**弱**。
- **中断/异常处理**: 双方均存在该模块。存在 2 个共同 AST 指纹，目标覆盖率 40.0%，参考覆盖率 25.0%。覆盖率虽高但函数基数极小，相似性为 `termios` 标准结构体字段访问。证据强度：**未形成**。
- **系统调用**: 双方均存在该模块。存在 7 个共同 AST 指纹，目标覆盖率 4.14%，参考覆盖率 9.33%。极低的覆盖率表明双方系统调用分发与实现架构存在根本差异。证据强度：**未形成**。
- **文件系统**: 双方均存在该模块。存在 159 个共同 AST 指纹，目标覆盖率 37.5%，参考覆盖率 33.97%。相似性主要源于共享 `axfs`/`axfs-ng` 等 VFS 框架的接口实现。证据强度：**弱**。
- **设备驱动**: 仅参考项目体现。目标项目无此模块，覆盖率 0.0%。证据强度：**未形成**。
- **网络协议栈**: 仅参考项目体现。目标项目无此模块，覆盖率 0.0%。证据强度：**未形成**。
- **第三方库/公共依赖**: 仅参考项目体现。目标项目无此模块，覆盖率 0.0%。证据强度：**未形成**。

#### 3. 底层 AST 关键证据指纹表
| 所属板块 | 目标项目路径 | 参考项目路径 | 证据类型 | 技术说明 |
| :--- | :--- | :--- | :--- | :--- |
| 进程管理 | `kernel/src/pseudofs/proc.rs#child_names` | `api/src/vfs/proc.rs#child_names` | 同板块同名，但存在关键差异 | 目标项目数组**不包含** `"task"` 条目并附有独立注释，参考项目**包含** `"task"`。此差异表明目标项目对 Linux `/proc` 行为有独立理解与修正，是独立开发的强佐证。 |
| 进程管理 | `kernel/src/pseudofs/proc.rs#lookup_child` | `api/src/vfs/proc.rs#lookup_child` | 同板块同名，共享框架 API | `FD_TABLE.scope(...).read().get(fd).ok_or(...)?.inner.path().into_owned` 调用链是 `FD_TABLE` 框架的唯一合理用法。目标使用 `VfsError::NotFound`，参考使用 `VfsError::ENOENT`，体现不同错误模型设计。 |
| 进程管理 | `kernel/src/task/ops.rs#do_exit` | `api/src/task.rs#do_exit` | 同板块同名，存在架构差异 | 目标项目调用 `clear_elf_cache` 进行退出清理，参考项目调用 `SHM_MANAGER.lock().clear_proc_shm`。两者资源清理策略不同，表明退出流程是独立实现的。 |
| 进程管理 | `kernel/src/syscall/task/thread.rs#sys_getppid` | `api/src/syscall/task/thread.rs#sys_getppid` | 同板块同名，不同错误体系 | 目标使用 `AxError::NoSuchProcess`，参考使用 `LinuxError::ESRCH`。错误类型的根本不同是架构级差异，非表层换名。 |
| 进程管理 | `kernel/src/syscall/fs/pidfd.rs#sys_pidfd_open` | `api/src/syscall/fs/pidfd.rs#sys_pidfd_open` | 同板块同名，不同 API 设计 | 目标使用 `PidFd::new_process` / `PidFd::new_thread` 区分进程/线程，参考使用单一的 `PidFd::new`。API 粒度设计不同。 |
| 任务调度 | `kernel/src/syscall/sync/futex.rs#sys_futex` | `api/src/syscall/sync/futex.rs#sys_futex` | 同板块同名，共享框架 | 均基于 `axtask` 框架实现 `futex`，调用 `FutexKey::new_current` 等标准 API。相似性源于共享运行时。 |
| 内存管理 | `kernel/src/syscall/mm/mmap.rs#sys_mmap` | `api/src/syscall/mm/mmap.rs#sys_mmap` | 同板块同名，标准系统调用 | `Backend::new_alloc/cow/file/linear` 等后端分发模式是 `mmap` 的标准实现范式。 |
| 文件系统 | `kernel/src/pseudofs/dev/loop.rs#ioctl` | `api/src/vfs/dev/loop.rs#ioctl` | 同板块同名，标准设备操作 | `(arg as *const loop_info).vm_read_uninit()?.assume_init` 等调用链是处理 `loop` 设备 `ioctl` 的标准范式。 |

#### 4. 核心代码调用流对比剖析
* **【进程管理/进程退出路径】**
* **底层技术析理**: 在 `do_exit` 函数中，双方均遵循 `FutexKey::new_current` -> `SignalInfo::new_kernel` -> `axtask::yield_now` -> `clear_child_tid.vm_write` 的 Linux 标准退出流程。然而，在资源清理阶段，目标项目调用 `clear_elf_cache` 释放 ELF 加载缓存，而参考项目调用 `SHM_MANAGER.lock().clear_proc_shm` 清理共享内存段。这揭示了双方在进程资源管理模型上的差异：目标项目关注可执行文件缓存，参考项目关注 IPC 资源。
* **代码溯源链接**:
  - 目标项目: `kernel/src/task/ops.rs#do_exit`
  - 参考项目: `api/src/task.rs#do_exit`

* **【进程管理/信号发送路径】**
* **底层技术析理**: 在 `send_signal_to_process` 函数中，目标项目通过 `current().try_as_thread().is_some_and(...)` 进行线程安全检查，而参考项目直接访问 `proc_data.signal.can_restart` 和 `proc_data.signal.send_signal`。这体现了不同的线程安全与信号分发模型：目标项目在入口处进行线程上下文校验，参考项目则依赖内部信号模块的状态管理。
* **代码溯源链接**:
  - 目标项目: `kernel/src/task/signal.rs#send_signal_to_process`
  - 参考项目: `core/src/task.rs#send_signal_to_process`

* **【文件系统/procfs 子项生成路径】**
* **底层技术析理**: 在 `child_names` 函数中，双方均通过迭代进程/线程 ID 并将其转换为字符串来生成 `/proc` 目录项。目标项目使用 `process.threads().into_iter().map(|tid| tid.to_string().into())`，参考项目使用 `task.id().as_u64().to_string().into()`。这反映了不同的线程对象模型：目标项目直接获取 `tid` 对象，参考项目通过 `task.id().as_u64()` 获取数字 ID。
* **代码溯源链接**:
  - 目标项目: `kernel/src/pseudofs/proc.rs#child_names`
  - 参考项目: `api/src/vfs/proc.rs#child_names`

#### 5. 单方模块与降权项
- **单方模块**: 参考项目独有的【设备驱动】、【网络协议栈】、【文件系统/第三方库】模块，目标项目完全没有对应实现。若存在系统性抄袭，这些大型模块理应出现，其缺失是独立开发的强有力佐证。
- **降权项**: 双方证据中大量出现的 `Ok`、`Err`、`Some`、`Arc::downgrade`、`format!` 等属于 Rust 语言标准库或核心原语，`FD_TABLE.scope`、`SimpleFs::new_with`、`NodeOpsMux::Dir` 等属于共享开源框架的公共 API。这些调用序列的相似性由语言规范和框架设计决定，不构成同源证据。

#### 6. 保守结论
整体 AST 相似度仅 13.83%，双方覆盖率均不足 30%，这在操作系统内核领域属于低水平。所有被标记为“同板块同名”的证据，其相似性均可被 Linux/POSIX ABI 强制语义、Rust 语言惯用法或共享开源框架（ArceOS/axtask/axfs）的固定 API 调用链所解释。关键差异点，如 `child_names` 中对 `"task"` 条目的不同处理、`do_exit` 中不同的资源清理策略（`clear_elf_cache` vs `clear_proc_shm`）、以及系统级错误模型（`AxError` vs `LinuxError`）的不同，均指向独立的设计与实现决策。参考项目独有的网络、驱动等大型模块在目标项目中的完全缺失，进一步否定了系统性抄袭的可能。综合判定，目标项目构成独立开发，无实质性同源风险。

------------------------------

