# 🛡️ OS 内核赛自动化代码级架构审计报告

**审计时间**: `2026-07-02 05:19:25`
**参赛作品名称**: `2026-T2026104869910383-XX`
**年份**: `2026`
**学校**: `XX`
**队伍名称**: `T2026104869910383`
**比赛名字**: `OS-Competition`
**比赛子赛道**: `Kernel-Implementation`
**仓库地址**: `https://gitlab.eduxiji.net/educg-group-43501-3132633/T2026104869910383-3620`
**推测开发语言**: `Rust / C (由AST探针自动识别)`
**特征函数量**: `1349`

## ⚔️ 底层代码同源性对比 (AST Homology)
> 本部分由专门的代码审查智能体，对该项目与符合年份条件的历届最高相似度项目进行控制流与底层调用对比。新版报告会先给出确定性的全模块覆盖矩阵，再给出 Agent 的分模块解释，避免只围绕少量抽样函数展开。

### 🏆 Top 5 底层对标: vs `2025-AstranciA-哈尔滨工业大学深圳`（哈尔滨工业大学（深圳） / AstranciA）

#### 对方作品信息

**对方年份**: `2025`
**对方学校**: `哈尔滨工业大学（深圳）`
**对方队伍名称**: `AstranciA`
**对方比赛名字**: `OS-Competition`
**对方比赛子赛道**: `Kernel-Implementation`
**对方仓库地址**: `https://gitlab.eduxiji.net/educg-group-36002-2710490/T202518123995667-1436`

> 双向覆盖率：共同 AST 指纹: 108；唯一 AST 指纹: 目标项目 1188，参考项目 3750；覆盖率: 目标项目 9.09%，参考项目 2.88%

## 全模块对比总览（确定性证据）

> 总体覆盖率：共同 AST 指纹: 108；唯一 AST 指纹: 目标项目 1188，参考项目 3750；覆盖率: 目标项目 9.09%，参考项目 2.88%

### 1. 模块覆盖矩阵
| 模块 | 目标函数数 | 参考函数数 | 共同唯一 AST 指纹 | Target 覆盖 | Base 覆盖 | 状态 |
| :--- | ---: | ---: | ---: | ---: | ---: | :--- |
| 进程管理 | 71 | 112 | 2 | 2.99% | 2.17% | 双方存在且有结构重合 |
| 任务调度/异步运行时 | 245 | 455 | 6 | 2.6% | 1.87% | 双方存在且有结构重合 |
| 内存管理 | 308 | 343 | 7 | 2.57% | 2.27% | 双方存在且有结构重合 |
| 中断/异常处理 | 5 | 71 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |
| 系统调用 | 181 | 136 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |
| 文件系统 | 496 | 620 | 4 | 0.94% | 0.82% | 双方存在且有结构重合 |
| 设备驱动 | 0 | 142 | 0 | 0.0% | 0.0% | 仅参考项目体现 |
| 网络协议栈 | 0 | 61 | 0 | 0.0% | 0.0% | 仅参考项目体现 |
| 文件系统/第三方库 | 0 | 775 | 0 | 0.0% | 0.0% | 仅参考项目体现 |
| 网络协议栈/第三方库 | 0 | 217 | 0 | 0.0% | 0.0% | 仅参考项目体现 |
| 第三方库 | 0 | 1155 | 0 | 0.0% | 0.0% | 仅参考项目体现 |
| 通用模块/其他 | 43 | 673 | 1 | 2.94% | 0.18% | 双方存在且有结构重合 |

### 2. 分模块证据清单

#### 进程管理
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `kernel/src/task/ops.rs#do_exit` | `AstrancE/ulib/axmono/src/task/exit.rs#do_exit` | 同板块同名 | FutexKey::new_current, SignalInfo::new_kernel, Some, axtask::yield_now, clear_child_tid.is_null, clear_child_tid.vm_write ...(+33) | FD_TABLE.clear, SigInfo_::Child, SigStatus::ExitCode, UserPtr::<Pid>::from, axtask::exit, clear_child_tid.get ...(+21) |
| `kernel/src/task/signal.rs#send_signal_to_process` | `AstrancE/ulib/axmono/src/task/timer.rs#send_signal_to_process` | 同板块同名 | Ok, Some, current, current()
            .try_as_thread, current()
            .try_as_thread()
            .is_some_and, get_process_data ...(+14) | PROCESS_TABLE.read, PROCESS_TABLE.read().get, SigCode::Common, SigInfo::new_simple, Some, proc_data.signal.lock ...(+2) |
| `kernel/src/syscall/signal.rs#sys_rt_sigprocmask` | `AstrancE/ulib/axmono/src/syscall/signal.rs#sys_rt_sigprocmask` | 同板块同名 | Err, Ok, check_sigset_size, curr.as_thread, current, debug! ...(+7) | crate::task::signal::sys_sigprocmask |
| `kernel/src/file/pidfd.rs#process_data` | `AstrancE/ulib/axmono/src/task/mod.rs#process_data` | 同板块同名 | Err, self.proc_data.upgrade, self.proc_data.upgrade().ok_or, self.process.is_zombie, thread_exit.load | self.thread.process, self.thread.process().data, self.thread.process().data().unwrap |

#### 任务调度/异步运行时
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `kernel/src/syscall/signal.rs#sys_rt_sigsuspend` | `AstrancE/ulib/axmono/src/task/signal.rs#sys_rt_sigsuspend` | 同板块同名 | LinuxError::EINTR.code, Ok, Poll::Ready, Some, block_on, check_signals ...(+14) | (*mask_ref).into, Err, core::mem::size_of::<sigset_t>, curr.task_ext, curr.task_ext().process_data, curr.task_ext().process_data().signal.lock ...(+7) |
| `kernel/src/syscall/signal.rs#sys_kill` | `AstrancE/ulib/axmono/src/task/signal.rs#sys_kill` | 同板块同名 | Ok, Some, current, current().as_thread, current().as_thread().proc_data.proc.group, current().as_thread().proc_data.proc.group().pgid ...(+9) | Ok, SigInfo_::Generic, Signal::from_u32, current, current().task_ext, current().task_ext().thread.process ...(+8) |
| `kernel/src/syscall/task/clone3.rs#try_from` | `AstrancE/ulib/axmono/src/pthread/mod.rs#try_from` | 同板块同名 | CloneFlags::from_bits_truncate, Err, Ok, flags.contains, flags.intersects, warn! | Self::try_from, Self::try_from((value as u32 & FUTEX_CMD_MASK as u32) as u8).map_err |
| `kernel/src/syscall/ipc/shm.rs#sys_shmdt` | `AstrancE/ulib/axmono/src/syscall/ipc.rs#sys_shmdt` | 同板块同名 | Ok, SHM_MANAGER.lock, VirtAddr::from, aspace.unmap, curr.as_thread, current ...(+15) | (shmaddr as usize).into, axmm_shm_dt, axmm_shm_dt((shmaddr as usize).into(), &mut aspace)
        .map, axmm_shm_dt((shmaddr as usize).into(), &mut aspace)
        .map(|_| 0 as isize) // Return 0 on success
        .map_err, curr.task_ext, curr.task_ext().process_data ...(+3) |

#### 内存管理
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `kernel/src/mm/aspace/mod.rs#handle_page_fault` | `AstrancE/modules/axmm/src/aspace/mod.rs#handle_page_fault` | 同板块同名 | VirtAddrRange::from_start_size, area.backend, area.backend().page_size, area.backend().populate, area.flags, axtask::yield_now ...(+8) | area
                    .backend, area
                    .backend()
                    .clone, area
                    .backend()
                    .clone()
                    .handle_page_fault, area.flags, debug!, orig_flags.contains ...(+2) |
| `kernel/src/mm/access.rs#check_null_terminated` | `AstrancE/ulib/axmono/src/ptr.rs#check_null_terminated` | 同板块同名 | Err, Layout::new::<T>, Layout::new::<T>().align, Ok, T::default, access_user_memory ...(+10) | Err, Layout::new::<T>, Layout::new::<T>().align, Ok, T::default, VirtAddrRange::from_start_size ...(+12) |
| `page_table_multiarch/src/arch/loongarch64.rs#flush_tlb` | `AstrancE/modules/axhal/src/arch/loongarch64/mod.rs#flush_tlb` | 同板块同名 | asid::read, asid::read().asid, asm! | asm! |
| `kernel/src/mm/aspace/mod.rs#process_area_data` | `AstrancE/modules/axmm/src/aspace/mod.rs#process_area_data` | 同板块同名 | (size - cnt).min, (start + size).align_up_4k, Ok, PageIter4K::new, PageIter4K::new(start.align_down_4k(), end_align_up)
            .expect, ax_bail! ...(+8) | Self::process_area_data_with_page_table |

#### 中断/异常处理
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `kernel/src/pseudofs/dev/tty/terminal/termios.rs#default` | `AstrancE/ulib/axsyscall/src/syscall_imp/errno.rs#try_from` | 同模块上下文对齐/差异观察 | ctl | Err, Ok |
| `kernel/src/file/signalfd.rs#from_signal_info` | `AstrancE/modules/axhal/src/arch/loongarch64/unaligned.rs#emulate_load_store_insn` | 同模块上下文对齐/差异观察 | sig_info.code, sig_info.errno, sig_info.signo | asm!, badv::read, badv::read().raw, core::slice::from_raw_parts_mut, panic!, read_fpr ...(+5) |
| `kernel/src/syscall/signal.rs#is_fatal_signal` | `AstrancE/ulib/axlibc/c/pow.c#exp_inline` | 同模块上下文对齐/差异观察 | matches! | __math_oflow, __math_uflow, asdouble, asuint64, converttoint, eval_as_double ...(+4) |
| `tools/ltp-runner.c#drain_child_output` | `AstrancE/modules/axhal/src/arch/aarch64/trap.rs#handle_sync_exception` | 同模块上下文对齐/差异观察 | output_count_feed, read | ESR_EL1.extract, crate::trap::handle_syscall, crate::trap::post_trap_callback, debug!, esr.read, esr.read_as_enum ...(+8) |

#### 系统调用
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `kernel/src/syscall/ipc/shm.rs#sys_shmctl` | `AstrancE/ulib/axmono/src/syscall/ipc.rs#sys_shmctl` | 同板块同名 | Err, Ok, SHM_MANAGER.lock, buf.get_as_mut, crate::syscall::sys::sys_getuid, monotonic_time_nanos ...(+4) | axmm_shm_ctl, axmm_shm_ctl(shmid as usize, cmd, buf as usize)
        .map, axmm_shm_ctl(shmid as usize, cmd, buf as usize)
        .map(|_| 0 as isize) // Return 0 on success
        .map_err, debug! |
| `kernel/src/syscall/time.rs#sys_setitimer` | `AstrancE/ulib/axmono/src/syscall/time.rs#sys_setitimer` | 同板块同名 | ITimerType::from_repr, ITimerType::from_repr(which).ok_or, Ok, TimeValue::default, curr
        .as_thread, curr
        .as_thread()
        .time
        .try_borrow_mut ...(+15) | Err, ItimerConfig::default, Ok, Some, new_value.is_null, old_value.is_null ...(+1) |
| `kernel/src/syscall/io_mpx/epoll.rs#sys_epoll_ctl` | `AstrancE/api/arceos_posix_api/src/imp/io_mpx/epoll.rs#sys_epoll_ctl` | 同板块同名 | Epoll::from_fd, EpollFlags::from_bits, EpollFlags::from_bits(event.events & !events.bits()).ok_or, Err, IoEvents::from_bits_truncate, Ok ...(+7) | debug!, syscall_body! |
| `kernel/src/syscall/io_mpx/poll.rs#sys_ppoll` | `AstrancE/api/arceos_posix_api/src/imp/fd_ops.rs#sys_ppoll` | 同板块同名 | check_sigset_size, do_poll, fds.get_as_mut_slice, nfds.try_into, nfds.try_into().map_err, nullable! ...(+4) | syscall_body! |

#### 文件系统
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `kernel/src/syscall/fs/fd_ops.rs#sys_fcntl` | `AstrancE/api/arceos_posix_api/src/imp/fd_ops.rs#sys_fcntl` | 同板块同名 | FD_TABLE
                .read, FD_TABLE
                .read()
                .get, FD_TABLE
                .read()
                .get(fd as _)
                .ok_or, FD_TABLE.write, FD_TABLE.write().set_cloexec, NodePermission::from_bits_truncate ...(+16) | debug!, syscall_body! |
| `kernel/src/pseudofs/dev/event.rs#read_at` | `AstrancE/modules/axmm/src/shm/posix.rs#read_at` | 同板块同名 | Err, Ok, buf.chunks_exact_mut, buf.is_empty, buf.len, inner.has_event ...(+8) | (self.size - offset).min, Ok, buf.len, buf[bytes_read..bytes_read + bytes_to_copy].copy_from_slice, self
                .get_slice_at, self
                .get_slice_at(current_offset)
                .ok_or ...(+2) |
| `kernel/src/syscall/fs/stat.rs#sys_fstatat` | `AstrancE/api/arceos_posix_api/src/imp/fs.rs#sys_fstatat` | 同板块同名 | Ok, Some, UserConstPtr::from, cache_absolute_stat, cached_absolute_stat, debug! ...(+11) | CURRENT_DIR.lock, CURRENT_DIR.lock().clone, Directory::from_fd, Err, File::new, Ok ...(+15) |
| `kernel/src/syscall/fs/ctl.rs#sys_utimensat` | `AstrancE/api/arceos_posix_api/src/imp/fs.rs#sys_utimensat` | 同板块同名 | Ok, Some, atime.is_none, mtime.is_none, path.is_null, time.try_into_time_value ...(+8) | CURRENT_DIR.lock, CURRENT_DIR.lock().clone, Directory::from_fd, Directory::from_fd(dirfd).map_err, Err, File::new ...(+19) |

#### 设备驱动
仅参考项目中观察到该模块代表函数，目标项目没有同模块可对齐证据：
- 参考项目：`AstrancE/modules/axdriver/src/lib.rs#init_drivers`，调用摘要：AllDevices::default, all_devs.block.iter_mut, all_devs.block.iter_mut().enumerate, all_devs.display.iter_mut, all_devs.display.iter_mut().enumerate, all_devs.net.iter_mut ...(+14)
- 参考项目：`AstrancE/tools/raspi4/chainloader/src/bsp/device_driver/bcm/bcm2xxx_pl011_uart.rs#init`，调用摘要：FBRD::BAUD_DIVFRAC.val, IBRD::BAUD_DIVINT.val, self.flush, self.registers
            .CR
            .write, self.registers
            .LCR_H
            .write, self.registers.CR.set ...(+3)
- 参考项目：`AstrancE/tools/bwbench_client/src/device.rs#new`，调用摘要：ETH_P_ALL.to_be, Err, Ok, dev.bind_interface, dev.interface_mtu, ifreq.ioctl ...(+5)
- 参考项目：`AstrancE/tools/bwbench_client/src/main.rs#transmit_benchmark`，调用摘要：Local::now, NetDevice::new, NetDevice::new(interface).unwrap, current_time.signed_duration_since, current_time.signed_duration_since(past_time).num_seconds, dev.send ...(+2)

#### 网络协议栈
仅参考项目中观察到该模块代表函数，目标项目没有同模块可对齐证据：
- 参考项目：`AstrancE/ulib/axlibc/c/network.c#inet_pton`，调用摘要：hexval, inet_pton, isdigit, memmove
- 参考项目：`AstrancE/api/arceos_posix_api/src/imp/net.rs#shutdown`，调用摘要：Ok, tcpsocket.lock, tcpsocket.peer_addr, tcpsocket.shutdown, udpsocket.lock, udpsocket.peer_addr ...(+1)
- 参考项目：`AstrancE/ulib/axlibc/c/socket.c#accept4`，调用摘要：accept, fcntl
- 参考项目：`AstrancE/api/arceos_posix_api/src/imp/net.rs#recvfrom`，调用摘要：Ok, Some, tcpsocket.lock, tcpsocket.lock().recv, tcpsocket.lock().recv(buf).map, udpsocket
                .lock ...(+2)

#### 文件系统/第三方库
仅参考项目中观察到该模块代表函数，目标项目没有同模块可对齐证据：
- 参考项目：`AstrancE/crates/lwext4_rust/c/lwext4/src/ext4_balloc.c#ext4_balloc_alloc_block`，调用摘要：ext4_balloc_get_bgid_of_block, ext4_balloc_get_block_of_bgid, ext4_balloc_set_bitmap_csum, ext4_balloc_verify_bitmap_csum, ext4_bg_get_block_bitmap, ext4_bg_get_free_blocks_count ...(+19)
- 参考项目：`AstrancE/crates/lwext4_rust/c/lwext4/src/ext4_journal.c#jbd_journal_prepare`，调用摘要：EXT4_BLOCK_ZERO, RB_FIND, TAILQ_FOREACH_REVERSE_SAFE, TAILQ_FOREACH_SAFE, TAILQ_REMOVE, ext4_bcache_clear_dirty ...(+17)
- 参考项目：`AstrancE/crates/lwext4_rust/c/lwext4/src/ext4.c#ext4_dir_rm`，调用摘要：EXT4_MP_LOCK, EXT4_MP_UNLOCK, ext4_block_cache_write_back, ext4_dir_en_get_inode, ext4_dir_iterator_fini, ext4_dir_iterator_init ...(+19)
- 参考项目：`AstrancE/crates/lwext4_rust/c/lwext4/src/ext4_dir_idx.c#ext4_dir_dx_split_index`，调用摘要：ext4_block_set, ext4_dir_dx_climit_get_count, ext4_dir_dx_climit_get_limit, ext4_dir_dx_climit_set_count, ext4_dir_dx_climit_set_limit, ext4_dir_dx_entry_get_hash ...(+10)

#### 网络协议栈/第三方库
仅参考项目中观察到该模块代表函数，目标项目没有同模块可对齐证据：
- 参考项目：`AstrancE/crates/virtio-drivers/src/device/socket/connectionmanager.rs#send_recv`，调用摘要："Hello from guest".as_bytes, (hello_from_guest.len() as u32).into, (hello_from_host.len() as u32).into, 0.into, 50.into, Arc::new ...(+43)
- 参考项目：`AstrancE/crates/virtio-drivers/src/device/socket/connectionmanager.rs#incoming_connection`，调用摘要：0.into, 50.into, Arc::new, Mutex::new, ReadOnly::new, SocketType::Stream.into ...(+24)
- 参考项目：`AstrancE/crates/virtio-drivers/src/device/socket/connectionmanager.rs#poll`，调用摘要：Connection::new, Err, Ok, SocketError::OutputBufferTooShort, SocketError::OutputBufferTooShort(length).into, Some ...(+16)
- 参考项目：`AstrancE/crates/virtio-drivers/examples/x86_64/src/tcp.rs#test_echo_server`，调用摘要：Config::new, DeviceWrapper::new, Instant::from_micros_const, Interface::new, IpAddress::from_str, IpAddress::from_str(IP).unwrap ...(+44)

#### 第三方库
仅参考项目中观察到该模块代表函数，目标项目没有同模块可对齐证据：
- 参考项目：`AstrancE/crates/axdriver_crates/axdriver_block/src/dw_mshc/mmc.rs#card_init`，调用摘要：CID::from, CLKDIV::offset, CMD::data_cmd, CMD::no_data_cmd, CMD::no_data_cmd_no_crc, CMD::no_data_cmd_no_crc(card_idx, 2).with_response_length ...(+33)
- 参考项目：`AstrancE/crates/virtio-drivers/src/queue.rs#fake_read_write_queue`，调用摘要：(*available_ring).idx.load, (*used_ring).idx.fetch_add, (*used_ring).idx.load, Vec::new, assert!, assert_eq! ...(+18)
- 参考项目：`AstrancE/crates/virtio-drivers/src/device/sound/fake.rs#handle_control_request`，调用摘要：CommandCode::SOk.into, Some, Vec::new, VirtIOSndHdr {
                            command_code: CommandCode::SOk.into(),
                        }
                        .as_bytes, VirtIOSndHdr::read_from_prefix, VirtIOSndHdr::read_from_prefix(&request)
                .expect ...(+16)
- 参考项目：`AstrancE/crates/virtio-drivers/src/transport/pci.rs#new`，调用摘要：Err, Ok, Some, UniqueMmioPointer::new, VirtioPciError::InvalidDeviceId, VirtioPciError::InvalidNotifyOffMultiplier ...(+15)

#### 通用模块/其他
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `tools/ltp-runner.c#exists` | `AstrancE/ulib/axlibc/c/log.c#top16` | 同板块同结构 | access | asuint64 |
| `tools/ltp-runner.c#output_write_legacy_result` | `AstrancE/ulib/axlibc/c/printf.c#print_exponential_number` | 同模块上下文对齐/差异观察 | fputc, fputs, fwrite, is_horizontal_space, is_ltp_status_token | ABS_FOR_PRINTING, PRINTF_ABS, bastardized_floor, get_components, get_normalized_components, get_sign_bit ...(+5) |
| `tools/ltp-runner.c#output_count_line` | `AstrancE/ulib/axlibc/c/pow.c#pow` | 同模块上下文对齐/差异观察 | is_horizontal_space, is_ltp_status_token, is_status_token_boundary, is_summary_start, strncmp | __builtin_fma, __math_invalid, __math_oflow, __math_uflow, asdouble, asuint64 ...(+8) |
| `tools/ltp-runner.c#output_write_normalized` | `AstrancE/ulib/axlibc/c/string.c#twoway_strstr` | 同模块上下文对齐/差异观察 | fputc, fwrite, is_horizontal_space, is_ltp_status_token, is_status_token_boundary, output_write_legacy_result | BITOP, MAX, memchr, memcmp |


好的，我将遵循审计指令，基于提供的证据材料，进行最终的同源性定性仲裁。

### 1. 宏观判定
- **定性结论**: **独立开发**
- **核心依据**: 客观 AST 相似度仅为 **2.24%**，双向覆盖率极低（目标项目 9.09%，参考项目 2.88%）。在双方共存的【进程管理】、【任务调度】、【内存管理】等核心模块中，共同 AST 指纹数量稀少，且经技术分析，这些重合点源于 Linux 标准 ABI 约束、Rust 语言标准库 API 或操作系统通用设计范式，不构成代码抄袭或架构复用的实质性证据。双方在关键子系统（如进程模型、VFS、信号处理）的架构设计与实现路径上存在根本性差异。

### 2. 全模块覆盖矩阵解读
| 模块 | 双方存在 | 共同指纹 | 覆盖率解读 | 证据强度 |
| :--- | :--- | :--- | :--- | :--- |
| **进程管理** | 是 | 2 | 目标覆盖率 2.99%，参考覆盖率 2.17%。重合点仅为两个由 Linux ABI 和 Rust 原子操作 API 决定的单行函数。 | **弱** |
| **任务调度/异步运行时** | 是 | 6 | 目标覆盖率 2.6%，参考覆盖率 1.87%。重合点包含通用宏、标准系统调用名和跨模块的通用结构，无私有实现逻辑重合。 | **弱** |
| **内存管理** | 是 | 7 | 目标覆盖率 2.57%，参考覆盖率 2.27%。重合点多为标准缺页处理流程和架构强相关的 TLB 刷新指令，非逻辑复用。 | **弱** |
| **中断/异常处理** | 是 | 0 | 覆盖率为 0。双方在该模块无任何共同 AST 指纹。 | **未形成** |
| **系统调用** | 是 | 0 | 覆盖率为 0。尽管存在大量同名函数（Linux 标准系统调用），但 AST 层面无重合。 | **未形成** |
| **文件系统** | 是 | 4 | 目标覆盖率 0.94%，参考覆盖率 0.82%。重合点均为标准 POSIX 接口，内部实现路径完全不同。 | **弱** |
| **设备驱动** | 仅参考 | 0 | 目标项目无此模块，无法比较。 | **未形成** |
| **网络协议栈** | 仅参考 | 0 | 目标项目无此模块，无法比较。 | **未形成** |
| **第三方库/公共依赖** | 仅参考 | 0 | 参考项目包含大量第三方库（ext4, virtio-drivers, lwip 等），目标项目无对应模块。 | **未形成** |

### 3. 底层 AST 关键证据指纹表
| 所属板块 | 目标项目路径 | 参考项目路径 | 证据类型 | 技术说明 |
| :--- | :--- | :--- | :--- | :--- |
| 进程管理 | `kernel/src/task/mod.rs#set_clear_child_tid` | `AstrancE/ulib/axmono/src/task/process.rs#set_clear_child_tid` | 同板块底层逻辑一致 | 字段名 `clear_child_tid` 源于 Linux `clone` 系统调用的 ABI 规范；`.store` 是 Rust `AtomicUsize` 类型的唯一标准写入方法。此重合是领域规范与语言标准库共同作用下的必然收敛。 |
| 进程管理 | `kernel/src/task/mod.rs#clear_child_tid` | `AstrancE/ulib/axmono/src/task/process.rs#clear_child_tid` | 同板块底层逻辑一致 | 同上，`.load` 是 Rust `AtomicUsize` 类型的唯一标准读取方法。这对 getter/setter 函数是任何实现 `set_tid_address` 机制的 Linux 兼容内核的最小必要接口。 |
| 进程管理 | `kernel/src/task/ops.rs#processes` | `AstrancE/ulib/axmono/src/task/mod.rs#processes` | 同板块同名 | `PROCESS_TABLE.read().values().collect()` 是遍历全局进程表的通用 Rust 惯用写法，属于功能型 API 调用，不涉及私有数据结构或算法逻辑。 |
| 进程管理 | `kernel/src/syscall/task/wait.rs#apply` | `AstrancE/ulib/axmono/src/task/wait.rs#apply` | 同板块同名 | `child.group().pgid, child.pid` 是 waitpid 标准 POSIX 语义下获取进程组和进程 ID 的必要步骤，逻辑必然相同。 |
| 任务调度 | `kernel/src/syscall/signal.rs#sys_rt_sigsuspend` | `AstrancE/ulib/axmono/src/task/signal.rs#sys_rt_sigsuspend` | 同板块同名 | 函数名是 Linux 标准系统调用名。目标项目使用 `block_on` 异步等待，参考项目直接操作 `curr.task_ext`，实现模型完全不同。 |
| 任务调度 | `kernel/src/syscall/task/clone3.rs#try_from` | `AstrancE/ulib/axmono/src/pthread/mod.rs#try_from` | 同板块同名 | 函数名 `try_from` 是 Rust 标准库 `TryFrom` trait 的强制方法名。双方转换的对象（`CloneFlags` vs `FutexCmd`）完全不同。 |
| 内存管理 | `kernel/src/mm/aspace/mod.rs#handle_page_fault` | `AstrancE/modules/axmm/src/aspace/mod.rs#handle_page_fault` | 同板块同名 | 函数名是描述缺页处理的标准术语。目标项目直接操作 `area.backend` 进行填充，参考项目则克隆 backend 后调用其方法，对象管理模型不同。 |
| 内存管理 | `page_table_multiarch/src/arch/loongarch64.rs#flush_tlb` | `AstrancE/modules/axhal/src/arch/loongarch64/mod.rs#flush_tlb` | 同板块同名 | 均包含 `asm!` 宏，用于执行 LoongArch 架构的 `tlbflush` 指令。这是 ISA 手册规定的唯一实现方式，属于硬件强相关代码。 |
| 文件系统 | `kernel/src/syscall/fs/fd_ops.rs#sys_fcntl` | `AstrancE/api/arceos_posix_api/src/imp/fd_ops.rs#sys_fcntl` | 同板块同名 | 函数名是 POSIX 标准系统调用名。目标项目有详细的 `FD_TABLE` 操作和 `NodePermission` 处理，参考项目仅用 `syscall_body!` 宏转发，实现细节差异巨大。 |
| 文件系统 | `kernel/src/syscall/fs/stat.rs#sys_fstatat` | `AstrancE/api/arceos_posix_api/src/imp/fs.rs#sys_fstatat` | 同板块同名 | 同为 POSIX 标准接口。目标项目使用 `cache_absolute_stat` 缓存机制，参考项目通过 `CURRENT_DIR` 和 `Directory::from_fd` 解析路径，实现策略完全不同。 |

### 4. 核心代码调用流对比剖析
* **【进程管理/进程退出路径】**
* **底层技术析理**: 双方均实现了 `do_exit` 函数，但进程退出时的资源清理模型截然不同。目标项目采用基于 Futex 的健壮列表清理 (`exit_robust_list`, `handle_futex_death`) 和 ELF 缓存清理 (`clear_elf_cache`) 机制，并通过 `SignalInfo::new_kernel` 构建信号信息。参考项目则采用全局文件描述符表清空 (`FD_TABLE.clear`)、`SigStatus::ExitCode` 状态传递和 `UserPtr` 写入 `clear_child_tid` 的方式。两者在同步原语、内存管理和信号传递模型上存在根本性架构差异。
* **代码溯源链接**:
  - 目标项目: `kernel/src/task/ops.rs#do_exit`
  - 参考项目: `AstrancE/ulib/axmono/src/task/exit.rs#do_exit`

* **【内存管理/缺页处理路径】**
* **底层技术析理**: 在 `handle_page_fault` 的核心路径上，目标项目直接获取 `area.backend` 的可变引用并调用 `populate` 方法进行物理页分配，体现了更直接的内存对象管理。参考项目则先 `clone` 后端对象，再通过克隆体调用 `handle_page_fault`，展现了不同的所有权和并发控制策略。这表明双方在内存管理子系统的抽象层次和对象生命周期管理上存在独立的设计选择。
* **代码溯源链接**:
  - 目标项目: `kernel/src/mm/aspace/mod.rs#handle_page_fault`
  - 参考项目: `AstrancE/modules/axmm/src/aspace/mod.rs#handle_page_fault`

* **【文件系统/伪文件系统路径】**
* **底层技术析理**: 在构建 `/proc` 文件系统时，目标项目使用 `NodeOpsMux::Dir`、`SimpleDir::new_maker` 和 `SimpleFile::new` 等原语，通过 `FD_TABLE.scope` 动态获取进程的文件描述符信息。参考项目则使用 `ProcDir::new`、`ProcEntry::Dir` 和 `proc_root.create_static_file` 等完全不同的 VFS 抽象。双方在 VFS 的节点类型、目录项生成和挂载方式上无任何共同指纹，证明其 VFS 层是独立构建的。
* **代码溯源链接**:
  - 目标项目: `kernel/src/pseudofs/proc.rs#lookup_child`
  - 参考项目: `AstrancE/ulib/axmono/src/fs/mod.rs#create_pid_dir_generator`

### 5. 单方模块与降权项
*   **单方模块**: 参考项目独有的【设备驱动】、【网络协议栈】及大量【第三方库】（如 `lwext4_rust`, `virtio-drivers`）模块，目标项目均无对应实现。这体现了双方在功能范围和项目复杂度上的巨大差异，无法形成同源性比较。
*   **降权项说明**:
    *   **标准系统调用名**: `sys_getpid`, `sys_exit`, `sys_fcntl` 等均为 POSIX/Linux 标准接口名，任何兼容内核都必须使用，其同名性不能作为复用证据。
    *   **通用宏与标准 API**: `syscall_body!`, `debug!`, `Arc::new`, `Box::new`, `Ok`, `Err` 等是 Rust 语言和宏内核的通用范式，高频出现是正常现象，不具备特异性。
    *   **跨板块硬凑**: 控方证据中混入了参考项目的 `tools/raspi4/chainloader` (bootloader) 和 `panic_wait` 等与【进程管理】无关的模块，此类证据无效。

### 6. 保守结论
本次审计的客观 AST 相似度仅为 2.24%，远低于可认定代码抄袭的阈值。控方提出的“底层逻辑一致”指控，经技术分析，其核心证据 `set_clear_child_tid` 与 `clear_child_tid` 函数的重合，是 Linux 内核 ABI 规范与 Rust 语言标准原子操作 API 共同约束下的必然结果，属于领域内“最小必要接口”的趋同实现，不构成独创性表达的抄袭。除此之外，双方在进程退出模型、VFS 架构、内存管理策略等核心子系统上展现出显著且根本的差异。综合判定，目标项目 `[2026-T2026104869910383-XX]` 相对于参考项目 `[2025-AstranciA-哈尔滨工业大学深圳]` 具有充分的独立开发合理性，复用指控证据不足。

------------------------------

