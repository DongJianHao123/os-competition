# 🛡️ OS 内核赛自动化代码级架构审计报告

**审计时间**: `2026-07-02 05:19:25`
**参赛作品名称**: `2026-T2026104869910383-XX`
**年份**: `2026`
**学校**: `XX`
**队伍名称**: `T2026104869910383`
**比赛名字**: `OS-Competition`
**比赛子赛道**: `Kernel-Implementation`
**仓库地址**: `https://gitlab.eduxiji.net/educg-group-43501-3132633/T2026104869910383-3620`
**推测开发语言**: `Rust / C (由AST探针自动识别)`
**特征函数量**: `1349`

## ⚔️ 底层代码同源性对比 (AST Homology)
> 本部分由专门的代码审查智能体，对该项目与符合年份条件的历届最高相似度项目进行控制流与底层调用对比。新版报告会先给出确定性的全模块覆盖矩阵，再给出 Agent 的分模块解释，避免只围绕少量抽样函数展开。

### 🏆 Top 4 底层对标: vs `2025-undefined-清华大学`（清华大学 / undefined）

#### 对方作品信息

**对方年份**: `2025`
**对方学校**: `清华大学`
**对方队伍名称**: `undefined`
**对方比赛名字**: `OS-Competition`
**对方比赛子赛道**: `Kernel-Implementation`
**对方仓库地址**: `https://gitlab.eduxiji.net/educg-group-36002-2710490/T202510003995291-2331`

> 双向覆盖率：共同 AST 指纹: 158；唯一 AST 指纹: 目标项目 1188，参考项目 2820；覆盖率: 目标项目 13.3%，参考项目 5.6%

## 全模块对比总览（确定性证据）

> 总体覆盖率：共同 AST 指纹: 158；唯一 AST 指纹: 目标项目 1188，参考项目 2820；覆盖率: 目标项目 13.3%，参考项目 5.6%

### 1. 模块覆盖矩阵
| 模块 | 目标函数数 | 参考函数数 | 共同唯一 AST 指纹 | Target 覆盖 | Base 覆盖 | 状态 |
| :--- | ---: | ---: | ---: | ---: | ---: | :--- |
| 进程管理 | 71 | 135 | 3 | 4.48% | 2.48% | 双方存在且有结构重合 |
| 任务调度/异步运行时 | 245 | 415 | 2 | 0.87% | 0.67% | 双方存在且有结构重合 |
| 内存管理 | 308 | 450 | 84 | 30.88% | 21.99% | 双方存在且有结构重合 |
| 中断/异常处理 | 5 | 53 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |
| 系统调用 | 181 | 109 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |
| 文件系统 | 496 | 999 | 49 | 11.56% | 7.34% | 双方存在且有结构重合 |
| 设备驱动 | 0 | 133 | 0 | 0.0% | 0.0% | 仅参考项目体现 |
| 网络协议栈 | 0 | 65 | 0 | 0.0% | 0.0% | 仅参考项目体现 |
| 文件系统/第三方库 | 0 | 643 | 0 | 0.0% | 0.0% | 仅参考项目体现 |
| 网络协议栈/第三方库 | 0 | 130 | 0 | 0.0% | 0.0% | 仅参考项目体现 |
| 通用模块/其他 | 43 | 555 | 1 | 2.94% | 0.23% | 双方存在且有结构重合 |

### 2. 分模块证据清单

#### 进程管理
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `kernel/src/task/ops.rs#do_exit` | `arceos/examples/shell/src/cmd.rs#do_exit` | 同板块同名 | FutexKey::new_current, SignalInfo::new_kernel, Some, axtask::yield_now, clear_child_tid.is_null, clear_child_tid.vm_write ...(+33) | println!, std::process::exit |
| `kernel/src/syscall/signal.rs#sys_rt_sigprocmask` | `api/src/imp/task/signal.rs#sys_rt_sigprocmask` | 同板块同名 | Err, Ok, check_sigset_size, curr.as_thread, current, debug! ...(+7) | Err, Ok, check_sigset_size, current_thread_data, current_thread_data()
        .signal
        .with_blocked_mut::<LinuxResult<_>>, oldset.nullable ...(+1) |
| `kernel/src/task/ops.rs#get_process_group` | `process/src/process_group.rs#get_process_group` | 同板块同名 | Ok, PROCESS_GROUP_TABLE.read, PROCESS_GROUP_TABLE.read().get, PROCESS_GROUP_TABLE.write, PROCESS_GROUP_TABLE.write().insert, PROCESS_TABLE
        .read ...(+7) | PROCESS_GROUP_TABLE.lock, process_group_table.get, process_group_table.get(&pgid).cloned |
| `kernel/src/syscall/task/wait.rs#apply` | `api/src/imp/task/wait.rs#apply` | 同板块同名 | child.group, child.group().pgid, child.pid | child.get_group, child.get_group().get_pgid, child.get_pid |

#### 任务调度/异步运行时
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `kernel/src/syscall/sync/futex.rs#sys_futex` | `api/src/imp/task/futex.rs#sys_futex` | 同板块同名 | AxError::from, Err, FutexKey::new_current, Ok, Some, assert_unsigned ...(+22) | Err, Ok, axtask::yield_now, current_process_data, drop, futex_table
                .entry ...(+27) |
| `kernel/src/syscall/signal.rs#sys_rt_sigsuspend` | `api/src/imp/task/signal.rs#sys_rt_sigsuspend` | 同板块同名 | LinuxError::EINTR.code, Ok, Poll::Ready, Some, block_on, check_signals ...(+14) | LinuxError::EINTR.code, Ok, Some, check_signals, check_sigset_size, current_process_data ...(+7) |
| `kernel/src/syscall/signal.rs#sys_kill` | `api/src/imp/task/signal.rs#sys_kill` | 同板块同名 | Ok, Some, current, current().as_thread, current().as_thread().proc_data.proc.group, current().as_thread().proc_data.proc.group().pgid ...(+9) | Err, Ok, current_process, current_process().get_group, debug!, get_all_processes ...(+8) |
| `kernel/src/syscall/task/clone.rs#sys_clone` | `api/src/interface/task/clone.rs#sys_clone` | 同板块同名 | CloneFlags::from_bits_truncate, Err, args.do_clone, clone_flags.contains | addr_child_tid.clone, addr_parent_tid.clone, sys_clone_ |

#### 内存管理
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `kernel/src/syscall/mod.rs#handle_syscall` | `src/syscall.rs#handle_syscall` | 同板块同名 | Err, LinuxError::ENOSYS.code, LinuxError::from, LinuxError::from(err).code, Ok, PERSONALITY.load ...(+239) | Err, Sysno::new, err.code, info!, result.unwrap_or_else, set_trap_frame ...(+151) |
| `kernel/src/syscall/mm/mmap.rs#sys_mmap` | `api/src/imp/mm/mmap.rs#sys_mmap` | 同板块同名 | (addr + length).align_up, Arc::new, Backend::new_alloc, Backend::new_cow, Backend::new_file, Backend::new_linear ...(+56) | Err, File::from_fd, MmapFlags::from_bits_truncate, MmapProt::from_bits_truncate, Ok, PhysAddr::from ...(+34) |
| `kernel/src/mm/loader.rs#load_user_app` | `core/src/mm.rs#load_user_app` | 同板块同名 | (ustack_top - user_sp_aligned).align_up_4k, Backend::new_alloc, ELF_LOADER.lock, ELF_LOADER.lock().load, Err, Ok ...(+48) | CStr::from_bytes_with_nul, CStr::from_bytes_with_nul(interp)
            .map_err, CStr::from_bytes_with_nul(interp)
            .map_err(|_| AxError::InvalidData)?
            .to_str, CStr::from_bytes_with_nul(interp)
            .map_err(|_| AxError::InvalidData)?
            .to_str()
            .map_err, CStr::from_bytes_with_nul(interp)
            .map_err(|_| AxError::InvalidData)?
            .to_str()
            .map_err(|_| AxError::InvalidData)?
            .to_string, ElfFile::new ...(+34) |
| `kernel/src/syscall/ipc/shm.rs#sys_shmat` | `api/src/interface/mm/shm.rs#sys_shmat` | 同板块同名 | Arc::new, Backend::new_shared, Ok, SHM_MANAGER.lock, SharedPages::new, ShmAtFlags::from_bits_truncate ...(+32) | Err, Ok, SHARED_MEMORY_MANAGER.get, SHARED_MEMORY_MANAGER.get(key).ok_or, ShmFlags::from_bits_truncate, Some ...(+16) |

#### 中断/异常处理
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `kernel/src/pseudofs/dev/tty/terminal/termios.rs#default` | `arceos/ulib/axlibc/c/pow.c#exp_inline` | 同模块上下文对齐/差异观察 | ctl | __math_oflow, __math_uflow, asdouble, asuint64, converttoint, eval_as_double ...(+4) |
| `kernel/src/file/signalfd.rs#from_signal_info` | `arceos/modules/axhal/src/arch/aarch64/trap.rs#handle_sync_exception` | 同模块上下文对齐/差异观察 | sig_info.code, sig_info.errno, sig_info.signo | ESR_EL1.extract, crate::trap::handle_syscall, crate::trap::post_trap_callback, debug!, esr.read, esr.read_as_enum ...(+6) |
| `kernel/src/syscall/signal.rs#is_fatal_signal` | `arceos/modules/axhal/src/arch/x86_64/idt.rs#new` | 同模块上下文对齐/差异观察 | matches! | InterruptDescriptorTable::new, core::mem::transmute, core::slice::from_raw_parts_mut, entries[i].set_handler_fn, opt.set_privilege_level |
| `tools/ltp-runner.c#drain_child_output` | `arceos/modules/axhal/src/platform/riscv64_qemu_virt/irq.rs#register_handler` | 同模块上下文对齐/差异观察 | output_count_feed, read | with_cause! |

#### 系统调用
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `kernel/src/syscall/net/opt.rs#sys_setsockopt` | `api/src/imp/net/socket.rs#sys_setsockopt` | 同板块同名 | Err, Ok, Socket::from_fd, call_dispatch!, debug!, size_of::<T> ...(+2) | Err, Ok, Socket::from_fd, Socket::from_fd(sockfd)?.setsockopt, core::slice::from_raw_parts, debug! ...(+1) |
| `kernel/src/syscall/time.rs#sys_times` | `api/src/imp/utils/time.rs#sys_times` | 同板块同名 | Ok, current, current()
        .as_thread, current()
        .as_thread()
        .time
        .try_borrow, current()
        .as_thread()
        .time
        .try_borrow()
        .map, current()
        .as_thread()
        .time
        .try_borrow()
        .map(|time| time.output())
        .unwrap_or_default ...(+6) | Ok, monotonic_time_nanos, nanos_to_ticks, time_stat_output, tms.get |
| `kernel/src/syscall/net/socket.rs#sys_connect` | `api/src/imp/net/socket.rs#sys_connect` | 同板块同名 | Ok, Socket::from_fd, Socket::from_fd(fd)?.connect, Socket::from_fd(fd)?.connect(addr).map_err, SocketAddrEx::read_from_user, debug! | Ok, Socket::from_fd, Socket::from_fd(socket_fd)?.connect, debug!, from_sockaddr |
| `kernel/src/syscall/net/socket.rs#sys_shutdown` | `api/src/imp/net/socket.rs#sys_shutdown` | 同板块同名 | Err, Socket::from_fd, debug!, socket.shutdown, socket.shutdown(how).map | Ok, Socket::from_fd, Socket::from_fd(socket_fd)?.shutdown, debug! |

#### 文件系统
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `kernel/src/pseudofs/dev/fb.rs#ioctl` | `api/src/core/fs/imp/dev/framebuffer.rs#ioctl` | 同板块同名 | (arg as *mut FixScreenInfo).vm_write, (arg as *mut VarScreenInfo).vm_write, Err, Ok, axdisplay::framebuffer_info | Default::default, Err, Ok, arg.into, framebuffer_flush, user_info.get_as_mut_ref |
| `kernel/src/pseudofs/dev/loop.rs#ioctl` | `api/src/core/fs/pseudo/file.rs#ioctl` | 同板块同名 | (arg as *const loop_info).vm_read_uninit, (arg as *const loop_info).vm_read_uninit()?.assume_init, (arg as *const u32).vm_read, (arg as *mut loop_info).vm_write, (arg as *mut u32).vm_write, (arg as *mut u64).vm_write ...(+22) | Err, warn! |
| `kernel/src/syscall/fs/io.rs#sys_splice` | `api/src/interface/fs/io.rs#sys_splice` | 同板块同名 | DummyFd::from_fd, DummyFd::from_fd(fd_in).is_ok, DummyFd::from_fd(fd_out).is_ok, Err, File::from_fd, Pipe::from_fd ...(+20) | Err, Ok, fd_lookup, file_like_as::<Pipe>, file_like_as::<Pipe>(in_file.clone()).is_none, file_like_as::<Pipe>(out_file.clone()).is_none ...(+6) |
| `kernel/src/file/fs.rs#get` | `api/src/core/file/fd.rs#get` | 同板块同名 | (self.seq.load(Ordering::Acquire) == seq).then_some, Duration::new, axfs_ng_vfs::DeviceId, self.atime_nsec.load, self.atime_sec.load, self.blksize.load ...(+15) | Ok, item.file_like.clone, self.inner.read, table.get, table.get(fd as _).ok_or |

#### 设备驱动
仅参考项目中观察到该模块代表函数，目标项目没有同模块可对齐证据：
- 参考项目：`arceos/tools/raspi4/chainloader/src/bsp/device_driver/bcm/bcm2xxx_pl011_uart.rs#init`，调用摘要：FBRD::BAUD_DIVFRAC.val, IBRD::BAUD_DIVINT.val, self.flush, self.registers
            .CR
            .write, self.registers
            .LCR_H
            .write, self.registers.CR.set ...(+3)
- 参考项目：`arceos/tools/bwbench_client/src/device.rs#new`，调用摘要：ETH_P_ALL.to_be, Err, Ok, dev.bind_interface, dev.interface_mtu, ifreq.ioctl ...(+5)
- 参考项目：`arceos/tools/bwbench_client/src/main.rs#transmit_benchmark`，调用摘要：Local::now, NetDevice::new, NetDevice::new(interface).unwrap, current_time.signed_duration_since, current_time.signed_duration_since(past_time).num_seconds, dev.send ...(+2)
- 参考项目：`arceos/tools/bwbench_client/src/device.rs#bind_interface`，调用摘要：ETH_P_ALL.to_be, Err, Ok, libc::bind, self.ifreq.ioctl, std::io::Error::last_os_error ...(+2)

#### 网络协议栈
仅参考项目中观察到该模块代表函数，目标项目没有同模块可对齐证据：
- 参考项目：`arceos/ulib/axlibc/c/network.c#inet_pton`，调用摘要：hexval, inet_pton, isdigit, memmove
- 参考项目：`api/src/imp/net/socket.rs#setsockopt`，调用摘要：Ok, SocketOptionLevel::try_from, error!, udpsocket.lock, unimplemented!, warn!
- 参考项目：`api/src/imp/net/socket.rs#sendto`，调用摘要：Err, IpAddr::V4, Ipv4Addr::new, SocketAddr::new, udpsocket
                    .bind, udpsocket
                    .bind(SocketAddr::new(IpAddr::V4(Ipv4Addr::new(127, 0, 0, 1)), 0))
                    .map_err ...(+3)
- 参考项目：`api/src/imp/net/socket.rs#shutdown`，调用摘要：Ok, tcpsocket.lock, tcpsocket.peer_addr, tcpsocket.shutdown, udpsocket.lock, udpsocket.peer_addr ...(+1)

#### 文件系统/第三方库
仅参考项目中观察到该模块代表函数，目标项目没有同模块可对齐证据：
- 参考项目：`modules/lwext4_rust/c/lwext4/src/ext4_balloc.c#ext4_balloc_alloc_block`，调用摘要：ext4_balloc_get_bgid_of_block, ext4_balloc_get_block_of_bgid, ext4_balloc_set_bitmap_csum, ext4_balloc_verify_bitmap_csum, ext4_bg_get_block_bitmap, ext4_bg_get_free_blocks_count ...(+19)
- 参考项目：`modules/lwext4_rust/c/lwext4/src/ext4_journal.c#jbd_journal_prepare`，调用摘要：EXT4_BLOCK_ZERO, RB_FIND, TAILQ_FOREACH_REVERSE_SAFE, TAILQ_FOREACH_SAFE, TAILQ_REMOVE, ext4_bcache_clear_dirty ...(+17)
- 参考项目：`modules/lwext4_rust/c/lwext4/src/ext4.c#ext4_dir_rm`，调用摘要：EXT4_MP_LOCK, EXT4_MP_UNLOCK, ext4_block_cache_write_back, ext4_dir_en_get_inode, ext4_dir_iterator_fini, ext4_dir_iterator_init ...(+19)
- 参考项目：`modules/lwext4_rust/c/lwext4/src/ext4_dir_idx.c#ext4_dir_dx_split_index`，调用摘要：ext4_block_set, ext4_dir_dx_climit_get_count, ext4_dir_dx_climit_get_limit, ext4_dir_dx_climit_set_count, ext4_dir_dx_climit_set_limit, ext4_dir_dx_entry_get_hash ...(+10)

#### 网络协议栈/第三方库
仅参考项目中观察到该模块代表函数，目标项目没有同模块可对齐证据：
- 参考项目：`arceos/modules/axnet/src/smoltcp_impl/tcp.rs#connect`，调用摘要：Err, Ok, Ok::<(IpEndpoint, IpEndpoint), AxError>, SOCKET_SET
                .with_socket_mut::<tcp::Socket, _, _>, SOCKET_SET.add, SocketSetWrapper::new_tcp_socket ...(+32)
- 参考项目：`arceos/modules/axnet/src/smoltcp_impl/dns.rs#query`，调用摘要：Err, Ok, SOCKET_SET
            .with_socket_mut::<dns::Socket, _, _>, SOCKET_SET
            .with_socket_mut::<dns::Socket, _, _>(handle, |socket| {
                socket.start_query(iface.lock().context(), name, query_type)
            })
            .map_err, SOCKET_SET.poll_interfaces, SOCKET_SET.with_socket_mut::<dns::Socket, _, _> ...(+12)
- 参考项目：`arceos/modules/axnet/src/smoltcp_impl/tcp.rs#recv_timeout`，调用摘要：Err, Ok, SOCKET_SET.with_socket_mut::<tcp::Socket, _, _>, ax_err!, ax_err_type!, current_ticks ...(+11)
- 参考项目：`arceos/modules/axnet/src/smoltcp_impl/bench.rs#bench_transmit_bandwidth`，调用摘要：(current_time - past_time).secs, AxNetTxToken::consume, InterfaceWrapper::current_time, info!, self.transmit, tx_buf[0..12].fill ...(+2)

#### 通用模块/其他
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `tools/ltp-runner.c#exists` | `arceos/ulib/axlibc/c/log.c#top16` | 同板块同结构 | access | asuint64 |
| `tools/ltp-runner.c#output_write_legacy_result` | `arceos/ulib/axlibc/c/printf.c#print_exponential_number` | 同模块上下文对齐/差异观察 | fputc, fputs, fwrite, is_horizontal_space, is_ltp_status_token | ABS_FOR_PRINTING, PRINTF_ABS, bastardized_floor, get_components, get_normalized_components, get_sign_bit ...(+5) |
| `tools/ltp-runner.c#output_count_line` | `arceos/ulib/axlibc/c/pow.c#pow` | 同模块上下文对齐/差异观察 | is_horizontal_space, is_ltp_status_token, is_status_token_boundary, is_summary_start, strncmp | __builtin_fma, __math_invalid, __math_oflow, __math_uflow, asdouble, asuint64 ...(+8) |
| `tools/ltp-runner.c#output_write_normalized` | `arceos/ulib/axlibc/c/string.c#twoway_strstr` | 同模块上下文对齐/差异观察 | fputc, fwrite, is_horizontal_space, is_ltp_status_token, is_status_token_boundary, output_write_legacy_result | BITOP, MAX, memchr, memcmp |


#### 1. 宏观判定
- **定性结论**: 独立开发 (相似度: 4.1%，共同 AST 指纹: 158；唯一 AST 指纹: 目标项目 1188，参考项目 2820；覆盖率: 目标项目 13.3%，参考项目 5.6%)

#### 2. 全模块覆盖矩阵解读

- **进程管理**
  1. 双方都存在该模块。
  2. 存在 3 个共同 AST 指纹。
  3. 目标覆盖率 4.48%，参考覆盖率 2.48%，均处于极低水平。
  4. 证据强度：**弱**。重合点集中于 POSIX 强制语义（如 `fork`, `exit`, `getpid`）和 Rust 语言惯用法（如 `Some`, `Ok`），未发现高熵、领域特定的 AST 子图重合。

- **任务调度/异步运行时**
  1. 双方都存在该模块。
  2. 存在 2 个共同 AST 指纹。
  3. 目标覆盖率 0.87%，参考覆盖率 0.67%，几乎可忽略不计。
  4. 证据强度：**未形成**。极低的覆盖率表明双方在调度器实现上采用了完全不同的架构和代码结构。

- **内存管理**
  1. 双方都存在该模块。
  2. 存在 84 个共同 AST 指纹，是重合数量最高的模块。
  3. 目标覆盖率 30.88%，参考覆盖率 21.99%，存在一定结构重合。
  4. 证据强度：**中**。重合主要源于共享依赖（如 `axtask`, `axhal` 的内存管理原语）、Rust 标准库的 `Arc`, `Vec` 等通用容器，以及 Linux ABI 对 `mmap` 等系统调用的参数结构约束。需在核心调用流中进一步剖析其架构差异。

- **中断/异常处理**
  1. 双方都存在该模块。
  2. 共同 AST 指纹为 0。
  3. 目标覆盖率 0.0%，参考覆盖率 0.0%。
  4. 证据强度：**未形成**。双方在中断/异常处理的底层实现上无任何 AST 层面的重合。

- **系统调用**
  1. 双方都存在该模块。
  2. 共同 AST 指纹为 0。
  3. 目标覆盖率 0.0%，参考覆盖率 0.0%。
  4. 证据强度：**未形成**。尽管双方都实现了 Linux 系统调用，但分发机制、参数解析和内部实现路径在 AST 层面完全不同。

- **文件系统**
  1. 双方都存在该模块。
  2. 存在 49 个共同 AST 指纹。
  3. 目标覆盖率 11.56%，参考覆盖率 7.34%。
  4. 证据强度：**弱**。重合主要源于 procfs、devfs 等伪文件系统的标准命名（如 `"proc"`, `"fb"`）和通用 VFS 操作（如 `ioctl`, `get`）。目标项目使用 `SimpleFs`/`NodeOpsMux` 体系，参考项目使用 `DynamicFs`/`DynamicDir` 体系，核心抽象层存在本质差异。

- **设备驱动**
  1. 仅参考项目存在该模块，目标项目无。
  2. 不存在共同 AST 指纹。
  3. 目标覆盖率 0.0%，参考覆盖率 0.0%。
  4. 证据强度：**未形成**。这是双方项目功能集和架构设计的显著差异点。

- **网络协议栈**
  1. 仅参考项目存在该模块，目标项目无。
  2. 不存在共同 AST 指纹。
  3. 目标覆盖率 0.0%，参考覆盖率 0.0%。
  4. 证据强度：**未形成**。这是双方项目功能集和架构设计的显著差异点。

- **第三方库/公共依赖**
  1. 仅参考项目存在该模块（如 `lwext4_rust`, `smoltcp`），目标项目无。
  2. 不存在共同 AST 指纹。
  3. 目标覆盖率 0.0%，参考覆盖率 0.0%。
  4. 证据强度：**未形成**。参考项目集成了大量第三方库，而目标项目未包含，这进一步证明了两者代码库构成的独立性。

#### 3. 底层 AST 关键证据指纹表

| 所属板块 | 目标项目路径 | 参考项目路径 | 证据类型 | 技术说明 |
| :--- | :--- | :--- | :--- | :--- |
| 进程管理 | kernel/src/task/mod.rs#is_clone_child | core/src/process.rs#is_clone_child | 弱证据 | 双方均匹配 `Some` 变体，属于 Rust 语言对 `Option` 类型处理的惯用法，不构成特异性复用。 |
| 进程管理 | kernel/src/syscall/task/wait.rs#apply | api/src/imp/task/wait.rs#apply | 弱证据 | 双方均访问子进程的 PID 和 PGID，这是 `wait` 系统调用族的标准 POSIX 语义要求，且访问路径风格不同（属性 vs getter 方法）。 |
| 进程管理 | kernel/src/syscall/signal.rs#sys_rt_sigprocmask | api/src/imp/task/signal.rs#sys_rt_sigprocmask | 弱证据 | 均包含 `check_sigset_size` 和 `nullable` 检查，这是 Linux `rt_sigprocmask` 系统调用的标准前置校验和用户态指针处理模式。 |
| 进程管理 | kernel/src/pseudofs/proc.rs#new_procfs | api/src/core/fs/imp/proc.rs#new_procfs | 弱证据 | 均使用字面量 `"proc"`，这是 Linux 对 procfs 挂载点的强制命名规范，但底层文件系统构造器不同（`SimpleFs` vs `DynamicFs`）。 |
| 进程管理 | kernel/src/task/ops.rs#get_process_group | process/src/process_group.rs#get_process_group | 弱证据 | 均访问 `PROCESS_GROUP_TABLE` 并执行 `get` 操作，这是进程组管理的通用数据结构设计，但并发控制原语不同（`read` vs `lock`）。 |
| 内存管理 | kernel/src/syscall/mm/mmap.rs#sys_mmap | api/src/imp/mm/mmap.rs#sys_mmap | 中证据 | 均包含 `MmapFlags::from_bits_truncate` 和 `MmapProt::from_bits_truncate`，这是 Linux `mmap` 系统调用对标志位和权限位解析的标准模式。 |
| 内存管理 | kernel/src/mm/loader.rs#load_user_app | core/src/mm.rs#load_user_app | 中证据 | 均包含 ELF 加载逻辑，但目标项目使用 `ELF_LOADER.lock` 和 `Backend::new_alloc`，参考项目使用 `ElfFile::new` 和 `CStr` 解析，加载器实现路径不同。 |
| 文件系统 | kernel/src/pseudofs/dev/fb.rs#ioctl | api/src/core/fs/imp/dev/framebuffer.rs#ioctl | 弱证据 | 均处理 framebuffer 的 `ioctl` 命令，这是 Linux 帧缓冲设备驱动的标准接口，但用户态数据交互方式不同（`vm_write` vs `get_as_mut_ref`）。 |
| 文件系统 | kernel/src/file/fs.rs#get | api/src/core/file/fd.rs#get | 弱证据 | 均实现通过 fd 获取文件对象的 `get` 操作，这是 VFS 层的通用功能，但内部并发模型和错误处理路径不同。 |
| 通用模块/其他 | tools/ltp-runner.c#exists | arceos/ulib/axlibc/c/log.c#top16 | 弱证据 | 双方均调用 `access`/`asuint64` 等 C 标准库或底层工具函数，属于不同功能模块间的巧合性低熵重合。 |

#### 4. 核心代码调用流对比剖析

* **【进程管理/进程退出路径】**
* **底层技术析理**: 双方均实现了 Linux 的 `exit` 系统调用，并遵循“清理 `clear_child_tid` -> 通知父进程 -> 让出 CPU”的通用语义。然而，在底层抽象上存在显著差异。目标项目使用 `clear_child_tid.vm_write(0)` 直接操作用户态内存，而参考项目则通过 `UserPtr::<Pid>` 抽象进行地址转换和访问。目标项目构造信号使用 `SignalInfo::new_kernel`，参考项目使用 `SignalInfo::new`。这表明双方在用户态内存访问和内核对象构造上采用了不同的抽象层和 API 设计。
* **代码溯源链接**:
  - 目标项目: `kernel/src/task/ops.rs#do_exit`
  - 参考项目: `api/src/imp/task/exit.rs#sys_exit_impl`

* **【内存管理/内存映射】**
* **底层技术析理**: 双方均实现了 `mmap` 系统调用，并包含对 `flags` 和 `prot` 参数的位解析。然而，后端内存管理模型截然不同。目标项目通过 `Backend::new_alloc`, `Backend::new_file` 等工厂方法创建不同的后端对象，体现了策略模式。参考项目则通过 `PhysAddr::from` 直接操作物理地址。这反映了双方在内存管理上“后端抽象层”与“物理地址直接操作”的架构差异。
* **代码溯源链接**:
  - 目标项目: `kernel/src/syscall/mm/mmap.rs#sys_mmap`
  - 参考项目: `api/src/imp/mm/mmap.rs#sys_mmap`

* **【文件系统/伪文件系统构建】**
* **底层技术析理**: 双方都构建了 `procfs` 伪文件系统。目标项目使用 `SimpleFs::new_with` 和 `SimpleDir`/`SimpleFile` 体系，通过 `NodeOpsMux` 枚举来统一文件和目录操作。参考项目则使用 `DynamicFs::new_with` 和 `DynamicDir::builder` 体系，通过闭包和动态分发来构建目录树。这两种 VFS 架构在对象模型和扩展机制上存在本质区别，前者是静态分发，后者是动态构建。
* **代码溯源链接**:
  - 目标项目: `kernel/src/pseudofs/proc.rs#new_procfs`
  - 参考项目: `api/src/core/fs/imp/proc.rs#new_procfs`

#### 5. 单方模块与降权项

- **单方模块**：设备驱动、网络协议栈、文件系统/第三方库、网络协议栈/第三方库仅在参考项目中存在。这构成了双方项目在功能范围和代码库组成上的巨大差异，是独立开发的有力证据。
- **降权项**：
  - **跨板块同名函数**：如目标项目 `kernel/src/task/ops.rs#do_exit` 与参考项目 `arceos/examples/shell/src/cmd.rs#do_exit`，后者是用户态示例代码，与内核实现无任何可比性，已降权为弱信号。
  - **通用语言惯用法**：`Some`, `Ok`, `Err`, `self.xxx.load()`, `Arc::new`, `vec!` 等 Rust 标准库及并发原语，不具备特异性，不能作为同源证据。
  - **共享依赖调用**：`axtask::yield_now`, `axhal::arch::flush_tlb` 等来自双方共同依赖的底层 crate，属于公共 API 调用，不构成代码复用。
  - **POSIX/Linux 强制语义**：`sys_getpid`, `sys_fork`, `sys_exit` 等系统调用名称、参数和基本行为由规范强制定义，其函数签名和核心步骤的相似性是领域必然性。

#### 6. 保守结论

综合评估，目标项目与参考项目的整体 AST 相似度仅为 4.1%，远低于可认定为“同源”或“复用”的阈值。尽管在内存管理等模块存在一定结构重合，但这主要源于共同的底层依赖、Rust 语言惯用法以及 Linux ABI 的规范性约束。在进程管理、文件系统等核心板块，双方采用了截然不同的架构设计（如 `SimpleFs` vs `DynamicFs`，线程中心模型 vs 进程中心模型）。此外，参考项目包含大量目标项目所没有的模块（如设备驱动、网络协议栈、第三方库），进一步证明了两者代码库的独立性。因此，现有 AST 证据不足以支持代码复用或抄袭的指控，应认定为独立开发。

------------------------------

