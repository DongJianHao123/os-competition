# 🛡️ OS 内核赛自动化代码级架构审计报告

**审计时间**: `2026-07-02 05:19:25`
**参赛作品名称**: `2026-T2026104869910383-XX`
**年份**: `2026`
**学校**: `XX`
**队伍名称**: `T2026104869910383`
**比赛名字**: `OS-Competition`
**比赛子赛道**: `Kernel-Implementation`
**仓库地址**: `https://gitlab.eduxiji.net/educg-group-43501-3132633/T2026104869910383-3620`
**推测开发语言**: `Rust / C (由AST探针自动识别)`
**特征函数量**: `1349`

## ⚔️ 底层代码同源性对比 (AST Homology)
> 本部分由专门的代码审查智能体，对该项目与符合年份条件的历届最高相似度项目进行控制流与底层调用对比。新版报告会先给出确定性的全模块覆盖矩阵，再给出 Agent 的分模块解释，避免只围绕少量抽样函数展开。

### 🏆 Top 3 底层对标: vs `2025-狗剩-海南大学`（海南大学 / 狗剩）

#### 对方作品信息

**对方年份**: `2025`
**对方学校**: `海南大学`
**对方队伍名称**: `狗剩`
**对方比赛名字**: `OS-Competition`
**对方比赛子赛道**: `Kernel-Implementation`
**对方仓库地址**: `https://gitlab.eduxiji.net/educg-group-36002-2710490/T202510589995148-2209`

> 双向覆盖率：共同 AST 指纹: 171；唯一 AST 指纹: 目标项目 1188，参考项目 3020；覆盖率: 目标项目 14.39%，参考项目 5.66%

## 全模块对比总览（确定性证据）

> 总体覆盖率：共同 AST 指纹: 171；唯一 AST 指纹: 目标项目 1188，参考项目 3020；覆盖率: 目标项目 14.39%，参考项目 5.66%

### 1. 模块覆盖矩阵
| 模块 | 目标函数数 | 参考函数数 | 共同唯一 AST 指纹 | Target 覆盖 | Base 覆盖 | 状态 |
| :--- | ---: | ---: | ---: | ---: | ---: | :--- |
| 进程管理 | 71 | 93 | 4 | 5.97% | 4.76% | 双方存在且有结构重合 |
| 任务调度/异步运行时 | 245 | 399 | 2 | 0.87% | 0.69% | 双方存在且有结构重合 |
| 内存管理 | 308 | 326 | 8 | 2.94% | 2.75% | 双方存在且有结构重合 |
| 中断/异常处理 | 5 | 62 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |
| 系统调用 | 181 | 153 | 1 | 0.59% | 0.78% | 双方存在且有结构重合 |
| 文件系统 | 496 | 747 | 48 | 11.32% | 8.48% | 双方存在且有结构重合 |
| 设备驱动 | 0 | 142 | 0 | 0.0% | 0.0% | 仅参考项目体现 |
| 网络协议栈 | 0 | 77 | 0 | 0.0% | 0.0% | 仅参考项目体现 |
| 文件系统/第三方库 | 0 | 689 | 0 | 0.0% | 0.0% | 仅参考项目体现 |
| 网络协议栈/第三方库 | 0 | 107 | 0 | 0.0% | 0.0% | 仅参考项目体现 |
| 第三方库 | 0 | 386 | 0 | 0.0% | 0.0% | 仅参考项目体现 |
| 通用模块/其他 | 43 | 665 | 1 | 2.94% | 0.2% | 双方存在且有结构重合 |

### 2. 分模块证据清单

#### 进程管理
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `kernel/src/task/ops.rs#do_exit` | `api/src/imp/task.rs#do_exit` | 同板块同名 | FutexKey::new_current, SignalInfo::new_kernel, Some, axtask::yield_now, clear_child_tid.is_null, clear_child_tid.vm_write ...(+33) | Layout::new::<Tid>, SignalInfo::new, UserPtr::<Tid>::from, axtask::exit, axtask::yield_now, clear_child_tid.get_as ...(+18) |
| `kernel/src/syscall/signal.rs#sys_rt_sigprocmask` | `api/src/imp/signal.rs#sys_rt_sigprocmask` | 同板块同名 | Err, Ok, check_sigset_size, curr.as_thread, current, debug! ...(+7) | Err, Ok, check_sigset, current, current()
        .task_ext, current()
        .task_ext()
        .thread
        .signal
        .with_blocked_mut::<LinuxResult<_>> ...(+2) |
| `kernel/src/syscall/ipc/shm.rs#clear_proc_shm` | `api/src/imp/ipc/shm.rs#clear_proc_shm` | 同板块同名 | self.get_inner_by_shmid, self.get_shmids_by_pid, self.remove_pid, self.remove_shmid, shm_inner.attach_count, shm_inner.detach_process ...(+1) | SHM_MANAGER.lock, shm_inner.detach_process, shm_inner.lock, shm_manager.get_inner_by_shmid, shm_manager.get_inner_by_shmid(shmid).unwrap, shm_manager.get_shmids_by_pid ...(+1) |
| `kernel/src/syscall/ipc/shm.rs#attach_process` | `api/src/imp/ipc/shm.rs#attach_process` | 同板块同名 | assert!, monotonic_time_nanos, self.va_range.insert | assert!, self.va_range.insert |

#### 任务调度/异步运行时
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `kernel/src/syscall/sync/futex.rs#sys_futex` | `api/src/imp/futex.rs#sys_futex` | 同板块同名 | AxError::from, Err, FutexKey::new_current, Ok, Some, assert_unsigned ...(+22) | Err, Ok, TimeValue::new, axtask::yield_now, futex_table.get, futex_table.get_or_insert ...(+14) |
| `kernel/src/syscall/signal.rs#sys_rt_sigsuspend` | `api/src/imp/signal.rs#sys_rt_sigsuspend` | 同板块同名 | LinuxError::EINTR.code, Ok, Poll::Ready, Some, block_on, check_signals ...(+14) | LinuxError::EINTR.code, Ok, Some, check_sig, check_sigset, curr.task_ext ...(+8) |
| `kernel/src/syscall/signal.rs#sys_kill` | `api/src/imp/signal.rs#sys_kill` | 同板块同名 | Ok, Some, current, current().as_thread, current().as_thread().proc_data.proc.group, current().as_thread().proc_data.proc.group().pgid ...(+9) | Err, Ok, current, find_process, get_all_processes, make_siginfo ...(+3) |
| `kernel/src/syscall/ipc/shm.rs#sys_shmdt` | `api/src/imp/ipc/shm.rs#sys_shmdt` | 同板块同名 | Ok, SHM_MANAGER.lock, VirtAddr::from, aspace.unmap, curr.as_thread, current ...(+15) | Ok, SHM_MANAGER.lock, VirtAddr::from, aspace.unmap, axhal::arch::flush_tlb, curr.task_ext ...(+17) |

#### 内存管理
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `kernel/src/syscall/mm/mmap.rs#sys_mmap` | `src/syscall_imp/mm/mmap.rs#sys_mmap` | 同板块同名 | (addr + length).align_up, Arc::new, Backend::new_alloc, Backend::new_cow, Backend::new_file, Backend::new_linear ...(+56) | (addr + length).align_up, Err, MmapFlags::MAP_PRIVATE.bits, MmapFlags::MAP_SHARED.bits, MmapFlags::MAP_SHARED_VALIDATE.bits, MmapFlags::from_bits_truncate ...(+32) |
| `kernel/src/mm/loader.rs#load_user_app` | `core/src/mm.rs#load_user_app` | 同板块同名 | (ustack_top - user_sp_aligned).align_up_4k, Backend::new_alloc, ELF_LOADER.lock, ELF_LOADER.lock().load, Err, Ok ...(+48) | CStr::from_bytes_with_nul, CStr::from_bytes_with_nul(interp)
                    .ok, CStr::from_bytes_with_nul(interp)
                    .ok()
                    .and_then, CStr::from_bytes_with_nul(interp)
                    .ok()
                    .and_then(|it| it.to_str().ok())
                    .ok_or, ElfFile::new, ElfFile::new(&file_data).map_err ...(+53) |
| `kernel/src/syscall/ipc/shm.rs#sys_shmat` | `api/src/imp/ipc/shm.rs#sys_shmat` | 同板块同名 | Arc::new, Backend::new_shared, Ok, SHM_MANAGER.lock, SharedPages::new, ShmAtFlags::from_bits_truncate ...(+32) | Err, MappingFlags::all, Ok, SHM_MANAGER.lock, ShmAtFlags::from_bits_truncate, VirtAddr::from ...(+26) |
| `kernel/src/mm/aspace/mod.rs#handle_page_fault` | `arceos/modules/axmm/src/aspace.rs#handle_page_fault` | 同板块同名 | VirtAddrRange::from_start_size, area.backend, area.backend().page_size, area.backend().populate, area.flags, axtask::yield_now ...(+8) | Self::handle_cow_fault, access_flags.contains, area
                    .backend, area
                    .backend()
                    .handle_page_fault, area.flags, debug! ...(+5) |

#### 中断/异常处理
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `kernel/src/pseudofs/dev/tty/terminal/termios.rs#default` | `arceos/ulib/axlibc/c/pow.c#exp_inline` | 同模块上下文对齐/差异观察 | ctl | __math_oflow, __math_uflow, asdouble, asuint64, converttoint, eval_as_double ...(+4) |
| `kernel/src/file/signalfd.rs#from_signal_info` | `arceos/modules/axhal/src/arch/aarch64/trap.rs#handle_sync_exception` | 同模块上下文对齐/差异观察 | sig_info.code, sig_info.errno, sig_info.signo | ESR_EL1.extract, crate::trap::handle_syscall, crate::trap::post_trap_callback, debug!, esr.read, esr.read_as_enum ...(+6) |
| `kernel/src/syscall/signal.rs#is_fatal_signal` | `arceos/modules/axhal/src/arch/x86_64/idt.rs#new` | 同模块上下文对齐/差异观察 | matches! | InterruptDescriptorTable::new, core::mem::transmute, core::slice::from_raw_parts_mut, entries[i].set_handler_fn, opt.set_privilege_level |
| `tools/ltp-runner.c#drain_child_output` | `arceos/modules/axhal/src/platform/riscv64_qemu_virt/irq.rs#register_handler` | 同模块上下文对齐/差异观察 | output_count_feed, read | with_cause! |

#### 系统调用
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `kernel/src/syscall/ipc/sem.rs#sys_semctl` | `api/src/imp/ipc/sem.rs#sys_semctl` | 同板块同名 | Err, Ok, SEM_STATE.lock, SEM_STATE.lock().remove, drop, has_ipc_permission ...(+28) | Err, Ok, SEM_MANAGER.lock, UserPtr::<CTypeSemidDs>::from, UserPtr::<CTypeSemidDs>::from(arg).get, UserPtr::<u16>::from ...(+14) |
| `kernel/src/syscall/ipc/shm.rs#sys_shmctl` | `api/src/imp/ipc/shm.rs#sys_shmctl` | 同板块同名 | Err, Ok, SHM_MANAGER.lock, buf.get_as_mut, crate::syscall::sys::sys_getuid, monotonic_time_nanos ...(+4) | Err, Ok, SHM_MANAGER.lock, shm_inner.lock, shm_manager
            .get_inner_by_shmid, shm_manager
            .get_inner_by_shmid(shmid)
            .ok_or |
| `kernel/src/syscall/ipc/sem.rs#sys_semget` | `api/src/imp/ipc/sem.rs#sys_semget` | 同板块同名 | Err, NEXT_SEM_ID.fetch_add, Ok, SEM_STATE.lock, has_ipc_permission, new_sem_set ...(+5) | Arc::new, Err, Mutex::new, Ok, SEM_MANAGER.lock, SemSet::new ...(+7) |
| `kernel/src/syscall/io_mpx/epoll.rs#sys_epoll_ctl` | `api/src/imp/io_mpx/epoll.rs#sys_epoll_ctl` | 同板块同名 | Epoll::from_fd, EpollFlags::from_bits, EpollFlags::from_bits(event.events & !events.bits()).ok_or, Err, IoEvents::from_bits_truncate, Ok ...(+7) | EpollInstance::from_fd, EpollInstance::from_fd(epfd)?.control, Err, Ok, debug!, event.get_as_mut |

#### 文件系统
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `kernel/src/syscall/fs/fd_ops.rs#sys_fcntl` | `api/src/imp/fd/fd_ops.rs#sys_fcntl` | 同板块同名 | FD_TABLE
                .read, FD_TABLE
                .read()
                .get, FD_TABLE
                .read()
                .get(fd as _)
                .ok_or, FD_TABLE.write, FD_TABLE.write().set_cloexec, NodePermission::from_bits_truncate ...(+16) | Ok, debug!, dup_fd, get_file_like, get_file_like(fd)?.set_nonblocking, warn! |
| `kernel/src/syscall/fs/io.rs#sys_splice` | `api/src/imp/io.rs#sys_splice` | 同板块同名 | DummyFd::from_fd, DummyFd::from_fd(fd_in).is_ok, DummyFd::from_fd(fd_out).is_ok, Err, File::from_fd, Pipe::from_fd ...(+20) | (len - total_copied).min, Err, Ok, SeekFrom::Current, SeekFrom::Start, buf.len ...(+43) |
| `kernel/src/syscall/fs/ctl.rs#write_entry` | `api/src/imp/fd/ctl.rs#write_entry` | 同板块同名 | align_of::<linux_dirent64>, core::mem::MaybeUninit::zeroed, dirent.as_mut_ptr, entry_ptr.add, entry_ptr.add(NAME_OFFSET + name.len()).write, entry_ptr.add(NAME_OFFSET).copy_from_nonoverlapping ...(+13) | Default::default, align_of::<linux_dirent64>, entry_ptr.add, entry_ptr.cast::<linux_dirent64>, entry_ptr.cast::<linux_dirent64>().write, len.next_multiple_of ...(+9) |
| `kernel/src/file/fs.rs#get` | `core/src/vfs/tmp.rs#get` | 同板块同名 | (self.seq.load(Ordering::Acquire) == seq).then_some, Duration::new, axfs_ng_vfs::DeviceId, self.atime_nsec.load, self.atime_sec.load, self.blksize.load ...(+15) | self.inodes.lock, self.inodes.lock()[ino as usize - 1].clone |

#### 设备驱动
仅参考项目中观察到该模块代表函数，目标项目没有同模块可对齐证据：
- 参考项目：`arceos/tools/raspi4/chainloader/src/bsp/device_driver/bcm/bcm2xxx_pl011_uart.rs#init`，调用摘要：FBRD::BAUD_DIVFRAC.val, IBRD::BAUD_DIVINT.val, self.flush, self.registers
            .CR
            .write, self.registers
            .LCR_H
            .write, self.registers.CR.set ...(+3)
- 参考项目：`arceos/tools/bwbench_client/src/device.rs#new`，调用摘要：ETH_P_ALL.to_be, Err, Ok, dev.bind_interface, dev.interface_mtu, ifreq.ioctl ...(+5)
- 参考项目：`arceos/tools/bwbench_client/src/main.rs#transmit_benchmark`，调用摘要：Local::now, NetDevice::new, NetDevice::new(interface).unwrap, current_time.signed_duration_since, current_time.signed_duration_since(past_time).num_seconds, dev.send ...(+2)
- 参考项目：`arceos/tools/bwbench_client/src/device.rs#bind_interface`，调用摘要：ETH_P_ALL.to_be, Err, Ok, libc::bind, self.ifreq.ioctl, std::io::Error::last_os_error ...(+2)

#### 网络协议栈
仅参考项目中观察到该模块代表函数，目标项目没有同模块可对齐证据：
- 参考项目：`arceos/ulib/axlibc/c/network.c#inet_pton`，调用摘要：hexval, inet_pton, isdigit, memmove
- 参考项目：`api/src/fd/net.rs#sendto`，调用摘要：Err, IpAddr::V4, Ipv4Addr::new, Ok, SocketAddr::new, socket
                    .bind ...(+3)
- 参考项目：`api/src/fd/net.rs#shutdown`，调用摘要：Ok, tcpsocket.lock, tcpsocket.peer_addr, tcpsocket.shutdown, udpsocket.lock, udpsocket.peer_addr ...(+1)
- 参考项目：`arceos/api/arceos_posix_api/src/imp/net.rs#shutdown`，调用摘要：Ok, tcpsocket.lock, tcpsocket.peer_addr, tcpsocket.shutdown, udpsocket.lock, udpsocket.peer_addr ...(+1)

#### 文件系统/第三方库
仅参考项目中观察到该模块代表函数，目标项目没有同模块可对齐证据：
- 参考项目：`crates/lwext4_rust/c/lwext4/src/ext4_balloc.c#ext4_balloc_alloc_block`，调用摘要：ext4_balloc_get_bgid_of_block, ext4_balloc_get_block_of_bgid, ext4_balloc_set_bitmap_csum, ext4_balloc_verify_bitmap_csum, ext4_bg_get_block_bitmap, ext4_bg_get_free_blocks_count ...(+19)
- 参考项目：`crates/lwext4_rust/c/lwext4/src/ext4_journal.c#jbd_journal_prepare`，调用摘要：EXT4_BLOCK_ZERO, RB_FIND, TAILQ_FOREACH_REVERSE_SAFE, TAILQ_FOREACH_SAFE, TAILQ_REMOVE, ext4_bcache_clear_dirty ...(+17)
- 参考项目：`crates/lwext4_rust/c/lwext4/src/ext4.c#ext4_dir_rm`，调用摘要：EXT4_MP_LOCK, EXT4_MP_UNLOCK, ext4_block_cache_write_back, ext4_dir_en_get_inode, ext4_dir_iterator_fini, ext4_dir_iterator_init ...(+19)
- 参考项目：`crates/lwext4_rust/c/lwext4/src/ext4_dir_idx.c#ext4_dir_dx_split_index`，调用摘要：ext4_block_set, ext4_dir_dx_climit_get_count, ext4_dir_dx_climit_get_limit, ext4_dir_dx_climit_set_count, ext4_dir_dx_climit_set_limit, ext4_dir_dx_entry_get_hash ...(+10)

#### 网络协议栈/第三方库
仅参考项目中观察到该模块代表函数，目标项目没有同模块可对齐证据：
- 参考项目：`arceos/modules/axnet/src/smoltcp_impl/tcp.rs#connect`，调用摘要：Err, Ok, SOCKET_SET
                .with_socket_mut::<tcp::Socket, _, _>, SOCKET_SET.add, SocketSetWrapper::new_tcp_socket, Some ...(+29)
- 参考项目：`arceos/modules/axnet/src/smoltcp_impl/dns.rs#query`，调用摘要：Err, Ok, SOCKET_SET
            .with_socket_mut::<dns::Socket, _, _>, SOCKET_SET
            .with_socket_mut::<dns::Socket, _, _>(handle, |socket| {
                socket.start_query(iface.lock().context(), name, query_type)
            })
            .map_err, SOCKET_SET.poll_interfaces, SOCKET_SET.with_socket_mut::<dns::Socket, _, _> ...(+12)
- 参考项目：`arceos/modules/axnet/src/smoltcp_impl/bench.rs#bench_transmit_bandwidth`，调用摘要：(current_time - past_time).secs, AxNetTxToken::consume, InterfaceWrapper::current_time, info!, self.transmit, tx_buf[0..12].fill ...(+2)
- 参考项目：`arceos/modules/axnet/src/smoltcp_impl/tcp.rs#shutdown`，调用摘要：LISTEN_TABLE.unlisten, Ok, SOCKET_SET.poll_interfaces, SOCKET_SET.with_socket_mut::<tcp::Socket, _, _>, debug!, self.handle.get ...(+9)

#### 第三方库
仅参考项目中观察到该模块代表函数，目标项目没有同模块可对齐证据：
- 参考项目：`crates/visionfive2-sd/src/lib.rs#send_cmd`，调用摘要：RawInterrupt::from, RawInterruptStatusReg::from, S::sleep_ms_until, Some, arg.into, assert! ...(+24)
- 参考项目：`crates/visionfive2-sd/src/lib.rs#init_sdcard`，调用摘要：CardTypeReg::from, CardTypeReg::from(0).with_card_width4_1, CmdArg::new, CmdReg::from, ControlReg::from, RawInterruptStatusReg::from ...(+20)
- 参考项目：`crates/page_table_multiarch/page_table_multiarch/src/bits64.rs#map_region`，调用摘要：Err, M::flush_tlb, Ok, PageSize::Size1G.is_aligned, PageSize::Size2M.is_aligned, PageSize::Size4K.is_aligned ...(+12)
- 参考项目：`crates/axdriver_crates/axdriver_virtio/src/net.rs#try_new`，调用摘要：Err, InnerDev::new, InnerDev::new(transport).map_err, NetBufPool::new, Ok, Some ...(+14)

#### 通用模块/其他
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `tools/ltp-runner.c#exists` | `arceos/ulib/axlibc/c/log.c#top16` | 同板块同结构 | access | asuint64 |
| `tools/ltp-runner.c#output_write_legacy_result` | `arceos/ulib/axlibc/c/printf.c#print_exponential_number` | 同模块上下文对齐/差异观察 | fputc, fputs, fwrite, is_horizontal_space, is_ltp_status_token | ABS_FOR_PRINTING, PRINTF_ABS, bastardized_floor, get_components, get_normalized_components, get_sign_bit ...(+5) |
| `tools/ltp-runner.c#output_count_line` | `arceos/ulib/axlibc/c/pow.c#pow` | 同模块上下文对齐/差异观察 | is_horizontal_space, is_ltp_status_token, is_status_token_boundary, is_summary_start, strncmp | __builtin_fma, __math_invalid, __math_oflow, __math_uflow, asdouble, asuint64 ...(+8) |
| `tools/ltp-runner.c#output_write_normalized` | `arceos/ulib/axlibc/c/string.c#twoway_strstr` | 同模块上下文对齐/差异观察 | fputc, fwrite, is_horizontal_space, is_ltp_status_token, is_status_token_boundary, output_write_legacy_result | BITOP, MAX, memchr, memcmp |


#### 1. 宏观判定
- **定性结论**: 独立开发 / 底层架构显著差异 (相似度: 4.24%，共同 AST 指纹: 171；唯一 AST 指纹: 目标项目 1188，参考项目 3020；覆盖率: 目标项目 14.39%，参考项目 5.66%)

#### 2. 全模块覆盖矩阵解读
- **进程管理**: 双方均存在。共同 AST 指纹 4 个，目标覆盖率 5.97%，参考覆盖率 4.76%。重合点集中在 `do_exit`、`sys_rt_sigprocmask` 等 POSIX 强制接口及 `clear_child_tid` 原子访问器。证据强度：**弱**。
- **任务调度/异步运行时**: 双方均存在。共同 AST 指纹 2 个，目标覆盖率 0.87%，参考覆盖率 0.69%。重合点仅为 `sys_futex`、`sys_rt_sigsuspend` 等系统调用入口。证据强度：**弱**。
- **内存管理**: 双方均存在。共同 AST 指纹 8 个，目标覆盖率 2.94%，参考覆盖率 2.75%。重合点集中在 `sys_mmap`、`load_user_app`、`handle_page_fault` 等核心路径，但外部调用对象（如 `Backend::new_alloc` vs `ElfFile::new`）差异显著。证据强度：**中**。
- **中断/异常处理**: 双方均存在，但共同 AST 指纹为 0。目标项目仅 5 个函数，参考项目 62 个函数，规模与实现路径完全不同。证据强度：**未形成**。
- **系统调用**: 双方均存在。共同 AST 指纹 1 个，目标覆盖率 0.59%，参考覆盖率 0.78%。重合点仅为 `sys_semctl`、`sys_shmctl` 等 IPC 系统调用入口。证据强度：**弱**。
- **文件系统**: 双方均存在。共同 AST 指纹 48 个，目标覆盖率 11.32%，参考覆盖率 8.48%。重合点集中在 `sys_fcntl`、`sys_splice`、`write_entry` 等 VFS 操作，但底层文件系统框架（`SimpleFs` vs `DynamicFs`）不同。证据强度：**中**。
- **设备驱动**: 仅参考项目存在（142 个函数），目标项目无此模块。证据强度：**未形成**。
- **网络协议栈**: 仅参考项目存在（77 个函数），目标项目无此模块。证据强度：**未形成**。
- **第三方库/公共依赖**: 参考项目包含大量第三方库（文件系统/第三方库 689 个函数，网络协议栈/第三方库 107 个函数，第三方库 386 个函数），目标项目无对应模块。证据强度：**未形成**。

#### 3. 底层 AST 关键证据指纹表
| 所属板块 | 目标项目路径 | 参考项目路径 | 证据类型 | 技术说明 |
| :--- | :--- | :--- | :--- | :--- |
| 进程管理 | `kernel/src/task/mod.rs#set_clear_child_tid` | `core/src/task/thread.rs#set_clear_child_tid` | 同板块同名 | 均为 `self.clear_child_tid.store`，属于 Linux ABI 强制字段的原子访问器，无创作表达。 |
| 进程管理 | `kernel/src/task/mod.rs#clear_child_tid` | `core/src/task/thread.rs#clear_child_tid` | 同板块同名 | 均为 `self.clear_child_tid.load`，同上。 |
| 进程管理 | `kernel/src/file/pidfd.rs#process` | `core/src/task/thread.rs#process` | 跨层同名 | 目标为 pidfd 层 getter，参考为线程层 getter，均返回内部字段，表现为“内部闭环逻辑”。 |
| 进程管理 | `kernel/src/syscall/ipc/shm.rs#attach_process` | `api/src/imp/ipc/shm.rs#attach_process` | 同板块同名 | 均含 `assert!, self.va_range.insert`，属于共享内存映射的必然操作序列。 |
| 进程管理 | `kernel/src/syscall/ipc/shm.rs#detach_process` | `api/src/imp/ipc/shm.rs#detach_process` | 同板块同名 | 均含 `assert!, self.va_range.remove`，同上。 |
| 内存管理 | `kernel/src/syscall/mm/mmap.rs#sys_mmap` | `src/syscall_imp/mm/mmap.rs#sys_mmap` | 同板块同名 | 均处理 `addr + length` 对齐及 `MmapFlags` 解析，但后端对象（`Backend::new_*` vs `ElfFile::new`）不同。 |
| 内存管理 | `kernel/src/mm/loader.rs#load_user_app` | `core/src/mm.rs#load_user_app` | 同板块同名 | 均加载 ELF 文件，但目标使用 `ELF_LOADER.lock`，参考使用 `ElfFile::new`，解析路径不同。 |
| 内存管理 | `kernel/src/mm/aspace/mod.rs#handle_page_fault` | `arceos/modules/axmm/src/aspace.rs#handle_page_fault` | 同板块同名 | 均处理缺页，但目标调用 `area.backend().populate`，参考调用 `Self::handle_cow_fault`，策略不同。 |
| 文件系统 | `kernel/src/syscall/fs/fd_ops.rs#sys_fcntl` | `api/src/imp/fd/fd_ops.rs#sys_fcntl` | 同板块同名 | 均操作 `FD_TABLE`，但目标使用 `FD_TABLE.read/write`，参考使用 `get_file_like`，表结构不同。 |
| 文件系统 | `kernel/src/syscall/fs/ctl.rs#write_entry` | `api/src/imp/fd/ctl.rs#write_entry` | 同板块同名 | 均构造 `linux_dirent64`，但目标使用 `MaybeUninit::zeroed`，参考使用 `Default::default`，初始化方式不同。 |

#### 4. 核心代码调用流对比剖析
* **【进程管理/进程退出路径】**
* **底层技术析理**: 目标项目 `do_exit` 使用 `FutexKey::new_current` 处理 robust futex，并通过 `clear_child_tid.vm_write(0)` 直接向用户空间写入零值；参考项目 `do_exit` 则通过 `UserPtr::<Tid>::from` 和 `clear_child_tid.query_paddr` 获取物理地址后操作。两者在内存抽象（虚拟内存写入 vs 物理地址查询）和 futex 清理机制上存在根本性差异。
* **代码溯源链接**:
  - 目标项目: `kernel/src/task/ops.rs#do_exit`
  - 参考项目: `api/src/imp/task.rs#do_exit`

* **【内存管理/缺页处理路径】**
* **底层技术析理**: 目标项目 `handle_page_fault` 通过 `area.backend().populate` 直接填充页表，依赖后端存储的页面供给能力；参考项目则调用 `Self::handle_cow_fault` 处理写时复制，并在 `area.backend().handle_page_fault` 中实现更细粒度的页面分配。两者在缺页处理策略（直接填充 vs COW）和后端抽象层次上异构。
* **代码溯源链接**:
  - 目标项目: `kernel/src/mm/aspace/mod.rs#handle_page_fault`
  - 参考项目: `arceos/modules/axmm/src/aspace.rs#handle_page_fault`

* **【文件系统/procfs 构建路径】**
* **底层技术析理**: 目标项目 `new_procfs` 使用 `SimpleFs::new_with` 创建静态文件系统，节点通过 `SimpleDir::new_maker` 和 `NodeOpsMux::Dir` 手动挂载；参考项目使用 `DynamicFs::new_with` 创建动态文件系统，节点通过 `DynamicDir::builder` 和 `Device::new` 构建。两者在 VFS 框架选型（静态 vs 动态）和节点管理模型上完全不同。
* **代码溯源链接**:
  - 目标项目: `kernel/src/pseudofs/proc.rs#new_procfs`
  - 参考项目: `core/src/vfs/proc.rs#new_procfs`

#### 5. 单方模块与降权项
- **设备驱动、网络协议栈、第三方库/公共依赖**: 仅参考项目存在，目标项目无对应模块，不构成同源证据。
- **跨板块同名函数**: 如目标 `kernel/src/file/pidfd.rs#process` 与参考 `core/src/task/thread.rs#process`，属于不同抽象层级的同名 getter，已降权为弱信号。
- **POSIX 强制接口**: `sys_exit`、`sys_exit_group`、`sys_getpid`、`sys_getppid` 等系统调用入口，函数名与调用链（如 `Ok, do_exit`）由规范强制，不构成创作表达。
- **通用宏与标准库调用**: `assert!`、`Vec::new`、`Arc::new`、`Ok/Err` 等属于 Rust 语言通用惯用法，已从强证据中排除。

#### 6. 保守结论
双方项目在进程管理、内存管理、文件系统等核心模块存在少量同名函数与相似调用序列，但均属于 POSIX/Linux 规范强制接口、原子访问器或领域内必然操作。目标项目在内存抽象（`vm_write` vs `UserPtr`）、VFS 框架（`SimpleFs` vs `DynamicFs`）、缺页处理策略（直接填充 vs COW）等关键架构决策上与参考项目显著不同。全量 AST 相似度仅 4.24%，且参考项目独有的设备驱动、网络协议栈、第三方库等模块在目标项目中完全缺失。综合判定，现有证据无法支持“直接代码复用”指控，双方为独立开发。

------------------------------

