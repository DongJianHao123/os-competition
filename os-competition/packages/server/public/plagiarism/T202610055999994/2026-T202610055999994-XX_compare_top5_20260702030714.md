# 🛡️ OS 内核赛自动化代码级架构审计报告

**审计时间**: `2026-07-02 03:07:14`
**参赛作品名称**: `2026-T202610055999994-XX`
**年份**: `2026`
**学校**: `XX`
**队伍名称**: `T202610055999994`
**比赛名字**: `OS-Competition`
**比赛子赛道**: `Kernel-Implementation`
**仓库地址**: `https://gitlab.eduxiji.net/educg-group-43501-3132633/T202610055999994-232`
**推测开发语言**: `Rust / C (由AST探针自动识别)`
**特征函数量**: `2230`

## ⚔️ 底层代码同源性对比 (AST Homology)
> 本部分由专门的代码审查智能体，对该项目与符合年份条件的历届最高相似度项目进行控制流与底层调用对比。新版报告会先给出确定性的全模块覆盖矩阵，再给出 Agent 的分模块解释，避免只围绕少量抽样函数展开。

### 🏆 Top 5 底层对标: vs `2024-undefinedType-南京航空航天大学`（南京航空航天大学 / undefinedType）

#### 对方作品信息

**对方年份**: `2024`
**对方学校**: `南京航空航天大学`
**对方队伍名称**: `undefinedType`
**对方比赛名字**: `OS-Competition`
**对方比赛子赛道**: `Kernel-Implementation`
**对方仓库地址**: `https://gitlab.eduxiji.net/educg-group-26010-2376550/T202410287992755-2176`

> 双向覆盖率：共同 AST 指纹: 382；唯一 AST 指纹: 目标项目 1898，参考项目 398；覆盖率: 目标项目 20.13%，参考项目 95.98%

## 全模块对比总览（确定性证据）

> 总体覆盖率：共同 AST 指纹: 382；唯一 AST 指纹: 目标项目 1898，参考项目 398；覆盖率: 目标项目 20.13%，参考项目 95.98%

### 1. 模块覆盖矩阵
| 模块 | 目标函数数 | 参考函数数 | 共同唯一 AST 指纹 | Target 覆盖 | Base 覆盖 | 状态 |
| :--- | ---: | ---: | ---: | ---: | ---: | :--- |
| 进程管理 | 83 | 0 | 0 | 0.0% | 0.0% | 仅目标项目体现 |
| 任务调度/异步运行时 | 123 | 0 | 0 | 0.0% | 0.0% | 仅目标项目体现 |
| 内存管理 | 297 | 0 | 0 | 0.0% | 0.0% | 仅目标项目体现 |
| 中断/异常处理 | 43 | 0 | 0 | 0.0% | 0.0% | 仅目标项目体现 |
| 系统调用 | 79 | 0 | 0 | 0.0% | 0.0% | 仅目标项目体现 |
| 文件系统 | 376 | 0 | 0 | 0.0% | 0.0% | 仅目标项目体现 |
| 设备驱动 | 373 | 0 | 0 | 0.0% | 0.0% | 仅目标项目体现 |
| 网络协议栈 | 0 | 0 | 0 | 0.0% | 0.0% | 双方均未体现 |
| 文件系统/第三方库 | 699 | 478 | 382 | 64.09% | 95.98% | 双方存在且有结构重合 |
| 第三方库 | 1 | 0 | 0 | 0.0% | 0.0% | 仅目标项目体现 |
| 通用模块/其他 | 156 | 0 | 0 | 0.0% | 0.0% | 仅目标项目体现 |

### 2. 分模块证据清单

#### 进程管理
仅目标项目中观察到该模块代表函数，参考项目没有同模块可对齐证据：
- 目标项目：`os/src/syscall/process.rs#sys_exec`，调用摘要：Err, Ok, String::from, Vec::<String>::new, Vec::new, abs_path.as_str ...(+54)
- 目标项目：`os/src/fs/mod.rs#create_proc_dir`，调用摘要：Arc::new, StaticVirtFile::new, VirtFile::new, create_mounts_file, open, register_vfile
- 目标项目：`os/src/syscall/signal.rs#sys_sig_proc_mask`，调用摘要：Err, Ok, SignalMaskFlag::from_bits, SignalMaskFlag::from_bits(how).ok_or, Some, current_task ...(+13)
- 目标项目：`os/src/syscall/signal.rs#sys_sig_timed_wait`，调用摘要：Err, Ok, SignalFlags::from_sig, Some, current_task, current_task().ok_or ...(+20)

#### 任务调度/异步运行时
仅目标项目中观察到该模块代表函数，参考项目没有同模块可对齐证据：
- 目标项目：`os/src/fs/mod.rs#create_ltp_command_helpers`，调用摘要：write_boot_file
- 目标项目：`os/src/syscall/mod.rs#syscall`，调用摘要：Err, sys_accept, sys_bind, sys_brk, sys_chdir, sys_clock_getres ...(+106)
- 目标项目：`user/src/bin/user_shell.rs#main`，调用摘要：ProcessArguments::new, String::new, Vec::new, args_addr.as_slice, args_copy[0].as_str, assert_eq! ...(+39)
- 目标项目：`os/src/task/task.rs#load_interpreter`，调用摘要：(ph.virtual_addr() as usize + interp_offset + ph.mem_size() as usize).into, (ph.virtual_addr() as usize + interp_offset).into, Aux::new, Err, MapArea::new, Ok ...(+32)

#### 内存管理
仅目标项目中观察到该模块代表函数，参考项目没有同模块可对齐证据：
- 目标项目：`os/src/task/task.rs#exec`，调用摘要：Arc::new, Aux::new, Err, MemorySet::new, MemorySetInner::from_elf, Ok ...(+52)
- 目标项目：`patch/virtio-drivers/src/device/socket/connectionmanager.rs#send_recv`，调用摘要："Hello from guest".as_bytes, (hello_from_guest.len() as u32).into, (hello_from_host.len() as u32).into, 0.into, 50.into, Arc::new ...(+44)
- 目标项目：`os/src/utils/error.rs#str`，调用摘要：panic!
- 目标项目：`os/src/task/task.rs#clone_task`，调用摘要：Arc::downgrade, Arc::new, FdTable::from_existed_user, FsInfo::from_another, KernelStack::new, MemorySet::from_existed_user ...(+32)

#### 中断/异常处理
仅目标项目中观察到该模块代表函数，参考项目没有同模块可对齐证据：
- 目标项目：`os/src/signal/sigflags.rs#default_op`，调用摘要：continue_signals.contains, dump_signals.contains, ignore_signals.contains, stop_signals.contains, terminate_signals.contains
- 目标项目：`patch/polyhal/src/arch/x86_64/idt.rs#new`，调用摘要：InterruptDescriptorTable::new, Self, core::slice::from_raw_parts_mut, entries[i].set_handler_fn, transmute::<extern "C" fn(), extern "x86-interrupt" fn(InterruptStackFrame)>
- 目标项目：`patch/polyhal/src/utils/mutex_no_irq.rs#try_lock`，调用摘要：IRQ::int_disable, IRQ::int_enabled, self.lock
            .try_lock, self.lock
            .try_lock()
            .map, self.lock.is_locked
- 目标项目：`os/src/trap/interrupts.rs#inc_irq_count`，调用摘要：IRQ_COUNTS.lock, counts[irq].saturating_add

#### 系统调用
仅目标项目中观察到该模块代表函数，参考项目没有同模块可对齐证据：
- 目标项目：`os/src/syscall/other.rs#sys_uname`，调用摘要：Ok, UserBuffer::new, buf[..len].copy_from_slice, bytes.len, bytes.len().min, copy_str_to_buf ...(+8)
- 目标项目：`os/src/syscall/other.rs#sys_clock_nanosleep`，调用摘要：Err, Ok, current_user_token, get_data, get_time_ms, get_time_ms()
            .checked_add ...(+6)
- 目标项目：`os/src/syscall/other.rs#sys_getrandom`，调用摘要：Err, Ok, UserBuffer::new, Vec::new, all_bytes.extend_from_slice, buf_ptr.is_null ...(+7)
- 目标项目：`os/src/syscall/other.rs#sys_nanosleep`，调用摘要：Err, Ok, current_user_token, debug!, get_time_ms, now.saturating_sub ...(+5)

#### 文件系统
仅目标项目中观察到该模块代表函数，参考项目没有同模块可对齐证据：
- 目标项目：`os/src/mm/memory_set.rs#split_area_by_range`，调用摘要：BTreeMap::new, MapArea::from_another, VAddrRange::new, Vec::new, area.clone, area.data_frames.retain ...(+19)
- 目标项目：`patch/polyhal/src/debug_console/x86_64/graphic.rs#put_char`，调用摘要：BIT_FONTS.len, self.backspace, self.scroll_up, self.write_pixel
- 目标项目：`patch/virtio-drivers/src/transport/pci.rs#new`，调用摘要：Err, Ok, Some, VirtioPciError::InvalidNotifyOffMultiplier, VirtioPciError::InvalidVendorId, common_cfg.is_none ...(+12)
- 目标项目：`patch/virtio-drivers/src/device/blk.rs#write`，调用摘要：Arc::new, BlkFeature::RING_INDIRECT_DESC.bits, Default::default, Mutex::new, NonNull::from, QUEUE_SIZE.into ...(+16)

#### 设备驱动
仅目标项目中观察到该模块代表函数，参考项目没有同模块可对齐证据：
- 目标项目：`patch/virtio-drivers/src/queue.rs#fake_read_write_queue`，调用摘要：(*available_ring).idx.load, (*used_ring).idx.fetch_add, (*used_ring).idx.load, Vec::new, assert!, assert_eq! ...(+18)
- 目标项目：`patch/virtio-drivers/src/device/sound/fake.rs#handle_control_request`，调用摘要：CommandCode::SOk.into, Some, Vec::new, VirtIOSndHdr {
                            command_code: CommandCode::SOk.into(),
                        }
                        .as_bytes, VirtIOSndHdr::read_from_prefix, VirtIOSndHdr::read_from_prefix(&request)
                .expect ...(+16)
- 目标项目：`patch/virtio-drivers/src/device/socket/connectionmanager.rs#poll`，调用摘要：Connection::new, Err, Ok, SocketError::OutputBufferTooShort, SocketError::OutputBufferTooShort(length).into, Some ...(+16)
- 目标项目：`patch/virtio-drivers/src/device/sound.rs#play`，调用摘要：FakeSoundDevice::new, PcmFeatures::empty, VirtIOSound::<FakeHal, FakeTransport<VirtIOSoundConfig>>::new, VirtIOSound::<FakeHal, FakeTransport<VirtIOSoundConfig>>::new(transport).unwrap, assert_eq!, expected_sound.extend ...(+22)

#### 文件系统/第三方库
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `lwext4_rust/c/lwext4/src/ext4_balloc.c#ext4_balloc_alloc_block` | `Kernel/File/lwext4/ext4_balloc.c#ext4_balloc_alloc_block` | 同板块同名同结构 | ext4_balloc_get_bgid_of_block, ext4_balloc_get_block_of_bgid, ext4_balloc_set_bitmap_csum, ext4_balloc_verify_bitmap_csum, ext4_bg_get_block_bitmap, ext4_bg_get_free_blocks_count ...(+19) | EXT4_Debug_u32, ext4_balloc_get_bgid_of_block, ext4_balloc_get_block_of_bgid, ext4_balloc_set_bitmap_csum, ext4_balloc_verify_bitmap_csum, ext4_bg_get_block_bitmap ...(+19) |
| `lwext4_rust/c/lwext4/src/ext4_journal.c#jbd_journal_prepare` | `Kernel/File/lwext4/ext4_journal.c#jbd_journal_prepare` | 同板块同名同结构 | EXT4_BLOCK_ZERO, RB_FIND, TAILQ_FOREACH_REVERSE_SAFE, TAILQ_FOREACH_SAFE, TAILQ_REMOVE, ext4_bcache_clear_dirty ...(+17) | EXT4_BLOCK_ZERO, RB_FIND, TAILQ_FOREACH_REVERSE_SAFE, TAILQ_FOREACH_SAFE, TAILQ_REMOVE, ext4_bcache_clear_dirty ...(+17) |
| `lwext4_rust/c/lwext4/src/ext4.c#ext4_dir_rm` | `Kernel/File/lwext4/ext4.c#ext4_dir_rm` | 同板块同名同结构 | EXT4_MP_LOCK, EXT4_MP_UNLOCK, ext4_block_cache_write_back, ext4_dir_en_get_inode, ext4_dir_iterator_fini, ext4_dir_iterator_init ...(+19) | EXT4_MP_LOCK, EXT4_MP_UNLOCK, ext4_block_cache_write_back, ext4_dir_en_get_inode, ext4_dir_iterator_fini, ext4_dir_iterator_init ...(+19) |
| `lwext4_rust/c/lwext4/src/ext4_dir_idx.c#ext4_dir_dx_split_index` | `Kernel/File/lwext4/ext4_dir_idx.c#ext4_dir_dx_split_index` | 同板块同名同结构 | ext4_block_set, ext4_dir_dx_climit_get_count, ext4_dir_dx_climit_get_limit, ext4_dir_dx_climit_set_count, ext4_dir_dx_climit_set_limit, ext4_dir_dx_entry_get_hash ...(+10) | ext4_block_set, ext4_dir_dx_climit_get_count, ext4_dir_dx_climit_get_limit, ext4_dir_dx_climit_set_count, ext4_dir_dx_climit_set_limit, ext4_dir_dx_entry_get_hash ...(+10) |

#### 第三方库
仅目标项目中观察到该模块代表函数，参考项目没有同模块可对齐证据：
- 目标项目：`patch/virtio-drivers/src/transport/mmio.rs#vendor_id`，调用摘要：volread!

#### 通用模块/其他
仅目标项目中观察到该模块代表函数，参考项目没有同模块可对齐证据：
- 目标项目：`user/src/bin/user_shell.rs#new`，调用摘要：String::new, arg.as_ptr, arg.as_str, arg.is_empty, args
            .iter, args
            .iter()
            .filter ...(+16)
- 目标项目：`patch/polyhal/src/mem.rs#parse_system_info`，调用摘要：CPU_NUM.init_once, display_info!, fdt.chosen, fdt.chosen().inspect, fdt.find_nodes, fdt.find_nodes("/cpus/cpu").count ...(+8)
- 目标项目：`os/src/signal/sigflags.rs#check_error`，调用摘要：Some, self.contains
- 目标项目：`user/src/lib.rs#_start`，调用摘要：((argv + i * core::mem::size_of::<usize>()) as *const usize).read_volatile, ((str_start + *i) as *const u8).read_volatile, (0usize..)
            .find, (0usize..)
            .find(|i| unsafe { ((str_start + *i) as *const u8).read_volatile() == 0 })
            .unwrap, HEAP.lock, HEAP.lock()
            .init ...(+10)


好的，作为本次代码同源性定性仲裁的审计专家，我已全面审阅了所有证据材料，包括客观AST数据、控辩双方陈词及原始证据。现依据审计指令，出具最终裁决报告。

### 1. 宏观判定
- **定性结论**: **独立开发，底层架构显著差异**
  总体相似度仅为19.96%，且所有重合点均集中于双方共同引入的开源第三方库 `lwext4`。目标项目拥有1898个唯一AST指纹，覆盖进程管理、任务调度、内存管理等完整操作系统内核栈，而参考项目在这些核心模块完全空白。双方在架构设计、编程语言、核心抽象上存在根本性差异，不构成抄袭。

### 2. 全模块覆盖矩阵解读
- **进程管理**: 仅目标项目体现（83个函数）。参考项目无此模块。无共同AST指纹。证据强度：**未形成**。
- **任务调度/异步运行时**: 仅目标项目体现（123个函数）。参考项目无此模块。无共同AST指纹。证据强度：**未形成**。
- **内存管理**: 仅目标项目体现（297个函数）。参考项目无此模块。无共同AST指纹。证据强度：**未形成**。
- **中断/异常处理**: 仅目标项目体现（43个函数）。参考项目无此模块。无共同AST指纹。证据强度：**未形成**。
- **系统调用**: 仅目标项目体现（79个函数）。参考项目无此模块。无共同AST指纹。证据强度：**未形成**。
- **文件系统**: 仅目标项目体现（376个函数）。参考项目无此模块。无共同AST指纹。证据强度：**未形成**。
- **设备驱动**: 仅目标项目体现（373个函数）。参考项目无此模块。无共同AST指纹。证据强度：**未形成**。
- **网络协议栈**: 双方均未体现。证据强度：**未形成**。
- **文件系统/第三方库**: 双方均存在且高度重合。目标项目699个函数，参考项目478个函数，共同AST指纹382个。目标覆盖率64.09%，参考覆盖率95.98%。这源于双方均集成了同一个开源 `lwext4` 库。证据强度：**强（但属第三方库同源，非抄袭）**。
- **第三方库**: 仅目标项目体现（1个函数）。参考项目无此模块。无共同AST指纹。证据强度：**未形成**。
- **通用模块/其他**: 仅目标项目体现（156个函数）。参考项目无此模块。无共同AST指纹。证据强度：**未形成**。

### 3. 底层 AST 关键证据指纹表
| 所属板块 | 目标项目路径 | 参考项目路径 | 证据类型 | 技术说明 |
| :--- | :--- | :--- | :--- | :--- |
| 第三方库/公共依赖 | `lwext4_rust/c/lwext4/src/ext4_balloc.c#ext4_balloc_alloc_block` | `Kernel/File/lwext4/ext4_balloc.c#ext4_balloc_alloc_block` | 同源第三方库 | 双方均调用 `ext4_balloc_get_bgid_of_block` 等标准ext4块分配API，源于同一上游库。 |
| 第三方库/公共依赖 | `lwext4_rust/c/lwext4/src/ext4_journal.c#jbd_journal_prepare` | `Kernel/File/lwext4/ext4_journal.c#jbd_journal_prepare` | 同源第三方库 | 调用图谱包含 `RB_FIND`, `TAILQ_FOREACH_SAFE` 等日志准备标准操作，为 `lwext4` 内部实现。 |
| 第三方库/公共依赖 | `lwext4_rust/c/lwext4/src/ext4_fs.c#ext4_fs_set_inode_data_block_index` | `Kernel/File/lwext4/ext4_fs.c#ext4_fs_set_inode_data_block_index` | 同源第三方库 | 调用 `ext4_balloc_alloc_block`, `ext4_inode_set_direct_block` 等，是 `lwext4` 间接块寻址的标准流程。 |
| 第三方库/公共依赖 | `lwext4_rust/c/lwext4/src/ext4_extent.c#ext4_ext_split_node` | `Kernel/File/lwext4/ext4_extent.c#ext4_ext_split_node` | 同源第三方库 | 调用 `EXT_FIRST_EXTENT`, `EXT_MAX_INDEX` 等宏，是 `lwext4` 内部extent树分裂算法。 |
| 第三方库/公共依赖 | `lwext4_rust/c/lwext4/src/ext4_xattr.c#ext4_xattr_set_entry` | `Kernel/File/lwext4/ext4_xattr.c#ext4_xattr_set_entry` | 同源第三方库 | 调用 `EXT4_XATTR_IS_LAST_ENTRY`, `memmove` 等，是 `lwext4` 扩展属性操作的标准实现。 |
| 第三方库/公共依赖 | `lwext4_rust/c/lwext4/src/ext4_dir_idx.c#ext4_dir_dx_add_entry` | `Kernel/File/lwext4/ext4_dir_idx.c#ext4_dir_dx_add_entry` | 同源第三方库 | 调用 `ext4_dir_dx_get_leaf`, `ext4_dir_dx_split_data` 等，是 `lwext4` 的HTree索引操作。 |
| 第三方库/公共依赖 | `lwext4_rust/c/lwext4/src/ext4_mkfs.c#fill_sb` | `Kernel/File/lwext4/ext4_mkfs.c#fill_sb` | 同源第三方库 | 调用 `ext4_sb_set_blocks_cnt`, `to_le16` 等，是 `lwext4` 创建ext4超级块的标准流程。 |

### 4. 核心代码调用流对比剖析
* **【文件系统/第三方库 - `lwext4` 库集成】**
* **底层技术析理**: 双方在 `lwext4` 库内部函数的调用流高度一致，这是因为它们都直接使用了相同的上游C代码。然而，在集成方式上存在显著差异。目标项目通过 `lwext4_rust` crate 进行封装，使用统一的 `ext4_dbg` 日志接口；而参考项目则直接在内核中嵌入 `lwext4`，并使用自定义的 `EXT4_Debug_u32` 等宏进行调试输出。这表明双方是独立地对同一第三方库进行了适配，而非一方抄袭另一方的集成代码。
* **代码溯源链接**:
  - 目标项目: `lwext4_rust/c/lwext4/src/ext4_balloc.c#ext4_balloc_alloc_block`
  - 参考项目: `Kernel/File/lwext4/ext4_balloc.c#ext4_balloc_alloc_block`

* **【内存管理 - 进程地址空间】**
* **底层技术析理**: 目标项目拥有独立且复杂的内存管理系统，基于Rust的 `Arc`、`MemorySet`、`MapArea` 等抽象构建，支持从ELF文件加载、进程克隆时的写时复制等高级特性。参考项目在此模块完全空白，无任何可对齐证据。这构成了双方在操作系统核心架构上根本性差异的铁证。
* **代码溯源链接**:
  - 目标项目: `os/src/task/task.rs#exec`
  - 参考项目: 无

### 5. 单方模块与降权项
- **单方模块**: 进程管理、任务调度/异步运行时、内存管理、中断/异常处理、系统调用、文件系统、设备驱动、通用模块/其他等所有核心模块均仅在目标项目中存在。这些模块构成了一个完整Rust OS的主体，是目标项目独立开发的强证据。
- **降权项**: 所有重合证据均被降权处理，因为它们全部位于 `lwext4` 第三方库中。双方函数同名同结构是共同使用同一份开源代码的必然结果，不能作为抄袭的证据。目标项目中的 `Arc::new`、`Vec::new` 等Rust标准库泛型函数名，属于公共接口，同样不能作为同源证据。

### 6. 保守结论
本次审计的核心发现是，19.96%的AST相似度完全由双方共同引入的开源第三方库 `lwext4` 贡献。目标项目是一个功能完备的Rust操作系统，拥有超过1500个独立函数，覆盖了从进程管理到设备驱动的全栈内核模块，而参考项目在这些方面完全空白。双方在架构设计、编程语言和核心抽象上存在根本性差异。因此，整体抄袭风险极低，应认定为独立开发。建议在后续比对中，将 `lwext4` 等明确可识别的第三方库从比对范围中排除，以更准确地评估原创代码的相似度。

------------------------------

