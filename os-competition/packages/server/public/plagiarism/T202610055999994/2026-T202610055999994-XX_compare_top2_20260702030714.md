# 🛡️ OS 内核赛自动化代码级架构审计报告

**审计时间**: `2026-07-02 03:07:14`
**参赛作品名称**: `2026-T202610055999994-XX`
**年份**: `2026`
**学校**: `XX`
**队伍名称**: `T202610055999994`
**比赛名字**: `OS-Competition`
**比赛子赛道**: `Kernel-Implementation`
**仓库地址**: `https://gitlab.eduxiji.net/educg-group-43501-3132633/T202610055999994-232`
**推测开发语言**: `Rust / C (由AST探针自动识别)`
**特征函数量**: `2230`

## ⚔️ 底层代码同源性对比 (AST Homology)
> 本部分由专门的代码审查智能体，对该项目与符合年份条件的历届最高相似度项目进行控制流与底层调用对比。新版报告会先给出确定性的全模块覆盖矩阵，再给出 Agent 的分模块解释，避免只围绕少量抽样函数展开。

### 🏆 Top 2 底层对标: vs `2024-cabbageOS-华中科技大学`（华中科技大学 / cabbageOS）

#### 对方作品信息

**对方年份**: `2024`
**对方学校**: `华中科技大学`
**对方队伍名称**: `cabbageOS`
**对方比赛名字**: `OS-Competition`
**对方比赛子赛道**: `Kernel-Implementation`
**对方仓库地址**: `https://gitlab.eduxiji.net/educg-group-26010-2376550/T202410487992456-422`

> 双向覆盖率：共同 AST 指纹: 711；唯一 AST 指纹: 目标项目 1898，参考项目 1579；覆盖率: 目标项目 37.46%，参考项目 45.03%

## 全模块对比总览（确定性证据）

> 总体覆盖率：共同 AST 指纹: 711；唯一 AST 指纹: 目标项目 1898，参考项目 1579；覆盖率: 目标项目 37.46%，参考项目 45.03%

### 1. 模块覆盖矩阵
| 模块 | 目标函数数 | 参考函数数 | 共同唯一 AST 指纹 | Target 覆盖 | Base 覆盖 | 状态 |
| :--- | ---: | ---: | ---: | ---: | ---: | :--- |
| 进程管理 | 83 | 95 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |
| 任务调度/异步运行时 | 123 | 56 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |
| 内存管理 | 297 | 290 | 33 | 11.79% | 11.58% | 双方存在且有结构重合 |
| 中断/异常处理 | 43 | 28 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |
| 系统调用 | 79 | 76 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |
| 文件系统 | 376 | 176 | 15 | 5.49% | 10.2% | 双方存在且有结构重合 |
| 设备驱动 | 373 | 446 | 266 | 78.93% | 70.18% | 双方存在且有结构重合 |
| 网络协议栈 | 0 | 0 | 0 | 0.0% | 0.0% | 双方均未体现 |
| 文件系统/第三方库 | 699 | 522 | 389 | 65.27% | 89.22% | 双方存在且有结构重合 |
| 第三方库 | 1 | 1 | 1 | 100.0% | 100.0% | 双方存在且有结构重合 |
| 通用模块/其他 | 156 | 228 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |

### 2. 分模块证据清单

#### 进程管理
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `os/src/syscall/process.rs#sys_clone` | `kernel/src/syscall/sysproc.c#sys_clone` | 同板块同名 | CloneFlags::from_bits, CloneFlags::from_bits(flags as u32).ok_or_else, Ok, add_task, current_task, current_task().unwrap ...(+6) | arg_addr, arg_int, do_clone |
| `os/src/syscall/process.rs#sys_sched_getaffinity` | `kernel/src/syscall/sysproc.c#sys_sched_getaffinity` | 同板块同名 | Err, Ok, UserBuffer::new, core::cmp::min, core::mem::size_of::<usize>, current_user_token ...(+5) | arg_addr, arg_int, either_copy_out |
| `os/src/syscall/process.rs#sys_sched_setaffinity` | `kernel/src/syscall/sysproc.c#sys_sched_setaffinity` | 同板块同名 | Err, Ok, mask.is_null, trace! | 内部闭环逻辑 |
| `os/src/syscall/process.rs#sys_sched_setscheduler` | `kernel/src/syscall/sysproc.c#sys_sched_setscheduler` | 同板块同名 | Err, Ok, param.is_null, trace! | 内部闭环逻辑 |

#### 任务调度/异步运行时
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `os/src/syscall/fs.rs#sys_fstatat` | `kernel/src/syscall/sysfile.c#sys_fstatat` | 同板块同名 | Err, FileClass::Abs, FstatatFlags::empty, FstatatFlags::from_bits, FstatatFlags::from_bits(flags as u32).unwrap_or, Ok ...(+25) | arg_addr, arg_int, arg_str, either_copy_out, find_inode, get_absolute_path ...(+6) |
| `os/src/syscall/fs.rs#sys_faccessat` | `kernel/src/syscall/sysfile.c#sys_faccessat` | 同板块同名 | Err, Ok, busybox_applet_probe_path, current_task, current_task().unwrap, current_user_token ...(+10) | arg_int, arg_str, find_inode, get_absolute_path, get_fs_from_path, ip->i_op->lock ...(+3) |
| `os/src/syscall/fs.rs#sys_unlinkat` | `kernel/src/syscall/sysfile.c#sys_unlinkat` | 同板块同名 | Ok, UnlinkatFlags::from_bits_truncate, abs_path.as_str, current_task, current_task().unwrap, current_user_token ...(+14) | __namecmp, arg_int, arg_str, dp->i_op->delete, dp->i_op->dirlookup, dp->i_op->lock ...(+13) |
| `os/src/syscall/fs.rs#sys_linkat` | `kernel/src/syscall/sysfile.c#sys_linkat` | 同板块同名 | Err, current_task, current_task().unwrap, current_user_token, debug!, drop ...(+8) | ASSERT, arg_int, arg_str, find_inode, get_absolute_path, get_fs_from_path ...(+3) |

#### 内存管理
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `patch/virtio-drivers/src/device/socket/connectionmanager.rs#send_recv` | `kernel/dep/virtio-drivers/src/device/socket/connectionmanager.rs#send_recv` | 同板块同名 | "Hello from guest".as_bytes, (hello_from_guest.len() as u32).into, (hello_from_host.len() as u32).into, 0.into, 50.into, Arc::new ...(+44) | "Hello from guest".as_bytes, (hello_from_guest.len() as u32).into, (hello_from_host.len() as u32).into, 0.into, 50.into, Arc::new ...(+43) |
| `patch/virtio-drivers/src/device/socket/connectionmanager.rs#incoming_connection` | `kernel/dep/virtio-drivers/src/device/socket/connectionmanager.rs#incoming_connection` | 同板块同名同结构 | 0.into, 50.into, Arc::new, Default::default, Mutex::new, NonNull::from ...(+25) | 0.into, 50.into, Arc::new, Default::default, Mutex::new, NonNull::from ...(+25) |
| `patch/virtio-drivers/src/queue.rs#recycle_descriptors` | `kernel/dep/virtio-drivers/src/queue.rs#recycle_descriptors` | 同板块同名同结构 | Box::from_raw, H::unshare, InputOutputIter::new, InputOutputIter::new(inputs, outputs).enumerate, Some, assert_eq! ...(+17) | Box::from_raw, H::unshare, InputOutputIter::new, InputOutputIter::new(inputs, outputs).enumerate, Some, assert_eq! ...(+17) |
| `os/src/drivers/virtio_blk.rs#dma_alloc` | `kernel/dep/virtio-drivers/examples/aarch64/src/hal.rs#dma_alloc` | 同板块同名 | NonNull::new_unchecked, PhysAddr::new, QUEUE_FRAMES.exclusive_access, QUEUE_FRAMES.exclusive_access().push, assert_eq!, base_addr.get_mut_ptr ...(+5) | Layout::from_size_align, Layout::from_size_align(pages * PAGE_SIZE, PAGE_SIZE).unwrap, NonNull::new, alloc_zeroed, handle_alloc_error, trace! ...(+2) |

#### 中断/异常处理
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `os/src/signal/sigflags.rs#default_op` | `kernel/platform/visionfive/src/trap.c#tf_flrestore` | 同模块上下文对齐/差异观察 | continue_signals.contains, dump_signals.contains, ignore_signals.contains, stop_signals.contains, terminate_signals.contains | 内部闭环逻辑 |
| `patch/polyhal/src/arch/x86_64/idt.rs#new` | `kernel/platform/visionfive/src/trap.c#tf_flstore` | 同模块上下文对齐/差异观察 | InterruptDescriptorTable::new, Self, core::slice::from_raw_parts_mut, entries[i].set_handler_fn, transmute::<extern "C" fn(), extern "x86-interrupt" fn(InterruptStackFrame)> | 内部闭环逻辑 |
| `patch/polyhal/src/utils/mutex_no_irq.rs#try_lock` | `kernel/platform/qemu/src/virtio_disk.c#virtio_disk_intr` | 同模块上下文对齐/差异观察 | IRQ::int_disable, IRQ::int_enabled, self.lock
            .try_lock, self.lock
            .try_lock()
            .map, self.lock.is_locked | R, __sync_synchronize, acquire, panic, release, sem_v |
| `os/src/trap/interrupts.rs#inc_irq_count` | `kernel/platform/qemu/src/trap.c#kerneltrap` | 同模块上下文对齐/差异观察 | IRQ_COUNTS.lock, counts[irq].saturating_add | backtrace, devintr, intr_get, panic, printf, r_scause ...(+7) |

#### 系统调用
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `os/src/syscall/other.rs#sys_uname` | `kernel/src/syscall/sysinfo.c#sys_uname` | 同板块同名 | Ok, UserBuffer::new, buf[..len].copy_from_slice, bytes.len, bytes.len().min, copy_str_to_buf ...(+8) | arg_addr, either_copy_out |
| `user/src/lib.rs#waitpid` | `user/deps/syscall.c#waitpid` | 同板块同名 | sys_waitpid, yield_ | syscall |
| `user/src/lib.rs#wait` | `user/deps/syscall.c#wait` | 同板块同名 | sys_waitpid, yield_ | waitpid |
| `user/src/lib.rs#sleep` | `user/deps/syscall.c#sleep` | 同板块同名 | sys_get_time, sys_yield | syscall |

#### 文件系统
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `patch/virtio-drivers/src/device/blk.rs#device_id` | `kernel/dep/virtio-drivers/src/device/blk.rs#device_id` | 同板块同名同结构 | Arc::new, BlkFeature::RING_INDIRECT_DESC.bits, Default::default, Mutex::new, NonNull::from, QUEUE_SIZE.into ...(+14) | Arc::new, BlkFeature::RING_INDIRECT_DESC.bits, Default::default, Mutex::new, NonNull::from, QUEUE_SIZE.into ...(+14) |
| `patch/virtio-drivers/src/device/blk.rs#flush` | `kernel/dep/virtio-drivers/src/device/blk.rs#flush` | 同板块同名同结构 | (BlkFeature::RING_INDIRECT_DESC | BlkFeature::FLUSH).bits, Arc::new, Default::default, Mutex::new, NonNull::from, QUEUE_SIZE.into ...(+13) | (BlkFeature::RING_INDIRECT_DESC | BlkFeature::FLUSH).bits, Arc::new, Default::default, Mutex::new, NonNull::from, QUEUE_SIZE.into ...(+13) |
| `patch/virtio-drivers/src/transport/pci/bus.rs#bar_info` | `kernel/dep/virtio-drivers/src/transport/pci/bus.rs#bar_info` | 同板块同名 | (!(size_mask & 0xfffffff0)).wrapping_add, Err, MemoryBarType::try_from, Ok, self
                    .configuration_access
                    .read_word, self
            .configuration_access
            .read_word ...(+2) | (!(size_mask & 0xfffffff0)).wrapping_add, Err, MemoryBarType::try_from, Ok, self.config_read_word, self.config_write_word ...(+1) |
| `patch/virtio-drivers/src/device/blk.rs#config` | `kernel/dep/virtio-drivers/src/device/blk.rs#config` | 同板块同名同结构 | Arc::new, BlkFeature::RO.bits, Default::default, Mutex::new, NonNull::from, QUEUE_SIZE.into ...(+6) | Arc::new, BlkFeature::RO.bits, Default::default, Mutex::new, NonNull::from, QUEUE_SIZE.into ...(+6) |

#### 设备驱动
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `patch/virtio-drivers/src/queue.rs#fake_read_write_queue` | `kernel/dep/virtio-drivers/src/queue.rs#fake_read_write_queue` | 同板块同名 | (*available_ring).idx.load, (*used_ring).idx.fetch_add, (*used_ring).idx.load, Vec::new, assert!, assert_eq! ...(+18) | (*available_ring).idx.load, (*used_ring).idx.fetch_add, (*used_ring).idx.load, Vec::new, assert!, assert_eq! ...(+18) |
| `patch/virtio-drivers/src/device/sound/fake.rs#handle_control_request` | `kernel/dep/virtio-drivers/src/device/sound/fake.rs#handle_control_request` | 同板块同名 | CommandCode::SOk.into, Some, Vec::new, VirtIOSndHdr {
                            command_code: CommandCode::SOk.into(),
                        }
                        .as_bytes, VirtIOSndHdr::read_from_prefix, VirtIOSndHdr::read_from_prefix(&request)
                .expect ...(+16) | CommandCode::SOk.into, Some, Vec::new, VirtIOSndHdr {
                            command_code: CommandCode::SOk.into(),
                        }
                        .as_bytes, VirtIOSndHdr::read_from_prefix, VirtIOSndHdr::read_from_prefix(&request).expect ...(+16) |
| `patch/virtio-drivers/src/device/sound.rs#play` | `kernel/dep/virtio-drivers/src/device/sound.rs#play` | 同板块同名同结构 | FakeSoundDevice::new, PcmFeatures::empty, VirtIOSound::<FakeHal, FakeTransport<VirtIOSoundConfig>>::new, VirtIOSound::<FakeHal, FakeTransport<VirtIOSoundConfig>>::new(transport).unwrap, assert_eq!, expected_sound.extend ...(+22) | FakeSoundDevice::new, PcmFeatures::empty, VirtIOSound::<FakeHal, FakeTransport<VirtIOSoundConfig>>::new, VirtIOSound::<FakeHal, FakeTransport<VirtIOSoundConfig>>::new(transport).unwrap, assert_eq!, expected_sound.extend ...(+22) |
| `patch/virtio-drivers/src/device/sound.rs#pcm_xfer` | `kernel/dep/virtio-drivers/src/device/sound.rs#pcm_xfer` | 同板块同名同结构 | CommandCode::SOk.into, Default::default, Err, Ok, Some, array::from_fn ...(+17) | CommandCode::SOk.into, Default::default, Err, Ok, Some, array::from_fn ...(+17) |

#### 文件系统/第三方库
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `lwext4_rust/c/lwext4/src/ext4_balloc.c#ext4_balloc_alloc_block` | `kernel/src/fs/ext4/lwext4/ext4_balloc.c#ext4_balloc_alloc_block` | 同板块同名同结构 | ext4_balloc_get_bgid_of_block, ext4_balloc_get_block_of_bgid, ext4_balloc_set_bitmap_csum, ext4_balloc_verify_bitmap_csum, ext4_bg_get_block_bitmap, ext4_bg_get_free_blocks_count ...(+19) | ext4_balloc_get_bgid_of_block, ext4_balloc_get_block_of_bgid, ext4_balloc_set_bitmap_csum, ext4_balloc_verify_bitmap_csum, ext4_bg_get_block_bitmap, ext4_bg_get_free_blocks_count ...(+19) |
| `lwext4_rust/c/lwext4/src/ext4_journal.c#jbd_journal_prepare` | `kernel/src/fs/ext4/lwext4/ext4_journal.c#jbd_journal_prepare` | 同板块同名同结构 | EXT4_BLOCK_ZERO, RB_FIND, TAILQ_FOREACH_REVERSE_SAFE, TAILQ_FOREACH_SAFE, TAILQ_REMOVE, ext4_bcache_clear_dirty ...(+17) | EXT4_BLOCK_ZERO, RB_FIND, TAILQ_FOREACH_REVERSE_SAFE, TAILQ_FOREACH_SAFE, TAILQ_REMOVE, ext4_bcache_clear_dirty ...(+17) |
| `lwext4_rust/c/lwext4/src/ext4.c#ext4_dir_rm` | `kernel/src/fs/ext4/lwext4/ext4.c#ext4_dir_rm` | 同板块同名同结构 | EXT4_MP_LOCK, EXT4_MP_UNLOCK, ext4_block_cache_write_back, ext4_dir_en_get_inode, ext4_dir_iterator_fini, ext4_dir_iterator_init ...(+19) | EXT4_MP_LOCK, EXT4_MP_UNLOCK, ext4_block_cache_write_back, ext4_dir_en_get_inode, ext4_dir_iterator_fini, ext4_dir_iterator_init ...(+19) |
| `lwext4_rust/c/lwext4/src/ext4_dir_idx.c#ext4_dir_dx_split_index` | `kernel/src/fs/ext4/lwext4/ext4_dir_idx.c#ext4_dir_dx_split_index` | 同板块同名同结构 | ext4_block_set, ext4_dir_dx_climit_get_count, ext4_dir_dx_climit_get_limit, ext4_dir_dx_climit_set_count, ext4_dir_dx_climit_set_limit, ext4_dir_dx_entry_get_hash ...(+10) | ext4_block_set, ext4_dir_dx_climit_get_count, ext4_dir_dx_climit_get_limit, ext4_dir_dx_climit_set_count, ext4_dir_dx_climit_set_limit, ext4_dir_dx_entry_get_hash ...(+10) |

#### 第三方库
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `patch/virtio-drivers/src/transport/mmio.rs#vendor_id` | `kernel/dep/virtio-drivers/src/transport/mmio.rs#vendor_id` | 同板块同名同结构 | volread! | volread! |

#### 通用模块/其他
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `patch/polyhal/src/debug_console/x86_64.rs#putchar` | `user/deps/printf.c#putchar` | 同板块同名 | com::putchar, graphic::putchar, vga_text::putchar | write |
| `patch/polyhal/src/debug_console/x86_64.rs#getchar` | `user/deps/printf.c#getchar` | 同板块同名 | com::getchar, keyboard::get_key | read |
| `user/src/bin/user_shell.rs#new` | `kernel/src/lib/qsort.c#_quicksort` | 同模块上下文对齐/差异观察 | String::new, arg.as_ptr, arg.as_str, arg.is_empty, args
            .iter, args
            .iter()
            .filter ...(+16) | (*cmp), POP, PUSH, SWAP, min |
| `patch/polyhal/src/mem.rs#parse_system_info` | `kernel/dep/sdcard/src/visionfive2_sd/sd.rs#init_sdcard` | 同模块上下文对齐/差异观察 | CPU_NUM.init_once, display_info!, fdt.chosen, fdt.chosen().inspect, fdt.find_nodes, fdt.find_nodes("/cpus/cpu").count ...(+8) | CardTypeReg::from, CardTypeReg::from(0).with_card_width4_1, CmdArg::new, CmdReg::from, ControlReg::from, RawInterruptStatusReg::from ...(+20) |


好的，审计官。以下是基于您提供的全案材料，依据《审计指令》生成的最终同源性定性仲裁报告。

***

#### 1. 宏观判定
- **定性结论**: **独立开发，底层架构显著差异**
- **核心依据**: 整体 AST 相似度仅为 25.7%，处于较低水平。该相似度主要由双方共用的第三方库（如 `lwext4`、`virtio-drivers`）贡献。在操作系统核心板块（进程管理、任务调度、内存管理、系统调用等），双方共同 AST 指纹极低或为零，且在内存模型、同步策略、对象管理模型上存在根本性架构差异。控方关于【进程管理】板块的指控，因该板块共同 AST 指纹为 0 而缺乏客观证据支持。

#### 2. 全模块覆盖矩阵解读
- **进程管理**: 双方均存在。共同 AST 指纹为 0，覆盖率为 0.0%。这表明双方在进程创建、调度、退出等核心逻辑的实现上，从 AST 结构层面观察是完全独立的。控方指控的“语义平移”未得到 AST 数据支持。**证据强度：未形成**。
- **任务调度/异步运行时**: 双方均存在。共同 AST 指纹为 0，覆盖率为 0.0%。目标项目拥有 123 个函数，远超参考项目的 56 个，暗示其可能实现了更复杂的异步运行时，与参考项目的传统调度器在架构上存在显著差异。**证据强度：未形成**。
- **内存管理**: 双方均存在。存在 33 个共同 AST 指纹，覆盖率约 11%。这部分重合极大概率源于双方共用的 `virtio-drivers` 等第三方库中的内存分配与队列管理逻辑，而非核心内核内存管理算法（如页表、分配器）的同源。**证据强度：弱**。
- **中断/异常处理**: 双方均存在。共同 AST 指纹为 0，覆盖率为 0.0%。目标项目基于 Rust 的 `InterruptDescriptorTable` 和 `IRQ` 抽象，与参考项目基于 C 语言的 `trap.c` 和 `intr_get/release` 在实现范式上完全不同。**证据强度：未形成**。
- **系统调用**: 双方均存在。共同 AST 指纹为 0，覆盖率为 0.0%。尽管存在大量同名函数（由 POSIX/ABI 决定），但 AST 图谱显示其内部实现（如参数解析、缓冲区管理）使用了完全不同的外部对象和调用流。**证据强度：未形成**。
- **文件系统**: 双方均存在。存在 15 个共同 AST 指纹，覆盖率较低（目标 5.49%，参考 10.2%）。这部分重合主要来自 `virtio-drivers` 中的块设备驱动和 `lwext4` 文件系统库。双方自研的 VFS/procfs 层（如目标项目的 `VirtFile` 与参考项目的 `proc_ops`）在 AST 层面无重合。**证据强度：弱**。
- **设备驱动**: 双方均存在。共同 AST 指纹高达 266 个，覆盖率极高（目标 78.93%，参考 70.18%）。这是整体相似度的主要来源，但证据明确指向双方均大量复用了 `virtio-drivers` 这一开源第三方库，属于公共依赖。**证据强度：强（但属第三方库）**。
- **网络协议栈**: 双方均未体现。**证据强度：未形成**。
- **第三方库/公共依赖**: 双方均存在。共同 AST 指纹高达 389 个，覆盖率极高（目标 65.27%，参考 89.22%）。证据明确指向 `lwext4_rust` 和 `kernel/src/fs/ext4/lwext4` 等文件系统库，属于对同一开源项目的复用。**证据强度：强（但属第三方库）**。

#### 3. 底层 AST 关键证据指纹表
| 所属板块 | 目标项目路径 | 参考项目路径 | 证据类型 | 技术说明 |
| :--- | :--- | :--- | :--- | :--- |
| 第三方库/公共依赖 | `lwext4_rust/c/lwext4/src/ext4_balloc.c#ext4_balloc_alloc_block` | `kernel/src/fs/ext4/lwext4/ext4_balloc.c#ext4_balloc_alloc_block` | 同板块同名同结构 | 完全一致的 `ext4` 块分配逻辑，调用链高度吻合，属于对同一开源 C 库的绑定或移植。 |
| 第三方库/公共依赖 | `patch/virtio-drivers/src/device/blk.rs#device_id` | `kernel/dep/virtio-drivers/src/device/blk.rs#device_id` | 同板块同名同结构 | 完全一致的 VirtIO 块设备初始化逻辑，包括特性位、队列大小等参数设置，属于同一 Rust 库。 |
| 第三方库/公共依赖 | `patch/virtio-drivers/src/device/sound.rs#play` | `kernel/dep/virtio-drivers/src/device/sound.rs#play` | 同板块同名同结构 | 完全一致的 VirtIO 音频设备播放测试逻辑，包括 `FakeSoundDevice` 和 `FakeTransport` 的使用。 |
| 设备驱动 | `patch/virtio-drivers/src/queue.rs#fake_read_write_queue` | `kernel/dep/virtio-drivers/src/queue.rs#fake_read_write_queue` | 同板块同名 | 完全一致的虚拟队列读写测试逻辑，对 `available_ring` 和 `used_ring` 的操作序列相同。 |
| 内存管理 | `patch/virtio-drivers/src/device/socket/connectionmanager.rs#send_recv` | `kernel/dep/virtio-drivers/src/device/socket/connectionmanager.rs#send_recv` | 同板块同名 | 高度一致的 VirtIO 套接字收发测试，包含相同的硬编码字符串和缓冲区长度。 |
| 文件系统 | `patch/virtio-drivers/src/transport/pci/bus.rs#bar_info` | `kernel/dep/virtio-drivers/src/transport/pci/bus.rs#bar_info` | 同板块同名 | 高度相似的 PCI 基址寄存器信息读取逻辑，差异仅在方法名（`configuration_access.read_word` vs `config_read_word`）。 |

#### 4. 核心代码调用流对比剖析
* **【进程管理/进程退出路径】**
* **底层技术析理**: 目标项目采用 Rust 的所有权与引用计数模型管理任务资源。`exit_current_group_and_run_next` 通过 `Arc::ptr_eq` 判断任务组归属，利用 `drop` 和调度器的 `run_next` 实现隐式资源回收与切换。参考项目则采用传统的 C 语言手动资源管理模型，通过 `acquire/release` 自旋锁保护、`list_for_each_entry_safe` 遍历链表、并显式调用 `free_thread` 和 `exit_wakeup` 进行资源释放与唤醒。两者在**内存抽象、同步原语和对象生命周期管理**上存在根本性差异，调用流无 AST 层面的同构。
* **代码溯源链接**:
  - 目标项目: `os/src/task/mod.rs#exit_current_group_and_run_next`
  - 参考项目: `kernel/src/proc/pcb_life.c#do_exit_group`

* **【系统调用/调度亲和性】**
* **底层技术析理**: 目标项目的 `sys_sched_getaffinity` 使用 `UserBuffer::new` 和 `slice.iter_mut()` 等 Rust 安全抽象来操作用户态内存，体现了基于类型和缓冲区的内存访问模型。参考项目则使用 `arg_addr` 获取原始地址，再通过 `either_copy_out` 进行拷贝，是典型的内核手动内存拷贝模型。两者在**用户态内存访问的抽象层次和实现机制**上完全不同。
* **代码溯源链接**:
  - 目标项目: `os/src/syscall/process.rs#sys_sched_getaffinity`
  - 参考项目: `kernel/src/syscall/sysproc.c#sys_sched_getaffinity`

* **【中断/异常处理/Trap 处理】**
* **底层技术析理**: 目标项目在 `x86_64` 架构下，通过 Rust 的 `InterruptDescriptorTable` 结构体和 `set_handler_fn` 方法，以类型安全的方式注册中断处理函数。参考项目则是在 `riscv64` 架构下，通过 C 语言的 `trap.c` 中的 `kerneltrap` 函数，使用 `r_scause()` 读取异常原因并手动分发。两者**硬件架构、编程语言和中断分发机制**均截然不同。
* **代码溯源链接**:
  - 目标项目: `patch/polyhal/src/arch/x86_64/idt.rs#new`
  - 参考项目: `kernel/platform/qemu/src/trap.c#kerneltrap`

#### 5. 单方模块与降权项
- **目标项目独有模块/函数**:
  - **平台兼容性测试过滤**: `matches_riscv_iozone_*`、`is_loongarch_musl_ltp_tail` 等函数，是目标项目为特定测试套件和硬件平台（RISC-V, LoongArch）实现的兼容性逻辑，参考项目完全不存在。
  - **脚本解释器**: `parse_shebang` 函数是目标项目独立实现的脚本解释器行解析逻辑。
  - **环境变量搜索**: `open_from_path_env` 函数实现了独立的 `PATH` 环境变量搜索。
  - **Rust 化 Procfs**: `create_proc_dir`、`register_vfile` 等函数展示了基于 `Arc` 和 `VirtFile` 的 Rust 化 procfs 注册机制，与参考项目的 C 风格 procfs 完全不同。
  以上均为目标项目独立开发的有力证据。

- **降权项说明**:
  - **同名系统调用**: `sys_getpid`、`sys_exit`、`sys_clone` 等函数名由 POSIX/Linux ABI 强制规定，不能作为复用证据。
  - **标准 POSIX 语义**: 进程退出、信号掩码修改等操作的高层状态机由规范定义，独立实现必然收敛，其相似性不构成代码抄袭。
  - **语言原语**: `Err`、`Ok`、`Some`、`vec!`、`println!` 等是 Rust 语言的标准库原语，`acquire`、`release`、`list_for_each` 等是 C 语言内核的常用范式，不能作为跨语言复用的证据。
  - **第三方库**: `virtio-drivers` 和 `lwext4` 是双方共用的开源公共依赖，其内部的高 AST 重合度是正常现象，应从核心自研代码的比对中剥离。

#### 6. 保守结论
本案整体 AST 相似度为 25.7%，处于较低水平。该相似度主要由双方共用的第三方开源库（`virtio-drivers`、`lwext4`）贡献，剥离后，在操作系统核心板块（进程、内存、中断、系统调用）的共同 AST 指纹为零或极低。控方对【进程管理】板块的指控缺乏客观 AST 证据支持，其列举的相似性均可归因于 POSIX 标准语义、ABI 同名约束或跨语言范式的主观类比。目标项目在内存模型（所有权 vs 手动锁）、同步策略（隐式 drop vs 显式 acquire/release）及平台兼容性等方面展现出显著的独立架构设计。综合判定，目标项目不存在对参考项目核心自研代码的系统性复用，原创性风险较低。

------------------------------

