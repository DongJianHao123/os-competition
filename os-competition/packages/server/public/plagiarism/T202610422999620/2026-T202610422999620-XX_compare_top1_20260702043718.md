# 🛡️ OS 内核赛自动化代码级架构审计报告

**审计时间**: `2026-07-02 04:37:18`
**参赛作品名称**: `2026-T202610422999620-XX`
**年份**: `2026`
**学校**: `XX`
**队伍名称**: `T202610422999620`
**比赛名字**: `OS-Competition`
**比赛子赛道**: `Kernel-Implementation`
**仓库地址**: `https://gitlab.eduxiji.net/educg-group-43501-3132633/T202610422999620-3166`
**推测开发语言**: `Rust / C (由AST探针自动识别)`
**特征函数量**: `2581`

## ⚔️ 底层代码同源性对比 (AST Homology)
> 本部分由专门的代码审查智能体，对该项目与符合年份条件的历届最高相似度项目进行控制流与底层调用对比。新版报告会先给出确定性的全模块覆盖矩阵，再给出 Agent 的分模块解释，避免只围绕少量抽样函数展开。

### 🏆 Top 1 底层对标: vs `2025-火箭队-哈尔滨工业大学`（哈尔滨工业大学 / 火箭队）

#### 对方作品信息

**对方年份**: `2025`
**对方学校**: `哈尔滨工业大学`
**对方队伍名称**: `火箭队`
**对方比赛名字**: `OS-Competition`
**对方比赛子赛道**: `Kernel-Implementation`
**对方仓库地址**: `https://gitlab.eduxiji.net/educg-group-36002-2710490/T202510213995926-2475`

> 双向覆盖率：共同 AST 指纹: 149；唯一 AST 指纹: 目标项目 1796，参考项目 2341；覆盖率: 目标项目 8.3%，参考项目 6.36%

## 全模块对比总览（确定性证据）

> 总体覆盖率：共同 AST 指纹: 149；唯一 AST 指纹: 目标项目 1796，参考项目 2341；覆盖率: 目标项目 8.3%，参考项目 6.36%

### 1. 模块覆盖矩阵
| 模块 | 目标函数数 | 参考函数数 | 共同唯一 AST 指纹 | Target 覆盖 | Base 覆盖 | 状态 |
| :--- | ---: | ---: | ---: | ---: | ---: | :--- |
| 进程管理 | 587 | 534 | 4 | 1.62% | 2.68% | 双方存在且有结构重合 |
| 任务调度/异步运行时 | 344 | 552 | 21 | 7.47% | 4.48% | 双方存在且有结构重合 |
| 内存管理 | 452 | 666 | 21 | 5.11% | 4.58% | 双方存在且有结构重合 |
| 中断/异常处理 | 21 | 41 | 3 | 15.0% | 8.57% | 双方存在且有结构重合 |
| 系统调用 | 209 | 85 | 12 | 6.94% | 15.79% | 双方存在且有结构重合 |
| 文件系统 | 558 | 813 | 12 | 3.16% | 2.48% | 双方存在且有结构重合 |
| 设备驱动 | 34 | 185 | 5 | 16.67% | 3.47% | 双方存在且有结构重合 |
| 网络协议栈 | 133 | 156 | 20 | 16.53% | 14.71% | 双方存在且有结构重合 |
| 文件系统/第三方库 | 69 | 313 | 1 | 1.49% | 0.53% | 双方存在且有结构重合 |
| 通用模块/其他 | 174 | 454 | 31 | 24.6% | 10.65% | 双方存在且有结构重合 |

### 2. 分模块证据清单

#### 进程管理
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `os/src/task/task.rs#exit_robust_list` | `os/src/futex/robust_list.rs#exit_robust_list` | 同板块同名 | copy_from_user, copy_from_user(
            &mut futex_offset as *mut isize,
            (head + core::mem::size_of::<usize>()) as *const isize,
            1,
        )
        .is_err, copy_from_user(
            &mut pending as *mut usize,
            (head + core::mem::size_of::<usize>() * 2) as *const usize,
            1,
        )
        .is_err, copy_from_user(&mut first_entry as *mut usize, head as *const usize, 1).is_err, copy_from_user(&mut next as *mut usize, entry as *const usize, 1).is_err, core::mem::size_of::<usize> ...(+3) | Ok, RobustListHead::default, copy_from_user, current_task, entry.is_null, handle_futex_death ...(+2) |
| `os/src/syscall/process.rs#sys_execve` | `os/src/syscall/task.rs#sys_execve` | 同板块同名 | Arc::new, Err, File::new, Ok, Vec::new, args_vec.as_slice ...(+18) | "/".to_string, Err, Ok, OpenFlags::empty, c_str_to_string, current_task ...(+13) |
| `os/src/syscall/process.rs#sys_execveat` | `os/src/syscall/task.rs#sys_execveat` | 同板块同名 | Arc::new, Err, File::new, Vec::new, args.is_null, args_vec.as_slice ...(+22) | "/".to_string, Err, Nameidata::new, Ok, OpenFlags::empty, c_str_to_string ...(+17) |
| `os/src/fs/proc/dirs.rs#lookup` | `os/src/fs/proc/fd.rs#lookup` | 同板块同名 | Arc::new, Err, Ok, ProcReadOnlyInode::new, ProcSysctlUsizeInode::new, ProcWritableStringInode::new | Dentry::negative, FdInode::new, Some, current_task, dentry
                    .flags
                    .write, dentry
                    .flags
                    .write()
                    .update_type_from_negative ...(+9) |

#### 任务调度/异步运行时
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `os/src/task/task.rs#init_user_stack` | `os/src/task/task.rs#init_user_stack` | 同板块同名 | (idx + 1usize).wrapping_mul, Ok, Vec::with_capacity, addresses.push, align_down, args_vec.len ...(+28) | VirtAddr::from, args_vec.len, aux_headers.iter, aux_headers.iter().enumerate, aux_headers.len, auxs_vec.push ...(+25) |
| `os/src/syscall/fs.rs#sys_splice` | `os/src/syscall/fs.rs#sys_splice` | 同板块同名 | Err, current_task, current_task().expect, explicit_in_offset.is_some, explicit_out_offset.is_some, in_entry.file.clone ...(+24) | Err, Ok, copy_from_user, copy_to_user, current_task, in_file.get_flags ...(+19) |
| `os/src/syscall/fs.rs#sys_vmsplice` | `os/src/syscall/fs.rs#sys_vmsplice` | 同板块同名 | Err, Ok, check_iovec_buffers, current_task, current_task().expect, fd_entry.file.clone ...(+15) | Err, Ok, copy_from_user, copy_to_user, current_task, file.as_any ...(+15) |
| `os/src/net/tcp.rs#connect` | `os/src/net/tcp.rs#connect` | 同板块同名 | Err, Ok, SocketSetWrapper::new_tcp_socket, Some, from_sockaddr_to_ipendpoint, iface.lock ...(+29) | ETH0_LA2000.lock, Err, LOOPBACK.get, LOOPBACK.get().unwrap, Ok, Ok::<(IpEndpoint, IpEndpoint), Errno> ...(+39) |

#### 内存管理
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `os/src/syscall/mod.rs#syscall` | `os/src/syscall/mod.rs#syscall` | 同板块同名 | Err, merge_offset_arg, sys_accept, sys_accept4, sys_adjtimex, sys_bind ...(+211) | Err, flush, log::trace!, log::warn!, sys_acct, sys_adjtimex ...(+205) |
| `os/src/mm/memory_set.rs#from_existed_user` | `os/src/arch/la64/mm/page_table.rs#from_existed_user` | 同板块同名 | BTreeSet::new, Err, MapArea::from_another, Ok, Self::from_kernel_page_table, area.data_frames.contains_key ...(+21) | PageTableEntry::from_pte_cow, PageTableEntry::new, Vec::new, VirtAddr::from, VirtAddr::from(USER_MAX_VA).ceil, cld_2_table.copy_from_slice ...(+23) |
| `os/src/syscall/fs.rs#sys_openat2` | `os/src/syscall/fs.rs#sys_openat2` | 同板块同名 | (how as *const u8).add, Err, FdEntry::new, OpenFlags::from, OpenFlags::from(khow.flags as usize).contains, alloc::vec! ...(+24) | Err, FdFlags::from, Nameidata::new2, OpenFlags::from_bits, OpenFlags::from_bits(how.flags as i32).ok_or, ResolveFlags::from_bits ...(+20) |
| `os/src/syscall/mm.rs#sys_mmap` | `os/src/syscall/mm.rs#sys_mmap` | 同板块同名 | Err, MMAPFLAGS::MAP_SHARED_VALIDATE.bits, MMAPFLAGS::all, MMAPFLAGS::all().bits, MMAPFLAGS::from_bits_truncate, MMapProt::from_bits ...(+18) | Err, MapArea::new, MmapFlags::from_bits, MmapProt::from_bits, MmapProt::from_bits(prot as u32).unwrap, Ok ...(+37) |

#### 中断/异常处理
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `os/src/arch/loongarch64/register/mod.rs#cause` | `os/src/arch/la64/register/base/estat.rs#cause` | 同板块同名 | Trap::Exception, Trap::Interrupt, ecode, exception_from_ecode, interrupt_bits, interrupt_from_index ...(+1) | Exception::try_from, Exception::try_from(ecode).unwrap, Interrupt::try_from, Interrupt::try_from(63 - self.get_is().leading_zeros() as usize).unwrap, MErrCtl::read, MErrCtl::read().is_merr ...(+11) |
| `os/src/arch/loongarch64/trap/mod.rs#enable_timer_interrupt` | `os/src/arch/la64/trap/timer.rs#enable_timer_interrupt` | 同板块同名 | ecfg::enable_timer_interrupt, super::register::crmd::set_interrupt_enabled | ECfg::empty, ECfg::empty()
        .set_line_based_interrupt_vector, ECfg::empty()
        .set_line_based_interrupt_vector(LineBasedInterrupt::TIMER)
        .write, get_clock_freq, get_timer_freq_first_time |
| `os/src/arch/loongarch64/register/mod.rs#enable_timer_interrupt` | `os/src/arch/riscv64/trap/mod.rs#enable_timer_interrupt` | 同板块同名 | read, write | sie::set_stimer |
| `os/src/signal/sig_handler.rs#default` | `os/src/signal/sig_handler.rs#default` | 同板块同名同结构 | 内部闭环逻辑 | 内部闭环逻辑 |

#### 系统调用
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `user/src/syscall.rs#syscall` | `user/src/syscall.rs#syscall` | 同板块同名 | asm! | asm! |
| `os/src/syscall/mm.rs#sys_get_mempolicy` | `os/src/syscall/mm.rs#sys_get_mempolicy` | 同板块同名 | Err, Ok, copy_to_user, maxnode.div_ceil, mode.is_null, nodemask.add ...(+1) | Ok, log::error! |
| `os/src/syscall/time.rs#sys_clock_getres` | `os/src/syscall/util.rs#sys_clock_getres` | 同板块同名 | Err, Ok, copy_to_user, is_supported_clock, res.is_null | Err, Ok, TimeSpec::from_nanos, copy_to_user, log::info! |
| `user/src/syscall.rs#sys_execve` | `user/src/syscall.rs#sys_execve` | 同板块同名 | args.as_ptr, path.as_ptr, syscall | argv.as_ptr, envp.as_ptr, path.as_ptr, syscall |

#### 文件系统
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `os/src/fs/mount.rs#do_mount` | `os/src/fs/mount.rs#do_mount` | 同板块同名 | Arc::downgrade, Arc::new, AtomicUsize::new, Err, Mount::new_child, Mount::new_child_with_backing_root ...(+43) | Ok |
| `os/src/fs/dev/mod.rs#readdir` | `os/src/fs/file.rs#readdir` | 同板块同名 | Ok, vec! | Err, Ok, copy_to_user, core::mem::size_of::<LinuxDirent64>, inner.inode.getdents, ker_buf[..buf_offset].as_ptr ...(+5) |
| `os/src/net/tcp.rs#send` | `os/src/net/tcp.rs#send` | 同板块同名 | Err, Ok, self.block_on, self.handle.get, self.handle.get().read, self.handle.get().read().unwrap ...(+13) | Err, Ok, SOCKET_SET.lock, SOCKET_SET.lock().get_mut, SOCKET_SET.lock().get_mut().unwrap, SOCKET_SET.lock().get_mut().unwrap().with_socket_mut::<_,tcp::Socket,_> ...(+16) |
| `os/src/fs/dev/shm.rs#create` | `os/src/fs/inode.rs#create` | 同板块同名 | Arc::new, Err, NEXT_SHM_INO.fetch_add, Ok, ShmDirInode::new, ShmFileInode::new ...(+7) | unimplemented! |

#### 设备驱动
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `os/src/net/addr.rs#from_ipendpoint_to_socketaddr` | `os/src/net/addr.rs#from_ipendpoint_to_socketaddr` | 同板块同名同结构 | Ipv4Addr::from, Ipv6Addr::from, SocketAddr::V4, SocketAddr::V6, SocketAddrV4::new, SocketAddrV6::new | Ipv4Addr::from, Ipv6Addr::from, SocketAddr::V4, SocketAddr::V6, SocketAddrV4::new, SocketAddrV6::new |
| `os/src/net/socket.rs#set_nonblocking` | `os/src/net/socket.rs#set_nonblocking` | 同板块同名 | self.nonblock.store, tcp.set_nonblocking, udp.set_nonblocking, unix.set_nonblocking | tcp_socket.set_nonblocking, udp_socket.set_nonblocking |
| `os/src/arch/loongarch64/sbi.rs#console_getchar` | `os/src/arch/la64/sbi.rs#console_getchar` | 同板块同名 | core::ptr::read_volatile, mmio_addr | 1usize.wrapping_neg, UART.read |
| `os/src/arch/loongarch64/sbi.rs#console_putchar` | `os/src/arch/la64/sbi.rs#console_putchar` | 同板块同名 | core::ptr::read_volatile, core::ptr::write_volatile, mmio_addr | UART.write |

#### 网络协议栈
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `os/src/net/udp.rs#bind` | `os/src/net/udp.rs#bind` | 同板块同名 | (!is_unspecified(local_endpoint.addr)).then_some, Err, Ok, Some, bind_addr.port, bind_addr.set_port ...(+16) | (!is_unspecified(local_endpoint.addr)).then_some, SOCKET_SET.lock, SOCKET_SET.lock().get, SOCKET_SET.lock().get().unwrap, SOCKET_SET.lock().get().unwrap().bind_check, SOCKET_SET.lock().get_mut ...(+18) |
| `os/src/net/tcp.rs#recv` | `os/src/net/udp.rs#recv` | 同板块同名 | Err, Ok, self.block_on, self.handle.get, self.handle.get().read, self.handle.get().read().unwrap ...(+11) | Err, Ok, from_sockaddr_to_ipendpoint, is_unspecified, panic!, self.recv_impl ...(+4) |
| `os/src/net/udp.rs#send_impl` | `os/src/net/udp.rs#send_impl` | 同板块同名 | Err, IpEndpoint::new, Ok, buf.len, from_ipendpoint_to_socketaddr, get_ephemeral_port ...(+14) | Err, Ok, SOCKET_SET.lock, SOCKET_SET.lock().get_mut, SOCKET_SET.lock().get_mut().unwrap, SOCKET_SET.lock().get_mut().unwrap().with_socket_mut::<_,udp::Socket,_> ...(+12) |
| `os/src/net/tcp.rs#poll_connect` | `os/src/net/tcp.rs#poll_connect` | 同板块同名 | self.local_addr.get, self.local_addr.get().write, self.remote_addr.get, self.remote_addr.get().write, self.reset_shutdown_flags, self.set_state ...(+4) | SOCKET_SET.lock, SOCKET_SET.lock().get, SOCKET_SET.lock().get().unwrap, SOCKET_SET.lock().get().unwrap().with_socket::<_,tcp::Socket,_>, log::error!, self.loacl_addr.get ...(+5) |

#### 文件系统/第三方库
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `os/src/fs/ext4/inode.rs#create` | `os/src/ext4/mod.rs#create` | 同板块同名 | EXT4_OP_LOCK.lock, Err, Ext4File::new, Ext4InodeTypes::from, Ok, Self::child_path ...(+21) | Ext4Inode::new, Some, current_task, debug_assert!, dentry
            .flags
            .write, dentry
            .flags
            .write()
            .update_type_from_negative ...(+15) |
| `os/src/fs/ext4/inode.rs#unlink` | `os/src/ext4/mod.rs#unlink` | 同板块同名 | EXT4_OP_LOCK.lock, Err, Ext4File::new, Ok, child_inode.node_type, child_inode.node_type().into ...(+13) | Err, current_task, current_task().fsuid, dentry.get_inode, dentry.get_last_name, ext4_inode.get_nlinks ...(+11) |
| `os/src/fs/ext4/inode.rs#set_mode` | `os/src/ext4/inode.rs#set_mode` | 同板块同名 | CString::new, CString::new(path).map_err, EXT4_OP_LOCK.lock, Err, Ok, Self::map_lwext4_err ...(+9) | self.inner.write |
| `os/src/fs/ext4/inode.rs#truncate` | `os/src/ext4/inode.rs#truncate` | 同板块同名 | EXT4_OP_LOCK.lock, Ext4File::new, Ok, Self::now_timespec, Some, file.file_close ...(+9) | Err, Ok, log::warn!, self.get_flags, self.get_seals, self.get_size ...(+5) |

#### 通用模块/其他
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `os/src/main.rs#clear_bss` | `os/src/main.rs#clear_bss` | 同板块同名 | (a as *mut u8).write_volatile, (sbss as *const () as usize..ebss as *const () as usize)
        .for_each, core::arch::asm!, core::mem::size_of::<usize>, cur.wrapping_add | core::slice::from_raw_parts_mut, core::slice::from_raw_parts_mut(sbss as usize as *mut u8, ebss as usize - sbss as usize)
            .fill |
| `os/src/signal/sig_struct.rs#find_signal` | `os/src/signal/sig_struct copy.rs#find_signal` | 同板块同名 | Sig::SIGKILL.index, Sig::SIGSTOP.index, Sig::from, Some, self.mask.contain_signal, self.pending.bits ...(+1) | (self.pending & wanted_sigset).bits, Sig::SIGKILL.index, Sig::SIGSTOP.index, Sig::from, Some, self.mask.contain_signal ...(+1) |
| `os/src/loader.rs#get_app_data` | `os/src/loader.rs#get_app_data` | 同板块同名 | app_ptr.add, app_ptr.read_volatile, assert!, core::slice::from_raw_parts | assert!, core::slice::from_raw_parts, get_num_app, num_app_ptr.add |
| `os/src/console.rs#log` | `os/src/logging.rs#log` | 同板块同名 | LOG_SEQ.fetch_add, print_color | println!, record.level, record.metadata, self.enabled |


好的，仲裁官。基于提供的证据底座、控辩双方陈述及原始AST抽样数据，本庭现对“[2026-T202610422999620-XX] vs [2025-火箭队-哈尔滨工业大学]”同源性争议进行最终定性仲裁。

### 1. 宏观判定
- **定性结论**: **独立开发**
  双方项目在宏观层面呈现出极低的AST相似度（3.74%）和双向覆盖率（目标8.3%，参考6.36%），远低于构成代码抄袭或同源衍生的阈值。目标项目拥有1796个唯一AST指纹，参考项目拥有2341个，表明双方代码库主体均为独立构建。控方提出的“复用指控”所依赖的证据，在宏观统计上属于背景噪音，不具备证明力。

### 2. 全模块覆盖矩阵解读
基于全模块对比总览，对双方共有模块的解读如下：

- **进程管理**: 双方均存在。共同AST指纹极少（4个），覆盖率极低（目标1.62%，参考2.68%）。控方将大量网络、文件系统、同步原语等模块的函数强行归入此板块，构成证据硬凑。该模块证据强度为**弱**。
- **任务调度/异步运行时**: 双方均存在。存在一定共同指纹（21个），但覆盖率不高（目标7.47%，参考4.48%）。证据清单中的函数如`init_user_stack`、`sys_splice`等，其调用流存在差异，属于同类系统调用接口的不同实现。证据强度为**中**。
- **内存管理**: 双方均存在。共同指纹21个，覆盖率较低（目标5.11%，参考4.58%）。核心函数如`sys_mmap`、`sys_openat2`的外部调用对象和实现路径（如目标使用`MMAPFLAGS`，参考使用`MmapFlags`）存在显著差异。证据强度为**中**。
- **中断/异常处理**: 双方均存在。共同指纹3个，覆盖率相对较高但函数基数小。`cause`、`enable_timer_interrupt`等函数因硬件架构（LoongArch vs RISC-V/LA64）和寄存器操作方式不同，实现必然异构。证据强度为**弱**。
- **系统调用**: 双方均存在。共同指纹12个，覆盖率差异大（目标6.94%，参考15.79%）。用户态`syscall`函数因依赖`asm!`宏而结构相似，是平台强相关的标准写法。内核态多数syscall实现路径不同。证据强度为**弱**。
- **文件系统**: 双方均存在。共同指纹12个，覆盖率低（目标3.16%，参考2.48%）。`do_mount`、`readdir`等函数调用流差异巨大，如目标`do_mount`有复杂逻辑而参考直接返回`Ok`。证据强度为**弱**。
- **设备驱动**: 双方均存在。共同指纹5个，覆盖率差异大（目标16.67%，参考3.47%）。`console_putchar`等函数因操作MMIO或UART寄存器而相似，是底层硬件驱动的通用模式。证据强度为**弱**。
- **网络协议栈**: 双方均存在。共同指纹20个，覆盖率相对较高（目标16.53%，参考14.71%）。`bind`、`send_impl`等函数均围绕`smoltcp`库构建，相似性源于对同一第三方库的封装模式，而非代码复用。证据强度为**中**。
- **文件系统/第三方库**: 双方均存在。共同指纹极少（1个），覆盖率极低（目标1.49%，参考0.53%）。`ext4`相关函数调用流不同，属于对同一第三方库`lwext4`的不同封装。证据强度为**弱**。
- **通用模块/其他**: 双方均存在。共同指纹31个，覆盖率差异大（目标24.6%，参考10.65%）。`clear_bss`、`get_app_data`等函数属于内核启动和加载器的通用范式，但具体实现（如`write_volatile` vs `fill`）不同。证据强度为**弱**。

### 3. 底层 AST 关键证据指纹表
以下表格按模块优先级排序，列出控辩双方争议的核心证据指纹及其技术实质：

| 所属板块 | 目标项目路径 | 参考项目路径 | 证据类型 | 技术说明 |
| :--- | :--- | :--- | :--- | :--- |
| 网络协议栈 | `os/src/net/tcp.rs#get_tcp_keepidle` | `os/src/net/tcp.rs#get_tcp_keepidle` | 通用接口实现 | 实现POSIX标准`TCP_KEEPIDLE` socket选项的getter方法。`self.tcp_keepidle.load`是读取`AtomicU32`的唯一标准Rust写法，无独创性。 |
| 进程管理 | `os/src/task/task.rs#add_child` | `os/src/task/task.rs#add_child` | 通用数据结构操作 | 向受`Mutex`保护的子进程集合中插入新进程的标准操作。`lock` -> `insert` -> `tid`是此数据结构的必然调用序列。 |
| 进程管理 | `os/src/task/task.rs#set_parent` | `os/src/task/task.rs#set_parent` | 通用内存管理范式 | 设置父进程时使用`Arc::downgrade`创建`Weak`指针以避免循环引用，是Rust语言在类似场景下的强制最佳实践。 |
| 进程管理 | `os/src/mutex/spin.rs#wait_unlock` | `os/src/mutex/spin_mutex.rs#wait_unlock` | 通用同步原语 | 自旋锁的等待循环实现。`core::hint::spin_loop` + `AtomicBool::load`是Rust中实现自旋锁的最小化、教科书式写法。 |
| 进程管理 | `os/src/syscall/process.rs#sys_execve` | `os/src/syscall/task.rs#sys_execve` | 异构实现 | 同为`execve`系统调用入口，但参数解析和文件查找路径完全不同。目标用`File::new`、`copy_cstr_from_user`，参考用`Nameidata::new`、`c_str_to_string`。 |
| 进程管理 | `os/src/task/futex/wait.rs#futex_wait` | `os/src/futex/futex.rs#futex_wait` | 异构实现 | 同为futex等待操作，但内部机制完全不同。目标调用`futex_wait_timed_common`，参考调用`FUTEXQUEUES`、`FutexQ::new`。 |
| 进程管理 | `os/src/fs/proc/dirs.rs#as_any` | `os/src/fs/proc/fd.rs#as_any` | 标准Trait实现 | Rust语言中为支持动态类型转换而实现的`AsAny` trait。`fn as_any(&self) -> &dyn Any { self }`是社区唯一标准写法，不具备特异性。 |
| 任务调度 | `os/src/task/processor.rs#run_tasks` | `os/src/task/processor.rs#run_tasks` | 异构实现 | 调度器主循环。目标使用`IDLE_TASK`和`__switch`，参考使用`BOOT_TASK`、`time_stat`和多核hart管理，架构设计不同。 |
| 内存管理 | `os/src/syscall/mm.rs#sys_mmap` | `os/src/syscall/mm.rs#sys_mmap` | 异构实现 | `mmap`系统调用。目标使用`MMAPFLAGS`、`MMapProt`，参考使用`MmapFlags`、`MmapProt`，类型定义和标志位处理逻辑不同。 |
| 第三方库 | `os/src/fs/ext4/inode.rs#create` | `os/src/ext4/mod.rs#create` | 第三方库封装 | 双方均封装了`lwext4`库，但调用路径和辅助函数（如目标用`EXT4_OP_LOCK`，参考用`Ext4Inode::new`）不同，属于独立封装。 |

### 4. 核心代码调用流对比剖析
选取双方均存在且具代表性的核心系统调用路径进行深度析理：

* **【进程管理/execve系统调用】**
* **底层技术析理**: 双方均实现了`execve`系统调用，但采用了完全不同的文件查找和参数解析模型。目标项目采用“文件对象优先”模型，先通过`File::new`打开可执行文件，再通过`copy_cstr_from_user`逐个拷贝参数，最后调用`builtin_for_fs_exec`执行。参考项目则采用“路径名解析”模型，使用`Nameidata::new`进行路径查找，并通过`extract_cstrings`一次性提取所有参数。两者在内存管理、对象生命周期和错误处理路径上存在根本性架构差异。
* **代码溯源链接**:
  - 目标项目: `os/src/syscall/process.rs#sys_execve`
  - 参考项目: `os/src/syscall/task.rs#sys_execve`

* **【进程管理/futex等待操作】**
* **底层技术析理**: 双方均实现了`futex_wait`，但同步原语和等待队列的管理模型截然不同。目标项目采用基于超时和通用等待函数的模型，调用`futex_deadline_ms`和`futex_wait_timed_common`。参考项目则采用基于全局哈希桶和专用队列的模型，调用`FUTEXQUEUES.buckets[futex_hash(&key)].lock`和`FutexQ::new`。这体现了在锁粒度、调度策略上的不同设计选择。
* **代码溯源链接**:
  - 目标项目: `os/src/task/futex/wait.rs#futex_wait`
  - 参考项目: `os/src/futex/futex.rs#futex_wait`

* **【任务调度/主调度循环】**
* **底层技术析理**: 双方`run_tasks`函数是内核调度器的核心，但实现差异显著。目标项目使用`IDLE_TASK`作为空闲任务，通过`__switch`进行上下文切换，结构相对简洁。参考项目则支持多核（`PROCESSOR[hart_id]`），使用`BOOT_TASK`，并集成了时间统计（`time_stat`）和更复杂的任务获取逻辑（`crate::task::scheduler::fetch_task`）。两者在并发模型和性能监控维度上异构。
* **代码溯源链接**:
  - 目标项目: `os/src/task/processor.rs#run_tasks`
  - 参考项目: `os/src/task/processor.rs#run_tasks`

### 5. 单方模块与降权项
- **单方模块**: 无。所有宏观模块双方均存在。
- **降权项**:
  - **`as_any` 系列函数**: 属于Rust语言标准`AsAny` trait的实现，是类型向下转换的通用模式，在所有使用该模式的项目中必然出现，不具备任何特异性，已从主证据链中剔除。
  - **`get_tcp_keepidle` 等POSIX接口**: 实现标准POSIX/Linux接口的函数，其命名、参数和核心逻辑受规范强制约束，相似性源于接口标准而非代码复用。
  - **`syscall` (用户态)**: 用户态系统调用入口依赖`asm!`宏和平台ABI，写法高度固定，属于平台强相关代码。
  - **第三方库封装代码**: 如`ext4`相关函数，双方均封装了相同的第三方C库（`lwext4`），其相似性源于对同一底层库的FFI调用模式，不能作为项目间代码复用的证据。

### 6. 保守结论
综合宏观覆盖率（3.74%）、全模块矩阵分析及核心调用流对比，本庭裁定目标项目与参考项目为独立开发。双方代码库中存在的少量表层相似性，可完全归因于操作系统内核开发的领域共性（如遵循POSIX规范、Linux ABI）、Rust语言的强制惯用法（如`Arc::downgrade`、`Mutex::lock`）以及对相同第三方库（如`smoltcp`、`lwext4`）的独立封装。控方提出的“复用指控”建立在跨板块硬凑证据、将通用范式误判为独创表达以及对标准接口实现的无视之上，其证据链无法排除独立开发的可能性。整体抄袭风险极低，不构成代码复用事实。

------------------------------

