# 🛡️ OS 内核赛自动化代码级架构审计报告

**审计时间**: `2026-07-02 04:37:18`
**参赛作品名称**: `2026-T202610422999620-XX`
**年份**: `2026`
**学校**: `XX`
**队伍名称**: `T202610422999620`
**比赛名字**: `OS-Competition`
**比赛子赛道**: `Kernel-Implementation`
**仓库地址**: `https://gitlab.eduxiji.net/educg-group-43501-3132633/T202610422999620-3166`
**推测开发语言**: `Rust / C (由AST探针自动识别)`
**特征函数量**: `2581`

## ⚔️ 底层代码同源性对比 (AST Homology)
> 本部分由专门的代码审查智能体，对该项目与符合年份条件的历届最高相似度项目进行控制流与底层调用对比。新版报告会先给出确定性的全模块覆盖矩阵，再给出 Agent 的分模块解释，避免只围绕少量抽样函数展开。

### 🏆 Top 4 底层对标: vs `2024-chaos-北京科技大学`（北京科技大学 / chaos）

#### 对方作品信息

**对方年份**: `2024`
**对方学校**: `北京科技大学`
**对方队伍名称**: `chaos`
**对方比赛名字**: `OS-Competition`
**对方比赛子赛道**: `Kernel-Implementation`
**对方仓库地址**: `https://gitlab.eduxiji.net/educg-group-26010-2376550/oskernel2024-chaos`

> 双向覆盖率：共同 AST 指纹: 74；唯一 AST 指纹: 目标项目 1796，参考项目 798；覆盖率: 目标项目 4.12%，参考项目 9.27%

## 全模块对比总览（确定性证据）

> 总体覆盖率：共同 AST 指纹: 74；唯一 AST 指纹: 目标项目 1796，参考项目 798；覆盖率: 目标项目 4.12%，参考项目 9.27%

### 1. 模块覆盖矩阵
| 模块 | 目标函数数 | 参考函数数 | 共同唯一 AST 指纹 | Target 覆盖 | Base 覆盖 | 状态 |
| :--- | ---: | ---: | ---: | ---: | ---: | :--- |
| 进程管理 | 587 | 45 | 1 | 0.4% | 2.27% | 双方存在且有结构重合 |
| 任务调度/异步运行时 | 344 | 76 | 4 | 1.42% | 5.71% | 双方存在且有结构重合 |
| 内存管理 | 452 | 178 | 32 | 7.79% | 19.16% | 双方存在且有结构重合 |
| 中断/异常处理 | 21 | 5 | 1 | 5.0% | 25.0% | 双方存在且有结构重合 |
| 系统调用 | 209 | 67 | 8 | 4.62% | 20.0% | 双方存在且有结构重合 |
| 文件系统 | 558 | 152 | 1 | 0.26% | 0.79% | 双方存在且有结构重合 |
| 设备驱动 | 34 | 19 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |
| 网络协议栈 | 133 | 0 | 0 | 0.0% | 0.0% | 仅目标项目体现 |
| 文件系统/第三方库 | 69 | 270 | 1 | 1.49% | 0.44% | 双方存在且有结构重合 |
| 第三方库 | 0 | 50 | 0 | 0.0% | 0.0% | 仅参考项目体现 |
| 通用模块/其他 | 174 | 75 | 18 | 14.29% | 26.47% | 双方存在且有结构重合 |

### 2. 分模块证据清单

#### 进程管理
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `os/src/syscall/process.rs#sys_execve` | `os/src/syscall/process.rs#sys_execve` | 同板块同名 | Arc::new, Err, File::new, Ok, Vec::new, args_vec.as_slice ...(+18) | String::from, Vec::new, all_data.as_slice, args.add, args_vec.insert, args_vec.len ...(+22) |
| `os/src/syscall/signal.rs#sys_sigprocmask` | `os/src/syscall/signal.rs#sys_sigprocmask` | 同板块同名 | Err, Ok, copy_from_user, copy_to_user, core::mem::size_of::<SigSet>, core::mem::zeroed ...(+7) | SignalFlags::from_bits, SignalFlags::from_bits(new_set).unwrap, current_task, current_task().unwrap, file!, line! ...(+5) |
| `os/src/mutex/spin.rs#wait_unlock` | `os/src/sync/mutex/spin_mutex.rs#wait_unlock` | 同板块同名 | core::hint::spin_loop, panic!, self.lock.load | core::hint::spin_loop, error!, panic!, self.lock.load |
| `os/src/syscall/process.rs#sys_getppid` | `os/src/syscall/process.rs#sys_getppid` | 同板块同名 | Ok, current_task, current_task().expect, parent.as_ref, parent.as_ref().unwrap, parent.as_ref().unwrap().upgrade ...(+3) | current_task, current_task()
        .unwrap, current_task()
        .unwrap()
        .inner_exclusive_access, file!, line!, parent.upgrade ...(+3) |

#### 任务调度/异步运行时
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `os/src/syscall/time.rs#sys_clock_gettime` | `os/src/syscall/time.rs#sys_clock_gettime` | 同板块同名 | Err, Ok, copy_to_user, get_time_ms, realtime_us, timespec_from_us | ClockId::from, TimeSpec::now, debug!, panic!, sstatus::clear_sum, sstatus::set_sum ...(+1) |
| `os/src/syscall/fs.rs#sys_ioctl` | `os/src/syscall/fs.rs#sys_ioctl` | 同板块同名 | Err, Ok, copy_to_user, current_task, current_task().expect, device_ioctl ...(+8) | trace! |
| `os/src/task/scheduler.rs#fetch` | `os/src/task/manager.rs#fetch` | 同板块同名 | Some, self.idle_queue.pop_front, self.normal_bitmap.trailing_zeros, self.normal_queues[idx].is_empty, self.normal_queues[idx].pop_front, self.rt_bitmap.leading_zeros ...(+4) | self.ready_queue.is_empty, self.ready_queue.pop_front |
| `os/src/syscall/signal.rs#sys_sigaction` | `os/src/syscall/signal.rs#sys_sigaction` | 同板块同名 | Err, Ok, Sig::from, act.is_null, copy_from_user, copy_to_user ...(+12) | SignalFlags::from_bits, SignalFlags::from_bits(40).unwrap, check_sigaction_error, current_task, current_task().unwrap, error! ...(+8) |

#### 内存管理
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `os/src/syscall/fs.rs#sys_fcntl` | `os/src/syscall/fs.rs#sys_fcntl` | 同板块同名 | Err, LinuxFlock::default, Ok, OpenFlags::from, copy_from_user, copy_to_user ...(+32) | current_task, current_task().unwrap, debug!, file!, inner.alloc_fd, inner.fd_table.len ...(+6) |
| `os/src/mm/memory_set.rs#from_existed_user` | `os/src/mm/memory_set.rs#from_existed_user` | 同板块同名 | BTreeSet::new, Err, MapArea::from_another, Ok, Self::from_kernel_page_table, area.data_frames.contains_key ...(+21) | MapArea::from_another, Self::new_process, area.vpn_range.get_start, dst_ppn
                    .get_bytes_array, dst_ppn
                    .get_bytes_array()
                    .copy_from_slice, dst_ppn
                .get_bytes_array ...(+17) |
| `os/src/arch/rv64/trap/mod.rs#trap_handler` | `os/src/trap/mod.rs#trap_handler` | 同板块同名 | SigInfo::new, check_all_task_timers, current_task, current_task()
                    .expect, current_task()
                    .expect("[kernel] current task is None.")
                    .receive_siginfo, current_task()
                .expect ...(+17) | check_signals_of_current, check_timer, current_add_signal, current_task, current_task()
                .unwrap, current_task()
                .unwrap()
                .inner_exclusive_access ...(+24) |
| `os/src/mm/memory_set.rs#map_one` | `os/src/mm/memory_set.rs#map_one` | 同板块同名 | (backing.len - page_offset).min, Arc::new, Err, Ok, PTEFlags::from, PhysPageNum ...(+15) | PTEFlags::from_bits, PTEFlags::from_bits(self.map_perm.bits).unwrap, PhysPageNum, frame_alloc, frame_alloc().unwrap, page_table.map ...(+1) |

#### 中断/异常处理
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `os/src/arch/loongarch64/trap/mod.rs#enable_timer_interrupt` | `os/src/trap/mod.rs#enable_timer_interrupt` | 同板块同名 | ecfg::enable_timer_interrupt, super::register::crmd::set_interrupt_enabled | sie::set_stimer |
| `os/src/signal/sig_handler.rs#default` | `os/src/fs/pipe.rs#read` | 同模块上下文对齐/差异观察 | 内部闭环逻辑 | assert!, buf.into_iter, buf.len, buf_iter.next, debug!, drop ...(+11) |
| `os/src/arch/loongarch64/register/mod.rs#interrupt_from_index` | `os/src/trap/mod.rs#set_kernel_trap_entry` | 同模块上下文对齐/差异观察 | Interrupt::Hardware, Interrupt::Unknown | stvec::write |
| `os/src/arch/loongarch64/register/mod.rs#cause` | `os/src/trap/mod.rs#set_user_trap_entry` | 同模块上下文对齐/差异观察 | Trap::Exception, Trap::Interrupt, ecode, exception_from_ecode, interrupt_bits, interrupt_from_index ...(+1) | stvec::write |

#### 系统调用
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `user/src/syscall.rs#syscall` | `user/src/syscall.rs#syscall` | 同板块同名 | asm! | asm! |
| `user/src/lib.rs#waitpid` | `user/src/lib.rs#waitpid` | 同板块同名同结构 | sys_wait4, yield_ | sys_waitpid, yield_ |
| `user/src/lib.rs#wait` | `user/src/lib.rs#wait` | 同板块同名同结构 | sys_wait4, yield_ | sys_waitpid, yield_ |
| `user/src/syscall.rs#sys_read` | `user/src/syscall.rs#sys_read` | 同板块同名 | buf.as_mut_ptr, buf.len, syscall | buffer.as_mut_ptr, buffer.len, syscall |

#### 文件系统
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `os/src/fs/dev/mod.rs#lookup` | `os/src/fs/fat32/inode.rs#lookup` | 同板块同名 | Arc::new, Err, LoopInode::new, Ok, RandomInode::random, RandomInode::urandom ...(+2) | Arc::clone, Arc::new, Dentry::new, Some, dentry.is_dir, dentry.is_file ...(+6) |
| `os/src/fs/dev/shm.rs#unlink` | `os/src/fs/fat32/inode.rs#unlink` | 同板块同名 | Err, Ok, dir.entries.lock, dir.entries.lock().is_empty, entries.get, entries.get(name).cloned ...(+11) | dentry.name, fs.fat.cluster_id_to_sector_id, fs.fat.cluster_id_to_sector_id(self.start_cluster).unwrap, fs.get_dentry, fs.remove_dentry, self.fs.as_ref |
| `os/src/fs/dev/shm.rs#write_at` | `os/src/fs/fat32/inode.rs#write_at` | 同板块同名 | Ok, buf.len, capacity.saturating_mul, data.capacity, data.len, data.resize ...(+8) | buf.len, cluster_buf[pos % CLUSTER_SIZE..pos % CLUSTER_SIZE + copy_size]
                .copy_from_slice, fs.cluster_chain, fs.read_cluster, fs.write_cluster, min ...(+2) |
| `os/src/fs/dev/random.rs#read_at` | `os/src/fs/fat32/inode.rs#read_at` | 同板块同名 | (idx + 1usize).wrapping_mul, Ok, buf.iter_mut, buf.iter_mut().enumerate, buf.len, crate::timer::get_time_ms | buf.len, buf[read_size..read_size + copy_size]
                .copy_from_slice, dentry.file_size, fs.cluster_chain, fs.read_cluster, min ...(+3) |

#### 设备驱动
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `os/src/drivers/virtio/mod.rs#unshare` | `os/src/drivers/block/virtio_blk.rs#unshare` | 同板块同名 | 内部闭环逻辑 | 内部闭环逻辑 |
| `os/src/drivers/virtio/block_dev.rs#read_block` | `os/src/drivers/block/virtio_blk.rs#read_block` | 同板块同名 | self.inner
            .lock, self.inner
            .lock()
            .read_blocks, self.inner
            .lock()
            .read_blocks(block_id as _, buf)
            .map_err | error!, panic!, res.is_err, res.unwrap, self.0.lock, self.0.lock().read_blocks ...(+1) |
| `os/src/drivers/virtio/block_dev.rs#write_block` | `os/src/drivers/block/virtio_blk.rs#write_block` | 同板块同名 | self.inner
            .lock, self.inner
            .lock()
            .write_blocks, self.inner
            .lock()
            .write_blocks(block_id as _, buf)
            .map_err | debug!, self.0
            .lock, self.0
            .lock()
            .write_blocks, self.0
            .lock()
            .write_blocks(block_id, buf)
            .expect |
| `os/src/drivers/virtio/mod.rs#mmio_phys_to_virt` | `os/src/drivers/block/virtio_blk.rs#mmio_phys_to_virt` | 同板块同名 | NonNull::new, NonNull::new(va as *mut u8).unwrap | KernelAddr::from, NonNull::new_unchecked, PhysAddr::from |

#### 网络协议栈
仅目标项目中观察到该模块代表函数，参考项目没有同模块可对齐证据：
- 目标项目：`os/src/net/socket.rs#accept`，调用摘要：AtomicBool::new, AtomicU64::new, Err, Mutex::new, Ok, SocketInner::Tcp ...(+7)
- 目标项目：`os/src/net/tcp.rs#listen`，调用摘要：Err, LISTEN_TABLE.lock, LISTEN_TABLE.lock().listen, Ok, SocketSetWrapper::new_tcp_socket, existing_handle.is_none ...(+16)
- 目标项目：`os/src/net/tcp.rs#close_all`，调用摘要：LISTEN_TABLE.lock, LISTEN_TABLE.lock().unlisten, Ok, self.handle.get, self.handle.get().read, self.handle.get().read().unwrap ...(+12)
- 目标项目：`os/src/net/socket.rs#new`，调用摘要：AtomicBool::new, AtomicU64::new, Err, Mutex::new, Ok, SocketInner::Tcp ...(+5)

#### 文件系统/第三方库
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `os/src/fs/ext4/inode.rs#create` | `os/src/fs/ext4/inode.rs#create` | 同板块同名 | EXT4_OP_LOCK.lock, Err, Ext4File::new, Ext4InodeTypes::from, Ok, Self::child_path ...(+21) | todo! |
| `os/src/fs/ext4/inode.rs#unlink` | `os/src/fs/ext4/inode.rs#unlink` | 同板块同名 | EXT4_OP_LOCK.lock, Err, Ext4File::new, Ok, child_inode.node_type, child_inode.node_type().into ...(+13) | self.fs.ext4.ext4_file_remove, self.fs.ext4.ext4_file_remove(self.ino, name).is_ok |
| `os/src/fs/ext4/inode.rs#write_at` | `os/src/fs/ext4/inode.rs#write_at` | 同板块同名 | EXT4_OP_LOCK.lock, Ext4File::new, Ok, Self::now_timespec, Some, file.file_close ...(+14) | Arc::downgrade, Ext4File::new, Ext4InodeRef::get_inode_ref, buf.len, inode_ref.inner.inode.inode_get_size, self.fs.ext4.ext4_file_write |
| `os/src/fs/ext4/inode.rs#read_at` | `os/src/fs/ext4/inode.rs#read_at` | 同板块同名 | EXT4_OP_LOCK.lock, Ext4File::new, Ok, file.file_close, file.file_close().map_err, file.file_open ...(+7) | Ext4File::new, buf.len, self
            .fs
            .ext4
            .ext4_file_read |

#### 第三方库
仅参考项目中观察到该模块代表函数，目标项目没有同模块可对齐证据：
- 参考项目：`os/libs/visionfive2-sd/src/lib.rs#send_cmd`，调用摘要：RawInterrupt::from, RawInterruptStatusReg::from, S::sleep_ms_until, Some, arg.into, assert! ...(+21)
- 参考项目：`os/libs/visionfive2-sd/src/lib.rs#init_sdcard`，调用摘要：CardTypeReg::from, CardTypeReg::from(0).with_card_width4_1, CmdArg::new, CmdReg::from, ControlReg::from, RawInterruptStatusReg::from ...(+18)
- 参考项目：`os/libs/visionfive2-sd/src/register.rs#from`，调用摘要：CmdReg::with_data, CmdReg::with_data(0, value.into()).with_transfer_dir, CmdReg::with_no_data, CmdReg::with_no_data(0, value.into())
                    .with_check_response_crc, CmdReg::with_no_data(0, value.into())
                    .with_check_response_crc(false)
                    .with_response_length, CmdReg::with_no_data(0, value.into()).with_check_response_crc ...(+3)
- 参考项目：`os/libs/visionfive2-sd/src/lib.rs#reset_clock`，调用摘要：ClockDividerReg::new, ClockDividerReg::new().with_clk_divider0, ClockEnableReg::from, CmdArg::new, CmdReg::from, CmdReg::from(0)
        .with_start_cmd ...(+7)

#### 通用模块/其他
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `os/src/main.rs#clear_bss` | `os/src/main.rs#clear_bss` | 同板块同名 | (a as *mut u8).write_volatile, (sbss as *const () as usize..ebss as *const () as usize)
        .for_each, core::arch::asm!, core::mem::size_of::<usize>, cur.wrapping_add | core::slice::from_raw_parts_mut, core::slice::from_raw_parts_mut(sbss as usize as *mut u8, ebss as usize - sbss as usize)
            .fill |
| `os/src/arch/loongarch64/sbi.rs#shutdown` | `os/src/sbi.rs#shutdown` | 同板块同名 | core::ptr::write_volatile, mmio_addr, register::idle | panic!, sbi_call |
| `os/src/mutex/spin.rs#lock` | `os/src/sync/mutex/spin_mutex.rs#lock` | 同板块同名 | S::before_lock, self
                .lock
                .compare_exchange, self
                .lock
                .compare_exchange(false, true, Ordering::Acquire, Ordering::Relaxed)
                .is_ok, self.wait_unlock | S::before_lock, self
                .lock
                .compare_exchange, self
                .lock
                .compare_exchange(false, true, Ordering::Acquire, Ordering::Relaxed)
                .is_ok, self.wait_unlock |
| `os/src/arch/rv64/sbi.rs#shutdown` | `os/src/boards/qemu.rs#shutdown` | 同板块同名 | system_reset, unreachable! | QEMU_EXIT_HANDLE.exit_success |


好的，仲裁官。基于您提供的全案材料，我将严格遵循审计指令，对目标项目 `[2026-T202610422999620-XX]` 与参考项目 `[2024-chaos-北京科技大学]` 进行最终的同源性定性仲裁。

### 1. 宏观判定
- **定性结论**: **独立开发，底层架构显著差异**
- **核心依据**: 客观 AST 相似度仅为 **2.94%**。目标项目 1796 个唯一 AST 指纹中，仅有 74 个与参考项目重合，覆盖率低至 **4.12%**。该重合度处于统计噪声水平，主要由 Rust 语言惯用法、Linux ABI 强制命名及通用数据结构操作导致，无法证明存在系统性代码复用或架构抄袭。

### 2. 全模块覆盖矩阵解读
| 模块 | 双方存在 | 共同指纹 | 覆盖率 (目标/参考) | 证据强度 | 解读 |
| :--- | :---: | :---: | :---: | :---: | :--- |
| **进程管理** | 是 | 1 | 0.4% / 2.27% | **弱** | 目标项目体量（587个函数）远超参考项目（45个），唯一的共同指纹是 `self.current.take` 这类单行语言惯用法。控方“铁证”不成立。 |
| **任务调度/异步运行时** | 是 | 4 | 1.42% / 5.71% | **弱** | 共同指纹集中在 `sys_clock_gettime`、`sys_ioctl` 等系统调用入口，其内部实现（如调度器 `fetch` 逻辑）已出现 bitmap vs 队列的根本性架构差异。 |
| **内存管理** | 是 | 32 | 7.79% / 19.16% | **中** | 覆盖率相对较高，但共同指纹多源于 `sys_fcntl`、`map_one`、`from_existed_user` 等标准 POSIX 接口或通用内存映射操作。双方在内存集抽象（`MemorySet`）和页表操作上存在独立设计。 |
| **中断/异常处理** | 是 | 1 | 5.0% / 25.0% | **弱** | 参考项目函数基数小（5个），导致覆盖率虚高。共同指纹 `enable_timer_interrupt` 是架构相关的必选操作。`trap_handler` 的核心信号处理路径已出现 `SigInfo` vs `SignalFlags` 的显著差异。 |
| **系统调用** | 是 | 8 | 4.62% / 20.0% | **弱** | 共同指纹集中在用户库（`user/src`）的 `syscall` 封装和 `waitpid`/`wait` 等 POSIX 标准接口，其内部 `asm!` 宏和循环等待结构是固定范式。 |
| **文件系统** | 是 | 1 | 0.26% / 0.79% | **弱** | 双方虽有同名 `lookup`/`unlink` 等函数，但底层文件系统实现完全不同（目标为 ext4/procfs/dev，参考为 fat32），共同指纹极少，架构异构性显著。 |
| **设备驱动** | 是 | 0 | 0.0% / 0.0% | **未形成** | 双方均存在该模块，但未观察到任何共同 AST 指纹，表明驱动模型和硬件交互逻辑完全独立。 |
| **网络协议栈** | 仅目标 | 0 | 0.0% / 0.0% | **未形成** | 目标项目独有的完整模块，是独立开发的有力证据。 |
| **第三方库/公共依赖** | 部分 | 1 | 1.49% / 0.44% | **弱** | 共同指纹在 ext4 的 `create`/`unlink` 等函数，但参考项目仅为 `todo!` 占位符，目标项目为完整实现，不构成复用。参考项目独有的 `visionfive2-sd` 库也证明双方硬件平台和依赖不同。 |

### 3. 底层 AST 关键证据指纹表
| 所属板块 | 目标项目路径 | 参考项目路径 | 证据类型 | 技术说明 |
| :--- | :--- | :--- | :--- | :--- |
| **进程管理** | `os/src/task/processor.rs#take_current` | `os/src/task/processor.rs#take_current` | 同板块同名 | 均为 `self.current.take()`。这是 Rust `Option` 类型的标准取值方法，属于语言惯用法，非创造性逻辑。 |
| **进程管理** | `os/src/task/processor.rs#current_user_token` | `os/src/task/processor.rs#current_user_token` | 同板块同名 | 调用链均为 `current_task().unwrap().get_user_token()`。这是获取当前进程页表基址的标准操作序列，由硬件机制决定。 |
| **进程管理** | `os/src/mutex/spin.rs#wait_unlock` | `os/src/sync/mutex/spin_mutex.rs#wait_unlock` | 同板块同名 | 核心逻辑均为 `while self.lock.load() { spin_loop(); }`。这是自旋锁的标准实现范式，差异仅在日志宏 (`panic!` vs `error!`)。 |
| **任务调度** | `os/src/syscall/time.rs#sys_clock_gettime` | `os/src/syscall/time.rs#sys_clock_gettime` | 同板块同名 | 均为系统调用入口，但目标项目调用 `get_time_ms`，参考项目调用 `TimeSpec::now`，底层时间获取机制不同。 |
| **内存管理** | `os/src/mm/memory_set.rs#map_one` | `os/src/mm/memory_set.rs#map_one` | 同板块同名 | 均执行“分配物理页 → 构建页表项 → 映射”操作，这是 MMU 编程的固定流程。具体实现如 `PTEFlags::from` vs `PTEFlags::from_bits` 存在差异。 |
| **内存管理** | `os/src/mm/memory_set.rs#from_existed_user` | `os/src/mm/memory_set.rs#from_existed_user` | 同板块同名 | 均实现进程地址空间的复制（fork 时），涉及遍历页表、复制物理页等标准步骤，但内部数据结构 (`BTreeSet` vs 直接复制) 不同。 |
| **系统调用** | `user/src/lib.rs#waitpid` | `user/src/lib.rs#waitpid` | 同板块同名同结构 | 用户态库函数，内部循环调用 `sys_wait4`/`sys_waitpid` 并 `yield_`。这是 POSIX `waitpid` 的经典忙等实现模式。 |
| **文件系统** | `os/src/fs/ext4/inode.rs#create` | `os/src/fs/ext4/inode.rs#create` | 同板块同名 | 目标项目为完整 ext4 实现，参考项目为 `todo!()` 占位符。同名但实现程度天差地别，不构成复用证据。 |
| **第三方库** | `os/src/fs/ext4/inode.rs#read_at` | `os/src/fs/ext4/inode.rs#read_at` | 同板块同名 | 同上，目标项目有完整的锁管理和文件操作，参考项目仅为简单的转发调用，实现路径不同。 |

### 4. 核心代码调用流对比剖析
*   **【进程管理/任务退出路径】**
    *   **底层技术析理**: 双方均实现了 `sys_exit` 系统调用，但进程销毁的底层调度策略存在差异。目标项目调用 `exit_and_run_next`，暗示其可能在退出函数内直接触发调度；参考项目调用 `exit_current_and_run_next`，语义上更强调“当前”任务的退出。更重要的是，目标项目实现了独立的 `sys_exit_group`，调用 `exit_group_and_run_next`，表明其支持完整的 POSIX 线程组退出语义，而参考项目则复用 `exit_current_and_run_next`。这体现了目标项目在进程生命周期管理上的独立设计。
    *   **代码溯源链接**:
        - 目标项目: `os/src/syscall/process.rs#sys_exit`
        - 参考项目: `os/src/syscall/process.rs#sys_exit`

*   **【内存管理/内存映射】**
    *   **底层技术析理**: 在 `sys_mmap` 的实现路径上，双方均依赖进程的内存集抽象。然而，目标项目的调用流为 `current_task().expect(...).mmap(...)`，而参考项目为 `task.inner_exclusive_access(file!(), line!()).mmap(...)`。这揭示了两个关键架构差异：1) **同步原语**：目标项目使用标准的 `Mutex`（通过 `expect` 解包），而参考项目使用带调试信息的 `exclusive_access`，表明其锁机制和调试策略独立；2) **对象模型**：目标项目直接访问任务控制块的 `mmap` 方法，参考项目则通过 `inner_exclusive_access` 获取内部可变引用后再操作，封装层次不同。
    *   **代码溯源链接**:
        - 目标项目: `os/src/syscall/process.rs#sys_mmap`
        - 参考项目: `os/src/syscall/process.rs#sys_mmap`

*   **【中断/异常处理/Trap 处理】**
    *   **底层技术析理**: 在核心的 `trap_handler` 中，双方都处理了用户态中断并检查信号/定时器。但目标项目的调用流涉及 `SigInfo::new`、`check_all_task_timers` 和 `receive_siginfo`，表明其拥有一套独立的信号信息对象模型和定时器管理机制。参考项目则使用 `check_signals_of_current`、`check_timer` 和 `current_add_signal`，其信号和定时器处理逻辑被整合在任务内部操作中。两者在信号处理的抽象层次和对象模型上存在根本不同。
    *   **代码溯源链接**:
        - 目标项目: `os/src/arch/rv64/trap/mod.rs#trap_handler`
        - 参考项目: `os/src/trap/mod.rs#trap_handler`

### 5. 单方模块与降权项
*   **目标项目独有模块/功能**：**网络协议栈**（`os/src/net/`）、**procfs**（`os/src/fs/proc/`）、**futex**（`os/src/task/futex/`）、**capabilities**（`sys_capset`）、**shebang 解析**（`shebang_args`）等。这些是目标项目独立设计与实现的有力证据，其工作量远超所谓的“复用”部分。
*   **参考项目独有模块/功能**：**第三方库** `visionfive2-sd`（`os/libs/visionfive2-sd/`），表明其硬件平台（VisionFive 2）与目标项目（支持 LoongArch64/RISC-V）不同。
*   **降权项说明**：
    *   **Linux ABI 强制命名**：`sys_exit`、`sys_getpid`、`sys_execve` 等系统调用名称为兼容 Linux 的必选项，同名不能作为抄袭证据。
    *   **Rust 语言惯用法与标准库**：`self.current.take()`、`Vec::new()`、`String::from()`、`asm!`、`println!` 等是任何 Rust 内核都会产生的 AST 指纹，不具备项目特异性。
    *   **跨板块同名**：如目标项目的 `os/src/fs/dev/mod.rs#lookup` 与参考项目的 `os/src/fs/fat32/inode.rs#lookup`，虽同名但分属不同文件系统实现，不构成同板块主证据。

### 6. 保守结论
本次仲裁的客观 AST 相似度仅为 2.94%，远低于可判定为“抄袭”或“架构复用”的阈值。控方提出的“铁证”均被证实为 Rust 语言惯用法或操作系统领域的标准实现范式，其指控无法覆盖目标项目庞大的独立代码体量。双方在进程模型、内存管理、文件系统、设备驱动及同步原语等核心子系统上均表现出显著的架构差异。特别是目标项目独有的网络协议栈、procfs 等复杂模块，以及参考项目独有的特定硬件驱动，共同指向了两个独立演进的项目。因此，综合判定目标项目 `[2026-T202610422999620-XX]` 为独立开发，复用指控不成立。

------------------------------

