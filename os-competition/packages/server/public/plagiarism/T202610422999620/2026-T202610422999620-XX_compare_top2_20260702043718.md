# 🛡️ OS 内核赛自动化代码级架构审计报告

**审计时间**: `2026-07-02 04:37:18`
**参赛作品名称**: `2026-T202610422999620-XX`
**年份**: `2026`
**学校**: `XX`
**队伍名称**: `T202610422999620`
**比赛名字**: `OS-Competition`
**比赛子赛道**: `Kernel-Implementation`
**仓库地址**: `https://gitlab.eduxiji.net/educg-group-43501-3132633/T202610422999620-3166`
**推测开发语言**: `Rust / C (由AST探针自动识别)`
**特征函数量**: `2581`

## ⚔️ 底层代码同源性对比 (AST Homology)
> 本部分由专门的代码审查智能体，对该项目与符合年份条件的历届最高相似度项目进行控制流与底层调用对比。新版报告会先给出确定性的全模块覆盖矩阵，再给出 Agent 的分模块解释，避免只围绕少量抽样函数展开。

### 🏆 Top 2 底层对标: vs `2025-Del0n1x-哈尔滨工业大学深圳`（哈尔滨工业大学（深圳） / Del0n1x）

#### 对方作品信息

**对方年份**: `2025`
**对方学校**: `哈尔滨工业大学（深圳）`
**对方队伍名称**: `Del0n1x`
**对方比赛名字**: `OS-Competition`
**对方比赛子赛道**: `Kernel-Implementation`
**对方仓库地址**: `https://gitlab.eduxiji.net/educg-group-36002-2710490/T202518123995600-2523`

> 双向覆盖率：共同 AST 指纹: 109；唯一 AST 指纹: 目标项目 1796，参考项目 1681；覆盖率: 目标项目 6.07%，参考项目 6.48%

## 全模块对比总览（确定性证据）

> 总体覆盖率：共同 AST 指纹: 109；唯一 AST 指纹: 目标项目 1796，参考项目 1681；覆盖率: 目标项目 6.07%，参考项目 6.48%

### 1. 模块覆盖矩阵
| 模块 | 目标函数数 | 参考函数数 | 共同唯一 AST 指纹 | Target 覆盖 | Base 覆盖 | 状态 |
| :--- | ---: | ---: | ---: | ---: | ---: | :--- |
| 进程管理 | 587 | 239 | 7 | 2.83% | 3.85% | 双方存在且有结构重合 |
| 任务调度/异步运行时 | 344 | 254 | 12 | 4.27% | 5.33% | 双方存在且有结构重合 |
| 内存管理 | 452 | 396 | 23 | 5.6% | 6.93% | 双方存在且有结构重合 |
| 中断/异常处理 | 21 | 113 | 2 | 10.0% | 2.44% | 双方存在且有结构重合 |
| 系统调用 | 209 | 97 | 15 | 8.67% | 20.0% | 双方存在且有结构重合 |
| 文件系统 | 558 | 496 | 6 | 1.58% | 1.63% | 双方存在且有结构重合 |
| 设备驱动 | 34 | 155 | 10 | 33.33% | 7.58% | 双方存在且有结构重合 |
| 网络协议栈 | 133 | 88 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |
| 文件系统/第三方库 | 69 | 44 | 1 | 1.49% | 2.33% | 双方存在且有结构重合 |
| 通用模块/其他 | 174 | 265 | 23 | 18.25% | 10.0% | 双方存在且有结构重合 |

### 2. 分模块证据清单

#### 进程管理
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `os/src/syscall/process.rs#sys_wait4` | `os/src/syscall/process.rs#sys_wait4` | 同板块同名 | (matches_child(*child_tid, child) && child.is_exited())
                        .then, (pid > 0).then_some, Err, Ok, RUsage::default, Some ...(+37) | Err, Ok, Some, WaitOptions::from_bits, WaitOptions::from_bits(options as i32).ok_or, break ...(+36) |
| `os/src/syscall/process.rs#sys_setpgid` | `os/src/syscall/process.rs#sys_setpgid` | 同板块同名 | Err, Ok, TASK_MANAGER.for_each, TASK_MANAGER.get, TASK_MANAGER.get(pid).ok_or, children.contains_key ...(+15) | Err, Ok, current_task, current_task().unwrap, extract_proc_to_new_group, get_target_proc_group ...(+9) |
| `os/src/syscall/signal.rs#sys_sigprocmask` | `os/src/syscall/process.rs#sys_sigprocmask` | 同板块同名 | Err, Ok, copy_from_user, copy_to_user, core::mem::size_of::<SigSet>, core::mem::zeroed ...(+7) | Err, Ok, core::ptr::read, core::ptr::write, current_task, current_task().unwrap ...(+8) |
| `os/src/syscall/process.rs#sys_prlimit64` | `os/src/syscall/process.rs#sys_prlimit64` | 同板块同名 | Err, Ok, TASK_MANAGER.get, TASK_MANAGER.get(pid).ok_or, copy_from_user, copy_to_user ...(+11) | Err, Ok, RLimit64::new, RLimit64::new_bare, RlimResource::try_from_primitive, RlimResource::try_from_primitive(resource).map_err ...(+10) |

#### 任务调度/异步运行时
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `os/src/syscall/fs.rs#sys_splice` | `os/src/syscall/fs.rs#sys_splice` | 同板块同名 | Err, current_task, current_task().expect, explicit_in_offset.is_some, explicit_out_offset.is_some, in_entry.file.clone ...(+24) | Err, Ok, core::ptr::write, current_task, current_task().unwrap, debug_point! ...(+24) |
| `os/src/syscall/fs.rs#sys_utimensat` | `os/src/syscall/fs.rs#sys_utimensat` | 同板块同名 | Err, Ok, atime.is_none, copy_cstr_from_user, current_task, current_task().expect ...(+16) | CLOCK_MANAGER.lock, CLOCK_MANAGER.lock().get, CLOCK_MANAGER.lock().get(0).unwrap, Ok, OpenFlags::from_bits, OpenFlags::from_bits(flags).ok_or ...(+24) |
| `os/src/syscall/fs.rs#sys_getdents64` | `os/src/syscall/fs.rs#sys_getdents64` | 同板块同名 | Err, Ok, copy_to_user, current_task, current_task().expect, dirent.copy_to_buffer ...(+17) | Err, Ok, current_task, current_task().unwrap, file.metadata, file.metadata().inode.metadata ...(+6) |
| `os/src/syscall/fs.rs#sys_faccessat` | `os/src/syscall/fs.rs#sys_faccessat` | 同板块同名 | Err, Ok, access_mode_allowed, copy_cstr_from_user, current_task, current_task().expect ...(+13) | AbsPath::new, Err, FaccessatMode::from_bits, FaccessatMode::from_bits(mode).ok_or, Ok, abs.get_parent_abs ...(+29) |

#### 内存管理
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `os/src/syscall/mod.rs#syscall` | `os/src/syscall/mod.rs#syscall` | 同板块同名 | Err, merge_offset_arg, sys_accept, sys_accept4, sys_adjtimex, sys_bind ...(+211) | Ok, SysCode::from, sys_accept, sys_accept4, sys_bind, sys_brk ...(+121) |
| `os/src/syscall/fs.rs#sys_fcntl` | `os/src/syscall/fs.rs#sys_fcntl` | 同板块同名 | Err, LinuxFlock::default, Ok, OpenFlags::from, copy_from_user, copy_to_user ...(+32) | Err, FcntlArgFlags::FD_CLOEXEC.bits, FcntlFlags::from_bits, FcntlFlags::from_bits(cmd).ok_or, FdInfo::new, Ok ...(+26) |
| `os/src/syscall/mm.rs#sys_mmap` | `os/src/syscall/mm.rs#sys_mmap` | 同板块同名 | Err, MMAPFLAGS::MAP_SHARED_VALIDATE.bits, MMAPFLAGS::all, MMAPFLAGS::all().bits, MMAPFLAGS::from_bits_truncate, MMapProt::from_bits ...(+18) | (addr + length).into, Err, MapPerm::from, MmapFlags::from_bits_truncate, MmapProt::from_bits_truncate, Ok ...(+20) |
| `os/src/fs/page_cache.rs#write_at` | `os/src/fs/tmp/inode.rs#write_at` | 同板块同名 | (buf.len() - copied).min, Arc::new, Mutex::new, Ok, PAGE_CACHE_DIRTY_PAGE_COUNT.fetch_add, PAGE_CACHE_PAGE_COUNT.fetch_add ...(+19) | cache.write, info!, self.write_directly |

#### 中断/异常处理
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `os/src/arch/loongarch64/trap/mod.rs#enable_timer_interrupt` | `os/src/sync/interrupt.rs#enable_timer_interrupt` | 同板块同名 | ecfg::enable_timer_interrupt, super::register::crmd::set_interrupt_enabled | crmd::set_ie, ecfg::set_lie |
| `os/src/arch/loongarch64/register/mod.rs#enable_timer_interrupt` | `os/src/sync/interrupt.rs#enable_timer_interrupt` | 同板块同名 | read, write | sie::set_stimer |
| `os/src/arch/loongarch64/interrupt.rs#drop` | `os/src/hal/la64/arch/interrupt.rs#drop` | 同板块同名同结构 | set_ie | enable_supervisor_interrupt |
| `os/src/arch/rv64/trap/mod.rs#enable_timer_interrupt` | `os/src/hal/rv64/arch/interrupt.rs#disable_supervisor_interrupt` | 同板块同结构 | sie::set_stimer | sstatus::clear_sie |

#### 系统调用
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `user/src/syscall.rs#syscall` | `user/src/syscall.rs#syscall` | 同板块同名 | asm! | asm! |
| `os/src/syscall/time.rs#sys_gettimeofday` | `user/src/syscall.rs#sys_gettimeofday` | 同板块同名 | Ok, copy_to_user, realtime_us, tv.is_null, tz.is_null | syscall, ts.as_ptr |
| `os/src/syscall/net.rs#sys_recvfrom` | `user/src/syscall.rs#sys_recvfrom` | 同板块同名 | Ok, copy_to_user, kernel_buf.as_ptr, n.min, sock.domain.clone, sock.recv_from ...(+3) | syscall |
| `os/src/syscall/net.rs#sys_sendto` | `user/src/syscall.rs#sys_sendto` | 同板块同名 | copy_from_user, kernel_buf.as_mut_ptr, read_sockaddr_for_domain, sock.send_to, sock.write, vec! ...(+1) | syscall |

#### 文件系统
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `os/src/drivers/disk.rs#read_one` | `os/src/drivers/disk.rs#read_one` | 同板块同名 | Ok, buf.len, buf.len().min, buf[..count].copy_from_slice, self.dev
                .read_block, self.dev.read_block | Ok, buf.len, buf.len().min, buf[..count].copy_from_slice, info!, self.dev
                .read_block ...(+1) |
| `os/src/drivers/disk.rs#write_one` | `os/src/drivers/disk.rs#write_one` | 同板块同名同结构 | Ok, buf.len, buf.len().min, data[start..start + count].copy_from_slice, self.dev.read_block, self.dev.write_block | Ok, buf.len, buf.len().min, data[start..start + count].copy_from_slice, self.dev.read_block, self.dev.write_block |
| `os/src/fs/dev/mod.rs#ioctl` | `os/src/fs/devfs/tty.rs#ioctl` | 同板块同名 | Err, Ok, crate::mm::copy_to_user | Err, Ok, TtyIoctlCmd::try_from, TtyIoctlCmd::try_from(op).map_err, arg.into, info! ...(+7) |
| `os/src/fs/dev/shm.rs#unlink` | `os/src/fs/vfs/inode.rs#unlink` | 同板块同名 | Err, Ok, dir.entries.lock, dir.entries.lock().is_empty, entries.get, entries.get(name).cloned ...(+11) | Err |

#### 设备驱动
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `os/src/drivers/virtio/block_dev.rs#as_dev_err` | `os/src/drivers/virtio_driver/mod.rs#as_dev_err` | 同板块同名同结构 | 内部闭环逻辑 | 内部闭环逻辑 |
| `os/src/arch/loongarch64/sbi.rs#console_getchar` | `os/src/hal/la64/arch/mod.rs#console_getchar` | 同板块同名 | core::ptr::read_volatile, mmio_addr | uart::uart_get |
| `os/src/arch/loongarch64/sbi.rs#console_putchar` | `os/src/hal/la64/arch/mod.rs#console_putchar` | 同板块同名 | core::ptr::read_volatile, core::ptr::write_volatile, mmio_addr | uart::uart_put |
| `os/src/drivers/virtio/mod.rs#unshare` | `os/src/drivers/virtio_driver/mod.rs#unshare` | 同板块同名同结构 | 内部闭环逻辑 | 内部闭环逻辑 |

#### 网络协议栈
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `os/src/net/socket.rs#accept` | `os/src/net/udp.rs#accept` | 同板块同名 | AtomicBool::new, AtomicU64::new, Err, Mutex::new, Ok, SocketInner::Tcp ...(+7) | Err |
| `os/src/net/tcp.rs#listen` | `os/src/net/tcp.rs#listen` | 同板块同名 | Err, LISTEN_TABLE.lock, LISTEN_TABLE.lock().listen, Ok, SocketSetWrapper::new_tcp_socket, existing_handle.is_none ...(+16) | Err, Ok, info!, self.set_state, self.sockmeta.lock, self.with_socket ...(+5) |
| `os/src/net/udp.rs#bind` | `os/src/net/tcp.rs#bind` | 同板块同名 | (!is_unspecified(local_endpoint.addr)).then_some, Err, Ok, Some, bind_addr.port, bind_addr.set_port ...(+16) | Err, IpEndpoint::try_from, Ok, addr.clone, info!, self.do_bind ...(+2) |
| `os/src/net/tcp.rs#shutdown` | `os/src/net/tcp.rs#shutdown` | 同板块同名 | Err, Ok, self.is_connected, self.is_connecting, self.is_listening, self.recv_shutdown.store ...(+2) | NET_DEV.lock, NET_DEV.lock().poll, Ok, SOCKET_SET.lock, Some, binding.get_mut::<tcp::Socket> ...(+5) |

#### 文件系统/第三方库
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `os/src/fs/ext4/inode.rs#unlink` | `os/src/fs/ext4/inode.rs#unlink` | 同板块同名 | EXT4_OP_LOCK.lock, Err, Ext4File::new, Ok, child_inode.node_type, child_inode.node_type().into ...(+13) | Err, Ok, debug_point!, info!, lock_file.dir_rm, lock_file.file_remove ...(+6) |
| `os/src/fs/ext4/inode.rs#write_at` | `os/src/fs/ext4/inode.rs#write_at` | 同板块同名 | EXT4_OP_LOCK.lock, Ext4File::new, Ok, Self::now_timespec, Some, file.file_close ...(+14) | cache.write, info!, self.get_size, self.set_size, self.write_directly |
| `os/src/fs/ext4/inode.rs#truncate` | `os/src/fs/ext4/inode.rs#truncate` | 同板块同名 | EXT4_OP_LOCK.lock, Ext4File::new, Ok, Self::now_timespec, Some, file.file_close ...(+9) | Errno::EIO.into, file.file_close, file.file_open, file.file_truncate, file.get_path, info! ...(+11) |
| `os/src/fs/ext4/inode.rs#read_at` | `os/src/fs/ext4/inode.rs#read_at` | 同板块同名 | EXT4_OP_LOCK.lock, Ext4File::new, Ok, file.file_close, file.file_close().map_err, file.file_open ...(+7) | buf.len, cache.read, info!, self.get_size, self.read_dirctly |

#### 通用模块/其他
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `os/src/arch/loongarch64/sbi.rs#shutdown` | `os/src/hal/la64/arch/mod.rs#shutdown` | 同板块同名 | core::ptr::write_volatile, mmio_addr, register::idle | power_off.write_volatile, println!, unreachable! |
| `os/src/mutex/spin.rs#lock` | `os/src/sync/mutex/spin.rs#lock` | 同板块同名 | S::before_lock, self
                .lock
                .compare_exchange, self
                .lock
                .compare_exchange(false, true, Ordering::Acquire, Ordering::Relaxed)
                .is_ok, self.wait_unlock | S::before_lock, self
                .lock
                .compare_exchange, self
                .lock
                .compare_exchange(false, true, Ordering::Acquire, Ordering::Relaxed)
                .is_ok, self.wait_unlock |
| `os/src/arch/rv64/sbi.rs#shutdown` | `os/src/hal/rv64/arch/sbi.rs#shutdown` | 同板块同名同结构 | system_reset, unreachable! | system_reset, unreachable! |
| `os/src/arch/rv64/sbi.rs#console_putchar` | `os/src/hal/rv64/arch/sbi.rs#console_putchar` | 同板块同名同结构 | sbi_rt::legacy::console_putchar | sbi_rt::legacy::console_putchar |


#### 1. 宏观判定
- **定性结论**: 独立开发 (相似度: 3.24%，共同 AST 指纹: 109；唯一 AST 指纹: 目标项目 1796，参考项目 1681；覆盖率: 目标项目 6.07%，参考项目 6.48%)

#### 2. 全模块覆盖矩阵解读
- **进程管理**: 双方均存在。共同唯一 AST 指纹仅 7 个，目标覆盖率 2.83%，参考覆盖率 3.85%。覆盖率极低，表明双方在进程创建、调度、通信等核心逻辑上采用了不同的对象模型与管理器架构。证据强度：**弱**。
- **任务调度/异步运行时**: 双方均存在。共同唯一 AST 指纹 12 个，目标覆盖率 4.27%，参考覆盖率 5.33%。覆盖率低，且从证据看，双方在调度器抽象（如 `TASK_MANAGER` vs `get_task_by_pid`）和系统调用实现上存在根本差异。证据强度：**弱**。
- **内存管理**: 双方均存在。共同唯一 AST 指纹 23 个，目标覆盖率 5.6%，参考覆盖率 6.93%。覆盖率低。`sys_mmap` 等核心调用使用的标志位与权限模型不同，表明内存管理策略独立。证据强度：**弱**。
- **中断/异常处理**: 双方均存在。共同唯一 AST 指纹 2 个，目标覆盖率 10.0%，参考覆盖率 2.44%。覆盖率虽因模块函数总数少而偏高，但证据显示双方在中断控制路径（如 `enable_timer_interrupt`）上使用了完全不同的硬件抽象层（寄存器抽象 vs SBI 调用）。证据强度：**未形成**。
- **系统调用**: 双方均存在。共同唯一 AST 指纹 15 个，目标覆盖率 8.67%，参考覆盖率 20.0%。覆盖率差异显著，参考项目在系统调用层的分发与实现上更为集中，而目标项目则分散于不同模块。证据强度：**弱**。
- **文件系统**: 双方均存在。共同唯一 AST 指纹 6 个，目标覆盖率 1.58%，参考覆盖率 1.63%。覆盖率极低。尽管存在同名函数，但 ext4 inode 操作、procfs 内容生成等核心逻辑的调用流完全不同。证据强度：**未形成**。
- **设备驱动**: 双方均存在。共同唯一 AST 指纹 10 个，目标覆盖率 33.33%，参考覆盖率 7.58%。高覆盖率主要由 `virtio` 驱动中数个“内部闭环逻辑”或简单映射函数贡献，此类函数无实质逻辑，不构成强证据。证据强度：**弱**。
- **网络协议栈**: 双方均存在。共同唯一 AST 指纹为 0，覆盖率均为 0.0%。这是双方独立开发的最有力证明，在网络编程模型、socket 抽象和协议实现上无任何 AST 层面重合。证据强度：**未形成**。
- **第三方库/公共依赖**: 双方均存在。共同唯一 AST 指纹 1 个，目标覆盖率 1.49%，参考覆盖率 2.33%。覆盖率极低，表明双方依赖或自研的第三方库/组件完全不同。证据强度：**未形成**。

#### 3. 底层 AST 关键证据指纹表
| 所属板块 | 目标项目路径 | 参考项目路径 | 证据类型 | 技术说明 |
| :--- | :--- | :--- | :--- | :--- |
| 进程管理 | `os/src/syscall/process.rs#sys_wait4` | `os/src/syscall/process.rs#sys_wait4` | 同板块同名 | 均为 POSIX 标准系统调用。目标使用 `matches_child`、`RUsage::default` 等封装；参考使用 `break`、`WaitOptions::from_bits` 等不同控制流与数据结构。 |
| 进程管理 | `os/src/syscall/process.rs#sys_prlimit64` | `os/src/syscall/process.rs#sys_prlimit64` | 同板块同名 | 目标通过 `TASK_MANAGER.get` 和 `copy_from_user` 实现；参考使用 `RLimit64::new` 和 `core::ptr::write` 裸指针操作。资源管理模型不同。 |
| 进程管理 | `os/src/mutex/spin.rs#wait_unlock` | `os/src/sync/mutex/spin.rs#wait_unlock` | 同板块底层逻辑一致 | 调用序列均为 `core::hint::spin_loop`, `panic!`, `self.lock.load`。这是 Rust `AtomicBool` 自旋锁的标准最小实现，受语言和硬件原语强约束，解空间极小。 |
| 任务调度 | `os/src/syscall/fs.rs#sys_splice` | `os/src/syscall/fs.rs#sys_splice` | 同板块同名 | 目标使用 `explicit_in_offset.is_some` 等逻辑；参考使用 `core::ptr::write` 和 `debug_point!`。数据搬运与错误处理路径不同。 |
| 内存管理 | `os/src/syscall/mm.rs#sys_mmap` | `os/src/syscall/mm.rs#sys_mmap` | 同板块同名 | 目标使用 `MMAPFLAGS::MAP_SHARED_VALIDATE` 等特定标志；参考使用 `MapPerm::from` 等不同权限模型。内存映射标志位体系不同。 |
| 系统调用 | `os/src/syscall/mod.rs#syscall` | `os/src/syscall/mod.rs#syscall` | 同板块同名 | 系统调用总入口。目标分发 211 个调用，参考分发 121 个，支持的系统调用数量与类型差异巨大。 |
| 文件系统 | `os/src/fs/ext4/inode.rs#write_at` | `os/src/fs/ext4/inode.rs#write_at` | 同板块同名 | 目标通过 `Ext4File::new` 和 `file.file_close` 管理生命周期；参考使用 `cache.write` 和 `self.write_directly`。写入策略与缓存机制不同。 |
| 设备驱动 | `os/src/drivers/virtio/block_dev.rs#as_dev_err` | `os/src/drivers/virtio_driver/mod.rs#as_dev_err` | 同板块同名同结构 | 均为“内部闭环逻辑”，即空函数或简单状态映射，无独创性逻辑表达。 |
| 设备驱动 | `os/src/drivers/disk.rs#write_one` | `os/src/drivers/disk.rs#write_one` | 同板块同名同结构 | 调用流均为 `buf.len().min`、`data.copy_from_slice`、`self.dev.read/write_block`。这是块设备驱动读写操作的标准模式。 |

#### 4. 核心代码调用流对比剖析
* **【进程管理/`sys_wait4` 系统调用】**
* **底层技术析理**: 两者均实现进程等待，但控制流与对象模型截然不同。目标项目采用函数式编程风格，通过 `matches_child` 闭包和 `(pid > 0).then_some` 等组合子进行条件匹配，并使用 `RUsage::default` 初始化资源统计。参考项目则采用命令式风格，通过 `break` 跳出循环和 `WaitOptions::from_bits` 进行选项处理。这反映了完全不同的进程状态管理与遍历策略。
* **代码溯源链接**:
  - 目标项目: `os/src/syscall/process.rs#sys_wait4`
  - 参考项目: `os/src/syscall/process.rs#sys_wait4`

* **【内存管理/`sys_mmap` 系统调用】**
* **底层技术析理**: 内存映射的核心在于标志位与权限检查。目标项目使用 `MMAPFLAGS::MAP_SHARED_VALIDATE.bits` 和 `MMAPFLAGS::all()` 进行标志位验证，其权限模型基于 `MMapProt::from_bits`。参考项目则使用 `MmapFlags::from_bits_truncate` 和 `MapPerm::from`，表明两者在支持的内存映射类型和权限校验体系上存在独立设计。
* **代码溯源链接**:
  - 目标项目: `os/src/syscall/mm.rs#sys_mmap`
  - 参考项目: `os/src/syscall/mm.rs#sys_mmap`

* **【中断/异常处理/`enable_timer_interrupt`】**
* **底层技术析理**: 目标项目通过架构相关的寄存器抽象层 `ecfg::enable_timer_interrupt` 和 `crmd::set_interrupt_enabled` 来使能定时器中断，直接操作控制寄存器。参考项目则通过 SBI 接口 `sie::set_stimer` 或 `ecfg::set_lie` 实现，依赖于更高层级的固件抽象。这体现了硬件抽象层（HAL）设计的根本性差异。
* **代码溯源链接**:
  - 目标项目: `os/src/arch/loongarch64/trap/mod.rs#enable_timer_interrupt`
  - 参考项目: `os/src/sync/interrupt.rs#enable_timer_interrupt`

* **【系统调用/`syscall` 总入口】**
* **底层技术析理**: 系统调用分发器是内核的神经中枢。目标项目的 `syscall` 函数分发了包括 `sys_accept`、`sys_adjtimex` 在内的 211 个调用，表明其系统调用支持更为广泛。参考项目则分发了 121 个调用。两者支持的系统调用号范围和具体实现函数完全不同，这是内核功能集与设计哲学的宏观差异。
* **代码溯源链接**:
  - 目标项目: `os/src/syscall/mod.rs#syscall`
  - 参考项目: `os/src/syscall/mod.rs#syscall`

* **【文件系统/`ext4` inode 写入操作】**
* **底层技术析理**: 在 ext4 文件系统的 `write_at` 实现中，目标项目显式地通过 `Ext4File::new` 创建文件对象，并在操作前后调用 `file.file_open` 和 `file.file_close` 管理生命周期，体现了 RAII 风格的资源管理。参考项目则直接调用 `self.write_directly` 或 `cache.write`，其内部缓存与写入路径与目标项目完全不同。
* **代码溯源链接**:
  - 目标项目: `os/src/fs/ext4/inode.rs#write_at`
  - 参考项目: `os/src/fs/ext4/inode.rs#write_at`

#### 5. 单方模块与降权项
- **网络协议栈**: 双方均存在该模块，但共同 AST 指纹为 0。这是双方独立开发的最有力证据，表明在 socket 抽象、协议实现（TCP/UDP）和网络设备交互上无任何代码层面的重合。
- **第三方库/公共依赖**: 双方均存在该模块，但共同指纹仅 1 个，覆盖率极低。这表明双方依赖或自研的第三方库、工具链或公共组件集合完全不同。
- **`wait_unlock` 函数**: 尽管 AST 调用图谱一致，但该函数是 Rust 语言中基于 `AtomicBool` 实现自旋锁等待的标准惯用法，其逻辑空间被 `core::sync::atomic` 库和 CPU 自旋原语严格限制，任何独立实现都必然收敛于此模式。此证据降权为**语言/硬件强制模式**，不构成抄袭。
- **`procfs` 的 `read_at`/`write_at`**: 这些函数名由文件系统 trait 强制定义，其内部的字节流拷贝操作（`buf[..n].copy_from_slice`）是 Rust 标准处理方式。双方在内容生成逻辑（`generate_*` vs `format!`/`meminfo2string`）和锁机制上完全不同，因此降权为**领域公共模板与语言惯用法**。

#### 6. 保守结论
本次审计基于 3.24% 的极低 AST 相似度和 6% 左右的模块覆盖率，未发现支持抄袭指控的客观证据。控方提出的唯一“铁证” `wait_unlock` 函数，经技术分析，属于 Rust 语言在特定并发原语下的标准实现，其相似性是技术约束的必然结果，而非代码复用。双方在进程管理、内存管理、系统调用分发及文件系统等核心子系统上，展现出截然不同的架构设计、对象抽象与数据流控制，特别是在网络协议栈模块实现了零 AST 指纹重合。综上，目标项目与参考项目在底层实现上存在根本性、系统性的差异，应认定为独立开发，整体同源性风险极低。

------------------------------

