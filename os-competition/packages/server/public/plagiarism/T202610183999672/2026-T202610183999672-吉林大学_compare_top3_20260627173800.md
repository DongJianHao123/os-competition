# 🛡️ OS 内核赛自动化代码级架构审计报告

**审计时间**: `2026-06-27 17:38:00`
**参赛作品名称**: `2026-T202610183999672-吉林大学`
**年份**: `2026`
**学校**: `吉林大学`
**队伍名称**: `T202610183999672`
**比赛名字**: `OS-Competition`
**比赛子赛道**: `Kernel-Implementation`
**仓库地址**: `https://gitlab.eduxiji.net/educg-group-43501-3132633/T202610183999672-2275`
**推测开发语言**: `Rust / C (由AST探针自动识别)`
**特征函数量**: `8857`

## ⚔️ 底层代码同源性对比 (AST Homology)
> 本部分由专门的代码审查智能体，对该项目与符合年份条件的历届最高相似度项目进行控制流与底层调用对比。新版报告会先给出确定性的全模块覆盖矩阵，再给出 Agent 的分模块解释，避免只围绕少量抽样函数展开。

### 🏆 Top 3 底层对标: vs `2025-SubsToKernel-北京科技大学`（北京科技大学 / SubsToKernel）

#### 对方作品信息

**对方年份**: `2025`
**对方学校**: `北京科技大学`
**对方队伍名称**: `SubsToKernel`
**对方比赛名字**: `OS-Competition`
**对方比赛子赛道**: `Kernel-Implementation`
**对方仓库地址**: `https://gitlab.eduxiji.net/educg-group-36002-2710490/T202510008995695-2720`

> 双向覆盖率：共同 AST 指纹: 1618；唯一 AST 指纹: 目标项目 6561，参考项目 3437；覆盖率: 目标项目 24.66%，参考项目 47.08%

## 全模块对比总览（确定性证据）

> 总体覆盖率：共同 AST 指纹: 1618；唯一 AST 指纹: 目标项目 6561，参考项目 3437；覆盖率: 目标项目 24.66%，参考项目 47.08%

### 1. 模块覆盖矩阵
| 模块 | 目标函数数 | 参考函数数 | 共同唯一 AST 指纹 | Target 覆盖 | Base 覆盖 | 状态 |
| :--- | ---: | ---: | ---: | ---: | ---: | :--- |
| 进程管理 | 925 | 86 | 6 | 0.78% | 8.11% | 双方存在且有结构重合 |
| 任务调度/异步运行时 | 465 | 141 | 6 | 1.58% | 4.41% | 双方存在且有结构重合 |
| 内存管理 | 1286 | 329 | 25 | 2.35% | 8.59% | 双方存在且有结构重合 |
| 中断/异常处理 | 55 | 8 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |
| 系统调用 | 258 | 108 | 17 | 8.29% | 22.67% | 双方存在且有结构重合 |
| 文件系统 | 1045 | 256 | 4 | 0.47% | 2.14% | 双方存在且有结构重合 |
| 设备驱动 | 122 | 57 | 1 | 0.85% | 2.13% | 双方存在且有结构重合 |
| 网络协议栈 | 360 | 124 | 1 | 0.38% | 0.84% | 双方存在且有结构重合 |
| 文件系统/第三方库 | 437 | 704 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |
| 网络协议栈/第三方库 | 2387 | 2388 | 1519 | 83.65% | 84.16% | 双方存在且有结构重合 |
| 第三方库 | 1230 | 0 | 0 | 0.0% | 0.0% | 仅目标项目体现 |
| 通用模块/其他 | 287 | 139 | 15 | 8.93% | 11.63% | 双方存在且有结构重合 |

### 2. 分模块证据清单

#### 进程管理
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `os/src/task/processor.rs#run_tasks` | `os/src/task/processor.rs#run_tasks` | 同板块同名 | Arc::as_ptr, CURRENT_EGID.store, CURRENT_EUID.store, CURRENT_GID.store, CURRENT_PARENT_PID.store, CURRENT_PGID.store ...(+74) | PROCESSOR.exclusive_access, Some, __switch, check_timer, debug!, drop ...(+10) |
| `os/src/syscall/process/futex.rs#sys_futex` | `os/src/syscall/process.rs#sys_futex` | 同板块同名 | FutexOption::from_bits_truncate, UserPtr::new, UserPtr::new(uaddr2 as *const u32).read, current_futex_key, current_task_ref, current_task_ref()
                        .unwrap ...(+23) | FutexCmd::from_bits, FutexCmd::from_bits(futex_op & 0x7f).unwrap, FutexKey::new, FutexOpt::from_bits_truncate, VirtAddr::from, add_timer ...(+29) |
| `os/src/syscall/process/lifecycle.rs#sys_get_robust_list` | `os/src/syscall/process.rs#sys_get_robust_list` | 同板块同名 | ProcessManager::find_task, copy_to_user, copy_to_user(token, &inner.robust_list.head, head_ptr).is_err, copy_to_user(token, &inner.robust_list.len, len_ptr).is_err, current.acquire_inner_lock, current.egid ...(+14) | debug!, head_ptr.is_null, len_ptr.is_null |
| `os/src/task/signal/mod.rs#sigprocmask` | `user/src/lib.rs#sigprocmask` | 同板块同名 | SigMaskHow::from_bits, Signals::from_bits_truncate, UserPtr::new, UserPtr::new(set as *const u64).read, UserPtrMut::new, UserPtrMut::new(oldset as *mut u64).write ...(+8) | sys_sigprocmask |

#### 任务调度/异步运行时
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `os/src/net/syscall/setsockopt.rs#sys_setsockopt` | `os/src/syscall/net.rs#sys_setsockopt` | 同板块同名 | (optlen as usize).min, (size as usize).min, (size as usize).min(256 * 1024).max, UserBufferReader::new, UserBufferReader::new(token, optval_ptr as *const u8, optlen as usize).is_err, UserPtr::<TimeVal>::from_addr ...(+29) | current_process, debug!, inner.fd_table.get, inner.fd_table.get(sockfd).file.sock, inner.fd_table.len, inner.fd_table.try_get ...(+4) |
| `os/src/net/syscall/bind.rs#sys_bind` | `os/src/syscall/net.rs#sys_bind` | 同板块同名 | ABSTRACT_TABLE
                        .create_abstract_name_bytes, ABSTRACT_TABLE
                        .create_abstract_name_bytes(&name, socket.clone())
                        .map, ABSTRACT_TABLE
                        .create_abstract_name_bytes(&name, socket.clone())
                        .map(|_| 0)
                        .unwrap_or_else, Arc::downgrade, Endpoint::Ip, Endpoint::Netlink ...(+43) | (data.len() - offset).min, Ipv4Addr::from, SocketAddr::V4, SocketAddrV4::new, buffer.is_empty, current_process ...(+15) |
| `os/src/syscall/fs.rs#sys_sendfile` | `os/src/syscall/fs.rs#sys_sendfile` | 同板块同名 | SeekFrom::SeekCurrent, Some, UserPtrMut::new, UserPtrMut::new(offset).read, UserPtrMut::new(offset).write, UserPtrMut::new(offset).write(token, &offset_value).is_err ...(+29) | UserBuffer::new, Vec::new, buf.as_mut_slice, buf.as_mut_slice().as_mut_ptr, buf.as_slice, buf.as_slice().len ...(+31) |
| `os/src/net/socket/unix/stream/mod.rs#connect` | `os/src/net/socket/tcp.rs#connect` | 同板块同名 | ABSTRACT_TABLE
                    .lookup_abstract_name_bytes, ABSTRACT_TABLE
                    .lookup_abstract_name_bytes(name)
                    .ok_or, Connected::new_pair, Err, Inner::Connected, Ok ...(+21) | Err, Ok, Ok::<(IpEndpoint, IpEndpoint), SysErrNo>, SOCKET_SET
                .with_socket_mut::<tcp::Socket, _, _>, SOCKET_SET
                .with_socket_mut::<tcp::Socket, _, _>(handle, |socket| {
                    socket
                        .connect(iface.lock().context(), remote_endpoint, bound_endpoint)
                        .or_else(|e| match e {
                            ConnectError::InvalidState => {
                                ax_err!(BadState, "socket connect() failed")
                            }
                            ConnectError::Unaddressable => {
                                ax_err!(ConnectionRefused, "socket connect() failed")
                            }
                        })?;
                    Ok::<(IpEndpoint, IpEndpoint), SysErrNo>((
                        socket.local_endpoint().unwrap(),
                        socket.remote_endpoint().unwrap(),
                    ))
                })
                .unwrap, SOCKET_SET.add ...(+30) |

#### 内存管理
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `os/src/syscall/fs.rs#sys_fcntl` | `os/src/syscall/fs.rs#sys_fcntl` | 同板块同名 | FcntlCommand::try_from_primitive, Some, UserPtr::<u64>::from_addr, UserPtr::<u64>::from_addr(arg).read, UserPtr::<vfs::FOwnerEx>::from_addr, UserPtr::<vfs::FOwnerEx>::from_addr(arg)
                .read ...(+63) | FcntlCmd::from_bits, FcntlCmd::from_bits(cmd).unwrap, OpenFlags::O_NONBLOCK.bits, OpenFlags::O_RDWR.bits, OpenFlags::from_bits_truncate, current_process ...(+16) |
| `os/src/hal/arch/loongarch64/trap/mod.rs#trap_handler` | `os/src/hal/trap/mod.rs#trap_handler` | 同板块同名 | (0..sz).rev, BadV::read, BadV::read().get_vaddr, ERA::read, ERA::read().next_ins, ERA::read().next_ins().write ...(+63) | check_signals_of_current, check_timer, core::hint::spin_loop, current_add_signal, current_process, current_process()
        .inner_exclusive_access ...(+18) |
| `os/src/mm/address_space.rs#map_elf` | `os/src/mm/memory_set.rs#map_elf` | 同板块同名 | ((ph.virtual_addr() + ph.mem_size()) as usize + bias).into, (VirtAddr::from(elf.input.as_ptr() as usize + file_offset)).floor, (ph.virtual_addr() as usize + bias).into, Err, KERNEL_SPACE
                        .lock, KERNEL_SPACE
                        .lock()
                        .remove_area_with_start_vpn ...(+53) | ((ph.virtual_addr() + ph.mem_size()) as usize + offset.0).into, (ph.virtual_addr() as usize + offset.0).into, MapArea::new, PTEFlags::from, Some, Vec::with_capacity ...(+23) |
| `os/src/hal/arch/riscv/trap/mod.rs#trap_handler` | `os/src/hal/trap/mod.rs#trap_handler` | 同板块同名 | VirtAddr::from, crate::task::perf::perf_time_now, crate::task::perf::perf_time_now().saturating_sub, crate::task::perf::record_page_fault, crate::task::perf::record_pagefault_time_us, crate::task::perf::record_timer_interrupt ...(+27) | VirtAddr::from, add.floor, badv::read, badv::read().vaddr, check_signals_of_current, crmd.ie ...(+26) |

#### 中断/异常处理
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `os/src/hal/arch/loongarch64/trap/mod.rs#trap_from_kernel` | `os/src/hal/trap/mod.rs#trap_from_kernel` | 同板块同名 | ((addr + i) as *mut u8).read_unaligned, ((addr + i) as *mut u8).write_unaligned, (0..sz).rev, BadV::read, BadV::read().get_vaddr, EStat::read ...(+16) | era::read, estat::read, panic!, warn! |
| `os/src/hal/arch/loongarch64/trap/mod.rs#enable_timer_interrupt` | `os/src/hal/trap/mod.rs#enable_timer_interrupt` | 同板块同名 | ECfg::empty, ECfg::empty()
        .set_line_based_interrupt_vector, ECfg::empty()
        .set_line_based_interrupt_vector(LineBasedInterrupt::TIMER)
        .write | crmd::set_ie, ecfg::set_lie, get_timer_freq, sie::set_stimer, tcfg::set_en, tcfg::set_init_val ...(+2) |
| `os/src/hal/arch/loongarch64/trap/mod.rs#set_kernel_trap_entry` | `os/src/hal/trap/mod.rs#set_kernel_trap_entry` | 同板块同名 | register::EEntry::read, register::EEntry::read()
        .set_exception_entry, register::EEntry::read()
        .set_exception_entry(__kern_trap as usize)
        .write | eentry::set_eentry, stvec::write |
| `os/src/hal/arch/loongarch64/trap/mod.rs#set_user_trap_entry` | `os/src/hal/trap/mod.rs#set_user_trap_entry` | 同板块同名 | register::EEntry::read, register::EEntry::read()
        .set_exception_entry, register::EEntry::read()
        .set_exception_entry(strampoline as usize)
        .write | eentry::set_eentry, stvec::write |

#### 系统调用
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `os/ltp_proto_compat.c#sbrk` | `user/src/lib.rs#sbrk` | 同板块同名 | brk_to, syscall | sys_sbrk |
| `user/src/syscall.rs#syscall6` | `user/src/syscall.rs#syscall6` | 同板块同名 | __syscall, core::arch::asm! | core::arch::asm! |
| `user/src/syscall.rs#syscall` | `user/src/syscall.rs#syscall` | 同板块同名 | __syscall, core::arch::asm! | core::arch::asm! |
| `user/src/syscall.rs#sys_linkat` | `user/src/syscall.rs#sys_linkat` | 同板块同名 | newpath.as_ptr, oldpath.as_ptr, syscall6 | new_path.as_ptr, old_path.as_ptr, syscall6 |

#### 文件系统
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `os/src/fs/tmpfs/mod.rs#rename` | `os/src/fs/vfs/mod.rs#rename` | 同板块同名 | Arc::ptr_eq, Err, Ok, String::from, child.0.lock, drop ...(+30) | unimplemented! |
| `os/src/fs/dev/pipe.rs#read_at` | `os/src/fs/vfs/mod.rs#read_at` | 同板块同名 | Err, Ok, buf.is_empty, pipe_finish_read, pipe_inc, pipe_profile_enabled ...(+12) | unimplemented! |
| `os/src/mm/uaccess.rs#write_at` | `os/src/fs/vfs/mod.rs#write_at` | 同板块同名 | (buf.len() - start).min, buf.as_mut_ptr, buf.as_mut_ptr().add, buf.len, buffer.as_mut_ptr, buffer.as_mut_ptr().add ...(+9) | unimplemented! |
| `os/src/fs/vfs/mount.rs#create` | `os/src/fs/vfs/mod.rs#create` | 同板块同名 | Arc::ptr_eq, MountFSInode::new, MountFSInode::overlaid_inode, Ok, String::from, cache.insert_or_get ...(+16) | unimplemented! |

#### 设备驱动
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `os/src/drivers/net/virtio_net.rs#receive` | `os/src/drivers/virtio/net.rs#receive` | 同板块同名 | Some, buf[..len].copy_from_slice, net.receive, net.recycle_rx_buffer, net.recycle_rx_buffer(rx_buffer).expect, packet.len ...(+3) | Err, NetBufPtr::new, Ok, self.dev.inner.lock, self.dev.inner.lock().receive |
| `os/src/drivers/block/virtio_blk_pci.rs#unshare` | `os/src/drivers/virtio/mod.rs#unshare` | 同板块同名 | QUEUE_FRAMES.lock, QUEUE_FRAMES.lock()
            .remove, QUEUE_FRAMES.lock()
            .remove(&paddr)
            .unwrap_or_else, buffer.as_mut, buffer.copy_from_slice, buffer.len ...(+4) | 内部闭环逻辑 |
| `os/src/drivers/block/partition.rs#read_block` | `os/src/drivers/virtio/blk.rs#read_block` | 同板块同名 | assert!, buf.len, self.parent.read_block | buf.fill, panic!, self.inner.lock, self.inner.lock().ack_interrupt, self.inner.lock().read_blocks |
| `os/src/drivers/block/partition.rs#write_block` | `os/src/drivers/virtio/blk.rs#write_block` | 同板块同名 | assert!, buf.len, self.parent.write_block | panic!, self.inner.lock, self.inner.lock().ack_interrupt, self.inner.lock().write_blocks |

#### 网络协议栈
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `os/src/net/socket/inet/stream/lifecycle.rs#set_nagle_enabled` | `os/src/net/socket/tcp.rs#set_nagle_enabled` | 同板块同名 | s.set_nagle_enabled, with_tcp_mut | Err, Ok, SOCKET_SET.with_socket_mut::<tcp::Socket, _, _>, self.handle.get, self.handle.get().read, socket.set_nagle_enabled |
| `os/src/net/socket/inet/stream/mod.rs#shutdown` | `os/src/net/socket/tcp.rs#shutdown` | 同板块同名 | inner.shutdown, result.is_ok, self.inner.lock, self.read_shutdown.store, self.write_shutdown.store | LISTEN_TABLE.unlisten, Ok, SOCKET_SET.poll_interfaces, SOCKET_SET.with_socket_mut::<tcp::Socket, _, _>, debug!, self.handle.get ...(+9) |
| `os/src/net/socket/unix/stream/mod.rs#new_connected` | `os/src/net/socket/tcp.rs#new_connected` | 同板块同名 | AtomicBool::new, AtomicUsize::new, EventWaitQueue::new, Inner::Connected, Mutex::new | AtomicBool::new, AtomicU8::new, Some, UnsafeCell::new |
| `os/src/net/socket/inet/stream/inner.rs#remote_endpoint` | `os/src/net/socket/udp.rs#remote_endpoint` | 同板块同名 | Some, c.remote_endpoint, e.remote_endpoint, s.remote_endpoint | Err, addr.ok_or, self.peer_addr.try_read |

#### 文件系统/第三方库
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `os/src/fs/ext4/ext4fs.rs#find` | `os/src/fs/ext4_lw/inode.rs#find` | 同板块同名 | Err, Ext4DirEntry::default, Ext4DirSearchResult::new, Ok, Some, all_entries.is_empty ...(+38) | Arc::new, Err, Ext4Inode::new, Ok, core::str::from_utf8, core::str::from_utf8(&file_name[..end]).unwrap ...(+12) |
| `os/src/fs/ext4/ext4fs.rs#rename` | `os/libs/lwext4_rsut/examples/src/ext4fs.rs#rename` | 同板块同名 | Err, Ext4DirEntry::default, Ext4DirSearchResult::new, Ok, alloc::string::String::from, alloc::sync::Arc::ptr_eq ...(+50) | debug!, file.file_rename, info!, self.0.borrow_mut |
| `os/src/fs/ext4/file.rs#write_at` | `os/src/fs/ext4_lw/inode.rs#write_at` | 同板块同名 | Block::load_offset, Ok, block.sync_blk_to_disk, block.write_offset, drop, inode_ref.inode.set_size ...(+11) | SysErrNo::from, file.file_open, file.file_open(path, O_RDWR)
            .map_err, file.file_seek, file.file_seek(off as i64, SEEK_SET)
            .map_err, file.file_write ...(+5) |
| `os/src/fs/ext4/file.rs#read_at` | `os/src/fs/ext4_lw/inode.rs#read_at` | 同板块同名 | Ok, core::cmp::min, inode_ref.inode.block_as_bytes, inode_ref.inode.flags, inode_ref.inode.get_file_type, inode_ref.inode.size ...(+11) | SysErrNo::from, file.file_open, file.file_open(path, O_RDONLY)
            .map_err, file.file_read, file.file_seek, file.file_seek(offset as i64, SEEK_SET)
            .map_err ...(+5) |

#### 网络协议栈/第三方库
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `dependency/smoltcp/src/socket/tcp.rs#process` | `os/libs/smoltcp/src/socket/tcp.rs#process` | 同板块同名 | AckDelayTimer::Waiting, IpEndpoint::new, Self::random_seq_no, Self::rst_reply, Some, control.quash_psh ...(+36) | AckDelayTimer::Waiting, IpEndpoint::new, Self::random_seq_no, Self::rst_reply, Some, control.quash_psh ...(+42) |
| `dependency/smoltcp/src/socket/tcp.rs#dispatch` | `os/libs/smoltcp/src/socket/tcp.rs#dispatch` | 同板块同名 | IpRepr::new, Ok, Some, cx.ip_mtu, cx.now, emit ...(+39) | IpRepr::new, Ok, Some, cx.ip_mtu, cx.now, emit ...(+45) |
| `dependency/smoltcp/src/iface/interface/sixlowpan.rs#dispatch_sixlowpan` | `os/libs/smoltcp/src/iface/interface/sixlowpan.rs#dispatch_sixlowpan` | 同板块同名 | Icmpv6Packet::new_unchecked, Ieee802154Frame::new_unchecked, SixlowpanFragPacket::new_unchecked, SixlowpanIphcPacket::new_unchecked, SixlowpanNextHeader::Uncompressed, SixlowpanUdpNhcPacket::new_unchecked ...(+35) | Ieee802154Frame::new_unchecked, Self::compressed_packet_size, Self::ipv6_to_sixlowpan, SixlowpanFragPacket::new_unchecked, frag1.buffer_len, frag1.emit ...(+13) |
| `dependency/smoltcp/src/iface/interface/mod.rs#dispatch_ip` | `os/libs/smoltcp/src/iface/interface/mod.rs#dispatch_ip` | 同板块同名 | EthernetAddress, EthernetFrame::<&[u8]>::buffer_len, EthernetFrame::<&[u8]>::header_len, EthernetFrame::new_unchecked, Ipv4Packet::new_unchecked, Ok ...(+35) | EthernetAddress, EthernetFrame::<&[u8]>::buffer_len, EthernetFrame::<&[u8]>::header_len, EthernetFrame::new_unchecked, Ipv4Packet::new_unchecked, Ok ...(+35) |

#### 第三方库
仅目标项目中观察到该模块代表函数，参考项目没有同模块可对齐证据：
- 目标项目：`dependency/virtio-drivers/src/queue.rs#fake_read_write_queue`，调用摘要：(*available_ring).idx.load, (*used_ring).idx.fetch_add, (*used_ring).idx.load, Vec::new, assert!, assert_eq! ...(+18)
- 目标项目：`dependency/dep_pci/pci/src/lib.rs#probe_function`，调用摘要：BAR::decode, BridgeControl::from_bits_truncate, Command::from_bits_truncate, DeviceKind::CardbusBridge, DeviceKind::Device, DeviceKind::PciBridge ...(+6)
- 目标项目：`dependency/virtio-drivers/src/device/sound/fake.rs#handle_control_request`，调用摘要：CommandCode::SOk.into, Some, Vec::new, VirtIOSndHdr {
                            command_code: CommandCode::SOk.into(),
                        }
                        .as_bytes, VirtIOSndHdr::read_from_prefix, VirtIOSndHdr::read_from_prefix(&request)
                .expect ...(+16)
- 目标项目：`dependency/dep_iso/src/block/ahci.rs#new`，调用摘要：(0..ghc.num_ports()).find, P::alloc_dma, Some, c.set_bit, debug!, ghc.enable ...(+16)

#### 通用模块/其他
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `os/src/hal/arch/loongarch64/boot.rs#_start` | `user/src/lib.rs#_start` | 同板块同名 | asm! | ((argv + i * core::mem::size_of::<usize>()) as *const usize).read_volatile, ((str_start + *i) as *const u8).read_volatile, (0usize..)
            .find, (0usize..)
            .find(|i| unsafe { ((str_start + *i) as *const u8).read_volatile() == 0 })
            .unwrap, HEAP.lock, HEAP.lock()
            .init ...(+11) |
| `os/src/timer.rs#sub` | `os/src/timer.rs#sub` | 同板块同名 | TimeSpec::new | TimeSpec::from_ns, TimeSpec::new, other.to_ns, self.to_ns |
| `os/src/console.rs#write_str` | `user/src/console.rs#write_str` | 同板块同名 | Ok, console_flush, console_putchar, s.chars | Err, Ok, s.as_bytes, s.as_bytes().iter, self.0.len, self.0.push_back ...(+1) |
| `os/src/trace.rs#push` | `os/src/users/mod.rs#push` | 同板块同名 | self.buf.len | self.groups.push |


#### 1. 宏观判定
- **定性结论**: 独立开发 (相似度: 19.31%，共同 AST 指纹: 1618；唯一 AST 指纹: 目标项目 6561，参考项目 3437；覆盖率: 目标项目 24.66%，参考项目 47.08%)

#### 2. 全模块覆盖矩阵解读
- **进程管理**: 双方均存在该模块。存在 6 个共同 AST 指纹。目标覆盖率 0.78%，参考覆盖率 8.11%，覆盖率极低。证据强度：**弱**。
- **任务调度/异步运行时**: 双方均存在该模块。存在 6 个共同 AST 指纹。目标覆盖率 1.58%，参考覆盖率 4.41%，覆盖率极低。证据强度：**弱**。
- **内存管理**: 双方均存在该模块。存在 25 个共同 AST 指纹。目标覆盖率 2.35%，参考覆盖率 8.59%，覆盖率低。证据强度：**弱**。
- **中断/异常处理**: 双方均存在该模块。共同 AST 指纹为 0。目标覆盖率 0.0%，参考覆盖率 0.0%，无共同结构指纹。证据强度：**未形成**。
- **系统调用**: 双方均存在该模块。存在 17 个共同 AST 指纹。目标覆盖率 8.29%，参考覆盖率 22.67%，覆盖率中等偏低。证据强度：**中**。
- **文件系统**: 双方均存在该模块。存在 4 个共同 AST 指纹。目标覆盖率 0.47%，参考覆盖率 2.14%，覆盖率极低。证据强度：**弱**。
- **设备驱动**: 双方均存在该模块。存在 1 个共同 AST 指纹。目标覆盖率 0.85%，参考覆盖率 2.13%，覆盖率极低。证据强度：**弱**。
- **网络协议栈**: 双方均存在该模块。存在 1 个共同 AST 指纹。目标覆盖率 0.38%，参考覆盖率 0.84%，覆盖率极低。证据强度：**弱**。
- **文件系统/第三方库**: 双方均存在该模块。共同 AST 指纹为 0。目标覆盖率 0.0%，参考覆盖率 0.0%，无共同结构指纹。证据强度：**未形成**。
- **网络协议栈/第三方库**: 双方均存在该模块。存在 1519 个共同 AST 指纹。目标覆盖率 83.65%，参考覆盖率 84.16%，覆盖率极高。证据强度：**强**，但属于第三方公共依赖。
- **第三方库**: 仅目标项目体现。共同 AST 指纹为 0。目标覆盖率 0.0%，参考覆盖率 0.0%。证据强度：**未形成**。
- **通用模块/其他**: 双方均存在该模块。存在 15 个共同 AST 指纹。目标覆盖率 8.93%，参考覆盖率 11.63%，覆盖率低。证据强度：**弱**。

#### 3. 底层 AST 关键证据指纹表
| 所属板块 | 目标项目路径 | 参考项目路径 | 证据类型 | 技术说明 |
| :--- | :--- | :--- | :--- | :--- |
| 网络协议栈/第三方库 | dependency/smoltcp/src/socket/tcp.rs#process | os/libs/smoltcp/src/socket/tcp.rs#process | 同源指纹 | 双方均直接依赖上游 `smoltcp` 库，该函数及其调用图谱（如 `AckDelayTimer::Waiting`, `IpEndpoint::new`）完全来自第三方代码，非任何一方原创。 |
| 网络协议栈/第三方库 | dependency/smoltcp/src/iface/interface/mod.rs#dispatch_ip | os/libs/smoltcp/src/iface/interface/mod.rs#dispatch_ip | 同源指纹 | 同上，`EthernetFrame::new_unchecked`, `Ipv4Packet::new_unchecked` 等调用链为 `smoltcp` 库的标准实现。 |
| 系统调用 | user/src/syscall.rs#syscall6 | user/src/syscall.rs#syscall6 | 公共接口 | 用户态系统调用封装，其核心 `core::arch::asm!` 宏是 Rust 进行底层汇编调用的标准方式，且系统调用号由 ABI 决定。 |
| 系统调用 | user/src/syscall.rs#syscall | user/src/syscall.rs#syscall | 公共接口 | 同上，为基础系统调用封装。 |
| 进程管理 | os/src/task/processor.rs#run_tasks | os/src/task/processor.rs#run_tasks | 弱信号 | 函数名相同，但目标项目调用图谱包含 `CURRENT_*` 系列全局原子变量，参考项目使用 `PROCESSOR.exclusive_access`，架构差异显著。 |
| 进程管理 | os/src/task/processor.rs#schedule | os/src/task/processor.rs#schedule | 弱信号 | 核心调度函数同名，但目标项目使用 `PROCESSOR.lock` 和性能计数器，参考项目使用 `PROCESSOR.exclusive_access`，同步原语不同。 |
| 内存管理 | os/src/mm/address_space.rs#map_elf | os/src/mm/memory_set.rs#map_elf | 弱信号 | ELF 文件映射的标准流程，但目标项目涉及 `KERNEL_SPACE.lock` 和 `bias` 计算，参考项目使用 `MapArea::new` 和 `offset`，实现细节不同。 |
| 中断/异常处理 | os/src/hal/arch/loongarch64/trap/mod.rs#trap_handler | os/src/hal/trap/mod.rs#trap_handler | 弱信号 | 同为 trap 处理入口，但目标项目基于 LoongArch64 架构（`BadV`, `ERA`），参考项目基于 RISC-V 架构（`badv`, `sepc`），硬件操作完全不同。 |

#### 4. 核心代码调用流对比剖析
* **【进程管理/调度器核心】**
* **底层技术析理**: 两者均实现了 `run_tasks` 调度循环，但采用了截然不同的同步与状态管理模型。参考项目使用 `PROCESSOR.exclusive_access()` 获取全局调度器实例的独占访问权，这是一种集中式的、基于 `UPSafeCell` 或类似机制的单核调度模型。目标项目则使用 `PROCESSOR.lock()` 获取锁，并在调度过程中通过 `CURRENT_PID.store` 等一系列全局原子变量来维护当前任务的上下文信息，这是一种为多核或更复杂同步场景设计的去中心化状态管理模型。两者仅在最终执行上下文切换的 `__switch` 汇编函数上存在必然的底层一致性。
* **代码溯源链接**:
  - 目标项目: `os/src/task/processor.rs#run_tasks`
  - 参考项目: `os/src/task/processor.rs#run_tasks`

* **【进程管理/进程ID获取】**
* **底层技术析理**: 两者都提供了 `sys_getpid` 系统调用，但实现路径完全不同。参考项目采用动态解析链路：`current_task() -> task.process.upgrade() -> process.getpid()`，即从当前任务指针出发，逐级向上查找其所属的进程控制块，再获取 PID。目标项目则采用静态缓存策略，直接读取全局原子变量 `current_pid`。这反映了两种不同的内核数据组织方式：动态关联 vs. 性能优化的状态缓存。
* **代码溯源链接**:
  - 目标项目: `os/src/syscall/process/ids.rs#sys_getpid`
  - 参考项目: `os/src/syscall/process.rs#sys_getpid`

* **【内存管理/ELF加载】**
* **底层技术析理**: 两者均实现了 `map_elf` 函数将 ELF 文件的各个段映射到内存，但映射策略和地址空间管理方式不同。目标项目在计算映射起始地址时引入了 `bias` 变量，并直接操作 `KERNEL_SPACE` 锁来管理内核地址空间区域。参考项目则使用 `offset` 变量，并通过 `MapArea::new` 构造函数和 `push` 方法来构建内存集合。这表明两者在地址空间布局和管理接口上存在独立设计。
* **代码溯源链接**:
  - 目标项目: `os/src/mm/address_space.rs#map_elf`
  - 参考项目: `os/src/mm/memory_set.rs#map_elf`

* **【中断/异常处理/Trap入口】**
* **底层技术析理**: 两者都有 `trap_handler` 作为异常处理的总入口，但面向的硬件架构不同，导致底层寄存器操作完全异构。目标项目针对 LoongArch64 架构，读取 `BadV`（坏地址寄存器）和 `ERA`（异常返回地址寄存器）等。参考项目针对 RISC-V 架构，读取 `badv`、`sepc` 等寄存器。尽管处理流程（保存上下文 -> 分发处理 -> 恢复上下文）在逻辑上相似，但具体的硬件操作代码无复用可能。
* **代码溯源链接**:
  - 目标项目: `os/src/hal/arch/loongarch64/trap/mod.rs#trap_handler`
  - 参考项目: `os/src/hal/trap/mod.rs#trap_handler`

#### 5. 单方模块与降权项
- **单方模块**: “第三方库”模块仅存在于目标项目中，包含 `virtio-drivers`、`pci`、`iso` 等依赖的独立函数，参考项目无对应模块，不构成同源证据。
- **第三方库/公共依赖**: “网络协议栈/第三方库”模块贡献了全项目 1618 个共同 AST 指纹中的 1519 个，占比高达 93.9%。这些指纹均来自双方共同依赖的上游 `smoltcp` 网络协议栈。此部分高重合度是引用公共第三方代码的必然结果，与任何一方的原创性无关，必须从核心原创代码的相似度评估中剥离。
- **公共接口与惯用法**: 大量同名函数如 `sys_getpid`、`sys_exit`、`sys_yield` 等，其名称由 POSIX/Linux 系统调用标准所规定。`TaskContext::zero_init`、`Option::take`、`Arc::as_ref` 等是 Rust 语言和 OS 开发中的标准惯用法。这些相似性源于共同的外部规范和语言特性，不能作为代码复用的证据。
- **跨板块/跨环境证据**: 控方提出的 `sigprocmask`（目标位于内核态 `os/src/task/signal/mod.rs`，参考位于用户态 `user/src/lib.rs`）以及 LTP 测试函数（目标位于用户态 `user/src/bin/initproc.rs`，参考对应逻辑在内核态 `os/src/syscall/process.rs`）属于跨运行环境或跨功能板块的比对，按规则降级为弱信号，不构成同板块主证据。

#### 6. 保守结论
综合评估，目标项目与参考项目的整体 AST 相似度为 19.31%，处于低位。高达 93.9% 的共同指纹源自双方共用的第三方网络协议栈 `smoltcp`，剥离此公共依赖后，核心自主代码的相似度极低。在操作系统核心板块（进程管理、内存管理、中断处理），两者展现出显著的架构差异：目标项目采用 per-CPU 全局原子变量和 `ProcessManager` 模型，并独立支持 LoongArch64 架构；参考项目则采用集中式 `PROCESSOR` 管理。大量同名函数由 POSIX 标准或 Rust 语言惯用法决定。因此，现有 AST 证据无法支持抄袭或直接复用的指控，两项目应判定为独立开发。

------------------------------

