# 🛡️ OS 内核赛自动化代码级架构审计报告

**审计时间**: `2026-06-27 17:38:00`
**参赛作品名称**: `2026-T202610183999672-吉林大学`
**年份**: `2026`
**学校**: `吉林大学`
**队伍名称**: `T202610183999672`
**比赛名字**: `OS-Competition`
**比赛子赛道**: `Kernel-Implementation`
**仓库地址**: `https://gitlab.eduxiji.net/educg-group-43501-3132633/T202610183999672-2275`
**推测开发语言**: `Rust / C (由AST探针自动识别)`
**特征函数量**: `8857`

## ⚔️ 底层代码同源性对比 (AST Homology)
> 本部分由专门的代码审查智能体，对该项目与符合年份条件的历届最高相似度项目进行控制流与底层调用对比。新版报告会先给出确定性的全模块覆盖矩阵，再给出 Agent 的分模块解释，避免只围绕少量抽样函数展开。

### 🏆 Top 4 底层对标: vs `2025-广告位招租-西安电子科技大学`（西安电子科技大学 / 广告位招租）

#### 对方作品信息

**对方年份**: `2025`
**对方学校**: `西安电子科技大学`
**对方队伍名称**: `广告位招租`
**对方比赛名字**: `OS-Competition`
**对方比赛子赛道**: `Kernel-Implementation`
**对方仓库地址**: `https://gitlab.eduxiji.net/educg-group-36002-2710490/T202510701995284-398`

> 双向覆盖率：共同 AST 指纹: 998；唯一 AST 指纹: 目标项目 6561，参考项目 1946；覆盖率: 目标项目 15.21%，参考项目 51.28%

## 全模块对比总览（确定性证据）

> 总体覆盖率：共同 AST 指纹: 998；唯一 AST 指纹: 目标项目 6561，参考项目 1946；覆盖率: 目标项目 15.21%，参考项目 51.28%

### 1. 模块覆盖矩阵
| 模块 | 目标函数数 | 参考函数数 | 共同唯一 AST 指纹 | Target 覆盖 | Base 覆盖 | 状态 |
| :--- | ---: | ---: | ---: | ---: | ---: | :--- |
| 进程管理 | 925 | 131 | 14 | 1.82% | 13.08% | 双方存在且有结构重合 |
| 任务调度/异步运行时 | 465 | 139 | 42 | 11.05% | 33.07% | 双方存在且有结构重合 |
| 内存管理 | 1286 | 630 | 180 | 16.9% | 40.09% | 双方存在且有结构重合 |
| 中断/异常处理 | 55 | 47 | 36 | 75.0% | 90.0% | 双方存在且有结构重合 |
| 系统调用 | 258 | 64 | 26 | 12.68% | 50.98% | 双方存在且有结构重合 |
| 文件系统 | 1045 | 530 | 115 | 13.56% | 33.33% | 双方存在且有结构重合 |
| 设备驱动 | 122 | 47 | 34 | 29.06% | 72.34% | 双方存在且有结构重合 |
| 网络协议栈 | 360 | 91 | 7 | 2.64% | 14.58% | 双方存在且有结构重合 |
| 文件系统/第三方库 | 437 | 366 | 136 | 38.2% | 47.72% | 双方存在且有结构重合 |
| 网络协议栈/第三方库 | 2387 | 21 | 21 | 1.16% | 100.0% | 双方存在且有结构重合 |
| 第三方库 | 1230 | 869 | 322 | 45.48% | 81.11% | 双方存在且有结构重合 |
| 通用模块/其他 | 287 | 226 | 99 | 58.93% | 86.84% | 双方存在且有结构重合 |

### 2. 分模块证据清单

#### 进程管理
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `os/src/task/processor.rs#run_tasks` | `os/src/task/processor.rs#run_tasks` | 同板块同名 | Arc::as_ptr, CURRENT_EGID.store, CURRENT_EUID.store, CURRENT_GID.store, CURRENT_PARENT_PID.store, CURRENT_PGID.store ...(+74) | PROCESSOR.lock, Some, __switch, do_wake_expired, drop, fetch_task ...(+2) |
| `os/src/syscall/process/futex.rs#sys_futex` | `os/src/syscall/process.rs#sys_futex` | 同板块同名 | FutexOption::from_bits_truncate, UserPtr::new, UserPtr::new(uaddr2 as *const u32).read, current_futex_key, current_task_ref, current_task_ref()
                        .unwrap ...(+23) | FutexOption::from_bits_truncate, current_task, current_task().unwrap, do_futex_wait, drop, info! ...(+15) |
| `os/src/syscall/process/ids.rs#sys_prlimit` | `os/src/syscall/process.rs#sys_prlimit` | 同板块同名 | Resource::from_primitive, UserPtr::new, UserPtr::new(new_limit).read, UserPtrMut::new, UserPtrMut::new(old_limit).write, UserPtrMut::new(old_limit).write(token, &limit).is_err ...(+15) | Resource::from_primitive, assert!, copy_from_user, copy_from_user(token, new_limit, rlimit).is_err, copy_to_user, copy_to_user(
                        token,
                        &(RLimit {
                            rlim_cur: MAX_FILE_SIZE,
                            rlim_max: MAX_FILE_SIZE,
                        }),
                        old_limit,
                    )
                    .is_err ...(+19) |
| `os/src/syscall/process/ipc.rs#sys_shmctl` | `os/src/syscall/process.rs#sys_shmctl` | 同板块同名 | LinuxIpcPerm::new, SHM_REGISTRY.lock, SHM_REGISTRY.lock().highest_index, can_modify_shm_segment, copy_from_user, copy_to_user ...(+10) | buf.is_null, info!, warn! |

#### 任务调度/异步运行时
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `os/src/net/syscall/getsockopt.rs#sys_getsockopt` | `os/src/syscall/net.rs#sys_getsockopt` | 同板块同名 | TcpInfo::new, TimeVal::new, UserBufferWriter::new, UserPtrMut::<u32>::from_addr, buf.write_from, buf.write_from(info_bytes).is_err ...(+46) | buf.copy_from_slice, congestion.as_bytes, congestion.len, core::mem::size_of::<u32>, core::slice::from_raw_parts_mut, current_task ...(+10) |
| `os/src/net/syscall/setsockopt.rs#sys_setsockopt` | `os/src/syscall/net.rs#sys_setsockopt` | 同板块同名 | (optlen as usize).min, (size as usize).min, (size as usize).min(256 * 1024).max, UserBufferReader::new, UserBufferReader::new(token, optval_ptr as *const u8, optlen as usize).is_err, UserPtr::<TimeVal>::from_addr ...(+29) | current_task, current_task().unwrap, get_socket!, log::debug!, log::warn!, socket.set_keep_alive ...(+6) |
| `os/src/net/syscall/bind.rs#sys_bind` | `os/src/syscall/net.rs#sys_bind` | 同板块同名 | ABSTRACT_TABLE
                        .create_abstract_name_bytes, ABSTRACT_TABLE
                        .create_abstract_name_bytes(&name, socket.clone())
                        .map, ABSTRACT_TABLE
                        .create_abstract_name_bytes(&name, socket.clone())
                        .map(|_| 0)
                        .unwrap_or_else, Arc::downgrade, Endpoint::Ip, Endpoint::Netlink ...(+43) | FileDescriptor::new, address::listen_endpoint, address::listen_endpoint(addr_buf).unwrap, current_task, current_task().unwrap, current_task().unwrap().files.lock ...(+14) |
| `os/src/syscall/fs.rs#sys_splice` | `os/src/syscall/fs.rs#sys_splice` | 同板块同名 | Some, UserPtrMut::new, UserPtrMut::new(off_in).read, UserPtrMut::new(off_in).write, UserPtrMut::new(off_in).write(token, &offset).is_err, UserPtrMut::new(off_out).read ...(+33) | Some, Vec::<u8>::with_capacity, buffer.as_mut_slice, buffer.as_slice, buffer.set_len, current_task ...(+17) |

#### 内存管理
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `os/src/syscall/mod.rs#syscall` | `os/src/syscall/mod.rs#syscall` | 同板块同名 | Errno::try_from, Some, [
            //black list
            SYSCALL_YIELD,
            // SYSCALL_READ,
            SYSCALL_WRITE,
            SYSCALL_GETDENTS64,
            SYSCALL_READV,
            SYSCALL_WRITEV,
            SYSCALL_PSELECT6,
            SYSCALL_SIGACTION,
            SYSCALL_SIGPROCMASK,
            // SYSCALL_WAIT4,
            // SYSCALL_GETPPID,
            SYSCALL_CLOCK_GETTIME,
        ]
        .contains, args.len, crate::fs::ext4::counters::sys_ext4_counters, crate::task::any_seccomp_enabled ...(+269) | Errno::try_from, [
            //black list
            SYSCALL_YIELD,
            // SYSCALL_READ,
            SYSCALL_WRITE,
            SYSCALL_GETDENTS64,
            SYSCALL_READV,
            SYSCALL_WRITEV,
            SYSCALL_PSELECT6,
            SYSCALL_SIGACTION,
            SYSCALL_SIGPROCMASK,
            // SYSCALL_WAIT4,
            // SYSCALL_GETPPID,
            SYSCALL_CLOCK_GETTIME,
        ]
        .contains, args.len, crate::task::current_task, crate::task::current_task()
                .unwrap, crate::task::current_task()
                .unwrap()
                .acquire_inner_lock ...(+113) |
| `os/src/task/task.rs#sys_clone` | `os/src/task/task.rs#sys_clone` | 同板块同名 | AddressSpace::from_existing_user, Arc::new, AtomicBool::new, AtomicI32::new, AtomicUsize::new, Err ...(+95) | Arc::downgrade, Arc::new, Futex::new, ITimerVal::new, MemorySet::from_existing_user, Mutex::new ...(+55) |
| `os/src/syscall/mod.rs#syscall_name` | `os/src/syscall/mod.rs#syscall_name` | 同板块同名 | 内部闭环逻辑 | 内部闭环逻辑 |
| `os/src/hal/arch/loongarch64/trap/mod.rs#trap_handler` | `os/src/hal/arch/loongarch64/trap/mod.rs#trap_handler` | 同板块同名 | (0..sz).rev, BadV::read, BadV::read().get_vaddr, ERA::read, ERA::read().next_ins, ERA::read().next_ins().write ...(+63) | (0..sz).rev, BadV::read, BadV::read().get_vaddr, ERA::read, ERA::read().next_ins, ERA::read().next_ins().write ...(+55) |

#### 中断/异常处理
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `os/src/hal/arch/loongarch64/trap/mod.rs#trap_from_kernel` | `os/src/hal/arch/loongarch64/trap/mod.rs#trap_from_kernel` | 同板块同名 | ((addr + i) as *mut u8).read_unaligned, ((addr + i) as *mut u8).write_unaligned, (0..sz).rev, BadV::read, BadV::read().get_vaddr, EStat::read ...(+16) | ((addr + i) as *mut u8).read_unaligned, ((addr + i) as *mut u8).write_unaligned, (0..sz).rev, BadV::read, BadV::read().get_vaddr, EStat::read ...(+14) |
| `os/src/hal/arch/loongarch64/trap/mem_access.rs#get_addr` | `os/src/hal/arch/loongarch64/trap/mem_access.rs#get_addr` | 同板块同名同结构 | self.get_op_code, self.get_op_code().map, self.get_rj_num, self.get_rk_num, self.get_si12, self.get_si14 | self.get_op_code, self.get_op_code().map, self.get_rj_num, self.get_rk_num, self.get_si12, self.get_si14 |
| `os/src/hal/arch/loongarch64/trap/mem_access.rs#get_size` | `os/src/hal/arch/loongarch64/trap/mem_access.rs#get_size` | 同板块同名同结构 | 内部闭环逻辑 | 内部闭环逻辑 |
| `os/src/hal/arch/loongarch64/trap/mem_access.rs#is_store` | `os/src/hal/arch/loongarch64/trap/mem_access.rs#is_store` | 同板块同名同结构 | 内部闭环逻辑 | 内部闭环逻辑 |

#### 系统调用
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `os/src/net/socket/inet/stream/mod.rs#connect` | `os/src/net/udp.rs#connect` | 同板块同名 | Closed::new, EPollEvent::EPOLLERR.bits, EPollEvent::EPOLLOUT.bits, Err, Inner::Closed, Inner::Connecting ...(+23) | Err, IpListenEndpoint::from, NET_INTERFACE.poll, NET_INTERFACE.udp_socket, Ok, SocketAddrv4::new ...(+11) |
| `os/src/net/socket/unix/stream/mod.rs#bind` | `os/src/net/udp.rs#bind` | 同板块同名 | Err, Ok, Some, UnixEndpointBound::Abstract, UnixEndpointBound::Path, init.addr.is_some ...(+5) | NET_INTERFACE.poll, NET_INTERFACE.udp_socket, Ok, log::info!, socket.bind, socket.bind(addr).ok ...(+1) |
| `os/src/net/socket/inet/stream/mod.rs#bind` | `os/src/net/tcp.rs#bind` | 同板块同名 | Closed::new, Err, Inner::Closed, IpEndpoint::new, Ok, Some ...(+8) | Ok, info!, self.inner.lock |
| `os/src/net/socket/unix/datagram/mod.rs#bind` | `os/src/net/unix.rs#bind` | 同板块同名 | BIND_TABLE.register, Err, Ok, Some, UnixEndpointBound::Abstract, UnixEndpointBound::Path ...(+8) | todo! |

#### 文件系统
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `os/src/fs/tmpfs/mod.rs#rename` | `os/src/fs/directory_tree.rs#rename` | 同板块同名 | Arc::ptr_eq, Err, Ok, String::from, child.0.lock, drop ...(+30) | Arc::downgrade, Arc::new, Err, Mutex::new, Ok, ROOT.cd_comp ...(+41) |
| `os/src/fs/ramfs/mod.rs#rename` | `os/src/fs/file_descriptor.rs#rename` | 同板块同名 | Err, Ok, String::from, child.0.lock, drop, existing.0.lock ...(+11) | DirectoryTreeNode::rename, Err, [new_inode.get_cwd(), new_path.to_string()].join, [old_inode.get_cwd(), old_path.to_string()].join, new_fd.file.get_dirtree_node, new_fd.file.is_file ...(+8) |
| `os/src/fs/vfs/file.rs#get_dirent` | `os/src/fs/fat32/fat_osinode.rs#get_dirent` | 同板块同名 | Err, Ok, Vec::new, child.metadata, core::mem::size_of::<crate::fs::dirent::Dirent>, crate::fs::dirent::Dirent::new ...(+14) | Dirent::new, assert!, core::mem::size_of::<Dirent>, log::debug!, name.as_str, self
            .inner
            .dirent_info_lock ...(+7) |
| `os/src/syscall/fs.rs#sys_utimensat` | `os/src/syscall/fs.rs#sys_utimensat` | 同板块同名 | Some, String::new, TimeSpec::from_s, UserPtr::new, UserPtr::new(times).read, UtimensatFlags::from_bits ...(+13) | Some, String::new, TimeSpec::now, UtimensatFlags::from_bits, __openat, copy_from_user ...(+10) |

#### 设备驱动
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `os/src/net/config.rs#_poll` | `os/src/net/config.rs#_poll` | 同板块同名 | Instant::from_millis, TCP_SOCKETS_TO_REMOVE.lock, TCP_SOCKETS_TO_REMOVE.lock().push, UDP_SOCKETS_TO_REMOVE.lock, crate::net::TCP_SOCKETS.lock, crate::net::neighbour::CURRENT_POLL_IFINDEX.lock ...(+29) | Instant::from_millis, current_time_duration, current_time_duration().as_millis, inner.iface.poll, log::debug!, self.inner_handler |
| `os/src/drivers/block/sata_blk.rs#pci_init` | `os/src/drivers/block/sata_blk.rs#pci_init` | 同板块同名同结构 | AHCI::new, Some, Status::empty, dev.bars.iter, dev.bars.iter().enumerate, dev.bars.iter().enumerate().for_each ...(+3) | AHCI::new, Some, Status::empty, dev.bars.iter, dev.bars.iter().enumerate, dev.bars.iter().enumerate().for_each ...(+3) |
| `os/src/drivers/block/sata_blk.rs#enable` | `os/src/drivers/block/sata_blk.rs#enable` | 同板块同名同结构 | am.read16, am.write32 | am.read16, am.write32 |
| `os/src/drivers/block/mod.rs#block_device_test` | `os/src/drivers/block/mod.rs#block_device_test` | 同板块同名同结构 | BLOCK_DEVICE.clone, assert_eq!, block_device.read_block, block_device.write_block, println!, write_buffer.iter_mut | BLOCK_DEVICE.clone, assert_eq!, block_device.read_block, block_device.write_block, println!, write_buffer.iter_mut |

#### 网络协议栈
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `os/src/net/socket/inet/stream/mod.rs#set_recv_buf_size` | `os/src/net/tcp.rs#set_recv_buf_size` | 同板块同名 | init.resize_buffers, sc.rx_cap.store, self.inner.lock | self.inner.lock |
| `os/src/net/socket/inet/stream/mod.rs#shutdown` | `os/src/net/tcp.rs#shutdown` | 同板块同名 | inner.shutdown, result.is_ok, self.inner.lock, self.read_shutdown.store, self.write_shutdown.store | NET_INTERFACE.poll, NET_INTERFACE.tcp_socket, Ok, info!, socket.abort, socket.close |
| `os/src/net/socket/inet/stream/mod.rs#set_send_buf_size` | `os/src/net/tcp.rs#set_send_buf_size` | 同板块同名 | init.resize_buffers, self.inner.lock | self.inner.lock |
| `os/src/net/config.rs#tcp_socket` | `os/src/net/config.rs#tcp_socket` | 同板块同名 | Some, f, inner.as_mut, inner_ref.stack_mut, self.inner.lock, stack.sockets.get_mut::<tcp::Socket> | f, self
            .inner
            .lock, self
            .inner
            .lock()
            .as_mut, self
            .inner
            .lock()
            .as_mut()
            .unwrap, self
            .inner
            .lock()
            .as_mut()
            .unwrap()
            .sockets
            .get_mut::<tcp::Socket> |

#### 文件系统/第三方库
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `os/src/fs/ext4/extent.rs#ext_remove_leaf` | `os/src/fs/ext4/extent.rs#ext_remove_leaf` | 同板块同名 | Block::load_inode_root_block, Ext4Extent::default, Ok, ex.get_actual_len, ex.get_pblock, ex.is_unwritten ...(+17) | Block::load_inode_root_block, Block::load_offset, Ext4Extent::default, Ok, ex.get_actual_len, ex.get_pblock ...(+14) |
| `os/src/fs/ext4/extent.rs#extent_remove_space` | `os/src/fs/ext4/extent.rs#extent_remove_space` | 同板块同名 | Ext4Extent::default, Ok, ex.get_actual_len, ex.get_first_block, ex.get_pblock, ex.is_unwritten ...(+12) | Block::load_offset, Ext4Extent::default, Ok, ex.get_actual_len, ex.get_first_block, ex.get_pblock ...(+14) |
| `os/src/fs/ext4/file.rs#write_at` | `os/src/fs/ext4/file.rs#write_at` | 同板块同名 | Block::load_offset, Ok, block.sync_blk_to_disk, block.write_offset, drop, inode_ref.inode.set_size ...(+11) | Block::load_offset, Ok, block.sync_blk_to_disk, block.write_offset, drop, inode_ref.inode.set_size ...(+10) |
| `os/src/fs/ext4/balloc.rs#balloc_alloc_block` | `os/src/fs/ext4/balloc.rs#balloc_alloc_block` | 同板块同名 | Err, Ok, block_group.get_block_bitmap_block, block_group.get_free_blocks_count, block_group.set_block_group_balloc_bitmap_csum, ext4_bmap_bit_find_clr ...(+15) | Block::load_offset, Err, Ext4BlockGroup::load_new, Ok, block_group
                        .set_block_group_balloc_bitmap_csum, block_group.get_block_bitmap_block ...(+17) |

#### 网络协议栈/第三方库
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `dependency/smoltcp/src/phy/fault_injector.rs#receive` | `dependency/dep_iso/isomorphic_drivers/src/net/ethernet/intel/e1000.rs#receive` | 同板块同名 | <D::RxToken<'_> as phy::RxToken>::meta, Some, buffer.len, net_trace!, self.inner.receive, self.rx_buf.len ...(+5) | Some, buffer.to_vec, recv_desc.status.get_bit, recv_desc.status.set_bit, self.recv_queue.len, self.registers[E1000_RDT].read ...(+5) |
| `dependency/dep_iso/src/net/ethernet/intel/ixgbe.rs#msend` | `dependency/dep_iso/isomorphic_drivers/src/net/ethernet/intel/ixgbe.rs#msend` | 同板块同名同结构 | data.len, data[data_index].len, fence, ixgbe[IXGBE_TDT + IXGBE_TDT_GAP * queue].read, ixgbe[IXGBE_TDT + IXGBE_TDT_GAP * queue].write, send_desc.status.get_bit ...(+2) | data.len, data[data_index].len, fence, ixgbe[IXGBE_TDT + IXGBE_TDT_GAP * queue].read, ixgbe[IXGBE_TDT + IXGBE_TDT_GAP * queue].write, send_desc.status.get_bit ...(+2) |
| `dependency/smoltcp/src/socket/dhcpv4.rs#recv` | `dependency/dep_iso/isomorphic_drivers/src/net/ethernet/intel/ixgbe.rs#recv` | 同板块同名 | Ok::<_, ()>, PollAt::Time, assert_eq!, net_trace!, panic!, reprs.get ...(+3) | Some, buffer.to_vec, ixgbe[IXGBE_RDT].read, ixgbe[IXGBE_RDT].write, recv_desc.status_error.get_bit, recv_desc.status_error.set_bit ...(+1) |
| `dependency/dep_iso/src/net/ethernet/intel/e1000.rs#send` | `dependency/dep_iso/isomorphic_drivers/src/net/ethernet/intel/e1000.rs#send` | 同板块同名同结构 | assert!, buffer.len, fence, self.registers[E1000_TDT].read, self.registers[E1000_TDT].write, self.send_queue.len ...(+2) | assert!, buffer.len, fence, self.registers[E1000_TDT].read, self.registers[E1000_TDT].write, self.send_queue.len ...(+2) |

#### 第三方库
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `dependency/dep_pci/pci/src/lib.rs#probe_function` | `dependency/dep_pci/pci/src/lib.rs#probe_function` | 同板块同名同结构 | BAR::decode, BridgeControl::from_bits_truncate, Command::from_bits_truncate, DeviceKind::CardbusBridge, DeviceKind::Device, DeviceKind::PciBridge ...(+6) | BAR::decode, BridgeControl::from_bits_truncate, Command::from_bits_truncate, DeviceKind::CardbusBridge, DeviceKind::Device, DeviceKind::PciBridge ...(+6) |
| `dependency/virtio-drivers/src/queue.rs#recycle_descriptors` | `dependency/virtio-drivers/src/queue.rs#recycle_descriptors` | 同板块同名 | Box::from_raw, H::unshare, InputOutputIter::new, InputOutputIter::new(inputs, outputs).enumerate, Some, assert_eq! ...(+17) | desc.flags.read, desc.next.read, desc.next.write, flags.contains |
| `dependency/virtio-drivers/src/device/blk.rs#flush` | `dependency/virtio-drivers/src/gpu.rs#flush` | 同板块同名 | (BlkFeature::RING_INDIRECT_DESC | BlkFeature::FLUSH).bits, Arc::new, Mutex::new, QUEUE_SIZE.into, ReadOnly::new, State::new ...(+12) | Ok, self.resource_flush, self.transfer_to_host_2d |
| `dependency/riscv/descriptor/generator.rs#generate` | `dependency/riscv/descriptor/generator.rs#generate` | 同板块同名同结构 | String::new, bf.generate_enum, format!, self.bfs.iter, write!, write!(&mut bit_sets, "{}", bf.generate_bitops()).unwrap ...(+2) | String::new, bf.generate_enum, format!, self.bfs.iter, write!, write!(&mut bit_sets, "{}", bf.generate_bitops()).unwrap ...(+2) |

#### 通用模块/其他
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `os/src/hal/arch/loongarch64/boot.rs#_start` | `user/src/lib.rs#_start` | 同板块同名 | asm! | _parameter, asm!, core::arch::asm!, unreachable! |
| `user/src/lib.rs#_parameter` | `user/src/lib.rs#_parameter` | 同板块同名同结构 | ((argv + i * core::mem::size_of::<usize>()) as *const usize).read_volatile, ((str_start + *i) as *const u8).read_volatile, (0usize..)
            .find, (0usize..)
            .find(|i| unsafe { ((str_start + *i) as *const u8).read_volatile() == 0 })
            .unwrap, HEAP.lock, HEAP.lock()
            .init ...(+10) | ((argv + i * core::mem::size_of::<usize>()) as *const usize).read_volatile, ((str_start + *i) as *const u8).read_volatile, (0usize..)
            .find, (0usize..)
            .find(|i| unsafe { ((str_start + *i) as *const u8).read_volatile() == 0 })
            .unwrap, HEAP.lock, HEAP.lock()
            .init ...(+10) |
| `os/src/hal/arch/loongarch64/time.rs#get_timer_freq_first_time` | `os/src/hal/arch/loongarch64/time.rs#get_timer_freq_first_time` | 同板块同名同结构 | cfg5.get_bits, config::CPUCfg4::read, config::CPUCfg4::read().get_bits, config::CPUCfg5::read, println! | cfg5.get_bits, config::CPUCfg4::read, config::CPUCfg4::read().get_bits, config::CPUCfg5::read, println! |
| `os/src/console.rs#log_init` | `os/src/console.rs#log_init` | 同板块同名 | log::set_logger, log::set_logger(&LOGGER).unwrap, log::set_max_level, option_env!, println! | log::set_logger, log::set_logger(&LOGGER).unwrap, log::set_max_level, option_env! |


#### 1. 宏观判定
- **定性结论**: **独立开发** (相似度: 13.29%，共同 AST 指纹: 998；唯一 AST 指纹: 目标项目 6561，参考项目 1946；覆盖率: 目标项目 15.21%，参考项目 51.28%)

#### 2. 全模块覆盖矩阵解读
- **进程管理**
  - 双方存在且有结构重合。
  - 共同 AST 指纹仅 14 个，目标覆盖率 1.82%，参考覆盖率 13.08%。
  - 极低的覆盖率表明目标项目在该板块拥有完全独立的实现体系，其 98.18% 的函数为独有指纹。
  - **证据强度: 弱**。同名函数源于 POSIX 系统调用标准，核心调度与任务管理架构存在本质差异。

- **任务调度/异步运行时**
  - 双方存在且有结构重合。
  - 共同 AST 指纹 42 个，目标覆盖率 11.05%，参考覆盖率 33.07%。
  - 覆盖率差异源于参考项目代码量较小，且双方共享了部分网络套接字处理的通用逻辑。
  - **证据强度: 弱**。重合点集中在网络系统调用接口，核心调度器实现路径不同。

- **内存管理**
  - 双方存在且有结构重合。
  - 共同 AST 指纹 180 个，目标覆盖率 16.9%，参考覆盖率 40.09%。
  - 目标项目 83.1% 的内存管理函数为独有指纹，表明其拥有独立的内存管理子系统。
  - **证据强度: 中**。部分重合源于共享的 ext4 文件系统库及 LoongArch 架构的 trap 处理逻辑，属于公共依赖与架构强制。

- **中断/异常处理**
  - 双方存在且有结构重合。
  - 共同 AST 指纹 36 个，目标覆盖率 75.0%，参考覆盖率 90.0%。
  - 高覆盖率源于双方均基于 LoongArch 架构实现，`trap_handler`、`mem_access` 等模块的指令解码逻辑由硬件手册强制规定。
  - **证据强度: 弱**。高相似度是特定硬件架构（LoongArch）的必然结果，非代码复用。

- **系统调用**
  - 双方存在且有结构重合。
  - 共同 AST 指纹 26 个，目标覆盖率 12.68%，参考覆盖率 50.98%。
  - 目标项目 87.32% 的系统调用实现为独有指纹。
  - **证据强度: 弱**。重合点多为跨模块的网络套接字 `connect`/`bind` 操作，属于 POSIX 标准接口。

- **文件系统**
  - 双方存在且有结构重合。
  - 共同 AST 指纹 115 个，目标覆盖率 13.56%，参考覆盖率 33.33%。
  - 目标项目 86.44% 的文件系统函数为独有指纹。
  - **证据强度: 中**。重合主要来自共享的 ext4 第三方库及部分 VFS 接口，核心文件系统实现差异显著。

- **设备驱动**
  - 双方存在且有结构重合。
  - 共同 AST 指纹 34 个，目标覆盖率 29.06%，参考覆盖率 72.34%。
  - 高覆盖率源于双方均使用了相同的第三方驱动库（如 `virtio-drivers`）和 PCI 枚举逻辑。
  - **证据强度: 弱**。重合点均为第三方库或标准 PCI 设备初始化流程。

- **网络协议栈**
  - 双方存在且有结构重合。
  - 共同 AST 指纹 7 个，目标覆盖率 2.64%，参考覆盖率 14.58%。
  - 极低的覆盖率表明双方网络协议栈实现几乎完全不同。
  - **证据强度: 未形成**。

- **第三方库/公共依赖**
  - 双方存在且有大量结构重合。
  - 共同 AST 指纹 479 个，目标覆盖率 11.86%，参考覆盖率 38.17%。
  - 这是双方项目相似度的主要来源。共同指纹集中在 `ext4`、`virtio-drivers`、`smoltcp`、`dep_pci`、`riscv` 等公共第三方库。
  - **证据强度: 未形成**。第三方库的同源指纹不能作为项目间抄袭的证据。

#### 3. 底层 AST 关键证据指纹表
| 所属板块 | 目标项目路径 | 参考项目路径 | 证据类型 | 技术说明 |
| :--- | :--- | :--- | :--- | :--- |
| 中断/异常处理 | `os/src/hal/arch/loongarch64/trap/mod.rs#trap_from_kernel` | `os/src/hal/arch/loongarch64/trap/mod.rs#trap_from_kernel` | 架构强制同源 | 内核态缺页异常的逐字节拷贝逻辑，由 LoongArch 架构的 `BadV`、`EStat` 寄存器处理规范强制决定。 |
| 中断/异常处理 | `os/src/hal/arch/loongarch64/trap/mem_access.rs#get_addr` | `os/src/hal/arch/loongarch64/trap/mem_access.rs#get_addr` | 架构强制同源 | 指令操作数地址解码逻辑，`get_op_code`、`get_rj_num` 等调用链由 LoongArch 指令编码格式唯一确定。 |
| 内存管理 | `os/src/hal/arch/loongarch64/trap/mod.rs#trap_handler` | `os/src/hal/arch/loongarch64/trap/mod.rs#trap_handler` | 架构强制同源 | 用户态 Trap 处理入口，`BadV::read().get_vaddr` 和 `ERA::read().next_ins` 的调用序列是 LoongArch 特权架构的标准处理流程。 |
| 设备驱动 | `os/src/drivers/block/sata_blk.rs#pci_init` | `os/src/drivers/block/sata_blk.rs#pci_init` | 第三方库/公共依赖 | 完全相同的 AHCI 控制器 PCI 初始化序列，源于共享的 `ahci` 驱动库。 |
| 设备驱动 | `os/src/drivers/block/sata_blk.rs#enable` | `os/src/drivers/block/sata_blk.rs#enable` | 第三方库/公共依赖 | 完全相同的 `am.read16` / `am.write32` 调用，是 `ahci` 库的标准寄存器操作。 |
| 第三方库 | `dependency/virtio-drivers/src/queue.rs#recycle_descriptors` | `dependency/virtio-drivers/src/queue.rs#recycle_descriptors` | 第三方库/公共依赖 | 共享的 `virtio-drivers` 库中 virtqueue 描述符回收逻辑。 |
| 第三方库 | `dependency/dep_pci/pci/src/lib.rs#probe_function` | `dependency/dep_pci/pci/src/lib.rs#probe_function` | 第三方库/公共依赖 | 完全相同的 PCI 设备探测与 BAR 解码逻辑，源于共享的 `dep_pci` 库。 |
| 第三方库 | `dependency/riscv/descriptor/generator.rs#generate` | `dependency/riscv/descriptor/generator.rs#generate` | 第三方库/公共依赖 | 完全相同的 RISC-V 寄存器描述符生成器，属于共享的代码生成工具。 |
| 通用模块/其他 | `user/src/lib.rs#_parameter` | `user/src/lib.rs#_parameter` | 公共范式 | 用户态程序参数解析的 `_parameter` 函数，其指针算术和 volatile 读取模式是 Rust 裸机环境下的标准范式。 |
| 通用模块/其他 | `os/src/hal/arch/loongarch64/time.rs#get_timer_freq_first_time` | `os/src/hal/arch/loongarch64/time.rs#get_timer_freq_first_time` | 架构强制同源 | 通过 `CPUCfg4`/`CPUCfg5` 寄存器读取 LoongArch 定时器频率的标准方法。 |

#### 4. 核心代码调用流对比剖析
* **【进程管理/任务调度核心】**
* **底层技术析理**: 目标项目采用基于 `CURRENT_TASK_PTR` 原子指针的无锁当前任务获取模型，并通过 `Arc::from_raw` 和 `Arc::increment_strong_count` 手动管理引用计数。参考项目则采用基于 `PROCESSOR.lock()` 的全局锁模型获取当前任务。在调度切换上，目标项目在 `__switch` 之外增加了 `SCHED_SWITCHES` 统计和 `record_context_switch` 性能追踪，而参考项目仅执行核心切换。两者在同步策略和性能监控架构上存在根本性差异。
* **代码溯源**:
  - 目标项目: `os/src/task/processor.rs#current_task` `os/src/task/processor.rs#schedule`
  - 参考项目: `os/src/task/processor.rs#current_task` `os/src/task/processor.rs#schedule`

* **【进程管理/进程退出】**
* **底层技术析理**: 目标项目的 `exit_current_and_run_next` 实现了线程组感知的退出逻辑，会遍历 `process.threads()` 并调用 `add_zombie_task` 和 `manager::remove_tasks_from_queues` 进行多线程清理。参考项目的实现则基于任务队列，使用 `VecDeque::new` 和 `exit_list.push_back` 管理退出任务。两者在进程模型（线程组 vs. 任务队列）和退出资源回收策略上完全不同。
* **代码溯源**:
  - 目标项目: `os/src/task/mod.rs#exit_current_and_run_next`
  - 参考项目: `os/src/task/mod.rs#exit_current_and_run_next`

* **【内存管理/系统调用】**
* **底层技术析理**: 目标项目在 `sys_brk` 和 `sys_mprotect` 中采用进程级虚拟内存管理模型，通过 `task.process.vm().lock()` 访问地址空间。参考项目则采用任务级模型，直接通过 `task.vm.lock()` 访问。目标项目还引入了 `parse_mmap_prot` 进行权限解析，而参考项目直接传递参数。这反映了双方在内存管理架构上的“进程-任务”分离程度不同。
* **代码溯源**:
  - 目标项目: `os/src/syscall/process/mm.rs#sys_brk` `os/src/syscall/process/mm.rs#sys_mprotect`
  - 参考项目: `os/src/syscall/process.rs#sys_brk` `os/src/syscall/process.rs#sys_mprotect`

* **【文件系统/公共依赖】**
* **底层技术析理**: 双方在 `ext4` 文件系统模块（如 `ext_remove_leaf`、`write_at`）上表现出高度一致性，调用链均包含 `Block::load_inode_root_block`、`ex.get_actual_len` 等。这是因为双方均集成了相同的开源 `ext4` 库。这种相似性属于第三方库复用，不代表项目间的抄袭。
* **代码溯源**:
  - 目标项目: `os/src/fs/ext4/extent.rs#ext_remove_leaf`
  - 参考项目: `os/src/fs/ext4/extent.rs#ext_remove_leaf`

#### 5. 单方模块与降权项
- **单方模块**: 目标项目独有的 `网络协议栈/第三方库` 模块包含 2387 个函数，远超参考项目的 21 个，表明目标项目集成了大量独立的第三方网络库（如 `smoltcp` 的更多组件），这是其代码量巨大的主要原因之一。
- **降权项**:
  - **第三方库/公共依赖**: 所有位于 `dependency/` 路径下的证据，如 `virtio-drivers`、`dep_pci`、`smoltcp`、`riscv` 等，均属于公共开源依赖，其同源指纹不能作为抄袭证据。
  - **架构强制同源**: 所有位于 `os/src/hal/arch/loongarch64/` 路径下的证据，其相似性由 LoongArch 特权架构规范强制决定，属于实现同一硬件接口的必然结果。
  - **跨板块同名函数**: 如 TCP `connect` 与 UDP `connect`、VFS `get_dirent` 与 FAT32 `get_dirent` 等，它们虽同名但实现不同子系统，不构成同板块主证据。
  - **通用 Rust 语法节点**: `Some`、`Ok`、`Err`、`unwrap`、`is_err`、`lock` 等标准库 API 调用，在任何 Rust 项目中普遍存在，不应作为特异性指纹。

#### 6. 保守结论
本次审计基于 AST 指纹比对，目标项目与参考项目的整体相似度仅为 13.29%。在核心的进程管理、内存管理等板块，目标项目覆盖率均低于 17%，展现出完全不同的架构设计，如无锁任务获取、进程级虚拟内存管理、线程组感知退出等。双方的主要相似度来源于共享的第三方开源库（ext4、virtio-drivers 等）和 LoongArch 硬件架构的强制规范，这些均属于正常的公共依赖和领域共性。目标项目拥有 6561 个独有 AST 指纹，远超参考项目的 1946 个，表明其绝大部分代码为独立开发。综合来看，现有证据不支持抄袭指控，两个项目应被判定为独立开发的作品。

------------------------------

