# 🛡️ OS 内核赛自动化代码级架构审计报告

**审计时间**: `2026-07-02 04:33:12`
**参赛作品名称**: `2026-T202610351999623-XX`
**年份**: `2026`
**学校**: `XX`
**队伍名称**: `T202610351999623`
**比赛名字**: `OS-Competition`
**比赛子赛道**: `Kernel-Implementation`
**仓库地址**: `https://gitlab.eduxiji.net/educg-group-43501-3132633/T202610351999623-1603`
**推测开发语言**: `Rust / C (由AST探针自动识别)`
**特征函数量**: `713`

## ⚔️ 底层代码同源性对比 (AST Homology)
> 本部分由专门的代码审查智能体，对该项目与符合年份条件的历届最高相似度项目进行控制流与底层调用对比。新版报告会先给出确定性的全模块覆盖矩阵，再给出 Agent 的分模块解释，避免只围绕少量抽样函数展开。

### 🏆 Top 1 底层对标: vs `2022-S0S-哈尔滨工业大学`（哈尔滨工业大学 / S0S）

#### 对方作品信息

**对方年份**: `2022`
**对方学校**: `哈尔滨工业大学`
**对方队伍名称**: `S0S`
**对方比赛名字**: `OS-Competition`
**对方比赛子赛道**: `Kernel-Implementation`
**对方仓库地址**: `https://gitlab.eduxiji.net/educg-group-14158-894147/oskernel2022-s0s`

> 双向覆盖率：共同 AST 指纹: 13；唯一 AST 指纹: 目标项目 674，参考项目 447；覆盖率: 目标项目 1.93%，参考项目 2.91%

## 全模块对比总览（确定性证据）

> 总体覆盖率：共同 AST 指纹: 13；唯一 AST 指纹: 目标项目 674，参考项目 447；覆盖率: 目标项目 1.93%，参考项目 2.91%

### 1. 模块覆盖矩阵
| 模块 | 目标函数数 | 参考函数数 | 共同唯一 AST 指纹 | Target 覆盖 | Base 覆盖 | 状态 |
| :--- | ---: | ---: | ---: | ---: | ---: | :--- |
| 进程管理 | 100 | 51 | 1 | 1.14% | 2.13% | 双方存在且有结构重合 |
| 任务调度/异步运行时 | 82 | 41 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |
| 内存管理 | 199 | 139 | 9 | 4.66% | 7.09% | 双方存在且有结构重合 |
| 中断/异常处理 | 46 | 5 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |
| 系统调用 | 43 | 35 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |
| 文件系统 | 126 | 166 | 1 | 0.82% | 0.67% | 双方存在且有结构重合 |
| 设备驱动 | 22 | 42 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |
| 网络协议栈 | 0 | 0 | 0 | 0.0% | 0.0% | 双方均未体现 |
| 文件系统/第三方库 | 48 | 0 | 0 | 0.0% | 0.0% | 仅目标项目体现 |
| 通用模块/其他 | 47 | 31 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |

### 2. 分模块证据清单

#### 进程管理
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `kernel/src/syscall/process.rs#sys_kill` | `os/src/syscall/process.rs#sys_kill` | 同板块同名 | crate::hal::irq::disable, crate::hal::irq::restore, crate::sched::manager::MANAGER.tasks[i].as_ref, crate::sched::manager::MANAGER.tasks[i].is_some, current_process_group, manager::current_pid ...(+8) | SignalFlags::from_bits, pid2task, task.inner_exclusive_access, task_ref.signals.contains, task_ref.signals.insert |
| `kernel/src/syscall/process.rs#sys_fork` | `os/src/syscall/process.rs#sys_fork` | 同板块同名 | crate::hal::irq::disable, crate::hal::irq::restore, crate::sched::manager::MANAGER.tasks[child_pid].as_mut, manager::fork_current, write_child_u32, write_user_u32 | add_task, current_task, current_task().unwrap, current_task.fork, new_task.inner_exclusive_access, new_task.inner_exclusive_access().get_trap_cx ...(+1) |
| `kernel/src/syscall/process.rs#sys_exec` | `os/src/syscall/process.rs#sys_exec` | 同板块同名 | manager::exec_current_path, parse_argv_envp, super::check::copy_cstr_from_user | Vec::new, all_data.as_slice, app_inode.read_all, args.add, args_vec.push, current_task ...(+7) |
| `kernel/src/syscall/process.rs#sys_set_tid_address` | `os/src/syscall/process.rs#sys_set_tid_address` | 同板块同名 | core::mem::size_of::<u32>, crate::hal::irq::disable, crate::hal::irq::restore, crate::sched::manager::MANAGER.tasks[pid].as_mut, manager::current_pid, super::check::validate_user_buffer_write | current_task, current_task().unwrap, current_task().unwrap().getpid, current_user_token, translated_refmut |

#### 任务调度/异步运行时
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `kernel/src/syscall/io.rs#sys_readlinkat` | `os/src/syscall/fs.rs#sys_readlinkat` | 同板块同名 | PROC_SELF_EXE.len, exe.len, exe_path
            .iter, exe_path
            .iter()
            .position, exe_path
            .iter()
            .position(|&b| b == 0)
            .unwrap_or, exe_path.len ...(+5) | "/".as_bytes, UserBuffer::new, buf.read, current_user_token, println!, translated_byte_buffer ...(+2) |
| `kernel/src/syscall/dispatch.rs#dispatch` | `os/src/syscall/mod.rs#syscall` | 同模块上下文对齐/差异观察 | crate::kwarn!, file::sys_chdir, file::sys_fchmodat, file::sys_fchownat, file::sys_fstat, file::sys_getcwd ...(+76) | println!, shutdown, sys_brk, sys_chdir, sys_clock_gettime, sys_close ...(+48) |
| `kernel/src/sched/sig_deliver.rs#setup_signal_frame` | `os/src/syscall/fs.rs#sys_pselect6` | 同模块上下文对齐/差异观察 | core::mem::size_of::<UserTrapFrame>, core::mem::size_of::<u32>, core::mem::size_of::<u64>, core::mem::size_of_val, core::ptr::read, crate::kwarn! ...(+7) | FdSet, check_readfds.is_set, check_writefds.is_set, current_task, current_task().unwrap, current_user_token ...(+13) |
| `kernel/src/syscall/io.rs#sys_statfs` | `os/src/drivers/block/sdcard.rs#init` | 同模块上下文对齐/差异观察 | core::mem::size_of::<crate::fs::StatFs>, core::slice::from_raw_parts, crate::fs::StatFs::zeroed, crate::fs::ext4::ext4_statfs_info, crate::sched::manager::current_cwd_inode, fs::inode_fs_id ...(+8) | Err, InitError::CMDFailed, InitError::CardCapacityStatusNotSet, self.CS_HIGH, self.HIGH_SPEED_ENABLE, self.end_cmd ...(+7) |

#### 内存管理
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `kernel/src/syscall/memory.rs#sys_mmap` | `os/src/syscall/process.rs#sys_mmap` | 同板块同名 | Some, VmAreaType::MmapFile, addr_space.mmap_region, crate::fs::file_inode_idx, crate::fs::inode_fs_raw, crate::kinfo! ...(+7) | MapFlags::from_bits, MapFlags::from_bits(flags).unwrap, ProtFlags::from_bits, ProtFlags::from_bits(prot).unwrap, UserBuffer::new, Vec::new ...(+20) |
| `kernel/src/syscall/io.rs#sys_pipe2` | `os/src/syscall/fs.rs#sys_pipe2` | 同板块同名 | (read_fd as i32).to_le_bytes, (write_fd as i32).to_le_bytes, FdKind::PipeRead, FdKind::PipeWrite, crate::sched::fd::set_fd_cloexec, manager::alloc_fd ...(+8) | FileClass::PIPE, Some, current_task, current_task().unwrap, current_user_token, inner.alloc_fd ...(+4) |
| `kernel/src/syscall/file.rs#sys_openat` | `os/src/syscall/fs.rs#sys_openat` | 同板块同名 | FdKind::File, crate::sched::fd::set_fd_cloexec, fs::dec_file_ref, fs::vfs_open, manager::alloc_fd, manager::resolve_dirfd ...(+1) | FileClass::OSInode, OpenFlags::from_bits, OpenFlags::from_bits(flags).unwrap, Some, current_task, current_task().unwrap ...(+6) |
| `kernel/src/mm/address_space.rs#push` | `os/src/mm/memory_set.rs#push` | 同板块同名 | (pa as *mut UserMapping).add, (pa as *mut UserMapping).add(li).write, PhysAddr::from, PhysAddr::from(f).into, PhysAddr::from(self.frames[fi]).into, alloc_frame ...(+3) | map_area.copy_data, map_area.map, self.areas.push |

#### 中断/异常处理
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `kernel/src/ipc/pipe/blocking.rs#pipe_write_blocking` | `os/src/trap/mod.rs#trap_from_kernel` | 同模块上下文对齐/差异观察 | WriteAction::Return, data.len, manager::block_current, manager::current_has_interrupting_signal, manager::current_pid, manager::wakeup ...(+3) | panic!, println! |
| `kernel/src/ipc/pipe/blocking.rs#pipe_read_blocking` | `os/src/trap/mod.rs#set_kernel_trap_entry` | 同模块上下文对齐/差异观察 | buf.len, buf.len().min, manager::block_current, manager::current_has_interrupting_signal, manager::current_pid, manager::wakeup ...(+3) | stvec::write |
| `kernel/src/arch/riscv64/trap/timer.rs#timer_v02_liveness_test` | `os/src/trap/mod.rs#set_user_trap_entry` | 同模块上下文对齐/差异观察 | core::hint::spin_loop, disable_stie, read_mtime, read_sie, restore_stie, sbi_set_timer_v02_result ...(+1) | stvec::write |
| `kernel/src/syscall/io.rs#sys_ppoll` | `os/src/trap/mod.rs#enable_timer_interrupt` | 同模块上下文对齐/差异观察 | crate::hal::time::read_ticks, manager::current_has_interrupting_signal, manager::yield_current, nfds.checked_mul, ppoll_parse_timeout, ppoll_scan_ready ...(+1) | sie::set_stimer |

#### 系统调用
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `user/init/src/main.rs#syscall` | `user/src/syscall.rs#syscall` | 同板块同名 | core::arch::asm! | asm! |
| `kernel/src/syscall/io.rs#sys_dup` | `user/src/syscall.rs#sys_dup` | 同板块同名 | manager::dup_fd | syscall |
| `kernel/src/syscall/io.rs#sys_close` | `user/src/syscall.rs#sys_close` | 同板块同名 | manager::close_fd | syscall |
| `user/init/src/main.rs#sys_waitpid` | `user/src/syscall.rs#sys_waitpid` | 同板块同名 | syscall | syscall |

#### 文件系统
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `kernel/src/syscall/io.rs#sys_ioctl` | `os/src/syscall/fs.rs#sys_ioctl` | 同板块同名 | crate::fs::devfs::devfs_ioctl, fs::file_inode_idx, fs::inode_fs_id, fs::inode_fs_inode, manager::current_fd_kind | Winsize::new, current_user_token, translated_refmut |
| `kernel/src/syscall/file.rs#sys_renameat2` | `os/src/syscall/fs.rs#sys_renameat2` | 同板块同名 | fs::vfs_rename, manager::resolve_dirfd, super::check::copy_cstr_from_user | current_user_token, rename, translated_str |
| `kernel/src/syscall/io.rs#sys_faccessat` | `os/src/syscall/fs.rs#sys_faccessat` | 同板块同名 | fs::dec_file_ref, fs::vfs_open, manager::resolve_dirfd, super::check::copy_cstr_from_user | current_user_token, open_file_at, open_file_at(dirfd, path.as_str(), OpenFlags::RDWR).is_some, path.as_str, translated_str |
| `kernel/src/syscall/file.rs#sys_mount` | `os/src/syscall/fs.rs#sys_mount` | 同板块同名 | crate::kwarn!, fs::vfs_bind_file, manager::current_cwd_inode, manager::current_pid, super::check::copy_cstr_from_user | 内部闭环逻辑 |

#### 设备驱动
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `kernel/src/drivers/virtio_blk/mod.rs#write_sector` | `os/src/drivers/block/sdcard.rs#write_sector` | 同板块同名 | buf.as_ptr, buf.len, crate::hal::irq::disable, crate::hal::irq::restore, io::block_io | (data_buf.len() / SEC_LEN).try_into, (data_buf.len() / SEC_LEN).try_into().unwrap, Err, Ok, assert!, chunk.iter ...(+9) |
| `kernel/src/drivers/virtio_blk/mod.rs#read_sector` | `os/src/drivers/block/sdcard.rs#read_sector` | 同板块同名 | buf.as_mut_ptr, buf.len, crate::hal::irq::disable, crate::hal::irq::restore, io::block_io | Err, Ok, assert!, chunk.iter_mut, chunk.iter_mut().zip, data_buf.chunks_mut ...(+6) |
| `kernel/src/drivers/virtio_blk/mod.rs#probe` | `os/src/drivers/block/sdcard.rs#get_cidregister` | 同模块上下文对齐/差异观察 | crate::kinfo!, crate::kwarn!, io::init_device, mmio_read32 | Err, Ok, self.end_cmd, self.get_response, self.read_data, self.send_cmd ...(+2) |
| `kernel/src/arch/riscv64/uart.rs#handle_irq` | `fat32/src/block_cache.rs#get_block_cache` | 同模块上下文对齐/差异观察 | RX_HEAD.load, RX_HEAD.store, RX_TAIL.load, lsr.read_volatile, rbr.read_volatile | Arc::clone, Arc::new, Arc::strong_count, BlockCache::new, RwLock::new, panic! ...(+8) |

#### 文件系统/第三方库
仅目标项目中观察到该模块代表函数，参考项目没有同模块可对齐证据：
- 目标项目：`kernel/src/fs/ext4/mod.rs#init`，调用摘要：crate::kerror!, crate::kinfo!, crate::kwarn!, disk_io::read_bytes, disk_io::u16_at, disk_io::u32_at ...(+1)
- 目标项目：`kernel/src/fs/ext4/dir.rs#dir_add_entry`，调用摘要：alloc_block, extent_append, extent_map_block, itype_to_ft, name.len, read_block ...(+7)
- 目标项目：`kernel/src/fs/ext4/dir.rs#dir_remove_entry`，调用摘要：Some, dir_blk.len, extent_map_block, name.len, read_block, read_raw_inode ...(+3)
- 目标项目：`kernel/src/fs/ext4/ops.rs#ext4_truncate`，调用摘要：(new_entries as u16).to_le_bytes, alloc::free_block, inode::read_raw_inode, inode::update_inode_size, inode::write_inode_iblock, new_len.to_le_bytes ...(+2)

#### 通用模块/其他
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `kernel/src/arch/riscv64/sbi.rs#shutdown` | `os/src/sbi.rs#shutdown` | 同板块同名 | core::arch::asm!, sbi_call | panic!, sbi_call |
| `kernel/src/arch/riscv64/sbi.rs#sbi_call` | `os/src/sbi.rs#sbi_call` | 同板块同名 | core::arch::asm! | asm! |
| `kernel/src/tty/mod.rs#write_str` | `os/src/console.rs#write_str` | 同板块同名 | Ok, tty_puts | Ok, console_putchar, s.chars |
| `kernel/src/hal/boot.rs#append_usable_region` | `user/src/bin/user_shell.rs#new` | 同模块上下文对齐/差异观察 | dtb_reserved_range, push_region, start.max | OpenFlags::empty, String::from, String::new, Vec::new, arg.as_ptr, arg.as_str ...(+37) |


#### 1. 宏观判定
- **定性结论**: 独立开发 (相似度: 1.17%，共同 AST 指纹: 13；唯一 AST 指纹: 目标项目 674，参考项目 447；覆盖率: 目标项目 1.93%，参考项目 2.91%)

#### 2. 全模块覆盖矩阵解读
- **进程管理**: 双方均存在。共同 AST 指纹仅 1 个，目标覆盖率 1.14%，参考覆盖率 2.13%。同名函数（如 `sys_fork`）的底层调用图谱完全不同，目标项目采用全局 `MANAGER` 集中调度，参考项目采用 `current_task()` 分散访问。证据强度：**弱**。
- **任务调度/异步运行时**: 双方均存在。共同 AST 指纹为 0，覆盖率均为 0.0%。控方提供的证据均为跨板块硬凑（如将 `sys_readlinkat` 与 `sys_readlinkat` 配对，但 AST 无交集），未形成有效证据。证据强度：**未形成**。
- **内存管理**: 双方均存在。共同 AST 指纹 9 个，目标覆盖率 4.66%，参考覆盖率 7.09%。存在同板块同名函数（如 `sys_mmap`），但调用链差异显著，目标项目使用 `addr_space.mmap_region`，参考项目使用 `MapFlags::from_bits` 等。证据强度：**弱**。
- **中断/异常处理**: 双方均存在。共同 AST 指纹为 0，覆盖率均为 0.0%。控方证据均为跨板块配对（如管道读写 vs 陷阱处理），不构成同板块证据。证据强度：**未形成**。
- **系统调用**: 双方均存在。共同 AST 指纹为 0，覆盖率均为 0.0%。控方证据将内核实现与用户库封装配对（如 `sys_dup` vs `sys_dup`），抽象层次不同，不构成同板块证据。证据强度：**未形成**。
- **文件系统**: 双方均存在。共同 AST 指纹 1 个，目标覆盖率 0.82%，参考覆盖率 0.67%。存在同板块同名函数（如 `sys_ioctl`），但调用链差异显著。证据强度：**弱**。
- **设备驱动**: 双方均存在。共同 AST 指纹为 0，覆盖率均为 0.0%。控方证据将 VirtIO 块设备与 SD 卡驱动配对，硬件协议完全不同，不构成同板块证据。证据强度：**未形成**。
- **网络协议栈**: 双方均未体现。证据强度：**未形成**。
- **第三方库/公共依赖**: 仅目标项目体现（`ext4` 等），参考项目无对应模块。属于目标项目独立扩展，不构成复用证据。证据强度：**未形成**。

#### 3. 底层 AST 关键证据指纹表
| 所属板块 | 目标项目路径 | 参考项目路径 | 证据类型 | 技术说明 |
| :--- | :--- | :--- | :--- | :--- |
| 进程管理 | `kernel/src/syscall/process.rs#sys_kill` | `os/src/syscall/process.rs#sys_kill` | 同板块同名 | 函数名由 POSIX 规范决定。目标项目通过 `MANAGER.tasks` 遍历和 `manager::send_signal` 实现；参考项目通过 `pid2task` 映射和 `task_ref.signals.insert` 实现。AST 调用链无交集。 |
| 进程管理 | `kernel/src/syscall/process.rs#sys_fork` | `os/src/syscall/process.rs#sys_fork` | 同板块同名 | 函数名由 POSIX 规范决定。目标项目封装至 `manager::fork_current` 并引入 `hal::irq` 中断保护；参考项目直接操作 `current_task().fork` 和 `trap_cx`。AST 拓扑完全不同。 |
| 进程管理 | `kernel/src/syscall/process.rs#sys_exec` | `os/src/syscall/process.rs#sys_exec` | 同板块同名 | 函数名由 POSIX 规范决定。目标项目将核心逻辑黑盒化至 `manager::exec_current_path`；参考项目暴露了从 inode 读取到内存空间切换的完整流水线。抽象层次不同。 |
| 进程管理 | `kernel/src/syscall/process.rs#sys_set_tid_address` | `os/src/syscall/process.rs#sys_set_tid_address` | 同板块同名 | 函数名由 POSIX 规范决定。目标项目显式调用 `validate_user_buffer_write` 进行安全校验；参考项目直接通过 `translated_refmut` 写入。安全模型异构。 |
| 内存管理 | `kernel/src/syscall/memory.rs#sys_mmap` | `os/src/syscall/process.rs#sys_mmap` | 同板块同名 | 函数名由 POSIX 规范决定。目标项目使用 `addr_space.mmap_region` 和文件系统交互；参考项目使用 `MapFlags::from_bits` 和 `UserBuffer`。内存映射模型不同。 |
| 文件系统 | `kernel/src/syscall/io.rs#sys_ioctl` | `os/src/syscall/fs.rs#sys_ioctl` | 同板块同名 | 函数名由 POSIX 规范决定。目标项目委托给 `devfs_ioctl` 处理；参考项目直接操作 `Winsize` 结构体。设备控制路径异构。 |

#### 4. 核心代码调用流对比剖析
* **【进程管理 / sys_fork 进程创建】**
* **底层技术析理**: 目标项目采用全局 `MANAGER` 集中式任务管理，通过 `MANAGER.tasks[child_pid]` 索引新任务控制块，并在整个 fork 流程中引入 `crate::hal::irq::disable/restore` 进行临界区保护，体现了生产级内核的中断安全设计。参考项目采用分散式 TCB 引用，直接通过 `current_task()` 获取当前任务句柄，链式调用 `inner_exclusive_access()` 进入临界区后操作 `TrapContext`，是典型的教学内核简化模型。两者在对象管理模型、同步原语和上下文切换入口上存在根本性架构分歧。
* **代码溯源链接**:
  - 目标项目: `kernel/src/syscall/process.rs#sys_fork`
  - 参考项目: `os/src/syscall/process.rs#sys_fork`

* **【进程管理 / sys_kill 信号投递】**
* **底层技术析理**: 目标项目通过遍历全局 `MANAGER.tasks` 数组并结合 `current_process_group` 来定位目标进程组，最终委托 `manager::send_signal` 完成信号投递，体现了调度器集中处理信号的架构思想。参考项目通过 `pid2task` 哈希映射直接定位任务控制块，然后对 `task_ref.signals` 位集合执行 `insert` 操作，信号状态内嵌于 TCB 中。两者在信号数据结构、进程查找机制和投递抽象层上无 AST 交集。
* **代码溯源链接**:
  - 目标项目: `kernel/src/syscall/process.rs#sys_kill`
  - 参考项目: `os/src/syscall/process.rs#sys_kill`

* **【内存管理 / sys_mmap 内存映射】**
* **底层技术析理**: 目标项目的 `sys_mmap` 调用链涉及 `addr_space.mmap_region`、`crate::fs::file_inode_idx` 等，显示其内存映射与文件系统 inode 紧密耦合，支持文件映射。参考项目的调用链围绕 `MapFlags::from_bits`、`ProtFlags::from_bits` 和 `UserBuffer` 展开，侧重于标志位解析和用户缓冲区操作，内存映射模型相对简化。两者在内存对象模型和文件映射支持上存在显著差异。
* **代码溯源链接**:
  - 目标项目: `kernel/src/syscall/memory.rs#sys_mmap`
  - 参考项目: `os/src/syscall/process.rs#sys_mmap`

#### 5. 单方模块与降权项
- **目标项目独有模块**: `文件系统/第三方库` 板块下的 `ext4` 文件系统（`dir_add_entry`、`ext4_truncate` 等）、`procfs` 伪文件系统（`procfs_readdir`、`generate_pid_stat` 等）、`timerfd` 定时器机制、`futex` 同步原语。这些模块在参考项目中无任何对应 AST 指纹，属于目标项目的原生功能扩展，不构成复用证据。
- **跨板块硬凑证据**: 控方将 `kernel/src/ipc/pipe/blocking.rs#pipe_write_blocking`（管道写阻塞）与 `os/src/trap/mod.rs#trap_from_kernel`（内核态陷阱处理）配对，将 `kernel/src/drivers/virtio_blk/mod.rs#write_sector`（VirtIO 块设备）与 `os/src/drivers/block/sdcard.rs#write_sector`（SD 卡驱动）配对。这些配对跨越了进程间通信、中断处理、设备驱动等不同模块和硬件协议，仅因功能关键词或函数名相同而硬凑，不构成同板块主证据，已全部降级为弱信号。
- **公共接口与泛型调用**: 双方共同出现的 `Vec::new`、`Some`、`Ok`、`asm!`、`core::mem::size_of`、`to_le_bytes` 等均为 Rust 标准库或编译器内建调用，属于公共领域表达，不能作为同源证据。

#### 6. 保守结论
本案客观 AST 相似度仅 1.17%，共同指纹 13 个，远低于实质性复用的判定阈值。目标项目 `[2026-T202610351999623-XX]` 在进程管理、内存管理等核心板块展现出与参考项目 `[2022-S0S-哈尔滨工业大学]` 根本不同的架构设计：全局 `MANAGER` 集中调度取代了分散式 TCB 访问，`hal` 硬件抽象层引入了统一的中断保护机制，且存在大量参考项目不具备的原生功能模块（如 `ext4`、`procfs`、`futex`）。所有同名函数均可由 POSIX 标准接口命名和 Rust 语言惯用法解释，底层 AST 调用链无实质性重合。综合判定，目标项目为独立开发，复用指控不成立。

------------------------------

