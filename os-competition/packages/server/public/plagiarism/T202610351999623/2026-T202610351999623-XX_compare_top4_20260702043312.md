# 🛡️ OS 内核赛自动化代码级架构审计报告

**审计时间**: `2026-07-02 04:33:12`
**参赛作品名称**: `2026-T202610351999623-XX`
**年份**: `2026`
**学校**: `XX`
**队伍名称**: `T202610351999623`
**比赛名字**: `OS-Competition`
**比赛子赛道**: `Kernel-Implementation`
**仓库地址**: `https://gitlab.eduxiji.net/educg-group-43501-3132633/T202610351999623-1603`
**推测开发语言**: `Rust / C (由AST探针自动识别)`
**特征函数量**: `713`

## ⚔️ 底层代码同源性对比 (AST Homology)
> 本部分由专门的代码审查智能体，对该项目与符合年份条件的历届最高相似度项目进行控制流与底层调用对比。新版报告会先给出确定性的全模块覆盖矩阵，再给出 Agent 的分模块解释，避免只围绕少量抽样函数展开。

### 🏆 Top 4 底层对标: vs `2024-NPUcore-重生之我是秦始皇-河南理工大学`（河南理工大学 / NPUcore-重生之我是秦始皇）

#### 对方作品信息

**对方年份**: `2024`
**对方学校**: `河南理工大学`
**对方队伍名称**: `NPUcore-重生之我是秦始皇`
**对方比赛名字**: `OS-Competition`
**对方比赛子赛道**: `Kernel-Implementation`
**对方仓库地址**: `https://gitlab.eduxiji.net/educg-group-26011-2376549/T202410460992502-2319`

> 双向覆盖率：共同 AST 指纹: 18；唯一 AST 指纹: 目标项目 674，参考项目 1037；覆盖率: 目标项目 2.67%，参考项目 1.74%

## 全模块对比总览（确定性证据）

> 总体覆盖率：共同 AST 指纹: 18；唯一 AST 指纹: 目标项目 674，参考项目 1037；覆盖率: 目标项目 2.67%，参考项目 1.74%

### 1. 模块覆盖矩阵
| 模块 | 目标函数数 | 参考函数数 | 共同唯一 AST 指纹 | Target 覆盖 | Base 覆盖 | 状态 |
| :--- | ---: | ---: | ---: | ---: | ---: | :--- |
| 进程管理 | 100 | 77 | 1 | 1.14% | 1.39% | 双方存在且有结构重合 |
| 任务调度/异步运行时 | 82 | 129 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |
| 内存管理 | 199 | 346 | 11 | 5.7% | 3.59% | 双方存在且有结构重合 |
| 中断/异常处理 | 46 | 27 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |
| 系统调用 | 43 | 46 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |
| 文件系统 | 126 | 412 | 1 | 0.82% | 0.34% | 双方存在且有结构重合 |
| 设备驱动 | 22 | 18 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |
| 网络协议栈 | 0 | 0 | 0 | 0.0% | 0.0% | 双方均未体现 |
| 文件系统/第三方库 | 48 | 140 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |
| 第三方库 | 0 | 10 | 0 | 0.0% | 0.0% | 仅参考项目体现 |
| 通用模块/其他 | 47 | 76 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |

### 2. 分模块证据清单

#### 进程管理
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `kernel/src/syscall/process.rs#sys_setpgid` | `os/src/syscall/process.rs#sys_setpgid` | 同板块同名 | crate::hal::irq::disable, crate::hal::irq::restore, crate::sched::manager::MANAGER.tasks[cur]
            .as_ref, crate::sched::manager::MANAGER.tasks[cur]
            .as_ref()
            .map, crate::sched::manager::MANAGER.tasks[pid].as_ref, crate::sched::manager::MANAGER.tasks[target]
            .as_ref ...(+4) | crate::task::find_task_by_tgid, task.setpgid |
| `kernel/src/syscall/process.rs#sys_kill` | `os/src/syscall/process.rs#sys_kill` | 同板块同名 | crate::hal::irq::disable, crate::hal::irq::restore, crate::sched::manager::MANAGER.tasks[i].as_ref, crate::sched::manager::MANAGER.tasks[i].is_some, current_process_group, manager::current_pid ...(+8) | Signals::from_signum, drop, find_task_by_tgid, inner.add_signal, signal.is_empty, task.acquire_inner_lock ...(+2) |
| `kernel/src/syscall/process.rs#sys_setitimer` | `os/src/syscall/process.rs#sys_setitimer` | 同板块同名 | crate::hal::irq::disable, crate::hal::irq::restore, crate::hal::time::read_ticks, crate::sched::manager::MANAGER.tasks[pid]
            .as_ref, crate::sched::manager::MANAGER.tasks[pid]
            .as_ref()
            .map, crate::sched::manager::MANAGER.tasks[pid].as_mut ...(+4) | copy_from_user, copy_from_user(token, new_value, &mut inner.timer[which]).unwrap, copy_to_user, copy_to_user(token, &inner.timer[which], old_value).unwrap, current_task, current_task().unwrap ...(+4) |
| `kernel/src/syscall/process.rs#sys_fork` | `os/src/syscall/process.rs#sys_fork` | 同板块同名 | crate::hal::irq::disable, crate::hal::irq::restore, crate::sched::manager::MANAGER.tasks[child_pid].as_mut, manager::fork_current, write_child_u32, write_user_u32 | CloneFlags::from_bits, CloneFlags::from_bits(0x00000100).unwrap, Signals::from_bits_truncate, add_task, current_task, current_task().unwrap ...(+1) |

#### 任务调度/异步运行时
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `kernel/src/syscall/io.rs#sys_statfs` | `os/src/syscall/fs.rs#sys_statfs` | 同板块同名 | core::mem::size_of::<crate::fs::StatFs>, core::slice::from_raw_parts, crate::fs::StatFs::zeroed, crate::fs::ext4::ext4_statfs_info, crate::sched::manager::current_cwd_inode, fs::inode_fs_id ...(+8) | Box::new, copy_to_user, copy_to_user(token, statfs.as_ref(), buf).is_err, current_task, current_task().unwrap, current_task().unwrap().get_user_token ...(+2) |
| `kernel/src/syscall/io.rs#sys_fcntl` | `os/src/syscall/fs.rs#sys_fcntl` | 同板块同名 | crate::sched::fd::get_fd_cloexec, crate::sched::fd::set_fd_cloexec, fs::file_get_flags, fs::file_set_flags, manager::current_fd_kind, manager::dup_fd | Fcntl_Command::from_primitive, OpenFlags::O_NONBLOCK.bits, OpenFlags::O_RDWR.bits, current_task, current_task().unwrap, fd_table.get_ref ...(+10) |
| `kernel/src/syscall/io.rs#sys_fstatat` | `os/src/syscall/fs.rs#sys_fstatat` | 同板块同名 | core::mem::size_of::<fs::Stat>, crate::sched::fd::resolve_dirfd, fs::dec_file_ref, fs::file_stat, fs::vfs_open, manager::current_cwd_inode ...(+4) | FstatatFlags::from_bits, copy_to_user, copy_to_user(token, &file_descriptor.get_stat(), buf as *mut Stat).is_err, current_task, current_task().unwrap, current_user_token ...(+12) |
| `kernel/src/syscall/io.rs#sys_readlinkat` | `os/src/syscall/fs.rs#sys_readlinkat` | 同板块同名 | PROC_SELF_EXE.len, exe.len, exe_path
            .iter, exe_path
            .iter()
            .position, exe_path
            .iter()
            .position(|&b| b == 0)
            .unwrap_or, exe_path.len ...(+5) | __openat, copy_to_user_string, copy_to_user_string(token, &real_path[0..len], buf).is_err, current_task, current_task().unwrap, debug! ...(+10) |

#### 内存管理
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `kernel/src/loader/mod.rs#load_elf` | `os/src/task/task.rs#load_elf` | 同板块同名 | AddressSpace::new, Err, Ok, PTEFlags::READ
        .union, PTEFlags::READ
        .union(PTEFlags::WRITE)
        .union, PTEFlags::READ
        .union(PTEFlags::WRITE)
        .union(PTEFlags::EXECUTE)
        .union ...(+29) | (&memory_set)
            .translate, (&memory_set)
            .translate(VirtAddr::from(self.trap_cx_user_va()).into())
            .unwrap, KERNEL_SPACE.lock, KERNEL_SPACE.lock().token, MemorySet::from_elf, Ok ...(+32) |
| `kernel/src/mm/address_space.rs#push` | `os/src/mm/memory_set.rs#push` | 同板块同名 | (pa as *mut UserMapping).add, (pa as *mut UserMapping).add(li).write, PhysAddr::from, PhysAddr::from(f).into, PhysAddr::from(self.frames[fi]).into, alloc_frame ...(+3) | Ok, data.len, len.min, map_area.map_one, ppn.get_bytes_array, ppn.get_bytes_array()[..src.len()].copy_from_slice ...(+2) |
| `kernel/src/fs/ramfs/mod.rs#alloc_blocks` | `os/src/fs/fat32/efs.rs#alloc_blocks` | 同板块同名 | DATA.as_mut_ptr, DATA.as_mut_ptr().add, Some, core::ptr::write_bytes | Vec::<usize>::with_capacity, block_ids.push, debug_assert_eq!, self.fat.alloc, self.first_sector_of_cluster |
| `kernel/src/sched/pid.rs#alloc` | `os/src/fs/fat32/bitmap.rs#alloc` | 同板块同名 | Some, crate::kinfo!, self.set, self.test | Vec::with_capacity, allocated_cluster.push, last.is_none, last.unwrap, log::error!, self.alloc_one ...(+2) |

#### 中断/异常处理
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `kernel/src/ipc/pipe/blocking.rs#pipe_write_blocking` | `os/src/arch/la64/trap/mod.rs#trap_from_kernel` | 同模块上下文对齐/差异观察 | WriteAction::Return, data.len, manager::block_current, manager::current_has_interrupting_signal, manager::current_pid, manager::wakeup ...(+3) | ((addr + i) as *mut u8).read_unaligned, ((addr + i) as *mut u8).write_unaligned, (0..sz).rev, BadV::read, BadV::read().get_vaddr, EStat::read ...(+14) |
| `kernel/src/ipc/pipe/blocking.rs#pipe_read_blocking` | `os/src/arch/la64/trap/mem_access.rs#get_addr` | 同模块上下文对齐/差异观察 | buf.len, buf.len().min, manager::block_current, manager::current_has_interrupting_signal, manager::current_pid, manager::wakeup ...(+3) | self.get_op_code, self.get_op_code().map, self.get_rj_num, self.get_rk_num, self.get_si12, self.get_si14 |
| `kernel/src/arch/riscv64/trap/timer.rs#timer_v02_liveness_test` | `os/src/arch/la64/trap/mem_access.rs#get_size` | 同模块上下文对齐/差异观察 | core::hint::spin_loop, disable_stie, read_mtime, read_sie, restore_stie, sbi_set_timer_v02_result ...(+1) | 内部闭环逻辑 |
| `kernel/src/syscall/io.rs#sys_ppoll` | `os/src/fs/dev/tty.rs#default` | 同模块上下文对齐/差异观察 | crate::hal::time::read_ticks, manager::current_has_interrupting_signal, manager::yield_current, nfds.checked_mul, ppoll_parse_timeout, ppoll_scan_ready ...(+1) | 内部闭环逻辑 |

#### 系统调用
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `user/init/src/main.rs#syscall` | `user/src/syscall.rs#syscall` | 同板块同名 | core::arch::asm! | __syscall, asm! |
| `kernel/src/syscall/io.rs#sys_dup` | `user/src/syscall.rs#sys_dup` | 同板块同名 | manager::dup_fd | syscall |
| `kernel/src/syscall/io.rs#sys_close` | `user/src/syscall.rs#sys_close` | 同板块同名 | manager::close_fd | syscall |
| `user/init/src/main.rs#sys_waitpid` | `user/src/syscall.rs#sys_waitpid` | 同板块同名 | syscall | syscall |

#### 文件系统
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `kernel/src/syscall/file.rs#sys_mount` | `os/src/syscall/fs.rs#sys_mount` | 同板块同名 | crate::kwarn!, fs::vfs_bind_file, manager::current_cwd_inode, manager::current_pid, super::check::copy_cstr_from_user | MountFlags::from_bits, MountFlags::from_bits(mountflags).unwrap, current_user_token, filesystemtype.is_null, info!, source.is_null ...(+3) |
| `kernel/src/fs/ramfs/mod.rs#empty` | `os/src/fs/fat32/layout.rs#empty` | 同板块同名 | DirChild::empty | 内部闭环逻辑 |
| `kernel/src/fs/mod.rs#free` | `os/src/fs/fat32/bitmap.rs#free` | 同板块同名 | 内部闭环逻辑 | Some, lock.len, lock.push_back, self.set_next_clus, self.vacant_clus.lock |
| `kernel/src/fs/mod.rs#empty` | `os/src/fs/poll.rs#empty` | 同板块同名 | 内部闭环逻辑 | 内部闭环逻辑 |

#### 设备驱动
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `kernel/src/drivers/virtio_blk/io.rs#block_io` | `os/src/drivers/block/mod.rs#block_device_test` | 同模块上下文对齐/差异观察 | BLK.last_used_idx.wrapping_add, core::hint::spin_loop, core::ptr::addr_of!, crate::hal::cpu::io_fence, crate::kwarn!, desc.add ...(+3) | BLOCK_DEVICE.clone, assert_eq!, block_device.read_block, block_device.write_block, println!, write_buffer.iter_mut |
| `kernel/src/arch/riscv64/plic.rs#init` | `os/src/mm/map_area.rs#force_swap_out` | 同模块上下文对齐/差异观察 | (PLIC_S_THRESHOLD as *mut u32).write_volatile, (enable_addr as *const u32).read_volatile, (enable_addr as *mut u32).write_volatile, (priority_addr as *mut u32).write_volatile, core::arch::asm!, crate::kinfo! | Err, Frame::SwappedOut, Ok, SWAP_DEVICE.lock, SWAP_DEVICE.lock().write, frame_ref.ppn.get_bytes_array |
| `kernel/src/drivers/virtio_blk/mod.rs#probe` | `os/src/drivers/block/virtio_blk.rs#read_block` | 同模块上下文对齐/差异观察 | crate::kinfo!, crate::kwarn!, io::init_device, mmio_read32 | buf.chunks_mut, self.0
                .lock, self.0
                .lock()
                .read_block, self.0
                .lock()
                .read_block(block_id, buf)
                .expect |
| `kernel/src/arch/riscv64/uart.rs#handle_irq` | `os/src/drivers/block/virtio_blk.rs#write_block` | 同模块上下文对齐/差异观察 | RX_HEAD.load, RX_HEAD.store, RX_TAIL.load, lsr.read_volatile, rbr.read_volatile | buf.chunks, self.0
                .lock, self.0
                .lock()
                .write_block, self.0
                .lock()
                .write_block(block_id, buf)
                .expect |

#### 文件系统/第三方库
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `kernel/src/fs/ext4/mod.rs#init` | `os/src/fs/ext4/lwext4_rust/src/blockdev.rs#new` | 同模块上下文对齐/差异观察 | crate::kerror!, crate::kinfo!, crate::kwarn!, disk_io::read_bytes, disk_io::u16_at, disk_io::u32_at ...(+1) | Box::into_raw, Box::new, CString::new, CString::new("/").unwrap, CString::new("ext4_fs").expect, Ok ...(+16) |
| `kernel/src/fs/ext4/dir.rs#dir_add_entry` | `os/src/fs/ext4/lwext4_rust/examples/src/main.rs#enable` | 同模块上下文对齐/差异观察 | alloc_block, extent_append, extent_map_block, itype_to_ft, name.len, read_block ...(+7) | Some, am.read16, am.read32, am.read8, am.write16, am.write32 ...(+1) |
| `kernel/src/fs/ext4/dir.rs#dir_remove_entry` | `os/src/fs/ext4/lwext4_rust/c/ulibc.c#qsort_r` | 同模块上下文对齐/差异观察 | Some, dir_blk.len, extent_map_block, name.len, read_block, read_raw_inode ...(+3) | (*comp), ext4_assert |
| `kernel/src/fs/ext4/ops.rs#ext4_truncate` | `os/src/fs/ext4/lwext4_rust/examples/src/main.rs#walk_dt_node` | 同模块上下文对齐/差异观察 | (new_entries as u16).to_le_bytes, alloc::free_block, inode::read_raw_inode, inode::update_inode_size, inode::write_inode_iblock, new_len.to_le_bytes ...(+2) | core::mem::size_of::<u32>, dt.children.iter, dt.prop_cells, dt.prop_cells("ranges").unwrap, dt.prop_raw, dt.prop_str ...(+12) |

#### 第三方库
仅参考项目中观察到该模块代表函数，目标项目没有同模块可对齐证据：
- 参考项目：`dependency/rlibc/src/lib.rs#memmove_overlapping`，调用摘要：assert_eq!, b"0123012345".iter, b"4567896789".iter, memmove
- 参考项目：`dependency/rlibc/src/lib.rs#memcmp_strings`，调用摘要：assert!, assert_eq!, x.len
- 参考项目：`dependency/rlibc/src/lib.rs#memmove`，调用摘要：dest.offset, src.offset
- 参考项目：`dependency/rlibc/src/lib.rs#memset_single_byte_pointers`，调用摘要：assert_eq!, memset

#### 通用模块/其他
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `kernel/src/arch/riscv64/sbi.rs#shutdown` | `os/src/arch/la64/sbi.rs#shutdown` | 同板块同名 | core::arch::asm!, sbi_call | Pm1Cnt::empty, pm1_cnt.set_s5, pm1_cnt.set_s5().write |
| `kernel/src/tty/mod.rs#write_str` | `os/src/console.rs#write_str` | 同板块同名 | Ok, tty_puts | Ok, console_flush, console_putchar, s.chars |
| `kernel/src/hal/boot.rs#append_usable_region` | `user/src/lib.rs#_parameter` | 同模块上下文对齐/差异观察 | dtb_reserved_range, push_region, start.max | ((argv + i * core::mem::size_of::<usize>()) as *const usize).read_volatile, ((str_start + *i) as *const u8).read_volatile, (0usize..)
            .find, (0usize..)
            .find(|i| unsafe { ((str_start + *i) as *const u8).read_volatile() == 0 })
            .unwrap, HEAP.lock, HEAP.lock()
            .init ...(+10) |
| `kernel/src/klog.rs#short_module_path` | `user/src/bin/exit.rs#main` | 同模块上下文对齐/差异观察 | bytes.as_ptr, bytes.as_ptr().add, bytes.len, core::slice::from_raw_parts, core::str::from_utf8_unchecked, full.as_bytes | assert!, exit, fork, println!, yield_ |


好的，我将遵循审计指令，基于客观 AST 证据与双方模块画像，对本案进行最终定性仲裁。

### 1. 宏观判定
- **定性结论**: **独立开发**
  底层 AST 结构与关键外部调用图谱显示，双方项目在核心架构设计、并发模型、内存管理及对象抽象上存在根本性差异。极低的 AST 相似度（1.06%）与覆盖率（目标 2.67%，参考 1.74%）属于噪声级重合，主要源于 POSIX 标准接口同名及 Rust 语言惯用法，不构成代码复用或架构抄袭的证据。

### 2. 全模块覆盖矩阵解读
| 模块 | 双方存在 | 共同 AST 指纹 | 覆盖率 (目标/参考) | 证据强度 | 解读 |
| :--- | :---: | :---: | :---: | :---: | :--- |
| **进程管理** | 是 | 1 | 1.14% / 1.39% | **弱** | 唯一的共同指纹源于 POSIX 标准系统调用同名。双方在进程查找、信号发送、并发控制等核心路径上的外部调用图谱完全不同，属于独立实现。 |
| **任务调度/异步运行时** | 是 | 0 | 0.0% / 0.0% | **未形成** | 尽管双方均实现了调度功能，但未观察到任何共同 AST 指纹，表明调度器内部逻辑与数据结构无重合。 |
| **内存管理** | 是 | 11 | 5.7% / 3.59% | **弱** | 共同指纹主要来自 `load_elf`、`push`、`alloc` 等通用操作名。这些是操作系统内存管理的标准动作，其外部调用图谱（如目标项目的 `AddressSpace::new` vs 参考项目的 `MemorySet::from_elf`）揭示了完全不同的内部实现。 |
| **中断/异常处理** | 是 | 0 | 0.0% / 0.0% | **未形成** | 未发现共同 AST 指纹。目标项目基于 RISC-V 架构，参考项目基于 LoongArch 架构，Trap 处理逻辑天然异构。 |
| **系统调用** | 是 | 0 | 0.0% / 0.0% | **未形成** | 未发现共同 AST 指纹。尽管存在同名系统调用，但其内部实现路径无重合。 |
| **文件系统** | 是 | 1 | 0.82% / 0.34% | **弱** | 共同指纹来自 `sys_mount` 等通用操作。目标项目实现了 ext4/ramfs/procfs，参考项目实现了 fat32/ext4，其底层磁盘布局、块分配及文件操作逻辑完全不同。 |
| **设备驱动** | 是 | 0 | 0.0% / 0.0% | **未形成** | 双方驱动模型和硬件平台不同，未发现共同 AST 指纹。 |
| **网络协议栈** | 否 | 0 | 0.0% / 0.0% | **未形成** | 双方均未体现该模块。 |
| **文件系统/第三方库** | 是 | 0 | 0.0% / 0.0% | **未形成** | 目标项目包含自研 ext4 库，参考项目包含 lwext4_rust 绑定库，实现路径完全不同，无共同指纹。 |
| **第三方库** | 仅参考 | 0 | 0.0% / 0.0% | **未形成** | 参考项目独有的 `rlibc` 库，目标项目未使用，不构成证据。 |
| **通用模块/其他** | 是 | 0 | 0.0% / 0.0% | **未形成** | 未发现共同 AST 指纹。 |

### 3. 底层 AST 关键证据指纹表
| 所属板块 | 目标项目路径 | 参考项目路径 | 证据类型 | 技术说明 |
| :--- | :--- | :--- | :--- | :--- |
| 进程管理 | `kernel/src/syscall/process.rs#sys_setpgid` | `os/src/syscall/process.rs#sys_setpgid` | 弱信号-同名接口 | POSIX 标准同名系统调用。目标项目通过 `MANAGER.tasks` 数组遍历实现，参考项目通过 `find_task_by_tgid` 查找，AST 结构完全不同。 |
| 进程管理 | `kernel/src/syscall/process.rs#sys_kill` | `os/src/syscall/process.rs#sys_kill` | 弱信号-同名接口 | POSIX 标准同名系统调用。目标项目使用 `send_signal` 直接发送，参考项目使用 `inner.add_signal` 配合 `wake_interruptible`，信号注入机制异构。 |
| 进程管理 | `kernel/src/syscall/process.rs#sys_fork` | `os/src/syscall/process.rs#sys_fork` | 弱信号-同名接口 | POSIX 标准同名系统调用。目标项目调用 `fork_current`，参考项目调用 `add_task` 并处理 `CloneFlags`，创建流程与标志位处理逻辑无重合。 |
| 内存管理 | `kernel/src/loader/mod.rs#load_elf` | `os/src/task/task.rs#load_elf` | 弱信号-通用操作 | 通用 ELF 加载操作名。目标项目直接操作 `AddressSpace` 和页表项，参考项目通过 `MemorySet::from_elf` 和 `translate` 完成，内部实现路径不同。 |
| 内存管理 | `kernel/src/mm/address_space.rs#push` | `os/src/mm/memory_set.rs#push` | 弱信号-通用操作 | 通用内存映射操作名。目标项目操作 `PhysAddr` 和 `alloc_frame`，参考项目操作 `map_area.map_one` 和 `ppn`，映射机制不同。 |
| 文件系统 | `kernel/src/syscall/file.rs#sys_mount` | `os/src/syscall/fs.rs#sys_mount` | 弱信号-同名接口 | POSIX 标准同名系统调用。目标项目调用 `vfs_bind_file`，参考项目处理 `MountFlags` 和 `filesystemtype`，挂载逻辑无重合。 |

### 4. 核心代码调用流对比剖析
* **【进程管理/进程创建与查找】**
* **底层技术析理**: 双方在进程抽象与并发控制上存在根本性架构差异。目标项目采用全局静态数组 `MANAGER.tasks` 配合 `irq::disable/restore` 中断屏蔽实现临界区保护，任务查找方式为直接索引 `MANAGER.tasks[pid]`。参考项目则采用动态数据结构，通过 `current_task().unwrap()` 获取当前任务指针，并使用 `acquire_inner_lock` 互斥锁进行同步，任务查找依赖 `find_task_by_tgid` 等哈希表函数。这两种模型在内存布局、同步原语和对象生命周期管理上完全不同，排除了架构同源的可能性。
* **代码溯源链接**:
  - 目标项目: `kernel/src/syscall/process.rs#sys_setpgid`
  - 参考项目: `os/src/syscall/process.rs#sys_setpgid`

* **【内存管理/ELF加载】**
* **底层技术析理**: 双方在内存映射的抽象层次上存在显著差异。目标项目的 `load_elf` 函数直接与物理内存管理交互，通过 `PTEFlags` 构建页表项并调用 `AddressSpace::new` 和 `push` 来建立虚拟地址到物理地址的映射。参考项目则通过更高层次的 `MemorySet::from_elf` 接口完成加载，该接口内部封装了页分配和映射逻辑，并返回一个 `MemorySet` 对象。目标项目的手动页表构建流与参考项目的封装式内存集操作流，体现了不同的设计哲学和实现路径。
* **代码溯源链接**:
  - 目标项目: `kernel/src/loader/mod.rs#load_elf`
  - 参考项目: `os/src/task/task.rs#load_elf`

### 5. 单方模块与降权项
*   **单方模块**:
    *   **第三方库 (参考项目)**: 参考项目包含 `dependency/rlibc` 库，目标项目无此模块。此为参考项目独有的外部依赖，不构成同源证据。
*   **降权项说明**:
    *   **POSIX 标准同名系统调用**: `sys_fork`, `sys_exit`, `sys_getpid`, `sys_kill` 等函数名是 POSIX 标准强制规定的接口名称，任何兼容 POSIX 的操作系统都必须实现。名称相同不代表代码复用，其内部实现经 AST 比对已证实完全不同。
    *   **通用操作名**: `load_elf`, `push`, `alloc`, `free`, `empty` 等是系统编程中的通用动词，其功能语义由问题域决定，不能作为抄袭指纹。
    *   **Rust 语言惯用法与标准库**: `Some`, `Ok`, `Err`, `unwrap`, `copy_from_slice`, `to_le_bytes`, `Vec::with_capacity` 等是 Rust 语言的标准库和惯用法，属于公共知识领域。
    *   **跨板块证据**: 控方将 `procfs_stat` (文件系统) 与 `sys_uname` (系统调用) 等不同板块的函数强行关联，此类指控因违反“同板块”原则而被降权。

### 6. 保守结论
综合全模块 AST 指纹比对与关键调用流分析，目标项目 [2026-T202610351999623-XX] 与参考项目 [2024-NPUcore-重生之我是秦始皇-河南理工大学] 的代码相似度处于极低水平（1.06%）。双方在进程管理、内存管理等核心板块展现出截然不同的架构设计：目标项目采用全局数组+中断屏蔽的同步模型，参考项目采用动态指针+互斥锁的模型。现有证据充分表明，两个项目是遵循 POSIX 标准接口规范的独立开发成果，不存在代码抄袭或架构换皮行为。极微量的 AST 重合完全可归因于标准接口同名、通用算法逻辑及编程语言惯用法，不构成任何形式的同源性风险。

------------------------------

