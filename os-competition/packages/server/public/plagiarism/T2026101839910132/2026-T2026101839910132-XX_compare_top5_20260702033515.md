# 🛡️ OS 内核赛自动化代码级架构审计报告

**审计时间**: `2026-07-02 03:35:15`
**参赛作品名称**: `2026-T2026101839910132-XX`
**年份**: `2026`
**学校**: `XX`
**队伍名称**: `T2026101839910132`
**比赛名字**: `OS-Competition`
**比赛子赛道**: `Kernel-Implementation`
**仓库地址**: `https://gitlab.eduxiji.net/educg-group-43501-3132633/T2026101839910132-3878`
**推测开发语言**: `Rust / C (由AST探针自动识别)`
**特征函数量**: `3735`

## ⚔️ 底层代码同源性对比 (AST Homology)
> 本部分由专门的代码审查智能体，对该项目与符合年份条件的历届最高相似度项目进行控制流与底层调用对比。新版报告会先给出确定性的全模块覆盖矩阵，再给出 Agent 的分模块解释，避免只围绕少量抽样函数展开。

### 🏆 Top 5 底层对标: vs `2025-KernalCraft-中山大学`（中山大学 / KernalCraft）

#### 对方作品信息

**对方年份**: `2025`
**对方学校**: `中山大学`
**对方队伍名称**: `KernalCraft`
**对方比赛名字**: `OS-Competition`
**对方比赛子赛道**: `Kernel-Implementation`
**对方仓库地址**: `https://gitlab.eduxiji.net/educg-group-36002-2710490/T202510558995240-2536`

> 双向覆盖率：共同 AST 指纹: 878；唯一 AST 指纹: 目标项目 2412，参考项目 1853；覆盖率: 目标项目 36.4%，参考项目 47.38%

## 全模块对比总览（确定性证据）

> 总体覆盖率：共同 AST 指纹: 878；唯一 AST 指纹: 目标项目 2412，参考项目 1853；覆盖率: 目标项目 36.4%，参考项目 47.38%

### 1. 模块覆盖矩阵
| 模块 | 目标函数数 | 参考函数数 | 共同唯一 AST 指纹 | Target 覆盖 | Base 覆盖 | 状态 |
| :--- | ---: | ---: | ---: | ---: | ---: | :--- |
| 进程管理 | 141 | 71 | 5 | 3.97% | 7.94% | 双方存在且有结构重合 |
| 任务调度/异步运行时 | 147 | 115 | 9 | 6.72% | 8.04% | 双方存在且有结构重合 |
| 内存管理 | 622 | 741 | 55 | 12.42% | 12.28% | 双方存在且有结构重合 |
| 中断/异常处理 | 48 | 128 | 3 | 7.5% | 3.0% | 双方存在且有结构重合 |
| 系统调用 | 72 | 21 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |
| 文件系统 | 573 | 147 | 5 | 1.4% | 3.91% | 双方存在且有结构重合 |
| 设备驱动 | 47 | 555 | 1 | 2.13% | 0.21% | 双方存在且有结构重合 |
| 网络协议栈 | 107 | 53 | 1 | 1.79% | 2.33% | 双方存在且有结构重合 |
| 文件系统/第三方库 | 372 | 340 | 112 | 38.49% | 42.75% | 双方存在且有结构重合 |
| 网络协议栈/第三方库 | 138 | 0 | 0 | 0.0% | 0.0% | 仅目标项目体现 |
| 第三方库 | 1230 | 2 | 1 | 0.14% | 50.0% | 双方存在且有结构重合 |
| 通用模块/其他 | 238 | 669 | 27 | 21.6% | 10.19% | 双方存在且有结构重合 |

### 2. 分模块证据清单

#### 进程管理
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `GCore/os/src/syscall/process.rs#sys_clone` | `os/src/syscall/process.rs#sys_clone` | 同板块同名 | CloneFlags::from_bits, CloneFlags::from_bits(flags & !0xff).unwrap, Signals::empty, Signals::from_signum, add_task, child.acquire_inner_lock ...(+10) | current_process, current_process.fork, new_process.getpid, new_process.inner_exclusive_access, new_process_inner.tasks[0].as_ref, new_process_inner.tasks[0].as_ref().unwrap ...(+3) |
| `GCore/os/src/task/processor.rs#run_tasks` | `os/src/task/processor.rs#run_tasks` | 同板块同名 | PROCESSORS.lock, Some, __switch, core::arch::asm!, crate::hal::arch::riscv::time::set_next_trigger, crate::hal::arch::riscv::trap::enable_timer_interrupt ...(+6) | PROCESSOR.exclusive_access, Some, __switch, drop, fetch_task, println! ...(+2) |
| `GCore/os/src/syscall/process.rs#sys_clock_gettime` | `os/src/syscall/process.rs#sys_clock_gettime` | 同板块同名 | TimeSpec::now, copy_to_user, crate::timer::current_time, crate::timer::get_time_ns, current_user_token, tp.is_null | ClockId::from, TimeSpec::now, current_user_token, panic!, timespec.is_null, translated_refmut |
| `GCore/os/src/syscall/process.rs#sys_kill` | `os/src/syscall/process.rs#sys_kill` | 同板块同名 | Signals::from_signum, drop, find_task_by_tgid, inner.add_signal, signal.is_empty, task.acquire_inner_lock ...(+1) | SignalFlags::from_bits, log::error!, pid2process, process.inner_exclusive_access |

#### 任务调度/异步运行时
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `GCore/os/src/syscall/fs.rs#sys_sendfile` | `os/src/syscall/fs.rs#sys_sendfile` | 同板块同名 | Some, Vec::<u8>::with_capacity, buffer.as_mut_slice, buffer.as_slice, buffer.set_len, current_task ...(+17) | (count - total_written).min, Inode::new, UserBuffer::new, curr_process.inner_exclusive_access, current_process, drop ...(+12) |
| `GCore/os/src/net/tcp.rs#accept` | `os/src/net/port_table.rs#accept` | 同板块同名 | Arc::new, FileDescriptor::new, Ok, TcpSocket::new, address::fill_with_endpoint, current_task ...(+26) | LISTEN_TABLE.exclusive_access, Some, assert!, listen_port.unwrap, listen_table[listen_index].as_mut |
| `GCore/os/src/syscall/fs.rs#sys_renameat2` | `os/src/syscall/fs.rs#sys_renameat2` | 同板块同名 | FileDescriptor::rename, current_task, current_task().unwrap, fd_table.get_ref, file_descriptor.clone, info! ...(+6) | current_process, current_user_token, file.getid, inner.fd_table.len, inner.fd_table[fd].as_ref, inner.fd_table[fd].as_ref().unwrap ...(+6) |
| `GCore/os/src/syscall/fs.rs#sys_readv` | `os/src/syscall/fs.rs#sys_readv` | 同板块同名 | UserBuffer::new, Vec::<IOVec>::with_capacity, Vec::with_capacity, copy_from_user_array, copy_from_user_array(token, iov as *const IOVec, iovecs.as_mut_ptr(), iovcnt).is_err, current_task ...(+11) | UserBuffer::new, Vec::new, buffers.append, core::mem::size_of::<IoVec>, current_process, current_user_token ...(+9) |

#### 内存管理
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `GCore/os/src/hal/arch/loongarch64/trap/mod.rs#trap_handler` | `virtio-drivers/examples/x86_64/src/trap.rs#trap_handler` | 同板块同名 | (0..sz).rev, BadV::read, BadV::read().get_vaddr, ERA::read, ERA::read().next_ins, ERA::read().next_ins().write ...(+57) | panic!, x86_64::registers::control::Cr2::read |
| `GCore/os/src/mm/memory_set.rs#map_elf` | `os/src/mm/memory_set.rs#map_elf` | 同板块同名 | ((ph.virtual_addr() + ph.mem_size()) as usize + bias).into, (VirtAddr::from(elf.input.as_ptr() as usize + (ph.offset() as usize)))
                                .floor, (ph.virtual_addr() as usize + bias).into, Err, KERNEL_SPACE
                        .lock, KERNEL_SPACE
                        .lock()
                        .remove_area_with_start_vpn ...(+44) | ((ph.virtual_addr() + ph.mem_size()) as usize + offset.0).into, (ph.virtual_addr() as usize + offset.0).into, MapArea::new, Some, VirtPageNum, assert_eq! ...(+17) |
| `GCore/os/src/mm/memory_set.rs#mmap` | `os/src/mm/memory_set.rs#mmap` | 同板块同名 | MMAP_BASE.into, MapArea::new, Some, USR_MMAP_BASE.into, VirtAddr::from, VirtAddr::from(MMAP_END).into ...(+31) | MapArea::new, log::error!, log::trace!, mmap_manager.mmap_alloc, self.mmap_manager.as_mut, self.push |
| `GCore/os/src/drivers/block/virtio_blk_pci.rs#enumerate_pci` | `virtio-drivers/examples/aarch64/src/main.rs#enumerate_pci` | 同板块同名 | MmioCam::new, PciRangeAllocator::new, PciRoot::new, PciTransport::new::<VirtioHal, MmioCam>, PciTransport::new::<VirtioHal, MmioCam>(&mut pci_root, device_function).unwrap, Some ...(+16) | MmioCam::new, PciMemory32Allocator::for_pci_ranges, PciRoot::new, PciTransport::new::<HalImpl, _>, PciTransport::new::<HalImpl, _>(&mut pci_root, device_function).unwrap, allocate_bars ...(+8) |

#### 中断/异常处理
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `GCore/os/src/hal/arch/loongarch64/register/base/estat.rs#cause` | `riscv/src/register/mcause.rs#cause` | 同板块同名 | Exception::try_from, Interrupt::try_from, MErrCtl::read, MErrCtl::read().is_merr, TLBRERA::read, TLBRERA::read().is_tlbr ...(+8) | Exception::from, Interrupt::from, Trap::Exception, Trap::Interrupt, self.code, self.is_interrupt |
| `GCore/os/src/hal/arch/loongarch64/trap/mod.rs#enable_timer_interrupt` | `os/src/hal/instruction/loongarch.rs#enable_timer_interrupt` | 同板块同名 | ECfg::empty, ECfg::empty()
        .set_line_based_interrupt_vector, ECfg::empty()
        .set_line_based_interrupt_vector(LineBasedInterrupt::TIMER)
        .write, TCfg::read, TCfg::read()
        .set_enable, TCfg::read()
        .set_enable(true)
        .set_periodic ...(+3) | crmd::set_ie, ecfg::set_lie, get_timer_freq, log::info!, tcfg::set_en, tcfg::set_init_val ...(+2) |
| `GCore/os/src/hal/arch/loongarch64/register/base/estat.rs#is_exception` | `riscv/src/register/mcause.rs#is_exception` | 同板块同名 | 内部闭环逻辑 | self.is_interrupt |
| `GCore/os/src/hal/arch/loongarch64/trap/mod.rs#set_kernel_trap_entry` | `os/src/hal/trap/loongarch.rs#set_kernel_trap_entry` | 同板块同名 | register::EEntry::read, register::EEntry::read()
        .set_exception_entry, register::EEntry::read()
        .set_exception_entry(__kern_trap as usize)
        .write | eentry::set_eentry |

#### 系统调用
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `GCore/os/src/net/tcp.rs#_read` | `os/src/syscall/net.rs#sys_socket` | 同模块上下文对齐/差异观察 | Err, GeneralRet::Err, GeneralRet::Ok, NET_INTERFACE.poll, NET_INTERFACE.tcp_socket, Ok ...(+7) | create_tcp_socket, create_udp_socket |
| `GCore/os/src/net/tcp.rs#_accept` | `os/src/syscall/input.rs#sys_event_get` | 同模块上下文对齐/差异观察 | Err, GeneralRet::Err, GeneralRet::Ok, NET_INTERFACE.poll, NET_INTERFACE.tcp_socket, Ok ...(+7) | KEYBOARD_DEVICE.clone, MOUSE_DEVICE.clone, kb.is_empty, kb.read_event, mouse.is_empty, mouse.read_event |
| `GCore/os/src/net/udp.rs#connect` | `os/src/syscall/info.rs#fmt` | 同模块上下文对齐/差异观察 | Err, IpListenEndpoint::from, NET_INTERFACE.poll, NET_INTERFACE.udp_socket, Ok, SocketAddrv4::new ...(+11) | writeln! |
| `GCore/os/src/net/udp.rs#_read` | `os/src/syscall/info.rs#sys_sysinfo` | 同模块上下文对齐/差异观察 | Err, GeneralRet::Err, GeneralRet::Ok, NET_INTERFACE.poll, NET_INTERFACE.udp_socket, Ok ...(+10) | Sysinfo::collect, current_user_token, translated_refmut |

#### 文件系统
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `GCore/os/src/fs/directory_tree.rs#mkdir` | `os/src/fs/inode.rs#mkdir` | 同板块同名 | (*last_comp).to_string, Arc::downgrade, Err, Ok, Self::new, Self::parse_dir_path ...(+13) | EXT4.ext4_dir_mk |
| `GCore/os/src/syscall/fs.rs#sys_mount` | `os/src/syscall/fs.rs#sys_mount` | 同板块同名 | MountFlags::from_bits, MountFlags::from_bits(mountflags).unwrap, current_user_token, filesystemtype.is_null, info!, source.is_null ...(+3) | 内部闭环逻辑 |
| `GCore/os/src/fs/fat32/fat_osinode.rs#get_stat` | `os/src/fs/inode.rs#get_stat` | 同板块同名 | (StatMode::S_IFDIR | StatMode::S_IRWXU | StatMode::S_IRWXG | StatMode::S_IRWXO)
                    .bits, (StatMode::S_IFREG | StatMode::S_IRWXU | StatMode::S_IRWXG | StatMode::S_IRWXO)
                    .bits, Stat::new, crate::makedev!, self.inner.is_dir, self.inner.read ...(+1) | EXT4.fuse_statfs, EXT4.fuse_statfs(inode_num as u64).unwrap, Kstat::from_linuxstat |
| `GCore/os/src/syscall/fs.rs#sys_umount2` | `os/src/syscall/fs.rs#sys_umount2` | 同板块同名 | UmountFlags::from_bits, current_user_token, info!, target.is_null, translated_str, warn! | 内部闭环逻辑 |

#### 设备驱动
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `GCore/os/src/drivers/block/virtio_blk.rs#read_block` | `virtio-drivers-old/src/blk.rs#read_block` | 同板块同名 | assert!, buf.chunks_mut, buf.chunks_mut(VIRT_IO_BLOCK_SZ).enumerate, self.0
                .lock, self.0
                .lock()
                .read_blocks, self.0
                .lock()
                .read_blocks(virtio_block_id as usize, chunk)
                .expect | BlkResp::default, Err, Ok, req.as_buf, resp.as_buf_mut, self.header.notify ...(+4) |
| `GCore/os/src/drivers/block/virtio_blk.rs#write_block` | `virtio-drivers-old/src/blk.rs#write_block` | 同板块同名 | assert!, buf.chunks, buf.chunks(VIRT_IO_BLOCK_SZ).enumerate, self.0
                .lock, self.0
                .lock()
                .write_blocks, self.0
                .lock()
                .write_blocks(virtio_block_id as usize, chunk)
                .expect | BlkResp::default, Err, Ok, req.as_buf, resp.as_buf_mut, self.header.notify ...(+4) |
| `GCore/os/src/drivers/block/virtio_blk.rs#mmio_phys_to_virt` | `virtio-drivers/examples/aarch64/src/hal.rs#mmio_phys_to_virt` | 同板块同名 | NonNull::new, NonNull::new(vaddr.0 as *mut u8)
            .expect, PhysAddr, virtio_phys_to_virt | NonNull::new, NonNull::new(paddr as _).unwrap |
| `GCore/os/src/drivers/block/virtio_blk_pci.rs#mmio_phys_to_virt` | `virtio-drivers/examples/riscv/src/virtio_impl.rs#mmio_phys_to_virt` | 同板块同名 | NonNull::new, NonNull::new(vaddr.0 as *mut u8).unwrap, PhysAddr, panic!, virtio_phys_to_virt | NonNull::new, NonNull::new(paddr as _).unwrap |

#### 网络协议栈
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `GCore/os/src/net/unix.rs#hang_up` | `os/src/net/port_table.rs#hang_up` | 同板块同名 | self.read_end.hang_up, self.shut_rd.lock, self.shut_wr.lock, self.write_end.hang_up | todo! |
| `GCore/os/src/net/config.rs#add_socket` | `os/src/net/socket.rs#add_socket` | 同板块同名 | self._add_socket | SOCKET_TABLE.exclusive_access, Some, VecDeque::new, get_socket, get_socket(raddr, lport, rport).is_some, socket_table.len ...(+2) |
| `GCore/os/src/net/unix.rs#writable` | `os/src/net/port_table.rs#writable` | 同板块同名 | self.shut_wr.lock, self.write_end.writable | 内部闭环逻辑 |
| `GCore/os/src/net/unix.rs#readable` | `os/src/net/port_table.rs#readable` | 同板块同名 | self.read_end.readable, self.shut_rd.lock | 内部闭环逻辑 |

#### 文件系统/第三方库
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `GCore/os/src/fs/ext4/extent.rs#extent_remove_space` | `ext4_rs/src/ext4_impls/extents.rs#extent_remove_space` | 同板块同名 | Block::load_offset, Ext4Extent::default, Ok, ex.get_actual_len, ex.get_first_block, ex.get_pblock ...(+14) | Block::load, Ext4Extent::default, Ok, ex.get_actual_len, ex.get_first_block, ex.get_pblock ...(+15) |
| `GCore/os/src/fs/ext4/extent.rs#ext_remove_leaf` | `ext4_rs/src/ext4_impls/extents.rs#ext_remove_leaf` | 同板块同名 | Block::load_inode_root_block, Block::load_offset, Ext4Extent::default, Ok, ex.get_actual_len, ex.get_pblock ...(+14) | Block::load, Block::load_inode_root_block, Ext4Extent::default, Ok, ex.get_actual_len, ex.get_pblock ...(+14) |
| `GCore/os/src/fs/ext4/file.rs#write_at` | `ext4_rs/src/ext4_impls/file.rs#write_at` | 同板块同名 | Block::load_offset, Ok, block.sync_blk_to_disk, block.write_offset, drop, inode_ref.inode.set_size ...(+10) | Block::load, Ok, block.sync_blk_to_disk, block.write_offset, drop, inode_ref
                .inode
                .set_size ...(+9) |
| `GCore/os/src/fs/ext4/balloc.rs#balloc_alloc_block` | `ext4_rs/src/ext4_impls/balloc.rs#balloc_alloc_block` | 同板块同名 | Block::load_offset, Err, Ext4BlockGroup::load_new, Ok, block_group
                        .set_block_group_balloc_bitmap_csum, block_group.get_block_bitmap_block ...(+17) | Block::load, Ext4BlockGroup::load_new, Ok, block_group.get_block_bitmap_block, block_group.get_free_blocks_count, block_group.set_block_group_balloc_bitmap_csum ...(+16) |

#### 网络协议栈/第三方库
仅目标项目中观察到该模块代表函数，参考项目没有同模块可对齐证据：
- 目标项目：`GCore/dependency/dep_iso/src/net/ethernet/intel/ixgbe.rs#new`，调用摘要：(IXGBE_FCRTH..IXGBE_FCRTH_END).step_by, (IXGBE_FCRTL..IXGBE_FCRTL_END).step_by, (IXGBE_FCTTV..IXGBE_FCTTV_END).step_by, EthernetAddress::from_bytes, MaybeUninit::uninit, MaybeUninit::uninit().assume_init ...(+56)
- 目标项目：`GCore/dependency/virtio-drivers/src/device/socket/connectionmanager.rs#send_recv`，调用摘要："Hello from guest".as_bytes, (hello_from_guest.len() as u32).into, (hello_from_host.len() as u32).into, 0.into, 50.into, Arc::new ...(+43)
- 目标项目：`GCore/dependency/virtio-drivers/src/device/socket/connectionmanager.rs#incoming_connection`，调用摘要：0.into, 50.into, Arc::new, Mutex::new, ReadOnly::new, SocketType::Stream.into ...(+24)
- 目标项目：`GCore/dependency/dep_iso/src/net/ethernet/intel/e1000.rs#new`，调用摘要：P::alloc_dma, Vec::with_capacity, assert_eq!, debug!, e1000[E1000_ICR].read, e1000[E1000_ICR].write ...(+25)

#### 第三方库
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `GCore/dependency/virtio-drivers/src/transport/mmio.rs#vendor_id` | `virtio-drivers/src/transport/mmio.rs#vendor_id` | 同板块同名同结构 | field_shared!, field_shared!(self.header, vendor_id).read | field_shared!, field_shared!(self.header, vendor_id).read |
| `GCore/dependency/dep_pci/pci/src/lib.rs#probe_function` | `virtio-drivers-old/src/header.rs#vendor_id` | 同模块上下文对齐/差异观察 | BAR::decode, BridgeControl::from_bits_truncate, Command::from_bits_truncate, DeviceKind::CardbusBridge, DeviceKind::Device, DeviceKind::PciBridge ...(+6) | self.vendor_id.read |

#### 通用模块/其他
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `GCore/os/src/hal/arch/loongarch64/boot.rs#_start` | `os/src/hal/boot/loongarch.rs#_start` | 同板块同名 | asm! | core::arch::naked_asm! |
| `GCore/user/src/lib.rs#_start` | `os/src/hal/boot/riscv.rs#_start` | 同板块同名 | _parameter, asm!, core::arch::asm!, unreachable! | core::arch::naked_asm! |
| `GCore/os/src/timer.rs#add` | `os/src/timer.rs#add` | 同板块同名同结构 | 内部闭环逻辑 | 内部闭环逻辑 |
| `GCore/os/src/timer.rs#cmp` | `os/src/timer.rs#cmp` | 同板块同名同结构 | self.tv_nsec.cmp, self.tv_sec.cmp | self.tv_nsec.cmp, self.tv_sec.cmp |


好的，我将以审计专家的身份，基于提供的客观证据，对本次同源性争议进行最终的定性仲裁。

### 1. 宏观判定
- **定性结论**: **独立开发，底层架构存在显著差异** (客观 AST 相似度: 25.92%，共同 AST 指纹: 878；唯一 AST 指纹: 目标项目 2412，参考项目 1853；覆盖率: 目标项目 36.4%，参考项目 47.38%)

### 2. 全模块覆盖矩阵解读
- **进程管理**: 双方均存在。共同唯一 AST 指纹仅 5 个，目标覆盖率 3.97%，参考覆盖率 7.94%。重合度极低，主要源于 POSIX 系统调用同名和 trivial accessor 函数的必然雷同。**证据强度：弱**。
- **任务调度/异步运行时**: 双方均存在。共同唯一 AST 指纹 9 个，目标覆盖率 6.72%，参考覆盖率 8.04%。重合点集中在 `sendfile`、`readv` 等复杂系统调用的用户态缓冲区处理流上，但底层锁和上下文切换机制已分化。**证据强度：中**。
- **内存管理**: 双方均存在。共同唯一 AST 指纹 55 个，目标覆盖率 12.42%，参考覆盖率 12.28%。重合主要源于 ELF 加载、mmap 等通用内存管理逻辑，以及共享的第三方库（如 `ext4_rs`）依赖。核心页表操作和分配器实现未体现强同源性。**证据强度：中**。
- **中断/异常处理**: 双方均存在。共同唯一 AST 指纹 3 个，目标覆盖率 7.5%，参考覆盖率 3.0%。重合点仅为 `cause`、`is_exception` 等与硬件架构强相关的寄存器位操作，属于 RISC-V / LoongArch 特权架构规范的必然产物。**证据强度：弱**。
- **系统调用**: 双方均存在。共同唯一 AST 指纹为 0，目标覆盖率 0.0%，参考覆盖率 0.0%。尽管存在大量同名系统调用，但 AST 层面未观察到任何共同指纹，表明双方的实现路径（内存拷贝、对象访问）完全不同。**证据强度：未形成**。
- **文件系统**: 双方均存在。共同唯一 AST 指纹 5 个，目标覆盖率 1.4%，参考覆盖率 3.91%。重合度极低，目标项目拥有独立的 `directory_tree` 和 `fat32` 实现，与参考项目直接依赖 `EXT4` 的架构不同。**证据强度：弱**。
- **设备驱动**: 双方均存在。共同唯一 AST 指纹 1 个，目标覆盖率 2.13%，参考覆盖率 0.21%。重合仅出现在 `virtio_blk` 的 `read_block`/`write_block` 等通用块设备操作流程上。**证据强度：弱**。
- **网络协议栈**: 双方均存在。共同唯一 AST 指纹 1 个，目标覆盖率 1.79%，参考覆盖率 2.33%。重合度极低，目标项目拥有独立的 `tcp.rs`、`udp.rs`、`unix.rs` 实现，与参考项目的 `port_table.rs` 架构完全不同。**证据强度：弱**。
- **第三方库/公共依赖**: 双方均存在。共同唯一 AST 指纹 113 个，是覆盖率的主要贡献者。这完全归因于双方都合法依赖了 `ext4_rs`、`virtio-drivers` 等公开的第三方 crate。**证据强度：强（但属于合法公共依赖，不构成抄袭）**。

### 3. 底层 AST 关键证据指纹表
| 所属板块 | 目标项目路径 | 参考项目路径 | 证据类型 | 技术说明 |
| :--- | :--- | :--- | :--- | :--- |
| 第三方库 | `GCore/os/src/fs/ext4/extent.rs#extent_remove_space` | `ext4_rs/src/ext4_impls/extents.rs#extent_remove_space` | 同板块同名同结构 | 均来自公开 crate `ext4_rs` 的合法复用，非原创性争议点。 |
| 第三方库 | `GCore/dependency/virtio-drivers/src/transport/mmio.rs#vendor_id` | `virtio-drivers/src/transport/mmio.rs#vendor_id` | 同板块同名同结构 | 均来自公开 crate `virtio-drivers` 的合法复用，非原创性争议点。 |
| 任务调度/异步运行时 | `GCore/os/src/syscall/fs.rs#sys_sendfile` | `os/src/syscall/fs.rs#sys_sendfile` | 同板块同名 | 调用流相似（`UserBuffer`、分块读写），但底层锁（`current_task` vs `current_process`）和文件系统交互模型不同。 |
| 内存管理 | `GCore/os/src/mm/memory_set.rs#map_elf` | `os/src/mm/memory_set.rs#map_elf` | 同板块同名 | ELF 加载的通用流程（遍历 Program Header，建立 `MapArea`），但地址空间管理（`KERNEL_SPACE.lock` vs 直接操作）和映射细节不同。 |
| 进程管理 | `GCore/os/src/task/processor.rs#current` | `os/src/task/processor.rs#current` | 同板块底层逻辑一致 | `self.current.as_ref().map(...)` 是 Rust `Option` 的标准 getter 模式，创造性为零，不构成抄袭证据。 |
| 进程管理 | `GCore/os/src/task/processor.rs#take_current` | `os/src/task/processor.rs#take_current` | 同板块底层逻辑一致 | `self.current.take()` 是 Rust `Option` 的标准所有权转移操作，创造性为零，不构成抄袭证据。 |
| 进程管理 | `GCore/os/src/syscall/process.rs#sys_get_time` | `os/src/syscall/process.rs#sys_get_time` | 同板块底层逻辑一致 | 均调用 `get_time_ms`，源于 rCore 教学内核的经典设计范式，属于公共领域知识。 |
| 进程管理 | `GCore/os/src/task/task.rs#getpid` | `os/src/task/process.rs#getpid` | 同板块底层逻辑一致 | 均为返回进程ID的内部闭环 getter，且文件位置不同（`task.rs` vs `process.rs`），反映了架构差异。 |

### 4. 核心代码调用流对比剖析
* **【进程管理 / `sys_clone` 系统调用】**
* **底层技术析理**: 目标项目实现了 Linux 兼容的 `clone` 语义，调用流涉及 `CloneFlags::from_bits`、`Signals::from_signum`、`add_task` 等，用于创建线程或进程并设置信号和线程组。参考项目则基于简化的 `fork` 语义，调用流为 `current_process.fork` -> `new_process.getpid` -> `tasks[0].as_ref`。两者在**进程/线程模型**上存在根本性架构差异，目标项目更接近宏内核的线程组模型，而参考项目是教学内核的进程内单任务模型。
* **代码溯源链接**:
  - 目标项目: `GCore/os/src/syscall/process.rs#sys_clone`
  - 参考项目: `os/src/syscall/process.rs#sys_clone`

* **【内存管理 / `mmap` 系统调用】**
* **底层技术析理**: 两者都实现了 `mmap`，调用流均包含 `MapArea::new` 和地址分配。但目标项目使用了独立的 `MMAP_BASE`/`USR_MMAP_BASE` 常量和 `VirtAddr::from` 进行地址计算，而参考项目则依赖一个独立的 `mmap_manager` 分配器。这表明双方在**虚拟地址空间管理策略**上采用了不同的内部实现和抽象层次。
* **代码溯源链接**:
  - 目标项目: `GCore/os/src/mm/memory_set.rs#mmap`
  - 参考项目: `os/src/mm/memory_set.rs#mmap`

* **【中断/异常处理 / `trap_handler`】**
* **底层技术析理**: 目标项目的 `trap_handler` 位于 `loongarch64` 架构目录下，调用流涉及 `BadV::read`、`ERA::read` 等 LoongArch 特定寄存器的读写。参考项目则位于 `x86_64` 示例目录下，调用 `x86_64::registers::control::Cr2::read`。这是**完全不同的 CPU 架构**下的独立实现，不存在同源性。
* **代码溯源链接**:
  - 目标项目: `GCore/os/src/hal/arch/loongarch64/trap/mod.rs#trap_handler`
  - 参考项目: `virtio-drivers/examples/x86_64/src/trap.rs#trap_handler`

### 5. 单方模块与降权项
- **单方模块**: `网络协议栈/第三方库` 模块仅存在于目标项目，包含 `ixgbe`、`e1000` 等独立网卡驱动，是目标项目独立开发的强证据。
- **降权项说明**:
  - **POSIX 系统调用同名**: `sys_clone`、`sys_kill`、`sys_getpid` 等函数名由 POSIX/Linux ABI 标准强制规定，同名不能作为抄袭证据。
  - **第三方库/公共依赖**: `ext4_rs`、`virtio-drivers` 等 crate 的同名同结构函数是合法公共依赖，已从原创性争议中排除。
  - **Trivial Accessor**: `current`、`take_current`、`getpid` 等单行 getter/setter 函数，其 AST 模式是 Rust 语言特性的必然结果，创造性为零，不构成强证据。
  - **跨板块归类错误**: 控方将 `fdt/src/node.rs`（设备树解析）、`os/src/syscall/info.rs`（procfs）等文件系统/启动相关函数归入“进程管理”板块，属于硬凑证据，已予排除。

### 6. 保守结论
本次审计的客观 AST 相似度为 25.92%，处于低水平。该相似度的主要贡献来自双方对 `ext4_rs`、`virtio-drivers` 等公开第三方 crate 的合法依赖，与原创性争议无关。在核心的进程管理、内存管理、系统调用等子系统上，双方覆盖率均低于 13%，且在进程模型（`clone` vs `fork`）、内存拷贝机制（`copy_to_user` vs `translated_refmut`）、同步原语（`acquire_inner_lock` vs `inner_exclusive_access`）及 CPU 架构支持（LoongArch vs RISC-V/x86）等关键维度上展现出实质性架构差异。控方所指控的“铁证”多为 Rust 语言的标准惯用法或 POSIX 强制接口，不构成抄袭证据。综合判定，目标项目 `[2026-T2026101839910132-XX]` 相对于参考项目 `[2025-KernalCraft-中山大学]` 为独立开发，不存在违规复用行为。

------------------------------

