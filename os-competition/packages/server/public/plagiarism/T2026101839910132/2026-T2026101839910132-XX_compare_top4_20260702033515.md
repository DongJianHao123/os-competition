# 🛡️ OS 内核赛自动化代码级架构审计报告

**审计时间**: `2026-07-02 03:35:15`
**参赛作品名称**: `2026-T2026101839910132-XX`
**年份**: `2026`
**学校**: `XX`
**队伍名称**: `T2026101839910132`
**比赛名字**: `OS-Competition`
**比赛子赛道**: `Kernel-Implementation`
**仓库地址**: `https://gitlab.eduxiji.net/educg-group-43501-3132633/T2026101839910132-3878`
**推测开发语言**: `Rust / C (由AST探针自动识别)`
**特征函数量**: `3735`

## ⚔️ 底层代码同源性对比 (AST Homology)
> 本部分由专门的代码审查智能体，对该项目与符合年份条件的历届最高相似度项目进行控制流与底层调用对比。新版报告会先给出确定性的全模块覆盖矩阵，再给出 Agent 的分模块解释，避免只围绕少量抽样函数展开。

### 🏆 Top 4 底层对标: vs `2024-NPUcore-重生之我是菜狗-西北工业大学`（西北工业大学 / NPUcore-重生之我是菜狗）

#### 对方作品信息

**对方年份**: `2024`
**对方学校**: `西北工业大学`
**对方队伍名称**: `NPUcore-重生之我是菜狗`
**对方比赛名字**: `OS-Competition`
**对方比赛子赛道**: `Kernel-Implementation`
**对方仓库地址**: `https://gitlab.eduxiji.net/educg-group-26011-2376549/T202410699992491-3136`

> 双向覆盖率：共同 AST 指纹: 897；唯一 AST 指纹: 目标项目 2412，参考项目 1111；覆盖率: 目标项目 37.19%，参考项目 80.74%

## 全模块对比总览（确定性证据）

> 总体覆盖率：共同 AST 指纹: 897；唯一 AST 指纹: 目标项目 2412，参考项目 1111；覆盖率: 目标项目 37.19%，参考项目 80.74%

### 1. 模块覆盖矩阵
| 模块 | 目标函数数 | 参考函数数 | 共同唯一 AST 指纹 | Target 覆盖 | Base 覆盖 | 状态 |
| :--- | ---: | ---: | ---: | ---: | ---: | :--- |
| 进程管理 | 141 | 73 | 44 | 34.92% | 66.67% | 双方存在且有结构重合 |
| 任务调度/异步运行时 | 147 | 135 | 102 | 76.12% | 82.93% | 双方存在且有结构重合 |
| 内存管理 | 622 | 520 | 310 | 69.98% | 82.45% | 双方存在且有结构重合 |
| 中断/异常处理 | 48 | 37 | 31 | 77.5% | 93.94% | 双方存在且有结构重合 |
| 系统调用 | 72 | 62 | 36 | 66.67% | 73.47% | 双方存在且有结构重合 |
| 文件系统 | 573 | 499 | 245 | 68.44% | 76.56% | 双方存在且有结构重合 |
| 设备驱动 | 47 | 34 | 32 | 68.09% | 94.12% | 双方存在且有结构重合 |
| 网络协议栈 | 107 | 88 | 33 | 58.93% | 70.21% | 双方存在且有结构重合 |
| 文件系统/第三方库 | 372 | 0 | 0 | 0.0% | 0.0% | 仅目标项目体现 |
| 网络协议栈/第三方库 | 138 | 0 | 0 | 0.0% | 0.0% | 仅目标项目体现 |
| 第三方库 | 1230 | 0 | 0 | 0.0% | 0.0% | 仅目标项目体现 |
| 通用模块/其他 | 238 | 211 | 92 | 73.6% | 92.0% | 双方存在且有结构重合 |

### 2. 分模块证据清单

#### 进程管理
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `GCore/os/src/syscall/process.rs#sys_prlimit` | `os/src/syscall/process.rs#sys_prlimit` | 同板块同名 | Resource::from_primitive, assert!, copy_from_user, copy_from_user(token, new_limit, rlimit).is_err, copy_to_user, copy_to_user(
                        token,
                        &(RLimit {
                            rlim_cur: SYSTEM_TASK_LIMIT,
                            rlim_max: SYSTEM_TASK_LIMIT,
                        }),
                        old_limit,
                    )
                    .is_err ...(+18) | Resource::from_primitive, assert!, copy_from_user, copy_from_user(token, new_limit, rlimit).is_err, copy_to_user, copy_to_user(
                        token,
                        &(RLimit {
                            rlim_cur: SYSTEM_TASK_LIMIT,
                            rlim_max: SYSTEM_TASK_LIMIT,
                        }),
                        old_limit,
                    )
                    .is_err ...(+18) |
| `GCore/os/src/syscall/process.rs#sys_futex` | `os/src/syscall/process.rs#sys_futex` | 同板块同名 | FutexOption::from_bits_truncate, current_task, current_task().unwrap, do_futex_wait, drop, info! ...(+16) | FutexOption::from_bits_truncate, current_task, current_task().unwrap, do_futex_wait, drop, info! ...(+15) |
| `GCore/os/src/syscall/process.rs#sys_execve` | `os/src/syscall/process.rs#sys_execve` | 同板块同名同结构 | Box::<[u8; 4]>::new, DEFAULT_SHELL.to_string, Some, Vec::with_capacity, arg_ptr.is_null, argv.add ...(+22) | Box::<[u8; 4]>::new, DEFAULT_SHELL.to_string, Some, Vec::with_capacity, arg_ptr.is_null, argv.add ...(+22) |
| `GCore/os/src/task/threads.rs#do_futex_wait` | `os/src/task/threads.rs#do_futex_wait` | 同板块同名同结构 | Arc::downgrade, TimeSpec::now, WaitQueue::new, block_current_and_run_next, current_task, current_task().unwrap ...(+11) | Arc::downgrade, TimeSpec::now, WaitQueue::new, block_current_and_run_next, current_task, current_task().unwrap ...(+11) |

#### 任务调度/异步运行时
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `GCore/os/src/syscall/fs.rs#sys_splice` | `os/src/syscall/fs.rs#sys_splice` | 同板块同名同结构 | Some, Vec::<u8>::with_capacity, buffer.as_mut_slice, buffer.as_slice, buffer.set_len, current_task ...(+16) | Some, Vec::<u8>::with_capacity, buffer.as_mut_slice, buffer.as_slice, buffer.set_len, current_task ...(+16) |
| `GCore/os/src/syscall/fs.rs#sys_fcntl` | `os/src/syscall/fs.rs#sys_fcntl` | 同板块同名 | Fcntl_Command::from_primitive, OpenFlags::O_APPEND.bits, OpenFlags::O_NONBLOCK.bits, OpenFlags::O_RDWR.bits, current_task, current_task().unwrap ...(+12) | Fcntl_Command::from_primitive, OpenFlags::O_NONBLOCK.bits, OpenFlags::O_RDWR.bits, current_task, current_task().unwrap, fd_table.get_ref ...(+10) |
| `GCore/os/src/syscall/fs.rs#sys_sendfile` | `os/src/syscall/fs.rs#sys_sendfile` | 同板块同名同结构 | Some, Vec::<u8>::with_capacity, buffer.as_mut_slice, buffer.as_slice, buffer.set_len, current_task ...(+17) | Some, Vec::<u8>::with_capacity, buffer.as_mut_slice, buffer.as_slice, buffer.set_len, current_task ...(+17) |
| `GCore/os/src/syscall/fs.rs#sys_fstatat` | `os/src/syscall/fs.rs#sys_fstatat` | 同板块同名 | FstatatFlags::from_bits, copy_to_user, copy_to_user(token, &stat, buf as *mut Stat).is_err, current_task, current_task().unwrap, current_user_token ...(+15) | FstatatFlags::from_bits, copy_to_user, copy_to_user(token, &file_descriptor.get_stat(), buf as *mut Stat).is_err, current_task, current_task().unwrap, current_user_token ...(+12) |

#### 内存管理
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `GCore/os/src/fs/mod.rs#flush_preload` | `os/src/fs/mod.rs#flush_preload` | 同板块同名 | Err, Ok, ROOT_FD
            .mkdir, ROOT_FD
            .mkdir("etc")
            .or_else, ROOT_FD
            .mkdir("etc")
            .or_else(|e| if e == 17 || e == -17 /* EEXIST */ || e == -1 { Ok(()) } else { Err(e) })
            .unwrap_or, ROOT_FD
            .mkdir("glibc")
            .or_else ...(+49) | ROOT_FD.open, ROOT_FD.open("bash", OpenFlags::O_CREAT, false).unwrap, ROOT_FD.open("initproc", OpenFlags::O_CREAT, false).unwrap, bash.write, core::slice::from_raw_parts, crate::mm::PPNRange::new ...(+8) |
| `GCore/os/src/task/signal.rs#do_signal` | `os/src/task/signal.rs#do_signal` | 同板块同名同结构 | (trap_cx as *const TrapContext).cast::<MachineContext>, SigInfo::new, SignalStack::new, Signals::SIGSEGV.to_signum, Signals::SIGSEGV.to_signum().unwrap, Signals::from_bits_truncate ...(+40) | (trap_cx as *const TrapContext).cast::<MachineContext>, SigInfo::new, SignalStack::new, Signals::SIGSEGV.to_signum, Signals::SIGSEGV.to_signum().unwrap, Signals::from_bits_truncate ...(+40) |
| `GCore/os/src/hal/arch/loongarch64/trap/mod.rs#trap_handler` | `os/src/arch/la64/trap/mod.rs#trap_handler` | 同板块同名 | (0..sz).rev, BadV::read, BadV::read().get_vaddr, ERA::read, ERA::read().next_ins, ERA::read().next_ins().write ...(+57) | (0..sz).rev, BadV::read, BadV::read().get_vaddr, ERA::read, ERA::read().next_ins, ERA::read().next_ins().write ...(+54) |
| `GCore/os/src/mm/memory_set.rs#do_page_fault` | `os/src/mm/memory_set.rs#do_page_fault` | 同板块同名 | Err, Ok, PhysAddr::from, VirtAddr::from, addr.floor, addr.page_offset ...(+34) | Err, Ok, PhysAddr::from, VirtAddr::from, addr.floor, addr.page_offset ...(+35) |

#### 中断/异常处理
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `GCore/os/src/hal/arch/loongarch64/trap/mod.rs#trap_from_kernel` | `os/src/arch/la64/trap/mod.rs#trap_from_kernel` | 同板块同名 | ((addr + i) as *mut u8).read_unaligned, ((addr + i) as *mut u8).write_unaligned, (0..sz).rev, BadV::read, BadV::read().get_vaddr, EStat::read ...(+14) | ((addr + i) as *mut u8).read_unaligned, ((addr + i) as *mut u8).write_unaligned, (0..sz).rev, BadV::read, BadV::read().get_vaddr, EStat::read ...(+14) |
| `GCore/os/src/hal/arch/loongarch64/trap/mem_access.rs#get_addr` | `os/src/arch/la64/trap/mem_access.rs#get_addr` | 同板块同名同结构 | self.get_op_code, self.get_op_code().map, self.get_rj_num, self.get_rk_num, self.get_si12, self.get_si14 | self.get_op_code, self.get_op_code().map, self.get_rj_num, self.get_rk_num, self.get_si12, self.get_si14 |
| `GCore/os/src/hal/arch/loongarch64/register/base/estat.rs#cause` | `os/src/arch/la64/register/base/estat.rs#cause` | 同板块同名 | Exception::try_from, Interrupt::try_from, MErrCtl::read, MErrCtl::read().is_merr, TLBRERA::read, TLBRERA::read().is_tlbr ...(+8) | Exception::try_from, Exception::try_from(ecode).unwrap, Interrupt::try_from, Interrupt::try_from(63 - self.get_is().leading_zeros() as usize).unwrap, MErrCtl::read, MErrCtl::read().is_merr ...(+11) |
| `GCore/os/src/hal/arch/loongarch64/trap/mem_access.rs#get_size` | `os/src/arch/la64/trap/mem_access.rs#get_size` | 同板块同名同结构 | 内部闭环逻辑 | 内部闭环逻辑 |

#### 系统调用
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `GCore/os/src/net/tcp.rs#_read` | `os/src/net/tcp.rs#_read` | 同板块同名同结构 | Err, GeneralRet::Err, GeneralRet::Ok, NET_INTERFACE.poll, NET_INTERFACE.tcp_socket, Ok ...(+7) | Err, GeneralRet::Err, GeneralRet::Ok, NET_INTERFACE.poll, NET_INTERFACE.tcp_socket, Ok ...(+7) |
| `GCore/os/src/net/tcp.rs#_accept` | `os/src/net/tcp.rs#_accept` | 同板块同名 | Err, GeneralRet::Err, GeneralRet::Ok, NET_INTERFACE.poll, NET_INTERFACE.tcp_socket, Ok ...(+7) | Err, GeneralRet::Err, GeneralRet::Ok, NET_INTERFACE.poll, NET_INTERFACE.tcp_socket, Ok ...(+7) |
| `GCore/os/src/net/udp.rs#connect` | `os/src/net/udp.rs#connect` | 同板块同名同结构 | Err, IpListenEndpoint::from, NET_INTERFACE.poll, NET_INTERFACE.udp_socket, Ok, SocketAddrv4::new ...(+11) | Err, IpListenEndpoint::from, NET_INTERFACE.poll, NET_INTERFACE.udp_socket, Ok, SocketAddrv4::new ...(+11) |
| `GCore/os/src/net/udp.rs#_read` | `os/src/net/udp.rs#_read` | 同板块同名同结构 | Err, GeneralRet::Err, GeneralRet::Ok, NET_INTERFACE.poll, NET_INTERFACE.udp_socket, Ok ...(+10) | Err, GeneralRet::Err, GeneralRet::Ok, NET_INTERFACE.poll, NET_INTERFACE.udp_socket, Ok ...(+10) |

#### 文件系统
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `GCore/os/src/fs/directory_tree.rs#rename` | `os/src/fs/directory_tree.rs#rename` | 同板块同名 | Arc::downgrade, Arc::new, Err, Mutex::new, Ok, ROOT.cd_comp ...(+41) | Arc::downgrade, Arc::new, Err, Mutex::new, Ok, ROOT.cd_comp ...(+37) |
| `GCore/os/src/fs/directory_tree.rs#init_device_directory` | `os/src/fs/directory_tree.rs#init_device_directory` | 同板块同名 | "null".to_string, "rtc".to_string, "tty".to_string, "urandom".to_string, "zero".to_string, Arc::downgrade ...(+23) | "null".to_string, "rtc".to_string, "tty".to_string, "urandom".to_string, "zero".to_string, Arc::downgrade ...(+20) |
| `GCore/os/src/fs/fat32/fat_inode.rs#delete_dir_ent` | `os/src/fs/fat32/vfs.rs#delete_dir_ent` | 同板块同名同结构 | Err, FATDirEnt::unused_and_last_entry, FATDirEnt::unused_not_last_entry, Ok, Some, debug_assert! ...(+14) | Err, FATDirEnt::unused_and_last_entry, FATDirEnt::unused_not_last_entry, Ok, Some, debug_assert! ...(+14) |
| `GCore/os/src/syscall/fs.rs#sys_utimensat` | `os/src/syscall/fs.rs#sys_utimensat` | 同板块同名 | Some, String::new, TimeSpec::now, UtimensatFlags::from_bits, __openat, copy_from_user ...(+11) | Some, String::new, TimeSpec::now, UtimensatFlags::from_bits, __openat, copy_from_user ...(+10) |

#### 设备驱动
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `GCore/os/src/drivers/block/sata_blk.rs#pci_init` | `os/src/drivers/block/sata_blk.rs#pci_init` | 同板块同名同结构 | AHCI::new, Some, Status::empty, dev.bars.iter, dev.bars.iter().enumerate, dev.bars.iter().enumerate().for_each ...(+3) | AHCI::new, Some, Status::empty, dev.bars.iter, dev.bars.iter().enumerate, dev.bars.iter().enumerate().for_each ...(+3) |
| `GCore/os/src/drivers/block/virtio_blk.rs#read_block` | `os/src/drivers/block/sata_blk.rs#read_block` | 同板块同名 | assert!, buf.chunks_mut, buf.chunks_mut(VIRT_IO_BLOCK_SZ).enumerate, self.0
                .lock, self.0
                .lock()
                .read_blocks, self.0
                .lock()
                .read_blocks(virtio_block_id as usize, chunk)
                .expect | buf.chunks_mut, self.0
                .lock, self.0
                .lock()
                .read_block |
| `GCore/os/src/drivers/block/virtio_blk.rs#write_block` | `os/src/drivers/block/sata_blk.rs#write_block` | 同板块同名 | assert!, buf.chunks, buf.chunks(VIRT_IO_BLOCK_SZ).enumerate, self.0
                .lock, self.0
                .lock()
                .write_blocks, self.0
                .lock()
                .write_blocks(virtio_block_id as usize, chunk)
                .expect | buf.chunks, self.0
                .lock, self.0
                .lock()
                .write_block |
| `GCore/os/src/mm/map_area.rs#force_swap_out` | `os/src/mm/map_area.rs#force_swap_out` | 同板块同名 | Err, Frame::SwappedOut, Ok, SWAP_DEVICE.lock, SWAP_DEVICE.lock().write, frame_ref.ppn.get_bytes_array | Err, Frame::SwappedOut, Ok, SWAP_DEVICE.lock, SWAP_DEVICE.lock().write, frame_ref.ppn.get_bytes_array |

#### 网络协议栈
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `GCore/os/src/net/tcp.rs#r_ready` | `os/src/net/unix.rs#r_ready` | 同板块同名 | NET_INTERFACE.tcp_socket, socket.can_recv, socket.state | todo! |
| `GCore/os/src/net/mod.rs#can_bind` | `os/src/net/mod.rs#can_bind` | 同板块同名同结构 | Some, log::info!, self.0.clone, socket.loacl_endpoint, socket.loacl_endpoint().eq, socket.socket_type ...(+1) | Some, log::info!, self.0.clone, socket.loacl_endpoint, socket.loacl_endpoint().eq, socket.socket_type ...(+1) |
| `GCore/os/src/net/tcp.rs#shutdown` | `os/src/net/tcp.rs#shutdown` | 同板块同名同结构 | NET_INTERFACE.poll, NET_INTERFACE.tcp_socket, Ok, info!, socket.abort, socket.close | NET_INTERFACE.poll, NET_INTERFACE.tcp_socket, Ok, info!, socket.abort, socket.close |
| `GCore/os/src/net/unix.rs#shutdown` | `os/src/net/unix.rs#shutdown` | 同板块同名 | Ok, self.shut_rd.lock, self.shut_wr.lock | Ok, log::info! |

#### 文件系统/第三方库
仅目标项目中观察到该模块代表函数，参考项目没有同模块可对齐证据：
- 目标项目：`GCore/os/src/fs/ext4/extent.rs#extent_remove_space`，调用摘要：Block::load_offset, Ext4Extent::default, Ok, ex.get_actual_len, ex.get_first_block, ex.get_pblock ...(+14)
- 目标项目：`GCore/os/src/fs/ext4/extent.rs#ext_remove_leaf`，调用摘要：Block::load_inode_root_block, Block::load_offset, Ext4Extent::default, Ok, ex.get_actual_len, ex.get_pblock ...(+14)
- 目标项目：`GCore/os/src/fs/ext4/file.rs#write_at`，调用摘要：Block::load_offset, Ok, block.sync_blk_to_disk, block.write_offset, drop, inode_ref.inode.set_size ...(+10)
- 目标项目：`GCore/os/src/fs/ext4/balloc.rs#balloc_alloc_block`，调用摘要：Block::load_offset, Err, Ext4BlockGroup::load_new, Ok, block_group
                        .set_block_group_balloc_bitmap_csum, block_group.get_block_bitmap_block ...(+17)

#### 网络协议栈/第三方库
仅目标项目中观察到该模块代表函数，参考项目没有同模块可对齐证据：
- 目标项目：`GCore/dependency/dep_iso/src/net/ethernet/intel/ixgbe.rs#new`，调用摘要：(IXGBE_FCRTH..IXGBE_FCRTH_END).step_by, (IXGBE_FCRTL..IXGBE_FCRTL_END).step_by, (IXGBE_FCTTV..IXGBE_FCTTV_END).step_by, EthernetAddress::from_bytes, MaybeUninit::uninit, MaybeUninit::uninit().assume_init ...(+56)
- 目标项目：`GCore/dependency/virtio-drivers/src/device/socket/connectionmanager.rs#send_recv`，调用摘要："Hello from guest".as_bytes, (hello_from_guest.len() as u32).into, (hello_from_host.len() as u32).into, 0.into, 50.into, Arc::new ...(+43)
- 目标项目：`GCore/dependency/virtio-drivers/src/device/socket/connectionmanager.rs#incoming_connection`，调用摘要：0.into, 50.into, Arc::new, Mutex::new, ReadOnly::new, SocketType::Stream.into ...(+24)
- 目标项目：`GCore/dependency/dep_iso/src/net/ethernet/intel/e1000.rs#new`，调用摘要：P::alloc_dma, Vec::with_capacity, assert_eq!, debug!, e1000[E1000_ICR].read, e1000[E1000_ICR].write ...(+25)

#### 第三方库
仅目标项目中观察到该模块代表函数，参考项目没有同模块可对齐证据：
- 目标项目：`GCore/dependency/virtio-drivers/src/queue.rs#fake_read_write_queue`，调用摘要：(*available_ring).idx.load, (*used_ring).idx.fetch_add, (*used_ring).idx.load, Vec::new, assert!, assert_eq! ...(+18)
- 目标项目：`GCore/dependency/dep_pci/pci/src/lib.rs#probe_function`，调用摘要：BAR::decode, BridgeControl::from_bits_truncate, Command::from_bits_truncate, DeviceKind::CardbusBridge, DeviceKind::Device, DeviceKind::PciBridge ...(+6)
- 目标项目：`GCore/dependency/virtio-drivers/src/device/sound/fake.rs#handle_control_request`，调用摘要：CommandCode::SOk.into, Some, Vec::new, VirtIOSndHdr {
                            command_code: CommandCode::SOk.into(),
                        }
                        .as_bytes, VirtIOSndHdr::read_from_prefix, VirtIOSndHdr::read_from_prefix(&request)
                .expect ...(+16)
- 目标项目：`GCore/dependency/dep_iso/src/block/ahci.rs#new`，调用摘要：(0..ghc.num_ports()).find, P::alloc_dma, Some, c.set_bit, debug!, ghc.enable ...(+16)

#### 通用模块/其他
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `GCore/os/src/hal/arch/loongarch64/boot.rs#_start` | `user/src/lib.rs#_start` | 同板块同名 | asm! | _parameter, asm!, unreachable! |
| `GCore/user/src/lib.rs#_parameter` | `user/src/lib.rs#_parameter` | 同板块同名同结构 | ((argv + i * core::mem::size_of::<usize>()) as *const usize).read_volatile, ((str_start + *i) as *const u8).read_volatile, (0usize..)
            .find, (0usize..)
            .find(|i| unsafe { ((str_start + *i) as *const u8).read_volatile() == 0 })
            .unwrap, HEAP.lock, HEAP.lock()
            .init ...(+10) | ((argv + i * core::mem::size_of::<usize>()) as *const usize).read_volatile, ((str_start + *i) as *const u8).read_volatile, (0usize..)
            .find, (0usize..)
            .find(|i| unsafe { ((str_start + *i) as *const u8).read_volatile() == 0 })
            .unwrap, HEAP.lock, HEAP.lock()
            .init ...(+10) |
| `GCore/os/src/hal/arch/loongarch64/time.rs#get_timer_freq_first_time` | `os/src/arch/la64/time.rs#get_timer_freq_first_time` | 同板块同名同结构 | cfg5.get_bits, config::CPUCfg4::read, config::CPUCfg4::read().get_bits, config::CPUCfg5::read, println! | cfg5.get_bits, config::CPUCfg4::read, config::CPUCfg4::read().get_bits, config::CPUCfg5::read, println! |
| `GCore/os/src/main.rs#mem_clear` | `os/src/main.rs#mem_clear` | 同板块同名同结构 | core::slice::from_raw_parts_mut, core::slice::from_raw_parts_mut(
            sbss as usize as *mut u8,
            crate::config::MEMORY_END - sbss as usize,
        )
        .fill, core::slice::from_raw_parts_mut(sbss as usize as *mut u8, ebss as usize - sbss as usize)
            .fill | core::slice::from_raw_parts_mut, core::slice::from_raw_parts_mut(
            sbss as usize as *mut u8,
            crate::config::MEMORY_END - sbss as usize,
        )
        .fill, core::slice::from_raw_parts_mut(sbss as usize as *mut u8, ebss as usize - sbss as usize)
            .fill |


好的，我将以仲裁专家身份，基于提供的证据底座、控辩双方陈述及AST抽样数据，进行最终同源性定性仲裁。

### 1. 宏观判定
- **定性结论**: **独立开发，存在同源参考痕迹**
  综合客观AST相似度（34.16%）、双向覆盖率（目标项目37.19%，参考项目80.74%）及双方模块画像，判定目标项目 `[2026-T2026101839910132-XX]` 相对于参考项目 `[2024-NPUcore-重生之我是菜狗-西北工业大学]` 为**独立开发**。目标项目在架构（多核SMP、HAL多架构抽象）、功能（ext4文件系统、独立驱动）上存在显著且重大的独立增量。然而，参考项目高达80.74%的覆盖率及进程管理模块中部分核心系统调用底层逻辑的高度一致性，表明目标项目在开发初期或特定POSIX接口实现上，深度参考或复用了参考项目的代码框架与设计思路，但并非简单的整体抄袭。

### 2. 全模块覆盖矩阵解读
- **进程管理**: 双方均存在。存在共同AST指纹。目标覆盖率34.92%，参考覆盖率66.67%。参考项目大部分实现能在目标项目中找到对应，但目标项目有大量独立扩展。证据强度：**中**。
- **任务调度/异步运行时**: 双方均存在。存在共同AST指纹。目标覆盖率76.12%，参考覆盖率82.93%。双方高度重合，但目标项目引入了SMP多核调度，架构有根本性变化。证据强度：**中**。
- **内存管理**: 双方均存在。存在共同AST指纹。目标覆盖率69.98%，参考覆盖率82.45%。核心页面管理与分配算法高度相似，但目标项目有独立扩展。证据强度：**中**。
- **中断/异常处理**: 双方均存在。存在共同AST指纹。目标覆盖率77.5%，参考覆盖率93.94%。参考项目几乎完全被目标项目覆盖，但目标项目因HAL层引入，在架构抽象上存在差异。证据强度：**弱**。
- **系统调用**: 双方均存在。存在共同AST指纹。目标覆盖率66.67%，参考覆盖率73.47%。POSIX系统调用骨架高度相似，这是规范约束的必然结果。证据强度：**弱**。
- **文件系统**: 双方均存在。存在共同AST指纹。目标覆盖率68.44%，参考覆盖率76.56%。FAT32等共有文件系统实现相似度高，但目标项目额外实现了ext4等。证据强度：**中**。
- **设备驱动**: 双方均存在。存在共同AST指纹。目标覆盖率68.09%，参考覆盖率94.12%。参考项目驱动几乎被完全覆盖，但目标项目驱动类型更丰富（如virtio-blk vs sata-blk）。证据强度：**弱**。
- **网络协议栈**: 双方均存在。存在共同AST指纹。目标覆盖率58.93%，参考覆盖率70.21%。TCP/UDP等协议栈骨架相似，但目标项目有独立网卡驱动和virtio-socket等扩展。证据强度：**弱**。
- **第三方库/公共依赖**: 仅目标项目存在。无共同AST指纹。目标覆盖率0.0%。这是目标项目独立开发的铁证，包含大量参考项目未涉及的驱动、文件系统、PCI枚举等模块。证据强度：**未形成**。

### 3. 底层 AST 关键证据指纹表
| 所属板块 | 目标项目路径 | 参考项目路径 | 证据类型 | 技术说明 |
| :--- | :--- | :--- | :--- | :--- |
| 进程管理 | `GCore/os/src/syscall/process.rs#sys_mmap` | `os/src/syscall/process.rs#sys_mmap` | 同源强指纹 | 内存映射权限转换使用了完全一致的位运算魔法数字 `MapPermission::from_bits(((prot as u8) << 1) \| (1 << 4))`，此为非标准映射，独立开发巧合概率极低。 |
| 进程管理 | `GCore/os/src/syscall/process.rs#sys_sigreturn` | `os/src/syscall/process.rs#sys_sigreturn` | 同源强指纹 | 从`ucontext_t`恢复上下文的偏移量计算逻辑完全一致，包括`2 * size_of::<usize>() + size_of::<SignalStack>() + ...`，仅路径前缀`crate::hal`与`crate::arch`不同，是典型的路径替换式复用。 |
| 进程管理 | `GCore/os/src/syscall/process.rs#sys_setsid` | `os/src/syscall/process.rs#sys_setsid` | 同源强指纹 | 脱离父进程会话组的操作链完全一致：`parent.acquire_inner_lock().children.retain` 及 `task.acquire_inner_lock().parent.as_ref().unwrap().upgrade`，此特定解包与过滤逻辑高度特异。 |
| 进程管理 | `GCore/os/src/task/threads.rs#do_futex_wait` | `os/src/task/threads.rs#do_futex_wait` | 同源强指纹 | futex等待队列的构建与阻塞逻辑完全一致：`Arc::downgrade, TimeSpec::now, WaitQueue::new, block_current_and_run_next, futex.inner.insert`，调用顺序与对象选择高度重合。 |
| 进程管理 | `GCore/os/src/syscall/process.rs#sys_nanosleep` | `os/src/syscall/process.rs#sys_nanosleep` | 同源强指纹 | 剩余时间计算与拷贝逻辑完全一致：`copy_to_user(token, &(end - now), rem).unwrap` 及异常时的 `copy_to_user(token, &TimeSpec::new(), rem).unwrap`。 |
| 进程管理 | `GCore/os/src/task/mod.rs#exit_group_and_run_next` | `os/src/task/mod.rs#exit_group_and_run_next` | 同源强指纹 | 进程组退出时，使用`VecDeque`进行子进程回收的逻辑完全一致：`VecDeque::new, exit_list.push_back, manager.interruptible_queue.pop_front, exit_list.into_iter`。 |
| 任务调度 | `GCore/os/src/task/processor.rs#run_tasks` | `os/src/task/processor.rs#run_tasks` | 架构差异 | 目标项目从单核`PROCESSOR`演进为多核`PROCESSORS`，并引入`hart_id`和per-hart idle task，是SMP架构的独立重构，掩盖了底层`__switch, fetch_task`的同源性。 |
| 内存管理 | `GCore/os/src/mm/memory_set.rs#do_page_fault` | `os/src/mm/memory_set.rs#do_page_fault` | 领域共性 | 缺页异常处理流程（`addr.floor, addr.page_offset`）高度相似，但这是任何虚拟内存管理实现的必经之路，属于领域共性，非抄袭铁证。 |
| 文件系统 | `GCore/os/src/fs/directory_tree.rs#init_proc_directory` | `os/src/fs/directory_tree.rs#init_proc_directory` | 功能增量 | 目标项目在参考项目`ROOT.mkdir, ROOT.open`基础上，增加了`meminfo`、`DirectoryTreeNode`等调用，是功能扩展，非简单复制。 |
| 第三方库 | `GCore/dependency/dep_iso/src/net/ethernet/intel/ixgbe.rs#new` | (无) | 独立开发铁证 | 目标项目独立实现了Intel ixgbe网卡驱动，参考项目无此模块。 |
| 第三方库 | `GCore/os/src/fs/ext4/extent.rs#extent_remove_space` | (无) | 独立开发铁证 | 目标项目独立实现了ext4文件系统的extent管理，参考项目无此模块。 |

### 4. 核心代码调用流对比剖析
* **【进程管理/sys_mmap 内存映射】**
* **底层技术析理**: 两者均实现了Linux `mmap`系统调用。核心同源性体现在将Linux `prot`标志（`PROT_READ=1, PROT_WRITE=2, PROT_EXEC=4`）转换为内部权限枚举的位运算逻辑上。两者均采用了 `((prot as u8) << 1) | (1 << 4)` 这一非标准映射公式，其中`1<<4`代表用户态可访问位。这种特定魔法数字的选择和位移操作组合，是极其强烈的同源指纹，几乎不可能由独立开发者巧合写出。目标项目仅将参考项目的`crate::arch`路径替换为`crate::hal::arch`，未改变任何核心逻辑。
* **代码溯源链接**:
  - 目标项目: `GCore/os/src/syscall/process.rs#sys_mmap`
  - 参考项目: `os/src/syscall/process.rs#sys_mmap`

* **【进程管理/sys_sigreturn 信号恢复】**
* **底层技术析理**: 两者均实现了从用户态`ucontext_t`结构恢复信号处理上下文的功能。核心同源性体现在计算`MachineContext`在`ucontext_t`中偏移量的逻辑上。两者均精确计算了`uc_flags`、`uc_link`、`uc_stack`、`uc_sigmask`及平台相关`PADDING`的总大小，公式 `2 * size_of::<usize>() + size_of::<SignalStack>() + size_of::<Signals>() + ... PADDING_SIZE` 完全一致。这直接反映了对相同内部数据结构布局的复用，而非对公开ABI的独立实现。
* **代码溯源链接**:
  - 目标项目: `GCore/os/src/syscall/process.rs#sys_sigreturn`
  - 参考项目: `os/src/syscall/process.rs#sys_sigreturn`

* **【任务调度/run_tasks 调度循环】**
* **底层技术析理**: 两者均实现了内核主调度循环。尽管底层`__switch`上下文切换和`fetch_task`任务获取逻辑同源，但目标项目进行了根本性的架构重构。参考项目使用单核`PROCESSOR.lock`，而目标项目演进为多核`PROCESSORS.lock`，并引入`hart_id`来索引per-hart数据结构。同时，目标项目增加了`crate::hal::arch::riscv::time::set_next_trigger`等HAL层调用，实现了多架构定时器抽象。这表明目标项目在参考项目基础上进行了SMP多核和HAL抽象的重大独立开发。
* **代码溯源链接**:
  - 目标项目: `GCore/os/src/task/processor.rs#run_tasks`
  - 参考项目: `os/src/task/processor.rs#run_tasks`

### 5. 单方模块与降权项
- **仅目标项目存在的模块/功能**:
  - **文件系统/第三方库**: ext4文件系统 (`GCore/os/src/fs/ext4/`)，是参考项目完全不具备的重大功能增量。
  - **网络协议栈/第三方库**: Intel ixgbe/e1000网卡驱动 (`GCore/dependency/dep_iso/src/net/ethernet/intel/`)，virtio-socket (`GCore/dependency/virtio-drivers/src/device/socket/`) 等，是独立开发的设备驱动。
  - **第三方库**: AHCI/SATA控制器驱动、PCI总线枚举、VirtIO Sound等大量独立第三方库集成。
  以上模块是目标项目独立开发的铁证，不能作为同源证据。

- **降权项说明**:
  - **跨板块同名/同Hash证据**: 如 `virtio_blk::read_block` vs `sata_blk::read_block`，`tcp::r_ready` vs `unix::r_ready`，虽函数名或局部哈希相同，但所属设备或协议完全不同，调用的外部对象和实现逻辑有本质区别，不构成同板块主证据，已降权为弱信号。
  - **泛型宏调用**: `info!`, `error!`, `panic!`, `assert!` 等Rust标准调试/断言宏，是语言层面的通用工具，其出现位置不构成独创性指纹，不作为抄袭证据采纳。

### 6. 保守结论
综合裁定，目标项目 `[2026-T2026101839910132-XX]` 并非对参考项目 `[2024-NPUcore-重生之我是菜狗-西北工业大学]` 的简单整体抄袭。34.16%的客观AST相似度及大量独有模块（ext4、ixgbe驱动等）表明，其主体是具备显著独立增量的二次开发。然而，在【进程管理】板块的核心POSIX接口实现上，如`sys_mmap`的位运算、`sys_sigreturn`的偏移量计算等，存在无法用“领域共性”解释的、像素级的同源强指纹，明确指向对参考项目代码的深度复用。因此，该项目应被定性为**在参考开源项目基础上进行重大架构升级与功能扩展的独立作品，但其进程管理子系统的部分实现存在明确的同源复用痕迹，不构成完全原创。**

------------------------------

