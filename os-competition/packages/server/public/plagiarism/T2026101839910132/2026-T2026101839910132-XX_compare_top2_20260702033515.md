# 🛡️ OS 内核赛自动化代码级架构审计报告

**审计时间**: `2026-07-02 03:35:15`
**参赛作品名称**: `2026-T2026101839910132-XX`
**年份**: `2026`
**学校**: `XX`
**队伍名称**: `T2026101839910132`
**比赛名字**: `OS-Competition`
**比赛子赛道**: `Kernel-Implementation`
**仓库地址**: `https://gitlab.eduxiji.net/educg-group-43501-3132633/T2026101839910132-3878`
**推测开发语言**: `Rust / C (由AST探针自动识别)`
**特征函数量**: `3735`

## ⚔️ 底层代码同源性对比 (AST Homology)
> 本部分由专门的代码审查智能体，对该项目与符合年份条件的历届最高相似度项目进行控制流与底层调用对比。新版报告会先给出确定性的全模块覆盖矩阵，再给出 Agent 的分模块解释，避免只围绕少量抽样函数展开。

### 🏆 Top 2 底层对标: vs `2025-NPUcore-BLOSSOM-西北工业大学`（西北工业大学 / NPUcore-BLOSSOM）

#### 对方作品信息

**对方年份**: `2025`
**对方学校**: `西北工业大学`
**对方队伍名称**: `NPUcore-BLOSSOM`
**对方比赛名字**: `OS-Competition`
**对方比赛子赛道**: `Kernel-Implementation`
**对方仓库地址**: `https://gitlab.eduxiji.net/educg-group-36002-2710490/oskernel2025-npucore-blossom`

> 双向覆盖率：共同 AST 指纹: 2051；唯一 AST 指纹: 目标项目 2412，参考项目 2354；覆盖率: 目标项目 85.03%，参考项目 87.13%

## 全模块对比总览（确定性证据）

> 总体覆盖率：共同 AST 指纹: 2051；唯一 AST 指纹: 目标项目 2412，参考项目 2354；覆盖率: 目标项目 85.03%，参考项目 87.13%

### 1. 模块覆盖矩阵
| 模块 | 目标函数数 | 参考函数数 | 共同唯一 AST 指纹 | Target 覆盖 | Base 覆盖 | 状态 |
| :--- | ---: | ---: | ---: | ---: | ---: | :--- |
| 进程管理 | 141 | 75 | 45 | 35.71% | 66.18% | 双方存在且有结构重合 |
| 任务调度/异步运行时 | 147 | 143 | 103 | 76.87% | 79.23% | 双方存在且有结构重合 |
| 内存管理 | 622 | 605 | 362 | 81.72% | 84.58% | 双方存在且有结构重合 |
| 中断/异常处理 | 48 | 47 | 36 | 90.0% | 90.0% | 双方存在且有结构重合 |
| 系统调用 | 72 | 65 | 38 | 70.37% | 73.08% | 双方存在且有结构重合 |
| 文件系统 | 573 | 561 | 301 | 84.08% | 80.91% | 双方存在且有结构重合 |
| 设备驱动 | 47 | 47 | 43 | 91.49% | 91.49% | 双方存在且有结构重合 |
| 网络协议栈 | 107 | 88 | 33 | 58.93% | 70.21% | 双方存在且有结构重合 |
| 文件系统/第三方库 | 372 | 369 | 219 | 75.26% | 75.78% | 双方存在且有结构重合 |
| 网络协议栈/第三方库 | 138 | 138 | 122 | 99.19% | 99.19% | 双方存在且有结构重合 |
| 第三方库 | 1230 | 1228 | 706 | 99.72% | 100.0% | 双方存在且有结构重合 |
| 通用模块/其他 | 238 | 241 | 109 | 87.2% | 84.5% | 双方存在且有结构重合 |

### 2. 分模块证据清单

#### 进程管理
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `GCore/os/src/syscall/process.rs#sys_prlimit` | `os/src/syscall/process.rs#sys_prlimit` | 同板块同名 | Resource::from_primitive, assert!, copy_from_user, copy_from_user(token, new_limit, rlimit).is_err, copy_to_user, copy_to_user(
                        token,
                        &(RLimit {
                            rlim_cur: SYSTEM_TASK_LIMIT,
                            rlim_max: SYSTEM_TASK_LIMIT,
                        }),
                        old_limit,
                    )
                    .is_err ...(+18) | Resource::from_primitive, assert!, copy_from_user, copy_from_user(token, new_limit, rlimit).is_err, copy_to_user, copy_to_user(
                        token,
                        &(RLimit {
                            rlim_cur: SYSTEM_TASK_LIMIT,
                            rlim_max: SYSTEM_TASK_LIMIT,
                        }),
                        old_limit,
                    )
                    .is_err ...(+18) |
| `GCore/os/src/syscall/process.rs#sys_futex` | `os/src/syscall/process.rs#sys_futex` | 同板块同名 | FutexOption::from_bits_truncate, current_task, current_task().unwrap, do_futex_wait, drop, info! ...(+16) | FutexOption::from_bits_truncate, current_task, current_task().unwrap, do_futex_wait, drop, info! ...(+15) |
| `GCore/os/src/syscall/process.rs#sys_execve` | `os/src/syscall/process.rs#sys_execve` | 同板块同名同结构 | Box::<[u8; 4]>::new, DEFAULT_SHELL.to_string, Some, Vec::with_capacity, arg_ptr.is_null, argv.add ...(+22) | Box::<[u8; 4]>::new, DEFAULT_SHELL.to_string, Some, Vec::with_capacity, arg_ptr.is_null, argv.add ...(+22) |
| `GCore/os/src/task/threads.rs#do_futex_wait` | `os/src/task/threads.rs#do_futex_wait` | 同板块同名同结构 | Arc::downgrade, TimeSpec::now, WaitQueue::new, block_current_and_run_next, current_task, current_task().unwrap ...(+11) | Arc::downgrade, TimeSpec::now, WaitQueue::new, block_current_and_run_next, current_task, current_task().unwrap ...(+11) |

#### 任务调度/异步运行时
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `GCore/os/src/syscall/fs.rs#sys_splice` | `os/src/syscall/fs.rs#sys_splice` | 同板块同名 | Some, Vec::<u8>::with_capacity, buffer.as_mut_slice, buffer.as_slice, buffer.set_len, current_task ...(+16) | Some, Vec::<u8>::with_capacity, buffer.as_mut_slice, buffer.as_slice, buffer.set_len, current_task ...(+17) |
| `GCore/os/src/syscall/fs.rs#sys_fcntl` | `os/src/syscall/fs.rs#sys_fcntl` | 同板块同名 | Fcntl_Command::from_primitive, OpenFlags::O_APPEND.bits, OpenFlags::O_NONBLOCK.bits, OpenFlags::O_RDWR.bits, current_task, current_task().unwrap ...(+12) | Fcntl_Command::from_primitive, OpenFlags::O_NONBLOCK.bits, OpenFlags::O_RDWR.bits, current_task, current_task().unwrap, fd_table.get_ref ...(+10) |
| `GCore/os/src/syscall/fs.rs#sys_sendfile` | `os/src/syscall/fs.rs#sys_sendfile` | 同板块同名同结构 | Some, Vec::<u8>::with_capacity, buffer.as_mut_slice, buffer.as_slice, buffer.set_len, current_task ...(+17) | Some, Vec::<u8>::with_capacity, buffer.as_mut_slice, buffer.as_slice, buffer.set_len, current_task ...(+17) |
| `GCore/os/src/syscall/fs.rs#sys_fstatat` | `os/src/syscall/fs.rs#sys_fstatat` | 同板块同名 | FstatatFlags::from_bits, copy_to_user, copy_to_user(token, &stat, buf as *mut Stat).is_err, current_task, current_task().unwrap, current_user_token ...(+15) | FstatatFlags::from_bits, copy_to_user, copy_to_user(token, &file_descriptor.get_stat(), buf as *mut Stat).is_err, current_task, current_task().unwrap, current_user_token ...(+12) |

#### 内存管理
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `GCore/os/src/fs/mod.rs#flush_preload` | `os/src/fs/mod.rs#flush_preload` | 同板块同名 | Err, Ok, ROOT_FD
            .mkdir, ROOT_FD
            .mkdir("etc")
            .or_else, ROOT_FD
            .mkdir("etc")
            .or_else(|e| if e == 17 || e == -17 /* EEXIST */ || e == -1 { Ok(()) } else { Err(e) })
            .unwrap_or, ROOT_FD
            .mkdir("glibc")
            .or_else ...(+49) | ROOT_FD.open, ROOT_FD.open("bash", OpenFlags::O_CREAT, false).unwrap, ROOT_FD.open("initproc", OpenFlags::O_CREAT, false).unwrap, bash.write, core::slice::from_raw_parts, crate::mm::PPNRange::new ...(+8) |
| `GCore/os/src/task/signal.rs#do_signal` | `os/src/task/signal.rs#do_signal` | 同板块同名同结构 | (trap_cx as *const TrapContext).cast::<MachineContext>, SigInfo::new, SignalStack::new, Signals::SIGSEGV.to_signum, Signals::SIGSEGV.to_signum().unwrap, Signals::from_bits_truncate ...(+40) | (trap_cx as *const TrapContext).cast::<MachineContext>, SigInfo::new, SignalStack::new, Signals::SIGSEGV.to_signum, Signals::SIGSEGV.to_signum().unwrap, Signals::from_bits_truncate ...(+40) |
| `GCore/os/src/hal/arch/loongarch64/trap/mod.rs#trap_handler` | `os/src/hal/arch/loongarch64/trap/mod.rs#trap_handler` | 同板块同名 | (0..sz).rev, BadV::read, BadV::read().get_vaddr, ERA::read, ERA::read().next_ins, ERA::read().next_ins().write ...(+57) | (0..sz).rev, BadV::read, BadV::read().get_vaddr, ERA::read, ERA::read().next_ins, ERA::read().next_ins().write ...(+55) |
| `GCore/os/src/mm/memory_set.rs#do_page_fault` | `os/src/mm/memory_set.rs#do_page_fault` | 同板块同名 | Err, Ok, PhysAddr::from, VirtAddr::from, addr.floor, addr.page_offset ...(+34) | Err, Ok, PhysAddr::from, VirtAddr::from, addr.floor, addr.page_offset ...(+34) |

#### 中断/异常处理
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `GCore/os/src/hal/arch/loongarch64/trap/mod.rs#trap_from_kernel` | `os/src/hal/arch/loongarch64/trap/mod.rs#trap_from_kernel` | 同板块同名同结构 | ((addr + i) as *mut u8).read_unaligned, ((addr + i) as *mut u8).write_unaligned, (0..sz).rev, BadV::read, BadV::read().get_vaddr, EStat::read ...(+14) | ((addr + i) as *mut u8).read_unaligned, ((addr + i) as *mut u8).write_unaligned, (0..sz).rev, BadV::read, BadV::read().get_vaddr, EStat::read ...(+14) |
| `GCore/os/src/hal/arch/loongarch64/trap/mem_access.rs#get_addr` | `os/src/hal/arch/loongarch64/trap/mem_access.rs#get_addr` | 同板块同名同结构 | self.get_op_code, self.get_op_code().map, self.get_rj_num, self.get_rk_num, self.get_si12, self.get_si14 | self.get_op_code, self.get_op_code().map, self.get_rj_num, self.get_rk_num, self.get_si12, self.get_si14 |
| `GCore/os/src/hal/arch/loongarch64/register/base/estat.rs#cause` | `os/src/hal/arch/loongarch64/register/base/estat.rs#cause` | 同板块同名 | Exception::try_from, Interrupt::try_from, MErrCtl::read, MErrCtl::read().is_merr, TLBRERA::read, TLBRERA::read().is_tlbr ...(+8) | Exception::try_from, Exception::try_from(ecode).unwrap, Interrupt::try_from, Interrupt::try_from(63 - self.get_is().leading_zeros() as usize).unwrap, MErrCtl::read, MErrCtl::read().is_merr ...(+11) |
| `GCore/os/src/hal/arch/loongarch64/trap/mem_access.rs#get_size` | `os/src/hal/arch/loongarch64/trap/mem_access.rs#get_size` | 同板块同名同结构 | 内部闭环逻辑 | 内部闭环逻辑 |

#### 系统调用
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `GCore/os/src/net/tcp.rs#_read` | `os/src/net/tcp.rs#_read` | 同板块同名同结构 | Err, GeneralRet::Err, GeneralRet::Ok, NET_INTERFACE.poll, NET_INTERFACE.tcp_socket, Ok ...(+7) | Err, GeneralRet::Err, GeneralRet::Ok, NET_INTERFACE.poll, NET_INTERFACE.tcp_socket, Ok ...(+7) |
| `GCore/os/src/net/tcp.rs#_accept` | `os/src/net/tcp.rs#_accept` | 同板块同名 | Err, GeneralRet::Err, GeneralRet::Ok, NET_INTERFACE.poll, NET_INTERFACE.tcp_socket, Ok ...(+7) | Err, GeneralRet::Err, GeneralRet::Ok, NET_INTERFACE.poll, NET_INTERFACE.tcp_socket, Ok ...(+7) |
| `GCore/os/src/net/udp.rs#connect` | `os/src/net/udp.rs#connect` | 同板块同名同结构 | Err, IpListenEndpoint::from, NET_INTERFACE.poll, NET_INTERFACE.udp_socket, Ok, SocketAddrv4::new ...(+11) | Err, IpListenEndpoint::from, NET_INTERFACE.poll, NET_INTERFACE.udp_socket, Ok, SocketAddrv4::new ...(+11) |
| `GCore/os/src/net/udp.rs#_read` | `os/src/net/udp.rs#_read` | 同板块同名同结构 | Err, GeneralRet::Err, GeneralRet::Ok, NET_INTERFACE.poll, NET_INTERFACE.udp_socket, Ok ...(+10) | Err, GeneralRet::Err, GeneralRet::Ok, NET_INTERFACE.poll, NET_INTERFACE.udp_socket, Ok ...(+10) |

#### 文件系统
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `GCore/os/src/fs/directory_tree.rs#rename` | `os/src/fs/directory_tree.rs#rename` | 同板块同名同结构 | Arc::downgrade, Arc::new, Err, Mutex::new, Ok, ROOT.cd_comp ...(+41) | Arc::downgrade, Arc::new, Err, Mutex::new, Ok, ROOT.cd_comp ...(+41) |
| `GCore/os/src/fs/directory_tree.rs#init_device_directory` | `os/src/fs/directory_tree.rs#init_device_directory` | 同板块同名 | "null".to_string, "rtc".to_string, "tty".to_string, "urandom".to_string, "zero".to_string, Arc::downgrade ...(+23) | "null".to_string, "rtc".to_string, "tty".to_string, "urandom".to_string, "zero".to_string, Arc::downgrade ...(+21) |
| `GCore/os/src/fs/fat32/fat_inode.rs#delete_dir_ent` | `os/src/fs/fat32/fat_inode.rs#delete_dir_ent` | 同板块同名同结构 | Err, FATDirEnt::unused_and_last_entry, FATDirEnt::unused_not_last_entry, Ok, Some, debug_assert! ...(+14) | Err, FATDirEnt::unused_and_last_entry, FATDirEnt::unused_not_last_entry, Ok, Some, debug_assert! ...(+14) |
| `GCore/os/src/syscall/fs.rs#sys_utimensat` | `os/src/syscall/fs.rs#sys_utimensat` | 同板块同名 | Some, String::new, TimeSpec::now, UtimensatFlags::from_bits, __openat, copy_from_user ...(+11) | Some, String::new, TimeSpec::now, UtimensatFlags::from_bits, __openat, copy_from_user ...(+10) |

#### 设备驱动
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `GCore/os/src/drivers/block/sata_blk.rs#pci_init` | `os/src/drivers/block/sata_blk.rs#pci_init` | 同板块同名同结构 | AHCI::new, Some, Status::empty, dev.bars.iter, dev.bars.iter().enumerate, dev.bars.iter().enumerate().for_each ...(+3) | AHCI::new, Some, Status::empty, dev.bars.iter, dev.bars.iter().enumerate, dev.bars.iter().enumerate().for_each ...(+3) |
| `GCore/os/src/drivers/block/virtio_blk.rs#read_block` | `os/src/drivers/block/virtio_blk.rs#read_block` | 同板块同名同结构 | assert!, buf.chunks_mut, buf.chunks_mut(VIRT_IO_BLOCK_SZ).enumerate, self.0
                .lock, self.0
                .lock()
                .read_blocks, self.0
                .lock()
                .read_blocks(virtio_block_id as usize, chunk)
                .expect | assert!, buf.chunks_mut, buf.chunks_mut(VIRT_IO_BLOCK_SZ).enumerate, self.0
                .lock, self.0
                .lock()
                .read_blocks, self.0
                .lock()
                .read_blocks(virtio_block_id as usize, chunk)
                .expect |
| `GCore/os/src/drivers/block/virtio_blk.rs#write_block` | `os/src/drivers/block/virtio_blk.rs#write_block` | 同板块同名同结构 | assert!, buf.chunks, buf.chunks(VIRT_IO_BLOCK_SZ).enumerate, self.0
                .lock, self.0
                .lock()
                .write_blocks, self.0
                .lock()
                .write_blocks(virtio_block_id as usize, chunk)
                .expect | assert!, buf.chunks, buf.chunks(VIRT_IO_BLOCK_SZ).enumerate, self.0
                .lock, self.0
                .lock()
                .write_blocks, self.0
                .lock()
                .write_blocks(virtio_block_id as usize, chunk)
                .expect |
| `GCore/os/src/mm/map_area.rs#force_swap_out` | `os/src/mm/map_area.rs#force_swap_out` | 同板块同名 | Err, Frame::SwappedOut, Ok, SWAP_DEVICE.lock, SWAP_DEVICE.lock().write, frame_ref.ppn.get_bytes_array | Err, Frame::SwappedOut, Ok, SWAP_DEVICE.lock, SWAP_DEVICE.lock().write, frame_ref.ppn.get_bytes_array |

#### 网络协议栈
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `GCore/os/src/net/tcp.rs#r_ready` | `os/src/net/unix.rs#r_ready` | 同板块同名 | NET_INTERFACE.tcp_socket, socket.can_recv, socket.state | todo! |
| `GCore/os/src/net/mod.rs#can_bind` | `os/src/net/mod.rs#can_bind` | 同板块同名同结构 | Some, log::info!, self.0.clone, socket.loacl_endpoint, socket.loacl_endpoint().eq, socket.socket_type ...(+1) | Some, log::info!, self.0.clone, socket.loacl_endpoint, socket.loacl_endpoint().eq, socket.socket_type ...(+1) |
| `GCore/os/src/net/tcp.rs#shutdown` | `os/src/net/tcp.rs#shutdown` | 同板块同名同结构 | NET_INTERFACE.poll, NET_INTERFACE.tcp_socket, Ok, info!, socket.abort, socket.close | NET_INTERFACE.poll, NET_INTERFACE.tcp_socket, Ok, info!, socket.abort, socket.close |
| `GCore/os/src/net/unix.rs#shutdown` | `os/src/net/unix.rs#shutdown` | 同板块同名 | Ok, self.shut_rd.lock, self.shut_wr.lock | Ok, log::info! |

#### 文件系统/第三方库
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `GCore/os/src/fs/ext4/extent.rs#extent_remove_space` | `os/src/fs/ext4/extent.rs#extent_remove_space` | 同板块同名同结构 | Block::load_offset, Ext4Extent::default, Ok, ex.get_actual_len, ex.get_first_block, ex.get_pblock ...(+14) | Block::load_offset, Ext4Extent::default, Ok, ex.get_actual_len, ex.get_first_block, ex.get_pblock ...(+14) |
| `GCore/os/src/fs/ext4/extent.rs#ext_remove_leaf` | `os/src/fs/ext4/extent.rs#ext_remove_leaf` | 同板块同名同结构 | Block::load_inode_root_block, Block::load_offset, Ext4Extent::default, Ok, ex.get_actual_len, ex.get_pblock ...(+14) | Block::load_inode_root_block, Block::load_offset, Ext4Extent::default, Ok, ex.get_actual_len, ex.get_pblock ...(+14) |
| `GCore/os/src/fs/ext4/file.rs#write_at` | `os/src/fs/ext4/file.rs#write_at` | 同板块同名同结构 | Block::load_offset, Ok, block.sync_blk_to_disk, block.write_offset, drop, inode_ref.inode.set_size ...(+10) | Block::load_offset, Ok, block.sync_blk_to_disk, block.write_offset, drop, inode_ref.inode.set_size ...(+10) |
| `GCore/os/src/fs/ext4/balloc.rs#balloc_alloc_block` | `os/src/fs/ext4/balloc.rs#balloc_alloc_block` | 同板块同名同结构 | Block::load_offset, Err, Ext4BlockGroup::load_new, Ok, block_group
                        .set_block_group_balloc_bitmap_csum, block_group.get_block_bitmap_block ...(+17) | Block::load_offset, Err, Ext4BlockGroup::load_new, Ok, block_group
                        .set_block_group_balloc_bitmap_csum, block_group.get_block_bitmap_block ...(+17) |

#### 网络协议栈/第三方库
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `GCore/dependency/virtio-drivers/src/device/socket/connectionmanager.rs#send_recv` | `dependency/virtio-drivers/src/device/socket/connectionmanager.rs#send_recv` | 同板块同名同结构 | "Hello from guest".as_bytes, (hello_from_guest.len() as u32).into, (hello_from_host.len() as u32).into, 0.into, 50.into, Arc::new ...(+43) | "Hello from guest".as_bytes, (hello_from_guest.len() as u32).into, (hello_from_host.len() as u32).into, 0.into, 50.into, Arc::new ...(+43) |
| `GCore/dependency/virtio-drivers/src/device/socket/connectionmanager.rs#incoming_connection` | `dependency/virtio-drivers/src/device/socket/connectionmanager.rs#incoming_connection` | 同板块同名同结构 | 0.into, 50.into, Arc::new, Mutex::new, ReadOnly::new, SocketType::Stream.into ...(+24) | 0.into, 50.into, Arc::new, Mutex::new, ReadOnly::new, SocketType::Stream.into ...(+24) |
| `GCore/dependency/virtio-drivers/examples/x86_64/src/tcp.rs#test_echo_server` | `dependency/virtio-drivers/examples/x86_64/src/tcp.rs#test_echo_server` | 同板块同名同结构 | Config::new, DeviceWrapper::new, Instant::from_micros_const, Interface::new, IpAddress::from_str, IpAddress::from_str(IP).unwrap ...(+44) | Config::new, DeviceWrapper::new, Instant::from_micros_const, Interface::new, IpAddress::from_str, IpAddress::from_str(IP).unwrap ...(+44) |
| `GCore/dependency/virtio-drivers/examples/riscv/src/tcp.rs#test_echo_server` | `dependency/virtio-drivers/examples/riscv/src/tcp.rs#test_echo_server` | 同板块同名同结构 | Config::new, DeviceWrapper::new, Instant::from_micros_const, Interface::new, IpAddress::from_str, IpAddress::from_str(IP).unwrap ...(+44) | Config::new, DeviceWrapper::new, Instant::from_micros_const, Interface::new, IpAddress::from_str, IpAddress::from_str(IP).unwrap ...(+44) |

#### 第三方库
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `GCore/dependency/virtio-drivers/src/queue.rs#fake_read_write_queue` | `dependency/virtio-drivers/src/queue.rs#fake_read_write_queue` | 同板块同名同结构 | (*available_ring).idx.load, (*used_ring).idx.fetch_add, (*used_ring).idx.load, Vec::new, assert!, assert_eq! ...(+18) | (*available_ring).idx.load, (*used_ring).idx.fetch_add, (*used_ring).idx.load, Vec::new, assert!, assert_eq! ...(+18) |
| `GCore/dependency/dep_pci/pci/src/lib.rs#probe_function` | `dependency/dep_pci/pci/src/lib.rs#probe_function` | 同板块同名同结构 | BAR::decode, BridgeControl::from_bits_truncate, Command::from_bits_truncate, DeviceKind::CardbusBridge, DeviceKind::Device, DeviceKind::PciBridge ...(+6) | BAR::decode, BridgeControl::from_bits_truncate, Command::from_bits_truncate, DeviceKind::CardbusBridge, DeviceKind::Device, DeviceKind::PciBridge ...(+6) |
| `GCore/dependency/virtio-drivers/src/device/sound/fake.rs#handle_control_request` | `dependency/virtio-drivers/src/device/sound/fake.rs#handle_control_request` | 同板块同名同结构 | CommandCode::SOk.into, Some, Vec::new, VirtIOSndHdr {
                            command_code: CommandCode::SOk.into(),
                        }
                        .as_bytes, VirtIOSndHdr::read_from_prefix, VirtIOSndHdr::read_from_prefix(&request)
                .expect ...(+16) | CommandCode::SOk.into, Some, Vec::new, VirtIOSndHdr {
                            command_code: CommandCode::SOk.into(),
                        }
                        .as_bytes, VirtIOSndHdr::read_from_prefix, VirtIOSndHdr::read_from_prefix(&request)
                .expect ...(+16) |
| `GCore/dependency/virtio-drivers/src/queue.rs#recycle_descriptors` | `dependency/virtio-drivers/src/queue.rs#recycle_descriptors` | 同板块同名同结构 | Box::from_raw, H::unshare, InputOutputIter::new, InputOutputIter::new(inputs, outputs).enumerate, Some, assert_eq! ...(+17) | Box::from_raw, H::unshare, InputOutputIter::new, InputOutputIter::new(inputs, outputs).enumerate, Some, assert_eq! ...(+17) |

#### 通用模块/其他
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `GCore/os/src/hal/arch/loongarch64/boot.rs#_start` | `user/src/lib.rs#_start` | 同板块同名 | asm! | _parameter, asm!, core::arch::asm!, unreachable! |
| `GCore/user/src/lib.rs#_start` | `os/src/hal/arch/loongarch64/boot.rs#_start` | 同板块同名 | _parameter, asm!, core::arch::asm!, unreachable! | naked_asm! |
| `GCore/user/src/lib.rs#_parameter` | `user/src/lib.rs#_parameter` | 同板块同名同结构 | ((argv + i * core::mem::size_of::<usize>()) as *const usize).read_volatile, ((str_start + *i) as *const u8).read_volatile, (0usize..)
            .find, (0usize..)
            .find(|i| unsafe { ((str_start + *i) as *const u8).read_volatile() == 0 })
            .unwrap, HEAP.lock, HEAP.lock()
            .init ...(+10) | ((argv + i * core::mem::size_of::<usize>()) as *const usize).read_volatile, ((str_start + *i) as *const u8).read_volatile, (0usize..)
            .find, (0usize..)
            .find(|i| unsafe { ((str_start + *i) as *const u8).read_volatile() == 0 })
            .unwrap, HEAP.lock, HEAP.lock()
            .init ...(+10) |
| `GCore/os/src/hal/arch/loongarch64/time.rs#get_timer_freq_first_time` | `os/src/hal/arch/loongarch64/time.rs#get_timer_freq_first_time` | 同板块同名同结构 | cfg5.get_bits, config::CPUCfg4::read, config::CPUCfg4::read().get_bits, config::CPUCfg5::read, println! | cfg5.get_bits, config::CPUCfg4::read, config::CPUCfg4::read().get_bits, config::CPUCfg5::read, println! |


### 🛡️ 审计报告：最终同源性定性仲裁

#### 1. 宏观判定
- **定性结论**: **部分逻辑借鉴 / 存在架构改动**
- **相似度**: 75.54%
- **共同 AST 指纹**: 2051
- **唯一 AST 指纹**: 目标项目 2412，参考项目 2354
- **覆盖率**: 目标项目 85.03%，参考项目 87.13%

**判定摘要**：目标项目 `GCore` 与参考项目 `NPUcore` 在整体 AST 层面存在显著的结构重合，尤其在任务调度、内存管理、文件系统等核心内核模块。然而，在【进程管理】这一关键板块，目标项目展现出明显的独立开发与架构改动特征（函数数量 141 vs 75，覆盖率仅 35.71%）。综合判断，该项目并非对参考项目的整体照搬，而是在一个高度同源的内核基座上，进行了以多核调度、进程模型扩展为代表的独立架构演进，同时存在对部分系统调用实现逻辑的借鉴。第三方库的高度一致属于合法的公共依赖复用。

#### 2. 全模块覆盖矩阵解读

| 模块 | 目标函数数 | 参考函数数 | 共同唯一 AST 指纹 | Target 覆盖 | Base 覆盖 | 证据强度 | 解读 |
| :--- | ---: | ---: | ---: | ---: | ---: | :--- | :--- |
| **进程管理** | 141 | 75 | 45 | 35.71% | 66.18% | **弱** | 目标项目函数规模远超参考项目，覆盖率低。大量独立实现的进程相关函数表明此模块存在显著的架构扩展与原创开发。 |
| **任务调度/异步运行时** | 147 | 143 | 103 | 76.87% | 79.23% | **强** | 双方函数规模相当，覆盖率极高。核心调度原语、同步机制（如 futex）的实现存在高度同源性，是内核基座重合的核心区域。 |
| **内存管理** | 622 | 605 | 362 | 81.72% | 84.58% | **强** | 函数规模庞大且覆盖率很高。页表操作、内存映射（mmap）、缺页处理等底层逻辑高度一致，表明内存管理子系统是同源内核基座的重要组成部分。 |
| **中断/异常处理** | 48 | 47 | 36 | 90.0% | 90.0% | **强** | 函数数量几乎一致，覆盖率极高。Trap 处理、上下文保存与恢复等架构强相关代码高度重合，是内核基座同源的有力证据。 |
| **系统调用** | 72 | 65 | 38 | 70.37% | 73.08% | **中** | 存在大量同名同功能的系统调用入口，但目标项目有更多扩展。部分调用（如 `sys_mmap`）底层逻辑一致，但亦有独立封装（如时间获取）。 |
| **文件系统** | 573 | 561 | 301 | 84.08% | 80.91% | **强** | 函数规模庞大且覆盖率很高。FAT32、EXT4 等复杂文件系统的实现逻辑高度一致，是内核基座重合的重要组成部分。 |
| **设备驱动** | 47 | 47 | 43 | 91.49% | 91.49% | **强** | 函数数量完全一致，覆盖率极高。VirtIO、SATA 等块设备驱动实现几乎完全相同，是内核基座同源的直接证据。 |
| **网络协议栈** | 107 | 88 | 33 | 58.93% | 70.21% | **中** | 目标项目函数更多，覆盖率中等。TCP/UDP 等核心协议实现存在重合，但目标项目有独立扩展（如 Unix Socket）。 |
| **文件系统/第三方库** | 372 | 369 | 219 | 75.26% | 75.78% | **中** | 此分类为混合模块，包含部分内核代码与第三方库。高度重合可能源于公共依赖或内核代码复用。 |
| **网络协议栈/第三方库** | 138 | 138 | 122 | 99.19% | 99.19% | **弱** | 覆盖率极高，函数数量完全一致。此模块极大概率是双方共同依赖的第三方网络库（如 smoltcp），不构成内核原创性抄袭证据。 |
| **第三方库** | 1230 | 1228 | 706 | 99.72% | 100.0% | **弱** | 覆盖率极高，函数规模庞大。明确为公共第三方库（如 virtio-drivers, pci, ext4 等）的合法复用，不构成抄袭证据。 |
| **通用模块/其他** | 238 | 241 | 109 | 87.2% | 84.5% | **中** | 包含架构相关启动代码、用户库等。部分启动逻辑（如 `_start`）存在差异，但用户库（如 `_parameter`）高度一致。 |

#### 3. 底层 AST 关键证据指纹表

| 所属板块 | 目标项目路径 | 参考项目路径 | 证据类型 | 技术说明 |
| :--- | :--- | :--- | :--- | :--- |
| **任务调度/异步运行时** | `GCore/os/src/task/threads.rs#do_futex_wait` | `os/src/task/threads.rs#do_futex_wait` | 同板块同名同结构 | Futex 等待队列的完整操作流（`Arc::downgrade` -> `WaitQueue::new` -> `block_current_and_run_next` -> `futex.inner.insert`）完全一致，是内核同步原语同源的强证据。 |
| **内存管理** | `GCore/os/src/mm/memory_set.rs#do_page_fault` | `os/src/mm/memory_set.rs#do_page_fault` | 同板块同名 | 缺页异常处理的核心逻辑（`VirtAddr::from` -> `addr.floor` -> `memory_set.mmap` 等）调用流高度一致，表明内存管理子系统同源。 |
| **中断/异常处理** | `GCore/os/src/hal/arch/loongarch64/trap/mod.rs#trap_from_kernel` | `os/src/hal/arch/loongarch64/trap/mod.rs#trap_from_kernel` | 同板块同名同结构 | 内核态 Trap 处理中的内存访问修复逻辑（`BadV::read` -> `(0..sz).rev` 循环读写）完全一致，是架构相关代码同源的铁证。 |
| **文件系统** | `GCore/os/src/fs/directory_tree.rs#rename` | `os/src/fs/directory_tree.rs#rename` | 同板块同名同结构 | 文件/目录重命名的复杂逻辑（`ROOT.cd_comp` -> `Arc::downgrade` -> `Mutex::new` 等）调用图谱完全重合，是文件系统实现同源的强证据。 |
| **设备驱动** | `GCore/os/src/drivers/block/virtio_blk.rs#read_block` | `os/src/drivers/block/virtio_blk.rs#read_block` | 同板块同名同结构 | VirtIO 块设备读取操作（`buf.chunks_mut` -> `self.0.lock().read_blocks`）完全一致，是驱动代码同源的直接证据。 |
| **进程管理** | `GCore/os/src/syscall/process.rs#sys_sigreturn` | `os/src/syscall/process.rs#sys_sigreturn` | 同板块底层逻辑一致 | `ucontext_addr + 2 * size_of::<usize>() + ... + PADDING_SIZE` 的复杂内存布局计算完全一致，是信号处理 ABI 实现逻辑借鉴的强证据。 |
| **进程管理** | `GCore/os/src/syscall/process.rs#sys_mmap` | `os/src/syscall/process.rs#sys_mmap` | 同板块底层逻辑一致 | `MapPermission::from_bits(((prot as u8) << 1) \| (1 << 4)).unwrap` 的权限位转换魔数运算完全一致，是 mmap 实现逻辑借鉴的强证据。 |
| **进程管理** | `GCore/os/src/task/processor.rs#run_tasks` | `os/src/task/processor.rs#run_tasks` | 同板块同名 | 核心调度函数。目标项目使用 `PROCESSORS.lock` 和 `hart_id` 实现多核调度，参考项目使用 `PROCESSOR.lock` 实现单核调度，展示了在同源骨架上的关键架构改动。 |
| **系统调用** | `GCore/os/src/syscall/process.rs#sys_clock_gettime` | `os/src/syscall/process.rs#sys_clock_gettime` | 同板块同名 | 目标项目使用 `crate::timer::current_time` 等独立时间子系统，参考项目使用 `TimeSpec::now`，展示了在相同系统调用接口下的不同底层封装。 |
| **第三方库/公共依赖** | `GCore/dependency/virtio-drivers/src/queue.rs#fake_read_write_queue` | `dependency/virtio-drivers/src/queue.rs#fake_read_write_queue` | 同板块同名同结构 | 完全一致的测试/模拟函数，明确为双方共同依赖的第三方库 `virtio-drivers` 的代码，不构成内核原创性抄袭。 |

#### 4. 核心代码调用流对比剖析

* **【进程管理 / 调度器架构】**
* **底层技术析理**: 这是本案最关键的架构差异点。参考项目 `NPUcore` 的 `run_tasks` 函数通过 `PROCESSOR.lock` 获取单核处理器结构体，并调用 `processor.get_idle_task_cx_ptr` 获取空闲任务上下文，是典型的单核调度模型。目标项目 `GCore` 则使用 `PROCESSORS.lock` 获取一个多核处理器数组，并通过 `hart_id` 索引当前核心，调用 `guard[id].get_idle_task_cx_ptr`，并显式地加入了 `set_next_trigger` 和 `enable_timer_interrupt` 等 RISC-V 架构特定的多核定时器中断逻辑。这表明 `GCore` 并非简单地将变量名从单数改为复数，而是在调度器的核心数据结构、CPU 索引方式及中断控制上进行了系统性的多核架构改造。
* **代码溯源链接**:
  - 目标项目: `GCore/os/src/task/processor.rs#run_tasks`
  - 参考项目: `os/src/task/processor.rs#run_tasks`

* **【进程管理 / 信号处理】**
* **底层技术析理**: 双方在 `sys_sigreturn` 中的内存布局计算 `ucontext_addr + 2 * size_of::<usize>() + size_of::<SignalStack>() + size_of::<Signals>() + crate::hal::UserContext::PADDING_SIZE` 完全一致。此表达式精确描述了 Linux `ucontext_t` 结构体在栈上的内存布局，用于恢复进程上下文。这种涉及多个结构体大小和对齐填充的复杂指针运算，其 AST 节点序列完全重合，是底层实现逻辑直接借鉴的强有力证据，难以用“独立实现同一规范”来解释。
* **代码溯源链接**:
  - 目标项目: `GCore/os/src/syscall/process.rs#sys_sigreturn`
  - 参考项目: `os/src/syscall/process.rs#sys_sigreturn`

* **【内存管理 / 缺页异常处理】**
* **底层技术析理**: 双方在 `do_page_fault` 函数中的处理流程高度一致，均遵循 `VirtAddr::from` -> `addr.floor` -> `memory_set.mmap` 的调用路径。这表明两者在虚拟地址解析、页对齐以及通过内存映射处理缺页的底层机制上共享同一套设计蓝图。这是内核内存管理子系统同源的核心证据。
* **代码溯源链接**:
  - 目标项目: `GCore/os/src/mm/memory_set.rs#do_page_fault`
  - 参考项目: `os/src/mm/memory_set.rs#do_page_fault`

* **【文件系统 / 目录操作】**
* **底层技术析理**: 双方在 `rename` 系统调用的底层实现中，其复杂的目录树操作逻辑（`ROOT.cd_comp` -> `Arc::downgrade` -> `Mutex::new` -> 节点替换）调用图谱完全重合。这揭示了文件系统核心的目录项管理、引用计数和并发控制模型的高度同源性。
* **代码溯源链接**:
  - 目标项目: `GCore/os/src/fs/directory_tree.rs#rename`
  - 参考项目: `os/src/fs/directory_tree.rs#rename`

#### 5. 单方模块与降权项

*   **单方模块/功能**:
    *   **目标项目独有**: 进程管理模块中大量超出参考项目的函数（141 vs 75），如更完善的 `sys_prlimit` 资源限制处理、独立的 `sys_futex` 实现（使用 `oldval.wrapping_add` 而非 `task.futex.lock`）等，表明目标项目进行了显著的独立功能扩展。
    *   **参考项目独有**: 无明显独有模块，但存在部分未完成接口（如 `sys_kill` 中的 `todo!`），目标项目已将其补全。

*   **降权项（不构成强同源证据）**:
    *   **第三方库/公共依赖**: `virtio-drivers`, `dep_pci`, `ext4` 等模块覆盖率高达 99% 以上，是明确的公共依赖复用，必须从内核原创性判定中剥离。
    *   **通用接口/宏**: `copy_from_user`, `copy_to_user`, `current_task`, `info!`, `error!` 等是操作系统内核中普遍存在的辅助函数或宏，其跨模块出现是正常框架设计，不能作为特定模块的抄袭证据。
    *   **过于简短或通用的函数**: `sys_memorybarrier`（仅调用 `error!`）、`sys_uname`（通用缓冲区操作）、`_start`（入口函数）等，因其逻辑过于简单或通用，其相似性不具备强证明力。

#### 6. 保守结论

综合审计，目标项目 `GCore` 与参考项目 `NPUcore` 在任务调度、内存管理、文件系统、设备驱动等内核核心模块上存在高度 AST 结构重合，表明两者共享一个同源的内核基座。然而，在【进程管理】这一关键板块，`GCore` 展现了明确的独立架构演进，包括将单核调度器改造为多核（SMP）架构、扩展大量进程管理系统调用、以及采用独立的时间子系统封装。因此，整体定性为 **“部分逻辑借鉴 / 存在架构改动”**。该项目并非对参考项目的简单复制，而是在一个同源基座上进行了非平凡的二次开发和架构升级。建议将 75.54% 的 AST 相似度视为高风险信号，但不应直接等价于重度违规，需结合架构差异和独立扩展部分进行综合评估。

------------------------------

