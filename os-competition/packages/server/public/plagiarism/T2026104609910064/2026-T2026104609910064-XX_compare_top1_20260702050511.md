# 🛡️ OS 内核赛自动化代码级架构审计报告

**审计时间**: `2026-07-02 05:05:11`
**参赛作品名称**: `2026-T2026104609910064-XX`
**年份**: `2026`
**学校**: `XX`
**队伍名称**: `T2026104609910064`
**比赛名字**: `OS-Competition`
**比赛子赛道**: `Kernel-Implementation`
**仓库地址**: `https://gitlab.eduxiji.net/educg-group-43501-3132633/T2026104609910064-854`
**推测开发语言**: `Rust / C (由AST探针自动识别)`
**特征函数量**: `4346`

## ⚔️ 底层代码同源性对比 (AST Homology)
> 本部分由专门的代码审查智能体，对该项目与符合年份条件的历届最高相似度项目进行控制流与底层调用对比。新版报告会先给出确定性的全模块覆盖矩阵，再给出 Agent 的分模块解释，避免只围绕少量抽样函数展开。

### 🏆 Top 1 底层对标: vs `2025-SubsToKernel-北京科技大学`（北京科技大学 / SubsToKernel）

#### 对方作品信息

**对方年份**: `2025`
**对方学校**: `北京科技大学`
**对方队伍名称**: `SubsToKernel`
**对方比赛名字**: `OS-Competition`
**对方比赛子赛道**: `Kernel-Implementation`
**对方仓库地址**: `https://gitlab.eduxiji.net/educg-group-36002-2710490/T202510008995695-2720`

> 双向覆盖率：共同 AST 指纹: 3429；唯一 AST 指纹: 目标项目 3441，参考项目 3437；覆盖率: 目标项目 99.65%，参考项目 99.77%

## 全模块对比总览（确定性证据）

> 总体覆盖率：共同 AST 指纹: 3429；唯一 AST 指纹: 目标项目 3441，参考项目 3437；覆盖率: 目标项目 99.65%，参考项目 99.77%

### 1. 模块覆盖矩阵
| 模块 | 目标函数数 | 参考函数数 | 共同唯一 AST 指纹 | Target 覆盖 | Base 覆盖 | 状态 |
| :--- | ---: | ---: | ---: | ---: | ---: | :--- |
| 进程管理 | 86 | 86 | 72 | 97.3% | 97.3% | 双方存在且有结构重合 |
| 任务调度/异步运行时 | 141 | 141 | 134 | 98.53% | 98.53% | 双方存在且有结构重合 |
| 内存管理 | 334 | 329 | 288 | 97.96% | 98.97% | 双方存在且有结构重合 |
| 中断/异常处理 | 8 | 8 | 7 | 100.0% | 100.0% | 双方存在且有结构重合 |
| 系统调用 | 109 | 108 | 75 | 98.68% | 100.0% | 双方存在且有结构重合 |
| 文件系统 | 256 | 256 | 187 | 100.0% | 100.0% | 双方存在且有结构重合 |
| 设备驱动 | 57 | 57 | 47 | 100.0% | 100.0% | 双方存在且有结构重合 |
| 网络协议栈 | 124 | 124 | 119 | 100.0% | 100.0% | 双方存在且有结构重合 |
| 文件系统/第三方库 | 704 | 704 | 597 | 100.0% | 100.0% | 双方存在且有结构重合 |
| 网络协议栈/第三方库 | 2388 | 2388 | 1805 | 100.0% | 100.0% | 双方存在且有结构重合 |
| 通用模块/其他 | 139 | 139 | 128 | 99.22% | 99.22% | 双方存在且有结构重合 |

### 2. 分模块证据清单

#### 进程管理
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `os/src/syscall/process.rs#sys_exec` | `os/src/syscall/process.rs#sys_exec` | 同板块同名 | "ENOUGH=100000".to_string, "LD_BIND_NOW=0".to_string, "LD_LIBRARY_PATH=/lib:/glibc/lib:/musl/lib:".to_string, "LD_PRELOAD=".to_string, "LD_WARN=".to_string, "PATH=/:/bin:".to_string ...(+32) | "ENOUGH=100000".to_string, "LD_BIND_NOW=0".to_string, "LD_LIBRARY_PATH=/lib:/glibc/lib:/musl/lib:".to_string, "LD_PRELOAD=".to_string, "LD_WARN=".to_string, "PATH=/:/bin:".to_string ...(+34) |
| `os/src/syscall/process.rs#sys_futex` | `os/src/syscall/process.rs#sys_futex` | 同板块同名同结构 | FutexCmd::from_bits, FutexCmd::from_bits(futex_op & 0x7f).unwrap, FutexKey::new, FutexOpt::from_bits_truncate, VirtAddr::from, add_timer ...(+29) | FutexCmd::from_bits, FutexCmd::from_bits(futex_op & 0x7f).unwrap, FutexKey::new, FutexOpt::from_bits_truncate, VirtAddr::from, add_timer ...(+29) |
| `os/src/syscall/process.rs#sys_clock_nano_sleep` | `os/src/syscall/process.rs#sys_clock_nano_sleep` | 同板块同名同结构 | calculate_left_timespec, check_if_any_sig_for_current_task, current_process, debug!, drop, get_time_ms ...(+7) | calculate_left_timespec, check_if_any_sig_for_current_task, current_process, debug!, drop, get_time_ms ...(+7) |
| `os/src/task/processor.rs#run_tasks` | `os/src/task/processor.rs#run_tasks` | 同板块同名同结构 | PROCESSOR.exclusive_access, Some, __switch, check_timer, debug!, drop ...(+10) | PROCESSOR.exclusive_access, Some, __switch, check_timer, debug!, drop ...(+10) |

#### 任务调度/异步运行时
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `os/src/syscall/mod.rs#syscall` | `os/src/syscall/mod.rs#syscall` | 同板块同名 | info!, panic!, sys_accept, sys_bind, sys_brk, sys_chdir ...(+108) | info!, panic!, sys_accept, sys_bind, sys_brk, sys_chdir ...(+106) |
| `os/src/syscall/net.rs#sys_setsockopt` | `os/src/syscall/net.rs#sys_setsockopt` | 同板块同名同结构 | current_process, debug!, inner.fd_table.get, inner.fd_table.get(sockfd).file.sock, inner.fd_table.len, inner.fd_table.try_get ...(+4) | current_process, debug!, inner.fd_table.get, inner.fd_table.get(sockfd).file.sock, inner.fd_table.len, inner.fd_table.try_get ...(+4) |
| `os/src/syscall/fs.rs#sys_pselect6` | `os/src/syscall/fs.rs#sys_pselect6` | 同板块同名同结构 | Some, current_process, current_task, current_task().unwrap, current_task().unwrap().set_sig_mask, drop ...(+24) | Some, current_process, current_task, current_task().unwrap, current_task().unwrap().set_sig_mask, drop ...(+24) |
| `os/src/syscall/net.rs#sys_sendto` | `os/src/syscall/net.rs#sys_sendto` | 同板块同名同结构 | (data.len() - offset).min, Ipv4Addr::from, Ipv4Addr::new, SocketAddr::V4, SocketAddrV4::new, Some ...(+27) | (data.len() - offset).min, Ipv4Addr::from, Ipv4Addr::new, SocketAddr::V4, SocketAddrV4::new, Some ...(+27) |

#### 内存管理
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `os/src/task/process.rs#fork` | `os/src/task/process.rs#fork` | 同板块同名同结构 | Arc::clone, Arc::downgrade, Arc::new, FdTable::from_another, FsInfo::from_another, MemorySet::from_existed_user ...(+54) | Arc::clone, Arc::downgrade, Arc::new, FdTable::from_another, FsInfo::from_another, MemorySet::from_existed_user ...(+54) |
| `os/src/utils/error.rs#str` | `os/src/utils/error.rs#str` | 同板块同名同结构 | panic! | panic! |
| `os/src/task/process.rs#exec` | `os/src/task/process.rs#exec` | 同板块同名同结构 | Arc::new, Aux::new, FdTable::from_another, KERNEL_SPACE.exclusive_access, KERNEL_SPACE.exclusive_access().token, MemorySet::from_elf ...(+39) | Arc::new, Aux::new, FdTable::from_another, KERNEL_SPACE.exclusive_access, KERNEL_SPACE.exclusive_access().token, MemorySet::from_elf ...(+39) |
| `os/src/task/mod.rs#exit_current_and_run_next` | `os/src/task/mod.rs#exit_current_and_run_next` | 同板块同名 | Arc::clone, Arc::get_mut, FutexKey::new, Some, THREAD_GROUP.lock, TaskContext::zero_init ...(+56) | Arc::clone, Arc::get_mut, FutexKey::new, Some, THREAD_GROUP.lock, TaskContext::zero_init ...(+57) |

#### 中断/异常处理
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `os/src/signal/signal.rs#default_op` | `os/src/signal/signal.rs#default_op` | 同板块同名同结构 | continue_signals.contains, dump_signals.contains, ignore_signals.contains, stop_signals.contains, terminate_signals.contains | continue_signals.contains, dump_signals.contains, ignore_signals.contains, stop_signals.contains, terminate_signals.contains |
| `os/src/hal/trap/mod.rs#trap_handler_kernel` | `os/src/hal/trap/mod.rs#trap_handler_kernel` | 同板块同名同结构 | crmd.plv, crmd::read, era::read, estat.cause, estat::read, panic! ...(+1) | crmd.plv, crmd::read, era::read, estat.cause, estat::read, panic! ...(+1) |
| `os/src/hal/trap/mod.rs#enable_timer_interrupt` | `os/src/hal/trap/mod.rs#enable_timer_interrupt` | 同板块同名同结构 | crmd::set_ie, ecfg::set_lie, get_timer_freq, sie::set_stimer, tcfg::set_en, tcfg::set_init_val ...(+2) | crmd::set_ie, ecfg::set_lie, get_timer_freq, sie::set_stimer, tcfg::set_en, tcfg::set_init_val ...(+2) |
| `os/src/hal/trap/mod.rs#trap_from_kernel` | `os/src/hal/trap/mod.rs#trap_from_kernel` | 同板块同名同结构 | era::read, estat::read, panic!, warn! | era::read, estat::read, panic!, warn! |

#### 系统调用
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `os/src/syscall/mem.rs#sys_shmget` | `os/src/syscall/mem.rs#sys_shmget` | 同板块同名同结构 | ShmFlags::from_bits, ShmFlags::from_bits(shmflag & !0x1ff).unwrap, flags.contains, shm_create, shm_find | ShmFlags::from_bits, ShmFlags::from_bits(shmflag & !0x1ff).unwrap, flags.contains, shm_create, shm_find |
| `user/src/syscall.rs#sys_basicsh` | `user/src/syscall.rs#sys_basicsh` | 同板块同名同结构 | "basic_testcode.sh\0".as_ptr, "busybox\0".as_ptr, "sh\0".as_ptr, [
                "busybox\0".as_ptr() as isize,
                "sh\0".as_ptr() as isize,
                "basic_testcode.sh\0".as_ptr() as isize,
                //"busybox_testcode.sh\0".as_ptr() as isize,
                //"./test-ltp.sh\0".as_ptr() as isize,
                //"ltp/testcases/bin/abort01\0".as_ptr() as isize,
                // "libctest_testcode.sh\0".as_ptr() as isize,
                0,
            ]
            .as_ptr, syscall6 | "basic_testcode.sh\0".as_ptr, "busybox\0".as_ptr, "sh\0".as_ptr, [
                "busybox\0".as_ptr() as isize,
                "sh\0".as_ptr() as isize,
                "basic_testcode.sh\0".as_ptr() as isize,
                //"busybox_testcode.sh\0".as_ptr() as isize,
                //"./test-ltp.sh\0".as_ptr() as isize,
                //"ltp/testcases/bin/abort01\0".as_ptr() as isize,
                // "libctest_testcode.sh\0".as_ptr() as isize,
                0,
            ]
            .as_ptr, syscall6 |
| `os/src/syscall/mem.rs#sys_shmat` | `os/src/syscall/mem.rs#sys_shmat` | 同板块同名同结构 | ShmFlags::from_bits, ShmFlags::from_bits(shmflag).unwrap, shm_attach, shmflg.contains | ShmFlags::from_bits, ShmFlags::from_bits(shmflag).unwrap, shm_attach, shmflg.contains |
| `user/src/syscall.rs#sys_busyboxsh` | `user/src/syscall.rs#sys_busyboxsh` | 同板块同名同结构 | "sh\0".as_ptr, [
                test.as_ptr() as isize,
                "sh\0".as_ptr() as isize,
                cwd.as_ptr() as isize,
                //"./test-ltp.sh\0".as_ptr() as isize,
                //"ltp/testcases/bin/abort01\0".as_ptr() as isize,
                // "libctest_testcode.sh\0".as_ptr() as isize,
                0,
            ]
            .as_ptr, cwd.as_ptr, syscall6, test.as_ptr | "sh\0".as_ptr, [
                test.as_ptr() as isize,
                "sh\0".as_ptr() as isize,
                cwd.as_ptr() as isize,
                //"./test-ltp.sh\0".as_ptr() as isize,
                //"ltp/testcases/bin/abort01\0".as_ptr() as isize,
                // "libctest_testcode.sh\0".as_ptr() as isize,
                0,
            ]
            .as_ptr, cwd.as_ptr, syscall6, test.as_ptr |

#### 文件系统
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `os/src/mm/memory_set.rs#mmap` | `os/src/mm/memory_set.rs#mmap` | 同板块同名同结构 | MapArea::new_mmap, VirtAddr::from, VirtAddr::from(addr + len).ceil, VirtAddr::from(addr).floor, area.vpn_range.range, debug! ...(+8) | MapArea::new_mmap, VirtAddr::from, VirtAddr::from(addr + len).ceil, VirtAddr::from(addr).floor, area.vpn_range.range, debug! ...(+8) |
| `os/src/mm/memory_set.rs#load_dl_interp_if_needed` | `os/src/mm/memory_set.rs#load_dl_interp_if_needed` | 同板块同名同结构 | DL_INTERP_OFFSET.into, Some, String::from_utf8, String::from_utf8(section.raw_data(&elf).to_vec()).unwrap, cwd.contains, debug! ...(+21) | DL_INTERP_OFFSET.into, Some, String::from_utf8, String::from_utf8(section.raw_data(&elf).to_vec()).unwrap, cwd.contains, debug! ...(+21) |
| `os/src/fs/stat.rs#convert_kstat_to_statx` | `os/src/fs/stat.rs#convert_kstat_to_statx` | 同板块同名同结构 | Statx::default | Statx::default |
| `os/src/drivers/disk.rs#read_one` | `os/src/drivers/disk.rs#read_one` | 同板块同名同结构 | buf.len, buf.len().min, buf[..count].copy_from_slice, info!, self.dev.read_block | buf.len, buf.len().min, buf[..count].copy_from_slice, info!, self.dev.read_block |

#### 设备驱动
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `os/src/drivers/virtio/blk.rs#read_block` | `os/src/drivers/virtio/blk.rs#read_block` | 同板块同名同结构 | buf.fill, panic!, self.inner.lock, self.inner.lock().ack_interrupt, self.inner.lock().read_blocks | buf.fill, panic!, self.inner.lock, self.inner.lock().ack_interrupt, self.inner.lock().read_blocks |
| `os/src/drivers/virtio/blk.rs#write_block` | `os/src/drivers/virtio/blk.rs#write_block` | 同板块同名同结构 | panic!, self.inner.lock, self.inner.lock().ack_interrupt, self.inner.lock().write_blocks | panic!, self.inner.lock, self.inner.lock().ack_interrupt, self.inner.lock().write_blocks |
| `os/src/drivers/mod.rs#new_device` | `os/src/drivers/mod.rs#new_device` | 同板块同名同结构 | VirtIoNetDev::<VirtIoHalImpl, MmioTransport>::new, VirtIoNetDev::<VirtIoHalImpl, PciTransport>::new | VirtIoNetDev::<VirtIoHalImpl, MmioTransport>::new, VirtIoNetDev::<VirtIoHalImpl, PciTransport>::new |
| `os/src/net/socket/tcp.rs#block_on` | `os/src/net/socket/tcp.rs#block_on` | 同板块同名同结构 | Err, Ok, SOCKET_SET.poll_interfaces, f, self.is_nonblocking, suspend_current_and_run_next | Err, Ok, SOCKET_SET.poll_interfaces, f, self.is_nonblocking, suspend_current_and_run_next |

#### 网络协议栈
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `os/src/net/socket/tcp.rs#recv_timeout` | `os/src/net/socket/tcp.rs#recv_timeout` | 同板块同名同结构 | Err, Ok, SOCKET_SET.with_socket_mut::<tcp::Socket, _, _>, ax_err!, ax_err_type!, get_time ...(+11) | Err, Ok, SOCKET_SET.with_socket_mut::<tcp::Socket, _, _>, ax_err!, ax_err_type!, get_time ...(+11) |
| `os/src/net/socket/bench.rs#bench_transmit_bandwidth` | `os/src/net/socket/bench.rs#bench_transmit_bandwidth` | 同板块同名同结构 | (current_time - past_time).secs, AxNetTxToken::consume, InterfaceWrapper::current_time, info!, self.transmit, tx_buf[0..12].fill ...(+2) | (current_time - past_time).secs, AxNetTxToken::consume, InterfaceWrapper::current_time, info!, self.transmit, tx_buf[0..12].fill ...(+2) |
| `os/src/net/socket/tcp.rs#shutdown` | `os/src/net/socket/tcp.rs#shutdown` | 同板块同名同结构 | LISTEN_TABLE.unlisten, Ok, SOCKET_SET.poll_interfaces, SOCKET_SET.with_socket_mut::<tcp::Socket, _, _>, debug!, self.handle.get ...(+9) | LISTEN_TABLE.unlisten, Ok, SOCKET_SET.poll_interfaces, SOCKET_SET.with_socket_mut::<tcp::Socket, _, _>, debug!, self.handle.get ...(+9) |
| `os/src/net/socket/bench.rs#bench_receive_bandwidth` | `os/src/net/socket/bench.rs#bench_receive_bandwidth` | 同板块同名同结构 | (current_time - past_time).secs, AxNetRxToken::consume, InterfaceWrapper::current_time, info!, rx_buf.len, self.receive | (current_time - past_time).secs, AxNetRxToken::consume, InterfaceWrapper::current_time, info!, rx_buf.len, self.receive |

#### 文件系统/第三方库
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `os/libs/lwext4_rsut/c/lwext4/src/ext4_balloc.c#ext4_balloc_alloc_block` | `os/libs/lwext4_rsut/c/lwext4/src/ext4_balloc.c#ext4_balloc_alloc_block` | 同板块同名同结构 | ext4_balloc_get_bgid_of_block, ext4_balloc_get_block_of_bgid, ext4_balloc_set_bitmap_csum, ext4_balloc_verify_bitmap_csum, ext4_bg_get_block_bitmap, ext4_bg_get_free_blocks_count ...(+19) | ext4_balloc_get_bgid_of_block, ext4_balloc_get_block_of_bgid, ext4_balloc_set_bitmap_csum, ext4_balloc_verify_bitmap_csum, ext4_bg_get_block_bitmap, ext4_bg_get_free_blocks_count ...(+19) |
| `os/libs/lwext4_rsut/c/lwext4/src/ext4_journal.c#jbd_journal_prepare` | `os/libs/lwext4_rsut/c/lwext4/src/ext4_journal.c#jbd_journal_prepare` | 同板块同名同结构 | EXT4_BLOCK_ZERO, RB_FIND, TAILQ_FOREACH_REVERSE_SAFE, TAILQ_FOREACH_SAFE, TAILQ_REMOVE, ext4_bcache_clear_dirty ...(+17) | EXT4_BLOCK_ZERO, RB_FIND, TAILQ_FOREACH_REVERSE_SAFE, TAILQ_FOREACH_SAFE, TAILQ_REMOVE, ext4_bcache_clear_dirty ...(+17) |
| `os/libs/lwext4_rsut/c/lwext4/src/ext4.c#ext4_dir_rm` | `os/libs/lwext4_rsut/c/lwext4/src/ext4.c#ext4_dir_rm` | 同板块同名同结构 | EXT4_MP_LOCK, EXT4_MP_UNLOCK, ext4_block_cache_write_back, ext4_dir_en_get_inode, ext4_dir_iterator_fini, ext4_dir_iterator_init ...(+19) | EXT4_MP_LOCK, EXT4_MP_UNLOCK, ext4_block_cache_write_back, ext4_dir_en_get_inode, ext4_dir_iterator_fini, ext4_dir_iterator_init ...(+19) |
| `os/libs/lwext4_rsut/c/lwext4/src/ext4_dir_idx.c#ext4_dir_dx_split_index` | `os/libs/lwext4_rsut/c/lwext4/src/ext4_dir_idx.c#ext4_dir_dx_split_index` | 同板块同名同结构 | ext4_block_set, ext4_dir_dx_climit_get_count, ext4_dir_dx_climit_get_limit, ext4_dir_dx_climit_set_count, ext4_dir_dx_climit_set_limit, ext4_dir_dx_entry_get_hash ...(+10) | ext4_block_set, ext4_dir_dx_climit_get_count, ext4_dir_dx_climit_get_limit, ext4_dir_dx_climit_set_count, ext4_dir_dx_climit_set_limit, ext4_dir_dx_entry_get_hash ...(+10) |

#### 网络协议栈/第三方库
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `os/libs/smoltcp/src/socket/tcp.rs#process` | `os/libs/smoltcp/src/socket/tcp.rs#process` | 同板块同名同结构 | AckDelayTimer::Waiting, IpEndpoint::new, Self::random_seq_no, Self::rst_reply, Some, control.quash_psh ...(+42) | AckDelayTimer::Waiting, IpEndpoint::new, Self::random_seq_no, Self::rst_reply, Some, control.quash_psh ...(+42) |
| `os/libs/smoltcp/src/socket/tcp.rs#dispatch` | `os/libs/smoltcp/src/socket/tcp.rs#dispatch` | 同板块同名同结构 | IpRepr::new, Ok, Some, cx.ip_mtu, cx.now, emit ...(+45) | IpRepr::new, Ok, Some, cx.ip_mtu, cx.now, emit ...(+45) |
| `os/libs/smoltcp/src/iface/interface/mod.rs#dispatch_ip` | `os/libs/smoltcp/src/iface/interface/mod.rs#dispatch_ip` | 同板块同名同结构 | EthernetAddress, EthernetFrame::<&[u8]>::buffer_len, EthernetFrame::<&[u8]>::header_len, EthernetFrame::new_unchecked, Ipv4Packet::new_unchecked, Ok ...(+35) | EthernetAddress, EthernetFrame::<&[u8]>::buffer_len, EthernetFrame::<&[u8]>::header_len, EthernetFrame::new_unchecked, Ipv4Packet::new_unchecked, Ok ...(+35) |
| `os/libs/smoltcp/src/socket/dhcpv4.rs#dispatch` | `os/libs/smoltcp/src/socket/dhcpv4.rs#dispatch` | 同板块同名同结构 | Ok, Self::random_transaction_id, Some, cx.hardware_addr, cx.ip_mtu, cx.now ...(+12) | Ok, Self::random_transaction_id, Some, cx.hardware_addr, cx.ip_mtu, cx.now ...(+12) |

#### 通用模块/其他
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `os/src/hal/arch/info.rs#test_csr_register` | `os/src/hal/arch/info.rs#test_csr_register` | 同板块同名同结构 | crmd.da, crmd.ie, crmd.pg, crmd.plv, crmd::read, ecfg.lie ...(+13) | crmd.da, crmd.ie, crmd.pg, crmd.plv, crmd::read, ecfg.lie ...(+13) |
| `os/src/boards/qemu.rs#exit` | `os/src/boards/qemu.rs#exit` | 同板块同名同结构 | asm!, exit_code_encode | asm!, exit_code_encode |
| `os/src/signal/signal.rs#check_error` | `os/src/signal/signal.rs#check_error` | 同板块同名同结构 | Some, self.contains | Some, self.contains |
| `user/src/lib.rs#_start` | `user/src/lib.rs#_start` | 同板块同名同结构 | ((argv + i * core::mem::size_of::<usize>()) as *const usize).read_volatile, ((str_start + *i) as *const u8).read_volatile, (0usize..)
            .find, (0usize..)
            .find(|i| unsafe { ((str_start + *i) as *const u8).read_volatile() == 0 })
            .unwrap, HEAP.lock, HEAP.lock()
            .init ...(+11) | ((argv + i * core::mem::size_of::<usize>()) as *const usize).read_volatile, ((str_start + *i) as *const u8).read_volatile, (0usize..)
            .find, (0usize..)
            .find(|i| unsafe { ((str_start + *i) as *const u8).read_volatile() == 0 })
            .unwrap, HEAP.lock, HEAP.lock()
            .init ...(+11) |


好的，我将遵循审计指令，基于提供的证据材料，进行最终的同源性定性仲裁。

### 1. 宏观判定
- **定性结论**: 架构高度同构 / 代码直接复用 (相似度: 99.42%，共同 AST 指纹: 3429；唯一 AST 指纹: 目标项目 3441，参考项目 3437；覆盖率: 目标项目 99.65%，参考项目 99.77%)

### 2. 全模块覆盖矩阵解读
- **进程管理**: 双方均存在，共同指纹72，覆盖率均为97.3%。证据强度**强**。函数数量完全一致，且从系统调用到任务调度的核心路径高度重合。
- **任务调度/异步运行时**: 双方均存在，共同指纹134，覆盖率均为98.53%。证据强度**强**。调度器核心逻辑、上下文切换路径完全一致。
- **内存管理**: 双方均存在，目标334个函数，参考329个函数。覆盖率分别为97.96%和98.97%。证据强度**强**。尽管函数数量有微小差异，但核心的`mmap`、`munmap`、`fork`时的内存拷贝逻辑高度同构。
- **中断/异常处理**: 双方均存在，共同指纹7，覆盖率均为100%。证据强度**中**。该模块函数少，且与硬件架构（LoongArch）强相关，CSR寄存器操作必然收敛。
- **系统调用**: 双方均存在，目标109个函数，参考108个函数。覆盖率分别为98.68%和100%。证据强度**强**。系统调用分发逻辑及大部分具体实现（如`sys_futex`）完全一致。
- **文件系统**: 双方均存在，共同指纹187，覆盖率均为100%。证据强度**强**。包括procfs在内的虚拟文件系统接口实现逻辑完全一致。
- **设备驱动**: 双方均存在，共同指纹47，覆盖率均为100%。证据强度**中**。驱动模型与具体硬件（virtio）绑定，相似性高。
- **网络协议栈**: 双方均存在，共同指纹119，覆盖率均为100%。证据强度**强**。socket层实现（如tcp的`recv_timeout`）完全一致。
- **第三方库/公共依赖**: 双方均存在，文件系统库（lwext4）704个函数，网络库（smoltcp）2388个函数，覆盖率均为100%。证据强度**未形成**。这些是公开的第三方开源代码，其100%覆盖率是两项目共同依赖的直接证据，不能作为抄袭依据，但解释了整体AST覆盖率为何极高。

### 3. 底层 AST 关键证据指纹表
| 所属板块 | 目标项目路径 | 参考项目路径 | 证据类型 | 技术说明 |
| :--- | :--- | :--- | :--- | :--- |
| 进程管理 | `os/src/syscall/process.rs#sys_exec` | `os/src/syscall/process.rs#sys_exec` | 硬编码字符串字面量 | 动态链接器环境变量字符串完全一致，如`"LD_LIBRARY_PATH=/lib:/glibc/lib:/musl/lib:"`。 |
| 进程管理 | `os/src/syscall/process.rs#sys_futex` | `os/src/syscall/process.rs#sys_futex` | 特定魔法数字与位运算 | 使用`futex_op & 0x7f`解析futex命令，该掩码源自Linux ABI，但调用链完全一致。 |
| 进程管理 | `os/src/task/processor.rs#run_tasks` | `os/src/task/processor.rs#run_tasks` | 调度器核心调用流 | `PROCESSOR.exclusive_access` -> `fetch_task` -> `__switch`的调度循环结构完全相同。 |
| 进程管理 | `os/src/task/processor.rs#munmap` | `os/src/task/processor.rs#munmap` | 深度嵌套链式调用 | `current_task().unwrap().process.upgrade()...memory_set.munmap`的5层解包链完全一致。 |
| 内存管理 | `os/src/task/process.rs#fork` | `os/src/task/process.rs#fork` | 复杂对象构造流 | `MemorySet::from_existed_user`、`FdTable::from_another`等自定义构造函数调用序列一致。 |
| 系统调用 | `os/src/syscall/process.rs#sys_clock_nano_sleep` | `os/src/syscall/process.rs#sys_clock_nano_sleep` | 自定义辅助函数调用 | 均调用非标准的`calculate_left_timespec`和`check_if_any_sig_for_current_task`。 |
| 文件系统 | `os/src/fs/mod.rs#create_proc_dir_and_file` | `os/src/fs/mod.rs#create_proc_dir_and_file` | procfs路径拼接 | `format!("/proc/{}/stat", pid)`的路径字符串生成逻辑完全一致。 |
| 网络协议栈 | `os/src/net/socket/tcp.rs#recv_timeout` | `os/src/net/socket/tcp.rs#recv_timeout` | 自定义超时接收逻辑 | `SOCKET_SET.with_socket_mut::<tcp::Socket, _, _>`的调用及超时计算逻辑一致。 |
| 第三方库/公共依赖 | `os/libs/lwext4_rsut/...` | `os/libs/lwext4_rsut/...` | 第三方开源库 | lwext4库代码100%匹配，是公共依赖，非抄袭证据。 |
| 第三方库/公共依赖 | `os/libs/smoltcp/...` | `os/libs/smoltcp/...` | 第三方开源库 | smoltcp库代码100%匹配，是公共依赖，非抄袭证据。 |

### 4. 核心代码调用流对比剖析
* **【进程管理/futex同步机制】**
* **底层技术析理**: 两者在实现`sys_futex`系统调用时，均遵循相同的逻辑流：首先通过`FutexCmd::from_bits(futex_op & 0x7f)`解析命令，然后构造`FutexKey`，再根据命令（如`FUTEX_WAIT`）调用`futex_wait`。在`futex_wait`中，均操作全局`FUTEX_QUEUE`，使用`AtomicU32`进行值比较，并在不匹配时通过`block_current_and_run_next`挂起当前任务。这种从系统调用入口到任务调度出口的完整调用链高度一致，特别是`0x7f`掩码和`FutexKey`的构造方式，是框架级复用的强证据。
* **代码溯源链接**:
  - 目标项目: `os/src/syscall/process.rs#sys_futex` -> `os/src/task/futex.rs#futex_wait`
  - 参考项目: `os/src/syscall/process.rs#sys_futex` -> `os/src/task/futex.rs#futex_wait`

* **【内存管理/进程地址空间】**
* **底层技术析理**: 在`munmap`系统调用的实现中，两者均采用了完全相同的深层调用链来获取进程的内存管理对象：`current_task() -> Option<TaskControlBlock>`，通过`.process: Weak<ProcessControlBlock>`升级为`Arc`，再通过`.inner_exclusive_access()`获取`MutexGuard`，最终访问`.memory_set.munmap`。这种由Rust类型系统（`Option`, `Weak`, `Mutex`）决定的链式调用虽然具有收敛性，但长达5层的连续解包和属性访问在独立实现中几乎不可能完全一致，表明其数据结构设计和访问模式是直接复用的。
* **代码溯源链接**:
  - 目标项目: `os/src/task/processor.rs#munmap`
  - 参考项目: `os/src/task/processor.rs#munmap`

* **【文件系统/procfs实现】**
* **底层技术析理**: 两者在创建和管理`/proc`虚拟文件系统时，`create_proc_dir_and_file`和`remove_proc_dir_and_file`函数的实现逻辑完全一致。它们都使用`format!("/proc/{}", pid)`和`format!("/proc/{}/stat", pid)`来动态构建路径，然后调用`open`、`UserBuffer::new`进行文件创建，或调用`root_inode().unlink`、`remove_inode_idx`进行删除。这种非标准的procfs布局和操作序列的一致性，是文件系统层面代码复用的明确证据。
* **代码溯源链接**:
  - 目标项目: `os/src/fs/mod.rs#create_proc_dir_and_file` -> `os/src/fs/mod.rs#remove_proc_dir_and_file`
  - 参考项目: `os/src/fs/mod.rs#create_proc_dir_and_file` -> `os/src/fs/mod.rs#remove_proc_dir_and_file`

### 5. 单方模块与降权项
- **第三方库/公共依赖**: `文件系统/第三方库` (lwext4) 和 `网络协议栈/第三方库` (smoltcp) 两个模块的函数数量庞大（共3092个），且覆盖率均为100%。这明确表明两项目引用了完全相同的第三方开源库。这部分代码的相似性不能作为抄袭证据，应予以排除。
- **低信息量指纹**: `sys_spawn`仅含`trace!`宏，`read_tlbrsave_and_sp`仅含`asm!`宏，`set_stime`和`get_idle_task_cx_ptr`为“内部闭环逻辑”。这些证据信息量极低，无法支撑抄袭指控，应予以降权。
- **硬件架构约束**: `中断/异常处理`模块中的CSR寄存器操作（如`crmd::read`）和汇编指令（`asm!`）由LoongArch硬件规范强制决定，任何实现都必然相同，不能作为强同源证据。
- **Rust语言惯用法**: `String::from`、`Vec::new`、`format!`、`debug!`、`unwrap()`等是Rust标准库和泛型宏，其调用图谱的相似性与抄袭无关。

### 6. 保守结论
本次审计显示，目标项目与参考项目在排除第三方公共库后，于进程管理、内存管理、文件系统等核心自研模块上仍存在大量高度一致的AST指纹和调用图谱，包括特定的硬编码字符串、魔法数字、深度嵌套调用链及非标准辅助函数，证据指向其架构高度同构，存在代码直接复用的极高风险。然而，99.42%的宏观相似度在很大程度上被100%匹配的第三方库（lwext4, smoltcp）所拉高。辩方提出的基于公共教学框架（如rCore-Tutorial）迭代开发的解释，能够部分说明`PROCESSOR`、`__switch`等框架级命名的相似性。因此，不能将AST覆盖率直接等价于全盘抄袭。最终定性需结合人工源码比对，重点审查上述关键证据指纹中具体代码行的实现是否完全一致，以区分是框架约束下的独立实现还是直接搬运。

------------------------------

