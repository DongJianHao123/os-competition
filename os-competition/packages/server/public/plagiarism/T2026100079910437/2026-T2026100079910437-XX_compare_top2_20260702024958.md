# 🛡️ OS 内核赛自动化代码级架构审计报告

**审计时间**: `2026-07-02 02:49:58`
**参赛作品名称**: `2026-T2026100079910437-XX`
**年份**: `2026`
**学校**: `XX`
**队伍名称**: `T2026100079910437`
**比赛名字**: `OS-Competition`
**比赛子赛道**: `Kernel-Implementation`
**仓库地址**: `https://gitlab.eduxiji.net/educg-group-43501-3132633/T2026100079910437-1299`
**推测开发语言**: `Rust / C (由AST探针自动识别)`
**特征函数量**: `3179`

## ⚔️ 底层代码同源性对比 (AST Homology)
> 本部分由专门的代码审查智能体，对该项目与符合年份条件的历届最高相似度项目进行控制流与底层调用对比。新版报告会先给出确定性的全模块覆盖矩阵，再给出 Agent 的分模块解释，避免只围绕少量抽样函数展开。

### 🏆 Top 2 底层对标: vs `2025-模仿游戏-燕山大学`（燕山大学 / 模仿游戏）

#### 对方作品信息

**对方年份**: `2025`
**对方学校**: `燕山大学`
**对方队伍名称**: `模仿游戏`
**对方比赛名字**: `OS-Competition`
**对方比赛子赛道**: `Kernel-Implementation`
**对方仓库地址**: `https://gitlab.eduxiji.net/educg-group-36002-2710490/T202510216995249-4014`

> 双向覆盖率：共同 AST 指纹: 776；唯一 AST 指纹: 目标项目 2662，参考项目 1942；覆盖率: 目标项目 29.15%，参考项目 39.96%

## 全模块对比总览（确定性证据）

> 总体覆盖率：共同 AST 指纹: 776；唯一 AST 指纹: 目标项目 2662，参考项目 1942；覆盖率: 目标项目 29.15%，参考项目 39.96%

### 1. 模块覆盖矩阵
| 模块 | 目标函数数 | 参考函数数 | 共同唯一 AST 指纹 | Target 覆盖 | Base 覆盖 | 状态 |
| :--- | ---: | ---: | ---: | ---: | ---: | :--- |
| 进程管理 | 162 | 76 | 13 | 9.03% | 18.57% | 双方存在且有结构重合 |
| 任务调度/异步运行时 | 617 | 383 | 107 | 19.35% | 39.34% | 双方存在且有结构重合 |
| 内存管理 | 422 | 302 | 74 | 19.22% | 26.81% | 双方存在且有结构重合 |
| 中断/异常处理 | 14 | 56 | 1 | 7.14% | 2.0% | 双方存在且有结构重合 |
| 系统调用 | 164 | 121 | 44 | 28.95% | 44.9% | 双方存在且有结构重合 |
| 文件系统 | 767 | 631 | 141 | 22.45% | 30.39% | 双方存在且有结构重合 |
| 设备驱动 | 183 | 135 | 71 | 41.04% | 58.2% | 双方存在且有结构重合 |
| 网络协议栈 | 256 | 67 | 53 | 23.77% | 94.64% | 双方存在且有结构重合 |
| 文件系统/第三方库 | 37 | 25 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |
| 网络协议栈/第三方库 | 0 | 99 | 0 | 0.0% | 0.0% | 仅参考项目体现 |
| 通用模块/其他 | 557 | 611 | 262 | 59.68% | 55.16% | 双方存在且有结构重合 |

### 2. 分模块证据清单

#### 进程管理
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `api/src/syscall/task/execve.rs#sys_execve` | `api/src/imp/task/execve.rs#sys_execve` | 同板块同名 | Arc::new, Box::into_raw, Box::new, Default::default, Err, FD_TABLE.write ...(+49) | Err, Ok, arg.get_as_str, arg.get_as_str().map, argv
        .get_as_null_terminated, argv
        .get_as_null_terminated()?
        .iter ...(+37) |
| `api/src/syscall/task/wait.rs#sys_waitpid` | `api/src/imp/task/wait.rs#sys_waitpid` | 同板块同名 | AxError::from, Err, Ok, Poll::Ready, Some, WaitOptions::from_bits_truncate ...(+28) | Err, Ok, WaitOptions::from_bits_truncate, WaitPid::Pgid, WaitPid::Pid, child.data::<ProcessData> ...(+25) |
| `api/src/task.rs#do_exit` | `api/src/imp/task/exit.rs#do_exit` | 同板块同名 | FutexKey::new_current, SHM_MANAGER.lock, SHM_MANAGER.lock().clear_proc_shm, SignalInfo::new_kernel, Some, axtask::yield_now ...(+31) | FD_TABLE.clear, SignalInfo::new, UserPtr::<Pid>::from, axtask::exit, axtask::yield_now, clear_child_tid.get_as_mut ...(+22) |
| `arceos/modules/axtask/src/run_queue.rs#exit_current` | `arceos/modules/axtask/src/run_queue.rs#exit_current` | 同板块同名 | EXITED_TASKS.current_ref_mut_raw, EXITED_TASKS.current_ref_mut_raw().clear, EXITED_TASKS.current_ref_mut_raw().push_back, WAIT_FOR_EXIT.current_ref_mut_raw, WAIT_FOR_EXIT.current_ref_mut_raw().wake, assert! ...(+7) | EXITED_TASKS.current_ref_mut_raw, EXITED_TASKS.current_ref_mut_raw().clear, EXITED_TASKS.current_ref_mut_raw().push_back, WAIT_FOR_EXIT.current_ref_mut_raw, WAIT_FOR_EXIT.current_ref_mut_raw().notify_one, assert! ...(+8) |

#### 任务调度/异步运行时
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `arceos/ulib/axlibc/c/printf.c#format_string_loop` | `arceos/ulib/axlibc/c/printf.c#format_string_loop` | 同板块同名同结构 | ABS_FOR_PRINTING, ADVANCE_IN_FORMAT_STRING, atou_, is_digit_, out_rev_, parse_flags ...(+5) | ABS_FOR_PRINTING, ADVANCE_IN_FORMAT_STRING, atou_, is_digit_, out_rev_, parse_flags ...(+5) |
| `arceos/modules/axlog/src/lib.rs#log` | `arceos/modules/axlog/src/lib.rs#log` | 同板块同名同结构 | cfg_if::cfg_if!, record.level, record.line, record.line().unwrap_or, record.metadata, record.target ...(+1) | cfg_if::cfg_if!, record.level, record.line, record.line().unwrap_or, record.metadata, record.target ...(+1) |
| `api/src/syscall/sync/futex.rs#sys_futex` | `api/src/imp/futex.rs#sys_futex` | 同板块同名 | AxError::from, Err, FutexKey::new_current, Ok, Some, assert_unsigned ...(+20) | Err, Ok, axtask::yield_now, curr.task_ext, curr.task_ext().process_data, current ...(+16) |
| `arceos/modules/axtask/src/run_queue.rs#switch_to` | `arceos/modules/axtask/src/run_queue.rs#switch_to` | 同板块同名 | (*prev_ctx_ptr).switch_to, Arc::downgrade, CurrentTask::set_current, PREV_TASK.current_ref_mut_raw, assert!, clear_prev_task_on_cpu ...(+11) | (*prev_ctx_ptr).switch_to, Arc::downgrade, CurrentTask::set_current, PREV_TASK.current_ref_mut_raw, assert!, clear_prev_task_on_cpu ...(+8) |

#### 内存管理
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `api/src/syscall/task/clone.rs#sys_clone` | `api/src/imp/task/clone.rs#sys_clone` | 同板块同名 | (parent_tid as *mut Pid).vm_write, (parent_tid as *mut Pid).vm_write(tid).ok, (parent_tid as *mut i32).vm_write, Arc::new, AxTaskExt::from_impl, Box::into_raw ...(+99) | Arc::default, Arc::new, CURRENT_DIR
                .deref_from, CURRENT_DIR
                .deref_from(&process_data.ns)
                .init_new, CURRENT_DIR
                .deref_from(&process_data.ns)
                .init_shared, CURRENT_DIR.copy_inner ...(+72) |
| `api/src/syscall/mm/mmap.rs#sys_mmap` | `api/src/imp/mm/mmap.rs#sys_mmap` | 同板块同名 | (addr + length).align_up, Arc::new, Backend::new_alloc, Backend::new_cow, Backend::new_file, Backend::new_linear ...(+50) | (addr + length).align_up, Err, File::from_fd, MmapFlags::from_bits_truncate, MmapProt::from_bits_truncate, Ok ...(+27) |
| `arceos/ulib/axlibc/src/strftime.rs#strftime_inner` | `arceos/ulib/axlibc/src/strftime.rs#strftime_inner` | 同板块同名同结构 | CountingWriter::new, format.offset, inner_strftime, w! | CountingWriter::new, format.offset, inner_strftime, w! |
| `core/src/mm.rs#load_user_app` | `core/src/mm.rs#load_user_app` | 同板块同名 | "/bin/sh".to_owned, (ustack_top - user_sp_aligned).align_up_4k, AuxEntry::new, Backend::new_alloc, ELF_LOADER.lock, ELF_LOADER.lock().load ...(+67) | CStr::from_bytes_with_nul, CStr::from_bytes_with_nul(interp)
                .map_err, CStr::from_bytes_with_nul(interp)
                .map_err(|_| AxError::InvalidData)?
                .to_str, CStr::from_bytes_with_nul(interp)
                .map_err(|_| AxError::InvalidData)?
                .to_str()
                .map_err, ElfFile::new, ElfFile::new(&file_data).map_err ...(+45) |

#### 中断/异常处理
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `arceos/ulib/axlibc/c/pow.c#exp_inline` | `arceos/ulib/axlibc/c/pow.c#exp_inline` | 同板块同名同结构 | __math_oflow, __math_uflow, asdouble, asuint64, converttoint, eval_as_double ...(+4) | __math_oflow, __math_uflow, asdouble, asuint64, converttoint, eval_as_double ...(+4) |
| `arceos/modules/axsync/src/mutex.rs#may_interrupt` | `arceos/modules/axsync/src/mutex.rs#may_interrupt` | 同板块同名 | fastrand::u8, thread::yield_now | rand::random::<u32>, thread::yield_now |
| `arceos/modules/trampoline/src/arch/riscv/mod.rs#set_trap_vector_base` | `arceos/modules/axhal/src/arch/riscv/mod.rs#set_trap_vector_base` | 同板块同名 | stvec::write | reg.set_address, reg.set_trap_mode, stvec::read, stvec::write |
| `arceos/modules/trampoline/src/arch/riscv/mod.rs#trap_vector_base` | `arceos/modules/axhal/src/arch/aarch64/trap.rs#handle_sync_exception` | 同模块上下文对齐/差异观察 | core::arch::naked_asm! | ESR_EL1.extract, crate::trap::handle_syscall, crate::trap::post_trap_callback, debug!, esr.read, esr.read_as_enum ...(+6) |

#### 系统调用
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `arceos/api/arceos_posix_api/src/imp/net.rs#sys_getaddrinfo` | `arceos/api/arceos_posix_api/src/imp/net.rs#sys_getaddrinfo` | 同板块同名 | char_ptr_to_str, debug!, syscall_body! | char_ptr_to_str, debug!, syscall_body! |
| `api/src/syscall/io_mpx/epoll.rs#sys_epoll_ctl` | `arceos/api/arceos_posix_api/src/imp/io_mpx/epoll.rs#sys_epoll_ctl` | 同板块同名 | Epoll::from_fd, EpollFlags::from_bits, EpollFlags::from_bits(event.events & !events.bits()).ok_or, Err, IoEvents::from_bits_truncate, Ok ...(+7) | debug!, syscall_body! |
| `arceos/api/arceos_posix_api/src/imp/net.rs#sys_recvfrom` | `arceos/api/arceos_posix_api/src/imp/net.rs#sys_recvfrom` | 同板块同名同结构 | debug!, syscall_body! | debug!, syscall_body! |
| `arceos/api/arceos_posix_api/src/imp/net.rs#sys_socket` | `arceos/api/arceos_posix_api/src/imp/net.rs#sys_socket` | 同板块同名 | debug!, syscall_body! | debug!, syscall_body! |

#### 文件系统
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `arceos/ulib/axlibc/c/glob.c#do_glob` | `arceos/ulib/axlibc/c/glob.c#do_glob` | 同板块同名同结构 | S_ISDIR, append, closedir, do_glob, errfunc, fnmatch ...(+7) | S_ISDIR, append, closedir, do_glob, errfunc, fnmatch ...(+7) |
| `arceos/ulib/axlibc/src/setjmp.rs#longjmp` | `arceos/ulib/axlibc/src/setjmp.rs#longjmp` | 同板块同名 | core::arch::naked_asm! | core::arch::naked_asm! |
| `arceos/ulib/axlibc/src/setjmp.rs#setjmp` | `arceos/ulib/axlibc/src/setjmp.rs#setjmp` | 同板块同名 | core::arch::naked_asm! | core::arch::naked_asm! |
| `arceos/api/arceos_posix_api/src/imp/io_mpx/select.rs#poll_all` | `api/src/imp/fs/io_mpx/select.rs#poll_all` | 同板块同名 | Ok, debug!, execpt_bits_ptr.add, get_file_like, get_file_like(fd as _)?.poll, read_bits_ptr.add ...(+3) | Ok, Some, except_bits_ptr.add, f.poll, get_file_like, get_file_like(fd as _).and_then ...(+9) |

#### 设备驱动
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `arceos/modules/axdriver/src/lib.rs#init_drivers` | `arceos/modules/axdriver/src/lib.rs#init_drivers` | 同板块同名 | AllDevices::default, all_devs.block.iter, all_devs.block.iter().enumerate, all_devs.display.iter, all_devs.display.iter().enumerate, all_devs.input.iter ...(+9) | AllDevices::default, all_devs.block.iter, all_devs.block.iter().enumerate, all_devs.display.iter, all_devs.display.iter().enumerate, all_devs.net.iter ...(+5) |
| `arceos/modules/axnet/src/lib.rs#init_network` | `arceos/modules/axnet/src/lib.rs#init_network` | 同板块同名 | "eth0".to_owned, Box::new, EthernetAddress, EthernetDevice::new, GATEWAY.parse, GATEWAY.parse().expect ...(+32) | info!, net_devs.take_one, net_devs.take_one().expect, net_impl::init |
| `arceos/modules/axnet/src/device/ethernet.rs#send` | `arceos/tools/bwbench_client/src/device.rs#send` | 同板块同名 | Self::send_to, Some, buf.copy_from_slice, dst_buffer.copy_from_slice, next_hop.is_broadcast, packet.len ...(+7) | Err, Ok, buffer.as_ptr, buffer.len, err.kind, libc::send ...(+2) |
| `arceos/tools/bwbench_client/src/main.rs#transmit_benchmark` | `arceos/tools/bwbench_client/src/main.rs#transmit_benchmark` | 同板块同名同结构 | Local::now, NetDevice::new, NetDevice::new(interface).unwrap, current_time.signed_duration_since, current_time.signed_duration_since(past_time).num_seconds, dev.send ...(+2) | Local::now, NetDevice::new, NetDevice::new(interface).unwrap, current_time.signed_duration_since, current_time.signed_duration_since(past_time).num_seconds, dev.send ...(+2) |

#### 网络协议栈
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `arceos/modules/axnet/src/udp.rs#recv` | `arceos/api/arceos_posix_api/src/imp/net.rs#recv` | 同板块同名 | Err, ExpectedRemote::Any, ExpectedRemote::Expecting, Ok, SocketAddrEx::Ip, ax_bail! ...(+19) | Ok, tcpsocket.lock, tcpsocket.lock().recv, udpsocket.lock, udpsocket.lock().recv_from, udpsocket.lock().recv_from(buf).map |
| `arceos/modules/axnet/src/udp.rs#send` | `arceos/api/arceos_posix_api/src/imp/net.rs#send` | 同板块同名 | Err, IpAddr::V4, IpEndpoint::from, Ok, PacketMeta::default, SERVICE.lock ...(+22) | Ok, tcpsocket.lock, tcpsocket.lock().send, udpsocket.lock, udpsocket.lock().send |
| `arceos/ulib/axlibc/c/network.c#inet_pton` | `arceos/ulib/axlibc/c/network.c#inet_pton` | 同板块同名同结构 | hexval, inet_pton, isdigit, memmove | hexval, inet_pton, isdigit, memmove |
| `arceos/modules/axnet/src/vsock/stream.rs#recv` | `arceos/ulib/axstd/src/net/udp.rs#recv` | 同板块同名 | Err, Ok, conn.lock, conn_guard.advance_rx_read, conn_guard.rx_buffer_used, conn_guard.rx_closed ...(+9) | api::ax_udp_recv |

#### 文件系统/第三方库
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `arceos/modules/axfs/src/fs/ext4/inode.rs#create` | `arceos/modules/axfs/src/fs/lwext4_rust.rs#create` | 同板块同名 | DirEntry::new_dir, DirEntry::new_file, DirNode::new, Err, FileNode::new, Inode::new ...(+14) | Ok, e.try_into, e.try_into().unwrap, file.check_inode_exist, file.dir_mk, file.dir_mk(fpath)
                    .map ...(+12) |
| `arceos/modules/axfs/src/fs/ext4/inode.rs#read_dir` | `arceos/modules/axfs/src/fs/lwext4_rust.rs#read_dir` | 同板块同名 | Ok, core::str::from_utf8, core::str::from_utf8(entry.name())
                .map_err, core::str::from_utf8(entry.name())
                .map_err(|_| VfsError::InvalidData)?
                .to_owned, entry.ino, entry.inode_type ...(+10) | Ok, VfsDirEntry::new, core::str::from_utf8, core::str::from_utf8(iname.unwrap()).unwrap, dirents.iter_mut, dirents.iter_mut().enumerate ...(+13) |
| `arceos/modules/axfs/src/fs/ext4/inode.rs#rename` | `arceos/modules/axfs/src/fs/lwext4_rust.rs#rename` | 同板块同名 | dst_dir.downcast, dst_dir.downcast().map_err, fs.rename, fs.rename(self.ino, src_name, dst_dir.ino, dst_name)
            .map_err, self.fs.lock | e.try_into, e.try_into().unwrap, file.file_rename, file.file_rename(src_path, dst_path)
            .map, file.file_rename(src_path, dst_path)
            .map(|_v| ())
            .map_err, self.0.lock |
| `arceos/modules/axfs/src/fs/ext4/inode.rs#read_at` | `arceos/modules/axfs/src/fs/lwext4_rust.rs#read_at` | 同板块同名 | self.fs
            .lock, self.fs
            .lock()
            .read_at, self.fs
            .lock()
            .read_at(self.ino, buf, offset)
            .map_err | <i32 as TryInto<AxError>>::try_into, <i32 as TryInto<AxError>>::try_into(e).unwrap, e.try_into, e.try_into().unwrap, file.file_close, file.file_open ...(+9) |

#### 网络协议栈/第三方库
仅参考项目中观察到该模块代表函数，目标项目没有同模块可对齐证据：
- 参考项目：`arceos/modules/axnet/src/smoltcp_impl/tcp.rs#connect`，调用摘要：Err, Ok, SOCKET_SET
                .with_socket_mut::<tcp::Socket, _, _>, SOCKET_SET.add, SocketSetWrapper::new_tcp_socket, Some ...(+25)
- 参考项目：`arceos/modules/axnet/src/smoltcp_impl/dns.rs#query`，调用摘要：Err, Ok, SOCKET_SET
            .with_socket_mut::<dns::Socket, _, _>, SOCKET_SET
            .with_socket_mut::<dns::Socket, _, _>(handle, |socket| {
                socket.start_query(iface.lock().context(), name, query_type)
            })
            .map_err, SOCKET_SET.poll_interfaces, SOCKET_SET.with_socket_mut::<dns::Socket, _, _> ...(+12)
- 参考项目：`arceos/modules/axnet/src/smoltcp_impl/bench.rs#bench_transmit_bandwidth`，调用摘要：(current_time - past_time).secs, AxNetTxToken::consume, InterfaceWrapper::current_time, info!, self.transmit, tx_buf[0..12].fill ...(+2)
- 参考项目：`arceos/modules/axnet/src/smoltcp_impl/tcp.rs#shutdown`，调用摘要：LISTEN_TABLE.unlisten, Ok, SOCKET_SET.poll_interfaces, SOCKET_SET.with_socket_mut::<tcp::Socket, _, _>, debug!, self.handle.get ...(+9)

#### 通用模块/其他
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `arceos/ulib/axlibc/c/printf.c#print_exponential_number` | `arceos/ulib/axlibc/c/printf.c#print_exponential_number` | 同板块同名同结构 | ABS_FOR_PRINTING, PRINTF_ABS, bastardized_floor, get_components, get_normalized_components, get_sign_bit ...(+5) | ABS_FOR_PRINTING, PRINTF_ABS, bastardized_floor, get_components, get_normalized_components, get_sign_bit ...(+5) |
| `arceos/ulib/axlibc/c/pow.c#pow` | `arceos/ulib/axlibc/c/pow.c#pow` | 同板块同名同结构 | __builtin_fma, __math_invalid, __math_oflow, __math_uflow, asdouble, asuint64 ...(+8) | __builtin_fma, __math_invalid, __math_oflow, __math_uflow, asdouble, asuint64 ...(+8) |
| `arceos/ulib/axlibc/c/string.c#twoway_strstr` | `arceos/ulib/axlibc/c/string.c#twoway_strstr` | 同板块同名同结构 | BITOP, MAX, memchr, memcmp | BITOP, MAX, memchr, memcmp |
| `arceos/ulib/axlibc/c/printf.c#print_broken_up_decimal` | `arceos/ulib/axlibc/c/printf.c#print_broken_up_decimal` | 同板块同名同结构 | out_rev_ | out_rev_ |


#### 1. 宏观判定
- **定性结论**: 独立开发 (相似度: 20.27%，共同 AST 指纹: 776；唯一 AST 指纹: 目标项目 2662，参考项目 1942；覆盖率: 目标项目 29.15%，参考项目 39.96%)

#### 2. 全模块覆盖矩阵解读
- **进程管理**: 双方均存在该模块。存在共同 AST 指纹。目标覆盖率 9.03%，参考覆盖率 18.57%，表明双方在进程管理上的实现细节差异巨大，重合部分极少。证据强度：弱。
- **任务调度/异步运行时**: 双方均存在该模块。存在共同 AST 指纹。目标覆盖率 19.35%，参考覆盖率 39.34%，覆盖率差距显著，且从证据看，目标项目采用异步运行时（`block_on`, `listener!`），参考项目采用同步调度（`current_run_queue`, `rq.blocked_resched`），架构范式不同。证据强度：弱。
- **内存管理**: 双方均存在该模块。存在共同 AST 指纹。目标覆盖率 19.22%，参考覆盖率 26.81%，覆盖率较低，表明内存管理核心逻辑（如分配器、映射器）的实现路径不同。证据强度：弱。
- **中断/异常处理**: 双方均存在该模块。存在共同 AST 指纹。目标覆盖率 7.14%，参考覆盖率 2.0%，覆盖率极低，且证据多为数学库或通用宏，与核心中断处理逻辑无关。证据强度：未形成。
- **系统调用**: 双方均存在该模块。存在共同 AST 指纹。目标覆盖率 28.95%，参考覆盖率 44.9%，覆盖率中等，但重合点多为 `syscall_body!`、`debug!` 等通用宏或 POSIX 标准命名的函数入口，核心参数解析与分发逻辑不同。证据强度：弱。
- **文件系统**: 双方均存在该模块。存在共同 AST 指纹。目标覆盖率 22.45%，参考覆盖率 30.39%，覆盖率较低。证据显示双方虽都涉及 ext4 等文件系统，但底层实现库（`ext4` vs `lwext4_rust`）和调用模式不同。证据强度：弱。
- **设备驱动**: 双方均存在该模块。存在共同 AST 指纹。目标覆盖率 41.04%，参考覆盖率 58.2%，覆盖率相对较高，但证据集中在 `init_drivers` 等框架初始化流程和 `bwbench` 等通用网络测试工具，属于 ArceOS 框架的标准范式。证据强度：中。
- **网络协议栈**: 双方均存在该模块。存在共同 AST 指纹。目标覆盖率 23.77%，参考覆盖率 94.64%，覆盖率差距悬殊。参考项目大量实现集中在 `smoltcp_impl`，而目标项目则分散在 `udp.rs`、`vsock` 等，实现路径和依赖不同。证据强度：弱。
- **第三方库/公共依赖**: 双方均存在大量第三方库代码（如 musl 数学库 `pow.c`、`printf.c` 等）。存在大量完全相同的 AST 指纹，但这部分代码属于上游公共依赖，不能作为抄袭证据。证据强度：未形成（作为抄袭证据）。

#### 3. 底层 AST 关键证据指纹表
| 所属板块 | 目标项目路径 | 参考项目路径 | 证据类型 | 技术说明 |
| :--- | :--- | :--- | :--- | :--- |
| 设备驱动 | `arceos/modules/axdriver/src/lib.rs#init_drivers` | `arceos/modules/axdriver/src/lib.rs#init_drivers` | 同板块同名 | 均为 ArceOS 框架的设备驱动初始化入口，调用 `AllDevices::default()` 并遍历各类设备进行初始化，属于框架规定的标准流程。 |
| 设备驱动 | `arceos/tools/bwbench_client/src/main.rs#transmit_benchmark` | `arceos/tools/bwbench_client/src/main.rs#transmit_benchmark` | 同板块同名同结构 | 网络带宽测试工具，AST 结构完全一致，属于同一上游工具链的复用，非核心内核逻辑。 |
| 进程管理 | `arceos/tools/raspi4/chainloader/src/panic_wait.rs#panic_prevent_reenter` | `arceos/tools/raspi4/chainloader/src/panic_wait.rs#panic_prevent_reenter` | 同板块同名 | 树莓派链式加载器中的防重入 panic 处理，属于嵌入式裸机通用模板，非原创内核逻辑。 |
| 进程管理 | `arceos/modules/axtask/src/api.rs#exit` | `arceos/modules/axtask/src/api.rs#exit` | 同板块同名 | ArceOS 任务退出 API 入口，调用 `current_run_queue::<NoPreemptIrqSave>().exit_current()`，由框架架构唯一确定。 |
| 进程管理 | `core/src/task.rs#set_clear_child_tid` | `core/src/task.rs#set_clear_child_tid` | 同板块同名 | 单行字段存取器 (`self.clear_child_tid.store`)，属于 trivial 代码，无独创性。 |
| 进程管理 | `arceos/ulib/axlibc/c/pthread.c#pthread_cond_wait` | `arceos/ulib/axlibc/c/pthread.c#pthread_cond_wait` | 同板块同名 | C 库未实现函数的 `unimplemented` 占位符，是标准移植惯例。 |
| 任务调度/异步运行时 | `arceos/modules/axtask/src/wait_queue.rs#wait_timeout_until` | `arceos/modules/axtask/src/wait_queue.rs#wait_timeout_until` | 同板块同名 | 目标项目使用 `block_on`, `listener!` 异步原语；参考项目使用 `current_run_queue`, `set_alarm_wakeup` 同步原语。架构范式根本不同。 |
| 系统调用 | `arceos/api/arceos_posix_api/src/imp/io_mpx/epoll.rs#sys_epoll_wait` | `arceos/api/arceos_posix_api/src/imp/io_mpx/epoll.rs#sys_epoll_wait` | 同板块同名 | 目标项目仅包含 `debug!, syscall_body!` 宏，为未实现的骨架；参考项目有完整的 `EpollInstance::from_fd` 等调用。实现程度完全不同。 |
| 第三方库/公共依赖 | `arceos/ulib/axlibc/c/printf.c#format_string_loop` | `arceos/ulib/axlibc/c/printf.c#format_string_loop` | 同板块同名同结构 | 完全相同的 musl libc 实现，属于公共上游依赖。 |
| 第三方库/公共依赖 | `arceos/ulib/axlibc/c/pow.c#pow` | `arceos/ulib/axlibc/c/pow.c#pow` | 同板块同名同结构 | 完全相同的 musl libc 数学库实现，属于公共上游依赖。 |

#### 4. 核心代码调用流对比剖析
* **【进程管理/任务退出路径】**
* **底层技术析理**: 双方均实现了 `do_exit` 函数，但资源清理与关机路径存在显著差异。目标项目在退出时调用 `SHM_MANAGER.lock().clear_proc_shm` 清理共享内存、`FutexKey::new_current` 处理 futex 健壮列表，并最终通过 `axhal::power::system_off` 关机。参考项目则调用 `FD_TABLE.clear` 清理文件描述符表，并通过 `axhal::misc::terminate` 终止系统。这表明两者在资源管理模型（共享内存 vs 文件描述符）和硬件抽象层（power vs misc）上做出了不同的设计选择。
* **代码溯源链接**:
  - 目标项目: `api/src/task.rs#do_exit`
  - 参考项目: `api/src/imp/task/exit.rs#do_exit`

* **【任务调度/等待队列机制】**
* **底层技术析理**: 双方均实现了 `wait_timeout_until` 函数，但底层同步范式截然不同。目标项目采用异步运行时，调用 `block_on`, `listener!`, `timeout_at` 等原语，将当前任务挂起并注册监听器。参考项目采用同步调度模型，直接操作运行队列，调用 `current_run_queue::<NoPreemptIrqSave>`, `rq.blocked_resched`, `set_alarm_wakeup` 等。这是内核调度器设计上的根本性差异，无法通过简单重构实现转换。
* **代码溯源链接**:
  - 目标项目: `arceos/modules/axtask/src/wait_queue.rs#wait_timeout_until`
  - 参考项目: `arceos/modules/axtask/src/wait_queue.rs#wait_timeout_until`

* **【内存管理/程序加载】**
* **底层技术析理**: 双方均实现了 `load_user_app` 函数，但程序加载与解析逻辑不同。目标项目硬编码了 `”/bin/sh”` 路径，并直接使用 `ELF_LOADER.lock().load` 进行加载。参考项目则实现了动态链接器解析，通过 `CStr::from_bytes_with_nul(interp)` 解析 ELF 文件的解释器路径，并调用 `ElfFile::new` 进行更细粒度的文件解析。这表明两者在用户态程序加载策略上存在独立设计。
* **代码溯源链接**:
  - 目标项目: `core/src/mm.rs#load_user_app`
  - 参考项目: `core/src/mm.rs#load_user_app`

#### 5. 单方模块与降权项
- **单方模块**: “网络协议栈/第三方库”模块仅存在于参考项目中，目标项目无对应模块。这属于参考项目的独立实现，不构成同源证据。
- **降权项**: 大量跨板块同名函数（如 `process_data` 在目标项目 `pidfd.rs` 和参考项目 `core/task.rs` 中）、通用宏（`println!`, `debug!`, `syscall_body!`）、标准 POSIX 系统调用名（`sys_execve`, `sys_waitpid`）以及 C 标准库的 `unimplemented` 占位符，均因其通用性、跨模块性或缺乏独创性，被降级为弱信号或不作为证据。

#### 6. 保守结论
综合评估，目标项目与参考项目的客观 AST 相似度仅为 20.27%，远低于可判定为“同源”或“抄袭”的阈值。双方在进程管理、任务调度、内存管理等核心子系统上展现出根本性的架构差异，特别是异步运行时与同步调度模型的对立。观察到的少量相似性，绝大部分可归因于对 ArceOS 上游框架、POSIX 标准、通用工具链（如 bwbench）及第三方开源库（如 musl libc）的共同继承与遵循。这些重合点属于领域内的标准范式或公共知识，不构成对参考项目原创代码的复用。因此，整体代码同源性风险极低。

------------------------------

