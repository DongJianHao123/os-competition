# 🛡️ OS 内核赛自动化代码级架构审计报告

**审计时间**: `2026-07-02 02:49:58`
**参赛作品名称**: `2026-T2026100079910437-XX`
**年份**: `2026`
**学校**: `XX`
**队伍名称**: `T2026100079910437`
**比赛名字**: `OS-Competition`
**比赛子赛道**: `Kernel-Implementation`
**仓库地址**: `https://gitlab.eduxiji.net/educg-group-43501-3132633/T2026100079910437-1299`
**推测开发语言**: `Rust / C (由AST探针自动识别)`
**特征函数量**: `3179`

## ⚔️ 底层代码同源性对比 (AST Homology)
> 本部分由专门的代码审查智能体，对该项目与符合年份条件的历届最高相似度项目进行控制流与底层调用对比。新版报告会先给出确定性的全模块覆盖矩阵，再给出 Agent 的分模块解释，避免只围绕少量抽样函数展开。

### 🏆 Top 1 底层对标: vs `2025-StarryMix-清华大学`（清华大学 / Starry Mix）

#### 对方作品信息

**对方年份**: `2025`
**对方学校**: `清华大学`
**对方队伍名称**: `Starry Mix`
**对方比赛名字**: `OS-Competition`
**对方比赛子赛道**: `Kernel-Implementation`
**对方仓库地址**: `local-upload://starry-mix`

> 双向覆盖率：共同 AST 指纹: 879；唯一 AST 指纹: 目标项目 2662，参考项目 1503；覆盖率: 目标项目 33.02%，参考项目 58.48%

## 全模块对比总览（确定性证据）

> 总体覆盖率：共同 AST 指纹: 879；唯一 AST 指纹: 目标项目 2662，参考项目 1503；覆盖率: 目标项目 33.02%，参考项目 58.48%

### 1. 模块覆盖矩阵
| 模块 | 目标函数数 | 参考函数数 | 共同唯一 AST 指纹 | Target 覆盖 | Base 覆盖 | 状态 |
| :--- | ---: | ---: | ---: | ---: | ---: | :--- |
| 进程管理 | 162 | 56 | 34 | 23.61% | 65.38% | 双方存在且有结构重合 |
| 任务调度/异步运行时 | 617 | 282 | 124 | 22.42% | 46.79% | 双方存在且有结构重合 |
| 内存管理 | 422 | 273 | 148 | 38.44% | 59.2% | 双方存在且有结构重合 |
| 中断/异常处理 | 14 | 8 | 4 | 28.57% | 50.0% | 双方存在且有结构重合 |
| 系统调用 | 164 | 78 | 10 | 6.58% | 13.33% | 双方存在且有结构重合 |
| 文件系统 | 767 | 562 | 291 | 46.34% | 62.18% | 双方存在且有结构重合 |
| 设备驱动 | 183 | 120 | 92 | 53.18% | 79.31% | 双方存在且有结构重合 |
| 网络协议栈 | 256 | 138 | 63 | 28.25% | 49.22% | 双方存在且有结构重合 |
| 文件系统/第三方库 | 37 | 37 | 33 | 89.19% | 89.19% | 双方存在且有结构重合 |
| 通用模块/其他 | 557 | 158 | 109 | 24.83% | 81.34% | 双方存在且有结构重合 |

### 2. 分模块证据清单

#### 进程管理
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `api/src/vfs/proc.rs#lookup_child` | `api/src/vfs/proc.rs#lookup_child` | 同板块同名同结构 | Arc::downgrade, Arc::new, CStr::from_bytes_until_nul, CStr::from_bytes_until_nul(&input)
                                    .map_err, CStr::from_bytes_until_nul(&input)
                                    .map_err(|_| VfsError::InvalidInput)?
                                    .to_str, CStr::from_bytes_until_nul(&input)
                                    .map_err(|_| VfsError::InvalidInput)?
                                    .to_str()
                                    .map_err ...(+55) | Arc::downgrade, Arc::new, CStr::from_bytes_until_nul, CStr::from_bytes_until_nul(&input)
                                    .map_err, CStr::from_bytes_until_nul(&input)
                                    .map_err(|_| VfsError::EINVAL)?
                                    .to_str, CStr::from_bytes_until_nul(&input)
                                    .map_err(|_| VfsError::EINVAL)?
                                    .to_str()
                                    .map_err ...(+55) |
| `arceos/modules/axnet/src/device/ethernet.rs#process_arp` | `arceos/modules/axnet/src/device/ethernet.rs#process_arp` | 同板块同名同结构 | ArpPacket::new_checked, ArpPacket::new_checked(payload).and_then, ArpPacket::new_unchecked, ArpRepr::parse, IpAddress::Ipv4, Self::send_to ...(+21) | ArpPacket::new_checked, ArpPacket::new_checked(payload).and_then, ArpPacket::new_unchecked, ArpRepr::parse, IpAddress::Ipv4, Self::send_to ...(+21) |
| `api/src/syscall/task/execve.rs#sys_execve` | `api/src/syscall/task/execve.rs#sys_execve` | 同板块同名 | Arc::new, Box::into_raw, Box::new, Default::default, Err, FD_TABLE.write ...(+49) | Arc::new, Default::default, Err, FD_TABLE.write, FS_CONTEXT.lock, FS_CONTEXT.lock().resolve ...(+37) |
| `api/src/syscall/task/wait.rs#sys_waitpid` | `api/src/syscall/task/wait.rs#sys_waitpid` | 同板块同名 | AxError::from, Err, Ok, Poll::Ready, Some, WaitOptions::from_bits_truncate ...(+28) | Err, Ok, Poll::Ready, WaitOptions::from_bits_truncate, WaitPid::Pgid, WaitPid::Pid ...(+28) |

#### 任务调度/异步运行时
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `api/src/vfs/dev/event.rs#ioctl` | `api/src/vfs/dev/event.rs#ioctl` | 同板块同名 | Err, EventType::Led.bits_count, EventType::Sound.bits_count, EventType::Switch.bits_count, Ok, UserPtr::<InputDeviceId>::from ...(+16) | Err, EventType::Led.bits_count, EventType::Sound.bits_count, EventType::Switch.bits_count, Ok, UserPtr::<InputDeviceId>::from ...(+16) |
| `api/src/vfs/dev/mod.rs#builder` | `api/src/vfs/dev/mod.rs#builder` | 同板块同名同结构 | Arc::new, Device::new, DeviceId::new, DirMapping::new, Ok, Random::new ...(+11) | Arc::new, Device::new, DeviceId::new, DirMapping::new, Ok, Random::new ...(+11) |
| `arceos/modules/axlog/src/lib.rs#log` | `arceos/modules/axlog/src/lib.rs#log` | 同板块同名同结构 | cfg_if::cfg_if!, record.level, record.line, record.line().unwrap_or, record.metadata, record.target ...(+1) | cfg_if::cfg_if!, record.level, record.line, record.line().unwrap_or, record.metadata, record.target ...(+1) |
| `api/src/syscall/sync/futex.rs#sys_futex` | `api/src/syscall/sync/futex.rs#sys_futex` | 同板块同名 | AxError::from, Err, FutexKey::new_current, Ok, Some, assert_unsigned ...(+20) | Err, FutexKey::new_current, Ok, Some, assert_unsigned, axtask::yield_now ...(+19) |

#### 内存管理
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `api/src/syscall/mod.rs#handle_syscall` | `api/src/syscall/mod.rs#handle_syscall` | 同板块同名 | Err, LinuxError::ENOSYS.code, LinuxError::from, LinuxError::from(err).code, Ok, Sysno::new ...(+211) | Err, LinuxError::ENOSYS.code, Ok, Sysno::new, debug!, err.code ...(+201) |
| `api/src/syscall/task/clone.rs#sys_clone` | `api/src/syscall/task/clone.rs#sys_clone` | 同板块同名 | (parent_tid as *mut Pid).vm_write, (parent_tid as *mut Pid).vm_write(tid).ok, (parent_tid as *mut i32).vm_write, Arc::new, AxTaskExt::from_impl, Box::into_raw ...(+99) | Arc::new, CloneFlags::from_bits_truncate, Err, FD_TABLE
                    .scope_mut, FD_TABLE
                    .scope_mut(&mut scope)
                    .write, FD_TABLE
                    .scope_mut(&mut scope)
                    .write()
                    .clone_from ...(+66) |
| `api/src/syscall/mm/mmap.rs#sys_mmap` | `api/src/syscall/mm/mmap.rs#sys_mmap` | 同板块同名 | (addr + length).align_up, Arc::new, Backend::new_alloc, Backend::new_cow, Backend::new_file, Backend::new_linear ...(+50) | (addr + length).align_up, Arc::new, Backend::new_alloc, Backend::new_cow, Backend::new_file, Backend::new_linear ...(+50) |
| `core/src/mm.rs#load_user_app` | `core/src/mm.rs#load_user_app` | 同板块同名 | "/bin/sh".to_owned, (ustack_top - user_sp_aligned).align_up_4k, AuxEntry::new, Backend::new_alloc, ELF_LOADER.lock, ELF_LOADER.lock().load ...(+67) | "/bin/sh".to_owned, (ustack_top - user_sp_aligned).align_up_4k, Backend::new_alloc, ELF_LOADER.lock, ELF_LOADER.lock().load, Err ...(+43) |

#### 中断/异常处理
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `api/src/terminal/termios.rs#signo_for` | `api/src/terminal/termios.rs#signo_for` | 同板块同名同结构 | Some, self.special_char | Some, self.special_char |
| `arceos/modules/axsync/src/mutex.rs#may_interrupt` | `arceos/modules/axsync/src/mutex.rs#may_interrupt` | 同板块同名 | fastrand::u8, thread::yield_now | rand::random::<u32>, thread::yield_now |
| `api/src/time.rs#inc_irq_cnt` | `api/src/time.rs#inc_irq_cnt` | 同板块同名同结构 | IRQ_CNT.fetch_add | IRQ_CNT.fetch_add |
| `api/src/time.rs#irq_cnt` | `api/src/time.rs#irq_cnt` | 同板块同名同结构 | IRQ_CNT.load | IRQ_CNT.load |

#### 系统调用
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `api/src/syscall/net/opt.rs#sys_getsockopt` | `api/src/syscall/net/opt.rs#sys_getsockopt` | 同板块同名 | Err, Ok, Socket::from_fd, call_dispatch!, debug!, optlen.get_as_mut ...(+3) | Err, Ok, Socket::from_fd, call_dispatch!, debug!, optlen.get_as_mut ...(+3) |
| `api/src/syscall/time.rs#sys_setitimer` | `api/src/syscall/time.rs#sys_setitimer` | 同板块同名 | ITimerType::from_repr, ITimerType::from_repr(which).ok_or, Ok, curr
        .as_thread, curr
        .as_thread()
        .time
        .borrow_mut, curr
        .as_thread()
        .time
        .borrow_mut()
        .set_itimer ...(+12) | ITimerType::from_repr, ITimerType::from_repr(which).ok_or, Ok, curr
        .as_thread, curr
        .as_thread()
        .time
        .borrow_mut, curr
        .as_thread()
        .time
        .borrow_mut()
        .set_itimer ...(+12) |
| `api/src/syscall/io_mpx/epoll.rs#sys_epoll_ctl` | `api/src/syscall/io_mpx/epoll.rs#sys_epoll_ctl` | 同板块同名 | Epoll::from_fd, EpollFlags::from_bits, EpollFlags::from_bits(event.events & !events.bits()).ok_or, Err, IoEvents::from_bits_truncate, Ok ...(+7) | Epoll::from_fd, EpollFlags::from_bits, EpollFlags::from_bits(event.events & !(events.bits() as u32))
            .ok_or, Err, IoEvents::from_bits_truncate, Ok ...(+7) |
| `api/src/syscall/net/opt.rs#sys_setsockopt` | `api/src/syscall/net/opt.rs#sys_setsockopt` | 同板块同名 | Err, Ok, Socket::from_fd, call_dispatch!, debug!, size_of::<T> ...(+2) | Err, Ok, Socket::from_fd, call_dispatch!, debug!, size_of::<T> ...(+2) |

#### 文件系统
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `api/src/vfs/dev/fb.rs#ioctl` | `api/src/vfs/dev/fb.rs#ioctl` | 同板块同名 | (arg as *mut FixScreenInfo).vm_write, (arg as *mut VarScreenInfo).vm_write, Err, Ok, axdisplay::framebuffer_info | (arg as *mut FixScreenInfo).vm_write, (arg as *mut VarScreenInfo).vm_write, Err, Ok, axdisplay::main_display, axdisplay::main_display().info |
| `api/src/vfs/dev/loop.rs#ioctl` | `api/src/vfs/dev/loop.rs#ioctl` | 同板块同名 | (arg as *const loop_info).vm_read_uninit, (arg as *const loop_info).vm_read_uninit()?.assume_init, (arg as *const u32).vm_read, (arg as *mut loop_info).vm_write, (arg as *mut u32).vm_write, (arg as *mut u64).vm_write ...(+22) | (arg as *const loop_info).vm_read_uninit, (arg as *const loop_info).vm_read_uninit()?.assume_init, (arg as *const u32).vm_read, (arg as *mut loop_info).vm_write, (arg as *mut u32).vm_write, (arg as *mut u64).vm_write ...(+22) |
| `api/src/file/epoll.rs#poll_events` | `api/src/file/epoll.rs#poll_events` | 同板块同名 | Arc::downgrade, Err, Ok, event.events.bits, file.as_ref, interest.consume ...(+13) | Arc::downgrade, Err, Ok, event.events.bits, file.as_ref, interest.enabled.store ...(+14) |
| `api/src/syscall/io_mpx/poll.rs#do_poll` | `api/src/syscall/io_mpx/poll.rs#do_poll` | 同板块同名 | Err, FdPollSet, IoEvents::empty, IoEvents::from_bits, IoEvents::from_bits(fd.events as _).ok_or, Ok ...(+17) | Err, FdPollSet, IoEvents::empty, IoEvents::from_bits, IoEvents::from_bits(fd.events as _).ok_or, Ok ...(+17) |

#### 设备驱动
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `arceos/modules/axnet/src/router.rs#dispatch` | `arceos/modules/axnet/src/router.rs#dispatch` | 同板块同名同结构 | IpAddress::Ipv4, IpAddress::Ipv6, IpVersion::of_packet, IpVersion::of_packet(packet).expect, assert_eq!, dev.send ...(+12) | IpAddress::Ipv4, IpAddress::Ipv6, IpVersion::of_packet, IpVersion::of_packet(packet).expect, assert_eq!, dev.send ...(+12) |
| `arceos/modules/axdriver/src/lib.rs#init_drivers` | `arceos/modules/axdriver/src/lib.rs#init_drivers` | 同板块同名 | AllDevices::default, all_devs.block.iter, all_devs.block.iter().enumerate, all_devs.display.iter, all_devs.display.iter().enumerate, all_devs.input.iter ...(+9) | AllDevices::default, all_devs.block.iter, all_devs.block.iter().enumerate, all_devs.display.iter, all_devs.display.iter().enumerate, all_devs.input.iter ...(+7) |
| `arceos/modules/axnet/src/lib.rs#init_network` | `arceos/modules/axnet/src/lib.rs#init_network` | 同板块同名 | "eth0".to_owned, Box::new, EthernetAddress, EthernetDevice::new, GATEWAY.parse, GATEWAY.parse().expect ...(+32) | "eth0".to_owned, Box::new, EthernetAddress, EthernetDevice::new, GATEWAY.parse, GATEWAY.parse().expect ...(+32) |
| `arceos/modules/axnet/src/device/ethernet.rs#send` | `arceos/modules/axnet/src/device/ethernet.rs#send` | 同板块同名同结构 | Self::send_to, Some, buf.copy_from_slice, dst_buffer.copy_from_slice, next_hop.is_broadcast, packet.len ...(+7) | Self::send_to, Some, buf.copy_from_slice, dst_buffer.copy_from_slice, next_hop.is_broadcast, packet.len ...(+7) |

#### 网络协议栈
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `arceos/modules/axnet/src/udp.rs#recv` | `arceos/modules/axnet/src/udp.rs#recv` | 同板块同名 | Err, ExpectedRemote::Any, ExpectedRemote::Expecting, Ok, SocketAddrEx::Ip, ax_bail! ...(+19) | Err, ExpectedRemote::Any, ExpectedRemote::Expecting, Ok, SocketAddrEx::Ip, ax_err! ...(+20) |
| `arceos/modules/axnet/src/udp.rs#send` | `arceos/modules/axnet/src/udp.rs#send` | 同板块同名 | Err, IpAddr::V4, IpEndpoint::from, Ok, PacketMeta::default, SERVICE.lock ...(+22) | Err, IpAddr::V4, IpEndpoint::from, Ok, PacketMeta::default, SERVICE.lock ...(+23) |
| `arceos/modules/axnet/src/vsock/stream.rs#recv` | `arceos/modules/axnet/src/tcp.rs#recv` | 同板块同名 | Err, Ok, conn.lock, conn_guard.advance_rx_read, conn_guard.rx_buffer_used, conn_guard.rx_closed ...(+9) | Err, Ok, ax_err!, dst.remaining_mut, dst.write, options.flags.contains ...(+13) |
| `arceos/modules/axnet/src/tcp.rs#recv` | `arceos/modules/axnet/src/unix/dgram.rs#recv` | 同板块同名 | Err, Ok, ax_err_type!, dst.remaining_mut, dst.write, options.flags.contains ...(+12) | Err, Ok, SocketAddrEx::Unix, data.len, dst.extend, dst.write ...(+9) |

#### 文件系统/第三方库
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `arceos/modules/axfs/src/fs/ext4/inode.rs#create` | `arceos/modules/axfs-ng/src/fs/ext4/inode.rs#create` | 同板块同名同结构 | DirEntry::new_dir, DirEntry::new_file, DirNode::new, Err, FileNode::new, Inode::new ...(+14) | DirEntry::new_dir, DirEntry::new_file, DirNode::new, Err, FileNode::new, Inode::new ...(+14) |
| `arceos/modules/axfs/src/fs/ext4/inode.rs#metadata` | `arceos/modules/axfs-ng/src/fs/ext4/inode.rs#metadata` | 同板块同名同结构 | DeviceId::default, FileAttr::default, NodePermission::from_bits_truncate, Ok, into_vfs_type, self.fs
            .lock ...(+2) | DeviceId::default, FileAttr::default, NodePermission::from_bits_truncate, Ok, into_vfs_type, self.fs
            .lock ...(+2) |
| `arceos/modules/axfs/src/fs/ext4/inode.rs#read_dir` | `arceos/modules/axfs-ng/src/fs/ext4/inode.rs#read_dir` | 同板块同名同结构 | Ok, core::str::from_utf8, core::str::from_utf8(entry.name())
                .map_err, core::str::from_utf8(entry.name())
                .map_err(|_| VfsError::InvalidData)?
                .to_owned, entry.ino, entry.inode_type ...(+10) | Ok, core::str::from_utf8, core::str::from_utf8(entry.name())
                .map_err, core::str::from_utf8(entry.name())
                .map_err(|_| VfsError::EINVAL)?
                .to_owned, entry.ino, entry.inode_type ...(+10) |
| `arceos/modules/axfs/src/fs/ext4/inode.rs#update_metadata` | `arceos/modules/axfs-ng/src/fs/ext4/inode.rs#update_metadata` | 同板块同名同结构 | Ok, fs.with_inode_ref, fs.with_inode_ref(self.ino, |inode| {
            if let Some(mode) = update.mode {
                inode.set_mode((inode.mode() & !0xfff) | (mode.bits() as u32));
            }
            if let Some((uid, gid)) = update.owner {
                inode.set_owner(uid as _, gid as _);
            }
            if let Some(atime) = update.atime {
                inode.set_atime(&atime);
            }
            if let Some(mtime) = update.mtime {
                inode.set_mtime(&mtime);
            }
            inode.update_ctime();
            Ok(())
        })
        .map_err, inode.mode, inode.set_atime, inode.set_mode ...(+5) | Ok, fs.with_inode_ref, fs.with_inode_ref(self.ino, |inode| {
            if let Some(mode) = update.mode {
                inode.set_mode((inode.mode() & !0xfff) | (mode.bits() as u32));
            }
            if let Some((uid, gid)) = update.owner {
                inode.set_owner(uid as _, gid as _);
            }
            if let Some(atime) = update.atime {
                inode.set_atime(&atime);
            }
            if let Some(mtime) = update.mtime {
                inode.set_mtime(&mtime);
            }
            inode.update_ctime();
            Ok(())
        })
        .map_err, inode.mode, inode.set_atime, inode.set_mode ...(+5) |

#### 通用模块/其他
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `arceos/tools/deptool/src/cmd_parser.rs#parse_cmd` | `arceos/tools/deptool/src/cmd_parser.rs#parse_cmd` | 同板块同名同结构 | APP_ROOT.to_string, Arg::new, Arg::new("features").short, Arg::new("features").short('f').long, Arg::new("features").short('f').long("name").action, Arg::new("format").short ...(+42) | APP_ROOT.to_string, Arg::new, Arg::new("features").short, Arg::new("features").short('f').long, Arg::new("features").short('f').long("name").action, Arg::new("format").short ...(+42) |
| `arceos/modules/axsync/src/mutex.rs#lock` | `arceos/modules/axsync/src/mutex.rs#lock` | 同板块同名 | Spin, assert_ne!, block_on, current, current().id, current().id().as_u64 ...(+4) | assert_ne!, block_on, current, current().id, current().id().as_u64, self.event.listen ...(+2) |
| `arceos/tools/deptool/src/mermaid_generator.rs#gen_mermaid_script` | `arceos/tools/deptool/src/mermaid_generator.rs#gen_mermaid_script` | 同板块同名同结构 | HashMap::new, Vec::new, deps_parsed.get, deps_parsed.get(idx).unwrap, deps_parsed.get(skip_idx).unwrap, deps_parsed.len ...(+6) | HashMap::new, Vec::new, deps_parsed.get, deps_parsed.get(idx).unwrap, deps_parsed.get(skip_idx).unwrap, deps_parsed.len ...(+6) |
| `arceos/tools/deptool/src/d2_generator.rs#gen_d2_script` | `arceos/tools/deptool/src/d2_generator.rs#gen_d2_script` | 同板块同名同结构 | HashMap::new, Vec::new, deps_parsed.get, deps_parsed.get(idx).unwrap, deps_parsed.get(skip_idx).unwrap, deps_parsed.len ...(+6) | HashMap::new, Vec::new, deps_parsed.get, deps_parsed.get(idx).unwrap, deps_parsed.get(skip_idx).unwrap, deps_parsed.len ...(+6) |


好的，我将遵循审计指令，以操作系统内核代码同源性审计专家的身份，对本案进行最终定性仲裁。

### 1. 宏观判定
- **定性结论**: **独立开发，存在共同上游代码继承**
  基于双向覆盖率指标（目标项目覆盖率 33.02%，参考项目覆盖率 58.48%）及双方模块画像的显著规模差异，本案在宏观层面不构成抄袭或违规复用。双方代码的相似性主要源于对 **ArceOS 上游框架**的共同继承、对 **POSIX/Linux 标准语义**的共同遵循，以及 **Rust 语言惯用法**的自然收敛。目标项目 66.98% 的 AST 指纹为独立存在，且在错误处理模型、异步运行时策略等方面展现出独立的架构设计。证据抽取环节的失败（401 Unauthorized）进一步削弱了控方证据链的完整性。

### 2. 全模块覆盖矩阵解读
| 模块 | 双方存在 | 共同指纹 | 目标覆盖率 | 参考覆盖率 | 证据强度 | 解读 |
| :--- | :---: | :---: | :---: | :---: | :---: | :--- |
| **进程管理** | 是 | 34 | 23.61% | 65.38% | **弱** | 目标函数数远超参考（162 vs 56），低覆盖率表明大部分为独立开发。重合点集中在 POSIX 标准系统调用（如 `sys_getppid`）和 ArceOS 上游模块（如 `axtask`），属于标准语义或共同继承。 |
| **任务调度/异步运行时** | 是 | 124 | 22.42% | 46.79% | **弱** | 目标函数体量巨大（617 vs 282），低覆盖率表明独立开发为主。重合点多为日志宏、设备构建器等框架通用逻辑，不构成核心调度算法的抄袭证据。 |
| **内存管理** | 是 | 148 | 38.44% | 59.2% | **中** | 覆盖率相对较高，但证据集中在 `sys_mmap`、`sys_clone` 等系统调用入口。这些函数的调用链由 Linux 标准（`MmapProt`、`CloneFlags`）和框架内存模型（`Backend`）决定，相似性可解释。 |
| **中断/异常处理** | 是 | 4 | 28.57% | 50.0% | **弱** | 模块规模极小，重合点如 `irq_cnt` 是简单的原子计数器，`signo_for` 是终端标准映射，无独创性。 |
| **系统调用** | 是 | 10 | 6.58% | 13.33% | **弱** | 覆盖率极低，表明双方系统调用实现差异巨大。重合点仅为 `sys_getsockopt` 等少数派发函数，其结构由 socket API 标准决定。 |
| **文件系统** | 是 | 291 | 46.34% | 62.18% | **中** | 覆盖率较高，但大量重合指纹指向 `arceos/modules/axfs` 下的 ext4 实现和 VFS 框架，属于明确的共同上游代码。 |
| **设备驱动** | 是 | 92 | 53.18% | 79.31% | **中** | 高覆盖率源于双方均使用 ArceOS 的 `axdriver` 和 `axnet` 驱动框架。`init_drivers`、`dispatch` 等函数是框架的标准初始化与派发逻辑。 |
| **网络协议栈** | 是 | 63 | 28.25% | 49.22% | **弱** | 重合点位于 `arceos/modules/axnet` 的 UDP/TCP 收发逻辑，属于共同上游。目标项目独有的 `vsock` 实现体现了独立开发。 |
| **文件系统/第三方库** | 是 | 33 | 89.19% | 89.19% | **强（第三方）** | 极高覆盖率确认为双方共同引用的第三方库或上游模块（如 ext4 文件系统实现），非任何一方原创，不构成抄袭。 |
| **通用模块/其他** | 是 | 109 | 24.83% | 81.34% | **弱** | 目标函数数远超参考（557 vs 158），低覆盖率表明独立开发为主。重合点集中在 `deptool` 等上游工具和 `axsync::mutex` 等基础同步原语。 |

### 3. 底层 AST 关键证据指纹表
| 所属板块 | 目标项目路径 | 参考项目路径 | 证据类型 | 技术说明 |
| :--- | :--- | :--- | :--- | :--- |
| **第三方库/公共依赖** | `arceos/modules/axfs/src/fs/ext4/inode.rs#create` | `arceos/modules/axfs-ng/src/fs/ext4/inode.rs#create` | 同板块同名同结构 | 完全一致的 ext4 inode 创建逻辑，属于共同依赖的上游文件系统库，非任何一方原创。 |
| **第三方库/公共依赖** | `arceos/modules/axfs/src/fs/ext4/inode.rs#metadata` | `arceos/modules/axfs-ng/src/fs/ext4/inode.rs#metadata` | 同板块同名同结构 | 完全一致的 ext4 inode 元数据获取逻辑，属于共同依赖的上游文件系统库。 |
| **第三方库/公共依赖** | `arceos/modules/axfs/src/fs/ext4/inode.rs#read_dir` | `arceos/modules/axfs-ng/src/fs/ext4/inode.rs#read_dir` | 同板块同名同结构 | 完全一致的 ext4 目录读取逻辑，属于共同依赖的上游文件系统库。 |
| **第三方库/公共依赖** | `arceos/modules/axfs/src/fs/ext4/inode.rs#update_metadata` | `arceos/modules/axfs-ng/src/fs/ext4/inode.rs#update_metadata` | 同板块同名同结构 | 完全一致的 ext4 inode 元数据更新逻辑，属于共同依赖的上游文件系统库。 |
| **进程管理** | `arceos/modules/axtask/src/run_queue.rs#exit_current` | `arceos/modules/axtask/src/run_queue.rs#exit_current` | 同板块同名同结构 | 完全一致的调度器退出逻辑，属于共同继承的 ArceOS 上游 `axtask` 模块。 |
| **进程管理** | `api/src/vfs/proc.rs#lookup_child` | `api/src/vfs/proc.rs#lookup_child` | 同板块同名 | Procfs 查找逻辑高度相似，但错误类型不同（`VfsError::InvalidInput` vs `VfsError::EINVAL`），体现了独立的错误模型设计。 |
| **进程管理** | `api/src/syscall/task/thread.rs#sys_getppid` | `api/src/syscall/task/thread.rs#sys_getppid` | 同板块同名 | `getppid` 系统调用实现高度相似，但其调用链由 POSIX 标准语义唯一确定，且错误类型不同（`AxError::NoSuchProcess` vs `LinuxError::ESRCH`）。 |
| **内存管理** | `api/src/syscall/mm/mmap.rs#sys_mmap` | `api/src/syscall/mm/mmap.rs#sys_mmap` | 同板块同名 | `mmap` 系统调用实现高度相似，调用链由 Linux 标准标志（`MmapProt`）和框架内存后端（`Backend`）决定。 |
| **内存管理** | `core/src/mm.rs#load_user_app` | `core/src/mm.rs#load_user_app` | 同板块同名 | 用户程序加载逻辑相似，但目标项目包含更多 `AuxEntry` 处理，体现了独立的功能扩展。 |
| **设备驱动** | `arceos/modules/axnet/src/router.rs#dispatch` | `arceos/modules/axnet/src/router.rs#dispatch` | 同板块同名同结构 | 完全一致的网络包派发逻辑，属于共同继承的 ArceOS 上游 `axnet` 模块。 |
| **设备驱动** | `arceos/modules/axdriver/src/lib.rs#init_drivers` | `arceos/modules/axdriver/src/lib.rs#init_drivers` | 同板块同名 | 高度相似的驱动初始化逻辑，属于共同继承的 ArceOS 上游 `axdriver` 框架。 |
| **网络协议栈** | `arceos/modules/axnet/src/udp.rs#recv` | `arceos/modules/axnet/src/udp.rs#recv` | 同板块同名 | UDP 接收逻辑高度相似，属于共同继承的 ArceOS 上游 `axnet` 模块。目标项目使用 `ax_bail!`，参考项目使用 `ax_err!`，为框架内不同宏。 |
| **通用模块/其他** | `arceos/tools/deptool/src/cmd_parser.rs#parse_cmd` | `arceos/tools/deptool/src/cmd_parser.rs#parse_cmd` | 同板块同名同结构 | 完全一致的命令行解析逻辑，属于共同继承的 ArceOS 上游工具。 |

### 4. 核心代码调用流对比剖析
* **【进程管理/系统调用路径】`sys_getppid` 实现**
* **底层技术析理**: 双方均实现了获取父进程ID的 `getppid` 系统调用。调用流均为 `current() -> as_thread() -> proc_data -> proc -> parent() -> ok_or(err) -> map(p.pid)`。此调用链是访问当前任务控制块以获取父进程信息的唯一自然路径，由内核数据结构设计决定。关键差异在于错误处理：目标项目使用 `AxError::NoSuchProcess`，而参考项目使用 `LinuxError::ESRCH`。这并非简单的宏替换，而是目标项目采用了一套独立的、语义化的错误类型系统，反映了架构层面的独立设计。
* **代码溯源链接**:
  - 目标项目: `api/src/syscall/task/thread.rs#sys_getppid`
  - 参考项目: `api/src/syscall/task/thread.rs#sys_getppid`

* **【内存管理/核心加载路径】`load_user_app` 实现**
* **底层技术析理**: 双方均实现了从ELF文件加载用户程序的核心逻辑。调用流均包含 `ELF_LOADER.lock().load()`、`Backend::new_alloc`、`ustack_top` 对齐等步骤，这是使用框架提供的ELF加载器和内存后端的标准流程。目标项目的调用链（+67）比参考项目（+43）更长，包含了 `AuxEntry::new` 等辅助向量（Auxiliary Vector）的构建逻辑，表明其在用户态启动参数传递方面有独立的功能实现和更丰富的设计。
* **代码溯源链接**:
  - 目标项目: `core/src/mm.rs#load_user_app`
  - 参考项目: `core/src/mm.rs#load_user_app`

* **【任务调度/异步运行时】`wait_if` Futex 等待逻辑**
* **底层技术析理**: 双方均实现了 Futex 的等待逻辑，核心流程均为 `condition.take() -> cx.waker().clone() -> Poll::Ready`，这是 Rust 异步编程中实现 `Future` 轮询的标准模式。关键差异在于阻塞原语：目标项目使用 `block_on`，而参考项目使用 `block_on_interruptible`。这代表了两者在可中断性设计上的根本分歧，目标项目选择了不可中断的等待，而参考项目允许信号中断，这是调度策略层面的独立设计选择。
* **代码溯源链接**:
  - 目标项目: `core/src/futex.rs#wait_if`
  - 参考项目: `core/src/futex.rs#wait_if`

### 5. 单方模块与降权项
*   **单方模块/功能**:
    *   **目标项目独有**: 目标项目在【进程管理】板块拥有远超参考项目的函数数量（162 vs 56），表明其实现了大量参考项目不具备的进程管理功能。此外，目标项目在网络协议栈中包含 `vsock`（虚拟套接字）实现，而参考项目未见此模块，此为独立开发的有力证据。
    *   **跨板块同名弱信号**: 控方指控的 `send_signal_to_process` 函数，在目标项目中位于 `core/src/executor/src/signal.rs`，调用链涉及 `PID2PC`；在参考项目中位于 `core/src/task.rs`，调用链涉及 `get_process_data`。两者文件路径、数据结构和调用链均不同，属于跨板块异构实现，已降级为弱信号，不构成同源证据。
*   **第三方库/公共依赖**:
    *   `arceos/modules/axfs/*`、`arceos/modules/axnet/*`、`arceos/modules/axtask/*`、`arceos/tools/*` 等路径下的代码，明确属于 ArceOS 上游框架或公共第三方库。双方在这些模块中表现出的极高相似性，应被认定为对共同基线的继承，而非相互抄袭。这些证据在证据指纹表中已明确标注为“第三方库/公共依赖”。

### 6. 保守结论
综合全模块证据，目标项目 `[2026-T2026100079910437-XX]` 与参考项目 `[2025-StarryMix-清华大学]` 的 AST 相似度（26.75%）较低，且目标项目覆盖率仅为 33.02%，表明其大部分代码为独立开发。双方存在的相似性高度集中在 ArceOS 上游框架、POSIX/Linux 标准系统调用接口以及 Rust 语言惯用法上。辩方关于“共同上游代码基线”和“领域标准约束”的抗辩理由充分。目标项目在错误处理模型（`AxError` vs `LinuxError`）、异步原语（`block_on` vs `block_on_interruptible`）等方面的差异，是其独立架构设计的体现。加之证据抽取环节失败，控方未能提供完整、可靠的证据链。因此，整体复用风险极低，应判定为独立开发，驳回控方关于【进程管理】板块的抄袭指控。

------------------------------

