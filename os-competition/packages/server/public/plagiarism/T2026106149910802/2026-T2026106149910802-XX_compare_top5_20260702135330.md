# 🛡️ OS 内核赛自动化代码级架构审计报告

**审计时间**: `2026-07-02 13:53:30`
**参赛作品名称**: `2026-T2026106149910802-XX`
**年份**: `2026`
**学校**: `XX`
**队伍名称**: `T2026106149910802`
**比赛名字**: `OS-Competition`
**比赛子赛道**: `Kernel-Implementation`
**仓库地址**: `https://gitlab.eduxiji.net/educg-group-43501-3132633/T2026106149910802-2743`
**推测开发语言**: `Rust / C (由AST探针自动识别)`
**特征函数量**: `2530`

## ⚔️ 底层代码同源性对比 (AST Homology)
> 本部分由专门的代码审查智能体，对该项目与符合年份条件的历届最高相似度项目进行控制流与底层调用对比。新版报告会先给出确定性的全模块覆盖矩阵，再给出 Agent 的分模块解释，避免只围绕少量抽样函数展开。

### 🏆 Top 5 底层对标: vs `2024-cabbageOS-华中科技大学`（华中科技大学 / cabbageOS）

#### 对方作品信息

**对方年份**: `2024`
**对方学校**: `华中科技大学`
**对方队伍名称**: `cabbageOS`
**对方比赛名字**: `OS-Competition`
**对方比赛子赛道**: `Kernel-Implementation`
**对方仓库地址**: `https://gitlab.eduxiji.net/educg-group-26010-2376550/T202410487992456-422`

> 双向覆盖率：共同 AST 指纹: 320；唯一 AST 指纹: 目标项目 1857，参考项目 1579；覆盖率: 目标项目 17.23%，参考项目 20.27%

## 全模块对比总览（确定性证据）

> 总体覆盖率：共同 AST 指纹: 320；唯一 AST 指纹: 目标项目 1857，参考项目 1579；覆盖率: 目标项目 17.23%，参考项目 20.27%

### 1. 模块覆盖矩阵
| 模块 | 目标函数数 | 参考函数数 | 共同唯一 AST 指纹 | Target 覆盖 | Base 覆盖 | 状态 |
| :--- | ---: | ---: | ---: | ---: | ---: | :--- |
| 进程管理 | 146 | 95 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |
| 任务调度/异步运行时 | 200 | 56 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |
| 内存管理 | 427 | 290 | 41 | 11.14% | 14.39% | 双方存在且有结构重合 |
| 中断/异常处理 | 44 | 28 | 2 | 5.26% | 8.7% | 双方存在且有结构重合 |
| 系统调用 | 63 | 76 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |
| 文件系统 | 634 | 176 | 22 | 6.34% | 14.97% | 双方存在且有结构重合 |
| 设备驱动 | 428 | 446 | 253 | 68.75% | 66.75% | 双方存在且有结构重合 |
| 网络协议栈 | 57 | 0 | 0 | 0.0% | 0.0% | 仅目标项目体现 |
| 文件系统/第三方库 | 376 | 522 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |
| 第三方库 | 1 | 1 | 1 | 100.0% | 100.0% | 双方存在且有结构重合 |
| 通用模块/其他 | 154 | 228 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |

### 2. 分模块证据清单

#### 进程管理
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `os/src/syscall/process.rs#sys_tgkill` | `kernel/src/syscall/sysproc.c#sys_tgkill` | 同板块同名 | Err, Ok, SignalFlags::from_bits, current_task, current_task().unwrap, current_task().unwrap().gettid ...(+5) | arg_int, arg_ulong, do_tkill, get_thread_by_tidx |
| `os/src/syscall/process.rs#sys_clone` | `kernel/src/syscall/sysproc.c#sys_clone` | 同板块同名 | CloneFlags::from_bits, Err, Ok, add_task, current_task, current_task().unwrap ...(+8) | arg_addr, arg_int, do_clone |
| `os/src/syscall/process.rs#sys_tkill` | `kernel/src/syscall/sysproc.c#sys_tkill` | 同板块同名 | Err, Ok, SignalFlags::from_bits, drop, inner.signal_queue.push, task.getpid ...(+2) | arg_int, arg_ulong, do_tkill, get_thread_by_tid |
| `os/src/syscall/process.rs#sys_rt_sigsuspend` | `kernel/src/syscall/sysproc.c#sys_rt_sigsuspend` | 同板块同名 | Err, SignalFlags::empty, current_task, current_task().unwrap, drop, inner.signals.is_empty ...(+3) | 内部闭环逻辑 |

#### 任务调度/异步运行时
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `os/src/syscall/fs.rs#sys_fstatat` | `kernel/src/syscall/sysfile.c#sys_fstatat` | 同板块同名 | AtFlags::from_bits_truncate, Ok, current_task, current_task().unwrap, current_user_token, fdtable.get ...(+14) | arg_addr, arg_int, arg_str, either_copy_out, find_inode, get_absolute_path ...(+6) |
| `os/src/task/futex.rs#futex_requeue` | `kernel/src/atomic/futex.c#futex_requeue` | 同板块同名 | Arc::downgrade, FUTEX_Q.lock, VecDeque::new, futex_q
            .entry, futex_q
            .entry(new_key)
            .or_insert_with, futex_q
            .entry(new_key)
            .or_insert_with(VecDeque::new)
            .extend ...(+6) | Queue_isempty_atomic, Queue_provide_atomic, Queue_push_back_atomic, acquire, futex_free, futex_wakeup ...(+5) |
| `os/src/syscall/fs.rs#sys_mkdirat` | `kernel/src/syscall/sysfile.c#sys_mkdirat` | 同板块同名 | Ok, Some, child_path.as_str, create_file, current_task, current_task().unwrap ...(+13) | arg_int, arg_str, assist_icreate, get_absolute_path, get_fs_from_path, ip->i_op->unlock_put ...(+2) |
| `os/src/syscall/fs.rs#sys_lseek` | `kernel/src/syscall/sysfile.c#sys_lseek` | 同板块同名 | Err, _file.seek, current_task, current_task().unwrap, drop, fdtable.get ...(+3) | MAX, arg_addr, arg_fd, arg_int, panic, strncmp ...(+1) |

#### 内存管理
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `virtio-drivers/src/device/socket/connectionmanager.rs#send_recv` | `kernel/dep/virtio-drivers/src/device/socket/connectionmanager.rs#send_recv` | 同板块同名 | "Hello from guest".as_bytes, (hello_from_guest.len() as u32).into, (hello_from_host.len() as u32).into, 0.into, 50.into, Arc::new ...(+43) | "Hello from guest".as_bytes, (hello_from_guest.len() as u32).into, (hello_from_host.len() as u32).into, 0.into, 50.into, Arc::new ...(+43) |
| `virtio-drivers/src/device/socket/connectionmanager.rs#incoming_connection` | `kernel/dep/virtio-drivers/src/device/socket/connectionmanager.rs#incoming_connection` | 同板块同名同结构 | 0.into, 50.into, Arc::new, Default::default, Mutex::new, NonNull::from ...(+25) | 0.into, 50.into, Arc::new, Default::default, Mutex::new, NonNull::from ...(+25) |
| `os/src/syscall/process.rs#sys_futex` | `kernel/src/syscall/sysproc.c#sys_futex` | 同板块同名 | Err, FutexCmd::from_bits_truncate, FutexKey::new, FutexOpt::from_bits_truncate, Ok, VirtAddr::from ...(+24) | arg_addr, arg_int, arg_uint, copy_in, do_futex, proc_current |
| `virtio-drivers/src/queue.rs#recycle_descriptors` | `kernel/dep/virtio-drivers/src/queue.rs#recycle_descriptors` | 同板块同名同结构 | Box::from_raw, H::unshare, InputOutputIter::new, InputOutputIter::new(inputs, outputs).enumerate, Some, assert_eq! ...(+17) | Box::from_raw, H::unshare, InputOutputIter::new, InputOutputIter::new(inputs, outputs).enumerate, Some, assert_eq! ...(+17) |

#### 中断/异常处理
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `virtio-drivers/examples/x86_64/src/main.rs#main` | `kernel/dep/virtio-drivers/examples/x86_64/src/main.rs#main` | 同板块同名同结构 | enumerate_pci, heap::init_heap, info!, logger::init, logger::init(log::LevelFilter::Info).unwrap, system_off ...(+1) | enumerate_pci, heap::init_heap, info!, logger::init, logger::init(log::LevelFilter::Info).unwrap, system_off ...(+1) |
| `virtio-drivers/examples/x86_64/src/trap.rs#init` | `kernel/dep/virtio-drivers/examples/x86_64/src/trap.rs#init` | 同板块同名同结构 | IDT.load, set_general_handler! | IDT.load, set_general_handler! |
| `lose-net-stack/src/connection/tcp.rs#interrupt` | `kernel/platform/visionfive/src/trap.c#tf_flrestore` | 同模块上下文对齐/差异观察 | data.len, data.to_vec, drop, flags.contains, self.datas.lock, self.datas.lock().push_back ...(+9) | 内部闭环逻辑 |
| `arch/src/x86_64/interrupt.rs#kernelvec` | `kernel/platform/visionfive/src/trap.c#tf_flstore` | 同模块上下文对齐/差异观察 | asm! | 内部闭环逻辑 |

#### 系统调用
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `user/src/lib.rs#waitpid` | `user/deps/syscall.c#waitpid` | 同板块同名 | sys_waitpid, yield_ | syscall |
| `user/src/lib.rs#wait` | `user/deps/syscall.c#wait` | 同板块同名 | sys_waitpid, yield_ | waitpid |
| `user/src/lib.rs#sleep` | `user/deps/syscall.c#sleep` | 同板块同名 | sys_get_time, sys_yield | syscall |
| `user/src/syscall.rs#sys_uname` | `kernel/src/syscall/sysinfo.c#sys_uname` | 同板块同名 | syscall | arg_addr, either_copy_out |

#### 文件系统
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `virtio-drivers/src/device/blk.rs#device_id` | `kernel/dep/virtio-drivers/src/device/blk.rs#device_id` | 同板块同名 | Arc::new, BlkFeature::RING_INDIRECT_DESC.bits, BlkResp {
                            status: RespStatus::OK,
                        }
                        .as_bytes, Default::default, Mutex::new, NonNull::from ...(+19) | Arc::new, BlkFeature::RING_INDIRECT_DESC.bits, Default::default, Mutex::new, NonNull::from, QUEUE_SIZE.into ...(+14) |
| `virtio-drivers/src/device/blk.rs#flush` | `kernel/dep/virtio-drivers/src/device/blk.rs#flush` | 同板块同名 | (BlkFeature::RING_INDIRECT_DESC | BlkFeature::FLUSH).bits, Arc::new, BlkResp {
                            status: RespStatus::OK,
                        }
                        .as_bytes, Default::default, Mutex::new, NonNull::from ...(+19) | (BlkFeature::RING_INDIRECT_DESC | BlkFeature::FLUSH).bits, Arc::new, Default::default, Mutex::new, NonNull::from, QUEUE_SIZE.into ...(+13) |
| `virtio-drivers/src/transport/pci/bus.rs#bar_info` | `kernel/dep/virtio-drivers/src/transport/pci/bus.rs#bar_info` | 同板块同名同结构 | (!(size_mask & 0xfffffff0)).wrapping_add, Err, MemoryBarType::try_from, Ok, self.config_read_word, self.config_write_word ...(+1) | (!(size_mask & 0xfffffff0)).wrapping_add, Err, MemoryBarType::try_from, Ok, self.config_read_word, self.config_write_word ...(+1) |
| `virtio-drivers/src/device/blk.rs#config` | `kernel/dep/virtio-drivers/src/device/blk.rs#config` | 同板块同名同结构 | Arc::new, BlkFeature::RO.bits, Default::default, Mutex::new, NonNull::from, QUEUE_SIZE.into ...(+6) | Arc::new, BlkFeature::RO.bits, Default::default, Mutex::new, NonNull::from, QUEUE_SIZE.into ...(+6) |

#### 设备驱动
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `virtio-drivers/src/queue.rs#fake_read_write_queue` | `kernel/dep/virtio-drivers/src/queue.rs#fake_read_write_queue` | 同板块同名 | Vec::new, assert!, assert_eq!, assert_ne!, descriptor.flags.contains, descriptor.next ...(+15) | (*available_ring).idx.load, (*used_ring).idx.fetch_add, (*used_ring).idx.load, Vec::new, assert!, assert_eq! ...(+18) |
| `virtio-drivers/examples/x86_64/src/tcp.rs#test_echo_server` | `kernel/dep/virtio-drivers/examples/x86_64/src/tcp.rs#test_echo_server` | 同板块同名同结构 | Config::new, DeviceWrapper::new, Instant::from_micros_const, Interface::new, IpAddress::from_str, IpAddress::from_str(IP).unwrap ...(+44) | Config::new, DeviceWrapper::new, Instant::from_micros_const, Interface::new, IpAddress::from_str, IpAddress::from_str(IP).unwrap ...(+44) |
| `virtio-drivers/examples/riscv/src/tcp.rs#test_echo_server` | `kernel/dep/virtio-drivers/examples/riscv/src/tcp.rs#test_echo_server` | 同板块同名同结构 | Config::new, DeviceWrapper::new, Instant::from_micros_const, Interface::new, IpAddress::from_str, IpAddress::from_str(IP).unwrap ...(+44) | Config::new, DeviceWrapper::new, Instant::from_micros_const, Interface::new, IpAddress::from_str, IpAddress::from_str(IP).unwrap ...(+44) |
| `virtio-drivers/src/queue.rs#add_buffers` | `kernel/dep/virtio-drivers/src/queue.rs#add_buffers` | 同板块同名同结构 | MmioTransport::new, NonNull::from, VirtIOHeader::make_fake_header, VirtQueue::<FakeHal, 4>::new, VirtQueue::<FakeHal, 4>::new(&mut transport, 0, false, false).unwrap, assert! ...(+6) | MmioTransport::new, NonNull::from, VirtIOHeader::make_fake_header, VirtQueue::<FakeHal, 4>::new, VirtQueue::<FakeHal, 4>::new(&mut transport, 0, false, false).unwrap, assert! ...(+6) |

#### 网络协议栈
仅目标项目中观察到该模块代表函数，参考项目没有同模块可对齐证据：
- 目标项目：`lose-net-stack/src/packets/arp.rs#build_data`，调用摘要：ARP_HRD_ETHER.to_be, EthRtype::ARP.into, EthRtype::IP.into, UnsafeRefIter::new, data_ptr_iter.next_mut::<Arp>, data_ptr_iter.next_mut::<Eth> ...(+5)
- 目标项目：`lose-net-stack/src/connection/mod.rs#analysis_ip`，调用摘要：cache_arp_entry, data_ptr_iter.next::<Ip>, self.analysis_icmp, self.analysis_tcp, self.analysis_udp, unsafe { data_ptr_iter.next::<Ip>() }.unwrap
- 目标项目：`lose-net-stack/src/connection/mod.rs#analysis_arp`，调用摘要：ArpPacket::new, ArpType::form_u16, T::send, arp
                .reply_packet, arp
                .reply_packet(self.local_ip, self.local_mac)
                .expect, arp
                .reply_packet(self.local_ip, self.local_mac)
                .expect("can't build reply")
                .build_data ...(+4)
- 目标项目：`lose-net-stack/src/utils.rs#check_sum`，调用摘要：内部闭环逻辑

#### 文件系统/第三方库
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `ext4_rs-1.3.1/src/utils/bitmap.rs#ext4_bmap_bit_find_clr` | `kernel/src/fs/ext4/lwext4/ext4_bitmap.c#ext4_bmap_bit_find_clr` | 同板块同名 | ext4_bmap_is_bit_clr | ext4_bmap_is_bit_clr |
| `ext4_rs-1.3.1/src/simple_interface/mod.rs#ext4_parse_flags` | `kernel/src/fs/ext4/lwext4/ext4.c#ext4_parse_flags` | 同板块同名 | Err, Ext4Error::new, Ok | strcmp |
| `ext4_rs-1.3.1/src/simple_interface/mod.rs#ext4_dir_mk` | `kernel/src/fs/ext4/lwext4/ext4.c#ext4_dir_mk` | 同板块同名 | Ext4DirEntry::default, Ext4DirSearchResult::new, filetype.bits, r.is_ok, return_errno!, self.dir_find_entry ...(+1) | EXT4_MP_LOCK, EXT4_MP_UNLOCK, ext4_generic_open, ext4_get_mount |
| `ext4_rs-1.3.1/src/ext4_defs/super_block.rs#ext4_ialloc_bitmap_csum` | `kernel/src/fs/ext4/lwext4/ext4_ialloc.c#ext4_ialloc_bitmap_csum` | 同板块同名 | ext4_crc32c, uuid.len | ext4_crc32c, ext4_get32, ext4_sb_feature_ro_com |

#### 第三方库
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `virtio-drivers/src/transport/mmio.rs#vendor_id` | `kernel/dep/virtio-drivers/src/transport/mmio.rs#vendor_id` | 同板块同名同结构 | volread! | volread! |

#### 通用模块/其他
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `arch/src/loongarch64/console.rs#getchar` | `user/deps/printf.c#getchar` | 同板块同名 | Some, lsr.contains, read_end.lsr.read, read_end.rbr.read, self.read_end | read |
| `arch/src/loongarch64/console.rs#putchar` | `user/deps/printf.c#putchar` | 同板块同名 | self.write_end, write_end.lsr.read, write_end.lsr.read().contains, write_end.thr.write | write |
| `arch/src/loongarch64/boot.rs#_start` | `kernel/src/lib/qsort.c#_quicksort` | 同模块上下文对齐/差异观察 | core::arch::asm! | (*cmp), POP, PUSH, SWAP, min |
| `sync/src/kspinbase.rs#lots_and_lots` | `kernel/dep/sdcard/src/visionfive2_sd/sd.rs#init_sdcard` | 同模块上下文对齐/差异观察 | M.lock, SpinMutex::<_>::new, Vec::new, assert_eq!, channel, drop ...(+10) | CardTypeReg::from, CardTypeReg::from(0).with_card_width4_1, CmdArg::new, CmdReg::from, ControlReg::from, RawInterruptStatusReg::from ...(+20) |


#### 1. 宏观判定
- **定性结论**: 独立开发 (相似度: 10.27%，共同 AST 指纹: 320；唯一 AST 指纹: 目标项目 1857，参考项目 1579；覆盖率: 目标项目 17.23%，参考项目 20.27%)

#### 2. 全模块覆盖矩阵解读

- **进程管理**
  - 双方均存在该模块。
  - 共同 AST 指纹为 0。
  - 目标覆盖率 0.0%，参考覆盖率 0.0%。表明双方在进程管理的核心逻辑上，未检测到任何结构级同源代码。
  - 证据强度：未形成。

- **任务调度/异步运行时**
  - 双方均存在该模块。
  - 共同 AST 指纹为 0。
  - 目标覆盖率 0.0%，参考覆盖率 0.0%。表明双方在调度器实现上，未检测到任何结构级同源代码。
  - 证据强度：未形成。

- **内存管理**
  - 双方均存在该模块。
  - 共同 AST 指纹为 41。
  - 目标覆盖率 11.14%，参考覆盖率 14.39%。该重合主要源于双方共同依赖的第三方 `virtio-drivers` 库中的内存操作，而非内核自身的内存管理算法。
  - 证据强度：弱。

- **中断/异常处理**
  - 双方均存在该模块。
  - 共同 AST 指纹为 2。
  - 目标覆盖率 5.26%，参考覆盖率 8.7%。重合点位于 `virtio-drivers` 的 x86_64 示例陷阱初始化代码，属于第三方库示例。
  - 证据强度：弱。

- **系统调用**
  - 双方均存在该模块。
  - 共同 AST 指纹为 0。
  - 目标覆盖率 0.0%，参考覆盖率 0.0%。表明双方在系统调用分发与实现上，未检测到任何结构级同源代码。
  - 证据强度：未形成。

- **文件系统**
  - 双方均存在该模块。
  - 共同 AST 指纹为 22。
  - 目标覆盖率 6.34%，参考覆盖率 14.97%。重合点主要位于 `virtio-drivers` 的块设备驱动及 `ext4_rs`/`lwext4` 库，属于第三方依赖。
  - 证据强度：弱。

- **设备驱动**
  - 双方均存在该模块。
  - 共同 AST 指纹为 253。
  - 目标覆盖率 68.75%，参考覆盖率 66.75%。这是唯一存在显著结构重合的板块，重合点几乎全部位于双方共同引用的开源第三方 crate `virtio-drivers` 及其示例代码。
  - 证据强度：强（第三方库/公共依赖）。

- **网络协议栈**
  - 仅目标项目体现。
  - 共同 AST 指纹为 0。
  - 目标覆盖率 0.0%，参考覆盖率 0.0%。目标项目拥有独立的网络协议栈实现，参考项目无此模块。
  - 证据强度：未形成。

- **第三方库/公共依赖**
  - 双方均存在该模块。
  - 共同 AST 指纹为 1。
  - 目标覆盖率 100.0%，参考覆盖率 100.0%。该指纹位于 `virtio-drivers` 的 MMIO 传输层，属于完全相同的第三方库代码。
  - 证据强度：强（第三方库/公共依赖）。

#### 3. 底层 AST 关键证据指纹表

| 所属板块 | 目标项目路径 | 参考项目路径 | 证据类型 | 技术说明 |
| :--- | :--- | :--- | :--- | :--- |
| 第三方库/公共依赖 | virtio-drivers/src/transport/mmio.rs#vendor_id | kernel/dep/virtio-drivers/src/transport/mmio.rs#vendor_id | 同板块同名同结构 | 完全相同的第三方库代码，使用相同的 `volread!` 宏。 |
| 设备驱动 | virtio-drivers/src/queue.rs#add_buffers | kernel/dep/virtio-drivers/src/queue.rs#add_buffers | 同板块同名同结构 | 完全相同的第三方库测试代码，用于创建虚拟队列。 |
| 设备驱动 | virtio-drivers/examples/x86_64/src/tcp.rs#test_echo_server | kernel/dep/virtio-drivers/examples/x86_64/src/tcp.rs#test_echo_server | 同板块同名同结构 | 完全相同的第三方库示例代码，用于 TCP 回显测试。 |
| 设备驱动 | virtio-drivers/examples/riscv/src/tcp.rs#test_echo_server | kernel/dep/virtio-drivers/examples/riscv/src/tcp.rs#test_echo_server | 同板块同名同结构 | 完全相同的第三方库示例代码，用于 TCP 回显测试。 |
| 中断/异常处理 | virtio-drivers/examples/x86_64/src/main.rs#main | kernel/dep/virtio-drivers/examples/x86_64/src/main.rs#main | 同板块同名同结构 | 完全相同的第三方库示例入口代码。 |
| 中断/异常处理 | virtio-drivers/examples/x86_64/src/trap.rs#init | kernel/dep/virtio-drivers/examples/x86_64/src/trap.rs#init | 同板块同名同结构 | 完全相同的第三方库示例陷阱初始化代码。 |
| 内存管理 | virtio-drivers/src/device/socket/connectionmanager.rs#send_recv | kernel/dep/virtio-drivers/src/device/socket/connectionmanager.rs#send_recv | 同板块同名 | 完全相同的第三方库代码，用于 VirtIO 套接字通信。 |
| 内存管理 | virtio-drivers/src/device/socket/connectionmanager.rs#incoming_connection | kernel/dep/virtio-drivers/src/device/socket/connectionmanager.rs#incoming_connection | 同板块同名同结构 | 完全相同的第三方库代码，用于处理 VirtIO 套接字连接。 |
| 文件系统 | virtio-drivers/src/device/blk.rs#device_id | kernel/dep/virtio-drivers/src/device/blk.rs#device_id | 同板块同名 | 高度相似的第三方库代码，用于初始化 VirtIO 块设备。 |
| 文件系统 | virtio-drivers/src/device/blk.rs#config | kernel/dep/virtio-drivers/src/device/blk.rs#config | 同板块同名同结构 | 完全相同的第三方库代码，用于配置只读 VirtIO 块设备。 |

#### 4. 核心代码调用流对比剖析

* **【设备驱动 / VirtIO 队列管理】**
* **底层技术析理**: 双方在 `virtio-drivers/src/queue.rs` 的 `add_buffers` 函数中呈现出完全相同的调用流：`MmioTransport::new` -> `VirtIOHeader::make_fake_header` -> `VirtQueue::<FakeHal, 4>::new` -> `assert!`。这是 `virtio-drivers` 库内部测试代码的标准写法，用于构造一个假的 VirtIO 队列进行单元测试。此同源指纹直接证明双方引用了完全相同的第三方库及测试用例，而非内核原创代码的复用。
* **代码溯源链接**:
  - 目标项目: `virtio-drivers/src/queue.rs#add_buffers`
  - 参考项目: `kernel/dep/virtio-drivers/src/queue.rs#add_buffers`

* **【进程管理 / 线程退出 (sys_exit)】**
* **底层技术析理**: 目标项目的 `sys_exit` 是一个薄封装，调用流为 `exit_current_and_run_next` -> `panic!`，体现了 Rust 语言下利用 `panic!` 处理不可恢复错误的模式。参考项目的 `do_exit` 是核心实现，调用流为 `acquire` -> `atomic_decrease` -> `free_thread` -> `list_del_reinit` -> `exit_wakeup` -> `release`，体现了 C 语言下基于手动锁和链表操作的传统内核资源清理模型。两者在抽象层次（系统调用入口 vs. 核心逻辑）、同步原语（`panic!` vs. `acquire/release`）和对象管理模型上完全不同，不存在 AST 层面的同构。
* **代码溯源链接**:
  - 目标项目: `os/src/syscall/process.rs#sys_exit`
  - 参考项目: `kernel/src/proc/pcb_life.c#do_exit`

* **【内存管理 / Futex 操作 (sys_futex)】**
* **底层技术析理**: 目标项目的 `sys_futex` 调用流为 `FutexCmd::from_bits_truncate` -> `FutexKey::new` -> `FutexOpt::from_bits_truncate` -> `VirtAddr::from`，使用了 Rust 的枚举和类型系统来解析 futex 命令。参考项目的 `sys_futex` 调用流为 `arg_int` -> `arg_addr` -> `arg_uint` -> `copy_in` -> `do_futex`，使用了 C 语言传统的参数提取和拷贝方式。两者在参数解析、内存抽象和错误处理模型上存在根本性差异，未发现共同 AST 指纹。
* **代码溯源链接**:
  - 目标项目: `os/src/syscall/process.rs#sys_futex`
  - 参考项目: `kernel/src/syscall/sysproc.c#sys_futex`

#### 5. 单方模块与降权项

- **网络协议栈**: 目标项目拥有独立的 `lose-net-stack` 实现，参考项目无此模块。这是目标项目的原创功能扩展，不构成同源证据。
- **第三方库/公共依赖**: 双方共同引用了 `virtio-drivers` 和 `ext4_rs`/`lwext4` 等开源库。这些库中的同名同结构函数（如 `vendor_id`, `add_buffers`, `test_echo_server`）是导致整体相似度和【设备驱动】板块高覆盖率的唯一原因。这些证据必须被降权处理，因为它们属于公共依赖复用，而非内核原创代码的抄袭。
- **同名系统调用**: `sys_clone`, `sys_getpid`, `sys_exit` 等函数名是 Linux ABI 的强制命名规范，任何兼容 Linux 的内核都必须使用。双方在这些函数上的同名现象不能作为复用证据，且其内部调用图谱完全不同。
- **标准接口字段**: `MemTotal`, `MemFree` 等字符串是 Linux `/proc/meminfo` 接口的标准输出字段，使用这些字段是遵循接口规范，不构成抄袭。

#### 6. 保守结论

本次审计的总体 AST 相似度仅为 10.27%，处于极低水平。唯一观察到的高覆盖率板块【设备驱动】（68.75%/66.75%）完全由双方共同引用的开源第三方库 `virtio-drivers` 驱动，属于公共依赖复用，不构成原创代码抄袭。控方重点指控的【进程管理】板块，其共同 AST 指纹为 0，表明双方在该核心领域不存在任何结构级代码同源。双方在内存模型、同步原语、错误处理机制和整体软件架构上存在由 Rust 和 C 语言特性决定的根本性差异。综合判定，目标项目 `[2026-T2026106149910802-XX]` 相对于参考项目 `[2024-cabbageOS-华中科技大学]` 构成独立开发，未发现足以定性为抄袭的结构性代码复用证据。

------------------------------

