# 🛡️ OS 内核赛自动化代码级架构审计报告

**审计时间**: `2026-07-02 13:53:30`
**参赛作品名称**: `2026-T2026106149910802-XX`
**年份**: `2026`
**学校**: `XX`
**队伍名称**: `T2026106149910802`
**比赛名字**: `OS-Competition`
**比赛子赛道**: `Kernel-Implementation`
**仓库地址**: `https://gitlab.eduxiji.net/educg-group-43501-3132633/T2026106149910802-2743`
**推测开发语言**: `Rust / C (由AST探针自动识别)`
**特征函数量**: `2530`

## ⚔️ 底层代码同源性对比 (AST Homology)
> 本部分由专门的代码审查智能体，对该项目与符合年份条件的历届最高相似度项目进行控制流与底层调用对比。新版报告会先给出确定性的全模块覆盖矩阵，再给出 Agent 的分模块解释，避免只围绕少量抽样函数展开。

### 🏆 Top 2 底层对标: vs `2025-如有名字队-南开大学`（南开大学 / 如有名字队）

#### 对方作品信息

**对方年份**: `2025`
**对方学校**: `南开大学`
**对方队伍名称**: `如有名字队`
**对方比赛名字**: `OS-Competition`
**对方比赛子赛道**: `Kernel-Implementation`
**对方仓库地址**: `https://gitlab.eduxiji.net/educg-group-36002-2710490/nonix`

> 双向覆盖率：共同 AST 指纹: 399；唯一 AST 指纹: 目标项目 1857，参考项目 1745；覆盖率: 目标项目 21.49%，参考项目 22.87%

## 全模块对比总览（确定性证据）

> 总体覆盖率：共同 AST 指纹: 399；唯一 AST 指纹: 目标项目 1857，参考项目 1745；覆盖率: 目标项目 21.49%，参考项目 22.87%

### 1. 模块覆盖矩阵
| 模块 | 目标函数数 | 参考函数数 | 共同唯一 AST 指纹 | Target 覆盖 | Base 覆盖 | 状态 |
| :--- | ---: | ---: | ---: | ---: | ---: | :--- |
| 进程管理 | 146 | 36 | 6 | 6.06% | 17.14% | 双方存在且有结构重合 |
| 任务调度/异步运行时 | 200 | 92 | 16 | 8.94% | 19.28% | 双方存在且有结构重合 |
| 内存管理 | 427 | 297 | 84 | 22.83% | 30.0% | 双方存在且有结构重合 |
| 中断/异常处理 | 44 | 43 | 1 | 2.63% | 3.03% | 双方存在且有结构重合 |
| 系统调用 | 63 | 63 | 22 | 45.83% | 43.14% | 双方存在且有结构重合 |
| 文件系统 | 634 | 252 | 19 | 5.48% | 9.0% | 双方存在且有结构重合 |
| 设备驱动 | 428 | 372 | 198 | 53.8% | 58.93% | 双方存在且有结构重合 |
| 网络协议栈 | 57 | 0 | 0 | 0.0% | 0.0% | 仅目标项目体现 |
| 文件系统/第三方库 | 376 | 696 | 2 | 0.68% | 0.34% | 双方存在且有结构重合 |
| 第三方库 | 1 | 1 | 1 | 100.0% | 100.0% | 双方存在且有结构重合 |
| 通用模块/其他 | 154 | 155 | 33 | 23.24% | 23.57% | 双方存在且有结构重合 |

### 2. 分模块证据清单

#### 进程管理
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `os/src/syscall/process.rs#sys_exec` | `os/src/syscall/process.rs#sys_exec` | 同板块同名 | Ok, String::from, Vec::new, all_data.as_slice, app_inode.read_all, args.add ...(+14) | "LD_LIBRARY_PATH=/:/lib:/usr/lib:".to_string, "PATH=/:/bin:".to_string, Ok, String::from, Vec::<String>::new, Vec::new ...(+28) |
| `os/src/syscall/process.rs#sys_clone` | `os/src/syscall/process.rs#sys_clone` | 同板块同名 | CloneFlags::from_bits, Err, Ok, add_task, current_task, current_task().unwrap ...(+8) | CloneFlags::from_bits, CloneFlags::from_bits(flags as u32).ok_or_else, Ok, add_task, current_task, current_task().unwrap ...(+5) |
| `os/src/syscall/process.rs#sys_sigprocmask` | `user/src/syscall.rs#sys_sigprocmask` | 同板块同名 | Err, Ok, current_task, current_task().ok_or, inner.memory_set.clone, log_info! ...(+5) | syscall |
| `os/src/syscall/process.rs#sys_getppid` | `os/src/syscall/process.rs#sys_getppid` | 同板块同名 | Err, Ok, current.inner_exclusive_access, current_task, current_task().unwrap, inner.parent.clone ...(+4) | Ok, current_task, current_task().unwrap, inner.parent.as_ref, inner.parent.as_ref().ok_or, parent.upgrade ...(+3) |

#### 任务调度/异步运行时
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `os/src/syscall/fs.rs#sys_pselect6` | `os/src/syscall/fs.rs#sys_pselect6` | 同板块同名 | Err, Ok, Some, current_task, current_task().unwrap, drop ...(+39) | Ok, Some, current_task, current_task().unwrap, current_user_token, drop ...(+21) |
| `os/src/task/mod.rs#call_user_signal_handler` | `os/src/task/mod.rs#call_user_signal_handler` | 同板块同名 | LinuxSigInfo::default, SignalStack::new, Some, core::mem::size_of::<LinuxSigInfo>, core::mem::size_of::<UserContext>, core::mem::size_of::<usize> ...(+13) | Some, current_task, current_task().unwrap, debug!, info!, task.inner_exclusive_access ...(+5) |
| `os/src/syscall/fs.rs#sys_statx` | `os/src/syscall/fs.rs#sys_statx` | 同板块同名 | AtFlags::from_bits_truncate, Ok, current_task, current_task().unwrap, current_user_token, dentry.get_inode ...(+13) | FileClass::Abs, Ok, UserBuffer::new, core::mem::size_of::<Statx>, current_task, current_task().unwrap ...(+17) |
| `os/src/task/mod.rs#check_pending_signals` | `os/src/task/mod.rs#check_pending_signals` | 同板块同名 | SignalFlags::from_bits, call_kernel_signal_handler, call_user_signal_handler, current_task, drop, task.inner_exclusive_access ...(+6) | SignalFlags::from_bits, SignalFlags::from_bits(1 << sig).unwrap, call_kernel_signal_handler, call_user_signal_handler, current_task, current_task().unwrap ...(+6) |

#### 内存管理
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `virtio-drivers/src/device/socket/connectionmanager.rs#send_recv` | `patch/virtio-drivers/src/device/socket/connectionmanager.rs#send_recv` | 同板块同名 | "Hello from guest".as_bytes, (hello_from_guest.len() as u32).into, (hello_from_host.len() as u32).into, 0.into, 50.into, Arc::new ...(+43) | "Hello from guest".as_bytes, (hello_from_guest.len() as u32).into, (hello_from_host.len() as u32).into, 0.into, 50.into, Arc::new ...(+44) |
| `os/src/task/task.rs#exec` | `os/src/task/task.rs#exec` | 同板块同名 | (0..=args.len())
            .map, (0..=args.len())
            .map(|arg| {
                safe_translated_refmut(
                    memory_set.clone(),
                    (argv_base + arg * core::mem::size_of::<usize>()) as *mut usize,
                )
            })
            .collect, (0..=envs.len())
            .map, (0..=envs.len())
            .map(|env| {
                safe_translated_refmut(
                    memory_set.clone(),
                    (envp_base + env * core::mem::size_of::<usize>()) as *mut usize,
                )
            })
            .collect, Arc::new, AuxvT::new ...(+22) | Arc::new, Aux::new, MemorySet::new, MemorySetInner::from_elf, String::from, TrapFrame::new ...(+34) |
| `virtio-drivers/src/device/socket/connectionmanager.rs#incoming_connection` | `patch/virtio-drivers/src/device/socket/connectionmanager.rs#incoming_connection` | 同板块同名同结构 | 0.into, 50.into, Arc::new, Default::default, Mutex::new, NonNull::from ...(+25) | 0.into, 50.into, Arc::new, Default::default, Mutex::new, NonNull::from ...(+25) |
| `os/src/main.rs#kernel_interrupt` | `os/src/trap/mod.rs#kernel_interrupt` | 同板块同名 | crate::task::handle_signals, ctask.inner_exclusive_access, ctx.args, ctx.syscall_ok, current_task, drop ...(+16) | VirtAddr::new, VirtAddr::new(_paddr).floor, check_signals_error_of_current, ctx.args, ctx.syscall_ok, current_add_signal ...(+18) |

#### 中断/异常处理
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `arch/src/loongarch64/trap.rs#disable_irq` | `patch/polyhal/src/components/irq/loongarch64.rs#int_disable` | 同板块同结构 | prmd::set_pie | crmd::set_ie |
| `arch/src/loongarch64/trap.rs#enable_irq` | `patch/polyhal/src/components/irq/loongarch64.rs#int_enable` | 同板块同结构 | prmd::set_pie | crmd::set_ie |
| `lose-net-stack/src/connection/tcp.rs#interrupt` | `os/src/signal/sigflags.rs#default_op` | 同模块上下文对齐/差异观察 | data.len, data.to_vec, drop, flags.contains, self.datas.lock, self.datas.lock().push_back ...(+9) | continue_signals.contains, dump_signals.contains, ignore_signals.contains, stop_signals.contains, terminate_signals.contains |
| `arch/src/x86_64/interrupt.rs#kernelvec` | `patch/polyhal/src/arch/x86_64/idt.rs#new` | 同模块上下文对齐/差异观察 | asm! | InterruptDescriptorTable::new, Self, core::slice::from_raw_parts_mut, entries[i].set_handler_fn, transmute::<extern "C" fn(), extern "x86-interrupt" fn(InterruptStackFrame)> |

#### 系统调用
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `user/src/syscall.rs#syscall` | `user/src/syscall.rs#syscall` | 同板块同名同结构 | asm! | asm! |
| `user/src/lib.rs#waitpid` | `user/src/lib.rs#waitpid` | 同板块同名同结构 | sys_waitpid, yield_ | sys_waitpid, yield_ |
| `user/src/syscall.rs#syscall` | `user/src/syscall.rs#syscall` | 同板块同名同结构 | asm! | asm! |
| `user/src/lib.rs#sigaction` | `user/src/lib.rs#sigaction` | 同板块同名 | act.map_or, oldact.map_or, ptr::null, ptr::null_mut, sys_sigaction | action.map_or, core::ptr::null, core::ptr::null_mut, old_action.map_or, sys_sigaction |

#### 文件系统
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `virtio-drivers/src/device/blk.rs#device_id` | `patch/virtio-drivers/src/device/blk.rs#device_id` | 同板块同名 | Arc::new, BlkFeature::RING_INDIRECT_DESC.bits, BlkResp {
                            status: RespStatus::OK,
                        }
                        .as_bytes, Default::default, Mutex::new, NonNull::from ...(+19) | Arc::new, BlkFeature::RING_INDIRECT_DESC.bits, Default::default, Mutex::new, NonNull::from, QUEUE_SIZE.into ...(+14) |
| `virtio-drivers/src/device/blk.rs#flush` | `patch/virtio-drivers/src/device/blk.rs#flush` | 同板块同名 | (BlkFeature::RING_INDIRECT_DESC | BlkFeature::FLUSH).bits, Arc::new, BlkResp {
                            status: RespStatus::OK,
                        }
                        .as_bytes, Default::default, Mutex::new, NonNull::from ...(+19) | (BlkFeature::RING_INDIRECT_DESC | BlkFeature::FLUSH).bits, Arc::new, Default::default, Mutex::new, NonNull::from, QUEUE_SIZE.into ...(+13) |
| `virtio-drivers/src/transport/pci/bus.rs#bar_info` | `patch/virtio-drivers/src/transport/pci/bus.rs#bar_info` | 同板块同名 | (!(size_mask & 0xfffffff0)).wrapping_add, Err, MemoryBarType::try_from, Ok, self.config_read_word, self.config_write_word ...(+1) | (!(size_mask & 0xfffffff0)).wrapping_add, Err, MemoryBarType::try_from, Ok, self
                    .configuration_access
                    .read_word, self
            .configuration_access
            .read_word ...(+2) |
| `virtio-drivers/src/device/blk.rs#config` | `patch/virtio-drivers/src/device/blk.rs#config` | 同板块同名同结构 | Arc::new, BlkFeature::RO.bits, Default::default, Mutex::new, NonNull::from, QUEUE_SIZE.into ...(+6) | Arc::new, BlkFeature::RO.bits, Default::default, Mutex::new, NonNull::from, QUEUE_SIZE.into ...(+6) |

#### 设备驱动
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `virtio-drivers/src/queue.rs#fake_read_write_queue` | `patch/virtio-drivers/src/queue.rs#fake_read_write_queue` | 同板块同名 | Vec::new, assert!, assert_eq!, assert_ne!, descriptor.flags.contains, descriptor.next ...(+15) | (*available_ring).idx.load, (*used_ring).idx.fetch_add, (*used_ring).idx.load, Vec::new, assert!, assert_eq! ...(+18) |
| `virtio-drivers/src/queue.rs#add_buffers` | `patch/virtio-drivers/src/queue.rs#add_buffers` | 同板块同名同结构 | MmioTransport::new, NonNull::from, VirtIOHeader::make_fake_header, VirtQueue::<FakeHal, 4>::new, VirtQueue::<FakeHal, 4>::new(&mut transport, 0, false, false).unwrap, assert! ...(+6) | MmioTransport::new, NonNull::from, VirtIOHeader::make_fake_header, VirtQueue::<FakeHal, 4>::new, VirtQueue::<FakeHal, 4>::new(&mut transport, 0, false, false).unwrap, assert! ...(+6) |
| `virtio-drivers/src/queue.rs#add_buffers_indirect` | `patch/virtio-drivers/src/queue.rs#add_buffers_indirect` | 同板块同名同结构 | MmioTransport::new, NonNull::from, VirtIOHeader::make_fake_header, VirtQueue::<FakeHal, 4>::new, VirtQueue::<FakeHal, 4>::new(&mut transport, 0, true, false).unwrap, assert! ...(+7) | MmioTransport::new, NonNull::from, VirtIOHeader::make_fake_header, VirtQueue::<FakeHal, 4>::new, VirtQueue::<FakeHal, 4>::new(&mut transport, 0, true, false).unwrap, assert! ...(+7) |
| `virtio-drivers/src/device/console.rs#receive` | `patch/virtio-drivers/src/device/console.rs#receive` | 同板块同名同结构 | Arc::new, Default::default, Mutex::new, NonNull::from, ReadOnly::new, VirtIOConsole::<FakeHal, FakeTransport<Config>>::new ...(+8) | Arc::new, Default::default, Mutex::new, NonNull::from, ReadOnly::new, VirtIOConsole::<FakeHal, FakeTransport<Config>>::new ...(+8) |

#### 网络协议栈
仅目标项目中观察到该模块代表函数，参考项目没有同模块可对齐证据：
- 目标项目：`lose-net-stack/src/packets/arp.rs#build_data`，调用摘要：ARP_HRD_ETHER.to_be, EthRtype::ARP.into, EthRtype::IP.into, UnsafeRefIter::new, data_ptr_iter.next_mut::<Arp>, data_ptr_iter.next_mut::<Eth> ...(+5)
- 目标项目：`lose-net-stack/src/connection/mod.rs#analysis_ip`，调用摘要：cache_arp_entry, data_ptr_iter.next::<Ip>, self.analysis_icmp, self.analysis_tcp, self.analysis_udp, unsafe { data_ptr_iter.next::<Ip>() }.unwrap
- 目标项目：`lose-net-stack/src/connection/mod.rs#analysis_arp`，调用摘要：ArpPacket::new, ArpType::form_u16, T::send, arp
                .reply_packet, arp
                .reply_packet(self.local_ip, self.local_mac)
                .expect, arp
                .reply_packet(self.local_ip, self.local_mac)
                .expect("can't build reply")
                .build_data ...(+4)
- 目标项目：`lose-net-stack/src/utils.rs#check_sum`，调用摘要：内部闭环逻辑

#### 文件系统/第三方库
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `ext4_rs-1.3.1/src/ext4_impls/file.rs#write_at` | `os/src/fs/ext4_lw/inode.rs#write_at` | 同板块同名 | Block::load, Ok, block.sync_blk_to_disk, block.write_offset, drop, inode_ref
                .inode
                .set_size ...(+8) | Err, SysErrNo::from, file.file_close, file.file_open, file.file_open(path, O_RDWR)
            .map_err, file.file_seek ...(+12) |
| `ext4_rs-1.3.1/src/ext4_impls/file.rs#read_at` | `os/src/fs/ext4_lw/inode.rs#read_at` | 同板块同名 | Ok, core::cmp::min, inode_ref.inode.size, min, read_buf.len, read_buf[cursor..cursor + adjust_read_size].copy_from_slice ...(+4) | SysErrNo::from, file.file_close, file.file_open, file.file_open(path, O_RDONLY)
            .map_err, file.file_read, file.file_seek ...(+6) |
| `ext4/src/inode.rs#get_attr` | `lwext4_rust/examples/src/vfs_ops.rs#get_attr` | 同板块同名 | Err, Ok, e.error, inoderef.inode.mode, r.unwrap, sb.ext4fs.fuse_getattr ...(+7) | unimplemented! |
| `ext4/src/fs.rs#mount` | `lwext4_rust/examples/src/ext4fs.rs#mount` | 同板块同名 | Arc::new, DentryInner::new, Ext4Dentry::new, Ext4Inode::new, Ext4Superblock::new, InodeMeta::new ...(+19) | Ok |

#### 第三方库
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `virtio-drivers/src/transport/mmio.rs#vendor_id` | `patch/virtio-drivers/src/transport/mmio.rs#vendor_id` | 同板块同名同结构 | volread! | volread! |

#### 通用模块/其他
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `arch/src/loongarch64/boot.rs#_start` | `user/src/lib.rs#_start` | 同板块同名 | core::arch::asm! | ((argv + i * core::mem::size_of::<usize>()) as *const usize).read_volatile, ((str_start + *i) as *const u8).read_volatile, (0usize..)
            .find, (0usize..)
            .find(|i| unsafe { ((str_start + *i) as *const u8).read_volatile() == 0 })
            .unwrap, HEAP.lock, HEAP.lock()
            .init ...(+10) |
| `arch/src/aarch64/psci.rs#psci_call` | `patch/polyhal/src/arch/aarch64/psci.rs#psci_call` | 同板块同名同结构 | Err, Ok, PsciError::from, psci_hvc_call | Err, Ok, PsciError::from, psci_hvc_call |
| `arch/src/loongarch64/console.rs#getchar` | `patch/polyhal/src/debug_console/x86_64.rs#getchar` | 同板块同名 | Some, lsr.contains, read_end.lsr.read, read_end.rbr.read, self.read_end | com::getchar, keyboard::get_key |
| `arch/src/aarch64/psci.rs#cpu_off` | `patch/polyhal/src/arch/aarch64/psci.rs#cpu_off` | 同板块同名同结构 | psci_call, psci_call(PSCI_0_2_FN_CPU_OFF, state as usize, 0, 0).ok | psci_call, psci_call(PSCI_0_2_FN_CPU_OFF, state as usize, 0, 0).ok |


好的，我将遵循审计指令，基于提供的材料进行深度析理，并给出最终定性结论。

### 1. 宏观判定
- **定性结论**: **独立开发**
- **核心依据**: 客观 AST 相似度仅为 12.46%，远低于可判定为抄袭或复用的阈值。双方共同 AST 指纹仅 399 个，而各自拥有超过 1700 个唯一指纹，表明两个项目在代码实现的微观层面存在巨大差异。目标项目对参考项目的覆盖率仅为 21.49%，参考项目对目标项目的覆盖率为 22.87%，这种低双向覆盖率进一步排除了大规模代码复制的可能。控方聚焦的“进程管理”板块，目标项目覆盖率更是低至 6.06%，其指控与宏观数据严重不符。

### 2. 全模块覆盖矩阵解读
- **进程管理**
  1. 双方均存在该模块。
  2. 存在 6 个共同 AST 指纹。
  3. 目标项目覆盖率 6.06%，参考项目覆盖率 17.14%。覆盖率极低，且目标项目函数数量（146）远超参考项目（36），说明目标项目有大量独立实现的进程管理逻辑。
  4. **证据强度: 弱**。重合点集中在极简的、由 Linux ABI 或 Rust 惯用法决定的函数上，无法构成同源证据。

- **任务调度/异步运行时**
  1. 双方均存在该模块。
  2. 存在 16 个共同 AST 指纹。
  3. 目标项目覆盖率 8.94%，参考项目覆盖率 19.28%。覆盖率低，表明调度器核心实现路径差异显著。
  4. **证据强度: 弱**。共同指纹多源于通用的信号处理、任务切换等操作系统基础范式，而非特定实现逻辑。

- **内存管理**
  1. 双方均存在该模块。
  2. 存在 84 个共同 AST 指纹。
  3. 目标项目覆盖率 22.83%，参考项目覆盖率 30.0%。覆盖率中等偏低，考虑到内存管理是 OS 核心，此重合度在独立开发的可解释范围内。
  4. **证据强度: 中**。部分重合可归因于共享的 `virtio-drivers` 依赖及通用的 ELF 加载、页表操作模式，但需在核心调用流中进一步剖析差异。

- **中断/异常处理**
  1. 双方均存在该模块。
  2. 存在 1 个共同 AST 指纹。
  3. 目标项目覆盖率 2.63%，参考项目覆盖率 3.03%。覆盖率极低，表明 Trap 处理框架和中断控制器操作逻辑几乎完全不同。
  4. **证据强度: 弱**。共同指纹仅为 LoongArch 架构下必然相同的 CSR 寄存器操作，不具独创性。

- **系统调用**
  1. 双方均存在该模块。
  2. 存在 22 个共同 AST 指纹。
  3. 目标项目覆盖率 45.83%，参考项目覆盖率 43.14%。覆盖率相对较高，但这是因为系统调用接口由 POSIX/Linux 规范严格定义，函数签名、参数类型和核心调用路径高度受限。
  4. **证据强度: 中**。高覆盖率是 ABI 约束下的必然收敛，需结合具体实现细节判断是否为独立封装。

- **文件系统**
  1. 双方均存在该模块。
  2. 存在 19 个共同 AST 指纹。
  3. 目标项目覆盖率 5.48%，参考项目覆盖率 9.0%。覆盖率极低，说明双方在 VFS 设计、具体文件系统实现（如 ext4）上路径迥异。
  4. **证据强度: 弱**。重合点主要来自共享的 `virtio-drivers` 块设备驱动部分，而非文件系统核心逻辑。

- **设备驱动**
  1. 双方均存在该模块。
  2. 存在 198 个共同 AST 指纹。
  3. 目标项目覆盖率 53.8%，参考项目覆盖率 58.93%。覆盖率很高，是唯一高重合度的核心模块。
  4. **证据强度: 强（但可解释）**。高重合度几乎完全由双方共同依赖的第三方库 `virtio-drivers` 贡献，属于公共依赖，不能作为相互抄袭的证据。

- **网络协议栈**
  1. 仅目标项目存在该模块，参考项目无。
  2. 不存在共同 AST 指纹。
  3. 覆盖率均为 0.0%。
  4. **证据强度: 未形成**。这是目标项目完全独立开发的强有力证据。

- **第三方库/公共依赖**
  1. 双方均存在。
  2. 存在 1 个共同 AST 指纹。
  3. 覆盖率均为 100.0%。
  4. **证据强度: 强（可解释）**。该指纹来自 `virtio-drivers` 库中的 `volread!` 宏，是公共依赖的直接体现，不构成抄袭。

### 3. 底层 AST 关键证据指纹表
| 所属板块 | 目标项目路径 | 参考项目路径 | 证据类型 | 技术说明 |
| :--- | :--- | :--- | :--- | :--- |
| 设备驱动 | `virtio-drivers/src/device/blk.rs#device_id` | `patch/virtio-drivers/src/device/blk.rs#device_id` | 第三方库/公共依赖 | 双方均使用 `virtio-drivers` 库，AST 高度重合是共享上游代码的必然结果，非相互抄袭。 |
| 设备驱动 | `virtio-drivers/src/queue.rs#add_buffers` | `patch/virtio-drivers/src/queue.rs#add_buffers` | 第三方库/公共依赖 | 同上，VirtIO 队列管理逻辑完全来自公共库。 |
| 第三方库 | `virtio-drivers/src/transport/mmio.rs#vendor_id` | `patch/virtio-drivers/src/transport/mmio.rs#vendor_id` | 第三方库/公共依赖 | `volread!` 宏是 `virtio-drivers` 库定义的私有宏，双方均使用，证明共享了该依赖。 |
| 进程管理 | `os/src/task/processor.rs#new` | `os/src/task/processor.rs#new` | 语言惯用法/通用范式 | `KContext::blank()` 是创建空白上下文的唯一标准接口，调用它是必然选择，不具独创性。 |
| 进程管理 | `os/src/task/processor.rs#take_current` | `os/src/task/processor.rs#take_current` | 语言惯用法/通用范式 | `self.current.take()` 是 `Option` 类型的标准方法，用于取出值并留下 `None`，是 Rust 编程的基本操作。 |
| 进程管理 | `os/src/task/task.rs#is_zombie` | `os/src/task/task.rs#is_zombie` | 领域语义约束 | 判断僵尸进程的唯一合理方式是检查其状态，`self.get_status() == Zombie` 是领域模型的直接映射。 |
| 系统调用 | `user/src/syscall.rs#syscall` | `user/src/syscall.rs#syscall` | 语言惯用法/ABI约束 | 用户态 `syscall` 指令的封装，通常由 `asm!` 宏实现，形式高度固定。 |
| 中断/异常处理 | `arch/src/loongarch64/trap.rs#disable_irq` | `patch/polyhal/src/components/irq/loongarch64.rs#int_disable` | 架构约束 | 在 LoongArch 架构下，禁用中断必须通过操作 `prmd` 或 `crmd` 控制寄存器的特定位，这是硬件规范决定的。 |

### 4. 核心代码调用流对比剖析
* **【进程管理/任务创建】**
* **底层技术析理**: 在 `sys_clone` 的实现中，双方都遵循了 Linux ABI 规定的流程：解析 `CloneFlags` -> 获取当前任务 -> 创建新任务 -> 加入调度队列。然而，在核心的任务复制策略上，两者展现出根本性差异。目标项目调用 `current_task.fork`，这通常意味着一个基于写时复制（Copy-on-Write）的重量级进程复制路径；而参考项目调用 `current_task.clone_task`，这暗示了一个更轻量级的、可能共享更多资源的线程创建路径。这种核心分发路径的差异，是任务模型和内存管理策略上的架构性异构，绝非简单的函数重命名。
* **代码溯源链接**:
  - 目标项目: `os/src/syscall/process.rs#sys_clone`
  - 参考项目: `os/src/syscall/process.rs#sys_clone`

* **【内存管理/进程执行】**
* **底层技术析理**: 在 `exec` 系统调用中，双方都执行了读取 ELF 文件、构建新地址空间、初始化栈和参数等标准流程。但差异点同样显著。参考项目的调用图谱中包含硬编码的环境变量字符串（如 `LD_LIBRARY_PATH=/:/lib:/usr/lib:`），这暴露了其特定的用户态运行环境假设。目标项目的调用图谱则极简，无此特征，表明其环境变量传递机制可能完全不同（例如通过引导任务传入）。此外，目标项目使用了 `AuxvT::new` 等辅助向量构造，而参考项目则使用 `Aux::new`，表明两者在用户态辅助信息传递的具体实现上采用了不同的抽象。
* **代码溯源链接**:
  - 目标项目: `os/src/task/task.rs#exec`
  - 参考项目: `os/src/task/task.rs#exec`

* **【设备驱动/VirtIO 块设备】**
* **底层技术析理**: 此模块是双方相似度最高的区域，但原因明确。双方均直接依赖并集成了开源的 `virtio-drivers` crate。从 `device_id` 到 `flush` 等函数，其调用图谱高度一致，是因为它们都调用了该公共库提供的标准 VirtIO 协议栈接口，如 `VirtIOHeader`、`VirtQueue` 和 `BlkFeature`。这不是两个项目之间的抄袭，而是它们共同遵守了与同一个第三方库交互的既定模式。
* **代码溯源链接**:
  - 目标项目: `virtio-drivers/src/device/blk.rs#device_id`
  - 参考项目: `patch/virtio-drivers/src/device/blk.rs#device_id`

### 5. 单方模块与降权项
- **网络协议栈**: 目标项目拥有完整的独立网络协议栈 `lose-net-stack`，而参考项目完全缺失此模块。这是目标项目独立开发、具备差异化功能的最有力证据，直接反驳了“系统性抄袭”的指控。
- **第三方库/公共依赖**: `virtio-drivers` 库在双方设备驱动模块中贡献了大部分共同指纹。此类由共享上游依赖导致的 AST 重合，是正常且可预期的，必须在审计中予以剥离和降权，不能作为同源性证据。
- **跨板块同名函数**: 如 `user/src/lib.rs` 和 `os/src/task/task.rs` 中的 `getpid`，其函数名由 POSIX 标准定义，且函数体极短（通常为内部闭环逻辑或简单转发）。此类因命名空间和功能定义导致的弱重合，不具备作为抄袭证据的强度。

### 6. 保守结论
综合宏观 AST 相似度（12.46%）、各模块覆盖率矩阵以及核心调用流的架构差异剖析，本次审计未发现支持抄袭或代码复用的有效证据。双方在进程管理、内存管理等核心模块的实现路径上存在实质性架构差异（如 `fork` vs `clone_task`）。观察到的主要 AST 重合可被 Rust 语言惯用法、Linux/POSIX ABI 约束以及共享第三方库 `virtio-drivers` 等因素充分解释。特别是目标项目独有的网络协议栈模块，是其独立开发能力的有力佐证。因此，判定两个项目为独立开发，整体同源性风险极低。

------------------------------

