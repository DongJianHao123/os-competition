# 🛡️ OS 内核赛自动化代码级架构审计报告

**审计时间**: `2026-07-02 13:53:30`
**参赛作品名称**: `2026-T2026106149910802-XX`
**年份**: `2026`
**学校**: `XX`
**队伍名称**: `T2026106149910802`
**比赛名字**: `OS-Competition`
**比赛子赛道**: `Kernel-Implementation`
**仓库地址**: `https://gitlab.eduxiji.net/educg-group-43501-3132633/T2026106149910802-2743`
**推测开发语言**: `Rust / C (由AST探针自动识别)`
**特征函数量**: `2530`

## ⚔️ 底层代码同源性对比 (AST Homology)
> 本部分由专门的代码审查智能体，对该项目与符合年份条件的历届最高相似度项目进行控制流与底层调用对比。新版报告会先给出确定性的全模块覆盖矩阵，再给出 Agent 的分模块解释，避免只围绕少量抽样函数展开。

### 🏆 Top 4 底层对标: vs `2025-NPUcore-BLOSSOM-西北工业大学`（西北工业大学 / NPUcore-BLOSSOM）

#### 对方作品信息

**对方年份**: `2025`
**对方学校**: `西北工业大学`
**对方队伍名称**: `NPUcore-BLOSSOM`
**对方比赛名字**: `OS-Competition`
**对方比赛子赛道**: `Kernel-Implementation`
**对方仓库地址**: `https://gitlab.eduxiji.net/educg-group-36002-2710490/oskernel2025-npucore-blossom`

> 双向覆盖率：共同 AST 指纹: 423；唯一 AST 指纹: 目标项目 1857，参考项目 2354；覆盖率: 目标项目 22.78%，参考项目 17.97%

## 全模块对比总览（确定性证据）

> 总体覆盖率：共同 AST 指纹: 423；唯一 AST 指纹: 目标项目 1857，参考项目 2354；覆盖率: 目标项目 22.78%，参考项目 17.97%

### 1. 模块覆盖矩阵
| 模块 | 目标函数数 | 参考函数数 | 共同唯一 AST 指纹 | Target 覆盖 | Base 覆盖 | 状态 |
| :--- | ---: | ---: | ---: | ---: | ---: | :--- |
| 进程管理 | 146 | 75 | 6 | 6.06% | 8.82% | 双方存在且有结构重合 |
| 任务调度/异步运行时 | 200 | 143 | 8 | 4.47% | 6.15% | 双方存在且有结构重合 |
| 内存管理 | 427 | 605 | 25 | 6.79% | 5.84% | 双方存在且有结构重合 |
| 中断/异常处理 | 44 | 47 | 2 | 5.26% | 5.0% | 双方存在且有结构重合 |
| 系统调用 | 63 | 65 | 15 | 31.25% | 28.85% | 双方存在且有结构重合 |
| 文件系统 | 634 | 561 | 5 | 1.44% | 1.34% | 双方存在且有结构重合 |
| 设备驱动 | 428 | 47 | 2 | 0.54% | 4.26% | 双方存在且有结构重合 |
| 网络协议栈 | 57 | 88 | 1 | 2.08% | 2.13% | 双方存在且有结构重合 |
| 文件系统/第三方库 | 376 | 369 | 124 | 42.03% | 42.91% | 双方存在且有结构重合 |
| 网络协议栈/第三方库 | 0 | 138 | 0 | 0.0% | 0.0% | 仅参考项目体现 |
| 第三方库 | 1 | 1228 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |
| 通用模块/其他 | 154 | 241 | 5 | 3.52% | 3.88% | 双方存在且有结构重合 |

### 2. 分模块证据清单

#### 进程管理
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `os/src/syscall/process.rs#sys_sigreturn` | `os/src/syscall/process.rs#sys_sigreturn` | 同板块同名 | Err, Ok, assert!, core::mem::size_of::<crate::task::LinuxSigInfo>, core::mem::size_of::<usize>, current_task ...(+8) | (trap_cx as *mut TrapContext).cast::<MachineContext>, copy_from_user, copy_from_user(
        token,
        (ucontext_addr
            + 2 * size_of::<usize>()
            + size_of::<SignalStack>()
            + size_of::<Signals>()
            + crate::hal::UserContext::PADDING_SIZE) as *mut MachineContext,
        (trap_cx as *mut TrapContext).cast::<MachineContext>(),
    )
    .unwrap, current_task, current_task().unwrap, info! ...(+9) |
| `os/src/syscall/process.rs#sys_clock_nanosleep` | `os/src/syscall/process.rs#sys_clock_nanosleep` | 同板块同名 | Err, Ok, Time::now, Time::now().to_nsec, current_user_token, has_pending_signals ...(+6) | Arc::downgrade, TimeSpec::new, TimeSpec::now, assert!, block_current_and_run_next, copy_to_user ...(+13) |
| `os/src/syscall/process.rs#sys_sigaction` | `os/src/syscall/process.rs#sys_sigaction` | 同板块同名 | Err, Ok, SigAction::ignore, SigAction::new, SignalFlags::empty, SignalFlags::from_bits ...(+12) | sigaction, trace! |
| `os/src/syscall/process.rs#sys_tgkill` | `os/src/syscall/process.rs#sys_tgkill` | 同板块同名 | Err, Ok, SignalFlags::from_bits, current_task, current_task().unwrap, current_task().unwrap().gettid ...(+5) | Signals::from_signum, drop, find_task_by_tgid, inner.add_signal, signal.is_empty, task.acquire_inner_lock ...(+2) |

#### 任务调度/异步运行时
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `os/src/syscall/fs.rs#sys_statx` | `os/src/syscall/fs.rs#sys_statx` | 同板块同名 | AtFlags::from_bits_truncate, Ok, current_task, current_task().unwrap, current_user_token, dentry.get_inode ...(+13) | FstatatFlags::from_bits, copy_to_user, copy_to_user(token, &file_descriptor.get_statx(mask), buf as *mut Statx).is_err, current_task, current_task().unwrap, current_user_token ...(+13) |
| `os/src/syscall/socket.rs#sys_bind` | `os/src/syscall/net.rs#sys_bind` | 同板块同名 | Arc::new, Err, Fd::new, FdFlags::empty, NET_SERVER.clone, Ok ...(+19) | FileDescriptor::new, address::listen_endpoint, address::listen_endpoint(addr_buf).unwrap, current_task, current_task().unwrap, current_task().unwrap().files.lock ...(+14) |
| `os/src/syscall/socket.rs#sys_recvfrom` | `os/src/syscall/net.rs#sys_recvfrom` | 同板块同名 | Err, Ok, addr.is_null, buffer.len, buffer[..rlen].copy_from_slice, core::cmp::min ...(+20) | Some, core::slice::from_raw_parts_mut, current_task, current_task().unwrap, current_task().unwrap().files.lock, current_task().unwrap().files.lock().get_ref ...(+11) |
| `os/src/syscall/socket.rs#sys_sendto` | `os/src/syscall/net.rs#sys_sendto` | 同板块同名 | Ipv4Addr::new, Ok, SocketAddrV4::new, Some, addr.port, addr_ptr.is_null ...(+19) | IpListenEndpoint::from, SocketAddrv4::new, Some, [0; 16].as_slice, current_task, current_task().unwrap ...(+13) |

#### 内存管理
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `os/src/mm/memory_set.rs#map_elf` | `os/src/mm/memory_set.rs#map_elf` | 同板块同名 | ((ph.virtual_addr() + ph.mem_size()) as usize + offset).into, (ph.virtual_addr() as usize + offset).into, MapArea::new, Some, assert_eq!, elf.program_header ...(+16) | ((ph.virtual_addr() + ph.mem_size()) as usize + bias).into, (VirtAddr::from(elf.input.as_ptr() as usize + (ph.offset() as usize)))
                                .floor, (ph.virtual_addr() as usize + bias).into, Err, KERNEL_SPACE
                        .lock, KERNEL_SPACE
                        .lock()
                        .remove_area_with_start_vpn ...(+44) |
| `easy-fs/src/efs.rs#create` | `os/src/fs/dev/hwclock.rs#create` | 同板块同名 | Arc::clone, Arc::new, Bitmap::new, Mutex::new, assert_eq!, block_cache_sync_all ...(+13) | todo! |
| `os/src/drivers/block/pci_virtio_blk.rs#enumerate_pci` | `os/src/drivers/block/virtio_blk_pci.rs#enumerate_pci` | 同板块同名 | PciRangeAllocator::new, PciRoot::new, PciTransport::new::<VirtioHal>, PciTransport::new::<VirtioHal>(&mut pci_root, device_function).unwrap, Some, bar_info.takes_two_entries ...(+10) | MmioCam::new, PciRangeAllocator::new, PciRoot::new, PciTransport::new::<VirtioHal, MmioCam>, PciTransport::new::<VirtioHal, MmioCam>(&mut pci_root, device_function).unwrap, Some ...(+10) |
| `os/src/mm/memory_set.rs#munmap` | `os/src/mm/memory_set.rs#munmap` | 同板块同名 | Ok, VPNRange::new, VPNRange::new(start, end, start.into(), end.into()).into_iter, VPNRange::new(start, end, start.into(), end.into()).into_iter().for_each, VirtAddr::from, VirtPage::from ...(+22) | Err, Ok, Some, Vec::new, VirtAddr::from, area.check_overlapping ...(+21) |

#### 中断/异常处理
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `arch/src/aarch64/trap.rs#init_interrupt` | `os/src/hal/arch/riscv/mod.rs#bootstrap_init` | 同板块同结构 | 内部闭环逻辑 | 内部闭环逻辑 |
| `arch/src/riscv64/interrupt.rs#enable_external_irq` | `os/src/hal/arch/riscv/trap/mod.rs#enable_timer_interrupt` | 同板块同结构 | sie::set_sext | sie::set_stimer |
| `lose-net-stack/src/connection/tcp.rs#interrupt` | `os/src/hal/arch/loongarch64/trap/mod.rs#trap_from_kernel` | 同模块上下文对齐/差异观察 | data.len, data.to_vec, drop, flags.contains, self.datas.lock, self.datas.lock().push_back ...(+9) | ((addr + i) as *mut u8).read_unaligned, ((addr + i) as *mut u8).write_unaligned, (0..sz).rev, BadV::read, BadV::read().get_vaddr, EStat::read ...(+14) |
| `arch/src/x86_64/interrupt.rs#kernelvec` | `os/src/hal/arch/loongarch64/trap/mem_access.rs#get_addr` | 同模块上下文对齐/差异观察 | asm! | self.get_op_code, self.get_op_code().map, self.get_rj_num, self.get_rk_num, self.get_si12, self.get_si14 |

#### 系统调用
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `user/src/syscall.rs#syscall` | `user/src/syscall.rs#syscall` | 同板块同名 | asm! | __syscall, core::arch::asm!, unimplemented! |
| `user/src/lib.rs#waitpid` | `user/src/usr_call.rs#waitpid` | 同板块同名 | sys_waitpid, yield_ | sys_waitpid |
| `user/src/lib.rs#wait` | `user/src/usr_call.rs#wait` | 同板块同名 | sys_waitpid, yield_ | sys_waitpid |
| `user/src/syscall.rs#sys_getpid` | `user/src/syscall.rs#sys_getpid` | 同板块同名 | assert!, syscall | syscall |

#### 文件系统
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `virtio-drivers/src/device/blk.rs#flush` | `os/src/drivers/serial/ns16550a.rs#flush` | 同板块同名 | (BlkFeature::RING_INDIRECT_DESC | BlkFeature::FLUSH).bits, Arc::new, BlkResp {
                            status: RespStatus::OK,
                        }
                        .as_bytes, Default::default, Mutex::new, NonNull::from ...(+19) | Err, Ok, read_volatile |
| `os/src/syscall/fs.rs#sys_mount` | `os/src/syscall/fs.rs#sys_mount` | 同板块同名 | BLOCK_DEVICE.get, BLOCK_DEVICE.get().unwrap, BLOCK_DEVICE.get().unwrap().clone, Err, FILE_SYSTEMS.lock, FILE_SYSTEMS.lock().find_fs ...(+12) | MountFlags::from_bits, MountFlags::from_bits(mountflags).unwrap, current_user_token, filesystemtype.is_null, info!, source.is_null ...(+3) |
| `os/src/fs/pipe.rs#make_pipe` | `os/src/fs/dev/pipe.rs#make_pipe` | 同板块同名 | Arc::new, Mutex::new, Pipe::read_end_with_buffer, Pipe::write_end_with_buffer, PipeDentry::new, PipeInode::new ...(+9) | Arc::new, Mutex::new, Pipe::read_end_with_buffer, Pipe::write_end_with_buffer, PipeRingBuffer::new, buffer.clone ...(+3) |
| `os/src/mm/memory_set.rs#from_another` | `os/src/mm/map_area.rs#from_another` | 同板块同名 | BTreeMap::new, VPNRange::new, another.map_file.clone, another.mmap_flag.clone, another.vpn_range.get_end, another.vpn_range.get_end_addr ...(+2) | LinearMap::new, VPNRange::new, another.inner.vpn_range.get_end, another.inner.vpn_range.get_start, another.map_file.clone |

#### 设备驱动
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `os/src/drivers/block/mod.rs#block_device_test` | `os/src/drivers/block/mod.rs#block_device_test` | 同板块同名 | BLOCK_DEVICE.clone, assert_eq!, block_device.read_block, block_device.write_block, println!, write_buffer.iter_mut | BLOCK_DEVICE.clone, assert_eq!, block_device.read_block, block_device.write_block, println!, write_buffer.iter_mut |
| `arch/src/aarch64/pl011.rs#console_putchar` | `os/src/hal/arch/loongarch64/sbi.rs#console_putchar` | 同板块同名 | UART.lock, uart.putchar | UART.write |
| `os/src/drivers/block/pci_virtio_blk.rs#read_block` | `os/src/drivers/block/virtio_blk.rs#read_block` | 同板块同名 | self.0
            .lock, self.0
            .lock()
            .read_blocks, self.0
            .lock()
            .read_blocks(block_id, buf)
            .expect | assert!, buf.chunks_mut, buf.chunks_mut(VIRT_IO_BLOCK_SZ).enumerate, self.0
                .lock, self.0
                .lock()
                .read_blocks, self.0
                .lock()
                .read_blocks(virtio_block_id as usize, chunk)
                .expect |
| `os/src/drivers/block/virtio_blk.rs#read_block` | `os/src/drivers/block/virtio_blk_pci.rs#read_block` | 同板块同名 | self.0
            .lock, self.0
            .lock()
            .read_blocks, self.0
            .lock()
            .read_blocks(block_id, buf)
            .expect | assert!, buf.chunks_mut, buf.chunks_mut(VIRT_IO_BLOCK_SZ).enumerate, self.0.lock, self.0.lock().read_blocks, self.0.lock().read_blocks(virtio_block_id, chunk).expect |

#### 网络协议栈
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `lose-net-stack/src/connection/tcp.rs#readable` | `os/src/net/udp.rs#readable` | 同板块同名 | Ok, self.clients.lock, self.clients.lock()[0].readable, self.is_client.read, self.is_client.read().clone, self.wait_queue.lock ...(+1) | todo! |
| `lose-net-stack/src/connection/udp.rs#readable` | `os/src/net/unix.rs#readable` | 同板块同名 | Ok, self.inner.lock, self.inner.lock().packets.len | todo! |
| `lose-net-stack/src/net_trait.rs#readable` | `os/src/net/tcp.rs#readable` | 同板块同名 | Err | 内部闭环逻辑 |
| `os/src/socket.rs#writable` | `os/src/net/tcp.rs#writable` | 同板块同名同结构 | 内部闭环逻辑 | 内部闭环逻辑 |

#### 文件系统/第三方库
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `ext4_rs-1.3.1/src/ext4_impls/extents.rs#extent_remove_space` | `os/src/fs/ext4/extent.rs#extent_remove_space` | 同板块同名 | Block::load, Ext4Extent::default, Ok, ex.get_actual_len, ex.get_first_block, ex.get_pblock ...(+14) | Block::load_offset, Ext4Extent::default, Ok, ex.get_actual_len, ex.get_first_block, ex.get_pblock ...(+14) |
| `ext4_rs-1.3.1/src/ext4_impls/extents.rs#ext_remove_leaf` | `os/src/fs/ext4/extent.rs#ext_remove_leaf` | 同板块同名 | Block::load, Block::load_inode_root_block, Ext4Extent::default, Ok, ex.get_actual_len, ex.get_pblock ...(+14) | Block::load_inode_root_block, Block::load_offset, Ext4Extent::default, Ok, ex.get_actual_len, ex.get_pblock ...(+14) |
| `ext4_rs-1.3.1/src/ext4_impls/balloc.rs#balloc_alloc_block` | `os/src/fs/ext4/balloc.rs#balloc_alloc_block` | 同板块同名 | Block::load, Ext4BlockGroup::load_new, Ok, block_group.get_block_bitmap_block, block_group.get_free_blocks_count, block_group.set_block_group_balloc_bitmap_csum ...(+16) | Block::load_offset, Err, Ext4BlockGroup::load_new, Ok, block_group
                        .set_block_group_balloc_bitmap_csum, block_group.get_block_bitmap_block ...(+17) |
| `ext4_rs-1.3.1/src/ext4_impls/extents.rs#insert_extent` | `os/src/fs/ext4/extent.rs#insert_extent` | 同板块同名 | Ok, inode_ref.inode.root_extent_mut_at, self.can_merge, self.create_new_leaf, self.find_extent, self.get_extent_from_node ...(+2) | Ok, inode_ref.inode.root_extent_mut_at, self.can_merge, self.create_new_leaf, self.find_extent, self.get_extent_from_node ...(+2) |

#### 网络协议栈/第三方库
仅参考项目中观察到该模块代表函数，目标项目没有同模块可对齐证据：
- 参考项目：`dependency/dep_iso/src/net/ethernet/intel/ixgbe.rs#new`，调用摘要：(IXGBE_FCRTH..IXGBE_FCRTH_END).step_by, (IXGBE_FCRTL..IXGBE_FCRTL_END).step_by, (IXGBE_FCTTV..IXGBE_FCTTV_END).step_by, EthernetAddress::from_bytes, MaybeUninit::uninit, MaybeUninit::uninit().assume_init ...(+56)
- 参考项目：`dependency/virtio-drivers/src/device/socket/connectionmanager.rs#send_recv`，调用摘要："Hello from guest".as_bytes, (hello_from_guest.len() as u32).into, (hello_from_host.len() as u32).into, 0.into, 50.into, Arc::new ...(+43)
- 参考项目：`dependency/virtio-drivers/src/device/socket/connectionmanager.rs#incoming_connection`，调用摘要：0.into, 50.into, Arc::new, Mutex::new, ReadOnly::new, SocketType::Stream.into ...(+24)
- 参考项目：`dependency/dep_iso/src/net/ethernet/intel/e1000.rs#new`，调用摘要：P::alloc_dma, Vec::with_capacity, assert_eq!, debug!, e1000[E1000_ICR].read, e1000[E1000_ICR].write ...(+25)

#### 第三方库
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `virtio-drivers/src/transport/mmio.rs#vendor_id` | `dependency/virtio-drivers/src/transport/mmio.rs#vendor_id` | 同板块同名 | volread! | field_shared!, field_shared!(self.header, vendor_id).read |

#### 通用模块/其他
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `arch/src/loongarch64/boot.rs#_start` | `user/src/lib.rs#_start` | 同板块同名 | core::arch::asm! | _parameter, asm!, core::arch::asm!, unreachable! |
| `user/src/lib.rs#_start` | `os/src/hal/arch/loongarch64/boot.rs#_start` | 同板块同名 | ((argv + i * core::mem::size_of::<usize>()) as *const usize).read_volatile, ((str_start + *i) as *const u8).read_volatile, (0usize..)
            .find, (0usize..)
            .find(|i| unsafe { ((str_start + *i) as *const u8).read_volatile() == 0 })
            .unwrap, HEAP.lock, HEAP.lock()
            .init ...(+9) | naked_asm! |
| `time/src/lib.rs#partial_cmp` | `os/src/timer.rs#partial_cmp` | 同板块同名 | Some | Some, self.cmp |
| `time/src/lib.rs#partial_cmp` | `os/src/timer.rs#partial_cmp` | 同板块同名 | Some | Some, self.cmp |


# 最终同源性定性仲裁裁决书

**案号**: 2026-T2026106149910802-XX vs 2025-NPUcore-BLOSSOM-西北工业大学
**审计模块**: 全模块综合仲裁
**裁决日期**: 2026年

---

## 1. 宏观判定

- **定性结论**: **独立开发，底层架构显著差异**
- **客观 AST 相似度**: 11.17%
- **双向覆盖率**: 目标项目 22.78%，参考项目 17.97%
- **共同 AST 指纹**: 423
- **唯一 AST 指纹**: 目标项目 1857，参考项目 2354

**裁决依据**: 全模块双向覆盖率均低于 50% 的实质性相似阈值。共同 AST 指纹仅 423 个，且大量集中于第三方开源库（ext4_rs）的公共代码。在操作系统核心模块（进程管理、内存管理、任务调度）中，双方采用了不同的抽象体系、同步原语和内存模型，属于同一功能目标下的独立实现路径。

---

## 2. 全模块覆盖矩阵解读

### 进程管理
- **双方均存在**: 是
- **共同 AST 指纹**: 6 个
- **覆盖率**: 目标 6.06%，参考 8.82%
- **证据强度**: **弱**

**解读**: 共同指纹均为单句包装函数（`take_current`、`current_task`、`is_zombie` 等），属于 Rust `Option<Arc<T>>` 模式下的唯一合理实现。信号处理子系统（`sys_sigreturn`、`sys_tgkill` 等）的底层调用图谱存在实质性差异——目标项目使用 `SignalFlags::from_bits` + `safe_translated_ref` 体系，参考项目使用 `Signals::from_signum` + `copy_to_user` 体系，证明双方在信号抽象层独立设计。

### 任务调度/异步运行时
- **双方均存在**: 是
- **共同 AST 指纹**: 8 个
- **覆盖率**: 目标 4.47%，参考 6.15%
- **证据强度**: **弱**

**解读**: `run_tasks` 调度循环骨架（`lock → fetch_task → get_idle_task_cx_ptr → switch`）是操作系统理论的必然结果，不构成独创性表达。关键差异在于：目标项目使用 `context_switch_pt`（暗示页表切换）和 `check_futex_timer`（futex 超时集成），参考项目使用 `__switch`（纯上下文切换）和 `do_wake_expired`（独立超时队列），反映不同的调度器设计哲学。

### 内存管理
- **双方均存在**: 是
- **共同 AST 指纹**: 25 个
- **覆盖率**: 目标 6.79%，参考 5.84%
- **证据强度**: **中（部分函数）**

**解读**: `map_elf` 函数的 ELF 加载流程存在结构相似性，但这是 ELF 标准格式解析的必然路径。目标项目使用 `MapArea::new` + `assert_eq!` 进行段映射，参考项目使用 `KERNEL_SPACE.lock().remove_area_with_start_vpn` 进行内核空间管理，内存分配策略不同。`munmap` 函数中，目标项目使用 `VPNRange::new` 迭代器模式，参考项目使用 `Vec::new` + `check_overlapping` 收集模式，实现路径异构。

### 中断/异常处理
- **双方均存在**: 是
- **共同 AST 指纹**: 2 个
- **覆盖率**: 目标 5.26%，参考 5.0%
- **证据强度**: **未形成**

**解读**: 共同指纹仅涉及架构相关的 CSR 寄存器操作（`sie::set_sext` vs `sie::set_stimer`），属于 RISC-V 特权架构的强制接口。目标项目支持 aarch64/riscv64/x86_64/loongarch64 四架构，参考项目以 loongarch64/riscv 为主，中断控制器抽象层完全不同。

### 系统调用
- **双方均存在**: 是
- **共同 AST 指纹**: 15 个
- **覆盖率**: 目标 31.25%，参考 28.85%
- **证据强度**: **弱**

**解读**: 覆盖率相对较高，但原因在于系统调用函数名由 Linux ABI 强制一致（`sys_clone`、`sys_nanosleep`、`sys_getpid` 等）。用户态包装函数（`waitpid`、`wait`、`sigprocmask`）均为 libc 标准接口的薄封装，调用 `sys_waitpid` 或 `sys_sigprocmask` 是唯一实现方式。`syscall` 函数的 `asm!` 宏调用是系统调用陷入指令的必然形式。

### 文件系统
- **双方均存在**: 是
- **共同 AST 指纹**: 5 个
- **覆盖率**: 目标 1.44%，参考 1.34%
- **证据强度**: **未形成**

**解读**: 覆盖率极低，证明双方文件系统实现完全独立。目标项目采用 `vfs` + `procfs` + `ext4_rs` 独立 crate 架构，参考项目采用内置 `os/src/fs/ext4` 实现。`sys_mount` 函数中，目标项目使用 `BLOCK_DEVICE.get().unwrap().clone` + `FILE_SYSTEMS.lock().find_fs`，参考项目使用 `MountFlags::from_bits` + `current_user_token`，挂载流程完全不同。

### 设备驱动
- **双方均存在**: 是
- **共同 AST 指纹**: 2 个
- **覆盖率**: 目标 0.54%，参考 4.26%
- **证据强度**: **未形成**

**解读**: `block_device_test` 的读写测试流程相似，但这是块设备接口测试的通用范式。VirtIO 块设备驱动中，目标项目使用 `pci_virtio_blk`（PCI 传输层），参考项目使用 `virtio_blk_pci`（不同抽象层），且 `read_block` 实现中参考项目包含 `buf.chunks_mut(VIRT_IO_BLOCK_SZ).enumerate` 的分块逻辑，目标项目直接调用 `read_blocks`，实现细节不同。

### 网络协议栈
- **双方均存在**: 是
- **共同 AST 指纹**: 1 个
- **覆盖率**: 目标 2.08%，参考 2.13%
- **证据强度**: **未形成**

**解读**: 目标项目使用独立 `lose-net-stack` crate，参考项目使用内置 `os/src/net` 实现。`readable`/`writable` 函数中，参考项目大量标记为 `todo!`，目标项目有完整实现，证明双方开发进度和实现路径不同。

### 第三方库/公共依赖
- **双方均存在**: 是
- **共同 AST 指纹**: 124 个（文件系统/第三方库）
- **覆盖率**: 目标 42.03%，参考 42.91%
- **证据强度**: **弱（第三方库共性）**

**解读**: 高覆盖率集中于 `ext4_rs` 库的 `extents.rs`、`balloc.rs` 等文件。这是双方均引用的**同一开源第三方库**（ext4_rs-1.3.1），属于公共依赖，不构成抄袭证据。参考项目另有 1228 个第三方库函数（`dependency/virtio-drivers`、`dependency/dep_iso`），目标项目仅 1 个，证明双方第三方库选型策略不同。

---

## 3. 底层 AST 关键证据指纹表

| 所属板块 | 目标项目路径 | 参考项目路径 | 证据类型 | 技术说明 |
| :--- | :--- | :--- | :--- | :--- |
| 进程管理 | `os/src/task/processor.rs#take_current` | `os/src/task/processor.rs#take_current` | 弱信号-通用范式 | `Option.take()` 是 Rust 标准惯用法，无其他合理实现 |
| 进程管理 | `os/src/task/processor.rs#current_task` | `os/src/task/processor.rs#current_task` | 弱信号-通用范式 | 全局单例加锁访问是 `lazy_static` 模式的标准写法 |
| 进程管理 | `os/src/syscall/process.rs#sys_sigreturn` | `os/src/syscall/process.rs#sys_sigreturn` | 弱信号-架构差异 | 目标用 `LinuxSigInfo`+`size_of`，参考用 `MachineContext`+`SignalStack`，信号帧布局不同 |
| 进程管理 | `os/src/syscall/process.rs#sys_clone` | `os/src/syscall/process.rs#sys_clone` | 弱信号-ABI强制 | `clone` 系统调用名由 Linux ABI 强制，但目标用 `current_task.fork`，参考用 `CloneFlags::from_bits(flags & !0xff)` |
| 任务调度 | `os/src/task/processor.rs#run_tasks` | `os/src/task/processor.rs#run_tasks` | 弱信号-架构差异 | 调度循环骨架是理论必然，但目标用 `context_switch_pt`+`check_futex_timer`，参考用 `__switch`+`do_wake_expired` |
| 内存管理 | `os/src/mm/memory_set.rs#map_elf` | `os/src/mm/memory_set.rs#map_elf` | 中信号-ELF标准 | ELF 加载流程由标准强制，但目标用 `MapArea::new`，参考用 `KERNEL_SPACE.lock().remove_area_with_start_vpn` |
| 内存管理 | `os/src/mm/memory_set.rs#munmap` | `os/src/mm/memory_set.rs#munmap` | 中信号-实现异构 | 目标用 `VPNRange::new` 迭代器，参考用 `Vec::new`+`check_overlapping` 收集模式 |
| 系统调用 | `user/src/syscall.rs#syscall` | `user/src/syscall.rs#syscall` | 弱信号-ABI强制 | `asm!` 宏是系统调用陷入的唯一实现方式 |
| 系统调用 | `user/src/lib.rs#waitpid` | `user/src/usr_call.rs#waitpid` | 弱信号-libc标准 | libc 标准接口的薄封装，调用 `sys_waitpid` 是唯一路径 |
| 文件系统 | `os/src/fs/pipe.rs#make_pipe` | `os/src/fs/dev/pipe.rs#make_pipe` | 弱信号-通用模式 | 管道创建模式相似，但目标用 `PipeDentry::new`，参考用 `PipeRingBuffer::new` |
| 设备驱动 | `os/src/drivers/block/mod.rs#block_device_test` | `os/src/drivers/block/mod.rs#block_device_test` | 弱信号-测试范式 | 块设备读写测试是通用测试模式 |
| 第三方库 | `ext4_rs-1.3.1/src/ext4_impls/extents.rs#extent_remove_space` | `os/src/fs/ext4/extent.rs#extent_remove_space` | 弱信号-公共依赖 | 双方均使用 ext4_rs 开源库，属于公共第三方代码 |
| 第三方库 | `ext4_rs-1.3.1/src/ext4_impls/balloc.rs#balloc_alloc_block` | `os/src/fs/ext4/balloc.rs#balloc_alloc_block` | 弱信号-公共依赖 | 同上，ext4 块分配算法的开源实现 |

---

## 4. 核心代码调用流对比剖析

### 【进程管理/信号处理路径】

**底层技术析理**: 
目标项目的信号处理基于 `SignalFlags` 位标志 + `safe_translated_ref` 用户态内存安全访问抽象。`sys_sigreturn` 通过 `core::mem::size_of::<LinuxSigInfo>` 计算信号帧大小，信号队列使用 `inner.signal_queue.push` 直接操作。参考项目基于 `Signals` 枚举 + `copy_to_user`/`copy_from_user` 传统用户态拷贝接口。`sys_sigreturn` 通过 `TrapContext` 指针偏移 + `MachineContext` 结构体进行上下文恢复，涉及 `SignalStack`、`Signals`、`UserContext::PADDING_SIZE` 等复杂布局计算。两套信号抽象体系在类型系统、内存安全策略、上下文恢复机制上完全不同，无法通过简单改名实现转换。

**代码溯源链接**:
- 目标项目: `os/src/syscall/process.rs#sys_sigreturn`
- 参考项目: `os/src/syscall/process.rs#sys_sigreturn`

---

### 【任务调度/主循环路径】

**底层技术析理**:
目标项目 `run_tasks` 使用 `context_switch_pt` 进行上下文切换（暗示页表切换逻辑），集成 `crate::timer::check_futex_timer` 进行 futex 超时检查，使用 `log_info!` 进行调试输出。参考项目使用 `__switch` 进行纯上下文切换，使用 `do_wake_expired` 进行独立超时队列唤醒，使用 `task.acquire_inner_lock` 进行任务锁管理。两者在上下文切换机制（页表切换 vs 纯寄存器切换）、定时器集成方式（futex 内联 vs 独立超时队列）、调试基础设施（`log_info!` vs 无日志）上存在实质性架构差异。

**代码溯源链接**:
- 目标项目: `os/src/task/processor.rs#run_tasks`
- 参考项目: `os/src/task/processor.rs#run_tasks`

---

### 【内存管理/ELF 加载路径】

**底层技术析理**:
目标项目 `map_elf` 使用 `MapArea::new` 直接创建映射区域，通过 `assert_eq!` 进行对齐断言，使用 `elf.program_header` 迭代 ELF 段。参考项目使用 `KERNEL_SPACE.lock().remove_area_with_start_vpn` 进行内核空间区域管理，通过 `VirtAddr::from(elf.input.as_ptr() as usize + (ph.offset() as usize)).floor` 进行地址对齐，涉及内核空间锁机制。两者在内存区域管理策略（直接创建 vs 先移除后创建）、地址对齐方式（断言 vs 位运算）、内核空间并发控制（无锁 vs 有锁）上存在不同设计选择。

**代码溯源链接**:
- 目标项目: `os/src/mm/memory_set.rs#map_elf`
- 参考项目: `os/src/mm/memory_set.rs#map_elf`

---

### 【系统调用/用户态陷入路径】

**底层技术析理**:
目标项目 `syscall` 函数直接使用 `asm!` 宏内联汇编进行系统调用陷入。参考项目使用 `__syscall` 辅助函数 + `core::arch::asm!` + `unimplemented!` 宏进行分派。两者在系统调用分派机制（直接陷入 vs 辅助函数分派）、未实现系统调用处理（无处理 vs `unimplemented!` 标记）上存在差异。用户态 `waitpid`/`wait` 函数均为 libc 标准接口的薄封装，调用 `sys_waitpid` 是唯一实现路径，不构成独创性表达。

**代码溯源链接**:
- 目标项目: `user/src/syscall.rs#syscall`
- 参考项目: `user/src/syscall.rs#syscall`

---

## 5. 单方模块与降权项

### 仅参考项目存在的模块
- **网络协议栈/第三方库**（138 个函数）: 参考项目包含 `dependency/dep_iso/src/net/ethernet/intel/ixgbe.rs`、`dependency/virtio-drivers/src/device/socket/connectionmanager.rs` 等独立网络驱动栈，目标项目无此模块。这证明双方第三方库选型和网络栈架构策略完全不同。

### 降权项说明
以下证据类型因不具备独创性或属于公共领域知识，降权为弱信号或不作为同源证据：

1. **Linux ABI 强制接口**: `sys_clone`、`sys_nanosleep`、`sys_getpid`、`sys_gettid`、`sys_exit`、`sys_exit_group`、`sys_yield` 等系统调用名由 Linux ABI 强制规定，任何兼容 Linux 的操作系统必须使用这些名称。同名不等于同实现。

2. **Rust 标准库惯用法**: `Option.take()`、`Option.as_ref().map()`、`Mutex::lock()`、`Arc::clone()`、`Arc::new()` 等是 Rust 标准库的核心 API，在操作系统开发中只有唯一合理调用方式。

3. **libc 标准接口薄封装**: `waitpid`、`wait`、`sigprocmask` 等用户态函数是 POSIX 标准接口的薄封装，调用对应的 `sys_*` 系统调用是唯一实现路径。

4. **第三方开源库公共代码**: `ext4_rs-1.3.1` 是双方均引用的开源 ext4 文件系统库，其 `extents.rs`、`balloc.rs` 等文件的高 AST 相似度属于公共依赖，不构成抄袭证据。

5. **架构强制指令**: `asm!`、`naked_asm!`、`sie::set_sext`、`sie::set_stimer` 等是 CPU 架构强制指令或 CSR 寄存器操作，无其他实现方式。

6. **通用测试范式**: `block_device_test` 的读写验证流程、`run_basic_tests` 的测试用例执行流程是软件测试的通用模式。

7. **操作系统理论必然结构**: 调度循环骨架（`lock → fetch_task → switch`）、进程状态机（`is_zombie` 判断）、初始进程注入（`add_initproc`）是操作系统理论的必然实现路径。

---

## 6. 保守结论

基于全模块客观 AST 证据，目标项目 `[2026-T2026106149910802-XX]` 与参考项目 `[2025-NPUcore-BLOSSOM-西北工业大学]` 的整体相似度仅 11.17%，双向覆盖率均低于 23%，远未达到实质性相似的判定阈值。共同 AST 指纹 423 个，大量集中于双方均引用的开源第三方库 `ext4_rs`，属于公共依赖而非代码复用。

在操作系统核心模块中，双方展现出显著的架构差异：进程管理采用不同的信号抽象体系（`SignalFlags` vs `Signals`）和用户态内存访问策略（`safe_translated_ref` vs `copy_to_user`）；任务调度集成不同的定时器机制（futex 内联 vs 独立超时队列）和上下文切换路径（`context_switch_pt` vs `__switch`）；内存管理使用不同的区域管理策略和地址对齐方式。这些差异无法通过简单的函数改名或接口替换实现，证明双方是基于不同的设计哲学独立开发。

控方指控的“100% 镜像复用”函数（`take_current`、`current_task`、`is_zombie` 等）均为 Rust 标准库惯用法下的单句包装函数，在 `Option<Arc<T>>` 模式下只有唯一合理实现，不具备著作权法意义上的独创性表达。`run_tasks` 调度循环骨架的相似性是操作系统理论的必然结果，而非代码抄袭的证据。

**综合裁定**: 无充分证据支持代码复用或抄袭指控。双方项目属于同一功能目标（兼容 Linux ABI 的操作系统内核）下的独立实现，底层架构存在显著差异。建议驳回控方全部指控，不支持对 git commit 历史及汇编代码的强制取证请求。

---

------------------------------

