# 🛡️ OS 内核赛自动化代码级架构审计报告

**审计时间**: `2026-07-02 15:08:10`
**参赛作品名称**: `2026-T2026102599911006-XX`
**年份**: `2026`
**学校**: `XX`
**队伍名称**: `T2026102599911006`
**比赛名字**: `OS-Competition`
**比赛子赛道**: `Kernel-Implementation`
**仓库地址**: `https://gitlab.eduxiji.net/educg-group-43501-3132633/T2026102599911006-429`
**推测开发语言**: `Rust / C (由AST探针自动识别)`
**特征函数量**: `332`

## ⚔️ 底层代码同源性对比 (AST Homology)
> 本部分由专门的代码审查智能体，对该项目与符合年份条件的历届最高相似度项目进行控制流与底层调用对比。新版报告会先给出确定性的全模块覆盖矩阵，再给出 Agent 的分模块解释，避免只围绕少量抽样函数展开。

### 🏆 Top 3 底层对标: vs `2024-你起吧-长春理工大学`（长春理工大学 / 你起吧）

#### 对方作品信息

**对方年份**: `2024`
**对方学校**: `长春理工大学`
**对方队伍名称**: `你起吧`
**对方比赛名字**: `OS-Competition`
**对方比赛子赛道**: `Kernel-Implementation`
**对方仓库地址**: `https://gitlab.eduxiji.net/educg-group-26010-2376550/T202410186992540-1599`

> 双向覆盖率：共同 AST 指纹: 113；唯一 AST 指纹: 目标项目 289，参考项目 573；覆盖率: 目标项目 39.1%，参考项目 19.72%

## 全模块对比总览（确定性证据）

> 总体覆盖率：共同 AST 指纹: 113；唯一 AST 指纹: 目标项目 289，参考项目 573；覆盖率: 目标项目 39.1%，参考项目 19.72%

### 1. 模块覆盖矩阵
| 模块 | 目标函数数 | 参考函数数 | 共同唯一 AST 指纹 | Target 覆盖 | Base 覆盖 | 状态 |
| :--- | ---: | ---: | ---: | ---: | ---: | :--- |
| 进程管理 | 28 | 37 | 18 | 64.29% | 54.55% | 双方存在且有结构重合 |
| 任务调度/异步运行时 | 24 | 22 | 5 | 20.83% | 22.73% | 双方存在且有结构重合 |
| 内存管理 | 88 | 140 | 30 | 34.48% | 22.73% | 双方存在且有结构重合 |
| 中断/异常处理 | 8 | 9 | 7 | 87.5% | 77.78% | 双方存在且有结构重合 |
| 系统调用 | 28 | 3 | 2 | 28.57% | 100.0% | 双方存在且有结构重合 |
| 文件系统 | 59 | 67 | 22 | 42.31% | 34.38% | 双方存在且有结构重合 |
| 设备驱动 | 15 | 14 | 5 | 33.33% | 35.71% | 双方存在且有结构重合 |
| 网络协议栈 | 0 | 0 | 0 | 0.0% | 0.0% | 双方均未体现 |
| 文件系统/第三方库 | 12 | 323 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |
| 通用模块/其他 | 70 | 71 | 24 | 37.5% | 39.34% | 双方存在且有结构重合 |

### 2. 分模块证据清单

#### 进程管理
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `kernel/proc.c#scheduler` | `kernel/proc.c#scheduler` | 同板块同名 | acquire, intr_off, intr_on, mycpu, release, swtch | acquire, intr_on, mycpu, release, swtch |
| `kernel/proc.c#procdump` | `kernel/proc.c#procdump` | 同板块同名同结构 | NELEM, printf | NELEM, printf |
| `kernel/proc.c#sleep` | `kernel/proc.c#sleep` | 同板块同名同结构 | acquire, myproc, release, sched | acquire, myproc, release, sched |
| `kernel/proc.c#sched` | `kernel/proc.c#sched` | 同板块同名 | holding, intr_get, mycpu, myproc, panic, swtch | holding, intr_get, mycpu, myproc, panic, swtch |

#### 任务调度/异步运行时
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `kernel/virtio_disk.c#virtio_disk_intr` | `kernel/virtio_disk.c#virtio_disk_intr` | 同板块同名 | R, __atomic_thread_fence, acquire, panic, release, wakeup | R, __sync_synchronize, acquire, free_chain, panic, release ...(+1) |
| `kernel/console.c#consoleintr` | `kernel/console.c#consoleintr` | 同板块同名同结构 | C, acquire, consputc, procdump, release, wakeup | C, acquire, consputc, procdump, release, wakeup |
| `kernel/console.c#consoleread` | `kernel/console.c#consoleread` | 同板块同名同结构 | C, acquire, either_copyout, killed, myproc, release ...(+1) | C, acquire, either_copyout, killed, myproc, release ...(+1) |
| `kernel/trap.c#kerneltrap` | `kernel/trap.c#kerneltrap` | 同板块同名 | devintr, intr_get, myproc, panic, printf, r_scause ...(+6) | devintr, intr_get, myproc, panic, printf, r_scause ...(+6) |

#### 内存管理
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `kernel/virtio_disk.c#virtio_disk_init` | `kernel/virtio_disk.c#virtio_disk_init` | 同板块同名 | R, initlock, kalloc, memset, panic, probe_virtio | R, initlock, memset, panic |
| `kernel/file.c#filewrite` | `kernel/file.c#filewrite` | 同板块同名同结构 | begin_op, devsw[f->major].write, end_op, ilock, iunlock, panic ...(+2) | begin_op, devsw[f->major].write, end_op, ilock, iunlock, panic ...(+2) |
| `kernel/vm.c#kvmmake` | `kernel/vm.c#kvmmake` | 同板块同名 | kalloc, kvmmap, memset, proc_mapstacks | kalloc, kvmmap, memset, proc_mapstacks |
| `kernel/proc.c#allocproc` | `src/task.rs#allocproc` | 同板块同名 | acquire, allocpid, freeproc, kalloc, memset, proc_pagetable ...(+1) | PageTable::new, SpinLock::new, TASK_POOL.alloc, TrapFrame::new, UnsafeCell::new, allocpid ...(+4) |

#### 中断/异常处理
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `kernel/plic.c#plicinithart` | `kernel/plic.c#plicinithart` | 同板块同名同结构 | PLIC_SENABLE, PLIC_SPRIORITY, cpuid | PLIC_SENABLE, PLIC_SPRIORITY, cpuid |
| `kernel/trap.c#clockintr` | `kernel/trap.c#clockintr` | 同板块同名 | acquire, cpuid, r_time, release, w_stimecmp, wakeup | acquire, release, wakeup |
| `kernel/spinlock.c#pop_off` | `kernel/spinlock.c#pop_off` | 同板块同名同结构 | intr_get, intr_on, mycpu, panic | intr_get, intr_on, mycpu, panic |
| `kernel/plic.c#plicinit` | `kernel/plic.c#plicinit` | 同板块同名同结构 | 内部闭环逻辑 | 内部闭环逻辑 |

#### 系统调用
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `kernel/syscall.c#argstr` | `kernel/syscall.c#argstr` | 同板块同名同结构 | argaddr, fetchstr | argaddr, fetchstr |
| `kernel/syscall.c#argaddr` | `kernel/syscall.c#argaddr` | 同板块同名同结构 | argraw | argraw |
| `kernel/syscall.c#argint` | `kernel/syscall.c#argint` | 同板块同名同结构 | argraw | argraw |

#### 文件系统
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `mkfs/mkfs.c#iappend` | `mkfs/mkfs.c#iappend` | 同板块同名同结构 | assert, bcopy, min, rinode, rsect, winode ...(+2) | assert, bcopy, min, rinode, rsect, winode ...(+2) |
| `kernel/sysfile.c#sys_unlink` | `kernel/sysfile.c#sys_unlink` | 同板块同名 | argstr, begin_op, dirlookup, end_op, ilock, isdirempty ...(+7) | argstr, unlinkat |
| `kernel/fs.c#writei` | `kernel/fs.c#writei` | 同板块同名 | bmap, bread, brelse, either_copyin, iupdate, log_write ...(+1) | rs_inode_read, rs_writei |
| `kernel/fs.c#iget` | `kernel/fs.c#iget` | 同板块同名 | acquire, panic, release | acquire, panic, release |

#### 设备驱动
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `kernel/uart.c#uartinit` | `kernel/uart.c#uartinit` | 同板块同名同结构 | WriteReg, initlock | WriteReg, initlock |
| `kernel/console.c#consolewrite` | `kernel/console.c#consolewrite` | 同板块同名 | either_copyin, uartwrite | acquiresleep, either_copyin, releasesleep, uartputc |
| `kernel/uart.c#uartputc_sync` | `kernel/uart.c#uartputc_sync` | 同板块同名 | ReadReg, WriteReg, pop_off, push_off | ReadReg, WriteReg, pop_off, push_off, sbi_console_putchar |
| `kernel/virtio_disk.c#free_desc` | `kernel/virtio_disk.c#free_desc` | 同板块同名同结构 | panic, wakeup | panic, wakeup |

#### 文件系统/第三方库
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `kernel/ext4_disk.c#ext4_disk_init` | `third/ext4_rs/src/ext4_impls/extents.rs#extent_remove_space` | 同模块上下文对齐/差异观察 | R, initlock, kalloc, memset, printf, probe_ext4_virtio | Block::load, Ext4Extent::default, Ok, ex.get_actual_len, ex.get_first_block, ex.get_pblock ...(+16) |
| `kernel/ext4_disk.c#ext4_read_block` | `third/ext4_rs/src/ext4_impls/extents.rs#ext_remove_leaf` | 同模块上下文对齐/差异观察 | R, __atomic_thread_fence, acquire, ext4_alloc_desc, ext4_free_chain, release ...(+1) | Block::load, Block::load_inode_root_block, Ext4Extent::default, Ok, ex.get_actual_len, ex.get_pblock ...(+14) |
| `kernel/ext4.c#ext4_init` | `third/ext4_rs/src/ext4_impls/file.rs#write_at` | 同模块上下文对齐/差异观察 | ext4_read_block, memmove, printf | Block::load, Ok, block.sync_blk_to_disk, block.write_offset, drop, inode_ref
                .inode
                .set_size ...(+10) |
| `kernel/ext4.c#read_file_data` | `third/ext4_rs/src/ext4_impls/balloc.rs#balloc_alloc_block` | 同模块上下文对齐/差异观察 | extent_lookup, memmove, read_fs_block | Block::load, Ext4BlockGroup::load_new, Ok, block_group.get_block_bitmap_block, block_group.get_free_blocks_count, block_group.set_block_group_balloc_bitmap_csum ...(+16) |

#### 通用模块/其他
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `user/printf.c#vprintf` | `user/printf.c#vprintf` | 同板块同名 | printint, printptr, putc, va_arg | printint, printptr, putc, va_arg |
| `kernel/printf.c#printf` | `user/printf.c#printf` | 同板块同名 | acquire, consputc, printint, printptr, release, va_arg ...(+2) | va_start, vprintf |
| `kernel/spinlock.c#release` | `kernel/spinlock.c#release` | 同板块同名 | __atomic_store_n, holding, panic, pop_off | __sync_lock_release, __sync_synchronize, holding, panic, pop_off |
| `kernel/bio.c#bget` | `kernel/bio.c#bget` | 同板块同名同结构 | acquire, acquiresleep, panic, release | acquire, acquiresleep, panic, release |


好的，审判长。本庭已收到并审阅了控辩双方的全部陈述、证据底座及原始 AST 抽样数据。现在，我将依据《审计指令》的要求，对本案进行最终的、独立的同源性定性仲裁。

---

### 1. 宏观判定
- **定性结论**: **底层架构显著差异 / 独立开发**
  - **客观 AST 相似度**: 15.09%
  - **双向覆盖率**: 目标项目 39.1%，参考项目 19.72%
  - **核心依据**: 双方的整体 AST 相似度处于极低水平。目标项目有 60.9% 的 AST 指纹为独有，参考项目更是高达 80.28%。双向覆盖率严重不对称，表明双方是在一个极小的公共子集上各自进行了大规模、不同方向的独立演化。控方所指控的“全盘复用”与客观数据严重不符。

### 2. 全模块覆盖矩阵解读
| 模块 | 双方存在 | 共同 AST 指纹 | 目标覆盖率 | 参考覆盖率 | 证据强度 | 解读 |
| :--- | :---: | :---: | :---: | :---: | :---: | :--- |
| **进程管理** | 是 | 18 | 64.29% | 54.55% | **中** | 双方覆盖率均过半，是相似度最高的板块。但这源于双方均基于 xv6 教学框架，其核心调度、上下文切换、锁原语的调用链是领域规范的必然收敛。目标项目独有的 `kexit`/`kfork` 分层架构和 `sys_shutdown` 等函数，以及参考项目独有的 `sys_clone`/`sys_mmap` 等，证明双方在进程模型上已走向不同分支。 |
| **任务调度/异步运行时** | 是 | 5 | 20.83% | 22.73% | **弱** | 双方覆盖率均很低。重合点集中在 `consoleintr`、`kerneltrap` 等硬件强相关的标准中断/陷阱处理路径上，这是 RISC-V 架构和 xv6 模式的共同约束，不构成独创性复用证据。 |
| **内存管理** | 是 | 30 | 34.48% | 22.73% | **弱** | 覆盖率较低。参考项目规模（140个函数）远超目标（88个），且 `allocproc` 等核心函数已出现 C/Rust 语言级实现差异。重合部分多为 `memset`、`kalloc` 等通用内存操作，不具备特异性。 |
| **中断/异常处理** | 是 | 7 | 87.5% | 77.78% | **中** | 覆盖率极高，但这是因为该模块函数总数极少（8 vs 9），且功能完全由 RISC-V PLIC、`tp` 寄存器、`sstatus`/`scause`/`sepc` 等硬件规范决定。`plicinit`、`pop_off` 等函数的实现是硬件手册的 C 语言翻译，属于“必然相似”。 |
| **系统调用** | 是 | 2 | 28.57% | 100.0% | **弱** | 参考项目覆盖率 100% 是因为其系统调用层函数极少（3个），仅包含 `argint`/`argaddr`/`argstr` 等参数获取函数。目标项目有 28 个函数，包含了完整的 sys_* 包装层和 Linux 兼容层，两者在系统调用分发和实现规模上完全不同。 |
| **文件系统** | 是 | 22 | 42.31% | 34.38% | **弱** | 重合点主要在 xv6 原生的 buffer cache (`bget`)、inode (`iget`/`ilock`) 和日志 (`begin_op`/`end_op`) 操作上。但参考项目的 `writei` 已委托给 Rust 实现，`sys_unlink` 简化为调用 `unlinkat`，而目标项目保留了完整的 C 实现路径，架构差异显著。 |
| **设备驱动** | 是 | 5 | 33.33% | 35.71% | **弱** | 重合点均为 UART、VirtIO 等设备的初始化与中断处理标准流程。目标项目的 `virtio_disk_intr` 缺少参考项目的 `free_chain` 调用，`uartintr` 调用链也不同，表明驱动实现细节上存在独立修改。 |
| **网络协议栈** | 否 | 0 | 0.0% | 0.0% | **未形成** | 双方均未实现网络协议栈，无证据。 |
| **文件系统/第三方库** | 是 | 0 | 0.0% | 0.0% | **未形成** | **关键差异点**。参考项目包含 323 个函数的 Rust `ext4_rs` 库，目标项目仅有 12 个函数的 C 语言自研 ext4 实现，共同 AST 指纹为 0。这是双方技术路线和实现语言根本性分歧的铁证。 |

### 3. 底层 AST 关键证据指纹表
| 所属板块 | 目标项目路径 | 参考项目路径 | 证据类型 | 技术说明 |
| :--- | :--- | :--- | :--- | :--- |
| 进程管理 | `[kernel/proc.c]` `scheduler` | `[kernel/proc.c]` `scheduler` | 同板块同名，调用链差异 | 目标项目调用链包含 `intr_off`，参考项目无。这是更保守的中断关闭策略，非简单复制。 |
| 进程管理 | `[kernel/proc.c]` `kexit` | `[kernel/proc.c]` `exit` | 同板块，调用链显著差异 | 目标项目引入 `begin_op`/`end_op` 日志事务和 `iput`，参考项目调用 `sbi_shutdown` 和 `printf`。退出路径的架构设计完全不同。 |
| 进程管理 | `[kernel/sysproc.c]` `sys_fork` | `[kernel/sysproc.c]` `sys_fork` | 同板块同名同结构，底层调用不同 | 目标项目调用 `kfork`，参考项目调用 `clone`。`kfork` 是目标项目内核分层架构的产物，`clone` 是 Linux 风格接口，语义和实现均不同。 |
| 任务调度/异步运行时 | `[kernel/spinlock.c]` `acquire` | `[kernel/spinlock.c]` `acquire` | 同板块同名，原子原语差异 | 目标项目使用 C11 `__atomic_exchange_n`，参考项目使用 GCC `__sync_lock_test_and_set`。这是编译器标准与内存模型的选择差异，非等价替换。 |
| 任务调度/异步运行时 | `[kernel/virtio_disk.c]` `virtio_disk_intr` | `[kernel/virtio_disk.c]` `virtio_disk_intr` | 同板块同名，调用链差异 | 目标项目无 `free_chain` 调用，使用 `__atomic_thread_fence`；参考项目有 `free_chain`，使用 `__sync_synchronize`。资源回收与内存屏障策略均不同。 |
| 内存管理 | `[kernel/proc.c]` `allocproc` | `[src/task.rs]` `allocproc` | 同板块同名，语言/模型异构 | 目标项目为 C 实现，调用 `kalloc`/`memset`；参考项目为 Rust 实现，调用 `PageTable::new`/`TASK_POOL.alloc`。内存分配与对象管理模型完全不同。 |
| 文件系统 | `[kernel/fs.c]` `writei` | `[kernel/fs.c]` `writei` | 同板块同名，实现路径异构 | 目标项目为完整的 C 实现，调用 `bmap`/`bread`/`brelse`；参考项目委托给 Rust 函数 `rs_writei`。文件写入的底层实现语言和路径均不同。 |
| 文件系统/第三方库 | `[kernel/ext4_disk.c]` `ext4_disk_init` | `[third/ext4_rs/src/...]` `extent_remove_space` | 跨语言/跨库，共同指纹为 0 | 目标项目用 C 实现 ext4 磁盘初始化，参考项目用 Rust 库实现 extent 操作。无任何共同 AST 指纹，是完全独立的实现。 |

### 4. 核心代码调用流对比剖析
* **【进程管理/进程调度与切换路径】**
* **底层技术析理**: 双方均遵循 xv6 的调度模式，但目标项目在 `scheduler` 中显式增加了 `intr_off()` 调用，表明其采用了更严格的中断屏蔽策略来保护调度上下文。参考项目则依赖 `swtch` 前后的隐式中断状态管理。这是并发控制粒度上的具体实现差异。
* **代码溯源链接**:
  - 目标项目: `kernel/proc.c#scheduler`
  - 参考项目: `kernel/proc.c#scheduler`

* **【进程管理/进程退出路径】**
* **底层技术析理**: 目标项目的 `kexit` 函数在进程退出时，集成了文件系统日志事务（`begin_op`/`end_op`），以保证崩溃一致性。参考项目的 `exit` 函数则直接调用 `sbi_shutdown` 关机或 `printf` 输出。两者在进程退出这一关键路径上的副作用处理和资源回收策略存在根本性分歧。
* **代码溯源链接**:
  - 目标项目: `kernel/proc.c#kexit`
  - 参考项目: `kernel/proc.c#exit`

* **【任务调度/异步运行时/自旋锁实现】**
* **底层技术析理**: 目标项目使用 C11 标准的 `__atomic_exchange_n` 实现原子交换，代表了更现代的内存模型选择。参考项目使用 GCC 传统的 `__sync_lock_test_and_set`。这不仅是语法替换，更反映了目标项目对编译器标准和跨平台原子性语义的独立考量。
* **代码溯源链接**:
  - 目标项目: `kernel/spinlock.c#acquire`
  - 参考项目: `kernel/spinlock.c#acquire`

### 5. 单方模块与降权项
*   **目标项目独有**:
    *   **Linux 兼容层** (`kernel/linux_syscalls.c`): 包含 `l_getppid` 等函数，是目标项目为兼容 Linux 应用而做的独立架构扩展，参考项目无此设计。
    *   **C 语言 ext4 实现** (`kernel/ext4*.c`): 目标项目选择用 C 语言自行实现 ext4 文件系统，与参考项目使用 Rust 第三方库的策略完全不同。
    *   **系统调用** (`sys_shutdown`, `sys_pause`, `sys_kputs`): 目标项目提供了参考项目所没有的系统调用接口。
*   **参考项目独有**:
    *   **Rust 任务管理** (`src/task.rs`): 参考项目使用 Rust 的 `SpinLock`、`PageTable` 等抽象来管理进程，与目标项目的 C 语言手动管理方式有本质区别。
    *   **第三方 ext4 库** (`third/ext4_rs`): 参考项目依赖一个庞大的 Rust 外部库来实现 ext4 功能，这是其文件系统板块函数数量（323）远超目标项目（12）的直接原因。
    *   **高级进程管理** (`sys_mmap`, `sys_brk`, `sys_clone`, `sys_wait4`): 参考项目实现了更接近 Linux 的进程/内存管理接口，目标项目则更贴近 xv6 原版。
*   **降权项说明**: 控辩双方均提及的 `scheduler`、`sched`、`kerneltrap`、`syscall`、`namex` 等跨板块同名函数，以及 `__atomic` 与 `__sync` 系列编译器内置函数的差异，均属于操作系统领域的常规设计模式、硬件 ABI 约束或编译器标准演进的自然结果。根据审计指令，这些不能作为强同源证据，本庭将其列为弱信号或技术差异项，不作为判定抄袭的依据。

### 6. 保守结论
综合全案证据，目标项目与参考项目的客观 AST 相似度仅为 15.09%，双向覆盖率均未过半，表明两者在代码实现层面存在巨大差异。控方所指控的“进程管理”与“任务调度”板块的高度相似性，经技术析理，其根源在于双方均基于 xv6 这一公共教学框架和 RISC-V 硬件规范进行开发，其核心调度、锁、中断处理逻辑的调用链是领域知识的必然收敛，而非源码级复用。目标项目独有的 `k*` 内核分层架构、C 语言自研 ext4 实现、Linux 兼容层，以及参考项目独有的 Rust 任务管理、`ext4_rs` 第三方库依赖，均清晰地指向了双方在架构设计、技术选型和实现语言上的根本性分歧。因此，本案整体风险极低，不支持抄袭指控。

------------------------------

