# 🛡️ OS 内核赛自动化代码级架构审计报告

**审计时间**: `2026-07-02 15:08:10`
**参赛作品名称**: `2026-T2026102599911006-XX`
**年份**: `2026`
**学校**: `XX`
**队伍名称**: `T2026102599911006`
**比赛名字**: `OS-Competition`
**比赛子赛道**: `Kernel-Implementation`
**仓库地址**: `https://gitlab.eduxiji.net/educg-group-43501-3132633/T2026102599911006-429`
**推测开发语言**: `Rust / C (由AST探针自动识别)`
**特征函数量**: `332`

## ⚔️ 底层代码同源性对比 (AST Homology)
> 本部分由专门的代码审查智能体，对该项目与符合年份条件的历届最高相似度项目进行控制流与底层调用对比。新版报告会先给出确定性的全模块覆盖矩阵，再给出 Agent 的分模块解释，避免只围绕少量抽样函数展开。

### 🏆 Top 4 底层对标: vs `2024-月光加冕贝拉拉-南京理工大学`（南京理工大学 / 月光加冕贝拉拉）

#### 对方作品信息

**对方年份**: `2024`
**对方学校**: `南京理工大学`
**对方队伍名称**: `月光加冕贝拉拉`
**对方比赛名字**: `OS-Competition`
**对方比赛子赛道**: `Kernel-Implementation`
**对方仓库地址**: `https://gitlab.eduxiji.net/educg-group-26010-2376550/T202410288992519-2849`

> 双向覆盖率：共同 AST 指纹: 80；唯一 AST 指纹: 目标项目 289，参考项目 323；覆盖率: 目标项目 27.68%，参考项目 24.77%

## 全模块对比总览（确定性证据）

> 总体覆盖率：共同 AST 指纹: 80；唯一 AST 指纹: 目标项目 289，参考项目 323；覆盖率: 目标项目 27.68%，参考项目 24.77%

### 1. 模块覆盖矩阵
| 模块 | 目标函数数 | 参考函数数 | 共同唯一 AST 指纹 | Target 覆盖 | Base 覆盖 | 状态 |
| :--- | ---: | ---: | ---: | ---: | ---: | :--- |
| 进程管理 | 28 | 40 | 11 | 39.29% | 27.5% | 双方存在且有结构重合 |
| 任务调度/异步运行时 | 24 | 33 | 6 | 25.0% | 18.75% | 双方存在且有结构重合 |
| 内存管理 | 88 | 95 | 15 | 17.24% | 16.48% | 双方存在且有结构重合 |
| 中断/异常处理 | 8 | 9 | 1 | 12.5% | 11.11% | 双方存在且有结构重合 |
| 系统调用 | 28 | 4 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |
| 文件系统 | 59 | 73 | 8 | 15.38% | 11.43% | 双方存在且有结构重合 |
| 设备驱动 | 15 | 9 | 2 | 13.33% | 22.22% | 双方存在且有结构重合 |
| 网络协议栈 | 0 | 0 | 0 | 0.0% | 0.0% | 双方均未体现 |
| 文件系统/第三方库 | 12 | 0 | 0 | 0.0% | 0.0% | 仅目标项目体现 |
| 通用模块/其他 | 70 | 85 | 36 | 56.25% | 48.65% | 双方存在且有结构重合 |

### 2. 分模块证据清单

#### 进程管理
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `user/sh.c#parseexec` | `user/sh.c#parseexec` | 同板块同名同结构 | execcmd, gettoken, panic, parseblock, parseredirs, peek | execcmd, gettoken, panic, parseblock, parseredirs, peek |
| `kernel/proc.c#procdump` | `kernel/proc.c#procdump` | 同板块同名 | NELEM, printf | NELEM, printf |
| `kernel/proc.c#sleep` | `kernel/proc.c#sleep` | 同板块同名 | acquire, myproc, release, sched | acquire, myproc, release, sched |
| `kernel/proc.c#sched` | `kernel/proc.c#sched` | 同板块同名 | holding, intr_get, mycpu, myproc, panic, swtch | holding, intr_get, mycpu, myproc, panic, swtch |

#### 任务调度/异步运行时
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `user/sh.c#runcmd` | `user/sh.c#runcmd` | 同板块同名 | close, dup, exec, exit, fork1, fprintf ...(+5) | close, dup, exec, exit, fork1, fprintf ...(+5) |
| `kernel/console.c#consoleintr` | `kernel/console.c#consoleintr` | 同板块同名 | C, acquire, consputc, procdump, release, wakeup | C, acquire, consputc, procdump, release, wakeup |
| `kernel/console.c#consoleread` | `kernel/console.c#consoleread` | 同板块同名 | C, acquire, either_copyout, killed, myproc, release ...(+1) | C, acquire, either_copyout, myproc, release, sleep |
| `user/sh.c#nulterminate` | `user/sh.c#nulterminate` | 同板块同名同结构 | nulterminate | nulterminate |

#### 内存管理
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `kernel/sysfile.c#sys_open` | `kernel/sysfile.c#sys_open` | 同板块同名 | argint, argstr, begin_op, create, end_op, fdalloc ...(+7) | argint, argstr, create, elock, ename, eput ...(+5) |
| `kernel/sysfile.c#create` | `kernel/sysfile.c#create` | 同板块同名 | dirlink, dirlookup, ialloc, ilock, iunlockput, iupdate ...(+1) | ealloc, elock, enameparent, eput, eunlock |
| `kernel/proc.c#allocproc` | `kernel/proc.c#allocproc` | 同板块同名 | acquire, allocpid, freeproc, kalloc, memset, proc_pagetable ...(+1) | acquire, allocpid, freeproc, kalloc, memset, panic ...(+3) |
| `kernel/proc.c#proc_pagetable` | `kernel/proc.c#proc_pagetable` | 同板块同名 | (uint64), mappages, uvmcreate, uvmfree, uvmunmap | (uint64), mappages, uvmcreate, uvmfree, vmunmap |

#### 中断/异常处理
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `kernel/plic.c#plicinithart` | `kernel/plic.c#plicinithart` | 同板块同名 | PLIC_SENABLE, PLIC_SPRIORITY, cpuid | PLIC_SENABLE, printf |
| `kernel/spinlock.c#pop_off` | `kernel/intr.c#pop_off` | 同板块同名 | intr_get, intr_on, mycpu, panic | intr_get, intr_on, mycpu, panic |
| `kernel/plic.c#plicinit` | `kernel/plic.c#plicinit` | 同板块同名 | 内部闭环逻辑 | printf, writed |
| `kernel/plic.c#plic_claim` | `kernel/plic.c#plic_claim` | 同板块同名 | PLIC_SCLAIM, cpuid | PLIC_SCLAIM, cpuid |

#### 系统调用
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `kernel/syscall.c#argstr` | `kernel/syscall.c#argstr` | 同板块同名 | argaddr, fetchstr | argaddr, fetchstr |
| `kernel/syscall.c#argaddr` | `kernel/syscall.c#argaddr` | 同板块同名 | argraw | argraw |
| `kernel/syscall.c#argint` | `kernel/syscall.c#argint` | 同板块同名 | argraw | argraw |
| `kernel/linux_syscalls.c#linux_dispatch` | `kernel/systime.c#sys_gettimeofday` | 同模块上下文对齐/差异观察 | lsystab[mid].fn | argaddr, copyout2, r_time |

#### 文件系统
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `user/grind.c#go` | `user/grind.c#go` | 同板块同名 | chdir, close, dup, exec, exit, fork ...(+16) | chdir, close, dup, exec, exit, fork ...(+16) |
| `kernel/fs.c#dirlookup` | `kernel/fat32.c#dirlookup` | 同板块同名 | iget, namecmp, panic, readi | edup, eget, enext, eput, panic, reloc_clus ...(+2) |
| `kernel/file.c#fileclose` | `kernel/file.c#fileclose` | 同板块同名 | acquire, begin_op, end_op, iput, panic, pipeclose ...(+1) | acquire, eput, panic, pipeclose, release |
| `kernel/fs.c#skipelem` | `kernel/fat32.c#skipelem` | 同板块同名 | memmove | memmove |

#### 设备驱动
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `kernel/console.c#consolewrite` | `kernel/console.c#consolewrite` | 同板块同名 | either_copyin, uartwrite | acquire, either_copyin, release, sbi_console_putchar, uart_putchar |
| `user/sh.c#parseblock` | `user/sh.c#parseblock` | 同板块同名同结构 | gettoken, panic, parseline, parseredirs, peek | gettoken, panic, parseline, parseredirs, peek |
| `kernel/console.c#consputc` | `kernel/console.c#consputc` | 同板块同名 | uartputc_sync | sbi_console_putchar, uart_putchar |
| `kernel/printf.c#panic` | `kernel/printf.c#panic` | 同板块同名 | printf | backtrace, printf |

#### 文件系统/第三方库
仅目标项目中观察到该模块代表函数，参考项目没有同模块可对齐证据：
- 目标项目：`kernel/ext4_disk.c#ext4_disk_init`，调用摘要：R, initlock, kalloc, memset, printf, probe_ext4_virtio
- 目标项目：`kernel/ext4_disk.c#ext4_read_block`，调用摘要：R, __atomic_thread_fence, acquire, ext4_alloc_desc, ext4_free_chain, release ...(+1)
- 目标项目：`kernel/ext4.c#ext4_init`，调用摘要：ext4_read_block, memmove, printf
- 目标项目：`kernel/ext4.c#read_file_data`，调用摘要：extent_lookup, memmove, read_fs_block

#### 通用模块/其他
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `user/printf.c#vprintf` | `user/printf.c#vprintf` | 同板块同名 | printint, printptr, putc, va_arg | printint, printptr, putc, va_arg |
| `kernel/printf.c#printf` | `user/printf.c#printf` | 同板块同名 | acquire, consputc, printint, printptr, release, va_arg ...(+2) | fflush, va_start, vprintf |
| `kernel/spinlock.c#release` | `kernel/spinlock.c#release` | 同板块同名 | __atomic_store_n, holding, panic, pop_off | __sync_lock_release, __sync_synchronize, holding, panic, pop_off |
| `kernel/bio.c#bget` | `kernel/bio.c#bget` | 同板块同名同结构 | acquire, acquiresleep, panic, release | acquire, acquiresleep, panic, release |


好的，审计官。基于提供的证据底座、控辩双方陈述及AST抽样数据，现出具最终同源性定性仲裁报告。

---

### 1. 宏观判定
- **定性结论**: **独立开发 / 底层架构显著差异**
  本案客观AST相似度仅为15.04%，双向覆盖率均低于30%。该指标远未达到“抄袭”或“结构性复用”的阈值。双方项目虽共享xv6教学操作系统这一公共源头，但在核心子系统（尤其是文件系统、系统调用层）的架构选型与实现上存在根本性、不可通约的差异。控方所指控的“铁证”均为操作系统内核领域的公共知识、RISC-V架构的必然实现或xv6标准范式的功能同构，不构成代码抄袭的强证据。

### 2. 全模块覆盖矩阵解读
| 模块 | 双方存在 | 共同指纹 | 覆盖率 (T/B) | 证据强度 | 解读 |
| :--- | :---: | :---: | :---: | :---: | :--- |
| **进程管理** | 是 | 是 | 39.29% / 27.5% | **弱** | 覆盖率主要由调度、上下文切换、进程状态管理等xv6标准范式贡献。同名函数（如`sched`, `yield`）的调用链由RISC-V多核调度语义唯一确定，属于功能必要性重合。 |
| **任务调度/异步运行时** | 是 | 是 | 25.0% / 18.75% | **弱** | 相似性集中在Shell解析器（`parseexec`, `runcmd`）和自旋锁（`acquire`）等公开教学代码的标准实现。目标项目的`syscall`分发路径集成了Linux兼容层，与参考项目存在架构差异。 |
| **内存管理** | 是 | 是 | 17.24% / 16.48% | **弱** | 低覆盖率表明双方在物理/虚拟内存管理上实现差异巨大。`allocproc`等函数的相似性源于xv6进程初始化模型，但目标项目在页表操作上已有独立演化。 |
| **中断/异常处理** | 是 | 是 | 12.5% / 11.11% | **未形成** | 极低的覆盖率。`kerneltrap`等函数的相似调用链（读取`r_scause`等CSR）由RISC-V特权架构规范强制决定，不构成独创性表达。 |
| **系统调用** | 是 | 否 | 0.0% / 0.0% | **未形成** | 双方在此模块无共同AST指纹。目标项目实现了庞大的Linux兼容系统调用层，而参考项目仅有4个函数，这是两者架构根本性差异的铁证之一。 |
| **文件系统** | 是 | 是 | 15.38% / 11.43% | **中（反证）** | 此模块的相似性极低，且关键同名函数（如`dirlookup`, `create`）的调用链揭示了**根本性的对象模型差异**：目标项目基于传统Unix inode，参考项目基于FAT32的entry/cluster。这是独立开发的有力证据。 |
| **设备驱动** | 是 | 是 | 13.33% / 22.22% | **未形成** | 相似性集中在`console`驱动，其读写函数的调用链（`acquire/sleep/wakeup`）是字符设备中断处理的通用模式，不具特异性。 |
| **网络协议栈** | 否 | 否 | 0.0% / 0.0% | **未形成** | 双方均未实现。 |
| **文件系统/第三方库** | 仅目标 | 否 | 0.0% / 0.0% | **强（反证）** | 目标项目独有`ext4`文件系统模块，这是其独立开发、功能扩展的决定性证据，直接反驳了“抄袭”指控。 |
| **通用模块/其他** | 是 | 是 | 56.25% / 48.65% | **弱** | 覆盖率较高，但主要由`printf`、`panic`、`spinlock`等工具函数和基础库构成。这些函数在xv6衍生项目中高度同源，属于公共依赖。 |

### 3. 底层 AST 关键证据指纹表
| 所属板块 | 目标项目路径 | 参考项目路径 | 证据类型 | 技术说明 |
| :--- | :--- | :--- | :--- | :--- |
| 文件系统 | `kernel/sysfile.c#create` | `kernel/sysfile.c#create` | 架构差异 | 目标项目调用`ialloc, ilock, iupdate`（inode模型），参考项目调用`ealloc, elock, eunlock`（FAT32 entry模型）。对象管理模型根本不同。 |
| 文件系统 | `kernel/fs.c#dirlookup` | `kernel/fat32.c#dirlookup` | 架构差异 | 目标项目调用`iget, readi`（inode读取），参考项目调用`eget, enext, reloc_clus`（FAT32簇链遍历）。文件系统实现完全异构。 |
| 系统调用 | `kernel/linux_syscalls.c#linux_dispatch` | `kernel/systime.c#sys_gettimeofday` | 架构差异 | 目标项目实现Linux兼容层分发，参考项目仅有少量自定义系统调用。系统调用层的规模和设计哲学截然不同。 |
| 进程管理 | `kernel/proc.c#scheduler` | `kernel/proc.c#scheduler` | 功能同构 | 双方均实现MLFQ或轮询调度，调用`swtch`进行上下文切换。这是多任务OS内核的必然组件，但具体调度算法细节无法通过调用链图谱体现。 |
| 进程管理 | `kernel/spinlock.c#acquire` | `kernel/spinlock.c#acquire` | 实现差异 | 目标项目使用C11 `__atomic_exchange_n`，参考项目使用GCC `__sync_lock_test_and_set` + `__sync_synchronize`。原子操作模型与内存屏障策略不同。 |
| 任务调度 | `kernel/syscall.c#syscall` | `kernel/syscall.c#syscall` | 架构差异 | 目标项目在标准`syscalls[num]`分发后增加`linux_dispatch`路径，参考项目无此逻辑。系统调用入口存在结构性扩展。 |
| 设备驱动 | `kernel/console.c#consolewrite` | `kernel/console.c#consolewrite` | 实现差异 | 目标项目直接调用`uartwrite`，参考项目调用`acquire/release`保护并调用`sbi_console_putchar`。底层I/O路径和同步策略不同。 |
| 通用模块 | `kernel/printf.c#panic` | `kernel/printf.c#panic` | 实现差异 | 目标项目仅调用`printf`，参考项目额外调用`backtrace`。错误处理功能的丰富度不同。 |

### 4. 核心代码调用流对比剖析
* **【文件系统/文件创建路径】**
* **底层技术析理**: 此路径揭示了双方最根本的架构差异。目标项目采用经典的Unix inode模型，通过`ialloc`分配磁盘inode，`ilock`锁定inode，`iupdate`写回。参考项目则基于FAT32文件系统，通过`ealloc`分配目录项，`elock`锁定目录项，`enameparent`处理长文件名。两者在磁盘数据结构、分配算法、锁粒度上完全不同，属于不可通约的架构选型差异，绝非“改名”或“混淆”所能实现。
* **代码溯源链接**:
  - 目标项目: `kernel/sysfile.c#create`
  - 参考项目: `kernel/sysfile.c#create`

* **【系统调用/入口分发路径】**
* **底层技术析理**: 目标项目的`syscall`函数在标准xv6系统调用表分发之后，增加了一个`linux_dispatch`调用分支，用于处理额外的Linux兼容系统调用。这证明了目标项目在系统调用层进行了大规模的独立功能扩展。参考项目的`syscall`函数则保持标准的xv6实现，且其系统调用表规模极小。两者在ABI层的设计目标与实现规模存在显著差异。
* **代码溯源链接**:
  - 目标项目: `kernel/syscall.c#syscall`
  - 参考项目: `kernel/syscall.c#syscall`

* **【进程管理/自旋锁获取路径】**
* **底层技术析理**: 双方均实现了自旋锁，但底层原子指令集选择不同。目标项目使用C11标准的`__atomic_exchange_n`，其内存顺序参数内建了屏障语义。参考项目使用GCC传统的`__sync_lock_test_and_set`，并显式调用`__sync_synchronize`添加全屏障。这是两种不同代际、不同语义模型的并发控制实现，反映了独立的技术选型，而非简单的“叶子节点替换”。
* **代码溯源链接**:
  - 目标项目: `kernel/spinlock.c#acquire`
  - 参考项目: `kernel/spinlock.c#acquire`

### 5. 单方模块与降权项
*   **单方模块（强反证）**：
    *   **目标项目独有 - `文件系统/第三方库`**：包含`ext4_disk.c`、`ext4.c`等完整的ext4文件系统实现。这是目标项目独立开发、具备更高功能复杂度的决定性证据。
    *   **目标项目独有 - `系统调用`**：`linux_syscalls.c`实现了大量Linux兼容系统调用，是独立扩展的铁证。
*   **降权项（弱信号）**：
    *   **跨板块同名函数**：如`consoleintr`、`consoleread`、`kerneltrap`等，其相似性源于硬件规范（RISC-V CSR）或通用驱动模式，不构成同板块主证据。
    *   **公共依赖与标准范式**：`printf`、`panic`、`strchr`、`memmove`等C标准库函数或xv6标准工具函数，以及`parseexec`、`runcmd`等xv6 Shell标准递归下降解析器结构，属于公共领域知识，不能作为抄袭证据。
    *   **功能必要性重合**：`cpuid` -> `r_tp`，`mycpu` -> `cpuid`，`myproc` -> `mycpu`等调用链，是RISC-V架构下获取当前CPU和进程状态的唯一或高度受限的实现方式。

### 6. 保守结论
本案客观AST相似度仅15.04%，远低于抄袭判定阈值。双方代码的相似性高度集中在RISC-V架构的必然实现、xv6教学操作系统的标准范式以及C标准库等公共知识领域。辩方提供的技术辩护逻辑清晰、证据确凿，成功论证了“k-前缀”是合理的分层设计，原子操作替换是技术现代化迁移。决定性反证在于，双方在文件系统这一核心子系统上存在根本性架构差异（inode vs. FAT32），且目标项目独有ext4和Linux兼容层等大规模独立功能扩展。综上，目标项目`[2026-T2026102599911006-XX]`相对于参考项目`[2024-月光加冕贝拉拉-南京理工大学]`是**基于公共教学源头进行独立开发与功能演进的产物，不存在代码抄袭或结构性复用行为**。

------------------------------

