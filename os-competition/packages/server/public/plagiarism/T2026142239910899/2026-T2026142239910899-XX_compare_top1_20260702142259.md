# 🛡️ OS 内核赛自动化代码级架构审计报告

**审计时间**: `2026-07-02 14:22:59`
**参赛作品名称**: `2026-T2026142239910899-XX`
**年份**: `2026`
**学校**: `XX`
**队伍名称**: `T2026142239910899`
**比赛名字**: `OS-Competition`
**比赛子赛道**: `Kernel-Implementation`
**仓库地址**: `https://gitlab.eduxiji.net/educg-group-43501-3132633/T2026142239910899-2376`
**推测开发语言**: `Rust / C (由AST探针自动识别)`
**特征函数量**: `38`

## ⚔️ 底层代码同源性对比 (AST Homology)
> 本部分由专门的代码审查智能体，对该项目与符合年份条件的历届最高相似度项目进行控制流与底层调用对比。新版报告会先给出确定性的全模块覆盖矩阵，再给出 Agent 的分模块解释，避免只围绕少量抽样函数展开。

### 🏆 Top 1 底层对标: vs `2022-w以忽略所有警告-南京航空航天大学`（南京航空航天大学 / -w以忽略所有警告）

#### 对方作品信息

**对方年份**: `2022`
**对方学校**: `南京航空航天大学`
**对方队伍名称**: `-w以忽略所有警告`
**对方比赛名字**: `OS-Competition`
**对方比赛子赛道**: `Kernel-Implementation`
**对方仓库地址**: `https://gitlab.eduxiji.net/qianpinyi/pal_operatingsystem`

> 双向覆盖率：共同 AST 指纹: 1；唯一 AST 指纹: 目标项目 37，参考项目 64；覆盖率: 目标项目 2.7%，参考项目 1.56%

## 全模块对比总览（确定性证据）

> 总体覆盖率：共同 AST 指纹: 1；唯一 AST 指纹: 目标项目 37，参考项目 64；覆盖率: 目标项目 2.7%，参考项目 1.56%

### 1. 模块覆盖矩阵
| 模块 | 目标函数数 | 参考函数数 | 共同唯一 AST 指纹 | Target 覆盖 | Base 覆盖 | 状态 |
| :--- | ---: | ---: | ---: | ---: | ---: | :--- |
| 进程管理 | 2 | 1 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |
| 任务调度/异步运行时 | 6 | 12 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |
| 内存管理 | 11 | 6 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |
| 中断/异常处理 | 2 | 11 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |
| 系统调用 | 0 | 0 | 0 | 0.0% | 0.0% | 双方均未体现 |
| 文件系统 | 2 | 0 | 0 | 0.0% | 0.0% | 仅目标项目体现 |
| 设备驱动 | 6 | 47 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |
| 网络协议栈 | 0 | 0 | 0 | 0.0% | 0.0% | 双方均未体现 |
| 通用模块/其他 | 9 | 0 | 0 | 0.0% | 0.0% | 仅目标项目体现 |

### 2. 分模块证据清单

#### 进程管理
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `kernel/main.c#test_process_a` | `Kernel/HAL/Drivers/_dmac.c#dmac_wait_idle` | 同模块上下文对齐/差异观察 | proc_exit, uart_int, uart_puts | dmacSemWait, dmac_is_idle |

#### 任务调度/异步运行时
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `kernel/sched.c#schedule` | `Kernel/HAL/Drivers/_sysctl.c#sysctl_clock_device_en` | 同模块上下文对齐/差异观察 | 内部闭环逻辑 | 内部闭环逻辑 |
| `kernel/trap.c#handle_interrupt` | `Kernel/HAL/Drivers/_spi.c#spi_receive_data_standard` | 同模块上下文对齐/差异观察 | timer_triggered, uart_int, uart_puts | (uint32), spi_get_frame_size, spi_set_tmod |
| `kernel/sched.c#timer_triggered` | `Kernel/HAL/Drivers/_spi.c#spi_send_data_normal` | 同模块上下文对齐/差异观察 | schedule, set_next_timer | memmove, spi_get_frame_size, spi_set_tmod |
| `kernel/sched.c#sched_init` | `Kernel/HAL/Drivers/_sysctl.c#sysctl_clock_bus_en` | 同模块上下文对齐/差异观察 | memset, set_next_timer, uart_puts | 内部闭环逻辑 |

#### 内存管理
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `kernel/main.c#kernel_main` | `Kernel/HAL/Drivers/_spi.c#spi_receive_data_standard_dma` | 同模块上下文对齐/差异观察 | get_free_pages, get_total_pages, mem_init, page_alloc, page_free, proc_create ...(+8) | kalloc, kfree, spi_get_frame_size, spi_receive_data_normal_dma |
| `kernel/mem.c#mem_init` | `Kernel/HAL/Drivers/_spi.c#spi_send_data_standard_dma` | 同模块上下文对齐/差异观察 | memset, uart_int, uart_puts | kalloc, kfree, spi_get_frame_size, spi_send_data_normal_dma |
| `kernel/sched.c#proc_create` | `Kernel/HAL/Drivers/_spi.c#spi_init` | 同模块上下文对齐/差异观察 | disable_interrupt, enable_interrupt, memset, page_alloc, strncpy, uart_puts | 内部闭环逻辑 |
| `kernel/trap.c#handle_exception` | `Kernel/HAL/Drivers/_spi.c#spi_send_data_normal_dma` | 同模块上下文对齐/差异观察 | uart_hex, uart_int, uart_puts | dmac_set_single_mode, dmac_wait_done, kalloc, kfree, spi_set_tmod, sysctl_dma_select |

#### 中断/异常处理
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `kernel/trap.c#handle_trap` | `Kernel/HAL/Drivers/_plic.c#plicinithart` | 同模块上下文对齐/差异观察 | handle_exception, handle_interrupt | PLIC_MENABLE, PLIC_SENABLE, PLIC_SPRIORITY, cpuid, printf, readd |
| `kernel/trap.c#trap_init` | `Kernel/HAL/Drivers/_dmac.c#dmac_enable_common_interrupt_signal` | 同模块上下文对齐/差异观察 | uart_puts | readq, writeq |

#### 文件系统
仅目标项目中观察到该模块代表函数，参考项目没有同模块可对齐证据：
- 目标项目：`kernel/uart.c#uart_write_reg`，调用摘要：内部闭环逻辑
- 目标项目：`kernel/uart.c#uart_read_reg`，调用摘要：内部闭环逻辑

#### 设备驱动
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `kernel/uart.c#uart_init` | `Kernel/HAL/Drivers/_dmac.c#dmac_set_channel_param` | 同模块上下文对齐/差异观察 | uart_read_reg, uart_write_reg | is_memory, readq, writeq |
| `kernel/uart.c#uart_int` | `Kernel/HAL/Drivers/_sdcard.c#sdcard_write_sector` | 同模块上下文对齐/差异观察 | uart_putc | sd_end_cmd, sd_get_response_R1, sd_read_data, sd_send_cmd, sd_write_data, semSDCard->Signal ...(+1) |
| `kernel/uart.c#uart_hex` | `Kernel/HAL/Drivers/_spi.c#spi_receive_data_normal_dma` | 同模块上下文对齐/差异观察 | uart_putc, uart_puts | (uint32), dmac_set_single_mode, dmac_wait_done, spi_get_frame_format, spi_set_tmod, sysctl_dma_select |
| `kernel/uart.c#uart_putc` | `Kernel/HAL/Drivers/_sdcard.c#sdcard_read_sector` | 同模块上下文对齐/差异观察 | uart_read_reg, uart_write_reg | sd_end_cmd, sd_get_response_R1, sd_read_data, sd_send_cmd, semSDCard->Signal, semSDCard->Wait |

#### 通用模块/其他
仅目标项目中观察到该模块代表函数，参考项目没有同模块可对齐证据：
- 目标项目：`kernel/string.c#strncmp`，调用摘要：内部闭环逻辑
- 目标项目：`kernel/string.c#memcmp`，调用摘要：内部闭环逻辑
- 目标项目：`kernel/string.c#strncpy`，调用摘要：内部闭环逻辑
- 目标项目：`kernel/string.c#memcpy`，调用摘要：内部闭环逻辑


#### 1. 宏观判定
- **定性结论**: **独立开发 / 底层架构显著差异**
- **判定依据**: 客观 AST 相似度仅为 1.0%，共同 AST 指纹仅有 1 个，双向覆盖率均低于 3%。双方在核心系统架构、业务领域及外部调用对象上存在根本性差异，未发现任何同板块的结构性代码复用证据。

#### 2. 全模块覆盖矩阵解读
- **进程管理**: 双方均存在，但共同 AST 指纹为 0，覆盖率均为 0.0%。目标项目为典型的进程创建与退出逻辑，参考项目为 DMAC 空闲等待逻辑，两者无结构关联。证据强度：**未形成**。
- **任务调度/异步运行时**: 双方均存在，共同 AST 指纹为 0，覆盖率均为 0.0%。目标项目实现软件调度器与时钟中断处理，参考项目为 SoC 时钟与 SPI 传输控制，属于完全不同的领域。证据强度：**未形成**。
- **内存管理**: 双方均存在，共同 AST 指纹为 0，覆盖率均为 0.0%。目标项目实现页式内存分配器，参考项目为 SPI DMA 的堆内存申请与释放，内存模型与分配粒度截然不同。证据强度：**未形成**。
- **中断/异常处理**: 双方均存在，共同 AST 指纹为 0，覆盖率均为 0.0%。目标项目为 RISC-V 陷阱分发逻辑，参考项目为 PLIC 与 DMAC 中断控制器初始化，硬件抽象层级不同。证据强度：**未形成**。
- **系统调用**: 双方均未体现该模块。证据强度：**未形成**。
- **文件系统**: 仅目标项目体现，为 UART 寄存器读写函数被误归类至此。参考项目无此模块。证据强度：**未形成**。
- **设备驱动**: 双方均存在，共同 AST 指纹为 0，覆盖率均为 0.0%。目标项目为 UART 串口驱动，参考项目为 DMAC、SDCard、SPI 等复杂外设驱动，驱动对象完全不同。证据强度：**未形成**。
- **网络协议栈**: 双方均未体现。证据强度：**未形成**。
- **通用模块/其他**: 仅目标项目体现，为标准 C 字符串与内存操作库。参考项目无此独立模块。证据强度：**未形成**。

#### 3. 底层 AST 关键证据指纹表
| 所属板块 | 目标项目路径 | 参考项目路径 | 证据类型 | 技术说明 |
| :--- | :--- | :--- | :--- | :--- |
| 内存管理 | `[kernel/mem.c]` `page_alloc` | `[Kernel/HAL/Drivers/_spi.c]` `spi_send_data_standard_dma` | 差异证据 | 目标项目调用 `memset` 实现页分配后清零；参考项目调用 `kalloc`/`kfree` 进行动态堆内存管理。内存模型（页式 vs 堆式）与分配策略完全不同。 |
| 中断/异常处理 | `[kernel/trap.c]` `handle_trap` | `[Kernel/HAL/Drivers/_plic.c]` `plicinithart` | 差异证据 | 目标项目为二级陷阱分发器，调用 `handle_exception`/`handle_interrupt`；参考项目为 PLIC 中断控制器初始化，操作 `PLIC_MENABLE` 等硬件寄存器。抽象层级与操作对象完全不同。 |
| 设备驱动 | `[kernel/uart.c]` `uart_putc` | `[Kernel/HAL/Drivers/_dmac.c]` `dmac_set_channel_param` | 差异证据 | 目标项目通过 `uart_read_reg`/`uart_write_reg` 操作 UART 寄存器；参考项目通过 `readq`/`writeq` 操作 DMAC 寄存器。驱动外设与寄存器访问原语均不同。 |
| 任务调度/异步运行时 | `[kernel/sched.c]` `schedule` | `[Kernel/HAL/Drivers/_sysctl.c]` `sysctl_clock_device_en` | 差异证据 | 两者均为“内部闭环逻辑”叶子函数，但目标项目实现软件调度算法，参考项目实现硬件时钟门控。内部逻辑无任何结构关联。 |

#### 4. 核心代码调用流对比剖析
* **【内存管理/分配与初始化路径】**
* **底层技术析理**: 目标项目构建了完整的页式内存管理，`kernel_main` 通过 `mem_init` 初始化，`page_alloc` 分配单页并调用 `memset` 清零，`page_free` 回收。参考项目无独立内存管理板块，其内存操作散落在 SPI 驱动中，通过 `kalloc`/`kfree` 进行动态堆分配以服务于 DMA 传输，无页表概念。两者在内存抽象（物理页 vs 动态堆）、分配策略（伙伴/位图 vs 通用堆）及生命周期管理上存在根本性异构。
* **代码溯源链接**:
  - 目标项目: `kernel/main.c#kernel_main`, `kernel/mem.c#mem_init`, `kernel/mem.c#page_alloc`
  - 参考项目: `Kernel/HAL/Drivers/_spi.c#spi_send_data_standard_dma`, `Kernel/HAL/Drivers/_spi.c#spi_send_data_normal_dma`

* **【中断/异常处理/陷阱分发路径】**
* **底层技术析理**: 目标项目实现了一个通用的陷阱处理框架，`handle_trap` 作为总入口，根据陷阱原因分发至 `handle_exception` 或 `handle_interrupt`，后者再触发调度器。参考项目则直接操作 PLIC 硬件寄存器进行中断控制器初始化，或操作 DMAC 寄存器使能特定中断信号。目标项目构建的是软件分发层，参考项目实现的是硬件配置层，两者在中断处理的抽象层次上完全不同。
* **代码溯源链接**:
  - 目标项目: `kernel/trap.c#handle_trap`, `kernel/trap.c#handle_interrupt`
  - 参考项目: `Kernel/HAL/Drivers/_plic.c#plicinithart`, `Kernel/HAL/Drivers/_dmac.c#dmac_enable_common_interrupt_signal`

* **【设备驱动/寄存器访问模式】**
* **底层技术析理**: 目标项目的 UART 驱动通过 `uart_read_reg`/`uart_write_reg` 封装 MMIO 操作，构建了 `uart_putc`、`uart_getc` 等字符级接口。参考项目则使用 `readq`/`writeq` 等 64 位原语直接操作 DMAC 或 SDCard 控制器。虽然两者都体现了“寄存器读写封装”的通用范式，但封装粒度（带偏移的 UART 寄存器 vs 原始 64 位访问）、操作对象（UART vs DMAC/SDCard）及上层接口（字符流 vs 块设备/DMA 描述符）完全不同。
* **代码溯源链接**:
  - 目标项目: `kernel/uart.c#uart_init`, `kernel/uart.c#uart_putc`
  - 参考项目: `Kernel/HAL/Drivers/_dmac.c#dmac_set_channel_param`, `Kernel/HAL/Drivers/_dmac.c#dmac_init`

#### 5. 单方模块与降权项
- **目标项目独占模块**:
  - **文件系统**: 实为 UART 寄存器读写函数，非真实文件系统。
  - **通用模块/其他**: 包含 `memcpy`、`strncpy` 等标准 C 库函数实现。这些函数是任何 C 项目的通用基础组件，其实现遵循公开标准，不能作为同源证据。
- **参考项目独占模块**:
  - 大量 SoC 外设驱动（SDCard、SPI、DMAC、sysctl、PLIC 等），目标项目完全未涉及。
- **跨板块弱信号降权说明**: 控方提出的所有指控均为跨板块关联（如将 UART 驱动与 DMAC 驱动对比），违反了“同板块”比对原则。所指的“寄存器读写封装”、“分配即清零”、“内部闭环逻辑”、“初始化时序”等均为嵌入式/OS 领域的通用编程范式或抽象分类标签，不构成特定代码表达的复用证据。

#### 6. 保守结论
本次审计基于客观 AST 调用图谱，目标项目与参考项目的相似度极低（1.0%），在所有核心模块上均未发现共同结构指纹。目标项目是一个典型的微型操作系统内核教学实验，实现了进程、页式内存、调度、陷阱分发等 OS 核心机制；参考项目则是一个面向特定 SoC 的硬件抽象层驱动库。两者在架构层级、业务领域、内存模型、同步策略及外部调用对象上存在根本性差异。控方提出的“弱信号”均源于对通用编程范式的误认或跨板块的强行关联，缺乏客观 AST 证据支撑。综合判定，目标项目为独立开发，无代码复用风险。

------------------------------

