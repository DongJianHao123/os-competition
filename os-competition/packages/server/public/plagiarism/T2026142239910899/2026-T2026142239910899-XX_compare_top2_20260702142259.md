# 🛡️ OS 内核赛自动化代码级架构审计报告

**审计时间**: `2026-07-02 14:22:59`
**参赛作品名称**: `2026-T2026142239910899-XX`
**年份**: `2026`
**学校**: `XX`
**队伍名称**: `T2026142239910899`
**比赛名字**: `OS-Competition`
**比赛子赛道**: `Kernel-Implementation`
**仓库地址**: `https://gitlab.eduxiji.net/educg-group-43501-3132633/T2026142239910899-2376`
**推测开发语言**: `Rust / C (由AST探针自动识别)`
**特征函数量**: `38`

## ⚔️ 底层代码同源性对比 (AST Homology)
> 本部分由专门的代码审查智能体，对该项目与符合年份条件的历届最高相似度项目进行控制流与底层调用对比。新版报告会先给出确定性的全模块覆盖矩阵，再给出 Agent 的分模块解释，避免只围绕少量抽样函数展开。

### 🏆 Top 2 底层对标: vs `2023-接下来我要表扬一款游戏-广东工业大学`（广东工业大学 / 接下来我要表扬一款游戏）

#### 对方作品信息

**对方年份**: `2023`
**对方学校**: `广东工业大学`
**对方队伍名称**: `接下来我要表扬一款游戏`
**对方比赛名字**: `OS-Competition`
**对方比赛子赛道**: `Kernel-Implementation`
**对方仓库地址**: `https://gitlab.eduxiji.net/202311845101096/oskernel2023-gdut`

> 双向覆盖率：共同 AST 指纹: 2；唯一 AST 指纹: 目标项目 37，参考项目 179；覆盖率: 目标项目 5.41%，参考项目 1.12%

## 全模块对比总览（确定性证据）

> 总体覆盖率：共同 AST 指纹: 2；唯一 AST 指纹: 目标项目 37，参考项目 179；覆盖率: 目标项目 5.41%，参考项目 1.12%

### 1. 模块覆盖矩阵
| 模块 | 目标函数数 | 参考函数数 | 共同唯一 AST 指纹 | Target 覆盖 | Base 覆盖 | 状态 |
| :--- | ---: | ---: | ---: | ---: | ---: | :--- |
| 进程管理 | 2 | 29 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |
| 任务调度/异步运行时 | 6 | 21 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |
| 内存管理 | 11 | 67 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |
| 中断/异常处理 | 2 | 20 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |
| 系统调用 | 0 | 12 | 0 | 0.0% | 0.0% | 仅参考项目体现 |
| 文件系统 | 2 | 49 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |
| 设备驱动 | 6 | 7 | 1 | 16.67% | 14.29% | 双方存在且有结构重合 |
| 网络协议栈 | 0 | 0 | 0 | 0.0% | 0.0% | 双方均未体现 |
| 通用模块/其他 | 9 | 11 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |

### 2. 分模块证据清单

#### 进程管理
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `kernel/main.c#test_process_a` | `kernel/process/fork.c#copy_thread` | 同模块上下文对齐/差异观察 | proc_exit, uart_int, uart_puts | get_current_task, get_pt_reg, memset |
| `kernel/sched.c#proc_yield` | `kernel/process/schedule.c#__schedule` | 同模块上下文对齐/差异观察 | schedule | get_current_task, irq_disable, schedule_debug, schedule_tail, simple_sched_class.dequeue_task, simple_sched_class.pick_next_task ...(+1) |

#### 任务调度/异步运行时
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `kernel/sched.c#schedule` | `lib/printk.c#vsprintf` | 同模块上下文对齐/差异观察 | 内部闭环逻辑 | is_digit, number, skip_atoi, strlen, va_arg |
| `kernel/trap.c#handle_interrupt` | `kernel/driver/virtio.c#virtio_dev_init` | 同模块上下文对齐/差异观察 | timer_triggered, uart_int, uart_puts | printk, readword, refresh_cache, virtio_blk_init_legacy, writeword |
| `kernel/sched.c#timer_triggered` | `kernel/fs/loader.c#initElf` | 同模块上下文对齐/差异观察 | schedule, set_next_timer | LOADER_SEEK_SET, filp->f_ops->lseek, filp->f_ops->read, get_current_task, memcpy, printk ...(+1) |
| `kernel/sched.c#sched_init` | `kernel/trap/trap.c#do_exception` | 同模块上下文对齐/差异观察 | memset, set_next_timer, uart_puts | do_trap_error, handle_plic_irq, handle_timer, irq_enable, is_interrupt_fault, printk ...(+1) |

#### 内存管理
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `kernel/main.c#kernel_main` | `kernel/main.c#kernel_main` | 同板块同名 | get_free_pages, get_total_pages, mem_init, page_alloc, page_free, proc_create ...(+8) | FAT32_init, binit, do_fork, enable_uart_irq, enable_virtio_irq, get_current_task ...(+13) |
| `kernel/mem.c#mem_init` | `kernel/memory/page_alloc.c#mem_init` | 同板块同名 | memset, uart_int, uart_puts | PAGE_ALIGN_UP, printk |
| `kernel/mem.c#page_alloc` | `kernel/memory/page_alloc.c#page_alloc` | 同板块同名 | memset | 内部闭环逻辑 |
| `kernel/string.c#memmove` | `lib/lib.c#memmove` | 同板块同名 | 内部闭环逻辑 | 内部闭环逻辑 |

#### 中断/异常处理
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `kernel/trap.c#trap_init` | `kernel/trap/trap.c#trap_init` | 同板块同名 | uart_puts | printk, read_csr, write_csr |
| `kernel/trap.c#handle_trap` | `kernel/trap/trap.c#show_regs` | 同模块上下文对齐/差异观察 | handle_exception, handle_interrupt | printk |

#### 系统调用
仅参考项目中观察到该模块代表函数，目标项目没有同模块可对齐证据：
- 参考项目：`usr/user_syscall.c#internal_syscall`，调用摘要：内部闭环逻辑
- 参考项目：`usr/user_syscall.c#printf`，调用摘要：SYSCALL_1, va_end, va_start, vsprintf
- 参考项目：`usr/user_syscall.c#print`，调用摘要：SYSCALL_1, va_end, va_start, vsprintf
- 参考项目：`usr/user_syscall.c#clone`，调用摘要：__clone

#### 文件系统
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `kernel/uart.c#uart_write_reg` | `kernel/fs/loader.c#placeInfo` | 同模块上下文对齐/差异观察 | 内部闭环逻辑 | loadSecData, strcmp |
| `kernel/uart.c#uart_read_reg` | `kernel/fs/bufio.c#bget` | 同模块上下文对齐/差异观察 | 内部闭环逻辑 | acquire_sleeplock, container_of, printk, spin_lock, spin_unlock |

#### 设备驱动
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `kernel/uart.c#uart_init` | `kernel/urat.c#uart_init` | 同板块同名 | uart_read_reg, uart_write_reg | writebyte |
| `kernel/uart.c#uart_putc` | `kernel/urat.c#uart_send` | 同板块同结构 | uart_read_reg, uart_write_reg | readbyte, writebyte |
| `kernel/uart.c#uart_int` | `kernel/driver/virtio.c#free_chain` | 同模块上下文对齐/差异观察 | uart_putc | free_desc |
| `kernel/uart.c#uart_hex` | `lib/printk.c#print_f` | 同模块上下文对齐/差异观察 | uart_putc, uart_puts | uart_send_string, va_end, va_start, vsprintf |

#### 通用模块/其他
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `kernel/string.c#memcpy` | `lib/lib.c#memcpy` | 同板块同名 | 内部闭环逻辑 | 内部闭环逻辑 |
| `kernel/string.c#strcmp` | `lib/lib.c#strcmp` | 同板块同名 | 内部闭环逻辑 | 内部闭环逻辑 |
| `kernel/string.c#memset` | `lib/lib.c#memset` | 同板块同名 | 内部闭环逻辑 | 内部闭环逻辑 |
| `kernel/string.c#strcpy` | `lib/lib.c#strcpy` | 同板块同名 | 内部闭环逻辑 | 内部闭环逻辑 |


好的，审判长。基于提供的证据底座、双方模块画像及AST指纹数据，我将执行最终同源性定性仲裁。

### 1. 宏观判定
- **定性结论**: **独立开发 / 底层架构显著差异**
- **核心依据**: 客观AST相似度仅为0.93%，全项目共同AST指纹仅2个，目标项目覆盖率5.41%，参考项目覆盖率1.12%。双方在核心子系统（进程管理、内存管理、中断处理、文件系统）的共同指纹均为0，表明不存在系统性的结构复用。唯一的微弱重合点位于设备驱动模块，可归因于公共硬件协议。

### 2. 全模块覆盖矩阵解读
- **进程管理**: 双方均存在。共同AST指纹为0。目标项目仅2个函数，实现简单的协作式让步；参考项目有29个函数，包含`fork`、`copy_thread`等复杂原语。功能复杂度与实现体量差异巨大，证据强度为**弱**。
- **任务调度/异步运行时**: 双方均存在。共同AST指纹为0。目标项目为基于时钟中断的简单轮转调度；参考项目包含基于调度类的抽象框架、ELF加载器等。调度模型与核心抽象完全不同，证据强度为**弱**。
- **内存管理**: 双方均存在。共同AST指纹为0。目标项目为简单的页分配/释放；参考项目包含页表分配、FAT32文件系统缓存、VirtIO缓冲区等多层次内存管理。内存模型与子系统交互复杂度不在同一层级，证据强度为**弱**。
- **中断/异常处理**: 双方均存在。共同AST指纹为0。目标项目为扁平化的`handle_trap`分发至异常/中断处理；参考项目`do_exception`直接处理PLIC、定时器、系统调用，并与`printk`日志系统交互。分发机制与处理路径截然不同，证据强度为**弱**。
- **系统调用**: 仅参考项目体现。目标项目无此模块，是双方架构根本性差异的标志，证据强度为**未形成**。
- **文件系统**: 双方均存在。共同AST指纹为0。目标项目无实际文件系统，仅将UART寄存器读写函数归入此模块；参考项目包含完整的FAT32实现、块缓存层和ELF加载器。功能域完全不同，证据强度为**未形成**。
- **设备驱动**: 双方均存在。共同AST指纹为1。双方均有UART驱动，且`uart_putc`与`uart_send`在“轮询状态寄存器-读写数据寄存器”的控制流上存在同构性。这是全案唯一的强信号点，但可被公共硬件协议解释，证据强度为**中**。
- **网络协议栈**: 双方均未体现。证据强度为**未形成**。
- **第三方库/公共依赖**: 双方均存在`string.c`/`lib.c`中的标准C库函数（如`memset`, `memcpy`）。这些函数均为内部闭环逻辑，属于公共知识，不构成同源证据，证据强度为**弱**。

### 3. 底层 AST 关键证据指纹表
| 所属板块 | 目标项目路径 | 参考项目路径 | 证据类型 | 技术说明 |
| :--- | :--- | :--- | :--- | :--- |
| 设备驱动 | `[kernel/uart.c]` 函数 `uart_putc` | `[kernel/urat.c]` 函数 `uart_send` | 同板块同结构 | 双方均呈现“轮询状态寄存器 -> 写入/读取数据寄存器”的UART收发控制流骨架，是唯一的结构重合点。 |
| 设备驱动 | `[kernel/uart.c]` 函数 `uart_init` | `[kernel/urat.c]` 函数 `uart_init` | 同板块同名 | 同名初始化函数，但目标调用`uart_read_reg, uart_write_reg`，参考调用`writebyte`，抽象层不同。 |
| 内存管理 | `[kernel/main.c]` 函数 `kernel_main` | `[kernel/main.c]` 函数 `kernel_main` | 同板块同名 | 内核入口点必然同名，但调用链完全不同，目标初始化基础调度/内存，参考初始化FAT32/VirtIO/PLIC。 |
| 内存管理 | `[kernel/mem.c]` 函数 `page_alloc` | `[kernel/memory/page_alloc.c]` 函数 `page_alloc` | 同板块同名 | 同名页分配函数，但目标包含`memset`清零调用，参考为纯内部闭环逻辑，实现细节不同。 |
| 通用模块/其他 | `[kernel/string.c]` 函数 `memset` | `[lib/lib.c]` 函数 `memset` | 同板块同名 | 标准C库函数，内部闭环逻辑，属于公共知识，不构成同源证据。 |

### 4. 核心代码调用流对比剖析
* **【设备驱动 / UART初始化与发送路径】**
* **底层技术析理**: 这是全案唯一存在结构重合的子系统。目标项目`uart_init`直接调用`uart_read_reg`和`uart_write_reg`进行寄存器级MMIO操作，其`uart_putc`函数通过轮询`uart_read_reg`获取状态后调用`uart_write_reg`发送字符，构建了一个自包含的裸UART驱动。参考项目`uart_init`则调用`writebyte`这一更高层的字节流抽象，其`uart_send`函数通过`readbyte`/`writebyte`完成发送。尽管底层的“轮询-发送”循环骨架相似，但目标项目暴露了寄存器操作接口，而参考项目将其封装在字节读写函数之后，体现了不同的硬件抽象层设计。此外，目标项目的UART驱动自成体系，而参考项目的UART仅作为`printk`格式化输出系统的一个简单后端。
* **代码溯源链接**:
  - 目标项目: `kernel/uart.c#uart_init` -> `kernel/uart.c#uart_putc`
  - 参考项目: `kernel/urat.c#uart_init` -> `kernel/urat.c#uart_send`

* **【内存管理 / 内核入口与页分配路径】**
* **底层技术析理**: 双方均有`kernel_main`和`page_alloc`，但调用流揭示了架构的根本差异。目标项目的`kernel_main`调用`mem_init`、`page_alloc`、`proc_create`、`sched_init`，构建了一个极简的静态内存管理与协作式调度环境。其`page_alloc`在分配后调用`memset`清零。参考项目的`kernel_main`则调用`FAT32_init`、`binit`、`do_fork`、`enable_virtio_irq`，初始化了一个包含文件系统、块缓存、进程分叉和多外设中断的复杂运行时。其`page_alloc`为纯内部闭环逻辑，无清零操作。两者在内存管理的初始化路径、与文件系统/设备驱动的交互复杂度上完全不同。
* **代码溯源链接**:
  - 目标项目: `kernel/main.c#kernel_main` -> `kernel/mem.c#page_alloc`
  - 参考项目: `kernel/main.c#kernel_main` -> `kernel/memory/page_alloc.c#page_alloc`

* **【中断/异常处理 / 陷阱分发路径】**
* **底层技术析理**: 目标项目的`trap_init`仅调用`uart_puts`打印信息，其`handle_trap`作为顶层分发器，调用`handle_exception`和`handle_interrupt`。异常处理通过`uart_hex`/`uart_puts`直接输出寄存器值。参考项目的`trap_init`调用`read_csr`/`write_csr`操作特权寄存器，其`do_exception`函数内部集成了`is_interrupt_fault`判断，并直接调用`handle_plic_irq`、`handle_timer`、`syscall_handler`等具体处理器，通过`printk`格式化输出。目标项目是扁平分发+裸UART输出，参考项目是CSR配置+内联多源处理+格式化日志，中断处理的抽象层次、平台复杂度和输出机制均截然不同。
* **代码溯源链接**:
  - 目标项目: `kernel/trap.c#trap_init` -> `kernel/trap.c#handle_trap`
  - 参考项目: `kernel/trap/trap.c#trap_init` -> `kernel/trap/trap.c#do_exception`

### 5. 单方模块与降权项
- **单方模块**: **系统调用**模块仅存在于参考项目，目标项目完全缺失。这是双方架构设计上的根本性分歧，是独立开发的强有力证据。
- **降权项**: 所有跨模块同名函数（如`kernel_main`, `trap_init`）及通用模块中的标准C库函数（`memset`, `memcpy`, `strcmp`等）均被降权为弱信号。这些函数名是操作系统和C语言编程的通用惯例或标准，其内部闭环逻辑的AST相似性由语言语义和公开算法决定，不构成特定项目的独创性指纹。

### 6. 保守结论
本次审计的客观AST相似度极低（0.93%），全项目仅发现2个共同指纹，且集中于设备驱动模块的UART轮询逻辑。该逻辑高度受限于公开的硬件协议（如16550 UART），属于领域内的公共知识而非独创表达。在进程管理、内存管理、中断处理等核心子系统上，双方在抽象层次、同步策略、功能复杂度上均表现出根本性差异，且参考项目独有的文件系统和系统调用模块进一步拉大了两者的架构鸿沟。综上，现有证据无法支持“同源性复用”或“洗稿”的指控，目标项目应被认定为独立开发，整体风险极低。

------------------------------

