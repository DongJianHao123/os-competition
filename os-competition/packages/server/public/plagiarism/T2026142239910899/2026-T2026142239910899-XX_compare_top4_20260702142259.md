# 🛡️ OS 内核赛自动化代码级架构审计报告

**审计时间**: `2026-07-02 14:22:59`
**参赛作品名称**: `2026-T2026142239910899-XX`
**年份**: `2026`
**学校**: `XX`
**队伍名称**: `T2026142239910899`
**比赛名字**: `OS-Competition`
**比赛子赛道**: `Kernel-Implementation`
**仓库地址**: `https://gitlab.eduxiji.net/educg-group-43501-3132633/T2026142239910899-2376`
**推测开发语言**: `Rust / C (由AST探针自动识别)`
**特征函数量**: `38`

## ⚔️ 底层代码同源性对比 (AST Homology)
> 本部分由专门的代码审查智能体，对该项目与符合年份条件的历届最高相似度项目进行控制流与底层调用对比。新版报告会先给出确定性的全模块覆盖矩阵，再给出 Agent 的分模块解释，避免只围绕少量抽样函数展开。

### 🏆 Top 4 底层对标: vs `2024-ECNU九队-华东师范大学`（华东师范大学 / ECNU九队）

#### 对方作品信息

**对方年份**: `2024`
**对方学校**: `华东师范大学`
**对方队伍名称**: `ECNU九队`
**对方比赛名字**: `OS-Competition`
**对方比赛子赛道**: `Kernel-Implementation`
**对方仓库地址**: `https://gitlab.eduxiji.net/educg-group-26010-2376550/T202410269992492-843`

> 双向覆盖率：共同 AST 指纹: 2；唯一 AST 指纹: 目标项目 37，参考项目 405；覆盖率: 目标项目 5.41%，参考项目 0.49%

## 全模块对比总览（确定性证据）

> 总体覆盖率：共同 AST 指纹: 2；唯一 AST 指纹: 目标项目 37，参考项目 405；覆盖率: 目标项目 5.41%，参考项目 0.49%

### 1. 模块覆盖矩阵
| 模块 | 目标函数数 | 参考函数数 | 共同唯一 AST 指纹 | Target 覆盖 | Base 覆盖 | 状态 |
| :--- | ---: | ---: | ---: | ---: | ---: | :--- |
| 进程管理 | 2 | 53 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |
| 任务调度/异步运行时 | 6 | 32 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |
| 内存管理 | 11 | 91 | 1 | 10.0% | 1.11% | 双方存在且有结构重合 |
| 中断/异常处理 | 2 | 12 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |
| 系统调用 | 0 | 37 | 0 | 0.0% | 0.0% | 仅参考项目体现 |
| 文件系统 | 2 | 102 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |
| 设备驱动 | 6 | 10 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |
| 网络协议栈 | 0 | 0 | 0 | 0.0% | 0.0% | 双方均未体现 |
| 文件系统/第三方库 | 0 | 82 | 0 | 0.0% | 0.0% | 仅参考项目体现 |
| 通用模块/其他 | 9 | 85 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |

### 2. 分模块证据清单

#### 进程管理
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `kernel/sched.c#proc_yield` | `kernel/proc/proc.c#proc_yield` | 同板块同名 | schedule | myproc, proc_sched, spinlock_acquire, spinlock_release |
| `kernel/main.c#test_process_a` | `kernel/proc/proc.c#proc_exit` | 同模块上下文对齐/差异观察 | proc_exit, uart_int, uart_puts | assert, ext4_file_close, ext4_inode_put, fat32_file_close, fat32_inode_put, myproc ...(+6) |

#### 任务调度/异步运行时
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `kernel/sched.c#schedule` | `kernel/fs/fat32/fat32_dir.c#fat32_dir_addInode` | 同模块上下文对齐/差异观察 | 内部闭环逻辑 | (uint16), assert, checksum, fat32_cluster_write, fat32_inode_relocate, memset ...(+3) |
| `kernel/trap.c#handle_interrupt` | `kernel/lib/print.c#printf` | 同模块上下文对齐/差异观察 | timer_triggered, uart_int, uart_puts | (uint64), panic, print_10, print_16, spinlock_acquire, spinlock_release ...(+4) |
| `kernel/sched.c#timer_triggered` | `kernel/trap/trap_user.c#trap_user` | 同模块上下文对齐/差异观察 | schedule, set_next_timer | assert, external_interrupt_handler, intr_on, myproc, printf, proc_exit ...(+10) |
| `kernel/sched.c#sched_init` | `kernel/dev/console.c#cons_intr` | 同模块上下文对齐/差异观察 | memset, set_next_timer, uart_puts | 内部闭环逻辑 |

#### 内存管理
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `kernel/string.c#memmove` | `kernel/lib/string.c#memmove` | 同板块同名 | 内部闭环逻辑 | 内部闭环逻辑 |
| `kernel/mem.c#get_free_pages` | `kernel/fs/fat32/fat32_file.c#fat32_file_stat` | 同板块同结构 | 内部闭环逻辑 | 内部闭环逻辑 |
| `kernel/main.c#kernel_main` | `kernel/proc/exec.c#proc_exec` | 同模块上下文对齐/差异观察 | get_free_pages, get_total_pages, mem_init, page_alloc, page_free, proc_create ...(+8) | ADD_AUXV, ALIGN_DOWN, ALIGN_UP, assert, ext4_dir_path_to_inode, ext4_file_close ...(+20) |
| `kernel/mem.c#mem_init` | `kernel/dev/virtio_disk.c#virtio_disk_init` | 同模块上下文对齐/差异观察 | memset, uart_int, uart_puts | R, assert, memset, spinlock_acquire, spinlock_init, spinlock_release |

#### 中断/异常处理
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `kernel/trap.c#trap_init` | `kernel/trap/trap_kernel.c#trap_init` | 同板块同名 | uart_puts | timer_init |
| `kernel/trap.c#handle_trap` | `kernel/lock/spinlock.c#pop_off` | 同模块上下文对齐/差异观察 | handle_exception, handle_interrupt | assert, intr_get, intr_on, mycpu |

#### 系统调用
仅参考项目中观察到该模块代表函数，目标项目没有同模块可对齐证据：
- 参考项目：`user/initcode.c#main`，调用摘要：get_len, syscall
- 参考项目：`user/lib/syscall.c#get_time`，调用摘要：sys_get_time
- 参考项目：`user/lib/syscall.c#clone`，调用摘要：__clone
- 参考项目：`user/lib/syscall.c#sys_linkat`，调用摘要：syscall

#### 文件系统
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `kernel/uart.c#uart_write_reg` | `kernel/fs/fat32/fat32_dir.c#fat32_dir_getNextInode` | 同模块上下文对齐/差异观察 | 内部闭环逻辑 | assert, fat32_cluster_read, fat32_inode_relocate, memset, read_entry_info, read_entry_name |
| `kernel/uart.c#uart_read_reg` | `kernel/fs/fat32/fat32_dir.c#fat32_dir_link` | 同模块上下文对齐/差异观察 | 内部闭环逻辑 | 内部闭环逻辑 |

#### 设备驱动
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `kernel/uart.c#uart_init` | `kernel/dev/uart.c#uart_init` | 同板块同名 | uart_read_reg, uart_write_reg | spinlock_init, write_reg |
| `kernel/uart.c#uart_getc` | `kernel/dev/uart.c#uart_getc` | 同板块同名 | uart_read_reg | read_reg |
| `kernel/uart.c#uart_puts` | `kernel/dev/uart.c#uart_puts` | 同板块同名 | uart_putc | uart_putc |
| `kernel/uart.c#uart_hex` | `kernel/dev/console.c#cons_write` | 同模块上下文对齐/差异观察 | uart_putc, uart_puts | spinlock_acquire, spinlock_release, uart8250_putc, uart_putc, vm_copyin |

#### 文件系统/第三方库
仅参考项目中观察到该模块代表函数，目标项目没有同模块可对齐证据：
- 参考项目：`kernel/fs/ext4/ext4_sys.c#ext4_sys_ppoll`，调用摘要：assert, myproc, proc_iskilled, proc_sleep, spinlock_acquire, spinlock_release ...(+3)
- 参考项目：`kernel/fs/ext4/ext4_dir.c#ext4_dir_pinode_to_inode`，调用摘要：assert, ext4_inode_dup, ext4_inode_get, ext4_inode_read, ext4_inode_readback, ext4_inode_search ...(+8)
- 参考项目：`kernel/fs/ext4/ext4.c#ext4_init`，调用摘要：assert, buf_read, buf_release, com, ext4_block_read, ext4_inode_init ...(+3)
- 参考项目：`kernel/fs/ext4/ext4_inode.c#ext4_inode_write`，调用摘要：(uint16), EXTENT_LEAF, assert, ext4_block_alloc, ext4_block_write, ext4_inode_writeback ...(+3)

#### 通用模块/其他
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `kernel/string.c#strncmp` | `user/lib/string.c#strncmp` | 同板块同名 | 内部闭环逻辑 | 内部闭环逻辑 |
| `kernel/string.c#strncpy` | `user/lib/string.c#strncpy` | 同板块同名 | 内部闭环逻辑 | HASZERO, memset |
| `kernel/string.c#strcmp` | `user/lib/string.c#strcmp` | 同板块同名 | 内部闭环逻辑 | 内部闭环逻辑 |
| `kernel/string.c#memset` | `kernel/lib/string.c#memset` | 同板块同名 | 内部闭环逻辑 | 内部闭环逻辑 |


好的，我将遵循审计指令，以深度析理模式对本案进行最终同源性定性仲裁。

### 1. 宏观判定
- **定性结论**: **独立开发** (客观 AST 相似度: 0.45%，共同 AST 指纹: 2；唯一 AST 指纹: 目标项目 37，参考项目 405；覆盖率: 目标项目 5.41%，参考项目 0.49%)

### 2. 全模块覆盖矩阵解读
- **进程管理**: 双方均存在。共同 AST 指纹为 0。目标覆盖率 0.0%，参考覆盖率 0.0%。双方虽有同名函数 `proc_yield`，但 AST 结构无重合，调用图谱差异巨大（目标为单节点 `schedule`，参考为多节点含锁操作），表明调度与进程控制流的实现逻辑完全不同。证据强度：**弱**。
- **任务调度/异步运行时**: 双方均存在。共同 AST 指纹为 0。目标覆盖率 0.0%，参考覆盖率 0.0%。目标项目调度器为极简的 `schedule` 闭环，参考项目则分散在中断、控制台、文件系统等多个复杂调用链中，无任何结构级同源性。证据强度：**未形成**。
- **内存管理**: 双方均存在。共同 AST 指纹为 1。目标覆盖率 10.0%，参考覆盖率 1.11%。唯一的共同指纹极可能来自 `memmove` 这类标准库函数的公共实现模式。目标项目为简单的物理页分配 (`page_alloc/free`)，参考项目则包含完整的虚拟内存管理 (UVM/KVM)、页表复制和 mmap 支持，架构层级完全不同。证据强度：**弱**。
- **中断/异常处理**: 双方均存在。共同 AST 指纹为 0。目标覆盖率 0.0%，参考覆盖率 0.0%。目标项目 `trap_init` 仅调用 `uart_puts` 进行简单输出，参考项目则调用 `timer_init` 进行时钟初始化，中断处理流程和依赖对象完全不同。证据强度：**未形成**。
- **系统调用**: 仅参考项目体现。共同 AST 指纹为 0。目标项目无此模块，属于架构性缺失，无法对比。证据强度：**未形成**。
- **文件系统**: 双方均存在。共同 AST 指纹为 0。目标覆盖率 0.0%，参考覆盖率 0.0%。目标项目的“文件系统”实为 UART 寄存器读写函数，被错误归类；参考项目拥有完整的 FAT32 和 ext4 文件系统。两者在领域和实现上毫无关联。证据强度：**未形成**。
- **设备驱动**: 双方均存在。共同 AST 指纹为 0。目标覆盖率 0.0%，参考覆盖率 0.0%。尽管存在多个同名函数（如 `uart_init`, `uart_puts`），但 AST 结构无重合。目标项目是简单的轮询式 UART 驱动，参考项目则包含控制台抽象层、virtio 磁盘驱动等复杂设备模型。证据强度：**弱**。
- **网络协议栈**: 双方均未体现。证据强度：**未形成**。
- **第三方库/公共依赖**: 仅参考项目体现。共同 AST 指纹为 0。目标项目无此模块。参考项目包含 `user/lib` 等用户态库，目标项目无用户态。证据强度：**未形成**。

### 3. 底层 AST 关键证据指纹表
| 所属板块 | 目标项目路径 | 参考项目路径 | 证据类型 | 技术说明 |
| :--- | :--- | :--- | :--- | :--- |
| 内存管理 | `[kernel/string.c]` 函数 `memmove` | `[kernel/lib/string.c]` 函数 `memmove` | 同板块同名 | 双方均为内部闭环逻辑。`memmove` 是 C 标准库函数，其处理内存重叠的算法是公共知识，此重合为 trivial 的公共实现模式，非特定源码复用指纹。 |
| 内存管理 | `[kernel/mem.c]` 函数 `get_free_pages` | `[kernel/fs/fat32/fat32_file.c]` 函数 `fat32_file_stat` | 同板块同结构 | 双方均为内部闭环逻辑。这是极简内核中“获取状态/计数”类函数的常见形态，且参考方属于文件系统模块，归类存疑。此指纹为弱信号，不构成同源铁证。 |
| 设备驱动 | `[kernel/uart.c]` 函数 `uart_puts` | `[kernel/dev/uart.c]` 函数 `uart_puts` | 同板块同名 | 双方均调用 `uart_putc`。`puts` 通过循环调用 `putc` 实现是 UART 驱动的唯一自然方式，属于功能决定的必然设计，而非代码复用。 |
| 设备驱动 | `[kernel/uart.c]` 函数 `uart_getc` | `[kernel/dev/uart.c]` 函数 `uart_getc` | 同板块同名 | 目标调用 `uart_read_reg`，参考调用 `read_reg`。命名差异体现了目标项目自洽的 `uart_` 前缀规范，且底层寄存器操作实现不同（AST 指纹为 0）。 |
| 通用模块/其他 | `[kernel/string.c]` 函数 `memset` | `[kernel/lib/string.c]` 函数 `memset` | 同板块同名 | 双方均为内部闭环逻辑。`memset` 是 C 标准库函数，其实现高度标准化，此同名同结构为公共知识领域的必然重合。 |
| 通用模块/其他 | `[kernel/string.c]` 函数 `strcmp` | `[user/lib/string.c]` 函数 `strcmp` | 同板块同名 | 双方均为内部闭环逻辑。同为 C 标准库函数，实现模式固定，不构成特定项目的复用证据。 |

### 4. 核心代码调用流对比剖析
* **【进程管理/调度路径】**
* **底层技术析理**: 目标项目的 `proc_yield` 仅调用 `schedule`，是一个极简的协作式让步逻辑，无任何锁机制或进程状态查询。参考项目的 `proc_yield` 则深度依赖 `myproc` 获取当前进程结构体，并调用 `spinlock_acquire/release` 进行并发保护，最终调用 `proc_sched`。两者在**同步策略**（无锁 vs 自旋锁）和**对象管理模型**（无进程结构体 vs 复杂的 proc 结构体）上存在根本性差异。
* **代码溯源链接**:
  - 目标项目: `kernel/sched.c#proc_yield`
  - 参考项目: `kernel/proc/proc.c#proc_yield`

* **【设备驱动/UART 初始化路径】**
* **底层技术析理**: 目标项目的 `uart_init` 直接调用 `uart_read_reg` 和 `uart_write_reg` 进行硬件寄存器配置，是一个典型的裸机驱动初始化流程。参考项目的 `uart_init` 则首先调用 `spinlock_init` 初始化锁，再调用 `write_reg`，体现了其驱动模型中的**并发安全设计**。目标项目缺失锁初始化步骤，是其整个内核**同步策略**缺失的缩影，而非刻意删除。
* **代码溯源链接**:
  - 目标项目: `kernel/uart.c#uart_init`
  - 参考项目: `kernel/dev/uart.c#uart_init`

* **【内存管理/基础内存操作路径】**
* **底层技术析理**: 目标项目的 `memmove` 为内部闭环，采用朴素的逐字节拷贝逻辑。参考项目的 `memmove` 虽也为内部闭环，但其项目内其他字符串函数（如 `strlen`）广泛使用了 `HASZERO` 宏进行字对齐优化，这是 musl libc 风格的显著特征。目标项目完全未采用此优化宏，表明其**内存操作模型**的实现思路与参考项目不同源。
* **代码溯源链接**:
  - 目标项目: `kernel/string.c#memmove`
  - 参考项目: `kernel/lib/string.c#memmove`

### 5. 单方模块与降权项
- **单方模块**: 参考项目独有的**系统调用**、**文件系统（FAT32/ext4）**、**第三方库/公共依赖（user/lib）** 模块，目标项目完全缺失。这是两者架构层级和功能完整性上的根本差异，是独立开发的有力证据。
- **降权项**: 所有 C 标准库同名函数（`memset`, `strcmp`, `strncpy` 等）均被降权为公共知识领域的必然重合。跨板块的弱信号（如目标项目的 UART 函数与参考项目的 FAT32 函数对比）因领域完全无关，不构成同源证据，已予排除。

### 6. 保守结论
本次审计的核心结论为**独立开发**。尽管双方在设备驱动、通用模块等板块存在多个同名函数，但客观 AST 指纹重合度极低（0.45%），仅有的 2 个共同指纹均可归因于 C 标准库或极简内核的公共实现模式。通过对内存管理、进程调度、设备驱动等核心子系统进行架构差异剖析，发现双方在**内存模型**（物理页 vs 虚拟内存）、**同步策略**（无锁 vs 自旋锁）、**对象管理模型**（无进程结构体 vs 复杂 proc 结构体）及**功能完整性**（无文件系统/用户态 vs 完整操作系统）上存在根本性异构。现有证据远不足以支持“系统性复用”或“混淆式抄袭”的指控，目标项目更符合一个独立开发的、功能极简的教学用操作系统内核的特征。

------------------------------

