# 🛡️ OS 内核赛自动化代码级架构审计报告

**审计时间**: `2026-07-02 14:22:59`
**参赛作品名称**: `2026-T2026142239910899-XX`
**年份**: `2026`
**学校**: `XX`
**队伍名称**: `T2026142239910899`
**比赛名字**: `OS-Competition`
**比赛子赛道**: `Kernel-Implementation`
**仓库地址**: `https://gitlab.eduxiji.net/educg-group-43501-3132633/T2026142239910899-2376`
**推测开发语言**: `Rust / C (由AST探针自动识别)`
**特征函数量**: `38`

## ⚔️ 底层代码同源性对比 (AST Homology)
> 本部分由专门的代码审查智能体，对该项目与符合年份条件的历届最高相似度项目进行控制流与底层调用对比。新版报告会先给出确定性的全模块覆盖矩阵，再给出 Agent 的分模块解释，避免只围绕少量抽样函数展开。

### 🏆 Top 3 底层对标: vs `2021-TeamNameNotFound-重庆邮电大学`（重庆邮电大学 / TeamNameNotFound）

#### 对方作品信息

**对方年份**: `2021`
**对方学校**: `重庆邮电大学`
**对方队伍名称**: `TeamNameNotFound`
**对方比赛名字**: `OS-Competition`
**对方比赛子赛道**: `Kernel-Implementation`
**对方仓库地址**: `https://gitlab.eduxiji.net/qq976243638/xos`

> 双向覆盖率：共同 AST 指纹: 1；唯一 AST 指纹: 目标项目 37，参考项目 78；覆盖率: 目标项目 2.7%，参考项目 1.28%

## 全模块对比总览（确定性证据）

> 总体覆盖率：共同 AST 指纹: 1；唯一 AST 指纹: 目标项目 37，参考项目 78；覆盖率: 目标项目 2.7%，参考项目 1.28%

### 1. 模块覆盖矩阵
| 模块 | 目标函数数 | 参考函数数 | 共同唯一 AST 指纹 | Target 覆盖 | Base 覆盖 | 状态 |
| :--- | ---: | ---: | ---: | ---: | ---: | :--- |
| 进程管理 | 2 | 14 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |
| 任务调度/异步运行时 | 6 | 4 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |
| 内存管理 | 11 | 34 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |
| 中断/异常处理 | 2 | 2 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |
| 系统调用 | 0 | 0 | 0 | 0.0% | 0.0% | 双方均未体现 |
| 文件系统 | 2 | 9 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |
| 设备驱动 | 6 | 4 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |
| 网络协议栈 | 0 | 0 | 0 | 0.0% | 0.0% | 双方均未体现 |
| 第三方库 | 0 | 18 | 0 | 0.0% | 0.0% | 仅参考项目体现 |
| 通用模块/其他 | 9 | 0 | 0 | 0.0% | 0.0% | 仅目标项目体现 |

### 2. 分模块证据清单

#### 进程管理
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `kernel/main.c#test_process_a` | `kernel/proc/proc.c#proc_wait` | 同模块上下文对齐/差异观察 | proc_exit, uart_int, uart_puts | free_proc, kfree, list_rm, proc_switch |
| `kernel/sched.c#proc_yield` | `kernel/proc/proc.c#proc_init` | 同模块上下文对齐/差异观察 | schedule | init_fd_table, sche_ptr->init |

#### 任务调度/异步运行时
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `kernel/sched.c#schedule` | `kernel/trap/syscall.c#syscall` | 同模块上下文对齐/差异观察 | 内部闭环逻辑 | fs_close, fs_openat, fs_read, proc_clone, proc_execve, proc_exit ...(+2) |
| `kernel/trap.c#handle_interrupt` | `kernel/trap/trap.c#trap_ret_user` | 同模块上下文对齐/差异观察 | timer_triggered, uart_int, uart_puts | csrr, csrw, trap_end |
| `kernel/sched.c#timer_triggered` | `kernel/fs/fat32.c#fat32_read` | 同模块上下文对齐/差异观察 | schedule, set_next_timer | kfree, load_file, memcpy |
| `kernel/sched.c#sched_init` | `kernel/fs/fs.c#fs_close` | 同模块上下文对齐/差异观察 | memset, set_next_timer, uart_puts | 内部闭环逻辑 |

#### 内存管理
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `kernel/sched.c#proc_exit` | `kernel/proc/proc.c#proc_exit` | 同板块同名 | page_free, schedule | free_fd_table, free_segments, kalloc, list_add_front, proc_switch, proc_wait |
| `kernel/main.c#kernel_main` | `kernel/fs/elf.c#load_elf` | 同模块上下文对齐/差异观察 | get_free_pages, get_total_pages, mem_init, page_alloc, page_free, proc_create ...(+8) | (uint64), ROUNDDOWN, flags_trans, kalloc, list_add_back, magic_test ...(+4) |
| `kernel/mem.c#mem_init` | `kernel/batch/app.c#app_init` | 同模块上下文对齐/差异观察 | memset, uart_int, uart_puts | alloc_proc, fs_close, fs_openat, fs_read, init_fd_table, kalloc ...(+6) |
| `kernel/sched.c#proc_create` | `kernel/proc/proc.c#proc_clone` | 同模块上下文对齐/差异观察 | disable_interrupt, enable_interrupt, memset, page_alloc, strncpy, uart_puts | ROUNDDOWN, alloc_proc, copy_segments, copy_trapframe, init_fd_table, kalloc ...(+3) |

#### 中断/异常处理
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `kernel/trap.c#trap_init` | `kernel/trap/trap.c#trap_init` | 同板块同名 | uart_puts | csrr, csrw |
| `kernel/trap.c#handle_trap` | `kernel/trap/syscall.c#sys_write` | 同模块上下文对齐/差异观察 | handle_exception, handle_interrupt | console_putchar |

#### 文件系统
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `kernel/uart.c#uart_write_reg` | `kernel/fs/fat32.c#fname2short` | 同模块上下文对齐/差异观察 | 内部闭环逻辑 | strcpy, strlen, toupper |
| `kernel/uart.c#uart_read_reg` | `kernel/fs/elf.c#magic_test` | 同模块上下文对齐/差异观察 | 内部闭环逻辑 | panic |

#### 设备驱动
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `kernel/uart.c#uart_init` | `kernel/driver/virt_disk.c#disk_read_sector` | 同模块上下文对齐/差异观察 | uart_read_reg, uart_write_reg | memcpy |
| `kernel/uart.c#uart_int` | `kernel/driver/clock.c#clock_init` | 同模块上下文对齐/差异观察 | uart_putc | csrr, csrw, set_next_timer |
| `kernel/uart.c#uart_hex` | `kernel/driver/clock.c#set_next_timer` | 同模块上下文对齐/差异观察 | uart_putc, uart_puts | csrr, set_timer |
| `kernel/uart.c#uart_putc` | `kernel/driver/virt_disk.c#disk_write_sector` | 同模块上下文对齐/差异观察 | uart_read_reg, uart_write_reg | 内部闭环逻辑 |

#### 第三方库
仅参考项目中观察到该模块代表函数，目标项目没有同模块可对齐证据：
- 参考项目：`kernel/libs/sbi.c#sbi_ecall`，调用摘要：内部闭环逻辑
- 参考项目：`kernel/libs/list.c#list_add_back`，调用摘要：内部闭环逻辑
- 参考项目：`kernel/libs/list.c#list_rm_front`，调用摘要：panic
- 参考项目：`kernel/libs/list.c#list_rm`，调用摘要：list_rm_front

#### 通用模块/其他
仅目标项目中观察到该模块代表函数，参考项目没有同模块可对齐证据：
- 目标项目：`kernel/string.c#strncmp`，调用摘要：内部闭环逻辑
- 目标项目：`kernel/string.c#memcmp`，调用摘要：内部闭环逻辑
- 目标项目：`kernel/string.c#strncpy`，调用摘要：内部闭环逻辑
- 目标项目：`kernel/string.c#memcpy`，调用摘要：内部闭环逻辑


好的，审计官。基于提供的客观AST指纹数据、模块画像、证据底座及控辩双方论点，现出具最终同源性定性仲裁报告。

#### 1. 宏观判定
- **定性结论**: **独立开发**
- **核心依据**: 客观AST相似度仅为0.88%，远低于可判定为“同源”或“抄袭”的阈值。双方项目在全项目及所有可比模块中，共同AST指纹数量均为0或1，覆盖率均低于3%。这表明在抽象语法树结构层面，两个项目不存在实质性的代码复用。控方指控主要建立在通用命名和宏观设计模式的相似性上，缺乏底层代码结构重合的“铁证”。

#### 2. 全模块覆盖矩阵解读
- **进程管理**
  1. 双方均存在该模块。
  2. 不存在共同AST指纹。
  3. 目标覆盖率0.0%，参考覆盖率0.0%，说明双方在进程管理相关的数据结构、算法实现上无任何可检测的AST结构重合。
  4. 证据强度：**未形成**。

- **任务调度/异步运行时**
  1. 双方均存在该模块。
  2. 不存在共同AST指纹。
  3. 目标覆盖率0.0%，参考覆盖率0.0%，说明调度器核心逻辑、时钟中断处理等实现路径在AST层面完全不同。
  4. 证据强度：**未形成**。

- **内存管理**
  1. 双方均存在该模块。
  2. 不存在共同AST指纹。
  3. 目标覆盖率0.0%，参考覆盖率0.0%。尽管双方都有`proc_exit`等函数，但其内部调用图谱（`page_free` vs `free_fd_table, free_segments`）揭示了完全不同的资源管理模型，AST结构无重合。
  4. 证据强度：**未形成**。

- **中断/异常处理**
  1. 双方均存在该模块。
  2. 不存在共同AST指纹。
  3. 目标覆盖率0.0%，参考覆盖率0.0%。同名函数`trap_init`的调用图谱（`uart_puts` vs `csrr, csrw`）表明其底层操作完全不同，不存在结构复用。
  4. 证据强度：**未形成**。

- **系统调用**
  1. 双方均未体现该模块。
  2. 不存在共同AST指纹。
  3. 覆盖率均为0.0%，无可比性。
  4. 证据强度：**未形成**。

- **文件系统**
  1. 双方均存在该模块。
  2. 不存在共同AST指纹。
  3. 目标覆盖率0.0%，参考覆盖率0.0%。目标项目文件系统证据仅为UART寄存器读写，而参考项目为FAT32/ELF解析，两者处于不同抽象层次，无结构可比性。
  4. 证据强度：**未形成**。

- **设备驱动**
  1. 双方均存在该模块。
  2. 不存在共同AST指纹。
  3. 目标覆盖率0.0%，参考覆盖率0.0%。目标项目驱动围绕UART，参考项目包含虚拟磁盘和时钟，硬件抽象与交互模型完全不同。
  4. 证据强度：**未形成**。

- **网络协议栈**
  1. 双方均未体现该模块。
  2. 不存在共同AST指纹。
  3. 覆盖率均为0.0%，无可比性。
  4. 证据强度：**未形成**。

- **第三方库/公共依赖**
  1. 仅参考项目体现该模块。
  2. 不存在共同AST指纹。
  3. 目标覆盖率0.0%，参考覆盖率0.0%。参考项目使用了链表、SBI等库，目标项目无此依赖，体现了架构选型的差异。
  4. 证据强度：**未形成**。

#### 3. 底层 AST 关键证据指纹表
| 所属板块 | 目标项目路径 | 参考项目路径 | 证据类型 | 技术说明 |
| :--- | :--- | :--- | :--- | :--- |
| 内存管理 | `kernel/sched.c#proc_exit` | `kernel/proc/proc.c#proc_exit` | 同板块同名，无结构重合 | 函数名相同但调用图谱完全不同。目标项目调用`page_free, schedule`，参考项目调用`free_fd_table, free_segments, kalloc...`，表明两者进程退出时的资源回收模型和对象管理机制存在根本性差异。 |
| 中断/异常处理 | `kernel/trap.c#trap_init` | `kernel/trap/trap.c#trap_init` | 同板块同名，无结构重合 | 函数名相同但调用图谱完全不同。目标项目调用`uart_puts`进行打印，参考项目调用`csrr, csrw`进行CSR寄存器配置，底层硬件交互逻辑无任何AST结构重合。 |
| 任务调度/异步运行时 | `kernel/sched.c#timer_triggered` | `kernel/driver/clock.c#set_next_timer` | 跨板块弱信号 | 此为控方降级区证据。`set_next_timer`是操作系统设置下一次时钟中断的通用API，双方在不同板块（任务调度 vs 设备驱动）中调用此通用函数，不构成同板块结构复用证据。 |
| 设备驱动 | `kernel/uart.c#uart_hex` | `kernel/trap/trap.c#trap` | 跨板块弱信号 | 此为控方降级区证据。目标项目在设备驱动中调用`uart_putc, uart_puts`，参考项目在异常处理中调用`putchar, puts`。这是“格式化输出”通用模式的各自实现，函数前缀、所属模块均不同，无AST结构重合。 |

#### 4. 核心代码调用流对比剖析
* **【进程管理/进程退出路径】**
* **底层技术析理**: 目标项目的`proc_exit`采用极简模型，仅释放页帧并触发调度，无进程间父子关系管理。参考项目的`proc_exit`则实现了完整的进程销毁语义，包括释放文件描述符表、虚拟内存区、加入僵尸进程链表并唤醒父进程。两者在**内存模型**（物理页 vs 虚拟内存对象）、**同步策略**（直接调度 vs 链表操作+等待队列）上存在根本性架构差异。
* **代码溯源链接**:
  - 目标项目: `kernel/sched.c#proc_exit`
  - 参考项目: `kernel/proc/proc.c#proc_exit`

* **【中断/异常处理/陷阱初始化路径】**
* **底层技术析理**: 目标项目的`trap_init`行为是向UART打印信息，未涉及任何硬件寄存器配置，表明其运行环境可能依赖外部仿真器或引导加载程序完成特权级设置。参考项目的`trap_init`直接通过`csrr/csrw`宏操作RISC-V控制状态寄存器，设置中断向量入口。两者在**抽象层次**（无硬件操作 vs 直接寄存器编程）上截然不同。
* **代码溯源链接**:
  - 目标项目: `kernel/trap.c#trap_init`
  - 参考项目: `kernel/trap/trap.c#trap_init`

* **【内存管理/进程创建路径】**
* **底层技术析理**: 目标项目的`proc_create`通过`page_alloc`分配物理页帧，并用`strncpy`/`memset`进行原始内存初始化，是一个扁平的内存操作流程。参考项目的`proc_clone`则通过`kalloc`分配内核对象，并调用`copy_segments`和`copy_trapframe`进行结构化的地址空间和上下文拷贝。两者在**对象管理模型**（物理页帧 vs 内核堆对象）和**创建语义**（新建 vs 克隆）上存在本质区别。
* **代码溯源链接**:
  - 目标项目: `kernel/sched.c#proc_create`
  - 参考项目: `kernel/proc/proc.c#proc_clone`

* **【任务调度/时钟中断处理路径】**
* **底层技术析理**: 目标项目的`timer_triggered`是调度器的时钟滴答处理函数，直接调用`schedule`进行抢占。参考项目的`clock_init`是设备驱动初始化函数，负责配置硬件定时器并设置第一次中断。两者在**调用流**中的角色完全不同（中断处理 vs 驱动初始化），无可比性。
* **代码溯源链接**:
  - 目标项目: `kernel/sched.c#timer_triggered`
  - 参考项目: `kernel/driver/clock.c#clock_init`

* **【文件系统/设备驱动路径】**
* **底层技术析理**: 目标项目的文件系统/设备驱动证据均为UART原语（`uart_write_reg`, `uart_putc`等），属于裸机串口驱动。参考项目则包含FAT32文件系统（`fat32_read`, `dir_search`）和虚拟磁盘驱动（`disk_read_sector`）。两者处于**抽象层次**的两端（字符设备 vs 块设备+文件系统），架构完全不同。
* **代码溯源链接**:
  - 目标项目: `kernel/uart.c#uart_init`
  - 参考项目: `kernel/fs/fat32.c#fat32_init`

#### 5. 单方模块与降权项
- **单方模块**:
  - **第三方库**: 仅存在于参考项目（`kernel/libs/`），包含`sbi_ecall`、链表操作等。目标项目无此模块，表明其未复用参考项目的公共库代码。
  - **通用模块/其他**: 仅存在于目标项目（`kernel/string.c`），包含`strncmp`、`memcpy`等。这些是标准的C库函数实现，属于任何内核项目的通用基础组件，其存在不能作为同源证据。
- **降权项说明**:
  - 控方提出的`set_next_timer`和`uart_hex`/`putchar`等跨板块证据，已按规则降权处理。它们属于操作系统领域的通用API或设计模式，在不同模块中出现是正常现象，不能作为代码结构复用的强证据。
  - 同名函数`proc_exit`和`trap_init`，因调用图谱和底层逻辑完全不同，已从“同板块同名”降权为“无结构重合”，仅作为差异对比的典型案例。

#### 6. 保守结论
综合客观AST指纹覆盖率（0.88%）、全模块零重合矩阵及核心调用流的架构异构性分析，本次审计未发现任何支持代码抄袭或同源性复用的底层结构证据。目标项目[2026-T2026142239910899-XX]是一个极简的、以物理页管理为基础、无文件系统的教学内核；而参考项目[2021-TeamNameNotFound-重庆邮电大学]是一个具备虚拟内存、文件系统和完整进程生命周期的较完整内核。两者在内存模型、同步机制、抽象层次和模块划分上均表现出根本性差异，符合独立开发的特征。控方基于通用命名和宏观模式的指控缺乏客观AST指纹支撑，不予采纳。整体代码原创性风险极低。

------------------------------

