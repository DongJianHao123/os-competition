# 🛡️ OS 内核赛自动化代码级架构审计报告

**审计时间**: `2026-06-27 17:45:02`
**参赛作品名称**: `2026-T202610459999656-郑州大学`
**年份**: `2026`
**学校**: `郑州大学`
**队伍名称**: `T202610459999656`
**比赛名字**: `OS-Competition`
**比赛子赛道**: `Kernel-Implementation`
**仓库地址**: `https://gitlab.eduxiji.net/educg-group-43501-3132633/T202610459999656-3554`
**推测开发语言**: `Rust / C (由AST探针自动识别)`
**特征函数量**: `10439`

## ⚔️ 底层代码同源性对比 (AST Homology)
> 本部分由专门的代码审查智能体，对该项目与符合年份条件的历届最高相似度项目进行控制流与底层调用对比。新版报告会先给出确定性的全模块覆盖矩阵，再给出 Agent 的分模块解释，避免只围绕少量抽样函数展开。

### 🏆 Top 3 底层对标: vs `2025-StarryX-杭州电子科技大学`（杭州电子科技大学 / StarryX）

#### 对方作品信息

**对方年份**: `2025`
**对方学校**: `杭州电子科技大学`
**对方队伍名称**: `StarryX`
**对方比赛名字**: `OS-Competition`
**对方比赛子赛道**: `Kernel-Implementation`
**对方仓库地址**: `https://gitlab.eduxiji.net/educg-group-36002-2710490/T202510336995486-3037`

> 双向覆盖率：共同 AST 指纹: 75；唯一 AST 指纹: 目标项目 8414，参考项目 2897；覆盖率: 目标项目 0.89%，参考项目 2.59%

## 全模块对比总览（确定性证据）

> 总体覆盖率：共同 AST 指纹: 75；唯一 AST 指纹: 目标项目 8414，参考项目 2897；覆盖率: 目标项目 0.89%，参考项目 2.59%

### 1. 模块覆盖矩阵
| 模块 | 目标函数数 | 参考函数数 | 共同唯一 AST 指纹 | Target 覆盖 | Base 覆盖 | 状态 |
| :--- | ---: | ---: | ---: | ---: | ---: | :--- |
| 进程管理 | 1374 | 172 | 7 | 0.65% | 4.4% | 双方存在且有结构重合 |
| 任务调度/异步运行时 | 861 | 480 | 5 | 0.71% | 1.42% | 双方存在且有结构重合 |
| 内存管理 | 2374 | 430 | 7 | 0.34% | 1.8% | 双方存在且有结构重合 |
| 中断/异常处理 | 130 | 70 | 2 | 1.94% | 3.23% | 双方存在且有结构重合 |
| 系统调用 | 613 | 89 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |
| 文件系统 | 2689 | 801 | 6 | 0.28% | 0.97% | 双方存在且有结构重合 |
| 设备驱动 | 312 | 139 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |
| 网络协议栈 | 103 | 77 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |
| 文件系统/第三方库 | 1508 | 164 | 3 | 0.24% | 1.91% | 双方存在且有结构重合 |
| 网络协议栈/第三方库 | 2 | 166 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |
| 第三方库 | 0 | 349 | 0 | 0.0% | 0.0% | 仅参考项目体现 |
| 通用模块/其他 | 473 | 665 | 6 | 1.46% | 1.13% | 双方存在且有结构重合 |

### 2. 分模块证据清单

#### 进程管理
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `os/src/fs/procfs.rs#lookup` | `xcore/src/fs/vfs/proc/pid.rs#lookup` | 同板块同名 | Arc::new, Err, ProcCommInode::new_thread, ProcExeLinkInode::new_task, ProcFdDirInode::new_thread, ProcMapsInode::new_thread ...(+20) | VirtNodeOps::Dir, create_tid_root, get_task_by_id, get_task_by_id(tid.into())
            .and_then, get_task_by_id(tid.into())
            .and_then(|weak| weak.upgrade())
            .map, name.parse::<u32> ...(+5) |
| `os/src/fs/procfs.rs#read_dir` | `xcore/src/fs/vfs/proc/pid.rs#read_dir` | 同板块同名 | String::from, emit_fixed_dir_entries, proc_pid_task_comm_ino, proc_pid_task_exe_ino, proc_pid_task_fd_dir_ino, proc_pid_task_fdinfo_dir_ino ...(+5) | Cow::Owned, proc.pid, proc.pid().to_string, processes, processes()
            .into_iter, processes()
            .into_iter()
            .map |
| `os/src/syscall/proc.rs#sys_wait4` | `xapi/src/task/wait.rs#sys_wait4` | 同板块同名 | Err, SyscallControlFlow::Continue, arg_util::arg_i32, arg_util::arg_u32, args.raw, crate::println! ...(+2) | Err, Ok, WaitOptions::from_bits, WaitPid::Pgid, WaitPid::Pid, child.data::<XProcess> ...(+26) |
| `os/src/thread/scheduler.rs#exit_current` | `arceos/modules/axtask/src/run_queue.rs#exit_current` | 同板块同名 | SCHEDULER.lock, Some, Thread::current, Thread::current().expect, crate::cpu::current_hart_racy, current.release_kernel_fs_on_exit ...(+8) | EXITED_TASKS.current_ref_mut_raw, EXITED_TASKS.current_ref_mut_raw().clear, EXITED_TASKS.current_ref_mut_raw().push_back, WAIT_FOR_EXIT.current_ref_mut_raw, WAIT_FOR_EXIT.current_ref_mut_raw().notify_one, assert! ...(+8) |

#### 任务调度/异步运行时
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `os/src/syscall/futex.rs#sys_futex` | `xapi/src/task/futex.rs#sys_futex` | 同板块同名 | Err, Error::new, Some, SyscallControlFlow::Continue, args.raw, futex_requeue ...(+6) | Err, FutexKey::new, Ok, axtask::yield_now, current, current().task_ext ...(+24) |
| `os/src/syscall/ipc.rs#sys_semctl` | `xapi/src/ipc/sem.rs#sys_semctl` | 同板块同名 | Ok, SEM_REGISTRY.lock, SEM_REGISTRY.lock().semctl, SemCtlRequest::decode, SemCtlRequest::decode(raw).and_then, SyscallControlFlow::Continue ...(+4) | Err, with_ipc_manager! |
| `os/src/runner_timeout.rs#current` | `arceos/api/arceos_posix_api/src/imp/pthread/mod.rs#current` | 同板块同名 | crate::thread::Thread::current, current
                .and_then, current
                .and_then(|thread| thread.user_tid())
                .map, current
                .and_then(|thread| thread.user_tid())
                .map(|tid| tid.as_u32() as u64)
                .unwrap_or, current
                .as_ref, current
                .as_ref()
                .and_then ...(+11) | Self::current_ptr, core::ptr::NonNull::new, core::ptr::NonNull::new(Self::current_ptr()).map, ptr.as_ref |
| `os/src/runner_timeout.rs#current` | `arceos/ulib/axstd/src/thread/multi.rs#current` | 同板块同名 | crate::thread::Thread::current, current
                .and_then, current
                .and_then(|thread| thread.user_tid())
                .map, current
                .and_then(|thread| thread.user_tid())
                .map(|tid| tid.as_u32() as u64)
                .unwrap_or, current
                .as_ref, current
                .as_ref()
                .and_then ...(+11) | Thread::from_id, api::ax_current_task_id |

#### 内存管理
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `os/src/fs/procfs.rs#read_dir` | `arceos/modules/axfs-ng/src/fs/fat/dir.rs#read_dir` | 同板块同名 | Err, Ok, Some, String::from, alloc::format!, child_entries
            .chain ...(+31) | Ok, dir.iter, dir.iter().skip, dir_node.insert_cache, dir_node.lookup_cache, entry.file_name ...(+13) |
| `os/src/mm/vm_space.rs#unmap` | `arceos/modules/axmm/src/aspace.rs#unmap` | 同板块同名 | Err, Ok, TlbFlushOp::Address, TlbFlushOp::Range, assert!, end_va.as_usize ...(+13) | Ok, a.end, a.start, area.backend, area.end, area.start ...(+12) |
| `os/src/mm/vmar.rs#protect` | `arceos/modules/axmm/src/aspace.rs#protect` | 同板块同名 | Ok, VmMarker::decode, crate::mm::tlb::TlbFlushOp::Range, cursor
                .flusher, cursor
                .flusher()
                .issue_tlb_flush, cursor.protect_next ...(+13) | Ok, Some, flags.intersects, self.areas
                .protect, self.areas
                .protect(start, size, |_| Some(flags), &mut self.pt)
                .map_err, self.validate_region |
| `os/src/fs/procfs.rs#read_at` | `xmodules/xcache/src/lib.rs#read_at` | 同板块同名 | Ok, VirtAddr::new, buf
            .chunks_exact_mut, buf
            .chunks_exact_mut(PAGEMAP_ENTRY_SIZE)
            .zip, buf.is_empty, buf.len ...(+16) | (read_len - buf_offset).min, Ok, P::read_page, buf.len, buf.len().min, buf[buf_offset..buf_offset + copy_size]
                .copy_from_slice ...(+5) |

#### 中断/异常处理
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `os/src/hal/irq/external.rs#register_handler` | `arceos/modules/axhal/src/platform/riscv64_visionfive2/irq.rs#register_handler` | 同板块同名 | HANDLERS.lock, Some, assert!, panic!, table[irq].is_some | debug!, with_cause! |
| `os/src/hal/imp/arch/riscv64/irq.rs#local_enabled` | `arceos/modules/axhal/src/arch/riscv/mod.rs#irqs_enabled` | 同板块同结构 | sstatus::read, sstatus::read().sie | sstatus::read, sstatus::read().sie |
| `os/src/hal/irq.rs#disable_local` | `arceos/modules/axhal/src/arch/aarch64/mod.rs#wait_for_irqs` | 同板块同结构 | arch::irq::disable_local | aarch64_cpu::asm::wfi |
| `os/src/hal/imp/arch/loongarch64/irq.rs#local_enabled` | `arceos/modules/axhal/src/arch/loongarch64/mod.rs#irqs_enabled` | 同板块同结构 | crmd::read, crmd::read().ie | crmd::read, crmd::read().ie |

#### 系统调用
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `os/src/syscall/net.rs#accept` | `arceos/ulib/axlibc/src/net.rs#accept` | 同板块同名 | Err, Error::new, Ok, self.state.lock, state.pending.is_empty, state.pending.pop_front ...(+1) | e, sys_accept |
| `os/src/syscall/net.rs#shutdown` | `arceos/ulib/axlibc/src/net.rs#shutdown` | 同板块同名 | self.close_send_side, self.recv_closed.store, self.stream.wake_readiness_waiters | e, sys_shutdown |
| `os/src/syscall/net.rs#connect` | `arceos/ulib/axlibc/src/net.rs#connect` | 同板块同名 | peer.canonical, peer.is_loopback_route, self.connect_datagram, self.connect_external, self.connect_loopback | e, sys_connect |
| `os/src/syscall/net.rs#ktest_socket_timeout_option_contract` | `arceos/api/arceos_posix_api/src/imp/net.rs#sys_getaddrinfo` | 同模块上下文对齐/差异观察 | SocketOptions::new, assert!, assert_eq!, endpoint
        .options
        .lock, endpoint
        .options
        .lock()
        .set_timeout_from_timeval, new_ktest_unix_socket_end ...(+1) | char_ptr_to_str, debug!, syscall_body! |

#### 文件系统
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `os/src/fs/tmpfs.rs#rename` | `arceos/modules/axfs-ng/src/fs/fat/dir.rs#rename` | 同板块同名 | Arc::ptr_eq, Err, Error::new, Ok, Self::contains_dir_in_subtree, Self::validate_rename_replace ...(+35) | Err, dir.rename, dir.rename(src_name, dst_dir.inner.borrow(&fs), dst_name)
            .map_err, dst_dir.downcast, dst_dir.downcast().map_err, dst_dir.inner.borrow ...(+4) |
| `os/src/fs/tmpfs.rs#read_dir` | `xcore/src/fs/vfs/virt_file.rs#read_dir` | 同板块同名 | Err, Error::new, Ok, cookie.saturating_add, emit_tmpfs_dirent, entries
                    .snapshot_from_cookie ...(+9) | Ok, [DOT, DOTDOT]
            .into_iter, [DOT, DOTDOT]
            .into_iter()
            .map, [DOT, DOTDOT]
            .into_iter()
            .map(Cow::Borrowed)
            .chain, [DOT, DOTDOT]
            .into_iter()
            .map(Cow::Borrowed)
            .chain(self.children.keys().map(String::as_str).map(Cow::Borrowed))
            .chain, [DOT, DOTDOT]
            .into_iter()
            .map(Cow::Borrowed)
            .chain(self.children.keys().map(String::as_str).map(Cow::Borrowed))
            .chain(
                self.ops
                    .as_ref()
                    .map(|ops| ops.read_dir())
                    .into_iter()
                    .flatten(),
            )
            .enumerate ...(+20) |
| `os/src/fs/path/dentry.rs#rename` | `xcore/src/fs/vfs/tmp/mod.rs#rename` | 同板块同名 | Ok, children.delete, core::ptr::eq, detach_deleted_child, is_dot_or_dotdot, new_children.delete ...(+16) | Ok, dst_dir.downcast::<Self>, dst_dir.lookup, dst_name.into, dst_node
            .inode
            .as_dir, dst_node
            .inode
            .as_dir()?
            .lock ...(+8) |
| `os/src/fs/miscdevfs.rs#read_dir` | `xcore/src/fs/vfs/tmp/mod.rs#read_dir` | 同板块同名 | Ok, cookie.saturating_add, emit, from_cookie.saturating_sub, index.saturating_add, inode.ino ...(+3) | Ok, entries.iter, entries.iter().enumerate, entries.iter().enumerate().skip, entry.get, inode.metadata.lock ...(+3) |

#### 设备驱动
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `os/src/hal/imp/platform/fdt.rs#probe` | `arceos/modules/axdriver/src/lib.rs#probe` | 同板块同名 | Error::with_message, Fdt::from_ptr, Ok, Some, arch::mm::phys_to_virt, arch::mm::phys_to_virt(dtb_pa).as_ptr::<u8> ...(+13) | for_each_drivers!, self.probe_bus_devices |
| `os/src/drivers/virtio/transport_dma.rs#mmio_phys_to_virt` | `arceos/modules/axdriver/src/ixgbe.rs#mmio_phys_to_virt` | 同板块同名 | NonNull::new, NonNull::new(vaddr).expect, assert!, crate::hal::PhysAddr::new, crate::hal::phys_to_virt_io, crate::hal::phys_to_virt_io(crate::hal::PhysAddr::new(paddr as usize))
            .as_mut_ptr::<u8> | NonNull::new, NonNull::new(phys_to_virt(paddr.into()).as_mut_ptr()).unwrap, paddr.into, phys_to_virt, phys_to_virt(paddr.into()).as_mut_ptr |
| `os/src/hal/boot/bootinfo.rs#push` | `arceos/modules/axdriver/src/structs/static.rs#push` | 同板块同名 | panic!, self.entries.len, self.len | Some, self.0.is_none |
| `os/src/drivers/virtio/transport_dma.rs#unshare` | `arceos/modules/axdriver/src/virtio.rs#unshare` | 同板块同名 | 内部闭环逻辑 | 内部闭环逻辑 |

#### 网络协议栈
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `os/src/net/stack.rs#send_to` | `arceos/ulib/axstd/src/net/udp.rs#send_to` | 同板块同名 | NetworkDeadline::none, Ok, self.send_to_with_deadline | addr.to_socket_addrs, addr.to_socket_addrs()?.next, api::ax_udp_send_to, axerrno::ax_err! |
| `os/src/net/stack.rs#recv_from` | `arceos/ulib/axstd/src/net/udp.rs#recv_from` | 同板块同名 | NetworkDeadline::none, Ok, self.recv_from_with_deadline | api::ax_udp_recv_from |
| `os/src/net/stack.rs#recv` | `xcore/src/net/socket.rs#recv` | 同板块同名 | self.stack.tcp_recv | Ok, tcpsocket.lock, tcpsocket.lock().recv, udpsocket.lock, udpsocket.lock().recv_from, udpsocket.lock().recv_from(buf).map ...(+2) |
| `os/src/net/stack.rs#send` | `arceos/api/arceos_posix_api/src/imp/net.rs#send` | 同板块同名 | self.stack.tcp_send | Ok, tcpsocket.lock, tcpsocket.lock().send, udpsocket.lock, udpsocket.lock().send |

#### 文件系统/第三方库
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `os/src/fs/ext4.rs#rename` | `arceos/crates/axfs-ng-vfs/src/node/dir.rs#rename` | 同板块同名 | Arc::ptr_eq, DstUpdate::ReplacedExistingEntry, Err, Error::new, Error::with_message, Ok ...(+55) | Err, Self::forget_entry, Some, core::ptr::eq, dir.has_children, dst.as_dir ...(+14) |
| `os/src/fs/ext4.rs#create` | `arceos/modules/axfs-ng/src/fs/ext4/inode.rs#create` | 同板块同名 | Arc::new, Default::default, Err, Error::new, Ext4Extent::new, Ext4Inode::default ...(+51) | DirEntry::new_dir, DirEntry::new_file, DirNode::new, Err, FileNode::new, Inode::new ...(+11) |
| `os/third_party/rsext4/src/ext4_backend/file.rs#unlink` | `arceos/crates/lwext4_rust/src/fs.rs#unlink` | 同板块同名 | "/".to_string, DirEntryIterator::new, Some, blocks.values, child_name.as_bytes, fs
        .modify_inode ...(+21) | Err, Ext4Error::new, Ok, child_ref.dec_nlink, child_ref.is_dir, child_ref.nlink ...(+8) |
| `os/third_party/rsext4/src/ext4_backend/file.rs#link` | `arceos/crates/lwext4_rust/src/fs.rs#link` | 同板块同名 | "/".to_string, DirEntryIterator::new, Some, blocks.values, copied_ft.is_some, copied_ft.unwrap_or_else ...(+30) | Err, Ext4Error::new, Ok, child_ref.inc_nlink, child_ref.is_dir, self.inode_ref ...(+1) |

#### 网络协议栈/第三方库
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `os/src/net/stack.rs#smoltcp_now` | `arceos/modules/axnet/src/smoltcp_impl/tcp.rs#connect` | 同模块上下文对齐/差异观察 | Instant::from_micros, crate::time::uptime_us, crate::time::uptime_us().min | Err, Ok, Ok::<(IpEndpoint, IpEndpoint), NetError>, SOCKET_SET
                .with_socket_mut::<tcp::Socket, _, _>, SOCKET_SET.add, SocketSetWrapper::new_tcp_socket ...(+33) |
| `os/src/net/stack.rs#smoltcp_duration_to_capped_us` | `arceos/modules/axnet/src/smoltcp_impl/dns.rs#query` | 同模块上下文对齐/差异观察 | delay.total_micros, delay.total_micros().min | Err, IpAddr::from, Ok, SOCKET_SET
            .with_socket_mut::<dns::Socket, _, _>, SOCKET_SET
            .with_socket_mut::<dns::Socket, _, _>(handle, |socket| {
                socket.start_query(iface.lock().context(), name, query_type)
            })
            .map_err, SOCKET_SET.poll_interfaces ...(+11) |

#### 第三方库
仅参考项目中观察到该模块代表函数，目标项目没有同模块可对齐证据：
- 参考项目：`arceos/crates/page_table_multiarch/page_table_multiarch/src/bits64.rs#map_region`，调用摘要：Err, M::flush_tlb, Ok, PageSize::Size1G.is_aligned, PageSize::Size2M.is_aligned, PageSize::Size4K.is_aligned ...(+12)
- 参考项目：`arceos/crates/axdriver_crates/axdriver_virtio/src/net.rs#try_new`，调用摘要：Err, InnerDev::new, InnerDev::new(transport).map_err, NetBufPool::new, Ok, Some ...(+14)
- 参考项目：`arceos/crates/page_table_multiarch/page_table_multiarch/src/bits64.rs#protect_region`，调用摘要：Err, Ok, TlbFlushAll::new, assert!, error!, self.protect ...(+5)
- 参考项目：`arceos/crates/page_table_multiarch/page_table_multiarch/src/arch/loongarch64.rs#flush_tlb`，调用摘要：asm!

#### 通用模块/其他
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `os/src/logger.rs#read_bytes` | `arceos/modules/axhal/src/platform/riscv64_qemu_virt/console.rs#read_bytes` | 同板块同名 | out.is_empty, out.len, out.len().min, out[..first_len].copy_from_slice, out[first_len..].copy_from_slice, self.ring_index | bytes.iter_mut, bytes.iter_mut().take, sbi_rt::legacy::console_getchar |
| `os/src/logger.rs#log` | `arceos/ulib/axlibc/c/log.c#log` | 同板块同名 | StackLogLine::<STACK_LOG_LINE_CAPACITY>::new, crate::printk::write_bytes, fmt::write, format_args!, line.as_bytes, push ...(+2) | __builtin_fma, __math_divzero, __math_invalid, asdouble, asuint64, eval_as_double ...(+2) |
| `os/src/logger.rs#write_bytes` | `arceos/modules/axhal/src/platform/riscv64_qemu_virt/console.rs#write_bytes` | 同板块同名 | bytes.is_empty, bytes.len, bytes.len().min, self.buf[..bytes.len() - first_len].copy_from_slice, self.buf[start..start + first_len].copy_from_slice, self.ring_index | bytes.as_ptr, bytes.len, bytes.to_vec, kernel_bytes.len, try_write_bytes |
| `os/src/logger.rs#write_str` | `arceos/modules/axlog/src/lib.rs#write_str` | 同板块同名 | Ok, remain.min, s.as_bytes, s.len, self.buf.len, self.buf.len().saturating_sub ...(+1) | Ok, cfg_if::cfg_if! |


# 最终同源性定性仲裁报告

**案号：** 2026-T202610459999656（郑州大学） vs 2025-StarryX（杭州电子科技大学）
**审计日期：** 2026-01-24
**审计依据：** 客观 AST 相似度 0.67%，双向覆盖率均低于 3%

---

## 1. 宏观判定

- **定性结论**: **独立开发，底层架构存在显著差异**
- **核心数据**: 客观 AST 相似度 0.67%；共同 AST 指纹仅 75 个；目标项目覆盖率 0.89%，参考项目覆盖率 2.59%
- **判定依据**: 双方在进程模型、内存管理、文件系统、同步原语等核心子系统上采用完全不同的抽象层次与对象管理模型。控方所指控的“同板块底层逻辑一致”项均属于 Linux ABI 强制语义、Rust 原子类型标准 API 或 trivial getter/setter 的必然形态，不构成实质性代码复用证据。

---

## 2. 全模块覆盖矩阵解读

### 进程管理
- **双方均存在**: 是（目标 1374 函数，参考 172 函数）
- **共同 AST 指纹**: 7 个
- **覆盖率**: 目标 0.65%，参考 4.4%
- **证据强度**: **弱**
- **解读**: 共同指纹集中于 POSIX 强制系统调用（`sys_wait4`、`sys_exit`、`sys_getpid` 等）及 VFS 通用接口（`lookup`、`read_dir`、`read_at`）。目标项目使用 `SyscallControlFlow::Continue`、`arg_util`、`VmWriter` 等自有基础设施；参考项目使用 `syscall_body!`、`XProcess::from_thread`、`VirtNodeOps` 等完全不同的调用框架。函数同名是领域强制，调用图谱无交集。

### 任务调度/异步运行时
- **双方均存在**: 是（目标 861 函数，参考 480 函数）
- **共同 AST 指纹**: 5 个
- **覆盖率**: 目标 0.71%，参考 1.42%
- **证据强度**: **弱**
- **解读**: 共同指纹涉及 `sys_futex`、`sys_semctl` 等 POSIX 标准接口及 `current` 线程获取原语。目标项目通过 `crate::thread::Thread::current` 获取线程对象后调用 `.user_tid()` 方法；参考项目通过 `Self::current_ptr` 获取裸指针后转为 `NonNull`。线程局部存储访问路径与对象模型完全不同。

### 内存管理
- **双方均存在**: 是（目标 2374 函数，参考 430 函数）
- **共同 AST 指纹**: 7 个
- **覆盖率**: 目标 0.34%，参考 1.8%
- **证据强度**: **弱**
- **解读**: 共同指纹出现在 `unmap`、`protect` 等虚拟内存操作及 `read_at` 等通用接口。目标项目使用 `VmMarker::decode`、`TlbFlushOp::Range`、`cursor.protect_next` 等自研抽象；参考项目使用 `self.areas.protect`、`self.validate_region` 等基于区域的管理模型。TLB 刷新策略与页表遍历机制存在结构性差异。

### 中断/异常处理
- **双方均存在**: 是（目标 130 函数，参考 70 函数）
- **共同 AST 指纹**: 2 个
- **覆盖率**: 目标 1.94%，参考 3.23%
- **证据强度**: **未形成**
- **解读**: 共同指纹为 RISC-V/LoongArch 架构的标准 CSR 寄存器读取操作（`sstatus::read().sie`、`crmd::read().ie`），这是硬件架构决定的唯一写法，不构成软件复用证据。

### 系统调用
- **双方均存在**: 是（目标 613 函数，参考 89 函数）
- **共同 AST 指纹**: 0 个
- **覆盖率**: 目标 0.0%，参考 0.0%
- **证据强度**: **未形成**
- **解读**: 双方系统调用分发机制无任何共同 AST 指纹。目标项目使用 `SyscallControlFlow` 控制流抽象；参考项目使用 `syscall_body!` 宏。参数解析、错误处理、返回值传递路径完全不同。

### 文件系统
- **双方均存在**: 是（目标 2689 函数，参考 801 函数）
- **共同 AST 指纹**: 6 个
- **覆盖率**: 目标 0.28%，参考 0.97%
- **证据强度**: **弱**
- **解读**: 共同指纹出现在 `rename`、`read_dir` 等 VFS 通用操作。目标项目使用 `Arc::ptr_eq`、`Self::validate_rename_replace`、`emit_tmpfs_dirent` 等自研工具；参考项目使用 `dir.rename`、`dst_dir.downcast`、`Cow::Borrowed` 等不同抽象。Dentry 缓存与 Inode 管理模型异构。

### 设备驱动
- **双方均存在**: 是（目标 312 函数，参考 139 函数）
- **共同 AST 指纹**: 0 个
- **覆盖率**: 目标 0.0%，参考 0.0%
- **证据强度**: **未形成**
- **解读**: 双方设备驱动模型无任何共同 AST 指纹。目标项目使用 FDT 设备树探测；参考项目使用 `for_each_drivers!` 宏驱动注册机制。

### 网络协议栈
- **双方均存在**: 是（目标 103 函数，参考 77 函数）
- **共同 AST 指纹**: 0 个
- **覆盖率**: 目标 0.0%，参考 0.0%
- **证据强度**: **未形成**
- **解读**: 双方网络协议栈无任何共同 AST 指纹。目标项目使用 `NetworkDeadline` 抽象；参考项目使用 `SOCKET_SET` 全局套接字集合。

### 第三方库/公共依赖
- **双方均存在**: 目标项目体现于 `文件系统/第三方库`（1508 函数）和 `网络协议栈/第三方库`（2 函数）；参考项目体现于 `文件系统/第三方库`（164 函数）、`网络协议栈/第三方库`（166 函数）及独立 `第三方库`（349 函数）
- **共同 AST 指纹**: 3 个（均位于文件系统/第三方库板块）
- **覆盖率**: 目标 0.24%，参考 1.91%
- **证据强度**: **弱（且必须降权）**
- **解读**: 共同指纹涉及 ext4 文件系统的 `rename`、`create`、`unlink`、`link` 操作。目标项目使用 `rsext4` 第三方库；参考项目使用 `lwext4_rust` 第三方库。两者均是对同一 ext4 标准的独立 Rust 封装，函数签名与调用流由 ext4 磁盘格式规范决定，不构成互相抄袭。

---

## 3. 底层 AST 关键证据指纹表

| 所属板块 | 目标项目路径 | 参考项目路径 | 证据类型 | 技术说明 |
| :--- | :--- | :--- | :--- | :--- |
| 进程管理 | `os/src/thread/thread_local.rs#clear_child_tid` | `xcore/src/task/proc.rs#clear_child_tid` | 同板块同名同结构 | Linux `set_tid_address` 标准语义；`AtomicU64::load` 是 Rust 唯一标准读法；目标位于 TLS 层，参考位于 PCB 层，抽象层次不同 |
| 进程管理 | `os/src/process.rs#pid` | `xmodules/xprocess/src/process.rs#pid` | 同板块同名 | Trivial getter，任何 PCB 均需提供 `pid()` 访问器；AST 必然内部闭环 |
| 进程管理 | `os/src/process.rs#is_zombie` | `xmodules/xprocess/src/process.rs#is_zombie` | 同板块同名同结构 | 目标字段名 `zombie_notified`，参考字段名 `is_zombie`，命名不同；`AtomicBool::load` 是标准状态机读法 |
| 进程管理 | `os/src/thread/thread_local.rs#set_clear_child_tid` | `xcore/src/task/proc.rs#set_clear_child_tid` | 同板块同名同结构 | 与 `clear_child_tid` 配对的标准 setter；`AtomicU64::store` 是唯一标准写法 |
| 进程管理 | `os/src/syscall/proc.rs#sys_wait4` | `xapi/src/task/wait.rs#sys_wait4` | 同板块同名 | POSIX 强制系统调用；目标使用 `arg_util::arg_i32`，参考使用 `WaitOptions::from_bits`，参数解析路径不同 |
| 进程管理 | `os/src/thread/scheduler.rs#exit_current` | `arceos/modules/axtask/src/run_queue.rs#exit_current` | 同板块同名 | 调度器通用原语；目标使用 `SCHEDULER.lock`，参考使用 `EXITED_TASKS.current_ref_mut_raw`，队列管理模型不同 |
| 任务调度 | `os/src/syscall/futex.rs#sys_futex` | `xapi/src/task/futex.rs#sys_futex` | 同板块同名 | POSIX futex 系统调用；目标使用 `futex_requeue`，参考使用 `FutexKey::new`，futex 哈希表实现不同 |
| 内存管理 | `os/src/mm/vm_space.rs#unmap` | `arceos/modules/axmm/src/aspace.rs#unmap` | 同板块同名 | 虚拟内存通用操作；目标使用 `TlbFlushOp::Address`，参考使用 `area.backend`，区域管理模型不同 |
| 内存管理 | `os/src/mm/vmar.rs#protect` | `arceos/modules/axmm/src/aspace.rs#protect` | 同板块同名 | 内存保护通用操作；目标使用 `VmMarker::decode`，参考使用 `flags.intersects`，权限位编码不同 |
| 中断处理 | `os/src/hal/imp/arch/riscv64/irq.rs#local_enabled` | `arceos/modules/axhal/src/arch/riscv/mod.rs#irqs_enabled` | 同板块同结构 | RISC-V `sstatus.sie` 位读取，硬件架构决定的唯一写法 |
| 文件系统 | `os/src/fs/tmpfs.rs#rename` | `arceos/modules/axfs-ng/src/fs/fat/dir.rs#rename` | 同板块同名 | VFS rename 通用操作；目标为 tmpfs 实现，参考为 FAT 实现，底层文件系统类型不同 |
| 文件系统/第三方库 | `os/third_party/rsext4/src/ext4_backend/file.rs#unlink` | `arceos/crates/lwext4_rust/src/fs.rs#unlink` | 同板块同名 | ext4 标准操作；目标使用 `rsext4` 库，参考使用 `lwext4_rust` 库，是不同的第三方封装 |

---

## 4. 核心代码调用流对比剖析

### 【进程管理 / wait4 系统调用路径】

**底层技术析理**:
目标项目 `sys_wait4` 通过 `arg_util::arg_i32` / `arg_util::arg_u32` 解析参数，调用 `do_wait4` 内部函数，返回 `SyscallControlFlow::Continue` 控制流。参考项目 `sys_wait4` 通过 `WaitOptions::from_bits` 解析选项位，使用 `WaitPid::Pid` / `WaitPid::Pgid` 枚举区分等待目标，通过 `child.data::<XProcess>()` 动态类型转换获取进程对象。两者在参数解析策略、进程匹配模型、错误处理路径上完全不同。

**代码溯源链接**:
- 目标项目: `os/src/syscall/proc.rs#sys_wait4`
- 参考项目: `xapi/src/task/wait.rs#sys_wait4`

---

### 【进程管理 / 调度器 exit_current 路径】

**底层技术析理**:
目标项目 `exit_current` 通过 `SCHEDULER.lock` 获取全局调度器锁，调用 `Thread::current()` 获取当前线程对象，执行 `release_kernel_fs_on_exit` 释放内核文件系统资源，通过 `set_state` 切换线程状态。参考项目 `exit_current` 通过 `EXITED_TASKS.current_ref_mut_raw()` 获取当前任务裸指针，调用 `push_back` 加入退出队列，通过 `WAIT_FOR_EXIT.notify_one` 唤醒等待者，最后调用 `axhal::misc::terminate` 终止执行。两者在调度器数据结构、资源释放顺序、退出通知机制上完全不同。

**代码溯源链接**:
- 目标项目: `os/src/thread/scheduler.rs#exit_current`
- 参考项目: `arceos/modules/axtask/src/run_queue.rs#exit_current`

---

### 【内存管理 / unmap 虚拟内存操作路径】

**底层技术析理**:
目标项目 `unmap` 通过 `TlbFlushOp::Address` 或 `TlbFlushOp::Range` 指定 TLB 刷新粒度，使用 `assert!` 校验地址对齐，通过 `end_va.as_usize` 计算范围边界。参考项目 `unmap` 通过 `area.backend` 获取内存后端对象，使用 `area.start` / `area.end` 遍历区域列表，基于区域（area）模型进行解除映射。两者在地址空间管理粒度（基于范围 vs 基于区域）、TLB 刷新策略、后端存储模型上存在结构性差异。

**代码溯源链接**:
- 目标项目: `os/src/mm/vm_space.rs#unmap`
- 参考项目: `arceos/modules/axmm/src/aspace.rs#unmap`

---

### 【文件系统 / procfs lookup 路径】

**底层技术析理**:
目标项目 `lookup` 通过 `ProcCommInode::new_thread`、`ProcExeLinkInode::new_task`、`ProcFdDirInode::new_thread` 等类型化 Inode 构造函数创建 procfs 节点，使用 `Arc::new` 进行引用计数管理。参考项目 `lookup` 通过 `get_task_by_id` 获取任务弱引用后调用 `upgrade` 升级为强引用，使用 `VirtNodeOps::Dir` 枚举标识节点类型，通过 `create_tid_root` 创建线程目录根。两者在 procfs 节点类型体系、任务查找机制、引用计数策略上完全不同。

**代码溯源链接**:
- 目标项目: `os/src/fs/procfs.rs#lookup`
- 参考项目: `xcore/src/fs/vfs/proc/pid.rs#lookup`

---

## 5. 单方模块与降权项

### 仅参考项目体现的模块
- **第三方库**（349 函数）：目标项目无独立第三方库模块，其第三方依赖分散于 `文件系统/第三方库` 和 `网络协议栈/第三方库` 中。参考项目的 `page_table_multiarch`、`axdriver_virtio` 等第三方库在目标项目中无对应模块，不构成可比对证据。

### 降权项说明
以下证据类型因属于领域强制接口或通用范式，**依法降权为弱信号，不作为主证据**：

| 降权项 | 降权原因 |
| :--- | :--- |
| 所有 POSIX 系统调用同名函数（`sys_wait4`、`sys_exit`、`sys_getpid`、`sys_getppid`、`sys_rt_sigprocmask` 等） | POSIX/Linux ABI 强制命名，任何兼容内核必须使用相同函数名 |
| 所有 VFS 通用接口同名函数（`lookup`、`read_dir`、`read_at`、`write_at`、`rename` 等） | VFS 层通用操作命名，由操作系统领域惯例决定 |
| 所有原子操作（`.load`、`.store`） | Rust `AtomicBool`/`AtomicU64` 等类型的唯一标准 API，不存在第二种写法 |
| 所有 trivial getter/setter（`pid`、`pgid`、`sid`、`process`、`thread` 等） | 任何进程控制块必须提供的基础访问器，AST 必然呈现内部闭环或简单字段访问 |
| 所有架构相关 CSR 读取（`sstatus::read().sie`、`crmd::read().ie`） | RISC-V/LoongArch 硬件规范决定的唯一寄存器访问方式 |
| 跨板块同名函数（如目标 `os/src/fs/procfs.rs#read_dir` vs 参考 `arceos/modules/axfs-ng/src/fs/fat/dir.rs#read_dir`） | 所属子系统不同（procfs vs FAT），调用对象与数据结构无交集 |
| 第三方 ext4 库函数（`rsext4` vs `lwext4_rust`） | 均为对 ext4 磁盘格式标准的独立 Rust 封装，函数签名由 ext4 规范决定 |

---

## 6. 保守结论

本案客观 AST 相似度仅 0.67%，双向覆盖率均低于 3%，在统计学意义上已排除大规模代码复制的可能性。控方所指控的“同板块底层逻辑一致”项——`clear_child_tid` 原子读写、`pid` 内部闭环、`is_zombie` 状态判定——均属于 Linux ABI 强制语义、Rust 原子类型标准 API 或 trivial getter 的必然实现形态，任何独立开发的兼容内核均会呈现相同 AST 结构。双方在进程模型（`SyscallControlFlow` vs `syscall_body!`）、内存管理（基于范围 vs 基于区域）、文件系统（类型化 Inode vs `VirtNodeOps` 枚举）、同步原语（`Waiter::new_pair` vs `current_run_queue`）等核心子系统上采用完全不同的抽象层次与对象管理模型。综合判定：**双方为独立开发，不存在实质性代码复用，底层架构存在显著差异。**

------------------------------

