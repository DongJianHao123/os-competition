# 🛡️ OS 内核赛自动化代码级架构审计报告

**审计时间**: `2026-06-27 17:45:02`
**参赛作品名称**: `2026-T202610459999656-郑州大学`
**年份**: `2026`
**学校**: `郑州大学`
**队伍名称**: `T202610459999656`
**比赛名字**: `OS-Competition`
**比赛子赛道**: `Kernel-Implementation`
**仓库地址**: `https://gitlab.eduxiji.net/educg-group-43501-3132633/T202610459999656-3554`
**推测开发语言**: `Rust / C (由AST探针自动识别)`
**特征函数量**: `10439`

## ⚔️ 底层代码同源性对比 (AST Homology)
> 本部分由专门的代码审查智能体，对该项目与符合年份条件的历届最高相似度项目进行控制流与底层调用对比。新版报告会先给出确定性的全模块覆盖矩阵，再给出 Agent 的分模块解释，避免只围绕少量抽样函数展开。

### 🏆 Top 5 底层对标: vs `2025-AstranciA-哈尔滨工业大学深圳`（哈尔滨工业大学（深圳） / AstranciA）

#### 对方作品信息

**对方年份**: `2025`
**对方学校**: `哈尔滨工业大学（深圳）`
**对方队伍名称**: `AstranciA`
**对方比赛名字**: `OS-Competition`
**对方比赛子赛道**: `Kernel-Implementation`
**对方仓库地址**: `https://gitlab.eduxiji.net/educg-group-36002-2710490/T202518123995667-1436`

> 双向覆盖率：共同 AST 指纹: 71；唯一 AST 指纹: 目标项目 8414，参考项目 3750；覆盖率: 目标项目 0.84%，参考项目 1.89%

## 全模块对比总览（确定性证据）

> 总体覆盖率：共同 AST 指纹: 71；唯一 AST 指纹: 目标项目 8414，参考项目 3750；覆盖率: 目标项目 0.84%，参考项目 1.89%

### 1. 模块覆盖矩阵
| 模块 | 目标函数数 | 参考函数数 | 共同唯一 AST 指纹 | Target 覆盖 | Base 覆盖 | 状态 |
| :--- | ---: | ---: | ---: | ---: | ---: | :--- |
| 进程管理 | 1374 | 112 | 4 | 0.37% | 4.35% | 双方存在且有结构重合 |
| 任务调度/异步运行时 | 861 | 455 | 7 | 1.0% | 2.18% | 双方存在且有结构重合 |
| 内存管理 | 2374 | 343 | 2 | 0.1% | 0.65% | 双方存在且有结构重合 |
| 中断/异常处理 | 130 | 71 | 2 | 1.94% | 3.51% | 双方存在且有结构重合 |
| 系统调用 | 613 | 136 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |
| 文件系统 | 2689 | 620 | 4 | 0.19% | 0.82% | 双方存在且有结构重合 |
| 设备驱动 | 312 | 142 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |
| 网络协议栈 | 103 | 61 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |
| 文件系统/第三方库 | 1508 | 775 | 4 | 0.32% | 0.63% | 双方存在且有结构重合 |
| 网络协议栈/第三方库 | 2 | 217 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |
| 第三方库 | 0 | 1155 | 0 | 0.0% | 0.0% | 仅参考项目体现 |
| 通用模块/其他 | 473 | 673 | 7 | 1.7% | 1.29% | 双方存在且有结构重合 |

### 2. 分模块证据清单

#### 进程管理
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `os/src/syscall/proc.rs#sys_clone` | `AstrancE/ulib/axmono/src/syscall/process.rs#sys_clone` | 同板块同名 | CloneFlags::from_raw, CloneFlags::from_raw(flags).and_then, Err, Error::new, Ok, SyscallControlFlow::Continue ...(+12) | CloneFlags::from_bits_retain, Ok, Some, child_task.task_ext, child_task.task_ext().thread.process, child_task.task_ext().thread.process().pid ...(+1) |
| `os/src/syscall/proc.rs#sys_wait4` | `AstrancE/ulib/axsyscall/src/syscall_imp/process.rs#sys_wait4` | 同板块同名 | Err, SyscallControlFlow::Continue, arg_util::arg_i32, arg_util::arg_u32, args.raw, crate::println! ...(+2) | axmono::syscall::process::sys_wait4 |
| `os/src/thread/scheduler.rs#exit_current` | `AstrancE/modules/axtask/src/run_queue.rs#exit_current` | 同板块同名 | SCHEDULER.lock, Some, Thread::current, Thread::current().expect, crate::cpu::current_hart_racy, current.release_kernel_fs_on_exit ...(+8) | EXITED_TASKS.current_ref_mut_raw, EXITED_TASKS.current_ref_mut_raw().clear, EXITED_TASKS.current_ref_mut_raw().push_back, WAIT_FOR_EXIT.current_ref_mut_raw, WAIT_FOR_EXIT.current_ref_mut_raw().notify_one, assert! ...(+8) |
| `os/src/syscall/proc.rs#sys_tkill` | `AstrancE/ulib/axmono/src/syscall/process.rs#sys_tkill` | 同板块同名 | Err, Ok, SyscallControlFlow::Continue, arg_util::arg_i32, args.raw, do_tkill ...(+1) | task::signal::sys_tkill |

#### 任务调度/异步运行时
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `os/src/runner_timeout.rs#current` | `AstrancE/api/arceos_posix_api/src/imp/pthread/mod.rs#current` | 同板块同名 | crate::thread::Thread::current, current
                .and_then, current
                .and_then(|thread| thread.user_tid())
                .map, current
                .and_then(|thread| thread.user_tid())
                .map(|tid| tid.as_u32() as u64)
                .unwrap_or, current
                .as_ref, current
                .as_ref()
                .and_then ...(+11) | Self::current_ptr, core::ptr::NonNull::new, core::ptr::NonNull::new(Self::current_ptr()).map, ptr.as_ref |
| `os/src/runner_timeout.rs#current` | `AstrancE/ulib/axstd/src/thread/multi.rs#current` | 同板块同名 | crate::thread::Thread::current, current
                .and_then, current
                .and_then(|thread| thread.user_tid())
                .map, current
                .and_then(|thread| thread.user_tid())
                .map(|tid| tid.as_u32() as u64)
                .unwrap_or, current
                .as_ref, current
                .as_ref()
                .and_then ...(+11) | Thread::from_id, api::ax_current_task_id |
| `os/src/runner_timeout.rs#current` | `AstrancE/modules/axtask/src/api.rs#current` | 同板块同名 | crate::thread::Thread::current, current
                .and_then, current
                .and_then(|thread| thread.user_tid())
                .map, current
                .and_then(|thread| thread.user_tid())
                .map(|tid| tid.as_u32() as u64)
                .unwrap_or, current
                .as_ref, current
                .as_ref()
                .and_then ...(+11) | CurrentTask::get |
| `os/src/syscall/mm.rs#sys_mmap` | `AstrancE/ulib/axmono/src/syscall/mm.rs#sys_mmap` | 同板块同名 | SyscallControlFlow::Continue, do_mmap, log::debug! | 0x0.into, Arc::new, Err, MmapFlags::from_bits_retain, MmapPerm::from_bits_retain, MmapResource::file ...(+11) |

#### 内存管理
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `os/src/mm/vm_space.rs#unmap` | `AstrancE/modules/axmm/src/backend/mod.rs#unmap` | 同板块同名 | Err, Ok, TlbFlushOp::Address, TlbFlushOp::Range, assert!, end_va.as_usize ...(+13) | Self::unmap_alloc, Self::unmap_linear, va_type.clone |
| `os/src/mm/vmar.rs#protect` | `AstrancE/modules/axmm/src/aspace/mod.rs#protect` | 同板块同名 | Ok, VmMarker::decode, crate::mm::tlb::TlbFlushOp::Range, cursor
                .flusher, cursor
                .flusher()
                .issue_tlb_flush, cursor.protect_next ...(+13) | Ok, Some, old_flags.protect, self.areas
            .protect, self.areas
            .protect(
                start,
                size,
                |old_flags| Some(old_flags.protect(flags)),
                &mut self.pt,
            )
            .map_err, self.populate_area |
| `os/src/mm/heap/allocator.rs#alloc` | `AstrancE/modules/axalloc/src/lib.rs#alloc` | 同板块同名 | CommonSizeClass::from_size, CommonSizeClass::from_size(size).ok_or, Err, GLOBAL_HEAP_ALLOCATOR.lock, GLOBAL_HEAP_ALLOCATOR.lock().alloc_from_class, HeapSlot::alloc_large ...(+10) | Ok, balloc.add_memory, balloc.alloc, balloc.total_bytes, debug!, layout.size ...(+5) |
| `os/src/mm/heap/global_alloc.rs#alloc` | `AstrancE/modules/axhal/src/tls.rs#alloc` | 同板块同名 | allocator::alloc, caller_return_address, check_slab256_dealloc_watch, clear_slab256_dealloc_watch_if_reused, panic!, ptr::null_mut ...(+10) | Layout::from_size_align, Layout::from_size_align(tls_area_size(), TLS_ALIGN).unwrap, NonNull::new, NonNull::new(area_base).unwrap, alloc::alloc::alloc_zeroed, area_base.add ...(+4) |

#### 中断/异常处理
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `os/src/hal/irq/external.rs#register_handler` | `AstrancE/modules/axhal/src/platform/riscv64_qemu_virt/irq.rs#register_handler` | 同板块同名 | HANDLERS.lock, Some, assert!, panic!, table[irq].is_some | with_cause! |
| `os/src/hal/imp/arch/riscv64/irq.rs#local_enabled` | `AstrancE/modules/axhal/src/arch/riscv/mod.rs#irqs_enabled` | 同板块同结构 | sstatus::read, sstatus::read().sie | sstatus::read, sstatus::read().sie |
| `os/src/hal/irq.rs#disable_local` | `AstrancE/modules/axhal/src/arch/aarch64/mod.rs#wait_for_irqs` | 同板块同结构 | arch::irq::disable_local | aarch64_cpu::asm::wfi |
| `os/src/hal/imp/arch/loongarch64/irq.rs#local_enabled` | `AstrancE/modules/axhal/src/arch/loongarch64/mod.rs#irqs_enabled` | 同板块同结构 | crmd::read, crmd::read().ie | crmd::read, crmd::read().ie |

#### 系统调用
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `os/src/syscall/net.rs#accept` | `AstrancE/ulib/axlibc/src/net.rs#accept` | 同板块同名 | Err, Error::new, Ok, self.state.lock, state.pending.is_empty, state.pending.pop_front ...(+1) | e, sys_accept |
| `os/src/syscall/net.rs#shutdown` | `AstrancE/ulib/axlibc/src/net.rs#shutdown` | 同板块同名 | self.close_send_side, self.recv_closed.store, self.stream.wake_readiness_waiters | e, sys_shutdown |
| `os/src/syscall/net.rs#connect` | `AstrancE/ulib/axlibc/src/net.rs#connect` | 同板块同名 | peer.canonical, peer.is_loopback_route, self.connect_datagram, self.connect_external, self.connect_loopback | e, sys_connect |
| `os/src/syscall/net.rs#ktest_socket_timeout_option_contract` | `AstrancE/modules/axsignal/src/siginfo.rs#fill_raw_siginfo` | 同模块上下文对齐/差异观察 | SocketOptions::new, assert!, assert_eq!, endpoint
        .options
        .lock, endpoint
        .options
        .lock()
        .set_timeout_from_timeval, new_ktest_unix_socket_end ...(+1) | addr.as_ptr, call_addr.as_ptr, core::mem::size_of::<siginfo_t>, i32::from, ptr.as_ptr, ptr::write_bytes ...(+1) |

#### 文件系统
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `os/src/fs/tmpfs.rs#rename` | `AstrancE/modules/axfs/src/fs/fatfs.rs#rename` | 同板块同名 | Arc::ptr_eq, Err, Error::new, Ok, Self::contains_dir_in_subtree, Self::validate_rename_replace ...(+35) | debug!, self.0
            .rename, self.0
            .rename(src_path, &self.0, dst_path)
            .map_err |
| `os/src/fs/tmpfs.rs#read_dir` | `AstrancE/modules/axfs/src/fs/fatfs.rs#read_dir` | 同板块同名 | Err, Error::new, Ok, cookie.saturating_add, emit_tmpfs_dirent, entries
                    .snapshot_from_cookie ...(+9) | Ok, VfsDirEntry::new, dirents.iter_mut, dirents.iter_mut().enumerate, dirents.len, entry.file_name ...(+6) |
| `os/src/fs/path/dentry.rs#rename` | `AstrancE/modules/axfs/src/root.rs#rename` | 同板块同名 | Ok, children.delete, core::ptr::eq, detach_deleted_child, is_dot_or_dotdot, new_children.delete ...(+16) | ax_err!, fs.root_dir, fs.root_dir().rename, rest_path.is_empty, self.lookup_mounted_fs |
| `os/src/fs/miscdevfs.rs#read_dir` | `AstrancE/modules/axfs/src/fs/extra/devfs/shm.rs#read_dir` | 同板块同名 | Ok, cookie.saturating_add, emit, from_cookie.saturating_sub, index.saturating_add, inode.ino ...(+3) | Ok, axfs_vfs::VfsDirEntry::new, dirents.len, dirents[..n].copy_from_slice, dirs.as_slice, dirs.len ...(+6) |

#### 设备驱动
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `os/src/hal/imp/platform/fdt.rs#probe` | `AstrancE/modules/axdriver/src/lib.rs#probe` | 同板块同名 | Error::with_message, Fdt::from_ptr, Ok, Some, arch::mm::phys_to_virt, arch::mm::phys_to_virt(dtb_pa).as_ptr::<u8> ...(+13) | for_each_drivers!, info!, self.probe_bus_devices |
| `os/src/drivers/virtio/transport_dma.rs#mmio_phys_to_virt` | `AstrancE/modules/axdriver/src/ixgbe.rs#mmio_phys_to_virt` | 同板块同名 | NonNull::new, NonNull::new(vaddr).expect, assert!, crate::hal::PhysAddr::new, crate::hal::phys_to_virt_io, crate::hal::phys_to_virt_io(crate::hal::PhysAddr::new(paddr as usize))
            .as_mut_ptr::<u8> | NonNull::new, NonNull::new(phys_to_virt(paddr.into()).as_mut_ptr()).unwrap, paddr.into, phys_to_virt, phys_to_virt(paddr.into()).as_mut_ptr |
| `os/src/hal/boot/bootinfo.rs#push` | `AstrancE/modules/axdriver/src/structs/static.rs#push` | 同板块同名 | panic!, self.entries.len, self.len | Some, self.0.is_none |
| `os/src/drivers/virtio/transport_dma.rs#unshare` | `AstrancE/modules/axdriver/src/virtio.rs#unshare` | 同板块同名 | 内部闭环逻辑 | 内部闭环逻辑 |

#### 网络协议栈
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `os/src/net/stack.rs#send_to` | `AstrancE/ulib/axstd/src/net/udp.rs#send_to` | 同板块同名 | NetworkDeadline::none, Ok, self.send_to_with_deadline | addr.to_socket_addrs, addr.to_socket_addrs()?.next, api::ax_udp_send_to, axerrno::ax_err! |
| `os/src/net/stack.rs#recv_from` | `AstrancE/ulib/axstd/src/net/udp.rs#recv_from` | 同板块同名 | NetworkDeadline::none, Ok, self.recv_from_with_deadline | api::ax_udp_recv_from |
| `os/src/net/stack.rs#recv` | `AstrancE/api/arceos_posix_api/src/imp/net.rs#recv` | 同板块同名 | self.stack.tcp_recv | Ok, tcpsocket.lock, tcpsocket.lock().recv, udpsocket.lock, udpsocket.lock().recv_from, udpsocket.lock().recv_from(buf).map |
| `os/src/net/stack.rs#send` | `AstrancE/api/arceos_posix_api/src/imp/net.rs#send` | 同板块同名 | self.stack.tcp_send | Ok, tcpsocket.lock, tcpsocket.lock().send, udpsocket.lock, udpsocket.lock().send |

#### 文件系统/第三方库
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `os/src/fs/ext4.rs#rename` | `AstrancE/modules/axfs/src/fs/lwext4_rust.rs#rename` | 同板块同名 | Arc::ptr_eq, DstUpdate::ReplacedExistingEntry, Err, Error::new, Error::with_message, Ok ...(+55) | e.try_into, e.try_into().unwrap, file.file_rename, file.file_rename(src_path.as_str(), dst_path)
            .map, file.file_rename(src_path.as_str(), dst_path)
            .map(|_v| ())
            .map_err, self.0.lock ...(+2) |
| `os/src/fs/ext4.rs#create` | `AstrancE/modules/axfs/src/fs/lwext4_rust.rs#create` | 同板块同名 | Arc::new, Default::default, Err, Error::new, Ext4Extent::new, Ext4Inode::default ...(+51) | Ok, e.try_into, e.try_into().unwrap, file.check_inode_exist, file.dir_mk, file.dir_mk(fpath)
                    .map ...(+12) |
| `os/third_party/rsext4/src/ext4_backend/ext4.rs#mount` | `AstrancE/crates/lwext4_rust/examples/src/ext4fs.rs#mount` | 同板块同名 | BitmapCache::default, BlockAllocator::new, CacheKey::new_block, CacheKey::new_inode, DataBlockCache::new, Err ...(+71) | Ok |
| `os/third_party/rsext4/src/ext4_backend/ext4.rs#alloc_inodes` | `AstrancE/crates/lwext4_rust/c/lwext4/src/ext4_mkfs.c#alloc_inodes` | 同板块同名 | CacheKey::new_inode, Err, Ok, Some, Vec::new, Vec::with_capacity ...(+33) | ext4_fs_alloc_inode, ext4_fs_inode_blocks_init, ext4_fs_put_inode_ref, ext4_inode_set_mode |

#### 网络协议栈/第三方库
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `os/src/net/stack.rs#smoltcp_now` | `AstrancE/crates/virtio-drivers/src/device/socket/connectionmanager.rs#send_recv` | 同模块上下文对齐/差异观察 | Instant::from_micros, crate::time::uptime_us, crate::time::uptime_us().min | "Hello from guest".as_bytes, (hello_from_guest.len() as u32).into, (hello_from_host.len() as u32).into, 0.into, 50.into, Arc::new ...(+43) |
| `os/src/net/stack.rs#smoltcp_duration_to_capped_us` | `AstrancE/crates/virtio-drivers/src/device/socket/connectionmanager.rs#incoming_connection` | 同模块上下文对齐/差异观察 | delay.total_micros, delay.total_micros().min | 0.into, 50.into, Arc::new, Mutex::new, ReadOnly::new, SocketType::Stream.into ...(+24) |

#### 第三方库
仅参考项目中观察到该模块代表函数，目标项目没有同模块可对齐证据：
- 参考项目：`AstrancE/crates/axdriver_crates/axdriver_block/src/dw_mshc/mmc.rs#card_init`，调用摘要：CID::from, CLKDIV::offset, CMD::data_cmd, CMD::no_data_cmd, CMD::no_data_cmd_no_crc, CMD::no_data_cmd_no_crc(card_idx, 2).with_response_length ...(+33)
- 参考项目：`AstrancE/crates/virtio-drivers/src/queue.rs#fake_read_write_queue`，调用摘要：(*available_ring).idx.load, (*used_ring).idx.fetch_add, (*used_ring).idx.load, Vec::new, assert!, assert_eq! ...(+18)
- 参考项目：`AstrancE/crates/virtio-drivers/src/device/sound/fake.rs#handle_control_request`，调用摘要：CommandCode::SOk.into, Some, Vec::new, VirtIOSndHdr {
                            command_code: CommandCode::SOk.into(),
                        }
                        .as_bytes, VirtIOSndHdr::read_from_prefix, VirtIOSndHdr::read_from_prefix(&request)
                .expect ...(+16)
- 参考项目：`AstrancE/crates/virtio-drivers/src/transport/pci.rs#new`，调用摘要：Err, Ok, Some, UniqueMmioPointer::new, VirtioPciError::InvalidDeviceId, VirtioPciError::InvalidNotifyOffMultiplier ...(+15)

#### 通用模块/其他
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `os/src/logger.rs#read_bytes` | `AstrancE/modules/axhal/src/platform/riscv64_qemu_virt/console.rs#read_bytes` | 同板块同名 | out.is_empty, out.len, out.len().min, out[..first_len].copy_from_slice, out[first_len..].copy_from_slice, self.ring_index | VirtAddr::from_mut_ptr_of, bytes.as_mut_ptr, bytes.len, bytes.len().min, sbi_rt::Physical::new, sbi_rt::console_read ...(+2) |
| `os/src/logger.rs#log` | `AstrancE/ulib/axlibc/c/log.c#log` | 同板块同名 | StackLogLine::<STACK_LOG_LINE_CAPACITY>::new, crate::printk::write_bytes, fmt::write, format_args!, line.as_bytes, push ...(+2) | __builtin_fma, __math_divzero, __math_invalid, asdouble, asuint64, eval_as_double ...(+2) |
| `os/src/logger.rs#write_bytes` | `AstrancE/modules/axhal/src/platform/riscv64_qemu_virt/console.rs#write_bytes` | 同板块同名 | bytes.is_empty, bytes.len, bytes.len().min, self.buf[..bytes.len() - first_len].copy_from_slice, self.buf[start..start + first_len].copy_from_slice, self.ring_index | bytes.as_ptr, bytes.len, bytes.to_vec, kernel_bytes.len, try_write_bytes |
| `os/src/logger.rs#write_str` | `AstrancE/modules/axlog/src/lib.rs#write_str` | 同板块同名 | Ok, remain.min, s.as_bytes, s.len, self.buf.len, self.buf.len().saturating_sub ...(+1) | Ok, cfg_if::cfg_if! |


#### 1. 宏观判定
- **定性结论**: 独立开发 (相似度: 0.59%，共同 AST 指纹: 71；唯一 AST 指纹: 目标项目 8414，参考项目 3750；覆盖率: 目标项目 0.84%，参考项目 1.89%)

#### 2. 全模块覆盖矩阵解读

- **进程管理**
  双方均存在该模块。存在 4 个共同 AST 指纹。目标项目覆盖率 0.37%，参考项目覆盖率 4.35%。覆盖率极低，且共同指纹集中于 POSIX 标准系统调用入口及原子操作惯用法。证据强度：弱。

- **任务调度/异步运行时**
  双方均存在该模块。存在 7 个共同 AST 指纹。目标项目覆盖率 1.0%，参考项目覆盖率 2.18%。共同指纹多为 `current` 等通用函数名，但底层实现（如 `SCHEDULER.lock` vs `EXITED_TASKS`）完全不同。证据强度：弱。

- **内存管理**
  双方均存在该模块。存在 2 个共同 AST 指纹。目标项目覆盖率 0.1%，参考项目覆盖率 0.65%。共同指纹为 `unmap`、`protect` 等通用操作名，但内部调用链（如 `VmMarker::decode` vs `self.areas.protect`）无重合。证据强度：未形成。

- **中断/异常处理**
  双方均存在该模块。存在 2 个共同 AST 指纹。目标项目覆盖率 1.94%，参考项目覆盖率 3.51%。共同指纹为 `register_handler` 和 `local_enabled`/`irqs_enabled`，均属硬件架构强相关的标准操作，且目标项目使用自旋锁保护，参考项目使用宏分发。证据强度：弱。

- **系统调用**
  双方均存在该模块。未观察到共同结构指纹。覆盖率均为 0.0%。尽管存在大量同名 POSIX 入口函数，但 AST 层面无任何结构重合，表明实现路径完全独立。证据强度：未形成。

- **文件系统**
  双方均存在该模块。存在 4 个共同 AST 指纹。目标项目覆盖率 0.19%，参考项目覆盖率 0.82%。共同指纹为 `rename`、`read_dir` 等 VFS 通用操作名，但底层文件系统实现（tmpfs vs fatfs）和调用链截然不同。证据强度：未形成。

- **设备驱动**
  双方均存在该模块。未观察到共同结构指纹。覆盖率均为 0.0%。双方驱动模型和硬件抽象层设计完全不同。证据强度：未形成。

- **网络协议栈**
  双方均存在该模块。未观察到共同结构指纹。覆盖率均为 0.0%。目标项目基于 smoltcp 深度集成，参考项目基于自研 socket 接口，架构迥异。证据强度：未形成。

- **第三方库/公共依赖**
  参考项目存在独立的第三方库模块（1155 个函数），目标项目无此独立分类。双方在文件系统和网络协议栈中均包含第三方库代码，但共同指纹仅出现在 ext4 和 lwext4_rust 的通用封装接口上，属于对同一开源库的独立封装。证据强度：弱（已降权）。

#### 3. 底层 AST 关键证据指纹表

| 所属板块 | 目标项目路径 | 参考项目路径 | 证据类型 | 技术说明 |
| :--- | :--- | :--- | :--- | :--- |
| 进程管理 | os/src/thread/thread_local.rs#clear_child_tid | AstrancE/ulib/axmono/src/task/process.rs#clear_child_tid | 同板块底层逻辑一致 | 均为 `self.clear_child_tid.load`，但这是 Linux `set_tid_address` 标准语义下对原子变量的唯一 Rust 惯用读法，且双方结构体归属不同（ThreadLocal vs Process）。 |
| 进程管理 | os/src/thread/thread_local.rs#set_clear_child_tid | AstrancE/ulib/axmono/src/task/process.rs#set_clear_child_tid | 同板块同名同结构 | 均为 `self.clear_child_tid.store`，同上，属于标准原子操作的唯一写法，不构成独创性表达。 |
| 进程管理 | os/src/process.rs#set_child_subreaper | AstrancE/modules/axtask/src/task.rs#set_in_wait_queue | 同板块证据 | 均为 `self.<field>.store`，但字段名和业务语义完全不同（子进程回收标记 vs 等待队列标记），仅共享 Rust 原子写入范式。 |
| 进程管理 | os/src/process.rs#is_child_subreaper | AstrancE/modules/axtask/src/task.rs#in_wait_queue | 同板块证据 | 均为 `self.<field>.load`，同上，属于跨业务领域的原子读取范式巧合。 |
| 中断/异常处理 | os/src/hal/imp/arch/riscv64/irq.rs#local_enabled | AstrancE/modules/axhal/src/arch/riscv/mod.rs#irqs_enabled | 同板块同结构 | 均为 `sstatus::read().sie`，这是 RISC-V 架构读取中断使能位的唯一标准指令序列，不构成作品性表达。 |
| 中断/异常处理 | os/src/hal/imp/arch/loongarch64/irq.rs#local_enabled | AstrancE/modules/axhal/src/arch/loongarch64/mod.rs#irqs_enabled | 同板块同结构 | 均为 `crmd::read().ie`，同理，这是 LoongArch 架构的标准 CSR 读取操作。 |
| 第三方库/公共依赖 | os/third_party/rsext4/src/ext4_backend/ext4.rs#mount | AstrancE/crates/lwext4_rust/examples/src/ext4fs.rs#mount | 同板块同名 | 目标项目调用链包含 `BitmapCache::default` 等 71 个节点，参考项目仅返回 `Ok`，表明目标项目进行了深度自研封装，参考项目仅为示例性挂载。 |
| 第三方库/公共依赖 | os/third_party/rsext4/src/ext4_backend/ext4.rs#alloc_inodes | AstrancE/crates/lwext4_rust/c/lwext4/src/ext4_mkfs.c#alloc_inodes | 同板块同名 | 目标项目为 Rust 实现（`CacheKey::new_inode`），参考项目为 C 实现（`ext4_fs_alloc_inode`），语言和调用链完全不同，仅共享 ext4 标准概念名。 |

#### 4. 核心代码调用流对比剖析

* **【进程管理/进程创建与克隆路径】**
* **底层技术析理**: 目标项目的 `sys_clone` 调用链为 `CloneFlags::from_raw` → `abi::decode_clone_args` → `child_tidptr.checked_add`，展现了自有的参数解码和错误处理流程，并返回自定义的 `SyscallControlFlow::Continue`。参考项目的 `sys_clone` 调用链为 `CloneFlags::from_bits_retain` → `task::clone_task` → `child_task.task_ext().thread.process().pid()`，直接委托给 `axtask` 子系统，并基于 `task_ext` 扩展数据模型。两者在标志位解析方式、任务创建委托层次、进程 ID 获取路径上完全不同，表明双方拥有独立的进程管理架构。
* **代码溯源链接**:
  - 目标项目: `os/src/syscall/proc.rs#sys_clone`
  - 参考项目: `AstrancE/ulib/axmono/src/syscall/process.rs#sys_clone`

* **【任务调度/线程退出路径】**
* **底层技术析理**: 目标项目的 `exit_current` 调用链为 `SCHEDULER.lock` → `Thread::current` → `current.release_kernel_fs_on_exit` → `current.set_state`，基于全局调度器自旋锁和线程状态机实现，并在退出时主动释放内核文件系统资源。参考项目的 `exit_current` 调用链为 `EXITED_TASKS.current_ref_mut_raw` → `WAIT_FOR_EXIT.notify_one` → `axhal::misc::terminate`，基于退出任务队列和等待通知机制，最终调用硬件抽象层终止。两者在同步原语（自旋锁 vs 无锁队列）、资源清理策略和终止路径上存在根本性架构差异。
* **代码溯源链接**:
  - 目标项目: `os/src/thread/scheduler.rs#exit_current`
  - 参考项目: `AstrancE/modules/axtask/src/run_queue.rs#exit_current`

* **【内存管理/地址空间解除映射路径】**
* **底层技术析理**: 目标项目的 `unmap` 调用链包含 `TlbFlushOp::Address` / `TlbFlushOp::Range` 的精细刷新策略，并通过 `assert!` 进行严格的地址对齐检查。参考项目的 `unmap` 调用链为 `Self::unmap_alloc` / `Self::unmap_linear` 的分发模式，并涉及 `va_type.clone`。两者在 TLB 刷新粒度控制、地址空间遍历策略和错误处理机制上采用完全不同的设计路径。
* **代码溯源链接**:
  - 目标项目: `os/src/mm/vm_space.rs#unmap`
  - 参考项目: `AstrancE/modules/axmm/src/backend/mod.rs#unmap`

* **【中断/异常处理/外部中断注册路径】**
* **底层技术析理**: 目标项目的 `register_handler` 使用 `HANDLERS.lock` 自旋锁保护内部表项，并通过 `assert!` 和 `panic!` 进行重复注册检查。参考项目的 `register_handler` 使用 `with_cause!` 宏进行分发。两者在并发保护策略（显式锁 vs 宏分发）和错误处理风格上截然不同。
* **代码溯源链接**:
  - 目标项目: `os/src/hal/irq/external.rs#register_handler`
  - 参考项目: `AstrancE/modules/axhal/src/platform/riscv64_qemu_virt/irq.rs#register_handler`

#### 5. 单方模块与降权项

- **仅参考项目体现的模块**：第三方库（1155 个函数）。目标项目将第三方库代码分散在各子系统中（如 `os/third_party/rsext4`），未独立成模块。此差异源于项目组织方式不同，不构成证据。
- **降权项说明**：
  - 所有 `sys_*` 开头的 POSIX 系统调用同名函数（如 `sys_clone`、`sys_wait4`、`sys_kill` 等）：属于操作系统领域的标准接口命名，任何 Linux 兼容内核均需实现，其函数名本身不具作品独创性。
  - `exit_code`、`signal`、`wait`、`current` 等泛型函数名：属于通用编程词汇，在 AST 层面仅体现为极简调用链（如 `Some`、`self.waker.do_wait`），无法形成结构性同源证据。
  - `clear_child_tid` 相关原子操作：字段名源自 Linux 内核 ABI 标准术语，`load/store` 是 Rust 原子类型的唯一惯用读写方法，属于语言和标准强约束下的必然趋同，已从核心证据降权为弱信号。

#### 6. 保守结论

基于 0.59% 的全局 AST 相似度和最高仅 4.35% 的单模块覆盖率，本次审计未发现任何足以认定代码复用或结构性抄袭的实质性证据。控方提出的“核心铁证”——`clear_child_tid` 的原子读写操作，经技术剖析，实为 Linux 标准 ABI 语义与 Rust 语言惯用法双重约束下的必然实现，且双方将其置于完全不同的架构层次（线程本地 vs 进程结构）。其余同名函数（如 `sys_clone`、`exit_current`）的底层调用图谱高度分化，揭示了双方在内存模型、同步策略和对象管理上的根本性架构差异。整体来看，71 个共同 AST 指纹在双方逾万独立指纹的背景下，仅构成操作系统开发中由公共标准、硬件架构和编程语言所导致的领域背景噪声，远未达到可合理怀疑抄袭的阈值。目标项目的独立原创性应予认可。

------------------------------

