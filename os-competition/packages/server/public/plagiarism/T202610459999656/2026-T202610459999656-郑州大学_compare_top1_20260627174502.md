# 🛡️ OS 内核赛自动化代码级架构审计报告

**审计时间**: `2026-06-27 17:45:02`
**参赛作品名称**: `2026-T202610459999656-郑州大学`
**年份**: `2026`
**学校**: `郑州大学`
**队伍名称**: `T202610459999656`
**比赛名字**: `OS-Competition`
**比赛子赛道**: `Kernel-Implementation`
**仓库地址**: `https://gitlab.eduxiji.net/educg-group-43501-3132633/T202610459999656-3554`
**推测开发语言**: `Rust / C (由AST探针自动识别)`
**特征函数量**: `10439`

## ⚔️ 底层代码同源性对比 (AST Homology)
> 本部分由专门的代码审查智能体，对该项目与符合年份条件的历届最高相似度项目进行控制流与底层调用对比。新版报告会先给出确定性的全模块覆盖矩阵，再给出 Agent 的分模块解释，避免只围绕少量抽样函数展开。

### 🏆 Top 1 底层对标: vs `2025-NexusOS-郑州大学`（郑州大学 / NexusOS）

#### 对方作品信息

**对方年份**: `2025`
**对方学校**: `郑州大学`
**对方队伍名称**: `NexusOS`
**对方比赛名字**: `OS-Competition`
**对方比赛子赛道**: `Kernel-Implementation`
**对方仓库地址**: `https://gitlab.eduxiji.net/educg-group-36002-2710490/T202510459995282-239`

> 双向覆盖率：共同 AST 指纹: 111；唯一 AST 指纹: 目标项目 8414，参考项目 3814；覆盖率: 目标项目 1.32%，参考项目 2.91%

## 全模块对比总览（确定性证据）

> 总体覆盖率：共同 AST 指纹: 111；唯一 AST 指纹: 目标项目 8414，参考项目 3814；覆盖率: 目标项目 1.32%，参考项目 2.91%

### 1. 模块覆盖矩阵
| 模块 | 目标函数数 | 参考函数数 | 共同唯一 AST 指纹 | Target 覆盖 | Base 覆盖 | 状态 |
| :--- | ---: | ---: | ---: | ---: | ---: | :--- |
| 进程管理 | 1374 | 26 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |
| 任务调度/异步运行时 | 861 | 268 | 11 | 1.57% | 4.62% | 双方存在且有结构重合 |
| 内存管理 | 2374 | 885 | 34 | 1.63% | 4.24% | 双方存在且有结构重合 |
| 中断/异常处理 | 130 | 132 | 3 | 2.91% | 3.06% | 双方存在且有结构重合 |
| 系统调用 | 613 | 7 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |
| 文件系统 | 2689 | 447 | 10 | 0.47% | 2.53% | 双方存在且有结构重合 |
| 设备驱动 | 312 | 88 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |
| 网络协议栈 | 103 | 0 | 0 | 0.0% | 0.0% | 仅目标项目体现 |
| 文件系统/第三方库 | 1508 | 360 | 7 | 0.56% | 2.25% | 双方存在且有结构重合 |
| 网络协议栈/第三方库 | 2 | 117 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |
| 第三方库 | 0 | 1781 | 0 | 0.0% | 0.0% | 仅参考项目体现 |
| 通用模块/其他 | 473 | 523 | 11 | 2.67% | 2.66% | 双方存在且有结构重合 |

### 2. 分模块证据清单

#### 进程管理
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `os/src/syscall/proc.rs#do_rt_sigprocmask` | `kernel/src/syscall/signal.rs#do_rt_sigprocmask` | 同板块同名 | (set_ptr != 0)
        .then, (set_ptr != 0)
        .then(|| read_user_sigset(ctx.vm_space(), set_ptr))
        .transpose, Err, Error::new, Ok, ctx
        .current ...(+12) | ControlFlow::Continue, Err, KernelSigset, Ok, Some, errno_with_message ...(+3) |
| `os/src/syscall/proc.rs#do_wait4` | `kernel/src/thread/wait.rs#do_wait4` | 同板块同名 | Ok, VirtAddr::new, WaitOptions::from_raw, ctx
                    .vm_space, ctx
                    .vm_space()
                    .writer, ctx
        .current ...(+11) | ControlFlow::Continue, Err, Ok, Some, child.clone, child.lifecycle.wait ...(+13) |
| `os/src/smp.rs#inter_processor_call` | `ostd/src/smp.rs#inter_processor_call` | 同板块同名 | CALL_QUEUE.get_on_hart, CALL_QUEUE.get_on_hart(hart_id).lock, CALL_QUEUE.get_on_hart(hart_id).lock().push_back, crate::cpu::disable_local_irq, crate::hal::arch_send_ipi, f ...(+2) | CALL_QUEUES.get_on_cpu, CALL_QUEUES.get_on_cpu(cpu_id).lock, CALL_QUEUES.get_on_cpu(cpu_id).lock().push_back, INTER_PROCESSOR_CALL_IRQ.get, INTER_PROCESSOR_CALL_IRQ.get().unwrap, INTER_PROCESSOR_CALL_IRQ.get().unwrap().num ...(+6) |
| `os/src/syscall/proc.rs#do_execve` | `kernel/src/thread/execve.rs#do_execve` | 同板块同名 | ctx
        .current, ctx
        .current()
        .user_process, ctx
        .current()
        .user_process()
        .expect, ctx
        .current()
        .user_process()
        .expect("execve requires a user process")
        .get_rlimit, do_execve_impl | ControlFlow::Continue, Ok, PathSlice::new, errno_with_message, load_elf_to_vm, path.is_absolute ...(+11) |

#### 任务调度/异步运行时
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `os/src/syscall/fs.rs#do_unlinkat` | `kernel/src/syscall/fs.rs#do_unlinkat` | 同板块同名 | Err, Error::new, FsPath::new, Ok, Some, args::copy_user_cstr ...(+17) | ControlFlow::Continue, Ok, Some, copy_cstr_from_user, cx.syscall_arguments, dir.unlink ...(+12) |
| `os/src/syscall/fs.rs#do_copy_file_range` | `kernel/src/syscall/fs.rs#do_copy_file_range` | 同板块同名 | DataMoveCopyOptions::new, DataMoveOffset::new, DataMoveWriteOptions::blocking, Err, Error::new, copy_file_range_data ...(+20) | ControlFlow::Continue, Default::default, Ok, Some, buf.len, core::cmp::max ...(+30) |
| `os/src/syscall/fs.rs#do_renameat2` | `kernel/src/syscall/fs.rs#do_renameat2` | 同板块同名 | Err, Error::new, FsPath::new, Ok, Some, args::copy_user_cstr ...(+18) | ControlFlow::Continue, Ok, PathBuf::new, PathSlice::from, PathSlice::from(&abs).file_name, PathSlice::from(&abs).file_name().ok_or_else ...(+33) |
| `os/src/syscall/fs.rs#do_read` | `kernel/src/syscall/fs.rs#do_read` | 同板块同名 | Err, Error::new, Some, VirtAddr::new, crate::println!, ctx
            .vm_space ...(+15) | ControlFlow::Continue, Ok, Some, VmReader::from, cx.syscall_arguments, fh.clone ...(+9) |

#### 内存管理
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `os/src/mm/vmar.rs#build` | `kernel/src/vm/vmar/mod.rs#build` | 同板块同名 | Err, Error::new, Ok, SharedMayWriteLeasePlan::Preacquired, Some, VmPlacement::HintThenTopDown ...(+30) | Err, Error::with_message, MappedVmo::new, NonZeroUsize::new, NonZeroUsize::new(map_size).unwrap, Ok ...(+14) |
| `os/src/hal/imp/arch/loongarch64/user.rs#execute` | `ostd/src/arch/x86/cpu/mod.rs#execute` | 同板块同名 | Some, crate::hal::VirtAddr::new, crate::hal::irq::enable_local, crate::hal::irq::external::handle_external_interrupt, crate::hal::tlb_flush_addr, crate::hal::trap::notify_trap ...(+12) | (RFlags::INTERRUPT_FLAG | RFlags::ID).bits, CpuException::to_cpu_exception, VirtualizationExceptionHandler::new, call_irq_callback_functions, crate::arch::irq::enable_local, exception.typ ...(+8) |
| `os/src/syscall/mm.rs#do_mmap` | `kernel/src/vm/mmap.rs#do_mmap` | 同板块同名 | Err, Error::new, MmapType::decode, Ok, Some, VirtAddr::new ...(+58) | ControlFlow::Continue, MMapOpts::try_from, Some, VmPerms::from_bits_truncate, anonymous_mapping, cx.syscall_arguments ...(+11) |
| `os/src/hal/imp/arch/riscv64/user.rs#execute` | `ostd/src/arch/loongarch/cpu/context.rs#execute` | 同板块同名 | Some, Trap::<Interrupt, Exception>::try_from, crate::hal::irq::enable_local, crate::hal::irq::external::handle_external_interrupt, crate::hal::trap::notify_trap, crate::smp::ack_ipi_on_current_hart ...(+11) | Exception::Syscall.into, crate::arch::irq::enable_local, crate::task::scheduler::might_preempt, crate::trap::disable_local, drop, e.into ...(+8) |

#### 中断/异常处理
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `os/src/hal/imp/arch/loongarch64/irq/external.rs#enable_irq` | `ostd/src/arch/loongarch/plic/regs.rs#enable_irq` | 同板块同名 | 1u8
            .checked_shl, 1u8
            .checked_shl(u32::try_from(hart).expect("hart id overflow"))
            .expect, crate::cpu::current_hart_racy, crate::cpu::current_hart_racy().as_usize, iocsr_write_b, self.enable_internal ...(+4) | iocsr_read_d, iocsr_write_d |
| `os/src/sync/spin_lock.rs#disable_irq` | `ostd/src/arch/loongarch/plic/regs.rs#disable_irq` | 同板块同名 | 内部闭环逻辑 | iocsr_read_d, iocsr_write_d |
| `os/src/hal/imp/arch/riscv64/trap.rs#run` | `ostd/src/arch/riscv/trap/trap.rs#run` | 同板块同名 | crate::hal::irq::disable_local, run_user | Sstatus::from_bits, assert_eq!, run_user |
| `os/src/mm/io.rs#memcpy` | `ostd/src/mm/io.rs#memcpy` | 同板块同名 | core::intrinsics::volatile_copy_memory | core::intrinsics::volatile_copy_memory |

#### 系统调用
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `os/src/syscall.rs#current` | `kernel/src/syscall/uts.rs#current` | 同板块同名 | 内部闭环逻辑 | buf[..len].copy_from_slice, bytes.len, bytes.len().min, fill, src.as_bytes |
| `os/src/syscall/net.rs#ktest_udp_pollout_loopback_read_skip_contract` | `kernel/src/syscall/uts.rs#fill` | 同模块上下文对齐/差异观察 | Ipv4Endpoint::raw, KTEST_UDP_LOOPBACK_POLL_READ_SIDE_CALLS.store, LoopbackUdpEndpoint::new, Ok, assert!, assert_eq! ...(+1) | buf[..len].copy_from_slice, bytes.len, bytes.len().min, src.as_bytes |
| `os/src/syscall/net.rs#flush_with_current` | `kernel/src/syscall/users.rs#do_getegid` | 同模块上下文对齐/差异观察 | Err, Error::new, Error::with_message, Ok, Vec::with_capacity, VmReader::from ...(+14) | ControlFlow::Continue, Ok, Some |
| `os/src/syscall/net.rs#writev_from_vm_space` | `kernel/src/syscall/users.rs#do_geteuid` | 同模块上下文对齐/差异观察 | Err, Error::new, SocketMsgFlags::empty_send, SocketSendOptions::from_nonblocking, SocketSendOptions::from_nonblocking(self.is_nonblocking())
                            .with_deadline, SocketSendOptions::from_nonblocking(self.is_nonblocking())
                        .with_deadline ...(+10) | ControlFlow::Continue, Ok, Some |

#### 文件系统
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `os/src/fs/tmpfs.rs#rename` | `kernel/comps/vfs/src/static_dispatch/vnode/dir.rs#rename` | 同板块同名 | Arc::ptr_eq, Err, Error::new, Ok, Self::contains_dir_in_subtree, Self::validate_rename_replace ...(+35) | Err, KernelError::with_message, KernelError::with_message(Errno::EINVAL, "rename").into, old.rename |
| `os/src/fs/path/dentry.rs#rename` | `kernel/comps/vfs/src/impls/memfs/vnode.rs#rename` | 同板块同名 | Ok, children.delete, core::ptr::eq, detach_deleted_child, is_dot_or_dotdot, new_children.delete ...(+16) | Err, vfs_err_not_implemented! |
| `os/src/fs/path.rs#rename` | `kernel/comps/vfs/src/impls/dev_fs/vnode.rs#rename` | 同板块同名 | Arc::ptr_eq, new_dir.ensure_linked_dir, new_dir.ensure_mount_writable, new_dir.lookup, new_dir.lookup(new_name).ok, new_dir.notify_child_move ...(+13) | Err, crate::vfs_err_unsupported! |
| `os/src/fs/tmpfs.rs#create` | `kernel/comps/vfs/src/static_dispatch/vnode/dir.rs#create` | 同板块同名 | Err, Error::new, Ok, String::from, add_link_count, child.clone ...(+7) | Ok, SVnode::from, v.create |

#### 设备驱动
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `os/src/drivers/virtio/blk/core.rs#handle_irq` | `ostd/src/bus/device_manager.rs#handle_irq` | 同板块同名 | TRACE_IRQ_ENTERED.fetch_add, TRACE_LAST_IRQ_HART.store, TRACE_USED_DRAINED.fetch_add, crate::cpu::current_hart_racy, crate::cpu::current_hart_racy().as_usize, disk.ack_interrupt ...(+5) | dev.ack_interrupt, self.devices.get, self.irq_map.get, tracing::trace! |
| `os/src/drivers/virtio/transport_dma.rs#mmio_phys_to_virt` | `ostd/src/drivers/virtio/hal.rs#mmio_phys_to_virt` | 同板块同名 | NonNull::new, NonNull::new(vaddr).expect, assert!, crate::hal::PhysAddr::new, crate::hal::phys_to_virt_io, crate::hal::phys_to_virt_io(crate::hal::PhysAddr::new(paddr as usize))
            .as_mut_ptr::<u8> | NonNull::new, NonNull::new(paddr_to_vaddr(paddr) as *mut u8).unwrap, paddr_to_vaddr |
| `os/src/net/device.rs#capabilities` | `ostd/src/bus/pci/common_device.rs#capabilities` | 同板块同名 | DeviceCapabilities::default, Some | 内部闭环逻辑 |
| `os/src/drivers/virtio/transport_dma.rs#unshare` | `ostd/src/drivers/virtio/hal.rs#unshare` | 同板块同名 | 内部闭环逻辑 | MAP.lock, MAP.lock().remove, drop, matches!, paddr_to_vaddr, ptr::copy_nonoverlapping ...(+1) |

#### 网络协议栈
仅目标项目中观察到该模块代表函数，参考项目没有同模块可对齐证据：
- 目标项目：`os/src/net/stack.rs#udp_send_to`，调用摘要：Err, Error::new, Ipv4Address::new, Ok, data.len, self.poll_once ...(+3)
- 目标项目：`os/src/net/stack.rs#udp_recv_once`，调用摘要：UdpRecvResult::Done, ipv4_octets, out.len, out[..copied_len].copy_from_slice, packet.len, packet.len().min ...(+2)
- 目标项目：`os/src/net/stack.rs#open_tcp_stream`，调用摘要：Arc::new, Err, Error::new, Ipv4Address::new, Ok, TcpWaitQueues::new ...(+4)
- 目标项目：`os/src/net/stack.rs#tcp_recv_with`，调用摘要：Ok, closed, self.poll_once, self.state.lock, self.state.lock().tcp_recv_once_with, wait.sleep_one_round

#### 文件系统/第三方库
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `os/src/fs/ext4.rs#rename` | `kernel/comps/another_ext4/ext4_fuse/src/fuse_fs.rs#rename` | 同板块同名 | Arc::ptr_eq, DstUpdate::ReplacedExistingEntry, Err, Error::new, Error::with_message, Ok ...(+55) | e.code, name.to_str, name.to_str().unwrap, newname.to_str, newname.to_str().unwrap, reply.error ...(+10) |
| `os/src/fs/ext4.rs#create` | `kernel/comps/another_ext4/ext4_fuse/src/fuse_fs.rs#create` | 同板块同名 | Arc::new, Default::default, Err, Error::new, Ext4Extent::new, Ext4Inode::default ...(+51) | InodeMode::from_bits_truncate, e.code, get_ttl, name.to_str, name.to_str().unwrap, reply.created ...(+5) |
| `os/third_party/rsext4/src/ext4_backend/ext4.rs#mount` | `kernel/comps/vfs/src/impls/ext4_fs/provider.rs#mount` | 同板块同名 | BitmapCache::default, BlockAllocator::new, CacheKey::new_block, CacheKey::new_inode, DataBlockCache::new, Err ...(+71) | Adapt, Arc::new, Block::default, Ext4Fs::new, Ok, VirtioBlockDevice::new ...(+16) |
| `os/third_party/rsext4/src/ext4_backend/file.rs#unlink` | `kernel/comps/another_ext4/src/ext4/low_level.rs#unlink` | 同板块同名 | "/".to_string, DirEntryIterator::new, Some, blocks.values, child_name.as_bytes, fs
        .modify_inode ...(+21) | child.inode.is_dir, parent.inode.is_dir, return_error!, self.dir_find_entry, self.read_inode, self.unlink_inode |

#### 网络协议栈/第三方库
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `os/src/net/stack.rs#smoltcp_now` | `ostd/libs/virtio-drivers/src/device/socket/connectionmanager.rs#send_recv` | 同模块上下文对齐/差异观察 | Instant::from_micros, crate::time::uptime_us, crate::time::uptime_us().min | "Hello from guest".as_bytes, (hello_from_guest.len() as u32).into, (hello_from_host.len() as u32).into, 0.into, 50.into, Arc::new ...(+43) |
| `os/src/net/stack.rs#smoltcp_duration_to_capped_us` | `ostd/libs/virtio-drivers/src/device/socket/connectionmanager.rs#incoming_connection` | 同模块上下文对齐/差异观察 | delay.total_micros, delay.total_micros().min | 0.into, 50.into, Arc::new, Mutex::new, ReadOnly::new, SocketType::Stream.into ...(+24) |

#### 第三方库
仅参考项目中观察到该模块代表函数，目标项目没有同模块可对齐证据：
- 参考项目：`kernel/libs/nexus-error/src/lib.rs#fmt`，调用摘要：write!
- 参考项目：`ostd/libs/maitake-sync/src/semaphore.rs#poll_acquire`，调用摘要：NonNull::from, Ok, Pin::into_inner_unchecked, Poll::Ready, Some, Waiter::with_node ...(+20)
- 参考项目：`ostd/libs/maitake-sync/src/wait_queue.rs#poll_wait`，调用摘要：NonNull::from, Ok, Pin::into_inner_unchecked, Poll::Ready, Wakeup::Waiting, crate::closed ...(+20)
- 参考项目：`ostd/libs/virtio-drivers/src/queue.rs#fake_read_write_queue`，调用摘要：(*available_ring).idx.load, (*used_ring).idx.fetch_add, (*used_ring).idx.load, Vec::new, assert!, assert_eq! ...(+18)

#### 通用模块/其他
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `os/src/logger.rs#log` | `ostd/src/logger.rs#log` | 同板块同名 | StackLogLine::<STACK_LOG_LINE_CAPACITY>::new, crate::printk::write_bytes, fmt::write, format_args!, line.as_bytes, push ...(+2) | GuardSpinLock::new, RECORD_LOCK.disable_irq, RECORD_LOCK.disable_irq().lock, crate::console::early_print, format_args!, logger.log ...(+2) |
| `os/src/logger.rs#write_str` | `ostd/src/arch/riscv/serial.rs#write_str` | 同板块同名 | Ok, remain.min, s.as_bytes, s.len, self.buf.len, self.buf.len().saturating_sub ...(+1) | Ok, s.as_bytes, send |
| `os/src/sync/spin_lock.rs#lock` | `ostd/src/sync/guard_spin.rs#lock` | 同板块同名 | G::guard, core::any::type_name::<T>, crate::hal::cpu::relax, crate::runner_timeout::record_spin_contention, self.inner.try_lock | G::guard, self.acquire_lock |
| `os/src/sync/rcu.rs#after_grace_period` | `ostd/src/sync/rcu/monitor.rs#after_grace_period` | 同板块同名 | core::mem::take, self.is_monitoring.store, self.state.disable_irq, self.state.disable_irq().lock, state.current_gp.is_complete, state.current_gp.restart ...(+1) | Box::new, core::mem::take, self.is_monitoring.store, self.state.disable_irq, self.state.disable_irq().lock, state.current_gp.is_complete ...(+2) |


好的，我将严格遵循审计指令，基于客观 AST 证据与双方画像，进行最终同源性定性仲裁。

### 1. 宏观判定
- **定性结论**: **独立开发，底层架构显著差异**
- **核心依据**: 客观 AST 相似度仅为 **0.92%**，共同 AST 指纹仅 111 个。目标项目拥有 8414 个唯一指纹，参考项目拥有 3814 个。双向覆盖率均处于极低水平（目标 1.32%，参考 2.91%）。该量级属于两个独立开发的复杂系统软件（操作系统内核）间，因遵循相同行业标准（POSIX）、硬件规范（RISC-V/LoongArch/x86）及使用相同编程语言（Rust）而产生的正常噪声范围，远未达到可判定为“同源”或“抄袭”的阈值。

### 2. 全模块覆盖矩阵解读
- **进程管理**: 双方均存在。**共同 AST 指纹为 0**，双向覆盖率 0.0%。目标项目体量（1374 函数）远超参考项目（26 函数），表明双方采用了完全不同的进程模型与实现策略。**证据强度：未形成**。
- **任务调度/异步运行时**: 双方均存在。共同指纹 11，覆盖率低（目标 1.57%，参考 4.62%）。重合点集中在 POSIX 文件系统系统调用（如 `do_read`, `do_mkdirat`）的实现路径上，属于接口规范导致的流程收敛。**证据强度：弱**。
- **内存管理**: 双方均存在。共同指纹 34，覆盖率低（目标 1.63%，参考 4.24%）。重合点涉及 `mmap`、`vmar` 构建等通用内存管理原语，但外部调用对象（如 `VmPlacement` vs `MappedVmo`）差异显著。**证据强度：弱**。
- **中断/异常处理**: 双方均存在。共同指纹 3，覆盖率低（目标 2.91%，参考 3.06%）。重合点如 `memcpy` 直接调用了 Rust 编译器内建函数 `volatile_copy_memory`，属于语言层面的标准惯用法。**证据强度：未形成**。
- **系统调用**: 双方均存在。**共同 AST 指纹为 0**，双向覆盖率 0.0%。目标项目体量（613 函数）远超参考项目（7 函数），表明双方的系统调用分发与实现框架完全不同。**证据强度：未形成**。
- **文件系统**: 双方均存在。共同指纹 10，覆盖率极低（目标 0.47%，参考 2.53%）。重合点集中在 VFS 层的通用操作（如 `rename`, `create`），但底层文件系统实现（如 ext4）的外部依赖和调用链完全不同。**证据强度：弱**。
- **设备驱动**: 双方均存在。**共同 AST 指纹为 0**，双向覆盖率 0.0%。双方驱动模型与设备管理框架无任何结构性重合。**证据强度：未形成**。
- **网络协议栈**: 仅目标项目存在。参考项目无此独立模块，其网络功能可能存在于第三方库中。双方无共同指纹。**证据强度：未形成**。
- **第三方库/公共依赖**: 双方均存在大量第三方库代码，但**共同 AST 指纹为 0**。目标项目依赖 `rsext4` 等，参考项目依赖 `maitake-sync`、`virtio-drivers` 等，双方选型与依赖库完全不同。**证据强度：未形成**。

### 3. 底层 AST 关键证据指纹表
| 所属板块 | 目标项目路径 | 参考项目路径 | 证据类型 | 技术说明 |
| :--- | :--- | :--- | :--- | :--- |
| 内存管理 | `os/src/mm/vmar.rs#build` | `kernel/src/vm/vmar/mod.rs#build` | 同板块同名 | 双方均实现了虚拟内存区域构建，但目标使用 `SharedMayWriteLeasePlan`、`VmPlacement` 等自有抽象；参考使用 `MappedVmo`、`NonZeroUsize`。外部调用对象完全不同。 |
| 内存管理 | `os/src/syscall/mm.rs#do_mmap` | `kernel/src/vm/mmap.rs#do_mmap` | 同板块同名 | 双方均实现 `mmap` 系统调用，但目标使用 `MmapType::decode` 和 `VirtAddr::new`；参考使用 `MMapOpts::try_from` 和 `VmPerms`。参数解析与权限模型各异。 |
| 中断/异常处理 | `os/src/mm/io.rs#memcpy` | `ostd/src/mm/io.rs#memcpy` | 同板块同名 | 双方均直接调用 Rust 核心库的 `core::intrinsics::volatile_copy_memory` 实现内存拷贝，这是 Rust 语言层面的标准惯用法，非项目独创。 |
| 任务调度/异步运行时 | `os/src/syscall/fs.rs#do_mkdirat` | `kernel/src/syscall/fs.rs#do_mkdirat` | 同板块同名 | 双方均实现 `mkdirat` 系统调用，流程均为“拷贝路径->解析目录->创建节点”。但目标使用 `FsPath`、`InodeMode`；参考使用 `PathSlice`、`FileMode`、`VnodeType`。VFS 抽象层不同。 |
| 任务调度/异步运行时 | `os/src/syscall/fs.rs#do_read` | `kernel/src/syscall/fs.rs#do_read` | 同板块同名 | 双方均实现 `read` 系统调用，但目标使用 `VmWriter` 向用户空间写数据；参考使用 `VmReader` 从文件读数据。数据流向与内存抽象不同。 |
| 文件系统 | `os/src/fs/tmpfs.rs#rename` | `kernel/comps/vfs/src/static_dispatch/vnode/dir.rs#rename` | 同板块同名 | 双方均实现 `rename` 操作，但目标包含复杂的子树检查逻辑 (`contains_dir_in_subtree`)；参考直接委托给 `old.rename`。实现复杂度与路径不同。 |
| 文件系统/第三方库 | `os/third_party/rsext4/src/ext4_backend/ext4.rs#mount` | `kernel/comps/vfs/src/impls/ext4_fs/provider.rs#mount` | 同板块同名 | 双方均挂载 ext4 文件系统，但目标依赖 `rsext4` 库，调用 `BlockAllocator::new` 等；参考依赖自研 `Ext4Fs`，调用 `VirtioBlockDevice::new`。底层依赖库完全不同。 |
| 通用模块/其他 | `os/src/sync/spin_lock.rs#lock` | `ostd/src/sync/guard_spin.rs#lock` | 同板块同名 | 双方均实现自旋锁，但目标包含超时竞态记录 (`record_spin_contention`)；参考使用 `acquire_lock` 内部方法。锁的调试与监控机制不同。 |

### 4. 核心代码调用流对比剖析
* **【内存管理/`do_mmap` 系统调用】**
* **底层技术析理**: 双方均实现了 POSIX `mmap` 语义，但底层内存模型截然不同。目标项目采用 `MmapType::decode` 解析映射类型，并通过 `VmPlacement` 策略决定地址分配，体现了更复杂的地址空间布局随机化 (ASLR) 或 hint 处理逻辑。参考项目则通过 `MMapOpts::try_from` 转换参数，直接使用 `VmPerms::from_bits_truncate` 处理权限，流程更线性。两者在对象职责划分和内存管理策略上存在架构级差异。
* **代码溯源链接**:
  - 目标项目: `os/src/syscall/mm.rs#do_mmap`
  - 参考项目: `kernel/src/vm/mmap.rs#do_mmap`

* **【进程管理/`do_execve` 系统调用】**
* **底层技术析理**: 双方均实现 `execve`，但执行流与依赖关系完全不同。目标项目的 `do_execve` 是一个薄层，核心逻辑完全委托给 `do_execve_impl`，并显式获取进程资源限制 (`get_rlimit`)。参考项目的 `do_execve` 则内聚了路径解析 (`PathSlice::new`)、ELF 加载 (`load_elf_to_vm`) 和参数拷贝 (`read_cstring_vec`) 的全流程。目标项目采用了更彻底的“机制与策略分离”设计，将复杂性下沉到 `do_execve_impl`。
* **代码溯源链接**:
  - 目标项目: `os/src/syscall/proc.rs#do_execve`
  - 参考项目: `kernel/src/thread/execve.rs#do_execve`

* **【中断/异常处理/用户态执行】**
* **底层技术析理**: 在从内核态进入用户态的 `execute` 路径上，双方都遵循了“检查可睡眠性 -> 执行上下文切换”的基本模式，这是由 CPU 特权级切换的硬件语义决定的。然而，目标项目在切换前显式调用了 `crate::hal::fpu::enable_user_fpu_context` 以启用浮点单元，这揭示了其在 FPU 上下文管理上与参考项目的不同架构考量。参考项目则无此步骤。
* **代码溯源链接**:
  - 目标项目: `os/src/user.rs#execute`
  - 参考项目: `ostd/src/user.rs#execute`

### 5. 单方模块与降权项
- **单方模块**: 目标项目独有的**网络协议栈**模块（103 个函数）和参考项目独有的**第三方库**模块（1781 个函数，如 `maitake-sync`, `nexus-error`），是双方架构设计方向和项目依赖完全不同的有力证明。这些单方存在的大规模模块无法构成同源证据。
- **降权项**: 控辩双方均提及的 `osdk/src/commands/mod.rs` 等构建工具代码，属于辅助工具链，与操作系统内核核心逻辑跨板块。根据审计指令，此类跨板块同名或相似**必须降权**，不构成内核代码同源的主证据。所有 POSIX 标准系统调用名（如 `do_fork`, `do_open`）和 Rust 语言标准库 API（如 `Arc::new`, `Option::map`）均为行业规范或语言基础设施，不具备项目来源特异性，一律降权为弱信号或无效信号。

### 6. 保守结论
综合客观 AST 证据，目标项目与参考项目的整体相似度极低（0.92%），在进程管理、系统调用、设备驱动等核心板块的共同结构指纹均为零。双方在内存模型、文件系统抽象、驱动框架、第三方库选型及网络协议栈实现上存在根本性架构差异。观察到的微弱相似性（覆盖率 1.32%）完全可归因于对 POSIX 规范、RISC-V/LoongArch 硬件手册的遵循以及 Rust 编程语言通用惯用法的使用。因此，无任何证据表明目标项目对参考项目存在代码抄袭或架构复用，应认定为**独立开发**。

------------------------------

