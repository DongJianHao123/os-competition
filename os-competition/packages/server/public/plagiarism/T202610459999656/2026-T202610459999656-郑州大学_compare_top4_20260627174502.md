# 🛡️ OS 内核赛自动化代码级架构审计报告

**审计时间**: `2026-06-27 17:45:02`
**参赛作品名称**: `2026-T202610459999656-郑州大学`
**年份**: `2026`
**学校**: `郑州大学`
**队伍名称**: `T202610459999656`
**比赛名字**: `OS-Competition`
**比赛子赛道**: `Kernel-Implementation`
**仓库地址**: `https://gitlab.eduxiji.net/educg-group-43501-3132633/T202610459999656-3554`
**推测开发语言**: `Rust / C (由AST探针自动识别)`
**特征函数量**: `10439`

## ⚔️ 底层代码同源性对比 (AST Homology)
> 本部分由专门的代码审查智能体，对该项目与符合年份条件的历届最高相似度项目进行控制流与底层调用对比。新版报告会先给出确定性的全模块覆盖矩阵，再给出 Agent 的分模块解释，避免只围绕少量抽样函数展开。

### 🏆 Top 4 底层对标: vs `2023-Starry-清华大学`（清华大学 / Starry）

#### 对方作品信息

**对方年份**: `2023`
**对方学校**: `清华大学`
**对方队伍名称**: `Starry`
**对方比赛名字**: `OS-Competition`
**对方比赛子赛道**: `Kernel-Implementation`
**对方仓库地址**: `https://gitlab.eduxiji.net/202310003101649/starry`

> 双向覆盖率：共同 AST 指纹: 95；唯一 AST 指纹: 目标项目 8414，参考项目 6206；覆盖率: 目标项目 1.13%，参考项目 1.53%

## 全模块对比总览（确定性证据）

> 总体覆盖率：共同 AST 指纹: 95；唯一 AST 指纹: 目标项目 8414，参考项目 6206；覆盖率: 目标项目 1.13%，参考项目 1.53%

### 1. 模块覆盖矩阵
| 模块 | 目标函数数 | 参考函数数 | 共同唯一 AST 指纹 | Target 覆盖 | Base 覆盖 | 状态 |
| :--- | ---: | ---: | ---: | ---: | ---: | :--- |
| 进程管理 | 1374 | 128 | 3 | 0.28% | 2.94% | 双方存在且有结构重合 |
| 任务调度/异步运行时 | 861 | 296 | 4 | 0.57% | 1.65% | 双方存在且有结构重合 |
| 内存管理 | 2374 | 257 | 4 | 0.19% | 1.71% | 双方存在且有结构重合 |
| 中断/异常处理 | 130 | 40 | 2 | 1.94% | 5.26% | 双方存在且有结构重合 |
| 系统调用 | 613 | 64 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |
| 文件系统 | 2689 | 379 | 8 | 0.37% | 2.6% | 双方存在且有结构重合 |
| 设备驱动 | 312 | 57 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |
| 网络协议栈 | 103 | 73 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |
| 文件系统/第三方库 | 1508 | 487 | 12 | 0.96% | 2.69% | 双方存在且有结构重合 |
| 网络协议栈/第三方库 | 2 | 110 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |
| 第三方库 | 0 | 5822 | 0 | 0.0% | 0.0% | 仅参考项目体现 |
| 通用模块/其他 | 473 | 341 | 4 | 0.97% | 1.41% | 双方存在且有结构重合 |

### 2. 分模块证据清单

#### 进程管理
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `os/src/thread/scheduler.rs#exit_current` | `modules/axtask/src/monolithic_task/run_queue.rs#exit_current` | 同板块同名 | SCHEDULER.lock, Some, Thread::current, Thread::current().expect, crate::cpu::current_hart_racy, current.release_kernel_fs_on_exit ...(+8) | EXITED_TASKS.lock, EXITED_TASKS.lock().clear, EXITED_TASKS.lock().push_back, WAIT_FOR_EXIT.notify_one_locked, assert!, axhal::misc::terminate ...(+6) |
| `os/src/sync/wait.rs#wait_until` | `modules/axtask/src/wait_queue.rs#wait_until` | 同板块同名 | Waiter::new_pair, cond, self.enqueue, self.remove_waker, waiter.wait_uninterruptible, waker.clone | RUN_QUEUE.lock, condition, crate::current, rq.block_current, self.cancel_events, self.queue.lock ...(+2) |
| `os/src/syscall/ipc.rs#current_process` | `modules/axprocess/src/process.rs#current_process` | 同板块同名 | Error::new, ctx.current, ctx.current()
        .user_process, ctx.current()
        .user_process()
        .ok_or_else | Arc::clone, PID2PC.lock, curr.get_process_id, current, drop, pid2pc_inner.get ...(+1) |
| `os/src/oscomp_runner/full_catalog.rs#executable` | `modules/axprocess/src/stdin.rs#executable` | 同板块同名 | 内部闭环逻辑 | 内部闭环逻辑 |

#### 任务调度/异步运行时
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `os/src/syscall/futex.rs#futex_requeue` | `ulib/starry_libax/src/syscall/futex.rs#futex_requeue` | 同板块同名 | FutexWakeTrace {
            key: target_key,
            uaddr: uaddr2,
            op,
            count: requeue_count,
            bitset: FUTEX_BITSET_MATCH_ANY,
        }
        .record, FutexWakeTrace {
        key: source_key,
        uaddr,
        op,
        count: wake_count,
        bitset: FUTEX_BITSET_MATCH_ANY,
    }
    .record, Ok, cmp_value
        .map, cmp_value
        .map(|_| fault_in_futex_value(ctx.vm_space(), uaddr))
        .transpose, cmp_value.unwrap ...(+9) | FUTEX_WAIT_TASK.lock, VecDeque::new, WAIT_FOR_FUTEX.notify_task, dst_wait_task.append, futex_wait_task.contains_key, futex_wait_task.get_mut ...(+9) |
| `os/src/runner_timeout.rs#current` | `ulib/libax/src/cbindings/pthread/mod.rs#current` | 同板块同名 | crate::thread::Thread::current, current
                .and_then, current
                .and_then(|thread| thread.user_tid())
                .map, current
                .and_then(|thread| thread.user_tid())
                .map(|tid| tid.as_u32() as u64)
                .unwrap_or, current
                .as_ref, current
                .as_ref()
                .and_then ...(+11) | Self::current_ptr, core::ptr::NonNull::new, core::ptr::NonNull::new(Self::current_ptr()).map, ptr.as_ref |
| `os/src/runner_timeout.rs#current` | `modules/axtask/src/api.rs#current` | 同板块同名 | crate::thread::Thread::current, current
                .and_then, current
                .and_then(|thread| thread.user_tid())
                .map, current
                .and_then(|thread| thread.user_tid())
                .map(|tid| tid.as_u32() as u64)
                .unwrap_or, current
                .as_ref, current
                .as_ref()
                .and_then ...(+11) | CurrentTask::get |
| `os/src/thread/scheduler.rs#yield_now` | `modules/axtask/src/api_s.rs#yield_now` | 同板块同名 | SCHEDULER.lock, Thread::current, crate::cpu::current_hart_racy, current.set_state, next.set_state, processor::switch_to_thread ...(+3) | axhal::arch::wait_for_irqs, cfg!, core::hint::spin_loop |

#### 内存管理
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `os/src/mm/heap/allocator.rs#alloc` | `modules/axalloc/src/lib.rs#alloc` | 同板块同名 | CommonSizeClass::from_size, CommonSizeClass::from_size(size).ok_or, Err, GLOBAL_HEAP_ALLOCATOR.lock, GLOBAL_HEAP_ALLOCATOR.lock().alloc_from_class, HeapSlot::alloc_large ...(+10) | Ok, balloc.add_memory, balloc.alloc, balloc.total_bytes, debug!, old_size.max ...(+4) |
| `os/src/mm/heap/global_alloc.rs#alloc` | `modules/axalloc/src/page.rs#alloc` | 同板块同名 | allocator::alloc, caller_return_address, check_slab256_dealloc_watch, clear_slab256_dealloc_watch_if_reused, panic!, ptr::null_mut ...(+10) | global_allocator, global_allocator()
            .alloc_pages, global_allocator()
            .alloc_pages(1, PAGE_SIZE)
            .map, global_allocator()
            .alloc_pages(1, PAGE_SIZE)
            .map(|vaddr| Self {
                start_vaddr: vaddr.into(),
                num_pages: 1,
            })
            .map_err, vaddr.into |
| `os/src/mm/heap/global_alloc.rs#dealloc` | `modules/axmem/src/area.rs#dealloc` | 同板块同名 | HeapSlot::new, Slab256CurrentDealloc::new, Some, allocator::dealloc, arm_slab256_dealloc_watch_if_needed, caller_return_address ...(+9) | page_table.unmap_region, page_table.unmap_region(self.vaddr, self.size()).unwrap, self.pages.clear, self.size |
| `os/src/hal/imp/arch/riscv64/boot_translation.rs#init_boot_page_table` | `modules/axhal/src/platform/raspi4_aarch64/mem.rs#init_boot_page_table` | 同板块同名 | (*boot_l3pt).0.as_mut_ptr, (*boot_l4pt).0.as_mut_ptr, KERNEL_L3_BASE_INDEX.wrapping_add, addr_of!, addr_of!(BOOT_L3PT).cast_mut, addr_of!(BOOT_L4PT).cast_mut ...(+9) | A64PTE::new_page, A64PTE::new_table, PhysAddr::from, boot_pt_l1.as_ptr |

#### 中断/异常处理
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `os/src/hal/irq/external.rs#register_handler` | `modules/axhal/src/platform/qemu_virt_riscv/irq.rs#register_handler` | 同板块同名 | HANDLERS.lock, Some, assert!, panic!, table[irq].is_some | with_cause! |
| `os/src/hal/imp/arch/riscv64/irq.rs#local_enabled` | `modules/axhal/src/arch/riscv/mod.rs#irqs_enabled` | 同板块同结构 | sstatus::read, sstatus::read().sie | sstatus::read, sstatus::read().sie |
| `os/src/hal/irq.rs#disable_local` | `modules/axhal/src/arch/aarch64/mod.rs#wait_for_irqs` | 同板块同结构 | arch::irq::disable_local | aarch64_cpu::asm::wfi |
| `os/src/syscall/net.rs#ktest_socket_finite_timeout_data_plane` | `ulib/libax/src/cbindings/time.rs#ax_nanosleep` | 同模块上下文对齐/差异观察 | Err, Error::new, Ok, SocketDeadline, SocketDeadline::none, SocketSendOptions::from_nonblocking ...(+33) | ax_call_body! |

#### 系统调用
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `os/src/syscall/net.rs#accept` | `ulib/starry_libax/src/syscall/socket.rs#accept` | 同板块同名 | Err, Error::new, Ok, self.state.lock, state.pending.is_empty, state.pending.pop_front ...(+1) | Err, Ok, SocketInner::Tcp, String::from, new_socket.peer_addr, s.accept ...(+2) |
| `os/src/syscall/net.rs#shutdown` | `ulib/starry_libax/src/syscall/socket.rs#shutdown` | 同板块同名 | self.close_send_side, self.recv_closed.store, self.stream.wake_readiness_waiters | s.close, s.shutdown |
| `os/src/syscall/net.rs#connect` | `ulib/starry_libax/src/syscall/socket.rs#connect` | 同板块同名 | peer.canonical, peer.is_loopback_route, self.connect_datagram, self.connect_external, self.connect_loopback | s.connect |
| `os/src/syscall/net.rs#is_nonblocking` | `ulib/starry_libax/src/syscall/socket.rs#is_nonblocking` | 同板块同名 | self.current_status_flags, self.current_status_flags()
            .contains | s.is_nonblocking |

#### 文件系统
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `os/src/fs/tmpfs.rs#read_dir` | `modules/axfs/src/fs/fatfs.rs#read_dir` | 同板块同名 | Err, Error::new, Ok, cookie.saturating_add, emit_tmpfs_dirent, entries
                    .snapshot_from_cookie ...(+9) | Ok, VfsDirEntry::new, dirents.iter_mut, dirents.iter_mut().enumerate, dirents.len, entry.file_name ...(+6) |
| `os/src/fs/miscdevfs.rs#read_dir` | `modules/axfs/src/fops.rs#read_dir` | 同板块同名 | Ok, cookie.saturating_add, emit, from_cookie.saturating_sub, index.saturating_add, inode.ino ...(+3) | Ok, self
            .node
            .access, self
            .node
            .access(Cap::READ)?
            .read_dir |
| `os/src/fs/miscdevfs.rs#ioctl` | `modules/axfs/src/monolithic_fs/file_io.rs#ioctl` | 同板块同名 | Err, Error::new, IoctlRequest::decode, Ok, Self::is_epoch_read_request, Self::is_read_time_request ...(+5) | Err |
| `os/src/fs/tmpfs.rs#create` | `modules/axfs/src/fs/fatfs.rs#create` | 同板块同名 | Err, Error::new, Ok, String::from, add_link_count, child.clone ...(+7) | Err, Ok, info!, path.is_empty, path.strip_prefix, path.trim_matches ...(+5) |

#### 设备驱动
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `os/src/hal/imp/platform/fdt.rs#probe` | `modules/axdriver/src/lib.rs#probe` | 同板块同名 | Error::with_message, Fdt::from_ptr, Ok, Some, arch::mm::phys_to_virt, arch::mm::phys_to_virt(dtb_pa).as_ptr::<u8> ...(+13) | for_each_drivers! |
| `os/src/drivers/virtio/transport_dma.rs#mmio_phys_to_virt` | `modules/axdriver/src/virtio.rs#mmio_phys_to_virt` | 同板块同名 | NonNull::new, NonNull::new(vaddr).expect, assert!, crate::hal::PhysAddr::new, crate::hal::phys_to_virt_io, crate::hal::phys_to_virt_io(crate::hal::PhysAddr::new(paddr as usize))
            .as_mut_ptr::<u8> | NonNull::new, NonNull::new(phys_to_virt(paddr.into()).as_mut_ptr()).unwrap, paddr.into, phys_to_virt, phys_to_virt(paddr.into()).as_mut_ptr |
| `os/src/hal/boot/bootinfo.rs#push` | `modules/axdriver/src/structs/static.rs#push` | 同板块同名 | panic!, self.entries.len, self.len | Some, self.0.is_none |
| `os/src/drivers/virtio/transport_dma.rs#unshare` | `modules/axdriver/src/virtio.rs#unshare` | 同板块同名 | 内部闭环逻辑 | 内部闭环逻辑 |

#### 网络协议栈
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `os/src/net/stack.rs#send_to` | `ulib/libax/src/net/udp.rs#send_to` | 同板块同名 | NetworkDeadline::none, Ok, self.send_to_with_deadline | self.socket.send_to |
| `os/src/net/stack.rs#recv_from` | `ulib/libax/src/net/udp.rs#recv_from` | 同板块同名 | NetworkDeadline::none, Ok, self.recv_from_with_deadline | self.socket.recv_from |
| `os/src/net/stack.rs#recv` | `ulib/libax/src/cbindings/socket.rs#recv` | 同板块同名 | self.stack.tcp_recv | Ok, tcpsocket.lock, tcpsocket.lock().recv, udpsocket.lock, udpsocket.lock().recv_from, udpsocket.lock().recv_from(buf).map |
| `os/src/net/stack.rs#send` | `ulib/libax/src/cbindings/socket.rs#send` | 同板块同名 | self.stack.tcp_send | Ok, tcpsocket.lock, tcpsocket.lock().send, udpsocket.lock, udpsocket.lock().send |

#### 文件系统/第三方库
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `os/src/fs/ext4.rs#rename` | `extern_crates/rust-fatfs-a3a834e/src/dir.rs#rename` | 同板块同名 | Arc::ptr_eq, DstUpdate::ReplacedExistingEntry, Err, Error::new, Error::with_message, Ok ...(+55) | Some, dst_dir.find_entry, e.to_dir, e.to_dir().rename, self.find_entry, self.rename ...(+3) |
| `os/src/fs/ext4.rs#create` | `crates/axfs_ramfs/src/dir.rs#create` | 同板块同名 | Arc::new, Default::default, Err, Error::new, Ext4Extent::new, Ext4Inode::default ...(+51) | Ok, log::debug!, name.is_empty, self
                        .children
                        .read, self
                        .children
                        .read()
                        .get, self
                        .children
                        .read()
                        .get(name)
                        .ok_or ...(+8) |
| `os/third_party/rsext4/src/ext4_backend/hashtree.rs#lookup` | `crates/axfs_devfs/src/dir.rs#lookup` | 同板块同名 | Ok, debug!, dir_inode.is_htree_indexed, htree_dir::calculate_hash, is_supported_hash_version, root_info.hash_version ...(+6) | Ok, node.lookup, self
                .children
                .read, self
                .children
                .read()
                .get, self
                .children
                .read()
                .get(name)
                .cloned, self
                .children
                .read()
                .get(name)
                .cloned()
                .ok_or ...(+4) |
| `os/src/fs/ext4.rs#flush` | `extern_crates/rust-fatfs-a3a834e/src/dir_entry.rs#flush` | 同板块同名 | EXT4_DEV_FLUSH_SEQ.fetch_add, Err, Ok, crate::resource_accounting::record_ext4_flush_error, log::debug!, map_bio_enqueue_error_to_rsext4 ...(+2) | Ok, self.write |

#### 网络协议栈/第三方库
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `os/src/net/stack.rs#smoltcp_now` | `modules/axnet/src/smoltcp_impl/tcp.rs#connect` | 同模块上下文对齐/差异观察 | Instant::from_micros, crate::time::uptime_us, crate::time::uptime_us().min | Err, LOOPBACK.try_get, LOOPBACK.try_get().unwrap, Ok, SOCKET_SET.add, SOCKET_SET.with_socket_mut::<tcp::Socket, _, _> ...(+27) |
| `os/src/net/stack.rs#smoltcp_duration_to_capped_us` | `modules/axnet/src/smoltcp_impl/dns.rs#query` | 同模块上下文对齐/差异观察 | delay.total_micros, delay.total_micros().min | Err, Ok, SOCKET_SET
            .with_socket_mut::<dns::Socket, _, _>, SOCKET_SET
            .with_socket_mut::<dns::Socket, _, _>(handle, |socket| {
                socket.start_query(iface.lock().context(), name, query_type)
            })
            .map_err, SOCKET_SET.poll_interfaces, SOCKET_SET.with_socket_mut::<dns::Socket, _, _> ...(+9) |

#### 第三方库
仅参考项目中观察到该模块代表函数，目标项目没有同模块可对齐证据：
- 参考项目：`extern_crates/libz-sys-1.1.8/src/zlib/contrib/inflate86/inffas86.c#inflate_fast`，调用摘要：内部闭环逻辑
- 参考项目：`extern_crates/libz-sys-1.1.8/src/zlib-ng/inflate.c#PREFIX`，调用摘要：BITS, BYTEBITS, CRC2, CRC4, DROPBITS, INFLATE_MARK_HOOK ...(+38)
- 参考项目：`extern_crates/miniz_oxide-0.6.2/src/inflate/core.rs#decompress`，调用摘要：OutputBuffer::from_slice_and_pos, cfg!, generate_state!, in_buf.iter, in_buf.len, in_iter.len ...(+9)
- 参考项目：`extern_crates/cc-1.0.79/src/lib.rs#add_default_flags`，调用摘要："--target=aarch64-unknown-windows-gnu".into, "--target=i686-unknown-windows-gnu".into, "--target=x86_64-unknown-windows-gnu".into, "-Brepro".into, "-DANDROID".into, "-D_ARM_WINAPI_PARTITION_DESKTOP_SDK_AVAILABLE=1".into ...(+103)

#### 通用模块/其他
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `os/src/logger.rs#write_bytes` | `modules/axhal/src/lib.rs#write_bytes` | 同板块同名 | bytes.is_empty, bytes.len, bytes.len().min, self.buf[..bytes.len() - first_len].copy_from_slice, self.buf[start..start + first_len].copy_from_slice, self.ring_index | putchar |
| `os/src/logger.rs#write_str` | `modules/axlog/src/lib.rs#write_str` | 同板块同名 | Ok, remain.min, s.as_bytes, s.len, self.buf.len, self.buf.len().saturating_sub ...(+1) | Ok, cfg_if::cfg_if! |
| `os/src/sync/spin_lock.rs#lock` | `modules/axsync/src/mutex.rs#lock` | 同板块同名 | G::guard, core::any::type_name::<T>, crate::hal::cpu::relax, crate::runner_timeout::record_spin_contention, self.inner.try_lock | assert_ne!, current, current().id, current().id().as_u64, self.data.get, self.is_locked ...(+2) |
| `os/src/hal/boot/bss.rs#clear_bss` | `modules/axhal/src/mem.rs#clear_bss` | 同板块同名 | core::ptr::addr_of_mut!, core::ptr::write_bytes, debug_assert! | core::slice::from_raw_parts_mut, core::slice::from_raw_parts_mut(sbss as usize as *mut u8, ebss as usize - sbss as usize)
            .fill |


#### 1. 宏观判定
- **定性结论**: 独立开发 / 底层架构显著差异 (相似度: 0.65%，共同 AST 指纹: 95；唯一 AST 指纹: 目标项目 8414，参考项目 6206；覆盖率: 目标项目 1.13%，参考项目 1.53%)

#### 2. 全模块覆盖矩阵解读
- **进程管理**: 双方均存在。共同 AST 指纹仅 3 个，目标覆盖率 0.28%，参考覆盖率 2.94%。共同指纹均为 `self.field.store/load` 单节点原子操作，属于 Rust 惯用法与 Linux ABI 命名约束下的巧合，证据强度为**弱**。
- **任务调度/异步运行时**: 双方均存在。共同 AST 指纹 4 个，目标覆盖率 0.57%，参考覆盖率 1.65%。同名函数如 `yield_now` 的实现路径完全不同（目标使用 `SCHEDULER.lock`，参考使用 `wait_for_irqs`），证据强度为**弱**。
- **内存管理**: 双方均存在。共同 AST 指纹 4 个，目标覆盖率 0.19%，参考覆盖率 1.71%。`alloc` 等函数虽同名，但底层分配器实现（目标基于 size-class，参考基于 pages）截然不同，证据强度为**弱**。
- **中断/异常处理**: 双方均存在。共同 AST 指纹 2 个，目标覆盖率 1.94%，参考覆盖率 5.26%。共同指纹为 RISC-V `sstatus::read().sie` 这类架构强制指令序列，证据强度为**弱**。
- **系统调用**: 双方均存在。未观察到共同结构指纹，覆盖率为 0.0%。同名函数如 `accept`、`shutdown` 等调用流完全不同，证据强度为**未形成**。
- **文件系统**: 双方均存在。共同 AST 指纹 8 个，目标覆盖率 0.37%，参考覆盖率 2.6%。同名函数如 `read_dir`、`create` 等底层实现（目标基于 ext4/tmpfs，参考基于 fatfs/ramfs）完全不同，证据强度为**弱**。
- **设备驱动**: 双方均存在。未观察到共同结构指纹，覆盖率为 0.0%。`probe` 等函数实现路径完全不同，证据强度为**未形成**。
- **网络协议栈**: 双方均存在。未观察到共同结构指纹，覆盖率为 0.0%。同名函数如 `send_to`、`recv_from` 等调用流完全不同，证据强度为**未形成**。
- **第三方库/公共依赖**: 参考项目存在大量第三方库（5822 个函数），目标项目无此独立模块。双方在文件系统/第三方库、网络协议栈/第三方库子模块中虽有少量共同指纹，但均为 Rust 标准库或通用算法惯用法，证据强度为**弱**。

#### 3. 底层 AST 关键证据指纹表
| 所属板块 | 目标项目路径 | 参考项目路径 | 证据类型 | 技术说明 |
| :--- | :--- | :--- | :--- | :--- |
| 进程管理 | os/src/thread/thread_local.rs#set_clear_child_tid | modules/axtask/src/monolithic_task/task.rs#set_clear_child_tid | 同板块同名 | 均为 `self.clear_child_tid.store`。字段名源自 Linux `CLONE_CHILD_CLEARTID` ABI，原子存储为 Rust 标准用法，不构成独创表达。 |
| 进程管理 | os/src/thread/thread_local.rs#set_child_tid | modules/axtask/src/monolithic_task/task.rs#set_child_tid | 同板块同名 | 均为 `self.set_child_tid.store`。字段名源自 Linux `CLONE_CHILD_SETTID` ABI，原子存储为 Rust 标准用法，不构成独创表达。 |
| 进程管理 | os/src/process.rs#set_child_subreaper | modules/axtask/src/monolithic_task/task.rs#set_in_wait_queue | 同板块同结构 | 均为 `self.xxx.store`。业务语义不同（进程收养标记 vs 等待队列标记），仅原子操作惯用法巧合。 |
| 进程管理 | os/src/process.rs#is_child_subreaper | modules/axtask/src/monolithic_task/task.rs#in_wait_queue | 同板块同结构 | 均为 `self.xxx.load`。同上，原子操作惯用法巧合。 |
| 进程管理 | os/src/thread/scheduler.rs#exit_current | modules/axtask/src/run_queue.rs#exit_current | 同板块同名 | 目标使用 `SCHEDULER.lock` + `release_kernel_fs_on_exit`；参考使用 `EXITED_TASKS.lock` + `axhal::misc::terminate`。调度架构根本不同。 |
| 进程管理 | os/src/sync/wait.rs#wait_until | modules/axtask/src/wait_queue.rs#wait_until | 同板块同名 | 目标使用 `Waiter::new_pair` + `wait_uninterruptible`；参考使用 `RUN_QUEUE.lock` + `block_current`。同步原语实现完全不同。 |
| 进程管理 | os/src/syscall/ipc.rs#current_process | modules/axprocess/src/process.rs#current_process | 同板块同名 | 目标使用 `ctx.current().user_process()`；参考使用 `PID2PC.lock` + `get_process_id`。进程查找机制完全不同。 |
| 内存管理 | os/src/mm/heap/allocator.rs#alloc | modules/axalloc/src/lib.rs#alloc | 同板块同名 | 目标基于 `CommonSizeClass` 分级分配；参考基于 `balloc` 字节分配器。分配策略完全不同。 |
| 内存管理 | os/src/hal/imp/arch/riscv64/boot_translation.rs#init_boot_page_table | modules/axhal/src/platform/raspi4_aarch64/mem.rs#init_boot_page_table | 同板块同名 | 目标为 RISC-V 四级页表初始化；参考为 AArch64 页表初始化。架构指令集完全不同。 |
| 中断/异常处理 | os/src/hal/imp/arch/riscv64/irq.rs#local_enabled | modules/axhal/src/arch/riscv/mod.rs#irqs_enabled | 同板块同结构 | 均为 `sstatus::read().sie`。RISC-V 特权架构标准 CSR 读取序列，无独创性。 |
| 文件系统 | os/src/fs/tmpfs.rs#read_dir | modules/axfs/src/fs/fatfs.rs#read_dir | 同板块同名 | 目标基于 tmpfs 内存目录项快照；参考基于 fatfs 磁盘目录项迭代。文件系统类型与实现完全不同。 |
| 文件系统 | os/src/fs/ext4.rs#create | crates/axfs_ramfs/src/dir.rs#create | 同板块同名 | 目标基于 ext4 磁盘 inode 分配；参考基于 ramfs 内存哈希表插入。文件系统类型与实现完全不同。 |
| 文件系统/第三方库 | os/third_party/rsext4/src/ext4_backend/hashtree.rs#lookup | crates/axfs_devfs/src/dir.rs#lookup | 同板块同名 | 目标为 ext4 哈希树磁盘查找；参考为 devfs 内存哈希表查找。数据结构与存储介质完全不同。 |
| 通用模块/其他 | os/src/logger.rs#write_bytes | modules/axhal/src/lib.rs#write_bytes | 同板块同名 | 目标使用环形缓冲区；参考使用逐字符 `putchar`。日志输出机制完全不同。 |
| 通用模块/其他 | os/src/sync/spin_lock.rs#lock | modules/axsync/src/mutex.rs#lock | 同板块同名 | 目标使用 `G::guard` + `try_lock`；参考使用 `assert_ne!` + `current().id()`。锁实现与调试机制完全不同。 |

#### 4. 核心代码调用流对比剖析
* **【进程管理/线程退出路径】**
* **底层技术析理**: 目标项目线程退出通过 `Thread::exit_current` 调用 `scheduler::exit_current`，在 `SCHEDULER.lock` 下进行状态切换，并调用 `release_kernel_fs_on_exit` 清理内核文件系统资源。参考项目通过 `run_queue::exit_current`，将任务推入 `EXITED_TASKS` 队列，通过 `WAIT_FOR_EXIT` 通知等待者，最终调用 `axhal::misc::terminate` 终止执行。两者在调度器数据结构、资源清理路径和终止机制上完全不同。
* **代码溯源链接**:
  - 目标项目: `os/src/thread.rs#exit_current` -> `os/src/thread/scheduler.rs#exit_current`
  - 参考项目: `modules/axtask/src/run_queue.rs#exit_current`

* **【内存管理/堆分配路径】**
* **底层技术析理**: 目标项目 `GlobalHeapAllocator::alloc` 根据请求大小通过 `CommonSizeClass::from_size` 选择大小类，从对应 slab 缓存或大块分配器中获取内存。参考项目 `balloc::alloc` 基于字节级分配器，直接调用 `add_memory` 和 `alloc` 方法。两者在分配策略（size-class vs. byte-level）、元数据管理和碎片处理上采用完全不同的算法。
* **代码溯源链接**:
  - 目标项目: `os/src/mm/heap/global_alloc.rs#alloc` -> `os/src/mm/heap/allocator.rs#alloc`
  - 参考项目: `modules/axalloc/src/lib.rs#alloc`

* **【中断处理/中断启用状态检查】**
* **底层技术析理**: 两者均通过读取 RISC-V `sstatus` 寄存器的 `sie` 位来判断中断是否启用。这是 RISC-V 特权架构规范定义的唯一方法，任何 RISC-V 内核都必须执行相同的 CSR 读取序列，不构成可保护的表达。
* **代码溯源链接**:
  - 目标项目: `os/src/hal/imp/arch/riscv64/irq.rs#local_enabled`
  - 参考项目: `modules/axhal/src/arch/riscv/mod.rs#irqs_enabled`

* **【文件系统/目录读取路径】**
* **底层技术析理**: 目标项目 `tmpfs::read_dir` 从内存中的 `BTreeMap` 快照目录项，通过 `cookie` 实现分页读取。参考项目 `fatfs::read_dir` 迭代 FAT 文件系统的磁盘目录项结构 `VfsDirEntry`。两者在存储介质（内存 vs. 磁盘）、目录项格式和迭代机制上完全不同。
* **代码溯源链接**:
  - 目标项目: `os/src/fs/tmpfs.rs#read_dir`
  - 参考项目: `modules/axfs/src/fs/fatfs.rs#read_dir`

#### 5. 单方模块与降权项
- **第三方库**: 参考项目包含 5822 个第三方库函数（如 `libz-sys`, `miniz_oxide`, `cc`），目标项目无此独立模块。这些库为标准开源组件，与目标项目无任何关联。
- **公共接口/泛型函数名**: 双方存在大量同名函数（如 `executable`, `readable`, `writable`, `is_dentry_cacheable`），但均为 trait 实现或标准文件/IO 接口，函数体为空或仅返回常量，属于“同名异构”，不构成同源证据。
- **Linux ABI 约束项**: `set_child_tid`, `clear_child_tid`, `child_subreaper` 等字段名直接来自 Linux 内核 ABI 与 `prctl` 接口，是任何兼容 Linux 的内核都必须实现的公共接口，不能作为独创表达的证据。

#### 6. 保守结论
综合全模块 AST 指纹分析，目标项目与参考项目的客观相似度仅为 0.65%，共同 AST 指纹仅 95 个，覆盖率不足 1.6%。所有观察到的共同指纹均为 Rust 语言原子操作惯用法、RISC-V 架构强制指令序列或 Linux 标准 ABI 命名约束下的最小公共子树，不具备可识别来源的独创性。在进程调度、内存分配、文件系统、中断处理等核心子系统上，两者采用了完全不同的架构设计、数据结构和算法实现。现有证据远不足以支持代码复用或同源性指控，应认定为独立开发。

------------------------------

