# 🛡️ OS 内核赛自动化代码级架构审计报告

**审计时间**: `2026-07-02 11:31:23`
**参赛作品名称**: `2026-T202610486999873-XX`
**年份**: `2026`
**学校**: `XX`
**队伍名称**: `T202610486999873`
**比赛名字**: `OS-Competition`
**比赛子赛道**: `Kernel-Implementation`
**仓库地址**: `https://gitlab.eduxiji.net/educg-group-43501-3132633/T202610486999873-1237`
**推测开发语言**: `Rust / C (由AST探针自动识别)`
**特征函数量**: `1888`

## ⚔️ 底层代码同源性对比 (AST Homology)
> 本部分由专门的代码审查智能体，对该项目与符合年份条件的历届最高相似度项目进行控制流与底层调用对比。新版报告会先给出确定性的全模块覆盖矩阵，再给出 Agent 的分模块解释，避免只围绕少量抽样函数展开。

### 🏆 Top 1 底层对标: vs `2024-undefinedType-南京航空航天大学`（南京航空航天大学 / undefinedType）

#### 对方作品信息

**对方年份**: `2024`
**对方学校**: `南京航空航天大学`
**对方队伍名称**: `undefinedType`
**对方比赛名字**: `OS-Competition`
**对方比赛子赛道**: `Kernel-Implementation`
**对方仓库地址**: `https://gitlab.eduxiji.net/educg-group-26010-2376550/T202410287992755-2176`

> 双向覆盖率：共同 AST 指纹: 378；唯一 AST 指纹: 目标项目 1513，参考项目 398；覆盖率: 目标项目 24.98%，参考项目 94.97%

## 全模块对比总览（确定性证据）

> 总体覆盖率：共同 AST 指纹: 378；唯一 AST 指纹: 目标项目 1513，参考项目 398；覆盖率: 目标项目 24.98%，参考项目 94.97%

### 1. 模块覆盖矩阵
| 模块 | 目标函数数 | 参考函数数 | 共同唯一 AST 指纹 | Target 覆盖 | Base 覆盖 | 状态 |
| :--- | ---: | ---: | ---: | ---: | ---: | :--- |
| 进程管理 | 263 | 0 | 0 | 0.0% | 0.0% | 仅目标项目体现 |
| 任务调度/异步运行时 | 158 | 0 | 0 | 0.0% | 0.0% | 仅目标项目体现 |
| 内存管理 | 185 | 0 | 0 | 0.0% | 0.0% | 仅目标项目体现 |
| 中断/异常处理 | 0 | 0 | 0 | 0.0% | 0.0% | 双方均未体现 |
| 系统调用 | 181 | 0 | 0 | 0.0% | 0.0% | 仅目标项目体现 |
| 文件系统 | 290 | 0 | 0 | 0.0% | 0.0% | 仅目标项目体现 |
| 设备驱动 | 23 | 0 | 0 | 0.0% | 0.0% | 仅目标项目体现 |
| 网络协议栈 | 36 | 0 | 0 | 0.0% | 0.0% | 仅目标项目体现 |
| 文件系统/第三方库 | 646 | 478 | 378 | 70.52% | 94.97% | 双方存在且有结构重合 |
| 网络协议栈/第三方库 | 1 | 0 | 0 | 0.0% | 0.0% | 仅目标项目体现 |
| 通用模块/其他 | 105 | 0 | 0 | 0.0% | 0.0% | 仅目标项目体现 |

### 2. 分模块证据清单

#### 进程管理
仅目标项目中观察到该模块代表函数，参考项目没有同模块可对齐证据：
- 目标项目：`os/src/syscall/process.rs#sys_pselect6`，调用摘要：copy_fdset, core::mem::size_of::<FdSet>, core::mem::size_of::<crate::task::TimeSpec>, crate::net::net_poll, crate::task::check_itimers, current_task ...(+23)
- 目标项目：`os/src/syscall/process.rs#sys_setitimer`，调用摘要：(new_itv.it_interval.sec as u64)
        .checked_mul, (new_itv.it_interval.sec as u64)
        .checked_mul(1_000_000)
        .and_then, (new_itv.it_value.sec as u64)
        .checked_mul, (new_itv.it_value.sec as u64)
        .checked_mul(1_000_000)
        .and_then, core::mem::size_of::<ITimerVal>, current_task ...(+14)
- 目标项目：`os/src/syscall/process.rs#sys_waitpid`，调用摘要：Some, child
                    .try_inner_borrow, child
                    .try_inner_borrow()
                    .map, child
                    .try_inner_borrow()
                    .map(|inner| inner.is_zombie())
                    .unwrap_or, child.getpid, child.inner_exclusive_access ...(+19)
- 目标项目：`os/src/syscall/process.rs#sys_waitid`，调用摘要：Some, child
                    .try_inner_borrow, child
                    .try_inner_borrow()
                    .map, child
                    .try_inner_borrow()
                    .map(|inner| inner.is_zombie())
                    .unwrap_or, child.getpid, child.inner_exclusive_access ...(+17)

#### 任务调度/异步运行时
仅目标项目中观察到该模块代表函数，参考项目没有同模块可对齐证据：
- 目标项目：`user/src/bin/user_shell.rs#main`，调用摘要：ProcessArguments::new, String::from, String::new, Vec::new, args_addr.as_slice, args_copy.len ...(+51)
- 目标项目：`os/src/syscall/fs.rs#sys_ioctl`，调用摘要：core::mem::size_of::<usize>, crate::fs::inode::path_fs_flags, crate::fs::inode::set_path_fs_flags, current_task, current_task().unwrap, current_user_token ...(+17)
- 目标项目：`os/src/syscall/fs.rs#sys_utimensat`，调用摘要：(times as *const TimeSpec).add, Some, a.clamp, crate::fs::inode::update_path_cached_times, current_task, current_task().unwrap ...(+31)
- 目标项目：`os/src/syscall/fs.rs#sys_fcntl`，调用摘要：(arg..inner.fd_table.len()).find, F_UNLCK.to_le_bytes, Some, buf[0..2].copy_from_slice, buf[24..28].copy_from_slice, crate::mm::translated_byte_buffer ...(+26)

#### 内存管理
仅目标项目中观察到该模块代表函数，参考项目没有同模块可对齐证据：
- 目标项目：`os/src/syscall/mod.rs#syscall`，调用摘要：LAST_SYSCALL_ARG0.store, LAST_SYSCALL_ARG1.store, LAST_SYSCALL_ARG2.store, LAST_SYSCALL_ID.store, LAST_SYSCALL_PID.store, LAST_SYSCALL_RET.store ...(+158)
- 目标项目：`os/src/task/task.rs#exec_loaded`，调用摘要：(polyhal::time::Time::now().to_nsec() as u64)
            .wrapping_mul, (polyhal::time::Time::now().to_nsec() as u64)
            .wrapping_mul(6364136223846793005)
            .wrapping_add, (tls_va + crate::config::PAGE_SIZE).into, TrapFrame::new, UserFloatState::new, Vec::with_capacity ...(+39)
- 目标项目：`os/src/syscall/process.rs#sys_mmap`，调用摘要：crate::task::current_task, crate::task::current_task().unwrap, file.as_any, file.as_any().is::<crate::fs::DevZero>, file.as_ref, file.clone ...(+21)
- 目标项目：`os/src/syscall/process.rs#sys_exec`，调用摘要：Some, Vec::new, a.clone, alloc::format!, alloc::string::String::from, alloc::vec! ...(+59)

#### 系统调用
仅目标项目中观察到该模块代表函数，参考项目没有同模块可对齐证据：
- 目标项目：`os/src/net/syscall.rs#sys_setsockopt`，调用摘要：Some, crate::net::with_net_iface, current_user_token, iface.sockets.get_mut, net_ready, optval.is_null ...(+5)
- 目标项目：`os/src/net/syscall.rs#sys_bind`，调用摘要：IpAddress::Ipv4, IpAddress::from, IpEndpoint::new, Ipv4Address::from_bytes, Some, addr.is_null ...(+16)
- 目标项目：`os/src/net/syscall.rs#sys_getsockopt`，调用摘要：crate::net::with_net_iface, crate::net::with_net_iface(|iface| {
                let sock: &TcpSocket = iface.sockets.get(handle);
                !sock.nagle_enabled()
            })
            .unwrap_or, current_user_token, iface.sockets.get, optlen.is_null, optval.is_null ...(+4)
- 目标项目：`os/src/net/syscall.rs#sys_recvfrom`，调用摘要：BlockReason::UdpRecv, addrlen.is_null, block_current_and_run_next, core::mem::size_of::<SockAddrIn>, core::mem::transmute, core::slice::from_raw_parts_mut ...(+15)

#### 文件系统
仅目标项目中观察到该模块代表函数，参考项目没有同模块可对齐证据：
- 目标项目：`os/src/fs/inode.rs#write`，调用摘要：Self::fill_gap_with_zero, Self::now_sec, Self::update_cached_data_time, buf.is_empty, buf.len, buf[total..].as_ptr ...(+24)
- 目标项目：`os/src/fs/inode.rs#write_at`，调用摘要：Self::fill_gap_with_zero, Self::now_sec, Self::update_cached_data_time, Some, buf.is_empty, buf.len ...(+24)
- 目标项目：`os/src/fs/vfs.rs#get_inode_info`，调用摘要：CString::new, CString::new("rb").ok, CString::new(fs_path).ok, Some, c_path2.as_ptr, c_path3.as_ptr ...(+18)
- 目标项目：`os/src/fs/inode.rs#stat_inner`，调用摘要：(unsafe { ext4_fsize(file) as i64 }).max, Kstat::new, Some, cached_times
                    .atime
                    .unwrap_or_else, cached_times
                    .ctime
                    .unwrap_or_else, cached_times
                    .mtime
                    .unwrap_or_else ...(+14)

#### 设备驱动
仅目标项目中观察到该模块代表函数，参考项目没有同模块可对齐证据：
- 目标项目：`os/src/drivers/block/virtio_blk.rs#probe_all`，调用摘要：Arc::new, MmioTransport::new, NonNull::new, UPSafeCell::new, Vec::new, VirtIOBlk::<VirtioHal, MmioTransport>::new ...(+5)
- 目标项目：`os/src/drivers/net/virtio_net.rs#receive`，调用摘要：Some, data.len, data.len().min, frame.len, frame.len().min, inner.net.receive ...(+6)
- 目标项目：`os/src/drivers/net/virtio_net.rs#consume`，调用摘要：f, frame.to_vec, inner.net.new_tx_buffer, inner.net.send, inner.net.send(tx_buf).ok, queue.len ...(+5)
- 目标项目：`os/src/drivers/block/virtio_blk.rs#new_at`，调用摘要：MmioTransport::new, MmioTransport::new(NonNull::new_unchecked(addr.get_mut_ptr()))
                .expect, NonNull::new_unchecked, Self, Self::new_pci, UPSafeCell::new ...(+5)

#### 网络协议栈
仅目标项目中观察到该模块代表函数，参考项目没有同模块可对齐证据：
- 目标项目：`os/src/net/socket.rs#new`，调用摘要：AtomicBool::new, UPSafeCell::new
- 目标项目：`os/src/net/socket.rs#set_flags`，调用摘要：self.nonblock
            .store
- 目标项目：`os/src/net/mod.rs#local_tcp_recv`，调用摘要：内部闭环逻辑
- 目标项目：`os/src/net/mod.rs#with_net_iface`，调用摘要：NET_IFACE.exclusive_access, f, guard.as_mut, guard.as_mut().map

#### 文件系统/第三方库
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `lwext4_rust/c/lwext4/src/ext4_balloc.c#ext4_balloc_alloc_block` | `Kernel/File/lwext4/ext4_balloc.c#ext4_balloc_alloc_block` | 同板块同名同结构 | ext4_balloc_get_bgid_of_block, ext4_balloc_get_block_of_bgid, ext4_balloc_set_bitmap_csum, ext4_balloc_verify_bitmap_csum, ext4_bg_get_block_bitmap, ext4_bg_get_free_blocks_count ...(+19) | EXT4_Debug_u32, ext4_balloc_get_bgid_of_block, ext4_balloc_get_block_of_bgid, ext4_balloc_set_bitmap_csum, ext4_balloc_verify_bitmap_csum, ext4_bg_get_block_bitmap ...(+19) |
| `lwext4_rust/c/lwext4/src/ext4_journal.c#jbd_journal_prepare` | `Kernel/File/lwext4/ext4_journal.c#jbd_journal_prepare` | 同板块同名同结构 | EXT4_BLOCK_ZERO, RB_FIND, TAILQ_FOREACH_REVERSE_SAFE, TAILQ_FOREACH_SAFE, TAILQ_REMOVE, ext4_bcache_clear_dirty ...(+17) | EXT4_BLOCK_ZERO, RB_FIND, TAILQ_FOREACH_REVERSE_SAFE, TAILQ_FOREACH_SAFE, TAILQ_REMOVE, ext4_bcache_clear_dirty ...(+17) |
| `lwext4_rust/c/lwext4/src/ext4.c#ext4_dir_rm` | `Kernel/File/lwext4/ext4.c#ext4_dir_rm` | 同板块同名同结构 | EXT4_MP_LOCK, EXT4_MP_UNLOCK, ext4_block_cache_write_back, ext4_dir_en_get_inode, ext4_dir_iterator_fini, ext4_dir_iterator_init ...(+19) | EXT4_MP_LOCK, EXT4_MP_UNLOCK, ext4_block_cache_write_back, ext4_dir_en_get_inode, ext4_dir_iterator_fini, ext4_dir_iterator_init ...(+19) |
| `lwext4_rust/c/lwext4/src/ext4_dir_idx.c#ext4_dir_dx_split_index` | `Kernel/File/lwext4/ext4_dir_idx.c#ext4_dir_dx_split_index` | 同板块同名同结构 | ext4_block_set, ext4_dir_dx_climit_get_count, ext4_dir_dx_climit_get_limit, ext4_dir_dx_climit_set_count, ext4_dir_dx_climit_set_limit, ext4_dir_dx_entry_get_hash ...(+10) | ext4_block_set, ext4_dir_dx_climit_get_count, ext4_dir_dx_climit_get_limit, ext4_dir_dx_climit_set_count, ext4_dir_dx_climit_set_limit, ext4_dir_dx_entry_get_hash ...(+10) |

#### 网络协议栈/第三方库
仅目标项目中观察到该模块代表函数，参考项目没有同模块可对齐证据：
- 目标项目：`os/src/net/syscall.rs#smoltcp_udp_endpoint`，调用摘要：IpAddress::Ipv4, IpEndpoint::new, Some, crate::net::with_net_iface, crate::net::with_net_iface(|iface| {
        let sock: &UdpSocket = iface.sockets.get(handle);
        let ep = sock.endpoint();
        if ep.port == 0 {
            None
        } else {
            Some(IpEndpoint::new(
                ep.addr.unwrap_or(IpAddress::Ipv4(Ipv4Address::UNSPECIFIED)),
                ep.port,
            ))
        }
    })
    .flatten, ep.addr.unwrap_or ...(+2)

#### 通用模块/其他
仅目标项目中观察到该模块代表函数，参考项目没有同模块可对齐证据：
- 目标项目：`user/src/bin/user_shell.rs#new`，调用摘要：String::new, arg.as_ptr, arg.as_str, args
            .iter, args
            .iter()
            .map, args
            .iter()
            .map(|arg| {
                let mut string = String::new();
                string.push_str(arg);
                string.push('\0');
                string
            })
            .collect ...(+13)
- 目标项目：`user/src/bin/until_timeout.rs#main`，调用摘要：SignalFlags::SIGINT.bits, argv[2]
        .parse::<isize>, argv[2]
        .parse::<isize>()
        .expect, assert_eq!, core::ptr::null::<u8>, exec ...(+5)
- 目标项目：`user/src/bin/ltp_runner.rs#run_test`，调用摘要：case_path, core::ptr::null, exec, fork, get_time, is_elf ...(+7)
- 目标项目：`user/src/bin/iperf_runner.rs#main`，调用摘要：b"-c\0".as_ptr, b"-i\0".as_ptr, b"-l\0".as_ptr, b"-n\0".as_ptr, b"-p\0".as_ptr, b"-s\0".as_ptr ...(+12)


好的，评审庭。我已收到控辩双方的陈述及所有底层证据。现在，我将依据客观的AST指纹数据、模块覆盖矩阵及代码调用流，进行最终的独立审计与定性仲裁。

### 1. 宏观判定
- **定性结论**: **独立开发，存在第三方公共库同源复用**
- **核心依据**:
  - **客观 AST 相似度：24.66%**。该数值远低于通常判定抄袭或严重复用的阈值（>50%）。
  - **覆盖率严重不对等**：参考项目覆盖率高达94.97%，而目标项目覆盖率仅为24.98%。这表明参考项目几乎全部由重合代码构成，而目标项目的大部分代码（75.02%）是参考项目所不具备的。
  - **重合高度集中**：所有共同AST指纹（378个）均集中于【文件系统/第三方库】这一个板块，且均指向 `lwext4` 这一公开的第三方C语言库。目标项目的核心原创模块（进程管理、内存管理、网络协议栈等）与参考项目**零重合**。

### 2. 全模块覆盖矩阵解读

| 模块 | 双方是否都存在 | 共同AST指纹 | 覆盖率解读 | 证据强度 |
| :--- | :--- | :--- | :--- | :--- |
| **进程管理** | 否（仅目标项目） | 0 | 目标项目有263个函数，参考项目为0。目标项目独立实现了进程管理。 | **未形成** |
| **任务调度/异步运行时** | 否（仅目标项目） | 0 | 目标项目有158个函数，参考项目为0。目标项目独立实现了调度与异步运行时。 | **未形成** |
| **内存管理** | 否（仅目标项目） | 0 | 目标项目有185个函数，参考项目为0。目标项目独立实现了内存管理。 | **未形成** |
| **中断/异常处理** | 否（双方均未体现） | 0 | 双方在此模块均无函数被提取，无法对比。 | **未形成** |
| **系统调用** | 否（仅目标项目） | 0 | 目标项目有181个函数，参考项目为0。目标项目独立实现了系统调用层。 | **未形成** |
| **文件系统** | 否（仅目标项目） | 0 | 目标项目有290个自研文件系统函数，参考项目为0。目标项目独立实现了VFS等核心逻辑。 | **未形成** |
| **设备驱动** | 否（仅目标项目） | 0 | 目标项目有23个函数，参考项目为0。目标项目独立实现了VirtIO等驱动。 | **未形成** |
| **网络协议栈** | 否（仅目标项目） | 0 | 目标项目有36个函数，参考项目为0。目标项目独立实现了网络协议栈。 | **未形成** |
| **文件系统/第三方库** | **是** | **378** | 目标项目覆盖率70.52%，参考项目覆盖率94.97%。这是**唯一重合的板块**，且代码均属于 `lwext4` 库。 | **强（但仅限第三方库同源）** |
| **网络协议栈/第三方库** | 否（仅目标项目） | 0 | 目标项目有1个函数，参考项目为0。 | **未形成** |
| **通用模块/其他** | 否（仅目标项目） | 0 | 目标项目有105个函数，参考项目为0。 | **未形成** |

**矩阵解读结论**：除了【文件系统/第三方库】板块因共同引入 `lwext4` 而产生重合外，目标项目在操作系统内核的**所有核心原创模块**上均与参考项目无任何AST指纹重合。这构成了“独立开发”的压倒性证据。

### 3. 底层 AST 关键证据指纹表
> 所有重合证据均属于第三方库，按审计指令后置。

| 所属板块 | 目标项目路径 | 参考项目路径 | 证据类型 | 技术说明 |
| :--- | :--- | :--- | :--- | :--- |
| **第三方库/公共依赖** | `lwext4_rust/c/lwext4/src/ext4_balloc.c#ext4_balloc_alloc_block` | `Kernel/File/lwext4/ext4_balloc.c#ext4_balloc_alloc_block` | 同板块同名同结构 | 两者均为 `lwext4` 库中ext4块分配器的标准实现，调用链完全遵循ext4规范。 |
| **第三方库/公共依赖** | `lwext4_rust/c/lwext4/src/ext4_journal.c#jbd_journal_prepare` | `Kernel/File/lwext4/ext4_journal.c#jbd_journal_prepare` | 同板块底层逻辑一致 | 两者均为 `lwext4` 库中JBD2日志准备事务的标准实现，红黑树与尾队列操作是标准数据结构用法。 |
| **第三方库/公共依赖** | `lwext4_rust/c/lwext4/src/ext4_extent.c#ext4_extent_remove_space` | `Kernel/File/lwext4/ext4_extent.c#ext4_extent_remove_space` | 同板块底层逻辑一致 | 两者均为 `lwext4` 库中ext4 Extent树空间移除的标准算法实现，宏调用顺序由ext4磁盘布局决定。 |
| **第三方库/公共依赖** | `lwext4_rust/c/lwext4/src/ext4_mkfs.c#fill_sb` | `Kernel/File/lwext4/ext4_mkfs.c#fill_sb` | 同板块底层逻辑一致 | 两者均为 `lwext4` 库中创建ext4文件系统时初始化超级块的标准流程，`to_le16/32`调用顺序由超级块结构体定义决定。 |
| **第三方库/公共依赖** | `lwext4_rust/c/lwext4/src/ext4.c#ext4_fwrite` | `Kernel/File/lwext4/ext4.c#ext4_fwrite` | 同板块同名 | 两者均为 `lwext4` 库中的文件写入函数。目标项目使用 `ext4_blocks_get_direct`，参考项目使用 `ext4_block_writebytes`，表明两者可能来自不同版本或分支。 |
| **第三方库/公共依赖** | `lwext4_rust/c/lwext4/src/ext4_journal.c#jbd_iterate_log` | `Kernel/File/lwext4/ext4_journal.c#jbd_iterate_log` | 同板块同名同结构 | 两者均为 `lwext4` 库中的日志迭代函数。目标项目包含 `jbd_verify_commit_csum` 调用，参考项目无，表明两者版本或配置不同。 |

### 4. 核心代码调用流对比剖析
由于双方仅在【第三方库/公共依赖】板块存在重合，不存在可对齐的原创核心模块。以下剖析旨在论证该重合板块内的差异，以消除“定向抄袭”的疑虑。

* **【第三方库/公共依赖 / ext4文件写入路径】**
* **底层技术析理**: 控方指控目标项目对参考项目进行了“宏替换与去调试化”。然而，在 `ext4_fwrite` 这一核心写入函数中，我们观察到更深层的**实现路径差异**。目标项目的调用流为 `ext4_blocks_get_direct` -> `ext4_blocks_set_direct`，这表明其采用了基于块映射的写入方式。而参考项目的调用流为 `ext4_block_writebytes` -> `ext4_blocks_set_direct`，这是一个基于字节写入的路径。这种核心逻辑流的差异，无法用简单的“宏替换”来解释，更可能指向两者集成的 `lwext4` 库版本或配置选项不同。
* **代码溯源链接**:
  - 目标项目: `lwext4_rust/c/lwext4/src/ext4.c#ext4_fwrite`
  - 参考项目: `Kernel/File/lwext4/ext4.c#ext4_fwrite`

* **【第三方库/公共依赖 / ext4日志迭代路径】**
* **底层技术析理**: 在 `jbd_iterate_log` 函数中，目标项目的调用流包含了 `jbd_verify_commit_csum`，这是一个用于验证日志提交校验和的安全/完整性功能。而参考项目的调用流中则没有此调用。这进一步证明，目标项目所使用的 `lwext4` 代码并非从参考项目“复制粘贴”而来，而是引入了一个功能更完整或配置了校验和验证的版本。
* **代码溯源链接**:
  - 目标项目: `lwext4_rust/c/lwext4/src/ext4_journal.c#jbd_iterate_log`
  - 参考项目: `Kernel/File/lwext4/ext4_journal.c#jbd_iterate_log`

### 5. 单方模块与降权项
以下模块仅存在于目标项目，是目标项目独立开发的强有力证据，**不能作为任何同源指控的证据**：
- **进程管理** (263个函数)
- **任务调度/异步运行时** (158个函数)
- **内存管理** (185个函数)
- **系统调用** (181个函数)
- **文件系统** (290个函数，自研VFS部分)
- **设备驱动** (23个函数)
- **网络协议栈** (36个函数)
- **通用模块/其他** (105个函数)

**【第三方库/公共依赖】板块的重合证据必须降权处理**：
所有378个共同AST指纹均源于 `lwext4` 这一公开的第三方库。根据审计指令，第三方库/公共接口的同名同结构不能作为判定项目间抄袭的强证据。两个独立项目完全可以通过各自的依赖管理（如Git子模块、手动源码集成）引入同一上游库，从而产生高度相似的AST指纹。控方未能证明目标项目获取 `lwext4` 的唯一渠道是参考项目。

### 6. 保守结论
本庭裁定，目标项目 `[2026-T202610486999873-XX]` 与参考项目 `[2024-undefinedType-南京航空航天大学]` 之间**不存在抄袭或系统性代码复用关系**。客观AST相似度仅为24.66%，且全部重合点均集中于 `lwext4` 这一公开第三方库。目标项目在进程管理、内存管理、网络协议栈等操作系统核心领域展现了大规模、成体系的独立开发成果，与参考项目形成鲜明架构异构。控方提出的“宏替换混淆”指控，在 `ext4_fwrite` 等函数的核心逻辑差异面前不成立。综上，此次重合属于正常的开源组件复用，不构成学术不端。

------------------------------

