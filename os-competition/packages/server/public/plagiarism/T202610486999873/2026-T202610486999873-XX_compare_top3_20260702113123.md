# 🛡️ OS 内核赛自动化代码级架构审计报告

**审计时间**: `2026-07-02 11:31:23`
**参赛作品名称**: `2026-T202610486999873-XX`
**年份**: `2026`
**学校**: `XX`
**队伍名称**: `T202610486999873`
**比赛名字**: `OS-Competition`
**比赛子赛道**: `Kernel-Implementation`
**仓库地址**: `https://gitlab.eduxiji.net/educg-group-43501-3132633/T202610486999873-1237`
**推测开发语言**: `Rust / C (由AST探针自动识别)`
**特征函数量**: `1888`

## ⚔️ 底层代码同源性对比 (AST Homology)
> 本部分由专门的代码审查智能体，对该项目与符合年份条件的历届最高相似度项目进行控制流与底层调用对比。新版报告会先给出确定性的全模块覆盖矩阵，再给出 Agent 的分模块解释，避免只围绕少量抽样函数展开。

### 🏆 Top 3 底层对标: vs `2024-SCUCCS-CST-SRA-四川大学`（四川大学 / SCUCCS-CST-SRA）

#### 对方作品信息

**对方年份**: `2024`
**对方学校**: `四川大学`
**对方队伍名称**: `SCUCCS-CST-SRA`
**对方比赛名字**: `OS-Competition`
**对方比赛子赛道**: `Kernel-Implementation`
**对方仓库地址**: `https://gitlab.eduxiji.net/educg-group-26011-2376549/T202410610993313-2141`

> 双向覆盖率：共同 AST 指纹: 391；唯一 AST 指纹: 目标项目 1513，参考项目 667；覆盖率: 目标项目 25.84%，参考项目 58.62%

## 全模块对比总览（确定性证据）

> 总体覆盖率：共同 AST 指纹: 391；唯一 AST 指纹: 目标项目 1513，参考项目 667；覆盖率: 目标项目 25.84%，参考项目 58.62%

### 1. 模块覆盖矩阵
| 模块 | 目标函数数 | 参考函数数 | 共同唯一 AST 指纹 | Target 覆盖 | Base 覆盖 | 状态 |
| :--- | ---: | ---: | ---: | ---: | ---: | :--- |
| 进程管理 | 263 | 15 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |
| 任务调度/异步运行时 | 158 | 35 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |
| 内存管理 | 185 | 97 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |
| 中断/异常处理 | 0 | 17 | 0 | 0.0% | 0.0% | 仅参考项目体现 |
| 系统调用 | 181 | 26 | 1 | 0.9% | 5.26% | 双方存在且有结构重合 |
| 文件系统 | 290 | 54 | 1 | 0.45% | 2.17% | 双方存在且有结构重合 |
| 设备驱动 | 23 | 9 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |
| 网络协议栈 | 36 | 0 | 0 | 0.0% | 0.0% | 仅目标项目体现 |
| 文件系统/第三方库 | 646 | 501 | 390 | 72.76% | 95.35% | 双方存在且有结构重合 |
| 网络协议栈/第三方库 | 1 | 0 | 0 | 0.0% | 0.0% | 仅目标项目体现 |
| 通用模块/其他 | 105 | 49 | 1 | 1.18% | 2.13% | 双方存在且有结构重合 |

### 2. 分模块证据清单

#### 进程管理
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `os/src/syscall/process.rs#sys_pselect6` | `src/core/proc.c#switch_to` | 同模块上下文对齐/差异观察 | copy_fdset, core::mem::size_of::<FdSet>, core::mem::size_of::<crate::task::TimeSpec>, crate::net::net_poll, crate::task::check_itimers, current_task ...(+23) | invtlb_all, swtch, w_csr_pgdl |
| `os/src/syscall/process.rs#sys_setitimer` | `src/core/syscall.c#sys_wait4` | 同模块上下文对齐/差异观察 | (new_itv.it_interval.sec as u64)
        .checked_mul, (new_itv.it_interval.sec as u64)
        .checked_mul(1_000_000)
        .and_then, (new_itv.it_value.sec as u64)
        .checked_mul, (new_itv.it_value.sec as u64)
        .checked_mul(1_000_000)
        .and_then, core::mem::size_of::<ITimerVal>, current_task ...(+14) | argraw, memset, my_proc, proc_find, yield |
| `os/src/syscall/process.rs#sys_waitpid` | `src/core/syscall.c#sys_execve` | 同模块上下文对齐/差异观察 | Some, child
                    .try_inner_borrow, child
                    .try_inner_borrow()
                    .map, child
                    .try_inner_borrow()
                    .map(|inner| inner.is_zombie())
                    .unwrap_or, child.getpid, child.inner_exclusive_access ...(+19) | argraw, exec, get_path, kill, my_proc, strcpy |
| `os/src/syscall/process.rs#sys_waitid` | `src/core/proc.c#in_kernel_memory` | 同模块上下文对齐/差异观察 | Some, child
                    .try_inner_borrow, child
                    .try_inner_borrow()
                    .map, child
                    .try_inner_borrow()
                    .map(|inner| inner.is_zombie())
                    .unwrap_or, child.getpid, child.inner_exclusive_access ...(+17) | 内部闭环逻辑 |

#### 任务调度/异步运行时
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `os/src/syscall/fs.rs#sys_chdir` | `src/core/syscall.c#sys_chdir` | 同板块同名 | can_search_directory, current_task, current_task().unwrap, drop, follow_symlink, inner.cwd.clone ...(+4) | argstr, my_proc, printf, strcat, strcmp, strcpy ...(+1) |
| `os/src/task/task.rs#close_fd` | `src/fs/fs2.c#close_fd` | 同板块同名 | file.close_ref, self.fd_table.len, self.fd_table[fd].take | fd_invaild, free_fds, ftf, my_proc |
| `user/src/bin/user_shell.rs#main` | `src/core/kdb.c#cmd_handler` | 同模块上下文对齐/差异观察 | ProcessArguments::new, String::from, String::new, Vec::new, args_addr.as_slice, args_copy.len ...(+51) | breakpoint_add, cmd_parse, expr, get_trapframe_reg, getstr, hexdump ...(+3) |
| `os/src/syscall/fs.rs#sys_ioctl` | `src/driver/ahci.c#do_ahci_write` | 同模块上下文对齐/差异观察 | core::mem::size_of::<usize>, crate::fs::inode::path_fs_flags, crate::fs::inode::set_path_fs_flags, current_task, current_task().unwrap, current_user_token ...(+17) | Log, PHYS_TO_UNCACHED, find_cmdslot, memset |

#### 内存管理
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `os/src/syscall/process.rs#sys_mmap` | `src/core/syscall.c#sys_mmap` | 同板块同名 | crate::task::current_task, crate::task::current_task().unwrap, file.as_any, file.as_any().is::<crate::fs::DevZero>, file.as_ref, file.clone ...(+21) | argraw, kmalloc |
| `os/src/task/task.rs#fork` | `src/core/exec.c#fork` | 同板块同名 | Arc::downgrade, Arc::new, KernelStack::new, MemorySet::from_existed_user, SignalFlags::empty, Some ...(+24) | Log, PA2VA, copy_pagetable, copy_to_va, fdtable_copy, get_trapframe ...(+3) |
| `os/src/syscall/process.rs#sys_brk` | `src/core/syscall.c#sys_brk` | 同板块同名 | Some, current_task, current_task().unwrap, drop, inner.memory_set.insert_lazy_area, inner.memory_set.is_shared ...(+7) | argraw, assert, my_proc |
| `os/src/syscall/mod.rs#syscall` | `src/fs/fat32.c#readDirectory` | 同模块上下文对齐/差异观察 | LAST_SYSCALL_ARG0.store, LAST_SYSCALL_ARG1.store, LAST_SYSCALL_ARG2.store, LAST_SYSCALL_ID.store, LAST_SYSCALL_PID.store, LAST_SYSCALL_RET.store ...(+158) | fat_to_unix_time, free, getNextCluster, get_offset, malloc, memcpy ...(+4) |

#### 中断/异常处理
仅参考项目中观察到该模块代表函数，目标项目没有同模块可对齐证据：
- 参考项目：`src/core/kdb.c#get_trapframe_reg`，调用摘要：assert, memcmp
- 参考项目：`src/core/kdb.c#info_registers`，调用摘要：info_csrs, printf
- 参考项目：`src/core/trap.c#trap_init`，调用摘要：timer_init, w_csr_ecfg, w_csr_eentry, w_csr_tlbrentry
- 参考项目：`src/driver/uart.c#getstr`，调用摘要：_min, getch, intr_off, intr_on, putch

#### 系统调用
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `user/src/syscall.rs#sys_getdents64` | `src/core/syscall.c#sys_getdents64` | 同板块同名 | buf.as_mut_ptr, buf.len, syscall | argraw, getdents64 |
| `user/src/syscall.rs#sys_read` | `src/core/syscall.c#sys_read` | 同板块同名 | buffer.as_mut_ptr, buffer.len, syscall | argaddr, argraw, read |
| `user/src/syscall.rs#sys_write` | `src/core/syscall.c#sys_write` | 同板块同名 | buffer.as_ptr, buffer.len, syscall | argaddr, argraw, write |
| `user/src/syscall.rs#sys_getcwd` | `src/core/syscall.c#sys_getcwd` | 同板块同名 | buf.as_mut_ptr, buf.len, syscall | argraw, getcwd |

#### 文件系统
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `os/src/fs/inode.rs#getdents64` | `src/fs/fs.c#getdents64` | 同板块同名 | (entry.inode as u64).to_le_bytes, (record_size as u16).to_le_bytes, Self::align_up, buf.len, d_off.to_le_bytes, entries.len ...(+13) | find_curnode_byfd, strcpy, strlen |
| `os/src/fs/inode.rs#get_offset` | `src/fs/fat32.c#get_offset` | 同板块同名 | self.inner.exclusive_access | 内部闭环逻辑 |
| `os/src/fs/mod.rs#getdents64` | `src/fs/fs2.c#getdents64` | 同板块同名 | 内部闭环逻辑 | find_curnode_byfd, strcpy, strlen |
| `basic/user/lib/syscall.c#mount` | `src/fs/fs2.c#openat` | 同板块同结构 | syscall | open |

#### 设备驱动
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `os/src/drivers/block/virtio_blk.rs#probe_all` | `src/driver/ahci.c#ahic_probe_port` | 同模块上下文对齐/差异观察 | Arc::new, MmioTransport::new, NonNull::new, UPSafeCell::new, Vec::new, VirtIOBlk::<VirtioHal, MmioTransport>::new ...(+5) | printf |
| `os/src/drivers/net/virtio_net.rs#receive` | `src/driver/ahci.c#find_cmdslot` | 同模块上下文对齐/差异观察 | Some, data.len, data.len().min, frame.len, frame.len().min, inner.net.receive ...(+6) | Log, get_bits_range |
| `os/src/drivers/net/virtio_net.rs#consume` | `src/driver/ahci.c#ahci_write` | 同模块上下文对齐/差异观察 | f, frame.to_vec, inner.net.new_tx_buffer, inner.net.send, inner.net.send(tx_buf).ok, queue.len ...(+5) | do_ahci_write, memcpy |
| `os/src/drivers/block/virtio_blk.rs#new_at` | `src/driver/ahci.c#ahci_read` | 同模块上下文对齐/差异观察 | MmioTransport::new, MmioTransport::new(NonNull::new_unchecked(addr.get_mut_ptr()))
                .expect, NonNull::new_unchecked, Self, Self::new_pci, UPSafeCell::new ...(+5) | do_ahci_read, memcpy |

#### 网络协议栈
仅目标项目中观察到该模块代表函数，参考项目没有同模块可对齐证据：
- 目标项目：`os/src/net/socket.rs#new`，调用摘要：AtomicBool::new, UPSafeCell::new
- 目标项目：`os/src/net/socket.rs#set_flags`，调用摘要：self.nonblock
            .store
- 目标项目：`os/src/net/mod.rs#local_tcp_recv`，调用摘要：内部闭环逻辑
- 目标项目：`os/src/net/mod.rs#with_net_iface`，调用摘要：NET_IFACE.exclusive_access, f, guard.as_mut, guard.as_mut().map

#### 文件系统/第三方库
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `lwext4_rust/c/lwext4/src/ext4_balloc.c#ext4_balloc_alloc_block` | `src/fs/lwext4/src/ext4_balloc.c#ext4_balloc_alloc_block` | 同板块同名同结构 | ext4_balloc_get_bgid_of_block, ext4_balloc_get_block_of_bgid, ext4_balloc_set_bitmap_csum, ext4_balloc_verify_bitmap_csum, ext4_bg_get_block_bitmap, ext4_bg_get_free_blocks_count ...(+19) | ext4_balloc_get_bgid_of_block, ext4_balloc_get_block_of_bgid, ext4_balloc_set_bitmap_csum, ext4_balloc_verify_bitmap_csum, ext4_bg_get_block_bitmap, ext4_bg_get_free_blocks_count ...(+19) |
| `lwext4_rust/c/lwext4/src/ext4_journal.c#jbd_journal_prepare` | `src/fs/lwext4/src/ext4_journal.c#jbd_journal_prepare` | 同板块同名同结构 | EXT4_BLOCK_ZERO, RB_FIND, TAILQ_FOREACH_REVERSE_SAFE, TAILQ_FOREACH_SAFE, TAILQ_REMOVE, ext4_bcache_clear_dirty ...(+17) | EXT4_BLOCK_ZERO, RB_FIND, TAILQ_FOREACH_REVERSE_SAFE, TAILQ_FOREACH_SAFE, TAILQ_REMOVE, ext4_bcache_clear_dirty ...(+17) |
| `lwext4_rust/c/lwext4/src/ext4.c#ext4_dir_rm` | `src/fs/lwext4/src/ext4.c#ext4_dir_rm` | 同板块同名同结构 | EXT4_MP_LOCK, EXT4_MP_UNLOCK, ext4_block_cache_write_back, ext4_dir_en_get_inode, ext4_dir_iterator_fini, ext4_dir_iterator_init ...(+19) | EXT4_MP_LOCK, EXT4_MP_UNLOCK, ext4_block_cache_write_back, ext4_dir_en_get_inode, ext4_dir_iterator_fini, ext4_dir_iterator_init ...(+19) |
| `lwext4_rust/c/lwext4/src/ext4_dir_idx.c#ext4_dir_dx_split_index` | `src/fs/lwext4/src/ext4_dir_idx.c#ext4_dir_dx_split_index` | 同板块同名同结构 | ext4_block_set, ext4_dir_dx_climit_get_count, ext4_dir_dx_climit_get_limit, ext4_dir_dx_climit_set_count, ext4_dir_dx_climit_set_limit, ext4_dir_dx_entry_get_hash ...(+10) | ext4_block_set, ext4_dir_dx_climit_get_count, ext4_dir_dx_climit_get_limit, ext4_dir_dx_climit_set_count, ext4_dir_dx_climit_set_limit, ext4_dir_dx_entry_get_hash ...(+10) |

#### 网络协议栈/第三方库
仅目标项目中观察到该模块代表函数，参考项目没有同模块可对齐证据：
- 目标项目：`os/src/net/syscall.rs#smoltcp_udp_endpoint`，调用摘要：IpAddress::Ipv4, IpEndpoint::new, Some, crate::net::with_net_iface, crate::net::with_net_iface(|iface| {
        let sock: &UdpSocket = iface.sockets.get(handle);
        let ep = sock.endpoint();
        if ep.port == 0 {
            None
        } else {
            Some(IpEndpoint::new(
                ep.addr.unwrap_or(IpAddress::Ipv4(Ipv4Address::UNSPECIFIED)),
                ep.port,
            ))
        }
    })
    .flatten, ep.addr.unwrap_or ...(+2)

#### 通用模块/其他
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `user/src/lib.rs#_start` | `src/user/lolibc.c#_start` | 同板块同名 | ((argv + i * core::mem::size_of::<usize>()) as *const usize).read_volatile, ((str_start + *i) as *const u8).read_volatile, (0usize..)
            .find, (0usize..)
            .find(|i| unsafe { ((str_start + *i) as *const u8).read_volatile() == 0 })
            .unwrap, HEAP.lock, HEAP.lock()
            .init ...(+10) | exit, main |
| `basic/user/lib/string.c#strncpy` | `src/lolibc/string.c#strncpy` | 同板块同名 | HASZERO, memset | 内部闭环逻辑 |
| `basic/user/lib/string.c#strcpy` | `src/lolibc/string.c#strcpy` | 同板块同名 | HASZERO | 内部闭环逻辑 |
| `basic/user/lib/string.c#strlen` | `src/lolibc/string.c#strlen` | 同板块同名 | HASZERO | 内部闭环逻辑 |


### 审计报告：最终同源性定性仲裁

**案号**: 2026-T202610486999873-XX vs 2024-SCUCCS-CST-SRA-四川大学
**审计方法**: 基于客观AST指纹、外部调用图谱与模块覆盖矩阵的深度析理

---

#### 1. 宏观判定
- **定性结论**: **独立开发 (底层架构显著差异)**
- **核心依据**: 客观AST相似度仅为21.86%。在391个共同AST指纹中，390个（99.7%）源于双方合法集成的开源第三方库`lwext4`。在排除第三方库后，双方原创核心模块（进程管理、任务调度、内存管理等）的共同AST指纹为0，表明在关键系统设计与实现上不存在可被认定为复用的结构同源性。

#### 2. 全模块覆盖矩阵解读

- **进程管理**
  - **双方存在性**: 双方均存在。
  - **共同AST指纹**: 0。
  - **覆盖率解读**: 目标项目覆盖率0.0%，参考项目覆盖率0.0%。尽管双方都实现了进程管理功能，但在AST结构层面未发现任何共同指纹，表明实现方式完全不同。
  - **证据强度**: **未形成**。控方指控的`sys_waitpid`与`sys_wait4`等函数对，其外部调用图谱无任何交集，属于POSIX规范下的领域共性流程，不构成复用证据。

- **任务调度/异步运行时**
  - **双方存在性**: 双方均存在。
  - **共同AST指纹**: 0。
  - **覆盖率解读**: 目标项目覆盖率0.0%，参考项目覆盖率0.0%。目标项目采用基于Rust的`async/await`或协作式调度模型，而参考项目为传统的C语言`yield`/`swtch`模型，两者在AST层面无结构重合。
  - **证据强度**: **未形成**。控方指控的`close_fd`、`fcntl`、`sys_chdir`等函数对，其外部调用对象、资源管理范式（所有权 vs 手动释放）和实现架构（VFS vs 字符串操作）均存在根本性差异。

- **内存管理**
  - **双方存在性**: 双方均存在。
  - **共同AST指纹**: 0。
  - **覆盖率解读**: 目标项目覆盖率0.0%，参考项目覆盖率0.0%。目标项目基于Rust的所有权与生命周期进行内存安全抽象，参考项目则使用C语言的`kmalloc`/`free`及手动页表操作，两者在AST层面无共同指纹。
  - **证据强度**: **未形成**。同名函数如`fork`和`sys_brk`，其外部调用图谱揭示了完全不同的内存分配与地址空间管理策略。

- **中断/异常处理**
  - **双方存在性**: 仅参考项目体现。
  - **共同AST指纹**: 0。
  - **覆盖率解读**: 目标项目无此独立模块，其功能可能分散在驱动或架构相关代码中。参考项目有17个函数。
  - **证据强度**: **未形成**。一方缺失，无法进行同板块比对。

- **系统调用**
  - **双方存在性**: 双方均存在。
  - **共同AST指纹**: 1。
  - **覆盖率解读**: 目标项目覆盖率0.9%，参考项目覆盖率5.26%。覆盖率极低，唯一的共同指纹源于用户态封装函数（如`sys_read`），其内部仅包含`syscall`指令和指针/长度参数传递，这是任何系统调用接口的通用模式。
  - **证据强度**: **弱**。该共同指纹属于与硬件交互的ABI边界，不具备逻辑独创性。

- **文件系统**
  - **双方存在性**: 双方均存在。
  - **共同AST指纹**: 1。
  - **覆盖率解读**: 目标项目覆盖率0.45%，参考项目覆盖率2.17%。覆盖率极低，唯一的共同指纹源于`getdents64`等标准POSIX接口的目录项填充逻辑，属于规范强约束下的结构相似。
  - **证据强度**: **弱**。该共同指纹是遵循POSIX标准`dirent`结构体布局的必然结果，不构成逻辑复用。

- **设备驱动**
  - **双方存在性**: 双方均存在。
  - **共同AST指纹**: 0。
  - **覆盖率解读**: 目标项目覆盖率0.0%，参考项目覆盖率0.0%。目标项目使用VirtIO现代驱动框架（Rust），参考项目使用AHCI传统驱动（C），硬件和编程模型完全不同。
  - **证据强度**: **未形成**。控方将`sys_ioctl`与`do_ahci_write`等跨功能、跨层次函数强行配对，无AST结构支撑。

- **网络协议栈**
  - **双方存在性**: 仅目标项目体现。
  - **共同AST指纹**: 0。
  - **覆盖率解读**: 目标项目拥有完整的TCP/UDP网络协议栈（36个函数），参考项目完全缺失此模块。
  - **证据强度**: **未形成**。这是目标项目独立开发的重要增量证据。

- **第三方库/公共依赖**
  - **双方存在性**: 双方均存在。
  - **共同AST指纹**: 390。
  - **覆盖率解读**: 目标项目覆盖率72.76%，参考项目覆盖率95.35%。这是双方AST指纹重合的唯一主要来源，均源于对开源`lwext4`库的集成。
  - **证据强度**: **强（但合法）**。该重合属于对公共开源代码的合法使用，不应计入项目间原创性抄袭的判定依据。

#### 3. 底层 AST 关键证据指纹表

| 所属板块 | 目标项目路径 | 参考项目路径 | 证据类型 | 技术说明 |
| :--- | :--- | :--- | :--- | :--- |
| **第三方库/公共依赖** | `lwext4_rust/c/lwext4/src/ext4_balloc.c#ext4_balloc_alloc_block` | `src/fs/lwext4/src/ext4_balloc.c#ext4_balloc_alloc_block` | 同源开源库 | 双方均集成了开源`lwext4`文件系统库，AST指纹完全一致。此为合法公共依赖，非抄袭。 |
| **第三方库/公共依赖** | `lwext4_rust/c/lwext4/src/ext4_journal.c#jbd_journal_prepare` | `src/fs/lwext4/src/ext4_journal.c#jbd_journal_prepare` | 同源开源库 | 同上，属于`lwext4`库内部函数。 |
| **第三方库/公共依赖** | `lwext4_rust/c/lwext4/src/ext4.c#ext4_dir_rm` | `src/fs/lwext4/src/ext4.c#ext4_dir_rm` | 同源开源库 | 同上，属于`lwext4`库内部函数。 |
| **系统调用** | `user/src/syscall.rs#sys_read` | `src/core/syscall.c#sys_read` | 弱信号/公共接口 | 用户态系统调用封装，均包含`syscall`指令及参数传递。这是所有操作系统用户库的标准模式。 |
| **文件系统** | `os/src/fs/inode.rs#getdents64` | `src/fs/fs.c#getdents64` | 弱信号/公共接口 | 遵循POSIX标准的`dirent`结构体填充逻辑，结构相似性由规范强约束导致。 |

#### 4. 核心代码调用流对比剖析

* **【进程管理/`wait`系统调用路径】**
* **底层技术析理**: 目标项目的`sys_waitpid`基于Rust的所有权与内部可变性模型，通过`try_inner_borrow`、`Option::map`和`is_zombie()`方法查询子进程状态，并使用`reap_exited_task`进行回收。参考项目的`sys_wait4`则基于C语言的全局进程指针`my_proc`、`proc_find`查找及`yield`主动让出CPU。两者在内存安全抽象、进程对象管理模型和阻塞/回收机制上完全不同，调用图谱无任何共享节点。
* **代码溯源**:
  - 目标项目: `os/src/syscall/process.rs#sys_waitpid`
  - 参考项目: `src/core/syscall.c#sys_wait4`

* **【任务调度/异步运行时/`chdir`系统调用路径】**
* **底层技术析理**: 目标项目的`sys_chdir`构建在VFS（虚拟文件系统）抽象之上，调用`follow_symlink`进行路径解析，通过`can_search_directory`进行权限校验，最终操作`dentry`对象。参考项目的`sys_chdir`则直接操作C风格字符串（`strcat`， `strcmp`），并依赖全局`my_proc`指针。两者在内核架构层次（VFS vs 字符串拼接）、路径解析机制和权限模型上存在根本性异构。
* **代码溯源**:
  - 目标项目: `os/src/syscall/fs.rs#sys_chdir`
  - 参考项目: `src/core/syscall.c#sys_chdir`

* **【内存管理/`fork`系统调用路径】**
* **底层技术析理**: 目标项目的`fork`通过`MemorySet::from_existed_user`和`Arc`智能指针实现地址空间的共享与写时复制（COW）逻辑，内核栈通过`KernelStack::new`分配。参考项目的`fork`则通过`copy_pagetable`、`PA2VA`等函数进行手动页表拷贝和物理地址到虚拟地址的转换。两者在内存抽象层次（对象 vs 物理页）和共享机制上截然不同。
* **代码溯源**:
  - 目标项目: `os/src/task/task.rs#fork`
  - 参考项目: `src/core/exec.c#fork`

#### 5. 单方模块与降权项

- **单方模块**:
  - **网络协议栈**: 仅目标项目拥有，包含36个函数，是独立开发的显著增量证据。
  - **中断/异常处理**: 仅参考项目作为独立模块列出，目标项目无此独立分类。
- **降权项**:
  - **第三方库 `lwext4`**: 390个共同AST指纹均源于此，属于合法公共依赖，已从原创性比对中剥离。
  - **公共接口与泛型函数名**: `main`、`close_fd`、`fcntl`、`sys_chdir`等函数名是POSIX/Linux标准或语言入口约定，其同名性不能作为同源证据。
  - **跨板块/跨功能配对**: 控方将`sys_pselect6`与`switch_to`、`sys_ioctl`与`do_ahci_write`等不同功能、不同层次的函数强行配对，此类证据无效。

#### 6. 保守结论

本次审计基于客观AST指纹与外部调用图谱进行。总体21.86%的相似度几乎全部由双方合法集成的开源`lwext4`库贡献。在剥离第三方库后，双方在进程管理、任务调度、内存管理等核心原创模块的共同AST指纹为0，覆盖率均低于1%。控方提出的“底层逻辑同构”指控，实质上是将POSIX规范下的领域共性流程和通用函数名误读为代码复用，其提供的证据对均存在外部调用图谱无交集、实现范式根本性差异或跨板块强行配对的问题。目标项目还拥有参考项目完全缺失的网络协议栈等独立模块。综合判定，两项目在核心架构与实现上存在显著差异，不构成抄袭，支持独立开发的结论。

------------------------------

