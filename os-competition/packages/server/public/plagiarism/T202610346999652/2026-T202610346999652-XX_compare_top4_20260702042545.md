# 🛡️ OS 内核赛自动化代码级架构审计报告

**审计时间**: `2026-07-02 04:25:45`
**参赛作品名称**: `2026-T202610346999652-XX`
**年份**: `2026`
**学校**: `XX`
**队伍名称**: `T202610346999652`
**比赛名字**: `OS-Competition`
**比赛子赛道**: `Kernel-Implementation`
**仓库地址**: `https://gitlab.eduxiji.net/educg-group-43501-3132633/T202610346999652-1129`
**推测开发语言**: `Rust / C (由AST探针自动识别)`
**特征函数量**: `9083`

## ⚔️ 底层代码同源性对比 (AST Homology)
> 本部分由专门的代码审查智能体，对该项目与符合年份条件的历届最高相似度项目进行控制流与底层调用对比。新版报告会先给出确定性的全模块覆盖矩阵，再给出 Agent 的分模块解释，避免只围绕少量抽样函数展开。

### 🏆 Top 4 底层对标: vs `2025-undefined-清华大学`（清华大学 / undefined）

#### 对方作品信息

**对方年份**: `2025`
**对方学校**: `清华大学`
**对方队伍名称**: `undefined`
**对方比赛名字**: `OS-Competition`
**对方比赛子赛道**: `Kernel-Implementation`
**对方仓库地址**: `https://gitlab.eduxiji.net/educg-group-36002-2710490/T202510003995291-2331`

> 双向覆盖率：共同 AST 指纹: 864；唯一 AST 指纹: 目标项目 7428，参考项目 2820；覆盖率: 目标项目 11.63%，参考项目 30.64%

## 全模块对比总览（确定性证据）

> 总体覆盖率：共同 AST 指纹: 864；唯一 AST 指纹: 目标项目 7428，参考项目 2820；覆盖率: 目标项目 11.63%，参考项目 30.64%

### 1. 模块覆盖矩阵
| 模块 | 目标函数数 | 参考函数数 | 共同唯一 AST 指纹 | Target 覆盖 | Base 覆盖 | 状态 |
| :--- | ---: | ---: | ---: | ---: | ---: | :--- |
| 进程管理 | 430 | 135 | 4 | 1.12% | 3.31% | 双方存在且有结构重合 |
| 任务调度/异步运行时 | 682 | 415 | 3 | 0.48% | 1.0% | 双方存在且有结构重合 |
| 内存管理 | 422 | 450 | 8 | 2.0% | 2.09% | 双方存在且有结构重合 |
| 中断/异常处理 | 5 | 53 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |
| 系统调用 | 308 | 109 | 1 | 0.39% | 1.16% | 双方存在且有结构重合 |
| 文件系统 | 1347 | 999 | 42 | 3.56% | 6.29% | 双方存在且有结构重合 |
| 设备驱动 | 1 | 133 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |
| 网络协议栈 | 0 | 65 | 0 | 0.0% | 0.0% | 仅参考项目体现 |
| 文件系统/第三方库 | 735 | 643 | 509 | 81.31% | 94.09% | 双方存在且有结构重合 |
| 网络协议栈/第三方库 | 2617 | 130 | 4 | 0.2% | 3.45% | 双方存在且有结构重合 |
| 第三方库 | 2322 | 0 | 0 | 0.0% | 0.0% | 仅目标项目体现 |
| 通用模块/其他 | 214 | 555 | 1 | 0.57% | 0.23% | 双方存在且有结构重合 |

### 2. 分模块证据清单

#### 进程管理
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `kernel/src/syscall/signal.rs#sys_rt_sigprocmask` | `api/src/imp/task/signal.rs#sys_rt_sigprocmask` | 同板块同名 | Err, Ok, check_sigset_size, curr.as_thread, current, debug! ...(+7) | Err, Ok, check_sigset_size, current_thread_data, current_thread_data()
        .signal
        .with_blocked_mut::<LinuxResult<_>>, oldset.nullable ...(+1) |
| `kernel/src/task/ops.rs#get_process_group` | `process/src/process_group.rs#get_process_group` | 同板块同名 | (group.pgid() == pgid).then_some, Ok, PROCESS_GROUP_TABLE.read, PROCESS_GROUP_TABLE.read().get, group.pgid, proc_data.proc.group ...(+5) | PROCESS_GROUP_TABLE.lock, process_group_table.get, process_group_table.get(&pgid).cloned |
| `kernel/src/syscall/task/execve.rs#sys_execve` | `api/src/interface/task/execve.rs#sys_execve` | 同板块同名 | FS_CONTEXT.lock, FS_CONTEXT.lock().resolve, debug!, do_execve, load_exec_args_env, vm_load_string | BUSYBOX.to_string, Some, abs_path.to_string, args[1..].iter, args[1..].iter().cloned, argv.clone ...(+10) |
| `kernel/src/syscall/task/wait.rs#apply` | `api/src/imp/task/wait.rs#apply` | 同板块同名 | child.group, child.group().pgid, child.pid | child.get_group, child.get_group().get_pgid, child.get_pid |

#### 任务调度/异步运行时
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `kernel/src/syscall/fs/fd_ops.rs#sys_fcntl` | `api/src/imp/fs/fd_ops.rs#sys_fcntl` | 同板块同名 | AsyncIoOwner::Pgrp, AsyncIoOwner::Pid, AsyncIoOwner::Tid, Err, FD_TABLE
                .read, FD_TABLE
                .read()
                .get ...(+65) | Err, FdFlags::empty, FdFlags::from_bits_truncate, Ok, current_process_data, current_process_data()
                .resource_limits
                .lock ...(+13) |
| `kernel/src/syscall/signal.rs#sys_kill` | `api/src/imp/task/signal.rs#sys_kill` | 同板块同名 | Err, Ok, Some, check_signal_permission, current, current().as_thread ...(+12) | Err, Ok, current_process, current_process().get_group, debug!, get_all_processes ...(+8) |
| `kernel/src/task/signal.rs#check_signals` | `api/src/imp/task/signal.rs#check_signals` | 同板块同名 | acknowledge_posix_timer_signal, delivered.info.signo, do_continue, do_exit, do_stop, super::coredump::generate_core_dump ...(+3) | current_thread_data, debug!, sig.signo, signal.check_signals, sys_exit_impl |
| `kernel/src/syscall/task/clone.rs#sys_clone` | `api/src/interface/task/clone.rs#sys_clone` | 同板块同名 | CloneFlags::from_bits_truncate, Err, args.do_clone, clone_flags.contains | addr_child_tid.clone, addr_parent_tid.clone, sys_clone_ |

#### 内存管理
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `kernel/src/syscall/mod.rs#handle_syscall` | `src/syscall.rs#handle_syscall` | 同板块同名 | Err, LinuxError::ENOSYS.code, LinuxError::from, LinuxError::from(err).code, Sysno::new, bpf::sys_bpf ...(+342) | Err, Sysno::new, err.code, info!, result.unwrap_or_else, set_trap_frame ...(+151) |
| `kernel/src/syscall/mm/mmap.rs#sys_mmap` | `api/src/imp/mm/mmap.rs#sys_mmap` | 同板块同名 | (addr + length).align_up, Arc::new, Backend::new_alloc, Backend::new_cow, Backend::new_file, Backend::new_linear ...(+78) | Err, File::from_fd, MmapFlags::from_bits_truncate, MmapProt::from_bits_truncate, Ok, PhysAddr::from ...(+34) |
| `kernel/src/syscall/ipc/shm.rs#sys_shmctl` | `api/src/interface/mm/shm.rs#sys_shmctl` | 同板块同名 | AxError::from, Err, Ok, SHM_MANAGER.lock, SHM_MANAGER.lock().remove_shmid, admin_ipc_permission ...(+31) | Err, Ok, SHARED_MEMORY_MANAGER.delete, SHARED_MEMORY_MANAGER.get, SHARED_MEMORY_MANAGER.get(key).ok_or |
| `kernel/src/mm/loader.rs#load_user_app` | `core/src/mm.rs#load_user_app` | 同板块同名 | (ustack_top - user_sp_aligned).align_up_4k, Backend::new_alloc, ELF_LOADER.lock, ELF_LOADER.lock().load, Err, Ok ...(+41) | CStr::from_bytes_with_nul, CStr::from_bytes_with_nul(interp)
            .map_err, CStr::from_bytes_with_nul(interp)
            .map_err(|_| AxError::InvalidData)?
            .to_str, CStr::from_bytes_with_nul(interp)
            .map_err(|_| AxError::InvalidData)?
            .to_str()
            .map_err, CStr::from_bytes_with_nul(interp)
            .map_err(|_| AxError::InvalidData)?
            .to_str()
            .map_err(|_| AxError::InvalidData)?
            .to_string, ElfFile::new ...(+34) |

#### 中断/异常处理
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `scripts/support-tools/oscomp-timeout.c#main` | `arceos/ulib/axlibc/c/pow.c#exp_inline` | 同模块上下文对齐/差异观察 | WEXITSTATUS, WIFEXITED, WIFSIGNALED, WTERMSIG, _exit, execvp ...(+9) | __math_oflow, __math_uflow, asdouble, asuint64, converttoint, eval_as_double ...(+4) |
| `kernel/src/pseudofs/dev/tty/terminal/termios.rs#default` | `arceos/modules/axhal/src/arch/aarch64/trap.rs#handle_sync_exception` | 同模块上下文对齐/差异观察 | ctl | ESR_EL1.extract, crate::trap::handle_syscall, crate::trap::post_trap_callback, debug!, esr.read, esr.read_as_enum ...(+6) |
| `scripts/support-tools/sleep.c#main` | `arceos/modules/axhal/src/arch/x86_64/idt.rs#new` | 同模块上下文对齐/差异观察 | fprintf, parse_seconds, perror, syscall | InterruptDescriptorTable::new, core::mem::transmute, core::slice::from_raw_parts_mut, entries[i].set_handler_fn, opt.set_privilege_level |
| `kernel/src/file/signalfd.rs#from_signal_info` | `arceos/modules/axhal/src/platform/riscv64_qemu_virt/irq.rs#register_handler` | 同模块上下文对齐/差异观察 | sig_info.code, sig_info.errno, sig_info.signo | with_cause! |

#### 系统调用
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `kernel/src/syscall/net/io.rs#sys_recvfrom` | `api/src/imp/net/socket.rs#sys_recvfrom` | 同板块同名 | Ok, Some, UserConstPtr::<socklen_t>::from, VmBytesMut::new, addr.is_null, addrlen_ptr.address ...(+7) | Err, Ok, Socket::from_fd, addrlen.is_null, buf_ptr.is_null, core::slice::from_raw_parts_mut ...(+4) |
| `kernel/src/syscall/net/name.rs#sys_getsockname` | `api/src/imp/net/socket.rs#sys_getsockname` | 同板块同名 | NetlinkSocket::from_fd, Ok, Socket::from_fd, checked_socklen_mut, debug!, local_addr.write_to_user ...(+2) | Err, Ok, Socket::from_fd, Socket::from_fd(sock_fd)?.local_addr, addr.is_null, addrlen.is_null ...(+3) |
| `scripts/support-tools/glibc-compat.c#clock_nanosleep` | `apps/nimbos/c/lib/time.c#clock_nanosleep` | 同板块同名 | syscall | syscall |
| `scripts/support-tools/musl-compat.c#clock_nanosleep` | `apps/nimbos/rust/src/time.rs#clock_nanosleep` | 同板块同名 | syscall | sys_clock_nanosleep |

#### 文件系统
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `kernel/src/pseudofs/dev/loop.rs#ioctl` | `api/src/core/fs/imp/dev/framebuffer.rs#ioctl` | 同板块同名 | (arg as *const loop_config).vm_read_uninit, (arg as *const loop_config).vm_read_uninit()?.assume_init, (arg as *const loop_info).vm_read_uninit, (arg as *const loop_info).vm_read_uninit()?.assume_init, (arg as *const loop_info64).vm_read_uninit, (arg as *const loop_info64).vm_read_uninit()?.assume_init ...(+34) | Default::default, Err, Ok, arg.into, framebuffer_flush, user_info.get_as_mut_ref |
| `kernel/src/pseudofs/dev/fb.rs#ioctl` | `api/src/core/fs/pseudo/file.rs#ioctl` | 同板块同名 | (arg as *mut FixScreenInfo).vm_write, (arg as *mut VarScreenInfo).vm_write, Err, Ok, axdisplay::framebuffer_info | Err, warn! |
| `kernel/src/syscall/fs/io.rs#sys_splice` | `api/src/interface/fs/io.rs#sys_splice` | 同板块同名 | AxError::from, Err, File::from_fd, Ok, Pipe::from_fd, SendFile::Direct ...(+21) | Err, Ok, fd_lookup, file_like_as::<Pipe>, file_like_as::<Pipe>(in_file.clone()).is_none, file_like_as::<Pipe>(out_file.clone()).is_none ...(+6) |
| `kernel/src/syscall/fs/io.rs#sys_copy_file_range` | `api/src/interface/fs/io.rs#sys_copy_file_range` | 同板块同名 | AxError::from, Err, Ok, SendFile::Direct, SendFile::Offset, copy_file_range_offset ...(+21) | Ok, fd_lookup, nullable!, sys_copy_file_range_impl |

#### 设备驱动
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `scripts/support-tools/tar.c#is_zero_block` | `arceos/tools/raspi4/chainloader/src/bsp/device_driver/bcm/bcm2xxx_pl011_uart.rs#init` | 同模块上下文对齐/差异观察 | 内部闭环逻辑 | FBRD::BAUD_DIVFRAC.val, IBRD::BAUD_DIVINT.val, self.flush, self.registers
            .CR
            .write, self.registers
            .LCR_H
            .write, self.registers.CR.set ...(+3) |

#### 网络协议栈
仅参考项目中观察到该模块代表函数，目标项目没有同模块可对齐证据：
- 参考项目：`arceos/ulib/axlibc/c/network.c#inet_pton`，调用摘要：hexval, inet_pton, isdigit, memmove
- 参考项目：`api/src/imp/net/socket.rs#setsockopt`，调用摘要：Ok, SocketOptionLevel::try_from, error!, udpsocket.lock, unimplemented!, warn!
- 参考项目：`api/src/imp/net/socket.rs#sendto`，调用摘要：Err, IpAddr::V4, Ipv4Addr::new, SocketAddr::new, udpsocket
                    .bind, udpsocket
                    .bind(SocketAddr::new(IpAddr::V4(Ipv4Addr::new(127, 0, 0, 1)), 0))
                    .map_err ...(+3)
- 参考项目：`api/src/imp/net/socket.rs#shutdown`，调用摘要：Ok, tcpsocket.lock, tcpsocket.peer_addr, tcpsocket.shutdown, udpsocket.lock, udpsocket.peer_addr ...(+1)

#### 文件系统/第三方库
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `third_party/rust-patches/lwext4_rust/c/lwext4/src/ext4_balloc.c#ext4_balloc_alloc_block` | `modules/lwext4_rust/c/lwext4/src/ext4_balloc.c#ext4_balloc_alloc_block` | 同板块同名同结构 | ext4_balloc_get_bgid_of_block, ext4_balloc_get_block_of_bgid, ext4_balloc_set_bitmap_csum, ext4_balloc_verify_bitmap_csum, ext4_bg_get_block_bitmap, ext4_bg_get_free_blocks_count ...(+19) | ext4_balloc_get_bgid_of_block, ext4_balloc_get_block_of_bgid, ext4_balloc_set_bitmap_csum, ext4_balloc_verify_bitmap_csum, ext4_bg_get_block_bitmap, ext4_bg_get_free_blocks_count ...(+19) |
| `third_party/rust-patches/lwext4_rust/c/lwext4/src/ext4_journal.c#jbd_journal_prepare` | `modules/lwext4_rust/c/lwext4/src/ext4_journal.c#jbd_journal_prepare` | 同板块同名同结构 | EXT4_BLOCK_ZERO, RB_FIND, TAILQ_FOREACH_REVERSE_SAFE, TAILQ_FOREACH_SAFE, TAILQ_REMOVE, ext4_bcache_clear_dirty ...(+17) | EXT4_BLOCK_ZERO, RB_FIND, TAILQ_FOREACH_REVERSE_SAFE, TAILQ_FOREACH_SAFE, TAILQ_REMOVE, ext4_bcache_clear_dirty ...(+17) |
| `third_party/rust-patches/lwext4_rust/c/lwext4/src/ext4.c#ext4_dir_rm` | `modules/lwext4_rust/c/lwext4/src/ext4.c#ext4_dir_rm` | 同板块同名同结构 | EXT4_MP_LOCK, EXT4_MP_UNLOCK, ext4_block_cache_write_back, ext4_dir_en_get_inode, ext4_dir_iterator_fini, ext4_dir_iterator_init ...(+19) | EXT4_MP_LOCK, EXT4_MP_UNLOCK, ext4_block_cache_write_back, ext4_dir_en_get_inode, ext4_dir_iterator_fini, ext4_dir_iterator_init ...(+19) |
| `third_party/rust-patches/lwext4_rust/c/lwext4/src/ext4_dir_idx.c#ext4_dir_dx_split_index` | `modules/lwext4_rust/c/lwext4/src/ext4_dir_idx.c#ext4_dir_dx_split_index` | 同板块同名同结构 | ext4_block_set, ext4_dir_dx_climit_get_count, ext4_dir_dx_climit_get_limit, ext4_dir_dx_climit_set_count, ext4_dir_dx_climit_set_limit, ext4_dir_dx_entry_get_hash ...(+10) | ext4_block_set, ext4_dir_dx_climit_get_count, ext4_dir_dx_climit_get_limit, ext4_dir_dx_climit_set_count, ext4_dir_dx_climit_set_limit, ext4_dir_dx_entry_get_hash ...(+10) |

#### 网络协议栈/第三方库
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `crates/axnet-ng/src/tcp.rs#connect` | `arceos/modules/axnet/src/smoltcp_impl/tcp.rs#connect` | 同板块同名 | Err, IpEndpoint::from, Ipv4Addr::LOCALHOST.into, Ipv6Addr::LOCALHOST.into, Ok, Ok::<(), AxError> ...(+30) | Err, Ok, Ok::<(IpEndpoint, IpEndpoint), AxError>, SOCKET_SET
                .with_socket_mut::<tcp::Socket, _, _>, SOCKET_SET.add, SocketSetWrapper::new_tcp_socket ...(+32) |
| `third_party/rust-patches/starry-smoltcp/src/storage/assembler.rs#add` | `arceos/modules/axnet/src/smoltcp_impl/mod.rs#add` | 同板块同名 | Contig::hole_and_data, Err, Ok, contig.has_data, contig.shrink_hole_to, contig.total_size ...(+11) | debug!, self.0.lock, self.0.lock().add |
| `crates/axnet-ng/src/udp.rs#recv` | `arceos/modules/axnet/src/smoltcp_impl/tcp.rs#recv` | 同板块同名 | Err, ExpectedRemote::Any, ExpectedRemote::Expecting, Ok, SocketAddrEx::Ip, ax_bail! ...(+19) | Err, Ok, SOCKET_SET.with_socket_mut::<tcp::Socket, _, _>, ax_err!, ax_err_type!, self.block_on ...(+10) |
| `crates/axnet-ng/src/udp.rs#send` | `arceos/modules/axnet/src/smoltcp_impl/tcp.rs#send` | 同板块同名 | Err, IpAddr::V4, IpEndpoint::from, LinuxError::EMSGSIZE.into, Ok, PacketMeta::default ...(+28) | Err, Ok, SOCKET_SET.with_socket_mut::<tcp::Socket, _, _>, ax_err!, ax_err_type!, self.block_on ...(+10) |

#### 第三方库
仅目标项目中观察到该模块代表函数，参考项目没有同模块可对齐证据：
- 目标项目：`third_party/rust-patches/virtio-drivers/src/queue.rs#fake_read_write_queue`，调用摘要：(*available_ring).idx.load, (*used_ring).idx.fetch_add, (*used_ring).idx.load, Vec::new, assert!, assert_eq! ...(+18)
- 目标项目：`third_party/rust-patches/axio/src/read/mod.rs#default_read_to_end`，调用摘要：Err, Ok, Some, buf.capacity, buf.extend_from_slice, buf.len ...(+24)
- 目标项目：`third_party/rust-patches/virtio-drivers/src/device/sound/fake.rs#handle_control_request`，调用摘要：CommandCode::SOk.into, Some, Vec::new, VirtIOSndHdr {
                            command_code: CommandCode::SOk.into(),
                        }
                        .as_bytes, VirtIOSndHdr::read_from_prefix, VirtIOSndHdr::read_from_prefix(&request).expect ...(+16)
- 目标项目：`third_party/rust-patches/axruntime/src/lib.rs#rust_main`，调用摘要：INITED_CPUS.fetch_add, _edata.as_ptr, _etext.as_ptr, _stext.as_ptr, ax_println!, axbacktrace::init ...(+29)

#### 通用模块/其他
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `kernel/src/bpf/helpers.rs#read_bytes` | `arceos/modules/axhal/src/platform/riscv64_qemu_virt/console.rs#read_bytes` | 同板块同名 | Err, Ok, Vec::new, allowed.contains, bytes.len, charge_aux_budget ...(+3) | VirtAddr::from_mut_ptr_of, bytes.as_mut_ptr, bytes.len, bytes.len().min, sbi_rt::Physical::new, sbi_rt::console_read ...(+2) |
| `kernel/src/bpf/helpers.rs#write_bytes` | `arceos/modules/axhal/src/platform/riscv64_qemu_virt/console.rs#write_bytes` | 同板块同名 | Err, Ok, allowed.contains, bytes.is_empty, self.write_ctx_bytes, self.write_map_value_bytes ...(+1) | bytes.as_ptr, bytes.len, bytes.to_vec, kernel_bytes.len, try_write_bytes |
| `kernel/src/bpf/verifier.rs#log` | `arceos/ulib/axlibc/c/log.c#log` | 同板块同名 | self.buf.push, self.buf.push_str | __builtin_fma, __math_divzero, __math_invalid, asdouble, asuint64, eval_as_double ...(+2) |
| `kernel/src/bpf/helpers.rs#base` | `arceos/modules/axns/src/lib.rs#base` | 同板块同名 | self.data.as_ptr | 内部闭环逻辑 |


好的，我将遵循审计指令，基于提供的证据材料，进行最终的独立定性仲裁。

### 1. 宏观判定
- **定性结论**: 底层架构显著差异 / 独立开发 (相似度: 9.21%，共同 AST 指纹: 864；唯一 AST 指纹: 目标项目 7428，参考项目 2820；覆盖率: 目标项目 11.63%，参考项目 30.64%)

### 2. 全模块覆盖矩阵解读
- **进程管理**: 双方均存在。共同 AST 指纹仅 4 个，目标覆盖率 1.12%，参考覆盖率 3.31%。极低的覆盖率表明双方在进程管理的核心实现上高度异构。证据强度：**弱**。
- **任务调度/异步运行时**: 双方均存在。共同 AST 指纹仅 3 个，目标覆盖率 0.48%，参考覆盖率 1.0%。覆盖率极低，说明调度器与运行时实现路径完全不同。证据强度：**弱**。
- **内存管理**: 双方均存在。共同 AST 指纹 8 个，目标覆盖率 2.0%，参考覆盖率 2.09%。覆盖率低，表明内存管理子系统（如 mmap、加载器等）在实现上存在根本性差异。证据强度：**弱**。
- **中断/异常处理**: 双方均存在，但未观察到共同结构指纹。目标项目仅 5 个函数，参考项目 53 个，规模与实现完全不同。证据强度：**未形成**。
- **系统调用**: 双方均存在。共同 AST 指纹仅 1 个，目标覆盖率 0.39%，参考覆盖率 1.16%。覆盖率极低，说明系统调用分发与实现机制异构。证据强度：**弱**。
- **文件系统**: 双方均存在。共同 AST 指纹 42 个，目标覆盖率 3.56%，参考覆盖率 6.29%。覆盖率低，表明文件系统核心逻辑（如 procfs、ioctl、splice 等）的实现方式不同。证据强度：**弱**。
- **设备驱动**: 双方均存在，但未观察到共同结构指纹。目标项目仅 1 个函数，参考项目 133 个，规模差异巨大，无共同证据。证据强度：**未形成**。
- **网络协议栈**: 仅参考项目体现，目标项目无此独立模块。证据强度：**未形成**。
- **文件系统/第三方库**: 双方均存在。共同 AST 指纹 509 个，目标覆盖率 81.31%，参考覆盖率 94.09%。覆盖率极高，证据显示双方均集成了 `lwext4_rust` 等公共第三方库，这是覆盖率的主要贡献者。证据强度：**强（第三方库/公共依赖）**。
- **网络协议栈/第三方库**: 双方均存在。共同 AST 指纹 4 个，目标覆盖率 0.2%，参考覆盖率 3.45%。覆盖率低，表明双方对 `smoltcp` 等网络库的集成或修改方式不同。证据强度：**弱**。
- **第三方库**: 仅目标项目体现，参考项目无此独立模块。证据强度：**未形成**。
- **通用模块/其他**: 双方均存在。共同 AST 指纹仅 1 个，目标覆盖率 0.57%，参考覆盖率 0.23%。覆盖率极低，表明通用工具函数实现不同。证据强度：**弱**。

### 3. 底层 AST 关键证据指纹表
| 所属板块 | 目标项目路径 | 参考项目路径 | 证据类型 | 技术说明 |
| :--- | :--- | :--- | :--- | :--- |
| 文件系统/第三方库 | `third_party/rust-patches/lwext4_rust/c/lwext4/src/ext4_balloc.c#ext4_balloc_alloc_block` | `modules/lwext4_rust/c/lwext4/src/ext4_balloc.c#ext4_balloc_alloc_block` | 同板块同名同结构 | 双方均使用了相同的第三方库 `lwext4_rust`，AST 指纹完全一致，属于公共依赖复用。 |
| 文件系统/第三方库 | `third_party/rust-patches/lwext4_rust/c/lwext4/src/ext4_journal.c#jbd_journal_prepare` | `modules/lwext4_rust/c/lwext4/src/ext4_journal.c#jbd_journal_prepare` | 同板块同名同结构 | 同上，为 `lwext4_rust` 库内部函数，AST 指纹完全一致。 |
| 文件系统/第三方库 | `third_party/rust-patches/lwext4_rust/c/lwext4/src/ext4.c#ext4_dir_rm` | `modules/lwext4_rust/c/lwext4/src/ext4.c#ext4_dir_rm` | 同板块同名同结构 | 同上，为 `lwext4_rust` 库内部函数，AST 指纹完全一致。 |
| 文件系统/第三方库 | `third_party/rust-patches/lwext4_rust/c/lwext4/src/ext4_dir_idx.c#ext4_dir_dx_split_index` | `modules/lwext4_rust/c/lwext4/src/ext4_dir_idx.c#ext4_dir_dx_split_index` | 同板块同名同结构 | 同上，为 `lwext4_rust` 库内部函数，AST 指纹完全一致。 |
| 进程管理 | `kernel/src/task/process.rs#set_heap_top` | `core/src/process.rs#set_heap_top` | 同板块底层逻辑一致 | 均为 `self.heap_top.store`，是原子变量 setter 的通用 Rust 惯用法，不构成特异性同源。 |
| 进程管理 | `kernel/src/task/process.rs#get_heap_top` | `core/src/process.rs#get_heap_top` | 同板块底层逻辑一致 | 均为 `self.heap_top.load`，是原子变量 getter 的通用 Rust 惯用法，不构成特异性同源。 |
| 进程管理 | `kernel/src/task/process.rs#is_clone_child` | `core/src/process.rs#is_clone_child` | 同板块底层逻辑一致 | 图谱均含 `Some`，是 `Option<T>` 构造子的通用用法，无法证明函数体逻辑一致。 |

### 4. 核心代码调用流对比剖析
* **【进程管理/进程创建与退出】**
* **底层技术析理**: 两者在进程创建与退出的核心路径上采用了完全不同的抽象与同步模型。目标项目 `fork` 函数直接操作 `Arc::new` 和 `SpinNoIrq::new` 进行自旋锁保护下的状态克隆，而参考项目 `fork` 则通过 `Arc::downgrade`、`create_process` 和 `generate_next_pid` 等辅助函数，依赖进程表与 PID 生成器。在退出路径上，目标项目 `sys_exit` 直接调用 `do_exit`，而参考项目则通过 `sys_exit_impl` 进行更复杂的清理流程，包括处理 `addr_clear_child_tid` 等。这体现了在内存管理、锁策略和资源回收机制上的根本性架构差异。
* **代码溯源链接**:
  - 目标项目: `kernel/src/task/process.rs#fork`，`kernel/src/syscall/task/exit.rs#sys_exit`
  - 参考项目: `process/src/process.rs#fork`，`api/src/imp/task/exit.rs#sys_exit_impl`

* **【内存管理/程序加载】**
* **底层技术析理**: 在用户程序加载这一核心内存管理操作上，双方实现路径截然不同。目标项目 `load_user_app` 通过 `ELF_LOADER.lock` 获取全局加载器锁，并直接操作 `Backend::new_alloc` 等后端接口。参考项目 `load_user_app` 则包含了对 ELF 解释器（interp）的复杂处理逻辑，通过 `CStr::from_bytes_with_nul` 解析解释器路径并尝试加载。这表明两者在 ELF 加载策略、地址空间构建和错误处理流程上存在显著差异。
* **代码溯源链接**:
  - 目标项目: `kernel/src/mm/loader.rs#load_user_app`
  - 参考项目: `core/src/mm.rs#load_user_app`

* **【系统调用/信号处理】**
* **底层技术析理**: 在 `sys_rt_sigprocmask` 的实现中，目标项目通过 `curr.as_thread` 获取当前线程，然后操作其 `proc_data` 中的信号掩码，并使用 `oldset.vm_write` 写回用户空间。参考项目则通过 `current_thread_data().signal.with_blocked_mut` 直接操作线程信号结构体。两者在获取当前上下文、访问信号数据结构的路径上完全不同，体现了不同的对象模型和封装策略。
* **代码溯源链接**:
  - 目标项目: `kernel/src/syscall/signal.rs#sys_rt_sigprocmask`
  - 参考项目: `api/src/imp/task/signal.rs#sys_rt_sigprocmask`

### 5. 单方模块与降权项
- **单方模块**: 目标项目独有的“第三方库”模块（2322个函数）和参考项目独有的“网络协议栈”模块（65个函数），表明双方在技术选型和依赖管理上存在根本差异，无法形成同源证据。
- **降权项**: 控辩双方提及的 `sys_execve`、`sys_getpid`、`sys_fork` 等大量同名函数，其函数名由 POSIX/Linux 标准 ABI 定义，任何兼容内核都必须实现，属于**公共接口**。其内部 AST 调用图谱已证实存在显著差异，因此同名不能作为同源证据。`new`、`builder`、`apply` 等属于**泛型函数名**，在大型项目中极为常见，不具备特异性。跨板块出现的同名函数（如目标项目 `kernel/src/bpf/helpers.rs#read_bytes` 与参考项目 `arceos/modules/axhal/.../console.rs#read_bytes`）属于**跨板块硬凑**，不构成同板块主证据。

### 6. 保守结论
本次审计的客观 AST 相似度仅为 9.21%，远低于可判定为抄袭或架构同源的阈值。高达 90% 以上的 AST 指纹是双方项目独有的，这从根本上否定了系统性复用的可能性。双方在进程管理、内存管理、系统调用等核心子系统中的调用流、同步策略和对象模型均表现出显著差异。观察到的共同指纹高度集中在 `lwext4_rust` 等第三方公共依赖库上，这属于正常的开源组件复用，而非原创代码抄袭。控方提出的“底层逻辑一致”证据，经技术析理，均为 Rust 语言原子操作或标准库构造子的通用惯用法，不具备特异性。综合判定，目标项目相对于参考项目是独立开发的成果，整体原创性风险极低。

------------------------------

