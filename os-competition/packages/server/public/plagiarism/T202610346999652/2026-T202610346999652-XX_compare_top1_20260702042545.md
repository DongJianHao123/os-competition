# 🛡️ OS 内核赛自动化代码级架构审计报告

**审计时间**: `2026-07-02 04:25:45`
**参赛作品名称**: `2026-T202610346999652-XX`
**年份**: `2026`
**学校**: `XX`
**队伍名称**: `T202610346999652`
**比赛名字**: `OS-Competition`
**比赛子赛道**: `Kernel-Implementation`
**仓库地址**: `https://gitlab.eduxiji.net/educg-group-43501-3132633/T202610346999652-1129`
**推测开发语言**: `Rust / C (由AST探针自动识别)`
**特征函数量**: `9083`

## ⚔️ 底层代码同源性对比 (AST Homology)
> 本部分由专门的代码审查智能体，对该项目与符合年份条件的历届最高相似度项目进行控制流与底层调用对比。新版报告会先给出确定性的全模块覆盖矩阵，再给出 Agent 的分模块解释，避免只围绕少量抽样函数展开。

### 🏆 Top 1 底层对标: vs `2025-SubsToKernel-北京科技大学`（北京科技大学 / SubsToKernel）

#### 对方作品信息

**对方年份**: `2025`
**对方学校**: `北京科技大学`
**对方队伍名称**: `SubsToKernel`
**对方比赛名字**: `OS-Competition`
**对方比赛子赛道**: `Kernel-Implementation`
**对方仓库地址**: `https://gitlab.eduxiji.net/educg-group-36002-2710490/T202510008995695-2720`

> 双向覆盖率：共同 AST 指纹: 2019；唯一 AST 指纹: 目标项目 7428，参考项目 3437；覆盖率: 目标项目 27.18%，参考项目 58.74%

## 全模块对比总览（确定性证据）

> 总体覆盖率：共同 AST 指纹: 2019；唯一 AST 指纹: 目标项目 7428，参考项目 3437；覆盖率: 目标项目 27.18%，参考项目 58.74%

### 1. 模块覆盖矩阵
| 模块 | 目标函数数 | 参考函数数 | 共同唯一 AST 指纹 | Target 覆盖 | Base 覆盖 | 状态 |
| :--- | ---: | ---: | ---: | ---: | ---: | :--- |
| 进程管理 | 430 | 86 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |
| 任务调度/异步运行时 | 682 | 141 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |
| 内存管理 | 422 | 329 | 2 | 0.5% | 0.69% | 双方存在且有结构重合 |
| 中断/异常处理 | 5 | 8 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |
| 系统调用 | 308 | 108 | 1 | 0.39% | 1.33% | 双方存在且有结构重合 |
| 文件系统 | 1347 | 256 | 2 | 0.17% | 1.07% | 双方存在且有结构重合 |
| 设备驱动 | 1 | 57 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |
| 网络协议栈 | 0 | 124 | 0 | 0.0% | 0.0% | 仅参考项目体现 |
| 文件系统/第三方库 | 735 | 704 | 413 | 65.97% | 69.18% | 双方存在且有结构重合 |
| 网络协议栈/第三方库 | 2617 | 2388 | 1571 | 77.89% | 87.04% | 双方存在且有结构重合 |
| 第三方库 | 2322 | 0 | 0 | 0.0% | 0.0% | 仅目标项目体现 |
| 通用模块/其他 | 214 | 139 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |

### 2. 分模块证据清单

#### 进程管理
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `kernel/src/syscall/task/wait.rs#pid` | `os/src/task/task.rs#pid` | 同板块同名 | child.pid | process.getpid, self.process.upgrade, self.process.upgrade().unwrap |
| `kernel/src/syscall/task/thread.rs#sys_getppid` | `os/src/syscall/process.rs#sys_getppid` | 同板块同名 | current, current()
        .as_thread, current()
        .as_thread()
        .proc_data
        .proc
        .parent, current()
        .as_thread()
        .proc_data
        .proc
        .parent()
        .ok_or, current()
        .as_thread()
        .proc_data
        .proc
        .parent()
        .ok_or(AxError::NoSuchProcess)
        .map, p.pid | current_task, current_task().unwrap, current_task().unwrap().process.upgrade, current_task().unwrap().process.upgrade().unwrap, current_task().unwrap().process.upgrade().unwrap().getppid |
| `kernel/src/syscall/task/thread.rs#sys_getpid` | `os/src/syscall/process.rs#sys_getpid` | 同板块同名 | Ok, curr.as_thread, current, proc_data.pid_ns, proc_data.pid_ns().visible_pid, proc_data.proc.pid | current_task, current_task().unwrap, current_task().unwrap().process.upgrade, current_task().unwrap().process.upgrade().unwrap, current_task().unwrap().process.upgrade().unwrap().getpid |
| `kernel/src/task/thread.rs#set_clear_child_tid` | `os/src/task/process.rs#set_clear_child_tid` | 同板块同名 | self.clear_child_tid
            .store | self.inner_exclusive_access |

#### 任务调度/异步运行时
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `kernel/src/syscall/fs/ctl.rs#sys_renameat2` | `os/src/syscall/fs.rs#sys_renameat2` | 同板块同名 | Err, Ok, Path::new, Some, check_rename_permissions, crate::file::inotify::next_rename_cookie ...(+27) | current_process, inner.get_abs_path, inner.get_user_token, open, open(&old_abs_path, OpenFlags::O_RDWR, NONE_MODE, "")
        .unwrap, open(&old_abs_path, OpenFlags::O_RDWR, NONE_MODE, "")
        .unwrap()
        .file ...(+4) |
| `kernel/src/syscall/fs/stat.rs#sys_statx` | `os/src/syscall/fs.rs#sys_statx` | 同板块同名 | Err, Ok, Path::new, check_parent_search_permissions, curr.as_thread, current ...(+15) | StatxFlags::AT_EMPTY_PATH.bits, StatxFlags::AT_SYMLINK_NOFOLLOW.bits, abs_path.is_empty, convert_kstat_to_statx, copy_to_virt, current_process ...(+12) |
| `kernel/src/syscall/fs/ctl.rs#sys_linkat` | `os/src/syscall/fs.rs#sys_linkat` | 同板块同名 | Err, Ok, Path::new, Some, check_create_permissions, check_search_permissions ...(+35) | unimplemented! |
| `kernel/src/syscall/sync/membarrier.rs#sys_membarrier` | `os/src/syscall/mem.rs#sys_membarrier` | 同板块同名 | Err, Ok, axtask::current, axtask::current().as_thread, axtask::current().as_thread().proc_data.clone, compiler_fence ...(+5) | debug!, fence |

#### 内存管理
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `kernel/src/syscall/mm/mmap.rs#sys_mmap` | `os/src/syscall/mem.rs#sys_mmap` | 同板块同名 | (addr + length).align_up, Arc::new, Backend::new_alloc, Backend::new_cow, Backend::new_file, Backend::new_linear ...(+78) | MapPermission::empty, MmapFlags::from_bits, MmapFlags::from_bits(flags).unwrap, MmapProt::from_bits, MmapProt::from_bits(port).unwrap, Some ...(+16) |
| `kernel/src/syscall/task/wait.rs#sys_waitpid` | `os/src/syscall/process.rs#sys_waitpid` | 同板块同名 | AxError::from, Err, Ok, Poll::Ready, Some, WaitPid::Pgid ...(+33) | assert_eq!, block_current_and_run_next, child.getpid, child.inner_exclusive_access, current_process, debug! ...(+14) |
| `kernel/src/syscall/mm/mmap.rs#sys_msync` | `os/src/syscall/fs.rs#sys_msync` | 同板块同名 | Err, LinuxError::EBUSY.into, Ok, Vec::new, VirtAddr::from, addr.is_multiple_of ...(+10) | VirtAddr::from, area.mmap_file.file.is_some, area.vpn_range.get_end, area.vpn_range.get_end().into, area.vpn_range.get_start, area.vpn_range.get_start().into ...(+5) |
| `kernel/src/mm/loader.rs#map_elf` | `os/src/mm/memory_set.rs#map_elf` | 同板块同名 | Backend::new_cow, ELFParser::new, ELFParser::new(entry.borrow_elf(), base).map_err, Ok, Some, VirtAddr::from_usize ...(+15) | ((ph.virtual_addr() + ph.mem_size()) as usize + offset.0).into, (ph.virtual_addr() as usize + offset.0).into, MapArea::new, PTEFlags::from, Some, Vec::with_capacity ...(+23) |

#### 中断/异常处理
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `scripts/support-tools/oscomp-timeout.c#main` | `os/src/signal/signal.rs#default_op` | 同模块上下文对齐/差异观察 | WEXITSTATUS, WIFEXITED, WIFSIGNALED, WTERMSIG, _exit, execvp ...(+9) | continue_signals.contains, dump_signals.contains, ignore_signals.contains, stop_signals.contains, terminate_signals.contains |
| `kernel/src/pseudofs/dev/tty/terminal/termios.rs#default` | `os/src/hal/trap/mod.rs#trap_handler_kernel` | 同模块上下文对齐/差异观察 | ctl | crmd.plv, crmd::read, era::read, estat.cause, estat::read, panic! ...(+1) |
| `scripts/support-tools/sleep.c#main` | `os/src/hal/trap/mod.rs#enable_timer_interrupt` | 同模块上下文对齐/差异观察 | fprintf, parse_seconds, perror, syscall | crmd::set_ie, ecfg::set_lie, get_timer_freq, sie::set_stimer, tcfg::set_en, tcfg::set_init_val ...(+2) |
| `kernel/src/file/signalfd.rs#from_signal_info` | `os/src/hal/trap/mod.rs#trap_from_kernel` | 同模块上下文对齐/差异观察 | sig_info.code, sig_info.errno, sig_info.signo | era::read, estat::read, panic!, warn! |

#### 系统调用
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `kernel/src/syscall/net/opt.rs#sys_getsockopt` | `os/src/syscall/net.rs#sys_getsockopt` | 同板块同名 | AxError::from, Err, NetlinkSocket::from_fd, Ok, PacketSocket::from_fd, Socket::from_fd ...(+14) | unimplemented! |
| `scripts/support-tools/musl-compat.c#sbrk` | `user/src/lib.rs#sbrk` | 同板块同名 | add_overflows_uintptr, mmap, syscall | sys_sbrk |
| `kernel/src/syscall/net/io.rs#sys_sendmsg` | `os/src/syscall/net.rs#sys_sendmsg` | 同板块同名 | AfAlgSocket::from_fd, Box::new, CMsg::parse, Err, IoVectorBuf::new, UserConstPtr::<cmsghdr>::from ...(+14) | unimplemented! |
| `kernel/src/syscall/net/socket.rs#sys_accept4` | `os/src/syscall/net.rs#sys_accept4` | 同板块同名 | AfAlgSocket::from_fd, Ok, Socket::from_fd, Socket::new, addr.is_null, addrlen.get_as_mut ...(+12) | unimplemented! |

#### 文件系统
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `kernel/src/pseudofs/dev/event.rs#read_at` | `os/src/fs/vfs/mod.rs#read_at` | 同板块同名 | Err, Ok, buf.chunks_exact_mut, buf.is_empty, buf.len, inner.has_event ...(+8) | unimplemented! |
| `kernel/src/pseudofs/cgroup.rs#rename` | `os/src/fs/vfs/mod.rs#rename` | 同板块同名 | Arc::ptr_eq, Err, Ok, dst_dir.children.lock, dst_dir.children.lock().contains_key, dst_dir.children.lock().insert ...(+8) | unimplemented! |
| `kernel/src/pseudofs/cgroup.rs#create` | `os/src/fs/vfs/mod.rs#create` | 同板块同名 | Arc::downgrade, DirEntry::new_dir, DirNode::new, Err, Ok, Self::new ...(+13) | unimplemented! |
| `kernel/src/pseudofs/tmp.rs#unlink` | `os/src/fs/vfs/mod.rs#unlink` | 同板块同名 | Err, Ok, child_entries.clear, child_entries.len, child_entries.lock, dir.entries.lock ...(+5) | unimplemented! |

#### 设备驱动
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `scripts/support-tools/tar.c#is_zero_block` | `os/src/drivers/virtio/blk.rs#read_block` | 同模块上下文对齐/差异观察 | 内部闭环逻辑 | buf.fill, panic!, self.inner.lock, self.inner.lock().ack_interrupt, self.inner.lock().read_blocks |

#### 网络协议栈
仅参考项目中观察到该模块代表函数，目标项目没有同模块可对齐证据：
- 参考项目：`os/src/net/socket/tcp.rs#recv_timeout`，调用摘要：Err, Ok, SOCKET_SET.with_socket_mut::<tcp::Socket, _, _>, ax_err!, ax_err_type!, get_time ...(+11)
- 参考项目：`os/src/net/socket/bench.rs#bench_transmit_bandwidth`，调用摘要：(current_time - past_time).secs, AxNetTxToken::consume, InterfaceWrapper::current_time, info!, self.transmit, tx_buf[0..12].fill ...(+2)
- 参考项目：`os/src/net/socket/tcp.rs#shutdown`，调用摘要：LISTEN_TABLE.unlisten, Ok, SOCKET_SET.poll_interfaces, SOCKET_SET.with_socket_mut::<tcp::Socket, _, _>, debug!, self.handle.get ...(+9)
- 参考项目：`os/src/net/socket/bench.rs#bench_receive_bandwidth`，调用摘要：(current_time - past_time).secs, AxNetRxToken::consume, InterfaceWrapper::current_time, info!, rx_buf.len, self.receive

#### 文件系统/第三方库
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `third_party/rust-patches/lwext4_rust/c/lwext4/src/ext4_balloc.c#ext4_balloc_alloc_block` | `os/libs/lwext4_rsut/c/lwext4/src/ext4_balloc.c#ext4_balloc_alloc_block` | 同板块同名同结构 | ext4_balloc_get_bgid_of_block, ext4_balloc_get_block_of_bgid, ext4_balloc_set_bitmap_csum, ext4_balloc_verify_bitmap_csum, ext4_bg_get_block_bitmap, ext4_bg_get_free_blocks_count ...(+19) | ext4_balloc_get_bgid_of_block, ext4_balloc_get_block_of_bgid, ext4_balloc_set_bitmap_csum, ext4_balloc_verify_bitmap_csum, ext4_bg_get_block_bitmap, ext4_bg_get_free_blocks_count ...(+19) |
| `third_party/rust-patches/lwext4_rust/c/lwext4/src/ext4_journal.c#jbd_journal_prepare` | `os/libs/lwext4_rsut/c/lwext4/src/ext4_journal.c#jbd_journal_prepare` | 同板块同名同结构 | EXT4_BLOCK_ZERO, RB_FIND, TAILQ_FOREACH_REVERSE_SAFE, TAILQ_FOREACH_SAFE, TAILQ_REMOVE, ext4_bcache_clear_dirty ...(+17) | EXT4_BLOCK_ZERO, RB_FIND, TAILQ_FOREACH_REVERSE_SAFE, TAILQ_FOREACH_SAFE, TAILQ_REMOVE, ext4_bcache_clear_dirty ...(+17) |
| `third_party/rust-patches/lwext4_rust/c/lwext4/src/ext4.c#ext4_dir_rm` | `os/libs/lwext4_rsut/c/lwext4/src/ext4.c#ext4_dir_rm` | 同板块同名同结构 | EXT4_MP_LOCK, EXT4_MP_UNLOCK, ext4_block_cache_write_back, ext4_dir_en_get_inode, ext4_dir_iterator_fini, ext4_dir_iterator_init ...(+19) | EXT4_MP_LOCK, EXT4_MP_UNLOCK, ext4_block_cache_write_back, ext4_dir_en_get_inode, ext4_dir_iterator_fini, ext4_dir_iterator_init ...(+19) |
| `third_party/rust-patches/lwext4_rust/c/lwext4/src/ext4_dir_idx.c#ext4_dir_dx_split_index` | `os/libs/lwext4_rsut/c/lwext4/src/ext4_dir_idx.c#ext4_dir_dx_split_index` | 同板块同名同结构 | ext4_block_set, ext4_dir_dx_climit_get_count, ext4_dir_dx_climit_get_limit, ext4_dir_dx_climit_set_count, ext4_dir_dx_climit_set_limit, ext4_dir_dx_entry_get_hash ...(+10) | ext4_block_set, ext4_dir_dx_climit_get_count, ext4_dir_dx_climit_get_limit, ext4_dir_dx_climit_set_count, ext4_dir_dx_climit_set_limit, ext4_dir_dx_entry_get_hash ...(+10) |

#### 网络协议栈/第三方库
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `third_party/rust-patches/starry-smoltcp/src/socket/tcp.rs#process` | `os/libs/smoltcp/src/socket/tcp.rs#process` | 同板块同名 | AckDelayTimer::Waiting, IpEndpoint::new, Self::random_seq_no, Self::rst_reply, Some, control.quash_psh ...(+48) | AckDelayTimer::Waiting, IpEndpoint::new, Self::random_seq_no, Self::rst_reply, Some, control.quash_psh ...(+42) |
| `third_party/rust-patches/starry-smoltcp/src/socket/tcp.rs#dispatch` | `os/libs/smoltcp/src/socket/tcp.rs#dispatch` | 同板块同名 | IpRepr::new, Ok, Some, TcpTimestampRepr::generate_reply_with_tsval, cx.ip_mtu, cx.now ...(+49) | IpRepr::new, Ok, Some, cx.ip_mtu, cx.now, emit ...(+45) |
| `third_party/rust-patches/starry-smoltcp/src/iface/interface/mod.rs#dispatch_ip` | `os/libs/smoltcp/src/iface/interface/mod.rs#dispatch_ip` | 同板块同名 | EthernetAddress, EthernetFrame::<&[u8]>::buffer_len, EthernetFrame::<&[u8]>::header_len, EthernetFrame::new_unchecked, Ipv4Packet::new_unchecked, Ok ...(+34) | EthernetAddress, EthernetFrame::<&[u8]>::buffer_len, EthernetFrame::<&[u8]>::header_len, EthernetFrame::new_unchecked, Ipv4Packet::new_unchecked, Ok ...(+35) |
| `third_party/rust-patches/starry-smoltcp/src/socket/dhcpv4.rs#dispatch` | `os/libs/smoltcp/src/socket/dhcpv4.rs#dispatch` | 同板块同名同结构 | Ok, Self::random_transaction_id, Some, cx.hardware_addr, cx.ip_mtu, cx.now ...(+12) | Ok, Self::random_transaction_id, Some, cx.hardware_addr, cx.ip_mtu, cx.now ...(+12) |

#### 第三方库
仅目标项目中观察到该模块代表函数，参考项目没有同模块可对齐证据：
- 目标项目：`third_party/rust-patches/virtio-drivers/src/queue.rs#fake_read_write_queue`，调用摘要：(*available_ring).idx.load, (*used_ring).idx.fetch_add, (*used_ring).idx.load, Vec::new, assert!, assert_eq! ...(+18)
- 目标项目：`third_party/rust-patches/axio/src/read/mod.rs#default_read_to_end`，调用摘要：Err, Ok, Some, buf.capacity, buf.extend_from_slice, buf.len ...(+24)
- 目标项目：`third_party/rust-patches/virtio-drivers/src/device/sound/fake.rs#handle_control_request`，调用摘要：CommandCode::SOk.into, Some, Vec::new, VirtIOSndHdr {
                            command_code: CommandCode::SOk.into(),
                        }
                        .as_bytes, VirtIOSndHdr::read_from_prefix, VirtIOSndHdr::read_from_prefix(&request).expect ...(+16)
- 目标项目：`third_party/rust-patches/axruntime/src/lib.rs#rust_main`，调用摘要：INITED_CPUS.fetch_add, _edata.as_ptr, _etext.as_ptr, _stext.as_ptr, ax_println!, axbacktrace::init ...(+29)

#### 通用模块/其他
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `kernel/src/bpf/verifier.rs#log` | `os/src/logging.rs#log` | 同板块同名 | self.buf.push, self.buf.push_str | println!, record.level, record.metadata, self.enabled |
| `kernel/src/bpf/verifier.rs#eval_scalar_alu_state` | `os/src/hal/arch/info.rs#test_csr_register` | 同模块上下文对齐/差异观察 | (lhs as u32).wrapping_add, (lhs as u32).wrapping_mul, (lhs as u32).wrapping_sub, RegState::scalar, RegState::scalar_const, insn.class ...(+7) | crmd.da, crmd.ie, crmd.pg, crmd.plv, crmd::read, ecfg.lie ...(+13) |
| `kernel/src/bpf/verifier.rs#successor_states` | `os/src/boards/qemu.rs#exit` | 同模块上下文对齐/差异观察 | Ok, RegState::map_value, RegState::ringbuf_mem, calc_jump_target, insn.dst_reg, insn.op ...(+10) | asm!, exit_code_encode |
| `kernel/src/bpf/verifier.rs#verify_program` | `os/src/signal/signal.rs#check_error` | 同模块上下文对齐/差异观察 | Err, Ok, VerifierLog::new, insns.is_empty, insns.len, insns.to_vec ...(+3) | Some, self.contains |


好的，我将遵循审计指令，以仲裁专家身份，基于客观 AST 证据与双方论点，对本案进行最终定性。

### 1. 宏观判定
- **定性结论**: **独立开发，底层架构存在显著差异** (相似度: 22.82%，共同 AST 指纹: 2019；唯一 AST 指纹: 目标项目 7428，参考项目 3437；覆盖率: 目标项目 27.18%，参考项目 58.74%)
- **核心依据**: 双方代码的 AST 重合高度集中于 `lwext4_rust`、`smoltcp` 等合法共享的第三方开源库。在原创内核核心板块，尤其是控方重点指控的【进程管理】板块，**共同唯一 AST 指纹为 0，覆盖率为 0.0%**。这从根本上否定了核心逻辑存在抄袭或同源复用的可能性。控方指控的“底层逻辑一致”实为 Linux ABI 强制语义路径与标准内核设计范式下的趋同，而非代码结构的复用。

### 2. 全模块覆盖矩阵解读
1.  **进程管理**
    *   **双方存在性**: 双方均存在。
    *   **共同 AST 指纹**: **0**。
    *   **覆盖率解读**: Target 0.0%，Base 0.0%。覆盖率双双为零，是“独立开发”最直接的证据。双方在进程标识获取、退出、克隆等系统调用上采用了完全不同的对象模型和调用路径。
    *   **证据强度**: **未形成**。控方所有关于此模块的指控均被客观 AST 数据证伪。

2.  **任务调度/异步运行时**
    *   **双方存在性**: 双方均存在。
    *   **共同 AST 指纹**: **0**。
    *   **覆盖率解读**: Target 0.0%，Base 0.0%。双方调度器实现（如 `run_tasks` vs `execute`）在调用图谱上无任何结构重合，表明其调度实体、队列机制和上下文切换逻辑是独立构建的。
    *   **证据强度**: **未形成**。

3.  **内存管理**
    *   **双方存在性**: 双方均存在。
    *   **共同 AST 指纹**: 2。
    *   **覆盖率解读**: Target 0.5%，Base 0.69%。极低的覆盖率源于 `sys_mmap` 等标准系统调用中对 `MmapProt`、`MapPermission` 等标志位的解析，以及 `map_elf` 中 ELF 加载的标准流程。这些是 POSIX 规范和 ELF 格式所决定的，不构成特定项目的复用指纹。
    *   **证据强度**: **弱**。重合点均为标准接口和通用文件格式解析。

4.  **中断/异常处理**
    *   **双方存在性**: 双方均存在。
    *   **共同 AST 指纹**: **0**。
    *   **覆盖率解读**: Target 0.0%，Base 0.0%。双方在 Trap 处理、中断控制器操作等底层硬件交互代码上无任何结构重合。
    *   **证据强度**: **未形成**。

5.  **系统调用**
    *   **双方存在性**: 双方均存在。
    *   **共同 AST 指纹**: 1。
    *   **覆盖率解读**: Target 0.39%，Base 1.33%。重合点仅为 `sys_getsockopt` 等极少数标准系统调用入口，且参考项目实现为 `unimplemented!`，目标项目则有完整实现。这证明双方仅在 ABI 命名上一致，实现上无继承关系。
    *   **证据强度**: **弱**。仅为系统调用接口的同名，无实现逻辑重合。

6.  **文件系统**
    *   **双方存在性**: 双方均存在。
    *   **共同 AST 指纹**: 2。
    *   **覆盖率解读**: Target 0.17%，Base 1.07%。重合点源于 `read_at`、`rename` 等 VFS 标准接口的同名。参考项目实现为 `unimplemented!`，目标项目则有完整的伪文件系统实现，两者在实现层面无任何共同结构。
    *   **证据强度**: **弱**。仅为 VFS 层接口的同名，实现逻辑完全不同。

7.  **设备驱动**
    *   **双方存在性**: 双方均存在。
    *   **共同 AST 指纹**: **0**。
    *   **覆盖率解读**: Target 0.0%，Base 0.0%。目标项目仅1个函数，参考项目有57个函数，双方在驱动模型和实现上无任何交集。
    *   **证据强度**: **未形成**。

8.  **网络协议栈**
    *   **双方存在性**: 仅参考项目体现。
    *   **共同 AST 指纹**: **0**。
    *   **覆盖率解读**: 目标项目无此原创模块，其网络功能由第三方库 `smoltcp` 提供。参考项目虽有原创网络协议栈，但与目标项目无任何 AST 重合。
    *   **证据强度**: **未形成**。

9.  **第三方库/公共依赖**
    *   **双方存在性**: 双方均存在大量第三方库代码。
    *   **共同 AST 指纹**: 1984 (413 + 1571)。
    *   **覆盖率解读**: 这是双方 AST 重合的**唯一主要来源**。`lwext4_rust` 和 `smoltcp` 等上游开源项目贡献了几乎全部的共同指纹。这是双方合法引用相同依赖的必然结果，与参赛作品的原创性无关。
    *   **证据强度**: **强**，但性质为**合法共享依赖**，不构成抄袭。

### 3. 底层 AST 关键证据指纹表
| 所属板块 | 目标项目路径 | 参考项目路径 | 证据类型 | 技术说明 |
| :--- | :--- | :--- | :--- | :--- |
| 第三方库/公共依赖 | `third_party/.../lwext4/src/ext4_balloc.c#ext4_balloc_alloc_block` | `os/libs/.../lwext4/src/ext4_balloc.c#ext4_balloc_alloc_block` | 同源文件/同函数 | 双方均引用了相同的开源 ext4 文件系统库，AST 结构完全一致。 |
| 第三方库/公共依赖 | `third_party/.../smoltcp/src/socket/tcp.rs#process` | `os/libs/smoltcp/src/socket/tcp.rs#process` | 同源文件/同函数 | 双方均引用了相同的开源网络协议栈 smoltcp，AST 结构高度一致。 |
| 进程管理 | `kernel/src/syscall/task/thread.rs#sys_getpid` | `os/src/syscall/process.rs#sys_getpid` | 同板块同名/无共同指纹 | 双方均实现了 Linux ABI 规定的 `getpid` 系统调用，但调用路径完全不同，AST 指纹为 0。 |
| 进程管理 | `kernel/src/syscall/task/exit.rs#sys_exit` | `os/src/syscall/process.rs#sys_exit` | 同板块同名/无共同指纹 | 双方均实现了 `exit` 系统调用，但目标项目委托给 `do_exit`，参考项目内联了调度逻辑，无结构重合。 |
| 内存管理 | `kernel/src/syscall/mm/mmap.rs#sys_mmap` | `os/src/syscall/mem.rs#sys_mmap` | 同板块同名/弱证据 | 双方均实现了 `mmap` 系统调用，对标志位的解析是 POSIX 标准行为，非特定代码复用。 |
| 文件系统 | `kernel/src/pseudofs/cgroup.rs#rename` | `os/src/fs/vfs/mod.rs#rename` | 同板块同名/弱证据 | 双方均有 VFS `rename` 接口，但参考项目为 `unimplemented!`，目标项目有完整实现，无逻辑重合。 |

### 4. 核心代码调用流对比剖析
*   **【进程管理/进程标识获取】**
*   **底层技术析理**: 两者在获取当前任务上下文的方式上存在根本性架构差异。目标项目基于 `axtask` 的线程-进程模型，通过 `current().as_thread().proc_data.proc` 路径直接访问进程控制块，并支持 PID 命名空间 (`pid_ns().visible_pid`)。参考项目则基于传统的 `current_task().unwrap().process.upgrade().unwrap()` 弱引用模型，通过 `getpid()` 方法获取 PID。两者的对象管理、所有权模型和并发控制策略完全不同。
*   **代码溯源链接**:
  - 目标项目: `kernel/src/syscall/task/thread.rs#sys_getpid`
  - 参考项目: `os/src/syscall/process.rs#sys_getpid`

*   **【进程管理/进程退出】**
*   **底层技术析理**: 目标项目采用分层委托架构，`sys_exit` 和 `sys_exit_group` 均将核心逻辑下沉到统一的 `do_exit` 函数，入口处仅做简单的 `Ok` 返回，体现了清晰的模块化设计。参考项目则在系统调用入口处直接内联了进程状态管理、调试打印和调度器操作 (`exit_current_and_run_next`)，逻辑流与调度器紧密耦合。两者在职责分离和调用层次上存在显著差异。
*   **代码溯源链接**:
  - 目标项目: `kernel/src/syscall/task/exit.rs#sys_exit_group`
  - 参考项目: `os/src/syscall/process.rs#sys_exit_group`

*   **【内存管理/内存映射】**
*   **底层技术析理**: 目标项目的 `sys_mmap` 调用图谱展示了基于 `Backend` 抽象的多种映射后端（`new_alloc`, `new_cow`, `new_file`），体现了更复杂的现代内核内存管理架构。参考项目则通过 `MmapFlags` 和 `MmapProt` 解析后直接操作 `memory_set.mmap`。两者在内存管理子系统的抽象层次和扩展性设计上存在本质区别。
*   **代码溯源链接**:
  - 目标项目: `kernel/src/syscall/mm/mmap.rs#sys_mmap`
  - 参考项目: `os/src/syscall/mem.rs#sys_mmap`

### 5. 单方模块与降权项
*   **单方模块**:
    *   **目标项目独有**: `第三方库` (2322个函数) 和 `网络协议栈/第三方库` (2617个函数) 中的大量补丁及自研组件，如 `virtio-drivers`、`axruntime` 等，体现了其独立的驱动和运行时生态。
    *   **参考项目独有**: `网络协议栈` (124个函数) 原创实现，是其教学内核特性的体现。
    *   **结论**: 单方模块的存在进一步证明了两者技术栈和开发重点的独立性。
*   **降权项**:
    *   **跨板块硬凑**: 控方将目标项目的 eBPF 虚拟机 (`kernel/src/bpf/vm.rs`)、procfs (`kernel/src/pseudofs/proc.rs`)、cgroup (`kernel/src/pseudofs/cgroup.rs`) 等子系统函数强行归入【进程管理】板块进行比对，这些函数与参考项目的进程管理函数无任何逻辑关联，不构成同板块证据。
    *   **语言惯用法与标准库**: `Arc::downgrade`、`format!`、`debug!`、`assert!` 等 Rust 语言标准库和宏的调用，是任何 Rust 项目的通用实践，不能作为特定项目的复用指纹。
    *   **ABI 强制语义**: `sys_getpid`、`sys_exit`、`sys_fork` 等 POSIX/Linux 标准系统调用的同名和基本语义路径，是接口规范强制要求的，不能作为抄袭证据。

### 6. 保守结论
综合全模块 AST 指纹分析，双方代码的相似度（22.82%）几乎全部由 `lwext4_rust` 和 `smoltcp` 等合法共享的第三方开源库贡献。在操作系统内核最核心、最能体现原创性的【进程管理】、【任务调度】、【中断/异常处理】等板块，双方的共同 AST 指纹均为 0，覆盖率均为 0.0%。这确凿地证明，被告项目在关键内核子系统上进行了完全独立的架构设计与实现，其技术 lineage 与原告项目截然不同。控方基于语义和同名的指控，在客观 AST 结构证据面前无法成立。因此，本案不存在代码抄袭或同源复用风险，双方作品均为独立开发。

------------------------------

