# 🛡️ OS 内核赛自动化代码级架构审计报告

**审计时间**: `2026-07-02 04:25:45`
**参赛作品名称**: `2026-T202610346999652-XX`
**年份**: `2026`
**学校**: `XX`
**队伍名称**: `T202610346999652`
**比赛名字**: `OS-Competition`
**比赛子赛道**: `Kernel-Implementation`
**仓库地址**: `https://gitlab.eduxiji.net/educg-group-43501-3132633/T202610346999652-1129`
**推测开发语言**: `Rust / C (由AST探针自动识别)`
**特征函数量**: `9083`

## ⚔️ 底层代码同源性对比 (AST Homology)
> 本部分由专门的代码审查智能体，对该项目与符合年份条件的历届最高相似度项目进行控制流与底层调用对比。新版报告会先给出确定性的全模块覆盖矩阵，再给出 Agent 的分模块解释，避免只围绕少量抽样函数展开。

### 🏆 Top 5 底层对标: vs `2024-cabbageOS-华中科技大学`（华中科技大学 / cabbageOS）

#### 对方作品信息

**对方年份**: `2024`
**对方学校**: `华中科技大学`
**对方队伍名称**: `cabbageOS`
**对方比赛名字**: `OS-Competition`
**对方比赛子赛道**: `Kernel-Implementation`
**对方仓库地址**: `https://gitlab.eduxiji.net/educg-group-26010-2376550/T202410487992456-422`

> 双向覆盖率：共同 AST 指纹: 749；唯一 AST 指纹: 目标项目 7428，参考项目 1579；覆盖率: 目标项目 10.08%，参考项目 47.44%

## 全模块对比总览（确定性证据）

> 总体覆盖率：共同 AST 指纹: 749；唯一 AST 指纹: 目标项目 7428，参考项目 1579；覆盖率: 目标项目 10.08%，参考项目 47.44%

### 1. 模块覆盖矩阵
| 模块 | 目标函数数 | 参考函数数 | 共同唯一 AST 指纹 | Target 覆盖 | Base 覆盖 | 状态 |
| :--- | ---: | ---: | ---: | ---: | ---: | :--- |
| 进程管理 | 430 | 95 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |
| 任务调度/异步运行时 | 682 | 56 | 1 | 0.16% | 1.85% | 双方存在且有结构重合 |
| 内存管理 | 422 | 290 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |
| 中断/异常处理 | 5 | 28 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |
| 系统调用 | 308 | 76 | 1 | 0.39% | 4.55% | 双方存在且有结构重合 |
| 文件系统 | 1347 | 176 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |
| 设备驱动 | 1 | 446 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |
| 网络协议栈 | 0 | 0 | 0 | 0.0% | 0.0% | 双方均未体现 |
| 文件系统/第三方库 | 735 | 522 | 390 | 62.3% | 89.45% | 双方存在且有结构重合 |
| 网络协议栈/第三方库 | 2617 | 0 | 0 | 0.0% | 0.0% | 仅目标项目体现 |
| 第三方库 | 2322 | 1 | 1 | 0.05% | 100.0% | 双方存在且有结构重合 |
| 通用模块/其他 | 214 | 228 | 1 | 0.57% | 0.54% | 双方存在且有结构重合 |

### 2. 分模块证据清单

#### 进程管理
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `kernel/src/syscall/task/thread.rs#sys_getppid` | `kernel/src/syscall/sysproc.c#sys_getppid` | 同板块同名 | current, current()
        .as_thread, current()
        .as_thread()
        .proc_data
        .proc
        .parent, current()
        .as_thread()
        .proc_data
        .proc
        .parent()
        .ok_or, current()
        .as_thread()
        .proc_data
        .proc
        .parent()
        .ok_or(AxError::NoSuchProcess)
        .map, p.pid | proc_current |
| `kernel/src/syscall/task/thread.rs#sys_getpid` | `kernel/src/syscall/sysproc.c#sys_getpid` | 同板块同名 | Ok, curr.as_thread, current, proc_data.pid_ns, proc_data.pid_ns().visible_pid, proc_data.proc.pid | proc_current |
| `kernel/src/syscall/task/exit.rs#sys_exit_group` | `kernel/src/syscall/sysproc.c#sys_exit_group` | 同板块同名 | Ok, do_exit | atomic_read, do_exit_group, proc_current |
| `kernel/src/syscall/task/exit.rs#sys_exit` | `kernel/src/syscall/sysproc.c#sys_exit` | 同板块同名 | Ok, do_exit | arg_int, do_exit |

#### 任务调度/异步运行时
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `kernel/src/syscall/fs/ctl.rs#sys_renameat2` | `kernel/src/syscall/sysfile.c#sys_renameat2` | 同板块同名 | Err, Ok, Path::new, Some, check_rename_permissions, crate::file::inotify::next_rename_cookie ...(+27) | ASSERT, arg_int, arg_str, do_renameat2, find_inode, get_absolute_path ...(+3) |
| `kernel/src/syscall/io_mpx/select.rs#do_select` | `kernel/src/fs/select.c#do_select` | 同板块同名 | Err, FD_SET, FD_ZERO, FdPollSet, FdSet::new, IoEvents::empty ...(+42) | TIME2NS, panic, printf, proc_current, rdtime |
| `kernel/src/syscall/fs/ctl.rs#sys_linkat` | `kernel/src/syscall/sysfile.c#sys_linkat` | 同板块同名 | Err, Ok, Path::new, Some, check_create_permissions, check_search_permissions ...(+35) | ASSERT, arg_int, arg_str, find_inode, get_absolute_path, get_fs_from_path ...(+3) |
| `kernel/src/syscall/fs/ctl.rs#sys_unlinkat` | `kernel/src/syscall/sysfile.c#sys_unlinkat` | 同板块同名 | Err, Ok, Path::new, Some, axfs::mark_cached_file_unlinked, check_remove_permissions ...(+27) | __namecmp, arg_int, arg_str, dp->i_op->delete, dp->i_op->dirlookup, dp->i_op->lock ...(+13) |

#### 内存管理
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `kernel/src/syscall/task/clone.rs#do_clone` | `kernel/src/proc/pcb_life.c#do_clone` | 同板块同名 | (parent_tid as *mut Pid).vm_write, (parent_tid as *mut Pid).vm_write(tid).ok, (pidfd as *mut i32).vm_write, Arc::new, AxTaskExt::from_impl, Default::default ...(+127) | TCB_Q_changeState, acquire, alloc_thread, appendChild, atomic_add, copy_out ...(+12) |
| `kernel/src/syscall/mm/mmap.rs#sys_mmap` | `kernel/src/mm/mmap.c#sys_mmap` | 同板块同名 | (addr + length).align_up, Arc::new, Backend::new_alloc, Backend::new_cow, Backend::new_file, Backend::new_linear ...(+78) | acquire, arg_addr, arg_fd, arg_int, arg_long, arg_ulong ...(+3) |
| `kernel/src/syscall/fs/io.rs#sys_fallocate` | `kernel/src/syscall/sysfile.c#sys_fallocate` | 同板块同名 | AxError::from, Err, FileLikeKind::from_file_like, Ok, backend.location, backend.location().clone ...(+35) | 内部闭环逻辑 |
| `kernel/src/syscall/ipc/shm.rs#sys_shmctl` | `kernel/src/syscall/sysipc.c#sys_shmctl` | 同板块同名 | AxError::from, Err, Ok, SHM_MANAGER.lock, SHM_MANAGER.lock().remove_shmid, admin_ipc_permission ...(+31) | arg_addr, arg_int, copy_in, do_IPC_INFO, do_SHM_INFO, do_STAT ...(+3) |

#### 中断/异常处理
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `scripts/support-tools/oscomp-timeout.c#main` | `kernel/platform/visionfive/src/trap.c#tf_flrestore` | 同模块上下文对齐/差异观察 | WEXITSTATUS, WIFEXITED, WIFSIGNALED, WTERMSIG, _exit, execvp ...(+9) | 内部闭环逻辑 |
| `kernel/src/pseudofs/dev/tty/terminal/termios.rs#default` | `kernel/platform/visionfive/src/trap.c#tf_flstore` | 同模块上下文对齐/差异观察 | ctl | 内部闭环逻辑 |
| `scripts/support-tools/sleep.c#main` | `kernel/platform/qemu/src/virtio_disk.c#virtio_disk_intr` | 同模块上下文对齐/差异观察 | fprintf, parse_seconds, perror, syscall | R, __sync_synchronize, acquire, panic, release, sem_v |
| `kernel/src/file/signalfd.rs#from_signal_info` | `kernel/platform/qemu/src/trap.c#kerneltrap` | 同模块上下文对齐/差异观察 | sig_info.code, sig_info.errno, sig_info.signo | backtrace, devintr, intr_get, panic, printf, r_scause ...(+7) |

#### 系统调用
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `scripts/support-tools/musl-compat.c#sbrk` | `user/deps/syscall.c#sbrk` | 同板块同名 | add_overflows_uintptr, mmap, syscall | syscall |
| `kernel/src/syscall/sync/futex.rs#sys_set_robust_list` | `kernel/src/syscall/syscallnew.c#sys_set_robust_list` | 同板块同名 | Err, Ok, current, current().as_thread, current().as_thread().set_robust_list_head, head.addr ...(+1) | 内部闭环逻辑 |
| `scripts/support-tools/glibc-compat.c#clock_gettime` | `user/deps/syscall.c#clock_gettime` | 同板块同名同结构 | syscall | syscall |
| `scripts/support-tools/musl-compat.c#timer_settime` | `user/deps/syscall.c#renameat2` | 同板块同结构 | syscall | syscall |

#### 文件系统
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `kernel/src/syscall/fs/io.rs#sys_splice` | `kernel/src/syscall/sysfile.c#sys_splice` | 同板块同名 | AxError::from, Err, File::from_fd, Ok, Pipe::from_fd, SendFile::Direct ...(+21) | arg_addr, arg_fd, arg_int, splice |
| `kernel/src/file/fanotify.rs#notify` | `kernel/dep/virtio-drivers/src/transport/pci.rs#notify` | 同板块同名 | FanotifyFile::enqueue_locked, Some, WatchKey::from_location, drop, each_fanotify_file, event_loc.clone ...(+9) | addr_of_mut!, addr_of_mut!((*self.notify_region.as_ptr())[index]).vwrite, size_of::<u16>, usize::from, volread!, volwrite! |
| `kernel/src/syscall/fs/io.rs#sys_pwrite64` | `kernel/src/syscall/sysfile.c#sys_pwrite64` | 同板块同名 | Err, Ok, VmBytes::new, allowed_write_len, f.as_ref, f.inner ...(+15) | arg_addr, arg_fd, arg_long, arg_ulong, f->f_op->pwrite |
| `kernel/src/syscall/fs/io.rs#sys_write` | `kernel/src/syscall/sysfile.c#sys_write` | 同板块同名 | Ok, VmBytes::new, check_writable_mount, cooperate_after_regular_file_io, current_file_offset, debug! ...(+13) | arg_addr, arg_fd, arg_int, f->f_op->write |

#### 设备驱动
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `scripts/support-tools/tar.c#is_zero_block` | `kernel/dep/virtio-drivers/src/queue.rs#fake_read_write_queue` | 同模块上下文对齐/差异观察 | 内部闭环逻辑 | (*available_ring).idx.load, (*used_ring).idx.fetch_add, (*used_ring).idx.load, Vec::new, assert!, assert_eq! ...(+18) |

#### 文件系统/第三方库
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `third_party/rust-patches/lwext4_rust/c/lwext4/src/ext4_balloc.c#ext4_balloc_alloc_block` | `kernel/src/fs/ext4/lwext4/ext4_balloc.c#ext4_balloc_alloc_block` | 同板块同名同结构 | ext4_balloc_get_bgid_of_block, ext4_balloc_get_block_of_bgid, ext4_balloc_set_bitmap_csum, ext4_balloc_verify_bitmap_csum, ext4_bg_get_block_bitmap, ext4_bg_get_free_blocks_count ...(+19) | ext4_balloc_get_bgid_of_block, ext4_balloc_get_block_of_bgid, ext4_balloc_set_bitmap_csum, ext4_balloc_verify_bitmap_csum, ext4_bg_get_block_bitmap, ext4_bg_get_free_blocks_count ...(+19) |
| `third_party/rust-patches/lwext4_rust/c/lwext4/src/ext4_journal.c#jbd_journal_prepare` | `kernel/src/fs/ext4/lwext4/ext4_journal.c#jbd_journal_prepare` | 同板块同名同结构 | EXT4_BLOCK_ZERO, RB_FIND, TAILQ_FOREACH_REVERSE_SAFE, TAILQ_FOREACH_SAFE, TAILQ_REMOVE, ext4_bcache_clear_dirty ...(+17) | EXT4_BLOCK_ZERO, RB_FIND, TAILQ_FOREACH_REVERSE_SAFE, TAILQ_FOREACH_SAFE, TAILQ_REMOVE, ext4_bcache_clear_dirty ...(+17) |
| `third_party/rust-patches/lwext4_rust/c/lwext4/src/ext4.c#ext4_dir_rm` | `kernel/src/fs/ext4/lwext4/ext4.c#ext4_dir_rm` | 同板块同名同结构 | EXT4_MP_LOCK, EXT4_MP_UNLOCK, ext4_block_cache_write_back, ext4_dir_en_get_inode, ext4_dir_iterator_fini, ext4_dir_iterator_init ...(+19) | EXT4_MP_LOCK, EXT4_MP_UNLOCK, ext4_block_cache_write_back, ext4_dir_en_get_inode, ext4_dir_iterator_fini, ext4_dir_iterator_init ...(+19) |
| `third_party/rust-patches/lwext4_rust/c/lwext4/src/ext4_dir_idx.c#ext4_dir_dx_split_index` | `kernel/src/fs/ext4/lwext4/ext4_dir_idx.c#ext4_dir_dx_split_index` | 同板块同名同结构 | ext4_block_set, ext4_dir_dx_climit_get_count, ext4_dir_dx_climit_get_limit, ext4_dir_dx_climit_set_count, ext4_dir_dx_climit_set_limit, ext4_dir_dx_entry_get_hash ...(+10) | ext4_block_set, ext4_dir_dx_climit_get_count, ext4_dir_dx_climit_get_limit, ext4_dir_dx_climit_set_count, ext4_dir_dx_climit_set_limit, ext4_dir_dx_entry_get_hash ...(+10) |

#### 网络协议栈/第三方库
仅目标项目中观察到该模块代表函数，参考项目没有同模块可对齐证据：
- 目标项目：`third_party/rust-patches/starry-smoltcp/src/socket/tcp.rs#process`，调用摘要：AckDelayTimer::Waiting, IpEndpoint::new, Self::random_seq_no, Self::rst_reply, Some, control.quash_psh ...(+48)
- 目标项目：`third_party/rust-patches/starry-smoltcp/src/socket/tcp.rs#dispatch`，调用摘要：IpRepr::new, Ok, Some, TcpTimestampRepr::generate_reply_with_tsval, cx.ip_mtu, cx.now ...(+49)
- 目标项目：`third_party/rust-patches/starry-smoltcp/src/iface/interface/multicast.rs#multicast_egress`，调用摘要：(timeout + interval).max, MldAddressRecordRepr::new, PacketMeta::default, Some, device.transmit, self
                    .inner
                    .multicast
                    .groups
                    .iter ...(+15)
- 目标项目：`third_party/rust-patches/starry-smoltcp/src/iface/interface/mod.rs#dispatch_ip`，调用摘要：EthernetAddress, EthernetFrame::<&[u8]>::buffer_len, EthernetFrame::<&[u8]>::header_len, EthernetFrame::new_unchecked, Ipv4Packet::new_unchecked, Ok ...(+34)

#### 第三方库
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `third_party/rust-patches/virtio-drivers/src/transport/mmio.rs#vendor_id` | `kernel/dep/virtio-drivers/src/transport/mmio.rs#vendor_id` | 同板块同名同结构 | volread! | volread! |

#### 通用模块/其他
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `scripts/support-tools/date.c#die_usage` | `kernel/platform/qemu/src/start.c#start` | 同板块同结构 | exit, usage | main, w_tp |
| `kernel/src/bpf/verifier.rs#eval_scalar_alu_state` | `kernel/src/lib/qsort.c#_quicksort` | 同模块上下文对齐/差异观察 | (lhs as u32).wrapping_add, (lhs as u32).wrapping_mul, (lhs as u32).wrapping_sub, RegState::scalar, RegState::scalar_const, insn.class ...(+7) | (*cmp), POP, PUSH, SWAP, min |
| `kernel/src/bpf/verifier.rs#successor_states` | `kernel/dep/sdcard/src/visionfive2_sd/sd.rs#init_sdcard` | 同模块上下文对齐/差异观察 | Ok, RegState::map_value, RegState::ringbuf_mem, calc_jump_target, insn.dst_reg, insn.op ...(+10) | CardTypeReg::from, CardTypeReg::from(0).with_card_width4_1, CmdArg::new, CmdReg::from, ControlReg::from, RawInterruptStatusReg::from ...(+20) |
| `kernel/src/bpf/verifier.rs#verify_program` | `user/deps/sprintf.c#number` | 同模块上下文对齐/差异观察 | Err, Ok, VerifierLog::new, insns.is_empty, insns.len, insns.to_vec ...(+3) | do_div |


好的，审判长。本庭已收到控辩双方陈词及全部底层 AST 证据，现依据《审计指令》进行最终定性仲裁。

### 1. 宏观判定
- **定性结论**: **独立开发 (底层架构显著差异)**
- **核心依据**: 客观 AST 相似度仅为 9.07%。目标项目唯一 AST 指纹高达 7428，参考项目为 1579，双方共同指纹仅 749。目标项目覆盖率 10.08%，参考项目覆盖率 47.44%。在控方重点指控的【进程管理】板块，共同 AST 指纹为 0，覆盖率均为 0.0%，不存在任何可被 AST 层面量化的结构同源性。749 条共同指纹中，超过半数（391 条）源于双方共同引用的开源第三方库，属于合法公共依赖，不应计入内核原创性抄袭判定。

### 2. 全模块覆盖矩阵解读
| 模块 | 双方均存在 | 共同 AST 指纹 | 覆盖率 (目标/参考) | 证据强度 | 解读 |
| :--- | :--- | :--- | :--- | :--- | :--- |
| **进程管理** | 是 | **0** | 0.0% / 0.0% | **未形成** | 控方指控的核心板块。双方虽均有实现，但 AST 层面无任何共同指纹。目标项目（430 函数）规模远超参考项目（95 函数），体现了 Rust 异步、Capability、PID 命名空间等现代特性，与参考项目的 C 语言传统实现存在根本性架构差异。 |
| **任务调度/异步运行时** | 是 | 1 | 0.16% / 1.85% | **弱** | 仅 1 个共同指纹，覆盖率极低。目标项目（682 函数）基于 Rust 异步运行时，参考项目（56 函数）为传统 C 调度器，两者在调度模型、同步原语上完全不同。 |
| **内存管理** | 是 | **0** | 0.0% / 0.0% | **未形成** | 双方均存在但无任何共同 AST 指纹。目标项目（422 函数）与参考项目（290 函数）在内存抽象（如 `Arc`、`VmBytes` vs `kfree`、`memset`）上完全不同。 |
| **中断/异常处理** | 是 | **0** | 0.0% / 0.0% | **未形成** | 双方均存在但无共同指纹。目标项目（5 函数）与参考项目（28 函数）规模差异大，且与平台强相关，无同源性证据。 |
| **系统调用** | 是 | 1 | 0.39% / 4.55% | **弱** | 仅 1 个共同指纹，源于 POSIX 标准接口的必然相似性（如 `syscall` 包装函数），不构成特异性复用证据。 |
| **文件系统** | 是 | **0** | 0.0% / 0.0% | **未形成** | 双方均存在但无共同指纹。目标项目（1347 函数）规模远超参考项目（176 函数），文件系统实现路径完全不同。 |
| **设备驱动** | 是 | **0** | 0.0% / 0.0% | **未形成** | 双方均存在但无共同指纹。目标项目仅 1 个函数，参考项目 446 个函数，双方驱动模型和硬件平台完全不同。 |
| **网络协议栈** | 否 | 0 | 0.0% / 0.0% | **未形成** | 仅目标项目存在（2617 函数），参考项目无此模块，无比对基础。 |
| **文件系统/第三方库** | 是 | **390** | 62.3% / 89.45% | **强（第三方库）** | 共同指纹高度集中于此。双方均引用了 `lwext4` 开源文件系统库，AST 结构完全一致，属于合法公共依赖。 |
| **第三方库** | 是 | 1 | 0.05% / 100.0% | **强（第三方库）** | 共同指纹源于 `virtio-drivers` 开源项目，属于合法公共依赖。 |
| **通用模块/其他** | 是 | 1 | 0.57% / 0.54% | **弱** | 仅 1 个共同指纹，覆盖率极低，不构成系统性复用证据。 |

### 3. 底层 AST 关键证据指纹表
| 所属板块 | 目标项目路径 | 参考项目路径 | 证据类型 | 技术说明 |
| :--- | :--- | :--- | :--- | :--- |
| **文件系统/第三方库** | `third_party/rust-patches/lwext4_rust/c/lwext4/src/ext4_balloc.c#ext4_balloc_alloc_block` | `kernel/src/fs/ext4/lwext4/ext4_balloc.c#ext4_balloc_alloc_block` | 同源开源库 | 双方均完整引用了 `lwext4` 库，AST 调用图谱完全一致，包括 `ext4_balloc_get_bgid_of_block` 等 19 个相同外部调用。此为合法第三方代码复用。 |
| **文件系统/第三方库** | `third_party/rust-patches/lwext4_rust/c/lwext4/src/ext4_journal.c#jbd_journal_prepare` | `kernel/src/fs/ext4/lwext4/ext4_journal.c#jbd_journal_prepare` | 同源开源库 | 同上，AST 调用图谱完全一致，包括 `TAILQ_FOREACH_SAFE` 等 17 个相同外部调用。 |
| **文件系统/第三方库** | `third_party/rust-patches/lwext4_rust/c/lwext4/src/ext4.c#ext4_dir_rm` | `kernel/src/fs/ext4/lwext4/ext4.c#ext4_dir_rm` | 同源开源库 | 同上，AST 调用图谱完全一致，包括 `ext4_dir_iterator_init` 等 19 个相同外部调用。 |
| **文件系统/第三方库** | `third_party/rust-patches/lwext4_rust/c/lwext4/src/ext4_dir_idx.c#ext4_dir_dx_split_index` | `kernel/src/fs/ext4/lwext4/ext4_dir_idx.c#ext4_dir_dx_split_index` | 同源开源库 | 同上，AST 调用图谱完全一致，包括 `ext4_dir_dx_entry_get_hash` 等 10 个相同外部调用。 |
| **第三方库** | `third_party/rust-patches/virtio-drivers/src/transport/mmio.rs#vendor_id` | `kernel/dep/virtio-drivers/src/transport/mmio.rs#vendor_id` | 同源开源库 | 双方均引用了 `virtio-drivers` 库，`vendor_id` 函数均使用 `volread!` 宏，AST 结构一致。 |
| **任务调度/异步运行时** | `kernel/src/syscall/fs/ctl.rs#sys_renameat2` | `kernel/src/syscall/sysfile.c#sys_renameat2` | 同板块同名 | 双方均实现 POSIX `renameat2` 系统调用。目标项目调用 `Path::new`、`check_rename_permissions` 等 Rust 抽象；参考项目调用 `arg_str`、`do_renameat2` 等 C 函数。调用链完全不同，无结构复用。 |
| **系统调用** | `scripts/support-tools/glibc-compat.c#clock_gettime` | `user/deps/syscall.c#clock_gettime` | 同板块同名同结构 | 均为用户态 `syscall` 包装函数，AST 极简，属于 POSIX 标准接口的通用实现模式。 |

### 4. 核心代码调用流对比剖析
* **【进程管理/进程退出路径】**
* **底层技术析理**: 双方均实现了 `sys_exit` 和 `sys_exit_group` 系统调用，但底层调用流存在本质差异。目标项目（Rust）的 `sys_exit` 和 `sys_exit_group` 均直接调用 `do_exit`，其调用流为 `syscall -> Ok(do_exit())`，体现了 Rust 的 `Result` 类型系统。参考项目（C）的 `sys_exit` 调用 `arg_int, do_exit`，而 `sys_exit_group` 调用 `atomic_read, do_exit_group, proc_current`，其调用流为 `syscall -> arg_int -> do_exit` 和 `syscall -> atomic_read -> do_exit_group`。目标项目缺少参考项目中的 `arg_int` 参数解析和 `do_exit_group` 中间层，两者在函数分派拓扑上完全不同。此外，目标项目的 `do_exit` 涉及 Rust 的所有权系统和异步任务清理，参考项目的 `do_exit` 涉及 C 语言的裸指针和链表操作，内存管理模型截然不同。
* **代码溯源链接**:
  - 目标项目: `kernel/src/syscall/task/exit.rs#sys_exit`
  - 参考项目: `kernel/src/syscall/sysproc.c#sys_exit`

* **【任务调度/异步运行时 vs 传统调度器】**
* **底层技术析理**: 目标项目的调度核心基于 Rust 的 `async/await` 和 `Waker` 机制（见 `kernel/src/task/futex.rs` 中的 `Context::from_waker`, `Waker::noop`），任务挂起和唤醒通过 `block_on` 和 `Poll::Ready` 实现。参考项目的调度核心是传统的 `thread_scheduler` 和 `swtch` 汇编上下文切换（见 `kernel/src/proc/sched.c`），任务状态通过 `TCB_Q_changeState` 和 `Queue_push_back_atomic` 管理。两者在调度模型（协作式异步 vs 抢占式时间片）、同步原语（`Waker` vs `cond_wait`）、任务队列数据结构（Rust 迭代器 vs C 链表宏）上存在根本性架构差异。
* **代码溯源链接**:
  - 目标项目: `kernel/src/task/futex.rs#dropping_requeued_waiter_cleans_target_queue`
  - 参考项目: `kernel/src/proc/sched.c#thread_scheduler`

* **【内存管理/内存映射路径】**
* **底层技术析理**: 双方均实现了 `sys_mmap` 系统调用，但内存抽象层完全不同。目标项目使用 `Backend::new_alloc`、`Backend::new_cow`、`Backend::new_file` 等类型安全的枚举体来区分内存映射后端，并依赖 `Arc::new` 进行共享所有权管理。参考项目则直接使用 `arg_addr`、`arg_fd`、`arg_long` 等底层参数解析，并直接操作页表。目标项目的内存管理深度集成了 Rust 的所有权和生命周期检查，而参考项目是传统的 C 语言手动内存管理，两者在抽象层次和安全性模型上存在显著差异。
* **代码溯源链接**:
  - 目标项目: `kernel/src/syscall/mm/mmap.rs#sys_mmap`
  - 参考项目: `kernel/src/mm/mmap.c#sys_mmap`

### 5. 单方模块与降权项
*   **单方模块**:
    *   **网络协议栈/第三方库** (目标项目独有): 目标项目包含基于 `smoltcp` 的完整 TCP/IP 网络栈（2617 个函数），参考项目无此模块。这是目标项目独立开发的重要证据。
    *   **BPF 虚拟机** (目标项目独有): 目标项目在 `kernel/src/bpf/vm.rs` 中实现了完整的 eBPF 执行器，参考项目无对应实现。这属于目标项目的独立功能扩展。
*   **降权项**:
    *   **跨板块同名/同结构**: 控方及证据底座中出现的跨板块同名项（如目标项目 `fanotify.rs#notify` 与参考项目 `virtio-drivers#notify`）不构成同板块主证据，已全部降权。
    *   **POSIX 标准接口**: `sys_getpid`、`sys_getppid`、`clock_gettime` 等函数，因其实现受 POSIX 标准严格约束，AST 结构必然极简且相似，缺乏特异性，不能作为抄袭证据。
    *   **第三方库/公共依赖**: `lwext4` 和 `virtio-drivers` 中的共同指纹，已明确标定为合法引用的开源组件，不计入内核原创代码抄袭判定。

### 6. 保守结论
本庭裁定，目标项目 `[2026-T202610346999652-XX]` 与参考项目 `[2024-cabbageOS-华中科技大学]` 在底层架构上存在显著差异，定性为**独立开发**。客观 AST 相似度仅 9.07%，且高度集中在双方共同引用的 `lwext4` 等开源第三方库上。在控方重点指控的【进程管理】等核心内核板块，共同 AST 指纹为零，覆盖率均为 0.0%，不存在任何可量化的结构同源性。目标项目基于 Rust 语言，构建了包含异步运行时、eBPF 虚拟机、完整 TCP/IP 协议栈在内的现代操作系统内核，其内存模型、同步策略、抽象层次与参考项目的 C 语言传统实现有本质区别。控方指控的“换皮复用”在 AST 层面无法得到证实，整体抄袭风险极低。

------------------------------

