# 🛡️ OS 内核赛自动化代码级架构审计报告

**审计时间**: `2026-07-02 04:25:45`
**参赛作品名称**: `2026-T202610346999652-XX`
**年份**: `2026`
**学校**: `XX`
**队伍名称**: `T202610346999652`
**比赛名字**: `OS-Competition`
**比赛子赛道**: `Kernel-Implementation`
**仓库地址**: `https://gitlab.eduxiji.net/educg-group-43501-3132633/T202610346999652-1129`
**推测开发语言**: `Rust / C (由AST探针自动识别)`
**特征函数量**: `9083`

## ⚔️ 底层代码同源性对比 (AST Homology)
> 本部分由专门的代码审查智能体，对该项目与符合年份条件的历届最高相似度项目进行控制流与底层调用对比。新版报告会先给出确定性的全模块覆盖矩阵，再给出 Agent 的分模块解释，避免只围绕少量抽样函数展开。

### 🏆 Top 2 底层对标: vs `2025-AstranciA-哈尔滨工业大学深圳`（哈尔滨工业大学（深圳） / AstranciA）

#### 对方作品信息

**对方年份**: `2025`
**对方学校**: `哈尔滨工业大学（深圳）`
**对方队伍名称**: `AstranciA`
**对方比赛名字**: `OS-Competition`
**对方比赛子赛道**: `Kernel-Implementation`
**对方仓库地址**: `https://gitlab.eduxiji.net/educg-group-36002-2710490/T202518123995667-1436`

> 双向覆盖率：共同 AST 指纹: 959；唯一 AST 指纹: 目标项目 7428，参考项目 3750；覆盖率: 目标项目 12.91%，参考项目 25.57%

## 全模块对比总览（确定性证据）

> 总体覆盖率：共同 AST 指纹: 959；唯一 AST 指纹: 目标项目 7428，参考项目 3750；覆盖率: 目标项目 12.91%，参考项目 25.57%

### 1. 模块覆盖矩阵
| 模块 | 目标函数数 | 参考函数数 | 共同唯一 AST 指纹 | Target 覆盖 | Base 覆盖 | 状态 |
| :--- | ---: | ---: | ---: | ---: | ---: | :--- |
| 进程管理 | 430 | 112 | 2 | 0.56% | 2.17% | 双方存在且有结构重合 |
| 任务调度/异步运行时 | 682 | 455 | 6 | 0.96% | 1.87% | 双方存在且有结构重合 |
| 内存管理 | 422 | 343 | 8 | 2.0% | 2.6% | 双方存在且有结构重合 |
| 中断/异常处理 | 5 | 71 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |
| 系统调用 | 308 | 136 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |
| 文件系统 | 1347 | 620 | 4 | 0.34% | 0.82% | 双方存在且有结构重合 |
| 设备驱动 | 1 | 142 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |
| 网络协议栈 | 0 | 61 | 0 | 0.0% | 0.0% | 仅参考项目体现 |
| 文件系统/第三方库 | 735 | 775 | 412 | 65.81% | 64.58% | 双方存在且有结构重合 |
| 网络协议栈/第三方库 | 2617 | 217 | 84 | 4.16% | 43.52% | 双方存在且有结构重合 |
| 第三方库 | 2322 | 1155 | 227 | 11.58% | 24.38% | 双方存在且有结构重合 |
| 通用模块/其他 | 214 | 673 | 1 | 0.57% | 0.18% | 双方存在且有结构重合 |

### 2. 分模块证据清单

#### 进程管理
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `kernel/src/syscall/signal.rs#sys_rt_sigprocmask` | `AstrancE/ulib/axmono/src/syscall/signal.rs#sys_rt_sigprocmask` | 同板块同名 | Err, Ok, check_sigset_size, curr.as_thread, current, debug! ...(+7) | crate::task::signal::sys_sigprocmask |
| `kernel/src/task/ops.rs#get_process_group` | `AstrancE/ulib/axmono/src/task/mod.rs#get_process_group` | 同板块同名 | (group.pgid() == pgid).then_some, Ok, PROCESS_GROUP_TABLE.read, PROCESS_GROUP_TABLE.read().get, group.pgid, proc_data.proc.group ...(+5) | PROCESS_GROUP_TABLE
        .read, PROCESS_GROUP_TABLE
        .read()
        .get, PROCESS_GROUP_TABLE
        .read()
        .get(&pgid)
        .ok_or |
| `kernel/src/syscall/task/execve.rs#sys_execve` | `AstrancE/ulib/axmono/src/syscall/process.rs#sys_execve` | 同板块同名 | FS_CONTEXT.lock, FS_CONTEXT.lock().resolve, debug!, do_execve, load_exec_args_env, vm_load_string | Err, char_ptr_to_str, err.into, str_vec_ptr_to_str, str_vec_ptr_to_str(argv as *const *const c_char)?
        .into_iter, str_vec_ptr_to_str(argv as *const *const c_char)?
        .into_iter()
        .map ...(+6) |
| `kernel/src/file/pidfd.rs#process_data` | `AstrancE/ulib/axmono/src/task/mod.rs#process_data` | 同板块同名 | Err, self.proc_data.upgrade, self.proc_data.upgrade().ok_or, thread_exit.load | self.thread.process, self.thread.process().data, self.thread.process().data().unwrap |

#### 任务调度/异步运行时
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `kernel/src/syscall/signal.rs#sys_kill` | `AstrancE/ulib/axmono/src/task/signal.rs#sys_kill` | 同板块同名 | Err, Ok, Some, check_signal_permission, current, current().as_thread ...(+12) | Ok, SigInfo_::Generic, Signal::from_u32, current, current().task_ext, current().task_ext().thread.process ...(+8) |
| `kernel/src/syscall/task/clone3.rs#try_from` | `AstrancE/ulib/axmono/src/pthread/mod.rs#try_from` | 同板块同名 | CloneFlags::from_bits, CloneFlags::from_bits(args.flags).ok_or, Err, Ok, flags
                .contains, flags
                .contains(CloneFlags::INTO_CGROUP)
                .then_some ...(+2) | Self::try_from, Self::try_from((value as u32 & FUTEX_CMD_MASK as u32) as u8).map_err |
| `kernel/src/syscall/ipc/shm.rs#sys_shmdt` | `AstrancE/ulib/axmono/src/syscall/ipc.rs#sys_shmdt` | 同板块同名 | Ok, SHM_MANAGER.lock, VirtAddr::from, aspace.unmap, aspace_handle.lock, curr.as_thread ...(+15) | (shmaddr as usize).into, axmm_shm_dt, axmm_shm_dt((shmaddr as usize).into(), &mut aspace)
        .map, axmm_shm_dt((shmaddr as usize).into(), &mut aspace)
        .map(|_| 0 as isize) // Return 0 on success
        .map_err, curr.task_ext, curr.task_ext().process_data ...(+3) |
| `kernel/src/syscall/signal.rs#sys_rt_sigaction` | `AstrancE/ulib/axmono/src/syscall/signal.rs#sys_rt_sigaction` | 同板块同名 | Err, Ok, act.nullable, act.vm_read_uninit, act.vm_read_uninit()?.assume_init, actions[signo].clone ...(+11) | crate::task::signal::sys_sigaction, crate::task::signal::sys_sigaction(signum, act as _, oldact as _).map_err, panic! |

#### 内存管理
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `kernel/src/mm/access.rs#check_null_terminated` | `AstrancE/ulib/axmono/src/ptr.rs#check_null_terminated` | 同板块同名 | Err, Layout::new::<T>, Layout::new::<T>().align, Ok, T::default, access_user_memory ...(+11) | Err, Layout::new::<T>, Layout::new::<T>().align, Ok, T::default, VirtAddrRange::from_start_size ...(+12) |
| `kernel/src/mm/aspace/mod.rs#process_area_data` | `AstrancE/modules/axmm/src/aspace/mod.rs#process_area_data` | 同板块同名 | (size - cnt).min, (start + size).align_up_4k, Ok, PageIter4K::new, PageIter4K::new(start.align_down_4k(), end_align_up)
            .expect, ax_bail! ...(+8) | Self::process_area_data_with_page_table |
| `kernel/src/mm/aspace/backend/shared.rs#read_bytes` | `AstrancE/modules/axhal/src/platform/aarch64_common/pl011.rs#read_bytes` | 同板块同名 | (page_bytes - page_offset).min, Err, Ok, axhal::mem::phys_to_virt, axhal::mem::phys_to_virt(phys).as_usize, buf.as_mut_ptr ...(+8) | bytes.len, getchar |
| `kernel/src/mm/aspace/backend/shared.rs#write_bytes` | `AstrancE/modules/axhal/src/platform/aarch64_common/pl011.rs#write_bytes` | 同板块同名 | (page_bytes - page_offset).min, Err, Ok, axhal::mem::phys_to_virt, axhal::mem::phys_to_virt(phys).as_usize, buf.as_ptr ...(+7) | putchar |

#### 中断/异常处理
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `scripts/support-tools/oscomp-timeout.c#main` | `AstrancE/ulib/axsyscall/src/syscall_imp/errno.rs#try_from` | 同模块上下文对齐/差异观察 | WEXITSTATUS, WIFEXITED, WIFSIGNALED, WTERMSIG, _exit, execvp ...(+9) | Err, Ok |
| `kernel/src/pseudofs/dev/tty/terminal/termios.rs#default` | `AstrancE/modules/axhal/src/arch/loongarch64/unaligned.rs#emulate_load_store_insn` | 同模块上下文对齐/差异观察 | ctl | asm!, badv::read, badv::read().raw, core::slice::from_raw_parts_mut, panic!, read_fpr ...(+5) |
| `scripts/support-tools/sleep.c#main` | `AstrancE/ulib/axlibc/c/pow.c#exp_inline` | 同模块上下文对齐/差异观察 | fprintf, parse_seconds, perror, syscall | __math_oflow, __math_uflow, asdouble, asuint64, converttoint, eval_as_double ...(+4) |
| `kernel/src/file/signalfd.rs#from_signal_info` | `AstrancE/modules/axhal/src/arch/aarch64/trap.rs#handle_sync_exception` | 同模块上下文对齐/差异观察 | sig_info.code, sig_info.errno, sig_info.signo | ESR_EL1.extract, crate::trap::handle_syscall, crate::trap::post_trap_callback, debug!, esr.read, esr.read_as_enum ...(+8) |

#### 系统调用
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `kernel/src/syscall/sync/futex.rs#sys_futex` | `AstrancE/ulib/axmono/src/syscall/pthread.rs#sys_futex` | 同板块同名 | Arc::as_ptr, Arc::downgrade, Err, LinuxError::ENOSYS.into, Ok, RestartBlock::FutexWait ...(+17) | Duration::from, FutexFlags::from_bits_truncate, FutexOp::try_from, Some, VirtAddr::from, futex ...(+1) |
| `kernel/src/syscall/time.rs#sys_setitimer` | `AstrancE/ulib/axmono/src/syscall/time.rs#sys_setitimer` | 同板块同名 | ITimerType::from_repr, ITimerType::from_repr(which).ok_or, Ok, curr
        .as_thread, curr
        .as_thread()
        .time
        .borrow_mut, curr
        .as_thread()
        .time
        .borrow_mut()
        .set_itimer ...(+13) | Err, ItimerConfig::default, Ok, Some, new_value.is_null, old_value.is_null ...(+1) |
| `kernel/src/syscall/net/io.rs#sys_recvfrom` | `AstrancE/api/arceos_posix_api/src/imp/net.rs#sys_recvfrom` | 同板块同名 | Ok, Some, UserConstPtr::<socklen_t>::from, VmBytesMut::new, addr.is_null, addrlen_ptr.address ...(+7) | debug!, syscall_body! |
| `kernel/src/syscall/net/name.rs#sys_getsockname` | `AstrancE/api/arceos_posix_api/src/imp/net.rs#sys_getsockname` | 同板块同名 | NetlinkSocket::from_fd, Ok, Socket::from_fd, checked_socklen_mut, debug!, local_addr.write_to_user ...(+2) | debug!, syscall_body! |

#### 文件系统
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `kernel/src/syscall/fs/io.rs#sys_copy_file_range` | `AstrancE/api/arceos_posix_api/src/imp/fd_ops.rs#sys_copy_file_range` | 同板块同名 | AxError::from, Err, Ok, SendFile::Direct, SendFile::Offset, copy_file_range_offset ...(+21) | Err, Ok, SeekFrom::Current, SeekFrom::Start, core::cmp::min, debug! ...(+11) |
| `kernel/src/syscall/fs/io.rs#sys_pwrite64` | `AstrancE/api/arceos_posix_api/src/imp/fs.rs#sys_pwrite64` | 同板块同名 | Err, Ok, VmBytes::new, allowed_write_len, f.as_ref, f.inner ...(+15) | file.write_at, file.write_at(slice, offset as u64).map, get_file_like, get_file_like(fd).map_err, slice::from_raw_parts |
| `kernel/src/syscall/fs/ctl.rs#sys_utimensat` | `AstrancE/api/arceos_posix_api/src/imp/fs.rs#sys_utimensat` | 同板块同名 | Err, Ok, Some, atime.is_none, atime.transpose, mtime.is_none ...(+9) | CURRENT_DIR.lock, CURRENT_DIR.lock().clone, Directory::from_fd, Directory::from_fd(dirfd).map_err, Err, File::new ...(+19) |
| `kernel/src/pseudofs/cgroup.rs#read_dir` | `AstrancE/modules/axfs/src/fs/fatfs.rs#read_dir` | 同板块同名 | ".".into, "..".into, Cow::Owned, Ok, Vec::new, entries.extend ...(+14) | Ok, VfsDirEntry::new, dirents.iter_mut, dirents.iter_mut().enumerate, dirents.len, entry.file_name ...(+6) |

#### 设备驱动
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `scripts/support-tools/tar.c#is_zero_block` | `AstrancE/modules/axdriver/src/lib.rs#init_drivers` | 同模块上下文对齐/差异观察 | 内部闭环逻辑 | AllDevices::default, all_devs.block.iter_mut, all_devs.block.iter_mut().enumerate, all_devs.display.iter_mut, all_devs.display.iter_mut().enumerate, all_devs.net.iter_mut ...(+14) |

#### 网络协议栈
仅参考项目中观察到该模块代表函数，目标项目没有同模块可对齐证据：
- 参考项目：`AstrancE/ulib/axlibc/c/network.c#inet_pton`，调用摘要：hexval, inet_pton, isdigit, memmove
- 参考项目：`AstrancE/api/arceos_posix_api/src/imp/net.rs#shutdown`，调用摘要：Ok, tcpsocket.lock, tcpsocket.peer_addr, tcpsocket.shutdown, udpsocket.lock, udpsocket.peer_addr ...(+1)
- 参考项目：`AstrancE/ulib/axlibc/c/socket.c#accept4`，调用摘要：accept, fcntl
- 参考项目：`AstrancE/api/arceos_posix_api/src/imp/net.rs#recvfrom`，调用摘要：Ok, Some, tcpsocket.lock, tcpsocket.lock().recv, tcpsocket.lock().recv(buf).map, udpsocket
                .lock ...(+2)

#### 文件系统/第三方库
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `third_party/rust-patches/lwext4_rust/c/lwext4/src/ext4_balloc.c#ext4_balloc_alloc_block` | `AstrancE/crates/lwext4_rust/c/lwext4/src/ext4_balloc.c#ext4_balloc_alloc_block` | 同板块同名同结构 | ext4_balloc_get_bgid_of_block, ext4_balloc_get_block_of_bgid, ext4_balloc_set_bitmap_csum, ext4_balloc_verify_bitmap_csum, ext4_bg_get_block_bitmap, ext4_bg_get_free_blocks_count ...(+19) | ext4_balloc_get_bgid_of_block, ext4_balloc_get_block_of_bgid, ext4_balloc_set_bitmap_csum, ext4_balloc_verify_bitmap_csum, ext4_bg_get_block_bitmap, ext4_bg_get_free_blocks_count ...(+19) |
| `third_party/rust-patches/lwext4_rust/c/lwext4/src/ext4_journal.c#jbd_journal_prepare` | `AstrancE/crates/lwext4_rust/c/lwext4/src/ext4_journal.c#jbd_journal_prepare` | 同板块同名同结构 | EXT4_BLOCK_ZERO, RB_FIND, TAILQ_FOREACH_REVERSE_SAFE, TAILQ_FOREACH_SAFE, TAILQ_REMOVE, ext4_bcache_clear_dirty ...(+17) | EXT4_BLOCK_ZERO, RB_FIND, TAILQ_FOREACH_REVERSE_SAFE, TAILQ_FOREACH_SAFE, TAILQ_REMOVE, ext4_bcache_clear_dirty ...(+17) |
| `third_party/rust-patches/lwext4_rust/c/lwext4/src/ext4.c#ext4_dir_rm` | `AstrancE/crates/lwext4_rust/c/lwext4/src/ext4.c#ext4_dir_rm` | 同板块同名同结构 | EXT4_MP_LOCK, EXT4_MP_UNLOCK, ext4_block_cache_write_back, ext4_dir_en_get_inode, ext4_dir_iterator_fini, ext4_dir_iterator_init ...(+19) | EXT4_MP_LOCK, EXT4_MP_UNLOCK, ext4_block_cache_write_back, ext4_dir_en_get_inode, ext4_dir_iterator_fini, ext4_dir_iterator_init ...(+19) |
| `third_party/rust-patches/lwext4_rust/c/lwext4/src/ext4_dir_idx.c#ext4_dir_dx_split_index` | `AstrancE/crates/lwext4_rust/c/lwext4/src/ext4_dir_idx.c#ext4_dir_dx_split_index` | 同板块同名同结构 | ext4_block_set, ext4_dir_dx_climit_get_count, ext4_dir_dx_climit_get_limit, ext4_dir_dx_climit_set_count, ext4_dir_dx_climit_set_limit, ext4_dir_dx_entry_get_hash ...(+10) | ext4_block_set, ext4_dir_dx_climit_get_count, ext4_dir_dx_climit_get_limit, ext4_dir_dx_climit_set_count, ext4_dir_dx_climit_set_limit, ext4_dir_dx_entry_get_hash ...(+10) |

#### 网络协议栈/第三方库
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `third_party/rust-patches/virtio-drivers/src/device/socket/connectionmanager.rs#send_recv` | `AstrancE/crates/virtio-drivers/src/device/socket/connectionmanager.rs#send_recv` | 同板块同名 | "Hello from guest".as_bytes, (hello_from_guest.len() as u32).into, (hello_from_host.len() as u32).into, 0.into, 50.into, Arc::new ...(+43) | "Hello from guest".as_bytes, (hello_from_guest.len() as u32).into, (hello_from_host.len() as u32).into, 0.into, 50.into, Arc::new ...(+43) |
| `third_party/rust-patches/virtio-drivers/src/device/socket/connectionmanager.rs#incoming_connection` | `AstrancE/crates/virtio-drivers/src/device/socket/connectionmanager.rs#incoming_connection` | 同板块同名 | 0.into, 50.into, Arc::new, Default::default, Mutex::new, NonNull::from ...(+25) | 0.into, 50.into, Arc::new, Mutex::new, ReadOnly::new, SocketType::Stream.into ...(+24) |
| `crates/axnet-ng/src/tcp.rs#connect` | `AstrancE/modules/axnet/src/smoltcp_impl/tcp.rs#connect` | 同板块同名 | Err, IpEndpoint::from, Ipv4Addr::LOCALHOST.into, Ipv6Addr::LOCALHOST.into, Ok, Ok::<(), AxError> ...(+30) | Err, Ok, Ok::<(IpEndpoint, IpEndpoint), AxError>, SOCKET_SET
                .with_socket_mut::<tcp::Socket, _, _>, SOCKET_SET.add, SocketSetWrapper::new_tcp_socket ...(+26) |
| `third_party/rust-patches/starry-smoltcp/src/storage/assembler.rs#add` | `AstrancE/crates/virtio-drivers/src/device/socket/connectionmanager.rs#add` | 同板块同名 | Contig::hole_and_data, Err, Ok, contig.has_data, contig.shrink_hole_to, contig.total_size ...(+11) | bytes.get, bytes.len, bytes_after_wraparound.len, min, self.buffer.len, self.buffer[0..bytes_after_wraparound.len()].copy_from_slice ...(+2) |

#### 第三方库
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `third_party/rust-patches/virtio-drivers/src/queue.rs#fake_read_write_queue` | `AstrancE/crates/virtio-drivers/src/queue.rs#fake_read_write_queue` | 同板块同名 | (*available_ring).idx.load, (*used_ring).idx.fetch_add, (*used_ring).idx.load, Vec::new, assert!, assert_eq! ...(+18) | (*available_ring).idx.load, (*used_ring).idx.fetch_add, (*used_ring).idx.load, Vec::new, assert!, assert_eq! ...(+18) |
| `third_party/rust-patches/virtio-drivers/src/device/sound/fake.rs#handle_control_request` | `AstrancE/crates/virtio-drivers/src/device/sound/fake.rs#handle_control_request` | 同板块同名 | CommandCode::SOk.into, Some, Vec::new, VirtIOSndHdr {
                            command_code: CommandCode::SOk.into(),
                        }
                        .as_bytes, VirtIOSndHdr::read_from_prefix, VirtIOSndHdr::read_from_prefix(&request).expect ...(+16) | CommandCode::SOk.into, Some, Vec::new, VirtIOSndHdr {
                            command_code: CommandCode::SOk.into(),
                        }
                        .as_bytes, VirtIOSndHdr::read_from_prefix, VirtIOSndHdr::read_from_prefix(&request)
                .expect ...(+16) |
| `third_party/rust-patches/virtio-drivers/src/queue.rs#add_notify_wait_pop` | `AstrancE/crates/virtio-drivers/src/queue.rs#add_notify_wait_pop` | 同板块同名 | (*self.used.as_ptr()).idx.load, H::mmio_phys_to_virt, H::mmio_phys_to_virt(d0.addr as usize, d0.len as usize).as_ptr, core::slice::from_raw_parts, dma_sync_barrier, log::warn! ...(+18) | self.add, self.can_pop, self.pop_used, self.should_notify, spin_loop, transport.notify |
| `third_party/rust-patches/virtio-drivers/src/queue.rs#recycle_descriptors` | `AstrancE/crates/virtio-drivers/src/queue.rs#recycle_descriptors` | 同板块同名同结构 | Box::from_raw, H::unshare, InputOutputIter::new, InputOutputIter::new(inputs, outputs).enumerate, Some, assert_eq! ...(+17) | Box::from_raw, H::unshare, InputOutputIter::new, InputOutputIter::new(inputs, outputs).enumerate, Some, assert_eq! ...(+17) |

#### 通用模块/其他
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `kernel/src/bpf/helpers.rs#read_bytes` | `AstrancE/modules/axhal/src/platform/riscv64_qemu_virt/console.rs#read_bytes` | 同板块同名 | Err, Ok, Vec::new, allowed.contains, bytes.len, charge_aux_budget ...(+3) | VirtAddr::from_mut_ptr_of, bytes.as_mut_ptr, bytes.len, bytes.len().min, sbi_rt::Physical::new, sbi_rt::console_read ...(+2) |
| `kernel/src/bpf/helpers.rs#write_bytes` | `AstrancE/modules/axhal/src/platform/riscv64_qemu_virt/console.rs#write_bytes` | 同板块同名 | Err, Ok, allowed.contains, bytes.is_empty, self.write_ctx_bytes, self.write_map_value_bytes ...(+1) | bytes.as_ptr, bytes.len, bytes.to_vec, kernel_bytes.len, try_write_bytes |
| `kernel/src/bpf/helpers.rs#write_bytes` | `AstrancE/modules/axhal/src/platform/riscv64_visionfive2/console.rs#write_bytes` | 同板块同名 | bytes.len, charge_aux_budget, self
            .range_for, self
            .range_for(ptr, bytes.len())
            .ok_or, self.data.len, self.data[range].copy_from_slice ...(+2) | bytes.as_ptr, bytes.len, bytes.to_vec, kernel_bytes.len, try_write_bytes |
| `kernel/src/bpf/helpers.rs#read_bytes` | `AstrancE/modules/axhal/src/platform/riscv64_visionfive2/console.rs#read_bytes` | 同板块同名 | Some, self.data[range].to_vec, self.range_for | VirtAddr::from_mut_ptr_of, bytes.as_mut_ptr, bytes.len, bytes.len().min, sbi_rt::Physical::new, sbi_rt::console_read ...(+2) |


### 🛡️ 审计结论：最终同源性定性仲裁

#### 1. 宏观判定
- **定性结论**: **独立开发，底层架构存在显著差异**
- **核心依据**: 整体 AST 相似度仅为 **9.38%**。在排除第三方库/公共依赖后，核心内核模块（进程管理、内存管理、任务调度等）的共同 AST 指纹极少，覆盖率均低于 3%。双方在进程/线程对象模型、内存抽象、同步原语等核心架构层面展现出系统性差异，符合独立开发的典型特征。

#### 2. 全模块覆盖矩阵解读
| 模块 | 双方存在 | 共同指纹 | 目标覆盖 | 参考覆盖 | 证据强度 | 解读 |
| :--- | :---: | :---: | :---: | :---: | :---: | :--- |
| **进程管理** | 是 | 2 | 0.56% | 2.17% | **弱** | 重合度极低。共同指纹源于 Linux 标准语义（`clear_child_tid`）和 Rust 原子操作惯用语，非架构级复用。 |
| **任务调度/异步运行时** | 是 | 6 | 0.96% | 1.87% | **弱** | 重合度极低。共同指纹多为 POSIX 系统调用同名函数（如 `sys_kill`），但内部调用图谱和对象访问路径完全不同。 |
| **内存管理** | 是 | 8 | 2.0% | 2.6% | **弱** | 重合度低。共同指纹集中在通用辅助函数（如 `check_null_terminated`），其实现受限于 Rust 语言和硬件 MMU 的标准操作范式。 |
| **中断/异常处理** | 是 | 0 | 0.0% | 0.0% | **未形成** | 无共同 AST 指纹。双方在此底层架构上无任何可观测的结构重合。 |
| **系统调用** | 是 | 0 | 0.0% | 0.0% | **未形成** | 无共同 AST 指纹。尽管存在大量同名 POSIX 系统调用，但底层实现路径和调用图谱完全不同。 |
| **文件系统** | 是 | 4 | 0.34% | 0.82% | **弱** | 重合度极低。共同指纹为同名系统调用入口（如 `sys_copy_file_range`），内部实现逻辑差异显著。 |
| **设备驱动** | 是 | 0 | 0.0% | 0.0% | **未形成** | 无共同 AST 指纹。目标项目仅1个函数，参考项目142个，规模与实现完全不同。 |
| **网络协议栈** | 仅参考 | 0 | 0.0% | 0.0% | **未形成** | 目标项目未体现自研网络协议栈，参考项目有61个函数，无可比性。 |
| **文件系统/第三方库** | 是 | 412 | 65.81% | 64.58% | **强（第三方库）** | 高覆盖率源于双方共同依赖的 `lwext4_rust` 上游 C/Rust 绑定库，属于合法公共依赖，非项目间抄袭。 |
| **网络协议栈/第三方库** | 是 | 84 | 4.16% | 43.52% | **中（第三方库）** | 共同指纹源于 `virtio-drivers`、`smoltcp` 等共同上游依赖。目标项目覆盖率低是因为其自研了 `axnet-ng` 等新网络组件。 |
| **第三方库** | 是 | 227 | 11.58% | 24.38% | **中（第三方库）** | 共同指纹源于 `virtio-drivers` 等公共依赖。双方均对这些上游库进行了各自的修补（patches），但核心逻辑同源。 |
| **通用模块/其他** | 是 | 1 | 0.57% | 0.18% | **弱** | 重合度极低。共同指纹为跨板块的 `read/write_bytes` 同名函数，但分别服务于 BPF 和串口驱动，上下文完全不同。 |

#### 3. 底层 AST 关键证据指纹表
| 所属板块 | 目标项目路径 | 参考项目路径 | 证据类型 | 技术说明 |
| :--- | :--- | :--- | :--- | :--- |
| **进程管理** | `kernel/src/task/thread.rs#set_clear_child_tid` | `.../axmono/src/task/process.rs#set_clear_child_tid` | 同板块同名，底层逻辑一致 | 均调用 `self.clear_child_tid.store`。这是 Linux `set_tid_address` 系统调用的标准语义，且对 `AtomicUsize` 类型进行存储的唯一 Rust 惯用方法。双方将其置于不同对象（`thread.rs` vs `process.rs`）体现了架构差异。 |
| **进程管理** | `kernel/src/task/thread.rs#clear_child_tid` | `.../axmono/src/task/process.rs#clear_child_tid` | 同板块同名，底层逻辑一致 | 同上，均调用 `self.clear_child_tid.load`。是上述 `store` 操作的对应读取，成对出现由 Linux ABI 决定，非代码复用。 |
| **进程管理** | `kernel/src/syscall/signal.rs#sys_rt_sigprocmask` | `.../axmono/src/syscall/signal.rs#sys_rt_sigprocmask` | 同板块同名 | 目标项目独立实现了 `check_sigset_size`、`curr.as_thread` 等调用；参考项目直接转发到 `task::signal::sys_sigprocmask`。同名源于 POSIX 标准。 |
| **任务调度** | `kernel/src/syscall/signal.rs#sys_kill` | `.../axmono/src/task/signal.rs#sys_kill` | 同板块同名 | 目标项目调用 `check_signal_permission` 进行权限检查；参考项目直接构造 `SigInfo_::Generic` 并发送。实现路径不同。 |
| **内存管理** | `kernel/src/mm/access.rs#check_null_terminated` | `.../axmono/src/ptr.rs#check_null_terminated` | 同板块同名 | 均使用 `Layout::new::<T>().align()` 进行内存对齐检查。这是处理空终止字符串的通用且高效的方法，属于 Rust 语言下的标准实践。 |
| **文件系统** | `kernel/src/syscall/fs/io.rs#sys_copy_file_range` | `.../arceos_posix_api/src/imp/fd_ops.rs#sys_copy_file_range` | 同板块同名 | 目标项目使用 `SendFile::Direct/Offset` 枚举分发逻辑；参考项目使用 `SeekFrom::Current/Start` 和 `core::cmp::min` 手动计算。实现模式不同。 |
| **第三方库** | `third_party/.../ext4_balloc.c#ext4_balloc_alloc_block` | `crates/.../ext4_balloc.c#ext4_balloc_alloc_block` | 同板块同名同结构 | 完全一致。源于共同依赖的上游 `lwext4_rust` 库，属于公共第三方代码。 |
| **第三方库** | `third_party/.../connectionmanager.rs#send_recv` | `crates/.../connectionmanager.rs#send_recv` | 同板块同名同结构 | 完全一致。源于共同依赖的上游 `virtio-drivers` 库，属于公共第三方代码。 |

#### 4. 核心代码调用流对比剖析
* **【进程管理/进程对象模型】**
* **底层技术析理**: 两者在获取当前进程 PID 的调用流上展现了根本性的对象模型差异。目标项目采用 `axtask::current().as_thread().proc_data.proc.pid` 路径，将任务、线程、进程数据、进程对象层层分离，体现了更细粒度的职责划分。参考项目则采用 `current().task_ext().thread.process().pid()` 路径，通过任务扩展（task_ext）来桥接线程与进程。这两种访问路径的抽象层次和对象聚合方式完全不同，是内核架构设计上的根本性分歧，排除了基于同一套代码进行修改的可能性。
* **代码溯源链接**:
  - 目标项目: `kernel/src/syscall/task/thread.rs#sys_getpid`
  - 参考项目: `AstrancE/ulib/axmono/src/syscall/process.rs#sys_getppid`

* **【内存管理/用户态内存检查】**
* **底层技术析理**: 在 `check_null_terminated` 函数中，两者都使用了 `Layout::new::<T>().align()` 来计算类型 T 的对齐要求，这是 Rust 语言处理内存布局的标准方式。然而，在后续的内存访问校验上，目标项目调用自定义的 `access_user_memory` 函数，而参考项目则构造 `VirtAddrRange::from_start_size` 对象进行检查。这反映了双方在内存虚拟化抽象上的不同封装策略：一个采用过程式校验，一个采用对象式范围管理。
* **代码溯源链接**:
  - 目标项目: `kernel/src/mm/access.rs#check_null_terminated`
  - 参考项目: `AstrancE/ulib/axmono/src/ptr.rs#check_null_terminated`

* **【任务调度/信号处理】**
* **底层技术析理**: 在 `sys_kill` 系统调用中，目标项目在发送信号前显式调用了 `check_signal_permission` 进行权限检查，遵循了严格的 Linux 安全模型。参考项目则没有此独立步骤，直接构造 `SigInfo_::Generic` 并调用底层的 `send_signal` 逻辑。这种在系统调用入口层的实现差异，表明双方对 POSIX 标准的实现侧重点和内部 API 划分不同。
* **代码溯源链接**:
  - 目标项目: `kernel/src/syscall/signal.rs#sys_kill`
  - 参考项目: `AstrancE/ulib/axmono/src/task/signal.rs#sys_kill`

#### 5. 单方模块与降权项
*   **单方模块**:
    *   **网络协议栈 (自研)**: 参考项目拥有 61 个自研网络协议栈函数，而目标项目此项为 0。目标项目的网络功能完全由第三方库或自研的 `axnet-ng` 组件承担，未体现在此模块分类中。这是双方技术选型和架构的巨大差异。
    *   **设备驱动**: 目标项目仅 1 个函数，参考项目有 142 个函数。目标项目可能将驱动实现在内核之外或采用了完全不同的驱动模型。
*   **降权项 (第三方库/公共依赖)**:
    *   **`lwext4_rust` (文件系统/第三方库)**: 贡献了 412 个共同指纹，是覆盖率最高的部分。双方均使用了该开源库来提供 EXT4 文件系统支持，属于正常的开源生态复用。
    *   **`virtio-drivers` (第三方库)**: 贡献了大量共同指纹。双方均基于或参考了该开源项目来实现 VirtIO 驱动，属于共同上游依赖。
    *   **`smoltcp` / `starry-smoltcp` (网络协议栈/第三方库)**: 双方均使用了该开源网络协议栈，导致该板块出现共同指纹。
    *   **POSIX 同名系统调用**: 如 `sys_getpid`、`sys_exit`、`sys_execve` 等，其函数名由 POSIX 标准强制规定，不能作为同源性证据。本次审计已严格审查其内部调用流，确认实现各异。

#### 6. 保守结论
本次审计基于严格的 AST 调用图谱比对，整体相似度仅 9.38%。控方提出的“复用铁证”均能被解释为对 Linux/POSIX 标准语义的实现或 Rust 语言标准库惯用法的使用，且双方在核心架构（如进程/线程对象模型、内存访问抽象）上存在系统性差异。高达 75% 的共同指纹源于 `lwext4_rust`、`virtio-drivers` 等合法第三方公共依赖，与内核原创性无关。综合来看，目标项目 `[2026-T202610346999652-XX]` 在核心内核模块上展现了独立的设计与实现，不存在对参考项目 `[2025-AstranciA-哈尔滨工业大学深圳]` 的代码抄袭或结构性复用。整体学术不端风险极低。

------------------------------

