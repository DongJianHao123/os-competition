# 🛡️ OS 内核赛自动化代码级架构审计报告

**审计时间**: `2026-07-02 02:26:16`
**参赛作品名称**: `2026-T2026100069910698-XX`
**年份**: `2026`
**学校**: `XX`
**队伍名称**: `T2026100069910698`
**比赛名字**: `OS-Competition`
**比赛子赛道**: `Kernel-Implementation`
**仓库地址**: `https://gitlab.eduxiji.net/educg-group-43501-3132633/T2026100069910698-3954`
**推测开发语言**: `Rust / C (由AST探针自动识别)`
**特征函数量**: `527`

## ⚔️ 底层代码同源性对比 (AST Homology)
> 本部分由专门的代码审查智能体，对该项目与符合年份条件的历届最高相似度项目进行控制流与底层调用对比。新版报告会先给出确定性的全模块覆盖矩阵，再给出 Agent 的分模块解释，避免只围绕少量抽样函数展开。

### 🏆 Top 3 底层对标: vs `2021-是谁在呼叫舰队-西北工业大学`（西北工业大学 / 是谁在呼叫舰队）

#### 对方作品信息

**对方年份**: `2021`
**对方学校**: `西北工业大学`
**对方队伍名称**: `是谁在呼叫舰队`
**对方比赛名字**: `OS-Competition`
**对方比赛子赛道**: `Kernel-Implementation`
**对方仓库地址**: `https://gitlab.eduxiji.net/zhanghr/os`

> 双向覆盖率：共同 AST 指纹: 11；唯一 AST 指纹: 目标项目 448，参考项目 626；覆盖率: 目标项目 2.46%，参考项目 1.76%

## 全模块对比总览（确定性证据）

> 总体覆盖率：共同 AST 指纹: 11；唯一 AST 指纹: 目标项目 448，参考项目 626；覆盖率: 目标项目 2.46%，参考项目 1.76%

### 1. 模块覆盖矩阵
| 模块 | 目标函数数 | 参考函数数 | 共同唯一 AST 指纹 | Target 覆盖 | Base 覆盖 | 状态 |
| :--- | ---: | ---: | ---: | ---: | ---: | :--- |
| 进程管理 | 26 | 48 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |
| 任务调度/异步运行时 | 132 | 55 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |
| 内存管理 | 95 | 173 | 2 | 2.22% | 1.26% | 双方存在且有结构重合 |
| 中断/异常处理 | 3 | 27 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |
| 系统调用 | 76 | 18 | 2 | 3.28% | 12.5% | 双方存在且有结构重合 |
| 文件系统 | 75 | 305 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |
| 设备驱动 | 38 | 46 | 2 | 6.9% | 5.41% | 双方存在且有结构重合 |
| 网络协议栈 | 0 | 0 | 0 | 0.0% | 0.0% | 双方均未体现 |
| 文件系统/第三方库 | 35 | 11 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |
| 第三方库 | 0 | 86 | 0 | 0.0% | 0.0% | 仅参考项目体现 |
| 通用模块/其他 | 47 | 41 | 2 | 4.65% | 5.0% | 双方存在且有结构重合 |

### 2. 分模块证据清单

#### 进程管理
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `src/user/init.c#exec_external_capture` | `kern/process/proc.c#do_wait` | 同模块上下文对齐/差异观察 | copy_cstr, prepare_exec_args, split_words, syscall1, syscall2, syscall3 ...(+2) | do_exit, find_proc, kfree, local_intr_restore, local_intr_save, panic ...(+5) |
| `src/kernel/syscall.c#sys_epoll_pwait` | `kern/process/proc.c#do_mmap` | 同模块上下文对齐/差异观察 | current_task, epoll_get, fd_is_valid, fd_poll_revents, read_time, task_request_resched ...(+2) | ROUNDDOWN, ROUNDUP, copy_from_user, get_unmapped_area, lock_mm, mm_map ...(+2) |
| `src/kernel/syscall.c#sys_rt_sigtimedwait` | `kern/process/proc.c#get_pid` | 同模块上下文对齐/差异观察 | current_task, memset, read_time, task_consume_sigchld, task_request_resched, task_sleep_until ...(+2) | le2proc, list_next, static_assert |
| `src/kernel/task.c#task_wait` | `kern/sync/monitor.c#cond_wait` | 同模块上下文对齐/差异观察 | current_task, memset, task_request_resched, task_wait_status_value, task_write_user_int | cprintf, down, up |

#### 任务调度/异步运行时
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `src/kernel/syscall.c#sys_read` | `kern/driver/sysctl.c#sysctl_clock_get_freq` | 同模块上下文对齐/差异观察 | current_task, eventfd_get, fd_after_io, fd_before_io, fs_read, memcpy ...(+12) | sysctl_clock_get_clock_select, sysctl_clock_get_freq, sysctl_clock_get_threshold, sysctl_clock_source_get_freq |
| `src/kernel/syscall.c#sys_statx` | `kern/fs/ccsbcs.c#ff_wtoupper` | 同模块上下文对齐/差异观察 | copy_user_cstr, current_task, fill_kstat_from_fs, fs_stat, fs_stat_path, is_dev_null_path ...(+3) | 内部闭环逻辑 |
| `src/kernel/syscall.c#sys_utimensat` | `kern/driver/sysctl.c#sysctl_clock_device_end_en` | 同模块上下文对齐/差异观察 | copy_user_cstr, current_task, fs_set_times_id, fs_set_times_path, fs_stat, fs_stat_path ...(+3) | 内部闭环逻辑 |
| `src/kernel/syscall.c#sys_write` | `libs/printfmt.c#vprintfmt` | 同模块上下文对齐/差异观察 | current_task, eventfd_get, fd_after_io, fd_before_io, fd_is_valid, fs_write ...(+10) | getint, getuint, printfmt, printnum, putch, strnlen ...(+1) |

#### 内存管理
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `src/kernel/string.c#memmove` | `libs/string.c#memmove` | 同板块同名 | 内部闭环逻辑 | __memmove |
| `src/user/init.c#run_command` | `kern/mm/vmm.c#vma_destroy` | 同板块同结构 | run_command_status | kfree |
| `src/kernel/vm.c#vm_kernel_pagetable` | `kern/mm/swap_fifo.c#_fifo_set_unswappable` | 同板块同结构 | 内部闭环逻辑 | 内部闭环逻辑 |
| `src/kernel/vm.c#vm_free_page_count` | `kern/syscall/syscall.c#sys_pgdir` | 同板块同结构 | 内部闭环逻辑 | 内部闭环逻辑 |

#### 中断/异常处理
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `src/arch/riscv64/trap.c#trap_enable_timer` | `kern/trap/trap.c#print_regs` | 同模块上下文对齐/差异观察 | read_sie, read_sstatus, timer_set_next, write_sie, write_sstatus | cprintf |
| `src/arch/riscv64/trap.c#trap_init` | `kern/syscall/syscall.c#syscall` | 同模块上下文对齐/差异观察 | printk, write_stvec | cprintf, panic, syscalls[num] |
| `src/arch/riscv64/trap.c#timer_set_next` | `tools/vector.c#main` | 同模块上下文对齐/差异观察 | riscv_read_time, sbi_set_timer | printf |

#### 系统调用
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `src/kernel/syscall.c#sys_uname` | `kern/syscall/syscall.c#sys_uname` | 同板块同名 | copy_cstr, user_range_ok | snprintf |
| `src/kernel/syscall.c#sys_times` | `kern/syscall/syscall.c#sys_times` | 同板块同名 | read_time, ticks_to_ms, user_range_ok | 内部闭环逻辑 |
| `src/kernel/syscall.c#sys_clock_nanosleep` | `kern/syscall/syscall.c#sys_yield` | 同板块同结构 | sys_nanosleep | do_yield |
| `src/kernel/syscall.c#sys_path_permission_success` | `kern/syscall/syscall.c#sys_io_setup` | 同板块同结构 | 内部闭环逻辑 | 内部闭环逻辑 |

#### 文件系统
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `src/user/init.c#run_command_status` | `kern/fs/ff.c#create_name` | 同模块上下文对齐/差异观察 | exec_external, is_script_path, print, read_all, run_busybox_echo, run_echo ...(+15) | (BYTE), IsDBCS1, IsDBCS2, IsLower, IsUpper, chk_chr ...(+3) |
| `src/user/init.c#interpret_script_text` | `kern/fs/ff.c#f_lseek` | 同模块上下文对齐/差异观察 | copy_cstr, exec_external, print, run_busybox_cmd_file, run_command, starts_with ...(+2) | (DWORD), (FSIZE_t), ABORT, LEAVE_FF, SS, clmt_clust ...(+6) |
| `src/kernel/elf.c#elf_interp_is_optional` | `kern/fs/ff.c#f_rename` | 同模块上下文对齐/差异观察 | (usize), elf_is_valid | FREE_NAMBUF, INIT_NAMBUF, LEAVE_FF, chk_lock, clust2sect, dir_register ...(+11) |
| `src/kernel/fs.c#memfs_compact_pool` | `kern/fs/ff.c#f_read` | 同模块上下文对齐/差异观察 | memcpy | (UINT), ABORT, LEAVE_FF, SS, clmt_clust, clust2sect ...(+6) |

#### 设备驱动
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `src/arch/loongarch64/minikernel.c#puts` | `kern/driver/sdcard.c#sd_get_R3_rest_response` | 同板块同结构 | uart_puts | sd_read_data |
| `src/arch/loongarch64/virtio_pci_blk.c#block_available` | `kern/driver/ide.c#ide_wait_ready` | 同板块同结构 | 内部闭环逻辑 | 内部闭环逻辑 |
| `src/drivers/virtio_net.c#net_init` | `kern/driver/sdcard.c#sd_test` | 同模块上下文对齐/差异观察 | net_read, net_write, printk, queue_setup, rx_refill_one | assert, cprintf, memcmp, memcpy, memset, sd_read_sector ...(+2) |
| `src/arch/loongarch64/virtio_pci_blk.c#block_init` | `kern/driver/sdcard.c#sd_get_cidregister` | 同模块上下文对齐/差异观察 | (u32), align_up, find_legacy_blk, io_read16, io_read8, io_write16 ...(+6) | cprintf, sd_end_cmd, sd_get_R1_response, sd_read_data, sd_send_cmd |

#### 文件系统/第三方库
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `src/kernel/fs.c#preload_ext4_root_files` | `user/libs/dir.c#opendir` | 同模块上下文对齐/差异观察 | ext4_available, ext4_getdents, ext4_read_all, memfs_alias_file, memfs_create_path_raw, memfs_fill_file ...(+3) | S_ISDIR, fstat, open |
| `src/kernel/ext4.c#ext4_init` | `user/libs/dir.c#readdir` | 同模块上下文对齐/差异观察 | block_available, ext4_getdents, file_cache_insert, memset, printk, rd16 ...(+2) | sys_getdirentry |
| `src/kernel/ext4.c#extent_lookup_rec` | `user/libs/syscall.c#sys_getdirentry` | 同模块上下文对齐/差异观察 | (u16), extent_lookup_rec, rd16, rd32, read_block | syscall |
| `src/kernel/ext4.c#ext4_getdents` | `user/libs/syscall.c#sys_mkdir` | 同模块上下文对齐/差异观察 | copy_name, inode_read_at, rd16, rd32 | syscall |

#### 第三方库
仅参考项目中观察到该模块代表函数，目标项目没有同模块可对齐证据：
- 参考项目：`user/libs/syscall.c#syscall`，调用摘要：va_arg, va_end, va_start
- 参考项目：`kern/libs/readline.c#readline`，调用摘要：cprintf, cputchar, getchar
- 参考项目：`user/libs/syscall.c#sys_mmap`，调用摘要：syscall
- 参考项目：`user/libs/umain.c#umain`，调用摘要：exit, initfd, main, warn

#### 通用模块/其他
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `src/kernel/string.c#memcpy` | `libs/string.c#memcpy` | 同板块同名 | 内部闭环逻辑 | __memcpy |
| `src/user/init.c#strcmp` | `libs/string.c#strcmp` | 同板块同名 | 内部闭环逻辑 | __strcmp |
| `src/kernel/string.c#memset` | `libs/string.c#memset` | 同板块同名 | 内部闭环逻辑 | __memset |
| `src/user/init.c#strlen` | `libs/string.c#strlen` | 同板块同名 | 内部闭环逻辑 | 内部闭环逻辑 |


好的，我将遵循审计指令，基于客观数据与底层证据，进行最终同源性定性仲裁。

### 1. 宏观判定
- **定性结论**: **独立开发** (相似度: 1.03%，共同 AST 指纹: 11；唯一 AST 指纹: 目标项目 448，参考项目 626；覆盖率: 目标项目 2.46%，参考项目 1.76%)
- **核心依据**: 整体 AST 相似度处于极低水平。在控辩双方争议焦点的【进程管理】与【任务调度/异步运行时】两大核心板块，**共同 AST 指纹为 0，覆盖率为 0%**。该客观数据从根本上否定了“重度代码洗稿抄袭”的指控。双方代码在架构设计、外部对象模型、系统调用语义上存在根本性差异，少量重合点集中于标准 C 库函数与 POSIX 同名系统调用，属于公共知识领域与接口规范约束下的正常现象。

### 2. 全模块覆盖矩阵解读
- **进程管理**: 双方均存在。共同 AST 指纹为 0，覆盖率为 0%。证据强度：**未形成**。控方指控的函数对（如 `sys_wait4` vs `do_wait`）在 AST 层面无任何共同指纹，调用图谱完全不同，属于通用操作系统语义下的独立实现。
- **任务调度/异步运行时**: 双方均存在。共同 AST 指纹为 0，覆盖率为 0%。证据强度：**未形成**。目标项目实现的是 Linux 兼容的 epoll/futex 异步模型，参考项目实现的是 RISC-V 时钟驱动与 FatFs 文件系统格式化，两者处于完全不同的子领域，无任何结构重合。
- **内存管理**: 双方均存在。共同 AST 指纹 2，目标覆盖率 2.22%，参考覆盖率 1.26%。证据强度：**弱**。重合点仅为 `memmove` 等标准 C 库函数，属于公共算法实现，不构成项目间逻辑复用证据。
- **中断/异常处理**: 双方均存在。共同 AST 指纹为 0，覆盖率为 0%。证据强度：**未形成**。目标项目基于 RISC-V `stvec`/`sie` 寄存器操作，参考项目为 `print_regs` 等调试辅助，无结构重合。
- **系统调用**: 双方均存在。共同 AST 指纹 2，目标覆盖率 3.28%，参考覆盖率 12.5%。证据强度：**弱**。重合点为 `sys_uname`、`sys_times` 等 POSIX 同名系统调用，其内部实现（`copy_cstr` vs `snprintf`）完全不同，同名源于接口规范。
- **文件系统**: 双方均存在。共同 AST 指纹为 0，覆盖率为 0%。证据强度：**未形成**。目标项目实现 ext4/memfs，参考项目实现 FatFs，两者在数据结构、磁盘布局、调用链上无任何重合。
- **设备驱动**: 双方均存在。共同 AST 指纹 2，目标覆盖率 6.9%，参考覆盖率 5.41%。证据强度：**弱**。重合点为 `puts`/`sd_get_R3_rest_response` 等，属于跨板块同 hash 的弱信号，且调用上下文完全不同，不构成同板块主证据。
- **网络协议栈**: 双方均未体现。证据强度：**未形成**。
- **第三方库/公共依赖**: 参考项目单独存在 86 个函数，目标项目无此独立模块。双方在 `string.c` 中的 `memcpy`、`memset`、`strcmp`、`strlen` 等函数存在同名/同结构指纹。证据强度：**弱**。这些均为标准 C 库函数，属于公共领域代码，不能作为抄袭证据。

### 3. 底层 AST 关键证据指纹表
| 所属板块 | 目标项目路径 | 参考项目路径 | 证据类型 | 技术说明 |
| :--- | :--- | :--- | :--- | :--- |
| 通用模块/其他 | `[src/kernel/string.c]` `memcpy` | `[libs/string.c]` `memcpy` | 同板块同名 | 标准 C 库函数，公共算法实现。目标项目为内部闭环逻辑，参考项目调用 `__memcpy`。 |
| 通用模块/其他 | `[src/kernel/string.c]` `memset` | `[libs/string.c]` `memset` | 同板块同名 | 标准 C 库函数，公共算法实现。目标项目为内部闭环逻辑，参考项目调用 `__memset`。 |
| 通用模块/其他 | `[src/user/init.c]` `strcmp` | `[libs/string.c]` `strcmp` | 同板块同名 | 标准 C 库函数，公共算法实现。目标项目为内部闭环逻辑，参考项目调用 `__strcmp`。 |
| 通用模块/其他 | `[src/user/init.c]` `strlen` | `[libs/string.c]` `strlen` | 同板块同名 | 标准 C 库函数，公共算法实现。双方均为内部闭环逻辑。 |
| 内存管理 | `[src/kernel/string.c]` `memmove` | `[libs/string.c]` `memmove` | 同板块同名 | 标准 C 库函数，公共算法实现。目标项目为内部闭环逻辑，参考项目调用 `__memmove`。 |
| 系统调用 | `[src/kernel/syscall.c]` `sys_uname` | `[kern/syscall/syscall.c]` `sys_uname` | 同板块同名 | POSIX 标准系统调用。目标项目调用 `copy_cstr`, `user_range_ok`；参考项目调用 `snprintf`。实现完全不同。 |
| 系统调用 | `[src/kernel/syscall.c]` `sys_times` | `[kern/syscall/syscall.c]` `sys_times` | 同板块同名 | POSIX 标准系统调用。目标项目调用 `read_time`, `ticks_to_ms`；参考项目为内部闭环逻辑。实现完全不同。 |
| 设备驱动 | `[src/arch/loongarch64/minikernel.c]` `puts` | `[kern/driver/sdcard.c]` `sd_get_R3_rest_response` | 跨板块同结构 | 弱信号。目标项目为 LoongArch 串口输出，参考项目为 SD 卡响应读取，功能与上下文完全不同。 |
| 设备驱动 | `[src/arch/loongarch64/virtio_pci_blk.c]` `block_available` | `[kern/driver/ide.c]` `ide_wait_ready` | 跨板块同结构 | 弱信号。目标项目为 virtio-blk 设备状态检查，参考项目为 IDE 硬盘就绪等待，硬件模型完全不同。 |

### 4. 核心代码调用流对比剖析
* **【进程管理/进程等待与退出路径】**
* **底层技术析理**: 目标项目构建于以 `current_task` 为核心的现代任务调度抽象之上，通过 `task_request_resched`、`task_sleep_until` 实现阻塞与唤醒，并通过 `user_range_ok`、`task_write_user_int` 严格处理用户态地址空间的安全性。其退出路径统一收敛于 `task_finish`。参考项目则基于教学内核的 `local_intr_save`/`restore` 中断控制原语、`wait_queue_add` 等待队列以及 `le2proc`、`list_del` 等手动链表管理。两者在**同步策略**（任务调度器 vs. 中断屏蔽+等待队列）、**内存模型**（用户态安全拷贝 vs. 内核态直接操作）和**对象管理模型**（`current_task` 抽象 vs. 全局进程链表）上存在根本性异构。
* **代码溯源链接**:
  - 目标项目: `src/kernel/syscall.c#sys_wait4` -> `src/kernel/task.c#task_wait`
  - 参考项目: `kern/process/proc.c#do_wait` -> `kern/sync/wait.c#wait_current_set`

* **【任务调度/异步运行时/异步I/O调度路径】**
* **底层技术析理**: 目标项目实现了完整的 Linux 兼容异步 I/O 模型，`sys_read`/`sys_write` 调用链涉及 `fd_before_io`/`fd_after_io`（文件描述符级 I/O 钩子）、`eventfd_get`、`pipe_get`、`fs_read`/`fs_write` 等多态文件系统抽象。参考项目的 `vprintfmt` 是格式化字符串打印函数，`sysctl_clock_*` 是 RISC-V 时钟控制接口，`f_printf` 是 FatFs 文件系统内部格式化输出。两者在**抽象层次**（完整多态 VFS vs. 具体硬件/文件系统实现）、**对象模型**（`fd` 抽象 vs. 具体设备寄存器/FAT表操作）上完全不同，不存在可比性。
* **代码溯源链接**:
  - 目标项目: `src/kernel/syscall.c#sys_read` -> `src/kernel/syscall.c#sys_write`
  - 参考项目: `libs/printfmt.c#vprintfmt` -> `kern/driver/sysctl.c#sysctl_clock_get_freq`

### 5. 单方模块与降权项
- **单方模块**: 参考项目中的【第三方库】模块（86个函数，如 `user/libs/syscall.c` 中的 `syscall`、`sys_mmap` 等）在目标项目中无对应独立模块。这体现了双方在用户态库组织方式上的差异，目标项目可能将其功能内聚到内核或用户初始化程序中。
- **降权项**:
  - **标准 C 库函数**: `memcpy`、`memset`、`memmove`、`strcmp`、`strlen` 等函数，虽存在同名/同结构指纹，但属于公共领域算法实现，**不能作为同源证据**。
  - **POSIX 同名系统调用**: `sys_uname`、`sys_times` 等，同名源于遵循 POSIX 标准，其内部实现与调用图谱完全不同，**不能作为复用证据**。
  - **跨板块同 hash**: 如设备驱动板块的 `puts` 与 `sd_get_R3_rest_response`，功能、上下文、硬件模型均不同，属于 AST 指纹的偶然碰撞，**不构成同板块主证据**。
  - **通用编程范式**: 控方提及的“前后包裹”模式（`fd_before_io`/`fd_after_io` vs `local_intr_save`/`local_intr_restore`）是操作系统开发中通用的资源获取-释放范式，属于思想层面，**不受著作权保护**。

### 6. 保守结论
本次审计基于 1.03% 的极低 AST 相似度与核心板块零重合的客观事实，判定目标项目 `[2026-T2026100069910698-XX]` 相对于参考项目 `[2021-是谁在呼叫舰队-西北工业大学]` 为**独立开发**。控方提出的“重度代码洗稿抄袭”指控，在【进程管理】与【任务调度/异步运行时】两大核心板块缺乏任何 AST 指纹支撑，其论证过程存在功能模块错位、跨板块硬凑证据、混淆通用范式与具体表达等根本性缺陷。双方代码在架构设计、同步策略、内存模型、系统调用语义上展现出显著且根本的差异。少量重合点均可归因于标准 C 库、POSIX 接口规范等公共知识领域，不构成原创性争议。整体代码同源性风险极低。

------------------------------

