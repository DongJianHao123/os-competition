# 🛡️ OS 内核赛自动化代码级架构审计报告

**审计时间**: `2026-07-02 14:08:13`
**参赛作品名称**: `2026-T202610699999765-XX`
**年份**: `2026`
**学校**: `XX`
**队伍名称**: `T202610699999765`
**比赛名字**: `OS-Competition`
**比赛子赛道**: `Kernel-Implementation`
**仓库地址**: `https://gitlab.eduxiji.net/educg-group-43501-3132633/T202610699999765-256`
**推测开发语言**: `Rust / C (由AST探针自动识别)`
**特征函数量**: `6363`

## ⚔️ 底层代码同源性对比 (AST Homology)
> 本部分由专门的代码审查智能体，对该项目与符合年份条件的历届最高相似度项目进行控制流与底层调用对比。新版报告会先给出确定性的全模块覆盖矩阵，再给出 Agent 的分模块解释，避免只围绕少量抽样函数展开。

### 🏆 Top 4 底层对标: vs `2025-KernalCraft-中山大学`（中山大学 / KernalCraft）

#### 对方作品信息

**对方年份**: `2025`
**对方学校**: `中山大学`
**对方队伍名称**: `KernalCraft`
**对方比赛名字**: `OS-Competition`
**对方比赛子赛道**: `Kernel-Implementation`
**对方仓库地址**: `https://gitlab.eduxiji.net/educg-group-36002-2710490/T202510558995240-2536`

> 双向覆盖率：共同 AST 指纹: 870；唯一 AST 指纹: 目标项目 3627，参考项目 1853；覆盖率: 目标项目 23.99%，参考项目 46.95%

## 全模块对比总览（确定性证据）

> 总体覆盖率：共同 AST 指纹: 870；唯一 AST 指纹: 目标项目 3627，参考项目 1853；覆盖率: 目标项目 23.99%，参考项目 46.95%

### 1. 模块覆盖矩阵
| 模块 | 目标函数数 | 参考函数数 | 共同唯一 AST 指纹 | Target 覆盖 | Base 覆盖 | 状态 |
| :--- | ---: | ---: | ---: | ---: | ---: | :--- |
| 进程管理 | 651 | 71 | 6 | 1.49% | 9.52% | 双方存在且有结构重合 |
| 任务调度/异步运行时 | 442 | 115 | 7 | 1.77% | 6.25% | 双方存在且有结构重合 |
| 内存管理 | 1058 | 741 | 155 | 22.02% | 34.6% | 双方存在且有结构重合 |
| 中断/异常处理 | 101 | 128 | 22 | 27.85% | 22.0% | 双方存在且有结构重合 |
| 系统调用 | 342 | 21 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |
| 文件系统 | 1079 | 147 | 10 | 1.28% | 7.81% | 双方存在且有结构重合 |
| 设备驱动 | 79 | 555 | 2 | 2.53% | 0.43% | 双方存在且有结构重合 |
| 网络协议栈 | 106 | 53 | 1 | 1.41% | 2.33% | 双方存在且有结构重合 |
| 文件系统/第三方库 | 404 | 340 | 112 | 35.44% | 42.75% | 双方存在且有结构重合 |
| 网络协议栈/第三方库 | 138 | 0 | 0 | 0.0% | 0.0% | 仅目标项目体现 |
| 第三方库 | 1263 | 2 | 2 | 0.27% | 100.0% | 双方存在且有结构重合 |
| 通用模块/其他 | 700 | 669 | 85 | 29.72% | 32.08% | 双方存在且有结构重合 |

### 2. 分模块证据清单

#### 进程管理
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `os/src/syscall/process.rs#sys_wait4` | `os/src/syscall/process.rs#sys_wait4` | 同板块同名 | WaitOption::from_bits, WaitTarget::from_waitpid_arg, _ru.is_null, block_current_and_run_next, child.acquire_inner_lock, child.getpid ...(+41) | current_process, current_process().getpid, log::trace!, sys_waitpid, sys_yield |
| `os/src/task/processor.rs#schedule` | `os/src/task/processor.rs#schedule` | 同板块同名 | PROCESSORS[cpu_id].lock, PROCESSORS[cpu_id].lock().get_idle_task_cx_ptr, __switch, current_cpu_id, disable_interrupts, panic! | PROCESSOR.exclusive_session, __switch, processor.get_idle_task_cx_ptr |
| `os/src/syscall/process.rs#sys_uname` | `os/src/syscall/process.rs#sys_uname` | 同板块同名 | UTSName::zeroed, UTS_STATE.lock, copy_to_user, copy_to_user(token, &uts, buf as *mut UTSName).is_err, current_task, current_task().unwrap ...(+5) | "#1 PREEMPT Thu May 29 00:00:00 UTC".as_bytes, "0.1.0".as_bytes, "KernelCraft".as_bytes, "None".as_bytes, "localhost".as_bytes, "riscv64".as_bytes ...(+14) |
| `os/src/syscall/process.rs#sys_times` | `os/src/syscall/process.rs#sys_times` | 同板块同名 | copy_to_user, copy_to_user(token, &times, buf).is_err, current_task, current_task().unwrap, get_time_ms, get_time_ms().saturating_mul ...(+4) | curr_process.inner_exclusive_access, current_process, current_user_token, get_time_ms, process_inner.get_cstime, process_inner.get_cutime ...(+3) |

#### 任务调度/异步运行时
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `os/src/syscall/fs.rs#sys_sendfile` | `os/src/syscall/fs.rs#sys_sendfile` | 同板块同名 | Ok, Some, Vec::<u8>::with_capacity, buffer.as_mut_slice, buffer.as_slice, buffer.set_len ...(+24) | (count - total_written).min, Inode::new, UserBuffer::new, curr_process.inner_exclusive_access, current_process, drop ...(+12) |
| `os/src/syscall/fs.rs#sys_renameat2` | `os/src/syscall/fs.rs#sys_renameat2` | 同板块同名 | FileDescriptor::exchange, FileDescriptor::rename, current_task, current_task().unwrap, debug!, fd_table.get_ref ...(+18) | current_process, current_user_token, file.getid, inner.fd_table.len, inner.fd_table[fd].as_ref, inner.fd_table[fd].as_ref().unwrap ...(+6) |
| `os/src/syscall/fs.rs#sys_getdents64` | `os/src/syscall/fs.rs#sys_getdents64` | 同板块同名 | (dirent.d_ino as u64).to_ne_bytes, (dirent.d_off as i64).to_ne_bytes, (dirp as usize).checked_add, (dirp as usize).checked_add(count).is_none, (reclen as u16).to_ne_bytes, Vec::new ...(+34) | UserBuffer::new, current_process, current_user_token, drop, file.clone, file.get_inner_refmut ...(+7) |
| `os/src/syscall/fs.rs#sys_utimensat` | `os/src/syscall/fs.rs#sys_utimensat` | 同板块同名 | Some, UtimensatFlags::from_bits, __openat, apply_ltp_fd_stat_overrides, apply_ltp_stat_overrides, atime.is_some ...(+31) | (*atime_ptr).to_ms, (*mtime_ptr).to_ms, Some, TimeSpec::now, TimeSpec::now().to_ms, current_process ...(+15) |

#### 内存管理
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `os/src/hal/arch/loongarch64/trap/mod.rs#trap_handler` | `virtio-drivers/examples/x86_64/src/trap.rs#trap_handler` | 同板块同名 | (0..sz).rev, BadV::read, BadV::read().get_vaddr, ERA::read, ERA::read().next_ins, ERA::read().next_ins().write ...(+61) | panic!, x86_64::registers::control::Cr2::read |
| `os/src/mm/memory_set.rs#mmap` | `os/src/mm/memory_set.rs#mmap` | 同板块同名 | MapArea::new, OpenFlags::O_RDWR.bits, OpenFlags::O_WRONLY.bits, Some, VirtAddr::from, area.expand_to::<T> ...(+33) | MapArea::new, log::error!, log::trace!, mmap_manager.mmap_alloc, self.mmap_manager.as_mut, self.push |
| `os/src/mm/memory_set.rs#map_elf` | `os/src/mm/memory_set.rs#map_elf` | 同板块同名 | ((ph.virtual_addr() + ph.mem_size()) as usize + bias).into, (VirtAddr::from(elf.input.as_ptr() as usize + (ph.offset() as usize)))
                                .floor, (ph.virtual_addr() as usize + bias).into, Err, KERNEL_SPACE
                        .lock, KERNEL_SPACE
                        .lock()
                        .remove_area_with_start_vpn ...(+44) | ((ph.virtual_addr() + ph.mem_size()) as usize + offset.0).into, (ph.virtual_addr() as usize + offset.0).into, MapArea::new, Some, VirtPageNum, assert_eq! ...(+17) |
| `os/src/fs/poll.rs#pselect` | `os/src/syscall/ppoll.rs#pselect` | 同板块同名 | ((current_task().unwrap().trap_cx_user_va() + crate::config::PAGE_SIZE)
        as *mut Signals)
        .wrapping_sub, SigMaskHow::SIG_SETMASK.bits, Some, crate::timer::TimeSpec::now, current_task, current_task().unwrap ...(+24) | FdSet::empty, Some, crate::timer::TimeSpec::now, curr_process.inner_exclusive_access, current_process, drop ...(+11) |

#### 中断/异常处理
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `os/src/hal/arch/loongarch64/register/base/estat.rs#cause` | `riscv/src/register/mcause.rs#cause` | 同板块同名 | Exception::try_from, Interrupt::try_from, MErrCtl::read, MErrCtl::read().is_merr, TLBRERA::read, TLBRERA::read().is_tlbr ...(+9) | Exception::from, Interrupt::from, Trap::Exception, Trap::Interrupt, self.code, self.is_interrupt |
| `os/path-deps/riscv/src/interrupt.rs#free` | `riscv/src/interrupt.rs#free` | 同板块同名同结构 | CriticalSection::new, disable, enable, f, mstatus.mie, mstatus::read | CriticalSection::new, disable, enable, f, mstatus.mie, mstatus::read |
| `os/path-deps/riscv/src/register/mcause.rs#is_interrupt` | `riscv/src/register/mcause.rs#is_interrupt` | 同板块同名同结构 | 内部闭环逻辑 | 内部闭环逻辑 |
| `os/src/hal/arch/loongarch64/trap/mod.rs#enable_timer_interrupt` | `os/src/hal/instruction/loongarch.rs#enable_timer_interrupt` | 同板块同名 | ECfg::empty, ECfg::empty()
        .set_line_based_interrupt_vector, ECfg::empty()
        .set_line_based_interrupt_vector(LineBasedInterrupt::TIMER)
        .write, TCfg::read, TCfg::read()
        .set_enable, TCfg::read()
        .set_enable(true)
        .set_periodic ...(+3) | crmd::set_ie, ecfg::set_lie, get_timer_freq, log::info!, tcfg::set_en, tcfg::set_init_val ...(+2) |

#### 系统调用
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `os/src/syscall/net.rs#sys_getsockopt` | `os/src/syscall/net.rs#sys_socket` | 同模块上下文对齐/差异观察 | TimeVal::new, copy_sockopt_bytes, copy_sockopt_u32, core::mem::size_of::<TimeVal>, core::mem::size_of::<UCred>, core::slice::from_raw_parts ...(+8) | create_tcp_socket, create_udp_socket |
| `os/src/net/tcp.rs#_read` | `os/src/syscall/input.rs#sys_event_get` | 同模块上下文对齐/差异观察 | Err, GeneralRet::Err, GeneralRet::Ok, NET_INTERFACE.poll, NET_INTERFACE.tcp_socket, Ok ...(+6) | KEYBOARD_DEVICE.clone, MOUSE_DEVICE.clone, kb.is_empty, kb.read_event, mouse.is_empty, mouse.read_event |
| `os/path-deps/rustsbi/src/ecall.rs#handle_ecall` | `os/src/syscall/info.rs#fmt` | 同模块上下文对齐/差异观察 | SbiRet::not_supported, base::handle_ecall_base, hsm::handle_ecall_hsm, ipi::handle_ecall_ipi, legacy::console_getchar, legacy::console_getchar().legacy_return ...(+15) | writeln! |
| `os/src/net/udp.rs#_read` | `os/src/syscall/info.rs#sys_sysinfo` | 同模块上下文对齐/差异观察 | Err, GeneralRet::Err, GeneralRet::Ok, NET_INTERFACE.poll, NET_INTERFACE.udp_socket, Ok ...(+15) | Sysinfo::collect, current_user_token, translated_refmut |

#### 文件系统
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `os/src/fs/dev/block.rs#ioctl` | `os/src/fs/mod.rs#ioctl` | 同板块同名 | copy_from_user, copy_to_user, current_user_token, self.state.lock | 内部闭环逻辑 |
| `os/path-deps/riscv/descriptor/generator.rs#generate` | `riscv/descriptor/generator.rs#generate` | 同板块同名同结构 | String::new, bf.generate_enum, format!, self.bfs.iter, write!, write!(&mut bit_sets, "{}", bf.generate_bitops()).unwrap ...(+2) | String::new, bf.generate_enum, format!, self.bfs.iter, write!, write!(&mut bit_sets, "{}", bf.generate_bitops()).unwrap ...(+2) |
| `os/src/fs/directory_tree.rs#mkdir` | `os/src/fs/inode.rs#mkdir` | 同板块同名 | (*last_comp).to_string, Arc::downgrade, Err, Ok, Self::new, Self::parse_dir_path ...(+13) | EXT4.ext4_dir_mk |
| `os/src/syscall/fs.rs#sys_mount` | `os/src/syscall/fs.rs#sys_mount` | 同板块同名 | MountFlags::from_bits, String::new, crate::fs::dev::block::loop_device_is_read_only, current_user_token, debug!, filesystemtype.is_null ...(+8) | 内部闭环逻辑 |

#### 设备驱动
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `os/path-deps/isomorphic_drivers/src/net/ethernet/intel/e1000.rs#receive` | `virtio-drivers/src/device/console.rs#receive` | 同板块同名 | Some, buffer.to_vec, recv_desc.status.get_bit, recv_desc.status.set_bit, self.recv_queue.len, self.registers[E1000_RDT].read ...(+5) | Arc::new, Mutex::new, ReadOnly::new, State::new, VirtIOConsole::<FakeHal, FakeTransport<Config>>::new, VirtIOConsole::<FakeHal, FakeTransport<Config>>::new(transport).unwrap ...(+7) |
| `os/path-deps/isomorphic_drivers/src/net/ethernet/intel/e1000.rs#send` | `virtio-drivers/src/device/console.rs#send` | 同板块同名 | assert!, buffer.len, fence, self.registers[E1000_TDT].read, self.registers[E1000_TDT].write, self.send_queue.len ...(+2) | Arc::new, Mutex::new, ReadOnly::new, State::new, State::wait_until_queue_notified, VirtIOConsole::<FakeHal, FakeTransport<Config>>::new ...(+12) |
| `os/path-deps/isomorphic_drivers/src/block/ahci.rs#write_block` | `virtio-drivers-old/src/blk.rs#write_block` | 同板块同名 | CommandHeaderFlags::WRITE.bits, buf.len, buf.len().min, fis.set_lba, self.data[0..len].clone_from_slice, self.port.issue_command ...(+1) | BlkResp::default, Err, Ok, req.as_buf, resp.as_buf_mut, self.header.notify ...(+4) |
| `os/path-deps/isomorphic_drivers/src/net/ethernet/intel/ixgbe.rs#recv` | `virtio-drivers/src/device/socket/connectionmanager.rs#recv` | 同板块同名 | Some, buffer.to_vec, ixgbe[IXGBE_RDT].read, ixgbe[IXGBE_RDT].write, recv_desc.status_error.get_bit, recv_desc.status_error.set_bit ...(+1) | Ok, connection.buffer.drain, connection.buffer.is_empty, connection.info.done_forwarding, get_connection, self.connections.swap_remove ...(+1) |

#### 网络协议栈
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `os/src/net/udp.rs#readable` | `os/src/net/port_table.rs#readable` | 同板块同名 | NET_INTERFACE.poll, NET_INTERFACE.udp_socket, has_loopback_packet, self.loacl_endpoint, socket.can_recv | 内部闭环逻辑 |
| `os/src/net/config.rs#add_socket` | `os/src/net/socket.rs#add_socket` | 同板块同名 | self._add_socket | SOCKET_TABLE.exclusive_access, Some, VecDeque::new, get_socket, get_socket(raddr, lport, rport).is_some, socket_table.len ...(+2) |
| `os/src/net/unix.rs#hang_up` | `os/src/net/port_table.rs#hang_up` | 同板块同名 | self.read_end.lock, self.read_end.lock().hang_up, self.write_end.lock, self.write_end.lock().hang_up | todo! |
| `os/src/net/unix.rs#writable` | `os/src/net/port_table.rs#writable` | 同板块同名 | self.write_end.lock, self.write_end.lock().writable | 内部闭环逻辑 |

#### 文件系统/第三方库
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `os/src/fs/ext4/extent.rs#ext_remove_leaf` | `ext4_rs/src/ext4_impls/extents.rs#ext_remove_leaf` | 同板块同名 | Block::load_inode_root_block, Block::load_offset, Ext4Extent::default, Ok, chunk.try_into, chunk.try_into().unwrap ...(+25) | Block::load, Block::load_inode_root_block, Ext4Extent::default, Ok, ex.get_actual_len, ex.get_pblock ...(+14) |
| `os/src/fs/ext4/extent.rs#extent_remove_space` | `ext4_rs/src/ext4_impls/extents.rs#extent_remove_space` | 同板块同名 | Block::load_offset, Ext4Extent::default, Ok, ex.get_actual_len, ex.get_first_block, ex.get_pblock ...(+14) | Block::load, Ext4Extent::default, Ok, ex.get_actual_len, ex.get_first_block, ex.get_pblock ...(+15) |
| `os/src/fs/ext4/balloc.rs#balloc_alloc_block_from` | `ext4_rs/src/ext4_impls/balloc.rs#balloc_alloc_block_from` | 同板块同名 | Block::load_offset, Err, Ext4BlockGroup::load_new, Ok, block_group.get_block_bitmap_block, block_group.get_free_blocks_count ...(+12) | Block::load, Ext4BlockGroup::load_new, Ok, block_group.get_block_bitmap_block, block_group.get_free_blocks_count, block_group.set_block_group_balloc_bitmap_csum ...(+15) |
| `os/src/fs/ext4/balloc.rs#balloc_alloc_block` | `ext4_rs/src/ext4_impls/balloc.rs#balloc_alloc_block` | 同板块同名 | Block::load_offset, Err, Ext4BlockGroup::load_new, block_group.get_block_bitmap_block, block_group.get_free_blocks_count, ext4_bmap_bit_find_clr ...(+12) | Block::load, Ext4BlockGroup::load_new, Ok, block_group.get_block_bitmap_block, block_group.get_free_blocks_count, block_group.set_block_group_balloc_bitmap_csum ...(+16) |

#### 网络协议栈/第三方库
仅目标项目中观察到该模块代表函数，参考项目没有同模块可对齐证据：
- 目标项目：`dependency/dep_iso/src/net/ethernet/intel/ixgbe.rs#new`，调用摘要：(IXGBE_FCRTH..IXGBE_FCRTH_END).step_by, (IXGBE_FCRTL..IXGBE_FCRTL_END).step_by, (IXGBE_FCTTV..IXGBE_FCTTV_END).step_by, EthernetAddress::from_bytes, MaybeUninit::uninit, MaybeUninit::uninit().assume_init ...(+56)
- 目标项目：`dependency/virtio-drivers/src/device/socket/connectionmanager.rs#send_recv`，调用摘要："Hello from guest".as_bytes, (hello_from_guest.len() as u32).into, (hello_from_host.len() as u32).into, 0.into, 50.into, Arc::new ...(+43)
- 目标项目：`dependency/virtio-drivers/src/device/socket/connectionmanager.rs#incoming_connection`，调用摘要：0.into, 50.into, Arc::new, Mutex::new, ReadOnly::new, SocketType::Stream.into ...(+24)
- 目标项目：`dependency/dep_iso/src/net/ethernet/intel/e1000.rs#new`，调用摘要：P::alloc_dma, Vec::with_capacity, assert_eq!, debug!, e1000[E1000_ICR].read, e1000[E1000_ICR].write ...(+25)

#### 第三方库
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `dependency/virtio-drivers/src/transport/mmio.rs#vendor_id` | `virtio-drivers/src/transport/mmio.rs#vendor_id` | 同板块同名同结构 | field_shared!, field_shared!(self.header, vendor_id).read | field_shared!, field_shared!(self.header, vendor_id).read |
| `dependency/virtio-drivers/src/header.rs#vendor_id` | `virtio-drivers-old/src/header.rs#vendor_id` | 同板块同名同结构 | self.vendor_id.read | self.vendor_id.read |

#### 通用模块/其他
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `os/path-deps/rustsbi/src/util.rs#set` | `os/src/utils.rs#set` | 同板块同名 | (*self.thin_ptr.get()).is_null, Box::from_raw, Box::into_raw, Err, Ok, asm! ...(+4) | panic! |
| `user/src/lib.rs#_start` | `os/src/hal/boot/loongarch.rs#_start` | 同板块同名 | _parameter, asm!, core::arch::asm!, unreachable! | core::arch::naked_asm! |
| `os/path-deps/riscv/descriptor/generator.rs#generate_enum` | `riscv/descriptor/generator.rs#generate_enum` | 同板块同名同结构 | String::new, self.enumerations.iter, write!, write!(
            &mut ret,
            "#[derive(Copy, Clone, Debug)]
#[repr(usize)]
"
        )
        .unwrap, write!(
            &mut ret,
            "}}
impl {}{{
    fn from(x: usize)->Self{{
        match x{{
{}            _ => unreachable!()
        }}
    }}
}}
",
            name, branches
        )
        .unwrap, write!(&mut branches, "            {} => Self::{},\n", e.1, e.0).unwrap ...(+2) | String::new, self.enumerations.iter, write!, write!(
            &mut ret,
            "#[derive(Copy, Clone, Debug)]
#[repr(usize)]
"
        )
        .unwrap, write!(
            &mut ret,
            "}}
impl {}{{
    fn from(x: usize)->Self{{
        match x{{
{}            _ => unreachable!()
        }}
    }}
}}
",
            name, branches
        )
        .unwrap, write!(&mut branches, "            {} => Self::{},\n", e.1, e.0).unwrap ...(+2) |
| `os/path-deps/rustsbi/src/util.rs#lock` | `os/src/sync/mutex.rs#lock` | 同板块同名 | asm!, self.data.get, self.lock.get | drop, self.locked.exclusive_access, suspend_current_and_run_next |


### 审计报告：最终同源性定性仲裁

#### 1. 宏观判定
- **定性结论**: **独立开发** (相似度: 18.87%，共同 AST 指纹: 870；唯一 AST 指纹: 目标项目 3627，参考项目 1853；覆盖率: 目标项目 23.99%，参考项目 46.95%)
- **核心依据**: 双方整体 AST 相似度处于低位。在指控最集中的“进程管理”板块，目标项目 651 个函数中仅有 6 个唯一 AST 指纹与参考项目重合，覆盖率仅 1.49%。所谓的“铁证”均为第三方库代码、Rust 语言标准库惯用法或 POSIX 标准接口的必然趋同，不构成项目间代码复用的证据。

#### 2. 全模块覆盖矩阵解读
- **进程管理**: 双方均存在。共同唯一 AST 指纹 6 个，目标覆盖率 1.49%，参考覆盖率 9.52%。极低的覆盖率表明双方在此核心模块的实现上存在本质差异。证据强度：**弱**。
- **任务调度/异步运行时**: 双方均存在。共同唯一 AST 指纹 7 个，目标覆盖率 1.77%，参考覆盖率 6.25%。低覆盖率指向不同的调度器实现策略。证据强度：**弱**。
- **内存管理**: 双方均存在。共同唯一 AST 指纹 155 个，目标覆盖率 22.02%，参考覆盖率 34.6%。覆盖率相对较高，但证据多集中在 `ext4` 文件系统实现和 ELF 加载等通用复杂算法上，属于对同一公开规范（EXT4, ELF）的实现趋同。证据强度：**中**。
- **中断/异常处理**: 双方均存在。共同唯一 AST 指纹 22 个，目标覆盖率 27.85%，参考覆盖率 22.0%。相似指纹主要源于对 RISC-V 特权架构规范的标准实现（如 `mcause` 寄存器解析）和共享的 `riscv` 第三方库。证据强度：**弱**。
- **系统调用**: 双方均存在。共同唯一 AST 指纹 0 个。尽管存在大量同名 POSIX 系统调用，但 AST 层面未发现任何唯一指纹重合，证明实现路径完全独立。证据强度：**未形成**。
- **文件系统**: 双方均存在。共同唯一 AST 指纹 10 个，目标覆盖率 1.28%，参考覆盖率 7.81%。低覆盖率表明双方在虚拟文件系统（VFS）或具体文件系统实现上架构不同。证据强度：**弱**。
- **设备驱动**: 双方均存在。共同唯一 AST 指纹 2 个，目标覆盖率 2.53%，参考覆盖率 0.43%。极低覆盖率，且相似点出现在对硬件标准接口（如 virtio）的驱动实现上。证据强度：**弱**。
- **网络协议栈**: 双方均存在。共同唯一 AST 指纹 1 个，目标覆盖率 1.41%，参考覆盖率 2.33%。极低覆盖率，表明双方网络栈实现独立。证据强度：**弱**。
- **第三方库/公共依赖**: 双方均存在。共同唯一 AST 指纹 114 个（文件系统/第三方库 112 + 第三方库 2），覆盖率较高（目标 35.44%，参考 42.75%）。这些指纹几乎全部来自 `ext4_rs`、`riscv`、`virtio-drivers` 等明确的开源第三方库，属于共享上游依赖，不能作为项目间抄袭的证据。证据强度：**强（但属于公共依赖，排除出原创性审查）**。

#### 3. 底层 AST 关键证据指纹表
| 所属板块 | 目标项目路径 | 参考项目路径 | 证据类型 | 技术说明 |
| :--- | :--- | :--- | :--- | :--- |
| 第三方库/公共依赖 | `os/path-deps/riscv/src/register/mimpid.rs#read` | `riscv/src/register/mimpid.rs#read` | 共享上游库 | 两者均为 `rust-embedded/riscv` 开源库的 vendored 副本，非项目原创逻辑。 |
| 第三方库/公共依赖 | `os/path-deps/riscv/src/register/mimpid.rs#bits` | `riscv/src/register/mimpid.rs#bits` | 共享上游库 | 同上，属于对 RISC-V 标准 CSR 的标准 Rust 封装。 |
| 进程管理 | `os/src/task/processor.rs#take_current` | `os/src/task/processor.rs#take_current` | 语言惯用法 | `self.current.take()` 是 Rust `Option` 类型的标准方法，无创造性。 |
| 进程管理 | `os/src/task/processor.rs#current` | `os/src/task/processor.rs#current` | 语言惯用法 | `self.current.as_ref().map(...)` 是 Rust `Option` 类型的标准方法链。 |
| 进程管理 | `os/src/task/processor.rs#get_idle_task_cx_ptr` | `os/src/task/processor.rs#get_idle_task_cx_ptr` | 通用 Getter | 返回空闲任务上下文指针的内部逻辑极其简单，AST 必然趋同。 |
| 进程管理 | `os/src/syscall/process.rs#sys_get_time` | `os/src/syscall/process.rs#sys_get_time` | POSIX 标准接口 | 对 `get_time_ms` 的调用是教育内核与 POSIX 接口的通用实现范式。 |
| 内存管理 | `os/src/fs/ext4/extent.rs#ext_remove_leaf` | `ext4_rs/src/ext4_impls/extents.rs#ext_remove_leaf` | 共享第三方库 | 两者均依赖 `ext4_rs` 库，该库是对 EXT4 文件系统规范的实现。 |
| 内存管理 | `os/src/fs/ext4/balloc.rs#balloc_alloc_block` | `ext4_rs/src/ext4_impls/balloc.rs#balloc_alloc_block` | 共享第三方库 | 同上，属于对 EXT4 块分配算法的标准实现。 |
| 中断/异常处理 | `os/path-deps/riscv/src/interrupt.rs#free` | `riscv/src/interrupt.rs#free` | 共享上游库 | 来自 `riscv` 库的中断临界区管理模板代码。 |
| 文件系统 | `os/path-deps/riscv/descriptor/generator.rs#generate` | `riscv/descriptor/generator.rs#generate` | 共享上游库 | 来自 `riscv` 库的代码生成器，用于处理 CSR 描述符。 |

#### 4. 核心代码调用流对比剖析
* **【进程管理/调度器架构】**
* **底层技术析理**: 这是双方架构差异最显著的部分。目标项目采用 **SMP 多核架构**，通过 `PROCESSORS[cpu_id].lock()` 访问 per-CPU 结构，并使用 `disable_interrupts`/`restore_interrupts` 关中断来保护临界区。参考项目采用 **单核全局调度器**，通过 `PROCESSOR.exclusive_access` 或 `exclusive_session` 进行基于协作式/独占访问的调度。两者在并发模型、CPU 抽象和同步策略上存在根本性分歧，不可能通过简单复制代码实现。
* **代码溯源链接**:
  - 目标项目: `os/src/task/processor.rs#schedule`
  - 参考项目: `os/src/task/processor.rs#schedule`

* **【进程管理/进程等待 (sys_wait4)】**
* **底层技术析理**: 目标项目实现了复杂的等待目标抽象 (`WaitTarget::from_waitpid_arg`)、等待选项位标志 (`WaitOption::from_bits`) 和子进程资源使用统计 (`child.snapshot_inclusive_rusage`)。参考项目的实现则相对简单，直接调用 `sys_waitpid` 并主动让出 CPU (`sys_yield`)。两者在对象模型和功能完备性上差异巨大。
* **代码溯源链接**:
  - 目标项目: `os/src/syscall/process.rs#sys_wait4`
  - 参考项目: `os/src/syscall/process.rs#sys_wait4`

* **【内存管理/ELF 加载 (map_elf)】**
* **底层技术析理**: 目标项目在映射 ELF 段时，涉及对内核空间 (`KERNEL_SPACE.lock()`) 的复杂操作，如 `remove_area_with_start_vpn`，表明其地址空间管理更为精细。参考项目则使用简单的 `offset.0` 进行计算。两者在内存管理模型的复杂度上存在明显差异。
* **代码溯源链接**:
  - 目标项目: `os/src/mm/memory_set.rs#map_elf`
  - 参考项目: `os/src/mm/memory_set.rs#map_elf`

#### 5. 单方模块与降权项
- **单方模块**: “网络协议栈/第三方库”模块仅存在于目标项目中，包含 `ixgbe`、`e1000` 等独立于参考项目的网卡驱动依赖，是独立开发的正面证据。
- **降权项**:
  - **第三方库/公共依赖**: `riscv`、`ext4_rs`、`virtio-drivers` 等共享库的代码指纹必须从原创性审查中剥离。
  - **POSIX/标准接口**: `sys_wait4`、`sys_uname`、`sys_clone` 等函数名由标准强制规定，其存在本身不构成证据。证据强度取决于其内部调用图谱的独创性。
  - **语言惯用法与 Trivial Getter/Setter**: `self.current.take()`、`self.bits.get()` 等单行代码的 AST 一致性是编程语言和数据结构设计的必然结果，不具证据价值。

#### 6. 保守结论
综合全模块证据，目标项目与参考项目的整体 AST 相似度仅为 18.87%，远未达到可判定为抄袭或同源的阈值。控方提出的“核心铁证”均能被合理解释：部分属于共享的第三方开源库（`riscv`、`ext4_rs`），部分属于 Rust 语言对 `Option` 类型的标准惯用法，部分属于 POSIX 标准接口的通用实现。最关键的是，双方在调度器架构（SMP 多核 vs. 单核）、进程等待模型、内存管理等核心子系统上展现出根本性的设计差异。目标项目还包含大量参考项目所没有的 Linux 兼容层特性（如 `prctl`、`futex`、`waitid`）。因此，本次审计判定目标项目为独立开发，不存在对参考项目的代码复用或抄袭行为。

------------------------------

