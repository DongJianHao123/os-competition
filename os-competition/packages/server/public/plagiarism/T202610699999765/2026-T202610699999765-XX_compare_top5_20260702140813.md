# 🛡️ OS 内核赛自动化代码级架构审计报告

**审计时间**: `2026-07-02 14:08:13`
**参赛作品名称**: `2026-T202610699999765-XX`
**年份**: `2026`
**学校**: `XX`
**队伍名称**: `T202610699999765`
**比赛名字**: `OS-Competition`
**比赛子赛道**: `Kernel-Implementation`
**仓库地址**: `https://gitlab.eduxiji.net/educg-group-43501-3132633/T202610699999765-256`
**推测开发语言**: `Rust / C (由AST探针自动识别)`
**特征函数量**: `6363`

## ⚔️ 底层代码同源性对比 (AST Homology)
> 本部分由专门的代码审查智能体，对该项目与符合年份条件的历届最高相似度项目进行控制流与底层调用对比。新版报告会先给出确定性的全模块覆盖矩阵，再给出 Agent 的分模块解释，避免只围绕少量抽样函数展开。

### 🏆 Top 5 底层对标: vs `2024-NPUcore-重生之我是菜狗-西北工业大学`（西北工业大学 / NPUcore-重生之我是菜狗）

#### 对方作品信息

**对方年份**: `2024`
**对方学校**: `西北工业大学`
**对方队伍名称**: `NPUcore-重生之我是菜狗`
**对方比赛名字**: `OS-Competition`
**对方比赛子赛道**: `Kernel-Implementation`
**对方仓库地址**: `https://gitlab.eduxiji.net/educg-group-26011-2376549/T202410699992491-3136`

> 双向覆盖率：共同 AST 指纹: 712；唯一 AST 指纹: 目标项目 3627，参考项目 1111；覆盖率: 目标项目 19.63%，参考项目 64.09%

## 全模块对比总览（确定性证据）

> 总体覆盖率：共同 AST 指纹: 712；唯一 AST 指纹: 目标项目 3627，参考项目 1111；覆盖率: 目标项目 19.63%，参考项目 64.09%

### 1. 模块覆盖矩阵
| 模块 | 目标函数数 | 参考函数数 | 共同唯一 AST 指纹 | Target 覆盖 | Base 覆盖 | 状态 |
| :--- | ---: | ---: | ---: | ---: | ---: | :--- |
| 进程管理 | 651 | 73 | 14 | 3.47% | 21.21% | 双方存在且有结构重合 |
| 任务调度/异步运行时 | 442 | 135 | 48 | 12.12% | 39.02% | 双方存在且有结构重合 |
| 内存管理 | 1058 | 520 | 252 | 35.8% | 67.02% | 双方存在且有结构重合 |
| 中断/异常处理 | 101 | 37 | 30 | 37.97% | 90.91% | 双方存在且有结构重合 |
| 系统调用 | 342 | 62 | 30 | 22.22% | 61.22% | 双方存在且有结构重合 |
| 文件系统 | 1079 | 499 | 213 | 27.31% | 66.56% | 双方存在且有结构重合 |
| 设备驱动 | 79 | 34 | 31 | 39.24% | 91.18% | 双方存在且有结构重合 |
| 网络协议栈 | 106 | 88 | 28 | 39.44% | 59.57% | 双方存在且有结构重合 |
| 文件系统/第三方库 | 404 | 0 | 0 | 0.0% | 0.0% | 仅目标项目体现 |
| 网络协议栈/第三方库 | 138 | 0 | 0 | 0.0% | 0.0% | 仅目标项目体现 |
| 第三方库 | 1263 | 0 | 0 | 0.0% | 0.0% | 仅目标项目体现 |
| 通用模块/其他 | 700 | 211 | 89 | 31.12% | 89.0% | 双方存在且有结构重合 |

### 2. 分模块证据清单

#### 进程管理
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `os/src/syscall/process.rs#sys_prlimit` | `os/src/syscall/process.rs#sys_prlimit` | 同板块同名 | (new_limit as usize)
            .checked_add, (new_limit as usize)
            .checked_add(size_of::<RLimit>())
            .is_none, (old_limit as usize)
            .checked_add, (old_limit as usize)
            .checked_add(size_of::<RLimit>())
            .is_none, Resource::from_primitive, copy_from_user ...(+34) | Resource::from_primitive, assert!, copy_from_user, copy_from_user(token, new_limit, rlimit).is_err, copy_to_user, copy_to_user(
                        token,
                        &(RLimit {
                            rlim_cur: SYSTEM_TASK_LIMIT,
                            rlim_max: SYSTEM_TASK_LIMIT,
                        }),
                        old_limit,
                    )
                    .is_err ...(+18) |
| `os/src/syscall/process.rs#sys_futex` | `os/src/syscall/process.rs#sys_futex` | 同板块同名 | FutexOption::from_bits_truncate, SHARED_FUTEX.lock, SHARED_FUTEX.lock().wake, current_task, current_task().unwrap, do_futex_wait ...(+16) | FutexOption::from_bits_truncate, current_task, current_task().unwrap, do_futex_wait, drop, info! ...(+15) |
| `os/src/syscall/process.rs#sys_setitimer` | `os/src/syscall/process.rs#sys_setitimer` | 同板块同名 | Arc::downgrade, ITimerVal::new, TimeSpec::from_us, TimeSpec::new, TimeSpec::now, TimeVal::from_us ...(+20) | copy_from_user, copy_from_user(token, new_value, &mut inner.timer[which]).unwrap, copy_to_user, copy_to_user(token, &inner.timer[which], old_value).unwrap, current_task, current_task().unwrap ...(+4) |
| `os/src/syscall/process.rs#sys_memorybarrier` | `os/src/syscall/process.rs#sys_memorybarrier` | 同板块同名 | core::sync::atomic::fence, current_task, current_task().unwrap, task.membarrier_state
                .fetch_or, task.membarrier_state.fetch_or, task.membarrier_state.load | error! |

#### 任务调度/异步运行时
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `os/src/syscall/fs.rs#sys_fcntl` | `os/src/syscall/fs.rs#sys_fcntl` | 同板块同名 | FOwnerEx::default, Fcntl_Command::from_primitive, OpenFlags::O_ASYNC.bits, arg.min, copy_from_user, copy_to_user ...(+41) | Fcntl_Command::from_primitive, OpenFlags::O_NONBLOCK.bits, OpenFlags::O_RDWR.bits, current_task, current_task().unwrap, fd_table.get_ref ...(+10) |
| `os/src/syscall/fs.rs#sys_splice` | `os/src/syscall/fs.rs#sys_splice` | 同板块同名 | OpenFlags::from_bits_truncate, OpenFlags::from_bits_truncate(out_file.get_status_flags()).contains, Some, Vec::<u8>::with_capacity, buffer.as_mut_slice, buffer.as_slice ...(+23) | Some, Vec::<u8>::with_capacity, buffer.as_mut_slice, buffer.as_slice, buffer.set_len, current_task ...(+16) |
| `os/src/fs/dev/tty.rs#ioctl` | `os/src/fs/dev/tty.rs#ioctl` | 同板块同名 | TeletypeCommand::from_primitive, Termio::default, Termio::from_termios, copy_from_user, copy_to_user, crate::task::current_user_token ...(+5) | TeletypeCommand::from_primitive, copy_from_user, copy_to_user, crate::task::current_user_token, info!, self.inner.lock ...(+3) |
| `os/src/syscall/net.rs#sys_setsockopt` | `os/src/syscall/net.rs#sys_setsockopt` | 同板块同名 | (size as usize).min, Err, core::mem::size_of::<TimeVal>, core::mem::size_of::<u32>, current_task, current_task().unwrap ...(+17) | current_task, current_task().unwrap, get_socket!, log::debug!, log::warn!, socket.set_keep_alive ...(+6) |

#### 内存管理
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `os/src/task/signal.rs#do_signal` | `os/src/task/signal.rs#do_signal` | 同板块同名 | Box::new, MachineContext::from_trap_context, Signals::SIGSEGV.wait_status_for_signal, Signals::from_bits_truncate, Some, act.flags.contains ...(+48) | (trap_cx as *const TrapContext).cast::<MachineContext>, SigInfo::new, SignalStack::new, Signals::SIGSEGV.to_signum, Signals::SIGSEGV.to_signum().unwrap, Signals::from_bits_truncate ...(+40) |
| `os/src/task/task.rs#sys_clone` | `os/src/task/task.rs#sys_clone` | 同板块同名 | Arc::downgrade, Arc::new, AtomicBool::new, AtomicUsize::new, Futex::new, ITimerVal::new ...(+101) | Arc::downgrade, Arc::new, Futex::new, ITimerVal::new, MemorySet::from_existing_user, Mutex::new ...(+55) |
| `os/src/hal/arch/loongarch64/trap/mod.rs#trap_handler` | `os/src/arch/la64/trap/mod.rs#trap_handler` | 同板块同名 | (0..sz).rev, BadV::read, BadV::read().get_vaddr, ERA::read, ERA::read().next_ins, ERA::read().next_ins().write ...(+61) | (0..sz).rev, BadV::read, BadV::read().get_vaddr, ERA::read, ERA::read().next_ins, ERA::read().next_ins().write ...(+54) |
| `os/src/mm/memory_set.rs#mmap` | `os/src/mm/memory_set.rs#mmap` | 同板块同名 | MapArea::new, OpenFlags::O_RDWR.bits, OpenFlags::O_WRONLY.bits, Some, VirtAddr::from, area.expand_to::<T> ...(+33) | MapArea::new, Some, USR_MMAP_BASE.into, VirtAddr::from, VirtAddr::from(USR_MMAP_END).into, area.expand_to::<T> ...(+28) |

#### 中断/异常处理
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `os/src/hal/arch/loongarch64/trap/mod.rs#trap_from_kernel` | `os/src/arch/la64/trap/mod.rs#trap_from_kernel` | 同板块同名 | ((addr + i) as *mut u8).read_unaligned, ((addr + i) as *mut u8).write_unaligned, (0..sz).rev, BadV::read, BadV::read().get_vaddr, EStat::read ...(+20) | ((addr + i) as *mut u8).read_unaligned, ((addr + i) as *mut u8).write_unaligned, (0..sz).rev, BadV::read, BadV::read().get_vaddr, EStat::read ...(+14) |
| `os/src/hal/arch/loongarch64/trap/mem_access.rs#get_addr` | `os/src/arch/la64/trap/mem_access.rs#get_addr` | 同板块同名同结构 | self.get_op_code, self.get_op_code().map, self.get_rj_num, self.get_rk_num, self.get_si12, self.get_si14 | self.get_op_code, self.get_op_code().map, self.get_rj_num, self.get_rk_num, self.get_si12, self.get_si14 |
| `os/src/hal/arch/loongarch64/trap/mem_access.rs#get_size` | `os/src/arch/la64/trap/mem_access.rs#get_size` | 同板块同名同结构 | 内部闭环逻辑 | 内部闭环逻辑 |
| `os/src/hal/arch/loongarch64/trap/mem_access.rs#is_store` | `os/src/arch/la64/trap/mem_access.rs#is_store` | 同板块同名同结构 | 内部闭环逻辑 | 内部闭环逻辑 |

#### 系统调用
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `os/src/net/tcp.rs#_read` | `os/src/net/tcp.rs#_read` | 同板块同名 | Err, GeneralRet::Err, GeneralRet::Ok, NET_INTERFACE.poll, NET_INTERFACE.tcp_socket, Ok ...(+6) | Err, GeneralRet::Err, GeneralRet::Ok, NET_INTERFACE.poll, NET_INTERFACE.tcp_socket, Ok ...(+7) |
| `os/src/net/udp.rs#_read` | `os/src/net/udp.rs#_read` | 同板块同名 | Err, GeneralRet::Err, GeneralRet::Ok, NET_INTERFACE.poll, NET_INTERFACE.udp_socket, Ok ...(+15) | Err, GeneralRet::Err, GeneralRet::Ok, NET_INTERFACE.poll, NET_INTERFACE.udp_socket, Ok ...(+10) |
| `os/src/net/udp.rs#connect` | `os/src/net/udp.rs#connect` | 同板块同名 | Err, IpListenEndpoint::from, NET_INTERFACE.poll, NET_INTERFACE.udp_socket, Ok, SocketAddrv4::new ...(+12) | Err, IpListenEndpoint::from, NET_INTERFACE.poll, NET_INTERFACE.udp_socket, Ok, SocketAddrv4::new ...(+11) |
| `os/src/net/address.rs#_listen_endpoint` | `os/src/net/address.rs#_listen_endpoint` | 同板块同名 | Err, IpListenEndpoint::from, Ok, SocketAddrv4::new, SocketAddrv6::new, addr_buf.len ...(+7) | Err, IpListenEndpoint::from, Ok, SocketAddrv4::new, SocketAddrv6::new, addr_buf[0..2].try_into ...(+3) |

#### 文件系统
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `os/src/fs/directory_tree.rs#rename` | `os/src/fs/directory_tree.rs#rename` | 同板块同名 | "/".to_string, Arc::downgrade, Arc::new, Err, Mutex::new, Ok ...(+62) | Arc::downgrade, Arc::new, Err, Mutex::new, Ok, ROOT.cd_comp ...(+37) |
| `os/src/fs/directory_tree.rs#init_device_directory` | `os/src/fs/directory_tree.rs#init_device_directory` | 同板块同名 | "0".to_string, "loop-control".to_string, "loop0".to_string, "null".to_string, "ptmx".to_string, "rtc".to_string ...(+40) | "null".to_string, "rtc".to_string, "tty".to_string, "urandom".to_string, "zero".to_string, Arc::downgrade ...(+20) |
| `os/src/fs/dev/block.rs#ioctl` | `os/src/fs/mod.rs#ioctl` | 同板块同名 | copy_from_user, copy_to_user, current_user_token, self.state.lock | self.file.ioctl |
| `os/src/fs/dev/anon.rs#read_user` | `os/src/fs/fat32/inode.rs#read_user` | 同板块同名 | buf.buffers.iter_mut, buf.len, buf.write, expirations.to_ne_bytes, self.eventfd.lock, self.eventfd.lock().read_value ...(+10) | buf.buffers.iter_mut, self.inner
                            .read_at_block_cache_rlock, self.inner.read, self.offset.lock |

#### 设备驱动
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `os/src/drivers/block/sata_blk.rs#pci_init` | `os/src/drivers/block/sata_blk.rs#pci_init` | 同板块同名同结构 | AHCI::new, Some, Status::empty, dev.bars.iter, dev.bars.iter().enumerate, dev.bars.iter().enumerate().for_each ...(+3) | AHCI::new, Some, Status::empty, dev.bars.iter, dev.bars.iter().enumerate, dev.bars.iter().enumerate().for_each ...(+3) |
| `os/path-deps/isomorphic_drivers/src/block/ahci.rs#write_block` | `os/src/drivers/block/sata_blk.rs#write_block` | 同板块同名 | CommandHeaderFlags::WRITE.bits, buf.len, buf.len().min, fis.set_lba, self.data[0..len].clone_from_slice, self.port.issue_command ...(+1) | buf.chunks, self.0
                .lock, self.0
                .lock()
                .write_block |
| `os/path-deps/isomorphic_drivers/src/block/ahci.rs#read_block` | `os/src/drivers/block/sata_blk.rs#read_block` | 同板块同名 | buf.len, buf.len().min, buf[..len].clone_from_slice, fis.set_lba, self.port.issue_command, self.port.spin_on_slot | buf.chunks_mut, self.0
                .lock, self.0
                .lock()
                .read_block |
| `os/src/drivers/block/sata_blk.rs#enable` | `os/src/drivers/block/sata_blk.rs#enable` | 同板块同名同结构 | am.read16, am.write32 | am.read16, am.write32 |

#### 网络协议栈
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `os/src/net/mod.rs#can_bind` | `os/src/net/mod.rs#can_bind` | 同板块同名 | Some, self.0.iter, socket.clone, socket.loacl_endpoint, socket.loacl_endpoint().eq, socket.socket_type ...(+1) | Some, log::info!, self.0.clone, socket.loacl_endpoint, socket.loacl_endpoint().eq, socket.socket_type ...(+1) |
| `os/src/net/config.rs#tcp_socket` | `os/src/net/config.rs#tcp_socket` | 同板块同名同结构 | f, self
            .inner
            .lock, self
            .inner
            .lock()
            .as_mut, self
            .inner
            .lock()
            .as_mut()
            .unwrap, self
            .inner
            .lock()
            .as_mut()
            .unwrap()
            .sockets
            .get_mut::<tcp::Socket> | f, self
            .inner
            .lock, self
            .inner
            .lock()
            .as_mut, self
            .inner
            .lock()
            .as_mut()
            .unwrap, self
            .inner
            .lock()
            .as_mut()
            .unwrap()
            .sockets
            .get_mut::<tcp::Socket> |
| `os/src/net/config.rs#udp_socket` | `os/src/net/config.rs#udp_socket` | 同板块同名同结构 | f, self
            .inner
            .lock, self
            .inner
            .lock()
            .as_mut, self
            .inner
            .lock()
            .as_mut()
            .unwrap, self
            .inner
            .lock()
            .as_mut()
            .unwrap()
            .sockets
            .get_mut::<udp::Socket> | f, self
            .inner
            .lock, self
            .inner
            .lock()
            .as_mut, self
            .inner
            .lock()
            .as_mut()
            .unwrap, self
            .inner
            .lock()
            .as_mut()
            .unwrap()
            .sockets
            .get_mut::<udp::Socket> |
| `os/src/net/mod.rs#from_another` | `os/src/net/mod.rs#from_another` | 同板块同名同结构 | BTreeMap::new, Ok, Self, ret.insert, socket.clone, socket_table.0.iter | BTreeMap::new, Ok, Self, ret.insert, socket.clone, socket_table.0.iter |

#### 文件系统/第三方库
仅目标项目中观察到该模块代表函数，参考项目没有同模块可对齐证据：
- 目标项目：`os/src/fs/ext4/extent.rs#ext_remove_leaf`，调用摘要：Block::load_inode_root_block, Block::load_offset, Ext4Extent::default, Ok, chunk.try_into, chunk.try_into().unwrap ...(+25)
- 目标项目：`os/src/fs/ext4/extent.rs#extent_remove_space`，调用摘要：Block::load_offset, Ext4Extent::default, Ok, ex.get_actual_len, ex.get_first_block, ex.get_pblock ...(+14)
- 目标项目：`os/src/fs/ext4/file.rs#write_at_with_sync`，调用摘要：Block::load_offset, Ok, block.sync_blk_to_disk, block.write_offset, drop, inode_ref.inode.set_size ...(+11)
- 目标项目：`os/src/fs/ext4/balloc.rs#balloc_alloc_block_from`，调用摘要：Block::load_offset, Err, Ext4BlockGroup::load_new, Ok, block_group.get_block_bitmap_block, block_group.get_free_blocks_count ...(+12)

#### 网络协议栈/第三方库
仅目标项目中观察到该模块代表函数，参考项目没有同模块可对齐证据：
- 目标项目：`dependency/dep_iso/src/net/ethernet/intel/ixgbe.rs#new`，调用摘要：(IXGBE_FCRTH..IXGBE_FCRTH_END).step_by, (IXGBE_FCRTL..IXGBE_FCRTL_END).step_by, (IXGBE_FCTTV..IXGBE_FCTTV_END).step_by, EthernetAddress::from_bytes, MaybeUninit::uninit, MaybeUninit::uninit().assume_init ...(+56)
- 目标项目：`dependency/virtio-drivers/src/device/socket/connectionmanager.rs#send_recv`，调用摘要："Hello from guest".as_bytes, (hello_from_guest.len() as u32).into, (hello_from_host.len() as u32).into, 0.into, 50.into, Arc::new ...(+43)
- 目标项目：`dependency/virtio-drivers/src/device/socket/connectionmanager.rs#incoming_connection`，调用摘要：0.into, 50.into, Arc::new, Mutex::new, ReadOnly::new, SocketType::Stream.into ...(+24)
- 目标项目：`dependency/dep_iso/src/net/ethernet/intel/e1000.rs#new`，调用摘要：P::alloc_dma, Vec::with_capacity, assert_eq!, debug!, e1000[E1000_ICR].read, e1000[E1000_ICR].write ...(+25)

#### 第三方库
仅目标项目中观察到该模块代表函数，参考项目没有同模块可对齐证据：
- 目标项目：`dependency/dep_pci/pci/src/lib.rs#probe_function`，调用摘要：BAR::decode, BridgeControl::from_bits_truncate, Command::from_bits_truncate, DeviceKind::CardbusBridge, DeviceKind::Device, DeviceKind::PciBridge ...(+6)
- 目标项目：`dependency/virtio-drivers/src/device/sound/fake.rs#handle_control_request`，调用摘要：CommandCode::SOk.into, Some, Vec::new, VirtIOSndHdr {
                            command_code: CommandCode::SOk.into(),
                        }
                        .as_bytes, VirtIOSndHdr::read_from_prefix, VirtIOSndHdr::read_from_prefix(&request)
                .expect ...(+16)
- 目标项目：`dependency/dep_iso/src/block/ahci.rs#new`，调用摘要：(0..ghc.num_ports()).find, P::alloc_dma, Some, c.set_bit, debug!, ghc.enable ...(+16)
- 目标项目：`dependency/virtio-drivers/src/transport/pci.rs#new`，调用摘要：Err, Ok, Some, UniqueMmioPointer::new, VirtioPciError::InvalidDeviceId, VirtioPciError::InvalidNotifyOffMultiplier ...(+15)

#### 通用模块/其他
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `user/src/lib.rs#_start` | `user/src/lib.rs#_start` | 同板块同名 | _parameter, asm!, core::arch::asm!, unreachable! | _parameter, asm!, unreachable! |
| `os/src/hal/arch/loongarch64/time.rs#get_timer_freq_first_time` | `os/src/arch/la64/time.rs#get_timer_freq_first_time` | 同板块同名 | cfg5.get_bits, config::CPUCfg4::read, config::CPUCfg4::read().get_bits, config::CPUCfg5::read, println! | cfg5.get_bits, config::CPUCfg4::read, config::CPUCfg4::read().get_bits, config::CPUCfg5::read, println! |
| `user/src/lib.rs#_parameter` | `user/src/lib.rs#_parameter` | 同板块同名 | ((argv + i * core::mem::size_of::<usize>()) as *const usize).read_volatile, ((str_start + *i) as *const u8).read_volatile, (0usize..)
            .find, (0usize..)
            .find(|i| unsafe { ((str_start + *i) as *const u8).read_volatile() == 0 })
            .unwrap, Vec::new, core::mem::size_of::<usize> ...(+8) | ((argv + i * core::mem::size_of::<usize>()) as *const usize).read_volatile, ((str_start + *i) as *const u8).read_volatile, (0usize..)
            .find, (0usize..)
            .find(|i| unsafe { ((str_start + *i) as *const u8).read_volatile() == 0 })
            .unwrap, HEAP.lock, HEAP.lock()
            .init ...(+10) |
| `os/src/console.rs#write_str` | `os/src/console.rs#write_str` | 同板块同名 | Ok, console_flush, console_putchar, s.chars | Ok, console_flush, console_putchar, s.chars |


#### 1. 宏观判定
- **定性结论**: 独立开发 (相似度: 17.69%，共同 AST 指纹: 712；唯一 AST 指纹: 目标项目 3627，参考项目 1111；覆盖率: 目标项目 19.63%，参考项目 64.09%)

#### 2. 全模块覆盖矩阵解读
- **进程管理**: 双方均存在。共同 AST 指纹仅 14 个，目标覆盖率 3.47%，参考覆盖率 21.21%。极低的覆盖率表明目标项目在该模块的绝大部分实现是独立的。证据强度：**弱**。
- **任务调度/异步运行时**: 双方均存在。共同 AST 指纹 48 个，目标覆盖率 12.12%，参考覆盖率 39.02%。覆盖率差异源于参考项目体量小，且目标项目引入了多核、多级队列等复杂架构。证据强度：**弱**。
- **内存管理**: 双方均存在。共同 AST 指纹 252 个，目标覆盖率 35.8%，参考覆盖率 67.02%。覆盖率较高，但内存管理涉及大量标准算法与结构，需深入分析。证据强度：**中**。
- **中断/异常处理**: 双方均存在。共同 AST 指纹 30 个，目标覆盖率 37.97%，参考覆盖率 90.91%。参考项目覆盖率极高，因其体量小，而目标项目在相同路径上存在独立扩展。证据强度：**中**。
- **系统调用**: 双方均存在。共同 AST 指纹 30 个，目标覆盖率 22.22%，参考覆盖率 61.22%。系统调用入口由 ABI 强制规定，同名同参是必然的。证据强度：**弱**。
- **文件系统**: 双方均存在。共同 AST 指纹 213 个，目标覆盖率 27.31%，参考覆盖率 66.56%。目标项目实现了 ext4 等复杂系统，而参考项目为 FAT32，架构差异巨大。证据强度：**弱**。
- **设备驱动**: 双方均存在。共同 AST 指纹 31 个，目标覆盖率 39.24%，参考覆盖率 91.18%。高覆盖率源于双方都使用了相同的硬件驱动库（如 AHCI）。证据强度：**弱**。
- **网络协议栈**: 双方均存在。共同 AST 指纹 28 个，目标覆盖率 39.44%，参考覆盖率 59.57%。双方均集成了 smoltcp 等第三方库，相似性源于公共依赖。证据强度：**弱**。
- **第三方库/公共依赖**: 仅目标项目存在大量第三方库（ext4, ixgbe, virtio 等），参考项目无对应模块。这是目标项目独立扩展的铁证。证据强度：**未形成**。

#### 3. 底层 AST 关键证据指纹表
| 所属板块 | 目标项目路径 | 参考项目路径 | 证据类型 | 技术说明 |
| :--- | :--- | :--- | :--- | :--- |
| 进程管理 | os/src/syscall/process.rs#sys_mprotect | os/src/syscall/process.rs#sys_mprotect | 同板块同名 | 调用链完全一致，但这是 Linux 标准系统调用的唯一实现路径，属于领域强制收敛，非代码复用。 |
| 进程管理 | os/src/syscall/process.rs#sys_sbrk | os/src/syscall/process.rs#sys_sbrk | 同板块同名 | 核心逻辑相似，但目标项目使用 `task.heap.lock` 独立管理堆锁，参考项目使用 `task.acquire_inner_lock`，体现了不同的并发控制策略。 |
| 进程管理 | os/src/task/processor.rs#schedule | os/src/task/processor.rs#schedule | 同板块同名 | 目标项目使用 `PROCESSORS[cpu_id].lock` 支持多核，参考项目使用 `PROCESSOR.lock` 仅支持单核，架构设计根本不同。 |
| 任务调度 | os/src/task/manager.rs#find_by_pid | os/src/task/manager.rs#find_by_pid | 同板块同名 | 目标项目遍历 `cfs_rq`, `rt_rq`, `idle_rq` 等多级调度队列，参考项目仅遍历 `ready_queue` 和 `interruptible_queue`，调度模型完全不同。 |
| 内存管理 | os/src/mm/memory_set.rs#mmap | os/src/mm/memory_set.rs#mmap | 同板块同名 | 双方均实现 mmap，但目标项目使用 `OpenFlags` 进行权限检查，参考项目使用 `USR_MMAP_BASE` 常量，内存布局策略不同。 |
| 中断/异常处理 | os/src/hal/arch/loongarch64/trap/mod.rs#trap_handler | os/src/arch/la64/trap/mod.rs#trap_handler | 同板块同名 | 核心的指令解码与修复逻辑高度相似，但这是 LoongArch 架构标准 trap 处理流程，属于硬件规范决定的必然实现。 |
| 系统调用 | os/src/net/tcp.rs#_read | os/src/net/tcp.rs#_read | 同板块同名 | 调用 `NET_INTERFACE.poll` 和 `tcp_socket` 的模式一致，源于双方都集成了相同的 `smoltcp` 网络栈。 |
| 文件系统 | os/src/fs/directory_tree.rs#rename | os/src/fs/directory_tree.rs#rename | 同板块同名 | 目标项目包含 `"/".to_string()` 等路径处理，参考项目使用 `ROOT.cd_comp`，文件系统树遍历的根节点处理方式不同。 |
| 设备驱动 | os/src/drivers/block/sata_blk.rs#pci_init | os/src/drivers/block/sata_blk.rs#pci_init | 同板块同名同结构 | 完全一致，源于双方都使用了相同的 `AHCI` 驱动库初始化代码，属于公共依赖。 |
| 网络协议栈 | os/src/net/config.rs#tcp_socket | os/src/net/config.rs#tcp_socket | 同板块同名同结构 | 完全一致，源于双方都使用了相同的 `smoltcp` 库接口来获取 socket 实例。 |
| 第三方库 | dependency/dep_iso/src/block/ahci.rs#new | (无) | 仅目标项目存在 | 目标项目集成了独立的 AHCI 驱动库，参考项目无此模块，是独立开发的正面证据。 |
| 第三方库 | dependency/virtio-drivers/src/transport/pci.rs#new | (无) | 仅目标项目存在 | 目标项目集成了 virtio 驱动框架，参考项目无此模块，是独立开发的正面证据。 |

#### 4. 核心代码调用流对比剖析
* **【进程管理/sys_futex 系统调用】**
* **底层技术析理**: 两者均实现了 futex 系统调用，但同步原语模型完全不同。目标项目使用全局的 `SHARED_FUTEX.lock().wake` 进行跨进程唤醒，暗示其 futex 机制是基于全局共享表实现的。参考项目则使用 `task.futex.lock`，表明其 futex 是绑定在任务控制块内的私有结构。这是两种截然不同的 futex 对象管理模型。
* **代码溯源链接**:
  - 目标项目: `os/src/syscall/process.rs#sys_futex`
  - 参考项目: `os/src/syscall/process.rs#sys_futex`

* **【任务调度/schedule 函数】**
* **底层技术析理**: 调度器的核心架构存在代差。目标项目通过 `PROCESSORS[cpu_id].lock` 和 `current_cpu_id` 实现了多核 SMP 调度，并在调度前调用 `disable_interrupts` 保证原子性。参考项目仅使用 `PROCESSOR.lock`，是典型的单核调度器。目标项目不是对参考项目的简单重构，而是面向多核架构的完全重写。
* **代码溯源链接**:
  - 目标项目: `os/src/task/processor.rs#schedule`
  - 参考项目: `os/src/task/processor.rs#schedule`

* **【内存管理/sys_brk 系统调用】**
* **底层技术析理**: 两者都通过 `memory_set.sbrk` 调整堆大小，但并发控制粒度不同。目标项目使用 `task.heap.lock` 和 `task.vm.lock` 分别保护堆和虚拟内存空间，实现了细粒度锁。参考项目则使用单一的 `task.acquire_inner_lock` 保护整个任务结构，是粗粒度锁。目标项目的设计更利于多核并发性能，体现了独立的架构思考。
* **代码溯源链接**:
  - 目标项目: `os/src/syscall/process.rs#sys_brk`
  - 参考项目: `os/src/syscall/process.rs#sys_brk`

#### 5. 单方模块与降权项
- **单方模块**: 目标项目独有的 `文件系统/第三方库` (ext4)、`网络协议栈/第三方库` (ixgbe, e1000)、`第三方库` (virtio, pci) 等模块，在参考项目中完全不存在。这些模块体量庞大、实现复杂，是目标项目独立开发、功能远超参考项目的强有力正面证据，不构成同源嫌疑。
- **降权项**: 控方指控中，`sys_mprotect` 等系统调用入口的相似性，因受限于 Linux ABI 标准，属于“领域强制收敛”，已降权为弱信号。`sys_prlimit` 等函数中，目标项目增加的 `checked_add` 安全检查，是独立开发的安全增强，而非洗稿噪音。所有跨模块同名函数（如 `ioctl`, `read_user`）因操作对象完全不同，不构成同板块主证据，已全部降权。

#### 6. 保守结论
综合评估，目标项目与参考项目的整体 AST 相似度仅为 17.69%，且重合点主要集中在受 Linux ABI 强制约束的系统调用入口和公共第三方库（如 smoltcp、AHCI 驱动）的调用模式上。在核心的进程管理、任务调度和内存管理子系统，目标项目展现了与参考项目截然不同的架构设计，包括多核 SMP 支持、多级调度队列和细粒度锁机制。目标项目还独立实现了 ext4 文件系统、多种网卡驱动等大量参考项目不具备的功能模块。因此，现有证据无法支持“抄袭”或“同源”的指控，应判定为独立开发作品。

------------------------------

