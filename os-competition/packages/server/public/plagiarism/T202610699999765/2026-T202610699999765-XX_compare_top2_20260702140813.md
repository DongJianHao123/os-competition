# 🛡️ OS 内核赛自动化代码级架构审计报告

**审计时间**: `2026-07-02 14:08:13`
**参赛作品名称**: `2026-T202610699999765-XX`
**年份**: `2026`
**学校**: `XX`
**队伍名称**: `T202610699999765`
**比赛名字**: `OS-Competition`
**比赛子赛道**: `Kernel-Implementation`
**仓库地址**: `https://gitlab.eduxiji.net/educg-group-43501-3132633/T202610699999765-256`
**推测开发语言**: `Rust / C (由AST探针自动识别)`
**特征函数量**: `6363`

## ⚔️ 底层代码同源性对比 (AST Homology)
> 本部分由专门的代码审查智能体，对该项目与符合年份条件的历届最高相似度项目进行控制流与底层调用对比。新版报告会先给出确定性的全模块覆盖矩阵，再给出 Agent 的分模块解释，避免只围绕少量抽样函数展开。

### 🏆 Top 2 底层对标: vs `2025-西北工业大学一二三队-西北工业大学`（西北工业大学 / 西北工业大学一二三队）

#### 对方作品信息

**对方年份**: `2025`
**对方学校**: `西北工业大学`
**对方队伍名称**: `西北工业大学一二三队`
**对方比赛名字**: `OS-Competition`
**对方比赛子赛道**: `Kernel-Implementation`
**对方仓库地址**: `https://gitlab.eduxiji.net/educg-group-36002-2710490/T202510699995276-827`

> 双向覆盖率：共同 AST 指纹: 1819；唯一 AST 指纹: 目标项目 3627，参考项目 2317；覆盖率: 目标项目 50.15%，参考项目 78.51%

## 全模块对比总览（确定性证据）

> 总体覆盖率：共同 AST 指纹: 1819；唯一 AST 指纹: 目标项目 3627，参考项目 2317；覆盖率: 目标项目 50.15%，参考项目 78.51%

### 1. 模块覆盖矩阵
| 模块 | 目标函数数 | 参考函数数 | 共同唯一 AST 指纹 | Target 覆盖 | Base 覆盖 | 状态 |
| :--- | ---: | ---: | ---: | ---: | ---: | :--- |
| 进程管理 | 651 | 74 | 14 | 3.47% | 20.9% | 双方存在且有结构重合 |
| 任务调度/异步运行时 | 442 | 139 | 49 | 12.37% | 38.89% | 双方存在且有结构重合 |
| 内存管理 | 1058 | 593 | 292 | 41.48% | 70.19% | 双方存在且有结构重合 |
| 中断/异常处理 | 101 | 47 | 37 | 46.84% | 92.5% | 双方存在且有结构重合 |
| 系统调用 | 342 | 65 | 32 | 23.7% | 61.54% | 双方存在且有结构重合 |
| 文件系统 | 1079 | 552 | 278 | 35.64% | 76.58% | 双方存在且有结构重合 |
| 设备驱动 | 79 | 47 | 39 | 49.37% | 82.98% | 双方存在且有结构重合 |
| 网络协议栈 | 106 | 88 | 28 | 39.44% | 59.57% | 双方存在且有结构重合 |
| 文件系统/第三方库 | 404 | 369 | 225 | 71.2% | 77.85% | 双方存在且有结构重合 |
| 网络协议栈/第三方库 | 138 | 138 | 123 | 100.0% | 100.0% | 双方存在且有结构重合 |
| 第三方库 | 1263 | 1228 | 657 | 89.75% | 93.06% | 双方存在且有结构重合 |
| 通用模块/其他 | 700 | 230 | 108 | 37.76% | 91.53% | 双方存在且有结构重合 |

### 2. 分模块证据清单

#### 进程管理
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `os/src/syscall/process.rs#sys_prlimit` | `os/src/syscall/process.rs#sys_prlimit` | 同板块同名 | (new_limit as usize)
            .checked_add, (new_limit as usize)
            .checked_add(size_of::<RLimit>())
            .is_none, (old_limit as usize)
            .checked_add, (old_limit as usize)
            .checked_add(size_of::<RLimit>())
            .is_none, Resource::from_primitive, copy_from_user ...(+34) | Resource::from_primitive, assert!, copy_from_user, copy_from_user(token, new_limit, rlimit).is_err, copy_to_user, copy_to_user(
                        token,
                        &(RLimit {
                            rlim_cur: SYSTEM_TASK_LIMIT,
                            rlim_max: SYSTEM_TASK_LIMIT,
                        }),
                        old_limit,
                    )
                    .is_err ...(+18) |
| `os/src/syscall/process.rs#sys_futex` | `os/src/syscall/process.rs#sys_futex` | 同板块同名 | FutexOption::from_bits_truncate, SHARED_FUTEX.lock, SHARED_FUTEX.lock().wake, current_task, current_task().unwrap, do_futex_wait ...(+16) | FutexOption::from_bits_truncate, current_task, current_task().unwrap, do_futex_wait, drop, info! ...(+15) |
| `os/src/syscall/process.rs#sys_setitimer` | `os/src/syscall/process.rs#sys_setitimer` | 同板块同名 | Arc::downgrade, ITimerVal::new, TimeSpec::from_us, TimeSpec::new, TimeSpec::now, TimeVal::from_us ...(+20) | copy_from_user, copy_from_user(token, new_value, &mut inner.timer[which]).unwrap, copy_to_user, copy_to_user(token, &inner.timer[which], old_value).unwrap, current_task, current_task().unwrap ...(+4) |
| `os/src/syscall/process.rs#sys_memorybarrier` | `os/src/syscall/process.rs#sys_memorybarrier` | 同板块同名 | core::sync::atomic::fence, current_task, current_task().unwrap, task.membarrier_state
                .fetch_or, task.membarrier_state.fetch_or, task.membarrier_state.load | error! |

#### 任务调度/异步运行时
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `os/src/syscall/fs.rs#sys_fcntl` | `os/src/syscall/fs.rs#sys_fcntl` | 同板块同名 | FOwnerEx::default, Fcntl_Command::from_primitive, OpenFlags::O_ASYNC.bits, arg.min, copy_from_user, copy_to_user ...(+41) | Fcntl_Command::from_primitive, OpenFlags::O_NONBLOCK.bits, OpenFlags::O_RDWR.bits, current_task, current_task().unwrap, fd_table.get_ref ...(+10) |
| `os/src/syscall/fs.rs#sys_copy_file_range` | `os/src/syscall/fs.rs#sys_copy_file_range` | 同板块同名 | (isize::MAX as usize).saturating_sub, OpenFlags::from_bits_truncate, OpenFlags::from_bits_truncate(out_file.get_status_flags()).contains, Some, StatMode::S_IFBLK.bits, StatMode::S_IFCHR.bits ...(+42) | Some, Vec::new, Vec::with_capacity, buf.set_len, current_task, current_task().unwrap ...(+18) |
| `os/src/syscall/fs.rs#sys_splice` | `os/src/syscall/fs.rs#sys_splice` | 同板块同名 | OpenFlags::from_bits_truncate, OpenFlags::from_bits_truncate(out_file.get_status_flags()).contains, Some, Vec::<u8>::with_capacity, buffer.as_mut_slice, buffer.as_slice ...(+23) | Some, Vec::<u8>::with_capacity, buffer.as_mut_slice, buffer.as_slice, buffer.set_len, current_task ...(+17) |
| `os/src/fs/dev/tty.rs#ioctl` | `os/src/fs/dev/tty.rs#ioctl` | 同板块同名 | TeletypeCommand::from_primitive, Termio::default, Termio::from_termios, copy_from_user, copy_to_user, crate::task::current_user_token ...(+5) | TeletypeCommand::from_primitive, copy_from_user, copy_to_user, crate::task::current_user_token, info!, self.inner.lock ...(+3) |

#### 内存管理
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `os/src/task/signal.rs#do_signal` | `os/src/task/signal.rs#do_signal` | 同板块同名 | Box::new, MachineContext::from_trap_context, Signals::SIGSEGV.wait_status_for_signal, Signals::from_bits_truncate, Some, act.flags.contains ...(+48) | (trap_cx as *const TrapContext).cast::<MachineContext>, SigInfo::new, SignalStack::new, Signals::SIGSEGV.to_signum, Signals::SIGSEGV.to_signum().unwrap, Signals::from_bits_truncate ...(+40) |
| `os/src/task/task.rs#sys_clone` | `os/src/task/task.rs#sys_clone` | 同板块同名 | Arc::downgrade, Arc::new, AtomicBool::new, AtomicUsize::new, Futex::new, ITimerVal::new ...(+101) | Arc::downgrade, Arc::new, Futex::new, ITimerVal::new, MemorySet::from_existing_user, Mutex::new ...(+55) |
| `os/src/hal/arch/loongarch64/trap/mod.rs#trap_handler` | `os/src/hal/arch/loongarch64/trap/mod.rs#trap_handler` | 同板块同名 | (0..sz).rev, BadV::read, BadV::read().get_vaddr, ERA::read, ERA::read().next_ins, ERA::read().next_ins().write ...(+61) | (0..sz).rev, BadV::read, BadV::read().get_vaddr, ERA::read, ERA::read().next_ins, ERA::read().next_ins().write ...(+55) |
| `os/src/hal/arch/riscv/trap/mod.rs#trap_handler` | `os/src/hal/arch/riscv/trap/mod.rs#trap_handler` | 同板块同名 | Some, VirtAddr::from, core::arch::asm!, crate::fs::dev::interrupts::Interrupts::increment_interrupt_count, crate::syscall::is_fast_syscall, current_task ...(+26) | VirtAddr::from, crate::fs::dev::interrupts::Interrupts::increment_interrupt_count, current_task, current_task().unwrap, current_trap_cx, do_wake_expired ...(+18) |

#### 中断/异常处理
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `os/src/hal/arch/loongarch64/trap/mod.rs#trap_from_kernel` | `os/src/hal/arch/loongarch64/trap/mod.rs#trap_from_kernel` | 同板块同名 | ((addr + i) as *mut u8).read_unaligned, ((addr + i) as *mut u8).write_unaligned, (0..sz).rev, BadV::read, BadV::read().get_vaddr, EStat::read ...(+20) | ((addr + i) as *mut u8).read_unaligned, ((addr + i) as *mut u8).write_unaligned, (0..sz).rev, BadV::read, BadV::read().get_vaddr, EStat::read ...(+14) |
| `os/src/hal/arch/loongarch64/trap/mem_access.rs#get_addr` | `os/src/hal/arch/loongarch64/trap/mem_access.rs#get_addr` | 同板块同名同结构 | self.get_op_code, self.get_op_code().map, self.get_rj_num, self.get_rk_num, self.get_si12, self.get_si14 | self.get_op_code, self.get_op_code().map, self.get_rj_num, self.get_rk_num, self.get_si12, self.get_si14 |
| `os/src/hal/arch/loongarch64/trap/mem_access.rs#get_size` | `os/src/hal/arch/loongarch64/trap/mem_access.rs#get_size` | 同板块同名同结构 | 内部闭环逻辑 | 内部闭环逻辑 |
| `os/src/hal/arch/loongarch64/trap/mem_access.rs#is_store` | `os/src/hal/arch/loongarch64/trap/mem_access.rs#is_store` | 同板块同名同结构 | 内部闭环逻辑 | 内部闭环逻辑 |

#### 系统调用
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `os/src/net/tcp.rs#_read` | `os/src/net/tcp.rs#_read` | 同板块同名 | Err, GeneralRet::Err, GeneralRet::Ok, NET_INTERFACE.poll, NET_INTERFACE.tcp_socket, Ok ...(+6) | Err, GeneralRet::Err, GeneralRet::Ok, NET_INTERFACE.poll, NET_INTERFACE.tcp_socket, Ok ...(+7) |
| `os/src/net/udp.rs#_read` | `os/src/net/udp.rs#_read` | 同板块同名 | Err, GeneralRet::Err, GeneralRet::Ok, NET_INTERFACE.poll, NET_INTERFACE.udp_socket, Ok ...(+15) | Err, GeneralRet::Err, GeneralRet::Ok, NET_INTERFACE.poll, NET_INTERFACE.udp_socket, Ok ...(+10) |
| `os/src/net/udp.rs#connect` | `os/src/net/udp.rs#connect` | 同板块同名 | Err, IpListenEndpoint::from, NET_INTERFACE.poll, NET_INTERFACE.udp_socket, Ok, SocketAddrv4::new ...(+12) | Err, IpListenEndpoint::from, NET_INTERFACE.poll, NET_INTERFACE.udp_socket, Ok, SocketAddrv4::new ...(+11) |
| `os/src/net/address.rs#_listen_endpoint` | `os/src/net/address.rs#_listen_endpoint` | 同板块同名 | Err, IpListenEndpoint::from, Ok, SocketAddrv4::new, SocketAddrv6::new, addr_buf.len ...(+7) | Err, IpListenEndpoint::from, Ok, SocketAddrv4::new, SocketAddrv6::new, addr_buf[0..2].try_into ...(+3) |

#### 文件系统
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `os/src/fs/directory_tree.rs#rename` | `os/src/fs/directory_tree.rs#rename` | 同板块同名 | "/".to_string, Arc::downgrade, Arc::new, Err, Mutex::new, Ok ...(+62) | Arc::downgrade, Arc::new, Err, Mutex::new, Ok, ROOT.cd_comp ...(+41) |
| `os/src/fs/directory_tree.rs#init_device_directory` | `os/src/fs/directory_tree.rs#init_device_directory` | 同板块同名 | "0".to_string, "loop-control".to_string, "loop0".to_string, "null".to_string, "ptmx".to_string, "rtc".to_string ...(+40) | "null".to_string, "rtc".to_string, "tty".to_string, "urandom".to_string, "zero".to_string, Arc::downgrade ...(+21) |
| `os/src/fs/dev/block.rs#ioctl` | `os/src/fs/file_descriptor.rs#ioctl` | 同板块同名 | copy_from_user, copy_to_user, current_user_token, self.state.lock | self.file.ioctl |
| `os/src/fs/dev/anon.rs#read_user` | `os/src/fs/fat32/fat_osinode.rs#read_user` | 同板块同名 | buf.buffers.iter_mut, buf.len, buf.write, expirations.to_ne_bytes, self.eventfd.lock, self.eventfd.lock().read_value ...(+10) | buf.buffers.iter_mut, self.inner
                            .read_at_block_cache_rlock, self.inner.read, self.offset.lock |

#### 设备驱动
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `os/src/drivers/block/sata_blk.rs#pci_init` | `os/src/drivers/block/sata_blk.rs#pci_init` | 同板块同名同结构 | AHCI::new, Some, Status::empty, dev.bars.iter, dev.bars.iter().enumerate, dev.bars.iter().enumerate().for_each ...(+3) | AHCI::new, Some, Status::empty, dev.bars.iter, dev.bars.iter().enumerate, dev.bars.iter().enumerate().for_each ...(+3) |
| `os/path-deps/isomorphic_drivers/src/block/ahci.rs#write_block` | `os/src/drivers/block/virtio_blk.rs#write_block` | 同板块同名 | CommandHeaderFlags::WRITE.bits, buf.len, buf.len().min, fis.set_lba, self.data[0..len].clone_from_slice, self.port.issue_command ...(+1) | assert!, buf.chunks, buf.chunks(VIRT_IO_BLOCK_SZ).enumerate, self.0
                .lock, self.0
                .lock()
                .write_blocks, self.0
                .lock()
                .write_blocks(virtio_block_id as usize, chunk)
                .expect |
| `os/path-deps/isomorphic_drivers/src/block/ahci.rs#read_block` | `os/src/drivers/block/virtio_blk.rs#read_block` | 同板块同名 | buf.len, buf.len().min, buf[..len].clone_from_slice, fis.set_lba, self.port.issue_command, self.port.spin_on_slot | assert!, buf.chunks_mut, buf.chunks_mut(VIRT_IO_BLOCK_SZ).enumerate, self.0
                .lock, self.0
                .lock()
                .read_blocks, self.0
                .lock()
                .read_blocks(virtio_block_id as usize, chunk)
                .expect |
| `os/src/drivers/block/sata_blk.rs#enable` | `os/src/drivers/block/sata_blk.rs#enable` | 同板块同名同结构 | am.read16, am.write32 | am.read16, am.write32 |

#### 网络协议栈
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `os/src/net/mod.rs#can_bind` | `os/src/net/mod.rs#can_bind` | 同板块同名 | Some, self.0.iter, socket.clone, socket.loacl_endpoint, socket.loacl_endpoint().eq, socket.socket_type ...(+1) | Some, log::info!, self.0.clone, socket.loacl_endpoint, socket.loacl_endpoint().eq, socket.socket_type ...(+1) |
| `os/src/net/config.rs#tcp_socket` | `os/src/net/config.rs#tcp_socket` | 同板块同名同结构 | f, self
            .inner
            .lock, self
            .inner
            .lock()
            .as_mut, self
            .inner
            .lock()
            .as_mut()
            .unwrap, self
            .inner
            .lock()
            .as_mut()
            .unwrap()
            .sockets
            .get_mut::<tcp::Socket> | f, self
            .inner
            .lock, self
            .inner
            .lock()
            .as_mut, self
            .inner
            .lock()
            .as_mut()
            .unwrap, self
            .inner
            .lock()
            .as_mut()
            .unwrap()
            .sockets
            .get_mut::<tcp::Socket> |
| `os/src/net/config.rs#udp_socket` | `os/src/net/config.rs#udp_socket` | 同板块同名同结构 | f, self
            .inner
            .lock, self
            .inner
            .lock()
            .as_mut, self
            .inner
            .lock()
            .as_mut()
            .unwrap, self
            .inner
            .lock()
            .as_mut()
            .unwrap()
            .sockets
            .get_mut::<udp::Socket> | f, self
            .inner
            .lock, self
            .inner
            .lock()
            .as_mut, self
            .inner
            .lock()
            .as_mut()
            .unwrap, self
            .inner
            .lock()
            .as_mut()
            .unwrap()
            .sockets
            .get_mut::<udp::Socket> |
| `os/src/net/mod.rs#from_another` | `os/src/net/mod.rs#from_another` | 同板块同名同结构 | BTreeMap::new, Ok, Self, ret.insert, socket.clone, socket_table.0.iter | BTreeMap::new, Ok, Self, ret.insert, socket.clone, socket_table.0.iter |

#### 文件系统/第三方库
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `os/src/fs/ext4/extent.rs#ext_remove_leaf` | `os/src/fs/ext4/extent.rs#ext_remove_leaf` | 同板块同名 | Block::load_inode_root_block, Block::load_offset, Ext4Extent::default, Ok, chunk.try_into, chunk.try_into().unwrap ...(+25) | Block::load_inode_root_block, Block::load_offset, Ext4Extent::default, Ok, ex.get_actual_len, ex.get_pblock ...(+14) |
| `os/src/fs/ext4/extent.rs#extent_remove_space` | `os/src/fs/ext4/extent.rs#extent_remove_space` | 同板块同名同结构 | Block::load_offset, Ext4Extent::default, Ok, ex.get_actual_len, ex.get_first_block, ex.get_pblock ...(+14) | Block::load_offset, Ext4Extent::default, Ok, ex.get_actual_len, ex.get_first_block, ex.get_pblock ...(+14) |
| `os/src/fs/ext4/balloc.rs#balloc_alloc_block_from` | `os/src/fs/ext4/balloc.rs#balloc_alloc_block_from` | 同板块同名 | Block::load_offset, Err, Ext4BlockGroup::load_new, Ok, block_group.get_block_bitmap_block, block_group.get_free_blocks_count ...(+12) | Block::load_offset, Err, Ext4BlockGroup::load_new, Ok, block_group
                        .set_block_group_balloc_bitmap_csum, block_group.get_block_bitmap_block ...(+16) |
| `os/src/fs/ext4/balloc.rs#balloc_alloc_block` | `os/src/fs/ext4/balloc.rs#balloc_alloc_block` | 同板块同名 | Block::load_offset, Err, Ext4BlockGroup::load_new, block_group.get_block_bitmap_block, block_group.get_free_blocks_count, ext4_bmap_bit_find_clr ...(+12) | Block::load_offset, Err, Ext4BlockGroup::load_new, Ok, block_group
                        .set_block_group_balloc_bitmap_csum, block_group.get_block_bitmap_block ...(+17) |

#### 网络协议栈/第三方库
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `dependency/virtio-drivers/src/device/socket/connectionmanager.rs#send_recv` | `dependency/virtio-drivers/src/device/socket/connectionmanager.rs#send_recv` | 同板块同名同结构 | "Hello from guest".as_bytes, (hello_from_guest.len() as u32).into, (hello_from_host.len() as u32).into, 0.into, 50.into, Arc::new ...(+43) | "Hello from guest".as_bytes, (hello_from_guest.len() as u32).into, (hello_from_host.len() as u32).into, 0.into, 50.into, Arc::new ...(+43) |
| `dependency/virtio-drivers/src/device/socket/connectionmanager.rs#incoming_connection` | `dependency/virtio-drivers/src/device/socket/connectionmanager.rs#incoming_connection` | 同板块同名同结构 | 0.into, 50.into, Arc::new, Mutex::new, ReadOnly::new, SocketType::Stream.into ...(+24) | 0.into, 50.into, Arc::new, Mutex::new, ReadOnly::new, SocketType::Stream.into ...(+24) |
| `dependency/virtio-drivers/examples/x86_64/src/tcp.rs#test_echo_server` | `dependency/virtio-drivers/examples/x86_64/src/tcp.rs#test_echo_server` | 同板块同名同结构 | Config::new, DeviceWrapper::new, Instant::from_micros_const, Interface::new, IpAddress::from_str, IpAddress::from_str(IP).unwrap ...(+44) | Config::new, DeviceWrapper::new, Instant::from_micros_const, Interface::new, IpAddress::from_str, IpAddress::from_str(IP).unwrap ...(+44) |
| `dependency/virtio-drivers/examples/riscv/src/tcp.rs#test_echo_server` | `dependency/virtio-drivers/examples/riscv/src/tcp.rs#test_echo_server` | 同板块同名同结构 | Config::new, DeviceWrapper::new, Instant::from_micros_const, Interface::new, IpAddress::from_str, IpAddress::from_str(IP).unwrap ...(+44) | Config::new, DeviceWrapper::new, Instant::from_micros_const, Interface::new, IpAddress::from_str, IpAddress::from_str(IP).unwrap ...(+44) |

#### 第三方库
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `dependency/dep_pci/pci/src/lib.rs#probe_function` | `dependency/dep_pci/pci/src/lib.rs#probe_function` | 同板块同名同结构 | BAR::decode, BridgeControl::from_bits_truncate, Command::from_bits_truncate, DeviceKind::CardbusBridge, DeviceKind::Device, DeviceKind::PciBridge ...(+6) | BAR::decode, BridgeControl::from_bits_truncate, Command::from_bits_truncate, DeviceKind::CardbusBridge, DeviceKind::Device, DeviceKind::PciBridge ...(+6) |
| `dependency/virtio-drivers/src/device/sound/fake.rs#handle_control_request` | `dependency/virtio-drivers/src/device/sound/fake.rs#handle_control_request` | 同板块同名同结构 | CommandCode::SOk.into, Some, Vec::new, VirtIOSndHdr {
                            command_code: CommandCode::SOk.into(),
                        }
                        .as_bytes, VirtIOSndHdr::read_from_prefix, VirtIOSndHdr::read_from_prefix(&request)
                .expect ...(+16) | CommandCode::SOk.into, Some, Vec::new, VirtIOSndHdr {
                            command_code: CommandCode::SOk.into(),
                        }
                        .as_bytes, VirtIOSndHdr::read_from_prefix, VirtIOSndHdr::read_from_prefix(&request)
                .expect ...(+16) |
| `dependency/virtio-drivers/src/transport/pci/bus.rs#bar_info` | `dependency/virtio-drivers/src/transport/pci/bus.rs#bar_info` | 同板块同名同结构 | (!(size_mask & !flag_bits)).wrapping_add, Err, MemoryBarType::try_from, Ok, Some, self
                .configuration_access
                .read_word ...(+6) | (!(size_mask & !flag_bits)).wrapping_add, Err, MemoryBarType::try_from, Ok, Some, self
                .configuration_access
                .read_word ...(+6) |
| `dependency/virtio-drivers/src/device/input.rs#config` | `dependency/virtio-drivers/src/device/input.rs#config` | 同板块同名同结构 | "Serial number".as_bytes, "Test input device".as_bytes, Arc::new, Default::default, Mutex::new, QUEUE_SIZE.try_into ...(+14) | "Serial number".as_bytes, "Test input device".as_bytes, Arc::new, Default::default, Mutex::new, QUEUE_SIZE.try_into ...(+14) |

#### 通用模块/其他
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `user/src/lib.rs#_start` | `user/src/lib.rs#_start` | 同板块同名 | _parameter, asm!, core::arch::asm!, unreachable! | _parameter, asm!, core::arch::asm!, unreachable! |
| `os/src/hal/arch/loongarch64/time.rs#get_timer_freq_first_time` | `os/src/hal/arch/loongarch64/time.rs#get_timer_freq_first_time` | 同板块同名 | cfg5.get_bits, config::CPUCfg4::read, config::CPUCfg4::read().get_bits, config::CPUCfg5::read, println! | cfg5.get_bits, config::CPUCfg4::read, config::CPUCfg4::read().get_bits, config::CPUCfg5::read, println! |
| `user/src/lib.rs#_parameter` | `user/src/lib.rs#_parameter` | 同板块同名 | ((argv + i * core::mem::size_of::<usize>()) as *const usize).read_volatile, ((str_start + *i) as *const u8).read_volatile, (0usize..)
            .find, (0usize..)
            .find(|i| unsafe { ((str_start + *i) as *const u8).read_volatile() == 0 })
            .unwrap, Vec::new, core::mem::size_of::<usize> ...(+8) | ((argv + i * core::mem::size_of::<usize>()) as *const usize).read_volatile, ((str_start + *i) as *const u8).read_volatile, (0usize..)
            .find, (0usize..)
            .find(|i| unsafe { ((str_start + *i) as *const u8).read_volatile() == 0 })
            .unwrap, HEAP.lock, HEAP.lock()
            .init ...(+10) |
| `os/src/hal/arch/loongarch64/boot.rs#_start` | `os/src/hal/arch/loongarch64/boot.rs#_start` | 同板块同名同结构 | naked_asm! | naked_asm! |


好的，我将遵循审计指令，基于提供的所有证据材料，进行最终的同源性定性仲裁。

### 1. 宏观判定
- **定性结论**: **独立开发，底层架构显著差异**
  尽管整体 AST 相似度为 44.1%，但深入分析揭示，该相似度主要由双方共同引用的第三方库和遵循相同 POSIX/Linux 标准接口的样板代码贡献。在操作系统核心子系统中，双方在架构设计、同步策略、内存模型上存在根本性分歧。目标项目（覆盖率 50.15%）展现出明确的多核、多调度域演进特征，而参考项目（覆盖率 78.51%）为单核、简单调度模型。参考项目的高覆盖率表明其代码规模较小，其大部分代码能在规模更大的目标项目中找到对应，这符合“子集”特征而非“抄袭”特征。

### 2. 全模块覆盖矩阵解读
- **进程管理**:
  1. 双方均存在该模块。
  2. 存在共同 AST 指纹。
  3. 目标覆盖率仅 3.47%，参考覆盖率 20.9%。极低的目标覆盖率表明，目标项目在此模块有大量独立新增代码，远非对参考项目的简单复用。
  4. 证据强度：**弱**。同名函数多由 Linux ABI 规定，核心实现路径（锁、调度器）已发生根本性变化。

- **任务调度/异步运行时**:
  1. 双方均存在该模块。
  2. 存在共同 AST 指纹。
  3. 目标覆盖率 12.37%，参考覆盖率 38.89%。覆盖率差异显著，说明目标项目扩展了远超参考项目的调度逻辑。
  4. 证据强度：**弱**。目标项目引入了 `cfs_rq`、`rt_rq` 等多调度域，与参考项目的简单 `ready_queue` 模型有本质区别。

- **内存管理**:
  1. 双方均存在该模块。
  2. 存在共同 AST 指纹。
  3. 目标覆盖率 41.48%，参考覆盖率 70.19%。这是核心模块中覆盖率较高的区域，需要仔细甄别。
  4. 证据强度：**中**。部分函数（如 `sys_clone`）调用链长且复杂，存在结构相似性，但目标项目在锁粒度（`task.heap.lock` vs `acquire_inner_lock`）和资源获取方式（`snapshot_self_rusage` vs `inner.rusage`）上展现出独立设计。

- **中断/异常处理**:
  1. 双方均存在该模块。
  2. 存在共同 AST 指纹。
  3. 目标覆盖率 46.84%，参考覆盖率 92.5%。参考覆盖率极高，说明此模块逻辑相对固定且参考项目实现完整。
  4. 证据强度：**中**。`trap_handler` 等函数因硬件架构（LoongArch64/RISC-V）规定，其操作寄存器的序列必然高度相似，属于领域强约束。

- **系统调用**:
  1. 双方均存在该模块。
  2. 存在共同 AST 指纹。
  3. 目标覆盖率 23.7%，参考覆盖率 61.54%。
  4. 证据强度：**弱**。此模块多为转发调用，其相似性源于对同一套 POSIX/Linux 系统调用号的响应，是接口分发层的标准模式。

- **文件系统**:
  1. 双方均存在该模块。
  2. 存在共同 AST 指纹。
  3. 目标覆盖率 35.64%，参考覆盖率 76.58%。
  4. 证据强度：**弱**。共同指纹多集中在 ext4 等第三方文件系统实现上，属于公共依赖。

- **设备驱动**:
  1. 双方均存在该模块。
  2. 存在共同 AST 指纹。
  3. 目标覆盖率 49.37%，参考覆盖率 82.98%。
  4. 证据强度：**弱**。共同指纹主要来自 `sata_blk` 和 `virtio-drivers` 等公共驱动框架或第三方库。

- **网络协议栈**:
  1. 双方均存在该模块。
  2. 存在共同 AST 指纹。
  3. 目标覆盖率 39.44%，参考覆盖率 59.57%。
  4. 证据强度：**弱**。共同指纹多来自 `smoltcp` 等第三方网络栈的封装，属于公共依赖。

- **第三方库/公共依赖**:
  1. 双方均存在。
  2. 存在大量共同 AST 指纹。
  3. 覆盖率极高（目标 71.2%~100%，参考 77.85%~100%）。
  4. 证据强度：**未形成**。这些模块（如 `virtio-drivers`, `pci`, `ext4`）是明确的开源第三方库，其高相似度是共同引用的结果，不能作为项目间抄袭的证据。

### 3. 底层 AST 关键证据指纹表
| 所属板块 | 目标项目路径 | 参考项目路径 | 证据类型 | 技术说明 |
| :--- | :--- | :--- | :--- | :--- |
| 进程管理 | `os/src/task/signal.rs#sigprocmask` | `os/src/task/signal.rs#sigprocmask` | 同板块同名，调用序列相似 | 两者均遵循 `SigMaskHow::from_bits` -> `current_task` -> `acquire_inner_lock` -> `sigmask.insert/remove` 的标准信号掩码修改流程。这是 POSIX 语义下的标准实现，但目标项目增加了 `read_rt_sigset` 调用。 |
| 进程管理 | `os/src/syscall/process.rs#sys_getrusage` | `os/src/syscall/process.rs#sys_getrusage` | 同板块同名，调用序列相似 | 核心流程均为 `current_task` -> `acquire_inner_lock` -> `get_user_token` -> `copy_to_user`。但目标项目通过 `task.snapshot_self_rusage()` 获取数据，参考项目直接访问 `inner.rusage`，数据获取路径不同。 |
| 内存管理 | `os/src/task/task.rs#sys_clone` | `os/src/task/task.rs#sys_clone` | 同板块同名，调用图谱复杂且相似 | 两者均包含 `Arc::new`, `Futex::new`, `ITimerVal::new`, `MemorySet::from_existing_user` 等大量共同调用，表明 `clone` 系统调用的实现框架高度雷同。 |
| 中断/异常处理 | `os/src/hal/arch/loongarch64/trap/mod.rs#trap_handler` | `os/src/hal/arch/loongarch64/trap/mod.rs#trap_handler` | 同板块同名，调用图谱高度一致 | 两者均包含 `BadV::read`, `ERA::read`, `(0..sz).rev` 等操作。这是处理 LoongArch64 架构陷阱的硬件规定序列，属于领域强约束，但如此长且一致的序列仍具高参考价值。 |
| 中断/异常处理 | `os/src/hal/arch/loongarch64/trap/mem_access.rs#get_addr` | `os/src/hal/arch/loongarch64/trap/mem_access.rs#get_addr` | 同板块同名同结构 | 调用图谱完全一致，均为 `self.get_op_code` -> `self.get_rj_num` -> `self.get_rk_num` 等。这是对 LoongArch64 指令格式的解析，实现方式高度雷同。 |
| 第三方库/公共依赖 | `dependency/virtio-drivers/src/device/socket/connectionmanager.rs#send_recv` | `dependency/virtio-drivers/src/device/socket/connectionmanager.rs#send_recv` | 同板块同名同结构 | 调用图谱完全一致，包含 `“Hello from guest”.as_bytes` 等测试特征字符串。这是对 `virtio-drivers` 开源库示例代码的直接复用。 |
| 第三方库/公共依赖 | `dependency/dep_pci/pci/src/lib.rs#probe_function` | `dependency/dep_pci/pci/src/lib.rs#probe_function` | 同板块同名同结构 | 调用图谱完全一致，包含 `BAR::decode`, `DeviceKind::PciBridge` 等。这是对 `pci` 开源库核心逻辑的直接复用。 |

### 4. 核心代码调用流对比剖析
* **【进程管理/调度与退出路径】**
* **底层技术析理**: 此路径揭示了双方在调度器架构上的根本差异。参考项目采用全局单核模型，通过 `take_current_task` 直接取走当前任务并调用 `schedule`。目标项目则实现了多核调度，通过 `current_cpu_id` 定位当前 CPU 的 `PROCESSORS` 结构，并在关中断环境下通过 `set_pending` 设置待处理状态，最后调用 `schedule`。两者的任务管理对象（`PROCESSOR` vs `PROCESSORS[cpu_id]`）和同步策略（无 vs `disable_interrupts`）完全不同。
* **代码溯源链接**:
  - 目标项目: `os/src/task/mod.rs#exit_current_and_run_next`
  - 参考项目: `os/src/task/mod.rs#exit_current_and_run_next`

* **【内存管理/资源使用量获取】**
* **底层技术析理**: 两者均实现了 `sys_getrusage` 系统调用，但数据获取模型不同。参考项目直接访问任务内部结构 `inner.rusage`，这是一种简单的、紧密耦合的数据访问方式。目标项目则通过调用 `task.snapshot_self_rusage()` 方法来获取资源使用快照，这体现了更好的封装和抽象层次，将资源统计逻辑从系统调用处理函数中分离出来。
* **代码溯源链接**:
  - 目标项目: `os/src/syscall/process.rs#sys_getrusage`
  - 参考项目: `os/src/syscall/process.rs#sys_getrusage`

* **【中断/异常处理/LoongArch64 Trap 处理】**
* **底层技术析理**: 此路径是领域强约束的典型代表。对于 LoongArch64 架构的页错误等陷阱，硬件规定了必须读取 `BadV`（错误虚拟地址）和 `ERA`（异常返回地址）等寄存器，并按固定格式解析指令。因此，两者在 `trap_handler` 中展现出高度一致的寄存器读取和指令解码序列。这是硬件 ABI 决定的，而非代码抄袭。
* **代码溯源链接**:
  - 目标项目: `os/src/hal/arch/loongarch64/trap/mod.rs#trap_handler`
  - 参考项目: `os/src/hal/arch/loongarch64/trap/mod.rs#trap_handler`

### 5. 单方模块与降权项
- **单方模块**: 无。所有宏观模块在双方项目中均存在。
- **降权项**:
  - **第三方库/公共依赖**: `virtio-drivers`, `pci`, `ext4`, `fat32` 等目录下的代码。这些是明确的开源项目或参考实现，其高 AST 相似度是共同引用的结果，不构成项目间的抄袭证据。
  - **公共接口/样板代码**: 大量 `sys_*` 开头的系统调用包装函数（如 `sys_sigaction`, `sys_getpgid`），其函数名由 Linux ABI 规定，内部逻辑仅为简单的参数转发或状态检查，调用链极短，属于任何实现该接口的操作系统都会产生的样板代码，证明力极弱。
  - **硬件强约束代码**: 如 `os/src/hal/arch/loongarch64/trap/` 下的指令解析和寄存器操作，其实现由硬件手册规定，不同项目的实现必然趋同。

### 6. 保守结论
综合全模块证据，目标项目 `[2026-T202610699999765-XX]` 与参考项目 `[2025-西北工业大学一二三队-西北工业大学]` 的 44.1% AST 相似度，主要由共同引用的第三方开源库和遵循相同 POSIX/Linux 标准接口的样板代码贡献。在操作系统核心子系统中，双方在进程调度（多核 vs 单核）、同步策略（细粒度锁+关中断 vs 全局锁）、内存管理抽象（快照方法 vs 直接访问）等关键架构设计上存在实质性差异。虽然部分底层陷阱处理和系统调用实现存在结构相似性，但这可以用硬件强约束和领域标准实践来解释。整体来看，现有证据不支持“结构级抄袭与深度复用”的指控，项目更符合独立开发的特征，整体同源性风险较低。

------------------------------

