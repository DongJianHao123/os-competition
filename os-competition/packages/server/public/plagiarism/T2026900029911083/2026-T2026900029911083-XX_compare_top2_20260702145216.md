# 🛡️ OS 内核赛自动化代码级架构审计报告

**审计时间**: `2026-07-02 14:52:16`
**参赛作品名称**: `2026-T2026900029911083-XX`
**年份**: `2026`
**学校**: `XX`
**队伍名称**: `T2026900029911083`
**比赛名字**: `OS-Competition`
**比赛子赛道**: `Kernel-Implementation`
**仓库地址**: `https://gitlab.eduxiji.net/educg-group-43501-3132633/T2026900029911083-3735`
**推测开发语言**: `Rust / C (由AST探针自动识别)`
**特征函数量**: `1031`

## ⚔️ 底层代码同源性对比 (AST Homology)
> 本部分由专门的代码审查智能体，对该项目与符合年份条件的历届最高相似度项目进行控制流与底层调用对比。新版报告会先给出确定性的全模块覆盖矩阵，再给出 Agent 的分模块解释，避免只围绕少量抽样函数展开。

### 🏆 Top 2 底层对标: vs `2025-啊对的对的嗷不对不对-华中科技大学`（华中科技大学 / 啊对的对的,嗷不对不对）

#### 对方作品信息

**对方年份**: `2025`
**对方学校**: `华中科技大学`
**对方队伍名称**: `啊对的对的,嗷不对不对`
**对方比赛名字**: `OS-Competition`
**对方比赛子赛道**: `Kernel-Implementation`
**对方仓库地址**: `https://gitlab.eduxiji.net/educg-group-36002-2710490/T202510487995136-2371`

> 双向覆盖率：共同 AST 指纹: 597；唯一 AST 指纹: 目标项目 878，参考项目 873；覆盖率: 目标项目 68.0%，参考项目 68.38%

## 全模块对比总览（确定性证据）

> 总体覆盖率：共同 AST 指纹: 597；唯一 AST 指纹: 目标项目 878，参考项目 873；覆盖率: 目标项目 68.0%，参考项目 68.38%

### 1. 模块覆盖矩阵
| 模块 | 目标函数数 | 参考函数数 | 共同唯一 AST 指纹 | Target 覆盖 | Base 覆盖 | 状态 |
| :--- | ---: | ---: | ---: | ---: | ---: | :--- |
| 进程管理 | 56 | 87 | 23 | 48.94% | 30.26% | 双方存在且有结构重合 |
| 任务调度/异步运行时 | 64 | 33 | 13 | 20.63% | 40.62% | 双方存在且有结构重合 |
| 内存管理 | 162 | 164 | 47 | 30.13% | 31.33% | 双方存在且有结构重合 |
| 中断/异常处理 | 35 | 28 | 13 | 46.43% | 54.17% | 双方存在且有结构重合 |
| 系统调用 | 7 | 10 | 2 | 40.0% | 28.57% | 双方存在且有结构重合 |
| 文件系统 | 109 | 66 | 26 | 27.66% | 49.06% | 双方存在且有结构重合 |
| 设备驱动 | 38 | 50 | 27 | 87.1% | 67.5% | 双方存在且有结构重合 |
| 网络协议栈 | 0 | 0 | 0 | 0.0% | 0.0% | 双方均未体现 |
| 文件系统/第三方库 | 489 | 537 | 389 | 95.34% | 86.83% | 双方存在且有结构重合 |
| 通用模块/其他 | 71 | 68 | 37 | 56.92% | 62.71% | 双方存在且有结构重合 |

### 2. 分模块证据清单

#### 进程管理
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `kernel/proc/proc.c#scheduler` | `kernel/proc/proc.c#scheduler` | 同板块同名 | acquire, intr_on, mycpu, release, swtch | acquire, intr_on, mycpu, release, swtch |
| `kernel/proc/proc.c#forkret` | `kernel/proc/proc.c#forkret` | 同板块同名 | filesystem_init, myproc, release, usertrapret | filesystem2_init, filesystem_init, myproc, release, usertrapret |
| `kernel/syscall/sysproc.c#sys_set_robust_list` | `kernel/sys/sysproc.c#sys_set_robust_list` | 同板块同名 | argaddr, myproc | 内部闭环逻辑 |
| `user/sh.c#parseexec` | `user/app/sh.c#parseexec` | 同板块同名同结构 | execcmd, gettoken, panic, parseblock, parseredirs, peek | execcmd, gettoken, panic, parseblock, parseredirs, peek |

#### 任务调度/异步运行时
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `kernel/syscall/sysfile.c#sys_faccessat` | `kernel/sys/sysfile.c#sys_faccessat` | 同板块同名 | argint, argstr, get_absolute_path, k_str_contains, myproc, strcmp ...(+1) | argint, argstr, get_absolute_path, myproc, vfs_ext_faccessat |
| `kernel/drive/la/virtio_pci.c#virtio_pci_read_caps` | `kernel/driver/loongarch/virtio_pci.c#virtio_pci_read_caps` | 同板块同名同结构 | get_cfg_addr, pci_config_read, pci_config_read8, printf | get_cfg_addr, pci_config_read, pci_config_read8, printf |
| `kernel/util/printf.c#printf` | `kernel/lib/printf.c#printf` | 同板块同名 | acquire, consputc, printint, printptr, release, va_arg ...(+2) | acquire, consputc, panic, printint, printptr, release ...(+3) |
| `kernel/syscall/sysfile.c#sys_renameat2` | `kernel/sys/sysfile.c#sys_renameat2` | 同板块同名 | argint, argstr, get_absolute_path, get_fs_from_path, myproc, printf ...(+3) | argint, argstr, get_absolute_path, myproc, vfs_ext_frename |

#### 内存管理
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `kernel/syscall/sysproc.c#sys_clock_nanosleep` | `kernel/sys/sysproc.c#sys_clock_nanosleep` | 同板块同名 | acquire, argaddr, argint, copyin, copyout, exit ...(+6) | acquire, argaddr, copyin, copyout, killed, myproc ...(+2) |
| `kernel/proc/exec.c#execve` | `kernel/proc/exec.c#execve` | 同板块同名 | AUXV, PGROUNDDOWN, PGROUNDUP, acquire, copyout, ip->i_op->lock ...(+12) | PGROUNDDOWN, PGROUNDUP, alloc_aux, copyout, ip->i_op->lock, ip->i_op->read ...(+13) |
| `kernel/syscall/sysfile.c#sys_execve` | `kernel/sys/sysfile.c#sys_execve` | 同板块同名 | NELEM, argaddr, argstr, execve, fetchaddr, fetchstr ...(+6) | NELEM, argaddr, argstr, execve, fetchaddr, fetchstr ...(+6) |
| `kernel/drive/la/pci.c#pci_scan_device` | `kernel/driver/loongarch/pci.c#pci_scan_device` | 同板块同名 | pci_alloc_device, pci_device_bar_init, pci_device_init, pci_read_config, pci_write_config, printf | pci_alloc_device, pci_device_bar_init, pci_device_init, pci_read_config, pci_write_config, printf |

#### 中断/异常处理
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `kernel/drive/rv/plic.c#plicinithart` | `kernel/sys/plic.c#plicinithart` | 同板块同名 | PLIC_SENABLE, PLIC_SPRIORITY, cpuid, printf | PLIC_SENABLE, PLIC_SPRIORITY, cpuid |
| `kernel/drive/la/pci.c#pci_device_init` | `kernel/driver/loongarch/pci.c#pci_device_init` | 同板块同名同结构 | 内部闭环逻辑 | 内部闭环逻辑 |
| `kernel/drive/rv/virtio_disk.c#virtio_disk_intr` | `kernel/driver/riscv/virtio_disk.c#virtio_disk_intr` | 同板块同名同结构 | R, acquire, panic, release, wakeup | R, acquire, panic, release, wakeup |
| `kernel/trap/la/apic.c#apic_init` | `kernel/trap/loongarch/apic.c#apic_init` | 同板块同名 | iointc_2k1000_init | 内部闭环逻辑 |

#### 系统调用
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `kernel/syscall/syssig.c#sys_tgkill` | `kernel/sys/syssig.c#sys_tgkill` | 同板块同名 | argint, send_signal_thread | argint, printf |
| `kernel/syscall/syscall.c#argstr` | `kernel/sys/syscall.c#argstr` | 同板块同名 | argaddr, fetchstr | argaddr, fetchstr |
| `kernel/syscall/syssig.c#sys_tkill` | `kernel/sys/syssig.c#sys_tkill` | 同板块同名同结构 | argint, send_signal_thread | argint, tkill |
| `kernel/syscall/syscall.c#argint` | `kernel/sys/syscall.c#argint` | 同板块同名同结构 | argraw | argraw |

#### 文件系统
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `kernel/fs/vfs/ops.c#get_absolute_path` | `kernel/fs/vfs/ops.c#get_absolute_path` | 同板块同名 | strcat, strcpy, strlen | strcat, strcpy, strlen, strncpy |
| `kernel/drive/la/pci.c#pci_device_probe` | `kernel/driver/loongarch/pci.c#pci_device_probe` | 同板块同名同结构 | PCI_ADDR, __sync_synchronize, printf | PCI_ADDR, __sync_synchronize, printf |
| `kernel/fs/vfs/file.c#filewrite` | `kernel/fs/vfs/file.c#filewrite` | 同板块同名 | devsw[f->f_major].write, get_fops, get_fops()->writable, panic, pipewrite, printf ...(+1) | devsw[f->f_major].write, get_fops, get_fops()->writable, panic, pipewrite, vfs_ext_write |
| `kernel/drive/la/virtio_pci.c#virtio_pci_setup_queue` | `kernel/driver/loongarch/virtio_pci.c#virtio_pci_setup_queue` | 同板块同名同结构 | PCI_REG16, PCI_REG32, PCI_REG64, printf | PCI_REG16, PCI_REG32, PCI_REG64, printf |

#### 设备驱动
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `kernel/drive/la/pci.c#pci_device_bar_init` | `kernel/driver/loongarch/pci.c#pci_device_bar_init` | 同板块同名同结构 | 内部闭环逻辑 | 内部闭环逻辑 |
| `kernel/drive/la/virtio_ring.c#virtio_vring_init` | `kernel/driver/loongarch/virtio_ring.c#virtio_vring_init` | 同板块同名同结构 | VQ_ALIGN, virtio_vring_size | VQ_ALIGN, virtio_vring_size |
| `kernel/drive/la/virtio_pci.c#virtio_pci_set_queue_addr2` | `kernel/driver/loongarch/virtio_pci.c#virtio_pci_set_queue_addr2` | 同板块同名同结构 | PCI_REG16, PCI_REG64, VIRT2PHY, dsb | PCI_REG16, PCI_REG64, VIRT2PHY, dsb |
| `kernel/drive/uart.c#uartstart` | `kernel/mem/uart.c#uartstart` | 同板块同名 | ReadReg, WriteReg, wakeup | ReadReg, WriteReg, wakeup |

#### 文件系统/第三方库
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `kernel/fs/lwext4/ext4.c#ext4_dir_rm` | `kernel/fs/ext4/lwext4/ext4.c#ext4_dir_rm` | 同板块同名同结构 | EXT4_MP_LOCK, EXT4_MP_UNLOCK, ext4_block_cache_write_back, ext4_dir_en_get_inode, ext4_dir_iterator_fini, ext4_dir_iterator_init ...(+19) | EXT4_MP_LOCK, EXT4_MP_UNLOCK, ext4_block_cache_write_back, ext4_dir_en_get_inode, ext4_dir_iterator_fini, ext4_dir_iterator_init ...(+19) |
| `kernel/fs/lwext4/ext4_balloc.c#ext4_balloc_alloc_block` | `kernel/fs/ext4/lwext4/ext4_balloc.c#ext4_balloc_alloc_block` | 同板块同名同结构 | ext4_balloc_get_bgid_of_block, ext4_balloc_get_block_of_bgid, ext4_balloc_set_bitmap_csum, ext4_balloc_verify_bitmap_csum, ext4_bg_get_block_bitmap, ext4_bg_get_free_blocks_count ...(+19) | ext4_balloc_get_bgid_of_block, ext4_balloc_get_block_of_bgid, ext4_balloc_set_bitmap_csum, ext4_balloc_verify_bitmap_csum, ext4_bg_get_block_bitmap, ext4_bg_get_free_blocks_count ...(+19) |
| `kernel/fs/lwext4/ext4_journal.c#jbd_journal_prepare` | `kernel/fs/ext4/lwext4/ext4_journal.c#jbd_journal_prepare` | 同板块同名 | EXT4_BLOCK_ZERO, RB_FIND, TAILQ_FOREACH_REVERSE_SAFE, TAILQ_FOREACH_SAFE, TAILQ_REMOVE, ext4_bcache_clear_dirty ...(+17) | EXT4_BLOCK_ZERO, RB_FIND, TAILQ_FOREACH_REVERSE_SAFE, TAILQ_FOREACH_SAFE, TAILQ_REMOVE, ext4_bcache_clear_dirty ...(+17) |
| `kernel/fs/lwext4/ext4_dir_idx.c#ext4_dir_dx_split_index` | `kernel/fs/ext4/lwext4/ext4_dir_idx.c#ext4_dir_dx_split_index` | 同板块同名同结构 | ext4_block_set, ext4_dir_dx_climit_get_count, ext4_dir_dx_climit_get_limit, ext4_dir_dx_climit_set_count, ext4_dir_dx_climit_set_limit, ext4_dir_dx_entry_get_hash ...(+10) | ext4_block_set, ext4_dir_dx_climit_get_count, ext4_dir_dx_climit_get_limit, ext4_dir_dx_climit_set_count, ext4_dir_dx_climit_set_limit, ext4_dir_dx_entry_get_hash ...(+10) |

#### 通用模块/其他
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `user/printf.c#vprintf` | `user/deps/printf.c#vprintf` | 同板块同名 | putc_buf, va_arg, write | printint, printptr, putc, va_arg |
| `user/wc.c#wc` | `user/app/wc.c#wc` | 同板块同名同结构 | exit, printf, read, strchr | exit, printf, read, strchr |
| `user/grep.c#grep` | `user/app/grep.c#grep` | 同板块同名同结构 | match, memmove, read, strchr, write | match, memmove, read, strchr, write |
| `kernel/util/printf.c#printint` | `user/deps/printf.c#printint` | 同板块同名 | consputc | putc |


好的，审计官。基于提供的证据底座、控辩双方陈述及AST抽样数据，现出具最终同源性定性仲裁报告。

---

#### 1. 宏观判定
- **定性结论**: **部分逻辑借鉴，存在显著架构改动** (相似度: 51.73%，共同 AST 指纹: 597；唯一 AST 指纹: 目标项目 878，参考项目 873；覆盖率: 目标项目 68.0%，参考项目 68.38%)
- **判定依据**: 双方项目均基于xv6或类似教学内核的公共代码基础，在进程调度、中断处理、系统调用等核心机制上存在必然的规范相似性。然而，超过30%的独有AST指纹及在内存管理、设备驱动、系统调用语义上的具体实现差异，表明目标项目进行了大量的独立开发与架构适配，不构成对参考项目的整体源码抄袭。第三方库（lwext4）的高度重合是双方独立引入同一上游开源代码所致，不应计入原创性抄袭证据。

#### 2. 全模块覆盖矩阵解读
- **进程管理**: 双方均存在。存在共同AST指纹。目标覆盖率48.94%，参考覆盖率30.26%。双方在核心调度原语（`sched`, `sleep`, `yield`）上高度相似，符合xv6规范。但参考项目函数更多，目标项目有超过一半的独立指纹，表明存在独立的进程管理逻辑扩展。证据强度：**中**。
- **任务调度/异步运行时**: 双方均存在。存在共同AST指纹。目标覆盖率20.63%，参考覆盖率40.62%。覆盖率差异显著，表明双方在调度与运行时框架上采用了不同的实现策略或功能范围。证据强度：**弱**。
- **内存管理**: 双方均存在。存在共同AST指纹。目标覆盖率30.13%，参考覆盖率31.33%。覆盖率低且相近，说明双方在`execve`等核心函数外，内存管理的大部分实现是独立的。证据强度：**弱**。
- **中断/异常处理**: 双方均存在。存在共同AST指纹。目标覆盖率46.43%，参考覆盖率54.17%。在PLIC、PCI设备初始化等底层硬件处理上存在相似性，这与硬件手册规定的初始化序列强相关。证据强度：**中**。
- **系统调用**: 双方均存在。存在共同AST指纹。目标覆盖率40.0%，参考覆盖率28.57%。覆盖率不高，且从证据看，双方在信号处理（`sys_tgkill`）等系统调用上存在语义差异。证据强度：**弱**。
- **文件系统**: 双方均存在。存在共同AST指纹。目标覆盖率27.66%，参考覆盖率49.06%。覆盖率差异大，参考项目文件系统模块更为精简，目标项目则可能包含更多独立开发的VFS或特殊文件系统逻辑。证据强度：**弱**。
- **设备驱动**: 双方均存在。存在共同AST指纹。目标覆盖率87.1%，参考覆盖率67.5%。覆盖率极高，主要因为双方都实现了virtio PCI设备驱动，其初始化及队列配置流程由virtio标准严格定义，导致AST高度重合。证据强度：**中**。
- **网络协议栈**: 双方均未体现。证据强度：**未形成**。
- **第三方库/公共依赖**: 双方均存在。存在大量共同AST指纹。目标覆盖率95.34%，参考覆盖率86.83%。此模块是整体相似度的主要贡献者。双方均集成了开源的`lwext4`文件系统库，路径分别为`kernel/fs/lwext4`和`kernel/fs/ext4/lwext4`，这是对同一公共代码库的独立引用，不构成互相抄袭。证据强度：**弱（作为原创性证据）**。

#### 3. 底层 AST 关键证据指纹表
| 所属板块 | 目标项目路径 | 参考项目路径 | 证据类型 | 技术说明 |
| :--- | :--- | :--- | :--- | :--- |
| 进程管理 | `kernel/proc/proc.c#scheduler` | `kernel/proc/proc.c#scheduler` | 同板块同名同调用流 | 标准xv6调度器循环：`acquire` → `intr_on` → `swtch` → `release`。调用图谱完全一致，是遵循同一教学内核架构的强证据，但非抄袭铁证。 |
| 进程管理 | `kernel/proc/proc.c#sched` | `kernel/proc/proc.c#sched` | 同板块同名同调用流 | 上下文切换核心，包含`holding`锁校验、`intr_get`中断检查、`swtch`切换及`panic`路径，是xv6的规范安全逻辑。 |
| 进程管理 | `user/sh.c#parseexec` | `user/app/sh.c#parseexec` | 同板块同名同调用流 | xv6 Shell标准递归下降解析器，调用`execcmd`, `parseredirs`等，其结构由Shell语法唯一确定。 |
| 设备驱动 | `kernel/drive/la/virtio_pci.c#virtio_pci_read_caps` | `kernel/driver/loongarch/virtio_pci.c#virtio_pci_read_caps` | 同板块同名同调用流 | virtio PCI能力读取流程，调用`pci_config_read`等。此流程由virtio规范定义，不同架构（LoongArch vs RISC-V）下的实现必然相似。 |
| 设备驱动 | `kernel/drive/la/virtio_pci.c#virtio_pci_setup_queue` | `kernel/driver/loongarch/virtio_pci.c#virtio_pci_setup_queue` | 同板块同名同调用流 | virtio队列初始化，涉及`PCI_REG16/32/64`宏操作，是virtio标准初始化序列的体现。 |
| 中断/异常处理 | `kernel/drive/rv/virtio_disk.c#virtio_disk_intr` | `kernel/driver/riscv/virtio_disk.c#virtio_disk_intr` | 同板块同名同调用流 | virtio磁盘中断处理，调用`acquire`, `release`, `wakeup`。中断处理的“加锁-操作-唤醒”模式是内核编程的通用范式。 |
| 第三方库/公共依赖 | `kernel/fs/lwext4/ext4.c#ext4_dir_rm` | `kernel/fs/ext4/lwext4/ext4.c#ext4_dir_rm` | 同板块同名同调用流 | 双方均使用开源lwext4库，此函数调用图谱完全一致（含20+内部调用），是引用同一第三方库的典型特征，非互相抄袭。 |
| 第三方库/公共依赖 | `kernel/fs/lwext4/ext4_balloc.c#ext4_balloc_alloc_block` | `kernel/fs/ext4/lwext4/ext4_balloc.c#ext4_balloc_alloc_block` | 同板块同名同调用流 | 同上，lwext4库内部函数，块分配逻辑复杂且调用链长，高度一致源于引用同一代码库。 |

#### 4. 核心代码调用流对比剖析
* **【进程管理/调度原语】**
* **底层技术析理**: 双方在`sleep`和`yield`等函数中均采用“`acquire`锁 → 修改进程状态 → `sched`调度 → `release`锁”的调用流。这是xv6内核的经典同步与调度范式，其调用图谱由架构强制决定，不反映独创性。差异点在于`sys_sleep`中，目标项目未显式调用`killed`检查，可能将信号处理逻辑下沉或采用不同睡眠语义，体现了实现细节上的分歧。
* **代码溯源链接**:
  - 目标项目: `kernel/proc/proc.c#sleep`
  - 参考项目: `kernel/proc/proc.c#sleep`

* **【设备驱动/virtio驱动】**
* **底层技术析理**: 双方在`kernel/drive`和`kernel/driver`目录下均实现了virtio PCI设备驱动。`virtio_pci_read_caps`和`virtio_pci_setup_queue`等函数的调用图谱高度一致，这是因为virtio规范严格定义了PCI配置空间的访问顺序和队列初始化步骤。目标项目路径中的`la`（LoongArch）与参考项目中的`loongarch`表明双方均针对龙芯架构进行了适配，但具体实现文件路径和内部宏定义可能存在差异。
* **代码溯源链接**:
  - 目标项目: `kernel/drive/la/virtio_pci.c#virtio_pci_read_caps`
  - 参考项目: `kernel/driver/loongarch/virtio_pci.c#virtio_pci_read_caps`

* **【第三方库/文件系统】**
* **底层技术析理**: `ext4_dir_rm`等函数的调用图谱完全一致，包含`ext4_dir_iterator_init`、`ext4_block_cache_write_back`等20余个内部调用。这是双方项目均直接集成了开源的`lwext4`库的明确证据。目标项目路径为`kernel/fs/lwext4/`，参考项目为`kernel/fs/ext4/lwext4/`，表明是各自独立引入的公共依赖，此部分相似度不能作为抄袭指控的依据。
* **代码溯源链接**:
  - 目标项目: `kernel/fs/lwext4/ext4.c#ext4_dir_rm`
  - 参考项目: `kernel/fs/ext4/lwext4/ext4.c#ext4_dir_rm`

#### 5. 单方模块与降权项
- **第三方库/公共依赖**: `lwext4`文件系统库是双方共有的开源依赖，其内部所有函数（如`ext4_*`系列）的高度重合是引用公共代码的必然结果，**降权为弱证据**，不指向任何一方的原创性抄袭。
- **通用原语与宏**: `printf`, `panic`, `acquire`, `release`, `NELEM`, `va_arg`等调用是C语言和操作系统内核编程的通用原语、标准库函数或常见宏。它们在不同上下文中的调用不能作为同源证据，**予以降权**。
- **单方模块**: 目标项目独有的878个AST指纹和参考项目独有的873个AST指纹，是双方独立开发工作的直接体现。这些指纹分布在各个模块中，是驳斥“大面积抄袭”指控的核心数据支撑。
- **跨板块证据**: 控方将`user/sh.c#parseexec`（用户态Shell）与内核进程管理函数并列，属于跨板块硬凑。Shell解析器与内核调度器在技术上分属不同层次，应独立评估，**降权为弱信号**。

#### 6. 保守结论
本次审计的客观AST相似度为51.73%，处于中等水平。该相似度主要由两部分构成：一是双方共同遵循的xv6教学内核架构及virtio等硬件标准所导致的必然相似性；二是双方独立引入的`lwext4`开源文件系统库。剥离第三方库和通用原语后，核心原创模块的相似度显著降低。双方在内存管理、系统调用语义、文件系统架构等方面存在超过30%的独有AST指纹和明确的实现差异（如`sys_clone`、`sys_shutdown`的调用流分歧），这不符合源码级抄袭后简单“洗稿”的特征，更倾向于在公共代码基础上的独立演进与架构适配。因此，整体风险等级为**中等**，不支持“大面积逐字复用”的抄袭指控，但建议对进程管理、设备驱动等中等证据强度的模块进行人工代码审查，以排除对特定复杂逻辑的借鉴行为。

------------------------------

