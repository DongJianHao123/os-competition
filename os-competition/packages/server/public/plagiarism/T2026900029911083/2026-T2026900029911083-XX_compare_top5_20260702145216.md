# 🛡️ OS 内核赛自动化代码级架构审计报告

**审计时间**: `2026-07-02 14:52:16`
**参赛作品名称**: `2026-T2026900029911083-XX`
**年份**: `2026`
**学校**: `XX`
**队伍名称**: `T2026900029911083`
**比赛名字**: `OS-Competition`
**比赛子赛道**: `Kernel-Implementation`
**仓库地址**: `https://gitlab.eduxiji.net/educg-group-43501-3132633/T2026900029911083-3735`
**推测开发语言**: `Rust / C (由AST探针自动识别)`
**特征函数量**: `1031`

## ⚔️ 底层代码同源性对比 (AST Homology)
> 本部分由专门的代码审查智能体，对该项目与符合年份条件的历届最高相似度项目进行控制流与底层调用对比。新版报告会先给出确定性的全模块覆盖矩阵，再给出 Agent 的分模块解释，避免只围绕少量抽样函数展开。

### 🏆 Top 5 底层对标: vs `2024-赤道会师-合肥工业大学`（合肥工业大学 / 赤道会师）

#### 对方作品信息

**对方年份**: `2024`
**对方学校**: `合肥工业大学`
**对方队伍名称**: `赤道会师`
**对方比赛名字**: `OS-Competition`
**对方比赛子赛道**: `Kernel-Implementation`
**对方仓库地址**: `https://gitlab.eduxiji.net/educg-group-26010-2376550/T202410359992686-2094`

> 双向覆盖率：共同 AST 指纹: 459；唯一 AST 指纹: 目标项目 878，参考项目 832；覆盖率: 目标项目 52.28%，参考项目 55.17%

## 全模块对比总览（确定性证据）

> 总体覆盖率：共同 AST 指纹: 459；唯一 AST 指纹: 目标项目 878，参考项目 832；覆盖率: 目标项目 52.28%，参考项目 55.17%

### 1. 模块覆盖矩阵
| 模块 | 目标函数数 | 参考函数数 | 共同唯一 AST 指纹 | Target 覆盖 | Base 覆盖 | 状态 |
| :--- | ---: | ---: | ---: | ---: | ---: | :--- |
| 进程管理 | 56 | 41 | 12 | 25.53% | 31.58% | 双方存在且有结构重合 |
| 任务调度/异步运行时 | 64 | 57 | 6 | 9.52% | 10.91% | 双方存在且有结构重合 |
| 内存管理 | 162 | 112 | 19 | 12.18% | 17.59% | 双方存在且有结构重合 |
| 中断/异常处理 | 35 | 18 | 3 | 10.71% | 17.65% | 双方存在且有结构重合 |
| 系统调用 | 7 | 10 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |
| 文件系统 | 109 | 106 | 9 | 9.57% | 10.23% | 双方存在且有结构重合 |
| 设备驱动 | 38 | 29 | 2 | 6.45% | 8.7% | 双方存在且有结构重合 |
| 网络协议栈 | 0 | 0 | 0 | 0.0% | 0.0% | 双方均未体现 |
| 文件系统/第三方库 | 489 | 485 | 382 | 93.63% | 95.5% | 双方存在且有结构重合 |
| 通用模块/其他 | 71 | 130 | 31 | 47.69% | 26.96% | 双方存在且有结构重合 |

### 2. 分模块证据清单

#### 进程管理
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `kernel/proc/proc.c#forkret` | `kernel/proc.c#forkret` | 同板块同名 | filesystem_init, myproc, release, usertrapret | dir_open, ext4_init, myproc, r_time, release, usertrapret |
| `user/sh.c#parseexec` | `user/sh.c#parseexec` | 同板块同名同结构 | execcmd, gettoken, panic, parseblock, parseredirs, peek | execcmd, gettoken, panic, parseblock, parseredirs, peek |
| `kernel/proc/proc.c#procdump` | `kernel/proc.c#procdump` | 同板块同名 | NELEM, printf | NELEM, printf |
| `kernel/proc/proc.c#sleep` | `kernel/proc.c#sleep` | 同板块同名 | acquire, myproc, release, sched | acquire, myproc, release, sched |

#### 任务调度/异步运行时
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `kernel/util/printf.c#printf` | `kernel/printf.c#printf` | 同板块同名 | acquire, consputc, printint, printptr, release, va_arg ...(+2) | acquire, consputc, panic, printint, printptr, release ...(+2) |
| `user/sh.c#runcmd` | `user/sh.c#runcmd` | 同板块同名 | close, dup, execve, exit, fork1, fprintf ...(+5) | close, dup, exec, exit, fork1, fprintf ...(+5) |
| `kernel/drive/uart.c#uartintr` | `kernel/uart.c#uartintr` | 同板块同名 | ReadReg, Reg, WriteReg, acquire, consoleintr, release ...(+2) | acquire, consoleintr, release, uartgetc, uartstart |
| `kernel/fs/console.c#consoleintr` | `kernel/console.c#consoleintr` | 同板块同名 | C, acquire, consputc, procdump, release, wakeup | C, acquire, consputc, procdump, release, wakeup |

#### 内存管理
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `kernel/proc/exec.c#execve` | `kernel/exec.c#execve` | 同板块同名 | AUXV, PGROUNDDOWN, PGROUNDUP, acquire, copyout, ip->i_op->lock ...(+12) | ADD_AUXV, MAKE_SATP, PGROUNDDOWN, PGROUNDUP, PX, copyout ...(+17) |
| `kernel/syscall/sysfile.c#sys_execve` | `kernel/sysproc.c#sys_execve` | 同板块同名 | NELEM, argaddr, argstr, execve, fetchaddr, fetchstr ...(+6) | NELEM, argaddr, argstr, execve, fetchaddr, fetchstr ...(+3) |
| `kernel/drive/la/virtio_disk.c#virtio_disk_init` | `kernel/virtio_disk.c#virtio_disk_init` | 同板块同名 | initlock, memset, pci_device_init, printf, virtio_pci_get_device_features, virtio_pci_get_queue_enable ...(+8) | R, initlock, memset, panic, printf |
| `kernel/syscall/sysfile.c#sys_openat` | `kernel/sysfile.c#sys_openat` | 同板块同名 | argaddr, argint, argstr, fdalloc, fdalloc2, filealloc ...(+9) | argfd, argint, argstr, dir_open, fdalloc, filealloc ...(+8) |

#### 中断/异常处理
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `kernel/drive/rv/plic.c#plicinithart` | `kernel/plic.c#plicinithart` | 同板块同名 | PLIC_SENABLE, PLIC_SPRIORITY, cpuid, printf | PLIC_MENABLE, PLIC_SENABLE, PLIC_SPRIORITY, cpuid, printf, readd |
| `kernel/drive/rv/virtio_disk.c#virtio_disk_intr` | `kernel/virtio_disk.c#virtio_disk_intr` | 同板块同名同结构 | R, acquire, panic, release, wakeup | R, acquire, panic, release, wakeup |
| `kernel/drive/la/virtio_disk.c#free_desc` | `kernel/virtio_disk.c#free_desc` | 同板块同名 | panic, wakeup | panic, wakeup |
| `kernel/drive/rv/plic.c#plicinit` | `kernel/plic.c#plicinit` | 同板块同名 | printf | printf, writed |

#### 系统调用
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `kernel/syscall/syscall.c#argstr` | `kernel/syscall.c#argstr` | 同板块同名 | argaddr, fetchstr | argaddr, fetchstr |
| `kernel/syscall/syscall.c#argaddr` | `kernel/syscall.c#argaddr` | 同板块同名 | argraw | argraw |
| `kernel/syscall/syscall.c#argint` | `kernel/syscall.c#argint` | 同板块同名 | argraw | argraw |
| `user/new.c#main` | `kernel/syssig.c#sys_rt_sigaction` | 同模块上下文对齐/差异观察 | printf, strcmp, stress_test_all_syscalls, test_clock_gettime_precision, test_error_conditions, test_getrandom_quality ...(+3) | argaddr, argint, copyin2, copyout2, sigaction |

#### 文件系统
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `user/grind.c#go` | `user/grind.c#go` | 同板块同名 | chdir, close, dup, exec, exit, fork ...(+16) | chdir, close, dup, exec, exit, fork ...(+16) |
| `kernel/fs/vfs/file.c#filewrite` | `kernel/file.c#filewrite` | 同板块同名 | devsw[f->f_major].write, get_fops, get_fops()->writable, panic, pipewrite, printf ...(+1) | devsw[f->major].write, fwrite, panic, pipewrite |
| `kernel/fs/vfs/file.c#fileclose` | `kernel/file.c#fileclose` | 同板块同名 | acquire, get_fops, get_fops()->writable, panic, pipeclose, release ...(+4) | acquire, fclose, panic, pipeclose, release |
| `kernel/fs/pipe.c#pipeclose` | `kernel/pipe.c#pipeclose` | 同板块同名同结构 | acquire, kfree, release, wakeup | acquire, kfree, release, wakeup |

#### 设备驱动
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `kernel/drive/uart.c#uartstart` | `kernel/uart.c#uartstart` | 同板块同名 | ReadReg, WriteReg, wakeup | ReadReg, WriteReg, wakeup |
| `kernel/drive/uart.c#uartputc_sync` | `kernel/uart.c#uartputc_sync` | 同板块同名 | ReadReg, WriteReg, pop_off, push_off | ReadReg, WriteReg, pop_off, push_off |
| `kernel/drive/uart.c#uartputc` | `kernel/uart.c#uartputc` | 同板块同名 | ReadReg, WriteReg, acquire, release, uartstart | acquire, release, sleep, uartstart |
| `user/sh.c#parseblock` | `user/sh.c#parseblock` | 同板块同名同结构 | gettoken, panic, parseline, parseredirs, peek | gettoken, panic, parseline, parseredirs, peek |

#### 文件系统/第三方库
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `kernel/fs/lwext4/ext4.c#ext4_dir_rm` | `kernel/lwext4/ext4.c#ext4_dir_rm` | 同板块同名同结构 | EXT4_MP_LOCK, EXT4_MP_UNLOCK, ext4_block_cache_write_back, ext4_dir_en_get_inode, ext4_dir_iterator_fini, ext4_dir_iterator_init ...(+19) | EXT4_MP_LOCK, EXT4_MP_UNLOCK, ext4_block_cache_write_back, ext4_dir_en_get_inode, ext4_dir_iterator_fini, ext4_dir_iterator_init ...(+19) |
| `kernel/fs/lwext4/ext4_balloc.c#ext4_balloc_alloc_block` | `kernel/lwext4/ext4_balloc.c#ext4_balloc_alloc_block` | 同板块同名同结构 | ext4_balloc_get_bgid_of_block, ext4_balloc_get_block_of_bgid, ext4_balloc_set_bitmap_csum, ext4_balloc_verify_bitmap_csum, ext4_bg_get_block_bitmap, ext4_bg_get_free_blocks_count ...(+19) | ext4_balloc_get_bgid_of_block, ext4_balloc_get_block_of_bgid, ext4_balloc_set_bitmap_csum, ext4_balloc_verify_bitmap_csum, ext4_bg_get_block_bitmap, ext4_bg_get_free_blocks_count ...(+19) |
| `kernel/fs/lwext4/ext4_journal.c#jbd_journal_prepare` | `kernel/lwext4/ext4_journal.c#jbd_journal_prepare` | 同板块同名同结构 | EXT4_BLOCK_ZERO, RB_FIND, TAILQ_FOREACH_REVERSE_SAFE, TAILQ_FOREACH_SAFE, TAILQ_REMOVE, ext4_bcache_clear_dirty ...(+17) | EXT4_BLOCK_ZERO, RB_FIND, TAILQ_FOREACH_REVERSE_SAFE, TAILQ_FOREACH_SAFE, TAILQ_REMOVE, ext4_bcache_clear_dirty ...(+17) |
| `kernel/fs/lwext4/ext4_dir_idx.c#ext4_dir_dx_split_index` | `kernel/lwext4/ext4_dir_idx.c#ext4_dir_dx_split_index` | 同板块同名同结构 | ext4_block_set, ext4_dir_dx_climit_get_count, ext4_dir_dx_climit_get_limit, ext4_dir_dx_climit_set_count, ext4_dir_dx_climit_set_limit, ext4_dir_dx_entry_get_hash ...(+10) | ext4_block_set, ext4_dir_dx_climit_get_count, ext4_dir_dx_climit_get_limit, ext4_dir_dx_climit_set_count, ext4_dir_dx_climit_set_limit, ext4_dir_dx_entry_get_hash ...(+10) |

#### 通用模块/其他
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `user/printf.c#vprintf` | `user/printf.c#vprintf` | 同板块同名 | putc_buf, va_arg, write | printint, printptr, putc, va_arg |
| `kernel/lock/spinlock.c#release` | `kernel/spinlock.c#release` | 同板块同名同结构 | __sync_lock_release, __sync_synchronize, holding, panic, pop_off | __sync_lock_release, __sync_synchronize, holding, panic, pop_off |
| `user/grind.c#iter` | `user/grind.c#iter` | 同板块同名同结构 | exit, fork, go, kill, printf, unlink ...(+1) | exit, fork, go, kill, printf, unlink ...(+1) |
| `user/wc.c#wc` | `user/wc.c#wc` | 同板块同名 | exit, printf, read, strchr | exit, printf, read, strchr |


好的，审计官。以下是基于提供的证据、双方陈述及审计指令形成的最终同源性定性仲裁报告。

---

### 1. 宏观判定
- **定性结论**: **独立开发**
- **核心依据**: 总体 AST 相似度为 36.69%，处于较低水平。该相似度主要由“文件系统/第三方库”板块（共同指纹 382/459）贡献，剔除该公共依赖后，核心内核模块的共同指纹覆盖率极低。双方在进程管理、内存管理、文件系统等核心子系统的架构设计、功能集和实现路径上存在实质性差异，符合基于相同公共上游（xv6）和第三方库（lwext4）进行独立扩展和开发的典型特征。

### 2. 全模块覆盖矩阵解读
- **进程管理**: 双方均存在。共同 AST 指纹仅 12 条，目标覆盖率 25.53%，参考覆盖率 31.58%。超过三分之二的指纹为各自独有。重合部分主要为 xv6 标准调度与系统调用包装函数。**证据强度: 弱**。
- **任务调度/异步运行时**: 双方均存在。共同 AST 指纹 6 条，覆盖率均低于 11%。重合点集中在 `printf`、`consoleintr` 等通用 I/O 和 Shell 执行流，非核心调度算法。**证据强度: 弱**。
- **内存管理**: 双方均存在。共同 AST 指纹 19 条，覆盖率均低于 18%。重合点如 `execve`、`sys_execve` 等，其调用图谱（如 `PGROUNDUP`, `copyout`）是 xv6 内存管理标准流程。**证据强度: 弱**。
- **中断/异常处理**: 双方均存在。共同 AST 指纹 3 条，覆盖率均低于 18%。重合点如 `plicinithart`、`virtio_disk_intr` 是 RISC-V PLIC 和 VirtIO 标准驱动初始化及中断处理流程。**证据强度: 弱**。
- **系统调用**: 双方均存在。共同 AST 指纹为 0。尽管存在同名函数，但 AST 指纹无重合，表明系统调用分发和参数解析的实现路径完全不同。**证据强度: 未形成**。
- **文件系统**: 双方均存在。共同 AST 指纹 9 条，覆盖率均低于 11%。重合点如 `filewrite`、`pipeclose` 是类 Unix 文件系统和管道操作的标准实现。目标项目采用 VFS 抽象（`get_fops`），参考项目为直接调用，架构差异显著。**证据强度: 弱**。
- **设备驱动**: 双方均存在。共同 AST 指纹 2 条，覆盖率均低于 9%。重合点如 `uartstart` 是 UART 设备寄存器操作的标准流程。**证据强度: 弱**。
- **网络协议栈**: 双方均未体现。**证据强度: 未形成**。
- **第三方库/公共依赖**: 双方均存在。共同 AST 指纹 382 条，覆盖率均超过 93%。该板块是总体相似度的主要贡献者，源于双方共同使用了 `lwext4` 开源库。**证据强度: 强（但属于公共依赖，不构成抄袭证据）**。

### 3. 底层 AST 关键证据指纹表
| 所属板块 | 目标项目路径 | 参考项目路径 | 证据类型 | 技术说明 |
| :--- | :--- | :--- | :--- | :--- |
| 进程管理 | `kernel/proc/proc.c#yield` | `kernel/proc.c#yield` | 同板块同名同结构 | xv6 标准进程让出逻辑：`acquire, myproc, release, sched`。 |
| 进程管理 | `kernel/proc/proc.c#sched` | `kernel/proc.c#sched` | 同板块同名 | xv6 核心调度器上下文切换逻辑，目标项目缺少 `r_time` 调用。 |
| 进程管理 | `kernel/proc/proc.c#forkret` | `kernel/proc.c#forkret` | 同板块同名 | 首次调度返回路径，目标项目使用 `filesystem_init`，参考项目使用 `dir_open, ext4_init`，架构不同。 |
| 进程管理 | `kernel/syscall/sysproc.c#sys_fork` | `kernel/sysproc.c#sys_fork` | 同板块同名同结构 | 系统调用薄包装，直接透传至 `fork`。 |
| 内存管理 | `kernel/proc/exec.c#execve` | `kernel/exec.c#execve` | 同板块同名 | xv6 `exec` 标准流程，包含 `PGROUNDUP`, `copyout` 等标准内存操作。 |
| 内存管理 | `kernel/syscall/sysfile.c#sys_openat` | `kernel/sysfile.c#sys_openat` | 同板块同名 | 文件打开系统调用，目标项目使用 `fdalloc2`，参考项目使用 `argfd`，实现路径不同。 |
| 中断/异常处理 | `kernel/drive/rv/virtio_disk.c#virtio_disk_intr` | `kernel/virtio_disk.c#virtio_disk_intr` | 同板块同名同结构 | VirtIO 磁盘中断处理标准流程：`R, acquire, panic, release, wakeup`。 |
| 文件系统 | `kernel/fs/vfs/file.c#filewrite` | `kernel/file.c#filewrite` | 同板块同名 | 文件写入多路分发，目标项目通过 `get_fops()` 抽象，参考项目直接调用 `fwrite`。 |
| 设备驱动 | `kernel/drive/uart.c#uartstart` | `kernel/uart.c#uartstart` | 同板块同名 | UART 发送启动标准流程：`ReadReg, WriteReg, wakeup`。 |
| 第三方库/公共依赖 | `kernel/fs/lwext4/ext4_balloc.c#ext4_balloc_alloc_block` | `kernel/lwext4/ext4_balloc.c#ext4_balloc_alloc_block` | 同板块同名同结构 | lwext4 库内部函数，调用图谱完全一致，属于公共依赖。 |

### 4. 核心代码调用流对比剖析
* **【进程管理/fork 返回路径】**
* **底层技术析理**: 对比 `forkret` 函数，两者均完成从 `fork` 系统调用返回用户态的准备。然而，在文件系统初始化这一关键步骤上，目标项目调用 `filesystem_init`，表明其采用了一个抽象的 VFS 层来统一初始化；而参考项目则直接调用 `dir_open` 和 `ext4_init`，表明其文件系统实现与 ext4 紧耦合。这反映了在**抽象层次和模块解耦**上的根本性架构差异，而非简单的函数重命名。
* **代码溯源链接**:
  - 目标项目: `kernel/proc/proc.c#forkret`
  - 参考项目: `kernel/proc.c#forkret`

* **【文件系统/文件写入路径】**
* **底层技术析理**: 对比 `filewrite` 函数，两者均实现了向文件写入数据的功能。目标项目的调用图谱中包含 `get_fops` 和 `get_fops()->writable`，这是典型的 VFS（虚拟文件系统）设计，通过函数指针表实现多态。参考项目的调用图谱中则是 `fwrite`，属于直接函数调用。这揭示了双方在**对象管理模型**上的核心区别：面向接口的抽象设计 vs. 面向实现的直接调用。
* **代码溯源链接**:
  - 目标项目: `kernel/fs/vfs/file.c#filewrite`
  - 参考项目: `kernel/file.c#filewrite`

* **【内存管理/程序加载路径】**
* **底层技术析理**: 对比 `execve` 函数，两者均遵循 xv6 的程序加载标准流程：解析 ELF、分配页表、加载段到内存、准备用户栈。调用图谱中的 `PGROUNDDOWN`, `PGROUNDUP`, `copyout` 等是 xv6 内存管理的标准原语。差异点在于目标项目使用 `ip->i_op->lock` 这种基于 inode 操作表的锁机制，而参考项目使用 `MAKE_SATP` 宏，反映了在**同步策略和宏定义风格**上的不同。
* **代码溯源链接**:
  - 目标项目: `kernel/proc/exec.c#execve`
  - 参考项目: `kernel/exec.c#execve`

### 5. 单方模块与降权项
- **单方独有模块/功能**:
  - **目标项目独有**: 实现了 `ppoll_check`, `sys_futex`, `sys_set_robust_list`, `sys_setreuid/setregid` 等 Linux 兼容系统调用，以及 `scheduler` 函数和多架构驱动分层 (`kernel/drive/rv/`, `kernel/drive/la/`)。
  - **参考项目独有**: 实现了信号子系统 (`sys_rt_sigprocmask`, `sigprocmask`)、DMA 驱动 (`dmac_wait_idle`) 和 `r_time` 时间源。
  这些单方存在的模块和功能是独立开发的强有力正向证据。
- **降权项**:
  - **第三方库/公共依赖**: `lwext4` 库贡献了 382 条共同指纹，占总数的 83.2%。该部分重合与双方项目原创性无关，必须降权处理。
  - **xv6 公共代码**: `yield`, `sleep`, `sched`, `myproc`, `sys_fork`, `sys_getpid` 等函数是 MIT xv6 教学操作系统的标准实现或接口强制的薄包装，创作空间极小，其调用图谱重合不能作为抄袭证据。
  - **用户态程序**: `user/sh.c`, `user/zombie.c` 等属于 xv6 官方提供的用户态测试工具，与内核进程管理属于不同抽象层，其重合应降权为弱信号。

### 6. 保守结论
本次审计的总体 AST 相似度为 36.69%，处于“独立开发”的典型阈值范围。该相似度主要由双方共同使用的开源第三方库 `lwext4` 贡献，剔除后核心内核模块的相似度极低。控方所指控的“铁证”函数，经分析均为 xv6 公共教学代码或操作系统接口强制的标准实现，不具备证明抄袭的效力。更重要的是，双方在文件系统架构（VFS vs. 紧耦合）、系统调用功能集（Linux兼容 vs. 信号处理）、驱动分层模型等核心设计上存在实质性差异，这些差异是独立架构决策的产物，而非“洗稿”所能解释。综合判定，无证据表明目标项目对参考项目构成抄袭。

------------------------------

