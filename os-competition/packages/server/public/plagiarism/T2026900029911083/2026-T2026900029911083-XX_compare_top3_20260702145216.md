# 🛡️ OS 内核赛自动化代码级架构审计报告

**审计时间**: `2026-07-02 14:52:16`
**参赛作品名称**: `2026-T2026900029911083-XX`
**年份**: `2026`
**学校**: `XX`
**队伍名称**: `T2026900029911083`
**比赛名字**: `OS-Competition`
**比赛子赛道**: `Kernel-Implementation`
**仓库地址**: `https://gitlab.eduxiji.net/educg-group-43501-3132633/T2026900029911083-3735`
**推测开发语言**: `Rust / C (由AST探针自动识别)`
**特征函数量**: `1031`

## ⚔️ 底层代码同源性对比 (AST Homology)
> 本部分由专门的代码审查智能体，对该项目与符合年份条件的历届最高相似度项目进行控制流与底层调用对比。新版报告会先给出确定性的全模块覆盖矩阵，再给出 Agent 的分模块解释，避免只围绕少量抽样函数展开。

### 🏆 Top 3 底层对标: vs `2024-undefinedType-南京航空航天大学`（南京航空航天大学 / undefinedType）

#### 对方作品信息

**对方年份**: `2024`
**对方学校**: `南京航空航天大学`
**对方队伍名称**: `undefinedType`
**对方比赛名字**: `OS-Competition`
**对方比赛子赛道**: `Kernel-Implementation`
**对方仓库地址**: `https://gitlab.eduxiji.net/educg-group-26010-2376550/T202410287992755-2176`

> 双向覆盖率：共同 AST 指纹: 375；唯一 AST 指纹: 目标项目 878，参考项目 398；覆盖率: 目标项目 42.71%，参考项目 94.22%

## 全模块对比总览（确定性证据）

> 总体覆盖率：共同 AST 指纹: 375；唯一 AST 指纹: 目标项目 878，参考项目 398；覆盖率: 目标项目 42.71%，参考项目 94.22%

### 1. 模块覆盖矩阵
| 模块 | 目标函数数 | 参考函数数 | 共同唯一 AST 指纹 | Target 覆盖 | Base 覆盖 | 状态 |
| :--- | ---: | ---: | ---: | ---: | ---: | :--- |
| 进程管理 | 56 | 0 | 0 | 0.0% | 0.0% | 仅目标项目体现 |
| 任务调度/异步运行时 | 64 | 0 | 0 | 0.0% | 0.0% | 仅目标项目体现 |
| 内存管理 | 162 | 0 | 0 | 0.0% | 0.0% | 仅目标项目体现 |
| 中断/异常处理 | 35 | 0 | 0 | 0.0% | 0.0% | 仅目标项目体现 |
| 系统调用 | 7 | 0 | 0 | 0.0% | 0.0% | 仅目标项目体现 |
| 文件系统 | 109 | 0 | 0 | 0.0% | 0.0% | 仅目标项目体现 |
| 设备驱动 | 38 | 0 | 0 | 0.0% | 0.0% | 仅目标项目体现 |
| 网络协议栈 | 0 | 0 | 0 | 0.0% | 0.0% | 双方均未体现 |
| 文件系统/第三方库 | 489 | 478 | 375 | 91.91% | 94.22% | 双方存在且有结构重合 |
| 通用模块/其他 | 71 | 0 | 0 | 0.0% | 0.0% | 仅目标项目体现 |

### 2. 分模块证据清单

#### 进程管理
仅目标项目中观察到该模块代表函数，参考项目没有同模块可对齐证据：
- 目标项目：`kernel/syscall/sysproc.c#ppoll_check`，调用摘要：acquire, release
- 目标项目：`kernel/proc/proc.c#scheduler`，调用摘要：acquire, intr_on, mycpu, release, swtch
- 目标项目：`kernel/syscall/sysproc.c#sys_setreuid`，调用摘要：acquire, argint, myproc, release
- 目标项目：`kernel/syscall/sysproc.c#sys_setregid`，调用摘要：acquire, argint, myproc, release

#### 任务调度/异步运行时
仅目标项目中观察到该模块代表函数，参考项目没有同模块可对齐证据：
- 目标项目：`kernel/syscall/sysfile.c#sys_faccessat`，调用摘要：argint, argstr, get_absolute_path, k_str_contains, myproc, strcmp ...(+1)
- 目标项目：`kernel/drive/la/virtio_pci.c#virtio_pci_read_caps`，调用摘要：get_cfg_addr, pci_config_read, pci_config_read8, printf
- 目标项目：`kernel/util/printf.c#printf`，调用摘要：acquire, consputc, printint, printptr, release, va_arg ...(+2)
- 目标项目：`kernel/trap/la/trap.c#trapframedump`，调用摘要：myproc, printf

#### 内存管理
仅目标项目中观察到该模块代表函数，参考项目没有同模块可对齐证据：
- 目标项目：`kernel/syscall/sysproc.c#sys_clock_nanosleep`，调用摘要：acquire, argaddr, argint, copyin, copyout, exit ...(+6)
- 目标项目：`user/init-rv.c#test_pre`，调用摘要：chdir, exec, execve, exit, fork, printf ...(+1)
- 目标项目：`kernel/proc/exec.c#execve`，调用摘要：AUXV, PGROUNDDOWN, PGROUNDUP, acquire, copyout, ip->i_op->lock ...(+12)
- 目标项目：`user/init-la.c#test_pre`，调用摘要：chdir, exec, execve, exit, fork, printf ...(+1)

#### 中断/异常处理
仅目标项目中观察到该模块代表函数，参考项目没有同模块可对齐证据：
- 目标项目：`kernel/drive/uart.c#uartinit`，调用摘要：WriteReg, initlock
- 目标项目：`kernel/fs/VFS_ext.c#vfs_ext_futimens`，调用摘要：NS_to_S, TIME2NS, TIMESEPC2NS, ext4_atime_set, ext4_mtime_set, panic ...(+1)
- 目标项目：`kernel/fs/VFS_ext.c#vfs_ext_utimens`，调用摘要：NS_to_S, TIME2NS, TIMESEPC2NS, ext4_atime_set, ext4_mtime_set, rdtime
- 目标项目：`kernel/drive/rv/plic.c#plicinithart`，调用摘要：PLIC_SENABLE, PLIC_SPRIORITY, cpuid, printf

#### 系统调用
仅目标项目中观察到该模块代表函数，参考项目没有同模块可对齐证据：
- 目标项目：`user/new.c#main`，调用摘要：printf, strcmp, stress_test_all_syscalls, test_clock_gettime_precision, test_error_conditions, test_getrandom_quality ...(+3)
- 目标项目：`kernel/syscall/syssig.c#sys_tgkill`，调用摘要：argint, send_signal_thread
- 目标项目：`kernel/syscall/syscall.c#argstr`，调用摘要：argaddr, fetchstr
- 目标项目：`kernel/syscall/syssig.c#sys_tkill`，调用摘要：argint, send_signal_thread

#### 文件系统
仅目标项目中观察到该模块代表函数，参考项目没有同模块可对齐证据：
- 目标项目：`user/grind.c#go`，调用摘要：chdir, close, dup, exec, exit, fork ...(+16)
- 目标项目：`user/init-la.c#test_final`，调用摘要：chdir, exec, execve, exit, fork, printf ...(+1)
- 目标项目：`kernel/fs/vfs/ops.c#get_absolute_path`，调用摘要：strcat, strcpy, strlen
- 目标项目：`user/new.c#test_openat_functionality`，调用摘要：close, mkdirat, openat, printf, read, unlinkat ...(+1)

#### 设备驱动
仅目标项目中观察到该模块代表函数，参考项目没有同模块可对齐证据：
- 目标项目：`kernel/drive/la/pci.c#pci_device_bar_init`，调用摘要：内部闭环逻辑
- 目标项目：`kernel/drive/la/virtio_ring.c#virtio_vring_init`，调用摘要：VQ_ALIGN, virtio_vring_size
- 目标项目：`kernel/drive/la/virtio_pci.c#virtio_pci_set_queue_addr2`，调用摘要：PCI_REG16, PCI_REG64, VIRT2PHY, dsb
- 目标项目：`kernel/drive/uart.c#uartstart`，调用摘要：ReadReg, WriteReg, wakeup

#### 文件系统/第三方库
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `kernel/fs/lwext4/ext4.c#ext4_dir_rm` | `Kernel/File/lwext4/ext4.c#ext4_dir_rm` | 同板块同名同结构 | EXT4_MP_LOCK, EXT4_MP_UNLOCK, ext4_block_cache_write_back, ext4_dir_en_get_inode, ext4_dir_iterator_fini, ext4_dir_iterator_init ...(+19) | EXT4_MP_LOCK, EXT4_MP_UNLOCK, ext4_block_cache_write_back, ext4_dir_en_get_inode, ext4_dir_iterator_fini, ext4_dir_iterator_init ...(+19) |
| `kernel/fs/lwext4/ext4_balloc.c#ext4_balloc_alloc_block` | `Kernel/File/lwext4/ext4_balloc.c#ext4_balloc_alloc_block` | 同板块同名同结构 | ext4_balloc_get_bgid_of_block, ext4_balloc_get_block_of_bgid, ext4_balloc_set_bitmap_csum, ext4_balloc_verify_bitmap_csum, ext4_bg_get_block_bitmap, ext4_bg_get_free_blocks_count ...(+19) | EXT4_Debug_u32, ext4_balloc_get_bgid_of_block, ext4_balloc_get_block_of_bgid, ext4_balloc_set_bitmap_csum, ext4_balloc_verify_bitmap_csum, ext4_bg_get_block_bitmap ...(+19) |
| `kernel/fs/lwext4/ext4_journal.c#jbd_journal_prepare` | `Kernel/File/lwext4/ext4_journal.c#jbd_journal_prepare` | 同板块同名同结构 | EXT4_BLOCK_ZERO, RB_FIND, TAILQ_FOREACH_REVERSE_SAFE, TAILQ_FOREACH_SAFE, TAILQ_REMOVE, ext4_bcache_clear_dirty ...(+17) | EXT4_BLOCK_ZERO, RB_FIND, TAILQ_FOREACH_REVERSE_SAFE, TAILQ_FOREACH_SAFE, TAILQ_REMOVE, ext4_bcache_clear_dirty ...(+17) |
| `kernel/fs/lwext4/ext4_dir_idx.c#ext4_dir_dx_split_index` | `Kernel/File/lwext4/ext4_dir_idx.c#ext4_dir_dx_split_index` | 同板块同名同结构 | ext4_block_set, ext4_dir_dx_climit_get_count, ext4_dir_dx_climit_get_limit, ext4_dir_dx_climit_set_count, ext4_dir_dx_climit_set_limit, ext4_dir_dx_entry_get_hash ...(+10) | ext4_block_set, ext4_dir_dx_climit_get_count, ext4_dir_dx_climit_get_limit, ext4_dir_dx_climit_set_count, ext4_dir_dx_climit_set_limit, ext4_dir_dx_entry_get_hash ...(+10) |

#### 通用模块/其他
仅目标项目中观察到该模块代表函数，参考项目没有同模块可对齐证据：
- 目标项目：`user/printf.c#vprintf`，调用摘要：putc_buf, va_arg, write
- 目标项目：`kernel/util/string.c#snprintf`，调用摘要：printint_to_buf, putchar_to_buf, puts_to_buf, va_arg, va_end, va_start
- 目标项目：`kernel/util/printf.c#printf_highlight`，调用摘要：acquire, consputc, printint, printptr, release, reset_text_color ...(+4)
- 目标项目：`kernel/util/printf.c#printf_color`，调用摘要：acquire, consputc, printint, printptr, release, reset_text_color ...(+4)


好的，审计员。以下基于所提供的证据底座、控辩双方陈述及原始AST抽样数据，进行最终同源性定性仲裁。

---

### 1. 宏观判定
- **定性结论**: 底层架构显著差异 / 独立开发 (相似度: 41.62%，共同 AST 指纹: 375；唯一 AST 指纹: 目标项目 878，参考项目 398；覆盖率: 目标项目 42.71%，参考项目 94.22%)
- **核心依据**: 41.62%的总体相似度几乎完全由双方共同集成的第三方开源库 `lwext4` 贡献。剔除该公共依赖后，双方在操作系统内核的核心模块（进程、内存、调度、驱动等）上无任何共同AST指纹，架构异构性显著。参考项目覆盖率高达94.22%，表明其本身高度依赖 `lwext4`，以此作为比对基线会产生误导性高重合度。

### 2. 全模块覆盖矩阵解读
| 模块 | 双方均存在 | 共同AST指纹 | 覆盖率(Target/Base) | 证据强度 | 解读 |
| :--- | :---: | :---: | :---: | :---: | :--- |
| **进程管理** | 否 | 0 | 0.0% / 0.0% | 未形成 | 目标项目有56个函数，参考项目为0。双方在此核心模块无任何可比性。 |
| **任务调度/异步运行时** | 否 | 0 | 0.0% / 0.0% | 未形成 | 目标项目有64个函数，参考项目为0。双方在此核心模块无任何可比性。 |
| **内存管理** | 否 | 0 | 0.0% / 0.0% | 未形成 | 目标项目有162个函数，参考项目为0。双方在此核心模块无任何可比性。 |
| **中断/异常处理** | 否 | 0 | 0.0% / 0.0% | 未形成 | 目标项目有35个函数，参考项目为0。双方在此核心模块无任何可比性。 |
| **系统调用** | 否 | 0 | 0.0% / 0.0% | 未形成 | 目标项目有7个函数，参考项目为0。双方在此核心模块无任何可比性。 |
| **文件系统** | 否 | 0 | 0.0% / 0.0% | 未形成 | 目标项目有109个自研VFS函数，参考项目为0。双方在此核心模块无任何可比性。 |
| **设备驱动** | 否 | 0 | 0.0% / 0.0% | 未形成 | 目标项目有38个函数，参考项目为0。双方在此核心模块无任何可比性。 |
| **网络协议栈** | 否 | 0 | 0.0% / 0.0% | 未形成 | 双方均无此模块。 |
| **通用模块/其他** | 否 | 0 | 0.0% / 0.0% | 未形成 | 目标项目有71个函数，参考项目为0。双方在此模块无任何可比性。 |
| **文件系统/第三方库** | 是 | 375 | 91.91% / 94.22% | **强（第三方库）** | 双方均集成了 `lwext4` 开源库。高覆盖率是共同使用同一上游依赖的必然结果，不能作为项目间抄袭的证据。 |

### 3. 底层 AST 关键证据指纹表
| 所属板块 | 目标项目路径 | 参考项目路径 | 证据类型 | 技术说明 |
| :--- | :--- | :--- | :--- | :--- |
| **第三方库/公共依赖** | `[kernel/fs/lwext4/ext4.c]` 函数 `ext4_dir_rm` | `[Kernel/File/lwext4/ext4.c]` 函数 `ext4_dir_rm` | 同源第三方库调用 | 调用图谱完全一致，均为标准 `lwext4` 库的目录删除流程。 |
| **第三方库/公共依赖** | `[kernel/fs/lwext4/ext4_balloc.c]` 函数 `ext4_balloc_alloc_block` | `[Kernel/File/lwext4/ext4_balloc.c]` 函数 `ext4_balloc_alloc_block` | 同源第三方库调用 | 核心业务调用链一致。参考项目额外包含 `EXT4_Debug_u32` 调试宏，表明集成配置不同。 |
| **第三方库/公共依赖** | `[kernel/fs/lwext4/ext4_journal.c]` 函数 `jbd_journal_prepare` | `[Kernel/File/lwext4/ext4_journal.c]` 函数 `jbd_journal_prepare` | 同源第三方库调用 | 调用图谱完全一致，均为标准 `lwext4` 库的日志准备流程。 |
| **第三方库/公共依赖** | `[kernel/fs/lwext4/ext4_mkfs.c]` 函数 `ext4_mkfs` | `[Kernel/File/lwext4/ext4_mkfs.c]` 函数 `ext4_mkfs` | 同源第三方库调用 | 核心业务调用链一致。参考项目额外包含 `EXT4_Debug`, `EXT4_Debug_u32` 等多个调试宏，表明集成配置不同。 |
| **第三方库/公共依赖** | `[kernel/fs/lwext4/ext4_journal.c]` 函数 `jbd_iterate_log` | `[Kernel/File/lwext4/ext4_journal.c]` 函数 `jbd_iterate_log` | 同源第三方库调用 | 核心业务调用链一致。目标项目使用 `ext4_dbg`，参考项目使用 `EXT4_Debug` 系列宏，调试层实现不同。 |
| **第三方库/公共依赖** | `[kernel/fs/lwext4/ext4_xattr.c]` 函数 `ext4_xattr_set_entry` | `[Kernel/File/lwext4/ext4_xattr.c]` 函数 `ext4_xattr_set_entry` | 同源第三方库调用 | 调用图谱完全一致，包含 `memcpy`, `memmove`, `memset` 等C标准库泛型函数，不构成特异性证据。 |

### 4. 核心代码调用流对比剖析
* **【第三方库/公共依赖 - lwext4 文件系统格式化】**
* **底层技术析理**: 双方在 `ext4_mkfs` 函数中均调用了 `alloc_inodes`, `compute_blocks_per_group`, `create_journal_inode` 等核心初始化流程，这与 `lwext4` 上游库的实现完全一致。然而，参考项目在调用流中插入了 `EXT4_Debug`, `EXT4_Debug_u32`, `EXT4_Debug_u64` 等私有调试宏，而目标项目则无此调用。这表明双方是独立获取并配置了 `lwext4` 库，而非一方从另一方直接复制代码。
* **代码溯源链接**:
  - 目标项目: `kernel/fs/lwext4/ext4_mkfs.c#ext4_mkfs`
  - 参考项目: `Kernel/File/lwext4/ext4_mkfs.c#ext4_mkfs`

* **【第三方库/公共依赖 - lwext4 日志迭代】**
* **底层技术析理**: 双方在 `jbd_iterate_log` 函数中的核心业务逻辑（`jbd_block_get`, `jbd_build_revoke_tree`, `jbd_replay_descriptor_block`）调用流一致，符合 `lwext4` 标准实现。但在调试/日志输出层，目标项目使用 `ext4_dbg` 函数，而参考项目使用 `EXT4_Debug`, `EXT4_Debug_u32` 等宏。不同的调试抽象层是独立集成和修改的明确标志。
* **代码溯源链接**:
  - 目标项目: `kernel/fs/lwext4/ext4_journal.c#jbd_iterate_log`
  - 参考项目: `Kernel/File/lwext4/ext4_journal.c#jbd_iterate_log`

### 5. 单方模块与降权项
- **单方模块**: 进程管理、任务调度/异步运行时、内存管理、中断/异常处理、系统调用、文件系统（自研VFS）、设备驱动、通用模块/其他。这些模块仅在目标项目中存在，是目标项目作为完整操作系统内核独立开发的直接证据，与参考项目无任何可比性。
- **降权项**:
  - **第三方库 `lwext4`**: 所有高相似度证据均来源于此公共依赖。其函数名、调用链由 ext4 规范决定，任何集成方都会产生相似的 AST，不能作为项目间抄袭的证据。
  - **泛型函数与宏**: `memcpy`, `memmove`, `memset`, `RB_FIND`, `TAILQ_FOREACH` 等属于C标准库或通用数据结构宏，不具备项目特异性，已从强证据中降权。
  - **跨板块证据**: 本案中不存在跨板块对齐的证据，所有指控均限于 `lwext4` 内部，符合审计原则。

### 6. 保守结论
本次审计的核心发现是，41.62%的总体AST相似度完全由双方共同集成的第三方开源库 `lwext4` 贡献。参考项目本身高度依赖此库（覆盖率94.22%），导致比对基数严重偏向该公共依赖，产生了误导性的高重合度假象。在操作系统内核的核心自研模块（进程管理、内存管理、中断处理、设备驱动、自研VFS等）上，双方无任何共同AST指纹，架构异构性显著。目标项目展示了远超参考项目的独立代码量（唯一AST指纹878 vs 398）。因此，现有证据无法支持抄袭指控，双方相似度是共同使用同一上游开源组件的自然结果，而非项目间的代码复用。综合判定，目标项目为独立开发。

------------------------------

