# 🛡️ OS 内核赛自动化代码级架构审计报告

**审计时间**: `2026-07-02 14:52:16`
**参赛作品名称**: `2026-T2026900029911083-XX`
**年份**: `2026`
**学校**: `XX`
**队伍名称**: `T2026900029911083`
**比赛名字**: `OS-Competition`
**比赛子赛道**: `Kernel-Implementation`
**仓库地址**: `https://gitlab.eduxiji.net/educg-group-43501-3132633/T2026900029911083-3735`
**推测开发语言**: `Rust / C (由AST探针自动识别)`
**特征函数量**: `1031`

## ⚔️ 底层代码同源性对比 (AST Homology)
> 本部分由专门的代码审查智能体，对该项目与符合年份条件的历届最高相似度项目进行控制流与底层调用对比。新版报告会先给出确定性的全模块覆盖矩阵，再给出 Agent 的分模块解释，避免只围绕少量抽样函数展开。

### 🏆 Top 1 底层对标: vs `2025-静春山-中山大学`（中山大学 / 静春山）

#### 对方作品信息

**对方年份**: `2025`
**对方学校**: `中山大学`
**对方队伍名称**: `静春山`
**对方比赛名字**: `OS-Competition`
**对方比赛子赛道**: `Kernel-Implementation`
**对方仓库地址**: `https://gitlab.eduxiji.net/educg-group-36002-2710490/T202510558995330-264`

> 双向覆盖率：共同 AST 指纹: 814；唯一 AST 指纹: 目标项目 878，参考项目 949；覆盖率: 目标项目 92.71%，参考项目 85.77%

## 全模块对比总览（确定性证据）

> 总体覆盖率：共同 AST 指纹: 814；唯一 AST 指纹: 目标项目 878，参考项目 949；覆盖率: 目标项目 92.71%，参考项目 85.77%

### 1. 模块覆盖矩阵
| 模块 | 目标函数数 | 参考函数数 | 共同唯一 AST 指纹 | Target 覆盖 | Base 覆盖 | 状态 |
| :--- | ---: | ---: | ---: | ---: | ---: | :--- |
| 进程管理 | 56 | 68 | 38 | 80.85% | 70.37% | 双方存在且有结构重合 |
| 任务调度/异步运行时 | 64 | 69 | 54 | 85.71% | 79.41% | 双方存在且有结构重合 |
| 内存管理 | 162 | 161 | 119 | 76.28% | 76.77% | 双方存在且有结构重合 |
| 中断/异常处理 | 35 | 39 | 28 | 100.0% | 87.5% | 双方存在且有结构重合 |
| 系统调用 | 7 | 41 | 4 | 80.0% | 33.33% | 双方存在且有结构重合 |
| 文件系统 | 109 | 133 | 88 | 93.62% | 79.28% | 双方存在且有结构重合 |
| 设备驱动 | 38 | 42 | 30 | 96.77% | 85.71% | 双方存在且有结构重合 |
| 网络协议栈 | 0 | 0 | 0 | 0.0% | 0.0% | 双方均未体现 |
| 文件系统/第三方库 | 489 | 489 | 408 | 100.0% | 100.0% | 双方存在且有结构重合 |
| 通用模块/其他 | 71 | 126 | 64 | 98.46% | 64.0% | 双方存在且有结构重合 |

### 2. 分模块证据清单

#### 进程管理
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `kernel/syscall/sysproc.c#ppoll_check` | `kernel/syscall/sysproc.c#ppoll_check` | 同板块同名同结构 | acquire, release | acquire, release |
| `kernel/proc/proc.c#scheduler` | `kernel/proc/proc.c#scheduler` | 同板块同名同结构 | acquire, intr_on, mycpu, release, swtch | acquire, intr_on, mycpu, release, swtch |
| `kernel/syscall/sysproc.c#sys_setreuid` | `kernel/syscall/sysproc.c#sys_setreuid` | 同板块同名 | acquire, argint, myproc, release | acquire, argint, myproc, release |
| `kernel/syscall/sysproc.c#sys_setregid` | `kernel/syscall/sysproc.c#sys_setregid` | 同板块同名 | acquire, argint, myproc, release | acquire, argint, myproc, release |

#### 任务调度/异步运行时
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `kernel/syscall/sysfile.c#sys_faccessat` | `kernel/syscall/sysfile.c#sys_faccessat` | 同板块同名同结构 | argint, argstr, get_absolute_path, k_str_contains, myproc, strcmp ...(+1) | argint, argstr, get_absolute_path, k_str_contains, myproc, strcmp ...(+1) |
| `kernel/drive/la/virtio_pci.c#virtio_pci_read_caps` | `kernel/drive/la/virtio_pci.c#virtio_pci_read_caps` | 同板块同名同结构 | get_cfg_addr, pci_config_read, pci_config_read8, printf | get_cfg_addr, pci_config_read, pci_config_read8, printf |
| `kernel/util/printf.c#printf` | `kernel/util/printf.c#printf` | 同板块同名同结构 | acquire, consputc, printint, printptr, release, va_arg ...(+2) | acquire, consputc, printint, printptr, release, va_arg ...(+2) |
| `kernel/trap/la/trap.c#trapframedump` | `kernel/trap/la/trap.c#trapframedump` | 同板块同名同结构 | myproc, printf | myproc, printf |

#### 内存管理
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `kernel/syscall/sysproc.c#sys_clock_nanosleep` | `kernel/syscall/sysproc.c#sys_clock_nanosleep` | 同板块同名 | acquire, argaddr, argint, copyin, copyout, exit ...(+6) | acquire, argaddr, argint, copyin, copyout, exit ...(+6) |
| `kernel/proc/exec.c#execve` | `kernel/proc/exec.c#execve` | 同板块同名 | AUXV, PGROUNDDOWN, PGROUNDUP, acquire, copyout, ip->i_op->lock ...(+12) | AUXV, PGROUNDDOWN, PGROUNDUP, acquire, copyout, ip->i_op->lock ...(+12) |
| `kernel/syscall/sysfile.c#sys_execve` | `kernel/syscall/sysfile.c#sys_execve` | 同板块同名同结构 | NELEM, argaddr, argstr, execve, fetchaddr, fetchstr ...(+6) | NELEM, argaddr, argstr, execve, fetchaddr, fetchstr ...(+6) |
| `kernel/trap/la/trap.c#if` | `kernel/trap/la/trap.c#if` | 同板块同名同结构 | copyin, copyout, printf, r_csr_badv, r_csr_era, walkaddr | copyin, copyout, printf, r_csr_badv, r_csr_era, walkaddr |

#### 中断/异常处理
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `kernel/drive/uart.c#uartinit` | `kernel/drive/uart.c#uartinit` | 同板块同名同结构 | WriteReg, initlock | WriteReg, initlock |
| `kernel/fs/VFS_ext.c#vfs_ext_futimens` | `kernel/fs/VFS_ext.c#vfs_ext_futimens` | 同板块同名同结构 | NS_to_S, TIME2NS, TIMESEPC2NS, ext4_atime_set, ext4_mtime_set, panic ...(+1) | NS_to_S, TIME2NS, TIMESEPC2NS, ext4_atime_set, ext4_mtime_set, panic ...(+1) |
| `kernel/fs/VFS_ext.c#vfs_ext_utimens` | `kernel/fs/VFS_ext.c#vfs_ext_utimens` | 同板块同名同结构 | NS_to_S, TIME2NS, TIMESEPC2NS, ext4_atime_set, ext4_mtime_set, rdtime | NS_to_S, TIME2NS, TIMESEPC2NS, ext4_atime_set, ext4_mtime_set, rdtime |
| `kernel/drive/rv/plic.c#plicinithart` | `kernel/drive/rv/plic.c#plicinithart` | 同板块同名同结构 | PLIC_SENABLE, PLIC_SPRIORITY, cpuid, printf | PLIC_SENABLE, PLIC_SPRIORITY, cpuid, printf |

#### 系统调用
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `kernel/syscall/syssig.c#sys_tgkill` | `kernel/syscall/syssig.c#sys_tgkill` | 同板块同名 | argint, send_signal_thread | argint, send_signal_thread |
| `kernel/syscall/syscall.c#argstr` | `kernel/syscall/syscall.c#argstr` | 同板块同名同结构 | argaddr, fetchstr | argaddr, fetchstr |
| `kernel/syscall/syssig.c#sys_tkill` | `kernel/syscall/syssig.c#sys_tkill` | 同板块同名同结构 | argint, send_signal_thread | argint, send_signal_thread |
| `kernel/syscall/syssig.c#sys_kills` | `kernel/syscall/syssig.c#sys_kills` | 同板块同名同结构 | argint, send_signal | argint, send_signal |

#### 文件系统
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `user/grind.c#go` | `user/grind.c#go` | 同板块同名同结构 | chdir, close, dup, exec, exit, fork ...(+16) | chdir, close, dup, exec, exit, fork ...(+16) |
| `user/init-la.c#test_final` | `user/init-la.c#test_final` | 同板块同名同结构 | chdir, exec, execve, exit, fork, printf ...(+1) | chdir, exec, execve, exit, fork, printf ...(+1) |
| `kernel/fs/vfs/ops.c#get_absolute_path` | `kernel/fs/vfs/ops.c#get_absolute_path` | 同板块同名同结构 | strcat, strcpy, strlen | strcat, strcpy, strlen |
| `user/new.c#test_openat_functionality` | `user/new.c#test_openat_functionality` | 同板块同名同结构 | close, mkdirat, openat, printf, read, unlinkat ...(+1) | close, mkdirat, openat, printf, read, unlinkat ...(+1) |

#### 设备驱动
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `kernel/drive/la/pci.c#pci_device_bar_init` | `kernel/drive/la/pci.c#pci_device_bar_init` | 同板块同名同结构 | 内部闭环逻辑 | 内部闭环逻辑 |
| `kernel/drive/la/virtio_ring.c#virtio_vring_init` | `kernel/drive/la/virtio_ring.c#virtio_vring_init` | 同板块同名同结构 | VQ_ALIGN, virtio_vring_size | VQ_ALIGN, virtio_vring_size |
| `kernel/drive/la/virtio_pci.c#virtio_pci_set_queue_addr2` | `kernel/drive/la/virtio_pci.c#virtio_pci_set_queue_addr2` | 同板块同名同结构 | PCI_REG16, PCI_REG64, VIRT2PHY, dsb | PCI_REG16, PCI_REG64, VIRT2PHY, dsb |
| `kernel/drive/uart.c#uartstart` | `kernel/drive/uart.c#uartstart` | 同板块同名同结构 | ReadReg, WriteReg, wakeup | ReadReg, WriteReg, wakeup |

#### 文件系统/第三方库
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `kernel/fs/lwext4/ext4.c#ext4_dir_rm` | `kernel/fs/lwext4/ext4.c#ext4_dir_rm` | 同板块同名同结构 | EXT4_MP_LOCK, EXT4_MP_UNLOCK, ext4_block_cache_write_back, ext4_dir_en_get_inode, ext4_dir_iterator_fini, ext4_dir_iterator_init ...(+19) | EXT4_MP_LOCK, EXT4_MP_UNLOCK, ext4_block_cache_write_back, ext4_dir_en_get_inode, ext4_dir_iterator_fini, ext4_dir_iterator_init ...(+19) |
| `kernel/fs/lwext4/ext4_balloc.c#ext4_balloc_alloc_block` | `kernel/fs/lwext4/ext4_balloc.c#ext4_balloc_alloc_block` | 同板块同名同结构 | ext4_balloc_get_bgid_of_block, ext4_balloc_get_block_of_bgid, ext4_balloc_set_bitmap_csum, ext4_balloc_verify_bitmap_csum, ext4_bg_get_block_bitmap, ext4_bg_get_free_blocks_count ...(+19) | ext4_balloc_get_bgid_of_block, ext4_balloc_get_block_of_bgid, ext4_balloc_set_bitmap_csum, ext4_balloc_verify_bitmap_csum, ext4_bg_get_block_bitmap, ext4_bg_get_free_blocks_count ...(+19) |
| `kernel/fs/lwext4/ext4_journal.c#jbd_journal_prepare` | `kernel/fs/lwext4/ext4_journal.c#jbd_journal_prepare` | 同板块同名同结构 | EXT4_BLOCK_ZERO, RB_FIND, TAILQ_FOREACH_REVERSE_SAFE, TAILQ_FOREACH_SAFE, TAILQ_REMOVE, ext4_bcache_clear_dirty ...(+17) | EXT4_BLOCK_ZERO, RB_FIND, TAILQ_FOREACH_REVERSE_SAFE, TAILQ_FOREACH_SAFE, TAILQ_REMOVE, ext4_bcache_clear_dirty ...(+17) |
| `kernel/fs/lwext4/ext4_dir_idx.c#ext4_dir_dx_split_index` | `kernel/fs/lwext4/ext4_dir_idx.c#ext4_dir_dx_split_index` | 同板块同名同结构 | ext4_block_set, ext4_dir_dx_climit_get_count, ext4_dir_dx_climit_get_limit, ext4_dir_dx_climit_set_count, ext4_dir_dx_climit_set_limit, ext4_dir_dx_entry_get_hash ...(+10) | ext4_block_set, ext4_dir_dx_climit_get_count, ext4_dir_dx_climit_get_limit, ext4_dir_dx_climit_set_count, ext4_dir_dx_climit_set_limit, ext4_dir_dx_entry_get_hash ...(+10) |

#### 通用模块/其他
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `user/printf.c#vprintf` | `user/printf.c#vprintf` | 同板块同名同结构 | putc_buf, va_arg, write | putc_buf, va_arg, write |
| `kernel/util/string.c#snprintf` | `kernel/util/string.c#snprintf` | 同板块同名同结构 | printint_to_buf, putchar_to_buf, puts_to_buf, va_arg, va_end, va_start | printint_to_buf, putchar_to_buf, puts_to_buf, va_arg, va_end, va_start |
| `kernel/util/printf.c#printf_highlight` | `kernel/util/printf.c#printf_highlight` | 同板块同名同结构 | acquire, consputc, printint, printptr, release, reset_text_color ...(+4) | acquire, consputc, printint, printptr, release, reset_text_color ...(+4) |
| `kernel/util/printf.c#printf_color` | `kernel/util/printf.c#printf_color` | 同板块同名同结构 | acquire, consputc, printint, printptr, release, reset_text_color ...(+4) | acquire, consputc, printint, printptr, release, reset_text_color ...(+4) |


好的，审判长。本庭已收到控辩双方的全部陈述、证据底座及原始AST抽样数据。现依据《审计指令（深度析理模式）》，对本案进行最终同源性定性仲裁。

---

### 1. 宏观判定
- **定性结论**: **部分逻辑借鉴 / 存在架构改动** (相似度: 80.36%，共同 AST 指纹: 814；唯一 AST 指纹: 目标项目 878，参考项目 949；覆盖率: 目标项目 92.71%，参考项目 85.77%)

本庭认定，目标项目 `[2026-T2026900029911083-XX]` 与参考项目 `[2025-静春山-中山大学]` 之间存在显著的结构性相似，但该相似性主要源于**共同的技术生态、教学基线及第三方开源代码**，而非对参考项目的直接、系统性抄袭。目标项目在核心模块上展现出独立的架构改动与功能裁剪，其原创性主张在宏观层面成立。

### 2. 全模块覆盖矩阵解读

| 模块 | 目标函数数 | 参考函数数 | 共同 AST 指纹 | Target 覆盖 | Base 覆盖 | 证据强度 | 解读 |
| :--- | ---: | ---: | ---: | ---: | ---: | :--- | :--- |
| **进程管理** | 56 | 68 | 38 | 80.85% | 70.37% | **中** | 双方均存在该模块。高覆盖率源于共同遵循的xv6调度与同步范式。但双方各有约20%-30%的独立函数，表明非完全照搬，而是在共同基线之上的独立演进。 |
| **任务调度/异步运行时** | 64 | 69 | 54 | 85.71% | 79.41% | **中** | 双方均存在。高覆盖率同样由xv6的上下文切换、锁机制等标准实现导致。该模块与进程管理高度耦合，其相似性成因一致。 |
| **内存管理** | 162 | 161 | 119 | 76.28% | 76.77% | **中** | 双方均存在。覆盖率高度对称，说明两者在内存管理子系统的实现完整度上相近，且共享大量标准算法（如页表操作、分配器逻辑）。未覆盖部分指向各自的独立扩展或裁剪。 |
| **中断/异常处理** | 35 | 39 | 28 | 100.0% | 87.5% | **弱** | 双方均存在。目标项目覆盖率100%是因为其函数总数较少，且全部落入与参考项目的交集。这通常意味着目标项目实现了基线所需的最小中断/异常处理集，而非抄袭。 |
| **系统调用** | 7 | 41 | 4 | 80.0% | 33.33% | **弱** | 双方均存在。**这是关键差异点**。参考项目拥有41个系统调用函数，而目标项目仅7个。目标项目极低的函数数量表明其进行了大幅裁剪或采用了不同的系统调用分发机制，这是独立架构设计的强信号。 |
| **文件系统** | 109 | 133 | 88 | 93.62% | 79.28% | **中** | 双方均存在。高覆盖率主要由VFS（虚拟文件系统）层的标准接口和共同集成的`lwext4`库贡献。目标项目更少的函数数暗示其文件系统功能范围更窄或实现更精简。 |
| **设备驱动** | 38 | 42 | 30 | 96.77% | 85.71% | **弱** | 双方均存在。高覆盖率源于对相同硬件（如UART、VirtIO）的驱动实现，这些驱动的寄存器操作流程由硬件手册规定，逻辑高度收敛。 |
| **网络协议栈** | 0 | 0 | 0 | 0.0% | 0.0% | **未形成** | 双方均未实现网络协议栈，无证据。 |
| **文件系统/第三方库** | 489 | 489 | 408 | 100.0% | 100.0% | **弱（降权）** | 双方均存在。**这是总体高覆盖率的主要贡献者**。100%重合确证双方集成了完全相同的`lwext4`第三方开源库。此为公共依赖，**不能作为项目间抄袭的证据**。 |
| **通用模块/其他** | 71 | 126 | 64 | 98.46% | 64.0% | **弱** | 双方均存在。包含`printf`、`string`等基础工具函数。目标项目覆盖率极高而参考项目较低，说明目标项目工具集更贴近基线，参考项目则进行了大量扩展。 |

### 3. 底层 AST 关键证据指纹表
> 表格按模块优先级排序，第三方库证据后置。

| 所属板块 | 目标项目路径 | 参考项目路径 | 证据类型 | 技术说明 |
| :--- | :--- | :--- | :--- | :--- |
| 进程管理 | `kernel/proc/proc.c#scheduler` | `kernel/proc/proc.c#scheduler` | 同源指纹 | 调度器核心循环，调用流`acquire->intr_on->mycpu->release->swtch`完全一致。这是xv6调度器的标准骨架，属于教学基线特征。 |
| 进程管理 | `user/sh.c#parseexec` | `user/sh.c#parseexec` | 同源指纹 | Shell解析器，调用流`execcmd->gettoken->panic->parseblock->parseredirs->peek`一致。该结构由xv6 Shell的文法规则决定，是功能必然。 |
| 进程管理 | `kernel/proc/proc.c#sched` | `kernel/proc/proc.c#sched` | 同源指纹 | 上下文切换入口，调用流`holding->intr_get->mycpu->myproc->panic->swtch`一致。这是xv6中执行`swtch`前的标准状态检查序列。 |
| 内存管理 | `kernel/syscall/sysfile.c#sys_execve` | `kernel/syscall/sysfile.c#sys_execve` | 同源指纹 | `execve`系统调用包装，调用流`NELEM, argaddr, argstr, execve, fetchaddr, fetchstr...`一致。参数解析与内核派发逻辑遵循POSIX标准范式。 |
| 内存管理 | `kernel/proc/exec.c#execve` | `kernel/proc/exec.c#execve` | 同源指纹 | 程序加载核心实现，调用流`AUXV, PGROUNDDOWN, PGROUNDUP, acquire, copyout...`一致。涉及ELF加载、页面对齐、地址空间切换，是xv6中逻辑最复杂的函数之一，但仍是标准实现。 |
| 文件系统 | `kernel/fs/vfs/ops.c#get_absolute_path` | `kernel/fs/vfs/ops.c#get_absolute_path` | 同源指纹 | 路径解析函数，调用流`strcat, strcpy, strlen`一致。纯字符串操作，逻辑简单，独立实现重合概率高。 |
| 设备驱动 | `kernel/drive/la/virtio_pci.c#virtio_pci_read_caps` | `kernel/drive/la/virtio_pci.c#virtio_pci_read_caps` | 同源指纹 | VirtIO PCI能力读取，调用流`get_cfg_addr, pci_config_read, pci_config_read8, printf`一致。遵循PCI规范的标准操作序列。 |
| 第三方库 | `kernel/fs/lwext4/ext4.c#ext4_dir_rm` | `kernel/fs/lwext4/ext4.c#ext4_dir_rm` | 公共依赖 | 调用流`EXT4_MP_LOCK, ext4_dir_iterator_init...`等完全一致。确证双方均集成了相同的`lwext4`开源库，非项目间抄袭。 |

### 4. 核心代码调用流对比剖析

*   **【进程管理/调度与上下文切换】**
    *   **底层技术析理**: 两项目的进程调度器（`scheduler`）与上下文切换（`sched`）均采用xv6经典的“进程锁 + 开中断 + `swtch`”协作式调度模型。该模型的内存抽象（直接操作`struct proc`和`struct cpu`）、同步原语（自旋锁`acquire/release`）及对象管理模型（`mycpu()`/`myproc()`）完全一致。这种一致性是共同教学基线的直接体现，而非独立创作的巧合。
    *   **代码溯源链接**:
        -   目标项目: `kernel/proc/proc.c#scheduler`
        -   参考项目: `kernel/proc/proc.c#scheduler`

*   **【内存管理/程序加载】**
    *   **底层技术析理**: `execve`系统调用及其底层实现`execve`的调用流高度一致。两者均遵循“参数拷贝 → ELF加载 → 页表构建 → 用户栈初始化 → 返回用户态”的标准流程。内存抽象（使用`PGROUNDUP/DOWN`进行页对齐）、同步策略（在修改进程地址空间时持有`proc->lock`）均属xv6标准范式。
    *   **代码溯源链接**:
        -   目标项目: `kernel/proc/exec.c#execve`
        -   参考项目: `kernel/proc/exec.c#execve`

*   **【文件系统/路径解析】**
    *   **底层技术析理**: `get_absolute_path`函数负责将用户输入的路径转换为内核可识别的绝对路径。两者均采用基于字符串拼接（`strcat`/`strcpy`）的简单实现，未涉及复杂的VFS查找或缓存机制。这种实现方式在简单内核中极为常见，其调用流重合不构成强同源证据。
    *   **代码溯源链接**:
        -   目标项目: `kernel/fs/vfs/ops.c#get_absolute_path`
        -   参考项目: `kernel/fs/vfs/ops.c#get_absolute_path`

### 5. 单方模块与降权项

*   **系统调用模块的巨大差异（强独立信号）**：参考项目拥有41个系统调用函数，而目标项目仅有7个。这是一个根本性的架构差异，表明目标项目并非简单地复用参考项目的系统调用层，而是进行了独立的、大幅度的裁剪或重构。此差异有力地削弱了“系统性抄袭”的指控。
*   **第三方库 `lwext4`（降权为公共依赖）**：`文件系统/第三方库`模块的489个函数100%重合，是总体AST相似度高达80.36%的主要原因。双方均集成了该开源库，此部分重合必须从“项目间抄袭”的证据链中完全剔除。
*   **通用工具函数（降权为弱信号）**：`printf`、`snprintf`、`strlen`、`strncpy`等函数属于任何C语言内核的基础设施。其调用图谱（如`va_arg, consputc`）由C标准库和底层硬件（UART等）决定，独立实现必然高度相似，不构成同源证据。
*   **跨板块同名函数（降权为弱信号）**：如`acquire`、`release`、`printf`、`argint`等函数在多个板块出现。它们是内核的公共基础原语，其调用关系不能作为特定板块抄袭的证据。

### 6. 保守结论

本庭综合裁定，目标项目与参考项目之间80.36%的AST相似度，主要归因于双方共同遵循的**MIT xv6教学内核基线**、**POSIX/Linux系统调用标准**、**RISC-V硬件规范**以及共同集成的**第三方开源库lwext4**。控方基于函数级调用图谱的“系统性复用”指控，在方法论上存在局限性，无法区分“功能必然重合”与“代码抄袭”。辩方关于“共同基线+独立演进”的辩护更为合理，并得到了系统调用模块函数数量悬殊（7 vs 41）等关键架构差异证据的支持。因此，现有证据不足以认定目标项目构成对参考项目的抄袭或违规复用。建议将本案风险等级定为“低”，并建议控方若坚持指控，需补充提交逐行代码diff或token级相似度报告作为更精细的证据。

------------------------------

