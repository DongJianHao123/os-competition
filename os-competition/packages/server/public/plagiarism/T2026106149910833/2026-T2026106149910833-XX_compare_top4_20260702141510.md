# 🛡️ OS 内核赛自动化代码级架构审计报告

**审计时间**: `2026-07-02 14:15:10`
**参赛作品名称**: `2026-T2026106149910833-XX`
**年份**: `2026`
**学校**: `XX`
**队伍名称**: `T2026106149910833`
**比赛名字**: `OS-Competition`
**比赛子赛道**: `Kernel-Implementation`
**仓库地址**: `https://gitlab.eduxiji.net/educg-group-43501-3132633/os-kernel-2026-xt-2026106149910833`
**推测开发语言**: `Rust / C (由AST探针自动识别)`
**特征函数量**: `4350`

## ⚔️ 底层代码同源性对比 (AST Homology)
> 本部分由专门的代码审查智能体，对该项目与符合年份条件的历届最高相似度项目进行控制流与底层调用对比。新版报告会先给出确定性的全模块覆盖矩阵，再给出 Agent 的分模块解释，避免只围绕少量抽样函数展开。

### 🏆 Top 4 底层对标: vs `2022-FTLOS-哈尔滨工业大学深圳`（哈尔滨工业大学（深圳） / FTL OS）

#### 对方作品信息

**对方年份**: `2022`
**对方学校**: `哈尔滨工业大学（深圳）`
**对方队伍名称**: `FTL OS`
**对方比赛名字**: `OS-Competition`
**对方比赛子赛道**: `Kernel-Implementation`
**对方仓库地址**: `https://gitlab.eduxiji.net/DarkAngelEX/oskernel2022-ftlos.git`

> 双向覆盖率：共同 AST 指纹: 14；唯一 AST 指纹: 目标项目 3475，参考项目 4206；覆盖率: 目标项目 0.4%，参考项目 0.33%

## 全模块对比总览（确定性证据）

> 总体覆盖率：共同 AST 指纹: 14；唯一 AST 指纹: 目标项目 3475，参考项目 4206；覆盖率: 目标项目 0.4%，参考项目 0.33%

### 1. 模块覆盖矩阵
| 模块 | 目标函数数 | 参考函数数 | 共同唯一 AST 指纹 | Target 覆盖 | Base 覆盖 | 状态 |
| :--- | ---: | ---: | ---: | ---: | ---: | :--- |
| 进程管理 | 742 | 243 | 1 | 0.16% | 0.5% | 双方存在且有结构重合 |
| 任务调度/异步运行时 | 622 | 172 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |
| 内存管理 | 724 | 822 | 2 | 0.34% | 0.27% | 双方存在且有结构重合 |
| 中断/异常处理 | 148 | 203 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |
| 系统调用 | 380 | 65 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |
| 文件系统 | 1473 | 817 | 1 | 0.08% | 0.15% | 双方存在且有结构重合 |
| 设备驱动 | 61 | 73 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |
| 网络协议栈 | 0 | 0 | 0 | 0.0% | 0.0% | 双方均未体现 |
| 文件系统/第三方库 | 55 | 24 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |
| 第三方库 | 0 | 2375 | 0 | 0.0% | 0.0% | 仅参考项目体现 |
| 通用模块/其他 | 145 | 956 | 2 | 1.83% | 0.25% | 双方存在且有结构重合 |

### 2. 分模块证据清单

#### 进程管理
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `source/core/syscall/process_lifecycle.rs#sys_wait4` | `code/kernel/src/syscall/process.rs#sys_wait4` | 同板块同名 | Pid::new, SyscallBlock::wait_child, SyscallError::Child.errno, SyscallError::NotSupported.errno, SyscallOutcome::Blocked, SyscallOutcome::Return ...(+6) | Err, Ok, Pid::from_usize, Some, UserCheck::new, UserCheck::new(self.process)
                        .writable_value ...(+27) |
| `source/core/syscall/process_control.rs#sys_setpgid` | `code/kernel/src/syscall/process.rs#sys_setpgid` | 同板块同名 | error.errno, frame.arg, map_process_control_error, map_process_control_error(error).errno, pid_arg_or_current, process_group_arg_or_target ...(+1) | Ok, Some, p.deref, println!, process.pgid.store, process.pid ...(+3) |
| `source/kernel/exec/discovery.rs#append` | `code/kernel/src/process/children.rs#append` | 同板块同名 | Err, Ok, bytes.len, record_boot_init_blocker, self
            .len
            .checked_add, self
            .len
            .checked_add(bytes.len())
            .ok_or ...(+3) | self.alive.append, self.become_zombie, self.zombie.append, zombie_pending.append, zombie_pending.pop_first |
| `source/core/syscall/process_lifecycle.rs#sys_clone` | `code/kernel/src/syscall/process.rs#sys_clone` | 同板块同名 | ForkRequest::from_linux_clone, SyscallOutcome::Return, admit_fork_request, frame.arg, map_clone_error, map_clone_error(error, false).errno | (flag & CloneFlag::EXIT_SIGNAL).bits, CloneFlag::from_bits, CloneFlag::from_bits(flag as u64).ok_or, Ok, UserCheck::new, UserCheck::new(self.process).writable_value ...(+18) |

#### 任务调度/异步运行时
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `source/core/syscall/poll.rs#sys_pselect6` | `code/kernel/src/syscall/fs/select.rs#sys_pselect6` | 同板块同名 | Some, SyscallBlock::select_readable, SyscallBlock::select_writable, SyscallError::Invalid.errno, SyscallError::NotSupported.errno, SyscallOutcome::Blocked ...(+14) | Err, Fd, Ok, SelectFuture::new, TimeoutFuture::new, UserCheck::new ...(+56) |
| `source/core/syscall/fd_write/range_copy.rs#sys_sendfile` | `code/kernel/src/syscall/fs/mod.rs#sys_sendfile` | 同板块同名 | SyscallError::BadFileDescriptor.errno, SyscallError::Invalid.errno, SyscallOutcome::Return, copy_file_range_start_offset, copy_file_range_store_offset, error.errno ...(+16) | Err, Ok, UserCheck::new, UserCheck::new(self.process)
            .writable_value_nullable, Vec::new, a.fd_table.get ...(+12) |
| `source/core/syscall/fd_write.rs#sys_writev` | `code/kernel/src/syscall/fs/mod.rs#sys_writev` | 同板块同名 | SyscallOutcome::Return, copy_user_iovecs, ensure_pipe_buf_atomic_write, error.errno, fd_write_block_or_errno, frame.arg ...(+2) | Err, Fd::new, Ok, UserCheck::new, a.fd_table.get, a.fd_table.get(Fd::new(fd)).cloned ...(+11) |
| `source/core/task/credentials.rs#store` | `code/kernel/src/signal/context.rs#store` | 同板块同名 | self.effective_capabilities.store, self.effective_gid
            .store, self.effective_uid
            .store, self.filesystem_gid
            .store, self.filesystem_uid
            .store, self.inheritable_capabilities.store ...(+5) | self.sepc, uk_cx.user_rx[1..].copy_from_slice |

#### 内存管理
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `source/core/syscall/memory.rs#sys_mmap` | `code/kernel/src/syscall/mmap.rs#sys_mmap` | 同板块同名 | MappingFlags::user, Some, SyscallError::BadFileDescriptor.errno, SyscallError::Exists.errno, SyscallError::Invalid.errno, SyscallError::NoMemory.errno ...(+41) | Err, MmapFlags::from_bits, MmapFlags::from_bits(flags).unwrap, MmapHandler::box_new, MmapProt::from_bits, MmapProt::from_bits(prot).unwrap ...(+37) |
| `source/core/syscall/memory.rs#sys_brk` | `code/kernel/src/syscall/process.rs#sys_brk` | 同板块同名 | SyscallError::NoMemory.errno, address_space_limit_allows, align_up, data_size_limit_allows_break, frame.arg, memory.unmap_user_pages ...(+8) | Ok, UserAddr::try_from, a.user_space.get_brk, a.user_space.reset_brk, brk.into_usize, frame::default_allocator ...(+5) |
| `source/core/mm/address_space.rs#page_copy` | `code/ftl-util/src/faster.rs#page_copy` | 同板块同名 | Err, Ok, Some, max_usize, min_usize, page_start
            .checked_add ...(+8) | 内部闭环逻辑 |
| `source/core/syscall/memory.rs#sys_mprotect` | `code/kernel/src/syscall/mmap.rs#sys_mprotect` | 同板块同名 | MappingFlags::user, SyscallError::Invalid.errno, SyscallError::NoMemory.errno, align_up, flags.is_executable, flags.is_readable ...(+8) | MmapProt::from_bits_truncate, MmapProt::from_bits_truncate(prot).into_perm, Ok, PageCount::page_ceil, alive.asid, alive.user_space.map_segment.modify_perm ...(+12) |

#### 中断/异常处理
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `source/arch/loongarch64/timer.rs#enable_timer_interrupt` | `code/kernel/src/trap/mod.rs#enable_timer_interrupt` | 同板块同名 | core::arch::asm! | sie::set_stimer |
| `source/arch/riscv64/trap/block_dispatch.rs#consume_blocked_syscall` | `code/kernel/src/tools/container/intrusive_linked_list.rs#sort_no_buffer` | 同模块上下文对齐/差异观察 | block.pid, block.previous_single_signal_mask, block.reason, consume_epoll_wait_block, consume_fd_wait_block, consume_futex_wait_block ...(+10) | Some, assert!, merge, merge_sort, next, next_v ...(+9) |
| `source/arch/loongarch64/trap_block.rs#consume_blocked_syscall` | `code/kernel/src/syscall/futex.rs#sys_futex` | 同模块上下文对齐/差异观察 | block.pid, block.previous_single_signal_mask, block.reason, consume_epoll_wait_block, consume_fd_wait_block, consume_futex_wait_block ...(+10) | Err, Some, panic!, println!, self.cx.into, self.futex_requeue ...(+7) |
| `source/arch/riscv64/trap/fork.rs#map_user_map_error` | `code/fat32/src/inode/file_inode.rs#write_at_fast` | 同模块上下文对齐/差异观察 | SyscallError::Invalid.errno, SyscallError::NoDevice.errno, SyscallError::NoMemory.errno, SyscallError::NotSupported.errno | Err, Ok, buffer.len, buffer.len().min, buffer_0.len, buffer_0.len().min ...(+15) |

#### 系统调用
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `source/core/syscall/socket.rs#recvmsg_control` | `code/user/src/syscall.rs#syscall` | 同模块上下文对齐/差异观察 | Ok, write_recvmsg_credentials_control, write_recvmsg_ipv4_int_control, write_recvmsg_ipv4_packet_info_control, write_recvmsg_timestamp_control | a[0..args.len()].copy_from_slice, args.len, asm!, panic! |
| `source/core/syscall/system.rs#sys_getrandom` | `code/kernel/src/syscall/random.rs#fetch_random_state` | 同模块上下文对齐/差异观察 | SyscallError::Fault.errno, SyscallError::Invalid.errno, copy_to_user, copy_to_user(memory, target, &buffer[..chunk_len]).is_err, fill_pseudorandom, frame.arg ...(+2) | RANDOM_STATE
            .compare_exchange, RANDOM_STATE
            .compare_exchange(old, new.1, Ordering::Relaxed, Ordering::Relaxed)
            .is_ok, RANDOM_STATE.load, timer::now, timer::now().as_nanos, tools::xor_shift_128_plus |
| `source/core/syscall/sysv_msg.rs#write_msgid_ds` | `code/user/src/lib.rs#waitpid` | 同模块上下文对齐/差异观察 | SyscallError::Fault.errno, copy_to_user, copy_to_user(memory, address, &raw).is_err, put_u32, put_u64, queue.cbytes.load ...(+10) | sys_waitpid, yield_ |
| `source/core/syscall/socket.rs#read_sockaddr_unix` | `code/user/src/lib.rs#wait` | 同模块上下文对齐/差异观察 | Err, Ok, SocketEndpoint::UnixPath, SocketUnixAddress::new, copy_from_user, copy_from_user(memory, ptr, &mut raw[..copy_len]).map_err ...(+3) | sys_waitpid, yield_ |

#### 文件系统
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `source/core/fs/vfs/overlay.rs#rename` | `code/vfs/src/manager/mod.rs#rename` | 同板块同名 | Err, Ok, VfsPathBuffer::root, new.bytes, old.bytes, path_is_child ...(+15) | println!, stack_trace!, todo! |
| `source/core/loader/elf.rs#parse` | `code/kernel/src/elf/mod.rs#parse` | 同板块同名 | Err, Ok, UserEntryAddress::new, UserEntryAddress::new(entry).map_err, abi.elf_machine, abi.os_abi ...(+4) | Err, HeaderPt2::from_32header, Ok, core::mem::MaybeUninit::uninit, core::mem::MaybeUninit::uninit().assume_init, core::mem::transmute ...(+8) |
| `source/kernel/syscall_runtime/vfs_backend.rs#pipe` | `code/user/src/lib.rs#pipe` | 同板块同名 | Err, Ok, VfsRuntimeError::Fd, active_fd_table_mut, active_fd_table_mut().close, active_fd_table_mut().insert ...(+5) | sys_pipe |
| `source/kernel/exec/discovery.rs#finish` | `code/vfs/src/hash_name.rs#finish` | 同板块同名 | BootDiscoveryCommandSegment::empty, BootInitPath::new, BootInitPath::new(first.init_path.bytes()).map_err, Ok, Some, VfsPathBuffer::root ...(+5) | 内部闭环逻辑 |

#### 设备驱动
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `source/core/block.rs#read_sector` | `code/kernel/src/drivers/block/sdcard.rs#read_sector` | 同板块同名 | BlockReadRequest::new, Err, Ok, completion.error, completion.request, copy_sector ...(+2) | Err, Ok, assert!, chunk.iter_mut, chunk.iter_mut().zip, data_buf.chunks_mut ...(+6) |
| `source/arch/loongarch64/block.rs#read_sector` | `code/kernel/src/drivers/spi_sd/mod.rs#read_sector` | 同板块同名 | BlockReadCompletion::completed, BlockReadCompletion::failed, read_sector_inner, request.sector | Ok, assert!, data_buf.array_chunks_mut::<SEC_LEN>, panic!, self.end_cmd, self.read_one_block ...(+1) |
| `source/arch/riscv64/block.rs#read_sector_inner` | `code/kernel/src/drivers/block/sdcard.rs#write_sector` | 同模块上下文对齐/差异观察 | Err, Ok, avail_idx.wrapping_add, avail_ptr, compiler_fence, core::hint::spin_loop ...(+12) | (data_buf.len() / SEC_LEN).try_into, (data_buf.len() / SEC_LEN).try_into().unwrap, Err, Ok, assert!, chunk.iter ...(+9) |
| `source/arch/riscv64/block.rs#ensure_ready_modern` | `code/kernel/src/drivers/spi_sd/mod.rs#get_cidregister` | 同模块上下文对齐/差异观察 | Err, Ok, VirtqUsedElem::empty, compiler_fence, core::ptr::addr_of!, mmio_read ...(+1) | Err, Ok, self.end_cmd, self.get_response, self.read_data, self.send_cmd ...(+2) |

#### 文件系统/第三方库
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `source/core/fs/ext4/inode.rs#parse` | `code/dependencies/object/src/read/pe/data_directory.rs#parse` | 同板块同名 | Err, Ok, bytes.len, extent_root.copy_from_slice, read_u16, read_u32 | Ok, data
            .read_slice_at, data
            .read_slice_at(0, number as usize)
            .read_error |
| `source/core/fs/ext4.rs#read_dir_entry_at` | `code/dependencies/object/crates/examples/src/readobj/pe.rs#print_export_dir` | 同模块上下文对齐/差异观察 | Err, Ok, Some, VfsDirEntry::new, VfsDirEntry::new(inode_number, next_offset, name, kind)
                    .map_err, cursor
                .checked_add ...(+11) | Some, address.get, data_directories
        .export_directory, data_directories
        .export_directory(data, sections)
        .print_err, data_directories.export_table, data_directories.export_table(data, sections).print_err ...(+29) |
| `source/core/fs/ext4.rs#from_superblock` | `code/dependencies/object/crates/examples/src/readobj/pe.rs#print_import_dir` | 同模块上下文对齐/差异观察 | (EXT4_MIN_BLOCK_SIZE..=EXT4_MAX_BLOCK_SIZE).contains, EXT4_MIN_BLOCK_SIZE
            .checked_shl, EXT4_MIN_BLOCK_SIZE
            .checked_shl(log_block_size)
            .ok_or, Err, Ok, read_u16 ...(+1) | Some, address_thunks
                                .as_mut, address_thunks
                                .as_mut()
                                .and_then, address_thunks.clone, data_directories
        .import_table, data_directories
        .import_table(data, sections)
        .print_err ...(+31) |
| `source/core/fs/ext4.rs#lookup_path_followed_at` | `code/dependencies/object/crates/examples/src/readobj/pe.rs#print_delay_load_dir` | 同模块上下文对齐/差异观察 | Err, Ext4File::from_inode, Ok, VfsPathBuffer::root, append_child_path, child.is_symlink ...(+10) | Some, data_directories
        .delay_load_import_table, data_directories
        .delay_load_import_table(data, sections)
        .print_err, import_desc.attributes.get, import_desc.bound_import_address_table_rva.get, import_desc.dll_name_rva.get ...(+25) |

#### 第三方库
仅参考项目中观察到该模块代表函数，目标项目没有同模块可对齐证据：
- 参考项目：`code/dependencies/object/crates/examples/src/bin/elfcopy.rs#copy_file`，调用摘要：Default::default, Elf::parse, Ok, Some, Vec::new, Vec::with_capacity ...(+276)
- 参考项目：`code/dependencies/object/src/write/elf/object.rs#elf_write`，调用摘要：".rel".len, ".rela".len, Err, Error, Ok, Some ...(+80)
- 参考项目：`code/dependencies/object/src/write/coff.rs#coff_write`，调用摘要：Err, Error, Ok, Some, String::from, StringTable::default ...(+51)
- 参考项目：`code/dependencies/object/src/write/macho.rs#macho_write`，调用摘要：Err, Error, Ok, Some, String::from, StringTable::default ...(+58)

#### 通用模块/其他
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `source/core/loader/elf.rs#push` | `code/kernel/src/tools/container/lockfree/queue.rs#push` | 同板块同名 | Err, Ok, Some, existing.virtual_end, existing.virtual_start, ranges_overlap ...(+2) | Box::from_raw, Box::leak, Box::leak(Box::new(node)).into, Box::new, FailRun::new, LockfreeNode::new ...(+19) |
| `source/core/loader/elf.rs#push` | `code/kernel/src/tools/container/lockfree/stack.rs#push` | 同板块同名 | Err, Ok, self.entries.len | AtomicMarkedPtr::new, AtomicMarkedPtr::null, Box::leak, Box::leak(Box::new(node)).into, Box::new, MarkedPtr::new ...(+12) |
| `source/core/loader/elf.rs#get` | `code/ftl-util/src/container/str_map.rs#get` | 同板块同名 | 内部闭环逻辑 | Some, hash_fn, self.table[hash].iter |
| `source/core/loader/elf.rs#get` | `code/kernel/src/tools/container/range_map.rs#get` | 同板块同名 | 内部闭环逻辑 | Some, self.0.range, self.0.range(..=key).next_back |


好的，审计官。基于提供的全案材料，我将依据审计指令，对目标项目 `[2026-T2026106149910833-XX]` 与参考项目 `[2022-FTLOS-哈尔滨工业大学深圳]` 进行最终同源性定性仲裁。

### 1. 宏观判定
- **定性结论**: **独立开发 (底层架构显著差异)**
- **核心依据**: 客观 AST 相似度仅为 **0.18%**，共同 AST 指纹仅 **14** 个，在双方总计 **7681** 个唯一指纹中占比极低。目标项目覆盖率 **0.4%**，参考项目覆盖率 **0.33%**。该数据远低于代码抄袭或同源衍生的推定阈值。双方在核心架构范式（调度模型、进程表示、同步机制）上存在根本性差异，所有观察到的相似性均可被操作系统领域标准（POSIX/Linux ABI）和 Rust 语言惯用法所解释。

### 2. 全模块覆盖矩阵解读
| 模块 | 双方存在 | 共同指纹 | 覆盖率 (目标/参考) | 证据强度 | 解读 |
| :--- | :---: | :---: | :---: | :---: | :--- |
| **进程管理** | 是 | 1 | 0.16% / 0.5% | **弱** | 唯一共同指纹源于 POSIX 标准系统调用同名。双方实现模型完全不同：目标项目采用全局数组+位图，参考项目采用 `Arc<Process>` 对象+异步 Future。控方指控的“底层逻辑同构”实为 Linux 标准进程属性的领域共性。 |
| **任务调度/异步运行时** | 是 | 0 | 0.0% / 0.0% | **未形成** | 双方均实现调度模块，但 AST 层面无任何共同指纹。目标项目为同步阻塞式位图轮询，参考项目为 `async/await` 异步调度，架构范式截然不同。 |
| **内存管理** | 是 | 2 | 0.34% / 0.27% | **弱** | 共同指纹源于 `sys_mmap`、`sys_brk` 等 POSIX 标准系统调用。具体实现中，目标项目使用 `MappingFlags::user`，参考项目使用 `MmapFlags::from_bits`，外部调用对象无交集。 |
| **中断/异常处理** | 是 | 0 | 0.0% / 0.0% | **未形成** | 双方均存在该模块，但无共同指纹。目标项目为 RISC-V/LoongArch 双架构陷阱分发，参考项目为 RISC-V 单架构，且上下文切换路径调用的底层对象完全不同。 |
| **系统调用** | 是 | 0 | 0.0% / 0.0% | **未形成** | 双方均实现系统调用层，但无共同指纹。目标项目使用 `SyscallBlock` 和 `frame.arg`，参考项目使用 `UserCheck` 和 `self.cx.para1`，参数传递与分发模型异构。 |
| **文件系统** | 是 | 1 | 0.08% / 0.15% | **弱** | 共同指纹源于 `rename` 等通用 VFS 操作。目标项目有完整的 `ext4` 实现，参考项目依赖 `fat32`，核心文件系统逻辑无重合。 |
| **设备驱动** | 是 | 0 | 0.0% / 0.0% | **未形成** | 双方均有块设备驱动，但无共同指纹。目标项目使用 `VirtqUsedElem`，参考项目使用 `spi_sd`，硬件交互模型不同。 |
| **网络协议栈** | 否 | 0 | 0.0% / 0.0% | **未形成** | 双方均未体现该模块。 |
| **文件系统/第三方库** | 是 | 0 | 0.0% / 0.0% | **未形成** | 目标项目包含 `ext4` 解析，参考项目包含 `object` 库用于 PE/ELF 解析，功能与来源均不同，无共同指纹。 |
| **第三方库** | 仅参考 | 0 | 0.0% / 0.0% | **未形成** | 参考项目包含大量 `object`、`fat32` 等第三方库代码（2375个函数），目标项目未体现，是双方代码量差异的主要来源。 |
| **通用模块/其他** | 是 | 2 | 1.83% / 0.25% | **弱** | 共同指纹源于 `push`、`get` 等 Rust 语言级通用方法名，且分布在 `elf loader` 与 `lockfree queue` 等完全不同功能的模块中，属于跨板块同名硬凑。 |

### 3. 底层 AST 关键证据指纹表
| 所属板块 | 目标项目路径 | 参考项目路径 | 证据类型 | 技术说明 |
| :--- | :--- | :--- | :--- | :--- |
| 进程管理 | `source/core/syscall/process_lifecycle.rs#sys_wait4` | `code/kernel/src/syscall/process.rs#sys_wait4` | 同板块同名 | POSIX 标准系统调用。目标使用 `SyscallBlock::wait_child` 阻塞模型，参考使用 `UserCheck` 异步模型，实现异构。 |
| 进程管理 | `source/core/syscall/process_lifecycle.rs#sys_clone` | `code/kernel/src/syscall/process.rs#sys_clone` | 同板块同名 | POSIX 标准系统调用。目标使用 `ForkRequest::from_linux_clone` 请求准入，参考使用 `CloneFlag::from_bits` 位解析，实现异构。 |
| 内存管理 | `source/core/syscall/memory.rs#sys_mmap` | `code/kernel/src/syscall/mmap.rs#sys_mmap` | 同板块同名 | POSIX 标准系统调用。目标使用 `MappingFlags::user`，参考使用 `MmapFlags::from_bits`，参数解析与内部调用链无交集。 |
| 内存管理 | `source/core/syscall/memory.rs#sys_brk` | `code/kernel/src/syscall/process.rs#sys_brk` | 同板块同名 | POSIX 标准系统调用。目标通过 `address_space_limit_allows` 检查，参考通过 `UserAddr::try_from` 转换，实现异构。 |
| 文件系统 | `source/core/fs/vfs/overlay.rs#rename` | `code/vfs/src/manager/mod.rs#rename` | 同板块同名 | 通用 VFS 操作。目标有完整路径解析逻辑，参考函数体为 `todo!`，实现程度天差地别。 |
| 通用模块/其他 | `source/core/loader/elf.rs#push` | `code/kernel/src/tools/container/lockfree/queue.rs#push` | 跨板块同名 | 通用方法名。目标用于 ELF 加载器段合并，参考用于无锁队列节点添加，功能与调用对象完全不同。 |
| 通用模块/其他 | `source/core/loader/elf.rs#get` | `code/ftl-util/src/container/str_map.rs#get` | 跨板块同名 | 通用方法名。目标为内部闭环逻辑，参考为哈希表查找，功能与调用对象完全不同。 |

### 4. 核心代码调用流对比剖析
* **【进程管理/进程创建与上下文切换】**
* **底层技术析理**: 此路径是控方指控的核心，但两者在抽象层次与实现机制上存在根本差异。目标项目采用**同步阻塞式位图模型**，通过 `prepare_blocked_context_switch` 直接操作调度器状态，并调用 `enter_next_task_runtime` 进行显式上下文切换。参考项目则采用**异步 Future 模型**，通过 `set_user_trap_entry` 和 `sstatus::clear_sie` 准备陷阱入口，最终通过 `__entry_user` 以 `sret` 指令返回用户态，依赖异步运行时调度。两者在内存模型（全局数组 vs. `Arc<Process>`）、同步策略（原子位操作 vs. `async/await`）上完全异构。
* **代码溯源链接**:
  - 目标项目: `source/arch/riscv64/trap/fork.rs#apply_vfork_child_context_switch`
  - 参考项目: `code/kernel/src/trap/mod.rs#run_user_executor`

* **【内存管理/内存映射】**
* **底层技术析理**: 双方均实现了 `sys_mmap`，但内部调用流截然不同。目标项目的核心是 `MappingFlags::user` 和一系列 `SyscallError` 映射，而参考项目的核心是 `MmapFlags::from_bits`、`MmapProt::from_bits` 以及 `MmapHandler::box_new`。这表明两者在标志位解析、错误处理封装和内存区域管理对象上均无共同来源。
* **代码溯源链接**:
  - 目标项目: `source/core/syscall/memory.rs#sys_mmap`
  - 参考项目: `code/kernel/src/syscall/mmap.rs#sys_mmap`

* **【系统调用/参数传递与分发】**
* **底层技术析理**: 在 `sys_clone` 的实现中，目标项目通过 `frame.arg` 从陷阱帧寄存器中提取参数，并构造 `ForkRequest` 对象进行准入检查。参考项目则通过 `self.cx.para1` 获取参数，直接进行 `CloneFlag` 位解析。这体现了两种不同的系统调用分发哲学：目标项目采用请求封装与准入模式，参考项目采用上下文直接解析模式。
* **代码溯源链接**:
  - 目标项目: `source/core/syscall/process_lifecycle.rs#sys_clone`
  - 参考项目: `code/kernel/src/syscall/process.rs#sys_clone`

### 5. 单方模块与降权项
*   **第三方库 (仅参考项目)**: 参考项目包含 `code/dependencies/object` 等目录下的 2375 个函数，主要用于 PE/ELF 等目标文件解析。目标项目无此模块，这是双方代码规模差异的主要原因，与核心内核逻辑的同源性无关。
*   **降权项 (跨板块同名/通用方法)**: 控辩双方均提及的 `append`、`push`、`bytes`、`pid` 等函数，属于 Rust 语言标准库或编程惯例中的通用方法名。例如，目标项目的 `append` 用于启动参数拼接，参考项目的 `append` 用于子进程列表管理，功能与调用上下文完全不同。此类跨板块同名不能作为同源证据。
*   **降权项 (POSIX 标准接口)**: `sys_wait4`、`sys_clone`、`sys_setpgid` 等所有同名系统调用，均为 Linux/POSIX 标准所要求的接口。函数名相同是兼容性要求，其内部实现路径经 AST 比对已证实异构。

### 6. 保守结论
本次审计中，目标项目与参考项目的客观 AST 相似度仅为 0.18%，远低于同源衍生或代码抄袭的判定阈值。控方提出的“底层逻辑同构”指控，实质上是将 Linux 标准进程语义、POSIX 系统调用接口等**领域共性**，以及 Rust 语言通用方法名等**语言惯用法**，误判为代码复用证据。双方在进程模型（扁平数组 vs. 引用计数对象）、调度模型（同步位图 vs. 异步 Future）、内存管理及系统调用分发等核心架构上存在根本性差异，外部调用图谱无实质重叠。综合判定，目标项目 `[2026-T2026106149910833-XX]` 为独立开发，未发现其与参考项目 `[2022-FTLOS-哈尔滨工业大学深圳]` 存在违规代码复用。

------------------------------

