# 🛡️ OS 内核赛自动化代码级架构审计报告

**审计时间**: `2026-07-02 14:15:10`
**参赛作品名称**: `2026-T2026106149910833-XX`
**年份**: `2026`
**学校**: `XX`
**队伍名称**: `T2026106149910833`
**比赛名字**: `OS-Competition`
**比赛子赛道**: `Kernel-Implementation`
**仓库地址**: `https://gitlab.eduxiji.net/educg-group-43501-3132633/os-kernel-2026-xt-2026106149910833`
**推测开发语言**: `Rust / C (由AST探针自动识别)`
**特征函数量**: `4350`

## ⚔️ 底层代码同源性对比 (AST Homology)
> 本部分由专门的代码审查智能体，对该项目与符合年份条件的历届最高相似度项目进行控制流与底层调用对比。新版报告会先给出确定性的全模块覆盖矩阵，再给出 Agent 的分模块解释，避免只围绕少量抽样函数展开。

### 🏆 Top 3 底层对标: vs `2025-sudo_pacman_Syu-同济大学`（同济大学 / sudo_pacman_Syu）

#### 对方作品信息

**对方年份**: `2025`
**对方学校**: `同济大学`
**对方队伍名称**: `sudo_pacman_Syu`
**对方比赛名字**: `OS-Competition`
**对方比赛子赛道**: `Kernel-Implementation`
**对方仓库地址**: `https://gitlab.eduxiji.net/educg-group-36002-2710490/T202510247995532-2817`

> 双向覆盖率：共同 AST 指纹: 11；唯一 AST 指纹: 目标项目 3475，参考项目 1840；覆盖率: 目标项目 0.32%，参考项目 0.6%

## 全模块对比总览（确定性证据）

> 总体覆盖率：共同 AST 指纹: 11；唯一 AST 指纹: 目标项目 3475，参考项目 1840；覆盖率: 目标项目 0.32%，参考项目 0.6%

### 1. 模块覆盖矩阵
| 模块 | 目标函数数 | 参考函数数 | 共同唯一 AST 指纹 | Target 覆盖 | Base 覆盖 | 状态 |
| :--- | ---: | ---: | ---: | ---: | ---: | :--- |
| 进程管理 | 742 | 154 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |
| 任务调度/异步运行时 | 622 | 164 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |
| 内存管理 | 724 | 261 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |
| 中断/异常处理 | 148 | 28 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |
| 系统调用 | 380 | 58 | 1 | 0.28% | 2.22% | 双方存在且有结构重合 |
| 文件系统 | 1473 | 318 | 1 | 0.08% | 0.34% | 双方存在且有结构重合 |
| 设备驱动 | 61 | 162 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |
| 网络协议栈 | 0 | 51 | 0 | 0.0% | 0.0% | 仅参考项目体现 |
| 文件系统/第三方库 | 55 | 48 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |
| 第三方库 | 0 | 740 | 0 | 0.0% | 0.0% | 仅参考项目体现 |
| 通用模块/其他 | 145 | 188 | 1 | 0.92% | 0.64% | 双方存在且有结构重合 |

### 2. 分模块证据清单

#### 进程管理
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `source/core/task/process.rs#exit` | `src/kernel/syscall/procops.rs#exit` | 同板块同名 | ExitState::new, ProcessState::Exited, scheduler::remove_task | ProcessList::get, ProcessList::get().write, WaitType::Exited, procs
            .do_exit |
| `source/core/task/process.rs#set_robust_list` | `src/kernel/syscall/procops.rs#set_robust_list` | 同板块同名 | 内部闭环逻辑 | Err, Ok, Some, size_of::<RobustListHead>, thread.set_robust_list |
| `source/core/task/process.rs#set_tid_address` | `src/kernel/syscall/procops.rs#set_tid_address` | 同板块同名 | 内部闭环逻辑 | Ok, Some, thread.clear_child_tid |
| `source/core/task/process.rs#clear_child_tid` | `src/kernel/task/thread.rs#clear_child_tid` | 同板块同名 | 内部闭环逻辑 | 内部闭环逻辑 |

#### 任务调度/异步运行时
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `source/core/task/credentials.rs#load` | `src/kernel/task/loader/elf.rs#load` | 同板块同名 | self.effective_capabilities.load, self.effective_gid.load, self.effective_uid.load, self.filesystem_gid.load, self.filesystem_uid.load, self.inheritable_capabilities.load ...(+5) | MMList::new, Ok, ldso_load_info
                .as_ref, ldso_load_info
                .as_ref()
                .map, ldso_load_info.entry_ip.into, mm_list.register_break ...(+8) |
| `source/core/task/single/slots.rs#load` | `src/kernel/task/loader/elf.rs#load` | 同板块同名 | ResourceLimit::new, kind.index, self.current[index].load, self.maximum[index].load | elf32.load, elf64.load |
| `source/arch/contract/context.rs#current` | `src/kernel/task/thread.rs#current` | 同板块同名 | 内部闭环逻辑 | BorrowedArc::from_raw, CURRENT_THREAD.get, CURRENT_THREAD.get().expect |
| `source/core/task/single/memory_runtime.rs#load` | `src/kernel/task/loader/mod.rs#load` | 同板块同名 | Self::load_from, active_user_memory_slot | elf.load |

#### 内存管理
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `source/core/fs/fd/socket.rs#accept` | `src/kernel/syscall/net.rs#accept` | 同板块同名 | Err, Ok, OpenFileDescription::socket, SocketEndpoint::unnamed_for_domain, description.socket_id, description.socket_id().ok_or ...(+5) | accepted_socket.remote_addr, accepted_socket.remote_addr().unwrap, socket.accept, thread
        .files
        .get, thread
        .files
        .get(sockfd)
        .ok_or, thread
        .files
        .get(sockfd)
        .ok_or(EBADF)?
        .get_socket ...(+3) |
| `source/core/fs/fd/mqueue.rs#send` | `src/driver/e1000e.rs#send` | 同板块同名 | Err, Ok, description
            .message_queue_id, description
            .message_queue_id()
            .ok_or, description.writable, message_handle ...(+9) | Err, Ok, PAddr::from, PAddr::from(buffer_page.pfn()).addr, Page::alloc, Some ...(+11) |
| `source/core/fs/fd.rs#allocate` | `src/kernel/pcie/device.rs#allocate` | 同板块同名 | Err, Ok, Some, self.descriptions.len, self.descriptions[index].is_none | NonZero::new, eonix_log::println_trace!, self.start.align_up, size.next_power_of_two, size.next_power_of_two().try_into, size.next_power_of_two().try_into().ok |
| `source/core/mm/frame.rs#allocate` | `src/kernel/task/thread.rs#allocate` | 同板块同名 | Err, FrameAllocError::NotReady, Ok, PhysFrame::new | self.0.allocate |

#### 中断/异常处理
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `source/arch/loongarch64/trap_block.rs#wait_for_interrupt` | `src/driver/serial.rs#wait_for_interrupt` | 同板块同名 | core::arch::asm!, core::hint::spin_loop | pin!, self.disable_interrupts, self.enable_interrupts, self.working.lock_irq, wait.as_mut, wait.as_mut().add_to_wait_list |
| `source/arch/riscv64/trap/block_dispatch.rs#consume_blocked_syscall` | `src/kernel/terminal.rs#commit_char` | 同模块上下文对齐/差异观察 | block.pid, block.previous_single_signal_mask, block.reason, consume_epoll_wait_block, consume_fd_wait_block, consume_futex_wait_block ...(+10) | inner.termio.echok, inner.termio.echoke, inner.termio.icanon, inner.termio.icrnl, inner.termio.iexten, inner.termio.igncr ...(+16) |
| `source/arch/loongarch64/trap_block.rs#consume_blocked_syscall` | `src/kernel/terminal.rs#read` | 同模块上下文对齐/差异观察 | block.pid, block.previous_single_signal_mask, block.reason, consume_epoll_wait_block, consume_fd_wait_block, consume_futex_wait_block ...(+10) | Err, Thread::current, Thread::current().signal_list.has_pending_signal, buffer.available, buffer.available().min, buffer.fill ...(+17) |
| `source/arch/riscv64/trap/fork.rs#map_user_map_error` | `src/kernel/timer.rs#timer_interrupt` | 同模块上下文对齐/差异观察 | SyscallError::Invalid.errno, SyscallError::NoDevice.errno, SyscallError::NoMemory.errno, SyscallError::NotSupported.errno | CPU::local, CPU::local().cpuid, SLEEPERS_LIST.lock_irq, TICKS.fetch_add, WAKEUP_TICK.load, WAKEUP_TICK.store ...(+6) |

#### 系统调用
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `source/core/syscall/keyring.rs#signed_key_id` | `src/kernel/syscall.rs#from_arg` | 同板块同结构 | 内部闭环逻辑 | 内部闭环逻辑 |
| `source/core/syscall/socket.rs#recvmsg_control` | `src/kernel/mem/mm_list.rs#set_break` | 同模块上下文对齐/差异观察 | Ok, write_recvmsg_credentials_control, write_recvmsg_ipv4_int_control, write_recvmsg_ipv4_packet_info_control, write_recvmsg_timestamp_control | Some, VRange::new, assert!, drop, inner.locked.lock, locked.areas.check_overlapping_range ...(+5) |
| `source/core/syscall/system.rs#sys_getrandom` | `src/kernel/syscall/sysinfo.rs#newuname` | 同模块上下文对齐/差异观察 | SyscallError::Fault.errno, SyscallError::Invalid.errno, copy_to_user, copy_to_user(memory, target, &buffer[..chunk_len]).is_err, fill_pseudorandom, frame.arg ...(+2) | UserPointerMut::new, buffer.write, copy_cstr_to_array |
| `source/core/syscall/sysv_msg.rs#write_msgid_ds` | `src/kernel/syscall/net.rs#get_sockopt` | 同模块上下文对齐/差异观察 | SyscallError::Fault.errno, copy_to_user, copy_to_user(memory, address, &raw).is_err, put_u32, put_u64, queue.cbytes.load ...(+10) | Ok, UserPointerMut::new, UserPointerMut::new(optlen)?.write, UserPointerMut::new(optval.cast())?.write, optval.cast, size_of::<u32> |

#### 文件系统
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `source/core/fs/vfs/overlay.rs#rename` | `src/fs/tmpfs.rs#rename` | 同板块同名 | Err, Ok, VfsPathBuffer::root, new.bytes, old.bytes, path_is_child ...(+15) | (new_parent_inode.as_ref() as &dyn Any)
                .downcast_ref::<DirectoryInode>, (new_parent_inode.as_ref() as &dyn Any)
                .downcast_ref::<DirectoryInode>()
                .expect, Arc::as_ptr, Err, Instant::now, Ok ...(+53) |
| `source/core/fs/vfs/overlay.rs#reset` | `src/driver/e1000e.rs#reset` | 同板块同名 | VfsFileTimes::zero, VfsPathBuffer::root, self.files.len, self.files[index].clear_metadata, self.reset_xattrs | Ok, self.regs.read, self.regs.write |
| `source/core/fs/vfs/overlay.rs#symlink` | `src/fs/tmpfs.rs#symlink` | 同板块同名 | Err, Ok, link_path.bytes, self.clear_file_slot, self.clear_whiteout_slot, self.directory_index ...(+13) | SymlinkInode::new, acquire, astmp, at.get_name, at.save_symlink, block_on ...(+7) |
| `source/kernel/syscall_runtime/vfs_backend.rs#pipe` | `src/kernel/syscall/file_rw.rs#pipe` | 同板块同名 | Err, Ok, VfsRuntimeError::Fd, active_fd_table_mut, active_fd_table_mut().close, active_fd_table_mut().insert ...(+5) | OpenFlags::empty, sys_pipe2 |

#### 设备驱动
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `source/arch/loongarch64/block.rs#read_sector_inner` | `src/driver/virtio/riscv64.rs#init` | 同模块上下文对齐/差异观察 | Err, Ok, avail_idx.wrapping_add, core::hint::spin_loop, core::mem::size_of::<VirtioBlkReq>, core::ptr::addr_of! ...(+9) | Arc::new, ArchPhysAccess::as_ptr, BlockDevice::register_disk, BlockDevice::register_disk(
                        make_device(8, 16 * disk_id),
                        2147483647,
                        Arc::new(Spin::new(block_device)),
                    )
                    .expect, MmioTransport::new, Spin::new ...(+14) |
| `source/arch/riscv64/block.rs#read_sector_inner` | `src/driver/virtio/loongarch64.rs#handle_device` | 同模块上下文对齐/差异观察 | Err, Ok, avail_idx.wrapping_add, avail_ptr, compiler_fence, core::hint::spin_loop ...(+12) | Arc::new, BlockDevice::register_disk, Err, Ok, PciRoot::new, PciTransport::new::<HAL, _> ...(+13) |
| `source/arch/riscv64/block.rs#ensure_ready_modern` | `src/driver/serial.rs#init` | 同模块上下文对齐/差异观察 | Err, Ok, VirtqUsedElem::empty, compiler_fence, core::ptr::addr_of!, mmio_read ...(+1) | Ok, PAddr::from, Serial::new, SerialIO::new, port.register_as_char_device, serial.register_as_char_device |
| `source/arch/loongarch64/block.rs#find_virtio_block_device` | `src/driver/serial.rs#new` | 同模块上下文对齐/差异观察 | Some, config_read_u16, config_read_u8, is_virtio_block_device, pci_config_address | Arc::from, Err, Ok, Spin::new, VecDeque::new, WaitList::new ...(+12) |

#### 网络协议栈
仅参考项目中观察到该模块代表函数，目标项目没有同模块可对齐证据：
- 参考项目：`src/net/socket/tcp.rs#poll_impl`，调用摘要：Ok, PollEvent::empty, Some, block_on, events.contains, iface.lock ...(+12)
- 参考项目：`src/net/socket/udp.rs#bind`，调用摘要：BoundSocket::new_bind_all, BoundSocket::new_bind_single, Err, Ok, Self::bind_impl, Some ...(+23)
- 参考项目：`src/net/socket/tcp.rs#accept_impl`，调用摘要：Arc::new, BoundSocket::new_single, Err, Ok, RwLock::new, SocketAddr::from ...(+15)
- 参考项目：`src/net/socket/udp.rs#drop`，调用摘要：UDP_PORT_MAP.lock, UDP_PORT_MAP.lock().remove, block_on, bound_socket_guard.as_ref, bound_socket_guard.as_ref().unwrap, bound_socket_guard.is_none ...(+16)

#### 文件系统/第三方库
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `source/core/fs/ext4.rs#read_block` | `src/fs/ext4.rs#read_block` | 同板块同名 | Err, Ok, block
            .checked_mul, block
            .checked_mul(self.block_size as u64)
            .ok_or, read_bytes, zero_block | ByteBuffer::new, buffer.as_mut_slice, self
            .device
            .read_some, self
            .device
            .read_some((block_id as usize) * 4096, &mut byte_buffer)
            .expect |
| `source/core/fs/ext4.rs#read_dir_entry_at` | `src/fs/ext4.rs#lookup` | 同模块上下文对齐/差异观察 | Err, Ok, Some, VfsDirEntry::new, VfsDirEntry::new(inode_number, next_offset, name, kind)
                    .map_err, cursor
                .checked_add ...(+11) | AtomicMode::new, AtomicNlink::new, AtomicU32::new, AtomicU64::new, Err, InodeMode::BLOCKDEV.bits ...(+30) |
| `source/core/fs/ext4.rs#from_superblock` | `src/fs/ext4.rs#rename` | 同模块上下文对齐/差异观察 | (EXT4_MIN_BLOCK_SIZE..=EXT4_MAX_BLOCK_SIZE).contains, EXT4_MIN_BLOCK_SIZE
            .checked_shl, EXT4_MIN_BLOCK_SIZE
            .checked_shl(log_block_size)
            .ok_or, Err, Ok, read_u16 ...(+1) | Arc::as_ptr, Err, Instant::now, Ok, block_on, core::str::from_utf8 ...(+27) |
| `source/core/fs/ext4.rs#lookup_path_followed_at` | `src/fs/ext4.rs#create` | 同模块上下文对齐/差异观察 | Err, Ext4File::from_inode, Ok, VfsPathBuffer::root, append_child_path, child.is_symlink ...(+10) | Arc::downgrade, Arc::new, AtomicMode::new, AtomicNlink::new, AtomicU32::new, AtomicU64::new ...(+25) |

#### 第三方库
仅参考项目中观察到该模块代表函数，目标项目没有同模块可对齐证据：
- 参考项目：`crates/eonix_hal/src/arch/riscv64/trap/default.rs#_default_trap_entry`，调用摘要：naked_asm!
- 参考项目：`crates/eonix_hal/src/arch/loongarch64/trap/mod.rs#_raw_trap_entry`，调用摘要：naked_asm!
- 参考项目：`crates/eonix_percpu/eonix_percpu_macros/src/lib.rs#define_percpu_impl`，调用摘要：["u8", "u16", "u32", "u64", "usize"].contains, attrs.is_empty, format_ident!, get_percpu_pointer, panic!, parse2::<ItemStatic> ...(+6)
- 参考项目：`crates/eonix_hal/src/arch/riscv64/trap/captured.rs#_captured_trap_entry`，调用摘要：naked_asm!

#### 通用模块/其他
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `source/core/loader/elf.rs#get` | `src/kernel/pcie/header.rs#get` | 同板块同名 | 内部闭环逻辑 | NonZero::new, NonZero::new(size).map, bar.unwrap_or, memory_barrier, reg2.load, reg2.store ...(+5) |
| `source/core/loader/elf/segments.rs#empty` | `src/rcu.rs#empty` | 同板块同名 | 内部闭环逻辑 | AtomicPtr::new, Self, core::ptr::null_mut |
| `source/arch/contract/boot.rs#bytes` | `src/io.rs#data` | 同板块同结构 | 内部闭环逻辑 | 内部闭环逻辑 |
| `source/arch/riscv64/boot.rs#init_path_from_bootargs` | `src/kernel/user/dataflow.rs#new` | 同模块上下文对齐/差异观察 | BootInitPath::new, Err, Ok, Some, bootargs.len, bootargs[start..cursor].starts_with ...(+2) | CheckedUserPointer::new, Err, Ok, asm!, assert_preempt_enabled! |


好的，本庭已收到控辩双方的全部陈述、客观 AST 证据底座及审计指令。现依据客观数据、底层 AST 指纹及操作系统内核架构原理，作出最终定性仲裁。

---

### 1. 宏观判定

- **定性结论**: **独立开发**
- **核心依据**:
  - **客观 AST 相似度**: 0.21%
  - **共同 AST 指纹**: 11
  - **唯一 AST 指纹**: 目标项目 3475，参考项目 1840
  - **双向覆盖率**: 目标项目 0.32%，参考项目 0.6%
- **仲裁员注释**: 0.21% 的 AST 相似度与不足 1% 的双向覆盖率，是两个独立开发的大型软件项目在实现同类功能（POSIX/Linux 兼容操作系统内核）时的典型特征。控方《复用指控清单》所依赖的“语义克隆”、“逻辑复用”等论点，在客观 AST 指纹证据面前不成立。辩方关于“领域强制共性”和“通用设计模式”的辩护，与底层 AST 证据高度吻合。

---

### 2. 全模块覆盖矩阵解读

| 模块 | 双方均存在 | 共同 AST 指纹 | Target 覆盖 | Base 覆盖 | 证据强度 | 仲裁解读 |
| :--- | :---: | :---: | :---: | :---: | :---: | :--- |
| **进程管理** | 是 | **0** | 0.0% | 0.0% | **未形成** | 控方五项指控全部集中于此板块，但共同 AST 指纹为 0。双方在进程创建、调度、等待、销毁等核心路径上使用了完全不同的内部对象和同步原语，无任何结构性复用证据。 |
| **任务调度/异步运行时** | 是 | 0 | 0.0% | 0.0% | **未形成** | 双方均实现了任务调度，但目标项目采用显式同步上下文切换，参考项目采用基于 `Waker` 的异步运行时，这是两种截然不同的调度模型，AST 层面无重合。 |
| **内存管理** | 是 | 0 | 0.0% | 0.0% | **未形成** | 双方均实现了内存管理，但物理帧分配、虚拟内存映射等核心路径的外部调用对象完全不同，无共同指纹。 |
| **中断/异常处理** | 是 | 0 | 0.0% | 0.0% | **未形成** | 双方均存在 Trap 处理逻辑，但目标项目围绕 `SyscallBlock` 状态机，参考项目围绕终端/定时器中断，架构差异显著。 |
| **系统调用** | 是 | 1 | 0.28% | 2.22% | **弱** | 唯一的共同指纹来自 `signed_key_id` / `from_arg`，均为内部闭环逻辑，属于极简函数，无实质性复用信息量。 |
| **文件系统** | 是 | 1 | 0.08% | 0.34% | **弱** | 共同指纹未出现在控方指控的 procfs 或 exec 路径中。双方在 VFS 架构、inode 管理、文件操作实现上使用了不同的对象模型。 |
| **设备驱动** | 是 | 0 | 0.0% | 0.0% | **未形成** | 双方均实现了 virtio 块设备驱动，但初始化流程、寄存器访问方式、错误处理路径的 AST 对象完全不同。 |
| **网络协议栈** | 仅参考项目 | 0 | 0.0% | 0.0% | **未形成** | 目标项目无此模块，无法比对。 |
| **文件系统/第三方库** | 是 | 0 | 0.0% | 0.0% | **未形成** | 双方均包含 ext4 文件系统实现，但 `read_block` 等核心函数的内部实现路径不同，无共同指纹。 |
| **第三方库** | 仅参考项目 | 0 | 0.0% | 0.0% | **未形成** | 参考项目包含 `eonix_hal`、`eonix_percpu` 等自研库，目标项目无对应模块。 |
| **通用模块/其他** | 是 | 1 | 0.92% | 0.64% | **弱** | 共同指纹来自 `get`、`empty`、`bytes` 等极简函数，属于 Rust 语言通用模式，不构成复用证据。 |

---

### 3. 底层 AST 关键证据指纹表

| 所属板块 | 目标项目路径 | 参考项目路径 | 证据类型 | 技术说明 |
| :--- | :--- | :--- | :--- | :--- |
| 系统调用 | `source/core/syscall/keyring.rs#signed_key_id` | `src/kernel/syscall.rs#from_arg` | 同板块同结构 | 双方均为内部闭环逻辑，极简函数，无外部调用图谱，属于 Rust 通用构造。 |
| 文件系统 | `source/core/fs/ext4.rs#read_block` | `src/fs/ext4.rs#read_block` | 同板块同名 | 同名但实现不同：目标项目使用 `checked_mul` + `read_bytes`；参考项目使用 `ByteBuffer::new` + `device.read_some`。 |
| 通用模块/其他 | `source/core/loader/elf.rs#get` | `src/kernel/pcie/header.rs#get` | 同板块同名 | 跨子系统同名函数：目标项目为 ELF 加载器，参考项目为 PCIe 配置空间访问，功能无关。 |
| 通用模块/其他 | `source/core/loader/elf/segments.rs#empty` | `src/rcu.rs#empty` | 同板块同名 | 跨子系统同名函数：目标项目为 ELF 段描述，参考项目为 RCU 链表，功能无关。 |
| 通用模块/其他 | `source/arch/contract/boot.rs#bytes` | `src/io.rs#data` | 同板块同结构 | 均为内部闭环逻辑，极简 getter，无复用信息量。 |

**仲裁员注释**: 全项目仅有的 11 个共同 AST 指纹均属于上述极简函数或跨子系统同名函数，无法构成任何实质性复用证据链。控方指控的进程管理板块共同指纹为 0。

---

### 4. 核心代码调用流对比剖析

#### **【进程管理 / 进程等待与阻塞唤醒路径】**

- **底层技术析理**:
  目标项目与参考项目在进程等待机制上存在**根本性架构差异**。目标项目采用**原子位图槽位 + 显式调度器上下文切换**的同步模型，核心调用流为 `insert_wait_index`（通过 `CHILD_EXIT_WAIT_SLOTS[...].fetch_or` 注册等待位）→ `apply_wait_child_context_switch`（调用 `prepare_blocked_context_switch` 显式挂起当前任务并切换到下一个任务）→ `wake_wait_channel`（通过 `wake_child_wait_bucket` 等函数清除等待位并唤醒）。参考项目采用**条件变量 + RCU 进程列表**的同步模型，核心调用流为 `ProcessList::get().write`（获取全局进程列表写锁）→ `self.cv.wait`（在条件变量上阻塞，内部使用 `guard.unlock` 和 `pin!`）→ `wait_procs.relock`（重新获取锁）。两者在**同步原语类型**（原子位图 vs. 条件变量）、**阻塞机制**（显式任务切换 vs. 条件变量等待）、**进程状态管理**（静态槽位数组 vs. 动态进程列表）三个维度上均无重合。

- **代码溯源链接**:
  - 目标项目: `source/core/scheduler/blocking.rs#insert_wait_index`, `source/arch/riscv64/trap/block_switch.rs#apply_wait_child_context_switch`, `source/core/scheduler/blocking.rs#wake_wait_channel`
  - 参考项目: `src/kernel/task/process.rs#wait`, `src/sync/condvar.rs#wait`

---

#### **【内存管理 / 物理帧与对象分配路径】**

- **底层技术析理**:
  双方在内存分配路径上使用了完全不同的对象模型。目标项目的 `Frame::allocate` 直接构造 `PhysFrame::new` 并返回 `FrameAllocError::NotReady` 错误，表明其使用简单的物理帧分配器。参考项目的 `Thread::allocate` 通过 `self.0.allocate` 委托给内部分配器，属于线程级资源分配。目标项目的 `FdTable::allocate` 通过 `self.descriptions.len` 和 `self.descriptions[index].is_none` 进行线性槽位查找；参考项目的 `PcieDevice::allocate` 使用 `size.next_power_of_two` 和 `self.start.align_up` 进行对齐分配。两者在**分配器类型**、**错误处理策略**、**分配算法**上均无共同指纹。

- **代码溯源链接**:
  - 目标项目: `source/core/mm/frame.rs#allocate`, `source/core/fs/fd.rs#allocate`
  - 参考项目: `src/kernel/task/thread.rs#allocate`, `src/kernel/pcie/device.rs#allocate`

---

#### **【任务调度 / 上下文切换与执行模型】**

- **底层技术析理**:
  目标项目的上下文切换路径位于 `arch` 层，核心调用为 `prepare_blocked_context_switch` → `enter_next_task_runtime` → `map_context_switch_blocker`，使用 `current_monotonic_micros` 作为时间基准，属于**同步阻塞式调度**。参考项目的任务执行路径位于 `kernel/task.rs`，核心调用为 `Task::current().is_ready` → `Context::from_waker` → `Waker::from`，属于**异步 Waker 驱动调度**。两者在**调度模型**（同步 vs. 异步）、**上下文切换触发机制**（显式调用 vs. Future 轮询）、**时间管理**（单调微秒 vs. 无显式时间基准）上完全不同。

- **代码溯源链接**:
  - 目标项目: `source/arch/riscv64/trap/block_switch.rs#apply_wait_channel_context_switch`, `source/arch/riscv64/trap/fork.rs#apply_vfork_child_context_switch`
  - 参考项目: `src/kernel/task.rs#execute`

---

### 5. 单方模块与降权项

| 类别 | 项目 | 涉及模块/函数 | 降权/排除理由 |
| :--- | :--- | :--- | :--- |
| **单方模块** | 参考项目 | 网络协议栈 (`src/net/socket/tcp.rs`, `src/net/socket/udp.rs`) | 目标项目无此模块，无法比对，不构成任何证据。 |
| **单方模块** | 参考项目 | 第三方库 (`crates/eonix_hal`, `crates/eonix_percpu`) | 目标项目无对应自研库，属于参考项目独有的基础设施。 |
| **跨板块同名** | 双方 | `allocate`（目标: `fs/fd.rs` vs 参考: `pcie/device.rs`） | 同名但分属文件描述符分配与 PCIe 设备内存分配，功能无关。 |
| **跨板块同名** | 双方 | `send`（目标: `fs/fd/mqueue.rs` vs 参考: `driver/e1000e.rs`） | 同名但分属消息队列发送与网卡数据发送，功能无关。 |
| **跨板块同名** | 双方 | `accept`（目标: `fs/fd/socket.rs` vs 参考: `syscall/net.rs`） | 同名但分属 socket 文件描述符层与网络系统调用层，功能无关。 |
| **跨板块同名** | 双方 | `current`（目标: `arch/contract/context.rs` vs 参考: `task/thread.rs`） | 同名但分属架构抽象层与线程模块，功能无关。 |
| **跨板块同名** | 双方 | `load`（目标: `task/credentials.rs`, `task/single/slots.rs`, `task/single/memory_runtime.rs` vs 参考: `task/loader/elf.rs`, `task/loader/mod.rs`） | 同名但目标项目为凭证/槽位/内存加载，参考项目为 ELF 加载，功能无关。 |
| **POSIX 标准接口** | 双方 | `exit`, `vfork`, `pid`, `set_robust_list`, `clear_child_tid`, `set_child_tid`, `set_tid_address`, `exit_signal` | 均为 Linux/POSIX 兼容内核必须实现的 ABI 接口，同名是接口强制要求，且 AST 摘要显示为内部闭环逻辑或极简返回，无实质性复用信息量。 |
| **领域强制共性** | 双方 | `/proc/cpuinfo`, `/proc/meminfo`, `/proc/mounts` 等文件名 | 这些是 Linux `/proc` 文件系统的标准规定名称，任何兼容 Linux 用户态的内核都必须使用这些名称。 |
| **Rust 语言通用构造** | 双方 | `Err`, `Ok`, `Some`, `None`, `.map_err`, `.is_none` | 属于 Rust 标准库的基础类型和方法，任何 Rust 项目都会使用，不能作为复用证据。 |

---

### 6. 保守结论

本庭基于客观 AST 指纹证据作出最终裁定：目标项目 `[2026-T2026106149910833-XX]` 与参考项目 `[2025-sudo_pacman_Syu-同济大学]` 在底层代码实现上**不存在实质性同源复用**。全项目 0.21% 的 AST 相似度与不足 1% 的双向覆盖率，是两个独立开发的大型 Rust 操作系统内核在实现同类 POSIX/Linux 兼容接口时的正常现象。控方所指控的进程管理板块共同 AST 指纹为 0，双方在同步原语（原子位图 vs. 条件变量）、调度模型（同步上下文切换 vs. 异步 Waker）、内存管理（静态槽位 vs. 动态分配）等核心架构维度上存在根本性差异。所有观察到的同名函数均属于 POSIX 标准接口强制命名、Rust 语言通用构造或跨子系统偶然同名，不构成结构性复用证据。综合裁定：**独立开发，无违规事实**。

------------------------------

