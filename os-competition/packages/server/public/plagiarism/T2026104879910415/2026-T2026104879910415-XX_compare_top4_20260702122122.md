# 🛡️ OS 内核赛自动化代码级架构审计报告

**审计时间**: `2026-07-02 12:21:22`
**参赛作品名称**: `2026-T2026104879910415-XX`
**年份**: `2026`
**学校**: `XX`
**队伍名称**: `T2026104879910415`
**比赛名字**: `OS-Competition`
**比赛子赛道**: `Kernel-Implementation`
**仓库地址**: `https://gitlab.eduxiji.net/educg-group-43501-3132633/T2026104879910415-3331`
**推测开发语言**: `Rust / C (由AST探针自动识别)`
**特征函数量**: `6935`

## ⚔️ 底层代码同源性对比 (AST Homology)
> 本部分由专门的代码审查智能体，对该项目与符合年份条件的历届最高相似度项目进行控制流与底层调用对比。新版报告会先给出确定性的全模块覆盖矩阵，再给出 Agent 的分模块解释，避免只围绕少量抽样函数展开。

### 🏆 Top 4 底层对标: vs `2025-狗剩-海南大学`（海南大学 / 狗剩）

#### 对方作品信息

**对方年份**: `2025`
**对方学校**: `海南大学`
**对方队伍名称**: `狗剩`
**对方比赛名字**: `OS-Competition`
**对方比赛子赛道**: `Kernel-Implementation`
**对方仓库地址**: `https://gitlab.eduxiji.net/educg-group-36002-2710490/T202510589995148-2209`

> 双向覆盖率：共同 AST 指纹: 999；唯一 AST 指纹: 目标项目 5738，参考项目 3020；覆盖率: 目标项目 17.41%，参考项目 33.08%

## 全模块对比总览（确定性证据）

> 总体覆盖率：共同 AST 指纹: 999；唯一 AST 指纹: 目标项目 5738，参考项目 3020；覆盖率: 目标项目 17.41%，参考项目 33.08%

### 1. 模块覆盖矩阵
| 模块 | 目标函数数 | 参考函数数 | 共同唯一 AST 指纹 | Target 覆盖 | Base 覆盖 | 状态 |
| :--- | ---: | ---: | ---: | ---: | ---: | :--- |
| 进程管理 | 650 | 93 | 29 | 5.04% | 34.52% | 双方存在且有结构重合 |
| 任务调度/异步运行时 | 806 | 399 | 129 | 17.27% | 44.79% | 双方存在且有结构重合 |
| 内存管理 | 343 | 326 | 100 | 32.57% | 34.36% | 双方存在且有结构重合 |
| 中断/异常处理 | 35 | 62 | 2 | 6.25% | 4.08% | 双方存在且有结构重合 |
| 系统调用 | 239 | 153 | 56 | 25.57% | 43.41% | 双方存在且有结构重合 |
| 文件系统 | 826 | 747 | 221 | 31.94% | 39.05% | 双方存在且有结构重合 |
| 设备驱动 | 105 | 142 | 81 | 83.51% | 65.85% | 双方存在且有结构重合 |
| 网络协议栈 | 61 | 77 | 51 | 100.0% | 96.23% | 双方存在且有结构重合 |
| 文件系统/第三方库 | 899 | 689 | 2 | 0.24% | 0.34% | 双方存在且有结构重合 |
| 网络协议栈/第三方库 | 2529 | 107 | 64 | 3.31% | 64.0% | 双方存在且有结构重合 |
| 第三方库 | 0 | 386 | 0 | 0.0% | 0.0% | 仅参考项目体现 |
| 通用模块/其他 | 442 | 665 | 269 | 78.43% | 54.79% | 双方存在且有结构重合 |

### 2. 分模块证据清单

#### 进程管理
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `kernel/src/fs/procfs.rs#write_at` | `core/src/vfs/proc.rs#write_at` | 同板块同名 | Ok, PROC_SYS_DOMAINNAME.lock, PROC_SYS_DOMAINNAME.lock().set, PROC_SYS_NET_IPV4_CONF_DEFAULT_TAG.store, PROC_SYS_PIPE_MAX_SIZE.store, PROC_SYS_SCHED_AUTOGROUP_ENABLED.store ...(+19) | Err |
| `arceos/modules/axtask/src/run_queue.rs#exit_current` | `arceos/modules/axtask/src/run_queue.rs#exit_current` | 同板块同名同结构 | EXITED_TASKS.current_ref_mut_raw, EXITED_TASKS.current_ref_mut_raw().clear, EXITED_TASKS.current_ref_mut_raw().push_back, WAIT_FOR_EXIT.current_ref_mut_raw, WAIT_FOR_EXIT.current_ref_mut_raw().notify_one, assert! ...(+8) | EXITED_TASKS.current_ref_mut_raw, EXITED_TASKS.current_ref_mut_raw().clear, EXITED_TASKS.current_ref_mut_raw().push_back, WAIT_FOR_EXIT.current_ref_mut_raw, WAIT_FOR_EXIT.current_ref_mut_raw().notify_one, assert! ...(+8) |
| `arceos/api/arceos_posix_api/src/imp/io_mpx/epoll.rs#sys_epoll_wait` | `arceos/api/arceos_posix_api/src/imp/io_mpx/epoll.rs#sys_epoll_wait` | 同板块同名同结构 | debug!, syscall_body! | debug!, syscall_body! |
| `arceos/modules/axtask/src/wait_queue.rs#cancel_events` | `arceos/modules/axtask/src/wait_queue.rs#cancel_events` | 同板块同名同结构 | curr.in_wait_queue, curr.ptr_eq, curr.set_in_wait_queue, curr.timer_ticket_expired, self.queue.lock, self.queue.lock().retain | curr.in_wait_queue, curr.ptr_eq, curr.set_in_wait_queue, curr.timer_ticket_expired, self.queue.lock, self.queue.lock().retain |

#### 任务调度/异步运行时
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `arceos/ulib/axlibc/c/printf.c#format_string_loop` | `arceos/ulib/axlibc/c/printf.c#format_string_loop` | 同板块同名同结构 | ABS_FOR_PRINTING, ADVANCE_IN_FORMAT_STRING, atou_, is_digit_, out_rev_, parse_flags ...(+5) | ABS_FOR_PRINTING, ADVANCE_IN_FORMAT_STRING, atou_, is_digit_, out_rev_, parse_flags ...(+5) |
| `arceos/modules/axlog/src/lib.rs#log` | `arceos/modules/axlog/src/lib.rs#log` | 同板块同名同结构 | cfg_if::cfg_if!, record.level, record.line, record.line().unwrap_or, record.metadata, record.target ...(+1) | cfg_if::cfg_if!, record.level, record.line, record.line().unwrap_or, record.metadata, record.target ...(+1) |
| `arceos/modules/axtask/src/run_queue.rs#switch_to` | `arceos/modules/axtask/src/run_queue.rs#switch_to` | 同板块同名 | (*prev_ctx_ptr).switch_to, Arc::downgrade, CurrentTask::set_current, PREV_TASK.current_ref_mut_raw, assert!, clear_prev_task_on_cpu ...(+8) | (*prev_ctx_ptr).switch_to, Arc::downgrade, CurrentTask::set_current, PREV_TASK.current_ref_mut_raw, assert!, clear_prev_task_on_cpu ...(+11) |
| `arceos/modules/axtask/src/run_queue.rs#put_task_with_state` | `arceos/modules/axtask/src/run_queue.rs#put_task_with_state` | 同板块同名 | core::hint::spin_loop, self.scheduler.lock, self.scheduler.lock().put_prev_task, task.is_idle, task.on_cpu, task.set_cpu_id ...(+1) | core::hint::spin_loop, self.scheduler.lock, self.scheduler.lock().put_prev_task, task.is_idle, task.on_cpu, task.transition_state |

#### 内存管理
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `arceos/ulib/axlibc/src/strftime.rs#strftime_inner` | `arceos/ulib/axlibc/src/strftime.rs#strftime_inner` | 同板块同名同结构 | CountingWriter::new, format.offset, inner_strftime, w! | CountingWriter::new, format.offset, inner_strftime, w! |
| `arceos/ulib/axlibc/src/strftime.rs#inner_strftime` | `arceos/ulib/axlibc/src/strftime.rs#inner_strftime` | 同板块同名同结构 | format.offset, w! | format.offset, w! |
| `arceos/modules/axruntime/src/lib.rs#rust_main` | `arceos/modules/axruntime/src/lib.rs#rust_main` | 同板块同名 | INITED_CPUS.fetch_add, ax_println!, axdisplay::init_display, axdriver::init_drivers, axfs::init_filesystems, axhal::init_early ...(+24) | INITED_CPUS.fetch_add, all_devices
                .block
                .take_one, all_devices
                .block
                .take_one()
                .expect, ax_println!, axdisplay::init_display, axdriver::init_drivers ...(+28) |
| `arceos/modules/axdriver/src/bus/pci.rs#config_pci_device` | `arceos/modules/axdriver/src/bus/pci.rs#config_pci_device` | 同板块同名 | Ok, allocator
                    .as_mut, allocator
                    .as_mut()
                    .expect, allocator
                    .as_mut()
                    .expect("No memory ranges available for PCI BARs!")
                    .alloc, allocator
                    .as_mut()
                    .expect("No memory ranges available for PCI BARs!")
                    .alloc(size as _)
                    .ok_or, debug! ...(+8) | Ok, allocator
                    .as_mut, allocator
                    .as_mut()
                    .expect, allocator
                    .as_mut()
                    .expect("No memory ranges available for PCI BARs!")
                    .alloc, allocator
                    .as_mut()
                    .expect("No memory ranges available for PCI BARs!")
                    .alloc(size as _)
                    .ok_or, debug! ...(+7) |

#### 中断/异常处理
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `arceos/ulib/axlibc/c/pow.c#exp_inline` | `arceos/ulib/axlibc/c/pow.c#exp_inline` | 同板块同名同结构 | __math_oflow, __math_uflow, asdouble, asuint64, converttoint, eval_as_double ...(+4) | __math_oflow, __math_uflow, asdouble, asuint64, converttoint, eval_as_double ...(+4) |
| `arceos/modules/axsync/src/mutex.rs#may_interrupt` | `arceos/modules/axsync/src/mutex.rs#may_interrupt` | 同板块同名同结构 | rand::random::<u32>, thread::yield_now | rand::random::<u32>, thread::yield_now |
| `kernel/src/init.rs#render_ltp_runtest_driver` | `arceos/modules/axhal/src/arch/aarch64/trap.rs#handle_sync_exception` | 同模块上下文对齐/差异观察 | String::new, cases.is_empty, format!, runtime_dir.trim_start_matches, writeln! | ESR_EL1.extract, crate::trap::handle_syscall, crate::trap::post_trap_callback, debug!, esr.read, esr.read_as_enum ...(+6) |
| `kernel/src/syscall/poll.rs#syscall_epoll_pwait2` | `arceos/modules/axhal/src/arch/x86_64/idt.rs#new` | 同模块上下文对齐/差异观察 | Err, decode_epoll_maxevents, do_epoll_wait, install_temporary_signal_mask, read_optional_signal_mask, read_optional_timespec_deadline | InterruptDescriptorTable::new, core::mem::transmute, core::slice::from_raw_parts_mut, entries[i].set_handler_fn, opt.set_privilege_level |

#### 系统调用
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `arceos/api/arceos_posix_api/src/imp/net.rs#sys_getaddrinfo` | `arceos/api/arceos_posix_api/src/imp/net.rs#sys_getaddrinfo` | 同板块同名同结构 | char_ptr_to_str, debug!, syscall_body! | char_ptr_to_str, debug!, syscall_body! |
| `arceos/api/arceos_posix_api/src/imp/net.rs#sys_recvfrom` | `arceos/api/arceos_posix_api/src/imp/net.rs#sys_recvfrom` | 同板块同名同结构 | debug!, syscall_body! | debug!, syscall_body! |
| `arceos/api/arceos_posix_api/src/imp/net.rs#sys_socket` | `arceos/api/arceos_posix_api/src/imp/net.rs#sys_socket` | 同板块同名同结构 | debug!, syscall_body! | debug!, syscall_body! |
| `arceos/api/arceos_posix_api/src/imp/io.rs#sys_writev` | `api/src/imp/io.rs#sys_writev` | 同板块同名 | debug!, syscall_body! | (0..=1024).contains, Err, Ok, core::slice::from_raw_parts, debug!, iov.get_as_bytes ...(+2) |

#### 文件系统
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `arceos/ulib/axlibc/src/setjmp.rs#longjmp` | `arceos/ulib/axlibc/src/setjmp.rs#longjmp` | 同板块同名 | compile_error!, core::arch::naked_asm! | core::arch::naked_asm! |
| `arceos/ulib/axlibc/c/glob.c#do_glob` | `arceos/ulib/axlibc/c/glob.c#do_glob` | 同板块同名同结构 | S_ISDIR, append, closedir, do_glob, errfunc, fnmatch ...(+7) | S_ISDIR, append, closedir, do_glob, errfunc, fnmatch ...(+7) |
| `arceos/ulib/axlibc/src/setjmp.rs#setjmp` | `arceos/ulib/axlibc/src/setjmp.rs#setjmp` | 同板块同名 | compile_error!, core::arch::naked_asm! | core::arch::naked_asm! |
| `kernel/src/fs/rootfs.rs#create_file` | `arceos/modules/axfs/src/root.rs#create_file` | 同板块同名 | Err, Ok, attr.is_dir, ensure_runtime_dir_chain, ensure_runtime_dir_chain(self, &parent).map_err, existing.get_attr ...(+24) | ax_err!, parent.create, parent.lookup, parent_node_of, path.ends_with, path.is_empty |

#### 设备驱动
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `arceos/tools/bwbench_client/src/main.rs#transmit_benchmark` | `arceos/tools/bwbench_client/src/main.rs#transmit_benchmark` | 同板块同名同结构 | Local::now, NetDevice::new, NetDevice::new(interface).unwrap, current_time.signed_duration_since, current_time.signed_duration_since(past_time).num_seconds, dev.send ...(+2) | Local::now, NetDevice::new, NetDevice::new(interface).unwrap, current_time.signed_duration_since, current_time.signed_duration_since(past_time).num_seconds, dev.send ...(+2) |
| `arceos/tools/bwbench_client/src/device.rs#bind_interface` | `arceos/tools/bwbench_client/src/device.rs#bind_interface` | 同板块同名同结构 | ETH_P_ALL.to_be, Err, Ok, libc::bind, self.ifreq.ioctl, std::io::Error::last_os_error ...(+2) | ETH_P_ALL.to_be, Err, Ok, libc::bind, self.ifreq.ioctl, std::io::Error::last_os_error ...(+2) |
| `arceos/modules/axdriver/src/lib.rs#init_drivers` | `arceos/modules/axdriver/src/lib.rs#init_drivers` | 同板块同名同结构 | AllDevices::default, all_devs.block.iter, all_devs.block.iter().enumerate, all_devs.display.iter, all_devs.display.iter().enumerate, all_devs.net.iter ...(+5) | AllDevices::default, all_devs.block.iter, all_devs.block.iter().enumerate, all_devs.display.iter, all_devs.display.iter().enumerate, all_devs.net.iter ...(+5) |
| `arceos/tools/bwbench_client/src/main.rs#receive_benchmark` | `arceos/tools/bwbench_client/src/main.rs#receive_benchmark` | 同板块同名同结构 | Local::now, NetDevice::new, NetDevice::new(interface).unwrap, current_time.signed_duration_since, current_time.signed_duration_since(past_time).num_seconds, dev.recv ...(+1) | Local::now, NetDevice::new, NetDevice::new(interface).unwrap, current_time.signed_duration_since, current_time.signed_duration_since(past_time).num_seconds, dev.recv ...(+1) |

#### 网络协议栈
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `arceos/ulib/axlibc/c/network.c#inet_pton` | `arceos/ulib/axlibc/c/network.c#inet_pton` | 同板块同名同结构 | hexval, inet_pton, isdigit, memmove | hexval, inet_pton, isdigit, memmove |
| `arceos/api/arceos_posix_api/src/imp/net.rs#shutdown` | `api/src/fd/net.rs#shutdown` | 同板块同名同结构 | Ok, tcpsocket.lock, tcpsocket.peer_addr, tcpsocket.shutdown, udpsocket.lock, udpsocket.peer_addr ...(+1) | Ok, tcpsocket.lock, tcpsocket.peer_addr, tcpsocket.shutdown, udpsocket.lock, udpsocket.peer_addr ...(+1) |
| `arceos/ulib/axlibc/c/socket.c#accept4` | `arceos/ulib/axlibc/c/socket.c#accept4` | 同板块同名同结构 | accept, fcntl | accept, fcntl |
| `arceos/api/arceos_posix_api/src/imp/net.rs#recvfrom` | `api/src/fd/net.rs#recvfrom` | 同板块同名同结构 | Ok, Some, tcpsocket.lock, tcpsocket.lock().recv, tcpsocket.lock().recv(buf).map, udpsocket
                .lock ...(+2) | Ok, Some, tcpsocket.lock, tcpsocket.lock().recv, tcpsocket.lock().recv(buf).map, udpsocket
                .lock ...(+2) |

#### 文件系统/第三方库
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `third_party/rust/git/ext4-view-rs/src/file.rs#read_bytes` | `crates/lwext4_rust/src/inode/file.rs#read_bytes` | 同板块同名 | Ok, Some, block_size
            .to_u32, block_size
            .to_u32()
            .checked_sub, block_size
            .to_u32()
            .checked_sub(offset_within_block)
            .unwrap, block_size.to_nz_u64 ...(+19) | buf.as_mut_ptr, buf.len, ext4_block_readbytes, ext4_block_readbytes(bdev, offset, buf.as_mut_ptr() as _, buf.len() as _)
                .context |
| `third_party/rust/git/rust-fatfs/src/file.rs#seek` | `arceos/modules/axfs/src/fs/lwext4_rust.rs#seek` | 同板块同名 | Err, Ok, Some, debug_assert!, error!, i64::from ...(+16) | Ok, Some, dev.position, dev.position().checked_add_signed, dev.position().checked_add_signed(off).map, dev.set_position ...(+7) |
| `third_party/rust/git/ext4-view-rs/xtask/src/main.rs#create` | `arceos/modules/axfs-ng/src/fs/ext4/inode.rs#create` | 同板块同名 | Command::new, cmd
            // Set the ownership of the root directory in the filesystem
            // to the current uid/gid instead of root. That allows the
            // mounted filesystem to be edited without root permissions,
            // although the mount operation itself still requires root.
            .args, cmd
            // Set the ownership of the root directory in the filesystem
            // to the current uid/gid instead of root. That allows the
            // mounted filesystem to be edited without root permissions,
            // although the mount operation itself still requires root.
            .args(["-E", &format!("root_owner={uid}:{gid}")])
            // Set the volume label. This string is 16 bytes, which is
            // the maximum length.
            .args, cmd
            // Set the ownership of the root directory in the filesystem
            // to the current uid/gid instead of root. That allows the
            // mounted filesystem to be edited without root permissions,
            // although the mount operation itself still requires root.
            .args(["-E", &format!("root_owner={uid}:{gid}")])
            // Set the volume label. This string is 16 bytes, which is
            // the maximum length.
            .args(["-L", "ext4-view testfs"])
            .arg, cmd
            // Set the ownership of the root directory in the filesystem
            // to the current uid/gid instead of root. That allows the
            // mounted filesystem to be edited without root permissions,
            // although the mount operation itself still requires root.
            .args(["-E", &format!("root_owner={uid}:{gid}")])
            // Set the volume label. This string is 16 bytes, which is
            // the maximum length.
            .args(["-L", "ext4-view testfs"])
            .arg(&self.path)
            .arg, cmd.arg ...(+7) | DirEntry::new_dir, DirEntry::new_file, DirNode::new, Err, FileNode::new, Inode::new ...(+11) |
| `third_party/rust/git/rust-fatfs/src/dir.rs#remove` | `arceos/modules/axfs/src/fs/lwext4_rust.rs#remove` | 同板块同名 | DirEntryData::deserialize, Err, Ok, SeekFrom::Current, SeekFrom::Start, Some ...(+15) | assert!, e.try_into, e.try_into().unwrap, file.check_inode_exist, file.dir_rm, file.dir_rm(fpath)
                .map ...(+8) |

#### 网络协议栈/第三方库
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `arceos/modules/axnet/src/smoltcp_impl/tcp.rs#connect` | `arceos/modules/axnet/src/smoltcp_impl/tcp.rs#connect` | 同板块同名 | ETH0.iface.lock, Err, LO0.iface.lock, Ok, SOCKET_SET.add, SOCKET_SET.poll_interfaces ...(+29) | Err, Ok, SOCKET_SET
                .with_socket_mut::<tcp::Socket, _, _>, SOCKET_SET.add, SocketSetWrapper::new_tcp_socket, Some ...(+29) |
| `third_party/rust/git/smoltcp/src/storage/assembler.rs#add` | `arceos/modules/axnet/src/smoltcp_impl/mod.rs#add` | 同板块同名 | Contig::hole_and_data, Err, Ok, contig.has_data, contig.shrink_hole_to, contig.total_size ...(+11) | debug!, self.0.lock, self.0.lock().add |
| `arceos/modules/axnet/src/smoltcp_impl/dns.rs#query` | `arceos/modules/axnet/src/smoltcp_impl/dns.rs#query` | 同板块同名 | Err, IpAddr::from, Ok, SOCKET_SET
            .with_socket_mut::<dns::Socket, _, _>, SOCKET_SET
            .with_socket_mut::<dns::Socket, _, _>(handle, |socket| {
                socket.start_query(iface.lock().context(), name, query_type)
            })
            .map_err, SOCKET_SET.poll_interfaces ...(+12) | Err, Ok, SOCKET_SET
            .with_socket_mut::<dns::Socket, _, _>, SOCKET_SET
            .with_socket_mut::<dns::Socket, _, _>(handle, |socket| {
                socket.start_query(iface.lock().context(), name, query_type)
            })
            .map_err, SOCKET_SET.poll_interfaces, SOCKET_SET.with_socket_mut::<dns::Socket, _, _> ...(+12) |
| `third_party/rust/git/smoltcp/src/socket/tcp.rs#connect` | `arceos/modules/axnet/src/smoltcp_impl/udp.rs#connect` | 同板块同名 | Err, Ok, Self::random_seq_no, Some, addr.is_unspecified, cx
                    .get_source_address ...(+9) | Ok, Some, debug!, from_core_sockaddr, into_core_sockaddr, self.bind ...(+3) |

#### 第三方库
仅参考项目中观察到该模块代表函数，目标项目没有同模块可对齐证据：
- 参考项目：`crates/visionfive2-sd/src/lib.rs#send_cmd`，调用摘要：RawInterrupt::from, RawInterruptStatusReg::from, S::sleep_ms_until, Some, arg.into, assert! ...(+24)
- 参考项目：`crates/visionfive2-sd/src/lib.rs#init_sdcard`，调用摘要：CardTypeReg::from, CardTypeReg::from(0).with_card_width4_1, CmdArg::new, CmdReg::from, ControlReg::from, RawInterruptStatusReg::from ...(+20)
- 参考项目：`crates/page_table_multiarch/page_table_multiarch/src/bits64.rs#map_region`，调用摘要：Err, M::flush_tlb, Ok, PageSize::Size1G.is_aligned, PageSize::Size2M.is_aligned, PageSize::Size4K.is_aligned ...(+12)
- 参考项目：`crates/axdriver_crates/axdriver_virtio/src/net.rs#try_new`，调用摘要：Err, InnerDev::new, InnerDev::new(transport).map_err, NetBufPool::new, Ok, Some ...(+14)

#### 通用模块/其他
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `arceos/ulib/axlibc/c/printf.c#print_exponential_number` | `arceos/ulib/axlibc/c/printf.c#print_exponential_number` | 同板块同名同结构 | ABS_FOR_PRINTING, PRINTF_ABS, bastardized_floor, get_components, get_normalized_components, get_sign_bit ...(+5) | ABS_FOR_PRINTING, PRINTF_ABS, bastardized_floor, get_components, get_normalized_components, get_sign_bit ...(+5) |
| `arceos/ulib/axlibc/c/pow.c#pow` | `arceos/ulib/axlibc/c/pow.c#pow` | 同板块同名同结构 | __builtin_fma, __math_invalid, __math_oflow, __math_uflow, asdouble, asuint64 ...(+8) | __builtin_fma, __math_invalid, __math_oflow, __math_uflow, asdouble, asuint64 ...(+8) |
| `arceos/ulib/axlibc/c/string.c#twoway_strstr` | `arceos/ulib/axlibc/c/string.c#twoway_strstr` | 同板块同名同结构 | BITOP, MAX, memchr, memcmp | BITOP, MAX, memchr, memcmp |
| `arceos/ulib/axlibc/c/printf.c#print_broken_up_decimal` | `arceos/ulib/axlibc/c/printf.c#print_broken_up_decimal` | 同板块同名同结构 | out_rev_ | out_rev_ |


# 最终同源性定性仲裁报告

**案号**：[2026-T2026104879910415-XX] vs [2025-狗剩-海南大学]
**仲裁方**：代码同源性审计专家
**仲裁基准**：客观 AST 指纹、双向覆盖率、模块边界、外部调用图谱差异、共享上游框架事实

---

## 1. 宏观判定

- **定性结论**: **独立开发，底层架构显著差异**
- **客观 AST 相似度**: 12.88%
- **共同 AST 指纹**: 999
- **唯一 AST 指纹**: 目标项目 5738，参考项目 3020
- **覆盖率**: 目标项目 17.41%，参考项目 33.08%

**判定依据**：目标项目整体 AST 覆盖率仅 17.41%，远低于可认定为“结构性复用”的阈值（通常 ≥ 50%）。参考项目覆盖率 33.08% 高于目标，说明重合部分更多是参考项目被目标“覆盖”，而非目标从参考“搬运”——这与双方共同依赖 ArceOS 上游框架的事实高度吻合。目标项目 5738 个唯一 AST 指纹（占 82.59%）为独立代码，体量上占据绝对主导。

---

## 2. 全模块覆盖矩阵解读

| 模块 | 目标函数数 | 参考函数数 | 共同唯一 AST 指纹 | Target 覆盖 | Base 覆盖 | 证据强度 | 解读 |
| :--- | ---: | ---: | ---: | ---: | ---: | :--- | :--- |
| 进程管理 | 650 | 93 | 29 | 5.04% | 34.52% | **弱** | 目标 94.96% 代码独立；重合部分集中于 `arceos/` 上游路径，属共享框架代码；目标自有 `kernel/src/task/mod.rs` 与参考 `core/src/task/thread.rs` 在进程/线程模型上存在架构差异 |
| 任务调度/异步运行时 | 806 | 399 | 129 | 17.27% | 44.79% | **中（上游共享）** | 双方均存在该模块；重合集中于 `arceos/modules/axtask/`、`arceos/ulib/axlibc/` 等 ArceOS 上游路径；`NoPreemptIrqSave`、`SpinNoIrq` 等为框架标准泛型参数，非个人风格 |
| 内存管理 | 343 | 326 | 100 | 32.57% | 34.36% | **中（上游共享）** | 双方均存在；重合集中于 `arceos/modules/axruntime/`、`arceos/modules/axdriver/` 等上游路径；`rust_main` 等初始化流程为框架标准入口 |
| 中断/异常处理 | 35 | 62 | 2 | 6.25% | 4.08% | **弱** | 双方均存在但重合极低；目标 `kernel/src/syscall/poll.rs` 与参考 `arceos/modules/axhal/src/arch/x86_64/idt.rs` 为跨模块对齐，不构成同板块证据 |
| 系统调用 | 239 | 153 | 56 | 25.57% | 43.41% | **中（上游共享）** | 双方均存在；重合集中于 `arceos/api/arceos_posix_api/` 上游路径；`syscall_body!` 宏为框架标准模板 |
| 文件系统 | 826 | 747 | 221 | 31.94% | 39.05% | **中（上游共享）** | 双方均存在；重合集中于 `arceos/ulib/axlibc/`、`arceos/modules/axfs/` 上游路径；目标自有 `kernel/src/fs/procfs.rs` 与参考 `core/src/vfs/proc.rs` 在 sysctl 实现上存在显著差异 |
| 设备驱动 | 105 | 142 | 81 | 83.51% | 65.85% | **中（上游共享）** | 双方均存在；高覆盖率源于 `arceos/tools/bwbench_client/`、`arceos/modules/axdriver/` 等上游工具和驱动框架的完全复用 |
| 网络协议栈 | 61 | 77 | 51 | 100.0% | 96.23% | **中（上游共享）** | 双方均存在；近乎完全重合源于 `arceos/ulib/axlibc/c/network.c`、`arceos/api/arceos_posix_api/src/imp/net.rs` 等上游标准实现 |
| 文件系统/第三方库 | 899 | 689 | 2 | 0.24% | 0.34% | **弱** | 双方均存在但重合极低；`third_party/rust/git/` 与 `crates/` 为各自独立引入的第三方库 |
| 网络协议栈/第三方库 | 2529 | 107 | 64 | 3.31% | 64.0% | **弱** | 目标函数数远超参考；重合集中于 `arceos/modules/axnet/src/smoltcp_impl/` 上游路径；目标 `third_party/rust/git/smoltcp/` 为独立引入的第三方库 |
| 第三方库 | 0 | 386 | 0 | 0.0% | 0.0% | **未形成** | 仅参考项目体现（`crates/visionfive2-sd/`、`crates/page_table_multiarch/` 等），目标项目无对应模块 |
| 通用模块/其他 | 442 | 665 | 269 | 78.43% | 54.79% | **中（上游共享）** | 双方均存在；高覆盖率源于 `arceos/ulib/axlibc/c/printf.c`、`arceos/ulib/axlibc/c/pow.c` 等上游 C 库标准实现 |

---

## 3. 底层 AST 关键证据指纹表

| 所属板块 | 目标项目路径 | 参考项目路径 | 证据类型 | 技术说明 |
| :--- | :--- | :--- | :--- | :--- |
| 进程管理 | `kernel/src/fs/procfs.rs#write_at` | `core/src/vfs/proc.rs#write_at` | 同名异实 | 目标实现完整 sysctl 写入路径（`PROC_SYS_DOMAINNAME.lock().set` 等），参考仅返回 `Err`；外部调用对象完全不同 |
| 进程管理 | `kernel/src/fs/procfs.rs#read_at` | `core/src/vfs/proc.rs#read_at` | 同名异实 | 目标使用 `with_rendered_content` 渲染输出，参考使用 `IRQ_STATISTICS.iter` 统计输出；业务领域不同 |
| 进程管理 | `kernel/src/task/mod.rs#parent` | `arceos/api/arceos_posix_api/src/imp/path_link.rs#parent` | 跨板块同名硬凑 | 目标为进程父子关系（`self.parent.load`），参考为路径字符串取父目录（`path.rfind('/')`）；语义完全不同 |
| 进程管理 | `kernel/src/task/mod.rs#set_clear_child_tid` | `core/src/task/thread.rs#set_clear_child_tid` | 同名异模 | 目标为进程模型（`Some`），参考为线程模型（`self.clear_child_tid.store`）；外部调用不同 |
| 进程管理 | `arceos/modules/axtask/src/run_queue.rs#exit_current` | `arceos/modules/axtask/src/run_queue.rs#exit_current` | 上游共享 | 双方均引用 ArceOS 上游 `axtask` 模块；目标调用 `axhal::power::system_off`，参考调用 `axhal::misc::terminate`，HAL 抽象不同 |
| 进程管理 | `arceos/modules/axtask/src/wait_queue.rs#*` | `arceos/modules/axtask/src/wait_queue.rs#*` | 上游共享 | ArceOS 标准等待队列实现；`VecDeque`+`SpinNoIrq` 为框架固定数据结构；`NoPreemptIrqSave` 为框架泛型参数 |
| 进程管理 | `arceos/api/arceos_posix_api/src/imp/task.rs#sys_exit` | `arceos/api/arceos_posix_api/src/imp/task.rs#sys_exit` | 上游共享 | ArceOS POSIX 兼容层；目标 `axhal::power::system_off` vs 参考 `axhal::misc::terminate`，HAL 模块不同 |
| 进程管理 | `arceos/ulib/axlibc/c/pthread.c#pthread_cond_wait` | `arceos/ulib/axlibc/c/pthread.c#pthread_cond_wait` | 上游共享桩 | 仅含 `unimplemented` 宏，无独创性表达 |
| 任务调度/异步运行时 | `arceos/modules/axtask/src/run_queue.rs#switch_to` | `arceos/modules/axtask/src/run_queue.rs#switch_to` | 上游共享 | ArceOS 标准上下文切换；`Arc::downgrade`、`CurrentTask::set_current` 为框架标准流程 |
| 任务调度/异步运行时 | `arceos/modules/axlog/src/lib.rs#log` | `arceos/modules/axlog/src/lib.rs#log` | 上游共享 | ArceOS 标准日志宏；`cfg_if::cfg_if!` 为框架条件编译 |
| 内存管理 | `arceos/modules/axruntime/src/lib.rs#rust_main` | `arceos/modules/axruntime/src/lib.rs#rust_main` | 上游共享 | ArceOS 标准初始化入口；调用序列为框架规定 |
| 内存管理 | `arceos/modules/axdriver/src/bus/pci.rs#config_pci_device` | `arceos/modules/axdriver/src/bus/pci.rs#config_pci_device` | 上游共享 | ArceOS PCI 配置标准流程 |
| 系统调用 | `arceos/api/arceos_posix_api/src/imp/net.rs#sys_getaddrinfo` | `arceos/api/arceos_posix_api/src/imp/net.rs#sys_getaddrinfo` | 上游共享 | ArceOS POSIX 网络 API 模板；`syscall_body!` 为框架标准宏 |
| 系统调用 | `arceos/api/arceos_posix_api/src/imp/io.rs#sys_writev` | `api/src/imp/io.rs#sys_writev` | 上游共享 | 目标仅 `debug!`+`syscall_body!` 骨架，参考有完整 iov 处理；实现深度不同 |
| 文件系统 | `kernel/src/fs/rootfs.rs#create_file` | `arceos/modules/axfs/src/root.rs#create_file` | 同名异实 | 目标有完整 `ensure_runtime_dir_chain` 链式创建，参考为简单 `parent.create`；复杂度不同 |
| 文件系统 | `arceos/ulib/axlibc/c/glob.c#do_glob` | `arceos/ulib/axlibc/c/glob.c#do_glob` | 上游共享 | ArceOS 上游 C 库 glob 实现 |
| 设备驱动 | `arceos/tools/bwbench_client/src/main.rs#transmit_benchmark` | `arceos/tools/bwbench_client/src/main.rs#transmit_benchmark` | 上游共享 | ArceOS 上游带宽测试工具 |
| 设备驱动 | `arceos/modules/axdriver/src/lib.rs#init_drivers` | `arceos/modules/axdriver/src/lib.rs#init_drivers` | 上游共享 | ArceOS 标准驱动初始化 |
| 网络协议栈 | `arceos/ulib/axlibc/c/network.c#inet_pton` | `arceos/ulib/axlibc/c/network.c#inet_pton` | 上游共享 | ArceOS 上游 C 库网络函数 |
| 网络协议栈 | `arceos/api/arceos_posix_api/src/imp/net.rs#shutdown` | `api/src/fd/net.rs#shutdown` | 上游共享 | ArceOS 标准 socket shutdown 实现 |
| 第三方库/公共依赖 | `third_party/rust/git/ext4-view-rs/src/file.rs#read_bytes` | `crates/lwext4_rust/src/inode/file.rs#read_bytes` | 弱信号 | 目标使用块级读取，参考使用 `ext4_block_readbytes` FFI；均为 ext4 操作但实现路径不同 |
| 第三方库/公共依赖 | `third_party/rust/git/smoltcp/src/socket/tcp.rs#connect` | `arceos/modules/axnet/src/smoltcp_impl/udp.rs#connect` | 跨协议同名 | 目标为 TCP connect，参考为 UDP connect；协议不同 |
| 通用模块/其他 | `arceos/ulib/axlibc/c/printf.c#print_exponential_number` | `arceos/ulib/axlibc/c/printf.c#print_exponential_number` | 上游共享 | ArceOS 上游 C 库 printf 浮点格式化 |
| 通用模块/其他 | `arceos/ulib/axlibc/c/string.c#twoway_strstr` | `arceos/ulib/axlibc/c/string.c#twoway_strstr` | 上游共享 | ArceOS 上游 C 库 Two-Way 字符串匹配算法 |

---

## 4. 核心代码调用流对比剖析

### 【进程管理 / procfs 写入路径】

* **底层技术析理**: 目标项目 `kernel/src/fs/procfs.rs#write_at` 实现了完整的 Linux 兼容 sysctl 写入路径，外部调用涉及 `PROC_SYS_DOMAINNAME.lock().set`、`PROC_SYS_NET_IPV4_CONF_DEFAULT_TAG.store`、`PROC_SYS_PIPE_MAX_SIZE.store`、`PROC_SYS_SCHED_AUTOGROUP_ENABLED.store`、`binding.target.net_compat_state` 等 17+ 个系统配置对象，体现了面向 Linux sysctl 接口的完整设计。参考项目 `core/src/vfs/proc.rs#write_at` 仅返回 `Err`，为占位实现。两者在**抽象层次**（完整 sysctl vs 空桩）、**内存模型**（多锁精细控制 vs 无操作）、**业务领域**（系统配置管理 vs 无）上存在根本性差异。

* **代码溯源链接**:
  - 目标项目: `kernel/src/fs/procfs.rs#write_at`
  - 参考项目: `core/src/vfs/proc.rs#write_at`

---

### 【进程管理 / 任务退出路径】

* **底层技术析理**: 目标项目 `arceos/modules/axtask/src/run_queue.rs#exit_current` 与参考项目同路径函数在核心调度逻辑上一致（`EXITED_TASKS` 队列操作、`WAIT_FOR_EXIT` 通知），但**终端处理路径不同**：目标调用 `axhal::power::system_off`（电源管理模块），参考调用 `axhal::misc::terminate`（杂项模块）。这反映了两者在 HAL 抽象层的不同选择——目标面向 `power` 子系统，参考面向 `misc` 子系统。该差异虽小，但在嵌入式/OS 内核中，HAL 模块的选择直接影响系统架构和平台移植策略。

* **代码溯源链接**:
  - 目标项目: `arceos/modules/axtask/src/run_queue.rs#exit_current`
  - 参考项目: `arceos/modules/axtask/src/run_queue.rs#exit_current`

---

### 【进程管理 / 进程父子关系 vs 路径父子关系】

* **底层技术析理**: 目标项目 `kernel/src/task/mod.rs#parent` 通过 `self.parent.load` 获取进程的父进程引用，属于**进程树管理**范畴。参考项目 `arceos/api/arceos_posix_api/src/imp/path_link.rs#parent` 通过 `path.rfind('/').ok_or` 和 `path.strip_suffix` 解析路径字符串的父目录，属于**文件路径操作**范畴。两者函数名相同但**语义域完全不同**——一个是进程控制块字段访问，一个是字符串处理。控方将其列为同板块证据属于跨板块硬凑。

* **代码溯源链接**:
  - 目标项目: `kernel/src/task/mod.rs#parent`
  - 参考项目: `arceos/api/arceos_posix_api/src/imp/path_link.rs#parent`

---

### 【文件系统 / 根文件系统文件创建】

* **底层技术析理**: 目标项目 `kernel/src/fs/rootfs.rs#create_file` 实现了完整的链式目录创建逻辑（`ensure_runtime_dir_chain`），包含属性检查（`attr.is_dir`）、错误映射（`.map_err`）、已有节点处理（`existing.get_attr`）等 24+ 个外部调用，体现了面向运行时目录树的复杂文件系统设计。参考项目 `arceos/modules/axfs/src/root.rs#create_file` 为简单封装（`parent.create`、`parent.lookup`），调用链短且无链式创建。两者在**对象管理模型**（链式保证 vs 单层创建）和**复杂度**上存在显著差异。

* **代码溯源链接**:
  - 目标项目: `kernel/src/fs/rootfs.rs#create_file`
  - 参考项目: `arceos/modules/axfs/src/root.rs#create_file`

---

### 【系统调用 / writev 实现】

* **底层技术析理**: 目标项目 `arceos/api/arceos_posix_api/src/imp/io.rs#sys_writev` 仅包含 `debug!` + `syscall_body!` 宏骨架，为模板级占位实现。参考项目 `api/src/imp/io.rs#sys_writev` 包含完整的 `iov.get_as_bytes`、`core::slice::from_raw_parts`、`(0..=1024).contains` 等 iovec 处理逻辑。两者在**实现深度**上存在根本差异——目标为宏模板，参考为完整系统调用实现。

* **代码溯源链接**:
  - 目标项目: `arceos/api/arceos_posix_api/src/imp/io.rs#sys_writev`
  - 参考项目: `api/src/imp/io.rs#sys_writev`

---

## 5. 单方模块与降权项

### 仅参考项目体现的模块（目标项目无对应）

| 模块 | 代表函数 | 说明 |
| :--- | :--- | :--- |
| 第三方库 | `crates/visionfive2-sd/src/lib.rs#send_cmd` | VisionFive2 SD 卡驱动，目标项目无此硬件支持 |
| 第三方库 | `crates/visionfive2-sd/src/lib.rs#init_sdcard` | 同上，RISC-V 平台特定驱动 |
| 第三方库 | `crates/page_table_multiarch/page_table_multiarch/src/bits64.rs#map_region` | 多架构页表实现，目标项目无对应 |
| 第三方库 | `crates/axdriver_crates/axdriver_virtio/src/net.rs#try_new` | VirtIO 网络驱动，目标项目使用不同驱动路径 |

**降权说明**：这些模块仅在参考项目中存在，目标项目无同模块可对齐证据，不构成任何同源性指控的基础。

### 跨板块同名硬凑项（控方降级弱信号）

| 控方配对 | 实际问题 | 降权说明 |
| :--- | :--- | :--- |
| 目标 `kernel/src/task/mod.rs#parent` vs 参考 `path_link.rs#parent` | 进程父子关系 vs 路径父目录 | 语义域完全不同，函数名巧合 |
| 目标 `kernel/src/task/mod.rs#set_clear_child_tid` vs 参考 `core/src/task/thread.rs#set_clear_child_tid` | 进程模型 vs 线程模型 | 外部调用对象不同 |
| 目标 `kernel/src/fs/procfs.rs#read_at` vs 参考 `core/src/vfs/proc.rs#read_at` | sysctl 渲染 vs IRQ 统计 | 业务领域不同 |

### 公共接口/泛型函数名降权

以下名称属于 Rust 标准库或 ArceOS 框架的标准接口/泛型参数，不能作为同源证据：
- `read_at` / `write_at`：VFS 节点操作标准方法名
- `NoPreemptIrqSave`：ArceOS 调度器泛型参数
- `SpinNoIrq`：ArceOS 自旋锁类型
- `syscall_body!`：ArceOS 系统调用模板宏
- `unimplemented`：Rust 标准宏
- `VecDeque::with_capacity`：Rust 标准库方法
- `Arc::new` / `Arc::ptr_eq`：Rust 标准库方法

---

## 6. 保守结论

本案客观 AST 相似度仅 12.88%，目标项目 82.59% 的 AST 指纹为独立代码，进程管理板块覆盖率仅 5.04%，不满足“结构性复用”的认定条件。控方所列“铁证”绝大多数位于 `arceos/` 上游路径，属于双方共同依赖的 ArceOS 开源框架标准模块——参考项目自身也将这些文件置于 `arceos/` 目录下，说明其并非原创者。目标项目自有内核代码（`kernel/src/fs/procfs.rs`、`kernel/src/task/mod.rs`）在 sysctl 实现、进程模型、HAL 抽象选择上与参考项目存在显著架构差异。控方存在将标准接口名、上游公共代码、跨板块同名函数误判为原创表达的方法问题。综合判定：**目标项目 [2026-T2026104879910415-XX] 在进程管理板块为独立开发，不构成对参考项目 [2025-狗剩-海南大学] 的代码抄袭或结构性复用。**

------------------------------

