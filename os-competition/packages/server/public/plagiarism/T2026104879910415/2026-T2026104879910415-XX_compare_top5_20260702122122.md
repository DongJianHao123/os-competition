# 🛡️ OS 内核赛自动化代码级架构审计报告

**审计时间**: `2026-07-02 12:21:22`
**参赛作品名称**: `2026-T2026104879910415-XX`
**年份**: `2026`
**学校**: `XX`
**队伍名称**: `T2026104879910415`
**比赛名字**: `OS-Competition`
**比赛子赛道**: `Kernel-Implementation`
**仓库地址**: `https://gitlab.eduxiji.net/educg-group-43501-3132633/T2026104879910415-3331`
**推测开发语言**: `Rust / C (由AST探针自动识别)`
**特征函数量**: `6935`

## ⚔️ 底层代码同源性对比 (AST Homology)
> 本部分由专门的代码审查智能体，对该项目与符合年份条件的历届最高相似度项目进行控制流与底层调用对比。新版报告会先给出确定性的全模块覆盖矩阵，再给出 Agent 的分模块解释，避免只围绕少量抽样函数展开。

### 🏆 Top 5 底层对标: vs `2025-AstranciA-哈尔滨工业大学深圳`（哈尔滨工业大学（深圳） / AstranciA）

#### 对方作品信息

**对方年份**: `2025`
**对方学校**: `哈尔滨工业大学（深圳）`
**对方队伍名称**: `AstranciA`
**对方比赛名字**: `OS-Competition`
**对方比赛子赛道**: `Kernel-Implementation`
**对方仓库地址**: `https://gitlab.eduxiji.net/educg-group-36002-2710490/T202518123995667-1436`

> 双向覆盖率：共同 AST 指纹: 990；唯一 AST 指纹: 目标项目 5738，参考项目 3750；覆盖率: 目标项目 17.25%，参考项目 26.4%

## 全模块对比总览（确定性证据）

> 总体覆盖率：共同 AST 指纹: 990；唯一 AST 指纹: 目标项目 5738，参考项目 3750；覆盖率: 目标项目 17.25%，参考项目 26.4%

### 1. 模块覆盖矩阵
| 模块 | 目标函数数 | 参考函数数 | 共同唯一 AST 指纹 | Target 覆盖 | Base 覆盖 | 状态 |
| :--- | ---: | ---: | ---: | ---: | ---: | :--- |
| 进程管理 | 650 | 112 | 29 | 5.04% | 31.52% | 双方存在且有结构重合 |
| 任务调度/异步运行时 | 806 | 455 | 130 | 17.4% | 40.5% | 双方存在且有结构重合 |
| 内存管理 | 343 | 343 | 96 | 31.27% | 31.17% | 双方存在且有结构重合 |
| 中断/异常处理 | 35 | 71 | 2 | 6.25% | 3.51% | 双方存在且有结构重合 |
| 系统调用 | 239 | 136 | 56 | 25.57% | 48.28% | 双方存在且有结构重合 |
| 文件系统 | 826 | 620 | 211 | 30.49% | 43.51% | 双方存在且有结构重合 |
| 设备驱动 | 105 | 142 | 79 | 81.44% | 62.7% | 双方存在且有结构重合 |
| 网络协议栈 | 61 | 61 | 51 | 100.0% | 100.0% | 双方存在且有结构重合 |
| 文件系统/第三方库 | 899 | 775 | 4 | 0.48% | 0.63% | 双方存在且有结构重合 |
| 网络协议栈/第三方库 | 2529 | 217 | 64 | 3.31% | 33.16% | 双方存在且有结构重合 |
| 第三方库 | 0 | 1155 | 0 | 0.0% | 0.0% | 仅参考项目体现 |
| 通用模块/其他 | 442 | 673 | 267 | 77.84% | 49.17% | 双方存在且有结构重合 |

### 2. 分模块证据清单

#### 进程管理
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `arceos/modules/axtask/src/run_queue.rs#exit_current` | `AstrancE/modules/axtask/src/run_queue.rs#exit_current` | 同板块同名同结构 | EXITED_TASKS.current_ref_mut_raw, EXITED_TASKS.current_ref_mut_raw().clear, EXITED_TASKS.current_ref_mut_raw().push_back, WAIT_FOR_EXIT.current_ref_mut_raw, WAIT_FOR_EXIT.current_ref_mut_raw().notify_one, assert! ...(+8) | EXITED_TASKS.current_ref_mut_raw, EXITED_TASKS.current_ref_mut_raw().clear, EXITED_TASKS.current_ref_mut_raw().push_back, WAIT_FOR_EXIT.current_ref_mut_raw, WAIT_FOR_EXIT.current_ref_mut_raw().notify_one, assert! ...(+8) |
| `arceos/api/arceos_posix_api/src/imp/io_mpx/epoll.rs#sys_epoll_wait` | `AstrancE/api/arceos_posix_api/src/imp/io_mpx/epoll.rs#sys_epoll_wait` | 同板块同名同结构 | debug!, syscall_body! | debug!, syscall_body! |
| `arceos/modules/axtask/src/wait_queue.rs#cancel_events` | `AstrancE/modules/axtask/src/wait_queue.rs#cancel_events` | 同板块同名同结构 | curr.in_wait_queue, curr.ptr_eq, curr.set_in_wait_queue, curr.timer_ticket_expired, self.queue.lock, self.queue.lock().retain | curr.in_wait_queue, curr.ptr_eq, curr.set_in_wait_queue, curr.timer_ticket_expired, self.queue.lock, self.queue.lock().retain |
| `arceos/modules/axfs/src/mounts.rs#procfs` | `AstrancE/modules/axfs/src/mounts.rs#procfs` | 同板块同名 | Arc::new, Ok, file_over.write_at, file_somaxconn.write_at, fs::ramfs::RamFileSystem::new, proc_root.clone ...(+3) | Arc::new, Ok, ProcFileSystem::new, format!, proc_root.create_static_file, proc_version_string.as_bytes ...(+2) |

#### 任务调度/异步运行时
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `arceos/ulib/axlibc/c/printf.c#format_string_loop` | `AstrancE/ulib/axlibc/c/printf.c#format_string_loop` | 同板块同名同结构 | ABS_FOR_PRINTING, ADVANCE_IN_FORMAT_STRING, atou_, is_digit_, out_rev_, parse_flags ...(+5) | ABS_FOR_PRINTING, ADVANCE_IN_FORMAT_STRING, atou_, is_digit_, out_rev_, parse_flags ...(+5) |
| `arceos/modules/axlog/src/lib.rs#log` | `AstrancE/modules/axlog/src/lib.rs#log` | 同板块同名同结构 | cfg_if::cfg_if!, record.level, record.line, record.line().unwrap_or, record.metadata, record.target ...(+1) | cfg_if::cfg_if!, record.level, record.line, record.line().unwrap_or, record.metadata, record.target ...(+1) |
| `arceos/modules/axtask/src/run_queue.rs#switch_to` | `AstrancE/modules/axtask/src/run_queue.rs#switch_to` | 同板块同名 | (*prev_ctx_ptr).switch_to, Arc::downgrade, CurrentTask::set_current, PREV_TASK.current_ref_mut_raw, assert!, clear_prev_task_on_cpu ...(+8) | (*prev_ctx_ptr).switch_to, Arc::downgrade, CurrentTask::set_current, PREV_TASK.current_ref_mut_raw, assert!, clear_prev_task_on_cpu ...(+7) |
| `arceos/modules/axtask/src/run_queue.rs#put_task_with_state` | `AstrancE/modules/axtask/src/run_queue.rs#put_task_with_state` | 同板块同名 | core::hint::spin_loop, self.scheduler.lock, self.scheduler.lock().put_prev_task, task.is_idle, task.on_cpu, task.set_cpu_id ...(+1) | core::hint::spin_loop, self.scheduler.lock, self.scheduler.lock().put_prev_task, task.is_idle, task.on_cpu, task.transition_state |

#### 内存管理
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `arceos/ulib/axlibc/src/strftime.rs#strftime_inner` | `AstrancE/ulib/axlibc/src/strftime.rs#strftime_inner` | 同板块同名同结构 | CountingWriter::new, format.offset, inner_strftime, w! | CountingWriter::new, format.offset, inner_strftime, w! |
| `arceos/ulib/axlibc/src/strftime.rs#inner_strftime` | `AstrancE/ulib/axlibc/src/strftime.rs#inner_strftime` | 同板块同名同结构 | format.offset, w! | format.offset, w! |
| `arceos/modules/axruntime/src/lib.rs#rust_main` | `AstrancE/modules/axruntime/src/lib.rs#rust_main` | 同板块同名 | INITED_CPUS.fetch_add, ax_println!, axdisplay::init_display, axdriver::init_drivers, axfs::init_filesystems, axhal::init_early ...(+24) | INITED_CPUS.fetch_add, ax_println!, axdisplay::init_display, axdriver::init_drivers, axfs::init_filesystems, axhal::mem::memory_regions ...(+24) |
| `arceos/modules/axdriver/src/bus/pci.rs#config_pci_device` | `AstrancE/modules/axdriver/src/bus/pci.rs#config_pci_device` | 同板块同名 | Ok, allocator
                    .as_mut, allocator
                    .as_mut()
                    .expect, allocator
                    .as_mut()
                    .expect("No memory ranges available for PCI BARs!")
                    .alloc, allocator
                    .as_mut()
                    .expect("No memory ranges available for PCI BARs!")
                    .alloc(size as _)
                    .ok_or, debug! ...(+8) | Ok, allocator
                    .as_mut, allocator
                    .as_mut()
                    .expect, allocator
                    .as_mut()
                    .expect("No memory ranges available for PCI BARs!")
                    .alloc, allocator
                    .as_mut()
                    .expect("No memory ranges available for PCI BARs!")
                    .alloc(size as _)
                    .ok_or, debug! ...(+7) |

#### 中断/异常处理
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `arceos/ulib/axlibc/c/pow.c#exp_inline` | `AstrancE/ulib/axlibc/c/pow.c#exp_inline` | 同板块同名同结构 | __math_oflow, __math_uflow, asdouble, asuint64, converttoint, eval_as_double ...(+4) | __math_oflow, __math_uflow, asdouble, asuint64, converttoint, eval_as_double ...(+4) |
| `arceos/modules/axsync/src/mutex.rs#may_interrupt` | `AstrancE/modules/axsync/src/mutex.rs#may_interrupt` | 同板块同名同结构 | rand::random::<u32>, thread::yield_now | rand::random::<u32>, thread::yield_now |
| `kernel/src/init.rs#render_ltp_runtest_driver` | `AstrancE/modules/axhal/src/arch/loongarch64/unaligned.rs#emulate_load_store_insn` | 同模块上下文对齐/差异观察 | String::new, cases.is_empty, format!, runtime_dir.trim_start_matches, writeln! | asm!, badv::read, badv::read().raw, core::slice::from_raw_parts_mut, panic!, read_fpr ...(+5) |
| `kernel/src/syscall/poll.rs#syscall_epoll_wait` | `AstrancE/modules/axhal/src/arch/aarch64/trap.rs#handle_sync_exception` | 同模块上下文对齐/差异观察 | Err, deadline_from_timeout, decode_epoll_maxevents, do_epoll_wait, install_temporary_signal_mask, read_optional_signal_mask | ESR_EL1.extract, crate::trap::handle_syscall, crate::trap::post_trap_callback, debug!, esr.read, esr.read_as_enum ...(+8) |

#### 系统调用
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `arceos/api/arceos_posix_api/src/imp/net.rs#sys_getaddrinfo` | `AstrancE/api/arceos_posix_api/src/imp/net.rs#sys_getaddrinfo` | 同板块同名同结构 | char_ptr_to_str, debug!, syscall_body! | char_ptr_to_str, debug!, syscall_body! |
| `arceos/api/arceos_posix_api/src/imp/net.rs#sys_recvfrom` | `AstrancE/api/arceos_posix_api/src/imp/net.rs#sys_recvfrom` | 同板块同名同结构 | debug!, syscall_body! | debug!, syscall_body! |
| `arceos/api/arceos_posix_api/src/imp/net.rs#sys_socket` | `AstrancE/api/arceos_posix_api/src/imp/net.rs#sys_socket` | 同板块同名同结构 | debug!, syscall_body! | debug!, syscall_body! |
| `arceos/api/arceos_posix_api/src/imp/io.rs#sys_writev` | `AstrancE/api/arceos_posix_api/src/imp/io.rs#sys_writev` | 同板块同名 | debug!, syscall_body! | debug!, syscall_body! |

#### 文件系统
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `arceos/ulib/axlibc/src/setjmp.rs#longjmp` | `AstrancE/ulib/axlibc/src/setjmp.rs#longjmp` | 同板块同名 | compile_error!, core::arch::naked_asm! | core::arch::naked_asm! |
| `arceos/ulib/axlibc/c/glob.c#do_glob` | `AstrancE/ulib/axlibc/c/glob.c#do_glob` | 同板块同名同结构 | S_ISDIR, append, closedir, do_glob, errfunc, fnmatch ...(+7) | S_ISDIR, append, closedir, do_glob, errfunc, fnmatch ...(+7) |
| `arceos/ulib/axlibc/src/setjmp.rs#setjmp` | `AstrancE/ulib/axlibc/src/setjmp.rs#setjmp` | 同板块同名 | compile_error!, core::arch::naked_asm! | core::arch::naked_asm! |
| `kernel/src/fs/rootfs.rs#create_file` | `AstrancE/modules/axfs/src/root.rs#create_file` | 同板块同名 | Err, Ok, attr.is_dir, ensure_runtime_dir_chain, ensure_runtime_dir_chain(self, &parent).map_err, existing.get_attr ...(+24) | ax_err!, parent.create, parent.lookup, parent_node_of, path.ends_with, path.is_empty |

#### 设备驱动
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `arceos/tools/bwbench_client/src/main.rs#transmit_benchmark` | `AstrancE/tools/bwbench_client/src/main.rs#transmit_benchmark` | 同板块同名同结构 | Local::now, NetDevice::new, NetDevice::new(interface).unwrap, current_time.signed_duration_since, current_time.signed_duration_since(past_time).num_seconds, dev.send ...(+2) | Local::now, NetDevice::new, NetDevice::new(interface).unwrap, current_time.signed_duration_since, current_time.signed_duration_since(past_time).num_seconds, dev.send ...(+2) |
| `arceos/tools/bwbench_client/src/device.rs#bind_interface` | `AstrancE/tools/bwbench_client/src/device.rs#bind_interface` | 同板块同名同结构 | ETH_P_ALL.to_be, Err, Ok, libc::bind, self.ifreq.ioctl, std::io::Error::last_os_error ...(+2) | ETH_P_ALL.to_be, Err, Ok, libc::bind, self.ifreq.ioctl, std::io::Error::last_os_error ...(+2) |
| `arceos/modules/axdriver/src/lib.rs#init_drivers` | `AstrancE/modules/axdriver/src/lib.rs#init_drivers` | 同板块同名 | AllDevices::default, all_devs.block.iter, all_devs.block.iter().enumerate, all_devs.display.iter, all_devs.display.iter().enumerate, all_devs.net.iter ...(+5) | AllDevices::default, all_devs.block.iter_mut, all_devs.block.iter_mut().enumerate, all_devs.display.iter_mut, all_devs.display.iter_mut().enumerate, all_devs.net.iter_mut ...(+14) |
| `arceos/tools/bwbench_client/src/main.rs#receive_benchmark` | `AstrancE/tools/bwbench_client/src/main.rs#receive_benchmark` | 同板块同名同结构 | Local::now, NetDevice::new, NetDevice::new(interface).unwrap, current_time.signed_duration_since, current_time.signed_duration_since(past_time).num_seconds, dev.recv ...(+1) | Local::now, NetDevice::new, NetDevice::new(interface).unwrap, current_time.signed_duration_since, current_time.signed_duration_since(past_time).num_seconds, dev.recv ...(+1) |

#### 网络协议栈
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `arceos/ulib/axlibc/c/network.c#inet_pton` | `AstrancE/ulib/axlibc/c/network.c#inet_pton` | 同板块同名同结构 | hexval, inet_pton, isdigit, memmove | hexval, inet_pton, isdigit, memmove |
| `arceos/api/arceos_posix_api/src/imp/net.rs#shutdown` | `AstrancE/api/arceos_posix_api/src/imp/net.rs#shutdown` | 同板块同名同结构 | Ok, tcpsocket.lock, tcpsocket.peer_addr, tcpsocket.shutdown, udpsocket.lock, udpsocket.peer_addr ...(+1) | Ok, tcpsocket.lock, tcpsocket.peer_addr, tcpsocket.shutdown, udpsocket.lock, udpsocket.peer_addr ...(+1) |
| `arceos/ulib/axlibc/c/socket.c#accept4` | `AstrancE/ulib/axlibc/c/socket.c#accept4` | 同板块同名同结构 | accept, fcntl | accept, fcntl |
| `arceos/api/arceos_posix_api/src/imp/net.rs#recvfrom` | `AstrancE/api/arceos_posix_api/src/imp/net.rs#recvfrom` | 同板块同名同结构 | Ok, Some, tcpsocket.lock, tcpsocket.lock().recv, tcpsocket.lock().recv(buf).map, udpsocket
                .lock ...(+2) | Ok, Some, tcpsocket.lock, tcpsocket.lock().recv, tcpsocket.lock().recv(buf).map, udpsocket
                .lock ...(+2) |

#### 文件系统/第三方库
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `third_party/rust/git/rust-fatfs/src/file.rs#seek` | `AstrancE/crates/lwext4_rust/examples/src/disk.rs#seek` | 同板块同名 | Err, Ok, Some, debug_assert!, error!, i64::from ...(+16) | Ok, Some, debug!, dev.position, dev.position().checked_add_signed, dev.position().checked_add_signed(off).map ...(+7) |
| `third_party/rust/git/rust-fatfs/src/dir.rs#create_dir` | `AstrancE/crates/axfs_crates/axfs_procfs/src/dir.rs#create_dir` | 同板块同名 | Ok, ShortNameGenerator::generate_dot, ShortNameGenerator::generate_dotdot, Some, dir.write_entry, e.to_dir ...(+13) | Err, Ok, ProcDir::new, ProcEntry::Dir, Some, dir.clone ...(+5) |
| `third_party/rust/git/ext4-view-rs/xtask/src/main.rs#create` | `AstrancE/modules/axfs/src/fs/lwext4_rust.rs#create` | 同板块同名 | Command::new, cmd
            // Set the ownership of the root directory in the filesystem
            // to the current uid/gid instead of root. That allows the
            // mounted filesystem to be edited without root permissions,
            // although the mount operation itself still requires root.
            .args, cmd
            // Set the ownership of the root directory in the filesystem
            // to the current uid/gid instead of root. That allows the
            // mounted filesystem to be edited without root permissions,
            // although the mount operation itself still requires root.
            .args(["-E", &format!("root_owner={uid}:{gid}")])
            // Set the volume label. This string is 16 bytes, which is
            // the maximum length.
            .args, cmd
            // Set the ownership of the root directory in the filesystem
            // to the current uid/gid instead of root. That allows the
            // mounted filesystem to be edited without root permissions,
            // although the mount operation itself still requires root.
            .args(["-E", &format!("root_owner={uid}:{gid}")])
            // Set the volume label. This string is 16 bytes, which is
            // the maximum length.
            .args(["-L", "ext4-view testfs"])
            .arg, cmd
            // Set the ownership of the root directory in the filesystem
            // to the current uid/gid instead of root. That allows the
            // mounted filesystem to be edited without root permissions,
            // although the mount operation itself still requires root.
            .args(["-E", &format!("root_owner={uid}:{gid}")])
            // Set the volume label. This string is 16 bytes, which is
            // the maximum length.
            .args(["-L", "ext4-view testfs"])
            .arg(&self.path)
            .arg, cmd.arg ...(+7) | Ok, e.try_into, e.try_into().unwrap, file.check_inode_exist, file.dir_mk, file.dir_mk(fpath)
                    .map ...(+12) |
| `third_party/rust/git/rust-fatfs/src/dir.rs#remove` | `AstrancE/crates/axfs_crates/axfs_ramfs/src/dir.rs#remove` | 同板块同名 | DirEntryData::deserialize, Err, Ok, SeekFrom::Current, SeekFrom::Start, Some ...(+15) | Err, log::debug!, name.is_empty, self
                        .children
                        .read, self
                        .children
                        .read()
                        .get, self
                        .children
                        .read()
                        .get(name)
                        .ok_or ...(+8) |

#### 网络协议栈/第三方库
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `arceos/modules/axnet/src/smoltcp_impl/tcp.rs#connect` | `AstrancE/modules/axnet/src/smoltcp_impl/tcp.rs#connect` | 同板块同名 | ETH0.iface.lock, Err, LO0.iface.lock, Ok, SOCKET_SET.add, SOCKET_SET.poll_interfaces ...(+29) | Err, Ok, Ok::<(IpEndpoint, IpEndpoint), AxError>, SOCKET_SET
                .with_socket_mut::<tcp::Socket, _, _>, SOCKET_SET.add, SocketSetWrapper::new_tcp_socket ...(+26) |
| `third_party/rust/git/smoltcp/src/storage/assembler.rs#add` | `AstrancE/crates/virtio-drivers/src/device/socket/connectionmanager.rs#add` | 同板块同名 | Contig::hole_and_data, Err, Ok, contig.has_data, contig.shrink_hole_to, contig.total_size ...(+11) | bytes.get, bytes.len, bytes_after_wraparound.len, min, self.buffer.len, self.buffer[0..bytes_after_wraparound.len()].copy_from_slice ...(+2) |
| `arceos/modules/axnet/src/smoltcp_impl/dns.rs#query` | `AstrancE/modules/axnet/src/smoltcp_impl/dns.rs#query` | 同板块同名 | Err, IpAddr::from, Ok, SOCKET_SET
            .with_socket_mut::<dns::Socket, _, _>, SOCKET_SET
            .with_socket_mut::<dns::Socket, _, _>(handle, |socket| {
                socket.start_query(iface.lock().context(), name, query_type)
            })
            .map_err, SOCKET_SET.poll_interfaces ...(+12) | Err, Ok, SOCKET_SET
            .with_socket_mut::<dns::Socket, _, _>, SOCKET_SET
            .with_socket_mut::<dns::Socket, _, _>(handle, |socket| {
                socket.start_query(iface.lock().context(), name, query_type)
            })
            .map_err, SOCKET_SET.poll_interfaces, SOCKET_SET.with_socket_mut::<dns::Socket, _, _> ...(+12) |
| `third_party/rust/git/smoltcp/src/socket/tcp.rs#connect` | `AstrancE/crates/virtio-drivers/src/device/socket/connectionmanager.rs#connect` | 同板块同名 | Err, Ok, Self::random_seq_no, Some, addr.is_unspecified, cx
                    .get_source_address ...(+9) | Connection::new, Err, Ok, SocketError::ConnectionExists.into, debug!, self.connections.iter ...(+3) |

#### 第三方库
仅参考项目中观察到该模块代表函数，目标项目没有同模块可对齐证据：
- 参考项目：`AstrancE/crates/axdriver_crates/axdriver_block/src/dw_mshc/mmc.rs#card_init`，调用摘要：CID::from, CLKDIV::offset, CMD::data_cmd, CMD::no_data_cmd, CMD::no_data_cmd_no_crc, CMD::no_data_cmd_no_crc(card_idx, 2).with_response_length ...(+33)
- 参考项目：`AstrancE/crates/virtio-drivers/src/queue.rs#fake_read_write_queue`，调用摘要：(*available_ring).idx.load, (*used_ring).idx.fetch_add, (*used_ring).idx.load, Vec::new, assert!, assert_eq! ...(+18)
- 参考项目：`AstrancE/crates/virtio-drivers/src/device/sound/fake.rs#handle_control_request`，调用摘要：CommandCode::SOk.into, Some, Vec::new, VirtIOSndHdr {
                            command_code: CommandCode::SOk.into(),
                        }
                        .as_bytes, VirtIOSndHdr::read_from_prefix, VirtIOSndHdr::read_from_prefix(&request)
                .expect ...(+16)
- 参考项目：`AstrancE/crates/virtio-drivers/src/transport/pci.rs#new`，调用摘要：Err, Ok, Some, UniqueMmioPointer::new, VirtioPciError::InvalidDeviceId, VirtioPciError::InvalidNotifyOffMultiplier ...(+15)

#### 通用模块/其他
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `arceos/ulib/axlibc/c/printf.c#print_exponential_number` | `AstrancE/ulib/axlibc/c/printf.c#print_exponential_number` | 同板块同名同结构 | ABS_FOR_PRINTING, PRINTF_ABS, bastardized_floor, get_components, get_normalized_components, get_sign_bit ...(+5) | ABS_FOR_PRINTING, PRINTF_ABS, bastardized_floor, get_components, get_normalized_components, get_sign_bit ...(+5) |
| `arceos/ulib/axlibc/c/pow.c#pow` | `AstrancE/ulib/axlibc/c/pow.c#pow` | 同板块同名同结构 | __builtin_fma, __math_invalid, __math_oflow, __math_uflow, asdouble, asuint64 ...(+8) | __builtin_fma, __math_invalid, __math_oflow, __math_uflow, asdouble, asuint64 ...(+8) |
| `arceos/ulib/axlibc/c/string.c#twoway_strstr` | `AstrancE/ulib/axlibc/c/string.c#twoway_strstr` | 同板块同名同结构 | BITOP, MAX, memchr, memcmp | BITOP, MAX, memchr, memcmp |
| `arceos/ulib/axlibc/c/printf.c#print_broken_up_decimal` | `AstrancE/ulib/axlibc/c/printf.c#print_broken_up_decimal` | 同板块同名同结构 | out_rev_ | out_rev_ |


# 最终同源性定性仲裁报告

---

## 1. 宏观判定

- **定性结论**: **独立开发 / 底层架构显著差异**
- **客观 AST 相似度**: 11.65%
- **双向覆盖率**: 目标项目 17.25%，参考项目 26.4%
- **共同 AST 指纹**: 990
- **唯一 AST 指纹**: 目标项目 5738，参考项目 3750

**判定依据**：11.65% 的 AST 相似度远低于构成“实质性相似”的阈值（通常需 > 50%）。目标项目 82.75% 的代码在参考项目中无对应指纹，进程管理板块覆盖率仅 5.04%。存在的相似性主要源于共同上游开源框架（ArceOS）继承、第三方库共享（smoltcp、axlibc、rust-fatfs）及操作系统内核领域实现范式收敛，不构成对参考项目的直接抄袭。

---

## 2. 全模块覆盖矩阵解读

### 进程管理
- **双方均存在**：是
- **共同 AST 指纹**：29
- **覆盖率**：目标 5.04%，参考 31.52%
- **解读**：目标项目进程管理模块规模（650 函数）远超参考（112 函数），覆盖率仅 5.04%，说明目标项目在该模块进行了大量独立扩展。参考项目覆盖率 31.52% 较高，反映其保留了更多 ArceOS 上游原始代码。
- **证据强度**：**弱**。共同指纹集中于 `axtask` 等待队列等 ArceOS 框架核心文件，属于共同上游继承。

### 任务调度/异步运行时
- **双方均存在**：是
- **共同 AST 指纹**：130
- **覆盖率**：目标 17.4%，参考 40.5%
- **解读**：参考覆盖率显著高于目标，说明参考项目保留了更多 ArceOS 框架原始调度代码。目标项目进行了较多修改或扩展。
- **证据强度**：**中偏弱**。共同指纹主要来自 `axlibc`（printf、pow 等 C 库函数）和 `axlog` 等框架日志模块，属于第三方库或框架标准实现。

### 内存管理
- **双方均存在**：是
- **共同 AST 指纹**：96
- **覆盖率**：目标 31.27%，参考 31.17%
- **解读**：双方覆盖率接近且均约 31%，说明两项目在内存管理模块保留了相近比例的 ArceOS 上游代码。
- **证据强度**：**中**。共同指纹涉及 `axruntime::rust_main` 初始化流程、PCI 设备配置等框架核心路径，属于共同上游继承。

### 中断/异常处理
- **双方均存在**：是
- **共同 AST 指纹**：2
- **覆盖率**：目标 6.25%，参考 3.51%
- **解读**：覆盖率极低，双方在该模块几乎无重合。目标项目规模（35 函数）远小于参考（71 函数），架构差异显著。
- **证据强度**：**未形成**。共同指纹仅 2 个，且来自 `axlibc` 数学库和 `axsync` 互斥锁测试辅助函数，属于第三方库或测试代码。

### 系统调用
- **双方均存在**：是
- **共同 AST 指纹**：56
- **覆盖率**：目标 25.57%，参考 48.28%
- **解读**：参考覆盖率接近目标两倍，说明参考项目保留了更多 ArceOS POSIX API 原始包装代码。目标项目进行了较多替换或重写。
- **证据强度**：**弱**。共同指纹集中于 `arceos_posix_api` 网络和 IO 系统调用包装函数，均使用 `syscall_body!` 宏，属于框架标准写法。

### 文件系统
- **双方均存在**：是
- **共同 AST 指纹**：211
- **覆盖率**：目标 30.49%，参考 43.51%
- **解读**：参考覆盖率高于目标，反映其保留了更多 ArceOS 文件系统框架代码。
- **证据强度**：**中偏弱**。共同指纹大量来自 `axlibc`（setjmp/longjmp、glob 等）和第三方库（rust-fatfs），属于公共依赖。

### 设备驱动
- **双方均存在**：是
- **共同 AST 指纹**：79
- **覆盖率**：目标 81.44%，参考 62.7%
- **解读**：目标覆盖率极高，但目标项目设备驱动模块规模（105 函数）小于参考（142 函数）。高覆盖率主要因为目标项目的设备驱动代码大部分与参考重合。
- **证据强度**：**中**。共同指纹集中于 `bwbench_client` 网络基准测试工具和 `axdriver::init_drivers` 框架初始化，属于工具代码或框架标准流程。

### 网络协议栈
- **双方均存在**：是
- **共同 AST 指纹**：51
- **覆盖率**：目标 100%，参考 100%
- **解读**：双方网络协议栈模块完全重合，但该模块规模相同（均为 61 函数），且均来自第三方库 `smoltcp` 及其 ArceOS 封装层。
- **证据强度**：**弱（第三方库）**。100% 覆盖率源于共同依赖的 `smoltcp` 第三方库，非两项目原创代码。

### 第三方库/公共依赖
- **文件系统/第三方库**：共同指纹 4，覆盖率目标 0.48%、参考 0.63%。**证据强度：未形成**。
- **网络协议栈/第三方库**：共同指纹 64，覆盖率目标 3.31%、参考 33.16%。**证据强度：弱**。参考覆盖率较高因其保留了更多 `smoltcp` 封装代码。
- **第三方库（仅参考）**：参考项目独有 1155 函数，目标项目为 0。**证据强度：未形成**。若目标抄袭参考，不应缺失此模块。

---

## 3. 底层 AST 关键证据指纹表

| 所属板块 | 目标项目路径 | 参考项目路径 | 证据类型 | 技术说明 |
| :--- | :--- | :--- | :--- | :--- |
| 进程管理 | `arceos/modules/axtask/src/wait_queue.rs#cancel_events` | `AstrancE/modules/axtask/src/wait_queue.rs#cancel_events` | 共同上游继承 | 等待队列取消事件的标准实现：检查标志→清标志→从 VecDeque 移除。任何基于 SpinNoIrq<VecDeque> 的等待队列均会生成相同调用图谱。 |
| 进程管理 | `arceos/modules/axtask/src/run_queue.rs#exit_current` | `AstrancE/modules/axtask/src/run_queue.rs#exit_current` | 共同上游继承（含 HAL 差异） | 目标使用 `axhal::power::system_off`，参考使用 `axhal::misc::terminate`，说明目标已按自身 HAL 适配，非照搬参考。 |
| 进程管理 | `arceos/api/arceos_posix_api/src/imp/task.rs#sys_exit` | `AstrancE/api/arceos_posix_api/src/imp/task.rs#sys_exit` | 框架共性（含 HAL 差异） | 目标调用 `axhal::power::system_off`，参考调用 `axhal::misc::terminate`。关机路径不同，属独立适配。 |
| 进程管理 | `arceos/modules/axfs/src/mounts.rs#procfs` | `AstrancE/modules/axfs/src/mounts.rs#procfs` | 同板块同名但实现不同 | 目标使用 `RamFileSystem::new`，参考使用 `ProcFileSystem::new`。文件系统后端不同，非抄袭。 |
| 进程管理 | `kernel/src/task/mod.rs#new_child` | `AstrancE/modules/axsignal/src/siginfo.rs#new_child` | 跨板块同名（应排除） | 目标创建进程（`Process::new_user`），参考创建信号信息（`SigCode::SigChld`）。语义完全不同。 |
| 进程管理 | `kernel/src/task/mod.rs#parent` | `AstrancE/api/arceos_posix_api/src/imp/path_link.rs#parent` | 跨板块同名（应排除） | 目标取父进程指针（`self.parent.load`），参考解析路径父目录（`path.rfind`）。语义完全不同。 |
| 任务调度/异步运行时 | `arceos/ulib/axlibc/c/printf.c#format_string_loop` | `AstrancE/ulib/axlibc/c/printf.c#format_string_loop` | 第三方库（musl/newlib） | C 标准库 printf 格式化循环，来自开源 C 库，非两项目原创。 |
| 任务调度/异步运行时 | `arceos/modules/axlog/src/lib.rs#log` | `AstrancE/modules/axlog/src/lib.rs#log` | 框架共性 | ArceOS 日志宏标准实现，使用 `cfg_if` 和 `record.level/line/target` 等标准字段。 |
| 内存管理 | `arceos/modules/axruntime/src/lib.rs#rust_main` | `AstrancE/modules/axruntime/src/lib.rs#rust_main` | 共同上游继承 | ArceOS 内核初始化主函数，调用各子系统初始化。调用序列由框架决定。 |
| 内存管理 | `arceos/modules/axdriver/src/bus/pci.rs#config_pci_device` | `AstrancE/modules/axdriver/src/bus/pci.rs#config_pci_device` | 共同上游继承 | PCI 设备配置的标准流程：BAR 分配、中断路由、命令寄存器写入。 |
| 中断/异常处理 | `arceos/ulib/axlibc/c/pow.c#exp_inline` | `AstrancE/ulib/axlibc/c/pow.c#exp_inline` | 第三方库（musl） | 数学库指数函数内联实现，来自开源 musl，非两项目原创。 |
| 系统调用 | `arceos/api/arceos_posix_api/src/imp/net.rs#sys_getaddrinfo` | `AstrancE/api/arceos_posix_api/src/imp/net.rs#sys_getaddrinfo` | 框架共性 | ArceOS POSIX API 系统调用包装，均使用 `syscall_body!` 宏。 |
| 文件系统 | `arceos/ulib/axlibc/src/setjmp.rs#longjmp` | `AstrancE/ulib/axlibc/src/setjmp.rs#longjmp` | 第三方库 | setjmp/longjmp 汇编实现，架构相关，来自开源 C 库。 |
| 文件系统 | `third_party/rust/git/rust-fatfs/src/file.rs#seek` | `AstrancE/crates/lwext4_rust/examples/src/disk.rs#seek` | 第三方库（不同库） | 目标使用 rust-fatfs，参考使用 lwext4_rust。虽函数同名 `seek`，但底层文件系统完全不同。 |
| 设备驱动 | `arceos/tools/bwbench_client/src/main.rs#transmit_benchmark` | `AstrancE/tools/bwbench_client/src/main.rs#transmit_benchmark` | 工具代码 | 网络带宽基准测试工具，使用标准 `NetDevice` 接口和 `Local::now` 计时。 |
| 网络协议栈 | `arceos/ulib/axlibc/c/network.c#inet_pton` | `AstrancE/ulib/axlibc/c/network.c#inet_pton` | 第三方库（musl） | IP 地址转换函数，来自开源 musl C 库。 |
| 网络协议栈 | `arceos/modules/axnet/src/smoltcp_impl/tcp.rs#connect` | `AstrancE/modules/axnet/src/smoltcp_impl/tcp.rs#connect` | 共同上游继承 | smoltcp TCP 连接封装，调用 `SOCKET_SET.add` 和 `poll_interfaces`。框架标准流程。 |
| 第三方库/公共依赖 | `third_party/rust/git/smoltcp/src/storage/assembler.rs#add` | `AstrancE/crates/virtio-drivers/src/device/socket/connectionmanager.rs#add` | 第三方库（不同库） | 目标使用 smoltcp 分片重组，参考使用 virtio-drivers 连接管理。虽函数同名 `add`，但模块完全不同。 |

---

## 4. 核心代码调用流对比剖析

### 【进程管理 / 系统调用退出路径】

- **底层技术析理**: 
  目标项目 `sys_exit` 调用流为 `axhal::power::system_off` → `axtask::exit` → `debug!`，参考项目为 `axhal::misc::terminate` → `axtask::exit` → `debug!`。两者在系统终止的 HAL 抽象层存在本质差异：目标使用 `power::system_off`（电源管理模块的系统关机），参考使用 `misc::terminate`（杂项模块的终止）。这反映了两项目在硬件抽象层设计上的独立决策，非“混淆式复用”。若目标抄袭参考，无必要将核心关机 API 从 `misc::terminate` 替换为 `power::system_off`。

- **代码溯源链接**:
  - 目标项目: `arceos/api/arceos_posix_api/src/imp/task.rs#sys_exit`
  - 参考项目: `AstrancE/api/arceos_posix_api/src/imp/task.rs#sys_exit`

---

### 【进程管理 / procfs 文件系统挂载】

- **底层技术析理**:
  目标项目 `procfs` 使用 `fs::ramfs::RamFileSystem::new` 创建基于内存文件系统的 procfs，并通过 `file_over.write_at`、`file_somaxconn.write_at` 写入具体文件内容。参考项目使用 `ProcFileSystem::new` 创建专用 procfs 文件系统，通过 `proc_root.create_static_file` 创建静态文件。两者在文件系统后端选择（通用 ramfs vs 专用 ProcFileSystem）和文件创建方式上完全不同，属独立设计。

- **代码溯源链接**:
  - 目标项目: `arceos/modules/axfs/src/mounts.rs#procfs`
  - 参考项目: `AstrancE/modules/axfs/src/mounts.rs#procfs`

---

### 【任务调度/异步运行时 / 内核初始化主函数】

- **底层技术析理**:
  双方 `rust_main` 均调用 `axhal::init_early` → `axdriver::init_drivers` → `axfs::init_filesystems` → `axdisplay::init_display` 等初始化序列。这是 ArceOS 框架规定的标准启动流程，调用顺序由框架设计决定。值得注意的是，目标项目调用 `axhal::init_early` 后额外包含 `axhal::power::system_off` 相关逻辑，参考项目则调用 `axhal::mem::memory_regions`，反映了两者在内存管理和电源管理初始化上的差异。

- **代码溯源链接**:
  - 目标项目: `arceos/modules/axruntime/src/lib.rs#rust_main`
  - 参考项目: `AstrancE/modules/axruntime/src/lib.rs#rust_main`

---

### 【内存管理 / PCI 设备配置】

- **底层技术析理**:
  双方 `config_pci_device` 均遵循 PCI 设备配置的标准流程：BAR 空间分配（`allocator.alloc(size).ok_or`）→ 命令寄存器写入（`pci_bar.write`）→ 中断路由配置。这是 PCI 总线规范的必然实现路径。目标项目在 BAR 分配失败处理上使用 `expect("No memory ranges available for PCI BARs!")`，参考项目使用相同的 panic 消息，说明两者均继承自 ArceOS 上游的同一版本驱动框架。

- **代码溯源链接**:
  - 目标项目: `arceos/modules/axdriver/src/bus/pci.rs#config_pci_device`
  - 参考项目: `AstrancE/modules/axdriver/src/bus/pci.rs#config_pci_device`

---

### 【系统调用 / 网络系统调用包装】

- **底层技术析理**:
  双方 `sys_getaddrinfo`、`sys_recvfrom`、`sys_socket` 等网络系统调用均使用 `debug!` → `syscall_body!` 的宏调用模式。`syscall_body!` 是 ArceOS POSIX API 层的标准系统调用入口宏，所有 POSIX 包装函数均遵循此模式。真实业务逻辑在宏展开内部，AST 指纹仅捕获了宏调用名，无法反映实际实现差异。

- **代码溯源链接**:
  - 目标项目: `arceos/api/arceos_posix_api/src/imp/net.rs#sys_getaddrinfo`
  - 参考项目: `AstrancE/api/arceos_posix_api/src/imp/net.rs#sys_getaddrinfo`

---

### 【文件系统 / 第三方库差异】

- **底层技术析理**:
  目标项目 `third_party/rust/git/rust-fatfs/src/file.rs#seek` 使用 Rust FAT 文件系统库实现文件寻址，参考项目 `crates/lwext4_rust/examples/src/disk.rs#seek` 使用 ext4 文件系统库实现磁盘寻址。虽函数同名 `seek`，但底层文件系统（FAT vs ext4）和数据结构完全不同。这是典型的“同名异义”，不能作为抄袭证据。

- **代码溯源链接**:
  - 目标项目: `third_party/rust/git/rust-fatfs/src/file.rs#seek`
  - 参考项目: `AstrancE/crates/lwext4_rust/examples/src/disk.rs#seek`

---

## 5. 单方模块与降权项

### 仅参考项目存在的模块
- **第三方库**（1155 函数）：参考项目独有 `crates/axdriver_crates`、`crates/virtio-drivers` 等第三方驱动库，目标项目无此模块。若目标抄袭参考，不应完全缺失此模块。

### 降权项说明
以下证据因属于跨板块同名、第三方库、框架宏或语言惯用法，已从强同源证据中降权或排除：

| 降权项 | 降权原因 |
| :--- | :--- |
| `kernel/src/task/mod.rs#new_child` vs `axsignal/src/siginfo.rs#new_child` | 跨板块同名，语义完全不同（进程创建 vs 信号信息） |
| `kernel/src/task/mod.rs#parent` vs `api/.../path_link.rs#parent` | 跨板块同名，语义完全不同（父进程指针 vs 路径父目录） |
| `kernel/src/task/mod.rs#set_clear_child_tid` vs `axmono/.../process.rs#set_clear_child_tid` | 跨模块，使用场景不同（kernel 任务管理 vs axmono 用户库） |
| 所有 `unimplemented!` 调用（`pthread_cond_wait`、`execve`、`wait3`、`waitpid`、`fork`） | Rust 标准占位宏，未实现函数的标准写法 |
| 所有 `syscall_body!` 调用（`sys_epoll_wait`、`sys_getpid` 等） | ArceOS 框架宏，系统调用包装的标准写法 |
| 所有 `println!`、`debug!` 调用 | Rust 标准输出宏和日志宏，语言/框架共性 |
| `asm::wfe` | ARM64 标准等待事件指令，硬件规范决定 |
| `NoPreemptIrqSave`、`NoOp` 泛型实例化 | ArceOS 调度器框架类型，框架共性 |
| 网络协议栈 100% 覆盖（`smoltcp`） | 共同依赖的第三方库 |
| `axlibc` C 库函数（printf、pow、string、setjmp 等） | 来自 musl/newlib 等开源 C 库 |
| `bwbench_client` 工具代码 | 网络基准测试工具，标准套接字操作 |

---

## 6. 保守结论

经对目标项目 `[2026-T2026104879910415-XX]` 与参考项目 `[2025-AstranciA-哈尔滨工业大学深圳]` 的底层 AST 调用图谱进行全模块交叉核验，本席认定：**11.65% 的 AST 相似度不构成代码抄袭或违规复用**。

两项目相似性主要源于以下合理因素：（1）**共同上游继承**——双方均基于 ArceOS 开源框架，在 `axtask`、`axruntime`、`axdriver` 等核心模块保留了上游代码；（2）**第三方库共享**——`smoltcp`、`axlibc`（musl）、`rust-fatfs` 等公共依赖导致网络协议栈、C 库等模块出现高重合；（3）**领域实现收敛**——等待队列、PCI 配置、系统调用包装等操作系统基础机制的实现空间有限，调用图谱自然趋同。

关键差异证据包括：`sys_exit` 中 HAL 抽象层选择不同（`power::system_off` vs `misc::terminate`）、`procfs` 文件系统后端不同（`RamFileSystem` vs `ProcFileSystem`）、目标项目独有 `kernel/src/task/mod.rs` 进程管理模块（650 函数 vs 参考 112 函数）等，均指向独立开发路径。目标项目 82.75% 的代码在参考项目中无对应指纹，独立开发特征显著。综上，驳回控方复用指控，认定目标项目原创性合理。

------------------------------

