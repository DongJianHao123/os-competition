# 🛡️ OS 内核赛自动化代码级架构审计报告

**审计时间**: `2026-07-02 03:42:36`
**参赛作品名称**: `2026-T2026102479910691-XX`
**年份**: `2026`
**学校**: `XX`
**队伍名称**: `T2026102479910691`
**比赛名字**: `OS-Competition`
**比赛子赛道**: `Kernel-Implementation`
**仓库地址**: `https://gitlab.eduxiji.net/educg-group-43501-3132633/T2026102479910691-2751`
**推测开发语言**: `Rust / C (由AST探针自动识别)`
**特征函数量**: `81`

## ⚔️ 底层代码同源性对比 (AST Homology)
> 本部分由专门的代码审查智能体，对该项目与符合年份条件的历届最高相似度项目进行控制流与底层调用对比。新版报告会先给出确定性的全模块覆盖矩阵，再给出 Agent 的分模块解释，避免只围绕少量抽样函数展开。

### 🏆 Top 3 底层对标: vs `2023-终结者-FIVE-东北大学`（东北大学 / 终结者-FIVE）

#### 对方作品信息

**对方年份**: `2023`
**对方学校**: `东北大学`
**对方队伍名称**: `终结者-FIVE`
**对方比赛名字**: `OS-Competition`
**对方比赛子赛道**: `Kernel-Implementation`
**对方仓库地址**: `https://gitlab.eduxiji.net/202310145101593/ender-v`

> 双向覆盖率：共同 AST 指纹: 1；唯一 AST 指纹: 目标项目 66，参考项目 44；覆盖率: 目标项目 1.52%，参考项目 2.27%

## 全模块对比总览（确定性证据）

> 总体覆盖率：共同 AST 指纹: 1；唯一 AST 指纹: 目标项目 66，参考项目 44；覆盖率: 目标项目 1.52%，参考项目 2.27%

### 1. 模块覆盖矩阵
| 模块 | 目标函数数 | 参考函数数 | 共同唯一 AST 指纹 | Target 覆盖 | Base 覆盖 | 状态 |
| :--- | ---: | ---: | ---: | ---: | ---: | :--- |
| 进程管理 | 7 | 0 | 0 | 0.0% | 0.0% | 仅目标项目体现 |
| 任务调度/异步运行时 | 8 | 13 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |
| 内存管理 | 20 | 10 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |
| 中断/异常处理 | 28 | 6 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |
| 系统调用 | 3 | 2 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |
| 文件系统 | 1 | 0 | 0 | 0.0% | 0.0% | 仅目标项目体现 |
| 设备驱动 | 7 | 8 | 1 | 14.29% | 12.5% | 双方存在且有结构重合 |
| 网络协议栈 | 0 | 0 | 0 | 0.0% | 0.0% | 双方均未体现 |
| 文件系统/第三方库 | 7 | 0 | 0 | 0.0% | 0.0% | 仅目标项目体现 |
| 通用模块/其他 | 0 | 8 | 0 | 0.0% | 0.0% | 仅参考项目体现 |

### 2. 分模块证据清单

#### 进程管理
仅目标项目中观察到该模块代表函数，参考项目没有同模块可对齐证据：
- 目标项目：`kernel/syscall.c#sys_exit`，调用摘要：uart_puts
- 目标项目：`kernel/proc.c#fd_close`，调用摘要：内部闭环逻辑
- 目标项目：`kernel/proc.c#proc_init`，调用摘要：uart_puts
- 目标项目：`kernel/syscall.c#sys_getppid`，调用摘要：内部闭环逻辑

#### 任务调度/异步运行时
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `kernel/syscall.c#syscall_init_table` | `printf.c#_vsnprintf` | 同模块上下文对齐/差异观察 | 内部闭环逻辑 | va_arg |
| `kernel/main.c#kmain` | `trap.c#trap_handler` | 同模块上下文对齐/差异观察 | blk_dev_ready, ext4_init, ext4_scan_testcode, mm_init, proc_init, run_test ...(+4) | do_syscall, external_interrupt_handler, panic, printf, r_sepc, r_sip ...(+5) |
| `kernel/syscall.c#sys_read` | `timer.c#timer_create` | 同模块上下文对齐/差异观察 | fd_get | spin_lock, spin_unlock |
| `kernel/syscall.c#sys_dup3_impl` | `user.c#user_task0` | 同模块上下文对齐/差异观察 | fd_close, fd_get | gethid, printf, task_delay, uart_puts |

#### 内存管理
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `kernel/virtio.c#virtio_init` | `page.c#page_alloc` | 同模块上下文对齐/差异观察 | R, kalloc_zero, uart_putc, uart_puts | _is_free, _set_flag |
| `kernel/elf.c#elf_load` | `page.c#page_init` | 同模块上下文对齐/差异观察 | kalloc_zero, uart_putc, uart_puts, uvm_map | _align_page, _clear, printf |
| `kernel/virtio.c#virtio_read` | `kernel.c#start_kernel` | 同模块上下文对齐/差异观察 | (uint64), R, alloc_desc, free_chain | os_main, page_init, plic_init, printf, r_sstatus, sbi_shutdown ...(+6) |
| `kernel/main.c#run_test` | `page.c#page_free` | 同模块上下文对齐/差异观察 | get_kernel_pgtbl, kalloc_page, proc_alloc, uart_puts, uvm_create, uvm_map | _clear, _is_free, _is_last |

#### 中断/异常处理
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `kernel/trap.c#trap_init` | `trap.c#trap_init` | 同板块同名 | uart_puts, w_stvec | w_stvec |
| `kernel/trap.c#trap_handler` | `plic.c#plic_init` | 同模块上下文对齐/差异观察 | r_scause, syscall_handle, uart_putc, uart_puts | PLIC_PRIORITY, PLIC_SENABLE, PLIC_STHRESHOLD, printf, r_sie, r_tp ...(+1) |
| `kernel/syscall.c#syscall_handle` | `trap.c#trap_test` | 同模块上下文对齐/差异观察 | syscall_init_table, syscall_table[nr], uart_putc, uart_puts | uart_puts |
| `kernel/syscall.c#sys_uname` | `trap.c#external_interrupt_handler` | 同模块上下文对齐/差异观察 | str_cpy | plic_claim, plic_complete, printf, uart_isr |

#### 系统调用
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `kernel/syscall.c#str_eq` | `timer.c#timer_load` | 同模块上下文对齐/差异观察 | 内部闭环逻辑 | r_mtime |
| `kernel/syscall.c#str_len` | `syscall.c#sys_gethid` | 同模块上下文对齐/差异观察 | 内部闭环逻辑 | printf, r_tp |

#### 文件系统
仅目标项目中观察到该模块代表函数，参考项目没有同模块可对齐证据：
- 目标项目：`kernel/syscall.c#syscall_register_file`，调用摘要：内部闭环逻辑

#### 设备驱动
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `kernel/uart.c#uart_putc` | `uart.c#uart_putc` | 同板块同名 | 内部闭环逻辑 | uart_read_reg, uart_write_reg |
| `kernel/uart.c#uart_init` | `uart.c#uart_init` | 同板块同名 | 内部闭环逻辑 | uart_read_reg, uart_write_reg |
| `kernel/uart.c#uart_puts` | `uart.c#uart_puts` | 同板块同名同结构 | uart_putc | uart_putc |
| `kernel/mm.c#mm_init` | `printf.c#_vprintf` | 同模块上下文对齐/差异观察 | uart_putc, uart_puts | _vsnprintf, uart_puts |

#### 文件系统/第三方库
仅目标项目中观察到该模块代表函数，参考项目没有同模块可对齐证据：
- 目标项目：`kernel/ext4.c#ext4_scan_testcode`，调用摘要：aligned, read_extent, read_inode
- 目标项目：`kernel/ext4.c#ext4_open`，调用摘要：aligned, kalloc_page, read_extent, read_inode
- 目标项目：`kernel/ext4.c#read_extent`，调用摘要：read_block
- 目标项目：`kernel/ext4.c#ext4_init`，调用摘要：aligned, uart_putc, uart_puts, virtio_read

#### 通用模块/其他
仅参考项目中观察到该模块代表函数，目标项目没有同模块可对齐证据：
- 参考项目：`timer.c#timer_init`，调用摘要：r_sie, timer_load, w_sie
- 参考项目：`timer.c#timer_check`，调用摘要：t->func
- 参考项目：`timer.c#timer_delete`，调用摘要：spin_lock, spin_unlock
- 参考项目：`printf.c#printf`，调用摘要：_vprintf, va_end, va_start


好的，审计官。基于提供的证据底座、控辩双方主张及客观AST数据，我将进行最终同源性定性仲裁。

### 1. 宏观判定
- **定性结论**: **独立开发**
- **判定依据**: 客观AST相似度仅为0.92%，共同AST指纹仅有1个，且该指纹位于高度通用的设备驱动基础函数中。双方在进程管理、内存管理、文件系统等核心子系统上存在根本性的架构差异，无任何结构性复用痕迹。控方指控主要基于领域通用命名和标准硬件操作流程，缺乏底层代码结构复用的客观证据。

### 2. 全模块覆盖矩阵解读
- **进程管理**
  - 双方存在性: 仅目标项目体现。
  - 共同AST指纹: 0。
  - 覆盖率解读: 目标项目有7个函数，参考项目为0。此模块为单方特有，是架构差异的核心体现。
  - 证据强度: **未形成**。

- **任务调度/异步运行时**
  - 双方存在性: 双方均存在。
  - 共同AST指纹: 0。
  - 覆盖率解读: 目标项目8个函数，参考项目13个函数，但无任何结构重合。目标项目围绕POSIX系统调用（`sys_read`, `sys_sched_yield`）构建，参考项目是协作式任务模型（`task_create`, `task_yield`），调度哲学完全不同。
  - 证据强度: **弱**。

- **内存管理**
  - 双方存在性: 双方均存在。
  - 共同AST指纹: 0。
  - 覆盖率解读: 目标项目20个函数，参考项目10个函数，零重合。目标项目实现了完整的虚拟内存管理（`uvm_create`, `uvm_map`, `walk_pgtbl`），而参考项目仅为物理页帧分配器（`page_alloc`, `_set_flag`），抽象层次天差地别。
  - 证据强度: **弱**。

- **中断/异常处理**
  - 双方存在性: 双方均存在。
  - 共同AST指纹: 0。
  - 覆盖率解读: 目标项目28个函数，参考项目6个函数，零重合。目标项目的`trap_handler`是精简的系统调用分发器，而参考项目包含完整的外部中断处理（`plic_claim`, `external_interrupt_handler`）和调度逻辑，处理流程和复杂度不同。
  - 证据强度: **弱**。

- **系统调用**
  - 双方存在性: 双方均存在。
  - 共同AST指纹: 0。
  - 覆盖率解读: 目标项目实现了丰富的POSIX风格调用（`sys_read`, `sys_mmap`等），参考项目仅有`sys_gethid`。两者在系统调用层的功能范围和实现复杂度上无对标性。
  - 证据强度: **未形成**。

- **文件系统**
  - 双方存在性: 仅目标项目体现。
  - 共同AST指纹: 0。
  - 覆盖率解读: 目标项目包含`ext4_init`, `ext4_open`等7个函数，实现了ext4文件系统解析。参考项目无此模块。这是证明独立开发的最强架构差异点之一。
  - 证据强度: **未形成**。

- **设备驱动**
  - 双方存在性: 双方均存在。
  - 共同AST指纹: 1。
  - 覆盖率解读: 目标项目7个函数，参考项目8个函数，存在唯一共同指纹。该指纹位于`uart_puts`函数，其调用`uart_putc`的实现是串口驱动字符串输出的通用范式。
  - 证据强度: **中**。

- **网络协议栈**
  - 双方存在性: 双方均未体现。
  - 共同AST指纹: 0。
  - 覆盖率解读: 无。
  - 证据强度: **未形成**。

- **第三方库/公共依赖**
  - 双方存在性: 仅目标项目体现（文件系统/第三方库），仅参考项目体现（通用模块/其他）。
  - 共同AST指纹: 0。
  - 覆盖率解读: 目标项目的`ext4`相关函数和参考项目的`printf`库均为单方持有，无交叉重合。
  - 证据强度: **未形成**。

### 3. 底层 AST 关键证据指纹表
| 所属板块 | 目标项目路径 | 参考项目路径 | 证据类型 | 技术说明 |
| :--- | :--- | :--- | :--- | :--- |
| 设备驱动 | `[kernel/uart.c]` 函数 `uart_puts` | `[uart.c]` 函数 `uart_puts` | 通用惯用法 | 双方均调用`uart_putc`实现字符串输出，这是串口驱动的标准、最小实现模式，属于领域内必然收敛的通用逻辑，非创造性表达。 |
| 中断/异常处理 | `[kernel/trap.c]` 函数 `trap_init` | `[trap.c]` 函数 `trap_init` | 硬件规范命名 | 函数名和调用`w_stvec`的行为由RISC-V架构规范决定，是设置陷阱向量的标准步骤，同名同调用链是硬件接口的必然要求。 |
| 设备驱动 | `[kernel/uart.c]` 函数 `uart_init` | `[uart.c]` 函数 `uart_init` | 硬件规范命名 | 函数名由UART硬件功能决定。目标项目为“内部闭环逻辑”，参考项目调用`uart_read/write_reg`，实现路径不同。 |
| 设备驱动 | `[kernel/uart.c]` 函数 `uart_putc` | `[uart.c]` 函数 `uart_putc` | 硬件规范命名 | 函数名是UART驱动的标准命名。目标项目为“内部闭环逻辑”，参考项目依赖寄存器读写函数，实现策略存在差异。 |

### 4. 核心代码调用流对比剖析
* **【中断/异常处理】**
* **底层技术析理**: 目标项目的`trap_handler`是一个精简的异常分发器，读取`scause`后直接交由`syscall_handle`处理，调用流为`r_scause -> syscall_handle`。参考项目的`trap_handler`则是一个功能更全面的中断/异常处理中心，其调用流涉及`do_syscall`、`external_interrupt_handler`、`schedule`、`panic`等多个分支，处理外部中断和任务调度。两者在控制流复杂度和功能边界上存在显著差异。
* **代码溯源链接**:
  - 目标项目: `kernel/trap.c#trap_handler`
  - 参考项目: `trap.c#trap_handler`

* **【内存管理】**
* **底层技术析理**: 目标项目采用基于页表的虚拟内存模型，`elf_load`函数的调用流为`kalloc_zero -> uvm_map`，将ELF段映射到用户地址空间。参考项目采用物理页帧分配模型，`page_alloc`函数的调用流为`_is_free -> _set_flag`，操作的是物理内存位图。两者在内存抽象层次（虚拟内存 vs. 物理内存）和核心操作对象（页表项 vs. 页帧标志位）上完全不同。
* **代码溯源链接**:
  - 目标项目: `kernel/elf.c#elf_load`
  - 参考项目: `page.c#page_alloc`

* **【任务调度/异步运行时】**
* **底层技术析理**: 目标项目的`syscall_init_table`为“内部闭环逻辑”，其功能是初始化一个系统调用分发表，服务于`syscall_handle`。参考项目的`task_create`也为“内部闭环逻辑”，其功能是创建一个协作式任务结构体。两者在调度模型（系统调用分发 vs. 任务创建）和数据结构上无对标性。
* **代码溯源链接**:
  - 目标项目: `kernel/syscall.c#syscall_init_table`
  - 参考项目: `sched.c#task_create`

### 5. 单方模块与降权项
- **单方模块**: 目标项目的【进程管理】（`proc_alloc`, `fd_get`等）、【文件系统】（`ext4_open`, `read_extent`等）和【文件系统/第三方库】模块，以及参考项目的【通用模块/其他】（`timer_create`, `printf`等）均为各自项目独有。这些模块的存在是证明双方架构设计独立、解决不同问题的强证据，不能作为同源证据。
- **降权项**: 控方提出的`kmain`与`start_kernel`的初始化流程相似性，属于跨板块（任务调度 vs. 内存管理）的宏观架构对比，且调用对象完全不同，已按规则降级为弱信号，不构成同板块主证据。`uart_puts`、`trap_init`等函数名及调用`w_stvec`、`uart_putc`的行为，属于RISC-V内核开发中的标准命名和硬件规范操作，是领域内高度可预期的收敛现象，其证据价值已被降权。

### 6. 保守结论
本次审计的客观AST相似度极低（0.92%），唯一的共同指纹是设备驱动中`uart_puts`这一通用惯用法。控方指控主要建立在硬件规范决定的函数同名和标准操作流程之上，缺乏底层代码结构复用的实质性证据。相反，目标项目在进程模型、虚拟内存、文件系统等核心子系统上展现出与参考项目完全不同的架构设计和实现复杂度。这些根本性差异无法用“重命名”或“精简”来解释，强烈指向独立开发。综合判定，抄袭指控不成立，整体风险极低。

------------------------------

