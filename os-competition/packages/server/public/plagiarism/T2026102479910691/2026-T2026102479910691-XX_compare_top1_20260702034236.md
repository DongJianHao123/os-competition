# 🛡️ OS 内核赛自动化代码级架构审计报告

**审计时间**: `2026-07-02 03:42:36`
**参赛作品名称**: `2026-T2026102479910691-XX`
**年份**: `2026`
**学校**: `XX`
**队伍名称**: `T2026102479910691`
**比赛名字**: `OS-Competition`
**比赛子赛道**: `Kernel-Implementation`
**仓库地址**: `https://gitlab.eduxiji.net/educg-group-43501-3132633/T2026102479910691-2751`
**推测开发语言**: `Rust / C (由AST探针自动识别)`
**特征函数量**: `81`

## ⚔️ 底层代码同源性对比 (AST Homology)
> 本部分由专门的代码审查智能体，对该项目与符合年份条件的历届最高相似度项目进行控制流与底层调用对比。新版报告会先给出确定性的全模块覆盖矩阵，再给出 Agent 的分模块解释，避免只围绕少量抽样函数展开。

### 🏆 Top 1 底层对标: vs `2022-w以忽略所有警告-南京航空航天大学`（南京航空航天大学 / -w以忽略所有警告）

#### 对方作品信息

**对方年份**: `2022`
**对方学校**: `南京航空航天大学`
**对方队伍名称**: `-w以忽略所有警告`
**对方比赛名字**: `OS-Competition`
**对方比赛子赛道**: `Kernel-Implementation`
**对方仓库地址**: `https://gitlab.eduxiji.net/qianpinyi/pal_operatingsystem`

> 双向覆盖率：共同 AST 指纹: 2；唯一 AST 指纹: 目标项目 66，参考项目 64；覆盖率: 目标项目 3.03%，参考项目 3.12%

## 全模块对比总览（确定性证据）

> 总体覆盖率：共同 AST 指纹: 2；唯一 AST 指纹: 目标项目 66，参考项目 64；覆盖率: 目标项目 3.03%，参考项目 3.12%

### 1. 模块覆盖矩阵
| 模块 | 目标函数数 | 参考函数数 | 共同唯一 AST 指纹 | Target 覆盖 | Base 覆盖 | 状态 |
| :--- | ---: | ---: | ---: | ---: | ---: | :--- |
| 进程管理 | 7 | 1 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |
| 任务调度/异步运行时 | 8 | 12 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |
| 内存管理 | 20 | 6 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |
| 中断/异常处理 | 28 | 11 | 1 | 6.25% | 10.0% | 双方存在且有结构重合 |
| 系统调用 | 3 | 0 | 0 | 0.0% | 0.0% | 仅目标项目体现 |
| 文件系统 | 1 | 0 | 0 | 0.0% | 0.0% | 仅目标项目体现 |
| 设备驱动 | 7 | 47 | 1 | 14.29% | 2.78% | 双方存在且有结构重合 |
| 网络协议栈 | 0 | 0 | 0 | 0.0% | 0.0% | 双方均未体现 |
| 文件系统/第三方库 | 7 | 0 | 0 | 0.0% | 0.0% | 仅目标项目体现 |

### 2. 分模块证据清单

#### 进程管理
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `kernel/syscall.c#sys_exit` | `Kernel/HAL/Drivers/_dmac.c#dmac_wait_idle` | 同模块上下文对齐/差异观察 | uart_puts | dmacSemWait, dmac_is_idle |

#### 任务调度/异步运行时
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `kernel/syscall.c#syscall_init_table` | `Kernel/HAL/Drivers/_sysctl.c#sysctl_clock_device_en` | 同模块上下文对齐/差异观察 | 内部闭环逻辑 | 内部闭环逻辑 |
| `kernel/main.c#kmain` | `Kernel/HAL/Drivers/_spi.c#spi_receive_data_standard` | 同模块上下文对齐/差异观察 | blk_dev_ready, ext4_init, ext4_scan_testcode, mm_init, proc_init, run_test ...(+4) | (uint32), spi_get_frame_size, spi_set_tmod |
| `kernel/syscall.c#sys_read` | `Kernel/HAL/Drivers/_spi.c#spi_send_data_normal` | 同模块上下文对齐/差异观察 | fd_get | memmove, spi_get_frame_size, spi_set_tmod |
| `kernel/syscall.c#sys_dup3_impl` | `Kernel/HAL/Drivers/_sysctl.c#sysctl_clock_bus_en` | 同模块上下文对齐/差异观察 | fd_close, fd_get | 内部闭环逻辑 |

#### 内存管理
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `kernel/virtio.c#virtio_init` | `Kernel/HAL/Drivers/_spi.c#spi_receive_data_standard_dma` | 同模块上下文对齐/差异观察 | R, kalloc_zero, uart_putc, uart_puts | kalloc, kfree, spi_get_frame_size, spi_receive_data_normal_dma |
| `kernel/elf.c#elf_load` | `Kernel/HAL/Drivers/_spi.c#spi_send_data_standard_dma` | 同模块上下文对齐/差异观察 | kalloc_zero, uart_putc, uart_puts, uvm_map | kalloc, kfree, spi_get_frame_size, spi_send_data_normal_dma |
| `kernel/virtio.c#virtio_read` | `Kernel/HAL/Drivers/_spi.c#spi_init` | 同模块上下文对齐/差异观察 | (uint64), R, alloc_desc, free_chain | 内部闭环逻辑 |
| `kernel/main.c#run_test` | `Kernel/HAL/Drivers/_spi.c#spi_send_data_normal_dma` | 同模块上下文对齐/差异观察 | get_kernel_pgtbl, kalloc_page, proc_alloc, uart_puts, uvm_create, uvm_map | dmac_set_single_mode, dmac_wait_done, kalloc, kfree, spi_set_tmod, sysctl_dma_select |

#### 中断/异常处理
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `kernel/syscall.c#sys_gettimeofday` | `Kernel/HAL/Drivers/_sdcard.c#DiskInterruptSolve` | 同板块同结构 | 内部闭环逻辑 | 内部闭环逻辑 |
| `kernel/trap.c#trap_handler` | `Kernel/HAL/Drivers/_plic.c#plicinithart` | 同模块上下文对齐/差异观察 | r_scause, syscall_handle, uart_putc, uart_puts | PLIC_MENABLE, PLIC_SENABLE, PLIC_SPRIORITY, cpuid, printf, readd |
| `kernel/syscall.c#syscall_handle` | `Kernel/HAL/Drivers/_dmac.c#dmac_enable_common_interrupt_signal` | 同模块上下文对齐/差异观察 | syscall_init_table, syscall_table[nr], uart_putc, uart_puts | readq, writeq |
| `kernel/syscall.c#sys_uname` | `Kernel/HAL/Drivers/_dmac.c#dmac_enable_common_interrupt_status` | 同模块上下文对齐/差异观察 | str_cpy | readq, writeq |

#### 系统调用
仅目标项目中观察到该模块代表函数，参考项目没有同模块可对齐证据：
- 目标项目：`kernel/syscall.c#str_eq`，调用摘要：内部闭环逻辑
- 目标项目：`kernel/syscall.c#str_len`，调用摘要：内部闭环逻辑
- 目标项目：`kernel/syscall.c#str_cpy`，调用摘要：内部闭环逻辑

#### 文件系统
仅目标项目中观察到该模块代表函数，参考项目没有同模块可对齐证据：
- 目标项目：`kernel/syscall.c#syscall_register_file`，调用摘要：内部闭环逻辑

#### 设备驱动
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `kernel/uart.c#uart_init` | `Kernel/HAL/Drivers/_sdcard.c#SD_HIGH_SPEED_ENABLE` | 同板块同结构 | 内部闭环逻辑 | 内部闭环逻辑 |
| `kernel/mm.c#mm_init` | `Kernel/HAL/Drivers/_dmac.c#dmac_set_channel_param` | 同模块上下文对齐/差异观察 | uart_putc, uart_puts | is_memory, readq, writeq |
| `kernel/virtio.c#free_chain` | `Kernel/HAL/Drivers/_sdcard.c#sdcard_write_sector` | 同模块上下文对齐/差异观察 | 内部闭环逻辑 | sd_end_cmd, sd_get_response_R1, sd_read_data, sd_send_cmd, sd_write_data, semSDCard->Signal ...(+1) |
| `kernel/uart.c#uart_putc` | `Kernel/HAL/Drivers/_spi.c#spi_receive_data_normal_dma` | 同模块上下文对齐/差异观察 | 内部闭环逻辑 | (uint32), dmac_set_single_mode, dmac_wait_done, spi_get_frame_format, spi_set_tmod, sysctl_dma_select |

#### 文件系统/第三方库
仅目标项目中观察到该模块代表函数，参考项目没有同模块可对齐证据：
- 目标项目：`kernel/ext4.c#ext4_scan_testcode`，调用摘要：aligned, read_extent, read_inode
- 目标项目：`kernel/ext4.c#ext4_open`，调用摘要：aligned, kalloc_page, read_extent, read_inode
- 目标项目：`kernel/ext4.c#read_extent`，调用摘要：read_block
- 目标项目：`kernel/ext4.c#ext4_init`，调用摘要：aligned, uart_putc, uart_puts, virtio_read


好的，作为操作系统内核代码同源性审计专家，我将基于提供的客观AST证据、控辩双方陈述及原始抽样数据，进行最终定性仲裁。

### 1. 宏观判定
- **定性结论**: **底层架构显著差异 / 独立开发**
- **相似度**: 1.56%
- **共同 AST 指纹**: 2
- **唯一 AST 指纹**: 目标项目 66，参考项目 64
- **覆盖率**: 目标项目 3.03%，参考项目 3.12%

### 2. 全模块覆盖矩阵解读
- **进程管理**
  - 双方存在情况：双方均存在。
  - 共同 AST 指纹：0。
  - 覆盖率解读：目标覆盖率 0.0%，参考覆盖率 0.0%。双方虽被归入同一模块，但未发现任何共同结构指纹。
  - 证据强度：**未形成**。目标项目的 `sys_exit` 与参考项目的 `dmac_wait_idle` 在调用图谱上无任何交集。

- **任务调度/异步运行时**
  - 双方存在情况：双方均存在。
  - 共同 AST 指纹：0。
  - 覆盖率解读：目标覆盖率 0.0%，参考覆盖率 0.0%。目标项目为POSIX系统调用与进程调度，参考项目为外设时钟与DMA通道使能。
  - 证据强度：**未形成**。所有对齐证据均属于跨领域硬凑，无底层逻辑复用。

- **内存管理**
  - 双方存在情况：双方均存在。
  - 共同 AST 指纹：0。
  - 覆盖率解读：目标覆盖率 0.0%，参考覆盖率 0.0%。目标项目涉及页表映射、ELF加载，参考项目仅涉及SPI DMA传输的临时缓冲区分配。
  - 证据强度：**未形成**。`kalloc` 等命名重合属于公共教学范式残留，AST调用树结构完全不同。

- **中断/异常处理**
  - 双方存在情况：双方均存在。
  - 共同 AST 指纹：1。
  - 覆盖率解读：目标覆盖率 6.25%，参考覆盖率 10.0%。唯一的共同指纹是“内部闭环逻辑”。
  - 证据强度：**弱**。共同指纹 `sys_gettimeofday` 与 `DiskInterruptSolve` 均为无外部调用的退化结构，且语义领域完全不同，不构成抄袭证据。

- **系统调用**
  - 双方存在情况：仅目标项目体现。
  - 共同 AST 指纹：0。
  - 覆盖率解读：目标覆盖率 0.0%，参考覆盖率 0.0%。
  - 证据强度：**未形成**。参考项目完全缺失此模块，是两项目架构根本性差异的力证。

- **文件系统**
  - 双方存在情况：仅目标项目体现。
  - 共同 AST 指纹：0。
  - 覆盖率解读：目标覆盖率 0.0%，参考覆盖率 0.0%。
  - 证据强度：**未形成**。参考项目完全缺失此模块。

- **设备驱动**
  - 双方存在情况：双方均存在。
  - 共同 AST 指纹：1。
  - 覆盖率解读：目标覆盖率 14.29%，参考覆盖率 2.78%。唯一的共同指纹是“内部闭环逻辑”。
  - 证据强度：**弱**。共同指纹 `uart_init` 与 `SD_HIGH_SPEED_ENABLE` 为退化结构，且操作的外设（UART vs SD卡）完全不同。

- **网络协议栈**
  - 双方存在情况：双方均未体现。
  - 共同 AST 指纹：0。
  - 覆盖率解读：目标覆盖率 0.0%，参考覆盖率 0.0%。
  - 证据强度：**未形成**。

- **第三方库/公共依赖**
  - 双方存在情况：仅目标项目体现（ext4）。
  - 共同 AST 指纹：0。
  - 覆盖率解读：目标覆盖率 0.0%，参考覆盖率 0.0%。
  - 证据强度：**未形成**。参考项目完全缺失此模块。

### 3. 底层 AST 关键证据指纹表
| 所属板块 | 目标项目路径 | 参考项目路径 | 证据类型 | 技术说明 |
| :--- | :--- | :--- | :--- | :--- |
| 中断/异常处理 | `kernel/syscall.c#sys_gettimeofday` | `Kernel/HAL/Drivers/_sdcard.c#DiskInterruptSolve` | 同板块同结构 | 两者均为“内部闭环逻辑”的退化AST结构。目标为POSIX时间系统调用，参考为SD卡中断处理，语义无交集。 |
| 设备驱动 | `kernel/uart.c#uart_init` | `Kernel/HAL/Drivers/_sdcard.c#SD_HIGH_SPEED_ENABLE` | 同板块同结构 | 两者均为“内部闭环逻辑”的退化AST结构。目标为UART初始化，参考为SD卡高速模式使能，外设对象完全不同。 |

### 4. 核心代码调用流对比剖析
* **【中断/异常处理】**
* **底层技术析理**: 目标项目采用RISC-V特权级Trap处理模型，通过读取 `scause` 寄存器分发异常，核心路径为 `trap_handler` -> `syscall_handle` -> `syscall_table[nr]`，依赖 `w_stvec` 设置入口。参考项目则采用外设中断控制器（PLIC）模型，核心路径为 `plic_claim` -> `plic_complete`，中断处理函数如 `dmac_intr` 直接操作DMAC寄存器。两者在中断源识别、分发机制、上下文管理上属于完全不同的架构层级。
* **代码溯源链接**:
  - 目标项目: `kernel/trap.c#trap_handler`
  - 参考项目: `Kernel/HAL/Drivers/_plic.c#plicinithart`

* **【设备驱动】**
* **底层技术析理**: 目标项目的设备驱动围绕VirtIO虚拟化框架，如 `virtio_read` 通过 `alloc_desc` / `free_chain` 管理虚拟队列描述符。参考项目则直接操作物理外设，如 `sdcard_write_sector` 通过 `sd_send_cmd` / `sd_read_data` 等函数实现SD卡协议交互，并使用 `semSDCard` 信号量同步。两者的I/O模型（虚拟队列 vs. 物理总线协议）和同步策略（轮询/中断 vs. 信号量）截然不同。
* **代码溯源链接**:
  - 目标项目: `kernel/virtio.c#virtio_read`
  - 参考项目: `Kernel/HAL/Drivers/_sdcard.c#sdcard_write_sector`

### 5. 单方模块与降权项
- **单方模块**: **系统调用、文件系统、文件系统/第三方库（ext4）** 仅在目标项目中出现。这证明目标项目是一个功能完整的宏内核，而参考项目是一个缺乏这些核心组件的板级支持包（BSP），二者功能集合互补，是独立开发的强有力证据。
- **降权项**: 控辩双方均提及的 `kalloc`/`kfree` 命名重合及 `uart_puts`/`printf` 日志输出范式，已被降级为弱信号。这些属于操作系统开发中的通用范式或公共教学框架（如xv6）遗留，不具备特异性，不能作为同源证据。

### 6. 保守结论
本次审计的客观AST相似度仅为1.56%，处于背景噪声水平。两个项目在架构定位上存在根本性差异：目标项目为类Unix/RISC-V宏内核，核心是系统调用、文件系统和虚拟内存；参考项目为K210微控制器的HAL/BSP，核心是SPI、DMAC、SD卡等物理外设驱动。全项目仅有的2个共同AST指纹均为“内部闭环逻辑”的退化结构，且语义完全不同，应予排除。所有同板块对齐证据均被证实为跨领域硬凑。综合判定，目标项目 `[2026-T2026102479910691-XX]` 与参考项目 `[2022-w以忽略所有警告-南京航空航天大学]` 不存在可认定的代码抄袭或同源性复用，应认定为独立开发。

------------------------------

