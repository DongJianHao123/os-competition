# 🛡️ OS 内核赛自动化代码级架构审计报告

**审计时间**: `2026-07-02 03:42:36`
**参赛作品名称**: `2026-T2026102479910691-XX`
**年份**: `2026`
**学校**: `XX`
**队伍名称**: `T2026102479910691`
**比赛名字**: `OS-Competition`
**比赛子赛道**: `Kernel-Implementation`
**仓库地址**: `https://gitlab.eduxiji.net/educg-group-43501-3132633/T2026102479910691-2751`
**推测开发语言**: `Rust / C (由AST探针自动识别)`
**特征函数量**: `81`

## ⚔️ 底层代码同源性对比 (AST Homology)
> 本部分由专门的代码审查智能体，对该项目与符合年份条件的历届最高相似度项目进行控制流与底层调用对比。新版报告会先给出确定性的全模块覆盖矩阵，再给出 Agent 的分模块解释，避免只围绕少量抽样函数展开。

### 🏆 Top 2 底层对标: vs `2023-接下来我要表扬一款游戏-广东工业大学`（广东工业大学 / 接下来我要表扬一款游戏）

#### 对方作品信息

**对方年份**: `2023`
**对方学校**: `广东工业大学`
**对方队伍名称**: `接下来我要表扬一款游戏`
**对方比赛名字**: `OS-Competition`
**对方比赛子赛道**: `Kernel-Implementation`
**对方仓库地址**: `https://gitlab.eduxiji.net/202311845101096/oskernel2023-gdut`

> 双向覆盖率：共同 AST 指纹: 3；唯一 AST 指纹: 目标项目 66，参考项目 179；覆盖率: 目标项目 4.55%，参考项目 1.68%

## 全模块对比总览（确定性证据）

> 总体覆盖率：共同 AST 指纹: 3；唯一 AST 指纹: 目标项目 66，参考项目 179；覆盖率: 目标项目 4.55%，参考项目 1.68%

### 1. 模块覆盖矩阵
| 模块 | 目标函数数 | 参考函数数 | 共同唯一 AST 指纹 | Target 覆盖 | Base 覆盖 | 状态 |
| :--- | ---: | ---: | ---: | ---: | ---: | :--- |
| 进程管理 | 7 | 29 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |
| 任务调度/异步运行时 | 8 | 21 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |
| 内存管理 | 20 | 67 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |
| 中断/异常处理 | 28 | 20 | 1 | 6.25% | 5.0% | 双方存在且有结构重合 |
| 系统调用 | 3 | 12 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |
| 文件系统 | 1 | 49 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |
| 设备驱动 | 7 | 7 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |
| 网络协议栈 | 0 | 0 | 0 | 0.0% | 0.0% | 双方均未体现 |
| 文件系统/第三方库 | 7 | 0 | 0 | 0.0% | 0.0% | 仅目标项目体现 |
| 通用模块/其他 | 0 | 11 | 0 | 0.0% | 0.0% | 仅参考项目体现 |

### 2. 分模块证据清单

#### 进程管理
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `kernel/syscall.c#sys_exit` | `kernel/process/fork.c#copy_thread` | 同模块上下文对齐/差异观察 | uart_puts | get_current_task, get_pt_reg, memset |
| `kernel/proc.c#fd_close` | `kernel/process/schedule.c#__schedule` | 同模块上下文对齐/差异观察 | 内部闭环逻辑 | get_current_task, irq_disable, schedule_debug, schedule_tail, simple_sched_class.dequeue_task, simple_sched_class.pick_next_task ...(+1) |
| `kernel/proc.c#proc_init` | `kernel/process/schedule.c#pick_next_task_simple` | 同模块上下文对齐/差异观察 | uart_puts | container_of, reset_score |
| `kernel/syscall.c#sys_getppid` | `kernel/process/spinlock.c#spin_lock` | 同模块上下文对齐/差异观察 | 内部闭环逻辑 | csr_clr, csr_set |

#### 任务调度/异步运行时
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `kernel/syscall.c#syscall_init_table` | `lib/printk.c#vsprintf` | 同模块上下文对齐/差异观察 | 内部闭环逻辑 | is_digit, number, skip_atoi, strlen, va_arg |
| `kernel/main.c#kmain` | `kernel/driver/virtio.c#virtio_dev_init` | 同模块上下文对齐/差异观察 | blk_dev_ready, ext4_init, ext4_scan_testcode, mm_init, proc_init, run_test ...(+4) | printk, readword, refresh_cache, virtio_blk_init_legacy, writeword |
| `kernel/syscall.c#sys_read` | `kernel/fs/loader.c#initElf` | 同模块上下文对齐/差异观察 | fd_get | LOADER_SEEK_SET, filp->f_ops->lseek, filp->f_ops->read, get_current_task, memcpy, printk ...(+1) |
| `kernel/syscall.c#sys_dup3_impl` | `kernel/trap/trap.c#do_exception` | 同模块上下文对齐/差异观察 | fd_close, fd_get | do_trap_error, handle_plic_irq, handle_timer, irq_enable, is_interrupt_fault, printk ...(+1) |

#### 内存管理
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `kernel/virtio.c#virtio_init` | `kernel/driver/virtio.c#virtio_init` | 同板块同名 | R, kalloc_zero, uart_putc, uart_puts | virtio_dev_init |
| `kernel/virtio.c#alloc_desc` | `kernel/driver/virtio.c#alloc_desc` | 同板块同名 | 内部闭环逻辑 | 内部闭环逻辑 |
| `kernel/elf.c#elf_load` | `kernel/fs/fat32.c#lookup_test` | 同模块上下文对齐/差异观察 | kalloc_zero, uart_putc, uart_puts, uvm_map | alloc_pgtable, bread, brelease, printk, read_FAT_Entry_test |
| `kernel/virtio.c#virtio_read` | `kernel/main.c#user_main` | 同模块上下文对齐/差异观察 | (uint64), R, alloc_desc, free_chain | malloc, memset, parse_command, print, printf |

#### 中断/异常处理
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `kernel/trap.c#trap_init` | `kernel/trap/trap.c#trap_init` | 同板块同名 | uart_puts, w_stvec | printk, read_csr, write_csr |
| `kernel/syscall.c#sys_gettimeofday` | `kernel/trap/syscall.c#callback_sys_openat` | 同板块同结构 | 内部闭环逻辑 | 内部闭环逻辑 |
| `kernel/trap.c#trap_handler` | `kernel/trap/trap.c#show_regs` | 同模块上下文对齐/差异观察 | r_scause, syscall_handle, uart_putc, uart_puts | printk |
| `kernel/syscall.c#syscall_handle` | `kernel/trap/stacktrace.c#search_stackframe` | 同模块上下文对齐/差异观察 | syscall_init_table, syscall_table[nr], uart_putc, uart_puts | __builtin_frame_address, kernel_text, printk |

#### 系统调用
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `kernel/syscall.c#str_eq` | `usr/user_syscall.c#internal_syscall` | 同模块上下文对齐/差异观察 | 内部闭环逻辑 | 内部闭环逻辑 |
| `kernel/syscall.c#str_len` | `usr/user_syscall.c#printf` | 同模块上下文对齐/差异观察 | 内部闭环逻辑 | SYSCALL_1, va_end, va_start, vsprintf |
| `kernel/syscall.c#str_cpy` | `usr/user_syscall.c#print` | 同模块上下文对齐/差异观察 | 内部闭环逻辑 | SYSCALL_1, va_end, va_start, vsprintf |

#### 文件系统
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `kernel/syscall.c#syscall_register_file` | `kernel/fs/loader.c#placeInfo` | 同模块上下文对齐/差异观察 | 内部闭环逻辑 | loadSecData, strcmp |

#### 设备驱动
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `kernel/virtio.c#free_chain` | `kernel/driver/virtio.c#free_chain` | 同板块同名 | 内部闭环逻辑 | free_desc |
| `kernel/uart.c#uart_init` | `kernel/urat.c#uart_init` | 同板块同名 | 内部闭环逻辑 | writebyte |
| `kernel/uart.c#uart_putc` | `lib/printk.c#print_f` | 同模块上下文对齐/差异观察 | 内部闭环逻辑 | uart_send_string, va_end, va_start, vsprintf |
| `kernel/uart.c#uart_puts` | `kernel/urat.c#uart_get` | 同模块上下文对齐/差异观察 | uart_putc | readbyte |

#### 文件系统/第三方库
仅目标项目中观察到该模块代表函数，参考项目没有同模块可对齐证据：
- 目标项目：`kernel/ext4.c#ext4_scan_testcode`，调用摘要：aligned, read_extent, read_inode
- 目标项目：`kernel/ext4.c#ext4_open`，调用摘要：aligned, kalloc_page, read_extent, read_inode
- 目标项目：`kernel/ext4.c#read_extent`，调用摘要：read_block
- 目标项目：`kernel/ext4.c#ext4_init`，调用摘要：aligned, uart_putc, uart_puts, virtio_read

#### 通用模块/其他
仅参考项目中观察到该模块代表函数，目标项目没有同模块可对齐证据：
- 参考项目：`lib/printk.c#number`，调用摘要：do_div
- 参考项目：`lib/lib.c#strcmp`，调用摘要：内部闭环逻辑
- 参考项目：`lib/lib.c#strcpy`，调用摘要：内部闭环逻辑
- 参考项目：`usr/shell.c#find_cmd`，调用摘要：print, strcmp


# 最终同源性定性仲裁报告

---

## 1. 宏观判定

- **定性结论**: **独立开发 / 底层架构显著差异**
- **客观相似度**: 1.24%
- **共同 AST 指纹**: 3
- **唯一 AST 指纹**: 目标项目 66，参考项目 179
- **覆盖率**: 目标项目 4.55%，参考项目 1.68%

**判定依据**: 双方在操作系统内核这一高度规范化的领域中，共同 AST 指纹仅 3 个，覆盖率均低于 5%。除中断/异常处理模块存在 1 个共同指纹外，进程管理、任务调度、内存管理、系统调用、文件系统、设备驱动等核心模块的共同 AST 指纹均为 0。目标项目采用 ext4 文件系统、`uart_*` 日志子系统、`uvm_*` 页表抽象；参考项目采用 FAT32 文件系统、`printk` 日志子系统、`alloc_pgtable`/`more_page_alloc` 内存抽象。双方在文件系统选型、内存管理策略、日志输出机制上存在系统性架构差异，不属于“换皮复用”。

---

## 2. 全模块覆盖矩阵解读

### 进程管理
- **双方均存在**: 是（目标 7 函数，参考 29 函数）
- **共同 AST 指纹**: 0
- **覆盖率**: 目标 0.0%，参考 0.0%
- **证据强度**: **未形成**

目标项目进程管理函数（`sys_exit`、`fd_close`、`proc_init`、`sys_getppid`、`fd_get`、`proc_free`、`sys_getpid`）与参考项目（`copy_thread`、`__schedule`、`spin_lock` 等）在调用图谱上无任何共同 AST 指纹。目标项目使用 `fd_*` 系列文件描述符抽象，参考项目使用 `get_current_task` + `filp->f_ops->*` 的 VFS 多态分发模型，进程抽象层设计理念不同。

### 任务调度/异步运行时
- **双方均存在**: 是（目标 8 函数，参考 21 函数）
- **共同 AST 指纹**: 0
- **覆盖率**: 目标 0.0%，参考 0.0%
- **证据强度**: **未形成**

目标项目 `kmain` 调用 `ext4_init`、`ext4_scan_testcode`、`mm_init`、`proc_init`、`run_test`、`trap_init`、`uart_init` 等，体现 ext4 + 裸机 UART 的初始化链路。参考项目 `do_exception` 调用 `handle_plic_irq`、`handle_timer`、`syscall_handler` 等，体现 PLIC 中断控制器 + 定时器 + 系统调用分发链路。调度策略与初始化流程无结构重合。

### 内存管理
- **双方均存在**: 是（目标 20 函数，参考 67 函数）
- **共同 AST 指纹**: 0
- **覆盖率**: 目标 0.0%，参考 0.0%
- **证据强度**: **未形成**

目标项目使用 `uvm_map` → `walk_pgtbl` → `kalloc_zero` 的 xv6 风格页表映射链；参考项目使用 `alloc_pgtable` + `memset` 或 `more_page_alloc` + `more_page_free` 的自研页分配器。目标项目 `elf_load` 通过 `uvm_map` 直接映射 ELF 段，参考项目 `load_segment` 通过 `memcpy` 拷贝到已分配页。加载策略（map vs copy）本质不同。`virtio_init` 和 `alloc_desc` 虽同名，但目标项目直接操作 MMIO 寄存器（`R`），参考项目通过 `readword`/`writeword` 封装，实现路径不同。

### 中断/异常处理
- **双方均存在**: 是（目标 28 函数，参考 20 函数）
- **共同 AST 指纹**: 1
- **覆盖率**: 目标 6.25%，参考 5.0%
- **证据强度**: **弱**

唯一共同 AST 指纹来自 `trap_init` 函数。目标项目调用 `uart_puts` + `w_stvec`，参考项目调用 `printk` + `read_csr` + `write_csr`。`trap_init` 是 RISC-V 异常处理的行业通用命名，其核心逻辑（打印日志 + 设置 stvec 异常向量寄存器）是 RISC-V 特权指令集规范强制的标准初始化流程。双方在日志输出（`uart_puts` vs `printk`）和 CSR 访问方式（专用封装 `w_stvec` vs 通用 `read_csr`/`write_csr`）上存在实现差异。1 个共同指纹在 28 和 20 个函数的模块中属于领域共性噪声。

### 系统调用
- **双方均存在**: 是（目标 3 函数，参考 12 函数）
- **共同 AST 指纹**: 0
- **覆盖率**: 目标 0.0%，参考 0.0%
- **证据强度**: **未形成**

目标项目 `sys_write` 调用 `fd_get` + `uart_putc`，是简化版控制台输出；参考项目 `callback_sys_write` 调用 `filp->f_ops->write` + `get_current_task` + `printk`，是 VFS 多态分发路径。`sys_write` 是 POSIX 标准名称，同名不构成复用证据。

### 文件系统
- **双方均存在**: 是（目标 1 函数，参考 49 函数）
- **共同 AST 指纹**: 0
- **覆盖率**: 目标 0.0%，参考 0.0%
- **证据强度**: **未形成**

目标项目文件系统模块仅 1 个函数（`syscall_register_file`），但目标项目实际文件系统实现在 `kernel/ext4.c` 中（被归类为“文件系统/第三方库”模块），使用 ext4 文件系统。参考项目使用 FAT32 文件系统（`FAT32_read`、`FAT32_write`、`fat32_read_superblock` 等 49 个函数）。ext4 与 FAT32 是两种完全不同的文件系统，数据结构与算法无重合可能。

### 设备驱动
- **双方均存在**: 是（目标 7 函数，参考 7 函数）
- **共同 AST 指纹**: 0
- **覆盖率**: 目标 0.0%，参考 0.0%
- **证据强度**: **未形成**

双方均有 `free_chain` 和 `uart_init` 同名函数，但调用图谱显示目标项目 `free_chain` 为内部闭环逻辑，参考项目 `free_chain` 调用 `free_desc`；目标项目 `uart_init` 为内部闭环逻辑，参考项目 `uart_init` 调用 `writebyte`。同名源于 VirtIO 规范和 UART 驱动的领域通用命名，内部实现路径不同。

### 网络协议栈
- **双方均存在**: 否
- **共同 AST 指纹**: 0
- **覆盖率**: 双方 0.0%
- **证据强度**: **未形成**

双方均未体现网络协议栈模块。

### 第三方库/公共依赖
- **目标独有**: 文件系统/第三方库（7 函数，含 `ext4_init`、`ext4_open`、`read_extent`、`ext4_scan_testcode` 等）
- **参考独有**: 通用模块/其他（11 函数，含 `number`、`strcmp`、`strcpy`、`find_cmd` 等）
- **共同 AST 指纹**: 0
- **证据强度**: **未形成**

目标项目独有 ext4 文件系统实现，参考项目独有 shell 命令解析和通用字符串库。双方在第三方库/公共依赖上无任何重合，进一步证明独立开发。

---

## 3. 底层 AST 关键证据指纹表

| 所属板块 | 目标项目路径 | 参考项目路径 | 证据类型 | 技术说明 |
| :--- | :--- | :--- | :--- | :--- |
| 中断/异常处理 | `kernel/trap.c#trap_init` | `kernel/trap/trap.c#trap_init` | 同板块同名 + 弱共同指纹 | 唯一共同 AST 指纹。双方均实现 RISC-V trap 初始化，但目标使用 `uart_puts`+`w_stvec`，参考使用 `printk`+`read_csr`+`write_csr`。日志子系统和 CSR 访问封装不同。`trap_init` 为 RISC-V 特权指令集规范强制的标准命名与流程。 |
| 内存管理 | `kernel/virtio.c#virtio_init` | `kernel/driver/virtio.c#virtio_init` | 同板块同名 / 无共同指纹 | 同名但调用图谱完全不同。目标直接操作 MMIO（`R`）+ `kalloc_zero` + `uart_putc`；参考封装为 `virtio_dev_init`（内部调用 `printk`+`readword`+`writeword`）。抽象层次不同。 |
| 内存管理 | `kernel/virtio.c#alloc_desc` | `kernel/driver/virtio.c#alloc_desc` | 同板块同名 / 无共同指纹 | 双方均为内部闭环逻辑。`alloc_desc` 是 VirtIO 规范的标准命名，描述符分配天然是局部状态机操作，外部调用图谱为空是设计必然。 |
| 设备驱动 | `kernel/virtio.c#free_chain` | `kernel/driver/virtio.c#free_chain` | 同板块同名 / 无共同指纹 | 目标为内部闭环逻辑，参考调用 `free_desc`。同名源于 VirtIO 规范，实现路径不同。 |
| 设备驱动 | `kernel/uart.c#uart_init` | `kernel/urat.c#uart_init` | 同板块同名 / 无共同指纹 | 目标为内部闭环逻辑，参考调用 `writebyte`。同名源于 UART 驱动通用命名。 |

**说明**: 上表仅列出存在同名函数或共同指纹的条目。其余模块（进程管理、任务调度、系统调用、文件系统）共同 AST 指纹均为 0，无任何可列入的强或弱证据。

---

## 4. 核心代码调用流对比剖析

### 【中断/异常处理】trap_init 初始化路径

- **底层技术析理**: 双方均实现 RISC-V 异常处理的初始化，但日志输出子系统和 CSR 寄存器访问方式存在本质差异。目标项目使用专用封装 `w_stvec` 直接写入 stvec 寄存器，日志通过 `uart_puts` 直接串口输出；参考项目使用通用 CSR 读写接口 `read_csr`/`write_csr`，日志通过 `printk` 格式化输出子系统。目标项目为裸机直接驱动风格，参考项目为分层封装风格。

- **代码溯源链接**:
  - 目标项目: `kernel/trap.c#trap_init`
  - 参考项目: `kernel/trap/trap.c#trap_init`

---

### 【内存管理】VirtIO 块设备初始化路径

- **底层技术析理**: 目标项目 `virtio_init` 直接操作 MMIO 寄存器（`R` 宏）、调用 `kalloc_zero` 分配清零页、通过 `uart_putc`/`uart_puts` 输出日志，体现裸机直接驱动风格。参考项目 `virtio_init` 将初始化逻辑封装为 `virtio_dev_init`，内部通过 `readword`/`writeword` 抽象寄存器访问、通过 `printk` 输出日志、调用 `virtio_blk_init_legacy` 处理 Legacy 模式。双方在抽象层次（直接 MMIO vs 封装读写）、内存分配（`kalloc_zero` vs 无显式分配）、日志机制（UART vs printk）上均不同。

- **代码溯源链接**:
  - 目标项目: `kernel/virtio.c#virtio_init`
  - 参考项目: `kernel/driver/virtio.c#virtio_init`

---

### 【任务调度/异步运行时】系统调用写操作路径

- **底层技术析理**: 目标项目 `sys_write` 通过 `fd_get` 获取文件描述符后直接调用 `uart_putc` 输出字符，是简化版控制台写操作，无 VFS 层。参考项目 `callback_sys_write` 通过 `get_current_task` 获取当前任务上下文，再经 `filp->f_ops->write` 走 VFS 多态分发，最终通过 `printk` 输出。目标项目为扁平化直接调用，参考项目为分层多态分发，架构设计理念不同。

- **代码溯源链接**:
  - 目标项目: `kernel/syscall.c#sys_write`
  - 参考项目: `kernel/trap/syscall.c#callback_sys_write`

---

### 【内存管理】ELF 加载路径

- **底层技术析理**: 目标项目 `elf_load` 通过 `kalloc_zero` 分配清零页后调用 `uvm_map` 将 ELF 段直接映射到用户页表，采用 map 策略。参考项目 `load_elf` / `load_segment` 通过 `more_page_alloc` 分配页后调用 `memcpy` 将数据拷贝到已分配页，采用 copy 策略。目标项目使用 ext4 文件系统读取 ELF，参考项目使用 FAT32 + loader 框架。加载策略（map vs copy）和文件系统层均不同。

- **代码溯源链接**:
  - 目标项目: `kernel/elf.c#elf_load`
  - 参考项目: `kernel/fs/loader.c#load_elf`

---

## 5. 单方模块与降权项

### 目标项目独有模块
| 模块 | 代表函数 | 说明 |
| :--- | :--- | :--- |
| 文件系统/第三方库 | `ext4_init`、`ext4_open`、`read_extent`、`ext4_scan_testcode` | 目标项目实现了完整的 ext4 文件系统驱动，参考项目使用 FAT32。ext4 与 FAT32 在超级块结构、inode 管理、extent 树索引等方面完全不同，属于独立的文件系统选型。 |

### 参考项目独有模块
| 模块 | 代表函数 | 说明 |
| :--- | :--- | :--- |
| 通用模块/其他 | `number`、`strcmp`、`strcpy`、`find_cmd` | 参考项目实现了自研的 shell 命令解析器和通用字符串库，目标项目无对应模块。 |

### 降权项说明
控方在“弱信号降级区”中提及的跨板块比较（`proc_free` → `uvm_free` vs `FAT32_write` → `page_free_addr`；`sys_read` → `fd_get` vs `initElf` → `filp->f_ops->read`）属于跨模块硬凑，本报告不予采纳。前者分属进程管理与文件系统，释放对象完全不同（进程地址空间 vs FAT32 文件页缓存）；后者分属系统调用与 ELF 加载器，抽象层次不同（fd 抽象 vs 文件操作指针）。跨板块同名或功能相似不能作为同源证据。

---

## 6. 保守结论

目标项目 `[2026-T2026102479910691-XX]` 与参考项目 `[2023-接下来我要表扬一款游戏-广东工业大学]` 的客观 AST 相似度仅为 1.24%，共同 AST 指纹仅 3 个，覆盖率均低于 5%。在操作系统内核这一高度规范化的领域中，该量级的重合完全可由 RISC-V 特权指令集规范、VirtIO 标准、POSIX 系统调用接口等公开领域知识的共性约束所解释。双方在文件系统选型（ext4 vs FAT32）、内存管理策略（uvm_map 映射 vs memcpy 拷贝）、日志子系统（uart_* vs printk）、CSR 访问方式（专用封装 vs 通用接口）等核心架构维度上存在系统性差异。控方指控的“换皮复用”缺乏跨项目相同 AST hash 或 token 序列证据支持，同名函数（`trap_init`、`alloc_desc`、`virtio_init`）均为领域通用命名，不能作为抄袭证据。综合判定：双方为独立开发，不存在代码复用或抄袭行为。

------------------------------

