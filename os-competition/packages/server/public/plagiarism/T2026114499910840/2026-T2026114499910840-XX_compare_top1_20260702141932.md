# 🛡️ OS 内核赛自动化代码级架构审计报告

**审计时间**: `2026-07-02 14:19:32`
**参赛作品名称**: `2026-T2026114499910840-XX`
**年份**: `2026`
**学校**: `XX`
**队伍名称**: `T2026114499910840`
**比赛名字**: `OS-Competition`
**比赛子赛道**: `Kernel-Implementation`
**仓库地址**: `https://gitlab.eduxiji.net/educg-group-43501-3132633/T2026114499910840-1796`
**推测开发语言**: `Rust / C (由AST探针自动识别)`
**特征函数量**: `6932`

## ⚔️ 底层代码同源性对比 (AST Homology)
> 本部分由专门的代码审查智能体，对该项目与符合年份条件的历届最高相似度项目进行控制流与底层调用对比。新版报告会先给出确定性的全模块覆盖矩阵，再给出 Agent 的分模块解释，避免只围绕少量抽样函数展开。

### 🏆 Top 1 底层对标: vs `2023-AVX-华中科技大学`（华中科技大学 / AVX）

#### 对方作品信息

**对方年份**: `2023`
**对方学校**: `华中科技大学`
**对方队伍名称**: `AVX`
**对方比赛名字**: `OS-Competition`
**对方比赛子赛道**: `Kernel-Implementation`
**对方仓库地址**: `https://gitlab.eduxiji.net/202310487101114/oskernel2023-avx`

> 双向覆盖率：共同 AST 指纹: 1201；唯一 AST 指纹: 目标项目 5715，参考项目 1740；覆盖率: 目标项目 21.01%，参考项目 69.02%

## 全模块对比总览（确定性证据）

> 总体覆盖率：共同 AST 指纹: 1201；唯一 AST 指纹: 目标项目 5715，参考项目 1740；覆盖率: 目标项目 21.01%，参考项目 69.02%

### 1. 模块覆盖矩阵
| 模块 | 目标函数数 | 参考函数数 | 共同唯一 AST 指纹 | Target 覆盖 | Base 覆盖 | 状态 |
| :--- | ---: | ---: | ---: | ---: | ---: | :--- |
| 进程管理 | 275 | 82 | 16 | 6.67% | 23.19% | 双方存在且有结构重合 |
| 任务调度/异步运行时 | 1435 | 568 | 426 | 34.95% | 87.84% | 双方存在且有结构重合 |
| 内存管理 | 1419 | 485 | 273 | 20.81% | 58.58% | 双方存在且有结构重合 |
| 中断/异常处理 | 149 | 21 | 1 | 0.81% | 4.76% | 双方存在且有结构重合 |
| 系统调用 | 156 | 63 | 36 | 33.64% | 64.29% | 双方存在且有结构重合 |
| 文件系统 | 1219 | 168 | 73 | 7.2% | 46.5% | 双方存在且有结构重合 |
| 设备驱动 | 747 | 19 | 1 | 0.16% | 5.88% | 双方存在且有结构重合 |
| 网络协议栈 | 593 | 404 | 334 | 64.48% | 97.38% | 双方存在且有结构重合 |
| 第三方库 | 379 | 0 | 0 | 0.0% | 0.0% | 仅目标项目体现 |
| 通用模块/其他 | 560 | 173 | 7 | 1.69% | 4.52% | 双方存在且有结构重合 |

### 2. 分模块证据清单

#### 进程管理
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `modules/net/netserver/lwip/core/ipv6/nd6.c#nd6_process_autoconfig_prefix` | `kernel/lwip/core/ipv6/nd6.c#nd6_process_autoconfig_prefix` | 同板块同名同结构 | IP6_ADDR, LWIP_ASSERT, ip6_addr_assign_zone, ip6_addr_eq, ip6_addr_isduplicated, ip6_addr_isinvalid ...(+10) | IP6_ADDR, LWIP_ASSERT, ip6_addr_assign_zone, ip6_addr_eq, ip6_addr_isduplicated, ip6_addr_isinvalid ...(+10) |
| `kernel/src/task/task_syscall.c#sys_wait4` | `kernel/sysproc.c#sys_wait4` | 同板块同名 | arch_disable_interrupt, arch_enable_interrupt, check_user_overflow, copy_to_user, hashmap_entry_is_occupied, list_entry ...(+18) | argaddr, argint, wait4pid |
| `modules/net/netserver/lwip/netif/ppp/polarssl/sha1.c#sha1_process` | `kernel/lwip/netif/ppp/polarssl/sha1.c#sha1_process` | 同板块同名同结构 | GET_ULONG_BE, P, R | GET_ULONG_BE, P, R |
| `modules/net/netserver/lwip/netif/ppp/polarssl/md5.c#md5_process` | `kernel/lwip/netif/ppp/polarssl/md5.c#md5_process` | 同板块同名同结构 | GET_ULONG_LE, P | GET_ULONG_LE, P |

#### 任务调度/异步运行时
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `modules/net/netserver/lwip/netif/lowpan6_common.c#lowpan6_decompress_hdr` | `kernel/lwip/netif/lowpan6_common.c#lowpan6_decompress_hdr` | 同板块同名 | IP6H_HOPLIM_SET, IP6H_NEXTH_SET, IP6H_PLEN_SET, IP6H_VTCFL_SET, LWIP_ASSERT, LWIP_DEBUGF ...(+5) | IP6H_HOPLIM_SET, IP6H_NEXTH_SET, IP6H_PLEN_SET, IP6H_VTCFL_SET, LWIP_ASSERT, LWIP_DEBUGF ...(+5) |
| `modules/net/netserver/lwip/netif/ppp/lcp.c#lcp_nakci` | `kernel/lwip/netif/ppp/lcp.c#lcp_nakci` | 同板块同名同结构 | BZERO, CHAP_CANDIGEST, CHAP_DIGEST, CHAP_MDTYPE, CHAP_MDTYPE_D, GETCHAR ...(+15) | BZERO, CHAP_CANDIGEST, CHAP_DIGEST, CHAP_MDTYPE, CHAP_MDTYPE_D, GETCHAR ...(+15) |
| `modules/net/netserver/lwip/netif/ppp/eap.c#eap_request` | `kernel/lwip/netif/ppp/eap.c#eap_request` | 同板块同名同结构 | BZERO, GETCHAR, GETLONG, INCPTR, MEMCPY, SHA1Final ...(+37) | BZERO, GETCHAR, GETLONG, INCPTR, MEMCPY, SHA1Final ...(+37) |
| `modules/net/netserver/lwip/api/sockets.c#lwip_getsockopt_impl` | `kernel/lwip/api/sockets.c#lwip_getsockopt_impl` | 同板块同名 | LWIP_DEBUGF, LWIP_HOOK_SOCKETS_GETSOCKOPT, LWIP_SOCKOPT_CHECK_OPTLEN, LWIP_SOCKOPT_CHECK_OPTLEN_CONN, LWIP_SOCKOPT_CHECK_OPTLEN_CONN_PCB, LWIP_SOCKOPT_CHECK_OPTLEN_CONN_PCB_TYPE ...(+20) | LWIP_DEBUGF, LWIP_HOOK_SOCKETS_GETSOCKOPT, LWIP_SOCKOPT_CHECK_OPTLEN, LWIP_SOCKOPT_CHECK_OPTLEN_CONN, LWIP_SOCKOPT_CHECK_OPTLEN_CONN_PCB, LWIP_SOCKOPT_CHECK_OPTLEN_CONN_PCB_TYPE ...(+19) |

#### 内存管理
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `modules/net/netserver/lwip/core/tcp_in.c#tcp_receive` | `kernel/lwip/core/tcp_in.c#tcp_receive` | 同板块同名 | (s16_t), (tcpwnd_size_t), (u16_t), (u8_t), LWIP_ASSERT, LWIP_DEBUGF ...(+39) | (s16_t), (tcpwnd_size_t), (u16_t), (u8_t), LWIP_ASSERT, LWIP_DEBUGF ...(+39) |
| `modules/net/netserver/lwip/core/ipv6/nd6.c#nd6_input` | `kernel/lwip/core/ipv6/nd6.c#nd6_input` | 同板块同名 | IP6H_HOPLIM, IP_SET_TYPE_VAL, LWIP_MIN, MEMCPY, ND6_STATS_INC, SMEMCPY ...(+41) | IP6H_HOPLIM, IP_SET_TYPE_VAL, LWIP_MIN, MEMCPY, ND6_STATS_INC, SMEMCPY ...(+41) |
| `modules/net/netserver/lwip/core/tcp_out.c#tcp_write` | `kernel/lwip/core/tcp_out.c#tcp_write` | 同板块同名 | LWIP_ASSERT, LWIP_ASSERT_CORE_LOCKED, LWIP_DEBUGF, LWIP_ERROR, LWIP_MAX, LWIP_MIN ...(+20) | LWIP_ASSERT, LWIP_ASSERT_CORE_LOCKED, LWIP_DEBUGF, LWIP_ERROR, LWIP_MAX, LWIP_MIN ...(+20) |
| `modules/net/netserver/lwip/core/ipv6/ip6_frag.c#ip6_reass` | `kernel/lwip/core/ipv6/ip6_frag.c#ip6_reass` | 同板块同名 | (s16_t), (u16_t), IP6H_NEXTH, IP6_FRAG_STATS_INC, IPV6_FRAG_DEST, IPV6_FRAG_SRC ...(+21) | (s16_t), (u16_t), IP6H_NEXTH, IP6_FRAG_STATS_INC, IPV6_FRAG_DEST, IPV6_FRAG_SRC ...(+20) |

#### 中断/异常处理
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `modules/net/netserver/lwip/netif/ppp/utils.c#complete_read` | `kernel/lwip/netif/ppp/utils.c#complete_read` | 同板块同名同结构 | read | read |
| `kernel/src/net/socket.c#unix_socket_recvmsg_from_self` | `kernel/sd_final.c#sd_test` | 同模块上下文对齐/差异观察 | fd_get_flags, mutex_lock, mutex_unlock, skb_queue_packets, socket_notify_sock, socket_wait_node ...(+8) | Platform_CardNDetect, SDIO_Setup_Callback, SD_Card_Init, SD_Card_SetBlockSize, SD_IRQHandler, SD_Send_Command ...(+2) |
| `kernel/src/arch/aarch64/drivers/timer.c#timer_init_from_dtb` | `kernel/trap.c#trapframedump` | 同模块上下文对齐/差异观察 | boot_get_dtb, fdt_getprop, fdt_path_offset, printk, strstr, timer_get_freq_from_dtb ...(+1) | serious_print |
| `kernel/src/arch/aarch64/drivers/timer.c#timer_init` | `kernel/sd_final.c#SD_IRQHandler` | 同模块上下文对齐/差异观察 | irq_regist_irq, printk, read_cntfrq, timer_init_from_acpi, timer_init_from_dtb | sdioif->wake_evt |

#### 系统调用
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `modules/net/netserver/lwip/api/sockets.c#lwip_pollscan` | `kernel/lwip/api/sockets.c#lwip_pollscan` | 同板块同名同结构 | LWIP_ASSERT, LWIP_DEBUGF, SYS_ARCH_DECL_PROTECT, SYS_ARCH_PROTECT, SYS_ARCH_UNPROTECT, done_socket ...(+1) | LWIP_ASSERT, LWIP_DEBUGF, SYS_ARCH_DECL_PROTECT, SYS_ARCH_PROTECT, SYS_ARCH_UNPROTECT, done_socket ...(+1) |
| `modules/net/netserver/lwip/api/sockets.c#select_check_waiters` | `kernel/lwip/api/sockets.c#select_check_waiters` | 同板块同名同结构 | FD_ISSET, LWIP_ASSERT_CORE_LOCKED, SELECT_SEM_PTR, SYS_ARCH_DECL_PROTECT, SYS_ARCH_PROTECT, SYS_ARCH_UNPROTECT ...(+2) | FD_ISSET, LWIP_ASSERT_CORE_LOCKED, SELECT_SEM_PTR, SYS_ARCH_DECL_PROTECT, SYS_ARCH_PROTECT, SYS_ARCH_UNPROTECT ...(+2) |
| `modules/net/netserver/lwip/api/netdb.c#lwip_gethostbyname` | `kernel/lwip/api/netdb.c#lwip_gethostbyname` | 同板块同名同结构 | LWIP_DEBUGF, ipaddr_ntoa, netconn_gethostbyname, strncpy, sys_thread_hostent | LWIP_DEBUGF, ipaddr_ntoa, netconn_gethostbyname, strncpy, sys_thread_hostent |
| `modules/net/netserver/lwip/core/timeouts.c#lwip_cyclic_timer` | `kernel/lwip/core/timeouts.c#lwip_cyclic_timer` | 同板块同名同结构 | (u32_t), LWIP_DEBUGF, TIME_LESS_THAN, cyclic->handler, sys_now, sys_timeout_abs | (u32_t), LWIP_DEBUGF, TIME_LESS_THAN, cyclic->handler, sys_now, sys_timeout_abs |

#### 文件系统
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `modules/net/netserver/lwip/core/ipv4/etharp.c#etharp_find_entry` | `kernel/lwip/core/ipv4/etharp.c#etharp_find_entry` | 同板块同名 | LWIP_ASSERT, LWIP_DEBUGF, LWIP_UNUSED_ARG, etharp_free_entry, ip4_addr_copy, ip4_addr_eq | LWIP_ASSERT, LWIP_DEBUGF, LWIP_UNUSED_ARG, etharp_free_entry, ip4_addr_copy, ip4_addr_eq |
| `modules/net/netserver/lwip/core/ipv4/ip4_frag.c#ip_reass_chain_frag_into_datagram_and_validate` | `kernel/lwip/core/ipv4/ip4_frag.c#ip_reass_chain_frag_into_datagram_and_validate` | 同板块同名同结构 | (u16_t), IPH_HL_BYTES, IPH_LEN, IPH_OFFSET_BYTES, LWIP_ASSERT, lwip_ntohs | (u16_t), IPH_HL_BYTES, IPH_LEN, IPH_OFFSET_BYTES, LWIP_ASSERT, lwip_ntohs |
| `modules/net/netserver/lwip/core/ipv4/ip4.c#ip4_output_if_opt_src` | `kernel/lwip/core/ipv4/ip4.c#ip4_output_if_opt_src` | 同板块同名 | (u16_t), IF__NETIF_CHECKSUM_ENABLED, IPH_CHKSUM_SET, IPH_ID_SET, IPH_LEN_SET, IPH_OFFSET_SET ...(+25) | (u16_t), IF__NETIF_CHECKSUM_ENABLED, IPH_CHKSUM_SET, IPH_ID_SET, IPH_LEN_SET, IPH_OFFSET_SET ...(+25) |
| `modules/net/netserver/lwip/api/sockets.c#lwip_recv_tcp` | `kernel/lwip/api/sockets.c#lwip_recv_tcp` | 同板块同名同结构 | (u16_t), LWIP_ASSERT, LWIP_DEBUGF, NETCONNTYPE_GROUP, err_to_errno, lwip_strerr ...(+7) | (u16_t), LWIP_ASSERT, LWIP_DEBUGF, NETCONNTYPE_GROUP, err_to_errno, lwip_strerr ...(+7) |

#### 设备驱动
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `modules/net/netserver/lwip/netif/zepif.c#unknown` | `kernel/lwip/netif/zepif.c#unknown` | 同板块同名同结构 | 内部闭环逻辑 | 内部闭环逻辑 |
| `kernel/src/drivers/drm/plainfb.c#plainfb_cursor_draw` | `kernel/uart.c#uartinit` | 同模块上下文对齐/差异观察 | MIN, memcpy, phys_to_virt, plainfb_compress_component, plainfb_cursor_backup_ensure, plainfb_expand_component ...(+4) | WriteReg, initlock |
| `kernel/src/drivers/drm/drm_ioctl.c#drm_ioctl_mode_getconnector` | `kernel/console.c#consputc` | 同模块上下文对齐/差异观察 | MIN, dev->op->get_display_info, drm_connector_edid_blob_id, drm_connector_free, drm_connector_get, drm_copy_to_user_ptr | sbi_console_putchar, uart8250_putc |
| `modules/drivers/sound/sound.c#sound_hw_refine_fill_defaults` | `kernel/uart.c#uartstart` | 同模块上下文对齐/差异观察 | memset, sound_hw_param_interval, sound_hw_param_mask, sound_interval_intersect_range, sound_mask_intersect, sound_mask_set_bit ...(+2) | ReadReg, WriteReg, wakeup |

#### 网络协议栈
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `modules/net/netserver/lwip/core/ipv6/nd6.c#netif` | `kernel/lwip/core/ipv6/nd6.c#netif` | 同板块同名同结构 | LWIP_ASSERT, ip6_addr_isinvalid, ip6_addr_istentative, ip6_addr_life_isinfinite, nd6_send_ns, netif_ip6_addr ...(+9) | LWIP_ASSERT, ip6_addr_isinvalid, ip6_addr_istentative, ip6_addr_life_isinfinite, nd6_send_ns, netif_ip6_addr ...(+9) |
| `modules/net/netserver/lwip/core/ipv6/ip6.c#ip6_forward` | `kernel/lwip/core/ipv6/ip6.c#ip6_forward` | 同板块同名 | IP6H_HOPLIM, IP6H_HOPLIM_SET, IP6H_NEXTH, IP6_ADDR_BLOCK1, IP6_ADDR_BLOCK2, IP6_ADDR_BLOCK3 ...(+18) | IP6H_HOPLIM, IP6H_HOPLIM_SET, IP6H_NEXTH, IP6_ADDR_BLOCK1, IP6_ADDR_BLOCK2, IP6_ADDR_BLOCK3 ...(+18) |
| `modules/net/netserver/lwip/netif/ppp/auth.c#auth_check_options` | `kernel/lwip/netif/ppp/auth.c#auth_check_options` | 同板块同名 | auth_number, exit, have_chap_secret, have_pap_secret, have_route_to, have_srp_secret ...(+3) | auth_number, exit, have_chap_secret, have_pap_secret, have_route_to, have_srp_secret ...(+3) |
| `modules/net/netserver/lwip/api/sockets.c#lwip_recvmsg` | `kernel/lwip/api/sockets.c#lwip_recvmsg` | 同板块同名同结构 | LWIP_DEBUGF, LWIP_ERROR, NETCONNTYPE_GROUP, done_socket, err_to_errno, get_socket ...(+5) | LWIP_DEBUGF, LWIP_ERROR, NETCONNTYPE_GROUP, done_socket, err_to_errno, get_socket ...(+5) |

#### 第三方库
仅目标项目中观察到该模块代表函数，参考项目没有同模块可对齐证据：
- 目标项目：`kernel/src/libs/flanterm/flanterm_backends/fb.c#flanterm_fb_init`，调用摘要：_free, _malloc, convert_colour, flanterm_context_reinit, flanterm_fb_full_refresh, memcpy ...(+1)
- 目标项目：`kernel/src/libs/flanterm/flanterm.c#control_sequence_parse`，调用摘要：ctx->callback, ctx->clear, ctx->get_cursor_pos, ctx->move_character, ctx->raw_putchar, ctx->revscroll ...(+6)
- 目标项目：`kernel/src/libs/flanterm/flanterm.c#sgr`，调用摘要：(fg ? ctx->set_text_fg : ctx->set_text_bg), (fg ? ctx->set_text_fg_bright
                        : ctx->set_text_bg_bright), (fg ? ctx->set_text_fg_rgb
                        : ctx->set_text_bg_rgb), (fg ? ctx->set_text_fg_rgb : ctx->set_text_bg_rgb), ctx->set_text_bg, ctx->set_text_bg_bright ...(+7)
- 目标项目：`kernel/src/libs/flanterm/flanterm.c#unicode_to_cp437`，调用摘要：内部闭环逻辑

#### 通用模块/其他
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `kernel/src/acpi/namespace.c#uacpi_namespace_node_find` | `kernel/lwip/core/dns.c#dns_gethostbyname` | 同板块同结构 | uacpi_namespace_node_resolve | dns_gethostbyname_addrtype |
| `kernel/src/acpi/namespace.c#uacpi_namespace_node_resolve_from_aml_namepath` | `kernel/lwip/core/pbuf.c#pbuf_add_header_force` | 同板块同结构 | uacpi_namespace_node_resolve | pbuf_add_header_impl |
| `kernel/src/acpi/uacpi.c#uacpi_eval_simple_typed` | `kernel/lwip/core/pbuf.c#pbuf_add_header` | 同板块同结构 | uacpi_eval_typed | pbuf_add_header_impl |
| `kernel/src/acpi/uacpi.c#uacpi_eval_package` | `kernel/lwip/core/pbuf.c#pbuf_header_force` | 同板块同结构 | uacpi_eval_typed | pbuf_header_impl |


#### 1. 宏观判定
- **定性结论**: **独立开发，存在第三方公共库复用**
  基于客观 AST 相似度 19.2% 及双向覆盖率指标（目标项目覆盖率 21.01%，参考项目覆盖率 69.02%），目标项目 `[2026-T2026114499910840-XX]` 与参考项目 `[2023-AVX-华中科技大学]` 在**核心内核架构与自研代码层面存在根本性差异**。双方代码的重合部分高度集中在 `lwIP` 及 `PolarSSL` 等第三方开源库，属于对公共上游代码的共同引用，不构成对参考项目原创代码的抄袭。

#### 2. 全模块覆盖矩阵解读
- **进程管理**
  - 双方均存在该模块。
  - 存在共同 AST 指纹。
  - 目标覆盖率 6.67%，参考覆盖率 23.19%。覆盖率极低，表明双方在进程管理核心逻辑上几乎无重合。
  - 证据强度：**弱**。控方列举的多数“进程管理”证据实为网络、加密、驱动等模块函数，属于跨板块硬凑。真正的进程管理函数（如 `sys_wait4`）调用图谱差异巨大。

- **任务调度/异步运行时**
  - 双方均存在该模块。
  - 存在共同 AST 指纹。
  - 目标覆盖率 34.95%，参考覆盖率 87.84%。参考项目覆盖率极高，说明其调度模块大量代码与目标项目重合，而这部分重合代码经查为 lwIP 内部逻辑。
  - 证据强度：**弱**。重合部分为 lwIP 协议栈的内部定时与调度机制，非操作系统内核的任务调度器。

- **内存管理**
  - 双方均存在该模块。
  - 存在共同 AST 指纹。
  - 目标覆盖率 20.81%，参考覆盖率 58.58%。参考项目覆盖率过半，重合部分主要来自 lwIP 内部的 `pbuf` 内存管理及 TCP 分段重组逻辑。
  - 证据强度：**弱**。目标项目自研内存管理（如 `kernel/src/mm/`）与参考项目无重合。

- **中断/异常处理**
  - 双方均存在该模块。
  - 存在共同 AST 指纹。
  - 目标覆盖率 0.81%，参考覆盖率 4.76%。覆盖率极低，几乎无重合。
  - 证据强度：**未形成**。双方中断处理逻辑完全异构。

- **系统调用**
  - 双方均存在该模块。
  - 存在共同 AST 指纹。
  - 目标覆盖率 33.64%，参考覆盖率 64.29%。重合部分为 lwIP 的 socket API 实现及 POSIX 标准系统调用名。
  - 证据强度：**弱**。同名系统调用（如 `sys_wait4`）的底层实现完全不同，目标项目有复杂的 `ptrace`、`futex` 机制，参考项目仅为简单参数传递。

- **文件系统**
  - 双方均存在该模块。
  - 存在共同 AST 指纹。
  - 目标覆盖率 7.2%，参考覆盖率 46.5%。参考项目覆盖率较高，但目标项目拥有参考项目完全不具备的 `procfs`、`epoll` 等复杂虚拟文件系统。
  - 证据强度：**弱**。重合部分有限，且目标项目文件系统抽象层次远高于参考项目。

- **设备驱动**
  - 双方均存在该模块。
  - 存在共同 AST 指纹。
  - 目标覆盖率 0.16%，参考覆盖率 5.88%。覆盖率极低，几乎无重合。
  - 证据强度：**未形成**。目标项目拥有 xHCI、HID、DRM、Sound 等现代驱动模型，参考项目仅有简单的 UART/SDIO 驱动。

- **网络协议栈**
  - 双方均存在该模块。
  - 存在共同 AST 指纹。
  - 目标覆盖率 64.48%，参考覆盖率 97.38%。覆盖率极高，是双方代码重合的唯一集中区域。
  - 证据强度：**中**。重合代码经路径与调用图谱分析，确认为双方共同引用的第三方开源库 `lwIP` 及其附带的 `PolarSSL` 加密实现。这是“公共依赖复用”，而非“对参考项目的抄袭”。

- **第三方库/公共依赖**
  - 仅目标项目体现。
  - 不存在共同 AST 指纹。
  - 目标覆盖率 0.0%，参考覆盖率 0.0%。
  - 证据强度：**未形成**。目标项目包含 `flanterm` 等参考项目没有的第三方库，进一步证明其独立性。

#### 3. 底层 AST 关键证据指纹表
| 所属板块 | 目标项目路径 | 参考项目路径 | 证据类型 | 技术说明 |
| :--- | :--- | :--- | :--- | :--- |
| 网络协议栈 | `modules/net/netserver/lwip/core/tcp.c#tcp_pcb_remove` | `kernel/lwip/core/tcp.c#tcp_pcb_remove` | 第三方库同源 | 双方均引用 lwIP 开源协议栈，`TCP_RMV`、`tcp_pcbs_sane` 等是 lwIP 内部私有宏/函数，调用图谱必然一致。 |
| 网络协议栈 | `modules/net/netserver/lwip/core/ipv6/nd6.c#nd6_process_autoconfig_prefix` | `kernel/lwip/core/ipv6/nd6.c#nd6_process_autoconfig_prefix` | 第三方库同源 | 双方均引用 lwIP，IPv6 无状态自动配置逻辑由 RFC 定义且为 lwIP 固定实现，非参考项目原创。 |
| 网络协议栈 | `modules/net/netserver/lwip/netif/ppp/polarssl/sha1.c#sha1_process` | `kernel/lwip/netif/ppp/polarssl/sha1.c#sha1_process` | 第三方库同源 | 双方均引用 lwIP 附带的 PolarSSL 加密库，`GET_ULONG_BE`、`P`、`R` 等宏是 PolarSSL 固定编码风格。 |
| 网络协议栈 | `modules/net/netserver/lwip/core/tcp_in.c#tcp_timewait_input` | `kernel/lwip/core/tcp_in.c#tcp_timewait_input` | 第三方库同源 | lwIP TCP TIME-WAIT 状态机标准实现，`TCP_SEQ_BETWEEN` 等宏调用为 lwIP 内部逻辑。 |
| 网络协议栈 | `modules/net/netserver/lwip/core/ipv4/dhcp.c#dhcp_dec_pcb_refcount` | `kernel/lwip/core/ipv4/dhcp.c#dhcp_dec_pcb_refcount` | 第三方库同源 | lwIP DHCP 客户端标准实现，`udp_remove` 调用为 lwIP 内部资源释放逻辑。 |
| 进程管理 | `kernel/src/task/task_syscall.c#sys_wait4` | `kernel/sysproc.c#sys_wait4` | 同名异构 | POSIX 标准系统调用名。目标项目调用 `arch_disable_interrupt`, `ptrace_*` 等复杂抽象；参考项目仅调用 `argaddr`, `argint`, `wait4pid`。实现完全不同。 |
| 进程管理 | `kernel/src/fs/proc/proc.c#procfs_iterate_shared` | `kernel/proc.c#procdump` | 跨板块弱信号 | 目标项目为 procfs 文件系统实现，调用 `procfs_emit_entry` 等特有抽象；参考项目为简单的进程信息打印。功能与实现均不对齐。 |
| 设备驱动 | `modules/drivers/usb/xhci-hcd.c#xhci_process_event_ring` | `kernel/sd_final.c#SDIO_WaitEvent` | 跨板块弱信号 | 目标项目为 xHCI 控制器事件环处理，涉及 `TRB_TYPE`, `dma_sync_device_to_cpu`；参考项目为 SDIO 等待事件。硬件与驱动模型完全不同。 |

#### 4. 核心代码调用流对比剖析
* **【进程管理/系统调用路径】**
* **底层技术析理**: 以 `sys_wait4` 为例，目标项目构建了复杂的进程等待与追踪（ptrace）模型，调用流涉及中断管理、用户态内存拷贝、哈希表查找、链表遍历及 ptrace 事件消费。参考项目的实现则极为简单，仅进行参数解析后直接调用 `wait4pid`。两者在**同步原语、内存模型、对象管理**上存在根本性异构。
* **代码溯源链接**:
  - 目标项目: `kernel/src/task/task_syscall.c#sys_wait4`
  - 参考项目: `kernel/sysproc.c#sys_wait4`

* **【文件系统/虚拟文件系统路径】**
* **底层技术析理**: 目标项目实现了完整的 procfs 虚拟文件系统，`procfs_iterate_shared` 函数调用流涉及 `procfs_emit_entry`、`procfs_info_task`、`procfs_ino_for` 等一系列特有抽象，用于动态生成进程信息。参考项目无此架构，其 `procdump` 函数仅为简单的 `printf` 循环。两者在**抽象层次与设计复杂度**上差异显著。
* **代码溯源链接**:
  - 目标项目: `kernel/src/fs/proc/proc.c#procfs_iterate_shared`
  - 参考项目: `kernel/proc.c#procdump`

* **【网络协议栈/第三方库路径】**
* **底层技术析理**: 双方均集成了 lwIP 协议栈。`tcp_pcb_remove` 函数的调用流完全一致，均遵循 lwIP 内部的 `TCP_RMV` 宏、`tcp_pcb_purge` 清理逻辑及 `tcp_pcbs_sane` 一致性检查。这种高度一致是**引用同一开源代码库的必然结果**，而非互相抄袭。
* **代码溯源链接**:
  - 目标项目: `modules/net/netserver/lwip/core/tcp.c#tcp_pcb_remove`
  - 参考项目: `kernel/lwip/core/tcp.c#tcp_pcb_remove`

#### 5. 单方模块与降权项
- **第三方库**: 目标项目包含 `flanterm` 等参考项目完全未出现的第三方库，是独立开发的直接证据。
- **跨板块硬凑**: 控方将大量网络（`nd6_*`, `tcp_*`）、加密（`sha1_process`, `md5_process`）、驱动（`xhci_process_event_ring`, `hid_process_field`）函数强行归入【进程管理】板块，违反了“同板块底层逻辑一致”的比对原则。这些证据已被降权为弱信号，不构成抄袭主证据。
- **POSIX 标准接口**: `sys_wait4`、`sys_fork`、`sys_kill` 等函数名是操作系统通用标准接口，同名不能作为抄袭证据。其内部实现经比对已证明完全不同。

#### 6. 保守结论
本次审计的客观 AST 相似度为 19.2%，低于通常认定抄袭的阈值。双方代码的唯一重合点高度集中在 `lwIP` 和 `PolarSSL` 这两个第三方开源库上，属于对公共上游代码的共同依赖。目标项目在进程管理、内存管理、文件系统、设备驱动等核心自研领域，展现出与参考项目截然不同的架构设计、抽象层次与调用生态，拥有大量参考项目不具备的现代操作系统特性（如 procfs、epoll、futex、ptrace、ACPI、DRM 等）。综合判定，目标项目不存在对参考项目原创代码的系统性抄袭，整体风险为低。

------------------------------

