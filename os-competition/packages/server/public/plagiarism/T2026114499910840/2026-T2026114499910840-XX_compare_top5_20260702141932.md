# 🛡️ OS 内核赛自动化代码级架构审计报告

**审计时间**: `2026-07-02 14:19:32`
**参赛作品名称**: `2026-T2026114499910840-XX`
**年份**: `2026`
**学校**: `XX`
**队伍名称**: `T2026114499910840`
**比赛名字**: `OS-Competition`
**比赛子赛道**: `Kernel-Implementation`
**仓库地址**: `https://gitlab.eduxiji.net/educg-group-43501-3132633/T2026114499910840-1796`
**推测开发语言**: `Rust / C (由AST探针自动识别)`
**特征函数量**: `6932`

## ⚔️ 底层代码同源性对比 (AST Homology)
> 本部分由专门的代码审查智能体，对该项目与符合年份条件的历届最高相似度项目进行控制流与底层调用对比。新版报告会先给出确定性的全模块覆盖矩阵，再给出 Agent 的分模块解释，避免只围绕少量抽样函数展开。

### 🏆 Top 5 底层对标: vs `2021-小骨头-重庆师范大学`（重庆师范大学 / 小骨头）

#### 对方作品信息

**对方年份**: `2021`
**对方学校**: `重庆师范大学`
**对方队伍名称**: `小骨头`
**对方比赛名字**: `OS-Competition`
**对方比赛子赛道**: `Kernel-Implementation`
**对方仓库地址**: `local-upload://oskernel2021-xbook2`

> 双向覆盖率：共同 AST 指纹: 97；唯一 AST 指纹: 目标项目 5715，参考项目 5035；覆盖率: 目标项目 1.7%，参考项目 1.93%

## 全模块对比总览（确定性证据）

> 总体覆盖率：共同 AST 指纹: 97；唯一 AST 指纹: 目标项目 5715，参考项目 5035；覆盖率: 目标项目 1.7%，参考项目 1.93%

### 1. 模块覆盖矩阵
| 模块 | 目标函数数 | 参考函数数 | 共同唯一 AST 指纹 | Target 覆盖 | Base 覆盖 | 状态 |
| :--- | ---: | ---: | ---: | ---: | ---: | :--- |
| 进程管理 | 275 | 233 | 5 | 2.08% | 2.46% | 双方存在且有结构重合 |
| 任务调度/异步运行时 | 1435 | 679 | 11 | 0.9% | 1.74% | 双方存在且有结构重合 |
| 内存管理 | 1419 | 1213 | 18 | 1.37% | 1.68% | 双方存在且有结构重合 |
| 中断/异常处理 | 149 | 246 | 4 | 3.23% | 1.89% | 双方存在且有结构重合 |
| 系统调用 | 156 | 194 | 5 | 4.67% | 5.32% | 双方存在且有结构重合 |
| 文件系统 | 1219 | 963 | 13 | 1.28% | 1.8% | 双方存在且有结构重合 |
| 设备驱动 | 747 | 455 | 12 | 1.96% | 3.07% | 双方存在且有结构重合 |
| 网络协议栈 | 593 | 383 | 32 | 6.18% | 12.26% | 双方存在且有结构重合 |
| 第三方库 | 379 | 0 | 0 | 0.0% | 0.0% | 仅目标项目体现 |
| 通用模块/其他 | 560 | 2021 | 13 | 3.13% | 0.81% | 双方存在且有结构重合 |

### 2. 分模块证据清单

#### 进程管理
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `modules/net/netserver/lwip/core/tcp_in.c#tcp_timewait_input` | `sbin/netserv/lwip/core/tcp_in.c#tcp_timewait_input` | 同板块同名 | LWIP_ASSERT, TCP_SEQ_BETWEEN, ip_current_dest_addr, ip_current_src_addr, tcp_ack_now, tcp_output ...(+1) | TCP_SEQ_BETWEEN, ip_current_dest_addr, ip_current_src_addr, tcp_output, tcp_rst |
| `modules/net/netserver/lwip/core/tcp.c#tcp_debug_print_pcbs` | `sbin/netserv/lwip/core/tcp.c#tcp_debug_print_pcbs` | 同板块同名 | LWIP_DEBUGF, tcp_debug_print_state | LWIP_DEBUGF, tcp_debug_print_state |
| `modules/net/netserver/lwip/core/tcp.c#tcp_pcb_remove` | `sbin/netserv/lwip/core/tcp.c#tcp_pcb_remove` | 同板块同名 | LWIP_ASSERT, TCP_RMV, tcp_ack_now, tcp_output, tcp_pcb_purge, tcp_pcbs_sane | LWIP_ASSERT, TCP_RMV, tcp_output, tcp_pcb_purge, tcp_pcbs_sane |
| `modules/net/netserver/lwip/core/tcp.c#tcp_kill_timewait` | `sbin/netserv/lwip/core/tcp.c#tcp_kill_timewait` | 同板块同名同结构 | (u32_t), LWIP_DEBUGF, tcp_abort | (u32_t), LWIP_DEBUGF, tcp_abort |

#### 任务调度/异步运行时
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `modules/net/netserver/lwip/core/ipv4/dhcp.c#dhcp_parse_reply` | `sbin/netserv/lwip/core/dhcp.c#dhcp_parse_reply` | 同板块同名 | (u16_t), (u8_t), LWIP_ASSERT, LWIP_DEBUGF, LWIP_DHCP_INPUT_ERROR, LWIP_HOOK_DHCP_PARSE_OPTION ...(+12) | LWIP_ASSERT, LWIP_DEBUGF, LWIP_ERROR, LWIP_MIN, dhcp_clear_all_options, dhcp_clear_option ...(+7) |
| `modules/net/netserver/lwip/core/tcp_out.c#tcp_output` | `sbin/netserv/lwip/core/tcp_out.c#tcp_output` | 同板块同名 | LWIP_ASSERT, LWIP_ASSERT_CORE_LOCKED, LWIP_DEBUGF, LWIP_MIN, TCPH_FLAGS, TCPH_SET_FLAG ...(+13) | LWIP_ASSERT, LWIP_DEBUGF, LWIP_MIN, TCPH_FLAGS, TCPH_SET_FLAG, TCP_SEQ_LT ...(+6) |
| `modules/net/netserver/lwip/core/tcp_out.c#tcp_output_segment` | `sbin/netserv/lwip/core/tcp_out.c#tcp_output_segment` | 同板块同名 | (u16_t), FOLD_U32T, IF__NETIF_CHECKSUM_ENABLED, LWIP_ASSERT, LWIP_DEBUGF, LWIP_HOOK_TCP_OUT_ADD_TCPOPTS ...(+22) | (u16_t), FOLD_U32T, LWIP_ASSERT, LWIP_DEBUGF, SWAP_BYTES_IN_WORD, TCPH_HDRLEN ...(+15) |
| `modules/net/netserver/lwip/core/tcp_in.c#tcp_parseopt` | `sbin/netserv/lwip/core/tcp_in.c#tcp_parseopt` | 同板块同名 | (u16_t), LWIP_ASSERT, LWIP_DEBUGF, TCPWND_MIN16, TCP_SEQ_BETWEEN, tcp_get_next_optbyte ...(+1) | LWIP_DEBUGF, TCPH_HDRLEN, TCP_SEQ_BETWEEN, ntohl |

#### 内存管理
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `modules/net/netserver/lwip/core/tcp_in.c#tcp_receive` | `sbin/netserv/lwip/core/tcp_in.c#tcp_receive` | 同板块同名 | (s16_t), (tcpwnd_size_t), (u16_t), (u8_t), LWIP_ASSERT, LWIP_DEBUGF ...(+39) | (s16_t), (u16_t), (u8_t), LWIP_ASSERT, LWIP_DEBUGF, TCPH_FLAGS ...(+22) |
| `modules/net/netserver/lwip/core/tcp_out.c#tcp_write` | `sbin/netserv/lwip/core/tcp_out.c#tcp_write` | 同板块同名 | LWIP_ASSERT, LWIP_ASSERT_CORE_LOCKED, LWIP_DEBUGF, LWIP_ERROR, LWIP_MAX, LWIP_MIN ...(+20) | LWIP_ASSERT, LWIP_DEBUGF, LWIP_ERROR, LWIP_MIN, LWIP_TCP_OPT_LENGTH, TCPH_SET_FLAG ...(+15) |
| `modules/net/netserver/lwip/core/tcp.c#tcp_slowtmr` | `sbin/netserv/lwip/core/tcp.c#tcp_slowtmr` | 同板块同名 | (tcpwnd_size_t), (u32_t), LWIP_ASSERT, LWIP_DEBUGF, LWIP_MIN, TCP_EVENT_ERR ...(+15) | (u32_t), LWIP_ASSERT, LWIP_DEBUGF, LWIP_MIN, TCP_EVENT_ERR, TCP_EVENT_POLL ...(+15) |
| `modules/net/netserver/lwip/core/dns.c#dns_recv` | `sbin/netserv/lwip/core/dns.c#dns_recv` | 同板块同名 | (u16_t), LWIP_DEBUGF, LWIP_DNS_ADDRTYPE_IS_IPV6, LWIP_UNUSED_ARG, PP_HTONS, dns_backupserver_available ...(+12) | (*pEntry->found), LWIP_DEBUGF, LWIP_UNUSED_ARG, PP_HTONS, SMEMCPY, dns_compare_name ...(+6) |

#### 中断/异常处理
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `kernel/src/acpi/kernel_api.c#uacpi_kernel_install_interrupt_handler` | `src/kernel/softirq.c#softirq_get_evens` | 同板块同结构 | 内部闭环逻辑 | 内部闭环逻辑 |
| `kernel/src/acpi/kernel_api.c#uacpi_kernel_uninstall_interrupt_handler` | `bin/bash/array.c#signal_is_trapped` | 同板块同结构 | 内部闭环逻辑 | 内部闭环逻辑 |
| `kernel/src/arch/x86_64/core/apic.c#apic_tlb_shootdown_ipi_handler` | `bin/bash/trap.c#set_sigchld_trap` | 同板块同结构 | arch_flush_tlb_all | set_signal |
| `kernel/src/arch/x86_64/irq/irq.c#x64_do_irq` | `src/kernel/exception.c#exception_check` | 同板块同结构 | do_irq, x64_handle_signal_on_user_return | exception_check_kernel, exception_check_user |

#### 系统调用
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `modules/net/netserver/lwip/api/netdb.c#lwip_gethostbyname` | `sbin/netserv/lwip/api/netdb.c#lwip_gethostbyname` | 同板块同名 | LWIP_DEBUGF, ipaddr_ntoa, netconn_gethostbyname, strncpy, sys_thread_hostent | LWIP_DEBUGF, ip_ntoa, netconn_gethostbyname, sys_thread_hostent |
| `modules/net/netserver/lwip/core/init.c#lwip_init` | `sbin/netserv/lwip/core/init.c#lwip_init` | 同板块同名 | LWIP_ASSERT, LWIP_CONST_CAST, LWIP_UNUSED_ARG, dns_init, etharp_init, igmp_init ...(+12) | autoip_init, dns_init, etharp_init, igmp_init, ip_init, lwip_socket_init ...(+11) |
| `modules/net/netserver/lwip/core/mem.c#mem_init` | `sbin/netserv/lwip/core/mem.c#mem_init` | 同板块同名 | LWIP_ASSERT, LWIP_MEM_ALIGN, MEM_SANITY, MEM_STATS_AVAIL, ptr_to_mem, sys_mutex_new | LWIP_ASSERT, LWIP_MEM_ALIGN, MEM_STATS_AVAIL, sys_mutex_new |
| `modules/net/netserver/sys_arch.c#sys_arch_mbox_fetch` | `sbin/netserv/lwip/arch/sys_arch.c#sys_arch_mbox_fetch` | 同板块同名 | sys_arch_sem_wait, sys_mutex_lock, sys_mutex_unlock, sys_sem_signal | LWIP_ASSERT, LWIP_DEBUGF, sys_arch_sem_wait, sys_sem_signal |

#### 文件系统
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `modules/net/netserver/lwip/core/ipv4/etharp.c#etharp_find_entry` | `sbin/netserv/lwip/netif/etharp.c#etharp_find_entry` | 同板块同名 | LWIP_ASSERT, LWIP_DEBUGF, LWIP_UNUSED_ARG, etharp_free_entry, ip4_addr_copy, ip4_addr_eq | LWIP_ASSERT, LWIP_DEBUGF, etharp_free_entry, ip_addr_cmp, ip_addr_copy |
| `modules/net/netserver/lwip/core/ipv4/ip4_frag.c#ip_reass_chain_frag_into_datagram_and_validate` | `sbin/netserv/lwip/core/ipv4/ip_frag.c#ip_reass_chain_frag_into_datagram_and_validate` | 同板块同名 | (u16_t), IPH_HL_BYTES, IPH_LEN, IPH_OFFSET_BYTES, LWIP_ASSERT, lwip_ntohs | IPH_HL, IPH_LEN, IPH_OFFSET, LWIP_ASSERT, ntohs, pbuf_clen ...(+1) |
| `modules/net/netserver/lwip/core/ipv4/dhcp.c#dhcp_handle_ack` | `sbin/netserv/lwip/core/dhcp.c#dhcp_handle_ack` | 同板块同名 | dhcp_get_option_value, dhcp_option_given, dhcp_set_ntp_servers, dns_setserver, ip4_addr_copy, ip4_addr_set_u32 ...(+4) | dhcp_get_option_value, dhcp_option_given, dns_setserver, htonl, ip4_addr_set_u32, ip_addr_copy ...(+1) |
| `kernel/src/fs/syscall/generic.c#sys_mount` | `src/fs/fsif.c#sys_mount` | 同板块同名 | copy_from_user_str, generic_mount_flags_to_vfs, vfs_do_bind_mount, vfs_do_mount, vfs_do_move_mount, vfs_do_remount ...(+6) | account_selfcheck_permission, build_path, fsif.mount, mem_copy_from_user_str |

#### 设备驱动
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `kernel/src/drivers/bus/pci.c#pci_init` | `src/arch/x86/mach-i386/bus/pci.c#pci_init` | 同板块同名 | pci_drivers[d]->match, pci_drivers[d]->probe, printk | keprint, pci_scan_all_buses, pic_get_device_connected |
| `modules/drivers/e1000/e1000.c#e1000_reset` | `src/arch/x86/mach-i386/drivers/e1000.c#e1000_reset` | 同板块同名 | e1000_read32, e1000_write32 | DEBUGFUNC, E1000_WRITE_REG, atomic_set, e1000_init_hw, e1000_phy_get_info, e1000_reset_adaptive ...(+2) |
| `kernel/src/drivers/serial.c#serial_read` | `src/arch/x86/mach-i386/drivers/serial.c#serial_read` | 同板块同名 | active_serial_driver->read | io_complete_request, keprint, uart_recv |
| `kernel/src/acpi/event.c#uacpi_install_gpe_handler_raw` | `src/arch/x86/mach-i386/drivers/e1000.c#e1000_io_read` | 同板块同结构 | do_install_gpe_handler | in32 |

#### 网络协议栈
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `modules/net/netserver/lwip/core/ipv4/dhcp.c#dhcp_bind` | `sbin/netserv/lwip/core/dhcp.c#dhcp_bind` | 同板块同名 | DHCP_SET_TIMEOUT_FROM_OFFERED_T0_LEASE, DHCP_SET_TIMEOUT_FROM_OFFERED_T1_RENEW, DHCP_SET_TIMEOUT_FROM_OFFERED_T2_REBIND, LWIP_DEBUGF, LWIP_ERROR, PP_HTONL ...(+7) | LWIP_DEBUGF, LWIP_ERROR, PP_HTONL, autoip_stop, dhcp_set_state, ip4_addr1 ...(+9) |
| `modules/net/netserver/lwip/api/sockets.c#lwip_connect` | `sbin/netserv/lwip/api/sockets.c#lwip_connect` | 同板块同名 | IP_IS_V6_VAL, IP_SET_TYPE_VAL, IS_SOCK_ADDR_ALIGNED, IS_SOCK_ADDR_LEN_VALID, IS_SOCK_ADDR_TYPE_VALID_OR_UNSPEC, LWIP_DEBUGF ...(+15) | LWIP_DEBUGF, LWIP_ERROR, err_to_errno, get_socket, inet_addr_to_ipaddr, ip_addr_debug_print ...(+4) |
| `modules/net/netserver/lwip/api/netdb.c#lwip_gethostbyname_r` | `sbin/netserv/lwip/api/netdb.c#lwip_gethostbyname_r` | 同板块同名 | LWIP_DEBUGF, LWIP_MEM_ALIGN, LWIP_MEM_ALIGN_BUFFER, MEMCPY, netconn_gethostbyname, strlen | LWIP_DEBUGF, LWIP_MEM_ALIGN, MEMCPY, netconn_gethostbyname, strlen |
| `modules/net/netserver/lwip/core/ipv4/etharp.c#etharp_tmr` | `sbin/netserv/lwip/netif/etharp.c#etharp_tmr` | 同板块同名 | LWIP_DEBUGF, etharp_free_entry, etharp_request | LWIP_DEBUGF, etharp_free_entry |

#### 第三方库
仅目标项目中观察到该模块代表函数，参考项目没有同模块可对齐证据：
- 目标项目：`kernel/src/libs/flanterm/flanterm_backends/fb.c#flanterm_fb_init`，调用摘要：_free, _malloc, convert_colour, flanterm_context_reinit, flanterm_fb_full_refresh, memcpy ...(+1)
- 目标项目：`kernel/src/libs/flanterm/flanterm.c#control_sequence_parse`，调用摘要：ctx->callback, ctx->clear, ctx->get_cursor_pos, ctx->move_character, ctx->raw_putchar, ctx->revscroll ...(+6)
- 目标项目：`kernel/src/libs/flanterm/flanterm.c#sgr`，调用摘要：(fg ? ctx->set_text_fg : ctx->set_text_bg), (fg ? ctx->set_text_fg_bright
                        : ctx->set_text_bg_bright), (fg ? ctx->set_text_fg_rgb
                        : ctx->set_text_bg_rgb), (fg ? ctx->set_text_fg_rgb : ctx->set_text_bg_rgb), ctx->set_text_bg, ctx->set_text_bg_bright ...(+7)
- 目标项目：`kernel/src/libs/flanterm/flanterm.c#unicode_to_cp437`，调用摘要：内部闭环逻辑

#### 通用模块/其他
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `kernel/src/arch/aarch64/aarch64.c#cpu_init` | `src/arch/x86/mach-i386/cpu/cpu.c#cpu_init` | 同板块同名 | 内部闭环逻辑 | 内部闭环逻辑 |
| `kernel/src/acpi/namespace.c#uacpi_namespace_node_find` | `bin/bash/variables.c#if` | 同板块同结构 | uacpi_namespace_node_resolve | get_string_value |
| `kernel/src/acpi/namespace.c#uacpi_namespace_node_resolve_from_aml_namepath` | `bin/bash/lib/intl/intl-compat.c#dcgettext` | 同板块同结构 | uacpi_namespace_node_resolve | libintl_dcgettext |
| `kernel/src/acpi/uacpi.c#uacpi_eval_simple_typed` | `bin/bash/lib/intl/intl-compat.c#bind_textdomain_codeset` | 同板块同结构 | uacpi_eval_typed | libintl_bind_textdomain_codeset |


#### 1. 宏观判定
- **定性结论**: 独立开发 (相似度: 0.91%，共同 AST 指纹: 97；唯一 AST 指纹: 目标项目 5715，参考项目 5035；覆盖率: 目标项目 1.7%，参考项目 1.93%)

#### 2. 全模块覆盖矩阵解读

- **进程管理**
  1. 双方均存在该模块。
  2. 存在 5 个共同 AST 指纹。
  3. 目标覆盖率 2.08%，参考覆盖率 2.46%，均处于极低水平。
  4. 证据强度：**弱**。共同指纹均来自 lwIP 第三方库或跨板块误归类，无原创内核代码重合。

- **任务调度/异步运行时**
  1. 双方均存在该模块。
  2. 存在 11 个共同 AST 指纹。
  3. 目标覆盖率 0.9%，参考覆盖率 1.74%，均低于 2%。
  4. 证据强度：**弱**。共同指纹集中在 lwIP 协议栈，目标项目原创调度器与参考项目无重合。

- **内存管理**
  1. 双方均存在该模块。
  2. 存在 18 个共同 AST 指纹。
  3. 目标覆盖率 1.37%，参考覆盖率 1.68%，均低于 2%。
  4. 证据强度：**弱**。共同指纹来自 lwIP 内存管理函数，非内核内存分配器。

- **中断/异常处理**
  1. 双方均存在该模块。
  2. 存在 4 个共同 AST 指纹。
  3. 目标覆盖率 3.23%，参考覆盖率 1.89%，绝对值极低。
  4. 证据强度：**未形成**。共同指纹为“内部闭环逻辑”的极简函数，区分度不足。

- **系统调用**
  1. 双方均存在该模块。
  2. 存在 5 个共同 AST 指纹。
  3. 目标覆盖率 4.67%，参考覆盖率 5.32%，绝对值极低。
  4. 证据强度：**弱**。共同指纹来自 lwIP 初始化与同步原语，非系统调用分发逻辑。

- **文件系统**
  1. 双方均存在该模块。
  2. 存在 13 个共同 AST 指纹。
  3. 目标覆盖率 1.28%，参考覆盖率 1.8%，均低于 2%。
  4. 证据强度：**弱**。共同指纹来自 lwIP 网络协议栈，非 VFS 或磁盘文件系统。

- **设备驱动**
  1. 双方均存在该模块。
  2. 存在 12 个共同 AST 指纹。
  3. 目标覆盖率 1.96%，参考覆盖率 3.07%，绝对值极低。
  4. 证据强度：**弱**。共同指纹为 `pci_init`、`e1000_reset` 等标准硬件初始化流程，由硬件规范决定。

- **网络协议栈**
  1. 双方均存在该模块。
  2. 存在 32 个共同 AST 指纹，为所有模块中最高。
  3. 目标覆盖率 6.18%，参考覆盖率 12.26%。
  4. 证据强度：**中**，但归因于共同使用的第三方开源库 lwIP，非项目间抄袭。

- **第三方库/公共依赖**
  1. 仅目标项目存在该模块（379 个函数），参考项目无。
  2. 共同 AST 指纹为 0。
  3. 目标覆盖率 0.0%，参考覆盖率 0.0%。
  4. 证据强度：**未形成**。目标项目独有的 flanterm 等库进一步证明其独立选型策略。

#### 3. 底层 AST 关键证据指纹表

| 所属板块 | 目标项目路径 | 参考项目路径 | 证据类型 | 技术说明 |
| :--- | :--- | :--- | :--- | :--- |
| 网络协议栈 | `modules/net/netserver/lwip/core/tcp.c#tcp_kill_timewait` | `sbin/netserv/lwip/core/tcp.c#tcp_kill_timewait` | 同板块同名同结构 | 均为 lwIP 官方源码的标准函数，调用 `(u32_t), LWIP_DEBUGF, tcp_abort`，由 TCP Time-Wait 状态机协议语义决定。 |
| 网络协议栈 | `modules/net/netserver/lwip/core/tcp.c#tcp_pcbs_sane` | `sbin/netserv/lwip/core/tcp.c#tcp_pcbs_sane` | 同板块同名同结构 | 均为 lwIP 官方源码的断言包装函数，仅调用 `LWIP_ASSERT`，极简闭环逻辑区分度极低。 |
| 网络协议栈 | `modules/net/netserver/lwip/core/tcp.c#tcp_debug_print_pcbs` | `sbin/netserv/lwip/core/tcp.c#tcp_debug_print_pcbs` | 同板块同名 | lwIP 标准调试打印函数，调用 `LWIP_DEBUGF, tcp_debug_print_state`，未做无意义改写。 |
| 网络协议栈 | `modules/net/netserver/lwip/core/tcp_in.c#tcp_timewait_input` | `sbin/netserv/lwip/core/tcp_in.c#tcp_timewait_input` | 同板块同名 | lwIP 标准函数，目标项目额外调用 `LWIP_ASSERT, tcp_ack_now`，反映版本或配置差异。 |
| 网络协议栈 | `modules/net/netserver/lwip/core/tcp.c#tcp_pcb_remove` | `sbin/netserv/lwip/core/tcp.c#tcp_pcb_remove` | 同板块同名 | lwIP 标准函数，目标项目额外调用 `tcp_ack_now`，为正常工程决策。 |
| 进程管理 | `kernel/src/task/task.c#task_find_by_pid` | `src/task/task.c#task_find_by_pid` | 同板块同名异构 | 目标使用 `spin_lock/spin_unlock` + 哈希表，参考使用 `interrupt_save_and_disable` + 链表，底层同步与数据结构完全不同。 |
| 进程管理 | `kernel/src/task/task_syscall.c#sys_waitid` | `bin/bash/jobs.c#waitchld` | 跨板块弱信号 | 目标为内核态 syscall，参考为用户态 Shell 作业控制，调用对象 `copy_to_user` vs `WAITPID`，不构成同板块证据。 |
| 进程管理 | `kernel/src/task/task_syscall.c#task_execve` | `src/task/exec.c#proc_execve` | 跨板块弱信号 | 目标调用 `task_do_execve`，参考调用 `build_path, do_execute`，执行加载链路完全不同。 |
| 第三方库 | `kernel/src/libs/flanterm/flanterm.c#control_sequence_parse` | — | 单方模块 | 目标项目独有的终端模拟库，参考项目无此模块。 |
| 第三方库 | `kernel/src/libs/flanterm/flanterm.c#sgr` | — | 单方模块 | 目标项目独有的终端颜色控制库，参考项目无此模块。 |

#### 4. 核心代码调用流对比剖析

* **【进程管理 / 任务查找路径】**
* **底层技术析理**: 目标项目 `task_find_by_pid` 采用 `spin_lock/spin_unlock` 自旋锁保护 `task_lookup_by_pid_nolock` 哈希表查找，体现了基于哈希表的 O(1) 查找与细粒度自旋锁同步策略。参考项目 `task_find_by_pid` 采用 `interrupt_save_and_disable/interrupt_restore_state` 关中断保护 `list_for_each_owner` 链表遍历，体现了基于链表的 O(n) 遍历与关中断全局同步策略。两者在数据结构选择、锁机制、中断处理模型上存在根本性架构差异。
* **代码溯源链接**:
  - 目标项目: `kernel/src/task/task.c#task_find_by_pid`
  - 参考项目: `src/task/task.c#task_find_by_pid`

* **【系统调用 / 进程等待路径】**
* **底层技术析理**: 目标项目 `sys_waitid` 与 `sys_wait4` 为内核态系统调用，调用链涉及 `arch_disable_interrupt`、`copy_to_user`、`ptrace_consume_wait_event` 等内核原语，体现了内核态等待与 ptrace 集成的设计。参考项目 `waitchld` 与 `wait_for` 为用户态 Shell 作业控制函数，调用链涉及 `WAITPID`、`WIFEXITED`、`WIFSIGNALED` 等 POSIX 宏，体现了用户态进程管理逻辑。两者运行层级、调用对象、同步机制完全不同。
* **代码溯源链接**:
  - 目标项目: `kernel/src/task/task_syscall.c#sys_waitid`
  - 参考项目: `bin/bash/jobs.c#waitchld`

* **【文件系统 / procfs 路径】**
* **底层技术析理**: 目标项目 `procfs_iterate_shared` 与 `procfs_lookup` 基于 `hashmap_entry_is_occupied`、`procfs_new_inode`、`vfs_inode_permission` 等内核 VFS 接口，体现了完整的 procfs 虚拟文件系统实现。参考项目无对应 procfs 实现，其文件系统证据来自 `etharp_find_entry` 等 lwIP 网络函数，与 procfs 无任何关联。
* **代码溯源链接**:
  - 目标项目: `kernel/src/fs/proc/proc.c#procfs_iterate_shared`
  - 参考项目: 无对应实现

* **【设备驱动 / PCI 初始化路径】**
* **底层技术析理**: 目标项目 `pci_init` 调用 `pci_drivers[d]->match` 与 `pci_drivers[d]->probe`，采用驱动匹配-探测模型。参考项目 `pci_init` 调用 `pci_scan_all_buses` 与 `pic_get_device_connected`，采用总线扫描-中断控制器查询模型。两者 PCI 枚举策略不同。
* **代码溯源链接**:
  - 目标项目: `kernel/src/drivers/bus/pci.c#pci_init`
  - 参考项目: `src/arch/x86/mach-i386/bus/pci.c#pci_init`

* **【网络协议栈 / lwIP TCP 路径】**
* **底层技术析理**: 双方 `tcp_kill_timewait`、`tcp_pcbs_sane`、`tcp_debug_print_pcbs` 等函数调用图谱高度一致，但均来自 lwIP 官方源码。目标项目路径 `modules/net/netserver/lwip/` 与参考项目路径 `sbin/netserv/lwip/` 均符合 lwIP 目录结构，说明双方独立移植自上游 lwIP，非项目间复制。
* **代码溯源链接**:
  - 目标项目: `modules/net/netserver/lwip/core/tcp.c#tcp_kill_timewait`
  - 参考项目: `sbin/netserv/lwip/core/tcp.c#tcp_kill_timewait`

#### 5. 单方模块与降权项

- **第三方库（仅目标项目）**: `kernel/src/libs/flanterm/` 下的 `flanterm_fb_init`、`control_sequence_parse`、`sgr`、`unicode_to_cp437` 等函数为 flanterm 终端模拟库，参考项目无此模块。这证明目标项目有独立的第三方库选型，而非从参考项目继承。
- **跨板块硬凑项**: 控方将 `modules/drivers/hid/hid.c#hid_process_field`（HID 驱动）、`modules/drivers/usb/xhci-hcd.c#xhci_process_event_ring`（USB 驱动）、`kernel/src/acpi/namespace.c#uacpi_namespace_do_for_each_child`（ACPI 固件接口）、`modules/net/netserver/lwip/netif/ppp/polarssl/sha1.c#sha1_process`（密码学标准算法）等函数均归入“进程管理”板块，与参考项目的 `bin/bash/`（Shell）、`bin/mp3player/`（媒体播放器）、`libs/xlibc/`（用户态 C 库）进行比对，属于跨领域硬凑，不构成同板块证据。
- **泛型宏与标准库函数**: `MIN`、`MAX`、`memcpy`、`memset`、`snprintf`、`strlen`、`strcmp`、`free` 等属于 C 标准库或泛型宏，在任何 C 项目中高频出现，不能作为项目间复用的指纹。
- **极简闭环函数**: `uacpi_namespace_node_parent`、`tcp_trigger_input_pcb_close`、`sys_get_ppid`、`stop_making_children` 等标记为“内部闭环逻辑”的函数，AST 指纹区分度极低，极易产生哈希碰撞，降级为弱信号。

#### 6. 保守结论

目标项目与参考项目的客观 AST 相似度仅为 0.91%，共同指纹 97 个，覆盖率均低于 2%，处于大型 C 项目因使用相同第三方库和标准算法而产生的背景噪声区间。所有显著重合均集中在 lwIP 网络协议栈，且被指控函数均为 lwIP 官方标准函数，双方路径均符合 lwIP 目录结构，说明相似性来源于共同上游开源库，而非项目间代码复用。目标项目在进程管理、任务调度、内存管理、文件系统、设备驱动等原创内核模块上，与参考项目在数据结构、同步原语、中断模型等方面存在根本性架构差异。控方存在跨板块硬凑证据、泛型宏误判、极简函数区分度不足等方法论缺陷，且未提供提交历史或作者接触链等直接证据。综合判定，目标项目为独立开发，不存在对参考项目的代码抄袭或违规复用。

------------------------------

