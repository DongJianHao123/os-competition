# 🛡️ OS 内核赛自动化代码级架构审计报告

**审计时间**: `2026-07-02 02:49:31`
**参赛作品名称**: `2026-T2026100079911061-XX`
**年份**: `2026`
**学校**: `XX`
**队伍名称**: `T2026100079911061`
**比赛名字**: `OS-Competition`
**比赛子赛道**: `Kernel-Implementation`
**仓库地址**: `https://gitlab.eduxiji.net/educg-group-43501-3132633/T2026100079911061-2137`
**推测开发语言**: `Rust / C (由AST探针自动识别)`
**特征函数量**: `2788`

## ⚔️ 底层代码同源性对比 (AST Homology)
> 本部分由专门的代码审查智能体，对该项目与符合年份条件的历届最高相似度项目进行控制流与底层调用对比。新版报告会先给出确定性的全模块覆盖矩阵，再给出 Agent 的分模块解释，避免只围绕少量抽样函数展开。

### 🏆 Top 2 底层对标: vs `2022-啊队队队-河南科技大学`（河南科技大学 / 啊队队队）

#### 对方作品信息

**对方年份**: `2022`
**对方学校**: `河南科技大学`
**对方队伍名称**: `啊队队队`
**对方比赛名字**: `OS-Competition`
**对方比赛子赛道**: `Kernel-Implementation`
**对方仓库地址**: `https://gitlab.eduxiji.net/alexsondes/oskernel2022-byte-os.git`

> 双向覆盖率：共同 AST 指纹: 13；唯一 AST 指纹: 目标项目 2560，参考项目 759；覆盖率: 目标项目 0.51%，参考项目 1.71%

## 全模块对比总览（确定性证据）

> 总体覆盖率：共同 AST 指纹: 13；唯一 AST 指纹: 目标项目 2560，参考项目 759；覆盖率: 目标项目 0.51%，参考项目 1.71%

### 1. 模块覆盖矩阵
| 模块 | 目标函数数 | 参考函数数 | 共同唯一 AST 指纹 | Target 覆盖 | Base 覆盖 | 状态 |
| :--- | ---: | ---: | ---: | ---: | ---: | :--- |
| 进程管理 | 642 | 41 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |
| 任务调度/异步运行时 | 709 | 122 | 1 | 0.15% | 0.91% | 双方存在且有结构重合 |
| 内存管理 | 297 | 190 | 1 | 0.35% | 0.59% | 双方存在且有结构重合 |
| 中断/异常处理 | 21 | 15 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |
| 系统调用 | 451 | 9 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |
| 文件系统 | 493 | 436 | 1 | 0.22% | 0.26% | 双方存在且有结构重合 |
| 设备驱动 | 27 | 44 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |
| 网络协议栈 | 0 | 0 | 0 | 0.0% | 0.0% | 双方均未体现 |
| 文件系统/第三方库 | 131 | 0 | 0 | 0.0% | 0.0% | 仅目标项目体现 |
| 通用模块/其他 | 17 | 25 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |

### 2. 分模块证据清单

#### 进程管理
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `kernel/src/syscall/process_ops.rs#sys_wait4` | `kernel/src/sys_call/task/fork.rs#sys_wait4` | 同板块同名 | current, linux_error, report_wait4_event, scan_wait_children_by_target, schedule, wait4_target | Err, Ok, cprocess.exit_code.unwrap, cprocess_vec.len, cprocess_vec[0].borrow, debug! ...(+14) |
| `kernel/src/syscall/process_ops.rs#sys_clone` | `kernel/src/sys_call/task/fork.rs#sys_clone` | 同板块同名 | sys_clone_common | Ok, Task::new, add_task_to_scheduler, debug!, drop, inner.process.clone ...(+12) |
| `kernel/src/task/process.rs#release` | `kernel/src/task/process.rs#release` | 同板块同名 | crate::syscall::release_stage1_shm_attachment, scratchfs::release_file | self.heap.mem_set.release, self.mem_set.release, self.pmm.release, self.stack.release |
| `kernel/src/syscall/process_ops.rs#sys_exit` | `kernel/src/sys_call/task/exit.rs#sys_exit` | 同板块同名 | finish_current_process | Err, clear_child_tid.is_valid, clear_child_tid.transfer, inner.process.borrow_mut, inner.process.borrow_mut().exit, self.clear_child_tid.borrow ...(+3) |

#### 任务调度/异步运行时
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `kernel/src/syscall/io_ops.rs#sys_read` | `kernel/src/sys_call/fd/rw.rs#sys_read` | 同板块同名 | SocketOpenFile::from_fd, Some, buf.add, console::read_char, core::ptr::null_mut, ensure_user_range ...(+16) | Ok, buf_ptr.transfer_vec, debug!, drop, inner.process.borrow_mut, process.fd_table.get ...(+3) |
| `kernel/src/syscall/fs_ops.rs#sys_readlinkat` | `kernel/src/sys_call/fd/open.rs#sys_readlinkat` | 同板块同名 | copy_user_cstr, fs_path::has_trailing_slash, fs_path::resolve_stage1_path, fs_path::resolve_stage1_path_with_policy, linux_error, path_ptr.is_null ...(+7) | "/lmbench_all".to_string, Ok, buf.transfer_vec, buf[..path.len()].copy_from_slice, debug!, path.as_bytes ...(+3) |
| `kernel/src/task/scratchfs.rs#read_at` | `kernel/src/task/pipe.rs#read_at` | 同板块同名 | (count - copied).min, Ok, ensure_defaults, file_node, out.len, out.len().min ...(+3) | buf.len, pipe.buf.len, self.0.borrow_mut |
| `kernel/src/task/timerfd.rs#refresh` | `kernel/src/interrupt/timer.rs#refresh` | 同板块同名 | 1usize.saturating_add, now.saturating_add, now.saturating_sub, slot.interval_ticks.saturating_mul, slot.next_deadline.saturating_add, slot.pending.saturating_add | 内部闭环逻辑 |

#### 内存管理
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `kernel/src/main.rs#rust_main` | `kernel/src/main.rs#rust_main` | 同板块同名 | AddressSpace::new, FsContext::new, PTEFlags::new, PhysAddr, ProcessIdentity::new, RlimitContext::new ...(+47) | "proc".to_string, INode::get, INode::get(None, "busybox").expect, INode::get(None, "lmbench_all").expect, INode::mkdir, INode::mkdir(None, "/bin", 0).expect ...(+20) |
| `kernel/src/syscall/memory_ops/mapping.rs#sys_mmap` | `kernel/src/sys_call/mm.rs#sys_mmap` | 同板块同名 | MmapFileBacking::Ext4, MmapFileBacking::Rootfs, PTEFlags::new, Some, address_space.mmap_limit, backing.release ...(+30) | MapFlags::from_bits_truncate, MemMap::new, Ok, VirtAddr::from, VirtAddr::from(start).into, debug! ...(+18) |
| `kernel/src/task/inet_socket.rs#alloc` | `kernel/src/memory/page.rs#alloc` | 同板块同名 | INET_SOCKETS.iter_mut, INET_SOCKETS.iter_mut().enumerate, Some, slot.init | Err, Ok, PhysPageNum::from, init_pages, self.pages.len |
| `kernel/src/task/eventfd.rs#alloc` | `kernel/src/task/fd_table.rs#alloc` | 同板块同名 | EVENTFDS.as_mut_ptr, EventFd, Some, ptr.add | (0..).find, (0..).find(|fd| !self.0.contains_key(fd)).unwrap, self.0.contains_key |

#### 中断/异常处理
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `kernel/src/syscall/signal.rs#sys_rt_sigsuspend` | `kernel/src/interrupt/timer.rs#now` | 同模块上下文对齐/差异观察 | arm_sigsuspend_restore_current, block_on_sigsuspend, current, current_sigsuspend_ready_signal, finish_sigsuspend_immediate_stop, linux_error ...(+3) | time::read |
| `kernel/src/syscall/signal.rs#sys_sigaltstack` | `kernel/src/interrupt/mod.rs#init` | 同模块上下文对齐/差异观察 | Some, current, linux_error, new_stack.is_null, old_stack.is_null, read_signal_alt_stack ...(+3) | asm!, info!, timer::init |
| `kernel/src/syscall/signal.rs#stage1_signal_terminates` | `kernel/src/interrupt/timer.rs#timer_handler` | 同模块上下文对齐/差异观察 | matches! | NEXT_TICKS.force_get, NEXT_TICKS.force_get().need_change, set_next_timeout |
| `kernel/src/trap.rs#user_sync_exception_handler` | `kernel/src/interrupt/mod.rs#clone_from` | 同模块上下文对齐/差异观察 | deliver_or_finish_sigbus, deliver_or_finish_sigill, deliver_or_finish_sigsegv, deliver_or_finish_sigtrap | 内部闭环逻辑 |

#### 系统调用
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `kernel/src/syscall/poll_ops.rs#sys_ppoll` | `kernel/src/sys_call/net.rs#read_at` | 同模块上下文对齐/差异观察 | apply_poll_signal_mask, block_on_poll_timeout, block_on_pollfd_set, block_on_pollfd_set_until, block_on_signal_only_poll, ensure_user_range ...(+15) | buf.len, queue.pop_front, self.0.borrow_mut |
| `kernel/src/syscall/futex_ops.rs#sys_futex` | `kernel/src/sys_call/net.rs#write_at` | 同模块上下文对齐/差异观察 | FutexOp::decode, Some, block_on_futex, futex_cmp_requeue_pi, futex_key, futex_lock_pi ...(+14) | buf.len, queue.push_back, self.0.borrow_mut |
| `kernel/src/syscall/socket_ops/sockopt.rs#sys_setsockopt` | `kernel/src/sys_call/mod.rs#is_vfork_wait` | 同模块上下文对齐/差异观察 | core::mem::size_of::<u32>, i32::from, linux_error, normalize_socket_buffer_size, read_socket_timeout_ticks, read_user_i32 ...(+11) | VFORK_WAIT_LIST.lock, vfork_wait_list.iter, vfork_wait_list.iter().find, vfork_wait_list.iter().find(|&&x| x == pid).is_some |
| `kernel/src/syscall/socket_ops/sockopt.rs#sys_getsockopt` | `kernel/src/sys_call/mod.rs#remove_vfork_wait` | 同模块上下文对齐/差异观察 | bytes.len, core::cmp::min, get_inet_protocol_sockopt, linux_error, optval.is_null, read_user_u32 ...(+18) | VFORK_WAIT_LIST.lock, vfork_wait_list.drain_filter |

#### 文件系统
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `kernel/src/syscall/fs_mutation.rs#truncate` | `fatfs/src/file.rs#truncate` | 同板块同名 | copy_user_cstr, fs_path::resolve_stage1_path, inotify::notify_modify, linux_error, scratch_error_to_errno, scratchfs::file_path ...(+1) | Ok, debug_assert!, e.set_first_cluster, e.set_size, panic!, self.fs.clone ...(+3) |
| `kernel/src/syscall/fs_mutation.rs#linkat` | `kernel/src/fs/filetree.rs#linkat` | 同板块同名 | link_file_to_path, link_tmpfile_to_path, linkat_old_kind, linux_error | INode::get, INode::get(None, path).expect, INode::root, Self::new, filename.to_string, inner.file.clone ...(+4) |
| `kernel/src/syscall/fs_path.rs#root` | `kernel/src/fs/filetree.rs#root` | 同板块同名 | 内部闭环逻辑 | data.clone, todo! |
| `kernel/src/virtio_net.rs#read_u8` | `fatfs/src/io.rs#read_u8` | 同板块同名 | read_volatile | Ok, self.read_exact |

#### 设备驱动
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `kernel/src/virtio_blk.rs#read_sector` | `kernel/src/device/sdcard.rs#read_sector` | 同板块同名 | disable_interrupts, read_sector_inner, restore_interrupts | Err, Ok, assert!, chunk.iter_mut, chunk.iter_mut().zip, data_buf.chunks_mut ...(+6) |
| `kernel/src/console.rs#puts` | `device/src/console.rs#puts` | 同板块同名 | UART.putstr | console_putchar |
| `kernel/src/mm/frame.rs#remove_free_block` | `kernel/src/device/sdcard.rs#write_sector` | 同模块上下文对齐/差异观察 | FREE_AREA_HEADS[order].load, FREE_AREA_HEADS[order].store, FREE_NEXT[current as usize].load, FREE_NEXT[current as usize].store, FREE_NEXT[previous as usize].store, FREE_ORDER[current as usize].store | (data_buf.len() / SEC_LEN).try_into, (data_buf.len() / SEC_LEN).try_into().unwrap, Err, Ok, assert!, chunk.iter ...(+9) |
| `kernel/src/virtio_blk.rs#probe_and_init` | `kernel/src/device/sdcard.rs#get_cidregister` | 同模块上下文对齐/差异观察 | Some, init_device, mmio.read | Err, Ok, self.end_cmd, self.get_response, self.read_data, self.send_cmd ...(+2) |

#### 文件系统/第三方库
仅目标项目中观察到该模块代表函数，参考项目没有同模块可对齐证据：
- 目标项目：`kernel/src/syscall/fs_ops.rs#sys_getdents64_ext4_batch`，调用摘要：GetdentsWriter::new, Some, crate::ext4::visit_dir_visible_entries_from, crate::ext4::visit_root_visible_entries_from, fs_path::root_ext4_start_index, fs_path::scratch_overlay_start_index ...(+5)
- 目标项目：`kernel/src/ext4.rs#read_inode_data_at`，调用摘要：Err, Ok, core::cmp::min, debug_assert!, inode.data_extent_for_block, out.len ...(+4)
- 目标项目：`kernel/src/ext4.rs#parse_superblock`，调用摘要：Err, Ok, inode_size.is_power_of_two, read_u16, read_u32, volume_name.copy_from_slice
- 目标项目：`kernel/src/ext4.rs#walk_dir_entries`，调用摘要：Err, Ok, core::cmp::min, debug_assert!, dir.data_extent_for_block, dir.is_stage1_dir ...(+6)

#### 通用模块/其他
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `kernel/src/sbi.rs#sbi_call` | `kernel/src/sbi.rs#sbi_call` | 同板块同名 | core::arch::asm! | asm! |
| `kernel/src/sbi.rs#shutdown` | `kernel/src/sbi.rs#shutdown` | 同板块同名 | core::arch::asm!, sbi_call | sbi_call, unreachable! |
| `kernel/src/timer.rs#set_timer` | `kernel/src/sbi.rs#set_timer` | 同板块同名 | core::arch::asm! | sbi_call |
| `kernel/src/elf.rs#parse` | `kernel/src/sync/mutex.rs#obtain_lock` | 同模块上下文对齐/差异观察 | ELF_MAGIC.as_slice, Err, Ok, Some, checked_range, elf.load_segments ...(+9) | self.lock.compare_exchange, self.lock.load, spin_loop |


### 1. 宏观判定
- **定性结论**: **独立开发 / 底层架构显著差异**
- **核心依据**: 客观 AST 相似度仅为 **0.39%**，共同 AST 指纹仅 13 个，双向覆盖率均低于 2%（目标项目 0.51%，参考项目 1.71%）。在控辩双方聚焦的【进程管理】板块，共同唯一 AST 指纹为 **0**，覆盖率均为 **0.0%**。双方在内存模型、同步原语、调度粒度及文件系统架构上存在根本性异构，不存在可量化的同源结构复用。

### 2. 全模块覆盖矩阵解读
- **进程管理**: 双方均存在该模块。共同 AST 指纹为 0，覆盖率均为 0.0%。目标项目实现了包含 `pidfd`、`clone3`、线程组僵尸回收等复杂 Linux 语义，参考项目为基于 `Rc<RefCell<>>` 的简化教学模型。**证据强度：未形成**。
- **任务调度/异步运行时**: 双方均存在该模块。存在 1 个共同指纹，覆盖率极低（目标 0.15%，参考 0.91%）。目标项目采用全局 `PROCESS_LIST` 与阻塞原语调度，参考项目为简单的任务队列轮转。**证据强度：弱**。
- **内存管理**: 双方均存在该模块。存在 1 个共同指纹，覆盖率极低（目标 0.35%，参考 0.59%）。目标项目包含 `ext4` 文件映射与 `stage1` 共享内存，参考项目为基于 `MemSet` 的物理页分配。**证据强度：弱**。
- **中断/异常处理**: 双方均存在该模块。共同指纹为 0，覆盖率均为 0.0%。目标项目包含完整的信号交付与 `sigaltstack` 处理，参考项目仅为基础时钟与中断初始化。**证据强度：未形成**。
- **系统调用**: 双方均存在该模块。共同指纹为 0，覆盖率均为 0.0%。目标项目包含 `futex`、`ppoll`、`getsockopt` 等复杂同步与网络系统调用，参考项目仅包含基础的 `read_at`/`write_at`。**证据强度：未形成**。
- **文件系统**: 双方均存在该模块。存在 1 个共同指纹，覆盖率极低（目标 0.22%，参考 0.26%）。目标项目实现了完整的 `ext4` 只读支持与 `procfs` 目录树，参考项目为简单的 `FAT`/`INode` 与两个静态伪文件。**证据强度：弱**。
- **设备驱动**: 双方均存在该模块。共同指纹为 0，覆盖率均为 0.0%。目标项目包含 `virtio_blk` 与 `virtio_net` 驱动，参考项目为 `sdcard` 驱动。**证据强度：未形成**。
- **网络协议栈**: 双方均未体现该模块。**证据强度：未形成**。
- **第三方库/公共依赖**: 仅目标项目体现（`ext4` 解析等），参考项目无此模块。**证据强度：未形成**。

### 3. 底层 AST 关键证据指纹表
| 所属板块 | 目标项目路径 | 参考项目路径 | 证据类型 | 技术说明 |
| :--- | :--- | :--- | :--- | :--- |
| 任务调度/异步运行时 | `kernel/src/syscall/io_ops.rs#sys_read` | `kernel/src/sys_call/fd/rw.rs#sys_read` | 同板块同名 | 均为 POSIX 标准系统调用入口。目标项目调用 `SocketOpenFile::from_fd` 等复杂多态对象，参考项目仅通过 `fd_table.get` 获取文件句柄，底层 I/O 多路复用模型完全不同。 |
| 任务调度/异步运行时 | `kernel/src/syscall/fs_ops.rs#sys_readlinkat` | `kernel/src/sys_call/fd/open.rs#sys_readlinkat` | 同板块同名 | 均为 Linux 标准路径解析调用。目标项目调用 `resolve_stage1_path_with_policy` 进行多层路径解析，参考项目直接进行字符串拷贝，路径解析架构异构。 |
| 任务调度/异步运行时 | `kernel/src/task/scratchfs.rs#read_at` | `kernel/src/task/pipe.rs#read_at` | 同板块同名 | 均为 Rust 标准 `Read` trait 方法。目标项目操作 `scratchfs` 伪文件系统节点，参考项目操作管道缓冲区，外部对象与数据结构无关联。 |
| 任务调度/异步运行时 | `kernel/src/task/timerfd.rs#refresh` | `kernel/src/interrupt/timer.rs#refresh` | 同板块同名 | 同名方法但分属不同模块。目标项目为 `timerfd` 定时器刷新，参考项目为时钟中断刷新，功能语义与调用上下文完全不同。 |
| 内存管理 | `kernel/src/main.rs#rust_main` | `kernel/src/main.rs#rust_main` | 同板块同名 | 内核入口点。目标项目初始化 `AddressSpace`、`FsContext` 等复杂子系统，参考项目挂载 `busybox` 并创建目录，初始化流程与资源模型截然不同。 |
| 内存管理 | `kernel/src/syscall/memory_ops/mapping.rs#sys_mmap` | `kernel/src/sys_call/mm.rs#sys_mmap` | 同板块同名 | POSIX 标准内存映射调用。目标项目支持 `Ext4` 文件 backing，参考项目仅支持匿名映射，内存映射后端模型异构。 |
| 内存管理 | `kernel/src/task/inet_socket.rs#alloc` | `kernel/src/memory/page.rs#alloc` | 同板块同名 | 同名方法但分属不同模块。目标项目分配网络套接字槽位，参考项目分配物理页，分配器管理的资源类型完全不同。 |
| 内存管理 | `kernel/src/task/eventfd.rs#alloc` | `kernel/src/task/fd_table.rs#alloc` | 同板块同名 | 同名方法但分属不同模块。目标项目分配 `eventfd` 对象，参考项目分配文件描述符，资源池实现与索引策略不同。 |
| 文件系统 | `kernel/src/syscall/fs_mutation.rs#truncate` | `fatfs/src/file.rs#truncate` | 同板块同名 | 文件截断操作。目标项目通过 `scratchfs::file_path` 操作伪文件系统，参考项目直接操作 `FAT` 簇链，底层文件系统实现完全不同。 |
| 文件系统 | `kernel/src/syscall/fs_mutation.rs#linkat` | `kernel/src/fs/filetree.rs#linkat` | 同板块同名 | POSIX 标准硬链接调用。目标项目调用 `link_file_to_path` 等内部抽象，参考项目通过 `INode::get` 操作目录树，文件系统组织模型异构。 |
| 文件系统 | `kernel/src/syscall/fs_path.rs#root` | `kernel/src/fs/filetree.rs#root` | 同板块同名 | 获取根目录方法。目标项目为内部闭环逻辑，参考项目返回 `data.clone`，根目录表示与存储方式不同。 |
| 设备驱动 | `kernel/src/virtio_blk.rs#read_sector` | `kernel/src/device/sdcard.rs#read_sector` | 同板块同名 | 块设备扇区读取。目标项目通过 `disable_interrupts`/`restore_interrupts` 保护临界区，参考项目通过 `assert!` 与 `chunks_mut` 处理数据缓冲，硬件交互协议与同步策略不同。 |
| 设备驱动 | `kernel/src/console.rs#puts` | `device/src/console.rs#puts` | 同板块同名 | 控制台输出。目标项目调用 `UART.putstr`，参考项目调用 `console_putchar`，底层 UART 抽象与输出粒度不同。 |
| 通用模块/其他 | `kernel/src/sbi.rs#sbi_call` | `kernel/src/sbi.rs#sbi_call` | 同板块同名 | RISC-V SBI 标准调用。两者均使用 `asm!` 宏执行 `ecall` 指令，属于 RISC-V 特权规范规定的标准调用约定，不构成项目间特有复用。 |
| 通用模块/其他 | `kernel/src/sbi.rs#shutdown` | `kernel/src/sbi.rs#shutdown` | 同板块同名 | RISC-V SBI 标准关机调用。均通过 `sbi_call` 实现，属于 SBI 规范强制接口。 |
| 通用模块/其他 | `kernel/src/timer.rs#set_timer` | `kernel/src/sbi.rs#set_timer` | 同板块同名 | RISC-V SBI 标准定时器调用。均通过 `sbi_call` 或 `asm!` 实现，属于 SBI 规范强制接口。 |

### 4. 核心代码调用流对比剖析
- **【进程管理/进程创建】**
  - **底层技术析理**: 目标项目 `sys_clone` 直接委派给 `sys_clone_common`，该函数操作 `clone_pidfd`、`BlockReason::VforkChild` 等复杂对象，并通过 `complete_blocking_syscall` 与全局调度器交互。参考项目 `sys_clone` 直接调用 `Task::new` 创建任务，并通过 `add_task_to_scheduler` 加入简单队列。两者在进程表示（全局数组索引 vs. `Rc<RefCell<>>`）、同步原语（阻塞原语 vs. 无）和克隆语义（支持 `clone3`/`pidfd` vs. 基础 `fork`）上存在根本性架构差异。
  - **代码溯源链接**:
    - 目标项目: `kernel/src/syscall/process_ops.rs#sys_clone`
    - 参考项目: `kernel/src/sys_call/task/fork.rs#sys_clone`

- **【进程管理/进程退出】**
  - **底层技术析理**: 目标项目 `sys_exit` 调用 `finish_current_process` 进行复杂的生命周期终结，`release` 函数释放 `stage1_shm_attachment` 与 `scratchfs` 资源。参考项目 `sys_exit` 直接调用 `inner.process.borrow_mut().exit`，`release` 函数释放 `pmm` 物理页与 `stack`。两者释放的外部资源对象（`scratchfs`/`shm` vs. `pmm`/`stack`）和内存管理模型完全不同。
  - **代码溯源链接**:
    - 目标项目: `kernel/src/syscall/process_ops.rs#sys_exit`
    - 参考项目: `kernel/src/sys_call/task/exit.rs#sys_exit`

- **【任务调度/异步运行时/阻塞等待】**
  - **底层技术析理**: 目标项目 `sys_wait4` 调用 `scan_wait_children_by_target`，内部使用 `for_each_process` 遍历全局进程表，并匹配 `WaitEventKind::Exited` 等复杂事件。参考项目 `sys_wait4` 直接索引 `cprocess_vec` 数组并 `unwrap` 退出码。两者在进程遍历机制（全局表回调 vs. 局部向量索引）和等待语义（支持僵尸回收、进程组 vs. 简单退出码提取）上异构。
  - **代码溯源链接**:
    - 目标项目: `kernel/src/syscall/process_ops.rs#sys_wait4`
    - 参考项目: `kernel/src/sys_call/task/fork.rs#sys_wait4`

- **【内存管理/内存映射】**
  - **底层技术析理**: 目标项目 `sys_mmap` 支持 `MmapFileBacking::Ext4`，允许将 ext4 文件直接映射到用户空间。参考项目 `sys_mmap` 仅支持 `MemMap::new` 匿名映射。两者在内存映射后端（文件支持 vs. 纯匿名）和页错误处理路径上存在本质区别。
  - **代码溯源链接**:
    - 目标项目: `kernel/src/syscall/memory_ops/mapping.rs#sys_mmap`
    - 参考项目: `kernel/src/sys_call/mm.rs#sys_mmap`

- **【文件系统/文件操作】**
  - **底层技术析理**: 目标项目 `truncate` 通过 `scratchfs::file_path` 解析路径并操作伪文件系统。参考项目 `truncate` 直接操作 `FAT` 文件系统的簇链（`e.set_first_cluster`）。两者底层文件系统实现（`scratchfs`/`ext4` vs. `FAT`）和 inode 管理模型完全不同。
  - **代码溯源链接**:
    - 目标项目: `kernel/src/syscall/fs_mutation.rs#truncate`
    - 参考项目: `fatfs/src/file.rs#truncate`

### 5. 单方模块与降权项
- **单方模块**: 目标项目独有的【文件系统/第三方库】模块（含 `ext4` 解析、`sys_getdents64_ext4_batch` 等）在参考项目中无任何对应实现，属于完全独立开发的功能集，不构成同源证据。
- **降权项说明**: 全模块对比中出现的同名函数（如 `sys_clone`、`sys_wait4`、`sys_read`、`sys_mmap`、`sbi_call` 等）均为 **Linux/POSIX 系统调用标准名称** 或 **RISC-V SBI 规范强制接口**。这些名称由外部规范决定，任何实现 Linux 兼容或 RISC-V 内核的项目都必须使用，属于领域强制命名，不能作为抄袭证据。此外，`Some`、`Ok`、`Err`、`matches!`、`copy_from_slice` 等均为 Rust 语言级惯用法或标准库宏，属于通用编程元素，不构成项目间可区分的 AST 指纹。

### 6. 保守结论
本次审计基于 0.39% 的极低 AST 相似度与 13 个共同指纹的客观事实。双方在进程管理、内存管理、文件系统等核心板块的共同唯一 AST 指纹均为零或接近零，覆盖率均低于 2%。控方所依赖的“同板块同名”证据，经核实均为 POSIX/Linux 标准接口或 Rust 语言惯用法，属于领域强制命名与通用编程模式。目标项目在 `pidfd`、`clone3`、`ext4` 支持、`futex` 同步等高级特性上的实现，与参考项目的教学级简化模型存在根本性架构鸿沟。综合判定，目标项目不存在对参考项目代码结构的实质性复用，应认定为独立开发。

------------------------------

