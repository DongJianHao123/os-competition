# 🛡️ OS 内核赛自动化代码级架构审计报告

**审计时间**: `2026-07-02 02:49:31`
**参赛作品名称**: `2026-T2026100079911061-XX`
**年份**: `2026`
**学校**: `XX`
**队伍名称**: `T2026100079911061`
**比赛名字**: `OS-Competition`
**比赛子赛道**: `Kernel-Implementation`
**仓库地址**: `https://gitlab.eduxiji.net/educg-group-43501-3132633/T2026100079911061-2137`
**推测开发语言**: `Rust / C (由AST探针自动识别)`
**特征函数量**: `2788`

## ⚔️ 底层代码同源性对比 (AST Homology)
> 本部分由专门的代码审查智能体，对该项目与符合年份条件的历届最高相似度项目进行控制流与底层调用对比。新版报告会先给出确定性的全模块覆盖矩阵，再给出 Agent 的分模块解释，避免只围绕少量抽样函数展开。

### 🏆 Top 5 底层对标: vs `2025-如有名字队-南开大学`（南开大学 / 如有名字队）

#### 对方作品信息

**对方年份**: `2025`
**对方学校**: `南开大学`
**对方队伍名称**: `如有名字队`
**对方比赛名字**: `OS-Competition`
**对方比赛子赛道**: `Kernel-Implementation`
**对方仓库地址**: `https://gitlab.eduxiji.net/educg-group-36002-2710490/nonix`

> 双向覆盖率：共同 AST 指纹: 12；唯一 AST 指纹: 目标项目 2560，参考项目 1745；覆盖率: 目标项目 0.47%，参考项目 0.69%

## 全模块对比总览（确定性证据）

> 总体覆盖率：共同 AST 指纹: 12；唯一 AST 指纹: 目标项目 2560，参考项目 1745；覆盖率: 目标项目 0.47%，参考项目 0.69%

### 1. 模块覆盖矩阵
| 模块 | 目标函数数 | 参考函数数 | 共同唯一 AST 指纹 | Target 覆盖 | Base 覆盖 | 状态 |
| :--- | ---: | ---: | ---: | ---: | ---: | :--- |
| 进程管理 | 642 | 36 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |
| 任务调度/异步运行时 | 709 | 92 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |
| 内存管理 | 297 | 297 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |
| 中断/异常处理 | 21 | 43 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |
| 系统调用 | 451 | 63 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |
| 文件系统 | 493 | 252 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |
| 设备驱动 | 27 | 372 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |
| 网络协议栈 | 0 | 0 | 0 | 0.0% | 0.0% | 双方均未体现 |
| 文件系统/第三方库 | 131 | 696 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |
| 第三方库 | 0 | 1 | 0 | 0.0% | 0.0% | 仅参考项目体现 |
| 通用模块/其他 | 17 | 155 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |

### 2. 分模块证据清单

#### 进程管理
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `kernel/src/syscall/process_ops.rs#sys_clone` | `os/src/syscall/process.rs#sys_clone` | 同板块同名 | sys_clone_common | CloneFlags::from_bits, CloneFlags::from_bits(flags as u32).ok_or_else, Ok, add_task, current_task, current_task().unwrap ...(+5) |
| `kernel/src/syscall/process_ops.rs#sys_exit_group` | `os/src/syscall/process.rs#sys_exit_group` | 同板块同名 | finish_current_thread_group | exit_current_and_run_next, trace!, unreachable! |
| `kernel/src/syscall/process_ops.rs#sys_exit` | `os/src/syscall/process.rs#sys_exit` | 同板块同名 | finish_current_process | exit_current_and_run_next, panic!, trace! |
| `kernel/src/syscall/process_ops.rs#sys_clone_common` | `os/src/syscall/process.rs#sys_exec` | 同模块上下文对齐/差异观察 | BlockReason::VforkChild, Some, clone_pidfd.map, close_reserved_clone_pidfd, complete_blocking_syscall, crate::task::scheduler::request_reschedule ...(+13) | "LD_LIBRARY_PATH=/:/lib:/usr/lib:".to_string, "PATH=/:/bin:".to_string, Ok, String::from, Vec::<String>::new, Vec::new ...(+28) |

#### 任务调度/异步运行时
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `kernel/src/syscall/io_ops.rs#sys_read` | `os/src/syscall/fs.rs#sys_read` | 同板块同名 | SocketOpenFile::from_fd, Some, buf.add, console::read_char, core::ptr::null_mut, ensure_user_range ...(+16) | Err, Ok, UserBuffer::new, current_task, current_task().unwrap, current_user_token ...(+10) |
| `kernel/src/syscall/fs_ops.rs#sys_readlinkat` | `os/src/syscall/fs.rs#sys_readlinkat` | 同板块同名 | copy_user_cstr, fs_path::has_trailing_slash, fs_path::resolve_stage1_path, fs_path::resolve_stage1_path_with_policy, linux_error, path_ptr.is_null ...(+7) | Ok, UserBuffer::new, buffer.write, current_task, current_task().unwrap, current_user_token ...(+14) |
| `kernel/src/syscall/fs_ops.rs#sys_getdents64` | `os/src/syscall/fs.rs#sys_getdents64` | 同板块同名 | dirent.dtype, dirent.ino, dirent.name, fd_open_file, fd_path_only, fs_path::align_up_dirent ...(+7) | Err, Ok, UserBuffer::new, buf.is_null, buffer.write, current_task ...(+17) |
| `kernel/src/task/epoll.rs#add` | `os/src/task/manager.rs#add` | 同板块同名 | Err, Ok, find_watch, find_watch(slot, fd, open_file).is_some, retain_open_file_ref, slot.watches.iter_mut ...(+2) | self.ready_queue.push_back |

#### 内存管理
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `kernel/src/syscall/memory_ops/mapping.rs#sys_mmap` | `os/src/syscall/mm.rs#sys_mmap` | 同板块同名 | MmapFileBacking::Ext4, MmapFileBacking::Rootfs, PTEFlags::new, Some, address_space.mmap_limit, backing.release ...(+30) | Err, MapPermission::from, MmapFlags::from_bits, MmapFlags::from_bits(flags as u32).unwrap, MmapProt::from_bits, MmapProt::from_bits(prot as u32).unwrap ...(+15) |
| `kernel/src/syscall/memory_ops/mapping.rs#sys_mprotect` | `os/src/syscall/mm.rs#sys_mprotect` | 同板块同名 | Some, VirtAddr, addr.checked_add, address_space.page_table.lookup, asm!, check_mprotect_shared_write_allowed ...(+13) | Err, MmapProt::from_bits, MmapProt::from_bits(prot).unwrap, MmapProt::from_bits(prot).unwrap().into, Ok, VirtAddr::from ...(+7) |
| `kernel/src/task/scheduler.rs#schedule` | `os/src/task/processor.rs#schedule` | 同板块同名 | TrapFrame::copy_from_stack, asm!, crate::sbi::shutdown, crate::syscall::signal::prepare_signal_return, cur.kernel_trap_frame_base, current ...(+6) | PROCESSOR.exclusive_access, context_switch_pt, drop, processor.get_idle_task_cx_ptr |
| `kernel/src/syscall/fd_ops.rs#sys_pipe2` | `os/src/syscall/fs.rs#sys_pipe2` | 同板块同名 | FdKind::PipeRead, FdKind::PipeWrite, alloc_fd, close_fd, ensure_user_range, fd_error ...(+8) | FileClass::Abs, FileDescriptor::default, Ok, Pipe2Flags::from_bits_truncate, current_task, current_task().unwrap ...(+11) |

#### 中断/异常处理
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `kernel/src/syscall/signal.rs#sys_rt_sigsuspend` | `os/src/signal/sigflags.rs#default_op` | 同模块上下文对齐/差异观察 | arm_sigsuspend_restore_current, block_on_sigsuspend, current, current_sigsuspend_ready_signal, finish_sigsuspend_immediate_stop, linux_error ...(+3) | continue_signals.contains, dump_signals.contains, ignore_signals.contains, stop_signals.contains, terminate_signals.contains |
| `kernel/src/syscall/signal.rs#sys_sigaltstack` | `patch/polyhal/src/arch/x86_64/idt.rs#new` | 同模块上下文对齐/差异观察 | Some, current, linux_error, new_stack.is_null, old_stack.is_null, read_signal_alt_stack ...(+3) | InterruptDescriptorTable::new, Self, core::slice::from_raw_parts_mut, entries[i].set_handler_fn, transmute::<extern "C" fn(), extern "x86-interrupt" fn(InterruptStackFrame)> |
| `kernel/src/syscall/signal.rs#stage1_signal_terminates` | `patch/polyhal/src/utils/mutex_no_irq.rs#try_lock` | 同模块上下文对齐/差异观察 | matches! | IRQ::int_disable, IRQ::int_enabled, self.lock
            .try_lock, self.lock
            .try_lock()
            .map, self.lock.is_locked |
| `kernel/src/trap.rs#user_sync_exception_handler` | `os/src/trap/interrupts.rs#inc_irq_count` | 同模块上下文对齐/差异观察 | deliver_or_finish_sigbus, deliver_or_finish_sigill, deliver_or_finish_sigsegv, deliver_or_finish_sigtrap | IRQ_COUNTS.lock, counts[irq].saturating_add |

#### 系统调用
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `kernel/src/syscall/time_ops.rs#sys_clock_nanosleep` | `os/src/syscall/other.rs#sys_clock_nanosleep` | 同板块同名 | linux_error, matches!, now.checked_add, read_user_timespec, sleep_current_until, timer::read_time ...(+1) | Ok, current_user_token, get_data, get_time_ms, suspend_current_and_run_next, trace! |
| `kernel/src/syscall/socket_ops.rs#sys_shutdown` | `user/src/syscall.rs#sys_shutdown` | 同板块同名 | inet_socket::kind, inet_socket::shutdown_stream, inet_socket::shutdown_stream(socket, how)
                .map, inet_socket::shutdown_stream(socket, how)
                .map(|()| 0)
                .unwrap_or_else, linux_error, matches! ...(+2) | syscall |
| `kernel/src/syscall/klog.rs#sys_syslog` | `os/src/syscall/other.rs#sys_syslog` | 同板块同名 | buf.is_null, ensure_user_range, linux_error | Ok, warn! |
| `kernel/src/syscall/misc_ops.rs#sys_uname` | `os/src/syscall/other.rs#sys_uname` | 同板块同名 | core::ptr::addr_of!, ensure_user_range, out.as_mut_ptr, write_user_bytes, write_uts_field, write_uts_stored_field | Ok, UserBuffer::new, buf[..len].copy_from_slice, bytes.len, bytes.len().min, copy_str_to_buf ...(+8) |

#### 文件系统
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `kernel/src/syscall/fs_mutation.rs#truncate` | `os/src/fs/inode.rs#truncate` | 同板块同名 | copy_user_cstr, fs_path::resolve_stage1_path, inotify::notify_modify, linux_error, scratch_error_to_errno, scratchfs::file_path ...(+1) | Err, self.inode.truncate |
| `kernel/src/syscall/fs_ops.rs#sys_statfs` | `os/src/syscall/fs.rs#sys_statfs` | 同板块同名 | fs_path::path_kind_from_user, fs_path::write_statfs | Ok, current_user_token, fs_stat, put_data, trace! |
| `kernel/src/syscall/fs_path.rs#as_bytes` | `os/src/fs/stat.rs#as_bytes` | 同板块同名 | 内部闭环逻辑 | core::mem::size_of::<Self>, core::slice::from_raw_parts |
| `kernel/src/syscall/fs_path.rs#name` | `os/src/fs/inode.rs#name` | 同板块同名 | 内部闭环逻辑 | self.inode.path |

#### 设备驱动
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `kernel/src/virtio_blk.rs#disable_interrupts` | `patch/virtio-drivers/src/device/net/dev_raw.rs#disable_interrupts` | 同板块同名 | asm! | self.recv_queue.set_dev_notify, self.send_queue.set_dev_notify |
| `kernel/src/console.rs#putchar` | `patch/polyhal/src/debug_console/aarch64.rs#putchar` | 同板块同名 | UART.putchar | UART.lock, UART.lock().putchar |
| `kernel/src/console.rs#puts` | `os/src/logging.rs#puts` | 同板块同名 | UART.putstr | DebugConsole::putchar |
| `kernel/src/virtio_blk.rs#read_sector_inner` | `patch/virtio-drivers/src/queue.rs#fake_read_write_queue` | 同模块上下文对齐/差异观察 | Err, Ok, avail_idx.wrapping_add, avail_ring, core::hint::spin_loop, core::mem::size_of::<BlkReqHeader> ...(+12) | (*available_ring).idx.load, (*used_ring).idx.fetch_add, (*used_ring).idx.load, Vec::new, assert!, assert_eq! ...(+18) |

#### 文件系统/第三方库
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `kernel/src/ext4.rs#path` | `os/src/fs/ext4_lw/inode.rs#path` | 同板块同名 | 内部闭环逻辑 | self.inner
            .exclusive_access, self.inner
            .exclusive_access()
            .f
            .path, self.inner
            .exclusive_access()
            .f
            .path()
            .into_string, self.inner
            .exclusive_access()
            .f
            .path()
            .into_string()
            .unwrap |
| `kernel/src/ext4.rs#path` | `lwext4_rust/src/file.rs#path` | 同板块同名 | 内部闭环逻辑 | self.file_path.clone |
| `kernel/src/ext4.rs#file_type` | `lwext4_rust/src/file.rs#file_type` | 同板块同名 | 内部闭环逻辑 | self.file_mode, self.file_mode().unwrap, warn! |
| `kernel/src/ext4.rs#size` | `os/src/fs/ext4_lw/inode.rs#size` | 同板块同名 | 内部闭环逻辑 | as_inode_type, file.file_close, file.file_open, file.file_size, file.path, file.types ...(+3) |

#### 第三方库
仅参考项目中观察到该模块代表函数，目标项目没有同模块可对齐证据：
- 参考项目：`patch/virtio-drivers/src/transport/mmio.rs#vendor_id`，调用摘要：volread!

#### 通用模块/其他
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `kernel/src/sbi.rs#shutdown` | `patch/polyhal/src/components/instruction/loongarch64.rs#shutdown` | 同板块同名 | core::arch::asm!, sbi_call | ged_addr.get_mut_ptr::<u8>, ged_addr.get_mut_ptr::<u8>().write_volatile, log::info!, log::warn!, loongArch64::asm::idle, unreachable! ...(+1) |
| `kernel/src/console.rs#putchar` | `patch/polyhal/src/debug_console/x86_64.rs#putchar` | 同板块同名 | self.read_reg, self.write_reg | com::putchar, graphic::putchar, vga_text::putchar |
| `kernel/src/elf.rs#parse` | `user/src/bin/user_shell.rs#new` | 同模块上下文对齐/差异观察 | ELF_MAGIC.as_slice, Err, Ok, Some, checked_range, elf.load_segments ...(+9) | String::new, arg.as_ptr, arg.as_str, arg.is_empty, args
            .iter, args
            .iter()
            .filter ...(+16) |
| `kernel/src/main.rs#fdt_prop_name_eq` | `patch/polyhal/src/mem.rs#parse_system_info` | 同模块上下文对齐/差异观察 | core::ptr::read_volatile, expected.len | CPU_NUM.init_once, display_info!, fdt.chosen, fdt.chosen().inspect, fdt.find_nodes, fdt.find_nodes("/cpus/cpu").count ...(+8) |


好的，审判长。作为本次代码同源性定性仲裁的审计专家，我已全面审阅了控辩双方的陈词、客观 AST 指纹数据、全模块覆盖矩阵以及原始抽样证据。现在，我将基于事实和逻辑，出具最终审计意见。

---

### 1. 宏观判定
- **定性结论**: **独立开发，底层架构存在显著差异。**
- **核心依据**: 全局客观 AST 相似度仅为 **0.28%**，共同 AST 指纹仅 **12** 个，而双方各自拥有上千个唯一指纹（目标 2560，参考 1745）。双向覆盖率均低于 1%（目标 0.47%，参考 0.69%）。在控方重点指控的“进程管理”板块，共同唯一 AST 指纹为 **0**，覆盖率均为 **0.0%**。该数据从根本上否定了任何形式的“架构级复用”或“恶意混淆”指控。

### 2. 全模块覆盖矩阵解读
| 模块 | 双方存在 | 共同 AST 指纹 | 覆盖率解读 | 证据强度 |
| :--- | :---: | :---: | :--- | :--- |
| **进程管理** | 是 | **0** | 0.0% vs 0.0%，双方实现完全独立，无任何可观测的结构重合。 | **未形成** |
| **任务调度/异步运行时** | 是 | **0** | 0.0% vs 0.0%，调度器、epoll、futex 等核心机制实现完全不同。 | **未形成** |
| **内存管理** | 是 | **0** | 0.0% vs 0.0%，`sys_mmap`、`sys_mprotect` 等调用图谱无交集。 | **未形成** |
| **中断/异常处理** | 是 | **0** | 0.0% vs 0.0%，信号处理、Trap 分发逻辑无结构重合。 | **未形成** |
| **系统调用** | 是 | **0** | 0.0% vs 0.0%，同名系统调用入口下的实现逻辑完全不同。 | **未形成** |
| **文件系统** | 是 | **0** | 0.0% vs 0.0%，`truncate`、`statfs` 等操作调用链无交集。 | **未形成** |
| **设备驱动** | 是 | **0** | 0.0% vs 0.0%，virtio 驱动实现方式（直接操作 vs 第三方库）不同。 | **未形成** |
| **网络协议栈** | 否 | 0 | 双方均未体现，无对比基础。 | **未形成** |
| **文件系统/第三方库** | 是 | **0** | 0.0% vs 0.0%，ext4 等模块的封装方式与内部实现不同。 | **未形成** |
| **第三方库** | 仅参考 | 0 | 目标项目未使用，属于单方特征。 | **未形成** |
| **通用模块/其他** | 是 | **0** | 0.0% vs 0.0%，`shutdown`、`putchar` 等通用功能实现方式不同。 | **未形成** |

**解读总结**: 所有双方共存的模块，其共同 AST 指纹均为 0。这意味着在 AST 结构层面，两个项目没有任何一个模块存在可被检测到的代码复用。控方提出的所有指控均建立在函数同名或抽象设计思想相似的基础上，而非客观的代码结构证据。

### 3. 底层 AST 关键证据指纹表
> 本表仅列出双方可对齐的同名/同功能函数，并基于 AST 指纹数据说明其差异。所有证据均指向“独立实现”。

| 所属板块 | 目标项目路径 | 参考项目路径 | 证据类型 | 技术说明 |
| :--- | :--- | :--- | :--- | :--- |
| 进程管理 | `kernel/src/syscall/process_ops.rs#sys_clone` | `os/src/syscall/process.rs#sys_clone` | 同名异构 | 目标为 wrapper，调用 `sys_clone_common`；参考为直接实现。外部调用图谱零交集。 |
| 进程管理 | `kernel/src/syscall/process_ops.rs#sys_exit` | `os/src/syscall/process.rs#sys_exit` | 同名异构 | 目标调用 `finish_current_process`；参考调用 `exit_current_and_run_next`。退出模型不同。 |
| 进程管理 | `kernel/src/syscall/process_ops.rs#sys_exit_group` | `os/src/syscall/process.rs#sys_exit_group` | 同名异构 | 目标调用 `finish_current_thread_group`；参考调用 `exit_current_and_run_next`。线程组模型不同。 |
| 任务调度 | `kernel/src/syscall/io_ops.rs#sys_read` | `os/src/syscall/fs.rs#sys_read` | 同名异构 | 目标处理 socket/console；参考使用 `UserBuffer`。IO 模型完全不同。 |
| 内存管理 | `kernel/src/syscall/memory_ops/mapping.rs#sys_mmap` | `os/src/syscall/mm.rs#sys_mmap` | 同名异构 | 目标使用 `MmapFileBacking`、`PTEFlags`；参考使用 `MmapFlags::from_bits`。内存对象模型不同。 |
| 系统调用 | `kernel/src/syscall/time_ops.rs#sys_clock_nanosleep` | `os/src/syscall/other.rs#sys_clock_nanosleep` | 同名异构 | 目标使用 `sleep_current_until` 阻塞模型；参考使用 `suspend_current_and_run_next` 调度模型。 |
| 文件系统 | `kernel/src/syscall/fs_mutation.rs#truncate` | `os/src/fs/inode.rs#truncate` | 同名异构 | 目标通过 `scratchfs::file_path` 操作；参考直接操作 `self.inode.truncate`。 |
| 设备驱动 | `kernel/src/virtio_blk.rs#disable_interrupts` | `patch/virtio-drivers/src/device/net/dev_raw.rs#disable_interrupts` | 同名异构 | 目标使用内联汇编 `asm!`；参考调用队列的 `set_dev_notify` 方法。实现层次不同。 |
| 第三方库 | (无) | `lwext4_rust/src/file.rs#path` | 单方特征 | 目标项目未使用 `lwext4_rust` 库，其 ext4 实现为独立开发。 |

### 4. 核心代码调用流对比剖析
* **【进程管理 / 系统调用入口】**
* **底层技术析理**: 两者均实现了 Linux ABI 要求的 `sys_clone`、`sys_exit` 等入口，但底层**对象管理模型**和**同步策略**截然不同。参考项目采用 rCore 风格的 `PID2TCB` HashMap 和 `PROCESSOR` 单例进行任务管理，退出时直接调用 `suspend_current_and_run_next` 切换上下文。目标项目则实现了更接近 Linux 的 `PROCESS_LIST` 数组、`pidfd` 和 `generation` 机制，并通过 `BlockReason` 状态机和 `complete_blocking_syscall` 进行阻塞调度。两者在进程状态管理、回收和调度这一核心路径上的 AST 指纹为 0，证明其内部实现是完全独立的。
* **代码溯源链接**:
  - 目标项目: `kernel/src/syscall/process_ops.rs#sys_clone_common`
  - 参考项目: `os/src/syscall/process.rs#sys_clone`

* **【内存管理 / mmap 系统调用】**
* **底层技术析理**: 在 `sys_mmap` 实现中，两者的**内存抽象**完全不同。参考项目通过 `MmapFlags::from_bits` 和 `MmapProt::from_bits` 直接操作位标志，映射到简单的 `MapPermission`。目标项目则引入了 `MmapFileBacking` 枚举来区分 `Ext4` 和 `Rootfs` 等不同文件 backing，并使用 `PTEFlags::new` 和 `address_space.mmap_limit` 进行更复杂的地址空间管理。这种在内存映射核心路径上的抽象层次差异，是架构独立演化的强证据。
* **代码溯源链接**:
  - 目标项目: `kernel/src/syscall/memory_ops/mapping.rs#sys_mmap`
  - 参考项目: `os/src/syscall/mm.rs#sys_mmap`

### 5. 单方模块与降权项
- **第三方库/公共依赖**: 参考项目使用了 `lwext4_rust`、`virtio-drivers` 等第三方库，而目标项目未使用。这些库的接口（如 `path`、`file_type`）产生的同名匹配属于公共 API 调用，不能作为两个项目之间的同源证据。
- **单方模块**: 参考项目独有的“第三方库”模块（1个函数）是单方特征，不构成对比基础。
- **降权项**: 所有跨模块的同名函数（如 `add`、`shutdown`、`current_task`）或同名方法（如 `path`、`size`），均属于通用编程命名或 Linux ABI 强制命名。在 AST 指纹为 0 的前提下，这些名称匹配的证明力极弱，予以降权。

### 6. 保守结论
本次审计的核心结论是：**未发现任何可被客观 AST 证据支持的代码复用行为。** 0.28% 的全局相似度和各板块均为 0 的共同指纹，是两个项目在操作系统内核这一特定领域下独立开发的典型特征。控方基于“同名函数”、“相似状态机”等宏观设计思想的指控，在严格的 AST 结构比对下无法成立。目标项目与参考项目在进程模型、内存管理、同步机制等核心架构上存在根本性差异，符合从教学 OS 向 Linux 兼容内核独立演化的技术逻辑。建议法庭采纳“独立开发”的定性，驳回所有复用指控。

------------------------------

