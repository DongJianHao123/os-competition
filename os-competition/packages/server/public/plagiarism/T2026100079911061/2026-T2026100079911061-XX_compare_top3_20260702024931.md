# 🛡️ OS 内核赛自动化代码级架构审计报告

**审计时间**: `2026-07-02 02:49:31`
**参赛作品名称**: `2026-T2026100079911061-XX`
**年份**: `2026`
**学校**: `XX`
**队伍名称**: `T2026100079911061`
**比赛名字**: `OS-Competition`
**比赛子赛道**: `Kernel-Implementation`
**仓库地址**: `https://gitlab.eduxiji.net/educg-group-43501-3132633/T2026100079911061-2137`
**推测开发语言**: `Rust / C (由AST探针自动识别)`
**特征函数量**: `2788`

## ⚔️ 底层代码同源性对比 (AST Homology)
> 本部分由专门的代码审查智能体，对该项目与符合年份条件的历届最高相似度项目进行控制流与底层调用对比。新版报告会先给出确定性的全模块覆盖矩阵，再给出 Agent 的分模块解释，避免只围绕少量抽样函数展开。

### 🏆 Top 3 底层对标: vs `2024-萌新-南开大学`（南开大学 / 萌新）

#### 对方作品信息

**对方年份**: `2024`
**对方学校**: `南开大学`
**对方队伍名称**: `萌新`
**对方比赛名字**: `OS-Competition`
**对方比赛子赛道**: `Kernel-Implementation`
**对方仓库地址**: `https://gitlab.eduxiji.net/educg-group-26010-2376550/T202410055992606-2876`

> 双向覆盖率：共同 AST 指纹: 19；唯一 AST 指纹: 目标项目 2560，参考项目 2483；覆盖率: 目标项目 0.74%，参考项目 0.77%

## 全模块对比总览（确定性证据）

> 总体覆盖率：共同 AST 指纹: 19；唯一 AST 指纹: 目标项目 2560，参考项目 2483；覆盖率: 目标项目 0.74%，参考项目 0.77%

### 1. 模块覆盖矩阵
| 模块 | 目标函数数 | 参考函数数 | 共同唯一 AST 指纹 | Target 覆盖 | Base 覆盖 | 状态 |
| :--- | ---: | ---: | ---: | ---: | ---: | :--- |
| 进程管理 | 642 | 39 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |
| 任务调度/异步运行时 | 709 | 272 | 1 | 0.15% | 0.44% | 双方存在且有结构重合 |
| 内存管理 | 297 | 174 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |
| 中断/异常处理 | 21 | 29 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |
| 系统调用 | 451 | 67 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |
| 文件系统 | 493 | 285 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |
| 设备驱动 | 27 | 42 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |
| 网络协议栈 | 0 | 0 | 0 | 0.0% | 0.0% | 双方均未体现 |
| 文件系统/第三方库 | 131 | 730 | 4 | 3.31% | 0.62% | 双方存在且有结构重合 |
| 网络协议栈/第三方库 | 0 | 118 | 0 | 0.0% | 0.0% | 仅参考项目体现 |
| 第三方库 | 0 | 1374 | 0 | 0.0% | 0.0% | 仅参考项目体现 |
| 通用模块/其他 | 17 | 106 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |

### 2. 分模块证据清单

#### 进程管理
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `kernel/src/syscall/process_ops.rs#sys_clone_common` | `api/linux_syscall_api/src/syscall_task/imp/task.rs#syscall_exec` | 同模块上下文对齐/差异观察 | BlockReason::VforkChild, Some, clone_pidfd.map, close_reserved_clone_pidfd, complete_blocking_syscall, crate::task::scheduler::request_reschedule ...(+13) | Err, Ok, Some, Vec::new, args_vec.len, args_vec.push ...(+20) |
| `kernel/src/procfs.rs#render_file` | `modules/axfs/src/mounts.rs#procfs` | 同模块上下文对齐/差异观察 | ProcBuf::new, Some, buf.len, render_cpuinfo, render_devices, render_diskstats ...(+28) | Arc::new, Ok, file_over.write_at, file_somaxconn.write_at, fs::ramfs::RamFileSystem::new, proc_root.clone ...(+3) |
| `kernel/src/procfs.rs#file_ino` | `api/linux_syscall_api/src/syscall_task/imp/task.rs#syscall_wait4` | 同模块上下文对齐/差异观察 | pid_fdinfo_file_ino, pid_file_ino, target.pid | Err, Ok, WaitFlags::from_bits, WaitFlags::from_bits(args[2] as u32).unwrap, WaitFuture::new, axtask::block_task ...(+8) |
| `kernel/src/syscall/process_ops.rs#scan_wait_children_for_parent_inner` | `modules/axtask/src/run_queue.rs#exit_current` | 同模块上下文对齐/差异观察 | Some, WaitEventKind::Exited, WaitEventKind::Stopped, WaitScan::Event, child.is_waitable_representative, child.waitable_pid ...(+6) | EXITED_TASKS.lock, EXITED_TASKS.lock().clear, assert!, axhal::misc::terminate, axlog::warn!, crate::current ...(+4) |

#### 任务调度/异步运行时
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `kernel/src/task/inet_socket.rs#reset` | `modules/axhal/src/arch/riscv/context.rs#reset` | 同板块同名 | self.accept_queue.reset, self.datagrams.reset, self.stream.reset | 内部闭环逻辑 |
| `kernel/src/task/inet_socket.rs#get` | `modules/axtask/src/task.rs#get` | 同板块同名 | 内部闭环逻辑 | Self::try_get, Self::try_get().expect |
| `kernel/src/task/scheduler.rs#current` | `modules/axtask/src/api.rs#current` | 同板块同名 | PROCESS_LIST[idx].as_mut, panic! | CurrentTask::get |
| `kernel/src/task/signalfd.rs#set_mask` | `modules/axtask/src/fd_manager.rs#set_mask` | 同板块同名 | Ok, slot_mut, slot_mut(signalfd).ok_or | self.umask
            .store, self.umask.load |

#### 内存管理
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `kernel/src/main.rs#rust_main` | `modules/axruntime/src/lib.rs#rust_main` | 同板块同名 | AddressSpace::new, FsContext::new, PTEFlags::new, PhysAddr, ProcessIdentity::new, RlimitContext::new ...(+47) | INITED_CPUS.fetch_add, ax_println!, axdisplay::init_display, axdriver::init_drivers, axfs::init_filesystems, axhal::mem::memory_regions ...(+9) |
| `kernel/src/task/inet_socket.rs#alloc` | `modules/axalloc/src/lib.rs#alloc` | 同板块同名 | INET_SOCKETS.iter_mut, INET_SOCKETS.iter_mut().enumerate, Some, slot.init | Ok, balloc.add_memory, balloc.alloc, balloc.total_bytes, debug!, layout.size ...(+5) |
| `kernel/src/task/eventfd.rs#alloc` | `modules/axhal/src/tls.rs#alloc` | 同板块同名 | EVENTFDS.as_mut_ptr, EventFd, Some, ptr.add | Layout::from_size_align, Layout::from_size_align(tls_area_size(), TLS_ALIGN).unwrap, NonNull::new, NonNull::new(area_base).unwrap, alloc::alloc::alloc_zeroed, area_base.add ...(+5) |
| `kernel/src/task/epoll.rs#alloc` | `modules/axalloc/src/page.rs#alloc` | 同板块同名 | EPOLLS.as_mut_ptr, Epoll, Some, ptr.add | global_allocator, global_allocator()
            .alloc_pages, global_allocator()
            .alloc_pages(1, PAGE_SIZE)
            .map, global_allocator()
            .alloc_pages(1, PAGE_SIZE)
            .map(|vaddr| Self {
                start_vaddr: vaddr.into(),
                num_pages: 1,
            })
            .map_err, vaddr.into |

#### 中断/异常处理
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `kernel/src/syscall/signal.rs#sys_rt_sigsuspend` | `modules/axhal/src/platform/VisionFive2/irq.rs#register_handler` | 同模块上下文对齐/差异观察 | arm_sigsuspend_restore_current, block_on_sigsuspend, current, current_sigsuspend_ready_signal, finish_sigsuspend_immediate_stop, linux_error ...(+3) | with_cause! |
| `kernel/src/syscall/signal.rs#sys_sigaltstack` | `modules/axhal/src/platform/riscv64_qemu_virt/irq.rs#register_handler` | 同模块上下文对齐/差异观察 | Some, current, linux_error, new_stack.is_null, old_stack.is_null, read_signal_alt_stack ...(+3) | with_cause! |
| `kernel/src/syscall/signal.rs#stage1_signal_terminates` | `modules/axtrap/src/arch/riscv/mod.rs#riscv_kernel_trap_handler` | 同模块上下文对齐/差异观察 | matches! | handle_irq, panic!, scause.bits, scause.cause, scause::read |
| `kernel/src/trap.rs#user_sync_exception_handler` | `modules/axhal/src/irq.rs#register_handler_common` | 同模块上下文对齐/差异观察 | deliver_or_finish_sigbus, deliver_or_finish_sigill, deliver_or_finish_sigsegv, deliver_or_finish_sigtrap | IRQ_HANDLER_TABLE.register_handler, axlog::ax_println!, set_enable, warn! |

#### 系统调用
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `kernel/src/syscall/poll_ops.rs#sys_ppoll` | `api/linux_syscall_api/src/syscall_net/socket.rs#get` | 同模块上下文对齐/差异观察 | apply_poll_signal_mask, block_on_poll_timeout, block_on_pollfd_set, block_on_pollfd_set_until, block_on_signal_only_poll, ensure_user_range ...(+15) | copy_nonoverlapping, drop, keep_alive.to_ne_bytes, panic!, s.keep_alive, s.keep_alive().is_some ...(+10) |
| `kernel/src/syscall/futex_ops.rs#sys_futex` | `api/linux_syscall_api/src/syscall_net/socket.rs#set` | 同模块上下文对齐/差异观察 | FutexOp::decode, Some, block_on_futex, futex_cmp_requeue_pi, futex_key, futex_lock_pi ...(+14) | <[u8; 4]>::try_from, <[u8; 4]>::try_from(&opt[0..4]).unwrap, Err, Ok, Some, axnet::Duration::from_secs ...(+14) |
| `kernel/src/syscall/socket_ops/sockopt.rs#sys_setsockopt` | `api/linux_syscall_api/src/syscall_mem/imp.rs#syscall_shmget` | 同模块上下文对齐/差异观察 | core::mem::size_of::<u32>, i32::from, linux_error, normalize_socket_buffer_size, read_socket_timeout_ticks, read_user_i32 ...(+11) | Err, MemorySet::add_shared_mem, MemorySet::create_shared_mem, Ok, ShmFlags::from_bits, axmem::KEY_TO_SHMID.lock ...(+8) |
| `kernel/src/syscall/socket_ops/sockopt.rs#sys_getsockopt` | `api/linux_syscall_api/src/syscall_net/socket.rs#get` | 同模块上下文对齐/差异观察 | bytes.len, core::cmp::min, get_inet_protocol_sockopt, linux_error, optval.is_null, read_user_u32 ...(+18) | bytes.as_bytes, bytes.as_ptr, bytes.len, copy_nonoverlapping, panic!, raw_socket.get_congestion ...(+4) |

#### 文件系统
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `kernel/src/syscall/fs_mutation.rs#truncate` | `modules/axfs/src/fs/fatfs.rs#truncate` | 同板块同名 | copy_user_cstr, fs_path::resolve_stage1_path, inotify::notify_modify, linux_error, scratch_error_to_errno, scratchfs::file_path ...(+1) | SeekFrom::Start, file.seek, file.seek(SeekFrom::Start(size)).map_err, file.truncate, file.truncate().map_err, self.0.lock |
| `kernel/src/syscall/poll_ops.rs#sys_pselect6` | `api/linux_syscall_api/src/syscall_fs/mod.rs#fs_syscall` | 同模块上下文对齐/差异观察 | apply_poll_signal_mask, block_on_poll_timeout, block_on_select_fdset, block_on_select_fdset_until, block_on_signal_only_poll, effective_nfds.div_ceil ...(+19) | Ok, panic!, sys_linkat, syscall_chdir, syscall_close, syscall_dup ...(+31) |
| `kernel/src/syscall/io_transfer/copy_range.rs#sys_copy_file_range` | `ulib/src/file.rs#fs_init` | 同模块上下文对齐/差异观察 | buffer.len, copy_file_range_offset, core::cmp::min, fd_open_file, fd_readable, fd_writable ...(+12) | "/bin/".to_string, "/lib/".to_string, "/usr/bin/".to_string, "/usr/sbin/".to_string, ("/lib/".to_string() + libc_so).as_str, ("/lib/".to_string() + libc_so2).as_str ...(+17) |
| `kernel/src/syscall/socket_ops.rs#sys_sendto` | `api/linux_syscall_api/src/syscall_fs/ctype/file.rs#get_stat` | 同模块上下文对齐/差异观察 | SocketOpenFile::from_fd, Some, dest_addr.is_null, fd_open_file, fd_path_only, inet_socket::family ...(+11) | INODE_NAME_MAP.lock, Ok, assert!, attr.blocks, attr.size, drop ...(+12) |

#### 设备驱动
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `kernel/src/virtio_net.rs#probe` | `modules/axdriver/src/lib.rs#probe` | 同板块同名 | Some, mmio.read, read_info | for_each_drivers!, self.probe_bus_devices |
| `kernel/src/virtio_blk.rs#read_sector_inner` | `modules/axdriver/src/lib.rs#init_drivers` | 同模块上下文对齐/差异观察 | Err, Ok, avail_idx.wrapping_add, avail_ring, core::hint::spin_loop, core::mem::size_of::<BlkReqHeader> ...(+12) | AllDevices::default, all_devs.block.iter, all_devs.block.iter().enumerate, all_devs.display.iter, all_devs.display.iter().enumerate, all_devs.net.iter ...(+5) |
| `kernel/src/mm/frame.rs#remove_free_block` | `modules/axdriver/src/virtio.rs#probe_pci` | 同模块上下文对齐/差异观察 | FREE_AREA_HEADS[order].load, FREE_AREA_HEADS[order].store, FREE_NEXT[current as usize].load, FREE_NEXT[current as usize].store, FREE_NEXT[previous as usize].store, FREE_ORDER[current as usize].store | D::try_new, Some, driver_virtio::probe_pci_device::<VirtIoHalImpl>, warn! |
| `kernel/src/virtio_blk.rs#probe_and_init` | `modules/axdriver/src/virtio.rs#probe_mmio` | 同模块上下文对齐/差异观察 | Some, init_device, mmio.read | D::try_new, Some, base_vaddr.as_mut_ptr, driver_virtio::probe_mmio_device, mmio_base.into, phys_to_virt ...(+1) |

#### 文件系统/第三方库
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `kernel/src/ext4.rs#read_inode` | `crates/another_ext4/src/ext4/rw.rs#read_inode` | 同板块同名 | Err, Ok, copy_inode_blocks, read_exact, read_group_inode_table_block, read_u16 ...(+2) | InodeRef::new, block.read_offset_as, self.inode_disk_pos, self.read_block |
| `kernel/src/ext4.rs#read_exact` | `crates/rust-fatfs/src/io.rs#read_exact` | 同板块同名 | Ok, core::cmp::min, out.len, out[done..done + chunk].copy_from_slice, virtio_blk::read_sector, virtio_blk::read_sector(sector_id, &mut sector).map_err | Err, Ok, Self::Error::new_unexpected_eof_error, buf.is_empty, debug!, e.is_interrupted ...(+1) |
| `kernel/src/ext4.rs#read_block` | `crates/another_ext4/src/ext4_defs/cache.rs#read_block` | 同板块同名 | Ok, block_id
        .checked_mul, block_id
        .checked_mul(info.block_size as u64)
        .ok_or, read_exact | block.clone, cache[set_id].access, debug!, self.block_dev.read_block, self.block_dev.write_block, self.cache.lock ...(+1) |
| `kernel/src/ext4.rs#name` | `crates/another_ext4/src/ext4_defs/xattr.rs#name` | 同板块同名 | 内部闭环逻辑 | String::from_utf8_unchecked, name_bytes.to_vec, prefix.to_string |

#### 网络协议栈/第三方库
仅参考项目中观察到该模块代表函数，目标项目没有同模块可对齐证据：
- 参考项目：`modules/axnet/src/smoltcp_impl/tcp.rs#connect`，调用摘要：Ok, Ok::<(IpEndpoint, IpEndpoint), AxError>, SOCKET_SET
                .with_socket_mut::<tcp::Socket, _, _>, SOCKET_SET.add, SocketSetWrapper::new_tcp_socket, Some ...(+23)
- 参考项目：`modules/axnet/src/smoltcp_impl/tcp.rs#recv_timeout`，调用摘要：Err, Ok, SOCKET_SET.with_socket_mut::<tcp::Socket, _, _>, ax_err!, ax_err_type!, current_ticks ...(+11)
- 参考项目：`modules/axnet/src/smoltcp_impl/mod.rs#init`，调用摘要：Config::new, ETH0.init_by, EthernetAddress, GATEWAY.parse, GATEWAY.parse().expect, IP.parse ...(+22)
- 参考项目：`modules/axnet/src/smoltcp_impl/tcp.rs#shutdown`，调用摘要：LISTEN_TABLE.unlisten, Ok, SOCKET_SET.poll_interfaces, SOCKET_SET.with_socket_mut::<tcp::Socket, _, _>, debug!, self.handle.get ...(+9)

#### 第三方库
仅参考项目中观察到该模块代表函数，目标项目没有同模块可对齐证据：
- 参考项目：`crates/bcm2835-sdhci/src/Bcm2835SDhci.rs#sd_card_init`，调用摘要：ACMD!, byte_swap, debug!, info!, self.cmd_timeout, self.dumpregs ...(+26)
- 参考项目：`crates/elf_parser/src/arch/x86_64.rs#get_relocate_pairs`，调用摘要：Ok, Vec::new, VirtAddr::from, assert_eq!, dyn_sym.get_name, dyn_sym.get_name(elf).unwrap ...(+23)
- 参考项目：`crates/elf_parser/src/arch/aarch64.rs#get_relocate_pairs`，调用摘要：Ok, Vec::new, VirtAddr::from, assert_eq!, dyn_sym.get_name, dyn_sym.get_name(elf).unwrap ...(+22)
- 参考项目：`crates/elf_parser/src/arch/riscv.rs#get_relocate_pairs`，调用摘要：Ok, Vec::new, VirtAddr::from, assert_eq!, dyn_sym.get_name, dyn_sym.get_name(elf).unwrap ...(+23)

#### 通用模块/其他
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `kernel/src/sbi.rs#sbi_call` | `modules/axhal/src/platform/VisionFive2/mod.rs#sbi_call` | 同板块同名 | core::arch::asm! | asm! |
| `kernel/src/timer.rs#set_timer` | `modules/axhal/src/platform/VisionFive2/time.rs#set_timer` | 同板块同名 | core::arch::asm! | super::sbi_call |
| `kernel/src/console.rs#putchar` | `modules/axhal/src/platform/riscv64_qemu_virt/console.rs#putchar` | 同板块同名 | self.read_reg, self.write_reg | sbi_rt::legacy::console_putchar |
| `kernel/src/elf.rs#parse` | `modules/axhal/src/mem.rs#kernel_image_regions` | 同模块上下文对齐/差异观察 | ELF_MAGIC.as_slice, Err, Ok, Some, checked_range, elf.load_segments ...(+9) | (_sbss as usize).into, (_sdata as usize).into, (_srodata as usize).into, (_stext as usize).into, (boot_stack as usize).into, [
        MemRegion {
            paddr: virt_to_phys((_stext as usize).into()),
            size: _etext as usize - _stext as usize,
            flags: MemRegionFlags::RESERVED | MemRegionFlags::READ | MemRegionFlags::EXECUTE,
            name: ".text",
        },
        MemRegion {
            paddr: virt_to_phys((_srodata as usize).into()),
            size: _erodata as usize - _srodata as usize,
            flags: MemRegionFlags::RESERVED | MemRegionFlags::READ,
            name: ".rodata",
        },
        MemRegion {
            paddr: virt_to_phys((_sdata as usize).into()),
            size: _edata as usize - _sdata as usize,
            flags: MemRegionFlags::RESERVED | MemRegionFlags::READ | MemRegionFlags::WRITE,
            name: ".data .tdata .tbss .percpu",
        },
        MemRegion {
            paddr: virt_to_phys((boot_stack as usize).into()),
            size: boot_stack_top as usize - boot_stack as usize,
            flags: MemRegionFlags::RESERVED | MemRegionFlags::READ | MemRegionFlags::WRITE,
            name: "boot stack",
        },
        MemRegion {
            paddr: virt_to_phys((_sbss as usize).into()),
            size: _ebss as usize - _sbss as usize,
            flags: MemRegionFlags::RESERVED | MemRegionFlags::READ | MemRegionFlags::WRITE,
            name: ".bss",
        },
    ]
    .into_iter ...(+1) |


好的，审计官。以下是基于客观 AST 证据与双方模块画像的最终同源性定性仲裁报告。

---

#### 1. 宏观判定
- **定性结论**: **独立开发 / 底层架构显著差异**
- **核心依据**: 客观 AST 相似度仅为 **0.38%**，共同 AST 指纹仅 **19** 个，而双方各自拥有超过 2400 个唯一指纹。双向覆盖率均低于 1%，表明两个项目在代码实现层面几乎完全独立。控方提出的“复用”指控均基于功能主题的相似性推断，而非客观的 AST 结构重合，在【进程管理】等核心指控模块，共同 AST 指纹为 **0**，无法支持代码复用结论。

#### 2. 全模块覆盖矩阵解读
- **进程管理**: 双方均存在。共同 AST 指纹为 **0**。目标覆盖率 0.0%，参考覆盖率 0.0%。双方虽实现了相同的 POSIX 语义，但底层数据结构与状态机实现完全不同。**证据强度：未形成**。
- **任务调度/异步运行时**: 双方均存在。共同 AST 指纹为 **1**。目标覆盖率 0.15%，参考覆盖率 0.44%。唯一的共同指纹源于同名函数（如 `current`），属于内核领域的通用命名，不构成复用证据。**证据强度：弱**。
- **内存管理**: 双方均存在。共同 AST 指纹为 **0**。目标覆盖率 0.0%，参考覆盖率 0.0%。双方的内存分配器实现路径无任何 AST 结构重合。**证据强度：未形成**。
- **中断/异常处理**: 双方均存在。共同 AST 指纹为 **0**。目标覆盖率 0.0%，参考覆盖率 0.0%。Trap 处理与中断注册机制在调用图谱上无重合。**证据强度：未形成**。
- **系统调用**: 双方均存在。共同 AST 指纹为 **0**。目标覆盖率 0.0%，参考覆盖率 0.0%。系统调用分发与实现路径无结构重合。**证据强度：未形成**。
- **文件系统**: 双方均存在。共同 AST 指纹为 **0**。目标覆盖率 0.0%，参考覆盖率 0.0%。文件系统实现（如 procfs）的底层架构完全不同。**证据强度：未形成**。
- **设备驱动**: 双方均存在。共同 AST 指纹为 **0**。目标覆盖率 0.0%，参考覆盖率 0.0%。驱动模型与硬件交互方式无结构重合。**证据强度：未形成**。
- **网络协议栈**: 仅参考项目体现。共同 AST 指纹为 **0**。目标项目无此模块，无法形成证据。**证据强度：未形成**。
- **第三方库/公共依赖**: 双方均存在。共同 AST 指纹为 **4**。目标覆盖率 3.31%，参考覆盖率 0.62%。共同指纹源于 `ext4` 和 `fatfs` 等第三方库的同名函数，属于公共依赖，不反映项目本身的原创性。**证据强度：弱（已降权）**。

#### 3. 底层 AST 关键证据指纹表
| 所属板块 | 目标项目路径 | 参考项目路径 | 证据类型 | 技术说明 |
| :--- | :--- | :--- | :--- | :--- |
| 任务调度/异步运行时 | `kernel/src/task/scheduler.rs#current` | `modules/axtask/src/api.rs#current` | 同名函数 | 获取当前任务指针的通用操作，双方实现方式不同（目标项目用全局数组，参考项目用线程局部存储）。 |
| 文件系统/第三方库 | `kernel/src/ext4.rs#read_inode` | `crates/another_ext4/src/ext4/rw.rs#read_inode` | 同名函数 | 均为对 `ext4` 文件系统标准的解析，函数名由标准定义，调用图谱不同。 |
| 文件系统/第三方库 | `kernel/src/ext4.rs#read_exact` | `crates/rust-fatfs/src/io.rs#read_exact` | 同名函数 | 通用的“读取指定长度数据”操作，是文件系统驱动的常见模式。 |
| 文件系统/第三方库 | `kernel/src/ext4.rs#read_block` | `crates/another_ext4/src/ext4_defs/cache.rs#read_block` | 同名函数 | 读取磁盘块的通用操作，函数签名由功能决定，非独创性设计。 |
| 文件系统/第三方库 | `kernel/src/ext4.rs#name` | `crates/another_ext4/src/ext4_defs/xattr.rs#name` | 同名函数 | 获取名称的简单访问器，属于常规编程范式。 |

#### 4. 核心代码调用流对比剖析
* **【进程管理 / 进程退出与回收路径】**
* **底层技术析理**: 目标项目采用显式的全局 `PROCESS_LIST` 数组和手动状态机管理进程退出，通过 `finish_live_process_for_exit_idx`、`reparent_orphans_to_init_for_parent` 等函数直接操作进程索引。参考项目则基于 `axtask` 异步运行时，通过 `EXITED_TASKS` 锁保护的任务队列和 `set_state`、`set_zombie` 等内部状态方法实现。前者是静态数组 + 显式状态流转，后者是动态任务 + 异步状态机，内存模型与同步策略完全不同。
* **代码溯源链接**:
  - 目标项目: `kernel/src/syscall/process_ops.rs#finish_process_index_as_zombie`
  - 参考项目: `modules/axtask/src/run_queue.rs#exit_current`

* **【进程管理 / 子进程等待与唤醒路径】**
* **底层技术析理**: 目标项目在 `blocked_wait_request_for_wake` 中硬编码了对 `BlockReason::WaitChild` 的解构，并通过 `with_process` 闭包在父进程上下文中进行复杂的条件匹配（如 `terminal_exit_mismatch`）。参考项目则通过 `WaitFuture` 和 `axtask::block_task` 将等待逻辑封装为异步 Future，依赖运行时调度。目标项目是同步阻塞 + 手动匹配，参考项目是异步 Future + 运行时唤醒，抽象层次与同步原语截然不同。
* **代码溯源链接**:
  - 目标项目: `kernel/src/syscall/process_ops.rs#blocked_wait_request_for_wake`
  - 参考项目: `api/linux_syscall_api/src/syscall_task/imp/task.rs#syscall_wait4`

* **【文件系统 / Procfs 实现路径】**
* **底层技术析理**: 目标项目构建了完整的动态 `procfs` 虚拟文件系统，通过 `ProcBuf`、`ProcDirent`、`ProcEntry` 等自研类型体系，按需渲染 `/proc/pid/` 下的各类信息。参考项目仅在 `ramfs` 上挂载了少数静态文件，通过 `file_over.write_at` 直接写入内容。前者是动态 VFS 架构，后者是静态内存文件系统，对象管理模型与数据组织方式完全不同。
* **代码溯源链接**:
  - 目标项目: `kernel/src/procfs.rs#render_file`
  - 参考项目: `modules/axfs/src/mounts.rs#procfs`

#### 5. 单方模块与降权项
- **单方模块**: 网络协议栈、第三方库（如 `elf_parser`、`bcm2835-sdhci`）仅存在于参考项目中，是参考项目独有的功能模块或依赖，无法构成同源证据。
- **降权项**: 双方在 `ext4`、`fatfs` 等第三方库中出现的同名函数（如 `read_inode`、`read_block`）属于公共依赖，其相似性由外部库的 API 定义决定。跨板块的同名函数（如 `reset`、`get`、`alloc`）均为 Rust 或内核领域的通用命名，其调用图谱完全不同，已依规降级为弱信号或不构成证据。

#### 6. 保守结论
本次审计的客观 AST 证据表明，两个项目的代码结构重合度极低（0.38%），远未达到可判定为“代码复用”或“抄袭”的阈值。控方基于功能主题（如进程等待、退出、Futex）提出的指控，本质上是 Linux/POSIX 规范所定义的领域必然相似性，而非代码实现层面的同源。目标项目在进程管理、文件系统等核心板块展现出与参考项目截然不同的架构设计、数据结构和同步策略，例如其自研的 `pidfd` 代际管理、显式调度器状态机和动态 procfs VFS 等。因此，综合判定两个项目为独立开发，不存在违规复用行为。

------------------------------

