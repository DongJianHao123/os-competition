# 🛡️ OS 内核赛自动化代码级架构审计报告

**审计时间**: `2026-07-02 11:20:21`
**参赛作品名称**: `2026-T202610486999578-XX`
**年份**: `2026`
**学校**: `XX`
**队伍名称**: `T202610486999578`
**比赛名字**: `OS-Competition`
**比赛子赛道**: `Kernel-Implementation`
**仓库地址**: `https://gitlab.eduxiji.net/educg-group-43501-3132633/T202610486999578-700`
**推测开发语言**: `Rust / C (由AST探针自动识别)`
**特征函数量**: `3235`

## ⚔️ 底层代码同源性对比 (AST Homology)
> 本部分由专门的代码审查智能体，对该项目与符合年份条件的历届最高相似度项目进行控制流与底层调用对比。新版报告会先给出确定性的全模块覆盖矩阵，再给出 Agent 的分模块解释，避免只围绕少量抽样函数展开。

### 🏆 Top 5 底层对标: vs `2025-AstranciA-哈尔滨工业大学深圳`（哈尔滨工业大学（深圳） / AstranciA）

#### 对方作品信息

**对方年份**: `2025`
**对方学校**: `哈尔滨工业大学（深圳）`
**对方队伍名称**: `AstranciA`
**对方比赛名字**: `OS-Competition`
**对方比赛子赛道**: `Kernel-Implementation`
**对方仓库地址**: `https://gitlab.eduxiji.net/educg-group-36002-2710490/T202518123995667-1436`

> 双向覆盖率：共同 AST 指纹: 180；唯一 AST 指纹: 目标项目 2659，参考项目 3750；覆盖率: 目标项目 6.77%，参考项目 4.8%

## 全模块对比总览（确定性证据）

> 总体覆盖率：共同 AST 指纹: 180；唯一 AST 指纹: 目标项目 2659，参考项目 3750；覆盖率: 目标项目 6.77%，参考项目 4.8%

### 1. 模块覆盖矩阵
| 模块 | 目标函数数 | 参考函数数 | 共同唯一 AST 指纹 | Target 覆盖 | Base 覆盖 | 状态 |
| :--- | ---: | ---: | ---: | ---: | ---: | :--- |
| 进程管理 | 139 | 112 | 2 | 1.63% | 2.17% | 双方存在且有结构重合 |
| 任务调度/异步运行时 | 471 | 455 | 25 | 7.02% | 7.79% | 双方存在且有结构重合 |
| 内存管理 | 410 | 343 | 14 | 3.61% | 4.55% | 双方存在且有结构重合 |
| 中断/异常处理 | 59 | 71 | 12 | 21.43% | 21.05% | 双方存在且有结构重合 |
| 系统调用 | 224 | 136 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |
| 文件系统 | 1034 | 620 | 11 | 1.3% | 2.27% | 双方存在且有结构重合 |
| 设备驱动 | 100 | 142 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |
| 网络协议栈 | 290 | 61 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |
| 文件系统/第三方库 | 39 | 775 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |
| 网络协议栈/第三方库 | 0 | 217 | 0 | 0.0% | 0.0% | 仅参考项目体现 |
| 第三方库 | 0 | 1155 | 0 | 0.0% | 0.0% | 仅参考项目体现 |
| 通用模块/其他 | 469 | 673 | 77 | 20.92% | 14.18% | 双方存在且有结构重合 |

### 2. 分模块证据清单

#### 进程管理
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `kernel/src/syscall/signal.rs#sys_rt_sigprocmask` | `AstrancE/ulib/axmono/src/syscall/signal.rs#sys_rt_sigprocmask` | 同板块同名 | Err, Ok, check_sigset_size, curr.as_thread, current, debug! ...(+7) | crate::task::signal::sys_sigprocmask |
| `kernel/src/task/signal.rs#send_signal_to_process` | `AstrancE/ulib/axmono/src/task/timer.rs#send_signal_to_process` | 同板块同名 | Ok, get_process_data, get_task, info!, proc_data.signal.send_signal, sig.signo ...(+1) | PROCESS_TABLE.read, PROCESS_TABLE.read().get, SigCode::Common, SigInfo::new_simple, Some, proc_data.signal.lock ...(+2) |
| `kernel/src/file/pidfd.rs#process_data` | `AstrancE/ulib/axmono/src/task/mod.rs#process_data` | 同板块同名 | Err, self.proc_data.upgrade, self.proc_data.upgrade().ok_or, thread_exit.load | self.thread.process, self.thread.process().data, self.thread.process().data().unwrap |
| `kernel/src/syscall/task/wait.rs#apply` | `AstrancE/ulib/axmono/src/task/wait.rs#apply` | 同板块同名 | child.group, child.group().pgid, child.pid | child.group, child.group().pgid, child.pid |

#### 任务调度/异步运行时
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `kernel/src/syscall/signal.rs#sys_kill` | `AstrancE/ulib/axmono/src/task/signal.rs#sys_kill` | 同板块同名 | Err, Ok, Some, crate::net::lookup_pid_netns, crate::net::lookup_pid_netns(pid).is_some, current ...(+16) | Ok, SigInfo_::Generic, Signal::from_u32, current, current().task_ext, current().task_ext().thread.process ...(+8) |
| `patches/axcpu/src/aarch64/init.rs#switch_to_el1` | `AstrancE/modules/axhal/src/platform/aarch64_common/boot.rs#switch_to_el1` | 同板块同名 | CNTHCTL_EL2.modify, CNTVOFF_EL2.set, CurrentEL.read, ELR_EL2.set, ELR_EL3.set, HCR_EL2.write ...(+9) | CNTHCTL_EL2.modify, CNTVOFF_EL2.set, CurrentEL.read, ELR_EL2.set, ELR_EL3.set, HCR_EL2.write ...(+8) |
| `kernel/src/syscall/task/clone3.rs#try_from` | `AstrancE/ulib/axmono/src/pthread/mod.rs#try_from` | 同板块同名 | CloneFlags::from_bits_truncate, Err, Ok, flags.contains, flags.intersects, warn! | Self::try_from, Self::try_from((value as u32 & FUTEX_CMD_MASK as u32) as u8).map_err |
| `kernel/src/syscall/ipc/shm.rs#sys_shmdt` | `AstrancE/ulib/axmono/src/syscall/ipc.rs#sys_shmdt` | 同板块同名 | Ok, SHM_MANAGER.lock, VirtAddr::from, aspace.unmap, curr.as_thread, current ...(+14) | (shmaddr as usize).into, axmm_shm_dt, axmm_shm_dt((shmaddr as usize).into(), &mut aspace)
        .map, axmm_shm_dt((shmaddr as usize).into(), &mut aspace)
        .map(|_| 0 as isize) // Return 0 on success
        .map_err, curr.task_ext, curr.task_ext().process_data ...(+3) |

#### 内存管理
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `patches/axplat-loongarch64-qemu-virt/src/boot.rs#_start` | `AstrancE/modules/axhal/src/platform/loongarch64_2k1000/boot.rs#_start` | 同板块同名 | core::arch::naked_asm! | core::arch::naked_asm! |
| `kernel/src/mm/aspace/mod.rs#handle_page_fault` | `AstrancE/modules/axmm/src/aspace/mod.rs#handle_page_fault` | 同板块同名 | VirtAddrRange::from_start_size, area.backend, area.backend().page_size, area.backend().populate, area.flags, cb ...(+6) | area
                    .backend, area
                    .backend()
                    .clone, area
                    .backend()
                    .clone()
                    .handle_page_fault, area.flags, debug!, orig_flags.contains ...(+2) |
| `kernel/src/mm/access.rs#check_null_terminated` | `AstrancE/ulib/axmono/src/ptr.rs#check_null_terminated` | 同板块同名 | Err, Layout::new::<T>, Layout::new::<T>().align, Ok, T::default, access_user_memory ...(+10) | Err, Layout::new::<T>, Layout::new::<T>().align, Ok, T::default, VirtAddrRange::from_start_size ...(+12) |
| `patches/axcpu/src/riscv/trap.rs#riscv_trap_handler` | `AstrancE/modules/axhal/src/arch/riscv/trap.rs#riscv_trap_handler` | 同板块同名 | handle_breakpoint, handle_page_fault, handle_trap!, panic!, scause.cause, scause.cause().try_into::<I, E> ...(+4) | crate::trap::handle_syscall, handle_breakpoint, handle_page_fault, handle_trap!, mask_irqs, panic! ...(+9) |

#### 中断/异常处理
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `patches/axplat-loongarch64-qemu-virt/src/irq.rs#set_enable` | `AstrancE/modules/axhal/src/platform/loongarch64_2k1000/irq.rs#set_enable` | 同板块同名 | IrqType::new, ecfg::read, ecfg::read().lie, ecfg::set_lie, eiointc::disable_irq, eiointc::enable_irq ...(+4) | ecfg::read, ecfg::read().lie, ecfg::set_lie |
| `patches/axcpu/src/x86_64/asm.rs#disable_irqs` | `AstrancE/modules/axhal/src/arch/x86_64/mod.rs#disable_irqs` | 同板块同名同结构 | interrupts::disable, warn! | interrupts::disable, warn! |
| `patches/axcpu/src/x86_64/asm.rs#enable_irqs` | `AstrancE/modules/axhal/src/arch/x86_64/mod.rs#enable_irqs` | 同板块同名同结构 | interrupts::enable, warn! | interrupts::enable, warn! |
| `patches/axcpu/src/x86_64/asm.rs#wait_for_irqs` | `AstrancE/modules/axhal/src/arch/x86_64/mod.rs#wait_for_irqs` | 同板块同名同结构 | asm!, cfg!, core::hint::spin_loop | asm!, cfg!, core::hint::spin_loop |

#### 系统调用
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `kernel/src/syscall/io_mpx/epoll.rs#sys_epoll_ctl` | `AstrancE/api/arceos_posix_api/src/imp/io_mpx/epoll.rs#sys_epoll_ctl` | 同板块同名 | Epoll::from_fd, EpollFlags::from_bits, EpollFlags::from_bits(event.events & !events.bits()).ok_or, Err, IoEvents::from_bits_truncate, Ok ...(+7) | debug!, syscall_body! |
| `kernel/src/syscall/time.rs#sys_times` | `AstrancE/ulib/axsyscall/src/syscall_imp/time.rs#sys_times` | 同板块同名 | Ok, borrow_thread_time, borrow_thread_time(current().as_thread())?.output, current, current().as_thread, monotonic_time_nanos ...(+4) | axmono::syscall::time::sys_times |
| `kernel/src/syscall/time.rs#sys_getitimer` | `AstrancE/ulib/axmono/src/syscall/time.rs#sys_getitimer` | 同板块同名 | ITimerType::from_repr, ITimerType::from_repr(which).ok_or, Ok, borrow_thread_time, borrow_thread_time(current().as_thread())?.get_itimer, current ...(+3) | Err, ItimerConfig::default, Ok, curr_value.is_null, timer::get_itimer |
| `kernel/src/syscall/net/socket.rs#sys_shutdown` | `AstrancE/api/arceos_posix_api/src/imp/net.rs#sys_shutdown` | 同板块同名 | Err, Socket::from_fd, debug!, socket.shutdown, socket.shutdown(how).map | debug!, syscall_body! |

#### 文件系统
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `kernel/src/syscall/fs/fd_ops.rs#sys_close` | `AstrancE/api/arceos_posix_api/src/imp/fd_ops.rs#sys_close` | 同板块同名 | Ok, cleanup_flock_locks_on_close, close_file_like, crate::file::fanotify::is_nonotify_fd, debug!, f.downcast_arc::<File> ...(+14) | debug!, syscall_body! |
| `kernel/src/syscall/fs/io.rs#sys_copy_file_range` | `AstrancE/api/arceos_posix_api/src/imp/fd_ops.rs#sys_copy_file_range` | 同板块同名 | Default::default, File::from_fd, Ok, SendFile::Direct, SendFile::Offset, Some ...(+12) | Err, Ok, SeekFrom::Current, SeekFrom::Start, core::cmp::min, debug! ...(+11) |
| `kernel/src/syscall/fs/fd_ops.rs#flags_to_options` | `AstrancE/api/arceos_posix_api/src/imp/fs.rs#flags_to_options` | 同板块同名 | OpenOptions::new, options.append, options.create, options.create_new, options.direct, options.directory ...(+8) | OpenOptions::new, options.append, options.create, options.directory, options.execute, options.read ...(+2) |
| `kernel/src/pseudofs/dev/event.rs#read_at` | `AstrancE/modules/axmm/src/shm/posix.rs#read_at` | 同板块同名 | Err, Ok, buf.chunks_exact_mut, buf.is_empty, buf.len, inner.has_event ...(+8) | (self.size - offset).min, Ok, buf.len, buf[bytes_read..bytes_read + bytes_to_copy].copy_from_slice, self
                .get_slice_at, self
                .get_slice_at(current_offset)
                .ok_or ...(+2) |

#### 设备驱动
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `patches/axnet-ng/src/udp.rs#send` | `AstrancE/tools/bwbench_client/src/device.rs#send` | 同板块同名 | Err, IpEndpoint::from, Ok, PacketMeta::default, Some, addr.into_ip ...(+21) | Err, Ok, buffer.as_ptr, buffer.len, err.kind, libc::send ...(+2) |
| `patches/axnet-ng/src/lib.rs#init_network` | `AstrancE/modules/axnet/src/lib.rs#init_network` | 同板块同名 | "eth0".to_owned, Box::new, EthernetAddress, EthernetDevice::new, GATEWAY.parse, GATEWAY.parse().expect ...(+28) | info!, net_devs.take_one, net_devs.take_one().expect, net_impl::init |
| `patches/axnet-ng/src/device/ethernet.rs#recv` | `AstrancE/tools/bwbench_client/src/device.rs#recv` | 同板块同名 | matches!, rx_buf.packet, self.handle_frame, self.inner.receive, self.inner.recycle_rx_buffer, self.inner.recycle_rx_buffer(rx_buf).unwrap ...(+2) | Err, Ok, buffer.as_mut_ptr, buffer.len, err.kind, libc::recv ...(+2) |
| `patches/axplat-loongarch64-qemu-virt/src/console.rs#init_early` | `AstrancE/modules/axhal/src/platform/loongarch64_2k1000/console.rs#init_early` | 同板块同名 | UART_BASE.init_once, p.add, p.add(1).write_volatile, p.add(2).write_volatile, p.add(3).write_volatile, p.add(4).write_volatile ...(+4) | SpinNoIrq::new, UART.init_once, Uart::new |

#### 网络协议栈
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `patches/axnet-ng/src/udp.rs#recv` | `AstrancE/api/arceos_posix_api/src/imp/net.rs#recv` | 同板块同名 | (Ipv4Addr::UNSPECIFIED, 0).into, Err, ExpectedRemote::Any, ExpectedRemote::Expecting, Ok, SocketAddrEx::Ip ...(+22) | Ok, tcpsocket.lock, tcpsocket.lock().recv, udpsocket.lock, udpsocket.lock().recv_from, udpsocket.lock().recv_from(buf).map |
| `patches/axnet-ng/src/vsock/stream.rs#recv` | `AstrancE/ulib/axstd/src/net/udp.rs#recv` | 同板块同名 | Err, Ok, conn.lock, conn_guard.advance_rx_read, conn_guard.rx_buffer_used, conn_guard.rx_closed ...(+9) | api::ax_udp_recv |
| `patches/axnet-ng/src/listen_table.rs#accept` | `AstrancE/api/arceos_posix_api/src/imp/net.rs#accept` | 同板块同名 | Err, Ok, entry.lock, is_closed, is_connected, is_connected(handle).then_some ...(+9) | Err, Ok, tcpsocket.lock, tcpsocket.lock().accept |
| `patches/axnet-ng/src/vsock/stream.rs#accept` | `AstrancE/ulib/axstd/src/net/tcp.rs#accept` | 同板块同名 | Err, GeneralOptions::default, Mutex::new, Ok, Some, StateLock::new ...(+12) | TcpStream, api::ax_tcp_accept, api::ax_tcp_accept(&self.0).map |

#### 文件系统/第三方库
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `patches/axfs-ng/src/fs/ext4/inode.rs#create` | `AstrancE/modules/axfs/src/fs/lwext4_rust.rs#create` | 同板块同名 | DirEntry::new_dir, DirEntry::new_file, DirNode::new, Err, FileNode::new, Inode::new ...(+14) | Ok, e.try_into, e.try_into().unwrap, file.check_inode_exist, file.dir_mk, file.dir_mk(fpath)
                    .map ...(+12) |
| `patches/axfs-ng/src/fs/ext4/inode.rs#read_dir` | `AstrancE/crates/axfs_crates/axfs_procfs/src/dir.rs#read_dir` | 同板块同名 | Ok, core::str::from_utf8, core::str::from_utf8(entry.name())
                .map_err, core::str::from_utf8(entry.name())
                .map_err(|_| VfsError::InvalidData)?
                .to_owned, entry.ino, entry.inode_type ...(+10) | BTreeMap::new, Ok, VfsDirEntry::new, all_children.insert, all_children.iter, all_children.iter().skip ...(+10) |
| `patches/axfs-ng/src/fs/ext4/inode.rs#rename` | `AstrancE/modules/axfs/src/fs/lwext4_rust.rs#rename` | 同板块同名 | dst_dir.downcast, dst_dir.downcast().map_err, fs.rename, fs.rename(self.ino, src_name, dst_dir.ino, dst_name)
            .map_err, self.fs.lock | e.try_into, e.try_into().unwrap, file.file_rename, file.file_rename(src_path.as_str(), dst_path)
            .map, file.file_rename(src_path.as_str(), dst_path)
            .map(|_v| ())
            .map_err, self.0.lock ...(+2) |
| `patches/axfs-ng/src/fs/ext4/inode.rs#link` | `AstrancE/modules/axfs/src/fs/lwext4_rust.rs#link` | 同板块同名 | fs.link, fs.link(self.ino, name, node.inode() as _)
            .map_err, node.inode, self.fs.lock, self.lookup_locked, self.update_ctime_locked | e.try_into, e.try_into().unwrap, file.file_link, file.file_link(src_path.as_str(), dst_path)
            .map, file.file_link(src_path.as_str(), dst_path)
            .map(|_v| ())
            .map_err, self.0.lock ...(+2) |

#### 网络协议栈/第三方库
仅参考项目中观察到该模块代表函数，目标项目没有同模块可对齐证据：
- 参考项目：`AstrancE/crates/virtio-drivers/src/device/socket/connectionmanager.rs#send_recv`，调用摘要："Hello from guest".as_bytes, (hello_from_guest.len() as u32).into, (hello_from_host.len() as u32).into, 0.into, 50.into, Arc::new ...(+43)
- 参考项目：`AstrancE/crates/virtio-drivers/src/device/socket/connectionmanager.rs#incoming_connection`，调用摘要：0.into, 50.into, Arc::new, Mutex::new, ReadOnly::new, SocketType::Stream.into ...(+24)
- 参考项目：`AstrancE/crates/virtio-drivers/src/device/socket/connectionmanager.rs#poll`，调用摘要：Connection::new, Err, Ok, SocketError::OutputBufferTooShort, SocketError::OutputBufferTooShort(length).into, Some ...(+16)
- 参考项目：`AstrancE/crates/virtio-drivers/examples/x86_64/src/tcp.rs#test_echo_server`，调用摘要：Config::new, DeviceWrapper::new, Instant::from_micros_const, Interface::new, IpAddress::from_str, IpAddress::from_str(IP).unwrap ...(+44)

#### 第三方库
仅参考项目中观察到该模块代表函数，目标项目没有同模块可对齐证据：
- 参考项目：`AstrancE/crates/axdriver_crates/axdriver_block/src/dw_mshc/mmc.rs#card_init`，调用摘要：CID::from, CLKDIV::offset, CMD::data_cmd, CMD::no_data_cmd, CMD::no_data_cmd_no_crc, CMD::no_data_cmd_no_crc(card_idx, 2).with_response_length ...(+33)
- 参考项目：`AstrancE/crates/virtio-drivers/src/queue.rs#fake_read_write_queue`，调用摘要：(*available_ring).idx.load, (*used_ring).idx.fetch_add, (*used_ring).idx.load, Vec::new, assert!, assert_eq! ...(+18)
- 参考项目：`AstrancE/crates/virtio-drivers/src/device/sound/fake.rs#handle_control_request`，调用摘要：CommandCode::SOk.into, Some, Vec::new, VirtIOSndHdr {
                            command_code: CommandCode::SOk.into(),
                        }
                        .as_bytes, VirtIOSndHdr::read_from_prefix, VirtIOSndHdr::read_from_prefix(&request)
                .expect ...(+16)
- 参考项目：`AstrancE/crates/virtio-drivers/src/transport/pci.rs#new`，调用摘要：Err, Ok, Some, UniqueMmioPointer::new, VirtioPciError::InvalidDeviceId, VirtioPciError::InvalidNotifyOffMultiplier ...(+15)

#### 通用模块/其他
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `patches/axcpu/src/loongarch64/unaligned.rs#read_fpr` | `AstrancE/modules/axhal/src/arch/loongarch64/unaligned.rs#read_fpr` | 同板块同名同结构 | asm_read_fpr_0, asm_read_fpr_1, asm_read_fpr_10, asm_read_fpr_11, asm_read_fpr_12, asm_read_fpr_13 ...(+27) | asm_read_fpr_0, asm_read_fpr_1, asm_read_fpr_10, asm_read_fpr_11, asm_read_fpr_12, asm_read_fpr_13 ...(+27) |
| `patches/axio/src/buffered/linewriter/shim.rs#write_all` | `AstrancE/ulib/axlibc/src/strftime.rs#write_all` | 同板块同名 | buf.split_at, memchr::memrchr, self.buffer.flush_buf, self.buffer.write_all, self.buffered, self.buffered().is_empty ...(+3) | Err, Ok, buf.len, self.inner.write_all |
| `patches/axcpu/src/loongarch64/unaligned.rs#write_fpr` | `AstrancE/modules/axhal/src/arch/loongarch64/unaligned.rs#write_fpr` | 同板块同名同结构 | asm_write_fpr_0, asm_write_fpr_1, asm_write_fpr_10, asm_write_fpr_11, asm_write_fpr_12, asm_write_fpr_13 ...(+27) | asm_write_fpr_0, asm_write_fpr_1, asm_write_fpr_10, asm_write_fpr_11, asm_write_fpr_12, asm_write_fpr_13 ...(+27) |
| `patches/axio/src/read/mod.rs#read_until` | `AstrancE/ulib/axstd/src/io/stdio.rs#read_until` | 同板块同名 | Ok, available.len, buf.extend_from_slice, memchr::memchr, self.consume, self.fill_buf | self.inner.read_until |


好的，我将遵循审计指令，以仲裁专家身份，基于客观数据与底层证据，对本案进行最终定性。

### 1. 宏观判定
- **定性结论**: **独立开发，底层架构存在显著差异**
  客观 AST 相似度仅为 2.89%，远低于构成“实质性相似”的阈值。双方在进程管理、内存管理、任务调度等核心模块虽有少量同板块同名函数，但其调用链多为 Linux/POSIX ABI 强制的单行存取器或标准模式，不构成代码抄袭。全模块矩阵中，系统调用、设备驱动、网络协议栈等关键板块的共同指纹为零，进一步印证了双方在技术选型与实现路径上的独立性。

### 2. 全模块覆盖矩阵解读
- **进程管理**: 双方均存在。共同 AST 指纹仅 2 个，覆盖率极低（目标 1.63%，参考 2.17%）。控方列举的“铁证”均为 ABI 强制的单行存取器或标准查询模式，表达空间有限。**证据强度：弱**。
- **任务调度/异步运行时**: 双方均存在。共同指纹 25 个，覆盖率中等（目标 7.02%，参考 7.79%）。重合点多为 `sys_kill`、`sys_shmdt` 等系统调用入口，其内部实现（如信号投递路径、内存映射管理）存在显著差异。**证据强度：弱**。
- **内存管理**: 双方均存在。共同指纹 14 个，覆盖率较低（目标 3.61%，参考 4.55%）。`handle_page_fault` 等核心函数同名，但目标项目通过 `area.backend().populate` 处理缺页，参考项目则调用 `backend.clone().handle_page_fault`，二者在内存模型和错误处理上路径不同。**证据强度：弱**。
- **中断/异常处理**: 双方均存在。共同指纹 12 个，覆盖率较高（目标 21.43%，参考 21.05%）。重合点集中在 `disable_irqs`、`enable_irqs` 等极短汇编封装函数，以及 `riscv_trap_handler` 这类由硬件规范定义的标准分发逻辑。**证据强度：中（但属硬件规范强制）**。
- **系统调用**: 双方均存在。共同指纹为 0。尽管存在 `sys_epoll_ctl`、`sys_times` 等同名函数，但目标项目为直接实现，参考项目多被 `syscall_body!` 宏包裹，导致 AST 结构无法对齐。**证据强度：未形成**。
- **文件系统**: 双方均存在。共同指纹 11 个，覆盖率低（目标 1.3%，参考 2.27%）。`flags_to_options` 等函数同名，但此类将 POSIX 标志位转换为内部选项的逻辑是标准模式。**证据强度：弱**。
- **设备驱动**: 双方均存在。共同指纹为 0。目标项目基于 `axnet-ng` 的自研网络驱动栈，参考项目则包含 `virtio-drivers` 等第三方库，二者驱动模型完全不同。**证据强度：未形成**。
- **网络协议栈**: 双方均存在。共同指纹为 0。目标项目拥有独立的 `axnet-ng` 协议栈（含 vsock 支持），参考项目则依赖 `axnet` 及用户态工具，架构差异巨大。**证据强度：未形成**。
- **第三方库/公共依赖**: 参考项目包含大量第三方库（如 `virtio-drivers`），目标项目则无。此部分为单方特征，不构成同源证据。**证据强度：未形成**。

### 3. 底层 AST 关键证据指纹表
| 所属板块 | 目标项目路径 | 参考项目路径 | 证据类型 | 技术说明 |
| :--- | :--- | :--- | :--- | :--- |
| 进程管理 | `kernel/src/task/mod.rs#set_clear_child_tid` | `AstrancE/ulib/axmono/src/task/process.rs#set_clear_child_tid` | 同板块同名，单行存取器 | Linux `futex` 机制强制的 `clear_child_tid` 字段原子写入，表达单一。 |
| 进程管理 | `kernel/src/task/mod.rs#clear_child_tid` | `AstrancE/ulib/axmono/src/task/process.rs#clear_child_tid` | 同板块同名，单行存取器 | 与上一条成对，为同一 ABI 字段的原子读取，无创造性。 |
| 进程管理 | `kernel/src/syscall/task/wait.rs#apply` | `AstrancE/ulib/axmono/src/task/wait.rs#apply` | 同板块同名，短调用链 | POSIX `waitpid` 按进程组和 PID 过滤子进程的标准语义映射，调用链极短。 |
| 进程管理 | `kernel/src/task/ops.rs#get_process_group` | `AstrancE/ulib/axmono/src/task/mod.rs#get_process_group` | 同板块同名，标准模式 | Rust 中 `RwLock<HashMap>.read().get(key).ok_or` 惯用的全局表查询模式。 |
| 进程管理 | `kernel/src/task/ops.rs#processes` | `AstrancE/ulib/axmono/src/task/mod.rs#processes` | 同板块同名，标准模式 | Rust 中 `HashMap.values().collect()` 标准迭代器收集逻辑。 |
| 中断/异常处理 | `patches/axcpu/src/x86_64/asm.rs#disable_irqs` | `AstrancE/modules/axhal/src/arch/x86_64/mod.rs#disable_irqs` | 同板块同名同结构 | x86 `CLI` 指令的极简 Rust 封装，由硬件规范决定，无独立表达空间。 |
| 中断/异常处理 | `patches/axcpu/src/riscv/trap.rs#riscv_trap_handler` | `AstrancE/modules/axhal/src/arch/riscv/trap.rs#riscv_trap_handler` | 同板块同名 | RISC-V 特权架构规范定义的标准陷阱分发流程，包含 `handle_page_fault` 等公共入口。 |
| 内存管理 | `kernel/src/mm/aspace/mod.rs#handle_page_fault` | `AstrancE/modules/axmm/src/aspace/mod.rs#handle_page_fault` | 同板块同名 | 缺页处理的标准入口，但内部处理逻辑（`populate` vs `clone().handle_page_fault`）不同。 |
| 通用模块/其他 | `patches/axcpu/src/loongarch64/unaligned.rs#read_fpr` | `AstrancE/modules/axhal/src/arch/loongarch64/unaligned.rs#read_fpr` | 同板块同名同结构 | LoongArch 架构下读写浮点寄存器的汇编宏展开，由硬件规范决定。 |

### 4. 核心代码调用流对比剖析
* **【进程管理 / 获取父进程ID (`sys_getppid`)】**
* **底层技术析理**: 两者均遵循 POSIX 语义，从当前任务出发，向上查找父进程并返回其 PID。然而，其内部对象模型截然不同。目标项目采用 `current().as_thread().proc_data.proc.parent()` 路径，体现了 `proc_data` 作为线程内嵌数据的扁平化设计。参考项目则采用 `current().task_ext().thread.process().parent()` 路径，通过 `task_ext` 扩展和 `thread.process()` 进行多层解引用，展现了不同的任务结构抽象层次。错误处理上，目标项目使用 `AxError::NoSuchProcess`，参考项目则通过 `map` 和 `ok_or` 组合处理，API 选择不同。
* **代码溯源链接**:
  - 目标项目: `kernel/src/syscall/task/thread.rs#sys_getppid`
  - 参考项目: `AstrancE/ulib/axmono/src/syscall/process.rs#sys_getppid`

* **【内存管理 / 缺页处理 (`handle_page_fault`)】**
* **底层技术析理**: 两者均处理地址空间中的缺页异常，但后端交互模式不同。目标项目直接调用 `area.backend().populate(addr, area.flags)`，将填充逻辑委托给后端，体现了面向后端的策略模式。参考项目则先 `clone` 后端，再调用 `clone().handle_page_fault(offset, orig_flags)`，其 `handle_page_fault` 方法接收偏移量和原始标志，表明其内存后端抽象封装了更完整的缺页处理逻辑。两者在错误处理、日志输出和权限检查的具体实现上亦存在差异。
* **代码溯源链接**:
  - 目标项目: `kernel/src/mm/aspace/mod.rs#handle_page_fault`
  - 参考项目: `AstrancE/modules/axmm/src/aspace/mod.rs#handle_page_fault`

### 5. 单方模块与降权项
- **单方模块**: 参考项目中的 `网络协议栈/第三方库`、`第三方库` 模块（如 `virtio-drivers`）在目标项目中无对应实现，属于参考项目的独立技术选型，不构成同源证据。
- **降权项**: 控辩双方均已主动降级的 `sys_rt_sigprocmask`、`send_signal_to_process`、`process_data` 等函数，因 AST 结构或实现路径存在实质性差异，不作为抄袭证据。此外，所有跨板块的同名函数（如将网络协议栈的 `process_arp` 归入进程管理）均被降权，因其违反了同板块比对原则。
- **公共接口与泛型宏**: `println!`、`format!`、`debug!`、`syscall_body!` 等 Rust 标准库或项目内泛型宏，其产生的 AST 节点具有语言级共性，不能作为项目间复用的依据。

### 6. 保守结论
本案客观 AST 相似度仅 2.89%，全模块覆盖矩阵中多数核心板块共同指纹为零，表明双方在整体架构与实现路径上存在根本性差异。控方所指控的“铁证”均为 Linux ABI、POSIX 语义或 Rust 语言惯用法强制形成的极短调用链或单行存取器，其表达空间极其有限，不构成著作权法意义上的“实质性相似”。辩方关于独立开发的辩护与客观数据吻合。综合判定，目标项目不存在对参考项目的整体或局部代码抄袭，其底层架构的显著差异支持独立开发的结论。建议驳回控方全部指控。

------------------------------

