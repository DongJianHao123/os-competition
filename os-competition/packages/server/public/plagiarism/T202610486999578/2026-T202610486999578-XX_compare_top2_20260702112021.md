# 🛡️ OS 内核赛自动化代码级架构审计报告

**审计时间**: `2026-07-02 11:20:21`
**参赛作品名称**: `2026-T202610486999578-XX`
**年份**: `2026`
**学校**: `XX`
**队伍名称**: `T202610486999578`
**比赛名字**: `OS-Competition`
**比赛子赛道**: `Kernel-Implementation`
**仓库地址**: `https://gitlab.eduxiji.net/educg-group-43501-3132633/T202610486999578-700`
**推测开发语言**: `Rust / C (由AST探针自动识别)`
**特征函数量**: `3235`

## ⚔️ 底层代码同源性对比 (AST Homology)
> 本部分由专门的代码审查智能体，对该项目与符合年份条件的历届最高相似度项目进行控制流与底层调用对比。新版报告会先给出确定性的全模块覆盖矩阵，再给出 Agent 的分模块解释，避免只围绕少量抽样函数展开。

### 🏆 Top 2 底层对标: vs `2025-StarryX-杭州电子科技大学`（杭州电子科技大学 / StarryX）

#### 对方作品信息

**对方年份**: `2025`
**对方学校**: `杭州电子科技大学`
**对方队伍名称**: `StarryX`
**对方比赛名字**: `OS-Competition`
**对方比赛子赛道**: `Kernel-Implementation`
**对方仓库地址**: `https://gitlab.eduxiji.net/educg-group-36002-2710490/T202510336995486-3037`

> 双向覆盖率：共同 AST 指纹: 239；唯一 AST 指纹: 目标项目 2659，参考项目 2897；覆盖率: 目标项目 8.99%，参考项目 8.25%

## 全模块对比总览（确定性证据）

> 总体覆盖率：共同 AST 指纹: 239；唯一 AST 指纹: 目标项目 2659，参考项目 2897；覆盖率: 目标项目 8.99%，参考项目 8.25%

### 1. 模块覆盖矩阵
| 模块 | 目标函数数 | 参考函数数 | 共同唯一 AST 指纹 | Target 覆盖 | Base 覆盖 | 状态 |
| :--- | ---: | ---: | ---: | ---: | ---: | :--- |
| 进程管理 | 139 | 172 | 5 | 4.07% | 3.14% | 双方存在且有结构重合 |
| 任务调度/异步运行时 | 471 | 480 | 25 | 7.02% | 7.12% | 双方存在且有结构重合 |
| 内存管理 | 410 | 430 | 23 | 5.93% | 5.91% | 双方存在且有结构重合 |
| 中断/异常处理 | 59 | 70 | 12 | 21.43% | 19.35% | 双方存在且有结构重合 |
| 系统调用 | 224 | 89 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |
| 文件系统 | 1034 | 801 | 93 | 10.97% | 14.98% | 双方存在且有结构重合 |
| 设备驱动 | 100 | 139 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |
| 网络协议栈 | 290 | 77 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |
| 文件系统/第三方库 | 39 | 164 | 16 | 41.03% | 10.19% | 双方存在且有结构重合 |
| 网络协议栈/第三方库 | 0 | 166 | 0 | 0.0% | 0.0% | 仅参考项目体现 |
| 第三方库 | 0 | 349 | 0 | 0.0% | 0.0% | 仅参考项目体现 |
| 通用模块/其他 | 469 | 665 | 13 | 3.53% | 2.45% | 双方存在且有结构重合 |

### 2. 分模块证据清单

#### 进程管理
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `kernel/src/task/ops.rs#exit_robust_list` | `xapi/src/task/futex.rs#exit_robust_list` | 同板块同名 | Err, Ok, axtask::yield_now, core::ptr::eq, entry.vm_read, handle_futex_death ...(+2) | Err, Ok, UserPtr::from, axtask::yield_now, core::ptr::eq, handle_futex_death ...(+2) |
| `kernel/src/syscall/signal.rs#sys_rt_sigprocmask` | `xapi/src/task/signal.rs#sys_rt_sigprocmask` | 同板块同名 | Err, Ok, check_sigset_size, curr.as_thread, current, debug! ...(+7) | Err, Ok, XProcess::from_thread, XProcess::from_thread(thread).uspace, XThread::from_thread, check_sigset_size ...(+3) |
| `kernel/src/syscall/ipc/shm.rs#clear_proc_shm` | `xapi/src/ipc/shm.rs#clear_proc_shm` | 同板块同名 | self.get_inner_by_shmid, self.get_shmids_by_pid, self.remove_pid, self.remove_shmid, shm_inner.attach_count, shm_inner.detach_process ...(+1) | IPC_MANAGER.with_shm, segment.detach_process, segment.detach_process(pid).is_ok, segment.lock, segment.should_remove, shm_manager.cleanup_orphaned_segments ...(+3) |
| `kernel/src/syscall/fs/pidfd.rs#sys_pidfd_getfd` | `xapi/src/fs/fd/pid.rs#sys_pidfd_getfd` | 同板块同名 | FD_TABLE
        .scope, FD_TABLE
        .scope(&proc_data.scope.read())
        .read, FD_TABLE
        .scope(&proc_data.scope.read())
        .read()
        .get, FD_TABLE
        .scope(&proc_data.scope.read())
        .read()
        .get(target_fd as usize)
        .ok_or, FD_TABLE
        .scope(&proc_data.scope.read())
        .read()
        .get(target_fd as usize)
        .ok_or(AxError::BadFileDescriptor)
        .and_then, Ok ...(+6) | Err, FD_TABLE
        .deref_from, FD_TABLE
        .deref_from(&XProcess::from_thread(&thread).ns)
        .get, FD_TABLE
        .deref_from(&XProcess::from_thread(&thread).ns)
        .get(targetfd as _)
        .ok_or, Ok, PidFd::from_fd ...(+5) |

#### 任务调度/异步运行时
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `kernel/src/syscall/sync/futex.rs#sys_futex` | `xapi/src/task/futex.rs#sys_futex` | 同板块同名 | AxError::from, Err, FutexKey::new_current, Ok, Some, assert_unsigned ...(+25) | Err, FutexKey::new, Ok, axtask::yield_now, current, current().task_ext ...(+24) |
| `kernel/src/syscall/fs/stat.rs#sys_faccessat2` | `xapi/src/fs/stat.rs#sys_faccessat2` | 同板块同名 | Err, LinuxError::EINVAL.into, LinuxError::ENOENT.into, Ok, curr.as_thread, current ...(+16) | AccessMode::from_bits, AccessMode::from_bits(mode).ok_or, Err, NodePermission::empty, Ok, access_mode.contains ...(+14) |
| `kernel/src/syscall/time.rs#sys_clock_gettime` | `xapi/src/sys/time.rs#sys_clock_gettime` | 同板块同名 | (now_ns + off_ns).max, Duration::from_nanos, Err, Ok, WALL_CLOCK_OFFSET.lock, current ...(+11) | Err, Ok, monotonic_time, timespec::from_time_value, trace!, uspace.write ...(+3) |
| `kernel/src/syscall/signal.rs#sys_kill` | `xapi/src/task/signal.rs#sys_kill` | 同板块同名 | Err, Ok, Some, crate::net::lookup_pid_netns, crate::net::lookup_pid_netns(pid).is_some, current ...(+16) | Ok, get_process, get_process_group, make_siginfo, proc.is_init, process.group ...(+5) |

#### 内存管理
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `kernel/src/syscall/fs/fd_ops.rs#sys_openat` | `xapi/src/fs/fd_ops.rs#sys_openat` | 同板块同名 | Arc::new, Default::default, Err, FS_CONTEXT.lock, FS_CONTEXT.lock().resolve, File::new ...(+61) | PAGE_CACHE_MANAGER.clear_stale_cache, add_to_fd, debug!, flags_to_options, fs.open, options.to_flags ...(+7) |
| `kernel/src/syscall/task/clone.rs#do_clone` | `xapi/src/task/clone.rs#do_clone` | 同板块同名 | (parent_tid as *mut Pid).vm_write, (parent_tid as *mut Pid).vm_write(tid).ok, (pidfd as *mut i32).vm_write, Arc::get_mut, Arc::new, AxTaskExt::from_impl ...(+97) | Arc::default, Arc::new, CloneFlags::from_bits_truncate, Err, FD_TABLE
                .deref_from, FD_TABLE
                .deref_from(&process_data.ns)
                .init_new ...(+76) |
| `kernel/src/syscall/mm/mmap.rs#sys_mmap` | `xapi/src/mm/mmap.rs#sys_mmap` | 同板块同名 | (1usize << rnd_bits.min(20)).saturating_sub, (addr + length).align_up, (curr.as_thread().proc_data.proc.pid() as u64).wrapping_shl, (max_slide / align).max, (max_slide_pages * PAGE_SIZE_4K).min, (slot * align).min ...(+68) | (addr + length).align_up, Err, FD_TABLE.is_assigned, File::from_fd, File::from_fd(fd, FileFlags::READ, FileFlags::empty())
            .map_err, FileFlags::empty ...(+39) |
| `kernel/src/syscall/task/execve.rs#sys_execve` | `xapi/src/task/execve.rs#sys_execve` | 同板块同名 | "/bin/sh".to_string, "TST_TIMEOUT=-1".into, Arc::new, Default::default, Err, FD_TABLE.write ...(+66) | Err, FD_TABLE.close_on_exec, FS_CONTEXT.lock, FS_CONTEXT.lock().canonicalize, FS_CONTEXT.lock().canonicalize(path)?.to_string, Ok ...(+35) |

#### 中断/异常处理
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `patches/axplat-loongarch64-qemu-virt/src/irq.rs#set_enable` | `arceos/modules/axhal/src/platform/loongarch64_qemu_virt/irq.rs#set_enable` | 同板块同名 | IrqType::new, ecfg::read, ecfg::read().lie, ecfg::set_lie, eiointc::disable_irq, eiointc::enable_irq ...(+4) | ecfg::read, ecfg::read().lie, ecfg::set_lie |
| `patches/axcpu/src/x86_64/asm.rs#disable_irqs` | `arceos/modules/axhal/src/arch/x86_64/mod.rs#disable_irqs` | 同板块同名同结构 | interrupts::disable, warn! | interrupts::disable, warn! |
| `patches/axcpu/src/x86_64/asm.rs#enable_irqs` | `arceos/modules/axhal/src/arch/x86_64/mod.rs#enable_irqs` | 同板块同名同结构 | interrupts::enable, warn! | interrupts::enable, warn! |
| `patches/axcpu/src/x86_64/asm.rs#wait_for_irqs` | `arceos/modules/axhal/src/arch/x86_64/mod.rs#wait_for_irqs` | 同板块同名同结构 | asm!, cfg!, core::hint::spin_loop | asm!, cfg!, core::hint::spin_loop |

#### 系统调用
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `kernel/src/syscall/io_mpx/epoll.rs#sys_epoll_ctl` | `arceos/api/arceos_posix_api/src/imp/io_mpx/epoll.rs#sys_epoll_ctl` | 同板块同名 | Epoll::from_fd, EpollFlags::from_bits, EpollFlags::from_bits(event.events & !events.bits()).ok_or, Err, IoEvents::from_bits_truncate, Ok ...(+7) | debug!, syscall_body! |
| `kernel/src/syscall/io_mpx/select.rs#sys_select` | `xapi/src/iomux/select.rs#sys_select` | 同板块同名 | 0.into, do_select, it.try_into_time_value, nullable!, nullable!(timeout.get_as_ref())?
            .map, nullable!(timeout.get_as_ref())?
            .map(|it| it.try_into_time_value())
            .transpose | do_select, nullable!, nullable!(uspace.read(timeout))?
                .map, nullable!(uspace.read(timeout))?
                .map(timeval::to_time_value)
                .transpose, with_uspace |
| `kernel/src/syscall/time.rs#sys_times` | `xapi/src/sys/time.rs#sys_times` | 同板块同名 | Ok, borrow_thread_time, borrow_thread_time(current().as_thread())?.output, current, current().as_thread, monotonic_time_nanos ...(+4) | Ok, monotonic_time_nanos, nanos_to_ticks, time_stat_output, uspace.write, with_uspace |
| `kernel/src/syscall/io_mpx/epoll.rs#sys_epoll_pwait2` | `xapi/src/iomux/epoll.rs#sys_epoll_pwait2` | 同板块同名 | do_epoll_wait, nullable!, nullable!(timeout.get_as_ref())?
        .map, nullable!(timeout.get_as_ref())?
        .map(|ts| ts.try_into_time_value())
        .transpose, ts.try_into_time_value | TimeValue::from_millis, nullable!, nullable!(uspace.read(timeout))?
            .map, nullable!(uspace.read(timeout))?
            .map(timespec::to_time_value)
            .transpose, sys_epoll_wait, timeout.unwrap_or ...(+2) |

#### 文件系统
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `kernel/src/pseudofs/dev/fb.rs#ioctl` | `xcore/src/fs/vfs/dev/loopx.rs#ioctl` | 同板块同名 | (arg as *mut FixScreenInfo).vm_write, (arg as *mut VarScreenInfo).vm_write, Err, Ok, axdisplay::framebuffer_info | Err, Ok, Some, argp.address, argp.address().as_usize, argp.cast ...(+24) |
| `kernel/src/syscall/net/opt.rs#sys_getsockopt` | `xapi/src/net/sockopt.rs#sys_getsockopt` | 同板块同名 | Arc::clone, Arc::clone(&f).downcast_arc::<CanSocket>, Arc::clone(&f).downcast_arc::<Ipv6RawSocket>, Err, IcmpSocket::from_fd, IcmpSocket::from_fd(fd).is_ok ...(+23) | CONGESTION_BYTES.len, Err, FileFlags::empty, Ok, Socket::from_fd, debug! ...(+9) |
| `kernel/src/file/ipv6_raw.rs#ioctl` | `xcore/src/fs/vfs/dev/tty.rs#ioctl` | 同板块同名 | AxError::from, Err, LinuxError::ENODEV.into, Ok, UserConstPtr::<u8>::from, UserPtr::<i16>::from ...(+20) | Err, Ok, argp.cast::<termios>, argp.cast::<winsize>, debug!, self.get_termios ...(+6) |
| `kernel/src/pseudofs/dev/loop.rs#ioctl` | `xcore/src/fs/vfs/dev/mod.rs#ioctl` | 同板块同名 | (arg as *const loop_info).vm_read_uninit, (arg as *const loop_info).vm_read_uninit()?.assume_init, (arg as *const u32).vm_read, (arg as *mut loop_info).vm_write, (arg as *mut u32).vm_write, (arg as *mut u64).vm_write ...(+22) | Ok, argp.cast::<rtc_time>, chrono::DateTime::from_timestamp_nanos, uspace.write, wall.day, wall.hour ...(+6) |

#### 设备驱动
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `patches/axnet-ng/src/udp.rs#send` | `arceos/tools/bwbench_client/src/device.rs#send` | 同板块同名 | Err, IpEndpoint::from, Ok, PacketMeta::default, Some, addr.into_ip ...(+21) | Err, Ok, buffer.as_ptr, buffer.len, err.kind, libc::send ...(+2) |
| `patches/axnet-ng/src/lib.rs#init_network` | `arceos/modules/axnet/src/lib.rs#init_network` | 同板块同名 | "eth0".to_owned, Box::new, EthernetAddress, EthernetDevice::new, GATEWAY.parse, GATEWAY.parse().expect ...(+28) | info!, net_devs.take_one, net_impl::init, warn! |
| `patches/axnet-ng/src/device/ethernet.rs#recv` | `arceos/tools/bwbench_client/src/device.rs#recv` | 同板块同名 | matches!, rx_buf.packet, self.handle_frame, self.inner.receive, self.inner.recycle_rx_buffer, self.inner.recycle_rx_buffer(rx_buf).unwrap ...(+2) | Err, Ok, buffer.as_mut_ptr, buffer.len, err.kind, libc::recv ...(+2) |
| `patches/axplat-loongarch64-qemu-virt/src/console.rs#init_early` | `arceos/modules/axhal/src/platform/loongarch64_qemu_virt/console.rs#init_early` | 同板块同名 | UART_BASE.init_once, p.add, p.add(1).write_volatile, p.add(2).write_volatile, p.add(3).write_volatile, p.add(4).write_volatile ...(+4) | SpinNoIrq::new, UART.init_once, Uart::new, phys_to_virt, vaddr.as_usize |

#### 网络协议栈
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `patches/axnet-ng/src/udp.rs#recv` | `xcore/src/net/socket.rs#recv` | 同板块同名 | (Ipv4Addr::UNSPECIFIED, 0).into, Err, ExpectedRemote::Any, ExpectedRemote::Expecting, Ok, SocketAddrEx::Ip ...(+22) | Ok, tcpsocket.lock, tcpsocket.lock().recv, udpsocket.lock, udpsocket.lock().recv_from, udpsocket.lock().recv_from(buf).map ...(+2) |
| `patches/axnet-ng/src/vsock/stream.rs#recv` | `arceos/api/arceos_posix_api/src/imp/net.rs#recv` | 同板块同名 | Err, Ok, conn.lock, conn_guard.advance_rx_read, conn_guard.rx_buffer_used, conn_guard.rx_closed ...(+9) | Ok, tcpsocket.lock, tcpsocket.lock().recv, udpsocket.lock, udpsocket.lock().recv_from, udpsocket.lock().recv_from(buf).map |
| `patches/axnet-ng/src/tcp.rs#recv` | `arceos/ulib/axstd/src/net/udp.rs#recv` | 同板块同名 | Err, Ok, ax_err_type!, dst.remaining_mut, dst.write, options.flags.contains ...(+12) | api::ax_udp_recv |
| `patches/axnet-ng/src/listen_table.rs#accept` | `xcore/src/net/socket.rs#accept` | 同板块同名 | Err, Ok, entry.lock, is_closed, is_connected, is_connected(handle).then_some ...(+9) | Err, Mutex::new, Socket::Tcp, Socket::Unix, tcpsocket
                .lock, tcpsocket
                .lock()
                .accept ...(+4) |

#### 文件系统/第三方库
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `patches/axfs-ng/src/fs/ext4/inode.rs#create` | `arceos/modules/axfs-ng/src/fs/ext4/inode.rs#create` | 同板块同名 | DirEntry::new_dir, DirEntry::new_file, DirNode::new, Err, FileNode::new, Inode::new ...(+14) | DirEntry::new_dir, DirEntry::new_file, DirNode::new, Err, FileNode::new, Inode::new ...(+11) |
| `patches/axfs-ng/src/fs/ext4/inode.rs#metadata` | `arceos/modules/axfs-ng/src/fs/ext4/inode.rs#metadata` | 同板块同名同结构 | DeviceId::default, FileAttr::default, NodePermission::from_bits_truncate, Ok, into_vfs_type, self.fs
            .lock ...(+2) | DeviceId::default, FileAttr::default, NodePermission::from_bits_truncate, Ok, into_vfs_type, self.fs
            .lock ...(+2) |
| `patches/axfs-ng/src/fs/ext4/inode.rs#read_dir` | `arceos/modules/axfs-ng/src/fs/ext4/inode.rs#read_dir` | 同板块同名同结构 | Ok, core::str::from_utf8, core::str::from_utf8(entry.name())
                .map_err, core::str::from_utf8(entry.name())
                .map_err(|_| VfsError::InvalidData)?
                .to_owned, entry.ino, entry.inode_type ...(+10) | Ok, core::str::from_utf8, core::str::from_utf8(entry.name())
                .map_err, core::str::from_utf8(entry.name())
                .map_err(|_| VfsError::EINVAL)?
                .to_owned, entry.ino, entry.inode_type ...(+10) |
| `patches/axfs-ng/src/fs/ext4/inode.rs#update_metadata` | `arceos/modules/axfs-ng/src/fs/ext4/inode.rs#update_metadata` | 同板块同名 | Ok, fs.with_inode_ref, fs.with_inode_ref(self.ino, |inode| {
            if let Some(mode) = update.mode {
                inode.set_mode((inode.mode() & !0xfff) | (mode.bits() as u32));
            }
            if let Some((uid, gid)) = update.owner {
                inode.set_owner(uid as _, gid as _);
            }
            if let Some(atime) = update.atime {
                inode.set_atime(&atime);
            }
            if let Some(mtime) = update.mtime {
                inode.set_mtime(&mtime);
            }
            inode.update_ctime();
            Ok(())
        })
        .map_err, inode.mode, inode.set_atime, inode.set_mode ...(+5) | Ok, fs.with_inode_ref, fs.with_inode_ref(self.ino, |inode| {
            if let Some(mode) = update.mode {
                inode.set_mode((inode.mode() & !0xfff) | (mode.bits() as u32));
            }
            if let Some((uid, gid)) = update.owner {
                inode.set_owner(uid as _, gid as _);
            }
            if let Some(atime) = update.atime {
                inode.set_atime(&atime);
            }
            if let Some(mtime) = update.mtime {
                inode.set_mtime(&mtime);
            }
            Ok(())
        })
        .map_err, inode.mode, inode.set_atime, inode.set_mode ...(+4) |

#### 网络协议栈/第三方库
仅参考项目中观察到该模块代表函数，目标项目没有同模块可对齐证据：
- 参考项目：`arceos/modules/axnet/src/smoltcp_impl/tcp.rs#connect`，调用摘要：Err, Ok, Ok::<(IpEndpoint, IpEndpoint), NetError>, SOCKET_SET
                .with_socket_mut::<tcp::Socket, _, _>, SOCKET_SET.add, SocketSetWrapper::new_tcp_socket ...(+33)
- 参考项目：`arceos/modules/axnet/src/smoltcp_impl/dns.rs#query`，调用摘要：Err, IpAddr::from, Ok, SOCKET_SET
            .with_socket_mut::<dns::Socket, _, _>, SOCKET_SET
            .with_socket_mut::<dns::Socket, _, _>(handle, |socket| {
                socket.start_query(iface.lock().context(), name, query_type)
            })
            .map_err, SOCKET_SET.poll_interfaces ...(+11)
- 参考项目：`arceos/modules/axnet/src/smoltcp_impl/bench.rs#bench_transmit_bandwidth`，调用摘要：(current_time - past_time).secs, AxNetTxToken::consume, InterfaceWrapper::current_time, info!, self.transmit, tx_buf[0..12].fill ...(+2)
- 参考项目：`arceos/modules/axnet/src/smoltcp_impl/tcp.rs#recv_timeout`，调用摘要：Err, Ok, SOCKET_SET.with_socket_mut::<tcp::Socket, _, _>, current_ticks, self.block_on, self.handle.get ...(+9)

#### 第三方库
仅参考项目中观察到该模块代表函数，目标项目没有同模块可对齐证据：
- 参考项目：`arceos/crates/page_table_multiarch/page_table_multiarch/src/bits64.rs#map_region`，调用摘要：Err, M::flush_tlb, Ok, PageSize::Size1G.is_aligned, PageSize::Size2M.is_aligned, PageSize::Size4K.is_aligned ...(+12)
- 参考项目：`arceos/crates/axdriver_crates/axdriver_virtio/src/net.rs#try_new`，调用摘要：Err, InnerDev::new, InnerDev::new(transport).map_err, NetBufPool::new, Ok, Some ...(+14)
- 参考项目：`arceos/crates/page_table_multiarch/page_table_multiarch/src/bits64.rs#protect_region`，调用摘要：Err, Ok, TlbFlushAll::new, assert!, error!, self.protect ...(+5)
- 参考项目：`arceos/crates/page_table_multiarch/page_table_multiarch/src/arch/loongarch64.rs#flush_tlb`，调用摘要：asm!

#### 通用模块/其他
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `patches/axio/src/buffered/linewriter/shim.rs#write_all` | `arceos/ulib/axlibc/src/strftime.rs#write_all` | 同板块同名 | buf.split_at, memchr::memrchr, self.buffer.flush_buf, self.buffer.write_all, self.buffered, self.buffered().is_empty ...(+3) | Err, Ok, buf.len, self.inner.write_all |
| `patches/axio/src/read/mod.rs#read_until` | `arceos/ulib/axstd/src/io/stdio.rs#read_until` | 同板块同名 | Ok, available.len, buf.extend_from_slice, memchr::memchr, self.consume, self.fill_buf | self.inner.read_until |
| `patches/axplat-loongarch64-qemu-virt/src/time.rs#set_oneshot_timer` | `arceos/modules/axhal/src/platform/x86_pc/time.rs#set_oneshot_timer` | 同板块同名 | Self::current_ticks, Self::nanos_to_ticks, tcfg::set_init_val, ticks_deadline.saturating_sub, ticks_deadline.saturating_sub(ticks_now).max | NANOS_TO_LAPIC_TICKS_RATIO.mul_trunc, apic_ticks.max, assert!, crate::time::monotonic_time_nanos, lapic.set_timer_initial, super::apic::local_apic |
| `patches/axio/src/utils/take.rs#fill_buf` | `arceos/ulib/axstd/src/io/stdio.rs#fill_buf` | 同板块同名 | Ok, buf.len, cmp::min, self.inner.fill_buf | self.inner.fill_buf |


好的，我将遵循审计指令，以深度析理模式对本案进行最终同源性定性仲裁。

### 1. 宏观判定
- **定性结论**: **独立开发，底层架构存在显著差异**
  基于客观 AST 相似度（4.5%）、极低的双向覆盖率（目标项目 8.99%，参考项目 8.25%）以及双方在核心子系统（如内存管理、进程模型、文件系统抽象）上的异构设计，本庭裁定目标项目 `[2026-T202610486999578-XX]` 与参考项目 `[2025-StarryX-杭州电子科技大学]` 不构成实质性相似或代码复用。观察到的少量相似性可归因于对 Linux/POSIX 公共语义的遵循、Rust 语言惯用法以及共同依赖的 ArceOS 上游生态。

### 2. 全模块覆盖矩阵解读
- **进程管理**: 双方均存在。共同 AST 指纹仅 5 个，覆盖率极低（4.07% vs 3.14%）。控方指控的 `set_clear_child_tid` 等函数是 Linux futex 标准语义与 Rust 原子操作惯用法的结合，不构成独创表达。证据强度：**弱**。
- **任务调度/异步运行时**: 双方均存在。覆盖率中等（7.02% vs 7.12%）。相似性集中在 `sys_futex`、`sys_kill` 等系统调用入口，其调用链相似源于 POSIX 规范，但内部实现（如 `FutexKey::new_current` vs `FutexKey::new`）已体现差异。证据强度：**弱**。
- **内存管理**: 双方均存在。覆盖率中等（5.93% vs 5.91%）。`sys_mmap`、`do_clone` 等函数虽同名，但调用图谱显示目标项目使用了更复杂的随机化与对齐逻辑，与参考项目有显著架构差异。证据强度：**弱**。
- **中断/异常处理**: 双方均存在。覆盖率较高（21.43% vs 19.35%），但这是因为该板块代码量小，且相似性集中在 `x86_64` 和 `loongarch64` 的硬件操作指令（如 `disable_irqs`），属于平台相关底层代码的必然趋同。证据强度：**弱**。
- **系统调用**: 双方均存在，但共同 AST 指纹为 0。这强有力地证明两个项目在系统调用分发与实现架构上完全不同。证据强度：**未形成**。
- **文件系统**: 双方均存在。覆盖率中等（10.97% vs 14.98%）。相似性集中在 `ioctl` 等通用接口，但内部对象模型（如 `UserConstPtr` vs `argp.cast`）完全不同。证据强度：**弱**。
- **设备驱动**: 双方均存在，但共同 AST 指纹为 0。目标项目的 `axnet-ng` 与参考项目的 `bwbench_client` 在 `send`/`recv` 上完全不同，一个是内核协议栈，一个是用户态基准测试工具。证据强度：**未形成**。
- **网络协议栈**: 双方均存在，但共同 AST 指纹为 0。目标项目基于 `axnet-ng` 自研，参考项目则大量使用 `smoltcp` 第三方库，架构迥异。证据强度：**未形成**。
- **第三方库/公共依赖**: 目标项目在 `文件系统/第三方库` 板块覆盖率高达 41.03%，这是因为双方均直接复用了 `axfs-ng` 等上游 crate，属于公共依赖，而非目标项目对参考项目的抄袭。参考项目独有的 `网络协议栈/第三方库` 和 `第三方库` 板块，目标项目未体现，进一步证明两者技术栈不同。

### 3. 底层 AST 关键证据指纹表
| 所属板块 | 目标项目路径 | 参考项目路径 | 证据类型 | 技术说明 |
| :--- | :--- | :--- | :--- | :--- |
| 进程管理 | `kernel/src/task/mod.rs#set_clear_child_tid` | `xcore/src/task/proc.rs#set_clear_child_tid` | 弱信号 | `clear_child_tid` 是 Linux 标准字段，`.store` 是 Rust `Atomic` 类型唯一写入方法。 |
| 进程管理 | `kernel/src/task/ops.rs#exit_robust_list` | `xapi/src/task/futex.rs#exit_robust_list` | 弱信号 | 均调用 `handle_futex_death`，但用户态内存访问方式不同（`vm_read` vs `uspace.read`）。 |
| 进程管理 | `kernel/src/syscall/ipc/shm.rs#attach_process` | `xcore/src/ipc/shm.rs#attach_process` | 弱信号 | `va_range.insert` 是领域自然表达，目标项目用 `assert!`，参考项目返回 `Result`，控制流不同。 |
| 任务调度 | `kernel/src/syscall/sync/futex.rs#sys_futex` | `xapi/src/task/futex.rs#sys_futex` | 弱信号 | 同为 POSIX `futex` 入口，但内部 Key 构造方式不同（`FutexKey::new_current` vs `FutexKey::new`）。 |
| 内存管理 | `kernel/src/syscall/mm/mmap.rs#sys_mmap` | `xapi/src/mm/mmap.rs#sys_mmap` | 弱信号 | 同为 POSIX `mmap` 入口，但目标项目包含复杂的随机化逻辑，参考项目则无。 |
| 中断/异常 | `patches/axcpu/src/x86_64/asm.rs#disable_irqs` | `arceos/modules/axhal/src/arch/x86_64/mod.rs#disable_irqs` | 弱信号 | 平台相关硬件指令序列，均为 `interrupts::disable` 的薄封装。 |
| 文件系统 | `kernel/src/pseudofs/dev/fb.rs#ioctl` | `xcore/src/fs/vfs/dev/loopx.rs#ioctl` | 弱信号 | 同为 `ioctl` 接口，但用户态数据拷贝方式不同（`vm_write` vs `argp.cast`）。 |
| 文件系统/第三方库 | `patches/axfs-ng/src/fs/ext4/inode.rs#metadata` | `arceos/modules/axfs-ng/src/fs/ext4/inode.rs#metadata` | 公共依赖 | 双方均直接复用上游 `axfs-ng` crate 的 ext4 实现，非互相抄袭。 |

### 4. 核心代码调用流对比剖析
* **【进程管理 / 进程退出与清理】**
* **底层技术析理**: 在进程退出清理共享内存的 `clear_proc_shm` 流程中，两者虽遵循相同的 SysV IPC 语义（查 pid -> 遍历 shmid -> detach），但管理器架构完全不同。目标项目通过 `self` 直接管理，调用 `self.remove_shmid` 立即移除；参考项目则通过全局 `IPC_MANAGER.with_shm` 访问，并引入 `segment.should_remove` 和 `cleanup_orphaned_segments` 延迟清理策略。这体现了“一体化管理器”与“全局管理器+策略模式”的架构差异。
* **代码溯源链接**:
  - 目标项目: `kernel/src/syscall/ipc/shm.rs#clear_proc_shm`
  - 参考项目: `xapi/src/ipc/shm.rs#clear_proc_shm`

* **【内存管理 / 内存映射】**
* **底层技术析理**: 在 `sys_mmap` 实现中，两者均处理地址对齐、权限检查等 POSIX 规范要求。然而，目标项目的调用图谱揭示了复杂的地址空间布局随机化（ASLR）逻辑，包含 `rnd_bits`、`max_slide` 等专用计算；参考项目的调用图谱则未体现此特征，其调用链更侧重于文件描述符的获取与标志处理。这表明两者在内存管理策略上存在根本性设计差异。
* **代码溯源链接**:
  - 目标项目: `kernel/src/syscall/mm/mmap.rs#sys_mmap`
  - 参考项目: `xapi/src/mm/mmap.rs#sys_mmap`

* **【文件系统 / 伪文件系统实现】**
* **底层技术析理**: 在 procfs 的 `read_at` 实现中，目标项目采用 `self.with_cache` 模式，暗示其内部维护了缓存机制；参考项目则通过 `self.0.upgrade().unwrap().read` 访问，表明其内部是 `Weak` 指针指向的实际数据节点。这两种截然不同的内部数据组织与访问模式，是独立设计的强有力证明。
* **代码溯源链接**:
  - 目标项目: `kernel/src/pseudofs/proc.rs#read_at`
  - 参考项目: `xcore/src/fs/vfs/proc/pid.rs#read_at`

### 5. 单方模块与降权项
- **单方模块**: 参考项目独有的 `网络协议栈/第三方库` 和 `第三方库` 板块（如 `smoltcp`、`page_table_multiarch`），目标项目均未体现，证明两者技术栈与依赖选择不同。
- **降权项**:
  - **跨板块同名函数**: 如目标项目的 `nl_pid`（网络）与参考项目的 `get_policy`（进程），仅共享 `.load` 操作，不构成同源证据。
  - **POSIX 标准接口**: `sys_getpid`、`sys_fork`、`sys_exit_group` 等函数同名同调用链是 ABI 规范强制要求，不能作为抄袭证据。
  - **Rust 惯用法与公共宏**: `Ok`、`Err`、`Some`、`Arc::new`、`format!` 等属于语言级标准词汇，无项目独创性。
  - **公共上游依赖**: `文件系统/第三方库` 板块的高相似度是双方共用 `axfs-ng` 等 ArceOS 生态 crate 的直接结果，应归因于上游复用。

### 6. 保守结论
本庭综合评估认为，目标项目与参考项目的客观 AST 相似度极低（4.5%），远未达到代码复用或抄袭的阈值。控方提出的“铁证”多为 Linux/POSIX 公共语义、Rust 语言惯用法或对共同上游依赖（ArceOS）的复用，且在核心指控中存在板块归类错误。辩方成功论证了双方在用户态内存访问抽象、进程管理器架构、内存随机化策略、伪文件系统内部模型等多个核心子系统上存在实质性架构差异。因此，现有证据不足以支持抄袭指控，目标项目应被认定为独立开发作品。建议将审查重点从代码同源性转向对开源协议合规性的常规核查。

------------------------------

