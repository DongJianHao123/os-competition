# 🛡️ OS 内核赛自动化代码级架构审计报告

**审计时间**: `2026-07-02 11:20:21`
**参赛作品名称**: `2026-T202610486999578-XX`
**年份**: `2026`
**学校**: `XX`
**队伍名称**: `T202610486999578`
**比赛名字**: `OS-Competition`
**比赛子赛道**: `Kernel-Implementation`
**仓库地址**: `https://gitlab.eduxiji.net/educg-group-43501-3132633/T202610486999578-700`
**推测开发语言**: `Rust / C (由AST探针自动识别)`
**特征函数量**: `3235`

## ⚔️ 底层代码同源性对比 (AST Homology)
> 本部分由专门的代码审查智能体，对该项目与符合年份条件的历届最高相似度项目进行控制流与底层调用对比。新版报告会先给出确定性的全模块覆盖矩阵，再给出 Agent 的分模块解释，避免只围绕少量抽样函数展开。

### 🏆 Top 3 底层对标: vs `2025-狗剩-海南大学`（海南大学 / 狗剩）

#### 对方作品信息

**对方年份**: `2025`
**对方学校**: `海南大学`
**对方队伍名称**: `狗剩`
**对方比赛名字**: `OS-Competition`
**对方比赛子赛道**: `Kernel-Implementation`
**对方仓库地址**: `https://gitlab.eduxiji.net/educg-group-36002-2710490/T202510589995148-2209`

> 双向覆盖率：共同 AST 指纹: 228；唯一 AST 指纹: 目标项目 2659，参考项目 3020；覆盖率: 目标项目 8.57%，参考项目 7.55%

## 全模块对比总览（确定性证据）

> 总体覆盖率：共同 AST 指纹: 228；唯一 AST 指纹: 目标项目 2659，参考项目 3020；覆盖率: 目标项目 8.57%，参考项目 7.55%

### 1. 模块覆盖矩阵
| 模块 | 目标函数数 | 参考函数数 | 共同唯一 AST 指纹 | Target 覆盖 | Base 覆盖 | 状态 |
| :--- | ---: | ---: | ---: | ---: | ---: | :--- |
| 进程管理 | 139 | 93 | 4 | 3.25% | 4.76% | 双方存在且有结构重合 |
| 任务调度/异步运行时 | 471 | 399 | 23 | 6.46% | 7.99% | 双方存在且有结构重合 |
| 内存管理 | 410 | 326 | 19 | 4.9% | 6.53% | 双方存在且有结构重合 |
| 中断/异常处理 | 59 | 62 | 12 | 21.43% | 24.49% | 双方存在且有结构重合 |
| 系统调用 | 224 | 153 | 1 | 0.49% | 0.78% | 双方存在且有结构重合 |
| 文件系统 | 1034 | 747 | 97 | 11.44% | 17.14% | 双方存在且有结构重合 |
| 设备驱动 | 100 | 142 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |
| 网络协议栈 | 290 | 77 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |
| 文件系统/第三方库 | 39 | 689 | 16 | 41.03% | 2.73% | 双方存在且有结构重合 |
| 网络协议栈/第三方库 | 0 | 107 | 0 | 0.0% | 0.0% | 仅参考项目体现 |
| 第三方库 | 0 | 386 | 0 | 0.0% | 0.0% | 仅参考项目体现 |
| 通用模块/其他 | 469 | 665 | 12 | 3.26% | 2.44% | 双方存在且有结构重合 |

### 2. 分模块证据清单

#### 进程管理
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `kernel/src/syscall/signal.rs#sys_rt_sigprocmask` | `api/src/imp/signal.rs#sys_rt_sigprocmask` | 同板块同名 | Err, Ok, check_sigset_size, curr.as_thread, current, debug! ...(+7) | Err, Ok, check_sigset, current, current()
        .task_ext, current()
        .task_ext()
        .thread
        .signal
        .with_blocked_mut::<LinuxResult<_>> ...(+2) |
| `kernel/src/syscall/ipc/shm.rs#clear_proc_shm` | `api/src/imp/ipc/shm.rs#clear_proc_shm` | 同板块同名 | self.get_inner_by_shmid, self.get_shmids_by_pid, self.remove_pid, self.remove_shmid, shm_inner.attach_count, shm_inner.detach_process ...(+1) | SHM_MANAGER.lock, shm_inner.detach_process, shm_inner.lock, shm_manager.get_inner_by_shmid, shm_manager.get_inner_by_shmid(shmid).unwrap, shm_manager.get_shmids_by_pid ...(+1) |
| `kernel/src/syscall/ipc/shm.rs#detach_process` | `api/src/imp/ipc/shm.rs#detach_process` | 同板块同名 | monotonic_time_nanos, self.va_range.remove, self.va_range.remove(&pid).is_none | assert!, self.va_range.remove |
| `kernel/src/syscall/ipc/shm.rs#attach_process` | `api/src/imp/ipc/shm.rs#attach_process` | 同板块同名 | assert!, monotonic_time_nanos, self.va_range.insert | assert!, self.va_range.insert |

#### 任务调度/异步运行时
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `kernel/src/syscall/sync/futex.rs#sys_futex` | `api/src/imp/futex.rs#sys_futex` | 同板块同名 | AxError::from, Err, FutexKey::new_current, Ok, Some, assert_unsigned ...(+25) | Err, Ok, TimeValue::new, axtask::yield_now, futex_table.get, futex_table.get_or_insert ...(+14) |
| `kernel/src/syscall/signal.rs#sys_kill` | `api/src/imp/signal.rs#sys_kill` | 同板块同名 | Err, Ok, Some, crate::net::lookup_pid_netns, crate::net::lookup_pid_netns(pid).is_some, current ...(+16) | Err, Ok, current, find_process, get_all_processes, make_siginfo ...(+3) |
| `patches/axcpu/src/aarch64/init.rs#switch_to_el1` | `arceos/modules/axhal/src/platform/aarch64_common/boot.rs#switch_to_el1` | 同板块同名 | CNTHCTL_EL2.modify, CNTVOFF_EL2.set, CurrentEL.read, ELR_EL2.set, ELR_EL3.set, HCR_EL2.write ...(+9) | CNTHCTL_EL2.modify, CNTVOFF_EL2.set, CurrentEL.read, ELR_EL2.set, ELR_EL3.set, HCR_EL2.write ...(+8) |
| `kernel/src/syscall/time.rs#sys_setitimer` | `api/src/imp/time.rs#sys_setitimer` | 同板块同名 | ITimerType::from_repr, ITimerType::from_repr(which).ok_or, Ok, borrow_thread_time, borrow_thread_time(curr.as_thread())?.renew_itimer, borrow_thread_time_mut ...(+14) | Err, Ok, axhal::time::monotonic_time_nanos, axtask::current, curr.task_ext, curr.task_ext().time.write ...(+8) |

#### 内存管理
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `kernel/src/syscall/mm/mmap.rs#sys_mmap` | `src/syscall_imp/mm/mmap.rs#sys_mmap` | 同板块同名 | (1usize << rnd_bits.min(20)).saturating_sub, (addr + length).align_up, (curr.as_thread().proc_data.proc.pid() as u64).wrapping_shl, (max_slide / align).max, (max_slide_pages * PAGE_SIZE_4K).min, (slot * align).min ...(+68) | (addr + length).align_up, Err, MmapFlags::MAP_PRIVATE.bits, MmapFlags::MAP_SHARED.bits, MmapFlags::MAP_SHARED_VALIDATE.bits, MmapFlags::from_bits_truncate ...(+32) |
| `kernel/src/syscall/task/execve.rs#sys_execve` | `src/syscall_imp/task/thread.rs#sys_execve` | 同板块同名 | "/bin/sh".to_string, "TST_TIMEOUT=-1".into, Arc::new, Default::default, Err, FD_TABLE.write ...(+66) | Default::default, Ok, UserConstPtr::<c_char>::from, UserConstPtr::<c_char>::from(*arg)
                .get_as_str, UserConstPtr::<c_char>::from(*arg)
                .get_as_str()
                .map, UserConstPtr::<c_char>::from(*env)
                .get_as_str ...(+42) |
| `kernel/src/syscall/ipc/shm.rs#sys_shmat` | `api/src/imp/ipc/shm.rs#sys_shmat` | 同板块同名 | Arc::new, Backend::new_shared, Err, Ok, SHM_MANAGER.lock, SharedPages::new ...(+30) | Err, MappingFlags::all, Ok, SHM_MANAGER.lock, ShmAtFlags::from_bits_truncate, VirtAddr::from ...(+26) |
| `kernel/src/mm/loader.rs#load_user_app` | `core/src/mm.rs#load_user_app` | 同板块同名 | "/bin/sh".to_owned, (ustack_top - user_sp_aligned).align_up_4k, Backend::new_alloc, ELF_LOADER.lock, ELF_LOADER.lock().load, Err ...(+44) | CStr::from_bytes_with_nul, CStr::from_bytes_with_nul(interp)
                    .ok, CStr::from_bytes_with_nul(interp)
                    .ok()
                    .and_then, CStr::from_bytes_with_nul(interp)
                    .ok()
                    .and_then(|it| it.to_str().ok())
                    .ok_or, ElfFile::new, ElfFile::new(&file_data).map_err ...(+53) |

#### 中断/异常处理
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `patches/axplat-loongarch64-qemu-virt/src/irq.rs#set_enable` | `arceos/modules/axhal/src/platform/loongarch64_2k1000la/irq.rs#set_enable` | 同板块同名 | IrqType::new, ecfg::read, ecfg::read().lie, ecfg::set_lie, eiointc::disable_irq, eiointc::enable_irq ...(+4) | ecfg::read, ecfg::read().lie, ecfg::set_lie |
| `patches/axcpu/src/x86_64/asm.rs#disable_irqs` | `arceos/modules/axhal/src/arch/x86_64/mod.rs#disable_irqs` | 同板块同名同结构 | interrupts::disable, warn! | interrupts::disable, warn! |
| `patches/axcpu/src/x86_64/asm.rs#enable_irqs` | `arceos/modules/axhal/src/arch/x86_64/mod.rs#enable_irqs` | 同板块同名同结构 | interrupts::enable, warn! | interrupts::enable, warn! |
| `patches/axcpu/src/x86_64/asm.rs#wait_for_irqs` | `arceos/modules/axhal/src/arch/x86_64/mod.rs#wait_for_irqs` | 同板块同名同结构 | asm!, cfg!, core::hint::spin_loop | asm!, cfg!, core::hint::spin_loop |

#### 系统调用
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `kernel/src/syscall/io_mpx/epoll.rs#sys_epoll_ctl` | `api/src/imp/io_mpx/epoll.rs#sys_epoll_ctl` | 同板块同名 | Epoll::from_fd, EpollFlags::from_bits, EpollFlags::from_bits(event.events & !events.bits()).ok_or, Err, IoEvents::from_bits_truncate, Ok ...(+7) | EpollInstance::from_fd, EpollInstance::from_fd(epfd)?.control, Err, Ok, debug!, event.get_as_mut |
| `kernel/src/syscall/io_mpx/select.rs#sys_pselect6` | `api/src/imp/io_mpx/poll.rs#sys_pselect6` | 同板块同名 | do_select, nullable!, nullable!(timeout.get_as_ref())?
            .map, nullable!(timeout.get_as_ref())?
            .map(|ts| ts.try_into_time_value())
            .transpose, ts.try_into_time_value | Err, do_select, timeout.is_none, timeout.nullable, timeout.unwrap |
| `kernel/src/syscall/time.rs#sys_times` | `api/src/imp/time.rs#sys_times` | 同板块同名 | Ok, borrow_thread_time, borrow_thread_time(current().as_thread())?.output, current, current().as_thread, monotonic_time_nanos ...(+4) | Ok, monotonic_time_nanos, nanos_to_ticks, time_stat_output, tms.get |
| `kernel/src/syscall/time.rs#sys_getitimer` | `api/src/imp/time.rs#sys_getitimer` | 同板块同名 | ITimerType::from_repr, ITimerType::from_repr(which).ok_or, Ok, borrow_thread_time, borrow_thread_time(current().as_thread())?.get_itimer, current ...(+3) | Err, Ok, debug!, get_timer_stat, value.nullable, warn! ...(+1) |

#### 文件系统
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `kernel/src/syscall/fs/io.rs#sys_splice` | `api/src/imp/io.rs#sys_splice` | 同板块同名 | DummyFd::from_fd, DummyFd::from_fd(fd_in).is_ok, DummyFd::from_fd(fd_out).is_ok, Err, File::from_fd, Pipe::from_fd ...(+20) | (len - total_copied).min, Err, Ok, SeekFrom::Current, SeekFrom::Start, buf.len ...(+43) |
| `kernel/src/syscall/fs/fd_ops.rs#sys_close` | `arceos/api/arceos_posix_api/src/imp/fd_ops.rs#sys_close` | 同板块同名 | Ok, cleanup_flock_locks_on_close, close_file_like, crate::file::fanotify::is_nonotify_fd, debug!, f.downcast_arc::<File> ...(+14) | (0..=2).contains, debug!, syscall_body! |
| `kernel/src/syscall/fs/io.rs#sys_copy_file_range` | `api/src/imp/io.rs#sys_copy_file_range` | 同板块同名 | Default::default, File::from_fd, Ok, SendFile::Direct, SendFile::Offset, Some ...(+12) | (len - total_copied).min, Err, File::from_fd, FileFlags::empty, Ok, SeekFrom::Current ...(+19) |
| `kernel/src/file/fs.rs#resolve_at` | `api/src/fd/fs.rs#resolve_at` | 同板块同名 | Err, Ok, ResolveAtResult::File, ResolveAtResult::Other, dir.inner, dir.inner().clone ...(+13) | Err, Ok, ResolveAtResult::File, ResolveAtResult::Other, dir.inner, dir.inner().clone ...(+12) |

#### 设备驱动
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `patches/axnet-ng/src/udp.rs#send` | `arceos/tools/bwbench_client/src/device.rs#send` | 同板块同名 | Err, IpEndpoint::from, Ok, PacketMeta::default, Some, addr.into_ip ...(+21) | Err, Ok, buffer.as_ptr, buffer.len, err.kind, libc::send ...(+2) |
| `patches/axnet-ng/src/lib.rs#init_network` | `arceos/modules/axnet/src/lib.rs#init_network` | 同板块同名 | "eth0".to_owned, Box::new, EthernetAddress, EthernetDevice::new, GATEWAY.parse, GATEWAY.parse().expect ...(+28) | info!, net_devs.take_one, net_devs.take_one().expect, net_impl::init |
| `patches/axnet-ng/src/device/ethernet.rs#recv` | `arceos/tools/bwbench_client/src/device.rs#recv` | 同板块同名 | matches!, rx_buf.packet, self.handle_frame, self.inner.receive, self.inner.recycle_rx_buffer, self.inner.recycle_rx_buffer(rx_buf).unwrap ...(+2) | Err, Ok, buffer.as_mut_ptr, buffer.len, err.kind, libc::recv ...(+2) |
| `patches/axplat-loongarch64-qemu-virt/src/console.rs#init_early` | `arceos/modules/axhal/src/platform/loongarch64_2k1000la/console.rs#init_early` | 同板块同名 | UART_BASE.init_once, p.add, p.add(1).write_volatile, p.add(2).write_volatile, p.add(3).write_volatile, p.add(4).write_volatile ...(+4) | SpinNoIrq::new, UART.init_once, Uart::new, VirtAddr::from, vaddr.as_usize |

#### 网络协议栈
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `patches/axnet-ng/src/udp.rs#recv` | `api/src/fd/net.rs#recv` | 同板块同名 | (Ipv4Addr::UNSPECIFIED, 0).into, Err, ExpectedRemote::Any, ExpectedRemote::Expecting, Ok, SocketAddrEx::Ip ...(+22) | Ok, tcpsocket.lock, tcpsocket.lock().recv, udpsocket.lock, udpsocket.lock().recv_from, udpsocket.lock().recv_from(buf).map |
| `patches/axnet-ng/src/vsock/stream.rs#recv` | `arceos/api/arceos_posix_api/src/imp/net.rs#recv` | 同板块同名 | Err, Ok, conn.lock, conn_guard.advance_rx_read, conn_guard.rx_buffer_used, conn_guard.rx_closed ...(+9) | Ok, tcpsocket.lock, tcpsocket.lock().recv, udpsocket.lock, udpsocket.lock().recv_from, udpsocket.lock().recv_from(buf).map |
| `patches/axnet-ng/src/tcp.rs#recv` | `arceos/ulib/axstd/src/net/udp.rs#recv` | 同板块同名 | Err, Ok, ax_err_type!, dst.remaining_mut, dst.write, options.flags.contains ...(+12) | api::ax_udp_recv |
| `patches/axnet-ng/src/listen_table.rs#accept` | `api/src/fd/net.rs#accept` | 同板块同名 | Err, Ok, entry.lock, is_closed, is_connected, is_connected(handle).then_some ...(+9) | Err, Ok, tcpsocket.lock, tcpsocket.lock().accept |

#### 文件系统/第三方库
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `patches/axfs-ng/src/fs/ext4/inode.rs#create` | `arceos/modules/axfs-ng/src/fs/ext4/inode.rs#create` | 同板块同名 | DirEntry::new_dir, DirEntry::new_file, DirNode::new, Err, FileNode::new, Inode::new ...(+14) | DirEntry::new_dir, DirEntry::new_file, DirNode::new, Err, FileNode::new, Inode::new ...(+11) |
| `patches/axfs-ng/src/fs/ext4/inode.rs#metadata` | `arceos/modules/axfs-ng/src/fs/ext4/inode.rs#metadata` | 同板块同名同结构 | DeviceId::default, FileAttr::default, NodePermission::from_bits_truncate, Ok, into_vfs_type, self.fs
            .lock ...(+2) | DeviceId::default, FileAttr::default, NodePermission::from_bits_truncate, Ok, into_vfs_type, self.fs
            .lock ...(+2) |
| `patches/axfs-ng/src/fs/ext4/inode.rs#read_dir` | `arceos/modules/axfs-ng/src/fs/ext4/inode.rs#read_dir` | 同板块同名同结构 | Ok, core::str::from_utf8, core::str::from_utf8(entry.name())
                .map_err, core::str::from_utf8(entry.name())
                .map_err(|_| VfsError::InvalidData)?
                .to_owned, entry.ino, entry.inode_type ...(+10) | Ok, core::str::from_utf8, core::str::from_utf8(entry.name())
                .map_err, core::str::from_utf8(entry.name())
                .map_err(|_| VfsError::EINVAL)?
                .to_owned, entry.ino, entry.inode_type ...(+10) |
| `patches/axfs-ng/src/fs/ext4/inode.rs#update_metadata` | `arceos/modules/axfs-ng/src/fs/ext4/inode.rs#update_metadata` | 同板块同名 | Ok, fs.with_inode_ref, fs.with_inode_ref(self.ino, |inode| {
            if let Some(mode) = update.mode {
                inode.set_mode((inode.mode() & !0xfff) | (mode.bits() as u32));
            }
            if let Some((uid, gid)) = update.owner {
                inode.set_owner(uid as _, gid as _);
            }
            if let Some(atime) = update.atime {
                inode.set_atime(&atime);
            }
            if let Some(mtime) = update.mtime {
                inode.set_mtime(&mtime);
            }
            inode.update_ctime();
            Ok(())
        })
        .map_err, inode.mode, inode.set_atime, inode.set_mode ...(+5) | Ok, fs.with_inode_ref, fs.with_inode_ref(self.ino, |inode| {
            if let Some(mode) = update.mode {
                inode.set_mode((inode.mode() & !0xfff) | (mode.bits() as u32));
            }
            if let Some((uid, gid)) = update.owner {
                inode.set_owner(uid as _, gid as _);
            }
            if let Some(atime) = update.atime {
                inode.set_atime(&atime);
            }
            if let Some(mtime) = update.mtime {
                inode.set_mtime(&mtime);
            }
            Ok(())
        })
        .map_err, inode.mode, inode.set_atime, inode.set_mode ...(+4) |

#### 网络协议栈/第三方库
仅参考项目中观察到该模块代表函数，目标项目没有同模块可对齐证据：
- 参考项目：`arceos/modules/axnet/src/smoltcp_impl/tcp.rs#connect`，调用摘要：Err, Ok, SOCKET_SET
                .with_socket_mut::<tcp::Socket, _, _>, SOCKET_SET.add, SocketSetWrapper::new_tcp_socket, Some ...(+29)
- 参考项目：`arceos/modules/axnet/src/smoltcp_impl/dns.rs#query`，调用摘要：Err, Ok, SOCKET_SET
            .with_socket_mut::<dns::Socket, _, _>, SOCKET_SET
            .with_socket_mut::<dns::Socket, _, _>(handle, |socket| {
                socket.start_query(iface.lock().context(), name, query_type)
            })
            .map_err, SOCKET_SET.poll_interfaces, SOCKET_SET.with_socket_mut::<dns::Socket, _, _> ...(+12)
- 参考项目：`arceos/modules/axnet/src/smoltcp_impl/bench.rs#bench_transmit_bandwidth`，调用摘要：(current_time - past_time).secs, AxNetTxToken::consume, InterfaceWrapper::current_time, info!, self.transmit, tx_buf[0..12].fill ...(+2)
- 参考项目：`arceos/modules/axnet/src/smoltcp_impl/tcp.rs#shutdown`，调用摘要：LISTEN_TABLE.unlisten, Ok, SOCKET_SET.poll_interfaces, SOCKET_SET.with_socket_mut::<tcp::Socket, _, _>, debug!, self.handle.get ...(+9)

#### 第三方库
仅参考项目中观察到该模块代表函数，目标项目没有同模块可对齐证据：
- 参考项目：`crates/visionfive2-sd/src/lib.rs#send_cmd`，调用摘要：RawInterrupt::from, RawInterruptStatusReg::from, S::sleep_ms_until, Some, arg.into, assert! ...(+24)
- 参考项目：`crates/visionfive2-sd/src/lib.rs#init_sdcard`，调用摘要：CardTypeReg::from, CardTypeReg::from(0).with_card_width4_1, CmdArg::new, CmdReg::from, ControlReg::from, RawInterruptStatusReg::from ...(+20)
- 参考项目：`crates/page_table_multiarch/page_table_multiarch/src/bits64.rs#map_region`，调用摘要：Err, M::flush_tlb, Ok, PageSize::Size1G.is_aligned, PageSize::Size2M.is_aligned, PageSize::Size4K.is_aligned ...(+12)
- 参考项目：`crates/axdriver_crates/axdriver_virtio/src/net.rs#try_new`，调用摘要：Err, InnerDev::new, InnerDev::new(transport).map_err, NetBufPool::new, Ok, Some ...(+14)

#### 通用模块/其他
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `patches/axio/src/buffered/linewriter/shim.rs#write_all` | `arceos/ulib/axlibc/src/strftime.rs#write_all` | 同板块同名 | buf.split_at, memchr::memrchr, self.buffer.flush_buf, self.buffer.write_all, self.buffered, self.buffered().is_empty ...(+3) | Err, Ok, buf.len, self.inner.write_all |
| `patches/axio/src/read/mod.rs#read_until` | `arceos/ulib/axstd/src/io/stdio.rs#read_until` | 同板块同名 | Ok, available.len, buf.extend_from_slice, memchr::memchr, self.consume, self.fill_buf | self.inner.read_until |
| `patches/axplat-loongarch64-qemu-virt/src/time.rs#set_oneshot_timer` | `arceos/modules/axhal/src/platform/x86_pc/time.rs#set_oneshot_timer` | 同板块同名 | Self::current_ticks, Self::nanos_to_ticks, tcfg::set_init_val, ticks_deadline.saturating_sub, ticks_deadline.saturating_sub(ticks_now).max | NANOS_TO_LAPIC_TICKS_RATIO.mul_trunc, apic_ticks.max, assert!, crate::time::monotonic_time_nanos, lapic.set_timer_initial, super::apic::local_apic |
| `patches/axio/src/utils/take.rs#fill_buf` | `arceos/ulib/axstd/src/io/stdio.rs#fill_buf` | 同板块同名 | Ok, buf.len, cmp::min, self.inner.fill_buf | self.inner.fill_buf |


好的，我将遵循审计指令，基于提供的所有材料，对本次代码同源性争议进行最终定性仲裁。

### 1. 宏观判定
- **定性结论**: **独立开发 / 底层架构显著差异**
  - **客观相似度**: 4.18%
  - **共同 AST 指纹**: 228
  - **唯一 AST 指纹**: 目标项目 2659，参考项目 3020
  - **覆盖率**: 目标项目 8.57%，参考项目 7.55%

该量化指标远低于可认定抄袭或深度复用的阈值。双方项目在宏观层面呈现出显著的架构异构性，共同指纹主要由操作系统内核领域的标准接口、通用编程范式及可能的公共第三方库依赖构成。

### 2. 全模块覆盖矩阵解读
| 模块 | 双方存在 | 共同指纹 | 覆盖率 (目标/参考) | 证据强度 | 解读 |
| :--- | :---: | :---: | :---: | :---: | :--- |
| **进程管理** | 是 | 4 | 3.25% / 4.76% | **弱** | 覆盖率极低。共同指纹集中在POSIX标准系统调用（如`sys_getppid`）和原子操作（`set_clear_child_tid`），属于领域内必然实现，不构成抄袭证据。 |
| **任务调度/异步运行时** | 是 | 23 | 6.46% / 7.99% | **弱** | 覆盖率低。共同指纹多为调度器通用操作（如`yield_now`）或同名硬件初始化函数（`switch_to_el1`），后者是架构强相关的标准流程。 |
| **内存管理** | 是 | 19 | 4.9% / 6.53% | **弱** | 覆盖率低。共同指纹出现在`sys_mmap`、`sys_execve`等复杂系统调用中，但调用链摘要显示双方在内存分配、标志位处理、错误传递上存在显著差异。 |
| **中断/异常处理** | 是 | 12 | 21.43% / 24.49% | **中** | 覆盖率相对较高，但证据集中在`x86_64`和`loongarch64`架构的特定硬件操作（如`disable_irqs`、`set_enable`），这是由硬件手册规定的标准序列，属于架构强相关代码。 |
| **系统调用** | 是 | 1 | 0.49% / 0.78% | **未形成** | 覆盖率极低，几乎无共同指纹。说明双方的系统调用分发和实现框架完全不同。 |
| **文件系统** | 是 | 97 | 11.44% / 17.14% | **中** | 覆盖率中等。共同指纹大量出现在`axfs-ng`等第三方库路径中，属于公共依赖。核心系统调用（如`sys_splice`）的调用链摘要显示出不同的实现策略。 |
| **设备驱动** | 是 | 0 | 0.0% / 0.0% | **未形成** | 无共同指纹。双方驱动模型和实现完全不同。 |
| **网络协议栈** | 是 | 0 | 0.0% / 0.0% | **未形成** | 无共同指纹。双方网络栈实现（如`axnet-ng` vs `smoltcp_impl`）完全不同。 |
| **文件系统/第三方库** | 是 | 16 | 41.03% / 2.73% | **强（第三方库）** | 目标项目覆盖率极高，但证据集中在`patches/axfs-ng`路径，与参考项目的`arceos/modules/axfs-ng`高度同源。这强烈表明双方共同依赖了`axfs-ng`这一第三方/上游crate，**不能作为被告抄袭原告的证据**。 |
| **网络协议栈/第三方库** | 仅参考 | 0 | 0.0% / 0.0% | **未形成** | 目标项目无此模块，属于原告独有的第三方库依赖。 |
| **第三方库** | 仅参考 | 0 | 0.0% / 0.0% | **未形成** | 目标项目无此模块，属于原告独有的第三方库依赖。 |
| **通用模块/其他** | 是 | 12 | 3.26% / 2.44% | **弱** | 覆盖率极低。共同指纹为`write_all`、`fill_buf`等通用I/O操作，属于Rust标准库或常见设计模式。 |

### 3. 底层 AST 关键证据指纹表
| 所属板块 | 目标项目路径 | 参考项目路径 | 证据类型 | 技术说明 |
| :--- | :--- | :--- | :--- | :--- |
| 进程管理 | `kernel/src/task/mod.rs#set_clear_child_tid` | `core/src/task/thread.rs#set_clear_child_tid` | 同板块底层逻辑一致 | 对Linux标准字段`clear_child_tid`的原子`store`操作，是ABI合规的必然实现。 |
| 进程管理 | `kernel/src/pseudofs/proc.rs#write_at` | `core/src/vfs/proc.rs#write_at` | 同板块底层逻辑一致 | 对只读procfs节点的`write_at`返回`Err`，是极简标准存根，信息量极低。 |
| 进程管理 | `kernel/src/syscall/ipc/shm.rs#attach_process` | `api/src/imp/ipc/shm.rs#attach_process` | 同板块同名 | 共享内存attach操作，均调用`self.va_range.insert`，是`BTreeMap`等数据结构的自然操作。 |
| 任务调度 | `patches/axcpu/src/x86_64/asm.rs#disable_irqs` | `arceos/modules/axhal/src/arch/x86_64/mod.rs#disable_irqs` | 同板块同名同结构 | x86_64架构下关中断的标准指令序列，由硬件决定，无独创性。 |
| 内存管理 | `kernel/src/syscall/mm/mmap.rs#sys_mmap` | `src/syscall_imp/mm/mmap.rs#sys_mmap` | 同板块同名 | 复杂的POSIX系统调用，调用链摘要显示参数处理和内部API完全不同。 |
| 中断/异常处理 | `patches/axplat-loongarch64-qemu-virt/src/irq.rs#set_enable` | `arceos/modules/axhal/src/platform/loongarch64_2k1000la/irq.rs#set_enable` | 同板块同名 | LoongArch64架构中断控制器的标准操作，操作`ecfg`寄存器是架构强相关代码。 |
| 文件系统 | `kernel/src/syscall/fs/fd_ops.rs#sys_close` | `arceos/api/arceos_posix_api/src/imp/fd_ops.rs#sys_close` | 同板块同名 | POSIX标准调用，但被告实现包含`fanotify`等独有逻辑，原告仅为简单封装。 |
| 文件系统/第三方库 | `patches/axfs-ng/src/fs/ext4/inode.rs#metadata` | `arceos/modules/axfs-ng/src/fs/ext4/inode.rs#metadata` | 同板块同名同结构 | **第三方库/公共依赖**。双方代码均源自`axfs-ng` crate，是公共上游依赖的铁证。 |
| 通用模块/其他 | `patches/axio/src/buffered/linewriter/shim.rs#write_all` | `arceos/ulib/axlibc/src/strftime.rs#write_all` | 同板块同名 | 函数同名但功能完全不同（行缓冲写 vs 时间格式化），属于巧合。 |

### 4. 核心代码调用流对比剖析
* **【进程管理 / 进程退出与状态判定】**
* **底层技术析理**: 控方指控`pending_exit`与`is_zombie`存在“换汤不换药”的洗稿。经比对，被告`pending_exit`调用`self.exit.load`，原告`is_zombie`调用`self.is_zombie.load`。两者字段名（`exit` vs `is_zombie`）和语义（待退出 vs 僵尸状态）均不同。对布尔原子标志执行`.load`判定是内核中检查进程状态的最基础、最通用模式，不构成独创性逻辑。被告的`exit_robust_list`处理的是Linux robust futex特定退出逻辑，而原告的`do_exit`是通用退出路径，两者操作的资源对象和子系统完全不同。
* **代码溯源链接**:
  - 目标项目: `kernel/src/task/mod.rs#pending_exit`
  - 参考项目: `core/src/task/process.rs#is_zombie`

* **【任务调度/异步运行时 / Futex等待机制】**
* **底层技术析理**: 控方指控被告的`wait_if`复用了原告的`wait_until`调度框架。经比对，被告`wait_if`基于`Poll::Ready`、`Waker`、`block_on`，是典型的**Rust异步Future条件等待**。原告`wait_until`基于`current_run_queue`、`blocked_resched`，是典型的**任务调度器挂起/唤醒**。两者调用的外部对象和底层机制完全不同：一个是异步运行时原语，一个是调度队列原语。唯一共现词`condition`仅为闭包参数名，非API调用。此指控不成立。
* **代码溯源链接**:
  - 目标项目: `kernel/src/task/futex.rs#wait_if`
  - 参考项目: `arceos/modules/axtask/src/wait_queue.rs#wait_until`

* **【文件系统 / ProcFS构建】**
* **底层技术析理**: 控方指控`lookup_child`与`builder`存在同构。经比对，被告`lookup_child`是**目录查找回调**，针对具体节点使用`SimpleDir::new_maker`和`FD_TABLE`。原告`builder`是**根目录构造器**，使用`DynamicDir::builder`和`root.add`。两者API名称不同（`SimpleDir` vs `DynamicDir`），功能不同（查找子节点 vs 构建根节点），文件路径不同（`pseudofs/proc.rs` vs `core/vfs/proc.rs`）。仅因都出现`Arc::new`和字符串转换就断言同构，属于硬凑证据。
* **代码溯源链接**:
  - 目标项目: `kernel/src/pseudofs/proc.rs#lookup_child`
  - 参考项目: `core/src/vfs/proc.rs#builder`

### 5. 单方模块与降权项
- **单方模块**: “网络协议栈/第三方库”和“第三方库”模块仅存在于参考项目中，如`smoltcp_impl`、`visionfive2-sd`等。这体现了原告项目独有的硬件支持和网络栈选型，与被告项目无关，不能作为同源证据。
- **第三方库/公共依赖**: “文件系统/第三方库”板块中，双方在`axfs-ng/src/fs/ext4/inode.rs`路径下存在高度一致的代码（如`metadata`、`read_dir`函数）。这是双方共同依赖了`axfs-ng`这一上游crate的明确证据，属于**公共依赖**，必须从抄袭指控中剔除。
- **降权项**: 所有POSIX标准系统调用（如`sys_getpid`、`sys_exit`、`sys_rt_sigprocmask`）、架构强相关硬件操作（如`switch_to_el1`、`disable_irqs`）、通用数据结构操作（如`va_range.insert`）以及极简存根（如`write_at`返回`Err`），均因其标准性、必然性或低信息量，被降权为弱信号，不构成抄袭主证据。

### 6. 保守结论
本次审计的客观AST相似度仅为4.18%，这是一个极低的水平，明确指向独立开发。控方提出的“铁证”均能被合理解释：`set_clear_child_tid`和`write_at`是Linux ABI合规或标准存根；`wait_if`与`wait_until`的底层机制完全不同；`lookup_child`与`builder`的功能和API不对等。双方在任务抽象、procfs框架、网络栈、驱动模型等核心架构上存在显著差异。唯一的高相似度区域（`axfs-ng`）被确认为公共第三方库依赖。综上所述，控方关于“精确到AST节点级别的代码剽窃”的指控**不能成立**，被告项目在进程管理及其他板块的实现具有充分的原创合理性。

------------------------------

