# 🛡️ OS 内核赛自动化代码级架构审计报告

**审计时间**: `2026-07-02 11:20:21`
**参赛作品名称**: `2026-T202610486999578-XX`
**年份**: `2026`
**学校**: `XX`
**队伍名称**: `T202610486999578`
**比赛名字**: `OS-Competition`
**比赛子赛道**: `Kernel-Implementation`
**仓库地址**: `https://gitlab.eduxiji.net/educg-group-43501-3132633/T202610486999578-700`
**推测开发语言**: `Rust / C (由AST探针自动识别)`
**特征函数量**: `3235`

## ⚔️ 底层代码同源性对比 (AST Homology)
> 本部分由专门的代码审查智能体，对该项目与符合年份条件的历届最高相似度项目进行控制流与底层调用对比。新版报告会先给出确定性的全模块覆盖矩阵，再给出 Agent 的分模块解释，避免只围绕少量抽样函数展开。

### 🏆 Top 1 底层对标: vs `2025-StarryMix-清华大学`（清华大学 / Starry Mix）

#### 对方作品信息

**对方年份**: `2025`
**对方学校**: `清华大学`
**对方队伍名称**: `Starry Mix`
**对方比赛名字**: `OS-Competition`
**对方比赛子赛道**: `Kernel-Implementation`
**对方仓库地址**: `local-upload://starry-mix`

> 双向覆盖率：共同 AST 指纹: 550；唯一 AST 指纹: 目标项目 2659，参考项目 1503；覆盖率: 目标项目 20.68%，参考项目 36.59%

## 全模块对比总览（确定性证据）

> 总体覆盖率：共同 AST 指纹: 550；唯一 AST 指纹: 目标项目 2659，参考项目 1503；覆盖率: 目标项目 20.68%，参考项目 36.59%

### 1. 模块覆盖矩阵
| 模块 | 目标函数数 | 参考函数数 | 共同唯一 AST 指纹 | Target 覆盖 | Base 覆盖 | 状态 |
| :--- | ---: | ---: | ---: | ---: | ---: | :--- |
| 进程管理 | 139 | 56 | 16 | 13.01% | 30.77% | 双方存在且有结构重合 |
| 任务调度/异步运行时 | 471 | 282 | 46 | 12.92% | 17.36% | 双方存在且有结构重合 |
| 内存管理 | 410 | 273 | 66 | 17.01% | 26.4% | 双方存在且有结构重合 |
| 中断/异常处理 | 59 | 8 | 2 | 3.57% | 25.0% | 双方存在且有结构重合 |
| 系统调用 | 224 | 78 | 9 | 4.37% | 12.0% | 双方存在且有结构重合 |
| 文件系统 | 1034 | 562 | 254 | 29.95% | 54.27% | 双方存在且有结构重合 |
| 设备驱动 | 100 | 120 | 20 | 22.47% | 17.24% | 双方存在且有结构重合 |
| 网络协议栈 | 290 | 138 | 59 | 23.05% | 46.09% | 双方存在且有结构重合 |
| 文件系统/第三方库 | 39 | 37 | 33 | 84.62% | 89.19% | 双方存在且有结构重合 |
| 通用模块/其他 | 469 | 158 | 6 | 1.63% | 4.48% | 双方存在且有结构重合 |

### 2. 分模块证据清单

#### 进程管理
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `patches/axnet-ng/src/device/ethernet.rs#process_arp` | `arceos/modules/axnet/src/device/ethernet.rs#process_arp` | 同板块同名同结构 | ArpPacket::new_checked, ArpPacket::new_checked(payload).and_then, ArpPacket::new_unchecked, ArpRepr::parse, IpAddress::Ipv4, Self::send_to ...(+21) | ArpPacket::new_checked, ArpPacket::new_checked(payload).and_then, ArpPacket::new_unchecked, ArpRepr::parse, IpAddress::Ipv4, Self::send_to ...(+21) |
| `kernel/src/pseudofs/proc.rs#lookup_child` | `api/src/vfs/proc.rs#lookup_child` | 同板块同名 | Arc::downgrade, Arc::new, Err, NodeOpsMux::Dir, Ok, SimpleDir::new_maker ...(+18) | Arc::downgrade, Arc::new, CStr::from_bytes_until_nul, CStr::from_bytes_until_nul(&input)
                                    .map_err, CStr::from_bytes_until_nul(&input)
                                    .map_err(|_| VfsError::EINVAL)?
                                    .to_str, CStr::from_bytes_until_nul(&input)
                                    .map_err(|_| VfsError::EINVAL)?
                                    .to_str()
                                    .map_err ...(+55) |
| `kernel/src/pseudofs/proc.rs#lookup_child` | `api/src/vfs/proc.rs#lookup_child` | 同板块同名 | Arc::new, Ipv6IfaceConfDir {
                    fs: self.fs.clone(),
                    ifname: ifname.to_string(),
                }
                .lookup_child, Ok, SimpleDir::new_maker, SimpleDir::new_maker(
            fs,
            Arc::new(Ipv6IfaceConfDir {
                fs: self.fs.clone(),
                ifname,
            }),
        )
        .into, ifname.to_string ...(+5) | Arc::downgrade, Arc::new, Err, NodeOpsMux::Dir, Ok, SimpleDir::new_maker ...(+10) |
| `kernel/src/task/ops.rs#exit_robust_list` | `api/src/task.rs#exit_robust_list` | 同板块同名 | Err, Ok, axtask::yield_now, core::ptr::eq, entry.vm_read, handle_futex_death ...(+2) | Err, Ok, axtask::yield_now, core::ptr::eq, entry.vm_read, handle_futex_death ...(+1) |

#### 任务调度/异步运行时
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `kernel/src/syscall/fs/mount.rs#sys_mount` | `api/src/syscall/fs/mount.rs#sys_mount` | 同板块同名 | Arc::new, BindMountFs::new, DiskImageBlockDevice::open, DiskImageBlockDevice::open(image_path, 4096, 16384).map_err, Err, FS_CONTEXT.lock ...(+75) | Err, FS_CONTEXT.lock, FS_CONTEXT.lock().resolve, MemoryFs::new, Ok, debug! ...(+2) |
| `kernel/src/pseudofs/dev/mod.rs#builder` | `api/src/vfs/dev/mod.rs#builder` | 同板块同名 | Arc::new, Device::new, DeviceId::new, DirMapping::new, Ok, Random::new ...(+13) | Arc::new, Device::new, DeviceId::new, DirMapping::new, Ok, Random::new ...(+11) |
| `kernel/src/syscall/net/socket.rs#sys_socket` | `api/src/syscall/net/socket.rs#sys_socket` | 同板块同名 | AlgSocket::new, AlgSocket::new().add_to_fd_table, AlgSocket::new().add_to_fd_table(cloexec).map, Arc::new, AxError::from, CanSocket::new ...(+39) | DgramTransport::new, Err, Socket, StreamTransport::new, TcpSocket::new, UdpSocket::new ...(+12) |
| `kernel/src/syscall/sync/futex.rs#sys_futex` | `api/src/syscall/sync/futex.rs#sys_futex` | 同板块同名 | AxError::from, Err, FutexKey::new_current, Ok, Some, assert_unsigned ...(+25) | Err, FutexKey::new_current, Ok, Some, assert_unsigned, axtask::yield_now ...(+19) |

#### 内存管理
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `kernel/src/syscall/mod.rs#handle_syscall` | `api/src/syscall/mod.rs#handle_syscall` | 同板块同名 | (uctx.arg1() as usize).into, Err, LinuxError::ENOSYS.code, LinuxError::from, LinuxError::from(err).code, Ok ...(+285) | Err, LinuxError::ENOSYS.code, Ok, Sysno::new, debug!, err.code ...(+201) |
| `kernel/src/pseudofs/proc.rs#builder` | `api/src/vfs/proc.rs#builder` | 同板块同名 | "0\n".to_string, "Name        ID\ncn_proc     1\n".to_string, "console=hvc0 root=/dev/vda rw quiet\n".to_string, Arc::new, AtomicUsize::new, DirMapping::new ...(+84) | "0\n".to_string, Arc::new, DirMapping::new, Ok, ProcFsHandler, SimpleDir::new_maker ...(+8) |
| `kernel/src/mm/loader.rs#load` | `core/src/mm.rs#load` | 同板块同名 | AuxEntry::new, CStr::from_bytes_with_nul, CStr::from_bytes_with_nul(&data)
                .ok, CStr::from_bytes_with_nul(&data)
                .ok()
                .and_then, CStr::from_bytes_with_nul(&data)
                .ok()
                .and_then(|cstr| cstr.to_str().ok())
                .ok_or, ElfCacheEntry::load ...(+55) | CStr::from_bytes_with_nul, CStr::from_bytes_with_nul(&data)
                .ok, CStr::from_bytes_with_nul(&data)
                .ok()
                .and_then, CStr::from_bytes_with_nul(&data)
                .ok()
                .and_then(|cstr| cstr.to_str().ok())
                .ok_or, ElfCacheEntry::load, ElfCacheEntry::load(loc)?.map_err ...(+42) |
| `kernel/src/syscall/mm/mmap.rs#sys_mmap` | `api/src/syscall/mm/mmap.rs#sys_mmap` | 同板块同名 | (1usize << rnd_bits.min(20)).saturating_sub, (addr + length).align_up, (curr.as_thread().proc_data.proc.pid() as u64).wrapping_shl, (max_slide / align).max, (max_slide_pages * PAGE_SIZE_4K).min, (slot * align).min ...(+68) | (addr + length).align_up, Arc::new, Backend::new_alloc, Backend::new_cow, Backend::new_file, Backend::new_linear ...(+50) |

#### 中断/异常处理
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `kernel/src/pseudofs/dev/tty/terminal/termios.rs#signo_for` | `api/src/terminal/termios.rs#signo_for` | 同板块同名同结构 | Some, self.special_char | Some, self.special_char |
| `kernel/src/pseudofs/dev/tty/terminal/termios.rs#default` | `api/src/terminal/termios.rs#default` | 同板块同名同结构 | ctl | ctl |
| `patches/axplat-loongarch64-qemu-virt/src/irq.rs#set_enable` | `api/src/syscall/signal.rs#sys_rt_sigreturn` | 同模块上下文对齐/差异观察 | IrqType::new, ecfg::read, ecfg::read().lie, ecfg::set_lie, eiointc::disable_irq, eiointc::enable_irq ...(+4) | Ok, block_next_signal, current, current().as_thread, current().as_thread().signal.restore, tf.retval |
| `patches/axplat-loongarch64-qemu-virt/src/irq/eiointc.rs#init` | `arceos/modules/axhal/src/irq.rs#register_irq_waker` | 同模块上下文对齐/差异观察 | iocsr_read_d, iocsr_write_d, iocsr_write_w | POLL_TABLE[irq as usize].register, axplat::irq::register |

#### 系统调用
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `kernel/src/syscall/io_mpx/epoll.rs#sys_epoll_ctl` | `api/src/syscall/io_mpx/epoll.rs#sys_epoll_ctl` | 同板块同名 | Epoll::from_fd, EpollFlags::from_bits, EpollFlags::from_bits(event.events & !events.bits()).ok_or, Err, IoEvents::from_bits_truncate, Ok ...(+7) | Epoll::from_fd, EpollFlags::from_bits, EpollFlags::from_bits(event.events & !(events.bits() as u32))
            .ok_or, Err, IoEvents::from_bits_truncate, Ok ...(+7) |
| `kernel/src/syscall/io_mpx/epoll.rs#do_epoll_wait` | `api/src/syscall/io_mpx/epoll.rs#do_epoll_wait` | 同板块同名 | Epoll::from_fd, Err, Ok, block_on, check_sigset_size, debug! ...(+9) | Epoll::from_fd, Err, Ok, Poller::new, Poller::new(epoll.as_ref(), IoEvents::IN)
            .timeout, Poller::new(epoll.as_ref(), IoEvents::IN)
            .timeout(timeout)
            .poll ...(+8) |
| `kernel/src/syscall/net/cmsg.rs#push` | `api/src/syscall/net/cmsg.rs#push` | 同板块同名 | (self.capacity - *self.len).checked_sub, Ok, UserPtr::<u8>::from, UserPtr::<u8>::from(self.hdr.address().as_usize() + size_of::<cmsghdr>())
            .get_as_mut_slice, UserPtr::from, body ...(+4) | (self.capacity - *self.len).checked_sub, Ok, UserPtr::<u8>::from, UserPtr::<u8>::from(self.hdr.address().as_usize() + size_of::<cmsghdr>())
            .get_as_mut_slice, UserPtr::from, body ...(+4) |
| `kernel/src/syscall/sys.rs#sys_getgroups` | `api/src/syscall/sys.rs#sys_getgroups` | 同板块同名 | Err, Ok, debug!, vm_write_slice | Err, Ok, debug!, vm_write_slice |

#### 文件系统
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `kernel/src/syscall/net/io.rs#recv_impl` | `api/src/syscall/net/io.rs#recv_impl` | 同板块同名 | (!addr.is_null()).then, (Ipv4Addr::UNSPECIFIED, 0).into, (base as *mut u8).add, IcmpSocket::from_fd, Ipv6Socket::from_fd, NetlinkSocket::from_fd ...(+35) | (!addr.is_null()).then, (Ipv4Addr::UNSPECIFIED, 0).into, Ok, RecvFlags::empty, Socket::from_fd, SocketAddrEx::Ip ...(+18) |
| `kernel/src/pseudofs/dev/fb.rs#ioctl` | `api/src/vfs/dev/fb.rs#ioctl` | 同板块同名 | (arg as *mut FixScreenInfo).vm_write, (arg as *mut VarScreenInfo).vm_write, Err, Ok, axdisplay::framebuffer_info | (arg as *mut FixScreenInfo).vm_write, (arg as *mut VarScreenInfo).vm_write, Err, Ok, axdisplay::main_display, axdisplay::main_display().info |
| `kernel/src/pseudofs/tracing.rs#lookup_child` | `api/src/vfs/dev/tty/pts.rs#lookup_child` | 同板块同名 | Arc::new, Ok, Ok(rw_file(fs, "# tracer: nop\n#\n sig 1  [000] 0.0: signal:signal_generate: sig=1 grp=0 res=0\n")).into, Ok(rw_file(fs, "0\n")).into, Ok(rw_file(fs, "1408\n")).into, Ok(rw_file(fs, "1\n")).into ...(+22) | NodeOpsMux::File, Ok, PTS_TABLE.lock, PTS_TABLE.lock().get, PTS_TABLE.lock().get(id).ok_or, PTS_TABLE.lock().get(id).ok_or(LinuxError::ENOENT)?.clone ...(+2) |
| `kernel/src/file/ipv6_raw.rs#ioctl` | `api/src/vfs/dev/loop.rs#ioctl` | 同板块同名 | AxError::from, Err, LinuxError::ENODEV.into, Ok, UserConstPtr::<u8>::from, UserPtr::<i16>::from ...(+20) | (arg as *const loop_info).vm_read_uninit, (arg as *const loop_info).vm_read_uninit()?.assume_init, (arg as *const u32).vm_read, (arg as *mut loop_info).vm_write, (arg as *mut u32).vm_write, (arg as *mut u64).vm_write ...(+22) |

#### 设备驱动
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `patches/axnet-ng/src/router.rs#dispatch` | `arceos/modules/axnet/src/router.rs#dispatch` | 同板块同名 | IpAddress::Ipv4, IpAddress::Ipv6, IpVersion::of_packet, IpVersion::of_packet(packet).expect, assert_eq!, dev.send ...(+14) | IpAddress::Ipv4, IpAddress::Ipv6, IpVersion::of_packet, IpVersion::of_packet(packet).expect, assert_eq!, dev.send ...(+12) |
| `patches/axnet-ng/src/udp.rs#bind` | `arceos/modules/axnet/src/tcp.rs#bind` | 同板块同名 | (!local_endpoint.addr.is_unspecified()).then_some, AxError::from, Err, IpEndpoint::from, Ipv4Addr::new, Ok ...(+32) | Err, Ok, SERVICE.lock, SERVICE.lock().device_mask_for, SOCKET_SET.bind_check, Some ...(+17) |
| `patches/axnet-ng/src/udp.rs#send` | `arceos/modules/axnet/src/device/ethernet.rs#send` | 同板块同名 | Err, IpEndpoint::from, Ok, PacketMeta::default, Some, addr.into_ip ...(+21) | Self::send_to, Some, buf.copy_from_slice, dst_buffer.copy_from_slice, next_hop.is_broadcast, packet.len ...(+7) |
| `patches/axnet-ng/src/device/ethernet.rs#send` | `arceos/tools/bwbench_client/src/device.rs#send` | 同板块同名 | Self::send_to, Some, buf.copy_from_slice, dst_buffer.copy_from_slice, next_hop.is_broadcast, packet.len ...(+7) | Err, Ok, buffer.as_ptr, buffer.len, err.kind, libc::send ...(+2) |

#### 网络协议栈
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `patches/axnet-ng/src/udp.rs#recv` | `arceos/modules/axnet/src/udp.rs#recv` | 同板块同名 | (Ipv4Addr::UNSPECIFIED, 0).into, Err, ExpectedRemote::Any, ExpectedRemote::Expecting, Ok, SocketAddrEx::Ip ...(+22) | Err, ExpectedRemote::Any, ExpectedRemote::Expecting, Ok, SocketAddrEx::Ip, ax_err! ...(+20) |
| `patches/axnet-ng/src/vsock/stream.rs#recv` | `arceos/modules/axnet/src/tcp.rs#recv` | 同板块同名 | Err, Ok, conn.lock, conn_guard.advance_rx_read, conn_guard.rx_buffer_used, conn_guard.rx_closed ...(+9) | Err, Ok, ax_err!, dst.remaining_mut, dst.write, options.flags.contains ...(+13) |
| `patches/axnet-ng/src/tcp.rs#recv` | `arceos/modules/axnet/src/unix/dgram.rs#recv` | 同板块同名 | Err, Ok, ax_err_type!, dst.remaining_mut, dst.write, options.flags.contains ...(+12) | Err, Ok, SocketAddrEx::Unix, data.len, dst.extend, dst.write ...(+9) |
| `patches/axnet-ng/src/unix/dgram.rs#recv` | `arceos/modules/axnet/src/unix/stream.rs#recv` | 同板块同名 | Err, Ok, SocketAddrEx::Unix, data.len, dst.extend, dst.write ...(+8) | Err, Ok, chan.poll_update.wake, chan.rx.advance_read_index, chan.rx.as_slices, dst.write ...(+5) |

#### 文件系统/第三方库
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `patches/axfs-ng/src/fs/ext4/inode.rs#create` | `arceos/modules/axfs-ng/src/fs/ext4/inode.rs#create` | 同板块同名同结构 | DirEntry::new_dir, DirEntry::new_file, DirNode::new, Err, FileNode::new, Inode::new ...(+14) | DirEntry::new_dir, DirEntry::new_file, DirNode::new, Err, FileNode::new, Inode::new ...(+14) |
| `patches/axfs-ng/src/fs/ext4/inode.rs#metadata` | `arceos/modules/axfs-ng/src/fs/ext4/inode.rs#metadata` | 同板块同名同结构 | DeviceId::default, FileAttr::default, NodePermission::from_bits_truncate, Ok, into_vfs_type, self.fs
            .lock ...(+2) | DeviceId::default, FileAttr::default, NodePermission::from_bits_truncate, Ok, into_vfs_type, self.fs
            .lock ...(+2) |
| `patches/axfs-ng/src/fs/ext4/inode.rs#read_dir` | `arceos/modules/axfs-ng/src/fs/ext4/inode.rs#read_dir` | 同板块同名同结构 | Ok, core::str::from_utf8, core::str::from_utf8(entry.name())
                .map_err, core::str::from_utf8(entry.name())
                .map_err(|_| VfsError::InvalidData)?
                .to_owned, entry.ino, entry.inode_type ...(+10) | Ok, core::str::from_utf8, core::str::from_utf8(entry.name())
                .map_err, core::str::from_utf8(entry.name())
                .map_err(|_| VfsError::EINVAL)?
                .to_owned, entry.ino, entry.inode_type ...(+10) |
| `patches/axfs-ng/src/fs/ext4/inode.rs#update_metadata` | `arceos/modules/axfs-ng/src/fs/ext4/inode.rs#update_metadata` | 同板块同名同结构 | Ok, fs.with_inode_ref, fs.with_inode_ref(self.ino, |inode| {
            if let Some(mode) = update.mode {
                inode.set_mode((inode.mode() & !0xfff) | (mode.bits() as u32));
            }
            if let Some((uid, gid)) = update.owner {
                inode.set_owner(uid as _, gid as _);
            }
            if let Some(atime) = update.atime {
                inode.set_atime(&atime);
            }
            if let Some(mtime) = update.mtime {
                inode.set_mtime(&mtime);
            }
            inode.update_ctime();
            Ok(())
        })
        .map_err, inode.mode, inode.set_atime, inode.set_mode ...(+5) | Ok, fs.with_inode_ref, fs.with_inode_ref(self.ino, |inode| {
            if let Some(mode) = update.mode {
                inode.set_mode((inode.mode() & !0xfff) | (mode.bits() as u32));
            }
            if let Some((uid, gid)) = update.owner {
                inode.set_owner(uid as _, gid as _);
            }
            if let Some(atime) = update.atime {
                inode.set_atime(&atime);
            }
            if let Some(mtime) = update.mtime {
                inode.set_mtime(&mtime);
            }
            inode.update_ctime();
            Ok(())
        })
        .map_err, inode.mode, inode.set_atime, inode.set_mode ...(+5) |

#### 通用模块/其他
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `kernel/src/time.rs#try_into_time_value` | `api/src/time.rs#try_into_time_value` | 同板块同名 | Err, Ok, TimeValue::new | Err, Ok, TimeValue::new |
| `kernel/src/time.rs#try_into_time_value` | `api/src/time.rs#try_into_time_value` | 同板块同名 | Err, Ok, TimeValue::new | Err, Ok, TimeValue::new |
| `kernel/src/time.rs#try_into_time_value` | `api/src/time.rs#try_into_time_value` | 同板块同名 | Err, Ok, TimeValue::new | Err, Ok, TimeValue::new |
| `patches/axio/src/write/mod.rs#write_str` | `arceos/modules/axlog/src/lib.rs#write_str` | 同板块同名 | Err, Ok, s.as_bytes, self.inner.write_all | Ok, cfg_if::cfg_if! |


#### 1. 宏观判定
- **定性结论**: 底层架构显著差异 / 独立开发 (相似度: 15.23%，共同 AST 指纹: 550；唯一 AST 指纹: 目标项目 2659，参考项目 1503；覆盖率: 目标项目 20.68%，参考项目 36.59%)

#### 2. 全模块覆盖矩阵解读
- **进程管理**: 双方均存在该模块。共同 AST 指纹 16 个，目标覆盖率 13.01%，参考覆盖率 30.77%。目标项目 139 个函数中仅 16 个与参考项目存在结构重合，且重合点集中于 Linux ABI 强制约束的系统调用封装（如 `sys_getpid`、`sys_exit`）和 POSIX 标准语义操作（如 SHM 生命周期管理）。该模块证据强度为**弱**，控方将网络协议栈函数（`process_arp`、`preprocess`）强行归入此模块，构成跨板块硬凑。
- **任务调度/异步运行时**: 双方均存在该模块。共同 AST 指纹 46 个，目标覆盖率 12.92%，参考覆盖率 17.36%。目标项目 471 个函数中绝大多数为独立实现。双方在 futex 等待机制上存在关键架构差异：目标使用 `block_on` + `Cell::new` 同步阻塞模型，参考使用 `block_on_interruptible` + `condition.take` 可中断模型，底层异步原语选择不同。该模块证据强度为**弱**。
- **内存管理**: 双方均存在该模块。共同 AST 指纹 66 个，目标覆盖率 17.01%，参考覆盖率 26.4%。目标项目 410 个函数中超过 80% 为独立 AST 指纹。双方在 mmap 实现上存在显著差异：目标使用 `rnd_bits`、`max_slide` 等随机化与滑动窗口策略，参考使用 `Backend::new_alloc/cow/file/linear` 后端抽象模型，内存映射策略异构。该模块证据强度为**弱**。
- **中断/异常处理**: 双方均存在该模块。共同 AST 指纹仅 2 个，目标覆盖率 3.57%，参考覆盖率 25.0%。目标项目基于 LoongArch64 架构（`eiointc`、`iocsr`），参考项目基于 ArceOS 通用 IRQ 框架（`POLL_TABLE`），硬件抽象层完全不同。该模块证据强度为**未形成**。
- **系统调用**: 双方均存在该模块。共同 AST 指纹 9 个，目标覆盖率 4.37%，参考覆盖率 12.0%。目标项目 224 个函数中仅 9 个存在结构重合，且均为 Linux 系统调用号分发与标准语义封装（如 `sys_epoll_ctl`、`sys_getgroups`）。该模块证据强度为**弱**。
- **文件系统**: 双方均存在该模块。共同 AST 指纹 254 个，目标覆盖率 29.95%，参考覆盖率 54.27%。参考覆盖率偏高系其文件系统模块体量较小（562 个函数）导致的分母效应。双方在 procfs 实现上存在显著差异：目标扩展了 `mem`、`fdinfo`、`attr`、`ns` 等 8 个额外目录项，FD_TABLE 访问模式不同（目标混合 `read()` 与 `scope()`，参考统一 `scope()`）。该模块证据强度为**中**，但重合主要集中于 VFS trait 方法名和 ext4 第三方库调用。
- **设备驱动**: 双方均存在该模块。共同 AST 指纹 20 个，目标覆盖率 22.47%，参考覆盖率 17.24%。双方在 framebuffer ioctl 等设备控制接口上存在结构相似，但此类相似源于 Linux 设备驱动 ABI 约束。该模块证据强度为**弱**。
- **网络协议栈**: 双方均存在该模块。共同 AST 指纹 59 个，目标覆盖率 23.05%，参考覆盖率 46.09%。双方均基于 smoltcp 库构建，`ArpPacket`、`IpEndpoint`、`SocketAddrEx` 等调用链系 smoltcp API 强制收敛。目标项目额外实现了 `vsock`、`AlgSocket`、`CanSocket` 等参考项目未支持的协议族。该模块证据强度为**弱**，重合源于共同第三方库依赖。
- **第三方库/公共依赖**: 双方均存在该模块。共同 AST 指纹 33 个，目标覆盖率 84.62%，参考覆盖率 89.19%。双方在 ext4 文件系统实现上调用图谱高度一致（`DirEntry::new_dir`、`Inode::new`、`update_metadata` 等），系双方均直接引用 `ext4-rs` 等公共第三方 crate，属于合法复用上游开源组件。该模块证据强度为**未形成**（不构成抄袭证据）。

#### 3. 底层 AST 关键证据指纹表
| 所属板块 | 目标项目路径 | 参考项目路径 | 证据类型 | 技术说明 |
| :--- | :--- | :--- | :--- | :--- |
| 第三方库/公共依赖 | `patches/axfs-ng/src/fs/ext4/inode.rs#create` | `arceos/modules/axfs-ng/src/fs/ext4/inode.rs#create` | 同板块同名同结构 | 双方均调用 `DirEntry::new_dir`、`Inode::new` 等 ext4-rs 公共库 API，属于合法复用上游开源组件 |
| 第三方库/公共依赖 | `patches/axfs-ng/src/fs/ext4/inode.rs#metadata` | `arceos/modules/axfs-ng/src/fs/ext4/inode.rs#metadata` | 同板块同名同结构 | `DeviceId::default`、`NodePermission::from_bits_truncate` 等均为 ext4-rs 标准元数据操作 |
| 第三方库/公共依赖 | `patches/axfs-ng/src/fs/ext4/inode.rs#update_metadata` | `arceos/modules/axfs-ng/src/fs/ext4/inode.rs#update_metadata` | 同板块同名同结构 | `fs.with_inode_ref` 闭包内 `inode.set_mode/set_atime/set_mtime/update_ctime` 完全一致，系 ext4-rs 库的标准更新流程 |
| 网络协议栈 | `patches/axnet-ng/src/device/ethernet.rs#process_arp` | `arceos/modules/axnet/src/device/ethernet.rs#process_arp` | 同板块同名同结构 | `ArpPacket::new_checked` → `ArpRepr::parse` → `IpAddress::Ipv4` 系 smoltcp 库 ARP 处理标准流程 |
| 网络协议栈 | `patches/axnet-ng/src/router.rs#dispatch` | `arceos/modules/axnet/src/router.rs#dispatch` | 同板块同名 | `IpVersion::of_packet` → `dev.send` 系 smoltcp 路由分发标准模式 |
| 进程管理 | `kernel/src/syscall/ipc/shm.rs#clear_proc_shm` | `core/src/shm.rs#clear_proc_shm` | 同板块同名 | `get_shmids_by_pid` → `get_inner_by_shmid` → `detach_process` → `remove_shmid` 系 POSIX SysV SHM 清理标准状态机 |
| 进程管理 | `kernel/src/syscall/task/thread.rs#sys_getppid` | `api/src/syscall/task/thread.rs#sys_getppid` | 同板块同名 | `current().as_thread().proc_data.proc.parent()` 系 Linux getppid 系统调用固定语义，错误类型不同（`AxError::NoSuchProcess` vs `LinuxError::ESRCH`） |
| 进程管理 | `kernel/src/task/signal.rs#send_signal_to_process_group` | `core/src/task.rs#send_signal_to_process_group` | 同板块同名 | `get_process_group` → `pg.processes` → `send_signal_to_process` 系 POSIX 信号组发送标准流程 |
| 系统调用 | `kernel/src/syscall/io_mpx/epoll.rs#sys_epoll_ctl` | `api/src/syscall/io_mpx/epoll.rs#sys_epoll_ctl` | 同板块同名 | `Epoll::from_fd` → `EpollFlags::from_bits` 系 Linux epoll_ctl 系统调用标准语义 |
| 系统调用 | `kernel/src/syscall/net/cmsg.rs#push` | `api/src/syscall/net/cmsg.rs#push` | 同板块同名 | `UserPtr::<u8>::from` → `get_as_mut_slice` 系 Linux cmsg 辅助数据标准处理模式 |
| 文件系统 | `kernel/src/pseudofs/proc.rs#lookup_child` | `api/src/vfs/proc.rs#lookup_child` | 同板块同名 | `SimpleDir::new_maker`、`Arc::new` 系 VFS trait 方法标准调用，目标额外处理 `Ipv4IfaceConfDir`/`Ipv6IfaceConfDir` |
| 文件系统 | `kernel/src/pseudofs/proc.rs#child_names` | `api/src/vfs/proc.rs#child_names` | 同板块同名 | 目标扩展 17 个 procfs 目录项（参考仅 10 个），FD_TABLE 访问模式不同 |
| 设备驱动 | `kernel/src/pseudofs/dev/fb.rs#ioctl` | `api/src/vfs/dev/fb.rs#ioctl` | 同板块同名 | `FixScreenInfo`/`VarScreenInfo` 系 Linux framebuffer ioctl 标准结构 |
| 内存管理 | `kernel/src/syscall/mm/mmap.rs#sys_mmap` | `api/src/syscall/mm/mmap.rs#sys_mmap` | 同板块同名 | 目标使用 `rnd_bits`/`max_slide` 随机化策略，参考使用 `Backend::new_alloc/cow` 后端抽象，映射模型异构 |
| 任务调度/异步运行时 | `kernel/src/task/futex.rs#wait_if` | `core/src/futex.rs#wait_if` | 同板块同名 | 目标使用 `block_on` + `Cell::new` 同步阻塞，参考使用 `block_on_interruptible` + `condition.take` 可中断模型 |
| 中断/异常处理 | `patches/axplat-loongarch64-qemu-virt/src/irq/eiointc.rs#init` | `arceos/modules/axhal/src/irq.rs#register_irq_waker` | 同模块上下文对齐/差异观察 | 目标基于 LoongArch `iocsr_read_d/write_d`，参考基于 `POLL_TABLE` 通用框架，硬件抽象层完全不同 |

#### 4. 核心代码调用流对比剖析
* **【进程管理/信号处理路径】**
* **底层技术析理**: 双方在 `send_signal_to_process` 的实现上存在关键差异。目标项目调用链为 `get_process_data` → `get_task` → `proc_data.signal.send_signal` → `task.interrupt`，直接发送信号并中断任务；参考项目在 `send_signal` 前额外插入 `proc_data.signal.can_restart` 检查，用于判断信号是否可重启被中断的系统调用。这一差异反映了两者在信号处理语义上的不同设计选择：目标采用简化信号模型，参考实现了更完整的 POSIX 信号重启语义。此外，目标项目 `get_process_data` 支持通过 `task.as_thread` 从任务引用获取进程数据，参考项目仅支持通过 `pid` 从 `PROCESS_TABLE` 查找，对象访问路径不同。
* **代码溯源链接**:
  - 目标项目: `kernel/src/task/signal.rs#send_signal_to_process`
  - 参考项目: `core/src/task.rs#send_signal_to_process`

* **【内存管理/mmap 路径】**
* **底层技术析理**: 双方在 `sys_mmap` 的内存映射策略上存在架构级差异。目标项目调用链包含 `rnd_bits`（随机化位数计算）、`max_slide`（最大滑动窗口）、`PAGE_SIZE_4K` 等底层地址空间随机化（ASLR）相关操作，表明其实现了独立的内存布局随机化策略。参考项目调用链为 `Backend::new_alloc`、`Backend::new_cow`、`Backend::new_file`、`Backend::new_linear`，采用后端抽象模型将不同映射类型解耦为独立后端对象。两者在内存抽象层次上截然不同：目标关注地址空间安全布局，参考关注映射后端的多态抽象。
* **代码溯源链接**:
  - 目标项目: `kernel/src/syscall/mm/mmap.rs#sys_mmap`
  - 参考项目: `api/src/syscall/mm/mmap.rs#sys_mmap`

* **【任务调度/异步运行时/futex 路径】**
* **底层技术析理**: 双方在 `wait_if` 的 futex 等待实现上存在同步原语层面的根本差异。目标项目使用 `block_on` 进行同步阻塞等待，通过 `core::cell::Cell::new` 管理条件变量状态，`enqueued.get` 检查是否已入队。参考项目使用 `block_on_interruptible` 支持可中断阻塞，通过 `condition.take` 取出条件变量所有权，`cond` 判断条件是否满足。目标采用同步阻塞 + Cell 内部可变性模型，参考采用可中断阻塞 + Option take 所有权转移模型，两者在异步原语选择和状态管理策略上异构。
* **代码溯源链接**:
  - 目标项目: `kernel/src/task/futex.rs#wait_if`
  - 参考项目: `core/src/futex.rs#wait_if`

* **【文件系统/procfs 路径】**
* **底层技术析理**: 双方在 `child_names` 的 procfs 子目录生成上存在显著功能差异。目标项目返回 17 个目录项（含 `mem`、`fdinfo`、`attr`、`ns`、`timens_offsets`、`uid_map`、`gid_map`、`setgroups`），更接近真实 Linux `/proc` 布局；参考项目仅返回 10 个目录项。目标项目 FD_TABLE 访问混合使用 `FD_TABLE.read()` 和 `FD_TABLE.scope()`，参考项目统一使用 `FD_TABLE.scope(&task.as_thread().proc_data.scope.read())`。目标项目额外实现了 `crate::task::is_current_process` 检查，参考项目通过 `self.process.upgrade` 获取进程引用。两者在功能完整性和内部对象访问模型上均存在独立设计。
* **代码溯源链接**:
  - 目标项目: `kernel/src/pseudofs/proc.rs#child_names`
  - 参考项目: `api/src/vfs/proc.rs#child_names`

* **【网络协议栈/ARP 处理路径】**
* **底层技术析理**: 双方在 `process_arp` 的调用图谱高度一致，但此一致性源于共同依赖的 smoltcp 网络协议栈库。`ArpPacket::new_checked` → `ArpRepr::parse` → `IpAddress::Ipv4` → `Self::send_to` 是 smoltcp 库定义的 ARP 包处理标准流程，任何基于 smoltcp 的实现都必须遵循此 API 调用顺序。目标项目位于 `patches/axnet-ng` 补丁层，参考项目位于 `arceos/modules/axnet` 模块层，两者均派生自 ArceOS 上游网络子系统，相似性来自共同上游而非互相抄袭。
* **代码溯源链接**:
  - 目标项目: `patches/axnet-ng/src/device/ethernet.rs#process_arp`
  - 参考项目: `arceos/modules/axnet/src/device/ethernet.rs#process_arp`

#### 5. 单方模块与降权项
- **单方模块**: 目标项目独有 `kernel/src/pseudofs/tracing.rs`（ftrace 追踪文件系统）、`kernel/src/syscall/ipc/shm.rs`（完整 SysV SHM 实现）、`kernel/src/file/ipv6_raw.rs`（IPv6 原始套接字）等模块，参考项目无对应实现。参考项目独有 `arceos/tools/bwbench_client`（带宽测试客户端）等工具模块，目标项目无对应实现。这些单方模块表明两者在功能边界和实现范围上存在独立决策。
- **降权项说明**:
  - **第三方库/公共依赖**: ext4 文件系统操作（`patches/axfs-ng` 与 `arceos/modules/axfs-ng`）覆盖率高达 84.62%/89.19%，系双方均直接引用 `ext4-rs` 公共 crate，属于合法复用上游开源组件，不构成抄袭证据。
  - **跨板块硬凑**: 控方将 `patches/axnet-ng/src/device/ethernet.rs#process_arp`（网络协议栈）、`patches/axnet-ng/src/router.rs#preprocess`（网络路由）、`kernel/src/pseudofs/proc.rs#new_procfs`（文件系统初始化）强行归入“进程管理”板块，违反同板块比对原则，已降级为弱信号。
  - **Trivial 单行操作**: `set_clear_child_tid`（`self.clear_child_tid.store`）、`clear_child_tid`（`self.clear_child_tid.load`）、`is_clone_child`（返回 `Some`）均为单行原子操作或 trivial 判断，是 Linux `clone`/`set_tid_address` 语义的直接映射，不构成可保护表达。
  - **Trait/ABI 强制同名**: `lookup_child`、`child_names`、`task_status` 是 VFS trait 方法名，`sys_getpid`、`sys_exit`、`sys_fork` 是 Linux 系统调用名，同名系接口规范强制要求，不能作为抄袭证据。

#### 6. 保守结论
目标项目 `[2026-T202610486999578-XX]` 与参考项目 `[2025-StarryMix-清华大学]` 的客观 AST 相似度为 15.23%，目标项目覆盖率仅 20.68%，约 80% 的 AST 指纹为独立拥有。双方在进程管理、内存管理、任务调度等核心模块的覆盖率均低于 20%，且存在架构级差异：目标采用 LoongArch64 硬件抽象层、`AxError` 错误模型、`block_on` 同步阻塞原语；参考采用 ArceOS 通用 IRQ 框架、`LinuxError` 错误模型、`block_on_interruptible` 可中断原语。高覆盖率集中于第三方库/公共依赖模块（84.62%），系双方共同引用 ext4-rs、smoltcp 等上游开源组件所致，属于合法复用。控方多项指控存在跨板块硬凑（将网络协议栈函数归入进程管理）和 trivial 操作过度解读（单行原子操作视为抄袭）的问题。综合判定：双方为同一领域、同一上游生态、同一 ABI 约束下的独立开发，不构成代码抄袭。

------------------------------

