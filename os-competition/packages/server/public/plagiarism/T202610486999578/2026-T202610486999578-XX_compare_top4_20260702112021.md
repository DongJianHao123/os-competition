# 🛡️ OS 内核赛自动化代码级架构审计报告

**审计时间**: `2026-07-02 11:20:21`
**参赛作品名称**: `2026-T202610486999578-XX`
**年份**: `2026`
**学校**: `XX`
**队伍名称**: `T202610486999578`
**比赛名字**: `OS-Competition`
**比赛子赛道**: `Kernel-Implementation`
**仓库地址**: `https://gitlab.eduxiji.net/educg-group-43501-3132633/T202610486999578-700`
**推测开发语言**: `Rust / C (由AST探针自动识别)`
**特征函数量**: `3235`

## ⚔️ 底层代码同源性对比 (AST Homology)
> 本部分由专门的代码审查智能体，对该项目与符合年份条件的历届最高相似度项目进行控制流与底层调用对比。新版报告会先给出确定性的全模块覆盖矩阵，再给出 Agent 的分模块解释，避免只围绕少量抽样函数展开。

### 🏆 Top 4 底层对标: vs `2025-undefined-清华大学`（清华大学 / undefined）

#### 对方作品信息

**对方年份**: `2025`
**对方学校**: `清华大学`
**对方队伍名称**: `undefined`
**对方比赛名字**: `OS-Competition`
**对方比赛子赛道**: `Kernel-Implementation`
**对方仓库地址**: `https://gitlab.eduxiji.net/educg-group-36002-2710490/T202510003995291-2331`

> 双向覆盖率：共同 AST 指纹: 211；唯一 AST 指纹: 目标项目 2659，参考项目 2820；覆盖率: 目标项目 7.94%，参考项目 7.48%

## 全模块对比总览（确定性证据）

> 总体覆盖率：共同 AST 指纹: 211；唯一 AST 指纹: 目标项目 2659，参考项目 2820；覆盖率: 目标项目 7.94%，参考项目 7.48%

### 1. 模块覆盖矩阵
| 模块 | 目标函数数 | 参考函数数 | 共同唯一 AST 指纹 | Target 覆盖 | Base 覆盖 | 状态 |
| :--- | ---: | ---: | ---: | ---: | ---: | :--- |
| 进程管理 | 139 | 135 | 3 | 2.44% | 2.48% | 双方存在且有结构重合 |
| 任务调度/异步运行时 | 471 | 415 | 23 | 6.46% | 7.67% | 双方存在且有结构重合 |
| 内存管理 | 410 | 450 | 20 | 5.15% | 5.24% | 双方存在且有结构重合 |
| 中断/异常处理 | 59 | 53 | 12 | 21.43% | 25.53% | 双方存在且有结构重合 |
| 系统调用 | 224 | 109 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |
| 文件系统 | 1034 | 999 | 96 | 11.32% | 14.37% | 双方存在且有结构重合 |
| 设备驱动 | 100 | 133 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |
| 网络协议栈 | 290 | 65 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |
| 文件系统/第三方库 | 39 | 643 | 16 | 41.03% | 2.96% | 双方存在且有结构重合 |
| 网络协议栈/第三方库 | 0 | 130 | 0 | 0.0% | 0.0% | 仅参考项目体现 |
| 通用模块/其他 | 469 | 555 | 9 | 2.45% | 2.06% | 双方存在且有结构重合 |

### 2. 分模块证据清单

#### 进程管理
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `kernel/src/syscall/signal.rs#sys_rt_sigprocmask` | `api/src/imp/task/signal.rs#sys_rt_sigprocmask` | 同板块同名 | Err, Ok, check_sigset_size, curr.as_thread, current, debug! ...(+7) | Err, Ok, check_sigset_size, current_thread_data, current_thread_data()
        .signal
        .with_blocked_mut::<LinuxResult<_>>, oldset.nullable ...(+1) |
| `kernel/src/task/ops.rs#get_process_data` | `core/src/process.rs#get_process_data` | 同板块同名 | Err, Ok, PROCESS_TABLE.read, PROCESS_TABLE.read().get, current, current().as_thread ...(+5) | Some, get_thread_data, thread_data.process_data.clone |
| `kernel/src/syscall/task/wait.rs#apply` | `api/src/imp/task/wait.rs#apply` | 同板块同名 | child.group, child.group().pgid, child.pid | child.get_group, child.get_group().get_pgid, child.get_pid |
| `kernel/src/syscall/task/thread.rs#sys_getppid` | `api/src/imp/task/thread.rs#sys_getppid` | 同板块同名 | current, current()
        .as_thread, current()
        .as_thread()
        .proc_data
        .proc
        .parent, current()
        .as_thread()
        .proc_data
        .proc
        .parent()
        .ok_or, current()
        .as_thread()
        .proc_data
        .proc
        .parent()
        .ok_or(AxError::NoSuchProcess)
        .map, p.pid | Ok, current_process, current_process().get_parent, p.get_pid |

#### 任务调度/异步运行时
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `kernel/src/syscall/sync/futex.rs#sys_futex` | `api/src/imp/task/futex.rs#sys_futex` | 同板块同名 | AxError::from, Err, FutexKey::new_current, Ok, Some, assert_unsigned ...(+25) | Err, Ok, axtask::yield_now, current_process_data, drop, futex_table
                .entry ...(+27) |
| `kernel/src/syscall/signal.rs#sys_kill` | `api/src/imp/task/signal.rs#sys_kill` | 同板块同名 | Err, Ok, Some, crate::net::lookup_pid_netns, crate::net::lookup_pid_netns(pid).is_some, current ...(+16) | Err, Ok, current_process, current_process().get_group, debug!, get_all_processes ...(+8) |
| `patches/axcpu/src/aarch64/init.rs#switch_to_el1` | `arceos/modules/axhal/src/platform/aarch64_common/boot.rs#switch_to_el1` | 同板块同名 | CNTHCTL_EL2.modify, CNTVOFF_EL2.set, CurrentEL.read, ELR_EL2.set, ELR_EL3.set, HCR_EL2.write ...(+9) | CNTHCTL_EL2.modify, CNTVOFF_EL2.set, CurrentEL.read, ELR_EL2.set, ELR_EL3.set, HCR_EL2.write ...(+8) |
| `kernel/src/syscall/fs/ctl.rs#sys_chdir` | `api/src/imp/fs/ctl.rs#sys_chdir` | 同板块同名 | Err, FS_CONTEXT.lock, Ok, current, current()
                .as_thread, current()
                .as_thread()
                .proc_data
                .cred_egid
                .load ...(+8) | Ok, change_current_dir, drop, get_fs_context, path.get_as_str, resolve_path |

#### 内存管理
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `kernel/src/syscall/mod.rs#handle_syscall` | `src/syscall.rs#handle_syscall` | 同板块同名 | (uctx.arg1() as usize).into, Err, LinuxError::ENOSYS.code, LinuxError::from, LinuxError::from(err).code, Ok ...(+285) | Err, Sysno::new, err.code, info!, result.unwrap_or_else, set_trap_frame ...(+151) |
| `kernel/src/syscall/mm/mmap.rs#sys_mmap` | `api/src/imp/mm/mmap.rs#sys_mmap` | 同板块同名 | (1usize << rnd_bits.min(20)).saturating_sub, (addr + length).align_up, (curr.as_thread().proc_data.proc.pid() as u64).wrapping_shl, (max_slide / align).max, (max_slide_pages * PAGE_SIZE_4K).min, (slot * align).min ...(+68) | Err, File::from_fd, MmapFlags::from_bits_truncate, MmapProt::from_bits_truncate, Ok, PhysAddr::from ...(+34) |
| `kernel/src/syscall/ipc/shm.rs#sys_shmat` | `api/src/interface/mm/shm.rs#sys_shmat` | 同板块同名 | Arc::new, Backend::new_shared, Err, Ok, SHM_MANAGER.lock, SharedPages::new ...(+30) | Err, Ok, SHARED_MEMORY_MANAGER.get, SHARED_MEMORY_MANAGER.get(key).ok_or, ShmFlags::from_bits_truncate, Some ...(+16) |
| `kernel/src/mm/loader.rs#load_user_app` | `core/src/mm.rs#load_user_app` | 同板块同名 | "/bin/sh".to_owned, (ustack_top - user_sp_aligned).align_up_4k, Backend::new_alloc, ELF_LOADER.lock, ELF_LOADER.lock().load, Err ...(+44) | CStr::from_bytes_with_nul, CStr::from_bytes_with_nul(interp)
            .map_err, CStr::from_bytes_with_nul(interp)
            .map_err(|_| AxError::InvalidData)?
            .to_str, CStr::from_bytes_with_nul(interp)
            .map_err(|_| AxError::InvalidData)?
            .to_str()
            .map_err, CStr::from_bytes_with_nul(interp)
            .map_err(|_| AxError::InvalidData)?
            .to_str()
            .map_err(|_| AxError::InvalidData)?
            .to_string, ElfFile::new ...(+34) |

#### 中断/异常处理
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `patches/axplat-loongarch64-qemu-virt/src/irq.rs#set_enable` | `arceos/modules/axhal/src/platform/loongarch64_qemu_virt/irq.rs#set_enable` | 同板块同名 | IrqType::new, ecfg::read, ecfg::read().lie, ecfg::set_lie, eiointc::disable_irq, eiointc::enable_irq ...(+4) | ecfg::read, ecfg::read().lie, ecfg::set_lie |
| `patches/axcpu/src/x86_64/asm.rs#disable_irqs` | `arceos/modules/axhal/src/arch/x86_64/mod.rs#disable_irqs` | 同板块同名同结构 | interrupts::disable, warn! | interrupts::disable, warn! |
| `patches/axcpu/src/x86_64/asm.rs#enable_irqs` | `arceos/modules/axhal/src/arch/x86_64/mod.rs#enable_irqs` | 同板块同名同结构 | interrupts::enable, warn! | interrupts::enable, warn! |
| `patches/axcpu/src/x86_64/asm.rs#wait_for_irqs` | `arceos/modules/axhal/src/arch/x86_64/mod.rs#wait_for_irqs` | 同板块同名同结构 | asm!, cfg!, core::hint::spin_loop | asm!, cfg!, core::hint::spin_loop |

#### 系统调用
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `kernel/src/syscall/time.rs#sys_times` | `api/src/imp/utils/time.rs#sys_times` | 同板块同名 | Ok, borrow_thread_time, borrow_thread_time(current().as_thread())?.output, current, current().as_thread, monotonic_time_nanos ...(+4) | Ok, monotonic_time_nanos, nanos_to_ticks, time_stat_output, tms.get |
| `kernel/src/syscall/net/socket.rs#sys_shutdown` | `api/src/imp/net/socket.rs#sys_shutdown` | 同板块同名 | Err, Socket::from_fd, debug!, socket.shutdown, socket.shutdown(how).map | Ok, Socket::from_fd, Socket::from_fd(socket_fd)?.shutdown, debug! |
| `kernel/src/syscall/net/socket.rs#sys_listen` | `api/src/imp/net/socket.rs#sys_listen` | 同板块同名 | Err, Ok, Socket::from_fd, Socket::from_fd(fd)?.listen, debug! | Ok, Socket::from_fd, Socket::from_fd(socket_fd)?.listen, debug! |
| `kernel/src/syscall/net/socket.rs#sys_accept` | `api/src/imp/net/socket.rs#sys_accept` | 同板块同名 | sys_accept4 | Err, Mutex::new, Ok, Socket::Tcp, Socket::add_to_fd_table, Socket::from_fd ...(+6) |

#### 文件系统
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `kernel/src/pseudofs/dev/fb.rs#ioctl` | `api/src/core/fs/imp/dev/framebuffer.rs#ioctl` | 同板块同名 | (arg as *mut FixScreenInfo).vm_write, (arg as *mut VarScreenInfo).vm_write, Err, Ok, axdisplay::framebuffer_info | Default::default, Err, Ok, arg.into, framebuffer_flush, user_info.get_as_mut_ref |
| `kernel/src/file/ipv6_raw.rs#ioctl` | `api/src/core/fs/pseudo/file.rs#ioctl` | 同板块同名 | AxError::from, Err, LinuxError::ENODEV.into, Ok, UserConstPtr::<u8>::from, UserPtr::<i16>::from ...(+20) | Err, warn! |
| `kernel/src/syscall/fs/io.rs#sys_splice` | `api/src/interface/fs/io.rs#sys_splice` | 同板块同名 | DummyFd::from_fd, DummyFd::from_fd(fd_in).is_ok, DummyFd::from_fd(fd_out).is_ok, Err, File::from_fd, Pipe::from_fd ...(+20) | Err, Ok, fd_lookup, file_like_as::<Pipe>, file_like_as::<Pipe>(in_file.clone()).is_none, file_like_as::<Pipe>(out_file.clone()).is_none ...(+6) |
| `kernel/src/syscall/fs/fd_ops.rs#sys_close` | `api/src/imp/fs/fd_ops.rs#sys_close` | 同板块同名 | Ok, cleanup_flock_locks_on_close, close_file_like, crate::file::fanotify::is_nonotify_fd, debug!, f.downcast_arc::<File> ...(+14) | Ok, fd_remove |

#### 设备驱动
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `patches/axnet-ng/src/udp.rs#send` | `arceos/tools/bwbench_client/src/device.rs#send` | 同板块同名 | Err, IpEndpoint::from, Ok, PacketMeta::default, Some, addr.into_ip ...(+21) | Err, Ok, buffer.as_ptr, buffer.len, err.kind, libc::send ...(+2) |
| `patches/axnet-ng/src/lib.rs#init_network` | `arceos/modules/axnet/src/lib.rs#init_network` | 同板块同名 | "eth0".to_owned, Box::new, EthernetAddress, EthernetDevice::new, GATEWAY.parse, GATEWAY.parse().expect ...(+28) | info!, net_devs.take_one, net_devs.take_one().expect, net_impl::init |
| `patches/axnet-ng/src/device/ethernet.rs#recv` | `arceos/tools/bwbench_client/src/device.rs#recv` | 同板块同名 | matches!, rx_buf.packet, self.handle_frame, self.inner.receive, self.inner.recycle_rx_buffer, self.inner.recycle_rx_buffer(rx_buf).unwrap ...(+2) | Err, Ok, buffer.as_mut_ptr, buffer.len, err.kind, libc::recv ...(+2) |
| `patches/axplat-loongarch64-qemu-virt/src/console.rs#init_early` | `arceos/modules/axhal/src/platform/loongarch64_qemu_virt/console.rs#init_early` | 同板块同名 | UART_BASE.init_once, p.add, p.add(1).write_volatile, p.add(2).write_volatile, p.add(3).write_volatile, p.add(4).write_volatile ...(+4) | SpinNoIrq::new, UART.init_once, Uart::new, phys_to_virt, vaddr.as_usize |

#### 网络协议栈
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `patches/axnet-ng/src/udp.rs#recv` | `api/src/imp/net/socket.rs#recv` | 同板块同名 | (Ipv4Addr::UNSPECIFIED, 0).into, Err, ExpectedRemote::Any, ExpectedRemote::Expecting, Ok, SocketAddrEx::Ip ...(+22) | Ok, tcpsocket.lock, tcpsocket.lock().recv, udpsocket.lock, udpsocket.lock().recv_from, udpsocket.lock().recv_from(buf).map |
| `patches/axnet-ng/src/vsock/stream.rs#recv` | `arceos/ulib/axstd/src/net/udp.rs#recv` | 同板块同名 | Err, Ok, conn.lock, conn_guard.advance_rx_read, conn_guard.rx_buffer_used, conn_guard.rx_closed ...(+9) | api::ax_udp_recv |
| `patches/axnet-ng/src/listen_table.rs#accept` | `api/src/imp/net/socket.rs#accept` | 同板块同名 | Err, Ok, entry.lock, is_closed, is_connected, is_connected(handle).then_some ...(+9) | Err, Ok, tcpsocket.lock, tcpsocket.lock().accept |
| `patches/axnet-ng/src/vsock/stream.rs#accept` | `arceos/ulib/axstd/src/net/tcp.rs#accept` | 同板块同名 | Err, GeneralOptions::default, Mutex::new, Ok, Some, StateLock::new ...(+12) | TcpStream, api::ax_tcp_accept, api::ax_tcp_accept(&self.0).map |

#### 文件系统/第三方库
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `patches/axfs-ng/src/fs/ext4/inode.rs#create` | `arceos/modules/axfs-ng/src/fs/ext4/inode.rs#create` | 同板块同名 | DirEntry::new_dir, DirEntry::new_file, DirNode::new, Err, FileNode::new, Inode::new ...(+14) | DirEntry::new_dir, DirEntry::new_file, DirNode::new, Err, FileNode::new, Inode::new ...(+11) |
| `patches/axfs-ng/src/fs/ext4/inode.rs#metadata` | `arceos/modules/axfs-ng/src/fs/ext4/inode.rs#metadata` | 同板块同名同结构 | DeviceId::default, FileAttr::default, NodePermission::from_bits_truncate, Ok, into_vfs_type, self.fs
            .lock ...(+2) | DeviceId::default, FileAttr::default, NodePermission::from_bits_truncate, Ok, into_vfs_type, self.fs
            .lock ...(+2) |
| `patches/axfs-ng/src/fs/ext4/inode.rs#read_dir` | `arceos/modules/axfs-ng/src/fs/ext4/inode.rs#read_dir` | 同板块同名同结构 | Ok, core::str::from_utf8, core::str::from_utf8(entry.name())
                .map_err, core::str::from_utf8(entry.name())
                .map_err(|_| VfsError::InvalidData)?
                .to_owned, entry.ino, entry.inode_type ...(+10) | Ok, core::str::from_utf8, core::str::from_utf8(entry.name())
                .map_err, core::str::from_utf8(entry.name())
                .map_err(|_| VfsError::EINVAL)?
                .to_owned, entry.ino, entry.inode_type ...(+10) |
| `patches/axfs-ng/src/fs/ext4/inode.rs#update_metadata` | `arceos/modules/axfs-ng/src/fs/ext4/inode.rs#update_metadata` | 同板块同名 | Ok, fs.with_inode_ref, fs.with_inode_ref(self.ino, |inode| {
            if let Some(mode) = update.mode {
                inode.set_mode((inode.mode() & !0xfff) | (mode.bits() as u32));
            }
            if let Some((uid, gid)) = update.owner {
                inode.set_owner(uid as _, gid as _);
            }
            if let Some(atime) = update.atime {
                inode.set_atime(&atime);
            }
            if let Some(mtime) = update.mtime {
                inode.set_mtime(&mtime);
            }
            inode.update_ctime();
            Ok(())
        })
        .map_err, inode.mode, inode.set_atime, inode.set_mode ...(+5) | Ok, fs.with_inode_ref, fs.with_inode_ref(self.ino, |inode| {
            if let Some(mode) = update.mode {
                inode.set_mode((inode.mode() & !0xfff) | (mode.bits() as u32));
            }
            if let Some((uid, gid)) = update.owner {
                inode.set_owner(uid as _, gid as _);
            }
            if let Some(atime) = update.atime {
                inode.set_atime(&atime);
            }
            if let Some(mtime) = update.mtime {
                inode.set_mtime(&mtime);
            }
            Ok(())
        })
        .map_err, inode.mode, inode.set_atime, inode.set_mode ...(+4) |

#### 网络协议栈/第三方库
仅参考项目中观察到该模块代表函数，目标项目没有同模块可对齐证据：
- 参考项目：`arceos/modules/axnet/src/smoltcp_impl/tcp.rs#connect`，调用摘要：Err, Ok, Ok::<(IpEndpoint, IpEndpoint), AxError>, SOCKET_SET
                .with_socket_mut::<tcp::Socket, _, _>, SOCKET_SET.add, SocketSetWrapper::new_tcp_socket ...(+32)
- 参考项目：`arceos/modules/axnet/src/smoltcp_impl/dns.rs#query`，调用摘要：Err, Ok, SOCKET_SET
            .with_socket_mut::<dns::Socket, _, _>, SOCKET_SET
            .with_socket_mut::<dns::Socket, _, _>(handle, |socket| {
                socket.start_query(iface.lock().context(), name, query_type)
            })
            .map_err, SOCKET_SET.poll_interfaces, SOCKET_SET.with_socket_mut::<dns::Socket, _, _> ...(+12)
- 参考项目：`arceos/modules/axnet/src/smoltcp_impl/tcp.rs#recv_timeout`，调用摘要：Err, Ok, SOCKET_SET.with_socket_mut::<tcp::Socket, _, _>, ax_err!, ax_err_type!, current_ticks ...(+11)
- 参考项目：`arceos/modules/axnet/src/smoltcp_impl/bench.rs#bench_transmit_bandwidth`，调用摘要：(current_time - past_time).secs, AxNetTxToken::consume, InterfaceWrapper::current_time, info!, self.transmit, tx_buf[0..12].fill ...(+2)

#### 通用模块/其他
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `patches/axio/src/buffered/linewriter/shim.rs#write_all` | `arceos/ulib/axlibc/src/strftime.rs#write_all` | 同板块同名 | buf.split_at, memchr::memrchr, self.buffer.flush_buf, self.buffer.write_all, self.buffered, self.buffered().is_empty ...(+3) | Err, Ok, buf.len, self.inner.write_all |
| `patches/axio/src/read/mod.rs#read_until` | `arceos/ulib/axstd/src/io/stdio.rs#read_until` | 同板块同名 | Ok, available.len, buf.extend_from_slice, memchr::memchr, self.consume, self.fill_buf | self.inner.read_until |
| `patches/axplat-loongarch64-qemu-virt/src/time.rs#set_oneshot_timer` | `arceos/modules/axhal/src/platform/x86_pc/time.rs#set_oneshot_timer` | 同板块同名 | Self::current_ticks, Self::nanos_to_ticks, tcfg::set_init_val, ticks_deadline.saturating_sub, ticks_deadline.saturating_sub(ticks_now).max | NANOS_TO_LAPIC_TICKS_RATIO.mul_trunc, apic_ticks.max, assert!, crate::time::monotonic_time_nanos, lapic.set_timer_initial, super::apic::local_apic |
| `patches/axio/src/utils/take.rs#fill_buf` | `arceos/ulib/axstd/src/io/stdio.rs#fill_buf` | 同板块同名 | Ok, buf.len, cmp::min, self.inner.fill_buf | self.inner.fill_buf |


好的，我将遵循审计指令，基于提供的客观证据，对本次同源性定性仲裁进行深度析理。

### 1. 宏观判定
- **定性结论**: **独立开发 / 底层架构显著差异**
  - **客观相似度**: 4.01%
  - **共同 AST 指纹**: 211
  - **唯一 AST 指纹**: 目标项目 2659，参考项目 2820
  - **双向覆盖率**: 目标项目 7.94%，参考项目 7.48%
  - **判定摘要**: 双方项目的客观 AST 相似度极低，远未达到实质性代码复用的阈值。共同指纹占比不足 8%，且大量集中于通用语言节点与 POSIX 标准接口命名。双方在核心子系统上展现出截然不同的架构设计、对象模型与抽象层级，符合独立开发的典型特征。

### 2. 全模块覆盖矩阵解读
| 模块 | 双方存在 | 共同指纹 | 覆盖率 (目标/参考) | 证据强度 | 解读 |
| :--- | :---: | :---: | :---: | :---: | :--- |
| **进程管理** | 是 | 3 | 2.44% / 2.48% | **弱** | 覆盖率极低。重合点仅限于 `sys_getpid`、`sys_fork` 等 POSIX 强制命名的系统调用入口，以及 `is_clone_child` 这类仅含 `Some` 的极简函数，不构成实质性复用证据。 |
| **任务调度/异步运行时** | 是 | 23 | 6.46% / 7.67% | **弱** | 覆盖率低。共同指纹多源于 `axtask::yield_now`、`current` 等通用内核调度原语，以及 `switch_to_el1` 等 Arm 架构标准初始化序列，属于同领域同平台下的自然技术收敛。 |
| **内存管理** | 是 | 20 | 5.15% / 5.24% | **弱** | 覆盖率低。`sys_mmap` 等函数虽同名，但内部调用流差异巨大。目标项目使用 `rnd_bits`、`max_slide` 等复杂地址随机化逻辑，参考项目则无，表明实现路径独立。 |
| **中断/异常处理** | 是 | 12 | 21.43% / 25.53% | **中** | 覆盖率相对较高，但证据集中于 `patches/axcpu` 和 `arceos/modules/axhal` 目录。这些是直接 fork 或兼容自同一上游开源项目 (ArceOS) 的硬件抽象层代码，属于公共依赖或第三方库范畴，不能作为应用层抄袭的证据。 |
| **系统调用** | 是 | 0 | 0.0% / 0.0% | **未形成** | 双方虽均有系统调用模块，但未观察到任何共同 AST 指纹。这表明系统调用分发、参数处理和错误模型等核心逻辑完全不同。 |
| **文件系统** | 是 | 96 | 11.32% / 14.37% | **弱** | 共同指纹数量最多，但大量集中于 `patches/axfs-ng` 和 `arceos/modules/axfs-ng`，同样是基于 ArceOS 的 ext4 文件系统实现，属于第三方库/公共依赖。其余重合点如 `sys_close`、`sys_chdir` 等均为 POSIX 标准接口。 |
| **设备驱动** | 是 | 0 | 0.0% / 0.0% | **未形成** | 双方均有设备驱动模块，但未观察到任何共同 AST 指纹，表明驱动模型和实现完全不同。 |
| **网络协议栈** | 是 | 0 | 0.0% / 0.0% | **未形成** | 双方均有网络协议栈，但未观察到任何共同 AST 指纹。目标项目基于 `axnet-ng`，参考项目基于 `smoltcp`，架构迥异。 |
| **第三方库/公共依赖** | 是 | 16 | 41.03% / 2.96% | **弱** | 目标项目覆盖率高达 41.03%，因为其 `patches/axfs-ng` 等目录直接复用了大量第三方代码，与参考项目的重合也源于此。这属于对公共上游代码的引用，而非参赛作品之间的抄袭。 |

### 3. 底层 AST 关键证据指纹表
| 所属板块 | 目标项目路径 | 参考项目路径 | 证据类型 | 技术说明 |
| :--- | :--- | :--- | :--- | :--- |
| 第三方库/公共依赖 | `patches/axfs-ng/src/fs/ext4/inode.rs#metadata` | `arceos/modules/axfs-ng/src/fs/ext4/inode.rs#metadata` | 同板块同名同结构 | 两者 AST 结构完全一致，均调用 `DeviceId::default`, `FileAttr::default` 等。这是双方共同依赖的 ArceOS ext4 文件系统实现，属于公共库代码。 |
| 第三方库/公共依赖 | `patches/axfs-ng/src/fs/ext4/inode.rs#read_dir` | `arceos/modules/axfs-ng/src/fs/ext4/inode.rs#read_dir` | 同板块同名同结构 | 同上，属于 ArceOS 公共库代码。两者在 `core::str::from_utf8` 等调用上完全一致，仅错误类型 (`VfsError::InvalidData` vs `VfsError::EINVAL`) 有微小差异。 |
| 中断/异常处理 | `patches/axcpu/src/x86_64/asm.rs#enable_irqs` | `arceos/modules/axhal/src/arch/x86_64/mod.rs#enable_irqs` | 同板块同名同结构 | 两者均调用 `interrupts::enable` 和 `warn!`，结构完全一致。这是来自 ArceOS 硬件抽象层的公共代码。 |
| 进程管理 | `kernel/src/task/mod.rs#is_clone_child` | `core/src/process.rs#is_clone_child` | 同板块同名 | 两者外部调用图谱均仅含 `Some`。函数名源于 Linux 语义，逻辑过于简单，无法作为抄袭证据。 |
| 进程管理 | `kernel/src/syscall/task/thread.rs#sys_getppid` | `api/src/imp/task/thread.rs#sys_getppid` | 同板块同名 | 目标调用链为 `current().as_thread().proc_data.proc.parent()...`，参考为 `current_process().get_parent...`。两者访问进程数据模型的路径和抽象层级完全不同。 |
| 进程管理 | `kernel/src/syscall/task/wait.rs#apply` | `api/src/imp/task/wait.rs#apply` | 同板块同名 | 目标调用 `child.group().pgid`，参考调用 `child.get_group().get_pgid()`。前者是字段访问，后者是 getter 方法，API 设计不同。 |
| 内存管理 | `kernel/src/syscall/mm/mmap.rs#sys_mmap` | `api/src/imp/mm/mmap.rs#sys_mmap` | 同板块同名 | 目标项目包含 `rnd_bits`、`max_slide` 等地址随机化逻辑，参考项目则无。核心实现路径差异显著。 |
| 系统调用 | `kernel/src/syscall/mod.rs#handle_syscall` | `src/syscall.rs#handle_syscall` | 同板块同名 | 目标项目调用 `uctx.arg1()` 并返回 `LinuxError`，参考项目调用 `Sysno::new` 并返回 `err.code`。系统调用分发机制和错误模型完全不同。 |

### 4. 核心代码调用流对比剖析
* **【进程管理 / 信号掩码处理】**
* **底层技术析理**: 两者均实现了 POSIX 标准的 `rt_sigprocmask` 系统调用，但内部信号子系统的抽象与数据流截然不同。目标项目通过 `current().as_thread()` 获取线程结构，然后显式调用 `oldset.vm_write` 将数据写回用户空间，体现了更底层的用户内存操作模型。参考项目则通过 `current_thread_data().signal.with_blocked_mut` 的封装接口来操作信号掩码，隐藏了内存写入细节，抽象层级更高。这种在核心数据流和抽象粒度上的根本差异，排除了代码同源的可能性。
* **代码溯源链接**:
  - 目标项目: `kernel/src/syscall/signal.rs#sys_rt_sigprocmask`
  - 参考项目: `api/src/imp/task/signal.rs#sys_rt_sigprocmask`

* **【进程管理 / 进程退出】**
* **底层技术析理**: 目标项目的 `sys_exit` 和 `sys_exit_group` 直接调用内部函数 `do_exit`，调用流极短。而参考项目则统一调用 `sys_exit_impl`，该函数内部涉及 `SignalInfo::new`、`axtask::exit`、`close_all_file_like` 等复杂的资源清理与信号发送流程。目标项目的简洁分发与参考项目的重量级实现形成鲜明对比，表明两者在进程生命周期管理上采用了完全不同的策略和实现路径。
* **代码溯源链接**:
  - 目标项目: `kernel/src/syscall/task/exit.rs#sys_exit`
  - 参考项目: `api/src/interface/task/exit.rs#sys_exit`

* **【内存管理 / 内存映射】**
* **底层技术析理**: 在 `sys_mmap` 的实现中，目标项目包含了大量用于地址空间布局随机化 (ASLR) 的逻辑，如 `rnd_bits`、`max_slide` 的计算，显示出其对安全性的侧重。参考项目的 `sys_mmap` 实现则更侧重于标准的 `MmapFlags`、`MmapProt` 位标志处理和文件映射，未体现同等的 ASLR 复杂度。两者在核心功能实现上的侧重点和算法复杂度完全不同。
* **代码溯源链接**:
  - 目标项目: `kernel/src/syscall/mm/mmap.rs#sys_mmap`
  - 参考项目: `api/src/imp/mm/mmap.rs#sys_mmap`

### 5. 单方模块与降权项
*   **单方模块**:
    *   **网络协议栈/第三方库**: 仅参考项目体现，包含 `smoltcp_impl` 下的 `tcp.rs`、`dns.rs` 等。目标项目无此模块，其网络栈基于 `axnet-ng`。这证明双方网络子系统实现完全独立。
    *   **目标项目独有子系统**: 目标项目包含大量参考项目未体现的子系统，如 `io_uring` (`kernel/src/syscall/fs/io_uring.rs`)、进程会计 (`kernel/src/syscall/fs/acct.rs`)、网络命名空间 (`kernel/src/file/ns.rs`) 等，这些都是独立开发的痕迹。
*   **降权项**:
    *   **第三方库/公共依赖**: `patches/axfs-ng`、`patches/axcpu` 等目录下的代码与参考项目重合，属于双方共同依赖的 ArceOS 开源项目代码，已从核心指控中剥离。
    *   **跨板块证据**: 控方将 `process_arp` (网络协议栈)、`process_sqe` (IO 子系统) 等归入“进程管理”板块进行指控，属于跨板块硬凑，已降级为弱信号。
    *   **通用节点**: `Ok`、`Err`、`Some`、`Arc::new`、`format!` 等 Rust 语言标准库和通用宏，以及 `"proc".into` 等 POSIX 领域常量，不构成特异性指纹，已从强证据中剔除。

### 6. 保守结论
综合客观 AST 相似度 (4.01%)、极低的模块覆盖率以及核心调用流的架构差异分析，可以稳健地判定：参赛作品 `[2026-T202610486999578-XX]` 与参考项目 `[2025-undefined-清华大学]` 之间**不存在实质性代码抄袭或同源复用行为**。双方在进程管理、内存管理、系统调用分发等核心子系统上展现出截然不同的抽象层次、数据模型和实现路径。观察到的少量 AST 指纹重合，绝大部分可归因于对 ArceOS 等公共第三方库的共同依赖、POSIX 标准接口的强制命名以及 Rust 语言的通用惯用法。整体证据链充分支持“独立开发”的结论，风险极低。

------------------------------

