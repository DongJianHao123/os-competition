# 🛡️ OS 内核赛自动化代码级架构审计报告

**审计时间**: `2026-07-02 04:06:25`
**参赛作品名称**: `2026-T2026103369910066-XX`
**年份**: `2026`
**学校**: `XX`
**队伍名称**: `T2026103369910066`
**比赛名字**: `OS-Competition`
**比赛子赛道**: `Kernel-Implementation`
**仓库地址**: `https://gitlab.eduxiji.net/educg-group-43501-3132633/T2026103369910066-3514`
**推测开发语言**: `Rust / C (由AST探针自动识别)`
**特征函数量**: `1409`

## ⚔️ 底层代码同源性对比 (AST Homology)
> 本部分由专门的代码审查智能体，对该项目与符合年份条件的历届最高相似度项目进行控制流与底层调用对比。新版报告会先给出确定性的全模块覆盖矩阵，再给出 Agent 的分模块解释，避免只围绕少量抽样函数展开。

### 🏆 Top 1 底层对标: vs `2023-MoOS-哈尔滨工业大学深圳`（哈尔滨工业大学（深圳） / MoOS）

#### 对方作品信息

**对方年份**: `2023`
**对方学校**: `哈尔滨工业大学（深圳）`
**对方队伍名称**: `MoOS`
**对方比赛名字**: `OS-Competition`
**对方比赛子赛道**: `Kernel-Implementation`
**对方仓库地址**: `https://gitlab.eduxiji.net/202318123101332/OSKernel2023-MoOS`

> 双向覆盖率：共同 AST 指纹: 309；唯一 AST 指纹: 目标项目 906，参考项目 929；覆盖率: 目标项目 34.11%，参考项目 33.26%

## 全模块对比总览（确定性证据）

> 总体覆盖率：共同 AST 指纹: 309；唯一 AST 指纹: 目标项目 906，参考项目 929；覆盖率: 目标项目 34.11%，参考项目 33.26%

### 1. 模块覆盖矩阵
| 模块 | 目标函数数 | 参考函数数 | 共同唯一 AST 指纹 | Target 覆盖 | Base 覆盖 | 状态 |
| :--- | ---: | ---: | ---: | ---: | ---: | :--- |
| 进程管理 | 128 | 12 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |
| 任务调度/异步运行时 | 119 | 61 | 1 | 0.85% | 1.69% | 双方存在且有结构重合 |
| 内存管理 | 188 | 107 | 61 | 35.06% | 64.89% | 双方存在且有结构重合 |
| 中断/异常处理 | 5 | 4 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |
| 系统调用 | 4 | 47 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |
| 文件系统 | 192 | 141 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |
| 设备驱动 | 8 | 0 | 0 | 0.0% | 0.0% | 仅目标项目体现 |
| 网络协议栈 | 0 | 0 | 0 | 0.0% | 0.0% | 双方均未体现 |
| 文件系统/第三方库 | 14 | 67 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |
| 第三方库 | 709 | 1058 | 240 | 89.55% | 43.56% | 双方存在且有结构重合 |
| 通用模块/其他 | 42 | 27 | 6 | 15.0% | 24.0% | 双方存在且有结构重合 |

### 2. 分模块证据清单

#### 进程管理
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `os/src/syscall/process.rs#sys_exec` | `os/kernel/src/syscall/process.rs#sys_exec` | 同板块同名 | Some, Vec::new, all_data.as_slice, all_data.is_empty, all_data.iter, all_data.iter().position ...(+55) | "/busybox".to_string, "busybox".to_string, "sh".to_string, ElfFile::new, GLOBAL_DENTRY_CACHE.get, Vec::new ...(+15) |
| `os/src/syscall/process.rs#sys_waitpid` | `os/kernel/src/syscall/process.rs#sys_waitpid` | 同板块同名 | block_current_and_run_next, child.getpid, child.inner_exclusive_access, current_process, drop, exit_code_ptr.is_null ...(+11) | Thread::async_yield, children.first_key_value, children.first_key_value().unwrap, children.get, children.is_empty, children.remove ...(+10) |
| `os/src/syscall/process.rs#sys_exit` | `os/kernel/src/syscall/process.rs#sys_exit` | 同板块同名 | crate::syscall::fs::release_process_file_locks, current_process, current_process().getpid, exit_current_and_run_next, panic!, trace! | nuclear.inner.lock, println!, proc.parent.as_ref, self.inner.exclusive_access, self.proc.inner.lock, shutdown ...(+1) |
| `os/src/syscall/process.rs#sys_getppid` | `os/kernel/src/syscall/process.rs#sys_getppid` | 同板块同名 | current_process, p.getpid, parent.upgrade, proc.inner_exclusive_access | self.proc.inner.lock, self.proc.inner.lock().parent.as_ref, self.proc.inner.lock().parent.as_ref().unwrap |

#### 任务调度/异步运行时
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `os/src/syscall/mod.rs#syscall` | `os/kernel/src/syscall/mod.rs#syscall` | 同板块同名 | sys_accept, sys_bind, sys_chdir, sys_clock_getres, sys_clock_gettime, sys_clock_nanosleep ...(+115) | Thread::async_yield, Thread::sys_mount, Thread::sys_munmap, Thread::sys_nanosleep, Thread::sys_uname, UserPtr::<isize,Out>::from_usize ...(+33) |
| `os/src/syscall/fs.rs#sys_fcntl` | `os/kernel/src/syscall/mm.rs#sys_fcntl` | 同板块同名 | Arc::clone, FILE_LOCKS.exclusive_access, Some, check_user_range, core::mem::size_of::<Flock>, crate::task::suspend_current_and_run_next ...(+38) | fd_manager.dup, fd_manager.fd_array[fd].lock, fd_manager.fd_array[fd].lock().set_close_on_exec, println!, self.proc.inner.lock |
| `os/src/syscall/fs.rs#sys_chdir` | `os/kernel/src/syscall/fs.rs#sys_chdir` | 同板块同名 | check_search_permission, check_user_cstr_readable, current_process, current_process().inner_exclusive_access, current_user_token, effective_permission_bits ...(+9) | String::from, current_dir.clone, path.split, self.proc.inner.lock, task.cwd.clone, task.memory_set.token ...(+1) |
| `os/src/syscall/fs.rs#sys_getcwd` | `os/kernel/src/syscall/fs.rs#sys_getcwd` | 同板块同名 | (slice.as_ptr() as *mut u8).add, (slice.as_ptr() as *mut u8).add(off).write, buf.is_null, bytes.iter, bytes.iter().copied, bytes.iter().copied().chain ...(+13) | cwd.as_str, cwd_str.as_bytes, cwd_str.as_bytes().iter, cwd_str.as_bytes().iter().enumerate, cwd_str.len, self.proc.inner.lock ...(+3) |

#### 内存管理
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `os/src/syscall/process.rs#sys_mmap` | `os/kernel/src/syscall/mm.rs#sys_mmap` | 同板块同名 | (data.len().saturating_sub(data_pos)).min, (slice.as_ptr() as *mut u8).add, Some, VirtAddr::from, _len
        .checked_add, _len
        .checked_add(PAGE_SIZE - 1)
        .map ...(+38) | (startva + len).into, (startva+len).into, MapArea::new, Some, pcb.deref_mut, pcb.fd_manager.fd_array[fd]
						.lock ...(+9) |
| `os/src/mm/memory_set.rs#new_kernel` | `os/kernel/src/mm/memory_set.rs#new_kernel` | 同板块同名 | ((*pair).0 + (*pair).1).into, (*pair).0.into, MEMORY_END.into, MapArea::new, Self::new_bare, ebss.into ...(+11) | ((*pair).0 + (*pair).1).into, (*pair).0.into, (ebss as usize).into, (edata as usize).into, (ekernel as usize).into, (erodata as usize).into ...(+11) |
| `easy-fs/src/bitmap.rs#alloc` | `os/kernel/src/mm/frame_allocator.rs#alloc` | 同板块同名 | Arc::clone, Some, bitmap_block
                    .iter, bitmap_block
                    .iter()
                    .enumerate, bitmap_block
                    .iter()
                    .enumerate()
                    .find, bitmap_block
                    .iter()
                    .enumerate()
                    .find(|(_, bits64)| **bits64 != u64::MAX)
                    .map ...(+5) | (self.current - 1).into, Some, ppn.into, println!, self.recycled.pop |
| `os/src/mm/page_table.rs#translated_byte_buffer` | `os/kernel/src/mm/page_table.rs#translated_byte_buffer` | 同板块同名 | PageTable::from_token, Vec::new, VirtAddr::from, end_va.into, end_va.min, end_va.page_offset ...(+8) | PageTable::from_token, Vec::new, VirtAddr::from, end_va.into, end_va.min, end_va.page_offset ...(+9) |

#### 中断/异常处理
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `os/src/trap/mod.rs#trap_from_kernel` | `os/kernel/src/trap/mod.rs#trap_from_kernel` | 同板块同名 | panic! | panic! |
| `os/src/trap/mod.rs#set_user_trap_entry` | `os/kernel/src/trap/mod.rs#set_user_trap` | 同模块上下文对齐/差异观察 | stvec::write | stvec::write |
| `os/src/trap/mod.rs#enable_timer_interrupt` | `os/kernel/src/trap/mod.rs#init` | 同模块上下文对齐/差异观察 | sie::set_stimer | stvec::write |

#### 系统调用
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `os/src/syscall/sync.rs#futex_timespec_us` | `os/kernel/src/syscall/interrupt.rs#sys_uname` | 同模块上下文对齐/差异观察 | Some, ts.nsec.saturating_add, ts.sec.saturating_mul, ts.sec.saturating_mul(1_000_000).saturating_add | utsname::new |
| `os/src/syscall/sync.rs#read_futex_word` | `os/kernel/src/syscall/interrupt.rs#new` | 同模块上下文对齐/差异观察 | Some, core::mem::size_of::<i32>, translated_ref | domainname.len, domainname_arr[..domainname.len()].copy_from_slice, machine.len, machine_arr[..machine.len()].copy_from_slice, nodename.len, nodename_arr[..nodename.len()].copy_from_slice ...(+6) |
| `os/src/syscall/sync.rs#sys_enable_deadlock_detect` | `os/kernel/src/syscall/interrupt.rs#sys_nanosleep` | 同模块上下文对齐/差异观察 | trace! | Thread::async_yield, get_time_us |
| `os/src/syscall/sync.rs#us_to_timer_ms` | `os/user_c/src/lib/syscall.c#get_time` | 同模块上下文对齐/差异观察 | deadline_us.saturating_add | sys_get_time |

#### 文件系统
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `easy-fs/src/layout.rs#read_at` | `os/kernel/src/fs/dev/mod.rs#read_at` | 同板块同名 | (offset + buf.len()).min, Arc::clone, buf.len, dst.copy_from_slice, end_current_block.min, get_block_cache ...(+3) | Ok, assert!, buf.copy_from_slice, buf.len, range.is_full, range.origin_begin ...(+3) |
| `easy-fs/src/layout.rs#write_at` | `os/kernel/src/fs/dev/mod.rs#write_at` | 同板块同名 | (offset + buf.len()).min, Arc::clone, assert!, buf.len, dst.copy_from_slice, end_current_block.min ...(+4) | Ok, assert!, block_buf[range.begin..range.end].copy_from_slice, buf.len, range.is_full, range.origin_begin ...(+3) |
| `os/src/syscall/fs.rs#sys_mount` | `os/kernel/src/syscall/fs.rs#sys_mount` | 同板块同名 | MOUNT_TABLE.exclusive_access, String::new, current_user_token, dir.is_null, fstype.is_empty, fstype.is_null ...(+10) | "/".to_string, Thread::full_search_mount, Volume::new, volume.root_dir |
| `os/src/syscall/fs.rs#sys_readv` | `os/kernel/src/syscall/fs.rs#sys_readv` | 同板块同名 | current_user_token, iov.add, iov.is_null, sys_read, translated_ref | (iov as *const usize).add, self.sys_read |

#### 设备驱动
仅目标项目中观察到该模块代表函数，参考项目没有同模块可对齐证据：
- 目标项目：`os/src/drivers/block/virtio_blk.rs#read_block`，调用摘要：READ_BOUNCE_BUFFER.exclusive_access, bounce.0.fill, buf.len, buf.len().min, buf[..n].copy_from_slice, buf[..n].fill ...(+3)
- 目标项目：`os/src/drivers/block/mod.rs#block_device_test`，调用摘要：BLOCK_DEVICE.clone, assert_eq!, block_device.read_block, block_device.write_block, println!, write_buffer.iter_mut
- 目标项目：`os/src/sbi.rs#console_getchar`，调用摘要：((UART0 + UART_REG_LSR) as *const u8).read_volatile, ((UART0 + UART_REG_RBR) as *const u8).read_volatile
- 目标项目：`os/src/drivers/block/virtio_blk.rs#write_block`，调用摘要：WRITE_BOUNCE_BUFFER.exclusive_access, bounce.0.fill, bounce.0[..n].copy_from_slice, buf.len, buf.len().min, self
                .0
                .exclusive_access ...(+2)

#### 文件系统/第三方库
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `os/src/fs/ext4.rs#ext4_list_dir` | `os/kernel/dependencies/fat32/src/dir.rs#create` | 同模块上下文对齐/差异观察 | EXT4_DIR_CACHE
                .exclusive_access, EXT4_DIR_CACHE
                .exclusive_access()
                .insert, EXT4_DIR_CACHE
        .exclusive_access, EXT4_DIR_CACHE
        .exclusive_access()
        .get, EXT4_DIR_CACHE
        .exclusive_access()
        .get(cache_key.as_str())
        .cloned, EXT4_DIR_CACHE.exclusive_access ...(+16) | "unsupported".as_bytes, (1..count).rev, DirectoryItem::new_lfn, DirectoryItem::new_sfn, DirectoryItem::new_sfn_bytes, Err ...(+12) |
| `os/src/fs/ext4.rs#ext4_metadata` | `os/kernel/dependencies/fat32/src/directory_item.rs#to_utf8` | 同模块上下文对齐/差异观察 | EXT4_METADATA_CACHE
                .exclusive_access, EXT4_METADATA_CACHE
                .exclusive_access()
                .insert, EXT4_METADATA_CACHE
        .exclusive_access, EXT4_METADATA_CACHE
        .exclusive_access()
        .get, EXT4_METADATA_CACHE
        .exclusive_access()
        .get(cache_key.as_str())
        .cloned, EXT4_METADATA_CACHE
        .exclusive_access()
        .insert ...(+12) | (0..part.len()).step_by, op, part.len |
| `os/src/fs/ext4.rs#path_variants` | `os/kernel/dependencies/fat32/src/dir.rs#delete` | 同模块上下文对齐/差异观察 | String::from, Vec::new, format!, norm, normalized
        .strip_prefix, normalized
        .strip_prefix('/')
        .map ...(+6) | DirIter::new, Err, Ok, di.cluster, di.is_dir, di.is_file ...(+9) |
| `os/src/fs/ext4.rs#read` | `os/kernel/dependencies/fat32/src/directory_item.rs#new` | 同模块上下文对齐/差异观察 | (dst.len() - pos).min, Box::new, EXT4_BOUNCE_BUFFER.exclusive_access, Err, Ok, dst.len ...(+2) | ShortDirectoryItem::from_buf, cluster.reverse, cluster.to_be_bytes, extension.as_bytes, extension.len, item[0x00..0x00 + name.len()].copy_from_slice ...(+9) |

#### 第三方库
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `third_party/riscv/descriptor/generator.rs#generate` | `os/kernel/dependencies/riscv/descriptor/generator.rs#generate` | 同板块同名同结构 | String::new, bf.generate_enum, format!, self.bfs.iter, write!, write!(&mut bit_sets, "{}", bf.generate_bitops()).unwrap ...(+2) | String::new, bf.generate_enum, format!, self.bfs.iter, write!, write!(&mut bit_sets, "{}", bf.generate_bitops()).unwrap ...(+2) |
| `third_party/riscv/src/paging/multi_level.rs#create_p1_if_not_exist` | `os/kernel/dependencies/riscv/src/paging/multi_level.rs#create_p1_if_not_exist` | 同板块同名同结构 | Ok, allocator.alloc, allocator.alloc().ok_or, frame.as_kernel_mut, frame.clone, p1_table.zero ...(+11) | Ok, allocator.alloc, allocator.alloc().ok_or, frame.as_kernel_mut, frame.clone, p1_table.zero ...(+11) |
| `third_party/virtio-drivers/src/queue.rs#add` | `os/kernel/dependencies/virtio-drivers/src/queue.rs#add` | 同板块同名 | Err, Ok, desc.flags.read, desc.flags.write, desc.next.read, desc.set_buf::<H> ...(+11) | Err, Ok, desc.flags.read, desc.flags.write, desc.next.read, desc.set_buf ...(+11) |
| `third_party/riscv/src/paging/multi_level.rs#unmap` | `os/kernel/dependencies/riscv/src/paging/multi_level.rs#unmap` | 同板块同名同结构 | Err, Ok, Self::MapperFlush::new, p1_entry.flags, p1_entry.flags().contains, p1_entry.frame::<PhysAddrSv48> ...(+14) | Err, Ok, Self::MapperFlush::new, p1_entry.flags, p1_entry.flags().contains, p1_entry.frame::<PhysAddrSv48> ...(+14) |

#### 通用模块/其他
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `os/src/sbi.rs#sbi_call` | `os/kernel/src/sbi.rs#sbi_call` | 同板块同名 | asm! | asm! |
| `os/src/timer.rs#get_time_us` | `os/kernel/src/timer.rs#get_time_us` | 同板块同名 | LAST_TIME_US.exclusive_access, time::read | time::read |
| `os/src/main.rs#clear_bss` | `os/kernel/src/main.rs#clear_bss` | 同板块同名 | core::slice::from_raw_parts_mut, core::slice::from_raw_parts_mut(start as *mut u8, end - start).fill | core::slice::from_raw_parts_mut, core::slice::from_raw_parts_mut(sbss as usize as *mut u8, ebss as usize - sbss as usize)
            .fill |
| `os/src/console.rs#write_str` | `os/kernel/src/console.rs#write_str` | 同板块同名同结构 | Ok, console_putchar, s.chars | Ok, console_putchar, s.chars |


### 1. 宏观判定
- **定性结论**: 独立开发 (相似度: 20.25%，共同 AST 指纹: 309；唯一 AST 指纹: 目标项目 906，参考项目 929；覆盖率: 目标项目 34.11%，参考项目 33.26%)

### 2. 全模块覆盖矩阵解读
- **进程管理**: 双方均存在该模块。共同 AST 指纹为 0，目标覆盖率 0.0%，参考覆盖率 0.0%。双方在进程创建、信号处理、资源回收等核心路径上无任何底层结构重合。证据强度：未形成。
- **任务调度/异步运行时**: 双方均存在该模块。共同 AST 指纹为 1，目标覆盖率 0.85%，参考覆盖率 1.69%。唯一的共同指纹源于 Rust 标准库 `VecDeque` 的通用操作，不构成特异性证据。证据强度：弱。
- **内存管理**: 双方均存在该模块。共同 AST 指纹为 61，目标覆盖率 35.06%，参考覆盖率 64.89%。重叠集中于 RISC-V Sv39 页表的标准操作与 rCore 教学范式的通用例程，属于同领域同教学体系下的必然共性。证据强度：中。
- **中断/异常处理**: 双方均存在该模块。共同 AST 指纹为 0，目标覆盖率 0.0%，参考覆盖率 0.0%。除 RISC-V 架构强制的 `stvec::write` 等寄存器操作外，无任何逻辑级同构。证据强度：未形成。
- **系统调用**: 双方均存在该模块。共同 AST 指纹为 0，目标覆盖率 0.0%，参考覆盖率 0.0%。系统调用分发与实现路径完全不同。证据强度：未形成。
- **文件系统**: 双方均存在该模块。共同 AST 指纹为 0，目标覆盖率 0.0%，参考覆盖率 0.0%。目标项目采用内存文件系统与 ext4 支持，参考项目依赖 FAT32 与自定义 Dentry Cache，底层对象模型截然不同。证据强度：未形成。
- **设备驱动**: 仅目标项目存在该模块（8个函数），参考项目无。目标项目独立实现了 VirtIO 块设备驱动与 UART 直接寄存器操作。证据强度：未形成（单方模块）。
- **网络协议栈**: 双方均未体现该模块。
- **第三方库/公共依赖**: 双方均存在该模块。共同 AST 指纹为 240，目标覆盖率 89.55%，参考覆盖率 43.56%。重叠文件均为上游开源 crate（如 `riscv`、`virtio-drivers`）的 vendored 副本，属于正常的公共依赖复用。证据强度：弱（已降权）。

### 3. 底层 AST 关键证据指纹表
| 所属板块 | 目标项目路径 | 参考项目路径 | 证据类型 | 技术说明 |
| :--- | :--- | :--- | :--- | :--- |
| 内存管理 | os/src/mm/page_table.rs#translated_byte_buffer | os/kernel/src/mm/page_table.rs#translated_byte_buffer | 同板块同名 | 均为 RISC-V Sv39 页表标准实现，调用 `PageTable::from_token`、`VirtAddr::from` 等固定 API，属于公开教学范式的共性。 |
| 内存管理 | os/src/mm/memory_set.rs#new_kernel | os/kernel/src/mm/memory_set.rs#new_kernel | 同板块同名 | 内核内存映射初始化，均遵循 rCore 教程的 `MapArea::new` 与段边界符号（`stext`, `etext` 等）映射模式。 |
| 内存管理 | easy-fs/src/bitmap.rs#alloc | os/kernel/src/mm/frame_allocator.rs#alloc | 同板块同名 | 虽同名但实现异构。目标采用位图迭代查找，参考采用栈式回收（`recycled.pop`），分配策略完全不同。 |
| 任务调度/异步运行时 | os/src/task/manager.rs#fetch | os/kernel/src/task/mod.rs#fetch | 同板块同名 | 均使用 `pop_front` 从队列取任务，这是 Rust `VecDeque` 的标准用法，非作者特异性指纹。 |
| 任务调度/异步运行时 | os/src/task/manager.rs#remove | os/kernel/src/task/mod.rs#remove | 同板块同名 | 均使用 `iter().enumerate().find().remove()` 链式操作，属于 Rust 语言级惯用法，非项目独创。 |
| 中断/异常处理 | os/src/trap/mod.rs#trap_from_kernel | os/kernel/src/trap/mod.rs#trap_from_kernel | 同板块同名 | 均调用 `panic!`，这是 Rust 语言处理不可恢复错误的标准宏，无特异性。 |
| 第三方库 | third_party/riscv/src/paging/multi_level.rs#unmap | os/kernel/dependencies/riscv/src/paging/multi_level.rs#unmap | 同板块同名同结构 | 完全相同的上游 `riscv` crate 副本，属于公共依赖，不作为项目间抄袭证据。 |
| 第三方库 | third_party/virtio-drivers/src/queue.rs#add | os/kernel/dependencies/virtio-drivers/src/queue.rs#add | 同板块同名 | 完全相同的上游 `virtio-drivers` crate 副本，属于公共依赖，不作为项目间抄袭证据。 |

### 4. 核心代码调用流对比剖析
* **【进程管理/sys_exec 系统调用路径】**
* **底层技术析理**: 目标项目通过 `app_inode.read_all` 将程序数据读入 `Vec<u8>` 后解析，而参考项目直接使用 `ElfFile::new` 解析 ELF 文件。两者在程序加载的抽象层次上存在根本差异：目标面向原始字节流，参考面向结构化 ELF 对象。此外，目标项目参数处理使用 `all_data.iter().position` 查找分隔符，参考项目则直接向 `argvs` 插入已知字符串，逻辑流完全不同。
* **代码溯源链接**:
  - 目标项目: `os/src/syscall/process.rs#sys_exec`
  - 参考项目: `os/kernel/src/syscall/process.rs#sys_exec`

* **【文件系统/sys_read 系统调用路径】**
* **底层技术析理**: 目标项目构建了 `UserBuffer` 抽象层来管理用户态缓冲区，并通过 `file.read` 接口进行读取。参考项目则直接操作 `inode.lock().read_at`，并依赖 `translated_byte_buffer` 进行地址转换。两者在 I/O 路径上的内存模型完全不同：目标采用封装好的 Buffer 对象，参考采用更底层的页表翻译与 Inode 直接操作。
* **代码溯源链接**:
  - 目标项目: `os/src/syscall/fs.rs#sys_read`
  - 参考项目: `os/kernel/src/syscall/fs.rs#sys_read`

* **【任务调度/异步运行时/sys_fcntl 系统调用路径】**
* **底层技术析理**: 此函数在双方项目中虽同名，但功能和所在模块完全不同。目标项目的 `sys_fcntl` 位于 `fs.rs`，核心是实现文件锁（`Flock`），调用 `FILE_LOCKS` 等对象。参考项目的 `sys_fcntl` 位于 `mm.rs`，核心是实现文件描述符复制（`dup`）和 `close_on_exec` 标志位设置。这是典型的“同名异构”，控方将其作为同板块证据属于跨板块硬凑。
* **代码溯源链接**:
  - 目标项目: `os/src/syscall/fs.rs#sys_fcntl`
  - 参考项目: `os/kernel/src/syscall/mm.rs#sys_fcntl`

### 5. 单方模块与降权项
- **设备驱动模块**: 仅目标项目拥有，包含 `virtio_blk.rs` 等独立实现的驱动，证明其具备独立开发硬件抽象层的能力。
- **第三方库/公共依赖**: 双方 `third_party` 和 `dependencies` 目录下的重叠文件（如 `riscv`、`virtio-drivers`）均为公开上游 crate 的本地副本。这是正常且合法的软件依赖管理方式，已从核心同源证据中降权。
- **POSIX 同名系统调用**: `sys_read`、`sys_write`、`sys_close` 等函数名由 POSIX 标准和 Linux ABI 强制规定，不能作为代码复用的证据。真正的鉴别点在于其内部实现所调用的对象，而证据显示双方在这些调用流上截然不同。
- **Rust 语言惯用法**: `Vec::new`、`Arc::clone`、`pop_front`、`iter().enumerate().find().remove()` 等是 Rust 标准库的通用模式，出现在任何 Rust 项目中均属正常，已从特异性指纹中降权。

### 6. 保守结论
本次审计基于底层 AST 调用图谱的客观比对，双方项目总体相似度仅 20.25%，且在进程管理、文件系统、系统调用等核心模块的共同 AST 指纹均为 0。现有证据清晰地指向两个项目在内存模型、同步策略、文件系统架构和驱动实现上存在根本性差异。观察到的少量重叠主要源于共同的 RISC-V 硬件规范、rCore 教学体系影响、POSIX 标准接口命名以及合法的第三方开源库复用。综合判定，目标项目 `[2026-T2026103369910066-XX]` 相对于参考项目 `[2023-MoOS-哈尔滨工业大学深圳]` 构成独立开发，不存在足以定性的代码抄袭或核心逻辑窃取行为。

------------------------------

