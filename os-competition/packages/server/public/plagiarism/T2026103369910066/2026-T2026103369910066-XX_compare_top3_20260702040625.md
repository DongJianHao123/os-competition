# 🛡️ OS 内核赛自动化代码级架构审计报告

**审计时间**: `2026-07-02 04:06:25`
**参赛作品名称**: `2026-T2026103369910066-XX`
**年份**: `2026`
**学校**: `XX`
**队伍名称**: `T2026103369910066`
**比赛名字**: `OS-Competition`
**比赛子赛道**: `Kernel-Implementation`
**仓库地址**: `https://gitlab.eduxiji.net/educg-group-43501-3132633/T2026103369910066-3514`
**推测开发语言**: `Rust / C (由AST探针自动识别)`
**特征函数量**: `1409`

## ⚔️ 底层代码同源性对比 (AST Homology)
> 本部分由专门的代码审查智能体，对该项目与符合年份条件的历届最高相似度项目进行控制流与底层调用对比。新版报告会先给出确定性的全模块覆盖矩阵，再给出 Agent 的分模块解释，避免只围绕少量抽样函数展开。

### 🏆 Top 3 底层对标: vs `2023-Titanix-哈尔滨工业大学深圳`（哈尔滨工业大学（深圳） / Titanix）

#### 对方作品信息

**对方年份**: `2023`
**对方学校**: `哈尔滨工业大学（深圳）`
**对方队伍名称**: `Titanix`
**对方比赛名字**: `OS-Competition`
**对方比赛子赛道**: `Kernel-Implementation`
**对方仓库地址**: `https://gitlab.eduxiji.net/202318123101314/oskernel2023-Titanix`

> 双向覆盖率：共同 AST 指纹: 323；唯一 AST 指纹: 目标项目 906，参考项目 1397；覆盖率: 目标项目 35.65%，参考项目 23.12%

## 全模块对比总览（确定性证据）

> 总体覆盖率：共同 AST 指纹: 323；唯一 AST 指纹: 目标项目 906，参考项目 1397；覆盖率: 目标项目 35.65%，参考项目 23.12%

### 1. 模块覆盖矩阵
| 模块 | 目标函数数 | 参考函数数 | 共同唯一 AST 指纹 | Target 覆盖 | Base 覆盖 | 状态 |
| :--- | ---: | ---: | ---: | ---: | ---: | :--- |
| 进程管理 | 128 | 187 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |
| 任务调度/异步运行时 | 119 | 132 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |
| 内存管理 | 188 | 273 | 43 | 24.71% | 16.73% | 双方存在且有结构重合 |
| 中断/异常处理 | 5 | 28 | 3 | 60.0% | 11.11% | 双方存在且有结构重合 |
| 系统调用 | 4 | 88 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |
| 文件系统 | 192 | 328 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |
| 设备驱动 | 8 | 92 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |
| 网络协议栈 | 0 | 46 | 0 | 0.0% | 0.0% | 仅参考项目体现 |
| 文件系统/第三方库 | 14 | 0 | 0 | 0.0% | 0.0% | 仅目标项目体现 |
| 第三方库 | 709 | 710 | 268 | 100.0% | 99.63% | 双方存在且有结构重合 |
| 通用模块/其他 | 42 | 151 | 6 | 15.0% | 4.32% | 双方存在且有结构重合 |

### 2. 分模块证据清单

#### 进程管理
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `os/src/syscall/process.rs#sys_clone` | `kernel/src/syscall/process.rs#sys_clone` | 同板块同名 | Arc::clone, Arc::new, Some, TaskControlBlock::new, add_task, current_process ...(+25) | CloneFlags::from_bits, Err, Ok, Some, clone_flags.contains, clone_flags.is_none ...(+13) |
| `os/src/syscall/process.rs#sys_rt_sigprocmask` | `kernel/src/syscall/signal.rs#sys_rt_sigprocmask` | 同板块同名 | current_process, current_user_token, process.inner_exclusive_access, size_of::<usize>, translated_ref, translated_refmut | Err, Ok, SigSet::from_bits, SigSet::from_bits(*set as usize).ok_or, SumGuard::new, UserCheck::new ...(+9) |
| `os/src/syscall/process.rs#sys_setpgid` | `kernel/src/syscall/process.rs#sys_setpgid` | 同板块同名 | child.getpid, current.getpid, current.inner_exclusive_access, current_process, inner
            .children
            .iter, inner
            .children
            .iter()
            .find ...(+2) | Err, Ok, PROCESS_GROUP_MANAGER.set_pgid_by_pid, PROCESS_MANAGER.get, SumGuard::new, current_process ...(+8) |
| `os/src/syscall/process.rs#sys_getpgid` | `kernel/src/syscall/process.rs#sys_getpgid` | 同板块同名 | current.getpid, current.inner_exclusive_access, current_process, pid2process, process.inner_exclusive_access | Err, Ok, PROCESS_MANAGER.get, SumGuard::new, current_process, current_process().pgid ...(+5) |

#### 任务调度/异步运行时
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `os/src/syscall/mod.rs#syscall` | `kernel/src/syscall/mod.rs#syscall` | 同板块同名 | sys_accept, sys_bind, sys_chdir, sys_clock_getres, sys_clock_gettime, sys_clock_nanosleep ...(+115) | Ok, error!, sys_handler! |
| `os/src/syscall/fs.rs#sys_ioctl` | `kernel/src/syscall/dev.rs#sys_ioctl` | 同板块同名 | LOOP_DEVICES
                .exclusive_access, LOOP_DEVICES
                .exclusive_access()
                .get, LOOP_DEVICES
                .exclusive_access()
                .get(&index)
                .filter, LOOP_DEVICES
                .exclusive_access()
                .get(&index)
                .filter(|state| state.attached)
                .map, LOOP_DEVICES
                .exclusive_access()
                .get(&index)
                .filter(|state| state.attached)
                .map(|state| state.size)
                .unwrap_or, LOOP_DEVICES
                .exclusive_access()
                .get(&index)
                .map ...(+25) | Err, SumGuard::new, current_process, current_process()
        .inner_handler, current_process()
        .inner_handler(move |proc| proc.fd_table.get_ref(fd).cloned())
        .ok_or, debug! ...(+7) |
| `os/src/syscall/sync.rs#sys_futex` | `kernel/src/syscall/sync.rs#sys_futex` | 同板块同名 | Arc::clone, FUTEX_WAITERS.exclusive_access, Some, VirtAddr::from, add_timer, block_current_and_run_next ...(+18) | Duration::from, Err, FutexFuture::new, Ok, Some, SumGuard::new ...(+20) |
| `os/src/syscall/fs.rs#sys_recvfrom` | `kernel/src/syscall/net.rs#sys_recvfrom` | 同板块同名 | (n - pos).min, SOCKETS.exclusive_access, buf.is_null, core::ptr::copy_nonoverlapping, current_process, current_user_token ...(+15) | Ok, SumGuard::new, UserCheck::new, UserCheck::new().check_writable_slice, core::slice::from_raw_parts_mut, current_process ...(+13) |

#### 内存管理
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `os/src/syscall/process.rs#sys_mmap` | `kernel/src/syscall/mm.rs#sys_mmap` | 同板块同名 | (data.len().saturating_sub(data_pos)).min, (slice.as_ptr() as *mut u8).add, Some, VirtAddr::from, _len
        .checked_add, _len
        .checked_add(PAGE_SIZE - 1)
        .map ...(+38) | Err, MmapFlags::from_bits, MmapFlags::from_bits(flags as u32).ok_or, MmapProt::from_bits, MmapProt::from_bits(prot as u32).ok_or, Ok ...(+25) |
| `os/src/trap/mod.rs#trap_handler` | `kernel/src/trap/user_trap.rs#trap_handler` | 同板块同名 | check_process_alarms, check_signals_of_current, check_timer, current_add_signal, current_process, current_process()
                    .inner_exclusive_access ...(+16) | IRQ_COUNTER.add1, K_COVERAGE.commit, VirtAddr::from, crate::driver::intr_handler, current_task, current_task().recv_signal ...(+23) |
| `os/src/mm/memory_set.rs#new_kernel` | `kernel/src/mm/memory_space/mod.rs#new_kernel` | 同板块同名 | ((*pair).0 + (*pair).1).into, (*pair).0.into, MEMORY_END.into, MapArea::new, Self::new_bare, ebss.into ...(+11) | ((*pair).0 + (*pair).1 + (KERNEL_DIRECT_OFFSET << PAGE_SIZE_BITS)).into, ((*pair).0 + (KERNEL_DIRECT_OFFSET << PAGE_SIZE_BITS)).into, (ebss as usize).into, (edata as usize).into, (ekernel as usize).into, (erodata as usize).into ...(+16) |
| `os/src/mm/memory_set.rs#from_existed_user` | `kernel/src/mm/memory_space/mod.rs#from_existed_user` | 同板块同名 | Arc::clone, Arc::new, MapArea::from_another, PTEFlags::from_bits, PTEFlags::from_bits(area.map_perm.bits).unwrap, Self::new_bare ...(+14) | Self::new_from_global, VmArea::from_another, dst_ppn.bytes_array, dst_ppn.bytes_array().copy_from_slice, memory_space.page_table.clone, memory_space.push_lazily ...(+7) |

#### 中断/异常处理
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `os/src/trap/mod.rs#set_kernel_trap_entry` | `kernel/src/trap/mod.rs#set_kernel_trap_entry` | 同板块同名 | stvec::write | stvec::write |
| `os/src/trap/mod.rs#set_user_trap_entry` | `kernel/src/trap/mod.rs#set_user_trap_entry` | 同板块同名同结构 | stvec::write | stvec::write |
| `os/src/trap/mod.rs#enable_timer_interrupt` | `kernel/src/trap/mod.rs#enable_timer_interrupt` | 同板块同名同结构 | sie::set_stimer | sie::set_stimer |
| `os/src/trap/mod.rs#init` | `kernel/src/trap/mod.rs#init` | 同板块同名同结构 | set_kernel_trap_entry | set_kernel_trap_entry |

#### 系统调用
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `os/src/syscall/sync.rs#futex_timespec_us` | `kernel/src/net/udp.rs#connect` | 同模块上下文对齐/差异观察 | Some, ts.nsec.saturating_add, ts.sec.saturating_mul, ts.sec.saturating_mul(1_000_000).saturating_add | Box::pin, Err, IpListenEndpoint::from, NET_INTERFACE.poll, NET_INTERFACE.udp_socket, Ok ...(+13) |
| `os/src/syscall/sync.rs#read_futex_word` | `kernel/src/net/tcp.rs#connect` | 同模块上下文对齐/差异观察 | Some, core::mem::size_of::<i32>, translated_ref | Box::pin, NET_INTERFACE.poll, NET_INTERFACE.tcp_socket, Ok, address::endpoint, info! ...(+4) |
| `os/src/syscall/sync.rs#sys_enable_deadlock_detect` | `kernel/src/net/address.rs#fill_with_endpoint` | 同模块上下文对齐/差异观察 | trace! | Ok, SocketAddrv4::from, SocketAddrv4::from(endpoint).fill, SocketAddrv6::from, SocketAddrv6::from(endpoint).fill, SumGuard::new ...(+9) |
| `os/src/syscall/sync.rs#us_to_timer_ms` | `kernel/src/syscall/resource.rs#sys_prlimit64` | 同模块上下文对齐/差异观察 | deadline_us.saturating_add | Err, Ok, RLimit::get_rlimit, RLimit::set_rlimit, SumGuard::new, UserCheck::new ...(+7) |

#### 文件系统
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `os/src/fs/inode.rs#truncate` | `kernel/src/fs/file.rs#truncate` | 同板块同名 | (len - data.len()).min, Some, Vec::new, buffer.len, data.as_slice, data.extend_from_slice ...(+10) | Box::pin, Ok, OpenFlags::default, SeekFrom::Start, inner.inode.as_ref, inner.inode.as_ref().ok_or ...(+9) |
| `os/src/syscall/fs.rs#sys_fchmodat` | `kernel/src/syscall/fs.rs#sys_fchmodat` | 同板块同名 | String::new, chmod_path, current_user_token, open_file, open_file(path.as_str(), OpenFlags::RDONLY).is_none, path.as_str ...(+6) | Ok, stack_trace! |
| `os/src/syscall/fs.rs#sys_unlinkat` | `kernel/src/syscall/fs.rs#sys_unlinkat` | 同板块同名 | current_user_token, inode.is_dir, open_file, path.as_str, remove_path, resolve_path ...(+1) | Duration::ZERO.into, Err, Ok, debug!, drop, parent.unlink ...(+17) |
| `os/src/fs/pipe.rs#make_pipe` | `kernel/src/fs/pipe.rs#make_pipe` | 同板块同名 | Arc::new, Pipe::read_end_with_buffer, Pipe::write_end_with_buffer, PipeRingBuffer::new, UPSafeCell::new, buffer.clone ...(+4) | Arc::new, Mutex::new, OpenFlags::empty, Pipe::new, PipeRingBuffer::new, buffer.clone ...(+5) |

#### 设备驱动
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `os/src/drivers/block/virtio_blk.rs#read_block` | `kernel/src/driver/fu740/sdcard.rs#read_block` | 同板块同名 | READ_BOUNCE_BUFFER.exclusive_access, bounce.0.fill, buf.len, buf.len().min, buf[..n].copy_from_slice, buf[..n].fill ...(+3) | Err, Ok, buf.len, self.end_cmd, self.get_resp, self.read_data ...(+1) |
| `os/src/sbi.rs#console_getchar` | `kernel/src/driver/sbi/mod.rs#console_getchar` | 同板块同名 | ((UART0 + UART_REG_LSR) as *const u8).read_volatile, ((UART0 + UART_REG_RBR) as *const u8).read_volatile | sbi_call |
| `os/src/drivers/block/virtio_blk.rs#write_block` | `kernel/src/driver/fu740/sdcard.rs#write_block` | 同板块同名 | WRITE_BOUNCE_BUFFER.exclusive_access, bounce.0.fill, bounce.0[..n].copy_from_slice, buf.len, buf.len().min, self
                .0
                .exclusive_access ...(+2) | Err, Ok, buf.len, self.end_cmd, self.get_resp, self.send_cmd ...(+1) |
| `os/src/sbi.rs#console_putchar` | `kernel/src/driver/sbi/mod.rs#console_putchar` | 同板块同名 | ((UART0 + UART_REG_LSR) as *const u8).read_volatile, ((UART0 + UART_REG_THR) as *mut u8).write_volatile | sbi_call |

#### 网络协议栈
仅参考项目中观察到该模块代表函数，目标项目没有同模块可对齐证据：
- 参考项目：`kernel/src/net/tcp.rs#drop`，调用摘要：NET_INTERFACE.poll, NET_INTERFACE.remove, NET_INTERFACE.tcp_socket, info!, socket.close, socket.is_open ...(+1)
- 参考项目：`kernel/src/net/mod.rs#can_bind`，调用摘要：Some, log::info!, self.0.clone, socket.loacl_endpoint, socket.loacl_endpoint().eq, socket.socket_type ...(+2)
- 参考项目：`kernel/src/net/udp.rs#drop`，调用摘要：NET_INTERFACE.poll, NET_INTERFACE.remove, NET_INTERFACE.udp_socket, log::info!, socket.close, socket.is_open ...(+1)
- 参考项目：`tftp-server/server.c#udp_out`，调用摘要：fflush, sendto

#### 文件系统/第三方库
仅目标项目中观察到该模块代表函数，参考项目没有同模块可对齐证据：
- 目标项目：`os/src/fs/ext4.rs#ext4_list_dir`，调用摘要：EXT4_DIR_CACHE
                .exclusive_access, EXT4_DIR_CACHE
                .exclusive_access()
                .insert, EXT4_DIR_CACHE
        .exclusive_access, EXT4_DIR_CACHE
        .exclusive_access()
        .get, EXT4_DIR_CACHE
        .exclusive_access()
        .get(cache_key.as_str())
        .cloned, EXT4_DIR_CACHE.exclusive_access ...(+16)
- 目标项目：`os/src/fs/ext4.rs#ext4_metadata`，调用摘要：EXT4_METADATA_CACHE
                .exclusive_access, EXT4_METADATA_CACHE
                .exclusive_access()
                .insert, EXT4_METADATA_CACHE
        .exclusive_access, EXT4_METADATA_CACHE
        .exclusive_access()
        .get, EXT4_METADATA_CACHE
        .exclusive_access()
        .get(cache_key.as_str())
        .cloned, EXT4_METADATA_CACHE
        .exclusive_access()
        .insert ...(+12)
- 目标项目：`os/src/fs/ext4.rs#path_variants`，调用摘要：String::from, Vec::new, format!, norm, normalized
        .strip_prefix, normalized
        .strip_prefix('/')
        .map ...(+6)
- 目标项目：`os/src/fs/ext4.rs#read`，调用摘要：(dst.len() - pos).min, Box::new, EXT4_BOUNCE_BUFFER.exclusive_access, Err, Ok, dst.len ...(+2)

#### 第三方库
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `third_party/riscv/descriptor/generator.rs#generate` | `dependencies/riscv/descriptor/generator.rs#generate` | 同板块同名同结构 | String::new, bf.generate_enum, format!, self.bfs.iter, write!, write!(&mut bit_sets, "{}", bf.generate_bitops()).unwrap ...(+2) | String::new, bf.generate_enum, format!, self.bfs.iter, write!, write!(&mut bit_sets, "{}", bf.generate_bitops()).unwrap ...(+2) |
| `third_party/riscv/src/paging/multi_level.rs#create_p1_if_not_exist` | `dependencies/riscv/src/paging/multi_level.rs#create_p1_if_not_exist` | 同板块同名同结构 | Ok, allocator.alloc, allocator.alloc().ok_or, frame.as_kernel_mut, frame.clone, p1_table.zero ...(+11) | Ok, allocator.alloc, allocator.alloc().ok_or, frame.as_kernel_mut, frame.clone, p1_table.zero ...(+11) |
| `third_party/virtio-drivers/src/queue.rs#add_buffers` | `dependencies/virtio-drivers/src/queue.rs#add_buffers` | 同板块同名同结构 | VirtIOHeader::make_fake_header, VirtQueue::<FakeHal>::new, VirtQueue::<FakeHal>::new(&mut header, 0, 4).unwrap, assert!, assert_eq!, queue
            .add ...(+5) | VirtIOHeader::make_fake_header, VirtQueue::<FakeHal>::new, VirtQueue::<FakeHal>::new(&mut header, 0, 4).unwrap, assert!, assert_eq!, queue
            .add ...(+5) |
| `third_party/virtio-drivers/src/header.rs#make_fake_header` | `dependencies/virtio-drivers/src/header.rs#make_fake_header` | 同板块同名同结构 | Default::default, DeviceStatus::empty, ReadOnly::new, Volatile::new, WriteOnly::default | Default::default, DeviceStatus::empty, ReadOnly::new, Volatile::new, WriteOnly::default |

#### 通用模块/其他
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `os/src/loader.rs#get_app_data` | `kernel/src/loader.rs#get_app_data` | 同板块同名同结构 | assert!, core::slice::from_raw_parts, get_num_app, num_app_ptr.add | assert!, core::slice::from_raw_parts, get_num_app, num_app_ptr.add |
| `os/src/sync/mutex.rs#lock` | `kernel/src/sync/mutex/remutex.rs#lock` | 同板块同名 | drop, self.locked.exclusive_access, suspend_current_and_run_next, trace! | S::before_lock, self
                .lock
                .compare_exchange, self
                .lock
                .compare_exchange(false, true, Ordering::Acquire, Ordering::Relaxed)
                .is_ok, self.wait_unlock |
| `os/src/main.rs#clear_bss` | `kernel/src/main.rs#clear_bss` | 同板块同名 | core::slice::from_raw_parts_mut, core::slice::from_raw_parts_mut(start as *mut u8, end - start).fill | core::slice::from_raw_parts_mut, core::slice::from_raw_parts_mut(sbss as usize as *mut u8, ebss as usize - sbss as usize)
            .fill |
| `os/src/loader.rs#get_app_data_by_name` | `kernel/src/loader.rs#get_app_data_by_name` | 同板块同名 | (0..num_app)
        .find, (0..num_app)
        .find(|&i| APP_NAMES[i] == name)
        .map, get_num_app | (0..num_app)
        .find, (0..num_app)
        .find(|&i| app_names[i] == name)
        .map, APP_NAMES.as_ref, APP_NAMES.as_ref().unwrap, get_num_app |


#### 1. 宏观判定
- **定性结论**: 独立开发 (相似度: 16.31%，共同 AST 指纹: 323；唯一 AST 指纹: 目标项目 906，参考项目 1397；覆盖率: 目标项目 35.65%，参考项目 23.12%)

#### 2. 全模块覆盖矩阵解读
- **进程管理**: 双方均存在该模块。共同 AST 指纹为 0，目标覆盖率 0.0%，参考覆盖率 0.0%。该模块证据强度为**弱**。尽管存在大量同名系统调用，但 AST 层面未形成任何可度量的结构重合，表明双方在进程控制块、调度原语、父子关系管理等底层实现上采用了不同的抽象与调用路径。
- **任务调度/异步运行时**: 双方均存在该模块。共同 AST 指纹为 0，目标覆盖率 0.0%，参考覆盖率 0.0%。该模块证据强度为**弱**。目标项目采用同步阻塞模型（`suspend_current_and_run_next`），参考项目采用异步 Future 模型（`WaitFuture`、`Select2Futures`），底层调度器架构存在根本性差异。
- **内存管理**: 双方均存在该模块。共同 AST 指纹为 43，目标覆盖率 24.71%，参考覆盖率 16.73%。该模块证据强度为**中**。重合集中于页表创建、内核空间映射等通用 RISC-V 内存管理逻辑，但目标项目使用 `MapArea`/`PTEFlags`，参考项目使用 `VmArea`/`KERNEL_DIRECT_OFFSET`，外部对象与地址空间布局不同。
- **中断/异常处理**: 双方均存在该模块。共同 AST 指纹为 3，目标覆盖率 60.0%，参考覆盖率 11.11%。该模块证据强度为**弱**。重合函数（`set_kernel_trap_entry`、`set_user_trap_entry`、`enable_timer_interrupt`）均为单行 RISC-V CSR 寄存器写入操作，属于硬件规范决定的必然写法，不构成独创表达。
- **系统调用**: 双方均存在该模块。共同 AST 指纹为 0，目标覆盖率 0.0%，参考覆盖率 0.0%。该模块证据强度为**弱**。目标项目系统调用分发为巨型 `match` 语句，参考项目使用 `sys_handler!` 宏，分发机制完全不同。
- **文件系统**: 双方均存在该模块。共同 AST 指纹为 0，目标覆盖率 0.0%，参考覆盖率 0.0%。该模块证据强度为**弱**。目标项目独有 ext4 文件系统实现（`EXT4_DIR_CACHE`、`EXT4_METADATA_CACHE`），参考项目独有 procfs 伪文件系统，功能演进路线不同。
- **设备驱动**: 双方均存在该模块。共同 AST 指纹为 0，目标覆盖率 0.0%，参考覆盖率 0.0%。该模块证据强度为**弱**。目标项目使用 virtio-blk 驱动，参考项目使用 fu740 sdcard 驱动，硬件平台与驱动模型完全不同。
- **网络协议栈**: 仅参考项目存在该模块（46 个函数），目标项目函数数为 0。该模块证据强度为**未形成**。属于单方功能模块，无法进行同板块比对。
- **第三方库/公共依赖**: 双方均存在该模块。共同 AST 指纹为 268，目标覆盖率 100.0%，参考覆盖率 99.63%。该模块证据强度为**强（但属合法复用）**。重合集中于 `riscv`、`virtio-drivers` 等开源 crate，是双方引用相同第三方依赖所致，与内核原创性无关。

#### 3. 底层 AST 关键证据指纹表
| 所属板块 | 目标项目路径 | 参考项目路径 | 证据类型 | 技术说明 |
| :--- | :--- | :--- | :--- | :--- |
| 第三方库/公共依赖 | third_party/riscv/descriptor/generator.rs#generate | dependencies/riscv/descriptor/generator.rs#generate | 同板块同名同结构 | 完全相同的 CSR 寄存器位域生成器，属于开源 crate 代码 |
| 第三方库/公共依赖 | third_party/riscv/src/paging/multi_level.rs#create_p1_if_not_exist | dependencies/riscv/src/paging/multi_level.rs#create_p1_if_not_exist | 同板块同名同结构 | 完全相同的多级页表创建逻辑，属于开源 crate 代码 |
| 第三方库/公共依赖 | third_party/virtio-drivers/src/queue.rs#add_buffers | dependencies/virtio-drivers/src/queue.rs#add_buffers | 同板块同名同结构 | 完全相同的 virtio 队列测试用例，属于开源 crate 代码 |
| 第三方库/公共依赖 | third_party/virtio-drivers/src/header.rs#make_fake_header | dependencies/virtio-drivers/src/header.rs#make_fake_header | 同板块同名同结构 | 完全相同的 virtio 头构造测试函数，属于开源 crate 代码 |
| 内存管理 | os/src/mm/memory_set.rs#new_kernel | kernel/src/mm/memory_space/mod.rs#new_kernel | 同板块同名 | 内核空间映射逻辑，均使用 `MapArea::new`/`VmArea::new` 但地址偏移量不同 |
| 内存管理 | os/src/mm/memory_set.rs#from_existed_user | kernel/src/mm/memory_space/mod.rs#from_existed_user | 同板块同名 | 用户空间派生逻辑，目标使用 `Arc::clone`+`MapArea::from_another`，参考使用 `Self::new_from_global`+`VmArea::from_another` |
| 中断/异常处理 | os/src/trap/mod.rs#set_user_trap_entry | kernel/src/trap/mod.rs#set_user_trap_entry | 同板块同名同结构 | 均为 `stvec::write` 单行调用，RISC-V 规范决定 |
| 中断/异常处理 | os/src/trap/mod.rs#enable_timer_interrupt | kernel/src/trap/mod.rs#enable_timer_interrupt | 同板块同名同结构 | 均为 `sie::set_stimer` 单行调用，RISC-V 规范决定 |
| 通用模块/其他 | os/src/loader.rs#get_app_data | kernel/src/loader.rs#get_app_data | 同板块同名同结构 | 应用数据加载器，均使用 `core::slice::from_raw_parts`+`get_num_app`，属于 rCore 教程通用骨架 |
| 通用模块/其他 | os/src/loader.rs#get_app_data_by_name | kernel/src/loader.rs#get_app_data_by_name | 同板块同名 | 按名称查找应用，均使用 `(0..num_app).find` 模式，属于 rCore 教程通用骨架 |

#### 4. 核心代码调用流对比剖析
* **【进程管理/进程退出路径】**
* **底层技术析理**: 目标项目 `sys_exit` 调用 `release_process_file_locks` 显式释放文件锁后，通过 `exit_current_and_run_next` 直接切换上下文。参考项目 `sys_exit` 调用 `terminate_given_thread`，该函数内部通过 `local_hart().current_task().tid` 获取线程 ID 后执行线程级终止。目标项目以进程为退出粒度，参考项目以线程为退出粒度，且目标项目独有的文件锁释放调用在参考项目同层 AST 中完全缺失。
* **代码溯源链接**:
  - 目标项目: `os/src/syscall/process.rs#sys_exit`
  - 参考项目: `kernel/src/syscall/process.rs#sys_exit`

* **【任务调度/异步运行时/等待机制】**
* **底层技术析理**: 目标项目 `sys_waitpid` 采用同步阻塞模型，调用 `block_current_and_run_next` 挂起当前任务后遍历 `inner.children.iter().any` 轮询子进程状态。参考项目 `sys_wait4` 采用异步事件驱动模型，构造 `WaitFuture::new` 并配合 `Select2Futures::new` 与 `Event::all` 进行异步等待。两者在并发模型上存在根本性差异，前者是协程式同步阻塞，后者是 Future 异步组合子。
* **代码溯源链接**:
  - 目标项目: `os/src/syscall/process.rs#sys_waitpid`
  - 参考项目: `kernel/src/syscall/process.rs#sys_wait4`

* **【内存管理/内核空间映射】**
* **底层技术析理**: 目标项目 `new_kernel` 使用 `MEMORY_END.into` 作为映射边界，通过 `(*pair).0.into` 直接转换地址。参考项目 `new_kernel` 使用 `KERNEL_DIRECT_OFFSET << PAGE_SIZE_BITS` 作为偏移量，对每个地址段均附加该偏移。两者在地址空间布局上存在差异：目标项目采用直接物理地址映射，参考项目采用带偏移的内核直接映射区。
* **代码溯源链接**:
  - 目标项目: `os/src/mm/memory_set.rs#new_kernel`
  - 参考项目: `kernel/src/mm/memory_space/mod.rs#new_kernel`

* **【中断/异常处理/Trap 入口设置】**
* **底层技术析理**: 双方 `set_user_trap_entry` 和 `set_kernel_trap_entry` 均为对 RISC-V `stvec` CSR 的单一写入操作，`enable_timer_interrupt` 均为对 `sie` CSR 的 `set_stimer` 单一操作。这些是 RISC-V 特权架构规范定义的必须操作，调用图谱完全一致是硬件规范约束的结果，不反映软件设计层面的独创性。
* **代码溯源链接**:
  - 目标项目: `os/src/trap/mod.rs#set_user_trap_entry`
  - 参考项目: `kernel/src/trap/mod.rs#set_user_trap_entry`

#### 5. 单方模块与降权项
- **目标项目独有模块**: 文件系统/第三方库（`os/src/fs/ext4.rs` 系列函数，含 `EXT4_DIR_CACHE`、`EXT4_METADATA_CACHE`、`EXT4_BOUNCE_BUFFER` 等 ext4 实现）；判题框架（`os/src/task/judge.rs` 系列函数，含 `JUDGE_STATE`、`QEMU_EXIT_HANDLE`、`spawn_next_action` 等）。这些模块在参考项目中无对应实现，属于目标项目的独立功能扩展。
- **参考项目独有模块**: 网络协议栈（`kernel/src/net/tcp.rs`、`udp.rs`、`address.rs` 等 46 个函数）；procfs 伪文件系统（`kernel/src/fs/procfs/meminfo.rs`、`filesystems.rs` 等）。这些模块在目标项目中无对应实现，属于参考项目的独立功能扩展。
- **降权项说明**: 进程管理板块中 `sys_getpid`、`sys_getuid`、`sys_getgid`、`sys_geteuid`、`sys_getegid` 等函数均为 POSIX 标准系统调用的单行返回实现，AST 极度简单（`current_process` → 返回内部字段），属于操作系统领域的通用范式，不能作为同源证据。`sys_yield` 双方均调用调度器的让出函数，但目标使用 `suspend_current_and_run_next`，参考使用 `process::yield_now`，调用接口完全不同。`current_task`/`current_process`/`current_trap_cx` 等访问器函数属于任何多任务操作系统都必须实现的通用骨架，且目标基于 `PROCESSOR.exclusive_access` 单核模型，参考基于 `local_hart` 多核模型，底层架构不同。

#### 6. 保守结论
本次审计基于客观 AST 指纹比对，整体相似度仅 16.31%，且 323 个共同指纹中超过 82%（268 个）集中于双方共同引用的开源第三方库（`riscv`、`virtio-drivers`），属于合法公共依赖复用。控方核心指控的进程管理板块共同 AST 指纹为 0，表明双方在进程派生、父子关系管理、退出回收、等待机制等关键路径上不存在可度量的结构同构。目标项目独有的 ext4 文件系统与判题框架、参考项目独有的网络协议栈与 procfs，进一步证明两者走的是独立的功能演进路线。综合判定：目标项目 `[2026-T2026103369910066-XX]` 与参考项目 `[2023-Titanix-哈尔滨工业大学深圳]` 在底层架构上存在显著差异，不构成实质性抄袭或系统性复用。

------------------------------

