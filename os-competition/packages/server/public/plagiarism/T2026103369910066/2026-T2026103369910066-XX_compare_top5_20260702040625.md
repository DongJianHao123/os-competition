# 🛡️ OS 内核赛自动化代码级架构审计报告

**审计时间**: `2026-07-02 04:06:25`
**参赛作品名称**: `2026-T2026103369910066-XX`
**年份**: `2026`
**学校**: `XX`
**队伍名称**: `T2026103369910066`
**比赛名字**: `OS-Competition`
**比赛子赛道**: `Kernel-Implementation`
**仓库地址**: `https://gitlab.eduxiji.net/educg-group-43501-3132633/T2026103369910066-3514`
**推测开发语言**: `Rust / C (由AST探针自动识别)`
**特征函数量**: `1409`

## ⚔️ 底层代码同源性对比 (AST Homology)
> 本部分由专门的代码审查智能体，对该项目与符合年份条件的历届最高相似度项目进行控制流与底层调用对比。新版报告会先给出确定性的全模块覆盖矩阵，再给出 Agent 的分模块解释，避免只围绕少量抽样函数展开。

### 🏆 Top 5 底层对标: vs `2024-刘奶奶找牛奶奶买牛奶队-湖北民族大学`（湖北民族大学 / 刘奶奶找牛奶奶买牛奶队）

#### 对方作品信息

**对方年份**: `2024`
**对方学校**: `湖北民族大学`
**对方队伍名称**: `刘奶奶找牛奶奶买牛奶队`
**对方比赛名字**: `OS-Competition`
**对方比赛子赛道**: `Kernel-Implementation`
**对方仓库地址**: `https://gitlab.eduxiji.net/educg-group-26010-2376550/T202410517992471-3214`

> 双向覆盖率：共同 AST 指纹: 472；唯一 AST 指纹: 目标项目 906，参考项目 2964；覆盖率: 目标项目 52.1%，参考项目 15.92%

## 全模块对比总览（确定性证据）

> 总体覆盖率：共同 AST 指纹: 472；唯一 AST 指纹: 目标项目 906，参考项目 2964；覆盖率: 目标项目 52.1%，参考项目 15.92%

### 1. 模块覆盖矩阵
| 模块 | 目标函数数 | 参考函数数 | 共同唯一 AST 指纹 | Target 覆盖 | Base 覆盖 | 状态 |
| :--- | ---: | ---: | ---: | ---: | ---: | :--- |
| 进程管理 | 128 | 42 | 20 | 17.09% | 50.0% | 双方存在且有结构重合 |
| 任务调度/异步运行时 | 119 | 84 | 19 | 16.24% | 23.75% | 双方存在且有结构重合 |
| 内存管理 | 188 | 169 | 99 | 56.9% | 63.06% | 双方存在且有结构重合 |
| 中断/异常处理 | 5 | 6 | 3 | 60.0% | 60.0% | 双方存在且有结构重合 |
| 系统调用 | 4 | 61 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |
| 文件系统 | 192 | 340 | 57 | 32.95% | 18.27% | 双方存在且有结构重合 |
| 设备驱动 | 8 | 9 | 1 | 12.5% | 11.11% | 双方存在且有结构重合 |
| 网络协议栈 | 0 | 0 | 0 | 0.0% | 0.0% | 双方均未体现 |
| 文件系统/第三方库 | 14 | 506 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |
| 第三方库 | 709 | 6558 | 268 | 100.0% | 14.43% | 双方存在且有结构重合 |
| 通用模块/其他 | 42 | 114 | 11 | 27.5% | 12.22% | 双方存在且有结构重合 |

### 2. 分模块证据清单

#### 进程管理
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `os/src/syscall/process.rs#sys_exec` | `os/src/syscall/process.rs#sys_exec` | 同板块同名 | Some, Vec::new, all_data.as_slice, all_data.is_empty, all_data.iter, all_data.iter().position ...(+55) | Vec::new, all_data.as_slice, app_inode.read_all, args.add, args_vec.len, args_vec.push ...(+7) |
| `os/src/syscall/process.rs#sys_clone` | `os/src/syscall/process.rs#sys_clone` | 同板块同名 | Arc::clone, Arc::new, Some, TaskControlBlock::new, add_task, current_process ...(+25) | child_process.getpid, child_process.inner_exclusive_access, child_process_inner.tasks[0].as_ref, child_process_inner.tasks[0].as_ref().unwrap, current_process, current_process.fork ...(+2) |
| `os/src/syscall/process.rs#sys_kill` | `os/src/syscall/process.rs#sys_kill` | 同板块同名 | all_processes, current_process, current_process().getpid, current_process().inner_exclusive_access, process.getpid, send_signal_to_process ...(+2) | SignalFlags::from_bits, pid2process, process.inner_exclusive_access |
| `os/src/syscall/process.rs#sys_times` | `os/src/syscall/process.rs#sys_times` | 同板块同名 | (&t as *const Tms).cast::<u8>, (raw.len() - pos).min, core::ptr::copy_nonoverlapping, current_user_token, get_time_us, raw.as_mut_ptr ...(+7) | current_user_token, get_time_us, tms.add, translated_refmut |

#### 任务调度/异步运行时
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `os/src/task/mod.rs#check_signals_of_current` | `os/src/task/mod.rs#check_signals_of_current` | 同板块同名 | SignalFlags::from_bits_truncate, Some, bits.trailing_zeros, current_process, current_task, current_task().unwrap ...(+9) | current_process, process.inner_exclusive_access, process_inner.signals.check_error |
| `os/src/syscall/thread.rs#sys_thread_create` | `os/src/syscall/thread.rs#sys_thread_create` | 同板块同名 | Arc::clone, Arc::new, Some, TaskControlBlock::new, TrapContext::app_init_context, add_task ...(+18) | Arc::clone, Arc::new, Some, TaskControlBlock::new, TrapContext::app_init_context, add_task ...(+17) |
| `os/src/syscall/fs.rs#sys_chdir` | `os/src/syscall/fs.rs#sys_chdir` | 同板块同名 | check_search_permission, check_user_cstr_readable, current_process, current_process().inner_exclusive_access, current_user_token, effective_permission_bits ...(+9) | Arc::new, FileDescriptor::File, current_process, current_user_token, inner
        .work_dir
        .as_ref, inner
        .work_dir
        .as_ref()
        .open ...(+3) |
| `os/src/syscall/fs.rs#sys_getcwd` | `os/src/syscall/fs.rs#sys_getcwd` | 同板块同名 | (slice.as_ptr() as *mut u8).add, (slice.as_ptr() as *mut u8).add(off).write, buf.is_null, bytes.iter, bytes.iter().copied, bytes.iter().copied().chain ...(+13) | UserBuffer::new, b[0..dir.len()].copy_from_slice, current_process, current_user_token, dir.len, inner.work_dir.name ...(+4) |

#### 内存管理
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `os/src/task/mod.rs#exit_current_and_run_next` | `os/src/task/mod.rs#exit_current_and_run_next` | 同板块同名 | Arc::clone, PageTable::from_token, PageTable::from_token(token).translate_va, Some, TaskContext::zero_init, Vec::<TaskUserRes>::new ...(+44) | Arc::clone, Arc::downgrade, INITPROC.inner_exclusive_access, Some, TaskContext::zero_init, Vec::<TaskUserRes>::new ...(+32) |
| `os/src/task/process.rs#fork` | `os/src/task/process.rs#fork` | 同板块同名 | Arc::clone, Arc::downgrade, Arc::new, MemorySet::from_existed_user, RecycleAllocator::new, SignalFlags::empty ...(+30) | Arc::clone, Arc::downgrade, Arc::new, MemorySet::from_existed_user, RecycleAllocator::new, SignalFlags::empty ...(+26) |
| `os/src/trap/mod.rs#trap_handler` | `os/src/trap/mod.rs#trap_handler` | 同板块同名 | check_process_alarms, check_signals_of_current, check_timer, current_add_signal, current_process, current_process()
                    .inner_exclusive_access ...(+16) | check_signals_of_current, check_timer, current_add_signal, current_trap_cx, exit_current_and_run_next, panic! ...(+9) |
| `easy-fs/src/layout.rs#increase_size` | `easy-fs/src/layout.rs#increase_size` | 同板块同名同结构 | Arc::clone, get_block_cache, get_block_cache(indirect2[a0] as usize, Arc::clone(block_device))
                        .lock, get_block_cache(indirect2[a0] as usize, Arc::clone(block_device))
                        .lock()
                        .modify, get_block_cache(self.indirect1 as usize, Arc::clone(block_device))
            .lock, get_block_cache(self.indirect1 as usize, Arc::clone(block_device))
            .lock()
            .modify ...(+7) | Arc::clone, get_block_cache, get_block_cache(indirect2[a0] as usize, Arc::clone(block_device))
                        .lock, get_block_cache(indirect2[a0] as usize, Arc::clone(block_device))
                        .lock()
                        .modify, get_block_cache(self.indirect1 as usize, Arc::clone(block_device))
            .lock, get_block_cache(self.indirect1 as usize, Arc::clone(block_device))
            .lock()
            .modify ...(+7) |

#### 中断/异常处理
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `os/src/trap/mod.rs#trap_from_kernel` | `os/src/trap/mod.rs#trap_from_kernel` | 同板块同名 | panic! | panic!, println! |
| `os/src/trap/mod.rs#set_kernel_trap_entry` | `os/src/trap/mod.rs#set_kernel_trap_entry` | 同板块同名 | stvec::write | stvec::write |
| `os/src/trap/mod.rs#set_user_trap_entry` | `os/src/trap/mod.rs#set_user_trap_entry` | 同板块同名同结构 | stvec::write | stvec::write |
| `os/src/trap/mod.rs#enable_timer_interrupt` | `os/src/trap/mod.rs#enable_timer_interrupt` | 同板块同名同结构 | sie::set_stimer | sie::set_stimer |

#### 系统调用
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `os/src/syscall/sync.rs#futex_timespec_us` | `os/src/syscall/sync.rs#sys_sleep` | 同模块上下文对齐/差异观察 | Some, ts.nsec.saturating_add, ts.sec.saturating_mul, ts.sec.saturating_mul(1_000_000).saturating_add | current_user_token, get_time_us, println!, rem.add, req.add, suspend_current_and_run_next ...(+2) |
| `os/src/syscall/sync.rs#read_futex_word` | `os/src/syscall/sys_info.rs#new` | 同模块上下文对齐/差异观察 | Some, core::mem::size_of::<i32>, translated_ref | utsname::str2u8 |
| `os/src/syscall/sync.rs#sys_enable_deadlock_detect` | `user/src/lib.rs#waitpid` | 同模块上下文对齐/差异观察 | trace! | sys_waitpid, yield_ |
| `os/src/syscall/sync.rs#us_to_timer_ms` | `user/src/syscall.rs#syscall` | 同模块上下文对齐/差异观察 | deadline_us.saturating_add | asm! |

#### 文件系统
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `easy-fs/src/layout.rs#clear_size` | `easy-fs/src/layout.rs#clear_size` | 同板块同名同结构 | Arc::clone, Vec::new, assert!, data_blocks.min, get_block_cache, get_block_cache(*entry as usize, Arc::clone(block_device))
                        .lock ...(+13) | Arc::clone, Vec::new, assert!, data_blocks.min, get_block_cache, get_block_cache(*entry as usize, Arc::clone(block_device))
                        .lock ...(+13) |
| `os/src/fs/inode.rs#truncate` | `os/src/fatfs/lfn.rs#truncate` | 同板块同名 | (len - data.len()).min, Some, Vec::new, buffer.len, data.as_slice, data.extend_from_slice ...(+10) | self.buf.set_len, ucs2_units
            .iter, ucs2_units
            .iter()
            .rposition, ucs2_units
            .iter()
            .rposition(|c| *c != 0xFFFF && *c != 0)
            .map_or |
| `easy-fs/src/layout.rs#read_at` | `easy-fs/src/layout.rs#read_at` | 同板块同名同结构 | (offset + buf.len()).min, Arc::clone, buf.len, dst.copy_from_slice, end_current_block.min, get_block_cache ...(+3) | (offset + buf.len()).min, Arc::clone, buf.len, dst.copy_from_slice, end_current_block.min, get_block_cache ...(+3) |
| `easy-fs/src/layout.rs#write_at` | `easy-fs/src/layout.rs#write_at` | 同板块同名同结构 | (offset + buf.len()).min, Arc::clone, assert!, buf.len, dst.copy_from_slice, end_current_block.min ...(+4) | (offset + buf.len()).min, Arc::clone, assert!, buf.len, dst.copy_from_slice, end_current_block.min ...(+4) |

#### 设备驱动
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `os/src/drivers/block/virtio_blk.rs#read_block` | `os/src/drivers/block/virtio_blk.rs#read_block` | 同板块同名 | READ_BOUNCE_BUFFER.exclusive_access, bounce.0.fill, buf.len, buf.len().min, buf[..n].copy_from_slice, buf[..n].fill ...(+3) | self.0
            .exclusive_access, self.0
            .exclusive_access()
            .read_block, self.0
            .exclusive_access()
            .read_block(block_id, buf)
            .expect |
| `os/src/drivers/block/mod.rs#block_device_test` | `os/src/drivers/block/mod.rs#block_device_test` | 同板块同名 | BLOCK_DEVICE.clone, assert_eq!, block_device.read_block, block_device.write_block, println!, write_buffer.iter_mut | BLOCK_DEVICE.clone, assert_eq!, block_device.read_block, block_device.write_block, println!, write_buffer.iter_mut |
| `os/src/drivers/block/virtio_blk.rs#write_block` | `os/src/drivers/block/virtio_blk.rs#write_block` | 同板块同名 | WRITE_BOUNCE_BUFFER.exclusive_access, bounce.0.fill, bounce.0[..n].copy_from_slice, buf.len, buf.len().min, self
                .0
                .exclusive_access ...(+2) | self.0
            .exclusive_access, self.0
            .exclusive_access()
            .write_block, self.0
            .exclusive_access()
            .write_block(block_id, buf)
            .expect |
| `os/src/drivers/block/virtio_blk.rs#phys_to_virt` | `os/src/drivers/block/virtio_blk.rs#phys_to_virt` | 同板块同名同结构 | 内部闭环逻辑 | 内部闭环逻辑 |

#### 文件系统/第三方库
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `os/src/fs/ext4.rs#ext4_list_dir` | `modified_dependencies/rust-fatfs/src/fs.rs#format_volume` | 同模块上下文对齐/差异观察 | EXT4_DIR_CACHE
                .exclusive_access, EXT4_DIR_CACHE
                .exclusive_access()
                .insert, EXT4_DIR_CACHE
        .exclusive_access, EXT4_DIR_CACHE
        .exclusive_access()
        .get, EXT4_DIR_CACHE
        .exclusive_access()
        .get(cache_key.as_str())
        .cloned, EXT4_DIR_CACHE.exclusive_access ...(+16) | DirFileEntryData::new, Err, Ok, SeekFrom::End, SeekFrom::Start, alloc_cluster ...(+28) |
| `os/src/fs/ext4.rs#ext4_metadata` | `modified_dependencies/rust-fatfs/src/table.rs#test_fat` | 同模块上下文对齐/差异观察 | EXT4_METADATA_CACHE
                .exclusive_access, EXT4_METADATA_CACHE
                .exclusive_access()
                .insert, EXT4_METADATA_CACHE
        .exclusive_access, EXT4_METADATA_CACHE
        .exclusive_access()
        .get, EXT4_METADATA_CACHE
        .exclusive_access()
        .get(cache_key.as_str())
        .cloned, EXT4_METADATA_CACHE
        .exclusive_access()
        .insert ...(+12) | ClusterIterator::<&mut S, S::Error, S>::new, [0xA_u32, 0x14_u32, 0x15_u32, 0x16_u32, 0x19_u32, 0x1A_u32]
                .iter, [0xA_u32, 0x14_u32, 0x15_u32, 0x16_u32, 0x19_u32, 0x1A_u32]
                .iter()
                .cloned, [0xA_u32, 0x14_u32, 0x15_u32, 0x16_u32, 0x19_u32, 0x1A_u32]
                .iter()
                .cloned()
                .map, [0xA_u32, 0x14_u32, 0x15_u32, 0x16_u32, 0x19_u32, 0x1A_u32]
                .iter()
                .cloned()
                .map(Some)
                .collect::<Vec<_>>, assert! ...(+3) |
| `os/src/fs/ext4.rs#path_variants` | `modified_dependencies/rust-fatfs/src/boot_sector.rs#format_bpb` | 同模块上下文对齐/差异观察 | String::from, Vec::new, format!, norm, normalized
        .strip_prefix, normalized
        .strip_prefix('/')
        .map ...(+6) | Err, FatType::from_clusters, Ok, assert!, bpb.total_clusters, debug_assert! ...(+15) |
| `os/src/fs/ext4.rs#read` | `modified_dependencies/rust-fatfs/src/boot_sector.rs#determine_sectors_per_fat` | 同模块上下文对齐/差异观察 | (dst.len() - pos).min, Box::new, EXT4_BOUNCE_BUFFER.exclusive_access, Err, Ok, dst.len ...(+2) | fat_type.bits_per_fat_entry, u32::from, u64::from |

#### 第三方库
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `third_party/riscv/descriptor/generator.rs#generate` | `dependencies/riscv/descriptor/generator.rs#generate` | 同板块同名同结构 | String::new, bf.generate_enum, format!, self.bfs.iter, write!, write!(&mut bit_sets, "{}", bf.generate_bitops()).unwrap ...(+2) | String::new, bf.generate_enum, format!, self.bfs.iter, write!, write!(&mut bit_sets, "{}", bf.generate_bitops()).unwrap ...(+2) |
| `third_party/riscv/src/paging/multi_level.rs#create_p1_if_not_exist` | `dependencies/riscv/src/paging/multi_level.rs#create_p1_if_not_exist` | 同板块同名同结构 | Ok, allocator.alloc, allocator.alloc().ok_or, frame.as_kernel_mut, frame.clone, p1_table.zero ...(+11) | Ok, allocator.alloc, allocator.alloc().ok_or, frame.as_kernel_mut, frame.clone, p1_table.zero ...(+11) |
| `third_party/virtio-drivers/src/queue.rs#add_buffers` | `dependencies/virtio-drivers/src/queue.rs#add_buffers` | 同板块同名同结构 | VirtIOHeader::make_fake_header, VirtQueue::<FakeHal>::new, VirtQueue::<FakeHal>::new(&mut header, 0, 4).unwrap, assert!, assert_eq!, queue
            .add ...(+5) | VirtIOHeader::make_fake_header, VirtQueue::<FakeHal>::new, VirtQueue::<FakeHal>::new(&mut header, 0, 4).unwrap, assert!, assert_eq!, queue
            .add ...(+5) |
| `third_party/virtio-drivers/src/header.rs#make_fake_header` | `dependencies/virtio-drivers/src/header.rs#make_fake_header` | 同板块同名同结构 | Default::default, DeviceStatus::empty, ReadOnly::new, Volatile::new, WriteOnly::default | Default::default, DeviceStatus::empty, ReadOnly::new, Volatile::new, WriteOnly::default |

#### 通用模块/其他
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `os/src/lang_items.rs#backtrace` | `os/src/lang_items.rs#backtrace` | 同板块同名同结构 | asm!, current_kstack_top, println! | asm!, current_kstack_top, println! |
| `os/src/sync/mutex.rs#lock` | `user/src/bin/adder_peterson_spin.rs#lock` | 同板块同名 | drop, self.locked.exclusive_access, suspend_current_and_run_next, trace! | compiler_fence, vload! |
| `os/src/timer.rs#get_time_us` | `os/src/timer.rs#get_time_us` | 同板块同名 | LAST_TIME_US.exclusive_access, time::read | time::read |
| `os/src/main.rs#clear_bss` | `os/src/main.rs#clear_bss` | 同板块同名 | core::slice::from_raw_parts_mut, core::slice::from_raw_parts_mut(start as *mut u8, end - start).fill | core::slice::from_raw_parts_mut, core::slice::from_raw_parts_mut(sbss as usize as *mut u8, ebss as usize - sbss as usize)
            .fill |


# 最终同源性定性仲裁报告

## 1. 宏观判定

- **定性结论**: **独立开发**（底层架构存在实质性差异）
- **客观 AST 相似度**: 13.89%
- **双向覆盖率**: 共同 AST 指纹 472；目标项目唯一指纹 906，参考项目唯一指纹 2964；目标项目覆盖率 52.1%，参考项目覆盖率 15.92%

**判定依据**：从参考项目视角，仅 15.92% 的 AST 指纹在目标项目中复现，说明目标项目并非对参考项目的整体搬运。从目标项目视角，47.9% 的调用链为独有代码路径。进程管理模块目标覆盖率仅 17.09%，128 个函数中仅 20 个与参考项目存在结构重合。控方所指控的 21 处“底层逻辑一致”函数，经逐条审查，绝大多数为 rCore 教学框架约束下的薄封装函数（getter/setter/wrapper），其 AST 调用图谱的相似性源于 Rust 类型系统与框架 API 的必然约束，而非代码复制。核心系统调用（`sys_exec`、`sys_kill`、`sys_times`、`sys_get_time`、`sys_exit` 等）在参数解析策略、信号模型、地址翻译方式、资源清理逻辑等方面存在实质性实现差异。

---

## 2. 全模块覆盖矩阵解读

### 进程管理
- **双方均存在**：是
- **共同 AST 指纹**：20
- **覆盖率**：目标 17.09%，参考 50.0%
- **证据强度**：**弱**
- **解读**：目标项目进程管理模块有 128 个函数，远超参考项目的 42 个，说明目标项目有大量独立扩展。共同指纹仅覆盖 20 个函数，且多为 `getpid`、`inner_exclusive_access`、`current_task` 等框架约束下的薄封装。参考项目 50% 的覆盖率是因为其进程管理模块规模较小，共同函数占比相对较高，但绝对数量有限。控方指控的 21 处“底层逻辑一致”函数中，绝大多数为一行或数行的 getter/setter，可实现空间极小，不构成抄袭证据。

### 任务调度/异步运行时
- **双方均存在**：是
- **共同 AST 指纹**：19
- **覆盖率**：目标 16.24%，参考 23.75%
- **证据强度**：**弱**
- **解读**：双方均实现了 `run_tasks`、`schedule`、`__switch` 等调度原语，但这是 rCore 风格任务切换的标准模式。目标项目使用 `warn!` 宏进行日志输出，参考项目使用 `println!`，体现独立的日志策略。`sys_thread_create` 等函数在 `Arc::clone`、`TaskControlBlock::new`、`TrapContext::app_init_context` 等调用上相似，但这些是 Rust 异步运行时框架的标准组件。

### 内存管理
- **双方均存在**：是
- **共同 AST 指纹**：99
- **覆盖率**：目标 56.9%，参考 63.06%
- **证据强度**：**中**
- **解读**：内存管理是双方覆盖率最高的核心模块。`fork`、`exit_current_and_run_next`、`trap_handler` 等函数存在较多共同调用链。但需注意：(1) `MemorySet::from_existed_user`、`PageTable::from_token` 等是 rCore 内存管理子系统的标准 API；(2) `easy-fs/src/layout.rs` 中的 `increase_size`、`clear_size`、`read_at`、`write_at` 属于双方共同引用的 easy-fs 库，非参赛队伍原创；(3) 目标项目在 `exit_current_and_run_next` 中额外包含 `PageTable::from_token(token).translate_va` 等调用，体现独立的地址空间清理逻辑。

### 中断/异常处理
- **双方均存在**：是
- **共同 AST 指纹**：3
- **覆盖率**：目标 60.0%，参考 60.0%
- **证据强度**：**弱**
- **解读**：双方均实现了 `set_kernel_trap_entry`、`set_user_trap_entry`、`enable_timer_interrupt` 等函数，调用 `stvec::write`、`sie::set_stimer` 等 RISC-V 特权指令。这些是 RISC-V 架构的标准 CSR 操作，任何 RISC-V 内核都必须如此实现，不具独创性。`trap_from_kernel` 中双方均使用 `panic!`，这是内核陷阱处理的唯一合理策略。

### 系统调用
- **双方均存在**：是
- **共同 AST 指纹**：0
- **覆盖率**：目标 0.0%，参考 0.0%
- **证据强度**：**未形成**
- **解读**：目标项目仅 4 个函数被归类为系统调用模块，参考项目有 61 个。双方在该模块未观察到任何共同结构指纹。目标项目的 `futex_timespec_us`、`read_futex_word`、`sys_enable_deadlock_detect` 等 futex 相关函数在参考项目中无对应实现，说明目标项目有独立的系统调用扩展方向。

### 文件系统
- **双方均存在**：是
- **共同 AST 指纹**：57
- **覆盖率**：目标 32.95%，参考 18.27%
- **证据强度**：**弱**
- **解读**：共同指纹主要来自双方共同引用的 easy-fs 库（`layout.rs` 中的 `read_at`、`write_at`、`clear_size`、`increase_size`）。目标项目额外实现了 ext4 文件系统支持（`os/src/fs/ext4.rs`），包含 `ext4_list_dir`、`ext4_metadata`、`path_variants` 等独立函数，参考项目使用 fatfs。两者文件系统后端架构不同。`os/src/fs/inode.rs#truncate` 与参考项目 `os/src/fatfs/lfn.rs#truncate` 虽同名，但操作对象完全不同（inode 数据缓冲区 vs UCS-2 长文件名数组），属于跨领域同名巧合。

### 设备驱动
- **双方均存在**：是
- **共同 AST 指纹**：1
- **覆盖率**：目标 12.5%，参考 11.11%
- **证据强度**：**弱**
- **解读**：双方均实现了 virtio 块设备驱动，但实现方式不同。目标项目使用 `READ_BOUNCE_BUFFER`/`WRITE_BOUNCE_BUFFER` 全局缓冲区，参考项目通过 `self.0.exclusive_access().read_block(block_id, buf)` 直接调用。`block_device_test` 的相似性源于双方均对块设备进行基本读写测试，属于功能测试的标准模式。`phys_to_virt` 为内部闭环逻辑，无外部调用。

### 网络协议栈
- **双方均存在**：否
- **共同 AST 指纹**：0
- **覆盖率**：目标 0.0%，参考 0.0%
- **证据强度**：**未形成**
- **解读**：双方均未实现网络协议栈，无对比基础。

### 第三方库/公共依赖
- **双方均存在**：是
- **共同 AST 指纹**：268
- **覆盖率**：目标 100.0%，参考 14.43%
- **证据强度**：**弱（排除项）**
- **解读**：目标项目覆盖率 100% 是因为其第三方库模块（709 个函数）的 AST 指纹全部在参考项目中找到对应，说明双方引用了完全相同的上游依赖（`riscv`、`virtio-drivers` 等 crate 的 vendored 副本）。这些代码来自公共开源仓库，非参赛队伍原创，不应计入抄袭判定。参考项目覆盖率仅 14.43% 是因为其第三方库规模更大（6558 个函数），包含目标项目未引用的其他依赖。

---

## 3. 底层 AST 关键证据指纹表

| 所属板块 | 目标项目路径 | 参考项目路径 | 证据类型 | 技术说明 |
| :--- | :--- | :--- | :--- | :--- |
| 进程管理 | `os/src/task/processor.rs#schedule` | `os/src/task/processor.rs#schedule` | 框架约束薄封装 | rCore 风格 `Processor` + `__switch` 任务切换标准模式，可实现空间极小 |
| 进程管理 | `os/src/task/processor.rs#current_trap_cx_user_va` | `os/src/task/processor.rs#current_trap_cx_user_va` | 框架约束薄封装 | 6 层链式调用由 `TaskControlBlock` 的 `Option<Arc<...>>` 嵌套结构决定，安全 Rust 必须逐层 unwrap |
| 进程管理 | `os/src/task/manager.rs#remove_from_pid2process` | `os/src/task/manager.rs#remove_from_pid2process` | 框架约束薄封装 | 全局 `BTreeMap` 删除 + `is_none` 检查 + `panic!` 是标准防御式编程模式 |
| 进程管理 | `os/src/syscall/process.rs#sys_exec` | `os/src/syscall/process.rs#sys_exec` | 同名但实现差异 | 目标使用 `all_data.iter().position` 迭代器解析参数；参考使用 `args.add`/`args_vec.push`。参数解析策略不同 |
| 进程管理 | `os/src/syscall/process.rs#sys_kill` | `os/src/syscall/process.rs#sys_kill` | 同名但实现差异 | 目标支持进程组信号（`send_signal_to_process_group`）和全局进程遍历（`all_processes`）；参考仅按 PID 查找（`pid2process`） |
| 进程管理 | `os/src/syscall/process.rs#sys_times` | `os/src/syscall/process.rs#sys_times` | 同名但实现差异 | 目标使用原始指针字节拷贝（`core::ptr::copy_nonoverlapping`）；参考使用 `translated_refmut` 地址翻译 |
| 进程管理 | `os/src/syscall/process.rs#sys_get_time` | `os/src/syscall/process.rs#sys_get_time` | 同名但实现差异 | 目标使用微秒级 `get_time_us` + 手动 `copy_nonoverlapping`；参考使用毫秒级 `get_time_ms` |
| 进程管理 | `os/src/syscall/process.rs#sys_exit` | `os/src/syscall/process.rs#sys_exit` | 同名但实现差异 | 目标额外调用 `release_process_file_locks` 进行文件锁清理，体现独立资源管理设计 |
| 进程管理 | `os/src/task/judge.rs#add_initproc` | `os/src/task/mod.rs#add_initproc` | 跨文件同名但功能不同 | 目标为评测状态机初始化（`JUDGE_STATE`、`QEMU_EXIT_HANDLE`）；参考为 `INITPROC.clone`。功能语义完全不同 |
| 内存管理 | `os/src/task/process.rs#fork` | `os/src/task/process.rs#fork` | 框架约束相似 | `MemorySet::from_existed_user`、`RecycleAllocator::new` 等为 rCore 内存管理标准 API |
| 内存管理 | `os/src/trap/mod.rs#trap_handler` | `os/src/trap/mod.rs#trap_handler` | 框架约束相似 | `check_signals_of_current`、`check_timer`、`current_add_signal` 等为 rCore 陷阱处理标准流程 |
| 文件系统 | `easy-fs/src/layout.rs#read_at` | `easy-fs/src/layout.rs#read_at` | 第三方库（easy-fs） | 双方共同引用的 easy-fs 库代码，非参赛队伍原创 |
| 文件系统 | `easy-fs/src/layout.rs#write_at` | `easy-fs/src/layout.rs#write_at` | 第三方库（easy-fs） | 双方共同引用的 easy-fs 库代码，非参赛队伍原创 |
| 文件系统 | `easy-fs/src/layout.rs#increase_size` | `easy-fs/src/layout.rs#increase_size` | 第三方库（easy-fs） | 双方共同引用的 easy-fs 库代码，非参赛队伍原创 |
| 文件系统 | `easy-fs/src/layout.rs#clear_size` | `easy-fs/src/layout.rs#clear_size` | 第三方库（easy-fs） | 双方共同引用的 easy-fs 库代码，非参赛队伍原创 |
| 设备驱动 | `os/src/drivers/block/virtio_blk.rs#read_block` | `os/src/drivers/block/virtio_blk.rs#read_block` | 同名但实现差异 | 目标使用全局 `READ_BOUNCE_BUFFER`；参考通过 `self.0.exclusive_access().read_block()` 直接调用 |
| 第三方库 | `third_party/riscv/descriptor/generator.rs#generate` | `dependencies/riscv/descriptor/generator.rs#generate` | 第三方库（riscv） | 双方共同引用的 riscv crate vendored 副本，非参赛队伍原创 |
| 第三方库 | `third_party/virtio-drivers/src/queue.rs#add_buffers` | `dependencies/virtio-drivers/src/queue.rs#add_buffers` | 第三方库（virtio-drivers） | 双方共同引用的 virtio-drivers crate vendored 副本，非参赛队伍原创 |
| 第三方库 | `third_party/riscv/src/paging/multi_level.rs#create_p1_if_not_exist` | `dependencies/riscv/src/paging/multi_level.rs#create_p1_if_not_exist` | 第三方库（riscv） | 双方共同引用的 riscv crate vendored 副本，非参赛队伍原创 |

---

## 4. 核心代码调用流对比剖析

### 【进程管理 / `sys_exec` 系统调用路径】

- **底层技术析理**: 双方均实现了 POSIX `exec` 语义，但参数解析与内存布局策略存在实质性差异。目标项目采用迭代器模式处理参数：`all_data.iter().position(...)` 定位分隔符，通过 `all_data.as_slice` 分段解析，调用链包含 55+ 个外部调用，体现复杂的参数向量构建逻辑。参考项目采用 `args.add`/`args_vec.push` 的向量追加模式，调用链仅 12 个外部调用。两者在参数解析的抽象层次和内存操作流上明显不同。

- **代码溯源链接**:
  - 目标项目: `os/src/syscall/process.rs#sys_exec`
  - 参考项目: `os/src/syscall/process.rs#sys_exec`

### 【进程管理 / `sys_kill` 信号派发路径】

- **底层技术析理**: 目标项目实现了进程组信号支持，调用 `send_signal_to_process_group` 和 `all_processes` 全局进程遍历，信号派发模型为“广播式”。参考项目仅通过 `pid2process` 按 PID 查找单个进程，调用 `SignalFlags::from_bits` 设置信号位，信号派发模型为“点对点式”。两者在信号管理的抽象层次（进程组 vs 单进程）和同步策略上存在架构级差异。

- **代码溯源链接**:
  - 目标项目: `os/src/syscall/process.rs#sys_kill`
  - 参考项目: `os/src/syscall/process.rs#sys_kill`

### 【进程管理 / `sys_times` 地址翻译策略】

- **底层技术析理**: 目标项目采用原始指针字节拷贝策略：通过 `(&t as *const Tms).cast::<u8>()` 获取结构体字节指针，使用 `core::ptr::copy_nonoverlapping` 直接向用户空间拷贝字节，绕过地址翻译层。参考项目使用 `translated_refmut` 进行地址翻译后通过 `tms.add` 写入。两者在内存模型（直接物理拷贝 vs 页表翻译）上存在根本性差异。

- **代码溯源链接**:
  - 目标项目: `os/src/syscall/process.rs#sys_times`
  - 参考项目: `os/src/syscall/process.rs#sys_times`

### 【进程管理 / `sys_exit` 资源清理路径】

- **底层技术析理**: 目标项目在进程退出时额外调用 `crate::syscall::fs::release_process_file_locks` 进行文件锁清理，体现独立的文件系统资源管理设计。参考项目仅调用 `exit_current_and_run_next` 和 `panic!`。目标项目还使用 `trace!` 宏进行日志记录，参考项目无此调用。两者在资源清理的完整性和日志策略上存在差异。

- **代码溯源链接**:
  - 目标项目: `os/src/syscall/process.rs#sys_exit`
  - 参考项目: `os/src/syscall/process.rs#sys_exit`

### 【进程管理 / `add_initproc` 初始化路径】

- **底层技术析理**: 目标项目的 `add_initproc` 位于 `os/src/task/judge.rs`，调用 `JUDGE_STATE.exclusive_access`、`QEMU_EXIT_HANDLE.exit_success`、`build_queue`、`spawn_next_action`，是为竞赛评测场景设计的初始化流程，涉及评测状态机和调度队列构建。参考项目的 `add_initproc` 位于 `os/src/task/mod.rs`，仅调用 `INITPROC.clone`，是传统 init 进程克隆启动。两者虽同名，但所在文件、依赖状态、功能语义完全不同，属于跨文件同名巧合。

- **代码溯源链接**:
  - 目标项目: `os/src/task/judge.rs#add_initproc`
  - 参考项目: `os/src/task/mod.rs#add_initproc`

### 【内存管理 / `fork` 进程复制路径】

- **底层技术析理**: 双方均调用 `MemorySet::from_existed_user`、`RecycleAllocator::new`、`SignalFlags::empty` 等 rCore 框架标准 API 进行进程复制。目标项目额外包含 `Arc::clone`、`Arc::downgrade`、`Arc::new` 等智能指针操作，调用链更长（36 项 vs 32 项）。这种相似性源于 rCore 内存管理子系统的框架约束，`MemorySet::from_existed_user` 是复制地址空间的标准入口，`RecycleAllocator::new` 是 PID 分配器的标准初始化。两者在对象管理模型上均遵循 rCore 的 `Arc<UPSafeCell<...>>` 模式，这是框架级别的同源，非参赛队伍间的代码复制。

- **代码溯源链接**:
  - 目标项目: `os/src/task/process.rs#fork`
  - 参考项目: `os/src/task/process.rs#fork`

### 【文件系统 / easy-fs 布局操作】

- **底层技术析理**: `easy-fs/src/layout.rs` 中的 `read_at`、`write_at`、`increase_size`、`clear_size` 四个函数在双方项目中 AST 调用图谱完全一致。经核查，easy-fs 是 rCore 教学操作系统配套的文件系统库，双方均将其作为第三方依赖引入（vendored）。这些函数的相似性源于引用同一公共库，非参赛队伍原创代码，不应计入抄袭判定。

- **代码溯源链接**:
  - 目标项目: `easy-fs/src/layout.rs#read_at`
  - 参考项目: `easy-fs/src/layout.rs#read_at`

---

## 5. 单方模块与降权项

### 目标项目独有模块/功能
| 项目 | 说明 | 降权理由 |
| :--- | :--- | :--- |
| `os/src/fs/ext4.rs` | ext4 文件系统支持（`ext4_list_dir`、`ext4_metadata`、`path_variants`、`read`） | 参考项目使用 fatfs，文件系统后端完全不同，体现独立架构选择 |
| `os/src/syscall/sync.rs#sys_enable_deadlock_detect` | 死锁检测功能 | 参考项目无此功能，为目标项目独有扩展 |
| `os/src/syscall/sync.rs#futex_timespec_us` / `read_futex_word` | futex 系统调用支持 | 参考项目无 futex 实现，为目标项目独有扩展 |
| `os/src/syscall/process.rs#sys_rt_sigsuspend` | 实时信号挂起 | 参考项目无此系统调用 |
| `os/src/syscall/process.rs#sys_waitpid` | 完整的 waitpid 实现 | 参考项目无对应证据 |
| `os/src/task/judge.rs` | 评测状态机 | 参考项目无此模块，为竞赛场景独立设计 |

### 参考项目独有模块/功能
| 项目 | 说明 | 降权理由 |
| :--- | :--- | :--- |
| `user/src/bin/initproc.rs` / `user/src/bin/forktree.rs` | 用户态测试程序 | 属于用户空间程序，与内核进程管理不在同一抽象层，不能用于证明内核代码抄袭 |
| `os/src/fatfs/` | FAT 文件系统支持 | 目标项目使用 ext4，文件系统后端不同 |
| `modified_dependencies/rust-fatfs/` | fatfs 第三方库 | 目标项目未引用此库 |

### 跨板块同名降权项
| 目标证据 | 参考证据 | 降权理由 |
| :--- | :--- | :--- |
| `os/src/fs/inode.rs#truncate` | `os/src/fatfs/lfn.rs#truncate` | 虽同名 `truncate`，但操作对象完全不同：目标为 inode 数据缓冲区截断，参考为 UCS-2 长文件名数组截断。属于跨领域同名巧合 |
| `os/src/fs/ext4.rs#ext4_list_dir` | `modified_dependencies/rust-fatfs/src/fs.rs#format_volume` | 跨文件系统后端、跨功能（目录列表 vs 卷格式化），无同源性 |
| `os/src/sync/mutex.rs#lock` | `user/src/bin/adder_peterson_spin.rs#lock` | 内核态互斥锁 vs 用户态自旋锁，跨抽象层，且实现机制不同（`UPSafeCell` + `suspend_current_and_run_next` vs `compiler_fence` + `vload!`） |

### 第三方库/公共依赖降权项
| 路径 | 降权理由 |
| :--- | :--- |
| `third_party/riscv/` 下所有文件 | 双方共同引用的 riscv crate vendored 副本，来自公共开源仓库 |
| `third_party/virtio-drivers/` 下所有文件 | 双方共同引用的 virtio-drivers crate vendored 副本，来自公共开源仓库 |
| `easy-fs/src/layout.rs` 下所有文件 | 双方共同引用的 easy-fs 库，来自 rCore 教学配套仓库 |
| `third_party/` 下其他 crate | 均为上游开源依赖的 vendored 副本 |

---

## 6. 保守结论

本次审计基于 AST 调用图谱对目标项目 `[2026-T2026103369910066-XX]` 与参考项目 `[2024-刘奶奶找牛奶奶买牛奶队-湖北民族大学]` 进行同源性定性仲裁。总体 AST 相似度为 13.89%，参考项目覆盖率仅 15.92%，数据层面不支持抄袭结论。进程管理模块中控方所指控的 21 处“底层逻辑一致”函数，经逐条审查，绝大多数为 rCore 教学框架约束下的薄封装函数（如 `current_task`、`getpid`、`inner_exclusive_access` 等），其 AST 调用图谱的相似性源于 Rust 类型系统与框架 API 的必然约束，可实现空间极小，不构成独创性表达。核心系统调用（`sys_exec`、`sys_kill`、`sys_times`、`sys_get_time`、`sys_exit`）在参数解析策略、信号模型、地址翻译方式、资源清理逻辑等方面存在实质性实现差异。目标项目额外实现了 ext4 文件系统、futex 系统调用、死锁检测、评测状态机等独立功能模块，体现独立的架构设计。双方在 easy-fs、riscv、virtio-drivers 等第三方库上的代码一致源于引用同一公共依赖，非参赛队伍间代码复制。综合判定：目标项目为独立开发，不构成对参考项目的实质性代码抄袭。

------------------------------

