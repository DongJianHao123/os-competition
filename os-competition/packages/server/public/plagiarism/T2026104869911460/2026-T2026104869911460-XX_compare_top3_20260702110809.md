# 🛡️ OS 内核赛自动化代码级架构审计报告

**审计时间**: `2026-07-02 11:08:09`
**参赛作品名称**: `2026-T2026104869911460-XX`
**年份**: `2026`
**学校**: `XX`
**队伍名称**: `T2026104869911460`
**比赛名字**: `OS-Competition`
**比赛子赛道**: `Kernel-Implementation`
**仓库地址**: `https://gitlab.eduxiji.net/educg-group-43501-3132633/T2026104869911460-387`
**推测开发语言**: `Rust / C (由AST探针自动识别)`
**特征函数量**: `4298`

## ⚔️ 底层代码同源性对比 (AST Homology)
> 本部分由专门的代码审查智能体，对该项目与符合年份条件的历届最高相似度项目进行控制流与底层调用对比。新版报告会先给出确定性的全模块覆盖矩阵，再给出 Agent 的分模块解释，避免只围绕少量抽样函数展开。

### 🏆 Top 3 底层对标: vs `2022-Maturin-清华大学`（清华大学 / Maturin）

#### 对方作品信息

**对方年份**: `2022`
**对方学校**: `清华大学`
**对方队伍名称**: `Maturin`
**对方比赛名字**: `OS-Competition`
**对方比赛子赛道**: `Kernel-Implementation`
**对方仓库地址**: `https://gitlab.eduxiji.net/scPointer/maturin.git`

> 双向覆盖率：共同 AST 指纹: 270；唯一 AST 指纹: 目标项目 3358，参考项目 1491；覆盖率: 目标项目 8.04%，参考项目 18.11%

## 全模块对比总览（确定性证据）

> 总体覆盖率：共同 AST 指纹: 270；唯一 AST 指纹: 目标项目 3358，参考项目 1491；覆盖率: 目标项目 8.04%，参考项目 18.11%

### 1. 模块覆盖矩阵
| 模块 | 目标函数数 | 参考函数数 | 共同唯一 AST 指纹 | Target 覆盖 | Base 覆盖 | 状态 |
| :--- | ---: | ---: | ---: | ---: | ---: | :--- |
| 进程管理 | 328 | 60 | 1 | 0.47% | 1.82% | 双方存在且有结构重合 |
| 任务调度/异步运行时 | 595 | 160 | 6 | 1.08% | 4.29% | 双方存在且有结构重合 |
| 内存管理 | 472 | 275 | 18 | 4.09% | 6.77% | 双方存在且有结构重合 |
| 中断/异常处理 | 109 | 4 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |
| 系统调用 | 146 | 8 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |
| 文件系统 | 1333 | 593 | 228 | 22.57% | 42.3% | 双方存在且有结构重合 |
| 设备驱动 | 230 | 15 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |
| 网络协议栈 | 75 | 8 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |
| 文件系统/第三方库 | 392 | 0 | 0 | 0.0% | 0.0% | 仅目标项目体现 |
| 第三方库 | 0 | 335 | 0 | 0.0% | 0.0% | 仅参考项目体现 |
| 通用模块/其他 | 618 | 218 | 2 | 0.43% | 0.97% | 双方存在且有结构重合 |

### 2. 分模块证据清单

#### 进程管理
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `NoAxiom/kernel/src/syscall/process.rs#sys_execve` | `kernel/src/syscall/process.rs#sys_execve` | 同板块同名 | Err, FileFlags::empty, Ok, SearchFlags::empty, Self::executable_has_write_open, String::from ...(+30) | sys_exec |
| `NoAxiom/kernel/src/syscall/process.rs#sys_wait4` | `kernel/src/syscall/process.rs#sys_wait4` | 同板块同名 | Err, Ok, PidSel::Group, PidSel::Task, Some, UserPtr::new ...(+15) | Err, Ok, info!, option.contains, suspend_current_task, waitpid |
| `NoAxiom/kernel/src/syscall/process.rs#sys_tkill` | `kernel/src/syscall/process.rs#sys_tkill` | 同板块同名 | Err, Ok, Signal::try_from_with_zero_as_none, TASK_MANAGER.get, TASK_MANAGER.get(tid).ok_or, self.__sys_tkill | Err, Ok, send_signal |
| `NoAxiom/kernel/src/syscall/process.rs#sys_getppid` | `kernel/src/syscall/process.rs#sys_getppid` | 同板块同名 | Err, parent_process
                .upgrade, parent_process
                .upgrade()
                .map, parent_process
                .upgrade()
                .map(|task| task.tid() as isize)
                .ok_or, self.task.pcb, self.task.pcb().parent.clone ...(+1) | Ok, get_current_task, get_current_task().unwrap, get_current_task().unwrap().get_ppid |

#### 任务调度/异步运行时
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `NoAxiom/lib/fatfs/src/dir.rs#read_dir_entry` | `rust-fatfs/src/dir.rs#read_dir_entry` | 同板块同名 | DirEntryData::deserialize, LongNameBuilder::new, Ok, SeekFrom::Current, ShortName::new, Some ...(+13) | DirEntryData::deserialize, LongNameBuilder::new, Ok, SeekFrom::Current, ShortName::new, Some ...(+12) |
| `NoAxiom/kernel/src/syscall/fs.rs#sys_linkat` | `kernel/src/syscall/fs.rs#sys_linkat` | 同板块同名 | Arc::ptr_eq, AtFlags::AT_EMPTY_PATH.bits, AtFlags::AT_SYMLINK_FOLLOW.bits, AtFlags::from_bits_retain, Err, Ok ...(+23) | Err, Ok, get_current_task, get_current_task().unwrap, resolve_path_from_fd, try_add_link |
| `NoAxiom/kernel/src/syscall/fs.rs#sys_lseek` | `kernel/src/syscall/fs.rs#sys_lseek` | 同板块同名 | Err, SeekFrom::Current, SeekFrom::End, SeekFrom::Start, Whence::from_repr, Whence::from_repr(whence).ok_or ...(+8) | Err, Ok, SeekFrom::Current, SeekFrom::End, SeekFrom::Start, file.seek ...(+5) |
| `NoAxiom/kernel/src/syscall/fs.rs#sys_mount` | `kernel/src/syscall/fs.rs#sys_mount` | 同板块同名 | Err, MountFlags::from_bits, MountFlags::from_bits(flags as u32).ok_or, Ok, SearchFlags::empty, Self::is_known_mount_fstype ...(+16) | Err, Ok, get_current_task, get_current_task().unwrap, mount_fat_fs, mount_path.ends_with ...(+3) |

#### 内存管理
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `NoAxiom/kernel/src/mm/validate.rs#validate` | `rust-fatfs/src/boot_sector.rs#validate` | 同板块同名 | Arch::current_root_ppn, Arch::tlb_flush, Err, MappingFlags::from, Ok, PageTable::from_ppn ...(+53) | Err, Ok, error!, self.validate_bytes_per_sector, self.validate_fats, self.validate_reserved_sectors ...(+5) |
| `NoAxiom/kernel/src/trap/utrap_handler.rs#user_trap_handler` | `kernel/src/trap/mod.rs#user_trap_handler` | 同板块同名 | Arch::disable_interrupt, Arch::enable_interrupt, SigInfo::new_simple, Signal::SIGILL.into, Signal::SIGTRAP.into, ext_int_handler ...(+15) | get_current_task, get_current_task().unwrap, get_current_task().unwrap().get_tid_num, handle_signals, handle_user_page_fault, info! ...(+11) |
| `NoAxiom/lib/fatfs/src/fs.rs#format_volume` | `rust-fatfs/src/fs.rs#format_volume` | 同板块同名 | DirFileEntryData::new, Err, Ok, SeekFrom::End, SeekFrom::Start, alloc_cluster ...(+27) | DirFileEntryData::new, Err, Ok, SeekFrom::End, SeekFrom::Start, alloc_cluster ...(+28) |
| `NoAxiom/kernel/src/syscall/fs.rs#sys_utimensat` | `kernel/src/syscall/fs.rs#sys_utimensat` | 同板块同名 | AtFlags::AT_EMPTY_PATH.bits, AtFlags::AT_SYMLINK_NOFOLLOW.bits, AtFlags::from_bits_truncate, Err, Ok, Self::check_readonly_mount ...(+34) | Err, Ok, OpenFlags::empty, TimeSpec::now, check_dir_exists, check_file_exists ...(+16) |

#### 中断/异常处理
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `NoAxiom/lib/arch/src/la64/interrupt.rs#enable_timer_interrupt` | `kernel/src/trap/mod.rs#enable_timer_interrupt` | 同板块同名 | ecfg::set_lie | sie::set_stimer |
| `NoAxiom/kernel/src/include/fs.rs#new` | `libc/src/functional/fcntl.c#main` | 同模块上下文对齐/差异观察 | 内部闭环逻辑 | TEST, TESTE, _exit, fclose, fcntl, fileno ...(+4) |
| `NoAxiom/kernel/src/signal/sig_action.rs#new_default` | `kernel/src/signal/sig_action.rs#get_restorer` | 同模块上下文对齐/差异观察 | 内部闭环逻辑 | self.flags.contains |
| `NoAxiom/kernel/src/trap/soft_int.rs#ipi_handler` | `kernel/src/trap/mod.rs#init` | 同模块上下文对齐/差异观察 | Arch::clear_ipi, Arch::tlb_flush, current_ipi_info, info!, trace! | stvec::write |

#### 系统调用
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `NoAxiom/kernel/src/syscall/net.rs#write_accept_addr` | `kernel/src/syscall/flags.rs#default` | 同模块上下文对齐/差异观察 | AddressFamily::try_from, Err, Ok, Self::check_socklen_ptr, SockAddr::from_endpoint, UserPtr::<i32>::new ...(+10) | Self::from_str |
| `NoAxiom/kernel/src/syscall/ipc.rs#sys_semctl` | `libc/src/regression/syscall-sign-extend.c#main` | 同模块上下文对齐/差异观察 | Ok, SYSV_SEM_MANAGER.lock, SYSV_SEM_MANAGER.lock().ctl_scalar, SYSV_SEM_MANAGER.lock().get_all, SYSV_SEM_MANAGER.lock().info, SYSV_SEM_MANAGER.lock().remove ...(+16) | T, open, syscall, t_error |
| `NoAxiom/kernel/src/syscall/mm.rs#sys_shmctl` | `kernel/src/syscall/flags.rs#set_info` | 同模块上下文对齐/差异观察 | IpcCtlCmd::from_repr, IpcCtlCmd::from_repr(cmd).ok_or, Ok, SHM_MANAGER.lock, SHM_MANAGER.lock().ipc_info, SHM_MANAGER.lock().linux_stat ...(+14) | 内部闭环逻辑 |
| `NoAxiom/kernel/src/syscall/net.rs#validate_socket_protocol` | `kernel/src/syscall/flags.rs#resolve_clone_flags_and_signal` | 同模块上下文对齐/差异观察 | Err, Ok | CloneFlags::from_bits, CloneFlags::from_bits(flag as u32 & (!0x3f)).unwrap, SignalNo::try_from, SignalNo::try_from(flag as u8 & 0x3f).unwrap |

#### 文件系统
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `NoAxiom/lib/fatfs/src/boot_sector.rs#determine_sectors_per_fat` | `rust-fatfs/src/boot_sector.rs#determine_sectors_per_fat` | 同板块同名同结构 | fat_type.bits_per_fat_entry, u32::from, u64::from | fat_type.bits_per_fat_entry, u32::from, u64::from |
| `NoAxiom/lib/fatfs/src/boot_sector.rs#format_bpb` | `rust-fatfs/src/boot_sector.rs#format_bpb` | 同板块同名 | Err, FatType::from_clusters, Ok, bpb.total_clusters, determine_fs_layout, error! ...(+5) | Err, FatType::from_clusters, Ok, assert!, bpb.total_clusters, debug_assert! ...(+15) |
| `NoAxiom/lib/fatfs/src/file.rs#seek` | `rust-fatfs/src/file.rs#seek` | 同板块同名 | Err, Ok, Some, debug_assert!, error!, i64::from ...(+16) | Err, Ok, Some, debug_assert!, error!, i64::from ...(+16) |
| `NoAxiom/lib/fatfs/src/boot_sector.rs#test_determine_sectors_per_fat_single` | `rust-fatfs/src/boot_sector.rs#test_determine_sectors_per_fat_single` | 同板块同名 | FatType::from_clusters, assert!, debug_assert!, determine_sectors_per_fat, fat_type.bits_per_fat_entry, format! ...(+2) | FatType::from_clusters, assert!, debug_assert!, determine_sectors_per_fat, fat_type.bits_per_fat_entry, format! ...(+2) |

#### 设备驱动
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `NoAxiom/lib/driver/src/impls/block/vf2_sdcard/mod.rs#read_block` | `kernel/src/drivers/block/virtio_block.rs#read_block` | 同板块同名 | CmdArg::new, CmdReg::from, DataTransType::Read, Ok, assert_eq!, buf.len ...(+3) | self.0
            .lock, self.0
            .lock()
            .read_block, self.0
            .lock()
            .read_block(block_id, buf)
            .expect |
| `NoAxiom/lib/driver/src/impls/block/vf2_sdcard/mod.rs#write_block` | `kernel/src/drivers/block/virtio_block.rs#write_block` | 同板块同名 | CmdArg::new, CmdReg::from, DataTransType::Write, Ok, assert_eq!, buf.len ...(+3) | self.0
            .lock, self.0
            .lock()
            .write_block, self.0
            .lock()
            .write_block(block_id, buf)
            .expect |
| `NoAxiom/lib/driver/src/impls/display/virtio_gpu.rs#flush` | `kernel/src/drivers/memory/wrapper.rs#flush` | 同板块同名 | self.gpu.lock, self.gpu.lock().flush, self.gpu.lock().flush().unwrap | TransferError, self.inner.flush, self.inner.flush().map_err |
| `NoAxiom/lib/driver_ahci/src/drv_ahci.rs#ahci_host_init` | `kernel/src/drivers/block/mod.rs#block_device_test` | 同模块上下文对齐/差异观察 | ahci_mdelay, ahci_port_base, ahci_readl, ahci_writel, log::debug! | BLOCK_DEVICE.clone, assert_eq!, block_device.read_block, block_device.write_block, println!, write_buffer.iter_mut |

#### 网络协议栈
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `NoAxiom/kernel/src/net/tcpsocket.rs#poll_inner` | `libc/src/functional/inet_pton.c#main` | 同模块上下文对齐/差异观察 | PollEvent::empty, SOCKET_SET.lock, matches!, poll_ifaces, req.intersects, self.shutdown_type.contains ...(+8) | V4, V6, inet_ntop, inet_pton, strerror, t_error |
| `NoAxiom/kernel/src/net/mod.rs#net_udp_test` | `libc/src/functional/socket.c#main` | 同模块上下文对齐/差异观察 | Instant::from_millis, LoopBackDev::new, SocketSet::new, SpinLock::new, client.bind, client.bind(233).unwrap ...(+26) | TEST, TESTE, accept, bind, close, connect ...(+9) |
| `NoAxiom/kernel/src/net/tcpsocket.rs#connect` | `libc/src/regression/inet_pton-empty-last-field.c#main` | 同模块上下文对齐/差异观察 | Err, Ok, SOCKET_SET.lock, debug!, drop, error! ...(+5) | inet_pton, memcmp, t_error, txt |
| `NoAxiom/kernel/src/include/net.rs#new` | `libc/src/regression/inet_ntop-v4mapped.c#main` | 同模块上下文对齐/差异观察 | AddressFamily::try_from, Err, Ok, core::cmp::min, core::mem::size_of::<SockAddrIpv4>, core::mem::size_of::<SockAddrIpv6> ...(+16) | inet_ntop, inet_pton, strchr, t_error |

#### 文件系统/第三方库
仅目标项目中观察到该模块代表函数，参考项目没有同模块可对齐证据：
- 目标项目：`NoAxiom/lib/ext4_rs/src/ext4_impls/extents.rs#ext_remove_leaf`，调用摘要：Block::load, Block::load_inode_root_block, Ext4Extent::default, Ok, ex.get_actual_len, ex.get_pblock ...(+21)
- 目标项目：`NoAxiom/lib/ext4_rs/src/ext4_impls/extents.rs#extent_remove_space`，调用摘要：Block::load, Ext4Extent::default, Ok, ex.get_actual_len, ex.get_first_block, ex.get_pblock ...(+13)
- 目标项目：`NoAxiom/lib/ext4_rs/src/ext4_impls/balloc.rs#balloc_alloc_block`，调用摘要：Block::load, Ext4BlockGroup::load_new, Ok, block_group.get_block_bitmap_block, block_group.get_free_blocks_count, block_group.set_block_group_balloc_bitmap_csum ...(+16)
- 目标项目：`NoAxiom/lib/ext4_rs/src/ext4_impls/balloc.rs#balloc_alloc_block_from`，调用摘要：Block::load, Ext4BlockGroup::load_new, Ok, block_group.get_block_bitmap_block, block_group.get_free_blocks_count, block_group.set_block_group_balloc_bitmap_csum ...(+15)

#### 第三方库
仅参考项目中观察到该模块代表函数，目标项目没有同模块可对齐证据：
- 参考项目：`dependencies/core2/src/io/buffered.rs#write`，调用摘要：Ok, memchr::memrchr, self.buffer.capacity, self.buffer.flush_buf, self.buffer.write, self.buffer.write_to_buf ...(+3)
- 参考项目：`dependencies/core2/src/io/buffered.rs#flush_buf`，调用摘要：BufGuard::new, Err, Error::new, Ok, e.kind, guard.consume ...(+7)
- 参考项目：`dependencies/core2/src/io/traits.rs#read_to_end_with_reservation`，调用摘要：Err, Ok, assert!, buf.len, e.kind, g.buf.capacity ...(+7)
- 参考项目：`dependencies/bitmap-allocator/src/lib.rs#bitalloc_contiguous`，调用摘要：BitAlloc16::default, BitAlloc4K::default, assert_eq!, ba.dealloc, ba.insert, ba.remove ...(+2)

#### 通用模块/其他
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `NoAxiom/kernel/src/time/timer.rs#check` | `kernel/src/signal/shadow_bitset.rs#check` | 同板块同名 | Reverse, debug!, get_time_duration, self.timers.lock, timer.callback, timers.peek ...(+4) | self.addr.add |
| `NoAxiom/kernel/src/ipc.rs#info` | `kernel/src/console.rs#info` | 同板块同名 | (SEMMNI * 8).max, self.sets.len, self.sets.len().saturating_sub, self.sets.values, self.sets.values().map, self.sets.values().map(|set| set.values.len()).sum ...(+1) | crate::arch::stdout::stdout_puts |
| `NoAxiom/lib/ksync/src/async_lock/once_cell.rs#set` | `libc/src/functional/ipc_shm.c#set` | 同板块同名 | Err, Ok, Some, self.get_or_init, self.get_unchecked, value.take ...(+1) | EQ, T, exit, ftok, getegid, geteuid ...(+9) |
| `NoAxiom/kernel/src/ipc.rs#set` | `libc/src/functional/search_tsearch.c#set` | 同板块同名 | Ok, get_time_s, self.queues.get_mut, self.queues.get_mut(&id).ok_or, stat.msg_qbytes.min, update_perm | get, strcmp, t_error, tsearch |


#### 1. 宏观判定
- **定性结论**: 底层架构显著差异 / 独立开发 (相似度: 5.9%，共同 AST 指纹: 270；唯一 AST 指纹: 目标项目 3358，参考项目 1491；覆盖率: 目标项目 8.04%，参考项目 18.11%)

#### 2. 全模块覆盖矩阵解读
- **进程管理**: 双方均存在该模块。共同 AST 指纹仅 1 个，目标覆盖率 0.47%，参考覆盖率 1.82%。极低的覆盖率表明双方在进程管理的核心实现上高度独立，唯一的共同指纹经查证为第三方库函数。证据强度：**未形成**。
- **任务调度/异步运行时**: 双方均存在该模块。共同 AST 指纹 6 个，目标覆盖率 1.08%，参考覆盖率 4.29%。覆盖率低，且重合点主要源于文件系统操作（如 `fatfs` 目录读取）和系统调用入口，非调度器核心逻辑。证据强度：**弱**。
- **内存管理**: 双方均存在该模块。共同 AST 指纹 18 个，目标覆盖率 4.09%，参考覆盖率 6.77%。覆盖率较低，重合点分散在 `fatfs` 格式化、`validate` 等非内存管理核心的函数上，未发现页表管理、分配器算法等核心逻辑的同源证据。证据强度：**弱**。
- **中断/异常处理**: 双方均存在该模块。共同 AST 指纹为 0，覆盖率为 0.0%。双方在 Trap 处理、中断控制器操作上无任何 AST 结构重合，是完全独立的实现。证据强度：**未形成**。
- **系统调用**: 双方均存在该模块。共同 AST 指纹为 0，覆盖率为 0.0%。尽管存在大量同名函数，但 AST 层面无结构重合，表明实现方式完全不同。证据强度：**未形成**。
- **文件系统**: 双方均存在该模块。共同 AST 指纹 228 个，目标覆盖率 22.57%，参考覆盖率 42.30%。这是唯一高重合的模块，但经查证，所有强证据均指向双方共同依赖的开源第三方库 `rust-fatfs`，属于合法公共依赖，非项目间抄袭。证据强度：**中（可解释为公共依赖）**。
- **设备驱动**: 双方均存在该模块。共同 AST 指纹为 0，覆盖率为 0.0%。双方驱动模型和硬件操作逻辑无任何 AST 重合。证据强度：**未形成**。
- **网络协议栈**: 双方均存在该模块。共同 AST 指纹为 0，覆盖率为 0.0%。网络实现完全独立。证据强度：**未形成**。
- **第三方库/公共依赖**: 目标项目独有 `ext4_rs` 等库，参考项目独有 `core2`、`bitmap-allocator` 等库。双方共有的 `rust-fatfs` 是文件系统高重合度的唯一来源。此模块的重合不构成抄袭证据，而是正常开源生态的体现。

#### 3. 底层 AST 关键证据指纹表
| 所属板块 | 目标项目路径 | 参考项目路径 | 证据类型 | 技术说明 |
| :--- | :--- | :--- | :--- | :--- |
| 第三方库/公共依赖 | `NoAxiom/lib/fatfs/src/dir.rs#process` | `rust-fatfs/src/dir.rs#process` | 同源第三方库 | 双方均使用了开源 crate `rust-fatfs`。该函数是库内部私有辅助函数，其“内部闭环逻辑”是无外部依赖函数的通用特征，不能作为抄袭证据。 |
| 第三方库/公共依赖 | `NoAxiom/lib/fatfs/src/boot_sector.rs#determine_sectors_per_fat` | `rust-fatfs/src/boot_sector.rs#determine_sectors_per_fat` | 同源第三方库 | 完全相同的 FAT 文件系统布局计算逻辑，源于同一个公共库。 |
| 第三方库/公共依赖 | `NoAxiom/lib/fatfs/src/file.rs#seek` | `rust-fatfs/src/file.rs#seek` | 同源第三方库 | 文件内游标移动逻辑完全一致，调用图谱高度重合，是使用同一公共库的铁证。 |
| 进程管理 | `NoAxiom/kernel/src/syscall/process.rs#sys_getppid` | `kernel/src/syscall/process.rs#sys_getppid` | 同名异构实现 | 函数名由 Linux ABI 强制规定。目标项目通过 `parent_process.upgrade()` 弱引用升级获取父进程，参考项目通过 `get_current_task().unwrap().get_ppid()` 全局单例获取，架构完全不同。 |
| 进程管理 | `NoAxiom/kernel/src/syscall/process.rs#sys_wait4` | `kernel/src/syscall/process.rs#sys_wait4` | 同名异构实现 | 目标项目使用 `PidSel` 枚举和 `WaitOption` 位掩码进行参数解析，参考项目直接调用内部辅助函数 `waitpid`，实现路径和对象模型截然不同。 |
| 进程管理 | `NoAxiom/kernel/src/syscall/process.rs#sys_execve` | `kernel/src/syscall/process.rs#sys_execve` | 同名异构实现 | 目标项目实现了完整的路径搜索、写执行检查和 `UserPtr` 封装，参考项目仅简单转发给 `sys_exec`，复杂度和抽象层次差异巨大。 |
| 内存管理 | `NoAxiom/kernel/src/mm/validate.rs#validate` | `rust-fatfs/src/boot_sector.rs#validate` | 跨板块同名 | 虽函数同名，但目标项目属于内存管理（页表验证），参考项目属于文件系统（FAT 引导扇区验证），是完全不同的技术领域，不构成同源证据。 |
| 任务调度/异步运行时 | `NoAxiom/kernel/src/syscall/fs.rs#sys_mount` | `kernel/src/syscall/fs.rs#sys_mount` | 同名异构实现 | 目标项目使用 `MountFlags` 位掩码和 `SearchFlags` 进行挂载选项解析，参考项目通过字符串匹配 `mount_path.ends_with` 判断文件系统类型，实现逻辑完全不同。 |

#### 4. 核心代码调用流对比剖析
* **【进程管理/任务模型】**
* **底层技术析理**: 双方获取当前任务的方式揭示了根本性的架构差异。参考项目采用全局单例模式 `get_current_task().unwrap()`，这是一种简单的全局变量访问。目标项目则采用上下文传递模式，通过 `self.task` 访问当前任务控制块，并引入 `TASK_MANAGER`、`PROCESS_GROUP_MANAGER` 等显式管理器进行任务索引和生命周期管理。这体现了从“全局状态”到“依赖注入”的架构演进，两者在对象模型和同步策略上完全不同。
* **代码溯源链接**:
  - 目标项目: `NoAxiom/kernel/src/syscall/process.rs#sys_getpid`
  - 参考项目: `kernel/src/syscall/process.rs#sys_getpid`

* **【内存管理/用户指针抽象】**
* **底层技术析理**: 在系统调用参数传递上，双方展现了不同的安全抽象层次。参考项目使用原始指针和手动转换函数，如 `raw_ptr_to_string`、`str_ptr_array_to_vec_string`，这是较为底层的 C 语言风格。目标项目则定义了一个泛型安全抽象 `UserPtr<T>`，在编译期和运行期对用户空间指针的读写进行检查和封装，如 `UserPtr::<i32>::new(arg2).write`。这代表了在内存安全和抽象层次上的独立设计与重大提升。
* **代码溯源链接**:
  - 目标项目: `NoAxiom/kernel/src/syscall/process.rs#sys_prctl`
  - 参考项目: `kernel/src/syscall/process.rs#sys_exec`

* **【中断/异常处理/Trap 处理】**
* **底层技术析理**: 用户态 Trap 处理流程是内核的核心逻辑。参考项目的 `user_trap_handler` 直接调用 `handle_user_page_fault`、`handle_signals` 等具体处理函数。目标项目的 `user_trap_handler` 则展现了更复杂的控制流，包括架构级的中断禁用/启用操作 (`Arch::disable_interrupt`)、信号信息的预构造 (`SigInfo::new_simple`) 以及外部中断处理的分发。两者在调用流和职责划分上存在显著差异。
* **代码溯源链接**:
  - 目标项目: `NoAxiom/kernel/src/trap/utrap_handler.rs#user_trap_handler`
  - 参考项目: `kernel/src/trap/mod.rs#user_trap_handler`

#### 5. 单方模块与降权项
- **单方模块**: 目标项目的 `文件系统/第三方库` (如 `ext4_rs`) 和参考项目的 `第三方库` (如 `core2`, `bitmap-allocator`) 均为对方所无，是独立选型的结果。
- **公共接口与泛型函数名**: 所有 `sys_*` 系统调用函数名均由 Linux/POSIX 标准 ABI 强制规定，任何兼容内核都必须实现，不能作为同源证据。
- **跨板块同名降权**: 如 `validate`、`check`、`info`、`set` 等通用动词命名，在双方项目中出现在完全不同的模块和上下文中，属于常规编程命名习惯，不构成同源证据。
- **libc 测试用例**: 参考项目中大量 `libc/src/` 路径下的 C 语言测试文件，属于用户空间测试程序，与目标项目的内核实现处于不同抽象层级，其对比结果必须降权处理，不能作为内核代码抄袭的证据。

#### 6. 保守结论
综合全模块 AST 指纹分析，双方项目整体相似度仅 5.9%，在进程管理、内存管理、中断处理、系统调用、设备驱动、网络协议栈等核心模块的共同指纹覆盖率均低于 7%，且大部分为 0。唯一高重合度的文件系统模块，经溯源确认为双方共同依赖的开源第三方库 `rust-fatfs` 所致，属于合法的公共代码复用。双方在内核架构的核心决策上，如任务模型（全局单例 vs 上下文传递）、内存安全抽象（原始指针 vs `UserPtr<T>` 泛型）、权限模型（空实现 vs capabilities）等方面，展现出根本性的异构设计。现有证据充分支持双方为独立开发项目，不存在系统性抄袭或同源嫌疑。

------------------------------

