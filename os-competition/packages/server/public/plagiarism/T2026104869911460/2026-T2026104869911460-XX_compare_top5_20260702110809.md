# 🛡️ OS 内核赛自动化代码级架构审计报告

**审计时间**: `2026-07-02 11:08:09`
**参赛作品名称**: `2026-T2026104869911460-XX`
**年份**: `2026`
**学校**: `XX`
**队伍名称**: `T2026104869911460`
**比赛名字**: `OS-Competition`
**比赛子赛道**: `Kernel-Implementation`
**仓库地址**: `https://gitlab.eduxiji.net/educg-group-43501-3132633/T2026104869911460-387`
**推测开发语言**: `Rust / C (由AST探针自动识别)`
**特征函数量**: `4298`

## ⚔️ 底层代码同源性对比 (AST Homology)
> 本部分由专门的代码审查智能体，对该项目与符合年份条件的历届最高相似度项目进行控制流与底层调用对比。新版报告会先给出确定性的全模块覆盖矩阵，再给出 Agent 的分模块解释，避免只围绕少量抽样函数展开。

### 🏆 Top 5 底层对标: vs `2023-Runable-中山大学`（中山大学 / Runable）

#### 对方作品信息

**对方年份**: `2023`
**对方学校**: `中山大学`
**对方队伍名称**: `Runable`
**对方比赛名字**: `OS-Competition`
**对方比赛子赛道**: `Kernel-Implementation`
**对方仓库地址**: `https://gitlab.eduxiji.net/runable/oskernel2023-Runable`

> 双向覆盖率：共同 AST 指纹: 285；唯一 AST 指纹: 目标项目 3358，参考项目 2950；覆盖率: 目标项目 8.49%，参考项目 9.66%

## 全模块对比总览（确定性证据）

> 总体覆盖率：共同 AST 指纹: 285；唯一 AST 指纹: 目标项目 3358，参考项目 2950；覆盖率: 目标项目 8.49%，参考项目 9.66%

### 1. 模块覆盖矩阵
| 模块 | 目标函数数 | 参考函数数 | 共同唯一 AST 指纹 | Target 覆盖 | Base 覆盖 | 状态 |
| :--- | ---: | ---: | ---: | ---: | ---: | :--- |
| 进程管理 | 328 | 47 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |
| 任务调度/异步运行时 | 595 | 87 | 3 | 0.54% | 3.66% | 双方存在且有结构重合 |
| 内存管理 | 472 | 201 | 6 | 1.36% | 3.21% | 双方存在且有结构重合 |
| 中断/异常处理 | 109 | 6 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |
| 系统调用 | 146 | 66 | 4 | 3.33% | 11.43% | 双方存在且有结构重合 |
| 文件系统 | 1333 | 345 | 43 | 4.26% | 13.56% | 双方存在且有结构重合 |
| 设备驱动 | 230 | 9 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |
| 网络协议栈 | 75 | 0 | 0 | 0.0% | 0.0% | 仅目标项目体现 |
| 文件系统/第三方库 | 392 | 443 | 2 | 0.65% | 0.5% | 双方存在且有结构重合 |
| 第三方库 | 0 | 6558 | 0 | 0.0% | 0.0% | 仅参考项目体现 |
| 通用模块/其他 | 618 | 115 | 7 | 1.49% | 7.69% | 双方存在且有结构重合 |

### 2. 分模块证据清单

#### 进程管理
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `NoAxiom/lib/ksync/src/async_lock/once_cell.rs#wait` | `os/src/sync/condvar.rs#wait` | 同板块同名 | debug_assert!, event_listener::listener!, self.get, self.get_unchecked | block_current_and_run_next, current_task, current_task().unwrap, drop, inner.wait_queue.push_back, mutex.lock ...(+2) |
| `NoAxiom/kernel/src/syscall/process.rs#sys_getppid` | `os/src/syscall/process.rs#sys_getppid` | 同板块同名 | Err, parent_process
                .upgrade, parent_process
                .upgrade()
                .map, parent_process
                .upgrade()
                .map(|task| task.tid() as isize)
                .ok_or, self.task.pcb, self.task.pcb().parent.clone ...(+1) | current_process, current_process()
        .inner_exclusive_access, current_process()
        .inner_exclusive_access()
        .parent
        .as_ref, current_process()
        .inner_exclusive_access()
        .parent
        .as_ref()
        .unwrap, current_process()
        .inner_exclusive_access()
        .parent
        .as_ref()
        .unwrap()
        .upgrade, current_process()
        .inner_exclusive_access()
        .parent
        .as_ref()
        .unwrap()
        .upgrade()
        .unwrap ...(+1) |
| `NoAxiom/kernel/src/syscall/process.rs#sys_exit` | `os/src/syscall/process.rs#sys_exit` | 同板块同名 | ExitCode::new, Ok, self.task.terminate | exit_current_and_run_next, panic!, println! |
| `NoAxiom/kernel/src/syscall/process.rs#sys_getpid` | `os/src/syscall/process.rs#sys_getpid` | 同板块同名 | Ok, self.task.pid_in_namespace | current_task, current_task().unwrap, current_task().unwrap().process.upgrade, current_task().unwrap().process.upgrade().unwrap, current_task().unwrap().process.upgrade().unwrap().getpid |

#### 任务调度/异步运行时
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `NoAxiom/kernel/src/syscall/fs.rs#sys_write` | `os/src/syscall/fs.rs#sys_write` | 同板块同名 | Err, Self::path_fd_operation_allowed, UserPtr::<u8>::new, buf_slice.len, drop, fd_table.get ...(+15) | UserBuffer::new, current_process, current_user_token, drop, file.clone, file.writable ...(+5) |
| `NoAxiom/kernel/src/syscall/fs.rs#sys_getcwd` | `os/src/syscall/fs.rs#sys_getcwd` | 同板块同名 | Err, Ok, UserPtr::<u8>::new, buf_slice[..cwd_bytes.len()].copy_from_slice, cwd.is_descendant_of, cwd_bytes.len ...(+9) | UserBuffer::new, b[0..dir.len()].copy_from_slice, current_process, current_user_token, dir.len, inner.work_dir.name ...(+4) |
| `NoAxiom/kernel/src/syscall/fs.rs#sys_read` | `os/src/syscall/fs.rs#sys_read` | 同板块同名 | Err, Self::path_fd_operation_allowed, UserPtr::<u8>::new, drop, fd_table.get, fd_table.get(fd).ok_or ...(+9) | UserBuffer::new, current_process, current_user_token, drop, file.clone, file.read ...(+4) |
| `NoAxiom/kernel/src/sched/cfs/cfs.rs#run` | `user/src/bin/stackless_coroutine.rs#run` | 同板块同名 | drop, local.pop, local.set_running, runnable.run, self.current_scheduler, self.current_scheduler().lock | Context::from_waker, create_waker, self.tasks.pop_front, self.tasks.push_back, task.as_mut, task.as_mut().poll |

#### 内存管理
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `NoAxiom/kernel/src/task/memory.rs#mmap` | `os/src/mm/memory_set.rs#mmap` | 同板块同名 | Ok, Some, VirtAddr::from, debug!, drop, fd_table.get ...(+27) | (start + len).into, MemoryMapArea::new, MmapFlags::empty, MmapFlags::from_bits, MmapFlags::from_bits(flags).unwrap, MmapProts::empty ...(+18) |
| `NoAxiom/lib/fatfs/src/dir.rs#create_dir` | `os/src/fatfs/dir_entry.rs#create_dir` | 同板块同名 | Box::pin, Ok, ShortNameGenerator::generate_dot, ShortNameGenerator::generate_dotdot, Some, dir.write_entry ...(+14) | Inode::Dir, Ok, ShortNameGenerator::generate_dot, ShortNameGenerator::generate_dotdot, Some, alloc_cluster ...(+10) |
| `NoAxiom/lib/fatfs/src/dir.rs#write_entry` | `os/src/fatfs/dir_entry.rs#write_entry` | 同板块同名 | Ok, Self::encode_lfn_utf16, ShortName::new, io::SeekFrom::Current, raw_entry.name, raw_entry.serialize ...(+9) | BlockCacheManager::new, Ok, Self::encode_lfn_utf16, ShortName::new, UPSafeCell::new, io::SeekFrom::Current ...(+5) |
| `NoAxiom/kernel/src/mm/map_area.rs#map_one` | `os/src/mm/memory_set.rs#map_one` | 同板块同名 | Err, Ok, VirtPageNum::from, VirtPageNum::from((vpn.raw() as isize + ppn_offset) as usize)
                    .kernel_translate_into_ppn, error!, frame.ppn ...(+11) | PTEFlags::from_bits, PTEFlags::from_bits(self.map_perm.bits as u16).unwrap, PhysPageNum, frame_alloc, frame_alloc().unwrap, page_table.map ...(+1) |

#### 中断/异常处理
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `NoAxiom/lib/arch/src/la64/trap.rs#set_kernel_trap_entry` | `os/src/trap/mod.rs#set_kernel_trap_entry` | 同板块同名 | ecfg::set_vs, eentry::set_eentry | stvec::write |
| `NoAxiom/lib/arch/src/la64/trap.rs#set_user_trap_entry` | `os/src/trap/mod.rs#set_user_trap_entry` | 同板块同名 | ecfg::set_vs, eentry::set_eentry | stvec::write |
| `NoAxiom/lib/arch/src/la64/interrupt.rs#enable_timer_interrupt` | `os/src/trap/mod.rs#enable_timer_interrupt` | 同板块同名 | ecfg::set_lie | sie::set_stimer |
| `NoAxiom/kernel/src/include/fs.rs#new` | `os/src/trap/mod.rs#trap_from_kernel` | 同模块上下文对齐/差异观察 | 内部闭环逻辑 | panic!, println! |

#### 系统调用
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `user/libd/src/arch/rv64/syscall.rs#syscall` | `user/src/syscall.rs#syscall` | 同板块同名 | asm! | asm! |
| `user/libd/src/syscall/wrapper.rs#wait` | `user/src/lib.rs#wait` | 同板块同名 | sys_waitpid | sys_waitpid, yield_ |
| `user/libd/src/syscall/wrapper.rs#munmap` | `user/src/lib.rs#munmap` | 同板块同名同结构 | sys_munmap | sys_munmap |
| `user/libd/src/syscall/wrapper.rs#kill` | `user/src/lib.rs#kill` | 同板块同名 | sys_kill | sys_kill |

#### 文件系统
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `NoAxiom/lib/fatfs/src/file.rs#seek` | `os/src/fatfs/fs.rs#seek` | 同板块同名 | Err, Ok, Some, debug_assert!, error!, i64::from ...(+16) | Err, Ok, Some, error!, i64::try_from, i64::try_from(self.offset)
                .ok ...(+9) |
| `NoAxiom/lib/fatfs/src/dir_entry.rs#deserialize` | `os/src/fatfs/boot_sector.rs#deserialize` | 同板块同名 | DirEntryData::File, DirEntryData::Lfn, DirFileEntryData::default, DirLfnEntryData::default, Err, FileAttributes::from_bits_truncate ...(+12) | Ok, Self::default, bpb.is_fat32, rdr.read_exact, rdr.read_u16_le, rdr.read_u32_le ...(+1) |
| `NoAxiom/lib/fatfs/src/boot_sector.rs#deserialize` | `os/src/fatfs/dir_entry.rs#deserialize` | 同板块同名 | Ok, Self::default, bpb.is_fat32, rdr.read_exact, rdr.read_u16_le, rdr.read_u32_le ...(+1) | DirAttr::from_bits_truncate, DirEntryData::File, DirEntryData::Lfn, DirLfnEntry::default, Ok, data.name_0.iter_mut ...(+9) |
| `NoAxiom/kernel/src/fs/vfs/impls/rust_fat32/dentry.rs#create` | `os/src/fatfs/file.rs#create` | 同板块同名 | Arc::new, Err, Fat32DirInode::new, Fat32FileInode::new, Ok, assert! ...(+17) | Some, dir.create_dir, dir.create_file |

#### 设备驱动
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `NoAxiom/lib/driver/src/impls/block/vf2_sdcard/mod.rs#read_block` | `os/src/drivers/block/virtio_blk.rs#read_block` | 同板块同名 | CmdArg::new, CmdReg::from, DataTransType::Read, Ok, assert_eq!, buf.len ...(+3) | self.0
            .exclusive_access, self.0
            .exclusive_access()
            .read_block, self.0
            .exclusive_access()
            .read_block(block_id, buf)
            .expect |
| `NoAxiom/lib/driver/src/impls/block/vf2_sdcard/mod.rs#write_block` | `os/src/drivers/block/virtio_blk.rs#write_block` | 同板块同名 | CmdArg::new, CmdReg::from, DataTransType::Write, Ok, assert_eq!, buf.len ...(+3) | self.0
            .exclusive_access, self.0
            .exclusive_access()
            .write_block, self.0
            .exclusive_access()
            .write_block(block_id, buf)
            .expect |
| `NoAxiom/lib/driver_ahci/src/drv_ahci.rs#ahci_host_init` | `user/src/bin/adder_mutex_blocking.rs#main` | 同模块上下文对齐/差异观察 | ahci_mdelay, ahci_port_base, ahci_readl, ahci_writel, log::debug! | Vec::new, argv[1].parse, argv[1].parse().unwrap, argv[2].parse, argv[2].parse().unwrap, assert_eq! ...(+6) |
| `NoAxiom/lib/driver/src/impls/block/vf2_sdcard/mod.rs#init_sdcard` | `user/src/bin/barrier_condvar.rs#block` | 同模块上下文对齐/差异观察 | CardTypeReg::from, CardTypeReg::from(0).with_card_width4_1, CmdArg::new, CmdReg::from, ControlReg::from, RawInterruptStatusReg::from ...(+20) | condvar_signal, condvar_wait, mutex_lock, mutex_unlock, self.count.get |

#### 网络协议栈
仅目标项目中观察到该模块代表函数，参考项目没有同模块可对齐证据：
- 目标项目：`NoAxiom/kernel/src/net/tcpsocket.rs#poll_inner`，调用摘要：PollEvent::empty, SOCKET_SET.lock, matches!, poll_ifaces, req.intersects, self.shutdown_type.contains ...(+8)
- 目标项目：`NoAxiom/kernel/src/net/mod.rs#net_udp_test`，调用摘要：Instant::from_millis, LoopBackDev::new, SocketSet::new, SpinLock::new, client.bind, client.bind(233).unwrap ...(+26)
- 目标项目：`NoAxiom/kernel/src/net/tcpsocket.rs#connect`，调用摘要：Err, Ok, SOCKET_SET.lock, debug!, drop, error! ...(+5)
- 目标项目：`NoAxiom/kernel/src/include/net.rs#new`，调用摘要：AddressFamily::try_from, Err, Ok, core::cmp::min, core::mem::size_of::<SockAddrIpv4>, core::mem::size_of::<SockAddrIpv6> ...(+16)

#### 文件系统/第三方库
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `NoAxiom/kernel/src/fs/vfs/impls/ext4/inode.rs#truncate` | `modified_dependencies/rust-fatfs/src/file.rs#truncate` | 同板块同名 | Ok, assert!, assert_no_lock!, debug!, ext4.get_inode_ref, ext4.truncate_inode ...(+5) | Ok, debug_assert!, e.set_first_cluster, e.set_size, panic!, self.fs.fat_type ...(+3) |
| `NoAxiom/lib/ext4_rs/src/ext4_defs/direntry.rs#write_entry` | `modified_dependencies/rust-fatfs/src/dir.rs#write_entry` | 同板块同名 | de_type.bits, name.as_bytes, name.len, self.name[..name.len()].copy_from_slice | Ok, Self::encode_lfn_utf16, ShortName::new, io::SeekFrom::Current, raw_entry.name, raw_entry.serialize ...(+7) |
| `NoAxiom/lib/ext4_rs/src/ext4_defs/inode.rs#set_size` | `modified_dependencies/rust-fatfs/src/dir_entry.rs#set_size` | 同板块同名 | 内部闭环逻辑 | self.data.set_size, self.data.size |
| `NoAxiom/lib/ext4_rs/src/ext4_defs/inode.rs#size` | `modified_dependencies/rust-fatfs/src/file.rs#size` | 同板块同名 | 内部闭环逻辑 | e.inner, e.inner().size |

#### 第三方库
仅参考项目中观察到该模块代表函数，目标项目没有同模块可对齐证据：
- 参考项目：`dependencies/k210-soc/src/sysctl/pll_compute.rs#compute_params`，调用摘要：FACTORS.iter, FACTORS.iter().peekable, Some, abs_diff, bit, div_round_closest ...(+6)
- 参考项目：`dependencies/k210-soc/src/sysctl.rs#clock_get_freq`，调用摘要：clock_get_clock_select, clock_get_threshold, clock_source_get_freq, panic!
- 参考项目：`dependencies/k210-soc/src/sysctl.rs#clock_set_threshold`，调用摘要：(*ptr)
                .clk_sel0
                .modify, (*ptr)
                .clk_th0
                .modify, (*ptr)
                .clk_th1
                .modify, (*ptr)
                .clk_th2
                .modify, (*ptr)
                .clk_th3
                .modify, (*ptr)
                .clk_th4
                .modify ...(+57)
- 参考项目：`dependencies/k210-soc/src/sysctl.rs#reset_ctl`，调用摘要：(*ptr)
                .peri_reset
                .modify, (*ptr)
                .soft_reset
                .modify, (*ptr).peri_reset.modify, pac::SYSCTL::ptr, w.aes_reset, w.aes_reset().bit ...(+56)

#### 通用模块/其他
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `NoAxiom/lib/ksync/src/async_mutex.rs#lock` | `user/src/bin/adder_peterson_spin.rs#lock` | 同板块同名 | SuspendFuture::new, self.try_lock, self.waiters.lock, self.waiters.lock().push_back | compiler_fence, vload! |
| `NoAxiom/lib/arch/src/la64/time.rs#set_timer` | `os/src/sbi.rs#set_timer` | 同板块同名 | tcfg::set_en, tcfg::set_init_val, ticlr::clear_timer_interrupt | sbi_rt::set_timer |
| `NoAxiom/lib/ksync/src/async_lock/mutex.rs#lock` | `os/src/sync/mutex.rs#lock` | 同板块同名 | Lock::_new | drop, self.locked.exclusive_access, suspend_current_and_run_next |
| `user/libd/src/lib_request.rs#shutdown` | `os/src/sbi.rs#shutdown` | 同板块同名 | vec! | system_reset, unreachable! |


#### 1. 宏观判定
- **定性结论**: **独立开发，底层架构显著差异**
- **核心指标**: 全局 AST 相似度 4.73%；共同 AST 指纹 285；唯一 AST 指纹: 目标项目 3358，参考项目 2950；覆盖率: 目标项目 8.49%，参考项目 9.66%。
- **判定依据**: 双方在进程管理、内存管理、任务调度等核心板块的 AST 调用图谱、数据结构选型、同步机制及系统调用集存在根本性架构分歧。目标项目呈现现代工程级/竞赛级 Linux 兼容内核特征，参考项目为教学级 rCore 风格内核，二者不存在同源衍生关系。

#### 2. 全模块覆盖矩阵解读

- **进程管理**
  - 双方均存在该模块。
  - 共同 AST 指纹: 0；覆盖率: 目标 0.0%，参考 0.0%。
  - 解读: 双方虽均有进程管理，但 AST 结构零重合。目标项目采用 `TASK_MANAGER`、`UserPtr`、`Futex` 等现代机制，参考项目使用 `PID2PCB`、`UserBuffer`、`condvar`。同名函数（如 `sys_getpid`）由 POSIX 规范强制，但内部调用链完全不同。
  - 证据强度: **未形成**。

- **任务调度/异步运行时**
  - 双方均存在该模块。
  - 共同 AST 指纹: 3；覆盖率: 目标 0.54%，参考 3.66%。
  - 解读: 极低覆盖率。目标项目基于 CFS 与 async/await 运行时，参考项目为简单的 `__switch` 协作式调度。共同指纹源于 `sys_write`、`sys_read` 等 POSIX 系统调用的通用错误处理路径，非核心调度逻辑。
  - 证据强度: **弱**。

- **内存管理**
  - 双方均存在该模块。
  - 共同 AST 指纹: 6；覆盖率: 目标 1.36%，参考 3.21%。
  - 解读: 覆盖率低。共同指纹集中在 `mmap`、`map_one` 等通用内存映射操作，但目标项目使用 `VirtPageNum`、`frame.ppn` 等独立页帧管理，参考项目依赖 `PhysPageNum`、`frame_alloc`。内存模型异构。
  - 证据强度: **弱**。

- **中断/异常处理**
  - 双方均存在该模块。
  - 共同 AST 指纹: 0；覆盖率: 目标 0.0%，参考 0.0%。
  - 解读: 双方均实现 trap 入口设置，但目标项目基于 LoongArch 的 `ecfg::set_vs`，参考项目基于 RISC-V 的 `stvec::write`。硬件架构不同导致 AST 零重合。
  - 证据强度: **未形成**。

- **系统调用**
  - 双方均存在该模块。
  - 共同 AST 指纹: 4；覆盖率: 目标 3.33%，参考 11.43%。
  - 解读: 覆盖率差异大。共同指纹为用户态 wrapper（如 `syscall` 内联汇编、`munmap` 等），属于系统调用 ABI 的通用实现模式。内核态系统调用路径（如 `sys_clone3` vs `sys_clone`）完全不同。
  - 证据强度: **弱**。

- **文件系统**
  - 双方均存在该模块。
  - 共同 AST 指纹: 43；覆盖率: 目标 4.26%，参考 13.56%。
  - 解读: 覆盖率相对较高，但共同指纹主要源于双方均集成的 `rust-fatfs` 第三方库（FAT32 解析逻辑）。目标项目额外实现了 ext4、procfs、VFS 抽象层，参考项目无此设计。
  - 证据强度: **中**（集中于第三方库）。

- **设备驱动**
  - 双方均存在该模块。
  - 共同 AST 指纹: 0；覆盖率: 目标 0.0%，参考 0.0%。
  - 解读: 双方驱动模型完全不同。目标项目使用 `vf2_sdcard`、`ahci` 等独立驱动框架，参考项目基于 `virtio_blk`。块设备读写接口虽同名，但底层硬件操作 AST 无重合。
  - 证据强度: **未形成**。

- **网络协议栈**
  - 仅目标项目体现。
  - 共同 AST 指纹: 0；覆盖率: 目标 0.0%，参考 0.0%。
  - 解读: 目标项目拥有完整的 TCP/UDP 协议栈（基于 smoltcp），参考项目无网络模块。此为目标项目独立扩展的铁证。
  - 证据强度: **未形成**。

- **第三方库/公共依赖**
  - 双方均存在该模块。
  - 共同 AST 指纹: 2；覆盖率: 目标 0.65%，参考 0.5%。
  - 解读: 共同指纹源于 `rust-fatfs` 库的 `truncate`、`write_entry` 等通用文件系统操作。参考项目还包含大量 k210-soc 等 BSP 依赖，目标项目无此部分。
  - 证据强度: **弱**（公共依赖）。

#### 3. 底层 AST 关键证据指纹表

| 所属板块 | 目标项目路径 | 参考项目路径 | 证据类型 | 技术说明 |
| :--- | :--- | :--- | :--- | :--- |
| 文件系统 | `NoAxiom/lib/fatfs/src/dir.rs#write_entry` | `os/src/fatfs/dir_entry.rs#write_entry` | 同板块同名 | 双方均使用 `rust-fatfs` 库的 FAT32 目录项写入逻辑，调用 `ShortName::new`、`encode_lfn_utf16` 等相同 API。属于第三方公共库复用，非核心架构抄袭。 |
| 文件系统 | `NoAxiom/lib/fatfs/src/boot_sector.rs#deserialize` | `os/src/fatfs/dir_entry.rs#deserialize` | 同板块同名 | 双方均包含 FAT 文件系统元数据反序列化，但目标项目用于引导扇区，参考项目用于目录项。调用 `rdr.read_u16_le` 等标准 IO 操作，属领域共性。 |
| 内存管理 | `NoAxiom/kernel/src/task/memory.rs#mmap` | `os/src/mm/memory_set.rs#mmap` | 同板块同名 | 双方均实现 `mmap` 系统调用，但目标项目使用 `VirtAddr::from`、`fd_table.get`，参考项目使用 `MemoryMapArea::new`、`MmapFlags::from_bits`。内存抽象层不同。 |
| 内存管理 | `NoAxiom/kernel/src/mm/map_area.rs#map_one` | `os/src/mm/memory_set.rs#map_one` | 同板块同名 | 双方均实现单页映射，但目标项目使用 `VirtPageNum::kernel_translate_into_ppn`，参考项目使用 `frame_alloc`。页帧分配策略异构。 |
| 系统调用 | `user/libd/src/arch/rv64/syscall.rs#syscall` | `user/src/syscall.rs#syscall` | 同板块同名 | 双方用户态均使用 `asm!` 宏实现系统调用指令，属于 RISC-V 架构的标准特权指令封装，无独创性。 |
| 任务调度 | `NoAxiom/kernel/src/syscall/fs.rs#sys_write` | `os/src/syscall/fs.rs#sys_write` | 同板块同名 | 双方均实现 `write` 系统调用，但目标项目使用 `UserPtr::<u8>` 进行用户内存安全访问，参考项目使用 `UserBuffer`。错误处理与文件描述符管理路径不同。 |
| 通用模块 | `NoAxiom/lib/ksync/src/async_mutex.rs#lock` | `os/src/sync/mutex.rs#lock` | 同板块同名 | 目标项目使用 `SuspendFuture` 实现异步互斥锁，参考项目使用 `suspend_current_and_run_next` 实现阻塞锁。并发模型代际差异。 |

#### 4. 核心代码调用流对比剖析

* **【进程管理/进程退出路径】**
* **底层技术析理**: 参考项目 `sys_exit` 直接耦合调度与退出逻辑，调用 `exit_current_and_run_next` 并包含 `println!` 调试输出，属于教学级简单实现。目标项目将退出拆分为 `sys_exit`（构造 `ExitCode` 并调用 `self.task.terminate` 标记）与 `exit_handler`（通过 `TASK_MANAGER.remove` 回收资源、`FUTEX_SHARED_QUEUE.lock().wake_waiter` 唤醒阻塞者、`SigInfo::new_detailed` 发送子进程信号）。目标项目展现了清晰的分层架构与现代 Linux 兼容的退出语义，与参考项目的全局哈希表直接移除模型有本质区别。
* **代码溯源链接**:
  - 目标项目: `NoAxiom/kernel/src/syscall/process.rs#sys_exit` / `NoAxiom/kernel/src/task/exit.rs#exit_handler`
  - 参考项目: `os/src/syscall/process.rs#sys_exit`

* **【内存管理/页映射路径】**
* **底层技术析理**: 参考项目 `map_one` 依赖全局 `frame_alloc` 分配物理页帧，并使用 `PTEFlags::from_bits` 构建页表项，是典型的物理页帧直接分配模型。目标项目 `map_one` 使用 `VirtPageNum::kernel_translate_into_ppn` 进行虚拟到物理的转换，并涉及 `frame.ppn` 等独立页帧描述符。目标项目的页管理抽象层次更高，支持更复杂的内核地址空间布局，与参考项目的简单线性映射模型异构。
* **代码溯源链接**:
  - 目标项目: `NoAxiom/kernel/src/mm/map_area.rs#map_one`
  - 参考项目: `os/src/mm/memory_set.rs#map_one`

* **【同步原语/锁实现路径】**
* **底层技术析理**: 参考项目 `mutex.rs#lock` 基于 `UPSafeCell` 与 `suspend_current_and_run_next` 实现阻塞式互斥锁，任务在获取锁失败时立即挂起。目标项目 `async_mutex.rs#lock` 返回 `SuspendFuture`，基于异步运行时实现非阻塞等待，任务可被轮询。这代表了从传统阻塞同步到现代异步同步的根本性并发模型演进，AST 调用图谱无重合。
* **代码溯源链接**:
  - 目标项目: `NoAxiom/lib/ksync/src/async_mutex.rs#lock`
  - 参考项目: `os/src/sync/mutex.rs#lock`

#### 5. 单方模块与降权项

- **仅目标项目存在**: **网络协议栈**（`NoAxiom/kernel/src/net/`）。包含完整的 TCP/UDP socket 实现，是目标项目独立扩展的显著标志，参考项目无任何对应模块，不构成同源证据。
- **仅参考项目存在**: **第三方库**（`dependencies/k210-soc/`）。包含大量 K210 平台 BSP 代码，目标项目无此硬件依赖，属于平台特定代码，不构成同源证据。
- **降权项说明**:
  - **同名系统调用**: `sys_getpid`、`sys_exit`、`sys_getppid` 等函数名由 POSIX/Linux ABI 强制定义，任何兼容内核均需实现。双方 AST 调用链完全不同，不能作为复用证据。
  - **公共第三方库**: `rust-fatfs` 库在双方文件系统模块中均被使用，产生的共同 AST 指纹属于公共依赖复用，不代表核心架构抄袭。
  - **跨板块同名**: 如目标项目 `once_cell.rs#wait` 与参考项目 `condvar.rs#wait`，虽同名但分属异步初始化与条件变量不同功能域，AST 图谱无交集，不构成同板块证据。

#### 6. 保守结论

基于 4.73% 的全局 AST 相似度及进程管理等核心板块零共同指纹的客观事实，目标项目 `[2026-T2026104869911460-XX]` 与参考项目 `[2023-Runable-中山大学]` 在底层架构上存在显著差异。目标项目采用现代 Linux 兼容设计（命名空间、Futex、CFS 调度、异步运行时、VFS 层），参考项目为教学级简单内核。双方在内存模型、同步策略、进程管理数据结构上的异构性，排除了“基于参考项目洗稿或演进”的可能。现有弱信号均源于 POSIX 规范、公共第三方库或领域共性逻辑，不构成代码复用证据。整体同源性风险极低。

------------------------------

