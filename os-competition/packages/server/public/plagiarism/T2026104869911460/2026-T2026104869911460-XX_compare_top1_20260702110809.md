# 🛡️ OS 内核赛自动化代码级架构审计报告

**审计时间**: `2026-07-02 11:08:09`
**参赛作品名称**: `2026-T2026104869911460-XX`
**年份**: `2026`
**学校**: `XX`
**队伍名称**: `T2026104869911460`
**比赛名字**: `OS-Competition`
**比赛子赛道**: `Kernel-Implementation`
**仓库地址**: `https://gitlab.eduxiji.net/educg-group-43501-3132633/T2026104869911460-387`
**推测开发语言**: `Rust / C (由AST探针自动识别)`
**特征函数量**: `4298`

## ⚔️ 底层代码同源性对比 (AST Homology)
> 本部分由专门的代码审查智能体，对该项目与符合年份条件的历届最高相似度项目进行控制流与底层调用对比。新版报告会先给出确定性的全模块覆盖矩阵，再给出 Agent 的分模块解释，避免只围绕少量抽样函数展开。

### 🏆 Top 1 底层对标: vs `2025-NoAxiom-杭州电子科技大学`（杭州电子科技大学 / NoAxiom）

#### 对方作品信息

**对方年份**: `2025`
**对方学校**: `杭州电子科技大学`
**对方队伍名称**: `NoAxiom`
**对方比赛名字**: `OS-Competition`
**对方比赛子赛道**: `Kernel-Implementation`
**对方仓库地址**: `https://gitlab.eduxiji.net/educg-group-36002-2710490/T202510336995214-148`

> 双向覆盖率：共同 AST 指纹: 1901；唯一 AST 指纹: 目标项目 3358，参考项目 2349；覆盖率: 目标项目 56.61%，参考项目 80.93%

## 全模块对比总览（确定性证据）

> 总体覆盖率：共同 AST 指纹: 1901；唯一 AST 指纹: 目标项目 3358，参考项目 2349；覆盖率: 目标项目 56.61%，参考项目 80.93%

### 1. 模块覆盖矩阵
| 模块 | 目标函数数 | 参考函数数 | 共同唯一 AST 指纹 | Target 覆盖 | Base 覆盖 | 状态 |
| :--- | ---: | ---: | ---: | ---: | ---: | :--- |
| 进程管理 | 328 | 207 | 75 | 34.88% | 55.97% | 双方存在且有结构重合 |
| 任务调度/异步运行时 | 595 | 434 | 268 | 48.38% | 67.85% | 双方存在且有结构重合 |
| 内存管理 | 472 | 382 | 265 | 60.23% | 74.86% | 双方存在且有结构重合 |
| 中断/异常处理 | 109 | 107 | 65 | 94.2% | 97.01% | 双方存在且有结构重合 |
| 系统调用 | 146 | 48 | 33 | 27.5% | 78.57% | 双方存在且有结构重合 |
| 文件系统 | 1333 | 877 | 605 | 59.9% | 83.8% | 双方存在且有结构重合 |
| 设备驱动 | 230 | 227 | 210 | 98.59% | 99.53% | 双方存在且有结构重合 |
| 网络协议栈 | 75 | 53 | 24 | 35.82% | 48.98% | 双方存在且有结构重合 |
| 文件系统/第三方库 | 392 | 49 | 35 | 11.36% | 79.55% | 双方存在且有结构重合 |
| 通用模块/其他 | 618 | 552 | 395 | 84.22% | 96.34% | 双方存在且有结构重合 |

### 2. 分模块证据清单

#### 进程管理
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `NoAxiom/kernel/src/syscall/process.rs#sys_prctl` | `NoAxiom/kernel/src/syscall/process.rs#sys_prctl` | 同板块同名 | Err, Ok, UserPtr::<i32>::new, UserPtr::<i32>::new(arg2)
                    .write, UserPtr::<u8>::new, UserPtr::<usize>::new ...(+13) | Err |
| `NoAxiom/kernel/src/syscall/process.rs#sys_futex` | `NoAxiom/kernel/src/syscall/process.rs#sys_futex` | 同板块同名 | Duration::from, Err, FUTEX_SHARED_QUEUE
                            .lock, FUTEX_SHARED_QUEUE
                            .lock()
                            .requeue, FUTEX_SHARED_QUEUE.lock, FUTEX_SHARED_QUEUE.lock().wake_waiter ...(+31) | Duration::from, Err, FUTEX_SHARED_QUEUE
                            .lock, FUTEX_SHARED_QUEUE
                            .lock()
                            .requeue, FUTEX_SHARED_QUEUE.lock, FUTEX_SHARED_QUEUE.lock().wake_waiter ...(+34) |
| `NoAxiom/lib/ksync/src/async_lock/once_cell.rs#initialize_or_wait` | `NoAxiom/lib/ksync/src/async_lock/once_cell.rs#initialize_or_wait` | 同板块同名同结构 | (initializer), Err, Guard, Ok, Some, State::Initialized.into ...(+21) | (initializer), Err, Guard, Ok, Some, State::Initialized.into ...(+21) |
| `NoAxiom/kernel/src/task/exit.rs#exit_handler` | `NoAxiom/kernel/src/task/exit.rs#exit_handler` | 同板块同名 | FUTEX_SHARED_QUEUE
                            .lock, FUTEX_SHARED_QUEUE
                            .lock()
                            .wake_waiter, SigDetail::Child, SigInfo::new_detailed, Some, TASK_MANAGER.remove ...(+34) | FUTEX_SHARED_QUEUE
                        .lock, FUTEX_SHARED_QUEUE
                        .lock()
                        .wake_waiter, PROCESS_GROUP_MANAGER.lock, PROCESS_GROUP_MANAGER.lock().remove, SigDetail::Child, SigInfo::new_detailed ...(+35) |

#### 任务调度/异步运行时
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `NoAxiom/kernel/src/syscall/fs.rs#sys_renameat2` | `NoAxiom/kernel/src/syscall/fs.rs#sys_renameat2` | 同板块同名 | Arc::as_ptr, Arc::ptr_eq, Err, FileFlags::empty, Ok, RenameFlags::from_bits ...(+54) | Arc::as_ptr, Arc::ptr_eq, Err, Ok, RenameFlags::from_bits, RenameFlags::from_bits(flags).ok_or ...(+28) |
| `NoAxiom/kernel/src/syscall/signal.rs#sys_kill` | `NoAxiom/kernel/src/syscall/signal.rs#sys_kill` | 同板块同名 | Err, Ok, PROCESS_GROUP_MANAGER
                    .lock, PROCESS_GROUP_MANAGER
                    .lock()
                    .get_group, PROCESS_GROUP_MANAGER
                    .lock()
                    .get_group(pgid)
                    .unwrap_or_default, PROCESS_GROUP_MANAGER
                    .lock()
                    .get_group(pgid)
                    .unwrap_or_default()
                    .into_iter ...(+20) | Err, Ok, PROCESS_GROUP_MANAGER
                    .lock, PROCESS_GROUP_MANAGER
                    .lock()
                    .get_group, PROCESS_GROUP_MANAGER
                    .lock()
                    .get_group(pgid)
                    .unwrap, PROCESS_GROUP_MANAGER
                    .lock()
                    .get_group(pgid)
                    .unwrap()
                    .into_iter ...(+16) |
| `NoAxiom/kernel/src/syscall/fs.rs#sys_ioctl` | `NoAxiom/kernel/src/syscall/fs.rs#sys_ioctl` | 同板块同名 | (file.size() as u64).max, BlkIoctlCmd::from_repr, Err, FsIoctlCmd::from_request, IoctlCmd::Block, IoctlCmd::Fs ...(+35) | BlkIoctlCmd::from_repr, Err, IoctlCmd::Block, IoctlCmd::Loop, IoctlCmd::Namespace, IoctlCmd::Other ...(+24) |
| `NoAxiom/kernel/src/syscall/io.rs#sys_pselect6` | `NoAxiom/kernel/src/syscall/io.rs#sys_pselect6` | 同板块同名 | Duration::from, Err, Ok, PollEvent::empty, PpollItem::new, UserPtr::<FdSet>::new ...(+45) | Duration::from, Err, Ok, PollEvent::empty, PpollItem::new, PselectFuture::new ...(+42) |

#### 内存管理
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `NoAxiom/kernel/src/syscall/syscall.rs#syscall_inner` | `NoAxiom/kernel/src/syscall/syscall.rs#syscall_inner` | 同板块同名 | Err, Self::combine_lo_hi_offset, Self::empty_syscall, Self::sys_gettimeofday, Self::sys_systemshutdown, error! ...(+257) | Ok, Self::empty_syscall, Self::sys_gettimeofday, Self::sys_madvise, Self::sys_syslog, Self::sys_systemshutdown ...(+156) |
| `NoAxiom/kernel/src/syscall/fs.rs#sys_openat` | `NoAxiom/kernel/src/syscall/fs.rs#sys_openat` | 同板块同名 | Err, FdFlags::from, FileFlags::empty, FileFlags::from_bits, FileFlags::from_bits(flags).ok_or, InodeMode::empty ...(+69) | Err, FdFlags::from, FileFlags::from_bits, FileFlags::from_bits(flags).ok_or, InodeMode::from_bits, InodeMode::from_bits(mode).ok_or ...(+36) |
| `NoAxiom/kernel/src/mm/validate.rs#validate` | `NoAxiom/kernel/src/mm/validate.rs#validate` | 同板块同名 | Arch::current_root_ppn, Arch::tlb_flush, Err, MappingFlags::from, Ok, PageTable::from_ppn ...(+53) | Arch::current_root_ppn, Arch::tlb_flush, Err, MappingFlags::from, Ok, PageTable::from_ppn ...(+33) |
| `NoAxiom/kernel/src/syscall/fs.rs#sys_fcntl` | `NoAxiom/kernel/src/syscall/fs.rs#sys_fcntl` | 同板块同名 | Arc::as_ptr, Err, FcntlFlags::from_repr, FcntlFlags::from_repr(cmd).ok_or, FdFlags::from_bits_truncate, FileFlags::from_bits_retain ...(+53) | Err, FcntlFlags::from_repr, FcntlFlags::from_repr(cmd).ok_or, FdFlags::FD_CLOEXEC.bits, FileFlags::from_bits_retain, Ok ...(+20) |

#### 中断/异常处理
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `NoAxiom/kernel/src/signal/sig_action.rs#new_default` | `NoAxiom/kernel/src/signal/sig_action.rs#new_default` | 同板块同名同结构 | 内部闭环逻辑 | 内部闭环逻辑 |
| `NoAxiom/kernel/src/trap/soft_int.rs#ipi_handler` | `NoAxiom/kernel/src/trap/soft_int.rs#ipi_handler` | 同板块同名同结构 | Arch::clear_ipi, Arch::tlb_flush, current_ipi_info, info!, trace! | Arch::clear_ipi, Arch::tlb_flush, current_ipi_info, info!, trace! |
| `NoAxiom/kernel/src/trap/ktrap_handler.rs#kernel_interrupt_handler` | `NoAxiom/kernel/src/trap/ktrap_handler.rs#kernel_interrupt_handler` | 同板块同名同结构 | ext_int_handler, inc_interrupts_count, kernel_timer_trap_handler, soft_int_handler | ext_int_handler, inc_interrupts_count, kernel_timer_trap_handler, soft_int_handler |
| `NoAxiom/kernel/src/trap/ktrap_handler.rs#kernel_trap_handler` | `NoAxiom/kernel/src/trap/ktrap_handler.rs#kernel_trap_handler` | 同板块同名同结构 | current_cpu, current_cpu().add_trap_depth, current_cpu().sub_trap_depth, kernel_exception_handler, kernel_interrupt_handler, panic! | current_cpu, current_cpu().add_trap_depth, current_cpu().sub_trap_depth, kernel_exception_handler, kernel_interrupt_handler, panic! |

#### 系统调用
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `NoAxiom/kernel/src/syscall/mm.rs#sys_shmctl` | `NoAxiom/kernel/src/syscall/mm.rs#sys_shmctl` | 同板块同名 | IpcCtlCmd::from_repr, IpcCtlCmd::from_repr(cmd).ok_or, Ok, SHM_MANAGER.lock, SHM_MANAGER.lock().ipc_info, SHM_MANAGER.lock().linux_stat ...(+14) | Err, IpcCtlCmd::from_repr, IpcCtlCmd::from_repr(cmd).ok_or, Ok, SHM_MANAGER.lock, SHM_MANAGER.lock().remove ...(+2) |
| `NoAxiom/kernel/src/syscall/signal.rs#sys_sigtimedwait` | `NoAxiom/kernel/src/syscall/signal.rs#sys_sigtimedwait` | 同板块同名 | Err, UserPtr::<TimeSpec>::new, UserPtr::<TimeSpec>::new(timeout).try_read, UserPtr::new, UserPtr::new(set).read, core::mem::size_of::<RawSigInfo> ...(+5) | Err, UserPtr::new, UserPtr::new(set).read, UserPtr::new(timeout).read, error!, self.__sys_sigtimedwait ...(+1) |
| `user/libd/src/syscall/wrapper.rs#ls_prase2` | `user/libd/src/syscall/wrapper.rs#ls_prase2` | 同板块同名同结构 | Vec::new, assert!, buf.as_ptr, buf.fill, dirent.get_name, dirent.get_name().to_string ...(+5) | Vec::new, assert!, buf.as_ptr, buf.fill, dirent.get_name, dirent.get_name().to_string ...(+5) |
| `NoAxiom/kernel/src/syscall/time.rs#sys_clock_settime` | `NoAxiom/kernel/src/syscall/time.rs#sys_clock_settime` | 同板块同名 | CLOCK_MANAGER.lock, ClockId::from_repr, ClockId::from_repr(clockid).ok_or, Err, Ok, UserPtr::<TimeSpec>::new ...(+10) | CLOCK_MANAGER.lock, CLOCK_MANAGER.lock().0.get_mut, ClockId::from_repr, ClockId::from_repr(clockid).ok_or, Err, Ok ...(+9) |

#### 文件系统
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `user/libd/src/lib_widget.rs#on_event` | `user/libd/src/lib_widget.rs#on_event` | 同板块同名同结构 | "input text".to_string, Box::new, Decoder::convert_key, PaintElement::Line, PaintElement::Rect, PaintElement::Text ...(+24) | "input text".to_string, Box::new, Decoder::convert_key, PaintElement::Line, PaintElement::Rect, PaintElement::Text ...(+24) |
| `NoAxiom/kernel/src/fs/vfs/basic/dentry.rs#mknodat_son` | `NoAxiom/kernel/src/fs/vfs/basic/dentry.rs#mknodat_son` | 同板块同名 | Arc::new, Arc::new(LoopControlDentry::new(
                            Some(self.clone()),
                            name,
                            super_block.clone(),
                        ))
                        .into_dyn, Arc::new(LoopDevDentry::new(
                            Some(self.clone()),
                            name,
                            super_block.clone(),
                        ))
                        .into_dyn, Arc::new(NullDentry::new(
                            Some(self.clone()),
                            name,
                            super_block.clone(),
                        ))
                        .into_dyn, Arc::new(RtcDentry::new(
                            Some(self.clone()),
                            name,
                            super_block.clone(),
                        ))
                        .into_dyn, Arc::new(TtyDentry::new(
                            Some(self.clone()),
                            name,
                            super_block.clone(),
                        ))
                        .into_dyn ...(+36) | Arc::new, Arc::new(LoopControlDentry::new(
                            Some(self.clone()),
                            name,
                            super_block.clone(),
                        ))
                        .into_dyn, Arc::new(LoopDevDentry::new(
                            Some(self.clone()),
                            &format!("loop{}", y),
                            super_block.clone(),
                        ))
                        .into_dyn, Arc::new(NullDentry::new(
                            Some(self.clone()),
                            name,
                            super_block.clone(),
                        ))
                        .into_dyn, Arc::new(PipeDentry::new(name)).into_dyn, Arc::new(RtcDentry::new(
                            Some(self.clone()),
                            name,
                            super_block.clone(),
                        ))
                        .into_dyn ...(+42) |
| `user/libd/src/lib_widget.rs#on_event` | `user/libd/src/lib_widget.rs#on_event` | 同板块同名同结构 | Decoder::convert_key, Point::new, Point::zero, char_vec.chunks, char_vec.chunks(char_per_line).enumerate, char_vec.is_empty ...(+22) | Decoder::convert_key, Point::new, Point::zero, char_vec.chunks, char_vec.chunks(char_per_line).enumerate, char_vec.is_empty ...(+22) |
| `NoAxiom/lib/fatfs/src/boot_sector.rs#determine_sectors_per_fat` | `NoAxiom/lib/fatfs/src/boot_sector.rs#determine_sectors_per_fat` | 同板块同名同结构 | fat_type.bits_per_fat_entry, u32::from, u64::from | fat_type.bits_per_fat_entry, u32::from, u64::from |

#### 设备驱动
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `NoAxiom/lib/driver_ahci/src/drv_ahci.rs#ahci_host_init` | `NoAxiom/lib/driver_ahci/src/drv_ahci.rs#ahci_host_init` | 同板块同名同结构 | ahci_mdelay, ahci_port_base, ahci_readl, ahci_writel, log::debug! | ahci_mdelay, ahci_port_base, ahci_readl, ahci_writel, log::debug! |
| `NoAxiom/lib/driver/src/impls/block/vf2_sdcard/mod.rs#init_sdcard` | `NoAxiom/lib/driver/src/impls/block/vf2_sdcard/mod.rs#init_sdcard` | 同板块同名同结构 | CardTypeReg::from, CardTypeReg::from(0).with_card_width4_1, CmdArg::new, CmdReg::from, ControlReg::from, RawInterruptStatusReg::from ...(+20) | CardTypeReg::from, CardTypeReg::from(0).with_card_width4_1, CmdArg::new, CmdReg::from, ControlReg::from, RawInterruptStatusReg::from ...(+20) |
| `NoAxiom/lib/driver/src/probe/realize.rs#of_realize` | `NoAxiom/lib/driver/src/probe/realize.rs#of_realize` | 同板块同名同结构 | DEV_BUS.add_block_device, LsAhciDevice::new, PlicDevice::new, VF2SdcardDevice::new, log::info!, log::warn! ...(+1) | DEV_BUS.add_block_device, LsAhciDevice::new, PlicDevice::new, VF2SdcardDevice::new, log::info!, log::warn! ...(+1) |
| `NoAxiom/lib/driver_ahci/src/drv_ahci.rs#ahci_print_info` | `NoAxiom/lib/driver_ahci/src/drv_ahci.rs#ahci_print_info` | 同板块同名同结构 | log::debug! | log::debug! |

#### 网络协议栈
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `NoAxiom/kernel/src/net/mod.rs#net_udp_test` | `NoAxiom/kernel/src/net/mod.rs#net_udp_test` | 同板块同名同结构 | Instant::from_millis, LoopBackDev::new, SocketSet::new, SpinLock::new, client.bind, client.bind(233).unwrap ...(+26) | Instant::from_millis, LoopBackDev::new, SocketSet::new, SpinLock::new, client.bind, client.bind(233).unwrap ...(+26) |
| `NoAxiom/kernel/src/net/tcpsocket.rs#connect` | `NoAxiom/kernel/src/net/udpsocket.rs#connect` | 同板块同名 | Err, Ok, SOCKET_SET.lock, debug!, drop, error! ...(+5) | Err, Ok, SOCKET_SET.lock, Some, UDP_PORT_MANAGER.lock, assert_ne! ...(+12) |
| `NoAxiom/kernel/src/net/socket.rs#setsockopt` | `NoAxiom/kernel/src/net/socket.rs#setsockopt` | 同板块同名 | Err, IpOption::from_repr, Ok, PosixSocketOption::from_repr, PosixTcpSocketOptions::from_repr, Self::read_bool_opt ...(+5) | Ok |
| `NoAxiom/kernel/src/net/tcpsocket.rs#accept` | `NoAxiom/kernel/src/net/tcpsocket.rs#accept` | 同板块同名 | Err, Ok, SOCKET_SET.lock, Self::new_socket, TcpSocket::from_handle, core::mem::replace ...(+16) | Err, Ok, SOCKET_SET.lock, Self::new_socket, TcpSocket::from_handle, core::mem::replace ...(+15) |

#### 文件系统/第三方库
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `NoAxiom/kernel/src/fs/vfs/impls/ext4/dentry.rs#create` | `NoAxiom/kernel/src/fs/vfs/impls/ext4/dentry.rs#create` | 同板块同名 | Arc::new, Err, Ext4DirInode::new, Ext4DirInode::new_with_owner, Ext4FileInode::new, Ok ...(+50) | Arc::new, Err, Ext4DirInode::new, Ext4FileInode::new, Ok, assert! ...(+42) |
| `NoAxiom/kernel/src/fs/vfs/impls/ext4/file.rs#load_dir` | `NoAxiom/kernel/src/fs/vfs/impls/ext4/file.rs#load_dir` | 同板块同名 | Arc::new, DirEntryType::from_bits, Ext4DirInode::new, Ext4FileInode::new, InodeMode::from_bits_truncate, Ok ...(+23) | Arc::new, Ext4DirInode::new, Ext4FileInode::new, InodeMode::from_bits, InodeMode::from_bits(mode as u32).unwrap, Ok ...(+23) |
| `NoAxiom/kernel/src/fs/vfs/impls/ext4/dentry.rs#symlink` | `NoAxiom/kernel/src/fs/vfs/impls/ext4/dentry.rs#symlink` | 同板块同名同结构 | Ok, assert!, assert_no_lock!, debug!, downcast_inode.get_inode, downcast_inode.get_inode().lock ...(+19) | Ok, assert!, assert_no_lock!, debug!, downcast_inode.get_inode, downcast_inode.get_inode().lock ...(+19) |
| `NoAxiom/kernel/src/fs/vfs/impls/ext4/file.rs#base_write` | `NoAxiom/kernel/src/fs/vfs/impls/ext4/file.rs#base_write` | 同板块同名 | Err, Ok, assert!, assert_no_lock!, buf.len, ext4.get_inode_ref ...(+16) | Err, Ok, assert!, assert_no_lock!, buf.len, ext4.write_at ...(+12) |

#### 通用模块/其他
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `user/libd/src/lib_client.rs#resize_widgets` | `user/libd/src/lib_client.rs#resize_widgets` | 同板块同名同结构 | Size::new, child_margin_size.x_axis, child_margin_size.y_axis, child_size.x_axis, child_size.y_axis, child_widget.get_margin ...(+23) | Size::new, child_margin_size.x_axis, child_margin_size.y_axis, child_size.x_axis, child_size.y_axis, child_widget.get_margin ...(+23) |
| `user/libd/src/lib_client.rs#resize_widget_subtree` | `user/libd/src/lib_client.rs#resize_widget_subtree` | 同板块同名同结构 | Size::new, child_margin_size.x_axis, child_margin_size.y_axis, child_size.x_axis, child_size.y_axis, child_widget.get_margin ...(+18) | Size::new, child_margin_size.x_axis, child_margin_size.y_axis, child_size.x_axis, child_size.y_axis, child_widget.get_margin ...(+18) |
| `user/libd/src/lib_client.rs#on_event` | `user/libd/src/lib_client.rs#on_event` | 同板块同名同结构 | Some, event.args[0].clone, event.args[0].clone().try_into, event.args[0].clone().try_into().unwrap, exit, handler ...(+13) | Some, event.args[0].clone, event.args[0].clone().try_into, event.args[0].clone().try_into().unwrap, exit, handler ...(+13) |
| `user/libd/src/lib_request.rs#decode` | `user/libd/src/lib_request.rs#decode` | 同板块同名同结构 | Point::new, RawU24::new, RawU24::new(val).into, RequestArgType::Bool, RequestArgType::Color, RequestArgType::Id ...(+14) | Point::new, RawU24::new, RawU24::new(val).into, RequestArgType::Bool, RequestArgType::Color, RequestArgType::Id ...(+14) |


好的，我将遵循审计指令，基于提供的证据底座、控辩双方陈述及AST抽样证据，进行最终同源性定性仲裁。

### 1. 宏观判定
- **定性结论**: **部分逻辑借鉴 / 存在架构改动** (相似度: 49.95%，共同 AST 指纹: 1901；唯一 AST 指纹: 目标项目 3358，参考项目 2349；覆盖率: 目标项目 56.61%，参考项目 80.93%)
- **判定摘要**: 目标项目与参考项目同属一个名为 `NoAxiom` 的操作系统内核框架，共享大量由POSIX/Linux ABI规范、Rust语言惯用法及该框架内部API所决定的确定性代码结构。宏观数据显示，目标项目拥有远超参考项目的唯一AST指纹（3358 vs 2349），表明其进行了大量的独立开发与功能扩展。在控辩双方聚焦的“进程管理”板块，目标项目覆盖率仅为34.88%，进一步证实其大部分实现为独立新增。然而，在部分核心系统调用（如UID/GID权限管理族、`sys_capget`）和同步原语（如`OnceCell`）上，双方AST调用图谱高度一致，存在明显的同源痕迹。综合判断，目标项目并非对参考项目的系统性、整体性抄袭，而是在一个共同基础框架之上进行了独立演进和功能增强，但在部分底层逻辑上存在借鉴。

### 2. 全模块覆盖矩阵解读
| 模块 | 双方存在 | 共同指纹 | 覆盖率解读 | 证据强度 |
| :--- | :---: | :---: | :--- | :---: |
| **进程管理** | 是 | 75 | 目标覆盖率(34.88%)远低于参考(55.97%)，说明目标项目在此模块有大量独立扩展，但核心系统调用存在同源。 | **强** |
| **任务调度/异步运行时** | 是 | 268 | 目标覆盖率(48.38%)低于参考(67.85%)，表明目标项目在调度与异步机制上有显著独立实现，但共享了部分核心运行时结构。 | **中** |
| **内存管理** | 是 | 265 | 目标覆盖率(60.23%)低于参考(74.86%)，双方在页表、验证等底层机制上高度相似，但目标项目有独立的内存管理逻辑。 | **中** |
| **中断/异常处理** | 是 | 65 | 覆盖率极高（目标94.2%，参考97.01%），表明此模块逻辑高度收敛，主要由硬件架构（如RISC-V）和内核态处理规范决定，独立创新空间极小。 | **弱** |
| **系统调用** | 是 | 33 | 目标覆盖率(27.5%)远低于参考(78.57%)，说明目标项目实现了大量参考项目所没有的系统调用，总入口和部分调用存在同源。 | **中** |
| **文件系统** | 是 | 605 | 目标覆盖率(59.9%)低于参考(83.8%)，目标项目在VFS框架、Ext4、Proc等文件系统上既有共享的框架代码，也有独立的实现。 | **中** |
| **设备驱动** | 是 | 210 | 覆盖率极高（目标98.59%，参考99.53%），双方几乎共享了相同的驱动集合（如AHCI、SD卡、PLIC），这是典型的硬件平台绑定和驱动框架复用。 | **弱** |
| **网络协议栈** | 是 | 24 | 目标覆盖率(35.82%)低于参考(48.98%)，双方共享了基础的smoltcp网络栈集成逻辑，但目标项目有独立的协议实现。 | **弱** |
| **文件系统/第三方库** | 是 | 35 | 目标覆盖率(11.36%)极低，而参考(79.55%)较高。这强烈表明目标项目引入了大量独立的第三方库或文件系统实现，而参考项目此部分代码量少且大部分被目标项目覆盖。 | **弱** |
| **通用模块/其他** | 是 | 395 | 覆盖率极高（目标84.22%，参考96.34%），说明基础数据结构、工具函数、通用组件高度同源，构成了两个项目的公共基础库。 | **弱** |

### 3. 底层 AST 关键证据指纹表
| 所属板块 | 目标项目路径 | 参考项目路径 | 证据类型 | 技术说明 |
| :--- | :--- | :--- | :--- | :--- |
| **进程管理** | `NoAxiom/lib/ksync/src/async_lock/once_cell.rs#initialize_or_wait` | `NoAxiom/lib/ksync/src/async_lock/once_cell.rs#initialize_or_wait` | **同源铁证** | AST调用图谱完全一致，包括`(initializer)`、`State::Initialized.into`等内部状态流转节点，属于核心异步锁逻辑的完全复用。 |
| **进程管理** | `NoAxiom/kernel/src/syscall/process.rs#sys_capget` | `NoAxiom/kernel/src/syscall/process.rs#sys_capget` | **同源铁证** | 从`TASK_MANAGER.get`到`UserPtr`的读写操作链完全一致，`debug!`宏位置相同，属于能力获取系统调用的完全复用。 |
| **进程管理** | `NoAxiom/kernel/src/entry/init_proc.rs#schedule_spawn_with_path` | `NoAxiom/kernel/src/entry/init_proc.rs#schedule_spawn_with_path` | **同源铁证** | 初始化进程的ELF加载与任务创建流程完全一致，`InodeMode::from_bits(ALL_PERMISSIONS_MASK).unwrap`等关键节点排列相同。 |
| **进程管理** | `NoAxiom/kernel/src/task/futex.rs#insert_waiter` | `NoAxiom/kernel/src/task/futex.rs#insert_waiter` | **同源铁证** | Futex等待队列插入逻辑完全复用，`FutexWaiter::new`, `WaiterQueue::new`, `waiters.push_back`链路高度咬合。 |
| **进程管理** | `NoAxiom/kernel/src/syscall/process.rs#sys_setresuid` | `NoAxiom/kernel/src/syscall/process.rs#sys_setresuid` | **强证据** | UID/GID权限设置族函数之一。核心调用链`user_id.euid`, `user_id.set_euid`, `user_id.set_fsuid`完全一致，仅日志宏从`info!`替换为`trace!`。 |
| **进程管理** | `NoAxiom/kernel/src/syscall/process.rs#sys_setresgid` | `NoAxiom/kernel/src/syscall/process.rs#sys_setresgid` | **强证据** | UID/GID权限设置族函数之一。核心调用链`user_id.egid`, `user_id.gid`, `user_id.set_egid`完全一致，仅日志宏从`info!`替换为`trace!`。 |
| **进程管理** | `NoAxiom/kernel/src/task/task.rs#print_child_tree_dfs` | `NoAxiom/kernel/src/task/task.rs#print_child_tree_dfs` | **强证据** | 超长链式调用`pcb.parent.as_ref().map(\|x\| x.upgrade()).flatten()...`完全一致，这种复杂表达式独立编写的巧合概率极低。 |
| **进程管理** | `NoAxiom/kernel/src/fs/vfs/impls/proc/fd/file.rs#load_dir` | `NoAxiom/kernel/src/fs/vfs/impls/proc/fd/file.rs#load_dir` | **强证据** | Proc文件系统fd目录加载逻辑完全复用，`FdDentry::new`, `FdFileInode::new`, `TASK_MANAGER.get(x).ok_or_else`调用链高度一致。 |
| **进程管理** | `NoAxiom/kernel/src/task/exit.rs#exit_handler` | `NoAxiom/kernel/src/task/exit.rs#exit_handler` | **中证据** | 核心骨架`FUTEX_SHARED_QUEUE.lock().wake_waiter`与`SigDetail::Child`一致，但目标项目用`WaitStatus::from_exit_code`替换了`PROCESS_GROUP_MANAGER`逻辑，存在架构改动。 |
| **进程管理** | `NoAxiom/kernel/src/task/futex.rs#wake_waiter` | `NoAxiom/kernel/src/task/futex.rs#wake_waiter` | **中证据** | 核心唤醒逻辑`waiter.done.store`, `waiter.waker.wake`一致，但遍历策略不同（`self.inner.remove` vs `self.inner.iter`），存在算法重构。 |
| **进程管理** | `NoAxiom/kernel/src/fs/path.rs#get_dentry_parent` | `NoAxiom/kernel/src/fs/path.rs#get_dentry_parent` | **中证据** | 目标项目引入了`PATH_CACHE`缓存机制，参考项目无缓存，核心路径解析逻辑同源但存在架构升级。 |
| **任务调度/异步运行时** | `NoAxiom/kernel/src/syscall/fs.rs#sys_renameat2` | `NoAxiom/kernel/src/syscall/fs.rs#sys_renameat2` | **中证据** | 核心调用链`Arc::as_ptr`, `Arc::ptr_eq`, `RenameFlags::from_bits`一致，但目标项目增加了`FileFlags::empty`等额外校验。 |
| **内存管理** | `NoAxiom/kernel/src/mm/validate.rs#validate` | `NoAxiom/kernel/src/mm/validate.rs#validate` | **中证据** | 页表验证与刷新逻辑`Arch::current_root_ppn`, `Arch::tlb_flush`, `PageTable::from_ppn`高度一致，属于底层内存管理规范。 |
| **系统调用** | `NoAxiom/kernel/src/syscall/syscall.rs#syscall_inner` | `NoAxiom/kernel/src/syscall/syscall.rs#syscall_inner` | **中证据** | 系统调用总入口的分发逻辑同源，但目标项目分发函数数量(+257)远超参考(+156)，表明其实现了大量新系统调用。 |
| **文件系统** | `NoAxiom/kernel/src/fs/vfs/impls/ext4/dentry.rs#symlink` | `NoAxiom/kernel/src/fs/vfs/impls/ext4/dentry.rs#symlink` | **中证据** | Ext4符号链接创建逻辑完全一致，`assert_no_lock!`, `downcast_inode.get_inode`等调用链相同，属于特定文件系统驱动复用。 |
| **第三方库/公共依赖** | `user/libd/src/lib_widget.rs#on_event` | `user/libd/src/lib_widget.rs#on_event` | **弱证据** | 用户态图形库事件处理逻辑完全一致，`Decoder::convert_key`, `PaintElement::Rect`等调用链相同，属于上层应用框架复用。 |
| **第三方库/公共依赖** | `NoAxiom/lib/fatfs/src/boot_sector.rs#determine_sectors_per_fat` | `NoAxiom/lib/fatfs/src/boot_sector.rs#determine_sectors_per_fat` | **弱证据** | FAT文件系统引导扇区解析逻辑完全一致，属于第三方库`fatfs`的复用，非核心内核逻辑。 |

### 4. 核心代码调用流对比剖析
* **【进程管理 / Futex 机制】**
* **底层技术析理**: 双方均实现了Linux的futex（快速用户空间互斥锁）机制。在`sys_futex`系统调用中，核心操作流均为：解析`FutexAddr` -> 获取全局`FUTEX_SHARED_QUEUE`锁 -> 根据操作码执行`requeue`或`wake_waiter`。目标项目在`wake_waiter`的内部实现上采用了不同的遍历-删除策略（`self.inner.remove`），而参考项目采用先收集再处理的策略（`self.inner.iter`），显示出在共享核心语义下的独立算法优化。在`insert_waiter`函数上，双方则表现出完全一致的调用流，属于底层数据结构的标准操作，独立创新空间极小。
* **代码溯源链接**:
  - 目标项目: `NoAxiom/kernel/src/syscall/process.rs#sys_futex`
  - 参考项目: `NoAxiom/kernel/src/syscall/process.rs#sys_futex`

* **【进程管理 / 用户权限管理】**
* **底层技术析理**: 双方均实现了Linux的`setuid`/`setgid`系列系统调用族。在`sys_setresuid`等函数中，核心调用流高度一致：获取任务用户ID结构 -> 检查权限 -> 依次调用`set_euid`, `set_fsuid`, `set_suid`等方法。目标项目将参考项目中的`info!`/`error!`日志宏系统性地替换为`trace!`，这是一种降低日志级别的工程化改动，但未改变核心逻辑流。这组函数是“同源基础上进行微调”的典型证据。
* **代码溯源链接**:
  - 目标项目: `NoAxiom/kernel/src/syscall/process.rs#sys_setresuid`
  - 参考项目: `NoAxiom/kernel/src/syscall/process.rs#sys_setresuid`

* **【进程管理 / 进程退出处理】**
* **底层技术析理**: 在`exit_handler`中，双方共享了“唤醒futex等待者”和“发送SIGCHLD信号”的核心骨架。然而，目标项目在进程组清理上采用了与参考项目不同的逻辑：参考项目调用`PROCESS_GROUP_MANAGER.lock().remove`，而目标项目则使用`WaitStatus::from_exit_code`和`UserPtr::<i32>::new`来处理退出状态和通知。这表明目标项目在进程退出这一关键路径上进行了架构重构，而非简单复用。
* **代码溯源链接**:
  - 目标项目: `NoAxiom/kernel/src/task/exit.rs#exit_handler`
  - 参考项目: `NoAxiom/kernel/src/task/exit.rs#exit_handler`

### 5. 单方模块与降权项
* **单方模块/功能**: 目标项目在【进程管理】板块拥有328个函数，远超参考项目的207个。大量函数（如`sys_prctl`的完整实现）是目标项目独有的，参考项目仅为空实现或不存在。这属于目标项目的独立开发增量，不能作为同源证据。
* **降权项说明**:
  - **中断/异常处理、设备驱动**: 这些模块覆盖率极高（>90%），其相似性主要由硬件架构（RISC-V）和板级支持包（BSP）决定，属于平台绑定代码，独立开发空间极小，不构成强同源证据。
  - **第三方库/公共依赖**: `user/libd`（用户库）、`lib/fatfs`（FAT文件系统）等模块中的高度一致函数，属于对公共第三方库或框架的复用，不应作为核心内核逻辑的抄袭证据。
  - **跨板块同名函数**: 控辩双方均提及的`load_dir`、`root`等函数，虽在【进程管理】中被讨论，但其本质属于【文件系统】的VFS通用接口实现。此类跨板块同名同hash只能作为弱信号，并已在本报告中降权处理。

### 6. 保守结论
本次审计的客观AST相似度为49.95%，低于通常认定系统性抄袭的阈值。目标项目拥有3358个唯一AST指纹，显著多于参考项目，表明其整体上是一个在“NoAxiom”共同基础框架之上进行了大量独立开发和功能扩展的项目。证据显示，在进程管理的UID/GID权限设置族、`OnceCell`同步原语等少数底层核心逻辑上，存在高度同源的代码结构，可判定为“部分逻辑借鉴”。然而，在进程退出、Futex唤醒、路径缓存等更多关键路径上，目标项目展现出不同的架构设计或算法实现。因此，整体风险在于对共同基础框架的依赖和局部逻辑借鉴，而非对参考项目的整体性、伪装性抄袭。

------------------------------

