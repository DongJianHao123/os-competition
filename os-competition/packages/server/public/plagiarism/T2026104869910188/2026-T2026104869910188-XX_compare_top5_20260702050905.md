# 🛡️ OS 内核赛自动化代码级架构审计报告

**审计时间**: `2026-07-02 05:09:05`
**参赛作品名称**: `2026-T2026104869910188-XX`
**年份**: `2026`
**学校**: `XX`
**队伍名称**: `T2026104869910188`
**比赛名字**: `OS-Competition`
**比赛子赛道**: `Kernel-Implementation`
**仓库地址**: `https://gitlab.eduxiji.net/educg-group-43501-3132633/T2026104869910188-478`
**推测开发语言**: `Rust / C (由AST探针自动识别)`
**特征函数量**: `3023`

## ⚔️ 底层代码同源性对比 (AST Homology)
> 本部分由专门的代码审查智能体，对该项目与符合年份条件的历届最高相似度项目进行控制流与底层调用对比。新版报告会先给出确定性的全模块覆盖矩阵，再给出 Agent 的分模块解释，避免只围绕少量抽样函数展开。

### 🏆 Top 5 底层对标: vs `2024-10183_BOOT-吉林大学`（吉林大学 / 10183_BOOT）

#### 对方作品信息

**对方年份**: `2024`
**对方学校**: `吉林大学`
**对方队伍名称**: `10183_BOOT`
**对方比赛名字**: `OS-Competition`
**对方比赛子赛道**: `Kernel-Implementation`
**对方仓库地址**: `https://gitlab.eduxiji.net/educg-group-26010-2376550/T202410183992516-775`

> 双向覆盖率：共同 AST 指纹: 410；唯一 AST 指纹: 目标项目 2615，参考项目 743；覆盖率: 目标项目 15.68%，参考项目 55.18%

## 全模块对比总览（确定性证据）

> 总体覆盖率：共同 AST 指纹: 410；唯一 AST 指纹: 目标项目 2615，参考项目 743；覆盖率: 目标项目 15.68%，参考项目 55.18%

### 1. 模块覆盖矩阵
| 模块 | 目标函数数 | 参考函数数 | 共同唯一 AST 指纹 | Target 覆盖 | Base 覆盖 | 状态 |
| :--- | ---: | ---: | ---: | ---: | ---: | :--- |
| 进程管理 | 84 | 39 | 13 | 18.57% | 40.62% | 双方存在且有结构重合 |
| 任务调度/异步运行时 | 704 | 34 | 8 | 1.3% | 23.53% | 双方存在且有结构重合 |
| 内存管理 | 625 | 138 | 21 | 3.51% | 15.79% | 双方存在且有结构重合 |
| 中断/异常处理 | 34 | 15 | 5 | 17.24% | 33.33% | 双方存在且有结构重合 |
| 系统调用 | 64 | 7 | 3 | 5.08% | 50.0% | 双方存在且有结构重合 |
| 文件系统 | 238 | 85 | 6 | 2.83% | 8.45% | 双方存在且有结构重合 |
| 设备驱动 | 28 | 29 | 4 | 17.39% | 15.38% | 双方存在且有结构重合 |
| 网络协议栈 | 433 | 0 | 0 | 0.0% | 0.0% | 仅目标项目体现 |
| 文件系统/第三方库 | 475 | 485 | 331 | 85.31% | 82.75% | 双方存在且有结构重合 |
| 通用模块/其他 | 338 | 52 | 24 | 8.05% | 54.55% | 双方存在且有结构重合 |

### 2. 分模块证据清单

#### 进程管理
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `kernel/sysproc.c#sys_kill` | `kernel/syscall/sysproc.c#sys_kill` | 同板块同名 | acquire, argint, myproc, release, signal_send | argint, kill |
| `kernel/proc.c#sleep` | `kernel/proc.c#sleep` | 同板块同名 | acquire, holding, myproc, release, sched, signal_mask_bit | acquire, mykthread, release, sched |
| `user/sh.c#parseexec` | `user/sh.c#parseexec` | 同板块同名同结构 | execcmd, gettoken, panic, parseblock, parseredirs, peek | execcmd, gettoken, panic, parseblock, parseredirs, peek |
| `kernel/proc.c#procdump` | `kernel/proc.c#procdump` | 同板块同名同结构 | NELEM, printf | NELEM, printf |

#### 任务调度/异步运行时
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `user/sh.c#runcmd` | `user/sh.c#runcmd` | 同板块同名 | close, dup, exec, exit, fork1, fprintf ...(+5) | close, dup, exec, exit, fork1, fprintf ...(+5) |
| `kernel/console.c#consoleintr` | `drivers/console.c#consoleintr` | 同板块同名同结构 | C, acquire, consputc, procdump, release, wakeup | C, acquire, consputc, procdump, release, wakeup |
| `kernel/console.c#consoleread` | `drivers/console.c#consoleread` | 同板块同名同结构 | C, acquire, either_copyout, killed, myproc, release ...(+1) | C, acquire, either_copyout, killed, myproc, release ...(+1) |
| `kernel/spinlock.c#acquire` | `kernel/atomic/spinlock.c#acquire` | 同板块同名 | __sync_lock_test_and_set, __sync_synchronize, holding, mycpu, panic, push_off | __sync_lock_test_and_set, __sync_synchronize, holding, mycpu, panic, printf ...(+1) |

#### 内存管理
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `kernel/arch/riscv/virtio_disk.c#virtio_disk_init` | `platform/qemu/virtio_disk.c#virtio_disk_init` | 同板块同名 | IS_USED, R, __sync_synchronize, alloc_pages, get_zone_dma, initlock ...(+3) | R, initlock, memset, panic |
| `kernel/trap.c#kerneltrap` | `kernel/trap.c#kerneltrap` | 同板块同名 | cpuid, devintr, intr_get, mycpu, myproc, panic ...(+15) | devintr, intr_get, kerneltrapframedump, mykthread, myproc, panic ...(+9) |
| `kernel/vm.c#kvmmake` | `mm/vm.c#kvmmake` | 同板块同名 | PGROUNDDOWN, kalloc, kvmmap, memset | kalloc, kvmmap, memset |
| `kernel/sysfile.c#sys_openat` | `kernel/syscall/sysfile.c#sys_openat` | 同板块同名 | argfd, argint, argstr, dp->i_op->create, f->f_op->open, fdalloc ...(+12) | argfd, argint, argstr, dir_open, fdalloc, filealloc ...(+6) |

#### 中断/异常处理
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `kernel/arch/loongarch/interrupt.c#plic_claim` | `platform/qemu/plic.c#plic_claim` | 同板块同名 | eio_reg | PLIC_SCLAIM, cpuid |
| `kernel/arch/riscv/plic.c#plicinithart` | `platform/qemu/plic.c#plicinithart` | 同板块同名 | PLIC_SENABLE, PLIC_SPRIORITY, cpuid | PLIC_SENABLE, PLIC_SPRIORITY, cpuid |
| `kernel/arch/loongarch/interrupt.c#plic_complete` | `platform/qemu/plic.c#plic_complete` | 同板块同名 | eio_reg | PLIC_SCLAIM, cpuid |
| `kernel/arch/loongarch/interrupt.c#plicinit` | `platform/qemu/plic.c#plicinit` | 同板块同名 | r_ecfg, w_ecfg | 内部闭环逻辑 |

#### 系统调用
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `kernel/syscall.c#argstr` | `kernel/syscall/syscall.c#argstr` | 同板块同名同结构 | argaddr, fetchstr | argaddr, fetchstr |
| `kernel/syscall.c#argaddr` | `kernel/syscall/syscall.c#argaddr` | 同板块同名同结构 | argraw | argraw |
| `kernel/syscall.c#argint` | `kernel/syscall/syscall.c#argint` | 同板块同名同结构 | argraw | argraw |
| `user/ulib.c#sbrklazy` | `kernel/syscall/syssig.c#sys_rt_sigreturn` | 同板块同结构 | sys_sbrk | rt_sigreturn |

#### 文件系统
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `kernel/sysfile.c#sys_renameat2` | `kernel/syscall/sysfile.c#sys_renameat2` | 同板块同名 | argint, argstr, iput, namecmp, nameiparent, old_dp->i_op->rename | 内部闭环逻辑 |
| `kernel/sysfile.c#sys_getdents64` | `kernel/syscall/sysfile.c#sys_getdents64` | 同板块同名 | argaddr, argfd, argint, f->f_op->iterate, ilock, iunlock | argaddr, argfd, argint, direntnext |
| `kernel/sysfile.c#sys_write` | `kernel/syscall/sysfile.c#sys_write` | 同板块同名 | argaddr, argfd, argint, filewrite, recover_stdio | argaddr, argfd, argint, filewrite |
| `kernel/vfs/vfs.c#skipelem` | `fs/fat32/fat.c#skipelem` | 同板块同名 | memmove | memmove |

#### 设备驱动
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `kernel/uart.c#uartinit` | `platform/qemu/uart.c#uartinit` | 同板块同名 | WriteReg, initlock | WriteReg, initlock |
| `kernel/console.c#consolewrite` | `drivers/console.c#consolewrite` | 同板块同名 | either_copyin, uartwrite | acquire, either_copyin, release, uart8250_putc, uartputc |
| `user/sh.c#parseblock` | `user/sh.c#parseblock` | 同板块同名同结构 | gettoken, panic, parseline, parseredirs, peek | gettoken, panic, parseline, parseredirs, peek |
| `kernel/uart.c#uartputc_sync` | `platform/qemu/uart.c#uartputc_sync` | 同板块同名 | ReadReg, WriteReg, pop_off, push_off | ReadReg, WriteReg, pop_off, push_off |

#### 网络协议栈
仅目标项目中观察到该模块代表函数，参考项目没有同模块可对齐证据：
- 目标项目：`kernel/lwip/src/core/ipv6/nd6.c#netif`，调用摘要：LWIP_ASSERT, ip6_addr_isinvalid, ip6_addr_istentative, ip6_addr_life_isinfinite, nd6_send_ns, netif_ip6_addr ...(+9)
- 目标项目：`kernel/lwip/src/core/ipv4/dhcp.c#dhcp_bind`，调用摘要：LWIP_DEBUGF, LWIP_ERROR, PP_HTONL, autoip_stop, dhcp_set_state, ip4_addr1 ...(+5)
- 目标项目：`kernel/lwip/src/core/ipv6/ip6.c#ip6_forward`，调用摘要：IP6H_HOPLIM, IP6H_HOPLIM_SET, IP6H_NEXTH, IP6_ADDR_BLOCK1, IP6_ADDR_BLOCK2, IP6_ADDR_BLOCK3 ...(+18)
- 目标项目：`kernel/lwip/src/netif/ppp/auth.c#auth_check_options`，调用摘要：auth_number, exit, have_chap_secret, have_pap_secret, have_route_to, have_srp_secret ...(+3)

#### 文件系统/第三方库
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `kernel/fs-ext4/lwext4/src/ext4_balloc.c#ext4_balloc_alloc_block` | `fs/lwext4/ext4_balloc.c#ext4_balloc_alloc_block` | 同板块同名同结构 | ext4_balloc_get_bgid_of_block, ext4_balloc_get_block_of_bgid, ext4_balloc_set_bitmap_csum, ext4_balloc_verify_bitmap_csum, ext4_bg_get_block_bitmap, ext4_bg_get_free_blocks_count ...(+19) | ext4_balloc_get_bgid_of_block, ext4_balloc_get_block_of_bgid, ext4_balloc_set_bitmap_csum, ext4_balloc_verify_bitmap_csum, ext4_bg_get_block_bitmap, ext4_bg_get_free_blocks_count ...(+19) |
| `kernel/fs-ext4/lwext4/src/ext4.c#ext4_dir_rm` | `fs/lwext4/ext4.c#ext4_dir_rm` | 同板块同名同结构 | EXT4_MP_LOCK, EXT4_MP_UNLOCK, ext4_block_cache_write_back, ext4_dir_en_get_inode, ext4_dir_iterator_fini, ext4_dir_iterator_init ...(+19) | EXT4_MP_LOCK, EXT4_MP_UNLOCK, ext4_block_cache_write_back, ext4_dir_en_get_inode, ext4_dir_iterator_fini, ext4_dir_iterator_init ...(+19) |
| `kernel/fs-ext4/lwext4/src/ext4.c#ext4_generic_open2` | `fs/lwext4/ext4.c#ext4_generic_open2` | 同板块同名 | ext4_dir_destroy_result, ext4_dir_en_get_inode, ext4_dir_en_get_inode_type, ext4_dir_find_entry, ext4_fs_alloc_inode, ext4_fs_correspond_inode_mode ...(+15) | ext4_dir_destroy_result, ext4_dir_en_get_inode, ext4_dir_en_get_inode_type, ext4_dir_find_entry, ext4_fs_alloc_inode, ext4_fs_correspond_inode_mode ...(+12) |
| `kernel/fs-ext4/lwext4/src/ext4_journal.c#jbd_journal_prepare` | `fs/lwext4/ext4_journal.c#jbd_journal_prepare` | 同板块同名 | EXT4_BLOCK_ZERO, RB_FIND, TAILQ_FOREACH_REVERSE_SAFE, TAILQ_FOREACH_SAFE, TAILQ_REMOVE, ext4_bcache_clear_dirty ...(+17) | EXT4_BLOCK_ZERO, RB_FIND, TAILQ_FOREACH_REVERSE_SAFE, TAILQ_FOREACH_SAFE, TAILQ_REMOVE, ext4_bcache_clear_dirty ...(+17) |

#### 通用模块/其他
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `user/printf.c#vprintf` | `user/printf.c#vprintf` | 同板块同名 | printint, printptr, putc, va_arg | printint, printptr, putc, va_arg |
| `kernel/printf.c#printf` | `user/printf.c#printf` | 同板块同名 | acquire, consputc, printint, printptr, release, va_arg ...(+2) | va_start, vprintf |
| `kernel/lwip/src/apps/sntp/sntp.c#unknown` | `kernel/atomic/mutex.c#unknown` | 同板块同名 | 内部闭环逻辑 | 内部闭环逻辑 |
| `user/printf.c#printint` | `user/printf.c#printint` | 同板块同名 | putc | putc |


好的，我将以审计专家的身份，基于所有材料进行最终的同源性定性仲裁。

### 📝 审计结论

#### 1. 宏观判定
- **定性结论**: **独立开发**
  - **客观 AST 相似度**: 13.91%
  - **共同 AST 指纹**: 410
  - **唯一 AST 指纹**: 目标项目 2615，参考项目 743
  - **覆盖率**: 目标项目 15.68%，参考项目 55.18%
- **核心依据**: 整体相似度远低于同源性阈值。参考项目的高覆盖率主要由双方共有的第三方开源库（lwext4）贡献，而非核心内核逻辑。目标项目在进程/线程模型、信号处理、网络协议栈等核心领域与参考项目存在根本性架构差异。

#### 2. 全模块覆盖矩阵解读
- **进程管理**: 双方均存在。存在少量共同 AST 指纹。目标覆盖率 18.57%，参考覆盖率 40.62%。重合点集中于 POSIX 标准系统调用接口和 xv6 公共教学基线，如 `sys_getpid`、`sys_kill` 等。双方在核心对象（`myproc` vs `mykthread`）和信号处理机制上存在显著差异。证据强度：**弱**。
- **任务调度/异步运行时**: 双方均存在。存在极少量共同 AST 指纹。目标覆盖率仅 1.30%，参考覆盖率 23.53%。目标项目函数体量（704）远超参考项目（34），表明其实现了高度复杂的异步运行时，与参考项目简单的调度器有本质区别。证据强度：**弱**。
- **内存管理**: 双方均存在。存在少量共同 AST 指纹。目标覆盖率 3.51%，参考覆盖率 15.79%。重合点如 `kvmmake` 是 xv6 标准内核虚拟内存初始化流程。目标项目在物理内存分配（`alloc_pages`, `get_zone_dma`）上与参考项目（`kalloc`）存在不同抽象层次。证据强度：**弱**。
- **中断/异常处理**: 双方均存在。存在少量共同 AST 指纹。目标覆盖率 17.24%，参考覆盖率 33.33%。重合点如 `plicinit`、`plic_claim` 是 RISC-V PLIC 中断控制器的标准驱动代码，由硬件规范决定。证据强度：**弱**。
- **系统调用**: 双方均存在。存在少量共同 AST 指纹。目标覆盖率 5.08%，参考覆盖率 50.00%。重合点 `argint`、`argstr` 等是 xv6 标准参数获取函数，实现逻辑固定。证据强度：**弱**。
- **文件系统**: 双方均存在。存在少量共同 AST 指纹。目标覆盖率 2.83%，参考覆盖率 8.45%。重合点如 `sys_write`、`sys_openat` 是 POSIX 标准接口。目标项目采用 VFS 抽象层（`f->f_op->open`），与参考项目的直接调用（`dir_open`）在架构上不同。证据强度：**弱**。
- **设备驱动**: 双方均存在。存在少量共同 AST 指纹。目标覆盖率 17.39%，参考覆盖率 15.38%。重合点如 `uartinit`、`plicinit` 是 UART 和 PLIC 硬件的标准初始化序列。证据强度：**弱**。
- **网络协议栈**: **仅目标项目存在**。参考项目无此模块。目标项目集成了完整的 lwIP 协议栈，是其独立扩展的核心功能。证据强度：**未形成**。
- **第三方库/公共依赖**: 双方均存在。存在大量共同 AST 指纹。目标覆盖率 85.31%，参考覆盖率 82.75%。重合点几乎全部来自 `lwext4` 文件系统库，这是双方共同引入的开源代码，不构成一方对另一方的抄袭。证据强度：**弱（已降权）**。

#### 3. 底层 AST 关键证据指纹表
| 所属板块 | 目标项目路径 | 参考项目路径 | 证据类型 | 技术说明 |
| :--- | :--- | :--- | :--- | :--- |
| 进程管理 | `kernel/sysproc.c#sys_getpid` | `kernel/syscall/sysproc.c#sys_getpid` | 同板块同名 | POSIX 标准接口，实现均为通过当前进程结构体返回 PID，逻辑必然。 |
| 进程管理 | `kernel/proc.c#mycpu` | `kernel/proc.c#mycpu` | 同板块同名 | RISC-V 架构标准操作，通过读取 `tp` 寄存器获取 Hart ID。 |
| 进程管理 | `kernel/proc.c#sched` | `kernel/proc.c#sched` | 同板块同名 | 核心调度器，但目标项目使用 `myproc`，参考项目使用 `mykthread`，上下文对象不同。 |
| 系统调用 | `kernel/syscall.c#argint` | `kernel/syscall/syscall.c#argint` | 同板块同名同结构 | xv6 标准系统调用参数获取函数，实现逻辑固定。 |
| 中断/异常处理 | `kernel/arch/riscv/plic.c#plicinithart` | `platform/qemu/plic.c#plicinithart` | 同板块同名 | RISC-V PLIC 中断控制器标准初始化流程。 |
| 设备驱动 | `kernel/uart.c#uartinit` | `platform/qemu/uart.c#uartinit` | 同板块同名 | UART 16550 兼容硬件的标准初始化序列。 |
| 第三方库/公共依赖 | `kernel/fs-ext4/lwext4/src/ext4_balloc.c#ext4_balloc_alloc_block` | `fs/lwext4/ext4_balloc.c#ext4_balloc_alloc_block` | 同板块同名同结构 | 来自 lwext4 开源库的块分配函数，双方均为库的使用者。 |
| 第三方库/公共依赖 | `kernel/fs-ext4/lwext4/src/ext4.c#ext4_dir_rm` | `fs/lwext4/ext4.c#ext4_dir_rm` | 同板块同名同结构 | 来自 lwext4 开源库的目录删除函数，双方均为库的使用者。 |

#### 4. 核心代码调用流对比剖析
* **【进程管理/调度器】**
* **底层技术析理**: 两者均实现 `sched` 函数，但上下文管理模型根本不同。目标项目以 `myproc` 获取进程结构体，围绕进程进行调度；参考项目以 `mykthread` 获取内核线程结构体，围绕线程进行调度。这反映了进程中心与线程中心的架构差异。
* **代码溯源链接**:
  - 目标项目: `kernel/proc.c#sched`
  - 参考项目: `kernel/proc.c#sched`

* **【进程管理/信号处理】**
* **底层技术析理**: `sys_kill` 系统调用实现完全不同。目标项目将信号发送内联，直接调用 `signal_send` 操作进程的信号队列；参考项目则委托给独立的 `kill` 函数。目标项目拥有独立的信号子系统，而参考项目的信号处理与进程管理耦合更紧。
* **代码溯源链接**:
  - 目标项目: `kernel/sysproc.c#sys_kill`
  - 参考项目: `kernel/syscall/sysproc.c#sys_kill`

* **【进程管理/上下文获取】**
* **底层技术析理**: `myproc` 的实现揭示了不同的并发控制策略。目标项目通过 `push_off`/`pop_off` 显式管理中断使能，从 `mycpu` 获取当前进程指针；参考项目则通过 `mykthread` 间接获取，无显式中断控制。这体现了对底层中断屏蔽策略的不同选择。
* **代码溯源链接**:
  - 目标项目: `kernel/proc.c#myproc`
  - 参考项目: `kernel/proc.c#myproc`

#### 5. 单方模块与降权项
- **单方模块**:
  - **网络协议栈**: 目标项目独有的 `kernel/lwip/` 模块，包含 433 个函数，是独立开发的显著标志。
  - **lwIP 内部函数**: 控方指控清单中的 `nd6_process_autoconfig_prefix`、`sha1_process` 等函数均属于 lwIP 库，在参考项目中无对应项，不构成同源证据。
  - **`sys_sigaltstack`**: 目标项目独有，参考项目无此系统调用。
- **降权项**:
  - **第三方库/公共依赖**: `lwext4` 和 `lwIP` 是开源第三方库，其内部函数的高度重合是库引用的必然结果，已从核心指控中降权。
  - **公共接口与基线**: `user/sh.c`、`kernel/syscall.c` 中的 `parseexec`、`argint` 等函数源自 MIT xv6 教学操作系统，属于公共领域代码，其同名同结构不能作为抄袭证据。
  - **硬件规范驱动**: `plic.c`、`uart.c` 等驱动代码由 RISC-V 架构和硬件手册规定，实现方式高度收敛。

#### 6. 保守结论
综合评估，目标项目与参考项目的客观 AST 相似度仅为 13.91%，远低于同源性阈值。双方在进程/线程模型、信号处理、系统调用封装、网络协议栈等核心架构上存在根本性差异，体现了不同的设计目标与实现路径。控方所指控的“铁证”均能被解释为对 POSIX 标准、RISC-V 架构规范、MIT xv6 公共教学基线或第三方开源库（lwext4）的共同遵循。特别是目标项目独有的网络协议栈和高度复杂的异步运行时，是其独立开发的强有力证明。因此，整体风险极低，应认定为独立开发，驳回全部复用指控。

------------------------------

