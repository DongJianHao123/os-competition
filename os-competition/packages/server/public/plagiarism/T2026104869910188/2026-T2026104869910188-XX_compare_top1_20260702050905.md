# 🛡️ OS 内核赛自动化代码级架构审计报告

**审计时间**: `2026-07-02 05:09:05`
**参赛作品名称**: `2026-T2026104869910188-XX`
**年份**: `2026`
**学校**: `XX`
**队伍名称**: `T2026104869910188`
**比赛名字**: `OS-Competition`
**比赛子赛道**: `Kernel-Implementation`
**仓库地址**: `https://gitlab.eduxiji.net/educg-group-43501-3132633/T2026104869910188-478`
**推测开发语言**: `Rust / C (由AST探针自动识别)`
**特征函数量**: `3023`

## ⚔️ 底层代码同源性对比 (AST Homology)
> 本部分由专门的代码审查智能体，对该项目与符合年份条件的历届最高相似度项目进行控制流与底层调用对比。新版报告会先给出确定性的全模块覆盖矩阵，再给出 Agent 的分模块解释，避免只围绕少量抽样函数展开。

### 🏆 Top 1 底层对标: vs `2023-AVX-华中科技大学`（华中科技大学 / AVX）

#### 对方作品信息

**对方年份**: `2023`
**对方学校**: `华中科技大学`
**对方队伍名称**: `AVX`
**对方比赛名字**: `OS-Competition`
**对方比赛子赛道**: `Kernel-Implementation`
**对方仓库地址**: `https://gitlab.eduxiji.net/202310487101114/oskernel2023-avx`

> 双向覆盖率：共同 AST 指纹: 1110；唯一 AST 指纹: 目标项目 2615，参考项目 1740；覆盖率: 目标项目 42.45%，参考项目 63.79%

## 全模块对比总览（确定性证据）

> 总体覆盖率：共同 AST 指纹: 1110；唯一 AST 指纹: 目标项目 2615，参考项目 1740；覆盖率: 目标项目 42.45%，参考项目 63.79%

### 1. 模块覆盖矩阵
| 模块 | 目标函数数 | 参考函数数 | 共同唯一 AST 指纹 | Target 覆盖 | Base 覆盖 | 状态 |
| :--- | ---: | ---: | ---: | ---: | ---: | :--- |
| 进程管理 | 84 | 82 | 24 | 34.29% | 34.78% | 双方存在且有结构重合 |
| 任务调度/异步运行时 | 704 | 568 | 350 | 56.91% | 72.16% | 双方存在且有结构重合 |
| 内存管理 | 625 | 485 | 258 | 43.14% | 55.36% | 双方存在且有结构重合 |
| 中断/异常处理 | 34 | 21 | 1 | 3.45% | 4.76% | 双方存在且有结构重合 |
| 系统调用 | 64 | 63 | 33 | 55.93% | 58.93% | 双方存在且有结构重合 |
| 文件系统 | 238 | 168 | 72 | 33.96% | 45.86% | 双方存在且有结构重合 |
| 设备驱动 | 28 | 19 | 2 | 8.7% | 11.76% | 双方存在且有结构重合 |
| 网络协议栈 | 433 | 404 | 301 | 80.48% | 87.76% | 双方存在且有结构重合 |
| 文件系统/第三方库 | 475 | 0 | 0 | 0.0% | 0.0% | 仅目标项目体现 |
| 通用模块/其他 | 338 | 173 | 83 | 27.85% | 53.55% | 双方存在且有结构重合 |

### 2. 分模块证据清单

#### 进程管理
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `kernel/lwip/src/core/ipv6/nd6.c#nd6_process_autoconfig_prefix` | `kernel/lwip/core/ipv6/nd6.c#nd6_process_autoconfig_prefix` | 同板块同名同结构 | IP6_ADDR, LWIP_ASSERT, ip6_addr_assign_zone, ip6_addr_cmp, ip6_addr_isduplicated, ip6_addr_isinvalid ...(+10) | IP6_ADDR, LWIP_ASSERT, ip6_addr_assign_zone, ip6_addr_eq, ip6_addr_isduplicated, ip6_addr_isinvalid ...(+10) |
| `kernel/lwip/src/netif/ppp/polarssl/sha1.c#sha1_process` | `kernel/lwip/netif/ppp/polarssl/sha1.c#sha1_process` | 同板块同名同结构 | GET_ULONG_BE, P, R | GET_ULONG_BE, P, R |
| `kernel/lwip/src/netif/ppp/polarssl/md5.c#md5_process` | `kernel/lwip/netif/ppp/polarssl/md5.c#md5_process` | 同板块同名同结构 | GET_ULONG_LE, P | GET_ULONG_LE, P |
| `kernel/lwip/src/netif/ppp/polarssl/md4.c#md4_process` | `kernel/lwip/netif/ppp/polarssl/md4.c#md4_process` | 同板块同名同结构 | GET_ULONG_LE, P | GET_ULONG_LE, P |

#### 任务调度/异步运行时
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `kernel/lwip/src/netif/lowpan6_common.c#lowpan6_decompress_hdr` | `kernel/lwip/netif/lowpan6_common.c#lowpan6_decompress_hdr` | 同板块同名 | IP6H_HOPLIM_SET, IP6H_NEXTH_SET, IP6H_PLEN_SET, IP6H_VTCFL_SET, LWIP_ASSERT, LWIP_DEBUGF ...(+5) | IP6H_HOPLIM_SET, IP6H_NEXTH_SET, IP6H_PLEN_SET, IP6H_VTCFL_SET, LWIP_ASSERT, LWIP_DEBUGF ...(+5) |
| `kernel/lwip/src/api/sockets.c#lwip_getsockopt_impl` | `kernel/lwip/api/sockets.c#lwip_getsockopt_impl` | 同板块同名 | LWIP_DEBUGF, LWIP_HOOK_SOCKETS_GETSOCKOPT, LWIP_SOCKOPT_CHECK_OPTLEN, LWIP_SOCKOPT_CHECK_OPTLEN_CONN, LWIP_SOCKOPT_CHECK_OPTLEN_CONN_PCB, LWIP_SOCKOPT_CHECK_OPTLEN_CONN_PCB_TYPE ...(+19) | LWIP_DEBUGF, LWIP_HOOK_SOCKETS_GETSOCKOPT, LWIP_SOCKOPT_CHECK_OPTLEN, LWIP_SOCKOPT_CHECK_OPTLEN_CONN, LWIP_SOCKOPT_CHECK_OPTLEN_CONN_PCB, LWIP_SOCKOPT_CHECK_OPTLEN_CONN_PCB_TYPE ...(+19) |
| `kernel/lwip/src/netif/ppp/lcp.c#lcp_nakci` | `kernel/lwip/netif/ppp/lcp.c#lcp_nakci` | 同板块同名 | BZERO, CHAP_CANDIGEST, CHAP_DIGEST, CHAP_MDTYPE, CHAP_MDTYPE_D, GETCHAR ...(+15) | BZERO, CHAP_CANDIGEST, CHAP_DIGEST, CHAP_MDTYPE, CHAP_MDTYPE_D, GETCHAR ...(+15) |
| `kernel/lwip/src/netif/ppp/eap.c#eap_request` | `kernel/lwip/netif/ppp/eap.c#eap_request` | 同板块同名 | BZERO, GETCHAR, GETLONG, INCPTR, MEMCPY, SHA1Final ...(+37) | BZERO, GETCHAR, GETLONG, INCPTR, MEMCPY, SHA1Final ...(+37) |

#### 内存管理
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `kernel/lwip/src/core/tcp_in.c#tcp_receive` | `kernel/lwip/core/tcp_in.c#tcp_receive` | 同板块同名 | (s16_t), (tcpwnd_size_t), (u16_t), (u8_t), LWIP_ASSERT, LWIP_DEBUGF ...(+39) | (s16_t), (tcpwnd_size_t), (u16_t), (u8_t), LWIP_ASSERT, LWIP_DEBUGF ...(+39) |
| `kernel/lwip/src/core/ipv6/nd6.c#nd6_input` | `kernel/lwip/core/ipv6/nd6.c#nd6_input` | 同板块同名同结构 | IP6H_HOPLIM, IP_SET_TYPE_VAL, LWIP_MIN, MEMCPY, ND6_STATS_INC, SMEMCPY ...(+41) | IP6H_HOPLIM, IP_SET_TYPE_VAL, LWIP_MIN, MEMCPY, ND6_STATS_INC, SMEMCPY ...(+41) |
| `kernel/lwip/src/api/sockets.c#lwip_setsockopt_impl` | `kernel/lwip/api/sockets.c#lwip_setsockopt_impl` | 同板块同名同结构 | (u32_t), (u8_t), LWIP_ASSERT, LWIP_DEBUGF, LWIP_HOOK_SOCKETS_SETSOCKOPT, LWIP_SOCKOPT_CHECK_OPTLEN_CONN ...(+38) | (u32_t), (u8_t), LWIP_ASSERT, LWIP_DEBUGF, LWIP_HOOK_SOCKETS_SETSOCKOPT, LWIP_SOCKOPT_CHECK_OPTLEN_CONN ...(+38) |
| `kernel/lwip/src/core/tcp_out.c#tcp_write` | `kernel/lwip/core/tcp_out.c#tcp_write` | 同板块同名 | LWIP_ASSERT, LWIP_ASSERT_CORE_LOCKED, LWIP_DEBUGF, LWIP_ERROR, LWIP_MAX, LWIP_MIN ...(+20) | LWIP_ASSERT, LWIP_ASSERT_CORE_LOCKED, LWIP_DEBUGF, LWIP_ERROR, LWIP_MAX, LWIP_MIN ...(+20) |

#### 中断/异常处理
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `kernel/arch/loongarch/interrupt.c#plic_claim` | `kernel/plic.c#plic_claim` | 同板块同名 | eio_reg | PLIC_MCLAIM, PLIC_SCLAIM, cpuid |
| `kernel/arch/riscv/virtio_disk.c#virtio_disk_intr` | `kernel/virtio_disk.c#virtio_disk_intr` | 同板块同名 | R, __sync_synchronize, acquire, panic, printf, release ...(+1) | R, acquire, panic, release, wakeup |
| `kernel/arch/riscv/plic.c#plicinithart` | `kernel/plic.c#plicinithart` | 同板块同名 | PLIC_SENABLE, PLIC_SPRIORITY, cpuid | PLIC_SENABLE, PLIC_SPRIORITY, cpuid, printf |
| `kernel/lwip/src/netif/ppp/utils.c#complete_read` | `kernel/lwip/netif/ppp/utils.c#complete_read` | 同板块同名同结构 | read | read |

#### 系统调用
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `kernel/lwip/src/api/sockets.c#lwip_pollscan` | `kernel/lwip/api/sockets.c#lwip_pollscan` | 同板块同名同结构 | LWIP_ASSERT, LWIP_DEBUGF, SYS_ARCH_DECL_PROTECT, SYS_ARCH_PROTECT, SYS_ARCH_UNPROTECT, done_socket ...(+1) | LWIP_ASSERT, LWIP_DEBUGF, SYS_ARCH_DECL_PROTECT, SYS_ARCH_PROTECT, SYS_ARCH_UNPROTECT, done_socket ...(+1) |
| `kernel/lwip/src/api/sockets.c#select_check_waiters` | `kernel/lwip/api/sockets.c#select_check_waiters` | 同板块同名同结构 | FD_ISSET, LWIP_ASSERT_CORE_LOCKED, SELECT_SEM_PTR, SYS_ARCH_DECL_PROTECT, SYS_ARCH_PROTECT, SYS_ARCH_UNPROTECT ...(+2) | FD_ISSET, LWIP_ASSERT_CORE_LOCKED, SELECT_SEM_PTR, SYS_ARCH_DECL_PROTECT, SYS_ARCH_PROTECT, SYS_ARCH_UNPROTECT ...(+2) |
| `kernel/lwip/src/api/netdb.c#lwip_gethostbyname` | `kernel/lwip/api/netdb.c#lwip_gethostbyname` | 同板块同名同结构 | LWIP_DEBUGF, ipaddr_ntoa, netconn_gethostbyname, strncpy, sys_thread_hostent | LWIP_DEBUGF, ipaddr_ntoa, netconn_gethostbyname, strncpy, sys_thread_hostent |
| `kernel/lwip/src/core/timeouts.c#lwip_cyclic_timer` | `kernel/lwip/core/timeouts.c#lwip_cyclic_timer` | 同板块同名同结构 | (u32_t), LWIP_DEBUGF, TIME_LESS_THAN, cyclic->handler, sys_now, sys_timeout_abs | (u32_t), LWIP_DEBUGF, TIME_LESS_THAN, cyclic->handler, sys_now, sys_timeout_abs |

#### 文件系统
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `kernel/lwip/src/core/ipv4/etharp.c#etharp_find_entry` | `kernel/lwip/core/ipv4/etharp.c#etharp_find_entry` | 同板块同名 | LWIP_ASSERT, LWIP_DEBUGF, LWIP_UNUSED_ARG, etharp_free_entry, ip4_addr_cmp, ip4_addr_copy | LWIP_ASSERT, LWIP_DEBUGF, LWIP_UNUSED_ARG, etharp_free_entry, ip4_addr_copy, ip4_addr_eq |
| `kernel/lwip/src/core/ipv4/ip4_frag.c#ip_reass_chain_frag_into_datagram_and_validate` | `kernel/lwip/core/ipv4/ip4_frag.c#ip_reass_chain_frag_into_datagram_and_validate` | 同板块同名同结构 | (u16_t), IPH_HL_BYTES, IPH_LEN, IPH_OFFSET_BYTES, LWIP_ASSERT, lwip_ntohs | (u16_t), IPH_HL_BYTES, IPH_LEN, IPH_OFFSET_BYTES, LWIP_ASSERT, lwip_ntohs |
| `kernel/lwip/src/core/ipv4/ip4.c#ip4_output_if_opt_src` | `kernel/lwip/core/ipv4/ip4.c#ip4_output_if_opt_src` | 同板块同名 | (u16_t), IF__NETIF_CHECKSUM_ENABLED, IPH_CHKSUM_SET, IPH_ID_SET, IPH_LEN_SET, IPH_OFFSET_SET ...(+25) | (u16_t), IF__NETIF_CHECKSUM_ENABLED, IPH_CHKSUM_SET, IPH_ID_SET, IPH_LEN_SET, IPH_OFFSET_SET ...(+25) |
| `kernel/lwip/src/api/sockets.c#lwip_recv_tcp` | `kernel/lwip/api/sockets.c#lwip_recv_tcp` | 同板块同名同结构 | (u16_t), LWIP_ASSERT, LWIP_DEBUGF, NETCONNTYPE_GROUP, err_to_errno, lwip_strerr ...(+7) | (u16_t), LWIP_ASSERT, LWIP_DEBUGF, NETCONNTYPE_GROUP, err_to_errno, lwip_strerr ...(+7) |

#### 设备驱动
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `kernel/uart.c#uartinit` | `kernel/uart.c#uartinit` | 同板块同名 | WriteReg, initlock | WriteReg, initlock |
| `kernel/lwip/src/netif/zepif.c#unknown` | `kernel/lwip/netif/zepif.c#unknown` | 同板块同名同结构 | 内部闭环逻辑 | 内部闭环逻辑 |
| `kernel/console.c#consolewrite` | `kernel/console.c#consolewrite` | 同板块同名 | either_copyin, uartwrite | acquire, either_copyin, release, sbi_console_putchar, uart8250_putc |
| `user/sh.c#parseblock` | `xv6-user/sh.c#parseblock` | 同板块同名同结构 | gettoken, panic, parseline, parseredirs, peek | gettoken, panic, parseline, parseredirs, peek |

#### 网络协议栈
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `kernel/lwip/src/core/ipv6/nd6.c#netif` | `kernel/lwip/core/ipv6/nd6.c#netif` | 同板块同名同结构 | LWIP_ASSERT, ip6_addr_isinvalid, ip6_addr_istentative, ip6_addr_life_isinfinite, nd6_send_ns, netif_ip6_addr ...(+9) | LWIP_ASSERT, ip6_addr_isinvalid, ip6_addr_istentative, ip6_addr_life_isinfinite, nd6_send_ns, netif_ip6_addr ...(+9) |
| `kernel/lwip/src/core/ipv4/dhcp.c#dhcp_bind` | `kernel/lwip/core/ipv4/dhcp.c#dhcp_bind` | 同板块同名 | LWIP_DEBUGF, LWIP_ERROR, PP_HTONL, autoip_stop, dhcp_set_state, ip4_addr1 ...(+5) | DHCP_SET_TIMEOUT_FROM_OFFERED_T0_LEASE, DHCP_SET_TIMEOUT_FROM_OFFERED_T1_RENEW, DHCP_SET_TIMEOUT_FROM_OFFERED_T2_REBIND, LWIP_DEBUGF, LWIP_ERROR, PP_HTONL ...(+7) |
| `kernel/lwip/src/core/ipv6/ip6.c#ip6_forward` | `kernel/lwip/core/ipv6/ip6.c#ip6_forward` | 同板块同名 | IP6H_HOPLIM, IP6H_HOPLIM_SET, IP6H_NEXTH, IP6_ADDR_BLOCK1, IP6_ADDR_BLOCK2, IP6_ADDR_BLOCK3 ...(+18) | IP6H_HOPLIM, IP6H_HOPLIM_SET, IP6H_NEXTH, IP6_ADDR_BLOCK1, IP6_ADDR_BLOCK2, IP6_ADDR_BLOCK3 ...(+18) |
| `kernel/lwip/src/netif/ppp/auth.c#auth_check_options` | `kernel/lwip/netif/ppp/auth.c#auth_check_options` | 同板块同名 | auth_number, exit, have_chap_secret, have_pap_secret, have_route_to, have_srp_secret ...(+3) | auth_number, exit, have_chap_secret, have_pap_secret, have_route_to, have_srp_secret ...(+3) |

#### 文件系统/第三方库
仅目标项目中观察到该模块代表函数，参考项目没有同模块可对齐证据：
- 目标项目：`kernel/fs-ext4/lwext4/src/ext4_balloc.c#ext4_balloc_alloc_block`，调用摘要：ext4_balloc_get_bgid_of_block, ext4_balloc_get_block_of_bgid, ext4_balloc_set_bitmap_csum, ext4_balloc_verify_bitmap_csum, ext4_bg_get_block_bitmap, ext4_bg_get_free_blocks_count ...(+19)
- 目标项目：`kernel/fs-ext4/lwext4/src/ext4.c#ext4_dir_rm`，调用摘要：EXT4_MP_LOCK, EXT4_MP_UNLOCK, ext4_block_cache_write_back, ext4_dir_en_get_inode, ext4_dir_iterator_fini, ext4_dir_iterator_init ...(+19)
- 目标项目：`kernel/fs-ext4/lwext4/src/ext4.c#ext4_generic_open2`，调用摘要：ext4_dir_destroy_result, ext4_dir_en_get_inode, ext4_dir_en_get_inode_type, ext4_dir_find_entry, ext4_fs_alloc_inode, ext4_fs_correspond_inode_mode ...(+15)
- 目标项目：`kernel/fs-ext4/lwext4/src/ext4_journal.c#jbd_journal_prepare`，调用摘要：EXT4_BLOCK_ZERO, RB_FIND, TAILQ_FOREACH_REVERSE_SAFE, TAILQ_FOREACH_SAFE, TAILQ_REMOVE, ext4_bcache_clear_dirty ...(+17)

#### 通用模块/其他
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `kernel/lwip/src/core/pbuf.c#pbuf_add_header_impl` | `kernel/lwip/core/pbuf.c#pbuf_add_header_impl` | 同板块同名 | (u16_t), LWIP_ASSERT, LWIP_DEBUGF | (u16_t), LWIP_ASSERT, LWIP_DEBUGF |
| `kernel/lwip/src/core/mem.c#mem_sanity` | `kernel/lwip/core/mem.c#mem_sanity` | 同板块同名同结构 | LWIP_ASSERT, LWIP_MEM_ALIGN, mem_link_valid, ptr_to_mem | LWIP_ASSERT, LWIP_MEM_ALIGN, mem_link_valid, ptr_to_mem |
| `user/printf.c#vprintf` | `xv6-user/printf.c#vprintf` | 同板块同名 | printint, printptr, putc, va_arg | printint, printptr, putc, va_arg |
| `kernel/printf.c#printf` | `xv6-user/printf.c#printf` | 同板块同名 | acquire, consputc, printint, printptr, release, va_arg ...(+2) | va_start, vprintf |


#### 1. 宏观判定
- **定性结论**: 底层架构显著差异 / 独立开发 (相似度: 34.21%，共同 AST 指纹: 1110；唯一 AST 指纹: 目标项目 2615，参考项目 1740；覆盖率: 目标项目 42.45%，参考项目 63.79%)

#### 2. 全模块覆盖矩阵解读
- **进程管理**: 双方均存在。共同 AST 指纹 24，目标覆盖率 34.29%，参考覆盖率 34.78%。双方覆盖率均较低且数值接近，表明该模块核心逻辑重合度有限。证据强度：**弱**。
- **任务调度/异步运行时**: 双方均存在。共同 AST 指纹 350，目标覆盖率 56.91%，参考覆盖率 72.16%。参考项目覆盖率显著高于目标项目，说明参考项目更接近公共基线，而目标项目有更多独立演化。证据强度：**中**。
- **内存管理**: 双方均存在。共同 AST 指纹 258，目标覆盖率 43.14%，参考覆盖率 55.36%。覆盖率差异表明双方在内存管理实现上存在不同侧重。证据强度：**中**。
- **中断/异常处理**: 双方均存在。共同 AST 指纹仅 1，覆盖率极低（目标 3.45%，参考 4.76%）。该模块几乎无结构重合，是架构差异的典型体现。证据强度：**未形成**。
- **系统调用**: 双方均存在。共同 AST 指纹 33，目标覆盖率 55.93%，参考覆盖率 58.93%。覆盖率相近，但辩方已指出在 `sys_kill`、`sys_clone` 等关键调用上存在实质性调用图谱差异。证据强度：**弱**。
- **文件系统**: 双方均存在。共同 AST 指纹 72，目标覆盖率 33.96%，参考覆盖率 45.86%。覆盖率不高，且目标项目额外拥有庞大的 `文件系统/第三方库` 模块（475个函数），是独立开发的强证据。证据强度：**弱**。
- **设备驱动**: 双方均存在。共同 AST 指纹 2，覆盖率极低（目标 8.7%，参考 11.76%）。双方驱动模型和硬件适配存在显著差异。证据强度：**未形成**。
- **网络协议栈**: 双方均存在。共同 AST 指纹 301，目标覆盖率 80.48%，参考覆盖率 87.76%。这是重合度最高的模块，但双方均源于对开源 lwIP 协议栈的集成，属于公共依赖。证据强度：**强（但降权为公共依赖）**。
- **第三方库/公共依赖**: 目标项目独有 `文件系统/第三方库` 模块（475个函数），包含 ext4 文件系统等实现，参考项目无此模块。这构成了目标项目独立开发的显著特征。证据强度：**未形成（指向独立开发）**。

#### 3. 底层 AST 关键证据指纹表
| 所属板块 | 目标项目路径 | 参考项目路径 | 证据类型 | 技术说明 |
| :--- | :--- | :--- | :--- | :--- |
| 网络协议栈 | `kernel/lwip/src/core/tcp_in.c#tcp_receive` | `kernel/lwip/core/tcp_in.c#tcp_receive` | 公共依赖 | 同为 lwIP 协议栈核心 TCP 接收处理函数，调用链高度一致源于同一开源基线。 |
| 网络协议栈 | `kernel/lwip/src/core/ipv6/nd6.c#nd6_input` | `kernel/lwip/core/ipv6/nd6.c#nd6_input` | 公共依赖 | 同为 lwIP 邻居发现协议输入处理，AST 结构由协议标准和库版本决定。 |
| 网络协议栈 | `kernel/lwip/src/api/sockets.c#lwip_setsockopt_impl` | `kernel/lwip/api/sockets.c#lwip_setsockopt_impl` | 公共依赖 | 同为 lwIP socket 层选项设置实现，宏和调用流一致是库内部 API 的必然结果。 |
| 网络协议栈 | `kernel/lwip/src/core/tcp_out.c#tcp_write` | `kernel/lwip/core/tcp_out.c#tcp_write` | 公共依赖 | 同为 lwIP TCP 数据发送逻辑，内部状态机与分段逻辑由协议栈统一实现。 |
| 任务调度/异步运行时 | `kernel/lwip/src/netif/lowpan6_common.c#lowpan6_decompress_hdr` | `kernel/lwip/netif/lowpan6_common.c#lowpan6_decompress_hdr` | 公共依赖 | 6LoWPAN 头部解压缩算法，调用宏序列一致源于 lwIP 官方实现。 |
| 任务调度/异步运行时 | `kernel/lwip/src/netif/ppp/lcp.c#lcp_nakci` | `kernel/lwip/netif/ppp/lcp.c#lcp_nakci` | 公共依赖 | PPP LCP 协议 NAK 配置项处理，复杂宏调用流一致是协议栈标准流程。 |
| 内存管理 | `kernel/lwip/src/core/tcp_in.c#tcp_receive` | `kernel/lwip/core/tcp_in.c#tcp_receive` | 公共依赖 | 内存管理操作（pbuf 分配/释放）内嵌于 lwIP 协议处理流程中，非独立内存管理模块。 |
| 系统调用 | `kernel/lwip/src/api/sockets.c#lwip_pollscan` | `kernel/lwip/api/sockets.c#lwip_pollscan` | 公共依赖 | lwIP socket 的 poll 扫描实现，临界区保护宏使用一致是库的可移植层要求。 |
| 文件系统 | `kernel/lwip/src/core/ipv4/etharp.c#etharp_find_entry` | `kernel/lwip/core/ipv4/etharp.c#etharp_find_entry` | 公共依赖 | ARP 表项查找逻辑，调用流一致源于 lwIP 协议栈内部实现。 |
| 第三方库/公共依赖 | `kernel/lwip/src/netif/ppp/polarssl/sha1.c#sha1_process` | `kernel/lwip/netif/ppp/polarssl/sha1.c#sha1_process` | 公共依赖 | PolarSSL 库的 SHA-1 轮函数，宏 `P`、`R` 是标准实现，非项目原创。 |
| 第三方库/公共依赖 | `kernel/lwip/src/netif/ppp/polarssl/md5.c#md5_process` | `kernel/lwip/netif/ppp/polarssl/md5.c#md5_process` | 公共依赖 | PolarSSL 库的 MD5 轮函数，算法流程由 RFC 1321 规定。 |
| 进程管理 | `kernel/proc.c#myproc` | `kernel/proc.c#myproc` | 弱信号 | 同为 xv6 风格获取当前进程控制块的函数，调用链 `mycpu -> push_off -> pop_off` 是 xv6 教学系统的经典范式。 |
| 进程管理 | `kernel/sysproc.c#sys_uptime` | `kernel/sysproc.c#sys_uptime` | 弱信号 | 同为 xv6 风格系统调用，`acquire/release` 保护 ticks 是 xv6 教材的标准同步模式。 |
| 进程管理 | `user/sh.c#parseexec` | `xv6-user/sh.c#parseexec` | 弱信号 | 同为 xv6 公开 Shell 的递归下降解析器，调用链由 Shell 语法决定，属公共教学代码。 |

#### 4. 核心代码调用流对比剖析
* **【进程管理/系统调用路径】**
* **底层技术析理**: 以 `sys_kill` 系统调用为例，目标项目调用链为 `acquire -> argint -> myproc -> release -> signal_send`，引入了**信号子系统**作为进程间通信与控制的抽象层。参考项目调用链为 `argint -> debug_print -> kill -> myproc`，采用直接调用 `kill` 函数的传统模型。目标项目在进程控制模型上增加了信号抽象，而参考项目则保留了更原始的 xv6 模型，两者在同步策略和对象管理模型上存在根本性架构差异。
* **代码溯源链接**:
  - 目标项目: `kernel/sysproc.c#sys_kill`
  - 参考项目: `kernel/sysproc.c#sys_kill`

* **【进程管理/调度路径】**
* **底层技术析理**: 以 `sched` 函数为例，目标项目调用链为 `holding -> intr_get -> mycpu -> myproc -> panic -> swtch`，调度上下文切换逻辑相对简洁。参考项目调用链为 `copytrapframe -> debug_print -> holding -> intr_get -> mycpu -> myproc -> panic -> swtch`，增加了 `copytrapframe` 用于陷阱帧的显式复制。这揭示了双方在上下文保存与恢复策略上的不同设计，目标项目可能在其他路径处理陷阱帧，而参考项目则在调度点集中处理。
* **代码溯源链接**:
  - 目标项目: `kernel/proc.c#sched`
  - 参考项目: `kernel/proc.c#sched`

* **【网络协议栈/公共依赖路径】**
* **底层技术析理**: 以 `tcp_timewait_input` 为例，双方调用链均为 `LWIP_ASSERT -> TCP_SEQ_BETWEEN -> ip_current_dest_addr -> ip_current_src_addr -> tcp_ack_now -> tcp_output -> tcp_rst`。此调用流完全由 lwIP 开源协议栈的 TCP TIME-WAIT 状态处理逻辑定义，是 RFC 793 标准的状态机实现，不包含任何项目特异性标识。此类高度一致是集成同一版本公共库的必然结果，不构成跨项目复用证据。
* **代码溯源链接**:
  - 目标项目: `kernel/lwip/src/core/tcp_in.c#tcp_timewait_input`
  - 参考项目: `kernel/lwip/core/tcp_in.c#tcp_timewait_input`

#### 5. 单方模块与降权项
- **单方模块**: 目标项目独有的 `文件系统/第三方库` 模块（475个函数），包含 `kernel/fs-ext4/lwext4/` 下的 ext4 文件系统实现，如 `ext4_balloc_alloc_block`、`ext4_dir_rm` 等。这是目标项目独立集成复杂子系统、与参考项目形成显著架构差异的强证据。
- **降权项**: 所有位于 `kernel/lwip/` 路径下的函数，以及 `user/sh.c`、`kernel/proc.c` 中与 xv6 公开教学代码同名的函数，均因存在明确的公共上游来源（lwIP、xv6、PolarSSL）而降权为“公共依赖”或“弱信号”。控方所指的 `ip6_addr_cmp` 与 `ip6_addr_eq` 的命名差异，经查是 lwIP 不同版本 API 的正常演变，反而佐证了双方可能集成了不同版本的公共库，而非相互抄袭。

#### 6. 保守结论
本次审计的宏观相似度仅 34.21%，低于通常判定抄袭的阈值。高重合度模块（网络协议栈）可被双方共同引入的开源 lwIP 协议栈完全解释，属于公共依赖。在操作系统核心的进程管理、内存管理、中断处理等模块，双方覆盖率不高，且在 `sys_kill`、`sched` 等关键路径上展现出信号模型、上下文切换策略等实质性架构差异。目标项目独有的 ext4 文件系统集成进一步强化了其独立演化的结论。综上，现有 AST 证据无法排他性地证明目标项目对参考项目构成抄袭，整体风险为低。

------------------------------

