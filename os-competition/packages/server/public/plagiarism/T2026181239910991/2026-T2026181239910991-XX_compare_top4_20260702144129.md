# 🛡️ OS 内核赛自动化代码级架构审计报告

**审计时间**: `2026-07-02 14:41:29`
**参赛作品名称**: `2026-T2026181239910991-XX`
**年份**: `2026`
**学校**: `XX`
**队伍名称**: `T2026181239910991`
**比赛名字**: `OS-Competition`
**比赛子赛道**: `Kernel-Implementation`
**仓库地址**: `https://gitlab.eduxiji.net/educg-group-43501-3132633/T2026181239910991-797`
**推测开发语言**: `Rust / C (由AST探针自动识别)`
**特征函数量**: `12900`

## ⚔️ 底层代码同源性对比 (AST Homology)
> 本部分由专门的代码审查智能体，对该项目与符合年份条件的历届最高相似度项目进行控制流与底层调用对比。新版报告会先给出确定性的全模块覆盖矩阵，再给出 Agent 的分模块解释，避免只围绕少量抽样函数展开。

### 🏆 Top 4 底层对标: vs `2025-StarryX-杭州电子科技大学`（杭州电子科技大学 / StarryX）

#### 对方作品信息

**对方年份**: `2025`
**对方学校**: `杭州电子科技大学`
**对方队伍名称**: `StarryX`
**对方比赛名字**: `OS-Competition`
**对方比赛子赛道**: `Kernel-Implementation`
**对方仓库地址**: `https://gitlab.eduxiji.net/educg-group-36002-2710490/T202510336995486-3037`

> 双向覆盖率：共同 AST 指纹: 85；唯一 AST 指纹: 目标项目 10464，参考项目 2897；覆盖率: 目标项目 0.81%，参考项目 2.93%

## 全模块对比总览（确定性证据）

> 总体覆盖率：共同 AST 指纹: 85；唯一 AST 指纹: 目标项目 10464，参考项目 2897；覆盖率: 目标项目 0.81%，参考项目 2.93%

### 1. 模块覆盖矩阵
| 模块 | 目标函数数 | 参考函数数 | 共同唯一 AST 指纹 | Target 覆盖 | Base 覆盖 | 状态 |
| :--- | ---: | ---: | ---: | ---: | ---: | :--- |
| 进程管理 | 35 | 172 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |
| 任务调度/异步运行时 | 525 | 480 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |
| 内存管理 | 338 | 430 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |
| 中断/异常处理 | 160 | 70 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |
| 系统调用 | 80 | 89 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |
| 文件系统 | 84 | 801 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |
| 设备驱动 | 15 | 139 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |
| 网络协议栈 | 63 | 77 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |
| 文件系统/第三方库 | 432 | 164 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |
| 网络协议栈/第三方库 | 4136 | 166 | 8 | 0.24% | 5.56% | 双方存在且有结构重合 |
| 第三方库 | 6527 | 349 | 17 | 0.32% | 5.8% | 双方存在且有结构重合 |
| 通用模块/其他 | 505 | 665 | 2 | 0.47% | 0.38% | 双方存在且有结构重合 |

### 2. 分模块证据清单

#### 进程管理
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `tools/netfast/netfast.c#main_tst_ns_exec` | `xcore/src/fs/vfs/proc/sys.rs#create_kernel_root` | 同模块上下文对齐/差异观察 | execvp_path, nf_exit, nf_fork, nf_memcpy, nf_strchr, nf_strcmp ...(+12) | "0\n".to_string, "16384\n".to_string, "2097152\n".to_string, "250	32000	32	128\n".to_string, "32000\n".to_string, "33554432\n".to_string ...(+32) |
| `tools/tx-trace-daemon/src/perfetto/writer.rs#emit_fork_flow` | `xapi/src/task/exit.rs#do_exit` | 同模块上下文对齐/差异观察 | Default::default, Some, child_comm.as_deref, child_pgrp.map, compute_flow_id, format! ...(+13) | FD_TABLE.clear, FutexKey::new, IPC_MANAGER.clear, SignalInfo::new, UserPtr::<Pid>::from, axtask::exit ...(+32) |
| `xtask/src/lint_invariants_step_v3.rs#lint_invariants_step_no_await` | `xapi/src/task/wait.rs#sys_wait4` | 同模块上下文对齐/差异观察 | Err, Ok, Vec::new, body_line.matches, body_line.matches('{').count, body_line.matches('}').count ...(+24) | Err, Ok, WaitOptions::from_bits, WaitPid::Pgid, WaitPid::Pid, child.data::<XProcess> ...(+26) |
| `xtask/src/lint_invariants_syscall.rs#lint_invariants_syscall_no_await` | `arceos/modules/axtask/src/run_queue.rs#exit_current` | 同模块上下文对齐/差异观察 | Err, Ok, Vec::new, body_line.contains, body_line.matches, body_line.matches('{').count ...(+29) | EXITED_TASKS.current_ref_mut_raw, EXITED_TASKS.current_ref_mut_raw().clear, EXITED_TASKS.current_ref_mut_raw().push_back, WAIT_FOR_EXIT.current_ref_mut_raw, WAIT_FOR_EXIT.current_ref_mut_raw().notify_one, assert! ...(+8) |

#### 任务调度/异步运行时
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `boards/tx-hal-loongarch64-qemu-virt/src/platform_impls.rs#restore` | `xmodules/xsignal/src/api/thread.rs#restore` | 同板块同名 | la64_restore_fp_context_raw | Layout::new::<SignalFrame>, check_region, check_region(
            uspace,
            tf.sp().into(),
            Layout::new::<SignalFrame>(),
            MappingFlags::WRITE,
        )
        .map_err, check_region(
            uspace,
            tf.sp().into(),
            Layout::new::<SignalFrame>(),
            MappingFlags::WRITE,
        )
        .map_err(|_| panic!("Failed to check signal frame"))
        .ok, frame.ucontext.mcontext.restore, panic! ...(+3) |
| `boards/tx-hal-loongarch64-qemu-virt/src/platform_impls.rs#save` | `arceos/modules/axhal/src/arch/x86_64/context.rs#save` | 同板块同名 | la64_save_fp_context | core::arch::x86_64::_fxsave64 |
| `tools/netfast/netfast.c#main_ping` | `arceos/ulib/axlibc/c/printf.c#format_string_loop` | 同模块上下文对齐/差异观察 | (u16), (u8), fmt1, fmt2, fmt3, fmt4 ...(+19) | ABS_FOR_PRINTING, ADVANCE_IN_FORMAT_STRING, atou_, is_digit_, out_rev_, parse_flags ...(+5) |
| `xtask/src/observe.rs#observe_demo` | `xapi/src/ipc/sem.rs#sys_semctl` | 同模块上下文对齐/差异观察 | ((i as u64 + 1) * 1000).to_le_bytes, (HEADER_SIZE as u16).to_le_bytes, (RECORD_SIZE as u16).to_le_bytes, (RINGS_OFF as u64).to_le_bytes, (i as u64).to_le_bytes, (num_records as u64).to_le_bytes ...(+78) | Err, with_ipc_manager! |

#### 内存管理
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `tools/user/tcp-loopback-smoke.c#_start` | `arceos/modules/axhal/src/platform/loongarch64_qemu_virt/boot.rs#_start` | 同板块同名 | bytes_equal, fail, loopback_addr, sys_accept, sys_bind, sys_close ...(+6) | core::arch::naked_asm! |
| `tools/tx-trace-daemon/src/perfetto/writer.rs#push_record` | `src/syscall.rs#handle_syscall_impl` | 同模块上下文对齐/差异观察 | "txtrace.repair.orphan_end".to_string, (r.payload_tag == TxPayloadTag::ArgValue as u16)
                        .then, Default::default, Some, comm.clone, debug_annotation_names.is_empty ...(+57) | Err, Ok, sys_accept, sys_accept4, sys_access, sys_arch_prctl ...(+203) |
| `xtask/src/lint_invariants_drive.rs#lint_invariants_no_adhoc_drive` | `arceos/ulib/axlibc/src/strftime.rs#strftime_inner` | 同模块上下文对齐/差异观察 | Err, Ok, Vec::new, [
        "crates/tx-scripts/src/drive.rs",
        "crates/tx-substrate/src/step/mod.rs",
    ]
    .iter, [
        "crates/tx-scripts/src/drive.rs",
        "crates/tx-substrate/src/step/mod.rs",
    ]
    .iter()
    .copied, [
        "crates/tx-scripts/src/drive.rs",
        "crates/tx-substrate/src/step/mod.rs",
    ]
    .iter()
    .copied()
    .collect ...(+33) | CountingWriter::new, format.offset, inner_strftime, w! |
| `xtask/src/ci.rs#ci` | `xapi/src/task/clone.rs#do_clone` | 同模块上下文对齐/差异观察 | Err, Ok, ci_run, ci_target_check, format!, matches! ...(+6) | Arc::default, Arc::new, CloneFlags::from_bits_truncate, Err, FD_TABLE
                .deref_from, FD_TABLE
                .deref_from(&process_data.ns)
                .init_new ...(+76) |

#### 中断/异常处理
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `boards/tx-hal-loongarch64-qemu-virt/src/platform_impls.rs#dispatch_irq` | `arceos/modules/axhal/src/platform/riscv64_visionfive2/irq.rs#dispatch_irq` | 同板块同名 | (table != 0)
            .then, (table != 0)
            .then(|| unsafe { &*(table as *const IrqDispatchTable) })
            .and_then, LA64_IRQ_DISPATCH_TABLE.load, Self::mask, handler, table.entries.get ...(+2) | with_cause! |
| `boards/tx-hal-riscv64-qemu-virt/src/lib.rs#dispatch_irq` | `arceos/modules/axhal/src/platform/riscv64_qemu_virt/irq.rs#dispatch_irq` | 同板块同名 | Self::mask, handler, installed_irq_table, valid_plic_irq | with_cause! |
| `boards/tx-hal-riscv64-qemu-virt/src/boot_static.rs#capture` | `arceos/ulib/axlibc/c/pow.c#exp_inline` | 同模块上下文对齐/差异观察 | BOOTSTRAP_ROOT.0.get, BootLinkedAddr::from_linked, BootLinkedAddr::from_runtime_addr, FirmwareDtb::from_addr, KERNEL_ALIAS_L0_TABLES_STORAGE.0.get, KERNEL_ALIAS_L1.0.get ...(+2) | __math_oflow, __math_uflow, asdouble, asuint64, converttoint, eval_as_double ...(+4) |
| `boards/tx-hal-loongarch64-qemu-virt/src/la64_irq_trap.rs#write_la64_csr` | `xmodules/xsignal/src/types.rs#default_action` | 同模块上下文对齐/差异观察 | core::arch::asm! | 内部闭环逻辑 |

#### 系统调用
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `tools/user/netns-helper.c#sys_recvfrom` | `arceos/api/arceos_posix_api/src/imp/net.rs#sys_recvfrom` | 同板块同名 | syscall6 | debug!, syscall_body! |
| `tools/user/netns-helper.c#sys_sendto` | `arceos/api/arceos_posix_api/src/imp/net.rs#sys_sendto` | 同板块同名 | syscall6 | debug!, syscall_body! |
| `tools/user/packet-probe.c#sys_getsockname` | `arceos/api/arceos_posix_api/src/imp/net.rs#sys_getsockname` | 同板块同名 | syscall6 | debug!, syscall_body! |
| `tools/user/tcp-loopback-smoke.c#sys_connect` | `arceos/api/arceos_posix_api/src/imp/net.rs#sys_connect` | 同板块同名 | syscall6 | debug!, syscall_body! |

#### 文件系统
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `tools/tx-trace-daemon/src/replay.rs#run` | `arceos/tools/deptool/src/lib.rs#run` | 同板块同名 | Ok, decode_file_bytes, decode_file_bytes(&data)
        .map_err, emit_json::emit, level_str_to_byte, std::fs::read ...(+1) | generate_deps_graph, output_deps_graph, print!, println! |
| `boards/tx-kernel-loongarch64-qemu-virt/src/main.rs#rust_entry` | `arceos/modules/axhal/src/platform/loongarch64_qemu_virt/mod.rs#rust_entry` | 同板块同名 | tx_hal::entry::<ActivePlatform, Kernel>, tx_hal_loongarch64_qemu_virt::capture_loongarch64_qemu_boot_args | crate::cpu::init_primary, crate::mem::clear_bss, rust_main, super::console::init_early, super::time::init_early, super::time::init_percpu ...(+1) |
| `tools/tx-trace-daemon/src/perfetto/span.rs#entry` | `xmodules/kernel_elf_parser/src/info.rs#entry` | 同板块同名 | 内部闭环逻辑 | self.elf.header.pt2.entry_point |
| `boards/tx-kernel-riscv64-m1dock-mock/src/main.rs#rust_entry` | `arceos/modules/axhal/src/platform/riscv64_qemu_virt/mod.rs#rust_entry` | 同板块同名 | tx_hal::entry::<ActivePlatform, Kernel> | crate::cpu::init_primary, crate::mem::clear_bss, riscv::register::sstatus::set_sum, rust_main, self::time::init_early |

#### 设备驱动
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `boards/tx-hal-loongarch64-qemu-virt/src/lib.rs#read_bytes` | `arceos/modules/axhal/src/platform/aarch64_bsta1000b/dw_apb_uart.rs#read_bytes` | 同板块同名 | uart_try_get_byte | bytes.len, getchar |
| `boards/tx-hal-loongarch64-qemu-virt/src/lib.rs#write_bytes` | `arceos/modules/axhal/src/platform/loongarch64_qemu_virt/console.rs#write_bytes` | 同板块同名 | uart_put_byte | UART.lock, uart.put |
| `boards/tx-hal-riscv64-qemu-virt/src/boot_static.rs#qemu_mmio_regions` | `arceos/tools/raspi4/chainloader/src/bsp/device_driver/bcm/bcm2xxx_pl011_uart.rs#init` | 同模块上下文对齐/差异观察 | PhysAddr, VirtAddr | FBRD::BAUD_DIVFRAC.val, IBRD::BAUD_DIVINT.val, self.flush, self.registers
            .CR
            .write, self.registers
            .LCR_H
            .write, self.registers.CR.set ...(+3) |
| `boards/tx-hal-loongarch64-qemu-virt/src/boot_firmware.rs#parse_efi_boot_info` | `arceos/tools/bwbench_client/src/device.rs#new` | 同模块上下文对齐/差异观察 | PhysAddr, Some, core::mem::size_of::<EfiConfigurationTable>, read_boot_phys::<EfiConfigurationTable>, read_boot_phys::<EfiInitrd>, read_boot_phys::<EfiSystemTable> ...(+8) | ETH_P_ALL.to_be, Err, Ok, dev.bind_interface, dev.interface_mtu, ifreq.ioctl ...(+5) |

#### 网络协议栈
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `tools/netfast/netfast.c#hexval` | `arceos/ulib/axlibc/c/network.c#hexval` | 同板块同名 | 内部闭环逻辑 | 内部闭环逻辑 |
| `tools/user/tcp-loopback-smoke.c#htonl` | `arceos/ulib/axlibc/c/network.c#htonl` | 同板块同名 | 内部闭环逻辑 | __bswap_32 |
| `tools/user/netcap-probe.c#htons` | `arceos/ulib/axlibc/c/network.c#htons` | 同板块同名 | (u16) | __bswap_16 |
| `tools/netfast/netfast.c#ip_run` | `arceos/ulib/axlibc/c/network.c#inet_pton` | 同模块上下文对齐/差异观察 | (u64), ifreq_ioctl, ip_addr_netlink, ip_route_state, nf_memcpy, nf_memset ...(+8) | hexval, inet_pton, isdigit, memmove |

#### 文件系统/第三方库
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `crates/tx-ext4/src/namespace.rs#rename` | `arceos/crates/axfs-ng-vfs/src/node/dir.rs#rename` | 同板块同名 | Errno::ENOENT.into, Errno::EROFS.into, StepOutcome::done, StepOutcome::err, e.into, inode_no ...(+7) | Err, Self::forget_entry, Some, core::ptr::eq, dir.has_children, dst.as_dir ...(+14) |
| `crates/tx-ext4/src/read_backend.rs#lookup` | `arceos/crates/lwext4_rust/src/inode/dir.rs#lookup` | 同板块同名 | Some, dir_entry.name, entry
                .entries
                .iter, entry
                .entries
                .iter()
                .take, entry
                .entries
                .iter()
                .take(entry.count)
                .any, entry.entries.iter ...(+4) | Ok, ext4_dir_find_entry, ext4_dir_find_entry(
                &mut result,
                self.inner.as_mut(),
                name.as_ptr() as *const _,
                name.len() as _,
            )
            .context, mem::zeroed, name.as_ptr, name.len ...(+1) |
| `crates/tx-ext4/src/host_async.rs#lookup` | `arceos/crates/axfs-ng-vfs/src/node/dir.rs#lookup` | 同板块同名 | DirEntryIter::new, Err, FsObjectId::new, Ok, Some, div_ceil_u64 ...(+5) | Err, name.len, self.cache.lock, self.lookup_locked, self.ops.is_cacheable, self.ops.lookup |
| `crates/tx-ext4/src/namespace.rs#lookup` | `arceos/modules/axfs-ng/src/fs/ext4/inode.rs#lookup` | 同板块同名 | Errno::ENOENT.into, StepOutcome::done, StepOutcome::err, err.into, inode_fs_object_id, inode_no ...(+2) | self.fs.lock, self.lookup_locked |

#### 网络协议栈/第三方库
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `external/smoltcp-asterinas/src/storage/assembler.rs#add` | `arceos/modules/axnet/src/smoltcp_impl/mod.rs#add` | 同板块同名 | Contig::hole_and_data, Err, Ok, contig.has_data, contig.shrink_hole_to, contig.total_size ...(+11) | debug!, self.0.lock, self.0.lock().add |
| `crates/tx-subsystems/src/net/device/bridge.rs#transmit` | `arceos/modules/axnet/src/smoltcp_impl/mod.rs#transmit` | 同板块同名 | Some, StepOutcome::Done, StepOutcome::Err, dst.is_multicast, egress.ops.transmit, egresses.is_empty ...(+11) | AxNetTxToken, Some, dev.can_transmit, dev.recycle_tx_buffers, self.inner.borrow_mut, warn! |
| `external/smoltcp-asterinas/src/socket/tcp.rs#connect` | `arceos/modules/axnet/src/smoltcp_impl/tcp.rs#connect` | 同板块同名 | Err, Ok, Self::random_seq_no, Some, addr.is_unspecified, cx
                    .get_source_address ...(+9) | Err, Ok, Ok::<(IpEndpoint, IpEndpoint), NetError>, SOCKET_SET
                .with_socket_mut::<tcp::Socket, _, _>, SOCKET_SET.add, SocketSetWrapper::new_tcp_socket ...(+33) |
| `external/smoltcp-asterinas/src/phy/fault_injector.rs#receive` | `arceos/modules/axnet/src/smoltcp_impl/mod.rs#receive` | 同板块同名 | <D::RxToken<'_> as phy::RxToken>::meta, Some, buffer.len, net_trace!, self.inner.receive, self.rx_buf.len ...(+5) | AxNetRxToken, AxNetTxToken, Some, dev.can_transmit, dev.receive, dev.recycle_tx_buffers ...(+3) |

#### 第三方库
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `crates/tx-fs/src/tmpfs/mod.rs#rename` | `arceos/crates/axfs-ng-vfs/src/mount.rs#rename` | 同板块同名 | InlineName::new, Some, StepOutcome::done, StepOutcome::err, adjust_directory_nlink, children.get ...(+16) | Arc::ptr_eq, Err, dst_dir.entry.as_dir, self.entry
            .as_dir, self.entry
            .as_dir()?
            .rename, self.entry.is_ancestor_of ...(+1) |
| `crates/tx-fs/src/tmpfs/mod.rs#unlink` | `arceos/crates/axfs-ng-vfs/src/mount.rs#unlink` | 同板块同名 | InlineName::new, StepOutcome::done, StepOutcome::err, children.get, children.get(&inline).map, children.remove ...(+8) | self.entry.as_dir, self.entry.as_dir()?.unlink |
| `crates/tx-fs/src/tmpfs/mod.rs#link` | `arceos/crates/axfs-ng-vfs/src/mount.rs#link` | 同板块同名 | InlineName::new, StepOutcome::done, StepOutcome::err, children.contains_key, children.insert, matches! ...(+7) | Arc::ptr_eq, Err, self.entry
            .as_dir, self.entry
            .as_dir()?
            .link, self.entry
            .as_dir()?
            .link(name, &node.entry)
            .map, self.wrap |
| `crates/tx-fs/src/procfs/mod.rs#read_link` | `arceos/crates/axfs-ng-vfs/src/node/mod.rs#read_link` | 同板块同名 | Errno::ENOENT.into, StepOutcome::done, StepOutcome::err, alloc::format!, path.into_boxed_slice, pid_from_exe_id ...(+8) | Err, String::from_utf8, String::from_utf8(buf).map_err, file.read_at, self.as_file, self.node_type ...(+1) |

#### 通用模块/其他
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `boards/tx-hal-riscv64-qemu-virt/src/lib.rs#write_bytes` | `arceos/modules/axhal/src/platform/riscv64_qemu_virt/console.rs#write_bytes` | 同板块同名 | for_each_console_byte_for_sbi | bytes.as_ptr, bytes.len, bytes.to_vec, kernel_bytes.len, try_write_bytes |
| `boards/tx-hal-riscv64-qemu-virt/src/pmap/mod.rs#current` | `xcore/src/ipc/util.rs#current` | 同板块同名 | core::arch::asm! | self.next_ipcid.load |
| `boards/tx-kernel-loongarch64-qemu-virt/src/main.rs#panic` | `arceos/modules/axruntime/src/lang_items.rs#panic` | 同板块同名 | Ok, tx_hal::console_write_str::<ActivePlatform>, tx_kernel::panic_shutdown::<ActivePlatform>, writeln! | axhal::misc::terminate, error! |
| `boards/tx-hal-riscv64-m1dock-mock/src/lib.rs#write_bytes` | `arceos/modules/axhal/src/platform/riscv64_visionfive2/console.rs#write_bytes` | 同板块同名 | sbi_console_putchar | bytes.as_ptr, bytes.len, bytes.to_vec, kernel_bytes.len, try_write_bytes |


#### 1. 宏观判定
- **定性结论**: 底层架构显著差异 / 独立开发 (相似度: 0.64%，共同 AST 指纹: 85；唯一 AST 指纹: 目标项目 10464，参考项目 2897；覆盖率: 目标项目 0.81%，参考项目 2.93%)

#### 2. 全模块覆盖矩阵解读

- **进程管理**
  1. 双方均存在该模块。
  2. 不存在共同 AST 指纹。
  3. 目标覆盖率 0.0%，参考覆盖率 0.0%。双方虽均有进程管理模块，但目标项目证据集中在用户态工具链（`netfast.c`、`xtask` lint 脚本、`tx-trace-daemon` 追踪解析器），参考项目证据为内核态实现（`xcore`、`xapi`、`arceos`）。两者调用的外部对象、数据结构和运行时环境完全不同，AST 层面无任何重合。
  4. 证据强度：未形成。

- **任务调度/异步运行时**
  1. 双方均存在该模块。
  2. 不存在共同 AST 指纹。
  3. 目标覆盖率 0.0%，参考覆盖率 0.0%。同名函数 `restore` 和 `save` 揭示了根本性差异：目标项目操作 LoongArch 硬件浮点上下文（`la64_restore_fp_context_raw`），参考项目操作信号栈帧与地址空间（`Layout::new::<SignalFrame>`、`check_region`）。两者调用链截然不同。
  4. 证据强度：未形成。

- **内存管理**
  1. 双方均存在该模块。
  2. 不存在共同 AST 指纹。
  3. 目标覆盖率 0.0%，参考覆盖率 0.0%。目标项目证据为启动阶段的静态内存捕获（`capture`）和用户态工具，参考项目为内核系统调用处理（`handle_syscall_impl`）和 C 库实现。无共同 AST 指纹。
  4. 证据强度：未形成。

- **中断/异常处理**
  1. 双方均存在该模块。
  2. 不存在共同 AST 指纹。
  3. 目标覆盖率 0.0%，参考覆盖率 0.0%。同名函数 `dispatch_irq` 在目标项目中基于自研的 `LA64_IRQ_DISPATCH_TABLE` 和 `installed_irq_table` 进行分发，参考项目使用 `with_cause!` 宏。两者中断控制器抽象和分发机制完全不同。
  4. 证据强度：未形成。

- **系统调用**
  1. 双方均存在该模块。
  2. 不存在共同 AST 指纹。
  3. 目标覆盖率 0.0%，参考覆盖率 0.0%。同名函数如 `sys_recvfrom`、`sys_connect` 源于 POSIX 标准接口。目标项目为用户态 libc 封装（直接调用 `syscall6`），参考项目为内核 POSIX API 包装（`debug!, syscall_body!`）。实现层次和调用路径完全不同。
  4. 证据强度：未形成。

- **文件系统**
  1. 双方均存在该模块。
  2. 不存在共同 AST 指纹。
  3. 目标覆盖率 0.0%，参考覆盖率 0.0%。同名函数如 `rust_entry` 是 Rust OS 启动约定入口，`run` 是通用工具入口。目标项目调用 `tx_hal::entry`，参考项目调用 `crate::cpu::init_primary`，实现路径无交集。
  4. 证据强度：未形成。

- **设备驱动**
  1. 双方均存在该模块。
  2. 不存在共同 AST 指纹。
  3. 目标覆盖率 0.0%，参考覆盖率 0.0%。同名函数 `read_bytes`、`write_bytes` 是 HAL 层标准接口。目标项目通过自研 `uart_try_get_byte`/`uart_put_byte` 实现，参考项目通过 `getchar`/`UART.lock` 实现，底层硬件抽象不同。
  4. 证据强度：未形成。

- **网络协议栈**
  1. 双方均存在该模块。
  2. 不存在共同 AST 指纹。
  3. 目标覆盖率 0.0%，参考覆盖率 0.0%。同名函数 `hexval`、`htonl`、`htons` 是标准算法或 POSIX 操作，极易独立实现。目标项目 `ip_run` 与参考项目 `inet_pton` 调用链完全不同。
  4. 证据强度：未形成。

- **第三方库/公共依赖**
  1. 双方均存在该模块。
  2. 存在共同 AST 指纹（文件系统/第三方库: 8，第三方库: 17，通用模块/其他: 2）。
  3. 目标覆盖率 0.24%~0.47%，参考覆盖率 0.38%~5.8%。所有重合指纹均来自共享第三方依赖（如 `smoltcp`、`lwext4`、`axfs-ng`）的 API 调用和语言级惯用法（`Err`/`Ok`、`Vec::new`、`format!`、`assert!`）。例如 `external/smoltcp-asterinas` 与 `arceos/modules/axnet` 均基于 `smoltcp` 库，`crates/tx-ext4` 与 `arceos/crates/lwext4_rust` 均操作 ext4 文件系统。这些共同指纹应归因于上游公共库，而非项目间抄袭。
  4. 证据强度：弱（已降权）。

#### 3. 底层 AST 关键证据指纹表

| 所属板块 | 目标项目路径 | 参考项目路径 | 证据类型 | 技术说明 |
| :--- | :--- | :--- | :--- | :--- |
| 任务调度/异步运行时 | `boards/tx-hal-loongarch64-qemu-virt/src/platform_impls.rs#restore` | `xmodules/xsignal/src/api/thread.rs#restore` | 同板块同名 | 目标操作 LoongArch 浮点上下文，参考操作信号栈帧，AST 调用链完全不同。 |
| 任务调度/异步运行时 | `boards/tx-hal-loongarch64-qemu-virt/src/platform_impls.rs#save` | `arceos/modules/axhal/src/arch/x86_64/context.rs#save` | 同板块同名 | 目标使用 `la64_save_fp_context`，参考使用 x86_64 `_fxsave64`，不同架构独立实现。 |
| 中断/异常处理 | `boards/tx-hal-loongarch64-qemu-virt/src/platform_impls.rs#dispatch_irq` | `arceos/modules/axhal/src/platform/riscv64_visionfive2/irq.rs#dispatch_irq` | 同板块同名 | 目标基于自研 `LA64_IRQ_DISPATCH_TABLE`，参考使用 `with_cause!` 宏，分发机制不同。 |
| 中断/异常处理 | `boards/tx-hal-riscv64-qemu-virt/src/lib.rs#dispatch_irq` | `arceos/modules/axhal/src/platform/riscv64_qemu_virt/irq.rs#dispatch_irq` | 同板块同名 | 目标使用 `installed_irq_table`，参考使用 `with_cause!`，实现路径不同。 |
| 系统调用 | `tools/user/netns-helper.c#sys_recvfrom` | `arceos/api/arceos_posix_api/src/imp/net.rs#sys_recvfrom` | 同板块同名 | 同名源于 POSIX 标准，目标为用户态 `syscall6` 封装，参考为内核 `syscall_body!` 包装。 |
| 系统调用 | `tools/user/tcp-loopback-smoke.c#sys_connect` | `arceos/api/arceos_posix_api/src/imp/net.rs#sys_connect` | 同板块同名 | 同上，实现层次完全不同。 |
| 文件系统 | `tools/tx-trace-daemon/src/replay.rs#run` | `arceos/tools/deptool/src/lib.rs#run` | 同板块同名 | 通用工具入口名，目标为追踪重放，参考为依赖图生成，功能无关。 |
| 文件系统 | `boards/tx-kernel-loongarch64-qemu-virt/src/main.rs#rust_entry` | `arceos/modules/axhal/src/platform/loongarch64_qemu_virt/mod.rs#rust_entry` | 同板块同名 | Rust OS 启动约定入口，目标调用 `tx_hal::entry`，参考调用 `crate::cpu::init_primary`。 |
| 设备驱动 | `boards/tx-hal-loongarch64-qemu-virt/src/lib.rs#read_bytes` | `arceos/modules/axhal/src/platform/aarch64_bsta1000b/dw_apb_uart.rs#read_bytes` | 同板块同名 | HAL 标准接口，目标用自研 `uart_try_get_byte`，参考用 `getchar`，硬件抽象不同。 |
| 设备驱动 | `boards/tx-hal-loongarch64-qemu-virt/src/lib.rs#write_bytes` | `arceos/modules/axhal/src/platform/loongarch64_qemu_virt/console.rs#write_bytes` | 同板块同名 | HAL 标准接口，目标用自研 `uart_put_byte`，参考用 `UART.lock`，实现不同。 |
| 网络协议栈 | `tools/netfast/netfast.c#hexval` | `arceos/ulib/axlibc/c/network.c#hexval` | 同板块同名 | 十六进制转换是标准算法，双方均为内部闭环逻辑，极易独立实现。 |
| 网络协议栈 | `tools/user/tcp-loopback-smoke.c#htonl` | `arceos/ulib/axlibc/c/network.c#htonl` | 同板块同名 | 字节序转换是 POSIX 标准操作，目标为内部逻辑，参考调用 `__bswap_32`。 |
| 第三方库 | `external/smoltcp-asterinas/src/socket/tcp.rs#connect` | `arceos/modules/axnet/src/smoltcp_impl/tcp.rs#connect` | 同板块同名 | 双方均基于 `smoltcp` 第三方库，共同指纹归因于上游 API。 |
| 第三方库 | `crates/tx-ext4/src/namespace.rs#rename` | `arceos/crates/axfs-ng-vfs/src/node/dir.rs#rename` | 同板块同名 | 双方均操作 VFS/ext4 语义，接口名源于文件系统领域公共命名。 |
| 第三方库 | `crates/tx-ext4/src/read_backend.rs#lookup` | `arceos/crates/lwext4_rust/src/inode/dir.rs#lookup` | 同板块同名 | 目标为自研遍历，参考调用 `lwext4` 库函数 `ext4_dir_find_entry`，实现路径不同。 |

#### 4. 核心代码调用流对比剖析

* **【进程管理/进程等待机制】**
* **底层技术析理**: 目标项目的 `wait_for_online_secondaries` 用于 SMP 启动阶段，通过原子位运算（`ONLINE_CPUS.load(Ordering::Acquire) & target`）和忙等待（`core::hint::spin_loop`）轮询 CPU 在线状态。参考项目的 `wait_until`/`wait_timeout` 用于运行时任务调度，通过获取当前运行队列（`current_run_queue::<NoPreemptIrqSave>`）并将任务阻塞在等待队列上（`rq.blocked_resched`、`self.queue.lock`）。前者是启动阶段的主动轮询，后者是调度器的被动阻塞，两者在同步策略、内存模型和调度层次上完全不同。
* **代码溯源链接**:
  - 目标项目: `boards/tx-hal-loongarch64-qemu-virt/src/boot_smp.rs#wait_for_online_secondaries`
  - 参考项目: `arceos/modules/axtask/src/wait_queue.rs#wait_until`

* **【任务调度/异步运行时/上下文恢复】**
* **底层技术析理**: 目标项目的 `restore` 函数直接调用架构相关汇编例程 `la64_restore_fp_context_raw`，仅负责恢复 LoongArch 浮点寄存器组。参考项目的 `restore` 函数则构建 `SignalFrame` 的内存布局（`Layout::new::<SignalFrame>`），检查用户空间地址有效性（`check_region`），并最终通过 `frame.ucontext.mcontext.restore` 恢复信号上下文。前者是硬件上下文操作，后者是软件信号栈帧管理，两者操作对象和抽象层次完全不同。
* **代码溯源链接**:
  - 目标项目: `boards/tx-hal-loongarch64-qemu-virt/src/platform_impls.rs#restore`
  - 参考项目: `xmodules/xsignal/src/api/thread.rs#restore`

* **【中断/异常处理/中断分发】**
* **底层技术析理**: 目标项目的 `dispatch_irq` 通过加载全局原子变量 `LA64_IRQ_DISPATCH_TABLE` 获取中断分发表，并从中索引 `handler` 进行调用。参考项目的 `dispatch_irq` 使用 `with_cause!` 宏，根据 RISC-V 特权架构的 `mcause` 寄存器值进行分支分发。两者在中断控制器的抽象层（自研表结构 vs 宏展开）和分发逻辑上无共同 AST 指纹。
* **代码溯源链接**:
  - 目标项目: `boards/tx-hal-loongarch64-qemu-virt/src/platform_impls.rs#dispatch_irq`
  - 参考项目: `arceos/modules/axhal/src/platform/riscv64_qemu_virt/irq.rs#dispatch_irq`

#### 5. 单方模块与降权项

- **单方独有模块**: 目标项目的 `xtask`（lint 脚本）、`tx-trace-daemon`（追踪解析器）、`netfast`（用户态网络工具）等模块在参考项目中无对应实现。这些属于目标项目独立构建的工程工具和测试基础设施，与参考项目的内核实现属于完全不同的软件层次。
- **第三方库/公共依赖降权**: 所有 85 个共同 AST 指纹均来自 `smoltcp`、`lwext4`、`axfs-ng` 等共享第三方库的 API 调用，以及 `Err`/`Ok`、`Vec::new`、`format!`、`assert!` 等语言级惯用法。这些指纹具有高度普遍性，不能作为项目间抄袭的证据。
- **同名函数降权**: `restore`、`dispatch_irq`、`rust_entry`、`write_bytes`、`sys_recvfrom` 等同名函数源于 OS 通用约定、POSIX 标准或 HAL 接口规范。尽管函数名相同，但底层 AST 调用链和实现逻辑均不同，已降级为弱信号。

#### 6. 保守结论

本次审计基于底层 AST 指纹比对，目标项目与参考项目的客观相似度仅为 0.64%，核心模块（进程管理、任务调度、内存管理、中断/异常、系统调用、文件系统、设备驱动、网络协议栈）的共同 AST 指纹均为 0。所有观察到的重合均来自共享第三方库（如 `smoltcp`、`lwext4`）的 API 调用和语言级惯用法，属于正常且不可避免的公共依赖。双方在进程等待机制、上下文恢复、中断分发等核心路径上的同步策略、内存模型和抽象层次存在根本性差异。综合判定，目标项目为独立开发，不存在对参考项目的系统性复用或抄袭。

------------------------------

