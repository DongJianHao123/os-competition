# 🛡️ OS 内核赛自动化代码级架构审计报告

**审计时间**: `2026-07-02 14:41:29`
**参赛作品名称**: `2026-T2026181239910991-XX`
**年份**: `2026`
**学校**: `XX`
**队伍名称**: `T2026181239910991`
**比赛名字**: `OS-Competition`
**比赛子赛道**: `Kernel-Implementation`
**仓库地址**: `https://gitlab.eduxiji.net/educg-group-43501-3132633/T2026181239910991-797`
**推测开发语言**: `Rust / C (由AST探针自动识别)`
**特征函数量**: `12900`

## ⚔️ 底层代码同源性对比 (AST Homology)
> 本部分由专门的代码审查智能体，对该项目与符合年份条件的历届最高相似度项目进行控制流与底层调用对比。新版报告会先给出确定性的全模块覆盖矩阵，再给出 Agent 的分模块解释，避免只围绕少量抽样函数展开。

### 🏆 Top 5 底层对标: vs `2025-sudo_pacman_Syu-同济大学`（同济大学 / sudo_pacman_Syu）

#### 对方作品信息

**对方年份**: `2025`
**对方学校**: `同济大学`
**对方队伍名称**: `sudo_pacman_Syu`
**对方比赛名字**: `OS-Competition`
**对方比赛子赛道**: `Kernel-Implementation`
**对方仓库地址**: `https://gitlab.eduxiji.net/educg-group-36002-2710490/T202510247995532-2817`

> 双向覆盖率：共同 AST 指纹: 68；唯一 AST 指纹: 目标项目 10464，参考项目 1840；覆盖率: 目标项目 0.65%，参考项目 3.7%

## 全模块对比总览（确定性证据）

> 总体覆盖率：共同 AST 指纹: 68；唯一 AST 指纹: 目标项目 10464，参考项目 1840；覆盖率: 目标项目 0.65%，参考项目 3.7%

### 1. 模块覆盖矩阵
| 模块 | 目标函数数 | 参考函数数 | 共同唯一 AST 指纹 | Target 覆盖 | Base 覆盖 | 状态 |
| :--- | ---: | ---: | ---: | ---: | ---: | :--- |
| 进程管理 | 35 | 154 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |
| 任务调度/异步运行时 | 525 | 164 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |
| 内存管理 | 338 | 261 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |
| 中断/异常处理 | 160 | 28 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |
| 系统调用 | 80 | 58 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |
| 文件系统 | 84 | 318 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |
| 设备驱动 | 15 | 162 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |
| 网络协议栈 | 63 | 51 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |
| 文件系统/第三方库 | 432 | 48 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |
| 网络协议栈/第三方库 | 4136 | 0 | 0 | 0.0% | 0.0% | 仅目标项目体现 |
| 第三方库 | 6527 | 740 | 31 | 0.58% | 5.02% | 双方存在且有结构重合 |
| 通用模块/其他 | 505 | 188 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |

### 2. 分模块证据清单

#### 进程管理
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `tools/netfast/netfast.c#main_tst_ns_exec` | `src/kernel/task.rs#execute` | 同模块上下文对齐/差异观察 | execvp_path, nf_exit, nf_fork, nf_memcpy, nf_strchr, nf_strcmp ...(+12) | Arc::new, AtomicBool::new, Context::from_waker, Some, Task::current, Task::current().clone ...(+14) |
| `tools/tx-trace-daemon/src/perfetto/writer.rs#emit_fork_flow` | `src/kernel/task/process.rs#build` | 同模块上下文对齐/差异观察 | Default::default, Some, child_comm.as_deref, child_pgrp.map, compute_flow_id, format! ...(+13) | Arc::new, BTreeMap::new, Locked::new, MMList::new, ProcessGroupBuilder::new, ProcessGroupBuilder::new()
                .leader ...(+22) |
| `xtask/src/lint_invariants_step_v3.rs#lint_invariants_step_no_await` | `src/kernel/task/process.rs#wait` | 同模块上下文对齐/差异观察 | Err, Ok, Vec::new, body_line.matches, body_line.matches('{').count, body_line.matches('}').count ...(+24) | Err, Ok, ProcessList::get, ProcessList::get().write, Some, assert! ...(+10) |
| `xtask/src/lint_invariants_syscall.rs#lint_invariants_syscall_no_await` | `src/kernel/task/process.rs#do_setpgid` | 同模块上下文对齐/差异观察 | Err, Ok, Vec::new, body_line.contains, body_line.matches, body_line.matches('{').count ...(+29) | Err, Ok, ProcessGroupBuilder::new, ProcessGroupBuilder::new()
                .leader, ProcessGroupBuilder::new()
                .leader(self)
                .session, ProcessGroupBuilder::new()
                .leader(self)
                .session(session.clone())
                .build ...(+16) |

#### 任务调度/异步运行时
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `xtask/src/fault_decode.rs#parse` | `src/kernel/task/loader/mod.rs#parse` | 同板块同名 | "explicit trap input requires --scause HEX --sepc HEX --stval HEX".into, "missing required option --target".to_string, "provide --serial, --addr, or --scause/--sepc/--stval".into, Err, FaultDecodeInput::Address, FaultDecodeInput::Trap ...(+15) | ByteBuffer::new, CString::new, CString::new(&line[..blank_pos]).unwrap, CString::new(&line[blank_pos + 1..]).unwrap, CString::new(line).unwrap, Dentry::open ...(+19) |
| `xtask/src/fault_decode.rs#load` | `src/kernel/task/loader/elf.rs#load` | 同板块同名 | Loader::new, Loader::new(path).ok, Ok, Vec::new, data.to_vec, demangle_symbol ...(+27) | MMList::new, Ok, ldso_load_info
                .as_ref, ldso_load_info
                .as_ref()
                .map, ldso_load_info.entry_ip.into, mm_list.register_break ...(+8) |
| `xtask/src/lib.rs#run` | `src/kernel/task/thread.rs#run` | 同板块同名 | Err, Ok, args.collect, args.next, boundary_report::boundary_report, check_build::build ...(+31) | self.contexted, self.real_run, stackful |
| `xtask/src/progress/mod.rs#parse` | `src/kernel/task/loader/elf.rs#parse` | 同板块同名 | Err, Ok, format! | ByteBuffer::from, Ok, UninitBuffer::<ElfHeader<E::Ea>>::new, elf_file.read, elf_header.assume_init, elf_header.assume_init().map_err ...(+3) |

#### 内存管理
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `boards/tx-hal-riscv64-qemu-virt/src/boot_static.rs#addr` | `src/kernel/mem/access.rs#addr` | 同板块同名 | 内部闭环逻辑 | 内部闭环逻辑 |
| `tools/tx-trace-daemon/src/perfetto/writer.rs#push_record` | `macros/src/lib.rs#define_syscall_impl` | 同模块上下文对齐/差异观察 | "txtrace.repair.orphan_end".to_string, (r.payload_tag == TxPayloadTag::ArgValue as u16)
                        .then, Default::default, Some, comm.clone, debug_annotation_names.is_empty ...(+57) | (0..arg_count)
            .map, (0..arg_count)
            .map(|_| String::from("{:x?}"))
            .collect::<Vec<_>>, (0..arg_count)
            .map(|_| String::from("{:x?}"))
            .collect::<Vec<_>>()
            .join, Ident::new, LitStr::new, Span::call_site ...(+18) |
| `xtask/src/lint_invariants_drive.rs#lint_invariants_no_adhoc_drive` | `src/kernel/task.rs#stackful` | 同模块上下文对齐/差异观察 | Err, Ok, Vec::new, [
        "crates/tx-scripts/src/drive.rs",
        "crates/tx-substrate/src/step/mod.rs",
    ]
    .iter, [
        "crates/tx-scripts/src/drive.rs",
        "crates/tx-substrate/src/step/mod.rs",
    ]
    .iter()
    .copied, [
        "crates/tx-scripts/src/drive.rs",
        "crates/tx-substrate/src/step/mod.rs",
    ]
    .iter()
    .copied()
    .collect ...(+33) | Arc::new, AtomicBool::new, Context::from_waker, KernelStack::new, Ok::<(), u32>, Some ...(+37) |
| `xtask/src/ci.rs#ci` | `src/kernel/pcie/init.rs#init_pcie` | 同模块上下文对齐/差异观察 | Err, Ok, ci_run, ci_target_check, format!, matches! ...(+6) | AcpiTables::search_for_rsdp_bios, Err, FDT
            .find_compatible, FDT
            .find_compatible(&["pci-host-ecam-generic"])
            .ok_or, Ok, PAddr::from ...(+50) |

#### 中断/异常处理
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `boards/tx-hal-riscv64-qemu-virt/src/boot_static.rs#capture` | `src/kernel/terminal.rs#commit_char` | 同模块上下文对齐/差异观察 | BOOTSTRAP_ROOT.0.get, BootLinkedAddr::from_linked, BootLinkedAddr::from_runtime_addr, FirmwareDtb::from_addr, KERNEL_ALIAS_L0_TABLES_STORAGE.0.get, KERNEL_ALIAS_L1.0.get ...(+2) | inner.termio.echok, inner.termio.echoke, inner.termio.icanon, inner.termio.icrnl, inner.termio.iexten, inner.termio.igncr ...(+16) |
| `boards/tx-hal-loongarch64-qemu-virt/src/la64_irq_trap.rs#write_la64_csr` | `src/kernel/terminal.rs#read` | 同模块上下文对齐/差异观察 | core::arch::asm! | Err, Thread::current, Thread::current().signal_list.has_pending_signal, buffer.available, buffer.available().min, buffer.fill ...(+17) |
| `boards/tx-hal-riscv64-qemu-virt/src/debug_trace.rs#record_trap` | `src/kernel/timer.rs#timer_interrupt` | 同模块上下文对齐/差异观察 | TRACE_COUNTER.fetch_add, crate::trap::console_write_hex, crate::trap::console_write_literal | CPU::local, CPU::local().cpuid, SLEEPERS_LIST.lock_irq, TICKS.fetch_add, WAKEUP_TICK.load, WAKEUP_TICK.store ...(+6) |
| `boards/tx-hal-loongarch64-qemu-virt/src/la64_irq_trap.rs#la64_pch_pic_write_u32` | `src/kernel/terminal.rs#new_standard` | 同模块上下文对齐/差异观察 | LA64_HOST_EIOINTC_COREISR0.fetch_and, LA64_HOST_PCH_PIC_MASK
                .fetch_update, LA64_HOST_PCH_PIC_MASK
                .fetch_update(Ordering::AcqRel, Ordering::Acquire, |old| {
                    Some((old & !(u32::MAX as u64)) | value as u64)
                })
                .ok, LA64_HOST_PCH_PIC_MASK
                .fetch_update(Ordering::AcqRel, Ordering::Acquire, |old| {
                    Some((old & (u32::MAX as u64)) | ((value as u64) << u32::BITS))
                })
                .ok, Some, core::mem::size_of::<u32> ...(+2) | CTRL!, core::array::from_fn |

#### 系统调用
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `tools/user/packet-probe.c#run_probe` | `src/kernel/mem/mm_list.rs#set_break` | 同模块上下文对齐/差异观察 | copy_ifname, fail_lit, htons, puts_lit, sys_bind, sys_close ...(+6) | Some, VRange::new, assert!, drop, inner.locked.lock, locked.areas.check_overlapping_range ...(+5) |
| `tools/user/nft-probe.c#start_c` | `src/kernel/syscall/sysinfo.rs#newuname` | 同模块上下文对齐/差异观察 | append_batch_marker, append_delchain, append_delrule, append_deltable, append_get, append_newchain ...(+8) | UserPointerMut::new, buffer.write, copy_cstr_to_array |
| `tools/netfast/netfast.c#applet_main` | `src/kernel/syscall/net.rs#get_sockopt` | 同模块上下文对齐/差异观察 | basename_nf, main_awk, main_bench_syscall, main_cat, main_cut, main_grep ...(+8) | Ok, UserPointerMut::new, UserPointerMut::new(optlen)?.write, UserPointerMut::new(optval.cast())?.write, optval.cast, size_of::<u32> |
| `tools/user/netns-helper.c#rtnl_setlink_u32_attr` | `src/kernel/syscall/net.rs#read_socket_addr` | 同模块上下文对齐/差异观察 | append_u32_attr, get_i32, put_u16, put_u32, sys_close, sys_recvfrom ...(+3) | Err, IpAddr::V4, Ipv4Addr::from_bits, Ok, SockDomain::try_from, SocketAddr::new ...(+6) |

#### 文件系统
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `tools/tx-trace-daemon/src/replay.rs#trace_stats_from_bytes` | `src/fs/tmpfs.rs#rename` | 同模块上下文对齐/差异观察 | Err, Ok, Vec::with_capacity, c.wrapping_add, data.as_ptr, data.len ...(+14) | (new_parent_inode.as_ref() as &dyn Any)
                .downcast_ref::<DirectoryInode>, (new_parent_inode.as_ref() as &dyn Any)
                .downcast_ref::<DirectoryInode>()
                .expect, Arc::as_ptr, Err, Instant::now, Ok ...(+53) |
| `tools/tx-trace-daemon/src/replay.rs#decode_file_bytes` | `src/kernel/syscall/file_rw.rs#do_copy_or_splice` | 同模块上下文对齐/差异观察 | Err, Ok, Vec::new, c.wrapping_add, data.as_ptr, data.len ...(+13) | (&buffer[..read_bytes]).into_stream, (offset + total_copied as isize).to_le_bytes, ByteBuffer::new, Ok, Some, UserBuffer::new ...(+19) |
| `tools/netfast/netfast.c#main_grep` | `src/kernel/vfs/inode.rs#statx` | 同模块上下文对齐/差异观察 | (u64), exec_busybox, grep_literal_safe, nf_memcpy, nf_strchr, nf_strlen ...(+3) | Ok, atime.into, ctime.into, mode.format_bits, mode.is_blk, mode.is_chr ...(+15) |
| `tools/tx-trace-daemon/src/replay.rs#run_live_guest_mem` | `src/fs/fat32/dir.rs#next` | 同模块上下文对齐/差异观察 | BufWriter::new, Duration::from_millis, File::create, Instant::now, LiveDrainTotals::default, LiveRingLayout::new ...(+34) | Err, Ok, Some, String::new, bytes.into, bytes.reverse ...(+25) |

#### 设备驱动
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `boards/tx-hal-riscv64-qemu-virt/src/boot_static.rs#qemu_mmio_regions` | `src/driver/virtio/riscv64.rs#init` | 同模块上下文对齐/差异观察 | PhysAddr, VirtAddr | Arc::new, ArchPhysAccess::as_ptr, BlockDevice::register_disk, BlockDevice::register_disk(
                        make_device(8, 16 * disk_id),
                        2147483647,
                        Arc::new(Spin::new(block_device)),
                    )
                    .expect, MmioTransport::new, Spin::new ...(+14) |
| `boards/tx-hal-loongarch64-qemu-virt/src/boot_firmware.rs#parse_efi_boot_info` | `src/driver/virtio/loongarch64.rs#handle_device` | 同模块上下文对齐/差异观察 | PhysAddr, Some, core::mem::size_of::<EfiConfigurationTable>, read_boot_phys::<EfiConfigurationTable>, read_boot_phys::<EfiInitrd>, read_boot_phys::<EfiSystemTable> ...(+8) | Arc::new, BlockDevice::register_disk, Err, Ok, PciRoot::new, PciTransport::new::<HAL, _> ...(+13) |
| `boards/tx-hal-loongarch64-qemu-virt/src/la64_pmap.rs#uart_try_get_byte` | `src/driver/serial.rs#init` | 同模块上下文对齐/差异观察 | Some, base.add, core::ptr::read_volatile, la64_uncached_virt | Ok, PAddr::from, Serial::new, SerialIO::new, port.register_as_char_device, serial.register_as_char_device |
| `boards/tx-hal-loongarch64-qemu-virt/src/platform_impls.rs#enable_uart_rx_irq` | `src/driver/serial.rs#new` | 同模块上下文对齐/差异观察 | base.add, core::ptr::write_volatile, la64_uncached_virt, read_la64_csr, write_la64_csr | Arc::from, Err, Ok, Spin::new, VecDeque::new, WaitList::new ...(+12) |

#### 网络协议栈
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `tools/netfast/netfast.c#ip_run` | `src/net/socket/tcp.rs#poll_impl` | 同模块上下文对齐/差异观察 | (u64), ifreq_ioctl, ip_addr_netlink, ip_route_state, nf_memcpy, nf_memset ...(+8) | Ok, PollEvent::empty, Some, block_on, events.contains, iface.lock ...(+12) |
| `tools/netfast/netfast.c#ip_route_state` | `src/net/socket/udp.rs#bind` | 同模块上下文对齐/差异观察 | (u64), nf_memcpy, nf_strcmp, nf_strlen, nf_strncmp, puts_fd ...(+3) | BoundSocket::new_bind_all, BoundSocket::new_bind_single, Err, Ok, Self::bind_impl, Some ...(+23) |
| `tools/netfast/netfast.c#parse_ipv6` | `src/net/socket/tcp.rs#accept_impl` | 同模块上下文对齐/差异观察 | (u16), (u8), hexval, nf_memset, nf_strchr, nf_strlen ...(+1) | Arc::new, BoundSocket::new_single, Err, Ok, RwLock::new, SocketAddr::from ...(+15) |
| `tools/netfast/netfast.c#vfmt` | `src/net/socket/udp.rs#drop` | 同模块上下文对齐/差异观察 | (u64), write_all | UDP_PORT_MAP.lock, UDP_PORT_MAP.lock().remove, block_on, bound_socket_guard.as_ref, bound_socket_guard.as_ref().unwrap, bound_socket_guard.is_none ...(+16) |

#### 文件系统/第三方库
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `crates/tx-ext4/src/namespace.rs#rename` | `src/fs/ext4.rs#rename` | 同板块同名 | Errno::ENOENT.into, Errno::EROFS.into, StepOutcome::done, StepOutcome::err, e.into, inode_no ...(+7) | Arc::as_ptr, Err, Instant::now, Ok, block_on, core::str::from_utf8 ...(+27) |
| `crates/tx-fs/src/tmpfs/mod.rs#mkdir` | `src/fs/ext4.rs#mkdir` | 同板块同名 | BTreeMap::new, InlineName::new, InodeMeta::new, StepOutcome::done, StepOutcome::err, TmpfsPayload::Directory ...(+9) | DirInode::new, InodeMode::from, Instant::now, Mode::DIR.perm, String::from_utf8_lossy, at.get_name ...(+18) |
| `crates/tx-fs/src/tx_ext4_bridge.rs#write_block` | `src/fs/ext4.rs#write_block` | 同板块同名 | Arc::new, Err, Frame::new, NonNull::new, NonNull::new(dst).ok_or, Ok ...(+21) | self
            .device
            .write_some |
| `crates/tx-ext4/src/read_backend.rs#lookup` | `src/fs/ext4.rs#lookup` | 同板块同名 | Some, dir_entry.name, entry
                .entries
                .iter, entry
                .entries
                .iter()
                .take, entry
                .entries
                .iter()
                .take(entry.count)
                .any, entry.entries.iter ...(+4) | AtomicMode::new, AtomicNlink::new, AtomicU32::new, AtomicU64::new, Err, InodeMode::BLOCKDEV.bits ...(+30) |

#### 网络协议栈/第三方库
仅目标项目中观察到该模块代表函数，参考项目没有同模块可对齐证据：
- 目标项目：`crates/tx-shims/src/linux_syscall/socket.rs#sys_setsockopt`，调用摘要：(TPACKET_V1..=TPACKET_V3).contains, Err, Ok, Some, SyscallResult::Error, SyscallResult::Return ...(+49)
- 目标项目：`external/smoltcp-asterinas/src/socket/tcp.rs#process`，调用摘要：AckDelayTimer::Waiting, IpEndpoint::new, Self::random_seq_no, Self::rst_reply, Some, control.quash_psh ...(+44)
- 目标项目：`crates/tx-shims/src/linux_syscall/socket.rs#sys_getsockopt`，调用摘要：(new_fd as i32).to_le_bytes, 1500u32.to_le_bytes, 2i32.to_le_bytes, 4i32.to_le_bytes, AF_INET.to_le_bytes, Err ...(+100)
- 目标项目：`crates/tx-shims/src/linux_syscall/socket.rs#sys_socket_ioctl`，调用摘要：(u32::from_be_bytes(addr.octets()) | !mask).to_be_bytes, AF_INET_U16.to_le_bytes, ARPHRD_ETHER.to_le_bytes, ARPHRD_LOOPBACK.to_le_bytes, Ipv4Address::new, Some ...(+87)

#### 第三方库
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `crates/tx-fs/src/tmpfs/mod.rs#truncate` | `crates/posix_types/src/open.rs#truncate` | 同板块同名 | StepOutcome::done, StepOutcome::err, container.clone, self.state.lock, state.inodes.get, state.inodes.get_mut ...(+1) | self.contains |
| `crates/tx-substrate/src/zone/slab.rs#allocate` | `crates/eonix_hal/src/mm.rs#allocate` | 同板块同名 | Err, NonNull::new, NonNull::new(slab_ptr).ok_or, NonNull::new(slots).ok_or, Ok, PhysAddr ...(+26) | Err, NonNull::slice_from_raw_parts, Ok, addr.add, addr.align_offset, allocated.cast::<u8> ...(+6) |
| `crates/tx-subsystems/src/vm/pmap/resident.rs#insert` | `crates/intrusive_list/src/lib.rs#insert` | 同板块同名 | ResidentChunk::new, chunk.push, last.len, last.push, replaced.is_none, self
            .chunks
            .last ...(+11) | NonNull::new, next.as_ptr |
| `crates/tx-substrate/src/zone/registry.rs#register` | `crates/eonix_percpu/src/lib.rs#register` | 同板块同名 | Err, Ok, Some, self.entries[index].get, self.lock.lock, self.published[index].store | PERCPU_POINTERS[cpuid].store, self.data.as_ptr |

#### 通用模块/其他
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `boards/tx-hal-loongarch64-qemu-virt/src/lib.rs#empty` | `src/rcu.rs#empty` | 同板块同名 | 内部闭环逻辑 | AtomicPtr::new, Self, core::ptr::null_mut |
| `boards/tx-kernel-loongarch64-qemu-virt/src/main.rs#write_str` | `src/io.rs#write_str` | 同板块同名 | Ok, tx_hal::console_write_str::<ActivePlatform> | Err, Ok, s.as_bytes, self.0.fill |
| `boards/tx-hal-loongarch64-qemu-virt/src/lib.rs#as_ptr` | `src/kernel/mem/access.rs#as_ptr` | 同板块同名 | self.0.get | align_of::<T>, panic!, self.addr.get, self.as_ptr_unchecked |
| `boards/tx-hal-riscv64-qemu-virt/src/lib.rs#as_ptr` | `src/kernel/mem/access.rs#as_ptr` | 同板块同名 | self.0.get | ArchPhysAccess::as_ptr |


#### 1. 宏观判定
- **定性结论**: **独立开发**
- **判定依据**: 客观 AST 相似度仅为 0.56%，共同 AST 指纹仅 68 个，而目标项目与参考项目的唯一 AST 指纹分别高达 10464 和 1840 个。双向覆盖率极低（目标项目 0.65%，参考项目 3.7%），且所有重合点均集中于“第三方库/公共依赖”模块。在操作系统内核的核心子系统（进程管理、内存管理、任务调度、系统调用等）中，**共同 AST 指纹均为 0**，表明双方在核心架构与实现上不存在可观测的代码复用。

#### 2. 全模块覆盖矩阵解读
- **进程管理**: 双方均存在该模块。共同 AST 指纹为 0，双向覆盖率均为 0.0%。证据强度：**未形成**。
- **任务调度/异步运行时**: 双方均存在该模块。共同 AST 指纹为 0，双向覆盖率均为 0.0%。证据强度：**未形成**。
- **内存管理**: 双方均存在该模块。共同 AST 指纹为 0，双向覆盖率均为 0.0%。证据强度：**未形成**。
- **中断/异常处理**: 双方均存在该模块。共同 AST 指纹为 0，双向覆盖率均为 0.0%。证据强度：**未形成**。
- **系统调用**: 双方均存在该模块。共同 AST 指纹为 0，双向覆盖率均为 0.0%。证据强度：**未形成**。
- **文件系统**: 双方均存在该模块。共同 AST 指纹为 0，双向覆盖率均为 0.0%。证据强度：**未形成**。
- **设备驱动**: 双方均存在该模块。共同 AST 指纹为 0，双向覆盖率均为 0.0%。证据强度：**未形成**。
- **网络协议栈**: 双方均存在该模块。共同 AST 指纹为 0，双向覆盖率均为 0.0%。证据强度：**未形成**。
- **第三方库/公共依赖**: 双方均存在该模块。存在 31 个共同 AST 指纹，目标覆盖率 0.58%，参考覆盖率 5.02%。重合源于共享的底层 crate（如 smoltcp 网络栈、ext4 文件系统抽象等），属于开源生态的正常依赖，不能作为核心架构复用的证据。证据强度：**弱**。

#### 3. 底层 AST 关键证据指纹表
| 所属板块 | 目标项目路径 | 参考项目路径 | 证据类型 | 技术说明 |
| :--- | :--- | :--- | :--- | :--- |
| 第三方库/公共依赖 | `crates/tx-fs/src/tmpfs/mod.rs#truncate` | `crates/posix_types/src/open.rs#truncate` | 同板块同名 | 同名函数 `truncate`，但目标操作 `StepOutcome` 状态机，参考操作 `self.contains` 位掩码，实现逻辑完全不同。 |
| 第三方库/公共依赖 | `crates/tx-substrate/src/zone/slab.rs#allocate` | `crates/eonix_hal/src/mm.rs#allocate` | 同板块同名 | 同名函数 `allocate`，均为内存分配器，但目标基于 `slab` 分配器模型，参考基于 `addr` 算术对齐，内部逻辑异构。 |
| 第三方库/公共依赖 | `crates/tx-subsystems/src/vm/pmap/resident.rs#insert` | `crates/intrusive_list/src/lib.rs#insert` | 同板块同名 | 同名函数 `insert`，目标向 `ResidentChunk` 链表插入，参考向侵入式链表插入，数据结构与操作对象不同。 |
| 第三方库/公共依赖 | `crates/tx-substrate/src/zone/registry.rs#register` | `crates/eonix_percpu/src/lib.rs#register` | 同板块同名 | 同名函数 `register`，目标注册到全局 `registry`，参考注册到 per-CPU 指针数组，架构设计不同。 |

#### 4. 核心代码调用流对比剖析
* **【进程管理/进程创建与执行路径】**
* **底层技术析理**: 目标项目的进程创建路径位于用户态工具 `netfast.c`，通过 `sys5` (nf_fork) 和 `sys3` (try_execve) 发起标准 Linux 系统调用，其本质是对 POSIX 标准 ABI 的裸调用封装。参考项目的路径位于内核 `src/kernel/syscall/procops.rs`，其 `execve` 函数通过 `Dentry::open`、`Path::new`、`ProgramLoader::parse(...).load`、`TrapContext::new` 等一系列内核对象协作完成程序加载。两者处于操作系统的不同抽象层（用户态 vs 内核态），操作的对象（系统调用号 vs 内核数据结构）和调用流完全不同，不存在 AST 级复用。
* **代码溯源链接**:
  - 目标项目: `tools/netfast/netfast.c#try_execve`
  - 参考项目: `src/kernel/syscall/procops.rs#execve`

* **【任务调度/异步运行时/调度入口】**
* **底层技术析理**: 目标项目的 `xtask/src/lib.rs#run` 是一个构建期任务入口，其调用流涉及 `args.collect`、`boundary_report`、`check_build` 等构建与检查工具。参考项目的 `src/kernel/task/thread.rs#run` 是内核线程调度入口，其调用流涉及 `self.contexted`、`self.real_run`、`stackful` 等内核栈与上下文切换操作。两者虽同名 `run`，但一个属于构建系统，一个属于内核调度核心，抽象层次与执行环境完全不同。
* **代码溯源链接**:
  - 目标项目: `xtask/src/lib.rs#run`
  - 参考项目: `src/kernel/task/thread.rs#run`

* **【内存管理/地址转换】**
* **底层技术析理**: 目标项目的 `boards/tx-hal-riscv64-qemu-virt/src/boot_static.rs#addr` 是一个板级启动阶段的静态地址转换函数，内部为闭环逻辑。参考项目的 `src/kernel/mem/access.rs#addr` 是内核内存访问抽象的一部分，同样为内部闭环逻辑。两者在各自模块中扮演不同角色，且均无复杂外部调用，其同名仅为通用词汇，不构成复用。
* **代码溯源链接**:
  - 目标项目: `boards/tx-hal-riscv64-qemu-virt/src/boot_static.rs#addr`
  - 参考项目: `src/kernel/mem/access.rs#addr`

* **【系统调用/网络相关】**
* **底层技术析理**: 目标项目的 `tools/user/packet-probe.c#run_probe` 是用户态网络探测工具，通过 `sys_bind`、`sys_close` 等标准 socket 系统调用实现功能。参考项目的 `src/kernel/syscall/net.rs#get_sockopt` 是内核态 `getsockopt` 系统调用的实现，通过 `UserPointerMut` 操作用户态内存。两者是同一套标准 Linux 网络 API 的调用方与实现方，遵循公共 ABI 契约，而非代码复用。
* **代码溯源链接**:
  - 目标项目: `tools/user/packet-probe.c#run_probe`
  - 参考项目: `src/kernel/syscall/net.rs#get_sockopt`

* **【文件系统/文件操作】**
* **底层技术析理**: 目标项目的 `crates/tx-ext4/src/namespace.rs#rename` 是第三方 ext4 库中的重命名实现，通过 `StepOutcome` 状态机返回结果。参考项目的 `src/fs/ext4.rs#rename` 是内核集成的 ext4 文件系统实现，通过 `block_on`、`Arc::as_ptr` 等内核原语操作。两者虽都处理 ext4 重命名，但一个在用户态/库层面，一个在内核态，调用流与依赖环境截然不同。
* **代码溯源链接**:
  - 目标项目: `crates/tx-ext4/src/namespace.rs#rename`
  - 参考项目: `src/fs/ext4.rs#rename`

#### 5. 单方模块与降权项
- **单方模块**: “网络协议栈/第三方库”模块仅在目标项目中体现（4136个函数），参考项目无此独立分类。这反映了目标项目将大量网络协议栈逻辑（如 smoltcp）作为外部库引入，而参考项目将其集成在内核源码树中，属于项目组织方式的差异，不构成同源证据。
- **第三方库/公共依赖**: 双方在 `truncate`、`allocate`、`insert`、`register` 等函数上存在同名，但这些均属于通用编程词汇，且实现逻辑完全不同。此类证据已降权处理。
- **公共接口与泛型函数名**: 控辩双方证据中大量出现的 `Vec::new`、`Err`/`Ok`、`Some`、`assert!`、`format!`、`matches!` 等属于 Rust 语言标准库的通用 API 和宏，`parse`、`run`、`new`、`as_ptr` 等属于软件工程中的高频函数名。这些均不构成独创性表达，不能作为同源性证据。

#### 6. 保守结论
本次审计基于客观 AST 指纹与调用图谱比对，认定两个项目在操作系统内核的核心子系统上不存在可观测的代码复用。整体 0.56% 的 AST 相似度完全源于第三方公共依赖库的正常重合。控方提出的“镜像解析”、“强耦合 Lint”、“ABI 契约复用”等指控，均建立在跨抽象层、跨模块的概念联想之上，缺乏 AST 层面的直接证据支持。双方在进程管理、内存管理、任务调度等关键路径上的实现技术栈、抽象层次和调用流均表现出根本性差异。综合判定，目标项目为独立开发，不构成对参考项目的抄袭或深度寄生。

------------------------------

