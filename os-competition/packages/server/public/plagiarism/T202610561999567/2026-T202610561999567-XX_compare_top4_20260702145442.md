# 🛡️ OS 内核赛自动化代码级架构审计报告

**审计时间**: `2026-07-02 14:54:42`
**参赛作品名称**: `2026-T202610561999567-XX`
**年份**: `2026`
**学校**: `XX`
**队伍名称**: `T202610561999567`
**比赛名字**: `OS-Competition`
**比赛子赛道**: `Kernel-Implementation`
**仓库地址**: `https://gitlab.eduxiji.net/educg-group-43501-3132633/T202610561999567-1105`
**推测开发语言**: `Rust / C (由AST探针自动识别)`
**特征函数量**: `2535`

## ⚔️ 底层代码同源性对比 (AST Homology)
> 本部分由专门的代码审查智能体，对该项目与符合年份条件的历届最高相似度项目进行控制流与底层调用对比。新版报告会先给出确定性的全模块覆盖矩阵，再给出 Agent 的分模块解释，避免只围绕少量抽样函数展开。

### 🏆 Top 4 底层对标: vs `2024-undefinedType-南京航空航天大学`（南京航空航天大学 / undefinedType）

#### 对方作品信息

**对方年份**: `2024`
**对方学校**: `南京航空航天大学`
**对方队伍名称**: `undefinedType`
**对方比赛名字**: `OS-Competition`
**对方比赛子赛道**: `Kernel-Implementation`
**对方仓库地址**: `https://gitlab.eduxiji.net/educg-group-26010-2376550/T202410287992755-2176`

> 双向覆盖率：共同 AST 指纹: 381；唯一 AST 指纹: 目标项目 2148，参考项目 398；覆盖率: 目标项目 17.74%，参考项目 95.73%

## 全模块对比总览（确定性证据）

> 总体覆盖率：共同 AST 指纹: 381；唯一 AST 指纹: 目标项目 2148，参考项目 398；覆盖率: 目标项目 17.74%，参考项目 95.73%

### 1. 模块覆盖矩阵
| 模块 | 目标函数数 | 参考函数数 | 共同唯一 AST 指纹 | Target 覆盖 | Base 覆盖 | 状态 |
| :--- | ---: | ---: | ---: | ---: | ---: | :--- |
| 进程管理 | 81 | 0 | 0 | 0.0% | 0.0% | 仅目标项目体现 |
| 任务调度/异步运行时 | 311 | 0 | 0 | 0.0% | 0.0% | 仅目标项目体现 |
| 内存管理 | 351 | 0 | 0 | 0.0% | 0.0% | 仅目标项目体现 |
| 中断/异常处理 | 30 | 0 | 0 | 0.0% | 0.0% | 仅目标项目体现 |
| 系统调用 | 184 | 0 | 0 | 0.0% | 0.0% | 仅目标项目体现 |
| 文件系统 | 477 | 0 | 0 | 0.0% | 0.0% | 仅目标项目体现 |
| 设备驱动 | 97 | 0 | 0 | 0.0% | 0.0% | 仅目标项目体现 |
| 网络协议栈 | 111 | 0 | 0 | 0.0% | 0.0% | 仅目标项目体现 |
| 文件系统/第三方库 | 707 | 478 | 381 | 63.5% | 95.73% | 双方存在且有结构重合 |
| 通用模块/其他 | 186 | 0 | 0 | 0.0% | 0.0% | 仅目标项目体现 |

### 2. 分模块证据清单

#### 进程管理
仅目标项目中观察到该模块代表函数，参考项目没有同模块可对齐证据：
- 目标项目：`os/src/syscall/task/wait.rs#sys_waitid`，调用摘要：Arc::clone, Err, Ok, Poll::Ready, Process::remove_from_global_map, SigInfo::new ...(+65)
- 目标项目：`os/src/syscall/task/wait.rs#sys_waitpid`，调用摘要：Arc::clone, Err, Ok, Poll::Ready, Process::remove_from_global_map, SigSet::from_sig ...(+68)
- 目标项目：`os/src/syscall/task/execve.rs#sys_execve`，调用摘要："ENOUGH=100000".to_string, "LANG=C".to_string, "LC_CTYPE=C".to_string, "PATH=/bin:.".to_string, Err, Ok ...(+47)
- 目标项目：`os/src/net/device/ethernet.rs#process_arp`，调用摘要：ArpPacket::new_checked, ArpPacket::new_checked(payload).and_then, ArpPacket::new_unchecked, ArpRepr::parse, IpAddress::Ipv4, Self::send_to ...(+20)

#### 任务调度/异步运行时
仅目标项目中观察到该模块代表函数，参考项目没有同模块可对齐证据：
- 目标项目：`os/src/fs/kernel_fs_ops/initfiles.rs#create_init_files`，调用摘要：Ok, String::from, UserBuffer::new, Vec::new, adj.as_mut_ptr, adj.len ...(+71)
- 目标项目：`os/src/syscall/io_mpx/select.rs#sys_pselect6`，调用摘要：Duration::new, Err, Ok, Poll::Ready, SelectResult::empty, SigSet::default ...(+43)
- 目标项目：`os/src/syscall/net/opt.rs#sys_getsockopt`，调用摘要：(optlen as usize).min, (val as i32).to_ne_bytes, Duration::from_secs, Err, GetSocketOption::DontRoute, GetSocketOption::Error ...(+38)
- 目标项目：`os/src/syscall/task/keys.rs#sys_keyctl`，调用摘要：Err, KEY_STORE.lock, Ok, crate::mm::copy_from_user, crate::mm::copy_to_user, current_task ...(+19)

#### 内存管理
仅目标项目中观察到该模块代表函数，参考项目没有同模块可对齐证据：
- 目标项目：`os/src/syscall/mod.rs#syscall`，调用摘要：Err, Ok, Syscall::from, log::debug!, shutdown, sys_accept ...(+223)
- 目标项目：`os/src/task/task/task.rs#clone_process`，调用摘要：0.into, Arc::clone, Arc::downgrade, Arc::new, AtomicBool::new, AtomicWaker::new ...(+67)
- 目标项目：`os/src/syscall/fs/fd_ops.rs#sys_fcntl`，调用摘要：Err, FcntlCmd::from_bits, FcntlCmd::from_bits(cmd).ok_or, Flock::from_bytes, Flock::from_bytes(&flock_bytes).ok_or, Ok ...(+35)
- 目标项目：`os/src/utils/error.rs#str`，调用摘要：panic!

#### 中断/异常处理
仅目标项目中观察到该模块代表函数，参考项目没有同模块可对齐证据：
- 目标项目：`os/src/signal/signal.rs#default_op`，调用摘要：continue_signals.contains, dump_signals.contains, ignore_signals.contains, stop_signals.contains, terminate_signals.contains
- 目标项目：`os/src/trap/mod.rs#trap_from_kernel`，调用摘要：Trap::Interrupt, VirtAddr::from, VirtAddr::from(stval).floor, backtrace, core::arch::asm!, get_trap_cause ...(+5)
- 目标项目：`os/src/trap/mod.rs#trap_from_kernel`，调用摘要：Trap::Interrupt, VirtAddr::from, VirtAddr::from(stval).floor, backtrace, error!, get_trap_cause ...(+1)
- 目标项目：`os/src/arch/irq/mod.rs#handle`，调用摘要：IRQ_HANDLERS.handle, IRQ_HOOK.load, Some, core::mem::transmute, hook_fn, println! ...(+1)

#### 系统调用
仅目标项目中观察到该模块代表函数，参考项目没有同模块可对齐证据：
- 目标项目：`os/src/syscall/mm/mmap.rs#sys_shmget`，调用摘要：Err, Ok, ShmFlags::from_bits, ShmFlags::from_bits(shmflag & !0x1ff).unwrap, flags.contains, shm_create ...(+1)
- 目标项目：`os/src/syscall/net/io.rs#sys_recvmsg`，调用摘要：(!msg.msg_name.is_null()).then_some, Ok, copy_msghdr_from_user, copy_msghdr_to_user, iovecs_to_buf_and_ub, msg.msg_name.is_null ...(+2)
- 目标项目：`os/src/syscall/net/io.rs#recv_impl`，调用摘要：(Ipv4Addr::UNSPECIFIED, 0).into, Ok, Socket::from_fd, SocketAddrEx::Ip, Some, addr.is_null ...(+5)
- 目标项目：`os/src/syscall/mm/mmap.rs#sys_shmat`，调用摘要：Err, ShmFlags::from_bits, ShmFlags::from_bits(shmflag).unwrap, shm_attach, shmflg.contains

#### 文件系统
仅目标项目中观察到该模块代表函数，参考项目没有同模块可对齐证据：
- 目标项目：`os/src/fs/files/loopdev.rs#ioctl`，调用摘要：Err, LOOP_TABLE[idx].lock, Ok, Some, copy_from_user, copy_to_user ...(+9)
- 目标项目：`os/src/net/tcp.rs#set_option_inner`，调用摘要：Duration::from_secs, Err, Ok, debug!, drop, extract_ipaddr_from_sockaddr ...(+17)
- 目标项目：`os/src/net/udp.rs#set_option_inner`，调用摘要：Err, Ok, Some, debug!, drop, extract_ipaddr_from_sockaddr ...(+15)
- 目标项目：`os/src/fs/map_dynamic_link.rs#map_library_path`，调用摘要：Some

#### 设备驱动
仅目标项目中观察到该模块代表函数，参考项目没有同模块可对齐证据：
- 目标项目：`os/src/net/router.rs#dispatch`，调用摘要：IpAddress::Ipv4, IpAddress::Ipv6, IpVersion::of_packet, IpVersion::of_packet(packet).expect, assert_eq!, dev.name ...(+13)
- 目标项目：`os/src/net/device/ethernet.rs#send`，调用摘要：Self::send_to, Some, addr.is_multicast, buf.copy_from_slice, dst_buffer.copy_from_slice, ipv4_multicast_mac ...(+9)
- 目标项目：`os/src/net/mod.rs#init_network`，调用摘要："eth0".to_owned, Box::new, EthernetAddress, EthernetDevice::new, GATEWAY.parse, GATEWAY.parse().expect ...(+29)
- 目标项目：`os/src/net/tcp.rs#bind`，调用摘要：Err, Ok, SOCKET_SET.bind_check, Some, get_ephemeral_port, get_service ...(+15)

#### 网络协议栈
仅目标项目中观察到该模块代表函数，参考项目没有同模块可对齐证据：
- 目标项目：`os/src/net/udp.rs#recv`，调用摘要：Err, ExpectedRemote::Any, ExpectedRemote::Expecting, Ok, SocketAddrEx::Ip, dst.write ...(+17)
- 目标项目：`os/src/net/udp.rs#send`，调用摘要：Err, IpAddr::V4, IpEndpoint::from, Ok, PacketMeta::default, SocketAddr::new ...(+23)
- 目标项目：`os/src/net/tcp.rs#get_option_inner`，调用摘要：Ok, self.general.get_option_inner, self.state, self.with_smol_socket, socket.keep_alive, socket.keep_alive().is_some ...(+1)
- 目标项目：`os/src/net/tcp.rs#recv`，调用摘要：Err, Ok, dst.write, options.flags.contains, poll_interfaces, recv_result.map_err ...(+9)

#### 文件系统/第三方库
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `crates/lwext4_rust/c/lwext4/src/ext4_balloc.c#ext4_balloc_alloc_block` | `Kernel/File/lwext4/ext4_balloc.c#ext4_balloc_alloc_block` | 同板块同名同结构 | ext4_balloc_get_bgid_of_block, ext4_balloc_get_block_of_bgid, ext4_balloc_set_bitmap_csum, ext4_balloc_verify_bitmap_csum, ext4_bg_get_block_bitmap, ext4_bg_get_free_blocks_count ...(+19) | EXT4_Debug_u32, ext4_balloc_get_bgid_of_block, ext4_balloc_get_block_of_bgid, ext4_balloc_set_bitmap_csum, ext4_balloc_verify_bitmap_csum, ext4_bg_get_block_bitmap ...(+19) |
| `crates/lwext4_rust/c/lwext4/src/ext4_journal.c#jbd_journal_prepare` | `Kernel/File/lwext4/ext4_journal.c#jbd_journal_prepare` | 同板块同名同结构 | EXT4_BLOCK_ZERO, RB_FIND, TAILQ_FOREACH_REVERSE_SAFE, TAILQ_FOREACH_SAFE, TAILQ_REMOVE, ext4_bcache_clear_dirty ...(+17) | EXT4_BLOCK_ZERO, RB_FIND, TAILQ_FOREACH_REVERSE_SAFE, TAILQ_FOREACH_SAFE, TAILQ_REMOVE, ext4_bcache_clear_dirty ...(+17) |
| `crates/lwext4_rust/c/lwext4/src/ext4.c#ext4_dir_rm` | `Kernel/File/lwext4/ext4.c#ext4_dir_rm` | 同板块同名同结构 | EXT4_MP_LOCK, EXT4_MP_UNLOCK, ext4_block_cache_write_back, ext4_dir_en_get_inode, ext4_dir_iterator_fini, ext4_dir_iterator_init ...(+19) | EXT4_MP_LOCK, EXT4_MP_UNLOCK, ext4_block_cache_write_back, ext4_dir_en_get_inode, ext4_dir_iterator_fini, ext4_dir_iterator_init ...(+19) |
| `crates/lwext4_rust/c/lwext4/src/ext4_dir_idx.c#ext4_dir_dx_split_index` | `Kernel/File/lwext4/ext4_dir_idx.c#ext4_dir_dx_split_index` | 同板块同名同结构 | ext4_block_set, ext4_dir_dx_climit_get_count, ext4_dir_dx_climit_get_limit, ext4_dir_dx_climit_set_count, ext4_dir_dx_climit_set_limit, ext4_dir_dx_entry_get_hash ...(+10) | ext4_block_set, ext4_dir_dx_climit_get_count, ext4_dir_dx_climit_get_limit, ext4_dir_dx_climit_set_count, ext4_dir_dx_climit_set_limit, ext4_dir_dx_entry_get_hash ...(+10) |

#### 通用模块/其他
仅目标项目中观察到该模块代表函数，参考项目没有同模块可对齐证据：
- 目标项目：`user/src/bin/ltp/mod.rs#run_ltp_tests_musl_separately`，调用摘要：LtpSummary::default, blacklist.contains, fork_run_ltp_and_collect, println!, summary.print, summary.record_run_result ...(+2)
- 目标项目：`user/src/bin/ltp/mod.rs#push`，调用摘要：chunk.len, self.tail.len, self.tail_len.saturating_sub
- 目标项目：`user/src/bin/fantastic_text.rs#main`，调用摘要：println!
- 目标项目：`user/src/bin/ltp/mod.rs#run_ltp_tests_glibc`，调用摘要：LtpSummary::default, blacklist.contains, fork_run_ltp_and_collect, println!, summary.print, summary.record_run_result ...(+1)


好的，我将遵循审计指令，以深度析理模式对本案进行最终同源性定性仲裁。

### 1. 宏观判定
- **定性结论**: **独立开发，底层架构显著差异**
  本案中，目标项目与参考项目的全部 AST 重合均集中于 **【文件系统/第三方库】** 板块，即双方共同引入的开源 C 库 `lwext4`。目标项目原创的 Rust 操作系统内核（涵盖进程管理、内存管理、网络协议栈等 9 大核心模块）与参考项目无任何 AST 级重合。相似度指标（17.6%）因第三方公共库的存在而被拉高，不能反映项目原创部分的同源性。双方在核心架构、编程语言、抽象层次上存在根本性差异。

### 2. 全模块覆盖矩阵解读
- **进程管理**: 仅目标项目存在（81个函数），参考项目无此模块。共同 AST 指纹为 0。证据强度：**未形成**。
- **任务调度/异步运行时**: 仅目标项目存在（311个函数），参考项目无此模块。共同 AST 指纹为 0。证据强度：**未形成**。
- **内存管理**: 仅目标项目存在（351个函数），参考项目无此模块。共同 AST 指纹为 0。证据强度：**未形成**。
- **中断/异常处理**: 仅目标项目存在（30个函数），参考项目无此模块。共同 AST 指纹为 0。证据强度：**未形成**。
- **系统调用**: 仅目标项目存在（184个函数），参考项目无此模块。共同 AST 指纹为 0。证据强度：**未形成**。
- **文件系统**: 仅目标项目存在原创文件系统层（477个函数），参考项目无此模块。共同 AST 指纹为 0。证据强度：**未形成**。
- **设备驱动**: 仅目标项目存在（97个函数），参考项目无此模块。共同 AST 指纹为 0。证据强度：**未形成**。
- **网络协议栈**: 仅目标项目存在（111个函数），参考项目无此模块。共同 AST 指纹为 0。证据强度：**未形成**。
- **第三方库/公共依赖**: 双方均存在，且所有 381 个共同 AST 指纹均集中于此。目标项目覆盖率 63.5%，参考项目覆盖率 95.73%。此模块是双方唯一的重合点，源于共同依赖的开源库 `lwext4`。证据强度：**强（但仅证明共同依赖第三方库，不构成跨项目抄袭）**。
- **通用模块/其他**: 仅目标项目存在（186个函数），参考项目无此模块。共同 AST 指纹为 0。证据强度：**未形成**。

### 3. 底层 AST 关键证据指纹表
| 所属板块 | 目标项目路径 | 参考项目路径 | 证据类型 | 技术说明 |
| :--- | :--- | :--- | :--- | :--- |
| 第三方库/公共依赖 | `crates/lwext4_rust/c/lwext4/src/ext4_balloc.c#ext4_balloc_alloc_block` | `Kernel/File/lwext4/ext4_balloc.c#ext4_balloc_alloc_block` | 同板块同名同结构 | 均为 `lwext4` 库的标准块分配函数，调用链一致源于内部API。 |
| 第三方库/公共依赖 | `crates/lwext4_rust/c/lwext4/src/ext4_journal.c#jbd_journal_prepare` | `Kernel/File/lwext4/ext4_journal.c#jbd_journal_prepare` | 同板块底层逻辑一致 | 日志准备事务的标准流程，使用 `lwext4` 内部的 `TAILQ` 和红黑树宏。 |
| 第三方库/公共依赖 | `crates/lwext4_rust/c/lwext4/src/ext4_extent.c#ext4_extent_remove_space` | `Kernel/File/lwext4/ext4_extent.c#ext4_extent_remove_space` | 同板块底层逻辑一致 | Extent 空间移除的复杂逻辑，调用 `EXT_FIRST_EXTENT` 等 `lwext4` 内部宏。 |
| 第三方库/公共依赖 | `crates/lwext4_rust/c/lwext4/src/ext4_mkfs.c#fill_sb` | `Kernel/File/lwext4/ext4_mkfs.c#fill_sb` | 同板块底层逻辑一致 | 填充 ext4 超级块的标准操作，调用 `to_le16` 等字节序转换宏。 |
| 第三方库/公共依赖 | `crates/lwext4_rust/c/lwext4/src/ext4_dir_idx.c#ext4_dir_dx_add_entry` | `Kernel/File/lwext4/ext4_dir_idx.c#ext4_dir_dx_add_entry` | 同板块同名同结构 | 目录索引项添加，目标项目使用 `ext4_dbg`，参考项目使用 `EXT4_Debug_u32_u32`，体现集成差异。 |

### 4. 核心代码调用流对比剖析
* **【进程管理/任务创建】**
* **底层技术析理**: 目标项目基于 Rust 的 `Arc`、`AtomicWaker` 和 `Poll` 模型构建异步任务系统，通过 `clone_process` 等函数实现进程管理。参考项目无此模块，其内核架构未在证据中体现任何 Rust 异步运行时或类似进程抽象。两者在并发模型和内存管理上存在根本性异构。
* **代码溯源链接**:
  - 目标项目: `os/src/task/task/task.rs#clone_process`
  - 参考项目: 无

* **【内存管理/用户空间访问】**
* **底层技术析理**: 目标项目使用 Rust 特有的 `UserBuffer`、`copy_from_user`/`copy_to_user` 模式进行用户态与内核态的数据安全交换，这依赖于 Rust 的所有权和类型系统。参考项目作为 C 内核，其内存管理基于原始指针和 `memcpy`/`memset`，两者在内存安全抽象上完全不同。
* **代码溯源链接**:
  - 目标项目: `os/src/syscall/task/keys.rs#sys_keyctl`
  - 参考项目: 无

* **【网络协议栈/套接字层】**
* **底层技术析理**: 目标项目实现了完整的 Rust 网络协议栈，使用 `Socket`、`IpEndpoint`、`TcpSocket` 等类型安全的抽象，并通过 `smoltcp` 库进行交互。参考项目无此模块，其网络能力未在证据中体现。两者在网络子系统上无任何可比性。
* **代码溯源链接**:
  - 目标项目: `os/src/net/tcp.rs#recv`
  - 参考项目: 无

* **【第三方库/公共依赖/ext4 调试宏差异】**
* **底层技术析理**: 在 `ext4_dir_dx_add_entry` 等函数中，目标项目使用 `ext4_dbg`，而参考项目使用 `EXT4_Debug_u32_u32`。`ext4_dbg` 是 `lwext4` 上游的标准调试宏，而 `EXT4_Debug_*` 是参考项目的二次封装。此差异表明双方独立集成了上游库，而非目标项目从参考项目“洗稿”。
* **代码溯源链接**:
  - 目标项目: `crates/lwext4_rust/c/lwext4/src/ext4_dir_idx.c#ext4_dir_dx_add_entry`
  - 参考项目: `Kernel/File/lwext4/ext4_dir_idx.c#ext4_dir_dx_add_entry`

### 5. 单方模块与降权项
- **单方模块**: 进程管理、任务调度/异步运行时、内存管理、中断/异常处理、系统调用、文件系统（原创层）、设备驱动、网络协议栈、通用模块/其他。这九大模块仅在目标项目中存在，是目标项目作为完整 Rust 操作系统内核的原创性体现，与参考项目无任何可比性。
- **降权项**: 所有标记为“同板块同名同结构”或“同板块底层逻辑一致”的证据均来自 **【文件系统/第三方库】** 板块。该板块本质是双方共同依赖的开源 C 库 `lwext4`。根据审计指令“跨板块同名或同 hash 只能作为弱信号”，此板块内的重合应降权处理，不能作为指控跨项目抄袭的主证据。其证明力仅限于“双方使用了同一第三方开源组件”。

### 6. 保守结论
本案 17.6% 的 AST 相似度完全由双方共同引入的第三方开源 C 库 `lwext4` 贡献。目标项目原创的 Rust 内核部分（2148 个唯一 AST 指纹）与参考项目（398 个唯一 AST 指纹，几乎全为 `lwext4` 库）在进程、内存、网络、驱动等 9 大核心模块上零重合，展现出从编程语言（Rust vs C）到内核架构（异步运行时 vs 传统 C 内核）的根本性差异。调试宏的差异（`ext4_dbg` vs `EXT4_Debug_*`）进一步支持双方独立集成上游库的结论。因此，现有证据无法支持“抄袭”或“同源”指控，应认定目标项目为独立开发，其对 `lwext4` 的使用属于正常的开源组件依赖。

------------------------------

