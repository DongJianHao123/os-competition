# 🛡️ OS 内核赛自动化代码级架构审计报告

**审计时间**: `2026-07-02 14:14:53`
**参赛作品名称**: `2026-T2026107309910746-XX`
**年份**: `2026`
**学校**: `XX`
**队伍名称**: `T2026107309910746`
**比赛名字**: `OS-Competition`
**比赛子赛道**: `Kernel-Implementation`
**仓库地址**: `https://gitlab.eduxiji.net/educg-group-43501-3132633/T2026107309910746-152`
**推测开发语言**: `Rust / C (由AST探针自动识别)`
**特征函数量**: `1186`

## ⚔️ 底层代码同源性对比 (AST Homology)
> 本部分由专门的代码审查智能体，对该项目与符合年份条件的历届最高相似度项目进行控制流与底层调用对比。新版报告会先给出确定性的全模块覆盖矩阵，再给出 Agent 的分模块解释，避免只围绕少量抽样函数展开。

### 🏆 Top 2 底层对标: vs `2024-undefinedType-南京航空航天大学`（南京航空航天大学 / undefinedType）

#### 对方作品信息

**对方年份**: `2024`
**对方学校**: `南京航空航天大学`
**对方队伍名称**: `undefinedType`
**对方比赛名字**: `OS-Competition`
**对方比赛子赛道**: `Kernel-Implementation`
**对方仓库地址**: `https://gitlab.eduxiji.net/educg-group-26010-2376550/T202410287992755-2176`

> 双向覆盖率：共同 AST 指纹: 368；唯一 AST 指纹: 目标项目 1001，参考项目 398；覆盖率: 目标项目 36.76%，参考项目 92.46%

## 全模块对比总览（确定性证据）

> 总体覆盖率：共同 AST 指纹: 368；唯一 AST 指纹: 目标项目 1001，参考项目 398；覆盖率: 目标项目 36.76%，参考项目 92.46%

### 1. 模块覆盖矩阵
| 模块 | 目标函数数 | 参考函数数 | 共同唯一 AST 指纹 | Target 覆盖 | Base 覆盖 | 状态 |
| :--- | ---: | ---: | ---: | ---: | ---: | :--- |
| 进程管理 | 101 | 0 | 0 | 0.0% | 0.0% | 仅目标项目体现 |
| 任务调度/异步运行时 | 48 | 0 | 0 | 0.0% | 0.0% | 仅目标项目体现 |
| 内存管理 | 183 | 0 | 0 | 0.0% | 0.0% | 仅目标项目体现 |
| 中断/异常处理 | 9 | 0 | 0 | 0.0% | 0.0% | 仅目标项目体现 |
| 系统调用 | 42 | 0 | 0 | 0.0% | 0.0% | 仅目标项目体现 |
| 文件系统 | 111 | 0 | 0 | 0.0% | 0.0% | 仅目标项目体现 |
| 设备驱动 | 19 | 0 | 0 | 0.0% | 0.0% | 仅目标项目体现 |
| 网络协议栈 | 5 | 0 | 0 | 0.0% | 0.0% | 仅目标项目体现 |
| 文件系统/第三方库 | 532 | 478 | 368 | 82.51% | 92.46% | 双方存在且有结构重合 |
| 通用模块/其他 | 136 | 0 | 0 | 0.0% | 0.0% | 仅目标项目体现 |

### 2. 分模块证据清单

#### 进程管理
仅目标项目中观察到该模块代表函数，参考项目没有同模块可对齐证据：
- 目标项目：`kernel/proc.c#scheduler`，调用摘要：acquire, freeproc, intr_off, intr_on, mycpu, release ...(+1)
- 目标项目：`kernel/exec.c#exec_open_interp`，调用摘要：exec_open_vfs, myproc, snprintf, strncmp
- 目标项目：`kernel/sysproc.c#sys_linux_clone`，调用摘要：argaddr, kclone
- 目标项目：`kernel/proc.c#kexit`，调用摘要：acquire, linux_clear_child_tid, linux_group_dec_live, linux_group_exiting, linux_group_leader, linux_group_live_count ...(+11)

#### 任务调度/异步运行时
仅目标项目中观察到该模块代表函数，参考项目没有同模块可对齐证据：
- 目标项目：`kernel/sysfile.c#sys_linux_newfstatat`，调用摘要：argaddr, argint, argstr, linux_copy_stat_time, linux_stat_proc_path, myproc ...(+1)
- 目标项目：`user/sh.c#runcmd`，调用摘要：close, dup, exec, exit, fork1, fprintf ...(+6)
- 目标项目：`user/ls.c#ls`，调用摘要：__sys_getdents64, close, fmtname, fprintf, fstat, memmove ...(+5)
- 目标项目：`user/init.c#main`，调用摘要：dup, exec, exit, fork, halt, mkdir ...(+5)

#### 内存管理
仅目标项目中观察到该模块代表函数，参考项目没有同模块可对齐证据：
- 目标项目：`kernel/exec.c#kexec`，调用摘要：PGROUNDUP, acquire, build_auxv, copyout, exec_linux_visible_path, exec_nonelf ...(+20)
- 目标项目：`kernel/sysproc.c#sys_linux_futex`，调用摘要：acquire, argaddr, argint, copyin, futex_init, futex_set_bitset ...(+13)
- 目标项目：`kernel/proc.c#forkat`，调用摘要：(uint64), acquire, allocproc, copy_mapped_vma_pages, copyout, filedup ...(+18)
- 目标项目：`kernel/vm.c#vmfault`，调用摘要：PA2PTE, PGROUNDDOWN, PTE2PA, PTE_FLAGS, PTE_LEAF, ismapped ...(+14)

#### 中断/异常处理
仅目标项目中观察到该模块代表函数，参考项目没有同模块可对齐证据：
- 目标项目：`kernel/virtio_disk.c#virtio_disk_intr`，调用摘要：__sync_synchronize, acquire, panic, release, wakeup
- 目标项目：`kernel/plic.c#plicinithart`，调用摘要：PLIC_SENABLE, PLIC_SPRIORITY, cpuid
- 目标项目：`kernel/trap.c#clockintr`，调用摘要：cpuid, futex_tick, r_time, w_stimecmp, wakeup, write_acquire ...(+1)
- 目标项目：`kernel/plic.c#plicinit`，调用摘要：内部闭环逻辑

#### 系统调用
仅目标项目中观察到该模块代表函数，参考项目没有同模块可对齐证据：
- 目标项目：`user/sh.c#match_dir`，调用摘要：__sys_getdents64, close, hasprefix, open, strcpy, strlen ...(+1)
- 目标项目：`kernel/net.c#sys_bind`，调用摘要：acquire, argint, find_binding, memset, release
- 目标项目：`kernel/sbi.c#sbi_call`，调用摘要：内部闭环逻辑
- 目标项目：`kernel/syscall.c#syscall_lookup`，调用摘要：NELEM

#### 文件系统
仅目标项目中观察到该模块代表函数，参考项目没有同模块可对齐证据：
- 目标项目：`user/grind.c#go`，调用摘要：chdir, close, dup, exec, exit, fork ...(+16)
- 目标项目：`kernel/vfs.c#vfs_mount`，调用摘要：acquire, memset, mnt->ops->mount, release, safestrcpy, vfs_normalize_target ...(+4)
- 目标项目：`kernel/vfs.c#vfs_resolve`，调用摘要：acquire, release, safestrcpy, strlen, vfs_clean_path, vfs_path_matches_mount ...(+1)
- 目标项目：`kernel/vfs.c#xv6_vfs_rename`，调用摘要：begin_op, dirlookup, end_op, ilock, iput, iunlockput ...(+7)

#### 设备驱动
仅目标项目中观察到该模块代表函数，参考项目没有同模块可对齐证据：
- 目标项目：`kernel/virtio_net.c#virtio_net_init`，调用摘要：initlock, netq_init, panic, refill_rx
- 目标项目：`kernel/uart.c#uartinit`，调用摘要：WriteReg, initlock
- 目标项目：`kernel/kcsan.c#kcsan_write`，调用摘要：acquire, delay, race, release, wp_install, wp_lookup ...(+1)
- 目标项目：`kernel/virtio_net.c#reclaim_tx`，调用摘要：__sync_synchronize, free_desc, kfree

#### 网络协议栈
仅目标项目中观察到该模块代表函数，参考项目没有同模块可对齐证据：
- 目标项目：`kernel/net.c#ip_rx`，调用摘要：acquire, find_binding, kfree, memmove, ntohl, ntohs ...(+3)
- 目标项目：`kernel/net.c#in_cksum`，调用摘要：内部闭环逻辑
- 目标项目：`kernel/net.c#net_rx`，调用摘要：arp_rx, ip_rx, kfree, ntohs
- 目标项目：`kernel/net.c#find_binding`，调用摘要：内部闭环逻辑

#### 文件系统/第三方库
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `kernel/lwext4/src/ext4_balloc.c#ext4_balloc_alloc_block` | `Kernel/File/lwext4/ext4_balloc.c#ext4_balloc_alloc_block` | 同板块同名同结构 | ext4_balloc_get_bgid_of_block, ext4_balloc_get_block_of_bgid, ext4_balloc_set_bitmap_csum, ext4_balloc_verify_bitmap_csum, ext4_bg_get_block_bitmap, ext4_bg_get_free_blocks_count ...(+19) | EXT4_Debug_u32, ext4_balloc_get_bgid_of_block, ext4_balloc_get_block_of_bgid, ext4_balloc_set_bitmap_csum, ext4_balloc_verify_bitmap_csum, ext4_bg_get_block_bitmap ...(+19) |
| `kernel/lwext4/src/ext4.c#ext4_dir_rm` | `Kernel/File/lwext4/ext4.c#ext4_dir_rm` | 同板块同名 | EXT4_MP_LOCK, EXT4_MP_UNLOCK, ext4_block_cache_write_back, ext4_dir_en_get_inode, ext4_dir_iterator_fini, ext4_dir_iterator_init ...(+19) | EXT4_MP_LOCK, EXT4_MP_UNLOCK, ext4_block_cache_write_back, ext4_dir_en_get_inode, ext4_dir_iterator_fini, ext4_dir_iterator_init ...(+19) |
| `kernel/lwext4/src/ext4_journal.c#jbd_journal_prepare` | `Kernel/File/lwext4/ext4_journal.c#jbd_journal_prepare` | 同板块同名同结构 | EXT4_BLOCK_ZERO, RB_FIND, TAILQ_FOREACH_REVERSE_SAFE, TAILQ_FOREACH_SAFE, TAILQ_REMOVE, ext4_bcache_clear_dirty ...(+17) | EXT4_BLOCK_ZERO, RB_FIND, TAILQ_FOREACH_REVERSE_SAFE, TAILQ_FOREACH_SAFE, TAILQ_REMOVE, ext4_bcache_clear_dirty ...(+17) |
| `kernel/lwext4/src/ext4_dir_idx.c#ext4_dir_dx_split_index` | `Kernel/File/lwext4/ext4_dir_idx.c#ext4_dir_dx_split_index` | 同板块同名同结构 | ext4_block_set, ext4_dir_dx_climit_get_count, ext4_dir_dx_climit_get_limit, ext4_dir_dx_climit_set_count, ext4_dir_dx_climit_set_limit, ext4_dir_dx_entry_get_hash ...(+10) | ext4_block_set, ext4_dir_dx_climit_get_count, ext4_dir_dx_climit_get_limit, ext4_dir_dx_climit_set_count, ext4_dir_dx_climit_set_limit, ext4_dir_dx_entry_get_hash ...(+10) |

#### 通用模块/其他
仅目标项目中观察到该模块代表函数，参考项目没有同模块可对齐证据：
- 目标项目：`user/sh.c#getcmd`，调用摘要：complete, memset, read, redraw, redraw_at, remember ...(+3)
- 目标项目：`user/printf.c#vprintf`，调用摘要：printint, printptr, putc, va_arg
- 目标项目：`kernel/printf.c#printf`，调用摘要：acquire, consputc, printint, printptr, release, va_arg ...(+2)
- 目标项目：`user/sh.c#complete`，调用摘要：match_dir, memmove, redraw, strcpy, strlen, write


#### 1. 宏观判定
- **定性结论**: **独立开发 / 底层架构显著差异**
- **核心依据**: 客观 AST 相似度仅为 35.69%。所有 368 个共同 AST 指纹均集中于“文件系统/第三方库”板块，该板块被识别为双方共同集成的开源 `lwext4` 库。在构成操作系统内核核心的进程管理、内存管理、任务调度、中断异常、系统调用、设备驱动、网络协议栈及自研文件系统（VFS）等关键模块中，**参考项目未提供任何可对齐的同板块证据**。目标项目在这些核心模块中拥有 1001 个唯一 AST 指纹，展现出完整的独立内核架构。

#### 2. 全模块覆盖矩阵解读
- **进程管理**: 仅目标项目存在（101个函数）。参考项目无此模块。共同指纹为0，目标覆盖率0%。证据强度：**未形成**。目标项目独立实现了完整的进程模型。
- **任务调度/异步运行时**: 仅目标项目存在（48个函数）。参考项目无此模块。共同指纹为0，目标覆盖率0%。证据强度：**未形成**。目标项目拥有独立的调度和异步机制。
- **内存管理**: 仅目标项目存在（183个函数）。参考项目无此模块。共同指纹为0，目标覆盖率0%。证据强度：**未形成**。目标项目实现了包含页错误处理、VMA等在内的完整内存管理系统。
- **中断/异常处理**: 仅目标项目存在（9个函数）。参考项目无此模块。共同指纹为0，目标覆盖率0%。证据强度：**未形成**。目标项目包含独立的中断控制器和陷阱处理逻辑。
- **系统调用**: 仅目标项目存在（42个函数）。参考项目无此模块。共同指纹为0，目标覆盖率0%。证据强度：**未形成**。目标项目实现了独立的Linux兼容系统调用层。
- **文件系统**: 仅目标项目存在（111个函数）。参考项目无此模块。共同指纹为0，目标覆盖率0%。证据强度：**未形成**。目标项目拥有自研的VFS层，与集成的`lwext4`库交互。
- **设备驱动**: 仅目标项目存在（19个函数）。参考项目无此模块。共同指纹为0，目标覆盖率0%。证据强度：**未形成**。目标项目包含virtio-net、virtio-disk、UART等独立驱动。
- **网络协议栈**: 仅目标项目存在（5个函数）。参考项目无此模块。共同指纹为0，目标覆盖率0%。证据强度：**未形成**。目标项目实现了包含IP、ARP等协议的网络栈。
- **第三方库/公共依赖**: 双方均存在此模块。目标项目532个函数，参考项目478个函数。共同AST指纹368个，目标覆盖率82.51%，参考覆盖率92.46%。证据强度：**强**，但性质明确为**对同一开源项目 `lwext4` 的分别集成**。参考项目几乎完全由该库构成，而目标项目将其作为众多子系统之一。

#### 3. 底层 AST 关键证据指纹表
| 所属板块 | 目标项目路径 | 参考项目路径 | 证据类型 | 技术说明 |
| :--- | :--- | :--- | :--- | :--- |
| 第三方库/公共依赖 | `kernel/lwext4/src/ext4_balloc.c#ext4_balloc_alloc_block` | `Kernel/File/lwext4/ext4_balloc.c#ext4_balloc_alloc_block` | 同板块同名同结构 | 均为 `lwext4` 库的标准块分配函数，调用链由ext4规范决定。目标项目使用 `ext4_dbg` 日志，参考项目使用 `EXT4_Debug_u32`，表明适配层不同。 |
| 第三方库/公共依赖 | `kernel/lwext4/src/ext4_journal.c#jbd_journal_prepare` | `Kernel/File/lwext4/ext4_journal.c#jbd_journal_prepare` | 同板块底层逻辑一致 | 实现JBD2日志协议的标准prepare阶段。`RB_FIND`、`TAILQ_FOREACH_*` 等是协议规定的数据结构操作，非私有逻辑。 |
| 第三方库/公共依赖 | `kernel/lwext4/src/ext4_extent.c#ext4_ext_split_node` | `Kernel/File/lwext4/ext4_extent.c#ext4_ext_split_node` | 同板块底层逻辑一致 | 实现ext4 extent树节点分裂算法。`EXT_FIRST_EXTENT`、`EXT_MAX_INDEX` 等宏及辅助函数是ext4规范的直接体现。 |
| 第三方库/公共依赖 | `kernel/lwext4/src/ext4_xattr.c#ext4_xattr_set_entry` | `Kernel/File/lwext4/ext4_xattr.c#ext4_xattr_set_entry` | 同板块底层逻辑一致 | 实现ext4扩展属性条目设置。`EXT4_XATTR_IS_LAST_ENTRY`、`memmove` 等是处理变长属性布局的必然操作组合。 |
| 第三方库/公共依赖 | `kernel/lwext4/src/ext4_mkfs.c#fill_sb` | `Kernel/File/lwext4/ext4_mkfs.c#fill_sb` | 同板块底层逻辑一致 | 实现 `mkfs.ext4` 的superblock填充。`to_le16`、`to_le32`、`log_2` 等是公开的格式化标准流程。 |
| 第三方库/公共依赖 | `kernel/lwext4/src/ext4_dir_idx.c#ext4_dir_dx_add_entry` | `Kernel/File/lwext4/ext4_dir_idx.c#ext4_dir_dx_add_entry` | 同板块同名同结构 | 实现ext4目录索引的条目添加。目标项目使用 `ext4_dbg`，参考项目使用 `EXT4_Debug_u32_u32`，核心调用链由HTree算法决定。 |

#### 4. 核心代码调用流对比剖析
* **【文件系统/第三方库 - lwext4 块分配路径】**
* **底层技术析理**: 双方在 `ext4_balloc_alloc_block` 函数中均调用了 `ext4_balloc_get_bgid_of_block`、`ext4_balloc_verify_bitmap_csum` 等一组长达17个以上的辅助函数。此调用链是 `lwext4` 库为实现 ext4 块分配协议而设计的内部 API。调用图谱的高度一致，源于双方均需通过该库的内部接口来完成块位图的查找、校验与更新，这是使用同一第三方库的必然结果，而非跨项目抄袭。关键差异在于调试日志的封装：目标项目使用统一的 `ext4_dbg`，而参考项目使用类型化的 `EXT4_Debug_u32` 等宏，揭示了两个项目对 `lwext4` 进行了不同的适配层封装。
* **代码溯源链接**:
  - 目标项目: `kernel/lwext4/src/ext4_balloc.c#ext4_balloc_alloc_block`
  - 参考项目: `Kernel/File/lwext4/ext4_balloc.c#ext4_balloc_alloc_block`

* **【文件系统/第三方库 - lwext4 日志提交路径】**
* **底层技术析理**: 双方在 `__jbd_journal_commit_trans` 函数中均使用了 `RB_EMPTY`、`RB_FOREACH_SAFE`、`TAILQ_INSERT_TAIL` 等宏和 `ext4_bcache_find_get` 等函数。这是实现 JBD2 日志提交阶段的标准流程，涉及对红黑树和尾队列等特定数据结构的操作，这些操作由 JBD2 协议和 `lwext4` 库的内部实现严格定义。调用流的一致性是协议合规性的体现，而非代码克隆。
* **代码溯源链接**:
  - 目标项目: `kernel/lwext4/src/ext4_journal.c#__jbd_journal_commit_trans`
  - 参考项目: `Kernel/File/lwext4/ext4_journal.c#__jbd_journal_commit_trans`

#### 5. 单方模块与降权项
- **单方原创模块（强独立开发证据）**:
  - **进程管理、内存管理、任务调度、中断/异常处理、系统调用、文件系统(VFS)、设备驱动、网络协议栈**：这些构成操作系统内核核心的模块**仅在目标项目中存在**，拥有大量唯一AST指纹。参考项目完全没有这些模块的任何证据。这从根本上排除了目标项目“系统性移植”参考项目的可能性。
- **第三方库/公共依赖（降权处理）**:
  - **`lwext4` 库所有相似函数**：所有被控方指控的“铁证”均属于此范畴。`lwext4` 是一个公开的、独立的开源项目。双方项目中该库的函数同名、调用图谱相似，是共同遵守 ext4 文件系统规范及使用同一开源库的必然结果。调试宏（`ext4_dbg` vs `EXT4_Debug_*`）和目录组织（`kernel/lwext4/src/` vs `Kernel/File/lwext4/`）的差异，进一步证明双方是分别集成，而非一方抄袭另一方。此类证据不能作为项目间抄袭的强同源证据。

#### 6. 保守结论
本次审计的客观 AST 相似度为 35.69%，该数值完全由双方共同集成的开源第三方库 `lwext4` 贡献。所有共同指纹均源于对同一 ext4 协议规范的实现，且调试封装等细节存在差异，符合分别集成的特征。在操作系统内核的核心领域（进程、内存、调度、驱动、网络、系统调用等），目标项目展现了完整且独立的架构，而参考项目未提供任何可对齐的证据。因此，整体风险极低，无证据表明目标项目对参考项目存在抄袭或系统性移植行为。建议将 `lwext4` 库的相似性从侵权判定中排除，认定目标项目为独立开发。

------------------------------

