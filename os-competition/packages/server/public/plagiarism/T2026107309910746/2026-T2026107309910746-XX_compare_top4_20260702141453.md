# 🛡️ OS 内核赛自动化代码级架构审计报告

**审计时间**: `2026-07-02 14:14:53`
**参赛作品名称**: `2026-T2026107309910746-XX`
**年份**: `2026`
**学校**: `XX`
**队伍名称**: `T2026107309910746`
**比赛名字**: `OS-Competition`
**比赛子赛道**: `Kernel-Implementation`
**仓库地址**: `https://gitlab.eduxiji.net/educg-group-43501-3132633/T2026107309910746-152`
**推测开发语言**: `Rust / C (由AST探针自动识别)`
**特征函数量**: `1186`

## ⚔️ 底层代码同源性对比 (AST Homology)
> 本部分由专门的代码审查智能体，对该项目与符合年份条件的历届最高相似度项目进行控制流与底层调用对比。新版报告会先给出确定性的全模块覆盖矩阵，再给出 Agent 的分模块解释，避免只围绕少量抽样函数展开。

### 🏆 Top 4 底层对标: vs `2024-赤道会师-合肥工业大学`（合肥工业大学 / 赤道会师）

#### 对方作品信息

**对方年份**: `2024`
**对方学校**: `合肥工业大学`
**对方队伍名称**: `赤道会师`
**对方比赛名字**: `OS-Competition`
**对方比赛子赛道**: `Kernel-Implementation`
**对方仓库地址**: `https://gitlab.eduxiji.net/educg-group-26010-2376550/T202410359992686-2094`

> 双向覆盖率：共同 AST 指纹: 458；唯一 AST 指纹: 目标项目 1001，参考项目 832；覆盖率: 目标项目 45.75%，参考项目 55.05%

## 全模块对比总览（确定性证据）

> 总体覆盖率：共同 AST 指纹: 458；唯一 AST 指纹: 目标项目 1001，参考项目 832；覆盖率: 目标项目 45.75%，参考项目 55.05%

### 1. 模块覆盖矩阵
| 模块 | 目标函数数 | 参考函数数 | 共同唯一 AST 指纹 | Target 覆盖 | Base 覆盖 | 状态 |
| :--- | ---: | ---: | ---: | ---: | ---: | :--- |
| 进程管理 | 101 | 41 | 13 | 14.94% | 34.21% | 双方存在且有结构重合 |
| 任务调度/异步运行时 | 48 | 57 | 7 | 14.89% | 12.73% | 双方存在且有结构重合 |
| 内存管理 | 183 | 112 | 17 | 9.44% | 15.74% | 双方存在且有结构重合 |
| 中断/异常处理 | 9 | 18 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |
| 系统调用 | 42 | 10 | 1 | 8.33% | 11.11% | 双方存在且有结构重合 |
| 文件系统 | 111 | 106 | 11 | 10.58% | 12.5% | 双方存在且有结构重合 |
| 设备驱动 | 19 | 29 | 2 | 10.53% | 8.7% | 双方存在且有结构重合 |
| 网络协议栈 | 5 | 0 | 0 | 0.0% | 0.0% | 仅目标项目体现 |
| 文件系统/第三方库 | 532 | 485 | 373 | 83.63% | 93.25% | 双方存在且有结构重合 |
| 通用模块/其他 | 136 | 130 | 37 | 32.46% | 32.17% | 双方存在且有结构重合 |

### 2. 分模块证据清单

#### 进程管理
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `user/sh.c#parseexec` | `user/sh.c#parseexec` | 同板块同名同结构 | execcmd, gettoken, panic, parseblock, parseredirs, peek | execcmd, gettoken, panic, parseblock, parseredirs, peek |
| `kernel/proc.c#sleep` | `kernel/proc.c#sleep` | 同板块同名 | acquire, myproc, release, sched | acquire, myproc, release, sched |
| `kernel/proc.c#procdump` | `kernel/proc.c#procdump` | 同板块同名 | NELEM, printf | NELEM, printf |
| `kernel/proc.c#sched` | `kernel/proc.c#sched` | 同板块同名 | holding, intr_get, mycpu, myproc, panic, swtch | holding, intr_get, mycpu, myproc, panic, r_time ...(+1) |

#### 任务调度/异步运行时
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `user/sh.c#runcmd` | `user/sh.c#runcmd` | 同板块同名 | close, dup, exec, exit, fork1, fprintf ...(+6) | close, dup, exec, exit, fork1, fprintf ...(+5) |
| `kernel/console.c#consoleread` | `kernel/console.c#consoleread` | 同板块同名 | C, acquire, either_copyout, killed, myproc, release ...(+1) | C, acquire, either_copyout, myproc, release, sleep |
| `user/sh.c#nulterminate` | `user/sh.c#nulterminate` | 同板块同名同结构 | nulterminate | nulterminate |
| `kernel/trap.c#kerneltrap` | `kernel/trap.c#kerneltrap` | 同板块同名 | devintr, intr_get, myproc, panic, printf, r_scause ...(+6) | devintr, intr_get, myproc, panic, printf, r_scause ...(+7) |

#### 内存管理
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `kernel/virtio_disk.c#virtio_disk_init` | `kernel/virtio_disk.c#virtio_disk_init` | 同板块同名 | initlock, kalloc, memset, panic, superalloc | R, initlock, memset, panic, printf |
| `kernel/proc.c#allocproc` | `kernel/proc.c#allocproc` | 同板块同名 | TRAPFRAME_SLOT, acquire, allocpid, freeproc, kalloc, linux_group_add_member ...(+7) | acquire, allocpid, freeproc, kalloc, memset, proc_kpagetable ...(+2) |
| `kernel/virtio_disk.c#virtio_disk_rw` | `kernel/virtio_disk.c#virtio_disk_rw` | 同板块同名 | __sync_synchronize, acquire, alloc3_desc, checkbuf, free_chain, panic ...(+2) | R, __sync_synchronize, acquire, alloc3_desc, free_chain, kwalkaddr ...(+3) |
| `kernel/trap.c#usertrap` | `kernel/trap.c#usertrap` | 同板块同名 | MAKE_SATP, devintr, intr_on, kexit, killed, linux_deliver_signal ...(+13) | PGROUNDDOWN, devintr, exit, intr_on, myproc, panic ...(+15) |

#### 中断/异常处理
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `kernel/virtio_disk.c#virtio_disk_intr` | `kernel/virtio_disk.c#virtio_disk_intr` | 同板块同名 | __sync_synchronize, acquire, panic, release, wakeup | R, acquire, panic, release, wakeup |
| `kernel/plic.c#plicinithart` | `kernel/plic.c#plicinithart` | 同板块同名 | PLIC_SENABLE, PLIC_SPRIORITY, cpuid | PLIC_MENABLE, PLIC_SENABLE, PLIC_SPRIORITY, cpuid, printf, readd |
| `kernel/plic.c#plicinit` | `kernel/plic.c#plicinit` | 同板块同名 | 内部闭环逻辑 | printf, writed |
| `kernel/spinlock.c#pop_off` | `kernel/intr.c#pop_off` | 同板块同名 | intr_get, intr_on, mycpu, panic | intr_get, intr_on, mycpu, panic |

#### 系统调用
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `kernel/syscall.c#argstr` | `kernel/syscall.c#argstr` | 同板块同名 | argaddr, fetchstr | argaddr, fetchstr |
| `kernel/syscall.c#argaddr` | `kernel/syscall.c#argaddr` | 同板块同名 | argraw | argraw |
| `kernel/syscall.c#argint` | `kernel/syscall.c#argint` | 同板块同名 | argraw | argraw |
| `kernel/syscall.c#sys_linux_success` | `kernel/sysmem.c#sys_madvice` | 同板块同结构 | 内部闭环逻辑 | 内部闭环逻辑 |

#### 文件系统
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `user/grind.c#go` | `user/grind.c#go` | 同板块同名 | chdir, close, dup, exec, exit, fork ...(+16) | chdir, close, dup, exec, exit, fork ...(+16) |
| `user/find.c#find` | `user/find.c#find` | 同板块同名 | base_name, close, find, fprintf, fstat, memmove ...(+7) | close, find, fprintf, fstat, open, readdir ...(+3) |
| `kernel/fs.c#dirlookup` | `kernel/fat32.c#dirlookup` | 同板块同名 | iget, namecmp, panic, readi | edup, eget, enext, eput, panic, reloc_clus ...(+2) |
| `kernel/file.c#filewrite` | `kernel/file.c#filewrite` | 同板块同名 | devsw[f->major].write, f->vfs_ops->file->write, panic, pipewrite | devsw[f->major].write, fwrite, panic, pipewrite |

#### 设备驱动
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `kernel/uart.c#uartinit` | `kernel/uart.c#uartinit` | 同板块同名 | WriteReg, initlock | WriteReg, initlock |
| `user/sh.c#parseblock` | `user/sh.c#parseblock` | 同板块同名同结构 | gettoken, panic, parseline, parseredirs, peek | gettoken, panic, parseline, parseredirs, peek |
| `kernel/uart.c#uartputc_sync` | `kernel/uart.c#uartputc_sync` | 同板块同名 | ReadReg, WriteReg, pop_off, push_off | ReadReg, WriteReg, pop_off, push_off |
| `kernel/virtio_disk.c#free_chain` | `kernel/virtio_disk.c#free_chain` | 同板块同名 | free_desc | free_desc |

#### 网络协议栈
仅目标项目中观察到该模块代表函数，参考项目没有同模块可对齐证据：
- 目标项目：`kernel/net.c#ip_rx`，调用摘要：acquire, find_binding, kfree, memmove, ntohl, ntohs ...(+3)
- 目标项目：`kernel/net.c#in_cksum`，调用摘要：内部闭环逻辑
- 目标项目：`kernel/net.c#net_rx`，调用摘要：arp_rx, ip_rx, kfree, ntohs
- 目标项目：`kernel/net.c#find_binding`，调用摘要：内部闭环逻辑

#### 文件系统/第三方库
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `kernel/lwext4/src/ext4_balloc.c#ext4_balloc_alloc_block` | `kernel/lwext4/ext4_balloc.c#ext4_balloc_alloc_block` | 同板块同名同结构 | ext4_balloc_get_bgid_of_block, ext4_balloc_get_block_of_bgid, ext4_balloc_set_bitmap_csum, ext4_balloc_verify_bitmap_csum, ext4_bg_get_block_bitmap, ext4_bg_get_free_blocks_count ...(+19) | ext4_balloc_get_bgid_of_block, ext4_balloc_get_block_of_bgid, ext4_balloc_set_bitmap_csum, ext4_balloc_verify_bitmap_csum, ext4_bg_get_block_bitmap, ext4_bg_get_free_blocks_count ...(+19) |
| `kernel/lwext4/src/ext4.c#ext4_dir_rm` | `kernel/lwext4/ext4.c#ext4_dir_rm` | 同板块同名 | EXT4_MP_LOCK, EXT4_MP_UNLOCK, ext4_block_cache_write_back, ext4_dir_en_get_inode, ext4_dir_iterator_fini, ext4_dir_iterator_init ...(+19) | EXT4_MP_LOCK, EXT4_MP_UNLOCK, ext4_block_cache_write_back, ext4_dir_en_get_inode, ext4_dir_iterator_fini, ext4_dir_iterator_init ...(+19) |
| `kernel/lwext4/src/ext4_journal.c#jbd_journal_prepare` | `kernel/lwext4/ext4_journal.c#jbd_journal_prepare` | 同板块同名同结构 | EXT4_BLOCK_ZERO, RB_FIND, TAILQ_FOREACH_REVERSE_SAFE, TAILQ_FOREACH_SAFE, TAILQ_REMOVE, ext4_bcache_clear_dirty ...(+17) | EXT4_BLOCK_ZERO, RB_FIND, TAILQ_FOREACH_REVERSE_SAFE, TAILQ_FOREACH_SAFE, TAILQ_REMOVE, ext4_bcache_clear_dirty ...(+17) |
| `kernel/lwext4/src/ext4_dir_idx.c#ext4_dir_dx_split_index` | `kernel/lwext4/ext4_dir_idx.c#ext4_dir_dx_split_index` | 同板块同名同结构 | ext4_block_set, ext4_dir_dx_climit_get_count, ext4_dir_dx_climit_get_limit, ext4_dir_dx_climit_set_count, ext4_dir_dx_climit_set_limit, ext4_dir_dx_entry_get_hash ...(+10) | ext4_block_set, ext4_dir_dx_climit_get_count, ext4_dir_dx_climit_get_limit, ext4_dir_dx_climit_set_count, ext4_dir_dx_climit_set_limit, ext4_dir_dx_entry_get_hash ...(+10) |

#### 通用模块/其他
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `user/sh.c#getcmd` | `user/sh.c#getcmd` | 同板块同名 | complete, memset, read, redraw, redraw_at, remember ...(+3) | fprintf, gets, memset |
| `user/printf.c#vprintf` | `user/printf.c#vprintf` | 同板块同名 | printint, printptr, putc, va_arg | printint, printptr, putc, va_arg |
| `kernel/printf.c#printf` | `user/printf.c#printf` | 同板块同名 | acquire, consputc, printint, printptr, release, va_arg ...(+2) | fflush, va_start, vprintf |
| `kernel/spinlock.c#release` | `kernel/spinlock.c#release` | 同板块同名同结构 | __sync_lock_release, __sync_synchronize, holding, panic, pop_off | __sync_lock_release, __sync_synchronize, holding, panic, pop_off |


好的，审判长。本庭已审查控辩双方提交的全部证据、指控与辩护意见，并依据客观 AST 指纹数据及底层调用图谱，现作出最终同源性定性仲裁。

### 1. 宏观判定
- **定性结论**: **独立开发 (相似度: 33.31%)**
  该相似度处于较低水平。共同 AST 指纹（458）远低于双方各自唯一的指纹数（目标 1001，参考 832）。高覆盖率主要由共用的第三方开源库 `lwext4` 贡献，剔除后，核心系统模块的相似度极低。目标项目展现出显著的独立架构演进，特别是 Linux 兼容层与网络协议栈的实现。

### 2. 全模块覆盖矩阵解读
- **进程管理**: 双方均存在。共同 AST 指纹仅 13 个，目标覆盖率 14.94%，参考覆盖率 34.21%。覆盖率低，表明目标项目在此模块有大量独立实现。证据强度：**弱**。
- **任务调度/异步运行时**: 双方均存在。共同 AST 指纹 7 个，双方覆盖率均低于 15%。重合点多为 xv6 标准调度原语。证据强度：**弱**。
- **内存管理**: 双方均存在。共同 AST 指纹 17 个，双方覆盖率均低于 16%。重合点集中在标准内存分配与页表操作。证据强度：**弱**。
- **中断/异常处理**: 双方均存在。共同 AST 指纹为 0。尽管存在同名函数，但 AST 调用图谱无重合，表明实现路径完全不同。证据强度：**未形成**。
- **系统调用**: 双方均存在。共同 AST 指纹仅 1 个，覆盖率极低。目标项目实现了大量 Linux 兼容系统调用，与参考项目架构迥异。证据强度：**未形成**。
- **文件系统**: 双方均存在。共同 AST 指纹 11 个，覆盖率约 10%。重合点主要在 xv6 标准文件操作接口，核心文件系统实现（ext4/fat32）不同。证据强度：**弱**。
- **设备驱动**: 双方均存在。共同 AST 指纹 2 个，覆盖率约 10%。重合点为 virtio 和 uart 的标准初始化序列，属硬件约束下的标准写法。证据强度：**弱**。
- **网络协议栈**: **仅目标项目体现**。参考项目无此模块。这是目标项目独立开发的强有力证据。证据强度：**未形成（单方模块）**。
- **第三方库/公共依赖**: 双方均存在。共同 AST 指纹 373 个，覆盖率高达 83.63%/93.25%。这是双方共用的 `lwext4` 库，属合法开源复用，不构成项目间抄袭证据。证据强度：**强（但属公共依赖，不指向抄袭）**。

### 3. 底层 AST 关键证据指纹表
| 所属板块 | 目标项目路径 | 参考项目路径 | 证据类型 | 技术说明 |
| :--- | :--- | :--- | :--- | :--- |
| 进程管理 | `user/sh.c#parseexec` | `user/sh.c#parseexec` | 同板块同名同结构 | xv6 Shell 标准递归下降解析器模板，调用图谱由语法规则决定，属公共领域实现。 |
| 进程管理 | `kernel/proc.c#yield` | `kernel/proc.c#yield` | 同板块同名同结构 | 标准进程调度原语，调用序列（acquire->myproc->release->sched）为 OS 理论唯一确定。 |
| 进程管理 | `kernel/proc.c#myproc` | `kernel/proc.c#myproc` | 同板块同名同结构 | 获取当前进程的标准方法，`push_off/pop_off` 中断屏蔽是 RISC-V 多核下的必然操作。 |
| 进程管理 | `kernel/proc.c#sched` | `kernel/proc.c#sched` | 同板块同名 | 上下文切换核心，调用流高度一致但目标项目删减了 `r_time`，属功能取舍差异。 |
| 进程管理 | `kernel/sysproc.c#sys_uptime` | `kernel/sysproc.c#sys_uptime` | 同板块同名同结构 | 系统调用骨架相同，但目标项目使用 `read_acquire/release`，参考项目使用 `acquire/release`，同步策略不同。 |
| 进程管理 | `kernel/sysproc.c#sys_exit` | `kernel/sysproc.c#sys_exit` | 同板块同名 | 系统调用入口结构一致（argint->核心函数），但目标项目调用 `kexit`，参考项目调用 `exit`，底层实现已分化。 |
| 进程管理 | `user/zombie.c#main` | `user/zombie.c#main` | 同板块同名同结构 | 测试用例结构相似，但目标项目使用 `pause`（测试信号），参考项目使用 `sleep`，测试目的不同。 |
| 任务调度 | `kernel/trap.c#kerneltrap` | `kernel/trap.c#kerneltrap` | 同板块同名 | 内核陷阱处理标准流程，调用流高度相似，属 RISC-V 特权架构下的标准实现。 |
| 内存管理 | `kernel/proc.c#allocproc` | `kernel/proc.c#allocproc` | 同板块同名 | 进程结构体初始化流程，包含标准内存分配（kalloc）和页表创建，目标项目增加了 Linux 线程组初始化。 |
| 文件系统 | `kernel/file.c#filewrite` | `kernel/file.c#filewrite` | 同板块同名 | 标准 VFS 写操作分发逻辑，根据文件类型调用不同底层函数，属经典 OS 设计模式。 |
| 设备驱动 | `kernel/virtio_disk.c#virtio_disk_rw` | `kernel/virtio_disk.c#virtio_disk_rw` | 同板块同名 | VirtIO 块设备驱动的标准读写流程，包含描述符链管理和内存屏障，由硬件协议决定。 |
| 第三方库 | `kernel/lwext4/src/ext4_balloc.c#ext4_balloc_alloc_block` | `kernel/lwext4/ext4_balloc.c#ext4_balloc_alloc_block` | 同板块同名同结构 | 双方共用的 `lwext4` 开源库函数，AST 指纹完全一致，属合法第三方代码复用。 |

### 4. 核心代码调用流对比剖析
* **【进程管理/进程退出路径】**
* **底层技术析理**: 目标项目的 `sys_exit` -> `kexit` 调用链与参考项目的 `sys_exit` -> `exit` 调用链在系统调用入口层结构相似，均为获取参数后调用内核退出函数。然而，进入内核层后，两者在**对象管理模型**上彻底分化。目标项目的 `kexit` 调用图谱包含 `linux_clear_child_tid`, `linux_group_dec_live`, `linux_group_exiting` 等一系列 Linux 线程组管理函数，展现了复杂的线程组清理与信号处理逻辑。参考项目的 `exit` 则直接操作进程结构体，进行资源回收和状态转换，模型相对简单。这表明目标项目并非简单重命名，而是实现了独立的 Linux 兼容进程模型。
* **代码溯源链接**:
  - 目标项目: `kernel/sysproc.c#sys_exit` -> `kernel/proc.c#kexit`
  - 参考项目: `kernel/sysproc.c#sys_exit` -> `kernel/proc.c#exit`

* **【内存管理/进程分配】**
* **底层技术析理**: 双方 `allocproc` 函数均遵循 xv6 的进程分配模式：获取锁 -> 遍历进程表 -> 分配页面（`kalloc`）-> 初始化内核页表。但目标项目在初始化序列中插入了 `linux_group_add_member` 等调用，将新进程纳入 Linux 线程组管理。这体现了在**内存抽象**（均为 kalloc 分配）之上的**进程抽象层**差异，目标项目构建了更复杂的层级结构。
* **代码溯源链接**:
  - 目标项目: `kernel/proc.c#allocproc`
  - 参考项目: `kernel/proc.c#allocproc`

### 5. 单方模块与降权项
- **单方模块（目标项目独有）**:
  - **网络协议栈**: 目标项目独立实现了 `kernel/net.c` 中的 `ip_rx`, `net_rx` 等函数，参考项目无此模块。这是独立开发的强证据。
  - **Linux 兼容层**: 目标项目存在大量 `linux_*` 前缀的函数（如 `linux_kill`, `linux_futex_requeue_locked`），构成完整的 Linux 兼容层，为参考项目所无。
- **降权项（第三方库/公共接口）**:
  - **第三方库 `lwext4`**: `文件系统/第三方库` 模块的高相似度（373 个共同指纹）完全由此开源库贡献，是双方共用的公共依赖，不构成抄袭证据。
  - **xv6 公共基线**: `fork1`, `parseexec`, `scheduler`, `swtch` 等函数是 xv6 教学系统的标准组件，其调用图谱由系统设计决定，属于公共领域知识，不能作为抄袭证据。
  - **硬件/架构约束**: `cpuid` (读取 `tp` 寄存器), `mycpu` (per-CPU 变量), `push_off/pop_off` (中断屏蔽) 等操作由 RISC-V 特权架构唯一确定，任何独立实现都必然相同。

### 6. 保守结论
综合全模块证据，33.31% 的 AST 相似度主要源于共同的上游代码基线（xv6）和第三方开源库（lwext4），而非项目间的直接抄袭。控方所指控的“铁证”函数均为标准 OS 原语、硬件架构约束下的必然实现或公共领域模板，其调用图谱一致是领域逻辑收敛的结果，不构成独创性表达的复用。目标项目独有的 Linux 兼容层与网络协议栈，以及双方在核心模块（如进程退出、内存管理）底层实现上的显著架构差异，共同指向了独立开发的结论。因此，本庭裁定，目标项目 `[2026-T2026107309910746-XX]` 不存在对参考项目 `[2024-赤道会师-合肥工业大学]` 的系统性抄袭行为，整体风险为低。

------------------------------

