# 🛡️ OS 内核赛自动化代码级架构审计报告

**审计时间**: `2026-07-02 14:14:53`
**参赛作品名称**: `2026-T2026107309910746-XX`
**年份**: `2026`
**学校**: `XX`
**队伍名称**: `T2026107309910746`
**比赛名字**: `OS-Competition`
**比赛子赛道**: `Kernel-Implementation`
**仓库地址**: `https://gitlab.eduxiji.net/educg-group-43501-3132633/T2026107309910746-152`
**推测开发语言**: `Rust / C (由AST探针自动识别)`
**特征函数量**: `1186`

## ⚔️ 底层代码同源性对比 (AST Homology)
> 本部分由专门的代码审查智能体，对该项目与符合年份条件的历届最高相似度项目进行控制流与底层调用对比。新版报告会先给出确定性的全模块覆盖矩阵，再给出 Agent 的分模块解释，避免只围绕少量抽样函数展开。

### 🏆 Top 1 底层对标: vs `2024-10183_BOOT-吉林大学`（吉林大学 / 10183_BOOT）

#### 对方作品信息

**对方年份**: `2024`
**对方学校**: `吉林大学`
**对方队伍名称**: `10183_BOOT`
**对方比赛名字**: `OS-Competition`
**对方比赛子赛道**: `Kernel-Implementation`
**对方仓库地址**: `https://gitlab.eduxiji.net/educg-group-26010-2376550/T202410183992516-775`

> 双向覆盖率：共同 AST 指纹: 459；唯一 AST 指纹: 目标项目 1001，参考项目 743；覆盖率: 目标项目 45.85%，参考项目 61.78%

## 全模块对比总览（确定性证据）

> 总体覆盖率：共同 AST 指纹: 459；唯一 AST 指纹: 目标项目 1001，参考项目 743；覆盖率: 目标项目 45.85%，参考项目 61.78%

### 1. 模块覆盖矩阵
| 模块 | 目标函数数 | 参考函数数 | 共同唯一 AST 指纹 | Target 覆盖 | Base 覆盖 | 状态 |
| :--- | ---: | ---: | ---: | ---: | ---: | :--- |
| 进程管理 | 101 | 39 | 14 | 16.09% | 43.75% | 双方存在且有结构重合 |
| 任务调度/异步运行时 | 48 | 34 | 6 | 12.77% | 17.65% | 双方存在且有结构重合 |
| 内存管理 | 183 | 138 | 24 | 13.33% | 18.05% | 双方存在且有结构重合 |
| 中断/异常处理 | 9 | 15 | 4 | 44.44% | 26.67% | 双方存在且有结构重合 |
| 系统调用 | 42 | 7 | 4 | 33.33% | 66.67% | 双方存在且有结构重合 |
| 文件系统 | 111 | 85 | 8 | 7.69% | 11.27% | 双方存在且有结构重合 |
| 设备驱动 | 19 | 29 | 7 | 36.84% | 26.92% | 双方存在且有结构重合 |
| 网络协议栈 | 5 | 0 | 0 | 0.0% | 0.0% | 仅目标项目体现 |
| 文件系统/第三方库 | 532 | 485 | 373 | 83.63% | 93.25% | 双方存在且有结构重合 |
| 通用模块/其他 | 136 | 52 | 24 | 21.05% | 54.55% | 双方存在且有结构重合 |

### 2. 分模块证据清单

#### 进程管理
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `user/sh.c#parseexec` | `user/sh.c#parseexec` | 同板块同名同结构 | execcmd, gettoken, panic, parseblock, parseredirs, peek | execcmd, gettoken, panic, parseblock, parseredirs, peek |
| `kernel/proc.c#sleep` | `kernel/proc.c#sleep` | 同板块同名 | acquire, myproc, release, sched | acquire, mykthread, release, sched |
| `kernel/proc.c#procdump` | `kernel/proc.c#procdump` | 同板块同名同结构 | NELEM, printf | NELEM, printf |
| `kernel/proc.c#sched` | `kernel/proc.c#sched` | 同板块同名 | holding, intr_get, mycpu, myproc, panic, swtch | holding, intr_get, mycpu, mykthread, panic, swtch |

#### 任务调度/异步运行时
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `user/sh.c#runcmd` | `user/sh.c#runcmd` | 同板块同名 | close, dup, exec, exit, fork1, fprintf ...(+6) | close, dup, exec, exit, fork1, fprintf ...(+5) |
| `kernel/console.c#consoleread` | `drivers/console.c#consoleread` | 同板块同名同结构 | C, acquire, either_copyout, killed, myproc, release ...(+1) | C, acquire, either_copyout, killed, myproc, release ...(+1) |
| `user/sh.c#nulterminate` | `user/sh.c#nulterminate` | 同板块同名同结构 | nulterminate | nulterminate |
| `kernel/spinlock.c#acquire` | `kernel/atomic/spinlock.c#acquire` | 同板块同名 | __sync_lock_test_and_set, __sync_synchronize, holding, mycpu, panic, push_off | __sync_lock_test_and_set, __sync_synchronize, holding, mycpu, panic, printf ...(+1) |

#### 内存管理
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `kernel/virtio_disk.c#virtio_disk_init` | `platform/qemu/virtio_disk.c#virtio_disk_init` | 同板块同名 | initlock, kalloc, memset, panic, superalloc | R, initlock, memset, panic |
| `kernel/proc.c#allocproc` | `kernel/proc.c#allocproc` | 同板块同名 | TRAPFRAME_SLOT, acquire, allocpid, freeproc, kalloc, linux_group_add_member ...(+7) | acquire, allocpid, allocthread, freeproc, proc_pagetable, release |
| `kernel/trap.c#usertrap` | `kernel/trap.c#usertrap` | 同板块同名 | MAKE_SATP, devintr, intr_on, kexit, killed, linux_deliver_signal ...(+13) | PGROUNDDOWN, devintr, exit, fread, intr_on, kalloc ...(+20) |
| `kernel/vm.c#uvmunmap` | `mm/vm.c#uvmunmap` | 同板块同名 | PA2PTE, PTE2PA, PTE_FLAGS, PTE_LEAF, kalloc, kfree ...(+7) | PTE2PA, PTE_FLAGS, kfree, panic, walk |

#### 中断/异常处理
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `kernel/plic.c#plicinithart` | `platform/qemu/plic.c#plicinithart` | 同板块同名 | PLIC_SENABLE, PLIC_SPRIORITY, cpuid | PLIC_SENABLE, PLIC_SPRIORITY, cpuid |
| `kernel/trap.c#clockintr` | `kernel/trap.c#clockintr` | 同板块同名 | cpuid, futex_tick, r_time, w_stimecmp, wakeup, write_acquire ...(+1) | acquire, release, wakeup |
| `kernel/plic.c#plicinit` | `platform/qemu/plic.c#plicinit` | 同板块同名 | 内部闭环逻辑 | 内部闭环逻辑 |
| `kernel/spinlock.c#pop_off` | `kernel/atomic/spinlock.c#pop_off` | 同板块同名同结构 | intr_get, intr_on, mycpu, panic | intr_get, intr_on, mycpu, panic |

#### 系统调用
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `kernel/syscall.c#argstr` | `kernel/syscall/syscall.c#argstr` | 同板块同名同结构 | argaddr, fetchstr | argaddr, fetchstr |
| `kernel/syscall.c#argaddr` | `kernel/syscall/syscall.c#argaddr` | 同板块同名同结构 | argraw | argraw |
| `kernel/syscall.c#argint` | `kernel/syscall/syscall.c#argint` | 同板块同名同结构 | argraw | argraw |
| `user/ulib.c#recv` | `kernel/syscall/syssig.c#sys_rt_sigreturn` | 同板块同结构 | __sys_recv | rt_sigreturn |

#### 文件系统
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `kernel/fs.c#dirlookup` | `fs/fat32/fat.c#dirlookup` | 同板块同名 | iget, namecmp, panic, readi | edup, eget, enext, eput, panic, reloc_clus ...(+2) |
| `kernel/file.c#filewrite` | `fs/vfs/file.c#filewrite` | 同板块同名 | devsw[f->major].write, f->vfs_ops->file->write, panic, pipewrite | devsw[f->major].write, fwrite, panic, pipewrite |
| `kernel/file.c#fileclose` | `fs/vfs/file.c#fileclose` | 同板块同名 | acquire, begin_op, end_op, ff.vfs_ops->file->close, iput, panic ...(+2) | acquire, dir_close, fclose, panic, pipeclose, release |
| `kernel/file.c#fileread` | `fs/vfs/file.c#fileread` | 同板块同名 | devsw[f->major].read, f->vfs_ops->file->read, panic, piperead | devsw[f->major].read, fread, panic, piperead |

#### 设备驱动
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `kernel/uart.c#uartinit` | `platform/qemu/uart.c#uartinit` | 同板块同名同结构 | WriteReg, initlock | WriteReg, initlock |
| `user/sh.c#parseblock` | `user/sh.c#parseblock` | 同板块同名同结构 | gettoken, panic, parseline, parseredirs, peek | gettoken, panic, parseline, parseredirs, peek |
| `kernel/console.c#consolewrite` | `drivers/console.c#consolewrite` | 同板块同名 | either_copyin, uartwrite | acquire, either_copyin, release, uart8250_putc, uartputc |
| `kernel/uart.c#uartputc_sync` | `platform/qemu/uart.c#uartputc_sync` | 同板块同名 | ReadReg, WriteReg, pop_off, push_off | ReadReg, WriteReg, pop_off, push_off |

#### 网络协议栈
仅目标项目中观察到该模块代表函数，参考项目没有同模块可对齐证据：
- 目标项目：`kernel/net.c#ip_rx`，调用摘要：acquire, find_binding, kfree, memmove, ntohl, ntohs ...(+3)
- 目标项目：`kernel/net.c#in_cksum`，调用摘要：内部闭环逻辑
- 目标项目：`kernel/net.c#net_rx`，调用摘要：arp_rx, ip_rx, kfree, ntohs
- 目标项目：`kernel/net.c#find_binding`，调用摘要：内部闭环逻辑

#### 文件系统/第三方库
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `kernel/lwext4/src/ext4_balloc.c#ext4_balloc_alloc_block` | `fs/lwext4/ext4_balloc.c#ext4_balloc_alloc_block` | 同板块同名同结构 | ext4_balloc_get_bgid_of_block, ext4_balloc_get_block_of_bgid, ext4_balloc_set_bitmap_csum, ext4_balloc_verify_bitmap_csum, ext4_bg_get_block_bitmap, ext4_bg_get_free_blocks_count ...(+19) | ext4_balloc_get_bgid_of_block, ext4_balloc_get_block_of_bgid, ext4_balloc_set_bitmap_csum, ext4_balloc_verify_bitmap_csum, ext4_bg_get_block_bitmap, ext4_bg_get_free_blocks_count ...(+19) |
| `kernel/lwext4/src/ext4.c#ext4_dir_rm` | `fs/lwext4/ext4.c#ext4_dir_rm` | 同板块同名 | EXT4_MP_LOCK, EXT4_MP_UNLOCK, ext4_block_cache_write_back, ext4_dir_en_get_inode, ext4_dir_iterator_fini, ext4_dir_iterator_init ...(+19) | EXT4_MP_LOCK, EXT4_MP_UNLOCK, ext4_block_cache_write_back, ext4_dir_en_get_inode, ext4_dir_iterator_fini, ext4_dir_iterator_init ...(+19) |
| `kernel/lwext4/src/ext4_journal.c#jbd_journal_prepare` | `fs/lwext4/ext4_journal.c#jbd_journal_prepare` | 同板块同名同结构 | EXT4_BLOCK_ZERO, RB_FIND, TAILQ_FOREACH_REVERSE_SAFE, TAILQ_FOREACH_SAFE, TAILQ_REMOVE, ext4_bcache_clear_dirty ...(+17) | EXT4_BLOCK_ZERO, RB_FIND, TAILQ_FOREACH_REVERSE_SAFE, TAILQ_FOREACH_SAFE, TAILQ_REMOVE, ext4_bcache_clear_dirty ...(+17) |
| `kernel/lwext4/src/ext4_dir_idx.c#ext4_dir_dx_split_index` | `fs/lwext4/ext4_dir_idx.c#ext4_dir_dx_split_index` | 同板块同名同结构 | ext4_block_set, ext4_dir_dx_climit_get_count, ext4_dir_dx_climit_get_limit, ext4_dir_dx_climit_set_count, ext4_dir_dx_climit_set_limit, ext4_dir_dx_entry_get_hash ...(+10) | ext4_block_set, ext4_dir_dx_climit_get_count, ext4_dir_dx_climit_get_limit, ext4_dir_dx_climit_set_count, ext4_dir_dx_climit_set_limit, ext4_dir_dx_entry_get_hash ...(+10) |

#### 通用模块/其他
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `user/sh.c#getcmd` | `user/sh.c#getcmd` | 同板块同名 | complete, memset, read, redraw, redraw_at, remember ...(+3) | gets, memset, write |
| `user/printf.c#vprintf` | `user/printf.c#vprintf` | 同板块同名 | printint, printptr, putc, va_arg | printint, printptr, putc, va_arg |
| `kernel/printf.c#printf` | `user/printf.c#printf` | 同板块同名 | acquire, consputc, printint, printptr, release, va_arg ...(+2) | va_start, vprintf |
| `kernel/spinlock.c#release` | `kernel/atomic/spinlock.c#release` | 同板块同名同结构 | __sync_lock_release, __sync_synchronize, holding, panic, pop_off | __sync_lock_release, __sync_synchronize, holding, panic, pop_off |


好的，作为本次同源性定性仲裁的审计专家，我已详细审阅了控辩双方的陈述、AST 指纹数据及证据底座。现基于客观数据与底层技术逻辑，出具最终审计意见。

### 1. 宏观判定
- **定性结论**: **独立开发，底层架构存在显著差异**
  尽管双方项目均基于公共教学内核（xv6）演进，且共同引用了第三方库（lwext4），导致整体 AST 相似度达到 35.72%，但在核心的“进程管理”与“内存管理”等自研板块，目标项目展现出与参考项目截然不同的设计哲学与实现路径。目标项目覆盖率仅为 45.85%，且大量唯一 AST 指纹（1001 个）指向其独立实现的 Linux 兼容层与网络协议栈，排除了大规模、系统性抄袭的可能。

### 2. 全模块覆盖矩阵解读
- **进程管理**: 双方均存在。共同 AST 指纹仅 14 个，目标覆盖率低至 16.09%。控方指控的“铁证”多为 trivial 函数或公共 Shell 代码。目标项目独有的 `linux_group_*`、`kclone` 等函数表明其实现了独立的 Linux 进程组模型。**证据强度：弱**。
- **任务调度/异步运行时**: 双方均存在。共同 AST 指纹 6 个，目标覆盖率 12.77%。`sched`、`yield` 等函数的调用流相似性源于 xv6 规定的标准调度状态机，但目标项目以 `myproc` 替代参考项目的 `mykthread`，体现了进程与线程调度模型的根本差异。**证据强度：弱**。
- **内存管理**: 双方均存在。共同 AST 指纹 24 个，目标覆盖率 13.33%。`allocproc`、`usertrap` 等函数虽同名，但调用图谱差异巨大。目标项目在 `usertrap` 中集成了 `linux_deliver_signal` 等信号处理路径，而参考项目则包含 `fread` 等文件系统操作，两者在异常处理流程上完全不同。**证据强度：弱**。
- **中断/异常处理**: 双方均存在。共同 AST 指纹 4 个，目标覆盖率 44.44%。`plicinit`、`pop_off` 等函数高度相似，但这是因为它们直接操作 RISC-V 特权架构规定的 PLIC 和 CSR 寄存器，属于硬件规范驱动的必然实现。**证据强度：弱**。
- **系统调用**: 双方均存在。共同 AST 指纹 4 个，目标覆盖率 33.33%。`argint`、`argaddr` 等参数解析函数完全一致，这是 xv6 系统调用入口的标准模板。目标项目大量新增的 `sys_linux_*` 系统调用是其独立开发的有力证明。**证据强度：弱**。
- **文件系统**: 双方均存在。共同 AST 指纹 8 个，目标覆盖率 7.69%。`filewrite`、`fileread` 等 VFS 抽象层函数相似，但目标项目引入了 `f->vfs_ops` 接口，实现了更灵活的文件系统挂载机制，与参考项目的直接调用方式有架构级差异。**证据强度：弱**。
- **设备驱动**: 双方均存在。共同 AST 指纹 7 个，目标覆盖率 36.84%。`uartinit`、`virtio_disk_init` 等函数相似度极高，这是因为它们初始化 QEMU 虚拟平台的标准硬件（UART, VirtIO），寄存器地址与操作序列由硬件手册定义，独立开发必然趋同。**证据强度：弱**。
- **网络协议栈**: **仅目标项目存在**。参考项目完全缺失此模块。目标项目独立实现了包含 `ip_rx`、`arp_rx` 等函数在内的完整网络栈，这是其具备独立开发复杂子系统能力的铁证。**证据强度：未形成**。
- **第三方库/公共依赖**: 双方均存在。共同 AST 指纹高达 373 个，覆盖率极高（目标 83.63%，参考 93.25%）。这源于双方均完整引入了 `lwext4` 这一开源 ext4 文件系统库。此模块的高相似度是公共依赖所致，**不构成抄袭证据，必须从核心系统原创性判定中剥离**。

### 3. 底层 AST 关键证据指纹表
| 所属板块 | 目标项目路径 | 参考项目路径 | 证据类型 | 技术说明 |
| :--- | :--- | :--- | :--- | :--- |
| 进程管理 | `kernel/proc.c#kexit` | `kernel/proc.c#exit` | 架构差异 | 目标项目调用 `linux_group_*` 等 9 个独有函数实现 Linux 线程组退出语义；参考项目调用 `fileclose`, `mythread` 等实现传统进程退出。两者状态机完全不同。 |
| 进程管理 | `kernel/sysproc.c#sys_linux_clone` | `kernel/syscall/sysproc.c#sys_clone` | 架构差异 | 目标项目调用 `kclone` (内部调用 `forkat`) 实现 Linux `clone` ABI；参考项目调用 `clone`, `clone_thread`, `fork` 实现自定义线程克隆。接口与实现均不同。 |
| 内存管理 | `kernel/trap.c#usertrap` | `kernel/trap.c#usertrap` | 架构差异 | 目标项目在陷阱处理中集成 `linux_deliver_signal` 进行信号分发；参考项目则调用 `fread` 处理文件系统请求。异常处理路径的职责划分截然不同。 |
| 系统调用 | `kernel/sysproc.c#sys_uptime` | `kernel/syscall/sysproc.c#sys_uptime` | 同步原语差异 | 目标项目使用 `read_acquire/release` (读写锁/顺序锁) 保护 uptime；参考项目使用 `acquire/release` (互斥锁)。同步策略存在根本差异。 |
| 文件系统 | `kernel/file.c#filewrite` | `fs/vfs/file.c#filewrite` | 架构差异 | 目标项目通过 `f->vfs_ops->file->write` 进行间接调用，支持多种文件系统挂载；参考项目直接调用 `fwrite`。VFS 抽象层的实现方式不同。 |
| 设备驱动 | `kernel/virtio_disk.c#virtio_disk_init` | `platform/qemu/virtio_disk.c#virtio_disk_init` | 硬件规范驱动 | 双方均初始化 VirtIO MMIO 寄存器，操作序列由 VirtIO 标准定义。目标项目额外调用 `superalloc` 进行内存分配，体现了实现差异。 |
| 第三方库 | `kernel/lwext4/src/ext4_balloc.c#ext4_balloc_alloc_block` | `fs/lwext4/ext4_balloc.c#ext4_balloc_alloc_block` | 公共依赖 | 调用图谱 100% 一致。双方均完整复用了开源 `lwext4` 库的块分配逻辑，此为公共上游代码，非项目间抄袭。 |

### 4. 核心代码调用流对比剖析
* **【进程管理/进程退出路径】**
* **底层技术析理**: 目标项目的 `kexit` 实现了复杂的 Linux 线程组模型。当一个线程退出时，需通过 `linux_group_exiting`、`linux_group_dec_live` 等函数维护线程组状态，并通过 `linux_handle_robust_list` 处理健壮 futex。参考项目的 `exit` 则遵循传统 xv6 模型，主要工作是关闭文件、将子进程托付给 `initproc` 并唤醒父进程。两者在进程生命周期的抽象层次与状态管理复杂度上存在根本性差异，目标项目绝非在参考项目基础上简单“塞入”代码。
* **代码溯源链接**:
  - 目标项目: `kernel/proc.c#kexit`
  - 参考项目: `kernel/proc.c#exit`

* **【内存管理/用户态陷阱处理】**
* **底层技术析理**: 当发生用户态中断/异常时，目标项目的 `usertrap` 核心路径是检测 `killed` 标志并调用 `linux_deliver_signal` 进行信号分发，这符合 Linux 的异步信号处理模型。参考项目的 `usertrap` 在相同位置则可能因设备中断而调用 `fread` 去读取文件系统数据。这表明两个内核将“文件系统请求”这一操作放在了完全不同的上下文中处理，一个在陷阱处理中，一个可能在系统调用或内核线程中，体现了内核架构设计的本质区别。
* **代码溯源链接**:
  - 目标项目: `kernel/trap.c#usertrap`
  - 参考项目: `kernel/trap.c#usertrap`

### 5. 单方模块与降权项
- **单方模块**:
  - **网络协议栈**: 目标项目独有的 `kernel/net.c` 模块，包含 `ip_rx`、`arp_rx` 等完整网络处理函数。参考项目无此模块，是目标项目具备独立开发能力的最有力证明。
- **降权项**:
  - **第三方库 `lwext4`**: 双方在“文件系统/第三方库”模块的高覆盖率（>80%）完全归因于共同引入的开源库，在判定核心系统原创性时必须剥离此项。
  - **公共 Shell 代码 `user/sh.c`**: `parseexec`、`fork1`、`runcmd` 等函数是 xv6 公开发布的 Shell 代码，其相似性是公共上游依赖，不构成抄袭。
  - **Trivial 函数**: `reparent`、`killed`、`setkilled`、`procdump` 等仅包含 1-2 个标准原语调用的函数，其调用图谱一致是独立开发的必然收敛，不具备区分度。
  - **跨板块弱信号**: 控方已依法降级的 `vfs_resolve_proc_path` 等跨板块证据，其调用链中包含大量目标项目独有实体，反而支持独立实现。

### 6. 保守结论
本次审计的核心结论是，目标项目 `[2026-T2026107309910746-XX]` 与参考项目 `[2024-10183_BOOT-吉林大学]` 之间**不存在系统性、实质性的代码抄袭**。35.72% 的 AST 相似度主要由公共教育内核框架（xv6）的惯用模式、硬件规范驱动的设备驱动以及双方共同引入的第三方开源库（lwext4）所贡献。在决定内核原创性的核心板块，如进程管理（覆盖率 16.09%）和内存管理（覆盖率 13.33%），目标项目展现了与参考项目截然不同的设计哲学，其实现的 Linux 兼容层（线程组、信号、Futex）和独立开发的网络协议栈，是参考项目所不具备的。控方所指控的“拙劣混淆”，实为目标项目为适配 Linux ABI 而进行的独立架构演进。综上，目标项目代码具有充分的原创合理性，风险极低。

------------------------------

