# 🛡️ OS 内核赛自动化代码级架构审计报告

**审计时间**: `2026-07-02 14:14:53`
**参赛作品名称**: `2026-T2026107309910746-XX`
**年份**: `2026`
**学校**: `XX`
**队伍名称**: `T2026107309910746`
**比赛名字**: `OS-Competition`
**比赛子赛道**: `Kernel-Implementation`
**仓库地址**: `https://gitlab.eduxiji.net/educg-group-43501-3132633/T2026107309910746-152`
**推测开发语言**: `Rust / C (由AST探针自动识别)`
**特征函数量**: `1186`

## ⚔️ 底层代码同源性对比 (AST Homology)
> 本部分由专门的代码审查智能体，对该项目与符合年份条件的历届最高相似度项目进行控制流与底层调用对比。新版报告会先给出确定性的全模块覆盖矩阵，再给出 Agent 的分模块解释，避免只围绕少量抽样函数展开。

### 🏆 Top 5 底层对标: vs `2025-静春山-中山大学`（中山大学 / 静春山）

#### 对方作品信息

**对方年份**: `2025`
**对方学校**: `中山大学`
**对方队伍名称**: `静春山`
**对方比赛名字**: `OS-Competition`
**对方比赛子赛道**: `Kernel-Implementation`
**对方仓库地址**: `https://gitlab.eduxiji.net/educg-group-36002-2710490/T202510558995330-264`

> 双向覆盖率：共同 AST 指纹: 482；唯一 AST 指纹: 目标项目 1001，参考项目 949；覆盖率: 目标项目 48.15%，参考项目 50.79%

## 全模块对比总览（确定性证据）

> 总体覆盖率：共同 AST 指纹: 482；唯一 AST 指纹: 目标项目 1001，参考项目 949；覆盖率: 目标项目 48.15%，参考项目 50.79%

### 1. 模块覆盖矩阵
| 模块 | 目标函数数 | 参考函数数 | 共同唯一 AST 指纹 | Target 覆盖 | Base 覆盖 | 状态 |
| :--- | ---: | ---: | ---: | ---: | ---: | :--- |
| 进程管理 | 101 | 68 | 19 | 21.84% | 35.19% | 双方存在且有结构重合 |
| 任务调度/异步运行时 | 48 | 69 | 7 | 14.89% | 10.29% | 双方存在且有结构重合 |
| 内存管理 | 183 | 161 | 22 | 12.22% | 14.19% | 双方存在且有结构重合 |
| 中断/异常处理 | 9 | 39 | 4 | 44.44% | 12.5% | 双方存在且有结构重合 |
| 系统调用 | 42 | 41 | 4 | 33.33% | 33.33% | 双方存在且有结构重合 |
| 文件系统 | 111 | 133 | 20 | 19.23% | 18.02% | 双方存在且有结构重合 |
| 设备驱动 | 19 | 42 | 3 | 15.79% | 8.57% | 双方存在且有结构重合 |
| 网络协议栈 | 5 | 0 | 0 | 0.0% | 0.0% | 仅目标项目体现 |
| 文件系统/第三方库 | 532 | 489 | 371 | 83.18% | 90.93% | 双方存在且有结构重合 |
| 通用模块/其他 | 136 | 126 | 37 | 32.46% | 37.0% | 双方存在且有结构重合 |

### 2. 分模块证据清单

#### 进程管理
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `kernel/proc.c#scheduler` | `kernel/proc/proc.c#scheduler` | 同板块同名 | acquire, freeproc, intr_off, intr_on, mycpu, release ...(+1) | acquire, intr_on, mycpu, release, swtch |
| `user/sh.c#parseexec` | `user/sh.c#parseexec` | 同板块同名同结构 | execcmd, gettoken, panic, parseblock, parseredirs, peek | execcmd, gettoken, panic, parseblock, parseredirs, peek |
| `kernel/proc.c#sleep` | `kernel/proc/proc.c#sleep` | 同板块同名 | acquire, myproc, release, sched | acquire, myproc, release, sched |
| `kernel/proc.c#procdump` | `kernel/proc/proc.c#procdump` | 同板块同名 | NELEM, printf | NELEM, printf |

#### 任务调度/异步运行时
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `user/sh.c#runcmd` | `user/sh.c#runcmd` | 同板块同名 | close, dup, exec, exit, fork1, fprintf ...(+6) | close, dup, execve, exit, fork1, fprintf ...(+5) |
| `kernel/console.c#consoleread` | `kernel/fs/console.c#consoleread` | 同板块同名同结构 | C, acquire, either_copyout, killed, myproc, release ...(+1) | C, acquire, either_copyout, killed, myproc, release ...(+1) |
| `user/sh.c#nulterminate` | `user/sh.c#nulterminate` | 同板块同名同结构 | nulterminate | nulterminate |
| `kernel/trap.c#kerneltrap` | `kernel/trap/la/trap.c#kerneltrap` | 同板块同名 | devintr, intr_get, myproc, panic, printf, r_scause ...(+6) | backtrace, devintr, intr_get, myproc, panic, printf ...(+11) |

#### 内存管理
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `kernel/virtio_disk.c#virtio_disk_init` | `kernel/drive/la/virtio_disk.c#virtio_disk_init` | 同板块同名 | initlock, kalloc, memset, panic, superalloc | initlock, memset, pci_device_init, printf, virtio_pci_get_device_features, virtio_pci_get_queue_enable ...(+8) |
| `kernel/proc.c#allocproc` | `kernel/proc/proc.c#allocproc` | 同板块同名 | TRAPFRAME_SLOT, acquire, allocpid, freeproc, kalloc, linux_group_add_member ...(+7) | acquire, allocpid, freeproc, kalloc, memset, proc_pagetable ...(+2) |
| `kernel/virtio_disk.c#virtio_disk_rw` | `kernel/drive/la/virtio_disk.c#virtio_disk_rw` | 同板块同名 | __sync_synchronize, acquire, alloc3_desc, checkbuf, free_chain, panic ...(+2) | PA2VA, VIRT2PHY, __sync_synchronize, acquire, alloc3_desc, free_chain ...(+7) |
| `kernel/trap.c#usertrap` | `kernel/trap/rv/trap.c#usertrap` | 同板块同名 | MAKE_SATP, devintr, intr_on, kexit, killed, linux_deliver_signal ...(+13) | devintr, exit, intr_on, killed, myproc, panic ...(+12) |

#### 中断/异常处理
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `kernel/virtio_disk.c#virtio_disk_intr` | `kernel/drive/rv/virtio_disk.c#virtio_disk_intr` | 同板块同名 | __sync_synchronize, acquire, panic, release, wakeup | R, acquire, panic, release, wakeup |
| `kernel/plic.c#plicinithart` | `kernel/drive/rv/plic.c#plicinithart` | 同板块同名 | PLIC_SENABLE, PLIC_SPRIORITY, cpuid | PLIC_SENABLE, PLIC_SPRIORITY, cpuid, printf |
| `kernel/trap.c#clockintr` | `kernel/trap/rv/trap.c#clockintr` | 同板块同名 | cpuid, futex_tick, r_time, w_stimecmp, wakeup, write_acquire ...(+1) | SBI_SET_TIMER, acquire, r_time, release, wakeup |
| `kernel/plic.c#plicinit` | `kernel/drive/rv/plic.c#plicinit` | 同板块同名 | 内部闭环逻辑 | printf |

#### 系统调用
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `user/ulib.c#mmap` | `basic/user/lib/syscall.c#mmap` | 同板块同名同结构 | __sys_mmap | syscall |
| `kernel/syscall.c#argstr` | `kernel/syscall/syscall.c#argstr` | 同板块同名 | argaddr, fetchstr | argaddr, fetchstr |
| `user/ulib.c#link` | `basic/user/lib/syscall.c#link` | 同板块同名同结构 | __sys_linkat | sys_linkat |
| `user/ulib.c#exec` | `basic/user/lib/syscall.c#exec` | 同板块同名同结构 | __sys_execve | syscall |

#### 文件系统
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `user/grind.c#go` | `user/grind.c#go` | 同板块同名同结构 | chdir, close, dup, exec, exit, fork ...(+16) | chdir, close, dup, exec, exit, fork ...(+16) |
| `mkfs/mkfs.c#iappend` | `mkfs/mkfs.c#iappend` | 同板块同名同结构 | assert, bcopy, min, rinode, rsect, winode ...(+2) | assert, bcopy, min, rinode, rsect, winode ...(+2) |
| `kernel/file.c#filewrite` | `kernel/fs/vfs/file.c#filewrite` | 同板块同名 | devsw[f->major].write, f->vfs_ops->file->write, panic, pipewrite | devsw[f->f_major].write, get_fops, get_fops()->writable, panic, pipewrite, printf ...(+1) |
| `kernel/file.c#fileclose` | `kernel/fs/vfs/file.c#fileclose` | 同板块同名 | acquire, begin_op, end_op, ff.vfs_ops->file->close, iput, panic ...(+2) | acquire, get_fops, get_fops()->writable, panic, pipeclose, release ...(+4) |

#### 设备驱动
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `user/sh.c#parseblock` | `user/sh.c#parseblock` | 同板块同名同结构 | gettoken, panic, parseline, parseredirs, peek | gettoken, panic, parseline, parseredirs, peek |
| `kernel/uart.c#uartputc_sync` | `kernel/drive/uart.c#uartputc_sync` | 同板块同名 | ReadReg, WriteReg, pop_off, push_off | ReadReg, WriteReg, pop_off, push_off |
| `kernel/virtio_disk.c#free_chain` | `kernel/drive/la/virtio_disk.c#free_chain` | 同板块同名同结构 | free_desc | free_desc |
| `kernel/printf.c#panic` | `kernel/util/printf.c#panic` | 同板块同名 | backtrace, printf | backtrace, printf |

#### 网络协议栈
仅目标项目中观察到该模块代表函数，参考项目没有同模块可对齐证据：
- 目标项目：`kernel/net.c#ip_rx`，调用摘要：acquire, find_binding, kfree, memmove, ntohl, ntohs ...(+3)
- 目标项目：`kernel/net.c#in_cksum`，调用摘要：内部闭环逻辑
- 目标项目：`kernel/net.c#net_rx`，调用摘要：arp_rx, ip_rx, kfree, ntohs
- 目标项目：`kernel/net.c#find_binding`，调用摘要：内部闭环逻辑

#### 文件系统/第三方库
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `kernel/lwext4/src/ext4_balloc.c#ext4_balloc_alloc_block` | `kernel/fs/lwext4/ext4_balloc.c#ext4_balloc_alloc_block` | 同板块同名同结构 | ext4_balloc_get_bgid_of_block, ext4_balloc_get_block_of_bgid, ext4_balloc_set_bitmap_csum, ext4_balloc_verify_bitmap_csum, ext4_bg_get_block_bitmap, ext4_bg_get_free_blocks_count ...(+19) | ext4_balloc_get_bgid_of_block, ext4_balloc_get_block_of_bgid, ext4_balloc_set_bitmap_csum, ext4_balloc_verify_bitmap_csum, ext4_bg_get_block_bitmap, ext4_bg_get_free_blocks_count ...(+19) |
| `kernel/lwext4/src/ext4.c#ext4_dir_rm` | `kernel/fs/lwext4/ext4.c#ext4_dir_rm` | 同板块同名 | EXT4_MP_LOCK, EXT4_MP_UNLOCK, ext4_block_cache_write_back, ext4_dir_en_get_inode, ext4_dir_iterator_fini, ext4_dir_iterator_init ...(+19) | EXT4_MP_LOCK, EXT4_MP_UNLOCK, ext4_block_cache_write_back, ext4_dir_en_get_inode, ext4_dir_iterator_fini, ext4_dir_iterator_init ...(+19) |
| `kernel/lwext4/src/ext4_journal.c#jbd_journal_prepare` | `kernel/fs/lwext4/ext4_journal.c#jbd_journal_prepare` | 同板块同名同结构 | EXT4_BLOCK_ZERO, RB_FIND, TAILQ_FOREACH_REVERSE_SAFE, TAILQ_FOREACH_SAFE, TAILQ_REMOVE, ext4_bcache_clear_dirty ...(+17) | EXT4_BLOCK_ZERO, RB_FIND, TAILQ_FOREACH_REVERSE_SAFE, TAILQ_FOREACH_SAFE, TAILQ_REMOVE, ext4_bcache_clear_dirty ...(+17) |
| `kernel/lwext4/src/ext4_dir_idx.c#ext4_dir_dx_split_index` | `kernel/fs/lwext4/ext4_dir_idx.c#ext4_dir_dx_split_index` | 同板块同名同结构 | ext4_block_set, ext4_dir_dx_climit_get_count, ext4_dir_dx_climit_get_limit, ext4_dir_dx_climit_set_count, ext4_dir_dx_climit_set_limit, ext4_dir_dx_entry_get_hash ...(+10) | ext4_block_set, ext4_dir_dx_climit_get_count, ext4_dir_dx_climit_get_limit, ext4_dir_dx_climit_set_count, ext4_dir_dx_climit_set_limit, ext4_dir_dx_entry_get_hash ...(+10) |

#### 通用模块/其他
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `user/sh.c#getcmd` | `user/sh.c#getcmd` | 同板块同名 | complete, memset, read, redraw, redraw_at, remember ...(+3) | gets, memset, write |
| `user/printf.c#vprintf` | `user/printf.c#vprintf` | 同板块同名 | printint, printptr, putc, va_arg | putc_buf, va_arg, write |
| `kernel/printf.c#printf` | `user/printf.c#printf` | 同板块同名 | acquire, consputc, printint, printptr, release, va_arg ...(+2) | va_start, vprintf |
| `kernel/spinlock.c#release` | `kernel/lock/spinlock.c#release` | 同板块同名同结构 | __sync_lock_release, __sync_synchronize, holding, panic, pop_off | __sync_lock_release, __sync_synchronize, holding, panic, pop_off |


#### 1. 宏观判定
- **定性结论**: 独立开发 (相似度: 32.83%，共同 AST 指纹: 482；唯一 AST 指纹: 目标项目 1001，参考项目 949；覆盖率: 目标项目 48.15%，参考项目 50.79%)

整体 AST 相似度处于低位，双方唯一指纹数量均远超共同指纹。共同指纹中，第三方开源库 lwext4 贡献了 371 条（占比 76.97%），剔除后核心模块的实质重合度极低。双方在进程管理、内存管理、系统调用等核心模块的架构设计、同步原语和功能域上存在本质差异，符合基于公共教学代码（xv6）独立演进的特征。

#### 2. 全模块覆盖矩阵解读

- **进程管理**
  - 双方均存在该模块。
  - 存在 19 条共同 AST 指纹。
  - 目标覆盖率 21.84%，参考覆盖率 35.19%。覆盖率较低，且重合点集中在 xv6 标准函数（如 `scheduler`, `sleep`, `yield` 等）。
  - 证据强度：**弱**。相似性可被公共代码基础解释，且目标项目存在大量参考项目没有的 Linux 兼容功能（如 `kclone`, `linux_kill`, `linux_futex_requeue_locked` 等）。

- **任务调度/异步运行时**
  - 双方均存在该模块。
  - 存在 7 条共同 AST 指纹。
  - 目标覆盖率 14.89%，参考覆盖率 10.29%。覆盖率极低。
  - 证据强度：**弱**。重合点主要在 `user/sh.c` 的 Shell 调度逻辑和 `console` 驱动，属于用户态公共代码或标准设备驱动模式。

- **内存管理**
  - 双方均存在该模块。
  - 存在 22 条共同 AST 指纹。
  - 目标覆盖率 12.22%，参考覆盖率 14.19%。覆盖率极低。
  - 证据强度：**弱**。重合点如 `allocproc`, `virtio_disk_init` 等，其核心调用流存在差异（如目标项目使用 `superalloc`，参考项目使用 `pci_device_init`），表明内存分配和设备初始化策略不同。

- **中断/异常处理**
  - 双方均存在该模块。
  - 存在 4 条共同 AST 指纹。
  - 目标覆盖率 44.44%，参考覆盖率 12.5%。目标项目覆盖率较高是因为其模块函数总数极少（9个），而重合的 4 个函数均为 PLIC 和时钟中断的标准处理流程。
  - 证据强度：**弱**。`plicinit`, `clockintr` 等是 RISC-V 平台的标准中断控制器和定时器操作，实现方式趋同是硬件规范决定的。

- **系统调用**
  - 双方均存在该模块。
  - 存在 4 条共同 AST 指纹。
  - 目标覆盖率 33.33%，参考覆盖率 33.33%。覆盖率中等，但重合点均为用户态库的简单系统调用封装（如 `mmap`, `exec`, `link`）。
  - 证据强度：**弱**。这些封装函数仅包含一两条指令，其相似性是系统调用 ABI 和 POSIX 接口规范决定的，不反映内核实现。

- **文件系统**
  - 双方均存在该模块。
  - 存在 20 条共同 AST 指纹。
  - 目标覆盖率 19.23%，参考覆盖率 18.02%。覆盖率低。
  - 证据强度：**弱**。重合点如 `filewrite`, `fileclose` 是 VFS 层的标准操作，但目标项目引入了 `vfs_ops` 结构体，参考项目使用 `get_fops()`，架构抽象层次不同。

- **设备驱动**
  - 双方均存在该模块。
  - 存在 3 条共同 AST 指纹。
  - 目标覆盖率 15.79%，参考覆盖率 8.57%。覆盖率极低。
  - 证据强度：**未形成**。重合点 `uartputc_sync`, `free_chain` 是 UART 和 VirtIO 设备的硬件操作原语，由硬件手册定义。

- **网络协议栈**
  - 仅目标项目存在该模块（5个函数），参考项目为 0。
  - 不存在共同 AST 指纹。
  - 证据强度：**未形成**。这是目标项目独立开发的功能模块，是证明双方设计域不同的强证据。

- **第三方库/公共依赖**
  - 双方均存在该模块（lwext4 文件系统库）。
  - 存在 371 条共同 AST 指纹。
  - 目标覆盖率 83.18%，参考覆盖率 90.93%。覆盖率极高。
  - 证据强度：**强（但无争议）**。该模块是双方共同引用的开源第三方库，其高相似性是预期内的，不构成抄袭证据，应在同源性判定中剔除。

#### 3. 底层 AST 关键证据指纹表

| 所属板块 | 目标项目路径 | 参考项目路径 | 证据类型 | 技术说明 |
| :--- | :--- | :--- | :--- | :--- |
| 进程管理 | `kernel/proc.c#scheduler` | `kernel/proc/proc.c#scheduler` | 同板块同名 | xv6 标准调度器骨架。目标项目增加了 `freeproc` 和 `intr_off/intr_on`，表明资源回收和中断管理策略不同。 |
| 进程管理 | `kernel/proc.c#yield` | `kernel/proc/proc.c#yield` | 同板块同名 | xv6 标准让出 CPU 流程 (`acquire, myproc, release, sched`)，是进程调度的通用模式。 |
| 进程管理 | `kernel/proc.c#myproc` | `kernel/proc/proc.c#myproc` | 同板块同名 | xv6 标准获取当前进程控制块流程 (`mycpu, push_off, pop_off`)，是 RISC-V 多核 OS 的标准原语。 |
| 进程管理 | `kernel/sysproc.c#sys_uptime` | `kernel/syscall/sysproc.c#sys_uptime` | 同板块同名同结构 | 系统调用入口相同，但目标项目使用 `read_acquire/release`（顺序锁），参考项目使用 `acquire/release`（自旋锁），同步机制存在本质差异。 |
| 进程管理 | `kernel/sysproc.c#sys_exit` | `kernel/syscall/sysproc.c#sys_exit` | 同板块同名同结构 | 系统调用入口相同，但目标项目调用 `kexit`（内核线程组退出逻辑），参考项目调用 `exit`，调用链和功能域完全不同。 |
| 系统调用 | `user/ulib.c#mmap` | `basic/user/lib/syscall.c#mmap` | 同板块同名同结构 | 用户态库的 trivial 封装，仅包含一次系统调用指令，相似性由 POSIX 规范决定。 |
| 文件系统 | `kernel/file.c#filewrite` | `kernel/fs/vfs/file.c#filewrite` | 同板块同名 | VFS 写操作入口。目标项目通过 `f->vfs_ops->file->write` 分发，参考项目通过 `get_fops()->writable` 分发，VFS 架构抽象不同。 |
| 第三方库 | `kernel/lwext4/src/ext4_balloc.c#ext4_balloc_alloc_block` | `kernel/fs/lwext4/ext4_balloc.c#ext4_balloc_alloc_block` | 同板块同名同结构 | 双方共同引用的开源第三方库 lwext4，代码完全相同，不构成原创性侵权证据。 |

#### 4. 核心代码调用流对比剖析

* **【进程管理/进程退出路径】**
* **底层技术析理**: 目标项目的 `sys_exit` 调用 `kexit`，进而触发 `linux_group_dec_live`, `linux_group_exiting` 等复杂的 Linux 线程组管理逻辑。参考项目的 `sys_exit` 直接调用 `exit`，执行标准的进程资源回收。目标项目的调用流深度和外部对象（`linux_group_*`）表明其实现了一个 Linux 兼容的线程组模型，而参考项目是传统的 Unix 进程模型。两者在进程抽象层次和退出语义上存在根本性架构差异。
* **代码溯源链接**:
  - 目标项目: `kernel/sysproc.c#sys_exit` -> `kernel/proc.c#kexit`
  - 参考项目: `kernel/syscall/sysproc.c#sys_exit` -> `kernel/proc/proc.c#exit`

* **【进程管理/时间读取路径】**
* **底层技术析理**: 双方均有 `sys_uptime` 系统调用，但底层同步原语截然不同。目标项目使用 `read_acquire` 和 `read_release`，这是为读取 64 位时间戳而设计的顺序锁（seqlock）读端原语，允许无锁读取。参考项目使用 `acquire` 和 `release`，是标准的自旋锁操作，读取期间会阻塞并发写入。这体现了在并发性能和实现复杂度上的不同设计权衡。
* **代码溯源链接**:
  - 目标项目: `kernel/sysproc.c#sys_uptime`
  - 参考项目: `kernel/syscall/sysproc.c#sys_uptime`

* **【文件系统/虚拟文件系统（VFS）写路径】**
* **底层技术析理**: 双方均有 `filewrite` 函数，但 VFS 层的对象管理模型不同。目标项目将文件操作封装在 `vfs_ops` 结构体中，通过 `f->vfs_ops->file->write` 进行多级分发，暗示了更复杂的 VFS 层抽象。参考项目使用 `get_fops()` 直接获取文件操作函数集，模型更扁平。这表明双方在 VFS 架构设计上存在差异。
* **代码溯源链接**:
  - 目标项目: `kernel/file.c#filewrite`
  - 参考项目: `kernel/fs/vfs/file.c#filewrite`

#### 5. 单方模块与降权项

- **单方模块**:
  - **网络协议栈**: 仅存在于目标项目 (`kernel/net.c`)，包含 `ip_rx`, `net_rx` 等函数。这是目标项目独立开发的强证据，直接证明其功能域与参考项目完全不同。
  - **Linux 兼容层**: 目标项目独有的 `sys_linux_clone`, `linux_kill`, `linux_futex_requeue_locked` 等大量函数，构成了一个参考项目完全不具备的 Linux 系统调用兼容层。

- **降权项**:
  - **第三方库 (lwext4)**: 贡献了 371 条共同指纹，占共同指纹总数的 76.97%。该部分为双方共同引用的开源代码，必须从原创性比对中剔除。
  - **公共教学代码 (xv6)**: `scheduler`, `sleep`, `yield`, `myproc`, `mycpu`, `cpuid`, `wakeup`, `killed`, `setkilled`, `parseexec`, `fork1` 等函数均为 MIT xv6 操作系统的标准实现。其调用图谱的相似性源于共同祖先，不能作为抄袭证据。
  - **跨板块硬凑**: 控方将 `user/sh.c` 的函数拆分到进程管理、任务调度、设备驱动等多个模块进行指控，这人为制造了跨板块同名证据。`user/sh.c` 是一个完整的用户态 Shell 程序，其内部函数调用关系由 Shell 语法决定，不应被拆散作为不同模块的复用证据。
  - **硬件规范决定项**: `plicinit`, `clockintr`, `uartputc_sync`, `free_chain` 等函数的相似性由 RISC-V 平台规范、VirtIO 标准和 UART 硬件手册决定，是操作硬件的必然实现方式。

#### 6. 保守结论

综合评估，双方项目整体 AST 相似度仅 32.83%，处于独立开发的典型区间。高达 76.97% 的共同指纹源自双方共同引用的开源第三方库 lwext4，在剔除该公共依赖后，核心模块的实质相似度极低。控方所指控的“大面积抄袭”函数，如 `yield`, `myproc`, `sleep` 等，均为 MIT xv6 教学操作系统的标准实现，其调用图谱相似是共同代码基础的必然结果，不构成对参考项目的侵权。更重要的是，目标项目在进程管理、系统调用等领域实现了参考项目完全不具备的 Linux 兼容层和网络协议栈，且在 `sys_uptime` 等共有路径上采用了不同的同步原语（顺序锁 vs 自旋锁），在 `sys_exit` 等路径上展现了不同的进程抽象模型（线程组 vs 传统进程）。这些架构层面的本质差异，结合极低的模块覆盖率，共同排除了对参考项目进行抄袭或代码复用的可能。

------------------------------

