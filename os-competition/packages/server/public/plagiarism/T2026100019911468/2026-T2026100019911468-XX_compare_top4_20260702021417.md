# 🛡️ OS 内核赛自动化代码级架构审计报告

**审计时间**: `2026-07-02 02:14:17`
**参赛作品名称**: `2026-T2026100019911468-XX`
**年份**: `2026`
**学校**: `XX`
**队伍名称**: `T2026100019911468`
**比赛名字**: `OS-Competition`
**比赛子赛道**: `Kernel-Implementation`
**仓库地址**: `https://gitlab.eduxiji.net/educg-group-43501-3132633/T2026100019911468-282`
**推测开发语言**: `Rust / C (由AST探针自动识别)`
**特征函数量**: `604`

## ⚔️ 底层代码同源性对比 (AST Homology)
> 本部分由专门的代码审查智能体，对该项目与符合年份条件的历届最高相似度项目进行控制流与底层调用对比。新版报告会先给出确定性的全模块覆盖矩阵，再给出 Agent 的分模块解释，避免只围绕少量抽样函数展开。

### 🏆 Top 4 底层对标: vs `2021-HDACP-青海大学`（青海大学 / HDACP）

#### 对方作品信息

**对方年份**: `2021`
**对方学校**: `青海大学`
**对方队伍名称**: `HDACP`
**对方比赛名字**: `OS-Competition`
**对方比赛子赛道**: `Kernel-Implementation`
**对方仓库地址**: `https://gitlab.eduxiji.net/chino/chinos`

> 双向覆盖率：共同 AST 指纹: 263；唯一 AST 指纹: 目标项目 528，参考项目 363；覆盖率: 目标项目 49.81%，参考项目 72.45%

## 全模块对比总览（确定性证据）

> 总体覆盖率：共同 AST 指纹: 263；唯一 AST 指纹: 目标项目 528，参考项目 363；覆盖率: 目标项目 49.81%，参考项目 72.45%

### 1. 模块覆盖矩阵
| 模块 | 目标函数数 | 参考函数数 | 共同唯一 AST 指纹 | Target 覆盖 | Base 覆盖 | 状态 |
| :--- | ---: | ---: | ---: | ---: | ---: | :--- |
| 进程管理 | 60 | 76 | 24 | 45.28% | 48.0% | 双方存在且有结构重合 |
| 任务调度/异步运行时 | 56 | 39 | 27 | 50.0% | 72.97% | 双方存在且有结构重合 |
| 内存管理 | 119 | 102 | 61 | 53.98% | 62.89% | 双方存在且有结构重合 |
| 中断/异常处理 | 29 | 20 | 13 | 48.15% | 72.22% | 双方存在且有结构重合 |
| 系统调用 | 25 | 3 | 2 | 11.76% | 100.0% | 双方存在且有结构重合 |
| 文件系统 | 84 | 56 | 33 | 42.31% | 62.26% | 双方存在且有结构重合 |
| 设备驱动 | 39 | 27 | 20 | 60.61% | 95.24% | 双方存在且有结构重合 |
| 网络协议栈 | 0 | 0 | 0 | 0.0% | 0.0% | 双方均未体现 |
| 文件系统/第三方库 | 42 | 0 | 0 | 0.0% | 0.0% | 仅目标项目体现 |
| 通用模块/其他 | 150 | 114 | 88 | 68.22% | 94.62% | 双方存在且有结构重合 |

### 2. 分模块证据清单

#### 进程管理
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `xv6-k210/kernel/proc.c#wait` | `kernel/proc.c#wait` | 同板块同名同结构 | acquire, copyout2, freeproc, myproc, release, sleep | acquire, copyout2, freeproc, myproc, release, sleep |
| `xv6-k210/kernel/proc.c#reparent` | `kernel/proc.c#reparent` | 同板块同名同结构 | acquire, release | acquire, release |
| `xv6-k210/kernel/proc.c#sleep` | `kernel/proc.c#sleep` | 同板块同名同结构 | acquire, myproc, release, sched | acquire, myproc, release, sched |
| `xv6-k210/kernel/proc.c#procdump` | `kernel/proc.c#procdump` | 同板块同名同结构 | NELEM, printf | NELEM, printf |

#### 任务调度/异步运行时
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `xv6-k210/kernel/sysctl.c#sysctl_clock_device_en` | `kernel/sysctl.c#sysctl_clock_device_en` | 同板块同名同结构 | 内部闭环逻辑 | 内部闭环逻辑 |
| `xv6-k210/bootloader/SBI/rustsbi-qemu/src/main.rs#start_trap_rust` | `bootloader/SBI/rustsbi-qemu/src/main.rs#start_trap_rust` | 同板块同名同结构 | clint.get_mtime, get_vaddr_u32, hal::Clint::new, llvm_asm!, mcause::read, mcause::read().cause ...(+9) | clint.get_mtime, get_vaddr_u32, hal::Clint::new, llvm_asm!, mcause::read, mcause::read().cause ...(+9) |
| `xv6-k210/kernel/fat32.c#emake` | `kernel/fat32.c#emake` | 同板块同名 | (uint16), cal_checksum, ext4_active, generate_shortname, memset, panic ...(+4) | (uint16), cal_checksum, generate_shortname, memset, panic, reloc_clus ...(+3) |
| `xv6-k210/kernel/spi.c#spi_receive_data_standard` | `kernel/spi.c#spi_receive_data_standard` | 同板块同名同结构 | (uint32), spi_get_frame_size, spi_set_tmod | (uint32), spi_get_frame_size, spi_set_tmod |

#### 内存管理
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `xv6-k210/bootloader/SBI/rustsbi-k210/src/main.rs#start_trap_rust` | `bootloader/SBI/rustsbi-k210/src/main.rs#start_trap_rust` | 同板块同名同结构 | get_vaddr_u32, k210_hal::clint::mtime::read, llvm_asm!, mcause::read, mcause::read().cause, mepc::read ...(+18) | get_vaddr_u32, k210_hal::clint::mtime::read, llvm_asm!, mcause::read, mcause::read().cause, mepc::read ...(+18) |
| `xv6-k210/bootloader/SBI/rustsbi-k210/src/main.back.rs#start_trap_rust` | `bootloader/SBI/rustsbi-k210/src/main.back.rs#start_trap_rust` | 同板块同名同结构 | devintr, get_vaddr_u32, k210_hal::clint::mtime::read, llvm_asm!, mcause::read, mcause::read().cause ...(+16) | devintr, get_vaddr_u32, k210_hal::clint::mtime::read, llvm_asm!, mcause::read, mcause::read().cause ...(+16) |
| `xv6-k210/kernel/exec.c#exec` | `kernel/exec.c#exec` | 同板块同名 | MAKE_SATP, NELEM, PGROUNDUP, PX, copyout, elock ...(+21) | MAKE_SATP, PGROUNDDOWN, PGROUNDUP, PX, create_user_stack, elock ...(+20) |
| `xv6-k210/kernel/spi.c#spi_receive_data_standard_dma` | `kernel/spi.c#spi_receive_data_standard_dma` | 同板块同名同结构 | kalloc, kfree, spi_get_frame_size, spi_receive_data_normal_dma | kalloc, kfree, spi_get_frame_size, spi_receive_data_normal_dma |

#### 中断/异常处理
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `xv6-k210/kernel/trap.c#devintr` | `bootloader/SBI/rustsbi-k210/src/main.back.rs#devintr` | 同板块同名 | consoleintr, disk_intr, plic_claim, plic_complete, printf, r_scause ...(+7) | llvm_asm! |
| `xv6-k210/kernel/trap.c#trapframedump` | `kernel/trap.c#trapframedump` | 同板块同名同结构 | printf | printf |
| `xv6-k210/kernel/plic.c#plicinithart` | `kernel/plic.c#plicinithart` | 同板块同名 | PLIC_MENABLE, PLIC_SENABLE, PLIC_SPRIORITY, cpuid, plic_enable_irq_for_hart, printf ...(+1) | PLIC_MENABLE, PLIC_SENABLE, PLIC_SPRIORITY, cpuid, printf, readd |
| `xv6-k210/bootloader/SBI/rustsbi-k210/src/main.back.rs#set_rd` | `bootloader/SBI/rustsbi-k210/src/main.back.rs#set_rd` | 同板块同名同结构 | panic! | panic! |

#### 系统调用
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `xv6-k210/kernel/syscall.c#argstr` | `kernel/syscall.c#argstr` | 同板块同名同结构 | argaddr, fetchstr | argaddr, fetchstr |
| `xv6-k210/kernel/syscall.c#argaddr` | `kernel/syscall.c#argaddr` | 同板块同名同结构 | argraw | argraw |
| `xv6-k210/kernel/syscall.c#argint` | `kernel/syscall.c#argint` | 同板块同名同结构 | argraw | argraw |

#### 文件系统
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `xv6-k210/kernel/fat32.c#fat32_init` | `kernel/fat32.c#fat32_init` | 同板块同名 | bread, brelse, ext4_init, ext4_probe, initlock, initsleeplock ...(+5) | bread, brelse, initlock, initsleeplock, memmove, memset ...(+3) |
| `xv6-k210/kernel/sysfile.c#sys_rename` | `kernel/sysfile.c#sys_rename` | 同板块同名 | argstr, dirlookup, edup, elock, emake, ename ...(+8) | argstr, dirlookup, edup, elock, emake, ename ...(+7) |
| `xv6-k210/bootloader/SBI/rustsbi-qemu/src/main.rs#count_harts` | `bootloader/SBI/rustsbi-qemu/src/main.rs#count_harts` | 同板块同名同结构 | DeviceTree::load, cluster_node.children.iter, cluster_node.children.iter().count, compiled_max_hartid, core::slice::from_raw_parts, cpu_map_node.children.iter ...(+4) | DeviceTree::load, cluster_node.children.iter, cluster_node.children.iter().count, compiled_max_hartid, core::slice::from_raw_parts, cpu_map_node.children.iter ...(+4) |
| `xv6-k210/kernel/fat32.c#enext` | `kernel/fat32.c#enext` | 同板块同名 | ext4_active, ext4_enext, memset, panic, read_entry_info, read_entry_name ...(+2) | memset, panic, read_entry_info, read_entry_name, reloc_clus, rw_clus |

#### 设备驱动
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `xv6-k210/kernel/spi.c#spi_receive_data_normal_dma` | `kernel/spi.c#spi_receive_data_normal_dma` | 同板块同名同结构 | (uint32), dmac_set_single_mode, dmac_wait_done, spi_get_frame_format, spi_set_tmod, sysctl_dma_select | (uint32), dmac_set_single_mode, dmac_wait_done, spi_get_frame_format, spi_set_tmod, sysctl_dma_select |
| `xv6-k210/kernel/sdcard.c#check_block_size` | `kernel/sdcard.c#check_block_size` | 同板块同名同结构 | printf, sd_end_cmd, sd_get_response_R1, sd_get_response_R3_rest, sd_send_cmd | printf, sd_end_cmd, sd_get_response_R1, sd_get_response_R3_rest, sd_send_cmd |
| `xv6-k210/kernel/uart.c#uartinit` | `kernel/uart.c#uartinit` | 同板块同名 | WriteReg, initlock | WriteReg, initlock |
| `xv6-k210/kernel/uart.c#uartstart` | `kernel/uart.c#uartstart` | 同板块同名同结构 | ReadReg, WriteReg, wakeup | ReadReg, WriteReg, wakeup |

#### 文件系统/第三方库
仅目标项目中观察到该模块代表函数，参考项目没有同模块可对齐证据：
- 目标项目：`xv6-k210/kernel/ext4.c#extent_map_from_disk`，调用摘要：block_byte, disk_read_bytes, extent_map_from_disk, rd16, rd32, rd64_from_lohi
- 目标项目：`xv6-k210/kernel/ext4.c#ext4_init`，调用摘要：fill_entry_from_inode, initlock, initsleeplock, memset, printf, rd16 ...(+3)
- 目标项目：`xv6-k210/kernel/ext4.c#extent_map_from_mem`，调用摘要：extent_map_from_disk, rd16, rd32, rd64_from_lohi
- 目标项目：`xv6-k210/kernel/ext4.c#ext4_enext`，调用摘要：fill_entry_from_inode, inode_read_bytes, memset, rd16, rd32, read_inode

#### 通用模块/其他
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `xv6-k210/kernel/dmac.c#dmac_set_channel_param` | `kernel/dmac.c#dmac_set_channel_param` | 同板块同名同结构 | is_memory, readq, writeq | is_memory, readq, writeq |
| `xv6-k210/kernel/sdcard.c#sdcard_write_sector` | `kernel/sdcard.c#sdcard_write_sector` | 同板块同名同结构 | acquiresleep, panic, printf, releasesleep, sd_end_cmd, sd_get_response_R1 ...(+4) | acquiresleep, panic, printf, releasesleep, sd_end_cmd, sd_get_response_R1 ...(+4) |
| `xv6-k210/kernel/dmac.c#dmac_init` | `kernel/dmac.c#dmac_init` | 同板块同名同结构 | dmac_enable, readq, sysctl_clock_enable, writeq | dmac_enable, readq, sysctl_clock_enable, writeq |
| `xv6-k210/kernel/logo.c#print_logo` | `kernel/logo.c#print_logo` | 同板块同名同结构 | printf | printf |


好的，遵循审计指令，我将以仲裁专家身份，基于客观数据、双方证据及控辩逻辑，进行最终定性裁决。

### 1. 宏观判定
- **定性结论**: **独立开发 / 底层架构显著差异**
  - **相似度**: 41.88%
  - **共同 AST 指纹**: 263
  - **唯一 AST 指纹**: 目标项目 528，参考项目 363
  - **覆盖率**: 目标项目 49.81%，参考项目 72.45%
  - **裁决摘要**: 双方项目共享了源自 xv6 教学操作系统及 RISC-V 架构的公共基线代码，导致约 42% 的 AST 指纹重合。然而，目标项目超过一半（50.19%）的代码指纹是独立且唯一的，并在进程调度、文件系统、系统调用等核心模块展现出与参考项目截然不同的架构设计与功能扩展。控方所指控的“系统性克隆”仅聚焦于公共基线部分，忽视了双方在关键子系统上的根本性差异，指控不成立。

### 2. 全模块覆盖矩阵解读

| 模块 | 双方存在 | 共同指纹 | 目标覆盖 | 参考覆盖 | 证据强度 | 解读 |
| :--- | :---: | :---: | :---: | :---: | :---: | :--- |
| **进程管理** | 是 | 24 | 45.28% | 48.0% | **中** | 双方均实现了 xv6 标准的进程模型。重合部分主要是 `wait`、`sched` 等 POSIX/xv6 规范下的标准原语。目标项目独立扩展了 MLFQ 调度、Linux ABI 兼容等大量新函数，导致其覆盖率不足 50%。 |
| **任务调度/异步运行时** | 是 | 27 | 50.0% | 72.97% | **中** | 重合点集中在 SBI 层和 FAT32 文件系统初始化等底层硬件适配代码，这些是 K210 平台和 xv6 框架的公共依赖。目标项目在此模块有半数独立实现。 |
| **内存管理** | 是 | 61 | 53.98% | 62.89% | **中** | 这是重合度较高的模块，主要因为 SBI 启动、页表构建、`exec` 加载等流程在 xv6 体系下高度标准化。尽管如此，目标项目仍有近半数的独立 AST 指纹。 |
| **中断/异常处理** | 是 | 13 | 48.15% | 72.22% | **弱** | 重合点主要是 PLIC 初始化、`devintr` 分发等 RISC-V 架构标准流程。目标项目覆盖率低，表明其在 trap 处理上有独立实现。 |
| **系统调用** | 是 | 2 | 11.76% | 100.0% | **弱** | 这是双方差异最显著的模块之一。目标项目有 25 个函数，参考项目仅 3 个。极低的覆盖率证明目标项目实现了大量参考项目不具备的系统调用，功能集完全不同。 |
| **文件系统** | 是 | 33 | 42.31% | 62.26% | **中** | 双方都基于 FAT32，但目标项目额外引入了 ext4 支持，这是重大的架构性差异。重合部分主要是 FAT32 的标准目录操作。 |
| **设备驱动** | 是 | 20 | 60.61% | 95.24% | **中** | 这是为 K210 特定硬件（SPI、SD卡、UART）编写的驱动，代码受硬件手册强约束，因此重合度较高。这是典型的“平台锁定”效应，而非抄袭。 |
| **网络协议栈** | 否 | 0 | 0.0% | 0.0% | **未形成** | 双方均未实现。 |
| **文件系统/第三方库** | 否 | 0 | 0.0% | 0.0% | **未形成** | 仅目标项目包含 ext4 相关代码，是其独立扩展的铁证。 |
| **通用模块/其他** | 是 | 88 | 68.22% | 94.62% | **中** | 包含 DMA、SD卡驱动、启动 logo 等底层硬件操作和工具函数，受硬件手册和通用编程模式约束，重合度较高。 |

### 3. 底层 AST 关键证据指纹表

| 所属板块 | 目标项目路径 | 参考项目路径 | 证据类型 | 技术说明 |
| :--- | :--- | :--- | :--- | :--- |
| **文件系统/第三方库** | `xv6-k210/kernel/ext4.c#ext4_init` | (无) | **单方独立模块** | 目标项目独立集成了 ext4 文件系统，这是与参考项目 FAT32 架构的根本性差异。 |
| **进程管理** | `xv6-k210/kernel/proc.c#proc_on_timer_tick` | (无) | **单方独立实现** | 目标项目实现了 MLFQ 调度算法的时钟中断处理逻辑，调用了 `mlfq_maybe_update_locked` 等独立函数。 |
| **进程管理** | `xv6-k210/kernel/proc.c#run_next_init_test` | (无) | **单方独立实现** | 目标项目包含一套独立的 init 进程测试框架，调用了 `ext4_active`, `reset_init_runtime` 等特有函数。 |
| **系统调用** | `xv6-k210/kernel/syscall.c#is_linux_abi_proc` | (无) | **单方独立实现** | 目标项目实现了 Linux ABI 兼容层，这是参考项目完全没有的功能。 |
| **进程管理** | `xv6-k210/kernel/proc.c#wait` | `kernel/proc.c#wait` | **公共基线同源** | 双方均实现了 xv6 标准的 `wait` 系统调用，调用图谱一致是遵循 POSIX 语义和 xv6 教科书实现的必然结果。 |
| **进程管理** | `xv6-k210/kernel/proc.c#sched` | `kernel/proc.c#sched` | **公共基线同源** | 上下文切换的核心，其 `holding, intr_get, swtch` 调用序列是 xv6 在多核 RISC-V 上的标准调度协议。 |
| **设备驱动** | `xv6-k210/kernel/spi.c#spi_receive_data_standard_dma` | `kernel/spi.c#spi_receive_data_standard_dma` | **平台锁定同源** | K210 SPI 驱动，其 DMA 操作流程由硬件手册严格规定，独立开发必然产生高度相似的代码。 |
| **内存管理** | `xv6-k210/bootloader/SBI/rustsbi-k210/src/main.rs#start_trap_rust` | `bootloader/SBI/rustsbi-k210/src/main.rs#start_trap_rust` | **公共基线同源** | RustSBI 是 RISC-V 标准的 SBI 实现，作为第三方引导固件被双方共同使用。 |
| **通用模块/其他** | `xv6-k210/kernel/dmac.c#dmac_set_channel_param` | `kernel/dmac.c#dmac_set_channel_param` | **平台锁定同源** | K210 平台 DMA 控制器配置，操作寄存器序列由硬件手册定义。 |

### 4. 核心代码调用流对比剖析

* **【内存管理 / exec 系统调用】**
* **底层技术析理**: 双方 `exec` 函数均遵循 xv6 经典的“创建新页表、加载 ELF、分配用户栈”流程。然而，目标项目在 `exec` 中引入了对 `ext4_active` 的检查，表明其文件系统后端已从单一的 FAT32 扩展到支持 ext4，这是一个根本性的架构差异。参考项目的 `exec` 则无此逻辑，其文件系统操作完全基于 FAT32。此外，目标项目在用户栈构建上使用了 `create_user_stack`，而参考项目使用了 `merge_stack`，显示出在内存布局策略上的独立设计。
* **代码溯源链接**:
  - 目标项目: `xv6-k210/kernel/exec.c#exec`
  - 参考项目: `kernel/exec.c#exec`

* **【系统调用 / 进程控制】**
* **底层技术析理**: 这是双方差异最大的核心模块。目标项目实现了 `sys_get_priority`、`sys_getprocsz`、`clone_proc` 等系统调用，并引入了 `is_linux_abi_proc` 来判断进程的 ABI 类型，显示出向 Linux 兼容方向演进的架构意图。参考项目则实现了 `sys_mmap`、`sys_futex`、`sys_sendfile` 等更偏向 POSIX 高级特性的系统调用。两者在系统调用层的功能集和设计哲学完全不同，是独立开发的有力证明。
* **代码溯源链接**:
  - 目标项目: `xv6-k210/kernel/sysproc.c#sys_get_priority`
  - 参考项目: `kernel/sysproc.c#sys_mmap`

* **【进程管理 / 调度器】**
* **底层技术析理**: 双方都保留了 xv6 的 `sched` 基础调度框架。但目标项目在此之上构建了完整的 MLFQ 调度层，通过 `proc_on_timer_tick`、`mlfq_maybe_update_locked`、`pick_priority_proc` 等函数实现了动态优先级调整和多级队列调度。参考项目的调度器则更接近原始的 xv6 轮转调度，其扩展集中在 `sys_sched_get_priority_max` 等静态优先级查询接口上。两者在调度策略和实现复杂度上存在显著差异。
* **代码溯源链接**:
  - 目标项目: `xv6-k210/kernel/proc.c#proc_on_timer_tick`
  - 参考项目: `kernel/sysproc.c#sys_sched_get_priority_max`

### 5. 单方模块与降权项

*   **单方模块**:
    *   **文件系统/第三方库 (ext4)**: 目标项目独有的 `ext4.c` 模块，包含 `ext4_init`, `ext4_enext` 等函数，是独立开发的铁证。
    *   **进程管理 (MLFQ 调度)**: 目标项目独有的 `pick_priority_proc`, `mlfq_maybe_update_locked` 等函数，构成了独立的 MLFQ 调度子系统。
    *   **进程管理 (Linux ABI)**: 目标项目独有的 `is_linux_abi_proc` 函数，表明其架构向 Linux 兼容方向演进。
*   **降权项**:
    *   **公共基线/平台锁定代码**: 所有标记为 `[🚨同板块底层逻辑一致]` 的函数，如 `wait`, `sched`, `mycpu`, `cpuid` 等，以及 SPI、DMA 等 K210 设备驱动，均属于 xv6 教学基线、RISC-V 架构规范或特定硬件平台所强约束的实现。它们的调用图谱一致是接口、架构或硬件手册的必然要求，不具备鉴别抄袭的效力。
    *   **第三方库**: `bootloader/SBI/` 下的 RustSBI 代码是双方共同依赖的第三方开源引导固件，不应作为比对证据。
    *   **通用工具函数**: `printf`, `panic`, `memset`, `NELEM` 等是 C 语言标准库或内核通用基础设施，其调用关系无鉴别意义。

### 6. 保守结论

综合评估，目标项目 `[2026-T2026100019911468-XX]` 与参考项目 `[2021-HDACP-青海大学]` 的 41.88% AST 相似度，主要源于双方对 xv6 教学操作系统、RISC-V 特权架构及 Kendryte K210 硬件平台的共同依赖。这种相似性集中在受规范、教材或硬件手册强约束的底层代码上，属于典型的“平台锁定”和“公共基线复用”，而非抄袭。目标项目超过一半的代码指纹是独立的，并在文件系统（引入 ext4）、进程调度（实现 MLFQ）和系统调用（扩展 Linux ABI）等核心模块展现出与参考项目截然不同的架构设计和功能演进方向。因此，整体风险等级为**低**，现有证据不支持抄袭指控，更符合双方基于同一开源基线进行独立开发和扩展的结论。

------------------------------

