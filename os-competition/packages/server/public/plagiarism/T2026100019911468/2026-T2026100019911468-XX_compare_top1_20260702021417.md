# 🛡️ OS 内核赛自动化代码级架构审计报告

**审计时间**: `2026-07-02 02:14:17`
**参赛作品名称**: `2026-T2026100019911468-XX`
**年份**: `2026`
**学校**: `XX`
**队伍名称**: `T2026100019911468`
**比赛名字**: `OS-Competition`
**比赛子赛道**: `Kernel-Implementation`
**仓库地址**: `https://gitlab.eduxiji.net/educg-group-43501-3132633/T2026100019911468-282`
**推测开发语言**: `Rust / C (由AST探针自动识别)`
**特征函数量**: `604`

## ⚔️ 底层代码同源性对比 (AST Homology)
> 本部分由专门的代码审查智能体，对该项目与符合年份条件的历届最高相似度项目进行控制流与底层调用对比。新版报告会先给出确定性的全模块覆盖矩阵，再给出 Agent 的分模块解释，避免只围绕少量抽样函数展开。

### 🏆 Top 1 底层对标: vs `2024-孤独的根号五-宁波工程学院`（宁波工程学院 / 孤独的根号五）

#### 对方作品信息

**对方年份**: `2024`
**对方学校**: `宁波工程学院`
**对方队伍名称**: `孤独的根号五`
**对方比赛名字**: `OS-Competition`
**对方比赛子赛道**: `Kernel-Implementation`
**对方仓库地址**: `https://gitlab.eduxiji.net/educg-group-26010-2376550/T202411058992483-3556`

> 双向覆盖率：共同 AST 指纹: 321；唯一 AST 指纹: 目标项目 528，参考项目 388；覆盖率: 目标项目 60.8%，参考项目 82.73%

## 全模块对比总览（确定性证据）

> 总体覆盖率：共同 AST 指纹: 321；唯一 AST 指纹: 目标项目 528，参考项目 388；覆盖率: 目标项目 60.8%，参考项目 82.73%

### 1. 模块覆盖矩阵
| 模块 | 目标函数数 | 参考函数数 | 共同唯一 AST 指纹 | Target 覆盖 | Base 覆盖 | 状态 |
| :--- | ---: | ---: | ---: | ---: | ---: | :--- |
| 进程管理 | 60 | 29 | 27 | 50.94% | 93.1% | 双方存在且有结构重合 |
| 任务调度/异步运行时 | 56 | 41 | 32 | 59.26% | 82.05% | 双方存在且有结构重合 |
| 内存管理 | 119 | 105 | 73 | 64.6% | 72.28% | 双方存在且有结构重合 |
| 中断/异常处理 | 29 | 20 | 13 | 48.15% | 72.22% | 双方存在且有结构重合 |
| 系统调用 | 25 | 3 | 2 | 11.76% | 100.0% | 双方存在且有结构重合 |
| 文件系统 | 84 | 75 | 46 | 58.97% | 69.7% | 双方存在且有结构重合 |
| 设备驱动 | 39 | 28 | 21 | 63.64% | 95.45% | 双方存在且有结构重合 |
| 网络协议栈 | 0 | 0 | 0 | 0.0% | 0.0% | 双方均未体现 |
| 文件系统/第三方库 | 42 | 2 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |
| 通用模块/其他 | 150 | 138 | 113 | 87.6% | 96.58% | 双方存在且有结构重合 |

### 2. 分模块证据清单

#### 进程管理
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `xv6-k210/kernel/proc.c#wait` | `kernel/proc.c#wait` | 同板块同名同结构 | acquire, copyout2, freeproc, myproc, release, sleep | acquire, copyout2, freeproc, myproc, release, sleep |
| `xv6-k210/kernel/proc.c#reparent` | `kernel/proc.c#reparent` | 同板块同名同结构 | acquire, release | acquire, release |
| `xv6-k210/xv6-user/sh.c#parseexec` | `user/sh.c#parseexec` | 同板块同名同结构 | execcmd, gettoken, panic, parseblock, parseredirs, peek | execcmd, gettoken, panic, parseblock, parseredirs, peek |
| `xv6-k210/kernel/proc.c#sleep` | `kernel/proc.c#sleep` | 同板块同名同结构 | acquire, myproc, release, sched | acquire, myproc, release, sched |

#### 任务调度/异步运行时
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `xv6-k210/kernel/sysctl.c#sysctl_clock_device_en` | `kernel/sysctl.c#sysctl_clock_device_en` | 同板块同名同结构 | 内部闭环逻辑 | 内部闭环逻辑 |
| `xv6-k210/bootloader/SBI/rustsbi-qemu/src/main.rs#start_trap_rust` | `bootloader/SBI/rustsbi-qemu/src/main.rs#start_trap_rust` | 同板块同名同结构 | clint.get_mtime, get_vaddr_u32, hal::Clint::new, llvm_asm!, mcause::read, mcause::read().cause ...(+9) | clint.get_mtime, get_vaddr_u32, hal::Clint::new, llvm_asm!, mcause::read, mcause::read().cause ...(+9) |
| `xv6-k210/kernel/fat32.c#emake` | `kernel/fat32.c#emake` | 同板块同名 | (uint16), cal_checksum, ext4_active, generate_shortname, memset, panic ...(+4) | (uint16), cal_checksum, generate_shortname, memset, panic, reloc_clus ...(+3) |
| `xv6-k210/kernel/spi.c#spi_receive_data_standard` | `kernel/spi.c#spi_receive_data_standard` | 同板块同名同结构 | (uint32), spi_get_frame_size, spi_set_tmod | (uint32), spi_get_frame_size, spi_set_tmod |

#### 内存管理
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `xv6-k210/bootloader/SBI/rustsbi-k210/src/main.rs#start_trap_rust` | `bootloader/SBI/rustsbi-k210/src/main.rs#start_trap_rust` | 同板块同名同结构 | get_vaddr_u32, k210_hal::clint::mtime::read, llvm_asm!, mcause::read, mcause::read().cause, mepc::read ...(+18) | get_vaddr_u32, k210_hal::clint::mtime::read, llvm_asm!, mcause::read, mcause::read().cause, mepc::read ...(+18) |
| `xv6-k210/bootloader/SBI/rustsbi-k210/src/main.back.rs#start_trap_rust` | `bootloader/SBI/rustsbi-k210/src/main.back.rs#start_trap_rust` | 同板块同名同结构 | devintr, get_vaddr_u32, k210_hal::clint::mtime::read, llvm_asm!, mcause::read, mcause::read().cause ...(+16) | devintr, get_vaddr_u32, k210_hal::clint::mtime::read, llvm_asm!, mcause::read, mcause::read().cause ...(+16) |
| `xv6-k210/kernel/exec.c#exec` | `kernel/exec.c#exec` | 同板块同名 | MAKE_SATP, NELEM, PGROUNDUP, PX, copyout, elock ...(+21) | MAKE_SATP, PGROUNDUP, PX, copyout, elock, ename ...(+17) |
| `xv6-k210/kernel/spi.c#spi_receive_data_standard_dma` | `kernel/spi.c#spi_receive_data_standard_dma` | 同板块同名同结构 | kalloc, kfree, spi_get_frame_size, spi_receive_data_normal_dma | kalloc, kfree, spi_get_frame_size, spi_receive_data_normal_dma |

#### 中断/异常处理
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `xv6-k210/kernel/trap.c#devintr` | `bootloader/SBI/rustsbi-k210/src/main.back.rs#devintr` | 同板块同名 | consoleintr, disk_intr, plic_claim, plic_complete, printf, r_scause ...(+7) | llvm_asm! |
| `xv6-k210/kernel/trap.c#trapframedump` | `kernel/trap.c#trapframedump` | 同板块同名同结构 | printf | printf |
| `xv6-k210/kernel/plic.c#plicinithart` | `kernel/plic.c#plicinithart` | 同板块同名 | PLIC_MENABLE, PLIC_SENABLE, PLIC_SPRIORITY, cpuid, plic_enable_irq_for_hart, printf ...(+1) | PLIC_MENABLE, PLIC_SENABLE, PLIC_SPRIORITY, cpuid, printf, readd |
| `xv6-k210/bootloader/SBI/rustsbi-k210/src/main.back.rs#set_rd` | `bootloader/SBI/rustsbi-k210/src/main.back.rs#set_rd` | 同板块同名同结构 | panic! | panic! |

#### 系统调用
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `xv6-k210/kernel/syscall.c#argstr` | `kernel/syscall.c#argstr` | 同板块同名同结构 | argaddr, fetchstr | argaddr, fetchstr |
| `xv6-k210/kernel/syscall.c#argaddr` | `kernel/syscall.c#argaddr` | 同板块同名同结构 | argraw | argraw |
| `xv6-k210/kernel/syscall.c#argint` | `kernel/syscall.c#argint` | 同板块同名同结构 | argraw | argraw |

#### 文件系统
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `xv6-k210/xv6-user/grind.c#go` | `user/grind.c#go` | 同板块同名同结构 | chdir, close, dup, exec, exit, fork ...(+16) | chdir, close, dup, exec, exit, fork ...(+16) |
| `xv6-k210/kernel/fat32.c#fat32_init` | `kernel/fat32.c#fat32_init` | 同板块同名 | bread, brelse, ext4_init, ext4_probe, initlock, initsleeplock ...(+5) | bread, brelse, initlock, initsleeplock, memmove, memset ...(+3) |
| `xv6-k210/kernel/sysfile.c#sys_rename` | `kernel/sysfile.c#sys_rename` | 同板块同名 | argstr, dirlookup, edup, elock, emake, ename ...(+8) | argstr, dirlookup, edup, elock, emake, ename ...(+7) |
| `xv6-k210/bootloader/SBI/rustsbi-qemu/src/main.rs#count_harts` | `bootloader/SBI/rustsbi-qemu/src/main.rs#count_harts` | 同板块同名同结构 | DeviceTree::load, cluster_node.children.iter, cluster_node.children.iter().count, compiled_max_hartid, core::slice::from_raw_parts, cpu_map_node.children.iter ...(+4) | DeviceTree::load, cluster_node.children.iter, cluster_node.children.iter().count, compiled_max_hartid, core::slice::from_raw_parts, cpu_map_node.children.iter ...(+4) |

#### 设备驱动
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `xv6-k210/kernel/spi.c#spi_receive_data_normal_dma` | `kernel/spi.c#spi_receive_data_normal_dma` | 同板块同名同结构 | (uint32), dmac_set_single_mode, dmac_wait_done, spi_get_frame_format, spi_set_tmod, sysctl_dma_select | (uint32), dmac_set_single_mode, dmac_wait_done, spi_get_frame_format, spi_set_tmod, sysctl_dma_select |
| `xv6-k210/kernel/sdcard.c#check_block_size` | `kernel/sdcard.c#check_block_size` | 同板块同名同结构 | printf, sd_end_cmd, sd_get_response_R1, sd_get_response_R3_rest, sd_send_cmd | printf, sd_end_cmd, sd_get_response_R1, sd_get_response_R3_rest, sd_send_cmd |
| `xv6-k210/kernel/uart.c#uartinit` | `kernel/uart.c#uartinit` | 同板块同名 | WriteReg, initlock | WriteReg, initlock |
| `xv6-k210/kernel/uart.c#uartstart` | `kernel/uart.c#uartstart` | 同板块同名同结构 | ReadReg, WriteReg, wakeup | ReadReg, WriteReg, wakeup |

#### 文件系统/第三方库
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `xv6-k210/kernel/ext4.c#ext4_init` | `kernel/fs.c#ext4_init` | 同板块同名 | fill_entry_from_inode, initlock, initsleeplock, memset, printf, rd16 ...(+3) | ext4_blockdev_get, ext4_device_register, ext4_mount, printf |
| `xv6-k210/kernel/ext4.c#extent_map_from_disk` | `kernel/vfs.c#ext4_read_superblock` | 同模块上下文对齐/差异观察 | block_byte, disk_read_bytes, extent_map_from_disk, rd16, rd32, rd64_from_lohi | free, malloc |

#### 通用模块/其他
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `xv6-k210/kernel/dmac.c#dmac_set_channel_param` | `kernel/dmac.c#dmac_set_channel_param` | 同板块同名同结构 | is_memory, readq, writeq | is_memory, readq, writeq |
| `xv6-k210/kernel/sdcard.c#sdcard_write_sector` | `kernel/sdcard.c#sdcard_write_sector` | 同板块同名同结构 | acquiresleep, panic, printf, releasesleep, sd_end_cmd, sd_get_response_R1 ...(+4) | acquiresleep, panic, printf, releasesleep, sd_end_cmd, sd_get_response_R1 ...(+4) |
| `xv6-k210/kernel/dmac.c#dmac_init` | `kernel/dmac.c#dmac_init` | 同板块同名同结构 | dmac_enable, readq, sysctl_clock_enable, writeq | dmac_enable, readq, sysctl_clock_enable, writeq |
| `xv6-k210/kernel/logo.c#print_logo` | `kernel/logo.c#print_logo` | 同板块同名 | printf | printf |


好的，审计官。以下是基于现有证据、控辩双方主张及审计指令形成的最终同源性定性仲裁报告。

---

#### 1. 宏观判定
- **定性结论**: **部分逻辑借鉴 / 存在架构改动** (相似度: 53.95%，共同 AST 指纹: 321；唯一 AST 指纹: 目标项目 528，参考项目 388；覆盖率: 目标项目 60.8%，参考项目 82.73%)

**判定依据**：目标项目与参考项目均基于 MIT xv6-riscv 及 xv6-k210 等公开上游代码进行移植和开发。53.95% 的 AST 相似度及覆盖率指标的倒挂现象（参考项目 82.73% 的指纹被目标项目覆盖，而目标项目仅 60.8% 被参考项目覆盖）强烈表明，参考项目是共同上游的一个较小子集，而目标项目在同一上游基础上进行了显著的功能扩展和架构改动。控方所指控的“系统性克隆”主要源于对 API 契约、硬件规范及共同上游代码的误判，未能有效区分“源于同一祖先”与“抄袭自参考项目”。

#### 2. 全模块覆盖矩阵解读

| 模块 | 双方存在 | 共同指纹 | Target 覆盖 | Base 覆盖 | 证据强度 | 解读 |
| :--- | :---: | :---: | :---: | :---: | :---: | :--- |
| **进程管理** | 是 | 是 | 50.94% | 93.1% | **中** | 高 Base 覆盖率说明参考项目的进程管理代码几乎完全是 xv6 标准实现。目标项目在此基础上进行了大量扩展（60 个函数 vs 29 个），重合部分主要是 xv6 规范下的标准调度与同步原语。 |
| **任务调度/异步运行时** | 是 | 是 | 59.26% | 82.05% | **弱** | 该板块存在严重的领域错配。证据包含时钟驱动、SPI 驱动、FAT32 文件系统等，并非核心调度逻辑。重合部分多为 K210 SDK 或 RustSBI 的标准模板代码。 |
| **内存管理** | 是 | 是 | 64.6% | 72.28% | **中** | 双方均实现了 xv6 标准的内存管理（页表、内核分配器）。覆盖率相对均衡，表明双方在共同核心之外均有各自的实现，例如目标项目的 `exec` 函数调用链更为复杂。 |
| **中断/异常处理** | 是 | 是 | 48.15% | 72.22% | **弱** | 重合点主要在 PLIC 初始化、Trapframe 打印等标准流程。`devintr` 等核心函数因跨文件（目标在 `trap.c`，参考在 `main.back.rs`）且调用图谱差异巨大，不构成强同源证据。 |
| **系统调用** | 是 | 是 | 11.76% | 100.0% | **弱** | 参考项目仅有 3 个系统调用函数，全部与目标项目重合。这 3 个函数（`argstr`, `argaddr`, `argint`）是 xv6 内核参数解析的标准实现，无特异性。目标项目另有 22 个独立系统调用。 |
| **文件系统** | 是 | 是 | 58.97% | 69.7% | **中** | 双方均移植了 xv6 的 FAT32 文件系统。目标项目额外集成了 ext4 文件系统（`ext4_active`），导致 `fat32_init`、`emake` 等函数调用图谱出现差异，体现了架构改动。 |
| **设备驱动** | 是 | 是 | 63.64% | 95.45% | **弱** | 高 Base 覆盖率再次证明参考项目代码主要是 K210 SDK 的标准驱动封装（SPI, SD卡, UART）。目标项目在复用这些标准驱动的基础上进行了扩展。 |
| **网络协议栈** | 否 | 否 | 0.0% | 0.0% | **未形成** | 双方均未涉及。 |
| **文件系统/第三方库** | 是 | 否 | 0.0% | 0.0% | **未形成** | 目标项目有 42 个函数，参考项目有 2 个，但 AST 指纹无重合。这证明双方在此模块的实现完全不同，是独立开发的强证据。 |
| **通用模块/其他** | 是 | 是 | 87.6% | 96.58% | **弱** | 覆盖率双高，说明此模块包含了大量公共工具函数、硬件寄存器操作（如 `dmac.c`, `sdcard.c`）及标准库函数，这些代码的同源性由硬件手册和 SDK 决定。 |

#### 3. 底层 AST 关键证据指纹表

| 所属板块 | 目标项目路径 | 参考项目路径 | 证据类型 | 技术说明 |
| :--- | :--- | :--- | :--- | :--- |
| 进程管理 | `xv6-k210/kernel/proc.c#wait` | `kernel/proc.c#wait` | 同板块同名同结构 | xv6 标准 `wait` 系统调用实现，调用链 `acquire, copyout2, freeproc, myproc, release, sleep` 由 POSIX 语义和 xv6 内核锁规范强制决定。 |
| 进程管理 | `xv6-k210/kernel/proc.c#sched` | `kernel/proc.c#sched` | 同板块同名同结构 | xv6 核心上下文切换函数，调用链 `holding, intr_get, mycpu, myproc, panic, swtch` 是 xv6 调度器的标准路径。 |
| 进程管理 | `xv6-k210/kernel/sysproc.c#sys_sleep` | `kernel/sysproc.c#sys_sleep` | 同板块同名，有差异 | 目标项目在标准调用链中增加了 `proc_record_sleep`，这是一个功能扩展钩子，而非反查重混淆。 |
| 进程管理 | `xv6-k210/kernel/proc.c#forkret` | `kernel/proc.c#forkret` | 同板块同名，有差异 | 目标项目调用 `ename, fat32_init`，参考项目调用 `printf, fat32_init`。初始化路径的差异反映了错误处理或日志策略的不同。 |
| 任务调度/异步运行时 | `xv6-k210/bootloader/SBI/rustsbi-qemu/src/main.rs#start_trap_rust` | `bootloader/SBI/rustsbi-qemu/src/main.rs#start_trap_rust` | 同板块同名同结构 | 开源 RustSBI 固件的标准陷阱处理入口，调用链由 RISC-V 特权架构和 RustSBI 框架定义，非自行设计。 |
| 任务调度/异步运行时 | `xv6-k210/kernel/fat32.c#emake` | `kernel/fat32.c#emake` | 同板块同名，有差异 | 目标项目调用 `ext4_active`，表明其集成了 ext4 支持；参考项目调用 `strlen`。这是文件系统架构上的根本差异。 |
| 内存管理 | `xv6-k210/kernel/exec.c#exec` | `kernel/exec.c#exec` | 同板块同名，有差异 | `exec` 系统调用实现。目标项目调用链包含 `NELEM, elock` 等，参考项目包含 `ename`，表明在程序加载和路径解析逻辑上存在不同实现。 |
| 内存管理 | `xv6-k210/kernel/spi.c#spi_receive_data_standard_dma` | `kernel/spi.c#spi_receive_data_standard_dma` | 同板块同名同结构 | K210 SDK 标准 SPI DMA 接收逻辑，调用 `kalloc, kfree` 进行缓冲区管理，是驱动开发的常规模式。 |
| 中断/异常处理 | `xv6-k210/kernel/plic.c#plicinithart` | `kernel/plic.c#plicinithart` | 同板块同名 | RISC-V PLIC 中断控制器初始化，调用 `PLIC_MENABLE, PLIC_SENABLE` 等宏，是 SiFive PLIC 规范的标准操作序列。 |
| 系统调用 | `xv6-k210/kernel/syscall.c#argint` | `kernel/syscall.c#argint` | 同板块同名同结构 | xv6 内核参数解析的标准封装，调用 `argraw` 获取原始参数，逻辑简单且由 ABI 固定。 |
| 文件系统 | `xv6-k210/kernel/fat32.c#fat32_init` | `kernel/fat32.c#fat32_init` | 同板块同名，有差异 | 目标项目增加了 `ext4_init, ext4_probe` 调用，证明其文件系统架构是多文件系统并存，而非单纯复制。 |
| 设备驱动 | `xv6-k210/kernel/sdcard.c#check_block_size` | `kernel/sdcard.c#check_block_size` | 同板块同名同结构 | K210 SD 卡驱动初始化标准流程，调用 `sd_send_cmd, sd_get_response_R1` 等，由 SD 卡协议和 SDK 决定。 |
| 第三方库/公共依赖 | `xv6-k210/kernel/ext4.c#ext4_init` | `kernel/fs.c#ext4_init` | 跨文件同名，逻辑不同 | 目标项目调用 `fill_entry_from_inode` 等具体实现函数，参考项目调用 `ext4_device_register` 等抽象层函数，表明两者 ext4 移植来源或实现层次完全不同。 |

#### 4. 核心代码调用流对比剖析

*   **【进程管理/调度与同步路径】**
    *   **底层技术析理**: 在 `sleep` 与 `sched` 这一核心调度链路上，双方均遵循 xv6 的经典同步模型：进程通过 `acquire` 持有保护其睡眠条件的锁，调用 `sleep` 原子性地释放该锁并进入睡眠，被 `wakeup` 唤醒后重新竞争获取该锁。`sched` 函数中的 `holding`, `intr_get`, `swtch` 调用顺序是 xv6 上下文切换的强制安全协议。此调用流的高度一致源于对同一内核设计规范的遵循，而非对参考项目代码的抄袭。
    *   **代码溯源链接**:
        - 目标项目: `xv6-k210/kernel/proc.c#sleep` -> `xv6-k210/kernel/proc.c#sched`
        - 参考项目: `kernel/proc.c#sleep` -> `kernel/proc.c#sched`

*   **【文件系统/初始化路径】**
    *   **底层技术析理**: 在文件系统初始化 (`fat32_init`) 上，双方都调用了 `bread`, `brelse` 等 xv6 缓冲区缓存接口。然而，目标项目的调用流中明确出现了 `ext4_init` 和 `ext4_probe`，这表明其架构目标是构建一个支持多文件系统（FAT32 + ext4）的内核。参考项目则无此调用。这一差异是目标项目进行独立架构设计与功能扩展的直接证据，而非对参考项目的简单修改。
    *   **代码溯源链接**:
        - 目标项目: `xv6-k210/kernel/fat32.c#fat32_init`
        - 参考项目: `kernel/fat32.c#fat32_init`

*   **【设备驱动/SPI DMA 路径】**
    *   **底层技术析理**: 在 SPI DMA 接收 (`spi_receive_data_standard_dma`) 路径中，双方都遵循了 K210 SDK 的标准模式：调用 `spi_get_frame_size` 获取传输参数，调用 `kalloc` 动态分配 DMA 缓冲区，最后调用 `spi_receive_data_normal_dma` 触发传输。此调用流由 K210 硬件 DMA 引擎的工作方式和官方 SDK 的驱动框架所决定，独立开发者遵循同一套手册和示例代码必然会写出结构相似的代码。
    *   **代码溯源链接**:
        - 目标项目: `xv6-k210/kernel/spi.c#spi_receive_data_standard_dma`
        - 参考项目: `kernel/spi.c#spi_receive_data_standard_dma`

#### 5. 单方模块与降权项

*   **单方模块/功能**:
    *   **文件系统/第三方库 (目标项目独有)**: 目标项目拥有 42 个函数，参考项目仅 2 个，且 AST 指纹无任何重合。特别是目标项目的 `kernel/ext4.c` 中的 `extent_map_from_disk` 等函数，与参考项目 `kernel/vfs.c` 中的 `ext4_read_superblock` 虽同属 ext4 范畴，但调用图谱完全不同，证明其 ext4 实现是独立移植或开发的。这是目标项目原创性的强证据。
    *   **系统调用 (目标项目大幅领先)**: 目标项目有 25 个系统调用函数，参考项目仅 3 个。目标项目扩展了如 `sys_test_proc` 等大量自有功能。

*   **降权项说明**:
    *   **跨板块同名函数**: 如 `dmac_wait_idle` 被控方归入【进程管理】，实为【设备驱动】；`emake` 被归入【任务调度】，实为【文件系统】。此类跨板块硬凑的证据不能作为同源性主证据。
    *   **公共接口与泛型函数名**: `printf`, `panic`, `memset`, `memmove`, `acquire`, `release` 等是操作系统内核中无处不在的通用函数或原语，其出现不构成特异性指纹。
    *   **第三方库/公共依赖**: `bootloader/SBI/rustsbi-qemu/src/main.rs` 和 `bootloader/SBI/rustsbi-k210/src/main.rs` 中的函数，如 `start_trap_rust`，属于开源 RustSBI 项目模板代码。双方共同使用该开源组件导致的结构重合，应从同源证据中排除。

#### 6. 保守结论

本次审计的核心发现是，53.95% 的 AST 相似度主要由共同的上游开源代码（MIT xv6, xv6-k210, K210 SDK, RustSBI）和 RISC-V 硬件规范所贡献。覆盖率指标的倒挂（参考项目 82.73% vs 目标项目 60.8%）清晰地表明，参考项目更像是目标项目所基于的上游代码库的一个子集，而非被抄袭的原创作品。目标项目在进程管理、文件系统、系统调用等多个核心模块展现出显著的功能扩展（如 ext4 集成、新增系统调用）和架构差异（如 `forkret` 的初始化路径），并拥有 207 个独立的 AST 指纹。控方将 DMA 驱动、SPI 驱动等跨板块函数强行归入进程调度进行指控，并将 `proc_record_sleep` 等功能扩展点误判为反查重混淆，其证据链存在根本性缺陷。因此，整体风险较低，不足以判定为抄袭。

------------------------------

