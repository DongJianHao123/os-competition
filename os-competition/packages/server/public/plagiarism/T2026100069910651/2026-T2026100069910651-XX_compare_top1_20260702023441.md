# 🛡️ OS 内核赛自动化代码级架构审计报告

**审计时间**: `2026-07-02 02:34:41`
**参赛作品名称**: `2026-T2026100069910651-XX`
**年份**: `2026`
**学校**: `XX`
**队伍名称**: `T2026100069910651`
**比赛名字**: `OS-Competition`
**比赛子赛道**: `Kernel-Implementation`
**仓库地址**: `https://gitlab.eduxiji.net/educg-group-43501-3132633/T2026100069910651-1260`
**推测开发语言**: `Rust / C (由AST探针自动识别)`
**特征函数量**: `3661`

## ⚔️ 底层代码同源性对比 (AST Homology)
> 本部分由专门的代码审查智能体，对该项目与符合年份条件的历届最高相似度项目进行控制流与底层调用对比。新版报告会先给出确定性的全模块覆盖矩阵，再给出 Agent 的分模块解释，避免只围绕少量抽样函数展开。

### 🏆 Top 1 底层对标: vs `2025-undefined-清华大学`（清华大学 / undefined）

#### 对方作品信息

**对方年份**: `2025`
**对方学校**: `清华大学`
**对方队伍名称**: `undefined`
**对方比赛名字**: `OS-Competition`
**对方比赛子赛道**: `Kernel-Implementation`
**对方仓库地址**: `https://gitlab.eduxiji.net/educg-group-36002-2710490/T202510003995291-2331`

> 双向覆盖率：共同 AST 指纹: 2663；唯一 AST 指纹: 目标项目 2816，参考项目 2820；覆盖率: 目标项目 94.57%，参考项目 94.43%

## 全模块对比总览（确定性证据）

> 总体覆盖率：共同 AST 指纹: 2663；唯一 AST 指纹: 目标项目 2816，参考项目 2820；覆盖率: 目标项目 94.57%，参考项目 94.43%

### 1. 模块覆盖矩阵
| 模块 | 目标函数数 | 参考函数数 | 共同唯一 AST 指纹 | Target 覆盖 | Base 覆盖 | 状态 |
| :--- | ---: | ---: | ---: | ---: | ---: | :--- |
| 进程管理 | 132 | 135 | 113 | 95.76% | 93.39% | 双方存在且有结构重合 |
| 任务调度/异步运行时 | 438 | 415 | 272 | 84.47% | 90.67% | 双方存在且有结构重合 |
| 内存管理 | 480 | 450 | 368 | 89.54% | 96.34% | 双方存在且有结构重合 |
| 中断/异常处理 | 53 | 53 | 47 | 100.0% | 100.0% | 双方存在且有结构重合 |
| 系统调用 | 69 | 109 | 52 | 91.23% | 60.47% | 双方存在且有结构重合 |
| 文件系统 | 1000 | 999 | 647 | 96.57% | 96.86% | 双方存在且有结构重合 |
| 设备驱动 | 134 | 133 | 122 | 99.19% | 100.0% | 双方存在且有结构重合 |
| 网络协议栈 | 66 | 65 | 52 | 92.86% | 94.55% | 双方存在且有结构重合 |
| 文件系统/第三方库 | 647 | 643 | 534 | 97.98% | 98.71% | 双方存在且有结构重合 |
| 网络协议栈/第三方库 | 132 | 130 | 107 | 91.45% | 92.24% | 双方存在且有结构重合 |
| 通用模块/其他 | 510 | 555 | 398 | 99.0% | 91.08% | 双方存在且有结构重合 |

### 2. 分模块证据清单

#### 进程管理
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `api/src/imp/task/exit.rs#sys_exit_impl` | `api/src/imp/task/exit.rs#sys_exit_impl` | 同板块同名 | SignalInfo::new, UserPtr::<Pid>::from, access_user_memory, addr_clear_child_tid.address, addr_clear_child_tid.address().as_usize, addr_clear_child_tid.get ...(+27) | SignalInfo::new, UserPtr::<Pid>::from, addr_clear_child_tid.address, addr_clear_child_tid.address().as_usize, addr_clear_child_tid.get, axtask::exit ...(+27) |
| `src/fs/imp/proc.rs#builder` | `src/fs/imp/proc.rs#builder` | 同板块同名同结构 | CORE_PATTERN.to_string, DynamicDir::builder, FS.add, FS.build, LEASE_BREAK_TIME.to_string, PID_MAX.to_string ...(+20) | CORE_PATTERN.to_string, DynamicDir::builder, FS.add, FS.build, LEASE_BREAK_TIME.to_string, PID_MAX.to_string ...(+20) |
| `api/src/core/fs/imp/proc1/mod.rs#builder` | `api/src/core/fs/imp/proc1/mod.rs#builder` | 同板块同名同结构 | CORE_PATTERN.to_string, DynamicDir::builder, FS.add, FS.build, LEASE_BREAK_TIME.to_string, NodePermission::default ...(+22) | CORE_PATTERN.to_string, DynamicDir::builder, FS.add, FS.build, LEASE_BREAK_TIME.to_string, NodePermission::default ...(+22) |
| `api/src/imp/task/execve.rs#sys_execve_impl` | `api/src/imp/task/execve.rs#sys_execve_impl` | 同板块同名同结构 | Default::default, FD_TABLE.close_on_exec, Ok, Some, addr_space.lock, addr_space.unmap_user_areas ...(+21) | Default::default, FD_TABLE.close_on_exec, Ok, Some, addr_space.lock, addr_space.unmap_user_areas ...(+21) |

#### 任务调度/异步运行时
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `arceos/ulib/axlibc/c/printf.c#format_string_loop` | `arceos/ulib/axlibc/c/printf.c#format_string_loop` | 同板块同名同结构 | ABS_FOR_PRINTING, ADVANCE_IN_FORMAT_STRING, atou_, is_digit_, out_rev_, parse_flags ...(+5) | ABS_FOR_PRINTING, ADVANCE_IN_FORMAT_STRING, atou_, is_digit_, out_rev_, parse_flags ...(+5) |
| `api/src/imp/task/futex.rs#sys_futex` | `api/src/imp/task/futex.rs#sys_futex` | 同板块同名 | Err, Ok, Some, axtask::yield_now, current_process_data, drop ...(+27) | Err, Ok, axtask::yield_now, current_process_data, drop, futex_table
                .entry ...(+27) |
| `arceos/modules/axlog/src/lib.rs#log` | `arceos/modules/axlog/src/lib.rs#log` | 同板块同名同结构 | cfg_if::cfg_if!, record.level, record.line, record.line().unwrap_or, record.metadata, record.target ...(+1) | cfg_if::cfg_if!, record.level, record.line, record.line().unwrap_or, record.metadata, record.target ...(+1) |
| `api/src/imp/fs/fd_ops.rs#sys_fcntl` | `api/src/imp/fs/fd_ops.rs#sys_fcntl` | 同板块同名 | Err, FdFlags::empty, FdFlags::from_bits_truncate, Ok, UserPtr::<flock>::from, UserPtr::<flock>::from(arg as usize).get_as_mut_ref ...(+16) | Err, FdFlags::empty, FdFlags::from_bits_truncate, Ok, current_process_data, current_process_data()
                .resource_limits
                .lock ...(+13) |

#### 内存管理
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `src/syscall.rs#handle_syscall` | `src/syscall.rs#handle_syscall` | 同板块同名 | Err, LinuxError::EINTR.code, Ok, Sysno::new, axtask::yield_now, err.code ...(+172) | Err, Sysno::new, err.code, info!, result.unwrap_or_else, set_trap_frame ...(+151) |
| `api/src/imp/mm/mmap.rs#sys_mmap` | `api/src/imp/mm/mmap.rs#sys_mmap` | 同板块同名 | Arc::new, Err, File::from_fd, MmapFlags::from_bits_truncate, MmapProt::from_bits_truncate, Ok ...(+34) | Err, File::from_fd, MmapFlags::from_bits_truncate, MmapProt::from_bits_truncate, Ok, PhysAddr::from ...(+34) |
| `syscall_trace/src/lib.rs#syscall_trace` | `syscall_trace/src/lib.rs#syscall_trace` | 同板块同名同结构 | Some, arg_names
        .iter, arg_names
        .iter()
        .map, arg_names
        .iter()
        .map(|name| format!("{} = {{}}", name.to_string()))
        .collect::<Vec<_>>, arg_names
        .iter()
        .map(|name| format!("{} = {{}}", name.to_string()))
        .collect::<Vec<_>>()
        .join, args.args.first ...(+21) | Some, arg_names
        .iter, arg_names
        .iter()
        .map, arg_names
        .iter()
        .map(|name| format!("{} = {{}}", name.to_string()))
        .collect::<Vec<_>>, arg_names
        .iter()
        .map(|name| format!("{} = {{}}", name.to_string()))
        .collect::<Vec<_>>()
        .join, args.args.first ...(+21) |
| `arceos/ulib/axlibc/src/strftime.rs#strftime_inner` | `arceos/ulib/axlibc/src/strftime.rs#strftime_inner` | 同板块同名同结构 | CountingWriter::new, format.offset, inner_strftime, w! | CountingWriter::new, format.offset, inner_strftime, w! |

#### 中断/异常处理
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `arceos/ulib/axlibc/c/pow.c#exp_inline` | `arceos/ulib/axlibc/c/pow.c#exp_inline` | 同板块同名同结构 | __math_oflow, __math_uflow, asdouble, asuint64, converttoint, eval_as_double ...(+4) | __math_oflow, __math_uflow, asdouble, asuint64, converttoint, eval_as_double ...(+4) |
| `arceos/modules/axhal/src/arch/aarch64/trap.rs#handle_sync_exception` | `arceos/modules/axhal/src/arch/aarch64/trap.rs#handle_sync_exception` | 同板块同名同结构 | ESR_EL1.extract, crate::trap::handle_syscall, crate::trap::post_trap_callback, debug!, esr.read, esr.read_as_enum ...(+6) | ESR_EL1.extract, crate::trap::handle_syscall, crate::trap::post_trap_callback, debug!, esr.read, esr.read_as_enum ...(+6) |
| `arceos/modules/axhal/src/platform/riscv64_qemu_virt/irq.rs#register_handler` | `arceos/modules/axhal/src/platform/riscv64_qemu_virt/irq.rs#register_handler` | 同板块同名同结构 | with_cause! | with_cause! |
| `arceos/modules/axhal/src/platform/loongarch64_qemu_virt/irq.rs#set_enable` | `arceos/modules/axhal/src/platform/loongarch64_qemu_virt/irq.rs#set_enable` | 同板块同名同结构 | ecfg::read, ecfg::read().lie, ecfg::set_lie | ecfg::read, ecfg::read().lie, ecfg::set_lie |

#### 系统调用
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `api/src/imp/net/socket.rs#sys_getaddrinfo` | `api/src/imp/net/socket.rs#sys_getaddrinfo` | 同板块同名同结构 | Err, Ok, SocketAddrV4::new, SocketAddrV4::new(ip, port).into, Vec::with_capacity, axnet::dns_query ...(+21) | Err, Ok, SocketAddrV4::new, SocketAddrV4::new(ip, port).into, Vec::with_capacity, axnet::dns_query ...(+21) |
| `api/src/interface/mm/shm.rs#sys_shmget` | `api/src/interface/mm/shm.rs#sys_shmget` | 同板块同名同结构 | Err, Ok, SHARED_MEMORY_MANAGER.create, SHARED_MEMORY_MANAGER.get, SHARED_MEMORY_MANAGER.get(key).is_none, SHARED_MEMORY_MANAGER.mem_map.lock ...(+4) | Err, Ok, SHARED_MEMORY_MANAGER.create, SHARED_MEMORY_MANAGER.get, SHARED_MEMORY_MANAGER.get(key).is_none, SHARED_MEMORY_MANAGER.mem_map.lock ...(+4) |
| `api/src/imp/net/socket.rs#sys_recvfrom` | `api/src/imp/net/socket.rs#sys_recvfrom` | 同板块同名 | Err, Ok, Socket::from_fd, addrlen.is_null, buf_ptr.is_null, core::slice::from_raw_parts_mut ...(+4) | Err, Ok, Socket::from_fd, addrlen.is_null, buf_ptr.is_null, core::slice::from_raw_parts_mut ...(+4) |
| `arceos/api/arceos_posix_api/src/imp/time.rs#sys_clock_gettime` | `arceos/api/arceos_posix_api/src/imp/time.rs#sys_clock_gettime` | 同板块同名同结构 | syscall_body! | syscall_body! |

#### 文件系统
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `arceos/ulib/axlibc/c/glob.c#do_glob` | `arceos/ulib/axlibc/c/glob.c#do_glob` | 同板块同名同结构 | S_ISDIR, append, closedir, do_glob, errfunc, fnmatch ...(+7) | S_ISDIR, append, closedir, do_glob, errfunc, fnmatch ...(+7) |
| `arceos/ulib/axlibc/src/setjmp.rs#longjmp` | `arceos/ulib/axlibc/src/setjmp.rs#longjmp` | 同板块同名同结构 | core::arch::naked_asm! | core::arch::naked_asm! |
| `arceos/ulib/axlibc/src/setjmp.rs#setjmp` | `arceos/ulib/axlibc/src/setjmp.rs#setjmp` | 同板块同名同结构 | core::arch::naked_asm! | core::arch::naked_asm! |
| `api/src/interface/fs/poll.rs#sys_select_` | `api/src/interface/fs/poll.rs#sys_select_` | 同板块同名同结构 | (0..n_fds)
        .map, (0..n_fds)
        .map(|i| {
            let mut events = PollFlags::empty();
            if read_fds_slice.get_bit(i) {
                events |= PollFlags::POLLIN;
            }
            if write_fds_slice.get_bit(i) {
                events |= PollFlags::POLLOUT;
            }
            if except_fds_slice.get_bit(i) {
                events |= PollFlags::POLLPRI;
            }
            PollEntry {
                fd: i as i32,
                events,
                results: PollFlags::empty(),
            }
        })
        .filter, (0..n_fds)
        .map(|i| {
            let mut events = PollFlags::empty();
            if read_fds_slice.get_bit(i) {
                events |= PollFlags::POLLIN;
            }
            if write_fds_slice.get_bit(i) {
                events |= PollFlags::POLLOUT;
            }
            if except_fds_slice.get_bit(i) {
                events |= PollFlags::POLLPRI;
            }
            PollEntry {
                fd: i as i32,
                events,
                results: PollFlags::empty(),
            }
        })
        .filter(|entry| !entry.events.is_empty())
        .collect, Ok, PollFlags::empty, core::slice::from_raw_parts_mut ...(+16) | (0..n_fds)
        .map, (0..n_fds)
        .map(|i| {
            let mut events = PollFlags::empty();
            if read_fds_slice.get_bit(i) {
                events |= PollFlags::POLLIN;
            }
            if write_fds_slice.get_bit(i) {
                events |= PollFlags::POLLOUT;
            }
            if except_fds_slice.get_bit(i) {
                events |= PollFlags::POLLPRI;
            }
            PollEntry {
                fd: i as i32,
                events,
                results: PollFlags::empty(),
            }
        })
        .filter, (0..n_fds)
        .map(|i| {
            let mut events = PollFlags::empty();
            if read_fds_slice.get_bit(i) {
                events |= PollFlags::POLLIN;
            }
            if write_fds_slice.get_bit(i) {
                events |= PollFlags::POLLOUT;
            }
            if except_fds_slice.get_bit(i) {
                events |= PollFlags::POLLPRI;
            }
            PollEntry {
                fd: i as i32,
                events,
                results: PollFlags::empty(),
            }
        })
        .filter(|entry| !entry.events.is_empty())
        .collect, Ok, PollFlags::empty, core::slice::from_raw_parts_mut ...(+16) |

#### 设备驱动
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `arceos/tools/bwbench_client/src/main.rs#transmit_benchmark` | `arceos/tools/bwbench_client/src/main.rs#transmit_benchmark` | 同板块同名同结构 | Local::now, NetDevice::new, NetDevice::new(interface).unwrap, current_time.signed_duration_since, current_time.signed_duration_since(past_time).num_seconds, dev.send ...(+2) | Local::now, NetDevice::new, NetDevice::new(interface).unwrap, current_time.signed_duration_since, current_time.signed_duration_since(past_time).num_seconds, dev.send ...(+2) |
| `arceos/tools/bwbench_client/src/device.rs#bind_interface` | `arceos/tools/bwbench_client/src/device.rs#bind_interface` | 同板块同名同结构 | ETH_P_ALL.to_be, Err, Ok, libc::bind, self.ifreq.ioctl, std::io::Error::last_os_error ...(+2) | ETH_P_ALL.to_be, Err, Ok, libc::bind, self.ifreq.ioctl, std::io::Error::last_os_error ...(+2) |
| `arceos/modules/axdriver/src/lib.rs#init_drivers` | `arceos/modules/axdriver/src/lib.rs#init_drivers` | 同板块同名同结构 | AllDevices::default, all_devs.block.iter, all_devs.block.iter().enumerate, all_devs.display.iter, all_devs.display.iter().enumerate, all_devs.net.iter ...(+5) | AllDevices::default, all_devs.block.iter, all_devs.block.iter().enumerate, all_devs.display.iter, all_devs.display.iter().enumerate, all_devs.net.iter ...(+5) |
| `arceos/modules/axdriver/src/virtio.rs#probe_pci` | `arceos/modules/axdriver/src/virtio.rs#probe_pci` | 同板块同名同结构 | D::try_new, Some, axdriver_virtio::probe_pci_device::<VirtIoHalImpl>, warn! | D::try_new, Some, axdriver_virtio::probe_pci_device::<VirtIoHalImpl>, warn! |

#### 网络协议栈
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `api/src/imp/net/socket.rs#setsockopt` | `api/src/imp/net/socket.rs#setsockopt` | 同板块同名 | Err, Ok, SocketOptionLevel::try_from, error!, i32::from_ne_bytes, int_opt ...(+10) | Ok, SocketOptionLevel::try_from, error!, udpsocket.lock, unimplemented!, warn! |
| `arceos/ulib/axlibc/c/network.c#inet_pton` | `arceos/ulib/axlibc/c/network.c#inet_pton` | 同板块同名同结构 | hexval, inet_pton, isdigit, memmove | hexval, inet_pton, isdigit, memmove |
| `api/src/imp/net/socket.rs#getsockopt` | `arceos/ulib/axlibc/c/socket.c#getsockopt` | 同板块同名 | Err, Ok, optlen.is_null, optval.is_null, size_of::<i32>, tcpsocket.lock ...(+5) | unimplemented |
| `api/src/imp/net/socket.rs#sendto` | `api/src/imp/net/socket.rs#sendto` | 同板块同名 | Err, IpAddr::V4, SocketAddr::new, udpsocket
                    .send_to, udpsocket
                    .send_to(buf, addr)
                    .map_err, udpsocket.bind ...(+3) | Err, IpAddr::V4, Ipv4Addr::new, SocketAddr::new, udpsocket
                    .bind, udpsocket
                    .bind(SocketAddr::new(IpAddr::V4(Ipv4Addr::new(127, 0, 0, 1)), 0))
                    .map_err ...(+3) |

#### 文件系统/第三方库
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `modules/lwext4_rust/c/lwext4/src/ext4_balloc.c#ext4_balloc_alloc_block` | `modules/lwext4_rust/c/lwext4/src/ext4_balloc.c#ext4_balloc_alloc_block` | 同板块同名同结构 | ext4_balloc_get_bgid_of_block, ext4_balloc_get_block_of_bgid, ext4_balloc_set_bitmap_csum, ext4_balloc_verify_bitmap_csum, ext4_bg_get_block_bitmap, ext4_bg_get_free_blocks_count ...(+19) | ext4_balloc_get_bgid_of_block, ext4_balloc_get_block_of_bgid, ext4_balloc_set_bitmap_csum, ext4_balloc_verify_bitmap_csum, ext4_bg_get_block_bitmap, ext4_bg_get_free_blocks_count ...(+19) |
| `modules/lwext4_rust/c/lwext4/src/ext4_journal.c#jbd_journal_prepare` | `modules/lwext4_rust/c/lwext4/src/ext4_journal.c#jbd_journal_prepare` | 同板块同名同结构 | EXT4_BLOCK_ZERO, RB_FIND, TAILQ_FOREACH_REVERSE_SAFE, TAILQ_FOREACH_SAFE, TAILQ_REMOVE, ext4_bcache_clear_dirty ...(+17) | EXT4_BLOCK_ZERO, RB_FIND, TAILQ_FOREACH_REVERSE_SAFE, TAILQ_FOREACH_SAFE, TAILQ_REMOVE, ext4_bcache_clear_dirty ...(+17) |
| `modules/lwext4_rust/c/lwext4/src/ext4.c#ext4_dir_rm` | `modules/lwext4_rust/c/lwext4/src/ext4.c#ext4_dir_rm` | 同板块同名同结构 | EXT4_MP_LOCK, EXT4_MP_UNLOCK, ext4_block_cache_write_back, ext4_dir_en_get_inode, ext4_dir_iterator_fini, ext4_dir_iterator_init ...(+19) | EXT4_MP_LOCK, EXT4_MP_UNLOCK, ext4_block_cache_write_back, ext4_dir_en_get_inode, ext4_dir_iterator_fini, ext4_dir_iterator_init ...(+19) |
| `modules/lwext4_rust/c/lwext4/src/ext4_dir_idx.c#ext4_dir_dx_split_index` | `modules/lwext4_rust/c/lwext4/src/ext4_dir_idx.c#ext4_dir_dx_split_index` | 同板块同名同结构 | ext4_block_set, ext4_dir_dx_climit_get_count, ext4_dir_dx_climit_get_limit, ext4_dir_dx_climit_set_count, ext4_dir_dx_climit_set_limit, ext4_dir_dx_entry_get_hash ...(+10) | ext4_block_set, ext4_dir_dx_climit_get_count, ext4_dir_dx_climit_get_limit, ext4_dir_dx_climit_set_count, ext4_dir_dx_climit_set_limit, ext4_dir_dx_entry_get_hash ...(+10) |

#### 网络协议栈/第三方库
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `arceos/modules/axnet/src/smoltcp_impl/tcp.rs#connect` | `arceos/modules/axnet/src/smoltcp_impl/tcp.rs#connect` | 同板块同名同结构 | Err, Ok, Ok::<(IpEndpoint, IpEndpoint), AxError>, SOCKET_SET
                .with_socket_mut::<tcp::Socket, _, _>, SOCKET_SET.add, SocketSetWrapper::new_tcp_socket ...(+32) | Err, Ok, Ok::<(IpEndpoint, IpEndpoint), AxError>, SOCKET_SET
                .with_socket_mut::<tcp::Socket, _, _>, SOCKET_SET.add, SocketSetWrapper::new_tcp_socket ...(+32) |
| `arceos/modules/axnet/src/smoltcp_impl/dns.rs#query` | `arceos/modules/axnet/src/smoltcp_impl/dns.rs#query` | 同板块同名同结构 | Err, Ok, SOCKET_SET
            .with_socket_mut::<dns::Socket, _, _>, SOCKET_SET
            .with_socket_mut::<dns::Socket, _, _>(handle, |socket| {
                socket.start_query(iface.lock().context(), name, query_type)
            })
            .map_err, SOCKET_SET.poll_interfaces, SOCKET_SET.with_socket_mut::<dns::Socket, _, _> ...(+12) | Err, Ok, SOCKET_SET
            .with_socket_mut::<dns::Socket, _, _>, SOCKET_SET
            .with_socket_mut::<dns::Socket, _, _>(handle, |socket| {
                socket.start_query(iface.lock().context(), name, query_type)
            })
            .map_err, SOCKET_SET.poll_interfaces, SOCKET_SET.with_socket_mut::<dns::Socket, _, _> ...(+12) |
| `arceos/modules/axnet/src/smoltcp_impl/tcp.rs#recv_timeout` | `arceos/modules/axnet/src/smoltcp_impl/tcp.rs#recv_timeout` | 同板块同名同结构 | Err, Ok, SOCKET_SET.with_socket_mut::<tcp::Socket, _, _>, ax_err!, ax_err_type!, current_ticks ...(+11) | Err, Ok, SOCKET_SET.with_socket_mut::<tcp::Socket, _, _>, ax_err!, ax_err_type!, current_ticks ...(+11) |
| `arceos/modules/axnet/src/smoltcp_impl/bench.rs#bench_transmit_bandwidth` | `arceos/modules/axnet/src/smoltcp_impl/bench.rs#bench_transmit_bandwidth` | 同板块同名同结构 | (current_time - past_time).secs, AxNetTxToken::consume, InterfaceWrapper::current_time, info!, self.transmit, tx_buf[0..12].fill ...(+2) | (current_time - past_time).secs, AxNetTxToken::consume, InterfaceWrapper::current_time, info!, self.transmit, tx_buf[0..12].fill ...(+2) |

#### 通用模块/其他
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `arceos/ulib/axlibc/c/printf.c#print_exponential_number` | `arceos/ulib/axlibc/c/printf.c#print_exponential_number` | 同板块同名同结构 | ABS_FOR_PRINTING, PRINTF_ABS, bastardized_floor, get_components, get_normalized_components, get_sign_bit ...(+5) | ABS_FOR_PRINTING, PRINTF_ABS, bastardized_floor, get_components, get_normalized_components, get_sign_bit ...(+5) |
| `arceos/ulib/axlibc/c/pow.c#pow` | `arceos/ulib/axlibc/c/pow.c#pow` | 同板块同名同结构 | __builtin_fma, __math_invalid, __math_oflow, __math_uflow, asdouble, asuint64 ...(+8) | __builtin_fma, __math_invalid, __math_oflow, __math_uflow, asdouble, asuint64 ...(+8) |
| `arceos/ulib/axlibc/c/string.c#twoway_strstr` | `arceos/ulib/axlibc/c/string.c#twoway_strstr` | 同板块同名同结构 | BITOP, MAX, memchr, memcmp | BITOP, MAX, memchr, memcmp |
| `arceos/ulib/axlibc/c/printf.c#print_broken_up_decimal` | `arceos/ulib/axlibc/c/printf.c#print_broken_up_decimal` | 同板块同名同结构 | out_rev_ | out_rev_ |


#### 1. 宏观判定
- **定性结论**: 局部微调 (相似度: 89.57%，共同 AST 指纹: 2663；唯一 AST 指纹: 目标项目 2816，参考项目 2820；覆盖率: 目标项目 94.57%，参考项目 94.43%)

#### 2. 全模块覆盖矩阵解读
- **进程管理**: 双方均存在。共同 AST 指纹 113，覆盖率均超 93%。证据强度**强**，存在大量同名同结构函数及底层逻辑一致的同板块函数。
- **任务调度/异步运行时**: 双方均存在。共同 AST 指纹 272，覆盖率分别为 84.47% 和 90.67%。证据强度**强**，核心调度与等待队列机制高度重合。
- **内存管理**: 双方均存在。共同 AST 指纹 368，覆盖率分别为 89.54% 和 96.34%。证据强度**中**，存在同名同结构函数，但部分证据来自系统调用入口，属于标准接口。
- **中断/异常处理**: 双方均存在。共同 AST 指纹 47，覆盖率均为 100.0%。证据强度**弱**，证据多为平台相关或数学库函数，属于硬件规范或标准数学实现。
- **系统调用**: 双方均存在。共同 AST 指纹 52，覆盖率分别为 91.23% 和 60.47%。证据强度**中**，存在同名同结构函数，但系统调用接口本身由 POSIX 标准定义。
- **文件系统**: 双方均存在。共同 AST 指纹 647，覆盖率均超 96%。证据强度**中**，大量证据来自 `axlibc` 等公共库，核心文件系统逻辑重合度高。
- **设备驱动**: 双方均存在。共同 AST 指纹 122，覆盖率均超 99%。证据强度**弱**，证据多为 `ArceOS` 驱动框架的标准初始化或探测流程。
- **网络协议栈**: 双方均存在。共同 AST 指纹 52，覆盖率均超 92%。证据强度**中**，存在同名同结构函数，但部分证据来自 `smoltcp` 等第三方库的封装。
- **第三方库/公共依赖**: 双方均存在。共同 AST 指纹 534/107，覆盖率均超 91%。证据强度**未形成**，这些模块（如 `lwext4_rust`, `axlibc`）是明确的上游依赖，其高度一致是预期结果，不应作为同源性证据。

#### 3. 底层 AST 关键证据指纹表
| 所属板块 | 目标项目路径 | 参考项目路径 | 证据类型 | 技术说明 |
| :--- | :--- | :--- | :--- | :--- |
| 进程管理 | `api/src/imp/task/exit.rs#sys_exit_impl` | `api/src/imp/task/exit.rs#sys_exit_impl` | 同板块同名 | 进程退出系统调用的核心实现，调用链高度一致，但目标项目存在 `access_user_memory` 这一独立调用。 |
| 进程管理 | `process/src/process.rs#exit` | `process/src/process.rs#exit` | 同板块底层逻辑一致 | 进程退出的内部状态机逻辑，孤儿进程接管 (`reaper.children.lock`) 与资源释放 (`PROCESS_TABLE.lock().remove`) 的特定锁顺序完全一致。 |
| 进程管理 | `process/src/process_group.rs#create_process_group` | `process/src/process_group.rs#create_process_group` | 同板块底层逻辑一致 | 进程组创建逻辑，`contains_key` 检查后 `insert` 并关联会话的防御式编程链路完全一致。 |
| 任务调度/异步运行时 | `arceos/modules/axtask/src/wait_queue.rs#wait_timeout_until` | `arceos/modules/axtask/src/wait_queue.rs#wait_timeout_until` | 同板块底层逻辑一致 | 带超时的等待队列核心阻塞逻辑，`current_run_queue::<NoPreemptIrqSave>` 等特定泛型与关中断保护调用链完全一致。 |
| 任务调度/异步运行时 | `arceos/modules/axtask/src/run_queue.rs#exit_current` | `arceos/modules/axtask/src/run_queue.rs#exit_current` | 同板块底层逻辑一致 | 任务退出时的资源回收逻辑，`EXITED_TASKS` 与 `WAIT_FOR_EXIT` 等全局 PerCPU 变量的操作序列完全一致。 |
| 内存管理 | `api/src/imp/mm/mmap.rs#sys_mmap` | `api/src/imp/mm/mmap.rs#sys_mmap` | 同板块同名 | `mmap` 系统调用实现，`MmapFlags`、`MmapProt` 等标志位处理及文件/匿名映射的底层逻辑高度一致。 |
| 系统调用 | `api/src/imp/net/socket.rs#sys_getaddrinfo` | `api/src/imp/net/socket.rs#sys_getaddrinfo` | 同板块同名同结构 | `getaddrinfo` 网络系统调用实现，`SocketAddrV4::new` 及 `axnet::dns_query` 的调用方式完全一致。 |
| 文件系统 | `api/src/interface/fs/poll.rs#sys_select_` | `api/src/interface/fs/poll.rs#sys_select_` | 同板块同名同结构 | `select` 系统调用的文件系统接口实现，`PollEntry` 的构造与过滤逻辑完全一致。 |
| 第三方库/公共依赖 | `modules/lwext4_rust/c/lwext4/src/ext4_balloc.c#ext4_balloc_alloc_block` | `modules/lwext4_rust/c/lwext4/src/ext4_balloc.c#ext4_balloc_alloc_block` | 同板块同名同结构 | 第三方库 `lwext4` 的 ext4 块分配函数，调用链完全一致，属于公共依赖。 |
| 第三方库/公共依赖 | `arceos/modules/axnet/src/smoltcp_impl/tcp.rs#connect` | `arceos/modules/axnet/src/smoltcp_impl/tcp.rs#connect` | 同板块同名同结构 | 第三方库 `smoltcp` 的 TCP 连接封装，`SOCKET_SET` 的操作逻辑完全一致，属于公共依赖。 |

#### 4. 核心代码调用流对比剖析
* **【进程管理/进程退出与回收】**
* **底层技术析理**: 双方在进程退出 (`exit`) 的内部实现上，均遵循了完全相同的状态机模型与锁顺序。具体表现为：获取子进程收割者 (`self.get_child_reaper`) -> 锁定收割者的子进程列表 (`reaper.children.lock`) -> 将自身插入孤儿列表 (`reaper_children.insert`) -> 锁定全局进程表 (`PROCESS_TABLE.lock`) -> 移除自身 (`remove`)。这种涉及多层 `Mutex` 嵌套的特定资源清理顺序，是高度敏感的底层逻辑指纹，难以用“遵循同一规范”解释。
* **代码溯源链接**:
  - 目标项目: `process/src/process.rs#exit`
  - 参考项目: `process/src/process.rs#exit`

* **【任务调度/异步运行时/等待队列机制】**
* **底层技术析理**: 在带超时的等待队列阻塞逻辑 (`wait_timeout_until`) 中，双方均采用了完全相同的同步策略与调度交互模式。调用流为：获取当前任务 (`crate::current`) -> 设置闹钟唤醒 (`crate::timers::set_alarm_wakeup`) -> 获取特定泛型参数的运行队列 (`current_run_queue::<NoPreemptIrqSave>`) -> 执行阻塞重调度 (`rq.blocked_resched`)。`NoPreemptIrqSave` 这一特定策略类型的使用，表明双方在关中断与防抢占的底层调度细节上实现了完全一致。
* **代码溯源链接**:
  - 目标项目: `arceos/modules/axtask/src/wait_queue.rs#wait_timeout_until`
  - 参考项目: `arceos/modules/axtask/src/wait_queue.rs#wait_timeout_until`

* **【进程管理/进程组与会话管理】**
* **底层技术析理**: 在创建进程组 (`create_process_group`) 时，双方均采用了相同的防御式编程与状态关联模型。调用流为：锁定全局进程组表 (`PROCESS_GROUP_TABLE.lock`) -> 检查键值是否存在 (`process_group_table.contains_key`) -> 若不存在则插入新组 (`process_group_table.insert`) -> 将新组关联到会话 (`session.add_process_group`)。这种“检查-插入-关联”的特定组合，以及对 `panic!` 的使用位置，构成了清晰的逻辑指纹。
* **代码溯源链接**:
  - 目标项目: `process/src/process_group.rs#create_process_group`
  - 参考项目: `process/src/process_group.rs#create_process_group`

#### 5. 单方模块与降权项
- **第三方库/公共依赖**: `modules/lwext4_rust`、`arceos/ulib/axlibc`、`arceos/modules/axnet/src/smoltcp_impl` 等路径下的代码属于明确的上游第三方库或公共框架依赖。双方在这些模块上的 AST 高度一致是引用同一代码源的必然结果，不能作为项目间抄袭的证据，应予以降权或排除。
- **中断/异常处理**: 该板块的证据多为平台特定的硬件处理（如 `handle_sync_exception`）或标准 C 数学库（如 `pow.c`），其实现由硬件架构或 IEEE 标准严格约束，独创性空间极小，证据强度弱。
- **设备驱动**: 该板块的证据多为 `ArceOS` 驱动框架的标准初始化流程（如 `init_drivers`, `probe_pci`），属于框架规定的模板代码，证据强度弱。
- **泛型函数与标准宏**: `println!`, `debug!`, `assert!`, `Ok`, `Err`, `Arc::new` 等 Rust 标准库宏与构造，在任何 Rust 项目中均高频出现，不能作为同源性的强证据。

#### 6. 保守结论
本次审计显示，目标项目与参考项目在 AST 层面存在极高相似度（89.57%），但这主要源于双方共享庞大的 `ArceOS` 内核框架、第三方库（`lwext4`, `smoltcp`）及 POSIX 标准接口。在排除这些公共基座后，双方在【进程管理】和【任务调度】等核心板块的底层实现上，仍存在大量无法用“领域强制解”解释的特定逻辑指纹，如孤儿进程接管时的多层锁顺序、等待队列中特定调度策略类型的使用等。然而，目标项目在 `sys_exit_impl` 等函数中也展现出独立的修改（如 `access_user_memory` 调用）。因此，整体定性为在高度同源的基座代码上进行的**局部微调**，而非完全独立的原创开发。控方“像素级抄袭”的指控因未充分剥离公共依赖而显得过重，但辩方“完全独立开发”的论点亦无法解释核心逻辑指纹的高度重合。

------------------------------

