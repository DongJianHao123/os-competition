# 🛡️ OS 内核赛自动化代码级架构审计报告

**审计时间**: `2026-07-02 02:34:41`
**参赛作品名称**: `2026-T2026100069910651-XX`
**年份**: `2026`
**学校**: `XX`
**队伍名称**: `T2026100069910651`
**比赛名字**: `OS-Competition`
**比赛子赛道**: `Kernel-Implementation`
**仓库地址**: `https://gitlab.eduxiji.net/educg-group-43501-3132633/T2026100069910651-1260`
**推测开发语言**: `Rust / C (由AST探针自动识别)`
**特征函数量**: `3661`

## ⚔️ 底层代码同源性对比 (AST Homology)
> 本部分由专门的代码审查智能体，对该项目与符合年份条件的历届最高相似度项目进行控制流与底层调用对比。新版报告会先给出确定性的全模块覆盖矩阵，再给出 Agent 的分模块解释，避免只围绕少量抽样函数展开。

### 🏆 Top 3 底层对标: vs `2025-九莲宝灯一向听-天津理工大学`（天津理工大学 / 九莲宝灯一向听）

#### 对方作品信息

**对方年份**: `2025`
**对方学校**: `天津理工大学`
**对方队伍名称**: `九莲宝灯一向听`
**对方比赛名字**: `OS-Competition`
**对方比赛子赛道**: `Kernel-Implementation`
**对方仓库地址**: `https://gitlab.eduxiji.net/educg-group-36002-2710490/T202510060995409-3585`

> 双向覆盖率：共同 AST 指纹: 1670；唯一 AST 指纹: 目标项目 2816，参考项目 2610；覆盖率: 目标项目 59.3%，参考项目 63.98%

## 全模块对比总览（确定性证据）

> 总体覆盖率：共同 AST 指纹: 1670；唯一 AST 指纹: 目标项目 2816，参考项目 2610；覆盖率: 目标项目 59.3%，参考项目 63.98%

### 1. 模块覆盖矩阵
| 模块 | 目标函数数 | 参考函数数 | 共同唯一 AST 指纹 | Target 覆盖 | Base 覆盖 | 状态 |
| :--- | ---: | ---: | ---: | ---: | ---: | :--- |
| 进程管理 | 132 | 79 | 31 | 26.27% | 42.47% | 双方存在且有结构重合 |
| 任务调度/异步运行时 | 438 | 385 | 189 | 58.7% | 68.73% | 双方存在且有结构重合 |
| 内存管理 | 480 | 301 | 199 | 48.42% | 73.16% | 双方存在且有结构重合 |
| 中断/异常处理 | 53 | 60 | 43 | 91.49% | 84.31% | 双方存在且有结构重合 |
| 系统调用 | 69 | 130 | 35 | 61.4% | 32.71% | 双方存在且有结构重合 |
| 文件系统 | 1000 | 616 | 153 | 22.84% | 33.7% | 双方存在且有结构重合 |
| 设备驱动 | 134 | 156 | 115 | 93.5% | 80.99% | 双方存在且有结构重合 |
| 网络协议栈 | 66 | 61 | 50 | 89.29% | 98.04% | 双方存在且有结构重合 |
| 文件系统/第三方库 | 647 | 577 | 414 | 75.96% | 85.89% | 双方存在且有结构重合 |
| 网络协议栈/第三方库 | 132 | 130 | 107 | 91.45% | 92.24% | 双方存在且有结构重合 |
| 第三方库 | 0 | 209 | 0 | 0.0% | 0.0% | 仅参考项目体现 |
| 通用模块/其他 | 510 | 630 | 362 | 90.05% | 75.26% | 双方存在且有结构重合 |

### 2. 分模块证据清单

#### 进程管理
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `arceos/modules/axtask/src/run_queue.rs#exit_current` | `arceos/modules/axtask/src/run_queue.rs#exit_current` | 同板块同名同结构 | EXITED_TASKS.current_ref_mut_raw, EXITED_TASKS.current_ref_mut_raw().clear, EXITED_TASKS.current_ref_mut_raw().push_back, WAIT_FOR_EXIT.current_ref_mut_raw, WAIT_FOR_EXIT.current_ref_mut_raw().notify_one, assert! ...(+8) | EXITED_TASKS.current_ref_mut_raw, EXITED_TASKS.current_ref_mut_raw().clear, EXITED_TASKS.current_ref_mut_raw().push_back, WAIT_FOR_EXIT.current_ref_mut_raw, WAIT_FOR_EXIT.current_ref_mut_raw().notify_one, assert! ...(+8) |
| `arceos/modules/axtask/src/wait_queue.rs#cancel_events` | `arceos/modules/axtask/src/wait_queue.rs#cancel_events` | 同板块同名同结构 | curr.in_wait_queue, curr.ptr_eq, curr.set_in_wait_queue, curr.timer_ticket_expired, self.queue.lock, self.queue.lock().retain | curr.in_wait_queue, curr.ptr_eq, curr.set_in_wait_queue, curr.timer_ticket_expired, self.queue.lock, self.queue.lock().retain |
| `arceos/modules/axtask/src/wait_queue.rs#wait_timeout_until` | `arceos/modules/axtask/src/wait_queue.rs#wait_timeout_until` | 同板块同名同结构 | axhal::time::wall_time, condition, crate::current, crate::timers::set_alarm_wakeup, curr.clone, current_run_queue::<NoPreemptIrqSave> ...(+4) | axhal::time::wall_time, condition, crate::current, crate::timers::set_alarm_wakeup, curr.clone, current_run_queue::<NoPreemptIrqSave> ...(+4) |
| `process/src/process.rs#exit` | `arceos/modules/axtask/src/api.rs#exit` | 同板块同名 | Arc::downgrade, assert!, child.parent.lock, core::mem::take, reaper.children.lock, reaper_children.insert ...(+7) | current_run_queue::<NoPreemptIrqSave>, current_run_queue::<NoPreemptIrqSave>().exit_current |

#### 任务调度/异步运行时
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `arceos/ulib/axlibc/c/printf.c#format_string_loop` | `arceos/ulib/axlibc/c/printf.c#format_string_loop` | 同板块同名同结构 | ABS_FOR_PRINTING, ADVANCE_IN_FORMAT_STRING, atou_, is_digit_, out_rev_, parse_flags ...(+5) | ABS_FOR_PRINTING, ADVANCE_IN_FORMAT_STRING, atou_, is_digit_, out_rev_, parse_flags ...(+5) |
| `api/src/imp/task/futex.rs#sys_futex` | `api/src/imp/futex.rs#sys_futex` | 同板块同名 | Err, Ok, Some, axtask::yield_now, current_process_data, drop ...(+27) | Err, Ok, axtask::yield_now, curr.task_ext, curr.task_ext().process_data, current ...(+16) |
| `arceos/modules/axlog/src/lib.rs#log` | `arceos/modules/axlog/src/lib.rs#log` | 同板块同名同结构 | cfg_if::cfg_if!, record.level, record.line, record.line().unwrap_or, record.metadata, record.target ...(+1) | cfg_if::cfg_if!, record.level, record.line, record.line().unwrap_or, record.metadata, record.target ...(+1) |
| `arceos/modules/axtask/src/run_queue.rs#switch_to` | `arceos/modules/axtask/src/run_queue.rs#switch_to` | 同板块同名同结构 | (*prev_ctx_ptr).switch_to, Arc::downgrade, CurrentTask::set_current, PREV_TASK.current_ref_mut_raw, assert!, clear_prev_task_on_cpu ...(+8) | (*prev_ctx_ptr).switch_to, Arc::downgrade, CurrentTask::set_current, PREV_TASK.current_ref_mut_raw, assert!, clear_prev_task_on_cpu ...(+8) |

#### 内存管理
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `api/src/imp/mm/mmap.rs#sys_mmap` | `api/src/imp/mm/mmap.rs#sys_mmap` | 同板块同名 | Arc::new, Err, File::from_fd, MmapFlags::from_bits_truncate, MmapProt::from_bits_truncate, Ok ...(+34) | Err, File::from_fd, MmapFlags::from_bits_truncate, MmapProt::from_bits_truncate, Ok, VirtAddr::from ...(+27) |
| `arceos/ulib/axlibc/src/strftime.rs#strftime_inner` | `arceos/ulib/axlibc/src/strftime.rs#strftime_inner` | 同板块同名同结构 | CountingWriter::new, format.offset, inner_strftime, w! | CountingWriter::new, format.offset, inner_strftime, w! |
| `arceos/ulib/axlibc/src/strftime.rs#inner_strftime` | `arceos/ulib/axlibc/src/strftime.rs#inner_strftime` | 同板块同名同结构 | format.offset, w! | format.offset, w! |
| `core/src/mm.rs#load_user_app` | `core/src/mm.rs#load_user_app` | 同板块同名 | CStr::from_bytes_with_nul, CStr::from_bytes_with_nul(interp)
            .map_err, CStr::from_bytes_with_nul(interp)
            .map_err(|_| AxError::InvalidData)?
            .to_str, CStr::from_bytes_with_nul(interp)
            .map_err(|_| AxError::InvalidData)?
            .to_str()
            .map_err, CStr::from_bytes_with_nul(interp)
            .map_err(|_| AxError::InvalidData)?
            .to_str()
            .map_err(|_| AxError::InvalidData)?
            .to_string, ElfFile::new ...(+34) | CStr::from_bytes_with_nul, CStr::from_bytes_with_nul(interp)
                .map_err, CStr::from_bytes_with_nul(interp)
                .map_err(|_| AxError::InvalidData)?
                .to_str, CStr::from_bytes_with_nul(interp)
                .map_err(|_| AxError::InvalidData)?
                .to_str()
                .map_err, ElfFile::new, ElfFile::new(&file_data).map_err ...(+41) |

#### 中断/异常处理
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `arceos/ulib/axlibc/c/pow.c#exp_inline` | `arceos/ulib/axlibc/c/pow.c#exp_inline` | 同板块同名同结构 | __math_oflow, __math_uflow, asdouble, asuint64, converttoint, eval_as_double ...(+4) | __math_oflow, __math_uflow, asdouble, asuint64, converttoint, eval_as_double ...(+4) |
| `arceos/modules/axhal/src/arch/aarch64/trap.rs#handle_sync_exception` | `arceos/modules/axhal/src/arch/aarch64/trap.rs#handle_sync_exception` | 同板块同名同结构 | ESR_EL1.extract, crate::trap::handle_syscall, crate::trap::post_trap_callback, debug!, esr.read, esr.read_as_enum ...(+6) | ESR_EL1.extract, crate::trap::handle_syscall, crate::trap::post_trap_callback, debug!, esr.read, esr.read_as_enum ...(+6) |
| `arceos/modules/axhal/src/platform/riscv64_qemu_virt/irq.rs#register_handler` | `arceos/modules/axhal/src/platform/riscv64_qemu_virt/irq.rs#register_handler` | 同板块同名同结构 | with_cause! | with_cause! |
| `arceos/modules/axhal/src/platform/loongarch64_qemu_virt/irq.rs#set_enable` | `arceos/modules/axhal/src/platform/loongarch64_qemu_virt/irq.rs#set_enable` | 同板块同名同结构 | ecfg::read, ecfg::read().lie, ecfg::set_lie | ecfg::read, ecfg::read().lie, ecfg::set_lie |

#### 系统调用
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `api/src/imp/net/socket.rs#sys_getaddrinfo` | `arceos/api/arceos_posix_api/src/imp/net.rs#sys_getaddrinfo` | 同板块同名 | Err, Ok, SocketAddrV4::new, SocketAddrV4::new(ip, port).into, Vec::with_capacity, axnet::dns_query ...(+21) | char_ptr_to_str, debug!, syscall_body! |
| `api/src/imp/net/socket.rs#sys_recvfrom` | `arceos/api/arceos_posix_api/src/imp/net.rs#sys_recvfrom` | 同板块同名 | Err, Ok, Socket::from_fd, addrlen.is_null, buf_ptr.is_null, core::slice::from_raw_parts_mut ...(+4) | debug!, syscall_body! |
| `arceos/api/arceos_posix_api/src/imp/time.rs#sys_clock_gettime` | `arceos/api/arceos_posix_api/src/imp/time.rs#sys_clock_gettime` | 同板块同名 | syscall_body! | syscall_body! |
| `api/src/imp/net/socket.rs#sys_accept` | `arceos/api/arceos_posix_api/src/imp/net.rs#sys_accept` | 同板块同名 | FdFlags::empty, Mutex::new, Ok, Socket::Tcp, Socket::add_to_fd_table, Socket::from_fd ...(+6) | debug!, syscall_body! |

#### 文件系统
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `arceos/ulib/axlibc/c/glob.c#do_glob` | `arceos/ulib/axlibc/c/glob.c#do_glob` | 同板块同名同结构 | S_ISDIR, append, closedir, do_glob, errfunc, fnmatch ...(+7) | S_ISDIR, append, closedir, do_glob, errfunc, fnmatch ...(+7) |
| `arceos/ulib/axlibc/src/setjmp.rs#longjmp` | `arceos/ulib/axlibc/src/setjmp.rs#longjmp` | 同板块同名 | core::arch::naked_asm! | core::arch::naked_asm! |
| `arceos/ulib/axlibc/src/setjmp.rs#setjmp` | `arceos/ulib/axlibc/src/setjmp.rs#setjmp` | 同板块同名 | core::arch::naked_asm! | core::arch::naked_asm! |
| `arceos/ulib/axlibc/c/pow.c#log_inline` | `arceos/ulib/axlibc/c/pow.c#log_inline` | 同板块同名同结构 | __builtin_fma, asdouble | __builtin_fma, asdouble |

#### 设备驱动
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `arceos/tools/bwbench_client/src/main.rs#transmit_benchmark` | `arceos/tools/bwbench_client/src/main.rs#transmit_benchmark` | 同板块同名同结构 | Local::now, NetDevice::new, NetDevice::new(interface).unwrap, current_time.signed_duration_since, current_time.signed_duration_since(past_time).num_seconds, dev.send ...(+2) | Local::now, NetDevice::new, NetDevice::new(interface).unwrap, current_time.signed_duration_since, current_time.signed_duration_since(past_time).num_seconds, dev.send ...(+2) |
| `arceos/tools/bwbench_client/src/device.rs#bind_interface` | `arceos/tools/bwbench_client/src/device.rs#bind_interface` | 同板块同名同结构 | ETH_P_ALL.to_be, Err, Ok, libc::bind, self.ifreq.ioctl, std::io::Error::last_os_error ...(+2) | ETH_P_ALL.to_be, Err, Ok, libc::bind, self.ifreq.ioctl, std::io::Error::last_os_error ...(+2) |
| `arceos/modules/axdriver/src/lib.rs#init_drivers` | `arceos/modules/axdriver/src/lib.rs#init_drivers` | 同板块同名 | AllDevices::default, all_devs.block.iter, all_devs.block.iter().enumerate, all_devs.display.iter, all_devs.display.iter().enumerate, all_devs.net.iter ...(+5) | AllDevices::default, all_devs.block.iter, all_devs.block.iter().enumerate, all_devs.display.iter, all_devs.display.iter().enumerate, all_devs.net.iter ...(+5) |
| `arceos/modules/axdriver/src/virtio.rs#probe_pci` | `arceos/modules/axdriver/src/virtio.rs#probe_pci` | 同板块同名同结构 | D::try_new, Some, axdriver_virtio::probe_pci_device::<VirtIoHalImpl>, warn! | D::try_new, Some, axdriver_virtio::probe_pci_device::<VirtIoHalImpl>, warn! |

#### 网络协议栈
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `api/src/imp/net/socket.rs#setsockopt` | `arceos/ulib/axlibc/c/socket.c#setsockopt` | 同板块同名 | Err, Ok, SocketOptionLevel::try_from, error!, i32::from_ne_bytes, int_opt ...(+10) | unimplemented |
| `arceos/ulib/axlibc/c/network.c#inet_pton` | `arceos/ulib/axlibc/c/network.c#inet_pton` | 同板块同名同结构 | hexval, inet_pton, isdigit, memmove | hexval, inet_pton, isdigit, memmove |
| `api/src/imp/net/socket.rs#getsockopt` | `arceos/ulib/axlibc/c/socket.c#getsockopt` | 同板块同名 | Err, Ok, optlen.is_null, optval.is_null, size_of::<i32>, tcpsocket.lock ...(+5) | unimplemented |
| `api/src/imp/net/socket.rs#sendto` | `arceos/api/arceos_posix_api/src/imp/net.rs#sendto` | 同板块同名 | Err, IpAddr::V4, SocketAddr::new, udpsocket
                    .send_to, udpsocket
                    .send_to(buf, addr)
                    .map_err, udpsocket.bind ...(+3) | Err, Ok, udpsocket.lock, udpsocket.lock().send_to |

#### 文件系统/第三方库
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `modules/lwext4_rust/c/lwext4/src/ext4_balloc.c#ext4_balloc_alloc_block` | `crates/lwext4_rust/c/lwext4/src/ext4_balloc.c#ext4_balloc_alloc_block` | 同板块同名同结构 | ext4_balloc_get_bgid_of_block, ext4_balloc_get_block_of_bgid, ext4_balloc_set_bitmap_csum, ext4_balloc_verify_bitmap_csum, ext4_bg_get_block_bitmap, ext4_bg_get_free_blocks_count ...(+19) | ext4_balloc_get_bgid_of_block, ext4_balloc_get_block_of_bgid, ext4_balloc_set_bitmap_csum, ext4_balloc_verify_bitmap_csum, ext4_bg_get_block_bitmap, ext4_bg_get_free_blocks_count ...(+19) |
| `modules/lwext4_rust/c/lwext4/src/ext4_journal.c#jbd_journal_prepare` | `crates/lwext4_rust/c/lwext4/src/ext4_journal.c#jbd_journal_prepare` | 同板块同名同结构 | EXT4_BLOCK_ZERO, RB_FIND, TAILQ_FOREACH_REVERSE_SAFE, TAILQ_FOREACH_SAFE, TAILQ_REMOVE, ext4_bcache_clear_dirty ...(+17) | EXT4_BLOCK_ZERO, RB_FIND, TAILQ_FOREACH_REVERSE_SAFE, TAILQ_FOREACH_SAFE, TAILQ_REMOVE, ext4_bcache_clear_dirty ...(+17) |
| `modules/lwext4_rust/c/lwext4/src/ext4.c#ext4_dir_rm` | `crates/lwext4_rust/c/lwext4/src/ext4.c#ext4_dir_rm` | 同板块同名同结构 | EXT4_MP_LOCK, EXT4_MP_UNLOCK, ext4_block_cache_write_back, ext4_dir_en_get_inode, ext4_dir_iterator_fini, ext4_dir_iterator_init ...(+19) | EXT4_MP_LOCK, EXT4_MP_UNLOCK, ext4_block_cache_write_back, ext4_dir_en_get_inode, ext4_dir_iterator_fini, ext4_dir_iterator_init ...(+19) |
| `modules/lwext4_rust/c/lwext4/src/ext4_dir_idx.c#ext4_dir_dx_split_index` | `crates/lwext4_rust/c/lwext4/src/ext4_dir_idx.c#ext4_dir_dx_split_index` | 同板块同名同结构 | ext4_block_set, ext4_dir_dx_climit_get_count, ext4_dir_dx_climit_get_limit, ext4_dir_dx_climit_set_count, ext4_dir_dx_climit_set_limit, ext4_dir_dx_entry_get_hash ...(+10) | ext4_block_set, ext4_dir_dx_climit_get_count, ext4_dir_dx_climit_get_limit, ext4_dir_dx_climit_set_count, ext4_dir_dx_climit_set_limit, ext4_dir_dx_entry_get_hash ...(+10) |

#### 网络协议栈/第三方库
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `arceos/modules/axnet/src/smoltcp_impl/tcp.rs#connect` | `arceos/modules/axnet/src/smoltcp_impl/tcp.rs#connect` | 同板块同名同结构 | Err, Ok, Ok::<(IpEndpoint, IpEndpoint), AxError>, SOCKET_SET
                .with_socket_mut::<tcp::Socket, _, _>, SOCKET_SET.add, SocketSetWrapper::new_tcp_socket ...(+32) | Err, Ok, Ok::<(IpEndpoint, IpEndpoint), AxError>, SOCKET_SET
                .with_socket_mut::<tcp::Socket, _, _>, SOCKET_SET.add, SocketSetWrapper::new_tcp_socket ...(+32) |
| `arceos/modules/axnet/src/smoltcp_impl/dns.rs#query` | `arceos/modules/axnet/src/smoltcp_impl/dns.rs#query` | 同板块同名同结构 | Err, Ok, SOCKET_SET
            .with_socket_mut::<dns::Socket, _, _>, SOCKET_SET
            .with_socket_mut::<dns::Socket, _, _>(handle, |socket| {
                socket.start_query(iface.lock().context(), name, query_type)
            })
            .map_err, SOCKET_SET.poll_interfaces, SOCKET_SET.with_socket_mut::<dns::Socket, _, _> ...(+12) | Err, Ok, SOCKET_SET
            .with_socket_mut::<dns::Socket, _, _>, SOCKET_SET
            .with_socket_mut::<dns::Socket, _, _>(handle, |socket| {
                socket.start_query(iface.lock().context(), name, query_type)
            })
            .map_err, SOCKET_SET.poll_interfaces, SOCKET_SET.with_socket_mut::<dns::Socket, _, _> ...(+12) |
| `arceos/modules/axnet/src/smoltcp_impl/tcp.rs#recv_timeout` | `arceos/modules/axnet/src/smoltcp_impl/tcp.rs#recv_timeout` | 同板块同名同结构 | Err, Ok, SOCKET_SET.with_socket_mut::<tcp::Socket, _, _>, ax_err!, ax_err_type!, current_ticks ...(+11) | Err, Ok, SOCKET_SET.with_socket_mut::<tcp::Socket, _, _>, ax_err!, ax_err_type!, current_ticks ...(+11) |
| `arceos/modules/axnet/src/smoltcp_impl/bench.rs#bench_transmit_bandwidth` | `arceos/modules/axnet/src/smoltcp_impl/bench.rs#bench_transmit_bandwidth` | 同板块同名同结构 | (current_time - past_time).secs, AxNetTxToken::consume, InterfaceWrapper::current_time, info!, self.transmit, tx_buf[0..12].fill ...(+2) | (current_time - past_time).secs, AxNetTxToken::consume, InterfaceWrapper::current_time, info!, self.transmit, tx_buf[0..12].fill ...(+2) |

#### 第三方库
仅参考项目中观察到该模块代表函数，目标项目没有同模块可对齐证据：
- 参考项目：`crates/axdriver_net/src/dwmac/mod.rs#init_phy`，调用摘要：Err, H::wait_until, Ok, Yt8531cPhy::<H>::new, core::time::Duration::from_millis, log::error! ...(+18)
- 参考项目：`crates/axdriver_net/src/dwmac/mod.rs#init`，调用摘要：AtomicBool::new, DescriptorRing::<RX_DESC_COUNT, H>::new, DescriptorRing::<TX_DESC_COUNT, H>::new, H::configure_platform, Ok, log::error! ...(+20)
- 参考项目：`crates/visionfive2-sd/src/lib.rs#send_cmd`，调用摘要：RawInterrupt::from, RawInterruptStatusReg::from, S::sleep_ms_until, Some, arg.into, assert! ...(+22)
- 参考项目：`crates/visionfive2-sd/src/lib.rs#init_sdcard`，调用摘要：CardTypeReg::from, CardTypeReg::from(0).with_card_width4_1, CmdArg::new, CmdReg::from, ControlReg::from, RawInterruptStatusReg::from ...(+20)

#### 通用模块/其他
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `arceos/ulib/axlibc/c/printf.c#print_exponential_number` | `arceos/ulib/axlibc/c/printf.c#print_exponential_number` | 同板块同名同结构 | ABS_FOR_PRINTING, PRINTF_ABS, bastardized_floor, get_components, get_normalized_components, get_sign_bit ...(+5) | ABS_FOR_PRINTING, PRINTF_ABS, bastardized_floor, get_components, get_normalized_components, get_sign_bit ...(+5) |
| `arceos/ulib/axlibc/c/pow.c#pow` | `arceos/ulib/axlibc/c/pow.c#pow` | 同板块同名同结构 | __builtin_fma, __math_invalid, __math_oflow, __math_uflow, asdouble, asuint64 ...(+8) | __builtin_fma, __math_invalid, __math_oflow, __math_uflow, asdouble, asuint64 ...(+8) |
| `arceos/ulib/axlibc/c/string.c#twoway_strstr` | `arceos/ulib/axlibc/c/string.c#twoway_strstr` | 同板块同名同结构 | BITOP, MAX, memchr, memcmp | BITOP, MAX, memchr, memcmp |
| `arceos/ulib/axlibc/c/printf.c#print_broken_up_decimal` | `arceos/ulib/axlibc/c/printf.c#print_broken_up_decimal` | 同板块同名同结构 | out_rev_ | out_rev_ |


好的，仲裁员。基于提供的证据底座、控辩双方陈述及原始AST抽样数据，现对本次同源性争议进行最终定性仲裁。

### 1. 宏观判定
- **定性结论**: **独立开发，存在共享上游框架及领域通用模式的合理收敛**
  综合客观AST相似度（44.46%）、双向覆盖率指标及核心模块的调用流异构性分析，本庭认定目标项目 `[2026-T2026100069910651-XX]` 与参考项目 `[2025-九莲宝灯一向听-天津理工大学]` 在整体架构与核心实现上存在显著差异，不构成抄袭或大规模代码复用。双方在底层基础设施（如等待队列、硬件抽象层）的相似性，可归因于共同依赖的 `ArceOS` 上游框架及操作系统领域的必然实现模式。

### 2. 全模块覆盖矩阵解读
| 模块 | 双方存在 | 共同AST指纹 | 覆盖率解读 | 证据强度 |
| :--- | :---: | :---: | :--- | :---: |
| **进程管理** | 是 | 是 | 目标覆盖率仅26.27%，表明其大部分实现独立。相似部分集中于`axtask`等上游框架代码。 | **弱** |
| **任务调度/异步运行时** | 是 | 是 | 双方覆盖率均较高（58.7%/68.73%），但证据显示高度一致项多为`axtask`、`axlog`等ArceOS内核子模块，属上游共用。 | **中（上游共用）** |
| **内存管理** | 是 | 是 | 目标覆盖率48.42%，参考73.16%。差异显著，例如`sys_mmap`等核心系统调用调用链不同，表明独立实现。 | **弱** |
| **中断/异常处理** | 是 | 是 | 覆盖率极高（91.49%/84.31%），但证据指纹均为`trap.rs`、`irq.rs`等平台相关且逻辑固定的硬件处理代码，属领域必然。 | **弱（领域必然）** |
| **系统调用** | 是 | 是 | 覆盖率差异大（61.4%/32.71%）。目标项目实现了具体逻辑，参考项目多用`syscall_body!`宏，架构完全不同。 | **弱** |
| **文件系统** | 是 | 是 | 覆盖率低（22.84%/33.7%），表明核心文件系统逻辑独立。相似部分多为`axlibc`的C库函数。 | **弱** |
| **设备驱动** | 是 | 是 | 覆盖率极高（93.5%/80.99%），但证据指纹为`virtio.rs`、`axdriver`等驱动框架初始化代码，属上游共用。 | **弱（上游共用）** |
| **网络协议栈** | 是 | 是 | 覆盖率极高（89.29%/98.04%），证据指纹为`smoltcp_impl`下的`tcp.rs`、`dns.rs`，属第三方库`smoltcp`的封装，非项目原创。 | **弱（第三方库）** |
| **文件系统/第三方库** | 是 | 是 | 覆盖率极高（75.96%/85.89%），证据指纹为`lwext4_rust`的C源码，属第三方库。 | **弱（第三方库）** |
| **网络协议栈/第三方库** | 是 | 是 | 覆盖率极高（91.45%/92.24%），证据指纹为`smoltcp`实现，属第三方库。 | **弱（第三方库）** |
| **第三方库** | 否 | 否 | 仅参考项目存在，目标项目无此模块，体现了项目间的差异。 | **未形成** |
| **通用模块/其他** | 是 | 是 | 覆盖率极高（90.05%/75.26%），证据指纹为`printf.c`、`pow.c`等C库函数，属第三方库或标准实现。 | **弱（第三方库）** |

### 3. 底层 AST 关键证据指纹表
| 所属板块 | 目标项目路径 | 参考项目路径 | 证据类型 | 技术说明 |
| :--- | :--- | :--- | :--- | :--- |
| **第三方库/公共依赖** | `modules/lwext4_rust/c/lwext4/src/ext4_balloc.c#ext4_balloc_alloc_block` | `crates/lwext4_rust/c/lwext4/src/ext4_balloc.c#ext4_balloc_alloc_block` | 第三方库代码 | 完全一致的ext4块分配逻辑，源自同一开源C库`lwext4`。 |
| **第三方库/公共依赖** | `arceos/modules/axnet/src/smoltcp_impl/tcp.rs#connect` | `arceos/modules/axnet/src/smoltcp_impl/tcp.rs#connect` | 第三方库代码 | 完全一致的TCP连接封装，调用`smoltcp`库的内部接口。 |
| **任务调度/异步运行时** | `arceos/modules/axtask/src/wait_queue.rs#wait_timeout_until` | `arceos/modules/axtask/src/wait_queue.rs#wait_timeout_until` | 上游框架共用 | 完全一致的超时等待逻辑，调用`ArceOS`内核的`NoPreemptIrqSave`、`blocked_resched`等内部API。 |
| **中断/异常处理** | `arceos/modules/axhal/src/arch/aarch64/trap.rs#handle_sync_exception` | `arceos/modules/axhal/src/arch/aarch64/trap.rs#handle_sync_exception` | 领域必然实现 | 完全一致的AArch64同步异常处理入口，读取`ESR_EL1`并分发，是ARM架构的标准模式。 |
| **进程管理** | `arceos/modules/axtask/src/run_queue.rs#exit_current` | `arceos/modules/axtask/src/run_queue.rs#exit_current` | 上游框架共用 | 完全一致的任务退出逻辑，操作`EXITED_TASKS`和`WAIT_FOR_EXIT`等ArceOS内部结构。 |
| **进程管理** | `api/src/imp/task/signal.rs#sys_rt_sigprocmask` | `api/src/imp/signal.rs#sys_rt_sigprocmask` | 同名异构 | 目标通过`current_thread_data()`获取上下文，参考通过`current().task_ext().thread_data()`链式获取，抽象层级不同。 |
| **进程管理** | `process/src/process.rs#exit` | `arceos/modules/axtask/src/api.rs#exit` | 同名异构 | 目标实现复杂的进程树重组（`reaper`），参考仅简单调用`exit_current`，功能域完全不同。 |
| **内存管理** | `api/src/imp/mm/mmap.rs#sys_mmap` | `api/src/imp/mm/mmap.rs#sys_mmap` | 同名异构 | 目标使用`Arc::new`创建映射对象，参考未使用，内存管理模型不同。 |
| **系统调用** | `api/src/imp/net/socket.rs#sys_epoll_wait` | `arceos/api/arceos_posix_api/src/imp/io_mpx/epoll.rs#sys_epoll_wait` | 同名异构 | 目标实现具体epoll逻辑，参考使用`syscall_body!`宏，实现架构完全不同。 |

### 4. 核心代码调用流对比剖析
* **【进程管理/进程退出】**
* **底层技术析理**: 目标项目在 `process/src/process.rs` 中实现了类Unix的进程收养（reaper）机制，调用流涉及 `Arc::downgrade`、`reaper.children.lock`、`core::mem::take` 等复杂的进程树重组操作。而参考项目在 `arceos/modules/axtask/src/api.rs` 中的 `exit` 函数仅是 `current_run_queue::<NoPreemptIrqSave>().exit_current()` 的简单包装，将线程退出直接委托给调度器。两者在进程生命周期的抽象层次和功能复杂度上存在根本性差异。
* **代码溯源链接**:
  - 目标项目: `process/src/process.rs#exit`
  - 参考项目: `arceos/modules/axtask/src/api.rs#exit`

* **【进程管理/信号处理】**
* **底层技术析理**: 目标项目通过 `current_thread_data()` 辅助函数直接获取线程数据，进而操作信号结构体。参考项目则通过 `current().task_ext().thread_data()` 的链式调用逐层穿透抽象。这反映了不同的内部API设计哲学：目标项目倾向于封装便捷访问函数，而参考项目则暴露了更完整的调用路径。
* **代码溯源链接**:
  - 目标项目: `api/src/imp/task/signal.rs#sys_rt_sigprocmask`
  - 参考项目: `api/src/imp/signal.rs#sys_rt_sigprocmask`

* **【内存管理/mmap系统调用】**
* **底层技术析理**: 在 `sys_mmap` 的实现中，目标项目使用 `Arc::new` 为内存映射创建原子引用计数指针，暗示其可能采用了更细粒度的共享所有权模型。参考项目的调用流中则未见此模式。这指向了双方在内存对象生命周期管理上的不同策略。
* **代码溯源链接**:
  - 目标项目: `api/src/imp/mm/mmap.rs#sys_mmap`
  - 参考项目: `api/src/imp/mm/mmap.rs#sys_mmap`

### 5. 单方模块与降权项
*   **单方模块**: “第三方库”模块仅存在于参考项目中，包含 `axdriver_net`、`visionfive2-sd` 等独立的crate。目标项目无此模块，这构成了两项目架构上的显著差异，是独立开发的有力证据。
*   **降权项说明**:
    *   **第三方库/公共依赖**: `lwext4_rust`、`smoltcp`、`axlibc` 的C库函数（如 `printf.c`、`pow.c`）等，因源自公开的第三方开源代码，其AST指纹一致不能作为项目间抄袭的证据。
    *   **上游框架共用**: `arceos/modules/axtask`、`arceos/modules/axhal` 等路径下的代码，因两项目均基于 `ArceOS` 框架，其内部实现（如等待队列、陷阱处理）的高度一致是共用上游的必然结果。
    *   **领域必然实现**: `busy_wait_until`（`core::hint::spin_loop` + `wall_time`）、`panic_prevent_reenter`（`AtomicBool` + `cpu::wait_forever`）等函数，其实现逻辑是操作系统或裸机编程领域的标准范式，无创造性表达空间。
    *   **未实现桩函数**: `unimplemented` 宏是Rust/C语言中声明未完成功能的标准占位符，不构成任何原创性表达。
    *   **跨板块归类**: 控方将 `sys_epoll_wait`（I/O多路复用）、`busy_wait_until`（硬件抽象层）等函数归入“进程管理”板块进行指控，属于跨板块硬凑证据，本庭不予采信。

### 6. 保守结论
综合全模块矩阵分析，目标项目与参考项目的整体AST相似度为44.46%，处于中等偏低水平。在“进程管理”这一核心争议板块，目标项目覆盖率仅26.27%，表明其大部分实现是独立的。双方高度一致的代码指纹几乎全部集中于 `ArceOS` 上游框架、`lwext4`/`smoltcp` 等第三方开源库，以及中断处理等无创造性表达的领域必然实现。而在 `sys_mmap`、`sys_execve`、信号处理、进程退出等核心系统调用和机制上，双方在抽象层次、内存模型、并发策略上展现出显著且根本的异构性。因此，本庭认定，现有证据不足以支持抄袭指控，两项目的相似性源于共享的开源基础与操作系统领域的通用实践，应定性为独立开发。

------------------------------

