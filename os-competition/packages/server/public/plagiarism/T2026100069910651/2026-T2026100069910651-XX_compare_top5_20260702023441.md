# 🛡️ OS 内核赛自动化代码级架构审计报告

**审计时间**: `2026-07-02 02:34:41`
**参赛作品名称**: `2026-T2026100069910651-XX`
**年份**: `2026`
**学校**: `XX`
**队伍名称**: `T2026100069910651`
**比赛名字**: `OS-Competition`
**比赛子赛道**: `Kernel-Implementation`
**仓库地址**: `https://gitlab.eduxiji.net/educg-group-43501-3132633/T2026100069910651-1260`
**推测开发语言**: `Rust / C (由AST探针自动识别)`
**特征函数量**: `3661`

## ⚔️ 底层代码同源性对比 (AST Homology)
> 本部分由专门的代码审查智能体，对该项目与符合年份条件的历届最高相似度项目进行控制流与底层调用对比。新版报告会先给出确定性的全模块覆盖矩阵，再给出 Agent 的分模块解释，避免只围绕少量抽样函数展开。

### 🏆 Top 5 底层对标: vs `2025-模仿游戏-燕山大学`（燕山大学 / 模仿游戏）

#### 对方作品信息

**对方年份**: `2025`
**对方学校**: `燕山大学`
**对方队伍名称**: `模仿游戏`
**对方比赛名字**: `OS-Competition`
**对方比赛子赛道**: `Kernel-Implementation`
**对方仓库地址**: `https://gitlab.eduxiji.net/educg-group-36002-2710490/T202510216995249-4014`

> 双向覆盖率：共同 AST 指纹: 1257；唯一 AST 指纹: 目标项目 2816，参考项目 1942；覆盖率: 目标项目 44.64%，参考项目 64.73%

## 全模块对比总览（确定性证据）

> 总体覆盖率：共同 AST 指纹: 1257；唯一 AST 指纹: 目标项目 2816，参考项目 1942；覆盖率: 目标项目 44.64%，参考项目 64.73%

### 1. 模块覆盖矩阵
| 模块 | 目标函数数 | 参考函数数 | 共同唯一 AST 指纹 | Target 覆盖 | Base 覆盖 | 状态 |
| :--- | ---: | ---: | ---: | ---: | ---: | :--- |
| 进程管理 | 132 | 76 | 31 | 26.27% | 44.29% | 双方存在且有结构重合 |
| 任务调度/异步运行时 | 438 | 383 | 197 | 61.18% | 72.43% | 双方存在且有结构重合 |
| 内存管理 | 480 | 302 | 222 | 54.01% | 80.43% | 双方存在且有结构重合 |
| 中断/异常处理 | 53 | 56 | 47 | 100.0% | 94.0% | 双方存在且有结构重合 |
| 系统调用 | 69 | 121 | 34 | 59.65% | 34.69% | 双方存在且有结构重合 |
| 文件系统 | 1000 | 631 | 151 | 22.54% | 32.54% | 双方存在且有结构重合 |
| 设备驱动 | 134 | 135 | 115 | 93.5% | 94.26% | 双方存在且有结构重合 |
| 网络协议栈 | 66 | 67 | 50 | 89.29% | 89.29% | 双方存在且有结构重合 |
| 文件系统/第三方库 | 647 | 25 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |
| 网络协议栈/第三方库 | 132 | 99 | 72 | 61.54% | 79.12% | 双方存在且有结构重合 |
| 通用模块/其他 | 510 | 611 | 356 | 88.56% | 74.95% | 双方存在且有结构重合 |

### 2. 分模块证据清单

#### 进程管理
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `arceos/modules/axtask/src/run_queue.rs#exit_current` | `arceos/modules/axtask/src/run_queue.rs#exit_current` | 同板块同名同结构 | EXITED_TASKS.current_ref_mut_raw, EXITED_TASKS.current_ref_mut_raw().clear, EXITED_TASKS.current_ref_mut_raw().push_back, WAIT_FOR_EXIT.current_ref_mut_raw, WAIT_FOR_EXIT.current_ref_mut_raw().notify_one, assert! ...(+8) | EXITED_TASKS.current_ref_mut_raw, EXITED_TASKS.current_ref_mut_raw().clear, EXITED_TASKS.current_ref_mut_raw().push_back, WAIT_FOR_EXIT.current_ref_mut_raw, WAIT_FOR_EXIT.current_ref_mut_raw().notify_one, assert! ...(+8) |
| `arceos/modules/axtask/src/wait_queue.rs#cancel_events` | `arceos/modules/axtask/src/wait_queue.rs#cancel_events` | 同板块同名同结构 | curr.in_wait_queue, curr.ptr_eq, curr.set_in_wait_queue, curr.timer_ticket_expired, self.queue.lock, self.queue.lock().retain | curr.in_wait_queue, curr.ptr_eq, curr.set_in_wait_queue, curr.timer_ticket_expired, self.queue.lock, self.queue.lock().retain |
| `arceos/modules/axtask/src/wait_queue.rs#wait_timeout_until` | `arceos/modules/axtask/src/wait_queue.rs#wait_timeout_until` | 同板块同名同结构 | axhal::time::wall_time, condition, crate::current, crate::timers::set_alarm_wakeup, curr.clone, current_run_queue::<NoPreemptIrqSave> ...(+4) | axhal::time::wall_time, condition, crate::current, crate::timers::set_alarm_wakeup, curr.clone, current_run_queue::<NoPreemptIrqSave> ...(+4) |
| `process/src/process.rs#exit` | `arceos/modules/axtask/src/api.rs#exit` | 同板块同名 | Arc::downgrade, assert!, child.parent.lock, core::mem::take, reaper.children.lock, reaper_children.insert ...(+7) | current_run_queue::<NoPreemptIrqSave>, current_run_queue::<NoPreemptIrqSave>().exit_current |

#### 任务调度/异步运行时
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `arceos/ulib/axlibc/c/printf.c#format_string_loop` | `arceos/ulib/axlibc/c/printf.c#format_string_loop` | 同板块同名同结构 | ABS_FOR_PRINTING, ADVANCE_IN_FORMAT_STRING, atou_, is_digit_, out_rev_, parse_flags ...(+5) | ABS_FOR_PRINTING, ADVANCE_IN_FORMAT_STRING, atou_, is_digit_, out_rev_, parse_flags ...(+5) |
| `api/src/imp/task/futex.rs#sys_futex` | `api/src/imp/futex.rs#sys_futex` | 同板块同名 | Err, Ok, Some, axtask::yield_now, current_process_data, drop ...(+27) | Err, Ok, axtask::yield_now, curr.task_ext, curr.task_ext().process_data, current ...(+16) |
| `arceos/modules/axlog/src/lib.rs#log` | `arceos/modules/axlog/src/lib.rs#log` | 同板块同名同结构 | cfg_if::cfg_if!, record.level, record.line, record.line().unwrap_or, record.metadata, record.target ...(+1) | cfg_if::cfg_if!, record.level, record.line, record.line().unwrap_or, record.metadata, record.target ...(+1) |
| `arceos/modules/axtask/src/run_queue.rs#switch_to` | `arceos/modules/axtask/src/run_queue.rs#switch_to` | 同板块同名同结构 | (*prev_ctx_ptr).switch_to, Arc::downgrade, CurrentTask::set_current, PREV_TASK.current_ref_mut_raw, assert!, clear_prev_task_on_cpu ...(+8) | (*prev_ctx_ptr).switch_to, Arc::downgrade, CurrentTask::set_current, PREV_TASK.current_ref_mut_raw, assert!, clear_prev_task_on_cpu ...(+8) |

#### 内存管理
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `api/src/imp/mm/mmap.rs#sys_mmap` | `api/src/imp/mm/mmap.rs#sys_mmap` | 同板块同名 | Arc::new, Err, File::from_fd, MmapFlags::from_bits_truncate, MmapProt::from_bits_truncate, Ok ...(+34) | (addr + length).align_up, Err, File::from_fd, MmapFlags::from_bits_truncate, MmapProt::from_bits_truncate, Ok ...(+27) |
| `arceos/ulib/axlibc/src/strftime.rs#strftime_inner` | `arceos/ulib/axlibc/src/strftime.rs#strftime_inner` | 同板块同名同结构 | CountingWriter::new, format.offset, inner_strftime, w! | CountingWriter::new, format.offset, inner_strftime, w! |
| `arceos/ulib/axlibc/src/strftime.rs#inner_strftime` | `arceos/ulib/axlibc/src/strftime.rs#inner_strftime` | 同板块同名同结构 | format.offset, w! | format.offset, w! |
| `arceos/modules/axmm/src/aspace.rs#try_clone` | `arceos/modules/axmm/src/aspace.rs#try_clone` | 同板块同名 | Backend::new_alloc, Err, MemoryArea::new, Ok, PageIterWrapper::new, PageIterWrapper::new(area.start(), area.end(), align)
                .expect ...(+29) | Backend::new_alloc, Err, MemoryArea::new, Ok, PageIterWrapper::new, PageIterWrapper::new(area.start(), area.end(), align)
                .expect ...(+29) |

#### 中断/异常处理
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `arceos/ulib/axlibc/c/pow.c#exp_inline` | `arceos/ulib/axlibc/c/pow.c#exp_inline` | 同板块同名同结构 | __math_oflow, __math_uflow, asdouble, asuint64, converttoint, eval_as_double ...(+4) | __math_oflow, __math_uflow, asdouble, asuint64, converttoint, eval_as_double ...(+4) |
| `arceos/modules/axhal/src/arch/aarch64/trap.rs#handle_sync_exception` | `arceos/modules/axhal/src/arch/aarch64/trap.rs#handle_sync_exception` | 同板块同名同结构 | ESR_EL1.extract, crate::trap::handle_syscall, crate::trap::post_trap_callback, debug!, esr.read, esr.read_as_enum ...(+6) | ESR_EL1.extract, crate::trap::handle_syscall, crate::trap::post_trap_callback, debug!, esr.read, esr.read_as_enum ...(+6) |
| `arceos/modules/axhal/src/platform/riscv64_qemu_virt/irq.rs#register_handler` | `arceos/modules/axhal/src/platform/riscv64_qemu_virt/irq.rs#register_handler` | 同板块同名同结构 | with_cause! | with_cause! |
| `arceos/modules/axhal/src/platform/loongarch64_qemu_virt/irq.rs#set_enable` | `arceos/modules/axhal/src/platform/loongarch64_qemu_virt/irq.rs#set_enable` | 同板块同名同结构 | ecfg::read, ecfg::read().lie, ecfg::set_lie | ecfg::read, ecfg::read().lie, ecfg::set_lie |

#### 系统调用
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `api/src/imp/net/socket.rs#sys_getaddrinfo` | `arceos/api/arceos_posix_api/src/imp/net.rs#sys_getaddrinfo` | 同板块同名 | Err, Ok, SocketAddrV4::new, SocketAddrV4::new(ip, port).into, Vec::with_capacity, axnet::dns_query ...(+21) | char_ptr_to_str, debug!, syscall_body! |
| `api/src/interface/mm/shm.rs#sys_shmget` | `api/src/imp/mm/shm.rs#sys_shmget` | 同板块同名 | Err, Ok, SHARED_MEMORY_MANAGER.create, SHARED_MEMORY_MANAGER.get, SHARED_MEMORY_MANAGER.get(key).is_none, SHARED_MEMORY_MANAGER.mem_map.lock ...(+4) | Err, Ok, info!, shm_manager, shm_manager().lock, shm_manager().lock().get_or_create |
| `api/src/imp/net/socket.rs#sys_recvfrom` | `arceos/api/arceos_posix_api/src/imp/net.rs#sys_recvfrom` | 同板块同名 | Err, Ok, Socket::from_fd, addrlen.is_null, buf_ptr.is_null, core::slice::from_raw_parts_mut ...(+4) | debug!, syscall_body! |
| `arceos/api/arceos_posix_api/src/imp/time.rs#sys_clock_gettime` | `arceos/api/arceos_posix_api/src/imp/time.rs#sys_clock_gettime` | 同板块同名 | syscall_body! | syscall_body! |

#### 文件系统
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `arceos/ulib/axlibc/c/glob.c#do_glob` | `arceos/ulib/axlibc/c/glob.c#do_glob` | 同板块同名同结构 | S_ISDIR, append, closedir, do_glob, errfunc, fnmatch ...(+7) | S_ISDIR, append, closedir, do_glob, errfunc, fnmatch ...(+7) |
| `arceos/ulib/axlibc/src/setjmp.rs#longjmp` | `arceos/ulib/axlibc/src/setjmp.rs#longjmp` | 同板块同名 | core::arch::naked_asm! | core::arch::naked_asm! |
| `arceos/ulib/axlibc/src/setjmp.rs#setjmp` | `arceos/ulib/axlibc/src/setjmp.rs#setjmp` | 同板块同名 | core::arch::naked_asm! | core::arch::naked_asm! |
| `arceos/ulib/axlibc/c/pow.c#log_inline` | `arceos/ulib/axlibc/c/pow.c#log_inline` | 同板块同名同结构 | __builtin_fma, asdouble | __builtin_fma, asdouble |

#### 设备驱动
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `arceos/tools/bwbench_client/src/main.rs#transmit_benchmark` | `arceos/tools/bwbench_client/src/main.rs#transmit_benchmark` | 同板块同名同结构 | Local::now, NetDevice::new, NetDevice::new(interface).unwrap, current_time.signed_duration_since, current_time.signed_duration_since(past_time).num_seconds, dev.send ...(+2) | Local::now, NetDevice::new, NetDevice::new(interface).unwrap, current_time.signed_duration_since, current_time.signed_duration_since(past_time).num_seconds, dev.send ...(+2) |
| `arceos/tools/bwbench_client/src/device.rs#bind_interface` | `arceos/tools/bwbench_client/src/device.rs#bind_interface` | 同板块同名同结构 | ETH_P_ALL.to_be, Err, Ok, libc::bind, self.ifreq.ioctl, std::io::Error::last_os_error ...(+2) | ETH_P_ALL.to_be, Err, Ok, libc::bind, self.ifreq.ioctl, std::io::Error::last_os_error ...(+2) |
| `arceos/modules/axdriver/src/lib.rs#init_drivers` | `arceos/modules/axdriver/src/lib.rs#init_drivers` | 同板块同名同结构 | AllDevices::default, all_devs.block.iter, all_devs.block.iter().enumerate, all_devs.display.iter, all_devs.display.iter().enumerate, all_devs.net.iter ...(+5) | AllDevices::default, all_devs.block.iter, all_devs.block.iter().enumerate, all_devs.display.iter, all_devs.display.iter().enumerate, all_devs.net.iter ...(+5) |
| `arceos/modules/axdriver/src/virtio.rs#probe_pci` | `arceos/modules/axdriver/src/virtio.rs#probe_pci` | 同板块同名同结构 | D::try_new, Some, axdriver_virtio::probe_pci_device::<VirtIoHalImpl>, warn! | D::try_new, Some, axdriver_virtio::probe_pci_device::<VirtIoHalImpl>, warn! |

#### 网络协议栈
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `api/src/imp/net/socket.rs#setsockopt` | `arceos/ulib/axlibc/c/socket.c#setsockopt` | 同板块同名 | Err, Ok, SocketOptionLevel::try_from, error!, i32::from_ne_bytes, int_opt ...(+10) | unimplemented |
| `arceos/ulib/axlibc/c/network.c#inet_pton` | `arceos/ulib/axlibc/c/network.c#inet_pton` | 同板块同名同结构 | hexval, inet_pton, isdigit, memmove | hexval, inet_pton, isdigit, memmove |
| `api/src/imp/net/socket.rs#getsockopt` | `arceos/ulib/axlibc/c/socket.c#getsockopt` | 同板块同名 | Err, Ok, optlen.is_null, optval.is_null, size_of::<i32>, tcpsocket.lock ...(+5) | unimplemented |
| `api/src/imp/net/socket.rs#sendto` | `arceos/api/arceos_posix_api/src/imp/net.rs#sendto` | 同板块同名 | Err, IpAddr::V4, SocketAddr::new, udpsocket
                    .send_to, udpsocket
                    .send_to(buf, addr)
                    .map_err, udpsocket.bind ...(+3) | Err, Ok, udpsocket.lock, udpsocket.lock().send_to |

#### 文件系统/第三方库
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `modules/lwext4_rust/src/inode/file.rs#read_at` | `arceos/modules/axfs/src/fs/lwext4_rust.rs#read_at` | 同板块同名 | (inode as *const _ as *const u8).add, (pos + buf.len() as u64).min, Ok, WritebackGuard::new, assert!, buf.as_mut_ptr ...(+22) | <i32 as TryInto<AxError>>::try_into, <i32 as TryInto<AxError>>::try_into(e).unwrap, e.try_into, e.try_into().unwrap, file.file_close, file.file_open ...(+9) |
| `modules/lwext4_rust/src/inode/file.rs#write_at` | `arceos/modules/axfs/src/fs/lwext4_rust.rs#write_at` | 同板块同名 | Ok, assert!, assert_eq!, buf.as_ptr, buf.is_empty, buf.len ...(+15) | <i32 as TryInto<AxError>>::try_into, <i32 as TryInto<AxError>>::try_into(e).unwrap, e.try_into, e.try_into().unwrap, file
                    .file_write, file
                    .file_write(&zero_buf[..write_size as usize])
                    .map_err ...(+15) |
| `arceos/modules/axfs-ng/src/fs/ext4/inode.rs#create` | `arceos/modules/axfs/src/fs/lwext4_rust.rs#create` | 同板块同名 | DirEntry::new_dir, DirEntry::new_file, DirNode::new, Err, FileNode::new, Inode::new ...(+13) | Ok, e.try_into, e.try_into().unwrap, file.check_inode_exist, file.dir_mk, file.dir_mk(fpath)
                    .map ...(+12) |
| `modules/lwext4_rust/src/fs.rs#rename` | `arceos/modules/axfs/src/fs/lwext4_rust.rs#rename` | 同板块同名 | Err, Ok, dst_dir_ref.add_entry, dst_dir_ref.inc_nlink, dst_dir_ref.remove_entry, result.entry ...(+12) | e.try_into, e.try_into().unwrap, file.file_rename, file.file_rename(src_path, dst_path)
            .map, file.file_rename(src_path, dst_path)
            .map(|_v| ())
            .map_err, self.0.lock |

#### 网络协议栈/第三方库
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `arceos/modules/axnet/src/smoltcp_impl/tcp.rs#connect` | `arceos/modules/axnet/src/smoltcp_impl/tcp.rs#connect` | 同板块同名 | Err, Ok, Ok::<(IpEndpoint, IpEndpoint), AxError>, SOCKET_SET
                .with_socket_mut::<tcp::Socket, _, _>, SOCKET_SET.add, SocketSetWrapper::new_tcp_socket ...(+32) | Err, Ok, SOCKET_SET
                .with_socket_mut::<tcp::Socket, _, _>, SOCKET_SET.add, SocketSetWrapper::new_tcp_socket, Some ...(+25) |
| `arceos/modules/axnet/src/smoltcp_impl/dns.rs#query` | `arceos/modules/axnet/src/smoltcp_impl/dns.rs#query` | 同板块同名 | Err, Ok, SOCKET_SET
            .with_socket_mut::<dns::Socket, _, _>, SOCKET_SET
            .with_socket_mut::<dns::Socket, _, _>(handle, |socket| {
                socket.start_query(iface.lock().context(), name, query_type)
            })
            .map_err, SOCKET_SET.poll_interfaces, SOCKET_SET.with_socket_mut::<dns::Socket, _, _> ...(+12) | Err, Ok, SOCKET_SET
            .with_socket_mut::<dns::Socket, _, _>, SOCKET_SET
            .with_socket_mut::<dns::Socket, _, _>(handle, |socket| {
                socket.start_query(iface.lock().context(), name, query_type)
            })
            .map_err, SOCKET_SET.poll_interfaces, SOCKET_SET.with_socket_mut::<dns::Socket, _, _> ...(+12) |
| `arceos/modules/axnet/src/smoltcp_impl/bench.rs#bench_transmit_bandwidth` | `arceos/modules/axnet/src/smoltcp_impl/bench.rs#bench_transmit_bandwidth` | 同板块同名同结构 | (current_time - past_time).secs, AxNetTxToken::consume, InterfaceWrapper::current_time, info!, self.transmit, tx_buf[0..12].fill ...(+2) | (current_time - past_time).secs, AxNetTxToken::consume, InterfaceWrapper::current_time, info!, self.transmit, tx_buf[0..12].fill ...(+2) |
| `arceos/modules/axnet/src/smoltcp_impl/tcp.rs#bind` | `arceos/modules/axnet/src/smoltcp_impl/udp.rs#bind` | 同板块同名 | Ok, SOCKET_SET.add, SOCKET_SET.tcp_bind_check, SOCKET_SET.with_socket_mut::<tcp::Socket, _, _>, SocketSetWrapper::new_tcp_socket, ax_err! ...(+15) | (!is_unspecified(local_endpoint.addr)).then_some, Ok, SOCKET_SET.with_socket_mut::<udp::Socket, _, _>, Some, ax_err!, debug! ...(+9) |

#### 通用模块/其他
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `arceos/ulib/axlibc/c/printf.c#print_exponential_number` | `arceos/ulib/axlibc/c/printf.c#print_exponential_number` | 同板块同名同结构 | ABS_FOR_PRINTING, PRINTF_ABS, bastardized_floor, get_components, get_normalized_components, get_sign_bit ...(+5) | ABS_FOR_PRINTING, PRINTF_ABS, bastardized_floor, get_components, get_normalized_components, get_sign_bit ...(+5) |
| `arceos/ulib/axlibc/c/pow.c#pow` | `arceos/ulib/axlibc/c/pow.c#pow` | 同板块同名同结构 | __builtin_fma, __math_invalid, __math_oflow, __math_uflow, asdouble, asuint64 ...(+8) | __builtin_fma, __math_invalid, __math_oflow, __math_uflow, asdouble, asuint64 ...(+8) |
| `arceos/ulib/axlibc/c/string.c#twoway_strstr` | `arceos/ulib/axlibc/c/string.c#twoway_strstr` | 同板块同名同结构 | BITOP, MAX, memchr, memcmp | BITOP, MAX, memchr, memcmp |
| `arceos/ulib/axlibc/c/printf.c#print_broken_up_decimal` | `arceos/ulib/axlibc/c/printf.c#print_broken_up_decimal` | 同板块同名同结构 | out_rev_ | out_rev_ |


#### 1. 宏观判定
- **定性结论**: 独立开发 / 共同依赖开源基线 (相似度: 35.9%，共同 AST 指纹: 1257；唯一 AST 指纹: 目标项目 2816，参考项目 1942；覆盖率: 目标项目 44.64%，参考项目 64.73%)

#### 2. 全模块覆盖矩阵解读
- **进程管理**
  - 双方是否都存在：是
  - 是否存在共同 AST 指纹：是 (31个)
  - Target/Base 覆盖率说明：目标覆盖率26.27%，参考覆盖率44.29%。目标项目在该板块有132个函数，远超参考的76个，且大部分为独有实现，导致其覆盖率较低。
  - 证据强度：弱。共同指纹主要集中在 `arceos/modules/axtask` 等上游调度器模块，属于公共依赖。双方在API封装层（如信号处理、进程创建）存在显著架构差异。

- **任务调度/异步运行时**
  - 双方是否都存在：是
  - 是否存在共同 AST 指纹：是 (197个)
  - Target/Base 覆盖率说明：目标覆盖率61.18%，参考覆盖率72.43%。双方在该板块高度重合，但目标项目函数总数更多（438 vs 383）。
  - 证据强度：中。高覆盖率源于双方均基于 ArceOS 的 `axtask` 模块构建，这是共同的开源根基。证据指向共同来源，而非单向抄袭。

- **内存管理**
  - 双方是否都存在：是
  - 是否存在共同 AST 指纹：是 (222个)
  - Target/Base 覆盖率说明：目标覆盖率54.01%，参考覆盖率80.43%。参考项目覆盖率极高，说明其内存管理实现大部分能在目标项目中找到对应，但目标项目有近一半的独立代码。
  - 证据强度：中。共同指纹主要来自 `arceos/modules/axmm` 和 `axlibc` 等上游模块。双方在 `sys_mmap` 等关键系统调用上存在实现差异。

- **中断/异常处理**
  - 双方是否都存在：是
  - 是否存在共同 AST 指纹：是 (47个)
  - Target/Base 覆盖率说明：目标覆盖率100.0%，参考覆盖率94.0%。覆盖率极高，几乎完全重合。
  - 证据强度：强（指向共同来源）。此模块代码几乎完全来自 ArceOS 的 `axhal` 硬件抽象层，包含大量架构定义和标准中断处理流程，是使用该开源系统的必然结果。

- **系统调用**
  - 双方是否都存在：是
  - 是否存在共同 AST 指纹：是 (34个)
  - Target/Base 覆盖率说明：目标覆盖率59.65%，参考覆盖率34.69%。目标项目覆盖率显著高于参考，说明目标项目实现了更多与参考项目兼容的系统调用。
  - 证据强度：弱。共同指纹多为 `syscall_body!` 等宏或标准调用模式。双方在具体系统调用实现（如 `sys_getaddrinfo`）上差异巨大，目标项目有更完整的实现。

- **文件系统**
  - 双方是否都存在：是
  - 是否存在共同 AST 指纹：是 (151个)
  - Target/Base 覆盖率说明：目标覆盖率22.54%，参考覆盖率32.54%。双方覆盖率均较低，表明文件系统是各自独立扩展的重点区域。
  - 证据强度：弱。共同指纹主要来自 `axlibc` 的C库函数（如 `glob.c`, `pow.c`）和 `setjmp` 等底层实现，属于公共依赖。

- **设备驱动**
  - 双方是否都存在：是
  - 是否存在共同 AST 指纹：是 (115个)
  - Target/Base 覆盖率说明：目标覆盖率93.5%，参考覆盖率94.26%。覆盖率极高，几乎完全重合。
  - 证据强度：强（指向共同来源）。代码几乎完全来自 ArceOS 的 `axdriver` 和 `tools` 模块，包含VirtIO驱动、基准测试工具等，是使用该开源系统的标准配置。

- **网络协议栈**
  - 双方是否都存在：是
  - 是否存在共同 AST 指纹：是 (50个)
  - Target/Base 覆盖率说明：目标覆盖率89.29%，参考覆盖率89.29%。覆盖率极高且完全相等。
  - 证据强度：强（指向共同来源）。代码来自 ArceOS 的 `axnet` 模块及 smoltcp 协议栈的集成代码，属于共同依赖的开源网络栈实现。

- **第三方库/公共依赖**
  - 双方是否都存在：是
  - 是否存在共同 AST 指纹：文件系统/第三方库为0，网络协议栈/第三方库为72。
  - Target/Base 覆盖率说明：文件系统/第三方库覆盖率为0，说明双方使用的第三方库（如 lwext4_rust）版本或集成方式完全不同。网络协议栈/第三方库覆盖率较高（61.54% / 79.12%），指向共同的 smoltcp 等网络库依赖。
  - 证据强度：文件系统/第三方库为未形成；网络协议栈/第三方库为强（指向共同来源）。

#### 3. 底层 AST 关键证据指纹表
| 所属板块 | 目标项目路径 | 参考项目路径 | 证据类型 | 技术说明 |
| :--- | :--- | :--- | :--- | :--- |
| 中断/异常处理 | `arceos/modules/axhal/src/arch/aarch64/trap.rs#handle_sync_exception` | `arceos/modules/axhal/src/arch/aarch64/trap.rs#handle_sync_exception` | 同源指纹 | 完全相同的中断分发逻辑，包括 `ESR_EL1.extract` 和 `esr.read_as_enum` 等底层硬件操作，是 ArceOS 官方架构代码。 |
| 设备驱动 | `arceos/modules/axdriver/src/lib.rs#init_drivers` | `arceos/modules/axdriver/src/lib.rs#init_drivers` | 同源指纹 | 完全相同的驱动初始化流程，遍历 `AllDevices` 并枚举 block/net/display 设备，是 ArceOS 官方驱动框架。 |
| 网络协议栈 | `arceos/modules/axnet/src/smoltcp_impl/dns.rs#query` | `arceos/modules/axnet/src/smoltcp_impl/dns.rs#query` | 同源指纹 | 完全相同的 DNS 查询实现，包括 `SOCKET_SET.with_socket_mut` 和 `socket.start_query` 的调用流，是 ArceOS 对 smoltcp 的官方集成代码。 |
| 任务调度/异步运行时 | `arceos/modules/axtask/src/run_queue.rs#switch_to` | `arceos/modules/axtask/src/run_queue.rs#switch_to` | 同源指纹 | 完全相同的任务上下文切换逻辑，包括 `(*prev_ctx_ptr).switch_to` 和 `CurrentTask::set_current` 等核心操作，是 ArceOS 官方调度器实现。 |
| 内存管理 | `arceos/modules/axmm/src/aspace.rs#try_clone` | `arceos/modules/axmm/src/aspace.rs#try_clone` | 同源指纹 | 完全相同的地址空间克隆逻辑，包括 `PageIterWrapper::new` 和 `Backend::new_alloc` 的调用序列，是 ArceOS 官方内存管理实现。 |
| 进程管理 | `api/src/imp/task/signal.rs#sys_rt_sigprocmask` | `api/src/imp/signal.rs#sys_rt_sigprocmask` | 架构差异 | 目标使用 `current_thread_data()` 统一入口；参考使用 `current().task_ext().thread_data()` 逐层钻取。展示了不同的抽象封装策略。 |
| 系统调用 | `api/src/imp/net/socket.rs#sys_getaddrinfo` | `arceos/api/arceos_posix_api/src/imp/net.rs#sys_getaddrinfo` | 架构差异 | 目标有完整的 `SocketAddrV4::new` 和 `axnet::dns_query` 实现；参考仅为一个 `syscall_body!` 宏调用。实现完整度天差地别。 |
| 文件系统 | `modules/lwext4_rust/src/inode/file.rs#read_at` | `arceos/modules/axfs/src/fs/lwext4_rust.rs#read_at` | 架构差异 | 目标直接操作 inode 指针和 `WritebackGuard`；参考通过 `file.file_open` 等高层封装。表明双方对 lwext4 库的集成方式完全不同。 |
| 第三方库/公共依赖 | `arceos/ulib/axlibc/c/printf.c#format_string_loop` | `arceos/ulib/axlibc/c/printf.c#format_string_loop` | 同源指纹 | 完全相同的格式化字符串循环逻辑，是来自 musl 或其他C库的公共代码，非任何一方原创。 |

#### 4. 核心代码调用流对比剖析
* **【进程管理/信号处理路径】**
* **底层技术析理**: 双方均实现了 `sys_rt_sigprocmask` 系统调用，但采用了不同的抽象路径。目标项目封装了 `current_thread_data()` 辅助函数来直接获取线程数据，调用流更扁平。参考项目则遵循 `current().task_ext().thread_data()` 的层级调用，暴露了更多的内部结构。这表明双方在进程/线程数据模型的设计上存在独立决策。
* **代码溯源链接**:
  - 目标项目: `api/src/imp/task/signal.rs#sys_rt_sigprocmask`
  - 参考项目: `api/src/imp/signal.rs#sys_rt_sigprocmask`

* **【系统调用/网络路径】**
* **底层技术析理**: 对于 `sys_getaddrinfo`，目标项目实现了完整的地址解析逻辑，直接构造 `SocketAddrV4` 并调用 `axnet::dns_query`。而参考项目仅使用 `syscall_body!` 宏，表明其并未提供真实的实现。这揭示了双方在网络系统调用完整度上的根本性差异。
* **代码溯源链接**:
  - 目标项目: `api/src/imp/net/socket.rs#sys_getaddrinfo`
  - 参考项目: `arceos/api/arceos_posix_api/src/imp/net.rs#sys_getaddrinfo`

* **【文件系统/第三方库集成路径】**
* **底层技术析理**: 双方都集成了 lwext4_rust 库，但 `read_at` 的实现方式截然不同。目标项目直接操作底层 inode 指针并进行内存拷贝，属于较底层的集成。参考项目则通过 `file.file_open`、`file.file_read` 等更贴近 lwext4 原生接口的函数进行操作，抽象层次更高。这证明双方独立完成了对该第三方库的适配。
* **代码溯源链接**:
  - 目标项目: `modules/lwext4_rust/src/inode/file.rs#read_at`
  - 参考项目: `arceos/modules/axfs/src/fs/lwext4_rust.rs#read_at`

#### 5. 单方模块与降权项
- **单方模块**:
  - **目标项目独有**: `文件系统/第三方库` 模块中，目标项目有 647 个函数，而参考项目仅有 25 个。这表明目标项目集成了大量参考项目未使用的第三方文件系统库（如 lwext4_rust 的独立封装），是其独立开发工作的体现。
- **降权项**:
  - **跨板块同名函数**: 如目标项目的 `process/src/process.rs#exit` 与参考项目的 `arceos/modules/axtask/src/api.rs#exit`，虽同名但分属进程管理和任务调度两个不同板块，实现逻辑完全不同，不构成同源证据。
  - **Trivial 代码**: 如 `unimplemented`、`println!`、`AtomicBool::load` 等调用，是语言层面的惯用法或占位符，在任何独立项目中都会产生相同的 AST 指纹，不具备区分度。
  - **公共接口/泛型函数**: 如 `VecDeque::with_capacity`、`SpinNoIrq::new` 等标准库或通用内核原语，是构建任何并发系统的基础，不能作为抄袭证据。

#### 6. 保守结论
综合评估，目标项目与参考项目的整体 AST 相似度为 35.9%，处于较低水平。高相似度模块（中断处理、设备驱动、网络协议栈）的代码均明确指向 ArceOS 开源生态系统及其依赖的第三方库（如 smoltcp、musl），属于共同来源，而非单向抄袭。在核心的进程管理、系统调用和文件系统板块，双方在抽象层次、实现完整度和第三方库集成方式上表现出显著差异。目标项目拥有 2816 个唯一 AST 指纹，远超参考项目的 1942 个，且目标覆盖率更低，这有力证明了其包含了大量独立开发的代码。因此，现有证据不支持抄袭指控，项目应被判定为独立开发，同源性风险低。

------------------------------

