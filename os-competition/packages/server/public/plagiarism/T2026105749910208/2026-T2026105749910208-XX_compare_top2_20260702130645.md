# 🛡️ OS 内核赛自动化代码级架构审计报告

**审计时间**: `2026-07-02 13:06:45`
**参赛作品名称**: `2026-T2026105749910208-XX`
**年份**: `2026`
**学校**: `XX`
**队伍名称**: `T2026105749910208`
**比赛名字**: `OS-Competition`
**比赛子赛道**: `Kernel-Implementation`
**仓库地址**: `https://gitlab.eduxiji.net/educg-group-43501-3132633/T2026105749910208-1758`
**推测开发语言**: `Rust / C (由AST探针自动识别)`
**特征函数量**: `2026`

## ⚔️ 底层代码同源性对比 (AST Homology)
> 本部分由专门的代码审查智能体，对该项目与符合年份条件的历届最高相似度项目进行控制流与底层调用对比。新版报告会先给出确定性的全模块覆盖矩阵，再给出 Agent 的分模块解释，避免只围绕少量抽样函数展开。

### 🏆 Top 2 底层对标: vs `2024-你起吧-长春理工大学`（长春理工大学 / 你起吧）

#### 对方作品信息

**对方年份**: `2024`
**对方学校**: `长春理工大学`
**对方队伍名称**: `你起吧`
**对方比赛名字**: `OS-Competition`
**对方比赛子赛道**: `Kernel-Implementation`
**对方仓库地址**: `https://gitlab.eduxiji.net/educg-group-26010-2376550/T202410186992540-1599`

> 双向覆盖率：共同 AST 指纹: 48；唯一 AST 指纹: 目标项目 1855，参考项目 573；覆盖率: 目标项目 2.59%，参考项目 8.38%

## 全模块对比总览（确定性证据）

> 总体覆盖率：共同 AST 指纹: 48；唯一 AST 指纹: 目标项目 1855，参考项目 573；覆盖率: 目标项目 2.59%，参考项目 8.38%

### 1. 模块覆盖矩阵
| 模块 | 目标函数数 | 参考函数数 | 共同唯一 AST 指纹 | Target 覆盖 | Base 覆盖 | 状态 |
| :--- | ---: | ---: | ---: | ---: | ---: | :--- |
| 进程管理 | 110 | 37 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |
| 任务调度/异步运行时 | 335 | 22 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |
| 内存管理 | 479 | 140 | 43 | 9.45% | 32.58% | 双方存在且有结构重合 |
| 中断/异常处理 | 114 | 9 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |
| 系统调用 | 108 | 3 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |
| 文件系统 | 510 | 67 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |
| 设备驱动 | 20 | 14 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |
| 网络协议栈 | 0 | 0 | 0 | 0.0% | 0.0% | 双方均未体现 |
| 文件系统/第三方库 | 160 | 323 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |
| 通用模块/其他 | 190 | 71 | 2 | 1.2% | 3.28% | 双方存在且有结构重合 |

### 2. 分模块证据清单

#### 进程管理
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `os/src/syscall/process.rs#sys_getppid` | `kernel/sysproc.c#sys_getppid` | 同板块同名 | Ok, current_task, current_task().ok_or, inner
        .parent
        .as_ref, inner
        .parent
        .as_ref()
        .map, inner
        .parent
        .as_ref()
        .map(|p| p.thread_group.tgid())
        .unwrap_or ...(+3) | myproc |
| `os/src/syscall/process.rs#sys_getpid` | `kernel/sysproc.c#sys_getpid` | 同板块同名 | Err, Ok, current_task, log::debug!, task.thread_group.tgid | myproc |
| `os/src/syscall/process.rs#sys_sched_yield` | `kernel/sysproc.c#sys_sched_yield` | 同板块同名 | Ok, crate::trap::foreground_driver_active, log::debug!, suspend_current_and_run_next, yield_current_once | yield |
| `os/src/syscall/process.rs#sys_exit_group` | `kernel/sysproc.c#sys_exit_group` | 同板块同名 | Ok, exit_thread_group_and_run_next, log::info! | argint, exit |

#### 任务调度/异步运行时
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `os/src/syscall/fs.rs#sys_dup3` | `kernel/sysfile.c#sys_dup3` | 同板块同名 | Err, Ok, current_task, current_task().ok_or, drop, fds.dup2_below ...(+8) | argfd, argint, filedup, myproc |
| `os/src/fs/vfs.rs#open_path` | `kernel/virtio_disk.c#virtio_disk_intr` | 同模块上下文对齐/差异观察 | Err, MEM_FS
                .lock, MEM_FS
                .lock()
                .add_file_with_mode, MEM_FS.lock, MEM_FS.lock().get_file, MEM_FS.lock().get_file(&open_norm).is_some ...(+55) | R, __sync_synchronize, acquire, free_chain, panic, release ...(+1) |
| `os/src/fs/fd.rs#read` | `kernel/trap.c#usertrap` | 同模块上下文对齐/差异观察 | (*offset).saturating_add, Err, Ok, backing.content.len, backing.content.read_at, backing.lock ...(+31) | devintr, exit, intr_on, killed, myproc, panic ...(+10) |
| `os/src/syscall/fs.rs#sys_splice` | `kernel/console.c#consoleintr` | 同模块上下文对齐/差异观察 | (len - copied).min, Err, Ok, Vec::new, checked_fixed_offset, checked_io_count ...(+32) | C, acquire, consputc, procdump, release, wakeup |

#### 内存管理
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `os/src/syscall/process.rs#sys_execve` | `kernel/sysfile.c#sys_execve` | 同板块同名 | ElfFile::choose_interpreter_bias, ElfFile::parse, Err, Ok, String::from, VirtAddr::new ...(+56) | NELEM, argaddr, argstr, execve, fetchaddr, fetchstr ...(+3) |
| `os/src/syscall/fs.rs#sys_fcntl` | `kernel/sysfile.c#sys_fcntl` | 同板块同名 | Err, Ok, current_task, current_task().ok_or, fd_status_flags, fds.alloc_from_with_flags_below ...(+21) | argfd, fdalloc, filedup, myproc |
| `os/src/syscall/fs.rs#sys_pipe2` | `kernel/sysfile.c#sys_pipe2` | 同板块同名 | Err, Ok, copy_to_user, core::mem::size_of_val, core::slice::from_raw_parts, crate::fs::fd::create_pipe ...(+9) | copyout, fdalloc, fileclose, myproc, pipealloc |
| `offline_crate_sources/buddy_system_allocator/src/lib.rs#alloc` | `third/buddy_system_allocator/src/lib.rs#alloc` | 同板块同名同结构 | (class + 1..i + 1).rev, Err, NonNull::new, Ok, layout.align, layout.size ...(+11) | (class + 1..i + 1).rev, Err, NonNull::new, Ok, layout.align, layout.size ...(+11) |

#### 中断/异常处理
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `patch/polyhal-trap/src/trap/loongarch64/unaligned.rs#emulate_load_store_insn` | `kernel/plic.c#plicinithart` | 同模块上下文对齐/差异观察 | badv::read, badv::read().vaddr, core::arch::asm!, panic!, unaligned_read, unaligned_write | PLIC_SENABLE, PLIC_SPRIORITY, cpuid |
| `patch/polyhal-trap/src/trapframe/x86_64.rs#index_mut` | `kernel/spinlock.c#pop_off` | 同模块上下文对齐/差异观察 | (self.rsp as *mut usize).as_mut, (self.rsp as *mut usize).as_mut().unwrap, log::warn!, unreachable! | intr_get, intr_on, mycpu, panic |
| `patch/polyhal-trap/src/trap/loongarch64/unaligned.rs#unaligned_read` | `kernel/plic.c#plicinit` | 同模块上下文对齐/差异观察 | naked_asm! | 内部闭环逻辑 |
| `patch/polyhal-trap/src/trap/riscv64.rs#uservec` | `kernel/spinlock.c#push_off` | 同模块上下文对齐/差异观察 | naked_asm! | intr_get, intr_off, mycpu |

#### 系统调用
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `os/src/syscall/net.rs#sys_setsockopt` | `kernel/syscall.c#argstr` | 同模块上下文对齐/差异观察 | Err, Ok, copy_i32_option_in, copy_i32_option_in(optval, optlen)?.max, copy_timeval_option_in, socket.is_stream ...(+3) | argaddr, fetchstr |
| `os/src/syscall/other.rs#sys_futex_stub` | `kernel/syscall.c#argaddr` | 同模块上下文对齐/差异观察 | Err, Ok, Some, deadline_from_absolute_timespec_ptr, deadline_from_timespec_ptr, futex_requeue_addr ...(+4) | argraw |
| `os/src/syscall/net.rs#sys_getsockopt` | `kernel/syscall.c#argint` | 同模块上下文对齐/差异观察 | Err, Ok, copy_i32_option_out, core::mem::size_of::<i32>, drop, socket.is_stream ...(+4) | argraw |

#### 文件系统
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `os/src/fs/block_dev.rs#write_offset` | `src/fs.rs#write_offset` | 同板块同名 | log::warn!, self.write_at, self.write_at(offset, data).is_err | bwriteoffset, data.as_ptr, data.len |
| `os/src/fs/block_dev.rs#read_offset` | `src/fs.rs#read_offset` | 同板块同名 | log::warn!, self.read_at, self.read_at(offset, &mut buf).is_err, vec! | breadoffset, trace!, v.as_mut_ptr, v.truncate, vec! |
| `os/src/syscall/fs.rs#sys_openat` | `kernel/sysfile.c#sys_openat` | 同板块同名 | open_resolved_path, resolve_host_path | argint, argstr, openat |
| `os/src/syscall/fs.rs#sys_unlink` | `kernel/sysfile.c#sys_unlink` | 同板块同名 | sys_unlinkat | argstr, unlinkat |

#### 设备驱动
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `os/src/drivers/virtio_mmio_blk.rs#probe_first_virtio_disk_from_dt` | `kernel/uart.c#uartinit` | 同模块上下文对齐/差异观察 | Arc::new, Some, VirtioMmioBlock::attach, compat.all, compat.all().any, core::slice::from_raw_parts ...(+8) | WriteReg, initlock |
| `patch/polyhal/src/debug_console/riscv64.rs#getchar` | `kernel/uart.c#uartstart` | 同模块上下文对齐/差异观察 | Some, core::ptr::read_volatile | ReadReg, WriteReg, wakeup |
| `patch/polyhal/src/boards/qemu.rs#init_device` | `kernel/uart.c#uartputc` | 同模块上下文对齐/差异观察 | sstatus::set_sum | acquire, release, sbi_console_putchar, sleep, uartstart |
| `os/src/main.rs#early_la_putchar` | `kernel/console.c#consoleinit` | 同模块上下文对齐/差异观察 | core::ptr::read_volatile, core::ptr::write_volatile | initlock, initsleeplock, uartinit |

#### 文件系统/第三方库
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `os/src/fs/ext4_vol.rs#rename_ext4` | `third/ext4_rs/src/ext4_impls/extents.rs#extent_remove_space` | 同模块上下文对齐/差异观察 | Err, Ok, ROOT_EXT4.lock, ROOT_EXT4.lock().clone, ROOT_EXT4.lock().clone().ok_or, cached_dir_entries ...(+32) | Block::load, Ext4Extent::default, Ok, ex.get_actual_len, ex.get_first_block, ex.get_pblock ...(+16) |
| `os/src/fs/ext4_vol.rs#unlink_non_dir` | `third/ext4_rs/src/ext4_impls/extents.rs#ext_remove_leaf` | 同模块上下文对齐/差异观察 | Err, Ok, PENDING_UNLINK_REGULAR.lock, PENDING_UNLINK_REGULAR.lock().insert, ROOT_EXT4.lock, ROOT_EXT4.lock().clone ...(+26) | Block::load, Block::load_inode_root_block, Ext4Extent::default, Ok, ex.get_actual_len, ex.get_pblock ...(+14) |
| `os/src/fs/ext4_vol.rs#mount_block_device` | `third/ext4_rs/src/ext4_impls/file.rs#write_at` | 同模块上下文对齐/差异观察 | Arc::new, Ext4::open, ROOT_EXT4.lock, Some, block0.len, clear_all_caches ...(+8) | Block::load, Ok, block.sync_blk_to_disk, block.write_offset, drop, inode_ref
                .inode
                .set_size ...(+10) |
| `os/src/fs/ext4_vol.rs#apply_writeback_snapshot` | `third/ext4_rs/src/ext4_impls/balloc.rs#balloc_alloc_block` | 同模块上下文对齐/差异观察 | DATA_TIME_OVERRIDES.lock, DATA_TIME_OVERRIDES.lock().remove, Err, Ok, ROOT_EXT4.lock, ROOT_EXT4.lock().clone ...(+17) | Block::load, Ext4BlockGroup::load_new, Ok, block_group.get_block_bitmap_block, block_group.get_free_blocks_count, block_group.set_block_group_balloc_bitmap_csum ...(+16) |

#### 通用模块/其他
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `os/src/logging.rs#log` | `src/logging.rs#log` | 同板块同名 | println!, record.level, record.metadata, self.enabled | println!, record.level, record.metadata, self.enabled |
| `os/src/console.rs#write_str` | `src/console.rs#write_str` | 同板块同名 | DebugConsole::putchar, Ok, s.bytes | Ok, consputc, s.bytes |
| `patch/polyhal/src/utils/percpu.rs#deref` | `src/pool.rs#deref` | 同板块同名 | self.get_mut_ptr, self.get_mut_ptr().as_ref, self.get_mut_ptr().as_ref().unwrap | self.target
                .get, self.target
                .get()
                .as_ref, self.target
                .get()
                .as_ref()
                .unwrap_unchecked, self.target
                .get()
                .as_ref()
                .unwrap_unchecked()
                .1
                .as_ref, self.target
                .get()
                .as_ref()
                .unwrap_unchecked()
                .1
                .as_ref()
                .unwrap |
| `patch/polyhal/src/debug_console/mod.rs#enabled` | `src/logging.rs#enabled` | 同板块同名 | 内部闭环逻辑 | 内部闭环逻辑 |


好的，我将遵循审计指令，基于提供的客观证据，对本次同源性定性仲裁进行深度析理。

### 1. 宏观判定
- **定性结论**: **独立开发** (相似度: 2.02%，共同 AST 指纹: 48；唯一 AST 指纹: 目标项目 1855，参考项目 573；覆盖率: 目标项目 2.59%，参考项目 8.38%)

### 2. 全模块覆盖矩阵解读
- **进程管理**
  1. 双方均存在该模块。
  2. 不存在共同 AST 指纹。
  3. 目标覆盖率 0.0%，参考覆盖率 0.0%。这表明尽管双方都实现了进程管理功能，但在 AST 结构层面无任何重合，属于完全异构的实现。
  4. 该模块证据强度：**未形成**。控方所有指控均基于功能语义的相似性，而非 AST 结构重合，不构成有效证据。

- **任务调度/异步运行时**
  1. 双方均存在该模块。
  2. 不存在共同 AST 指纹。
  3. 目标覆盖率 0.0%，参考覆盖率 0.0%。双方调度机制在 AST 层面无任何共同结构。
  4. 该模块证据强度：**未形成**。

- **内存管理**
  1. 双方均存在该模块。
  2. 存在共同 AST 指纹 (43个)。
  3. 目标覆盖率 9.45%，参考覆盖率 32.58%。这是唯一存在结构性重合的模块，但覆盖率差异巨大，表明重合部分在参考项目中占比较高，而在目标项目中占比很低。
  4. 该模块证据强度：**中**。重合指纹主要源于第三方库，需进一步分析。

- **中断/异常处理**
  1. 双方均存在该模块。
  2. 不存在共同 AST 指纹。
  3. 目标覆盖率 0.0%，参考覆盖率 0.0%。双方在 Trap 处理、中断控制器驱动等底层机制上无结构重合。
  4. 该模块证据强度：**未形成**。

- **系统调用**
  1. 双方均存在该模块。
  2. 不存在共同 AST 指纹。
  3. 目标覆盖率 0.0%，参考覆盖率 0.0%。系统调用分发与实现路径在 AST 层面完全不同。
  4. 该模块证据强度：**未形成**。

- **文件系统**
  1. 双方均存在该模块。
  2. 不存在共同 AST 指纹。
  3. 目标覆盖率 0.0%，参考覆盖率 0.0%。目标项目实现了复杂的 ext4 支持，参考项目为简单的 inode 文件系统，结构差异巨大。
  4. 该模块证据强度：**未形成**。

- **设备驱动**
  1. 双方均存在该模块。
  2. 不存在共同 AST 指纹。
  3. 目标覆盖率 0.0%，参考覆盖率 0.0%。驱动模型（如 VirtIO vs 直接 MMIO）和硬件平台差异导致无结构重合。
  4. 该模块证据强度：**未形成**。

- **网络协议栈**
  1. 双方均未体现该模块。
  2. 不存在共同 AST 指纹。
  3. 无覆盖率数据。
  4. 该模块证据强度：**未形成**。

- **第三方库/公共依赖**
  1. 双方均存在该模块。
  2. 存在共同 AST 指纹。
  3. 目标覆盖率 0.0%，参考覆盖率 0.0%。虽然模块整体覆盖率显示为0，但内存管理模块的重合指纹主要来自此处的 `buddy_system_allocator` 库。
  4. 该模块证据强度：**弱**。重合源于共同依赖的开源第三方库，是独立开发的正常现象。

### 3. 底层 AST 关键证据指纹表
| 所属板块 | 目标项目路径 | 参考项目路径 | 证据类型 | 技术说明 |
| :--- | :--- | :--- | :--- | :--- |
| 第三方库/公共依赖 | `offline_crate_sources/buddy_system_allocator/src/lib.rs#alloc` | `third/buddy_system_allocator/src/lib.rs#alloc` | 同板块同名同结构 | 双方均使用了开源的 `buddy_system_allocator` 伙伴系统内存分配器。其核心分配逻辑 `alloc` 函数的 AST 调用图谱完全一致，这是本次审计中发现的唯一强结构重合点，但属于对公共第三方库的正常引用，不构成业务逻辑抄袭。 |
| 通用模块/其他 | `os/src/logging.rs#log` | `src/logging.rs#log` | 同板块同名 | 双方均实现了简单的日志记录函数，调用图谱为 `println!` 和 `record.level` 等 Rust 标准库或常见模式，属于通用功能的标准实现。 |
| 通用模块/其他 | `os/src/console.rs#write_str` | `src/console.rs#write_str` | 同板块同名 | 双方均实现了向控制台输出字符串的函数，核心逻辑为遍历字节并调用底层 `putchar` 或 `consputc`。这是实现 `fmt::Write` trait 的标准范式，属于语言层面的强制约定。 |

### 4. 核心代码调用流对比剖析
* **【进程管理/进程退出路径】**
* **底层技术析理**: 目标项目的 `exit_current_and_run_next` 函数展示了 Rust 语言下基于 `Arc<Mutex>` 的多线程进程组退出模型。其调用流涉及 `foreground_driver_active` 检查、`is_kernel_task` 判断、`finish_task_exit`（刷新控制台、清理 futex、记录退出状态）和 `finish_process_exit`（释放文件锁、通知子进程、唤醒等待者、分离共享内存），最终调用 `run_next_task`。参考项目的 `exit` 函数则是典型的 C/xv6 风格单线程进程退出模型，调用流为 `fileclose`、`reparent`、`wakeup`，最后可能调用 `sbi_shutdown`。两者在**内存模型**（`Arc<Mutex>` 共享所有权 vs 全局 `proc` 数组）、**同步策略**（基于等待队列的 `sleep_on_child_exit` vs 基于 `sleep`/`wakeup` 的全局唤醒）和**对象管理模型**（线程组、任务、子进程事件 vs 单一 `proc` 结构）上存在根本性架构差异，无同源迹象。
* **代码溯源链接**:
  - 目标项目: `os/src/task/mod.rs#exit_current_and_run_next`
  - 参考项目: `kernel/proc.c#exit`

* **【内存管理/伙伴分配器】**
* **底层技术析理**: 双方在 `buddy_system_allocator` 库的 `alloc` 函数上表现出完全一致的 AST 指纹。调用图谱均为 `(class + 1..i + 1).rev` 循环向上合并伙伴块，使用 `NonNull::new` 返回指针，并处理 `layout.align` 和 `layout.size`。这是对同一开源第三方库的直接复用，属于公共依赖，不构成业务层面的抄袭。
* **代码溯源链接**:
  - 目标项目: `offline_crate_sources/buddy_system_allocator/src/lib.rs#alloc`
  - 参考项目: `third/buddy_system_allocator/src/lib.rs#alloc`

### 5. 单方模块与降权项
- **单方模块**: 目标项目中的 `文件系统/第三方库` 模块（160个函数）包含了大量 ext4 文件系统的实现逻辑，而参考项目无此实现。参考项目中的 `文件系统/第三方库` 模块（323个函数）可能包含了不同的第三方库或工具集。这些单方存在的模块是独立开发的直接证据。
- **降权项**:
  - **公共接口名**: `sys_getpid`、`sys_exit` 等函数名是 POSIX/Linux 系统调用标准 ABI 名称，任何实现兼容层的操作系统都必须使用。此类同名是接口契约，不构成同源证据。
  - **第三方库**: `buddy_system_allocator` 是双方共同依赖的开源库，其产生的 AST 指纹重合是正常现象，必须从核心业务逻辑的原创性判定中剥离。
  - **通用功能**: `logging.rs` 和 `console.rs` 中的函数实现了极其通用的功能，其调用图谱（如 `println!`）是语言和标准库提供的标准范式，不能作为强同源证据。

### 6. 保守结论
本次审计的客观 AST 相似度仅为 2.02%，处于极低水平。双方在进程管理、任务调度、文件系统等核心业务模块的 AST 指纹覆盖率为 0%，表明其底层实现结构完全不同。唯一的结构性重合点源于对 `buddy_system_allocator` 这一开源第三方库的共同依赖。控方基于功能语义相似性提出的指控，在缺乏 AST 结构证据支撑的情况下，无法对抗辩方关于架构异构性的论证。综合来看，目标项目与参考项目在底层架构、内存模型、并发策略和系统调用实现上存在系统性差异，整体风险极低，符合独立开发的特征。

------------------------------

