# 🛡️ OS 内核赛自动化代码级架构审计报告

**审计时间**: `2026-07-02 13:06:45`
**参赛作品名称**: `2026-T2026105749910208-XX`
**年份**: `2026`
**学校**: `XX`
**队伍名称**: `T2026105749910208`
**比赛名字**: `OS-Competition`
**比赛子赛道**: `Kernel-Implementation`
**仓库地址**: `https://gitlab.eduxiji.net/educg-group-43501-3132633/T2026105749910208-1758`
**推测开发语言**: `Rust / C (由AST探针自动识别)`
**特征函数量**: `2026`

## ⚔️ 底层代码同源性对比 (AST Homology)
> 本部分由专门的代码审查智能体，对该项目与符合年份条件的历届最高相似度项目进行控制流与底层调用对比。新版报告会先给出确定性的全模块覆盖矩阵，再给出 Agent 的分模块解释，避免只围绕少量抽样函数展开。

### 🏆 Top 5 底层对标: vs `2023-Starry-清华大学`（清华大学 / Starry）

#### 对方作品信息

**对方年份**: `2023`
**对方学校**: `清华大学`
**对方队伍名称**: `Starry`
**对方比赛名字**: `OS-Competition`
**对方比赛子赛道**: `Kernel-Implementation`
**对方仓库地址**: `https://gitlab.eduxiji.net/202310003101649/starry`

> 双向覆盖率：共同 AST 指纹: 80；唯一 AST 指纹: 目标项目 1855，参考项目 6206；覆盖率: 目标项目 4.31%，参考项目 1.29%

## 全模块对比总览（确定性证据）

> 总体覆盖率：共同 AST 指纹: 80；唯一 AST 指纹: 目标项目 1855，参考项目 6206；覆盖率: 目标项目 4.31%，参考项目 1.29%

### 1. 模块覆盖矩阵
| 模块 | 目标函数数 | 参考函数数 | 共同唯一 AST 指纹 | Target 覆盖 | Base 覆盖 | 状态 |
| :--- | ---: | ---: | ---: | ---: | ---: | :--- |
| 进程管理 | 110 | 128 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |
| 任务调度/异步运行时 | 335 | 296 | 1 | 0.33% | 0.41% | 双方存在且有结构重合 |
| 内存管理 | 479 | 257 | 1 | 0.22% | 0.43% | 双方存在且有结构重合 |
| 中断/异常处理 | 114 | 40 | 2 | 2.17% | 5.26% | 双方存在且有结构重合 |
| 系统调用 | 108 | 64 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |
| 文件系统 | 510 | 379 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |
| 设备驱动 | 20 | 57 | 3 | 16.67% | 5.88% | 双方存在且有结构重合 |
| 网络协议栈 | 0 | 73 | 0 | 0.0% | 0.0% | 仅参考项目体现 |
| 文件系统/第三方库 | 160 | 487 | 1 | 0.66% | 0.22% | 双方存在且有结构重合 |
| 网络协议栈/第三方库 | 0 | 110 | 0 | 0.0% | 0.0% | 仅参考项目体现 |
| 第三方库 | 0 | 5822 | 0 | 0.0% | 0.0% | 仅参考项目体现 |
| 通用模块/其他 | 190 | 341 | 7 | 4.19% | 2.47% | 双方存在且有结构重合 |

### 2. 分模块证据清单

#### 进程管理
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `os/src/syscall/other.rs#process_robust_list_on_exit` | `ulib/starry_libax/src/syscall/epoll/file.rs#epoll_wait` | 同模块上下文对齐/差异观察 | (entry as isize)
            .checked_add, (entry as isize)
            .checked_add(futex_offset)
            .and_then, Ok, core::mem::size_of::<i32>, core::mem::size_of::<usize>, futex_wake_addr_private_and_shared ...(+15) | EpollEventType::empty, Vec::new, current_process, current_process.inner.lock, events.iter, file.lock ...(+11) |
| `os/src/syscall/process.rs#sys_waitid` | `ulib/starry_libax/src/syscall/task.rs#syscall_exec` | 同模块上下文对齐/差异观察 | Err, Ok, crate::task::wait_queue::sleep_on_child_exit, current_task, current_task().ok_or, has_matching_child ...(+7) | "./busybox".to_string, "busybox".to_string, "entry-dynamic.exe".to_string, "entry-static.exe".to_string, "lmbench_all".to_string, Some ...(+25) |
| `patch/polyhal/src/components/timer/x86_64.rs#timer_wait` | `modules/axprocess/src/process.rs#wait_pid` | 同模块上下文对齐/差异观察 | Port::new, duration.as_millis, pc_speaker.read, pc_speaker.write, pit_ch2.write, pit_cmd.write ...(+1) | Err, Ok, child.get_code_if_exit, curr_process.inner.lock, current_process, exit_code_ptr.is_null ...(+3) |
| `os/src/syscall/process.rs#sys_wait4_thread_group` | `modules/axprocess/src/loader/user_stack.rs#init_stack` | 同模块上下文对齐/差异观察 | Err, Ok, WaitTarget::Tgid, crate::task::wait_queue::sleep_on_child_exit, crate::task::wait_status_from_exit_code, current_task ...(+5) | UserStack::new, arg.as_str, args
        .iter, args
        .iter()
        .map, args
        .iter()
        .map(|arg| stack.push_str(arg.as_str()))
        .collect, args.len ...(+13) |

#### 任务调度/异步运行时
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `os/src/trap/mod.rs#handle_syscall` | `ulib/starry_libax/src/trap.rs#handle_syscall` | 同板块同名 | CURRENT_SYSCALL_CTX_PTR.lock, crate::syscall::signal::handle_pending_for_user, crate::task::current_task, crate::timer::wake_expired_timers, ctx.args, ctx.syscall_ok ...(+12) | axprocess::time_stat_from_kernel_to_user, axprocess::time_stat_from_user_to_kernel, syscall |
| `os/src/task/context.rs#switch_to` | `modules/axtask/src/run_queue.rs#switch_to` | 同板块同名 | core::arch::naked_asm! | (*prev_ctx_ptr).switch_to, CurrentTask::set_current, assert!, next_task.ctx_mut_ptr, next_task.set_preempt_pending, next_task.set_state ...(+3) |
| `os/src/task/context.rs#switch_to` | `modules/axtask/src/monolithic_task/stat.rs#switch_to` | 同板块同名 | core::arch::naked_asm! | current_time_nanos, self.update_timer |
| `patch/polyhal/src/components/kcontext/x86_64.rs#context_switch` | `modules/axhal/src/arch/riscv/context.rs#context_switch` | 同板块同名 | naked_asm! | asm! |

#### 内存管理
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `os/src/main.rs#rust_main` | `modules/axruntime/src/lib.rs#rust_main` | 同板块同名 | AtomicBool::new, INIT_GUARD.swap, PhysAddr::new, add_riscv_available_frames, block_dev::register_virtio_disk, crate::drivers::virtio_pci_blk::probe_pci_virtio_blk ...(+23) | INITED_CPUS.fetch_add, ax_println!, axdisplay::init_display, axdriver::init_drivers, axfs::init_filesystems, axhal::mem::memory_regions ...(+19) |
| `os/src/mm/elf_loader.rs#load` | `modules/axprocess/src/loader/mod.rs#load` | 同板块同名 | (src.len() - src_offset).min, (vaddr + page_size).min, Err, MemorySet::from_kernel, Ok, VirtAddr::new ...(+13) | FilePath::new, Loader::new, Ok, Some, VirtAddr::from, [0 as u8].repeat ...(+30) |
| `os/src/mm/memory_set.rs#handle_page_fault` | `modules/axmem/src/area.rs#handle_page_fault` | 同板块同名 | Err, Ok, Some, VirtAddr::new, align_down, area.flags.contains ...(+30) | PhysPage::alloc, PhysPage::alloc().expect, SeekFrom::Current, Some, addr.align_down_4k, addr.align_down_4k().into ...(+17) |
| `patch/polyhal-boot/src/arch/aarch64.rs#init_mmu` | `modules/axhal/src/platform/aarch64_common/boot.rs#init_mmu` | 同板块同名 | MAIR_EL1.write, SCTLR_EL1.modify, SPSel.write, SP_EL0.set, TCR_EL1.write, TCR_EL1::T0SZ.val ...(+5) | BOOT_PT_L0.as_ptr, MAIR_EL1.write, PhysAddr::from, PhysAddr::from(BOOT_PT_L0.as_ptr() as usize).as_usize, SCTLR_EL1.modify, TCR_EL1.write ...(+6) |

#### 中断/异常处理
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `patch/polyhal/src/components/irq/riscv64.rs#int_enabled` | `modules/axhal/src/arch/riscv/mod.rs#irqs_enabled` | 同板块同结构 | sstatus::read, sstatus::read().sie | sstatus::read, sstatus::read().sie |
| `patch/polyhal-trap/src/trap/riscv64.rs#init_timer` | `modules/axhal/src/arch/aarch64/mod.rs#wait_for_irqs` | 同板块同结构 | polyhal::timer::init | aarch64_cpu::asm::wfi |
| `patch/polyhal-trap/src/trap/loongarch64/unaligned.rs#emulate_load_store_insn` | `ulib/libax/src/cbindings/time.rs#ax_nanosleep` | 同模块上下文对齐/差异观察 | badv::read, badv::read().vaddr, core::arch::asm!, panic!, unaligned_read, unaligned_write | ax_call_body! |
| `patch/polyhal-trap/src/trapframe/x86_64.rs#index_mut` | `modules/axhal/src/arch/x86_64/idt.rs#new` | 同模块上下文对齐/差异观察 | (self.rsp as *mut usize).as_mut, (self.rsp as *mut usize).as_mut().unwrap, log::warn!, unreachable! | InterruptDescriptorTable::new, core::mem::transmute, core::slice::from_raw_parts_mut, entries[i].set_handler_fn |

#### 系统调用
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `os/src/syscall/signal.rs#set` | `ulib/starry_libax/src/syscall/socket.rs#set` | 同板块同名 | 内部闭环逻辑 | <[u8; 4]>::try_from, <[u8; 4]>::try_from(&opt[0..4]).unwrap, Some, axnet::Duration::from_secs, i32::from_ne_bytes, opt.as_ptr ...(+6) |
| `os/src/syscall/signal.rs#get` | `ulib/starry_libax/src/syscall/socket.rs#get` | 同板块同名 | 内部闭环逻辑 | copy_nonoverlapping, keep_alive.to_ne_bytes, panic!, s.keep_alive, s.keep_alive().is_some, s.with_socket ...(+4) |
| `os/src/syscall/net.rs#sys_getsockopt` | `ulib/starry_libax/src/syscall/socket.rs#get` | 同模块上下文对齐/差异观察 | Err, Ok, copy_i32_option_out, core::mem::size_of::<i32>, drop, socket.is_stream ...(+4) | bytes.as_ptr, bytes.len, copy_nonoverlapping, panic!, raw_socket.congestion.as_bytes, size_of::<usize> ...(+2) |
| `os/src/syscall/other.rs#futex_wake_op` | `ulib/starry_libax/src/syscall/socket.rs#accept` | 同模块上下文对齐/差异观察 | Err, Ok, futex_wake_addr_bitset, futex_wake_op_arg, old.wrapping_add, read_user_i32 ...(+2) | Err, Ok, SocketInner::Tcp, String::from, new_socket.peer_addr, s.accept ...(+2) |

#### 文件系统
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `os/src/fs/vfs.rs#metadata` | `modules/axfs/src/api/file.rs#metadata` | 同板块同名 | Err, MEM_FS.lock, Ok, current_times, default_mem_metadata, drop ...(+36) | self.inner.get_attr, self.inner.get_attr().map |
| `os/src/fs/fd.rs#write_at` | `modules/axfs/src/fs/fatfs.rs#write_at` | 同板块同名 | Err, Ok, Self::checked_file_end, backing.content.write_at, backing.lock, backing.times.touch_modified ...(+6) | Ok, SeekFrom::Start, ans.is_err, ans.unwrap, buf.len, buf.to_vec ...(+6) |
| `os/src/fs/fd.rs#read_at` | `modules/axfs/src/fs/fatfs.rs#read_at` | 同板块同名 | Err, Ok, backing.content.len, backing.content.read_at, backing.lock, block_dev::loop_read_at ...(+5) | Ok, SeekFrom::Start, ans.is_err, ans.unwrap, buf.len, buf.to_vec ...(+7) |
| `os/src/fs/vfs.rs#remove_dir` | `modules/axfs/src/root.rs#remove_dir` | 同板块同名 | Err, MEM_FS.lock, Ok, check_delete_access, ensure_mount_writable, ext4_vol::lookup_kind ...(+20) | ROOT_DIR.contains, absolute_path, attr.is_dir, attr.perm, attr.perm().owner_writable, ax_err! ...(+8) |

#### 设备驱动
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `patch/polyhal/src/debug_console/riscv64.rs#getchar` | `modules/axhal/src/platform/pc_x86/uart16550.rs#getchar` | 同板块同名 | Some, core::ptr::read_volatile | Some, self.data.read, self.line_sts, self.line_sts().contains |
| `patch/polyhal/src/debug_console/aarch64.rs#putchar` | `modules/axhal/src/platform/aarch64_common/pl011.rs#putchar` | 同板块同名 | UART.lock, UART.lock().putchar | UART.lock, uart.putchar |
| `os/src/drivers/hal.rs#share` | `modules/axdriver/src/virtio.rs#share` | 同板块同名 | buffer.as_ptr, buffer.as_ptr().cast::<u8>, virt_to_phys | buffer.as_ptr, vaddr.into, virt_to_phys, virt_to_phys(vaddr.into()).into |
| `os/src/drivers/hal.rs#mmio_phys_to_virt` | `modules/axdriver/src/virtio.rs#mmio_phys_to_virt` | 同板块同名 | NonNull::new_unchecked, phys_to_virt_mmio | NonNull::new, NonNull::new(phys_to_virt(paddr.into()).as_mut_ptr()).unwrap, paddr.into, phys_to_virt, phys_to_virt(paddr.into()).as_mut_ptr |

#### 网络协议栈
仅参考项目中观察到该模块代表函数，目标项目没有同模块可对齐证据：
- 参考项目：`ulib/c_libax/src/network.c#inet_pton`，调用摘要：hexval, inet_pton, isdigit, memmove
- 参考项目：`apps/c/udpserver/udpserver.c#main`，调用摘要：bind, htons, inet_pton, ntohs, perror, printf ...(+5)
- 参考项目：`ulib/libax/src/cbindings/socket.rs#ax_recvfrom`，调用摘要：ax_call_body!, debug!
- 参考项目：`ulib/libax/src/cbindings/socket.rs#ax_socket`，调用摘要：ax_call_body!, debug!

#### 文件系统/第三方库
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `os/src/fs/ext4_vol.rs#get` | `extern_crates/rust-fatfs-a3a834e/src/table.rs#get` | 同板块同名 | Some, self
            .runs
            .range, self
            .runs
            .range(..=(ino, lblock))
            .next_back, self
            .runs
            .range(..=(ino, lblock))
            .next_back()
            .map, self.bump_age, self.runs.get_mut | (0x0FFF_FFF7..=0x0FFF_FFFF).contains, FatValue::Data, Ok, Self::get_raw, warn! |
| `os/src/fs/ext4_vol.rs#get` | `extern_crates/rust-fatfs-a3a834e/src/table.rs#get` | 同板块同名 | Some, page.frame.clone, self.bump_age, self.pages.get_mut | FatValue::Data, Ok, Self::get_raw |
| `os/src/fs/ext4_vol.rs#clear` | `extern_crates/rust-fatfs-a3a834e/src/dir.rs#clear` | 同板块同名 | self.pending.clear, self.queued.clear | 内部闭环逻辑 |
| `os/src/fs/ext4_vol.rs#remove` | `extern_crates/rust-fatfs-a3a834e/src/dir.rs#remove` | 同板块同名 | self.queued.remove | DirEntryData::deserialize, Err, Ok, SeekFrom::Current, SeekFrom::Start, Some ...(+15) |

#### 网络协议栈/第三方库
仅参考项目中观察到该模块代表函数，目标项目没有同模块可对齐证据：
- 参考项目：`modules/axnet/src/smoltcp_impl/tcp.rs#connect`，调用摘要：Err, LOOPBACK.try_get, LOOPBACK.try_get().unwrap, Ok, SOCKET_SET.add, SOCKET_SET.with_socket_mut::<tcp::Socket, _, _> ...(+27)
- 参考项目：`modules/axnet/src/smoltcp_impl/dns.rs#query`，调用摘要：Err, Ok, SOCKET_SET
            .with_socket_mut::<dns::Socket, _, _>, SOCKET_SET
            .with_socket_mut::<dns::Socket, _, _>(handle, |socket| {
                socket.start_query(iface.lock().context(), name, query_type)
            })
            .map_err, SOCKET_SET.poll_interfaces, SOCKET_SET.with_socket_mut::<dns::Socket, _, _> ...(+9)
- 参考项目：`modules/axnet/src/smoltcp_impl/tcp.rs#recv_timeout`，调用摘要：Err, Ok, SOCKET_SET.with_socket_mut::<tcp::Socket, _, _>, ax_err!, ax_err_type!, current_ticks ...(+11)
- 参考项目：`modules/axnet/src/smoltcp_impl/tcp.rs#shutdown`，调用摘要：LISTEN_TABLE.unlisten, Ok, SOCKET_SET.poll_interfaces, SOCKET_SET.with_socket_mut::<tcp::Socket, _, _>, debug!, self.handle.get ...(+9)

#### 第三方库
仅参考项目中观察到该模块代表函数，目标项目没有同模块可对齐证据：
- 参考项目：`extern_crates/libz-sys-1.1.8/src/zlib/contrib/inflate86/inffas86.c#inflate_fast`，调用摘要：内部闭环逻辑
- 参考项目：`extern_crates/libz-sys-1.1.8/src/zlib-ng/inflate.c#PREFIX`，调用摘要：BITS, BYTEBITS, CRC2, CRC4, DROPBITS, INFLATE_MARK_HOOK ...(+38)
- 参考项目：`extern_crates/miniz_oxide-0.6.2/src/inflate/core.rs#decompress`，调用摘要：OutputBuffer::from_slice_and_pos, cfg!, generate_state!, in_buf.iter, in_buf.len, in_iter.len ...(+9)
- 参考项目：`extern_crates/cc-1.0.79/src/lib.rs#add_default_flags`，调用摘要："--target=aarch64-unknown-windows-gnu".into, "--target=i686-unknown-windows-gnu".into, "--target=x86_64-unknown-windows-gnu".into, "-Brepro".into, "-DANDROID".into, "-D_ARM_WINAPI_PARTITION_DESKTOP_SDK_AVAILABLE=1".into ...(+103)

#### 通用模块/其他
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `os/src/lang_items.rs#panic` | `modules/axruntime/src/lang_items.rs#panic` | 同板块同名 | core::arch::asm! | axhal::misc::terminate, error! |
| `patch/polyhal/src/debug_console/x86_64.rs#putchar` | `modules/axhal/src/platform/qemu_virt_riscv/console.rs#putchar` | 同板块同名 | com::putchar, graphic::putchar, vga_text::putchar | sbi_rt::legacy::console_putchar |
| `patch/polyhal-boot/src/arch/mod.rs#clear_bss` | `modules/axhal/src/mem.rs#clear_bss` | 同板块同名 | core::slice::from_raw_parts_mut, core::slice::from_raw_parts_mut(
            _sbss as *const () as usize as *mut u128,
            (_ebss as *const () as usize - _sbss as *const () as usize) / size_of::<u128>(),
        )
        .fill, size_of::<u128> | core::slice::from_raw_parts_mut, core::slice::from_raw_parts_mut(sbss as usize as *mut u8, ebss as usize - sbss as usize)
            .fill |
| `patch/polyhal/src/debug_console/x86_64/vga_text.rs#clear` | `modules/axsignal/src/lib.rs#clear` | 同板块同名 | FChar::default, vga_buffer.add, vga_buffer.add(i).write_volatile | self.handlers.iter_mut |


好的，审判长。本庭已收到控辩双方的全部陈述、客观 AST 证据底座以及相关审计指令。现在，本庭将依据客观事实与逻辑，对本案进行最终定性仲裁。

### 1. 宏观判定

- **定性结论**: **独立开发**
- **核心依据**: 客观 AST 相似度仅为 **1.0%**，远低于构成实质性相似的阈值。双方共同 AST 指纹仅有 80 个，而各自拥有上千个唯一指纹（目标 1855，参考 6206）。双向覆盖率极低（目标项目 4.31%，参考项目 1.29%），表明双方代码库在底层实现上存在根本性、系统性的差异。控方指控的“底层逻辑复用”在宏观数据层面不成立。

### 2. 全模块覆盖矩阵解读

| 模块 | 双方存在 | 共同指纹 | 覆盖率解读 | 证据强度 |
| :--- | :---: | :---: | :--- | :--- |
| **进程管理** | 是 | 0 | 目标 0.0% / 参考 0.0%。双方在此核心板块无任何可检测的底层结构重合，直接否定控方所有关于此板块的复用指控。 | **未形成** |
| **任务调度/异步运行时** | 是 | 1 | 目标 0.33% / 参考 0.41%。覆盖率极低，唯一的共同指纹不足以支撑任何复用推断，表明调度器实现独立。 | **弱** |
| **内存管理** | 是 | 1 | 目标 0.22% / 参考 0.43%。覆盖率极低，双方在内存分配、映射等核心机制上实现路径迥异。 | **弱** |
| **中断/异常处理** | 是 | 2 | 目标 2.17% / 参考 5.26%。覆盖率相对较高，但共同指纹源于硬件架构规定的标准操作（如读取 `sstatus` 寄存器），属于领域常识。 | **弱** |
| **系统调用** | 是 | 0 | 目标 0.0% / 参考 0.0%。系统调用是内核与用户态的接口，双方无共同指纹，说明系统调用实现框架和内部逻辑完全不同。 | **未形成** |
| **文件系统** | 是 | 0 | 目标 0.0% / 参考 0.0%。双方在文件系统这一复杂模块上无任何底层结构重合，表明 VFS 设计、具体文件系统驱动实现均独立。 | **未形成** |
| **设备驱动** | 是 | 3 | 目标 16.67% / 参考 5.88%。覆盖率较高，但共同指纹集中在 VirtIO 等标准化硬件驱动框架的 MMIO 和共享内存操作上，属于硬件规范驱动的标准写法。 | **中** |
| **网络协议栈** | 仅参考 | 0 | 目标项目无此模块，无法比较。 | **未形成** |
| **文件系统/第三方库** | 是 | 1 | 目标 0.66% / 参考 0.22%。覆盖率极低，共同指纹源于对第三方库（如 ext4/fatfs）的封装调用，而非核心逻辑复用。 | **弱** |
| **网络协议栈/第三方库** | 仅参考 | 0 | 目标项目无此模块，无法比较。 | **未形成** |
| **第三方库** | 仅参考 | 0 | 目标项目无此模块，无法比较。 | **未形成** |
| **通用模块/其他** | 是 | 7 | 目标 4.19% / 参考 2.47%。共同指纹多源于语言级特性（如 `panic` 处理、`clear_bss`）或标准硬件操作（如 UART 输出），属于 Rust 内核开发的通用模板代码。 | **弱** |

### 3. 底层 AST 关键证据指纹表

| 所属板块 | 目标项目路径 | 参考项目路径 | 证据类型 | 技术说明 |
| :--- | :--- | :--- | :--- | :--- |
| 中断/异常处理 | `patch/polyhal/src/components/irq/riscv64.rs#int_enabled` | `modules/axhal/src/arch/riscv/mod.rs#irqs_enabled` | 同板块同结构 | 均通过读取 RISC-V `sstatus` 寄存器的 `sie` 位来判断中断使能状态，这是 RISC-V 特权架构规范定义的标准操作，非原创逻辑。 |
| 设备驱动 | `os/src/drivers/hal.rs#mmio_phys_to_virt` | `modules/axdriver/src/virtio.rs#mmio_phys_to_virt` | 同板块同名 | 均实现 MMIO 物理地址到虚拟地址的转换，调用 `phys_to_virt` 并构造 `NonNull` 指针，是 VirtIO 驱动开发的通用模式。 |
| 设备驱动 | `os/src/drivers/hal.rs#share` | `modules/axdriver/src/virtio.rs#share` | 同板块同名 | 均实现 VirtIO 设备与驱动共享内存的物理地址获取，核心是 `buffer.as_ptr` 和 `virt_to_phys` 的组合，由 VirtIO 规范决定。 |
| 通用模块/其他 | `os/src/lang_items.rs#panic` | `modules/axruntime/src/lang_items.rs#panic` | 同板块同名 | Rust 内核的 panic 处理入口，目标项目使用 `asm!` 停机，参考项目调用 `axhal::misc::terminate`，是 Rust `#[panic_handler]` 的标准实现差异。 |
| 通用模块/其他 | `patch/polyhal-boot/src/arch/mod.rs#clear_bss` | `modules/axhal/src/mem.rs#clear_bss` | 同板块同名 | 均通过 `core::slice::from_raw_parts_mut` 获取 BSS 段原始指针并填充 0，是几乎所有 Rust 内核启动代码的标准模板。 |
| 任务调度/异步运行时 | `os/src/task/context.rs#switch_to` | `modules/axtask/src/run_queue.rs#switch_to` | 同板块同名 | 任务上下文切换函数，目标项目直接使用 `naked_asm!`，参考项目通过函数指针调用 `(*prev_ctx_ptr).switch_to`，实现层次不同。 |
| 内存管理 | `os/src/main.rs#rust_main` | `modules/axruntime/src/lib.rs#rust_main` | 同板块同名 | 内核主入口，均进行一系列初始化，但调用的子系统（驱动、文件系统、内存）完全不同，是 Rust 内核的通用结构。 |

### 4. 核心代码调用流对比剖析

**【进程管理 / 任务退出路径】**
* **底层技术析理**: 控方指控此路径存在复用，但底层 AST 揭示了截然不同的架构。目标项目的 `exit_current_and_run_next` 是一个高层调度器，它调用 `finish_process_exit` 和 `finish_task_exit` 进行复杂的资源回收，包括刷新控制台缓冲、处理 robust list、唤醒 Futex 等待者、向用户态拷贝数据等，最终调用 `run_next_task`。参考项目的 `exit_current` 则直接在运行队列层面操作，将任务放入 `EXITED_TASKS` 队列，通知 `WAIT_FOR_EXIT` 等待队列，并调用 `axhal::misc::terminate` 或 `block_current`。两者在退出状态机、资源回收粒度和调度器交互模型上完全不同。
* **代码溯源链接**:
  - 目标项目: `os/src/task/mod.rs#exit_current_and_run_next`
  - 参考项目: `modules/axtask/src/run_queue.rs#exit_current`

**【进程管理 / 等待与唤醒路径】**
* **底层技术析理**: 目标项目的 `wake_matching` 服务于其自定义的子进程等待事件队列，通过 `matches` 宏匹配特定事件并调用 `wake_task_token_with` 唤醒。参考项目的 `notify_task` 则直接操作调度器的 `RUN_QUEUE`，通过 `Arc::ptr_eq` 匹配任务后调用 `rq.unblock_task` 解除阻塞。两者操作的队列、匹配条件和唤醒机制均服务于不同的子系统（子进程管理 vs. 核心调度），`Arc::ptr_eq` + `iter().position()` + `remove()` 是 Rust 中从 `Vec<Arc<T>>` 中删除指定元素的惯用法，不能作为复用证据。
* **代码溯源链接**:
  - 目标项目: `os/src/task/wait_queue.rs#wake_matching`
  - 参考项目: `modules/axtask/src/wait_queue.rs#notify_task`

**【内存管理 / 页错误处理路径】**
* **底层技术析理**: 双方均实现了 `handle_page_fault`，但内部逻辑差异巨大。目标项目的处理函数直接操作 `MemorySet`，检查 `area.flags`，并手动进行页对齐和映射操作。参考项目则通过 `PhysPage::alloc` 分配物理页，并使用 `SeekFrom::Current` 等文件操作接口从后端文件读取内容。这表明两者在内存对象模型（显式 `MemorySet` vs. 隐式 `Backend`）和填充策略上存在根本性架构差异。
* **代码溯源链接**:
  - 目标项目: `os/src/mm/memory_set.rs#handle_page_fault`
  - 参考项目: `modules/axmem/src/area.rs#handle_page_fault`

### 5. 单方模块与降权项

* **单方模块**: **网络协议栈**、**第三方库** 仅存在于参考项目中。这体现了参考项目功能更丰富，但无法用于证明目标项目抄袭，因为目标项目根本不包含这些代码。
* **降权项**: 控方提出的“弱信号”及全模块对比中大量的“同板块同名”证据（如 `metadata`、`write_at`、`getchar` 等），其调用图谱差异显著，属于操作系统领域的通用命名和功能划分，不构成同源证据。本庭支持辩方关于“领域通用语义”、“Rust 标准库惯用法”的辩护意见。

### 6. 保守结论

综合全案客观证据，目标项目 `[2026-T2026105749910208-XX]` 与参考项目 `[2023-Starry-清华大学]` 的 AST 相似度仅为 1.0%，且在进程管理、系统调用、文件系统等核心板块的共同指纹为零。控方提出的“底层逻辑复用”指控，实质上是将 POSIX 规范要求、Rust 语言惯用法和操作系统领域通用设计模式误判为抄袭证据。双方在任务调度、内存管理、设备驱动等模块的少量相似点，均能合理地解释为硬件规范驱动或标准编程实践。因此，本庭裁定，无证据表明目标项目对参考项目构成代码抄袭或实质性逻辑复用，双方为独立开发。

------------------------------

