# 🛡️ OS 内核赛自动化代码级架构审计报告

**审计时间**: `2026-07-02 11:16:27`
**参赛作品名称**: `2026-T2026104869911155-XX`
**年份**: `2026`
**学校**: `XX`
**队伍名称**: `T2026104869911155`
**比赛名字**: `OS-Competition`
**比赛子赛道**: `Kernel-Implementation`
**仓库地址**: `https://gitlab.eduxiji.net/educg-group-43501-3132633/T2026104869911155-3856`
**推测开发语言**: `Rust / C (由AST探针自动识别)`
**特征函数量**: `1519`

## ⚔️ 底层代码同源性对比 (AST Homology)
> 本部分由专门的代码审查智能体，对该项目与符合年份条件的历届最高相似度项目进行控制流与底层调用对比。新版报告会先给出确定性的全模块覆盖矩阵，再给出 Agent 的分模块解释，避免只围绕少量抽样函数展开。

### 🏆 Top 3 底层对标: vs `2022-Maturin-清华大学`（清华大学 / Maturin）

#### 对方作品信息

**对方年份**: `2022`
**对方学校**: `清华大学`
**对方队伍名称**: `Maturin`
**对方比赛名字**: `OS-Competition`
**对方比赛子赛道**: `Kernel-Implementation`
**对方仓库地址**: `https://gitlab.eduxiji.net/scPointer/maturin.git`

> 双向覆盖率：共同 AST 指纹: 15；唯一 AST 指纹: 目标项目 1367，参考项目 1491；覆盖率: 目标项目 1.1%，参考项目 1.01%

## 全模块对比总览（确定性证据）

> 总体覆盖率：共同 AST 指纹: 15；唯一 AST 指纹: 目标项目 1367，参考项目 1491；覆盖率: 目标项目 1.1%，参考项目 1.01%

### 1. 模块覆盖矩阵
| 模块 | 目标函数数 | 参考函数数 | 共同唯一 AST 指纹 | Target 覆盖 | Base 覆盖 | 状态 |
| :--- | ---: | ---: | ---: | ---: | ---: | :--- |
| 进程管理 | 99 | 60 | 1 | 1.09% | 1.82% | 双方存在且有结构重合 |
| 任务调度/异步运行时 | 444 | 160 | 4 | 0.94% | 2.86% | 双方存在且有结构重合 |
| 内存管理 | 252 | 275 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |
| 中断/异常处理 | 0 | 4 | 0 | 0.0% | 0.0% | 仅参考项目体现 |
| 系统调用 | 0 | 8 | 0 | 0.0% | 0.0% | 仅参考项目体现 |
| 文件系统 | 629 | 593 | 6 | 1.09% | 1.11% | 双方存在且有结构重合 |
| 设备驱动 | 19 | 15 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |
| 网络协议栈 | 0 | 8 | 0 | 0.0% | 0.0% | 仅参考项目体现 |
| 文件系统/第三方库 | 44 | 0 | 0 | 0.0% | 0.0% | 仅目标项目体现 |
| 第三方库 | 0 | 335 | 0 | 0.0% | 0.0% | 仅参考项目体现 |
| 通用模块/其他 | 32 | 218 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |

### 2. 分模块证据清单

#### 进程管理
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `src/task/mod.rs#sys_execve` | `kernel/src/syscall/process.rs#sys_execve` | 同板块同名 | SyscallResult::Return, self.exec_path_with_user_args, self.resolve_user_path | sys_exec |
| `src/task/mod.rs#sys_exit` | `kernel/src/syscall/process.rs#sys_exit` | 同板块同名 | Some | exit_current_task, panic! |
| `src/task/mod.rs#exit_signal` | `kernel/src/task/task.rs#get_pid_num` | 同板块同结构 | 内部闭环逻辑 | 内部闭环逻辑 |
| `src/task/mod.rs#fork_from` | `kernel/src/syscall/process.rs#waitpid` | 同模块上下文对齐/差异观察 | Err, Ok, RealTimer::default, Vec::new, self.aspace.deep_clone, self.aspace.reserved_virtual_bytes ...(+14) | child.get_code_if_exit, child.get_pid_num, get_current_task, get_current_task().unwrap, task.inner.lock, tcb_inner.children.iter ...(+2) |

#### 任务调度/异步运行时
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `src/task/mod.rs#sys_mount` | `kernel/src/syscall/fs.rs#sys_mount` | 同板块同名 | Some, crate::fs::file::fanotify_synthetic_inode_for_path, data.as_deref, error.errno, fstype.as_deref, fstype.as_deref().unwrap_or ...(+17) | Err, Ok, get_current_task, get_current_task().unwrap, mount_fat_fs, mount_path.ends_with ...(+3) |
| `src/task/mod.rs#sys_close` | `kernel/src/syscall/fs.rs#sys_close` | 同板块同名 | Some, error.errno, self.close_bpf_fd, self.fd_table.close_for_pid, self.fd_table.directory_path, self.fd_table.file_path ...(+11) | Err, Ok, get_current_task, get_current_task().unwrap, info!, task.fd_manager.lock ...(+1) |
| `src/task/mod.rs#sys_nanosleep` | `kernel/src/syscall/times.rs#sys_nanosleep` | 同板块同名 | SyscallResult::Return, SyscallResult::Sleep, crate::arch::monotonic_time_us, crate::arch::monotonic_time_us().saturating_add, println!, read_u64 ...(+4) | (*req).time_in_sec, Ok, TimeSpec::new, get_time_f64, suspend_current_task |
| `src/task/mod.rs#sys_unlinkat` | `kernel/src/syscall/fs.rs#sys_unlinkat` | 同板块同名 | error.errno, parent_path, self.files
                    .queue_inotify_delete_self, self.files.metadata, self.files.metadata(&path).ok, self.files.unlink_memory ...(+4) | Err, Ok, get_current_task, get_current_task().unwrap, resolve_path_from_fd, try_remove_link |

#### 内存管理
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `src/task/mod.rs#sys_mmap` | `kernel/src/syscall/process.rs#sys_mmap` | 同板块同名 | _offset.checked_add, chunk.len, chunk.len().min, crate::mm::align_down, crate::mm::align_up, error.errno ...(+13) | BackEndFile::new, Err, Ok, SeekFrom::Start, Some, drop ...(+11) |
| `src/mm/user.rs#write_inner` | `kernel/src/file/vfs/virt_file.rs#write_inner` | 同板块同名 | (PAGE_SIZE - page_offset).min, Err, Ok, bytes.len, core::slice::from_raw_parts_mut, crate::arch::phys_to_virt ...(+10) | Frame::new, Frame::new().unwrap, Some, addr_to_page_id, buf.len, page.as_slice_mut ...(+9) |
| `src/fs/file.rs#empty` | `kernel/src/memory/page_table.rs#empty` | 同板块同名 | Rc::new, RefCell::new, StorageQuota::new, String::new, Vec::new | 内部闭环逻辑 |
| `src/mm/heap.rs#alloc` | `kernel/src/memory/allocator/fd.rs#alloc` | 同板块同名 | align_up, alloc_start.checked_add, layout.align, layout.size, layout.size().max, null_mut ...(+7) | self.0.alloc |

#### 中断/异常处理
仅参考项目中观察到该模块代表函数，目标项目没有同模块可对齐证据：
- 参考项目：`libc/src/functional/fcntl.c#main`，调用摘要：TEST, TESTE, _exit, fclose, fcntl, fileno ...(+4)
- 参考项目：`kernel/src/signal/sig_action.rs#get_restorer`，调用摘要：self.flags.contains
- 参考项目：`kernel/src/trap/mod.rs#init`，调用摘要：stvec::write
- 参考项目：`kernel/src/trap/mod.rs#enable_timer_interrupt`，调用摘要：sie::set_stimer

#### 系统调用
仅参考项目中观察到该模块代表函数，目标项目没有同模块可对齐证据：
- 参考项目：`kernel/src/syscall/flags.rs#default`，调用摘要：Self::from_str
- 参考项目：`libc/src/regression/syscall-sign-extend.c#main`，调用摘要：T, open, syscall, t_error
- 参考项目：`kernel/src/syscall/flags.rs#set_info`，调用摘要：内部闭环逻辑
- 参考项目：`kernel/src/syscall/flags.rs#resolve_clone_flags_and_signal`，调用摘要：CloneFlags::from_bits, CloneFlags::from_bits(flag as u32 & (!0x3f)).unwrap, SignalNo::try_from, SignalNo::try_from(flag as u8 & 0x3f).unwrap

#### 文件系统
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `src/fs/file.rs#seek` | `rust-fatfs/src/file.rs#seek` | 同板块同名 | Err, Ok, base.checked_add, base.checked_add(offset).ok_or, self.data.borrow, self.data.borrow().len ...(+2) | Err, Ok, Some, debug_assert!, error!, i64::from ...(+16) |
| `src/fs/file.rs#set` | `rust-fatfs/src/table.rs#set` | 同板块同名 | Err, Ok, String::from, existing.is_none, existing.is_some, self.entries.iter ...(+2) | Self::get_raw, Self::set_raw, panic! |
| `src/fs/file.rs#open_dir` | `rust-fatfs/src/dir.rs#open_dir` | 同板块同名 | Ok, OpenDirectory::new, directories
                .iter, directories
                .iter()
                .find, directories
                .iter()
                .find(|entry| entry.path == path)
                .ok_or, directory.entries.clone ...(+5) | Ok, Some, e.to_dir, e.to_dir().open_dir, self.find_entry, split_path ...(+1) |
| `src/fs/file.rs#seek` | `rust-fatfs/src/fs.rs#seek` | 同板块同名 | (next as usize).min, Err, Ok, base.checked_add, base.checked_add(offset).ok_or, self.entries.len | Err, Ok, Some, error!, i64::try_from, i64::try_from(self.offset)
                .ok ...(+9) |

#### 设备驱动
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `src/drivers/block/virtio_mmio.rs#read_sector` | `kernel/src/drivers/block/mod.rs#block_device_test` | 同模块上下文对齐/差异观察 | Err, Error::DeviceStatus, Ok, fence, phys_addr, read_volatile ...(+5) | BLOCK_DEVICE.clone, assert_eq!, block_device.read_block, block_device.write_block, println!, write_buffer.iter_mut |
| `src/mm/heap.rs#add_free_block` | `kernel/src/drivers/block/virtio_block.rs#read_block` | 同模块上下文对齐/差异观察 | block.ptr.checked_add, ptr.checked_add, self.free_count.load, self.free_count.store, self.remove_free_block | self.0
            .lock, self.0
            .lock()
            .read_block, self.0
            .lock()
            .read_block(block_id, buf)
            .expect |
| `src/drivers/block/virtio_mmio.rs#wait_used` | `kernel/src/drivers/block/virtio_block.rs#write_block` | 同模块上下文对齐/差异观察 | Err, Error::UnexpectedUsedId, Ok, core::hint::spin_loop, read_volatile | self.0
            .lock, self.0
            .lock()
            .write_block, self.0
            .lock()
            .write_block(block_id, buf)
            .expect |
| `src/drivers/block/virtio_mmio.rs#probe` | `kernel/src/drivers/block/virtio_block.rs#new` | 同模块上下文对齐/差异观察 | Self::init, Some, read_reg | Mutex::new, Self, VirtIOBlk::new, VirtIOBlk::new(&mut *(VIRTIO0 as *mut VirtIOHeader)).unwrap |

#### 网络协议栈
仅参考项目中观察到该模块代表函数，目标项目没有同模块可对齐证据：
- 参考项目：`libc/src/functional/inet_pton.c#main`，调用摘要：V4, V6, inet_ntop, inet_pton, strerror, t_error
- 参考项目：`libc/src/functional/socket.c#main`，调用摘要：TEST, TESTE, accept, bind, close, connect ...(+9)
- 参考项目：`libc/src/regression/inet_pton-empty-last-field.c#main`，调用摘要：inet_pton, memcmp, t_error, txt
- 参考项目：`libc/src/regression/inet_ntop-v4mapped.c#main`，调用摘要：inet_ntop, inet_pton, strchr, t_error

#### 文件系统/第三方库
仅目标项目中观察到该模块代表函数，参考项目没有同模块可对齐证据：
- 目标项目：`src/fs/file.rs#from_ext4`，调用摘要：Self::empty, candidate_path, catalog.add_busybox_aliases, catalog.add_dynamic_linker_aliases, catalog.add_from_ext4, catalog.add_from_ext4_dir ...(+7)
- 目标项目：`src/fs/ext4.rs#smoke_test`，调用摘要：Ext4::open, ScriptPlan::oscomp, Some, String::from, alloc::vec!, candidate_path ...(+22)
- 目标项目：`src/fs/ext4.rs#visit_dir_entries`，调用摘要：((dir.size - file_offset) as usize).min, Err, Ok, core::str::from_utf8, core::str::from_utf8(&block[name_start..name_end])
                            .map_err, extent.block_count ...(+9)
- 目标项目：`src/fs/ext4.rs#collect_extent_node`，调用摘要：Err, Error::BadExtentMagic, Ok, extents.push, le_u16, le_u32 ...(+3)

#### 第三方库
仅参考项目中观察到该模块代表函数，目标项目没有同模块可对齐证据：
- 参考项目：`dependencies/core2/src/io/buffered.rs#write`，调用摘要：Ok, memchr::memrchr, self.buffer.capacity, self.buffer.flush_buf, self.buffer.write, self.buffer.write_to_buf ...(+3)
- 参考项目：`dependencies/core2/src/io/buffered.rs#flush_buf`，调用摘要：BufGuard::new, Err, Error::new, Ok, e.kind, guard.consume ...(+7)
- 参考项目：`dependencies/core2/src/io/traits.rs#read_to_end_with_reservation`，调用摘要：Err, Ok, assert!, buf.len, e.kind, g.buf.capacity ...(+7)
- 参考项目：`dependencies/bitmap-allocator/src/lib.rs#bitalloc_contiguous`，调用摘要：BitAlloc16::default, BitAlloc4K::default, assert_eq!, ba.dealloc, ba.insert, ba.remove ...(+2)

#### 通用模块/其他
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `src/console.rs#write_str` | `kernel/src/arch/riscv/stdout.rs#write_str` | 同板块同名 | Ok, text.as_bytes, write_bytes | Ok, putchar_raw, s.bytes |
| `src/elf/mod.rs#push` | `libc/src/functional/strtol.c#main` | 同模块上下文对齐/差异观察 | Err, Ok, self.max_vaddr.max, self.min_vaddr.min | TEST, TEST2, atol, strtol, strtoll, strtoul ...(+2) |
| `src/main.rs#rust_main` | `libc/src/functional/wcstol.c#main` | 同模块上下文对齐/差异观察 | arch::park, arch::shutdown, console::init, contest::run, mm::init, mm::zero_bss ...(+1) | TEST, TEST2, t_error, wcstol, wcstoul |
| `src/elf/mod.rs#dynamic_segment` | `libc/src/functional/string.c#main` | 同模块上下文对齐/差异观察 | Ok, Some, self.program_header | TEST, TEST_S, memcmp, memcpy, memset, strchr ...(+11) |


好的，审计官。基于提供的全量证据底座、控辩双方陈述及原始AST抽样数据，本庭现对参赛作品 `[2026-T2026104869911155-XX]` 与参考项目 `[2022-Maturin-清华大学]` 的同源性争议进行最终定性仲裁。

### 1. 宏观判定
- **定性结论**: **独立开发**
- **核心依据**: 客观AST相似度仅为 **0.53%**，共同AST指纹仅15个，双向覆盖率均处于极低水平（目标项目1.1%，参考项目1.01%）。该数据远低于构成“代码复用”或“架构同源”的阈值，属于大型项目中因编程语言标准库、硬件规范及操作系统领域共性而产生的背景噪声。控方提出的“复用指控”主要基于操作系统领域的必然语义推断，缺乏可量化的AST调用图谱重合证据，不予采信。

### 2. 全模块覆盖矩阵解读
| 模块 | 双方存在 | 共同AST指纹 | 覆盖率 (目标/参考) | 证据强度 | 解读 |
| :--- | :---: | :---: | :---: | :---: | :--- |
| **进程管理** | 是 | 1 | 1.09% / 1.82% | **弱** | 唯一的共同指纹源于POSIX规范强制的`sys_execve`/`sys_exit`同名函数，其内部调用链完全不同。控方指控的“僵尸回收”等逻辑，双方在数据结构(`zombies` vs `children`)、唤醒机制(`frame.set_return_value` vs `suspend_current_task`)上存在根本性差异，无共同AST节点。 |
| **任务调度/异步运行时** | 是 | 4 | 0.94% / 2.86% | **弱** | 共同指纹源于`sys_mount`, `sys_close`等规范系统调用。目标项目展现出更复杂的异步状态机（如`TaskState::EpollWaiting`），而参考项目则通过`suspend_current_task`实现同步阻塞，调度模型截然不同。 |
| **内存管理** | 是 | 0 | 0.0% / 0.0% | **未形成** | 尽管双方均实现了`sys_mmap`，但AST层面无任何共同指纹。目标项目采用`crate::mm::align_up/down`等底层工具函数，参考项目则依赖`BackEndFile`等抽象，表明内存管理子系统完全独立构建。 |
| **中断/异常处理** | 否 | 0 | 0.0% / 0.0% | **未形成** | 该模块仅存在于参考项目，目标项目无对应实现，是两者架构差异的显著标志。 |
| **系统调用** | 否 | 0 | 0.0% / 0.0% | **未形成** | 该模块仅存在于参考项目，目标项目无对应实现，是两者架构差异的显著标志。 |
| **文件系统** | 是 | 6 | 1.09% / 1.11% | **弱** | 共同指纹主要源于对`rust-fatfs`等第三方库的封装（如`seek`, `open_dir`）。这是使用相同公共依赖的正常结果，不代表内核代码复用。 |
| **设备驱动** | 是 | 0 | 0.0% / 0.0% | **未形成** | 双方均有VirtIO块设备驱动，但AST指纹为零。目标项目直接操作MMIO寄存器(`read_volatile`)，参考项目则通过`VirtIOBlk`抽象层，驱动模型完全不同。 |
| **网络协议栈** | 否 | 0 | 0.0% / 0.0% | **未形成** | 该模块仅存在于参考项目，目标项目无对应实现，是两者架构差异的显著标志。 |
| **第三方库/公共依赖** | 部分 | 0 | 0.0% / 0.0% | **未形成** | 双方均包含第三方库，但库的种类和版本不同（目标含`ext4`，参考含`core2`, `bitmap-allocator`），且无共同AST指纹，表明依赖项选择独立。 |

### 3. 底层 AST 关键证据指纹表
| 所属板块 | 目标项目路径 | 参考项目路径 | 证据类型 | 技术说明 |
| :--- | :--- | :--- | :--- | :--- |
| **进程管理** | `[src/task/mod.rs]` `sys_execve` | `[kernel/src/syscall/process.rs]` `sys_execve` | 同板块同名 | Linux规范强制系统调用名。目标调用`exec_path_with_user_args`，参考调用`sys_exec`，内部实现无共同AST节点。 |
| **进程管理** | `[src/task/mod.rs]` `sys_exit` | `[kernel/src/syscall/process.rs]` `sys_exit` | 同板块同名 | Linux规范强制系统调用名。目标调用`Some`，参考调用`exit_current_task`，内部实现无共同AST节点。 |
| **任务调度/异步运行时** | `[src/task/mod.rs]` `sys_nanosleep` | `[kernel/src/syscall/times.rs]` `sys_nanosleep` | 同板块同名 | 规范系统调用。目标使用`SyscallResult::Sleep`和`monotonic_time_us`，参考使用`TimeSpec::new`和`suspend_current_task`，实现路径不同。 |
| **文件系统** | `[src/fs/file.rs]` `seek` | `[rust-fatfs/src/file.rs]` `seek` | 同板块同名 | 共同依赖第三方库`rust-fatfs`的公共接口，非内核原创逻辑复用。 |
| **文件系统** | `[src/fs/file.rs]` `open_dir` | `[rust-fatfs/src/dir.rs]` `open_dir` | 同板块同名 | 共同依赖第三方库`rust-fatfs`的公共接口，非内核原创逻辑复用。 |
| **通用模块/其他** | `[src/console.rs]` `write_str` | `[kernel/src/arch/riscv/stdout.rs]` `write_str` | 同板块同名 | 实现`core::fmt::Write` trait的标准方法，是Rust语言层面的规范要求，与业务逻辑无关。 |

### 4. 核心代码调用流对比剖析
*   **【进程管理 / Fork 地址空间拷贝】**
    *   **底层技术析理**: 两者在进程创建时的内存处理策略存在根本性架构差异。参考项目采用手动遍历内存区域（`areas`）并逐个拷贝的细粒度控制模型；目标项目则将整个地址空间视为一个对象，通过`deep_clone`进行整体复制，并同步克隆了参考项目完全不存在的BPF资源。这体现了从“过程式内存管理”到“面向对象资源管理”的抽象层次差异。
    *   **代码溯源链接**:
        - 目标项目: `src/task/mod.rs#fork_from`
        - 参考项目: `kernel/src/memory/vmm.rs#copy_as_fork`

*   **【进程管理 / 进程退出与回收】**
    *   **底层技术析理**: 双方在僵尸进程回收的数据结构和同步策略上截然不同。目标项目维护独立的`zombies`队列，使用`swap_remove`进行O(1)无序移除，并通过`frame.set_return_value`直接向父进程上下文写入返回值。参考项目则在`children`列表上执行`remove`，并通过`send_signal`和`suspend_current_task`实现进程间同步。两者在内存操作和调度交互上无共同调用流。
    *   **代码溯源链接**:
        - 目标项目: `src/task/riscv64.rs#reap_zombie`
        - 参考项目: `kernel/src/syscall/process.rs#waitpid`

*   **【内存管理 / 用户态内存写入】**
    *   **底层技术析理**: 尽管函数同名`write_inner`，但底层内存模型完全不同。目标项目直接操作物理页和页内偏移（`PAGE_SIZE`, `phys_to_virt`），采用手动物理内存映射。参考项目则通过`Frame`对象和页表ID（`addr_to_page_id`）进行虚拟内存管理，体现了“物理地址直接操作”与“虚拟页帧抽象”的模型差异。
    *   **代码溯源链接**:
        - 目标项目: `src/mm/user.rs#write_inner`
        - 参考项目: `kernel/src/file/vfs/virt_file.rs#write_inner`

### 5. 单方模块与降权项
*   **单方模块**: **中断/异常处理**、**系统调用**、**网络协议栈**模块仅存在于参考项目；**文件系统/第三方库**（特指ext4集成）仅存在于目标项目。这些模块的完全缺失或独有，是两者架构设计独立、功能目标不同的强有力证据，远非“代码复用”所能解释。
*   **降权项**:
    *   **同名系统调用** (`sys_execve`, `sys_exit`等): 属于Linux/POSIX标准接口规范，任何兼容内核都必须实现，不构成同源证据。
    *   **第三方库调用** (`rust-fatfs`的`seek`, `open_dir`): 使用相同公共依赖库的必然结果，不代表内核原创代码的复用。
    *   **Rust语言标准Trait实现** (`write_str`): 实现`core::fmt::Write`等标准Trait是Rust语言层面的强制要求，调用图谱相似是语言规范所致。
    *   **参考项目中的用户态测试用例** (`libc/src/...`): 属于POSIX测试套件或musl库代码，与目标项目内核态代码不在同一抽象层次，不能作为内核抄袭证据。

### 6. 保守结论
综合全模块AST指纹覆盖率（0.53%）、模块覆盖矩阵及核心调用流对比，本庭裁定目标项目 `[2026-T2026104869911155-XX]` 与参考项目 `[2022-Maturin-清华大学]` 在代码实现层面构成 **“独立开发”** 。双方在进程管理、内存管理、设备驱动等核心子系统上展现出截然不同的抽象层次、数据结构与同步策略。观察到的极少数共同指纹均可被Linux系统调用规范、Rust语言标准Trait及公共第三方库依赖所解释，属于独立大型项目间的正常噪声水平，不构成代码复用或架构抄袭的风险。控方基于操作系统领域共性语义的指控，因缺乏客观AST证据支撑，予以驳回。

------------------------------

