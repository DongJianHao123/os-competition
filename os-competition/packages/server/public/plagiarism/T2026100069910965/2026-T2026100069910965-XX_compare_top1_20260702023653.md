# 🛡️ OS 内核赛自动化代码级架构审计报告

**审计时间**: `2026-07-02 02:36:53`
**参赛作品名称**: `2026-T2026100069910965-XX`
**年份**: `2026`
**学校**: `XX`
**队伍名称**: `T2026100069910965`
**比赛名字**: `OS-Competition`
**比赛子赛道**: `Kernel-Implementation`
**仓库地址**: `https://gitlab.eduxiji.net/educg-group-43501-3132633/T2026100069910965-1418`
**推测开发语言**: `Rust / C (由AST探针自动识别)`
**特征函数量**: `424`

## ⚔️ 底层代码同源性对比 (AST Homology)
> 本部分由专门的代码审查智能体，对该项目与符合年份条件的历届最高相似度项目进行控制流与底层调用对比。新版报告会先给出确定性的全模块覆盖矩阵，再给出 Agent 的分模块解释，避免只围绕少量抽样函数展开。

### 🏆 Top 1 底层对标: vs `2021-HDACP-青海大学`（青海大学 / HDACP）

#### 对方作品信息

**对方年份**: `2021`
**对方学校**: `青海大学`
**对方队伍名称**: `HDACP`
**对方比赛名字**: `OS-Competition`
**对方比赛子赛道**: `Kernel-Implementation`
**对方仓库地址**: `https://gitlab.eduxiji.net/chino/chinos`

> 双向覆盖率：共同 AST 指纹: 8；唯一 AST 指纹: 目标项目 391，参考项目 363；覆盖率: 目标项目 2.05%，参考项目 2.2%

## 全模块对比总览（确定性证据）

> 总体覆盖率：共同 AST 指纹: 8；唯一 AST 指纹: 目标项目 391，参考项目 363；覆盖率: 目标项目 2.05%，参考项目 2.2%

### 1. 模块覆盖矩阵
| 模块 | 目标函数数 | 参考函数数 | 共同唯一 AST 指纹 | Target 覆盖 | Base 覆盖 | 状态 |
| :--- | ---: | ---: | ---: | ---: | ---: | :--- |
| 进程管理 | 58 | 76 | 2 | 3.85% | 4.0% | 双方存在且有结构重合 |
| 任务调度/异步运行时 | 47 | 39 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |
| 内存管理 | 169 | 102 | 3 | 1.86% | 3.09% | 双方存在且有结构重合 |
| 中断/异常处理 | 13 | 20 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |
| 系统调用 | 4 | 3 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |
| 文件系统 | 40 | 56 | 1 | 2.63% | 1.89% | 双方存在且有结构重合 |
| 设备驱动 | 26 | 27 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |
| 网络协议栈 | 1 | 0 | 0 | 0.0% | 0.0% | 仅目标项目体现 |
| 文件系统/第三方库 | 18 | 0 | 0 | 0.0% | 0.0% | 仅目标项目体现 |
| 通用模块/其他 | 48 | 114 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |

### 2. 分模块证据清单

#### 进程管理
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `kernel/sysproc.c#sys_setpgid` | `kernel/sysproc.c#sys_setpgid` | 同板块同名 | proc_find, proc_pgrp_exists | argint |
| `kernel/proc.c#yield` | `kernel/proc.c#yield` | 同板块同名 | arch_intr_get, arch_intr_off, arch_intr_on, myproc, sched | acquire, myproc, release, sched |
| `kernel/proc.c#sleep` | `kernel/proc.c#sleep` | 同板块同名 | arch_intr_off, myproc, proc_exit_signal, sched | acquire, myproc, release, sched |
| `kernel/sysproc.c#sys_getpgid` | `kernel/sysproc.c#sys_getpgid` | 同板块同名 | proc_find | argint |

#### 任务调度/异步运行时
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `arch/riscv/trap.c#kerneltrap` | `kernel/trap.c#kerneltrap` | 同板块同名 | dev_intr, intr_get, myproc, panic, r_scause, r_sepc ...(+5) | devintr, intr_get, myproc, panic, printf, r_scause ...(+7) |
| `kernel/sysfile.c#sys_chdir` | `kernel/sysfile.c#sys_chdir` | 同板块同名 | ext4_is_dir, ext4_lookup, fetch_str, ramfs_lookup, resolve, strlcpy | argstr, elock, ename, eput, eunlock, myproc |
| `kernel/sysfile.c#sys_unlinkat` | `kernel/sysfile.c#sys_unlinkat` | 同板块同名 | fetch_str, ramfs_unlink, resolve_at | argfd, argstr, dirent_name, elock, ename, eput ...(+4) |
| `kernel/sysfile.c#sys_close` | `kernel/sysfile.c#sys_close` | 同板块同名 | file_close, getf | argfd, fileclose, myproc |

#### 内存管理
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `kernel/sysfile.c#sys_openat` | `kernel/sysfile.c#sys_openat` | 同板块同名 | (u16), dev_kind, ext4_is_dir, ext4_lookup, ext4_size, fd_alloc ...(+8) | argint, argstr, create, elock, ename, eput ...(+5) |
| `arch/riscv/usertrap.c#usertrapret` | `kernel/trap.c#usertrapret` | 同板块同名 | ((void (*)(u64))fn), MAKE_SATP, arch_intr_off, myproc, r_satp, r_sstatus ...(+3) | ((void (*)(uint64,uint64))fn), MAKE_SATP, intr_off, myproc, r_satp, r_sstatus ...(+4) |
| `arch/riscv/mmu.c#walk` | `kernel/vm.c#walk` | 同板块同名 | PA2PTE, PTE2PA, PX, kalloc_page | PA2PTE, PTE2PA, PX, kalloc, memset, panic |
| `arch/loongarch/mmu.c#copyout` | `kernel/vm.c#copyout` | 同板块同名 | PGROUNDDOWN, memmove, uvmcow, walkaddr | PGROUNDDOWN, memmove, walkaddr |

#### 中断/异常处理
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `arch/loongarch/trap.c#dev_intr` | `kernel/trap.c#trapframedump` | 同模块上下文对齐/差异观察 | ESTAT_ECODE, timer_tick | printf |
| `arch/riscv/sbi.c#arch_intr_off` | `bootloader/SBI/rustsbi-k210/src/main.back.rs#set_rd` | 同模块上下文对齐/差异观察 | 内部闭环逻辑 | panic! |
| `arch/riscv/sbi.c#arch_intr_on` | `bootloader/SBI/rustsbi-k210/src/main.rs#set_rd` | 同模块上下文对齐/差异观察 | 内部闭环逻辑 | panic! |
| `arch/riscv/sbi.c#arch_intr_get` | `kernel/plic.c#plicinithart` | 同模块上下文对齐/差异观察 | 内部闭环逻辑 | PLIC_MENABLE, PLIC_SENABLE, PLIC_SPRIORITY, cpuid, printf, readd |

#### 系统调用
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `arch/riscv/sbi.c#sbi_ecall` | `kernel/syscall.c#argstr` | 同模块上下文对齐/差异观察 | 内部闭环逻辑 | argaddr, fetchstr |
| `arch/riscv/sbi.c#arch_shutdown` | `kernel/syscall.c#argaddr` | 同模块上下文对齐/差异观察 | arch_halt, sbi_ecall | argraw |
| `arch/riscv/sbi.c#sbi_set_timer` | `kernel/syscall.c#argint` | 同模块上下文对齐/差异观察 | sbi_ecall | argraw |

#### 文件系统
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `fs/file.c#ramfs_seed_dir` | `kernel/utils.c#set_gpio_bit` | 同板块同结构 | ramfs_create | set_bit_offset |
| `drivers/virtio_blk.c#blk_init_pci_legacy` | `kernel/fat32.c#fat32_init` | 同模块上下文对齐/差异观察 | (u32), kva_to_pa, pa_to_kva, pci_cfg_read32, pci_cfg_write16, pci_cfg_write32 ...(+7) | bread, brelse, initlock, initsleeplock, memmove, memset ...(+3) |
| `fs/file.c#ramfs_readdir` | `kernel/sysfile.c#sys_rename` | 同模块上下文对齐/差异观察 | cb, strlen, strncmp | argstr, dirlookup, edup, elock, emake, ename ...(+7) |
| `fs/file.c#ramfs_rename` | `bootloader/SBI/rustsbi-qemu/src/main.rs#count_harts` | 同模块上下文对齐/差异观察 | ramfs_lookup, ramfs_unlink, strlcpy, strlen, strncmp | DeviceTree::load, cluster_node.children.iter, cluster_node.children.iter().count, compiled_max_hartid, core::slice::from_raw_parts, cpu_map_node.children.iter ...(+4) |

#### 设备驱动
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `drivers/virtio_blk.c#virtio_rw` | `kernel/spi.c#spi_receive_data_normal_dma` | 同模块上下文对齐/差异观察 | __sync_synchronize, kva_to_pa, pci_wo16, printk, wr | (uint32), dmac_set_single_mode, dmac_wait_done, spi_get_frame_format, spi_set_tmod, sysctl_dma_select |
| `drivers/virtio_blk.c#setup_legacy_vring` | `kernel/sdcard.c#check_block_size` | 同模块上下文对齐/差异观察 | PGROUNDUP, memset | printf, sd_end_cmd, sd_get_response_R1, sd_get_response_R3_rest, sd_send_cmd |
| `drivers/virtio_blk.c#find_block_device` | `kernel/uart.c#uartinit` | 同模块上下文对齐/差异观察 | 内部闭环逻辑 | WriteReg, initlock |
| `arch/loongarch/power.c#arch_shutdown` | `kernel/uart.c#uartstart` | 同模块上下文对齐/差异观察 | arch_halt, uart_puts | ReadReg, WriteReg, wakeup |

#### 网络协议栈
仅目标项目中观察到该模块代表函数，参考项目没有同模块可对齐证据：
- 目标项目：`kernel/main.c#is_netperf_script`，调用摘要：has_suffix

#### 文件系统/第三方库
仅目标项目中观察到该模块代表函数，参考项目没有同模块可对齐证据：
- 目标项目：`fs/ext4/ext4.c#ext4_readdir_from`，调用摘要：(u32), cb, extent_map, inode_size_bytes, le16, le32 ...(+3)
- 目标项目：`fs/ext4/ext4.c#extent_map`，调用摘要：(u32), le16, le32, read_block
- 目标项目：`fs/ext4/ext4.c#ext4_mount`，调用摘要：le16, le32, printk, read_bytes
- 目标项目：`fs/ext4/ext4.c#ext4_pread`，调用摘要：(u32), extent_map, inode_size_bytes, memcpy, read_block, read_inode

#### 通用模块/其他
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `lib/printf.c#panic` | `bootloader/SBI/rustsbi-k210/src/main.back.rs#panic` | 同板块同名 | arch_halt, do_format, emit, emit_str, va_end, va_start | println! |
| `lib/string.c#strncmp` | `kernel/string.c#strncmp` | 同板块同名 | 内部闭环逻辑 | 内部闭环逻辑 |
| `lib/string.c#memcmp` | `kernel/string.c#memcmp` | 同板块同名 | 内部闭环逻辑 | 内部闭环逻辑 |
| `lib/string.c#memcpy` | `xv6-user/ulib.c#memcpy` | 同板块同名 | 内部闭环逻辑 | memmove |


好的，审判长。基于双方提交的证据、控辩意见及客观 AST 比对数据，本庭现对本案进行最终同源性定性仲裁。

### 1. 宏观判定
- **定性结论**: **独立开发，底层架构存在显著差异**
  本案客观 AST 相似度仅为 1.07%，共同 AST 指纹仅 8 个，远低于可认定“抄袭”或“非法复用”的阈值。双方在进程管理、内存管理等核心板块虽存在同名函数，但其内部实现所依赖的同步原语、内存模型及文件系统架构存在根本性异构。控方提出的“复用指控”实质上是将操作系统内核领域的公共知识、POSIX 标准接口命名惯例以及必然的算法骨架误判为抄袭证据。

### 2. 全模块覆盖矩阵解读
- **进程管理**: 双方均存在。共同 AST 指纹 2 个，覆盖率极低（目标 3.85%，参考 4.0%）。同名函数如 `sched`、`yield` 的核心调用流因同步机制（中断控制 vs 自旋锁）不同而存在本质差异。**证据强度：弱**。
- **任务调度/异步运行时**: 双方均存在。共同 AST 指纹为 0，覆盖率 0.0%。未观察到任何共同结构指纹。**证据强度：未形成**。
- **内存管理**: 双方均存在。共同 AST 指纹 3 个，覆盖率低（目标 1.86%，参考 3.09%）。`walk` 等函数属于页表遍历的标准算法实现，其内部调用的内存分配器（`kalloc_page` vs `kalloc`）不同。**证据强度：弱**。
- **中断/异常处理**: 双方均存在。共同 AST 指纹为 0，覆盖率 0.0%。未观察到共同结构指纹。**证据强度：未形成**。
- **系统调用**: 双方均存在。共同 AST 指纹为 0，覆盖率 0.0%。未观察到共同结构指纹。**证据强度：未形成**。
- **文件系统**: 双方均存在。共同 AST 指纹 1 个，覆盖率低（目标 2.63%，参考 1.89%）。目标项目采用 ext4/ramfs 架构，参考项目为 FAT32/xv6 风格，底层对象管理模型完全不同。**证据强度：弱**。
- **设备驱动**: 双方均存在。共同 AST 指纹为 0，覆盖率 0.0%。目标项目包含 VirtIO PCI 驱动，参考项目包含 SPI/SD/UART 驱动，硬件抽象层完全不同。**证据强度：未形成**。
- **网络协议栈**: 仅目标项目存在。参考项目无此模块，无法形成比对。**证据强度：未形成**。
- **第三方库/公共依赖**: 仅目标项目存在 ext4 等第三方库。参考项目无此模块。`strncmp`、`memcmp` 等函数属于标准 C 库的公共实现，其内部闭环逻辑是语言标准规定的必然结果，不构成同源证据。**证据强度：未形成**。

### 3. 底层 AST 关键证据指纹表
| 所属板块 | 目标项目路径 | 参考项目路径 | 证据类型 | 技术说明 |
| :--- | :--- | :--- | :--- | :--- |
| 进程管理 | `kernel/proc.c#sched` | `kernel/proc.c#sched` | 同板块同名，弱证据 | 核心调度骨架 `mycpu->myproc->swtch` 相似，但同步原语异构（`arch_intr_get` vs `holding+intr_get`），属领域内必然逻辑。 |
| 进程管理 | `kernel/proc.c#yield` | `kernel/proc.c#yield` | 同板块同名，弱证据 | 均调用 `myproc` 和 `sched`，但同步模型不同（中断控制 vs 自旋锁），属同一功能的不同实现。 |
| 内存管理 | `arch/riscv/mmu.c#walk` | `kernel/vm.c#walk` | 同板块同名，弱证据 | 页表遍历的标准算法，调用 `PA2PTE`、`PX` 等通用宏，但底层内存分配函数不同（`kalloc_page` vs `kalloc`）。 |
| 文件系统 | `fs/file.c#ramfs_seed_dir` | `kernel/utils.c#set_gpio_bit` | 跨板块伪匹配，无效证据 | 目标为内存文件系统目录操作，参考为 GPIO 位操作，功能完全无关，属于比对算法的假阳性结果。 |
| 通用模块/其他 | `lib/string.c#strncmp` | `kernel/string.c#strncmp` | 第三方库/公共依赖，弱证据 | 标准 C 库函数，实现由语言规范定义，任何独立实现均高度相似，不构成同源证据。 |
| 通用模块/其他 | `lib/printf.c#panic` | `bootloader/SBI/rustsbi-k210/src/main.back.rs#panic` | 跨语言伪匹配，无效证据 | 目标为 C 语言内核 panic 函数，参考为 Rust 语言 SBI 的 panic 处理，语言和运行层级完全不同。 |

### 4. 核心代码调用流对比剖析
* **【进程管理/核心调度路径】**
* **底层技术析理**: 双方均实现了 `sched` 函数，但其同步策略揭示了根本性的架构差异。参考项目采用经典的**自旋锁（Spinlock）模型**，在调度入口通过 `holding` 检查锁状态，依赖 `acquire/release` 对保护临界区。而目标项目采用**中断使能控制模型**，通过 `arch_intr_get` 保存并检查中断状态，直接操作 CPU 中断标志位来实现临界区保护。这是两种互斥的并发控制哲学，前者基于锁变量，后者基于处理器状态，其实现逻辑和调用链不可能通过简单重命名或内联完成转换。
* **代码溯源链接**:
  - 目标项目: `kernel/proc.c#sched`
  - 参考项目: `kernel/proc.c#sched`

* **【内存管理/页表遍历】**
* **底层技术析理**: 双方均实现了 `walk` 函数来模拟 MMU 的页表遍历过程，这属于 RISC-V 架构下的标准算法。然而，当需要分配新的页表页时，目标项目调用 `kalloc_page`，参考项目调用 `kalloc` 并随后执行 `memset` 和 `panic`。这表明两者在物理内存分配器的接口和错误处理模型上存在差异，目标项目似乎拥有更简洁的分配接口，而参考项目保留了 xv6 风格的显式初始化与 panic 处理。
* **代码溯源链接**:
  - 目标项目: `arch/riscv/mmu.c#walk`
  - 参考项目: `kernel/vm.c#walk`

### 5. 单方模块与降权项
- **单方模块**：目标项目独有的**网络协议栈** (`kernel/main.c#is_netperf_script`) 和 **ext4 文件系统** (`fs/ext4/ext4.c`) 等复杂模块，在参考项目中无任何对应证据。这体现了目标项目超出参考项目范围的独立开发能力，是反驳系统性抄袭的有力证据。
- **跨语言/跨模块伪匹配**：证据底座中出现了大量此类无效匹配，如将 C 语言的 `panic` 与 Rust 语言的 `panic!` 宏进行比对，或将文件系统操作 (`ramfs_seed_dir`) 与 GPIO 操作 (`set_gpio_bit`) 强行关联。这些证据违反了“禁止跨板块硬凑”原则，必须降权为无效证据。
- **公共接口与泛型函数**：`sys_getpid`、`sys_clone`、`sys_wait4` 等 POSIX 标准系统调用接口，以及 `strncmp`、`memcpy` 等标准 C 库函数，其命名和实现骨架由行业标准或语言规范决定，属于公共知识领域。双方均实现这些函数是必然结果，不能作为同源证据。

### 6. 保守结论
本案客观 AST 覆盖率仅约 2%，处于独立开发项目的正常背景噪声水平。控方提出的“复用指控”所依赖的证据，或为操作系统内核领域的公共知识（如 POSIX 接口、标准调度骨架），或为同步机制异构（中断 vs 自旋锁）掩盖下的弱信号，甚至包含大量跨语言、跨模块的伪匹配。目标项目在文件系统（ext4）、设备驱动（VirtIO PCI）及网络协议栈等方面展现出与参考项目完全不同的架构选型和实现能力。综合裁定，无证据表明目标项目对参考项目构成抄袭或非法复用，维持独立开发结论。

------------------------------

