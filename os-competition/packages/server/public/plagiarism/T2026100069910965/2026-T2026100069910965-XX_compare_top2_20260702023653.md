# 🛡️ OS 内核赛自动化代码级架构审计报告

**审计时间**: `2026-07-02 02:36:53`
**参赛作品名称**: `2026-T2026100069910965-XX`
**年份**: `2026`
**学校**: `XX`
**队伍名称**: `T2026100069910965`
**比赛名字**: `OS-Competition`
**比赛子赛道**: `Kernel-Implementation`
**仓库地址**: `https://gitlab.eduxiji.net/educg-group-43501-3132633/T2026100069910965-1418`
**推测开发语言**: `Rust / C (由AST探针自动识别)`
**特征函数量**: `424`

## ⚔️ 底层代码同源性对比 (AST Homology)
> 本部分由专门的代码审查智能体，对该项目与符合年份条件的历届最高相似度项目进行控制流与底层调用对比。新版报告会先给出确定性的全模块覆盖矩阵，再给出 Agent 的分模块解释，避免只围绕少量抽样函数展开。

### 🏆 Top 2 底层对标: vs `2021-nostayuplate-北京大学`（北京大学 / no stay up late）

#### 对方作品信息

**对方年份**: `2021`
**对方学校**: `北京大学`
**对方队伍名称**: `no stay up late`
**对方比赛名字**: `OS-Competition`
**对方比赛子赛道**: `Kernel-Implementation`
**对方仓库地址**: `https://gitlab.eduxiji.net/AaronWu/oskernel2021-x`

> 双向覆盖率：共同 AST 指纹: 8；唯一 AST 指纹: 目标项目 391，参考项目 413；覆盖率: 目标项目 2.05%，参考项目 1.94%

## 全模块对比总览（确定性证据）

> 总体覆盖率：共同 AST 指纹: 8；唯一 AST 指纹: 目标项目 391，参考项目 413；覆盖率: 目标项目 2.05%，参考项目 1.94%

### 1. 模块覆盖矩阵
| 模块 | 目标函数数 | 参考函数数 | 共同唯一 AST 指纹 | Target 覆盖 | Base 覆盖 | 状态 |
| :--- | ---: | ---: | ---: | ---: | ---: | :--- |
| 进程管理 | 58 | 53 | 2 | 3.85% | 4.88% | 双方存在且有结构重合 |
| 任务调度/异步运行时 | 47 | 44 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |
| 内存管理 | 169 | 110 | 3 | 1.86% | 2.83% | 双方存在且有结构重合 |
| 中断/异常处理 | 13 | 20 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |
| 系统调用 | 4 | 3 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |
| 文件系统 | 40 | 77 | 1 | 2.63% | 1.41% | 双方存在且有结构重合 |
| 设备驱动 | 26 | 28 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |
| 网络协议栈 | 1 | 0 | 0 | 0.0% | 0.0% | 仅目标项目体现 |
| 文件系统/第三方库 | 18 | 0 | 0 | 0.0% | 0.0% | 仅目标项目体现 |
| 通用模块/其他 | 48 | 140 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |

### 2. 分模块证据清单

#### 进程管理
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `kernel/proc.c#yield` | `kernel/proc.c#yield` | 同板块同名 | arch_intr_get, arch_intr_off, arch_intr_on, myproc, sched | acquire, myproc, release, sched |
| `kernel/proc.c#sleep` | `kernel/proc.c#sleep` | 同板块同名 | arch_intr_off, myproc, proc_exit_signal, sched | acquire, myproc, release, sched |
| `kernel/sysproc.c#sys_clone` | `kernel/sysproc.c#sys_clone` | 同板块同名 | clone | argaddr, argint, clone, fork |
| `kernel/proc.c#sched` | `kernel/proc.c#sched` | 同板块同名 | arch_intr_get, mycpu, myproc, panic, swtch | holding, intr_get, mycpu, myproc, panic, swtch |

#### 任务调度/异步运行时
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `arch/riscv/trap.c#kerneltrap` | `kernel/trap.c#kerneltrap` | 同板块同名 | dev_intr, intr_get, myproc, panic, r_scause, r_sepc ...(+5) | devintr, intr_get, myproc, panic, printf, r_scause ...(+7) |
| `kernel/sysfile.c#sys_chdir` | `kernel/sysfile.c#sys_chdir` | 同板块同名 | ext4_is_dir, ext4_lookup, fetch_str, ramfs_lookup, resolve, strlcpy | argstr, elock, ename, eput, eunlock, myproc |
| `kernel/sysfile.c#sys_mkdirat` | `kernel/sysfile.c#sys_mkdirat` | 同板块同名 | (u16), fetch_str, ramfs_create, ramfs_lookup, resolve_at | argint, argstr, create, eput, eunlock, myproc ...(+2) |
| `kernel/sysfile.c#sys_dup3` | `kernel/sysfile.c#sys_dup3` | 同板块同名 | file_close, file_dup, getf | argfd, argint, filedup, myproc |

#### 内存管理
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `kernel/sysproc.c#sys_mmap` | `kernel/sysfile.c#sys_mmap` | 同板块同名 | PGROUNDUP, clear_vma_range, file_pread_k, kalloc_page, kfree_page, kref_page ...(+6) | PGROUNDUP, acquire, argaddr, argint, filedup, myproc ...(+1) |
| `kernel/sysproc.c#sys_execve` | `kernel/sysproc.c#sys_execve` | 同板块同名 | copyin, exec_replace, fetch_str, strlcpy | NELEM, argaddr, argstr, execve, fetchaddr, fetchstr ...(+3) |
| `arch/riscv/usertrap.c#usertrap` | `kernel/trap.c#usertrap` | 同板块同名 | arch_intr_on, dev_intr, myproc, panic, printk, proc_exit ...(+10) | PGROUNDDOWN, devintr, elock, eread, eunlock, exit ...(+16) |
| `arch/riscv/usertrap.c#usertrapret` | `kernel/trap.c#usertrapret` | 同板块同名 | ((void (*)(u64))fn), MAKE_SATP, arch_intr_off, myproc, r_satp, r_sstatus ...(+3) | ((void (*)(uint64,uint64))fn), MAKE_SATP, intr_off, myproc, r_satp, r_sstatus ...(+4) |

#### 中断/异常处理
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `arch/loongarch/trap.c#dev_intr` | `kernel/trap.c#trapframedump` | 同模块上下文对齐/差异观察 | ESTAT_ECODE, timer_tick | printf |
| `arch/riscv/sbi.c#arch_intr_off` | `bootloader/SBI/rustsbi-k210/src/main.back.rs#set_rd` | 同模块上下文对齐/差异观察 | 内部闭环逻辑 | panic! |
| `arch/riscv/sbi.c#arch_intr_on` | `bootloader/SBI/rustsbi-k210/src/main.rs#set_rd` | 同模块上下文对齐/差异观察 | 内部闭环逻辑 | panic! |
| `arch/riscv/sbi.c#arch_intr_get` | `kernel/plic.c#plicinithart` | 同模块上下文对齐/差异观察 | 内部闭环逻辑 | PLIC_MENABLE, PLIC_SENABLE, PLIC_SPRIORITY, cpuid, printf, readd |

#### 系统调用
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `arch/riscv/sbi.c#sbi_ecall` | `kernel/syscall.c#argstr` | 同模块上下文对齐/差异观察 | 内部闭环逻辑 | argaddr, fetchstr |
| `arch/riscv/sbi.c#arch_shutdown` | `kernel/syscall.c#argaddr` | 同模块上下文对齐/差异观察 | arch_halt, sbi_ecall | argraw |
| `arch/riscv/sbi.c#sbi_set_timer` | `kernel/syscall.c#argint` | 同模块上下文对齐/差异观察 | sbi_ecall | argraw |

#### 文件系统
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `fs/file.c#ramfs_seed_dir` | `kernel/utils.c#set_gpio_bit` | 同板块同结构 | ramfs_create | set_bit_offset |
| `drivers/virtio_blk.c#blk_init_pci_legacy` | `xv6-user/grind.c#go` | 同模块上下文对齐/差异观察 | (u32), kva_to_pa, pa_to_kva, pci_cfg_read32, pci_cfg_write16, pci_cfg_write32 ...(+7) | chdir, close, dup, exec, exit, fork ...(+16) |
| `fs/file.c#ramfs_readdir` | `kernel/fat32.c#fat32_init` | 同模块上下文对齐/差异观察 | cb, strlen, strncmp | bread, brelse, initlock, initsleeplock, memmove, memset ...(+3) |
| `fs/file.c#ramfs_rename` | `kernel/sysfile.c#sys_rename` | 同模块上下文对齐/差异观察 | ramfs_lookup, ramfs_unlink, strlcpy, strlen, strncmp | argstr, dirlookup, edup, elock, emake, ename ...(+7) |

#### 设备驱动
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `drivers/virtio_blk.c#virtio_rw` | `kernel/spi.c#spi_receive_data_normal_dma` | 同模块上下文对齐/差异观察 | __sync_synchronize, kva_to_pa, pci_wo16, printk, wr | (uint32), dmac_set_single_mode, dmac_wait_done, spi_get_frame_format, spi_set_tmod, sysctl_dma_select |
| `drivers/virtio_blk.c#setup_legacy_vring` | `kernel/sdcard.c#check_block_size` | 同模块上下文对齐/差异观察 | PGROUNDUP, memset | printf, sd_end_cmd, sd_get_response_R1, sd_get_response_R3_rest, sd_send_cmd |
| `drivers/virtio_blk.c#find_block_device` | `kernel/uart.c#uartinit` | 同模块上下文对齐/差异观察 | 内部闭环逻辑 | WriteReg, initlock |
| `arch/loongarch/power.c#arch_shutdown` | `kernel/uart.c#uartstart` | 同模块上下文对齐/差异观察 | arch_halt, uart_puts | ReadReg, WriteReg, wakeup |

#### 网络协议栈
仅目标项目中观察到该模块代表函数，参考项目没有同模块可对齐证据：
- 目标项目：`kernel/main.c#is_netperf_script`，调用摘要：has_suffix

#### 文件系统/第三方库
仅目标项目中观察到该模块代表函数，参考项目没有同模块可对齐证据：
- 目标项目：`fs/ext4/ext4.c#ext4_readdir_from`，调用摘要：(u32), cb, extent_map, inode_size_bytes, le16, le32 ...(+3)
- 目标项目：`fs/ext4/ext4.c#extent_map`，调用摘要：(u32), le16, le32, read_block
- 目标项目：`fs/ext4/ext4.c#ext4_mount`，调用摘要：le16, le32, printk, read_bytes
- 目标项目：`fs/ext4/ext4.c#ext4_pread`，调用摘要：(u32), extent_map, inode_size_bytes, memcpy, read_block, read_inode

#### 通用模块/其他
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `lib/printf.c#panic` | `bootloader/SBI/rustsbi-k210/src/main.back.rs#panic` | 同板块同名 | arch_halt, do_format, emit, emit_str, va_end, va_start | println! |
| `lib/string.c#strncmp` | `kernel/string.c#strncmp` | 同板块同名 | 内部闭环逻辑 | 内部闭环逻辑 |
| `lib/string.c#memcmp` | `kernel/string.c#memcmp` | 同板块同名 | 内部闭环逻辑 | 内部闭环逻辑 |
| `lib/string.c#memcpy` | `xv6-user/ulib.c#memcpy` | 同板块同名 | 内部闭环逻辑 | memmove |


好的，审判长。本庭已收到控辩双方的全部陈述、证据及原始数据。现在，我将依据客观的AST（抽象语法树）数据、全模块确定性对比上下文以及双方论点，对本案进行最终的独立审计与定性仲裁。

### 1. 宏观判定
- **定性结论**: **独立开发，底层架构显著差异**
- **核心依据**: 客观AST相似度仅为1.01%，共同指纹仅8个，双方覆盖率均低于3%。该数据远低于同源性阈值，表明两个项目在代码实现层面几乎完全独立。控方所指控的“复用”行为，经查证，均源于操作系统内核领域的标准接口命名与必然的通用设计范式，而非代码抄袭。

### 2. 全模块覆盖矩阵解读
本庭对全模块覆盖矩阵进行逐项审查，以评估各模块的证据强度：

- **进程管理**: 双方均存在。存在2个共同AST指纹，覆盖率极低（目标3.85%，参考4.88%）。证据表明，同名函数（如`sched`, `yield`）虽多，但其内部调用图谱（同步原语、锁机制）存在本质差异。**证据强度：弱**。
- **任务调度/异步运行时**: 双方均存在。共同AST指纹为0，覆盖率为0.0%。尽管存在同名函数（如`kerneltrap`），但AST层面未发现任何结构重合。**证据强度：未形成**。
- **内存管理**: 双方均存在。存在3个共同AST指纹，覆盖率极低（目标1.86%，参考2.83%）。同名函数（如`sys_mmap`）的调用图谱差异巨大，目标项目涉及`kalloc_page`等自定义内存操作，参考项目则依赖`argaddr`等参数提取。**证据强度：弱**。
- **中断/异常处理**: 双方均存在。共同AST指纹为0，覆盖率为0.0%。证据列表中的对齐项（如`dev_intr` vs `trapframedump`）仅为同模块下的上下文对齐，AST结构无任何重合。**证据强度：未形成**。
- **系统调用**: 双方均存在。共同AST指纹为0，覆盖率为0.0%。对齐项（如`sbi_ecall` vs `argstr`）仅为同模块下的差异观察，无结构重合。**证据强度：未形成**。
- **文件系统**: 双方均存在。存在1个共同AST指纹，覆盖率极低（目标2.63%，参考1.41%）。目标项目基于`ext4`/`ramfs`，参考项目基于`fat32`，技术栈完全不同。**证据强度：弱**。
- **设备驱动**: 双方均存在。共同AST指纹为0，覆盖率为0.0%。对齐项（如`virtio_rw` vs `spi_receive_data_normal_dma`）展示了完全不同的硬件操作流。**证据强度：未形成**。
- **网络协议栈**: 仅目标项目存在。参考项目无此模块。此为单方模块，不构成同源证据。**证据强度：未形成**。
- **文件系统/第三方库**: 仅目标项目存在。参考项目无此模块。此为单方模块，不构成同源证据。**证据强度：未形成**。
- **通用模块/其他**: 双方均存在。共同AST指纹为0，覆盖率为0.0%。尽管存在同名基础函数（如`panic`, `strncmp`），但均为标准库或语言规范下的必然实现。**证据强度：未形成**。

### 3. 底层 AST 关键证据指纹表
本庭提取了全模块中仅有的、可对齐的AST证据指纹，并进行了技术审查。所有证据均为弱信号，无法支撑抄袭指控。

| 所属板块 | 目标项目路径 | 参考项目路径 | 证据类型 | 技术说明 |
| :--- | :--- | :--- | :--- | :--- |
| 进程管理 | `kernel/proc.c#sched` | `kernel/proc.c#sched` | 同板块同名 | 核心调度器骨架相似（`mycpu->myproc->swtch`），但同步原语不同（`arch_intr_get` vs `holding`），属OS调度器通用范式。 |
| 进程管理 | `kernel/proc.c#yield` | `kernel/proc.c#yield` | 同板块同名 | 进程让步逻辑骨架相似，但临界区保护机制不同（中断控制 vs 自旋锁）。 |
| 进程管理 | `kernel/proc.c#sleep` | `kernel/proc.c#sleep` | 同板块同名 | 进程休眠骨架相似，但同步原语不同，且目标项目额外集成了`proc_exit_signal`逻辑。 |
| 进程管理 | `kernel/sysproc.c#sys_clone` | `kernel/sysproc.c#sys_clone` | 同板块同名 | 系统调用入口相似，但内部实现路径不同（目标项目独立`clone`，参考项目关联`fork`）。 |
| 内存管理 | `kernel/sysproc.c#sys_mmap` | `kernel/sysfile.c#sys_mmap` | 同板块同名 | 函数名与功能相同，但内部调用图谱完全不同，反映了独立的内存管理实现。 |
| 内存管理 | `kernel/sysproc.c#sys_execve` | `kernel/sysproc.c#sys_execve` | 同板块同名 | 程序加载入口，调用流差异显著，目标项目使用`exec_replace`，参考项目使用`execve`。 |
| 内存管理 | `arch/riscv/usertrap.c#usertrap` | `kernel/trap.c#usertrap` | 同板块同名 | 用户态陷阱处理入口，调用图谱差异巨大，反映了不同的系统调用分发与处理机制。 |
| 文件系统 | `fs/file.c#ramfs_seed_dir` | `kernel/utils.c#set_gpio_bit` | 同板块同结构 | 极弱证据。AST结构巧合相似，但功能和上下文完全不同（文件系统初始化 vs GPIO位操作）。 |

### 4. 核心代码调用流对比剖析
本庭选取双方均存在的核心模块，对其关键路径进行深度析理，以论证其架构异构性。

* **【进程管理/调度器路径】**
* **底层技术析理**: 这是控方指控的核心。两者均实现了`mycpu()->myproc()->swtch()`的调度器骨架，但这属于操作系统调度器的**标准逻辑流**，是思想而非表达的复用。真正的差异在于**同步原语**：目标项目使用`arch_intr_get/off/on`，构建了基于中断控制的架构抽象层，面向多平台（RISC-V/LoongArch）；参考项目使用`acquire/release/holding`，是基于自旋锁的经典xv6同步模型。这种底层并发控制策略的根本性分歧，是独立开发的有力证明。
* **代码溯源链接**:
  - 目标项目: `kernel/proc.c#sched`
  - 参考项目: `kernel/proc.c#sched`

* **【内存管理/内存映射路径】**
* **底层技术析理**: 以`sys_mmap`为例，虽然函数名相同，但内部实现泾渭分明。目标项目的调用流涉及`clear_vma_range`, `kalloc_page`, `file_pread_k`等自定义VMA和物理页管理函数，体现了独立的内存管理子系统。参考项目的调用流则基于`argaddr`, `argint`, `filedup`等典型的xv6系统调用参数处理范式。两者在内存抽象和对象管理模型上完全不同。
* **代码溯源链接**:
  - 目标项目: `kernel/sysproc.c#sys_mmap`
  - 参考项目: `kernel/sysfile.c#sys_mmap`

* **【文件系统/文件操作路径】**
* **底层技术析理**: 目标项目实现了完整的`ext4`只读支持和`ramfs`内存文件系统，其文件操作函数（如`sys_chdir`）调用`ext4_lookup`, `ramfs_lookup`。参考项目则基于`fat32`文件系统，其`sys_chdir`调用`elock`, `ename`, `eput`等FAT32相关的inode操作接口。两者基于完全不同的文件系统技术栈，不存在代码复用基础。
* **代码溯源链接**:
  - 目标项目: `kernel/sysfile.c#sys_chdir`
  - 参考项目: `kernel/sysfile.c#sys_chdir`

### 5. 单方模块与降权项
以下模块或证据因仅存在于一方或属于公共知识，本庭将其列为降权项，不作为同源性判断依据：
- **单方模块**:
  - **网络协议栈** (`kernel/main.c#is_netperf_script`): 仅目标项目拥有，体现了超出参考项目的功能实现。
  - **文件系统/第三方库** (`fs/ext4/`): 目标项目集成了完整的ext4文件系统读取能力，这是其独立开发的显著特征。
- **降权项（公共接口/标准命名）**:
  - **POSIX系统调用封装**: `sys_getpid`, `sys_getppid`, `sys_getuid`, `sys_sched_yield`等函数，其实现通常是极简的寄存器读取或单一函数调用，是任何类Unix内核的必然形态，不具备独创性，不能作为抄袭证据。
  - **C标准库函数**: `memcmp`, `memcpy`, `strncmp`等函数，其实现受语言规范严格约束，代码高度收敛是正常现象。
  - **通用内核命名**: `panic`, `myproc`, `wakeup`等函数名是操作系统内核领域的通用术语，其存在本身不构成证据。

### 6. 保守结论
经本庭独立审计，目标项目`[2026-T2026100069910965-XX]`与参考项目`[2021-nostayuplate-北京大学]`的客观AST相似度仅为1.01%，这是一个可以忽略不计的数值。控方提出的“复用指控”，其证据基础是操作系统内核领域广泛使用的标准接口命名（如`sched`, `yield`, `sleep`）和通用设计范式（如调度器骨架）。然而，深入分析调用图谱后，双方在**同步原语、内存管理模型、文件系统技术栈、驱动架构**等核心底层实现上存在根本性、系统性的差异。目标项目展现出的多架构抽象层、ext4文件系统集成、独立网络协议栈等特征，是其独立开发的有力证明。综上所述，本庭裁定，所有指控均不成立，目标项目不存在对参考项目的代码抄袭或结构性复用行为。

------------------------------

