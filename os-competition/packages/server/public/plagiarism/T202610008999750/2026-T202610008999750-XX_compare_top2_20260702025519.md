# 🛡️ OS 内核赛自动化代码级架构审计报告

**审计时间**: `2026-07-02 02:55:19`
**参赛作品名称**: `2026-T202610008999750-XX`
**年份**: `2026`
**学校**: `XX`
**队伍名称**: `T202610008999750`
**比赛名字**: `OS-Competition`
**比赛子赛道**: `Kernel-Implementation`
**仓库地址**: `https://gitlab.eduxiji.net/educg-group-43501-3132633/T202610008999750-417`
**推测开发语言**: `Rust / C (由AST探针自动识别)`
**特征函数量**: `609`

## ⚔️ 底层代码同源性对比 (AST Homology)
> 本部分由专门的代码审查智能体，对该项目与符合年份条件的历届最高相似度项目进行控制流与底层调用对比。新版报告会先给出确定性的全模块覆盖矩阵，再给出 Agent 的分模块解释，避免只围绕少量抽样函数展开。

### 🏆 Top 2 底层对标: vs `2025-九莲宝灯一向听-天津理工大学`（天津理工大学 / 九莲宝灯一向听）

#### 对方作品信息

**对方年份**: `2025`
**对方学校**: `天津理工大学`
**对方队伍名称**: `九莲宝灯一向听`
**对方比赛名字**: `OS-Competition`
**对方比赛子赛道**: `Kernel-Implementation`
**对方仓库地址**: `https://gitlab.eduxiji.net/educg-group-36002-2710490/T202510060995409-3585`

> 双向覆盖率：共同 AST 指纹: 294；唯一 AST 指纹: 目标项目 542，参考项目 2610；覆盖率: 目标项目 54.24%，参考项目 11.26%

## 全模块对比总览（确定性证据）

> 总体覆盖率：共同 AST 指纹: 294；唯一 AST 指纹: 目标项目 542，参考项目 2610；覆盖率: 目标项目 54.24%，参考项目 11.26%

### 1. 模块覆盖矩阵
| 模块 | 目标函数数 | 参考函数数 | 共同唯一 AST 指纹 | Target 覆盖 | Base 覆盖 | 状态 |
| :--- | ---: | ---: | ---: | ---: | ---: | :--- |
| 进程管理 | 57 | 79 | 25 | 44.64% | 34.25% | 双方存在且有结构重合 |
| 任务调度/异步运行时 | 77 | 385 | 48 | 66.67% | 17.45% | 双方存在且有结构重合 |
| 内存管理 | 60 | 301 | 9 | 15.25% | 3.31% | 双方存在且有结构重合 |
| 中断/异常处理 | 3 | 60 | 1 | 33.33% | 1.96% | 双方存在且有结构重合 |
| 系统调用 | 53 | 130 | 32 | 82.05% | 29.91% | 双方存在且有结构重合 |
| 文件系统 | 230 | 616 | 88 | 43.56% | 19.38% | 双方存在且有结构重合 |
| 设备驱动 | 3 | 156 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |
| 网络协议栈 | 6 | 61 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |
| 文件系统/第三方库 | 0 | 577 | 0 | 0.0% | 0.0% | 仅参考项目体现 |
| 网络协议栈/第三方库 | 0 | 130 | 0 | 0.0% | 0.0% | 仅参考项目体现 |
| 第三方库 | 0 | 209 | 0 | 0.0% | 0.0% | 仅参考项目体现 |
| 通用模块/其他 | 120 | 630 | 95 | 87.16% | 19.75% | 双方存在且有结构重合 |

### 2. 分模块证据清单

#### 进程管理
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `api/src/imp/task/wait.rs#sys_waitpid` | `api/src/imp/task/wait.rs#sys_waitpid` | 同板块同名 | Err, Ok, WaitOptions::from_bits_truncate, WaitPid::Pgid, WaitPid::Pid, all_children
        .into_iter ...(+33) | Err, Ok, WaitOptions::from_bits_truncate, WaitPid::Pgid, WaitPid::Pid, child.data::<ProcessData> ...(+25) |
| `api/src/imp/task/exit.rs#do_exit` | `api/src/imp/task/exit.rs#do_exit` | 同板块同名 | FD_TABLE.clear, SignalInfo::new, UserPtr::<Pid>::from, axtask::exit, axtask::yield_now, clear_child_tid.get_as_mut ...(+29) | FD_TABLE.clear, SignalInfo::new, UserPtr::<Pid>::from, axtask::exit, axtask::yield_now, clear_child_tid.get_as_mut ...(+22) |
| `api/src/imp/signal.rs#sys_rt_sigprocmask` | `api/src/imp/signal.rs#sys_rt_sigprocmask` | 同板块同名同结构 | Err, Ok, check_sigset_size, current, current()
        .task_ext, current()
        .task_ext()
        .thread_data ...(+2) | Err, Ok, check_sigset_size, current, current()
        .task_ext, current()
        .task_ext()
        .thread_data ...(+2) |
| `apps/libc/c/signal/signal.c#test_sigwait` | `apps/libc/c/signal/signal.c#test_sigwait` | 同板块同名同结构 | exit, fork, kill, puts, sigaddset, sigemptyset ...(+5) | exit, fork, kill, puts, sigaddset, sigemptyset ...(+5) |

#### 任务调度/异步运行时
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `src/syscall.rs#handle_syscall` | `src/syscall.rs#handle_syscall` | 同板块同名 | Err, Sysno::from, check_process_itimers, err.code, info!, result.unwrap_or_else ...(+112) | Ok, Sysno::from, err.code, info!, result.unwrap_or_else, stub_bypass ...(+115) |
| `api/src/imp/fs/stat.rs#sys_statx` | `api/src/imp/fs/stat.rs#sys_statx` | 同板块同名 | Err, Ok, apply_fd_timestamp, debug!, format_args!, get_file_like ...(+9) | Err, Ok, debug!, f.stat, f.stat()?.into, get_file_like ...(+9) |
| `api/src/imp/futex.rs#sys_futex` | `api/src/imp/futex.rs#sys_futex` | 同板块同名同结构 | Err, Ok, axtask::yield_now, curr.task_ext, curr.task_ext().process_data, current ...(+16) | Err, Ok, axtask::yield_now, curr.task_ext, curr.task_ext().process_data, current ...(+16) |
| `apps/nimbos/c/lib/stdio.c#fprintf` | `apps/nimbos/c/lib/stdio.c#fprintf` | 同板块同名同结构 | out, printint, printptr, putchar, strnlen, va_arg ...(+2) | out, printint, printptr, putchar, strnlen, va_arg ...(+2) |

#### 内存管理
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `api/src/imp/task/clone.rs#sys_clone` | `api/src/imp/task/clone.rs#sys_clone` | 同板块同名 | Arc::default, Arc::new, CURRENT_DIR
                .deref_from, CURRENT_DIR
                .deref_from(&process_data.ns)
                .init_new, CURRENT_DIR
                .deref_from(&process_data.ns)
                .init_shared, CURRENT_DIR.copy_inner ...(+84) | Arc::default, Arc::new, CURRENT_DIR
                .deref_from, CURRENT_DIR
                .deref_from(&process_data.ns)
                .init_new, CURRENT_DIR
                .deref_from(&process_data.ns)
                .init_shared, CURRENT_DIR.copy_inner ...(+72) |
| `api/src/imp/mm/mmap.rs#sys_mmap` | `api/src/imp/mm/mmap.rs#sys_mmap` | 同板块同名 | (addr + length).align_up, Err, File::from_fd, MmapFlags::from_bits_truncate, MmapProt::from_bits_truncate, Ok ...(+27) | Err, File::from_fd, MmapFlags::from_bits_truncate, MmapProt::from_bits_truncate, Ok, VirtAddr::from ...(+27) |
| `core/src/mm.rs#load_user_app` | `core/src/mm.rs#load_user_app` | 同板块同名 | CStr::from_bytes_with_nul, CStr::from_bytes_with_nul(interp)
            .map_err, CStr::from_bytes_with_nul(interp)
            .map_err(|_| AxError::InvalidData)?
            .to_str, CStr::from_bytes_with_nul(interp)
            .map_err(|_| AxError::InvalidData)?
            .to_str()
            .map_err, ElfFile::new, ElfFile::new(&file_data).map_err ...(+22) | CStr::from_bytes_with_nul, CStr::from_bytes_with_nul(interp)
                .map_err, CStr::from_bytes_with_nul(interp)
                .map_err(|_| AxError::InvalidData)?
                .to_str, CStr::from_bytes_with_nul(interp)
                .map_err(|_| AxError::InvalidData)?
                .to_str()
                .map_err, ElfFile::new, ElfFile::new(&file_data).map_err ...(+41) |
| `src/entry.rs#run_user_app` | `src/entry.rs#run_user_app` | 同板块同名 | Arc::default, Arc::new, CURRENT_DIR
        .deref_from, CURRENT_DIR
        .deref_from(&process_data.ns)
        .init_new, CURRENT_DIR.copy_inner, CURRENT_DIR_PATH
        .deref_from ...(+54) | Arc::default, Arc::new, CURRENT_DIR
        .deref_from, CURRENT_DIR
        .deref_from(&process_data.ns)
        .init_new, CURRENT_DIR.copy_inner, CURRENT_DIR_PATH
        .deref_from ...(+46) |

#### 中断/异常处理
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `api/src/signal.rs#post_trap_callback` | `api/src/signal.rs#post_trap_callback` | 同板块同名同结构 | check_signals | check_signals |
| `api/src/file/pipe.rs#write` | `arceos/ulib/axlibc/c/pow.c#exp_inline` | 同模块上下文对齐/差异观察 | Arc::strong_count, Err, Ok, buf.is_empty, buf.len, drop ...(+11) | __math_oflow, __math_uflow, asdouble, asuint64, converttoint, eval_as_double ...(+4) |
| `api/src/file/pipe.rs#read` | `arceos/modules/axhal/src/arch/aarch64/trap.rs#handle_sync_exception` | 同模块上下文对齐/差异观察 | Arc::strong_count, Err, Ok, buf.is_empty, buf.iter_mut, buf.iter_mut().take ...(+13) | ESR_EL1.extract, crate::trap::handle_syscall, crate::trap::post_trap_callback, debug!, esr.read, esr.read_as_enum ...(+6) |

#### 系统调用
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `apps/nimbos/rust/src/arch/riscv.rs#sys_clone` | `apps/nimbos/rust/src/arch/riscv.rs#sys_clone` | 同板块同名同结构 | naked_asm! | naked_asm! |
| `apps/nimbos/rust/src/arch/x86_64.rs#sys_clone` | `apps/nimbos/rust/src/arch/x86_64.rs#sys_clone` | 同板块同名同结构 | naked_asm! | naked_asm! |
| `apps/nimbos/rust/src/arch/aarch64.rs#sys_clone` | `apps/nimbos/rust/src/arch/aarch64.rs#sys_clone` | 同板块同名同结构 | naked_asm! | naked_asm! |
| `apps/nimbos/rust/src/lib.rs#thread_spawn` | `apps/nimbos/rust/src/lib.rs#thread_spawn` | 同板块同名同结构 | AtomicUsize::new, THREAD_COUNT.fetch_add, THREAD_STACKS[thread_id].as_ptr_range, sys_clone | AtomicUsize::new, THREAD_COUNT.fetch_add, THREAD_STACKS[thread_id].as_ptr_range, sys_clone |

#### 文件系统
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `api/src/imp/fs/io.rs#sys_sendfile` | `api/src/imp/fs/io.rs#sys_sendfile` | 同板块同名 | Err, File::from_fd, Ok, SeekFrom::Current, SeekFrom::Start, buf.len ...(+10) | Ok, debug!, dest.as_ref, do_sendfile, get_file_like, if let Some(offset) = offset {
        let src = src
            .into_any()
            .downcast::<File>()
            .map_err(|_| LinuxError::ESPIPE)?;

        do_sendfile(
            |buf| {
                let bytes_read = src.inner().read_at(*offset, buf)?;
                *offset += bytes_read as u64;
                Ok(bytes_read)
            },
            dest.as_ref(),
        )
    } else {
        do_sendfile(|buf| src.read(buf), dest.as_ref())
    }
    .map ...(+7) |
| `api/src/imp/fs/ctl.rs#sys_renameat2` | `api/src/imp/fs/ctl.rs#sys_renameat2` | 同板块同名 | Err, Ok, axfs::api::metadata, axfs::api::metadata(new_path.as_str()).ok, axfs::api::remove_dir, axfs::api::remove_file ...(+10) | Err, Ok, RenameFlags::from_bits_truncate, axfs::api::metadata, axfs::api::metadata(&new_path).is_ok, axfs::api::rename ...(+5) |
| `api/src/imp/fs/fd_ops.rs#sys_fcntl` | `api/src/imp/fs/fd_ops.rs#sys_fcntl` | 同板块同名 | Ok, debug!, dup_fd_from_with_flags, file.into_any, file.into_any().downcast::<Socket>, file.set_nonblocking ...(+7) | Ok, debug!, dup_fd, get_file_like, get_file_like(fd)?.set_nonblocking, warn! |
| `api/src/imp/fs/stat.rs#sys_fstatat` | `api/src/imp/fs/stat.rs#sys_fstatat` | 同板块同名 | Err, Ok, apply_fd_timestamp, debug!, format_args!, get_file_like ...(+9) | Err, Ok, debug!, f.stat, f.stat()?.into, get_file_like ...(+9) |

#### 设备驱动
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `api/src/socket.rs#read_from_user` | `arceos/modules/axdriver/src/dwmac.rs#set_clocks_uboot` | 同模块上下文对齐/差异观察 | Err, SocketAddrV4::read_from_user, SocketAddrV4::read_from_user(addr, addrlen).map, SocketAddrV6::read_from_user, SocketAddrV6::read_from_user(addr, addrlen).map, addr.get_as_ref ...(+2) | <Self as DwmacHal>::mmio_phys_to_virt, <Self as DwmacHal>::mmio_phys_to_virt(pac::AONCRG::ptr() as usize, 0x1000).as_ptr, <Self as DwmacHal>::mmio_phys_to_virt(pac::SYSCRG::ptr() as usize, 0x1000).as_ptr, DwmacHalImpl::wait_until, Self::check_diff, aoncrg.clk_ahb_gmac5 ...(+67) |
| `api/src/socket.rs#read_from_user` | `arceos/modules/axdriver/src/dwmac.rs#print_preserved_status` | 同模块上下文对齐/差异观察 | Err, Ipv6Addr::from, Ok, SocketAddrV6::new, copy_sockaddr_from_user, size_of::<sockaddr_in6> ...(+3) | <Self as DwmacHal>::mmio_phys_to_virt, <Self as DwmacHal>::mmio_phys_to_virt(pac::AONCRG::ptr() as usize, 0x1000).as_ptr, <Self as DwmacHal>::mmio_phys_to_virt(pac::SYSCRG::ptr() as usize, 0x1000).as_ptr, log::info!, pac::AONCRG::ptr, pac::SYSCRG::ptr ...(+24) |
| `api/src/socket.rs#read_from_user` | `arceos/modules/axdriver/src/dwmac.rs#print_clocks` | 同模块上下文对齐/差异观察 | Err, Ipv4Addr::from_bits, Ok, SocketAddrV4::new, copy_sockaddr_from_user, size_of::<sockaddr_in> ...(+3) | <Self as DwmacHal>::mmio_phys_to_virt, <Self as DwmacHal>::mmio_phys_to_virt(pac::AONCRG::ptr() as usize, 0x1000).as_ptr, <Self as DwmacHal>::mmio_phys_to_virt(pac::SYSCRG::ptr() as usize, 0x1000).as_ptr, aoncrg.clk_axi_gmac5, aoncrg.clk_axi_gmac5().read, aoncrg.clk_axi_gmac5().read().clk_icg ...(+27) |

#### 网络协议栈
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `api/src/socket.rs#write_to_user` | `arceos/ulib/axlibc/c/network.c#inet_pton` | 同模块上下文对齐/差异观察 | Err, Ok, addr.get_as_mut, addr.is_null, core::ptr::copy_nonoverlapping, self.flowinfo ...(+7) | hexval, inet_pton, isdigit, memmove |
| `api/src/socket.rs#write_to_user` | `arceos/api/arceos_posix_api/src/imp/net.rs#shutdown` | 同模块上下文对齐/差异观察 | Err, Ok, addr.get_as_mut, addr.is_null, core::ptr::copy_nonoverlapping, self.ip ...(+5) | Ok, tcpsocket.lock, tcpsocket.peer_addr, tcpsocket.shutdown, udpsocket.lock, udpsocket.peer_addr ...(+1) |
| `api/src/socket.rs#write_to_user` | `arceos/ulib/axlibc/c/socket.c#accept4` | 同模块上下文对齐/差异观察 | Err, addr.is_null, v4.write_to_user, v6.write_to_user | accept, fcntl |
| `api/src/socket.rs#family` | `arceos/api/arceos_posix_api/src/imp/net.rs#recvfrom` | 同模块上下文对齐/差异观察 | v4.family, v6.family | Ok, Some, tcpsocket.lock, tcpsocket.lock().recv, tcpsocket.lock().recv(buf).map, udpsocket
                .lock ...(+2) |

#### 文件系统/第三方库
仅参考项目中观察到该模块代表函数，目标项目没有同模块可对齐证据：
- 参考项目：`crates/lwext4_rust/c/lwext4/src/ext4_balloc.c#ext4_balloc_alloc_block`，调用摘要：ext4_balloc_get_bgid_of_block, ext4_balloc_get_block_of_bgid, ext4_balloc_set_bitmap_csum, ext4_balloc_verify_bitmap_csum, ext4_bg_get_block_bitmap, ext4_bg_get_free_blocks_count ...(+19)
- 参考项目：`crates/lwext4_rust/c/lwext4/src/ext4_journal.c#jbd_journal_prepare`，调用摘要：EXT4_BLOCK_ZERO, RB_FIND, TAILQ_FOREACH_REVERSE_SAFE, TAILQ_FOREACH_SAFE, TAILQ_REMOVE, ext4_bcache_clear_dirty ...(+17)
- 参考项目：`crates/lwext4_rust/c/lwext4/src/ext4.c#ext4_dir_rm`，调用摘要：EXT4_MP_LOCK, EXT4_MP_UNLOCK, ext4_block_cache_write_back, ext4_dir_en_get_inode, ext4_dir_iterator_fini, ext4_dir_iterator_init ...(+19)
- 参考项目：`crates/lwext4_rust/c/lwext4/src/ext4_dir_idx.c#ext4_dir_dx_split_index`，调用摘要：ext4_block_set, ext4_dir_dx_climit_get_count, ext4_dir_dx_climit_get_limit, ext4_dir_dx_climit_set_count, ext4_dir_dx_climit_set_limit, ext4_dir_dx_entry_get_hash ...(+10)

#### 网络协议栈/第三方库
仅参考项目中观察到该模块代表函数，目标项目没有同模块可对齐证据：
- 参考项目：`arceos/modules/axnet/src/smoltcp_impl/tcp.rs#connect`，调用摘要：Err, Ok, Ok::<(IpEndpoint, IpEndpoint), AxError>, SOCKET_SET
                .with_socket_mut::<tcp::Socket, _, _>, SOCKET_SET.add, SocketSetWrapper::new_tcp_socket ...(+32)
- 参考项目：`arceos/modules/axnet/src/smoltcp_impl/dns.rs#query`，调用摘要：Err, Ok, SOCKET_SET
            .with_socket_mut::<dns::Socket, _, _>, SOCKET_SET
            .with_socket_mut::<dns::Socket, _, _>(handle, |socket| {
                socket.start_query(iface.lock().context(), name, query_type)
            })
            .map_err, SOCKET_SET.poll_interfaces, SOCKET_SET.with_socket_mut::<dns::Socket, _, _> ...(+12)
- 参考项目：`arceos/modules/axnet/src/smoltcp_impl/tcp.rs#recv_timeout`，调用摘要：Err, Ok, SOCKET_SET.with_socket_mut::<tcp::Socket, _, _>, ax_err!, ax_err_type!, current_ticks ...(+11)
- 参考项目：`arceos/modules/axnet/src/smoltcp_impl/mod.rs#init`，调用摘要：Config::new, ETH0.init_once, EthernetAddress, GATEWAY.parse, GATEWAY.parse().expect, IP.parse ...(+22)

#### 第三方库
仅参考项目中观察到该模块代表函数，目标项目没有同模块可对齐证据：
- 参考项目：`crates/axdriver_net/src/dwmac/mod.rs#init_phy`，调用摘要：Err, H::wait_until, Ok, Yt8531cPhy::<H>::new, core::time::Duration::from_millis, log::error! ...(+18)
- 参考项目：`crates/axdriver_net/src/dwmac/mod.rs#init`，调用摘要：AtomicBool::new, DescriptorRing::<RX_DESC_COUNT, H>::new, DescriptorRing::<TX_DESC_COUNT, H>::new, H::configure_platform, Ok, log::error! ...(+20)
- 参考项目：`crates/visionfive2-sd/src/lib.rs#send_cmd`，调用摘要：RawInterrupt::from, RawInterruptStatusReg::from, S::sleep_ms_until, Some, arg.into, assert! ...(+22)
- 参考项目：`crates/visionfive2-sd/src/lib.rs#init_sdcard`，调用摘要：CardTypeReg::from, CardTypeReg::from(0).with_card_width4_1, CmdArg::new, CmdReg::from, ControlReg::from, RawInterruptStatusReg::from ...(+20)

#### 通用模块/其他
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `apps/libc/c/signal/signal.c#test_sigsuspend` | `apps/libc/c/signal/signal.c#test_sigsuspend` | 同板块同名同结构 | exit, fork, kill, puts, sigaction, sigaddset ...(+4) | exit, fork, kill, puts, sigaction, sigaddset ...(+4) |
| `apps/nimbos/c/lib/stdio.c#out` | `apps/nimbos/c/lib/stdio.c#out` | 同板块同名同结构 | __clear_buffer, __write_buffer, write | __clear_buffer, __write_buffer, write |
| `api/src/path.rs#name` | `api/src/path.rs#name` | 同板块同名同结构 | Ok, path.ends_with, path.len, path.rfind, path.rfind('/').ok_or, path.strip_suffix ...(+3) | Ok, path.ends_with, path.len, path.rfind, path.rfind('/').ok_or, path.strip_suffix ...(+3) |
| `apps/nimbos/c/lib/stdio.c#printint` | `apps/nimbos/c/lib/stdio.c#printint` | 同板块同名同结构 | assert, out | assert, out |


好的，我将遵循审计指令，以深度析理模式对本案进行最终同源性定性仲裁。

### 1. 宏观判定
- **定性结论**: **独立开发，底层架构存在显著差异**。
- **核心依据**: 客观 AST 相似度仅为 10.29%，处于极低水平。参考项目 88.74% 的 AST 指纹未在目标项目中出现，且目标项目完全缺失参考项目中成体系的设备驱动、网络协议栈及第三方文件系统库。双方在进程管理、文件系统等核心模块的相似性，可被同一开源框架（ArceOS/axtask）与 POSIX 规范强约束下的接口层同构合理解释，不构成代码抄袭。

### 2. 全模块覆盖矩阵解读
- **进程管理**: 双方均存在。存在 25 个共同 AST 指纹。目标覆盖率 44.64%，参考覆盖率 34.25%。重合点集中在 POSIX 系统调用入口和框架强制的对象访问路径上，证据强度为**中**。
- **任务调度/异步运行时**: 双方均存在。存在 48 个共同 AST 指纹。目标覆盖率 66.67%，参考覆盖率 17.45%。目标项目在该模块高度依赖参考框架，但参考项目自身有更丰富的调度器实现，证据强度为**中**。
- **内存管理**: 双方均存在。存在 9 个共同 AST 指纹。目标覆盖率 15.25%，参考覆盖率 3.31%。覆盖率极低，表明双方在内存管理核心策略上差异巨大，证据强度为**弱**。
- **中断/异常处理**: 双方均存在。存在 1 个共同 AST 指纹。目标覆盖率 33.33%，参考覆盖率 1.96%。仅有的共同指纹为信号回调，属于框架接口，证据强度为**弱**。
- **系统调用**: 双方均存在。存在 32 个共同 AST 指纹。目标覆盖率 82.05%，参考覆盖率 29.91%。目标项目在系统调用分发层高度复用了参考框架的路径，但参考项目有更多独立实现，证据强度为**中**。
- **文件系统**: 双方均存在。存在 88 个共同 AST 指纹。目标覆盖率 43.56%，参考覆盖率 19.38%。重合点主要在 VFS 接口层，底层实现（如 ext4）完全不同，证据强度为**中**。
- **设备驱动**: 双方均存在，但**未观察到共同结构指纹** (0.0%)。双方驱动体系完全不同，证据**未形成**。
- **网络协议栈**: 双方均存在，但**未观察到共同结构指纹** (0.0%)。双方网络栈实现完全不同，证据**未形成**。
- **第三方库/公共依赖**: 参考项目包含大量第三方库（lwext4, smoltcp, dwmac 驱动等），目标项目**完全缺失**这些模块。这是支持独立开发的强反向证据，证据强度为**强（反向）**。

### 3. 底层 AST 关键证据指纹表
| 所属板块 | 目标项目路径 | 参考项目路径 | 证据类型 | 技术说明 |
| :--- | :--- | :--- | :--- | :--- |
| 进程管理 | `api/src/imp/signal.rs#sys_rt_sigprocmask` | `api/src/imp/signal.rs#sys_rt_sigprocmask` | 同板块同名同结构 | 信号掩码修改逻辑完全一致，调用链 `current().task_ext().thread_data().signal.with_blocked_mut` 是框架强约束下的唯一路径。 |
| 进程管理 | `api/src/imp/task/thread.rs#sys_getppid` | `api/src/imp/task/thread.rs#sys_getppid` | 同板块同名同结构 | 获取父进程 PID 的长链路 `axtask::current().task_ext().thread.process().parent().unwrap().pid` 由 axtask 对象模型唯一决定。 |
| 任务调度/异步运行时 | `src/syscall.rs#handle_syscall` | `src/syscall.rs#handle_syscall` | 同板块同名 | 系统调用分发逻辑高度相似，均使用 `Sysno::from` 解析调用号，但目标项目增加了 `check_process_itimers` 调用。 |
| 任务调度/异步运行时 | `api/src/imp/futex.rs#sys_futex` | `api/src/imp/futex.rs#sys_futex` | 同板块同名同结构 | futex 核心等待逻辑 `curr.task_ext().process_data().wait_timeout` 调用路径一致，是框架提供的标准同步原语。 |
| 内存管理 | `api/src/imp/task/clone.rs#sys_clone` | `api/src/imp/task/clone.rs#sys_clone` | 同板块同名 | `clone` 系统调用中处理 `CURRENT_DIR` 的 Arc 共享逻辑相似，但目标项目缺少参考项目的部分命名空间初始化细节。 |
| 内存管理 | `core/src/mm.rs#load_user_app` | `core/src/mm.rs#load_user_app` | 同板块同名 | ELF 加载逻辑相似，均使用 `ElfFile::new` 解析，但目标项目增加了对解释器路径的额外处理。 |
| 系统调用 | `apps/nimbos/rust/src/arch/riscv.rs#sys_clone` | `apps/nimbos/rust/src/arch/riscv.rs#sys_clone` | 同板块同名同结构 | 架构相关的 `clone` 汇编实现完全一致，均为 `naked_asm!` 宏包裹的上下文切换代码，属于公共底层模板。 |
| 文件系统 | `api/src/imp/fs/stat.rs#sys_statx` | `api/src/imp/fs/stat.rs#sys_statx` | 同板块同名 | `statx` 系统调用实现相似，均调用 `get_file_like` 获取文件对象并转换状态信息，但目标项目增加了 `apply_fd_timestamp` 调用。 |
| 文件系统 | `api/src/imp/fs/fd_ops.rs#sys_fcntl` | `api/src/imp/fs/fd_ops.rs#sys_fcntl` | 同板块同名 | `fcntl` 实现相似，但目标项目使用 `dup_fd_from_with_flags` 并尝试 downcast 到 `Socket`，参考项目使用 `dup_fd`，表明功能扩展。 |
| 第三方库/公共依赖 | `apps/libc/c/signal/signal.c#test_sigwait` | `apps/libc/c/signal/signal.c#test_sigwait` | 同板块同名同结构 | POSIX 信号测试用例，调用流 `sigemptyset` -> `sigaddset` -> `sigprocmask` -> `kill` 是标准测试流程，属于公共测试代码。 |

### 4. 核心代码调用流对比剖析
* **【进程管理/信号处理路径】**
* **底层技术析理**: 双方在 `sys_rt_sigprocmask` 的实现中，均遵循了从当前任务 `current()` 出发，通过 `task_ext()` 获取扩展，再通过 `thread_data()` 访问线程特定数据，最终操作 `signal` 对象的 `with_blocked_mut` 方法这一调用流。此路径由 ArceOS 的 `axtask` 框架和其 POSIX 兼容层 `arceos_posix_api` 的 `SignalModule` 接口强制定义，是访问和修改当前任务信号掩码的唯一标准范式，不构成独创性表达。
* **代码溯源链接**:
  - 目标项目: `api/src/imp/signal.rs#sys_rt_sigprocmask`
  - 参考项目: `api/src/imp/signal.rs#sys_rt_sigprocmask`

* **【任务调度/异步运行时/系统调用分发】**
* **底层技术析理**: 双方在 `handle_syscall` 中均采用 `Sysno::from` 解析系统调用号，并通过宏或匹配表分发到具体处理函数。目标项目在分发前插入了 `check_process_itimers` 调用，用于处理进程定时器，而参考项目则包含 `stub_bypass` 等桩代码。这表明目标项目在参考框架的基础上进行了功能增强和定制，而非简单复制。
* **代码溯源链接**:
  - 目标项目: `src/syscall.rs#handle_syscall`
  - 参考项目: `api/src/imp/task/execve.rs#sys_execve`

* **【文件系统/状态获取】**
* **底层技术析理**: 在 `sys_statx` 的实现中，双方都调用了 `get_file_like` 获取文件对象，然后调用其 `stat` 方法并转换为 Linux 的 `statx` 结构。目标项目额外调用了 `apply_fd_timestamp`，表明其在文件时间戳管理上有独立逻辑。这种在相同框架接口上增加差异化功能的模式，是独立开发的典型特征。
* **代码溯源链接**:
  - 目标项目: `api/src/imp/fs/stat.rs#sys_statx`
  - 参考项目: `api/src/imp/fs/stat.rs#sys_statx`

### 5. 单方模块与降权项
* **仅参考项目存在的模块**:
  - **文件系统/第三方库**: `crates/lwext4_rust` (ext4 实现)
  - **网络协议栈/第三方库**: `arceos/modules/axnet` (smoltcp 集成)
  - **设备驱动/第三方库**: `arceos/modules/axdriver/src/dwmac.rs` (网卡驱动), `crates/visionfive2-sd` (SD卡驱动)
  - **第三方库**: `crates/axdriver_net` 等。
  这些模块是参考项目的核心组成部分，目标项目完全缺失，构成强有力的“反向证据”，证明目标项目并非对参考项目的全量克隆，而是基于不同硬件和需求进行了裁剪和独立实现。
* **降权项说明**:
  - **公共测试代码**: `apps/nimbos` 和 `apps/libc` 下的测试用例（如 `forktree`, `signal.c`）属于公共教学示例或 libc-test 套件，其 AST 重合不能作为内核代码抄袭的证据。
  - **跨板块证据**: 控方将参考项目中 `arceos/modules/axtask/src/run_queue.rs` (调度器) 等底层模块与目标项目的 `api` 层进行对比，属于跨抽象层级硬凑，不构成同板块主证据。
  - **Trivial 代码**: `core/src/task.rs` 中的 `get_process`, `clear_child_tid` 等函数为简单的原子操作或哈希表访问，其 AST 结构由 Rust 标准库惯用法决定，不具备可保护性。

### 6. 保守结论
本次审计基于 10.29% 的极低 AST 相似度，认定目标项目 `[2026-T202610008999750-XX]` 与参考项目 `[2025-九莲宝灯一向听-天津理工大学]` 为**独立开发**。双方在进程管理、文件系统等模块的局部相似性，根源于对同一开源框架 (ArceOS) 和 POSIX 规范的遵循，属于接口层的必然同构，而非核心逻辑的抄袭。关键的反向证据在于，目标项目完全缺失参考项目中成体系的设备驱动、网络协议栈及第三方文件系统库，且覆盖率指标呈现显著的方向性差异（参考覆盖率仅 11.26%）。整体来看，目标项目是在公共框架基础上进行的独立裁剪、移植和功能扩展，不存在违规复用风险。

------------------------------

