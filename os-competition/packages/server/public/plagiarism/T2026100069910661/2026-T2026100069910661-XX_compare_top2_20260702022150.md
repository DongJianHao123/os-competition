# 🛡️ OS 内核赛自动化代码级架构审计报告

**审计时间**: `2026-07-02 02:21:50`
**参赛作品名称**: `2026-T2026100069910661-XX`
**年份**: `2026`
**学校**: `XX`
**队伍名称**: `T2026100069910661`
**比赛名字**: `OS-Competition`
**比赛子赛道**: `Kernel-Implementation`
**仓库地址**: `https://gitlab.eduxiji.net/educg-group-43501-3132633/T2026100069910661-1865`
**推测开发语言**: `Rust / C (由AST探针自动识别)`
**特征函数量**: `2`

## ⚔️ 底层代码同源性对比 (AST Homology)
> 本部分由专门的代码审查智能体，对该项目与符合年份条件的历届最高相似度项目进行控制流与底层调用对比。新版报告会先给出确定性的全模块覆盖矩阵，再给出 Agent 的分模块解释，避免只围绕少量抽样函数展开。

### 🏆 Top 2 底层对标: vs `2024-ECNU九队-华东师范大学`（华东师范大学 / ECNU九队）

#### 对方作品信息

**对方年份**: `2024`
**对方学校**: `华东师范大学`
**对方队伍名称**: `ECNU九队`
**对方比赛名字**: `OS-Competition`
**对方比赛子赛道**: `Kernel-Implementation`
**对方仓库地址**: `https://gitlab.eduxiji.net/educg-group-26010-2376550/T202410269992492-843`

> 双向覆盖率：共同 AST 指纹: 1；唯一 AST 指纹: 目标项目 2，参考项目 405；覆盖率: 目标项目 50.0%，参考项目 0.25%

## 全模块对比总览（确定性证据）

> 总体覆盖率：共同 AST 指纹: 1；唯一 AST 指纹: 目标项目 2，参考项目 405；覆盖率: 目标项目 50.0%，参考项目 0.25%

### 1. 模块覆盖矩阵
| 模块 | 目标函数数 | 参考函数数 | 共同唯一 AST 指纹 | Target 覆盖 | Base 覆盖 | 状态 |
| :--- | ---: | ---: | ---: | ---: | ---: | :--- |
| 进程管理 | 0 | 53 | 0 | 0.0% | 0.0% | 仅参考项目体现 |
| 任务调度/异步运行时 | 0 | 32 | 0 | 0.0% | 0.0% | 仅参考项目体现 |
| 内存管理 | 1 | 91 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |
| 中断/异常处理 | 0 | 12 | 0 | 0.0% | 0.0% | 仅参考项目体现 |
| 系统调用 | 0 | 37 | 0 | 0.0% | 0.0% | 仅参考项目体现 |
| 文件系统 | 0 | 102 | 0 | 0.0% | 0.0% | 仅参考项目体现 |
| 设备驱动 | 0 | 10 | 0 | 0.0% | 0.0% | 仅参考项目体现 |
| 网络协议栈 | 0 | 0 | 0 | 0.0% | 0.0% | 双方均未体现 |
| 文件系统/第三方库 | 0 | 82 | 0 | 0.0% | 0.0% | 仅参考项目体现 |
| 通用模块/其他 | 1 | 85 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |

### 2. 分模块证据清单

#### 进程管理
仅参考项目中观察到该模块代表函数，目标项目没有同模块可对齐证据：
- 参考项目：`kernel/proc/proc.c#proc_exit`，调用摘要：assert, ext4_file_close, ext4_inode_put, fat32_file_close, fat32_inode_put, myproc ...(+6)
- 参考项目：`kernel/proc/proc.c#proc_schedule`，调用摘要：intr_on, mycpu, spinlock_acquire, spinlock_release, swtch
- 参考项目：`user/src/waitpid.c#test_waitpid`，调用摘要：TEST_END, TEST_START, WEXITSTATUS, assert, exit, fork ...(+3)
- 参考项目：`kernel/proc/proc.c#proc_kill`，调用摘要：spinlock_acquire, spinlock_release

#### 任务调度/异步运行时
仅参考项目中观察到该模块代表函数，目标项目没有同模块可对齐证据：
- 参考项目：`kernel/fs/fat32/fat32_dir.c#fat32_dir_addInode`，调用摘要：(uint16), assert, checksum, fat32_cluster_write, fat32_inode_relocate, memset ...(+3)
- 参考项目：`kernel/lib/print.c#printf`，调用摘要：(uint64), panic, print_10, print_16, spinlock_acquire, spinlock_release ...(+4)
- 参考项目：`kernel/trap/trap_user.c#trap_user`，调用摘要：assert, external_interrupt_handler, intr_on, myproc, printf, proc_exit ...(+10)
- 参考项目：`kernel/dev/console.c#cons_intr`，调用摘要：内部闭环逻辑

#### 内存管理
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `kernel/init.c#mips_init` | `kernel/proc/exec.c#proc_exec` | 同模块上下文对齐/差异观察 | halt, putchar | ADD_AUXV, ALIGN_DOWN, ALIGN_UP, assert, ext4_dir_path_to_inode, ext4_file_close ...(+20) |

#### 中断/异常处理
仅参考项目中观察到该模块代表函数，目标项目没有同模块可对齐证据：
- 参考项目：`kernel/lock/spinlock.c#pop_off`，调用摘要：assert, intr_get, intr_on, mycpu
- 参考项目：`kernel/dev/uart.c#uart_intr`，调用摘要：cons_intr, spinlock_acquire, spinlock_release, uart_getc, uart_start
- 参考项目：`kernel/dev/plic.c#plic_inithart`，调用摘要：PLIC_SENABLE, PLIC_SPRIORITY, r_tp
- 参考项目：`kernel/lock/spinlock.c#push_off`，调用摘要：intr_get, intr_off, mycpu

#### 系统调用
仅参考项目中观察到该模块代表函数，目标项目没有同模块可对齐证据：
- 参考项目：`user/initcode.c#main`，调用摘要：get_len, syscall
- 参考项目：`user/lib/syscall.c#get_time`，调用摘要：sys_get_time
- 参考项目：`user/lib/syscall.c#clone`，调用摘要：__clone
- 参考项目：`user/lib/syscall.c#sys_linkat`，调用摘要：syscall

#### 文件系统
仅参考项目中观察到该模块代表函数，目标项目没有同模块可对齐证据：
- 参考项目：`kernel/fs/fat32/fat32_dir.c#fat32_dir_getNextInode`，调用摘要：assert, fat32_cluster_read, fat32_inode_relocate, memset, read_entry_info, read_entry_name
- 参考项目：`kernel/fs/fat32/fat32_dir.c#fat32_dir_link`，调用摘要：内部闭环逻辑
- 参考项目：`kernel/fs/fat32/fat32_cluster.c#fat32_cluster_write`，调用摘要：assert, buf_read, buf_release, buf_write, cluster_firstSector, min ...(+1)
- 参考项目：`kernel/dev/uart8250.c#uart8250_init`，调用摘要：get_reg, set_reg

#### 设备驱动
仅参考项目中观察到该模块代表函数，目标项目没有同模块可对齐证据：
- 参考项目：`kernel/dev/console.c#cons_putc`，调用摘要：uart8250_putc, uart_putc_sync
- 参考项目：`kernel/dev/uart.c#uart_init`，调用摘要：spinlock_init, write_reg
- 参考项目：`kernel/dev/console.c#cons_write`，调用摘要：spinlock_acquire, spinlock_release, uart8250_putc, uart_putc, vm_copyin
- 参考项目：`kernel/dev/virtio_disk.c#free_chain`，调用摘要：free_desc

#### 文件系统/第三方库
仅参考项目中观察到该模块代表函数，目标项目没有同模块可对齐证据：
- 参考项目：`kernel/fs/ext4/ext4_sys.c#ext4_sys_ppoll`，调用摘要：assert, myproc, proc_iskilled, proc_sleep, spinlock_acquire, spinlock_release ...(+3)
- 参考项目：`kernel/fs/ext4/ext4_dir.c#ext4_dir_pinode_to_inode`，调用摘要：assert, ext4_inode_dup, ext4_inode_get, ext4_inode_read, ext4_inode_readback, ext4_inode_search ...(+8)
- 参考项目：`kernel/fs/ext4/ext4.c#ext4_init`，调用摘要：assert, buf_read, buf_release, com, ext4_block_read, ext4_inode_init ...(+3)
- 参考项目：`kernel/fs/ext4/ext4_inode.c#ext4_inode_write`，调用摘要：(uint16), EXTENT_LEAF, assert, ext4_block_alloc, ext4_block_write, ext4_inode_writeback ...(+3)

#### 通用模块/其他
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `kernel/init.c#halt` | `kernel/lib/print.c#print_10` | 同模块上下文对齐/差异观察 | 内部闭环逻辑 | cons_puts |


好的，我将遵循审计指令，基于客观证据进行深度析理，给出最终定性结论。

### 1. 宏观判定
- **定性结论**: **独立开发**
  基于双向覆盖率指标（目标项目 50.0%，参考项目 0.25%）及共同 AST 指纹仅为 1 的客观事实，两项目在整体架构、代码规模和核心模块实现上存在根本性差异。目标项目是一个极简的 MIPS 架构启动原型，而参考项目是一个功能完备的 RISC-V/x86 风格操作系统内核。唯一的共同指纹在统计学上更可能是通用语法结构导致的偶然匹配，而非核心逻辑复用。

### 2. 全模块覆盖矩阵解读
- **进程管理**: 仅参考项目存在（53个函数）。目标项目无此模块，共同指纹为0。**证据强度: 未形成**。
- **任务调度/异步运行时**: 仅参考项目存在（32个函数）。目标项目无此模块，共同指纹为0。**证据强度: 未形成**。
- **内存管理**: 双方均存在，但规模悬殊（目标1个，参考91个）。共同指纹为0。目标项目仅包含一个初始化函数，未涉及页表、分配器等核心内存管理逻辑；参考项目则包含完整的 `exec` 加载、对齐、辅助向量等复杂实现。**证据强度: 未形成**。
- **中断/异常处理**: 仅参考项目存在（12个函数）。目标项目无此模块，共同指纹为0。**证据强度: 未形成**。
- **系统调用**: 仅参考项目存在（37个函数）。目标项目无此模块，共同指纹为0。**证据强度: 未形成**。
- **文件系统**: 仅参考项目存在（102个函数）。目标项目无此模块，共同指纹为0。**证据强度: 未形成**。
- **设备驱动**: 仅参考项目存在（10个函数）。目标项目无此模块，共同指纹为0。**证据强度: 未形成**。
- **网络协议栈**: 双方均未体现。**证据强度: 未形成**。
- **第三方库/公共依赖**: 仅参考项目存在（82个函数）。目标项目无此模块，共同指纹为0。**证据强度: 未形成**。

### 3. 底层 AST 关键证据指纹表
| 所属板块 | 目标项目路径 | 参考项目路径 | 证据类型 | 技术说明 |
| :--- | :--- | :--- | :--- | :--- |
| 通用模块/其他 | `kernel/init.c#halt` | `kernel/lib/print.c#print_10` | 弱信号 | 目标为内部闭环死循环，参考为调用 `cons_puts` 的有限迭代循环。两者在循环终止性、外部依赖和语义上完全不同，不构成同源证据。 |
| 内存管理 | `kernel/init.c#mips_init` | `kernel/proc/exec.c#proc_exec` | 弱信号 | 目标为启动初始化，调用 `halt`/`putchar`；参考为 ELF 加载器，调用 `ext4_*`/`ADD_AUXV` 等。功能、调用图谱和 AST 子图均无重合。 |

### 4. 核心代码调用流对比剖析
* **【内存管理/系统初始化路径】**
* **底层技术析理**: 目标项目的 `mips_init` 是 MIPS 架构的板级启动入口，其调用流为 `mips_init -> halt / putchar`，属于最简化的“初始化-停机”模型，无内存分配、页表映射或进程空间管理。参考项目的 `proc_exec` 是进程执行加载器，其调用流涉及 `ADD_AUXV`、`ALIGN_DOWN`、`ext4_*` 等，构建了完整的虚拟内存空间与文件系统交互模型。两者在抽象层次、内存模型和核心数据结构上完全异构。
* **代码溯源链接**:
  - 目标项目: `kernel/init.c#mips_init`
  - 参考项目: `kernel/proc/exec.c#proc_exec`

* **【通用模块/其他/系统停机路径】**
* **底层技术析理**: 目标项目的 `halt` 函数是“内部闭环逻辑”，在 AST 上表现为一个无出口的非终止循环节点，目的是锁定 CPU 状态。参考项目的 `print_10` 是一个格式化输出函数，其核心是一个基于除法/取模的终止循环，并通过 `cons_puts` 产生输出副作用。两者的控制流图在终止性、副作用和外部依赖上存在本质区别，不存在状态机复用。
* **代码溯源链接**:
  - 目标项目: `kernel/init.c#halt`
  - 参考项目: `kernel/lib/print.c#print_10`

### 5. 单方模块与降权项
- **单方模块**: 进程管理、任务调度、中断/异常处理、系统调用、文件系统、设备驱动等模块仅存在于参考项目，是构成其完整内核功能的核心。目标项目完全缺失这些模块，这是两项目架构异构的最有力证据。
- **降权项**: 控方提出的 `putchar` 与 `cons_puts` 的跨板块调用链，属于操作系统开发中常规的底层字符输出操作，且出现在完全不同的上下文（启动调试 vs. 格式化打印）中，不构成同源证据。唯一的共同 AST 指纹，在参考项目 405 个指纹的基数下覆盖率仅 0.25%，属于可忽略的统计噪声，极可能为通用语法结构。

### 6. 保守结论
本次审计基于双向覆盖率（目标 50.0%，参考 0.25%）和唯一共同 AST 指纹的客观事实。目标项目仅含 2 个函数，是一个极简的 MIPS 启动原型；参考项目拥有 405 个独立指纹，是一个功能完备的现代教学内核。两者在架构完整性、核心模块实现和调用图谱上存在根本性差异。目标项目 50% 的覆盖率是极小样本下的统计假象，而参考项目 0.25% 的覆盖率表明两项目整体高度异构。唯一的共同指纹无法排除是通用语法结构（如 `while(1)`）的偶然匹配。综合判定，无证据支持抄袭指控，目标项目应认定为独立开发。

------------------------------

