# 🛡️ OS 内核赛自动化代码级架构审计报告

**审计时间**: `2026-07-02 02:21:50`
**参赛作品名称**: `2026-T2026100069910661-XX`
**年份**: `2026`
**学校**: `XX`
**队伍名称**: `T2026100069910661`
**比赛名字**: `OS-Competition`
**比赛子赛道**: `Kernel-Implementation`
**仓库地址**: `https://gitlab.eduxiji.net/educg-group-43501-3132633/T2026100069910661-1865`
**推测开发语言**: `Rust / C (由AST探针自动识别)`
**特征函数量**: `2`

## ⚔️ 底层代码同源性对比 (AST Homology)
> 本部分由专门的代码审查智能体，对该项目与符合年份条件的历届最高相似度项目进行控制流与底层调用对比。新版报告会先给出确定性的全模块覆盖矩阵，再给出 Agent 的分模块解释，避免只围绕少量抽样函数展开。

### 🏆 Top 4 底层对标: vs `2022-ThreeEasyPieces-南京大学`（南京大学 / Three Easy Pieces）

#### 对方作品信息

**对方年份**: `2022`
**对方学校**: `南京大学`
**对方队伍名称**: `Three Easy Pieces`
**对方比赛名字**: `OS-Competition`
**对方比赛子赛道**: `Kernel-Implementation`
**对方仓库地址**: `https://gitlab.eduxiji.net/piper/oskernel2022-ovo.git`

> 双向覆盖率：共同 AST 指纹: 1；唯一 AST 指纹: 目标项目 2，参考项目 562；覆盖率: 目标项目 50.0%，参考项目 0.18%

## 全模块对比总览（确定性证据）

> 总体覆盖率：共同 AST 指纹: 1；唯一 AST 指纹: 目标项目 2，参考项目 562；覆盖率: 目标项目 50.0%，参考项目 0.18%

### 1. 模块覆盖矩阵
| 模块 | 目标函数数 | 参考函数数 | 共同唯一 AST 指纹 | Target 覆盖 | Base 覆盖 | 状态 |
| :--- | ---: | ---: | ---: | ---: | ---: | :--- |
| 进程管理 | 0 | 34 | 0 | 0.0% | 0.0% | 仅参考项目体现 |
| 任务调度/异步运行时 | 0 | 143 | 0 | 0.0% | 0.0% | 仅参考项目体现 |
| 内存管理 | 1 | 120 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |
| 中断/异常处理 | 0 | 31 | 0 | 0.0% | 0.0% | 仅参考项目体现 |
| 系统调用 | 0 | 14 | 0 | 0.0% | 0.0% | 仅参考项目体现 |
| 文件系统 | 0 | 97 | 0 | 0.0% | 0.0% | 仅参考项目体现 |
| 设备驱动 | 0 | 55 | 0 | 0.0% | 0.0% | 仅参考项目体现 |
| 网络协议栈 | 0 | 0 | 0 | 0.0% | 0.0% | 双方均未体现 |
| 通用模块/其他 | 1 | 170 | 1 | 100.0% | 0.69% | 双方存在且有结构重合 |

### 2. 分模块证据清单

#### 进程管理
仅参考项目中观察到该模块代表函数，目标项目没有同模块可对齐证据：
- 参考项目：`kernel/src/syscall.c#sys_wait4`，调用摘要：argraw, copy_to_user, kmt->gettask, yield
- 参考项目：`kernel/src/vfs.c#delete_proc`，调用摘要：get_proc_idx, kmt->sem_signal, kmt->sem_wait, memcpy, release_proc
- 参考项目：`kernel/src/vfs.c#proc_lseek`，调用摘要：Assert, kmt->sem_signal, kmt->sem_wait
- 参考项目：`kernel/src/vfs.c#modify_proc_info`，调用摘要：get_proc_idx, kmt->sem_signal, kmt->sem_wait, memcpy, search_proc_dirent_idx

#### 任务调度/异步运行时
仅参考项目中观察到该模块代表函数，目标项目没有同模块可对齐证据：
- 参考项目：`board/k210/src/sysctl.c#sysctl_clock_get_freq`，调用摘要：sysctl_clock_get_clock_select, sysctl_clock_get_freq, sysctl_clock_get_threshold, sysctl_clock_source_get_freq
- 参考项目：`board/k210/src/sysctl.c#sysctl_pll_source_set_freq`，调用摘要：ceil, fabs, floor, rint, sysctl_clock_source_get_freq, sysctl_pll_get_freq
- 参考项目：`board/k210/src/sysctl.c#sysctl_clock_device_en`，调用摘要：内部闭环逻辑
- 参考项目：`board/k210/src/sysctl.c#sysctl_clock_set_threshold`，调用摘要：内部闭环逻辑

#### 内存管理
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `kernel/init.c#mips_init` | `kernel/src/uproc.c#uproc_execve` | 同模块上下文对齐/差异观察 | halt, putchar | ADD_AUX, Assert, KSTACK_END, ROUNDDOWN, ROUNDUP, SET_TASK ...(+20) |

#### 中断/异常处理
仅参考项目中观察到该模块代表函数，目标项目没有同模块可对齐证据：
- 参考项目：`board/k210/src/timer.c#get_timer_irqn_by_device_and_channel`，调用摘要：内部闭环逻辑
- 参考项目：`board/k210/src/rtc.c#rtc_alarm_irq_register`，调用摘要：plic_irq_disable, plic_irq_enable, plic_irq_register, plic_set_priority, rtc_alarm_set_interrupt, rtc_alarm_set_mask ...(+3)
- 参考项目：`kernel/src/trm.c#timerinit`，调用摘要：CLINT_MTIMECMP, r_csr, w_csr
- 参考项目：`board/k210/src/timer.c#timer_irq_unregister`，调用摘要：get_timer_irqn_by_device_and_channel, plic_irq_unregister

#### 系统调用
仅参考项目中观察到该模块代表函数，目标项目没有同模块可对齐证据：
- 参考项目：`initcode/initcode.c#initcode_test`，调用摘要：initcode_syscall, oscmp_test_busybox, oscmp_test_lmbench, oscmp_test_lua
- 参考项目：`initcode/initcode.c#oscmp_test_busybox`，调用摘要：initcode_syscall, strcmp, strlen
- 参考项目：`kernel/src/lock.c#spin_lock`，调用摘要：Assert, _sys_time, atomic_xchg, cpu_current, holding, pushcli
- 参考项目：`initcode/initcode.c#initcode_syscall`，调用摘要：内部闭环逻辑

#### 文件系统
仅参考项目中观察到该模块代表函数，目标项目没有同模块可对齐证据：
- 参考项目：`kernel/src/fat.c#insert_dirent_to_dirent`，调用摘要：Assert, addr_for_dirent_offset, c2wc, checksum, get_sname, memmove ...(+3)
- 参考项目：`tools/mkfs.c#main`，调用摘要：Assert, IN_DISK, UP_BLK_NUM, assert, atoi, close ...(+8)
- 参考项目：`kernel/src/fat.c#fat_init`，调用摘要：bfs_init, dev->lookup, dmac_init, fpioa_pin_init, init_stdfd, kmt->spin_init ...(+6)
- 参考项目：`tools/mkfs.c#readFileList`，调用摘要：BLK2ADDR, INODE_ADDR, IN_DISK, find_dirent_by_idx, get_blk_idx, printf ...(+2)

#### 设备驱动
仅参考项目中观察到该模块代表函数，目标项目没有同模块可对齐证据：
- 参考项目：`board/k210/src/spi.c#spi_receive_data_normal_dma`，调用摘要：(uint32), dmac_set_single_mode, dmac_wait_done, spi_get_frame_format, spi_set_tmod, sysctl_dma_select
- 参考项目：`board/k210/src/gpiohs.c#gpiohs_set_pin_edge`，调用摘要：set_gpio_bit
- 参考项目：`board/k210/src/sdcard.c#check_block_size`，调用摘要：printf, sd_end_cmd, sd_get_response_R1, sd_get_response_R3_rest, sd_send_cmd
- 参考项目：`kernel/src/gpu.c#__am_gpu_fbdraw`，调用摘要：io_read, outl

#### 通用模块/其他
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `kernel/init.c#halt` | `kernel/src/trm.c#halt` | 同板块同名同结构 | 内部闭环逻辑 | 内部闭环逻辑 |


好的，审计官。以下是基于现有证据与审计指令的最终定性仲裁报告。

---

### 1. 宏观判定
- **定性结论**: **独立开发 / 底层架构显著差异**
- **核心依据**: 客观 AST 相似度仅为 **0.18%**。双方项目在 562 个参考项目唯一 AST 指纹中仅碰撞 1 个，该碰撞点为操作系统内核中最基础、最通用的停机原语 `halt`。目标项目体量极小（2个指纹），其 50% 的覆盖率是“小分母陷阱”，不具备统计学意义。双方在核心系统模块（进程、内存、文件、驱动）上无任何共同 AST 指纹，架构与功能规模存在根本性差异。

### 2. 全模块覆盖矩阵解读
- **进程管理**: 仅参考项目存在（34个函数）。共同 AST 指纹为 0。**证据强度: 未形成**。
- **任务调度/异步运行时**: 仅参考项目存在（143个函数）。共同 AST 指纹为 0。**证据强度: 未形成**。
- **内存管理**: 双方均存在，但共同 AST 指纹为 0。目标项目 `mips_init` 与参考项目 `uproc_execve` 的外部调用图谱完全断裂，无复用痕迹。**证据强度: 未形成**。
- **中断/异常处理**: 仅参考项目存在（31个函数）。共同 AST 指纹为 0。**证据强度: 未形成**。
- **系统调用**: 仅参考项目存在（14个函数）。共同 AST 指纹为 0。**证据强度: 未形成**。
- **文件系统**: 仅参考项目存在（97个函数）。共同 AST 指纹为 0。**证据强度: 未形成**。
- **设备驱动**: 仅参考项目存在（55个函数）。共同 AST 指纹为 0。**证据强度: 未形成**。
- **网络协议栈**: 双方均不存在。**证据强度: 未形成**。
- **通用模块/其他**: 双方均存在，共同 AST 指纹为 1（`halt` 函数）。该函数为无外部依赖的死循环，是领域内高度收敛的通用惯用表达，不构成强同源证据。**证据强度: 弱**。

### 3. 底层 AST 关键证据指纹表
| 所属板块 | 目标项目路径 | 参考项目路径 | 证据类型 | 技术说明 |
| :--- | :--- | :--- | :--- | :--- |
| 通用模块/其他 | `kernel/init.c#halt` | `kernel/src/trm.c#halt` | 弱信号-通用惯用表达 | 两者均为无外部调用的内部闭环逻辑（死循环），是操作系统停机原语的标准实现模式，AST 结构天然高度收敛，不构成特异性复用指纹。 |
| 内存管理 | `kernel/init.c#mips_init` | `kernel/src/uproc.c#uproc_execve` | 差异观察 | 目标项目调用 `halt`, `putchar`；参考项目调用 `ADD_AUX`, `ROUNDUP`, `SET_TASK` 等 26+ 个进程/内存布局宏。调用图谱在深度、广度和语义上完全断裂。 |

### 4. 核心代码调用流对比剖析
* **【内存管理/系统初始化路径】**
* **底层技术析理**: 目标项目的 `mips_init` 是一个极简的 MIPS 架构初始化入口，其核心逻辑仅为输出字符后停机，未涉及任何复杂的内存抽象或同步原语。参考项目的 `uproc_execve` 是一个完整的进程执行加载器，深度依赖自定义的进程控制块（SET_TASK）、栈布局宏（KSTACK_END, STACK_START）和内存对齐操作（ROUNDDOWN, ROUNDUP）。两者在**抽象层次**（裸机初始化 vs. 进程管理）、**内存模型**（无模型 vs. 多段栈布局）和**对象管理模型**上存在根本性异构，排除了同源性。
* **代码溯源链接**:
  - 目标项目: `kernel/init.c#mips_init`
  - 参考项目: `kernel/src/uproc.c#uproc_execve`

### 5. 单方模块与降权项
- **单方模块**: 进程管理、任务调度/异步运行时、中断/异常处理、系统调用、文件系统、设备驱动等模块仅在参考项目中存在，目标项目完全缺失。这直接证明了双方项目在功能规模和系统复杂度上存在巨大鸿沟，是独立开发的有力佐证。
- **降权项**: 唯一的共同指纹 `halt` 函数被降权处理。理由如下：
    1.  **通用惯用表达**: 它是操作系统内核中功能最单一、实现最标准的停机原语，其“内部闭环逻辑”是功能需求的必然结果，而非创造性表达。
    2.  **跨板块同名不可升级**: 尽管目标项目的 `mips_init` 调用了 `halt`，但这属于跨板块调用链，不能作为内存管理板块的同源证据。
    3.  **统计噪声**: 在参考项目 562 个唯一指纹的庞大基数下，仅碰撞 1 个通用原语，属于可预期的随机噪声，不具备指控价值。

### 6. 保守结论
本次审计的客观 AST 相似度仅为 0.18%，唯一的共同指纹是操作系统内核中功能极简、实现高度标准化的 `halt` 停机函数。目标项目是一个仅包含启动和停机逻辑的 MIPS 裸机程序，而参考项目是一个覆盖进程、文件、驱动等子系统的完整 RISC-V 内核。双方在架构、规模、核心模块实现上均无任何实质性结构重合。现有证据充分支持双方为独立开发，不存在代码抄袭或违规复用行为。

------------------------------

