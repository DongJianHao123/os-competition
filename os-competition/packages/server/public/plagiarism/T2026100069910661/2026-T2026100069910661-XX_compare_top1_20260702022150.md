# 🛡️ OS 内核赛自动化代码级架构审计报告

**审计时间**: `2026-07-02 02:21:50`
**参赛作品名称**: `2026-T2026100069910661-XX`
**年份**: `2026`
**学校**: `XX`
**队伍名称**: `T2026100069910661`
**比赛名字**: `OS-Competition`
**比赛子赛道**: `Kernel-Implementation`
**仓库地址**: `https://gitlab.eduxiji.net/educg-group-43501-3132633/T2026100069910661-1865`
**推测开发语言**: `Rust / C (由AST探针自动识别)`
**特征函数量**: `2`

## ⚔️ 底层代码同源性对比 (AST Homology)
> 本部分由专门的代码审查智能体，对该项目与符合年份条件的历届最高相似度项目进行控制流与底层调用对比。新版报告会先给出确定性的全模块覆盖矩阵，再给出 Agent 的分模块解释，避免只围绕少量抽样函数展开。

### 🏆 Top 1 底层对标: vs `2025-OSakura-武汉大学`（武汉大学 / OSakura）

#### 对方作品信息

**对方年份**: `2025`
**对方学校**: `武汉大学`
**对方队伍名称**: `OSakura`
**对方比赛名字**: `OS-Competition`
**对方比赛子赛道**: `Kernel-Implementation`
**对方仓库地址**: `https://gitlab.eduxiji.net/educg-group-36002-2710490/T202510486995252-114`

> 双向覆盖率：共同 AST 指纹: 1；唯一 AST 指纹: 目标项目 2，参考项目 367；覆盖率: 目标项目 50.0%，参考项目 0.27%

## 全模块对比总览（确定性证据）

> 总体覆盖率：共同 AST 指纹: 1；唯一 AST 指纹: 目标项目 2，参考项目 367；覆盖率: 目标项目 50.0%，参考项目 0.27%

### 1. 模块覆盖矩阵
| 模块 | 目标函数数 | 参考函数数 | 共同唯一 AST 指纹 | Target 覆盖 | Base 覆盖 | 状态 |
| :--- | ---: | ---: | ---: | ---: | ---: | :--- |
| 进程管理 | 0 | 53 | 0 | 0.0% | 0.0% | 仅参考项目体现 |
| 任务调度/异步运行时 | 0 | 29 | 0 | 0.0% | 0.0% | 仅参考项目体现 |
| 内存管理 | 1 | 94 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |
| 中断/异常处理 | 0 | 10 | 0 | 0.0% | 0.0% | 仅参考项目体现 |
| 系统调用 | 0 | 37 | 0 | 0.0% | 0.0% | 仅参考项目体现 |
| 文件系统 | 0 | 80 | 0 | 0.0% | 0.0% | 仅参考项目体现 |
| 设备驱动 | 0 | 7 | 0 | 0.0% | 0.0% | 仅参考项目体现 |
| 网络协议栈 | 0 | 0 | 0 | 0.0% | 0.0% | 双方均未体现 |
| 文件系统/第三方库 | 0 | 82 | 0 | 0.0% | 0.0% | 仅参考项目体现 |
| 通用模块/其他 | 1 | 41 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |

### 2. 分模块证据清单

#### 进程管理
仅参考项目中观察到该模块代表函数，目标项目没有同模块可对齐证据：
- 参考项目：`OSakura-rv/kernel/proc/proc.c#proc_exit`，调用摘要：assert, ext4_file_close, ext4_inode_put, fat32_file_close, fat32_inode_put, myproc ...(+6)
- 参考项目：`OSakura-rv/kernel/fs/procfs.c#read_proc_meminfo`，调用摘要：memcpy, strcat, strcpy, strlen
- 参考项目：`OSakura-rv/kernel/fs/procfs.c#procfs_stat`，调用摘要：ARRAY_SIZE, memset, strcmp, timer_get_ts
- 参考项目：`OSakura-rv/kernel/proc/proc.c#proc_schedule`，调用摘要：intr_on, mycpu, spinlock_acquire, spinlock_release, swtch

#### 任务调度/异步运行时
仅参考项目中观察到该模块代表函数，目标项目没有同模块可对齐证据：
- 参考项目：`OSakura-rv/kernel/fs/fat32/fat32_dir.c#fat32_dir_addInode`，调用摘要：(uint16), assert, checksum, fat32_cluster_write, fat32_inode_relocate, memset ...(+3)
- 参考项目：`OSakura-rv/kernel/lib/print.c#printf`，调用摘要：(uint64), panic, print_10, print_16, spinlock_acquire, spinlock_release ...(+4)
- 参考项目：`OSakura-rv/kernel/trap/trap_user.c#trap_user`，调用摘要：assert, external_interrupt_handler, intr_on, myproc, printf, proc_exit ...(+10)
- 参考项目：`OSakura-rv/kernel/fs/fat32/fat32_dir.c#lookup_path`，调用摘要：fat32_dir_searchInode, fat32_inode_dup, fat32_inode_lock, fat32_inode_put, fat32_inode_unlock, fat32_inode_unlockput ...(+2)

#### 内存管理
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `kernel/init.c#mips_init` | `OSakura-rv/kernel/proc/exec.c#proc_exec` | 同模块上下文对齐/差异观察 | halt, putchar | ADD_AUXV, ALIGN_DOWN, ALIGN_UP, assert, ext4_dir_path_to_inode, ext4_file_close ...(+20) |

#### 中断/异常处理
仅参考项目中观察到该模块代表函数，目标项目没有同模块可对齐证据：
- 参考项目：`OSakura-rv/kernel/lock/spinlock.c#pop_off`，调用摘要：assert, intr_get, intr_on, mycpu
- 参考项目：`OSakura-rv/kernel/dev/uart.c#uart_intr`，调用摘要：cons_intr, spinlock_acquire, spinlock_release, uart_getc, uart_start
- 参考项目：`OSakura-rv/kernel/dev/plic.c#plic_inithart`，调用摘要：PLIC_SENABLE, PLIC_SPRIORITY, r_tp
- 参考项目：`OSakura-rv/kernel/lock/spinlock.c#push_off`，调用摘要：intr_get, intr_off, mycpu

#### 系统调用
仅参考项目中观察到该模块代表函数，目标项目没有同模块可对齐证据：
- 参考项目：`OSakura-rv/user/initcode.c#main`，调用摘要：get_len, syscall
- 参考项目：`OSakura-rv/user/lib/syscall.c#get_time`，调用摘要：sys_get_time
- 参考项目：`OSakura-rv/user/lib/syscall.c#clone`，调用摘要：__clone
- 参考项目：`OSakura-rv/user/lib/syscall.c#sys_linkat`，调用摘要：syscall

#### 文件系统
仅参考项目中观察到该模块代表函数，目标项目没有同模块可对齐证据：
- 参考项目：`OSakura-rv/kernel/fs/fat32/fat32_dir.c#fat32_dir_getNextInode`，调用摘要：assert, fat32_cluster_read, fat32_inode_relocate, memset, read_entry_info, read_entry_name
- 参考项目：`OSakura-rv/kernel/fs/fat32/fat32_dir.c#fat32_dir_link`，调用摘要：内部闭环逻辑
- 参考项目：`OSakura-rv/kernel/fs/fat32/fat32_cluster.c#fat32_cluster_write`，调用摘要：assert, buf_read, buf_release, buf_write, cluster_firstSector, min ...(+1)
- 参考项目：`OSakura-rv/kernel/fs/fat32/fat32_cluster.c#fat32_cluster_read`，调用摘要：assert, buf_read, buf_release, cluster_firstSector, min, vm_copyout

#### 设备驱动
仅参考项目中观察到该模块代表函数，目标项目没有同模块可对齐证据：
- 参考项目：`OSakura-rv/kernel/dev/uart.c#uart_init`，调用摘要：spinlock_init, write_reg
- 参考项目：`OSakura-rv/kernel/dev/console.c#cons_write`，调用摘要：spinlock_acquire, spinlock_release, uart_putc, vm_copyin
- 参考项目：`OSakura-rv/kernel/dev/virtio_disk.c#free_chain`，调用摘要：free_desc
- 参考项目：`OSakura-rv/kernel/dev/console.c#cons_putc`，调用摘要：uart_putc_sync

#### 文件系统/第三方库
仅参考项目中观察到该模块代表函数，目标项目没有同模块可对齐证据：
- 参考项目：`OSakura-rv/kernel/fs/ext4/ext4_sys.c#ext4_sys_ppoll`，调用摘要：assert, myproc, proc_iskilled, proc_sleep, spinlock_acquire, spinlock_release ...(+3)
- 参考项目：`OSakura-rv/kernel/fs/ext4/ext4_sys.c#ext4_sys_openat`，调用摘要：check_flags, ext4_dir_create, ext4_dir_path_to_pinode, ext4_dir_pinode_to_inode, ext4_file_alloc, ext4_inode_create ...(+8)
- 参考项目：`OSakura-rv/kernel/fs/ext4/ext4_inode.c#ext4_inode_write`，调用摘要：(uint16), EXTENT_LEAF, assert, ext4_block_alloc, ext4_block_write, ext4_inode_writeback ...(+3)
- 参考项目：`OSakura-rv/kernel/fs/ext4/ext4_dir.c#ext4_dir_pinode_to_inode`，调用摘要：assert, ext4_inode_dup, ext4_inode_get, ext4_inode_read, ext4_inode_readback, ext4_inode_search ...(+8)

#### 通用模块/其他
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `kernel/init.c#halt` | `OSakura-rv/kernel/lib/print.c#print_10` | 同模块上下文对齐/差异观察 | 内部闭环逻辑 | cons_puts |


### 1. 宏观判定
- **定性结论**: **独立开发 / 底层架构显著差异**
  基于客观 AST 指纹比对，双方共同 AST 指纹仅 1 个，参考项目覆盖率低至 0.27%，属于统计学噪声级。目标项目为极简 MIPS 启动内核，参考项目为全功能 RISC-V 操作系统，二者在架构规模、子系统覆盖、调用拓扑上存在根本性差异，未发现任何同板块代码复用或实质性相似的证据。

### 2. 全模块覆盖矩阵解读
| 模块 | 双方存在 | 共同 AST 指纹 | 覆盖率解读 | 证据强度 |
| :--- | :---: | :---: | :--- | :--- |
| **进程管理** | 否 (仅参考) | 0 | 目标项目无此模块，无法比对。 | 未形成 |
| **任务调度/异步运行时** | 否 (仅参考) | 0 | 目标项目无此模块，无法比对。 | 未形成 |
| **内存管理** | 是 | 0 | 双方虽有此模块，但目标项目仅 1 个极简函数，参考项目有 94 个函数，且无共同指纹，表明实现完全异构。 | 弱 (已排除) |
| **中断/异常处理** | 否 (仅参考) | 0 | 目标项目无此模块，无法比对。 | 未形成 |
| **系统调用** | 否 (仅参考) | 0 | 目标项目无此模块，无法比对。 | 未形成 |
| **文件系统** | 否 (仅参考) | 0 | 目标项目无此模块，无法比对。 | 未形成 |
| **设备驱动** | 否 (仅参考) | 0 | 目标项目无此模块，无法比对。 | 未形成 |
| **网络协议栈** | 否 (双方均无) | 0 | 双方均未体现。 | 未形成 |
| **文件系统/第三方库** | 否 (仅参考) | 0 | 目标项目无此模块，无法比对。 | 未形成 |
| **通用模块/其他** | 是 | 0 | 双方虽有此模块，但目标项目为停机闭环，参考项目为格式化输出，功能与调用流完全不同。 | 弱 (已排除) |

**解读总结**：目标项目仅包含“内存管理”和“通用模块/其他”两个模块的雏形，而参考项目拥有 9 个功能完备的子系统。在双方共存的模块中，未观察到任何共同 AST 指纹，表明其内部实现毫无关联。目标项目 50% 的覆盖率是因其函数基数极小（2个）造成的统计幻象，不具备参考意义。

### 3. 底层 AST 关键证据指纹表
| 所属板块 | 目标项目路径 | 参考项目路径 | 证据类型 | 技术说明 |
| :--- | :--- | :--- | :--- | :--- |
| 内存管理 | `kernel/init.c#mips_init` | `OSakura-rv/kernel/proc/exec.c#proc_exec` | 同板块差异对比 | 目标为极简启动流 (halt, putchar)，参考为复杂 ELF 加载流 (ext4_dir_path_to_inode 等 20+ 调用)，AST 拓扑无交集。 |
| 通用模块/其他 | `kernel/init.c#halt` | `OSakura-rv/kernel/lib/print.c#print_10` | 同板块差异对比 | 目标为内部闭环控制流，参考为依赖 cons_puts 的数据输出流，AST 节点类型与功能完全相悖。 |

### 4. 核心代码调用流对比剖析
* **【内存管理/系统初始化路径】**
* **底层技术析理**: 目标项目的 `mips_init` 是 MIPS 架构下的最早期启动函数，其调用流仅包含停机 (`halt`) 和字符输出 (`putchar`)，未涉及任何内存分配、页表映射或地址空间管理逻辑。参考项目的 `proc_exec` 是 RISC-V 架构下进程执行加载的核心，其调用流深度依赖 `ext4` 文件系统进行映像读取，并使用 `ALIGN_DOWN/UP` 等宏进行严格的内存对齐操作。两者在**抽象层次**（板级初始化 vs 进程加载器）、**内存模型**（无模型 vs 基于文件系统的虚拟内存装载）上存在根本性异构。
* **代码溯源链接**:
  - 目标项目: `kernel/init.c#mips_init`
  - 参考项目: `OSakura-rv/kernel/proc/exec.c#proc_exec`

* **【通用模块/基础库路径】**
* **底层技术析理**: 目标项目的 `halt` 函数是一个无外部依赖的“内部闭环逻辑”，通常实现为 `while(1);` 的死循环，用于系统不可恢复时的终止，属于**控制流终止原语**。参考项目的 `print_10` 函数是一个依赖 `cons_puts` 的格式化输出函数，用于将十进制数字转换为字符串并发送至控制台，属于**数据流输出原语**。两者在**同步策略**（无交互 vs 依赖控制台驱动同步）、**对象管理模型**（无状态 vs 操作控制台设备对象）上完全不同。
* **代码溯源链接**:
  - 目标项目: `kernel/init.c#halt`
  - 参考项目: `OSakura-rv/kernel/lib/print.c#print_10`

### 5. 单方模块与降权项
- **单方模块**: 进程管理、任务调度、中断/异常处理、系统调用、文件系统、设备驱动、文件系统/第三方库等 7 个模块仅在参考项目中存在。这证明目标项目是一个处于极早期开发阶段的最小化内核，根本不具备参考项目的复杂功能，从源头上排除了大规模代码复用的可能。
- **降权项**: 控方提出的 `putchar` 与 `cons_puts`、`halt` 与 `assert` 的跨板块语义联想，已被降级为弱信号并排除。这些均为操作系统领域的**通用原语或公共接口**，其功能语义的相似性不能作为特定项目间代码同源的证据。

### 6. 保守结论
本案客观 AST 相似度仅 0.27%，共同指纹仅 1 个，属于典型的噪声级匹配。目标项目是一个仅含 2 个函数的 MIPS 启动原型，而参考项目是包含 367 个函数的完整 RISC-V 操作系统，二者在架构规模、技术栈和实现阶段上存在无法逾越的鸿沟。所有同板块比对均揭示出底层调用流与抽象模型的根本性差异，未发现任何代码复用或“洗稿”痕迹。综合判定，目标项目为独立开发，不存在抄袭违规风险。

------------------------------

