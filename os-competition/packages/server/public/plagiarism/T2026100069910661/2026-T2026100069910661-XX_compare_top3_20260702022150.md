# 🛡️ OS 内核赛自动化代码级架构审计报告

**审计时间**: `2026-07-02 02:21:50`
**参赛作品名称**: `2026-T2026100069910661-XX`
**年份**: `2026`
**学校**: `XX`
**队伍名称**: `T2026100069910661`
**比赛名字**: `OS-Competition`
**比赛子赛道**: `Kernel-Implementation`
**仓库地址**: `https://gitlab.eduxiji.net/educg-group-43501-3132633/T2026100069910661-1865`
**推测开发语言**: `Rust / C (由AST探针自动识别)`
**特征函数量**: `2`

## ⚔️ 底层代码同源性对比 (AST Homology)
> 本部分由专门的代码审查智能体，对该项目与符合年份条件的历届最高相似度项目进行控制流与底层调用对比。新版报告会先给出确定性的全模块覆盖矩阵，再给出 Agent 的分模块解释，避免只围绕少量抽样函数展开。

### 🏆 Top 3 底层对标: vs `2022-有道理队-天津理工大学`（天津理工大学 / 有道理队）

#### 对方作品信息

**对方年份**: `2022`
**对方学校**: `天津理工大学`
**对方队伍名称**: `有道理队`
**对方比赛名字**: `OS-Competition`
**对方比赛子赛道**: `Kernel-Implementation`
**对方仓库地址**: `https://gitlab.eduxiji.net/maoxianjia/oskernal2022-youdaoliteam.git`

> 双向覆盖率：共同 AST 指纹: 1；唯一 AST 指纹: 目标项目 2，参考项目 546；覆盖率: 目标项目 50.0%，参考项目 0.18%

## 全模块对比总览（确定性证据）

> 总体覆盖率：共同 AST 指纹: 1；唯一 AST 指纹: 目标项目 2，参考项目 546；覆盖率: 目标项目 50.0%，参考项目 0.18%

### 1. 模块覆盖矩阵
| 模块 | 目标函数数 | 参考函数数 | 共同唯一 AST 指纹 | Target 覆盖 | Base 覆盖 | 状态 |
| :--- | ---: | ---: | ---: | ---: | ---: | :--- |
| 进程管理 | 0 | 34 | 0 | 0.0% | 0.0% | 仅参考项目体现 |
| 任务调度/异步运行时 | 0 | 140 | 0 | 0.0% | 0.0% | 仅参考项目体现 |
| 内存管理 | 1 | 113 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |
| 中断/异常处理 | 0 | 31 | 0 | 0.0% | 0.0% | 仅参考项目体现 |
| 系统调用 | 0 | 14 | 0 | 0.0% | 0.0% | 仅参考项目体现 |
| 文件系统 | 0 | 89 | 0 | 0.0% | 0.0% | 仅参考项目体现 |
| 设备驱动 | 0 | 55 | 0 | 0.0% | 0.0% | 仅参考项目体现 |
| 网络协议栈 | 0 | 0 | 0 | 0.0% | 0.0% | 双方均未体现 |
| 通用模块/其他 | 1 | 170 | 1 | 100.0% | 0.69% | 双方存在且有结构重合 |

### 2. 分模块证据清单

#### 进程管理
仅参考项目中观察到该模块代表函数，目标项目没有同模块可对齐证据：
- 参考项目：`kernel/src/vfs.c#delete_proc`，调用摘要：get_proc_idx, kmt->sem_signal, kmt->sem_wait, memcpy, release_proc
- 参考项目：`kernel/src/syscall.c#sys_wait4`，调用摘要：argraw, kmt->gettask, yield
- 参考项目：`kernel/src/vfs.c#proc_lseek`，调用摘要：Assert, kmt->sem_signal, kmt->sem_wait
- 参考项目：`kernel/src/vfs.c#modify_proc_info`，调用摘要：get_proc_idx, kmt->sem_signal, kmt->sem_wait, memcpy, search_proc_dirent_idx

#### 任务调度/异步运行时
仅参考项目中观察到该模块代表函数，目标项目没有同模块可对齐证据：
- 参考项目：`hardware/k210/src/sysctl.c#sysctl_clock_get_freq`，调用摘要：sysctl_clock_get_clock_select, sysctl_clock_get_freq, sysctl_clock_get_threshold, sysctl_clock_source_get_freq
- 参考项目：`hardware/k210/src/sysctl.c#sysctl_pll_source_set_freq`，调用摘要：ceil, fabs, floor, rint, sysctl_clock_source_get_freq, sysctl_pll_get_freq
- 参考项目：`hardware/k210/src/sysctl.c#sysctl_clock_device_en`，调用摘要：内部闭环逻辑
- 参考项目：`hardware/k210/src/sysctl.c#sysctl_clock_set_threshold`，调用摘要：内部闭环逻辑

#### 内存管理
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `kernel/init.c#mips_init` | `kernel/src/uproc.c#uproc_execve` | 同模块上下文对齐/差异观察 | halt, putchar | ADD_AUX, Assert, ROUNDDOWN, ROUNDUP, SET_TASK, STACK_END ...(+19) |

#### 中断/异常处理
仅参考项目中观察到该模块代表函数，目标项目没有同模块可对齐证据：
- 参考项目：`hardware/k210/src/timer.c#get_timer_irqn_by_device_and_channel`，调用摘要：内部闭环逻辑
- 参考项目：`hardware/k210/src/rtc.c#rtc_alarm_irq_register`，调用摘要：plic_irq_disable, plic_irq_enable, plic_irq_register, plic_set_priority, rtc_alarm_set_interrupt, rtc_alarm_set_mask ...(+3)
- 参考项目：`kernel/src/trm.c#timerinit`，调用摘要：CLINT_MTIMECMP, r_csr, w_csr
- 参考项目：`hardware/k210/src/timer.c#timer_irq_unregister`，调用摘要：get_timer_irqn_by_device_and_channel, plic_irq_unregister

#### 系统调用
仅参考项目中观察到该模块代表函数，目标项目没有同模块可对齐证据：
- 参考项目：`initcode/initcode.c#initcode_test`，调用摘要：initcode_syscall, oscmp_test_busybox, oscmp_test_lmbench
- 参考项目：`initcode/initcode.c#oscmp_test_busybox`，调用摘要：initcode_syscall, strlen
- 参考项目：`kernel/src/lock.c#spin_lock`，调用摘要：Assert, _sys_time, atomic_xchg, cpu_current, holding, pushcli
- 参考项目：`initcode/initcode.c#initcode_syscall`，调用摘要：内部闭环逻辑

#### 文件系统
仅参考项目中观察到该模块代表函数，目标项目没有同模块可对齐证据：
- 参考项目：`kernel/src/fat.c#insert_dirent_to_dirent`，调用摘要：Assert, addr_for_dirent_offset, c2wc, checksum, get_sname, memmove ...(+3)
- 参考项目：`kernel/src/fat.c#fat_init`，调用摘要：bfs_init, dev->lookup, dmac_init, fpioa_pin_init, init_stdfd, kmt->spin_init ...(+6)
- 参考项目：`kernel/src/vfs.c#file_read`，调用摘要：Assert, BLK2ADDR, MIN, get_blk_idx, get_inode_by_no, kmt->sem_signal ...(+3)
- 参考项目：`kernel/src/bfs.c#bfs_unlink`，调用摘要：find_replace, get_bfs_pg, memcpy, search_in_bfs

#### 设备驱动
仅参考项目中观察到该模块代表函数，目标项目没有同模块可对齐证据：
- 参考项目：`hardware/k210/src/spi.c#spi_receive_data_normal_dma`，调用摘要：(uint32), dmac_set_single_mode, dmac_wait_done, spi_get_frame_format, spi_set_tmod, sysctl_dma_select
- 参考项目：`hardware/k210/src/gpiohs.c#gpiohs_set_pin_edge`，调用摘要：set_gpio_bit
- 参考项目：`hardware/k210/src/sdcard.c#check_block_size`，调用摘要：printf, sd_end_cmd, sd_get_response_R1, sd_get_response_R3_rest, sd_send_cmd
- 参考项目：`kernel/src/gpu.c#__am_gpu_fbdraw`，调用摘要：io_read, outl

#### 通用模块/其他
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `kernel/init.c#halt` | `kernel/src/trm.c#halt` | 同板块同名同结构 | 内部闭环逻辑 | 内部闭环逻辑 |


#### 1. 宏观判定
- **定性结论**: 独立开发 / 无复用 (相似度: 0.18%，共同 AST 指纹: 1；唯一 AST 指纹: 目标项目 2，参考项目 546；覆盖率: 目标项目 50.0%，参考项目 0.18%)

#### 2. 全模块覆盖矩阵解读
- **进程管理**: 仅参考项目存在。共同 AST 指纹为 0。目标项目无此功能，未形成证据。
- **任务调度/异步运行时**: 仅参考项目存在。共同 AST 指纹为 0。目标项目无此功能，未形成证据。
- **内存管理**: 双方均存在，但共同 AST 指纹为 0。目标项目仅含极简初始化函数 `mips_init`，参考项目含 113 个函数，功能复杂度天差地别。双方在该模块无任何结构重合，证据强度为**未形成**。
- **中断/异常处理**: 仅参考项目存在。共同 AST 指纹为 0。目标项目无此功能，未形成证据。
- **系统调用**: 仅参考项目存在。共同 AST 指纹为 0。目标项目无此功能，未形成证据。
- **文件系统**: 仅参考项目存在。共同 AST 指纹为 0。目标项目无此功能，未形成证据。
- **设备驱动**: 仅参考项目存在。共同 AST 指纹为 0。目标项目无此功能，未形成证据。
- **网络协议栈**: 双方均未体现。未形成证据。
- **通用模块/其他**: 双方均存在，共同 AST 指纹为 1。目标项目覆盖率 100% 系样本过小（仅 2 个函数）导致的统计失真；参考项目覆盖率 0.69% 表明重合度极低。唯一重合点 `halt` 函数为操作系统领域的公共惯用表达，证据强度为**弱**。

#### 3. 底层 AST 关键证据指纹表
| 所属板块 | 目标项目路径 | 参考项目路径 | 证据类型 | 技术说明 |
| :--- | :--- | :--- | :--- | :--- |
| 通用模块/其他 | `kernel/init.c#halt` | `kernel/src/trm.c#halt` | 公共惯用表达 | 双方均为“内部闭环逻辑”的死循环停机函数。`halt` 是操作系统内核的标准语义，其实现必然是无限循环，属于有限表达方式的必然趋同，不构成独创性复用证据。 |
| 内存管理 | `kernel/init.c#mips_init` | `kernel/src/uproc.c#uproc_execve` | 无共同指纹 | 虽同属内存管理板块，但功能完全不同。目标项目为 MIPS 架构启动初始化，调用 `halt` 和 `putchar`；参考项目为完整的用户程序加载器，调用 `ADD_AUX`, `ROUNDDOWN`, `SET_TASK` 等 17+ 个复杂函数。双方 AST 指纹为 0，无任何结构重合。 |

#### 4. 核心代码调用流对比剖析
* **【内存管理/初始化与加载路径】**
* **底层技术析理**: 目标项目的 `mips_init` 是一个极简的 MIPS 架构启动入口，其调用流为 `mips_init` -> `putchar` (输出) -> `halt` (停机)，属于教学实验内核的标准初始化-测试-终止模式。参考项目的 `uproc_execve` 是一个功能完整的用户进程加载器，其调用流涉及栈布局 (`STACK_START`, `STACK_END`)、内存对齐 (`ROUNDUP`, `ROUNDDOWN`)、任务状态设置 (`SET_TASK`, `TOP_CONTEXT`) 和辅助向量注入 (`ADD_AUX`)。两者在**抽象层次**（裸机启动 vs. 系统调用服务）、**内存模型**（无复杂管理 vs. 完整的虚拟内存与栈管理）和**调用对象**上存在根本性差异，无任何同源结构。
* **代码溯源链接**:
  - 目标项目: `kernel/init.c#mips_init`
  - 参考项目: `kernel/src/uproc.c#uproc_execve`

* **【通用模块/其他/停机处理路径】**
* **底层技术析理**: 双方的 `halt` 函数均实现为“内部闭环逻辑”（即无限循环）。这是操作系统在完成所有任务或遇到致命错误后使 CPU 停机的唯一标准做法。该逻辑在 AST 层面必然表现为一个无外部调用的死循环节点，属于应被“抽象-过滤-比较”法排除的公共领域惯用表达，不能作为侵权证据。
* **代码溯源链接**:
  - 目标项目: `kernel/init.c#halt`
  - 参考项目: `kernel/src/trm.c#halt`

#### 5. 单方模块与降权项
- **单方模块**: 进程管理、任务调度/异步运行时、中断/异常处理、系统调用、文件系统、设备驱动等模块仅存在于参考项目，表明参考项目是一个功能完备的操作系统内核，而目标项目是一个仅包含启动和停机逻辑的极简骨架。双方在功能完整度上存在巨大鸿沟，不存在系统性复用的基础。
- **降权项**: `halt` 函数作为唯一的共同指纹，因其属于操作系统领域的标准停机惯用法，被降权为弱信号。其同名、同结构是功能语义的必然要求，而非抄袭的证据。

#### 6. 保守结论
本次审计的客观 AST 相似度仅为 0.18%，共同指纹仅有一个公共惯用表达 `halt` 函数。目标项目是一个仅含 2 个函数的极简 MIPS 启动骨架，而参考项目是一个包含 546 个唯一指纹、覆盖进程、文件、驱动等 8 大模块的完整 RISC-V 内核。双方在架构（MIPS vs. RISC-V）、功能规模、核心模块实现上均无实质重合。唯一的 `halt` 重合属于任何操作系统内核都必然包含的停机死循环，不构成独创性复用。综合判定，目标项目为独立开发，不存在对参考项目的抄袭或违规复用行为，整体风险极低。

------------------------------

