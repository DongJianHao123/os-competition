# 🛡️ OS 内核赛自动化代码级架构审计报告

**审计时间**: `2026-07-02 02:21:50`
**参赛作品名称**: `2026-T2026100069910661-XX`
**年份**: `2026`
**学校**: `XX`
**队伍名称**: `T2026100069910661`
**比赛名字**: `OS-Competition`
**比赛子赛道**: `Kernel-Implementation`
**仓库地址**: `https://gitlab.eduxiji.net/educg-group-43501-3132633/T2026100069910661-1865`
**推测开发语言**: `Rust / C (由AST探针自动识别)`
**特征函数量**: `2`

## ⚔️ 底层代码同源性对比 (AST Homology)
> 本部分由专门的代码审查智能体，对该项目与符合年份条件的历届最高相似度项目进行控制流与底层调用对比。新版报告会先给出确定性的全模块覆盖矩阵，再给出 Agent 的分模块解释，避免只围绕少量抽样函数展开。

### 🏆 Top 5 底层对标: vs `2023-种田队-北京航空航天大学`（北京航空航天大学 / 种田队）

#### 对方作品信息

**对方年份**: `2023`
**对方学校**: `北京航空航天大学`
**对方队伍名称**: `种田队`
**对方比赛名字**: `OS-Competition`
**对方比赛子赛道**: `Kernel-Implementation`
**对方仓库地址**: `https://gitlab.eduxiji.net/202310006101080/zhongtianos`

> 双向覆盖率：共同 AST 指纹: 1；唯一 AST 指纹: 目标项目 2，参考项目 572；覆盖率: 目标项目 50.0%，参考项目 0.17%

## 全模块对比总览（确定性证据）

> 总体覆盖率：共同 AST 指纹: 1；唯一 AST 指纹: 目标项目 2，参考项目 572；覆盖率: 目标项目 50.0%，参考项目 0.17%

### 1. 模块覆盖矩阵
| 模块 | 目标函数数 | 参考函数数 | 共同唯一 AST 指纹 | Target 覆盖 | Base 覆盖 | 状态 |
| :--- | ---: | ---: | ---: | ---: | ---: | :--- |
| 进程管理 | 0 | 76 | 0 | 0.0% | 0.0% | 仅参考项目体现 |
| 任务调度/异步运行时 | 0 | 57 | 0 | 0.0% | 0.0% | 仅参考项目体现 |
| 内存管理 | 1 | 157 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |
| 中断/异常处理 | 0 | 20 | 0 | 0.0% | 0.0% | 仅参考项目体现 |
| 系统调用 | 0 | 86 | 0 | 0.0% | 0.0% | 仅参考项目体现 |
| 文件系统 | 0 | 173 | 0 | 0.0% | 0.0% | 仅参考项目体现 |
| 设备驱动 | 0 | 28 | 0 | 0.0% | 0.0% | 仅参考项目体现 |
| 网络协议栈 | 0 | 0 | 0 | 0.0% | 0.0% | 双方均未体现 |
| 通用模块/其他 | 1 | 114 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |

### 2. 分模块证据清单

#### 进程管理
仅参考项目中观察到该模块代表函数，目标项目没有同模块可对齐证据：
- 参考项目：`kern/proc/thread.c#td_destroy`，调用摘要：TAILQ_FIRST, TAILQ_LAST, TAILQ_REMOVE, copyOut, cpu_this, error ...(+11)
- 参考项目：`kern/proc/wait.c#wait`，调用摘要：LIST_EMPTY, LIST_FOREACH, assert, cpu_this, log, mtx_lock ...(+7)
- 参考项目：`kern/syscall/sys_proc.c#sys_prlimit64`，调用摘要：copyIn, copyOut, cur_proc_fs_struct, warn
- 参考项目：`kern/proc/proc.c#proc_destroy`，调用摘要：LIST_REMOVE, LIST_UNTIL_EMPTY, PID_TO_INDEX, assert, log, mtx_hold ...(+7)

#### 任务调度/异步运行时
仅参考项目中观察到该模块代表函数，目标项目没有同模块可对齐证据：
- 参考项目：`lib/vprint.c#vprintfmt`，调用摘要：out, print_char, print_num, print_str, va_arg
- 参考项目：`kern/fs/socket.c#connect`，调用摘要：SOCK_IS_UDP, copyIn, cpu_this, cur_proc_fs_struct, find_listening_socket, find_udp_connect_socket ...(+6)
- 参考项目：`kern/fs/socket.c#recvfrom`，调用摘要：MIN, TAILQ_FIRST, TAILQ_FOREACH, TAILQ_REMOVE, copyOut, cpu_this ...(+7)
- 参考项目：`kern/fs/fd/pipe.c#fd_pipe_write`，调用摘要：MIN, copyIn, cpu_this, mtx_lock, mtx_lock_sleep, mtx_unlock ...(+5)

#### 内存管理
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `kernel/init.c#mips_init` | `kern/driver/virtio.c#virtio_disk_init` | 同模块上下文对齐/差异观察 | halt, putchar | R, memset, mtx_init, mtx_lock, mtx_unlock, panic |

#### 中断/异常处理
仅参考项目中观察到该模块代表函数，目标项目没有同模块可对齐证据：
- 参考项目：`kern/lib/printf.c#printReg`，调用摘要：mtx_lock, mtx_unlock, printfNoLock
- 参考项目：`kern/trap/ktrap.c#kerneltrap`，调用摘要：cpuid, ktrap_timer, log, panic, printf, r_scause ...(+3)
- 参考项目：`kern/driver/virtio.c#virtio_disk_intr`，调用摘要：R, __sync_synchronize, assert, log, mtx_lock, mtx_unlock ...(+2)
- 参考项目：`kern/lock/lock.c#lo_critical_leave`，调用摘要：assert, cpu_this, error, intr_get, intr_restore

#### 系统调用
仅参考项目中观察到该模块代表函数，目标项目没有同模块可对齐证据：
- 参考项目：`kern/syscall/sys_info.c#sys_clock_gettime`，调用摘要：copy_out, cpu_this, log, time_mono_ts, time_rtc_ts, warn
- 参考项目：`kern/syscall/sys_mmap.c#getIntersection`，调用摘要：MIN, swap
- 参考项目：`kern/syscall/sys_signal.c#sys_tkill`，调用摘要：TID_TO_INDEX, cpu_this, sig_send_td, warn
- 参考项目：`kern/syscall/sys_signal.c#sys_setitimer`，调用摘要：copyIn, copyOut, cpu_this, itimer_get, itimer_update

#### 文件系统
仅参考项目中观察到该模块代表函数，目标项目没有同模块可对齐证据：
- 参考项目：`kern/fs/socket.c#fd_socket_write`，调用摘要：MIN, PROFILING_END_WITH_NAME, SOCK_IS_UDP, copyIn, cpu_this, mtx_lock ...(+7)
- 参考项目：`kern/syscall/sys_fs.c#sys_pselect6`，调用摘要：FD_CLR, FD_SET, FD_SET_FOREACH, TS_USEC, check_pselect_r, check_pselect_w ...(+9)
- 参考项目：`kern/fs/socket.c#fd_socket_read`，调用摘要：MIN, PROFILING_END_WITH_NAME, SOCK_IS_UDP, copyOut, cpu_this, mtx_lock ...(+4)
- 参考项目：`kern/fs/fat32/dirent.c#walk_path`，调用摘要：IS_DIRECTORY, dget, dget_path, dir_lookup, dput, dput_path ...(+6)

#### 设备驱动
仅参考项目中观察到该模块代表函数，目标项目没有同模块可对齐证据：
- 参考项目：`kern/driver/virtio.c#virtioTest`，调用摘要：assert, log, strncmp, virtio_disk_rw
- 参考项目：`kern/driver/sd.c#sdInit`，调用摘要：REG32, error, log, sd_acmd41, sd_cmd0, sd_cmd16 ...(+3)
- 参考项目：`kern/driver/sd.c#sd_cmd`，调用摘要：REG32, sd_dummy, spi_xfer, warn
- 参考项目：`kern/driver/sd.c#sdTest`，调用摘要：error, printf, sdRead, sdWrite

#### 通用模块/其他
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `kernel/init.c#halt` | `kern/dev/dtb.c#parseFdtNode` | 同模块上下文对齐/差异观察 | 内部闭环逻辑 | (uint64), FOURROUNDUP, assert, log, parseFdtNode, readBigEndian32 ...(+4) |


好的，我将遵循审计指令，基于客观证据进行最终同源性定性仲裁。

### 1. 宏观判定
- **定性结论**: **独立开发**
- **判定依据**: 客观 AST 相似度仅为 0.17%，共同 AST 指纹仅有 1 个。参考项目是一个包含 572 个唯一 AST 指纹的完整操作系统内核，而目标项目仅包含 2 个唯一 AST 指纹，是一个极简的初始化片段。参考项目覆盖率低至 0.17%，表明目标项目的代码结构在参考项目中几乎不存在。双方在模块构成、代码规模和复杂度上存在根本性差异，无任何证据支持代码复用或“洗稿式”抄袭的指控。

### 2. 全模块覆盖矩阵解读
- **进程管理**
  1. 双方是否都存在：否，仅参考项目体现。
  2. 是否存在共同 AST 指纹：否。
  3. 覆盖率说明：目标项目无此模块，覆盖率为 0.0%，表明其不具备进程管理功能。
  4. 证据强度：未形成。

- **任务调度/异步运行时**
  1. 双方是否都存在：否，仅参考项目体现。
  2. 是否存在共同 AST 指纹：否。
  3. 覆盖率说明：目标项目无此模块，覆盖率为 0.0%，表明其不具备调度或异步运行时。
  4. 证据强度：未形成。

- **内存管理**
  1. 双方是否都存在：是，双方均有函数被归类于此。
  2. 是否存在共同 AST 指纹：否（共同唯一 AST 指纹为 0）。
  3. 覆盖率说明：目标项目覆盖率 0.0%，参考项目覆盖率 0.0%。尽管双方都存在此模块，但未发现任何共同的结构指纹，表明实现完全异构。
  4. 证据强度：弱。控方强行将参考项目的设备驱动初始化函数归入此板块与目标项目的架构启动入口进行比对，属于跨领域对齐，不构成有效证据。

- **中断/异常处理**
  1. 双方是否都存在：否，仅参考项目体现。
  2. 是否存在共同 AST 指纹：否。
  3. 覆盖率说明：目标项目无此模块，覆盖率为 0.0%，表明其不具备中断处理能力。
  4. 证据强度：未形成。

- **系统调用**
  1. 双方是否都存在：否，仅参考项目体现。
  2. 是否存在共同 AST 指纹：否。
  3. 覆盖率说明：目标项目无此模块，覆盖率为 0.0%，表明其未实现系统调用接口。
  4. 证据强度：未形成。

- **文件系统**
  1. 双方是否都存在：否，仅参考项目体现。
  2. 是否存在共同 AST 指纹：否。
  3. 覆盖率说明：目标项目无此模块，覆盖率为 0.0%，表明其不具备文件系统功能。
  4. 证据强度：未形成。

- **设备驱动**
  1. 双方是否都存在：否，仅参考项目体现。
  2. 是否存在共同 AST 指纹：否。
  3. 覆盖率说明：目标项目无此模块，覆盖率为 0.0%，表明其不包含设备驱动逻辑。
  4. 证据强度：未形成。

- **网络协议栈**
  1. 双方是否都存在：否，双方均未体现。
  2. 是否存在共同 AST 指纹：否。
  3. 覆盖率说明：双方均无此模块。
  4. 证据强度：未形成。

- **第三方库/公共依赖**
  1. 双方是否都存在：否，双方均未体现。
  2. 是否存在共同 AST 指纹：否。
  3. 覆盖率说明：双方均未观察到第三方库特征。
  4. 证据强度：未形成。

### 3. 底层 AST 关键证据指纹表
| 所属板块 | 目标项目路径 | 参考项目路径 | 证据类型 | 技术说明 |
| :--- | :--- | :--- | :--- | :--- |
| 通用模块/其他 | `kernel/init.c#halt` | `kern/dev/dtb.c#parseFdtNode` | 弱信号-跨领域 | 控方指控的“退化复用”不成立。`halt` 是标准的停机死循环，`parseFdtNode` 是复杂的设备树递归解析器，两者在AST结构、调用图谱和功能语义上无任何共同点。共同指纹为0。 |
| 内存管理 | `kernel/init.c#mips_init` | `kern/driver/virtio.c#virtio_disk_init` | 弱信号-跨领域 | 控方指控的“节点替换”不成立。`mips_init` 是架构启动入口，`virtio_disk_init` 是设备驱动初始化，调用图谱完全不同（`halt/putchar` vs `R/memset/mtx_*`）。共同指纹为0。 |

### 4. 核心代码调用流对比剖析
* **【内存管理/初始化路径】**
* **底层技术析理**: 目标项目的 `mips_init` 与参考项目的 `virtio_disk_init` 代表了完全不同的初始化层次与模型。目标项目是架构级的极简启动，调用流为 `mips_init -> putchar / halt`，仅涉及字符输出与停机，无任何内存分配、并发控制或设备交互。参考项目是设备驱动初始化，调用流为 `virtio_disk_init -> R / memset / mtx_init / mtx_lock / mtx_unlock / panic`，深度依赖互斥锁（`mtx_*`）进行并发保护，并通过寄存器读写（`R`）与内存操作（`memset`）与硬件交互。两者在抽象层次（架构入口 vs. 设备驱动）、内存模型（无 vs. 直接内存操作）和同步策略（无 vs. 互斥锁）上完全异构，不存在同源性。
* **代码溯源链接**:
  - 目标项目: `kernel/init.c#mips_init`
  - 参考项目: `kern/driver/virtio.c#virtio_disk_init`

### 5. 单方模块与降权项
- **单方模块**: 进程管理、任务调度/异步运行时、中断/异常处理、系统调用、文件系统、设备驱动等模块仅在参考项目中存在，目标项目完全缺失。这证明了目标项目是一个功能极度精简的代码片段，而非一个完整的操作系统内核，其与参考项目在架构和规模上不具备可比性，无法形成有效的同源指控。
- **降权项**: 控方提出的“初始化->异常终止”跨板块调用链相似性，属于操作系统启动代码的通用设计模式，不具备独创性，已按规则降权为弱信号，不构成抄袭证据。

### 6. 保守结论
本次审计的客观 AST 相似度极低（0.17%），唯一的共同指纹更可能源于通用控制流结构。目标项目仅包含 2 个函数，是一个极简的初始化片段，而参考项目是一个拥有 572 个唯一指纹的完整内核。双方在模块构成、代码规模和复杂度上存在数量级的差距。控方指控所依赖的证据，均是将功能、领域、复杂度完全不同的函数进行强行归类和对齐，其“节点替换”或“退化复用”的论断缺乏底层 AST 结构指纹的支持。综合判断，目标项目独立开发的可能性极高，不存在对参考项目的代码复用或定向混淆，整体风险评级为“极低”。

------------------------------

