# 🛡️ OS 内核赛自动化代码级架构审计报告

**审计时间**: `2026-07-02 04:45:10`
**参赛作品名称**: `2026-T202610422999926-XX`
**年份**: `2026`
**学校**: `XX`
**队伍名称**: `T202610422999926`
**比赛名字**: `OS-Competition`
**比赛子赛道**: `Kernel-Implementation`
**仓库地址**: `https://gitlab.eduxiji.net/educg-group-43501-3132633/T202610422999926-98`
**推测开发语言**: `Rust / C (由AST探针自动识别)`
**特征函数量**: `4162`

## ⚔️ 底层代码同源性对比 (AST Homology)
> 本部分由专门的代码审查智能体，对该项目与符合年份条件的历届最高相似度项目进行控制流与底层调用对比。新版报告会先给出确定性的全模块覆盖矩阵，再给出 Agent 的分模块解释，避免只围绕少量抽样函数展开。

### 🏆 Top 1 底层对标: vs `2025-sudo_pacman_Syu-同济大学`（同济大学 / sudo_pacman_Syu）

#### 对方作品信息

**对方年份**: `2025`
**对方学校**: `同济大学`
**对方队伍名称**: `sudo_pacman_Syu`
**对方比赛名字**: `OS-Competition`
**对方比赛子赛道**: `Kernel-Implementation`
**对方仓库地址**: `https://gitlab.eduxiji.net/educg-group-36002-2710490/T202510247995532-2817`

> 双向覆盖率：共同 AST 指纹: 36；唯一 AST 指纹: 目标项目 3046，参考项目 1840；覆盖率: 目标项目 1.18%，参考项目 1.96%

## 全模块对比总览（确定性证据）

> 总体覆盖率：共同 AST 指纹: 36；唯一 AST 指纹: 目标项目 3046，参考项目 1840；覆盖率: 目标项目 1.18%，参考项目 1.96%

### 1. 模块覆盖矩阵
| 模块 | 目标函数数 | 参考函数数 | 共同唯一 AST 指纹 | Target 覆盖 | Base 覆盖 | 状态 |
| :--- | ---: | ---: | ---: | ---: | ---: | :--- |
| 进程管理 | 525 | 154 | 1 | 0.24% | 0.75% | 双方存在且有结构重合 |
| 任务调度/异步运行时 | 835 | 164 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |
| 内存管理 | 760 | 261 | 1 | 0.16% | 0.41% | 双方存在且有结构重合 |
| 中断/异常处理 | 132 | 28 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |
| 系统调用 | 530 | 58 | 1 | 0.49% | 2.22% | 双方存在且有结构重合 |
| 文件系统 | 799 | 318 | 3 | 0.46% | 1.03% | 双方存在且有结构重合 |
| 设备驱动 | 237 | 162 | 1 | 0.53% | 0.78% | 双方存在且有结构重合 |
| 网络协议栈 | 0 | 51 | 0 | 0.0% | 0.0% | 仅参考项目体现 |
| 文件系统/第三方库 | 130 | 48 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |
| 网络协议栈/第三方库 | 13 | 0 | 0 | 0.0% | 0.0% | 仅目标项目体现 |
| 第三方库 | 0 | 740 | 0 | 0.0% | 0.0% | 仅参考项目体现 |
| 通用模块/其他 | 201 | 188 | 1 | 0.59% | 0.64% | 双方存在且有结构重合 |

### 2. 分模块证据清单

#### 进程管理
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `os/components/wateros-fs/fs-procfs/procfs-impl/impl-dummy/src/lib.rs#test` | `src/kernel/syscall/procops.rs#keep_alive` | 同板块同结构 | 内部闭环逻辑 | 内部闭环逻辑 |
| `os/components/wateros-task/task-impl/impl-core/src/lib.rs#process_model_self_test` | `src/kernel/task.rs#execute` | 同模块上下文对齐/差异观察 | AddressSpaceRef::new, ProcessRegistry::new, Some, TaskClearTid::new, api_v0::AddressSpaceHandle::from_raw, assert! ...(+12) | Arc::new, AtomicBool::new, Context::from_waker, Some, Task::current, Task::current().clone ...(+14) |
| `os/components/wateros-task/task-scheduler/scheduler-api/api-v0/src/wait_queues.rs#enqueue_task` | `src/kernel/task/process.rs#build` | 同模块上下文对齐/差异观察 | matches!, ready_queue.detach_ready_task, ready_queue.enqueue_ready_task, registry.mark_blocking, registry.mark_exited, registry.mark_ready ...(+14) | Arc::new, BTreeMap::new, Locked::new, MMList::new, ProcessGroupBuilder::new, ProcessGroupBuilder::new()
                .leader ...(+22) |
| `os/components/wateros-task/task-scheduler/scheduler-api/api-v0/src/wait_queues.rs#finish_blocked_task` | `src/kernel/task/process.rs#wait` | 同模块上下文对齐/差异观察 | ready_queue.enqueue_ready_task, registry.finish_wait, registry.mark_ready, registry.state, registry.state(task_id)
                           .is_none, registry.state(task_id)
                       .is_none ...(+4) | Err, Ok, ProcessList::get, ProcessList::get().write, Some, assert! ...(+10) |

#### 任务调度/异步运行时
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `os/src/user_bringup_bus.rs#run` | `src/kernel/task/thread.rs#run` | 同板块同名 | crate::user_bringup_busybox::run_stage_busybox, crate::user_bringup_root_layout::ensure_busybox_path_links, crate::user_bringup_root_layout::prune_ltp_excluded_testcases, fs::mount_default_root_rw, info!, vfs::ensure_proc_mount_point ...(+2) | self.contexted, self.real_run, stackful |
| `os/components/wateros-ipc/ipc-futex/futex-impl/impl-task/src/hub.rs#wake` | `src/kernel/task.rs#wake` | 同板块同名 | Ok, Self::cleanup_empty_queue, self.with_tables, tables.queues.get, tables.queues.get(&key).copied, wq.wake_one ...(+1) | self.wake_by_ref |
| `os/components/wateros-ipc/ipc-signal/src/lib.rs#get_action` | `src/kernel/task/signal.rs#get_action` | 同板块同名 | Err, Ok, self.processes
               .get, self.processes
               .get(&pid)
               .ok_or, self.processes
               .get(&pid)
               .ok_or(SignalError::NoSuchProcess)?
               .action, self.thread ...(+1) | self.inner.lock, self.inner.lock().actions.get |
| `os/components/wateros-ipc/ipc-futex/futex-impl/impl-task/src/hub.rs#set_robust_list` | `src/kernel/task/thread.rs#set_robust_list` | 同板块同名 | Err, Ok, self.with_tables, tables.robust.insert | self.inner.lock |

#### 内存管理
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `os/components/wateros-vfs/vfs-impl/impl-page-cache/src/lib.rs#truncate` | `src/fs/tmpfs.rs#truncate` | 同板块同名 | cache.frames[slot].data[tail..].fill, cache.free
                     .push, cache.index
                                          .get, cache.index
                                     .remove, cache.index
                 .keys, cache.index
                 .keys()
                 .filter ...(+14) | Instant::now, Ok, block_on, self.mtime.lock, self.pages.resize, self.rwsem.write ...(+1) |
| `os/components/wateros-driver/driver-block/block-impl/impl-virtio-pci/src/lib.rs#dma_alloc` | `src/driver/virtio/hal.rs#dma_alloc` | 同板块同名 | NonNull::dangling, NonNull::new, Vec::new, base_ppn.checked_mul, frame_alloc_result, frame_dealloc_result ...(+3) | Page::alloc_at_least, page.as_memblk, page.as_memblk().as_byte_ptr, page.into_raw, page.start, page.start().addr |
| `os/components/wateros-mm/mm-impl/impl-sv39/src/user_heap_mmap.rs#mprotect` | `src/kernel/syscall/mm.rs#mprotect` | 同板块同名 | Err, Ok, VirtAddr, VirtPageNum, VirtPageNum(vpn.0 + 1).start_addr, addr.0
                               .checked_add ...(+15) | Err, VAddr::from, addr.is_page_aligned, len.align_up, prot.contains, thread
        .process
        .mm_list
        .protect |
| `os/components/wateros-syscall/syscall-impl/impl-kernel/src/unix_sock.rs#accept` | `src/kernel/syscall/net.rs#accept` | 同板块同名 | Arc::new, BOUND.lock, Box::new, Err, Mutex::new, NEXT_INODE.fetch_add ...(+14) | accepted_socket.remote_addr, accepted_socket.remote_addr().unwrap, socket.accept, thread
        .files
        .get, thread
        .files
        .get(sockfd)
        .ok_or, thread
        .files
        .get(sockfd)
        .ok_or(EBADF)?
        .get_socket ...(+3) |

#### 中断/异常处理
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `os/components/wateros-platform/platform-arch/arch-impl/impl-loongarch64/src/interrupt.rs#wait_for_interrupt` | `src/driver/serial.rs#wait_for_interrupt` | 同板块同名 | asm! | pin!, self.disable_interrupts, self.enable_interrupts, self.working.lock_irq, wait.as_mut, wait.as_mut().add_to_wait_list |
| `os/components/wateros-platform/platform-arch/arch-impl/impl-loongarch64/src/trap.rs#save_fp_state` | `src/kernel/terminal.rs#commit_char` | 同模块上下文对齐/差异观察 | asm!, regs.as_mut_ptr | inner.termio.echok, inner.termio.echoke, inner.termio.icanon, inner.termio.icrnl, inner.termio.iexten, inner.termio.igncr ...(+16) |
| `os/components/wateros-platform/platform-arch/arch-impl/impl-loongarch64/src/trap.rs#restore_fp_state` | `src/kernel/terminal.rs#read` | 同模块上下文对齐/差异观察 | asm!, regs.as_ptr | Err, Thread::current, Thread::current().signal_list.has_pending_signal, buffer.available, buffer.available().min, buffer.fill ...(+17) |
| `os/components/wateros-platform/platform-arch/arch-impl/impl-riscv64/src/trap.rs#save_fp_state` | `src/kernel/timer.rs#timer_interrupt` | 同模块上下文对齐/差异观察 | asm!, regs.as_mut_ptr | CPU::local, CPU::local().cpuid, SLEEPERS_LIST.lock_irq, TICKS.fetch_add, WAKEUP_TICK.load, WAKEUP_TICK.store ...(+6) |

#### 系统调用
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `os/components/wateros-syscall/syscall-impl/impl-kernel/src/sys/ioctl.rs#ioctl_req` | `src/kernel/syscall.rs#from_arg` | 同板块同结构 | 内部闭环逻辑 | 内部闭环逻辑 |
| `os/components/wateros-driver/driver-impl/impl-qemu-riscv64-opensbi/src/lib.rs#probe_virtio_blk_and_collect_unsupported` | `src/kernel/mem/mm_list.rs#set_break` | 同模块上下文对齐/差异观察 | Arc::new, BlockCacheManager::default_config, BlockCacheManager::wrap, Box::new, DEVICE_INFOS.lock, DEVICE_INFOS.lock().clone ...(+21) | Some, VRange::new, assert!, drop, inner.locked.lock, locked.areas.check_overlapping_range ...(+5) |
| `os/components/wateros-syscall/syscall-impl/impl-kernel/src/sys/sendto.rs#sys_sendto` | `src/kernel/syscall/sysinfo.rs#newuname` | 同模块上下文对齐/差异观察 | UserRet::from_error, UserRet::from_success, args.arg, copy_from_user, copy_from_user_struct::<SockAddrIn>, crate::unix_sock::is_unix_fd ...(+8) | UserPointerMut::new, buffer.write, copy_cstr_to_array |
| `os/components/wateros-syscall/syscall-impl/impl-kernel/src/poll_engine.rs#poll_socket_revents` | `src/kernel/syscall/net.rs#get_sockopt` | 同模块上下文对齐/差异观察 | matches!, socket.handle, socket_fd::lookup, stack::socket_can_recv, stack::socket_can_recv(handle).unwrap_or, stack::socket_has_pending_accept ...(+13) | Ok, UserPointerMut::new, UserPointerMut::new(optlen)?.write, UserPointerMut::new(optval.cast())?.write, optval.cast, size_of::<u32> |

#### 文件系统
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `os/components/wateros-vfs/vfs-impl/impl-fs-bridge/src/file_handle.rs#seek` | `src/kernel/vfs/file/inode_file.rs#seek` | 同板块同名 | Err, Ok, base.checked_add, base.checked_add(offset as u64)
                        .ok_or, base.checked_sub, base.checked_sub((-offset) as u64)
                        .ok_or ...(+5) | Err, Ok, cursor.checked_add_signed, cursor.checked_add_signed(off).ok_or, inode.size.load, inode_file.cursor.lock ...(+3) |
| `os/components/wateros-vfs/vfs-impl/impl-fs-bridge/src/tmpfs.rs#chmod` | `src/fs/tmpfs.rs#chmod` | 同板块同名 | Ok, Self::dir_mut, Self::split_path | Instant::now, Ok, acquire, block_on, mode.non_format_bits, old.perm ...(+4) |
| `os/components/wateros-vfs/vfs-impl/impl-fs-bridge/src/tmpfs.rs#rename` | `src/fs/tmpfs.rs#rename` | 同板块同名 | Err, Self::dir_ref, Self::dir_ref(&self.root, &new_parts).is_ok, Self::insert_leaf, Self::remove_leaf, Self::split_path | (new_parent_inode.as_ref() as &dyn Any)
                .downcast_ref::<DirectoryInode>, (new_parent_inode.as_ref() as &dyn Any)
                .downcast_ref::<DirectoryInode>()
                .expect, Arc::as_ptr, Err, Instant::now, Ok ...(+53) |
| `os/components/wateros-vfs/vfs-impl/impl-fs-bridge/src/file_handle.rs#truncate` | `src/kernel/vfs/file/mod.rs#truncate` | 同板块同名 | Err, Ok, self.data
            .resize, usize::try_from, usize::try_from(len).map_err | inode_file.truncate, panic! |

#### 设备驱动
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `os/components/wateros-driver/driver-network/network-impl/impl-virtio-mmio/src/lib.rs#receive` | `src/net/device.rs#receive` | 同板块同名 | Err, Ok, buf.len, buf[..len].copy_from_slice, logging::warn!, packet.len ...(+6) | RxToken, Some, TxToken, self.can_receive, self.can_send, self.recv ...(+1) |
| `os/components/wateros-driver/driver-api/api-v0/src/lib.rs#test` | `src/driver/e1000e.rs#test` | 同板块同名 | Some, String::from, Vec::from, assert!, assert_eq!, log::trace! | 内部闭环逻辑 |
| `os/components/wateros-driver/driver-network/network-impl/impl-virtio-mmio/src/lib.rs#send` | `src/driver/virtio/virtio_net.rs#send` | 同板块同名 | self.inner.send, self.inner.send(tx_buf).map_err, virtio_drivers::device::net::TxBuffer::from | net::TxBuffer::from, self.send, self.send(net::TxBuffer::from(data))
            .map_err |
| `os/components/wateros-driver/driver-network/network-impl/impl-virtio-pci/src/lib.rs#send` | `src/net/device/loopback.rs#send` | 同板块同名 | self.inner.send, self.inner.send(tx_buf).map_err, virtio_drivers::device::net::TxBuffer::from | Ok, Vec::from, self.queue.push_back |

#### 网络协议栈
仅参考项目中观察到该模块代表函数，目标项目没有同模块可对齐证据：
- 参考项目：`src/net/socket/tcp.rs#poll_impl`，调用摘要：Ok, PollEvent::empty, Some, block_on, events.contains, iface.lock ...(+12)
- 参考项目：`src/net/socket/udp.rs#bind`，调用摘要：BoundSocket::new_bind_all, BoundSocket::new_bind_single, Err, Ok, Self::bind_impl, Some ...(+23)
- 参考项目：`src/net/socket/tcp.rs#accept_impl`，调用摘要：Arc::new, BoundSocket::new_single, Err, Ok, RwLock::new, SocketAddr::from ...(+15)
- 参考项目：`src/net/socket/udp.rs#drop`，调用摘要：UDP_PORT_MAP.lock, UDP_PORT_MAP.lock().remove, block_on, bound_socket_guard.as_ref, bound_socket_guard.as_ref().unwrap, bound_socket_guard.is_none ...(+16)

#### 文件系统/第三方库
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `os/components/wateros-fs/fs-impl/impl-ext4-rs/src/lib.rs#rename` | `src/fs/ext4.rs#rename` | 同板块同名 | Err, Ok, child_ref.inode.is_dir, ensure_dir_inode, fs.dir_add_entry, fs.dir_add_entry(&mut parent_ref, &child_ref, new_name)
              .map_err ...(+14) | Arc::as_ptr, Err, Instant::now, Ok, block_on, core::str::from_utf8 ...(+27) |
| `os/components/wateros-fs/fs-impl/impl-ext4-rs/src/lib.rs#unlink` | `src/fs/ext4.rs#unlink` | 同板块同名 | Err, Ok, child_ref.inode
                             .links_count, child_ref.inode
                        .size, child_ref.inode
                     .set_links_count, ensure_dir_inode ...(+14) | Instant::now, Ok, String::from_utf8_lossy, at.get_inode, at.get_name, block_on ...(+16) |
| `os/components/wateros-fs/fs-impl/impl-ext4/src/rw.rs#mkdir` | `src/fs/ext4.rs#mkdir` | 同板块同名 | Dir::open_inode, Dir::open_inode(fs, parent_inode).map_err, DirEntryName::try_from, DirEntryName::try_from(name).map_err, Duration::from_secs, Err ...(+16) | DirInode::new, InodeMode::from, Instant::now, Mode::DIR.perm, String::from_utf8_lossy, at.get_name ...(+18) |
| `os/components/wateros-fs/fs-impl/impl-ext4-rs/src/lib.rs#truncate` | `src/fs/ext4.rs#truncate` | 同板块同名 | Err, Ok, fs.get_inode_ref, fs.truncate_inode, fs.truncate_inode(&mut inode_ref, len)
              .map_err, fs.write_back_inode ...(+4) | Instant::now, Ok, block_on, ext4fs.inner.write, self.mtime.lock, self.page_cache.resize ...(+8) |

#### 网络协议栈/第三方库
仅目标项目中观察到该模块代表函数，参考项目没有同模块可对齐证据：
- 目标项目：`os/components/wateros-driver/driver-network/network-impl/impl-smoltcp/src/lib.rs#should_loopback`，调用摘要：frame.len, is_local_ipv4, u16::from_be_bytes, usize::from
- 目标项目：`os/components/wateros-driver/driver-network/network-impl/impl-smoltcp/src/lib.rs#receive`，调用摘要：SmoltcpRxToken, Some, frame.len, frame.len().min, inner.as_ref, rx_buf[..n].copy_from_slice ...(+4)
- 目标项目：`os/components/wateros-driver/driver-network/network-impl/impl-smoltcp/src/lib.rs#drain_rx_staging`，调用摘要：dev.receive, dev_handle.lock, self.inner.as_ref, self.loopback_queue.pop_front, self.rx_buf[..n].to_vec, self.rx_staging
                        .push_back ...(+2)
- 目标项目：`os/components/wateros-driver/driver-network/network-impl/impl-smoltcp/src/lib.rs#consume`，调用摘要：dev.lock, dev.lock().send, f, frame.to_vec, log::warn!, self.loopback_queue.len ...(+2)

#### 第三方库
仅参考项目中观察到该模块代表函数，目标项目没有同模块可对齐证据：
- 参考项目：`crates/eonix_hal/src/arch/riscv64/trap/default.rs#_default_trap_entry`，调用摘要：naked_asm!
- 参考项目：`crates/eonix_hal/src/arch/loongarch64/trap/mod.rs#_raw_trap_entry`，调用摘要：naked_asm!
- 参考项目：`crates/eonix_percpu/eonix_percpu_macros/src/lib.rs#define_percpu_impl`，调用摘要：["u8", "u16", "u32", "u64", "usize"].contains, attrs.is_empty, format_ident!, get_percpu_pointer, panic!, parse2::<ItemStatic> ...(+6)
- 参考项目：`crates/eonix_hal/src/arch/riscv64/trap/captured.rs#_captured_trap_entry`，调用摘要：naked_asm!

#### 通用模块/其他
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `os/components/wateros-ipc/ipc-signal/src/lib.rs#remaining` | `src/kernel/user/dataflow.rs#remaining` | 同板块同名 | deadline.saturating_sub, self.deadline_ns
                                           .map, self.deadline_ns
                                           .map(|deadline| deadline.saturating_sub(now_ns))
                                           .unwrap_or | 内部闭环逻辑 |
| `os/components/wateros-klog/src/lib.rs#write_str` | `src/io.rs#write_str` | 同板块同名 | Ok, bytes.len, bytes.len().min, s.as_bytes, self.buf.len, self.buf.len().saturating_sub ...(+1) | Err, Ok, s.as_bytes, self.0.fill |
| `os/components/wateros-ipc/ipc-signal/src/lib.rs#replace` | `src/rcu.rs#replace` | 同板块同名 | (spec.value_ns != 0).then, now_ns.saturating_add, self.generation
                              .wrapping_add | Arc::from_raw, Arc::into_raw, core::ptr::null_mut, debug_assert!, new_node.clone, new_node.rcu_next ...(+21) |
| `os/components/wateros-ipc/ipc-signal/signal-api/api-v0/src/lib.rs#ignore` | `src/kernel/user/dataflow.rs#ignore` | 同板块同名 | SignalSet::empty | Ok, Some, len.min, self.pointer.forward |


### 1. 宏观判定
- **定性结论**: **独立开发，底层架构显著差异**
  客观 AST 相似度仅为 0.74%，共同 AST 指纹仅 36 个，远低于“系统性复用”的阈值。双方在核心抽象、内存模型、同步策略及整体架构上存在根本性差异，属于领域共性（POSIX 语义、Rust 标准库模式）下的独立实现。

### 2. 全模块覆盖矩阵解读
- **进程管理**: 双方均存在。共同 AST 指纹仅 1 个，覆盖率极低（0.24% / 0.75%）。证据强度：**弱**。控方证据多为跨层硬凑或基于 POSIX 通用语义的关联，外部调用图谱无项目特定交集。
- **任务调度/异步运行时**: 双方均存在。共同 AST 指纹为 0。证据强度：**未形成**。双方调度器实现机制完全不同（如目标项目有 Round-Robin 和 Multi-class 两种实现，参考项目基于条件变量）。
- **内存管理**: 双方均存在。共同 AST 指纹仅 1 个，覆盖率极低（0.16% / 0.41%）。证据强度：**弱**。同名函数（如 `dma_alloc`）调用图谱完全不同，分别依赖帧分配器和页分配器。
- **中断/异常处理**: 双方均存在。共同 AST 指纹为 0。证据强度：**未形成**。控方配对荒谬（如将 `save_fp_state` 与 `commit_char` 对比），无任何同源迹象。
- **系统调用**: 双方均存在。共同 AST 指纹仅 1 个，覆盖率低（0.49% / 2.22%）。证据强度：**弱**。用户态交互抽象完全不同（`copy_from_user_struct` vs `UserPointer`），仅 POSIX 标准流程相似。
- **文件系统**: 双方均存在。共同 AST 指纹 3 个，覆盖率低（0.46% / 1.03%）。证据强度：**弱**。同名函数（如 `truncate`, `rename`）操作的对象和底层机制不同（页缓存 vs tmpfs 页表）。
- **设备驱动**: 双方均存在。共同 AST 指纹 1 个，覆盖率低（0.53% / 0.78%）。证据强度：**弱**。同名函数（如 `send`, `receive`）依赖的驱动框架和 VirtIO 库封装不同。
- **网络协议栈**: 仅参考项目存在。证据强度：**未形成**。目标项目无此模块，无法对比。
- **第三方库/公共依赖**: 双方均存在大量第三方库代码，但无共同指纹。证据强度：**未形成**。各自依赖的库（如 `virtio-drivers`, `smoltcp`）和自研基础库完全不同。

### 3. 底层 AST 关键证据指纹表
| 所属板块 | 目标项目路径 | 参考项目路径 | 证据类型 | 技术说明 |
| :--- | :--- | :--- | :--- | :--- |
| 进程管理 | `os/components/wateros-fs/fs-procfs/procfs-impl/impl-dummy/src/lib.rs#test` | `src/kernel/syscall/procops.rs#keep_alive` | 同板块同结构 | 双方均存在内部闭环逻辑的函数，但此类空函数或简单测试函数在大型项目中普遍存在，不构成特异性复用证据。 |
| 系统调用 | `os/components/wateros-syscall/syscall-impl/impl-kernel/src/sys/ioctl.rs#ioctl_req` | `src/kernel/syscall.rs#from_arg` | 同板块同结构 | 双方均存在内部闭环逻辑的函数，属于参数解析的通用占位或简单分发，无独创性逻辑。 |
| 通用模块/其他 | `os/components/wateros-ipc/ipc-signal/src/lib.rs#remaining` | `src/kernel/user/dataflow.rs#remaining` | 同板块同名 | 目标项目为时间计算逻辑，参考项目为内部闭环逻辑，实现完全不同。同名仅为巧合。 |
| 文件系统 | `os/components/wateros-vfs/vfs-impl/impl-fs-bridge/src/file_handle.rs#seek` | `src/kernel/vfs/file/inode_file.rs#seek` | 同板块同名 | 均实现 `seek` 操作，但目标项目基于 `base` 偏移量计算，参考项目基于 `cursor` 和 `inode.size`，内部状态管理模型不同。 |
| 文件系统 | `os/components/wateros-vfs/vfs-impl/impl-fs-bridge/src/tmpfs.rs#chmod` | `src/fs/tmpfs.rs#chmod` | 同板块同名 | 均实现 `chmod`，但目标项目通过路径分割操作，参考项目直接操作 inode 权限位，实现路径不同。 |
| 文件系统 | `os/components/wateros-vfs/vfs-impl/impl-fs-bridge/src/tmpfs.rs#rename` | `src/fs/tmpfs.rs#rename` | 同板块同名 | 均实现 `rename`，但目标项目通过 `split_path` 和 `insert_leaf` 操作树结构，参考项目通过 `downcast_ref` 和 `Arc::as_ptr` 操作 inode，内部数据结构完全不同。 |
| 设备驱动 | `os/components/wateros-driver/driver-api/api-v0/src/lib.rs#test` | `src/driver/e1000e.rs#test` | 同板块同名 | 目标项目为包含 `assert!` 的普通测试，参考项目为内部闭环逻辑，无实质相似性。 |
| 内存管理 | `os/components/wateros-vfs/vfs-impl/impl-page-cache/src/lib.rs#truncate` | `src/fs/tmpfs.rs#truncate` | 同板块同名 | 目标项目操作页缓存（`cache.frames`），参考项目操作 tmpfs 的 `self.pages`，底层数据结构不同。 |
| 第三方库/公共依赖 | `os/components/wateros-fs/fs-impl/impl-ext4-rs/src/lib.rs#rename` | `src/fs/ext4.rs#rename` | 同板块同名 | 双方均封装或使用 ext4 库，但目标项目调用 `ext4-rs` 库的 `fs.dir_add_entry`，参考项目调用自研封装，依赖库和实现路径不同。 |
| 第三方库/公共依赖 | `os/components/wateros-fs/fs-impl/impl-ext4-rs/src/lib.rs#unlink` | `src/fs/ext4.rs#unlink` | 同板块同名 | 同上，均为对 ext4 功能的封装，但底层库和操作细节不同。 |
| 第三方库/公共依赖 | `os/components/wateros-fs/fs-impl/impl-ext4/src/rw.rs#mkdir` | `src/fs/ext4.rs#mkdir` | 同板块同名 | 目标项目使用 `ext4` 库，参考项目使用自研实现，两者在 inode 创建和目录项管理上完全不同。 |
| 第三方库/公共依赖 | `os/components/wateros-fs/fs-impl/impl-ext4-rs/src/lib.rs#truncate` | `src/fs/ext4.rs#truncate` | 同板块同名 | 同上，均为 ext4 文件系统的截断操作，但依赖的底层库和实现细节不同。 |

### 4. 核心代码调用流对比剖析
* **【进程管理/进程退出与回收路径】**
* **底层技术析理**: 目标项目的 `sys_exit_group` 是系统调用入口，通过调用 `drop_task_runtime_resources`, `robust_exit_cleanup`, `notify_parent_sigchld` 等一系列函数式组件完成资源回收，体现了组件化架构。参考项目的 `remove_process` 是进程列表的内部维护函数，通过操作 `ProcessList` 的 `processes`, `sessions`, `pgroups` 等集合，并调用 `rcu_sync` 完成同步移除，体现了单体内核的集中式管理。两者处于不同抽象层次，调用对象和同步机制完全不同。
* **代码溯源链接**:
  - 目标项目: `os/components/wateros-syscall/syscall-impl/impl-kernel/src/sys/task.rs#sys_exit_group`
  - 参考项目: `src/kernel/task/process_list.rs#remove_process`

* **【进程管理/等待与阻塞调度】**
* **底层技术析理**: 目标项目的 `schedule_wait` (以 Round-Robin 为例) 是调度器核心逻辑，通过操作 `ready_queue`、设置 `TaskBlockReason::Wait`、调用 `registry.take_current_switch_out` 实现任务切换，依赖自定义的调度器框架。参考项目的 `wait` 是进程同步原语，基于条件变量 `self.cv.wait` 和 `wait_procs` 列表实现阻塞，依赖其自研的 `Condvar` 同步机制。两者在实现阻塞的底层机制上完全不同。
* **代码溯源链接**:
  - 目标项目: `os/components/wateros-task/task-scheduler/scheduler-impl/impl-round-robin/src/scheduler.rs#schedule_wait`
  - 参考项目: `src/kernel/task/process.rs#wait`

* **【系统调用/信号掩码管理】**
* **底层技术析理**: 目标项目的 `sys_rt_sigprocmask` 使用 `copy_from_user_struct` 和 `copy_to_user_struct` 进行用户态数据交互，并通过 `ipc::signal::with_registry` 闭包操作信号注册表。参考项目的 `rt_sigprocmask` 使用 `UserPointer` / `UserPointerMut` 抽象进行用户态内存读写。两者在用户态访问的安全抽象层设计上存在根本差异。
* **代码溯源链接**:
  - 目标项目: `os/components/wateros-syscall/syscall-impl/impl-kernel/src/sys/task.rs#sys_rt_sigprocmask`
  - 参考项目: `src/kernel/syscall/procops.rs#rt_sigprocmask`

* **【文件系统/文件寻址】**
* **底层技术析理**: 目标项目的 `seek` 方法基于一个独立的 `base` 偏移量进行 `checked_add` 或 `checked_sub` 运算来更新位置。参考项目的 `seek` 方法则直接操作 `inode_file` 内部的 `cursor` 锁，并结合 `inode.size` 进行边界检查。两者管理文件偏移量的内部状态模型不同。
* **代码溯源链接**:
  - 目标项目: `os/components/wateros-vfs/vfs-impl/impl-fs-bridge/src/file_handle.rs#seek`
  - 参考项目: `src/kernel/vfs/file/inode_file.rs#seek`

### 5. 单方模块与降权项
- **单方模块**:
  - **网络协议栈**: 仅参考项目拥有完整的 TCP/UDP 协议栈实现，目标项目无此模块。这体现了双方在功能范围上的巨大差异。
  - **第三方库**: 参考项目包含大量自研基础库（如 `eonix_hal`, `eonix_percpu`），目标项目则依赖不同的第三方库（如 `smoltcp`, `ext4-rs`）。这些库的代码量巨大且完全不同，是架构差异的铁证。
- **降权项**:
  - **同名函数**: 如 `run`, `wake`, `send`, `accept` 等，均为操作系统领域的通用动词，其存在不能作为复用证据。
  - **POSIX 语义**: 如 `waitid`, `kill`, `exit` 等系统调用的标准流程，任何兼容 POSIX 的系统都必须实现，属于领域共性。
  - **Rust 标准库模式**: 如 `Some`, `Ok`, `Err`, `map`, `unwrap_or`, `Vec::new`, `assert!` 等，是 Rust 编程的通用习惯，不具独创性。
  - **跨板块配对**: 控方将不同层次、不同职责的函数（如 `sys_exit_group` 与 `remove_process`）强行对比，此类证据无效。

### 6. 保守结论
综合客观 AST 覆盖率（0.74%）、全模块指纹分析及核心调用流对比，目标项目 `[2026-T202610422999926-XX]` 与参考项目 `[2025-sudo_pacman_Syu-同济大学]` 在底层代码实现上存在根本性差异。双方在进程管理、内存管理、文件系统等核心板块的抽象层设计、同步机制和对象模型均截然不同。控方提出的“系统性复用”指控主要基于 POSIX 通用语义、Rust 标准模式及同名函数的强行关联，缺乏项目特定 AST 指纹的支持。极低的共同指纹数量（36个）属于大型项目中领域共性和巧合的正常范围，不构成抄袭或违规复用。因此，维持“独立开发”的结论。

------------------------------

