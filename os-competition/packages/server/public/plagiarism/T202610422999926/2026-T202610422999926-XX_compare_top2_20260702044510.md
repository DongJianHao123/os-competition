# 🛡️ OS 内核赛自动化代码级架构审计报告

**审计时间**: `2026-07-02 04:45:10`
**参赛作品名称**: `2026-T202610422999926-XX`
**年份**: `2026`
**学校**: `XX`
**队伍名称**: `T202610422999926`
**比赛名字**: `OS-Competition`
**比赛子赛道**: `Kernel-Implementation`
**仓库地址**: `https://gitlab.eduxiji.net/educg-group-43501-3132633/T202610422999926-98`
**推测开发语言**: `Rust / C (由AST探针自动识别)`
**特征函数量**: `4162`

## ⚔️ 底层代码同源性对比 (AST Homology)
> 本部分由专门的代码审查智能体，对该项目与符合年份条件的历届最高相似度项目进行控制流与底层调用对比。新版报告会先给出确定性的全模块覆盖矩阵，再给出 Agent 的分模块解释，避免只围绕少量抽样函数展开。

### 🏆 Top 2 底层对标: vs `2024-萌新-南开大学`（南开大学 / 萌新）

#### 对方作品信息

**对方年份**: `2024`
**对方学校**: `南开大学`
**对方队伍名称**: `萌新`
**对方比赛名字**: `OS-Competition`
**对方比赛子赛道**: `Kernel-Implementation`
**对方仓库地址**: `https://gitlab.eduxiji.net/educg-group-26010-2376550/T202410055992606-2876`

> 双向覆盖率：共同 AST 指纹: 40；唯一 AST 指纹: 目标项目 3046，参考项目 2483；覆盖率: 目标项目 1.31%，参考项目 1.61%

## 全模块对比总览（确定性证据）

> 总体覆盖率：共同 AST 指纹: 40；唯一 AST 指纹: 目标项目 3046，参考项目 2483；覆盖率: 目标项目 1.31%，参考项目 1.61%

### 1. 模块覆盖矩阵
| 模块 | 目标函数数 | 参考函数数 | 共同唯一 AST 指纹 | Target 覆盖 | Base 覆盖 | 状态 |
| :--- | ---: | ---: | ---: | ---: | ---: | :--- |
| 进程管理 | 525 | 39 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |
| 任务调度/异步运行时 | 835 | 272 | 1 | 0.15% | 0.44% | 双方存在且有结构重合 |
| 内存管理 | 760 | 174 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |
| 中断/异常处理 | 132 | 29 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |
| 系统调用 | 530 | 67 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |
| 文件系统 | 799 | 285 | 4 | 0.61% | 1.72% | 双方存在且有结构重合 |
| 设备驱动 | 237 | 42 | 1 | 0.53% | 2.5% | 双方存在且有结构重合 |
| 网络协议栈 | 0 | 0 | 0 | 0.0% | 0.0% | 双方均未体现 |
| 文件系统/第三方库 | 130 | 730 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |
| 网络协议栈/第三方库 | 13 | 118 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |
| 第三方库 | 0 | 1374 | 0 | 0.0% | 0.0% | 仅参考项目体现 |
| 通用模块/其他 | 201 | 106 | 1 | 0.59% | 1.14% | 双方存在且有结构重合 |

### 2. 分模块证据清单

#### 进程管理
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `os/components/wateros-task/task-scheduler/scheduler-impl/impl-multi-class/src/lib.rs#exit_current` | `modules/axtask/src/run_queue.rs#exit_current` | 同板块同名 | InterruptGuard::new, ScheduleReason::Exit, __switch, core::hint::unreachable_unchecked, guard.release_before_switch, panic! ...(+2) | EXITED_TASKS.lock, EXITED_TASKS.lock().clear, assert!, axhal::misc::terminate, axlog::warn!, crate::current ...(+4) |
| `os/components/wateros-task/task-impl/impl-core/src/lib.rs#process_model_self_test` | `api/linux_syscall_api/src/syscall_task/imp/task.rs#syscall_exec` | 同模块上下文对齐/差异观察 | AddressSpaceRef::new, ProcessRegistry::new, Some, TaskClearTid::new, api_v0::AddressSpaceHandle::from_raw, assert! ...(+12) | Err, Ok, Some, Vec::new, args_vec.len, args_vec.push ...(+20) |
| `os/components/wateros-task/task-scheduler/scheduler-api/api-v0/src/wait_queues.rs#enqueue_task` | `modules/axfs/src/mounts.rs#procfs` | 同模块上下文对齐/差异观察 | matches!, ready_queue.detach_ready_task, ready_queue.enqueue_ready_task, registry.mark_blocking, registry.mark_exited, registry.mark_ready ...(+14) | Arc::new, Ok, file_over.write_at, file_somaxconn.write_at, fs::ramfs::RamFileSystem::new, proc_root.clone ...(+3) |
| `os/components/wateros-task/task-scheduler/scheduler-api/api-v0/src/wait_queues.rs#finish_blocked_task` | `api/linux_syscall_api/src/syscall_task/imp/task.rs#syscall_wait4` | 同模块上下文对齐/差异观察 | ready_queue.enqueue_ready_task, registry.finish_wait, registry.mark_ready, registry.state, registry.state(task_id)
                           .is_none, registry.state(task_id)
                       .is_none ...(+4) | Err, Ok, WaitFlags::from_bits, WaitFlags::from_bits(args[2] as u32).unwrap, WaitFuture::new, axtask::block_task ...(+8) |

#### 任务调度/异步运行时
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `os/components/wateros-task/task-scheduler/scheduler-impl/impl-multi-class/src/lib.rs#init_scheduler` | `modules/axtask/src/api.rs#init_scheduler` | 同板块同名 | InterruptGuard::new, SCHEDULER.as_ptr, SCHEDULER_READY.load, SCHEDULER_READY.store, assert_addr_outside_kernel_heap, compiler_fence ...(+7) | crate::run_queue::init, info! |
| `os/components/wateros-task/src/lib.rs#spawn_user_task` | `modules/axtrap/src/arch/riscv/mod.rs#spawn_user_task` | 同板块同名 | active_impl::with_process_registry, current_process_task_snapshot, current_process_task_snapshot().map, registry.create_process_for_task, scheduler::create_user_task_spec, scheduler::enqueue_ready_task ...(+1) | UserTaskFuture::new, axtask::executor::task_spawn, runnable.schedule, task.clone, task.detach, task_loop |
| `os/components/wateros-task/task-scheduler/scheduler-impl/impl-multi-class/src/lib.rs#spawn_kernel_task` | `modules/axtask/src/future.rs#spawn_kernel_task` | 同板块同名 | InterruptGuard::new, TAG.as_ptr, TAG.len, assert_addr_outside_kernel_heap, scheduler.spawn_kernel_task, scheduler_ready_addr ...(+2) | KernelTaskFuture::new, kernel_task_spawn, runnable.schedule, task.detach |
| `os/components/wateros-vfs/vfs-impl/impl-fd-session/src/cwd.rs#get_cwd` | `modules/axtask/src/task.rs#get_cwd` | 同板块同名 | self.cwd_tables
            .get, self.cwd_tables
            .get(&owner)
            .map, self.cwd_tables
            .get(&owner)
            .map(String::as_str)
            .unwrap_or, self.effective_owner | self.fd_manager.cwd.lock, self.fd_manager.cwd.lock().clone |

#### 内存管理
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `os/components/wateros-driver/driver-block/block-impl/impl-virtio-pci/src/lib.rs#dma_alloc` | `modules/axdriver/src/virtio.rs#dma_alloc` | 同板块同名 | NonNull::dangling, NonNull::new, Vec::new, base_ppn.checked_mul, frame_alloc_result, frame_dealloc_result ...(+3) | NonNull::dangling, NonNull::new, NonNull::new(vaddr as _).unwrap, global_allocator, global_allocator().alloc_pages, paddr.as_usize ...(+2) |
| `os/components/wateros-driver/driver-block/block-impl/impl-virtio-mmio/src/lib.rs#dma_alloc` | `modules/axdriver/src/ixgbe.rs#dma_alloc` | 同板块同名 | NonNull::dangling, NonNull::new, Vec::new, base_ppn.checked_mul, frame_alloc_result, frame_dealloc_result ...(+3) | (vaddr.as_ptr() as usize).into, Layout::from_size_align, Layout::from_size_align(size, 8).unwrap, NonNull::dangling, global_allocator, global_allocator().alloc ...(+3) |
| `os/components/wateros-mm/mm-impl/impl-loongarch64/src/user_heap_mmap.rs#mremap` | `modules/axmem/src/lib.rs#mremap` | 同板块同名 | Err, mmap_map_end, mremap_range, self.range_overlaps_kernel_reserved, self.range_overlaps_stack, self.validate_user_mapping_range | (old_page_start..=old_page_end).step_by, VirtAddr::from, assert!, check_page_table_entry_validity, core::slice::from_raw_parts, core::slice::from_raw_parts_mut ...(+18) |
| `os/components/wateros-mm/mm-impl/impl-sv39/src/user_heap_mmap.rs#mprotect` | `modules/axmem/src/lib.rs#mprotect` | 同板块同名 | Err, Ok, VirtAddr, VirtPageNum, VirtPageNum(vpn.0 + 1).start_addr, addr.0
                               .checked_add ...(+15) | BTreeMap::new, Vec::new, area.contained_in, area.overlap_with, area.split, area.split3 ...(+13) |

#### 中断/异常处理
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `os/components/wateros-platform/platform-arch/arch-impl/impl-loongarch64/src/trap.rs#save_fp_state` | `modules/axhal/src/platform/VisionFive2/irq.rs#register_handler` | 同模块上下文对齐/差异观察 | asm!, regs.as_mut_ptr | with_cause! |
| `os/components/wateros-platform/platform-arch/arch-impl/impl-loongarch64/src/trap.rs#restore_fp_state` | `modules/axhal/src/platform/riscv64_qemu_virt/irq.rs#register_handler` | 同模块上下文对齐/差异观察 | asm!, regs.as_ptr | with_cause! |
| `os/components/wateros-platform/platform-arch/arch-impl/impl-riscv64/src/trap.rs#save_fp_state` | `modules/axtrap/src/arch/riscv/mod.rs#riscv_kernel_trap_handler` | 同模块上下文对齐/差异观察 | asm!, regs.as_mut_ptr | handle_irq, panic!, scause.bits, scause.cause, scause::read |
| `os/components/wateros-platform/platform-arch/arch-impl/impl-riscv64/src/trap.rs#restore_fp_state` | `modules/axhal/src/irq.rs#register_handler_common` | 同模块上下文对齐/差异观察 | asm!, regs.as_ptr | IRQ_HANDLER_TABLE.register_handler, axlog::ax_println!, set_enable, warn! |

#### 系统调用
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `os/components/wateros-syscall/syscall-impl/impl-kernel/src/unix_sock.rs#bind` | `api/linux_syscall_api/src/syscall_net/socket.rs#bind` | 同板块同名 | BOUND.lock, Err, Ok, Some, VecDeque::new, addr.key.clone ...(+9) | into_core_sockaddr, s.bind, self.inner.lock |
| `os/components/wateros-syscall/syscall-impl/impl-kernel/src/unix_sock.rs#listen` | `api/linux_syscall_api/src/syscall_net/socket.rs#listen` | 同板块同名 | BOUND.lock, Err, Ok, bound.get_mut, bound.get_mut(&key).ok_or, inner.bound_key.clone ...(+3) | Err, s.listen, self.inner.lock |
| `os/components/wateros-syscall/syscall-impl/impl-kernel/src/unix_sock.rs#connect` | `api/linux_syscall_api/src/syscall_net/socket.rs#connect` | 同板块同名 | Ok, Some, connect_stream, lookup_current, parse_sockaddr_un, sock.inner.lock | into_core_sockaddr, s.connect, self.inner.lock |
| `os/components/wateros-syscall/syscall-impl/impl-kernel/src/socket_fd.rs#is_nonblocking` | `api/linux_syscall_api/src/syscall_net/socket.rs#is_nonblocking` | 同板块同名 | status_flags, status_flags(fd).is_some_and | s.is_nonblocking, self.inner.lock |

#### 文件系统
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `os/components/wateros-vfs/vfs-impl/impl-fs-bridge/src/file_handle.rs#seek` | `modules/axfs/src/fs/fatfs.rs#seek` | 同板块同名 | Err, Ok, base.checked_add, base.checked_add(offset as u64)
                        .ok_or, base.checked_sub, base.checked_sub((-offset) as u64)
                        .ok_or ...(+5) | Ok, Some, match pos {
            SeekFrom::Start(pos) => Some(pos),
            SeekFrom::Current(off) => self.position().checked_add_signed(off),
            SeekFrom::End(off) => size.checked_add_signed(off),
        }
        .ok_or, self.position, self.position().checked_add_signed, self.set_position ...(+3) |
| `os/components/wateros-vfs/vfs-impl/impl-fs-bridge/src/tmpfs.rs#read_dir` | `modules/axfs/src/fs/fatfs.rs#read_dir` | 同板块同名 | Err, Ok, Self::dir_ref, Self::split_path, Vec::new, children.iter ...(+3) | Ok, VfsDirEntry::new, dirents.iter_mut, dirents.iter_mut().enumerate, dirents.len, entry.file_name ...(+6) |
| `os/components/wateros-vfs/vfs-impl/impl-fs-bridge/src/file_handle.rs#write_at` | `modules/axfs/src/fs/fatfs.rs#write_at` | 同板块同名 | Err, Ok, buf.is_empty, buf.len, off.checked_add, off.checked_add(buf.len())
                     .ok_or ...(+5) | Ok, SeekFrom::Start, ans.is_err, ans.unwrap, buf.len, buf.to_vec ...(+6) |
| `os/components/wateros-vfs/vfs-impl/impl-fs-bridge/src/tmpfs.rs#rename` | `modules/axfs/src/fs/fatfs.rs#rename` | 同板块同名 | Err, Self::dir_ref, Self::dir_ref(&self.root, &new_parts).is_ok, Self::insert_leaf, Self::remove_leaf, Self::split_path | debug!, dst_path.trim_matches, self.0
            .rename, self.0
            .rename(src_path, &self.0, dst_path)
            .map_err |

#### 设备驱动
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `os/components/wateros-driver/driver-block/block-impl/impl-virtio-mmio/src/lib.rs#share` | `modules/axdriver/src/virtio.rs#share` | 同板块同名 | buffer.as_ptr | buffer.as_ptr, vaddr.into, virt_to_phys, virt_to_phys(vaddr.into()).into |
| `os/components/wateros-driver/driver-block/block-impl/impl-virtio-mmio/src/lib.rs#mmio_phys_to_virt` | `modules/axdriver/src/ixgbe.rs#mmio_phys_to_virt` | 同板块同名 | NonNull::new, NonNull::new(paddr as *mut u8).expect | NonNull::new, NonNull::new(phys_to_virt(paddr.into()).as_mut_ptr()).unwrap, paddr.into, phys_to_virt, phys_to_virt(paddr.into()).as_mut_ptr |
| `os/components/wateros-driver/driver-block/block-impl/impl-virtio-pci/src/lib.rs#mmio_phys_to_virt` | `modules/axdriver/src/virtio.rs#mmio_phys_to_virt` | 同板块同名 | NonNull::new, NonNull::new(paddr as *mut u8).expect | NonNull::new, NonNull::new(phys_to_virt(paddr.into()).as_mut_ptr()).unwrap, paddr.into, phys_to_virt, phys_to_virt(paddr.into()).as_mut_ptr |
| `os/components/wateros-driver/driver-block/block-impl/impl-virtio-mmio/src/lib.rs#unshare` | `modules/axdriver/src/virtio.rs#unshare` | 同板块同名同结构 | 内部闭环逻辑 | 内部闭环逻辑 |

#### 文件系统/第三方库
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `os/components/wateros-fs/fs-impl/impl-ext4-rs/src/lib.rs#rename` | `crates/another_ext4/src/ext4/low_level.rs#rename` | 同板块同名 | Err, Ok, child_ref.inode.is_dir, ensure_dir_inode, fs.dir_add_entry, fs.dir_add_entry(&mut parent_ref, &child_ref, new_name)
              .map_err ...(+14) | new_parent.inode.is_dir, parent.inode.is_dir, return_error!, self.dir_find_entry, self.dir_find_entry(&new_parent, new_name).is_ok, self.link_inode ...(+2) |
| `os/components/wateros-fs/fs-impl/impl-ext4-rs/src/lib.rs#unlink` | `crates/another_ext4/src/ext4/low_level.rs#unlink` | 同板块同名 | Err, Ok, child_ref.inode
                             .links_count, child_ref.inode
                        .size, child_ref.inode
                     .set_links_count, ensure_dir_inode ...(+14) | child.inode.is_dir, parent.inode.is_dir, return_error!, self.dir_find_entry, self.read_inode, self.unlink_inode |
| `os/components/wateros-fs/fs-impl/impl-ext4/src/rw.rs#rename` | `crates/rust-fatfs/src/dir.rs#rename` | 同板块同名 | Dir::open_inode, Dir::open_inode(fs, parent_inode).map_err, DirEntryName::try_from, DirEntryName::try_from(new_name).map_err, DirEntryName::try_from(old_name).map_err, Err ...(+15) | Some, dst_dir.find_entry, e.to_dir, e.to_dir().rename, self.find_entry, self.rename ...(+3) |
| `os/components/wateros-fs/fs-impl/impl-ext4/src/rw.rs#rmdir` | `crates/another_ext4/src/ext4/low_level.rs#rmdir` | 同板块同名 | Dir::open_inode, Dir::open_inode(fs, parent_inode).map_err, DirEntryName::try_from, DirEntryName::try_from(name).map_err, Err, Ok ...(+19) | child.inode.is_dir, parent.inode.is_dir, return_error!, self.dir_find_entry, self.dir_list_entries, self.dir_list_entries(&child).len ...(+2) |

#### 网络协议栈/第三方库
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `os/components/wateros-driver/driver-network/network-impl/impl-smoltcp/src/lib.rs#receive` | `modules/axnet/src/smoltcp_impl/mod.rs#receive` | 同板块同名 | SmoltcpRxToken, Some, frame.len, frame.len().min, inner.as_ref, rx_buf[..n].copy_from_slice ...(+4) | AxNetRxToken, AxNetTxToken, Some, dev.can_transmit, dev.receive, dev.recycle_tx_buffers ...(+3) |
| `os/components/wateros-driver/driver-network/network-impl/impl-smoltcp/src/lib.rs#consume` | `modules/axnet/src/smoltcp_impl/mod.rs#consume` | 同板块同名 | dev.lock, dev.lock().send, f, frame.to_vec, log::warn!, self.loopback_queue.len ...(+2) | f, rx_buf.packet_mut, self.0.borrow_mut, self.0.borrow_mut().recycle_rx_buffer, self.0.borrow_mut().recycle_rx_buffer(rx_buf).unwrap, trace! |
| `os/components/wateros-driver/driver-network/network-impl/impl-smoltcp/src/lib.rs#transmit` | `modules/axnet/src/smoltcp_impl/mod.rs#transmit` | 同板块同名 | Some, inner.as_ref, tx_buf.as_mut_slice | AxNetTxToken, Some, dev.can_transmit, dev.recycle_tx_buffers, self.inner.borrow_mut, warn! |
| `os/components/wateros-driver/driver-network/network-impl/impl-smoltcp/src/lib.rs#capabilities` | `modules/axnet/src/smoltcp_impl/mod.rs#capabilities` | 同板块同名 | DeviceCapabilities::default, dev.lock, dev.lock().mtu, self
            .inner
            .as_ref, self
            .inner
            .as_ref()
            .map, self
            .inner
            .as_ref()
            .map(|dev| dev.lock().mtu())
            .unwrap_or | DeviceCapabilities::default |

#### 第三方库
仅参考项目中观察到该模块代表函数，目标项目没有同模块可对齐证据：
- 参考项目：`crates/bcm2835-sdhci/src/Bcm2835SDhci.rs#sd_card_init`，调用摘要：ACMD!, byte_swap, debug!, info!, self.cmd_timeout, self.dumpregs ...(+26)
- 参考项目：`crates/elf_parser/src/arch/x86_64.rs#get_relocate_pairs`，调用摘要：Ok, Vec::new, VirtAddr::from, assert_eq!, dyn_sym.get_name, dyn_sym.get_name(elf).unwrap ...(+23)
- 参考项目：`crates/elf_parser/src/arch/aarch64.rs#get_relocate_pairs`，调用摘要：Ok, Vec::new, VirtAddr::from, assert_eq!, dyn_sym.get_name, dyn_sym.get_name(elf).unwrap ...(+22)
- 参考项目：`crates/elf_parser/src/arch/riscv.rs#get_relocate_pairs`，调用摘要：Ok, Vec::new, VirtAddr::from, assert_eq!, dyn_sym.get_name, dyn_sym.get_name(elf).unwrap ...(+23)

#### 通用模块/其他
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `os/components/wateros-platform/platform-impl/impl-qemu-loongarch64-virt/src/timer.rs#set_timer` | `modules/axhal/src/platform/VisionFive2/time.rs#set_timer` | 同板块同名 | Err, Ok, read_stable_counter, time
        .0
        .saturating_sub, time
        .0
        .saturating_sub(now)
        .max, usize::try_from ...(+3) | super::sbi_call |
| `os/components/wateros-runtime/runtime-logging/src/logger.rs#enabled` | `modules/axlog/src/lib.rs#enabled` | 同板块同名 | log::max_level, log::max_level().to_level, log::max_level().to_level()
                        .unwrap, metadata.level, metadata.target, metadata.target()
                   .starts_with | 内部闭环逻辑 |
| `os/components/wateros-klog/src/lib.rs#write_str` | `modules/axlog/src/lib.rs#write_str` | 同板块同名 | Ok, bytes.len, bytes.len().min, s.as_bytes, self.buf.len, self.buf.len().saturating_sub ...(+1) | Ok, cfg_if::cfg_if! |
| `os/components/wateros-ipc/ipc-signal/signal-api/api-v0/src/lib.rs#contains` | `modules/axmem/src/area.rs#contains` | 同板块同名 | signal_bit | self.end_va |


#### 1. 宏观判定
- **定性结论**: 独立开发 (相似度: 0.73%，共同 AST 指纹: 40；唯一 AST 指纹: 目标项目 3046，参考项目 2483；覆盖率: 目标项目 1.31%，参考项目 1.61%)

#### 2. 全模块覆盖矩阵解读
- **进程管理**
  1. 双方均存在该模块。
  2. 不存在共同 AST 指纹。
  3. 目标项目覆盖率 0.0%，参考项目覆盖率 0.0%。这表明在进程管理这一核心领域，两个项目在抽象语法树层面没有任何可匹配的结构片段，是架构异构性最强的模块。
  4. 该模块证据强度：**未形成**。

- **任务调度/异步运行时**
  1. 双方均存在该模块。
  2. 存在 1 个共同 AST 指纹。
  3. 目标项目覆盖率 0.15%，参考项目覆盖率 0.44%。极低的覆盖率表明，尽管存在一个同名函数 `init_scheduler` 的微弱结构重合，但双方调度器的整体实现（如目标项目的多级反馈队列与参考项目的简单 `VecDeque`）完全不同。
  4. 该模块证据强度：**弱**。

- **内存管理**
  1. 双方均存在该模块。
  2. 不存在共同 AST 指纹。
  3. 目标项目覆盖率 0.0%，参考项目覆盖率 0.0%。零覆盖率直接证明双方在内存分配、地址空间管理、页表操作等底层实现上无代码级复用。
  4. 该模块证据强度：**未形成**。

- **中断/异常处理**
  1. 双方均存在该模块。
  2. 不存在共同 AST 指纹。
  3. 目标项目覆盖率 0.0%，参考项目覆盖率 0.0%。双方在 Trap 处理、上下文保存与恢复等架构强相关代码上无任何结构重合。
  4. 该模块证据强度：**未形成**。

- **系统调用**
  1. 双方均存在该模块。
  2. 不存在共同 AST 指纹。
  3. 目标项目覆盖率 0.0%，参考项目覆盖率 0.0%。尽管双方都实现了 POSIX/Linux 系统调用，但内部派发、参数验证、具体逻辑的 AST 结构完全不同。
  4. 该模块证据强度：**未形成**。

- **文件系统**
  1. 双方均存在该模块。
  2. 存在 4 个共同 AST 指纹。
  3. 目标项目覆盖率 0.61%，参考项目覆盖率 1.72%。重合点集中在 `seek`、`read_dir`、`write_at`、`rename` 等通用 VFS 操作上，属于文件系统接口的领域共性实现，不构成核心逻辑复用。
  4. 该模块证据强度：**弱**。

- **设备驱动**
  1. 双方均存在该模块。
  2. 存在 1 个共同 AST 指纹。
  3. 目标项目覆盖率 0.53%，参考项目覆盖率 2.5%。重合点 `unshare` 为内部闭环逻辑，且 `dma_alloc`、`mmio_phys_to_virt` 等函数虽同名但实现路径不同，属于驱动模型的通用范式。
  4. 该模块证据强度：**弱**。

- **网络协议栈**
  1. 双方均未体现自研网络协议栈，均以第三方库形式存在。
  2. 不存在共同 AST 指纹。
  3. 覆盖率均为 0.0%。
  4. 该模块证据强度：**未形成**。

- **第三方库/公共依赖**
  1. 目标项目在 `文件系统/第三方库` 和 `网络协议栈/第三方库` 中存在少量函数，参考项目存在大量第三方库函数。
  2. 不存在共同 AST 指纹。
  3. 覆盖率均为 0.0%。双方使用的第三方库（如 ext4、fatfs、smoltcp）虽功能相同，但集成方式和版本不同，未产生跨项目的 AST 重合。
  4. 该模块证据强度：**未形成**。

#### 3. 底层 AST 关键证据指纹表
| 所属板块 | 目标项目路径 | 参考项目路径 | 证据类型 | 技术说明 |
| :--- | :--- | :--- | :--- | :--- |
| 任务调度/异步运行时 | `os/components/wateros-task/task-scheduler/scheduler-impl/impl-multi-class/src/lib.rs#init_scheduler` | `modules/axtask/src/api.rs#init_scheduler` | 同板块同名 | 双方均存在调度器初始化函数，但目标项目涉及 `InterruptGuard` 和原子操作，参考项目仅调用 `run_queue::init`，实现层次不同。 |
| 文件系统 | `os/components/wateros-vfs/vfs-impl/impl-fs-bridge/src/file_handle.rs#seek` | `modules/axfs/src/fs/fatfs.rs#seek` | 同板块同名 | 均实现 `SeekFrom` 逻辑，使用 `checked_add_signed` 等标准库方法，属于 POSIX 文件操作的标准实现模式。 |
| 文件系统 | `os/components/wateros-vfs/vfs-impl/impl-fs-bridge/src/tmpfs.rs#read_dir` | `modules/axfs/src/fs/fatfs.rs#read_dir` | 同板块同名 | 均实现目录读取，目标项目操作 `children` 集合，参考项目操作 `dirents` 迭代器，数据结构不同。 |
| 文件系统 | `os/components/wateros-vfs/vfs-impl/impl-fs-bridge/src/file_handle.rs#write_at` | `modules/axfs/src/fs/fatfs.rs#write_at` | 同板块同名 | 均实现指定偏移量写入，包含边界检查，是文件系统的基础通用操作。 |
| 文件系统 | `os/components/wateros-vfs/vfs-impl/impl-fs-bridge/src/tmpfs.rs#rename` | `modules/axfs/src/fs/fatfs.rs#rename` | 同板块同名 | 均实现重命名，目标项目通过 `insert_leaf`/`remove_leaf` 操作树结构，参考项目直接调用底层 FAT 接口。 |
| 设备驱动 | `os/components/wateros-driver/driver-block/block-impl/impl-virtio-mmio/src/lib.rs#unshare` | `modules/axdriver/src/virtio.rs#unshare` | 同板块同名同结构 | 双方均为内部闭环逻辑，可能是极其简单的资源释放操作，如重置指针或释放锁，巧合性极高。 |
| 通用模块/其他 | `os/components/wateros-platform/platform-impl/impl-qemu-loongarch64-virt/src/timer.rs#set_timer` | `modules/axhal/src/platform/VisionFive2/time.rs#set_timer` | 同板块同名 | 均设置定时器，目标项目操作硬件计数器，参考项目通过 SBI 调用，底层硬件抽象完全不同。 |
| 通用模块/其他 | `os/components/wateros-runtime/runtime-logging/src/logger.rs#enabled` | `modules/axlog/src/lib.rs#enabled` | 同板块同名 | 目标项目调用 `log` 库标准接口检查日志级别，参考项目为内部闭环逻辑，实现方式不同。 |
| 通用模块/其他 | `os/components/wateros-klog/src/lib.rs#write_str` | `modules/axlog/src/lib.rs#write_str` | 同板块同名 | 均实现日志写入，目标项目操作字节缓冲区，参考项目使用 `cfg_if` 宏，实现路径不同。 |

#### 4. 核心代码调用流对比剖析
* **【进程管理/进程退出路径】**
* **底层技术析理**: 目标项目的 `sys_exit_group` 展现了完整的多线程进程退出流程，涉及 `drop_task_runtime_resources`（释放任务运行时资源）、`reap_exited_member_threads_runtime_resources`（回收其他线程资源）、`robust_exit_cleanup`（处理 Robust Futex）、`notify_parent_sigchld`（发送 SIGCHLD 信号）等一系列复杂操作。其核心是遍历并清理进程内的所有线程资源。参考项目的 `syscall_exit` 仅调用 `exit_current_task`，最终在 `exit_current` 中操作 `EXITED_TASKS` 队列并调用 `axhal::misc::terminate` 终止执行，是典型的单任务退出模型。两者在**并发模型（多线程 vs 单任务）、资源管理粒度（线程级资源回收 vs 任务状态标记）、退出通知机制（信号 vs 无）**上存在根本性架构差异。
* **代码溯源链接**:
  - 目标项目: `os/components/wateros-syscall/syscall-impl/impl-kernel/src/sys/task.rs#sys_exit_group`
  - 参考项目: `api/linux_syscall_api/src/syscall_task/imp/task.rs#syscall_exit`

* **【内存管理/内存映射调整】**
* **底层技术析理**: 目标项目的 `mremap` 函数通过 `validate_user_mapping_range` 和 `range_overlaps_kernel_reserved` 等方法进行严格的区域验证，其核心是操作内部的 `mmap` 区域描述符。参考项目的 `mremap` 则直接操作页表项，通过 `check_page_table_entry_validity` 和 `core::slice::from_raw_parts` 进行底层内存拷贝。这揭示了两者在**内存管理抽象层次（区域描述符 vs 直接页表操作）和地址空间模型**上的根本不同。
* **代码溯源链接**:
  - 目标项目: `os/components/wateros-mm/mm-impl/impl-loongarch64/src/user_heap_mmap.rs#mremap`
  - 参考项目: `modules/axmem/src/lib.rs#mremap`

* **【系统调用/套接字绑定】**
* **底层技术析理**: 目标项目的 `bind` 函数通过一个全局的 `BOUND` 锁来管理 Unix 套接字地址绑定，并操作 `VecDeque` 作为等待队列。参考项目的 `bind` 则通过 `self.inner.lock` 获取套接字内部的锁，并调用 `s.bind` 进行绑定。两者在**同步策略（全局锁 vs 对象内锁）和对象管理模型（全局表 vs 对象自有状态）**上存在显著差异。
* **代码溯源链接**:
  - 目标项目: `os/components/wateros-syscall/syscall-impl/impl-kernel/src/unix_sock.rs#bind`
  - 参考项目: `api/linux_syscall_api/src/syscall_net/socket.rs#bind`

#### 5. 单方模块与降权项
- **第三方库/公共依赖**: 参考项目存在大量第三方库函数（`crates/bcm2835-sdhci`, `crates/elf_parser` 等），目标项目无此分类。这些是参考项目独有的外部依赖，目标项目未使用，不构成同源证据。
- **跨板块同名函数**: 如 `spawn_user_task`、`get_cwd` 等函数在双方不同模块中出现，属于操作系统领域的通用术语，且实现完全不同，降权为弱信号。
- **简单 Getter/Setter**: 参考项目中 `get_exit_code`、`set_parent` 等函数过于简单，不具备可保护的独创性，不能作为复用证据。
- **通用标准库调用**: 双方证据中大量出现的 `Vec::new`、`assert!`、`Ok`、`Err` 等为标准库或 Rust 语言惯用法，属于公共知识，不构成同源证据。

#### 6. 保守结论
本次审计的客观 AST 相似度仅为 0.73%，远低于可认定代码复用的阈值。在争议焦点的“进程管理”板块，共同 AST 指纹为 0，直接否定了“架构级套壳复用”的指控。双方在进程退出、内存管理、同步策略等核心子系统上展现出截然不同的设计哲学和抽象层次：目标项目采用组件化、多线程、显式状态机的复杂架构，而参考项目则是单体式、单任务、基于 Future 的轻量实现。所有观察到的微弱相似性均可归因于 POSIX/Linux ABI 的强制性约束、操作系统领域的通用设计范式以及 Rust 语言的标准惯用法。综合判定，目标项目 `[2026-T202610422999926-XX]` 相对于参考项目 `[2024-萌新-南开大学]` 为独立开发，整体代码同源性风险极低。

------------------------------

