# 🛡️ OS 内核赛自动化代码级架构审计报告

**审计时间**: `2026-07-02 04:45:10`
**参赛作品名称**: `2026-T202610422999926-XX`
**年份**: `2026`
**学校**: `XX`
**队伍名称**: `T202610422999926`
**比赛名字**: `OS-Competition`
**比赛子赛道**: `Kernel-Implementation`
**仓库地址**: `https://gitlab.eduxiji.net/educg-group-43501-3132633/T202610422999926-98`
**推测开发语言**: `Rust / C (由AST探针自动识别)`
**特征函数量**: `4162`

## ⚔️ 底层代码同源性对比 (AST Homology)
> 本部分由专门的代码审查智能体，对该项目与符合年份条件的历届最高相似度项目进行控制流与底层调用对比。新版报告会先给出确定性的全模块覆盖矩阵，再给出 Agent 的分模块解释，避免只围绕少量抽样函数展开。

### 🏆 Top 5 底层对标: vs `2023-Starry-清华大学`（清华大学 / Starry）

#### 对方作品信息

**对方年份**: `2023`
**对方学校**: `清华大学`
**对方队伍名称**: `Starry`
**对方比赛名字**: `OS-Competition`
**对方比赛子赛道**: `Kernel-Implementation`
**对方仓库地址**: `https://gitlab.eduxiji.net/202310003101649/starry`

> 双向覆盖率：共同 AST 指纹: 56；唯一 AST 指纹: 目标项目 3046，参考项目 6206；覆盖率: 目标项目 1.84%，参考项目 0.9%

## 全模块对比总览（确定性证据）

> 总体覆盖率：共同 AST 指纹: 56；唯一 AST 指纹: 目标项目 3046，参考项目 6206；覆盖率: 目标项目 1.84%，参考项目 0.9%

### 1. 模块覆盖矩阵
| 模块 | 目标函数数 | 参考函数数 | 共同唯一 AST 指纹 | Target 覆盖 | Base 覆盖 | 状态 |
| :--- | ---: | ---: | ---: | ---: | ---: | :--- |
| 进程管理 | 525 | 128 | 1 | 0.24% | 0.98% | 双方存在且有结构重合 |
| 任务调度/异步运行时 | 835 | 296 | 1 | 0.15% | 0.41% | 双方存在且有结构重合 |
| 内存管理 | 760 | 257 | 2 | 0.31% | 0.85% | 双方存在且有结构重合 |
| 中断/异常处理 | 132 | 40 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |
| 系统调用 | 530 | 64 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |
| 文件系统 | 799 | 379 | 4 | 0.61% | 1.3% | 双方存在且有结构重合 |
| 设备驱动 | 237 | 57 | 1 | 0.53% | 1.96% | 双方存在且有结构重合 |
| 网络协议栈 | 0 | 73 | 0 | 0.0% | 0.0% | 仅参考项目体现 |
| 文件系统/第三方库 | 130 | 487 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |
| 网络协议栈/第三方库 | 13 | 110 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |
| 第三方库 | 0 | 5822 | 0 | 0.0% | 0.0% | 仅参考项目体现 |
| 通用模块/其他 | 201 | 341 | 1 | 0.59% | 0.35% | 双方存在且有结构重合 |

### 2. 分模块证据清单

#### 进程管理
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `os/components/wateros-syscall/syscall-impl/impl-kernel/src/sys/kill.rs#send_signal_to_process` | `modules/axprocess/src/signal.rs#send_signal_to_process` | 同板块同名 | Err, Ok, ipc::signal::with_registry, ipc::signal::with_registry(|registry| registry.send_process(process.raw(), sig))
        .map_err, process.raw, registry.has_deliverable ...(+9) | Err, Ok, PID2PC.lock, Some, inner.signal_module.get_mut, inner.signal_module.get_mut(&now_id.unwrap()).unwrap ...(+11) |
| `os/components/wateros-task/task-scheduler/scheduler-impl/impl-multi-class/src/lib.rs#exit_current` | `modules/axtask/src/monolithic_task/run_queue.rs#exit_current` | 同板块同名 | InterruptGuard::new, ScheduleReason::Exit, __switch, core::hint::unreachable_unchecked, guard.release_before_switch, panic! ...(+2) | EXITED_TASKS.lock, EXITED_TASKS.lock().clear, EXITED_TASKS.lock().push_back, WAIT_FOR_EXIT.notify_one_locked, assert!, axhal::misc::terminate ...(+6) |
| `os/components/wateros-task/task-scheduler/scheduler-impl/impl-round-robin/src/lib.rs#exit_current` | `modules/axtask/src/run_queue.rs#exit_current` | 同板块同名 | InterruptGuard::new, ScheduleReason::Exit, __switch, core::hint::unreachable_unchecked, guard.release_before_switch, panic! ...(+2) | EXITED_TASKS.lock, EXITED_TASKS.lock().clear, EXITED_TASKS.lock().push_back, WAIT_FOR_EXIT.notify_one_locked, assert!, axhal::misc::terminate ...(+8) |
| `os/components/wateros-vfs/vfs-impl/impl-fs-bridge/src/proc_handle.rs#seek` | `modules/axprocess/src/stdin.rs#seek` | 同板块同名 | Ok, offset.max, self
                .data
                .len, self
                .data
                .len()
                .saturating_add_signed, self.offset.saturating_add_signed | Err |

#### 任务调度/异步运行时
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `os/components/wateros-ipc/ipc-signal/src/lib.rs#set_timer` | `modules/axtask/src/monolithic_task/stat.rs#set_timer` | 同板块同名 | Ok, process.timer, process.timer(which)?
                         .remaining, process.timer_clock, process.timer_mut, self.processes
                          .get_mut ...(+5) | timer_type.into |
| `os/components/wateros-task/task-scheduler/scheduler-impl/impl-multi-class/src/lib.rs#init_scheduler` | `modules/axtask/src/api.rs#init_scheduler` | 同板块同名 | InterruptGuard::new, SCHEDULER.as_ptr, SCHEDULER_READY.load, SCHEDULER_READY.store, assert_addr_outside_kernel_heap, compiler_fence ...(+7) | crate::timers::init, info!, init |
| `os/components/wateros-task/task-scheduler/scheduler-impl/impl-multi-class/src/lib.rs#block_current` | `modules/axtask/src/monolithic_task/run_queue.rs#block_current` | 同板块同名 | InterruptGuard::new, ScheduleReason::Block, __switch, scheduler.schedule, with_scheduler | assert!, crate::current, curr.clone, curr.set_state, debug!, self.resched_inner ...(+1) |
| `os/components/wateros-task/task-scheduler/scheduler-impl/impl-round-robin/src/lib.rs#block_current` | `modules/axtask/src/run_queue.rs#block_current` | 同板块同名 | InterruptGuard::new, ScheduleReason::Block, __switch, scheduler.schedule, with_scheduler | assert!, crate::current, curr.clone, curr.set_state, debug!, self.resched ...(+1) |

#### 内存管理
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `os/components/wateros-driver/driver-block/block-impl/impl-virtio-pci/src/lib.rs#dma_alloc` | `modules/axdriver/src/virtio.rs#dma_alloc` | 同板块同名 | NonNull::dangling, NonNull::new, Vec::new, base_ppn.checked_mul, frame_alloc_result, frame_dealloc_result ...(+3) | NonNull::dangling, NonNull::new, NonNull::new(vaddr as _).unwrap, global_allocator, global_allocator().alloc_pages, paddr.as_usize ...(+2) |
| `os/components/wateros-mm/mm-impl/impl-loongarch64/src/user_heap_mmap.rs#mremap` | `ulib/c_libax/src/mmap.c#mremap` | 同板块同名 | Err, mmap_map_end, mremap_range, self.range_overlaps_kernel_reserved, self.range_overlaps_stack, self.validate_user_mapping_range | unimplemented |
| `os/components/wateros-runtime/runtime-heap-allocator/src/backend_tlsf.rs#realloc` | `ulib/c_libax/src/stdlib.c#realloc` | 同板块同名 | Layout::from_size_align, Layout::from_size_align(new_size, layout.align())
                                     .unwrap_or, Layout::from_size_align(new_size, layout.align())
                                 .unwrap_or, NonNull::new_unchecked, layout.align, layout.size ...(+13) | ax_free, ax_malloc, malloc |
| `os/components/wateros-mm/mm-impl/impl-sv39/src/user_heap_mmap.rs#mprotect` | `modules/axmem/src/lib.rs#mprotect` | 同板块同名 | Err, Ok, VirtAddr, VirtPageNum, VirtPageNum(vpn.0 + 1).start_addr, addr.0
                               .checked_add ...(+15) | area.contained_in, area.overlap_with, area.split, area.split3, area.strict_contain, area.update_flags ...(+9) |

#### 中断/异常处理
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `os/components/wateros-platform/platform-arch/arch-impl/impl-loongarch64/src/trap.rs#save_fp_state` | `ulib/libax/src/cbindings/time.rs#ax_nanosleep` | 同模块上下文对齐/差异观察 | asm!, regs.as_mut_ptr | ax_call_body! |
| `os/components/wateros-platform/platform-arch/arch-impl/impl-loongarch64/src/trap.rs#restore_fp_state` | `modules/axhal/src/arch/x86_64/idt.rs#new` | 同模块上下文对齐/差异观察 | asm!, regs.as_ptr | InterruptDescriptorTable::new, core::mem::transmute, core::slice::from_raw_parts_mut, entries[i].set_handler_fn |
| `os/components/wateros-platform/platform-arch/arch-impl/impl-riscv64/src/trap.rs#save_fp_state` | `modules/axhal/src/platform/qemu_virt_riscv/irq.rs#register_handler` | 同模块上下文对齐/差异观察 | asm!, regs.as_mut_ptr | with_cause! |
| `os/components/wateros-platform/platform-arch/arch-impl/impl-riscv64/src/trap.rs#restore_fp_state` | `modules/axhal/src/irq.rs#register_handler_common` | 同模块上下文对齐/差异观察 | asm!, regs.as_ptr | IRQ_HANDLER_TABLE.register_handler, set_enable, warn! |

#### 系统调用
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `os/components/wateros-syscall/syscall-impl/impl-kernel/src/unix_sock.rs#bind` | `ulib/starry_libax/src/syscall/socket.rs#bind` | 同板块同名 | BOUND.lock, Err, Ok, Some, VecDeque::new, addr.key.clone ...(+9) | s.bind |
| `os/components/wateros-syscall/syscall-impl/impl-kernel/src/unix_sock.rs#listen` | `ulib/starry_libax/src/syscall/socket.rs#listen` | 同板块同名 | BOUND.lock, Err, Ok, bound.get_mut, bound.get_mut(&key).ok_or, inner.bound_key.clone ...(+3) | Err, s.listen |
| `os/components/wateros-syscall/syscall-impl/impl-kernel/src/unix_sock.rs#connect` | `ulib/starry_libax/src/syscall/socket.rs#connect` | 同板块同名 | Ok, Some, connect_stream, lookup_current, parse_sockaddr_un, sock.inner.lock | s.connect |
| `os/components/wateros-syscall/syscall-impl/impl-kernel/src/socket_fd.rs#is_nonblocking` | `ulib/starry_libax/src/syscall/socket.rs#is_nonblocking` | 同板块同名 | status_flags, status_flags(fd).is_some_and | s.is_nonblocking |

#### 文件系统
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `os/components/wateros-vfs/src/lib.rs#run` | `tools/deptool/src/lib.rs#run` | 同板块同名 | active_impl::backend, active_impl::backend().list_dev_nodes, log::info!, log::warn!, null_dev_smoke, open_read_seek_smoke ...(+3) | generate_deps_graph, output_deps_graph, print!, println! |
| `os/components/wateros-vfs/vfs-impl/impl-fs-bridge/src/file_handle.rs#seek` | `ulib/starry_libax/src/fs/file.rs#seek` | 同板块同名 | Err, Ok, base.checked_add, base.checked_add(offset as u64)
                        .ok_or, base.checked_sub, base.checked_sub((-offset) as u64)
                        .ok_or ...(+5) | self.file.lock, self.file.lock().seek |
| `os/components/wateros-vfs/vfs-impl/impl-fs-bridge/src/tmpfs.rs#read_dir` | `modules/axfs/src/fs/fatfs.rs#read_dir` | 同板块同名 | Err, Ok, Self::dir_ref, Self::split_path, Vec::new, children.iter ...(+3) | Ok, VfsDirEntry::new, dirents.iter_mut, dirents.iter_mut().enumerate, dirents.len, entry.file_name ...(+6) |
| `os/components/wateros-vfs/vfs-impl/impl-fs-bridge/src/tmpfs.rs#rmdir` | `ulib/c_libax/src/unistd.c#rmdir` | 同板块同名 | Err, Ok, Self::is_cgroup_control_name, Self::parent_dir_mut, Self::split_path, children.get ...(+6) | unimplemented |

#### 设备驱动
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `os/components/wateros-driver/driver-block/block-impl/impl-virtio-mmio/src/lib.rs#share` | `modules/axdriver/src/virtio.rs#share` | 同板块同名 | buffer.as_ptr | buffer.as_ptr, vaddr.into, virt_to_phys, virt_to_phys(vaddr.into()).into |
| `os/components/wateros-driver/driver-block/block-impl/impl-virtio-mmio/src/lib.rs#mmio_phys_to_virt` | `modules/axdriver/src/virtio.rs#mmio_phys_to_virt` | 同板块同名 | NonNull::new, NonNull::new(paddr as *mut u8).expect | NonNull::new, NonNull::new(phys_to_virt(paddr.into()).as_mut_ptr()).unwrap, paddr.into, phys_to_virt, phys_to_virt(paddr.into()).as_mut_ptr |
| `os/components/wateros-driver/driver-block/block-impl/impl-virtio-mmio/src/lib.rs#unshare` | `modules/axdriver/src/virtio.rs#unshare` | 同板块同名同结构 | 内部闭环逻辑 | 内部闭环逻辑 |
| `os/components/wateros-driver/driver-network/src/socket_handles.rs#handle` | `modules/axhal/src/platform/aarch64_common/pl011.rs#handle` | 同板块同名 | self.inner.lock | UART.lock, UART.lock().ack_interrupts, UART.lock().is_receive_interrupt, getchar, putchar |

#### 网络协议栈
仅参考项目中观察到该模块代表函数，目标项目没有同模块可对齐证据：
- 参考项目：`ulib/c_libax/src/network.c#inet_pton`，调用摘要：hexval, inet_pton, isdigit, memmove
- 参考项目：`apps/c/udpserver/udpserver.c#main`，调用摘要：bind, htons, inet_pton, ntohs, perror, printf ...(+5)
- 参考项目：`ulib/libax/src/cbindings/socket.rs#ax_recvfrom`，调用摘要：ax_call_body!, debug!
- 参考项目：`ulib/libax/src/cbindings/socket.rs#ax_socket`，调用摘要：ax_call_body!, debug!

#### 文件系统/第三方库
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `os/components/wateros-fs/fs-impl/impl-ext4-rs/src/lib.rs#rename` | `extern_crates/rust-fatfs-a3a834e/src/dir.rs#rename` | 同板块同名 | Err, Ok, child_ref.inode.is_dir, ensure_dir_inode, fs.dir_add_entry, fs.dir_add_entry(&mut parent_ref, &child_ref, new_name)
              .map_err ...(+14) | Some, dst_dir.find_entry, e.to_dir, e.to_dir().rename, self.find_entry, self.rename ...(+3) |
| `os/components/wateros-fs/fs-impl/impl-ext4/src/rw.rs#mkdir` | `crates/axfs_devfs/src/dir.rs#mkdir` | 同板块同名 | Dir::open_inode, Dir::open_inode(fs, parent_inode).map_err, DirEntryName::try_from, DirEntryName::try_from(name).map_err, Duration::from_secs, Err ...(+16) | Self::new, Some, node.clone, self.children.write, self.children.write().insert, self.clone |
| `os/components/wateros-fs/fs-impl/impl-ext4/src/rw.rs#read_dir` | `crates/axfs_devfs/src/dir.rs#read_dir` | 同板块同名 | Ok, Path::try_from, Path::try_from(path).map_err, String::from, Vec::new, core::str::from_utf8 ...(+14) | Ok, VfsDirEntry::new, children.iter, children.iter().skip, children.next, dirents.iter_mut ...(+7) |
| `os/components/wateros-fs/fs-impl/impl-ext4/src/ro.rs#read_dir` | `crates/axfs_ramfs/src/dir.rs#read_dir` | 同板块同名 | Ok, Path::try_from, Path::try_from(path).map_err, String::from, Vec::new, core::str::from_utf8 ...(+13) | Ok, VfsDirEntry::new, children.iter, children.iter().skip, children.next, dirents.iter_mut ...(+7) |

#### 网络协议栈/第三方库
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `os/components/wateros-driver/driver-network/network-impl/impl-smoltcp/src/lib.rs#receive` | `modules/axnet/src/smoltcp_impl/mod.rs#receive` | 同板块同名 | SmoltcpRxToken, Some, frame.len, frame.len().min, inner.as_ref, rx_buf[..n].copy_from_slice ...(+4) | AxNetRxToken, AxNetTxToken, Some, dev.can_transmit, dev.receive, dev.recycle_tx_buffers ...(+3) |
| `os/components/wateros-driver/driver-network/network-impl/impl-smoltcp/src/lib.rs#consume` | `modules/axnet/src/smoltcp_impl/mod.rs#consume` | 同板块同名 | dev.lock, dev.lock().send, f, frame.to_vec, log::warn!, self.loopback_queue.len ...(+2) | f, rx_buf.packet_mut, self.0.borrow_mut, self.0.borrow_mut().recycle_rx_buffer, self.0.borrow_mut().recycle_rx_buffer(rx_buf).unwrap, trace! |
| `os/components/wateros-driver/driver-network/network-impl/impl-smoltcp/src/lib.rs#transmit` | `modules/axnet/src/smoltcp_impl/mod.rs#transmit` | 同板块同名 | Some, inner.as_ref, tx_buf.as_mut_slice | AxNetTxToken, Some, dev.can_transmit, dev.recycle_tx_buffers, self.inner.borrow_mut, warn! |
| `os/components/wateros-driver/driver-network/network-impl/impl-smoltcp/src/lib.rs#capabilities` | `modules/axnet/src/smoltcp_impl/mod.rs#capabilities` | 同板块同名 | DeviceCapabilities::default, dev.lock, dev.lock().mtu, self
            .inner
            .as_ref, self
            .inner
            .as_ref()
            .map, self
            .inner
            .as_ref()
            .map(|dev| dev.lock().mtu())
            .unwrap_or | DeviceCapabilities::default |

#### 第三方库
仅参考项目中观察到该模块代表函数，目标项目没有同模块可对齐证据：
- 参考项目：`extern_crates/libz-sys-1.1.8/src/zlib/contrib/inflate86/inffas86.c#inflate_fast`，调用摘要：内部闭环逻辑
- 参考项目：`extern_crates/libz-sys-1.1.8/src/zlib-ng/inflate.c#PREFIX`，调用摘要：BITS, BYTEBITS, CRC2, CRC4, DROPBITS, INFLATE_MARK_HOOK ...(+38)
- 参考项目：`extern_crates/miniz_oxide-0.6.2/src/inflate/core.rs#decompress`，调用摘要：OutputBuffer::from_slice_and_pos, cfg!, generate_state!, in_buf.iter, in_buf.len, in_iter.len ...(+9)
- 参考项目：`extern_crates/cc-1.0.79/src/lib.rs#add_default_flags`，调用摘要："--target=aarch64-unknown-windows-gnu".into, "--target=i686-unknown-windows-gnu".into, "--target=x86_64-unknown-windows-gnu".into, "-Brepro".into, "-DANDROID".into, "-D_ARM_WINAPI_PARTITION_DESKTOP_SDK_AVAILABLE=1".into ...(+103)

#### 通用模块/其他
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `os/components/wateros-runtime/runtime-logging/src/logger.rs#enabled` | `modules/axlog/src/lib.rs#enabled` | 同板块同名 | log::max_level, log::max_level().to_level, log::max_level().to_level()
                        .unwrap, metadata.level, metadata.target, metadata.target()
                   .starts_with | 内部闭环逻辑 |
| `os/components/wateros-klog/src/lib.rs#write_str` | `modules/axlog/src/lib.rs#write_str` | 同板块同名 | Ok, bytes.len, bytes.len().min, s.as_bytes, self.buf.len, self.buf.len().saturating_sub ...(+1) | Ok, cfg_if::cfg_if! |
| `os/components/wateros-ipc/ipc-signal/signal-api/api-v0/src/lib.rs#contains` | `modules/axmem/src/area.rs#contains` | 同板块同名 | signal_bit | self.end_va |
| `os/components/wateros-runtime/runtime-logging/src/logger.rs#flush` | `modules/axlog/src/lib.rs#flush` | 同板块同名同结构 | 内部闭环逻辑 | 内部闭环逻辑 |


#### 1. 宏观判定
- **定性结论**: 底层架构显著差异 / 独立开发 (相似度: 0.61%，共同 AST 指纹: 56；唯一 AST 指纹: 目标项目 3046，参考项目 6206；覆盖率: 目标项目 1.84%，参考项目 0.9%)

#### 2. 全模块覆盖矩阵解读
- **进程管理**
  - 双方均存在该模块。
  - 存在 1 个共同 AST 指纹。
  - 目标覆盖率 0.24%，参考覆盖率 0.98%，表明双方在进程管理上虽有少量结构重合，但整体实现路径差异巨大。
  - 证据强度：弱。共同指纹仅为 `send_signal_to_process` 等通用功能名，外部调用图谱完全不同。

- **任务调度/异步运行时**
  - 双方均存在该模块。
  - 存在 1 个共同 AST 指纹。
  - 目标覆盖率 0.15%，参考覆盖率 0.41%，覆盖率极低，说明调度器核心逻辑无实质重叠。
  - 证据强度：弱。共同指纹源于 `exit_current`、`block_current` 等通用调度操作名，底层实现（如 `InterruptGuard` vs `EXITED_TASKS`）截然不同。

- **内存管理**
  - 双方均存在该模块。
  - 存在 2 个共同 AST 指纹。
  - 目标覆盖率 0.31%，参考覆盖率 0.85%，覆盖率低，共同指纹为 `dma_alloc`、`mprotect` 等通用内存操作。
  - 证据强度：弱。目标项目使用 `frame_alloc_result` 等自定义分配器，参考项目使用 `global_allocator`，内存模型异构。

- **中断/异常处理**
  - 双方均存在该模块。
  - 未观察到共同结构指纹。
  - 覆盖率均为 0.0%，说明中断处理框架无任何 AST 级重叠。
  - 证据强度：未形成。目标项目基于 `loongarch64/riscv64` 汇编，参考项目基于 `x86_64/aarch64`，硬件层级完全不同。

- **系统调用**
  - 双方均存在该模块。
  - 未观察到共同结构指纹。
  - 覆盖率均为 0.0%，表明系统调用实现路径无 AST 级重合。
  - 证据强度：未形成。目标项目使用 `UserRet::from_error` 等统一返回封装，参考项目直接返回或 `panic!`，错误处理模型不同。

- **文件系统**
  - 双方均存在该模块。
  - 存在 4 个共同 AST 指纹。
  - 目标覆盖率 0.61%，参考覆盖率 1.3%，覆盖率低，共同指纹为 `seek`、`read_dir` 等 VFS 通用接口。
  - 证据强度：弱。目标项目有独立 `procfs` 实现和 `mount_table` 管理，参考项目无对应组件。

- **设备驱动**
  - 双方均存在该模块。
  - 存在 1 个共同 AST 指纹。
  - 目标覆盖率 0.53%，参考覆盖率 1.96%，覆盖率低，共同指纹为 `share`、`mmio_phys_to_virt` 等 virtio 驱动通用操作。
  - 证据强度：弱。virtio 协议操作（如 `buffer.as_ptr`）由硬件标准定义，属于领域通识。

- **网络协议栈**
  - 仅参考项目体现该模块。
  - 无共同指纹。
  - 目标项目无此模块，参考项目有独立网络协议栈实现。
  - 证据强度：未形成。单方存在，不构成同源证据。

- **第三方库/公共依赖**
  - 双方均存在文件系统/第三方库、网络协议栈/第三方库，但参考项目有大量独立第三方库（5822 个函数）。
  - 无共同指纹。
  - 覆盖率均为 0.0%，说明双方使用的第三方库无 AST 级重叠。
  - 证据强度：未形成。第三方库来源不同，无复用痕迹。

#### 3. 底层 AST 关键证据指纹表
| 所属板块 | 目标项目路径 | 参考项目路径 | 证据类型 | 技术说明 |
| :--- | :--- | :--- | :--- | :--- |
| 进程管理 | `os/components/wateros-syscall/.../kill.rs#send_signal_to_process` | `modules/axprocess/src/signal.rs#send_signal_to_process` | 同板块同名 | 同名源于 POSIX 标准功能，外部调用图谱完全不同（`ipc::signal::with_registry` vs `PID2PC.lock`）。 |
| 任务调度/异步运行时 | `os/components/wateros-task/.../impl-multi-class/src/lib.rs#exit_current` | `modules/axtask/src/monolithic_task/run_queue.rs#exit_current` | 同板块同名 | 调度器退出操作通用名，实现异构（`InterruptGuard` + `__switch` vs `EXITED_TASKS.lock`）。 |
| 内存管理 | `os/components/wateros-driver/.../impl-virtio-pci/src/lib.rs#dma_alloc` | `modules/axdriver/src/virtio.rs#dma_alloc` | 同板块同名 | DMA 分配通用操作，分配器后端不同（`frame_alloc_result` vs `global_allocator`）。 |
| 文件系统 | `os/components/wateros-vfs/.../file_handle.rs#seek` | `ulib/starry_libax/src/fs/file.rs#seek` | 同板块同名 | VFS 通用接口，实现细节不同（`base.checked_add` vs `self.file.lock().seek`）。 |
| 设备驱动 | `os/components/wateros-driver/.../impl-virtio-mmio/src/lib.rs#share` | `modules/axdriver/src/virtio.rs#share` | 同板块同名 | virtio 协议标准操作，均为 `buffer.as_ptr` 传递，属硬件规范决定。 |
| 通用模块/其他 | `os/components/wateros-runtime/.../logger.rs#enabled` | `modules/axlog/src/lib.rs#enabled` | 同板块同名 | 日志级别检查通用功能，实现不同（`log::max_level` vs 内部闭环逻辑）。 |

#### 4. 核心代码调用流对比剖析
- **【进程管理/信号投递路径】**
  - **底层技术析理**: 目标项目通过 `ipc::signal::with_registry` 获取全局信号注册中心，再调用 `registry.send_process`，内部使用 `SignalDispatch` 枚举进行状态机分类；参考项目直接锁定 `PID2PC` 全局表，通过 `inner.signal_module.get_mut` 操作信号模块。两者在同步原语（目标用 `registry` 封装，参考用 `PID2PC.lock`）和对象管理模型（目标有独立 IPC 组件，参考内嵌于 `axprocess`）上存在根本性架构差异。
  - **代码溯源链接**:
    - 目标项目: `os/components/wateros-syscall/syscall-impl/impl-kernel/src/sys/kill.rs#send_signal_to_process`
    - 参考项目: `modules/axprocess/src/signal.rs#send_signal_to_process`

- **【任务调度/退出路径】**
  - **底层技术析理**: 目标项目使用 `InterruptGuard::new` 关中断保护临界区，通过 `ScheduleReason::Exit` 标记调度原因，最终调用 `__switch` 进行上下文切换；参考项目直接操作 `EXITED_TASKS` 全局队列，使用 `WAIT_FOR_EXIT` 条件变量通知等待者。目标项目采用多类调度器抽象（`impl-multi-class`），参考项目为单一运行队列，调度策略和同步机制完全不同。
  - **代码溯源链接**:
    - 目标项目: `os/components/wateros-task/task-scheduler/scheduler-impl/impl-multi-class/src/lib.rs#exit_current`
    - 参考项目: `modules/axtask/src/monolithic_task/run_queue.rs#exit_current`

- **【内存管理/DMA分配路径】**
  - **底层技术析理**: 目标项目通过 `frame_alloc_result` 和 `frame_dealloc_result` 进行物理帧分配，使用 `base_ppn.checked_mul` 进行页号计算；参考项目通过 `global_allocator().alloc_pages` 从全局分配器获取内存，使用 `virt_to_phys` 进行地址转换。两者在内存分配后端和地址转换策略上无任何 AST 重叠。
  - **代码溯源链接**:
    - 目标项目: `os/components/wateros-driver/driver-block/block-impl/impl-virtio-pci/src/lib.rs#dma_alloc`
    - 参考项目: `modules/axdriver/src/virtio.rs#dma_alloc`

- **【文件系统/寻址路径】**
  - **底层技术析理**: 目标项目在 `file_handle.rs` 中直接对 `base` 进行 `checked_add`/`checked_sub` 算术操作，并处理饱和偏移；参考项目通过 `self.file.lock().seek` 将寻址委托给文件系统后端。目标项目在 VFS 层直接管理偏移量，参考项目通过锁机制下传操作，抽象层次和职责划分不同。
  - **代码溯源链接**:
    - 目标项目: `os/components/wateros-vfs/vfs-impl/impl-fs-bridge/src/file_handle.rs#seek`
    - 参考项目: `ulib/starry_libax/src/fs/file.rs#seek`

#### 5. 单方模块与降权项
- **仅参考项目存在的模块**：网络协议栈（73 个函数）、第三方库（5822 个函数）。目标项目无对应实现，不构成同源证据。
- **仅目标项目存在的独立组件**：`wateros-fs/fs-procfs`（完整 procfs 实现）、`wateros-syscall/sys/robust.rs`（robust futex 清理）、`wateros-task/scheduler-impl/impl-multi-class`（多类调度器）。这些组件在参考项目中无对应，体现独立开发。
- **跨板块同名/同 hash 降权项**：如目标项目 `sys_exit`（系统调用板块）与参考项目 `exit_current`（调度器板块），虽均涉及进程退出，但所属模块不同，按规则降级为弱信号，不构成同板块主证据。
- **第三方库/公共依赖**：双方均使用了 `smoltcp`、`ext4` 等第三方库，但 AST 指纹无重叠，说明各自集成方式或版本不同，无复用痕迹。

#### 6. 保守结论
本次审计基于 0.61% 的客观 AST 相似度和 1.84%/0.90% 的双向覆盖率，该数值在操作系统内核级项目中属于噪声级巧合。56 个共同指纹主要为 Rust 语言惯用法（`Some`、`Err`、`Vec::new`）和 POSIX 标准功能名（`send_signal_to_process`、`exit_current`），不构成独创性表达复用。双方在进程管理、内存管理、调度器等核心子系统上展现出根本性的架构差异：目标项目采用 `registry` 状态机、多类调度器、独立 procfs 等自研抽象；参考项目使用全局锁、单一运行队列、内嵌信号模块。跨板块同名项已严格降权，单方模块进一步印证独立演进。综合判定，无证据表明存在违规代码复用，整体风险为低。

------------------------------

