# 🛡️ OS 内核赛自动化代码级架构审计报告

**审计时间**: `2026-07-02 04:45:10`
**参赛作品名称**: `2026-T202610422999926-XX`
**年份**: `2026`
**学校**: `XX`
**队伍名称**: `T202610422999926`
**比赛名字**: `OS-Competition`
**比赛子赛道**: `Kernel-Implementation`
**仓库地址**: `https://gitlab.eduxiji.net/educg-group-43501-3132633/T202610422999926-98`
**推测开发语言**: `Rust / C (由AST探针自动识别)`
**特征函数量**: `4162`

## ⚔️ 底层代码同源性对比 (AST Homology)
> 本部分由专门的代码审查智能体，对该项目与符合年份条件的历届最高相似度项目进行控制流与底层调用对比。新版报告会先给出确定性的全模块覆盖矩阵，再给出 Agent 的分模块解释，避免只围绕少量抽样函数展开。

### 🏆 Top 3 底层对标: vs `2025-NoAxiom-杭州电子科技大学`（杭州电子科技大学 / NoAxiom）

#### 对方作品信息

**对方年份**: `2025`
**对方学校**: `杭州电子科技大学`
**对方队伍名称**: `NoAxiom`
**对方比赛名字**: `OS-Competition`
**对方比赛子赛道**: `Kernel-Implementation`
**对方仓库地址**: `https://gitlab.eduxiji.net/educg-group-36002-2710490/T202510336995214-148`

> 双向覆盖率：共同 AST 指纹: 36；唯一 AST 指纹: 目标项目 3046，参考项目 2349；覆盖率: 目标项目 1.18%，参考项目 1.53%

## 全模块对比总览（确定性证据）

> 总体覆盖率：共同 AST 指纹: 36；唯一 AST 指纹: 目标项目 3046，参考项目 2349；覆盖率: 目标项目 1.18%，参考项目 1.53%

### 1. 模块覆盖矩阵
| 模块 | 目标函数数 | 参考函数数 | 共同唯一 AST 指纹 | Target 覆盖 | Base 覆盖 | 状态 |
| :--- | ---: | ---: | ---: | ---: | ---: | :--- |
| 进程管理 | 525 | 207 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |
| 任务调度/异步运行时 | 835 | 434 | 3 | 0.46% | 0.76% | 双方存在且有结构重合 |
| 内存管理 | 760 | 382 | 3 | 0.47% | 0.85% | 双方存在且有结构重合 |
| 中断/异常处理 | 132 | 107 | 2 | 2.47% | 2.99% | 双方存在且有结构重合 |
| 系统调用 | 530 | 48 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |
| 文件系统 | 799 | 877 | 6 | 0.91% | 0.83% | 双方存在且有结构重合 |
| 设备驱动 | 237 | 227 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |
| 网络协议栈 | 0 | 53 | 0 | 0.0% | 0.0% | 仅参考项目体现 |
| 文件系统/第三方库 | 130 | 49 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |
| 网络协议栈/第三方库 | 13 | 0 | 0 | 0.0% | 0.0% | 仅目标项目体现 |
| 通用模块/其他 | 201 | 552 | 3 | 1.76% | 0.73% | 双方存在且有结构重合 |

### 2. 分模块证据清单

#### 进程管理
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `os/components/wateros-syscall/syscall-impl/impl-kernel/src/sys/task.rs#sys_exit_group` | `NoAxiom/kernel/src/syscall/process.rs#sys_exit_group` | 同板块同名 | drop_task_runtime_resources, process_task.pid
                                                      .raw, reap_exited_member_threads_runtime_resources, super::acct::record_current_process_exit, super::robust::robust_exit_cleanup, super::shm::drop_task_attachments ...(+8) | ExitCode::new, Ok, task.terminate, task.terminate_group |
| `os/components/wateros-syscall/syscall-impl/impl-kernel/src/sys/task.rs#sys_exit` | `NoAxiom/kernel/src/syscall/process.rs#sys_exit` | 同板块同名 | drop_task_runtime_resources, process_task.pid
                                                      .raw, reap_exited_member_threads_runtime_resources, super::acct::record_current_process_exit, super::bringup_stats::record_sys_exit, super::robust::robust_exit_cleanup ...(+9) | ExitCode::new, Ok, self.task.terminate |
| `os/components/wateros-syscall/syscall-impl/impl-kernel/src/sys/task.rs#sys_getppid` | `NoAxiom/kernel/src/syscall/process.rs#sys_getppid` | 同板块同名 | UserRet::from_error, UserRet::from_success, pid.raw, snapshot.parent_pid
                                  .map, snapshot.parent_pid
                                  .map(|pid| pid.raw())
                                  .unwrap_or, task::current_process_snapshot | Err, Ok, parent_process.upgrade, parent_process.upgrade().unwrap, parent_process.upgrade().unwrap().tid, self.task.pcb ...(+1) |
| `os/components/wateros-syscall/syscall-impl/impl-kernel/src/sys/task.rs#sys_getpid` | `NoAxiom/kernel/src/syscall/process.rs#sys_getpid` | 同板块同名 | UserRet::from_error, UserRet::from_success, snapshot.pid.raw, task::current_process_task_snapshot, task::current_process_task_snapshot().map, task::current_process_task_snapshot().map(|snapshot| UserRet::from_success(snapshot.pid.raw()))
                                         .unwrap_or_else | Ok, self.task.tgid |

#### 任务调度/异步运行时
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `os/components/wateros-task/task-scheduler/scheduler-impl/impl-multi-class/src/scheduler.rs#schedule` | `NoAxiom/kernel/src/sched/cfs/cfs.rs#schedule` | 同板块同名 | QueueTarget::Blocked, QueueTarget::Exited, QueueTarget::Sleeping, Some, VecDeque::new, current.is_some_and ...(+30) | local.push_with_info, self.current_scheduler, self.current_scheduler().lock |
| `os/components/wateros-task/task-scheduler/scheduler-impl/impl-round-robin/src/scheduler.rs#schedule` | `NoAxiom/kernel/src/sched/runtime.rs#schedule` | 同板块同名 | QueueTarget::Blocked, QueueTarget::Exited, QueueTarget::Sleeping, Some, matches!, self.current_task_ticks
                                              .saturating_add ...(+22) | self.scheduler.lock, self.scheduler.lock().push |
| `os/components/wateros-syscall/syscall-impl/impl-kernel/src/sys/task.rs#sys_setitimer` | `NoAxiom/kernel/src/syscall/time.rs#sys_setitimer` | 同板块同名 | UserITimerVal::default, UserRet::from_error, UserRet::from_success, args.arg, copy_from_user_struct::<UserITimerVal>, copy_to_user_struct ...(+9) | Box::new, ITimer::register, ITimerReal::new, ITimerType::from_repr, ITimerType::from_repr(which).ok_or, Ok ...(+12) |
| `os/components/wateros-syscall/syscall-impl/impl-kernel/src/sys/priority.rs#sys_setpriority` | `NoAxiom/kernel/src/syscall/sched.rs#sys_setpriority` | 同板块同名 | UserRet::from_error, UserRet::from_success, args.arg, check_setpermission, clamp_nice, matches! ...(+4) | Err, Ok, PROCESS_GROUP_MANAGER.lock, PROCESS_GROUP_MANAGER.lock().get_group, PriorityWhich::from_repr, PriorityWhich::from_repr(which).ok_or ...(+17) |

#### 内存管理
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `os/components/wateros-syscall/syscall-impl/impl-kernel/src/sys/mmap.rs#sys_mmap` | `NoAxiom/kernel/src/syscall/mm.rs#sys_mmap` | 同板块同名 | MmapOps::mmap, MmapOps::mmap_file_lazy, MmapOps::mmap_file_with_loader, Ok, Some, UserRet::from_error ...(+15) | MmapFlags::from_bits, MmapFlags::from_bits(flags).ok_or, MmapProts::from_bits_truncate, Ok, align_ceil, flags.contains ...(+3) |
| `os/components/wateros-syscall/syscall-impl/impl-kernel/src/sys/openat.rs#sys_openat` | `NoAxiom/kernel/src/syscall/fs.rs#sys_openat` | 同板块同名 | UserRet::from_error, UserRet::from_success, active_impl::backend, active_impl::backend().metadata, args.arg, backend.open ...(+18) | Err, FdFlags::from, FileFlags::from_bits, FileFlags::from_bits(flags).ok_or, InodeMode::from_bits, InodeMode::from_bits(mode).ok_or ...(+36) |
| `os/components/wateros-syscall/syscall-impl/impl-kernel/src/sys/socket.rs#sys_socket` | `NoAxiom/kernel/src/syscall/net.rs#sys_socket` | 同板块同名 | Box::new, SocketRef::new, UserRet::from_error, UserRet::from_success, args.arg, crate::unix_sock::alloc_unix_socket ...(+9) | AddressFamily::try_from, Arc::new, Ok, PosixSocketType::try_from, SocketFile::new, debug! ...(+9) |
| `os/components/wateros-syscall/syscall-impl/impl-kernel/src/sys/mmap.rs#sys_madvise` | `NoAxiom/kernel/src/syscall/mm.rs#sys_madvise` | 同板块同名 | UserRet::from_error, UserRet::from_success, addr.checked_add, addr.checked_add(len)
           .is_none, args.arg, log::trace! ...(+3) | Err, Madv::from_repr, Madv::from_repr(advice).ok_or, Ok, info! |

#### 中断/异常处理
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `os/components/wateros-platform/platform-arch/arch-impl/impl-loongarch64/src/interrupt.rs#enable_timer_interrupt` | `NoAxiom/lib/arch/src/la64/interrupt.rs#enable_timer_interrupt` | 同板块同名 | Ok, read_csr::<CSR_ECFG>, write_csr::<CSR_ECFG> | ecfg::set_lie |
| `os/components/wateros-platform/platform-arch/arch-impl/impl-riscv64/src/interrupt.rs#enable_timer_interrupt` | `NoAxiom/lib/arch/src/rv64/interrupt.rs#enable_timer_interrupt` | 同板块同名 | Ok, sie::set_stimer | enable_stimer_interrupt |
| `os/components/wateros-platform/platform-arch/src/lib.rs#enable_timer_interrupt` | `NoAxiom/lib/arch/src/la64/interrupt.rs#enable_timer_interrupt` | 同板块同名 | ArchInterruptImpl::enable_timer_interrupt | enable_timer_interrupt |
| `os/components/wateros-platform/platform-arch/arch-api/api-v0/src/trap.rs#is_exception` | `NoAxiom/lib/arch/src/common/trap.rs#is_exception` | 同板块同名同结构 | matches! | matches! |

#### 系统调用
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `os/components/wateros-syscall/syscall-impl/impl-kernel/src/sys/syslog.rs#sys_syslog` | `NoAxiom/kernel/src/syscall/system.rs#sys_syslog` | 同板块同名 | UserRet::from_error, UserRet::from_success, args.arg, copy_from_user, copy_to_user, is_write_priority ...(+3) | Err, Ok, SyslogAction::from_repr, SyslogAction::from_repr(log_type).ok_or, UserPtr::<u8>::new, user_ptr.as_slice_const_checked |
| `os/components/wateros-syscall/syscall-impl/impl-kernel/src/sys/sockopt.rs#sys_getsockopt` | `NoAxiom/kernel/src/syscall/net.rs#sys_getsockopt` | 同板块同名 | UserRet::from_error, UserRet::from_success, args.arg, copy_from_user_struct::<u32>, copy_to_user, copy_to_user_struct ...(+5) | CONGESTION.as_bytes, CONGESTION.len, Ok, PosixSocketOption::from_repr, PosixTcpSocketOptions::from_repr, SocketLevel::try_from ...(+12) |
| `os/components/wateros-syscall/syscall-impl/impl-kernel/src/sys/clock.rs#sys_clock_settime` | `NoAxiom/kernel/src/syscall/time.rs#sys_clock_settime` | 同板块同名 | UserRet::from_error, UserRet::from_success, args.arg, copy_from_user_struct::<UserTimespec>, cred::current_credentials, set_realtime_ns ...(+2) | CLOCK_MANAGER.lock, CLOCK_MANAGER.lock().0.get_mut, ClockId::from_repr, ClockId::from_repr(clockid).ok_or, Err, Ok ...(+9) |
| `os/components/wateros-syscall/syscall-impl/impl-kernel/src/sys/clock.rs#sys_gettimeofday` | `NoAxiom/kernel/src/syscall/time.rs#sys_gettimeofday` | 同板块同名 | UserRet::from_error, UserRet::from_success, args.arg, clock_id_to_ns, copy_to_user_struct | Ok, UserPtr::<TimeVal>::new, buf.write, get_time_ms, get_timeval |

#### 文件系统
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `os/components/wateros-vfs/vfs-impl/impl-fs-bridge/src/file_handle.rs#seek` | `NoAxiom/lib/fatfs/src/file.rs#seek` | 同板块同名 | Err, Ok, base.checked_add, base.checked_add(offset as u64)
                        .ok_or, base.checked_sub, base.checked_sub((-offset) as u64)
                        .ok_or ...(+5) | Err, Ok, Some, debug_assert!, error!, i64::from ...(+16) |
| `os/components/wateros-syscall/syscall-impl/impl-kernel/src/sys/umount2.rs#sys_umount2` | `NoAxiom/kernel/src/syscall/fs.rs#sys_umount2` | 同板块同名 | UserRet::from_error, UserRet::from_success, args.arg, copy_user_path_cstr, log::warn!, mount_point.as_str ...(+4) | Ok, UserPtr::<u8>::new, info!, ptr.get_string_from_ptr |
| `os/components/wateros-klog/src/syscall.rs#read_all` | `NoAxiom/kernel/src/fs/vfs/basic/file.rs#read_all` | 同板块同名 | KlogRingbuf::with, Some, buf[offset..offset + copy_n].copy_from_slice, format_traditional, len.saturating_sub, n.min ...(+3) | Ok, self.meta, self.meta().inode.size, self.read_at, vec! |
| `os/components/wateros-vfs/vfs-impl/impl-fs-bridge/src/tmpfs.rs#read_dir` | `NoAxiom/kernel/src/fs/vfs/basic/file.rs#read_dir` | 同板块同名 | Err, Ok, Self::dir_ref, Self::split_path, Vec::new, children.iter ...(+3) | ".".to_string, "..".to_string, DENTRY_FRONT.clone, DENTRY_HERE.clone, Err, LinuxDirent64::new ...(+25) |

#### 设备驱动
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `os/components/wateros-driver/driver-network/network-impl/impl-virtio-mmio/src/lib.rs#send` | `NoAxiom/lib/driver/src/impls/char/uart8250.rs#send` | 同板块同名 | self.inner.send, self.inner.send(tx_buf).map_err, virtio_drivers::device::net::TxBuffer::from | panic!, self.send_u32, self.send_u8 |
| `os/components/wateros-driver/driver-impl/impl-qemu-loongarch64-virt/src/uart.rs#write_reg` | `NoAxiom/lib/driver/src/impls/block/vf2_sdcard/utils.rs#write_reg` | 同板块同名 | write_volatile | get_sdio_phys_base, io.write_reg_at |
| `os/components/wateros-driver/driver-impl/impl-qemu-loongarch64-virt/src/uart.rs#read_reg` | `NoAxiom/lib/driver/src/impls/block/vf2_sdcard/utils.rs#read_reg` | 同板块同名 | read_volatile | get_sdio_phys_base, io.read_reg_at |
| `os/components/wateros-driver/driver-block/block-impl/impl-block-cache/src/lib.rs#flush` | `NoAxiom/lib/driver/src/impls/display/virtio_gpu.rs#flush` | 同板块同名 | Ok | self.gpu.lock, self.gpu.lock().flush, self.gpu.lock().flush().unwrap |

#### 网络协议栈
仅参考项目中观察到该模块代表函数，目标项目没有同模块可对齐证据：
- 参考项目：`NoAxiom/kernel/src/net/mod.rs#net_udp_test`，调用摘要：Instant::from_millis, LoopBackDev::new, SocketSet::new, SpinLock::new, client.bind, client.bind(233).unwrap ...(+26)
- 参考项目：`NoAxiom/kernel/src/net/tcpsocket.rs#poll`，调用摘要：PollEvent::empty, SOCKET_SET.lock, debug!, poll_ifaces, req.contains, socket.can_recv ...(+6)
- 参考项目：`NoAxiom/kernel/src/net/tcpsocket.rs#accept`，调用摘要：Err, Ok, SOCKET_SET.lock, Self::new_socket, TcpSocket::from_handle, core::mem::replace ...(+15)
- 参考项目：`NoAxiom/kernel/src/net/poll.rs#tcp_poll`，调用摘要：PollEvent::empty, ShutdownType::SHUTDOWN_MASK.bits, events.insert, shutdown.bits, shutdown.contains, socket.can_recv ...(+5)

#### 文件系统/第三方库
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `os/components/wateros-fs/fs-impl/impl-ext4-rs/src/lib.rs#truncate` | `NoAxiom/kernel/src/fs/vfs/impls/ext4/inode.rs#truncate` | 同板块同名 | Err, Ok, fs.get_inode_ref, fs.truncate_inode, fs.truncate_inode(&mut inode_ref, len)
              .map_err, fs.write_back_inode ...(+4) | Ok, assert!, assert_no_lock!, debug!, ext4.truncate_inode, ext4.truncate_inode(&mut inode, new as u64)
                .await
                .map_err ...(+7) |
| `os/components/wateros-fs/fs-impl/impl-ext4-rs/src/lib.rs#symlink` | `NoAxiom/kernel/src/fs/vfs/impls/ext4/dentry.rs#symlink` | 同板块同名 | Ok, ensure_dir_inode, fs.fuse_lookup, fs.fuse_lookup(parent as u64, name)
                     .map_err, fs.fuse_symlink, fs.fuse_symlink(parent as u64, name, target)
          .map_err ...(+8) | Ok, assert!, assert_no_lock!, debug!, downcast_inode.get_inode, downcast_inode.get_inode().lock ...(+19) |
| `os/components/wateros-fs/fs-impl/impl-ext4/src/rw.rs#truncate` | `NoAxiom/kernel/src/fs/vfs/impls/ext4/inode.rs#truncate` | 同板块同名 | Err, Path::try_from, Path::try_from(path).map_err, fs
            .path_to_inode, fs
            .path_to_inode(pathv, FollowSymlinks::All)
            .map_err, inode.file_type ...(+3) | Err |
| `os/components/wateros-fs/fs-impl/impl-ext4-rs/src/lib.rs#name` | `NoAxiom/kernel/src/fs/vfs/impls/ext4/filesystem.rs#name` | 同板块同名 | 内部闭环逻辑 | 内部闭环逻辑 |

#### 网络协议栈/第三方库
仅目标项目中观察到该模块代表函数，参考项目没有同模块可对齐证据：
- 目标项目：`os/components/wateros-driver/driver-network/network-impl/impl-smoltcp/src/lib.rs#should_loopback`，调用摘要：frame.len, is_local_ipv4, u16::from_be_bytes, usize::from
- 目标项目：`os/components/wateros-driver/driver-network/network-impl/impl-smoltcp/src/lib.rs#receive`，调用摘要：SmoltcpRxToken, Some, frame.len, frame.len().min, inner.as_ref, rx_buf[..n].copy_from_slice ...(+4)
- 目标项目：`os/components/wateros-driver/driver-network/network-impl/impl-smoltcp/src/lib.rs#drain_rx_staging`，调用摘要：dev.receive, dev_handle.lock, self.inner.as_ref, self.loopback_queue.pop_front, self.rx_buf[..n].to_vec, self.rx_staging
                        .push_back ...(+2)
- 目标项目：`os/components/wateros-driver/driver-network/network-impl/impl-smoltcp/src/lib.rs#consume`，调用摘要：dev.lock, dev.lock().send, f, frame.to_vec, log::warn!, self.loopback_queue.len ...(+2)

#### 通用模块/其他
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `os/components/wateros-platform/platform-impl/impl-qemu-loongarch64-virt/src/reset.rs#reset` | `NoAxiom/kernel/src/signal/sig_action.rs#reset` | 同板块同名 | Err, core::ptr::write_volatile | SAHandlerType::new_default, Signal::from_raw_sa_index, self.actions.iter_mut, self.actions.iter_mut().enumerate |
| `os/components/wateros-platform/platform-impl/impl-qemu-loongarch64-virt/src/timer.rs#set_timer` | `NoAxiom/lib/arch/src/la64/time.rs#set_timer` | 同板块同名 | Err, Ok, read_stable_counter, time
        .0
        .saturating_sub, time
        .0
        .saturating_sub(now)
        .max, usize::try_from ...(+3) | tcfg::set_en, tcfg::set_init_val, ticlr::clear_timer_interrupt |
| `os/components/wateros-runtime/runtime-logging/src/logger.rs#enabled` | `NoAxiom/kernel/src/utils/log.rs#enabled` | 同板块同名 | log::max_level, log::max_level().to_level, log::max_level().to_level()
                        .unwrap, metadata.level, metadata.target, metadata.target()
                   .starts_with | is_log_booted, is_log_enabled |
| `os/components/wateros-klog/src/lib.rs#write_str` | `user/libd/src/console.rs#write_str` | 同板块同名 | Ok, bytes.len, bytes.len().min, s.as_bytes, self.buf.len, self.buf.len().saturating_sub ...(+1) | Ok, s.as_bytes, write |


#### 1. 宏观判定
- **定性结论**: 独立开发 (相似度: 0.67%，共同 AST 指纹: 36；唯一 AST 指纹: 目标项目 3046，参考项目 2349；覆盖率: 目标项目 1.18%，参考项目 1.53%)

#### 2. 全模块覆盖矩阵解读
- **进程管理**: 双方均存在该模块。共同 AST 指纹为 0，双向覆盖率均为 0.0%。控方虽在此板块提出多项指控，但无任何客观结构指纹支撑，证据强度为**未形成**。
- **任务调度/异步运行时**: 双方均存在该模块。共同 AST 指纹为 3，覆盖率极低（目标 0.46%，参考 0.76%）。共同指纹经核实为 `schedule` 等通用函数名及 Rust 标准库调用，证据强度为**弱**。
- **内存管理**: 双方均存在该模块。共同 AST 指纹为 3，覆盖率极低（目标 0.47%，参考 0.85%）。共同指纹对应 `sys_mmap` 等 POSIX 强制接口，证据强度为**弱**。
- **中断/异常处理**: 双方均存在该模块。共同 AST 指纹为 2，覆盖率略高（目标 2.47%，参考 2.99%）。共同指纹为 `enable_timer_interrupt` 等硬件相关操作，属于架构强制模式，证据强度为**弱**。
- **系统调用**: 双方均存在该模块。共同 AST 指纹为 0，双向覆盖率均为 0.0%。控方指控的 `sys_rt_sigprocmask` 等函数无共同指纹，证据强度为**未形成**。
- **文件系统**: 双方均存在该模块。共同 AST 指纹为 6，覆盖率低（目标 0.91%，参考 0.83%）。共同指纹对应 `seek`、`read_dir` 等通用文件操作，证据强度为**弱**。
- **设备驱动**: 双方均存在该模块。共同 AST 指纹为 0，双向覆盖率均为 0.0%。证据强度为**未形成**。
- **网络协议栈**: 仅参考项目体现，目标项目无此模块。证据强度为**未形成**。
- **第三方库/公共依赖**: 双方均存在该模块。共同 AST 指纹为 0，双向覆盖率均为 0.0%。证据强度为**未形成**。

#### 3. 底层 AST 关键证据指纹表
| 所属板块 | 目标项目路径 | 参考项目路径 | 证据类型 | 技术说明 |
| :--- | :--- | :--- | :--- | :--- |
| 中断/异常处理 | `.../impl-loongarch64/src/interrupt.rs#enable_timer_interrupt` | `.../la64/interrupt.rs#enable_timer_interrupt` | 同板块同名 | 均操作 LoongArch CSR 寄存器以启用时钟中断，属于硬件架构强制模式。 |
| 中断/异常处理 | `.../impl-riscv64/src/interrupt.rs#enable_timer_interrupt` | `.../rv64/interrupt.rs#enable_timer_interrupt` | 同板块同名 | 均操作 RISC-V `sie` 寄存器，属于硬件架构强制模式。 |
| 中断/异常处理 | `.../arch-api/api-v0/src/trap.rs#is_exception` | `.../common/trap.rs#is_exception` | 同板块同名同结构 | 均使用 `matches!` 宏进行异常原因判断，属于 Rust 语言惯用法。 |
| 任务调度 | `.../impl-multi-class/src/scheduler.rs#schedule` | `.../cfs/cfs.rs#schedule` | 同板块同名 | 调度入口函数同名，但内部调用对象（`QueueTarget` vs `local.push_with_info`）完全不同。 |
| 任务调度 | `.../impl-round-robin/src/scheduler.rs#schedule` | `.../runtime.rs#schedule` | 同板块同名 | 调度入口函数同名，但内部操作（`self.current_task_ticks` vs `self.scheduler.lock`）完全不同。 |
| 内存管理 | `.../sys/mmap.rs#sys_mmap` | `.../mm.rs#sys_mmap` | 同板块同名 | POSIX `mmap` 系统调用，参数解析模式（`MmapOps` vs `MmapFlags`）不同。 |
| 内存管理 | `.../sys/mmap.rs#sys_madvise` | `.../mm.rs#sys_madvise` | 同板块同名 | POSIX `madvise` 系统调用，实现极简，均为参数校验后调用内核服务。 |
| 文件系统 | `.../file_handle.rs#seek` | `.../fatfs/src/file.rs#seek` | 同板块同名 | 文件 `seek` 操作，均处理 `base.checked_add/sub`，属于通用算法模式。 |
| 文件系统 | `.../tmpfs.rs#read_dir` | `.../basic/file.rs#read_dir` | 同板块同名 | 目录读取操作，均返回 `"."` 和 `".."` 条目，属于文件系统标准行为。 |
| 通用模块/其他 | `.../logger.rs#enabled` | `.../utils/log.rs#enabled` | 同板块同名 | 日志过滤函数，均检查日志级别和模块名，属于 `log` 框架标准用法。 |

#### 4. 核心代码调用流对比剖析
* **【进程管理/进程退出路径】**
* **底层技术析理**: 目标项目 `sys_exit_group` 采用组件化架构，将退出清理分发至 `acct`、`robust`、`shm`、`signal` 等独立子系统，通过 `super::` 路径调用。参考项目 `exit_handler` 采用单体内核模式，在 `TASK_MANAGER`、`PROCESS_GROUP_MANAGER`、`FUTEX_SHARED_QUEUE` 等全局管理器上集中操作。两者在抽象层次（组件分发 vs 全局锁管理）和对象模型（`FutexHub::global` vs `FUTEX_SHARED_QUEUE`）上存在根本差异。
* **代码溯源链接**:
  - 目标项目: `os/components/wateros-syscall/syscall-impl/impl-kernel/src/sys/task.rs#sys_exit_group`
  - 参考项目: `NoAxiom/kernel/src/task/exit.rs#exit_handler`

* **【系统调用/信号掩码处理路径】**
* **底层技术析理**: 目标项目 `sys_rt_sigprocmask` 使用 `copy_from_user_struct::<u64>` 读取用户态 `u64` 位图，通过 `ipc::signal::with_registry` 闭包操作全局信号注册表。参考项目 `sys_sigprocmask` 使用 `UserPtr::<SigSet>::new` 直接读取用户态 `SigSet` 结构体，通过 `set.try_read/try_write` 操作任务私有字段。两者在数据载体（`u64` vs `SigSet`）、访问路径（全局注册表 vs 任务私有字段）和同步策略（闭包 vs 直接读写）上完全不同。
* **代码溯源链接**:
  - 目标项目: `os/components/wateros-syscall/syscall-impl/impl-kernel/src/sys/task.rs#sys_rt_sigprocmask`
  - 参考项目: `NoAxiom/kernel/src/syscall/signal.rs#sys_sigprocmask`

* **【进程管理/进程等待路径】**
* **底层技术析理**: 目标项目 `sys_waitpid` 通过 `task::current_process_snapshot` 获取进程快照，使用 `validate_wait_options` 校验选项，调用 `wait_for_child_exit` 执行等待。参考项目 `sys_wait4` 通过 `TASK_MANAGER.get` 直接查询 PCB，使用 `WaitOption::from_bits` 解析选项。两者在数据来源（Snapshot vs PCB）和选项校验方式（独立校验器 vs 位掩码解析）上存在显著差异。
* **代码溯源链接**:
  - 目标项目: `os/components/wateros-syscall/syscall-impl/impl-kernel/src/sys/task.rs#sys_waitpid`
  - 参考项目: `NoAxiom/kernel/src/syscall/process.rs#sys_wait4`

* **【文件系统/Procfs 元数据生成路径】**
* **底层技术析理**: 目标项目 `format_status` 从 `process` 快照、`cred::credentials_for`、`comm_for` 等独立模块聚合数据，使用 `format!` 生成文本。参考项目 `base_read` 从 `TASK_MANAGER.get` 获取 PCB，直接读取 `exit_code`、`time_stat` 等字段。两者在数据源（快照+凭证服务 vs 全局管理器+PCB）和聚合方式上完全不同。
* **代码溯源链接**:
  - 目标项目: `os/components/wateros-fs/fs-procfs/procfs-impl/impl-kernel/src/lib.rs#format_status`
  - 参考项目: `NoAxiom/kernel/src/fs/vfs/impls/proc/stat/file.rs#base_read`

#### 5. 单方模块与降权项
- **网络协议栈**: 参考项目存在独立网络协议栈模块（53个函数），目标项目无此模块。目标项目的网络相关代码位于第三方库板块（13个函数），属于 `smoltcp` 等库的封装。双方在此领域无任何可比性。
- **第三方库/公共依赖**: 双方均存在该板块，但共同 AST 指纹为 0。目标项目使用 `ext4-rs`、`smoltcp` 等库，参考项目使用自研或不同第三方库。`name` 等函数返回静态字符串，属于必然相似项，降权处理。
- **`sys_getpid` / `sys_getppid`**: 双方均存在同名函数，但实现极简（仅返回 PID 字段），属于 POSIX 标准接口的必然相似，降权处理。

#### 6. 保守结论
本次审计基于客观 AST 指纹比对，双向覆盖率仅 1.18% 与 1.53%，远低于同源性阈值。控方指控的“进程管理”板块共同指纹为 0，所有相似性均可由 POSIX/Linux ABI 强制语义、Rust 语言惯用法及硬件架构强制模式解释。双方在架构设计上存在根本差异：目标项目采用组件化注册表模式，参考项目采用单体内核全局管理器模式。现有证据无法支持代码复用或“洗稿”指控，整体同源风险极低。

------------------------------

