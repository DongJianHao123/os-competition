# 🛡️ OS 内核赛自动化代码级架构审计报告

**审计时间**: `2026-07-02 05:25:38`
**参赛作品名称**: `2026-T2026104869910397-XX`
**年份**: `2026`
**学校**: `XX`
**队伍名称**: `T2026104869910397`
**比赛名字**: `OS-Competition`
**比赛子赛道**: `Kernel-Implementation`
**仓库地址**: `https://gitlab.eduxiji.net/educg-group-43501-3132633/T2026104869910397-1406`
**推测开发语言**: `Rust / C (由AST探针自动识别)`
**特征函数量**: `2584`

## ⚔️ 底层代码同源性对比 (AST Homology)
> 本部分由专门的代码审查智能体，对该项目与符合年份条件的历届最高相似度项目进行控制流与底层调用对比。新版报告会先给出确定性的全模块覆盖矩阵，再给出 Agent 的分模块解释，避免只围绕少量抽样函数展开。

### 🏆 Top 5 底层对标: vs `2025-KernalCraft-中山大学`（中山大学 / KernalCraft）

#### 对方作品信息

**对方年份**: `2025`
**对方学校**: `中山大学`
**对方队伍名称**: `KernalCraft`
**对方比赛名字**: `OS-Competition`
**对方比赛子赛道**: `Kernel-Implementation`
**对方仓库地址**: `https://gitlab.eduxiji.net/educg-group-36002-2710490/T202510558995240-2536`

> 双向覆盖率：共同 AST 指纹: 275；唯一 AST 指纹: 目标项目 1883，参考项目 1853；覆盖率: 目标项目 14.6%，参考项目 14.84%

## 全模块对比总览（确定性证据）

> 总体覆盖率：共同 AST 指纹: 275；唯一 AST 指纹: 目标项目 1883，参考项目 1853；覆盖率: 目标项目 14.6%，参考项目 14.84%

### 1. 模块覆盖矩阵
| 模块 | 目标函数数 | 参考函数数 | 共同唯一 AST 指纹 | Target 覆盖 | Base 覆盖 | 状态 |
| :--- | ---: | ---: | ---: | ---: | ---: | :--- |
| 进程管理 | 489 | 71 | 4 | 1.73% | 6.35% | 双方存在且有结构重合 |
| 任务调度/异步运行时 | 270 | 115 | 6 | 2.33% | 5.36% | 双方存在且有结构重合 |
| 内存管理 | 486 | 741 | 45 | 10.27% | 10.04% | 双方存在且有结构重合 |
| 中断/异常处理 | 30 | 128 | 2 | 8.0% | 2.0% | 双方存在且有结构重合 |
| 系统调用 | 17 | 21 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |
| 文件系统 | 577 | 147 | 9 | 2.41% | 7.03% | 双方存在且有结构重合 |
| 设备驱动 | 412 | 555 | 192 | 55.01% | 41.03% | 双方存在且有结构重合 |
| 网络协议栈 | 29 | 53 | 1 | 5.0% | 2.33% | 双方存在且有结构重合 |
| 文件系统/第三方库 | 88 | 340 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |
| 第三方库 | 1 | 2 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |
| 通用模块/其他 | 185 | 669 | 5 | 2.87% | 1.89% | 双方存在且有结构重合 |

### 2. 分模块证据清单

#### 进程管理
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `os/src/syscall/process.rs#sys_exec` | `os/src/syscall/process.rs#sys_exec` | 同板块同名 | Err, Ok, String::from, Vec::new, Vec::with_capacity, all_data.as_slice ...(+52) | BUSYBOX_BIN.to_vec, String::from, Vec::new, all_data.as_slice, app_inode.read_all, args.add ...(+15) |
| `os/src/syscall/process.rs#sys_clone` | `os/src/syscall/process.rs#sys_clone` | 同板块同名 | Arc::downgrade, Arc::ptr_eq, CloneFlags::from_bits, Err, Ok, Some ...(+37) | current_process, current_process.fork, new_process.getpid, new_process.inner_exclusive_access, new_process_inner.tasks[0].as_ref, new_process_inner.tasks[0].as_ref().unwrap ...(+3) |
| `os/src/syscall/signal.rs#sys_sigprocmask` | `os/src/syscall/signal.rs#sys_sigprocmask` | 同板块同名 | Err, Ok, current_task, current_task().ok_or, log_info!, oldset.is_null ...(+4) | SignalFlags::from_bits, SignalFlags::from_bits(new_set).unwrap, curr_process.inner_exclusive_access, current_process, inner.memory_set.token, inner.memory_set.write_to_user_ptr ...(+2) |
| `os/src/task/processor.rs#run_tasks` | `os/src/task/processor.rs#run_tasks` | 同板块同名 | PROCESSOR.lock, Some, context_switch_pt, crate::timer::check_futex_timer, drop, fetch_task ...(+5) | PROCESSOR.exclusive_access, Some, __switch, drop, fetch_task, println! ...(+2) |

#### 任务调度/异步运行时
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `os/src/syscall/fs.rs#sys_ioctl` | `os/src/syscall/fs.rs#sys_ioctl` | 同板块同名 | Err, Ok, argp.is_null, block_device_size, block_device_size(path.as_str()).ok_or, current_rtc_time ...(+28) | curr_process.inner_exclusive_access, current_process, current_user_token, file.ioctl, inner.fd_table.len, inner.fd_table[fd].as_ref ...(+3) |
| `os/src/syscall/signal.rs#sys_sigaction` | `os/src/syscall/signal.rs#sys_sigaction` | 同板块同名 | Err, Ok, SigAction::ignore, SigAction::new, SigActionFlags::all, SigActionFlags::all().bits ...(+15) | SignalFlags::from_bits, SignalFlags::from_bits(40).unwrap, check_sigaction_error, current_process, inner.memory_set.token, inner.memory_set.token().into ...(+6) |
| `os/src/syscall/fs.rs#sys_utimensat` | `os/src/syscall/fs.rs#sys_utimensat` | 同板块同名 | Err, InodeMode::all_perm, Ok, OpenFlags::from_bits, current_task, current_task().unwrap ...(+20) | (*atime_ptr).to_ms, (*mtime_ptr).to_ms, Some, TimeSpec::now, TimeSpec::now().to_ms, current_process ...(+15) |
| `os/src/syscall/fs.rs#sys_write` | `os/src/syscall/fs.rs#sys_write` | 同板块同名 | Err, Ok, Some, current_task, current_task().unwrap, drop ...(+25) | UserBuffer::new, current_process, current_user_token, drop, file.clone, file.writable ...(+5) |

#### 内存管理
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `os/src/task/task.rs#exec` | `os/src/task/process.rs#exec` | 同板块同名 | (&aux as *const AuxvT).cast::<u8>, 0usize.to_ne_bytes, Arc::new, AuxvT::new, MemorySet::from_elf, Mutex::new ...(+38) | KERNEL_SPACE.exclusive_access, KERNEL_SPACE.exclusive_access().token, MemorySet::from_elf, TrapContextImpl::app_init_ctx, assert_eq!, core::mem::size_of::<usize> ...(+12) |
| `os/src/task/task.rs#fork` | `os/src/task/process.rs#fork` | 同板块同名 | Arc::clone, Arc::downgrade, Arc::new, Default::default, FdTable::from_existed_table, KernelStack::new ...(+32) | Arc::clone, Arc::downgrade, Arc::new, MemorySet::from_existed_user, RLimit::new, RecycleAllocator::new ...(+29) |
| `os/src/syscall/process.rs#sys_waitpid` | `os/src/syscall/process.rs#sys_waitpid` | 同板块同名 | Err, Ok, TimeVal::default, WaitpidFlags::from_bits_truncate, block_current_and_run_next, child.gettid ...(+29) | assert_eq!, child.getpid, child.inner_exclusive_access, current_process, exit_code_ptr.is_null, inner.children.is_empty ...(+13) |
| `virtio-drivers/src/device/socket/connectionmanager.rs#send_recv` | `virtio-drivers/src/device/socket/connectionmanager.rs#send_recv` | 同板块同名 | "Hello from guest".as_bytes, (hello_from_guest.len() as u32).into, (hello_from_host.len() as u32).into, 0.into, 50.into, Arc::new ...(+43) | "Hello from guest".as_bytes, (hello_from_guest.len() as u32).into, (hello_from_host.len() as u32).into, 0.into, 50.into, Arc::new ...(+43) |

#### 中断/异常处理
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `isomorphic_drivers/src/net/ethernet/intel/ixgbe.rs#enable_irq` | `os/src/hal/irq/loongarch64/mod.rs#enable_irq` | 同板块同名 | self.registers[IXGBE_EIMS].write | Eiointc::enable_irq, log::warn!, self.platic.read_w, self.platic.write_b, self.platic.write_w |
| `isomorphic_drivers/src/net/ethernet/intel/ixgbe.rs#disable_irq` | `os/src/hal/irq/loongarch64/mod.rs#disable_irq` | 同板块同名 | self.registers[IXGBE_EIMS].write | Eiointc::disable_irq, log::warn!, self.platic.read_w, self.platic.write_w |
| `arch/src/riscv64/interrupt.rs#disable_irq` | `os/src/hal/irq/riscv64.rs#disable_irq` | 同板块同名 | sie::clear_sext, sie::clear_ssoft | (*plic).disable, PLICCtxWrapper::new, PLICSrcWrapper::new |
| `arch/src/riscv64/interrupt.rs#enable_irq` | `os/src/hal/irq/loongarch64/eiointc.rs#enable_irq` | 同板块同名 | sie::set_sext, sie::set_ssoft | iocsr_read_d, iocsr_write_d |

#### 系统调用
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `os/src/syscall/signal.rs#check_sigaction_error` | `os/src/syscall/signal.rs#check_sigaction_error` | 同板块同名 | 内部闭环逻辑 | 内部闭环逻辑 |
| `os/src/syscall/signal.rs#sigaction_layout_score` | `os/src/syscall/net.rs#sys_socket` | 同模块上下文对齐/差异观察 | SigActionFlags::all, SigActionFlags::all().bits, action.flags.bits, action.flags.contains, action.mask.contains | create_tcp_socket, create_udp_socket |
| `os/src/syscall/memory.rs#sys_shmget` | `os/src/syscall/input.rs#sys_event_get` | 同模块上下文对齐/差异观察 | Err, KernIpcPerm::from_bits, KernIpcPerm::from_bits(shmflag & !0x1ff).unwrap, Ok, flags.contains, shm_exists ...(+1) | KEYBOARD_DEVICE.clone, MOUSE_DEVICE.clone, kb.is_empty, kb.read_event, mouse.is_empty, mouse.read_event |
| `os/src/syscall/signal.rs#to_linux_siginfo` | `os/src/syscall/info.rs#fmt` | 同模块上下文对齐/差异观察 | LinuxSigInfo::default, sig_info.details.extract_band, sig_info.details.extract_band().unwrap_or, sig_info.details.extract_fd, sig_info.details.extract_pid, sig_info.details.extract_pid().unwrap_or ...(+2) | writeln! |

#### 文件系统
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `virtio-drivers/src/device/blk.rs#device_id` | `virtio-drivers/src/device/blk.rs#device_id` | 同板块同名 | Arc::new, BlkFeature::RING_INDIRECT_DESC.bits, BlkResp {
                            status: RespStatus::OK,
                        }
                        .as_bytes, Default::default, Mutex::new, NonNull::from ...(+19) | Arc::new, BlkFeature::RING_INDIRECT_DESC.bits, Mutex::new, QUEUE_SIZE.into, ReadOnly::new, State::new ...(+13) |
| `virtio-drivers/src/device/blk.rs#flush` | `virtio-drivers/src/device/blk.rs#flush` | 同板块同名 | (BlkFeature::RING_INDIRECT_DESC | BlkFeature::FLUSH).bits, Arc::new, BlkResp {
                            status: RespStatus::OK,
                        }
                        .as_bytes, Default::default, Mutex::new, NonNull::from ...(+19) | (BlkFeature::RING_INDIRECT_DESC | BlkFeature::FLUSH).bits, Arc::new, Mutex::new, QUEUE_SIZE.into, ReadOnly::new, State::new ...(+12) |
| `os/src/fs/inode.rs#open_file` | `os/src/fs/inode.rs#open_file` | 同板块同名 | Err, Ok, OpenFlags::empty, crate::syscall::check_lease_open_conflict, create_file, dentry.get_inode ...(+20) | Arc::new, EXT4.ext4_file_open_at, OSInode::new, Some, flags.contains, flags.read_write |
| `virtio-drivers/src/transport/pci/bus.rs#bar_info` | `virtio-drivers/src/transport/pci/bus.rs#bar_info` | 同板块同名 | (!(size_mask & 0xfffffff0)).wrapping_add, Err, MemoryBarType::try_from, Ok, self.config_read_word, self.config_write_word ...(+1) | (!(size_mask & !flag_bits)).wrapping_add, Err, MemoryBarType::try_from, Ok, Some, self
                .configuration_access
                .read_word ...(+6) |

#### 设备驱动
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `virtio-drivers/src/queue.rs#fake_read_write_queue` | `virtio-drivers/src/queue.rs#fake_read_write_queue` | 同板块同名 | Vec::new, assert!, assert_eq!, assert_ne!, descriptor.flags.contains, descriptor.next ...(+15) | (*available_ring).idx.load, (*used_ring).idx.fetch_add, (*used_ring).idx.load, Vec::new, assert!, assert_eq! ...(+18) |
| `virtio-drivers/examples/x86_64/src/tcp.rs#test_echo_server` | `virtio-drivers/examples/x86_64/src/tcp.rs#test_echo_server` | 同板块同名同结构 | Config::new, DeviceWrapper::new, Instant::from_micros_const, Interface::new, IpAddress::from_str, IpAddress::from_str(IP).unwrap ...(+44) | Config::new, DeviceWrapper::new, Instant::from_micros_const, Interface::new, IpAddress::from_str, IpAddress::from_str(IP).unwrap ...(+44) |
| `virtio-drivers/examples/riscv/src/tcp.rs#test_echo_server` | `virtio-drivers/examples/riscv/src/tcp.rs#test_echo_server` | 同板块同名同结构 | Config::new, DeviceWrapper::new, Instant::from_micros_const, Interface::new, IpAddress::from_str, IpAddress::from_str(IP).unwrap ...(+44) | Config::new, DeviceWrapper::new, Instant::from_micros_const, Interface::new, IpAddress::from_str, IpAddress::from_str(IP).unwrap ...(+44) |
| `virtio-drivers/src/queue.rs#add_buffers` | `virtio-drivers/src/queue.rs#add_buffers` | 同板块同名 | MmioTransport::new, NonNull::from, VirtIOHeader::make_fake_header, VirtQueue::<FakeHal, 4>::new, VirtQueue::<FakeHal, 4>::new(&mut transport, 0, false, false).unwrap, assert! ...(+6) | MmioTransport::new_from_unique, MmioTransport::new_from_unique(
            UniqueMmioPointer::from(&mut header),
            UniqueMmioPointer::from([].as_mut_slice()),
        )
        .unwrap, UniqueMmioPointer::from, VirtIOHeader::make_fake_header, VirtQueue::<FakeHal, 4>::new, VirtQueue::<FakeHal, 4>::new(&mut transport, 0, false, false).unwrap ...(+7) |

#### 网络协议栈
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `os/src/socket/mod.rs#readable` | `os/src/net/port_table.rs#readable` | 同板块同名同结构 | 内部闭环逻辑 | 内部闭环逻辑 |
| `os/src/socket/mod.rs#readable` | `os/src/net/tcp.rs#readable` | 同板块同名同结构 | 内部闭环逻辑 | 内部闭环逻辑 |
| `os/src/socket/mod.rs#writable` | `os/src/net/port_table.rs#writable` | 同板块同名同结构 | 内部闭环逻辑 | 内部闭环逻辑 |
| `os/src/socket/mod.rs#writable` | `os/src/net/tcp.rs#writable` | 同板块同名同结构 | 内部闭环逻辑 | 内部闭环逻辑 |

#### 文件系统/第三方库
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `lwext4/src/file.rs#write_at` | `ext4_rs/src/ext4_impls/file.rs#write_at` | 同板块同名 | CString::new, CString::new(path.as_str()).map_err, Ok, Self::to_lw_flags, Self::trace_file_path, Some ...(+20) | Block::load, Ok, block.sync_blk_to_disk, block.write_offset, drop, inode_ref
                .inode
                .set_size ...(+9) |
| `lwext4/src/file.rs#read_at` | `ext4_rs/src/ext4_impls/file.rs#read_at` | 同板块同名 | Ok, Self::trace_file_path, Some, buf.as_mut_ptr, buf.len, cvt ...(+9) | Ok, core::cmp::min, inode_ref.inode.size, log::trace!, min, read_buf.len ...(+5) |
| `lwext4/src/inode.rs#set_mode` | `ext4_rs/src/ext4_defs/inode.rs#set_mode` | 同板块同名 | CString::new, InodeMode::from_bits_truncate, cpath.as_ptr, ffi::ext4_mode_set, path.as_str, perm.bits ...(+2) | 内部闭环逻辑 |
| `lwext4/src/fs.rs#mount` | `ext4_rs/src/ext4_impls/extents.rs#ext_remove_leaf` | 同模块上下文对齐/差异观察 | Arc::new, CString::new, CString::new(Self::c_mount_path(abs_mount_path.as_str()))
                .map_err, CString::new(dev_name.as_str()).map_err, DEFAULT_BLOCK_COUNT.load, DentryInner::new ...(+44) | Block::load, Block::load_inode_root_block, Ext4Extent::default, Ok, ex.get_actual_len, ex.get_pblock ...(+14) |

#### 第三方库
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `virtio-drivers/src/transport/mmio.rs#vendor_id` | `virtio-drivers/src/transport/mmio.rs#vendor_id` | 同板块同名 | volread! | field_shared!, field_shared!(self.header, vendor_id).read |

#### 通用模块/其他
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `arch/src/loongarch64/boot.rs#_start` | `os/src/hal/boot/loongarch.rs#_start` | 同板块同名 | core::arch::asm! | core::arch::naked_asm! |
| `time/src/lib.rs#partial_cmp` | `os/src/timer.rs#partial_cmp` | 同板块同名 | Some | Some, a.cmp |
| `time/src/lib.rs#partial_cmp` | `os/src/timer.rs#partial_cmp` | 同板块同名 | Some | Some, self.cmp |
| `arch/src/lib.rs#clear_bss` | `os/src/main.rs#clear_bss` | 同板块同名 | core::slice::from_raw_parts_mut, core::slice::from_raw_parts_mut(
            _sbss as usize as *mut u128,
            (_ebss as usize - _sbss as usize) / size_of::<u128>(),
        )
        .fill, size_of::<u128> | core::slice::from_raw_parts_mut, core::slice::from_raw_parts_mut(sbss as usize as *mut u8, ebss as usize - sbss as usize)
            .fill |


好的，审判长。本庭已收到控辩双方的全部陈述、证据材料及底层AST指纹数据。现依据《审计指令》，对本案进行最终同源性定性仲裁。

---

### 1. 宏观判定
- **定性结论**: **独立开发 (底层架构显著差异)**
- **核心依据**: 客观AST相似度仅为 **7.95%**，双向覆盖率均低于15%。该数据远未达到“抄袭”或“换皮复用”的阈值。双方在进程管理、内存管理等核心板块的架构设计、同步原语、对象模型上存在根本性差异。控方所指控的“100%克隆”函数，经核查，均为Rust语言标准库惯用法或POSIX规范下的唯一语义实现，不构成独创性表达的抄袭。

### 2. 全模块覆盖矩阵解读

| 模块 | 双方存在 | 共同指纹 | 覆盖率 (目标/参考) | 证据强度 | 解读 |
| :--- | :---: | :---: | :---: | :---: | :--- |
| **进程管理** | 是 | 4 | 1.73% / 6.35% | **弱** | 共同指纹极少，且均为`Option::take`等语言惯用法。双方在`sys_clone`、`sys_exec`等核心调用上实现路径完全不同。 |
| **任务调度/异步运行时** | 是 | 6 | 2.33% / 5.36% | **弱** | 调度器核心函数`run_tasks`的上下文切换机制（`context_switch_pt` vs `__switch`）和同步原语（`lock` vs `exclusive_access`）异构。 |
| **内存管理** | 是 | 45 | 10.27% / 10.04% | **中** | 覆盖率相对较高，但深入分析发现，共同指纹多集中在`virtio-drivers`等第三方公共库，以及`MemorySet::from_elf`等通用内存操作。核心的`fork`/`exec`流程中，内存复制与映射策略存在差异。 |
| **中断/异常处理** | 是 | 2 | 8.0% / 2.0% | **弱** | 双方均实现了`enable_irq`/`disable_irq`，但底层硬件抽象层（HAL）完全不同，目标项目直接操作寄存器，参考项目通过`Eiointc`等封装。 |
| **系统调用** | 是 | 0 | 0.0% / 0.0% | **未形成** | 无共同AST指纹，表明双方在系统调用分发与实现上完全独立。 |
| **文件系统** | 是 | 9 | 2.41% / 7.03% | **弱** | 共同指纹主要来自`virtio-drivers`块设备驱动。双方自研的文件系统逻辑（如`open_file`）调用链不同，目标项目使用`dentry`抽象，参考项目直接调用`EXT4`。 |
| **设备驱动** | 是 | 192 | 55.01% / 41.03% | **强（第三方库）** | 这是唯一高覆盖率的模块。证据明确指向双方均复用了开源的`virtio-drivers`库，属于**合法公共依赖**，不构成相互抄袭。 |
| **网络协议栈** | 是 | 1 | 5.0% / 2.33% | **弱** | 共同指纹为`readable`/`writable`等空实现或简单状态查询，属于通用接口。 |
| **第三方库/公共依赖** | 是 | 0 | 0.0% / 0.0% | **未形成** | 双方各自引用了不同的第三方库（如`lwext4` vs `ext4_rs`），无共同指纹。 |

### 3. 底层 AST 关键证据指纹表

| 所属板块 | 目标项目路径 | 参考项目路径 | 证据类型 | 技术说明 |
| :--- | :--- | :--- | :--- | :--- |
| 进程管理 | `os/src/task/processor.rs#current` | `os/src/task/processor.rs#current` | 语言惯用法 | `self.current.as_ref().map(...)` 是Rust `Option`类型的标准访问模式，不构成独创性表达。 |
| 进程管理 | `os/src/task/processor.rs#take_current` | `os/src/task/processor.rs#take_current` | 语言惯用法 | `self.current.take()` 是Rust `Option`类型的标准方法调用。 |
| 进程管理 | `os/src/syscall/process.rs#sys_exit` | `os/src/syscall/process.rs#sys_exit` | 语义唯一性 | `exit_current_and_run_next` 是`exit`系统调用的唯一自然实现路径。 |
| 进程管理 | `os/src/syscall/process.rs#sys_yield` | `os/src/syscall/process.rs#sys_yield` | 语义唯一性 | `suspend_current_and_run_next` 是`yield`系统调用的唯一自然实现路径。 |
| 任务调度 | `os/src/task/processor.rs#run_tasks` | `os/src/task/processor.rs#run_tasks` | 架构异构 | 目标项目使用`context_switch_pt`和`PROCESSOR.lock()`，参考项目使用`__switch`和`PROCESSOR.exclusive_access()`，底层机制不同。 |
| 内存管理 | `os/src/task/task.rs#exec` | `os/src/task/process.rs#exec` | 流程异构 | 目标项目通过`AuxvT::new`构建辅助向量，参考项目通过`TrapContextImpl::app_init_ctx`初始化上下文，实现路径不同。 |
| 内存管理 | `os/src/task/task.rs#fork` | `os/src/task/process.rs#fork` | 流程异构 | 目标项目使用`FdTable::from_existed_table`复制文件描述符表，参考项目使用`RecycleAllocator::new`，资源管理策略不同。 |
| 设备驱动 | `virtio-drivers/src/device/blk.rs#device_id` | `virtio-drivers/src/device/blk.rs#device_id` | **第三方库** | 双方均使用了开源的`virtio-drivers`库，此指纹源于公共依赖，非互相抄袭。 |
| 设备驱动 | `virtio-drivers/examples/riscv/src/tcp.rs#test_echo_server` | `virtio-drivers/examples/riscv/src/tcp.rs#test_echo_server` | **第三方库** | 双方均使用了开源的`virtio-drivers`库中的示例代码，此指纹源于公共依赖。 |

### 4. 核心代码调用流对比剖析

* **【进程管理/任务调度核心】**
* **底层技术析理**: 双方均实现了`run_tasks`调度循环，但底层同步与上下文切换机制截然不同。目标项目采用`PROCESSOR.lock()`（标准自旋锁）和`context_switch_pt`函数，并在循环中集成了`check_futex_timer`和`handle_signals`，表明其调度器需同时处理futex定时器与信号。参考项目则使用`PROCESSOR.exclusive_access()`（自定义独占访问原语）和`__switch`函数，调度逻辑相对简洁。这种在**同步原语、切换函数、功能集成度**上的三重差异，是架构独立设计的强有力证明。
* **代码溯源链接**:
  - 目标项目: `os/src/task/processor.rs#run_tasks`
  - 参考项目: `os/src/task/processor.rs#run_tasks`

* **【进程管理/进程创建】**
* **底层技术析理**: `sys_clone`系统调用是进程创建的核心。目标项目的实现基于`CloneFlags::from_bits`解析标志位，并通过`add_task`等底层函数进行任务分发，调用链涉及`Arc::downgrade`、`Arc::ptr_eq`等智能指针操作。参考项目则基于`current_process.fork`这一高层封装，直接进行进程分叉。两者在**抽象层次和调用对象**上完全不同，目标项目更贴近底层任务管理，参考项目则更侧重于进程级操作。
* **代码溯源链接**:
  - 目标项目: `os/src/syscall/process.rs#sys_clone`
  - 参考项目: `os/src/syscall/process.rs#sys_clone`

### 5. 单方模块与降权项

*   **目标项目独有功能**: `sys_waitid`, `sys_futex`, `sys_prctl`, `sys_prlimit64`, `sys_keyctl`, `sys_msgctl`/`sys_msgget`/`sys_msgrcv`/`sys_msgsnd`, `sys_mq_open`, `sys_unshare` 等系统调用，以及 `vfs/src/procfs/mod.rs` 中的独立procfs实现，在参考项目中均无对应实现。这证明目标项目有大量独立开发、功能更丰富的代码，**无源可抄**。
*   **第三方库/公共依赖**: `virtio-drivers` 库在双方项目中均有使用，是设备驱动模块高AST覆盖率的唯一来源。此为**合法公共代码复用**，不作为同源性抄袭证据。
*   **跨板块误归类**: 控方将参考项目中属于设备树解析器的 `fdt/src/node.rs#children` 函数归入“进程管理”板块，属于**证据归类错误**，予以排除。
*   **同名异构函数**: `sys_exec`, `sys_sigprocmask`, `sys_getpid` 等函数虽同名，但调用图谱显示其内部实现、数据获取路径（如`get_dentry().path` vs `read_all`）完全不同，降级为弱信号。

### 6. 保守结论

本次审计的客观AST相似度仅为7.95%，远低于通常认定抄袭的阈值。控方基于少数几个在Rust语言和POSIX规范下具有唯一性或通用性的代码片段提出的“严重抄袭”指控，证据不足。深入分析表明，双方在进程调度、内存管理、系统调用等核心板块的**同步原语、上下文切换机制、对象模型和抽象层次**上存在根本性架构差异。设备驱动模块的高相似度完全源于对开源第三方库`virtio-drivers`的合法复用。综合判定，目标项目[2026-T2026104869910397-XX]相对于参考项目[2025-KernalCraft-中山大学]是**独立开发**的作品，不存在违规抄袭或换皮复用行为。

------------------------------

