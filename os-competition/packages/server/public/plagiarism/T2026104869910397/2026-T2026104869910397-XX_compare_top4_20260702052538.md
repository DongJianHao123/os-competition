# 🛡️ OS 内核赛自动化代码级架构审计报告

**审计时间**: `2026-07-02 05:25:38`
**参赛作品名称**: `2026-T2026104869910397-XX`
**年份**: `2026`
**学校**: `XX`
**队伍名称**: `T2026104869910397`
**比赛名字**: `OS-Competition`
**比赛子赛道**: `Kernel-Implementation`
**仓库地址**: `https://gitlab.eduxiji.net/educg-group-43501-3132633/T2026104869910397-1406`
**推测开发语言**: `Rust / C (由AST探针自动识别)`
**特征函数量**: `2584`

## ⚔️ 底层代码同源性对比 (AST Homology)
> 本部分由专门的代码审查智能体，对该项目与符合年份条件的历届最高相似度项目进行控制流与底层调用对比。新版报告会先给出确定性的全模块覆盖矩阵，再给出 Agent 的分模块解释，避免只围绕少量抽样函数展开。

### 🏆 Top 4 底层对标: vs `2025-NPUcore-BLOSSOM-西北工业大学`（西北工业大学 / NPUcore-BLOSSOM）

#### 对方作品信息

**对方年份**: `2025`
**对方学校**: `西北工业大学`
**对方队伍名称**: `NPUcore-BLOSSOM`
**对方比赛名字**: `OS-Competition`
**对方比赛子赛道**: `Kernel-Implementation`
**对方仓库地址**: `https://gitlab.eduxiji.net/educg-group-36002-2710490/oskernel2025-npucore-blossom`

> 双向覆盖率：共同 AST 指纹: 313；唯一 AST 指纹: 目标项目 1883，参考项目 2354；覆盖率: 目标项目 16.62%，参考项目 13.3%

## 全模块对比总览（确定性证据）

> 总体覆盖率：共同 AST 指纹: 313；唯一 AST 指纹: 目标项目 1883，参考项目 2354；覆盖率: 目标项目 16.62%，参考项目 13.3%

### 1. 模块覆盖矩阵
| 模块 | 目标函数数 | 参考函数数 | 共同唯一 AST 指纹 | Target 覆盖 | Base 覆盖 | 状态 |
| :--- | ---: | ---: | ---: | ---: | ---: | :--- |
| 进程管理 | 489 | 75 | 6 | 2.6% | 8.82% | 双方存在且有结构重合 |
| 任务调度/异步运行时 | 270 | 143 | 7 | 2.72% | 5.38% | 双方存在且有结构重合 |
| 内存管理 | 486 | 605 | 12 | 2.74% | 2.8% | 双方存在且有结构重合 |
| 中断/异常处理 | 30 | 47 | 1 | 4.0% | 2.5% | 双方存在且有结构重合 |
| 系统调用 | 17 | 65 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |
| 文件系统 | 577 | 561 | 4 | 1.07% | 1.08% | 双方存在且有结构重合 |
| 设备驱动 | 412 | 47 | 9 | 2.58% | 19.15% | 双方存在且有结构重合 |
| 网络协议栈 | 29 | 88 | 1 | 5.0% | 2.13% | 双方存在且有结构重合 |
| 文件系统/第三方库 | 88 | 369 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |
| 网络协议栈/第三方库 | 0 | 138 | 0 | 0.0% | 0.0% | 仅参考项目体现 |
| 第三方库 | 1 | 1228 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |
| 通用模块/其他 | 185 | 241 | 5 | 2.87% | 3.88% | 双方存在且有结构重合 |

### 2. 分模块证据清单

#### 进程管理
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `os/src/syscall/process.rs#sys_clone` | `os/src/syscall/process.rs#sys_clone` | 同板块同名 | Arc::downgrade, Arc::ptr_eq, CloneFlags::from_bits, Err, Ok, Some ...(+37) | CloneFlags::from_bits, CloneFlags::from_bits(flags & !0xff).unwrap, Signals::empty, Signals::from_signum, add_task, current_task ...(+7) |
| `os/src/syscall/process.rs#sys_futex` | `os/src/syscall/process.rs#sys_futex` | 同板块同名 | Arc::clone, Err, FutexCmd::from_bits_truncate, FutexKey::new, FutexOpt::from_bits_truncate, Ok ...(+20) | FutexOption::from_bits_truncate, current_task, current_task().unwrap, do_futex_wait, drop, info! ...(+15) |
| `os/src/syscall/signal.rs#sys_sigprocmask` | `os/src/syscall/process.rs#sys_sigprocmask` | 同板块同名 | Err, Ok, current_task, current_task().ok_or, log_info!, oldset.is_null ...(+4) | info!, sigprocmask |
| `os/src/task/processor.rs#run_tasks` | `os/src/task/processor.rs#run_tasks` | 同板块同名 | PROCESSOR.lock, Some, context_switch_pt, crate::timer::check_futex_timer, drop, fetch_task ...(+5) | PROCESSOR.lock, Some, __switch, do_wake_expired, drop, fetch_task ...(+2) |

#### 任务调度/异步运行时
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `os/src/syscall/fs.rs#sys_fcntl` | `os/src/syscall/fs.rs#sys_fcntl` | 同板块同名 | Err, FCNTL_LOCK_TABLE.lock, FdFlags::CLOEXEC.bits, FdFlags::from, Ok, OpenFlags::empty ...(+63) | Fcntl_Command::from_primitive, OpenFlags::O_NONBLOCK.bits, OpenFlags::O_RDWR.bits, current_task, current_task().unwrap, fd_table.get_ref ...(+10) |
| `os/src/syscall/fs.rs#sys_ioctl` | `os/src/syscall/fs.rs#sys_ioctl` | 同板块同名 | Err, Ok, argp.is_null, block_device_size, block_device_size(path.as_str()).ok_or, current_rtc_time ...(+28) | current_task, current_task().unwrap, fd_table.get_ref, file_descriptor.ioctl, task.files.lock |
| `os/src/syscall/fs.rs#sys_statx` | `os/src/syscall/fs.rs#sys_statx` | 同板块同名 | AtFlags::from_bits_truncate, Err, Ok, at_path_to_dentry, current_task, current_task().unwrap ...(+5) | FstatatFlags::from_bits, copy_to_user, copy_to_user(token, &file_descriptor.get_statx(mask), buf as *mut Statx).is_err, current_task, current_task().unwrap, current_user_token ...(+13) |
| `os/src/syscall/fs.rs#sys_openat` | `os/src/syscall/fs.rs#sys_openat` | 同板块同名 | Err, Fd::new, FdFlags::from, InodeMode::from_bits_truncate, Ok, OpenFlags::from_bits ...(+28) | OpenFlags::from_bits, StatMode::from_bits, current_task, current_task().unwrap, debug!, fd_table.get_ref ...(+11) |

#### 内存管理
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `os/src/mm/memory_set.rs#munmap` | `os/src/mm/memory_set.rs#munmap` | 同板块同名 | Ok, Some, page_up, removed
            .removed
            .iter, removed
            .removed
            .iter()
            .filter_map, removed
            .removed
            .iter()
            .filter_map(|area| match area.backend {
                VmaBackend::File { inode_id, .. } => Some(inode_id),
                _ => None,
            })
            .collect ...(+16) | Err, Ok, Some, Vec::new, VirtAddr::from, area.check_overlapping ...(+21) |
| `os/src/drivers/block/pci_virtio_blk.rs#enumerate_pci` | `os/src/drivers/block/virtio_blk_pci.rs#enumerate_pci` | 同板块同名 | PciRangeAllocator::new, PciRoot::new, PciTransport::new::<VirtioHal>, PciTransport::new::<VirtioHal>(&mut pci_root, device_function).unwrap, Some, bar_info.takes_two_entries ...(+10) | MmioCam::new, PciRangeAllocator::new, PciRoot::new, PciTransport::new::<VirtioHal, MmioCam>, PciTransport::new::<VirtioHal, MmioCam>(&mut pci_root, device_function).unwrap, Some ...(+10) |
| `os/src/drivers/virtio.rs#unshare` | `os/src/drivers/block/virtio_blk.rs#unshare` | 同板块同名 | SHARED_BUFFERS.lock, buffers.iter, buffers.iter().position, buffers.swap_remove, log::error!, matches! ...(+5) | PhysAddr, PhysAddr(paddr + buffer.len()).ceil, PhysAddr(paddr).floor, buffer.as_mut, buffer.copy_from_slice, buffer.len ...(+4) |
| `arch/src/pagetable.rs#translate` | `os/src/hal/arch/loongarch64/laflex.rs#translate` | 同板块同名 | PhysAddr, Self::get_pte_list, Some, pte.address, pte.flags, pte.flags().into ...(+5) | pte.ppn, self.find_pte, self.find_pte(vpn).map |

#### 中断/异常处理
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `arch/src/riscv64/interrupt.rs#enable_external_irq` | `os/src/hal/arch/riscv/trap/mod.rs#enable_timer_interrupt` | 同板块同结构 | sie::set_sext | sie::set_stimer |
| `arch/src/loongarch64/unaligned.rs#emulate_load_store_insn` | `os/src/hal/arch/loongarch64/trap/mod.rs#trap_from_kernel` | 同模块上下文对齐/差异观察 | asm!, badv::read, badv::read().raw, panic!, read_fpr, unaligned_read ...(+2) | ((addr + i) as *mut u8).read_unaligned, ((addr + i) as *mut u8).write_unaligned, (0..sz).rev, BadV::read, BadV::read().get_vaddr, EStat::read ...(+14) |
| `arch/src/riscv64/interrupt.rs#uservec` | `os/src/hal/arch/loongarch64/trap/mem_access.rs#get_addr` | 同模块上下文对齐/差异观察 | asm! | self.get_op_code, self.get_op_code().map, self.get_rj_num, self.get_rk_num, self.get_si12, self.get_si14 |
| `arch/src/loongarch64/trap.rs#user_vec` | `os/src/hal/arch/loongarch64/trap/mem_access.rs#get_size` | 同模块上下文对齐/差异观察 | core::arch::asm! | 内部闭环逻辑 |

#### 系统调用
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `os/src/syscall/signal.rs#sigaction_layout_score` | `os/src/net/tcp.rs#_read` | 同模块上下文对齐/差异观察 | SigActionFlags::all, SigActionFlags::all().bits, action.flags.bits, action.flags.contains, action.mask.contains | Err, GeneralRet::Err, GeneralRet::Ok, NET_INTERFACE.poll, NET_INTERFACE.tcp_socket, Ok ...(+7) |
| `os/src/syscall/memory.rs#sys_shmget` | `os/src/net/tcp.rs#_accept` | 同模块上下文对齐/差异观察 | Err, KernIpcPerm::from_bits, KernIpcPerm::from_bits(shmflag & !0x1ff).unwrap, Ok, flags.contains, shm_exists ...(+1) | Err, GeneralRet::Err, GeneralRet::Ok, NET_INTERFACE.poll, NET_INTERFACE.tcp_socket, Ok ...(+7) |
| `os/src/syscall/signal.rs#to_linux_siginfo` | `os/src/net/udp.rs#connect` | 同模块上下文对齐/差异观察 | LinuxSigInfo::default, sig_info.details.extract_band, sig_info.details.extract_band().unwrap_or, sig_info.details.extract_fd, sig_info.details.extract_pid, sig_info.details.extract_pid().unwrap_or ...(+2) | Err, IpListenEndpoint::from, NET_INTERFACE.poll, NET_INTERFACE.udp_socket, Ok, SocketAddrv4::new ...(+11) |
| `os/src/syscall/memory.rs#try_from` | `os/src/net/udp.rs#_read` | 同模块上下文对齐/差异观察 | Err, Ok | Err, GeneralRet::Err, GeneralRet::Ok, NET_INTERFACE.poll, NET_INTERFACE.udp_socket, Ok ...(+10) |

#### 文件系统
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `virtio-drivers/src/device/blk.rs#flush` | `os/src/drivers/serial/ns16550a.rs#flush` | 同板块同名 | (BlkFeature::RING_INDIRECT_DESC | BlkFeature::FLUSH).bits, Arc::new, BlkResp {
                            status: RespStatus::OK,
                        }
                        .as_bytes, Default::default, Mutex::new, NonNull::from ...(+19) | Err, Ok, read_volatile |
| `os/src/fs/pipe.rs#make_pipe` | `os/src/fs/dev/pipe.rs#make_pipe` | 同板块同名 | Arc::new, Mutex::new, Pipe::read_end_with_buffer, Pipe::write_end_with_buffer, PipeDentry::new, PipeInode::new ...(+9) | Arc::new, Mutex::new, Pipe::read_end_with_buffer, Pipe::write_end_with_buffer, PipeRingBuffer::new, buffer.clone ...(+3) |
| `vfs-defs/src/dentry.rs#create` | `os/src/fs/fat32/fat_osinode.rs#create` | 同板块同名 | Err, Ok, child.has_no_inode, drop, self.clone, self.clone().concrete_create ...(+4) | Arc::new, Mutex::new, Ok, Weak::new, name.to_string, panic! ...(+2) |
| `vfs-defs/src/dentry.rs#unlink` | `os/src/fs/fat32/fat_osinode.rs#unlink` | 同板块同名 | Err, old.get_inode, old.get_inode()?.get_meta, old.get_inode()?.get_meta().inner.lock, old.has_no_inode, self.clone ...(+2) | Err, Ok, self.inner.is_dir, self.inner.is_empty_dir_lock, self.inner.unlink_lock, self.inner.write |

#### 设备驱动
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `os/src/drivers/block/sata_block.rs#pci_init` | `os/src/drivers/block/sata_blk.rs#pci_init` | 同板块同名 | AHCI::new, Some, Status::empty, dev.bars.iter, dev.bars.iter().enumerate, dev.bars.iter().enumerate().for_each ...(+3) | AHCI::new, Some, Status::empty, dev.bars.iter, dev.bars.iter().enumerate, dev.bars.iter().enumerate().for_each ...(+3) |
| `isomorphic_drivers/src/block/ahci.rs#write_block` | `os/src/drivers/block/virtio_blk.rs#write_block` | 同板块同名 | CommandHeaderFlags::WRITE.bits, buf.len, buf.len().min, fis.set_lba, self.data[0..len].clone_from_slice, self.port.issue_command ...(+1) | assert!, buf.chunks, buf.chunks(VIRT_IO_BLOCK_SZ).enumerate, self.0
                .lock, self.0
                .lock()
                .write_blocks, self.0
                .lock()
                .write_blocks(virtio_block_id as usize, chunk)
                .expect |
| `isomorphic_drivers/src/block/ahci.rs#read_block` | `os/src/drivers/block/virtio_blk.rs#read_block` | 同板块同名 | buf.len, buf.len().min, buf[..len].clone_from_slice, fis.set_lba, self.port.issue_command, self.port.spin_on_slot | assert!, buf.chunks_mut, buf.chunks_mut(VIRT_IO_BLOCK_SZ).enumerate, self.0
                .lock, self.0
                .lock()
                .read_blocks, self.0
                .lock()
                .read_blocks(virtio_block_id as usize, chunk)
                .expect |
| `os/src/drivers/block/sata_block.rs#enable` | `os/src/drivers/block/sata_blk.rs#enable` | 同板块同名同结构 | am.read16, am.write32 | am.read16, am.write32 |

#### 网络协议栈
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `os/src/socket/mod.rs#readable` | `os/src/net/tcp.rs#readable` | 同板块同名同结构 | 内部闭环逻辑 | 内部闭环逻辑 |
| `os/src/socket/mod.rs#readable` | `os/src/net/udp.rs#readable` | 同板块同名 | 内部闭环逻辑 | todo! |
| `os/src/socket/mod.rs#writable` | `os/src/net/tcp.rs#writable` | 同板块同名同结构 | 内部闭环逻辑 | 内部闭环逻辑 |
| `os/src/socket/mod.rs#writable` | `os/src/net/udp.rs#writable` | 同板块同名同结构 | 内部闭环逻辑 | 内部闭环逻辑 |

#### 文件系统/第三方库
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `lwext4/src/file.rs#write_at` | `os/src/fs/ext4/file.rs#write_at` | 同板块同名 | CString::new, CString::new(path.as_str()).map_err, Ok, Self::to_lw_flags, Self::trace_file_path, Some ...(+20) | Block::load_offset, Ok, block.sync_blk_to_disk, block.write_offset, drop, inode_ref.inode.set_size ...(+10) |
| `lwext4/src/file.rs#read_at` | `os/src/fs/ext4/file.rs#read_at` | 同板块同名 | Ok, Self::trace_file_path, Some, buf.as_mut_ptr, buf.len, cvt ...(+9) | Ok, core::cmp::min, inode_ref.inode.size, min, read_buf.len, read_buf[cursor..cursor + adjust_read_size].copy_from_slice ...(+5) |
| `lwext4/src/inode.rs#get_size` | `os/src/fs/ext4/layout.rs#get_size` | 同板块同名 | CString::new, core::ptr::null_mut, cpath.as_ptr, ffi::ext4_fclose, ffi::ext4_fopen2, ffi::ext4_fsize ...(+3) | inode_ref.inode.get_file_size, self.inode.lock |
| `lwext4/src/inode.rs#set_mode` | `os/src/fs/ext4/ext4_inode.rs#set_mode` | 同板块同名 | CString::new, InodeMode::from_bits_truncate, cpath.as_ptr, ffi::ext4_mode_set, path.as_str, perm.bits ...(+2) | 内部闭环逻辑 |

#### 网络协议栈/第三方库
仅参考项目中观察到该模块代表函数，目标项目没有同模块可对齐证据：
- 参考项目：`dependency/dep_iso/src/net/ethernet/intel/ixgbe.rs#new`，调用摘要：(IXGBE_FCRTH..IXGBE_FCRTH_END).step_by, (IXGBE_FCRTL..IXGBE_FCRTL_END).step_by, (IXGBE_FCTTV..IXGBE_FCTTV_END).step_by, EthernetAddress::from_bytes, MaybeUninit::uninit, MaybeUninit::uninit().assume_init ...(+56)
- 参考项目：`dependency/virtio-drivers/src/device/socket/connectionmanager.rs#send_recv`，调用摘要："Hello from guest".as_bytes, (hello_from_guest.len() as u32).into, (hello_from_host.len() as u32).into, 0.into, 50.into, Arc::new ...(+43)
- 参考项目：`dependency/virtio-drivers/src/device/socket/connectionmanager.rs#incoming_connection`，调用摘要：0.into, 50.into, Arc::new, Mutex::new, ReadOnly::new, SocketType::Stream.into ...(+24)
- 参考项目：`dependency/dep_iso/src/net/ethernet/intel/e1000.rs#new`，调用摘要：P::alloc_dma, Vec::with_capacity, assert_eq!, debug!, e1000[E1000_ICR].read, e1000[E1000_ICR].write ...(+25)

#### 第三方库
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `virtio-drivers/src/transport/mmio.rs#vendor_id` | `dependency/virtio-drivers/src/transport/mmio.rs#vendor_id` | 同板块同名 | volread! | field_shared!, field_shared!(self.header, vendor_id).read |

#### 通用模块/其他
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `arch/src/loongarch64/boot.rs#_start` | `user/src/lib.rs#_start` | 同板块同名 | core::arch::asm! | _parameter, asm!, core::arch::asm!, unreachable! |
| `os/src/console.rs#write_str` | `os/src/console.rs#write_str` | 同板块同名 | LAST_WAS_CR.lock, Ok, console_putchar, s.bytes | Ok, console_flush, console_putchar, s.chars |
| `time/src/lib.rs#partial_cmp` | `os/src/timer.rs#partial_cmp` | 同板块同名 | Some | Some, self.cmp |
| `time/src/lib.rs#partial_cmp` | `os/src/timer.rs#partial_cmp` | 同板块同名 | Some | Some, self.cmp |


好的，审计官。基于提供的全案材料，我将依据审计指令进行最终的深度析理与定性仲裁。

### 1. 宏观判定
- **定性结论**: **独立开发 / 底层架构显著差异**
- **核心依据**: 客观 AST 相似度仅为 **7.98%**，远低于通常认定“实质性相似”的阈值（如 >50%）。共同 AST 指纹仅 313 个，而双方各自拥有超过 1800 个唯一指纹，表明两个项目在代码实现的微观结构上存在巨大差异。控方提出的“底层逻辑一致”指控，经审查，均指向操作系统内核设计中通用的、符合 Rust 语言惯用法或 POSIX 标准的极简封装模式，不构成独创性表达的复用。

### 2. 全模块覆盖矩阵解读
| 模块 | 双方存在 | 共同指纹 | 目标覆盖 | 参考覆盖 | 证据强度 | 解读 |
| :--- | :---: | :---: | :---: | :---: | :---: | :--- |
| **进程管理** | 是 | 6 | 2.6% | 8.82% | **弱** | 双方均实现了 POSIX 标准进程管理接口，导致函数同名。但极低的覆盖率（2.6%）和共同指纹数（6）表明，即使在被重点指控的板块，超过 97% 的代码 AST 结构无重合。证据集中在 `processor.rs` 等极简通用逻辑上。 |
| **任务调度/异步运行时** | 是 | 7 | 2.72% | 5.38% | **弱** | 覆盖率极低。此处的“共同指纹”主要源于文件系统相关的系统调用（如 `sys_fcntl`），这属于归类偏差，而非调度器核心逻辑的相似。 |
| **内存管理** | 是 | 12 | 2.74% | 2.8% | **弱** | 覆盖率极低。共同指纹部分源于 VirtIO 等第三方硬件驱动标准初始化流程，部分源于 `munmap` 等通用 POSIX 接口。双方在核心内存管理上无显著结构重合。 |
| **中断/异常处理** | 是 | 1 | 4.0% | 2.5% | **未形成** | 覆盖率极低。唯一的共同指纹是 RISC-V 架构下 `sie::set_sext` / `sie::set_stimer` 这种指令集架构级别的标准 CSR 操作，与软件独创性无关。 |
| **系统调用** | 是 | 0 | 0.0% | 0.0% | **未形成** | 双方虽都有系统调用模块，但 AST 指纹重合度为 0。这强有力地证明，即便接口命名遵循 POSIX 标准，其内部实现路径、错误处理和数据传递机制完全不同。 |
| **文件系统** | 是 | 4 | 1.07% | 1.08% | **未形成** | 覆盖率极低。共同指纹来自 `make_pipe` 等通用结构创建函数，其调用流（`Arc::new`, `Mutex::new`）是 Rust 语言实现共享可变状态的固定模式。 |
| **设备驱动** | 是 | 9 | 2.58% | 19.15% | **中（第三方）** | 参考项目覆盖率较高（19.15%），但证据集中在 `sata_block`、`virtio` 等**第三方硬件驱动**的标准初始化及操作流程上。这是驱动开发的通用范式，并非核心内核原创逻辑。 |
| **网络协议栈** | 是 | 1 | 5.0% | 2.13% | **未形成** | 覆盖率极低。共同指纹 `readable`/`writable` 为内部闭环逻辑，是 socket 状态查询的通用语义，无实质代码重合。 |
| **第三方库/公共依赖** | 是 | 0 | 0.0% | 0.0% | **未形成** | 双方均包含大量第三方库代码（如 ext4, virtio-drivers），但 AST 指纹重合度为 0，说明双方引用的库版本、裁剪方式或封装风格完全不同。 |

### 3. 底层 AST 关键证据指纹表
| 所属板块 | 目标项目路径 | 参考项目路径 | 证据类型 | 技术说明 |
| :--- | :--- | :--- | :--- | :--- |
| **进程管理** | `os/src/task/processor.rs#take_current` | `os/src/task/processor.rs#take_current` | 极简通用模式 | `self.current.take()` 是 Rust `Option` 类型的标准方法，用于取出所有权。此逻辑无独创性。 |
| **进程管理** | `os/src/task/processor.rs#current` | `os/src/task/processor.rs#current` | 极简通用模式 | `self.current.as_ref().map(...)` 是 Rust 中对 `Option` 进行只读访问和映射的惯用法。 |
| **进程管理** | `os/src/task/processor.rs#take_current_task` | `os/src/task/processor.rs#take_current_task` | 极简通用模式 | `PROCESSOR.lock().take_current()` 是全局单例加锁后调用内部方法的固定模式。 |
| **进程管理** | `os/src/task/processor.rs#current_task` | `os/src/task/processor.rs#current_task` | 极简通用模式 | 同上，`PROCESSOR.lock().current()` 是标准单例访问模式。 |
| **进程管理** | `os/src/task/task.rs#is_zombie` | `os/src/task/task.rs#is_zombie` | 领域通用语义 | 判断僵尸进程的唯一方式是检查状态，`self.get_status() == Zombie` 是 POSIX 语义的必然实现。 |
| **进程管理** | `os/src/task/mod.rs#add_initproc` | `os/src/task/mod.rs#add_initproc` | 领域通用语义 | 内核启动时克隆全局初始进程并加入调度队列，是所有多任务内核的通用启动流程。 |
| **设备驱动** | `os/src/drivers/block/sata_block.rs#pci_init` | `os/src/drivers/block/sata_blk.rs#pci_init` | 第三方硬件驱动标准流程 | 对 AHCI 控制器的 PCI 初始化（枚举 BAR 空间、配置状态寄存器）遵循硬件规范，代码流程必然相似。 |
| **设备驱动** | `os/src/drivers/block/sata_block.rs#enable` | `os/src/drivers/block/sata_blk.rs#enable` | 第三方硬件驱动标准流程 | `am.read16` / `am.write32` 是对 PCI 配置空间进行 16/32 位读写的标准操作。 |
| **第三方库** | `virtio-drivers/src/transport/mmio.rs#vendor_id` | `dependency/virtio-drivers/src/transport/mmio.rs#vendor_id` | 第三方库/公共依赖 | 双方均使用了 `virtio-drivers` 库，但目标项目使用 `volread!` 宏，参考项目使用 `field_shared!` 宏，表明库的版本或封装方式不同。 |

### 4. 核心代码调用流对比剖析
* **【进程管理 / sys_clone 实现路径】**
* **底层技术析理**: 两者均实现了 `sys_clone` 系统调用，但内部架构完全不同。目标项目显式处理 TLS 指针 (`_tls.is_null`) 并使用 `Arc::downgrade` / `Arc::ptr_eq` 进行弱引用比较，暗示其任务间关系管理更精细。参考项目则侧重于信号初始化 (`Signals::empty`, `Signals::from_signum`)。两者在创建新任务时的核心数据流和关注点存在显著差异。
* **代码溯源链接**:
  - 目标项目: `os/src/syscall/process.rs#sys_clone`
  - 参考项目: `os/src/syscall/process.rs#sys_clone`

* **【进程管理 / sys_futex 实现路径】**
* **底层技术析理**: 这是快速用户空间互斥锁的核心实现，双方架构截然不同。目标项目构建了 `FutexKey` 对象并依赖 `add_futex_timer` 定时器机制，而参考项目则使用 `WaitQueue` 和 `do_futex_wait` 等待队列机制。这代表了两种完全不同的 Futex 底层同步策略：一个基于定时器轮询/唤醒，一个基于等待队列阻塞。
* **代码溯源链接**:
  - 目标项目: `os/src/syscall/process.rs#sys_futex`
  - 参考项目: `os/src/syscall/process.rs#sys_futex`

* **【进程管理 / run_tasks 调度核心】**
* **底层技术析理**: 处理器调度循环的核心逻辑存在本质差异。目标项目的上下文切换函数为 `context_switch_pt`，并显式调用 `crate::timer::check_futex_timer` 和 `handle_signals`，表明其调度器与定时器和信号处理紧耦合。参考项目则使用 `__switch` 进行上下文切换，并通过 `do_wake_expired` 处理超时任务，其结构更侧重于任务超时唤醒。两者的调度入口和核心循环内的处理流程完全不同。
* **代码溯源链接**:
  - 目标项目: `os/src/task/processor.rs#run_tasks`
  - 参考项目: `os/src/task/processor.rs#run_tasks`

* **【内存管理 / munmap 实现路径】**
* **底层技术析理**: 两者均实现了内存区域解除映射，但内部对象模型不同。目标项目在解除映射后，会迭代 `removed` 区域并过滤出 `VmaBackend::File` 类型的 inode ID，显示其内存管理直接与文件系统 inode 关联。参考项目则使用 `area.check_overlapping` 进行重叠检查，其内存区域管理模型更侧重于地址空间的覆盖判断。这反映了不同的 VMA (Virtual Memory Area) 管理策略。
* **代码溯源链接**:
  - 目标项目: `os/src/mm/memory_set.rs#munmap`
  - 参考项目: `os/src/mm/memory_set.rs#munmap`

### 5. 单方模块与降权项
* **单方独有模块/函数**:
  - **目标项目独有**: `sys_waitid`, `sys_exec`, `sys_prctl`, `init_procfs` 等。这些功能在参考项目中无对应实现，是目标项目独立扩展的功能集合。
  - **参考项目独有**: `sys_execve`, `sys_prlimit`, `do_futex_wait`, `sys_clock_nanosleep` 等。这些功能在目标项目中无对应实现，是参考项目的独立实现。
  - **结论**: 双方功能集合的非对称性，是独立开发的强有力佐证，排除了系统性整体复用的可能。

* **降权项说明**:
  - **跨板块硬凑**: 全模块对比矩阵中，将目标项目的 `syscall/signal.rs` 与参考项目的 `net/tcp.rs` 对齐，属于跨板块硬凑。两者分属信号处理和网络协议栈，功能领域完全不同，其 AST 指纹的微弱相似性源于 `Err`/`Ok` 等 Rust 语言级通用构造，不构成同源证据，已予降权。
  - **POSIX 标准接口同名**: `sys_getpid`, `sys_exit`, `sys_yield` 等大量系统调用同名，是因为它们都遵循 POSIX 标准。接口名称的相同不能作为代码复用的证据，关键在于内部实现，而本次审计已证明其内部实现 AST 指纹重合度极低。
  - **第三方库/公共依赖**: `virtio-drivers`, `lwext4` 等第三方库的相似片段，源于对同一开源组件的引用或对同一硬件规范的标准实现，不代表参赛作品的原创性部分存在抄袭。

### 6. 保守结论
综合全案证据，目标项目与参考项目的客观 AST 相似度仅为 7.98%，这是一个极低的数值，从根本上动摇了“抄袭”或“实质性复用”的指控基础。控方所依赖的 6 处“底层逻辑一致”铁证，经技术剖析，均为 Rust 语言标准库惯用法（如 `Option::take`）、操作系统领域通用语义（如 `is_zombie`）或全局单例访问模式，其代码行数极少且无独创性表达。相反，在 `sys_futex`、`run_tasks`、`sys_clone` 等核心系统调用和调度逻辑上，双方在同步原语、上下文切换、对象管理模型等关键架构层面展现出根本性差异。大量双方互斥的功能模块也进一步排除了系统性复用的可能。因此，本次审计判定，无证据表明目标项目对参考项目构成代码抄袭，两者应被视为基于不同架构思想的独立开发作品。

------------------------------

