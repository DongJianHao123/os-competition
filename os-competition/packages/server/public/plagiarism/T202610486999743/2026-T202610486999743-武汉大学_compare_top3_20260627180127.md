# 🛡️ OS 内核赛自动化代码级架构审计报告

**审计时间**: `2026-06-27 18:01:27`
**参赛作品名称**: `2026-T202610486999743-武汉大学`
**年份**: `2026`
**学校**: `武汉大学`
**队伍名称**: `T202610486999743`
**比赛名字**: `OS-Competition`
**比赛子赛道**: `Kernel-Implementation`
**仓库地址**: `https://gitlab.eduxiji.net/educg-group-43501-3132633/T202610486999743-3107`
**推测开发语言**: `Rust / C (由AST探针自动识别)`
**特征函数量**: `3416`

## ⚔️ 底层代码同源性对比 (AST Homology)
> 本部分由专门的代码审查智能体，对该项目与符合年份条件的历届最高相似度项目进行控制流与底层调用对比。新版报告会先给出确定性的全模块覆盖矩阵，再给出 Agent 的分模块解释，避免只围绕少量抽样函数展开。

### 🏆 Top 3 底层对标: vs `2024-undefinedType-南京航空航天大学`（南京航空航天大学 / undefinedType）

#### 对方作品信息

**对方年份**: `2024`
**对方学校**: `南京航空航天大学`
**对方队伍名称**: `undefinedType`
**对方比赛名字**: `OS-Competition`
**对方比赛子赛道**: `Kernel-Implementation`
**对方仓库地址**: `https://gitlab.eduxiji.net/educg-group-26010-2376550/T202410287992755-2176`

> 双向覆盖率：共同 AST 指纹: 382；唯一 AST 指纹: 目标项目 2815，参考项目 398；覆盖率: 目标项目 13.57%，参考项目 95.98%

## 全模块对比总览（确定性证据）

> 总体覆盖率：共同 AST 指纹: 382；唯一 AST 指纹: 目标项目 2815，参考项目 398；覆盖率: 目标项目 13.57%，参考项目 95.98%

### 1. 模块覆盖矩阵
| 模块 | 目标函数数 | 参考函数数 | 共同唯一 AST 指纹 | Target 覆盖 | Base 覆盖 | 状态 |
| :--- | ---: | ---: | ---: | ---: | ---: | :--- |
| 进程管理 | 275 | 0 | 0 | 0.0% | 0.0% | 仅目标项目体现 |
| 任务调度/异步运行时 | 277 | 0 | 0 | 0.0% | 0.0% | 仅目标项目体现 |
| 内存管理 | 733 | 0 | 0 | 0.0% | 0.0% | 仅目标项目体现 |
| 中断/异常处理 | 124 | 0 | 0 | 0.0% | 0.0% | 仅目标项目体现 |
| 系统调用 | 194 | 0 | 0 | 0.0% | 0.0% | 仅目标项目体现 |
| 文件系统 | 838 | 0 | 0 | 0.0% | 0.0% | 仅目标项目体现 |
| 设备驱动 | 32 | 0 | 0 | 0.0% | 0.0% | 仅目标项目体现 |
| 网络协议栈 | 56 | 0 | 0 | 0.0% | 0.0% | 仅目标项目体现 |
| 文件系统/第三方库 | 719 | 478 | 382 | 62.21% | 95.98% | 双方存在且有结构重合 |
| 通用模块/其他 | 168 | 0 | 0 | 0.0% | 0.0% | 仅目标项目体现 |

### 2. 分模块证据清单

#### 进程管理
仅目标项目中观察到该模块代表函数，参考项目没有同模块可对齐证据：
- 目标项目：`os/src/syscall/process.rs#sys_clone`，调用摘要：Arc::downgrade, Arc::new, Arc::ptr_eq, CLONE_CALLS.fetch_add, Some, add_task ...(+38)
- 目标项目：`os/src/syscall/process.rs#sys_waitid`，调用摘要：Err, Ok, Some, WaitIdSigInfo::default, child.inner_exclusive_access, child_inner.waited_cputime_us ...(+29)
- 目标项目：`os/src/syscall/process.rs#sys_futex`，调用摘要：(ts.tv_sec as usize).saturating_mul, FUTEX_INTR_LOGS.fetch_add, FUTEX_STATE.exclusive_access, Some, core::cmp::min, crate::timer::get_time_us ...(+26)
- 目标项目：`os/src/syscall/process.rs#sys_kcmp`，调用摘要：(0..KCMP_TYPES).contains, Arc::ptr_eq, Err, find_task_by_pid, inner.fd_table.len, inner.fd_table[fd].as_ref ...(+20)

#### 任务调度/异步运行时
仅目标项目中观察到该模块代表函数，参考项目没有同模块可对齐证据：
- 目标项目：`os/src/syscall/key.rs#sys_keyctl`，调用摘要："keyring".to_string, KEY_STATE.exclusive_access, Some, Vec::new, arg3.saturating_mul, clear_keyring ...(+51)
- 目标项目：`os/src/syscall/fs.rs#sys_fcntl`，调用摘要：OpenFlags::PATH.bits, Some, core::cmp::min, crate::mission::RLimit::new, current_task, current_task().unwrap ...(+63)
- 目标项目：`os/src/syscall/fs.rs#sys_write`，调用摘要：OpenFlags::APPEND.bits, OpenFlags::NONBLOCK.bits, UserBuffer::new, cgroup_maybe_charge_file_write, copy_from_user_retry, copy_from_user_retry(&mut byte, buf).is_err ...(+54)
- 目标项目：`os/src/syscall/fs.rs#sys_mount`，调用摘要：Some, apply_mount_propagation_change, bind_result_propagation_state, clear_mount_info, container_root.as_str, current_has_capability ...(+45)

#### 内存管理
仅目标项目中观察到该模块代表函数，参考项目没有同模块可对齐证据：
- 目标项目：`os/src/syscall/mod.rs#syscall`，调用摘要：note_last_syscall, println!, sys_accept, sys_accept4, sys_add_key, sys_adjtimex ...(+246)
- 目标项目：`os/src/syscall/fs.rs#sys_openat`，调用摘要：OpenFlags::from_bits_truncate, Some, abs_path.as_str, abs_path.clone, bump_path_topology_epoch, busybox_applet_fallback_path ...(+92)
- 目标项目：`os/src/syscall/fs.rs#open_virtual_compat`，调用摘要：Arc::new, CgroupFile::new, MemFile::new, ProcKpageflagsFile::new, ProcNetTagFile::new, ProcPagemapFile::new ...(+55)
- 目标项目：`os/src/syscall/process.rs#do_exec_common`，调用摘要："busybox".to_string, "sh".to_string, Arc::new, EXEC_LOAD_SYNC.exclusive_access, Vec::new, all_data.as_slice ...(+110)

#### 中断/异常处理
仅目标项目中观察到该模块代表函数，参考项目没有同模块可对齐证据：
- 目标项目：`os/src/syscall/fs.rs#sys_ioctl`，调用摘要：backing.as_str, fd_to_file, fd_to_file(arg).and_then, fd_to_path, file.is_path_only, file.path ...(+38)
- 目标项目：`os/src/trap/mod.rs#dump_syscall_profile_stats`，调用摘要：SYSCALL_PROF_COUNT.load, println!
- 目标项目：`hal/src/component/trap/loongarch64.rs#la_tlb_refill_failed`，调用摘要：decode_estat, panic!, read_estat, read_pgdl, read_tlbrbadv, walk_low_half_root
- 目标项目：`hal/src/component/trap/riscv64.rs#trap_return`，调用摘要：Self::set_user_trap_entry, Signal::trampoline_addr, asm!

#### 系统调用
仅目标项目中观察到该模块代表函数，参考项目没有同模块可对齐证据：
- 目标项目：`os/src/syscall/bpf.rs#run_program`，调用摘要：alu32, alu64, class, insn.dst, insn.src, map_lookup_addr ...(+13)
- 目标项目：`os/src/syscall/ipc.rs#sys_msgctl`，调用摘要：buf.is_null, current_effective_ids, current_supplementary_gids, read_user_pod_retry, supplementary.as_slice, sysv::msg_info ...(+10)
- 目标项目：`os/src/syscall/time.rs#sys_timerfd_settime`，调用摘要：TIMERFD_STATE.exclusive_access, clock_gettime_us, core::cmp::max, current_mm_activation, current_mm_activation().user_token, new_value.is_null ...(+9)
- 目标项目：`os/src/syscall/net.rs#parse_sockaddr`，调用摘要：Err, Ok, SockAddr::Inet, addr.is_null, core::mem::size_of::<SockAddrIn6>, core::mem::size_of::<SockAddrIn> ...(+7)

#### 文件系统
仅目标项目中观察到该模块代表函数，参考项目没有同模块可对齐证据：
- 目标项目：`os/src/syscall/fs.rs#fstatat_impl`，调用摘要：Err, Ok, StatMode::FILE.bits, StatMode::from_bits_truncate, abs.as_str, apply_path_times ...(+39)
- 目标项目：`os/src/syscall/fs.rs#quotactl_do_legacy`，调用摘要：IfDqInfo::default, QUOTA_STATES.exclusive_access, Some, current_has_capability, fs.legacy[qtype].reset, key.to_string ...(+26)
- 目标项目：`os/src/syscall/bpf.rs#verify_program`，调用摘要：(insn.off as usize).saturating_add, Err, Ok, Some, VReg::scalar_const, VReg::scalar_unknown ...(+19)
- 目标项目：`os/src/syscall/fs.rs#quotactl_do_xfs`，调用摘要：FsDiskQuota::default, FsQuotaState::new, QUOTA_STATES.exclusive_access, current_has_capability, fs
                .map, fs
                .map(xfs_quota_stat)
                .unwrap_or_else ...(+30)

#### 设备驱动
仅目标项目中观察到该模块代表函数，参考项目没有同模块可对齐证据：
- 目标项目：`os/src/net/mod.rs#unregister`，调用摘要：Arc::as_ptr, self
                    .stream_map
                    .get, self
                    .stream_map
                    .get(&addr)
                    .and_then, self
                    .stream_map
                    .get(&addr)
                    .and_then(|w| w.upgrade())
                    .map, self
                    .stream_map
                    .get(&addr)
                    .and_then(|w| w.upgrade())
                    .map(|x| Arc::as_ptr(&x) as usize == ptr)
                    .unwrap_or, self
            .listeners
            .get ...(+12)
- 目标项目：`hal/src/component/virtio/loongarch64.rs#pci_assign_bars`，调用摘要：(size as u64).next_power_of_two, align_up, root.bars, root.bars(devfn).unwrap, root.get_status_command, root.set_bar_32 ...(+2)
- 目标项目：`os/src/drivers/block/virtio_blk.rs#read_blocks`，调用摘要：assert!, buf.chunks_mut, buf.chunks_mut(VIRTIO_SHARED_BUFFER_LIMIT).enumerate, buf.len, driver.read_blocks, driver.read_blocks(chunk_block, chunk).unwrap_or_else ...(+3)
- 目标项目：`os/src/drivers/block/virtio_blk.rs#write_blocks`，调用摘要：assert!, buf.chunks, buf.chunks(VIRTIO_SHARED_BUFFER_LIMIT).enumerate, buf.len, driver.write_blocks, driver.write_blocks(chunk_block, chunk).unwrap_or_else ...(+3)

#### 网络协议栈
仅目标项目中观察到该模块代表函数，参考项目没有同模块可对齐证据：
- 目标项目：`os/src/net/mod.rs#disconnect`，调用摘要：NET_MANAGER.exclusive_access, NET_MANAGER.exclusive_access().unregister, inner.acceptq.clear, inner.local.is_some, inner.local.take, inner.peer.take ...(+5)
- 目标项目：`os/src/net/mod.rs#try_push_from_peer_vec`，调用摘要：Err, Ok, STREAM_BACKPRESSURE_LOGS.fetch_add, data.len, inner.recvq.push_back, inner.recvq_bytes.saturating_add ...(+2)
- 目标项目：`os/src/net/mod.rs#route_request`，调用摘要：build_netlink_ack, build_netlink_addr_dump, build_netlink_crypto_dump, build_netlink_link_dump, core::mem::size_of::<NetlinkHeader>, core::ptr::read_unaligned ...(+6)
- 目标项目：`os/src/net/mod.rs#build_netlink_link_dump`，调用摘要：Vec::new, align4, append_netlink_done, append_pod, attr_payload.len, core::mem::size_of::<IfInfoMsg> ...(+5)

#### 文件系统/第三方库
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `third_party/ext4fs/c/lwext4/src/ext4_balloc.c#ext4_balloc_alloc_block` | `Kernel/File/lwext4/ext4_balloc.c#ext4_balloc_alloc_block` | 同板块同名同结构 | ext4_balloc_get_bgid_of_block, ext4_balloc_get_block_of_bgid, ext4_balloc_set_bitmap_csum, ext4_balloc_verify_bitmap_csum, ext4_bg_get_block_bitmap, ext4_bg_get_free_blocks_count ...(+19) | EXT4_Debug_u32, ext4_balloc_get_bgid_of_block, ext4_balloc_get_block_of_bgid, ext4_balloc_set_bitmap_csum, ext4_balloc_verify_bitmap_csum, ext4_bg_get_block_bitmap ...(+19) |
| `third_party/ext4fs/c/lwext4/src/ext4_journal.c#jbd_journal_prepare` | `Kernel/File/lwext4/ext4_journal.c#jbd_journal_prepare` | 同板块同名同结构 | EXT4_BLOCK_ZERO, RB_FIND, TAILQ_FOREACH_REVERSE_SAFE, TAILQ_FOREACH_SAFE, TAILQ_REMOVE, ext4_bcache_clear_dirty ...(+17) | EXT4_BLOCK_ZERO, RB_FIND, TAILQ_FOREACH_REVERSE_SAFE, TAILQ_FOREACH_SAFE, TAILQ_REMOVE, ext4_bcache_clear_dirty ...(+17) |
| `third_party/ext4fs/c/lwext4/src/ext4.c#ext4_dir_rm` | `Kernel/File/lwext4/ext4.c#ext4_dir_rm` | 同板块同名同结构 | EXT4_MP_LOCK, EXT4_MP_UNLOCK, ext4_block_cache_write_back, ext4_dir_en_get_inode, ext4_dir_iterator_fini, ext4_dir_iterator_init ...(+19) | EXT4_MP_LOCK, EXT4_MP_UNLOCK, ext4_block_cache_write_back, ext4_dir_en_get_inode, ext4_dir_iterator_fini, ext4_dir_iterator_init ...(+19) |
| `third_party/ext4fs/c/lwext4/src/ext4_dir_idx.c#ext4_dir_dx_split_index` | `Kernel/File/lwext4/ext4_dir_idx.c#ext4_dir_dx_split_index` | 同板块同名同结构 | ext4_block_set, ext4_dir_dx_climit_get_count, ext4_dir_dx_climit_get_limit, ext4_dir_dx_climit_set_count, ext4_dir_dx_climit_set_limit, ext4_dir_dx_entry_get_hash ...(+10) | ext4_block_set, ext4_dir_dx_climit_get_count, ext4_dir_dx_climit_get_limit, ext4_dir_dx_climit_set_count, ext4_dir_dx_climit_set_limit, ext4_dir_dx_entry_get_hash ...(+10) |

#### 通用模块/其他
仅目标项目中观察到该模块代表函数，参考项目没有同模块可对齐证据：
- 目标项目：`os/src/ipc/sysv.rs#shmget`，调用摘要：Err, Ok, SHM_MANAGER.exclusive_access, ShmGetResult::Created, ShmGetResult::Existing, Some ...(+8)
- 目标项目：`os/src/ipc/sysv.rs#msg_info`，调用摘要：MSG_MANAGER.exclusive_access, SYSV_TUNABLES.exclusive_access, clamp_i32, mgr
        .by_id
        .keys, mgr
        .by_id
        .keys()
        .next_back, mgr
        .by_id
        .keys()
        .next_back()
        .copied ...(+9)
- 目标项目：`os/src/ipc/sysv.rs#queue_select_index`，调用摘要：(!queue.messages.is_empty()).then_some, (idx < queue.messages.len()).then_some, Some, queue.messages.is_empty, queue.messages.iter, queue.messages.iter().enumerate ...(+2)
- 目标项目：`os/src/ipc/sysv.rs#stat`，调用摘要：self.key.unwrap_or, self.messages.len


好的，审判长。作为本次代码同源性审计的最终仲裁者，我已全面审阅了控辩双方的陈述、客观AST相似度数据、全模块覆盖矩阵以及底层证据底座。现依据“深度析理模式”的审计指令，出具如下最终定性报告。

---

### 1. 宏观判定
- **定性结论**: **独立开发，底层架构显著差异**
  客观AST相似度仅为13.49%，且重合部分完全限定于双方共同引入的开源第三方库`lwext4`。目标项目在进程管理、内存管理、系统调用、设备驱动、网络协议栈等核心模块拥有2815个唯一AST指纹，与参考项目无任何交叉。参考项目95.98%的AST指纹与目标项目的`lwext4`组件重合，表明其本身高度依赖该库，而非被抄袭的原创源。控方提出的“复用指控”混淆了“共同使用开源上游”与“项目间代码抄袭”的本质区别，其证据均为`lwext4`库的标准实现，不构成对参考项目的抄袭。

### 2. 全模块覆盖矩阵解读
- **进程管理**: 仅目标项目存在（275个函数），参考项目无此模块。无共同AST指纹。目标项目覆盖率0%，参考项目覆盖率0%。证据强度：**未形成**。
- **任务调度/异步运行时**: 仅目标项目存在（277个函数），参考项目无此模块。无共同AST指纹。目标项目覆盖率0%，参考项目覆盖率0%。证据强度：**未形成**。
- **内存管理**: 仅目标项目存在（733个函数），参考项目无此模块。无共同AST指纹。目标项目覆盖率0%，参考项目覆盖率0%。证据强度：**未形成**。
- **中断/异常处理**: 仅目标项目存在（124个函数），参考项目无此模块。无共同AST指纹。目标项目覆盖率0%，参考项目覆盖率0%。证据强度：**未形成**。
- **系统调用**: 仅目标项目存在（194个函数），参考项目无此模块。无共同AST指纹。目标项目覆盖率0%，参考项目覆盖率0%。证据强度：**未形成**。
- **文件系统**: 仅目标项目存在（838个函数），参考项目无此模块。无共同AST指纹。目标项目覆盖率0%，参考项目覆盖率0%。证据强度：**未形成**。
- **设备驱动**: 仅目标项目存在（32个函数），参考项目无此模块。无共同AST指纹。目标项目覆盖率0%，参考项目覆盖率0%。证据强度：**未形成**。
- **网络协议栈**: 仅目标项目存在（56个函数），参考项目无此模块。无共同AST指纹。目标项目覆盖率0%，参考项目覆盖率0%。证据强度：**未形成**。
- **第三方库/公共依赖**: 双方均存在。共同AST指纹382个，全部集中于此。目标项目覆盖率62.21%，参考项目覆盖率95.98%。该模块是双方唯一的交集，但重合源于共同引入的开源库`lwext4`。证据强度：**弱**，不构成项目间抄袭。

### 3. 底层 AST 关键证据指纹表
| 所属板块 | 目标项目路径 | 参考项目路径 | 证据类型 | 技术说明 |
| :--- | :--- | :--- | :--- | :--- |
| 第三方库/公共依赖 | `third_party/ext4fs/c/lwext4/src/ext4_balloc.c#ext4_balloc_alloc_block` | `Kernel/File/lwext4/ext4_balloc.c#ext4_balloc_alloc_block` | 同板块同名同结构 | 均为`lwext4`开源库的标准块分配函数，调用链由ext4规范决定。 |
| 第三方库/公共依赖 | `third_party/ext4fs/c/lwext4/src/ext4_journal.c#jbd_journal_prepare` | `Kernel/File/lwext4/ext4_journal.c#jbd_journal_prepare` | 同板块底层逻辑一致 | 均为`lwext4`的JBD2日志准备逻辑，使用相同的红黑树/队列宏。 |
| 第三方库/公共依赖 | `third_party/ext4fs/c/lwext4/src/ext4_dir_idx.c#ext4_dir_dx_split_index` | `Kernel/File/lwext4/ext4_dir_idx.c#ext4_dir_dx_split_index` | 同板块底层逻辑一致 | 均为`lwext4`的HTree索引分裂实现，操作`climit`、`entry`等标准结构。 |
| 第三方库/公共依赖 | `third_party/ext4fs/c/lwext4/src/ext4_extent.c#ext4_ext_split_node` | `Kernel/File/lwext4/ext4_extent.c#ext4_ext_split_node` | 同板块底层逻辑一致 | 均为`lwext4`的extent树节点分裂逻辑，使用`EXT_FIRST_EXTENT`等标准宏。 |
| 第三方库/公共依赖 | `third_party/ext4fs/c/lwext4/src/ext4_xattr.c#ext4_xattr_set_entry` | `Kernel/File/lwext4/ext4_xattr.c#ext4_xattr_set_entry` | 同板块底层逻辑一致 | 均为`lwext4`的扩展属性设置函数，操作`EXT4_XATTR_*`标准宏和`memmove`。 |
| 第三方库/公共依赖 | `third_party/ext4fs/c/lwext4/src/ext4_mkfs.c#fill_sb` | `Kernel/File/lwext4/ext4_mkfs.c#fill_sb` | 同板块底层逻辑一致 | 均为`lwext4`的超级块填充逻辑，调用`to_le16`/`to_le32`等字节序转换。 |
| 第三方库/公共依赖 | `third_party/ext4fs/c/lwext4/src/ext4_ialloc.c#ext4_ialloc_alloc_inode` | `Kernel/File/lwext4/ext4_ialloc.c#ext4_ialloc_alloc_inode` | 同板块同名同结构 | 均为`lwext4`的inode分配函数，操作块组描述符和inode位图。 |
| 第三方库/公共依赖 | `third_party/ext4fs/c/lwext4/src/ext4_fs.c#ext4_fs_truncate_inode` | `Kernel/File/lwext4/ext4_fs.c#ext4_fs_truncate_inode` | 同板块底层逻辑一致 | 均为`lwext4`的inode截断逻辑，调用`ext4_extent_remove_space`等标准流程。 |

### 4. 核心代码调用流对比剖析
* **【第三方库/公共依赖 - ext4 块分配路径】**
* **底层技术析理**: 双方在`ext4_balloc_alloc_block`函数中的调用流完全一致，均遵循“校验位图校验和 -> 查找空闲位 -> 设置位 -> 更新校验和 -> 更新块组描述符”的流程。这是`lwext4`库实现ext4块分配的标准路径，由ext4磁盘格式规范强制决定，无独立创新空间。目标项目移除了参考项目中存在的`EXT4_Debug_u32`宏调用，表明其进行了独立的日志/调试裁剪。
* **代码溯源链接**:
  - 目标项目: `third_party/ext4fs/c/lwext4/src/ext4_balloc.c#ext4_balloc_alloc_block`
  - 参考项目: `Kernel/File/lwext4/ext4_balloc.c#ext4_balloc_alloc_block`

* **【第三方库/公共依赖 - ext4 目录索引查找路径】**
* **底层技术析理**: 双方在`ext4_dir_dx_find_entry`函数中均采用“校验目录校验和 -> 获取哈希树叶子块 -> 在块内线性查找”的调用链。这是`lwext4`库实现HTree目录查找的标准逻辑。目标项目使用`ext4_dbg`作为调试宏，而参考项目使用`EXT4_Debug_u32_u32`，这一差异是双方独立集成或配置裁剪的明确指纹，反驳了“洗稿式抄袭”的指控。
* **代码溯源链接**:
  - 目标项目: `third_party/ext4fs/c/lwext4/src/ext4_dir_idx.c#ext4_dir_dx_find_entry`
  - 参考项目: `Kernel/File/lwext4/ext4_dir_idx.c#ext4_dir_dx_find_entry`

* **【进程管理 - 系统调用 `sys_clone`】**
* **底层技术析理**: 此模块仅存在于目标项目。目标项目在`sys_clone`中使用了Rust语言的`Arc::new`、`Arc::downgrade`、`Arc::ptr_eq`进行任务控制块的共享所有权管理，并通过`CLONE_CALLS.fetch_add`进行原子计数。参考项目无任何可对齐的进程管理实现。这体现了目标项目在核心内核对象模型上的独立设计与实现。
* **代码溯源链接**:
  - 目标项目: `os/src/syscall/process.rs#sys_clone`
  - 参考项目: 无

* **【内存管理 - 系统调用 `sys_openat`】**
* **底层技术析理**: 此模块仅存在于目标项目。目标项目在`sys_openat`中构建了复杂的路径解析逻辑，涉及`OpenFlags::from_bits_truncate`、`bump_path_topology_epoch`、`busybox_applet_fallback_path`等自定义函数，展示了其独立的VFS和路径管理抽象层。参考项目无此实现。
* **代码溯源链接**:
  - 目标项目: `os/src/syscall/fs.rs#sys_openat`
  - 参考项目: 无

### 5. 单方模块与降权项
- **单方模块**: 进程管理、任务调度/异步运行时、内存管理、中断/异常处理、系统调用、文件系统、设备驱动、网络协议栈等模块**仅存在于目标项目**。这些模块构成了一个完整操作系统内核的主体，是目标项目独立开发的核心成果，与参考项目无任何关联。
- **降权项**: 所有382个共同AST指纹均属于**第三方库/公共依赖** `lwext4`。该库是公开可获取的开源软件，其函数名、宏定义、数据结构、调用逻辑均由上游项目和ext4规范定义。因此，这些证据被降权为“共同使用开源上游”，不能作为“项目间抄袭”的强同源证据。控方指控的“宏替换”实际上是目标项目独立裁剪调试日志的体现，反而证明了其非直接复制。

### 6. 保守结论
本次审计的核心发现是：13.49%的AST相似度完全由双方共同引入的开源第三方库`lwext4`贡献。目标项目是一个包含进程管理、内存管理、网络协议栈等完整子系统的Rust内核，其2815个唯一AST指纹与参考项目无任何重叠。参考项目本身则高度依赖`lwext4`，其95.98%的指纹均与此库重合。控方提出的所有“复用铁证”均局限于`lwext4`库内部的标准实现，无法证明存在跨项目抄袭。综合判定，双方在操作系统内核的顶层架构、核心实现和语言生态上存在根本性差异，现有证据远不足以支持抄袭或违规复用的指控。整体同源性风险极低。

------------------------------

