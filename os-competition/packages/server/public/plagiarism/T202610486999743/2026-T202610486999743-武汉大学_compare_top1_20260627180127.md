# 🛡️ OS 内核赛自动化代码级架构审计报告

**审计时间**: `2026-06-27 18:01:27`
**参赛作品名称**: `2026-T202610486999743-武汉大学`
**年份**: `2026`
**学校**: `武汉大学`
**队伍名称**: `T202610486999743`
**比赛名字**: `OS-Competition`
**比赛子赛道**: `Kernel-Implementation`
**仓库地址**: `https://gitlab.eduxiji.net/educg-group-43501-3132633/T202610486999743-3107`
**推测开发语言**: `Rust / C (由AST探针自动识别)`
**特征函数量**: `3416`

## ⚔️ 底层代码同源性对比 (AST Homology)
> 本部分由专门的代码审查智能体，对该项目与符合年份条件的历届最高相似度项目进行控制流与底层调用对比。新版报告会先给出确定性的全模块覆盖矩阵，再给出 Agent 的分模块解释，避免只围绕少量抽样函数展开。

### 🏆 Top 1 底层对标: vs `2024-RustTrustHuster-华中科技大学`（华中科技大学 / RustTrustHuster）

#### 对方作品信息

**对方年份**: `2024`
**对方学校**: `华中科技大学`
**对方队伍名称**: `RustTrustHuster`
**对方比赛名字**: `OS-Competition`
**对方比赛子赛道**: `Kernel-Implementation`
**对方仓库地址**: `https://gitlab.eduxiji.net/educg-group-26010-2376550/T202410487992457-1800`

> 双向覆盖率：共同 AST 指纹: 532；唯一 AST 指纹: 目标项目 2815，参考项目 1394；覆盖率: 目标项目 18.9%，参考项目 38.16%

## 全模块对比总览（确定性证据）

> 总体覆盖率：共同 AST 指纹: 532；唯一 AST 指纹: 目标项目 2815，参考项目 1394；覆盖率: 目标项目 18.9%，参考项目 38.16%

### 1. 模块覆盖矩阵
| 模块 | 目标函数数 | 参考函数数 | 共同唯一 AST 指纹 | Target 覆盖 | Base 覆盖 | 状态 |
| :--- | ---: | ---: | ---: | ---: | ---: | :--- |
| 进程管理 | 275 | 65 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |
| 任务调度/异步运行时 | 277 | 117 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |
| 内存管理 | 733 | 246 | 21 | 3.12% | 9.68% | 双方存在且有结构重合 |
| 中断/异常处理 | 124 | 10 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |
| 系统调用 | 194 | 112 | 1 | 0.57% | 1.25% | 双方存在且有结构重合 |
| 文件系统 | 838 | 285 | 3 | 0.49% | 1.4% | 双方存在且有结构重合 |
| 设备驱动 | 32 | 32 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |
| 网络协议栈 | 56 | 0 | 0 | 0.0% | 0.0% | 仅目标项目体现 |
| 文件系统/第三方库 | 719 | 703 | 501 | 81.6% | 83.92% | 双方存在且有结构重合 |
| 通用模块/其他 | 168 | 116 | 3 | 2.07% | 2.88% | 双方存在且有结构重合 |

### 2. 分模块证据清单

#### 进程管理
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `os/src/syscall/process.rs#sys_clone` | `os/src/syscall/process.rs#sys_clone` | 同板块同名 | Arc::downgrade, Arc::new, Arc::ptr_eq, CLONE_CALLS.fetch_add, Some, add_task ...(+38) | CloneFlags::from_bits, CloneFlags::from_bits(flags as u32).unwrap, Ok, add_task, current_task, current_task().unwrap ...(+3) |
| `os/src/syscall/process.rs#sys_futex` | `os/src/syscall/process.rs#sys_futex` | 同板块同名 | (ts.tv_sec as usize).saturating_mul, FUTEX_INTR_LOGS.fetch_add, FUTEX_STATE.exclusive_access, Some, core::cmp::min, crate::timer::get_time_us ...(+26) | Err, FutexCmd::from_bits, FutexCmd::from_bits(futex_op & 0x7f).unwrap, FutexKey::new, FutexOpt::from_bits_truncate, Ok ...(+22) |
| `os/src/syscall/process.rs#sys_wait4` | `os/src/syscall/process.rs#sys_wait4` | 同板块同名 | Arc::strong_count, Some, child.inner_exclusive_access, child_inner.waited_cputime_us, crate::memleak_log!, current_task ...(+24) | Arc::clone, Err, Ok, PROCESS_GROUP.lock, assert_eq!, child.inner_lock ...(+25) |
| `os/src/processor/processor.rs#run_tasks` | `os/src/task/processor.rs#run_tasks` | 同板块同名 | Context::from_waker, CoroutineUnit::new, POLLING_PIDS[poll_cpu].store, Pin::new, Pin::new(&mut co).poll, co.cpu_affinity ...(+16) | Some, __switch, add_task, change_current_uid, check_futex_timer, cur_task.inner_lock ...(+10) |

#### 任务调度/异步运行时
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `os/src/syscall/fs.rs#sys_write` | `os/src/syscall/fs.rs#sys_write` | 同板块同名 | OpenFlags::APPEND.bits, OpenFlags::NONBLOCK.bits, UserBuffer::new, cgroup_maybe_charge_file_write, copy_from_user_retry, copy_from_user_retry(&mut byte, buf).is_err ...(+54) | Err, Ok, UserBuffer::new, current_task, current_task().unwrap, drop ...(+13) |
| `os/src/syscall/fs.rs#sys_fchmodat` | `os/src/syscall/fs.rs#sys_fchmodat` | 同板块同名 | abs.as_str, apply_mode_to_path, base.as_str, check_parent_dir_search, check_path.as_str, compute_chmod_mode ...(+19) | Err, Ok, OpenFlags::empty, abs_path.as_str, current_task, current_task().unwrap ...(+14) |
| `os/src/syscall/fs.rs#sys_utimensat` | `os/src/syscall/fs.rs#sys_utimensat` | 同板块同名 | abs.as_str, abs.clone, apply_utimens, apply_utimens(path.as_str(), times)
            .map, apply_utimens(path.as_str(), times)
            .map(|_| 0)
            .unwrap_or_else, apply_utimens(target.as_str(), times)
            .map ...(+28) | Err, Ok, Some, String::new, current_task, current_task().unwrap ...(+11) |
| `os/src/syscall/signal.rs#sys_rt_sigaction` | `os/src/syscall/signal.rs#sys_rt_sigaction` | 同板块同名 | Some, action.is_null, core::mem::size_of::<SigSet>, current_mm_activation, current_mm_activation().user_token, current_task ...(+8) | Err, KSigAction::ignore, KSigAction::new, Ok, current_task, current_task().unwrap ...(+7) |

#### 内存管理
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `os/src/syscall/mod.rs#syscall` | `os/src/syscall/mod.rs#syscall` | 同板块同名 | note_last_syscall, println!, sys_accept, sys_accept4, sys_add_key, sys_adjtimex ...(+246) | Ok, Syscall::from, panic!, shutdown, sys_accept, sys_accept4 ...(+107) |
| `os/src/syscall/fs.rs#sys_openat` | `os/src/syscall/fs.rs#sys_openat` | 同板块同名 | OpenFlags::from_bits_truncate, Some, abs_path.as_str, abs_path.clone, bump_path_topology_epoch, busybox_applet_fallback_path ...(+92) | Err, FileDescriptor::new, Ok, OpenFlags::from_bits, OpenFlags::from_bits(flags).unwrap, current_task ...(+11) |
| `os/src/mission/mod.rs#exit_current_and_run_next` | `os/src/task/mod.rs#exit_current_and_run_next` | 同板块同名 | Arc::downgrade, Arc::strong_count, Firmware::shutdown, INITPROC.inner_exclusive_access, Some, TaskContext::goto_trap_return ...(+78) | FutexKey::new, THREAD_GROUP.lock, TaskContext::zero_init, VirtAddr::from, crate::fs::remove_proc_dir_and_file, debug! ...(+24) |
| `os/src/mm/memory_set.rs#from_elf` | `os/src/mm/memory_set.rs#from_elf` | 同板块同名 | MMAP_BASE.saturating_sub, MapArea::new, Self::elf_phdr_va, Self::map_elf_load_segments, Self::new_bare, Self::resolve_interp_path ...(+46) | Aux::new, MapArea::new, Self::new_from_kernel, Vec::new, VirtAddr, assert_eq! ...(+14) |

#### 中断/异常处理
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `hal/src/component/irq/loongarch64.rs#enable_timer_interrupt` | `os/src/trap/mod.rs#enable_timer_interrupt` | 同板块同名 | read_crmd, read_ecfg, write_crmd, write_ecfg | sie::set_stimer |
| `hal/src/component/trap/mod.rs#set_kernel_trap_entry` | `os/src/trap/mod.rs#set_kernel_trap_entry` | 同板块同名 | <TrapImpl as TrapHal>::set_kernel_trap_entry | stvec::write |
| `hal/src/component/trap/riscv64.rs#set_user_trap_entry` | `os/src/trap/mod.rs#set_user_trap_entry` | 同板块同名 | stvec::write | stvec::write |
| `os/src/syscall/fs.rs#sys_ioctl` | `os/src/signal/signal.rs#default_op` | 同模块上下文对齐/差异观察 | backing.as_str, fd_to_file, fd_to_file(arg).and_then, fd_to_path, file.is_path_only, file.path ...(+38) | continue_signals.contains, dump_signals.contains, ignore_signals.contains, stop_signals.contains, terminate_signals.contains |

#### 系统调用
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `os/src/syscall/ipc.rs#sys_shmctl` | `os/src/syscall/memory.rs#sys_shmctl` | 同板块同名 | buf.is_null, current_effective_ids, current_supplementary_gids, read_user_pod_retry, supplementary.as_slice, sysv::shm_mark_remove ...(+5) | Ok, panic!, shm_drop |
| `os/src/syscall/time.rs#sys_clock_gettime` | `user/src/syscall.rs#sys_clock_gettime` | 同板块同名 | clock_gettime_spec, copy_to_user_checked, core::mem::size_of::<TimeSpec>, core::slice::from_raw_parts, current_mm_activation, current_mm_activation().user_token ...(+1) | syscall, tp.as_mut_ptr |
| `os/src/syscall/time.rs#sys_nanosleep` | `user/src/syscall.rs#sys_nanosleep` | 同板块同名 | (now_ns() as u128).saturating_add, current_mm_activation, current_mm_activation().user_token, now_ns, read_timespec_checked, req.is_null ...(+2) | rem.as_mut_ptr, req.as_mut_ptr, syscall |
| `os/src/syscall/net.rs#sys_getsockname` | `os/src/syscall/net.rs#sys_getsockname` | 同板块同名 | get_netlink_socket, get_socket, nl_sock.local_pid, sock.local_sockaddr, write_netlink_sockaddr, write_sockaddr | Ok |

#### 文件系统
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `os/src/syscall/fs.rs#sys_linkat` | `os/src/syscall/fs.rs#sys_linkat` | 同板块同名 | bump_path_topology_epoch, check_create_parent, check_parent_dir_search, current_effective_ids, ext4_exists, ext4_link ...(+12) | todo! |
| `os/src/syscall/fs.rs#sys_fchownat` | `os/src/syscall/fs.rs#sys_fchownat` | 同板块同名 | abs.as_str, apply_chown_to_path, check_parent_dir_search, check_path.as_str, current_effective_ids, current_mm_activation ...(+12) | Ok |
| `os/src/syscall/fs.rs#sys_copy_file_range` | `user/src/syscall.rs#sys_copy_file_range` | 同板块同名 | copy_between_files, copy_file_range_offset, copy_file_range_regular_info, fd_to_file, in_off.checked_add, in_off.checked_add(len_u64).is_none ...(+4) | syscall |
| `os/src/syscall/fs.rs#sys_readlinkat` | `user/src/syscall.rs#sys_readlinkat` | 同板块同名 | abs.as_str, bytes.len, copy_to_user_strict, core::cmp::min, current_mm_activation, current_mm_activation().user_token ...(+4) | buf.as_mut_ptr, pathname.as_ptr, syscall |

#### 设备驱动
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `os/src/drivers/block/virtio_blk.rs#read_block` | `os/src/drivers/ramdisk.rs#read_block` | 同板块同名 | panic!, self.inner
            .lock, self.inner
            .lock()
            .read_blocks, self.inner
            .lock()
            .read_blocks(block_id, buf)
            .unwrap_or_else | blk.block_ref, buf.copy_from_slice, buf.len, self.0.lock |
| `os/src/drivers/block/virtio_blk.rs#write_block` | `os/src/drivers/ramdisk.rs#write_block` | 同板块同名 | panic!, self.inner
            .lock, self.inner
            .lock()
            .write_blocks, self.inner
            .lock()
            .write_blocks(block_id, buf)
            .unwrap_or_else | blk.block_refmut, blk.block_refmut(block_id, buf.len()).copy_from_slice, buf.len, self.0.lock |
| `os/src/net/mod.rs#unregister` | `os/src/drivers/sdcard.rs#sleep_ms_until` | 同模块上下文对齐/差异观察 | Arc::as_ptr, self
                    .stream_map
                    .get, self
                    .stream_map
                    .get(&addr)
                    .and_then, self
                    .stream_map
                    .get(&addr)
                    .and_then(|w| w.upgrade())
                    .map, self
                    .stream_map
                    .get(&addr)
                    .and_then(|w| w.upgrade())
                    .map(|x| Arc::as_ptr(&x) as usize == ptr)
                    .unwrap_or, self
            .listeners
            .get ...(+12) | core::hint::spin_loop, f, get_time |
| `hal/src/component/virtio/loongarch64.rs#pci_assign_bars` | `os/src/drivers/sdcard.rs#new_device` | 同模块上下文对齐/差异观察 | (size as u64).next_power_of_two, align_up, root.bars, root.bars(devfn).unwrap, root.get_status_command, root.set_bar_32 ...(+2) | Mutex::new, Vf2BlkDev, Vf2SdDriver::new, vf2_driver.init |

#### 网络协议栈
仅目标项目中观察到该模块代表函数，参考项目没有同模块可对齐证据：
- 目标项目：`os/src/net/mod.rs#disconnect`，调用摘要：NET_MANAGER.exclusive_access, NET_MANAGER.exclusive_access().unregister, inner.acceptq.clear, inner.local.is_some, inner.local.take, inner.peer.take ...(+5)
- 目标项目：`os/src/net/mod.rs#try_push_from_peer_vec`，调用摘要：Err, Ok, STREAM_BACKPRESSURE_LOGS.fetch_add, data.len, inner.recvq.push_back, inner.recvq_bytes.saturating_add ...(+2)
- 目标项目：`os/src/net/mod.rs#route_request`，调用摘要：build_netlink_ack, build_netlink_addr_dump, build_netlink_crypto_dump, build_netlink_link_dump, core::mem::size_of::<NetlinkHeader>, core::ptr::read_unaligned ...(+6)
- 目标项目：`os/src/net/mod.rs#build_netlink_link_dump`，调用摘要：Vec::new, align4, append_netlink_done, append_pod, attr_payload.len, core::mem::size_of::<IfInfoMsg> ...(+5)

#### 文件系统/第三方库
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `third_party/ext4fs/c/lwext4/src/ext4_balloc.c#ext4_balloc_alloc_block` | `lwext4_rust/c/lwext4/src/ext4_balloc.c#ext4_balloc_alloc_block` | 同板块同名同结构 | ext4_balloc_get_bgid_of_block, ext4_balloc_get_block_of_bgid, ext4_balloc_set_bitmap_csum, ext4_balloc_verify_bitmap_csum, ext4_bg_get_block_bitmap, ext4_bg_get_free_blocks_count ...(+19) | ext4_balloc_get_bgid_of_block, ext4_balloc_get_block_of_bgid, ext4_balloc_set_bitmap_csum, ext4_balloc_verify_bitmap_csum, ext4_bg_get_block_bitmap, ext4_bg_get_free_blocks_count ...(+19) |
| `third_party/ext4fs/c/lwext4/src/ext4_journal.c#jbd_journal_prepare` | `lwext4_rust/c/lwext4/src/ext4_journal.c#jbd_journal_prepare` | 同板块同名同结构 | EXT4_BLOCK_ZERO, RB_FIND, TAILQ_FOREACH_REVERSE_SAFE, TAILQ_FOREACH_SAFE, TAILQ_REMOVE, ext4_bcache_clear_dirty ...(+17) | EXT4_BLOCK_ZERO, RB_FIND, TAILQ_FOREACH_REVERSE_SAFE, TAILQ_FOREACH_SAFE, TAILQ_REMOVE, ext4_bcache_clear_dirty ...(+17) |
| `third_party/ext4fs/c/lwext4/src/ext4.c#ext4_dir_rm` | `lwext4_rust/c/lwext4/src/ext4.c#ext4_dir_rm` | 同板块同名同结构 | EXT4_MP_LOCK, EXT4_MP_UNLOCK, ext4_block_cache_write_back, ext4_dir_en_get_inode, ext4_dir_iterator_fini, ext4_dir_iterator_init ...(+19) | EXT4_MP_LOCK, EXT4_MP_UNLOCK, ext4_block_cache_write_back, ext4_dir_en_get_inode, ext4_dir_iterator_fini, ext4_dir_iterator_init ...(+19) |
| `third_party/ext4fs/c/lwext4/src/ext4_dir_idx.c#ext4_dir_dx_split_index` | `lwext4_rust/c/lwext4/src/ext4_dir_idx.c#ext4_dir_dx_split_index` | 同板块同名同结构 | ext4_block_set, ext4_dir_dx_climit_get_count, ext4_dir_dx_climit_get_limit, ext4_dir_dx_climit_set_count, ext4_dir_dx_climit_set_limit, ext4_dir_dx_entry_get_hash ...(+10) | ext4_block_set, ext4_dir_dx_climit_get_count, ext4_dir_dx_climit_get_limit, ext4_dir_dx_climit_set_count, ext4_dir_dx_climit_set_limit, ext4_dir_dx_entry_get_hash ...(+10) |

#### 通用模块/其他
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `hal/src/component/firmware/loongarch64.rs#set_timer` | `os/src/timer.rs#set_timer` | 同板块同名 | clear_timer_interrupt, log::trace!, normalize_timer_delta, read_time, timer.saturating_sub, write_tcfg | TimeVal::new, self.inner.get_unchecked_mut |
| `os/src/signal/manager.rs#set_action` | `os/src/signal/sigact.rs#set_action` | 同板块同名 | action_valid_signal | self.get_mut |
| `hal/src/component/firmware/riscv64.rs#shutdown` | `os/src/sbi.rs#shutdown` | 同板块同名 | core::arch::asm!, sbi_call, sbi_call_ex | system_reset, unreachable! |
| `os/src/main.rs#clear_bss` | `os/src/main.rs#clear_bss` | 同板块同名同结构 | core::slice::from_raw_parts_mut, core::slice::from_raw_parts_mut(sbss as usize as *mut u8, ebss as usize - sbss as usize)
            .fill | core::slice::from_raw_parts_mut, core::slice::from_raw_parts_mut(sbss as usize as *mut u8, ebss as usize - sbss as usize)
            .fill |


#### 1. 宏观判定
- **定性结论**: 独立开发 (相似度: 14.47%，共同 AST 指纹: 532；唯一 AST 指纹: 目标项目 2815，参考项目 1394；覆盖率: 目标项目 18.9%，参考项目 38.16%)

#### 2. 全模块覆盖矩阵解读
- **进程管理**: 双方均存在该模块。共同唯一 AST 指纹为 0，目标覆盖率 0.0%，参考覆盖率 0.0%。这表明在进程管理的核心逻辑上，双方未检测到任何同源结构指纹。同名函数由 Linux ABI 强制，但底层实现路径完全不同。证据强度：**未形成**。
- **任务调度/异步运行时**: 双方均存在该模块。共同唯一 AST 指纹为 0，目标覆盖率 0.0%，参考覆盖率 0.0%。目标项目采用协程/异步模型（`CoroutineUnit`, `Pin::new`），参考项目采用传统上下文切换（`__switch`），调度范式存在根本性架构差异。证据强度：**未形成**。
- **内存管理**: 双方均存在该模块。共同唯一 AST 指纹 21 个，目标覆盖率 3.12%，参考覆盖率 9.68%。存在少量结构重合，但覆盖率极低，表明内存管理的主体架构（如内存激活抽象 `current_mm_activation` vs 直接传递 `user_token`）存在显著差异。证据强度：**弱**。
- **中断/异常处理**: 双方均存在该模块。共同唯一 AST 指纹为 0，目标覆盖率 0.0%，参考覆盖率 0.0%。虽然存在同名函数，但底层实现（如 `enable_timer_interrupt` 中直接操作 CSR 寄存器 vs 调用 SBI 封装）属于不同硬件抽象层设计。证据强度：**未形成**。
- **系统调用**: 双方均存在该模块。共同唯一 AST 指纹 1 个，目标覆盖率 0.57%，参考覆盖率 1.25%。极低的覆盖率说明系统调用分发与实现逻辑高度异构。证据强度：**弱**。
- **文件系统**: 双方均存在该模块。共同唯一 AST 指纹 3 个，目标覆盖率 0.49%，参考覆盖率 1.4%。覆盖率极低，表明除了共用 lwext4 库外，自研文件系统逻辑（如路径解析、权限检查）无同源痕迹。证据强度：**弱**。
- **设备驱动**: 双方均存在该模块。共同唯一 AST 指纹为 0，目标覆盖率 0.0%，参考覆盖率 0.0%。双方驱动模型和硬件平台不同（VirtIO vs Ramdisk/SD卡），无同源证据。证据强度：**未形成**。
- **网络协议栈**: 仅目标项目体现。参考项目无此模块，无法形成比对。证据强度：**未形成**。
- **第三方库/公共依赖**: 双方均存在该模块。共同唯一 AST 指纹 501 个，目标覆盖率 81.6%，参考覆盖率 83.92%。这是双方共用的开源 C 库 `lwext4`，其极高的覆盖率是项目总相似度的主要贡献者，但这属于公共依赖，不能作为原创代码复用证据。证据强度：**强（但属公共依赖，已降权）**。

#### 3. 底层 AST 关键证据指纹表
| 所属板块 | 目标项目路径 | 参考项目路径 | 证据类型 | 技术说明 |
| :--- | :--- | :--- | :--- | :--- |
| 第三方库/公共依赖 | third_party/ext4fs/c/lwext4/src/ext4_balloc.c#ext4_balloc_alloc_block | lwext4_rust/c/lwext4/src/ext4_balloc.c#ext4_balloc_alloc_block | 同源开源库 | 双方均使用了相同的开源 ext4 文件系统库 `lwext4`，调用图谱完全一致，这是 532 个共同指纹的主要来源。 |
| 第三方库/公共依赖 | third_party/ext4fs/c/lwext4/src/ext4_journal.c#jbd_journal_prepare | lwext4_rust/c/lwext4/src/ext4_journal.c#jbd_journal_prepare | 同源开源库 | 同上，属于共用第三方库的内部逻辑。 |
| 第三方库/公共依赖 | third_party/ext4fs/c/lwext4/src/ext4.c#ext4_dir_rm | lwext4_rust/c/lwext4/src/ext4.c#ext4_dir_rm | 同源开源库 | 同上，属于共用第三方库的内部逻辑。 |
| 进程管理 | os/src/processor/processor.rs#current | os/src/task/processor.rs#current | 语言惯用法 | `self.current.as_ref().map(...)` 是 Rust `Option` 类型的标准操作，任何独立开发者都会写出相同的 AST 结构。 |
| 进程管理 | os/src/processor/processor.rs#take_current | os/src/task/processor.rs#take_current | 语言惯用法 | `self.current.take()` 是 Rust `Option` 类型的标准方法调用，无独创性。 |
| 内存管理 | os/src/mm/memory_set.rs#from_elf | os/src/mm/memory_set.rs#from_elf | 领域共性 | ELF 文件加载必须遵循标准格式，调用 `MapArea::new` 等是通用步骤，但目标项目引入了 `MMAP_BASE` 和 `resolve_interp_path` 等独立逻辑。 |
| 系统调用 | os/src/syscall/mod.rs#syscall | os/src/syscall/mod.rs#syscall | 领域共性 | 系统调用分发函数必然包含大量 `sys_xxx` 分支，同名是 ABI 强制，但目标项目支持的系统调用数量（246+）远超参考项目（107+）。 |

#### 4. 核心代码调用流对比剖析
* **【进程管理/任务调度】**
* **底层技术析理**: 这是双方架构差异最核心的体现。目标项目采用基于 `CoroutineUnit` 和 `Pin::new` 的异步协程调度模型，通过 `Context::from_waker` 实现协作式调度。参考项目则采用传统的抢占式内核调度，核心是 `__switch` 函数进行上下文切换。两者在调度实体抽象、状态机流转和上下文切换机制上完全不同，属于根本性的设计范式差异。
* **代码溯源链接**:
  - 目标项目: `os/src/processor/processor.rs#run_tasks`
  - 参考项目: `os/src/task/processor.rs#run_tasks`

* **【进程管理/内存访问抽象】**
* **底层技术析理**: 目标项目封装了 `current_mm_activation()` 抽象层来管理用户态内存访问，并通过 `write_user_value`、`translated_refmut` 等函数进行类型化操作。参考项目则直接通过 `current_token()` 获取页表令牌，并使用通用的 `put_data` 函数写入。目标项目的抽象层次更高，将内存激活状态显式管理，与参考项目的直接传递令牌模式存在架构级差异。
* **代码溯源链接**:
  - 目标项目: `os/src/syscall/process.rs#sys_sysinfo`
  - 参考项目: `os/src/syscall/process.rs#sys_sysinfo`

* **【进程管理/同步原语】**
* **底层技术析理**: 目标项目普遍使用 `task.inner_exclusive_access()` 获取任务内部状态，这通常对应更复杂的同步原语（如读写锁或独占访问令牌）。参考项目则使用 `task.inner_lock()`，通常对应互斥锁。方法名的不同反映了底层同步策略的差异，而非简单的重命名。目标项目还引入了 `Arc::downgrade`、`Arc::ptr_eq` 等更精细的引用计数管理，与参考项目主要使用 `Arc::clone` 的模式不同。
* **代码溯源链接**:
  - 目标项目: `os/src/syscall/process.rs#sys_wait4`
  - 参考项目: `os/src/syscall/process.rs#sys_wait4`

#### 5. 单方模块与降权项
- **网络协议栈**: 仅存在于目标项目，包含 Netlink 协议族等完整实现，是目标项目独立开发的强有力证据。
- **第三方库/公共依赖**: `lwext4` 库贡献了 94.2% 的共同 AST 指纹。这是双方共同引用的开源组件，其内部结构一致是必然的，不能作为原创代码抄袭的证据，必须严格降权。
- **语言惯用法与公共接口**: `current`、`take_current` 等函数是 Rust `Option` 枚举类型的标准用法；`sys_clone`、`sys_write` 等是 POSIX/Linux 标准系统调用名。这些由语言规范和操作系统 ABI 强制，任何独立实现都会产生相同的函数签名或极简调用流，不构成同源证据。

#### 6. 保守结论
综合全模块覆盖矩阵与底层 AST 指纹分析，目标项目与参考项目的整体相似度仅为 14.47%，且其中 94.2% 的共同指纹源自双方共用的开源第三方库 `lwext4`。在核心的进程管理、任务调度等原创逻辑板块，共同唯一 AST 指纹均为 0，表明双方在调度范式（协程 vs 传统抢占）、内存抽象（显式激活 vs 直接令牌）和同步策略上存在根本性架构差异。控方指控的“骨架复用”实为 Linux ABI 强制、Rust 语言惯用法或领域共性所导致的表层相似，缺乏底层 AST 结构同源的实质性证据。因此，综合判定两个项目为独立开发，复用指控不成立。

------------------------------

