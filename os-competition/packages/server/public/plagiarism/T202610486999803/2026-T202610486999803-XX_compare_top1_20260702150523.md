# 🛡️ OS 内核赛自动化代码级架构审计报告

**审计时间**: `2026-07-02 15:05:23`
**参赛作品名称**: `2026-T202610486999803-XX`
**年份**: `2026`
**学校**: `XX`
**队伍名称**: `T202610486999803`
**比赛名字**: `OS-Competition`
**比赛子赛道**: `Kernel-Implementation`
**仓库地址**: `https://gitlab.eduxiji.net/educg-group-43501-3132633/T202610486999803-20`
**推测开发语言**: `Rust / C (由AST探针自动识别)`
**特征函数量**: `8247`

## ⚔️ 底层代码同源性对比 (AST Homology)
> 本部分由专门的代码审查智能体，对该项目与符合年份条件的历届最高相似度项目进行控制流与底层调用对比。新版报告会先给出确定性的全模块覆盖矩阵，再给出 Agent 的分模块解释，避免只围绕少量抽样函数展开。

### 🏆 Top 1 底层对标: vs `2023-LostWakeup-杭州电子科技大学`（杭州电子科技大学 / LostWakeup）

#### 对方作品信息

**对方年份**: `2023`
**对方学校**: `杭州电子科技大学`
**对方队伍名称**: `LostWakeup`
**对方比赛名字**: `OS-Competition`
**对方比赛子赛道**: `Kernel-Implementation`
**对方仓库地址**: `https://gitlab.eduxiji.net/202310336101112/LostWakeup`

> 双向覆盖率：共同 AST 指纹: 1616；唯一 AST 指纹: 目标项目 6374，参考项目 9252；覆盖率: 目标项目 25.35%，参考项目 17.47%

## 全模块对比总览（确定性证据）

> 总体覆盖率：共同 AST 指纹: 1616；唯一 AST 指纹: 目标项目 6374，参考项目 9252；覆盖率: 目标项目 25.35%，参考项目 17.47%

### 1. 模块覆盖矩阵
| 模块 | 目标函数数 | 参考函数数 | 共同唯一 AST 指纹 | Target 覆盖 | Base 覆盖 | 状态 |
| :--- | ---: | ---: | ---: | ---: | ---: | :--- |
| 进程管理 | 360 | 521 | 49 | 16.55% | 11.34% | 双方存在且有结构重合 |
| 任务调度/异步运行时 | 1156 | 1041 | 215 | 21.41% | 22.14% | 双方存在且有结构重合 |
| 内存管理 | 1241 | 1838 | 192 | 16.52% | 10.97% | 双方存在且有结构重合 |
| 中断/异常处理 | 271 | 143 | 28 | 11.91% | 20.74% | 双方存在且有结构重合 |
| 系统调用 | 482 | 372 | 102 | 38.64% | 67.11% | 双方存在且有结构重合 |
| 文件系统 | 1212 | 1797 | 223 | 21.63% | 13.8% | 双方存在且有结构重合 |
| 设备驱动 | 186 | 165 | 2 | 1.6% | 1.3% | 双方存在且有结构重合 |
| 网络协议栈 | 359 | 837 | 62 | 21.16% | 9.32% | 双方存在且有结构重合 |
| 文件系统/第三方库 | 56 | 0 | 0 | 0.0% | 0.0% | 仅目标项目体现 |
| 通用模块/其他 | 2924 | 4661 | 808 | 38.42% | 22.61% | 双方存在且有结构重合 |

### 2. 分模块证据清单

#### 进程管理
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `user/external/musl/src/thread/pthread_create.c#__pthread_exit` | `apps/musl-1.2.4/src/thread/pthread_create.c#__pthread_exit` | 同板块同名同结构 | LOCK, UNLOCK, __block_all_sigs, __block_app_sigs, __dl_thread_cleanup, __do_orphaned_stdio_locks ...(+16) | LOCK, UNLOCK, __block_all_sigs, __block_app_sigs, __dl_thread_cleanup, __do_orphaned_stdio_locks ...(+16) |
| `user/external/musl/src/process/posix_spawn.c#child` | `apps/musl-1.2.4/src/process/posix_spawn.c#child` | 同板块同名 | __get_handler_set, __libc_sigaction, __sys_dup2, __sys_open, __syscall, _exit ...(+4) | __get_handler_set, __libc_sigaction, __sys_dup2, __sys_open, __syscall, _exit ...(+4) |
| `user/external/mksh-cvs2git/os2.c#execve` | `apps/musl-1.2.4/src/process/execve.c#execve` | 同板块同名 | DosEnterMustComplete, DosExitList, DosExitMustComplete, WEXITSTATUS, WIFSIGNALED, WTERMSIG ...(+14) | syscall |
| `user/external/musl/src/thread/pthread_barrier_wait.c#pthread_barrier_wait` | `apps/musl-1.2.4/src/thread/pthread_barrier_wait.c#pthread_barrier_wait` | 同板块同名同结构 | __syscall, __wait, __wake, a_fetch_add, a_inc, a_spin ...(+3) | __syscall, __wait, __wake, a_fetch_add, a_inc, a_spin ...(+3) |

#### 任务调度/异步运行时
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `user/external/musl/src/locale/iconv.c#iconv` | `apps/musl-1.2.4/src/locale/iconv.c#iconv` | 同板块同名 | combine_to_from, countof, extract_from, extract_to, find_charmap, get_16 ...(+9) | combine_to_from, extract_from, extract_to, find_charmap, get_16, get_32 ...(+8) |
| `user/external/musl/src/time/strptime.c#strptime` | `apps/musl-1.2.4/src/time/strptime.c#strptime` | 同板块同名 | __tzname_to_isdst, isdigit, isspace, nl_langinfo, strlen, strncasecmp ...(+2) | isdigit, isspace, nl_langinfo, strlen, strncasecmp, strptime ...(+1) |
| `user/external/musl/src/internal/floatscan.c#decfloat` | `apps/musl-1.2.4/src/internal/floatscan.c#decfloat` | 同板块同名同结构 | copysignl, fabsl, fmodl, scalbn, scalbnl, scanexp ...(+3) | copysignl, fabsl, fmodl, scalbn, scalbnl, scanexp ...(+3) |
| `user/external/musl/src/stdio/vfprintf.c#printf_core` | `apps/musl-1.2.4/src/stdio/vfprintf.c#printf_core` | 同板块同名 | MAX, OOB, S, ferror, fmt_fp, fmt_o ...(+11) | MAX, OOB, S, ferror, fmt_fp, fmt_o ...(+11) |

#### 内存管理
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `user/external/mksh-cvs2git/eval.c#expand` | `apps/busybox/coreutils/tr.c#expand` | 同板块同名 | Flag, ISMAGIC, KWF_ERR, ORD, XPput, Xcheck ...(+55) | bb_process_escape_sequence, bb_show_usage, free, index_in_strings, isalnum, iscntrl ...(+4) |
| `user/external/musl/src/regex/regcomp.c#tre_add_tags` | `apps/musl-1.2.4/src/regex/regcomp.c#tre_add_tags` | 同板块同名同结构 | IS_BACKREF, IS_SPECIAL, IS_TAG, STACK_PUSH, STACK_PUSHX, assert ...(+8) | IS_BACKREF, IS_SPECIAL, IS_TAG, STACK_PUSH, STACK_PUSHX, assert ...(+8) |
| `user/external/mksh-cvs2git/exec.c#execute` | `apps/busybox/e2fsprogs/fsck.c#execute` | 同板块同名 | FDSAVE, Flag, KWF_ERR, SAVEDFD, afree, alloc2 ...(+49) | base_device, bb_putchar, bb_simple_perror_msg, free, printf, progress_active ...(+6) |
| `user/external/musl/ldso/dynlink.c#__dls3` | `apps/musl-1.2.4/ldso/dynlink.c#__dls3` | 同板块同名同结构 | CRTJMP, MAXP2, __copy_tls, __init_tp, __pthread_self, _dl_debug_state ...(+27) | CRTJMP, MAXP2, __copy_tls, __init_tp, __pthread_self, _dl_debug_state ...(+27) |

#### 中断/异常处理
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `user/external/musl/src/thread/pthread_cond_timedwait.c#__pthread_cond_timedwait` | `apps/musl-1.2.4/src/thread/pthread_cond_timedwait.c#__pthread_cond_timedwait` | 同板块同名同结构 | __pthread_mutex_unlock, __pthread_self, __pthread_setcancelstate, __pthread_testcancel, __timedwait_cp, __wake ...(+8) | __pthread_mutex_unlock, __pthread_self, __pthread_setcancelstate, __pthread_testcancel, __timedwait_cp, __wake ...(+8) |
| `user/external/musl/src/math/pow.c#exp_inline` | `apps/musl-1.2.4/src/math/pow.c#exp_inline` | 同板块同名同结构 | __math_oflow, __math_uflow, asdouble, asuint64, converttoint, eval_as_double ...(+4) | __math_oflow, __math_uflow, asdouble, asuint64, converttoint, eval_as_double ...(+4) |
| `user/external/musl/src/string/memset.c#memset` | `apps/musl-1.2.4/src/string/memset.c#memset` | 同板块同名同结构 | 内部闭环逻辑 | 内部闭环逻辑 |
| `user/external/musl/src/math/exp.c#exp` | `apps/musl-1.2.4/src/math/exp.c#exp` | 同板块同名同结构 | __math_oflow, __math_uflow, asdouble, asuint64, converttoint, eval_as_double ...(+4) | __math_oflow, __math_uflow, asdouble, asuint64, converttoint, eval_as_double ...(+4) |

#### 系统调用
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `user/external/musl/src/thread/pthread_mutex_trylock.c#__pthread_mutex_trylock_owner` | `apps/musl-1.2.4/src/thread/pthread_mutex_trylock.c#__pthread_mutex_trylock_owner` | 同板块同名同结构 | __pthread_self, __syscall, a_cas | __pthread_self, __syscall, a_cas |
| `user/external/musl/src/stat/utimensat.c#utimensat` | `apps/musl-1.2.4/src/stat/utimensat.c#utimensat` | 同板块同名同结构 | IS32BIT, NS_SPECIAL, __syscall, __syscall_ret | IS32BIT, NS_SPECIAL, __syscall, __syscall_ret |
| `user/external/musl/src/aio/aio.c#cleanup` | `apps/musl-1.2.4/src/aio/aio.c#cleanup` | 同板块同名同结构 | __aio_unref_queue, __pthread_self, __syscall, __wake, a_store, a_swap ...(+5) | __aio_unref_queue, __pthread_self, __syscall, __wake, a_store, a_swap ...(+5) |
| `user/external/musl/src/time/clock_gettime.c#__clock_gettime` | `apps/musl-1.2.4/src/time/clock_gettime.c#__clock_gettime` | 同板块同名同结构 | __syscall, __syscall_ret, f | __syscall, __syscall_ret, f |

#### 文件系统
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `user/external/musl/src/stdio/vfprintf.c#fmt_fp` | `apps/musl-1.2.4/src/stdio/vfprintf.c#fmt_fp` | 同板块同名 | MAX, MIN, fmt_u, frexpl, isfinite, out ...(+3) | MAX, MIN, fmt_u, frexpl, isfinite, out ...(+2) |
| `user/external/musl/src/regex/glob.c#do_glob` | `apps/musl-1.2.4/src/regex/glob.c#do_glob` | 同板块同名同结构 | S_ISDIR, append, closedir, do_glob, errfunc, fnmatch ...(+7) | S_ISDIR, append, closedir, do_glob, errfunc, fnmatch ...(+7) |
| `user/external/musl/ldso/dlstart.c#_dlstart_c` | `apps/musl-1.2.4/ldso/dlstart.c#_dlstart_c` | 同板块同名同结构 | GETFUNCSYM, IS_RELATIVE, R_SYM, R_TYPE, dls2 | GETFUNCSYM, IS_RELATIVE, R_SYM, R_TYPE, dls2 |
| `user/external/sbase/libutil/cp.c#cp` | `apps/lmbench/src/lib_timing.c#cp` | 同板块同名 | S_ISBLK, S_ISCHR, S_ISDIR, S_ISFIFO, S_ISLNK, S_ISSOCK ...(+26) | close, fsync, open, read, write |

#### 设备驱动
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `kernel/core/panic.c#panic` | `src/lib/printf.c#panic` | 同板块同名 | arch_halt, firmware_shutdown, uart_puts, va_end, va_start, vprintf | printf, sbi_shutdown |
| `user/cmds/wget.c#set_nonblock` | `apps/netperf/src/netlib.c#set_nonblock` | 同板块同名 | fcntl | fcntl, ioctlsocket |
| `kernel/arch/x86_64/platform/virtio_probe.c#ecam_write` | `src/platform/qemu/virtio_disk.c#disk_rw` | 同板块同结构 | arch_pci_config_write32 | virtio_disk_rw |
| `kernel/arch/x86_64/platform/virtio_probe.c#ecam_read` | `apps/busybox/miscutils/setserial.c#uart_id` | 同板块同结构 | arch_pci_config_read32 | index_in_strings_case_insensitive |

#### 网络协议栈
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `user/external/musl/src/network/ns_parse.c#ns_parserr` | `apps/musl-1.2.4/src/network/ns_parse.c#ns_parserr` | 同板块同名同结构 | NS_GET16, NS_GET32, ns_name_uncompress, ns_skiprr | NS_GET16, NS_GET32, ns_name_uncompress, ns_skiprr |
| `user/external/musl/src/network/lookup_name.c#name_from_dns` | `apps/musl-1.2.4/src/network/lookup_name.c#name_from_dns` | 同板块同名同结构 | __dns_parse, __res_mkquery, __res_msend_rc | __dns_parse, __res_mkquery, __res_msend_rc |
| `user/external/musl/src/network/lookup_name.c#name_from_dns_search` | `apps/musl-1.2.4/src/network/lookup_name.c#name_from_dns_search` | 同板块同名同结构 | __get_resolv_conf, isspace, memcpy, name_from_dns | __get_resolv_conf, isspace, memcpy, name_from_dns |
| `user/external/musl/src/network/inet_pton.c#inet_pton` | `apps/musl-1.2.4/src/network/inet_pton.c#inet_pton` | 同板块同名同结构 | hexval, inet_pton, isdigit, memmove | hexval, inet_pton, isdigit, memmove |

#### 文件系统/第三方库
仅目标项目中观察到该模块代表函数，参考项目没有同模块可对齐证据：
- 目标项目：`kernel/fs/ext4.c#ext4_extent_grow`，调用摘要：EXT_PER_BLK, bcache_read_bytes, bcache_write_bytes, ext4_alloc_block, ext4_free_block, kfree ...(+3)
- 目标项目：`kernel/fs/ext4.c#ext4_vn_mkdir`，调用摘要：bcache_write_bytes, ext4_alloc_block, ext4_alloc_inode, ext4_dir_add, ext4_dir_find, ext4_free_block ...(+8)
- 目标项目：`kernel/fs/ext4.c#ext4_mount`，调用摘要：bcache_read_bytes, ext4_make_vnode, ext4_read_inode, kfree, kmalloc, memset ...(+2)
- 目标项目：`kernel/fs/ext4.c#ext4_vn_rename`，调用摘要：ext4_dir_add, ext4_dir_find, ext4_dir_remove, ext4_dir_update_entry, ext4_inode_remove, ext4_read_inode ...(+2)

#### 通用模块/其他
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `user/external/musl/src/crypt/crypt_blowfish.c#BF_set_key` | `apps/musl-1.2.4/src/crypt/crypt_blowfish.c#BF_set_key` | 同板块同名同结构 | 内部闭环逻辑 | 内部闭环逻辑 |
| `user/external/musl/src/math/powl.c#powl` | `apps/musl-1.2.4/src/math/powl.c#powl` | 同板块同名 | __p1evll, __polevll, fabsl, floorl, frexpl, isnan ...(+4) | __p1evll, __polevll, fabsl, floorl, frexpl, isfinite ...(+5) |
| `user/external/musl/src/crypt/crypt_sha512.c#sha512crypt` | `apps/musl-1.2.4/src/crypt/crypt_sha512.c#sha512crypt` | 同板块同名同结构 | hashmd, isdigit, sha512_init, sha512_sum, sha512_update, sprintf ...(+3) | hashmd, isdigit, sha512_init, sha512_sum, sha512_update, sprintf ...(+3) |
| `user/external/musl/src/math/jnf.c#jnf` | `apps/musl-1.2.4/src/math/jnf.c#jnf` | 同板块同名同结构 | GET_FLOAT_WORD, fabsf, j0f, j1f, logf | GET_FLOAT_WORD, fabsf, j0f, j1f, logf |


好的，合议庭。本庭已收到控辩双方的全部陈述、证据底座及原始 AST 抽样数据。现依据审计指令，对目标项目 `[2026-T202610486999803-XX]` 与参考项目 `[2023-LostWakeup-杭州电子科技大学]` 进行最终同源性定性仲裁。

### 1. 宏观判定
- **定性结论**: **独立开发，存在合法的公共开源代码复用**
  综合客观 AST 相似度（11.53%）、双向覆盖率（目标 25.35%，参考 17.47%）及底层调用图谱分析，双方项目在核心架构、内核实现及关键模块上存在根本性差异。指控方提出的“镜像复用”证据高度集中在双方共同引入的第三方开源库（musl libc），属于合法的公共代码复用，不构成对参考项目的抄袭。

### 2. 全模块覆盖矩阵解读
| 模块 | 双方存在 | 共同指纹 | 目标覆盖 | 参考覆盖 | 证据强度 | 解读 |
| :--- | :---: | :---: | :---: | :---: | :---: | :--- |
| **进程管理** | 是 | 49 | 16.55% | 11.34% | **弱** | 重合点几乎全部位于 `user/external/musl/` 与 `apps/musl-1.2.4/` 中，系共同引入的 musl libc 库代码。双方内核级进程管理（如 `sys_wait4`）调用图谱完全不同，证明核心实现独立。 |
| **任务调度/异步运行时** | 是 | 215 | 21.41% | 22.14% | **弱** | 覆盖率相对均衡，但证据清单显示重合函数（如 `iconv`, `strptime`, `printf_core`）均源于 musl 库或标准 C/POSIX 接口实现，非操作系统核心调度逻辑。 |
| **内存管理** | 是 | 192 | 16.52% | 10.97% | **弱** | 证据清单中的 `tre_add_tags`、`__dls3` 等均位于 musl 库中。`expand`、`execute` 等跨板块同名项因领域完全不同（shell vs. busybox），不构成有效证据。 |
| **中断/异常处理** | 是 | 28 | 11.91% | 20.74% | **弱** | 重合点如 `__pthread_cond_timedwait`、`memset`、`exp` 等均为 musl 库函数或标准数学库实现，与硬件中断/异常处理机制无关。 |
| **系统调用** | 是 | 102 | 38.64% | 67.11% | **中** | 覆盖率较高，但证据（如 `__pthread_mutex_trylock_owner`, `utimensat`）均为 musl 对 Linux 系统调用的薄封装，调用图谱必然高度相似。双方内核系统调用层实现（如 `sys_wait4`）存在本质差异。 |
| **文件系统** | 是 | 223 | 21.63% | 13.8% | **弱** | 重合点（如 `fmt_fp`, `do_glob`, `_dlstart_c`）均位于 musl 库。目标项目拥有独立的 Ext4 文件系统实现（`kernel/fs/ext4.c`），而参考项目无此模块，体现了核心能力的根本差异。 |
| **设备驱动** | 是 | 2 | 1.6% | 1.3% | **未形成** | 覆盖率极低。`panic` 函数调用对象完全不同（`arch_halt` vs `sbi_shutdown`），`set_nonblock` 等为极简系统调用封装，不构成抄袭证据。 |
| **网络协议栈** | 是 | 62 | 21.16% | 9.32% | **弱** | 重合点（如 `ns_parserr`, `name_from_dns`, `inet_pton`）均为 musl 库中的 DNS 解析与网络工具函数，属于标准化的网络协议处理实现。 |
| **文件系统/第三方库** | 仅目标 | 0 | 0.0% | 0.0% | **未形成** | 目标项目独有的 Ext4 文件系统模块，是证明其独立开发能力的关键证据。 |
| **通用模块/其他** | 是 | 808 | 38.42% | 22.61% | **弱** | 重合点数量多，但证据（如 `BF_set_key`, `sha512crypt`, `jnf`）均为 musl 库中的加密、数学等通用函数实现，属于标准算法或开源组件。 |

### 3. 底层 AST 关键证据指纹表
| 所属板块 | 目标项目路径 | 参考项目路径 | 证据类型 | 技术说明 |
| :--- | :--- | :--- | :--- | :--- |
| **系统调用** | `kernel/abi/linux/sys_proc.c#sys_wait4` | `src/kernel/sysproc.c#sys_wait4` | **强独立证据** | 目标调用 `copy_to_user, memset, proc_wait4`，参考调用 `argaddr, argint, waitpid`。内核对象模型与系统调用处理流完全不同，证明内核层独立实现。 |
| **系统调用** | `kernel/abi/linux/sys_proc.c#sys_set_robust_list` | `src/kernel/sysproc.c#sys_set_robust_list` | **强独立证据** | 目标显式调用 `proc_current` 获取当前进程对象，参考为内部闭环逻辑。体现了不同的进程/线程管理模型。 |
| **设备驱动** | `kernel/core/panic.c#panic` | `src/lib/printf.c#panic` | **强独立证据** | 目标调用 `arch_halt, firmware_shutdown, uart_puts`，参考调用 `printf, sbi_shutdown`。底层硬件抽象与平台固件接口完全不同。 |
| **文件系统** | `kernel/fs/ext4.c#ext4_mount` | (无) | **强独立证据** | 目标项目拥有完整的 Ext4 文件系统实现，参考项目无此模块，是目标项目独立开发能力的铁证。 |
| **第三方库/公共依赖** | `user/external/musl/src/thread/pthread_create.c#__pthread_exit` | `apps/musl-1.2.4/src/thread/pthread_create.c#__pthread_exit` | **合法复用** | 调用图谱完全一致，但均源于开源 musl libc 库。这是对公共上游代码的合法复用，非项目间抄袭。 |
| **第三方库/公共依赖** | `user/external/musl/src/process/posix_spawn.c#posix_spawn` | `apps/musl-1.2.4/src/process/posix_spawn.c#posix_spawn` | **合法复用** | 同上，POSIX 标准接口的 musl 库实现。 |
| **第三方库/公共依赖** | `user/external/musl/src/thread/pthread_barrier_wait.c#pthread_barrier_wait` | `apps/musl-1.2.4/src/thread/pthread_barrier_wait.c#pthread_barrier_wait` | **合法复用** | 同上，musl 库的 pthread 同步原语实现。 |
| **第三方库/公共依赖** | `user/external/musl/src/process/system.c#system` | `apps/musl-1.2.4/src/process/system.c#system` | **合法复用** | 同上，ISO C 标准 `system()` 函数的 musl 库实现。 |
| **第三方库/公共依赖** | `user/external/musl/src/exit/abort.c#abort` | `apps/musl-1.2.4/src/exit/abort.c#abort` | **合法复用** | 同上，ISO C 标准 `abort()` 函数的 musl 库实现。 |

### 4. 核心代码调用流对比剖析
*   **【系统调用 / 进程等待】**
    *   **底层技术析理**: 双方均实现了 `sys_wait4` 系统调用，但底层进程模型截然不同。目标项目采用类 Linux 的 `copy_to_user` 与 `proc_wait4` 内核对象模型，体现了对现代宏内核/混合内核架构的理解。参考项目则采用教学操作系统 xv6 风格的 `argaddr/argint` 参数获取与 `waitpid` 函数，模型更为简化。这种内核核心路径上的根本性差异，是双方独立开发的有力证明。
    *   **代码溯源链接**:
        - 目标项目: `kernel/abi/linux/sys_proc.c#sys_wait4`
        - 参考项目: `src/kernel/sysproc.c#sys_wait4`

*   **【进程管理 / 线程退出】**
    *   **底层技术析理**: 双方 `__pthread_exit` 函数的调用图谱完全一致，但这并非抄袭，而是因为它们都直接复用了开源 musl libc 的代码。该函数是 POSIX 线程标准中线程退出逻辑的标准实现，涉及 TSD 析构、锁清理、信号屏蔽等固定流程。双方均以“原样引入”的方式使用了该库，属于合法的第三方代码复用。
    *   **代码溯源链接**:
        - 目标项目: `user/external/musl/src/thread/pthread_create.c#__pthread_exit`
        - 参考项目: `apps/musl-1.2.4/src/thread/pthread_create.c#__pthread_exit`

### 5. 单方模块与降权项
*   **单方模块（强独立证据）**:
    *   **文件系统/第三方库**: 目标项目拥有独立的 `kernel/fs/ext4.c` 模块，实现了 Ext4 文件系统的挂载、文件创建、重命名等核心功能。参考项目无此模块，这是目标项目在核心能力上独立于参考项目的显著标志。
*   **降权项（跨板块同名/公共接口/泛型函数名）**:
    *   `processblock`: 此为哈希算法（MD5, SHA1, SHA256, SHA512）中通用的内部函数名。不同算法调用图谱不同，相同算法调用图谱必然一致，属于算法规范收敛，不能作为抄袭证据。
    *   `execve`: 目标项目位于 `mksh-cvs2git/os2.c`，调用 OS/2 平台 API；参考项目位于 `musl-1.2.4/src/process/execve.c`，调用 Linux 系统调用。跨平台、跨板块，底层逻辑完全不同。
    *   `expand`, `execute`, `cp`, `panic` 等: 均为跨板块同名项，函数功能、上下文及外部调用对象完全不同，不构成同板块主证据，已从强证据链中排除。

### 6. 保守结论
本庭裁定，目标项目与参考项目在宏观架构上存在显著差异，11.53% 的客观 AST 相似度及较低的覆盖率不支持抄袭指控。控方提出的“镜像复用”证据，经溯源分析，高度集中于双方共同引入的第三方开源库 musl libc，属于对公共代码的合法复用，而非对参考项目的抄袭。双方在内核核心路径（如系统调用处理、设备驱动模型）上的实现截然不同，且目标项目拥有参考项目不具备的独立文件系统模块，进一步佐证了其独立开发的性质。综上，本项目不存在违规抄袭行为，代码具有原创性。

------------------------------

