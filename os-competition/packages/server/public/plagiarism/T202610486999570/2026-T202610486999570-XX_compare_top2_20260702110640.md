# 🛡️ OS 内核赛自动化代码级架构审计报告

**审计时间**: `2026-07-02 11:06:40`
**参赛作品名称**: `2026-T202610486999570-XX`
**年份**: `2026`
**学校**: `XX`
**队伍名称**: `T202610486999570`
**比赛名字**: `OS-Competition`
**比赛子赛道**: `Kernel-Implementation`
**仓库地址**: `https://gitlab.eduxiji.net/educg-group-43501-3132633/T202610486999570-2543`
**推测开发语言**: `Rust / C (由AST探针自动识别)`
**特征函数量**: `2241`

## ⚔️ 底层代码同源性对比 (AST Homology)
> 本部分由专门的代码审查智能体，对该项目与符合年份条件的历届最高相似度项目进行控制流与底层调用对比。新版报告会先给出确定性的全模块覆盖矩阵，再给出 Agent 的分模块解释，避免只围绕少量抽样函数展开。

### 🏆 Top 2 底层对标: vs `2024-chaos-北京科技大学`（北京科技大学 / chaos）

#### 对方作品信息

**对方年份**: `2024`
**对方学校**: `北京科技大学`
**对方队伍名称**: `chaos`
**对方比赛名字**: `OS-Competition`
**对方比赛子赛道**: `Kernel-Implementation`
**对方仓库地址**: `https://gitlab.eduxiji.net/educg-group-26010-2376550/oskernel2024-chaos`

> 双向覆盖率：共同 AST 指纹: 128；唯一 AST 指纹: 目标项目 1824，参考项目 798；覆盖率: 目标项目 7.02%，参考项目 16.04%

## 全模块对比总览（确定性证据）

> 总体覆盖率：共同 AST 指纹: 128；唯一 AST 指纹: 目标项目 1824，参考项目 798；覆盖率: 目标项目 7.02%，参考项目 16.04%

### 1. 模块覆盖矩阵
| 模块 | 目标函数数 | 参考函数数 | 共同唯一 AST 指纹 | Target 覆盖 | Base 覆盖 | 状态 |
| :--- | ---: | ---: | ---: | ---: | ---: | :--- |
| 进程管理 | 406 | 45 | 6 | 1.97% | 13.64% | 双方存在且有结构重合 |
| 任务调度/异步运行时 | 258 | 76 | 7 | 3.04% | 10.0% | 双方存在且有结构重合 |
| 内存管理 | 484 | 178 | 58 | 14.08% | 34.73% | 双方存在且有结构重合 |
| 中断/异常处理 | 90 | 5 | 2 | 2.6% | 50.0% | 双方存在且有结构重合 |
| 系统调用 | 203 | 67 | 19 | 12.03% | 47.5% | 双方存在且有结构重合 |
| 文件系统 | 444 | 152 | 10 | 2.77% | 7.94% | 双方存在且有结构重合 |
| 设备驱动 | 78 | 19 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |
| 网络协议栈 | 28 | 0 | 0 | 0.0% | 0.0% | 仅目标项目体现 |
| 文件系统/第三方库 | 83 | 270 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |
| 网络协议栈/第三方库 | 1 | 0 | 0 | 0.0% | 0.0% | 仅目标项目体现 |
| 第三方库 | 0 | 50 | 0 | 0.0% | 0.0% | 仅参考项目体现 |
| 通用模块/其他 | 166 | 75 | 17 | 11.41% | 25.0% | 双方存在且有结构重合 |

### 2. 分模块证据清单

#### 进程管理
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `os/src/syscall/process.rs#sys_sigprocmask` | `os/src/syscall/signal.rs#sys_sigprocmask` | 同板块同名 | copy_to_user, core::mem::size_of::<usize>, core::slice::from_raw_parts, crate::syscall::should_trace_syscall, current_process, current_task ...(+14) | SignalFlags::from_bits, SignalFlags::from_bits(new_set).unwrap, current_task, current_task().unwrap, file!, line! ...(+5) |
| `os/src/syscall/process.rs#sys_set_priority` | `os/src/syscall/process.rs#sys_set_priority` | 同板块同名 | caller.effective_uid, clamp_nice, collect_priority_target_pids, current_process, errno, pid2process ...(+4) | trace! |
| `os/src/syscall/process.rs#sys_uname` | `os/src/syscall/process.rs#sys_uname` | 同板块同名 | UTS_STATE.exclusive_access, UtsName::default, arch::machine_name, bytes.len, bytes.len().min, copy_to_user ...(+15) | "Machine: riscv64".as_bytes, "None".as_bytes, SYS_NAME.as_bytes, SYS_NODENAME.as_bytes, SYS_RELEASE.as_bytes, SYS_VERSION.as_bytes ...(+15) |
| `os/src/syscall/process.rs#sys_kill` | `os/src/syscall/process.rs#sys_kill` | 同板块同名 | Some, crate::syscall::should_trace_syscall, current_process, current_process().pgid, kill_group, kill_single ...(+2) | SignalFlags::from_bits, file!, line!, pid2process, process.inner_exclusive_access, trace! |

#### 任务调度/异步运行时
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `os/src/syscall/fs.rs#sys_linkat` | `os/src/syscall/fs.rs#sys_linkat` | 同板块同名 | String::from, access_allowed, c.is_empty, component.is_empty, component.len, crate::syscall::should_trace_syscall ...(+35) | curdir.inode, curdir.inode().link, curdir.inode().lookup, curdir.inode().lookup(old_name.as_str()).unwrap, current_task, current_task()
        .unwrap ...(+8) |
| `os/src/syscall/fs.rs#sys_ioctl` | `os/src/syscall/fs.rs#sys_ioctl` | 同板块同名 | current_process, current_user_token, errno, file.get_offset, file.get_offset().unwrap_or, file.inode ...(+3) | trace! |
| `os/src/syscall/fs.rs#sys_ppoll` | `os/src/syscall/ppoll.rs#sys_ppoll` | 同板块同名 | PollEvents::empty, Some, Vec::new, current_process, current_process()
        .with_limits, current_user_token ...(+17) | Box::from_raw, Box::into_raw, Box::new, SignalFlags::empty, current_task, current_task().unwrap ...(+18) |
| `os/src/syscall/fs.rs#sys_unlinkat` | `os/src/syscall/fs.rs#sys_unlinkat` | 同板块同名 | OpenFlags::from_bits_truncate, _name.is_null, access_allowed_egid, component.len, crate::syscall::should_trace_syscall, current_process ...(+26) | curdir.inode, curdir.inode().unlink, current_task, current_task()
        .unwrap, current_task()
        .unwrap()
        .inner_exclusive_access, current_task()
        .unwrap()
        .inner_exclusive_access(file!(), line!())
        .work_dir
        .clone ...(+5) |

#### 内存管理
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `os/src/mm/memory_set.rs#from_existed_user` | `os/src/mm/memory_set.rs#from_existed_user` | 同板块同名 | Arc::new, MapArea::from_another, Self::new_bare, arch::flush_tlb_all, area
                .vpn_range
                .get_end, area
                .vpn_range
                .get_end()
                .0
                .saturating_sub ...(+24) | MapArea::from_another, Self::new_process, area.vpn_range.get_start, dst_ppn
                    .get_bytes_array, dst_ppn
                    .get_bytes_array()
                    .copy_from_slice, dst_ppn
                .get_bytes_array ...(+17) |
| `os/src/syscall/fs.rs#sys_fcntl` | `os/src/syscall/fs.rs#sys_fcntl` | 同板块同名 | Some, arg.max, copy_to_user, core::mem::size_of::<Flock>, core::slice::from_raw_parts, crate::syscall::should_trace_syscall ...(+27) | current_task, current_task().unwrap, debug!, file!, inner.alloc_fd, inner.fd_table.len ...(+6) |
| `os/src/task/process.rs#fork` | `os/src/task/task.rs#fork` | 同板块同名 | Arc::clone, Arc::downgrade, Arc::new, IntervalTimerState::default, RecycleAllocator::new, SignalFlags::empty ...(+31) | (MapPermission::R | MapPermission::W).bits, Arc::clone, Arc::downgrade, Arc::new, MemorySet::from_existed_user, PTEFlags::from_bits ...(+42) |
| `os/src/task/mod.rs#exit_current_and_run_next` | `os/src/task/mod.rs#exit_current_and_run_next` | 同板块同名 | Arc::clone, Arc::downgrade, Arc::ptr_eq, FutexKey::new, INITPROC.push_child, PageTable::from_token ...(+48) | Arc::clone, Arc::downgrade, INITPROC.inner_exclusive_access, Some, TaskContext::zero_init, child.clone ...(+25) |

#### 中断/异常处理
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `arch/src/riscv64/trap/mod.rs#enable_timer_interrupt` | `os/src/trap/mod.rs#enable_timer_interrupt` | 同板块同名同结构 | sie::set_stimer | sie::set_stimer |
| `arch/src/loongarch64/trap.rs#trap_enable_timer_interrupt` | `os/src/trap/mod.rs#init` | 同板块同结构 | enable_irq | set_kernel_trap_entry |
| `arch/src/loongarch64/unaligned.rs#emulate_load_store_insn` | `os/src/fs/pipe.rs#read` | 同模块上下文对齐/差异观察 | badv::read, badv::read().raw, panic!, unaligned_read, unaligned_write | assert!, buf.into_iter, buf.len, buf_iter.next, debug!, drop ...(+11) |
| `os/src/sync/up.rs#exclusive_access` | `os/src/trap/mod.rs#set_kernel_trap_entry` | 同模块上下文对齐/差异观察 | INTR_MASKING_INFO.get_mut, INTR_MASKING_INFO.get_mut().enter, Some, caller.file, caller.line, core::panic::Location::caller ...(+5) | stvec::write |

#### 系统调用
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `user/src/syscall.rs#syscall` | `user/src/syscall.rs#syscall` | 同板块同名 | core::arch::asm! | asm! |
| `user/src/syscall.rs#sys_exec` | `user/src/syscall.rs#sys_exec` | 同板块同名 | args.as_ptr, envp.as_ptr, path.as_ptr, syscall6 | args.as_ptr, path.as_ptr, syscall |
| `user/src/lib.rs#exec` | `user/src/lib.rs#exec` | 同板块同名 | args.is_empty, core::ptr::null, sys_exec | sys_exec |
| `user/src/lib.rs#waittid` | `user/src/lib.rs#waittid` | 同板块同名 | sys_waittid, yield_ | sys_waittid, yield_ |

#### 文件系统
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `easy-fs/src/layout.rs#read_at` | `os/src/fs/fat32/inode.rs#read_at` | 同板块同名 | (offset + buf.len()).min, Arc::clone, buf.len, dst.copy_from_slice, end_current_block.min, get_block_cache ...(+3) | buf.len, buf[read_size..read_size + copy_size]
                .copy_from_slice, dentry.file_size, fs.cluster_chain, fs.read_cluster, min ...(+3) |
| `easy-fs/src/layout.rs#write_at` | `os/src/fs/fat32/inode.rs#write_at` | 同板块同名 | (offset + buf.len()).min, Arc::clone, assert!, buf.len, dst.copy_from_slice, end_current_block.min ...(+4) | buf.len, cluster_buf[pos % CLUSTER_SIZE..pos % CLUSTER_SIZE + copy_size]
                .copy_from_slice, fs.cluster_chain, fs.read_cluster, fs.write_cluster, min ...(+2) |
| `easy-fs/src/vfs.rs#ls` | `os/src/fs/fat32/inode.rs#ls` | 同板块同名 | DirEntry::empty, String::from, Vec::new, assert_eq!, dirent.name, self.fs.lock ...(+2) | Vec::new, dentry.name, fs.fat.cluster_id_to_sector_id, fs.fat.cluster_id_to_sector_id(self.start_cluster).unwrap, fs.get_dentry, self.fs.as_ref ...(+1) |
| `os/src/fs/vfs/fat32/inode.rs#lookup` | `os/src/fs/fat32/inode.rs#lookup` | 同板块同名 | Arc::new, Fat32Inode::new_dir, Fat32Inode::new_file, Some, fat32_open_dir, fat32_open_dir(&fs, &path).is_ok ...(+6) | Arc::clone, Arc::new, Dentry::new, Some, dentry.is_dir, dentry.is_file ...(+6) |

#### 设备驱动
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `os/src/drivers/block/mod.rs#block_device_test` | `os/src/drivers/block/mod.rs#block_device_test` | 同板块同名 | BLOCK_DEVICE.clone, assert_eq!, block_device.read_block, block_device.write_block, println!, write_buffer.iter_mut | BLOCK_DEVICE.clone, assert_eq!, println!, write_buffer.iter_mut |
| `os/src/drivers/block/cached_block_device.rs#write_block` | `os/src/drivers/block/virtio_blk.rs#write_block` | 同板块同名 | blocks[block_id].copy_from_slice, self.blocks.lock, self.write_count.lock | debug!, self.0
            .lock, self.0
            .lock()
            .write_blocks, self.0
            .lock()
            .write_blocks(block_id, buf)
            .expect |
| `os/src/drivers/block/cached_block_device.rs#read_block` | `os/src/drivers/block/virtio_blk.rs#read_block` | 同板块同名 | buf.copy_from_slice, self.blocks.lock, self.read_count.lock | error!, panic!, res.is_err, res.unwrap, self.0.lock, self.0.lock().read_blocks ...(+1) |
| `os/src/drivers/bus/virtio_la.rs#mmio_phys_to_virt` | `os/src/drivers/block/virtio_blk.rs#mmio_phys_to_virt` | 同板块同名 | NonNull::new, NonNull::new(vaddr as *mut u8).unwrap, dma_uncached_base | KernelAddr::from, NonNull::new_unchecked, PhysAddr::from |

#### 网络协议栈
仅目标项目中观察到该模块代表函数，参考项目没有同模块可对齐证据：
- 目标项目：`os/src/net/mod.rs#loopback_udp_inject`，调用摘要：Some, connected_handle.or, stack.sockets.get_mut::<UdpSocket>, stack.sockets.iter, udp_sock.endpoint, udp_sock.inject_recv ...(+2)
- 目标项目：`os/src/net/unix_socket.rs#new`，调用摘要：Arc::new, AtomicBool::new, Mutex::new, String::new, VecDeque::new
- 目标项目：`os/src/net/unix_socket.rs#writable`，调用摘要：peer_weak.upgrade, peer_weak.upgrade().is_some, self.peer.lock, self.peer.lock().as_ref, self.state.lock
- 目标项目：`os/src/net/unix_socket.rs#write`，调用摘要：guard.as_ref, guard.as_ref().and_then, peer.unix_push_rx_bytes, self.peer.lock, user_buf.buffers.iter, w.upgrade

#### 文件系统/第三方库
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `os/src/fs/vfs/ext4/inode.rs#write_at` | `os/src/fs/ext4/inode.rs#write_at` | 同板块同名 | Some, Vec::with_capacity, error!, ext4_cache_touch_write_extend, file.file_seek, file.file_seek(offset as i64, SEEK_SET).is_err ...(+11) | Arc::downgrade, Ext4File::new, Ext4InodeRef::get_inode_ref, buf.len, inode_ref.inner.inode.inode_get_size, self.fs.ext4.ext4_file_write |
| `os/src/fs/vfs/ext4/inode.rs#create` | `os/src/fs/ext4/inode.rs#create` | 同板块同名 | Arc::new, Ext4File::new, Ext4Inode::new_file, Some, ext4_cache_remove_path, ext4_inode_exists ...(+6) | todo! |
| `os/src/fs/vfs/ext4/inode.rs#read_at` | `os/src/fs/ext4/inode.rs#read_at` | 同板块同名 | Some, error!, file.file_read, file.file_read(buf).unwrap_or, file.file_seek, file.file_seek(offset as i64, SEEK_SET).is_err ...(+3) | Ext4File::new, buf.len, self
            .fs
            .ext4
            .ext4_file_read |
| `os/src/fs/vfs/ext4/inode.rs#lookup` | `os/src/fs/ext4/inode.rs#lookup` | 同板块同名 | Arc::new, Ext4Inode::new, Some, ext4_cached_metadata, ext4_cached_metadata(&path).ok, ext4_path_join ...(+1) | Arc::new, Dentry::new, Ext4File::new, Some, UPSafeCell::new, self.fs
            .ext4
            .ext4_open_from ...(+2) |

#### 网络协议栈/第三方库
仅目标项目中观察到该模块代表函数，参考项目没有同模块可对齐证据：
- 目标项目：`os/src/net/mod.rs#smoltcp_now`，调用摘要：get_time_ms, smoltcp::time::Instant::from_millis

#### 第三方库
仅参考项目中观察到该模块代表函数，目标项目没有同模块可对齐证据：
- 参考项目：`os/libs/visionfive2-sd/src/lib.rs#send_cmd`，调用摘要：RawInterrupt::from, RawInterruptStatusReg::from, S::sleep_ms_until, Some, arg.into, assert! ...(+21)
- 参考项目：`os/libs/visionfive2-sd/src/lib.rs#init_sdcard`，调用摘要：CardTypeReg::from, CardTypeReg::from(0).with_card_width4_1, CmdArg::new, CmdReg::from, ControlReg::from, RawInterruptStatusReg::from ...(+18)
- 参考项目：`os/libs/visionfive2-sd/src/register.rs#from`，调用摘要：CmdReg::with_data, CmdReg::with_data(0, value.into()).with_transfer_dir, CmdReg::with_no_data, CmdReg::with_no_data(0, value.into())
                    .with_check_response_crc, CmdReg::with_no_data(0, value.into())
                    .with_check_response_crc(false)
                    .with_response_length, CmdReg::with_no_data(0, value.into()).with_check_response_crc ...(+3)
- 参考项目：`os/libs/visionfive2-sd/src/lib.rs#reset_clock`，调用摘要：ClockDividerReg::new, ClockDividerReg::new().with_clk_divider0, ClockEnableReg::from, CmdArg::new, CmdReg::from, CmdReg::from(0)
        .with_start_cmd ...(+7)

#### 通用模块/其他
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `user/src/lib.rs#_start` | `user/src/lib.rs#_start` | 同板块同名 | ((argv + i * core::mem::size_of::<usize>()) as *const usize).read_volatile, ((str_start + *i) as *const u8).read_volatile, (0usize..)
            .find, (0usize..)
            .find(|i| unsafe { ((str_start + *i) as *const u8).read_volatile() == 0 })
            .unwrap, HEAP.lock, HEAP.lock()
            .init ...(+11) | ((argv + i * core::mem::size_of::<usize>()) as *const usize).read_volatile, ((str_start + *i) as *const u8).read_volatile, (0usize..)
            .find, (0usize..)
            .find(|i| unsafe { ((str_start + *i) as *const u8).read_volatile() == 0 })
            .unwrap, HEAP.lock, HEAP.lock()
            .init ...(+10) |
| `arch/src/loongarch64/sbi.rs#shutdown` | `os/src/sbi.rs#shutdown` | 同板块同名 | core::hint::spin_loop, core::ptr::write_volatile, warn! | panic!, sbi_call |
| `os/src/sync/mutex.rs#lock` | `os/src/sync/mutex/spin_mutex.rs#lock` | 同板块同名 | drop, self.locked.exclusive_access, suspend_current_and_run_next | S::before_lock, self
                .lock
                .compare_exchange, self
                .lock
                .compare_exchange(false, true, Ordering::Acquire, Ordering::Relaxed)
                .is_ok, self.wait_unlock |
| `arch/src/riscv64/sbi.rs#sbi_call` | `os/src/sbi.rs#sbi_call` | 同板块同名 | asm! | asm! |


# 最终同源性定性仲裁报告

**案件编号**: 2026-T202610486999570-XX vs 2024-chaos-北京科技大学
**审计日期**: 2026 年 6 月 25 日
**审计模式**: 深度析理模式

---

## 1. 宏观判定

- **定性结论**: **独立开发**
- **相似度**: 5.13%
- **共同 AST 指纹**: 128
- **唯一 AST 指纹**: 目标项目 1824，参考项目 798
- **覆盖率**: 目标项目 7.02%，参考项目 16.04%

**判定依据**: 双方 AST 覆盖率均处于极低水平（目标项目仅 7.02%，参考项目因体量较小为 16.04%）。128 个共同 AST 指纹分散于多个模块，且经逐项审查，均属于操作系统内核领域的通用范式、Rust 语言标准惯用法或公开教学框架（rCore）的常规实现，不构成代码复用的有效证据。目标项目拥有大量参考项目完全不具备的独立模块（网络协议栈、Unix Socket、多架构支持、ext4/fat32 双文件系统、Linux capabilities 等），整体架构呈独立演进态势。

---

## 2. 全模块覆盖矩阵解读

### 进程管理
- **双方均存在**: 是
- **共同 AST 指纹**: 6 个
- **覆盖率**: 目标 1.97%，参考 13.64%
- **证据强度**: **弱**

目标项目拥有 406 个进程管理函数，参考项目仅 45 个，体量差异近 10 倍。6 个共同指纹均为 `sys_getpid`、`sys_exit`、`sys_yield` 等 POSIX 标准系统调用入口或 `pid2process`、`insert_into_pid2process` 等基础 PID 管理操作。目标项目采用 `current_process` 为中心的进程模型，参考项目采用 `current_task().unwrap().inner_exclusive_access(...)` 为中心的任务模型，两者在进程抽象层次上存在根本差异。目标项目独有的 `sys_prctl`、`sys_adjtimex`、`sys_capget`、`sys_waitid` 等 Linux 兼容系统调用在参考项目中完全不存在。

### 任务调度/异步运行时
- **双方均存在**: 是
- **共同 AST 指纹**: 7 个
- **覆盖率**: 目标 3.04%，参考 10.0%
- **证据强度**: **弱**

该板块的共同指纹实际来源于文件系统系统调用（`sys_linkat`、`sys_ioctl`、`sys_ppoll`、`sys_unlinkat`）的跨板块误归类，属于证据对齐错误。真正的调度器核心函数（`schedule`、`processor.rs` 系列）虽同名，但目标项目使用 `arch::switch_to_idle` 架构抽象，参考项目使用 `__schedule` 内部实现，调度后端已独立。

### 内存管理
- **双方均存在**: 是
- **共同 AST 指纹**: 58 个
- **覆盖率**: 目标 14.08%，参考 34.73%
- **证据强度**: **中**

内存管理是双方共同指纹最多的板块，但 58 个指纹分散于 484（目标）和 178（参考）个函数中。`from_existed_user`、`fork`、`exit_current_and_run_next` 等函数同名，均源于 rCore 教学框架的标准 API 命名。目标项目的 `MemorySet` 使用 `Arc` 共享，参考项目同样使用 `Arc`，这是 Rust 内存管理的标准惯用法。目标项目在 `sys_fcntl` 中集成了 `Flock` 文件锁，参考项目仅实现基本 fd 分配，功能层次不同。

### 中断/异常处理
- **双方均存在**: 是
- **共同 AST 指纹**: 2 个
- **覆盖率**: 目标 2.6%，参考 50.0%
- **证据强度**: **弱**

参考项目覆盖率 50% 是因为其仅有 5 个中断处理函数，基数极小。共同指纹 `enable_timer_interrupt` 仅调用 RISC-V 架构规定的 `sie::set_stimer`，由硬件规范唯一决定。目标项目额外支持 LoongArch64 架构（`arch/src/loongarch64/trap.rs`），参考项目仅支持 RISC-V64。

### 系统调用
- **双方均存在**: 是
- **共同 AST 指纹**: 19 个
- **覆盖率**: 目标 12.03%，参考 47.5%
- **证据强度**: **弱**

共同指纹集中在用户态系统调用封装（`user/src/syscall.rs`、`user/src/lib.rs`），如 `syscall`（仅含 `asm!` 宏）、`sys_exec`、`exec`、`waittid` 等。这些是任何 Rust OS 用户库的标准写法，`asm!` 宏由 RISC-V 生态调用规范决定。参考项目覆盖率偏高是因为其系统调用总数较少（67 vs 203）。

### 文件系统
- **双方均存在**: 是
- **共同 AST 指纹**: 10 个
- **覆盖率**: 目标 2.77%，参考 7.94%
- **证据强度**: **弱**

共同指纹主要来自 `easy-fs/src/layout.rs` 与 `os/src/fs/fat32/inode.rs` 的 `read_at`/`write_at` 比对，以及 `ls`/`lookup` 等 VFS 操作。但目标项目的 `easy-fs` 是独立实现的简单文件系统，参考项目使用 FAT32 实现，底层块设备访问逻辑不同。目标项目额外支持 ext4 文件系统（`os/src/fs/vfs/ext4/`），参考项目仅有 FAT32 简化版。

### 设备驱动
- **双方均存在**: 是
- **共同 AST 指纹**: 0 个
- **覆盖率**: 目标 0.0%，参考 0.0%
- **证据强度**: **未形成**

双方均有设备驱动模块，但未观察到任何共同 AST 指纹。目标项目使用 `cached_block_device.rs` 实现缓存块设备，参考项目使用 `virtio_blk.rs` 直接操作 VirtIO 块设备，驱动架构完全不同。

### 网络协议栈
- **双方均存在**: 否（仅目标项目有）
- **共同 AST 指纹**: 0 个
- **覆盖率**: 目标 0.0%，参考 0.0%
- **证据强度**: **未形成**

目标项目拥有完整的网络协议栈模块（`os/src/net/mod.rs`、`os/src/net/unix_socket.rs`），包含 UDP 套接字、Unix Domain Socket 等实现。参考项目无任何网络协议栈代码。这是目标项目独立开发的重要正向证据。

### 第三方库/公共依赖
- **双方均存在**: 是（但内容不同）
- **共同 AST 指纹**: 0 个
- **覆盖率**: 目标 0.0%，参考 0.0%
- **证据强度**: **未形成**

目标项目的第三方库为 ext4 文件系统实现（`os/src/fs/vfs/ext4/`），参考项目的第三方库为 `visionfive2-sd`（StarFive VisionFive2 开发板的 SD 卡驱动）。两者功能、硬件平台、代码来源完全不同，无任何重叠。

---

## 3. 底层 AST 关键证据指纹表

| 所属板块 | 目标项目路径 | 参考项目路径 | 证据类型 | 技术说明 |
| :--- | :--- | :--- | :--- | :--- |
| 进程管理 | `os/src/task/processor.rs#new` | `os/src/task/processor.rs#new` | 同板块同名 | 均调用 `TaskContext::zero_init`，这是 rCore 框架中初始化任务上下文的标准 API，任何基于 rCore 的项目都会使用 |
| 进程管理 | `os/src/task/processor.rs#current_user_token` | `os/src/task/processor.rs#current_user_token` | 同板块同名 | 调用链 `current_task().unwrap().get_user_token()` 由 Rust `Option` 类型和 rCore `TaskControlBlock` 结构决定，属标准 getter 模式 |
| 进程管理 | `os/src/task/processor.rs#take_current` | `os/src/task/processor.rs#take_current` | 同板块同名 | `self.current.take()` 是 Rust `Option<T>` 所有权移出的唯一标准方法 |
| 进程管理 | `os/src/task/processor.rs#current_kstack_top` | `os/src/task/processor.rs#current_kstack_top` | 同板块同名 | `kstack.get_top()` 是 rCore 内核栈的标准 API，字段命名源于教学框架 |
| 进程管理 | `os/src/task/processor.rs#current_trap_cx` | `os/src/task/processor.rs#current_trap_cx` | 同板块同名 | 目标使用字段访问 `.trap_cx`，参考使用方法调用 `.get_trap_cx()`，AST 节点类型不同，反映实现方式差异 |
| 进程管理 | `os/src/syscall/process.rs#sys_getpid` | `os/src/syscall/process.rs#sys_getpid` | 同板块同名 | POSIX 标准系统调用，目标使用 `current_process`，参考使用 `current_task().unwrap()`，进程模型不同 |
| 进程管理 | `os/src/syscall/process.rs#sys_exit` | `os/src/syscall/process.rs#sys_exit` | 同板块同名 | 均调用 `exit_current_and_run_next` 和 `panic!`，这是 rCore 标准退出流程 |
| 内存管理 | `os/src/mm/memory_set.rs#from_existed_user` | `os/src/mm/memory_set.rs#from_existed_user` | 同板块同名 | rCore 框架标准 API，用于 fork 时复制地址空间，命名和功能由框架定义 |
| 内存管理 | `os/src/task/process.rs#fork` | `os/src/task/task.rs#fork` | 同板块同名 | 均使用 `Arc::new`、`MemorySet::from_existed_user` 等标准模式，但目标项目额外包含 `IntervalTimerState`、`RecycleAllocator` 等独有组件 |
| 内存管理 | `os/src/task/mod.rs#exit_current_and_run_next` | `os/src/task/mod.rs#exit_current_and_run_next` | 同板块同名 | rCore 标准退出函数，目标项目包含 `FutexKey::new`、`PageTable::from_token` 等额外逻辑 |
| 系统调用 | `user/src/syscall.rs#syscall` | `user/src/syscall.rs#syscall` | 同板块同名 | 均仅含 `asm!` 宏，由 RISC-V `ecall` 指令规范决定 |
| 系统调用 | `user/src/lib.rs#exec` | `user/src/lib.rs#exec` | 同板块同名 | 用户态 `exec` 包装函数，调用 `sys_exec`，属标准用户库模式 |
| 中断/异常处理 | `arch/src/riscv64/trap/mod.rs#enable_timer_interrupt` | `os/src/trap/mod.rs#enable_timer_interrupt` | 同板块同名 | 均调用 `sie::set_stimer`，由 RISC-V 特权架构规范唯一决定 |
| 文件系统 | `easy-fs/src/layout.rs#read_at` | `os/src/fs/fat32/inode.rs#read_at` | 同板块同名 | 均实现块设备读取，但目标使用 `get_block_cache`，参考使用 `fs.read_cluster`，底层存储模型不同 |
| 文件系统 | `easy-fs/src/vfs.rs#ls` | `os/src/fs/fat32/inode.rs#ls` | 同板块同名 | 均实现目录列表，但目标使用 `DirEntry::empty`，参考使用 `fs.fat.cluster_id_to_sector_id`，实现路径不同 |
| 设备驱动 | `os/src/drivers/block/mod.rs#block_device_test` | `os/src/drivers/block/mod.rs#block_device_test` | 同板块同名 | 均含 `assert_eq!`、`println!` 测试逻辑，属通用测试模式，无共同 AST 指纹 |
| 通用模块/其他 | `user/src/lib.rs#_start` | `user/src/lib.rs#_start` | 同板块同名 | 用户态入口点，均含 `HEAP.lock().init()` 和 argv 解析，源于 rCore 用户库模板 |
| 通用模块/其他 | `os/src/sync/mutex.rs#lock` | `os/src/sync/mutex/spin_mutex.rs#lock` | 同板块同名 | 目标使用 `suspend_current_and_run_next` 实现阻塞锁，参考使用 `compare_exchange` 自旋，同步策略根本不同 |

---

## 4. 核心代码调用流对比剖析

### 【进程管理/调度器核心】

**底层技术析理**:
目标项目与参考项目在进程调度器的核心抽象上存在根本差异。目标项目采用**进程中心模型**，通过 `current_process()` 获取 `Process` 对象，进程包含多个任务/线程；参考项目采用**任务中心模型**，通过 `current_task().unwrap().inner_exclusive_access(file!(), line!())` 直接操作 `TaskControlBlock`。在 PID 管理上，目标项目封装了 `with_pid2pcb_read/write` 闭包式锁接口，参考项目直接使用 `PID2PCB.exclusive_access(file!(), line!())` 全局锁。在调度切换上，目标项目调用 `arch::switch_to_idle` 架构抽象层，参考项目调用内部 `__schedule` 函数。这些差异表明两者在**抽象层次、锁策略、架构解耦**上做出了独立的设计选择。

**代码溯源链接**:
- 目标项目: `os/src/task/processor.rs#schedule`
- 参考项目: `os/src/task/processor.rs#schedule`

---

### 【内存管理/地址空间复制】

**底层技术析理**:
`from_existed_user` 是 rCore 框架中用于 `fork` 时复制父进程地址空间的标准 API。双方均使用 `Arc` 共享物理页帧、`MapArea::from_another` 复制映射区域，这是 rCore 教学框架的标准实现路径。但目标项目在 `fork` 中额外初始化了 `IntervalTimerState::default()`、`RecycleAllocator::new()`、`SignalFlags::empty()` 等组件，参考项目则包含 `PTEFlags::from_bits` 等 RISC-V 页表标志操作。目标项目的 `exit_current_and_run_next` 包含 `FutexKey::new` 唤醒逻辑，参考项目无此功能。两者在基础框架之上各自扩展了不同的功能维度。

**代码溯源链接**:
- 目标项目: `os/src/mm/memory_set.rs#from_existed_user`
- 参考项目: `os/src/mm/memory_set.rs#from_existed_user`

---

### 【系统调用/用户态封装】

**底层技术析理**:
用户态系统调用封装（`user/src/syscall.rs`、`user/src/lib.rs`）是双方相似度最高的区域。`syscall` 函数均仅含 `asm!` 宏执行 `ecall` 指令，由 RISC-V 系统调用规范唯一决定。`exec`、`waittid` 等包装函数均遵循 `sys_xxx` 命名模式，这是 rCore 用户库的标准约定。目标项目的 `sys_exec` 额外支持 `envp` 参数（环境变量指针），参考项目仅支持 `args`，功能维度已有差异。

**代码溯源链接**:
- 目标项目: `user/src/syscall.rs#syscall`
- 参考项目: `user/src/syscall.rs#syscall`

---

### 【中断/异常处理/定时器】

**底层技术析理**:
`enable_timer_interrupt` 双方均仅调用 `sie::set_stimer`，这是 RISC-V 特权架构规范中开启定时器中断的唯一标准 CSR 操作。目标项目额外支持 LoongArch64 架构的 `trap_enable_timer_interrupt`（调用 `enable_irq`），参考项目仅支持 RISC-V64。目标项目的中断处理模块体量（90 个函数）远超参考项目（5 个函数），包含非对齐访存模拟（`emulate_load_store_insn`）等独立功能。

**代码溯源链接**:
- 目标项目: `arch/src/riscv64/trap/mod.rs#enable_timer_interrupt`
- 参考项目: `os/src/trap/mod.rs#enable_timer_interrupt`

---

### 【文件系统/块设备读写】

**底层技术析理**:
双方在文件系统层的共同指纹主要来自 `read_at`/`write_at` 等块设备读写操作。目标项目的 `easy-fs` 使用 `get_block_cache` 缓存块设备访问，参考项目的 FAT32 实现使用 `fs.read_cluster`/`fs.write_cluster` 簇链操作。目标项目额外实现了完整的 ext4 文件系统（`os/src/fs/vfs/ext4/`），包含 inode 缓存、路径解析、扩展属性等，参考项目仅有简化 FAT32 实现。两者在文件系统架构的复杂度和实现路径上存在显著差异。

**代码溯源链接**:
- 目标项目: `easy-fs/src/layout.rs#read_at`
- 参考项目: `os/src/fs/fat32/inode.rs#read_at`

---

## 5. 单方模块与降权项

### 目标项目独有模块（正向独立证据）

| 模块 | 代表文件 | 说明 |
| :--- | :--- | :--- |
| **网络协议栈** | `os/src/net/mod.rs` | 完整 TCP/IP 协议栈（基于 smoltcp），含 UDP 套接字、loopback 接口 |
| **Unix Domain Socket** | `os/src/net/unix_socket.rs` | 完整的 Unix 域套接字实现，含流式和数据报模式 |
| **多架构支持** | `arch/src/loongarch64/` | 独立的 LoongArch64 架构支持，含陷阱处理、非对齐访存模拟、SBI 调用 |
| **ext4 文件系统** | `os/src/fs/vfs/ext4/` | 完整的 ext4 只读/读写支持，含 inode 缓存、路径解析 |
| **Linux capabilities** | `os/src/syscall/process.rs#sys_capget` | Linux 权能系统调用 |
| **ptrace 支持** | `os/src/syscall/process.rs#sys_waitid` | 包含 `take_ptrace_stop_signal`、`child_wait_event` 等 ptrace 相关逻辑 |
| **进程凭证系统** | `os/src/syscall/process.rs#sys_prctl` | `credentials_snapshot`、`with_identity` 等进程身份管理 |

### 参考项目独有模块

| 模块 | 代表文件 | 说明 |
| :--- | :--- | :--- |
| **VisionFive2 SD 卡驱动** | `os/libs/visionfive2-sd/src/lib.rs` | StarFive VisionFive2 开发板的 SD 卡控制器驱动，目标项目无此硬件支持 |

### 降权项说明

以下证据类型因不具备区分度，降权为弱信号或不予采信：

1. **泛型宏调用**: `trace!`、`debug!`、`println!`、`panic!`、`file!()`、`line!()`、`asm!` 等是 Rust 生态中的通用宏，在任何 Rust OS 项目中均会出现，不能作为同源证据。
2. **POSIX 同名系统调用**: `sys_getpid`、`sys_exit`、`sys_yield`、`sys_kill` 等函数名由 POSIX 标准规定，任何实现 Linux 兼容层的 OS 都会使用相同命名。
3. **rCore 框架标准 API**: `TaskContext::zero_init`、`MemorySet::from_existed_user`、`exit_current_and_run_next`、`INITPROC.clone` 等是 rCore 教学框架定义的标准接口，基于该框架的项目必然使用相同 API。
4. **Rust 语言惯用法**: `self.current.take()`、`Arc::new`、`Option::unwrap`、`copy_from_slice` 等是 Rust 标准库提供的唯一或最佳实践写法。
5. **RISC-V 架构规范**: `sie::set_stimer`、`stvec::write`、`sstatus::clear_sum` 等 CSR 操作由 RISC-V 特权架构规范唯一决定。
6. **跨板块误归类证据**: `sys_linkat`、`sys_ioctl`、`sys_ppoll`、`sys_unlinkat` 等文件系统系统调用被归入“任务调度/异步运行时”板块，属于证据对齐错误，不予采信。

---

## 6. 保守结论

本案双方 AST 相似度仅 5.13%，目标项目覆盖率 7.02%，参考项目覆盖率 16.04%，均处于极低水平。128 个共同 AST 指纹经逐项审查，均属于 rCore 教学框架标准 API、Rust 语言惯用法、POSIX 标准命名或 RISC-V 架构规范，不具备代码复用的证据效力。目标项目拥有网络协议栈、Unix Socket、多架构支持、ext4 文件系统、Linux capabilities 等大量参考项目完全不具备的独立模块，整体架构呈独立演进态势。双方在进程模型（`current_process` vs `current_task().unwrap().inner_exclusive_access`）、同步策略（闭包锁 vs 全局 `UPSafeCell`）、调度后端（`arch::switch_to_idle` vs `__schedule`）等核心设计上存在根本差异。综合判定：**目标项目为独立开发，不存在代码复用或抄袭行为**。

------------------------------

