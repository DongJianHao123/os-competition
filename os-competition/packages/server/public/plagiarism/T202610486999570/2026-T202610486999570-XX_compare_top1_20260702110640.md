# 🛡️ OS 内核赛自动化代码级架构审计报告

**审计时间**: `2026-07-02 11:06:40`
**参赛作品名称**: `2026-T202610486999570-XX`
**年份**: `2026`
**学校**: `XX`
**队伍名称**: `T202610486999570`
**比赛名字**: `OS-Competition`
**比赛子赛道**: `Kernel-Implementation`
**仓库地址**: `https://gitlab.eduxiji.net/educg-group-43501-3132633/T202610486999570-2543`
**推测开发语言**: `Rust / C (由AST探针自动识别)`
**特征函数量**: `2241`

## ⚔️ 底层代码同源性对比 (AST Homology)
> 本部分由专门的代码审查智能体，对该项目与符合年份条件的历届最高相似度项目进行控制流与底层调用对比。新版报告会先给出确定性的全模块覆盖矩阵，再给出 Agent 的分模块解释，避免只围绕少量抽样函数展开。

### 🏆 Top 1 底层对标: vs `2022-S0S-哈尔滨工业大学`（哈尔滨工业大学 / S0S）

#### 对方作品信息

**对方年份**: `2022`
**对方学校**: `哈尔滨工业大学`
**对方队伍名称**: `S0S`
**对方比赛名字**: `OS-Competition`
**对方比赛子赛道**: `Kernel-Implementation`
**对方仓库地址**: `https://gitlab.eduxiji.net/educg-group-14158-894147/oskernel2022-s0s`

> 双向覆盖率：共同 AST 指纹: 112；唯一 AST 指纹: 目标项目 1824，参考项目 447；覆盖率: 目标项目 6.14%，参考项目 25.06%

## 全模块对比总览（确定性证据）

> 总体覆盖率：共同 AST 指纹: 112；唯一 AST 指纹: 目标项目 1824，参考项目 447；覆盖率: 目标项目 6.14%，参考项目 25.06%

### 1. 模块覆盖矩阵
| 模块 | 目标函数数 | 参考函数数 | 共同唯一 AST 指纹 | Target 覆盖 | Base 覆盖 | 状态 |
| :--- | ---: | ---: | ---: | ---: | ---: | :--- |
| 进程管理 | 406 | 51 | 8 | 2.63% | 17.02% | 双方存在且有结构重合 |
| 任务调度/异步运行时 | 258 | 41 | 7 | 3.04% | 17.5% | 双方存在且有结构重合 |
| 内存管理 | 484 | 139 | 60 | 14.56% | 47.24% | 双方存在且有结构重合 |
| 中断/异常处理 | 90 | 5 | 2 | 2.6% | 50.0% | 双方存在且有结构重合 |
| 系统调用 | 203 | 35 | 4 | 2.53% | 16.67% | 双方存在且有结构重合 |
| 文件系统 | 444 | 166 | 15 | 4.16% | 10.07% | 双方存在且有结构重合 |
| 设备驱动 | 78 | 42 | 1 | 1.43% | 2.63% | 双方存在且有结构重合 |
| 网络协议栈 | 28 | 0 | 0 | 0.0% | 0.0% | 仅目标项目体现 |
| 文件系统/第三方库 | 83 | 0 | 0 | 0.0% | 0.0% | 仅目标项目体现 |
| 网络协议栈/第三方库 | 1 | 0 | 0 | 0.0% | 0.0% | 仅目标项目体现 |
| 通用模块/其他 | 166 | 31 | 11 | 7.38% | 37.93% | 双方存在且有结构重合 |

### 2. 分模块证据清单

#### 进程管理
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `os/src/syscall/process.rs#sys_nanosleep` | `os/src/syscall/process.rs#sys_nanosleep` | 同板块同名 | Some, TimeSpec::default, cancel_related.is_empty, copy_to_user, core::mem::size_of::<TimeSpec>, core::slice::from_raw_parts ...(+20) | current_user_token, get_time_ns, get_time_s, suspend_current_and_run_next, translated_refmut |
| `os/src/syscall/process.rs#sys_sigaction` | `os/src/syscall/process.rs#sys_sigaction` | 同板块同名 | action.is_null, copy_to_user, core::mem::size_of::<SignalAction>, core::mem::size_of::<usize>, core::slice::from_raw_parts, crate::syscall::should_trace_syscall ...(+11) | SignalFlags::from_bits, SignalFlags::from_bits(40).unwrap, check_sigaction_error, current_task, current_user_token, task.inner_exclusive_access ...(+2) |
| `os/src/syscall/process.rs#sys_sigprocmask` | `os/src/syscall/process.rs#sys_sigprocmask` | 同板块同名 | copy_to_user, core::mem::size_of::<usize>, core::slice::from_raw_parts, crate::syscall::should_trace_syscall, current_process, current_task ...(+14) | SignalFlags::from_bits, current_task, old_mask.bits, task.inner_exclusive_access |
| `os/src/task/processor.rs#run_tasks` | `os/src/task/processor.rs#run_tasks` | 同板块同名 | PROCESSOR.exclusive_access, RUN_TASKS_EMPTY_COUNTER.fetch_add, Some, arch::disable_interrupts, arch::enable_interrupts, arch::switch_to_task ...(+9) | PROCESSORS[hartid()].lock, Some, __switch, drop, fetch_task, hartid ...(+2) |

#### 任务调度/异步运行时
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `os/src/syscall/fs.rs#sys_readlinkat` | `os/src/syscall/fs.rs#sys_readlinkat` | 同板块同名 | buf.is_null, bytes.len, bytes.len().min, check_search_permissions, copy_to_user, crate::syscall::should_trace_syscall ...(+21) | "/".as_bytes, UserBuffer::new, buf.read, current_user_token, println!, translated_byte_buffer ...(+2) |
| `os/src/syscall/fs.rs#sys_getdents64` | `os/src/syscall/fs.rs#sys_getdents64` | 同板块同名 | (reclen as u16).to_le_bytes, Vec::new, align_up, buf.is_null, copy_to_user, crate::syscall::should_trace_syscall ...(+23) | UserBuffer::new, buf.read, current_task, current_task().unwrap, current_user_token, inner.fd_table.len ...(+4) |
| `os/src/syscall/fs.rs#sys_lseek` | `os/src/syscall/fs.rs#sys_lseek` | 同板块同名 | crate::syscall::should_trace_syscall, current_process, errno, file.get_offset, file.get_offset().unwrap_or, file.inode ...(+8) | current_task, current_task().unwrap, inner.fd_table.len, inode.offset, inode.seek, inode.size ...(+1) |
| `os/src/syscall/fs.rs#sys_chdir` | `os/src/syscall/fs.rs#sys_chdir` | 同板块同名 | String::from, _path.is_null, component.is_empty, component.len, crate::syscall::should_trace_syscall, current_process ...(+14) | current_task, current_task().unwrap, current_user_token, open_file_at, open_file_at(AT_FDCWD, &path, OpenFlags::RDWR).unwrap, remove_from_file_table ...(+2) |

#### 内存管理
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `os/src/syscall/process.rs#sys_mmap` | `os/src/syscall/process.rs#sys_mmap` | 同板块同名 | Err, Ok, Some, VirtAddr, crate::syscall::should_trace_syscall, current_process ...(+28) | MapFlags::from_bits, MapFlags::from_bits(flags).unwrap, ProtFlags::from_bits, ProtFlags::from_bits(prot).unwrap, UserBuffer::new, Vec::new ...(+20) |
| `os/src/syscall/fs.rs#sys_fcntl` | `os/src/syscall/fs.rs#sys_fcntl` | 同板块同名 | Some, arg.max, copy_to_user, core::mem::size_of::<Flock>, core::slice::from_raw_parts, crate::syscall::should_trace_syscall ...(+27) | current_task, current_task().unwrap, inner.alloc_fd, inner.fd_table.len, inner.fd_table[fd].clone, inner.fd_table[fd].is_some ...(+1) |
| `os/src/task/process.rs#fork` | `os/src/task/task.rs#fork` | 同板块同名 | Arc::clone, Arc::downgrade, Arc::new, IntervalTimerState::default, RecycleAllocator::new, SignalFlags::empty ...(+31) | Arc::downgrade, Arc::new, KernelStack::new, MemorySet::from_existed_user, Mutex::new, SignalFlags::empty ...(+20) |
| `os/src/task/mod.rs#exit_current_and_run_next` | `os/src/task/mod.rs#exit_current_and_run_next` | 同板块同名 | Arc::clone, Arc::downgrade, Arc::ptr_eq, FutexKey::new, INITPROC.push_child, PageTable::from_token ...(+48) | Arc::downgrade, INITPROC.inner_exclusive_access, Some, TaskContext::zero_init, child.clone, child.inner_exclusive_access ...(+13) |

#### 中断/异常处理
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `arch/src/riscv64/trap/mod.rs#enable_timer_interrupt` | `os/src/trap/mod.rs#enable_timer_interrupt` | 同板块同名同结构 | sie::set_stimer | sie::set_stimer |
| `arch/src/loongarch64/trap.rs#trap_enable_timer_interrupt` | `os/src/trap/mod.rs#init` | 同板块同结构 | enable_irq | set_kernel_trap_entry |
| `arch/src/loongarch64/unaligned.rs#emulate_load_store_insn` | `os/src/trap/mod.rs#trap_from_kernel` | 同模块上下文对齐/差异观察 | badv::read, badv::read().raw, panic!, unaligned_read, unaligned_write | panic!, println! |
| `os/src/sync/up.rs#exclusive_access` | `os/src/trap/mod.rs#set_kernel_trap_entry` | 同模块上下文对齐/差异观察 | INTR_MASKING_INFO.get_mut, INTR_MASKING_INFO.get_mut().enter, Some, caller.file, caller.line, core::panic::Location::caller ...(+5) | stvec::write |

#### 系统调用
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `user/src/syscall.rs#syscall` | `user/src/syscall.rs#syscall` | 同板块同名 | core::arch::asm! | asm! |
| `user/src/syscall.rs#sys_exec` | `user/src/syscall.rs#sys_exec` | 同板块同名 | args.as_ptr, envp.as_ptr, path.as_ptr, syscall6 | args.as_ptr, path.as_ptr, syscall |
| `user/src/lib.rs#exec` | `user/src/lib.rs#exec` | 同板块同名 | args.is_empty, core::ptr::null, sys_exec | sys_exec |
| `user/src/lib.rs#sigaction` | `user/src/lib.rs#sigaction` | 同板块同名 | action.map_or, core::ptr::null, core::ptr::null_mut, old_action.map_or, sys_sigaction | sys_sigaction |

#### 文件系统
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `easy-fs/src/layout.rs#read_at` | `fat32/src/vfs.rs#read_at` | 同板块同名 | (offset + buf.len()).min, Arc::clone, buf.len, dst.copy_from_slice, end_current_block.min, get_block_cache ...(+3) | (offset + buf.len()).min, Arc::clone, buf.len, buf.len().min, dst.copy_from_slice, end_current_block.min ...(+12) |
| `easy-fs/src/layout.rs#write_at` | `fat32/src/vfs.rs#write_at` | 同板块同名 | (offset + buf.len()).min, Arc::clone, assert!, buf.len, dst.copy_from_slice, end_current_block.min ...(+4) | (offset + buf.len()).min, Arc::clone, buf.len, buf.len().min, dst.copy_from_slice, end_current_block.min ...(+17) |
| `os/src/fs/vfs/fat32/disk.rs#seek` | `os/src/fs/inode.rs#seek` | 同板块同名 | Err, Ok, Some, cur.checked_add, match pos {
            SeekFrom::Start(off) => Some(off as i64),
            SeekFrom::End(off) => self.total_bytes.map(|t| t as i64 + off),
            SeekFrom::Current(off) => cur.checked_add(off),
        }
        .ok_or, self.position ...(+2) | self.inner_exclusive_access |
| `os/src/fs/vfs/fat32/inode.rs#list` | `fat32/src/vfs.rs#list` | 同板块同名 | Vec::new, dir.iter, entry.file_name, fat32_open_dir, names.push, self.fs.exclusive_access | ShortDirEntry::empty, Vec::new, dirent.is_some, dirent.unwrap, dirent.unwrap().short_dir_entry.clone, res.push ...(+9) |

#### 设备驱动
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `os/src/drivers/block/mod.rs#block_device_test` | `os/src/drivers/block/mod.rs#block_device_test` | 同板块同名同结构 | BLOCK_DEVICE.clone, assert_eq!, block_device.read_block, block_device.write_block, println!, write_buffer.iter_mut | BLOCK_DEVICE.clone, assert_eq!, block_device.read_block, block_device.write_block, println!, write_buffer.iter_mut |
| `os/src/drivers/block/cached_block_device.rs#write_block` | `os/src/drivers/block/virtio_blk.rs#write_block` | 同板块同名 | blocks[block_id].copy_from_slice, self.blocks.lock, self.write_count.lock | self.0
            .lock, self.0
            .lock()
            .write_block, self.0
            .lock()
            .write_block(block_id, buf)
            .expect |
| `os/src/drivers/block/cached_block_device.rs#read_block` | `os/src/drivers/block/virtio_blk.rs#read_block` | 同板块同名 | buf.copy_from_slice, self.blocks.lock, self.read_count.lock | self.0
            .lock, self.0
            .lock()
            .read_block, self.0
            .lock()
            .read_block(block_id, buf)
            .expect |
| `os/src/drivers/block/virtio_blk_pci.rs#read_block` | `os/src/drivers/block/sdcard.rs#read_block` | 同板块同名 | self.blk
            .lock, self.blk
            .lock()
            .read_blocks, self.blk
            .lock()
            .read_blocks(block_id, buf)
            .expect | self.0.lock, self.0.lock().read_sector, self.0.lock().read_sector(buf, block_id as u32).unwrap |

#### 网络协议栈
仅目标项目中观察到该模块代表函数，参考项目没有同模块可对齐证据：
- 目标项目：`os/src/net/mod.rs#loopback_udp_inject`，调用摘要：Some, connected_handle.or, stack.sockets.get_mut::<UdpSocket>, stack.sockets.iter, udp_sock.endpoint, udp_sock.inject_recv ...(+2)
- 目标项目：`os/src/net/unix_socket.rs#new`，调用摘要：Arc::new, AtomicBool::new, Mutex::new, String::new, VecDeque::new
- 目标项目：`os/src/net/unix_socket.rs#writable`，调用摘要：peer_weak.upgrade, peer_weak.upgrade().is_some, self.peer.lock, self.peer.lock().as_ref, self.state.lock
- 目标项目：`os/src/net/unix_socket.rs#write`，调用摘要：guard.as_ref, guard.as_ref().and_then, peer.unix_push_rx_bytes, self.peer.lock, user_buf.buffers.iter, w.upgrade

#### 文件系统/第三方库
仅目标项目中观察到该模块代表函数，参考项目没有同模块可对齐证据：
- 目标项目：`os/src/fs/vfs/ext4/inode.rs#write_at`，调用摘要：Some, Vec::with_capacity, error!, ext4_cache_touch_write_extend, file.file_seek, file.file_seek(offset as i64, SEEK_SET).is_err ...(+11)
- 目标项目：`os/src/fs/vfs/ext4/inode.rs#ext4_raw_metadata`，调用摘要：Err, Ok, c_path.as_ptr, core::mem::zeroed, core::ptr::addr_of!, ext4_errno ...(+5)
- 目标项目：`os/src/fs/vfs/ext4/disk.rs#write_one`，调用摘要：DEV_READ_OPS.fetch_add, DEV_WRITE_OPS.fetch_add, KDEV_WRITE_CALLS.fetch_add, Ok, PARTIAL_WRITE_OPS.fetch_add, buf.len ...(+6)
- 目标项目：`os/src/fs/vfs/ext4/disk.rs#read_one`，调用摘要：DEV_READ_OPS.fetch_add, KDEV_READ_CALLS.fetch_add, Ok, PARTIAL_READ_OPS.fetch_add, buf.len, buf.len().min ...(+5)

#### 网络协议栈/第三方库
仅目标项目中观察到该模块代表函数，参考项目没有同模块可对齐证据：
- 目标项目：`os/src/net/mod.rs#smoltcp_now`，调用摘要：get_time_ms, smoltcp::time::Instant::from_millis

#### 通用模块/其他
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `user/src/lib.rs#_start` | `user/src/lib.rs#_start` | 同板块同名 | ((argv + i * core::mem::size_of::<usize>()) as *const usize).read_volatile, ((str_start + *i) as *const u8).read_volatile, (0usize..)
            .find, (0usize..)
            .find(|i| unsafe { ((str_start + *i) as *const u8).read_volatile() == 0 })
            .unwrap, HEAP.lock, HEAP.lock()
            .init ...(+11) | ((argv + i * core::mem::size_of::<usize>()) as *const usize).read_volatile, ((str_start + *i) as *const u8).read_volatile, (0usize..)
            .find, (0usize..)
            .find(|i| unsafe { ((str_start + *i) as *const u8).read_volatile() == 0 })
            .unwrap, HEAP.lock, HEAP.lock()
            .init ...(+10) |
| `arch/src/loongarch64/sbi.rs#shutdown` | `os/src/sbi.rs#shutdown` | 同板块同名 | core::hint::spin_loop, core::ptr::write_volatile, warn! | panic!, sbi_call |
| `arch/src/riscv64/sbi.rs#sbi_call` | `os/src/sbi.rs#sbi_call` | 同板块同名 | asm! | asm! |
| `user/src/console.rs#write_str` | `os/src/console.rs#write_str` | 同板块同名 | Err, Ok, s.as_bytes, s.as_bytes().iter, self.0.len, self.0.push_back ...(+1) | Ok, console_putchar, s.chars |


好的，审计官。基于提供的全案材料，我将依据审计指令，对目标项目 `[2026-T202610486999570-XX]` 与参考项目 `[2022-S0S-哈尔滨工业大学]` 进行最终的同源性定性仲裁。

### 1. 宏观判定
- **定性结论**: **独立开发 (相似度: 5.19%，共同 AST 指纹: 112；唯一 AST 指纹: 目标项目 1824，参考项目 447；覆盖率: 目标项目 6.14%，参考项目 25.06%)**
- **判定依据**: 双方的整体 AST 相似度极低（5.19%），目标项目仅有 6.14% 的代码与参考项目共享指纹，其 93.86% 的代码库为独有。参考项目 25.06% 的覆盖率源于其自身规模较小，其核心骨架在遵循同一教学范式（rCore）的目标项目中得到自然体现，但这不代表目标项目对参考项目存在大规模抄袭。双方在核心架构、内存模型、功能广度上存在本质差异。

### 2. 全模块覆盖矩阵解读
| 模块 | 双方存在 | 共同指纹 | 覆盖率解读 | 证据强度 |
| :--- | :---: | :---: | :--- | :---: |
| **进程管理** | 是 | 8 | 目标覆盖率 2.63%，参考覆盖率 17.02%。目标项目函数量（406）远超参考（51），极低的覆盖率表明目标项目进行了大规模独立扩展。参考项目的高覆盖率是因为其进程管理模块较小，基础部分被目标项目自然覆盖。 | **弱** |
| **任务调度/异步运行时** | 是 | 7 | 目标覆盖率 3.04%，参考覆盖率 17.5%。情况与进程管理类似，目标项目规模（258）远超参考（41）。共同指纹集中在调度器的公共逻辑上。 | **弱** |
| **内存管理** | 是 | 60 | 目标覆盖率 14.56%，参考覆盖率 47.24%。这是共同指纹最多的模块，但目标项目函数数（484）远超参考（139）。高覆盖率源于内存管理基础算法（如页表操作、分配器）的公共实现范式，不代表抄袭。 | **中** |
| **中断/异常处理** | 是 | 2 | 目标覆盖率 2.6%，参考覆盖率 50.0%。参考项目模块极小（5个函数），导致覆盖率虚高。共同指纹为架构强相关的寄存器操作，属于公共知识。 | **弱** |
| **系统调用** | 是 | 4 | 目标覆盖率 2.53%，参考覆盖率 16.67%。共同指纹为用户态侧的 syscall 封装，这是由 ABI 和指令集决定的 trivial code，无独创性。 | **弱** |
| **文件系统** | 是 | 15 | 目标覆盖率 4.16%，参考覆盖率 10.07%。目标项目（444）规模远超参考（166），且包含 ext4 等独有实现。共同指纹集中在块设备读写循环等基础逻辑上。 | **弱** |
| **设备驱动** | 是 | 1 | 目标覆盖率 1.43%，参考覆盖率 2.63%。覆盖率极低，唯一的共同指纹 `block_device_test` 是功能验证代码，逻辑固定。 | **未形成** |
| **网络协议栈** | 仅目标 | 0 | 目标项目独有模块（28个函数），参考项目完全不存在。这是目标项目独立演进的**强证据**。 | **未形成** |
| **文件系统/第三方库** | 仅目标 | 0 | 目标项目独有模块（83个函数），如 ext4 支持，参考项目完全不存在。这是目标项目独立演进的**强证据**。 | **未形成** |
| **网络协议栈/第三方库** | 仅目标 | 0 | 目标项目独有模块（1个函数），参考项目完全不存在。 | **未形成** |
| **通用模块/其他** | 是 | 11 | 目标覆盖率 7.38%，参考覆盖率 37.93%。共同指纹集中在用户态启动、panic 处理、sbi 调用等高度范式化或架构强相关的代码上。 | **弱** |

### 3. 底层 AST 关键证据指纹表
| 所属板块 | 目标项目路径 | 参考项目路径 | 证据类型 | 技术说明 |
| :--- | :--- | :--- | :--- | :--- |
| 内存管理 | `os/src/syscall/process.rs#sys_mmap` | `os/src/syscall/process.rs#sys_mmap` | 同板块同名 | 双方均实现 mmap，但目标项目使用 `VirtAddr`、`copy_to_user` 等 Linux 风格接口，参考项目使用 `MapFlags::from_bits`、`UserBuffer` 等 rCore 风格接口，内存模型不同。 |
| 内存管理 | `os/src/task/process.rs#fork` | `os/src/task/task.rs#fork` | 同板块同名 | 双方均实现 fork，核心逻辑均包含 `Arc::new`、`MemorySet` 复制、`SignalFlags::empty` 等，但目标项目额外引入了 `RecycleAllocator`、`IntervalTimerState` 等独立组件。 |
| 内存管理 | `os/src/task/mod.rs#exit_current_and_run_next` | `os/src/task/mod.rs#exit_current_and_run_next` | 同板块同名 | 进程退出核心函数。目标项目调用图谱包含 `FutexKey::new`、`PageTable::from_token` 等 48+ 节点，参考项目仅 13+ 节点，逻辑复杂度和功能范围差异巨大。 |
| 文件系统 | `easy-fs/src/layout.rs#read_at` | `fat32/src/vfs.rs#read_at` | 同板块同名 | 块设备读取循环。双方均使用 `(offset + buf.len()).min`、`get_block_cache` 等模式，这是处理块不对齐读取的标准算法，属于公共知识。 |
| 文件系统 | `easy-fs/src/layout.rs#write_at` | `fat32/src/vfs.rs#write_at` | 同板块同名 | 与 `read_at` 同理，块设备写入循环的标准实现。 |
| 设备驱动 | `os/src/drivers/block/mod.rs#block_device_test` | `os/src/drivers/block/mod.rs#block_device_test` | 同板块同名同结构 | 块设备读写测试函数，逻辑为“写入->读出->断言”，是功能测试的标准模板，无独创性。 |
| 通用模块/其他 | `user/src/lib.rs#_start` | `user/src/lib.rs#_start` | 同板块同名 | 用户态启动入口。双方均通过 volatile 读取 argv/argc 并初始化堆，这是 rCore 教学范式的标准用户态启动流程。 |
| 通用模块/其他 | `arch/src/riscv64/sbi.rs#sbi_call` | `os/src/sbi.rs#sbi_call` | 同板块同名 | RISC-V SBI 调用封装，均使用内联汇编 `asm!`，这是由硬件规范决定的固定实现。 |
| 进程管理 | `os/src/task/processor.rs#run_tasks` | `os/src/task/processor.rs#run_tasks` | 同板块同名 | 调度器主循环。目标项目使用 `PROCESSOR.exclusive_access`（单核），参考项目使用 `PROCESSORS[hartid()].lock`（多核），架构设计不同。 |
| 进程管理 | `os/src/syscall/process.rs#sys_nanosleep` | `os/src/syscall/process.rs#sys_nanosleep` | 同板块同名 | 目标项目使用 `TimeSpec::default`、`copy_to_user` 等 Linux 风格接口，参考项目使用 `get_time_ns`、`translated_refmut` 等 rCore 风格接口，内存模型不同。 |

### 4. 核心代码调用流对比剖析
* **【进程管理/调度器主循环】**
* **底层技术析理**: 双方均实现了 `run_tasks` 函数作为调度核心，但体现了不同的并发与架构设计。目标项目采用单核模型，通过 `PROCESSOR.exclusive_access()` 获取全局调度器实例，并使用 `arch::switch_to_task` 进行上下文切换，同时维护 `RUN_TASKS_EMPTY_COUNTER` 统计信息。参考项目则采用多核模型，通过 `PROCESSORS[hartid()].lock()` 获取当前硬件线程的调度器，并直接调用 `__switch` 汇编标签进行切换。两者在并发控制（单核互斥 vs 多核锁）和底层切换原语（架构抽象层 vs 直接汇编）上存在根本性差异，表明调度器是独立设计而非简单复用。
* **代码溯源链接**:
  - 目标项目: `os/src/task/processor.rs#run_tasks`
  - 参考项目: `os/src/task/processor.rs#run_tasks`

* **【内存管理/进程退出】**
* **底层技术析理**: `exit_current_and_run_next` 是进程生命周期的核心函数。目标项目的调用图谱极为复杂，涉及 `FutexKey::new`（futex 机制）、`PageTable::from_token`（页表操作）、`INITPROC.push_child`（进程回收）等 48+ 个调用节点，显示其实现了完整的 POSIX 进程退出语义和资源清理逻辑。参考项目的调用图谱则相对简单（13+ 节点），主要完成基础的状态设置和子进程唤醒。两者在功能深度和复杂度上不在同一层级，目标项目的实现是其庞大进程管理子系统（406个函数）的冰山一角，绝非对参考项目的简单扩充。
* **代码溯源链接**:
  - 目标项目: `os/src/task/mod.rs#exit_current_and_run_next`
  - 参考项目: `os/src/task/mod.rs#exit_current_and_run_next`

* **【内存管理/用户态内存映射】**
* **底层技术析理**: `sys_mmap` 系统调用。目标项目使用 `VirtAddr` 直接操作地址，并通过 `copy_to_user` 进行用户态数据写入，体现了 Linux 风格的内存管理模型。参考项目则使用 `MapFlags::from_bits` 和 `ProtFlags::from_bits` 解析参数，并通过 `UserBuffer` 和 `translated_byte_buffer` 等 rCore 特有的“地址翻译”机制访问用户内存。两者在用户态内存访问的抽象层次和模型上存在本质区别，目标项目并未继承参考项目的核心内存访问范式。
* **代码溯源链接**:
  - 目标项目: `os/src/syscall/process.rs#sys_mmap`
  - 参考项目: `os/src/syscall/process.rs#sys_mmap`

### 5. 单方模块与降权项
* **单方模块（强独立证据）**:
  - **网络协议栈**: 目标项目包含完整的网络支持（`os/src/net/mod.rs` 等），参考项目无。
  - **文件系统/第三方库**: 目标项目集成了 ext4 文件系统（`os/src/fs/vfs/ext4/` 等），参考项目无。
  - **网络协议栈/第三方库**: 目标项目集成了 smoltcp 库的适配层，参考项目无。
  这些模块的存在是目标项目独立开发、功能远超参考项目的决定性证据。

* **降权项（不构成强同源证据）**:
  - **POSIX 系统调用名**: `sys_fork`、`sys_exec`、`sys_getpid` 等函数名由 ABI 标准强制规定，同名不能作为抄袭证据。
  - **Rust 惯用法**: `Option::take`、`Vec::new`、`Arc::clone`、`drop` 等是 Rust 语言的标准库和惯用模式，AST 相似是语言特性使然。
  - **Trivial Code**: `processor.rs::new`（仅调用 `TaskContext::zero_init`）、`user/src/lib.rs::sigprocmask`（仅调用 `sys_sigprocmask`）等单行函数，其实现是唯一且无创造性的。
  - **公共教学范式**: `_start` 的用户态启动流程、`run_tasks` 的调度循环骨架、`read_at`/`write_at` 的块设备读写循环，均属于 rCore 教学操作系统及相关领域的公共知识，独立实现必然导致结构相似。
  - **跨板块证据**: 控方已自查排除，本仲裁不再考虑。

### 6. 保守结论
综合全案证据，目标项目 `[2026-T202610486999570-XX]` 与参考项目 `[2022-S0S-哈尔滨工业大学]` 的 AST 相似度仅为 5.19%，处于极低水平。双方共有的 112 个指纹高度集中在内存管理、文件系统等基础模块的公共算法实现、Rust 语言惯用法、POSIX 标准接口命名及 rCore 教学范式的自然表达上。目标项目不仅规模数倍于参考项目，更独立实现了网络协议栈、ext4 文件系统、ptrace 机制等参考项目完全不具备的复杂子系统。在核心架构层面，双方在进程模型（Process-centric vs Task-centric）、用户内存访问模型（copy_to_user vs translated_refmut）及并发模型（单核抽象 vs 多核数组）上存在本质性差异。因此，现有证据无法支持抄袭指控，应认定为基于公共知识领域的独立开发作品。

------------------------------

