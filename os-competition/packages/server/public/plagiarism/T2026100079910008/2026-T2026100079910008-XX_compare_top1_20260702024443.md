# 🛡️ OS 内核赛自动化代码级架构审计报告

**审计时间**: `2026-07-02 02:44:43`
**参赛作品名称**: `2026-T2026100079910008-XX`
**年份**: `2026`
**学校**: `XX`
**队伍名称**: `T2026100079910008`
**比赛名字**: `OS-Competition`
**比赛子赛道**: `Kernel-Implementation`
**仓库地址**: `https://gitlab.eduxiji.net/educg-group-43501-3132633/T2026100079910008-777`
**推测开发语言**: `Rust / C (由AST探针自动识别)`
**特征函数量**: `1758`

## ⚔️ 底层代码同源性对比 (AST Homology)
> 本部分由专门的代码审查智能体，对该项目与符合年份条件的历届最高相似度项目进行控制流与底层调用对比。新版报告会先给出确定性的全模块覆盖矩阵，再给出 Agent 的分模块解释，避免只围绕少量抽样函数展开。

### 🏆 Top 1 底层对标: vs `2024-你起吧-长春理工大学`（长春理工大学 / 你起吧）

#### 对方作品信息

**对方年份**: `2024`
**对方学校**: `长春理工大学`
**对方队伍名称**: `你起吧`
**对方比赛名字**: `OS-Competition`
**对方比赛子赛道**: `Kernel-Implementation`
**对方仓库地址**: `https://gitlab.eduxiji.net/educg-group-26010-2376550/T202410186992540-1599`

> 双向覆盖率：共同 AST 指纹: 148；唯一 AST 指纹: 目标项目 1456，参考项目 573；覆盖率: 目标项目 10.16%，参考项目 25.83%

## 全模块对比总览（确定性证据）

> 总体覆盖率：共同 AST 指纹: 148；唯一 AST 指纹: 目标项目 1456，参考项目 573；覆盖率: 目标项目 10.16%，参考项目 25.83%

### 1. 模块覆盖矩阵
| 模块 | 目标函数数 | 参考函数数 | 共同唯一 AST 指纹 | Target 覆盖 | Base 覆盖 | 状态 |
| :--- | ---: | ---: | ---: | ---: | ---: | :--- |
| 进程管理 | 73 | 37 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |
| 任务调度/异步运行时 | 148 | 22 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |
| 内存管理 | 223 | 140 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |
| 中断/异常处理 | 30 | 9 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |
| 系统调用 | 43 | 3 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |
| 文件系统 | 485 | 67 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |
| 设备驱动 | 17 | 14 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |
| 网络协议栈 | 0 | 0 | 0 | 0.0% | 0.0% | 双方均未体现 |
| 文件系统/第三方库 | 390 | 323 | 146 | 47.25% | 59.84% | 双方存在且有结构重合 |
| 通用模块/其他 | 349 | 71 | 1 | 0.41% | 1.64% | 双方存在且有结构重合 |

### 2. 分模块证据清单

#### 进程管理
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `os/src/syscall/process.rs#wait4_pred` | `kernel/proc.c#exit` | 同模块上下文对齐/差异观察 | SyscallResult::success, Waiting::new, child.time_stats.lock, child.wait_state.lock, children.front_mut, cur_child.get ...(+16) | acquire, fileclose, myproc, panic, printf, release ...(+4) |
| `os/src/syscall/process.rs#wait4` | `kernel/proc.c#scheduler` | 同模块上下文对齐/差异观察 | SyscallResult::success, WaitOptions::from_bits, WaitOptions::from_bits(options.try_into().map_err(|_| EINVAL)?).ok_or, Waiting::new, addr_eq, current_task ...(+19) | acquire, intr_on, mycpu, release, swtch |
| `os/src/multitask/task.rs#on_exit` | `kernel/proc.c#procdump` | 同模块上下文对齐/差异观察 | FileDescriptorTable::new, debug_assert!, futex_lock, futex_word.relaxed_atom_store, futex_word.wake, get_futex_key ...(+11) | NELEM, printf |
| `os/src/syscall/signal.rs#rt_sigtimedwait` | `kernel/proc.c#sleep` | 同模块上下文对齐/差异观察 | SyscallResult::success, copy_one_from_user, copy_one_from_user(set)?.difference, crate::syscall::basic::yield_, cur_task
                    .process
                    .signals_wl
                    .lock, cur_task
                    .process
                    .signals_wl
                    .lock()
                    .try_consume_one_with_mask ...(+12) | acquire, myproc, release, sched |

#### 任务调度/异步运行时
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `os/src/syscall/filesystem.rs#unlinkat` | `kernel/sysfile.c#unlinkat` | 同板块同名 | SyscallResult::success, copy_str_from_user, current_task, current_task().as_ref_unchecked, dentry
        .data
        .lock, dentry
        .data
        .lock()
        .parent
        .as_ref ...(+12) | makeatpath, myproc, rs_unlink |
| `edited_lib/rust-elf/src/to_str.rs#d_tag_to_str` | `kernel/virtio_disk.c#virtio_disk_intr` | 同模块上下文对齐/差异观察 | Some | R, __sync_synchronize, acquire, free_chain, panic, release ...(+1) |
| `os/src/filesystem/other/pipe.rs#kwrite` | `kernel/trap.c#usertrap` | 同模块上下文对齐/差异观察 | (len - first_count).min, Err, Ok, atomic_read, buffer.advance_write_index, buffer.vacant_len ...(+13) | devintr, exit, intr_on, killed, myproc, panic ...(+10) |
| `os/src/syscall/filesystem.rs#ppoll` | `kernel/console.c#consoleintr` | 同模块上下文对齐/差异观察 | DropGuard::new, PollStatus::POLLNVAL.bits, PollStatus::from_bits_retain, Some, SyscallResult::success, copy_one_from_user ...(+27) | C, acquire, consputc, procdump, release, wakeup |

#### 内存管理
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `os/src/syscall/process.rs#execve` | `kernel/exec.c#execve` | 同板块同名 | AuxiliaryEntry, ByteStr::new, File::get_page_cache, PathToWalk::auto, Some, SyscallResult::success ...(+56) | PGROUNDUP, alloc_aux, begin_op, copyout, copyoutstr, end_op ...(+15) |
| `os/src/syscall/memory.rs#mmap` | `kernel/proc.c#mmap` | 同板块同名 | (flags >> 2).try_into, (flags >> 2).try_into().map_err, MMapFlags::from_bits, MMapFlags::from_bits(flags).ok_or, MMapProtections::from_bits, MMapProtections::from_bits(prot).ok_or ...(+23) | PGROUNDUP, acquire, idup, ilock, iunlock, kalloc ...(+7) |
| `os/src/basic/global_allocator.rs#init_heap` | `third/buddy_system_allocator/benches/memory_allocator_benchmark.rs#init_heap` | 同板块同名 | GLOBAL_ALLOCATOR.0.lock, HashMap::new_in, PhysAddr, PhysAddr(0x00000000).to_virtual_address, PhysAddr(0x10000000).to_virtual_address, asm! ...(+9) | HEAP.as_ptr, HEAP_ALLOCATOR
            .lock, HEAP_ALLOCATOR
            .lock()
            .init |
| `os/src/basic/global_allocator.rs#alloc` | `third/buddy_system_allocator/src/lib.rs#alloc` | 同板块同名 | NonNull::new, foreach_filesystem, fs.full_gc_dentry, fs.gc_inode, fs.shrink_cache, fs.writeback ...(+12) | (class + 1..i + 1).rev, Err, NonNull::new, Ok, layout.align, layout.size ...(+11) |

#### 中断/异常处理
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `os/src/filesystem/vfs/result.rs#from` | `kernel/plic.c#plicinithart` | 同模块上下文对齐/差异观察 | InvalidArguments, InvalidUserPointer, todo!, value.error, value.msg.unwrap_or | PLIC_SENABLE, PLIC_SPRIORITY, cpuid |
| `os/src/trap/trap.rs#new` | `kernel/spinlock.c#pop_off` | 同模块上下文对齐/差异观察 | Exception::try_from, Exception::try_from(code).unwrap, Exception::try_from(ecode).unwrap, Interrupt::try_from, Interrupt::try_from((63 - is.leading_zeros()) as usize).unwrap, Interrupt::try_from(code).unwrap ...(+8) | intr_get, intr_on, mycpu, panic |
| `os/src/arch/loongarch64/basic.rs#execute_init` | `kernel/plic.c#plicinit` | 同模块上下文对齐/差异观察 | asm!, prmd.set_bit, prmd.set_bits | 内部闭环逻辑 |
| `os/src/arch/riscv64/basic.rs#execute_init` | `kernel/spinlock.c#push_off` | 同模块上下文对齐/差异观察 | asm!, sstatus::set_spp | intr_get, intr_off, mycpu |

#### 系统调用
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `programs/init/src/main.rs#main` | `kernel/syscall.c#argstr` | 同模块上下文对齐/差异观察 | b"-c\0".as_ptr, b"/dev/tty\0".as_ptr, b"/sdcd/musl/busybox\0".as_ptr, b"PATH=/bin\0".as_ptr, b"sh\0".as_ptr, concat_bytes! ...(+7) | argaddr, fetchstr |
| `os/src/syscall/sync.rs#futex` | `kernel/syscall.c#argaddr` | 同模块上下文对齐/差异观察 | (op & !FutexOpFlags::all().bits())
        .try_into, (op & !FutexOpFlags::all().bits())
        .try_into()
        .map_err, FutexOpFlags::all, FutexOpFlags::all().bits, FutexOpFlags::from_bits_truncate, SyscallResult::success ...(+16) | argraw |
| `os/src/syscall/mod.rs#call` | `kernel/syscall.c#argint` | 同模块上下文对齐/差异观察 | asm!, self.ptr.as_ptr | argraw |

#### 文件系统
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `os/src/syscall/filesystem.rs#mount` | `kernel/fs.c#mount` | 同板块同名 | MountPoint::mount, ROOT_PATH.0.clone, ROOT_PATH.1.sub_fs.clone, SyscallResult::success, copy_str_from_user, parse_walk_path_from ...(+2) | acquire, release |
| `os/src/filesystem/tmpfs/inode.rs#create` | `kernel/fs.c#create` | 同板块同名 | Err, NonNull::from_ref, Ok, Self::new_normal, debug_assert!, dentry.data.lock ...(+7) | iget, ilock, iunlockput, nameiparent, rs_create |
| `os/src/filesystem/tmpfs/inode.rs#rename` | `mkfs/mkfs.c#iappend` | 同模块上下文对齐/差异观察 | todo! | assert, bcopy, min, rinode, rsect, winode ...(+2) |
| `os/src/filesystem/vfs/filesystem.rs#walk_path_impl` | `src/fs.rs#do_create` | 同模块上下文对齐/差异观察 | ByteStr::new, Err, NPath, NonNull::from_ref, Ok, Path ...(+38) | EXT4FS.assume_init_ref, Ext4DirEntry::default, Ext4DirSearchResult::new, fs.create, fs.dir_find_entry, fs.dir_find_entry(idir, name, &mut res).is_ok ...(+2) |

#### 设备驱动
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `os/src/devices/virtio.rs#scan_virtio_devices` | `kernel/uart.c#uartinit` | 同模块上下文对齐/差异观察 | Box::new, MmioTransport::new, Mutex::new, NonNull::new, PhysAddr, PhysAddr(start_phy).to_virtual_address ...(+17) | WriteReg, initlock |
| `os/src/devices/mod.rs#add_block_device` | `kernel/uart.c#uartstart` | 同模块上下文对齐/差异观察 | CStr::from_bytes_until_nul, CStr::from_bytes_until_nul(str.as_bytes())
                        .map_or, CStr::from_bytes_until_nul(str.as_bytes()).ok, dev.block_count, dev.block_size, dev.device_id ...(+15) | ReadReg, WriteReg, wakeup |
| `os/src/devices/pci.rs#select_pci_range_32` | `kernel/uart.c#uartputc` | 同模块上下文对齐/差异观察 | matches!, memories.max_by_key, memories.max_by_key(|range| range.size).map, pci_ranges.filter | acquire, release, sbi_console_putchar, sleep, uartstart |
| `os/src/devices/pci.rs#scan_pci_devices` | `kernel/console.c#consoleinit` | 同模块上下文对齐/差异观察 | do_scan, fdt.find_compatible, log::debug! | initlock, initsleeplock, uartinit |

#### 文件系统/第三方库
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `edited_lib/ext4_rs/src/ext4_impls/extents.rs#insert_new_extent` | `third/ext4_rs/src/ext4_impls/extents.rs#insert_new_extent` | 同板块同名 | (inode_ref.inode.block.as_mut_ptr() as *mut u8)
                        .add, (insert_pos..entries).rev, Block::load, Ok, core::mem::size_of::<Ext4Extent>, core::mem::size_of::<Ext4ExtentHeader> ...(+18) | Ok, inode_ref.inode.root_extent_header_mut, inode_ref.inode.root_extent_mut_at, log::trace!, node.header.clone, return_errno_with_message! ...(+1) |
| `edited_lib/ext4_rs/src/ext4_impls/extents.rs#ext_remove_leaf` | `third/ext4_rs/src/ext4_impls/extents.rs#ext_remove_leaf` | 同板块同名 | Block::load, Block::load_inode_root_block, Ext4Extent::default, Ok, core::slice::from_raw_parts, ex.get_actual_len ...(+21) | Block::load, Block::load_inode_root_block, Ext4Extent::default, Ok, ex.get_actual_len, ex.get_pblock ...(+14) |
| `edited_lib/ext4_rs/src/ext4_impls/extents.rs#extent_remove_space` | `third/ext4_rs/src/ext4_impls/extents.rs#extent_remove_space` | 同板块同名 | Block::load, Ext4Extent::default, Ok, ex.get_actual_len, ex.get_first_block, ex.get_pblock ...(+13) | Block::load, Ext4Extent::default, Ok, ex.get_actual_len, ex.get_first_block, ex.get_pblock ...(+16) |
| `edited_lib/ext4_rs/src/ext4_impls/extents.rs#insert_extent` | `third/ext4_rs/src/ext4_impls/extents.rs#insert_extent` | 同板块同名 | Ok, inode_ref.inode.root_extent_mut_at, log::info!, self.can_merge, self.create_new_leaf, self.find_extent ...(+3) | Ok, inode_ref.inode.root_extent_mut_at, node.header.clone, self.can_merge, self.create_new_leaf, self.find_extent ...(+3) |

#### 通用模块/其他
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `os/src/basic/mod.rs#deref` | `src/pool.rs#deref` | 同板块同名 | assert!, self.value.get, self.value.get().as_ref_unchecked, self.value.get().as_ref_unchecked().assume_init_by_ref | self.target
                .get, self.target
                .get()
                .as_ref, self.target
                .get()
                .as_ref()
                .unwrap_unchecked, self.target
                .get()
                .as_ref()
                .unwrap_unchecked()
                .1
                .as_ref, self.target
                .get()
                .as_ref()
                .unwrap_unchecked()
                .1
                .as_ref()
                .unwrap |
| `programs/init/src/main.rs#write_str` | `src/console.rs#write_str` | 同板块同名 | Err, Ok, s.as_ptr, s.as_ptr().cast, s.len, write | Ok, consputc, s.bytes |
| `os/src/basic/logger.rs#enabled` | `src/logging.rs#enabled` | 同板块同名 | cfg! | 内部闭环逻辑 |
| `os/src/basic/mod.rs#release` | `kernel/spinlock.c#release` | 同板块同名 | ManuallyDrop::new, ManuallyDrop::take | __sync_lock_release, __sync_synchronize, holding, panic, pop_off |


#### 1. 宏观判定
- **定性结论**: 底层架构显著差异 / 独立开发 (相似度: 7.87%，共同 AST 指纹: 148；唯一 AST 指纹: 目标项目 1456，参考项目 573；覆盖率: 目标项目 10.16%，参考项目 25.83%)

#### 2. 全模块覆盖矩阵解读

- **进程管理**
  - 双方是否都存在该模块：是。目标项目 73 个函数，参考项目 37 个函数。
  - 是否存在共同 AST 指纹：否。共同唯一 AST 指纹为 0。
  - Target/Base 覆盖率说明了什么：覆盖率均为 0.0%，表明双方在进程管理的底层实现上无任何可识别的结构同源性。
  - 该模块证据强度：未形成。

- **任务调度/异步运行时**
  - 双方是否都存在该模块：是。目标项目 148 个函数，参考项目 22 个函数。
  - 是否存在共同 AST 指纹：否。共同唯一 AST 指纹为 0。
  - Target/Base 覆盖率说明了什么：覆盖率均为 0.0%，表明双方在调度和异步运行时的实现上无任何可识别的结构同源性。
  - 该模块证据强度：未形成。

- **内存管理**
  - 双方是否都存在该模块：是。目标项目 223 个函数，参考项目 140 个函数。
  - 是否存在共同 AST 指纹：否。共同唯一 AST 指纹为 0。
  - Target/Base 覆盖率说明了什么：覆盖率均为 0.0%，表明双方在内存管理的底层实现上无任何可识别的结构同源性。
  - 该模块证据强度：未形成。

- **中断/异常处理**
  - 双方是否都存在该模块：是。目标项目 30 个函数，参考项目 9 个函数。
  - 是否存在共同 AST 指纹：否。共同唯一 AST 指纹为 0。
  - Target/Base 覆盖率说明了什么：覆盖率均为 0.0%，表明双方在中断/异常处理的底层实现上无任何可识别的结构同源性。
  - 该模块证据强度：未形成。

- **系统调用**
  - 双方是否都存在该模块：是。目标项目 43 个函数，参考项目 3 个函数。
  - 是否存在共同 AST 指纹：否。共同唯一 AST 指纹为 0。
  - Target/Base 覆盖率说明了什么：覆盖率均为 0.0%，表明双方在系统调用分发与实现的底层结构上无任何可识别的同源性。
  - 该模块证据强度：未形成。

- **文件系统**
  - 双方是否都存在该模块：是。目标项目 485 个函数，参考项目 67 个函数。
  - 是否存在共同 AST 指纹：否。共同唯一 AST 指纹为 0。
  - Target/Base 覆盖率说明了什么：覆盖率均为 0.0%，表明双方在核心文件系统逻辑的实现上无任何可识别的结构同源性。
  - 该模块证据强度：未形成。

- **设备驱动**
  - 双方是否都存在该模块：是。目标项目 17 个函数，参考项目 14 个函数。
  - 是否存在共同 AST 指纹：否。共同唯一 AST 指纹为 0。
  - Target/Base 覆盖率说明了什么：覆盖率均为 0.0%，表明双方在设备驱动模型和实现上无任何可识别的结构同源性。
  - 该模块证据强度：未形成。

- **网络协议栈**
  - 双方是否都存在该模块：否。双方函数数均为 0。
  - 是否存在共同 AST 指纹：否。
  - Target/Base 覆盖率说明了什么：双方均未实现该模块，无可比性。
  - 该模块证据强度：未形成。

- **第三方库/公共依赖**
  - 双方是否都存在该模块：是。目标项目 390 个函数，参考项目 323 个函数。
  - 是否存在共同 AST 指纹：是。共同唯一 AST 指纹为 146。
  - Target/Base 覆盖率说明了什么：目标项目覆盖率 47.25%，参考项目覆盖率 59.84%。这表明双方均大量依赖了相同的第三方开源库（如 `ext4_rs`），这是结构重合的唯一来源。
  - 该模块证据强度：强，但属于对公共开源代码的合法复用，不构成抄袭。

#### 3. 底层 AST 关键证据指纹表

| 所属板块 | 目标项目路径 | 参考项目路径 | 证据类型 | 技术说明 |
| :--- | :--- | :--- | :--- | :--- |
| 第三方库/公共依赖 | `edited_lib/ext4_rs/src/ext4_impls/extents.rs#insert_new_extent` | `third/ext4_rs/src/ext4_impls/extents.rs#insert_new_extent` | 同板块同名函数，调用图谱高度一致 | 双方均使用了相同的开源 `ext4_rs` 库，函数逻辑、调用链（如 `Block::load`, `core::mem::size_of::<Ext4Extent>`）完全一致，属于公共依赖复用。 |
| 第三方库/公共依赖 | `edited_lib/ext4_rs/src/ext4_impls/extents.rs#ext_remove_leaf` | `third/ext4_rs/src/ext4_impls/extents.rs#ext_remove_leaf` | 同板块同名函数，调用图谱高度一致 | 同上，属于 `ext4_rs` 库内部逻辑，调用链（如 `Block::load_inode_root_block`, `ex.get_actual_len`）完全一致。 |
| 第三方库/公共依赖 | `edited_lib/ext4_rs/src/ext4_impls/extents.rs#extent_remove_space` | `third/ext4_rs/src/ext4_impls/extents.rs#extent_remove_space` | 同板块同名函数，调用图谱高度一致 | 同上，属于 `ext4_rs` 库内部逻辑，调用链（如 `ex.get_first_block`, `ex.get_pblock`）完全一致。 |
| 第三方库/公共依赖 | `edited_lib/ext4_rs/src/ext4_impls/extents.rs#insert_extent` | `third/ext4_rs/src/ext4_impls/extents.rs#insert_extent` | 同板块同名函数，调用图谱高度一致 | 同上，属于 `ext4_rs` 库内部逻辑，调用链（如 `self.can_merge`, `self.create_new_leaf`）完全一致。 |
| 通用模块/其他 | `os/src/basic/mod.rs#deref` | `src/pool.rs#deref` | 同板块同名函数，调用图谱部分相似 | 双方均实现了 `Deref` trait，调用链涉及 `self.value.get().as_ref_unchecked()`，属于 Rust 语言标准库的惯用模式，非独创逻辑。 |

#### 4. 核心代码调用流对比剖析

* **【进程管理/`exit` 与 `exit_group` 路径】**
* **底层技术析理**: 目标项目 `exit_group_kernel` 基于 Linux 多线程模型，通过设置 `is_exiting` 原子标志、遍历 `tasks` 列表并调用 `task.send_signal` 来广播退出信号，依赖信号机制和 `exit_kernel` 完成各线程清理。参考项目 `sys_exit_group` 仅是对 `exit` 函数的简单包装，直接终止整个进程，无多线程遍历和信号广播。两者在**抽象层次**（多线程组 vs 单线程进程）、**同步策略**（原子变量+信号 vs 直接函数调用）和**对象管理模型**（`Arc<RwLock<Process>>` + `tasks` 列表 vs 全局 `proc[]` 数组）上存在根本性差异。
* **代码溯源链接**:
  - 目标项目: `os/src/syscall/process.rs#exit_group_kernel`
  - 参考项目: `kernel/sysproc.c#sys_exit_group`

* **【进程管理/`wait4` 路径】**
* **底层技术析理**: 目标项目 `wait4` 实现了完整的 Linux `wait4` ABI，通过 `WaitOptions::from_bits` 解析 `WNOHANG` 等标志，使用 `wait4_pred` 闭包抽象匹配逻辑，并遍历侵入式链表 `children`。参考项目 `sys_wait4` 仅做简单的参数提取（`argaddr`, `argint`）后调用简化版 `wait4`，遍历固定大小的 `proc[]` 数组。两者在**内存模型**（侵入式链表 vs 静态数组）、**抽象层次**（谓词抽象 vs 硬编码循环）和**ABI 兼容性**（Linux 兼容 vs Xv6 简化）上完全不同。
* **代码溯源链接**:
  - 目标项目: `os/src/syscall/process.rs#wait4`
  - 参考项目: `kernel/sysproc.c#sys_wait4`

* **【内存管理/`mmap` 路径】**
* **底层技术析理**: 目标项目 `mmap` 解析 Linux `mmap` 的完整标志位（`MMapFlags::from_bits`）和保护位（`MMapProtections::from_bits`），并集成到 VFS 和页缓存系统中。参考项目 `sys_mmap` 仅做参数提取后调用 `mmap`，其底层基于 Xv6 简单的 `kalloc` 和文件系统 `idup`/`ilock`。两者在**内存模型**（页缓存 vs 物理页分配）、**同步策略**（`RwLock` vs 自旋锁）和**功能完整性**上存在显著差异。
* **代码溯源链接**:
  - 目标项目: `os/src/syscall/memory.rs#mmap`
  - 参考项目: `kernel/sysproc.c#sys_mmap`

#### 5. 单方模块与降权项

- **单方模块**：
  - **网络协议栈**：双方均未实现，无可比性。
  - **ProcFS 文件系统**：目标项目实现了完整的 `procfs`（`os/src/filesystem/procfs/`），而参考项目无此子系统。控方将 `procfs` 的 `get_dir`、`open` 等函数错误归入“进程管理”板块进行对比，属于跨板块硬凑，不构成同源证据。
  - **Futex 子系统**：目标项目实现了 Linux futex（`os/src/multitask/futex.rs`），参考项目无此机制。控方将其与参考项目的 `sleep` 对比，属于跨子系统错误配对。
  - **信号子系统**：目标项目实现了完整的 Linux 信号处理（`os/src/syscall/signal.rs`），参考项目仅有简单的 `kill`。两者功能域和实现复杂度不在同一层级。

- **降权项**：
  - **第三方库/公共依赖**：148 个共同 AST 指纹中的 146 个来自 `ext4_rs` 等第三方开源库。这是对公共代码的合法复用，不能作为抄袭证据。
  - **通用模块/其他**：唯一的 1 个共同指纹来自 `Deref` trait 的实现，属于 Rust 语言的标准惯用模式，不具备独创性。
  - **跨板块同名函数**：如 `mount`、`create`、`rename` 等，虽在文件系统板块同名，但目标项目基于 VFS 和 `ext4_rs`，参考项目基于 Xv6 简单 FS，底层调用图谱无任何共同指纹，属于领域通用接口的巧合，不构成同源证据。

#### 6. 保守结论

本次审计的客观 AST 相似度仅为 7.87%，且 148 个共同指纹中的 146 个（98.6%）明确归属于双方共同引用的第三方开源库 `ext4_rs`，属于合法公共依赖。在操作系统内核的核心板块，包括进程管理、内存管理、文件系统、中断处理和系统调用，双方的共同 AST 指纹均为 0，覆盖率均为 0.0%。这表明两个项目在底层架构、数据结构、同步机制和抽象层次上存在根本性差异，分别遵循了 Rust/Linux 兼容与 C/Xv6 教学系统的不同设计哲学。控方提出的“逻辑平移”指控缺乏 AST 层面的结构证据支撑，其证据多由跨板块硬凑和领域通用语义的过度解读构成。综合判定，目标项目 `[2026-T2026100079910008-XX]` 相对于参考项目 `[2024-你起吧-长春理工大学]` 为独立开发，整体抄袭风险极低。

------------------------------

