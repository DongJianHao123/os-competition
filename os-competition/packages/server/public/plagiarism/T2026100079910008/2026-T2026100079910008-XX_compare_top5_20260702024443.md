# 🛡️ OS 内核赛自动化代码级架构审计报告

**审计时间**: `2026-07-02 02:44:43`
**参赛作品名称**: `2026-T2026100079910008-XX`
**年份**: `2026`
**学校**: `XX`
**队伍名称**: `T2026100079910008`
**比赛名字**: `OS-Competition`
**比赛子赛道**: `Kernel-Implementation`
**仓库地址**: `https://gitlab.eduxiji.net/educg-group-43501-3132633/T2026100079910008-777`
**推测开发语言**: `Rust / C (由AST探针自动识别)`
**特征函数量**: `1758`

## ⚔️ 底层代码同源性对比 (AST Homology)
> 本部分由专门的代码审查智能体，对该项目与符合年份条件的历届最高相似度项目进行控制流与底层调用对比。新版报告会先给出确定性的全模块覆盖矩阵，再给出 Agent 的分模块解释，避免只围绕少量抽样函数展开。

### 🏆 Top 5 底层对标: vs `2025-西北工业大学一二三队-西北工业大学`（西北工业大学 / 西北工业大学一二三队）

#### 对方作品信息

**对方年份**: `2025`
**对方学校**: `西北工业大学`
**对方队伍名称**: `西北工业大学一二三队`
**对方比赛名字**: `OS-Competition`
**对方比赛子赛道**: `Kernel-Implementation`
**对方仓库地址**: `https://gitlab.eduxiji.net/educg-group-36002-2710490/T202510699995276-827`

> 双向覆盖率：共同 AST 指纹: 133；唯一 AST 指纹: 目标项目 1456，参考项目 2317；覆盖率: 目标项目 9.13%，参考项目 5.74%

## 全模块对比总览（确定性证据）

> 总体覆盖率：共同 AST 指纹: 133；唯一 AST 指纹: 目标项目 1456，参考项目 2317；覆盖率: 目标项目 9.13%，参考项目 5.74%

### 1. 模块覆盖矩阵
| 模块 | 目标函数数 | 参考函数数 | 共同唯一 AST 指纹 | Target 覆盖 | Base 覆盖 | 状态 |
| :--- | ---: | ---: | ---: | ---: | ---: | :--- |
| 进程管理 | 73 | 74 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |
| 任务调度/异步运行时 | 148 | 139 | 1 | 0.69% | 0.79% | 双方存在且有结构重合 |
| 内存管理 | 223 | 593 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |
| 中断/异常处理 | 30 | 47 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |
| 系统调用 | 43 | 65 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |
| 文件系统 | 485 | 552 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |
| 设备驱动 | 17 | 47 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |
| 网络协议栈 | 0 | 88 | 0 | 0.0% | 0.0% | 仅参考项目体现 |
| 文件系统/第三方库 | 390 | 369 | 114 | 36.89% | 39.45% | 双方存在且有结构重合 |
| 网络协议栈/第三方库 | 0 | 138 | 0 | 0.0% | 0.0% | 仅参考项目体现 |
| 第三方库 | 0 | 1228 | 0 | 0.0% | 0.0% | 仅参考项目体现 |
| 通用模块/其他 | 349 | 230 | 1 | 0.41% | 0.85% | 双方存在且有结构重合 |

### 2. 分模块证据清单

#### 进程管理
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `os/src/syscall/process.rs#getpid` | `os/src/task/task.rs#getpid` | 同板块同名 | current_task, current_task().as_ref_unchecked | 内部闭环逻辑 |
| `os/src/syscall/process.rs#wait4_pred` | `os/src/syscall/process.rs#sys_execve` | 同模块上下文对齐/差异观察 | SyscallResult::success, Waiting::new, child.time_stats.lock, child.wait_state.lock, children.front_mut, cur_child.get ...(+16) | Box::<[u8; 4]>::new, DEFAULT_SHELL.to_string, Some, Vec::with_capacity, arg_ptr.is_null, argv.add ...(+22) |
| `os/src/syscall/process.rs#wait4` | `os/src/syscall/process.rs#sys_prlimit` | 同模块上下文对齐/差异观察 | SyscallResult::success, WaitOptions::from_bits, WaitOptions::from_bits(options.try_into().map_err(|_| EINVAL)?).ok_or, Waiting::new, addr_eq, current_task ...(+19) | Resource::from_primitive, assert!, copy_from_user, copy_from_user(token, new_limit, rlimit).is_err, copy_to_user, copy_to_user(
                        token,
                        &(RLimit {
                            rlim_cur: SYSTEM_TASK_LIMIT,
                            rlim_max: SYSTEM_TASK_LIMIT,
                        }),
                        old_limit,
                    )
                    .is_err ...(+18) |
| `os/src/multitask/task.rs#on_exit` | `os/src/syscall/process.rs#sys_futex` | 同模块上下文对齐/差异观察 | FileDescriptorTable::new, debug_assert!, futex_lock, futex_word.relaxed_atom_store, futex_word.wake, get_futex_key ...(+11) | FutexOption::from_bits_truncate, current_task, current_task().unwrap, do_futex_wait, drop, info! ...(+15) |

#### 任务调度/异步运行时
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `os/src/multitask/futex.rs#eq` | `os/src/task/manager.rs#eq` | 同板块同名 | Arc::ptr_eq, i1.addr_eq | self.timeout.eq |
| `os/src/multitask/schedulers/realtime.rs#add_task` | `os/src/task/manager.rs#add_task` | 同板块同名 | Err, Ok, self.has_tasks.set, self.tasks[priority].is_empty, self.tasks[priority].push_back | self.inner.push |
| `os/src/multitask/futex.rs#wake` | `os/src/task/threads.rs#wake` | 同板块同名 | futex_q.task.as_ref_unchecked, self.queue
            .iter, self.queue
            .iter()
            .filter, self.queue
            .iter()
            .filter(|futex_q| futex_q.key == *self.key && (futex_q.mask & mask != 0))
            .filter_map, self.queue
            .iter()
            .filter(|futex_q| futex_q.key == *self.key && (futex_q.mask & mask != 0))
            .filter_map(|futex_q| unsafe { futex_q.task.as_ref_unchecked() }.try_wake())
            .take, self.queue
            .iter()
            .filter(|futex_q| futex_q.key == *self.key && (futex_q.mask & mask != 0))
            .filter_map(|futex_q| unsafe { futex_q.task.as_ref_unchecked() }.try_wake())
            .take(count)
            .count ...(+1) | self.inner.insert, self.inner.remove, wait_queue.is_empty, wait_queue.wake_at_most |
| `os/src/multitask/task_scheduler.rs#add_task` | `os/src/task/manager.rs#add_task` | 同板块同名 | panic!, scheduler.add_task | self.inner.push_back |

#### 内存管理
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `os/src/multitask/task.rs#load_elf` | `os/src/task/task.rs#load_elf` | 同板块同名 | ByteString, ElfBytes::<NativeEndian>::minimal_parse_no_section_headers, ElfBytes::<NativeEndian>::minimal_parse_no_section_headers(head).map_err, Err, File::get_page_cache, Ok ...(+49) | (&memory_set)
            .translate, (&memory_set)
            .translate(VirtAddr::from(self.trap_cx_user_va()).into())
            .unwrap, KERNEL_SPACE.lock, KERNEL_SPACE.lock().token, MemorySet::from_elf, Ok ...(+35) |
| `os/src/trap/trap_handler.rs#trap_handler` | `os/src/hal/arch/loongarch64/trap/mod.rs#trap_handler` | 同板块同名 | DropGuard::new, FIXUPS.iter, FIXUPS.iter().find_map, Some, TASK_SCHEDULER.lock, Task::switch_to ...(+41) | (0..sz).rev, BadV::read, BadV::read().get_vaddr, ERA::read, ERA::read().next_ins, ERA::read().next_ins().write ...(+55) |
| `os/src/memory/page_table.rs#map` | `os/src/hal/arch/loongarch64/laflex.rs#map` | 同板块同名 | Err, Ok, PTEFlagsNative::from, PTEFlagsNative::from(PTEFlags::Valid).bits, PTEFlagsNative::from_bits_truncate, PTEFlagsNative::from_bits_truncate(entry).into ...(+21) | LAFlexPageTableEntry::new, debug_assert!, flags.contains, self.find_pte_create, self.find_pte_create(vpn).unwrap |
| `os/src/syscall/memory.rs#mmap` | `os/src/mm/memory_set.rs#mmap` | 同板块同名 | (flags >> 2).try_into, (flags >> 2).try_into().map_err, MMapFlags::from_bits, MMapFlags::from_bits(flags).ok_or, MMapProtections::from_bits, MMapProtections::from_bits(prot).ok_or ...(+23) | MMAP_BASE.into, MapArea::new, Some, USR_MMAP_BASE.into, VirtAddr::from, VirtAddr::from(MMAP_END).into ...(+31) |

#### 中断/异常处理
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `os/src/arch/loongarch64/trap.rs#enable_timer_interrupt` | `os/src/hal/arch/loongarch64/trap/mod.rs#enable_timer_interrupt` | 同板块同名 | asm! | ECfg::empty, ECfg::empty()
        .set_line_based_interrupt_vector, ECfg::empty()
        .set_line_based_interrupt_vector(LineBasedInterrupt::TIMER)
        .write, TCfg::read, TCfg::read()
        .set_enable, TCfg::read()
        .set_enable(true)
        .set_periodic ...(+3) |
| `os/src/trap/mod.rs#enable_timer_interrupt` | `os/src/hal/arch/riscv/trap/mod.rs#enable_timer_interrupt` | 同板块同名 | arch::NativeTrap::enable_timer_interrupt | sie::set_stimer |
| `os/src/filesystem/vfs/result.rs#from` | `os/src/hal/arch/loongarch64/trap/mod.rs#trap_from_kernel` | 同模块上下文对齐/差异观察 | InvalidArguments, InvalidUserPointer, todo!, value.error, value.msg.unwrap_or | ((addr + i) as *mut u8).read_unaligned, ((addr + i) as *mut u8).write_unaligned, (0..sz).rev, BadV::read, BadV::read().get_vaddr, EStat::read ...(+14) |
| `os/src/trap/trap.rs#new` | `os/src/hal/arch/loongarch64/trap/mem_access.rs#get_addr` | 同模块上下文对齐/差异观察 | Exception::try_from, Exception::try_from(code).unwrap, Exception::try_from(ecode).unwrap, Interrupt::try_from, Interrupt::try_from((63 - is.leading_zeros()) as usize).unwrap, Interrupt::try_from(code).unwrap ...(+8) | self.get_op_code, self.get_op_code().map, self.get_rj_num, self.get_rk_num, self.get_si12, self.get_si14 |

#### 系统调用
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `os/src/syscall/signal.rs#addr` | `os/src/net/mod.rs#addr` | 同板块同名 | sa_handler.map_or, sa_sigaction.map_or | address::fill_with_endpoint, address::to_endpoint, self.loacl_endpoint |
| `os/src/syscall/basic.rs#exit` | `user/src/usr_call.rs#exit` | 同板块同名 | exit_kernel | sys_exit |
| `programs/init/src/main.rs#fork` | `user/src/usr_call.rs#fork` | 同板块同名 | syscall!, syscall!(220, 0, 0).cast_signed | sys_fork |
| `programs/init/src/main.rs#main` | `os/src/net/tcp.rs#_read` | 同模块上下文对齐/差异观察 | b"-c\0".as_ptr, b"/dev/tty\0".as_ptr, b"/sdcd/musl/busybox\0".as_ptr, b"PATH=/bin\0".as_ptr, b"sh\0".as_ptr, concat_bytes! ...(+7) | Err, GeneralRet::Err, GeneralRet::Ok, NET_INTERFACE.poll, NET_INTERFACE.tcp_socket, Ok ...(+7) |

#### 文件系统
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `os/src/filesystem/tmpfs/inode.rs#rename` | `os/src/fs/directory_tree.rs#rename` | 同板块同名 | todo! | Arc::downgrade, Arc::new, Err, Mutex::new, Ok, ROOT.cd_comp ...(+41) |
| `os/src/filesystem/devfs/drivers/tty.rs#ioctl` | `os/src/fs/file_descriptor.rs#ioctl` | 同板块同名 | Err, Ok, WinSize::from_u64, arg.cast, copy_one_from_user, copy_one_to_user ...(+5) | self.file.ioctl |
| `os/src/syscall/filesystem.rs#lseek` | `os/src/fs/dev/interrupts.rs#lseek` | 同板块同名 | SyscallResult::success, file.data.lock, file.data.lock().fseek, get_file_from_fd, offset.try_into, offset.try_into().map_err ...(+5) | Err, Ok, self.get_size, self.offset.lock |
| `os/src/filesystem/tmpfs/inode.rs#mkdir` | `os/src/fs/directory_tree.rs#mkdir` | 同板块同名 | Err, NonNull::from_ref, Ok, Self::new_normal, dentry.data.lock, dentry.data.lock().name.clone ...(+7) | (*last_comp).to_string, Arc::downgrade, Err, Ok, Self::new, Self::parse_dir_path ...(+13) |

#### 设备驱动
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `os/src/devices/hal.rs#mmio_phys_to_virt` | `os/src/drivers/block/virtio_blk.rs#mmio_phys_to_virt` | 同板块同名 | MyPhysAddr, NonNull::new_unchecked, paddr.to_virtual_address | NonNull::new, NonNull::new(vaddr.0 as *mut u8)
            .expect, PhysAddr, virtio_phys_to_virt |
| `os/src/devices/virtio.rs#scan_virtio_devices` | `os/src/drivers/block/sata_blk.rs#pci_init` | 同模块上下文对齐/差异观察 | Box::new, MmioTransport::new, Mutex::new, NonNull::new, PhysAddr, PhysAddr(start_phy).to_virtual_address ...(+17) | AHCI::new, Some, Status::empty, dev.bars.iter, dev.bars.iter().enumerate, dev.bars.iter().enumerate().for_each ...(+3) |
| `os/src/devices/mod.rs#add_block_device` | `os/src/net/config.rs#new` | 同模块上下文对齐/差异观察 | CStr::from_bytes_until_nul, CStr::from_bytes_until_nul(str.as_bytes())
                        .map_or, CStr::from_bytes_until_nul(str.as_bytes()).ok, dev.block_count, dev.block_size, dev.device_id ...(+15) | Config::new, EthernetAddress, EthernetAddress([0x02, 0x00, 0x00, 0x00, 0x00, 0x01]).into, Instant::from_millis, Interface::new, IpAddress::v4 ...(+12) |
| `os/src/devices/pci.rs#select_pci_range_32` | `os/src/drivers/block/virtio_blk.rs#read_block` | 同模块上下文对齐/差异观察 | matches!, memories.max_by_key, memories.max_by_key(|range| range.size).map, pci_ranges.filter | assert!, buf.chunks_mut, buf.chunks_mut(VIRT_IO_BLOCK_SZ).enumerate, self.0
                .lock, self.0
                .lock()
                .read_blocks, self.0
                .lock()
                .read_blocks(virtio_block_id as usize, chunk)
                .expect |

#### 网络协议栈
仅参考项目中观察到该模块代表函数，目标项目没有同模块可对齐证据：
- 参考项目：`os/src/net/tcp.rs#new`，调用摘要：Mutex::new, NET_INTERFACE.add_socket, NET_INTERFACE.poll, RNG.positive_u32, info!, socket::tcp::Socket::new ...(+2)
- 参考项目：`os/src/net/udp.rs#new`，调用摘要：Mutex::new, NET_INTERFACE.add_socket, NET_INTERFACE.poll, log::info!, socket::udp::PacketBuffer::new, socket::udp::Socket::new ...(+1)
- 参考项目：`os/src/net/tcp.rs#drop`，调用摘要：NET_INTERFACE.poll, NET_INTERFACE.remove, NET_INTERFACE.tcp_socket, info!, socket.close, socket.is_open
- 参考项目：`os/src/net/mod.rs#can_bind`，调用摘要：Some, log::info!, self.0.clone, socket.loacl_endpoint, socket.loacl_endpoint().eq, socket.socket_type ...(+1)

#### 文件系统/第三方库
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `edited_lib/ext4_rs/src/ext4_impls/extents.rs#insert_new_extent` | `os/src/fs/ext4/extent.rs#insert_new_extent` | 同板块同名 | (inode_ref.inode.block.as_mut_ptr() as *mut u8)
                        .add, (insert_pos..entries).rev, Block::load, Ok, core::mem::size_of::<Ext4Extent>, core::mem::size_of::<Ext4ExtentHeader> ...(+18) | Block::load_offset, Ok, core::mem::size_of::<Ext4Extent>, core::mem::size_of::<Ext4ExtentHeader>, ext4block.read_offset_as_mut, ext4block.sync_blk_to_disk ...(+5) |
| `edited_lib/ext4_rs/src/ext4_impls/extents.rs#ext_remove_leaf` | `os/src/fs/ext4/extent.rs#ext_remove_leaf` | 同板块同名 | Block::load, Block::load_inode_root_block, Ext4Extent::default, Ok, core::slice::from_raw_parts, ex.get_actual_len ...(+21) | Block::load_inode_root_block, Block::load_offset, Ext4Extent::default, Ok, ex.get_actual_len, ex.get_pblock ...(+14) |
| `edited_lib/ext4_rs/src/ext4_impls/extents.rs#extent_remove_space` | `os/src/fs/ext4/extent.rs#extent_remove_space` | 同板块同名 | Block::load, Ext4Extent::default, Ok, ex.get_actual_len, ex.get_first_block, ex.get_pblock ...(+13) | Block::load_offset, Ext4Extent::default, Ok, ex.get_actual_len, ex.get_first_block, ex.get_pblock ...(+14) |
| `edited_lib/ext4_rs/src/ext4_impls/extents.rs#ext_grow_indepth` | `os/src/fs/ext4/extent.rs#ext_grow_indepth` | 同板块同名 | Block::load, Ext4ExtentHeader::new, Ok, core::ptr::write_bytes, core::slice::from_raw_parts, inode_ref.inode.block.as_mut_ptr ...(+20) | Block::load_offset, Ext4ExtentHeader::load_from_u8_mut, Ok, core::mem::size_of::<Ext4Extent>, core::mem::size_of::<Ext4ExtentHeader>, core::ptr::copy_nonoverlapping ...(+15) |

#### 网络协议栈/第三方库
仅参考项目中观察到该模块代表函数，目标项目没有同模块可对齐证据：
- 参考项目：`dependency/dep_iso/src/net/ethernet/intel/ixgbe.rs#new`，调用摘要：(IXGBE_FCRTH..IXGBE_FCRTH_END).step_by, (IXGBE_FCRTL..IXGBE_FCRTL_END).step_by, (IXGBE_FCTTV..IXGBE_FCTTV_END).step_by, EthernetAddress::from_bytes, MaybeUninit::uninit, MaybeUninit::uninit().assume_init ...(+56)
- 参考项目：`dependency/virtio-drivers/src/device/socket/connectionmanager.rs#send_recv`，调用摘要："Hello from guest".as_bytes, (hello_from_guest.len() as u32).into, (hello_from_host.len() as u32).into, 0.into, 50.into, Arc::new ...(+43)
- 参考项目：`dependency/virtio-drivers/src/device/socket/connectionmanager.rs#incoming_connection`，调用摘要：0.into, 50.into, Arc::new, Mutex::new, ReadOnly::new, SocketType::Stream.into ...(+24)
- 参考项目：`dependency/dep_iso/src/net/ethernet/intel/e1000.rs#new`，调用摘要：P::alloc_dma, Vec::with_capacity, assert_eq!, debug!, e1000[E1000_ICR].read, e1000[E1000_ICR].write ...(+25)

#### 第三方库
仅参考项目中观察到该模块代表函数，目标项目没有同模块可对齐证据：
- 参考项目：`dependency/virtio-drivers/src/queue.rs#fake_read_write_queue`，调用摘要：(*available_ring).idx.load, (*used_ring).idx.fetch_add, (*used_ring).idx.load, Vec::new, assert!, assert_eq! ...(+18)
- 参考项目：`dependency/dep_pci/pci/src/lib.rs#probe_function`，调用摘要：BAR::decode, BridgeControl::from_bits_truncate, Command::from_bits_truncate, DeviceKind::CardbusBridge, DeviceKind::Device, DeviceKind::PciBridge ...(+6)
- 参考项目：`dependency/virtio-drivers/src/device/sound/fake.rs#handle_control_request`，调用摘要：CommandCode::SOk.into, Some, Vec::new, VirtIOSndHdr {
                            command_code: CommandCode::SOk.into(),
                        }
                        .as_bytes, VirtIOSndHdr::read_from_prefix, VirtIOSndHdr::read_from_prefix(&request)
                .expect ...(+16)
- 参考项目：`dependency/dep_iso/src/block/ahci.rs#new`，调用摘要：(0..ghc.num_ports()).find, P::alloc_dma, Some, c.set_bit, debug!, ghc.enable ...(+16)

#### 通用模块/其他
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `os/src/arch/riscv64/basic.rs#shutdown` | `os/src/hal/arch/loongarch64/sbi.rs#shutdown` | 同板块同名 | asm!, unreachable! | (0x100E_001C as *mut u8).write_volatile, Pm1Cnt::empty, pm1_cnt.set_s5, pm1_cnt.set_s5().write |
| `os/src/arch/loongarch64/chrono.rs#get_clock_freq` | `os/src/hal/arch/loongarch64/time.rs#get_clock_freq` | 同板块同名 | asm!, mul_div.get_bits | 内部闭环逻辑 |
| `os/src/arch/loongarch64/basic.rs#shutdown` | `os/src/hal/arch/riscv/sbi.rs#shutdown` | 同板块同名 | POWER_BASE_VADDR.as_mut_ptr::<u8>, POWER_BASE_VADDR.as_mut_ptr::<u8>().write_volatile | panic!, sbi_call |
| `programs/init/src/main.rs#write_str` | `os/src/console.rs#write_str` | 同板块同名 | Err, Ok, s.as_ptr, s.as_ptr().cast, s.len, write | Ok, console_flush, console_putchar, s.chars |


好的，我将以操作系统内核架构审计专家的身份，基于提供的证据材料，对本案进行最终的同源性定性仲裁。

### 1. 宏观判定
- **定性结论**: **独立开发 (底层架构显著差异)**
- **核心依据**: 客观 AST 相似度仅为 **3.65%**，处于极低水平。全项目共同 AST 指纹仅 133 个，而双方唯一指纹合计超过 3700 个。在控方重点指控的【进程管理】板块，共同唯一 AST 指纹为 **0**，覆盖率 **0%**，这从根本上排除了该板块存在大规模结构性复用的可能。唯一显著的结构重合集中在【文件系统/第三方库】模块，经分析为双方共同引用了开源 `ext4_rs` 库所致，属于公共依赖，不能作为核心代码抄袭的证据。

### 2. 全模块覆盖矩阵解读
- **进程管理**:
  - **双方存在性**: 双方均存在。
  - **共同 AST 指纹**: 0。
  - **覆盖率解读**: 目标 0.0%，参考 0.0%。表明在总计 147 个函数中，未发现任何共同的结构指纹。这直接否定了控方关于此模块“大量换皮洗稿”的指控。
  - **证据强度**: **未形成**。

- **任务调度/异步运行时**:
  - **双方存在性**: 双方均存在。
  - **共同 AST 指纹**: 1。
  - **覆盖率解读**: 目标 0.69%，参考 0.79%。覆盖率极低，唯一的共同指纹可能源于如 `add_task` 等通用函数名或极其简单的逻辑，不构成结构性同源。
  - **证据强度**: **弱**。

- **内存管理**:
  - **双方存在性**: 双方均存在。
  - **共同 AST 指纹**: 0。
  - **覆盖率解读**: 目标 0.0%，参考 0.0%。双方在内存管理子系统（如页表映射 `map`、`mmap` 实现）上无任何共同结构指纹，表明其内部实现（如分配器、地址空间管理）采用了完全不同的架构。
  - **证据强度**: **未形成**。

- **中断/异常处理**:
  - **双方存在性**: 双方均存在。
  - **共同 AST 指纹**: 0。
  - **覆盖率解读**: 目标 0.0%，参考 0.0%。尽管存在 `trap_handler` 等同名函数，但 AST 层面无重合，说明底层 trap 处理流程、上下文保存/恢复机制是异构的。
  - **证据强度**: **未形成**。

- **系统调用**:
  - **双方存在性**: 双方均存在。
  - **共同 AST 指纹**: 0。
  - **覆盖率解读**: 目标 0.0%，参考 0.0%。系统调用是 OS 与用户态的接口，函数名由 POSIX 标准定义，但双方在内部实现上无共同指纹，证明实现路径独立。
  - **证据强度**: **未形成**。

- **文件系统**:
  - **双方存在性**: 双方均存在。
  - **共同 AST 指纹**: 0。
  - **覆盖率解读**: 目标 0.0%，参考 0.0%。双方均实现了 VFS、tmpfs、devfs 等，但核心逻辑无共同指纹。例如 `rename` 实现，目标为 `todo!`，参考为完整逻辑，差异巨大。
  - **证据强度**: **未形成**。

- **设备驱动**:
  - **双方存在性**: 双方均存在。
  - **共同 AST 指纹**: 0。
  - **覆盖率解读**: 目标 0.0%，参考 0.0%。双方驱动模型（如 VirtIO 扫描、PCI 枚举）无共同指纹，表明驱动框架是独立构建的。
  - **证据强度**: **未形成**。

- **网络协议栈**:
  - **双方存在性**: 仅参考项目存在。
  - **共同 AST 指纹**: 0。
  - **覆盖率解读**: 目标项目无此模块，是双方架构上的重大差异。
  - **证据强度**: **未形成**。

- **第三方库/公共依赖**:
  - **双方存在性**: 双方均存在。
  - **共同 AST 指纹**: 114。
  - **覆盖率解读**: 目标 36.89%，参考 39.45%。这是全项目唯一显著的结构重合区域。证据明确指向 `edited_lib/ext4_rs` 与 `os/src/fs/ext4`，均为对开源 `ext4` 文件系统库的引用或修改。此重合属于公共依赖，不反映内核核心逻辑的原创性。
  - **证据强度**: **强（但属于公共依赖，已排除）**。

### 3. 底层 AST 关键证据指纹表
| 所属板块 | 目标项目路径 | 参考项目路径 | 证据类型 | 技术说明 |
| :--- | :--- | :--- | :--- | :--- |
| 第三方库/公共依赖 | `edited_lib/ext4_rs/src/ext4_impls/extents.rs#insert_new_extent` | `os/src/fs/ext4/extent.rs#insert_new_extent` | 同板块同名 | 双方均使用 `core::mem::size_of::<Ext4Extent>` 等结构，是引用同一开源 ext4 库的典型特征。 |
| 第三方库/公共依赖 | `edited_lib/ext4_rs/src/ext4_impls/extents.rs#ext_remove_leaf` | `os/src/fs/ext4/extent.rs#ext_remove_leaf` | 同板块同名 | 函数逻辑高度重合，均操作 `Ext4Extent` 结构，源于共同的 ext4 算法实现。 |
| 第三方库/公共依赖 | `edited_lib/ext4_rs/src/ext4_impls/extents.rs#extent_remove_space` | `os/src/fs/ext4/extent.rs#extent_remove_space` | 同板块同名 | 调用图谱高度相似，是 ext4 文件系统 extent 操作的标准化实现，非原创逻辑。 |
| 第三方库/公共依赖 | `edited_lib/ext4_rs/src/ext4_impls/extents.rs#ext_grow_indepth` | `os/src/fs/ext4/extent.rs#ext_grow_indepth` | 同板块同名 | 均涉及 `Ext4ExtentHeader` 的操作，是 ext4 库内部实现，与内核原创性无关。 |
| 任务调度/异步运行时 | `os/src/multitask/futex.rs#eq` | `os/src/task/manager.rs#eq` | 同板块同名 | 极弱信号。`eq` 是 Rust 标准 trait，任何包含比较逻辑的结构体都可能实现它。 |
| 通用模块/其他 | `os/src/arch/loongarch64/basic.rs#shutdown` | `os/src/hal/arch/loongarch64/sbi.rs#shutdown` | 同板块同名 | 弱信号。`shutdown` 是通用功能，但目标通过直接写 MMIO 寄存器实现，参考通过 SBI 调用，实现方式不同。 |

### 4. 核心代码调用流对比剖析
* **【进程管理 / clone 系统调用】**
* **底层技术析理**: 控方指控此路径存在“位运算洗稿”。经析理，两者在内存模型和对象管理上完全不同。目标项目使用 `(flags >> 8).try_into()` 来提取信号编号，并传递给 `SignalId::try_from`，其信号系统是独立的类型。参考项目使用 `flags & !0xff` 来清除低 8 位，并将结果直接传给 `CloneFlags::from_bits`。前者是提取并转换一个字段，后者是掩码过滤，位运算目的和结果均不等价。此外，目标项目通过 `current_task().as_ref_unchecked` 获取任务，而参考项目使用 `current_task().unwrap`，表明两者在任务引用和安全抽象上存在不同设计。
* **代码溯源链接**:
  - 目标项目: `os/src/syscall/process.rs#clone`
  - 参考项目: `os/src/syscall/process.rs#sys_clone`

* **【进程管理 / 信号发送与唤醒机制】**
* **底层技术析理**: 控方指控此路径存在“接口换皮复用”。析理发现，两者的同步策略和数据结构截然不同。目标项目在 `send_signal` 中使用 `self.signals_wl.lock()` 获取一个独立的写锁，然后遍历 `self.tasks` 并调用 `task.try_interrupt_sleep`。参考项目在 `sys_kill` 中通过 `task.acquire_inner_lock()` 获取一个通用的任务内部锁，然后调用 `inner.add_signal` 和全局的 `wake_interruptible`。目标项目将信号挂起队列（`signals_wl`）与任务控制块分离，使用细粒度锁；参考项目则使用粗粒度锁 `acquire_inner_lock` 管理包括信号在内的所有任务状态。这是两种不同的锁架构设计。
* **代码溯源链接**:
  - 目标项目: `os/src/multitask/process_data.rs#send_signal`
  - 参考项目: `os/src/syscall/process.rs#sys_kill`

* **【内存管理 / mmap 系统调用】**
* **底层技术析理**: 双方均实现了 `mmap`，但内存抽象层次不同。目标项目在 `os/src/syscall/memory.rs#mmap` 中直接处理 `MMapFlags` 和 `MMapProtections`，并调用 `sys_mmap`。参考项目在 `os/src/mm/memory_set.rs#mmap` 中，其核心逻辑在 `MemorySet` 结构体的方法中，涉及 `MapArea::new` 和 `USR_MMAP_BASE` 等内部地址空间管理细节。目标项目的系统调用层更薄，而参考项目将逻辑下沉到了内存管理子系统，体现了不同的模块化设计思想。
* **代码溯源链接**:
  - 目标项目: `os/src/syscall/memory.rs#mmap`
  - 参考项目: `os/src/mm/memory_set.rs#mmap`

### 5. 单方模块与降权项
- **单方模块**: 【网络协议栈】、【网络协议栈/第三方库】、【第三方库】模块仅存在于参考项目中。这是两者架构上的根本性差异，目标项目是一个无网络功能的轻量级内核，而参考项目是一个功能更丰富的宏内核。此差异是独立开发的强有力证据。
- **降权项说明**:
  - **`getpid` 同名函数**: 目标位于 `os/src/syscall/process.rs`，参考位于 `os/src/task/task.rs`。`getpid` 是 POSIX 标准系统调用，其实现通常仅是返回当前任务结构体中的一个字段，逻辑极其简单，任何独立实现都必然相似。此为通用接口，不构成同源证据。
  - **`init_proc` 与 `init_proc_directory`**: 两者功能完全不同。目标是启动 `init` 用户进程，参考是创建 `/proc` 文件系统目录。仅因名字相似而被控方关联，属于跨模块、跨功能硬凑，已降权排除。
  - **跨板块配对**: 控方将目标 `wait4_pred` 与参考 `sys_execve` 等不同功能的函数强行并列，属于“同文件就近配对”的硬凑行为，不构成有效证据。

### 6. 保守结论
综合全模块 AST 覆盖矩阵与核心调用流对比，目标项目与参考项目在操作系统内核的核心子系统（进程管理、内存管理、文件系统、设备驱动）上**不存在可量化的结构性同源**。3.65% 的极低相似度主要由双方共同引用的开源 `ext4` 第三方库贡献，该部分重合与内核原创性无关。控方基于跨函数硬凑和对 POSIX 通用语义的误读所提出的“换皮洗稿”指控，在 AST 指纹覆盖率为 0% 的铁证前无法成立。双方在网络协议栈等关键模块上的有无之别，以及信号处理、任务同步等核心路径中锁机制与数据结构的异构设计，均指向独立开发。综上，本案同源性风险极低，应认定为独立作品。

------------------------------

