# 🛡️ OS 内核赛自动化代码级架构审计报告

**审计时间**: `2026-07-02 14:47:58`
**参赛作品名称**: `2026-T2026191459910624-XX`
**年份**: `2026`
**学校**: `XX`
**队伍名称**: `T2026191459910624`
**比赛名字**: `OS-Competition`
**比赛子赛道**: `Kernel-Implementation`
**仓库地址**: `https://gitlab.eduxiji.net/educg-group-43501-3132633/T2026191459910624-2198`
**推测开发语言**: `Rust / C (由AST探针自动识别)`
**特征函数量**: `1827`

## ⚔️ 底层代码同源性对比 (AST Homology)
> 本部分由专门的代码审查智能体，对该项目与符合年份条件的历届最高相似度项目进行控制流与底层调用对比。新版报告会先给出确定性的全模块覆盖矩阵，再给出 Agent 的分模块解释，避免只围绕少量抽样函数展开。

### 🏆 Top 3 底层对标: vs `2025-狗剩-海南大学`（海南大学 / 狗剩）

#### 对方作品信息

**对方年份**: `2025`
**对方学校**: `海南大学`
**对方队伍名称**: `狗剩`
**对方比赛名字**: `OS-Competition`
**对方比赛子赛道**: `Kernel-Implementation`
**对方仓库地址**: `https://gitlab.eduxiji.net/educg-group-36002-2710490/T202510589995148-2209`

> 双向覆盖率：共同 AST 指纹: 173；唯一 AST 指纹: 目标项目 1549，参考项目 3020；覆盖率: 目标项目 11.17%，参考项目 5.73%

## 全模块对比总览（确定性证据）

> 总体覆盖率：共同 AST 指纹: 173；唯一 AST 指纹: 目标项目 1549，参考项目 3020；覆盖率: 目标项目 11.17%，参考项目 5.73%

### 1. 模块覆盖矩阵
| 模块 | 目标函数数 | 参考函数数 | 共同唯一 AST 指纹 | Target 覆盖 | Base 覆盖 | 状态 |
| :--- | ---: | ---: | ---: | ---: | ---: | :--- |
| 进程管理 | 51 | 93 | 3 | 6.38% | 3.57% | 双方存在且有结构重合 |
| 任务调度/异步运行时 | 206 | 399 | 2 | 1.02% | 0.69% | 双方存在且有结构重合 |
| 内存管理 | 221 | 326 | 11 | 5.31% | 3.78% | 双方存在且有结构重合 |
| 中断/异常处理 | 3 | 62 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |
| 系统调用 | 141 | 153 | 1 | 0.76% | 0.78% | 双方存在且有结构重合 |
| 文件系统 | 579 | 747 | 96 | 19.79% | 16.96% | 双方存在且有结构重合 |
| 设备驱动 | 43 | 142 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |
| 网络协议栈 | 187 | 77 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |
| 文件系统/第三方库 | 37 | 689 | 16 | 43.24% | 2.73% | 双方存在且有结构重合 |
| 网络协议栈/第三方库 | 0 | 107 | 0 | 0.0% | 0.0% | 仅参考项目体现 |
| 第三方库 | 0 | 386 | 0 | 0.0% | 0.0% | 仅参考项目体现 |
| 通用模块/其他 | 359 | 665 | 3 | 1.13% | 0.61% | 双方存在且有结构重合 |

### 2. 分模块证据清单

#### 进程管理
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `kernel/src/task/ops.rs#do_exit` | `api/src/imp/task.rs#do_exit` | 同板块同名 | FutexKey::new_current, SignalInfo::new_kernel, Some, axtask::yield_now, clear_child_tid.vm_write, clear_child_tid.vm_write(0).is_ok ...(+31) | Layout::new::<Tid>, SignalInfo::new, UserPtr::<Tid>::from, axtask::exit, axtask::yield_now, clear_child_tid.get_as ...(+18) |
| `kernel/src/syscall/signal.rs#sys_rt_sigprocmask` | `api/src/imp/signal.rs#sys_rt_sigprocmask` | 同板块同名 | Err, Ok, check_sigset_size, curr.as_thread, current, debug! ...(+7) | Err, Ok, check_sigset, current, current()
        .task_ext, current()
        .task_ext()
        .thread
        .signal
        .with_blocked_mut::<LinuxResult<_>> ...(+2) |
| `kernel/src/syscall/ipc/shm.rs#clear_proc_shm` | `api/src/imp/ipc/shm.rs#clear_proc_shm` | 同板块同名 | self.get_inner_by_shmid, self.get_shmids_by_pid, self.remove_pid, self.remove_shmid, shm_inner.attach_count, shm_inner.detach_process ...(+1) | SHM_MANAGER.lock, shm_inner.detach_process, shm_inner.lock, shm_manager.get_inner_by_shmid, shm_manager.get_inner_by_shmid(shmid).unwrap, shm_manager.get_shmids_by_pid ...(+1) |
| `kernel/src/syscall/ipc/shm.rs#attach_process` | `api/src/imp/ipc/shm.rs#attach_process` | 同板块同名 | assert!, monotonic_time_nanos, self.va_range.insert | assert!, self.va_range.insert |

#### 任务调度/异步运行时
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `kernel/src/syscall/sync/futex.rs#sys_futex` | `api/src/imp/futex.rs#sys_futex` | 同板块同名 | AxError::from, Err, FutexKey::new_current, Ok, Some, assert_unsigned ...(+18) | Err, Ok, TimeValue::new, axtask::yield_now, futex_table.get, futex_table.get_or_insert ...(+14) |
| `kernel/src/syscall/signal.rs#sys_kill` | `api/src/imp/signal.rs#sys_kill` | 同板块同名 | Ok, Some, current, current().as_thread, current().as_thread().proc_data.proc.group, current().as_thread().proc_data.proc.group().pgid ...(+9) | Err, Ok, current, find_process, get_all_processes, make_siginfo ...(+3) |
| `kernel/src/syscall/ipc/shm.rs#sys_shmdt` | `api/src/imp/ipc/shm.rs#sys_shmdt` | 同板块同名 | Ok, SHM_MANAGER.lock, VirtAddr::from, aspace.unmap, curr.as_thread, current ...(+14) | Ok, SHM_MANAGER.lock, VirtAddr::from, aspace.unmap, axhal::arch::flush_tlb, curr.task_ext ...(+17) |
| `kernel/src/pseudofs/mod.rs#mount_all` | `core/src/vfs/mod.rs#mount_all` | 同板块同名 | FS_CONTEXT.lock, Ok, Path::new, Path::new("/sys/class/graphics/fb0/device").components, PathBuf::new, comp.as_str ...(+13) | Ok, dev::new_devfs, mount_at, proc::new_procfs, tmp::MemoryFs::new |

#### 内存管理
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `kernel/src/syscall/mm/mmap.rs#sys_mmap` | `src/syscall_imp/mm/mmap.rs#sys_mmap` | 同板块同名 | (addr + length).align_up, Arc::new, Backend::new_alloc, Backend::new_cow, Backend::new_file, Backend::new_linear ...(+50) | (addr + length).align_up, Err, MmapFlags::MAP_PRIVATE.bits, MmapFlags::MAP_SHARED.bits, MmapFlags::MAP_SHARED_VALIDATE.bits, MmapFlags::from_bits_truncate ...(+32) |
| `kernel/src/mm/loader.rs#load_user_app` | `core/src/mm.rs#load_user_app` | 同板块同名 | "/bin/sh".to_owned, (ustack_top - user_sp_aligned).align_up_4k, Backend::new_alloc, ELF_LOADER.lock, ELF_LOADER.lock().load, Err ...(+43) | CStr::from_bytes_with_nul, CStr::from_bytes_with_nul(interp)
                    .ok, CStr::from_bytes_with_nul(interp)
                    .ok()
                    .and_then, CStr::from_bytes_with_nul(interp)
                    .ok()
                    .and_then(|it| it.to_str().ok())
                    .ok_or, ElfFile::new, ElfFile::new(&file_data).map_err ...(+53) |
| `kernel/src/syscall/ipc/shm.rs#sys_shmat` | `api/src/imp/ipc/shm.rs#sys_shmat` | 同板块同名 | Arc::new, Backend::new_shared, Ok, SHM_MANAGER.lock, SharedPages::new, ShmAtFlags::from_bits_truncate ...(+27) | Err, MappingFlags::all, Ok, SHM_MANAGER.lock, ShmAtFlags::from_bits_truncate, VirtAddr::from ...(+26) |
| `kernel/src/mm/aspace/mod.rs#handle_page_fault` | `arceos/modules/axmm/src/aspace.rs#handle_page_fault` | 同板块同名 | VirtAddrRange::from_start_size, area.backend, area.backend().page_size, area.backend().populate, area.flags, cb ...(+6) | Self::handle_cow_fault, access_flags.contains, area
                    .backend, area
                    .backend()
                    .handle_page_fault, area.flags, debug! ...(+5) |

#### 中断/异常处理
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `kernel/src/pseudofs/dev/tty/terminal/termios.rs#default` | `arceos/ulib/axlibc/c/pow.c#exp_inline` | 同模块上下文对齐/差异观察 | ctl | __math_oflow, __math_uflow, asdouble, asuint64, converttoint, eval_as_double ...(+4) |
| `kernel/src/file/signalfd.rs#from_signal_info` | `arceos/modules/axhal/src/arch/aarch64/trap.rs#handle_sync_exception` | 同模块上下文对齐/差异观察 | sig_info.code, sig_info.errno, sig_info.signo | ESR_EL1.extract, crate::trap::handle_syscall, crate::trap::post_trap_callback, debug!, esr.read, esr.read_as_enum ...(+6) |
| `kernel/src/pseudofs/dev/tty/terminal/termios.rs#signo_for` | `arceos/modules/axhal/src/arch/x86_64/idt.rs#new` | 同模块上下文对齐/差异观察 | Some, self.special_char | InterruptDescriptorTable::new, core::mem::transmute, core::slice::from_raw_parts_mut, entries[i].set_handler_fn, opt.set_privilege_level |

#### 系统调用
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `kernel/src/syscall/io_mpx/epoll.rs#sys_epoll_ctl` | `api/src/imp/io_mpx/epoll.rs#sys_epoll_ctl` | 同板块同名 | Epoll::from_fd, EpollFlags::from_bits, EpollFlags::from_bits(event.events & !events.bits()).ok_or, Err, IoEvents::from_bits_truncate, Ok ...(+7) | EpollInstance::from_fd, EpollInstance::from_fd(epfd)?.control, Err, Ok, debug!, event.get_as_mut |
| `kernel/src/syscall/ipc/shm.rs#sys_shmctl` | `api/src/imp/ipc/shm.rs#sys_shmctl` | 同板块同名 | Err, Ok, SHM_MANAGER.lock, buf.get_as_mut, monotonic_time_nanos, nullable! ...(+3) | Err, Ok, SHM_MANAGER.lock, shm_inner.lock, shm_manager
            .get_inner_by_shmid, shm_manager
            .get_inner_by_shmid(shmid)
            .ok_or |
| `kernel/src/syscall/io_mpx/poll.rs#sys_ppoll` | `api/src/imp/io_mpx/poll.rs#sys_ppoll` | 同板块同名 | check_sigset_size, do_poll, fds.get_as_mut_slice, nfds.try_into, nfds.try_into().map_err, nullable! ...(+4) | Err, core::slice::from_raw_parts_mut, fds.get_as_array, poll_impl, timeout.is_none, timeout.nullable ...(+2) |
| `kernel/src/syscall/io_mpx/select.rs#sys_pselect6` | `api/src/imp/io_mpx/poll.rs#sys_pselect6` | 同板块同名 | do_select, nullable!, nullable!(timeout.get_as_ref())?
            .map, nullable!(timeout.get_as_ref())?
            .map(|ts| ts.try_into_time_value())
            .transpose, ts.try_into_time_value | Err, do_select, timeout.is_none, timeout.nullable, timeout.unwrap |

#### 文件系统
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `kernel/src/syscall/fs/fd_ops.rs#sys_fcntl` | `api/src/imp/fd/fd_ops.rs#sys_fcntl` | 同板块同名 | FD_TABLE
                .read, FD_TABLE
                .read()
                .get, FD_TABLE
                .read()
                .get(fd as _)
                .ok_or, FD_TABLE
                .write, FD_TABLE
                .write()
                .get_mut, FD_TABLE
                .write()
                .get_mut(fd as _)
                .ok_or ...(+15) | Ok, debug!, dup_fd, get_file_like, get_file_like(fd)?.set_nonblocking, warn! |
| `kernel/src/syscall/fs/io.rs#sys_splice` | `api/src/imp/io.rs#sys_splice` | 同板块同名 | DummyFd::from_fd, DummyFd::from_fd(fd_in).is_ok, DummyFd::from_fd(fd_out).is_ok, Err, File::from_fd, Pipe::from_fd ...(+20) | (len - total_copied).min, Err, Ok, SeekFrom::Current, SeekFrom::Start, buf.len ...(+43) |
| `kernel/src/pseudofs/dev/event.rs#read_at` | `arceos/modules/axfs-ng/src/fs/fat/file.rs#read_at` | 同板块同名 | Err, Ok, buf.chunks_exact_mut, buf.is_empty, buf.len, inner.has_event ...(+8) | Ok, SeekFrom::Start, file.read, file.read(buf).map_err, file.seek, file.seek(SeekFrom::Start(offset)).map_err ...(+2) |
| `axio-patched/src/utils/take.rs#seek` | `arceos/modules/axfs-ng/src/highlevel/file.rs#seek` | 同板块同名 | Err, Ok, Some, self.inner.seek_relative, self.len.checked_add_signed, self.limit.wrapping_sub ...(+3) | (|| {
            Ok(match pos {
                SeekFrom::Start(pos) => pos,
                SeekFrom::End(off) => {
                    let size = self.access(FileFlags::empty())?.len()?;
                    size.checked_add_signed(off)
                        .ok_or(VfsError::EINVAL)?
                        .clamp(0, size)
                }
                SeekFrom::Current(off) => {
                    //let size = self.access(FileFlags::empty())?.len()?;
                    //error!("file size:{size}");
                    self.position
                        .checked_add_signed(off)
                        .ok_or(VfsError::EINVAL)?
                    //.clamp(0, size)
                }
            })
        }), (|| {
            Ok(match pos {
                SeekFrom::Start(pos) => pos,
                SeekFrom::End(off) => {
                    let size = self.access(FileFlags::empty())?.len()?;
                    size.checked_add_signed(off)
                        .ok_or(VfsError::EINVAL)?
                        .clamp(0, size)
                }
                SeekFrom::Current(off) => {
                    //let size = self.access(FileFlags::empty())?.len()?;
                    //error!("file size:{size}");
                    self.position
                        .checked_add_signed(off)
                        .ok_or(VfsError::EINVAL)?
                    //.clamp(0, size)
                }
            })
        })()
        .map_err, FileFlags::empty, Ok, self.access, self.access(FileFlags::empty())?.len ...(+5) |

#### 设备驱动
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `axnet-ng-patched/src/device/ethernet.rs#send` | `arceos/tools/bwbench_client/src/device.rs#send` | 同板块同名 | Self::send_to, Some, buf.copy_from_slice, dst_buffer.copy_from_slice, next_hop.is_broadcast, packet.len ...(+7) | Err, Ok, buffer.as_ptr, buffer.len, err.kind, libc::send ...(+2) |
| `axnet-ng-patched/src/lib.rs#init_network` | `arceos/modules/axnet/src/lib.rs#init_network` | 同板块同名 | "eth0".to_owned, Box::new, EthernetAddress, EthernetDevice::new, GATEWAY.parse, GATEWAY.parse().expect ...(+28) | info!, net_devs.take_one, net_devs.take_one().expect, net_impl::init |
| `axnet-ng-patched/src/device/ethernet.rs#recv` | `arceos/tools/bwbench_client/src/device.rs#recv` | 同板块同名 | matches!, rx_buf.packet, self.handle_frame, self.inner.receive, self.inner.recycle_rx_buffer, self.inner.recycle_rx_buffer(rx_buf).unwrap ...(+2) | Err, Ok, buffer.as_mut_ptr, buffer.len, err.kind, libc::recv ...(+2) |
| `axnet-ng-patched/src/router.rs#add_device` | `arceos/modules/axdriver/src/lib.rs#add_device` | 同板块同名 | self.devices.len, self.devices.push | self.block.push, self.display.push, self.net.push |

#### 网络协议栈
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `axnet-ng-patched/src/udp.rs#recv` | `api/src/fd/net.rs#recv` | 同板块同名 | Err, ExpectedRemote::Any, ExpectedRemote::Expecting, Ok, SocketAddrEx::Ip, ax_bail! ...(+19) | Ok, tcpsocket.lock, tcpsocket.lock().recv, udpsocket.lock, udpsocket.lock().recv_from, udpsocket.lock().recv_from(buf).map |
| `axnet-ng-patched/src/udp.rs#send` | `api/src/fd/net.rs#send` | 同板块同名 | Err, IpAddr::V4, IpEndpoint::from, Ok, PacketMeta::default, SocketAddr::new ...(+22) | Ok, tcpsocket.lock, tcpsocket.lock().send, udpsocket.lock, udpsocket.lock().send |
| `axnet-ng-patched/src/vsock/stream.rs#recv` | `arceos/api/arceos_posix_api/src/imp/net.rs#recv` | 同板块同名 | Err, Ok, conn.lock, conn_guard.advance_rx_read, conn_guard.rx_buffer_used, conn_guard.rx_closed ...(+9) | Ok, tcpsocket.lock, tcpsocket.lock().recv, udpsocket.lock, udpsocket.lock().recv_from, udpsocket.lock().recv_from(buf).map |
| `axnet-ng-patched/src/tcp.rs#recv` | `arceos/ulib/axstd/src/net/udp.rs#recv` | 同板块同名 | Err, Ok, ax_err_type!, dst.remaining_mut, dst.write, options.flags.contains ...(+12) | api::ax_udp_recv |

#### 文件系统/第三方库
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `axfs-ng-patched/src/fs/ext4/inode.rs#create` | `arceos/modules/axfs-ng/src/fs/ext4/inode.rs#create` | 同板块同名 | DirEntry::new_dir, DirEntry::new_file, DirNode::new, Err, FileNode::new, Inode::new ...(+14) | DirEntry::new_dir, DirEntry::new_file, DirNode::new, Err, FileNode::new, Inode::new ...(+11) |
| `axfs-ng-patched/src/fs/ext4/inode.rs#metadata` | `arceos/modules/axfs-ng/src/fs/ext4/inode.rs#metadata` | 同板块同名同结构 | DeviceId::default, FileAttr::default, NodePermission::from_bits_truncate, Ok, into_vfs_type, self.fs
            .lock ...(+2) | DeviceId::default, FileAttr::default, NodePermission::from_bits_truncate, Ok, into_vfs_type, self.fs
            .lock ...(+2) |
| `axfs-ng-patched/src/fs/ext4/inode.rs#read_dir` | `arceos/modules/axfs-ng/src/fs/ext4/inode.rs#read_dir` | 同板块同名同结构 | Ok, core::str::from_utf8, core::str::from_utf8(entry.name())
                .map_err, core::str::from_utf8(entry.name())
                .map_err(|_| VfsError::InvalidData)?
                .to_owned, entry.ino, entry.inode_type ...(+10) | Ok, core::str::from_utf8, core::str::from_utf8(entry.name())
                .map_err, core::str::from_utf8(entry.name())
                .map_err(|_| VfsError::EINVAL)?
                .to_owned, entry.ino, entry.inode_type ...(+10) |
| `axfs-ng-patched/src/fs/ext4/inode.rs#update_metadata` | `arceos/modules/axfs-ng/src/fs/ext4/inode.rs#update_metadata` | 同板块同名 | Ok, fs.with_inode_ref, fs.with_inode_ref(self.ino, |inode| {
            if let Some(mode) = update.mode {
                inode.set_mode((inode.mode() & !0xfff) | (mode.bits() as u32));
            }
            if let Some((uid, gid)) = update.owner {
                inode.set_owner(uid as _, gid as _);
            }
            if let Some(atime) = update.atime {
                inode.set_atime(&atime);
            }
            if let Some(mtime) = update.mtime {
                inode.set_mtime(&mtime);
            }
            inode.update_ctime();
            Ok(())
        })
        .map_err, inode.mode, inode.set_atime, inode.set_mode ...(+5) | Ok, fs.with_inode_ref, fs.with_inode_ref(self.ino, |inode| {
            if let Some(mode) = update.mode {
                inode.set_mode((inode.mode() & !0xfff) | (mode.bits() as u32));
            }
            if let Some((uid, gid)) = update.owner {
                inode.set_owner(uid as _, gid as _);
            }
            if let Some(atime) = update.atime {
                inode.set_atime(&atime);
            }
            if let Some(mtime) = update.mtime {
                inode.set_mtime(&mtime);
            }
            Ok(())
        })
        .map_err, inode.mode, inode.set_atime, inode.set_mode ...(+4) |

#### 网络协议栈/第三方库
仅参考项目中观察到该模块代表函数，目标项目没有同模块可对齐证据：
- 参考项目：`arceos/modules/axnet/src/smoltcp_impl/tcp.rs#connect`，调用摘要：Err, Ok, SOCKET_SET
                .with_socket_mut::<tcp::Socket, _, _>, SOCKET_SET.add, SocketSetWrapper::new_tcp_socket, Some ...(+29)
- 参考项目：`arceos/modules/axnet/src/smoltcp_impl/dns.rs#query`，调用摘要：Err, Ok, SOCKET_SET
            .with_socket_mut::<dns::Socket, _, _>, SOCKET_SET
            .with_socket_mut::<dns::Socket, _, _>(handle, |socket| {
                socket.start_query(iface.lock().context(), name, query_type)
            })
            .map_err, SOCKET_SET.poll_interfaces, SOCKET_SET.with_socket_mut::<dns::Socket, _, _> ...(+12)
- 参考项目：`arceos/modules/axnet/src/smoltcp_impl/bench.rs#bench_transmit_bandwidth`，调用摘要：(current_time - past_time).secs, AxNetTxToken::consume, InterfaceWrapper::current_time, info!, self.transmit, tx_buf[0..12].fill ...(+2)
- 参考项目：`arceos/modules/axnet/src/smoltcp_impl/tcp.rs#shutdown`，调用摘要：LISTEN_TABLE.unlisten, Ok, SOCKET_SET.poll_interfaces, SOCKET_SET.with_socket_mut::<tcp::Socket, _, _>, debug!, self.handle.get ...(+9)

#### 第三方库
仅参考项目中观察到该模块代表函数，目标项目没有同模块可对齐证据：
- 参考项目：`crates/visionfive2-sd/src/lib.rs#send_cmd`，调用摘要：RawInterrupt::from, RawInterruptStatusReg::from, S::sleep_ms_until, Some, arg.into, assert! ...(+24)
- 参考项目：`crates/visionfive2-sd/src/lib.rs#init_sdcard`，调用摘要：CardTypeReg::from, CardTypeReg::from(0).with_card_width4_1, CmdArg::new, CmdReg::from, ControlReg::from, RawInterruptStatusReg::from ...(+20)
- 参考项目：`crates/page_table_multiarch/page_table_multiarch/src/bits64.rs#map_region`，调用摘要：Err, M::flush_tlb, Ok, PageSize::Size1G.is_aligned, PageSize::Size2M.is_aligned, PageSize::Size4K.is_aligned ...(+12)
- 参考项目：`crates/axdriver_crates/axdriver_virtio/src/net.rs#try_new`，调用摘要：Err, InnerDev::new, InnerDev::new(transport).map_err, NetBufPool::new, Ok, Some ...(+14)

#### 通用模块/其他
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `axio-patched/src/buffered/linewriter/shim.rs#write_all` | `arceos/ulib/axlibc/src/strftime.rs#write_all` | 同板块同名 | buf.split_at, memchr::memrchr, self.buffer.flush_buf, self.buffer.write_all, self.buffered, self.buffered().is_empty ...(+3) | Err, Ok, buf.len, self.inner.write_all |
| `axio-patched/src/read/mod.rs#read_until` | `arceos/ulib/axstd/src/io/stdio.rs#read_until` | 同板块同名 | Ok, available.len, buf.extend_from_slice, memchr::memchr, self.consume, self.fill_buf | self.inner.read_until |
| `axio-patched/src/utils/take.rs#fill_buf` | `arceos/ulib/axstd/src/io/stdio.rs#fill_buf` | 同板块同名 | Ok, buf.len, cmp::min, self.inner.fill_buf | self.inner.fill_buf |
| `axio-patched/src/write/mod.rs#write_str` | `arceos/modules/axlog/src/lib.rs#write_str` | 同板块同名 | Err, Ok, s.as_bytes, self.inner.write_all | Ok, cfg_if::cfg_if! |


好的，审计官。以下是基于提供的证据底座、双方画像及控辩主张，生成的最终同源性定性仲裁报告。

---

#### 1. 宏观判定
- **定性结论**: **独立开发，底层架构显著差异**
- **核心依据**: 客观 AST 相似度仅为 **3.94%**，双向覆盖率均处于极低水平（目标项目 11.17%，参考项目 5.73%）。该指标远低于“同源”或“系统性复用”的阈值。控方提出的“核心铁证”经技术析理后，被证实为受限于 POSIX 规范、Linux 内核机制及 Rust 语言原子操作惯用法的必然收敛，不具备代码复用层面的区分力。双方在进程模型、文件描述符管理、虚拟文件系统实现等核心架构上存在根本性异构，支持独立开发的结论。

#### 2. 全模块覆盖矩阵解读
- **进程管理**: 双方均存在。共同 AST 指纹仅 3 个，覆盖率极低（目标 6.38%，参考 3.57%）。控方指控的 `set_clear_child_tid` 等“铁证”实为 Linux 标准机制与 Rust 原子操作惯用法。证据强度：**弱**。
- **任务调度/异步运行时**: 双方均存在。共同 AST 指纹仅 2 个，覆盖率极低（目标 1.02%，参考 0.69%）。双方在调度器实现、等待队列机制上无显著结构重合。证据强度：**未形成**。
- **内存管理**: 双方均存在。共同 AST 指纹 11 个，覆盖率低（目标 5.31%，参考 3.78%）。同名函数如 `sys_mmap` 和 `handle_page_fault` 的调用流分析显示，双方在后端内存分配、页错误处理的具体逻辑上存在差异。证据强度：**弱**。
- **中断/异常处理**: 双方均存在。共同 AST 指纹为 0。目标项目该模块极小（3个函数），与参考项目（62个函数）规模差异巨大，无任何结构重合。证据强度：**未形成**。
- **系统调用**: 双方均存在。共同 AST 指纹仅 1 个，覆盖率极低（目标 0.76%，参考 0.78%）。同名系统调用由 POSIX 规范强制，内部实现路径（如 `sys_epoll_ctl`）差异明显。证据强度：**未形成**。
- **文件系统**: 双方均存在。共同 AST 指纹 96 个，覆盖率相对较高（目标 19.79%，参考 16.96%）。重合主要集中在 `axfs-ng` 的 ext4 实现及 `axio` 工具库，这些属于第三方库或公共参考实现的衍生，不代表核心架构复用。证据强度：**中**（但指向第三方库复用，非核心逻辑抄袭）。
- **设备驱动**: 双方均存在。共同 AST 指纹为 0。双方驱动模型和具体设备实现完全不同。证据强度：**未形成**。
- **网络协议栈**: 双方均存在。共同 AST 指纹为 0。目标项目使用 `axnet-ng-patched`，参考项目使用 `axnet`，且内部实现（如 socket 操作）调用路径不同。证据强度：**未形成**。
- **第三方库/公共依赖**: 参考项目存在大量第三方库（107+386个函数），目标项目较少（37个函数）。文件系统模块的相似度主要由 `axfs-ng` 等公共依赖贡献，此为正常现象，不构成核心指控。

#### 3. 底层 AST 关键证据指纹表
| 所属板块 | 目标项目路径 | 参考项目路径 | 证据类型 | 技术说明 |
| :--- | :--- | :--- | :--- | :--- |
| 进程管理 | `kernel/src/task/mod.rs#set_clear_child_tid` | `core/src/task/thread.rs#set_clear_child_tid` | 弱信号-语言惯用法 | `self.clear_child_tid.store` 是 Rust 中操作 `AtomicU64` 等原子类型的标准 API，受 Linux `set_tid_address` 机制约束，命名和实现必然收敛。 |
| 进程管理 | `kernel/src/task/mod.rs#clear_child_tid` | `core/src/task/thread.rs#clear_child_tid` | 弱信号-语言惯用法 | 同上，`.load` 是原子类型的标准读取方法，与 `.store` 成对出现是数据结构访问的必然性。 |
| 进程管理 | `kernel/src/syscall/ipc/shm.rs#attach_process` | `api/src/imp/ipc/shm.rs#attach_process` | 弱信号-领域逻辑 | `self.va_range.insert` 是共享内存附着的必要操作，但目标项目额外包含 `monotonic_time_nanos` 调用，实现细节不同。 |
| 文件系统 | `axfs-ng-patched/src/fs/ext4/inode.rs#metadata` | `arceos/modules/axfs-ng/src/fs/ext4/inode.rs#metadata` | 强信号-第三方库同源 | 调用图谱几乎完全一致，包括 `DeviceId::default`、`NodePermission::from_bits_truncate` 等。表明双方均使用了同源的 `axfs-ng` 库。 |
| 文件系统 | `axfs-ng-patched/src/fs/ext4/inode.rs#read_dir` | `arceos/modules/axfs-ng/src/fs/ext4/inode.rs#read_dir` | 强信号-第三方库同源 | 核心逻辑完全一致，仅错误类型变体名不同（`InvalidData` vs `EINVAL`），是典型的同一代码库不同版本或轻微修改的特征。 |
| 文件系统 | `axfs-ng-patched/src/fs/ext4/inode.rs#update_metadata` | `arceos/modules/axfs-ng/src/fs/ext4/inode.rs#update_metadata` | 强信号-第三方库同源 | 包含完全相同的闭包结构 `fs.with_inode_ref(self.ino, \|inode\| { ... })` 及内部逻辑，确认为同源代码。 |

#### 4. 核心代码调用流对比剖析
* **【进程管理/进程退出路径】**
* **底层技术析理**: 双方均遵循 POSIX 规范实现 `sys_exit` 和 `sys_exit_group`，并委托给内部的 `do_exit` 函数。然而，`do_exit` 的实现揭示了完全不同的资源回收模型。目标项目围绕 `FutexKey`、`SHM_MANAGER` 和 `vm_write` 进行清理，表明其进程模型深度集成 futex 和共享内存，且内存操作基于虚拟地址写入。参考项目则使用 `Layout::new::<Tid>`、`UserPtr::<Tid>::from` 和 `close_all_filelike`，显示出基于物理地址查询和显式类型布局的资源释放策略。两者在清理路径、内存抽象和同步原语上存在根本性架构差异。
* **代码溯源链接**:
  - 目标项目: `kernel/src/syscall/task/exit.rs#sys_exit` -> `kernel/src/task/ops.rs#do_exit`
  - 参考项目: `src/syscall_imp/task/thread.rs#sys_exit` -> `api/src/imp/task.rs#do_exit`

* **【内存管理/页错误处理】**
* **底层技术析理**: 双方均有 `handle_page_fault` 函数，但处理逻辑异构。目标项目的调用流显示其通过 `area.backend().populate` 和 `area.backend().page_size` 直接与后端交互，暗示其 VMA 区域直接关联一个负责填充和提供页大小的后端对象。参考项目的调用流则包含 `Self::handle_cow_fault` 和 `access_flags.contains`，表明其将写时复制（COW）逻辑内聚在页错误处理中，并显式检查访问权限。两者在错误解析粒度和后端职责划分上采取了不同策略。
* **代码溯源链接**:
  - 目标项目: `kernel/src/mm/aspace/mod.rs#handle_page_fault`
  - 参考项目: `arceos/modules/axmm/src/aspace.rs#handle_page_fault`

* **【系统调用/信号掩码管理】**
* **底层技术析理**: `sys_rt_sigprocmask` 的实现路径体现了线程/信号模型的根本差异。目标项目通过 `curr.as_thread` 直接访问线程结构，并调用 `check_sigset_size` 和 `oldset.vm_write`，表明其信号集操作直接作用于线程，且结果通过虚拟内存写入用户空间。参考项目则通过 `current().task_ext().thread.signal.with_blocked_mut` 的调用链，显示出“任务扩展”和“信号管理器”的多层抽象，信号操作被封装在专门的信号模块中。两者在对象模型和操作封装上完全不同。
* **代码溯源链接**:
  - 目标项目: `kernel/src/syscall/signal.rs#sys_rt_sigprocmask`
  - 参考项目: `api/src/imp/signal.rs#sys_rt_sigprocmask`

#### 5. 单方模块与降权项
- **单方模块**: “网络协议栈/第三方库”和“第三方库”模块仅在参考项目中体现。这反映了参考项目依赖了更多外部 crate（如 `visionfive2-sd`, `page_table_multiarch`），而目标项目未使用。这属于项目依赖和功能范围的正常差异，不构成同源证据。
- **降权项说明**:
  - **POSIX 系统调用同名**: `sys_getpid`, `sys_getppid`, `sys_exit_group` 等函数名由规范强制，不能作为抄袭证据。
  - **通用宏与标准库调用**: `Ok`, `Err`, `Some`, `Arc::new`, `Box::new`, `format!` 等是 Rust 语言的基础元素，在任何项目中都会大量出现，不具备指纹区分度。
  - **跨板块证据**: 控方将目标项目的 `axnet-ng-patched/src/device/ethernet.rs#process_arp`（网络设备驱动）归入“进程管理”板块，属于分类错误，此类证据无效。
  - **重复条目**: 目标证据中 `lookup_child` 和 `child_names` 函数被重复列出，人为放大了证据数量，经合并后其指控力度显著降低。

#### 6. 保守结论
综合客观 AST 相似度（3.94%）、双向覆盖率（11.17%/5.73%）及全模块深度析理，本次审计未发现支持“系统性代码复用”或“抄袭”指控的实质性证据。双方在进程模型、内存管理、信号处理等核心子系统上展现出显著的架构异构性。观察到的相似度主要集中在 `axfs-ng` 等第三方公共依赖库，属于正常的技术栈复用。控方提出的“核心铁证”均为受规范、语言或领域逻辑强约束的原子操作，不具备代码原创性区分度。因此，整体风险评级为**低**，支持双方为独立开发的结论。

------------------------------

