# 🛡️ OS 内核赛自动化代码级架构审计报告

**审计时间**: `2026-07-02 14:47:58`
**参赛作品名称**: `2026-T2026191459910624-XX`
**年份**: `2026`
**学校**: `XX`
**队伍名称**: `T2026191459910624`
**比赛名字**: `OS-Competition`
**比赛子赛道**: `Kernel-Implementation`
**仓库地址**: `https://gitlab.eduxiji.net/educg-group-43501-3132633/T2026191459910624-2198`
**推测开发语言**: `Rust / C (由AST探针自动识别)`
**特征函数量**: `1827`

## ⚔️ 底层代码同源性对比 (AST Homology)
> 本部分由专门的代码审查智能体，对该项目与符合年份条件的历届最高相似度项目进行控制流与底层调用对比。新版报告会先给出确定性的全模块覆盖矩阵，再给出 Agent 的分模块解释，避免只围绕少量抽样函数展开。

### 🏆 Top 2 底层对标: vs `2025-StarryX-杭州电子科技大学`（杭州电子科技大学 / StarryX）

#### 对方作品信息

**对方年份**: `2025`
**对方学校**: `杭州电子科技大学`
**对方队伍名称**: `StarryX`
**对方比赛名字**: `OS-Competition`
**对方比赛子赛道**: `Kernel-Implementation`
**对方仓库地址**: `https://gitlab.eduxiji.net/educg-group-36002-2710490/T202510336995486-3037`

> 双向覆盖率：共同 AST 指纹: 181；唯一 AST 指纹: 目标项目 1549，参考项目 2897；覆盖率: 目标项目 11.68%，参考项目 6.25%

## 全模块对比总览（确定性证据）

> 总体覆盖率：共同 AST 指纹: 181；唯一 AST 指纹: 目标项目 1549，参考项目 2897；覆盖率: 目标项目 11.68%，参考项目 6.25%

### 1. 模块覆盖矩阵
| 模块 | 目标函数数 | 参考函数数 | 共同唯一 AST 指纹 | Target 覆盖 | Base 覆盖 | 状态 |
| :--- | ---: | ---: | ---: | ---: | ---: | :--- |
| 进程管理 | 51 | 172 | 4 | 8.51% | 2.52% | 双方存在且有结构重合 |
| 任务调度/异步运行时 | 206 | 480 | 8 | 4.08% | 2.28% | 双方存在且有结构重合 |
| 内存管理 | 221 | 430 | 16 | 7.73% | 4.11% | 双方存在且有结构重合 |
| 中断/异常处理 | 3 | 70 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |
| 系统调用 | 141 | 89 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |
| 文件系统 | 579 | 801 | 87 | 17.94% | 14.01% | 双方存在且有结构重合 |
| 设备驱动 | 43 | 139 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |
| 网络协议栈 | 187 | 77 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |
| 文件系统/第三方库 | 37 | 164 | 16 | 43.24% | 10.19% | 双方存在且有结构重合 |
| 网络协议栈/第三方库 | 0 | 166 | 0 | 0.0% | 0.0% | 仅参考项目体现 |
| 第三方库 | 0 | 349 | 0 | 0.0% | 0.0% | 仅参考项目体现 |
| 通用模块/其他 | 359 | 665 | 5 | 1.88% | 0.94% | 双方存在且有结构重合 |

### 2. 分模块证据清单

#### 进程管理
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `kernel/src/task/ops.rs#do_exit` | `xapi/src/task/exit.rs#do_exit` | 同板块同名 | FutexKey::new_current, SignalInfo::new_kernel, Some, axtask::yield_now, clear_child_tid.vm_write, clear_child_tid.vm_write(0).is_ok ...(+31) | FD_TABLE.clear, FutexKey::new, IPC_MANAGER.clear, SignalInfo::new, UserPtr::<Pid>::from, axtask::exit ...(+32) |
| `kernel/src/syscall/signal.rs#sys_rt_sigprocmask` | `xapi/src/task/signal.rs#sys_rt_sigprocmask` | 同板块同名 | Err, Ok, check_sigset_size, curr.as_thread, current, debug! ...(+7) | Err, Ok, XProcess::from_thread, XProcess::from_thread(thread).uspace, XThread::from_thread, check_sigset_size ...(+3) |
| `kernel/src/task/ops.rs#exit_robust_list` | `xapi/src/task/futex.rs#exit_robust_list` | 同板块同名 | Err, Ok, axtask::yield_now, core::ptr::eq, entry.vm_read, handle_futex_death ...(+1) | Err, Ok, UserPtr::from, axtask::yield_now, core::ptr::eq, handle_futex_death ...(+2) |
| `kernel/src/syscall/ipc/shm.rs#clear_proc_shm` | `xapi/src/ipc/shm.rs#clear_proc_shm` | 同板块同名 | self.get_inner_by_shmid, self.get_shmids_by_pid, self.remove_pid, self.remove_shmid, shm_inner.attach_count, shm_inner.detach_process ...(+1) | IPC_MANAGER.with_shm, segment.detach_process, segment.detach_process(pid).is_ok, segment.lock, segment.should_remove, shm_manager.cleanup_orphaned_segments ...(+3) |

#### 任务调度/异步运行时
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `kernel/src/syscall/sync/futex.rs#sys_futex` | `xapi/src/task/futex.rs#sys_futex` | 同板块同名 | AxError::from, Err, FutexKey::new_current, Ok, Some, assert_unsigned ...(+18) | Err, FutexKey::new, Ok, axtask::yield_now, current, current().task_ext ...(+24) |
| `kernel/src/syscall/signal.rs#sys_kill` | `xapi/src/task/signal.rs#sys_kill` | 同板块同名 | Ok, Some, current, current().as_thread, current().as_thread().proc_data.proc.group, current().as_thread().proc_data.proc.group().pgid ...(+9) | Ok, get_process, get_process_group, make_siginfo, proc.is_init, process.group ...(+5) |
| `kernel/src/syscall/resources.rs#sys_prlimit64` | `xapi/src/sys/resource.rs#sys_prlimit64` | 同板块同名 | Err, Ok, get_process_data, new_limit.nullable, new_limit.vm_read_uninit, new_limit.vm_read_uninit()?.assume_init ...(+4) | Err, Ok, XProcess::from_process, get_process, nullable!, trace! ...(+3) |
| `kernel/src/syscall/ipc/shm.rs#sys_shmdt` | `xapi/src/ipc/shm.rs#sys_shmdt` | 同板块同名 | Ok, SHM_MANAGER.lock, VirtAddr::from, aspace.unmap, curr.as_thread, current ...(+14) | IPC_MANAGER.with_shm, Ok, VirtAddr::from, axhal::arch::flush_tlb, process.pid, segment
                .detach_process ...(+16) |

#### 内存管理
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `kernel/src/syscall/mm/mmap.rs#sys_mmap` | `xapi/src/mm/mmap.rs#sys_mmap` | 同板块同名 | (addr + length).align_up, Arc::new, Backend::new_alloc, Backend::new_cow, Backend::new_file, Backend::new_linear ...(+50) | (addr + length).align_up, Err, FD_TABLE.is_assigned, File::from_fd, File::from_fd(fd, FileFlags::READ, FileFlags::empty())
            .map_err, FileFlags::empty ...(+39) |
| `kernel/src/syscall/task/clone.rs#do_clone` | `xapi/src/task/clone.rs#do_clone` | 同板块同名 | (parent_tid as *mut Pid).vm_write, (parent_tid as *mut Pid).vm_write(tid).ok, (pidfd as *mut i32).vm_write, Arc::new, AxTaskExt::from_impl, Default::default ...(+70) | Arc::default, Arc::new, CloneFlags::from_bits_truncate, Err, FD_TABLE
                .deref_from, FD_TABLE
                .deref_from(&process_data.ns)
                .init_new ...(+76) |
| `kernel/src/syscall/ipc/shm.rs#sys_shmat` | `xapi/src/ipc/shm.rs#sys_shmat` | 同板块同名 | Arc::new, Backend::new_shared, Ok, SHM_MANAGER.lock, SharedPages::new, ShmAtFlags::from_bits_truncate ...(+27) | Err, IPC_MANAGER.with_shm, Ok, ShmAtFlags::from_bits_truncate, VirtAddr::from, VirtAddrRange::new ...(+17) |
| `kernel/src/mm/aspace/mod.rs#handle_page_fault` | `src/mm.rs#handle_page_fault` | 同板块同名 | VirtAddrRange::from_start_size, area.backend, area.backend().page_size, area.backend().populate, area.flags, cb ...(+6) | (xcore::config::USER_STACK_TOP - xcore::config::USER_STACK_SIZE
        ..xcore::config::USER_STACK_TOP)
        .contains, SignalInfo::new, Signo::from_repr, Signo::from_repr(SIGSEGV as u8).unwrap, XTaskExt::from_task, current ...(+17) |

#### 中断/异常处理
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `kernel/src/pseudofs/dev/tty/terminal/termios.rs#default` | `arceos/ulib/axlibc/c/pow.c#exp_inline` | 同模块上下文对齐/差异观察 | ctl | __math_oflow, __math_uflow, asdouble, asuint64, converttoint, eval_as_double ...(+4) |
| `kernel/src/file/signalfd.rs#from_signal_info` | `xmodules/xsignal/src/types.rs#default_action` | 同模块上下文对齐/差异观察 | sig_info.code, sig_info.errno, sig_info.signo | 内部闭环逻辑 |
| `kernel/src/pseudofs/dev/tty/terminal/termios.rs#signo_for` | `arceos/modules/axhal/src/arch/aarch64/trap.rs#handle_sync_exception` | 同模块上下文对齐/差异观察 | Some, self.special_char | ESR_EL1.extract, crate::trap::handle_syscall, crate::trap::post_trap_callback, debug!, esr.read, esr.read_as_enum ...(+6) |

#### 系统调用
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `kernel/src/syscall/io_mpx/epoll.rs#sys_epoll_ctl` | `arceos/api/arceos_posix_api/src/imp/io_mpx/epoll.rs#sys_epoll_ctl` | 同板块同名 | Epoll::from_fd, EpollFlags::from_bits, EpollFlags::from_bits(event.events & !events.bits()).ok_or, Err, IoEvents::from_bits_truncate, Ok ...(+7) | debug!, syscall_body! |
| `kernel/src/syscall/ipc/shm.rs#sys_shmctl` | `xapi/src/ipc/shm.rs#sys_shmctl` | 同板块同名 | Err, Ok, SHM_MANAGER.lock, buf.get_as_mut, monotonic_time_nanos, nullable! ...(+3) | Err, IPC_MANAGER.with_shm, Ok, buf.is_null, nullable!, segment.lock ...(+5) |
| `kernel/src/syscall/io_mpx/poll.rs#sys_ppoll` | `xapi/src/iomux/poll.rs#sys_ppoll` | 同板块同名 | check_sigset_size, do_poll, fds.get_as_mut_slice, nfds.try_into, nfds.try_into().map_err, nullable! ...(+4) | nullable!, nullable!(uspace.read(timeout))?
            .map, nullable!(uspace.read(timeout))?
            .map(timespec::to_time_value)
            .transpose, poll, uspace.raw_slice, with_uspace |
| `kernel/src/syscall/io_mpx/select.rs#sys_select` | `xapi/src/iomux/select.rs#sys_select` | 同板块同名 | 0.into, do_select, it.try_into_time_value, nullable!, nullable!(timeout.get_as_ref())?
            .map, nullable!(timeout.get_as_ref())?
            .map(|it| it.try_into_time_value())
            .transpose | do_select, nullable!, nullable!(uspace.read(timeout))?
                .map, nullable!(uspace.read(timeout))?
                .map(timeval::to_time_value)
                .transpose, with_uspace |

#### 文件系统
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `kernel/src/pseudofs/dev/fb.rs#ioctl` | `xcore/src/fs/vfs/dev/loopx.rs#ioctl` | 同板块同名 | (arg as *mut FixScreenInfo).vm_write, (arg as *mut VarScreenInfo).vm_write, Err, Ok, axdisplay::framebuffer_info | Err, Ok, Some, argp.address, argp.address().as_usize, argp.cast ...(+24) |
| `kernel/src/pseudofs/dev/loop.rs#ioctl` | `xcore/src/fs/vfs/dev/tty.rs#ioctl` | 同板块同名 | (arg as *const loop_info).vm_read_uninit, (arg as *const loop_info).vm_read_uninit()?.assume_init, (arg as *const u32).vm_read, (arg as *mut loop_info).vm_write, (arg as *mut u32).vm_write, (arg as *mut u64).vm_write ...(+22) | Err, Ok, argp.cast::<termios>, argp.cast::<winsize>, debug!, self.get_termios ...(+6) |
| `kernel/src/syscall/fs/fd_ops.rs#sys_fcntl` | `xapi/src/fs/fd_ops.rs#sys_fcntl` | 同板块同名 | FD_TABLE
                .read, FD_TABLE
                .read()
                .get, FD_TABLE
                .read()
                .get(fd as _)
                .ok_or, FD_TABLE
                .write, FD_TABLE
                .write()
                .get_mut, FD_TABLE
                .write()
                .get_mut(fd as _)
                .ok_or ...(+15) | Err, FD_TABLE.has_cloexec, FD_TABLE.is_assigned, FD_TABLE.set_cloexec, NodePermission::from_bits_truncate, Ok ...(+8) |
| `kernel/src/syscall/fs/io.rs#sys_splice` | `xapi/src/fs/io.rs#sys_splice` | 同板块同名 | DummyFd::from_fd, DummyFd::from_fd(fd_in).is_ok, DummyFd::from_fd(fd_out).is_ok, Err, File::from_fd, Pipe::from_fd ...(+20) | Err, File::from_fd, Pipe::from_fd, Pipe::from_fd(fd_in, FileFlags::READ, FileFlags::PATH).is_ok, Pipe::from_fd(fd_out, FileFlags::WRITE, FileFlags::PATH).is_ok, SendFile::Direct ...(+14) |

#### 设备驱动
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `axnet-ng-patched/src/device/ethernet.rs#send` | `arceos/tools/bwbench_client/src/device.rs#send` | 同板块同名 | Self::send_to, Some, buf.copy_from_slice, dst_buffer.copy_from_slice, next_hop.is_broadcast, packet.len ...(+7) | Err, Ok, buffer.as_ptr, buffer.len, err.kind, libc::send ...(+2) |
| `axnet-ng-patched/src/lib.rs#init_network` | `arceos/modules/axnet/src/lib.rs#init_network` | 同板块同名 | "eth0".to_owned, Box::new, EthernetAddress, EthernetDevice::new, GATEWAY.parse, GATEWAY.parse().expect ...(+28) | info!, net_devs.take_one, net_impl::init, warn! |
| `axnet-ng-patched/src/device/ethernet.rs#recv` | `arceos/tools/bwbench_client/src/device.rs#recv` | 同板块同名 | matches!, rx_buf.packet, self.handle_frame, self.inner.receive, self.inner.recycle_rx_buffer, self.inner.recycle_rx_buffer(rx_buf).unwrap ...(+2) | Err, Ok, buffer.as_mut_ptr, buffer.len, err.kind, libc::recv ...(+2) |
| `axnet-ng-patched/src/router.rs#add_device` | `arceos/modules/axdriver/src/lib.rs#add_device` | 同板块同名 | self.devices.len, self.devices.push | self.block.push, self.display.push, self.net.push |

#### 网络协议栈
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `axnet-ng-patched/src/udp.rs#recv` | `xcore/src/net/socket.rs#recv` | 同板块同名 | Err, ExpectedRemote::Any, ExpectedRemote::Expecting, Ok, SocketAddrEx::Ip, ax_bail! ...(+19) | Ok, tcpsocket.lock, tcpsocket.lock().recv, udpsocket.lock, udpsocket.lock().recv_from, udpsocket.lock().recv_from(buf).map ...(+2) |
| `axnet-ng-patched/src/udp.rs#send` | `arceos/api/arceos_posix_api/src/imp/net.rs#send` | 同板块同名 | Err, IpAddr::V4, IpEndpoint::from, Ok, PacketMeta::default, SocketAddr::new ...(+22) | Ok, tcpsocket.lock, tcpsocket.lock().send, udpsocket.lock, udpsocket.lock().send |
| `axnet-ng-patched/src/vsock/stream.rs#recv` | `arceos/api/arceos_posix_api/src/imp/net.rs#recv` | 同板块同名 | Err, Ok, conn.lock, conn_guard.advance_rx_read, conn_guard.rx_buffer_used, conn_guard.rx_closed ...(+9) | Ok, tcpsocket.lock, tcpsocket.lock().recv, udpsocket.lock, udpsocket.lock().recv_from, udpsocket.lock().recv_from(buf).map |
| `axnet-ng-patched/src/tcp.rs#recv` | `arceos/ulib/axstd/src/net/udp.rs#recv` | 同板块同名 | Err, Ok, ax_err_type!, dst.remaining_mut, dst.write, options.flags.contains ...(+12) | api::ax_udp_recv |

#### 文件系统/第三方库
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `axfs-ng-patched/src/fs/ext4/inode.rs#create` | `arceos/modules/axfs-ng/src/fs/ext4/inode.rs#create` | 同板块同名 | DirEntry::new_dir, DirEntry::new_file, DirNode::new, Err, FileNode::new, Inode::new ...(+14) | DirEntry::new_dir, DirEntry::new_file, DirNode::new, Err, FileNode::new, Inode::new ...(+11) |
| `axfs-ng-patched/src/fs/ext4/inode.rs#metadata` | `arceos/modules/axfs-ng/src/fs/ext4/inode.rs#metadata` | 同板块同名同结构 | DeviceId::default, FileAttr::default, NodePermission::from_bits_truncate, Ok, into_vfs_type, self.fs
            .lock ...(+2) | DeviceId::default, FileAttr::default, NodePermission::from_bits_truncate, Ok, into_vfs_type, self.fs
            .lock ...(+2) |
| `axfs-ng-patched/src/fs/ext4/inode.rs#read_dir` | `arceos/modules/axfs-ng/src/fs/ext4/inode.rs#read_dir` | 同板块同名同结构 | Ok, core::str::from_utf8, core::str::from_utf8(entry.name())
                .map_err, core::str::from_utf8(entry.name())
                .map_err(|_| VfsError::InvalidData)?
                .to_owned, entry.ino, entry.inode_type ...(+10) | Ok, core::str::from_utf8, core::str::from_utf8(entry.name())
                .map_err, core::str::from_utf8(entry.name())
                .map_err(|_| VfsError::EINVAL)?
                .to_owned, entry.ino, entry.inode_type ...(+10) |
| `axfs-ng-patched/src/fs/ext4/inode.rs#update_metadata` | `arceos/modules/axfs-ng/src/fs/ext4/inode.rs#update_metadata` | 同板块同名 | Ok, fs.with_inode_ref, fs.with_inode_ref(self.ino, |inode| {
            if let Some(mode) = update.mode {
                inode.set_mode((inode.mode() & !0xfff) | (mode.bits() as u32));
            }
            if let Some((uid, gid)) = update.owner {
                inode.set_owner(uid as _, gid as _);
            }
            if let Some(atime) = update.atime {
                inode.set_atime(&atime);
            }
            if let Some(mtime) = update.mtime {
                inode.set_mtime(&mtime);
            }
            inode.update_ctime();
            Ok(())
        })
        .map_err, inode.mode, inode.set_atime, inode.set_mode ...(+5) | Ok, fs.with_inode_ref, fs.with_inode_ref(self.ino, |inode| {
            if let Some(mode) = update.mode {
                inode.set_mode((inode.mode() & !0xfff) | (mode.bits() as u32));
            }
            if let Some((uid, gid)) = update.owner {
                inode.set_owner(uid as _, gid as _);
            }
            if let Some(atime) = update.atime {
                inode.set_atime(&atime);
            }
            if let Some(mtime) = update.mtime {
                inode.set_mtime(&mtime);
            }
            Ok(())
        })
        .map_err, inode.mode, inode.set_atime, inode.set_mode ...(+4) |

#### 网络协议栈/第三方库
仅参考项目中观察到该模块代表函数，目标项目没有同模块可对齐证据：
- 参考项目：`arceos/modules/axnet/src/smoltcp_impl/tcp.rs#connect`，调用摘要：Err, Ok, Ok::<(IpEndpoint, IpEndpoint), NetError>, SOCKET_SET
                .with_socket_mut::<tcp::Socket, _, _>, SOCKET_SET.add, SocketSetWrapper::new_tcp_socket ...(+33)
- 参考项目：`arceos/modules/axnet/src/smoltcp_impl/dns.rs#query`，调用摘要：Err, IpAddr::from, Ok, SOCKET_SET
            .with_socket_mut::<dns::Socket, _, _>, SOCKET_SET
            .with_socket_mut::<dns::Socket, _, _>(handle, |socket| {
                socket.start_query(iface.lock().context(), name, query_type)
            })
            .map_err, SOCKET_SET.poll_interfaces ...(+11)
- 参考项目：`arceos/modules/axnet/src/smoltcp_impl/bench.rs#bench_transmit_bandwidth`，调用摘要：(current_time - past_time).secs, AxNetTxToken::consume, InterfaceWrapper::current_time, info!, self.transmit, tx_buf[0..12].fill ...(+2)
- 参考项目：`arceos/modules/axnet/src/smoltcp_impl/tcp.rs#recv_timeout`，调用摘要：Err, Ok, SOCKET_SET.with_socket_mut::<tcp::Socket, _, _>, current_ticks, self.block_on, self.handle.get ...(+9)

#### 第三方库
仅参考项目中观察到该模块代表函数，目标项目没有同模块可对齐证据：
- 参考项目：`arceos/crates/page_table_multiarch/page_table_multiarch/src/bits64.rs#map_region`，调用摘要：Err, M::flush_tlb, Ok, PageSize::Size1G.is_aligned, PageSize::Size2M.is_aligned, PageSize::Size4K.is_aligned ...(+12)
- 参考项目：`arceos/crates/axdriver_crates/axdriver_virtio/src/net.rs#try_new`，调用摘要：Err, InnerDev::new, InnerDev::new(transport).map_err, NetBufPool::new, Ok, Some ...(+14)
- 参考项目：`arceos/crates/page_table_multiarch/page_table_multiarch/src/bits64.rs#protect_region`，调用摘要：Err, Ok, TlbFlushAll::new, assert!, error!, self.protect ...(+5)
- 参考项目：`arceos/crates/page_table_multiarch/page_table_multiarch/src/arch/loongarch64.rs#flush_tlb`，调用摘要：asm!

#### 通用模块/其他
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `axio-patched/src/buffered/linewriter/shim.rs#write_all` | `arceos/ulib/axlibc/src/strftime.rs#write_all` | 同板块同名 | buf.split_at, memchr::memrchr, self.buffer.flush_buf, self.buffer.write_all, self.buffered, self.buffered().is_empty ...(+3) | Err, Ok, buf.len, self.inner.write_all |
| `axio-patched/src/read/mod.rs#read_until` | `arceos/ulib/axstd/src/io/stdio.rs#read_until` | 同板块同名 | Ok, available.len, buf.extend_from_slice, memchr::memchr, self.consume, self.fill_buf | self.inner.read_until |
| `axio-patched/src/utils/take.rs#fill_buf` | `arceos/ulib/axstd/src/io/stdio.rs#fill_buf` | 同板块同名 | Ok, buf.len, cmp::min, self.inner.fill_buf | self.inner.fill_buf |
| `axio-patched/src/write/mod.rs#write_str` | `arceos/modules/axlog/src/lib.rs#write_str` | 同板块同名 | Err, Ok, s.as_bytes, self.inner.write_all | Ok, cfg_if::cfg_if! |


### 1. 宏观判定
- **定性结论**: 底层架构显著差异 / 独立开发 (相似度: 4.24%，共同 AST 指纹: 181；唯一 AST 指纹: 目标项目 1549，参考项目 2897；覆盖率: 目标项目 11.68%，参考项目 6.25%)

### 2. 全模块覆盖矩阵解读
- **进程管理**
  - 双方存在性：双方均存在。
  - 共同 AST 指纹：存在（4个）。
  - 覆盖率解读：目标覆盖率 8.51%，参考覆盖率 2.52%。覆盖率极低，表明双方在进程管理的具体实现上存在巨大差异。
  - 证据强度：**弱**。所谓“同板块底层逻辑一致”的函数均为单行原子操作或简单状态返回，属于不可保护的惯用表达。其他同名函数（如 `do_exit`）在核心资源管理对象（`SHM_MANAGER` vs `IPC_MANAGER`）和清理流程上存在显著差异。

- **任务调度/异步运行时**
  - 双方存在性：双方均存在。
  - 共同 AST 指纹：存在（8个）。
  - 覆盖率解读：目标覆盖率 4.08%，参考覆盖率 2.28%。覆盖率极低。
  - 证据强度：**弱**。同名函数（如 `sys_futex`）的调用图谱显示，目标项目使用 `FutexKey::new_current` 和 `assert_unsigned`，而参考项目使用 `FutexKey::new` 和 `axtask::yield_now`，表明其内部同步和调度原语存在差异。

- **内存管理**
  - 双方存在性：双方均存在。
  - 共同 AST 指纹：存在（16个）。
  - 覆盖率解读：目标覆盖率 7.73%，参考覆盖率 4.11%。覆盖率低。
  - 证据强度：**弱**。同名函数（如 `sys_mmap`）的调用图谱显示，目标项目使用 `Backend::new_alloc`、`Backend::new_cow` 等后端抽象，而参考项目使用 `File::from_fd` 等文件系统集成方式，表明内存映射的后端实现模型不同。

- **中断/异常处理**
  - 双方存在性：双方均存在。
  - 共同 AST 指纹：无（0个）。
  - 覆盖率解读：目标覆盖率 0.0%，参考覆盖率 0.0%。双方在此核心模块无任何结构重合。
  - 证据强度：**未形成**。控辩双方提供的证据均为跨模块的弱信号，不构成同板块主证据。

- **系统调用**
  - 双方存在性：双方均存在。
  - 共同 AST 指纹：无（0个）。
  - 覆盖率解读：目标覆盖率 0.0%，参考覆盖率 0.0%。双方在此核心模块无任何结构重合。
  - 证据强度：**未形成**。同名函数（如 `sys_epoll_ctl`）的调用图谱显示，目标项目有具体的 `Epoll::from_fd` 等实现，而参考项目仅为 `syscall_body!` 宏调用，实现层次完全不同。

- **文件系统**
  - 双方存在性：双方均存在。
  - 共同 AST 指纹：存在（87个）。
  - 覆盖率解读：目标覆盖率 17.94%，参考覆盖率 14.01%。覆盖率相对较高。
  - 证据强度：**中**。高覆盖率主要由 `axfs-ng-patched` 与 `arceos/modules/axfs-ng` 的相似性贡献，这源于双方共同依赖的 ArceOS 上游生态及 ext4 文件系统标准实现，而非目标项目对参考项目的抄袭。

- **设备驱动**
  - 双方存在性：双方均存在。
  - 共同 AST 指纹：无（0个）。
  - 覆盖率解读：目标覆盖率 0.0%，参考覆盖率 0.0%。双方在此模块无任何结构重合。
  - 证据强度：**未形成**。同名函数（如 `send`）的调用图谱显示，目标项目操作的是 `Self::send_to` 和 `packet.len` 等内核协议栈对象，而参考项目调用的是 `libc::send`，属于用户态测试工具，本质不同。

- **网络协议栈**
  - 双方存在性：双方均存在。
  - 共同 AST 指纹：无（0个）。
  - 覆盖率解读：目标覆盖率 0.0%，参考覆盖率 0.0%。双方在此模块无任何结构重合。
  - 证据强度：**未形成**。同名函数（如 `tcp::recv`）的调用图谱显示，目标项目直接操作 `conn.lock` 等内部连接状态，而参考项目则通过 `tcpsocket.lock` 等更高层抽象进行操作。

- **第三方库/公共依赖**
  - 双方存在性：参考项目存在大量第三方库模块，目标项目无。
  - 共同 AST 指纹：无（0个）。
  - 覆盖率解读：目标覆盖率 0.0%，参考覆盖率 0.0%。
  - 证据强度：**未形成**。此部分体现了参考项目更庞大的代码基数，与抄袭指控无关。目标项目中的 `axfs-ng-patched`、`axio-patched` 等与参考项目共有的部分，应归因于共同上游 ArceOS 生态。

### 3. 底层 AST 关键证据指纹表
| 所属板块 | 目标项目路径 | 参考项目路径 | 证据类型 | 技术说明 |
| :--- | :--- | :--- | :--- | :--- |
| 进程管理 | `kernel/src/task/mod.rs#set_clear_child_tid` | `xcore/src/task/proc.rs#set_clear_child_tid` | 弱信号-简单原子操作 | 均为 `self.clear_child_tid.store`，是 Linux `set_tid_address` 机制的标准单行实现，变量名由 Linux 内核规范决定，不具备独创性。 |
| 进程管理 | `kernel/src/task/ops.rs#do_exit` | `xapi/src/task/exit.rs#do_exit` | 弱信号-领域共性 | 目标使用 `FutexKey::new_current`、`SHM_MANAGER`；参考使用 `FutexKey::new`、`IPC_MANAGER`。核心资源管理器与清理流程不同。 |
| 进程管理 | `kernel/src/syscall/fs/pidfd.rs#sys_pidfd_getfd` | `xapi/src/fs/fd/pid.rs#sys_pidfd_getfd` | 弱信号-架构差异 | 目标通过 `FD_TABLE.scope` 获取 fd，参考通过 `FD_TABLE.deref_from`。两者采用了不同的命名空间/作用域抽象模型。 |
| 任务调度/异步运行时 | `kernel/src/syscall/sync/futex.rs#sys_futex` | `xapi/src/task/futex.rs#sys_futex` | 弱信号-实现差异 | 目标使用 `FutexKey::new_current` 和 `assert_unsigned`；参考使用 `FutexKey::new` 和 `axtask::yield_now`。Futex 键的生成与调度原语不同。 |
| 内存管理 | `kernel/src/syscall/mm/mmap.rs#sys_mmap` | `xapi/src/mm/mmap.rs#sys_mmap` | 弱信号-模型差异 | 目标使用 `Backend::new_alloc`、`Backend::new_cow` 等后端抽象；参考使用 `File::from_fd` 集成文件系统。内存映射后端模型异构。 |
| 内存管理 | `kernel/src/syscall/task/clone.rs#do_clone` | `xapi/src/task/clone.rs#do_clone` | 弱信号-流程差异 | 目标使用 `AxTaskExt::from_impl` 扩展任务；参考使用 `FD_TABLE.deref_from` 初始化新命名空间。进程创建的扩展机制不同。 |
| 文件系统 | `axfs-ng-patched/src/fs/ext4/inode.rs#metadata` | `arceos/modules/axfs-ng/src/fs/ext4/inode.rs#metadata` | 中信号-公共依赖 | AST 结构高度一致，调用 `DeviceId::default`、`FileAttr::default` 等。源于双方共同依赖的 ArceOS `axfs-ng` 公共 crate。 |
| 文件系统 | `kernel/src/syscall/fs/fd_ops.rs#sys_fcntl` | `xapi/src/fs/fd_ops.rs#sys_fcntl` | 弱信号-实现差异 | 目标直接操作 `FD_TABLE` 的读写锁；参考使用 `FD_TABLE.has_cloexec`、`FD_TABLE.set_cloexec` 等封装方法。文件描述符操作的抽象层次不同。 |
| 通用模块/其他 | `axio-patched/src/buffered/linewriter/shim.rs#write_all` | `arceos/ulib/axlibc/src/strftime.rs#write_all` | 弱信号-跨模块同名 | 函数同名但功能无关。目标是缓冲写入器，参考是时间格式化写入，属于泛型函数名巧合。 |

### 4. 核心代码调用流对比剖析
* **【进程管理/进程退出路径】**
* **底层技术析理**: 目标项目的 `do_exit` 使用 `FutexKey::new_current` 创建当前线程的 Futex 键，并通过 `SHM_MANAGER.lock().clear_proc_shm` 进行共享内存清理，最后调用 `axtask::yield_now` 让出 CPU。参考项目的 `do_exit` 则使用 `FutexKey::new` 创建键，通过 `IPC_MANAGER.clear` 进行 IPC 清理，并直接调用 `axtask::exit` 终止任务。两者在进程退出这一核心路径上，采用了不同的 IPC/共享内存管理器（`SHM_MANAGER` vs `IPC_MANAGER`）和不同的任务终止原语（`yield_now` vs `exit`），体现了底层资源管理与调度策略的架构差异。
* **代码溯源链接**:
  - 目标项目: `kernel/src/task/ops.rs#do_exit`
  - 参考项目: `xapi/src/task/exit.rs#do_exit`

* **【内存管理/内存映射路径】**
* **底层技术析理**: 目标项目的 `sys_mmap` 系统调用在创建内存映射时，根据参数选择 `Backend::new_alloc`、`Backend::new_cow` 或 `Backend::new_file` 等多种后端策略，显示其拥有一个抽象良好的内存后端层。参考项目的 `sys_mmap` 则直接通过 `File::from_fd` 获取文件对象，并将其作为映射后端，表明其内存映射与文件系统耦合更紧密。两者在内存映射的后端模型上存在根本性异构。
* **代码溯源链接**:
  - 目标项目: `kernel/src/syscall/mm/mmap.rs#sys_mmap`
  - 参考项目: `xapi/src/mm/mmap.rs#sys_mmap`

* **【文件系统/公共依赖路径】**
* **底层技术析理**: 目标项目的 `axfs-ng-patched/src/fs/ext4/inode.rs` 与参考项目的 `arceos/modules/axfs-ng/src/fs/ext4/inode.rs` 在 `metadata`、`read_dir` 等函数上表现出高度一致的 AST 结构。两者均调用 `DeviceId::default`、`FileAttr::default` 并遵循相同的 ext4 inode 操作流程。这并非一方抄袭另一方，而是双方均基于 ArceOS 生态中的 `axfs-ng` 公共 crate 进行开发或修补，属于共同上游依赖。
* **代码溯源链接**:
  - 目标项目: `axfs-ng-patched/src/fs/ext4/inode.rs#metadata`
  - 参考项目: `arceos/modules/axfs-ng/src/fs/ext4/inode.rs#metadata`

### 5. 单方模块与降权项
- **仅参考项目体现的模块**：`网络协议栈/第三方库`、`第三方库`。这些模块（如 `page_table_multiarch`、`axdriver_virtio`）仅在参考项目中存在，是构成其更大代码规模（2897 个唯一 AST 指纹）的部分原因，与目标项目无关，不能作为同源证据。
- **跨板块硬凑证据**：控方将目标项目网络协议栈的 `process_arp` 函数归入“进程管理”板块进行指控，属于典型的跨板块误用。该证据无效。
- **POSIX/规范强约束接口**：`sys_getpid`、`sys_fork`、`sys_exit_group` 等函数名、参数及核心语义由 POSIX/Linux 标准强制规定。双方在这些接口上的相似性属于规范收敛，不能作为抄袭证据。
- **泛型宏与语言惯用法**：`debug!`、`println!`、`syscall_body!`、`nullable!` 等宏以及 `Some`、`Ok`、`Err` 等枚举构造是 Rust 语言和框架的通用惯用法，不具备项目独创性，应从有效证据中排除。

### 6. 保守结论
综合审计，目标项目与参考项目的整体 AST 相似度仅为 4.24%，双向覆盖率均处于低位。在进程管理、内存管理等核心模块中，双方虽存在少量同名函数和相似的调用片段，但这主要源于共同的 POSIX/Linux 规范约束、ArceOS 上游生态依赖以及不可保护的简单原子操作。深入分析核心调用流（如 `do_exit`、`sys_mmap`）表明，双方在资源管理器、内存后端模型、任务调度原语等关键架构决策上存在显著差异。跨板块硬凑证据和单方模块的存在进一步削弱了抄袭指控。因此，现有证据远不足以支持“系统性代码复用”或“实质性抄袭”的结论，目标项目应被视为独立开发。

------------------------------

