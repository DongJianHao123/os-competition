# 🛡️ OS 内核赛自动化代码级架构审计报告

**审计时间**: `2026-07-02 14:47:58`
**参赛作品名称**: `2026-T2026191459910624-XX`
**年份**: `2026`
**学校**: `XX`
**队伍名称**: `T2026191459910624`
**比赛名字**: `OS-Competition`
**比赛子赛道**: `Kernel-Implementation`
**仓库地址**: `https://gitlab.eduxiji.net/educg-group-43501-3132633/T2026191459910624-2198`
**推测开发语言**: `Rust / C (由AST探针自动识别)`
**特征函数量**: `1827`

## ⚔️ 底层代码同源性对比 (AST Homology)
> 本部分由专门的代码审查智能体，对该项目与符合年份条件的历届最高相似度项目进行控制流与底层调用对比。新版报告会先给出确定性的全模块覆盖矩阵，再给出 Agent 的分模块解释，避免只围绕少量抽样函数展开。

### 🏆 Top 5 底层对标: vs `2022-Maturin-清华大学`（清华大学 / Maturin）

#### 对方作品信息

**对方年份**: `2022`
**对方学校**: `清华大学`
**对方队伍名称**: `Maturin`
**对方比赛名字**: `OS-Competition`
**对方比赛子赛道**: `Kernel-Implementation`
**对方仓库地址**: `https://gitlab.eduxiji.net/scPointer/maturin.git`

> 双向覆盖率：共同 AST 指纹: 65；唯一 AST 指纹: 目标项目 1549，参考项目 1491；覆盖率: 目标项目 4.2%，参考项目 4.36%

## 全模块对比总览（确定性证据）

> 总体覆盖率：共同 AST 指纹: 65；唯一 AST 指纹: 目标项目 1549，参考项目 1491；覆盖率: 目标项目 4.2%，参考项目 4.36%

### 1. 模块覆盖矩阵
| 模块 | 目标函数数 | 参考函数数 | 共同唯一 AST 指纹 | Target 覆盖 | Base 覆盖 | 状态 |
| :--- | ---: | ---: | ---: | ---: | ---: | :--- |
| 进程管理 | 51 | 60 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |
| 任务调度/异步运行时 | 206 | 160 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |
| 内存管理 | 221 | 275 | 1 | 0.48% | 0.38% | 双方存在且有结构重合 |
| 中断/异常处理 | 3 | 4 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |
| 系统调用 | 141 | 8 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |
| 文件系统 | 579 | 593 | 3 | 0.62% | 0.56% | 双方存在且有结构重合 |
| 设备驱动 | 43 | 15 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |
| 网络协议栈 | 187 | 8 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |
| 文件系统/第三方库 | 37 | 0 | 0 | 0.0% | 0.0% | 仅目标项目体现 |
| 第三方库 | 0 | 335 | 0 | 0.0% | 0.0% | 仅参考项目体现 |
| 通用模块/其他 | 359 | 218 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |

### 2. 分模块证据清单

#### 进程管理
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `kernel/src/syscall/task/execve.rs#sys_execve` | `kernel/src/syscall/process.rs#sys_execve` | 同板块同名 | Arc::new, Default::default, Err, FD_TABLE.write, FS_CONTEXT.lock, FS_CONTEXT.lock().resolve ...(+42) | sys_exec |
| `kernel/src/syscall/task/thread.rs#sys_getppid` | `kernel/src/syscall/process.rs#sys_getppid` | 同板块同名 | current, current()
        .as_thread, current()
        .as_thread()
        .proc_data
        .proc
        .parent, current()
        .as_thread()
        .proc_data
        .proc
        .parent()
        .ok_or, current()
        .as_thread()
        .proc_data
        .proc
        .parent()
        .ok_or(AxError::NoSuchProcess)
        .map, p.pid | Ok, get_current_task, get_current_task().unwrap, get_current_task().unwrap().get_ppid |
| `kernel/src/syscall/task/exit.rs#sys_exit` | `kernel/src/syscall/process.rs#sys_exit` | 同板块同名 | Ok, do_exit | exit_current_task, panic! |
| `kernel/src/syscall/task/thread.rs#sys_getpid` | `kernel/src/syscall/process.rs#sys_getpid` | 同板块同名 | Ok, current, current().as_thread, current().as_thread().proc_data.proc.pid | Ok, get_current_task, get_current_task().unwrap, get_current_task().unwrap().get_pid_num |

#### 任务调度/异步运行时
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `kernel/src/syscall/net/socket.rs#sys_socket` | `kernel/src/syscall/socket.rs#sys_socket` | 同板块同名 | AxError::from, DgramTransport::new, Err, Socket, SocketInner::Tcp, SocketInner::Udp ...(+16) | Arc::new, Err, Ok, Socket::new, fd_manager.push, get_current_task ...(+2) |
| `kernel/src/syscall/fs/mount.rs#sys_mount` | `kernel/src/syscall/fs.rs#sys_mount` | 同板块同名 | Err, FS_CONTEXT.lock, FS_CONTEXT.lock().resolve, MemoryFs::new, Ok, axfs::get_blkdev_fs ...(+5) | Err, Ok, get_current_task, get_current_task().unwrap, mount_fat_fs, mount_path.ends_with ...(+3) |
| `kernel/src/syscall/fs/ctl.rs#sys_chdir` | `kernel/src/syscall/fs.rs#sys_chdir` | 同板块同名 | FS_CONTEXT.lock, Ok, debug!, fs.resolve, fs.set_current_dir, vm_load_string | Err, Ok, String::from, check_dir_exists, current_path.clone, current_path.ends_with ...(+6) |
| `kernel/src/task/timer.rs#output` | `kernel/src/task/time_stat.rs#output` | 同板块同名 | time_value_from_nanos | self.stime_us.into, self.utime_us.into |

#### 内存管理
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `kernel/src/syscall/mm/mmap.rs#sys_mmap` | `kernel/src/syscall/process.rs#sys_mmap` | 同板块同名 | (addr + length).align_up, Arc::new, Backend::new_alloc, Backend::new_cow, Backend::new_file, Backend::new_linear ...(+50) | BackEndFile::new, Err, Ok, SeekFrom::Start, Some, drop ...(+11) |
| `kernel/src/mm/aspace/mod.rs#handle_page_fault` | `kernel/src/memory/areas/mod.rs#handle_page_fault` | 同板块同名 | VirtAddrRange::from_start_size, area.backend, area.backend().page_size, area.backend().populate, area.flags, cb ...(+6) | (*entry).is_valid, (*entry).set_all, Err, Ok, Some, align_down ...(+7) |
| `kernel/src/mm/aspace/backend/cow.rs#unmap` | `kernel/src/memory/page_table.rs#unmap` | 同板块同名 | FRAME_TABLE
                    .lock, FRAME_TABLE
                    .lock()
                    .get_frame_ref, FRAME_TABLE
                    .lock()
                    .get_frame_ref(frame)
                    .ok_or, Ok, assert_eq!, debug! ...(+4) | (*pte).clear, (*pte).is_valid, Err, Ok, println!, self.find_pte |
| `kernel/src/mm/aspace/mod.rs#map` | `kernel/src/memory/page_table.rs#map` | 同板块同名 | MemoryArea::new, Ok, self.areas.map, self.populate_area, self.validate_region | Err, Ok, flags.is_empty, println!, pte.is_valid, pte.set_all ...(+1) |

#### 中断/异常处理
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `kernel/src/pseudofs/dev/tty/terminal/termios.rs#default` | `libc/src/functional/fcntl.c#main` | 同模块上下文对齐/差异观察 | ctl | TEST, TESTE, _exit, fclose, fcntl, fileno ...(+4) |
| `kernel/src/file/signalfd.rs#from_signal_info` | `kernel/src/signal/sig_action.rs#get_restorer` | 同模块上下文对齐/差异观察 | sig_info.code, sig_info.errno, sig_info.signo | self.flags.contains |
| `kernel/src/pseudofs/dev/tty/terminal/termios.rs#signo_for` | `kernel/src/trap/mod.rs#init` | 同模块上下文对齐/差异观察 | Some, self.special_char | stvec::write |

#### 系统调用
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `kernel/src/syscall/net/opt.rs#sys_getsockopt` | `kernel/src/syscall/flags.rs#default` | 同模块上下文对齐/差异观察 | Err, Ok, Socket::from_fd, call_dispatch!, debug!, optlen.get_as_mut ...(+3) | Self::from_str |
| `kernel/src/syscall/time.rs#sys_setitimer` | `libc/src/regression/syscall-sign-extend.c#main` | 同模块上下文对齐/差异观察 | ITimerType::from_repr, ITimerType::from_repr(which).ok_or, Ok, curr
        .as_thread, curr
        .as_thread()
        .time
        .borrow_mut, curr
        .as_thread()
        .time
        .borrow_mut()
        .set_itimer ...(+12) | T, open, syscall, t_error |
| `kernel/src/syscall/io_mpx/epoll.rs#sys_epoll_ctl` | `kernel/src/syscall/flags.rs#set_info` | 同模块上下文对齐/差异观察 | Epoll::from_fd, EpollFlags::from_bits, EpollFlags::from_bits(event.events & !events.bits()).ok_or, Err, IoEvents::from_bits_truncate, Ok ...(+7) | 内部闭环逻辑 |
| `kernel/src/syscall/net/opt.rs#sys_setsockopt` | `kernel/src/syscall/flags.rs#resolve_clone_flags_and_signal` | 同模块上下文对齐/差异观察 | Err, Ok, Socket::from_fd, call_dispatch!, debug!, size_of::<T> ...(+2) | CloneFlags::from_bits, CloneFlags::from_bits(flag as u32 & (!0x3f)).unwrap, SignalNo::try_from, SignalNo::try_from(flag as u8 & 0x3f).unwrap |

#### 文件系统
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `axio-patched/src/utils/take.rs#seek` | `rust-fatfs/src/file.rs#seek` | 同板块同名 | Err, Ok, Some, self.inner.seek_relative, self.len.checked_add_signed, self.limit.wrapping_sub ...(+3) | Err, Ok, Some, debug_assert!, error!, i64::from ...(+16) |
| `kernel/src/file/epoll.rs#consume` | `fscommon/src/buf_stream.rs#consume` | 同板块同名 | ConsumeResult::EventAndKeep, ConsumeResult::EventAndRemove, file.poll, matched.is_empty, mode.should_notify, self.mode.lock ...(+1) | cmp::min |
| `axfs-ng-patched/src/highlevel/file.rs#seek` | `rust-fatfs/src/fs.rs#seek` | 同板块同名 | FileFlags::empty, Ok, guard
                    .checked_add_signed, guard
                    .checked_add_signed(off)
                    .ok_or, guard.lock, self.access ...(+5) | Err, Ok, Some, error!, i64::try_from, i64::try_from(self.offset)
                .ok ...(+9) |
| `kernel/src/pseudofs/tmp.rs#rename` | `rust-fatfs/src/dir.rs#rename` | 同板块同名 | Ok, dst_dir.downcast::<Self>, dst_dir.lookup, dst_name.into, dst_node
            .inode
            .as_dir, dst_node
            .inode
            .as_dir()?
            .entries
            .lock ...(+8) | Some, dst_dir.find_entry, e.to_dir, e.to_dir().rename, self.find_entry, self.rename ...(+3) |

#### 设备驱动
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `axnet-ng-patched/src/router.rs#dispatch` | `kernel/src/drivers/block/mod.rs#block_device_test` | 同模块上下文对齐/差异观察 | IpAddress::Ipv4, IpAddress::Ipv6, IpVersion::of_packet, IpVersion::of_packet(packet).expect, assert_eq!, dev.send ...(+12) | BLOCK_DEVICE.clone, assert_eq!, block_device.read_block, block_device.write_block, println!, write_buffer.iter_mut |
| `axnet-ng-patched/src/device/vsock.rs#handle_vsock_event` | `kernel/src/drivers/block/virtio_block.rs#read_block` | 同模块上下文对齐/差异观察 | PENDING_EVENTS
                    .lock, PENDING_EVENTS
                    .lock()
                    .push_back, VSOCK_CONN_MANAGER.lock, VsockDriverEvent::Received, buf.len, conn.lock ...(+12) | self.0
            .lock, self.0
            .lock()
            .read_block, self.0
            .lock()
            .read_block(block_id, buf)
            .expect |
| `axnet-ng-patched/src/device/ethernet.rs#send` | `kernel/src/drivers/block/virtio_block.rs#write_block` | 同模块上下文对齐/差异观察 | Self::send_to, Some, buf.copy_from_slice, dst_buffer.copy_from_slice, next_hop.is_broadcast, packet.len ...(+7) | self.0
            .lock, self.0
            .lock()
            .write_block, self.0
            .lock()
            .write_block(block_id, buf)
            .expect |
| `axnet-ng-patched/src/lib.rs#init_network` | `kernel/src/drivers/block/virtio_block.rs#new` | 同模块上下文对齐/差异观察 | "eth0".to_owned, Box::new, EthernetAddress, EthernetDevice::new, GATEWAY.parse, GATEWAY.parse().expect ...(+28) | Mutex::new, Self, VirtIOBlk::new, VirtIOBlk::new(&mut *(VIRTIO0 as *mut VirtIOHeader)).unwrap |

#### 网络协议栈
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `axnet-ng-patched/src/udp.rs#recv` | `libc/src/functional/inet_pton.c#main` | 同模块上下文对齐/差异观察 | Err, ExpectedRemote::Any, ExpectedRemote::Expecting, Ok, SocketAddrEx::Ip, ax_bail! ...(+19) | V4, V6, inet_ntop, inet_pton, strerror, t_error |
| `axnet-ng-patched/src/udp.rs#send` | `libc/src/functional/socket.c#main` | 同模块上下文对齐/差异观察 | Err, IpAddr::V4, IpEndpoint::from, Ok, PacketMeta::default, SocketAddr::new ...(+22) | TEST, TESTE, accept, bind, close, connect ...(+9) |
| `axnet-ng-patched/src/vsock/stream.rs#recv` | `libc/src/regression/inet_pton-empty-last-field.c#main` | 同模块上下文对齐/差异观察 | Err, Ok, conn.lock, conn_guard.advance_rx_read, conn_guard.rx_buffer_used, conn_guard.rx_closed ...(+9) | inet_pton, memcmp, t_error, txt |
| `axnet-ng-patched/src/vsock/connection_manager.rs#on_connection_request` | `libc/src/regression/inet_ntop-v4mapped.c#main` | 同模块上下文对齐/差异观察 | Err, Ok, Some, drop, info!, queue.lock ...(+11) | inet_ntop, inet_pton, strchr, t_error |

#### 文件系统/第三方库
仅目标项目中观察到该模块代表函数，参考项目没有同模块可对齐证据：
- 目标项目：`axfs-ng-patched/src/fs/ext4/inode.rs#create`，调用摘要：DirEntry::new_dir, DirEntry::new_file, DirNode::new, Err, FileNode::new, Inode::new ...(+14)
- 目标项目：`axfs-ng-patched/src/fs/ext4/inode.rs#metadata`，调用摘要：DeviceId::default, FileAttr::default, NodePermission::from_bits_truncate, Ok, into_vfs_type, self.fs
            .lock ...(+2)
- 目标项目：`axfs-ng-patched/src/fs/ext4/inode.rs#read_dir`，调用摘要：Ok, core::str::from_utf8, core::str::from_utf8(entry.name())
                .map_err, core::str::from_utf8(entry.name())
                .map_err(|_| VfsError::InvalidData)?
                .to_owned, entry.ino, entry.inode_type ...(+10)
- 目标项目：`axfs-ng-patched/src/fs/ext4/inode.rs#update_metadata`，调用摘要：Ok, fs.with_inode_ref, fs.with_inode_ref(self.ino, |inode| {
            if let Some(mode) = update.mode {
                inode.set_mode((inode.mode() & !0xfff) | (mode.bits() as u32));
            }
            if let Some((uid, gid)) = update.owner {
                inode.set_owner(uid as _, gid as _);
            }
            if let Some(atime) = update.atime {
                inode.set_atime(&atime);
            }
            if let Some(mtime) = update.mtime {
                inode.set_mtime(&mtime);
            }
            inode.update_ctime();
            Ok(())
        })
        .map_err, inode.mode, inode.set_atime, inode.set_mode ...(+5)

#### 第三方库
仅参考项目中观察到该模块代表函数，目标项目没有同模块可对齐证据：
- 参考项目：`dependencies/core2/src/io/buffered.rs#write`，调用摘要：Ok, memchr::memrchr, self.buffer.capacity, self.buffer.flush_buf, self.buffer.write, self.buffer.write_to_buf ...(+3)
- 参考项目：`dependencies/core2/src/io/buffered.rs#flush_buf`，调用摘要：BufGuard::new, Err, Error::new, Ok, e.kind, guard.consume ...(+7)
- 参考项目：`dependencies/core2/src/io/traits.rs#read_to_end_with_reservation`，调用摘要：Err, Ok, assert!, buf.len, e.kind, g.buf.capacity ...(+7)
- 参考项目：`dependencies/bitmap-allocator/src/lib.rs#bitalloc_contiguous`，调用摘要：BitAlloc16::default, BitAlloc4K::default, assert_eq!, ba.dealloc, ba.insert, ba.remove ...(+2)

#### 通用模块/其他
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `axio-patched/src/write/mod.rs#write_str` | `kernel/src/arch/riscv/stdout.rs#write_str` | 同板块同名 | Err, Ok, s.as_bytes, self.inner.write_all | Ok, putchar_raw, s.bytes |
| `axio-patched/src/buffered/linewriter/shim.rs#write` | `libc/src/functional/strtol.c#main` | 同模块上下文对齐/差异观察 | Ok, memchr::memrchr, self.buffer.capacity, self.buffer.flush_buf, self.buffer.write, self.buffer.write_to_buf ...(+4) | TEST, TEST2, atol, strtol, strtoll, strtoul ...(+2) |
| `axio-patched/src/utils/take.rs#read_buf` | `libc/src/functional/wcstol.c#main` | 同模块上下文对齐/差异观察 | Ok, buf.advance, buf.advance_unchecked, buf.as_mut, buf.capacity, buf.init_mut ...(+13) | TEST, TEST2, t_error, wcstol, wcstoul |
| `axio-patched/src/buffered/bufwriter/mod.rs#write_all_cold` | `libc/src/functional/string.c#main` | 同模块上下文对齐/差异观察 | Ok, buf.len, self.buf.capacity, self.flush_buf, self.get_mut, self.get_mut().write_all ...(+2) | TEST, TEST_S, memcmp, memcpy, memset, strchr ...(+11) |


#### 1. 宏观判定
- **定性结论**: 底层架构显著差异 / 独立开发 (相似度: 2.18%，共同 AST 指纹: 65；唯一 AST 指纹: 目标项目 1549，参考项目 1491；覆盖率: 目标项目 4.2%，参考项目 4.36%)

#### 2. 全模块覆盖矩阵解读
- **进程管理**
  1. 双方均存在该模块。
  2. 不存在共同 AST 指纹。
  3. 目标项目覆盖率 0.0%，参考项目覆盖率 0.0%。说明双方在进程管理的底层实现上无任何可识别的结构重合。
  4. 证据强度：未形成。

- **任务调度/异步运行时**
  1. 双方均存在该模块。
  2. 不存在共同 AST 指纹。
  3. 目标项目覆盖率 0.0%，参考项目覆盖率 0.0%。说明双方的任务调度与异步运行时实现完全异构。
  4. 证据强度：未形成。

- **内存管理**
  1. 双方均存在该模块。
  2. 存在 1 个共同 AST 指纹。
  3. 目标项目覆盖率 0.48%，参考项目覆盖率 0.38%。极低的覆盖率表明该指纹极可能源于 Rust 语言级惯用法（如 `Ok`/`Err`），而非业务逻辑复用。
  4. 证据强度：弱。

- **中断/异常处理**
  1. 双方均存在该模块。
  2. 不存在共同 AST 指纹。
  3. 目标项目覆盖率 0.0%，参考项目覆盖率 0.0%。说明双方在 Trap 处理、上下文切换等底层机制上无结构重合。
  4. 证据强度：未形成。

- **系统调用**
  1. 双方均存在该模块。
  2. 不存在共同 AST 指纹。
  3. 目标项目覆盖率 0.0%，参考项目覆盖率 0.0%。尽管存在同名 POSIX 入口，但底层 AST 调用链完全不同。
  4. 证据强度：未形成。

- **文件系统**
  1. 双方均存在该模块。
  2. 存在 3 个共同 AST 指纹。
  3. 目标项目覆盖率 0.62%，参考项目覆盖率 0.56%。覆盖率处于噪声水平，且证据多指向通用的 `seek`、`consume` 等标准操作，非独创逻辑。
  4. 证据强度：弱。

- **设备驱动**
  1. 双方均存在该模块。
  2. 不存在共同 AST 指纹。
  3. 目标项目覆盖率 0.0%，参考项目覆盖率 0.0%。双方驱动的硬件模型和交互方式完全不同。
  4. 证据强度：未形成。

- **网络协议栈**
  1. 双方均存在该模块。
  2. 不存在共同 AST 指纹。
  3. 目标项目覆盖率 0.0%，参考项目覆盖率 0.0%。目标项目拥有完整的协议栈实现，参考项目仅存少量用户态测试，无内核级可比性。
  4. 证据强度：未形成。

- **第三方库/公共依赖**
  1. 目标项目存在“文件系统/第三方库”模块，参考项目存在“第三方库”模块。
  2. 不存在共同 AST 指纹。
  3. 双方覆盖率均为 0.0%。双方依赖的第三方库集合完全不同，无任何重合。
  4. 证据强度：未形成。

#### 3. 底层 AST 关键证据指纹表
| 所属板块 | 目标项目路径 | 参考项目路径 | 证据类型 | 技术说明 |
| :--- | :--- | :--- | :--- | :--- |
| 内存管理 | `kernel/src/mm/aspace/mod.rs#map` | `kernel/src/memory/page_table.rs#map` | 同板块同名 | 双方均实现了页表映射的 `map` 函数，但目标项目通过 `MemoryArea::new` 和 `self.areas.map` 操作，参考项目直接操作 `pte.set_all`，抽象层次和对象模型完全不同。 |
| 文件系统 | `axio-patched/src/utils/take.rs#seek` | `rust-fatfs/src/file.rs#seek` | 同板块同名 | 双方均实现了 `seek` 操作，但目标项目基于 `self.inner.seek_relative` 和 `checked_add_signed`，参考项目基于 `i64::from` 和 `debug_assert!`，内部状态管理机制异构。 |
| 文件系统 | `kernel/src/file/epoll.rs#consume` | `fscommon/src/buf_stream.rs#consume` | 同板块同名 | 函数同名但语义完全不同。目标项目处理 epoll 事件消费，参考项目处理字节流消费，属于不同子系统。 |
| 文件系统 | `axfs-ng-patched/src/highlevel/file.rs#seek` | `rust-fatfs/src/fs.rs#seek` | 同板块同名 | 目标项目通过 `guard.checked_add_signed` 操作锁保护的状态，参考项目通过 `i64::try_from` 转换偏移量，实现路径无重合。 |
| 通用模块/其他 | `axio-patched/src/write/mod.rs#write_str` | `kernel/src/arch/riscv/stdout.rs#write_str` | 同板块同名 | 目标项目通过 `self.inner.write_all` 写入字节流，参考项目通过 `putchar_raw` 逐字节输出，底层输出机制完全不同。 |

#### 4. 核心代码调用流对比剖析
* **【进程管理/sys_getpid 调用流】**
* **底层技术析理**: 目标项目采用 `current() -> as_thread() -> proc_data -> proc -> pid` 的显式数据结构访问链，体现了以 `proc_data` 为核心的进程模型。参考项目采用 `get_current_task() -> unwrap() -> get_pid_num()` 的封装 getter 模式，体现了以 `Task` 对象为中心的设计。两者在任务获取、数据访问、错误处理（`ok_or` vs `unwrap`）上存在根本性架构差异。
* **代码溯源链接**:
  - 目标项目: `kernel/src/syscall/task/thread.rs#sys_getpid`
  - 参考项目: `kernel/src/syscall/process.rs#sys_getpid`

* **【进程管理/sys_exit 调用流】**
* **底层技术析理**: 目标项目的退出流通过 `do_exit` 显式处理 `FutexKey`、`SignalInfo`、`clear_child_tid`、`SHM_MANAGER` 等现代 Linux 退出语义，资源清理路径清晰。参考项目的退出流通过 `exit_current_task` 和 `handle_zombie_task` 处理 `ORIGIN_USER_PROC` 和 `global_logoff_signals`，模型更接近教学操作系统。两者在退出状态机、资源管理对象和清理顺序上完全不同。
* **代码溯源链接**:
  - 目标项目: `kernel/src/syscall/task/exit.rs#sys_exit`
  - 参考项目: `kernel/src/syscall/process.rs#sys_exit`

* **【内存管理/map 调用流】**
* **底层技术析理**: 目标项目通过 `MemoryArea::new` 创建高级抽象对象，再调用 `self.areas.map` 和 `self.populate_area` 进行映射和填充，体现了基于区域的内存管理模型。参考项目直接操作页表项 `pte.set_all`，并检查 `flags.is_empty`，体现了基于页表项的直接操作模型。两者在内存管理的抽象层次和对象封装上存在显著差异。
* **代码溯源链接**:
  - 目标项目: `kernel/src/mm/aspace/mod.rs#map`
  - 参考项目: `kernel/src/memory/page_table.rs#map`

#### 5. 单方模块与降权项
- **单方模块**:
  - **目标项目独有**: `文件系统/第三方库` (37 个函数)，主要为 `axfs-ng-patched` 中的 ext4 文件系统实现。参考项目无此模块，无法形成对比。
  - **参考项目独有**: `第三方库` (335 个函数)，主要为 `core2`、`bitmap-allocator` 等依赖库。目标项目无此模块，无法形成对比。
- **降权项说明**:
  - **POSIX 标准接口**: `sys_getpid`、`sys_execve`、`sys_exit`、`sys_waitpid` 等函数名相同是接口规范强制要求，不代表代码复用。
  - **Rust 语言惯用法**: `Ok`、`Err`、`Some`、`Arc::new`、`Default::default`、`Mutex::lock` 等调用属于 Rust 生态的通用 AST 叶节点，不能作为抄袭依据。
  - **跨板块/跨抽象域证据**: 控方将目标项目的内核实现（如 `sys_rt_sigprocmask`、`exit_robust_list`）与参考项目的用户态 libc 测试用例（如 `sigprocmask-internal.c`、`pthread_robust.c`）进行对比，属于跨抽象层硬凑，不构成同板块主证据，已全部降权为弱信号或排除。

#### 6. 保守结论
本次审计基于全量 AST 指纹比对，整体相似度仅 2.18%，处于统计噪声水平。在控辩双方聚焦的进程管理板块，共同指纹为零，表明双方在核心进程模型、资源管理、错误处理上采用了完全不同的架构设计。目标项目展现了以 `current()`/`proc_data` 为核心的现代 Rust 风格，并实现了 `pidfd`、`procfs`、`robust futex` 等高级 Linux 特性；参考项目则保持了以 `get_current_task()` 为中心的传统教学操作系统风格。全库 4.2% 的覆盖率主要由 Rust 语言惯用法和通用操作贡献，无法支撑任何实质性抄袭指控。综合判定，两个项目为独立开发，不存在违规复用。

------------------------------

