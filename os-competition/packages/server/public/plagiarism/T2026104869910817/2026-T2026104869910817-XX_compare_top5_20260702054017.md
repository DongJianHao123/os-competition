# 🛡️ OS 内核赛自动化代码级架构审计报告

**审计时间**: `2026-07-02 05:40:17`
**参赛作品名称**: `2026-T2026104869910817-XX`
**年份**: `2026`
**学校**: `XX`
**队伍名称**: `T2026104869910817`
**比赛名字**: `OS-Competition`
**比赛子赛道**: `Kernel-Implementation`
**仓库地址**: `https://gitlab.eduxiji.net/educg-group-43501-3132633/T2026104869910817-2083`
**推测开发语言**: `Rust / C (由AST探针自动识别)`
**特征函数量**: `2320`

## ⚔️ 底层代码同源性对比 (AST Homology)
> 本部分由专门的代码审查智能体，对该项目与符合年份条件的历届最高相似度项目进行控制流与底层调用对比。新版报告会先给出确定性的全模块覆盖矩阵，再给出 Agent 的分模块解释，避免只围绕少量抽样函数展开。

### 🏆 Top 5 底层对标: vs `2023-MankorOS-哈尔滨工业大学深圳`（哈尔滨工业大学（深圳） / MankorOS）

#### 对方作品信息

**对方年份**: `2023`
**对方学校**: `哈尔滨工业大学（深圳）`
**对方队伍名称**: `MankorOS`
**对方比赛名字**: `OS-Competition`
**对方比赛子赛道**: `Kernel-Implementation`
**对方仓库地址**: `https://gitlab.eduxiji.net/MankorOS/OSKernel2023-MankorOS`

> 双向覆盖率：共同 AST 指纹: 110；唯一 AST 指纹: 目标项目 1900，参考项目 1233；覆盖率: 目标项目 5.79%，参考项目 8.92%

## 全模块对比总览（确定性证据）

> 总体覆盖率：共同 AST 指纹: 110；唯一 AST 指纹: 目标项目 1900，参考项目 1233；覆盖率: 目标项目 5.79%，参考项目 8.92%

### 1. 模块覆盖矩阵
| 模块 | 目标函数数 | 参考函数数 | 共同唯一 AST 指纹 | Target 覆盖 | Base 覆盖 | 状态 |
| :--- | ---: | ---: | ---: | ---: | ---: | :--- |
| 进程管理 | 176 | 264 | 6 | 3.8% | 2.5% | 双方存在且有结构重合 |
| 任务调度/异步运行时 | 283 | 79 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |
| 内存管理 | 579 | 320 | 2 | 0.4% | 0.71% | 双方存在且有结构重合 |
| 中断/异常处理 | 89 | 28 | 1 | 1.72% | 3.85% | 双方存在且有结构重合 |
| 系统调用 | 118 | 12 | 3 | 2.94% | 25.0% | 双方存在且有结构重合 |
| 文件系统 | 429 | 475 | 19 | 6.17% | 4.95% | 双方存在且有结构重合 |
| 设备驱动 | 200 | 123 | 34 | 20.73% | 31.48% | 双方存在且有结构重合 |
| 网络协议栈 | 144 | 0 | 0 | 0.0% | 0.0% | 仅目标项目体现 |
| 文件系统/第三方库 | 60 | 0 | 0 | 0.0% | 0.0% | 仅目标项目体现 |
| 通用模块/其他 | 242 | 159 | 14 | 6.67% | 10.29% | 双方存在且有结构重合 |

### 2. 分模块证据清单

#### 进程管理
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `os/src/syscall/process.rs#sys_execve` | `src/syscall/process.rs#sys_execve` | 同板块同名 | "/musl/busybox".to_string, "busybox".to_string, "sh".to_string, AtFlags::AT_FDCWD.bits, AtFlags::empty, Err ...(+51) | Ok, Path::from_string, Some, String::from, UserReadPtr::<u8>::from, UserReadPtr::<usize>::from ...(+24) |
| `os/src/task/task.rs#do_exit` | `src/process/lproc.rs#do_exit` | 同板块同名 | Arc::downgrade, Instruction::shutdown, Some, child.is_zombie, child.parent.lock, children.clear ...(+20) | Arc::downgrade, GlobalLProcManager::get, GlobalLProcManager::get(Pid::from(1)).unwrap, Pid::from, Some, c.parent.lock ...(+19) |
| `os/src/syscall/process.rs#sys_clone` | `src/syscall/process.rs#sys_clone` | 同板块同名 | CloneFlags::from_bits, CloneFlags::from_bits(flags & !0xff).ok_or, Ok, Some, UserPtrRaw::new, UserPtrRaw::new(child_tid.0 as *mut u32)
            .ensure_write ...(+19) | CloneFlags::from_bits_truncate, Err, Into::<usize>::into, Ok, Some, VirtAddr::from ...(+18) |
| `os/src/executor/mod.rs#spawn` | `src/executor/mod.rs#spawn` | 同板块同名 | <Arc<TaskControlBlock> as Clone>::clone, <Arc<TaskControlBlock> as Clone>::clone(&(&future.task.clone())).turn_cpu_mask_id, PROCESSORS[cpu_mask_id]
                        .unwrap_with_mut_task_queue, PROCESSORS[crate::processor::schedule::select_run_queue_index()]
                        .unwrap_with_mut_task_queue, TASK_QUEUE.push, TASK_QUEUE.push_preempt ...(+6) | TASK_QUEUE.push, async_task::spawn |

#### 任务调度/异步运行时
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `os/src/syscall/io.rs#sys_ppoll` | `src/syscall/io.rs#sys_ppoll` | 同板块同名 | Err, Ok, Select2Futures::new, Some, TimedTaskFuture::new, UserPtrRaw::new ...(+37) | AnyFuture::new_with, BTreeMap::<usize, (usize, i16)>::new, Ok, UserInOutPtr::<PollFd>::from_usize, Vec::<Async<SysResult<usize>>>::new, add_func_call ...(+24) |
| `os/src/syscall/signal.rs#sys_kill` | `src/syscall/signal.rs#sys_kill` | 同板块同名 | Err, Ok, PROCESS_GROUP_MANAGER
                .get_group, PROCESS_GROUP_MANAGER
                .get_group(pgid)
                .ok_or_else, PROCESS_GROUP_MANAGER
                .get_group(pgid)
                .ok_or_else(|| SysError::ESRCH)?
                .into_iter, PROCESS_GROUP_MANAGER
                .get_group(pgid)
                .ok_or_else(|| SysError::ESRCH)?
                .into_iter()
                .filter_map ...(+21) | GlobalLProcManager::get, GlobalLProcManager::get(pid.into()).ok_or, Ok, child.pgid, child.send_signal, info! ...(+8) |
| `os/src/fs/devfs/loop_dev.rs#ioctl` | `src/fs/memfs/tty.rs#ioctl` | 同板块同名 | Err, LoopIoctlCmd::from_repr, LoopIoctlCmd::from_repr(cmd)
            .ok_or, Ok, Some, UserPtrRaw::new ...(+26) | Ok, UserReadPtr::<pid_t>::from, UserWritePtr::<pid_t>::from, argp.read, argp.write, dyn_future ...(+5) |
| `os/src/syscall/fs.rs#sys_renameat2` | `src/syscall/fs.rs#sys_renameat2` | 同板块同名 | Arc::ptr_eq, Err, Ok, RenameFlags::from_bits, RenameFlags::from_bits(flags).ok_or, UserPtrRaw::new ...(+26) | Err, Ok, UserReadPtr::<u8>::from, new_dir.attach, new_dir.clone, new_dir.kind ...(+11) |

#### 内存管理
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `os/src/drivers/block/mmc/mod.rs#card_init` | `src/drivers/blk/dw_mshc/mmc.rs#card_init` | 同板块同名 | CID::from, CLKDIV::offset, CMD::data_cmd, CMD::no_data_cmd, CMD::no_data_cmd_no_crc, CMD::no_data_cmd_no_crc(card_idx, 2).with_response_length ...(+38) | CID::from, CLKDIV::offset, CMD::data_cmd, CMD::no_data_cmd, CMD::no_data_cmd_no_crc, CMD::no_data_cmd_no_crc(card_idx, 2).with_response_length ...(+38) |
| `os/src/drivers/block/mmc/mod.rs#send_cmd` | `src/drivers/blk/dw_mshc/mmc.rs#send_cmd` | 同板块同名 | CMD::offset, CMDARG::offset, RESP::offset, RINSTS::offset, Some, arg.into ...(+27) | CMD::offset, CMDARG::offset, RESP::offset, RINSTS::offset, Some, arg.into ...(+27) |
| `os/src/devices/manager.rs#map_devices` | `src/drivers/manager.rs#map_devices` | 同板块同名 | "virtio-blk".to_string, Arc::new, IrqCtrl::from_dt, MmioManager::scan_mmio_root, PciManager::scan_pcie_root, Some ...(+24) | PTEFlags::rw, VirtAddr::from, boot::boot_pagetable_paddr, boot::boot_pagetable_paddr().into, core::mem::forget, dev.mmio_base ...(+11) |
| `os/src/mm/vm/uvm.rs#reset_heap_break` | `src/process/user_space/user_area.rs#reset_heap_break` | 同板块同名 | UserVmArea::new, heap.range_vpn, heap.split_off, heap.unmap, new_brk.ceil, new_brk.max ...(+10) | Ok, self.map.extend_back, self.map.extend_back(start, new_brk).map_err, self.map.get, self.map.get(Self::HEAP_BEG).expect, self.map.reduce_back ...(+2) |

#### 中断/异常处理
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `hal/src/component/irq/loongarch64/eiointc.rs#claim_irq` | `src/drivers/plic.rs#claim_irq` | 同板块同名 | Some, flags0.leading_zeros, flags1.leading_zeros, flags2.leading_zeros, flags3.leading_zeros, iocsr_read_d | (*plic).claim, PLICCtxWrapper::new, irq.get, irq.map, kernel_phys_dev_to_virt |
| `hal/src/component/irq/loongarch64/mod.rs#enable_irq` | `src/drivers/plic.rs#enable_irq` | 同板块同名 | Eiointc::enable_irq, log::warn!, self.platic.read_w, self.platic.write_b, self.platic.write_w | (*plic).enable, (*plic).set_priority, (*plic).set_threshold, PLICCtxWrapper::new, PLICSrcWrapper::new, kernel_phys_dev_to_virt |
| `hal/src/component/irq/loongarch64/eiointc.rs#enable_irq` | `src/trap/trap.rs#enable_irq` | 同板块同名 | iocsr_read_d, iocsr_write_d | sstatus::set_sie |
| `hal/src/component/irq/riscv64.rs#complete_irq` | `src/drivers/plic.rs#complete_irq` | 同板块同名 | (*plic).complete, PLICCtxWrapper::new, PLICSrcWrapper::new | (*plic).complete, PLICCtxWrapper::new, PLICSrcWrapper::new, kernel_phys_dev_to_virt |

#### 系统调用
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `os/src/syscall/net.rs#set` | `src/syscall/io.rs#set` | 同板块同名 | <[u8; 4]>::try_from, <[u8; 4]>::try_from(&opt[0..4]).unwrap, Duration::from_secs, Err, Ok, SOCKET_SET.with_socket_mut::<smoltcp::socket::tcp::Socket,_,_> ...(+15) | 内部闭环逻辑 |
| `os/src/syscall/io.rs#is_set` | `src/syscall/io.rs#is_set` | 同板块同名同结构 | 内部闭环逻辑 | 内部闭环逻辑 |
| `os/src/syscall/misc.rs#from_str` | `src/syscall/misc.rs#from_str` | 同板块同名同结构 | data[..info.len()].copy_from_slice, info.as_bytes, info.len | data[..info.len()].copy_from_slice, info.as_bytes, info.len |
| `os/src/syscall/io.rs#clear` | `src/syscall/io.rs#clear` | 同板块同名 | self.fds_bits.fill | self.fds_bits.len |

#### 文件系统
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `os/src/fs/devfs/tty.rs#ioctl` | `src/fs/new_vfs/top.rs#ioctl` | 同板块同名 | Err, Ok, TtyIoctlCmd::from_repr, log::debug!, log::error!, self.meta.lock ...(+1) | Ok, dyn_future |
| `os/src/fs/vfs/dentry.rs#find` | `src/fs/nfat32/fs.rs#find` | 同板块同名 | DCACHE.lock, Ok, Some, Vec::new, cache.get, compoents.join ...(+14) | Some, self.next |
| `os/src/fs/fat32/inode.rs#create` | `src/fs/memfs/tmpdir.rs#create` | 同板块同名 | Arc::new, InodeInner::new, Ok, Some, String::from, UPSafeCell::new ...(+11) | Err, Ok, Self::new, TmpFile::new, VfsFileRef::new, children.contains_key ...(+8) |
| `os/src/fs/fat32/inode.rs#lookup` | `src/fs/new_vfs/path_cache.rs#lookup` | 同板块同名 | Arc::new, InodeInner::new, Some, String::from, UPSafeCell::new, dir.inner
        .iter ...(+16) | Err, Ok, dyn_future, file.clone, here!, name.to_string ...(+8) |

#### 设备驱动
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `os/src/drivers/serial/uart.rs#init_u32` | `src/drivers/serial/uart8250.rs#init_u32` | 同板块同名 | log::info!, reg.byte_add, reg.byte_add(0 << self.reg_shift)
                .write_volatile, reg.byte_add(1 << self.reg_shift)
                .write_volatile, reg.byte_add(FCR << self.reg_shift).write_volatile, reg.byte_add(IER << self.reg_shift).write_volatile ...(+2) | info!, reg.byte_add, reg.byte_add(0 << self.reg_shift).write_volatile, reg.byte_add(1 << self.reg_shift).write_volatile, reg.byte_add(FCR << self.reg_shift).write_volatile, reg.byte_add(IER << self.reg_shift).write_volatile ...(+2) |
| `os/src/drivers/serial/uart.rs#init_u8` | `src/drivers/serial/uart8250.rs#init_u8` | 同板块同名 | reg.byte_add, reg.byte_add(0 << self.reg_shift)
                .write_volatile, reg.byte_add(1 << self.reg_shift)
                .write_volatile, reg.byte_add(IER << self.reg_shift).write_volatile, reg.byte_add(LCR << self.reg_shift).write_volatile, reg.byte_add(MCR << self.reg_shift).write_volatile | reg.byte_add, reg.byte_add(0 << self.reg_shift).write_volatile, reg.byte_add(1 << self.reg_shift).write_volatile, reg.byte_add(FCR << self.reg_shift).write_volatile, reg.byte_add(IER << self.reg_shift).write_volatile, reg.byte_add(LCR << self.reg_shift).write_volatile ...(+1) |
| `os/src/drivers/serial/uart.rs#send_u8` | `src/drivers/serial/uart8250.rs#send_u8` | 同板块同名同结构 | ptr.byte_add, ptr.byte_add(THR << self.reg_shift).write_volatile, wait_for! | ptr.byte_add, ptr.byte_add(THR << self.reg_shift).write_volatile, wait_for! |
| `os/src/drivers/serial/uart.rs#send_u32` | `src/drivers/serial/uart8250.rs#send_u32` | 同板块同名同结构 | b' '.into, c.into, ptr.byte_add, ptr.byte_add(THR << self.reg_shift)
                        .write_volatile, ptr.byte_add(THR << self.reg_shift).write_volatile, wait_for! | b' '.into, c.into, ptr.byte_add, ptr.byte_add(THR << self.reg_shift).write_volatile, wait_for! |

#### 网络协议栈
仅目标项目中观察到该模块代表函数，参考项目没有同模块可对齐证据：
- 目标项目：`os/src/net/tcp.rs#recv`，调用摘要：Err, Ok, SOCKET_SET.with_socket_mut::<tcp::Socket,_,_>, get_current_time_duration, get_waker, info! ...(+15)
- 目标项目：`os/src/net/udp.rs#send_to`，调用摘要：Err, Ok, SOCKET_SET.with_socket_mut::<smoltcp::socket::udp::Socket,_,_>, data.len, get_waker, log::info! ...(+11)
- 目标项目：`user/src/bin/tcp.rs#server`，调用摘要：TEST_ADDR.to_be, TEST_PORT.to_be, accept, bind, buf.as_mut_slice, close ...(+11)
- 目标项目：`os/src/net/tcp.rs#send`，调用摘要：Duration::from_millis, Err, Ok, SOCKET_SET.poll_interfaces, SOCKET_SET.with_socket_mut::<tcp::Socket,_,_>, get_waker ...(+15)

#### 文件系统/第三方库
仅目标项目中观察到该模块代表函数，参考项目没有同模块可对齐证据：
- 目标项目：`os/src/fs/ext4/inode.rs#getxattr`，调用摘要：XstatMask::from_bits_truncate, cache.end, cmp::max, file.file_close, file.file_close().expect, file.get_type ...(+10)
- 目标项目：`os/src/fs/ext4/inode.rs#cache_read_at`，调用摘要：Arc::get_mut, Arc::get_mut(&mut page).unwrap, Arc::get_mut(&mut page).unwrap()
                    .read_from, Ok, Page::new, buf.len ...(+16)
- 目标项目：`os/src/fs/ext4/inode.rs#cache_write_at`，调用摘要：Arc::get_mut, Arc::get_mut(&mut page).unwrap, Arc::get_mut(&mut page).unwrap().read_from, Ok, Page::new, buf.len ...(+14)
- 目标项目：`os/src/fs/ext4/dentry.rs#load_child_dentry`，调用摘要：DCACHE.lock, DCACHE.lock().insert, Ok, Some, Vec::new, child.clone ...(+19)

#### 通用模块/其他
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `hal/src/util/mutex.rs#lock` | `src/sync/mutex.rs#lock` | 同板块同名 | SieGuard::new, core::hint::spin_loop, panic!, self.mutex.compare_exchange, self.mutex.compare_exchange(false, true, Ordering::Acquire, Ordering::Relaxed).is_ok | S::before_lock, self.ensure_support, self.obtain_lock |
| `os/src/sync/lazy.rs#deref` | `src/sync/sleep.rs#deref` | 同板块同名 | panic!, self.once_flag.load, self.payload.get | self.mutex.data.get |
| `utils/range-map/src/lib.rs#get_mut` | `src/lazy_init.rs#get_mut` | 同板块同名 | Some, self.0.range_mut, self.0.range_mut(..=key).next_back | self.check_init, self.get_mut_unchecked |
| `os/src/sync/lazy.rs#deref_mut` | `src/sync/sleep.rs#deref_mut` | 同板块同名 | panic!, self.once_flag.load, self.payload.get_mut | self.mutex.data.get |


#### 1. 宏观判定
- **定性结论**: **独立开发 (相似度: 3.64%，共同 AST 指纹: 110；唯一 AST 指纹: 目标项目 1900，参考项目 1233；覆盖率: 目标项目 5.79%，参考项目 8.92%)**

#### 2. 全模块覆盖矩阵解读
- **进程管理**: 双方均存在。共同 AST 指纹 6 个，覆盖率极低（3.8% / 2.5%）。核心函数如 `sys_execve`、`sys_clone`、`do_exit` 的调用图谱差异显著，体现了不同的进程抽象模型（`TaskControlBlock` vs `LightProcess`）。证据强度：**弱**。
- **任务调度/异步运行时**: 双方均存在。共同 AST 指纹为 **0**，覆盖率 **0%**。目标项目实现了多核任务迁移与抢占（`push_preempt`, `PROCESSORS`），参考项目为单队列轮询，架构完全不同。证据强度：**未形成**。
- **内存管理**: 双方均存在。共同 AST 指纹 2 个，覆盖率极低（0.4% / 0.71%）。用户指针 API 设计完全不同（`UserPtrRaw::ensure_write` vs `UserReadPtr::read_cstr`），内存映射模型异构。证据强度：**未形成**。
- **中断/异常处理**: 双方均存在。共同 AST 指纹 1 个，覆盖率低（1.72% / 3.85%）。目标项目支持 LoongArch 和 RISC-V 双架构，参考项目仅 RISC-V，中断控制器驱动实现不同。证据强度：**未形成**。
- **系统调用**: 双方均存在。共同 AST 指纹 3 个，覆盖率低（2.94% / 25.0%）。重合集中在 `from_str`、`is_set` 等极简辅助函数或 POSIX 同名接口，核心业务逻辑无重合。证据强度：**弱**。
- **文件系统**: 双方均存在。共同 AST 指纹 19 个，覆盖率低（6.17% / 4.95%）。目标项目包含 ext4、CNXFS/procfs，参考项目包含 nfat32、memfs，文件系统类型和实现路径不同。证据强度：**弱**。
- **设备驱动**: 双方均存在。共同 AST 指纹 34 个，覆盖率较高（20.73% / 31.48%）。重合集中在 MMC/SD 卡驱动和 UART 驱动，属于对同一硬件规范（SD 协议、8250 UART）的标准寄存器操作，是领域约束下的必然趋同。证据强度：**中**。
- **网络协议栈**: 仅目标项目存在。参考项目无此模块。证据强度：**未形成**。
- **第三方库/公共依赖**: 仅目标项目存在（ext4 等）。双方均使用了 `async_task`、`smoltcp` 等公共 crate，属于正常的第三方依赖，不构成同源证据。证据强度：**未形成**。

#### 3. 底层 AST 关键证据指纹表
| 所属板块 | 目标项目路径 | 参考项目路径 | 证据类型 | 技术说明 |
| :--- | :--- | :--- | :--- | :--- |
| 设备驱动 | `os/src/drivers/block/mmc/mod.rs#card_init` | `src/drivers/blk/dw_mshc/mmc.rs#card_init` | 同板块同名 | MMC/SD 卡初始化序列，如 `CMD::no_data_cmd_no_crc` 等，由 SD 协议规范决定，非代码复用。 |
| 设备驱动 | `os/src/drivers/block/mmc/mod.rs#send_cmd` | `src/drivers/blk/dw_mshc/mmc.rs#send_cmd` | 同板块同名 | MMC 控制器寄存器操作（`CMD::offset`, `RESP::offset`），由硬件手册定义。 |
| 设备驱动 | `os/src/drivers/serial/uart.rs#send_u8` | `src/drivers/serial/uart8250.rs#send_u8` | 同板块同名同结构 | 8250 UART 发送字节的标准流程：检查状态，写入 `THR` 寄存器。 |
| 设备驱动 | `os/src/drivers/serial/uart.rs#init_u32` | `src/drivers/serial/uart8250.rs#init_u32` | 同板块同名 | 8250 UART 初始化序列，配置波特率、FCR、IER 等标准寄存器。 |
| 进程管理 | `os/src/executor/mod.rs#new` | `src/executor/task_queue.rs#new` | 同板块同名 | `SpinNoIrqLock::new, VecDeque::new` 是 Rust 异步运行时的通用队列初始化模式。 |
| 进程管理 | `os/src/executor/mod.rs#fetch` | `src/executor/task_queue.rs#fetch` | 同板块同名 | `self.queue.lock().pop_front` 是 `VecDeque` 作为 FIFO 队列的标准出队操作。 |
| 进程管理 | `os/src/task/task.rs#parent` | `src/process/lproc.rs#parent` | 同板块同名 | `self.parent.lock().clone` 是获取 `Mutex` 内 `Arc` 指针的标准 Rust 惯用法。 |
| 进程管理 | `os/src/executor/mod.rs#spawn` | `src/executor/mod.rs#spawn` | 同板块同名 | `async_task::spawn` 是第三方 crate 的公共 API，`TASK_QUEUE.push` 是通用语义。 |
| 系统调用 | `os/src/syscall/misc.rs#from_str` | `src/syscall/misc.rs#from_str` | 同板块同名同结构 | `data[..info.len()].copy_from_slice` 是字符串复制的标准 Rust 实现。 |
| 系统调用 | `os/src/syscall/io.rs#is_set` | `src/syscall/io.rs#is_set` | 同板块同名同结构 | 内部闭环逻辑，极简函数，无实质 AST 重合。 |

#### 4. 核心代码调用流对比剖析
*   **【进程管理 / sys_execve】**
    *   **底层技术析理**: 目标项目硬编码了 `"/musl/busybox"` 路径，使用 `FileReader::new` 加载程序，体现了静态链接的用户态程序加载方式。参考项目则通过 `Path::from_string` 和 `UserReadPtr::read_cstr` 从用户空间动态读取路径，遵循标准的 Unix 路径解析流程。两者在程序加载的抽象层次和实现路径上完全不同。
    *   **代码溯源链接**:
        - 目标项目: `os/src/syscall/process.rs#sys_execve`
        - 参考项目: `src/syscall/process.rs#sys_execve`

*   **【进程管理 / sys_clone】**
    *   **底层技术析理**: 目标项目使用 `UserPtrRaw::new(child_tid.0 as *mut u32).ensure_write` 来写回子线程 ID，其用户态指针 API 围绕 `ensure_read/write` 构建。参考项目则使用 `VirtAddr::from` 和 `checked_write_u32`，其内存访问模型基于虚拟地址和手动检查。两者在用户态内存访问的安全抽象和具体 API 上存在根本性差异。
    *   **代码溯源链接**:
        - 目标项目: `os/src/syscall/process.rs#sys_clone`
        - 参考项目: `src/syscall/process.rs#sys_clone`

*   **【进程管理 / do_exit】**
    *   **底层技术析理**: 目标项目在退出时调用 `children.clear` 直接清空子进程列表，并包含 `Instruction::shutdown` 等关机逻辑。参考项目则通过 `children.iter().for_each` 遍历并为每个子进程重新设置父进程（通常为 init 进程），遵循 POSIX 的进程收养规则。两者在进程退出的清理策略和子进程处理逻辑上完全不同。
    *   **代码溯源链接**:
        - 目标项目: `os/src/task/task.rs#do_exit`
        - 参考项目: `src/process/lproc.rs#do_exit`

*   **【任务调度/异步运行时 / spawn】**
    *   **底层技术析理**: 目标项目的 `spawn` 包含复杂的 CPU 亲和性逻辑，通过 `turn_cpu_mask_id` 和 `PROCESSORS[cpu_mask_id]` 将任务分配到指定核心的队列，并支持 `push_preempt` 抢占。参考项目的 `spawn` 仅包含 `TASK_QUEUE.push` 和 `async_task::spawn`，是一个单队列、无抢占的简单调度模型。两者在调度器的复杂度和核心架构上存在巨大差异。
    *   **代码溯源链接**:
        - 目标项目: `os/src/executor/mod.rs#spawn`
        - 参考项目: `src/executor/mod.rs#spawn`

*   **【设备驱动 / card_init】**
    *   **底层技术析理**: 两者均包含 `CMD::no_data_cmd_no_crc(card_idx, 2).with_response_length` 等完全相同的调用序列。这并非代码抄袭，而是因为 MMC/SD 卡初始化流程（CMD0 -> CMD8 -> ACMD41 -> CMD2 等）由行业标准规范严格定义，任何驱动实现都必须遵循此序列，导致 AST 指纹必然重合。
    *   **代码溯源链接**:
        - 目标项目: `os/src/drivers/block/mmc/mod.rs#card_init`
        - 参考项目: `src/drivers/blk/dw_mshc/mmc.rs#card_init`

#### 5. 单方模块与降权项
- **单方模块**: **网络协议栈**（目标项目独有）、**文件系统/第三方库**（目标项目独有，如 ext4）。这些模块的存在是两项目独立开发的强有力证据。
- **降权项**: 控方指控的 `sys_getpid`、`sys_gettid`、`sys_exit` 等 POSIX 系统调用，虽同名但底层 AST 完全不同，属于接口同名，已降权为弱信号。`add_child`、`remove_child` 等函数因内部数据结构操作不同（`insert` vs `push`），也已降权。

#### 6. 保守结论
综合评估，目标项目与参考项目的整体 AST 相似度仅为 3.64%，处于同领域独立实现的正常背景噪声水平。控方提出的“铁证”均为 Rust 操作系统开发中的通用惯用法（如 `SpinNoIrqLock<VecDeque>`）、第三方库 API 调用（`async_task::spawn`）或由硬件规范决定的寄存器操作序列（MMC/UART 驱动）。双方在进程抽象（`TaskControlBlock` vs `LightProcess`）、用户指针 API、内存模型、调度器架构及文件系统选型等核心设计上存在根本性分歧。特别是目标项目独有网络协议栈和多架构中断支持，进一步佐证了其独立开发的本质。因此，现有证据无法支持“代码抄袭与复用”的指控，应认定为独立开发。

------------------------------

