# 🛡️ OS 内核赛自动化代码级架构审计报告

**审计时间**: `2026-07-02 04:49:47`
**参赛作品名称**: `2026-T202610459999616-XX`
**年份**: `2026`
**学校**: `XX`
**队伍名称**: `T202610459999616`
**比赛名字**: `OS-Competition`
**比赛子赛道**: `Kernel-Implementation`
**仓库地址**: `https://gitlab.eduxiji.net/educg-group-43501-3132633/T202610459999616-2917`
**推测开发语言**: `Rust / C (由AST探针自动识别)`
**特征函数量**: `7613`

## ⚔️ 底层代码同源性对比 (AST Homology)
> 本部分由专门的代码审查智能体，对该项目与符合年份条件的历届最高相似度项目进行控制流与底层调用对比。新版报告会先给出确定性的全模块覆盖矩阵，再给出 Agent 的分模块解释，避免只围绕少量抽样函数展开。

### 🏆 Top 3 底层对标: vs `2025-StarryX-杭州电子科技大学`（杭州电子科技大学 / StarryX）

#### 对方作品信息

**对方年份**: `2025`
**对方学校**: `杭州电子科技大学`
**对方队伍名称**: `StarryX`
**对方比赛名字**: `OS-Competition`
**对方比赛子赛道**: `Kernel-Implementation`
**对方仓库地址**: `https://gitlab.eduxiji.net/educg-group-36002-2710490/T202510336995486-3037`

> 双向覆盖率：共同 AST 指纹: 1124；唯一 AST 指纹: 目标项目 6123，参考项目 2897；覆盖率: 目标项目 18.36%，参考项目 38.8%

## 全模块对比总览（确定性证据）

> 总体覆盖率：共同 AST 指纹: 1124；唯一 AST 指纹: 目标项目 6123，参考项目 2897；覆盖率: 目标项目 18.36%，参考项目 38.8%

### 1. 模块覆盖矩阵
| 模块 | 目标函数数 | 参考函数数 | 共同唯一 AST 指纹 | Target 覆盖 | Base 覆盖 | 状态 |
| :--- | ---: | ---: | ---: | ---: | ---: | :--- |
| 进程管理 | 321 | 172 | 27 | 9.61% | 16.98% | 双方存在且有结构重合 |
| 任务调度/异步运行时 | 497 | 480 | 107 | 24.43% | 30.48% | 双方存在且有结构重合 |
| 内存管理 | 454 | 430 | 97 | 22.99% | 24.94% | 双方存在且有结构重合 |
| 中断/异常处理 | 14 | 70 | 2 | 18.18% | 3.23% | 双方存在且有结构重合 |
| 系统调用 | 118 | 89 | 55 | 53.4% | 68.75% | 双方存在且有结构重合 |
| 文件系统 | 608 | 801 | 186 | 36.05% | 29.95% | 双方存在且有结构重合 |
| 设备驱动 | 111 | 139 | 86 | 86.87% | 68.25% | 双方存在且有结构重合 |
| 网络协议栈 | 76 | 77 | 51 | 78.46% | 83.61% | 双方存在且有结构重合 |
| 文件系统/第三方库 | 863 | 164 | 12 | 1.55% | 7.64% | 双方存在且有结构重合 |
| 网络协议栈/第三方库 | 2635 | 166 | 58 | 2.87% | 40.28% | 双方存在且有结构重合 |
| 第三方库 | 1354 | 349 | 105 | 9.27% | 35.84% | 双方存在且有结构重合 |
| 通用模块/其他 | 562 | 665 | 280 | 67.47% | 52.83% | 双方存在且有结构重合 |

### 2. 分模块证据清单

#### 进程管理
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `pulse_syscalls/src/impls/misc.rs#sys_rt_sigtimedwait` | `xapi/src/task/signal.rs#sys_rt_sigtimedwait` | 同板块同名 | (axhal::time::monotonic_time_nanos() as u64).saturating_add, Duration::from_nanos, LinuxError::EAGAIN.code, LinuxError::EFAULT.code, LinuxError::EINTR.code, LinuxError::EINVAL.code ...(+21) | Err, Ok, XProcess::from_thread, XProcess::from_thread(thread).uspace, XThread::from_thread, check_sigset_size ...(+6) |
| `pulse_syscalls/src/impls/task/wait.rs#sys_wait4` | `xapi/src/task/wait.rs#sys_wait4` | 同板块同名 | LinuxError::ECHILD.code, LinuxError::ESRCH.code, axhal::time::monotonic_time_nanos, axlog::debug!, axlog::warn!, child_proc.exit_code ...(+17) | Err, Ok, WaitOptions::from_bits, WaitPid::Pgid, WaitPid::Pid, child.data::<XProcess> ...(+26) |
| `arceos/modules/axfs/src/fs/procfs.rs#write_at` | `xcore/src/fs/vfs/proc/pid.rs#write_at` | 同板块同名 | Err, Ok, PID_MAX.store, buf.len, core::str::from_utf8, data.len ...(+16) | self.0.upgrade, self.0.upgrade().unwrap, self.0.upgrade().unwrap().write |
| `arceos/modules/axtask/src/run_queue.rs#exit_current` | `arceos/modules/axtask/src/run_queue.rs#exit_current` | 同板块同名 | EXITED_TASKS.current_ref_mut_raw, EXITED_TASKS.current_ref_mut_raw().clear, EXITED_TASKS.current_ref_mut_raw().push_back, GC_TASK.with_current, WAIT_FOR_EXIT.current_ref_mut_raw, WAIT_FOR_EXIT.current_ref_mut_raw().notify_one ...(+15) | EXITED_TASKS.current_ref_mut_raw, EXITED_TASKS.current_ref_mut_raw().clear, EXITED_TASKS.current_ref_mut_raw().push_back, WAIT_FOR_EXIT.current_ref_mut_raw, WAIT_FOR_EXIT.current_ref_mut_raw().notify_one, assert! ...(+8) |

#### 任务调度/异步运行时
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `arceos/ulib/axlibc/c/printf.c#format_string_loop` | `arceos/ulib/axlibc/c/printf.c#format_string_loop` | 同板块同名同结构 | ABS_FOR_PRINTING, ADVANCE_IN_FORMAT_STRING, atou_, is_digit_, out_rev_, parse_flags ...(+5) | ABS_FOR_PRINTING, ADVANCE_IN_FORMAT_STRING, atou_, is_digit_, out_rev_, parse_flags ...(+5) |
| `pulse_syscalls/src/impls/task/clone.rs#sys_clone` | `xapi/src/task/clone.rs#sys_clone` | 同板块同名 | (child_stack != 0).then_some, CLONE_SIGHAND_WARNED.swap, CloneFlags::CSIGNAL.bits, CloneFlags::from_bits_truncate, LinuxError::EINVAL.code, LinuxError::ESRCH.code ...(+18) | do_clone |
| `pulse_syscalls/src/impls/ipc/sem.rs#sys_semctl` | `xapi/src/ipc/sem.rs#sys_semctl` | 同板块同名 | LinuxError::EFAULT.code, LinuxError::EIDRM.code, LinuxError::EINVAL.code, SEM_MANAGER.lock, axhal::time::wall_time, axhal::time::wall_time().as_secs ...(+19) | Err, with_ipc_manager! |
| `arceos/modules/axlog/src/lib.rs#log` | `arceos/modules/axlog/src/lib.rs#log` | 同板块同名同结构 | cfg_if::cfg_if!, record.level, record.line, record.line().unwrap_or, record.metadata, record.target ...(+1) | cfg_if::cfg_if!, record.level, record.line, record.line().unwrap_or, record.metadata, record.target ...(+1) |

#### 内存管理
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `pulse_syscalls/src/impls/mm.rs#sys_mmap` | `xapi/src/mm/mmap.rs#sys_mmap` | 同板块同名 | CachedFile::get_or_create, Err, LinuxError::EACCES.code, LinuxError::EBADF.code, LinuxError::EEXIST.code, LinuxError::EINVAL.code ...(+39) | (addr + length).align_up, Err, FD_TABLE.is_assigned, File::from_fd, File::from_fd(fd, FileFlags::READ, FileFlags::empty())
            .map_err, FileFlags::empty ...(+39) |
| `arceos/modules/axmm/src/backend/alloc.rs#handle_page_fault_alloc` | `arceos/modules/axmm/src/backend/alloc.rs#handle_page_fault_alloc` | 同板块同名 | alloc_contiguous_frames, alloc_frame, curr_flags.is_empty, curr_frame.as_usize, dealloc_frame, debug! ...(+26) | alloc_frame, pt.map, pt.map(vaddr, frame, PageSize::Size4K, orig_flags)
                .map, pt.map(vaddr, frame, PageSize::Size4K, orig_flags)
                .map(|tlb| tlb.flush())
                .is_ok, tlb.flush |
| `arceos/modules/axfs/src/fs/procfs.rs#read_dir` | `arceos/modules/axfs-ng/src/fs/fat/dir.rs#read_dir` | 同板块同名 | ".".to_owned, "..".to_owned, "cgroup".to_owned, "children".to_owned, "cmdline".to_owned, "comm".to_owned ...(+43) | Ok, dir.iter, dir.iter().skip, dir_node.insert_cache, dir_node.lookup_cache, entry.file_name ...(+13) |
| `arceos/modules/axfs/src/fs/procfs.rs#lookup` | `arceos/modules/axfs-ng/src/fs/fat/dir.rs#lookup` | 同板块同名 | Err, PROCESS_PROVIDER.get, PROCESS_PROVIDER.get().ok_or, Some, decode_pid_inode, entries.get ...(+11) | dir.iter, dir.iter()
            .find_map, dir.iter()
            .find_map(|entry| entry.ok().filter(|it| it.eq_name(name)))
            .map, dir.iter()
            .find_map(|entry| entry.ok().filter(|it| it.eq_name(name)))
            .map(|entry| self.create_entry(entry, name.to_ascii_lowercase(), fs.alloc_inode()))
            .ok_or, entry.ok, entry.ok().filter ...(+6) |

#### 中断/异常处理
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `arceos/ulib/axlibc/c/pow.c#exp_inline` | `arceos/ulib/axlibc/c/pow.c#exp_inline` | 同板块同名同结构 | __math_oflow, __math_uflow, asdouble, asuint64, converttoint, eval_as_double ...(+4) | __math_oflow, __math_uflow, asdouble, asuint64, converttoint, eval_as_double ...(+4) |
| `arceos/modules/axsync/src/mutex.rs#may_interrupt` | `arceos/modules/axsync/src/mutex.rs#may_interrupt` | 同板块同名同结构 | rand::random::<u32>, thread::yield_now | rand::random::<u32>, thread::yield_now |
| `pulse_core/src/fd_table.rs#wait_ready` | `xmodules/xsignal/src/types.rs#default_action` | 同模块上下文对齐/差异观察 | Err, Ok, Self::current_has_pending_signal, axhal::time::monotonic_time, self.ready_for, self.writable ...(+2) | 内部闭环逻辑 |
| `pulse_core/src/net/mod.rs#write` | `arceos/modules/axhal/src/arch/aarch64/trap.rs#handle_sync_exception` | 同模块上下文对齐/差异观察 | Err, Ok, buf.is_empty, buf.len, drop, ring.available_write ...(+7) | ESR_EL1.extract, crate::trap::handle_syscall, crate::trap::post_trap_callback, debug!, esr.read, esr.read_as_enum ...(+6) |

#### 系统调用
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `pulse_syscalls/src/impls/net/socket.rs#sys_socket` | `arceos/api/arceos_posix_api/src/imp/net.rs#sys_socket` | 同板块同名 | FdFlags::empty, LinuxError::EAFNOSUPPORT.code, LinuxError::EINVAL.code, LinuxError::EPROTONOSUPPORT.code, LinuxError::ESOCKTNOSUPPORT.code, Socket::new ...(+14) | debug!, syscall_body! |
| `pulse_syscalls/src/impls/net/name.rs#sys_getpeername` | `arceos/api/arceos_posix_api/src/imp/net.rs#sys_getpeername` | 同板块同名 | 16u16.to_ne_bytes, LinuxError::EFAULT.code, NetSocketAddr::from, Some, core::cmp::min, crate::impls::utils::write_user_bytes ...(+14) | debug!, syscall_body! |
| `arceos/api/arceos_posix_api/src/imp/net.rs#sys_getaddrinfo` | `arceos/api/arceos_posix_api/src/imp/net.rs#sys_getaddrinfo` | 同板块同名同结构 | char_ptr_to_str, debug!, syscall_body! | char_ptr_to_str, debug!, syscall_body! |
| `pulse_syscalls/src/impls/net/socket.rs#sys_shutdown` | `arceos/api/arceos_posix_api/src/imp/net.rs#sys_shutdown` | 同板块同名 | Err, LinuxError::EINVAL.code, LinuxError::from, Ok, debug!, e.canonicalize ...(+9) | debug!, syscall_body! |

#### 文件系统
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `arceos/modules/axfs/src/fs/tmpfs.rs#rename` | `arceos/modules/axfs-ng/src/fs/fat/dir.rs#rename` | 同板块同名 | "..".into, Err, InodeRef::new, Ok, dir_content.entries.lock, dst_dir.downcast::<Self> ...(+29) | Err, dir.rename, dir.rename(src_name, dst_dir.inner.borrow(&fs), dst_name)
            .map_err, dst_dir.downcast, dst_dir.downcast().map_err, dst_dir.inner.borrow ...(+4) |
| `arceos/ulib/axlibc/c/glob.c#do_glob` | `arceos/ulib/axlibc/c/glob.c#do_glob` | 同板块同名同结构 | S_ISDIR, append, closedir, do_glob, errfunc, fnmatch ...(+7) | S_ISDIR, append, closedir, do_glob, errfunc, fnmatch ...(+7) |
| `arceos/ulib/axlibc/src/setjmp.rs#longjmp` | `arceos/ulib/axlibc/src/setjmp.rs#longjmp` | 同板块同名 | core::arch::naked_asm! | core::arch::naked_asm! |
| `arceos/ulib/axlibc/src/setjmp.rs#setjmp` | `arceos/ulib/axlibc/src/setjmp.rs#setjmp` | 同板块同名 | core::arch::naked_asm! | core::arch::naked_asm! |

#### 设备驱动
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `arceos/tools/bwbench_client/src/main.rs#transmit_benchmark` | `arceos/tools/bwbench_client/src/main.rs#transmit_benchmark` | 同板块同名同结构 | Local::now, NetDevice::new, NetDevice::new(interface).unwrap, current_time.signed_duration_since, current_time.signed_duration_since(past_time).num_seconds, dev.send ...(+2) | Local::now, NetDevice::new, NetDevice::new(interface).unwrap, current_time.signed_duration_since, current_time.signed_duration_since(past_time).num_seconds, dev.send ...(+2) |
| `arceos/tools/bwbench_client/src/device.rs#bind_interface` | `arceos/tools/bwbench_client/src/device.rs#bind_interface` | 同板块同名同结构 | ETH_P_ALL.to_be, Err, Ok, libc::bind, self.ifreq.ioctl, std::io::Error::last_os_error ...(+2) | ETH_P_ALL.to_be, Err, Ok, libc::bind, self.ifreq.ioctl, std::io::Error::last_os_error ...(+2) |
| `arceos/modules/axdriver/src/lib.rs#init_drivers` | `arceos/modules/axdriver/src/lib.rs#init_drivers` | 同板块同名同结构 | AllDevices::default, all_devs.block.iter, all_devs.block.iter().enumerate, all_devs.display.iter, all_devs.display.iter().enumerate, all_devs.net.iter ...(+5) | AllDevices::default, all_devs.block.iter, all_devs.block.iter().enumerate, all_devs.display.iter, all_devs.display.iter().enumerate, all_devs.net.iter ...(+5) |
| `arceos/modules/axdriver/src/virtio.rs#probe_pci` | `arceos/modules/axdriver/src/virtio.rs#probe_pci` | 同板块同名同结构 | D::try_new, Some, axdriver_virtio::probe_pci_device::<VirtIoHalImpl>, warn! | D::try_new, Some, axdriver_virtio::probe_pci_device::<VirtIoHalImpl>, warn! |

#### 网络协议栈
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `arceos/ulib/axlibc/c/network.c#inet_pton` | `arceos/ulib/axlibc/c/network.c#inet_pton` | 同板块同名同结构 | hexval, inet_pton, isdigit, memmove | hexval, inet_pton, isdigit, memmove |
| `arceos/api/arceos_posix_api/src/imp/net.rs#shutdown` | `arceos/api/arceos_posix_api/src/imp/net.rs#shutdown` | 同板块同名同结构 | Ok, tcpsocket.lock, tcpsocket.peer_addr, tcpsocket.shutdown, udpsocket.lock, udpsocket.peer_addr ...(+1) | Ok, tcpsocket.lock, tcpsocket.peer_addr, tcpsocket.shutdown, udpsocket.lock, udpsocket.peer_addr ...(+1) |
| `arceos/ulib/axlibc/c/socket.c#accept4` | `arceos/ulib/axlibc/c/socket.c#accept4` | 同板块同名同结构 | accept, fcntl | accept, fcntl |
| `arceos/api/arceos_posix_api/src/imp/net.rs#recvfrom` | `arceos/api/arceos_posix_api/src/imp/net.rs#recvfrom` | 同板块同名同结构 | Ok, Some, tcpsocket.lock, tcpsocket.lock().recv, tcpsocket.lock().recv(buf).map, udpsocket
                .lock ...(+2) | Ok, Some, tcpsocket.lock, tcpsocket.lock().recv, tcpsocket.lock().recv(buf).map, udpsocket
                .lock ...(+2) |

#### 文件系统/第三方库
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `crates/ext4plus/src/file_blocks/extent_tree.rs#write_at` | `arceos/crates/lwext4_rust/src/inode/file.rs#write_at` | 同板块同名 | 1usize
                    .checked_add, 1usize
                    .checked_add(remaining_after_first.div_ceil(block_size))
                    .ok_or, CorruptKind::ExtentBlock, CorruptKind::ExtentBlock(inode.index).into, CorruptKind::InvalidBlockSize.into, Err ...(+102) | Ok, assert!, assert_eq!, buf.as_ptr, buf.is_empty, buf.len ...(+15) |
| `arceos/modules/axfs/src/fs/ext4/inode.rs#rename` | `arceos/crates/axfs-ng-vfs/src/node/dir.rs#rename` | 同板块同名 | Arc::as_ptr, Err, Ok, active.get_mut, active.remove, core::num::NonZeroU32::new ...(+42) | Err, Self::forget_entry, Some, core::ptr::eq, dir.has_children, dst.as_dir ...(+14) |
| `crates/ext4plus/src/lib.rs#alloc_inode` | `arceos/crates/lwext4_rust/src/fs.rs#alloc_inode` | 同板块同名 | Err, InodeIndex::try_from, InodeIndex::try_from(
                    inode_num
                        .checked_add(
                            self.0
                                .superblock
                                .inodes_per_block_group()
                                .get()
                                .checked_mul(bg_id)
                                .unwrap(),
                        )
                        .unwrap()
                        .checked_add(1)
                        .unwrap(),
                )
                .unwrap, Ok, bg.free_inodes_count, bg.set_free_inodes_count ...(+44) | InodeRef::new, Ok, ext4_fs_alloc_inode, ext4_fs_alloc_inode(self.inner.as_mut(), result.inner.as_mut(), ty as _)
                .context, ext4_fs_inode_blocks_init, mem::zeroed ...(+2) |
| `crates/ext4plus/src/file_blocks/block_map.rs#write_at` | `arceos/modules/axfs-ng/src/fs/ext4/inode.rs#write_at` | 同板块同名 | Ok, add_to_file_offset, block_size.to_u64, block_size.to_usize, block_size_usize
            .checked_sub, block_size_usize
            .checked_sub(offset_in_block)
            .ok_or ...(+27) | self.fs
            .lock, self.fs
            .lock()
            .write_at, self.fs
            .lock()
            .write_at(self.ino, buf, offset)
            .map_err |

#### 网络协议栈/第三方库
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `crates/smoltcp/src/storage/assembler.rs#add` | `arceos/modules/axnet/src/smoltcp_impl/mod.rs#add` | 同板块同名 | Contig::hole_and_data, Err, Ok, contig.has_data, contig.shrink_hole_to, contig.total_size ...(+11) | debug!, self.0.lock, self.0.lock().add |
| `arceos/modules/axnet/src/smoltcp_impl/listen_table.rs#accept` | `arceos/modules/axnet/src/smoltcp_impl/listen_table.rs#accept` | 同板块同名 | Err, Ok, SOCKET_SET.remove, ax_err!, drop, entry.syn_queue.iter ...(+15) | Err, Ok, entry.lock, get_addr_tuple, is_closed, is_connected ...(+10) |
| `arceos/modules/axnet/src/smoltcp_impl/dns.rs#query` | `arceos/modules/axnet/src/smoltcp_impl/dns.rs#query` | 同板块同名 | Err, Ok, SOCKET_SET
            .with_socket_mut::<dns::Socket, _, _>, SOCKET_SET
            .with_socket_mut::<dns::Socket, _, _>(handle, |socket| {
                socket.start_query(iface.lock().context(), name, query_type)
            })
            .map_err, SOCKET_SET.poll_interfaces, SOCKET_SET.with_socket_mut::<dns::Socket, _, _> ...(+12) | Err, IpAddr::from, Ok, SOCKET_SET
            .with_socket_mut::<dns::Socket, _, _>, SOCKET_SET
            .with_socket_mut::<dns::Socket, _, _>(handle, |socket| {
                socket.start_query(iface.lock().context(), name, query_type)
            })
            .map_err, SOCKET_SET.poll_interfaces ...(+11) |
| `arceos/modules/axnet/src/smoltcp_impl/tcp.rs#bind` | `arceos/modules/axnet/src/smoltcp_impl/tcp.rs#bind` | 同板块同名 | Ok, SOCKET_SET.bind_check, SOCKET_SET.with_socket_mut::<tcp::Socket, _, _>, Some, assert_ne!, ax_err! ...(+15) | Err, Ok, SOCKET_SET.add, SOCKET_SET.bind_check, SOCKET_SET.with_socket_mut::<tcp::Socket, _, _>, SocketSetWrapper::new_tcp_socket ...(+15) |

#### 第三方库
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `crates/memory_set/src/set.rs#protect` | `arceos/crates/page_table_multiarch/page_table_multiarch/src/bits64.rs#protect` | 同板块同名 | MemoryArea::new, Ok, Vec::new, area.backend, area.backend().clone, area.end ...(+19) | Err, Ok, TlbFlush::new, entry.is_present, entry.set_flags, self.get_entry_mut ...(+1) |
| `crates/virtio-drivers/src/device/blk.rs#flush` | `arceos/crates/axdriver_crates/axdriver_virtio/src/gpu.rs#flush` | 同板块同名 | (BlkFeature::RING_INDIRECT_DESC | BlkFeature::FLUSH).bits, Arc::new, BlkResp {
                            status: RespStatus::OK,
                        }
                        .as_bytes, Default::default, Mutex::new, NonNull::from ...(+19) | self.inner.flush, self.inner.flush().map_err |
| `crates/memory_set/src/set.rs#unmap` | `arceos/crates/page_table_multiarch/page_table_multiarch/src/bits64.rs#unmap` | 同板块同名 | AddrRange::try_from_start_size, AddrRange::try_from_start_size(start, size).ok_or, Ok, after.end, after_end.sub_addr, area.unmap_area ...(+18) | Err, Ok, TlbFlush::new, entry.clear, entry.is_present, entry.paddr ...(+1) |
| `crates/axio/src/utils/copy.rs#copy_from` | `arceos/crates/page_table_multiarch/page_table_multiarch/src/bits64.rs#copy_from` | 同板块同名 | Err, Ok, buf.len, buf.set_len, buf.spare_capacity_mut, buf.spare_capacity_mut().into ...(+12) | assert!, entry.paddr, index_fn, self.borrowed_entries.set, self.dealloc_tree, self.next_table ...(+5) |

#### 通用模块/其他
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `arceos/ulib/axlibc/c/printf.c#print_exponential_number` | `arceos/ulib/axlibc/c/printf.c#print_exponential_number` | 同板块同名同结构 | ABS_FOR_PRINTING, PRINTF_ABS, bastardized_floor, get_components, get_normalized_components, get_sign_bit ...(+5) | ABS_FOR_PRINTING, PRINTF_ABS, bastardized_floor, get_components, get_normalized_components, get_sign_bit ...(+5) |
| `arceos/ulib/axlibc/c/pow.c#pow` | `arceos/ulib/axlibc/c/pow.c#pow` | 同板块同名同结构 | __builtin_fma, __math_invalid, __math_oflow, __math_uflow, asdouble, asuint64 ...(+8) | __builtin_fma, __math_invalid, __math_oflow, __math_uflow, asdouble, asuint64 ...(+8) |
| `arceos/ulib/axlibc/c/string.c#twoway_strstr` | `arceos/ulib/axlibc/c/string.c#twoway_strstr` | 同板块同名同结构 | BITOP, MAX, memchr, memcmp | BITOP, MAX, memchr, memcmp |
| `arceos/ulib/axlibc/c/printf.c#print_broken_up_decimal` | `arceos/ulib/axlibc/c/printf.c#print_broken_up_decimal` | 同板块同名同结构 | out_rev_ | out_rev_ |


好的，我将以审计专家的身份，基于提供的证据和双方辩论，对本次同源性争议进行最终定性仲裁。

### 1. 宏观判定
- **定性结论**: **独立开发 (相似度: 14.24%)**
  共同 AST 指纹: 1124；唯一 AST 指纹: 目标项目 6123，参考项目 2897；覆盖率: 目标项目 18.36%，参考项目 38.8%。
  客观数据表明，目标项目超过 80% 的代码结构独立于参考项目。双方重合部分高度集中于共同继承的第三方开源代码（如 ArceOS 组件、smoltcp、lwext4_rust 等），而在核心自研模块（如进程管理、内存管理）上呈现出显著的架构差异。控方提出的“大规模直接复用”指控不成立。

### 2. 全模块覆盖矩阵解读
| 模块 | 双方存在 | 共同指纹 | 目标覆盖 | 参考覆盖 | 证据强度 | 解读 |
| :--- | :---: | :---: | :---: | :---: | :---: | :--- |
| **进程管理** | 是 | 27 | 9.61% | 16.98% | **弱** | 目标项目覆盖率极低，表明其进程管理实现高度独立。重合点多为 `arceos/modules/axtask` 下的上游调度原语，非参考项目原创。 |
| **任务调度/异步运行时** | 是 | 107 | 24.43% | 30.48% | **中** | 覆盖率中等，但证据显示重合集中在 `arceos/ulib/axlibc` 等公共 C 库和 `axtask` 上游组件，属于共同继承。 |
| **内存管理** | 是 | 97 | 22.99% | 24.94% | **弱** | 覆盖率较低。证据显示双方在核心系统调用 `sys_mmap` 上调用链差异巨大，架构设计独立。 |
| **中断/异常处理** | 是 | 2 | 18.18% | 3.23% | **未形成** | 共同指纹极少，且为 `pow.c` 等数学库函数，与中断/异常处理核心逻辑无关，不构成有效证据。 |
| **系统调用** | 是 | 55 | 53.4% | 68.75% | **弱** | 看似覆盖率高，但证据多为 `syscall_body!` 宏或 `debug!` 调用，属于无信息量的 trivial 模式，非实质性逻辑复用。 |
| **文件系统** | 是 | 186 | 36.05% | 29.95% | **中** | 重合点包含 `glob.c`、`setjmp.rs` 等公共 C 库和 `axfs` 上游组件。双方在自研的 `procfs` 和 `ext4` 实现上调用流差异显著。 |
| **设备驱动** | 是 | 86 | 86.87% | 68.25% | **弱** | 覆盖率极高，但证据明确指向 `axdriver`、`virtio` 等上游驱动框架和 `bwbench` 测试工具，均为公共代码。 |
| **网络协议栈** | 是 | 51 | 78.46% | 83.61% | **弱** | 高覆盖率源于双方均使用 `smoltcp` 协议栈和 `arceos_posix_api` 网络接口，属于第三方公共依赖。 |
| **第三方库/公共依赖** | 是 | 175 | 4.5% | 27.9% | **弱** | 目标项目覆盖率极低，说明其第三方库选型与参考项目差异大。重合部分为 `memory_set`、`virtio-drivers` 等通用组件。 |

### 3. 底层 AST 关键证据指纹表
| 所属板块 | 目标项目路径 | 参考项目路径 | 证据类型 | 技术说明 |
| :--- | :--- | :--- | :--- | :--- |
| **进程管理** | `pulse_syscalls/src/impls/task/wait.rs#sys_wait4` | `xapi/src/task/wait.rs#sys_wait4` | 架构差异 | 目标使用 `LinuxError` 错误码与 `child_proc` 对象；参考使用 `WaitOptions/WaitPid` 枚举与 `XProcess` 抽象。对象模型完全不同。 |
| **进程管理** | `pulse_core/src/task/process.rs#exit_robust_list` | `xapi/src/task/futex.rs#exit_robust_list` | 架构差异 | 目标在进程对象内部直接处理；参考通过 `UserPtr`/`uspace` 读写并调用独立 `handle_futex_death`。处理层与外部对象均不同。 |
| **进程管理** | `pulse_core/src/task/process.rs#requeue` | `arceos/modules/axtask/src/wait_queue.rs#requeue` | 架构差异 | 目标实现的是 futex requeue（跨地址队列迁移）；参考实现的是 wait_queue requeue（同队列内批量移动）。语义与拓扑完全不同。 |
| **进程管理** | `arceos/modules/axtask/src/run_queue.rs#exit_current` | `arceos/modules/axtask/src/run_queue.rs#exit_current` | 共同上游 | 路径完全一致，核心逻辑 `EXITED_TASKS`、`WAIT_FOR_EXIT` 为 ArceOS 公共 percpu 结构，非参考项目原创。 |
| **内存管理** | `pulse_syscalls/src/impls/mm.rs#sys_mmap` | `xapi/src/mm/mmap.rs#sys_mmap` | 架构差异 | 目标使用 `CachedFile::get_or_create` 等自定义缓存机制；参考使用 `FD_TABLE.is_assigned` 等文件描述符表检查。内存映射后端管理模型不同。 |
| **文件系统** | `arceos/modules/axfs/src/fs/procfs.rs#write_at` | `xcore/src/fs/vfs/proc/pid.rs#write_at` | 架构差异 | 目标直接操作裸 buffer (`data[start..end].copy_from_slice`)；参考通过 VFS inode 弱引用升级 (`self.0.upgrade().unwrap().write`) 委托写入。 |
| **文件系统** | `crates/ext4plus/src/lib.rs#alloc_inode` | `arceos/crates/lwext4_rust/src/fs.rs#alloc_inode` | 架构差异 | 目标手动管理块组与 inode 位图；参考封装 `ext4_fs_alloc_inode` 等 C API 调用。实现层级和依赖不同。 |
| **系统调用** | `pulse_syscalls/src/impls/net/socket.rs#sys_socket` | `arceos/api/arceos_posix_api/src/imp/net.rs#sys_socket` | 架构差异 | 目标包含完整的 `Socket::new` 构造及错误码映射；参考仅为 `syscall_body!` 宏调用，逻辑在上游 crate 中。 |
| **第三方库** | `crates/memory_set/src/set.rs#protect` | `arceos/crates/page_table_multiarch/.../bits64.rs#protect` | 架构差异 | 目标通过 `MemoryArea` 和 `backend` 抽象操作；参考直接操作页表项 `entry.set_flags`。内存管理抽象层次不同。 |

### 4. 核心代码调用流对比剖析
* **【进程管理 / 进程等待系统调用】**
* **底层技术析理**: `sys_wait4` 是进程管理的核心系统调用。目标项目采用 Linux 兼容的错误码模型 (`LinuxError::ECHILD`, `LinuxError::ESRCH`)，并通过 `child_proc` 对象直接管理子进程状态与资源释放 (`child_proc.exit_code`, `child_proc.release_task_refs`)。参考项目则构建了自研的 `WaitOptions`/`WaitPid` 枚举体系，并通过 `XProcess` 抽象和 `is_clone_child` 方法处理复杂的克隆进程等待逻辑。两者在错误处理模型、进程对象抽象和等待语义的实现上采用了完全不同的技术路线，不存在底层复用。
* **代码溯源链接**:
  - 目标项目: `pulse_syscalls/src/impls/task/wait.rs#sys_wait4`
  - 参考项目: `xapi/src/task/wait.rs#sys_wait4`

* **【内存管理 / 内存映射系统调用】**
* **底层技术析理**: `sys_mmap` 是内存管理的核心。目标项目的调用流涉及 `CachedFile::get_or_create`，表明其实现了基于文件缓存的内存映射后端。参考项目的调用流则侧重于 `FD_TABLE.is_assigned` 和 `File::from_fd`，表明其内存映射依赖于文件描述符表的状态检查。两者在文件与内存的关联管理、映射后端的实现策略上存在根本性架构差异。
* **代码溯源链接**:
  - 目标项目: `pulse_syscalls/src/impls/mm.rs#sys_mmap`
  - 参考项目: `xapi/src/mm/mmap.rs#sys_mmap`

### 5. 单方模块与降权项
* **单方模块**: 目标项目的 `网络协议栈/第三方库` (2635 个函数) 和 `文件系统/第三方库` (863 个函数) 规模远超参考项目，表明其集成了大量独立选型的第三方代码，这些是目标项目独有的，不构成同源证据。
* **降权项 (Trivial 模式与公共接口)**:
  * **泛型宏调用**: `syscall_body!`, `debug!`, `println!`, `unimplemented!` 等宏是 Rust 生态和 ArceOS 框架的通用设施，其调用模式不具备独创性，不能作为复用证据。
  * **单行原子操作**: `self.clear_child_tid.store`, `self.in_wait_queue.store` 等是标准的原子变量操作，变量名源自 Linux ABI 标准，属于不可保护的 trivial 模式。
  * **共同上游代码**: 所有位于 `arceos/modules/axtask`, `arceos/api/arceos_posix_api`, `arceos/ulib/axlibc` 等路径下的匹配项，均为双方共同继承自 ArceOS 上游项目的代码，不能作为项目间抄袭的证据。

### 6. 保守结论
本次审计基于 14.24% 的客观 AST 相似度，该指标远低于构成“抄袭”或“大规模复用”的阈值。双方代码库的重合部分，经逐项排查，几乎全部可归因于对 ArceOS 内核组件、smoltcp 协议栈、lwext4_rust 文件系统等第三方开源代码的共同继承，或是对 POSIX/Linux 标准 ABI 的合规实现。在核心的进程管理、内存管理等自研模块，双方在对象模型、错误处理、同步机制和抽象层次上展现出根本性的架构差异。控方提出的“基因级复用”指控缺乏实质性证据支持，大量所谓“铁证”实为公共上游代码或不可保护的 trivial 模式。因此，综合判定目标项目为独立开发，整体风险等级为低。

------------------------------

