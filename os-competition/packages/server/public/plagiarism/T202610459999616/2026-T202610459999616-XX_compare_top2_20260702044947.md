# 🛡️ OS 内核赛自动化代码级架构审计报告

**审计时间**: `2026-07-02 04:49:47`
**参赛作品名称**: `2026-T202610459999616-XX`
**年份**: `2026`
**学校**: `XX`
**队伍名称**: `T202610459999616`
**比赛名字**: `OS-Competition`
**比赛子赛道**: `Kernel-Implementation`
**仓库地址**: `https://gitlab.eduxiji.net/educg-group-43501-3132633/T202610459999616-2917`
**推测开发语言**: `Rust / C (由AST探针自动识别)`
**特征函数量**: `7613`

## ⚔️ 底层代码同源性对比 (AST Homology)
> 本部分由专门的代码审查智能体，对该项目与符合年份条件的历届最高相似度项目进行控制流与底层调用对比。新版报告会先给出确定性的全模块覆盖矩阵，再给出 Agent 的分模块解释，避免只围绕少量抽样函数展开。

### 🏆 Top 2 底层对标: vs `2025-AstranciA-哈尔滨工业大学深圳`（哈尔滨工业大学（深圳） / AstranciA）

#### 对方作品信息

**对方年份**: `2025`
**对方学校**: `哈尔滨工业大学（深圳）`
**对方队伍名称**: `AstranciA`
**对方比赛名字**: `OS-Competition`
**对方比赛子赛道**: `Kernel-Implementation`
**对方仓库地址**: `https://gitlab.eduxiji.net/educg-group-36002-2710490/T202518123995667-1436`

> 双向覆盖率：共同 AST 指纹: 1266；唯一 AST 指纹: 目标项目 6123，参考项目 3750；覆盖率: 目标项目 20.68%，参考项目 33.76%

## 全模块对比总览（确定性证据）

> 总体覆盖率：共同 AST 指纹: 1266；唯一 AST 指纹: 目标项目 6123，参考项目 3750；覆盖率: 目标项目 20.68%，参考项目 33.76%

### 1. 模块覆盖矩阵
| 模块 | 目标函数数 | 参考函数数 | 共同唯一 AST 指纹 | Target 覆盖 | Base 覆盖 | 状态 |
| :--- | ---: | ---: | ---: | ---: | ---: | :--- |
| 进程管理 | 321 | 112 | 25 | 8.9% | 27.17% | 双方存在且有结构重合 |
| 任务调度/异步运行时 | 497 | 455 | 108 | 24.66% | 33.64% | 双方存在且有结构重合 |
| 内存管理 | 454 | 343 | 91 | 21.56% | 29.55% | 双方存在且有结构重合 |
| 中断/异常处理 | 14 | 71 | 2 | 18.18% | 3.51% | 双方存在且有结构重合 |
| 系统调用 | 118 | 136 | 56 | 54.37% | 48.28% | 双方存在且有结构重合 |
| 文件系统 | 608 | 620 | 139 | 26.94% | 28.66% | 双方存在且有结构重合 |
| 设备驱动 | 111 | 142 | 85 | 85.86% | 67.46% | 双方存在且有结构重合 |
| 网络协议栈 | 76 | 61 | 51 | 78.46% | 100.0% | 双方存在且有结构重合 |
| 文件系统/第三方库 | 863 | 775 | 3 | 0.39% | 0.47% | 双方存在且有结构重合 |
| 网络协议栈/第三方库 | 2635 | 217 | 142 | 7.04% | 73.58% | 双方存在且有结构重合 |
| 第三方库 | 1354 | 1155 | 243 | 21.45% | 26.1% | 双方存在且有结构重合 |
| 通用模块/其他 | 562 | 673 | 273 | 65.78% | 50.28% | 双方存在且有结构重合 |

### 2. 分模块证据清单

#### 进程管理
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `pulse_syscalls/src/impls/misc.rs#sys_rt_sigtimedwait` | `AstrancE/ulib/axmono/src/syscall/signal.rs#sys_rt_sigtimedwait` | 同板块同名 | (axhal::time::monotonic_time_nanos() as u64).saturating_add, Duration::from_nanos, LinuxError::EAGAIN.code, LinuxError::EFAULT.code, LinuxError::EINTR.code, LinuxError::EINVAL.code ...(+21) | crate::task::signal::sys_sigtimedwait, crate::task::signal::sys_sigtimedwait(set as _, info as _, timeout as _).map |
| `pulse_syscalls/src/impls/task/wait.rs#sys_wait4` | `AstrancE/ulib/axsyscall/src/syscall_imp/process.rs#sys_wait4` | 同板块同名 | LinuxError::ECHILD.code, LinuxError::ESRCH.code, axhal::time::monotonic_time_nanos, axlog::debug!, axlog::warn!, child_proc.exit_code ...(+17) | axmono::syscall::process::sys_wait4 |
| `arceos/modules/axtask/src/run_queue.rs#exit_current` | `AstrancE/modules/axtask/src/run_queue.rs#exit_current` | 同板块同名 | EXITED_TASKS.current_ref_mut_raw, EXITED_TASKS.current_ref_mut_raw().clear, EXITED_TASKS.current_ref_mut_raw().push_back, GC_TASK.with_current, WAIT_FOR_EXIT.current_ref_mut_raw, WAIT_FOR_EXIT.current_ref_mut_raw().notify_one ...(+15) | EXITED_TASKS.current_ref_mut_raw, EXITED_TASKS.current_ref_mut_raw().clear, EXITED_TASKS.current_ref_mut_raw().push_back, WAIT_FOR_EXIT.current_ref_mut_raw, WAIT_FOR_EXIT.current_ref_mut_raw().notify_one, assert! ...(+8) |
| `arceos/modules/axtask/src/wait_queue.rs#cancel_events` | `AstrancE/modules/axtask/src/wait_queue.rs#cancel_events` | 同板块同名 | Arc::ptr_eq, curr.as_task_ref, curr.in_wait_queue, curr.set_in_wait_queue, curr.timer_ticket_expired, self.queue.lock ...(+3) | curr.in_wait_queue, curr.ptr_eq, curr.set_in_wait_queue, curr.timer_ticket_expired, self.queue.lock, self.queue.lock().retain |

#### 任务调度/异步运行时
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `arceos/ulib/axlibc/c/printf.c#format_string_loop` | `AstrancE/ulib/axlibc/c/printf.c#format_string_loop` | 同板块同名同结构 | ABS_FOR_PRINTING, ADVANCE_IN_FORMAT_STRING, atou_, is_digit_, out_rev_, parse_flags ...(+5) | ABS_FOR_PRINTING, ADVANCE_IN_FORMAT_STRING, atou_, is_digit_, out_rev_, parse_flags ...(+5) |
| `arceos/modules/axlog/src/lib.rs#log` | `AstrancE/modules/axlog/src/lib.rs#log` | 同板块同名同结构 | cfg_if::cfg_if!, record.level, record.line, record.line().unwrap_or, record.metadata, record.target ...(+1) | cfg_if::cfg_if!, record.level, record.line, record.line().unwrap_or, record.metadata, record.target ...(+1) |
| `arceos/modules/axtask/src/run_queue.rs#switch_to` | `AstrancE/modules/axtask/src/run_queue.rs#switch_to` | 同板块同名 | (*prev_ctx_ptr).switch_to, Arc::downgrade, CurrentTask::set_current, PREV_TASK.current_ref_mut_raw, assert!, clear_prev_task_on_cpu ...(+9) | (*prev_ctx_ptr).switch_to, Arc::downgrade, CurrentTask::set_current, PREV_TASK.current_ref_mut_raw, assert!, clear_prev_task_on_cpu ...(+7) |
| `pulse_syscalls/src/impls/misc.rs#sys_rt_sigaction` | `AstrancE/ulib/axmono/src/syscall/signal.rs#sys_rt_sigaction` | 同板块同名 | (&raw as *const sigaction).cast::<u8>, LinuxError::EFAULT.code, LinuxError::EINVAL.code, SigAction::from_parts, core::mem::size_of::<sigaction>, core::mem::size_of::<u64> ...(+13) | crate::task::signal::sys_sigaction, crate::task::signal::sys_sigaction(signum, act as _, oldact as _).map_err, panic! |

#### 内存管理
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `arceos/modules/axmm/src/backend/alloc.rs#handle_page_fault_alloc` | `AstrancE/modules/axmm/src/backend/alloc.rs#handle_page_fault_alloc` | 同板块同名 | alloc_contiguous_frames, alloc_frame, curr_flags.is_empty, curr_frame.as_usize, dealloc_frame, debug! ...(+26) | Ok, PageSize::Size4K.into, Self::handle_page_fault_cow, alloc_frame, aspace
                            .page_table, aspace
                            .page_table()
                            .map ...(+19) |
| `arceos/ulib/axlibc/src/strftime.rs#strftime_inner` | `AstrancE/ulib/axlibc/src/strftime.rs#strftime_inner` | 同板块同名同结构 | CountingWriter::new, format.offset, inner_strftime, w! | CountingWriter::new, format.offset, inner_strftime, w! |
| `arceos/modules/axmm/src/backend/cow.rs#handle_page_fault` | `AstrancE/modules/axmm/src/aspace/mod.rs#handle_page_fault` | 同板块同名 | access_flags.contains, alloc_frame, core::ptr::copy_nonoverlapping, curr_flags.contains, dealloc_frame, drop ...(+23) | area
                    .backend, area
                    .backend()
                    .clone, area
                    .backend()
                    .clone()
                    .handle_page_fault, area.flags, debug!, orig_flags.contains ...(+2) |
| `arceos/ulib/axlibc/src/strftime.rs#inner_strftime` | `AstrancE/ulib/axlibc/src/strftime.rs#inner_strftime` | 同板块同名同结构 | format.offset, w! | format.offset, w! |

#### 中断/异常处理
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `arceos/ulib/axlibc/c/pow.c#exp_inline` | `AstrancE/ulib/axlibc/c/pow.c#exp_inline` | 同板块同名同结构 | __math_oflow, __math_uflow, asdouble, asuint64, converttoint, eval_as_double ...(+4) | __math_oflow, __math_uflow, asdouble, asuint64, converttoint, eval_as_double ...(+4) |
| `arceos/modules/axsync/src/mutex.rs#may_interrupt` | `AstrancE/modules/axsync/src/mutex.rs#may_interrupt` | 同板块同名同结构 | rand::random::<u32>, thread::yield_now | rand::random::<u32>, thread::yield_now |
| `pulse_core/src/fd_table.rs#wait_ready` | `AstrancE/modules/axhal/src/arch/loongarch64/unaligned.rs#emulate_load_store_insn` | 同模块上下文对齐/差异观察 | Err, Ok, Self::current_has_pending_signal, axhal::time::monotonic_time, self.ready_for, self.writable ...(+2) | asm!, badv::read, badv::read().raw, core::slice::from_raw_parts_mut, panic!, read_fpr ...(+5) |
| `pulse_syscalls/src/impls/time.rs#sys_nanosleep` | `AstrancE/modules/axhal/src/arch/aarch64/trap.rs#handle_sync_exception` | 同模块上下文对齐/差异观察 | LinuxError::EFAULT.code, LinuxError::EINTR.code, axlog::trace!, axlog::warn!, duration_to_timespec, e.code ...(+5) | ESR_EL1.extract, crate::trap::handle_syscall, crate::trap::post_trap_callback, debug!, esr.read, esr.read_as_enum ...(+8) |

#### 系统调用
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `pulse_syscalls/src/impls/net/socket.rs#sys_socket` | `AstrancE/api/arceos_posix_api/src/imp/net.rs#sys_socket` | 同板块同名 | FdFlags::empty, LinuxError::EAFNOSUPPORT.code, LinuxError::EINVAL.code, LinuxError::EPROTONOSUPPORT.code, LinuxError::ESOCKTNOSUPPORT.code, Socket::new ...(+14) | debug!, syscall_body! |
| `pulse_syscalls/src/impls/net/name.rs#sys_getpeername` | `AstrancE/api/arceos_posix_api/src/imp/net.rs#sys_getpeername` | 同板块同名 | 16u16.to_ne_bytes, LinuxError::EFAULT.code, NetSocketAddr::from, Some, core::cmp::min, crate::impls::utils::write_user_bytes ...(+14) | debug!, syscall_body! |
| `arceos/api/arceos_posix_api/src/imp/net.rs#sys_getaddrinfo` | `AstrancE/api/arceos_posix_api/src/imp/net.rs#sys_getaddrinfo` | 同板块同名同结构 | char_ptr_to_str, debug!, syscall_body! | char_ptr_to_str, debug!, syscall_body! |
| `pulse_syscalls/src/impls/net/socket.rs#sys_shutdown` | `AstrancE/api/arceos_posix_api/src/imp/net.rs#sys_shutdown` | 同板块同名 | Err, LinuxError::EINVAL.code, LinuxError::from, Ok, debug!, e.canonicalize ...(+9) | debug!, syscall_body! |

#### 文件系统
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `arceos/modules/axfs/src/fs/tmpfs.rs#rename` | `AstrancE/modules/axfs/src/fs/fatfs.rs#rename` | 同板块同名 | "..".into, Err, InodeRef::new, Ok, dir_content.entries.lock, dst_dir.downcast::<Self> ...(+29) | debug!, self.0
            .rename, self.0
            .rename(src_path, &self.0, dst_path)
            .map_err |
| `arceos/ulib/axlibc/c/glob.c#do_glob` | `AstrancE/ulib/axlibc/c/glob.c#do_glob` | 同板块同名同结构 | S_ISDIR, append, closedir, do_glob, errfunc, fnmatch ...(+7) | S_ISDIR, append, closedir, do_glob, errfunc, fnmatch ...(+7) |
| `arceos/ulib/axlibc/src/setjmp.rs#longjmp` | `AstrancE/ulib/axlibc/src/setjmp.rs#longjmp` | 同板块同名 | core::arch::naked_asm! | core::arch::naked_asm! |
| `arceos/ulib/axlibc/src/setjmp.rs#setjmp` | `AstrancE/ulib/axlibc/src/setjmp.rs#setjmp` | 同板块同名 | core::arch::naked_asm! | core::arch::naked_asm! |

#### 设备驱动
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `arceos/tools/bwbench_client/src/main.rs#transmit_benchmark` | `AstrancE/tools/bwbench_client/src/main.rs#transmit_benchmark` | 同板块同名同结构 | Local::now, NetDevice::new, NetDevice::new(interface).unwrap, current_time.signed_duration_since, current_time.signed_duration_since(past_time).num_seconds, dev.send ...(+2) | Local::now, NetDevice::new, NetDevice::new(interface).unwrap, current_time.signed_duration_since, current_time.signed_duration_since(past_time).num_seconds, dev.send ...(+2) |
| `arceos/tools/bwbench_client/src/device.rs#bind_interface` | `AstrancE/tools/bwbench_client/src/device.rs#bind_interface` | 同板块同名同结构 | ETH_P_ALL.to_be, Err, Ok, libc::bind, self.ifreq.ioctl, std::io::Error::last_os_error ...(+2) | ETH_P_ALL.to_be, Err, Ok, libc::bind, self.ifreq.ioctl, std::io::Error::last_os_error ...(+2) |
| `arceos/modules/axdriver/src/lib.rs#init_drivers` | `AstrancE/modules/axdriver/src/lib.rs#init_drivers` | 同板块同名 | AllDevices::default, all_devs.block.iter, all_devs.block.iter().enumerate, all_devs.display.iter, all_devs.display.iter().enumerate, all_devs.net.iter ...(+5) | AllDevices::default, all_devs.block.iter_mut, all_devs.block.iter_mut().enumerate, all_devs.display.iter_mut, all_devs.display.iter_mut().enumerate, all_devs.net.iter_mut ...(+14) |
| `arceos/modules/axdriver/src/virtio.rs#probe_pci` | `AstrancE/modules/axdriver/src/virtio.rs#probe_pci` | 同板块同名同结构 | D::try_new, Some, axdriver_virtio::probe_pci_device::<VirtIoHalImpl>, warn! | D::try_new, Some, axdriver_virtio::probe_pci_device::<VirtIoHalImpl>, warn! |

#### 网络协议栈
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `arceos/ulib/axlibc/c/network.c#inet_pton` | `AstrancE/ulib/axlibc/c/network.c#inet_pton` | 同板块同名同结构 | hexval, inet_pton, isdigit, memmove | hexval, inet_pton, isdigit, memmove |
| `arceos/api/arceos_posix_api/src/imp/net.rs#shutdown` | `AstrancE/api/arceos_posix_api/src/imp/net.rs#shutdown` | 同板块同名同结构 | Ok, tcpsocket.lock, tcpsocket.peer_addr, tcpsocket.shutdown, udpsocket.lock, udpsocket.peer_addr ...(+1) | Ok, tcpsocket.lock, tcpsocket.peer_addr, tcpsocket.shutdown, udpsocket.lock, udpsocket.peer_addr ...(+1) |
| `arceos/ulib/axlibc/c/socket.c#accept4` | `AstrancE/ulib/axlibc/c/socket.c#accept4` | 同板块同名同结构 | accept, fcntl | accept, fcntl |
| `arceos/api/arceos_posix_api/src/imp/net.rs#recvfrom` | `AstrancE/api/arceos_posix_api/src/imp/net.rs#recvfrom` | 同板块同名同结构 | Ok, Some, tcpsocket.lock, tcpsocket.lock().recv, tcpsocket.lock().recv(buf).map, udpsocket
                .lock ...(+2) | Ok, Some, tcpsocket.lock, tcpsocket.lock().recv, tcpsocket.lock().recv(buf).map, udpsocket
                .lock ...(+2) |

#### 文件系统/第三方库
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `crates/ext4plus/src/file_blocks/extent_tree.rs#write_at` | `AstrancE/modules/axfs/src/fs/lwext4_rust.rs#write_at` | 同板块同名 | 1usize
                    .checked_add, 1usize
                    .checked_add(remaining_after_first.div_ceil(block_size))
                    .ok_or, CorruptKind::ExtentBlock, CorruptKind::ExtentBlock(inode.index).into, CorruptKind::InvalidBlockSize.into, Err ...(+102) | <i32 as TryInto<AxError>>::try_into, <i32 as TryInto<AxError>>::try_into(e).unwrap, Ok, buf.len, file.file_close, file.file_open ...(+11) |
| `arceos/modules/axfs/src/fs/ext4/inode.rs#rename` | `AstrancE/modules/axfs/src/fs/lwext4_rust.rs#rename` | 同板块同名 | Arc::as_ptr, Err, Ok, active.get_mut, active.remove, core::num::NonZeroU32::new ...(+42) | e.try_into, e.try_into().unwrap, file.file_rename, file.file_rename(src_path.as_str(), dst_path)
            .map, file.file_rename(src_path.as_str(), dst_path)
            .map(|_v| ())
            .map_err, self.0.lock ...(+2) |
| `crates/ext4plus/src/file_blocks/block_map.rs#write_at` | `AstrancE/crates/lwext4_rust/examples/src/ext4fs.rs#write_at` | 同板块同名 | Ok, add_to_file_offset, block_size.to_u64, block_size.to_usize, block_size_usize
            .checked_sub, block_size_usize
            .checked_sub(offset_in_block)
            .ok_or ...(+27) | debug!, file.file_close, file.file_open, file.file_seek, file.file_write, file.get_path ...(+3) |
| `arceos/modules/axfs/src/fs/ext4/inode.rs#create` | `AstrancE/modules/axfs/src/fs/lwext4_rust.rs#create` | 同板块同名 | (|| -> VfsResult<DirEntry> {
            let dir_idx = core::num::NonZeroU32::new(self.ino).ok_or(VfsError::InvalidData)?;
            let parent_inode = ext4plus::inode::Inode::read(&fs, dir_idx).map_err(into_vfs_err)?;
            let mut dir = ext4plus::dir::Dir::open_inode(&fs, parent_inode).map_err(into_vfs_err)?;

            if node_type == NodeType::Directory {
                let new_dir = ext4plus::dir::Dir::init(fs.clone(), new_inode, dir_idx).map_err(into_vfs_err)?;
                new_inode = new_dir.inode().clone();
            }

            let name_ref = ext4plus::DirEntryName::try_from(name).map_err(|_| VfsError::InvalidInput)?;
            dir.link(name_ref, &mut new_inode).map_err(into_vfs_err)?;

            self.invalidate_snapshot(self.ino);
            Ok(self.create_entry(
                new_inode.index.get(),
                node_type,
                node_type == NodeType::Directory,
                name,
            ))
        }), Err, Ok, axhal::time::wall_time, core::num::NonZeroU32::new, core::num::NonZeroU32::new(self.ino).ok_or ...(+32) | Ok, e.try_into, e.try_into().unwrap, file.check_inode_exist, file.dir_mk, file.dir_mk(fpath)
                    .map ...(+12) |

#### 网络协议栈/第三方库
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `crates/virtio-drivers/src/device/socket/connectionmanager.rs#send_recv` | `AstrancE/crates/virtio-drivers/src/device/socket/connectionmanager.rs#send_recv` | 同板块同名 | "Hello from guest".as_bytes, (hello_from_guest.len() as u32).into, (hello_from_host.len() as u32).into, 0.into, 50.into, Arc::new ...(+43) | "Hello from guest".as_bytes, (hello_from_guest.len() as u32).into, (hello_from_host.len() as u32).into, 0.into, 50.into, Arc::new ...(+43) |
| `crates/virtio-drivers/src/device/socket/connectionmanager.rs#incoming_connection` | `AstrancE/crates/virtio-drivers/src/device/socket/connectionmanager.rs#incoming_connection` | 同板块同名 | 0.into, 50.into, Arc::new, Default::default, Mutex::new, NonNull::from ...(+25) | 0.into, 50.into, Arc::new, Mutex::new, ReadOnly::new, SocketType::Stream.into ...(+24) |
| `crates/smoltcp/src/storage/assembler.rs#add` | `AstrancE/crates/virtio-drivers/src/device/socket/connectionmanager.rs#add` | 同板块同名 | Contig::hole_and_data, Err, Ok, contig.has_data, contig.shrink_hole_to, contig.total_size ...(+11) | bytes.get, bytes.len, bytes_after_wraparound.len, min, self.buffer.len, self.buffer[0..bytes_after_wraparound.len()].copy_from_slice ...(+2) |
| `arceos/modules/axnet/src/smoltcp_impl/listen_table.rs#accept` | `AstrancE/modules/axnet/src/smoltcp_impl/listen_table.rs#accept` | 同板块同名 | Err, Ok, SOCKET_SET.remove, ax_err!, drop, entry.syn_queue.iter ...(+15) | Ok, ax_err!, get_addr_tuple, is_connected, is_connected(handle).then, self.tcp[port as usize].lock ...(+8) |

#### 第三方库
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `crates/virtio-drivers/src/queue.rs#fake_read_write_queue` | `AstrancE/crates/virtio-drivers/src/queue.rs#fake_read_write_queue` | 同板块同名 | (*used_ring).idx.fetch_add, (*used_ring).idx.load, Vec::new, assert!, assert_eq!, assert_ne! ...(+18) | (*available_ring).idx.load, (*used_ring).idx.fetch_add, (*used_ring).idx.load, Vec::new, assert!, assert_eq! ...(+18) |
| `crates/virtio-drivers/src/queue.rs#recycle_descriptors` | `AstrancE/crates/virtio-drivers/src/queue.rs#recycle_descriptors` | 同板块同名同结构 | Box::from_raw, H::unshare, InputOutputIter::new, InputOutputIter::new(inputs, outputs).enumerate, Some, assert_eq! ...(+17) | Box::from_raw, H::unshare, InputOutputIter::new, InputOutputIter::new(inputs, outputs).enumerate, Some, assert_eq! ...(+17) |
| `crates/virtio-drivers/src/device/input.rs#config` | `AstrancE/crates/virtio-drivers/src/device/input.rs#config` | 同板块同名 | "Serial number".as_bytes, "Test input device".as_bytes, Arc::new, Default::default, Mutex::new, NonNull::from ...(+12) | "Serial number".as_bytes, "Test input device".as_bytes, Arc::new, Default::default, Mutex::new, QUEUE_SIZE.try_into ...(+14) |
| `crates/memory_set/src/set.rs#protect` | `AstrancE/crates/axmm_crates/memory_set/src/set.rs#protect` | 同板块同名 | MemoryArea::new, Ok, Vec::new, area.backend, area.backend().clone, area.end ...(+19) | Ok, Vec::new, area.end, area.flags, area.protect_area, area.set_flags ...(+15) |

#### 通用模块/其他
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `arceos/ulib/axlibc/c/printf.c#print_exponential_number` | `AstrancE/ulib/axlibc/c/printf.c#print_exponential_number` | 同板块同名同结构 | ABS_FOR_PRINTING, PRINTF_ABS, bastardized_floor, get_components, get_normalized_components, get_sign_bit ...(+5) | ABS_FOR_PRINTING, PRINTF_ABS, bastardized_floor, get_components, get_normalized_components, get_sign_bit ...(+5) |
| `arceos/ulib/axlibc/c/pow.c#pow` | `AstrancE/ulib/axlibc/c/pow.c#pow` | 同板块同名同结构 | __builtin_fma, __math_invalid, __math_oflow, __math_uflow, asdouble, asuint64 ...(+8) | __builtin_fma, __math_invalid, __math_oflow, __math_uflow, asdouble, asuint64 ...(+8) |
| `arceos/ulib/axlibc/c/string.c#twoway_strstr` | `AstrancE/ulib/axlibc/c/string.c#twoway_strstr` | 同板块同名同结构 | BITOP, MAX, memchr, memcmp | BITOP, MAX, memchr, memcmp |
| `arceos/ulib/axlibc/c/printf.c#print_broken_up_decimal` | `AstrancE/ulib/axlibc/c/printf.c#print_broken_up_decimal` | 同板块同名同结构 | out_rev_ | out_rev_ |


### 1. 宏观判定
- **定性结论**: **独立开发，存在共同上游依赖** (客观 AST 相似度: 14.71%，共同 AST 指纹: 1266；唯一 AST 指纹: 目标项目 6123，参考项目 3750；覆盖率: 目标项目 20.68%，参考项目 33.76%)。
    - **核心依据**: 14.71% 的宏观相似度远低于同源性阈值。双方重合的 1266 个 AST 指纹，经分析主要源于共同依赖的 ArceOS 开源操作系统基座、第三方库（如 `virtio-drivers`, `smoltcp`）以及 POSIX 标准接口。目标项目独有的 6123 个指纹（占 82.9%）和参考项目独有的 3750 个指纹（占 74.8%）构成了各自独立开发的主体证据。目标项目在 `pulse_syscalls` 和 `pulse_core` 等自研模块中展现出与参考项目完全不同的系统调用实现路径和对象模型，进一步排除了系统性抄袭的可能。

### 2. 全模块覆盖矩阵解读
| 模块 | 双方存在 | 共同指纹 | 目标覆盖 | 参考覆盖 | 证据强度 | 解读 |
| :--- | :---: | :---: | :---: | :---: | :---: | :--- |
| **进程管理** | 是 | 25 | 8.9% | 27.17% | **弱** | 目标项目覆盖率极低（8.9%），表明其进程管理绝大部分为独立实现。重合部分主要为 ArceOS 上游的 `axtask` 等待/运行队列及 POSIX 标准系统调用名，不构成抄袭证据。 |
| **任务调度/异步运行时** | 是 | 108 | 24.66% | 33.64% | **中** | 双方均基于 ArceOS 的 `axtask` 模块，共享其核心调度骨架（如 `switch_to`, `run_queue`）。覆盖率反映的是对上游基座的共同继承程度，而非单向抄袭。 |
| **内存管理** | 是 | 91 | 21.56% | 29.55% | **中** | 重合集中在 ArceOS 的 `axmm` 模块（如缺页处理、写时拷贝）。目标项目在 `handle_page_fault` 等函数中展现出与参考项目不同的后端处理逻辑，表明存在独立演化。 |
| **中断/异常处理** | 是 | 2 | 18.18% | 3.51% | **弱** | 双方模块规模均较小，重合指纹极少。证据 `may_interrupt` 为简单的随机退避逻辑，属通用惯用法，无同源指向性。 |
| **系统调用** | 是 | 56 | 54.37% | 48.28% | **中** | 高覆盖率源于双方都需实现大量 POSIX 标准系统调用。然而，目标项目在 `pulse_syscalls` 中直接展开具体逻辑（如信号处理、进程等待），而参考项目多采用内部转发，实现路径差异显著。 |
| **文件系统** | 是 | 139 | 26.94% | 28.66% | **中** | 重合主要来自 ArceOS 的 `axfs` 模块和第三方库 `axlibc` 的通用 C 函数（如 `glob`, `setjmp`）。目标项目自研的 `ext4plus` 与参考项目封装的 `lwext4_rust` 在实现上完全不同。 |
| **设备驱动** | 是 | 85 | 85.86% | 67.46% | **强（公共依赖）** | 极高的覆盖率源于双方几乎完整复用了 ArceOS 的 `axdriver` 驱动框架及 `virtio-drivers` 等第三方库。这是典型的共同上游依赖，非抄袭。 |
| **网络协议栈** | 是 | 51 | 78.46% | 100.0% | **强（公共依赖）** | 参考项目 100% 的覆盖率说明其网络协议栈完全被目标项目所包含。证据指向 `smoltcp` 和 `axnet` 等公共第三方库及 ArceOS 网络层，属共同依赖。 |
| **文件系统/第三方库** | 是 | 3 | 0.39% | 0.47% | **未形成** | 覆盖率极低，双方在此模块几乎无重合，表明在第三方文件系统库的选型和集成上路径完全不同。 |
| **网络协议栈/第三方库** | 是 | 142 | 7.04% | 73.58% | **强（公共依赖）** | 参考项目高覆盖率因其网络第三方库代码量少且被目标项目完全覆盖。证据指向 `virtio-drivers` 的 socket 实现等公共库。 |
| **第三方库** | 是 | 243 | 21.45% | 26.1% | **强（公共依赖）** | 重合指纹集中于 `virtio-drivers`、`memory_set` 等公共 Rust crate。这是双方共同依赖开源生态的直接证据。 |
| **通用模块/其他** | 是 | 273 | 65.78% | 50.28% | **强（公共依赖）** | 高覆盖率源于 `axlibc` 中大量标准 C 库函数的实现（如 `printf`, `pow`, `string`），这些函数算法固定、实现趋同，属公共知识领域。 |

### 3. 底层 AST 关键证据指纹表
| 所属板块 | 目标项目路径 | 参考项目路径 | 证据类型 | 技术说明 |
| :--- | :--- | :--- | :--- | :--- |
| **第三方库** | `crates/virtio-drivers/src/queue.rs#fake_read_write_queue` | `AstrancE/crates/virtio-drivers/src/queue.rs#fake_read_write_queue` | 公共依赖 | 完全一致的测试辅助函数，用于模拟 VirtIO 队列操作。属于 `virtio-drivers` 上游 crate 的代码。 |
| **第三方库** | `crates/virtio-drivers/src/device/socket/connectionmanager.rs#send_recv` | `AstrancE/crates/virtio-drivers/src/device/socket/connectionmanager.rs#send_recv` | 公共依赖 | 调用图谱 100% 重合，包含相同的硬编码字符串 `"Hello from guest"`。属于 `virtio-drivers` 上游 crate 的示例或测试代码。 |
| **设备驱动** | `arceos/modules/axdriver/src/virtio.rs#probe_pci` | `AstrancE/modules/axdriver/src/virtio.rs#probe_pci` | 公共依赖 | 调用图谱完全一致，均为对 `axdriver_virtio::probe_pci_device` 的泛型封装。属于 ArceOS 上游驱动框架。 |
| **网络协议栈** | `arceos/api/arceos_posix_api/src/imp/net.rs#shutdown` | `AstrancE/api/arceos_posix_api/src/imp/net.rs#shutdown` | 公共依赖 | 调用图谱完全一致，包含对 `tcpsocket`/`udpsocket` 的 `lock`, `peer_addr`, `shutdown` 操作。属于 ArceOS 上游 POSIX API 实现。 |
| **通用模块/其他** | `arceos/ulib/axlibc/c/printf.c#format_string_loop` | `AstrancE/ulib/axlibc/c/printf.c#format_string_loop` | 公共依赖 | 调用图谱完全一致，包含 `ABS_FOR_PRINTING`, `parse_flags` 等内部宏/函数。属于 `axlibc` 上游的 printf 核心实现。 |
| **任务调度/异步运行时** | `arceos/modules/axtask/src/run_queue.rs#switch_to` | `AstrancE/modules/axtask/src/run_queue.rs#switch_to` | 共同上游 | 核心调度逻辑高度一致，包含 `CurrentTask::set_current`, `Arc::downgrade` 等。属于 ArceOS 上游 `axtask` 的任务上下文切换骨架。 |
| **内存管理** | `arceos/modules/axmm/src/backend/alloc.rs#handle_page_fault_alloc` | `AstrancE/modules/axmm/src/backend/alloc.rs#handle_page_fault_alloc` | 共同上游 | 核心缺页处理流程一致，包含 `alloc_frame`, `dealloc_frame` 等。属于 ArceOS 上游 `axmm` 的内存分配后端。 |
| **进程管理** | `arceos/modules/axtask/src/wait_queue.rs#wait_timeout_until` | `AstrancE/modules/axtask/src/wait_queue.rs#wait_timeout_until` | 共同上游/架构差异 | 核心调度骨架一致（`set_alarm_wakeup`, `blocked_resched`），但时间源不同（`monotonic_time` vs `wall_time`），体现了独立的设计选择。 |
| **进程管理** | `pulse_syscalls/src/impls/task/wait.rs#sys_wait4` | `AstrancE/ulib/axsyscall/src/syscall_imp/process.rs#sys_wait4` | 独立实现 | 目标项目直接操作 `child_proc` 对象，参考项目转发至 `axmono`。实现路径和调用图谱完全不同，是独立开发的强证据。 |
| **进程管理** | `pulse_core/src/task/process.rs#requeue` | `AstrancE/modules/axtask/src/wait_queue.rs#requeue` | 独立实现 | 目标项目实现 futex 的地址重排队列，参考项目实现条件变量的等待队列迁移。功能领域和调用图谱完全不同，属跨板块同名。 |

### 4. 核心代码调用流对比剖析
* **【进程管理/任务退出路径】**
* **底层技术析理**: 双方均实现了 `exit_current` 函数，但底层关机原语和资源清理路径存在显著差异。目标项目调用 `axhal::power::system_off` 进行电源管理关机，并引入 `GC_TASK.with_current` 进行垃圾回收；参考项目则调用 `axhal::misc::terminate` 终止系统，并执行 `curr.clone`。这表明双方在硬件抽象层（HAL）和任务生命周期管理上做出了不同的架构选择。
* **代码溯源链接**:
  - 目标项目: `arceos/modules/axtask/src/run_queue.rs#exit_current`
  - 参考项目: `AstrancE/modules/axtask/src/run_queue.rs#exit_current`

* **【进程管理/等待队列超时等待】**
* **底层技术析理**: `wait_timeout_until` 是内核同步的核心操作。双方共享了 ArceOS 的调度骨架（`set_alarm_wakeup`, `blocked_resched`），但在时间基准上做出了根本性区分。目标项目使用 `axhal::time::monotonic_time`（单调时钟），保证超时不受系统时间调整影响，是更严谨的内核设计；参考项目使用 `axhal::time::wall_time`（墙上时钟）。这不是简单的 API 替换，而是对时间语义的不同建模。
* **代码溯源链接**:
  - 目标项目: `arceos/modules/axtask/src/wait_queue.rs#wait_timeout_until`
  - 参考项目: `AstrancE/modules/axtask/src/wait_queue.rs#wait_timeout_until`

* **【系统调用/进程等待】**
* **底层技术析理**: `sys_wait4` 系统调用展示了截然不同的实现哲学。目标项目 (`pulse_syscalls`) 采用“直通”模式，在系统调用入口直接操作 `child_proc` 的内部状态（`exit_code`, `pid`, `release_task_refs`），调用链短且内聚。参考项目 (`AstrancE`) 采用“转发”模式，将调用委托给 `axmono::syscall::process::sys_wait4`，形成了更长的调用链和更复杂的模块间耦合。这证明双方在系统架构和模块划分上存在根本性差异。
* **代码溯源链接**:
  - 目标项目: `pulse_syscalls/src/impls/task/wait.rs#sys_wait4`
  - 参考项目: `AstrancE/ulib/axsyscall/src/syscall_imp/process.rs#sys_wait4`

### 5. 单方模块与降权项
*   **单方模块**:
    *   **目标项目独有**: `pulse_syscalls` 和 `pulse_core` 是目标项目的核心自研模块，构成了其系统调用和任务管理的完整实现，在参考项目中无对应物。这 700+ 个函数是独立开发的最有力证明。
    *   **参考项目独有**: `axmono` 和 `axsyscall` 是参考项目的系统调用转发层，在目标项目中不存在。这体现了双方在系统调用架构上的根本分歧。
*   **降权项**:
    *   **第三方库/公共依赖**: `virtio-drivers`, `smoltcp`, `memory_set`, `axlibc` 等 crate 中的重合指纹，均被认定为对开源生态的共同依赖，不构成抄袭证据。
    *   **POSIX 标准接口**: `sys_getpid`, `sys_exit`, `sys_wait4` 等函数名由标准规定，同名本身无任何证据价值。
    *   **Trivial 实现**: `pthread_cond_wait` (`unimplemented`), `set_in_wait_queue` (单句原子存储) 等因代码量过少或无逻辑，被排除出强证据。
    *   **跨板块同名**: `requeue` 在目标项目中属于进程管理的 futex 操作，在参考项目中属于任务调度的等待队列操作，功能领域完全不同，不构成同板块证据。

### 6. 保守结论
综合评估，目标项目 `[2026-T202610459999616-XX]` 与参考项目 `[2025-AstranciA-哈尔滨工业大学深圳]` 的客观 AST 相似度仅为 14.71%，远低于同源性判定阈值。双方重合的代码指纹经逐项剖析，可明确归因于对 ArceOS 上游操作系统基座、`virtio-drivers` 等第三方开源库以及 POSIX 标准接口的共同依赖。目标项目独有的 6123 个 AST 指纹及其自研的 `pulse_syscalls`/`pulse_core` 模块，展现了与参考项目完全不同的系统调用实现路径和对象管理模型。控方关于“大规模、系统性抄袭”的指控，系将共同上游代码和标准实现误判为抄袭证据，缺乏事实基础。因此，本项目不存在代码抄袭违规，应认定为基于开源生态的独立开发作品。

------------------------------

