# 🛡️ OS 内核赛自动化代码级架构审计报告

**审计时间**: `2026-07-02 04:49:47`
**参赛作品名称**: `2026-T202610459999616-XX`
**年份**: `2026`
**学校**: `XX`
**队伍名称**: `T202610459999616`
**比赛名字**: `OS-Competition`
**比赛子赛道**: `Kernel-Implementation`
**仓库地址**: `https://gitlab.eduxiji.net/educg-group-43501-3132633/T202610459999616-2917`
**推测开发语言**: `Rust / C (由AST探针自动识别)`
**特征函数量**: `7613`

## ⚔️ 底层代码同源性对比 (AST Homology)
> 本部分由专门的代码审查智能体，对该项目与符合年份条件的历届最高相似度项目进行控制流与底层调用对比。新版报告会先给出确定性的全模块覆盖矩阵，再给出 Agent 的分模块解释，避免只围绕少量抽样函数展开。

### 🏆 Top 1 底层对标: vs `2025-SubsToKernel-北京科技大学`（北京科技大学 / SubsToKernel）

#### 对方作品信息

**对方年份**: `2025`
**对方学校**: `北京科技大学`
**对方队伍名称**: `SubsToKernel`
**对方比赛名字**: `OS-Competition`
**对方比赛子赛道**: `Kernel-Implementation`
**对方仓库地址**: `https://gitlab.eduxiji.net/educg-group-36002-2710490/T202510008995695-2720`

> 双向覆盖率：共同 AST 指纹: 1900；唯一 AST 指纹: 目标项目 6123，参考项目 3437；覆盖率: 目标项目 31.03%，参考项目 55.28%

## 全模块对比总览（确定性证据）

> 总体覆盖率：共同 AST 指纹: 1900；唯一 AST 指纹: 目标项目 6123，参考项目 3437；覆盖率: 目标项目 31.03%，参考项目 55.28%

### 1. 模块覆盖矩阵
| 模块 | 目标函数数 | 参考函数数 | 共同唯一 AST 指纹 | Target 覆盖 | Base 覆盖 | 状态 |
| :--- | ---: | ---: | ---: | ---: | ---: | :--- |
| 进程管理 | 321 | 86 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |
| 任务调度/异步运行时 | 497 | 141 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |
| 内存管理 | 454 | 329 | 2 | 0.47% | 0.69% | 双方存在且有结构重合 |
| 中断/异常处理 | 14 | 8 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |
| 系统调用 | 118 | 108 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |
| 文件系统 | 608 | 256 | 4 | 0.78% | 2.14% | 双方存在且有结构重合 |
| 设备驱动 | 111 | 57 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |
| 网络协议栈 | 76 | 124 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |
| 文件系统/第三方库 | 863 | 704 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |
| 网络协议栈/第三方库 | 2635 | 2388 | 1796 | 89.0% | 99.5% | 双方存在且有结构重合 |
| 第三方库 | 1354 | 0 | 0 | 0.0% | 0.0% | 仅目标项目体现 |
| 通用模块/其他 | 562 | 139 | 4 | 0.96% | 3.1% | 双方存在且有结构重合 |

### 2. 分模块证据清单

#### 进程管理
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `pulse_core/src/task/process.rs#futex_wait` | `os/src/task/futex.rs#futex_wait` | 同板块同名 | AxError::from, Err, GLOBAL_FUTEX_TABLE.queue, GLOBAL_FUTEX_TABLE.remove_if_empty, Ok, VirtAddr::from ...(+33) | Arc::downgrade, FUTEX_QUEUE.lock, VecDeque::new, block_current_and_run_next, current_task, current_task().unwrap ...(+9) |
| `pulse_syscalls/src/impls/task/exit.rs#sys_exit_group` | `os/src/syscall/process.rs#sys_exit_group` | 同板块同名 | axlog::debug!, current_thread, panic!, thread.exit_current, thread.process, thread.process().begin_group_exit | exit_current_group_and_run_next, unreachable! |
| `pulse_syscalls/src/impls/task/exit.rs#sys_exit` | `os/src/syscall/process.rs#sys_exit` | 同板块同名 | axlog::debug!, current_thread, panic!, thread.exit_current | current_process, current_process.getpid, debug!, drop, exit_current_and_run_next, panic! |
| `arceos/api/arceos_posix_api/src/imp/task.rs#sys_getpid` | `os/src/syscall/process.rs#sys_getpid` | 同板块同名 | syscall_body! | current_task, current_task().unwrap, current_task().unwrap().process.upgrade, current_task().unwrap().process.upgrade().unwrap, current_task().unwrap().process.upgrade().unwrap().getpid |

#### 任务调度/异步运行时
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `pulse_syscalls/src/impls/net/socket.rs#sys_bind` | `os/src/syscall/net.rs#sys_bind` | 同板块同名 | (addrlen as usize).saturating_sub, Arc::downgrade, Err, LinuxError::EACCES.code, LinuxError::EADDRINUSE.code, LinuxError::EADDRNOTAVAIL.code ...(+54) | (data.len() - offset).min, Ipv4Addr::from, SocketAddr::V4, SocketAddrV4::new, buffer.is_empty, current_process ...(+15) |
| `pulse_syscalls/src/impls/net/socket.rs#sys_connect` | `os/src/syscall/net.rs#sys_connect` | 同板块同名 | (addrlen as usize).saturating_sub, Err, LinuxError::EAFNOSUPPORT.code, LinuxError::ECONNREFUSED.code, LinuxError::EINVAL.code, LinuxError::EISCONN.code ...(+49) | (data.len() - offset).min, Ipv4Addr::from, SocketAddr::V4, SocketAddrV4::new, addr.is_null, buffer.is_empty ...(+19) |
| `pulse_syscalls/src/impls/fs/path.rs#sys_unlinkat` | `os/src/syscall/fs.rs#sys_unlinkat` | 同板块同名 | LinuxError::EACCES.code, LinuxError::EFAULT.code, LinuxError::EINVAL.code, LinuxError::EROFS.code, LinuxError::from, LinuxError::from(e.canonicalize()).code ...(+28) | current_process, debug!, inner.fs_info.has_fd, inner.get_abs_path, inner.get_user_token, of.file ...(+10) |
| `pulse_syscalls/src/impls/fs/path.rs#sys_linkat` | `os/src/syscall/fs.rs#sys_linkat` | 同板块同名 | LinuxError::EEXIST.code, LinuxError::EFAULT.code, LinuxError::EINVAL.code, LinuxError::ENOENT.code, LinuxError::EPERM.code, LinuxError::EROFS.code ...(+24) | unimplemented! |

#### 内存管理
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `pulse_syscalls/src/impls/mm.rs#sys_mmap` | `os/src/syscall/mem.rs#sys_mmap` | 同板块同名 | CachedFile::get_or_create, Err, LinuxError::EACCES.code, LinuxError::EBADF.code, LinuxError::EEXIST.code, LinuxError::EINVAL.code ...(+39) | MapPermission::empty, MmapFlags::from_bits, MmapFlags::from_bits(flags).unwrap, MmapProt::from_bits, MmapProt::from_bits(port).unwrap, Some ...(+16) |
| `pulse_core/src/task/exec.rs#exec` | `os/src/task/process.rs#exec` | 同板块同名 | Arc::new, Ok, RwLock::new, Some, argv.iter, argv.iter().map ...(+51) | Arc::new, Aux::new, FdTable::from_another, KERNEL_SPACE.exclusive_access, KERNEL_SPACE.exclusive_access().token, MemorySet::from_elf ...(+39) |
| `arceos/modules/axfs/src/highlevel/file.rs#write_at` | `os/src/mm/page_table.rs#write_at` | 同板块同名 | Err, Ok, axio::write_fn, cached.write_at, file.len, file.set_len ...(+12) | (*sub_buff).len, buff.len, self.buffers.iter_mut, self.len |
| `pulse_syscalls/src/impls/mm.rs#sys_mprotect` | `os/src/syscall/mem.rs#sys_mprotect` | 同板块同名 | LinuxError::EINVAL.code, LinuxError::ENOMEM.code, MappingFlags::empty, VirtAddr::from, aspace.can_access_range, aspace.protect ...(+6) | MmapProt::from_bits, MmapProt::from_bits(prot).unwrap, MmapProt::from_bits(prot).unwrap().into, VirtAddr::from, VirtAddr::from(addr + len).ceil, VirtAddr::from(addr).floor ...(+6) |

#### 中断/异常处理
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `arceos/ulib/axlibc/c/pow.c#exp_inline` | `os/src/signal/signal.rs#default_op` | 同模块上下文对齐/差异观察 | __math_oflow, __math_uflow, asdouble, asuint64, converttoint, eval_as_double ...(+4) | continue_signals.contains, dump_signals.contains, ignore_signals.contains, stop_signals.contains, terminate_signals.contains |
| `pulse_core/src/fd_table.rs#wait_ready` | `os/src/hal/trap/mod.rs#trap_handler_kernel` | 同模块上下文对齐/差异观察 | Err, Ok, Self::current_has_pending_signal, axhal::time::monotonic_time, self.ready_for, self.writable ...(+2) | crmd.plv, crmd::read, era::read, estat.cause, estat::read, panic! ...(+1) |
| `pulse_core/src/net/mod.rs#write` | `os/src/hal/trap/mod.rs#enable_timer_interrupt` | 同模块上下文对齐/差异观察 | Err, Ok, buf.is_empty, buf.len, drop, ring.available_write ...(+7) | crmd::set_ie, ecfg::set_lie, get_timer_freq, sie::set_stimer, tcfg::set_en, tcfg::set_init_val ...(+2) |
| `pulse_syscalls/src/impls/time.rs#sys_nanosleep` | `os/src/hal/trap/mod.rs#trap_from_kernel` | 同模块上下文对齐/差异观察 | LinuxError::EFAULT.code, LinuxError::EINTR.code, axlog::trace!, axlog::warn!, duration_to_timespec, e.code ...(+5) | era::read, estat::read, panic!, warn! |

#### 系统调用
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `pulse_syscalls/src/impls/net/name.rs#sys_getpeername` | `os/src/syscall/net.rs#sys_getpeername` | 同板块同名 | 16u16.to_ne_bytes, LinuxError::EFAULT.code, NetSocketAddr::from, Some, core::cmp::min, crate::impls::utils::write_user_bytes ...(+14) | unimplemented! |
| `pulse_syscalls/src/impls/net/socket.rs#sys_accept4` | `os/src/syscall/net.rs#sys_accept4` | 同板块同名 | FdFlags::empty, LinuxError::EINVAL.code, LinuxError::EOPNOTSUPP.code, LinuxError::from, NetSocketAddr::from, Socket::new ...(+24) | unimplemented! |
| `pulse_syscalls/src/impls/net/io.rs#sys_sendmsg` | `os/src/syscall/net.rs#sys_sendmsg` | 同板块同名 | debug!, do_sendmsg_core, e.code, get_socket, self.socket.set_nonblocking_inner, socket.is_nonblocking ...(+1) | unimplemented! |
| `arceos/ulib/axlibc/src/net.rs#sendto` | `os/src/net/socket_impl.rs#sendto` | 同板块同名 | e, socket_addr.is_null, sys_send, sys_sendto | Err, Ok, SysErrNo::EISCONN.into, udpsocket.lock, udpsocket.lock().send_to |

#### 文件系统
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `arceos/modules/axfs/src/fs/tmpfs.rs#rename` | `os/src/fs/vfs/mod.rs#rename` | 同板块同名 | "..".into, Err, InodeRef::new, Ok, dir_content.entries.lock, dst_dir.downcast::<Self> ...(+29) | unimplemented! |
| `arceos/modules/axfs/src/fs/devfs.rs#read_at` | `os/src/fs/vfs/mod.rs#read_at` | 同板块同名 | Err, Ok, TTY_CALLBACKS.get, axhal::console::read_bytes, block.disk.lock, buf.fill ...(+16) | unimplemented! |
| `arceos/modules/axfs/src/fs/devfs.rs#write_at` | `os/src/fs/vfs/mod.rs#write_at` | 同板块同名 | Err, Ok, TTY_CALLBACKS.get, axhal::console::write_bytes, block.disk.lock, buf.len ...(+10) | unimplemented! |
| `arceos/modules/axfs/src/fs/devfs.rs#create` | `os/src/fs/vfs/mod.rs#create` | 同板块同名 | Err, drop, entries.contains_key, entries.insert, inode_as_dir, name.into ...(+5) | unimplemented! |

#### 设备驱动
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `pulse_core/src/net/mod.rs#is_nonblocking` | `os/src/net/socket/tcp.rs#is_nonblocking` | 同板块同名 | s.is_nonblocking, s.nonblocking.load | self.nonblock.load |
| `arceos/modules/axdriver/src/structs/mod.rs#device_name` | `os/src/drivers/virtio/blk.rs#device_name` | 同板块同名 | dev.device_name, unreachable! | 内部闭环逻辑 |
| `arceos/modules/axdriver/src/structs/mod.rs#device_type` | `os/src/drivers/virtio/blk.rs#device_type` | 同板块同名 | unreachable! | 内部闭环逻辑 |
| `arceos/api/arceos_posix_api/src/imp/net.rs#set_nonblocking` | `os/src/net/socket_impl.rs#set_nonblocking` | 同板块同名 | Ok, tcpsocket.lock, tcpsocket.lock().set_nonblocking, udpsocket.lock, udpsocket.lock().set_nonblocking | Ok, tcpsocket.lock, tcpsocket.lock().set_nonblocking, udpsocket.lock, udpsocket.lock().set_nonblocking |

#### 网络协议栈
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `arceos/api/arceos_posix_api/src/imp/net.rs#shutdown` | `os/src/net/socket/tcp.rs#shutdown` | 同板块同名 | Ok, tcpsocket.lock, tcpsocket.peer_addr, tcpsocket.shutdown, udpsocket.lock, udpsocket.peer_addr ...(+1) | LISTEN_TABLE.unlisten, Ok, SOCKET_SET.poll_interfaces, SOCKET_SET.with_socket_mut::<tcp::Socket, _, _>, debug!, self.handle.get ...(+9) |
| `arceos/api/arceos_posix_api/src/imp/net.rs#recvfrom` | `os/src/net/socket_impl.rs#recvfrom` | 同板块同名 | Ok, Some, tcpsocket.lock, tcpsocket.lock().recv, tcpsocket.lock().recv(buf).map, udpsocket
                .lock ...(+2) | Err, Ok, Some, crate::net::AxError::from, tcpsocket.lock, tcpsocket.lock().recv ...(+2) |
| `arceos/ulib/axstd/src/net/tcp.rs#bind` | `os/src/net/socket/udp.rs#bind` | 同板块同名 | Ok, TcpListener, api::ax_tcp_bind, api::ax_tcp_listen, api::ax_tcp_socket, super::each_addr | (!is_unspecified(local_endpoint.addr)).then_some, Ok, SOCKET_SET.bind_check, SOCKET_SET.with_socket_mut::<udp::Socket, _, _>, Some, ax_err! ...(+11) |
| `arceos/ulib/axstd/src/net/tcp.rs#connect` | `os/src/net/socket_impl.rs#connect` | 同板块同名 | Ok, TcpStream, api::ax_tcp_connect, api::ax_tcp_socket, super::each_addr | Err, Ok, SysErrNo::EOPNOTSUPP.into, crate::net::AxError::from, debug!, tcpsocket.lock ...(+1) |

#### 文件系统/第三方库
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `crates/ext4plus/src/file_blocks/extent_tree.rs#write_at` | `os/src/fs/ext4_lw/inode.rs#write_at` | 同板块同名 | 1usize
                    .checked_add, 1usize
                    .checked_add(remaining_after_first.div_ceil(block_size))
                    .ok_or, CorruptKind::ExtentBlock, CorruptKind::ExtentBlock(inode.index).into, CorruptKind::InvalidBlockSize.into, Err ...(+102) | SysErrNo::from, file.file_open, file.file_open(path, O_RDWR)
            .map_err, file.file_seek, file.file_seek(off as i64, SEEK_SET)
            .map_err, file.file_write ...(+5) |
| `arceos/modules/axfs/src/fs/ext4/inode.rs#rename` | `os/libs/lwext4_rsut/examples/src/ext4fs.rs#rename` | 同板块同名 | Arc::as_ptr, Err, Ok, active.get_mut, active.remove, core::num::NonZeroU32::new ...(+42) | debug!, file.file_rename, info!, self.0.borrow_mut |
| `crates/ext4plus/src/file_blocks/block_map.rs#write_at` | `os/libs/lwext4_rsut/examples/src/ext4fs.rs#write_at` | 同板块同名 | Ok, add_to_file_offset, block_size.to_u64, block_size.to_usize, block_size_usize
            .checked_sub, block_size_usize
            .checked_sub(offset_in_block)
            .ok_or ...(+27) | file.file_close, file.file_open, file.file_seek, file.file_write, file.path, path.to_str ...(+2) |
| `arceos/modules/axfs/src/fs/ext4/inode.rs#create` | `os/src/fs/ext4_lw/inode.rs#create` | 同板块同名 | (|| -> VfsResult<DirEntry> {
            let dir_idx = core::num::NonZeroU32::new(self.ino).ok_or(VfsError::InvalidData)?;
            let parent_inode = ext4plus::inode::Inode::read(&fs, dir_idx).map_err(into_vfs_err)?;
            let mut dir = ext4plus::dir::Dir::open_inode(&fs, parent_inode).map_err(into_vfs_err)?;

            if node_type == NodeType::Directory {
                let new_dir = ext4plus::dir::Dir::init(fs.clone(), new_inode, dir_idx).map_err(into_vfs_err)?;
                new_inode = new_dir.inode().clone();
            }

            let name_ref = ext4plus::DirEntryName::try_from(name).map_err(|_| VfsError::InvalidInput)?;
            dir.link(name_ref, &mut new_inode).map_err(into_vfs_err)?;

            self.invalidate_snapshot(self.ino);
            Ok(self.create_entry(
                new_inode.index.get(),
                node_type,
                node_type == NodeType::Directory,
                name,
            ))
        }), Err, Ok, axhal::time::wall_time, core::num::NonZeroU32::new, core::num::NonZeroU32::new(self.ino).ok_or ...(+32) | Arc::new, Err, Ext4Inode::new, Ok, Some, SysErrNo::from ...(+11) |

#### 网络协议栈/第三方库
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `crates/smoltcp/src/socket/tcp.rs#process` | `os/libs/smoltcp/src/socket/tcp.rs#process` | 同板块同名同结构 | AckDelayTimer::Waiting, IpEndpoint::new, Self::random_seq_no, Self::rst_reply, Some, control.quash_psh ...(+42) | AckDelayTimer::Waiting, IpEndpoint::new, Self::random_seq_no, Self::rst_reply, Some, control.quash_psh ...(+42) |
| `crates/smoltcp/src/socket/tcp.rs#dispatch` | `os/libs/smoltcp/src/socket/tcp.rs#dispatch` | 同板块同名同结构 | IpRepr::new, Ok, Some, cx.ip_mtu, cx.now, emit ...(+45) | IpRepr::new, Ok, Some, cx.ip_mtu, cx.now, emit ...(+45) |
| `crates/smoltcp/src/iface/interface/mod.rs#dispatch_ip` | `os/libs/smoltcp/src/iface/interface/mod.rs#dispatch_ip` | 同板块同名同结构 | EthernetAddress, EthernetFrame::<&[u8]>::buffer_len, EthernetFrame::<&[u8]>::header_len, EthernetFrame::new_unchecked, Ipv4Packet::new_unchecked, Ok ...(+35) | EthernetAddress, EthernetFrame::<&[u8]>::buffer_len, EthernetFrame::<&[u8]>::header_len, EthernetFrame::new_unchecked, Ipv4Packet::new_unchecked, Ok ...(+35) |
| `crates/smoltcp/src/socket/dhcpv4.rs#dispatch` | `os/libs/smoltcp/src/socket/dhcpv4.rs#dispatch` | 同板块同名同结构 | Ok, Self::random_transaction_id, Some, cx.hardware_addr, cx.ip_mtu, cx.now ...(+12) | Ok, Self::random_transaction_id, Some, cx.hardware_addr, cx.ip_mtu, cx.now ...(+12) |

#### 第三方库
仅目标项目中观察到该模块代表函数，参考项目没有同模块可对齐证据：
- 目标项目：`crates/virtio-drivers/src/queue.rs#fake_read_write_queue`，调用摘要：(*used_ring).idx.fetch_add, (*used_ring).idx.load, Vec::new, assert!, assert_eq!, assert_ne! ...(+18)
- 目标项目：`crates/axio/src/read/mod.rs#default_read_to_end`，调用摘要：Err, Ok, Some, buf.capacity, buf.extend_from_slice, buf.len ...(+24)
- 目标项目：`crates/allocator/src/bitmap.rs#test_bitmap_page_allocator_size_2g`，调用摘要：BitmapPageAllocator::<PAGE_SIZE>::new, allocator
                .alloc_pages_at, allocator
                .alloc_pages_at(test_addr_base, num_pages, align)
                .unwrap, allocator.alloc_pages, allocator.alloc_pages(num_pages, PAGE_SIZE).unwrap, allocator.alloc_pages(num_pages, align).unwrap ...(+5)
- 目标项目：`crates/page_table_multiarch/src/bits64.rs#copy_cow_range_recursive`，调用摘要：GenericPTE::new_page, GenericPTE::new_table, H::phys_to_virt, H::phys_to_virt(dst_table_paddr).as_mut_ptr, H::phys_to_virt(src_table_paddr).as_mut_ptr, Ok ...(+19)

#### 通用模块/其他
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `arceos/ulib/axlibc/c/log.c#log` | `os/src/logging.rs#log` | 同板块同名 | __builtin_fma, __math_divzero, __math_invalid, asdouble, asuint64, eval_as_double ...(+2) | println!, record.level, record.metadata, self.enabled |
| `arceos/modules/axsync/src/mutex.rs#lock` | `os/src/sync/mutex.rs#lock` | 同板块同名 | assert_ne!, current, current().id, current().id().as_u64, self.is_locked, self.owner_id.compare_exchange_weak ...(+1) | drop, self.locked.exclusive_access, suspend_current_and_run_next |
| `arceos/modules/axsync/src/mutex.rs#unlock` | `os/src/sync/mutex.rs#unlock` | 同板块同名 | assert_eq!, self.owner_id.swap, self.wq.notify_one | self.locked.exclusive_access |
| `arceos/modules/axlog/src/lib.rs#write_str` | `user/src/console.rs#write_str` | 同板块同名 | Ok, cfg_if::cfg_if! | Err, Ok, s.as_bytes, s.as_bytes().iter, self.0.len, self.0.push_back ...(+1) |


好的，我将遵循审计指令，基于客观 AST 证据与双方论据，进行最终的定性仲裁。

### 1. 宏观判定
- **定性结论**: **独立开发 (相似度: 24.8%，共同 AST 指纹: 1900；唯一 AST 指纹: 目标项目 6123，参考项目 3437；覆盖率: 目标项目 31.03%，参考项目 55.28%)**

整体相似度处于低位，且高覆盖率完全由第三方公共库贡献。在操作系统核心板块（进程管理、任务调度、内存管理等），双方未形成有效的结构性同源证据。控方基于函数名和语义共性的指控，在零共同 AST 指纹的客观事实面前无法成立。

### 2. 全模块覆盖矩阵解读

*   **进程管理**
    *   **双方存在性**: 是。
    *   **共同 AST 指纹**: 0。
    *   **覆盖率解读**: 目标项目 0.0%，参考项目 0.0%。表明双方在进程管理的具体实现上，从数据结构到控制流均无 AST 级别的结构重合。
    *   **证据强度**: **未形成**。控方所有指控均基于 POSIX 标准接口名或通用概念名，缺乏底层代码结构支撑。

*   **任务调度/异步运行时**
    *   **双方存在性**: 是。
    *   **共同 AST 指纹**: 0。
    *   **覆盖率解读**: 目标项目 0.0%，参考项目 0.0%。表明双方的调度器、异步运行时等核心机制在 AST 层面是完全异构的。
    *   **证据强度**: **未形成**。无任何共同结构指纹。

*   **内存管理**
    *   **双方存在性**: 是。
    *   **共同 AST 指纹**: 2。
    *   **覆盖率解读**: 目标项目 0.47%，参考项目 0.69%。覆盖率极低，表明双方在内存管理的核心算法与数据结构上基本独立。
    *   **证据强度**: **弱**。仅有的共同指纹可能源于对相同硬件特性（如页表）的操作，而非核心逻辑复用。

*   **中断/异常处理**
    *   **双方存在性**: 是。
    *   **共同 AST 指纹**: 0。
    *   **覆盖率解读**: 目标项目 0.0%，参考项目 0.0%。双方在 Trap 处理、中断向量表等底层机制上无结构重合。
    *   **证据强度**: **未形成**。

*   **系统调用**
    *   **双方存在性**: 是。
    *   **共同 AST 指纹**: 0。
    *   **覆盖率解读**: 目标项目 0.0%，参考项目 0.0%。尽管存在大量同名系统调用，但内部实现路径无 AST 重合。
    *   **证据强度**: **未形成**。同名源于 Linux ABI 标准，非代码复用。

*   **文件系统**
    *   **双方存在性**: 是。
    *   **共同 AST 指纹**: 4。
    *   **覆盖率解读**: 目标项目 0.78%，参考项目 2.14%。覆盖率极低，表明双方在 VFS、具体文件系统实现上基本独立。
    *   **证据强度**: **弱**。共同指纹可能源于对通用文件系统概念（如 inode, rename）的实现。

*   **设备驱动**
    *   **双方存在性**: 是。
    *   **共同 AST 指纹**: 0。
    *   **覆盖率解读**: 目标项目 0.0%，参考项目 0.0%。双方驱动模型和具体驱动实现无结构重合。
    *   **证据强度**: **未形成**。

*   **网络协议栈**
    *   **双方存在性**: 是。
    *   **共同 AST 指纹**: 0。
    *   **覆盖率解读**: 目标项目 0.0%，参考项目 0.0%。双方在自研网络协议栈部分无 AST 重合。
    *   **证据强度**: **未形成**。

*   **第三方库/公共依赖**
    *   **双方存在性**: 是（`网络协议栈/第三方库` 模块）。
    *   **共同 AST 指纹**: 1796。
    *   **覆盖率解读**: 目标项目 89.0%，参考项目 99.5%。覆盖率极高，表明双方均大量集成了相同的第三方开源库（如 `smoltcp`）。
    *   **证据强度**: **强（但属公共依赖）**。这是整体相似度的主要来源，但属于对公共开源代码的合规使用，不能作为抄袭证据。目标项目独有的 `第三方库` 模块（1354个函数）进一步证明其独立集成与开发能力。

### 3. 底层 AST 关键证据指纹表

| 所属板块 | 目标项目路径 | 参考项目路径 | 证据类型 | 技术说明 |
| :--- | :--- | :--- | :--- | :--- |
| **第三方库/公共依赖** | `crates/smoltcp/src/socket/tcp.rs#process` | `os/libs/smoltcp/src/socket/tcp.rs#process` | 同源（公共库） | 双方均使用相同的第三方网络栈 `smoltcp`，AST 指纹高度一致，是整体相似度的主要来源。 |
| **第三方库/公共依赖** | `crates/smoltcp/src/iface/interface/mod.rs#dispatch_ip` | `os/libs/smoltcp/src/iface/interface/mod.rs#dispatch_ip` | 同源（公共库） | 同上，属于 `smoltcp` 库的核心 IP 分发逻辑。 |
| 内存管理 | `pulse_syscalls/src/impls/mm.rs#sys_mmap` | `os/src/syscall/mem.rs#sys_mmap` | 弱信号 | 同名系统调用，但目标项目使用 `CachedFile` 等复杂组件，参考项目使用 `MmapFlags` 等简单位标志，实现路径差异巨大。 |
| 文件系统 | `arceos/modules/axfs/src/fs/tmpfs.rs#rename` | `os/src/fs/vfs/mod.rs#rename` | 弱信号 | 同名函数，但目标项目有完整的 `tmpfs` 实现，参考项目则标记为 `unimplemented!`，无实际逻辑可比性。 |

### 4. 核心代码调用流对比剖析

*   **【进程管理 / futex 等待机制】**
    *   **底层技术析理**: 两者均实现了 `futex_wait`，但抽象层次与对象模型完全不同。目标项目采用全局 Futex 表 (`GLOBAL_FUTEX_TABLE`) 管理等待队列，并与进程地址空间 (`aspace`) 紧密集成，通过 `query_vaddr` 解析虚拟地址。参考项目则直接使用一个全局锁 (`FUTEX_QUEUE.lock`) 和任务阻塞原语 (`block_current_and_run_next`)，模型更简单直接。两者在数据结构设计、同步策略和地址空间交互上存在根本性架构差异。
    *   **代码溯源链接**:
        - 目标项目: `pulse_core/src/task/process.rs#futex_wait`
        - 参考项目: `os/src/task/futex.rs#futex_wait`

*   **【进程管理 / 退出系统调用】**
    *   **底层技术析理**: `sys_exit` 是 POSIX 标准调用，其语义顺序（获取当前上下文 -> 执行退出 -> 不可达兜底）是功能必然。目标项目通过 `current_thread` 获取线程上下文后调用 `thread.exit_current`，而参考项目通过 `current_process` 获取进程上下文后调用 `exit_current_and_run_next`。两者在获取上下文的对象模型（线程 vs 进程）和退出原语的实现上完全不同，`panic!` 作为不可达代码的防御性编程是 Rust 的惯用法，非特定项目特征。
    *   **代码溯源链接**:
        - 目标项目: `pulse_syscalls/src/impls/task/exit.rs#sys_exit`
        - 参考项目: `os/src/syscall/process.rs#sys_exit`

*   **【内存管理 / 内存映射】**
    *   **底层技术析理**: `sys_mmap` 是复杂系统调用，双方实现路径迥异。目标项目引入了 `CachedFile::get_or_create` 等缓存机制，并与 `LinuxError` 错误码体系深度绑定。参考项目则基于 `MmapFlags`、`MmapProt` 等位标志进行参数解析，错误处理更简单。两者在内存映射的生命周期管理、文件关联方式和错误处理模型上存在显著架构差异。
    *   **代码溯源链接**:
        - 目标项目: `pulse_syscalls/src/impls/mm.rs#sys_mmap`
        - 参考项目: `os/src/syscall/mem.rs#sys_mmap`

### 5. 单方模块与降权项

*   **目标项目独有模块**: `第三方库` (1354 个函数)。该模块包含 `virtio-drivers`、`axio`、`allocator` 等独立或深度定制的第三方库，在参考项目中无对应项。这体现了目标项目在驱动、I/O 和分配器方面的独立集成与开发工作，是独立开发的正面证据。
*   **降权项说明**:
    *   **POSIX 标准接口同名**: `sys_exit`, `sys_getpid`, `sys_mmap` 等函数名是 Linux ABI 的强制要求，任何兼容内核都必须使用。同名是标准符合性表现，不能作为代码复用的证据。
    *   **通用概念名**: `current_process`, `pid`, `thread_count`, `wait` 等是操作系统领域的通用概念，其命名具有高度趋同性，不能作为抄袭指控的依据。
    *   **跨板块同名**: 如目标项目的 `wait` 属于 `axtask::wait_queue` 模块，参考项目的 `wait` 属于 `sync::condvar` 模块，两者分属不同的同步原语子系统，控方将其硬凑为同源证据，违反了审计原则，已予降权。

### 6. 保守结论

综合审计，目标项目与参考项目的整体 AST 相似度为 24.8%，处于低位。该相似度主要由双方共同使用的第三方开源网络协议栈（`smoltcp`）贡献，属于对公共代码的合规复用，不构成抄袭。在操作系统核心板块（进程管理、内存管理、任务调度等），双方的共同 AST 指纹为零或极低，表明在架构设计、数据结构和核心算法上存在根本性差异。控方基于 POSIX 标准接口名和通用概念名提出的复用指控，缺乏底层 AST 结构证据支持，无法成立。因此，目标项目在核心原创性方面风险极低，应认定为独立开发作品。

------------------------------

