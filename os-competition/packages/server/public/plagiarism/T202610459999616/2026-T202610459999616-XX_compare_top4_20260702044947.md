# 🛡️ OS 内核赛自动化代码级架构审计报告

**审计时间**: `2026-07-02 04:49:47`
**参赛作品名称**: `2026-T202610459999616-XX`
**年份**: `2026`
**学校**: `XX`
**队伍名称**: `T202610459999616`
**比赛名字**: `OS-Competition`
**比赛子赛道**: `Kernel-Implementation`
**仓库地址**: `https://gitlab.eduxiji.net/educg-group-43501-3132633/T202610459999616-2917`
**推测开发语言**: `Rust / C (由AST探针自动识别)`
**特征函数量**: `7613`

## ⚔️ 底层代码同源性对比 (AST Homology)
> 本部分由专门的代码审查智能体，对该项目与符合年份条件的历届最高相似度项目进行控制流与底层调用对比。新版报告会先给出确定性的全模块覆盖矩阵，再给出 Agent 的分模块解释，避免只围绕少量抽样函数展开。

### 🏆 Top 4 底层对标: vs `2025-狗剩-海南大学`（海南大学 / 狗剩）

#### 对方作品信息

**对方年份**: `2025`
**对方学校**: `海南大学`
**对方队伍名称**: `狗剩`
**对方比赛名字**: `OS-Competition`
**对方比赛子赛道**: `Kernel-Implementation`
**对方仓库地址**: `https://gitlab.eduxiji.net/educg-group-36002-2710490/T202510589995148-2209`

> 双向覆盖率：共同 AST 指纹: 1123；唯一 AST 指纹: 目标项目 6123，参考项目 3020；覆盖率: 目标项目 18.34%，参考项目 37.19%

## 全模块对比总览（确定性证据）

> 总体覆盖率：共同 AST 指纹: 1123；唯一 AST 指纹: 目标项目 6123，参考项目 3020；覆盖率: 目标项目 18.34%，参考项目 37.19%

### 1. 模块覆盖矩阵
| 模块 | 目标函数数 | 参考函数数 | 共同唯一 AST 指纹 | Target 覆盖 | Base 覆盖 | 状态 |
| :--- | ---: | ---: | ---: | ---: | ---: | :--- |
| 进程管理 | 321 | 93 | 25 | 8.9% | 29.76% | 双方存在且有结构重合 |
| 任务调度/异步运行时 | 497 | 399 | 110 | 25.11% | 38.19% | 双方存在且有结构重合 |
| 内存管理 | 454 | 326 | 95 | 22.51% | 32.65% | 双方存在且有结构重合 |
| 中断/异常处理 | 14 | 62 | 2 | 18.18% | 4.08% | 双方存在且有结构重合 |
| 系统调用 | 118 | 153 | 56 | 54.37% | 43.41% | 双方存在且有结构重合 |
| 文件系统 | 608 | 747 | 186 | 36.05% | 32.86% | 双方存在且有结构重合 |
| 设备驱动 | 111 | 142 | 88 | 88.89% | 71.54% | 双方存在且有结构重合 |
| 网络协议栈 | 76 | 77 | 51 | 78.46% | 96.23% | 双方存在且有结构重合 |
| 文件系统/第三方库 | 863 | 689 | 11 | 1.42% | 1.87% | 双方存在且有结构重合 |
| 网络协议栈/第三方库 | 2635 | 107 | 67 | 3.32% | 67.0% | 双方存在且有结构重合 |
| 第三方库 | 1354 | 386 | 96 | 8.47% | 29.36% | 双方存在且有结构重合 |
| 通用模块/其他 | 562 | 665 | 278 | 66.99% | 56.62% | 双方存在且有结构重合 |

### 2. 分模块证据清单

#### 进程管理
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `pulse_syscalls/src/impls/misc.rs#sys_rt_sigtimedwait` | `api/src/imp/signal.rs#sys_rt_sigtimedwait` | 同板块同名 | (axhal::time::monotonic_time_nanos() as u64).saturating_add, Duration::from_nanos, LinuxError::EAGAIN.code, LinuxError::EFAULT.code, LinuxError::EINTR.code, LinuxError::EINVAL.code ...(+21) | Err, Ok, TimeValue::new, current, current()
        .task_ext, current()
        .task_ext()
        .thread
        .signal
        .wait_timeout ...(+4) |
| `pulse_syscalls/src/impls/task/wait.rs#sys_wait4` | `src/syscall_imp/task/thread.rs#sys_wait4` | 同板块同名 | LinuxError::ECHILD.code, LinuxError::ESRCH.code, axhal::time::monotonic_time_nanos, axlog::debug!, axlog::warn!, child_proc.exit_code ...(+17) | Err, Ok, Some, WaitFlags::from_bits, WaitFlags::from_bits(option).unwrap_or, child.exit_code ...(+22) |
| `arceos/modules/axfs/src/fs/procfs.rs#write_at` | `core/src/vfs/proc.rs#write_at` | 同板块同名 | Err, Ok, PID_MAX.store, buf.len, core::str::from_utf8, data.len ...(+16) | Err |
| `arceos/modules/axtask/src/run_queue.rs#exit_current` | `arceos/modules/axtask/src/run_queue.rs#exit_current` | 同板块同名 | EXITED_TASKS.current_ref_mut_raw, EXITED_TASKS.current_ref_mut_raw().clear, EXITED_TASKS.current_ref_mut_raw().push_back, GC_TASK.with_current, WAIT_FOR_EXIT.current_ref_mut_raw, WAIT_FOR_EXIT.current_ref_mut_raw().notify_one ...(+15) | EXITED_TASKS.current_ref_mut_raw, EXITED_TASKS.current_ref_mut_raw().clear, EXITED_TASKS.current_ref_mut_raw().push_back, WAIT_FOR_EXIT.current_ref_mut_raw, WAIT_FOR_EXIT.current_ref_mut_raw().notify_one, assert! ...(+8) |

#### 任务调度/异步运行时
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `arceos/ulib/axlibc/c/printf.c#format_string_loop` | `arceos/ulib/axlibc/c/printf.c#format_string_loop` | 同板块同名同结构 | ABS_FOR_PRINTING, ADVANCE_IN_FORMAT_STRING, atou_, is_digit_, out_rev_, parse_flags ...(+5) | ABS_FOR_PRINTING, ADVANCE_IN_FORMAT_STRING, atou_, is_digit_, out_rev_, parse_flags ...(+5) |
| `arceos/modules/axlog/src/lib.rs#log` | `arceos/modules/axlog/src/lib.rs#log` | 同板块同名同结构 | cfg_if::cfg_if!, record.level, record.line, record.line().unwrap_or, record.metadata, record.target ...(+1) | cfg_if::cfg_if!, record.level, record.line, record.line().unwrap_or, record.metadata, record.target ...(+1) |
| `pulse_syscalls/src/impls/futex.rs#sys_futex` | `api/src/imp/futex.rs#sys_futex` | 同板块同名 | LinuxError::EAGAIN.code, LinuxError::EFAULT.code, LinuxError::EINVAL.code, LinuxError::ENOSYS.code, LinuxError::ETIMEDOUT.code, axlog::debug! ...(+11) | Err, Ok, TimeValue::new, axtask::yield_now, futex_table.get, futex_table.get_or_insert ...(+14) |
| `arceos/modules/axtask/src/run_queue.rs#switch_to` | `arceos/modules/axtask/src/run_queue.rs#switch_to` | 同板块同名 | (*prev_ctx_ptr).switch_to, Arc::downgrade, CurrentTask::set_current, PREV_TASK.current_ref_mut_raw, assert!, clear_prev_task_on_cpu ...(+9) | (*prev_ctx_ptr).switch_to, Arc::downgrade, CurrentTask::set_current, PREV_TASK.current_ref_mut_raw, assert!, clear_prev_task_on_cpu ...(+11) |

#### 内存管理
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `pulse_syscalls/src/impls/mm.rs#sys_mmap` | `src/syscall_imp/mm/mmap.rs#sys_mmap` | 同板块同名 | CachedFile::get_or_create, Err, LinuxError::EACCES.code, LinuxError::EBADF.code, LinuxError::EEXIST.code, LinuxError::EINVAL.code ...(+39) | (addr + length).align_up, Err, MmapFlags::MAP_PRIVATE.bits, MmapFlags::MAP_SHARED.bits, MmapFlags::MAP_SHARED_VALIDATE.bits, MmapFlags::from_bits_truncate ...(+32) |
| `arceos/modules/axmm/src/backend/alloc.rs#handle_page_fault_alloc` | `arceos/modules/axmm/src/backend/alloc.rs#handle_page_fault_alloc` | 同板块同名 | alloc_contiguous_frames, alloc_frame, curr_flags.is_empty, curr_frame.as_usize, dealloc_frame, debug! ...(+26) | alloc_frame, pt.map, pt.map(vaddr, frame, align, orig_flags)
                .map, pt.map(vaddr, frame, align, orig_flags)
                .map(|tlb| tlb.flush())
                .is_ok, tlb.flush |
| `arceos/modules/axfs/src/fs/procfs.rs#read_dir` | `arceos/modules/axfs-ng/src/fs/fat/dir.rs#read_dir` | 同板块同名 | ".".to_owned, "..".to_owned, "cgroup".to_owned, "children".to_owned, "cmdline".to_owned, "comm".to_owned ...(+43) | Ok, dir.iter, dir.iter().skip, dir_node.insert_cache, dir_node.lookup_cache, entry.file_name ...(+13) |
| `arceos/modules/axfs/src/fs/procfs.rs#lookup` | `arceos/modules/axfs-ng/src/fs/fat/dir.rs#lookup` | 同板块同名 | Err, PROCESS_PROVIDER.get, PROCESS_PROVIDER.get().ok_or, Some, decode_pid_inode, entries.get ...(+11) | dir.iter, dir.iter()
            .find_map, dir.iter()
            .find_map(|entry| entry.ok().filter(|it| it.eq_name(name)))
            .map, dir.iter()
            .find_map(|entry| entry.ok().filter(|it| it.eq_name(name)))
            .map(|entry| self.create_entry(entry, name.to_ascii_lowercase(), fs.alloc_inode()))
            .ok_or, entry.ok, entry.ok().filter ...(+6) |

#### 中断/异常处理
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `arceos/ulib/axlibc/c/pow.c#exp_inline` | `arceos/ulib/axlibc/c/pow.c#exp_inline` | 同板块同名同结构 | __math_oflow, __math_uflow, asdouble, asuint64, converttoint, eval_as_double ...(+4) | __math_oflow, __math_uflow, asdouble, asuint64, converttoint, eval_as_double ...(+4) |
| `arceos/modules/axsync/src/mutex.rs#may_interrupt` | `arceos/modules/axsync/src/mutex.rs#may_interrupt` | 同板块同名同结构 | rand::random::<u32>, thread::yield_now | rand::random::<u32>, thread::yield_now |
| `pulse_core/src/fd_table.rs#wait_ready` | `arceos/modules/axhal/src/arch/aarch64/trap.rs#handle_sync_exception` | 同模块上下文对齐/差异观察 | Err, Ok, Self::current_has_pending_signal, axhal::time::monotonic_time, self.ready_for, self.writable ...(+2) | ESR_EL1.extract, crate::trap::handle_syscall, crate::trap::post_trap_callback, debug!, esr.read, esr.read_as_enum ...(+6) |
| `pulse_core/src/net/mod.rs#write` | `arceos/modules/axhal/src/arch/x86_64/idt.rs#new` | 同模块上下文对齐/差异观察 | Err, Ok, buf.is_empty, buf.len, drop, ring.available_write ...(+7) | InterruptDescriptorTable::new, core::mem::transmute, core::slice::from_raw_parts_mut, entries[i].set_handler_fn, opt.set_privilege_level |

#### 系统调用
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `pulse_syscalls/src/impls/net/socket.rs#sys_socket` | `arceos/api/arceos_posix_api/src/imp/net.rs#sys_socket` | 同板块同名 | FdFlags::empty, LinuxError::EAFNOSUPPORT.code, LinuxError::EINVAL.code, LinuxError::EPROTONOSUPPORT.code, LinuxError::ESOCKTNOSUPPORT.code, Socket::new ...(+14) | debug!, syscall_body! |
| `pulse_syscalls/src/impls/net/name.rs#sys_getpeername` | `arceos/api/arceos_posix_api/src/imp/net.rs#sys_getpeername` | 同板块同名 | 16u16.to_ne_bytes, LinuxError::EFAULT.code, NetSocketAddr::from, Some, core::cmp::min, crate::impls::utils::write_user_bytes ...(+14) | debug!, syscall_body! |
| `arceos/api/arceos_posix_api/src/imp/net.rs#sys_getaddrinfo` | `arceos/api/arceos_posix_api/src/imp/net.rs#sys_getaddrinfo` | 同板块同名同结构 | char_ptr_to_str, debug!, syscall_body! | char_ptr_to_str, debug!, syscall_body! |
| `pulse_syscalls/src/impls/net/socket.rs#sys_shutdown` | `arceos/api/arceos_posix_api/src/imp/net.rs#sys_shutdown` | 同板块同名 | Err, LinuxError::EINVAL.code, LinuxError::from, Ok, debug!, e.canonicalize ...(+9) | debug!, syscall_body! |

#### 文件系统
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `arceos/modules/axfs/src/fs/tmpfs.rs#rename` | `core/src/vfs/tmp.rs#rename` | 同板块同名 | "..".into, Err, InodeRef::new, Ok, dir_content.entries.lock, dst_dir.downcast::<Self> ...(+29) | Ok, dst_dir.downcast::<Self>, dst_dir.lookup, dst_name.into, dst_node
            .inode
            .as_dir, dst_node
            .inode
            .as_dir()?
            .entries
            .lock ...(+8) |
| `arceos/ulib/axlibc/c/glob.c#do_glob` | `arceos/ulib/axlibc/c/glob.c#do_glob` | 同板块同名同结构 | S_ISDIR, append, closedir, do_glob, errfunc, fnmatch ...(+7) | S_ISDIR, append, closedir, do_glob, errfunc, fnmatch ...(+7) |
| `arceos/ulib/axlibc/src/setjmp.rs#longjmp` | `arceos/ulib/axlibc/src/setjmp.rs#longjmp` | 同板块同名 | core::arch::naked_asm! | core::arch::naked_asm! |
| `arceos/ulib/axlibc/src/setjmp.rs#setjmp` | `arceos/ulib/axlibc/src/setjmp.rs#setjmp` | 同板块同名 | core::arch::naked_asm! | core::arch::naked_asm! |

#### 设备驱动
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `arceos/tools/bwbench_client/src/main.rs#transmit_benchmark` | `arceos/tools/bwbench_client/src/main.rs#transmit_benchmark` | 同板块同名同结构 | Local::now, NetDevice::new, NetDevice::new(interface).unwrap, current_time.signed_duration_since, current_time.signed_duration_since(past_time).num_seconds, dev.send ...(+2) | Local::now, NetDevice::new, NetDevice::new(interface).unwrap, current_time.signed_duration_since, current_time.signed_duration_since(past_time).num_seconds, dev.send ...(+2) |
| `arceos/tools/bwbench_client/src/device.rs#bind_interface` | `arceos/tools/bwbench_client/src/device.rs#bind_interface` | 同板块同名同结构 | ETH_P_ALL.to_be, Err, Ok, libc::bind, self.ifreq.ioctl, std::io::Error::last_os_error ...(+2) | ETH_P_ALL.to_be, Err, Ok, libc::bind, self.ifreq.ioctl, std::io::Error::last_os_error ...(+2) |
| `arceos/modules/axdriver/src/lib.rs#init_drivers` | `arceos/modules/axdriver/src/lib.rs#init_drivers` | 同板块同名同结构 | AllDevices::default, all_devs.block.iter, all_devs.block.iter().enumerate, all_devs.display.iter, all_devs.display.iter().enumerate, all_devs.net.iter ...(+5) | AllDevices::default, all_devs.block.iter, all_devs.block.iter().enumerate, all_devs.display.iter, all_devs.display.iter().enumerate, all_devs.net.iter ...(+5) |
| `arceos/modules/axdriver/src/virtio.rs#probe_pci` | `arceos/modules/axdriver/src/virtio.rs#probe_pci` | 同板块同名同结构 | D::try_new, Some, axdriver_virtio::probe_pci_device::<VirtIoHalImpl>, warn! | D::try_new, Some, axdriver_virtio::probe_pci_device::<VirtIoHalImpl>, warn! |

#### 网络协议栈
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `arceos/ulib/axlibc/c/network.c#inet_pton` | `arceos/ulib/axlibc/c/network.c#inet_pton` | 同板块同名同结构 | hexval, inet_pton, isdigit, memmove | hexval, inet_pton, isdigit, memmove |
| `arceos/api/arceos_posix_api/src/imp/net.rs#shutdown` | `api/src/fd/net.rs#shutdown` | 同板块同名同结构 | Ok, tcpsocket.lock, tcpsocket.peer_addr, tcpsocket.shutdown, udpsocket.lock, udpsocket.peer_addr ...(+1) | Ok, tcpsocket.lock, tcpsocket.peer_addr, tcpsocket.shutdown, udpsocket.lock, udpsocket.peer_addr ...(+1) |
| `arceos/ulib/axlibc/c/socket.c#accept4` | `arceos/ulib/axlibc/c/socket.c#accept4` | 同板块同名同结构 | accept, fcntl | accept, fcntl |
| `arceos/api/arceos_posix_api/src/imp/net.rs#recvfrom` | `api/src/fd/net.rs#recvfrom` | 同板块同名同结构 | Ok, Some, tcpsocket.lock, tcpsocket.lock().recv, tcpsocket.lock().recv(buf).map, udpsocket
                .lock ...(+2) | Ok, Some, tcpsocket.lock, tcpsocket.lock().recv, tcpsocket.lock().recv(buf).map, udpsocket
                .lock ...(+2) |

#### 文件系统/第三方库
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `crates/ext4plus/src/file_blocks/extent_tree.rs#write_at` | `crates/lwext4_rust/src/inode/file.rs#write_at` | 同板块同名 | 1usize
                    .checked_add, 1usize
                    .checked_add(remaining_after_first.div_ceil(block_size))
                    .ok_or, CorruptKind::ExtentBlock, CorruptKind::ExtentBlock(inode.index).into, CorruptKind::InvalidBlockSize.into, Err ...(+102) | Ok, assert!, assert_eq!, buf.as_ptr, buf.is_empty, buf.len ...(+15) |
| `arceos/modules/axfs/src/fs/ext4/inode.rs#rename` | `crates/axfs-ng-vfs/src/node/dir.rs#rename` | 同板块同名 | Arc::as_ptr, Err, Ok, active.get_mut, active.remove, core::num::NonZeroU32::new ...(+42) | Err, Self::forget_entry, Some, dir.has_children, dst.as_dir, dst.node_type ...(+13) |
| `crates/ext4plus/src/lib.rs#alloc_inode` | `crates/lwext4_rust/src/fs.rs#alloc_inode` | 同板块同名 | Err, InodeIndex::try_from, InodeIndex::try_from(
                    inode_num
                        .checked_add(
                            self.0
                                .superblock
                                .inodes_per_block_group()
                                .get()
                                .checked_mul(bg_id)
                                .unwrap(),
                        )
                        .unwrap()
                        .checked_add(1)
                        .unwrap(),
                )
                .unwrap, Ok, bg.free_inodes_count, bg.set_free_inodes_count ...(+44) | InodeRef::new, Ok, ext4_fs_alloc_inode, ext4_fs_alloc_inode(self.inner.as_mut(), result.inner.as_mut(), ty as _)
                .context, ext4_fs_inode_blocks_init, mem::zeroed ...(+2) |
| `crates/ext4plus/src/file_blocks/block_map.rs#write_at` | `arceos/modules/axfs/src/fs/lwext4_rust.rs#write_at` | 同板块同名 | Ok, add_to_file_offset, block_size.to_u64, block_size.to_usize, block_size_usize
            .checked_sub, block_size_usize
            .checked_sub(offset_in_block)
            .ok_or ...(+27) | <i32 as TryInto<AxError>>::try_into, <i32 as TryInto<AxError>>::try_into(e).unwrap, e.try_into, e.try_into().unwrap, file.file_close, file.file_open ...(+9) |

#### 网络协议栈/第三方库
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `crates/smoltcp/src/storage/assembler.rs#add` | `arceos/modules/axnet/src/smoltcp_impl/mod.rs#add` | 同板块同名 | Contig::hole_and_data, Err, Ok, contig.has_data, contig.shrink_hole_to, contig.total_size ...(+11) | debug!, self.0.lock, self.0.lock().add |
| `arceos/modules/axnet/src/smoltcp_impl/listen_table.rs#accept` | `arceos/modules/axnet/src/smoltcp_impl/listen_table.rs#accept` | 同板块同名 | Err, Ok, SOCKET_SET.remove, ax_err!, drop, entry.syn_queue.iter ...(+15) | Ok, ax_err!, get_addr_tuple, is_connected, is_connected(handle).then, self.tcp[port as usize].lock ...(+8) |
| `arceos/modules/axnet/src/smoltcp_impl/dns.rs#query` | `arceos/modules/axnet/src/smoltcp_impl/dns.rs#query` | 同板块同名 | Err, Ok, SOCKET_SET
            .with_socket_mut::<dns::Socket, _, _>, SOCKET_SET
            .with_socket_mut::<dns::Socket, _, _>(handle, |socket| {
                socket.start_query(iface.lock().context(), name, query_type)
            })
            .map_err, SOCKET_SET.poll_interfaces, SOCKET_SET.with_socket_mut::<dns::Socket, _, _> ...(+12) | Err, Ok, SOCKET_SET
            .with_socket_mut::<dns::Socket, _, _>, SOCKET_SET
            .with_socket_mut::<dns::Socket, _, _>(handle, |socket| {
                socket.start_query(iface.lock().context(), name, query_type)
            })
            .map_err, SOCKET_SET.poll_interfaces, SOCKET_SET.with_socket_mut::<dns::Socket, _, _> ...(+12) |
| `arceos/modules/axnet/src/smoltcp_impl/tcp.rs#bind` | `arceos/modules/axnet/src/smoltcp_impl/udp.rs#bind` | 同板块同名 | Ok, SOCKET_SET.bind_check, SOCKET_SET.with_socket_mut::<tcp::Socket, _, _>, Some, assert_ne!, ax_err! ...(+15) | (!is_unspecified(local_endpoint.addr)).then_some, Ok, SOCKET_SET.with_socket_mut::<udp::Socket, _, _>, Some, ax_err!, debug! ...(+9) |

#### 第三方库
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `crates/memory_set/src/set.rs#protect` | `crates/page_table_multiarch/page_table_multiarch/src/bits64.rs#protect` | 同板块同名 | MemoryArea::new, Ok, Vec::new, area.backend, area.backend().clone, area.end ...(+19) | Err, Ok, TlbFlush::new, entry.is_present, entry.set_flags, self.get_entry_mut ...(+1) |
| `crates/virtio-drivers/src/device/blk.rs#flush` | `crates/axdriver_crates/axdriver_virtio/src/gpu.rs#flush` | 同板块同名 | (BlkFeature::RING_INDIRECT_DESC | BlkFeature::FLUSH).bits, Arc::new, BlkResp {
                            status: RespStatus::OK,
                        }
                        .as_bytes, Default::default, Mutex::new, NonNull::from ...(+19) | self.inner.flush, self.inner.flush().map_err |
| `crates/memory_set/src/set.rs#unmap` | `crates/page_table_multiarch/page_table_multiarch/src/bits64.rs#unmap` | 同板块同名 | AddrRange::try_from_start_size, AddrRange::try_from_start_size(start, size).ok_or, Ok, after.end, after_end.sub_addr, area.unmap_area ...(+18) | Err, Ok, TlbFlush::new, entry.clear, entry.is_present, entry.paddr ...(+1) |
| `crates/axio/src/utils/copy.rs#copy_from` | `crates/page_table_multiarch/page_table_multiarch/src/bits64.rs#copy_from` | 同板块同名 | Err, Ok, buf.len, buf.set_len, buf.spare_capacity_mut, buf.spare_capacity_mut().into ...(+12) | dst_table[start_idx..end_idx].copy_from_slice, self.table_of, self.table_of_mut, self.top_level_idx_range |

#### 通用模块/其他
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `arceos/ulib/axlibc/c/printf.c#print_exponential_number` | `arceos/ulib/axlibc/c/printf.c#print_exponential_number` | 同板块同名同结构 | ABS_FOR_PRINTING, PRINTF_ABS, bastardized_floor, get_components, get_normalized_components, get_sign_bit ...(+5) | ABS_FOR_PRINTING, PRINTF_ABS, bastardized_floor, get_components, get_normalized_components, get_sign_bit ...(+5) |
| `arceos/ulib/axlibc/c/pow.c#pow` | `arceos/ulib/axlibc/c/pow.c#pow` | 同板块同名同结构 | __builtin_fma, __math_invalid, __math_oflow, __math_uflow, asdouble, asuint64 ...(+8) | __builtin_fma, __math_invalid, __math_oflow, __math_uflow, asdouble, asuint64 ...(+8) |
| `arceos/ulib/axlibc/c/string.c#twoway_strstr` | `arceos/ulib/axlibc/c/string.c#twoway_strstr` | 同板块同名同结构 | BITOP, MAX, memchr, memcmp | BITOP, MAX, memchr, memcmp |
| `arceos/ulib/axlibc/c/printf.c#print_broken_up_decimal` | `arceos/ulib/axlibc/c/printf.c#print_broken_up_decimal` | 同板块同名同结构 | out_rev_ | out_rev_ |


#### 1. 宏观判定
- **定性结论**: **独立开发，底层架构存在显著差异** (相似度: 14.0%，共同 AST 指纹: 1123；唯一 AST 指纹: 目标项目 6123，参考项目 3020；覆盖率: 目标项目 18.34%，参考项目 37.19%)

#### 2. 全模块覆盖矩阵解读
- **进程管理**: 双方均存在该模块。存在共同 AST 指纹。目标覆盖率 8.9%，参考覆盖率 29.76%。目标项目在该板块 91.1% 的代码为独立实现，且核心系统调用（如 `sys_wait4`、`sys_exit`）的调用图谱与参考项目完全不同，引入了 `process`/`thread` 双层抽象。证据强度：**弱**。
- **任务调度/异步运行时**: 双方均存在该模块。存在共同 AST 指纹。目标覆盖率 25.11%，参考覆盖率 38.19%。重合部分主要集中在 `arceos/` 上游模块（如 `axtask`、`axlog`）和 C 语言公共库（如 `printf.c`），属于共同依赖的上游代码。证据强度：**弱**。
- **内存管理**: 双方均存在该模块。存在共同 AST 指纹。目标覆盖率 22.51%，参考覆盖率 32.65%。目标项目在 `pulse_syscalls/src/impls/mm.rs` 中的 `sys_mmap` 实现与参考项目在 `src/syscall_imp/mm/mmap.rs` 中的实现调用链差异巨大，目标项目引入了 `CachedFile` 等独立抽象。证据强度：**弱**。
- **中断/异常处理**: 双方均存在该模块。存在共同 AST 指纹。目标覆盖率 18.18%，参考覆盖率 4.08%。双方在该模块的函数体量均很小（14 vs 62），且共同指纹来自数学库（`pow.c`）和互斥锁（`mutex.rs`）等公共组件，非核心中断处理逻辑。证据强度：**未形成**。
- **系统调用**: 双方均存在该模块。存在共同 AST 指纹。目标覆盖率 54.37%，参考覆盖率 43.41%。覆盖率较高，但证据显示重合点多为 `arceos_posix_api` 中的 `syscall_body!` 宏包装函数，属于 ArceOS 框架的标准模板代码，不涉及核心业务逻辑复用。证据强度：**弱**。
- **文件系统**: 双方均存在该模块。存在共同 AST 指纹。目标覆盖率 36.05%，参考覆盖率 32.86%。重合点包括 `tmpfs`、`procfs` 等内存文件系统的标准接口（如 `rename`、`lookup`），以及 C 语言公共库（`glob.c`、`setjmp.rs`）。目标项目在 ext4 实现上采用了独立的 `ext4plus` 库，与参考项目的 `lwext4_rust` 调用链完全不同。证据强度：**弱**。
- **设备驱动**: 双方均存在该模块。存在共同 AST 指纹。目标覆盖率 88.89%，参考覆盖率 71.54%。覆盖率极高，但证据显示重合点几乎全部位于 `arceos/tools`（如 `bwbench_client`）和 `arceos/modules/axdriver` 等上游工具和驱动框架代码，属于完全相同的公共依赖。证据强度：**未形成**（均为上游代码）。
- **网络协议栈**: 双方均存在该模块。存在共同 AST 指纹。目标覆盖率 78.46%，参考覆盖率 96.23%。重合点集中在 `arceos/ulib/axlibc`、`arceos/api/arceos_posix_api` 和 `smoltcp` 封装层，均为基于同一上游协议栈和 C 库的标准实现。证据强度：**未形成**（均为上游/公共库代码）。
- **第三方库/公共依赖**: 双方均存在大量第三方库代码。目标项目在 `文件系统/第三方库`、`网络协议栈/第三方库` 和 `第三方库` 分类下的函数总数远超参考项目（863+2635+1354 vs 689+107+386），说明目标项目集成了更多或更完整的第三方库。此模块的重合属于公共依赖，不构成项目间抄袭证据。证据强度：**未形成**。

#### 3. 底层 AST 关键证据指纹表
| 所属板块 | 目标项目路径 | 参考项目路径 | 证据类型 | 技术说明 |
| :--- | :--- | :--- | :--- | :--- |
| 进程管理 | `pulse_syscalls/src/impls/task/wait.rs#sys_wait4` | `src/syscall_imp/task/thread.rs#sys_wait4` | 同板块同名，架构异构 | 目标项目通过 `child_proc.exit_code`、`child_proc.pid` 等 `process` 对象方法实现；参考项目通过 `child.exit_code`、`child.parent` 等 `task_ext` 扩展实现。两者进程/线程模型抽象层次完全不同。 |
| 进程管理 | `pulse_syscalls/src/impls/task/exit.rs#sys_exit` | `src/syscall_imp/task/thread.rs#sys_exit` | 同板块同名，架构异构 | 目标项目调用 `current_thread`、`thread.exit_current` 等 `pulse_core` 抽象；参考项目直接调用 `do_exit`。退出流程的调用链无重合。 |
| 进程管理 | `arceos/modules/axtask/src/run_queue.rs#exit_current` | `arceos/modules/axtask/src/run_queue.rs#exit_current` | 同板块同名，上游代码版本差异 | 核心调度逻辑一致，但目标项目调用 `axhal::power::system_off` 关机，参考项目调用 `axhal::misc::terminate`。属于 ArceOS 上游 API 版本演进，非抄袭伪装。 |
| 进程管理 | `arceos/modules/axtask/src/wait_queue.rs#wait_timeout` | `arceos/modules/axtask/src/wait_queue.rs#wait_timeout` | 同板块同名，上游代码版本差异 | 核心等待逻辑一致，但目标项目使用 `axhal::time::monotonic_time`，参考项目使用 `axhal::time::wall_time`。属于时间源选择的架构差异。 |
| 内存管理 | `pulse_syscalls/src/impls/mm.rs#sys_mmap` | `src/syscall_imp/mm/mmap.rs#sys_mmap` | 同板块同名，架构异构 | 目标项目调用 `CachedFile::get_or_create` 等文件缓存抽象；参考项目调用 `MmapFlags::from_bits_truncate` 等标志位处理。内存映射的实现路径和依赖对象完全不同。 |
| 文件系统 | `crates/ext4plus/src/file_blocks/extent_tree.rs#write_at` | `crates/lwext4_rust/src/inode/file.rs#write_at` | 同板块同名，第三方库异构 | 目标项目使用自研/集成的 `ext4plus` 库，调用 `CorruptKind::ExtentBlock` 等；参考项目使用 `lwext4_rust` 库，调用 `file_open`/`file_close`。两者是不同的 ext4 实现库。 |
| 第三方库 | `crates/memory_set/src/set.rs#protect` | `crates/page_table_multiarch/.../bits64.rs#protect` | 跨板块同名，领域不同 | 目标项目操作 `MemoryArea` 和 `backend` 对象，属于内存区域管理；参考项目操作 `TlbFlush` 和 `entry.set_flags`，属于页表项操作。两者处于不同抽象层次，不构成同源证据。 |

#### 4. 核心代码调用流对比剖析
* **【进程管理/任务等待与唤醒】**
* **底层技术析理**: 双方均实现了等待队列的超时等待机制，但采用了不同的时间源和任务身份比较策略。目标项目使用单调时钟 (`monotonic_time`) 并通过 `Arc::ptr_eq` 比较任务身份，参考项目使用墙上时钟 (`wall_time`) 并通过 `curr.in_wait_queue` 检查状态。这反映了在时间语义和并发安全模型上的独立设计选择。
* **代码溯源链接**:
  - 目标项目: `arceos/modules/axtask/src/wait_queue.rs#wait_timeout`
  - 参考项目: `arceos/modules/axtask/src/wait_queue.rs#wait_timeout`

* **【进程管理/进程退出与回收】**
* **底层技术析理**: 目标项目构建了独立的 `process` 抽象层，`sys_wait4` 系统调用通过 `child_proc.exit_code`、`child_proc.release_task_refs` 等方法操作进程对象。参考项目则依赖 `task_ext` 扩展机制，直接访问 `child.exit_code`、`child.parent` 等字段。两者的进程控制块（PCB）模型和资源回收路径存在根本性架构差异。
* **代码溯源链接**:
  - 目标项目: `pulse_syscalls/src/impls/task/wait.rs#sys_wait4`
  - 参考项目: `src/syscall_imp/task/thread.rs#sys_wait4`

* **【内存管理/内存映射】**
* **底层技术析理**: 目标项目的 `sys_mmap` 实现深度集成了文件缓存层 (`CachedFile::get_or_create`)，表明其内存映射与文件系统的耦合方式与参考项目不同。参考项目的实现则更侧重于对 `MmapFlags` 的逐位解析和校验。两者在 VMA（虚拟内存区域）管理和文件页缓存策略上采用了不同的实现路径。
* **代码溯源链接**:
  - 目标项目: `pulse_syscalls/src/impls/mm.rs#sys_mmap`
  - 参考项目: `src/syscall_imp/mm/mmap.rs#sys_mmap`

#### 5. 单方模块与降权项
- **单方模块**: 目标项目存在大量 `pulse_core`、`pulse_syscalls` 前缀的独立模块，以及 `ext4plus` 等自研/集成库，这些在参考项目中无对应。参考项目也存在 `api/src/imp/`、`core/src/` 等独立模块。这些单方模块是各自独立开发的明证。
- **第三方库/公共依赖**: 双方共同包含的 `arceos/` 目录（`modules/axtask`、`api/arceos_posix_api`、`ulib/axlibc` 等）、`crates/smoltcp`、`crates/virtio-drivers` 等属于共同的上游操作系统框架和第三方库。所有指向这些路径的“同板块同名”证据，均应降权为**公共依赖**，不能作为项目间抄袭的证据。
- **Trivial 惯用实现**: `sys_epoll_wait`、`sys_getpid` 等仅包含 `syscall_body!` 宏的函数，`pthread_cond_wait` 等 `unimplemented` 桩函数，以及 `set_clear_child_tid`、`set_in_wait_queue` 等仅包含单行原子变量存储的函数，因其逻辑过于简单且为业界通用写法，不构成有效抄袭证据，予以降权。

#### 6. 保守结论
综合评估，目标项目与参考项目的整体 AST 相似度仅为 14.0%，远低于构成抄袭嫌疑的阈值。现有证据表明，双方代码的重合部分几乎全部源于共同依赖的 ArceOS 操作系统框架、第三方库（如 smoltcp）以及 C 语言标准公共库。在核心的进程管理、内存管理等子系统上，目标项目展现出了与参考项目截然不同的架构设计，包括独立的 `process`/`thread` 抽象、单调时钟选择以及独特的文件缓存集成方式。控方所指控的“大面积 AST 结构照搬”实为对上游公共代码和 trivial 惯用实现的误判。因此，判定目标项目为独立开发，不存在对参考项目的系统性代码抄袭。

------------------------------

