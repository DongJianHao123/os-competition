# 🛡️ OS 内核赛自动化代码级架构审计报告

**审计时间**: `2026-07-02 12:54:23`
**参赛作品名称**: `2026-T2026105549911448-XX`
**年份**: `2026`
**学校**: `XX`
**队伍名称**: `T2026105549911448`
**比赛名字**: `OS-Competition`
**比赛子赛道**: `Kernel-Implementation`
**仓库地址**: `https://gitlab.eduxiji.net/educg-group-43501-3132633/T2026105549911448-3008`
**推测开发语言**: `Rust / C (由AST探针自动识别)`
**特征函数量**: `1286`

## ⚔️ 底层代码同源性对比 (AST Homology)
> 本部分由专门的代码审查智能体，对该项目与符合年份条件的历届最高相似度项目进行控制流与底层调用对比。新版报告会先给出确定性的全模块覆盖矩阵，再给出 Agent 的分模块解释，避免只围绕少量抽样函数展开。

### 🏆 Top 5 底层对标: vs `2024-chaos-北京科技大学`（北京科技大学 / chaos）

#### 对方作品信息

**对方年份**: `2024`
**对方学校**: `北京科技大学`
**对方队伍名称**: `chaos`
**对方比赛名字**: `OS-Competition`
**对方比赛子赛道**: `Kernel-Implementation`
**对方仓库地址**: `https://gitlab.eduxiji.net/educg-group-26010-2376550/oskernel2024-chaos`

> 双向覆盖率：共同 AST 指纹: 16；唯一 AST 指纹: 目标项目 1112，参考项目 798；覆盖率: 目标项目 1.44%，参考项目 2.01%

## 全模块对比总览（确定性证据）

> 总体覆盖率：共同 AST 指纹: 16；唯一 AST 指纹: 目标项目 1112，参考项目 798；覆盖率: 目标项目 1.44%，参考项目 2.01%

### 1. 模块覆盖矩阵
| 模块 | 目标函数数 | 参考函数数 | 共同唯一 AST 指纹 | Target 覆盖 | Base 覆盖 | 状态 |
| :--- | ---: | ---: | ---: | ---: | ---: | :--- |
| 进程管理 | 106 | 45 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |
| 任务调度/异步运行时 | 253 | 76 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |
| 内存管理 | 271 | 178 | 10 | 3.75% | 5.99% | 双方存在且有结构重合 |
| 中断/异常处理 | 34 | 5 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |
| 系统调用 | 115 | 67 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |
| 文件系统 | 303 | 152 | 2 | 0.78% | 1.59% | 双方存在且有结构重合 |
| 设备驱动 | 39 | 19 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |
| 网络协议栈 | 42 | 0 | 0 | 0.0% | 0.0% | 仅目标项目体现 |
| 文件系统/第三方库 | 45 | 270 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |
| 第三方库 | 0 | 50 | 0 | 0.0% | 0.0% | 仅参考项目体现 |
| 通用模块/其他 | 78 | 75 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |

### 2. 分模块证据清单

#### 进程管理
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `kernel/src/syscall/mod.rs#sys_execve` | `os/src/syscall/process.rs#sys_execve` | 同板块同名 | copy_path, crate::println!, cwd_inode, exec_resolved, fs::lookup_path, fs::root ...(+5) | String::from, Vec::new, all_data.as_slice, args.add, args_vec.insert, args_vec.len ...(+22) |
| `kernel/src/syscall/mod.rs#sys_exit` | `os/src/syscall/process.rs#sys_exit` | 同板块同名 | current_task, exit_one_thread | exit_current_and_run_next, panic!, trace! |
| `kernel/src/task/mod.rs#current_pid` | `os/src/task/processor.rs#current_pid` | 同板块同名 | CURRENT_PID.load | Some, current_task |
| `kernel/src/syscall/mod.rs#sys_rt_sigtimedwait` | `os/src/syscall/process.rs#sys_clone` | 同模块上下文对齐/差异观察 | (*task.tf_ptr()).rewind_syscall, b[0..8].try_into, b[0..8].try_into().unwrap_or, b[8..16].try_into, b[8..16].try_into().unwrap_or, bytes.as_slice ...(+25) | CloneFlags::from_bits, CloneFlags::from_bits((flags & !CSIGNAL) as u32).unwrap, SignalFlags::from_bits, SignalFlags::from_bits(1 << ((flags & CSIGNAL) - 1)).unwrap, clone_signals.contains, ctid.is_null ...(+13) |

#### 任务调度/异步运行时
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `kernel/src/syscall/mod.rs#sys_read` | `os/src/syscall/fs.rs#sys_read` | 同板块同名 | (*tf).rewind_syscall, Some, crate::arch::now_ticks, crate::fs::notify::active, crate::fs::notify::report, crate::net::poll ...(+35) | core::slice::from_raw_parts_mut, current_task, current_task().unwrap, drop, file!, file.clone ...(+8) |
| `kernel/src/syscall/mod.rs#sys_ioctl` | `os/src/syscall/fs.rs#sys_ioctl` | 同板块同名 | (bytes / 512).to_le_bytes, 4096u64.to_le_bytes, 512i32.to_le_bytes, READAHEAD.load, READAHEAD.store, TTY_FG_PGID.load ...(+37) | trace! |
| `kernel/src/syscall/mod.rs#sys_mount` | `os/src/syscall/fs.rs#sys_mount` | 同板块同名 | b.dev.clone, copy_path, copy_path(fstype).unwrap_or_default, copy_path(source).unwrap_or_default, cwd_inode, dev.clone ...(+16) | trace! |
| `kernel/src/syscall/mod.rs#sys_close` | `os/src/syscall/fs.rs#sys_close` | 同板块同名 | Arc::as_ptr, FLOCK_RANGES.lock, Some, closing
        .as_ref, closing
        .as_ref()
        .map, crate::fs::notify::active ...(+16) | current_task, current_task().unwrap, file!, inner.fd_table.len, inner.fd_table[fd].is_none, inner.fd_table[fd].take ...(+3) |

#### 内存管理
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `kernel/src/syscall/mod.rs#sys_fcntl` | `os/src/syscall/fs.rs#sys_fcntl` | 同板块同名 | Flock::default, Some, b[0..4].try_into, b[0..4].try_into().unwrap_or, b[4..8].try_into, b[4..8].try_into().unwrap_or ...(+55) | current_task, current_task().unwrap, debug!, file!, inner.alloc_fd, inner.fd_table.len ...(+6) |
| `kernel/src/syscall/mod.rs#sys_openat` | `os/src/syscall/fs.rs#sys_openat` | 同板块同名 | Arc::new, File::from_inode, Some, String::new, abs.is_empty, alloc::format! ...(+33) | OpenFlags::from_bits, OpenFlags::from_bits(flags).unwrap, Some, cast_file_to_inode, cast_file_to_inode(dir).unwrap, cast_inode_to_file ...(+19) |
| `kernel/src/syscall/mod.rs#sys_getdents64` | `os/src/syscall/fs.rs#sys_getdents64` | 同板块同名 | ((idx + 1) as i64).to_le_bytes, (reclen as u16).to_le_bytes, alloc::string::String::from, alloc::vec!, alloc::vec::Vec::new, alloc::vec::Vec::with_capacity ...(+29) | Dirent::new, ROOT_INODE.clone, cast_file_to_inode, cast_file_to_inode(dir).unwrap, current_task, current_task().unwrap ...(+21) |
| `kernel/src/mm/memory_set.rs#fork` | `os/src/task/task.rs#fork` | 同板块同名 | Arc::clone, Arc::new, MemorySet::try_new, Some, alloc::collections::BTreeMap::new, alloc_frame ...(+9) | (MapPermission::R | MapPermission::W).bits, Arc::clone, Arc::downgrade, Arc::new, MemorySet::from_existed_user, PTEFlags::from_bits ...(+42) |

#### 中断/异常处理
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `kernel/src/signal.rs#default_action` | `os/src/fs/pipe.rs#read` | 同模块上下文对齐/差异观察 | 内部闭环逻辑 | assert!, buf.into_iter, buf.len, buf_iter.next, debug!, drop ...(+11) |
| `kernel/src/arch/riscv64/trap.rs#init` | `os/src/trap/mod.rs#set_kernel_trap_entry` | 同模块上下文对齐/差异观察 | arm_timer, riscv::register::sie::set_stimer, riscv::register::sscratch::write, stvec::write | stvec::write |
| `kernel/src/arch/loongarch64/trap.rs#csrrd` | `os/src/trap/mod.rs#set_user_trap_entry` | 同模块上下文对齐/差异观察 | core::arch::asm!, unreachable! | stvec::write |
| `kernel/src/arch/loongarch64/trap.rs#arm_timer` | `os/src/trap/mod.rs#enable_timer_interrupt` | 同模块上下文对齐/差异观察 | core::arch::asm! | sie::set_stimer |

#### 系统调用
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `kernel/src/syscall/mod.rs#sys_set_id` | `user/src/lib.rs#waitpid` | 同模块上下文对齐/差异观察 | CREDS.lock, CREDS.lock().insert, SAVED_IDS.lock, SAVED_IDS.lock().insert, creds_of, cur_tgid ...(+2) | sys_waitpid, yield_ |
| `kernel/src/syscall/keys.rs#sys_add_key` | `user/src/syscall.rs#syscall` | 同模块上下文对齐/差异观察 | ENOTDIR_AS_KEYRING, REG.lock, Vec::new, copy_cstr, copy_in, default_key_perm ...(+11) | asm! |
| `kernel/src/syscall/keys.rs#sys_request_key` | `user/src/lib.rs#wait` | 同模块上下文对齐/差异观察 | REG.lock, Vec::new, copy_cstr, copy_cstr(callout_ptr, 4096).is_none, default_key_perm, link_into ...(+3) | sys_waitpid, yield_ |
| `kernel/src/syscall/keys.rs#keyctl_read` | `user/src/lib.rs#waittid` | 同模块上下文对齐/差异观察 | REG.lock, Vec::with_capacity, bytes.extend_from_slice, copy_out_capped, k.is_keyring, k.ktype.as_str ...(+6) | sys_waittid, yield_ |

#### 文件系统
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `kernel/src/fs/socket.rs#read_at` | `os/src/fs/fat32/inode.rs#read_at` | 同板块同名 | Err, Ok, buf.len, buf[..n].copy_from_slice, core::cmp::min, dg.data.len ...(+10) | buf.len, buf[read_size..read_size + copy_size]
                .copy_from_slice, dentry.file_size, fs.cluster_chain, fs.read_cluster, min ...(+3) |
| `kernel/src/fs/ext2.rs#mount` | `os/src/fs/fs.rs#mount` | 同板块同名 | Arc::new, Err, Mutex::new, Ok, rd16, rd32 ...(+1) | Path::new, self.mounted_fs.insert |
| `kernel/src/fs/ext2.rs#unlink` | `os/src/fs/fat32/inode.rs#unlink` | 同板块同名 | Err, Ok, child.kind, self.di, self.di()?.kind, self.fs.dir_lookup ...(+5) | dentry.name, fs.fat.cluster_id_to_sector_id, fs.fat.cluster_id_to_sector_id(self.start_cluster).unwrap, fs.get_dentry, fs.remove_dentry, self.fs.as_ref |
| `kernel/src/fs/devfs.rs#write_at` | `os/src/fs/fat32/inode.rs#write_at` | 同板块同名 | Err, Ok, buf.len, core::cmp::min, sec[within..within + n].copy_from_slice, self.dev.capacity ...(+4) | buf.len, cluster_buf[pos % CLUSTER_SIZE..pos % CLUSTER_SIZE + copy_size]
                .copy_from_slice, fs.cluster_chain, fs.read_cluster, fs.write_cluster, min ...(+2) |

#### 设备驱动
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `kernel/src/drivers/virtio_blk.rs#mmio_phys_to_virt` | `os/src/drivers/block/virtio_blk.rs#mmio_phys_to_virt` | 同板块同名 | NonNull::new, NonNull::new((paddr | DMW1_UNCACHED) as *mut u8).unwrap, NonNull::new(paddr as *mut u8).unwrap | KernelAddr::from, NonNull::new_unchecked, PhysAddr::from |
| `kernel/src/drivers/virtio_blk.rs#unshare` | `os/src/drivers/block/virtio_blk.rs#unshare` | 同板块同名 | 内部闭环逻辑 | 内部闭环逻辑 |
| `kernel/src/drivers/virtio_blk.rs#probe_mmio` | `os/src/utils/platform_info.rs#fmt` | 同模块上下文对齐/差异观察 | Arc::new, BlkInner::Mmio, BlockCache::new, MmioTransport::new, Mutex::new, NonNull::new ...(+7) | Ok, core::str::from_utf8, core::str::from_utf8(&self.model[..index]).unwrap, core::str::from_utf8(&x[..self.bootargs_len]).unwrap, self
            .bootargs
            .as_ref, self
            .bootargs
            .as_ref()
            .map ...(+11) |
| `kernel/src/drivers/pci.rs#assign_bars` | `os/src/block/block_cache.rs#get_block_cache` | 同模块上下文对齐/差异观察 | align_up, aligned.checked_add, aligned.checked_add(size).map_or, crate::println!, info.takes_two_entries, root.bar_info ...(+2) | Arc::clone, Arc::new, Arc::strong_count, BlockCache::new, Mutex::new, panic! ...(+8) |

#### 网络协议栈
仅目标项目中观察到该模块代表函数，参考项目没有同模块可对齐证据：
- 目标项目：`kernel/src/net/mod.rs#udp_send`，调用摘要：IpAddress::Ipv4, STACK.get, STACK.get().ok_or, data.len, g.sockets.get_mut::<udp::Socket>, s.lock ...(+6)
- 目标项目：`kernel/src/net/mod.rs#udp_recv`，调用摘要：Err, Ok, STACK.get, STACK.get().ok_or, g.sockets.get_mut::<udp::Socket>, s.lock ...(+1)
- 目标项目：`kernel/src/net/mod.rs#add_udp_socket`，调用摘要：STACK.get, Some, g.sockets.add, s.lock, udp::PacketBuffer::new, udp::Socket::new ...(+1)
- 目标项目：`kernel/src/net/mod.rs#add_tcp_socket`，调用摘要：STACK.get, Some, g.sockets.add, s.lock, sock.set_nagle_enabled, tcp::Socket::new ...(+2)

#### 文件系统/第三方库
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `kernel/src/fs/ext4.rs#create` | `os/src/fs/ext4/inode.rs#create` | 同板块同名 | Arc::new, Err, Ok, TmpfsDir::new_root, TmpfsFile::new, added.contains_key ...(+11) | todo! |
| `kernel/src/fs/ext4.rs#read_at` | `os/src/fs/ext4/inode.rs#read_at` | 同板块同名 | Ok, buf.len, buf[..n].copy_from_slice, core::cmp::min, self.fs.read_inode, self.fs.read_inode(self.ino).map_err ...(+5) | Ext4File::new, buf.len, self
            .fs
            .ext4
            .ext4_file_read |
| `kernel/src/fs/ext4.rs#unlink` | `os/src/fs/ext4/inode.rs#unlink` | 同板块同名 | Err, Ok, added.remove, added.remove(name).is_some, m.contains_key, name.to_string ...(+9) | self.fs.ext4.ext4_file_remove, self.fs.ext4.ext4_file_remove(self.ino, name).is_ok |
| `kernel/src/fs/ext4.rs#write_at` | `os/src/fs/ext4/inode.rs#write_at` | 同板块同名 | Ok, Some, buf.len, data.to_vec, self.data, self.mutated.lock ...(+6) | Arc::downgrade, Ext4File::new, Ext4InodeRef::get_inode_ref, buf.len, inode_ref.inner.inode.inode_get_size, self.fs.ext4.ext4_file_write |

#### 第三方库
仅参考项目中观察到该模块代表函数，目标项目没有同模块可对齐证据：
- 参考项目：`os/libs/visionfive2-sd/src/lib.rs#send_cmd`，调用摘要：RawInterrupt::from, RawInterruptStatusReg::from, S::sleep_ms_until, Some, arg.into, assert! ...(+21)
- 参考项目：`os/libs/visionfive2-sd/src/lib.rs#init_sdcard`，调用摘要：CardTypeReg::from, CardTypeReg::from(0).with_card_width4_1, CmdArg::new, CmdReg::from, ControlReg::from, RawInterruptStatusReg::from ...(+18)
- 参考项目：`os/libs/visionfive2-sd/src/register.rs#from`，调用摘要：CmdReg::with_data, CmdReg::with_data(0, value.into()).with_transfer_dir, CmdReg::with_no_data, CmdReg::with_no_data(0, value.into())
                    .with_check_response_crc, CmdReg::with_no_data(0, value.into())
                    .with_check_response_crc(false)
                    .with_response_length, CmdReg::with_no_data(0, value.into()).with_check_response_crc ...(+3)
- 参考项目：`os/libs/visionfive2-sd/src/lib.rs#reset_clock`，调用摘要：ClockDividerReg::new, ClockDividerReg::new().with_clk_divider0, ClockEnableReg::from, CmdArg::new, CmdReg::from, CmdReg::from(0)
        .with_start_cmd ...(+7)

#### 通用模块/其他
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `kernel/src/arch/riscv64/power.rs#shutdown` | `os/src/sbi.rs#shutdown` | 同板块同名 | core::arch::asm!, sbi_rt::system_reset, sifive_exit | panic!, sbi_call |
| `kernel/src/console.rs#write_str` | `os/src/console.rs#write_str` | 同板块同名 | Ok, crate::arch::console_put, s.as_bytes | Ok, console_putchar, s.chars |
| `kernel/src/sync/spinlock.rs#lock` | `os/src/sync/mutex/spin_mutex.rs#lock` | 同板块同名 | Some, preempt_disable, self.inner.lock | S::before_lock, self
                .lock
                .compare_exchange, self
                .lock
                .compare_exchange(false, true, Ordering::Acquire, Ordering::Relaxed)
                .is_ok, self.wait_unlock |
| `kernel/src/sync/spinlock.rs#deref` | `os/src/sync/mutex/spin_mutex.rs#deref` | 同板块同名 | self.inner.as_ref, self.inner.as_ref().unwrap | self.mutex.data.get |


#### 1. 宏观判定
- **定性结论**: 独立开发 (相似度: 0.84%，共同 AST 指纹: 16；唯一 AST 指纹: 目标项目 1112，参考项目 798；覆盖率: 目标项目 1.44%，参考项目 2.01%)

#### 2. 全模块覆盖矩阵解读
- **进程管理**: 双方均存在该模块。共同 AST 指纹为 0，覆盖率均为 0.0%。这表明在 AST 结构层面，两个项目的进程管理实现无任何可被检测器识别的共同结构。控方指控基于函数名相同和 POSIX 标准语义，证据强度为**未形成**。
- **任务调度/异步运行时**: 双方均存在该模块。共同 AST 指纹为 0，覆盖率均为 0.0%。双方在调度器实现、异步运行时模型上无 AST 结构重合，证据强度为**未形成**。
- **内存管理**: 双方均存在该模块。共同 AST 指纹为 10，目标覆盖率 3.75%，参考覆盖率 5.99%。存在少量结构重合，但覆盖率极低，远低于复用阈值。重合点集中在 `sys_fcntl`、`sys_openat` 等标准系统调用的通用字节操作和标准库调用上，证据强度为**弱**。
- **中断/异常处理**: 双方均存在该模块。共同 AST 指纹为 0，覆盖率均为 0.0%。双方在 Trap 处理、架构相关寄存器操作上无 AST 结构重合，证据强度为**未形成**。
- **系统调用**: 双方均存在该模块。共同 AST 指纹为 0，覆盖率均为 0.0%。双方在系统调用分发、参数处理、安全模型上无 AST 结构重合，证据强度为**未形成**。
- **文件系统**: 双方均存在该模块。共同 AST 指纹为 2，目标覆盖率 0.78%，参考覆盖率 1.59%。重合点集中在 `read_at`、`write_at` 等标准 VFS 接口的通用边界检查逻辑上，证据强度为**弱**。
- **设备驱动**: 双方均存在该模块。共同 AST 指纹为 0，覆盖率均为 0.0%。双方在驱动模型、硬件抽象层上无 AST 结构重合，证据强度为**未形成**。
- **网络协议栈**: 仅目标项目体现。参考项目无此模块，无法形成比对，证据强度为**未形成**。
- **第三方库/公共依赖**: 双方均存在该模块。共同 AST 指纹为 0，覆盖率均为 0.0%。双方引用的第三方库或公共依赖完全不同，无 AST 结构重合，证据强度为**未形成**。

#### 3. 底层 AST 关键证据指纹表
| 所属板块 | 目标项目路径 | 参考项目路径 | 证据类型 | 技术说明 |
| :--- | :--- | :--- | :--- | :--- |
| 内存管理 | kernel/src/syscall/mod.rs#sys_fcntl | os/src/syscall/fs.rs#sys_fcntl | 同板块同名 | 均使用 `try_into`、`to_le_bytes` 进行字节序列化，但这是实现 `fcntl` 标准命令的通用惯用法，非独创逻辑。 |
| 内存管理 | kernel/src/syscall/mod.rs#sys_openat | os/src/syscall/fs.rs#sys_openat | 同板块同名 | 均调用 `Arc::new`、`File::from_inode` 等标准库和 VFS 接口，但路径解析和权限检查逻辑完全不同。 |
| 内存管理 | kernel/src/mm/memory_set.rs#fork | os/src/task/task.rs#fork | 同板块同名 | 均调用 `Arc::clone`、`Arc::new` 进行内存集复制，但目标项目使用 `BTreeMap`，参考项目使用 `from_existed_user`，内存模型不同。 |
| 文件系统 | kernel/src/fs/socket.rs#read_at | os/src/fs/fat32/inode.rs#read_at | 同板块同名 | 均包含 `buf[..n].copy_from_slice` 和 `core::cmp::min` 边界检查，这是 Rust 安全编程的标准模式。 |
| 文件系统 | kernel/src/fs/ext2.rs#mount | os/src/fs/fs.rs#mount | 同板块同名 | 均调用 `Arc::new`、`Mutex::new` 进行挂载点初始化，但底层文件系统实现（ext2 vs FAT32）完全不同。 |

#### 4. 核心代码调用流对比剖析
* **【进程管理/`sys_execve` 系统调用】**
* **底层技术析理**: 目标项目围绕 VFS 路径解析（`fs::lookup_path`、`fs::root`、`cwd_inode`）和执行体解析（`exec_resolved`）展开，采用显式的用户态路径拷贝（`copy_path`）。参考项目则围绕参数向量构建（`args_vec.push`、`args_vec.insert`）和 C 字符串转换（`c_ptr_to_string`）展开。两者在核心对象、数据流和抽象层次上完全不同，仅高层语义因 POSIX 标准而相似。
* **代码溯源链接**:
  - 目标项目: `kernel/src/syscall/mod.rs#sys_execve`
  - 参考项目: `os/src/syscall/process.rs#sys_execve`

* **【进程管理/`sys_exit` 系统调用】**
* **底层技术析理**: 目标项目调用 `exit_one_thread`，暗示其支持多线程进程中的线程级退出。参考项目调用 `exit_current_and_run_next`，是单线程教学内核的“退出并调度”模型。两者在线程模型、退出路径和调度接口上存在根本性架构差异。
* **代码溯源链接**:
  - 目标项目: `kernel/src/syscall/mod.rs#sys_exit`
  - 参考项目: `os/src/syscall/process.rs#sys_exit`

* **【内存管理/`fork` 核心逻辑】**
* **底层技术析理**: 目标项目使用 `MemorySet::try_new` 和 `BTreeMap` 构建新的内存集，并调用 `alloc_frame` 进行物理页分配。参考项目则使用 `MemorySet::from_existed_user` 和 `PTEFlags::from_bits` 进行页表项复制。两者在内存集构造方法、页表操作抽象和物理内存管理上采用完全不同的策略。
* **代码溯源链接**:
  - 目标项目: `kernel/src/mm/memory_set.rs#fork`
  - 参考项目: `os/src/task/task.rs#fork`

* **【文件系统/`read_at` 通用接口】**
* **底层技术析理**: 目标项目在 socket 的 `read_at` 中处理数据报边界（`dg.data.len`），参考项目在 FAT32 的 `read_at` 中处理簇链读取（`fs.cluster_chain`）。两者除了 `buf[..n].copy_from_slice` 和 `core::cmp::min` 这类 Rust 标准边界检查外，底层 I/O 路径和数据源完全不同。
* **代码溯源链接**:
  - 目标项目: `kernel/src/fs/socket.rs#read_at`
  - 参考项目: `os/src/fs/fat32/inode.rs#read_at`

#### 5. 单方模块与降权项
- **网络协议栈**: 仅目标项目拥有完整的 TCP/UDP 协议栈（`kernel/src/net/mod.rs`），参考项目无此模块。这是目标项目独立开发的强有力证据。
- **第三方库**: 参考项目包含 `visionfive2-sd` 等特定硬件平台的第三方库，目标项目无此依赖。双方在第三方库的选择和使用上无任何重合。
- **跨板块硬凑项**: 控方证据中，`default_action`（信号处理）与 `pipe::read`（管道读取）、`probe_mmio`（virtio 驱动）与 `platform_info::fmt`（平台信息）等配对，属于跨板块、跨子系统的强行比对，无任何逻辑关联，已全部降权为弱信号。
- **通用宏与标准库惯用法**: `println!`、`trace!`、`to_le_bytes`、`copy_from_slice`、`Arc::new`、`Mutex::new` 等调用，属于 Rust 语言的标准输出、序列化、并发原语和通用编程模式，不构成任何项目特有的独创性表达，不能作为复用证据。

#### 6. 保守结论
基于 0.84% 的客观 AST 相似度和仅 16 个共同指纹，两个项目在代码实现层面不存在可被检测的系统性复用。控方指控集中于 POSIX 标准语义和通用编程惯用法，缺乏 AST 结构重合的支撑。目标项目在进程管理、内存管理、文件系统等核心板块均展现出与参考项目截然不同的架构设计、数据结构和同步策略，且独立实现了网络协议栈等参考项目完全不具备的复杂子系统。综合判定，目标项目为独立开发，整体风险极低。

------------------------------

