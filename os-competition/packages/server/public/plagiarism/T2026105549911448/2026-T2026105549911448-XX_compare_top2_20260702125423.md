# 🛡️ OS 内核赛自动化代码级架构审计报告

**审计时间**: `2026-07-02 12:54:23`
**参赛作品名称**: `2026-T2026105549911448-XX`
**年份**: `2026`
**学校**: `XX`
**队伍名称**: `T2026105549911448`
**比赛名字**: `OS-Competition`
**比赛子赛道**: `Kernel-Implementation`
**仓库地址**: `https://gitlab.eduxiji.net/educg-group-43501-3132633/T2026105549911448-3008`
**推测开发语言**: `Rust / C (由AST探针自动识别)`
**特征函数量**: `1286`

## ⚔️ 底层代码同源性对比 (AST Homology)
> 本部分由专门的代码审查智能体，对该项目与符合年份条件的历届最高相似度项目进行控制流与底层调用对比。新版报告会先给出确定性的全模块覆盖矩阵，再给出 Agent 的分模块解释，避免只围绕少量抽样函数展开。

### 🏆 Top 2 底层对标: vs `2024-NPUcore-IMPACT-西北工业大学`（西北工业大学 / NPUcore-IMPACT!!!）

#### 对方作品信息

**对方年份**: `2024`
**对方学校**: `西北工业大学`
**对方队伍名称**: `NPUcore-IMPACT!!!`
**对方比赛名字**: `OS-Competition`
**对方比赛子赛道**: `Kernel-Implementation`
**对方仓库地址**: `https://gitlab.eduxiji.net/educg-group-26011-2376549/T202410699992496-312`

> 双向覆盖率：共同 AST 指纹: 16；唯一 AST 指纹: 目标项目 1112，参考项目 642；覆盖率: 目标项目 1.44%，参考项目 2.49%

## 全模块对比总览（确定性证据）

> 总体覆盖率：共同 AST 指纹: 16；唯一 AST 指纹: 目标项目 1112，参考项目 642；覆盖率: 目标项目 1.44%，参考项目 2.49%

### 1. 模块覆盖矩阵
| 模块 | 目标函数数 | 参考函数数 | 共同唯一 AST 指纹 | Target 覆盖 | Base 覆盖 | 状态 |
| :--- | ---: | ---: | ---: | ---: | ---: | :--- |
| 进程管理 | 106 | 69 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |
| 任务调度/异步运行时 | 253 | 123 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |
| 内存管理 | 271 | 259 | 11 | 4.12% | 4.8% | 双方存在且有结构重合 |
| 中断/异常处理 | 34 | 1 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |
| 系统调用 | 115 | 29 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |
| 文件系统 | 303 | 274 | 2 | 0.78% | 1.22% | 双方存在且有结构重合 |
| 设备驱动 | 39 | 22 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |
| 网络协议栈 | 42 | 0 | 0 | 0.0% | 0.0% | 仅目标项目体现 |
| 文件系统/第三方库 | 45 | 31 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |
| 通用模块/其他 | 78 | 50 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |

### 2. 分模块证据清单

#### 进程管理
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `kernel/src/syscall/mod.rs#sys_execve` | `os/src/syscall/process.rs#sys_execve` | 同板块同名 | copy_path, crate::println!, cwd_inode, exec_resolved, fs::lookup_path, fs::root ...(+5) | Box::<[u8; 4]>::new, DEFAULT_SHELL.to_string, Some, Vec::with_capacity, arg_ptr.is_null, argv.add ...(+22) |
| `kernel/src/syscall/mod.rs#sys_exit` | `os/src/syscall/process.rs#sys_exit` | 同板块同名 | current_task, exit_one_thread | exit_current_and_run_next |
| `kernel/src/syscall/mod.rs#sys_rt_sigtimedwait` | `os/src/syscall/process.rs#sys_prlimit` | 同模块上下文对齐/差异观察 | (*task.tf_ptr()).rewind_syscall, b[0..8].try_into, b[0..8].try_into().unwrap_or, b[8..16].try_into, b[8..16].try_into().unwrap_or, bytes.as_slice ...(+25) | Resource::from_primitive, assert!, copy_from_user, copy_from_user(token, new_limit, rlimit).is_err, copy_to_user, copy_to_user(
                        token,
                        &(RLimit {
                            rlim_cur: SYSTEM_TASK_LIMIT,
                            rlim_max: SYSTEM_TASK_LIMIT,
                        }),
                        old_limit,
                    )
                    .is_err ...(+18) |
| `kernel/src/fs/procfs.rs#lookup` | `os/src/syscall/process.rs#sys_futex` | 同模块上下文对齐/差异观察 | Arc::new, Err, Ok, ProcGenFile::new, ProcWritableFile::new, b"0::/\n".to_vec ...(+10) | FutexOption::from_bits_truncate, current_task, current_task().unwrap, do_futex_wait, drop, info! ...(+15) |

#### 任务调度/异步运行时
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `kernel/src/syscall/mod.rs#sys_read` | `os/src/syscall/fs.rs#sys_read` | 同板块同名 | (*tf).rewind_syscall, Some, crate::arch::now_ticks, crate::fs::notify::active, crate::fs::notify::report, crate::net::poll ...(+35) | UserBuffer::new, current_task, current_task().unwrap, fd_table.get_ref, file_descriptor.read_user, file_descriptor.readable ...(+3) |
| `kernel/src/syscall/mod.rs#sys_ioctl` | `os/src/syscall/fs.rs#sys_ioctl` | 同板块同名 | (bytes / 512).to_le_bytes, 4096u64.to_le_bytes, 512i32.to_le_bytes, READAHEAD.load, READAHEAD.store, TTY_FG_PGID.load ...(+37) | current_task, current_task().unwrap, fd_table.get_ref, file_descriptor.ioctl, task.files.lock |
| `kernel/src/syscall/mod.rs#sys_statx` | `os/src/syscall/fs.rs#sys_statx` | 同板块同名 | Statx::default, check_search_perm, copy_path, crate::fs::inode_identity, current_task, current_task().fd_table.lock ...(+18) | copy_to_user_debug, copy_to_user_debug(token, statx, buf as *mut Statx).is_err, current_task, current_task().unwrap, current_user_token, fd_table.get_ref ...(+10) |
| `kernel/src/syscall/mod.rs#sys_readv` | `os/src/syscall/fs.rs#sys_readv` | 同板块同名 | (*tf).rewind_syscall, core::mem::size_of::<IoVec>, core::slice::from_raw_parts, crate::net::poll, crate::net::tcp_may_recv, crate::task::mark_socket_waiter ...(+15) | UserBuffer::new, Vec::<IOVec>::with_capacity, Vec::with_capacity, copy_from_user_array, copy_from_user_array(token, iov as *const IOVec, iovecs.as_mut_ptr(), iovcnt).is_err, current_task ...(+11) |

#### 内存管理
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `kernel/src/loader/mod.rs#load_elf` | `os/src/task/task.rs#load_elf` | 同板块同名 | ElfFile::new, ElfFile::new(image).map_err, Err, Ok, VirtAddr, VmArea::new ...(+34) | (&memory_set)
            .translate, (&memory_set)
            .translate(VirtAddr::from(self.trap_cx_user_va()).into())
            .unwrap, KERNEL_SPACE.lock, KERNEL_SPACE.lock().token, MemorySet::from_elf, Ok ...(+32) |
| `kernel/src/fs/tmpfs.rs#write_at` | `os/src/mm/page_table.rs#write_at` | 同板块同名 | Err, Ok, buf.len, data.capacity, data.len, data.resize ...(+10) | buffer.len, buffer[copy_buffer_start..copy_buffer_end]
                .copy_from_slice, dst_end.min, dst_start.max, self.buffers.iter_mut, src.len |
| `kernel/src/fs/ext2.rs#create` | `os/src/fs/dev/hwclock.rs#create` | 同板块同名 | DiskInode::empty, Err, Ok, now, parent.kind, put_dirent ...(+10) | todo! |
| `kernel/src/syscall/mod.rs#sys_clone` | `os/src/task/task.rs#sys_clone` | 同板块同名 | crate::task::clone_current, crate::task::current_pid, crate::task::reap_orphan_zombies, crate::task::reclaim_if_low | Arc::downgrade, Arc::new, Futex::new, ITimerVal::new, MemorySet::from_existing_user, Mutex::new ...(+51) |

#### 中断/异常处理
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `kernel/src/signal.rs#default_action` | `os/src/fs/dev/tty.rs#default` | 同模块上下文对齐/差异观察 | 内部闭环逻辑 | 内部闭环逻辑 |

#### 系统调用
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `kernel/src/syscall/socket.rs#sys_getpeername` | `os/src/syscall/socket.rs#sys_getpeername` | 同板块同名 | Ipv4Address::new, Some, net::tcp_remote_endpoint, s.state.lock, s.state.lock().peer.map, st.loopback.as_ref ...(+3) | 内部闭环逻辑 |
| `kernel/src/syscall/socket.rs#sys_listen` | `os/src/syscall/socket.rs#sys_listen` | 同板块同名 | Err, Ok, Some, crate::net::loopback::register_listener, net::tcp_listen, s.state.lock ...(+3) | 内部闭环逻辑 |
| `kernel/src/syscall/mod.rs#sys_set_id` | `user/src/syscall.rs#sys_exec` | 同模块上下文对齐/差异观察 | CREDS.lock, CREDS.lock().insert, SAVED_IDS.lock, SAVED_IDS.lock().insert, creds_of, cur_tgid ...(+2) | args.as_ptr, envp.as_ptr, path.as_ptr, syscall |
| `kernel/src/syscall/keys.rs#sys_add_key` | `os/src/syscall/socket.rs#sys_sendto` | 同模块上下文对齐/差异观察 | ENOTDIR_AS_KEYRING, REG.lock, Vec::new, copy_cstr, copy_in, default_key_perm ...(+11) | 内部闭环逻辑 |

#### 文件系统
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `kernel/src/fs/ext2.rs#unlink` | `os/src/fs/dev/hwclock.rs#unlink` | 同板块同名 | Err, Ok, child.kind, self.di, self.di()?.kind, self.fs.dir_lookup ...(+5) | todo! |
| `kernel/src/fs/tmpfs.rs#create` | `os/src/fs/directory_tree.rs#create` | 同板块同名 | Arc::new, AtomicBool::new, BTreeMap::new, Err, Meta::default, Mutex::new ...(+7) | self.file.create |
| `kernel/src/syscall/mod.rs#sys_umount2` | `os/src/syscall/fs.rs#sys_umount2` | 同板块同名 | copy_path, cwd_inode, err_to_isize, fs::root, fs::split_parent, fs::umount_at ...(+1) | UmountFlags::from_bits, current_user_token, info!, target.is_null, translated_str, warn! |
| `kernel/src/fs/notify.rs#flush` | `os/src/drivers/serial/ns16550a.rs#flush` | 同板块同名 | WATCH_COUNT.fetch_sub, m.len, m.retain, self.marks.lock | Err, Ok, read_volatile |

#### 设备驱动
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `kernel/src/drivers/virtio_blk.rs#probe_mmio` | `os/src/drivers/block/sata_blk.rs#pci_init` | 同模块上下文对齐/差异观察 | Arc::new, BlkInner::Mmio, BlockCache::new, MmioTransport::new, Mutex::new, NonNull::new ...(+7) | AHCI::new, Some, Status::empty, dev.bars.iter, dev.bars.iter().enumerate, dev.bars.iter().enumerate().for_each ...(+3) |
| `kernel/src/drivers/pci.rs#assign_bars` | `os/src/drivers/block/sata_blk.rs#enable` | 同模块上下文对齐/差异观察 | align_up, aligned.checked_add, aligned.checked_add(size).map_or, crate::println!, info.takes_two_entries, root.bar_info ...(+2) | am.read16, am.write32 |
| `kernel/src/drivers/virtio_net.rs#init_mmio` | `os/src/drivers/block/mod.rs#block_device_test` | 同模块上下文对齐/差异观察 | Arc::new, MmioTransport::new, Mutex::new, NET.call_once, NetInner::Mmio, NonNull::new ...(+7) | BLOCK_DEVICE.clone, assert_eq!, block_device.read_block, block_device.write_block, println!, write_buffer.iter_mut |
| `kernel/src/drivers/virtio_net.rs#init_pci` | `os/src/mm/map_area.rs#force_swap_out` | 同模块上下文对齐/差异观察 | Arc::new, Mutex::new, NET.call_once, NetInner::Pci, PciTransport::new::<KernelHal>, Some ...(+5) | Err, Frame::SwappedOut, Ok, SWAP_DEVICE.lock, SWAP_DEVICE.lock().write, frame_ref.ppn.get_bytes_array |

#### 网络协议栈
仅目标项目中观察到该模块代表函数，参考项目没有同模块可对齐证据：
- 目标项目：`kernel/src/net/mod.rs#udp_send`，调用摘要：IpAddress::Ipv4, STACK.get, STACK.get().ok_or, data.len, g.sockets.get_mut::<udp::Socket>, s.lock ...(+6)
- 目标项目：`kernel/src/net/mod.rs#udp_recv`，调用摘要：Err, Ok, STACK.get, STACK.get().ok_or, g.sockets.get_mut::<udp::Socket>, s.lock ...(+1)
- 目标项目：`kernel/src/net/mod.rs#add_udp_socket`，调用摘要：STACK.get, Some, g.sockets.add, s.lock, udp::PacketBuffer::new, udp::Socket::new ...(+1)
- 目标项目：`kernel/src/net/mod.rs#add_tcp_socket`，调用摘要：STACK.get, Some, g.sockets.add, s.lock, sock.set_nagle_enabled, tcp::Socket::new ...(+2)

#### 文件系统/第三方库
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `kernel/src/fs/ext4.rs#create` | `os/src/fs/ext4_file.rs#create` | 同板块同名 | Arc::new, Err, Ok, TmpfsDir::new_root, TmpfsFile::new, added.contains_key ...(+11) | todo! |
| `kernel/src/fs/ext4.rs#unlink` | `os/src/fs/ext4_file.rs#unlink` | 同板块同名 | Err, Ok, added.remove, added.remove(name).is_some, m.contains_key, name.to_string ...(+9) | todo! |
| `kernel/src/fs/ext4.rs#parse` | `os/src/mm/mod.rs#ext4_user_malloc` | 同模块上下文对齐/差异观察 | Err, Ok, Self::read_blocks_raw, Vec::with_capacity, blk.read_block, blk.read_block(2, &mut tmp).map_err ...(+7) | HEAP_ALLOCATOR
        .lock, HEAP_ALLOCATOR
        .lock()
        .alloc, HEAP_ALLOCATOR
        .lock()
        .alloc(Layout::array::<u8>(size).unwrap())
        .unwrap, HEAP_ALLOCATOR
        .lock()
        .alloc(Layout::array::<u8>(size).unwrap())
        .unwrap()
        .as_ptr, Layout::array::<u8>, Layout::array::<u8>(size).unwrap |
| `kernel/src/fs/ext4.rs#build_children` | `os/src/fs/ext4_file.rs#get_single_cache` | 同模块上下文对齐/差异观察 | Arc::new, BTreeMap::new, BTreeSet::new, Mutex::new, Ok, Some ...(+8) | todo! |

#### 通用模块/其他
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `kernel/src/console.rs#write_str` | `os/src/console.rs#write_str` | 同板块同名 | Ok, crate::arch::console_put, s.as_bytes | Ok, console_flush, console_putchar, s.chars |
| `user/git/src/main.rs#cmd_self_test` | `user/src/lib.rs#_parameter` | 同模块上下文对齐/差异观察 | ExitCode::from, git_blob_sha1, io::stdout, io::stdout().flush, io::stdout().flush().ok, print! ...(+1) | ((argv + i * core::mem::size_of::<usize>()) as *const usize).read_volatile, ((str_start + *i) as *const u8).read_volatile, (0usize..)
            .find, (0usize..)
            .find(|i| unsafe { ((str_start + *i) as *const u8).read_volatile() == 0 })
            .unwrap, HEAP.lock, HEAP.lock()
            .init ...(+10) |
| `user/git/src/main.rs#main` | `os/src/main.rs#mem_clear` | 同模块上下文对齐/差异观察 | ExitCode::from, argv.len, argv[1].as_str, cmd_config, cmd_hash_object, cmd_init ...(+9) | core::slice::from_raw_parts_mut, core::slice::from_raw_parts_mut(
            sbss as usize as *mut u8,
            crate::config::MEMORY_END - sbss as usize,
        )
        .fill, core::slice::from_raw_parts_mut(sbss as usize as *mut u8, ebss as usize - sbss as usize)
            .fill |
| `kernel/src/ksyms.rs#resolve` | `user/src/lib.rs#_start` | 同模块上下文对齐/差异观察 | Some, at, b.get, blob, core::str::from_utf8, core::str::from_utf8(b.get(s..t.strtab_off + end)?).ok ...(+7) | _parameter, asm!, unreachable! |


#### 1. 宏观判定
- **定性结论**: 独立开发 (相似度: 0.92%，共同 AST 指纹: 16；唯一 AST 指纹: 目标项目 1112，参考项目 642；覆盖率: 目标项目 1.44%，参考项目 2.49%)

#### 2. 全模块覆盖矩阵解读
- **进程管理**: 双方均存在该模块。共同 AST 指纹为 0，覆盖率均为 0.0%。这表明双方在进程创建、调度、信号处理等核心逻辑的底层 AST 结构上无任何重合。证据强度：**未形成**。
- **任务调度/异步运行时**: 双方均存在该模块。共同 AST 指纹为 0，覆盖率均为 0.0%。尽管双方都实现了异步任务调度，但底层实现路径（如 Future 的调度方式、执行器模型）在 AST 层面完全不同。证据强度：**未形成**。
- **内存管理**: 双方均存在该模块。共同 AST 指纹为 11，目标覆盖率 4.12%，参考覆盖率 4.8%。这是唯一存在结构性重合的模块，但重合度极低。证据强度：**弱**。
- **中断/异常处理**: 双方均存在该模块。共同 AST 指纹为 0，覆盖率均为 0.0%。目标项目有 34 个函数，而参考项目仅 1 个，规模差异巨大，表明中断处理框架的实现策略完全不同。证据强度：**未形成**。
- **系统调用**: 双方均存在该模块。共同 AST 指纹为 0，覆盖率均为 0.0%。系统调用是标准接口，但双方在参数解析、权限校验、内核态数据拷贝等底层实现上无 AST 重合。证据强度：**未形成**。
- **文件系统**: 双方均存在该模块。共同 AST 指纹为 2，目标覆盖率 0.78%，参考覆盖率 1.22%。重合度极低，且可能源于对标准文件系统结构（如 Inode）的通用实现。证据强度：**弱**。
- **设备驱动**: 双方均存在该模块。共同 AST 指纹为 0，覆盖率均为 0.0%。双方驱动的硬件平台和驱动模型（如 VirtIO vs SATA）完全不同。证据强度：**未形成**。
- **网络协议栈**: 仅目标项目体现。参考项目无此模块，无法进行同源性比对。证据强度：**未形成**。
- **第三方库/公共依赖**: 双方均存在该模块。共同 AST 指纹为 0，覆盖率均为 0.0%。双方使用的第三方库或公共工具函数在 AST 层面无重合。证据强度：**未形成**。

#### 3. 底层 AST 关键证据指纹表
| 所属板块 | 目标项目路径 | 参考项目路径 | 证据类型 | 技术说明 |
| :--- | :--- | :--- | :--- | :--- |
| 内存管理 | `kernel/src/loader/mod.rs#load_elf` | `os/src/task/task.rs#load_elf` | 同板块同名 | 双方均实现了 ELF 加载功能，但目标项目使用 `ElfFile::new` 和 `VmArea::new` 构建内存区域，参考项目则通过 `MemorySet::from_elf` 和手动页表翻译。两者在 ELF 解析与内存映射的 AST 调用链上完全不同。 |
| 内存管理 | `kernel/src/fs/tmpfs.rs#write_at` | `os/src/mm/page_table.rs#write_at` | 同板块同名 | 函数名相同但功能迥异。目标项目是 tmpfs 文件的数据写入，操作 `Vec<u8>` 缓冲区；参考项目是页表项的写入操作，操作物理页帧。属于同名不同义，不构成复用。 |
| 内存管理 | `kernel/src/fs/ext2.rs#create` | `os/src/fs/dev/hwclock.rs#create` | 同板块同名 | 目标项目是 ext2 文件系统的 inode 创建，涉及磁盘操作；参考项目是硬件时钟设备的创建，函数体为 `todo!`。两者无任何关联。 |
| 内存管理 | `kernel/src/syscall/mod.rs#sys_clone` | `os/src/task/task.rs#sys_clone` | 同板块同名 | 目标项目的 `sys_clone` 是系统调用入口，调用 `clone_current` 等高层封装；参考项目是 `TaskControlBlock` 的方法，直接操作 `MemorySet`、`Futex` 等底层结构。抽象层次和实现路径完全不同。 |
| 文件系统 | `kernel/src/fs/ext2.rs#unlink` | `os/src/fs/dev/hwclock.rs#unlink` | 同板块同名 | 目标项目是 ext2 文件系统的删除操作；参考项目是硬件时钟设备，函数体为 `todo!`。无关联。 |
| 文件系统 | `kernel/src/fs/tmpfs.rs#create` | `os/src/fs/directory_tree.rs#create` | 同板块同名 | 目标项目在 tmpfs 中创建 inode；参考项目在目录树中创建文件节点。两者管理的对象和数据结构不同。 |
| 进程管理 | `kernel/src/syscall/mod.rs#sys_execve` | `os/src/syscall/process.rs#sys_execve` | 同板块同名 | 标准系统调用。目标项目通过 `fs::lookup_path` 等文件系统抽象解析路径；参考项目则手动遍历用户态指针数组 `argv`。实现策略截然不同。 |
| 进程管理 | `kernel/src/syscall/mod.rs#sys_exit` | `os/src/syscall/process.rs#sys_exit` | 同板块同名 | 标准系统调用。目标项目调用 `exit_one_thread`，参考项目调用 `exit_current_and_run_next`，内部调度逻辑不同。 |

#### 4. 核心代码调用流对比剖析
* **【进程管理/execve 系统调用】**
* **底层技术析理**: 两者均实现了 POSIX `execve` 标准，但底层路径解析与参数加载策略存在根本性差异。目标项目采用**文件系统抽象层**，通过 `fs::lookup_path` 和 `cwd_inode` 进行路径解析，并使用 `read_string_array` 统一读取用户态参数，体现了分层设计思想。参考项目则采用**底层指针直接操作**模式，手动遍历 `argv` 指针并逐个 `insert` 到 `Vec` 中，且包含 `DEFAULT_SHELL` 回退逻辑，更贴近早期操作系统的实现风格。两者在 AST 调用链上无重合节点。
* **代码溯源链接**:
  - 目标项目: `kernel/src/syscall/mod.rs#sys_execve`
  - 参考项目: `os/src/syscall/process.rs#sys_execve`

* **【进程管理/Futex 等待机制】**
* **底层技术析理**: 两者均实现了 Futex 等待，但同步原语和数据结构截然不同。目标项目使用**全局 `FUTEXES` 映射表** (`BTreeMap`) 配合 `VecDeque` 管理等待队列，时间度量基于 `ticks`。参考项目则为每个任务维护一个 `Futex` 结构体，内部使用 `WaitQueue`，时间度量基于 `TimeSpec`。目标项目的 `futex_wait` 调用 `FUTEXES.lock().entry(key).or_default().push_back`，参考项目的 `do_futex_wait` 调用 `futex.inner.insert`。两者在锁机制、队列管理和时间抽象上无共同 AST 指纹。
* **代码溯源链接**:
  - 目标项目: `kernel/src/sync/futex.rs#futex_wait`
  - 参考项目: `os/src/task/threads.rs#do_futex_wait`

* **【内存管理/ELF 加载】**
* **底层技术析理**: 双方均需解析并加载 ELF 文件，但内存映射策略完全不同。目标项目通过 `ElfFile::new` 解析 ELF，然后遍历段表，使用 `VmArea::new` 为每个段创建独立的虚拟内存区域，体现了现代内核的 VMA 管理思想。参考项目则调用 `MemorySet::from_elf` 一次性完成解析和映射，并手动进行页表翻译 (`(&memory_set).translate`)。两者在 AST 层面无复用痕迹。
* **代码溯源链接**:
  - 目标项目: `kernel/src/loader/mod.rs#load_elf`
  - 参考项目: `os/src/task/task.rs#load_elf`

#### 5. 单方模块与降权项
- **单方模块**:
  - **网络协议栈**: 仅存在于目标项目，包含 `udp_send`、`tcp_listen` 等完整实现。参考项目无此模块，无法比对，不构成同源证据。
- **降权项**:
  - **标准系统调用同名**: `sys_execve`、`sys_exit`、`sys_clone` 等均为 POSIX/Linux 标准接口名称，同名是规范要求，不构成抄袭证据。
  - **Rust 语言惯用法**: `Arc::new`、`Mutex::new`、`iter().find(...)`、`entry().or_default()` 等是 Rust 生态的标准写法，不能作为代码复用的特异性指纹。
  - **标准输出格式**: `gen_limits` 中的字符串是 Linux `/proc/self/limits` 的标准格式，属于规范文本。
  - **第三方库/公共依赖**: 双方均引用了部分外部 crate，但 AST 指纹显示无共同节点，表明依赖库的选择和使用方式不同。

#### 6. 保守结论
本次审计的客观 AST 相似度仅为 0.92%，项目整体覆盖率低于 3%，【进程管理】等核心板块的共同 AST 指纹为 0。这一量化指标从根本上否定了系统性代码复用的可能性。控方提出的“逻辑流复用”指控，本质上是将 POSIX 标准语义和 Rust 语言惯用法误判为抄袭证据。双方在内存管理、文件系统等存在微弱重合的模块，其重合点也仅限于少量同名函数，且经剖析均为同名不同义或标准接口实现。综上，[2026-T2026105549911448-XX] 与 [2024-NPUcore-IMPACT-西北工业大学] 在底层架构、抽象层次、同步策略上存在显著差异，应认定为独立开发项目，不存在代码抄袭或违规复用行为。

------------------------------

