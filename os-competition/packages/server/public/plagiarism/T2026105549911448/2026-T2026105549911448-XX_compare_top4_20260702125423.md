# 🛡️ OS 内核赛自动化代码级架构审计报告

**审计时间**: `2026-07-02 12:54:23`
**参赛作品名称**: `2026-T2026105549911448-XX`
**年份**: `2026`
**学校**: `XX`
**队伍名称**: `T2026105549911448`
**比赛名字**: `OS-Competition`
**比赛子赛道**: `Kernel-Implementation`
**仓库地址**: `https://gitlab.eduxiji.net/educg-group-43501-3132633/T2026105549911448-3008`
**推测开发语言**: `Rust / C (由AST探针自动识别)`
**特征函数量**: `1286`

## ⚔️ 底层代码同源性对比 (AST Homology)
> 本部分由专门的代码审查智能体，对该项目与符合年份条件的历届最高相似度项目进行控制流与底层调用对比。新版报告会先给出确定性的全模块覆盖矩阵，再给出 Agent 的分模块解释，避免只围绕少量抽样函数展开。

### 🏆 Top 4 底层对标: vs `2024-ᕕڼᕗ旺仔-哈尔滨工业大学`（哈尔滨工业大学 / ᕕ(◠ڼ◠)ᕗ旺仔）

#### 对方作品信息

**对方年份**: `2024`
**对方学校**: `哈尔滨工业大学`
**对方队伍名称**: `ᕕ(◠ڼ◠)ᕗ旺仔`
**对方比赛名字**: `OS-Competition`
**对方比赛子赛道**: `Kernel-Implementation`
**对方仓库地址**: `https://gitlab.eduxiji.net/educg-group-26010-2376550/T202410213992561-2280`

> 双向覆盖率：共同 AST 指纹: 17；唯一 AST 指纹: 目标项目 1112，参考项目 834；覆盖率: 目标项目 1.53%，参考项目 2.04%

## 全模块对比总览（确定性证据）

> 总体覆盖率：共同 AST 指纹: 17；唯一 AST 指纹: 目标项目 1112，参考项目 834；覆盖率: 目标项目 1.53%，参考项目 2.04%

### 1. 模块覆盖矩阵
| 模块 | 目标函数数 | 参考函数数 | 共同唯一 AST 指纹 | Target 覆盖 | Base 覆盖 | 状态 |
| :--- | ---: | ---: | ---: | ---: | ---: | :--- |
| 进程管理 | 106 | 68 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |
| 任务调度/异步运行时 | 253 | 55 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |
| 内存管理 | 271 | 180 | 7 | 2.62% | 4.24% | 双方存在且有结构重合 |
| 中断/异常处理 | 34 | 13 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |
| 系统调用 | 115 | 56 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |
| 文件系统 | 303 | 200 | 1 | 0.39% | 0.53% | 双方存在且有结构重合 |
| 设备驱动 | 39 | 7 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |
| 网络协议栈 | 42 | 0 | 0 | 0.0% | 0.0% | 仅目标项目体现 |
| 文件系统/第三方库 | 45 | 273 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |
| 通用模块/其他 | 78 | 86 | 1 | 1.49% | 1.2% | 双方存在且有结构重合 |

### 2. 分模块证据清单

#### 进程管理
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `kernel/src/syscall/mod.rs#sys_wait4` | `os/src/syscall/process.rs#sys_wait4` | 同板块同名 | (*me.tf_ptr()).rewind_syscall, c.state.lock, children
            .iter, children
            .iter()
            .filter_map, children
            .iter()
            .filter_map(|&cpid| crate::task::task_by_pid(cpid))
            .find, code.to_le_bytes ...(+10) | WaitFuture::new, WaitOption::from_bits, WaitOption::from_bits(options).unwrap, trace! |
| `kernel/src/syscall/mod.rs#sys_execve` | `os/src/syscall/process.rs#sys_execve` | 同板块同名 | copy_path, crate::println!, cwd_inode, exec_resolved, fs::lookup_path, fs::root ...(+5) | Err, Ok, Path::from, Vec::new, all_data.as_slice, app_inode.read_all ...(+15) |
| `kernel/src/syscall/mod.rs#sys_exit` | `os/src/syscall/process.rs#sys_exit` | 同板块同名 | current_task, exit_one_thread | Ok, exit_current, trace! |
| `kernel/src/sync/futex.rs#futex_wait` | `user/src/bin/initproc.rs#main` | 同模块上下文对齐/差异观察 | (*task.tf_ptr()).rewind_syscall, FUTEXES.lock, FUTEXES.lock().entry, FUTEXES.lock().entry(key).or_default, FUTEXES.lock().entry(key).or_default().push_back, crate::arch::now_ticks ...(+8) | WaitOption::empty, execve, execve(SHELL, &[SHELL], &[]).expect, fork, fork().expect, print! ...(+5) |

#### 任务调度/异步运行时
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `kernel/src/syscall/mod.rs#sys_read` | `os/src/syscall/fs.rs#sys_read` | 同板块同名 | (*tf).rewind_syscall, Some, crate::arch::now_ticks, crate::fs::notify::active, crate::fs::notify::report, crate::net::poll ...(+35) | Err, Ok, core::slice::from_raw_parts_mut, current_process, fdinfo
            .file
            .read, inner.fd_table.get ...(+1) |
| `kernel/src/syscall/mod.rs#sys_readv` | `os/src/syscall/fs.rs#sys_readv` | 同板块同名 | (*tf).rewind_syscall, core::mem::size_of::<IoVec>, core::slice::from_raw_parts, crate::net::poll, crate::net::tcp_may_recv, crate::task::mark_socket_waiter ...(+15) | Err, Ok, current_process, fdinfo.file.read, inner.fd_table.get, iovec.iter_mut ...(+3) |
| `kernel/src/syscall/mod.rs#sys_close` | `os/src/syscall/fs.rs#sys_close` | 同板块同名 | Arc::as_ptr, FLOCK_RANGES.lock, Some, closing
        .as_ref, closing
        .as_ref()
        .map, crate::fs::notify::active ...(+16) | current_process, process.inner_lock, process.inner_lock().fd_table.close, trace! |
| `kernel/src/main.rs#panic` | `os/src/lang_items.rs#panic` | 同板块同名 | AtomicBool::new, PANICKING.swap, arch::shutdown, println! | current_thread, info.location, info.message, kernel!, shutdown, thread.get_inner_mut ...(+1) |

#### 内存管理
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `kernel/src/syscall/mod.rs#sys_fcntl` | `os/src/syscall/fs.rs#sys_fcntl` | 同板块同名 | Flock::default, Some, b[0..4].try_into, b[0..4].try_into().unwrap_or, b[4..8].try_into, b[4..8].try_into().unwrap_or ...(+55) | Err, FcntlFlags::FD_CLOEXEC.bits, FcntlFlags::from_bits, FcntlFlags::from_bits(arg as u32).ok_or, Ok, OpenFlags::from_bits ...(+17) |
| `kernel/src/syscall/mod.rs#sys_openat` | `os/src/syscall/fs.rs#sys_openat` | 同板块同名 | Arc::new, File::from_inode, Some, String::new, abs.is_empty, alloc::format! ...(+33) | Err, FdInfo::default_flags, Ok, OpenFlags::from_bits, OpenFlags::from_bits(flags).ok_or, Path::from ...(+15) |
| `kernel/src/syscall/mod.rs#sys_mmap` | `os/src/syscall/mm.rs#sys_mmap` | 同板块同名 | Some, crate::println!, current_task, err_to_isize, file.inode.read_at, init.as_deref ...(+7) | Err, MMAPPROT::from_bits, MMAPPROT::from_bits(prot as u32).ok_or, MmapFlags::from_bits, MmapFlags::from_bits(flags as u32).ok_or, Ok ...(+26) |
| `kernel/src/mm/memory_set.rs#fork` | `os/src/task/task.rs#fork` | 同板块同名 | Arc::clone, Arc::new, MemorySet::try_new, Some, alloc::collections::BTreeMap::new, alloc_frame ...(+9) | Arc::downgrade, Arc::new, BTreeMap::new, Default::default, MemorySet::from_existed_user_lazily, PROCESS_MANAGER
            .lock ...(+20) |

#### 中断/异常处理
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `kernel/src/signal.rs#default_action` | `os/src/syscall/fs.rs#sys_sendfile` | 同模块上下文对齐/差异观察 | 内部闭环逻辑 | Err, Ok, in_file.read, in_file.seek, in_file.seek(*offset as usize).ok_or, offset.is_null ...(+6) |
| `kernel/src/arch/riscv64/trap.rs#init` | `os/src/fs/tty.rs#new` | 同模块上下文对齐/差异观察 | arm_timer, riscv::register::sie::set_stimer, riscv::register::sscratch::write, stvec::write | 内部闭环逻辑 |
| `kernel/src/arch/loongarch64/trap.rs#csrrd` | `user/src/lib.rs#nanosleep` | 同模块上下文对齐/差异观察 | core::arch::asm!, unreachable! | Err, Ok, Some, ret.into, sys_nanosleep |
| `kernel/src/arch/loongarch64/trap.rs#arm_timer` | `os/src/mm/user_check/mod.rs#try_write_user` | 同模块上下文对齐/差异观察 | core::arch::asm! | Some, __try_write_user_u8 |

#### 系统调用
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `kernel/src/syscall/mod.rs#sys_uname` | `os/src/syscall/util.rs#sys_uname` | 同板块同名 | DOMAINNAME.lock, HOSTNAME.lock, domain.is_empty, host.is_empty, write_field, write_field_bytes ...(+1) | Ok, Utsname::default, ptr::write, trace! |
| `kernel/src/syscall/mod.rs#sys_times` | `os/src/syscall/util.rs#sys_times` | 同板块同名 | crate::arch::now_ticks, write_struct | Ok, Tms::default, ptr::write, trace!, warn! |
| `kernel/src/syscall/mod.rs#sys_clock_getres` | `os/src/syscall/util.rs#sys_clock_getres` | 同板块同名 | write_struct | Ok, res.write_volatile, trace!, warn! |
| `user/hello/src/main.rs#write_str` | `user/src/console.rs#write_str` | 同板块同名 | s.as_ptr, s.len, syscall3 | Ok, s.as_bytes, sys_write |

#### 文件系统
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `kernel/src/syscall/mod.rs#sys_unlinkat` | `os/src/syscall/fs.rs#sys_unlinkat` | 同板块同名 | Some, copy_path, crate::fs::notify::inode_gone, crate::fs::notify::report, entries.is_empty, err_to_isize ...(+13) | Err, Ok, OpenFlags::empty, Path::from, c_str_to_string, inode.delete ...(+4) |
| `kernel/src/syscall/mod.rs#sys_faccessat` | `os/src/syscall/fs.rs#sys_faccessat` | 同板块同名 | check_search_perm, copy_path, creds_of, cur_tgid, inode_perm, path_str.is_empty ...(+1) | FaccessatFlags::from_bits, FaccessatFlags::from_bits(amode).ok_or, FcntlFlags::from_bits, FcntlFlags::from_bits(flag).ok_or, Ok, OpenFlags::empty ...(+5) |
| `kernel/src/fs/mod.rs#seek` | `os/src/fs/os_inode.rs#seek` | 同板块同名 | Err, Ok, self.inode.size, self.offset.lock | Some, self.inner_handler |
| `kernel/src/fs/fat32.rs#list` | `os/src/fs/inode.rs#list` | 同板块同名 | Ok, cache
            .as_ref, cache
            .as_ref()
            .unwrap, cache
            .as_ref()
            .unwrap()
            .iter, cache
            .as_ref()
            .unwrap()
            .iter()
            .map, cache
            .as_ref()
            .unwrap()
            .iter()
            .map(|(k, v)| (k.clone(), v.kind()))
            .collect ...(+4) | Err, Ok, children.values, children.values().cloned, children.values().cloned().collect, self.clone ...(+2) |

#### 设备驱动
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `kernel/src/drivers/virtio_blk.rs#probe_mmio` | `os/src/drivers/block/mod.rs#block_device_test` | 同模块上下文对齐/差异观察 | Arc::new, BlkInner::Mmio, BlockCache::new, MmioTransport::new, Mutex::new, NonNull::new ...(+7) | BLOCK_DEVICE.clone, assert_eq!, block_device.read_block, block_device.write_block, println!, write_buffer.iter_mut |
| `kernel/src/drivers/pci.rs#assign_bars` | `os/src/drivers/block/virtio_blk.rs#read_block` | 同模块上下文对齐/差异观察 | align_up, aligned.checked_add, aligned.checked_add(size).map_or, crate::println!, info.takes_two_entries, root.bar_info ...(+2) | self.0
            .exclusive_access, self.0
            .exclusive_access()
            .read_block, self.0
            .exclusive_access()
            .read_block(block_id, buf)
            .expect |
| `kernel/src/drivers/virtio_net.rs#init_mmio` | `os/src/drivers/block/virtio_blk.rs#write_block` | 同模块上下文对齐/差异观察 | Arc::new, MmioTransport::new, Mutex::new, NET.call_once, NetInner::Mmio, NonNull::new ...(+7) | self.0
            .exclusive_access, self.0
            .exclusive_access()
            .write_block, self.0
            .exclusive_access()
            .write_block(block_id, buf)
            .expect |
| `kernel/src/drivers/virtio_net.rs#init_pci` | `os/src/drivers/block/virtio_blk.rs#new` | 同模块上下文对齐/差异观察 | Arc::new, Mutex::new, NET.call_once, NetInner::Pci, PciTransport::new::<KernelHal>, Some ...(+5) | Self, UPSafeCell::new, VirtIOBlk::<VirtioHal>::new, VirtIOBlk::<VirtioHal>::new(&mut *(VIRTIO0 as *mut VirtIOHeader)).unwrap |

#### 网络协议栈
仅目标项目中观察到该模块代表函数，参考项目没有同模块可对齐证据：
- 目标项目：`kernel/src/net/mod.rs#udp_send`，调用摘要：IpAddress::Ipv4, STACK.get, STACK.get().ok_or, data.len, g.sockets.get_mut::<udp::Socket>, s.lock ...(+6)
- 目标项目：`kernel/src/net/mod.rs#udp_recv`，调用摘要：Err, Ok, STACK.get, STACK.get().ok_or, g.sockets.get_mut::<udp::Socket>, s.lock ...(+1)
- 目标项目：`kernel/src/net/mod.rs#add_udp_socket`，调用摘要：STACK.get, Some, g.sockets.add, s.lock, udp::PacketBuffer::new, udp::Socket::new ...(+1)
- 目标项目：`kernel/src/net/mod.rs#add_tcp_socket`，调用摘要：STACK.get, Some, g.sockets.add, s.lock, sock.set_nagle_enabled, tcp::Socket::new ...(+2)

#### 文件系统/第三方库
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `kernel/src/fs/ext4.rs#parse` | `ext4/src/ext4_interface.rs#ext4_generic_open` | 同模块上下文对齐/差异观察 | Err, Ok, Self::read_blocks_raw, Vec::with_capacity, blk.read_block, blk.read_block(2, &mut tmp).map_err ...(+7) | DirEntryType::EXT4_DE_DIR.bits, Errnum::ENOENT.into, Ext4DirEntry::default, Ext4DirSearchResult::new, Ext4InodeRef::get_inode_ref, Ext4InodeRef::new ...(+17) |
| `kernel/src/fs/ext4.rs#build_children` | `ext4/src/ext4_interface.rs#ext4_file_read` | 同模块上下文对齐/差异观察 | Arc::new, BTreeMap::new, BTreeSet::new, Mutex::new, Ok, Some ...(+8) | Ext4InodeRef::get_inode_ref, Ok, core::cmp::min, inode_ref.get_inode_dblk_idx, inode_ref.inner.inode.ext4_inode_type, inode_ref.inner.inode.inode_get_size ...(+8) |
| `kernel/src/fs/ext4.rs#walk_extent` | `ext4/src/ext4_interface.rs#ext4_generic_open_from` | 同模块上下文对齐/差异观察 | Err, Ok, node_bytes.len, out.push, self.read_block, self.walk_extent ...(+2) | DirEntryType::EXT4_DE_DIR.bits, Errnum::ENOENT.into, Ext4DirEntry::default, Ext4DirSearchResult::new, Ext4InodeRef::get_inode_ref, Ext4InodeRef::new ...(+17) |
| `kernel/src/fs/ext4.rs#read_range` | `ext4/src/ext4_interface.rs#ext4_generic_open2` | 同模块上下文对齐/差异观察 | Ok, Some, buf.len, buf[done..done + take].copy_from_slice, buf[done..done + take].iter_mut, core::cmp::min ...(+4) | DirEntryType::EXT4_DE_DIR.bits, Errnum::ENOENT.into, Ext4DirEntry::default, Ext4DirSearchResult::new, Ext4InodeRef::get_inode_ref, Ext4InodeRef::new ...(+16) |

#### 通用模块/其他
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `kernel/src/sync/spinlock.rs#try_lock` | `os/src/mutex/spin_mutex.rs#try_lock` | 同板块同名 | Some, preempt_disable, preempt_enable, self.inner.try_lock | S::after_unlock, S::before_lock, Some, self
            .lock
            .compare_exchange, self
            .lock
            .compare_exchange(false, true, Ordering::Acquire, Ordering::Relaxed)
            .is_ok, self.lock.load |
| `kernel/src/arch/riscv64/power.rs#shutdown` | `os/src/sbi.rs#shutdown` | 同板块同名 | core::arch::asm!, sbi_rt::system_reset, sifive_exit | panic!, sbi_call |
| `kernel/src/console.rs#write_str` | `os/src/console.rs#write_str` | 同板块同名 | Ok, crate::arch::console_put, s.as_bytes | Ok, console_putchar, s.chars |
| `kernel/src/sync/spinlock.rs#lock` | `os/src/mutex/spin_mutex.rs#lock` | 同板块同名 | Some, preempt_disable, self.inner.lock | S::before_lock, self
                .lock
                .compare_exchange, self
                .lock
                .compare_exchange(false, true, Ordering::Acquire, Ordering::Relaxed)
                .is_ok, self.wait_unlock |


好的，我将遵循审计指令，基于客观 AST 证据进行最终定性仲裁。

### 1. 宏观判定
- **定性结论**: **独立开发 (相似度: 0.88%，共同 AST 指纹: 17；唯一 AST 指纹: 目标项目 1112，参考项目 834；覆盖率: 目标项目 1.53%，参考项目 2.04%)**
- **判定依据**: 双方项目在宏观 AST 指纹层面呈现出极低的相似度（0.88%），共同指纹仅 17 个。在控方重点指控的【进程管理】板块，共同唯一 AST 指纹为 0，覆盖率均为 0.0%，不存在任何结构级代码复用。双方在并发模型、架构支持、功能完整性上存在系统性差异，符合独立开发的特征。

### 2. 全模块覆盖矩阵解读
- **进程管理**: 双方均存在。共同 AST 指纹为 0，覆盖率均为 0.0%。尽管存在同名系统调用，但底层调用图谱完全不同，反映了同步阻塞与异步运行时两种架构的根本差异。证据强度：**未形成**。
- **任务调度/异步运行时**: 双方均存在。共同 AST 指纹为 0，覆盖率均为 0.0%。目标项目采用同步阻塞模型，参考项目采用基于 `executor` 的异步模型，两者在调度器实现上无任何结构重合。证据强度：**未形成**。
- **内存管理**: 双方均存在。存在 7 个共同 AST 指纹，目标覆盖率 2.62%，参考覆盖率 4.24%。重合点主要源于 Rust 标准库的通用构造（如 `Arc::new`, `BTreeMap::new`）和 POSIX 规范下的系统调用参数解析（如 `sys_mmap` 的标志位解析）。证据强度：**弱**。
- **中断/异常处理**: 双方均存在。共同 AST 指纹为 0，覆盖率均为 0.0%。目标项目包含多架构（RISC-V, LoongArch）的底层汇编及计时器初始化，参考项目则较为单一，无结构重合。证据强度：**未形成**。
- **系统调用**: 双方均存在。共同 AST 指纹为 0，覆盖率均为 0.0%。同名系统调用（如 `sys_uname`, `sys_times`）由 ABI 强制规定，其内部实现调用的对象（如 `write_struct` vs `ptr::write`）完全不同。证据强度：**未形成**。
- **文件系统**: 双方均存在。存在 1 个共同 AST 指纹，目标覆盖率 0.39%，参考覆盖率 0.53%。重合度极低，目标项目实现了完整的动态 procfs、ext4、tmpfs 等，而参考项目多为静态占位或 `panic!` 实现。证据强度：**弱**。
- **设备驱动**: 双方均存在。共同 AST 指纹为 0，覆盖率均为 0.0%。目标项目包含 virtio 网络和块设备驱动，参考项目仅有块设备驱动，且实现方式（MMIO vs 直接内存映射）不同。证据强度：**未形成**。
- **网络协议栈**: 仅目标项目存在（42 个函数），参考项目无此模块。这体现了目标项目在功能完整性上的独立扩展。证据强度：**未形成**。
- **第三方库/公共依赖**: 双方均存在。共同 AST 指纹为 0，覆盖率均为 0.0%。目标项目自研了 ext4 解析逻辑，参考项目依赖外部 `ext4` 库，两者在文件系统实现上路径完全不同。证据强度：**未形成**。

### 3. 底层 AST 关键证据指纹表
| 所属板块 | 目标项目路径 | 参考项目路径 | 证据类型 | 技术说明 |
| :--- | :--- | :--- | :--- | :--- |
| 进程管理 | `kernel/src/syscall/mod.rs#sys_execve` | `os/src/syscall/process.rs#sys_execve` | 弱信号 | 同名系统调用，但调用流完全不同。目标项目使用 `copy_path`, `exec_resolved` 等 VFS 路径；参考项目使用 `Path::from`, `app_inode.read_all` 等简化路径。无共同外部调用节点。 |
| 进程管理 | `kernel/src/syscall/mod.rs#sys_wait4` | `os/src/syscall/process.rs#sys_wait4` | 弱信号 | 同名系统调用，但并发模型迥异。目标项目是同步轮询（`children.iter().filter_map`），参考项目是异步 `WaitFuture`。无共同外部调用节点。 |
| 进程管理 | `kernel/src/syscall/mod.rs#sys_exit` | `os/src/syscall/process.rs#sys_exit` | 弱信号 | 同名系统调用，但退出逻辑不同。目标项目调用 `exit_one_thread`，参考项目调用 `exit_current`，分别对应线程级和进程级退出。 |
| 内存管理 | `kernel/src/syscall/mod.rs#sys_mmap` | `os/src/syscall/mm.rs#sys_mmap` | 弱信号 | 同名系统调用，均需解析 `prot` 和 `flags` 参数，这是 POSIX 规范的强制要求。内部实现（`err_to_isize` vs `MmapFlags::from_bits`）完全不同。 |
| 内存管理 | `kernel/src/mm/memory_set.rs#fork` | `os/src/task/task.rs#fork` | 弱信号 | 同名函数，均使用 `Arc::new`, `BTreeMap::new` 等 Rust 标准库通用构造进行内存集拷贝，但具体分配策略（`alloc_frame` vs `from_existed_user_lazily`）不同。 |
| 系统调用 | `kernel/src/syscall/mod.rs#sys_uname` | `os/src/syscall/util.rs#sys_uname` | 弱信号 | 同名系统调用，功能相同，但实现方式不同。目标项目使用 `write_field` 辅助函数，参考项目直接使用 `ptr::write`。 |
| 文件系统 | `kernel/src/fs/mod.rs#seek` | `os/src/fs/os_inode.rs#seek` | 弱信号 | 同名函数，均实现文件偏移操作，但操作对象不同。目标项目操作 `self.offset.lock`，参考项目操作 `self.inner_handler`。 |
| 通用模块/其他 | `kernel/src/sync/spinlock.rs#lock` | `os/src/mutex/spin_mutex.rs#lock` | 弱信号 | 同名函数，均实现自旋锁，但内部机制不同。目标项目调用 `preempt_disable` 和 `self.inner.lock`，参考项目使用 `compare_exchange` 循环。 |
| 通用模块/其他 | `kernel/src/console.rs#write_str` | `os/src/console.rs#write_str` | 弱信号 | 同名函数，功能相同，但底层输出方式不同。目标项目调用 `crate::arch::console_put`，参考项目调用 `console_putchar`。 |

### 4. 核心代码调用流对比剖析
* **【进程管理 / execve 系统调用】**
* **底层技术析理**: 两者均实现了 `execve` 系统调用，但执行路径存在根本差异。目标项目 `[2026-XX]` 的 `sys_execve` 深度依赖其 VFS 层，通过 `copy_path` 复制用户态路径，然后调用 `fs::lookup_path` 在文件系统中查找并解析 inode，最终由 `exec_resolved` 执行加载。这是一个完整的、符合 Linux 语义的路径解析与执行流程。参考项目 `[2024-旺仔]` 的 `sys_execve` 则采用简化的教学 OS 路径，它直接通过 `Path::from` 构造路径，然后调用 `app_inode.read_all` 读取整个可执行文件到内存，再解析参数。两者在路径解析、文件操作、加载方式上无任何共同的外部调用节点，体现了“完整 VFS 解析”与“应用 inode 直接加载”的架构级差异。
* **代码溯源链接**:
  - 目标项目: `kernel/src/syscall/mod.rs#sys_execve`
  - 参考项目: `os/src/syscall/process.rs#sys_execve`

* **【进程管理 / wait4 系统调用】**
* **底层技术析理**: 两者均实现了 `wait4` 系统调用，但等待子进程的机制截然不同。目标项目 `[2026-XX]` 的 `sys_wait4` 采用同步阻塞模型，它直接遍历当前进程的 `children` 链表，通过 `filter_map` 和 `find` 组合子进程 PID，找到后调用 `crate::task::reap` 进行资源回收。参考项目 `[2024-旺仔]` 的 `sys_wait4` 则基于其异步运行时，它构造一个 `WaitFuture` 对象并挂起当前任务，等待子进程退出事件来唤醒。这是“主动轮询回收”与“异步事件驱动”两种并发范式的根本区别，调用图谱无任何交集。
* **代码溯源链接**:
  - 目标项目: `kernel/src/syscall/mod.rs#sys_wait4`
  - 参考项目: `os/src/syscall/process.rs#sys_wait4`

* **【内存管理 / mmap 系统调用】**
* **底层技术析理**: 两者均实现了 `mmap` 系统调用，参数解析部分因 POSIX 规范而存在功能等价性，但内部实现路径不同。目标项目 `[2026-XX]` 的 `sys_mmap` 在解析参数后，调用 `err_to_isize` 进行错误码转换，并尝试通过 `file.inode.read_at` 进行文件映射。参考项目 `[2024-旺仔]` 的 `sys_mmap` 则定义了 `MMAPPROT` 和 `MmapFlags` 等本地类型来解析标志位，其核心逻辑是操作页表进行内存映射。两者在错误处理、文件映射方式、内存管理对象上均表现出独立的设计选择。
* **代码溯源链接**:
  - 目标项目: `kernel/src/syscall/mod.rs#sys_mmap`
  - 参考项目: `os/src/syscall/mm.rs#sys_mmap`

### 5. 单方模块与降权项
- **网络协议栈**: 目标项目独有的模块，包含 `kernel/src/net/mod.rs` 中的 `udp_send`, `add_tcp_socket` 等 42 个函数，是独立开发的显著标志。
- **第三方库/公共依赖**: 目标项目自研了 ext4 文件系统解析逻辑（`kernel/src/fs/ext4.rs`），而参考项目依赖外部 `ext4` crate。两者虽然都处理 ext4 格式，但实现路径完全不同，属于对同一公开标准的独立实现，不能作为同源证据。
- **跨板块/跨层级配对**: 控方证据中存在大量跨板块、跨层级的强行配对，如将内核 futex 与用户态 init 程序配对，将信号处理与文件发送配对。这些配对违反了“同板块”原则，已全部降级为弱信号或不作为证据采纳。
- **同名系统调用**: 所有同名系统调用（如 `sys_exit`, `sys_uname`）均由 POSIX/Linux ABI 强制规定，其函数签名和基本功能必然相同。真正的复用证据在于内部实现流，而本案中所有同名系统调用的内部调用图谱均无共同指纹。

### 6. 保守结论
本次审计基于客观 AST 指纹比对，双方项目整体相似度仅 0.88%，共同指纹 17 个。在控方重点指控的【进程管理】板块，共同指纹为 0，覆盖率 0.0%，不存在任何结构级代码复用。双方在并发模型（同步 vs 异步）、架构支持（多架构 vs 单架构）、功能完整性（网络栈、动态 procfs 等）上存在系统性、根本性差异。控方列举的“复用铁证”实为 POSIX 规范趋同、Rust 语言惯用法及跨板块硬凑所致，不能证明代码抄袭。综合判定，`[2026-T2026105549911448-XX]` 为独立开发项目，复用指控不成立。

------------------------------

