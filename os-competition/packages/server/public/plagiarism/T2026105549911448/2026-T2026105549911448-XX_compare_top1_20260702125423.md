# 🛡️ OS 内核赛自动化代码级架构审计报告

**审计时间**: `2026-07-02 12:54:23`
**参赛作品名称**: `2026-T2026105549911448-XX`
**年份**: `2026`
**学校**: `XX`
**队伍名称**: `T2026105549911448`
**比赛名字**: `OS-Competition`
**比赛子赛道**: `Kernel-Implementation`
**仓库地址**: `https://gitlab.eduxiji.net/educg-group-43501-3132633/T2026105549911448-3008`
**推测开发语言**: `Rust / C (由AST探针自动识别)`
**特征函数量**: `1286`

## ⚔️ 底层代码同源性对比 (AST Homology)
> 本部分由专门的代码审查智能体，对该项目与符合年份条件的历届最高相似度项目进行控制流与底层调用对比。新版报告会先给出确定性的全模块覆盖矩阵，再给出 Agent 的分模块解释，避免只围绕少量抽样函数展开。

### 🏆 Top 1 底层对标: vs `2022-段错误-杭州电子科技大学`（杭州电子科技大学 / 段错误）

#### 对方作品信息

**对方年份**: `2022`
**对方学校**: `杭州电子科技大学`
**对方队伍名称**: `段错误`
**对方比赛名字**: `OS-Competition`
**对方比赛子赛道**: `Kernel-Implementation`
**对方仓库地址**: `https://gitlab.eduxiji.net/19061120/oskernel2022-segmentfault.git`

> 双向覆盖率：共同 AST 指纹: 16；唯一 AST 指纹: 目标项目 1112，参考项目 517；覆盖率: 目标项目 1.44%，参考项目 3.09%

## 全模块对比总览（确定性证据）

> 总体覆盖率：共同 AST 指纹: 16；唯一 AST 指纹: 目标项目 1112，参考项目 517；覆盖率: 目标项目 1.44%，参考项目 3.09%

### 1. 模块覆盖矩阵
| 模块 | 目标函数数 | 参考函数数 | 共同唯一 AST 指纹 | Target 覆盖 | Base 覆盖 | 状态 |
| :--- | ---: | ---: | ---: | ---: | ---: | :--- |
| 进程管理 | 106 | 52 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |
| 任务调度/异步运行时 | 253 | 56 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |
| 内存管理 | 271 | 183 | 10 | 3.75% | 5.95% | 双方存在且有结构重合 |
| 中断/异常处理 | 34 | 6 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |
| 系统调用 | 115 | 36 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |
| 文件系统 | 303 | 226 | 2 | 0.78% | 1.09% | 双方存在且有结构重合 |
| 设备驱动 | 39 | 37 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |
| 网络协议栈 | 42 | 0 | 0 | 0.0% | 0.0% | 仅目标项目体现 |
| 文件系统/第三方库 | 45 | 0 | 0 | 0.0% | 0.0% | 仅目标项目体现 |
| 通用模块/其他 | 78 | 41 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |

### 2. 分模块证据清单

#### 进程管理
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `kernel/src/syscall/mod.rs#sys_rt_sigprocmask` | `os/src/syscall/sigset.rs#sys_rt_sigprocmask` | 同板块同名 | bytes.as_slice, bytes.as_slice().try_into, bytes.as_slice().try_into().unwrap_or, cur.to_le_bytes, current_task, task.copy_in_bytes ...(+6) | UserBuffer::new, buf_vec.pop, buf_vec.pop().unwrap, current_task, current_task().unwrap, current_user_token ...(+7) |
| `kernel/src/syscall/mod.rs#sys_exit` | `os/src/syscall/process.rs#sys_exit` | 同板块同名 | current_task, exit_one_thread | exit_current_and_run_next, panic! |
| `kernel/src/syscall/mod.rs#sys_rt_sigtimedwait` | `os/src/task/task.rs#exec` | 同模块上下文对齐/差异观察 | (*task.tf_ptr()).rewind_syscall, b[0..8].try_into, b[0..8].try_into().unwrap_or, b[8..16].try_into, b[8..16].try_into().unwrap_or, bytes.as_slice ...(+25) | (0..args.len())
            .map, (0..args.len())
            .map(|arg| {
                user_sp -= args[arg].len() + 1; //1是手动添加结束标记的空间
                let mut p = user_sp;
                for c in args[arg].as_bytes() {
                    // 将参数写入到用户栈
                    *translated_refmut(memory_set.token(), p as *mut u8) = *c;
                    p += 1;
                } // 写入字符串结束标记
                *translated_refmut(memory_set.token(), p as *mut u8) = 0;
                user_sp
            })
            .collect, (0..envs.len())
            .map, (0..envs.len())
            .map(|env| {
                user_sp -= envs[env].len() + 1; //1是手动添加结束标记的空间
                let mut p = user_sp;
                for c in envs[env].as_bytes() {
                    // 将参数写入到用户栈
                    *translated_refmut(memory_set.token(), p as *mut u8) = *c;
                    p += 1;
                } // 写入字符串结束标记
                *translated_refmut(memory_set.token(), p as *mut u8) = 0;
                user_sp
            })
            .collect, KERNEL_SPACE.lock, KERNEL_SPACE.lock().token ...(+34) |
| `kernel/src/sync/futex.rs#futex_wait` | `os/src/syscall/process.rs#sys_waitpid` | 同模块上下文对齐/差异观察 | (*task.tf_ptr()).rewind_syscall, FUTEXES.lock, FUTEXES.lock().entry, FUTEXES.lock().entry(key).or_default, FUTEXES.lock().entry(key).or_default().push_back, crate::arch::now_ticks ...(+8) | assert_eq!, child.getpid, child.inner_exclusive_access, current_task, current_task().unwrap, drop ...(+12) |

#### 任务调度/异步运行时
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `kernel/src/syscall/mod.rs#sys_read` | `os/src/syscall/fs.rs#sys_read` | 同板块同名 | (*tf).rewind_syscall, Some, crate::arch::now_ticks, crate::fs::notify::active, crate::fs::notify::report, crate::net::poll ...(+35) | (0..userbuffer.buffers.len()).map, (0..userbuffer.buffers.len()).map(|_| 0).collect, UserBuffer::new, current_task, current_task().unwrap, current_user_token ...(+14) |
| `kernel/src/syscall/mod.rs#sys_ioctl` | `os/src/syscall/fs.rs#sys_ioctl` | 同板块同名 | (bytes / 512).to_le_bytes, 4096u64.to_le_bytes, 512i32.to_le_bytes, READAHEAD.load, READAHEAD.store, TTY_FG_PGID.load ...(+37) | current_task, current_task().unwrap, current_user_token, inner.fd_table.len, panic!, task.inner_exclusive_access ...(+1) |
| `kernel/src/syscall/mod.rs#sys_readv` | `os/src/syscall/fs.rs#sys_readv` | 同板块同名 | (*tf).rewind_syscall, core::mem::size_of::<IoVec>, core::slice::from_raw_parts, crate::net::poll, crate::net::tcp_may_recv, crate::task::mark_socket_waiter ...(+15) | UserBuffer::new, current_task, current_task().unwrap, current_user_token, drop, file.clone ...(+12) |
| `kernel/src/syscall/mod.rs#sys_close` | `os/src/syscall/fs.rs#sys_close` | 同板块同名 | Arc::as_ptr, FLOCK_RANGES.lock, Some, closing
        .as_ref, closing
        .as_ref()
        .map, crate::fs::notify::active ...(+16) | current_task, current_task().unwrap, inner.fd_table.len, inner.fd_table[fd].is_none, inner.fd_table[fd].take, task.inner_exclusive_access |

#### 内存管理
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `kernel/src/syscall/mod.rs#sys_fcntl` | `os/src/syscall/fs.rs#sys_fcntl` | 同板块同名 | Flock::default, Some, b[0..4].try_into, b[0..4].try_into().unwrap_or, b[4..8].try_into, b[4..8].try_into().unwrap_or ...(+55) | Arc::clone, Arc::new, FcntlFlags::from_bits, FcntlFlags::from_bits(cmd).unwrap, OpenFlags::from_bits, OpenFlags::from_bits(arg.unwrap() as u32).unwrap ...(+18) |
| `kernel/src/syscall/mod.rs#sys_openat` | `os/src/syscall/fs.rs#sys_openat` | 同板块同名 | Arc::new, File::from_inode, Some, String::new, abs.is_empty, alloc::format! ...(+33) | OpenFlags::from_bits, OpenFlags::from_bits(flags).expect, Some, current_task, current_task().unwrap, current_user_token ...(+9) |
| `kernel/src/loader/mod.rs#load_elf` | `os/src/mm/memory_set.rs#load_elf` | 同板块同名 | ElfFile::new, ElfFile::new(image).map_err, Err, Ok, VirtAddr, VmArea::new ...(+34) | ((ph.virtual_addr() + ph.mem_size()) as usize).into, (ph.virtual_addr() as usize + base_address).into, (ph.virtual_addr() as usize + ph.mem_size() as usize + base_address).into, (ph.virtual_addr() as usize).into, AuxEntry, ChunkArea::new ...(+47) |
| `kernel/src/mm/memory_set.rs#fork` | `os/src/task/task.rs#fork` | 同板块同名 | Arc::clone, Arc::new, MemorySet::try_new, Some, alloc::collections::BTreeMap::new, alloc_frame ...(+9) | Arc::downgrade, Arc::new, KernelStack::new, MemorySet::from_copy_on_write, Mutex::new, SigSet::new ...(+21) |

#### 中断/异常处理
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `kernel/src/signal.rs#default_action` | `os/src/trap/mod.rs#trap_from_kernel` | 同模块上下文对齐/差异观察 | 内部闭环逻辑 | panic!, println! |
| `kernel/src/arch/riscv64/trap.rs#init` | `os/src/trap/mod.rs#set_kernel_trap_entry` | 同模块上下文对齐/差异观察 | arm_timer, riscv::register::sie::set_stimer, riscv::register::sscratch::write, stvec::write | stvec::write |
| `kernel/src/arch/loongarch64/trap.rs#csrrd` | `os/src/trap/mod.rs#set_user_trap_entry` | 同模块上下文对齐/差异观察 | core::arch::asm!, unreachable! | stvec::write |
| `kernel/src/arch/loongarch64/trap.rs#arm_timer` | `os/src/trap/mod.rs#enable_timer_interrupt` | 同模块上下文对齐/差异观察 | core::arch::asm! | sie::set_stimer |

#### 系统调用
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `kernel/src/syscall/socket.rs#sys_listen` | `os/src/syscall/socket.rs#sys_listen` | 同板块同名 | Err, Ok, Some, crate::net::loopback::register_listener, net::tcp_listen, s.state.lock ...(+3) | 内部闭环逻辑 |
| `kernel/src/syscall/mod.rs#sys_futex` | `os/src/syscall/sigset.rs#sys_futex` | 同板块同名 | crate::sync::futex::do_futex | 内部闭环逻辑 |
| `kernel/src/syscall/mod.rs#sys_set_id` | `user_c/lib/syscall.c#clone` | 同模块上下文对齐/差异观察 | CREDS.lock, CREDS.lock().insert, SAVED_IDS.lock, SAVED_IDS.lock().insert, creds_of, cur_tgid ...(+2) | __clone |
| `kernel/src/syscall/keys.rs#sys_add_key` | `os/src/syscall/socket.rs#sys_recvfrom` | 同模块上下文对齐/差异观察 | ENOTDIR_AS_KEYRING, REG.lock, Vec::new, copy_cstr, copy_in, default_key_perm ...(+11) | UserBuffer::new, current_user_token, translated_byte_buffer, userbuf.write |

#### 文件系统
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `kernel/src/fs/socket.rs#read_at` | `simple-fat32/src/layout.rs#read_at` | 同板块同名 | Err, Ok, buf.len, buf[..n].copy_from_slice, core::cmp::min, dg.data.len ...(+10) | Arc::clone, buf.len, dst.copy_from_slice, fat.read, fat_reader.get_next_cluster, get_block_cache ...(+9) |
| `kernel/src/fs/ext2.rs#mount` | `os/src/fs/mount.rs#mount` | 同板块同名 | Arc::new, Err, Mutex::new, Ok, rd16, rd32 ...(+1) | self.mnt_list.iter, self.mnt_list.iter().find, self.mnt_list.iter().find(|&(_, d, _)| *d == dir).is_some, self.mnt_list.len, self.mnt_list.push |
| `kernel/src/fs/devfs.rs#write_at` | `simple-fat32/src/layout.rs#write_at` | 同板块同名 | Err, Ok, buf.len, core::cmp::min, sec[within..within + n].copy_from_slice, self.dev.capacity ...(+4) | Arc::clone, buf.len, dst.copy_from_slice, fat.read, fat_reader.get_next_cluster, get_block_cache ...(+9) |
| `kernel/src/fs/devfs.rs#read_at` | `simple-fat32/src/vfs.rs#read_at` | 同板块同名 | Ok, buf.iter_mut, buf.len, crate::arch::now_ticks, crate::arch::now_ticks()
                    .wrapping_mul, x.wrapping_mul ...(+1) | self.fs.read, self.fs.read().get_fat, self.read_short_dirent, short_entry.read_at |

#### 设备驱动
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `kernel/src/drivers/virtio_blk.rs#probe_mmio` | `os/src/drivers/block/sdcard.rs#read_sector` | 同模块上下文对齐/差异观察 | Arc::new, BlkInner::Mmio, BlockCache::new, MmioTransport::new, Mutex::new, NonNull::new ...(+7) | Err, Ok, assert!, chunk.iter_mut, chunk.iter_mut().zip, data_buf.chunks_mut ...(+6) |
| `kernel/src/drivers/pci.rs#assign_bars` | `os/src/drivers/block/sdcard.rs#write_sector` | 同模块上下文对齐/差异观察 | align_up, aligned.checked_add, aligned.checked_add(size).map_or, crate::println!, info.takes_two_entries, root.bar_info ...(+2) | (data_buf.len() / SEC_LEN).try_into, (data_buf.len() / SEC_LEN).try_into().unwrap, Err, Ok, assert!, chunk.iter ...(+9) |
| `kernel/src/drivers/virtio_net.rs#init_mmio` | `os/src/drivers/block/sdcard.rs#get_cidregister` | 同模块上下文对齐/差异观察 | Arc::new, MmioTransport::new, Mutex::new, NET.call_once, NetInner::Mmio, NonNull::new ...(+7) | Err, Ok, self.end_cmd, self.get_response, self.read_data, self.send_cmd ...(+2) |
| `kernel/src/drivers/virtio_net.rs#init_pci` | `simple-fat32/src/block_cache.rs#get_block_cache` | 同模块上下文对齐/差异观察 | Arc::new, Mutex::new, NET.call_once, NetInner::Pci, PciTransport::new::<KernelHal>, Some ...(+5) | Arc::clone, Arc::new, Arc::strong_count, BlockCache::new, RwLock::new, panic! ...(+7) |

#### 网络协议栈
仅目标项目中观察到该模块代表函数，参考项目没有同模块可对齐证据：
- 目标项目：`kernel/src/net/mod.rs#udp_send`，调用摘要：IpAddress::Ipv4, STACK.get, STACK.get().ok_or, data.len, g.sockets.get_mut::<udp::Socket>, s.lock ...(+6)
- 目标项目：`kernel/src/net/mod.rs#udp_recv`，调用摘要：Err, Ok, STACK.get, STACK.get().ok_or, g.sockets.get_mut::<udp::Socket>, s.lock ...(+1)
- 目标项目：`kernel/src/net/mod.rs#add_udp_socket`，调用摘要：STACK.get, Some, g.sockets.add, s.lock, udp::PacketBuffer::new, udp::Socket::new ...(+1)
- 目标项目：`kernel/src/net/mod.rs#add_tcp_socket`，调用摘要：STACK.get, Some, g.sockets.add, s.lock, sock.set_nagle_enabled, tcp::Socket::new ...(+2)

#### 文件系统/第三方库
仅目标项目中观察到该模块代表函数，参考项目没有同模块可对齐证据：
- 目标项目：`kernel/src/fs/ext4.rs#parse`，调用摘要：Err, Ok, Self::read_blocks_raw, Vec::with_capacity, blk.read_block, blk.read_block(2, &mut tmp).map_err ...(+7)
- 目标项目：`kernel/src/fs/ext4.rs#build_children`，调用摘要：Arc::new, BTreeMap::new, BTreeSet::new, Mutex::new, Ok, Some ...(+8)
- 目标项目：`kernel/src/fs/ext4.rs#walk_extent`，调用摘要：Err, Ok, node_bytes.len, out.push, self.read_block, self.walk_extent ...(+2)
- 目标项目：`kernel/src/fs/ext4.rs#read_range`，调用摘要：Ok, Some, buf.len, buf[done..done + take].copy_from_slice, buf[done..done + take].iter_mut, core::cmp::min ...(+4)

#### 通用模块/其他
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `kernel/src/arch/riscv64/power.rs#shutdown` | `os/src/sbi.rs#shutdown` | 同板块同名 | core::arch::asm!, sbi_rt::system_reset, sifive_exit | panic!, sbi_call |
| `kernel/src/console.rs#write_str` | `os/src/console.rs#write_str` | 同板块同名 | Ok, crate::arch::console_put, s.as_bytes | Ok, crate::sbi::console_putchar, s.chars |
| `user/hello/src/main.rs#panic` | `user_c/lib/stdlib.c#panic` | 同板块同名 | asm! | exit, puts |
| `user/git/src/main.rs#cmd_self_test` | `os/src/timer.rs#sub` | 同模块上下文对齐/差异观察 | ExitCode::from, git_blob_sha1, io::stdout, io::stdout().flush, io::stdout().flush().ok, print! ...(+1) | 内部闭环逻辑 |


#### 1. 宏观判定
- **定性结论**: 独立开发 (相似度: 0.99%，共同 AST 指纹: 16；唯一 AST 指纹: 目标项目 1112，参考项目 517；覆盖率: 目标项目 1.44%，参考项目 3.09%)

#### 2. 全模块覆盖矩阵解读
- **进程管理**: 双方均存在该模块。共同 AST 指纹为 0，目标覆盖率 0.0%，参考覆盖率 0.0%。该模块证据强度为**未形成**。控方指控的“同板块底层逻辑一致”缺乏客观 AST 指纹支撑。
- **任务调度/异步运行时**: 双方均存在该模块。共同 AST 指纹为 0，目标覆盖率 0.0%，参考覆盖率 0.0%。该模块证据强度为**未形成**。
- **内存管理**: 双方均存在该模块。共同 AST 指纹为 10，目标覆盖率 3.75%，参考覆盖率 5.95%。该模块证据强度为**弱**。重合点集中于 `load_elf`、`fork` 等通用内核操作，属于操作系统领域标准流程。
- **中断/异常处理**: 双方均存在该模块。共同 AST 指纹为 0，目标覆盖率 0.0%，参考覆盖率 0.0%。该模块证据强度为**未形成**。
- **系统调用**: 双方均存在该模块。共同 AST 指纹为 0，目标覆盖率 0.0%，参考覆盖率 0.0%。该模块证据强度为**未形成**。
- **文件系统**: 双方均存在该模块。共同 AST 指纹为 2，目标覆盖率 0.78%，参考覆盖率 1.09%。该模块证据强度为**弱**。重合点 `read_at`、`write_at` 为 VFS 通用接口，`mount` 为文件系统标准操作。
- **设备驱动**: 双方均存在该模块。共同 AST 指纹为 0，目标覆盖率 0.0%，参考覆盖率 0.0%。该模块证据强度为**未形成**。
- **网络协议栈**: 仅目标项目存在该模块。共同 AST 指纹为 0。该模块证据强度为**未形成**，且作为单方模块，是独立开发的正面证据。
- **第三方库/公共依赖**: 仅目标项目存在该模块（ext4 等）。共同 AST 指纹为 0。该模块证据强度为**未形成**，且作为单方模块，是独立开发的正面证据。

#### 3. 底层 AST 关键证据指纹表
| 所属板块 | 目标项目路径 | 参考项目路径 | 证据类型 | 技术说明 |
| :--- | :--- | :--- | :--- | :--- |
| 内存管理 | kernel/src/loader/mod.rs#load_elf | os/src/mm/memory_set.rs#load_elf | 同板块同名 | 双方均实现 ELF 加载，但目标项目使用 `ElfFile::new` 和 `VmArea::new` 抽象，参考项目使用 `ChunkArea::new` 和 `AuxEntry` 手动构建。调用图谱无重叠。 |
| 内存管理 | kernel/src/mm/memory_set.rs#fork | os/src/task/task.rs#fork | 同板块同名 | 双方均实现进程 fork，但目标项目使用 `MemorySet::try_new` 和 `BTreeMap`，参考项目使用 `MemorySet::from_copy_on_write` 和 `KernelStack::new`。内存管理模型不同。 |
| 文件系统 | kernel/src/fs/socket.rs#read_at | simple-fat32/src/layout.rs#read_at | 同板块同名 | 双方均实现 `read_at`，但目标项目操作 `dg.data`（数据报），参考项目操作 `fat` 和 `block_cache`。底层存储介质和数据结构完全不同。 |
| 文件系统 | kernel/src/fs/ext2.rs#mount | os/src/fs/mount.rs#mount | 同板块同名 | 双方均实现文件系统挂载，但目标项目使用 `rd16/rd32` 读取超级块，参考项目维护 `mnt_list` 挂载列表。挂载管理策略不同。 |
| 通用模块/其他 | kernel/src/console.rs#write_str | os/src/console.rs#write_str | 同板块同名 | 双方均实现控制台输出，但目标项目调用 `crate::arch::console_put`，参考项目调用 `crate::sbi::console_putchar`。底层驱动接口不同。 |
| 通用模块/其他 | kernel/src/arch/riscv64/power.rs#shutdown | os/src/sbi.rs#shutdown | 同板块同名 | 双方均实现关机，但目标项目使用 `sbi_rt::system_reset` 和 `sifive_exit`，参考项目使用 `sbi_call`。SBI 调用封装方式不同。 |

#### 4. 核心代码调用流对比剖析
* **【进程管理/信号掩码处理】**
* **底层技术析理**: 目标项目 `sys_rt_sigprocmask` 使用 `task.copy_in_bytes` / `task.copy_out_bytes` 进行用户态内存拷贝，数据容器为原始 `bytes` 切片，错误处理采用 `try_into().unwrap_or()`。参考项目 `sys_rt_sigprocmask` 使用 `UserBuffer::new` 抽象进行内存拷贝，数据容器为 `sigset.bits` 数组，错误处理采用 `pop().unwrap()`。两者在内存抽象层、数据容器和错误处理策略上均存在显著差异，调用图谱无共同外部对象。
* **代码溯源链接**:
  - 目标项目: `kernel/src/syscall/mod.rs#sys_rt_sigprocmask`
  - 参考项目: `os/src/syscall/sigset.rs#sys_rt_sigprocmask`

* **【进程管理/进程退出】**
* **底层技术析理**: 目标项目 `sys_exit` 调用 `exit_one_thread` 进行单线程退出，无 `panic!` 兜底。参考项目 `sys_exit` 调用 `exit_current_and_run_next` 并伴随 `panic!` 兜底。两者退出路径的函数名、调用对象和异常处理机制均不同。
* **代码溯源链接**:
  - 目标项目: `kernel/src/syscall/mod.rs#sys_exit`
  - 参考项目: `os/src/syscall/process.rs#sys_exit`

* **【内存管理/ELF加载】**
* **底层技术析理**: 目标项目 `load_elf` 使用 `ElfFile::new` 解析 ELF，通过 `VmArea::new` 构建虚拟内存区域，调用链围绕高层抽象。参考项目 `load_elf` 手动解析程序头，使用 `ChunkArea::new` 和 `AuxEntry` 构建内存映射，调用链围绕底层内存操作。两者处于不同抽象层次，外部调用对象无交集。
* **代码溯源链接**:
  - 目标项目: `kernel/src/loader/mod.rs#load_elf`
  - 参考项目: `os/src/mm/memory_set.rs#load_elf`

* **【文件系统/虚拟文件系统读取】**
* **底层技术析理**: 目标项目 `read_at`（socket）操作数据报 `dg.data`，使用 `buf[..n].copy_from_slice` 拷贝数据。参考项目 `read_at`（fat32）操作 FAT 表和块缓存，使用 `dst.copy_from_slice` 和 `get_block_cache`。两者底层存储模型（数据报 vs 块设备）和读取路径完全不同。
* **代码溯源链接**:
  - 目标项目: `kernel/src/fs/socket.rs#read_at`
  - 参考项目: `simple-fat32/src/layout.rs#read_at`

#### 5. 单方模块与降权项
- **网络协议栈**: 仅目标项目存在，包含 UDP/TCP socket 实现（`udp_send`、`tcp_listen` 等）。参考项目无此模块，无法形成同源证据，是独立开发的强正面证据。
- **文件系统/第三方库**: 仅目标项目存在 ext4 文件系统实现（`ext4.rs#parse`、`walk_extent` 等）。参考项目无此模块，无法形成同源证据，是独立开发的强正面证据。
- **跨板块同名降权**: 控方将 `sys_rt_sigtimedwait`（进程管理）与 `exec`（进程管理）对齐，但两者功能完全不同（信号等待 vs 程序执行），属于跨语义硬凑。`default_action`（信号处理）与 `trap_from_kernel`（中断处理）属于跨板块对齐，不构成同板块主证据。
- **规范同名降权**: `sys_exit`、`sys_waitpid`/`sys_wait4`、`sys_execve`、`sys_rt_sigprocmask`、`sys_read`、`sys_ioctl`、`sys_close` 等函数名均来自 POSIX/Linux 系统调用规范，任何实现这些接口的内核都会使用相同函数名。同名不等于同实现，不能作为复用证据。

#### 6. 保守结论
本次审计基于客观 AST 指纹比对，双方总体相似度仅 0.99%，共同指纹 16 个，覆盖率均低于 4%。控方在进程管理板块提出的四项核心指控，因该板块共同 AST 指纹为 0 而缺乏客观证据支撑。现有少量重合点（内存管理 10 个、文件系统 2 个）均集中于 `load_elf`、`fork`、`read_at`、`mount` 等操作系统领域通用基础设施，属于标准内核功能的必然实现，不构成特异性复用证据。目标项目具备网络协议栈、ext4 文件系统、多架构支持等参考项目完全缺失的功能模块，体现出显著的独立开发特征。综合判定，目标项目 `[2026-T2026105549911448-XX]` 对参考项目 `[2022-段错误-杭州电子科技大学]` 不存在系统性同源复用，抄袭风险极低。

------------------------------

