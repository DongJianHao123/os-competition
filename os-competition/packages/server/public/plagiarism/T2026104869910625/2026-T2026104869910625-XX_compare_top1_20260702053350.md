# 🛡️ OS 内核赛自动化代码级架构审计报告

**审计时间**: `2026-07-02 05:33:50`
**参赛作品名称**: `2026-T2026104869910625-XX`
**年份**: `2026`
**学校**: `XX`
**队伍名称**: `T2026104869910625`
**比赛名字**: `OS-Competition`
**比赛子赛道**: `Kernel-Implementation`
**仓库地址**: `https://gitlab.eduxiji.net/educg-group-43501-3132633/T2026104869910625-752`
**推测开发语言**: `Rust / C (由AST探针自动识别)`
**特征函数量**: `1142`

## ⚔️ 底层代码同源性对比 (AST Homology)
> 本部分由专门的代码审查智能体，对该项目与符合年份条件的历届最高相似度项目进行控制流与底层调用对比。新版报告会先给出确定性的全模块覆盖矩阵，再给出 Agent 的分模块解释，避免只围绕少量抽样函数展开。

### 🏆 Top 1 底层对标: vs `2025-智核速启队-武汉大学`（武汉大学 / 智核速启队）

#### 对方作品信息

**对方年份**: `2025`
**对方学校**: `武汉大学`
**对方队伍名称**: `智核速启队`
**对方比赛名字**: `OS-Competition`
**对方比赛子赛道**: `Kernel-Implementation`
**对方仓库地址**: `https://gitlab.eduxiji.net/educg-group-36002-2710490/T202510486995194-334`

> 双向覆盖率：共同 AST 指纹: 634；唯一 AST 指纹: 目标项目 948，参考项目 1038；覆盖率: 目标项目 66.88%，参考项目 61.08%

## 全模块对比总览（确定性证据）

> 总体覆盖率：共同 AST 指纹: 634；唯一 AST 指纹: 目标项目 948，参考项目 1038；覆盖率: 目标项目 66.88%，参考项目 61.08%

### 1. 模块覆盖矩阵
| 模块 | 目标函数数 | 参考函数数 | 共同唯一 AST 指纹 | Target 覆盖 | Base 覆盖 | 状态 |
| :--- | ---: | ---: | ---: | ---: | ---: | :--- |
| 进程管理 | 48 | 56 | 13 | 28.89% | 23.64% | 双方存在且有结构重合 |
| 任务调度/异步运行时 | 61 | 76 | 14 | 26.92% | 19.18% | 双方存在且有结构重合 |
| 内存管理 | 197 | 240 | 66 | 34.74% | 28.45% | 双方存在且有结构重合 |
| 中断/异常处理 | 22 | 26 | 15 | 75.0% | 65.22% | 双方存在且有结构重合 |
| 系统调用 | 46 | 51 | 12 | 40.0% | 29.27% | 双方存在且有结构重合 |
| 文件系统 | 118 | 103 | 60 | 67.42% | 76.92% | 双方存在且有结构重合 |
| 设备驱动 | 28 | 33 | 22 | 88.0% | 75.86% | 双方存在且有结构重合 |
| 网络协议栈 | 1 | 1 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |
| 文件系统/第三方库 | 534 | 523 | 407 | 90.04% | 92.08% | 双方存在且有结构重合 |
| 通用模块/其他 | 87 | 96 | 39 | 57.35% | 45.35% | 双方存在且有结构重合 |

### 2. 分模块证据清单

#### 进程管理
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `kernel/process.c#scheduler` | `kernel/process.c#scheduler` | 同板块同名 | DEBUG_LOG_LEVEL, acquire, copycontext, copytrapframe, futex_clear, hsai_swtch ...(+11) | acquire, copycontext, copytrapframe, futex_clear, hsai_swtch, intr_on ...(+9) |
| `kernel/futex.c#futex_wait` | `kernel/futex.c#futex_wait` | 同板块同名 | DEBUG_LOG_LEVEL, acquire, myproc, panic, release, sched | DEBUG_LOG_LEVEL, acquire, myproc, panic, release, sched ...(+1) |
| `kernel/process.c#tgkill` | `kernel/process.c#tgkill` | 同板块同名 | acquire, list_begin, list_end, list_entry, list_next, release ...(+1) | DEBUG_LOG_LEVEL, acquire, list_begin, list_end, list_entry, list_next ...(+2) |
| `kernel/signal.c#sigprocmask` | `kernel/signal.c#sigprocmask` | 同板块同名 | myproc | DEBUG_LOG_LEVEL, myproc |

#### 任务调度/异步运行时
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `kernel/driver/loongarch/virtio_pci.c#virtio_pci_read_caps` | `kernel/driver/loongarch/virtio_pci.c#virtio_pci_read_caps` | 同板块同名同结构 | get_cfg_addr, pci_config_read, pci_config_read8, printf | get_cfg_addr, pci_config_read, pci_config_read8, printf |
| `kernel/syscall.c#sys_ioctl` | `kernel/syscall.c#sys_ioctl` | 同板块同名 | ioctl_copyout_value, ioctl_is_tty, memset, myproc, printf, vfs_ext4_ioctl | loop_ioctl, myproc, printf, vfs_ext4_ioctl |
| `kernel/syscall.c#sys_lseek` | `kernel/syscall.c#sys_lseek` | 同板块同名 | DEBUG_LOG_LEVEL, myproc, strcmp, vfs_ext4_lseek | DEBUG_LOG_LEVEL, LOG_LEVEL, myproc, vfs_ext4_lseek |
| `user/riscv/user.c#init_main` | `user/loongarch/user.c#init_main` | 同板块同名 | openat, run_selected_profile, shutdown, sys_dup, sys_mknod | openat, printf, run_shell, shutdown, sys_dup, sys_mknod ...(+1) |

#### 内存管理
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `kernel/exec.c#exec` | `kernel/exec.c#exec` | 同板块同名 | DEBUG_LOG_LEVEL, LOG_LEVEL, PGROUNDDOWN, PGROUNDUP, alloc_aux, alloc_vma_stack ...(+30) | DEBUG_LOG_LEVEL, LOG_LEVEL, PGROUNDDOWN, PGROUNDUP, acquire, alloc_aux ...(+42) |
| `hsai/hsai_trap.c#usertrap` | `hsai/hsai_trap.c#usertrap` | 同板块同名 | LOG_LEVEL, devintr, exit, handle_exception, hsai_usertrapret, intr_on ...(+20) | LOG_LEVEL, check_and_handle_signals, devintr, exit, handle_exception, hsai_usertrapret ...(+21) |
| `kernel/pmem.c#buddy_free` | `kernel/pmem.c#buddy_free` | 同板块同名 | get_buddy_addr, is_buddy_available, list_push_front, list_remove, panic, printf ...(+2) | acquire, get_buddy_addr, is_buddy_available, list_push_front, list_remove, panic ...(+4) |
| `kernel/exec.c#debug_print_stack` | `kernel/exec.c#debug_print_stack` | 同板块同名同结构 | PRINT_COLOR, copyin, copyinstr | PRINT_COLOR, copyin, copyinstr |

#### 中断/异常处理
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `kernel/driver/riscv/virt.c#virtio_disk_intr` | `kernel/driver/riscv/virt.c#virtio_disk_intr` | 同板块同名 | R, __sync_synchronize, panic, printf | R, __sync_synchronize, acquire, panic, printf, release ...(+1) |
| `hsai/hsai_trap.c#hsai_trap_init` | `hsai/hsai_trap.c#hsai_trap_init` | 同板块同名同结构 | timer_init, w_csr_ecfg, w_csr_eentry, w_csr_euen, w_csr_merrentry, w_csr_tlbrentry ...(+1) | timer_init, w_csr_ecfg, w_csr_eentry, w_csr_euen, w_csr_merrentry, w_csr_tlbrentry ...(+1) |
| `hsai/hsai_trap.c#hsai_set_csr_to_usermode` | `hsai/hsai_trap.c#hsai_set_csr_to_usermode` | 同板块同名同结构 | r_csr_prmd, r_sstatus, w_csr_prmd, w_sstatus | r_csr_prmd, r_sstatus, w_csr_prmd, w_sstatus |
| `hsai/print.c#get_exception_description` | `hsai/print.c#get_exception_description` | 同板块同名同结构 | 内部闭环逻辑 | 内部闭环逻辑 |

#### 系统调用
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `user/loongarch/user.c#test_busybox` | `user/loongarch/user.c#test_busybox` | 同板块同名 | exit, fork, print, printf, sys_chdir, sys_execve ...(+1) | exit, fork, printf, sys_chdir, sys_execve, waitpid |
| `user/riscv/user.c#test_busybox` | `user/riscv/user.c#test_busybox` | 同板块同名 | exit, fork, print, printf, sys_chdir, sys_execve ...(+1) | exit, fork, printf, sys_chdir, sys_execve, waitpid |
| `user/loongarch/user.c#test_iozone` | `user/loongarch/user.c#test_iozone` | 同板块同名 | exit, fork, printf, sys_chdir, sys_execve, waitpid | exit, fork, printf, sys_chdir, sys_execve, waitpid |
| `user/riscv/user.c#test_iozone` | `user/riscv/user.c#test_iozone` | 同板块同名 | exit, fork, printf, setup_dynamic_library, sys_chdir, sys_execve ...(+1) | exit, fork, printf, sys_chdir, sys_execve, waitpid |

#### 文件系统
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `kernel/fs/qsort.c#_quicksort` | `kernel/fs/qsort.c#_quicksort` | 同板块同名同结构 | (*cmp), POP, PUSH, SWAP, min | (*cmp), POP, PUSH, SWAP, min |
| `kernel/driver/loongarch/pci.c#pci_device_probe` | `kernel/driver/loongarch/pci.c#pci_device_probe` | 同板块同名同结构 | PCI_ADDR, __sync_synchronize, printf | PCI_ADDR, __sync_synchronize, printf |
| `hsai/print.c#vpanic` | `hsai/print.c#vpanic` | 同板块同名 | consputc, print_line, printf, printint, printptr, shutdown ...(+1) | acquire, consputc, holding, hsai_get_cpuid, int_to_str, print_line ...(+5) |
| `kernel/fs/list.c#list_sort` | `kernel/fs/list.c#list_sort` | 同板块同名同结构 | ASSERT, find_end_of_run, inplace_merge, is_sorted, list_begin, list_end | ASSERT, find_end_of_run, inplace_merge, is_sorted, list_begin, list_end |

#### 设备驱动
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `hal/loongarch/uart.c#uart_init` | `hal/loongarch/uart.c#uart_init` | 同板块同名同结构 | _write_reg, initlock | _write_reg, initlock |
| `hal/riscv/uart.c#uart_init` | `hal/riscv/uart.c#uart_init` | 同板块同名同结构 | WriteReg, initlock | WriteReg, initlock |
| `kernel/driver/loongarch/virtio_pci.c#virtio_pci_set_queue_addr2` | `kernel/driver/loongarch/virtio_pci.c#virtio_pci_set_queue_addr2` | 同板块同名同结构 | PCI_REG16, PCI_REG64, dsb | PCI_REG16, PCI_REG64, dsb |
| `hal/riscv/uart.c#uartstart` | `hal/riscv/uart.c#uartstart` | 同板块同名同结构 | ReadReg, WriteReg, wakeup | ReadReg, WriteReg, wakeup |

#### 网络协议栈
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `kernel/socket.c#sock_bind` | `kernel/socket.c#sock_bind` | 同板块同名 | DEBUG_LOG_LEVEL, memmove | DEBUG_LOG_LEVEL, memmove |

#### 文件系统/第三方库
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `kernel/fs/ext4_balloc.c#ext4_balloc_alloc_block` | `kernel/fs/ext4_balloc.c#ext4_balloc_alloc_block` | 同板块同名同结构 | ext4_balloc_get_bgid_of_block, ext4_balloc_get_block_of_bgid, ext4_balloc_set_bitmap_csum, ext4_balloc_verify_bitmap_csum, ext4_bg_get_block_bitmap, ext4_bg_get_free_blocks_count ...(+19) | ext4_balloc_get_bgid_of_block, ext4_balloc_get_block_of_bgid, ext4_balloc_set_bitmap_csum, ext4_balloc_verify_bitmap_csum, ext4_bg_get_block_bitmap, ext4_bg_get_free_blocks_count ...(+19) |
| `kernel/fs/ext4.c#ext4_dir_rm` | `kernel/fs/ext4.c#ext4_dir_rm` | 同板块同名同结构 | EXT4_MP_LOCK, EXT4_MP_UNLOCK, ext4_block_cache_write_back, ext4_dir_en_get_inode, ext4_dir_iterator_fini, ext4_dir_iterator_init ...(+19) | EXT4_MP_LOCK, EXT4_MP_UNLOCK, ext4_block_cache_write_back, ext4_dir_en_get_inode, ext4_dir_iterator_fini, ext4_dir_iterator_init ...(+19) |
| `kernel/fs/ext4_journal.c#jbd_journal_prepare` | `kernel/fs/ext4_journal.c#jbd_journal_prepare` | 同板块同名同结构 | EXT4_BLOCK_ZERO, RB_FIND, TAILQ_FOREACH_REVERSE_SAFE, TAILQ_FOREACH_SAFE, TAILQ_REMOVE, ext4_bcache_clear_dirty ...(+17) | EXT4_BLOCK_ZERO, RB_FIND, TAILQ_FOREACH_REVERSE_SAFE, TAILQ_FOREACH_SAFE, TAILQ_REMOVE, ext4_bcache_clear_dirty ...(+17) |
| `kernel/fs/ext4_dir_idx.c#ext4_dir_dx_split_index` | `kernel/fs/ext4_dir_idx.c#ext4_dir_dx_split_index` | 同板块同名同结构 | ext4_block_set, ext4_dir_dx_climit_get_count, ext4_dir_dx_climit_get_limit, ext4_dir_dx_climit_set_count, ext4_dir_dx_climit_set_limit, ext4_dir_dx_entry_get_hash ...(+10) | ext4_block_set, ext4_dir_dx_climit_get_count, ext4_dir_dx_climit_get_limit, ext4_dir_dx_climit_set_count, ext4_dir_dx_climit_set_limit, ext4_dir_dx_entry_get_hash ...(+10) |

#### 通用模块/其他
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `hsai/print.c#printf` | `hsai/print.c#printf` | 同板块同名 | acquire, consputc, printint, printptr, release, va_arg ...(+2) | acquire, consputc, printint, printptr, release, va_arg ...(+2) |
| `kernel/pmem.c#buddy_safe_check` | `kernel/pmem.c#buddy_safe_check` | 同板块同名同结构 | list_begin, list_end, list_entry, list_next, printf | list_begin, list_end, list_entry, list_next, printf |
| `kernel/timer.c#timer_init` | `hsai/timer.c#timer_init` | 同板块同名 | calibrate_timer_freq_from_rtc, countdown_timer_init, initlock, printf, r_mcounteren, r_menvcfg ...(+12) | __sync_fetch_and_or, countdown_timer_init, initlock, r_mcounteren, r_menvcfg, r_mie ...(+9) |
| `kernel/pmem.c#buddy_free_tracked` | `kernel/pmem.c#buddy_free_tracked` | 同板块同名同结构 | buddy_free, printf, r_time | buddy_free, printf, r_time |


好的，审计官。基于提供的证据底座、控辩双方陈词及AST抽样数据，我将进行最终的独立同源性定性仲裁。

### 1. 宏观判定
- **定性结论**: 存在结构性借鉴与同源演化，但非简单抄袭 (相似度: 46.89%，共同 AST 指纹: 634；唯一 AST 指纹: 目标项目 948，参考项目 1038；覆盖率: 目标项目 66.88%，参考项目 61.08%)

### 2. 全模块覆盖矩阵解读
- **进程管理**: 双方均存在。共同AST指纹13个，目标覆盖率28.89%，参考覆盖率23.64%。覆盖率较低，表明双方在进程管理的主体实现上存在显著差异。证据强度：**中**。
- **任务调度/异步运行时**: 双方均存在。共同AST指纹14个，目标覆盖率26.92%，参考覆盖率19.18%。覆盖率低，核心调度逻辑虽有重合，但整体实现路径差异较大。证据强度：**弱**。
- **内存管理**: 双方均存在。共同AST指纹66个，目标覆盖率34.74%，参考覆盖率28.45%。覆盖率中等偏低，表明在庞大的内存管理子系统中，双方共享部分基础结构，但多数实现独立。证据强度：**中**。
- **中断/异常处理**: 双方均存在。共同AST指纹15个，目标覆盖率75.0%，参考覆盖率65.22%。覆盖率极高，说明底层Trap处理、CSR操作等硬件强相关代码几乎同源。证据强度：**强**。
- **系统调用**: 双方均存在。共同AST指纹12个，目标覆盖率40.0%，参考覆盖率29.27%。覆盖率中等，部分POSIX接口实现相似，但整体系统调用框架存在差异。证据强度：**弱**。
- **文件系统**: 双方均存在。共同AST指纹60个，目标覆盖率67.42%，参考覆盖率76.92%。覆盖率很高，尤其是ext4相关核心逻辑高度重合。证据强度：**强**。
- **设备驱动**: 双方均存在。共同AST指纹22个，目标覆盖率88.0%，参考覆盖率75.86%。覆盖率极高，VirtIO、UART等驱动实现几乎完全一致。证据强度：**强**。
- **网络协议栈**: 双方均存在。共同AST指纹0个，覆盖率均为0.0%。双方虽都有网络相关文件，但未检测到任何共同结构指纹，表明是完全独立的实现。证据强度：**未形成**。
- **第三方库/公共依赖**: 双方均存在。共同AST指纹407个，目标覆盖率90.04%，参考覆盖率92.08%。覆盖率极高，表明双方引用了大量相同的第三方库或公共基础代码（如ext4文件系统、通用算法库）。证据强度：**强（但需降权处理）**。

### 3. 底层 AST 关键证据指纹表
| 所属板块 | 目标项目路径 | 参考项目路径 | 证据类型 | 技术说明 |
| :--- | :--- | :--- | :--- | :--- |
| 中断/异常处理 | `hsai/hsai_trap.c#hsai_trap_init` | `hsai/hsai_trap.c#hsai_trap_init` | 同板块同名同结构 | 中断初始化序列`timer_init, w_csr_ecfg, w_csr_eentry...`完全一致，这是与硬件平台强相关的非标准初始化序列。 |
| 中断/异常处理 | `hsai/hsai_trap.c#hsai_set_csr_to_usermode` | `hsai/hsai_trap.c#hsai_set_csr_to_usermode` | 同板块同名同结构 | 切换到用户模式的CSR寄存器操作序列`r_csr_prmd, r_sstatus, w_csr_prmd, w_sstatus`完全一致。 |
| 设备驱动 | `hal/loongarch/uart.c#uart_init` | `hal/loongarch/uart.c#uart_init` | 同板块同名同结构 | UART初始化调用`_write_reg, initlock`完全一致，是特定硬件平台驱动的直接复用。 |
| 设备驱动 | `kernel/driver/loongarch/virtio_pci.c#virtio_pci_set_queue_addr2` | `kernel/driver/loongarch/virtio_pci.c#virtio_pci_set_queue_addr2` | 同板块同名同结构 | VirtIO PCI队列地址设置的特定寄存器操作序列`PCI_REG16, PCI_REG64, dsb`完全一致。 |
| 文件系统 | `kernel/fs/ext4_balloc.c#ext4_balloc_alloc_block` | `kernel/fs/ext4_balloc.c#ext4_balloc_alloc_block` | 同板块同名同结构 | ext4块分配器的复杂内部调用链`ext4_balloc_get_bgid_of_block...`长达20余个函数，完全一致，是典型的核心逻辑复刻。 |
| 文件系统 | `kernel/fs/ext4_journal.c#jbd_journal_prepare` | `kernel/fs/ext4_journal.c#jbd_journal_prepare` | 同板块同名同结构 | ext4日志准备的事务状态机操作`RB_FIND, TAILQ_FOREACH_REVERSE_SAFE...`等宏和函数调用序列完全一致。 |
| 进程管理 | `kernel/process.c#sched` | `kernel/process.c#sched` | 同板块同名 | 上下文切换入口的调用图谱`copytrapframe, holding, hsai_swtch, intr_get, mycpu, myproc, panic`达到100%一致。 |
| 进程管理 | `kernel/process.c#reg_info` | `kernel/process.c#reg_info` | 同板块底层逻辑一致 | 寄存器信息打印的CSR读取序列`r_csr_ecfg, r_csr_eentry, r_csr_era...`完全一致，此顺序并非架构强制。 |
| 第三方库 | `kernel/fs/qsort.c#_quicksort` | `kernel/fs/qsort.c#_quicksort` | 同板块同名同结构 | 快速排序的内部宏`(*cmp), POP, PUSH, SWAP, min`使用完全一致，属于公共算法库复用。 |
| 第三方库 | `kernel/fs/list.c#list_sort` | `kernel/fs/list.c#list_sort` | 同板块同名同结构 | 链表排序的辅助函数调用`find_end_of_run, inplace_merge, is_sorted...`完全一致，属于公共数据结构库复用。 |

### 4. 核心代码调用流对比剖析
* **【进程管理/上下文切换路径】**
* **底层技术析理**: 双方在`kernel/process.c#sched`函数中实现了完全相同的上下文切换调用流。该流程从获取当前CPU和进程信息开始，经过锁状态检查，最终调用`hsai_swtch`进行地址空间切换。此调用序列并非硬件架构强制要求，其高度一致性表明核心调度逻辑的同源性。
* **代码溯源链接**:
  - 目标项目: `kernel/process.c#sched`
  - 参考项目: `kernel/process.c#sched`

* **【中断/异常处理/Trap初始化路径】**
* **底层技术析理**: 在`hsai/hsai_trap.c#hsai_trap_init`中，双方对多个CSR寄存器的初始化顺序（`ecfg`, `eentry`, `euen`, `merrentry`, `tlbrentry`）完全一致。虽然寄存器名是架构定义的，但其初始化顺序和组合方式属于设计选择，高度一致表明此部分代码系同源。
* **代码溯源链接**:
  - 目标项目: `hsai/hsai_trap.c#hsai_trap_init`
  - 参考项目: `hsai/hsai_trap.c#hsai_trap_init`

* **【文件系统/ext4块分配路径】**
* **底层技术析理**: 在`kernel/fs/ext4_balloc.c#ext4_balloc_alloc_block`中，双方均实现了超过20个函数的复杂调用链，包括获取块组信息、校验位图校验和、设置位图、更新计数等。此调用流是ext4文件系统的核心算法实现，其高度复杂的内部逻辑完全一致，是强同源性证据。
* **代码溯源链接**:
  - 目标项目: `kernel/fs/ext4_balloc.c#ext4_balloc_alloc_block`
  - 参考项目: `kernel/fs/ext4_balloc.c#ext4_balloc_alloc_block`

### 5. 单方模块与降权项
- **网络协议栈**: 双方虽均有`socket.c`等文件，但共同AST指纹为0，表明是完全独立的实现，不构成同源证据。
- **第三方库/公共依赖**: `kernel/fs/qsort.c`、`kernel/fs/list.c`等文件中的排序、链表操作函数高度一致，属于对公共基础算法库的复用，不能作为核心业务逻辑抄袭的证据，已降权处理。
- **跨板块弱信号**: 控方提出的参考项目`kernel/procfs.c`与目标项目`kernel/fs/file.c`、`kernel/syscall.c`之间的函数逻辑相似性，属于跨子系统（原生procfs vs busybox兼容层）的适配。目标项目使用`busybox_virtual_*`系列函数，与参考项目的`generate_proc_*`系列函数调用对象完全不同，属于面向不同测试框架的独立适配，不构成强同源证据，已降权处理。
- **泛型函数与标准原语**: `myproc`、`yield`、`sleep_on_chan`等函数调用图谱相似，是xv6风格内核的标准并发原语，属于领域共性，不能单独作为抄袭证据。

### 6. 保守结论
综合来看，[2026-T2026104869910625-XX]项目与[2025-智核速启队-武汉大学]项目存在显著的同源性，尤其在**中断/异常处理、设备驱动和文件系统**等模块，大量非标准的底层硬件操作序列和复杂算法实现高度一致，已超出独立开发的巧合范畴，可判定为“存在结构性借鉴与同源演化”。然而，在**进程管理、内存管理**等核心模块，双方覆盖率较低，存在大量独立实现；**网络协议栈**更是完全异构。因此，目标项目并非对参考项目的简单整体抄袭，而是在一个共同的核心框架或代码基之上，进行了功能增删、架构调整（如引入busybox兼容层）和独立模块开发。整体风险等级为**中高级**，建议对高度重合的底层驱动和文件系统核心逻辑进行重点审查。

------------------------------

