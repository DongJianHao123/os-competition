# 🛡️ OS 内核赛自动化代码级架构审计报告

**审计时间**: `2026-07-02 05:33:50`
**参赛作品名称**: `2026-T2026104869910625-XX`
**年份**: `2026`
**学校**: `XX`
**队伍名称**: `T2026104869910625`
**比赛名字**: `OS-Competition`
**比赛子赛道**: `Kernel-Implementation`
**仓库地址**: `https://gitlab.eduxiji.net/educg-group-43501-3132633/T2026104869910625-752`
**推测开发语言**: `Rust / C (由AST探针自动识别)`
**特征函数量**: `1142`

## ⚔️ 底层代码同源性对比 (AST Homology)
> 本部分由专门的代码审查智能体，对该项目与符合年份条件的历届最高相似度项目进行控制流与底层调用对比。新版报告会先给出确定性的全模块覆盖矩阵，再给出 Agent 的分模块解释，避免只围绕少量抽样函数展开。

### 🏆 Top 2 底层对标: vs `2024-undefinedType-南京航空航天大学`（南京航空航天大学 / undefinedType）

#### 对方作品信息

**对方年份**: `2024`
**对方学校**: `南京航空航天大学`
**对方队伍名称**: `undefinedType`
**对方比赛名字**: `OS-Competition`
**对方比赛子赛道**: `Kernel-Implementation`
**对方仓库地址**: `https://gitlab.eduxiji.net/educg-group-26010-2376550/T202410287992755-2176`

> 双向覆盖率：共同 AST 指纹: 371；唯一 AST 指纹: 目标项目 948，参考项目 398；覆盖率: 目标项目 39.14%，参考项目 93.22%

## 全模块对比总览（确定性证据）

> 总体覆盖率：共同 AST 指纹: 371；唯一 AST 指纹: 目标项目 948，参考项目 398；覆盖率: 目标项目 39.14%，参考项目 93.22%

### 1. 模块覆盖矩阵
| 模块 | 目标函数数 | 参考函数数 | 共同唯一 AST 指纹 | Target 覆盖 | Base 覆盖 | 状态 |
| :--- | ---: | ---: | ---: | ---: | ---: | :--- |
| 进程管理 | 48 | 0 | 0 | 0.0% | 0.0% | 仅目标项目体现 |
| 任务调度/异步运行时 | 61 | 0 | 0 | 0.0% | 0.0% | 仅目标项目体现 |
| 内存管理 | 197 | 0 | 0 | 0.0% | 0.0% | 仅目标项目体现 |
| 中断/异常处理 | 22 | 0 | 0 | 0.0% | 0.0% | 仅目标项目体现 |
| 系统调用 | 46 | 0 | 0 | 0.0% | 0.0% | 仅目标项目体现 |
| 文件系统 | 118 | 0 | 0 | 0.0% | 0.0% | 仅目标项目体现 |
| 设备驱动 | 28 | 0 | 0 | 0.0% | 0.0% | 仅目标项目体现 |
| 网络协议栈 | 1 | 0 | 0 | 0.0% | 0.0% | 仅目标项目体现 |
| 文件系统/第三方库 | 534 | 478 | 371 | 82.08% | 93.22% | 双方存在且有结构重合 |
| 通用模块/其他 | 87 | 0 | 0 | 0.0% | 0.0% | 仅目标项目体现 |

### 2. 分模块证据清单

#### 进程管理
仅目标项目中观察到该模块代表函数，参考项目没有同模块可对齐证据：
- 目标项目：`kernel/process.c#scheduler`，调用摘要：DEBUG_LOG_LEVEL, acquire, copycontext, copytrapframe, futex_clear, hsai_swtch ...(+11)
- 目标项目：`kernel/futex.c#futex_wait`，调用摘要：DEBUG_LOG_LEVEL, acquire, myproc, panic, release, sched
- 目标项目：`kernel/fs/file.c#busybox_virtual_proc_pid_stat`，调用摘要：busybox_find_proc_by_pid_local, busybox_virtual_append_decimal
- 目标项目：`kernel/process.c#tgkill`，调用摘要：acquire, list_begin, list_end, list_entry, list_next, release ...(+1)

#### 任务调度/异步运行时
仅目标项目中观察到该模块代表函数，参考项目没有同模块可对齐证据：
- 目标项目：`kernel/syscall.c#syscall`，调用摘要：hsai_get_arg, myproc, sleep, sys_accept, sys_bind, sys_brk ...(+112)
- 目标项目：`kernel/driver/loongarch/virtio_pci.c#virtio_pci_read_caps`，调用摘要：get_cfg_addr, pci_config_read, pci_config_read8, printf
- 目标项目：`kernel/syscall.c#sys_ioctl`，调用摘要：ioctl_copyout_value, ioctl_is_tty, memset, myproc, printf, vfs_ext4_ioctl
- 目标项目：`kernel/fs/fs.c#dir_init`，调用摘要：free_inode, namei, vfs_ext4_mkdir, vfs_ext4_mknod

#### 内存管理
仅目标项目中观察到该模块代表函数，参考项目没有同模块可对齐证据：
- 目标项目：`kernel/exec.c#exec`，调用摘要：DEBUG_LOG_LEVEL, LOG_LEVEL, PGROUNDDOWN, PGROUNDUP, alloc_aux, alloc_vma_stack ...(+30)
- 目标项目：`hsai/hsai_trap.c#usertrap`，调用摘要：LOG_LEVEL, devintr, exit, handle_exception, hsai_usertrapret, intr_on ...(+20)
- 目标项目：`hsai/hsai_trap.c#kerneltrap`，调用摘要：LOG_LEVEL, MAKE_SATP, assert, devintr, exit, handle_exception ...(+23)
- 目标项目：`kernel/pmem.c#buddy_free`，调用摘要：get_buddy_addr, is_buddy_available, list_push_front, list_remove, panic, printf ...(+2)

#### 中断/异常处理
仅目标项目中观察到该模块代表函数，参考项目没有同模块可对齐证据：
- 目标项目：`kernel/driver/riscv/virt.c#virtio_disk_intr`，调用摘要：R, __sync_synchronize, panic, printf
- 目标项目：`hsai/hsai_trap.c#hsai_trap_init`，调用摘要：timer_init, w_csr_ecfg, w_csr_eentry, w_csr_euen, w_csr_merrentry, w_csr_tlbrentry ...(+1)
- 目标项目：`hsai/hsai_trap.c#hsai_set_csr_to_usermode`，调用摘要：r_csr_prmd, r_sstatus, w_csr_prmd, w_sstatus
- 目标项目：`hsai/print.c#get_exception_description`，调用摘要：内部闭环逻辑

#### 系统调用
仅目标项目中观察到该模块代表函数，参考项目没有同模块可对齐证据：
- 目标项目：`user/loongarch/user.c#test_busybox`，调用摘要：exit, fork, print, printf, sys_chdir, sys_execve ...(+1)
- 目标项目：`user/riscv/user.c#test_busybox`，调用摘要：exit, fork, print, printf, sys_chdir, sys_execve ...(+1)
- 目标项目：`user/loongarch/user.c#test_iozone`，调用摘要：exit, fork, printf, sys_chdir, sys_execve, waitpid
- 目标项目：`user/riscv/user.c#test_iozone`，调用摘要：exit, fork, printf, setup_dynamic_library, sys_chdir, sys_execve ...(+1)

#### 文件系统
仅目标项目中观察到该模块代表函数，参考项目没有同模块可对齐证据：
- 目标项目：`kernel/fs/inode.c#get_absolute_path`，调用摘要：memmove, strcat, strcmp, strcpy, strlen, strncmp
- 目标项目：`kernel/fs/qsort.c#_quicksort`，调用摘要：(*cmp), POP, PUSH, SWAP, min
- 目标项目：`kernel/fs/file.c#fileclose`，调用摘要：DEBUG_LOG_LEVEL, LOG_LEVEL, acquire, get_file_ops, get_file_ops()->writable, panic ...(+6)
- 目标项目：`user/loongarch/user.c#run_ltp_profile`，调用摘要：exit, fork, ltp_runtime_root_dir, print, printf, sys_chdir ...(+2)

#### 设备驱动
仅目标项目中观察到该模块代表函数，参考项目没有同模块可对齐证据：
- 目标项目：`hal/loongarch/uart.c#uart_init`，调用摘要：_write_reg, initlock
- 目标项目：`hal/riscv/uart.c#uart_init`，调用摘要：WriteReg, initlock
- 目标项目：`kernel/driver/loongarch/virtio_pci.c#virtio_pci_set_queue_addr2`，调用摘要：PCI_REG16, PCI_REG64, dsb
- 目标项目：`hal/riscv/uart.c#uartstart`，调用摘要：ReadReg, WriteReg, wakeup

#### 网络协议栈
仅目标项目中观察到该模块代表函数，参考项目没有同模块可对齐证据：
- 目标项目：`kernel/socket.c#sock_bind`，调用摘要：DEBUG_LOG_LEVEL, memmove

#### 文件系统/第三方库
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `kernel/fs/ext4_balloc.c#ext4_balloc_alloc_block` | `Kernel/File/lwext4/ext4_balloc.c#ext4_balloc_alloc_block` | 同板块同名同结构 | ext4_balloc_get_bgid_of_block, ext4_balloc_get_block_of_bgid, ext4_balloc_set_bitmap_csum, ext4_balloc_verify_bitmap_csum, ext4_bg_get_block_bitmap, ext4_bg_get_free_blocks_count ...(+19) | EXT4_Debug_u32, ext4_balloc_get_bgid_of_block, ext4_balloc_get_block_of_bgid, ext4_balloc_set_bitmap_csum, ext4_balloc_verify_bitmap_csum, ext4_bg_get_block_bitmap ...(+19) |
| `kernel/fs/ext4.c#ext4_dir_rm` | `Kernel/File/lwext4/ext4.c#ext4_dir_rm` | 同板块同名同结构 | EXT4_MP_LOCK, EXT4_MP_UNLOCK, ext4_block_cache_write_back, ext4_dir_en_get_inode, ext4_dir_iterator_fini, ext4_dir_iterator_init ...(+19) | EXT4_MP_LOCK, EXT4_MP_UNLOCK, ext4_block_cache_write_back, ext4_dir_en_get_inode, ext4_dir_iterator_fini, ext4_dir_iterator_init ...(+19) |
| `kernel/fs/ext4_journal.c#jbd_journal_prepare` | `Kernel/File/lwext4/ext4_journal.c#jbd_journal_prepare` | 同板块同名 | EXT4_BLOCK_ZERO, RB_FIND, TAILQ_FOREACH_REVERSE_SAFE, TAILQ_FOREACH_SAFE, TAILQ_REMOVE, ext4_bcache_clear_dirty ...(+17) | EXT4_BLOCK_ZERO, RB_FIND, TAILQ_FOREACH_REVERSE_SAFE, TAILQ_FOREACH_SAFE, TAILQ_REMOVE, ext4_bcache_clear_dirty ...(+17) |
| `kernel/fs/ext4_dir_idx.c#ext4_dir_dx_split_index` | `Kernel/File/lwext4/ext4_dir_idx.c#ext4_dir_dx_split_index` | 同板块同名同结构 | ext4_block_set, ext4_dir_dx_climit_get_count, ext4_dir_dx_climit_get_limit, ext4_dir_dx_climit_set_count, ext4_dir_dx_climit_set_limit, ext4_dir_dx_entry_get_hash ...(+10) | ext4_block_set, ext4_dir_dx_climit_get_count, ext4_dir_dx_climit_get_limit, ext4_dir_dx_climit_set_count, ext4_dir_dx_climit_set_limit, ext4_dir_dx_entry_get_hash ...(+10) |

#### 通用模块/其他
仅目标项目中观察到该模块代表函数，参考项目没有同模块可对齐证据：
- 目标项目：`hsai/print.c#printf`，调用摘要：acquire, consputc, printint, printptr, release, va_arg ...(+2)
- 目标项目：`kernel/pmem.c#buddy_safe_check`，调用摘要：list_begin, list_end, list_entry, list_next, printf
- 目标项目：`kernel/timer.c#timer_init`，调用摘要：calibrate_timer_freq_from_rtc, countdown_timer_init, initlock, printf, r_mcounteren, r_menvcfg ...(+12)
- 目标项目：`kernel/pmem.c#buddy_free_tracked`，调用摘要：buddy_free, printf, r_time


### 1. 宏观判定
- **定性结论**: **独立开发，存在第三方库同源引用** (相似度: 38.05%)
- **核心依据**: 目标项目 `[2026-T2026104869910625-XX]` 与参考项目 `[2024-undefinedType-南京航空航天大学]` 的 AST 指纹重合**完全且严格地局限于「文件系统/第三方库」这一单一模块**。参考项目几乎完全由该第三方库构成（覆盖率 93.22%），而该库仅占目标项目代码的 39.14%。目标项目其余 60.86% 的代码（涵盖进程管理、内存管理、中断处理、系统调用、设备驱动等完整操作系统内核核心模块）在参考项目中**零匹配**。双方在操作系统内核的核心架构上存在根本性差异，无任何同源迹象。重合部分源于双方均使用了开源 `lwext4` 库，属于公共依赖，不构成抄袭。

### 2. 全模块覆盖矩阵解读
- **进程管理**: 仅目标项目存在（48个函数）。参考项目无此模块。共同 AST 指纹为 0。该模块证据强度：**未形成**。
- **任务调度/异步运行时**: 仅目标项目存在（61个函数）。参考项目无此模块。共同 AST 指纹为 0。该模块证据强度：**未形成**。
- **内存管理**: 仅目标项目存在（197个函数）。参考项目无此模块。共同 AST 指纹为 0。该模块证据强度：**未形成**。
- **中断/异常处理**: 仅目标项目存在（22个函数）。参考项目无此模块。共同 AST 指纹为 0。该模块证据强度：**未形成**。
- **系统调用**: 仅目标项目存在（46个函数）。参考项目无此模块。共同 AST 指纹为 0。该模块证据强度：**未形成**。
- **文件系统**: 仅目标项目存在（118个函数）。参考项目无此模块。共同 AST 指纹为 0。该模块证据强度：**未形成**。
- **设备驱动**: 仅目标项目存在（28个函数）。参考项目无此模块。共同 AST 指纹为 0。该模块证据强度：**未形成**。
- **网络协议栈**: 仅目标项目存在（1个函数）。参考项目无此模块。共同 AST 指纹为 0。该模块证据强度：**未形成**。
- **第三方库/公共依赖**: 双方均存在。目标项目 534 个函数，参考项目 478 个函数。共同 AST 指纹 371 个。目标项目覆盖率 82.08%，参考项目覆盖率 93.22%。该模块证据强度：**强（但指向公共库同源，非项目间抄袭）**。

### 3. 底层 AST 关键证据指纹表
| 所属板块 | 目标项目路径 | 参考项目路径 | 证据类型 | 技术说明 |
| :--- | :--- | :--- | :--- | :--- |
| 第三方库/公共依赖 | `kernel/fs/ext4_balloc.c#ext4_balloc_alloc_block` | `Kernel/File/lwext4/ext4_balloc.c#ext4_balloc_alloc_block` | 同板块同名同结构 | 均为 `lwext4` 库标准块分配函数，调用图谱一致。 |
| 第三方库/公共依赖 | `kernel/fs/ext4.c#ext4_dir_rm` | `Kernel/File/lwext4/ext4.c#ext4_dir_rm` | 同板块底层逻辑一致 | 均为 `lwext4` 库标准目录删除函数，调用图谱一致。 |
| 第三方库/公共依赖 | `kernel/fs/ext4_journal.c#jbd_journal_prepare` | `Kernel/File/lwext4/ext4_journal.c#jbd_journal_prepare` | 同板块同名 | 均为 `lwext4` 库日志准备函数，调用图谱一致。 |
| 第三方库/公共依赖 | `kernel/fs/ext4_dir_idx.c#ext4_dir_dx_split_index` | `Kernel/File/lwext4/ext4_dir_idx.c#ext4_dir_dx_split_index` | 同板块底层逻辑一致 | 均为 `lwext4` 库目录索引分裂函数，调用图谱一致。 |
| 第三方库/公共依赖 | `kernel/fs/ext4_extent.c#ext4_extent_remove_space` | `Kernel/File/lwext4/ext4_extent.c#ext4_extent_remove_space` | 同板块底层逻辑一致 | 均为 `lwext4` 库 extent 空间移除函数，调用图谱一致。 |
| 第三方库/公共依赖 | `kernel/fs/ext4_fs.c#ext4_fs_truncate_inode` | `Kernel/File/lwext4/ext4_fs.c#ext4_fs_truncate_inode` | 同板块底层逻辑一致 | 均为 `lwext4` 库 inode 截断函数，调用图谱一致。 |
| 第三方库/公共依赖 | `kernel/fs/ext4_xattr.c#ext4_xattr_set_entry` | `Kernel/File/lwext4/ext4_xattr.c#ext4_xattr_set_entry` | 同板块底层逻辑一致 | 均为 `lwext4` 库扩展属性设置函数，调用图谱一致。 |
| 第三方库/公共依赖 | `kernel/fs/ext4_mkfs.c#fill_sb` | `Kernel/File/lwext4/ext4_mkfs.c#fill_sb` | 同板块底层逻辑一致 | 均为 `lwext4` 库超级块填充函数，调用图谱一致。 |
| 进程管理 | `kernel/process.c#scheduler` | (无) | 仅目标项目存在 | 目标项目独立实现的进程调度器。 |
| 内存管理 | `kernel/pmem.c#buddy_free` | (无) | 仅目标项目存在 | 目标项目独立实现的伙伴系统内存释放。 |
| 中断/异常处理 | `hsai/hsai_trap.c#usertrap` | (无) | 仅目标项目存在 | 目标项目独立实现的用户态陷阱处理。 |
| 系统调用 | `kernel/syscall.c#syscall` | (无) | 仅目标项目存在 | 目标项目独立实现的系统调用分发入口。 |
| 设备驱动 | `hal/riscv/uart.c#uart_init` | (无) | 仅目标项目存在 | 目标项目独立实现的 UART 驱动初始化。 |

### 4. 核心代码调用流对比剖析
* **【进程管理】**
* **底层技术析理**: 目标项目拥有完整的进程管理模型，包含进程调度器 (`scheduler`)、进程数据结构 (`myproc`)、同步原语 (`acquire`, `release`) 和 Futex 机制。参考项目中**完全不存在**任何进程管理相关的代码或调用流。双方在操作系统最核心的抽象层上无任何可比性。
* **代码溯源链接**:
  - 目标项目: `kernel/process.c#scheduler`
  - 参考项目: (无)

* **【内存管理】**
* **底层技术析理**: 目标项目实现了基于伙伴系统 (`buddy_free`, `buddy_free_tracked`) 的物理内存管理，以及 VMA (`alloc_vma_stack`) 和页表操作 (`PGROUNDUP`) 等虚拟内存管理。参考项目中**完全不存在**任何内存管理相关的代码或调用流。双方的内存管理模型和抽象层次完全不同。
* **代码溯源链接**:
  - 目标项目: `kernel/pmem.c#buddy_free`
  - 参考项目: (无)

* **【中断/异常处理】**
* **底层技术析理**: 目标项目实现了针对 RISC-V 和 LoongArch 双架构的陷阱处理机制 (`usertrap`, `kerneltrap`)，涉及 CSR 寄存器操作 (`w_csr_eentry`)、异常分发 (`handle_exception`) 和设备中断 (`devintr`)。参考项目中**完全不存在**任何中断/异常处理相关的代码或调用流。
* **代码溯源链接**:
  - 目标项目: `hsai/hsai_trap.c#usertrap`
  - 参考项目: (无)

* **【第三方库/公共依赖】**
* **底层技术析理**: 双方在 `ext4` 文件系统实现上的调用流高度一致，例如 `ext4_dir_rm` -> `ext4_dir_iterator_init` -> `ext4_fs_free_inode`。这是因为双方均移植或集成了同一个开源库 `lwext4`。目标项目将调试宏从 `EXT4_Debug_*` 替换为 `ext4_dbg`，体现了独立的工程适配。此相似性源于公共依赖，非项目间抄袭。
* **代码溯源链接**:
  - 目标项目: `kernel/fs/ext4.c#ext4_dir_rm`
  - 参考项目: `Kernel/File/lwext4/ext4.c#ext4_dir_rm`

### 5. 单方模块与降权项
- **单方模块**: 进程管理、任务调度/异步运行时、内存管理、中断/异常处理、系统调用、文件系统、设备驱动、网络协议栈、通用模块/其他。这九个模块合计 **948 个唯一 AST 指纹**，仅在目标项目中存在，是目标项目作为独立操作系统内核的铁证。
- **降权项**: 所有被控方指控为“像素级复用”的 24 项主证据及 10 项弱信号，均属于「第三方库/公共依赖」模块。其函数名、调用图谱的相似性由 `lwext4` 开源库的 API 和 ext4 规范决定，属于**领域共性**和**公共接口**，不能作为项目间抄袭的强同源证据。控方将“同板块底层逻辑一致”直接等同于“抄袭”，混淆了“使用同一开源库”与“复制参考项目代码”的本质区别。

### 6. 保守结论
本次审计的 38.05% 相似度完全由双方共有的开源 `lwext4` 库贡献，属于典型的第三方库同源引用，而非项目间的代码抄袭。参考项目本身几乎就是 `lwext4` 库的镜像（覆盖率 93.22%），不具备作为原创基准的资格。目标项目是一个远超参考项目范畴的完整操作系统内核，其进程管理、内存管理、中断处理、设备驱动等 9 大核心模块（占代码总量 60.86%）在参考项目中无任何匹配，展现了充分的独立设计与开发能力。综合判定，目标项目原创性成立，不存在对参考项目的系统性代码复用或抄袭行为。

------------------------------

