# 🛡️ OS 内核赛自动化代码级架构审计报告

**审计时间**: `2026-07-02 02:13:18`
**参赛作品名称**: `2026-T2026100049911101-XX`
**年份**: `2026`
**学校**: `XX`
**队伍名称**: `T2026100049911101`
**比赛名字**: `OS-Competition`
**比赛子赛道**: `Kernel-Implementation`
**仓库地址**: `https://gitlab.eduxiji.net/educg-group-43501-3132633/T2026100049911101-938`
**推测开发语言**: `Rust / C (由AST探针自动识别)`
**特征函数量**: `680`

## ⚔️ 底层代码同源性对比 (AST Homology)
> 本部分由专门的代码审查智能体，对该项目与符合年份条件的历届最高相似度项目进行控制流与底层调用对比。新版报告会先给出确定性的全模块覆盖矩阵，再给出 Agent 的分模块解释，避免只围绕少量抽样函数展开。

### 🏆 Top 1 底层对标: vs `2025-Moncake-天津大学`（天津大学 / Moncake）

#### 对方作品信息

**对方年份**: `2025`
**对方学校**: `天津大学`
**对方队伍名称**: `Moncake`
**对方比赛名字**: `OS-Competition`
**对方比赛子赛道**: `Kernel-Implementation`
**对方仓库地址**: `https://gitlab.eduxiji.net/educg-group-36002-2710490/T202510056995244-24`

> 双向覆盖率：共同 AST 指纹: 350；唯一 AST 指纹: 目标项目 605，参考项目 656；覆盖率: 目标项目 57.85%，参考项目 53.35%

## 全模块对比总览（确定性证据）

> 总体覆盖率：共同 AST 指纹: 350；唯一 AST 指纹: 目标项目 605，参考项目 656；覆盖率: 目标项目 57.85%，参考项目 53.35%

### 1. 模块覆盖矩阵
| 模块 | 目标函数数 | 参考函数数 | 共同唯一 AST 指纹 | Target 覆盖 | Base 覆盖 | 状态 |
| :--- | ---: | ---: | ---: | ---: | ---: | :--- |
| 进程管理 | 32 | 50 | 10 | 37.04% | 27.78% | 双方存在且有结构重合 |
| 任务调度/异步运行时 | 118 | 144 | 37 | 31.36% | 26.06% | 双方存在且有结构重合 |
| 内存管理 | 51 | 61 | 15 | 29.41% | 24.59% | 双方存在且有结构重合 |
| 中断/异常处理 | 15 | 7 | 6 | 40.0% | 85.71% | 双方存在且有结构重合 |
| 系统调用 | 34 | 42 | 3 | 12.5% | 7.89% | 双方存在且有结构重合 |
| 文件系统 | 190 | 191 | 124 | 75.61% | 74.7% | 双方存在且有结构重合 |
| 设备驱动 | 47 | 52 | 34 | 87.18% | 75.56% | 双方存在且有结构重合 |
| 网络协议栈 | 6 | 6 | 6 | 100.0% | 100.0% | 双方存在且有结构重合 |
| 文件系统/第三方库 | 54 | 54 | 33 | 67.35% | 67.35% | 双方存在且有结构重合 |
| 第三方库 | 115 | 107 | 80 | 72.73% | 74.77% | 双方存在且有结构重合 |
| 通用模块/其他 | 18 | 16 | 10 | 58.82% | 66.67% | 双方存在且有结构重合 |

### 2. 分模块证据清单

#### 进程管理
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `kernel/src/syscall/task.rs#sys_wait4` | `kernel/src/syscall/task.rs#sys_wait4` | 同板块同名 | Err, Ok, WaitPid, child.exit_code, child.exit_code().unwrap, child.release ...(+25) | Err, Ok, WaitPid, child_task.clone, child_task.clone().map_or, child_task.pcb.lock ...(+27) |
| `kernel/src/syscall/fd.rs#sys_epoll_wait` | `kernel/src/syscall/fd.rs#sys_epoll_wait` | 同板块同名 | Duration::from_millis, Ok, current_time, debug!, epfile.data.lock, epfile.data.lock().iter ...(+11) | Ok, current_nsec, debug!, epfile.data.lock, epfile.data.lock().iter, ev.clone ...(+10) |
| `kernel/src/tasks/initproc.rs#initproc` | `kernel/src/tasks/initproc.rs#initproc` | 同板块同名 | TASK_MAP
        .lock, TASK_MAP
        .lock()
        .values, TASK_MAP
        .lock()
        .values()
        .all, println!, run_tests, shutdown ...(+6) | "/glibc/lib".to_string, "/glibc/lib/ld-linux-loongarch-lp64d.so.1".to_string, "/lib/ld-musl-riscv64.so.1".to_string, "/musl/lib/libc.so".to_string, "/usr/lib".to_string, PathBuf::from ...(+21) |
| `kernel/src/syscall/task.rs#sys_exit_group` | `kernel/src/syscall/task.rs#sys_exit_group` | 同板块同名 | Ok, Some, debug!, pcb.lock, pcb_guard.threads.clone, self.task.exit ...(+3) | Ok, debug!, self.task.exit |

#### 任务调度/异步运行时
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `kernel/src/syscall/mod.rs#syscall` | `kernel/src/syscall/mod.rs#syscall` | 同板块同名 | Err, Ok, OpenFlags::RDWR.bits, Sysno::new, Sysno::new(call_id).ok_or, args[0].into ...(+119) | Err, Ok, OpenFlags::O_RDWR.bits, Sysno::new, Sysno::new(call_id).ok_or, args[0].into ...(+147) |
| `kernel/src/syscall/fd.rs#sys_pselect` | `kernel/src/syscall/fd.rs#sys_pselect` | 同板块同名 | Ok, cmp::min, current_time, debug!, drop, efds.get_bit ...(+24) | Ok, cmp::min, current_nsec, debug!, drop, efds.get_bit ...(+24) |
| `kernel/src/syscall/task.rs#sys_futex` | `kernel/src/syscall/task.rs#sys_futex` | 同板块同名 | Err, FutexFlags::try_from, FutexFlags::try_from(op).map_err, Ok, UserRef::<TimeSpec>::from, UserRef::<TimeSpec>::from(value2).read ...(+20) | Err, Ok, UserRef::<TimeSpec>::from, UserRef::<TimeSpec>::from(value2).get_mut, WaitFutex, WaitUntilsec ...(+20) |
| `kernel/src/syscall/task.rs#sys_clone` | `kernel/src/syscall/task.rs#sys_clone` | 同板块同名 | CloneFlags::from_bits_truncate, Ok, UserRef::from, clear_child_tid.addr, ctid.is_valid, ctid.write ...(+15) | CloneFlags::from_bits_truncate, Ok, UserRef::from, clear_child_tid.addr, ctid.get_mut, ctid.is_valid ...(+15) |

#### 内存管理
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `kernel/src/tasks/exec.rs#exec_with_process` | `kernel/src/tasks/exec.rs#exec_with_process` | 同板块同名 | (virt_addr + mem_size).div_ceil, File::open_link, File::open_link(path.clone(), OpenFlags::RDONLY)
        .map, File::open_link(path.clone(), OpenFlags::RDONLY)
        .map(Arc::new)?
        .clone, Ok, String::from ...(+69) | (virt_addr + mem_size).div_ceil, File::open, File::open(path.clone(), OpenFlags::O_RDONLY)
            .map, File::open(path.clone(), OpenFlags::O_RDONLY)
            .map(Arc::new)?
            .clone, Ok, String::from ...(+84) |
| `kernel/src/syscall/mm.rs#sys_mmap` | `kernel/src/syscall/mm.rs#sys_mmap` | 同板块同名 | Err, MapFlags::from_bits_truncate, MmapProt::from_bits_truncate, Ok, Some, UserRef::<u8>::from ...(+31) | (|| -> Result<(), Errno> {
                let buffer = UserRef::<u8>::from(addr).slice_mut_with_len(len);
                file.readat(off, buffer).map(|_| ())
            }), Err, MapFlags::from_bits_truncate, MmapProt::from_bits_truncate, Ok, Some ...(+32) |
| `kernel/src/main.rs#kernel_interrupt` | `kernel/src/main.rs#kernel_interrupt` | 同板块同名 | Duration::from_millis, current_task, current_task().and_then, current_time, current_user_task, get_int_device ...(+12) | current_task, current_task().downcast_arc::<UserTask>, current_task().downcast_arc::<UserTask>().ok, current_user_task, get_int_device, get_int_device().try_handle_interrupt ...(+8) |
| `kernel/src/user/mod.rs#user_cow_int` | `kernel/src/user/mod.rs#user_cow_int` | 同板块同名 | Arc::new, Arc::strong_count, area.mtrackers.iter_mut, area.mtrackers.iter_mut().find, area.mtrackers.push, core::ptr::write_bytes ...(+23) | Arc::new, Arc::strong_count, SignalFlags::SIGSEGV.num, Some, Stat::default, area.contains ...(+47) |

#### 中断/异常处理
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `driver/general-plic/src/plic.rs#set_irq_enable` | `driver/general-plic/src/plic.rs#set_irq_enable` | 同板块同名同结构 | read_volatile, write_volatile | read_volatile, write_volatile |
| `driver/general-plic/src/plic.rs#get_irq_claim` | `driver/general-plic/src/plic.rs#get_irq_claim` | 同板块同名同结构 | read_volatile | read_volatile |
| `driver/general-plic/src/plic.rs#complete_irq_claim` | `driver/general-plic/src/plic.rs#complete_irq_claim` | 同板块同名同结构 | write_volatile | write_volatile |
| `driver/general-plic/src/lib.rs#register_irq` | `driver/general-plic/src/lib.rs#register_irq` | 同板块同名同结构 | log::info!, self.set_irq_enable, self.set_priority | log::info!, self.set_irq_enable, self.set_priority |

#### 系统调用
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `kernel/src/syscall/sys.rs#sys_uname` | `kernel/src/syscall/sys.rs#sys_uname` | 同板块同名 | Ok, debug!, sys_domain.len, sys_machine.len, sys_name.len, sys_nodename.len ...(+9) | Ok, debug!, sys_domain.len, sys_machine.len, sys_name.len, sys_nodename.len ...(+9) |
| `kernel/src/syscall/sys.rs#sys_getrandom` | `kernel/src/syscall/sys.rs#sys_getrandom` | 同板块同名 | Ok, SEED.wrapping_mul, buf.iter_mut, buf.slice_mut_with_len, debug! | Ok, SEED.wrapping_mul, buf.iter_mut, buf.slice_mut_with_len, debug! |
| `kernel/src/syscall/types/poll.rs#ctl` | `kernel/src/syscall/types/poll.rs#ctl` | 同板块同名 | data_map.get_mut, data_map.get_mut(&fd).map, data_map.insert, data_map.remove, self.data.lock | self.data.lock, self.data.lock().get_mut, self.data.lock().get_mut(&fd).map, self.data.lock().insert, self.data.lock().remove |
| `kernel/src/syscall/shm.rs#sys_shmctl` | `kernel/src/syscall/shm.rs#sys_shmctl` | 同板块同名 | Err, Ok, SHARED_MEMORY.lock, SHARED_MEMORY.lock().get_mut, debug!, map.deleted.lock | Err, Ok, SHARED_MEMORY.lock, SHARED_MEMORY.lock().get_mut, debug!, map.deleted.lock |

#### 文件系统
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `filesystem/devfs/src/tty.rs#ioctl` | `filesystem/devfs/src/rtc.rs#ioctl` | 同板块同名 | (arg as *mut Termios).as_mut, (arg as *mut Termios).as_mut().unwrap, (arg as *mut Termios).write_volatile, (arg as *mut WinSize).as_mut, (arg as *mut WinSize).as_mut().unwrap, (arg as *mut u32).as_mut ...(+7) | (arg as *mut RtcTime).as_mut, (arg as *mut RtcTime).as_mut().unwrap, Ok |
| `kernel/src/socket.rs#readat` | `kernel/src/socket.rs#readat` | 同板块同名同结构 | DebugConsole::putchar, Err, Ok, buffer.len, buffer[..rlen].copy_from_slice, cmp::min ...(+7) | DebugConsole::putchar, Err, Ok, buffer.len, buffer[..rlen].copy_from_slice, cmp::min ...(+7) |
| `kernel/src/utils/mod.rs#hexdump` | `kernel/src/utils/mod.rs#hexdump` | 同板块同名同结构 | (0..data.len()).step_by, data.len, print!, println! | (0..data.len()).step_by, data.len, print!, println! |
| `filesystem/fs/src/fatfs_shim.rs#link` | `filesystem/fs/src/fatfs_shim.rs#link` | 同板块同名同结构 | unimplemented! | unimplemented! |

#### 设备驱动
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `driver/kvirtio/src/lib.rs#dump_bar_contents` | `driver/kvirtio/src/lib.rs#dump_bar_contents` | 同板块同名 | Mutex::new, PCI_RANGES.lock, assert!, buf.as_mut_ptr, buf.iter, buf.iter().any ...(+8) | buf.as_mut_ptr, buf.iter, buf.iter().any, core::ptr::copy, info!, root.bar_info ...(+3) |
| `driver/kvirtio/src/lib.rs#enumerate_pci` | `driver/kvirtio/src/lib.rs#enumerate_pci` | 同板块同名 | MmioCam::new, PciRoot::<MmioCam>::new, PciTransport::new::<HalImpl, MmioCam>, PciTransport::new::<HalImpl, MmioCam>(&mut pci_root, device_function).unwrap, dump_bar_contents, info! ...(+5) | MmioCam::new, PciRoot::<MmioCam>::new, PciTransport::new::<HalImpl, MmioCam>, assign_bars_with_pci_root, error!, info! ...(+7) |
| `driver/kvirtio/src/lib.rs#init_mmio` | `driver/kvirtio/src/lib.rs#init_mmio` | 同板块同名同结构 | Arc::new, MmioTransport::new, NonNull::new, NonNull::new(vaddr as *mut VirtIOHeader).unwrap, info!, node.reg ...(+3) | Arc::new, MmioTransport::new, NonNull::new, NonNull::new(vaddr as *mut VirtIOHeader).unwrap, info!, node.reg ...(+3) |
| `driver/ns16550a/src/lib.rs#init_driver` | `driver/ns16550a/src/lib.rs#init_driver` | 同板块同名同结构 | Arc::new, Uart::new, info!, node.reg, node.reg().unwrap, node.reg().unwrap().next ...(+5) | Arc::new, Uart::new, info!, node.reg, node.reg().unwrap, node.reg().unwrap().next ...(+5) |

#### 网络协议栈
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `kernel/src/socket.rs#reuse` | `kernel/src/socket.rs#reuse` | 同板块同名同结构 | Mutex::new, NET_SERVER.get_tcp, NET_SERVER.get_udp, self.inner.get_protocol, self.inner.get_protocol().unwrap, self.options.lock ...(+4) | Mutex::new, NET_SERVER.get_tcp, NET_SERVER.get_udp, self.inner.get_protocol, self.inner.get_protocol().unwrap, self.options.lock ...(+4) |
| `kernel/src/socket.rs#new_with_inner` | `kernel/src/socket.rs#new_with_inner` | 同板块同名同结构 | Arc::new, Mutex::new, vec! | Arc::new, Mutex::new, vec! |
| `kernel/src/socket.rs#from_usize` | `kernel/src/socket.rs#from_usize` | 同板块同名同结构 | Some | Some |
| `kernel/src/user/socket_pair.rs#create_socket_pair` | `kernel/src/user/socket_pair.rs#create_socket_pair` | 同板块同名同结构 | Arc::new, Mutex::new, VecDeque::new | Arc::new, Mutex::new, VecDeque::new |

#### 文件系统/第三方库
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `filesystem/ext4rsfs/src/lib.rs#write_offset` | `filesystem/fs/src/ext4_rs_shim.rs#write_offset` | 同板块同名 | buf.len, bytes_to_write.div_ceil, data[offset_in_block..offset_in_block + bytes_to_copy]
                .copy_from_slice, device.read_blocks, device.write_blocks, get_blk_device ...(+3) | buf.len, bytes_to_write.div_ceil, data[offset_in_block..offset_in_block + bytes_to_copy]
                .copy_from_slice, device.read_blocks, device.write_blocks, get_blk_device ...(+1) |
| `filesystem/ext4rsfs/src/lib.rs#lookup` | `filesystem/fs/src/ext4_rs_shim.rs#lookup` | 同板块同名同结构 | Arc::new, Errno::new, Ok, log::debug!, name.as_bytes, self
            .ext4
            .generic_open ...(+6) | Arc::new, Errno::new, Ok, log::debug!, name.as_bytes, self
            .ext4
            .generic_open ...(+6) |
| `filesystem/ext4rsfs/src/lib.rs#read_offset` | `filesystem/fs/src/ext4_rs_shim.rs#read_offset` | 同板块同名同结构 | buf[buf_start..buf_end]
                .copy_from_slice, device.read_blocks, get_blk_device, get_blk_device(self.device_id).unwrap, vec! | buf[buf_start..buf_end]
                .copy_from_slice, device.read_blocks, get_blk_device, get_blk_device(self.device_id).unwrap, vec! |
| `filesystem/ext4rsfs/src/lib.rs#read_dir` | `filesystem/fs/src/ext4_rs_shim.rs#read_dir` | 同板块同名 | Ok, String::from, Vec::new, child_ref.inode.is_dir, child_ref.inode.is_link, core::str::from_utf8 ...(+4) | todo! |

#### 第三方库
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `crates/runtime/src/frame.rs#alloc_much` | `crates/runtime/src/frame.rs#alloc_much` | 同板块同名 | (i..j).for_each, FrameTracker::new, Some, Vec::new, ans.push, pa! ...(+4) | FrameTracker::new, Some, Vec::new, ans.push, max_consecutive.max, pa! ...(+6) |
| `crates/executor/src/executor.rs#run_ready_task` | `crates/executor/src/executor.rs#run_ready_task` | 同板块同名同结构 | Arc::new, Arc::new(Waker {
                task_id: task.get_task_id(),
            })
            .into, Context::from_waker, Some, TASK_QUEUE.lock, TASK_QUEUE.lock().pop_front ...(+8) | Arc::new, Arc::new(Waker {
                task_id: task.get_task_id(),
            })
            .into, Context::from_waker, Some, TASK_QUEUE.lock, TASK_QUEUE.lock().pop_front ...(+8) |
| `crates/devices/src/device.rs#add_device` | `crates/devices/src/device.rs#add_device` | 同板块同名 | INT_DEVICE.init_by, MAIN_UART.init_by, device.clone, device.get_device_wrapper, self.blk.push, self.input.push ...(+4) | INT_DEVICE.init_by, MAIN_UART.init_by, device.clone, device.get_device_wrapper, self.blk.push, self.input.push ...(+4) |
| `crates/devices/src/lib.rs#try_to_add_device` | `crates/devices/src/lib.rs#try_to_add_device` | 同板块同名 | ALL_DEVICES.lock, ALL_DEVICES.lock().add_device, DRIVER_REGS.lock, driver_manager.get, f, info! ...(+2) | ALL_DEVICES.lock, ALL_DEVICES.lock().add_device, DRIVER_REGS.lock, driver_manager.get, f, info! ...(+2) |

#### 通用模块/其他
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `kernel/src/logging.rs#write_str` | `kernel/src/logging.rs#write_str` | 同板块同名同结构 | Ok, c.encode_utf8, c.encode_utf8(&mut buffer).as_bytes, puts, s.chars | Ok, c.encode_utf8, c.encode_utf8(&mut buffer).as_bytes, puts, s.chars |
| `kernel/src/utils/useref.rs#slice_until_valid` | `kernel/src/utils/useref.rs#slice_until_valid` | 同板块同名同结构 | self.addr.raw, self.addr.slice_until | self.addr.raw, self.addr.slice_until |
| `kernel/src/logging.rs#print` | `kernel/src/logging.rs#print` | 同板块同名同结构 | Logger
        .write_fmt, Logger
        .write_fmt(args)
        .expect | Logger
        .write_fmt, Logger
        .write_fmt(args)
        .expect |
| `kernel/src/utils/useref.rs#slice_mut_with_len` | `kernel/src/utils/useref.rs#slice_mut_with_len` | 同板块同名同结构 | self.addr.slice_mut_with_len | self.addr.slice_mut_with_len |


好的，合议庭已收到控辩双方材料及底层AST证据。现依据审计指令，对本案进行最终同源性定性仲裁。

### 1. 宏观判定
- **定性结论**: **独立开发**
- **核心依据**: 客观 AST 相似度为 38.42%，低于 50% 的强同源阈值。双向覆盖率（目标 57.85%，参考 53.35%）表明两者互为非子集关系，各自拥有大量独立代码。控方所指控的“复用”主要集中在 POSIX 系统调用接口、Rust 语言惯用法及 VFS 框架约束下的必然收敛，辩方提供的架构差异证据（如任务ID模型、时间抽象、线程组策略）被采信。

### 2. 全模块覆盖矩阵解读
| 模块 | 双方存在 | 共同指纹 | 目标覆盖 | 参考覆盖 | 证据强度 | 解读 |
| :--- | :---: | :---: | :---: | :---: | :---: | :--- |
| **进程管理** | 是 | 10 | 37.04% | 27.78% | **弱** | 覆盖率低，共同指纹多为 `sys_getpid` 等简单系统调用或 `procfs` 的 trivial 函数。控方所列“铁证”实为接口收敛或语言惯用法。 |
| **任务调度/异步运行时** | 是 | 37 | 31.36% | 26.06% | **弱** | 覆盖率低。`syscall` 分发、`sys_clone` 等核心函数存在 `task_id`/`process_id`、`current_time`/`current_nsec` 等实质性架构差异。 |
| **内存管理** | 是 | 15 | 29.41% | 24.59% | **弱** | 覆盖率低。`sys_mmap`、`exec_with_process` 等核心函数虽有共同调用，但辩方未重点指控，且此类函数受 POSIX 语义强约束。 |
| **中断/异常处理** | 是 | 6 | 40.0% | 85.71% | **弱** | 参考项目覆盖率极高因其函数总数少。共同指纹为 PLIC 驱动等硬件操作，逻辑由硬件手册决定，无独创性。 |
| **系统调用** | 是 | 3 | 12.5% | 7.89% | **未形成** | 覆盖率极低，共同指纹极少，表明双方系统调用层的实现细节差异巨大。 |
| **文件系统** | 是 | 124 | 75.61% | 74.7% | **中** | 覆盖率较高，但共同指纹大量集中于 `fatfs_shim`、`ext4_rs_shim` 等第三方库适配层及 `procfs`/`devfs` 的 VFS 接口实现，属于生态代码和框架约束。 |
| **设备驱动** | 是 | 34 | 87.18% | 75.56% | **中** | 覆盖率较高，共同指纹集中于 `kvirtio`、`ns16550a` 等常见设备驱动框架，初始化流程由硬件和驱动模型决定。 |
| **网络协议栈** | 是 | 6 | 100.0% | 100.0% | **弱** | 函数总数极少（6个），100%覆盖无统计学意义。共同指纹为 socket 的 trivial 构造或复用函数。 |
| **文件系统/第三方库** | 是 | 33 | 67.35% | 67.35% | **中** | 覆盖率相同，证实双方引用了高度相似的第三方库（如 `ext4_rs`）或实现了相同的 VFS trait，属于生态代码。 |
| **第三方库** | 是 | 80 | 72.73% | 74.77% | **中** | 覆盖率较高，共同指纹集中于 `runtime`、`executor`、`devices` 等 crate，属于 Rust 异步运行时和驱动模型的通用实现模式。 |
| **通用模块/其他** | 是 | 10 | 58.82% | 66.67% | **弱** | 共同指纹为 `logging`、`useref` 等基础工具函数，实现简单且模式固定。 |

### 3. 底层 AST 关键证据指纹表
| 所属板块 | 目标项目路径 | 参考项目路径 | 证据类型 | 技术说明 |
| :--- | :--- | :--- | :--- | :--- |
| 进程管理 | `kernel/src/syscall/task.rs#sys_getppid` | `kernel/src/syscall/task.rs#sys_getppid` | 架构差异 | 目标使用 `x.task_id`，参考使用 `x.process_id`，揭示两者任务/进程 ID 模型不同。 |
| 进程管理 | `kernel/src/syscall/task.rs#sys_exit_group` | `kernel/src/syscall/task.rs#sys_exit_group` | 架构差异 | 目标遍历 `pcb_guard.threads` 并写 `tcb`，实现线程组退出；参考仅调用 `self.task.exit`，模型更简单。 |
| 进程管理 | `kernel/src/tasks/initproc.rs#initproc` | `kernel/src/tasks/initproc.rs#initproc` | 架构差异 | 参考硬编码多架构 libc 路径，目标无此逻辑，采用 `run_tests`/`start_shell` 测试驱动初始化。 |
| 任务调度/异步运行时 | `kernel/src/syscall/mod.rs#syscall` | `kernel/src/syscall/mod.rs#syscall` | 架构差异 | 目标使用 `OpenFlags::RDWR`，参考使用 `OpenFlags::O_RDWR`；外部调用数不同 (117 vs 145)。 |
| 任务调度/异步运行时 | `kernel/src/syscall/fd.rs#sys_epoll_wait` | `kernel/src/syscall/fd.rs#sys_epoll_wait` | 架构差异 | 目标使用 `Duration::from_millis` + `current_time`，参考使用 `current_nsec`，时间子系统抽象不同。 |
| 任务调度/异步运行时 | `kernel/src/syscall/task.rs#sys_clone` | `kernel/src/syscall/task.rs#sys_clone` | 架构差异 | 目标使用 `ctid.write`，参考使用 `ctid.get_mut`，用户态内存访问 API 不同。 |
| 文件系统 | `filesystem/procfs/src/lib.rs#read_dir` | `filesystem/procfs/src/lib.rs#read_dir` | 实现差异 | 目标使用 `self.inner.map.keys()`，参考使用 `self.inner.map.iter()`，内部迭代策略不同。 |
| 文件系统 | `filesystem/procfs/src/lib.rs#stat` | `filesystem/procfs/src/cpuinfo.rs#stat` | 架构差异 | 目标 `stat` 直接返回 `Ok`，参考调用 `Self::generate_content`，内容生成时机与位置不同。 |
| 第三方库 | `crates/runtime/src/frame.rs#alloc_much` | `crates/runtime/src/frame.rs#alloc_much` | 实现差异 | 目标使用 `(i..j).for_each`，参考使用 `max_consecutive.max`，物理帧分配策略不同。 |

### 4. 核心代码调用流对比剖析
* **【进程管理 / `sys_wait4` 系统调用】**
* **底层技术析理**: 两者均实现了 POSIX `wait4` 语义，核心流程为查找子进程、获取退出码、释放资源。然而，对象管理模型存在差异：目标项目通过 `child.exit_code().unwrap()` 直接访问子任务状态，随后调用 `child.release`；参考项目则先 `child_task.clone()` 克隆任务句柄，再通过 `map_or` 和 `pcb.lock` 加锁访问进程控制块。这反映了不同的内部任务生命周期管理策略。
* **代码溯源链接**:
  - 目标项目: `kernel/src/syscall/task.rs#sys_wait4`
  - 参考项目: `kernel/src/syscall/task.rs#sys_wait4`

* **【任务调度/异步运行时 / `sys_epoll_wait` 系统调用】**
* **底层技术析理**: 两者均实现了 epoll 等待的核心逻辑：遍历事件列表、检查事件状态、将就绪事件拷贝至用户空间。关键差异在于时间管理抽象：目标项目使用 `current_time` 和 `Duration::from_millis` 进行超时计算，表明其时间子系统基于 `Duration` 抽象；参考项目则直接使用 `current_nsec` 纳秒级时间戳，暴露了更底层的时间实现。这并非简单的变量重命名，而是两套不同的时间子系统 API 设计。
* **代码溯源链接**:
  - 目标项目: `kernel/src/syscall/fd.rs#sys_epoll_wait`
  - 参考项目: `kernel/src/syscall/fd.rs#sys_epoll_wait`

* **【任务调度/异步运行时 / `syscall` 总分发函数】**
* **底层技术析理**: 两者均为系统调用总入口，执行参数解析与分发。尽管流程相似，但目标项目外部调用为 117 个，参考项目为 145 个，表明支持的系统调用数量或内部调用层次不同。此外，文件打开标志常量命名不同（`RDWR` vs `O_RDWR`），指向独立的常量定义体系。
* **代码溯源链接**:
  - 目标项目: `kernel/src/syscall/mod.rs#syscall`
  - 参考项目: `kernel/src/syscall/mod.rs#syscall`

### 5. 单方模块与降权项
*   **单方特有证据**: 参考项目中的 `detect_libc_type`、`command_iperf` 等函数在目标项目中无对应证据，属于参考项目独有实现，不构成同源指控。
*   **跨板块硬凑证据**: 控方将 `filesystem/procfs/` 下的 `stat`、`readat`、`new` 等函数归入【进程管理】板块进行指控。这些函数属于文件系统 VFS 层实现，其函数名和签名由 trait 强制约束，同名是必然的。此类跨板块证据已被降权，不构成同板块主证据。
*   **Trivial 函数**: `sys_getpid` (仅返回 `Ok`)、`exit_code` (仅 `self.pcb.lock`)、`root_dir` (仅 `Arc::new(self.root.clone)`) 等函数逻辑过于简单，在任何实现中都会高度相似，不具备鉴别力，已从强证据中排除。
*   **第三方库/公共依赖**: `filesystem/ext4rsfs`、`crates/executor`、`driver/kvirtio` 等路径下的高度相似，源于对 `ext4_rs`、`virtio-drivers` 等开源库的封装或 Rust 异步运行时的通用设计模式，属于生态代码复用，不能作为核心逻辑抄袭的证据。

### 6. 保守结论
合议庭裁定，目标项目 `[2026-T2026100049911101-XX]` 与参考项目 `[2025-Moncake-天津大学]` 在宏观架构上存在显著差异，不支持“系统性代码复用”的指控。38.42% 的 AST 相似度在同时实现 POSIX 接口的 Rust 操作系统内核领域属于正常范围，主要由接口收敛、语言惯用法和第三方库适配代码贡献。辩方提出的任务 ID 模型、时间抽象、线程组策略、初始化流程等核心差异，是独立开发的有力证明。控方基于表层 AST 指纹的“铁证”指控，在深入剖析调用流和架构设计后均不成立。因此，综合全模块证据，认定两个项目为独立开发。

------------------------------

