# 🛡️ OS 内核赛自动化代码级架构审计报告

**审计时间**: `2026-07-02 02:13:18`
**参赛作品名称**: `2026-T2026100049911101-XX`
**年份**: `2026`
**学校**: `XX`
**队伍名称**: `T2026100049911101`
**比赛名字**: `OS-Competition`
**比赛子赛道**: `Kernel-Implementation`
**仓库地址**: `https://gitlab.eduxiji.net/educg-group-43501-3132633/T2026100049911101-938`
**推测开发语言**: `Rust / C (由AST探针自动识别)`
**特征函数量**: `680`

## ⚔️ 底层代码同源性对比 (AST Homology)
> 本部分由专门的代码审查智能体，对该项目与符合年份条件的历届最高相似度项目进行控制流与底层调用对比。新版报告会先给出确定性的全模块覆盖矩阵，再给出 Agent 的分模块解释，避免只围绕少量抽样函数展开。

### 🏆 Top 4 底层对标: vs `2023-MankorOS-哈尔滨工业大学深圳`（哈尔滨工业大学（深圳） / MankorOS）

#### 对方作品信息

**对方年份**: `2023`
**对方学校**: `哈尔滨工业大学（深圳）`
**对方队伍名称**: `MankorOS`
**对方比赛名字**: `OS-Competition`
**对方比赛子赛道**: `Kernel-Implementation`
**对方仓库地址**: `https://gitlab.eduxiji.net/MankorOS/OSKernel2023-MankorOS`

> 双向覆盖率：共同 AST 指纹: 21；唯一 AST 指纹: 目标项目 605，参考项目 1233；覆盖率: 目标项目 3.47%，参考项目 1.7%

## 全模块对比总览（确定性证据）

> 总体覆盖率：共同 AST 指纹: 21；唯一 AST 指纹: 目标项目 605，参考项目 1233；覆盖率: 目标项目 3.47%，参考项目 1.7%

### 1. 模块覆盖矩阵
| 模块 | 目标函数数 | 参考函数数 | 共同唯一 AST 指纹 | Target 覆盖 | Base 覆盖 | 状态 |
| :--- | ---: | ---: | ---: | ---: | ---: | :--- |
| 进程管理 | 32 | 264 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |
| 任务调度/异步运行时 | 118 | 79 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |
| 内存管理 | 51 | 320 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |
| 中断/异常处理 | 15 | 28 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |
| 系统调用 | 34 | 12 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |
| 文件系统 | 190 | 475 | 1 | 0.61% | 0.26% | 双方存在且有结构重合 |
| 设备驱动 | 47 | 123 | 2 | 5.13% | 1.85% | 双方存在且有结构重合 |
| 网络协议栈 | 6 | 0 | 0 | 0.0% | 0.0% | 仅目标项目体现 |
| 文件系统/第三方库 | 54 | 0 | 0 | 0.0% | 0.0% | 仅目标项目体现 |
| 第三方库 | 115 | 0 | 0 | 0.0% | 0.0% | 仅目标项目体现 |
| 通用模块/其他 | 18 | 159 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |

### 2. 分模块证据清单

#### 进程管理
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `kernel/src/syscall/task.rs#sys_exit` | `src/syscall/process.rs#sys_exit` | 同板块同名 | Ok, debug!, self.task.thread_exit | Ok, info!, self.cx.syscall_args, self.lproc.set_exit_code |
| `kernel/src/syscall/task.rs#sys_getppid` | `src/syscall/process.rs#sys_getppid` | 同板块同名 | debug!, self.task
            .parent
            .read, self.task
            .parent
            .read()
            .upgrade, self.task
            .parent
            .read()
            .upgrade()
            .map, self.task
            .parent
            .read()
            .upgrade()
            .map(|x| x.task_id)
            .ok_or | Ok, info!, self.lproc.parent_id, self.lproc.parent_id().into |
| `filesystem/procfs/src/lib.rs#lookup` | `src/fs/procfs/mod.rs#lookup` | 同板块同名 | self.inner.map.get, self.inner.map.get(name).cloned, self.inner.map.get(name).cloned().ok_or | GlobalLProcManager::get, GlobalLProcManager::get(pid.into()).ok_or, Ok, ProcFSProcDir::new, VfsFileRef::new, dyn_future ...(+7) |
| `kernel/src/syscall/task.rs#sys_getpid` | `src/syscall/process.rs#sys_getpid` | 同板块同名 | Ok | Ok, g.tgid, info!, self.lproc.with_group, self.lproc.with_group(|g| g.tgid()).into |

#### 任务调度/异步运行时
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `kernel/src/syscall/mod.rs#syscall` | `src/syscall/mod.rs#syscall` | 同板块同名 | Err, Ok, OpenFlags::RDWR.bits, Sysno::new, Sysno::new(call_id).ok_or, args[0].into ...(+119) | AutoSUM::new, Err, info!, self.cx.set_user_a0, self.cx.set_user_pc_to_next, self.cx.syscall_args ...(+73) |
| `kernel/src/syscall/fd.rs#sys_pselect` | `src/syscall/io.rs#sys_pselect` | 同板块同名 | Ok, cmp::min, current_time, debug!, drop, efds.get_bit ...(+24) | AnyFuture::new_with, BTreeMap::<usize, (usize, PollKind)>::new, FdSet::zero, Ok, Some, UserInOutPtr::<FdSet>::from ...(+39) |
| `kernel/src/syscall/fd.rs#sys_fcntl` | `src/syscall/fs.rs#sys_fcntl` | 同板块同名 | Arc::make_mut, FcntlCmd::try_from, FcntlCmd::try_from(cmd).map_err, Ok, OpenFlags::from_bits_truncate, debug! ...(+7) | NewFdRequirement::GreaterThan, Ok, f.dup, f.get, f.get(fd).unwrap, info! ...(+3) |
| `kernel/src/syscall/fd.rs#sys_renameat2` | `src/syscall/fs.rs#sys_renameat2` | 同板块同名 | Ok, OpenFlags::from_bits_truncate, buffer.len, debug!, flags.clone, new_file.truncate ...(+12) | Err, Ok, UserReadPtr::<u8>::from, new_dir.attach, new_dir.clone, new_dir.kind ...(+11) |

#### 内存管理
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `kernel/src/syscall/fd.rs#sys_openat` | `src/syscall/io.rs#sys_openat` | 同板块同名 | Arc::new, Ok, OpenFlags::from_bits_truncate, debug!, filename.get_cstr, filename.get_cstr().map_err ...(+6) | Box::new, Err, Ok, OpenFlags::from_bits_truncate, Path::from_str, Path::from_string ...(+27) |
| `filesystem/ramfs/src/lib.rs#create` | `src/fs/nfat32/file.rs#create` | 同板块同名 | Arc::new, Default::default, FileContainer::Dir, FileContainer::File, Mutex::new, Ok ...(+7) | ClusterChain::new, Ok, StdEntryEditor::new_free, dyn_future, f.alloc, kind.into ...(+2) |
| `filesystem/ramfs/src/lib.rs#truncate` | `src/fs/nfat32/file.rs#truncate` | 同板块同名 | Ok, frame_alloc, frame_alloc().expect, log::info!, page.0.add, page.0.add(offset).clear_len ...(+10) | Ok, debug_assert!, dyn_future, self.chain.alloc_push, self.chain.free_pop, self.chain.len ...(+3) |
| `kernel/src/syscall/shm.rs#sys_shmget` | `src/syscall/memory.rs#sys_shmget` | 同板块同名 | Arc::new, Err, Ok, SHARED_MEMORY
                .lock, SHARED_MEMORY
                .lock()
                .insert, SHARED_MEMORY.lock ...(+15) | Err, Ok, Some, f.alloc, global_shm_mgr, info! ...(+5) |

#### 中断/异常处理
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `kernel/src/user/signal.rs#default_signal_action` | `src/trap/kernel_interrupt.rs#kernel_default_interrupt` | 同模块上下文对齐/差异观察 | 内部闭环逻辑 | drivers::get_device_manager_mut, drivers::get_device_manager_mut().interrupt_handler, panic!, scause::read, scause::read().cause, timer::timer_handler ...(+1) |
| `kernel/src/syscall/types/signal.rs#restore_ctx` | `src/drivers/serial/mod.rs#interrupt_handler` | 同模块上下文对齐/差异观察 | 内部闭环逻辑 | here!, info!, self.buffer.lock, self.buffer.lock(here!()).enqueue, self.inner.get, self.waiting.lock ...(+5) |
| `kernel/src/syscall/types/signal.rs#store_ctx` | `src/memory/frame_ref_cnt.rs#add_ref_cnt` | 同模块上下文对齐/差异观察 | 内部闭环逻辑 | intrinsics::atomic_xadd_seqcst, intrinsics::atomic_xsub_seqcst, self.get_ref_cnt, self.get_ref_cnt_ptr |
| `driver/general-plic/src/plic.rs#set_irq_enable` | `src/trap/trap.rs#set_kernel_trap` | 同模块上下文对齐/差异观察 | read_volatile, write_volatile | stvec::write, trace! |

#### 系统调用
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `kernel/src/syscall/sys.rs#sys_getuid` | `src/syscall/misc.rs#sys_getuid` | 同板块同名 | Ok | Ok, info! |
| `kernel/src/syscall/sys.rs#sys_uname` | `src/syscall/memory.rs#from` | 同模块上下文对齐/差异观察 | Ok, debug!, sys_domain.len, sys_machine.len, sys_name.len, sys_nodename.len ...(+9) | UserAreaPerm::empty, val.contains |
| `kernel/src/syscall/sys.rs#sys_getrandom` | `src/syscall/misc.rs#default` | 同模块上下文对齐/差异观察 | Ok, SEED.wrapping_mul, buf.iter_mut, buf.slice_mut_with_len, debug! | Self::from_str |
| `kernel/src/syscall/types/poll.rs#ctl` | `src/syscall/mod.rs#sys_do_nothing` | 同模块上下文对齐/差异观察 | data_map.get_mut, data_map.get_mut(&fd).map, data_map.insert, data_map.remove, self.data.lock | Ok, log::info! |

#### 文件系统
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `filesystem/devfs/src/tty.rs#ioctl` | `src/fs/new_vfs/top.rs#ioctl` | 同板块同名 | (arg as *mut Termios).as_mut, (arg as *mut Termios).as_mut().unwrap, (arg as *mut Termios).write_volatile, (arg as *mut WinSize).as_mut, (arg as *mut WinSize).as_mut().unwrap, (arg as *mut u32).as_mut ...(+7) | Ok, dyn_future |
| `filesystem/ramfs/src/lib.rs#remove` | `src/fs/new_vfs/path_cache.rs#remove` | 同板块同名 | Err, Ok, self
            .inner
            .children
            .lock, self
            .inner
            .children
            .lock()
            .extract_if, self
            .inner
            .children
            .lock()
            .extract_if(.., |x| match x {
                FileContainer::File(x) => x.name == name,
                FileContainer::Dir(_) => false,
                FileContainer::Link(x) => x.name == name,
            })
            .count | Err, Ok, dyn_future, file.mark_deleted, here!, self.extract_file ...(+5) |
| `filesystem/ramfs/src/lib.rs#lookup` | `src/fs/new_vfs/path_cache.rs#lookup` | 同板块同名 | self.inner
            .children
            .lock, self.inner
            .children
            .lock()
            .iter, self.inner
            .children
            .lock()
            .iter()
            .find, self.inner
            .children
            .lock()
            .iter()
            .find(|x| x.filename() == name)
            .map, self.inner
            .children
            .lock()
            .iter()
            .find(|x| x.filename() == name)
            .map(|x| x.to_inode())
            .ok_or, x.filename ...(+1) | Err, Ok, dyn_future, file.clone, here!, name.to_string ...(+8) |
| `filesystem/fs/src/fatfs_shim.rs#truncate` | `src/fs/memfs/tmpdir.rs#truncate` | 同板块同名 | SeekFrom::Start, self.inner
            .lock, self.inner
            .lock()
            .inner
            .seek, self.inner
            .lock()
            .inner
            .seek(SeekFrom::Start(size as u64))
            .map_err, self.inner.lock, self.inner.lock().inner.truncate ...(+1) | Ok, dyn_future, here!, self.content.lock, self.content.lock(here!()).resize |

#### 设备驱动
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `driver/kvirtio/src/virtio_impl.rs#unshare` | `src/drivers/blk/virtio.rs#unshare` | 同板块同名 | 内部闭环逻辑 | 内部闭环逻辑 |
| `driver/kvirtio/src/virtio_net.rs#send` | `src/drivers/serial/uart8250.rs#send` | 同板块同名 | Ok, TxBuffer::from, self.inner
            .lock, self.inner
            .lock()
            .send, self.inner
            .lock()
            .send(TxBuffer::from(buf))
            .expect | panic!, self.send_u32, self.send_u8 |
| `driver/kvirtio/src/virtio_impl.rs#mmio_phys_to_virt` | `src/drivers/blk/virtio.rs#mmio_phys_to_virt` | 同板块同名 | NonNull::new, NonNull::new((paddr | VIRT_ADDR_START) as *mut u8).unwrap | NonNull::new, NonNull::new(kernel_phys_to_virt(paddr) as *mut u8).unwrap, kernel_phys_to_virt |
| `driver/kramdisk/src/lib.rs#capacity` | `src/drivers/blk/dw_mshc/mmc.rs#mmio_base` | 同板块同结构 | 内部闭环逻辑 | 内部闭环逻辑 |

#### 网络协议栈
仅目标项目中观察到该模块代表函数，参考项目没有同模块可对齐证据：
- 目标项目：`kernel/src/socket.rs#reuse`，调用摘要：Mutex::new, NET_SERVER.get_tcp, NET_SERVER.get_udp, self.inner.get_protocol, self.inner.get_protocol().unwrap, self.options.lock ...(+4)
- 目标项目：`kernel/src/socket.rs#new`，调用摘要：Arc::new, Mutex::new, NET_SERVER.blank_tcp, NET_SERVER.blank_udp, panic!, vec!
- 目标项目：`kernel/src/socket.rs#drop`，调用摘要：Arc::strong_count, log::debug!, log::info!, self.inner.close, self.inner.is_closed, self.inner.is_closed().unwrap
- 目标项目：`kernel/src/socket.rs#new_with_inner`，调用摘要：Arc::new, Mutex::new, vec!

#### 文件系统/第三方库
仅目标项目中观察到该模块代表函数，参考项目没有同模块可对齐证据：
- 目标项目：`filesystem/ext4rsfs/src/lib.rs#write_offset`，调用摘要：buf.len, bytes_to_write.div_ceil, data[offset_in_block..offset_in_block + bytes_to_copy]
                .copy_from_slice, device.read_blocks, device.write_blocks, get_blk_device ...(+3)
- 目标项目：`filesystem/ext4fs/src/lib.rs#stat`，调用摘要：Ok, file.file_close, file.file_open, file.file_open(path, O_RDONLY).map_err, file.file_size, file.get_path ...(+5)
- 目标项目：`filesystem/ext4rsfs/src/lib.rs#lookup`，调用摘要：Arc::new, Errno::new, Ok, log::debug!, name.as_bytes, self
            .ext4
            .generic_open ...(+6)
- 目标项目：`filesystem/ext4rsfs/src/lib.rs#stat`，调用摘要：Ok, inodeif.inode.file_type, inodeif.inode.size, self.ext4.get_inode_ref, unreachable!

#### 第三方库
仅目标项目中观察到该模块代表函数，参考项目没有同模块可对齐证据：
- 目标项目：`crates/libc-types/src/termios.rs#default`，调用摘要：ControlFlags::CREAD.bits, ControlFlags::HUPCL.bits
- 目标项目：`crates/runtime/src/frame.rs#alloc_much`，调用摘要：(i..j).for_each, FrameTracker::new, Some, Vec::new, ans.push, pa! ...(+4)
- 目标项目：`crates/executor/src/lib.rs#poll`，调用摘要：Either::Left, Either::Right, Poll::Ready, poll_unpin, self.inner.as_mut, self.inner.as_mut().expect ...(+3)
- 目标项目：`crates/executor/src/executor.rs#run_ready_task`，调用摘要：Arc::new, Arc::new(Waker {
                task_id: task.get_task_id(),
            })
            .into, Context::from_waker, Some, TASK_QUEUE.lock, TASK_QUEUE.lock().pop_front ...(+8)

#### 通用模块/其他
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `kernel/src/logging.rs#write_str` | `src/utils.rs#write_str` | 同板块同名 | Ok, c.encode_utf8, c.encode_utf8(&mut buffer).as_bytes, puts, s.chars | Ok, s.as_bytes, self.inner.write, self.inner.write(s.as_bytes()).unwrap |
| `kernel/src/utils/useref.rs#is_null` | `src/tools/arena.rs#is_null` | 同板块同名 | self.addr.raw | self.0.is_null |
| `kernel/src/user/mod.rs#rwx_to_flags` | `src/sync/mutex.rs#obtain_lock` | 同模块上下文对齐/差异观察 | 内部闭环逻辑 | Ok, arch::get_hart_id, panic!, self.hart_id.get, self.hart_id.get().read, self.hart_id.get().write ...(+4) |
| `kernel/src/utils/useref.rs#fmt` | `src/consts/memlayout.rs#print_memlayout` | 同模块上下文对齐/差异观察 | f.write_fmt, format_args! | humansize::SizeFormatter::new, info! |


#### 1. 宏观判定
- **定性结论**: 独立开发 (相似度: 1.16%，共同 AST 指纹: 21；唯一 AST 指纹: 目标项目 605，参考项目 1233；覆盖率: 目标项目 3.47%，参考项目 1.7%)

#### 2. 全模块覆盖矩阵解读
- **进程管理**: 双方均存在该模块。共同 AST 指纹为 0，目标项目覆盖率 0.0%，参考项目覆盖率 0.0%。这表明双方在进程管理的底层实现上无任何可观测的结构重合。证据强度：未形成。
- **任务调度/异步运行时**: 双方均存在该模块。共同 AST 指纹为 0，目标项目覆盖率 0.0%，参考项目覆盖率 0.0%。这表明双方的任务调度与异步运行时机制在 AST 层面完全异构。证据强度：未形成。
- **内存管理**: 双方均存在该模块。共同 AST 指纹为 0，目标项目覆盖率 0.0%，参考项目覆盖率 0.0%。这表明双方的内存管理子系统在底层实现上无结构重合。证据强度：未形成。
- **中断/异常处理**: 双方均存在该模块。共同 AST 指纹为 0，目标项目覆盖率 0.0%，参考项目覆盖率 0.0%。这表明双方的中断处理流程在 AST 层面无共同结构。证据强度：未形成。
- **系统调用**: 双方均存在该模块。共同 AST 指纹为 0，目标项目覆盖率 0.0%，参考项目覆盖率 0.0%。这表明双方的系统调用分发与实现机制在底层结构上完全不同。证据强度：未形成。
- **文件系统**: 双方均存在该模块。共同 AST 指纹为 1，目标项目覆盖率 0.61%，参考项目覆盖率 0.26%。覆盖率极低，属于噪声级重合，无法构成有效证据。证据强度：弱。
- **设备驱动**: 双方均存在该模块。共同 AST 指纹为 2，目标项目覆盖率 5.13%，参考项目覆盖率 1.85%。覆盖率低，且重合点经分析为通用硬件操作模式，不构成特异性复用证据。证据强度：弱。
- **网络协议栈**: 仅目标项目体现该模块，参考项目无此模块。无共同 AST 指纹。证据强度：未形成。
- **第三方库/公共依赖**: 目标项目包含大量第三方库模块（如 `ext4rsfs`, `libc-types`, `runtime` 等），参考项目无对应模块。无共同 AST 指纹。证据强度：未形成。

#### 3. 底层 AST 关键证据指纹表
| 所属板块 | 目标项目路径 | 参考项目路径 | 证据类型 | 技术说明 |
| :--- | :--- | :--- | :--- | :--- |
| 设备驱动 | `driver/kvirtio/src/virtio_impl.rs#unshare` | `src/drivers/blk/virtio.rs#unshare` | 同板块同名 | 双方均为“内部闭环逻辑”，无外部调用。此类空操作或极简函数在 Rust 中常见，不构成特异性指纹。 |
| 设备驱动 | `driver/kvirtio/src/virtio_impl.rs#mmio_phys_to_virt` | `src/drivers/blk/virtio.rs#mmio_phys_to_virt` | 同板块同名 | 均调用 `NonNull::new(...).unwrap` 进行物理地址到虚拟地址的转换，这是 MMIO 操作的通用模式，非项目独创。 |
| 文件系统 | `filesystem/ramfs/src/lib.rs#remove` | `src/fs/new_vfs/path_cache.rs#remove` | 同板块同名 | 目标项目通过 `children.lock().extract_if(...)` 操作，参考项目通过 `self.extract_file` 操作，内部数据结构与错误处理流完全不同。 |

#### 4. 核心代码调用流对比剖析
* **【进程管理/进程退出路径】**
* **底层技术析理**: 目标项目的 `sys_exit` 直接调用 `self.task.thread_exit`，将退出逻辑封装在线程对象中，体现了以线程为最小执行单元的设计。参考项目的 `sys_exit` 则调用 `self.lproc.set_exit_code`，并显式操作 `self.cx.syscall_args`，其退出逻辑是围绕轻量级进程（LProc）对象和系统调用上下文展开的。两者在抽象层次（线程 vs 进程）和状态管理模型上存在根本差异。
* **代码溯源链接**:
  - 目标项目: `kernel/src/syscall/task.rs#sys_exit`
  - 参考项目: `src/syscall/process.rs#sys_exit`

* **【进程管理/父进程ID查询路径】**
* **底层技术析理**: 目标项目的 `sys_getppid` 调用链为 `self.task.parent.read().upgrade().map(|x| x.task_id).ok_or`，采用了 `RwLock` + `Weak` 引用的防御式编程模型，显式处理了父进程可能已退出的情况。参考项目的 `sys_getppid` 调用链为 `self.lproc.parent_id().into`，通过封装好的方法直接获取，其内部同步与错误处理机制对调用者透明。两者在同步原语（显式锁 vs 封装方法）和错误处理策略上截然不同。
* **代码溯源链接**:
  - 目标项目: `kernel/src/syscall/task.rs#sys_getppid`
  - 参考项目: `src/syscall/process.rs#sys_getppid`

* **【任务调度/异步运行时/系统调用分发路径】**
* **底层技术析理**: 目标项目的 `syscall` 函数通过 `Sysno::new(call_id).ok_or` 解析调用号，并将参数数组 `args` 逐一 `into()` 转换，其分发逻辑依赖于 `Sysno` 枚举。参考项目的 `syscall` 函数则直接从 `self.cx.syscall_no` 和 `self.cx.syscall_args` 获取信息，并调用 `self.sys_brk` 等具体处理函数，其分发逻辑内嵌于上下文 `cx` 中。两者在参数获取方式（数组 vs 上下文结构体）和分发机制（枚举匹配 vs 直接调用）上存在显著架构差异。
* **代码溯源链接**:
  - 目标项目: `kernel/src/syscall/mod.rs#syscall`
  - 参考项目: `src/syscall/mod.rs#syscall`

* **【任务调度/异步运行时/IO多路复用路径】**
* **底层技术析理**: 目标项目的 `sys_pselect` 基于位图操作（`efds.get_bit`, `efds_r.set_bit`）和同步时间比较（`current_time`, `Duration::from_millis`）实现。参考项目的 `sys_pselect` 则构建了复杂的异步 Future 集合（`Vec::<Async<...>>::new`, `AnyFuture::new_with`）和 `BTreeMap` 来管理轮询状态。两者在并发模型（同步轮询 vs 异步Future）和核心数据结构（位图 vs BTreeMap）上完全不同。
* **代码溯源链接**:
  - 目标项目: `kernel/src/syscall/fd.rs#sys_pselect`
  - 参考项目: `src/syscall/io.rs#sys_pselect`

#### 5. 单方模块与降权项
- **网络协议栈**: 目标项目拥有独立的网络协议栈模块（`kernel/src/socket.rs`），而参考项目中未观察到。此为单方独有功能，不构成同源证据。
- **第三方库/公共依赖**: 目标项目包含 `crates/libc-types`, `crates/runtime`, `crates/executor` 等大量第三方库风格的模块，参考项目无此结构。此为项目架构差异，不构成同源证据。
- **降权项说明**: 控辩双方证据中大量出现的 `Ok`, `Err`, `debug!`, `info!`, `Arc::new`, `BTreeMap::new`, `try_from().map_err()` 等调用，均为 Rust 语言标准库或通用宏的惯用法，在任何 Rust 项目中均高频出现，不能作为特异性复用证据。跨板块的同名函数（如多个模块下的 `stat`, `new`, `lookup`）因业务逻辑和所属模块完全不同，已依规降权，不纳入主证据链。

#### 6. 保守结论
本次审计基于 1.16% 的客观 AST 相似度及 21 个共同指纹，判定目标项目与参考项目为独立开发。双方在进程管理、任务调度、内存管理等核心板块的共同 AST 指纹均为 0，表明底层实现完全异构。观察到的极少数重合点（如 `mmio_phys_to_virt`）均为通用硬件操作或 Rust 语言惯用法，不构成特异性同源证据。双方在进程抽象（Task/PCB vs LightProcess）、用户态指针模型、异步运行时、文件系统生态等方面存在根本性架构差异。综合评估，目标项目不存在对参考项目的系统性代码复用，整体同源风险极低。

------------------------------

