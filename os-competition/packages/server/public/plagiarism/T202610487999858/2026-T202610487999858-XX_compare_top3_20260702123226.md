# 🛡️ OS 内核赛自动化代码级架构审计报告

**审计时间**: `2026-07-02 12:32:26`
**参赛作品名称**: `2026-T202610487999858-XX`
**年份**: `2026`
**学校**: `XX`
**队伍名称**: `T202610487999858`
**比赛名字**: `OS-Competition`
**比赛子赛道**: `Kernel-Implementation`
**仓库地址**: `https://gitlab.eduxiji.net/educg-group-43501-3132633/T202610487999858-1903`
**推测开发语言**: `Rust / C (由AST探针自动识别)`
**特征函数量**: `1078`

## ⚔️ 底层代码同源性对比 (AST Homology)
> 本部分由专门的代码审查智能体，对该项目与符合年份条件的历届最高相似度项目进行控制流与底层调用对比。新版报告会先给出确定性的全模块覆盖矩阵，再给出 Agent 的分模块解释，避免只围绕少量抽样函数展开。

### 🏆 Top 3 底层对标: vs `2024-10183_BOOT-吉林大学`（吉林大学 / 10183_BOOT）

#### 对方作品信息

**对方年份**: `2024`
**对方学校**: `吉林大学`
**对方队伍名称**: `10183_BOOT`
**对方比赛名字**: `OS-Competition`
**对方比赛子赛道**: `Kernel-Implementation`
**对方仓库地址**: `https://gitlab.eduxiji.net/educg-group-26010-2376550/T202410183992516-775`

> 双向覆盖率：共同 AST 指纹: 479；唯一 AST 指纹: 目标项目 906，参考项目 743；覆盖率: 目标项目 52.87%，参考项目 64.47%

## 全模块对比总览（确定性证据）

> 总体覆盖率：共同 AST 指纹: 479；唯一 AST 指纹: 目标项目 906，参考项目 743；覆盖率: 目标项目 52.87%，参考项目 64.47%

### 1. 模块覆盖矩阵
| 模块 | 目标函数数 | 参考函数数 | 共同唯一 AST 指纹 | Target 覆盖 | Base 覆盖 | 状态 |
| :--- | ---: | ---: | ---: | ---: | ---: | :--- |
| 进程管理 | 94 | 39 | 15 | 17.65% | 46.88% | 双方存在且有结构重合 |
| 任务调度/异步运行时 | 35 | 34 | 10 | 29.41% | 29.41% | 双方存在且有结构重合 |
| 内存管理 | 189 | 138 | 24 | 14.04% | 18.05% | 双方存在且有结构重合 |
| 中断/异常处理 | 28 | 15 | 5 | 20.83% | 33.33% | 双方存在且有结构重合 |
| 系统调用 | 10 | 7 | 3 | 50.0% | 50.0% | 双方存在且有结构重合 |
| 文件系统 | 62 | 85 | 4 | 7.84% | 5.63% | 双方存在且有结构重合 |
| 设备驱动 | 50 | 29 | 10 | 25.0% | 38.46% | 双方存在且有结构重合 |
| 网络协议栈 | 0 | 0 | 0 | 0.0% | 0.0% | 双方均未体现 |
| 文件系统/第三方库 | 537 | 485 | 382 | 85.27% | 95.5% | 双方存在且有结构重合 |
| 通用模块/其他 | 73 | 52 | 24 | 37.5% | 54.55% | 双方存在且有结构重合 |

### 2. 分模块证据清单

#### 进程管理
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `user/app/sh.c#parseexec` | `user/sh.c#parseexec` | 同板块同名同结构 | execcmd, gettoken, panic, parseblock, parseredirs, peek | execcmd, gettoken, panic, parseblock, parseredirs, peek |
| `kernel/proc/proc.c#kill` | `kernel/proc.c#kill` | 同板块同名 | acquire, release | acquire, release |
| `kernel/proc/signal.c#sigprocmask` | `ipc/signal.c#sigprocmask` | 同板块同名 | myproc, panic | mythread |
| `kernel/proc/proc.c#procdump` | `kernel/proc.c#procdump` | 同板块同名同结构 | NELEM, printf | NELEM, printf |

#### 任务调度/异步运行时
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `user/app/sh.c#runcmd` | `user/sh.c#runcmd` | 同板块同名同结构 | close, dup, execve, exit, fork1, fprintf ...(+5) | close, dup, exec, exit, fork1, fprintf ...(+5) |
| `kernel/lib/console.c#consoleintr` | `drivers/console.c#consoleintr` | 同板块同名同结构 | C, acquire, consputc, procdump, release, wakeup | C, acquire, consputc, procdump, release, wakeup |
| `kernel/lib/console.c#consoleread` | `drivers/console.c#consoleread` | 同板块同名同结构 | C, acquire, either_copyout, killed, myproc, release ...(+1) | C, acquire, either_copyout, killed, myproc, release ...(+1) |
| `kernel/lib/printf.c#printf` | `lib/printf.c#printf` | 同板块同名同结构 | acquire, consputc, panic, printint, printptr, release ...(+3) | acquire, consputc, panic, printint, printptr, release ...(+3) |

#### 内存管理
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `kernel/proc/exec.c#execve` | `kernel/exec.c#execve` | 同板块同名 | ADD_AUXV, PGROUNDDOWN, PGROUNDUP, clear_vmas_on_exec, copyout, ip->i_op->lock ...(+15) | ADD_AUXV, PGROUNDDOWN, PGROUNDUP, copyout, fclose, flags2perm ...(+17) |
| `kernel/sys/sysmem.c#sys_mprotect` | `kernel/syscall/sysmem.c#sys_mprotect` | 同板块同名 | PA2PTE, PGROUNDDOWN, PGROUNDUP, PTE2PA, PTE_FLAGS, acquire ...(+9) | PGROUNDUP, argaddr, argint, myproc, panic, setperm |
| `kernel/driver/loongarch/virtio_disk.c#virtio_disk_init` | `platform/qemu/virtio_disk.c#virtio_disk_init` | 同板块同名 | initlock, memset, pci_device_init, printf, virtio_pci_get_device_features, virtio_pci_get_queue_enable ...(+8) | R, initlock, memset, panic |
| `kernel/sys/sysfile.c#sys_openat` | `kernel/syscall/sysfile.c#sys_openat` | 同板块同名 | argint, argstr, fdalloc, fdalloc2, filealloc, get_fops ...(+11) | argfd, argint, argstr, dir_open, fdalloc, filealloc ...(+6) |

#### 中断/异常处理
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `kernel/trap/riscv/trap.c#trapframedump` | `kernel/trap.c#trapframedump` | 同板块同名同结构 | printf | printf |
| `kernel/trap/loongarch/trap.c#trapinit` | `kernel/trap.c#trapinit` | 同板块同名 | initlock, intr_on, w_csr_ecfg, w_csr_eentry, w_csr_merrentry, w_csr_tcfg ...(+1) | initlock |
| `kernel/sys/plic.c#plicinithart` | `platform/qemu/plic.c#plicinithart` | 同板块同名 | PLIC_SENABLE, PLIC_SPRIORITY, cpuid | PLIC_SENABLE, PLIC_SPRIORITY, cpuid |
| `kernel/sys/plic.c#plicinit` | `platform/qemu/plic.c#plicinit` | 同板块同名 | 内部闭环逻辑 | 内部闭环逻辑 |

#### 系统调用
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `kernel/sys/syscall.c#argstr` | `kernel/syscall/syscall.c#argstr` | 同板块同名同结构 | argaddr, fetchstr | argaddr, fetchstr |
| `kernel/sys/syscall.c#argint` | `kernel/syscall/syscall.c#argint` | 同板块同名同结构 | argraw | argraw |
| `kernel/sys/syssig.c#sys_tgkill` | `kernel/syscall/syssig.c#sys_tgkill` | 同板块同名 | 内部闭环逻辑 | argint, tkill |
| `kernel/sys/syscall.c#arglong` | `kernel/syscall/syscall.c#argaddr` | 同板块同结构 | argraw | argraw |

#### 文件系统
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `kernel/fs/vfs/file.c#fileclose` | `fs/vfs/file.c#fileclose` | 同板块同名 | acquire, get_fops, get_fops()->writable, panic, pipeclose, release ...(+5) | acquire, dir_close, fclose, panic, pipeclose, release |
| `kernel/fs/vfs/file.c#filewrite` | `fs/vfs/file.c#filewrite` | 同板块同名 | devsw[f->f_major].write, get_fops, get_fops()->writable, panic, pipewrite, vfs_ext_write | devsw[f->major].write, fwrite, panic, pipewrite |
| `kernel/fs/vfs/ops.c#skipelem` | `fs/fat32/fat.c#skipelem` | 同板块同名 | memmove | memmove |
| `user/app/sh.c#parseline` | `user/sh.c#parseline` | 同板块同名同结构 | backcmd, gettoken, listcmd, parseline, parsepipe, peek | backcmd, gettoken, listcmd, parseline, parsepipe, peek |

#### 设备驱动
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `kernel/driver/bio.c#bget` | `block/bio.c#bget` | 同板块同名同结构 | acquire, acquiresleep, panic, release | acquire, acquiresleep, panic, release |
| `kernel/mem/uart.c#uartinit` | `platform/qemu/uart.c#uartinit` | 同板块同名同结构 | WriteReg, initlock | WriteReg, initlock |
| `kernel/lib/console.c#consputc` | `drivers/console.c#consputc` | 同板块同名 | sbi_console_putchar, uartputc_sync | uart8250_putc, uartputc_sync |
| `kernel/mem/uart.c#uartstart` | `platform/qemu/uart.c#uartstart` | 同板块同名同结构 | ReadReg, WriteReg, wakeup | ReadReg, WriteReg, wakeup |

#### 文件系统/第三方库
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `kernel/fs/ext4/lwext4/ext4_balloc.c#ext4_balloc_alloc_block` | `fs/lwext4/ext4_balloc.c#ext4_balloc_alloc_block` | 同板块同名同结构 | ext4_balloc_get_bgid_of_block, ext4_balloc_get_block_of_bgid, ext4_balloc_set_bitmap_csum, ext4_balloc_verify_bitmap_csum, ext4_bg_get_block_bitmap, ext4_bg_get_free_blocks_count ...(+19) | ext4_balloc_get_bgid_of_block, ext4_balloc_get_block_of_bgid, ext4_balloc_set_bitmap_csum, ext4_balloc_verify_bitmap_csum, ext4_bg_get_block_bitmap, ext4_bg_get_free_blocks_count ...(+19) |
| `kernel/fs/ext4/lwext4/ext4.c#ext4_dir_rm` | `fs/lwext4/ext4.c#ext4_dir_rm` | 同板块同名同结构 | EXT4_MP_LOCK, EXT4_MP_UNLOCK, ext4_block_cache_write_back, ext4_dir_en_get_inode, ext4_dir_iterator_fini, ext4_dir_iterator_init ...(+19) | EXT4_MP_LOCK, EXT4_MP_UNLOCK, ext4_block_cache_write_back, ext4_dir_en_get_inode, ext4_dir_iterator_fini, ext4_dir_iterator_init ...(+19) |
| `kernel/fs/ext4/lwext4/ext4_journal.c#jbd_journal_prepare` | `fs/lwext4/ext4_journal.c#jbd_journal_prepare` | 同板块同名 | EXT4_BLOCK_ZERO, RB_FIND, TAILQ_FOREACH_REVERSE_SAFE, TAILQ_FOREACH_SAFE, TAILQ_REMOVE, ext4_bcache_clear_dirty ...(+17) | EXT4_BLOCK_ZERO, RB_FIND, TAILQ_FOREACH_REVERSE_SAFE, TAILQ_FOREACH_SAFE, TAILQ_REMOVE, ext4_bcache_clear_dirty ...(+17) |
| `kernel/fs/ext4/lwext4/ext4_dir_idx.c#ext4_dir_dx_split_index` | `fs/lwext4/ext4_dir_idx.c#ext4_dir_dx_split_index` | 同板块同名同结构 | ext4_block_set, ext4_dir_dx_climit_get_count, ext4_dir_dx_climit_get_limit, ext4_dir_dx_climit_set_count, ext4_dir_dx_climit_set_limit, ext4_dir_dx_entry_get_hash ...(+10) | ext4_block_set, ext4_dir_dx_climit_get_count, ext4_dir_dx_climit_get_limit, ext4_dir_dx_climit_set_count, ext4_dir_dx_climit_set_limit, ext4_dir_dx_entry_get_hash ...(+10) |

#### 通用模块/其他
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `user/deps/printf.c#vprintf` | `user/printf.c#vprintf` | 同板块同名同结构 | printint, printptr, putc, va_arg | printint, printptr, putc, va_arg |
| `user/deps/printf.c#printint` | `user/printf.c#printint` | 同板块同名同结构 | putc | putc |
| `kernel/lib/printf.c#printint` | `lib/printf.c#printint` | 同板块同名同结构 | consputc | consputc |
| `user/app/sh.c#parsecmd` | `user/sh.c#parsecmd` | 同板块同名同结构 | fprintf, nulterminate, panic, parseline, peek, strlen | fprintf, nulterminate, panic, parseline, peek, strlen |


好的，我将以审计专家身份，基于提供的证据底座、控辩双方陈述及AST抽样数据，进行最终同源性定性仲裁。

### 1. 宏观判定
- **定性结论**: **独立开发 / 底层架构显著差异**
- **核心依据**: 客观 AST 相似度为 40.94%，处于较低水平。目标项目拥有 906 个唯一 AST 指纹，远超共同指纹数 479，表明其代码主体为独立实现。在关键系统模块（如进程管理）中，双方存在根本性的内核模型差异（进程模型 vs 线程模型），且文件系统后端、初始化路径等核心子系统实现截然不同。控方所指控的“复用”主要集中在 POSIX 语义、硬件约束及公共教学范式所导致的调用图收敛上，不构成源码级抄袭。

### 2. 全模块覆盖矩阵解读
| 模块 | 双方存在 | 共同指纹 | 目标覆盖率 | 参考覆盖率 | 证据强度 | 解读 |
| :--- | :---: | :---: | :---: | :---: | :---: | :--- |
| **进程管理** | 是 | 15 | 17.65% | 46.88% | **弱** | 目标项目函数数远超参考项目（94 vs 39），覆盖率低，表明存在大量独立代码。相似点集中在 `kill`, `sched` 等 POSIX/xv6 标准函数，其调用图由功能和锁机制决定，非抄袭铁证。 |
| **任务调度/异步运行时** | 是 | 10 | 29.41% | 29.41% | **弱** | 双方函数数量相当，覆盖率一致。相似点如 `consoleintr`, `printf` 等均为内核基础组件或公共库函数，其实现范式高度收敛。 |
| **内存管理** | 是 | 24 | 14.04% | 18.05% | **弱** | 目标项目函数数（189）远超参考（138），覆盖率极低。`execve` 等函数虽有同名，但调用链差异巨大（见下文剖析），表明独立实现。 |
| **中断/异常处理** | 是 | 5 | 20.83% | 33.33% | **弱** | 双方函数数均不多，相似点如 `plicinit`, `trapframedump` 是与硬件强相关的标准操作，无独立表达空间。 |
| **系统调用** | 是 | 3 | 50.0% | 50.0% | **弱** | 函数数极少，均为系统调用入口的薄封装层，其调用图由 ABI 定义，高覆盖率是必然结果，不具鉴别意义。 |
| **文件系统** | 是 | 4 | 7.84% | 5.63% | **未形成** | 覆盖率极低，表明核心文件系统实现完全不同。`forkret` 中的初始化路径差异是决定性证据。 |
| **设备驱动** | 是 | 10 | 25.0% | 38.46% | **弱** | 相似点如 `virtio_disk_init`, `uartinit` 是特定硬件驱动的标准初始化序列，由硬件手册定义。 |
| **网络协议栈** | 否 | 0 | 0.0% | 0.0% | **未形成** | 双方均未实现，无对比基础。 |
| **第三方库/公共依赖** | 是 | 382 | 85.27% | 95.5% | **强（降权）** | 覆盖率极高，但双方均使用了 `lwext4` 等公共第三方库，此部分高相似度是引用公共代码的正常现象，不视为项目间抄袭。 |

### 3. 底层 AST 关键证据指纹表
| 所属板块 | 目标项目路径 | 参考项目路径 | 证据类型 | 技术说明 |
| :--- | :--- | :--- | :--- | :--- |
| **进程管理** | `kernel/proc/proc.c#forkret` | `kernel/proc.c#forkret` | **架构差异** | 目标调用 `filesystem2_init, filesystem_init`，参考调用 `ext4_init, dir_open`。文件系统后端和初始化路径完全不同，是独立架构设计的铁证。 |
| **进程管理** | `kernel/proc/proc.c#sched` | `kernel/proc.c#sched` | **模型差异** | 目标调用 `myproc`，参考调用 `mykthread`。调度上下文分别是进程和内核线程，揭示了“进程即执行体” vs “进程-线程分离”的根本模型差异。 |
| **进程管理** | `kernel/sys/sysproc.c#sys_exit_group` | `kernel/syscall/sysproc.c#sys_exit_group` | **功能差异** | 目标调用 `kill_thread_group`，参考无此调用。目标项目实现了独立的线程组退出逻辑，是新增功能。 |
| **内存管理** | `kernel/proc/exec.c#execve` | `kernel/exec.c#execve` | **实现差异** | 目标调用 `clear_vmas_on_exec, ip->i_op->lock`，参考调用 `fclose, flags2perm`。内存映射清理和文件权限处理的具体实现路径不同。 |
| **文件系统** | `kernel/fs/vfs/file.c#fileclose` | `fs/vfs/file.c#fileclose` | **实现差异** | 目标调用 `get_fops, get_fops()->writable`，参考调用 `dir_close, fclose`。VFS 层的函数指针抽象和具体关闭逻辑存在差异。 |
| **第三方库** | `kernel/fs/ext4/lwext4/ext4_balloc.c#ext4_balloc_alloc_block` | `fs/lwext4/ext4_balloc.c#ext4_balloc_alloc_block` | **公共代码** | 调用图谱完全一致。双方均引用了 `lwext4` 开源库，此部分为公共依赖，不构成项目间抄袭。 |

### 4. 核心代码调用流对比剖析
* **【进程管理 / 调度上下文】**
* **底层技术析理**: 这是本案最关键的架构差异。目标项目的 `sched` 函数调用 `myproc`，表明其调度器以 `proc` 结构体为基本单位，采用“单线程进程模型”。参考项目的 `sched` 调用 `mykthread`，表明其调度器操作的是内核线程，采用“进程-线程分离模型”。这一差异贯穿 `sleep`, `yield`, `sys_gettid` 等多个核心函数，是内核设计哲学的根本不同，绝非简单的“变量替换”可以解释。
* **代码溯源链接**:
  - 目标项目: `kernel/proc/proc.c#sched`
  - 参考项目: `kernel/proc.c#sched`

* **【内存管理 / 程序加载】**
* **底层技术析理**: 在 `execve` 系统调用的实现中，目标项目显式调用 `clear_vmas_on_exec` 来清理旧的内存映射，并通过 `ip->i_op->lock` 对 inode 操作加锁。参考项目则调用 `fclose` 关闭文件，并使用 `flags2perm` 转换权限标志。两者在内存区域管理和文件操作锁的粒度上采取了不同的实现策略，调用链差异显著。
* **代码溯源链接**:
  - 目标项目: `kernel/proc/exec.c#execve`
  - 参考项目: `kernel/exec.c#execve`

* **【文件系统 / 初始化路径】**
* **底层技术析理**: 在 `forkret`（首次用户态返回）中，目标项目通过 `filesystem_init` 和 `filesystem2_init` 进行双层文件系统初始化，暗示其可能支持多文件系统或更复杂的挂载逻辑。参考项目则直接调用 `ext4_init` 和 `dir_open`，初始化路径单一且具体。这证明两个项目在文件系统架构和启动流程上是独立设计的。
* **代码溯源链接**:
  - 目标项目: `kernel/proc/proc.c#forkret`
  - 参考项目: `kernel/proc.c#forkret`

### 5. 单方模块与降权项
* **单方模块/功能**:
  - **目标项目独有**: `kernel/proc/socket.c` 中的 `recvfrom`, `socketfree` 等函数，表明其独立实现了 Socket 通信子系统。
  - **参考项目独有**: `kernel/futex.c` 中的 `futexWait` 函数，表明其实现了 Futex 同步机制。
  - **结论**: 双方在进程间通信/同步机制上选择了完全不同的扩展方向，是独立开发的强有力证明。
* **降权项**:
  - **第三方库/公共依赖**: `lwext4` 文件系统库、`printf` 系列函数、`sh.c` Shell 解析器。这些部分 AST 覆盖率极高，但属于对公共开源代码或经典教学范例（xv6）的引用，不构成项目间的抄袭证据。
  - **硬件强相关函数**: `cpuid` (读取 `r_tp` 寄存器)、`plicinit`, `uartinit` 等。其实现由硬件架构手册唯一确定，无独立表达空间。
  - **POSIX 薄封装**: `sys_fork`, `sys_getpid`, `sys_sleep` 等系统调用入口。其调用图由 POSIX 标准 ABI 定义，高度相似是符合规范的必然结果。

### 6. 保守结论
综合全模块证据，目标项目与参考项目的客观 AST 相似度为 40.94%，处于“低度相似”区间。高相似度主要集中在双方引用的公共第三方库（如 lwext4）和由 POSIX/硬件约束决定的薄封装层，这些部分不具备鉴别原创性的价值。在决定项目架构的核心系统模块（进程管理、内存管理、文件系统）中，双方覆盖率低，且存在“进程 vs 线程”调度模型、文件系统初始化路径等根本性差异。目标项目独有的 Socket 子系统与参考项目独有的 Futex 机制进一步证明两者是独立演进的项目。因此，现有证据不足以支持代码抄袭或学术不端的指控，应认定为基于公共教学范式与开源组件的独立开发。

------------------------------

