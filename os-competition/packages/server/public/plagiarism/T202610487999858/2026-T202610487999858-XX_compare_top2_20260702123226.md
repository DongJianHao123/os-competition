# 🛡️ OS 内核赛自动化代码级架构审计报告

**审计时间**: `2026-07-02 12:32:26`
**参赛作品名称**: `2026-T202610487999858-XX`
**年份**: `2026`
**学校**: `XX`
**队伍名称**: `T202610487999858`
**比赛名字**: `OS-Competition`
**比赛子赛道**: `Kernel-Implementation`
**仓库地址**: `https://gitlab.eduxiji.net/educg-group-43501-3132633/T202610487999858-1903`
**推测开发语言**: `Rust / C (由AST探针自动识别)`
**特征函数量**: `1078`

## ⚔️ 底层代码同源性对比 (AST Homology)
> 本部分由专门的代码审查智能体，对该项目与符合年份条件的历届最高相似度项目进行控制流与底层调用对比。新版报告会先给出确定性的全模块覆盖矩阵，再给出 Agent 的分模块解释，避免只围绕少量抽样函数展开。

### 🏆 Top 2 底层对标: vs `2025-静春山-中山大学`（中山大学 / 静春山）

#### 对方作品信息

**对方年份**: `2025`
**对方学校**: `中山大学`
**对方队伍名称**: `静春山`
**对方比赛名字**: `OS-Competition`
**对方比赛子赛道**: `Kernel-Implementation`
**对方仓库地址**: `https://gitlab.eduxiji.net/educg-group-36002-2710490/T202510558995330-264`

> 双向覆盖率：共同 AST 指纹: 602；唯一 AST 指纹: 目标项目 906，参考项目 949；覆盖率: 目标项目 66.45%，参考项目 63.44%

## 全模块对比总览（确定性证据）

> 总体覆盖率：共同 AST 指纹: 602；唯一 AST 指纹: 目标项目 906，参考项目 949；覆盖率: 目标项目 66.45%，参考项目 63.44%

### 1. 模块覆盖矩阵
| 模块 | 目标函数数 | 参考函数数 | 共同唯一 AST 指纹 | Target 覆盖 | Base 覆盖 | 状态 |
| :--- | ---: | ---: | ---: | ---: | ---: | :--- |
| 进程管理 | 94 | 68 | 22 | 25.88% | 40.74% | 双方存在且有结构重合 |
| 任务调度/异步运行时 | 35 | 69 | 13 | 38.24% | 19.12% | 双方存在且有结构重合 |
| 内存管理 | 189 | 161 | 51 | 29.82% | 32.9% | 双方存在且有结构重合 |
| 中断/异常处理 | 28 | 39 | 13 | 54.17% | 40.62% | 双方存在且有结构重合 |
| 系统调用 | 10 | 41 | 3 | 50.0% | 25.0% | 双方存在且有结构重合 |
| 文件系统 | 62 | 133 | 25 | 49.02% | 22.52% | 双方存在且有结构重合 |
| 设备驱动 | 50 | 42 | 28 | 70.0% | 80.0% | 双方存在且有结构重合 |
| 网络协议栈 | 0 | 0 | 0 | 0.0% | 0.0% | 双方均未体现 |
| 文件系统/第三方库 | 537 | 489 | 389 | 86.83% | 95.34% | 双方存在且有结构重合 |
| 通用模块/其他 | 73 | 126 | 37 | 57.81% | 37.0% | 双方存在且有结构重合 |

### 2. 分模块证据清单

#### 进程管理
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `kernel/proc/proc.c#scheduler` | `kernel/proc/proc.c#scheduler` | 同板块同名 | acquire, intr_on, mycpu, release, swtch | acquire, intr_on, mycpu, release, swtch |
| `user/app/sh.c#parseexec` | `user/sh.c#parseexec` | 同板块同名同结构 | execcmd, gettoken, panic, parseblock, parseredirs, peek | execcmd, gettoken, panic, parseblock, parseredirs, peek |
| `kernel/proc/proc.c#kill` | `kernel/proc/proc.c#kill` | 同板块同名 | acquire, release | acquire, release |
| `kernel/proc/proc.c#procdump` | `kernel/proc/proc.c#procdump` | 同板块同名 | NELEM, printf | NELEM, printf |

#### 任务调度/异步运行时
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `kernel/driver/loongarch/virtio_pci.c#virtio_pci_read_caps` | `kernel/drive/la/virtio_pci.c#virtio_pci_read_caps` | 同板块同名同结构 | get_cfg_addr, pci_config_read, pci_config_read8, printf | get_cfg_addr, pci_config_read, pci_config_read8, printf |
| `user/app/sh.c#runcmd` | `user/sh.c#runcmd` | 同板块同名同结构 | close, dup, execve, exit, fork1, fprintf ...(+5) | close, dup, execve, exit, fork1, fprintf ...(+5) |
| `kernel/lib/console.c#consoleintr` | `kernel/fs/console.c#consoleintr` | 同板块同名同结构 | C, acquire, consputc, procdump, release, wakeup | C, acquire, consputc, procdump, release, wakeup |
| `kernel/lib/console.c#consoleread` | `kernel/fs/console.c#consoleread` | 同板块同名同结构 | C, acquire, either_copyout, killed, myproc, release ...(+1) | C, acquire, either_copyout, killed, myproc, release ...(+1) |

#### 内存管理
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `kernel/proc/exec.c#execve` | `kernel/proc/exec.c#execve` | 同板块同名 | ADD_AUXV, PGROUNDDOWN, PGROUNDUP, clear_vmas_on_exec, copyout, ip->i_op->lock ...(+15) | AUXV, PGROUNDDOWN, PGROUNDUP, acquire, copyout, ip->i_op->lock ...(+12) |
| `kernel/driver/loongarch/pci.c#pci_scan_device` | `kernel/drive/la/pci.c#pci_scan_device` | 同板块同名 | pci_alloc_device, pci_device_bar_init, pci_device_init, pci_read_config, pci_write_config, printf | pci_alloc_device, pci_device_bar_init, pci_device_init, pci_read_config, pci_write_config, printf |
| `kernel/driver/loongarch/virtio_disk.c#virtio_disk_rw` | `kernel/drive/la/virtio_disk.c#virtio_disk_rw` | 同板块同名同结构 | PA2VA, VIRT2PHY, __sync_synchronize, acquire, alloc3_desc, free_chain ...(+7) | PA2VA, VIRT2PHY, __sync_synchronize, acquire, alloc3_desc, free_chain ...(+7) |
| `kernel/sys/sysmem.c#sys_mprotect` | `kernel/syscall/sysproc.c#sys_mprotect` | 同板块同名 | PA2PTE, PGROUNDDOWN, PGROUNDUP, PTE2PA, PTE_FLAGS, acquire ...(+9) | PGROUNDUP, argaddr, argint, find_vma_by_addr, myproc, sfence_vma ...(+1) |

#### 中断/异常处理
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `kernel/driver/loongarch/pci.c#pci_device_init` | `kernel/drive/la/pci.c#pci_device_init` | 同板块同名同结构 | 内部闭环逻辑 | 内部闭环逻辑 |
| `kernel/driver/riscv/virtio_disk.c#virtio_disk_intr` | `kernel/drive/rv/virtio_disk.c#virtio_disk_intr` | 同板块同名同结构 | R, acquire, panic, release, wakeup | R, acquire, panic, release, wakeup |
| `kernel/driver/loongarch/virtio_disk.c#virtio_disk_intr` | `kernel/drive/la/virtio_disk.c#virtio_disk_intr` | 同板块同名同结构 | dsb, panic, virtio_pci_clear_isr, wakeup | dsb, panic, virtio_pci_clear_isr, wakeup |
| `kernel/trap/loongarch/apic.c#apic_init` | `kernel/trap/la/apic.c#apic_init` | 同板块同名 | 内部闭环逻辑 | iointc_2k1000_init |

#### 系统调用
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `kernel/sys/syssig.c#sys_tkill` | `kernel/syscall/syssig.c#sys_tkill` | 同板块同名同结构 | argint, tkill | argint, send_signal_thread |
| `kernel/sys/syscall.c#argstr` | `kernel/syscall/syscall.c#argstr` | 同板块同名 | argaddr, fetchstr | argaddr, fetchstr |
| `kernel/sys/syscall.c#argint` | `kernel/syscall/syscall.c#argint` | 同板块同名同结构 | argraw | argraw |
| `kernel/sys/syssig.c#sys_tgkill` | `kernel/syscall/syssig.c#sys_tgkill` | 同板块同名 | 内部闭环逻辑 | argint, send_signal_thread |

#### 文件系统
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `kernel/fs/vfs/ops.c#get_absolute_path` | `kernel/fs/vfs/ops.c#get_absolute_path` | 同板块同名 | strcat, strcpy, strlen, strncpy | strcat, strcpy, strlen |
| `kernel/driver/loongarch/pci.c#pci_device_probe` | `kernel/drive/la/pci.c#pci_device_probe` | 同板块同名同结构 | PCI_ADDR, __sync_synchronize, printf | PCI_ADDR, __sync_synchronize, printf |
| `kernel/driver/loongarch/virtio_pci.c#virtio_pci_setup_queue` | `kernel/drive/la/virtio_pci.c#virtio_pci_setup_queue` | 同板块同名同结构 | PCI_REG16, PCI_REG32, PCI_REG64, printf | PCI_REG16, PCI_REG32, PCI_REG64, printf |
| `kernel/fs/vfs/file.c#fileclose` | `kernel/fs/vfs/file.c#fileclose` | 同板块同名 | acquire, get_fops, get_fops()->writable, panic, pipeclose, release ...(+5) | acquire, get_fops, get_fops()->writable, panic, pipeclose, release ...(+4) |

#### 设备驱动
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `kernel/driver/loongarch/virtio_ring.c#virtio_vring_init` | `kernel/drive/la/virtio_ring.c#virtio_vring_init` | 同板块同名同结构 | VQ_ALIGN, virtio_vring_size | VQ_ALIGN, virtio_vring_size |
| `kernel/driver/loongarch/pci.c#pci_device_bar_init` | `kernel/drive/la/pci.c#pci_device_bar_init` | 同板块同名同结构 | 内部闭环逻辑 | 内部闭环逻辑 |
| `kernel/driver/loongarch/virtio_pci.c#virtio_pci_set_queue_addr2` | `kernel/drive/la/virtio_pci.c#virtio_pci_set_queue_addr2` | 同板块同名同结构 | PCI_REG16, PCI_REG64, VIRT2PHY, dsb | PCI_REG16, PCI_REG64, VIRT2PHY, dsb |
| `kernel/mem/uart.c#uartstart` | `kernel/drive/uart.c#uartstart` | 同板块同名 | ReadReg, WriteReg, wakeup | ReadReg, WriteReg, wakeup |

#### 文件系统/第三方库
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `kernel/fs/ext4/lwext4/ext4_balloc.c#ext4_balloc_alloc_block` | `kernel/fs/lwext4/ext4_balloc.c#ext4_balloc_alloc_block` | 同板块同名同结构 | ext4_balloc_get_bgid_of_block, ext4_balloc_get_block_of_bgid, ext4_balloc_set_bitmap_csum, ext4_balloc_verify_bitmap_csum, ext4_bg_get_block_bitmap, ext4_bg_get_free_blocks_count ...(+19) | ext4_balloc_get_bgid_of_block, ext4_balloc_get_block_of_bgid, ext4_balloc_set_bitmap_csum, ext4_balloc_verify_bitmap_csum, ext4_bg_get_block_bitmap, ext4_bg_get_free_blocks_count ...(+19) |
| `kernel/fs/ext4/lwext4/ext4.c#ext4_dir_rm` | `kernel/fs/lwext4/ext4.c#ext4_dir_rm` | 同板块同名同结构 | EXT4_MP_LOCK, EXT4_MP_UNLOCK, ext4_block_cache_write_back, ext4_dir_en_get_inode, ext4_dir_iterator_fini, ext4_dir_iterator_init ...(+19) | EXT4_MP_LOCK, EXT4_MP_UNLOCK, ext4_block_cache_write_back, ext4_dir_en_get_inode, ext4_dir_iterator_fini, ext4_dir_iterator_init ...(+19) |
| `kernel/fs/ext4/lwext4/ext4_journal.c#jbd_journal_prepare` | `kernel/fs/lwext4/ext4_journal.c#jbd_journal_prepare` | 同板块同名 | EXT4_BLOCK_ZERO, RB_FIND, TAILQ_FOREACH_REVERSE_SAFE, TAILQ_FOREACH_SAFE, TAILQ_REMOVE, ext4_bcache_clear_dirty ...(+17) | EXT4_BLOCK_ZERO, RB_FIND, TAILQ_FOREACH_REVERSE_SAFE, TAILQ_FOREACH_SAFE, TAILQ_REMOVE, ext4_bcache_clear_dirty ...(+17) |
| `kernel/fs/ext4/lwext4/ext4_dir_idx.c#ext4_dir_dx_split_index` | `kernel/fs/lwext4/ext4_dir_idx.c#ext4_dir_dx_split_index` | 同板块同名同结构 | ext4_block_set, ext4_dir_dx_climit_get_count, ext4_dir_dx_climit_get_limit, ext4_dir_dx_climit_set_count, ext4_dir_dx_climit_set_limit, ext4_dir_dx_entry_get_hash ...(+10) | ext4_block_set, ext4_dir_dx_climit_get_count, ext4_dir_dx_climit_get_limit, ext4_dir_dx_climit_set_count, ext4_dir_dx_climit_set_limit, ext4_dir_dx_entry_get_hash ...(+10) |

#### 通用模块/其他
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `user/deps/printf.c#vprintf` | `user/printf.c#vprintf` | 同板块同名 | printint, printptr, putc, va_arg | putc_buf, va_arg, write |
| `user/app/wc.c#wc` | `user/wc.c#wc` | 同板块同名同结构 | exit, printf, read, strchr | exit, printf, read, strchr |
| `user/app/grep.c#grep` | `user/grep.c#grep` | 同板块同名同结构 | match, memmove, read, strchr, write | match, memmove, read, strchr, write |
| `user/deps/printf.c#printint` | `basic/user/lib/stdio.c#printint` | 同板块同名 | putc | out, puts |


好的，我将遵循审计指令，以操作系统内核代码同源性审计专家的身份，对本案进行最终定性仲裁。

### 1. 宏观判定
- **定性结论**: 部分逻辑借鉴 / 存在架构改动 (相似度: 48.04%，共同 AST 指纹: 602；唯一 AST 指纹: 目标项目 906，参考项目 949；覆盖率: 目标项目 66.45%，参考项目 63.44%)

### 2. 全模块覆盖矩阵解读
- **进程管理**
  1. 双方均存在该模块。
  2. 存在共同 AST 指纹。
  3. 目标覆盖率 25.88%，参考覆盖率 40.74%。覆盖率较低，表明双方在进程管理的具体实现上存在大量独立代码，重合部分集中在核心调度原语和系统调用封装。
  4. 证据强度：**中**。核心调度逻辑调用链高度一致，但整体模块重合度低，且存在架构级差异。

- **任务调度/异步运行时**
  1. 双方均存在该模块。
  2. 存在共同 AST 指纹。
  3. 目标覆盖率 38.24%，参考覆盖率 19.12%。覆盖率不对称，说明双方在该模块的体量和实现重点不同，重合部分可能为设备驱动或Shell等公共组件。
  4. 证据强度：**弱**。重合点分散在驱动和用户态程序，未形成核心调度逻辑之外的强同源证据链。

- **内存管理**
  1. 双方均存在该模块。
  2. 存在共同 AST 指纹。
  3. 目标覆盖率 29.82%，参考覆盖率 32.9%。覆盖率中等偏低，表明大部分内存管理逻辑（如分配器、页表操作细节）存在差异。
  4. 证据强度：**中**。存在如`execve`、`sys_mprotect`等关键函数的同源指纹，但调用链细节（如`ADD_AUXV` vs `AUXV`）和锁策略存在差异。

- **中断/异常处理**
  1. 双方均存在该模块。
  2. 存在共同 AST 指纹。
  3. 目标覆盖率 54.17%，参考覆盖率 40.62%。覆盖率较高，但证据集中在设备驱动（如`virtio_disk_intr`）的中断处理函数，而非核心Trap分发逻辑。
  4. 证据强度：**中**。重合点主要是特定设备（virtio）的中断处理，其逻辑由硬件手册规定，属于领域强制收敛。

- **系统调用**
  1. 双方均存在该模块。
  2. 存在共同 AST 指纹。
  3. 目标覆盖率 50.0%，参考覆盖率 25.0%。覆盖率不对称，参考项目函数数远多于目标项目。重合点均为参数解析等trivial wrapper。
  4. 证据强度：**弱**。共同指纹均为`argint`、`argaddr`等标准参数解析函数，是系统调用入口的必然实现。

- **文件系统**
  1. 双方均存在该模块。
  2. 存在共同 AST 指纹。
  3. 目标覆盖率 49.02%，参考覆盖率 22.52%。覆盖率不对称，参考项目文件系统规模更大。重合点包含VFS辅助函数和PCI设备探测逻辑。
  4. 证据强度：**弱**。`get_absolute_path`等函数属于通用字符串操作；PCI探测逻辑由硬件规范决定。未发现自定义文件系统核心逻辑的同源证据。

- **设备驱动**
  1. 双方均存在该模块。
  2. 存在共同 AST 指纹。
  3. 目标覆盖率 70.0%，参考覆盖率 80.0%。覆盖率极高，是重合度最高的自定义代码模块。
  4. 证据强度：**强**。`virtio`驱动（`virtio_vring_init`、`virtio_pci_set_queue_addr2`等）和`pci`驱动（`pci_device_bar_init`）的AST指纹高度一致，调用链完全咬合，指向同一套驱动实现代码的复用。

- **网络协议栈**
  1. 双方均未体现该模块。
  2. 不存在共同 AST 指纹。
  3. 无覆盖率数据。
  4. 证据强度：**未形成**。

- **第三方库/公共依赖**
  1. 双方均存在该模块。
  2. 存在大量共同 AST 指纹。
  3. 目标覆盖率 86.83%，参考覆盖率 95.34%。覆盖率极高，是拉高整体相似度的主要因素。
  4. 证据强度：**强（但降权为公共依赖）**。`lwext4`文件系统库的函数（如`ext4_balloc_alloc_block`）AST指纹完全一致，调用链高度重合。这明确表明双方引用了同一开源第三方库，不能作为抄袭证据。

### 3. 底层 AST 关键证据指纹表
| 所属板块 | 目标项目路径 | 参考项目路径 | 证据类型 | 技术说明 |
| :--- | :--- | :--- | :--- | :--- |
| 设备驱动 | `kernel/driver/loongarch/virtio_ring.c#virtio_vring_init` | `kernel/drive/la/virtio_ring.c#virtio_vring_init` | 同板块同名同结构 | VirtIO队列初始化逻辑完全一致，均依赖`VQ_ALIGN`和`virtio_vring_size`，是特定驱动实现的直接复用。 |
| 设备驱动 | `kernel/driver/loongarch/virtio_pci.c#virtio_pci_set_queue_addr2` | `kernel/drive/la/virtio_pci.c#virtio_pci_set_queue_addr2` | 同板块同名同结构 | 设置VirtIO队列地址的PCI配置空间操作序列（`PCI_REG16, PCI_REG64, VIRT2PHY, dsb`）完全一致。 |
| 设备驱动 | `kernel/driver/loongarch/pci.c#pci_device_bar_init` | `kernel/drive/la/pci.c#pci_device_bar_init` | 同板块同名同结构 | PCI设备BAR基址寄存器初始化逻辑均为“内部闭环逻辑”，表明其复杂的寄存器读写序列完全重合。 |
| 进程管理 | `kernel/proc/proc.c#sched` | `kernel/proc/proc.c#sched` | 同板块同名同结构 | 上下文切换核心逻辑，调用链`holding→intr_get→mycpu→myproc→panic→swtch`完全一致，是xv6风格调度器的标准实现。 |
| 进程管理 | `user/app/sh.c#parseexec` | `user/sh.c#parseexec` | 同板块同名同结构 | Shell递归下降解析器的函数调用关系`execcmd→gettoken→panic→parseblock→parseredirs→peek`完全一致，源于公共的Shell语法和解析模式。 |
| 内存管理 | `kernel/proc/exec.c#execve` | `kernel/proc/exec.c#execve` | 同板块同名 | `exec`系统调用核心实现，调用链高度相似，但存在`ADD_AUXV` vs `AUXV`、`clear_vmas_on_exec` vs `acquire`等细节差异，表明存在独立修改。 |
| 第三方库 | `kernel/fs/ext4/lwext4/ext4_balloc.c#ext4_balloc_alloc_block` | `kernel/fs/lwext4/ext4_balloc.c#ext4_balloc_alloc_block` | 同板块同名同结构 | `lwext4`库的块分配函数，调用链包含20个函数且完全一致，是引用同一第三方库的铁证。 |
| 第三方库 | `kernel/fs/ext4/lwext4/ext4_dir_idx.c#ext4_dir_dx_split_index` | `kernel/fs/lwext4/ext4_dir_idx.c#ext4_dir_dx_split_index` | 同板块同名同结构 | `lwext4`库的目录索引分裂函数，调用链包含16个函数且完全一致，是引用同一第三方库的铁证。 |

### 4. 核心代码调用流对比剖析
* **【进程管理/上下文切换路径】**
* **底层技术析理**: 双方`kernel/proc/proc.c#sched`函数的调用流均为`holding→intr_get→mycpu→myproc→panic→swtch`。这是xv6操作系统教科书式的上下文切换安全检查序列：先检查是否持有锁（`holding`），再确保中断关闭（`intr_get`），然后获取当前CPU和进程指针（`mycpu`, `myproc`），最后调用汇编实现的上下文切换（`swtch`）。此调用流由并发模型和硬件特性强制决定，任何遵循xv6设计的内核都会自然收敛于此，属于领域规范实现而非代码抄袭。
* **代码溯源链接**:
  - 目标项目: `kernel/proc/proc.c#sched`
  - 参考项目: `kernel/proc/proc.c#sched`

* **【内存管理/程序加载路径】**
* **底层技术析理**: 双方`execve`函数均执行“读取ELF→设置页表→填充AUXV→拷贝参数”的标准流程。然而，目标项目调用`ADD_AUXV`和`clear_vmas_on_exec`，而参考项目调用`AUXV`和`acquire`。这揭示了内存管理模型上的差异：目标项目可能采用更细粒度的VMA（虚拟内存区域）管理，而参考项目使用更简单的锁保护。这种核心辅助函数的差异是独立架构设计的体现。
* **代码溯源链接**:
  - 目标项目: `kernel/proc/exec.c#execve`
  - 参考项目: `kernel/proc/exec.c#execve`

* **【文件系统/第三方库路径】**
* **底层技术析理**: 双方在`ext4_balloc_alloc_block`等函数上表现出100%的调用链重合。该函数属于`lwext4`这一开源嵌入式ext4文件系统库。两个项目均以不同路径（`kernel/fs/ext4/lwext4/` vs `kernel/fs/lwext4/`）集成了该库，导致文件系统/第三方库模块的AST覆盖率极高。这是引用公共依赖的典型特征，必须从自定义代码抄袭的评估中剔除。
* **代码溯源链接**:
  - 目标项目: `kernel/fs/ext4/lwext4/ext4_balloc.c#ext4_balloc_alloc_block`
  - 参考项目: `kernel/fs/lwext4/ext4_balloc.c#ext4_balloc_alloc_block`

### 5. 单方模块与降权项
- **单方模块/功能**:
  - **线程组管理**: 目标项目存在`kill_thread_group`、`sys_exit_group`等函数，支持完整的POSIX线程组语义，而参考项目缺失。这是目标项目独立扩展的显著特征。
  - **多文件系统初始化**: 目标项目`forkret`调用`filesystem2_init`，暗示其支持多文件系统实例或更复杂的初始化序列，参考项目仅调用`filesystem_init`。
  - **关机流程**: 目标项目`sys_shutdown`通过`vfs_ext_umount`进行文件系统卸载，而参考项目直接调用SBI关机。两者底层关机哲学完全不同。
- **降权项**:
  - **第三方库 (`lwext4`)**: 双方共同指纹602个中的389个（64.6%）来自此库。此部分重合必须降权为“公共依赖”，不计入自定义代码的抄袭评估。
  - **公共接口与泛型函数名**: `sys_fork`、`sys_kill`、`sys_getpid`等系统调用封装函数，以及`mycpu`、`cpuid`等底层辅助函数，其函数名是POSIX/OS标准术语，其调用图谱（如`sys_fork`仅调用`fork`）是trivial wrapper的必然结果。这些不能作为强同源证据。
  - **跨板块同名**: 控方将`user/app/sh.c`中的`parseexec`归入“进程管理”板块，但其本质是用户态Shell程序，属于公共算法实现。其与参考项目`user/sh.c`的同名同结构，源于递归下降解析器的标准设计模式，不构成内核代码复用证据。

### 6. 保守结论
综合审计，目标项目与参考项目的整体AST相似度为48.04%，处于“部分逻辑借鉴”的阈值区间。该相似度主要由**第三方公共库`lwext4`**（贡献了64.6%的共同指纹）和**设备驱动**（VirtIO/PCI）的高度重合拉高。在核心的进程管理、内存管理等模块，覆盖率仅25%-33%，且存在线程组、多文件系统初始化等显著的架构级差异。因此，目标项目并非对参考项目的“大面积、系统性复用”，而是在**兼容公共驱动实现和引用相同第三方库的基础上，进行了独立的架构设计与功能扩展**。建议剔除第三方库后，重新评估自定义内核代码的重合度，以得出更精确的结论。

------------------------------

