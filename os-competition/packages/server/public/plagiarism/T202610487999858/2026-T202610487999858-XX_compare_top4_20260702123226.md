# 🛡️ OS 内核赛自动化代码级架构审计报告

**审计时间**: `2026-07-02 12:32:26`
**参赛作品名称**: `2026-T202610487999858-XX`
**年份**: `2026`
**学校**: `XX`
**队伍名称**: `T202610487999858`
**比赛名字**: `OS-Competition`
**比赛子赛道**: `Kernel-Implementation`
**仓库地址**: `https://gitlab.eduxiji.net/educg-group-43501-3132633/T202610487999858-1903`
**推测开发语言**: `Rust / C (由AST探针自动识别)`
**特征函数量**: `1078`

## ⚔️ 底层代码同源性对比 (AST Homology)
> 本部分由专门的代码审查智能体，对该项目与符合年份条件的历届最高相似度项目进行控制流与底层调用对比。新版报告会先给出确定性的全模块覆盖矩阵，再给出 Agent 的分模块解释，避免只围绕少量抽样函数展开。

### 🏆 Top 4 底层对标: vs `2024-undefinedType-南京航空航天大学`（南京航空航天大学 / undefinedType）

#### 对方作品信息

**对方年份**: `2024`
**对方学校**: `南京航空航天大学`
**对方队伍名称**: `undefinedType`
**对方比赛名字**: `OS-Competition`
**对方比赛子赛道**: `Kernel-Implementation`
**对方仓库地址**: `https://gitlab.eduxiji.net/educg-group-26010-2376550/T202410287992755-2176`

> 双向覆盖率：共同 AST 指纹: 373；唯一 AST 指纹: 目标项目 906，参考项目 398；覆盖率: 目标项目 41.17%，参考项目 93.72%

## 全模块对比总览（确定性证据）

> 总体覆盖率：共同 AST 指纹: 373；唯一 AST 指纹: 目标项目 906，参考项目 398；覆盖率: 目标项目 41.17%，参考项目 93.72%

### 1. 模块覆盖矩阵
| 模块 | 目标函数数 | 参考函数数 | 共同唯一 AST 指纹 | Target 覆盖 | Base 覆盖 | 状态 |
| :--- | ---: | ---: | ---: | ---: | ---: | :--- |
| 进程管理 | 94 | 0 | 0 | 0.0% | 0.0% | 仅目标项目体现 |
| 任务调度/异步运行时 | 35 | 0 | 0 | 0.0% | 0.0% | 仅目标项目体现 |
| 内存管理 | 189 | 0 | 0 | 0.0% | 0.0% | 仅目标项目体现 |
| 中断/异常处理 | 28 | 0 | 0 | 0.0% | 0.0% | 仅目标项目体现 |
| 系统调用 | 10 | 0 | 0 | 0.0% | 0.0% | 仅目标项目体现 |
| 文件系统 | 62 | 0 | 0 | 0.0% | 0.0% | 仅目标项目体现 |
| 设备驱动 | 50 | 0 | 0 | 0.0% | 0.0% | 仅目标项目体现 |
| 网络协议栈 | 0 | 0 | 0 | 0.0% | 0.0% | 双方均未体现 |
| 文件系统/第三方库 | 537 | 478 | 373 | 83.26% | 93.72% | 双方存在且有结构重合 |
| 通用模块/其他 | 73 | 0 | 0 | 0.0% | 0.0% | 仅目标项目体现 |

### 2. 分模块证据清单

#### 进程管理
仅目标项目中观察到该模块代表函数，参考项目没有同模块可对齐证据：
- 目标项目：`kernel/proc/socket.c#recvfrom`，调用摘要：MIN, TAILQ_FIRST, TAILQ_FOREACH, TAILQ_REMOVE, acquire, fd_socket_read ...(+4)
- 目标项目：`kernel/proc/socket.c#socketfree`，调用摘要：TAILQ_EMPTY, TAILQ_FIRST, TAILQ_REMOVE, acquire, kfree, memset ...(+4)
- 目标项目：`kernel/proc/socket.c#shutdown`，调用摘要：acquire, release, remote_find_peer_socket, wakeup
- 目标项目：`kernel/proc/proc.c#futex_requeue_locked`，调用摘要：acquire, myproc, release

#### 任务调度/异步运行时
仅目标项目中观察到该模块代表函数，参考项目没有同模块可对齐证据：
- 目标项目：`kernel/driver/loongarch/virtio_pci.c#virtio_pci_read_caps`，调用摘要：get_cfg_addr, pci_config_read, pci_config_read8, printf
- 目标项目：`kernel/fs/vfs/ops.c#resolve_library_path`，调用摘要：memmove, memset, myproc, path_exists, strcat, strcmp ...(+3)
- 目标项目：`user/app/sh.c#runcmd`，调用摘要：close, dup, execve, exit, fork1, fprintf ...(+5)
- 目标项目：`kernel/lib/console.c#consoleintr`，调用摘要：C, acquire, consputc, procdump, release, wakeup

#### 内存管理
仅目标项目中观察到该模块代表函数，参考项目没有同模块可对齐证据：
- 目标项目：`kernel/proc/exec.c#execve`，调用摘要：ADD_AUXV, PGROUNDDOWN, PGROUNDUP, clear_vmas_on_exec, copyout, ip->i_op->lock ...(+15)
- 目标项目：`kernel/proc/exec.c#execve`，调用摘要：ADD_AUXV, PGROUNDDOWN, PGROUNDUP, clear_vmas_on_exec, copyout, ip->i_op->lock ...(+15)
- 目标项目：`kernel/driver/loongarch/pci.c#pci_scan_device`，调用摘要：pci_alloc_device, pci_device_bar_init, pci_device_init, pci_read_config, pci_write_config, printf
- 目标项目：`kernel/proc/proc.c#clone`，调用摘要：acquire, allocproc, copyout, freeproc, futex_owner_pid, get_fops ...(+9)

#### 中断/异常处理
仅目标项目中观察到该模块代表函数，参考项目没有同模块可对齐证据：
- 目标项目：`kernel/trap/riscv/trap.c#trapframedump`，调用摘要：printf
- 目标项目：`kernel/driver/loongarch/pci.c#pci_device_init`，调用摘要：内部闭环逻辑
- 目标项目：`kernel/driver/riscv/virtio_disk.c#virtio_disk_intr2`，调用摘要：R2, acquire, panic, release, wakeup
- 目标项目：`kernel/driver/riscv/virtio_disk.c#virtio_disk_intr`，调用摘要：R, acquire, panic, release, wakeup

#### 系统调用
仅目标项目中观察到该模块代表函数，参考项目没有同模块可对齐证据：
- 目标项目：`kernel/sys/sysothers.c#sys_syslog`，调用摘要：argaddr, argint, either_copyout
- 目标项目：`kernel/lib/printf.c#panic`，调用摘要：printf, sys_shutdown
- 目标项目：`kernel/sys/syssig.c#sys_kill_signal`，调用摘要：argint, kill_signal
- 目标项目：`kernel/sys/syssig.c#sys_tkill`，调用摘要：argint, tkill

#### 文件系统
仅目标项目中观察到该模块代表函数，参考项目没有同模块可对齐证据：
- 目标项目：`user/app/init-loongarch.c#run_busybox_group_by_cmdfile`，调用摘要：chdir, close, is_shell_exit_line, openat, printf, read ...(+4)
- 目标项目：`kernel/fs/vfs/ops.c#get_absolute_path`，调用摘要：strcat, strcpy, strlen, strncpy
- 目标项目：`kernel/driver/loongarch/pci.c#pci_device_probe`，调用摘要：PCI_ADDR, __sync_synchronize, printf
- 目标项目：`user/app/init-loongarch.c#main`，调用摘要：chdir, dup, exec, exit, fork, mknod ...(+6)

#### 设备驱动
仅目标项目中观察到该模块代表函数，参考项目没有同模块可对齐证据：
- 目标项目：`kernel/driver/loongarch/virtio_ring.c#virtio_vring_init`，调用摘要：VQ_ALIGN, virtio_vring_size
- 目标项目：`kernel/driver/loongarch/pci.c#pci_device_bar_init`，调用摘要：内部闭环逻辑
- 目标项目：`kernel/driver/loongarch/virtio_pci.c#virtio_pci_set_queue_addr2`，调用摘要：PCI_REG16, PCI_REG64, VIRT2PHY, dsb
- 目标项目：`kernel/driver/bio.c#bget`，调用摘要：acquire, acquiresleep, panic, release

#### 文件系统/第三方库
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `kernel/fs/ext4/lwext4/ext4_balloc.c#ext4_balloc_alloc_block` | `Kernel/File/lwext4/ext4_balloc.c#ext4_balloc_alloc_block` | 同板块同名同结构 | ext4_balloc_get_bgid_of_block, ext4_balloc_get_block_of_bgid, ext4_balloc_set_bitmap_csum, ext4_balloc_verify_bitmap_csum, ext4_bg_get_block_bitmap, ext4_bg_get_free_blocks_count ...(+19) | EXT4_Debug_u32, ext4_balloc_get_bgid_of_block, ext4_balloc_get_block_of_bgid, ext4_balloc_set_bitmap_csum, ext4_balloc_verify_bitmap_csum, ext4_bg_get_block_bitmap ...(+19) |
| `kernel/fs/ext4/lwext4/ext4.c#ext4_dir_rm` | `Kernel/File/lwext4/ext4.c#ext4_dir_rm` | 同板块同名同结构 | EXT4_MP_LOCK, EXT4_MP_UNLOCK, ext4_block_cache_write_back, ext4_dir_en_get_inode, ext4_dir_iterator_fini, ext4_dir_iterator_init ...(+19) | EXT4_MP_LOCK, EXT4_MP_UNLOCK, ext4_block_cache_write_back, ext4_dir_en_get_inode, ext4_dir_iterator_fini, ext4_dir_iterator_init ...(+19) |
| `kernel/fs/ext4/lwext4/ext4_journal.c#jbd_journal_prepare` | `Kernel/File/lwext4/ext4_journal.c#jbd_journal_prepare` | 同板块同名 | EXT4_BLOCK_ZERO, RB_FIND, TAILQ_FOREACH_REVERSE_SAFE, TAILQ_FOREACH_SAFE, TAILQ_REMOVE, ext4_bcache_clear_dirty ...(+17) | EXT4_BLOCK_ZERO, RB_FIND, TAILQ_FOREACH_REVERSE_SAFE, TAILQ_FOREACH_SAFE, TAILQ_REMOVE, ext4_bcache_clear_dirty ...(+17) |
| `kernel/fs/ext4/lwext4/ext4_dir_idx.c#ext4_dir_dx_split_index` | `Kernel/File/lwext4/ext4_dir_idx.c#ext4_dir_dx_split_index` | 同板块同名同结构 | ext4_block_set, ext4_dir_dx_climit_get_count, ext4_dir_dx_climit_get_limit, ext4_dir_dx_climit_set_count, ext4_dir_dx_climit_set_limit, ext4_dir_dx_entry_get_hash ...(+10) | ext4_block_set, ext4_dir_dx_climit_get_count, ext4_dir_dx_climit_get_limit, ext4_dir_dx_climit_set_count, ext4_dir_dx_climit_set_limit, ext4_dir_dx_entry_get_hash ...(+10) |

#### 通用模块/其他
仅目标项目中观察到该模块代表函数，参考项目没有同模块可对齐证据：
- 目标项目：`kernel/lib/qsort.c#_quicksort`，调用摘要：(*cmp), POP, PUSH, SWAP, min
- 目标项目：`user/app/init-riscv.c#main`，调用摘要：chdir, dup, exec, execve, exit, fork ...(+6)
- 目标项目：`user/app/init-loongarch.c#run_libc_group_by_script`，调用摘要：chdir, close, openat, printf, read, run_libc_line ...(+1)
- 目标项目：`user/app/init-loongarch.c#run_busybox_line`，调用摘要：execve, exit, fork, is_space_ch, kill, printf ...(+3)


好的，我将遵循审计指令，以深度析理模式对本案进行最终同源性定性仲裁。

### 1. 宏观判定
- **定性结论**: **独立开发，存在可合理解释的第三方库同源引用** (客观 AST 相似度: 40.06%，共同 AST 指纹: 373；唯一 AST 指纹: 目标项目 906，参考项目 398；覆盖率: 目标项目 41.17%，参考项目 93.72%)

### 2. 全模块覆盖矩阵解读
- **进程管理**: 仅目标项目存在 (94个函数)。参考项目无此模块。共同AST指纹为0。该模块证据强度：**未形成**。目标项目独立实现了完整的进程管理子系统。
- **任务调度/异步运行时**: 仅目标项目存在 (35个函数)。参考项目无此模块。共同AST指纹为0。该模块证据强度：**未形成**。目标项目拥有独立的调度和异步运行时实现。
- **内存管理**: 仅目标项目存在 (189个函数)。参考项目无此模块。共同AST指纹为0。该模块证据强度：**未形成**。目标项目拥有独立且庞大的内存管理子系统。
- **中断/异常处理**: 仅目标项目存在 (28个函数)。参考项目无此模块。共同AST指纹为0。该模块证据强度：**未形成**。目标项目拥有独立的中断/异常处理机制。
- **系统调用**: 仅目标项目存在 (10个函数)。参考项目无此模块。共同AST指纹为0。该模块证据强度：**未形成**。目标项目拥有独立的系统调用接口。
- **文件系统**: 仅目标项目存在 (62个函数)。参考项目无此模块。共同AST指纹为0。该模块证据强度：**未形成**。目标项目在第三方库之上构建了独立的VFS等文件系统框架。
- **设备驱动**: 仅目标项目存在 (50个函数)。参考项目无此模块。共同AST指纹为0。该模块证据强度：**未形成**。目标项目包含针对特定架构（如LoongArch, RISC-V）的独立设备驱动。
- **网络协议栈**: 双方均未体现。共同AST指纹为0。该模块证据强度：**未形成**。
- **第三方库/公共依赖**: 双方均存在此模块。目标项目537个函数，参考项目478个函数，共同AST指纹373个。目标项目覆盖率83.26%，参考项目覆盖率93.72%。该模块证据强度：**强**，但性质明确为**对同一开源库(lwext4)的共同依赖**。

### 3. 底层 AST 关键证据指纹表
| 所属板块 | 目标项目路径 | 参考项目路径 | 证据类型 | 技术说明 |
| :--- | :--- | :--- | :--- | :--- |
| 第三方库/公共依赖 | `kernel/fs/ext4/lwext4/ext4_extent.c#ext4_extent_remove_space` | `Kernel/File/lwext4/ext4_extent.c#ext4_extent_remove_space` | 强-同源库引用 | 调用图谱高度一致，均包含`EXT_FIRST_EXTENT`, `EXT_LAST_EXTENT`, `IN_RANGE`等ext4规范宏及`ext4_ext_dirty`等内部接口，是ext4 extent树操作的标准实现。 |
| 第三方库/公共依赖 | `kernel/fs/ext4/lwext4/ext4_journal.c#__jbd_journal_commit_trans` | `Kernel/File/lwext4/ext4_journal.c#__jbd_journal_commit_trans` | 强-同源库引用 | 调用图谱高度一致，均使用`RB_EMPTY`, `TAILQ_INSERT_TAIL`等BSD风格宏操作红黑树和尾队列，是JBD2日志事务提交的标准流程。 |
| 第三方库/公共依赖 | `kernel/fs/ext4/lwext4/ext4_xattr.c#ext4_xattr_set_entry` | `Kernel/File/lwext4/ext4_xattr.c#ext4_xattr_set_entry` | 强-同源库引用 | 调用图谱高度一致，均使用`EXT4_XATTR_IS_LAST_ENTRY`, `EXT4_XATTR_NEXT`等专用宏遍历扩展属性，并调用`memcpy`, `memmove`等标准C库函数。 |
| 第三方库/公共依赖 | `kernel/fs/ext4/lwext4/ext4_dir_idx.c#ext4_dir_dx_init` | `Kernel/File/lwext4/ext4_dir_idx.c#ext4_dir_dx_init` | 强-同源库引用 | 调用图谱高度一致，均包含`EXT4_DIRENT_TAIL`, `ext4_dir_dx_root_info_set_info_length`等HTree索引初始化特有的内部函数和宏。 |
| 第三方库/公共依赖 | `kernel/fs/ext4/lwext4/ext4_mkfs.c#fill_sb` | `Kernel/File/lwext4/ext4_mkfs.c#fill_sb` | 强-同源库引用 | 调用图谱高度一致，均调用`ext4_sb_set_blocks_cnt`, `to_le16`, `to_le32`等函数，是填充ext4超级块的标准操作序列。 |
| 第三方库/公共依赖 | `kernel/fs/ext4/lwext4/ext4.c#ext4_dir_rm` | `Kernel/File/lwext4/ext4.c#ext4_dir_rm` | 强-同源库引用 | 调用图谱高度一致，均遵循`EXT4_MP_LOCK` -> `ext4_dir_iterator_*` -> `ext4_fs_free_inode` -> `EXT4_MP_UNLOCK`的目录删除标准流程。 |
| 第三方库/公共依赖 | `kernel/fs/ext4/lwext4/ext4_fs.c#ext4_fs_truncate_inode` | `Kernel/File/lwext4/ext4_fs.c#ext4_fs_truncate_inode` | 强-同源库引用 | 调用图谱高度一致，均调用`ext4_extent_remove_space`和`ext4_fs_release_inode_block`来释放空间，并更新inode大小，是文件截断的标准实现。 |

### 4. 核心代码调用流对比剖析
* **【文件系统/第三方库 - lwext4 库集成】**
* **底层技术析理**: 双方在`lwext4`库上的调用流高度一致，源于对同一开源项目的引用。然而，在调试基础设施上存在显著差异。目标项目倾向于使用`ext4_dbg`或剥离调试调用，而参考项目大量使用`EXT4_Debug_u32`等细粒度宏。这证明双方是独立集成和适配该库的，而非一方抄袭另一方。目标项目在`lwext4`之外，还构建了完整的进程、内存、驱动等内核子系统，其架构复杂度和独立性远超参考项目。
* **代码溯源链接**:
  - 目标项目: `kernel/fs/ext4/lwext4/ext4_journal.c#jbd_iterate_log`
  - 参考项目: `Kernel/File/lwext4/ext4_journal.c#jbd_iterate_log`

### 5. 单方模块与降权项
- **单方模块**: 进程管理、任务调度/异步运行时、内存管理、中断/异常处理、系统调用、文件系统（自有VFS）、设备驱动、通用模块/其他等模块**仅存在于目标项目**。这些模块构成了一个完整操作系统的核心，是目标项目独立开发的强有力证据，与参考项目无任何可对齐证据。
- **降权项**: 所有标记为`[同板块同名]`或`[同板块同名同结构]`的证据，如`ext4_balloc_alloc_block`、`ext4_mkfs`等，均属于`lwext4`第三方库的公开API。其函数名、结构和调用关系由ext4规范和库本身定义，不能作为项目间抄袭的证据。控方已将其降级为弱信号，本仲裁庭予以确认。

### 6. 保守结论
本次审计的客观AST相似度为40.06%，此相似度完全由双方共同集成的开源第三方库`lwext4`贡献。目标项目`[2026-T202610487999858-XX]`是一个包含进程管理、内存管理、设备驱动等完整自研内核子系统的独立操作系统项目，其独有AST指纹数量远超参考项目。参考项目`[2024-undefinedType-南京航空航天大学]`则几乎完全由`lwext4`库构成。所有重合证据均指向对同一开源库的合法引用，且在调试接口等集成细节上存在差异，排除了直接抄袭的可能。综上，目标项目不存在对参考项目的代码抄袭或系统性搬运行为，整体风险为**极低**。

------------------------------

