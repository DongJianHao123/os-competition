# 🛡️ OS 内核赛自动化代码级架构审计报告

**审计时间**: `2026-07-02 04:18:51`
**参赛作品名称**: `2026-T2026103369910596-XX`
**年份**: `2026`
**学校**: `XX`
**队伍名称**: `T2026103369910596`
**比赛名字**: `OS-Competition`
**比赛子赛道**: `Kernel-Implementation`
**仓库地址**: `https://gitlab.eduxiji.net/educg-group-43501-3132633/T2026103369910596-635`
**推测开发语言**: `Rust / C (由AST探针自动识别)`
**特征函数量**: `4473`

## ⚔️ 底层代码同源性对比 (AST Homology)
> 本部分由专门的代码审查智能体，对该项目与符合年份条件的历届最高相似度项目进行控制流与底层调用对比。新版报告会先给出确定性的全模块覆盖矩阵，再给出 Agent 的分模块解释，避免只围绕少量抽样函数展开。

### 🏆 Top 3 底层对标: vs `2024-chaos-北京科技大学`（北京科技大学 / chaos）

#### 对方作品信息

**对方年份**: `2024`
**对方学校**: `北京科技大学`
**对方队伍名称**: `chaos`
**对方比赛名字**: `OS-Competition`
**对方比赛子赛道**: `Kernel-Implementation`
**对方仓库地址**: `https://gitlab.eduxiji.net/educg-group-26010-2376550/oskernel2024-chaos`

> 双向覆盖率：共同 AST 指纹: 165；唯一 AST 指纹: 目标项目 3354，参考项目 798；覆盖率: 目标项目 4.92%，参考项目 20.68%

## 全模块对比总览（确定性证据）

> 总体覆盖率：共同 AST 指纹: 165；唯一 AST 指纹: 目标项目 3354，参考项目 798；覆盖率: 目标项目 4.92%，参考项目 20.68%

### 1. 模块覆盖矩阵
| 模块 | 目标函数数 | 参考函数数 | 共同唯一 AST 指纹 | Target 覆盖 | Base 覆盖 | 状态 |
| :--- | ---: | ---: | ---: | ---: | ---: | :--- |
| 进程管理 | 664 | 45 | 2 | 0.55% | 4.55% | 双方存在且有结构重合 |
| 任务调度/异步运行时 | 357 | 76 | 4 | 1.19% | 5.71% | 双方存在且有结构重合 |
| 内存管理 | 766 | 178 | 41 | 5.97% | 24.55% | 双方存在且有结构重合 |
| 中断/异常处理 | 160 | 5 | 1 | 0.98% | 25.0% | 双方存在且有结构重合 |
| 系统调用 | 244 | 67 | 20 | 11.11% | 50.0% | 双方存在且有结构重合 |
| 文件系统 | 1039 | 152 | 17 | 2.25% | 13.49% | 双方存在且有结构重合 |
| 设备驱动 | 128 | 19 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |
| 网络协议栈 | 290 | 0 | 0 | 0.0% | 0.0% | 仅目标项目体现 |
| 文件系统/第三方库 | 451 | 270 | 54 | 14.88% | 23.68% | 双方存在且有结构重合 |
| 第三方库 | 0 | 50 | 0 | 0.0% | 0.0% | 仅参考项目体现 |
| 通用模块/其他 | 374 | 75 | 15 | 4.66% | 22.06% | 双方存在且有结构重合 |

### 2. 分模块证据清单

#### 进程管理
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `os/src/syscall/process.rs#sys_execve` | `os/src/syscall/process.rs#sys_execve` | 同板块同名 | current_user_token, syscall_body!, trace! | String::from, Vec::new, all_data.as_slice, args.add, args_vec.insert, args_vec.len ...(+22) |
| `os/src/syscall/signal.rs#sys_sigprocmask` | `os/src/syscall/signal.rs#sys_sigprocmask` | 同板块同名 | syscall_body!, trace! | SignalFlags::from_bits, SignalFlags::from_bits(new_set).unwrap, current_task, current_task().unwrap, file!, line! ...(+5) |
| `os/src/syscall/process.rs#sys_spawn` | `os/src/syscall/process.rs#sys_spawn` | 同板块同名 | current_user_token, syscall_body!, trace! | trace! |
| `os/src/sched/processor.rs#schedule` | `os/src/task/processor.rs#schedule` | 同板块同名 | __switch, crate::hal::disable_local_irqs, crate::hal::enable_local_irqs, crate::hal::local_irqs_enabled, current_processor, current_processor().lock ...(+2) | PROCESSOR.exclusive_access, __schedule, drop, file!, line!, processor.get_idle_task_cx_ptr |

#### 任务调度/异步运行时
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `os/src/sched/runqueue.rs#wakeup_task` | `os/src/task/manager.rs#wakeup_task` | 同板块同名 | Arc::ptr_eq, Some, core::hint::spin_loop, drop, enqueue_wakeup_task, hartid ...(+12) | add_task, drop, file!, line!, task.inner_exclusive_access, trace! |
| `os/src/signal/mod.rs#check_signals_of_current` | `os/src/task/mod.rs#check_signals_of_current` | 同板块同名 | SignalBit::from_signum, Some, crate::task::current_process, current_task, current_task().unwrap, debug! ...(+8) | current_task, current_task().unwrap, file!, line!, task.inner_exclusive_access, task_inner.signals.check_error |
| `os/src/sched/api.rs#block_current_and_run_next` | `os/src/task/mod.rs#block_current_and_run_next` | 同板块同名 | Some, assert!, boost_candidates.as_slice, boost_process_cfs_tasks, crate::hal::LocalIrqSave::new, current_processor ...(+25) | add_block_task, drop, file!, line!, schedule, take_current_task ...(+3) |
| `os/src/syscall/fs.rs#sys_mount` | `os/src/syscall/fs.rs#sys_mount` | 同板块同名 | syscall_body!, trace! | trace! |

#### 内存管理
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `os/src/trap/mod.rs#trap_handler` | `os/src/trap/mod.rs#trap_handler` | 同板块同名 | ArchTrapMachine::read_trap_info, ArchTrapMachine::syscall_instruction_len, ExitReason::Exit, ExitReason::Signal, check_fatal_signals_of_current, crate::net::poll ...(+36) | check_signals_of_current, check_timer, current_add_signal, current_task, current_task()
                .unwrap, current_task()
                .unwrap()
                .inner_exclusive_access ...(+24) |
| `os/src/mm/memory_set.rs#from_existed_user` | `os/src/mm/memory_set.rs#from_existed_user` | 同板块同名 | Arc::clone, Ok, Self::new_bare, area
                .data_frames
                .iter, area
                .data_frames
                .iter()
                .map, area
                .data_frames
                .iter()
                .map(|(&vpn, page)| (vpn, Arc::clone(page)))
                .collect ...(+39) | MapArea::from_another, Self::new_process, area.vpn_range.get_start, dst_ppn
                    .get_bytes_array, dst_ppn
                    .get_bytes_array()
                    .copy_from_slice, dst_ppn
                .get_bytes_array ...(+17) |
| `os/src/mm/memory_set.rs#from_elf` | `os/src/mm/memory_set.rs#from_elf` | 同板块同名 | Ok, Self::new_bare, Some, String::from, VirtPageNum, Vma::new_elf ...(+44) | ((ph.virtual_addr() + ph.mem_size()) as usize).into, (ph.virtual_addr() as usize).into, AuxHeader::new, MapArea::new, Self::new_process, Some ...(+29) |
| `os/src/task/process.rs#exec` | `os/src/task/task.rs#exec` | 同板块同名 | DeferredUserReclaim::new, DeferredUserReclaim::new(old_token, old_mask, old_batch).flush_then_release, KERNEL_SPACE.lock, KERNEL_SPACE.lock().token, Ok, ProcessVmLayout::from_user_layout ...(+39) | KERNEL_SPACE.exclusive_access, KERNEL_SPACE.exclusive_access(file!(), line!()).token, MemorySet::from_elf, TaskContext::goto_user_entry, TrapContext::app_init_context, assert_eq! ...(+22) |

#### 中断/异常处理
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `os/src/arch/loongarch64/trap.rs#set_user_trap_entry` | `os/src/trap/mod.rs#set_user_trap_entry` | 同板块同名 | asm! | stvec::write |
| `os/src/arch/riscv/trap.rs#set_kernel_trap_entry` | `os/src/trap/mod.rs#set_kernel_trap_entry` | 同板块同名 | stvec::write | stvec::write |
| `os/src/trap/mod.rs#enable_timer_interrupt` | `os/src/trap/mod.rs#enable_timer_interrupt` | 同板块同名同结构 | ArchInterrupt::enable_timer | sie::set_stimer |
| `os/src/fs/pipe.rs#read_at_result` | `os/src/fs/pipe.rs#read` | 同模块上下文对齐/差异观察 | Arc::clone, Err, Ok, assert!, buf.into_iter, buf.len ...(+12) | assert!, buf.into_iter, buf.len, buf_iter.next, debug!, drop ...(+11) |

#### 系统调用
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `user/src/syscall.rs#syscall` | `user/src/syscall.rs#syscall` | 同板块同名 | core::arch::asm! | asm! |
| `user/src/lib.rs#waitpid` | `user/src/lib.rs#waitpid` | 同板块同名 | sys_waitpid, sys_yield | sys_waitpid, yield_ |
| `user/src/lib.rs#wait` | `user/src/lib.rs#wait` | 同板块同名 | sys_waitpid, sys_yield | sys_waitpid, yield_ |
| `user/src/lib.rs#waittid` | `user/src/lib.rs#waittid` | 同板块同名同结构 | sys_waittid, yield_ | sys_waittid, yield_ |

#### 文件系统
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `fs/src/easyfs/inode.rs#ls` | `os/src/fs/fat32/inode.rs#ls` | 同板块同名 | DirEntry::empty, Self::new, String::from, Vec::new, assert_eq!, child.read_disk_inode ...(+12) | Vec::new, dentry.name, fs.fat.cluster_id_to_sector_id, fs.fat.cluster_id_to_sector_id(self.start_cluster).unwrap, fs.get_dentry, self.fs.as_ref ...(+1) |
| `fs/src/easyfs/layout.rs#read_at` | `os/src/fs/fat32/inode.rs#read_at` | 同板块同名 | (offset + buf.len()).min, Arc::clone, buf.len, dst.copy_from_slice, end_current_block.min, get_block_cache ...(+3) | buf.len, buf[read_size..read_size + copy_size]
                .copy_from_slice, dentry.file_size, fs.cluster_chain, fs.read_cluster, min ...(+3) |
| `fs/src/easyfs/layout.rs#write_at` | `os/src/fs/fat32/inode.rs#write_at` | 同板块同名 | (offset + buf.len()).min, Arc::clone, assert!, buf.len, dst.copy_from_slice, end_current_block.min ...(+4) | buf.len, cluster_buf[pos % CLUSTER_SIZE..pos % CLUSTER_SIZE + copy_size]
                .copy_from_slice, fs.cluster_chain, fs.read_cluster, fs.write_cluster, min ...(+2) |
| `fs/src/vfs.rs#unlink` | `os/src/fs/fat32/inode.rs#unlink` | 同板块同名 | Err, Ok, child.fs_id, child.ino, child.nlink, matches! ...(+9) | dentry.name, fs.fat.cluster_id_to_sector_id, fs.fat.cluster_id_to_sector_id(self.start_cluster).unwrap, fs.get_dentry, fs.remove_dentry, self.fs.as_ref |

#### 设备驱动
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `os/src/drivers/virtio/mod.rs#unshare` | `os/src/drivers/block/virtio_blk.rs#unshare` | 同板块同名 | BOUNCE_MAPPINGS.lock, buffer.as_ptr, buffer.len, copy_from_bounce, drop, mappings
                .iter ...(+4) | 内部闭环逻辑 |
| `os/src/drivers/block/mod.rs#block_device_test` | `os/src/drivers/block/mod.rs#block_device_test` | 同板块同名 | BLOCK_DEVICE.clone, assert_eq!, block_device.read_block, block_device.write_block, println!, write_buffer.iter_mut | BLOCK_DEVICE.clone, assert_eq!, println!, write_buffer.iter_mut |
| `os/src/drivers/virtio/mod.rs#mmio_phys_to_virt` | `os/src/drivers/block/virtio_blk.rs#mmio_phys_to_virt` | 同板块同名 | NonNull::new, NonNull::new(crate::platform::mmio_phys_to_virt(paddr as usize) as *mut u8)
            .expect, crate::platform::mmio_phys_to_virt | KernelAddr::from, NonNull::new_unchecked, PhysAddr::from |
| `os/src/drivers/block/virtio_blk.rs#read_block` | `os/src/drivers/block/virtio_blk.rs#read_block` | 同板块同名 | self.read_blocks | error!, panic!, res.is_err, res.unwrap, self.0.lock, self.0.lock().read_blocks ...(+1) |

#### 网络协议栈
仅目标项目中观察到该模块代表函数，参考项目没有同模块可对齐证据：
- 目标项目：`os/src/net/mod.rs#render_tcp_state_snapshot`，调用摘要：NET_STACK.lock, guard.as_mut, socket.state, st.is_listener_owned, st.orphaned.load, stack.sockets.get_mut::<tcp_socket::Socket> ...(+2)
- 目标项目：`os/src/net/tcp.rs#drop`，调用摘要：NEED_POLL.store, NET_STACK.lock, guard.as_mut, listener.source_id, listener.take_all_states, listener.wake_accept_all ...(+19)
- 目标项目：`os/src/net/mod.rs#deliver_udp_loopback`，调用摘要：Arc::clone, IpEndpoint::new, PacketMeta::default, Some, best
                .as_ref, best
                .as_ref()
                .map ...(+18)
- 目标项目：`os/src/net/tcp.rs#listen`，调用摘要：(*self.bound_endpoint.lock()).ok_or, Arc::clone, Arc::downgrade, Arc::new, Err, NEED_POLL.store ...(+31)

#### 文件系统/第三方库
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `fs/src/ext4_rs/src/ext4_impls/file.rs#write_at` | `os/src/fs/ext4/inode.rs#write_at` | 同板块同名 | (ifile_blocks as usize).saturating_sub, Block::load, Ok, Some, Vec::new, allocated_blocks.len ...(+20) | Arc::downgrade, Ext4File::new, Ext4InodeRef::get_inode_ref, buf.len, inode_ref.inner.inode.inode_get_size, self.fs.ext4.ext4_file_write |
| `fs/src/ext4_rs/src/ext4_impls/extents.rs#ext_remove_leaf` | `os/libs/ext4_rs/src/ext4_structs/inode.rs#ext_remove_leaf` | 同板块同名 | Block::load, Block::load_inode_root_block, Ext4Extent::default, Ok, core::slice::from_raw_parts, ex.get_actual_len ...(+21) | Ext4Extent::default, Ext4Extent::try_from, Ext4Extent::try_from(&self.inner.inode.block[idx..]).unwrap, Ext4ExtentHeader::try_from_u32, Ok, core::ptr::copy_nonoverlapping ...(+11) |
| `fs/src/ext4_rs/src/ext4_impls/extents.rs#extent_remove_space` | `os/libs/ext4_rs/src/ext4_structs/inode.rs#extent_remove_space` | 同板块同名 | Block::load, Ext4Extent::default, Ok, ex.get_actual_len, ex.get_first_block, ex.get_pblock ...(+13) | (from..=to).contains, Ext4Extent::default, Ext4Extent::try_from, Ext4Extent::try_from(&self.inner.inode.block[3..]).unwrap, Ext4Extent::try_from(&self.inner.inode.block[idx..]).unwrap, Ext4ExtentHeader::try_from ...(+18) |
| `fs/src/ext4_rs/src/ext4_impls/extents.rs#insert_extent` | `os/libs/ext4_rs/src/ext4_structs/inode.rs#insert_extent` | 同板块同名 | Ok, inode_ref.inode.root_extent_mut_at, log::info!, self.can_merge, self.create_new_leaf, self.find_extent ...(+4) | self.inner.inode.extent_depth, self.insert_leaf, self.write_back_inode_without_csum |

#### 第三方库
仅参考项目中观察到该模块代表函数，目标项目没有同模块可对齐证据：
- 参考项目：`os/libs/visionfive2-sd/src/lib.rs#send_cmd`，调用摘要：RawInterrupt::from, RawInterruptStatusReg::from, S::sleep_ms_until, Some, arg.into, assert! ...(+21)
- 参考项目：`os/libs/visionfive2-sd/src/lib.rs#init_sdcard`，调用摘要：CardTypeReg::from, CardTypeReg::from(0).with_card_width4_1, CmdArg::new, CmdReg::from, ControlReg::from, RawInterruptStatusReg::from ...(+18)
- 参考项目：`os/libs/visionfive2-sd/src/register.rs#from`，调用摘要：CmdReg::with_data, CmdReg::with_data(0, value.into()).with_transfer_dir, CmdReg::with_no_data, CmdReg::with_no_data(0, value.into())
                    .with_check_response_crc, CmdReg::with_no_data(0, value.into())
                    .with_check_response_crc(false)
                    .with_response_length, CmdReg::with_no_data(0, value.into()).with_check_response_crc ...(+3)
- 参考项目：`os/libs/visionfive2-sd/src/lib.rs#reset_clock`，调用摘要：ClockDividerReg::new, ClockDividerReg::new().with_clk_divider0, ClockEnableReg::from, CmdArg::new, CmdReg::from, CmdReg::from(0)
        .with_start_cmd ...(+7)

#### 通用模块/其他
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `os/src/sbi.rs#sbi_call` | `os/src/sbi.rs#sbi_call` | 同板块同名 | asm! | asm! |
| `os/src/platform/riscv/qemu_virt/board.rs#exit` | `os/src/boards/qemu.rs#exit` | 同板块同名同结构 | asm!, exit_code_encode | asm!, exit_code_encode |
| `os/src/sync/mutex.rs#lock` | `os/src/sync/mutex/spin_mutex.rs#lock` | 同板块同名 | drop, self.inner.lock, self.wait_queue
                .wait_with_reason_or_skip, trace! | S::before_lock, self
                .lock
                .compare_exchange, self
                .lock
                .compare_exchange(false, true, Ordering::Acquire, Ordering::Relaxed)
                .is_ok, self.wait_unlock |
| `os/src/lang_items.rs#backtrace` | `os/src/lang_items.rs#backtrace` | 同板块同名同结构 | asm!, current_kstack_top, println! | asm!, current_kstack_top, println! |


#### 1. 宏观判定
- **定性结论**: 独立开发 (相似度: 4.14%，共同 AST 指纹: 165；唯一 AST 指纹: 目标项目 3354，参考项目 798；覆盖率: 目标项目 4.92%，参考项目 20.68%)

#### 2. 全模块覆盖矩阵解读
- **进程管理**: 双方均存在。共同 AST 指纹仅 2 个，目标覆盖率 0.55%，参考覆盖率 4.55%。重合点仅为 `get_idle_task_cx_ptr` 与 `take_current` 两个单行访问器，其余 99.45% 的目标函数（如 `sys_wait4`、`futex_wait_addr`、`write_process_accounting_on_exit`）在参考项目中无对应。证据强度：**弱**。
- **任务调度/异步运行时**: 双方均存在。共同 AST 指纹 4 个，目标覆盖率 1.19%，参考覆盖率 5.71%。同名函数如 `block_current_and_run_next` 和 `check_signals_of_current` 的调用链差异显著（目标使用 `boost_process_cfs_tasks` 和 `SignalBit::from_signum`，参考使用 `add_block_task` 和 `task_inner.signals.check_error`）。证据强度：**弱**。
- **内存管理**: 双方均存在。共同 AST 指纹 41 个，目标覆盖率 5.97%，参考覆盖率 24.55%。核心函数如 `from_elf` 和 `from_existed_user` 同名，但目标项目使用 `Vma::new_elf` 和 `DeferredUserReclaim` 等抽象，参考项目使用 `MapArea::new` 和直接内存拷贝，内存模型与对象管理完全不同。证据强度：**中**。
- **中断/异常处理**: 双方均存在。共同 AST 指纹 1 个，目标覆盖率 0.98%，参考覆盖率 25.0%。重合点仅为 `set_kernel_trap_entry` 中的 `stvec::write`，这是 RISC-V 架构的标准 CSR 操作，无独创性。证据强度：**弱**。
- **系统调用**: 双方均存在。共同 AST 指纹 20 个，目标覆盖率 11.11%，参考覆盖率 50.0%。用户态 `syscall` 函数均使用内联汇编，这是系统调用 ABI 的必然实现。内核态同名 syscall 的实现路径差异巨大（目标使用 `syscall_body!` 宏，参考内联处理 `sstatus`）。证据强度：**弱**。
- **文件系统**: 双方均存在。共同 AST 指纹 17 个，目标覆盖率 2.25%，参考覆盖率 13.49%。同名函数如 `read_at`、`write_at` 均为块设备 I/O 的标准操作流（计算偏移、获取缓存、拷贝数据），但目标操作的是 `easyfs`，参考操作的是 `fat32`，底层数据结构无关联。证据强度：**弱**。
- **设备驱动**: 双方均存在。共同 AST 指纹 0 个，覆盖率均为 0.0%。双方虽均有 `virtio_blk` 驱动，但 AST 层面无任何重合指纹，实现完全独立。证据强度：**未形成**。
- **网络协议栈**: 仅目标项目存在（290 个函数）。参考项目无此模块。证据强度：**未形成**。
- **第三方库/公共依赖**: 双方均存在。共同 AST 指纹 54 个，目标覆盖率 14.88%，参考覆盖率 23.68%。重合主要集中在 `ext4_rs` 库，该库为第三方开源组件，其内部函数如 `ext_remove_leaf`、`insert_extent` 的 AST 指纹重合属于公共依赖的正常现象，不能作为项目间复用的证据。证据强度：**弱**（降权为公共依赖）。

#### 3. 底层 AST 关键证据指纹表
| 所属板块 | 目标项目路径 | 参考项目路径 | 证据类型 | 技术说明 |
| :--- | :--- | :--- | :--- | :--- |
| 进程管理 | os/src/sched/processor.rs#get_idle_task_cx_ptr | os/src/task/processor.rs#get_idle_task_cx_ptr | 单行访问器 | 均返回 `self.idle_task_cx` 的指针，是持有该字段的结构体的必然实现，属 trivial getter。 |
| 进程管理 | os/src/sched/processor.rs#take_current | os/src/task/processor.rs#take_current | 单行访问器 | 均调用 `self.current.take()`，是 `Option` 字段的标准 Rust 取值习语，无创造性。 |
| 内存管理 | os/src/mm/memory_set.rs#from_elf | os/src/mm/memory_set.rs#from_elf | 同名不同构 | 目标使用 `Vma::new_elf` 抽象，参考使用 `MapArea::new` 和 `AuxHeader`，内存区域建模方式根本不同。 |
| 内存管理 | os/src/task/process.rs#exec | os/src/task/task.rs#exec | 同名不同构 | 目标使用 `DeferredUserReclaim` 延迟回收旧页表，参考直接创建新 `MemorySet`，资源管理策略迥异。 |
| 系统调用 | user/src/syscall.rs#syscall | user/src/syscall.rs#syscall | 架构约束 | 均使用 `asm!` 宏执行 `ecall` 指令，是 RISC-V 系统调用 ABI 的唯一实现方式。 |
| 中断/异常处理 | os/src/arch/riscv/trap.rs#set_kernel_trap_entry | os/src/trap/mod.rs#set_kernel_trap_entry | 架构约束 | 均调用 `stvec::write`，是 RISC-V 特权架构规范定义的标准操作。 |
| 第三方库 | fs/src/ext4_rs/src/ext4_impls/extents.rs#ext_remove_leaf | os/libs/ext4_rs/src/ext4_structs/inode.rs#ext_remove_leaf | 公共依赖 | 均来自 `ext4_rs` 开源库，操作 `Ext4Extent` 等标准 ext4 数据结构，属第三方代码。 |

#### 4. 核心代码调用流对比剖析
* **【进程管理/调度器】**
* **底层技术析理**: 目标项目的 `schedule` 函数通过 `current_processor().lock()` 获取 per-CPU 的处理器锁，并调用 `crate::hal::disable_local_irqs` 进行硬件抽象层的中断管理。参考项目的 `schedule` 则使用全局单例 `PROCESSOR.exclusive_access` 并侵入式地传入 `file!()` 和 `line!()` 调试宏。两者在并发控制模型（per-CPU锁 vs 全局锁）和中断管理策略（HAL抽象 vs 直接操作）上存在根本性架构差异。
* **代码溯源链接**:
  - 目标项目: `os/src/sched/processor.rs#schedule`
  - 参考项目: `os/src/task/processor.rs#schedule`

* **【内存管理/进程执行】**
* **底层技术析理**: 在 `exec` 系统调用的核心实现中，目标项目引入了 `DeferredUserReclaim` 机制，先创建新内存布局再延迟回收旧页表，并显式操作 `KERNEL_SPACE.lock().token`。参考项目则直接调用 `MemorySet::from_elf` 创建新集合并通过 `TaskContext::goto_user_entry` 切换上下文。目标项目展现了更复杂的资源生命周期管理和内核/用户空间隔离策略。
* **代码溯源链接**:
  - 目标项目: `os/src/task/process.rs#exec`
  - 参考项目: `os/src/task/task.rs#exec`

* **【系统调用/进程创建】**
* **底层技术析理**: 目标项目的 `sys_clone` 将参数解析委托给 `CloneRequest::from_legacy` 结构体，实现了参数解析与逻辑执行的分离。参考项目则在 `sys_clone` 内部直接进行 `CloneFlags::from_bits` 和 `SignalFlags::from_bits` 的位操作，并手动检查 `ctid.is_null`。两者在系统调用入口的抽象层次和职责划分上截然不同。
* **代码溯源链接**:
  - 目标项目: `os/src/syscall/process.rs#sys_clone`
  - 参考项目: `os/src/syscall/process.rs#sys_clone`

#### 5. 单方模块与降权项
- **网络协议栈**: 目标项目独有的 `os/src/net/` 模块，包含完整的 TCP/UDP 协议栈实现（如 `tcp.rs#listen`、`mod.rs#deliver_udp_loopback`），参考项目无任何对应。这是目标项目独立开发的强证据。
- **第三方库**: 参考项目独有的 `os/libs/visionfive2-sd/` 是特定硬件（VisionFive2）的 SD 卡驱动库，目标项目无此硬件支持。双方共有的 `ext4_rs` 库属于公共开源依赖，其内部 AST 指纹重合应降权处理，不构成项目间复用证据。
- **公共接口与泛型函数**: 大量同名系统调用（如 `sys_getpid`、`sys_exit`）源于 POSIX/Linux 标准，`trace!`、`debug!`、`Arc::clone` 等源于 Rust 标准库或通用日志框架。这些名称和调用是领域规范或语言习语，不能作为同源性证据。

#### 6. 保守结论
综合评估，目标项目与参考项目的客观 AST 相似度仅为 4.14%，处于极低水平。在进程管理这一核心指控板块，重合的 2 个指纹均为单行、标准的字段访问器，不具备可保护性。双方在调度器并发模型、内存管理资源回收策略、系统调用入口抽象等关键架构决策上存在显著且根本的差异。目标项目独有的网络协议栈、进程记账、futex 等复杂子系统，以及参考项目独有的特定硬件驱动，进一步支持了独立开发的结论。现有证据远不足以构成抄袭或违规复用，应认定为两个独立演进的操作系统内核项目。

------------------------------

