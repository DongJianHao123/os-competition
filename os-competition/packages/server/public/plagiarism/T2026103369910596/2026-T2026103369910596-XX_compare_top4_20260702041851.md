# 🛡️ OS 内核赛自动化代码级架构审计报告

**审计时间**: `2026-07-02 04:18:51`
**参赛作品名称**: `2026-T2026103369910596-XX`
**年份**: `2026`
**学校**: `XX`
**队伍名称**: `T2026103369910596`
**比赛名字**: `OS-Competition`
**比赛子赛道**: `Kernel-Implementation`
**仓库地址**: `https://gitlab.eduxiji.net/educg-group-43501-3132633/T2026103369910596-635`
**推测开发语言**: `Rust / C (由AST探针自动识别)`
**特征函数量**: `4473`

## ⚔️ 底层代码同源性对比 (AST Homology)
> 本部分由专门的代码审查智能体，对该项目与符合年份条件的历届最高相似度项目进行控制流与底层调用对比。新版报告会先给出确定性的全模块覆盖矩阵，再给出 Agent 的分模块解释，避免只围绕少量抽样函数展开。

### 🏆 Top 4 底层对标: vs `2025-广告位招租-西安电子科技大学`（西安电子科技大学 / 广告位招租）

#### 对方作品信息

**对方年份**: `2025`
**对方学校**: `西安电子科技大学`
**对方队伍名称**: `广告位招租`
**对方比赛名字**: `OS-Competition`
**对方比赛子赛道**: `Kernel-Implementation`
**对方仓库地址**: `https://gitlab.eduxiji.net/educg-group-36002-2710490/T202510701995284-398`

> 双向覆盖率：共同 AST 指纹: 199；唯一 AST 指纹: 目标项目 3354，参考项目 1946；覆盖率: 目标项目 5.93%，参考项目 10.23%

## 全模块对比总览（确定性证据）

> 总体覆盖率：共同 AST 指纹: 199；唯一 AST 指纹: 目标项目 3354，参考项目 1946；覆盖率: 目标项目 5.93%，参考项目 10.23%

### 1. 模块覆盖矩阵
| 模块 | 目标函数数 | 参考函数数 | 共同唯一 AST 指纹 | Target 覆盖 | Base 覆盖 | 状态 |
| :--- | ---: | ---: | ---: | ---: | ---: | :--- |
| 进程管理 | 664 | 131 | 8 | 2.19% | 7.48% | 双方存在且有结构重合 |
| 任务调度/异步运行时 | 357 | 139 | 4 | 1.19% | 3.15% | 双方存在且有结构重合 |
| 内存管理 | 766 | 630 | 27 | 3.93% | 6.01% | 双方存在且有结构重合 |
| 中断/异常处理 | 160 | 47 | 1 | 0.98% | 2.5% | 双方存在且有结构重合 |
| 系统调用 | 244 | 64 | 15 | 8.33% | 29.41% | 双方存在且有结构重合 |
| 文件系统 | 1039 | 530 | 16 | 2.12% | 4.64% | 双方存在且有结构重合 |
| 设备驱动 | 128 | 47 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |
| 网络协议栈 | 290 | 91 | 1 | 0.46% | 2.08% | 双方存在且有结构重合 |
| 文件系统/第三方库 | 451 | 366 | 113 | 31.13% | 39.65% | 双方存在且有结构重合 |
| 网络协议栈/第三方库 | 0 | 21 | 0 | 0.0% | 0.0% | 仅参考项目体现 |
| 第三方库 | 0 | 869 | 0 | 0.0% | 0.0% | 仅参考项目体现 |
| 通用模块/其他 | 374 | 226 | 5 | 1.55% | 4.39% | 双方存在且有结构重合 |

### 2. 分模块证据清单

#### 进程管理
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `os/src/syscall/process.rs#sys_execve` | `os/src/syscall/process.rs#sys_execve` | 同板块同名 | current_user_token, syscall_body!, trace! | Box::<[u8; 4]>::new, DEFAULT_SHELL.to_string, Some, Vec::with_capacity, arg_ptr.is_null, argv.add ...(+22) |
| `os/src/sched/processor.rs#run_tasks` | `os/src/task/processor.rs#run_tasks` | 同板块同名 | INITPROC.inner_exclusive_access, INITPROC.inner_exclusive_access().is_zombie, Some, __switch, crate::fs::console_receive, crate::hal::hartid ...(+23) | PROCESSOR.lock, Some, __switch, do_wake_expired, drop, fetch_task ...(+2) |
| `os/src/syscall/process.rs#sys_setpgid` | `os/src/syscall/process.rs#sys_setpgid` | 同板块同名 | syscall_body!, trace! | Some, crate::task::current_task, crate::task::current_task().unwrap, crate::task::find_task_by_tgid, task.setpgid |
| `os/src/syscall/signal.rs#sys_sigprocmask` | `os/src/syscall/process.rs#sys_sigprocmask` | 同板块同名 | syscall_body!, trace! | info!, sigprocmask |

#### 任务调度/异步运行时
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `os/src/syscall/fs.rs#sys_splice` | `os/src/syscall/fs.rs#sys_splice` | 同板块同名 | syscall_body!, trace! | Some, Vec::<u8>::with_capacity, buffer.as_mut_slice, buffer.as_slice, buffer.set_len, current_task ...(+17) |
| `os/src/sched/api.rs#block_current_and_run_next` | `os/src/task/mod.rs#block_current_and_run_next` | 同板块同名 | Some, assert!, boost_candidates.as_slice, boost_process_cfs_tasks, crate::hal::LocalIrqSave::new, current_processor ...(+25) | drop, schedule, sleep_interruptible, take_current_task, take_current_task().unwrap, task.acquire_inner_lock |
| `os/src/syscall/net.rs#sys_bind` | `os/src/syscall/net.rs#sys_bind` | 同板块同名 | syscall_body! | FileDescriptor::new, address::listen_endpoint, address::listen_endpoint(addr_buf).unwrap, current_task, current_task().unwrap, current_task().unwrap().files.lock ...(+14) |
| `os/src/fs/tty.rs#ioctl` | `os/src/fs/dev/tty.rs#ioctl` | 同板块同名 | Err, Ok, current_process, current_user_token, process.getpgid, process.getsid ...(+18) | TeletypeCommand::from_primitive, copy_from_user, copy_to_user, crate::task::current_user_token, info!, self.inner.lock ...(+3) |

#### 内存管理
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `os/src/syscall/mod.rs#syscall` | `os/src/syscall/mod.rs#syscall` | 同板块同名 | (-4095..0).contains, ArchSyscallAbi::decode_clone_args, sys_accept, sys_accept4, sys_acct, sys_add_key ...(+192) | Errno::try_from, [
            //black list
            SYSCALL_YIELD,
            // SYSCALL_READ,
            SYSCALL_WRITE,
            SYSCALL_GETDENTS64,
            SYSCALL_READV,
            SYSCALL_WRITEV,
            SYSCALL_PSELECT6,
            SYSCALL_SIGACTION,
            SYSCALL_SIGPROCMASK,
            // SYSCALL_WAIT4,
            // SYSCALL_GETPPID,
            SYSCALL_CLOCK_GETTIME,
        ]
        .contains, args.len, crate::task::current_task, crate::task::current_task()
                .unwrap, crate::task::current_task()
                .unwrap()
                .acquire_inner_lock ...(+113) |
| `os/src/trap/mod.rs#trap_handler` | `os/src/hal/arch/loongarch64/trap/mod.rs#trap_handler` | 同板块同名 | ArchTrapMachine::read_trap_info, ArchTrapMachine::syscall_instruction_len, ExitReason::Exit, ExitReason::Signal, check_fatal_signals_of_current, crate::net::poll ...(+36) | (0..sz).rev, BadV::read, BadV::read().get_vaddr, ERA::read, ERA::read().next_ins, ERA::read().next_ins().write ...(+55) |
| `os/src/syscall/net.rs#sys_socket` | `os/src/syscall/net.rs#sys_socket` | 同板块同名 | syscall_body! | <dyn Socket>::alloc, info! |
| `os/src/mm/memory_set.rs#new_kernel` | `os/src/mm/memory_set.rs#new_kernel` | 同板块同名 | ((*pair).0 + (*pair).1).into, (*pair).0.into, (ebss as usize).into, (edata as usize).into, (erodata as usize).into, (etext as usize).into ...(+23) | Self::new_bare_kern, anonymous_identical_map!, memory_set.map_trampoline, println!, should_map_trampoline! |

#### 中断/异常处理
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `os/src/arch/loongarch64/trap.rs#set_user_trap_entry` | `os/src/hal/arch/loongarch64/trap/mod.rs#set_user_trap_entry` | 同板块同名 | asm! | register::EEntry::read, register::EEntry::read()
        .set_exception_entry, register::EEntry::read()
        .set_exception_entry(strampoline as usize)
        .write |
| `os/src/arch/riscv/trap.rs#set_kernel_trap_entry` | `os/src/hal/arch/loongarch64/trap/mod.rs#set_kernel_trap_entry` | 同板块同名 | stvec::write | register::EEntry::read, register::EEntry::read()
        .set_exception_entry, register::EEntry::read()
        .set_exception_entry(__kern_trap as usize)
        .write |
| `os/src/trap/mod.rs#set_kernel_trap_entry` | `os/src/hal/arch/riscv/trap/mod.rs#set_kernel_trap_entry` | 同板块同名 | ArchInterrupt::set_kernel_trap_entry | stvec::write |
| `os/src/trap/mod.rs#set_user_trap_entry` | `os/src/hal/arch/riscv/trap/mod.rs#set_user_trap_entry` | 同板块同名 | ArchInterrupt::set_user_trap_entry | stvec::write |

#### 系统调用
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `os/src/syscall/net.rs#sys_getsockname` | `os/src/syscall/net.rs#sys_getsockname` | 同板块同名 | syscall_body! | get_socket!, socket.addr, socket.addr(addr, addrlen).unwrap |
| `os/src/syscall/net.rs#sys_getpeername` | `os/src/syscall/net.rs#sys_getpeername` | 同板块同名 | syscall_body! | get_socket!, socket.peer_addr, socket.peer_addr(addr, addrlen).unwrap |
| `os/src/syscall/net.rs#sys_shutdown` | `user/src/syscall.rs#sys_shutdown` | 同板块同名 | syscall_body! | syscall |
| `os/src/syscall/net.rs#sys_listen` | `os/src/syscall/net.rs#sys_listen` | 同板块同名 | syscall_body! | get_socket!, socket.listen, socket.listen().unwrap |

#### 文件系统
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `fs/src/fat32/inode.rs#create` | `os/src/fs/fat32/fat_osinode.rs#create` | 同板块同名 | Arc::clone, Arc::new, Mutex::new, Some, attr.bits, dir::build_lfn_entries ...(+18) | Arc::new, Mutex::new, Ok, Weak::new, name.to_string, panic! ...(+2) |
| `os/src/fs/devfs.rs#ioctl` | `os/src/fs/file_descriptor.rs#ioctl` | 同板块同名 | (unix_secs as u64)
                    .checked_mul, (unix_secs as u64)
                    .checked_mul(1_000_000_000)
                    .ok_or, Err, Ok, current_user_token, debug! ...(+9) | self.file.ioctl |
| `fs/src/easyfs/layout.rs#get_block_id` | `os/src/fs/fat32/fat_inode.rs#get_block_id` | 同板块同名 | Arc::clone, get_block_cache, get_block_cache(indirect1 as usize, Arc::clone(block_device))
                .lock, get_block_cache(indirect1 as usize, Arc::clone(block_device))
                .lock()
                .read, get_block_cache(self.indirect1 as usize, Arc::clone(block_device))
                .lock, get_block_cache(self.indirect1 as usize, Arc::clone(block_device))
                .lock()
                .read ...(+2) | Some, clus_list.len, self.fs.first_sector_of_cluster |
| `os/src/fs/devfs.rs#ioctl` | `os/src/fs/dev/hwclock.rs#ioctl` | 同板块同名 | Err, Ok, self.size_bytes, write_pod_to_user | 内部闭环逻辑 |

#### 设备驱动
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `os/src/drivers/block/mod.rs#block_device_test` | `os/src/drivers/block/mod.rs#block_device_test` | 同板块同名 | BLOCK_DEVICE.clone, assert_eq!, block_device.read_block, block_device.write_block, println!, write_buffer.iter_mut | BLOCK_DEVICE.clone, assert_eq!, block_device.read_block, block_device.write_block, println!, write_buffer.iter_mut |
| `os/src/drivers/virtio/mod.rs#mmio_phys_to_virt` | `os/src/drivers/block/virtio_blk_mmio.rs#mmio_phys_to_virt` | 同板块同名 | NonNull::new, NonNull::new(crate::platform::mmio_phys_to_virt(paddr as usize) as *mut u8)
            .expect, crate::platform::mmio_phys_to_virt | NonNull::new, NonNull::new(vaddr.0 as *mut u8)
            .expect, PhysAddr, virtio_phys_to_virt |
| `os/src/drivers/block/virtio_blk.rs#read_block` | `os/src/drivers/block/virtio_blk_mmio.rs#read_block` | 同板块同名 | self.read_blocks | assert!, buf.chunks_mut, buf.chunks_mut(VIRT_IO_BLOCK_SZ).enumerate, self.0
                .lock, self.0
                .lock()
                .read_blocks, self.0
                .lock()
                .read_blocks(virtio_block_id as usize, chunk)
                .expect |
| `os/src/drivers/block/virtio_blk.rs#write_block` | `os/src/drivers/block/virtio_blk_mmio.rs#write_block` | 同板块同名 | self.write_blocks | assert!, buf.chunks, buf.chunks(VIRT_IO_BLOCK_SZ).enumerate, self.0
                .lock, self.0
                .lock()
                .write_blocks, self.0
                .lock()
                .write_blocks(virtio_block_id as usize, chunk)
                .expect |

#### 网络协议栈
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `os/src/net/unix_socket.rs#shutdown` | `os/src/net/tcp.rs#shutdown` | 同板块同名 | Err, Ok, drop, self.notify_peer, self.notify_self, self.state.lock ...(+1) | NET_INTERFACE.poll, NET_INTERFACE.tcp_socket, Ok, info!, socket.abort, socket.close |
| `os/src/net/tcp.rs#shutdown` | `os/src/net/unix.rs#shutdown` | 同板块同名 | Err, NEED_POLL.store, NET_STACK.lock, Ok, guard.as_mut, guard.as_mut().ok_or ...(+9) | Ok, log::info! |
| `os/src/net/tcp.rs#remote_endpoint` | `os/src/net/tcp.rs#remote_endpoint` | 同板块同名 | crate::net::NET_STACK.lock, guard.as_mut, self.state, socket.remote_endpoint, stack.sockets.get_mut::<tcp_socket::Socket> | NET_INTERFACE.poll, NET_INTERFACE.tcp_socket, socket.remote_endpoint |
| `os/src/net/compat_socket.rs#ioctl` | `os/src/net/tcp.rs#ioctl` | 同板块同名 | compat_ifreq_ioctl | todo! |

#### 文件系统/第三方库
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `fs/src/ext4_rs/src/ext4_impls/file.rs#write_at` | `os/src/fs/ext4/file.rs#write_at` | 同板块同名 | (ifile_blocks as usize).saturating_sub, Block::load, Ok, Some, Vec::new, allocated_blocks.len ...(+20) | Block::load_offset, Ok, block.sync_blk_to_disk, block.write_offset, drop, inode_ref.inode.set_size ...(+10) |
| `fs/src/ext4_rs/src/ext4_impls/extents.rs#insert_new_extent` | `os/src/fs/ext4/extent.rs#insert_new_extent` | 同板块同名 | (insert_pos..entries).rev, Block::load, Ok, core::mem::size_of::<Ext4Extent>, core::mem::size_of::<Ext4ExtentHeader>, ext4block.read_offset_as ...(+14) | Block::load_offset, Ok, core::mem::size_of::<Ext4Extent>, core::mem::size_of::<Ext4ExtentHeader>, ext4block.read_offset_as_mut, ext4block.sync_blk_to_disk ...(+5) |
| `fs/src/ext4_rs/src/ext4_impls/extents.rs#ext_remove_leaf` | `os/src/fs/ext4/extent.rs#ext_remove_leaf` | 同板块同名 | Block::load, Block::load_inode_root_block, Ext4Extent::default, Ok, core::slice::from_raw_parts, ex.get_actual_len ...(+21) | Block::load_inode_root_block, Block::load_offset, Ext4Extent::default, Ok, ex.get_actual_len, ex.get_pblock ...(+14) |
| `fs/src/ext4_rs/src/ext4_impls/extents.rs#ext_grow_indepth` | `os/src/fs/ext4/extent.rs#ext_grow_indepth` | 同板块同名 | Block::load, Ext4ExtentHeader::new, Ok, core::ptr::write_bytes, core::slice::from_raw_parts, inode_ref.inode.block.as_mut_ptr ...(+22) | Block::load_offset, Ext4ExtentHeader::load_from_u8_mut, Ok, core::mem::size_of::<Ext4Extent>, core::mem::size_of::<Ext4ExtentHeader>, core::ptr::copy_nonoverlapping ...(+15) |

#### 网络协议栈/第三方库
仅参考项目中观察到该模块代表函数，目标项目没有同模块可对齐证据：
- 参考项目：`dependency/dep_iso/isomorphic_drivers/src/net/ethernet/intel/ixgbe.rs#new`，调用摘要：(IXGBE_FCRTH..IXGBE_FCRTH_END).step_by, (IXGBE_FCRTL..IXGBE_FCRTL_END).step_by, (IXGBE_FCTTV..IXGBE_FCTTV_END).step_by, EthernetAddress::from_bytes, MaybeUninit::uninit, MaybeUninit::uninit().assume_init ...(+56)
- 参考项目：`dependency/dep_iso/isomorphic_drivers/src/net/ethernet/intel/e1000.rs#new`，调用摘要：P::alloc_dma, Vec::with_capacity, assert_eq!, debug!, e1000[E1000_ICR].read, e1000[E1000_ICR].write ...(+25)
- 参考项目：`dependency/dep_iso/isomorphic_drivers/src/net/ethernet/intel/ixgbe.rs#msend`，调用摘要：data.len, data[data_index].len, fence, ixgbe[IXGBE_TDT + IXGBE_TDT_GAP * queue].read, ixgbe[IXGBE_TDT + IXGBE_TDT_GAP * queue].write, send_desc.status.get_bit ...(+2)
- 参考项目：`dependency/dep_iso/isomorphic_drivers/src/net/ethernet/intel/ixgbe.rs#drop`，调用摘要：P::dealloc_dma, buffer.iter, info!, ixgbe[IXGBE_CTRL].read, ixgbe[IXGBE_CTRL].write, ixgbe[IXGBE_EIMC1].write ...(+8)

#### 第三方库
仅参考项目中观察到该模块代表函数，目标项目没有同模块可对齐证据：
- 参考项目：`dependency/dep_pci/pci/src/lib.rs#probe_function`，调用摘要：BAR::decode, BridgeControl::from_bits_truncate, Command::from_bits_truncate, DeviceKind::CardbusBridge, DeviceKind::Device, DeviceKind::PciBridge ...(+6)
- 参考项目：`dependency/dep_iso/isomorphic_drivers/src/block/ahci.rs#new`，调用摘要：(0..ghc.num_ports()).find, P::alloc_dma, Some, c.set_bit, debug!, ghc.enable ...(+16)
- 参考项目：`dependency/riscv/descriptor/generator.rs#generate`，调用摘要：String::new, bf.generate_enum, format!, self.bfs.iter, write!, write!(&mut bit_sets, "{}", bf.generate_bitops()).unwrap ...(+2)
- 参考项目：`dependency/rustsbi/src/ecall.rs#handle_ecall`，调用摘要：SbiRet::not_supported, base::handle_ecall_base, hsm::handle_ecall_hsm, ipi::handle_ecall_ipi, legacy::console_getchar, legacy::console_getchar().legacy_return ...(+15)

#### 通用模块/其他
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `os/src/random.rs#fill_bytes` | `os/src/utils/random.rs#fill_bytes` | 同板块同名 | Self::chacha20_block, core::cmp::min, out.is_empty, out.len, out[..take].copy_from_slice, self.counter.wrapping_add | dest.len, self.next_u32 |
| `os/src/sbi.rs#sbi_call` | `os/src/hal/arch/riscv/sbi.rs#sbi_call` | 同板块同名 | asm! | asm! |
| `user/src/console.rs#write_str` | `os/src/console.rs#write_str` | 同板块同名 | Err, Ok, s.as_bytes, s.as_bytes().iter, self.0.len, self.0.push_back ...(+1) | Ok, console_flush, console_putchar, s.chars |
| `os/src/console.rs#print` | `os/src/console.rs#print` | 同板块同名 | ConsoleGuard::lock, EarlyStdout.write_fmt, EarlyStdout.write_fmt(args).unwrap, Stdout.write_fmt, Stdout.write_fmt(args).unwrap, crate::platform::use_early_console | KernelOutput.write_fmt, KernelOutput.write_fmt(args).unwrap |


好的，审计官。以下是基于提供的证据底座、控辩双方论点及审计指令形成的最终同源性定性仲裁报告。

---

### 1. 宏观判定
- **定性结论**: **独立开发，存在因领域标准与惯用法导致的弱同源性痕迹**
- **核心依据**: 客观 AST 相似度仅为 **3.9%**，共同 AST 指纹仅 199 个，而双方独立指纹总数超过 5300 个。目标项目覆盖率仅 5.93%，参考项目覆盖率仅 10.23%。极低的量化指标从根本上否定了系统性抄袭或大规模架构复用的可能性。控方所指控的“底层逻辑一致”函数，经分析均为 Rust 语言标准惯用法或操作系统领域的必要功能表达，不构成可保护的独创性表达。

### 2. 全模块覆盖矩阵解读
- **进程管理**: 双方均存在。共同 AST 指纹仅 8 个，目标覆盖率 2.19%，参考覆盖率 7.48%。证据强度 **弱**。同名函数多为 POSIX 标准系统调用接口，其存在本身不构成同源证据。控方提出的“底层逻辑一致”函数均为极简的 getter/setter，属于 Rust `Option` 或结构体字段访问的标准模式。
- **任务调度/异步运行时**: 双方均存在。共同 AST 指纹仅 4 个，目标覆盖率 1.19%，参考覆盖率 3.15%。证据强度 **弱**。`block_current_and_run_next` 等函数虽同名，但调用图谱显示目标项目引入了复杂的 CFS 调度逻辑和硬件抽象层，与参考项目的简单睡眠/唤醒模型有本质区别。
- **内存管理**: 双方均存在。共同 AST 指纹 27 个，目标覆盖率 3.93%，参考覆盖率 6.01%。证据强度 **弱**。`new_kernel` 等函数同名，但目标项目使用 `anonymous_identical_map!` 等宏，参考项目则使用 `MemorySet::new_bare_kern`，内存映射的构建策略和抽象层次不同。
- **中断/异常处理**: 双方均存在。共同 AST 指纹仅 1 个，目标覆盖率 0.98%，参考覆盖率 2.5%。证据强度 **弱**。`set_user_trap_entry` 等函数同名，但这是与硬件架构强相关的必要操作（设置特定寄存器），其逻辑由硬件手册决定，而非软件原创。
- **系统调用**: 双方均存在。共同 AST 指纹 15 个，目标覆盖率 8.33%，参考覆盖率 29.41%。证据强度 **中**。`syscall` 分发函数是核心，但目标项目采用宏生成的巨大 `match` 分支，参考项目则包含黑名单过滤逻辑，架构设计思路不同。网络相关的 `sys_socket` 等函数同名，但调用链差异显著。
- **文件系统**: 双方均存在。共同 AST 指纹 16 个，目标覆盖率 2.12%，参考覆盖率 4.64%。证据强度 **弱**。`create`、`get_block_id` 等函数同名，但分属 `fat32` 和 `easyfs` 等不同实现，底层磁盘布局和块管理算法完全不同，同名仅为功能描述性重合。
- **设备驱动**: 双方均存在。共同 AST 指纹 0 个，覆盖率均为 0.0%。证据强度 **未形成**。`block_device_test` 等函数虽同名，但 AST 指纹未匹配，说明实现细节不同。这是双方独立开发的有力佐证。
- **网络协议栈**: 双方均存在。共同 AST 指纹仅 1 个，目标覆盖率 0.46%，参考覆盖率 2.08%。证据强度 **弱**。`shutdown` 等函数同名，但目标项目操作的是 `smoltcp` 库的 socket，参考项目操作的是自研或其它封装的 socket，底层对象模型完全不同。
- **第三方库/公共依赖**: 目标项目在 `文件系统/第三方库` 模块有 451 个函数，参考项目有 366 个，共同指纹 113 个，覆盖率分别高达 31.13% 和 39.65%。证据强度 **强**，但性质明确为**公共第三方库复用**。这些证据集中在 `ext4_rs` 等目录，属于对开源 ext4 库的共同依赖，不构成参赛作品之间的抄袭。参考项目独有的 `第三方库` 模块（869个函数）进一步证明双方依赖库集合不同。

### 3. 底层 AST 关键证据指纹表
| 所属板块 | 目标项目路径 | 参考项目路径 | 证据类型 | 技术说明 |
| :--- | :--- | :--- | :--- | :--- |
| 第三方库/公共依赖 | `fs/src/ext4_rs/...` | `os/src/fs/ext4/...` | 强-公共库复用 | 双方均集成了 ext4 文件系统的第三方实现，导致 `extents.rs`、`file.rs` 等文件中出现大量同源 AST 指纹。这是对开源代码的共同依赖，非相互抄袭。 |
| 进程管理 | `os/src/sched/processor.rs#take_current` | `os/src/task/processor.rs#take_current` | 弱-惯用法 | `self.current.take()` 是 Rust `Option` 枚举取出内部值的唯一标准方法，无独创性。 |
| 进程管理 | `os/src/sched/processor.rs#current` | `os/src/task/processor.rs#current` | 弱-惯用法 | `self.current.as_ref().map(...)` 是 Rust 中安全访问 `Option` 内引用并映射的标准链式调用。 |
| 进程管理 | `os/src/task/process.rs#getpid` | `os/src/task/task.rs#getpid` | 弱-功能必要 | 返回进程 PID 的 getter 方法，其实现必然是访问并返回内部整型字段，无其它表达方式。 |
| 系统调用 | `os/src/syscall/mod.rs#syscall` | `os/src/syscall/mod.rs#syscall` | 中-架构差异 | 核心系统调用分发函数同名，但目标项目为宏生成的巨型 match，参考项目包含黑名单逻辑，顶层设计不同。 |
| 内存管理 | `os/src/mm/memory_set.rs#new_kernel` | `os/src/mm/memory_set.rs#new_kernel` | 弱-功能必要 | 创建内核地址空间的入口函数同名，但内部映射机制（宏 vs 构造器）和段定义方式存在差异。 |

### 4. 核心代码调用流对比剖析
* **【进程管理/调度器核心】**
* **底层技术析理**: 双方均实现了 `run_tasks` 调度循环与 `schedule` 切换函数，但抽象层次与同步策略截然不同。目标项目 `run_tasks` 直接耦合了 `crate::hal::hartid`、`crate::platform` 等硬件抽象层，并显式处理多核（hart）与 INITPROC 僵尸状态，其 `schedule` 函数通过 `crate::hal::disable_local_irqs` 进行关中断保护。参考项目 `run_tasks` 则通过全局锁 `PROCESSOR.lock` 保护，并调用 `do_wake_expired` 处理定时器，其 `schedule` 函数极为简洁，仅包含锁获取与上下文切换。目标项目展现了更复杂的多核感知与硬件抽象设计，而参考项目是典型的单核、全局锁调度模型。两者在同步原语和架构分层上存在根本性异构。
* **代码溯源链接**:
  - 目标项目: `os/src/sched/processor.rs#run_tasks` / `os/src/sched/processor.rs#schedule`
  - 参考项目: `os/src/task/processor.rs#run_tasks` / `os/src/task/processor.rs#schedule`

* **【内存管理/内核映射】**
* **底层技术析理**: 双方均有 `new_kernel` 函数用于创建内核地址空间，但映射策略不同。目标项目使用声明宏 `anonymous_identical_map!` 批量完成等值映射，体现了以宏为核心的元编程抽象。参考项目则调用 `new_bare_kern` 构造器并显式调用 `map_trampoline`，展现了基于构造器和方法的对象组合模式。两者在代码生成与抽象粒度上存在显著差异。
* **代码溯源链接**:
  - 目标项目: `os/src/mm/memory_set.rs#new_kernel`
  - 参考项目: `os/src/fs/procfs/mod.rs#init_procfs`

* **【系统调用/网络子系统】**
* **底层技术析理**: 以 `sys_socket` 为例，目标项目通过 `syscall_body!` 宏隐藏了具体实现，表明其系统调用处理有统一的宏封装层。参考项目则直接调用 `<dyn Socket>::alloc` 和 `info!`，实现更为直接。`sys_bind` 的调用流对比更明显：目标项目仅见 `syscall_body!`，而参考项目则展开了完整的地址解析、文件描述符分配流程。目标项目的宏封装策略与参考项目的手动展开策略形成鲜明对比。
* **代码溯源链接**:
  - 目标项目: `os/src/syscall/net.rs#sys_socket` / `os/src/syscall/net.rs#sys_bind`
  - 参考项目: `os/src/syscall/net.rs#sys_socket` / `os/src/syscall/net.rs#sys_bind`

### 5. 单方模块与降权项
- **单方模块**: 参考项目独有的 `网络协议栈/第三方库` (21个函数) 和 `第三方库` (869个函数) 模块，目标项目均无对应 AST 指纹。这证明双方依赖的第三方库生态存在巨大差异，是独立选型与开发的强有力证据。
- **降权项**: 控方指控的 `readable` 函数，目标项目位于 `os/src/syscall/process.rs`，参考项目位于 `os/src/fs/procfs/interrupts.rs`，属于**跨板块、跨层级**的同名函数。`readable` 是描述“可读”状态的通用动词，在系统调用层和文件系统层均有其合理性，此证据不构成同源性指控，予以降权。
- **降权项**: 大量标记为 `[同板块同名]` 的函数，如 `sys_execve`、`sys_clone` 等，其名称源于 POSIX 标准，是操作系统提供标准化接口的必然结果。其内部调用图谱（如目标项目大量使用 `syscall_body!` 宏）已发生实质性重构，不能作为抄袭证据。

### 6. 保守结论
综合全模块证据，目标项目与参考项目的客观 AST 相似度仅为 3.9%，远低于构成抄袭嫌疑的阈值。双方在进程管理、内存管理、文件系统等核心模块虽有同名函数，但经调用流剖析，其在同步策略、抽象层次、第三方库依赖上存在根本性架构差异。控方提出的“底层逻辑一致”指控，实为 Rust 语言标准惯用法和操作系统领域必要功能表达所导致的不可避免的语法收敛。唯一高覆盖率模块明确指向公共第三方 ext4 库的复用。因此，整体风险极低，无证据表明目标项目对参考项目存在有意识的代码抄袭或架构复用，应判定为独立开发。

------------------------------

