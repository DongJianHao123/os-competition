# 🛡️ OS 内核赛自动化代码级架构审计报告

**审计时间**: `2026-07-02 02:49:58`
**参赛作品名称**: `2026-T202610008999575-XX`
**年份**: `2026`
**学校**: `XX`
**队伍名称**: `T202610008999575`
**比赛名字**: `OS-Competition`
**比赛子赛道**: `Kernel-Implementation`
**仓库地址**: `https://gitlab.eduxiji.net/educg-group-43501-3132633/T202610008999575-3763`
**推测开发语言**: `Rust / C (由AST探针自动识别)`
**特征函数量**: `1658`

## ⚔️ 底层代码同源性对比 (AST Homology)
> 本部分由专门的代码审查智能体，对该项目与符合年份条件的历届最高相似度项目进行控制流与底层调用对比。新版报告会先给出确定性的全模块覆盖矩阵，再给出 Agent 的分模块解释，避免只围绕少量抽样函数展开。

### 🏆 Top 1 底层对标: vs `2022-S0S-哈尔滨工业大学`（哈尔滨工业大学 / S0S）

#### 对方作品信息

**对方年份**: `2022`
**对方学校**: `哈尔滨工业大学`
**对方队伍名称**: `S0S`
**对方比赛名字**: `OS-Competition`
**对方比赛子赛道**: `Kernel-Implementation`
**对方仓库地址**: `https://gitlab.eduxiji.net/educg-group-14158-894147/oskernel2022-s0s`

> 双向覆盖率：共同 AST 指纹: 97；唯一 AST 指纹: 目标项目 1221，参考项目 447；覆盖率: 目标项目 7.94%，参考项目 21.7%

## 全模块对比总览（确定性证据）

> 总体覆盖率：共同 AST 指纹: 97；唯一 AST 指纹: 目标项目 1221，参考项目 447；覆盖率: 目标项目 7.94%，参考项目 21.7%

### 1. 模块覆盖矩阵
| 模块 | 目标函数数 | 参考函数数 | 共同唯一 AST 指纹 | Target 覆盖 | Base 覆盖 | 状态 |
| :--- | ---: | ---: | ---: | ---: | ---: | :--- |
| 进程管理 | 362 | 51 | 11 | 4.2% | 23.4% | 双方存在且有结构重合 |
| 任务调度/异步运行时 | 105 | 41 | 3 | 3.49% | 7.5% | 双方存在且有结构重合 |
| 内存管理 | 381 | 139 | 42 | 11.93% | 33.07% | 双方存在且有结构重合 |
| 中断/异常处理 | 13 | 5 | 2 | 18.18% | 50.0% | 双方存在且有结构重合 |
| 系统调用 | 123 | 35 | 5 | 5.95% | 20.83% | 双方存在且有结构重合 |
| 文件系统 | 381 | 166 | 13 | 5.94% | 8.72% | 双方存在且有结构重合 |
| 设备驱动 | 56 | 42 | 1 | 1.89% | 2.63% | 双方存在且有结构重合 |
| 网络协议栈 | 47 | 0 | 0 | 0.0% | 0.0% | 仅目标项目体现 |
| 文件系统/第三方库 | 69 | 0 | 0 | 0.0% | 0.0% | 仅目标项目体现 |
| 通用模块/其他 | 121 | 31 | 9 | 8.18% | 31.03% | 双方存在且有结构重合 |

### 2. 分模块证据清单

#### 进程管理
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `os/src/process/processor.rs#run_tasks` | `os/src/task/processor.rs#run_tasks` | 同板块同名 | MAIN_HART_ID.load, Some, __switch, asm!, crate::arch::timer::set_next_trigger, crate::task::add_task_into_pool_unlocked ...(+15) | PROCESSORS[hartid()].lock, Some, __switch, drop, fetch_task, hartid ...(+2) |
| `os/src/syscall/process.rs#sys_kill` | `os/src/syscall/process.rs#sys_kill` | 同板块同名 | EINVAL.as_isize, ESRCH.as_isize, SignalFlags::from_bits, Some, Vec::new, crate::process::wake_up_task ...(+24) | SignalFlags::from_bits, pid2task, task.inner_exclusive_access, task_ref.signals.contains, task_ref.signals.insert |
| `os/src/syscall/process.rs#sys_uname` | `os/src/syscall/process.rs#sys_uname` | 同板块同名 | EFAULT.as_isize, current_task, current_task().unwrap, current_user_token, dest[..len].copy_from_slice, drop ...(+10) | UserBuffer::new, Utsname::new, buffer.read, current_user_token, size_of::<Utsname>, translated_byte_buffer ...(+1) |
| `os/src/syscall/process.rs#sys_sigprocmask` | `os/src/syscall/process.rs#sys_sigprocmask` | 同板块同名 | EFAULT.as_isize, EINVAL.as_isize, SignalFlags::from_bits_truncate, current_task, current_task().unwrap, inner.signal_mask.bits ...(+9) | SignalFlags::from_bits, current_task, old_mask.bits, task.inner_exclusive_access |

#### 任务调度/异步运行时
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `os/src/syscall/fs.rs#sys_writev` | `os/src/syscall/fs.rs#sys_writev` | 同板块同名 | (iovec.len - written_in_iov).min, EACCES.as_isize, EAGAIN.as_isize, EBADF.as_isize, EFAULT.as_isize, EINVAL.as_isize ...(+26) | UserBuffer::new, current_task, current_task().unwrap, current_user_token, f.clone, f.write ...(+5) |
| `os/src/syscall/fs.rs#sys_sendfile` | `os/src/syscall/fs.rs#sys_sendfile` | 同板块同名 | EBADF.as_isize, ENOSYS.as_isize, buffer.as_mut_ptr, buffer.as_mut_ptr().add, buffer.len, core::slice::from_raw_parts_mut ...(+16) | UserBuffer::new, current_task, current_task().unwrap, current_user_token, f.clone, fin.offset ...(+7) |
| `os/src/syscall/fs.rs#sys_write` | `os/src/syscall/fs.rs#sys_write` | 同板块同名 | EACCES.as_isize, EAGAIN.as_isize, EBADF.as_isize, EFAULT.as_isize, UserBuffer::new, crate::mm::translated_byte_buffer ...(+21) | UserBuffer::new, current_task, current_task().unwrap, current_user_token, drop, f.clone ...(+5) |
| `os/src/syscall/fs.rs#sys_readv` | `os/src/syscall/fs.rs#sys_readv` | 同板块同名 | EBADF.as_isize, EFAULT.as_isize, core::mem::size_of::<IoVec>, crate::mm::translated_byte_buffer_mut, current_task, current_task().unwrap ...(+13) | UserBuffer::new, current_task, current_task().unwrap, current_user_token, f.clone, f.read ...(+5) |

#### 内存管理
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `os/src/syscall/process.rs#sys_wait4` | `os/src/syscall/process.rs#sys_wait4` | 同板块同名 | ECHILD.as_isize, EFAULT.as_isize, ESRCH.as_isize, Some, child.getpid, child.inner_exclusive_access ...(+16) | Arc::strong_count, assert_eq!, child.getpid, child.inner_exclusive_access, current_task, current_task().unwrap ...(+14) |
| `os/src/arch/la/trap/mod.rs#trap_handler` | `os/src/trap/mod.rs#trap_handler` | 同板块同名 | Arc::clone, VirtAddr::from, VirtAddr::from(badv).std_floor, asm!, crate::mm::mmap::tick_sync, crate::task::add_task ...(+32) | check_signals_error_of_current, current_add_signal, current_trap_cx, exit_current_and_run_next, handle_signals, panic! ...(+9) |
| `os/src/process/pcb.rs#fork` | `os/src/task/task.rs#fork` | 同板块同名 | Arc::downgrade, Arc::new, AtomicI32::new, MPSafeCell::new, MapArea::new, MemorySet::from_existed_user ...(+51) | Arc::downgrade, Arc::new, KernelStack::new, MemorySet::from_existed_user, Mutex::new, SignalFlags::empty ...(+20) |
| `os/src/mm/memory_set.rs#new_kernel` | `os/src/mm/memory_set.rs#new_kernel` | 同板块同名 | ((*pair).0 + (*pair).1).into, (*pair).0.into, (LOWRAM_BASE).into, (LOWRAM_END).into, (MEMORY_END - 4096).into, (ebss as *const () as usize).into ...(+16) | ((*pair).0 + (*pair).1).into, (*pair).0.into, (ebss as usize).into, (edata as usize).into, (ekernel as usize).into, (erodata as usize).into ...(+11) |

#### 中断/异常处理
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `os/src/arch/la/trap/mod.rs#enable_timer_interrupt` | `os/src/trap/mod.rs#enable_timer_interrupt` | 同板块同名 | asm!, crate::arch::timer::init_board_freq | sie::set_stimer |
| `os/src/arch/la/trap/mod.rs#set_kernel_trap_entry` | `os/src/trap/mod.rs#set_kernel_trap_entry` | 同板块同名 | asm! | stvec::write |
| `os/src/arch/riscv/trap/mod.rs#set_user_trap_entry` | `os/src/trap/mod.rs#set_user_trap_entry` | 同板块同名 | stvec::write | stvec::write |
| `os/src/arch/riscv/trap/mod.rs#init` | `os/src/trap/mod.rs#init` | 同板块同名同结构 | set_kernel_trap_entry | set_kernel_trap_entry |

#### 系统调用
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `user/src/syscall.rs#syscall` | `user/src/syscall.rs#syscall` | 同板块同名 | core::arch::asm! | asm! |
| `user/src/lib.rs#sigaction` | `user/src/lib.rs#sigaction` | 同板块同名 | action.map_or, core::ptr::null, core::ptr::null_mut, old_action.map_or, sys_sigaction | sys_sigaction |
| `user/src/lib.rs#waitpid` | `user/src/lib.rs#waitpid` | 同板块同名 | sys_waitpid, sys_yield | sys_waitpid |
| `user/src/lib.rs#wait` | `user/src/lib.rs#wait` | 同板块同名 | sys_waitpid, sys_yield | sys_waitpid, yield_ |

#### 文件系统
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `os/src/fs/inode.rs#open_file` | `os/src/fs/inode.rs#open_file` | 同板块同名 | Arc::new, OSInode::new, ROOT_DENTRY.clone, Some, create_file_in_dentry, file_name ...(+11) | Some, dir.inner_exclusive_access, dir.read, dir.read().find_entry, dir.read().find_entry(name).map, dir.write ...(+11) |
| `os/src/fs/inode.rs#list_apps` | `os/src/fs/inode.rs#list_apps` | 同板块同名 | ROOT_INODE.inode.getdents, buf[offset..].as_ptr, core::str::from_utf8, core::str::from_utf8(&entry.d_name[..name_len]).unwrap_or, entry.d_name.iter, entry.d_name.iter().position ...(+2) | println!, root_inode, root_inode.read, root_inode.read().list, root_osinode.inner_exclusive_access |
| `os/src/lang_items.rs#panic` | `os/src/lang_items.rs#panic` | 同板块同名 | PANICKING.swap, crate::ext4fs::block_cache_sync_all, info.location, println!, shutdown, spin_loop | info.location, println!, shutdown |
| `os/src/drivers/loopdev.rs#write_at` | `fat32/src/vfs.rs#write_at` | 同板块同名 | buf.len, debug_assert_eq!, self.backing_file.as_ref, self.backing_file.as_ref().unwrap, self.backing_file.as_ref().unwrap().get_size, self.backing_file.as_ref().unwrap().write_at | (offset + buf.len()).min, Arc::clone, buf.len, buf.len().min, dst.copy_from_slice, end_current_block.min ...(+17) |

#### 设备驱动
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `os/src/drivers/block/block_cache.rs#get_block_cache` | `fat32/src/block_cache.rs#get_block_cache` | 同板块同名 | Arc::clone, Arc::new, Arc::strong_count, BlockCache::new_empty, Mutex::new, block_cache.lock ...(+17) | Arc::clone, Arc::new, Arc::strong_count, BlockCache::new, RwLock::new, panic! ...(+8) |
| `os/src/arch/riscv/drivers/block/mod.rs#block_device_test` | `os/src/drivers/block/mod.rs#block_device_test` | 同板块同名 | BLOCK_DEVICE.clone, NET_DEVICE.0.exclusive_access, assert_eq!, block_device.read_block, block_device.write_block, println! ...(+1) | BLOCK_DEVICE.clone, assert_eq!, block_device.read_block, block_device.write_block, println!, write_buffer.iter_mut |
| `os/src/drivers/block/mod.rs#read_block` | `os/src/drivers/block/virtio_blk.rs#read_block` | 同板块同名 | BLOCK_DEVICE.clone, block.get_ref, buf.copy_from_slice, buf.len, cache.lock, get_block_cache | self.0
            .lock, self.0
            .lock()
            .read_block, self.0
            .lock()
            .read_block(block_id, buf)
            .expect |
| `os/src/drivers/block/mod.rs#write_block` | `os/src/drivers/block/virtio_blk.rs#write_block` | 同板块同名 | BLOCK_DEVICE.clone, block_data[..len].copy_from_slice, buf.len, cache.lock, cache.lock().modify, get_block_cache | self.0
            .lock, self.0
            .lock()
            .write_block, self.0
            .lock()
            .write_block(block_id, buf)
            .expect |

#### 网络协议栈
仅目标项目中观察到该模块代表函数，参考项目没有同模块可对齐证据：
- 目标项目：`os/src/net/socket.rs#new`，调用摘要：Arc::new, IpProtocol::from, Mutex::new, RawPacketBuffer::new, RawSocketSmol::new, SOCKET_SET.exclusive_access ...(+9)
- 目标项目：`os/src/net/socket.rs#recvfrom`，调用摘要：Some, buf.len, buf[..copy_len].copy_from_slice, crate::net::SOCKET_SET.exclusive_access, data.len, smoltcp::wire::IpAddress::v4 ...(+4)
- 目标项目：`os/src/net/socket.rs#readable`，调用摘要：crate::net::SOCKET_SET.exclusive_access, matches!, self.is_listener.load, socket.can_recv, socket.may_recv, socket.state ...(+1)
- 目标项目：`os/src/net/socket.rs#pair`，调用摘要：Arc::downgrade, Arc::new, Mutex::new, Some, VecDeque::new, left.lock ...(+1)

#### 文件系统/第三方库
仅目标项目中观察到该模块代表函数，参考项目没有同模块可对齐证据：
- 目标项目：`os/src/ext4fs/ext4inode.rs#add_extent_entry`，调用摘要：(*p).offset_from, (current_data_ptr as *mut Ext4ExtentHeader).write_unaligned, Ext4ExtentIndex::mut_from_bytes, Ext4ExtentIndex::mut_from_bytes(
                            unsafe { core::slice::from_raw_parts_mut(aim_ptr, 12) }
                        ).unwrap, Ext4ExtentLeaf::mut_from_bytes, Ext4ExtentLeaf::mut_from_bytes(
                            unsafe { core::slice::from_raw_parts_mut(ap, 12) }
                        ).unwrap ...(+74)
- 目标项目：`os/src/ext4fs/ext4inode.rs#add_extent_in_tree`，调用摘要：(*p).offset_from, Ext4ExtentIndex::mut_from_bytes, Ext4ExtentIndex::mut_from_bytes(
                            unsafe { core::slice::from_raw_parts_mut(aim_ptr, 12) }
                        ).unwrap, Ext4ExtentLeaf::mut_from_bytes, Ext4ExtentLeaf::mut_from_bytes(
                            unsafe { core::slice::from_raw_parts_mut(ap, 12) }
                        ).unwrap, Some ...(+42)
- 目标项目：`os/src/ext4fs/ext4inode.rs#truncate`，调用摘要：alloc::vec::Vec::new, blocks_to_free.push, buf.as_mut_ptr, buf.as_mut_ptr().add, disk_inode
                        .i_blocks_lo
                        .saturating_sub, disk_inode.i_block.as_mut_ptr ...(+24)
- 目标项目：`os/src/ext4fs/ext4inode.rs#raw_write_at`，调用摘要：alloc::vec!, block_modify_inode, buf.len, core::cmp::min, disk_inode.i_block[base..base + 4].copy_from_slice, error! ...(+13)

#### 通用模块/其他
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `user/src/lib.rs#_start` | `user/src/lib.rs#_start` | 同板块同名 | ((argv + i * core::mem::size_of::<usize>()) as *const usize).read_volatile, ((str_start + *i) as *const u8).read_volatile, (0usize..)
            .find, (0usize..)
            .find(|i| unsafe { ((str_start + *i) as *const u8).read_volatile() == 0 })
            .unwrap, HEAP.lock, HEAP.lock()
            .init ...(+11) | ((argv + i * core::mem::size_of::<usize>()) as *const usize).read_volatile, ((str_start + *i) as *const u8).read_volatile, (0usize..)
            .find, (0usize..)
            .find(|i| unsafe { ((str_start + *i) as *const u8).read_volatile() == 0 })
            .unwrap, HEAP.lock, HEAP.lock()
            .init ...(+10) |
| `os/src/arch/la/timer.rs#set_next_trigger` | `os/src/timer.rs#set_next_trigger` | 同板块同名 | asm!, ticks.max, time_slice_ms_for_policy, timer_frequency, timer_frequency()
        .saturating_mul | get_time, set_timer |
| `os/src/ipc/msg.rs#add` | `os/src/timer.rs#add` | 同板块同名 | EAGAIN.as_isize, Err, Ok, msg.get_size, self.msgs.push_back, self.update_msqds | 内部闭环逻辑 |
| `user/src/console.rs#write_str` | `os/src/console.rs#write_str` | 同板块同名 | Err, Ok, s.as_bytes, s.as_bytes().iter, self.0.len, self.0.push_back ...(+1) | Ok, console_putchar, s.chars |


好的，合议庭。本庭已收到控辩双方的全部陈述、证据底座及原始 AST 抽样数据，现依据审计指令，对本案进行最终同源性定性仲裁。

### 1. 宏观判定
- **定性结论**: **独立开发**
- **判定依据**: 双方项目客观 AST 相似度仅为 **6.17%**。目标项目独有 AST 指纹高达 1221，而共同指纹仅 97。目标项目对参考项目的覆盖率仅为 7.94%，远低于判定实质性相似的阈值。该宏观数据强有力地证明，目标项目在整体架构和代码实现上具有压倒性比例的独立开发成果，不存在对参考项目的整体性、结构性抄袭。

### 2. 全模块覆盖矩阵解读
本庭对全模块覆盖矩阵进行逐项审查，以评估各模块的证据强度：

- **进程管理**: 双方均存在。存在共同 AST 指纹，但目标项目覆盖率仅 4.2%。控方指控的“结构洗稿”主要集中于此。经查，双方在调度器架构（`current_processor` vs `PROCESSORS[hartid()]`）、进程模型（TCB/PCB 分离 vs 单一 TCB）等核心实现上存在根本性 AST 差异。证据强度：**弱**。
- **任务调度/异步运行时**: 双方均存在。目标项目覆盖率 3.49%。共同指纹主要源于同名的系统调用接口（如 `sys_writev`），但其内部调用流（如目标项目复杂的 `iovec` 处理逻辑）与参考项目差异显著。证据强度：**弱**。
- **内存管理**: 双方均存在。目标项目覆盖率 11.93%。共同指纹多集中在 `new_kernel` 等初始化函数，其调用链相似源于对硬件内存布局（如 `LOWRAM_BASE`, `ebss`）的共同依赖，属于硬件 ABI 约束。在更核心的 `sys_brk` 实现上，目标项目使用 `mmap::do_brk`，参考项目使用 `insert_framed_area`，AST 结构完全不同。证据强度：**弱**。
- **中断/异常处理**: 双方均存在。目标项目覆盖率 18.18%，但该模块函数总数极少（13 vs 5），覆盖率波动大。共同指纹 `set_kernel_trap_entry` 等均为对 RISC-V/LoongArch 特权架构寄存器的标准操作（`asm!` 或 `stvec::write`），属于硬件规范决定的必然写法。证据强度：**弱**。
- **系统调用**: 双方均存在。目标项目覆盖率 5.95%。共同指纹为用户态透传函数（如 `syscall`, `sigaction`），其实现是操作系统与用户程序之间的标准 ABI 约定，函数体极简，AST 必然趋同。证据强度：**弱**。
- **文件系统**: 双方均存在。目标项目覆盖率 5.94%。共同指纹如 `open_file`，其调用流均遵循“查找目录项 -> 创建/打开 inode”的标准 VFS 流程，但目标项目后续集成了庞大的 `ext4fs` 模块，参考项目则无。证据强度：**弱**。
- **设备驱动**: 双方均存在。目标项目覆盖率 1.89%。共同指纹 `get_block_cache` 等是块设备缓存的通用设计模式，且目标项目独有 LoongArch 平台驱动。证据强度：**弱**。
- **网络协议栈**: **仅目标项目体现**。参考项目无此模块。目标项目实现了完整的基于 `smoltcp` 的 socket 层（`os/src/net/socket.rs`）。证据强度：**未形成**。
- **第三方库/公共依赖**: **仅目标项目体现**。参考项目无此模块。目标项目集成了 `ext4fs`、`fat32` 等多个复杂第三方/自研库。证据强度：**未形成**。

### 3. 底层 AST 关键证据指纹表
本庭提取双方可对齐的关键证据指纹，并按模块优先级排序。所有证据均被判定为弱信号，理由详见技术说明。

| 所属板块 | 目标项目路径 | 参考项目路径 | 证据类型 | 技术说明 |
| :--- | :--- | :--- | :--- | :--- |
| 进程管理 | `os/src/process/processor.rs#run_tasks` | `os/src/task/processor.rs#run_tasks` | 弱信号 | 函数同名，但核心调度逻辑 AST 异构。目标项目使用 `MAIN_HART_ID.load` 和 `current_processor`，参考项目使用 `PROCESSORS[hartid()].lock`，表明多核调度架构完全不同。 |
| 进程管理 | `os/src/process/processor.rs#current_trap_cx` | `os/src/task/processor.rs#current_trap_cx` | 弱信号 | AST 调用链 `current_task().unwrap().inner_exclusive_access().get_trap_cx` 一致。此为获取当前任务 Trap 上下文的 Rust 标准惯用法，由 `Option<Arc<T>>` 和内部可变性模式唯一确定，非独创性逻辑。 |
| 进程管理 | `os/src/syscall/process.rs#sys_fork` | `os/src/syscall/process.rs#sys_fork` | 弱信号 | 函数同名，但 AST 调用链存在显著差异。目标项目调用 `add_process` 和 `copy_fd_table_between_processes`，体现了进程-线程分离模型；参考项目调用 `current_task.fork`，为单一任务模型。 |
| 内存管理 | `os/src/syscall/process.rs#sys_brk` | `os/src/syscall/process.rs#sys_brk` | 弱信号 | 函数同名，但底层内存管理 AST 完全不同。目标项目调用 `mmap::do_brk`，参考项目直接操作 `memory_set.insert_framed_area`，表明内存管理子系统实现路径异构。 |
| 内存管理 | `os/src/mm/memory_set.rs#new_kernel` | `os/src/mm/memory_set.rs#new_kernel` | 弱信号 | 调用链相似，均包含 `(ebss as usize).into()` 等地址转换。这是对内核链接脚本定义的符号（`ebss`）进行页表映射的标准操作，由硬件启动规范和内存布局决定，非代码复制。 |
| 中断/异常处理 | `os/src/arch/riscv/trap/mod.rs#set_user_trap_entry` | `os/src/trap/mod.rs#set_user_trap_entry` | 弱信号 | 均调用 `stvec::write`。这是设置 RISC-V 用户态陷阱入口地址的唯一标准 CSR 操作，由 RISC-V 特权指令集规范强制定义，无独立实现空间。 |
| 系统调用 | `user/src/syscall.rs#syscall` | `user/src/syscall.rs#syscall` | 弱信号 | 均使用内联汇编 `asm!` 执行 `ecall` 指令。这是 RISC-V 架构下发起系统调用的唯一硬件 ABI，AST 必然一致。 |
| 文件系统 | `os/src/fs/inode.rs#open_file` | `os/src/fs/inode.rs#open_file` | 弱信号 | 函数同名，调用流均遵循“父目录查找 -> 目录项创建/查找 -> inode 分配”的通用 VFS 模式。但目标项目后续支持 ext4 等复杂文件系统，参考项目无。 |
| 设备驱动 | `os/src/drivers/block/block_cache.rs#get_block_cache` | `fat32/src/block_cache.rs#get_block_cache` | 弱信号 | 函数同名，均实现“检查缓存 -> 未命中则分配新块”的通用块缓存逻辑。此设计模式在操作系统开发中极为普遍，且目标项目使用 `Mutex`，参考项目使用 `RwLock`，同步策略不同。 |

### 4. 核心代码调用流对比剖析
本庭选取双方均存在的核心模块，进行底层技术析理，以论证其架构异构性。

* **【进程管理/调度器架构】**
* **底层技术析理**: 双方 `run_tasks` 函数虽同名，但调度器的核心对象管理模型存在根本性分歧。目标项目采用 `current_processor` 这一线程局部结构，并通过 `MAIN_HART_ID.load` 获取当前核心 ID，体现了为多核环境设计的、去中心化的调度器架构。参考项目则使用全局数组 `PROCESSORS[hartid()].lock`，这是一种中心化的、基于数组索引和全局锁的调度模型。两者的并发控制粒度和数据流 AST 结构完全不同，表明调度器是独立设计实现。
* **代码溯源链接**:
  - 目标项目: `os/src/process/processor.rs#run_tasks`
  - 参考项目: `os/src/task/processor.rs#run_tasks`

* **【进程管理/进程模型与 fork 实现】**
* **底层技术析理**: `sys_fork` 的实现揭示了双方根本性的进程模型差异。目标项目的调用流包含 `add_process` 和 `copy_fd_table_between_processes`，明确展示了其 `ProcessControlBlock` (PCB) 与 `TaskControlBlock` (TCB) 分离的架构，进程（地址空间、文件描述符表）与线程（调度实体、Trap 上下文）是独立管理的。参考项目的调用流 `current_task.fork` 则是在单一 `TaskControlBlock` 上同时复制调度状态和资源句柄。这是操作系统内核最核心的设计决策之一，其 AST 差异是架构级独立演进的铁证。
* **代码溯源链接**:
  - 目标项目: `os/src/syscall/process.rs#sys_fork`
  - 参考项目: `os/src/syscall/process.rs#sys_fork`

* **【内存管理/堆管理接口】**
* **底层技术析理**: `sys_brk` 系统调用是用户态堆管理的核心接口。目标项目将其实现委托给 `mmap::do_brk`，这表明其进程地址空间管理是基于 `mmap` 区域这一更现代、更灵活的统一抽象。参考项目则直接调用 `memory_set.insert_framed_area`，暴露了其更早期、更直接基于物理页帧映射的内存管理模型。两者在抽象层次和内存管理策略上的选择截然不同，AST 调用链无重合。
* **代码溯源链接**:
  - 目标项目: `os/src/syscall/process.rs#sys_brk`
  - 参考项目: `os/src/syscall/process.rs#sys_brk`

### 5. 单方模块与降权项
以下模块或证据类型因仅存在于一方或属于公共接口，本庭将其降权，不作为同源性强证据：
- **网络协议栈**: 目标项目独有的 `os/src/net/socket.rs` 等模块，实现了基于 `smoltcp` 的完整网络功能。参考项目无此模块，是目标项目独立开发的强有力证据。
- **第三方库/公共依赖**: 目标项目独有的 `os/src/ext4fs/` 目录，实现了复杂的 Ext4 文件系统。参考项目无此模块，是目标项目独立开发的强有力证据。
- **公共接口与惯用法**: 控辩双方提及的 `sys_yield`、`sigprocmask`、`getpid`、`current`、`take_current` 等函数，其实现多为单语句透传或 Rust 标准库调用（`Option::take`、`Option::map`）。这些是操作系统 ABI 定义或 Rust 语言的标准惯用法，其 AST 结构由外部规范或语言语义唯一确定，不能作为代码抄袭的证据，本庭予以降权处理。

### 6. 保守结论
综合全案证据，本庭裁定目标项目 [2026-T202610008999575-XX] 与参考项目 [2022-S0S-哈尔滨工业大学] 之间**不存在实质性代码复用或结构性抄袭**。6.17% 的宏观 AST 相似度是决定性指标，它远低于抄袭阈值。控方提出的“结构洗稿”指控，其证据基础是操作系统内核开发中普遍存在的、由硬件 ABI、Rust 语言惯用法和公开教学 OS 设计模式所决定的极简函数或标准调用链。这些“同源指纹”在技术上不具备独创性，无法指向抄袭。相反，双方在进程模型（TCB/PCB 分离）、调度器架构（去中心化 vs 全局锁）、内存管理抽象（mmap vs 直接页帧映射）等核心子系统上，展现出根本性的 AST 结构差异。加之目标项目存在大量参考项目所没有的独立模块（网络协议栈、ext4 文件系统），足以认定其为独立架构演进成果。整体抄袭风险极低。

------------------------------

