# 🛡️ OS 内核赛自动化代码级架构审计报告

**审计时间**: `2026-07-02 02:49:58`
**参赛作品名称**: `2026-T202610008999575-XX`
**年份**: `2026`
**学校**: `XX`
**队伍名称**: `T202610008999575`
**比赛名字**: `OS-Competition`
**比赛子赛道**: `Kernel-Implementation`
**仓库地址**: `https://gitlab.eduxiji.net/educg-group-43501-3132633/T202610008999575-3763`
**推测开发语言**: `Rust / C (由AST探针自动识别)`
**特征函数量**: `1658`

## ⚔️ 底层代码同源性对比 (AST Homology)
> 本部分由专门的代码审查智能体，对该项目与符合年份条件的历届最高相似度项目进行控制流与底层调用对比。新版报告会先给出确定性的全模块覆盖矩阵，再给出 Agent 的分模块解释，避免只围绕少量抽样函数展开。

### 🏆 Top 5 底层对标: vs `2024-NPUcore-IMPACT-西北工业大学`（西北工业大学 / NPUcore-IMPACT!!!）

#### 对方作品信息

**对方年份**: `2024`
**对方学校**: `西北工业大学`
**对方队伍名称**: `NPUcore-IMPACT!!!`
**对方比赛名字**: `OS-Competition`
**对方比赛子赛道**: `Kernel-Implementation`
**对方仓库地址**: `https://gitlab.eduxiji.net/educg-group-26011-2376549/T202410699992496-312`

> 双向覆盖率：共同 AST 指纹: 69；唯一 AST 指纹: 目标项目 1221，参考项目 642；覆盖率: 目标项目 5.65%，参考项目 10.75%

## 全模块对比总览（确定性证据）

> 总体覆盖率：共同 AST 指纹: 69；唯一 AST 指纹: 目标项目 1221，参考项目 642；覆盖率: 目标项目 5.65%，参考项目 10.75%

### 1. 模块覆盖矩阵
| 模块 | 目标函数数 | 参考函数数 | 共同唯一 AST 指纹 | Target 覆盖 | Base 覆盖 | 状态 |
| :--- | ---: | ---: | ---: | ---: | ---: | :--- |
| 进程管理 | 362 | 69 | 9 | 3.44% | 14.06% | 双方存在且有结构重合 |
| 任务调度/异步运行时 | 105 | 123 | 2 | 2.33% | 1.8% | 双方存在且有结构重合 |
| 内存管理 | 381 | 259 | 33 | 9.38% | 14.41% | 双方存在且有结构重合 |
| 中断/异常处理 | 13 | 1 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |
| 系统调用 | 123 | 29 | 8 | 9.52% | 40.0% | 双方存在且有结构重合 |
| 文件系统 | 381 | 274 | 4 | 1.83% | 2.44% | 双方存在且有结构重合 |
| 设备驱动 | 56 | 22 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |
| 网络协议栈 | 47 | 0 | 0 | 0.0% | 0.0% | 仅目标项目体现 |
| 文件系统/第三方库 | 69 | 31 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |
| 通用模块/其他 | 121 | 50 | 4 | 3.64% | 11.43% | 双方存在且有结构重合 |

### 2. 分模块证据清单

#### 进程管理
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `os/src/process/processor.rs#run_tasks` | `os/src/task/processor.rs#run_tasks` | 同板块同名 | MAIN_HART_ID.load, Some, __switch, asm!, crate::arch::timer::set_next_trigger, crate::task::add_task_into_pool_unlocked ...(+15) | PROCESSOR.lock, Some, __switch, do_wake_expired, drop, fetch_task ...(+2) |
| `os/src/syscall/process.rs#sys_kill` | `os/src/syscall/process.rs#sys_kill` | 同板块同名 | EINVAL.as_isize, ESRCH.as_isize, SignalFlags::from_bits, Some, Vec::new, crate::process::wake_up_task ...(+24) | Signals::from_signum, drop, find_task_by_tgid, inner.add_signal, signal.is_empty, task.acquire_inner_lock ...(+2) |
| `os/src/process/mod.rs#exit_current_and_run_next` | `os/src/task/mod.rs#exit_current_and_run_next` | 同板块同名 | Arc::downgrade, INITTASK.process, Some, TaskContext::zero_init, child.inner_exclusive_access, crate::arch::mm::flush_tlb_for_asid ...(+36) | TaskContext::zero_init, do_exit, panic!, schedule, take_current_task, take_current_task().unwrap |
| `os/src/syscall/process.rs#sys_clone` | `os/src/syscall/process.rs#sys_clone` | 同板块同名 | (stack != 0).then_some, EFAULT.as_isize, EINVAL.as_isize, add_task, current_proc.clone_thread, current_task ...(+11) | CloneFlags::from_bits, CloneFlags::from_bits(flags & !0xff).unwrap, Signals::empty, Signals::from_signum, add_task, child.acquire_inner_lock ...(+9) |

#### 任务调度/异步运行时
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `os/src/syscall/net.rs#sys_recvfrom` | `os/src/syscall/socket.rs#sys_recvfrom` | 同板块同名 | (user_len as usize).min, 2u16.to_ne_bytes, EFAULT.as_isize, Errno::EAGAIN.as_isize, Errno::EBADF.as_isize, Errno::EFAULT.as_isize ...(+51) | copy_to_user, copy_to_user(token, src.as_ptr(), buf).unwrap, current_task, current_task().unwrap, src.as_ptr, task.get_user_token |
| `os/src/syscall/fs.rs#sys_writev` | `os/src/syscall/fs.rs#sys_writev` | 同板块同名 | (iovec.len - written_in_iov).min, EACCES.as_isize, EAGAIN.as_isize, EBADF.as_isize, EFAULT.as_isize, EINVAL.as_isize ...(+26) | UserBuffer::new, Vec::<IOVec>::with_capacity, Vec::with_capacity, copy_from_user_array, copy_from_user_array(token, iov as *const IOVec, iovecs.as_mut_ptr(), iovcnt).is_err, current_task ...(+11) |
| `os/src/syscall/fs.rs#sys_unlinkat` | `os/src/syscall/fs.rs#sys_unlinkat` | 同板块同名 | EACCES.as_isize, EBADF.as_isize, EFAULT.as_isize, EINVAL.as_isize, EISDIR.as_isize, ENAMETOOLONG.as_isize ...(+33) | UnlinkatFlags::from_bits, current_task, current_task().unwrap, fd_table.get_ref, file_descriptor.clone, file_descriptor.delete ...(+9) |
| `os/src/syscall/fs.rs#sys_statx` | `os/src/syscall/fs.rs#sys_statx` | 同板块同名 | EBADF.as_isize, EFAULT.as_isize, EINVAL.as_isize, ENOENT.as_isize, TIME_CACHE.lock, TIME_CACHE.lock().get ...(+16) | copy_to_user_debug, copy_to_user_debug(token, statx, buf as *mut Statx).is_err, current_task, current_task().unwrap, current_user_token, fd_table.get_ref ...(+10) |

#### 内存管理
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `os/src/syscall/process.rs#sys_wait4` | `os/src/syscall/process.rs#sys_wait4` | 同板块同名 | ECHILD.as_isize, EFAULT.as_isize, ESRCH.as_isize, Some, child.getpid, child.inner_exclusive_access ...(+16) | WaitOption::from_bits, WaitOption::from_bits(option).unwrap, assert_eq!, block_current_and_run_next, child.acquire_inner_lock, child.getpid ...(+23) |
| `os/src/mm/memory_set.rs#mmap` | `os/src/mm/memory_set.rs#mmap` | 同板块同名 | BTreeMap::new, Err, Errno::EEXIST.as_isize, Errno::ENOMEM.as_isize, MapPermission::empty, Ok ...(+38) | MapArea::new, Some, USR_MMAP_BASE.into, VirtAddr::from, VirtAddr::from(USR_MMAP_END).into, area.expand_to::<T> ...(+28) |
| `os/src/mm/memory_set.rs#new_kernel` | `os/src/mm/memory_set.rs#new_kernel` | 同板块同名 | ((*pair).0 + (*pair).1).into, (*pair).0.into, (LOWRAM_BASE).into, (LOWRAM_END).into, (MEMORY_END - 4096).into, (ebss as *const () as usize).into ...(+16) | Self::new_bare_kern, anonymous_identical_map!, memory_set.map_trampoline, println!, should_map_trampoline! |
| `os/src/mm/memory_set.rs#mprotect` | `os/src/mm/memory_set.rs#mprotect` | 同板块同名 | Err, Errno::EACCES.as_isize, Errno::EINVAL.as_isize, Errno::ENOMEM.as_isize, MapPermission::empty, Ok ...(+31) | Err, MapPermission::from_bits, MapPermission::from_bits(((prot as u8) << 1) | (1 << 4)).unwrap, Ok, VirtAddr::from, area.get_end::<T> ...(+18) |

#### 中断/异常处理
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `os/src/arch/la/trap/mod.rs#decode_estat` | `os/src/fs/dev/tty.rs#default` | 同模块上下文对齐/差异观察 | 内部闭环逻辑 | 内部闭环逻辑 |

#### 系统调用
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `user/src/syscall.rs#syscall` | `user/src/syscall.rs#syscall` | 同板块同名 | core::arch::asm! | __syscall |
| `user/src/lib.rs#waitpid` | `user/src/usr_call.rs#waitpid` | 同板块同名 | sys_waitpid, sys_yield | sys_waitpid |
| `user/src/lib.rs#wait` | `user/src/usr_call.rs#wait` | 同板块同名 | sys_waitpid, sys_yield | sys_waitpid |
| `user/src/lib.rs#get_time` | `user/src/usr_call.rs#get_time` | 同板块同名 | TimeVal::new, sys_get_time | sys_get_time |

#### 文件系统
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `os/src/fs/inode.rs#lseek` | `os/src/fs/mod.rs#lseek` | 同板块同名 | self.inner.lock, self.inode.get_size | self.file.lseek |
| `os/src/fs/devfs.rs#get_statx` | `os/src/fs/dev/tty.rs#get_statx` | 同板块同名 | Default::default, self.get_stat | StatMode::S_IFCHR.bits, Statx::new, crate::makedev! |
| `os/src/lang_items.rs#panic` | `os/src/lang_items.rs#panic` | 同板块同名 | PANICKING.swap, crate::ext4fs::block_cache_sync_all, info.location, println!, shutdown, spin_loop | info.location, println!, shutdown |
| `os/src/fs/devfs.rs#get_stat` | `os/src/fs/dev/tty.rs#get_stat` | 同板块同名 | 内部闭环逻辑 | Stat::new, StatMode::S_IFCHR.bits, crate::makedev! |

#### 设备驱动
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `os/src/arch/riscv/drivers/block/mod.rs#block_device_test` | `os/src/drivers/block/mod.rs#block_device_test` | 同板块同名 | BLOCK_DEVICE.clone, NET_DEVICE.0.exclusive_access, assert_eq!, block_device.read_block, block_device.write_block, println! ...(+1) | BLOCK_DEVICE.clone, assert_eq!, block_device.read_block, block_device.write_block, println!, write_buffer.iter_mut |
| `os/src/drivers/block/mod.rs#read_block` | `os/src/drivers/block/sata_blk.rs#read_block` | 同板块同名 | BLOCK_DEVICE.clone, block.get_ref, buf.copy_from_slice, buf.len, cache.lock, get_block_cache | Ok, buf.chunks_mut, self.0
                .lock, self.0
                .lock()
                .read_block |
| `os/src/drivers/block/mod.rs#write_block` | `os/src/drivers/block/sata_blk.rs#write_block` | 同板块同名 | BLOCK_DEVICE.clone, block_data[..len].copy_from_slice, buf.len, cache.lock, cache.lock().modify, get_block_cache | Ok, buf.chunks, self.0
                .lock, self.0
                .lock()
                .write_block |
| `os/src/drivers/loopdev.rs#read_block` | `os/src/drivers/block/sata_blk.rs#read_block` | 同板块同名 | self.inner.exclusive_access, self.inner.exclusive_access().read_at | buf.chunks_mut, self.0
                .lock, self.0
                .lock()
                .read_block |

#### 网络协议栈
仅目标项目中观察到该模块代表函数，参考项目没有同模块可对齐证据：
- 目标项目：`os/src/net/socket.rs#new`，调用摘要：Arc::new, IpProtocol::from, Mutex::new, RawPacketBuffer::new, RawSocketSmol::new, SOCKET_SET.exclusive_access ...(+9)
- 目标项目：`os/src/net/socket.rs#recvfrom`，调用摘要：Some, buf.len, buf[..copy_len].copy_from_slice, crate::net::SOCKET_SET.exclusive_access, data.len, smoltcp::wire::IpAddress::v4 ...(+4)
- 目标项目：`os/src/net/socket.rs#readable`，调用摘要：crate::net::SOCKET_SET.exclusive_access, matches!, self.is_listener.load, socket.can_recv, socket.may_recv, socket.state ...(+1)
- 目标项目：`os/src/net/socket.rs#pair`，调用摘要：Arc::downgrade, Arc::new, Mutex::new, Some, VecDeque::new, left.lock ...(+1)

#### 文件系统/第三方库
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `os/src/ext4fs/vfs.rs#get_statx` | `os/src/fs/ext4_file.rs#get_statx` | 同板块同名 | Default::default, disk_inode.size, self.fs.get_disk_inode | todo! |
| `os/src/ext4fs/vfs.rs#get_stat` | `os/src/fs/ext4_file.rs#get_stat` | 同板块同名 | Default::default, disk_inode.size, self.fs.get_disk_inode | todo! |
| `os/src/ext4fs/vfs.rs#get_size` | `os/src/fs/ext4_file.rs#get_size` | 同板块同名 | self.size.load | todo! |
| `os/src/ext4fs/ext4inode.rs#add_extent_entry` | `os/src/mm/mod.rs#ext4_user_malloc` | 同模块上下文对齐/差异观察 | (*p).offset_from, (current_data_ptr as *mut Ext4ExtentHeader).write_unaligned, Ext4ExtentIndex::mut_from_bytes, Ext4ExtentIndex::mut_from_bytes(
                            unsafe { core::slice::from_raw_parts_mut(aim_ptr, 12) }
                        ).unwrap, Ext4ExtentLeaf::mut_from_bytes, Ext4ExtentLeaf::mut_from_bytes(
                            unsafe { core::slice::from_raw_parts_mut(ap, 12) }
                        ).unwrap ...(+74) | HEAP_ALLOCATOR
        .lock, HEAP_ALLOCATOR
        .lock()
        .alloc, HEAP_ALLOCATOR
        .lock()
        .alloc(Layout::array::<u8>(size).unwrap())
        .unwrap, HEAP_ALLOCATOR
        .lock()
        .alloc(Layout::array::<u8>(size).unwrap())
        .unwrap()
        .as_ptr, Layout::array::<u8>, Layout::array::<u8>(size).unwrap |

#### 通用模块/其他
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `user/src/lib.rs#_start` | `user/src/lib.rs#_start` | 同板块同名 | ((argv + i * core::mem::size_of::<usize>()) as *const usize).read_volatile, ((str_start + *i) as *const u8).read_volatile, (0usize..)
            .find, (0usize..)
            .find(|i| unsafe { ((str_start + *i) as *const u8).read_volatile() == 0 })
            .unwrap, HEAP.lock, HEAP.lock()
            .init ...(+11) | _parameter, asm!, unreachable! |
| `os/src/ipc/msg.rs#add` | `os/src/timer.rs#add` | 同板块同名 | EAGAIN.as_isize, Err, Ok, msg.get_size, self.msgs.push_back, self.update_msqds | 内部闭环逻辑 |
| `user/src/console.rs#write_str` | `os/src/console.rs#write_str` | 同板块同名 | Err, Ok, s.as_bytes, s.as_bytes().iter, self.0.len, self.0.push_back ...(+1) | Ok, console_flush, console_putchar, s.chars |
| `user/src/console.rs#print` | `os/src/console.rs#print` | 同板块同名 | CONSOLE_BUFFER.lock, buf.write_fmt | KernelOutput.write_fmt, KernelOutput.write_fmt(args).unwrap |


#### 1. 宏观判定
- **定性结论**: 独立开发 (相似度: 3.85%，共同 AST 指纹: 69；唯一 AST 指纹: 目标项目 1221，参考项目 642；覆盖率: 目标项目 5.65%，参考项目 10.75%)

#### 2. 全模块覆盖矩阵解读

- **进程管理**
  1. 双方均存在该模块。
  2. 存在 9 个共同 AST 指纹。
  3. 目标覆盖率 3.44%，参考覆盖率 14.06%。覆盖率极低，且差异显著，表明目标项目在该模块有大量独立代码，而参考项目因体量较小导致覆盖率相对较高。
  4. 证据强度：**弱**。共同指纹多为 POSIX 标准系统调用名及 Rust 语言惯用法（如 `Option::take`），核心调度架构（多 Hart vs 全局锁）完全不同。

- **任务调度/异步运行时**
  1. 双方均存在该模块。
  2. 存在 2 个共同 AST 指纹。
  3. 目标覆盖率 2.33%，参考覆盖率 1.8%。双向覆盖率均极低。
  4. 证据强度：**未形成**。共同指纹源于 `sys_recvfrom` 和 `sys_writev` 等系统调用，其内部实现路径差异巨大，无实质性结构重合。

- **内存管理**
  1. 双方均存在该模块。
  2. 存在 33 个共同 AST 指纹，为所有模块中最多。
  3. 目标覆盖率 9.38%，参考覆盖率 14.41%。覆盖率处于低水平。
  4. 证据强度：**弱**。共同指纹集中在 `mmap`、`mprotect`、`new_kernel` 等通用内存管理接口。但具体实现（如目标项目使用 `try_translated_read/write`，参考项目使用 `copy_from/to_user`）和内部数据结构（如内存区域管理方式）存在显著差异。

- **中断/异常处理**
  1. 双方均存在该模块。
  2. 共同 AST 指纹为 0。
  3. 目标覆盖率 0.0%，参考覆盖率 0.0%。
  4. 证据强度：**未形成**。双方在该模块无任何可关联的代码结构。

- **系统调用**
  1. 双方均存在该模块。
  2. 存在 8 个共同 AST 指纹。
  3. 目标覆盖率 9.52%，参考覆盖率 40.0%。参考项目覆盖率畸高是因为其系统调用层函数总数极少（29个），少量同名即可推高比例。
  4. 证据强度：**弱**。共同指纹均为用户态库的 `syscall` 封装或 `waitpid`、`get_time` 等标准 POSIX 接口，其内核态实现路径完全不同。

- **文件系统**
  1. 双方均存在该模块。
  2. 存在 4 个共同 AST 指纹。
  3. 目标覆盖率 1.83%，参考覆盖率 2.44%。覆盖率极低。
  4. 证据强度：**未形成**。共同指纹如 `lseek`、`get_statx` 为通用文件操作接口，但底层文件系统实现（目标项目含完整 `ext4fs`，参考项目为简单 `ext4_file`）差异巨大。

- **设备驱动**
  1. 双方均存在该模块。
  2. 共同 AST 指纹为 0。
  3. 目标覆盖率 0.0%，参考覆盖率 0.0%。
  4. 证据强度：**未形成**。双方在该模块无任何可关联的代码结构。

- **网络协议栈**
  1. 仅目标项目存在（47个函数），参考项目无此模块。
  2. 不存在共同 AST 指纹。
  3. 覆盖率无意义。
  4. 证据强度：**未形成**。此为单方模块，是目标项目独立开发的有力证据。

- **第三方库/公共依赖**
  1. 双方均存在该模块。
  2. 共同 AST 指纹为 0。
  3. 目标覆盖率 0.0%，参考覆盖率 0.0%。
  4. 证据强度：**未形成**。双方在该模块无任何可关联的代码结构。

#### 3. 底层 AST 关键证据指纹表

| 所属板块 | 目标项目路径 | 参考项目路径 | 证据类型 | 技术说明 |
| :--- | :--- | :--- | :--- | :--- |
| 进程管理 | `os/src/process/processor.rs#current` | `os/src/task/processor.rs#current` | 语言惯用法 | 均为 `Option::as_ref().map(...)` 的标准 Rust 调用模式，用于访问当前任务。 |
| 进程管理 | `os/src/process/processor.rs#take_current` | `os/src/task/processor.rs#take_current` | 语言惯用法 | 均为 `Option::take()` 的标准 Rust 调用模式，用于移出当前任务。 |
| 进程管理 | `os/src/process/processor.rs#get_idle_task_cx_ptr` | `os/src/task/processor.rs#get_idle_task_cx_ptr` | 通用逻辑 | 均为“内部闭环逻辑”，无外部依赖，是获取空闲任务上下文的通用函数。 |
| 进程管理 | `os/src/syscall/process.rs#sys_kill` | `os/src/syscall/process.rs#sys_kill` | 标准接口 | 同名 POSIX 系统调用，但内部实现完全不同（信号模型、任务查找方式）。 |
| 进程管理 | `os/src/syscall/process.rs#sys_clone` | `os/src/syscall/process.rs#sys_clone` | 标准接口 | 同名 POSIX 系统调用，但内部实现完全不同（线程模型、内存拷贝机制）。 |
| 内存管理 | `os/src/mm/memory_set.rs#mmap` | `os/src/mm/memory_set.rs#mmap` | 标准接口 | 同名 POSIX 接口，但内部实现完全不同（地址分配策略、权限检查机制）。 |
| 内存管理 | `os/src/mm/memory_set.rs#new_kernel` | `os/src/mm/memory_set.rs#new_kernel` | 通用逻辑 | 内核内存集初始化，但映射方式（目标项目手动映射段，参考项目使用宏）不同。 |
| 系统调用 | `user/src/syscall.rs#syscall` | `user/src/syscall.rs#syscall` | 标准接口 | 用户态系统调用入口，实现方式（目标项目用 `asm!`，参考项目用 `__syscall`）不同。 |

#### 4. 核心代码调用流对比剖析

* **【进程管理/调度器核心】**
* **底层技术析理**: 目标项目 `run_tasks` 采用多核（Hart）感知架构，通过 `current_processor` 获取当前处理器实例，并使用 `lock_dispatch`、`add_task_into_pool_unlocked` 等接口操作任务池，体现了面向 SMP 系统的调度设计。参考项目 `run_tasks` 则基于全局单例 `PROCESSOR.lock` 和 `fetch_task`，是典型的单核全局锁调度模型。两者在并发模型、任务队列管理和处理器抽象上存在根本性架构差异。
* **代码溯源链接**:
  - 目标项目: `os/src/process/processor.rs#run_tasks`
  - 参考项目: `os/src/task/processor.rs#run_tasks`

* **【内存管理/用户态内存访问】**
* **底层技术析理**: 在 `sys_writev` 系统调用中，目标项目使用 `try_translated_read` 和 `UserBuffer::new` 进行基于页表翻译的直接内存访问，这是一种零拷贝或依赖硬件 MMU 翻译的机制。参考项目则使用 `copy_from_user_array` 和 `UserBuffer::new` 进行显式的内核-用户态数据拷贝。两者在用户态内存访问策略上存在本质区别，反映了不同的安全与性能设计权衡。
* **代码溯源链接**:
  - 目标项目: `os/src/syscall/fs.rs#sys_writev`
  - 参考项目: `os/src/syscall/fs.rs#sys_writev`

* **【系统调用/进程创建】**
* **底层技术析理**: 在 `sys_clone` 实现中，目标项目通过 `current_proc.clone_thread` 方法创建新线程，体现了进程-线程分离的对象模型。参考项目则直接通过 `add_task` 创建新任务，并手动设置 `CloneFlags`，体现了统一的任务模型。两者在操作系统核心抽象（进程与线程的关系）上设计不同。
* **代码溯源链接**:
  - 目标项目: `os/src/syscall/process.rs#sys_clone`
  - 参考项目: `os/src/syscall/process.rs#sys_clone`

#### 5. 单方模块与降权项

- **单方模块**：**网络协议栈**（47个函数）仅存在于目标项目，是目标项目独立开发、功能超越参考项目的强有力证据，不能作为同源线索。
- **降权项**：
  - **POSIX 标准系统调用名**：`sys_kill`, `sys_clone`, `sys_exit`, `sys_nanosleep` 等大量同名函数，其名称由接口规范强制决定，不能作为代码复用的证据。
  - **Rust 语言惯用法**：`current` 函数中的 `self.current.as_ref().map(...)` 和 `take_current` 函数中的 `self.current.take()`，是 Rust 标准库 `Option<T>` 类型的必然用法，不具备独创性。
  - **通用 OS 概念名**：`run_tasks`, `schedule`, `current_task`, `exit_current_and_run_next` 等是操作系统领域的通用术语，任何 OS 项目都可能使用。
  - **跨板块同名**：如目标项目的 `os/src/ipc/msg.rs#add` 与参考项目的 `os/src/timer.rs#add`，虽函数名相同，但分属进程间通信和定时器两个完全不同的模块，不构成同源证据。

#### 6. 保守结论

综合评估，目标项目与参考项目的客观 AST 相似度仅为 3.85%，双向覆盖率均低于 11%，处于极低水平。所有观察到的共同指纹均可归因于 POSIX 标准接口名、Rust 语言标准库惯用法或操作系统通用概念，而非保护性代码表达的重合。在核心的进程管理、内存管理和系统调用模块，双方在并发模型（多核 vs 单核）、内存访问策略（页表翻译 vs 显式拷贝）和对象抽象（进程-线程分离 vs 统一任务）上存在根本性架构差异。加之目标项目拥有参考项目完全缺失的网络协议栈等独立模块，整体证据链强烈支持独立开发的结论，不存在实质性代码复用或抄袭风险。

------------------------------

