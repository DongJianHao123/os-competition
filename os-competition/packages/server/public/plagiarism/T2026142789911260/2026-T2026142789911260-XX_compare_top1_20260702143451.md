# 🛡️ OS 内核赛自动化代码级架构审计报告

**审计时间**: `2026-07-02 14:34:51`
**参赛作品名称**: `2026-T2026142789911260-XX`
**年份**: `2026`
**学校**: `XX`
**队伍名称**: `T2026142789911260`
**比赛名字**: `OS-Competition`
**比赛子赛道**: `Kernel-Implementation`
**仓库地址**: `https://gitlab.eduxiji.net/educg-group-43501-3132633/T2026142789911260-2999`
**推测开发语言**: `Rust / C (由AST探针自动识别)`
**特征函数量**: `90`

## ⚔️ 底层代码同源性对比 (AST Homology)
> 本部分由专门的代码审查智能体，对该项目与符合年份条件的历届最高相似度项目进行控制流与底层调用对比。新版报告会先给出确定性的全模块覆盖矩阵，再给出 Agent 的分模块解释，避免只围绕少量抽样函数展开。

### 🏆 Top 1 底层对标: vs `2023-Alien-北京理工大学`（北京理工大学 / Alien）

#### 对方作品信息

**对方年份**: `2023`
**对方学校**: `北京理工大学`
**对方队伍名称**: `Alien`
**对方比赛名字**: `OS-Competition`
**对方比赛子赛道**: `Kernel-Implementation`
**对方仓库地址**: `local-upload://Alien`

> 双向覆盖率：共同 AST 指纹: 4；唯一 AST 指纹: 目标项目 89，参考项目 1006；覆盖率: 目标项目 4.49%，参考项目 0.4%

## 全模块对比总览（确定性证据）

> 总体覆盖率：共同 AST 指纹: 4；唯一 AST 指纹: 目标项目 89，参考项目 1006；覆盖率: 目标项目 4.49%，参考项目 0.4%

### 1. 模块覆盖矩阵
| 模块 | 目标函数数 | 参考函数数 | 共同唯一 AST 指纹 | Target 覆盖 | Base 覆盖 | 状态 |
| :--- | ---: | ---: | ---: | ---: | ---: | :--- |
| 进程管理 | 1 | 70 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |
| 任务调度/异步运行时 | 8 | 274 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |
| 内存管理 | 25 | 137 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |
| 中断/异常处理 | 1 | 21 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |
| 系统调用 | 3 | 41 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |
| 文件系统 | 18 | 300 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |
| 设备驱动 | 3 | 115 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |
| 网络协议栈 | 0 | 21 | 0 | 0.0% | 0.0% | 仅参考项目体现 |
| 通用模块/其他 | 31 | 233 | 1 | 3.23% | 0.52% | 双方存在且有结构重合 |

### 2. 分模块证据清单

#### 进程管理
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `kernel/src/main.rs#current_process` | `kernel/src/fs/poll.rs#epoll_pwait` | 同模块上下文对齐/差异观察 | kernel_core, kernel_core()
        .processes
        .get_mut, log!, panic! | Err, LinuxErrno::EINTR.into, Ok, Vec::new, Vec::with_capacity, current_task ...(+22) |

#### 任务调度/异步运行时
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `kernel/src/main.rs#schedule` | `kernel/src/task/schedule.rs#schedule` | 同板块同名 | self.processes.len, self.sched_in, trace!, unimplemented! | schedule_now, take_current_task, take_current_task().unwrap |
| `kernel/src/syscalls.rs#from_sys` | `kernel/src/fs/select.rs#pselect6` | 同模块上下文对齐/差异观察 | DirFD::from, SyscallOp::Unimpl, a0.into | Err, LinuxErrno::EBADF.into, LinuxErrno::EINTR.into, Ok, SimpleBitSet, SimpleBitSet(*mask).into ...(+31) |
| `kernel/src/main.rs#new` | `kernel/src/ipc/signal.rs#signal_handler` | 同模块上下文对齐/差异观察 | Fd::default, files.iter, from_fn, kernel_core, kernel_core().pipes[*i].readers.push, kernel_core().pipes[*i].writers.push ...(+16) | SigActionDefault::of_signal, SigInfo::default, SignalNumber::try_from, SignalNumber::try_from(signum as u8).unwrap, SignalUserContext::init, action.flags.contains ...(+26) |
| `kernel/src/syscalls.rs#syscall_read` | `kernel/src/ipc/mod.rs#futex` | 同模块上下文对齐/差异观察 | current_process, current_process().fd, current_process().set_state, data.len, dst[..data.len()].copy_from_slice, from_raw_parts_mut ...(+17) | Arc::new, AtomicI32::from_mut, FCOUNT.lock, FUTEX_WAITER.lock, FUTEX_WAITER.lock().add_waiter, FUTEX_WAITER.lock().requeue ...(+28) |

#### 内存管理
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `kernel/src/paging.rs#alloc` | `subsystems/mem/src/heap.rs#alloc` | 同板块同名 | Some, buf.fill, from_raw_parts_mut, log!, panic!, self.free_area.len ...(+4) | alloc_frames, layout.size, self.allocator.lock, self.allocator.lock().alloc, trace! |
| `kernel/src/paging.rs#dma_alloc` | `subsystems/drivers/src/hal.rs#dma_alloc` | 同板块同名 | MyPagingHandler::alloc_frames, MyPagingHandler::alloc_frames(pages, align).expect, MyPagingHandler::phys_to_virt, NonNull::new, NonNull::new(vaddr.as_usize() as *mut u8).unwrap, log! ...(+2) | NonNull::new, NonNull::new(start).unwrap, alloc_frames |
| `kernel/src/paging.rs#alloc_frames` | `subsystems/mem/src/frame.rs#alloc_frames` | 同板块同名 | Some, addr.into, assert!, kernel_core, kernel_core()
            .pager
            .as_mut, kernel_core()
            .pager
            .as_mut()
            .unwrap ...(+4) | FRAME_ALLOCATOR
        .lock, FRAME_ALLOCATOR
        .lock()
        .alloc_pages, FRAME_ALLOCATOR
        .lock()
        .alloc_pages(num, FRAME_SIZE)
        .expect |
| `kernel/src/paging.rs#dma_dealloc` | `subsystems/drivers/src/hal.rs#dma_dealloc` | 同板块同名 | MyPagingHandler::dealloc_frames, PhysAddr::from, log! | free_frames |

#### 中断/异常处理
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `kernel/src/main.rs#skip_ecall` | `user/musl/syscall_ebpf/syscall_ebpf/src/main.rs#main` | 同模块上下文对齐/差异观察 | 内部闭环逻辑 | HashMap::try_from, HashMap::try_from(ebpf.map("SYSCALL_LIST").unwrap()).unwrap, Ok, aya::Ebpf::load, aya::include_bytes_aligned!, aya_log::EbpfLogger::init ...(+23) |

#### 系统调用
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `kernel/src/syscalls.rs#do_iov` | `kernel/src/net/mod.rs#connect` | 同模块上下文对齐/差异观察 | core::slice::from_raw_parts, f | Err, LinuxErrno::EINPROGRESS.into, Ok, common_socket_syscall, do_suspend, e.into ...(+6) |
| `kernel/src/syscalls.rs#from` | `kernel/src/net/mod.rs#setsockopt` | 同模块上下文对齐/差异观察 | DirFD::Fd, panic! | Ok, SocketLevel::try_from, SocketLevel::try_from(level).map_err, SocketOption::try_from, SocketOption::try_from(opt_name).map_err, TcpSocketOption::try_from ...(+4) |
| `kernel/src/syscalls.rs#write_field` | `kernel/src/net/mod.rs#bind` | 同模块上下文对齐/差异观察 | bytes.len, dst.fill, dst[..bytes.len()].copy_from_slice, s.as_bytes | Err, Ok, common_socket_syscall, e.into, info!, socket.bind ...(+4) |

#### 文件系统
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `kernel/src/main.rs#panic_handler` | `subsystems/unwinder/src/panic.rs#panic_handler` | 同板块同名 | core::arch::asm!, info.location, info.message, info.message().as_str, loc.column, loc.file ...(+3) | Box::new, PanicGuard::new, RECURSION.swap, info.can_unwind, info.location, print_stack_trace ...(+4) |
| `kernel/src/fs.rs#current_time` | `subsystems/vfs/src/lib.rs#current_time` | 同板块同名 | Ext4Timestamp::new, Ok | DynFsKernelProvider::current_time |
| `kernel/src/fs.rs#read` | `user/apps/todo/src/main.rs#create_app` | 同模块上下文对齐/差异观察 | Ext4Error::io, Ok, block_id.raw, buf.len, buf[buf_offset..buf_offset + SECTOR_SIZE].copy_from_slice, read_sector ...(+2) | FilterModel::new, MainWindow::new, MainWindow::new().unwrap, Rc::new, Rc::new(FilterModel::new(window.get_todo_model(), |e| !e.checked)).into, Rc::new(SortModel::new(window.get_todo_model(), |lhs, rhs| {
                        lhs.title.to_lowercase().cmp(&rhs.title.to_lowercase())
                    }))
                    .into ...(+33) |
| `kernel/src/fs.rs#write` | `user/apps/sysinfo/src/main.rs#main` | 同模块上下文对齐/差异观察 | Err, Ext4Error::invalid_input, Ext4Error::io, Ok, block_id.raw, buf.len ...(+3) | "1".into, "2023.8.20".into, "Alien".into, "qemu".into, "riscv64".into, Box::new ...(+37) |

#### 设备驱动
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `kernel/src/paging.rs#mmio_phys_to_virt` | `subsystems/drivers/src/hal.rs#mmio_phys_to_virt` | 同板块同名 | MyPagingHandler::phys_to_virt, NonNull::new, NonNull::new(vaddr.as_usize() as *mut u8).unwrap, PhysAddr::from, vaddr.as_usize | NonNull::new, NonNull::new(paddr as *mut u8).unwrap |
| `kernel/src/paging.rs#unshare` | `subsystems/drivers/src/hal.rs#unshare` | 同板块同名 | 内部闭环逻辑 | 内部闭环逻辑 |
| `kernel/src/main.rs#sbi_getchar` | `subsystems/devices/src/lib.rs#init_virtio_mmio` | 同模块上下文对齐/差异观察 | uart.add, uart.add(5).read_volatile, uart.read_volatile | MmioTransport::new, NonNull::new, NonNull::new(paddr as *mut VirtIOHeader).unwrap, Some, info!, init_block_device ...(+5) |

#### 网络协议栈
仅参考项目中观察到该模块代表函数，目标项目没有同模块可对齐证据：
- 参考项目：`subsystems/knet/src/socket.rs#ready_read`，调用摘要：info!, panic!, tcp.poll, udp.poll, unix.ready_read
- 参考项目：`subsystems/knet/src/socket.rs#send_to`，调用摘要：dest_addr.get_socketaddr, neterror2alien, panic!, tcp.send, tcp.send(message).map_err, udp.send ...(+4)
- 参考项目：`subsystems/knet/src/socket.rs#ready_write`，调用摘要：panic!, tcp.poll, udp.poll
- 参考项目：`subsystems/knet/src/socket.rs#bind`，调用摘要：Ok, panic!, socket_addr.get_socketaddr, tcp.bind, tcp.bind(socket_addr.get_socketaddr())
                    .map_err, udp.bind ...(+1)

#### 通用模块/其他
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `kernel/src/main.rs#_start` | `user/userlib/src/lib.rs#_start` | 同板块同名 | core::arch::naked_asm! | naked_asm! |
| `kernel/src/main.rs#write_str` | `subsystems/platform/src/console.rs#write_str` | 同板块同名 | Ok, s.bytes, sbi_putchar | Ok, crate::console_putchar, s.as_bytes, s.as_bytes().iter, s.as_bytes().iter().for_each |
| `kernel/src/buftool.rs#pos` | `subsystems/gmanager/src/lib.rs#max` | 同板块同结构 | 内部闭环逻辑 | 内部闭环逻辑 |
| `kernel/src/main.rs#kmain` | `kernel/src/ebpf/mod.rs#bpf` | 同模块上下文对齐/差异观察 | log!, run, sbi_call, sbi_shutdown, trace! | BpfMapGetNextKeyArg::try_from, BpfMapGetNextKeyArg::try_from(attr).map_err, BpfMapUpdateArg::try_from, BpfMapUpdateArg::try_from(attr).map_err, Err, Ok ...(+18) |


好的，审计官。以下是基于客观 AST 证据与双方论点的最终同源性定性仲裁。

### 1. 宏观判定
- **定性结论**: **独立开发** (相似度: 0.37%，共同 AST 指纹: 4；唯一 AST 指纹: 目标项目 89，参考项目 1006；覆盖率: 目标项目 4.49%，参考项目 0.4%)
- **核心依据**: 客观 AST 相似度极低，仅为 0.37%。在操作系统内核的核心板块（进程管理、内存管理、任务调度、文件系统等），共同 AST 指纹数均为 0。双方在全局架构、内存模型、同步策略及外部依赖上存在根本性差异。控方指控主要基于领域通用命名和标准库惯用模式，不构成代码复用或架构抄袭的有效证据。

### 2. 全模块覆盖矩阵解读
| 模块 | 双方存在 | 共同 AST 指纹 | Target 覆盖 | Base 覆盖 | 证据强度 | 解读 |
| :--- | :---: | :---: | :---: | :---: | :---: | :--- |
| **进程管理** | 是 | 0 | 0.0% | 0.0% | **未形成** | 双方均有进程管理，但目标项目仅1个函数，参考项目有70个。无任何共同指纹，表明进程抽象、调度实体管理方式完全不同。 |
| **任务调度/异步运行时** | 是 | 0 | 0.0% | 0.0% | **弱** | 双方均有调度器，且存在同名函数 `schedule`。但 AST 指纹为0，调用链完全不同。目标项目基于 `processes` 数组，参考项目基于 `take_current_task`，调度模型异构。同名是领域通用命名所致。 |
| **内存管理** | 是 | 0 | 0.0% | 0.0% | **弱** | 这是控方指控的核心板块，但共同 AST 指纹为 0。双方均实现了 `alloc`、`alloc_frames` 等通用函数，但目标项目采用 Buddy 分配器，参考项目采用通用堆分配器，底层算法与数据结构不同。 |
| **中断/异常处理** | 是 | 0 | 0.0% | 0.0% | **未形成** | 双方均有该模块，但无任何共同指纹。目标项目仅1个函数，参考项目有21个，规模差异巨大，无证据表明存在复用。 |
| **系统调用** | 是 | 0 | 0.0% | 0.0% | **未形成** | 双方均有系统调用派发，但无共同指纹。目标项目派发至 `ext4` 文件操作，参考项目派发至 `socket` 网络操作，功能域完全不同。 |
| **文件系统** | 是 | 0 | 0.0% | 0.0% | **弱** | 双方均有文件系统，且存在同名函数 `current_time`、`panic_handler`。但目标项目基于 `ext4fs` 库，参考项目基于自研 VFS，底层实现与外部依赖完全不同。 |
| **设备驱动** | 是 | 0 | 0.0% | 0.0% | **弱** | 双方均有设备驱动，且存在同名函数 `mmio_phys_to_virt`、`unshare`。但目标项目通过 `MyPagingHandler` 实现，参考项目通过 `hal` 直接转换，实现路径不同。 |
| **网络协议栈** | 仅参考 | 0 | 0.0% | 0.0% | **未形成** | 目标项目无此模块，无法对比。 |
| **通用模块/其他** | 是 | 1 | 3.23% | 0.52% | **弱** | 唯一存在共同指纹的模块。包含 `_start`、`write_str` 等入口和基础输出函数，这些是 RISC-V 平台的通用实现模式，不构成核心逻辑复用。 |

### 3. 底层 AST 关键证据指纹表
| 所属板块 | 目标项目路径 | 参考项目路径 | 证据类型 | 技术说明 |
| :--- | :--- | :--- | :--- | :--- |
| 通用模块/其他 | `kernel/src/main.rs#_start` | `user/userlib/src/lib.rs#_start` | 同板块同名 | 均为 RISC-V 架构下使用 `naked_asm!` 宏编写的汇编入口点，是平台强相关的标准实现模式。 |
| 通用模块/其他 | `kernel/src/main.rs#write_str` | `subsystems/platform/src/console.rs#write_str` | 同板块同名 | 均实现了 `core::fmt::Write` trait 的 `write_str` 方法，是 Rust 格式化输出的标准实现路径。 |
| 通用模块/其他 | `kernel/src/buftool.rs#pos` | `subsystems/gmanager/src/lib.rs#max` | 同板块同结构 | 均为“内部闭环逻辑”，即不依赖任何外部非标准库的函数，属于简单的工具函数，无独创性。 |
| 通用模块/其他 | `kernel/src/main.rs#panic_handler` | `subsystems/unwinder/src/panic.rs#panic_handler` | 同板块同名 | 均为 Rust 的 `#[panic_handler]` 实现，是任何 `#![no_std]` 内核的必要组成部分，但实现细节不同。 |
| 文件系统 | `kernel/src/fs.rs#current_time` | `subsystems/vfs/src/lib.rs#current_time` | 同板块同名 | 同名但实现完全不同。目标项目调用 `Ext4Timestamp::new`，参考项目调用 `DynFsKernelProvider::current_time`。 |
| 设备驱动 | `kernel/src/paging.rs#mmio_phys_to_virt` | `subsystems/drivers/src/hal.rs#mmio_phys_to_virt` | 同板块同名 | 同名但实现不同。目标项目通过 `MyPagingHandler::phys_to_virt` 转换，参考项目直接进行指针运算。 |
| 设备驱动 | `kernel/src/paging.rs#unshare` | `subsystems/drivers/src/hal.rs#unshare` | 同板块同名 | 均为“内部闭环逻辑”，无外部调用，无法判定复用。 |
| 任务调度 | `kernel/src/main.rs#schedule` | `kernel/src/task/schedule.rs#schedule` | 同板块同名 | 调度器通用命名。目标项目操作 `self.processes`，参考项目调用 `take_current_task`，调用图谱完全不同。 |
| 内存管理 | `kernel/src/paging.rs#alloc` | `subsystems/mem/src/heap.rs#alloc` | 同板块同名 | 通用分配函数名。目标项目实现 Buddy 分配器，参考项目实现通用堆分配器，算法与数据结构不同。 |
| 内存管理 | `kernel/src/paging.rs#dma_alloc` | `subsystems/drivers/src/hal.rs#dma_alloc` | 同板块同名 | 通用 DMA 分配函数名。目标项目包含显式 `phys_to_virt` 转换，参考项目无此步骤，调用链不同。 |
| 内存管理 | `kernel/src/main.rs#init_heap` | `user/userlib/src/heap.rs#init_heap` | 同板块同名 | 通用堆初始化函数名。目标项目在内核态直接初始化，参考项目在用户态通过 `sbrk` 系统调用初始化。 |
| 内存管理 | `kernel/src/paging.rs#alloc_frames` | `subsystems/mem/src/frame.rs#alloc_frames` | 同板块同名 | 通用帧分配函数名。目标项目通过 `kernel_core().pager` 分配，参考项目通过 `FRAME_ALLOCATOR` 分配。 |
| 内存管理 | `kernel/src/paging.rs#dma_dealloc` | `subsystems/drivers/src/hal.rs#dma_dealloc` | 同板块同名 | 通用 DMA 释放函数名。目标项目调用 `MyPagingHandler::dealloc_frames`，参考项目调用 `free_frames`。 |

### 4. 核心代码调用流对比剖析
*   **【内存管理/物理内存分配】**
    *   **底层技术析理**: 双方均实现了 `alloc_frames` 函数，但体现了完全不同的架构设计。目标项目采用全局单例 `kernel_core()` 管理所有内核资源，通过 `pager` 子系统进行分配，调用链为 `kernel_core().pager.as_mut().unwrap().alloc(...)`。参考项目则采用独立的 `FRAME_ALLOCATOR` 全局锁进行管理，调用链为 `FRAME_ALLOCATOR.lock().alloc_pages(...)`。两者在**抽象层次**（集中式单例 vs. 独立子系统）、**同步策略**（`kernel_core()` 内部锁 vs. 显式全局锁）和**对象管理模型**上均存在本质差异。
    *   **代码溯源链接**:
        *   目标项目: `kernel/src/paging.rs#alloc_frames`
        *   参考项目: `subsystems/mem/src/frame.rs#alloc_frames`

*   **【任务调度/核心调度入口】**
    *   **底层技术析理**: 双方均有一个名为 `schedule` 的调度入口函数，但内部逻辑截然不同。目标项目的 `schedule` 函数直接操作 `self.processes` 数组，通过 `self.sched_in` 进行上下文切换，其调度逻辑内聚于 `KernelCore` 结构体中。参考项目的 `schedule` 函数则调用 `take_current_task` 从全局任务管理器中取出当前任务，再调用 `schedule_now` 执行切换。两者的**任务抽象**（进程数组 vs. 全局任务管理器）和**调度流程**完全不同。
    *   **代码溯源链接**:
        *   目标项目: `kernel/src/main.rs#schedule`
        *   参考项目: `kernel/src/task/schedule.rs#schedule`

*   **【文件系统/时间戳获取】**
    *   **底层技术析理**: 双方均存在 `current_time` 函数，但依赖的外部对象揭示了不同的文件系统架构。目标项目直接调用 `Ext4Timestamp::new`，表明其文件系统实现紧密依赖于 `ext4` 库。参考项目则调用 `DynFsKernelProvider::current_time`，这是一个自研 VFS 框架的抽象接口。两者在**文件系统架构**（单一具体实现 vs. 多层抽象）上完全不同。
    *   **代码溯源链接**:
        *   目标项目: `kernel/src/fs.rs#current_time`
        *   参考项目: `subsystems/vfs/src/lib.rs#current_time`

### 5. 单方模块与降权项
*   **单方模块**:
    *   **网络协议栈**: 仅存在于参考项目中，目标项目无此模块。这体现了两个项目在功能范围上的巨大差异，是独立开发的强信号。
*   **降权项（不构成强同源证据）**:
    *   **领域通用命名**: `schedule`, `alloc`, `dma_alloc`, `init_heap`, `alloc_frames`, `current_time`, `panic_handler` 等函数名是操作系统内核领域的标准术语，任何独立实现都可能使用这些名称，不能作为抄袭证据。
    *   **Rust 标准库与惯用模式**: `NonNull::new(...).unwrap()`, `ALLOCATOR.lock()`, `copy_from_slice`, `log!`, `Ok/Err` 等是 Rust 语言的标准库调用或极其普遍的编程模式，不具备代码独创性。
    *   **跨板块同名**: 控方将分属不同板块的函数（如目标项目的 `current_process` 与参考项目的 `epoll_pwait`）强行对齐，这种跨板块比较在方法论上无效，已予排除。
    *   **内部闭环逻辑**: `pos`, `max`, `unshare`, `skip_ecall` 等函数被标记为“内部闭环逻辑”，即不依赖任何外部非标准库，这类简单工具函数的重合具有偶然性，不能作为复用证据。

### 6. 保守结论
本次审计基于客观 AST 指纹比对，目标项目与参考项目的整体相似度仅为 0.37%，处于极低水平。在操作系统内核的核心板块，共同指纹数为零，直接否定了“同板块底层逻辑复用”的指控。控方提出的证据几乎全部基于领域通用命名、标准库惯用模式或跨板块的强行对齐，这些在严谨的同源性审计中均不构成有效证据。双方在全局架构（集中式单例 vs. 分布式子系统）、内存模型（Buddy 分配器 vs. 通用分配器）、文件系统（ext4 绑定 vs. 自研 VFS）及外部依赖上存在根本性差异，符合独立开发的典型特征。综合判定，目标项目不存在对参考项目的代码抄袭或架构复用行为。

------------------------------

