# 🛡️ OS 内核赛自动化代码级架构审计报告

**审计时间**: `2026-06-27 17:55:54`
**参赛作品名称**: `2026-T2026181239910972-哈尔滨工业大学深圳`
**年份**: `2026`
**学校**: `哈尔滨工业大学（深圳）`
**队伍名称**: `T2026181239910972`
**比赛名字**: `OS-Competition`
**比赛子赛道**: `Kernel-Implementation`
**仓库地址**: `https://gitlab.eduxiji.net/educg-group-43501-3132633/T2026181239910972-2383`
**推测开发语言**: `Rust / C (由AST探针自动识别)`
**特征函数量**: `3608`

## ⚔️ 底层代码同源性对比 (AST Homology)
> 本部分由专门的代码审查智能体，对该项目与符合年份条件的历届最高相似度项目进行控制流与底层调用对比。新版报告会先给出确定性的全模块覆盖矩阵，再给出 Agent 的分模块解释，避免只围绕少量抽样函数展开。

### 🏆 Top 2 底层对标: vs `2025-sudo_pacman_Syu-同济大学`（同济大学 / sudo_pacman_Syu）

#### 对方作品信息

**对方年份**: `2025`
**对方学校**: `同济大学`
**对方队伍名称**: `sudo_pacman_Syu`
**对方比赛名字**: `OS-Competition`
**对方比赛子赛道**: `Kernel-Implementation`
**对方仓库地址**: `https://gitlab.eduxiji.net/educg-group-36002-2710490/T202510247995532-2817`

> 双向覆盖率：共同 AST 指纹: 52；唯一 AST 指纹: 目标项目 3135，参考项目 1840；覆盖率: 目标项目 1.66%，参考项目 2.83%

## 全模块对比总览（确定性证据）

> 总体覆盖率：共同 AST 指纹: 52；唯一 AST 指纹: 目标项目 3135，参考项目 1840；覆盖率: 目标项目 1.66%，参考项目 2.83%

### 1. 模块覆盖矩阵
| 模块 | 目标函数数 | 参考函数数 | 共同唯一 AST 指纹 | Target 覆盖 | Base 覆盖 | 状态 |
| :--- | ---: | ---: | ---: | ---: | ---: | :--- |
| 进程管理 | 289 | 154 | 1 | 0.4% | 0.75% | 双方存在且有结构重合 |
| 任务调度/异步运行时 | 708 | 164 | 1 | 0.15% | 0.65% | 双方存在且有结构重合 |
| 内存管理 | 633 | 261 | 2 | 0.33% | 0.83% | 双方存在且有结构重合 |
| 中断/异常处理 | 169 | 28 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |
| 系统调用 | 95 | 58 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |
| 文件系统 | 692 | 318 | 2 | 0.32% | 0.69% | 双方存在且有结构重合 |
| 设备驱动 | 417 | 162 | 3 | 0.85% | 2.33% | 双方存在且有结构重合 |
| 网络协议栈 | 0 | 51 | 0 | 0.0% | 0.0% | 仅参考项目体现 |
| 文件系统/第三方库 | 75 | 48 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |
| 第三方库 | 244 | 740 | 6 | 2.75% | 0.97% | 双方存在且有结构重合 |
| 通用模块/其他 | 286 | 188 | 4 | 1.6% | 2.56% | 双方存在且有结构重合 |

### 2. 分模块证据清单

#### 进程管理
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `anemone-rs/src/os/linux.rs#execve` | `src/kernel/syscall/procops.rs#execve` | 同板块同名 | CString::new, CString::new(*arg).map_err, CString::new(*env).map_err, CString::new(path).map_err, arg.as_ptr, argv
            .iter ...(+19) | Dentry::open, Err, Ok, Path::new, ProgramLoader::parse, ProgramLoader::parse(&thread.fs_context, exec, dentry.clone(), argv, envp)?
        .load ...(+25) |
| `anemone-kernel/src/task/files.rs#fork` | `src/kernel/syscall/procops.rs#fork` | 同板块同名 | Arc::new, FileDesc::new, fd.fd_flags, fd.pfile.clone, fd_opt
                    .as_ref, fd_opt
                    .as_ref()
                    .map ...(+4) | CloneArgs::for_fork, do_clone |
| `anemone-rs/src/os/linux.rs#wait4` | `src/kernel/syscall/procops.rs#wait4` | 同板块同名 | Ok, Some, options.bits, process::wait4, process::wait4(
            target.to_raw() as u64,
            wstatus
                .and_then(|r| Some(r as *mut WStatusRaw as u64))
                .unwrap_or(0),
            options.bits() as u64,
            0,
        )
        .and_then, target.to_raw ...(+2) | WaitId::from_id, WaitInfo::Status, arg1.is_null, do_waitid |
| `anemone-rs/src/sys/linux.rs#rt_sigprocmask` | `src/kernel/syscall/procops.rs#rt_sigprocmask` | 同板块同名 | syscall | Err, Ok, UserPointer::new, UserPointer::new(set.as_const())?.read, UserPointerMut::new, UserPointerMut::new(oldset)?.write ...(+8) |

#### 任务调度/异步运行时
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `anemone-kernel/src/task/api/futex/futex.rs#futex_wake` | `src/kernel/task/futex.rs#futex_wake` | 同板块同名 | Ok, bitset.unwrap_or, futex.waiters.len, futex.waiters.pop_front, futex.waiters.push_back, kdebugln! ...(+3) | FUTEX_TABLE.get_bucket, FutexKey::new, Ok, futex_bucket.find, futex_bucket_ref.lock, futex_item.wait_list.notify_one |
| `scripts/xtask/src/tasks/qemu.rs#run` | `src/kernel/task/thread.rs#run` | 同板块同名 | Ok, PlatformConfig::from_str, Some, anyhow::bail!, cmd.status, cmd_echo ...(+5) | self.contexted, self.real_run, stackful |
| `anemone-kernel/src/task/files.rs#truncate` | `src/kernel/syscall/file_rw.rs#truncate` | 同板块同名 | Err, inode.truncate, inode.ty, self.can_write, self.pfile.file.inode, self.pfile.file.path ...(+2) | Dentry::open, Path::new, UserString::new, dentry.truncate, path.as_cstr, path.as_cstr().to_bytes |
| `anemone-kernel/src/driver/intc/sifive_plic.rs#unmask` | `src/kernel/task/signal.rs#unmask` | 同板块同名 | irq.get, kwarningln!, regs.set_enable, regs.set_priority, self.current_s_context, self.regs ...(+1) | self.inner.lock, self.inner.lock().mask.unmask |

#### 内存管理
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `anemone-kernel/src/mm/uspace/vma.rs#split` | `src/kernel/mem/mm_area.rs#split` | 同板块同名 | Err, Ok, Some, VirtPageRange::new, self.backing.clone, self.range.end ...(+1) | Mapping::File, Some, VRange::from, VRange::new, VRange::new(at, self.range_borrow().end()).into, assert! ...(+7) |
| `anemone-kernel/src/fs/ramfs/file.rs#truncate` | `src/fs/tmpfs.rs#truncate` | 同板块同名 | Self::resident_page_count, Self::zero_page_tail, pages.get, pages.retain, self.pages.write, self.size.swap | Instant::now, Ok, block_on, self.mtime.lock, self.pages.resize, self.rwsem.write ...(+1) |
| `anemone-kernel/src/mm/frame/buddy.rs#alloc` | `src/kernel/mem/allocator.rs#alloc` | 同板块同名 | (npages.next_power_of_two() as usize).trailing_zeros, PhysPageNum::new, VirtAddr::new, npages.next_power_of_two, paddr.get, ptr.as_ptr ...(+4) | Page::alloc_at_least, SLAB_ALLOCATOR.alloc, core::ptr::null_mut, layout.size, layout.size().next_power_of_two, page.as_memblk ...(+4) |
| `anemone-kernel/src/exception/page_fault.rs#handle_kernel_page_fault` | `src/kernel/mem/mm_list/page_fault.rs#handle_kernel_page_fault` | 同板块同名 | get_current_task, get_current_task().kstack, get_current_task().kstack().in_guard_page, info.fault_addr, kemergln!, panic! | Some, Thread::current, VRange::from, VRange::from(addr.floor()).grow, addr.floor, addr.floor().addr ...(+13) |

#### 中断/异常处理
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `anemone-kernel/src/exception/intr/irq.rs#handle_irq` | `src/driver/ahci/slot.rs#handle_irq` | 同板块同名 | ROOT_IRQ_DOMAIN.get, core.claim, drop, handle_domain_irq, handle_domain_irq(root_domain, hwirq).expect, kwarningln! ...(+3) | debug_assert_eq!, self.inner.lock, self.wait_list.notify_all |
| `anemone-kernel/src/driver/pcie_drivers/platform.rs#probe` | `src/kernel/terminal.rs#commit_char` | 同模块上下文对齐/差异观察 | (0..addr_cells_parent)
                .map, (0..addr_cells_parent)
                .map(|j| u32::from_be_bytes(mem_addr[(j as usize * 4)..][..4].try_into().unwrap()))
                .fold, (0..size_cells)
                .map, (0..size_cells)
                .map(|j| u32::from_be_bytes(size[(j as usize * 4)..][..4].try_into().unwrap()))
                .fold, Arc::new, Box::from ...(+91) | inner.termio.echok, inner.termio.echoke, inner.termio.icanon, inner.termio.icrnl, inner.termio.iexten, inner.termio.igncr ...(+16) |
| `anemone-kernel/src/driver/serial/ns16550a.rs#try_drain_irq` | `src/kernel/terminal.rs#read` | 同模块上下文对齐/差异观察 | self.read_reg | Err, Thread::current, Thread::current().signal_list.has_pending_signal, buffer.available, buffer.available().min, buffer.fill ...(+17) |
| `anemone-kernel/src/arch/loongarch64/bootstrap.rs#register_debugcon` | `src/kernel/timer.rs#timer_interrupt` | 同模块上下文对齐/差异观察 | Arc::new, Dmw::from_u64, Ns16550ARegisters::from_raw, SpinLock::new, dmw2::csr_write, register_console ...(+2) | CPU::local, CPU::local().cpuid, SLEEPERS_LIST.lock_irq, TICKS.fetch_add, WAKEUP_TICK.load, WAKEUP_TICK.store ...(+6) |

#### 系统调用
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `anemone-kernel/src/mm/uspace/shm/api/shmctl.rs#sys_shmctl` | `src/kernel/mem/mm_list.rs#set_break` | 同模块上下文对齐/差异观察 | Ok, ShmId::from_raw, ShmSlotIndex::from_linux_stat_target, check_access, ipc_info, ipc_set_update_from_linux ...(+16) | Some, VRange::new, assert!, drop, inner.locked.lock, locked.areas.check_overlapping_range ...(+5) |
| `anemone-kernel/src/syscall/mod.rs#register_syscall_handlers` | `src/kernel/syscall/sysinfo.rs#newuname` | 同模块上下文对齐/差异观察 | SYSCALL_TABLE.init, assert!, core::slice::from_raw_parts, handler_ptr.add, handler_ptr.add(handler.sysno).write, knoticeln! ...(+4) | UserPointerMut::new, buffer.write, copy_cstr_to_array |
| `anemone-kernel/src/mm/uspace/api/mod.rs#try_from_syscall_arg` | `src/kernel/syscall/net.rs#get_sockopt` | 同模块上下文对齐/差异观察 | AuxMmapFlags::from_bits, AuxMmapFlags::from_bits(raw_aux).ok_or_else, Err, Ok, kwarningln!, syscall_arg_flag32 | Ok, UserPointerMut::new, UserPointerMut::new(optlen)?.write, UserPointerMut::new(optval.cast())?.write, optval.cast, size_of::<u32> |
| `anemone-kernel/src/syscall/user_access.rs#to_read` | `src/kernel/syscall/net.rs#read_socket_addr` | 同模块上下文对齐/差异观察 | Ok, VirtAddr::new, self
                    .ptr
                    .len, self
                    .ptr
                    .len()
                    .checked_mul, self
                    .ptr
                    .len()
                    .checked_mul(size_of::<T>())
                    .expect, self.ptr.cast::<T> ...(+2) | Err, IpAddr::V4, Ipv4Addr::from_bits, Ok, SockDomain::try_from, SocketAddr::new ...(+6) |

#### 文件系统
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `anemone-kernel/src/fs/api/mod.rs#bits` | `src/kernel/vfs/inode.rs#bits` | 同板块同名 | self.perm.bits | 内部闭环逻辑 |
| `symtab/src/lib.rs#lookup` | `src/kernel/vfs/inode.rs#lookup` | 同板块同名 | Some, SymbolType::from, self.entries, self.strtab, strtab.get_str, symbols.binary_search_by_key | Err, self.is_dir |
| `anemone-kernel/src/driver/virtio/pcie.rs#write_word` | `src/driver/virtio/loongarch64.rs#write_word` | 同板块同名 | BusNum::try_from, BusNum::try_from(device_function.bus).unwrap, DevNum::try_from, DevNum::try_from(device_function.device).unwrap, FuncNum::try_from, FuncNum::try_from(device_function.function).unwrap ...(+4) | ArchPhysAccess::as_ptr, memory_barrier, pointer.write_volatile, self
            .get_conf_space, self
            .get_conf_space(
                device_function.bus,
                device_function.device,
                device_function.function,
            )
            .expect |
| `anemone-kernel/src/fs/mod.rs#register_filesystem` | `src/kernel/vfs/mount.rs#register_filesystem` | 同板块同名 | Arc::new, Err, FileSystem::new, Ok, VFS.fs_list.write, existing.name ...(+4) | Err, MOUNT_CREATORS.lock, Ok, creators.contains_key, creators.insert, fstype.to_string |

#### 设备驱动
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `anemone-kernel/src/driver/virtio/pcie.rs#probe` | `src/driver/goldfish_rtc.rs#probe` | 同板块同名 | (*bus).into, (*dev).into, Arc::new, DeviceBase::new, Err, FuncNum::MIN.into ...(+25) | ArchPhysAccess::as_ptr, register_rtc |
| `anemone-kernel/src/device/bus/mod.rs#register_driver` | `src/kernel/pcie/driver.rs#register_driver` | 同板块同名 | BusType::base, BusType::base(self)
            .drivers
            .write_irqsave, BusType::base(self)
            .drivers
            .write_irqsave()
            .add_kobject, BusType::base(self).devices.read_irqsave, BusType::base(self).devices.read_irqsave().iter, Some ...(+11) | Arc::new, Err, Ok, PCIE_DEVICES.lock, PCIE_DEVICES.lock().get, PCIE_DEVICES.lock().get(&index).cloned ...(+7) |
| `anemone-kernel/src/driver/virtio/mod.rs#mmio_phys_to_virt` | `src/driver/virtio/hal.rs#mmio_phys_to_virt` | 同板块同名 | NonNull::new_unchecked, PhysAddr::new, panic!, query_virt_addr, query_virt_addr(PhysAddr::new(paddr), size as u64).unwrap_or_else, vaddr.get | ArchPhysAccess::as_ptr, PAddr::from |
| `anemone-kernel/src/device/bus/pcie/ecam.rs#unsafe_clone` | `src/driver/virtio/loongarch64.rs#unsafe_clone` | 同板块同名 | 内部闭环逻辑 | 内部闭环逻辑 |

#### 网络协议栈
仅参考项目中观察到该模块代表函数，目标项目没有同模块可对齐证据：
- 参考项目：`src/net/socket/tcp.rs#poll_impl`，调用摘要：Ok, PollEvent::empty, Some, block_on, events.contains, iface.lock ...(+12)
- 参考项目：`src/net/socket/udp.rs#bind`，调用摘要：BoundSocket::new_bind_all, BoundSocket::new_bind_single, Err, Ok, Self::bind_impl, Some ...(+23)
- 参考项目：`src/net/socket/tcp.rs#accept_impl`，调用摘要：Arc::new, BoundSocket::new_single, Err, Ok, RwLock::new, SocketAddr::from ...(+15)
- 参考项目：`src/net/socket/udp.rs#drop`，调用摘要：UDP_PORT_MAP.lock, UDP_PORT_MAP.lock().remove, block_on, bound_socket_guard.as_ref, bound_socket_guard.as_ref().unwrap, bound_socket_guard.is_none ...(+16)

#### 文件系统/第三方库
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `anemone-kernel/src/fs/ext4/file.rs#size` | `src/fs/ext4.rs#size` | 同板块同名 | self.size.load | self.size.load |
| `anemone-kernel/src/fs/ext4/inode.rs#ext4_rename` | `src/fs/ext4.rs#lookup` | 同模块上下文对齐/差异观察 | Arc::ptr_eq, Err, Ok, Some, drop, ext4_lookup_child ...(+24) | AtomicMode::new, AtomicNlink::new, AtomicU32::new, AtomicU64::new, Err, InodeMode::BLOCKDEV.bits ...(+30) |
| `anemone-kernel/src/fs/ext4/mod.rs#ext4_mount` | `src/fs/ext4.rs#rename` | 同模块上下文对齐/差异观察 | AnyOpaque::new, Arc::clone, Arc::new, EXT4.get, EXT4.get().clone, Err ...(+22) | Arc::as_ptr, Err, Instant::now, Ok, block_on, core::str::from_utf8 ...(+27) |
| `anemone-kernel/src/fs/ext4/file.rs#copy_in` | `src/fs/ext4.rs#create` | 同模块上下文对齐/差异观察 | Ext4RegState::page_start, core::slice::from_raw_parts_mut, cur_offset
                .checked_add, cur_offset
                .checked_add(copy_len)
                .ok_or, data.len, dst[page_offset..page_offset + copy_len].copy_from_slice ...(+15) | Arc::downgrade, Arc::new, AtomicMode::new, AtomicNlink::new, AtomicU32::new, AtomicU64::new ...(+25) |

#### 第三方库
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `anemone-kernel/crates/buddy-system/src/zone.rs#dealloc` | `crates/slab_allocator/src/slab_cache.rs#dealloc` | 同板块同名 | AlignedAddr::<MIN_BLOCK_BYTES>::new, AlignedAddr::<MIN_BLOCK_BYTES>::new(addr.as_ptr() as usize)
            .ok_or, Err, FreeBlock::buddy_block_idx, LinkedListLink::new, Ok ...(+29) | Raw::in_which, page_ptr.dealloc_slot, page_ptr.get_link, page_ptr.is_empty, page_ptr.is_full, self.empty_list.insert ...(+3) |
| `anemone-kernel/crates/buddy-system/src/zone.rs#alloc` | `crates/slab_allocator/src/slab_cache.rs#alloc` | 同板块同名 | ((order + 1)..=o).rev, AlignedAddr::new, AlignedAddr::new(block.as_ptr() as usize)
                        .expect, Err, LinkedListLink::new, Ok ...(+13) | Raw::from_link, alloc.alloc, alloc.alloc().expect, new_page_ptr.alloc_slot, new_page_ptr.alloc_slot().expect, new_page_ptr.get_link ...(+17) |
| `anemone-kernel/crates/buddy-system/src/system.rs#dealloc` | `crates/slab_allocator/src/lib.rs#dealloc` | 同板块同名 | Err, addr.as_ptr, self.zones.iter, zone.contains, zone.dealloc, zone.inner.borrow_mut | max, self.slabs[idx as usize].lock, self.slabs[idx as usize].lock().dealloc, size.next_power_of_two, size.next_power_of_two().trailing_zeros |
| `anemone-kernel/crates/idalloc/src/stack.rs#alloc` | `crates/eonix_hal/src/mm.rs#alloc` | 同板块同名 | Some, self
                .end_exclusive
                .is_some_and, self.free_list.pop | panic!, self.alloc_one, self.alloc_order |

#### 通用模块/其他
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `anemone-kernel/src/utils/writer.rs#write_str` | `src/io.rs#write_str` | 同板块同名 | Err, Ok, bytes.len, panic!, s.as_bytes, self.buf.len ...(+3) | Err, Ok, s.as_bytes, self.0.fill |
| `anemone-kernel/src/sync/spinlock.rs#lock` | `src/rcu.rs#lock` | 同板块同名 | PreemptGuard::new, Some, core::hint::spin_loop, self.lock.try_lock | GLOBAL_RCU_SEM.read, block_on |
| `anemone-kernel/src/time/timer.rs#partial_cmp` | `src/kernel/timer.rs#partial_cmp` | 同板块同名 | Some, self.expire_ticks.cmp, self.expire_ticks.cmp(&other.expire_ticks).reverse | Some, self.cmp |
| `anemone-kernel/src/utils/bitmap.rs#clear` | `src/kernel/pcie/header.rs#clear` | 同板块同名 | Self::position | current.and, memory_barrier, self.read, self.write, value.not |


# 最终同源性定性仲裁裁决书

**案号**: 2026-T2026181239910972 vs 2025-sudo_pacman_Syu
**审理依据**: 客观 AST 指纹比对、全模块覆盖矩阵、底层调用图谱差异分析
**裁决原则**: 基于底层架构异构性判定，排除领域共性干扰，禁止跨板块硬凑

---

## 1. 宏观判定

| 指标 | 数值 | 解读 |
|------|------|------|
| **客观 AST 相似度** | **1.06%** | 极低，远低于同源阈值（通常 > 40% 方需警惕） |
| **共同 AST 指纹** | 52 | 占目标项目 3187 个函数指纹的 1.63% |
| **目标项目唯一指纹** | 3135 | 占比 98.34%，证明绝对主体为独立代码 |
| **参考项目唯一指纹** | 1840 | 占比 97.17% |
| **目标项目覆盖率** | 1.66% | 仅 1.66% 的函数与参考项目存在任何形式的结构重合 |
| **参考项目覆盖率** | 2.83% | 同理 |

**定性结论**: **底层架构显著差异 / 独立开发**

52 个共同 AST 指纹散布于 11 个模块中，且绝大多数对应 `Arc::new`、`Ok/Err`、`assert!`、`Some`、`Iterator::map/collect` 等 Rust 标准库通用范式或 POSIX 强制语义调用。双方在**内存抽象、同步原语、对象管理模型**三个核心架构维度上使用了完全不同的类型体系，不存在系统性逻辑复用。

---

## 2. 全模块覆盖矩阵解读

### 2.1 进程管理
- **双方均存在**: 是（目标 289 函数，参考 154 函数）
- **共同 AST 指纹**: 1
- **覆盖率**: 目标 0.4%，参考 0.75%
- **证据强度**: **弱**

**解读**: 仅 1 个共同指纹，覆盖率均不足 1%。双方均实现了 POSIX 进程管理语义（`fork`、`execve`、`exit`、`wait4`、`setsid` 等），但目标项目使用 `TOPOLOGY` + `NoIrqRwLock` + `UserReadPtr/UserWritePtr` 体系，参考项目使用 `ProcessList` + `Locked` + `UserPointer` 体系。同名函数（如 `execve`、`exit`）是 POSIX 标准系统调用名称的必然结果，不构成复用证据。

### 2.2 任务调度/异步运行时
- **双方均存在**: 是（目标 708 函数，参考 164 函数）
- **共同 AST 指纹**: 1
- **覆盖率**: 目标 0.15%，参考 0.65%
- **证据强度**: **弱**

**解读**: 目标项目调度器规模远超参考项目（708 vs 164），表明其实现了更复杂的调度逻辑。共同指纹仅 1 个，覆盖率极低。目标项目使用 `WakeCommit` 状态机 + `PROCESSOR.with_mut` 线程局部调度队列，参考项目使用 `SLEEPERS_LIST` + `TICKS` 全局计数器，调度架构完全不同。

### 2.3 内存管理
- **双方均存在**: 是（目标 633 函数，参考 261 函数）
- **共同 AST 指纹**: 2
- **覆盖率**: 目标 0.33%，参考 0.83%
- **证据强度**: **弱**

**解读**: 2 个共同指纹中，`split`（VMA 分割）和 `truncate`（文件截断对应内存回收）均为操作系统内存管理的通用操作。目标项目使用 `VirtPageRange` + `PhysPageNum` + `buddy` 分配器，参考项目使用 `VRange` + `Page::alloc_at_least` + `SLAB_ALLOCATOR`，物理内存管理模型异构。

### 2.4 中断/异常处理
- **双方均存在**: 是（目标 169 函数，参考 28 函数）
- **共同 AST 指纹**: 0
- **覆盖率**: 目标 0.0%，参考 0.0%
- **证据强度**: **未形成**

**解读**: 零共同指纹。目标项目实现了完整的中断域层次结构（`ROOT_IRQ_DOMAIN`、`handle_domain_irq`），参考项目中断处理规模较小。双方在此模块无任何可对齐的 AST 结构。

### 2.5 系统调用
- **双方均存在**: 是（目标 95 函数，参考 58 函数）
- **共同 AST 指纹**: 0
- **覆盖率**: 目标 0.0%，参考 0.0%
- **证据强度**: **未形成**

**解读**: 零共同指纹。目标项目使用 `SYSCALL_TABLE.init` + `handler_ptr.add(handler.sysno).write` 的动态注册机制，参考项目在 `procops.rs` 中集中实现系统调用。系统调用分发架构完全不同。

### 2.6 文件系统
- **双方均存在**: 是（目标 692 函数，参考 318 函数）
- **共同 AST 指纹**: 2
- **覆盖率**: 目标 0.32%，参考 0.69%
- **证据强度**: **弱**

**解读**: 2 个共同指纹中，`bits` 为 `self.perm.bits` 的简单字段访问，`lookup` 为符号表查找的通用二分搜索模式。目标项目使用 `Inode` + `NilOpaque` + `InodePerm` 模型，参考项目使用 `InodeData` + `Mode::DIR` + `AtomicMode` 模型，VFS 节点构造体系异构。

### 2.7 设备驱动
- **双方均存在**: 是（目标 417 函数，参考 162 函数）
- **共同 AST 指纹**: 3
- **覆盖率**: 目标 0.85%，参考 2.33%
- **证据强度**: **弱**

**解读**: 3 个共同指纹中，`probe` 为 PCI 设备探测通用流程，`register_driver` 为驱动注册的标准总线操作，`mmio_phys_to_virt` 为 MMIO 物理地址转换。目标项目使用 `BusType::base` + `kobject` 设备模型，参考项目使用 `PCIE_DEVICES.lock` 简单全局表，驱动架构异构。

### 2.8 网络协议栈
- **双方均存在**: 否（目标 0 函数，参考 51 函数）
- **共同 AST 指纹**: 0
- **覆盖率**: N/A
- **证据强度**: **未形成**

**解读**: 目标项目未实现网络协议栈，参考项目包含 TCP/UDP 套接字实现。此模块为参考项目独有，无法进行比对。

### 2.9 第三方库/公共依赖
- **双方均存在**: 是（目标 244 函数，参考 740 函数）
- **共同 AST 指纹**: 6
- **覆盖率**: 目标 2.75%，参考 0.97%
- **证据强度**: **弱（后置为第三方库）**

**解读**: 6 个共同指纹集中在伙伴系统分配器（`buddy-system`）和 Slab 分配器（`slab_allocator`）的 `alloc`/`dealloc` 接口。两者均为 Rust 社区常见的独立 crate（目标项目 `crates/buddy-system`，参考项目 `crates/slab_allocator`），属于独立开发的第三方库组件，不构成项目间代码复用。

---

## 3. 底层 AST 关键证据指纹表

| 所属板块 | 目标项目路径 | 参考项目路径 | 证据类型 | 技术说明 |
| :--- | :--- | :--- | :--- | :--- |
| 进程管理 | `anemone-rs/src/os/linux.rs#execve` | `src/kernel/syscall/procops.rs#execve` | 同板块同名 | POSIX 标准系统调用名称；目标为用户态 libc 包装（`CString::new`），参考为内核实现（`ProgramLoader::parse`），抽象层不同 |
| 进程管理 | `anemone-kernel/src/task/files.rs#fork` | `src/kernel/syscall/procops.rs#fork` | 同板块同名 | 同名但功能分支不同：目标克隆文件描述符表，参考调用 `CloneArgs::for_fork` |
| 进程管理 | `anemone-rs/src/os/linux.rs#wait4` | `src/kernel/syscall/procops.rs#wait4` | 同板块同名 | POSIX 标准名称；目标为用户态包装，参考为内核 `do_waitid` 调用 |
| 进程管理 | `anemone-rs/src/sys/linux.rs#rt_sigprocmask` | `src/kernel/syscall/procops.rs#rt_sigprocmask` | 同板块同名 | 目标仅含 `syscall` 宏调用，参考含完整 `UserPointer` 读写逻辑，复杂度差异显著 |
| 任务调度 | `anemone-kernel/src/task/api/futex/futex.rs#futex_wake` | `src/kernel/task/futex.rs#futex_wake` | 同板块同名 | 目标使用 `FUTEX_SET.lock` + `VecDeque`，参考使用 `FUTEX_TABLE.get_bucket` + 哈希桶，数据结构选型不同 |
| 内存管理 | `anemone-kernel/src/mm/uspace/vma.rs#split` | `src/kernel/mem/mm_area.rs#split` | 同板块同名 | 目标使用 `VirtPageRange`，参考使用 `VRange` + `Mapping::File` 枚举，类型体系不同 |
| 内存管理 | `anemone-kernel/src/mm/frame/buddy.rs#alloc` | `src/kernel/mem/allocator.rs#alloc` | 同板块同名 | 目标使用伙伴系统（`next_power_of_two`），参考使用 Slab 分配器（`SLAB_ALLOCATOR.alloc`），分配策略异构 |
| 文件系统 | `anemone-kernel/src/fs/api/mod.rs#bits` | `src/kernel/vfs/inode.rs#bits` | 同板块同名 | 均为 `self.perm.bits` 简单字段访问，通用 Rust 模式 |
| 文件系统 | `symtab/src/lib.rs#lookup` | `src/kernel/vfs/inode.rs#lookup` | 同板块同名 | 目标为 ELF 符号表二分查找，参考为目录 inode 查找，功能语义不同 |
| 设备驱动 | `anemone-kernel/src/driver/virtio/pcie.rs#probe` | `src/driver/goldfish_rtc.rs#probe` | 同板块同名 | 目标探测 virtio PCI 设备，参考探测 goldfish RTC，设备类型不同 |
| 设备驱动 | `anemone-kernel/src/device/bus/mod.rs#register_driver` | `src/kernel/pcie/driver.rs#register_driver` | 同板块同名 | 目标使用 `kobject` 设备模型，参考使用 `PCIE_DEVICES.lock` 简单表，架构复杂度差异显著 |
| 设备驱动 | `anemone-kernel/src/driver/virtio/mod.rs#mmio_phys_to_virt` | `src/driver/virtio/hal.rs#mmio_phys_to_virt` | 同板块同名 | 均为 MMIO 物理地址到虚拟地址转换，目标使用 `PhysAddr::new` + `query_virt_addr`，参考使用 `ArchPhysAccess::as_ptr` |
| 第三方库 | `anemone-kernel/crates/buddy-system/src/zone.rs#dealloc` | `crates/slab_allocator/src/slab_cache.rs#dealloc` | 同板块同名 | 独立第三方 crate；目标为伙伴系统释放，参考为 Slab 缓存释放，算法不同 |
| 第三方库 | `anemone-kernel/crates/buddy-system/src/zone.rs#alloc` | `crates/slab_allocator/src/slab_cache.rs#alloc` | 同板块同名 | 独立第三方 crate；分配算法异构 |
| 第三方库 | `anemone-kernel/crates/buddy-system/src/system.rs#dealloc` | `crates/slab_allocator/src/lib.rs#dealloc` | 同板块同名 | 独立第三方 crate；内存回收路径不同 |
| 通用模块 | `anemone-kernel/src/utils/writer.rs#write_str` | `src/io.rs#write_str` | 同板块同名 | 均为 `fmt::Write` trait 实现，Rust 标准库强制接口 |
| 通用模块 | `anemone-kernel/src/sync/spinlock.rs#lock` | `src/rcu.rs#lock` | 同板块同名 | 目标为自旋锁（`spin_loop`），参考为 RCU 读锁（`GLOBAL_RCU_SEM.read`），同步原语完全不同 |
| 通用模块 | `anemone-kernel/src/time/timer.rs#partial_cmp` | `src/kernel/timer.rs#partial_cmp` | 同板块同名 | 均为 `Ord` trait 实现，Rust 标准库强制接口 |

---

## 4. 核心代码调用流对比剖析

### 4.1 【进程管理 / execve 系统调用路径】

**底层技术析理**:

目标项目 `execve` 位于用户态 libc 层（`anemone-rs/src/os/linux.rs`），其调用流为：
```
CString::new(path) → CString::new(*arg) → argv.iter().map().collect() → syscall!
```
该函数仅负责将 Rust 字符串转换为 C 兼容格式，最终通过 `syscall!` 宏陷入内核。

参考项目 `execve` 位于内核系统调用层（`src/kernel/syscall/procops.rs`），其调用流为：
```
Path::new → Dentry::open → ProgramLoader::parse().load() → TrapContext::new
```
该函数直接操作内核 VFS 对象（`Dentry`、`Path`），通过 `ProgramLoader` 解析并加载可执行文件。

**异构性判定**: 两者处于完全不同的抽象层次。目标项目为 libc 包装，参考项目为内核实现。调用图谱中无任何共同的外部类型（目标使用 `CString`，参考使用 `Dentry`/`ProgramLoader`），不存在 AST 复用。

**代码溯源链接**:
- 目标项目: `anemone-rs/src/os/linux.rs#execve`
- 参考项目: `src/kernel/syscall/procops.rs#execve`

---

### 4.2 【进程管理 / 进程退出路径】

**底层技术析理**:

目标项目 `kernel_exit`（`anemone-kernel/src/task/api/exit/mod.rs`）调用流：
```
SigInfoFields::Chld → Signal::new → ThreadGroupLifeCycle::Exited → Tid::new → UserWritePtr::<Tid>::try_new → cpu_usage.reaped_kernel → cpu_usage.reaped_user → assert!
```
特征：显式构造 `SigInfoFields::Chld` 信号、写入 `UserWritePtr<Tid>`、记录 `cpu_usage` 统计。

参考项目 `exit`（`src/kernel/syscall/procops.rs`）调用流：
```
ProcessList::get → ProcessList::get().write → WaitType::Exited → procs.do_exit
```
特征：通过 `ProcessList` 全局单例获取写锁，调用 `do_exit` 执行退出。

**异构性判定**: 目标项目使用 `ThreadGroupLifeCycle` 枚举管理退出状态机，显式区分 `Exited`/`Exiting` 阶段，并记录内核/用户态 CPU 时间统计。参考项目使用 `WaitType::Exited` 单一标记，无 CPU 统计。两者在信号构造方式（`SigInfoFields::Chld` vs `WaitType::Exited`）、锁机制（`TOPOLOGY` vs `ProcessList`）、资源统计粒度上均不同。

**代码溯源链接**:
- 目标项目: `anemone-kernel/src/task/api/exit/mod.rs#kernel_exit`
- 参考项目: `src/kernel/syscall/procops.rs#exit`

---

### 4.3 【内存管理 / 物理页分配路径】

**底层技术析理**:

目标项目 `alloc`（`anemone-kernel/src/mm/frame/buddy.rs`）调用流：
```
npages.next_power_of_two → trailing_zeros → PhysPageNum::new → VirtAddr::new → paddr.get → ptr.as_ptr
```
特征：伙伴系统（Buddy System）算法，通过 `next_power_of_two` 计算分配阶数，返回 `PhysPageNum`。

参考项目 `alloc`（`src/kernel/mem/allocator.rs`）调用流：
```
Page::alloc_at_least → SLAB_ALLOCATOR.alloc → layout.size().next_power_of_two → page.as_memblk → core::ptr::null_mut
```
特征：Slab 分配器，通过 `SLAB_ALLOCATOR.alloc` 从预分配的 `Page` 中获取内存块。

**异构性判定**: 目标项目使用伙伴系统（Buddy System），参考项目使用 Slab 分配器。两者在分配算法（2^n 阶伙伴合并 vs 对象缓存 Slab）、返回类型（`PhysPageNum` vs `*mut u8`）、元数据管理方式上完全不同。`next_power_of_two` 是两种分配器的通用数学运算，不构成复用证据。

**代码溯源链接**:
- 目标项目: `anemone-kernel/src/mm/frame/buddy.rs#alloc`
- 参考项目: `src/kernel/mem/allocator.rs#alloc`

---

### 4.4 【文件系统 / procfs 初始化路径】

**底层技术析理**:

目标项目 `init`（`anemone-kernel/src/fs/proc/mod.rs`）调用流：
```
Arc::new → Inode::new → InodePerm::all_rwx → Instant::ZERO.to_duration → NilOpaque::new → PROCFS.get → PROCFS.init
```
特征：使用 `Inode` + `NilOpaque` 不透明数据模型，`InodePerm::all_rwx` 权限位，`Instant::ZERO` 初始时间戳。

参考项目 `new`（`src/fs/procfs.rs`）调用流：
```
Arc::new → InodeData::new → Mode::DIR.perm → Instant::now → file.can_read → file.can_write → inode.atime.get_mut → inode.ctime.get_mut → inode.idata.mode.store
```
特征：使用 `InodeData` + `Mode::DIR` 模型，`Instant::now` 当前时间戳，显式设置 `atime`/`ctime`/`mode`。

**异构性判定**: 目标项目使用 `NilOpaque` 作为 inode 的不透明数据载体，参考项目使用 `InodeData` 结构体直接存储 `atime`/`ctime`/`mode`。两者在 inode 构造模型（不透明 vs 结构化）、时间戳策略（`ZERO` vs `now`）、权限模型（`InodePerm` vs `Mode::DIR`）上均不同。

**代码溯源链接**:
- 目标项目: `anemone-kernel/src/fs/proc/mod.rs#init`
- 参考项目: `src/fs/procfs.rs#new`

---

### 4.5 【同步原语 / 锁机制路径】

**底层技术析理**:

目标项目 `lock`（`anemone-kernel/src/sync/spinlock.rs`）调用流：
```
PreemptGuard::new → core::hint::spin_loop → self.lock.try_lock
```
特征：自旋锁（Spinlock），使用 `PreemptGuard` 禁用抢占，`spin_loop` 忙等待，`try_lock` 原子操作。

参考项目 `lock`（`src/rcu.rs`）调用流：
```
GLOBAL_RCU_SEM.read → block_on
```
特征：RCU（Read-Copy-Update）读锁，使用 `GLOBAL_RCU_SEM` 信号量，`block_on` 异步等待。

**异构性判定**: 目标项目使用自旋锁（忙等待 + 抢占禁用），参考项目使用 RCU 信号量（异步阻塞等待）。两者在同步策略（忙等 vs 阻塞）、锁类型（自旋锁 vs RCU 读锁）、并发模型上完全不同。同名 `lock` 是 Rust 同步原语的通用方法名。

**代码溯源链接**:
- 目标项目: `anemone-kernel/src/sync/spinlock.rs#lock`
- 参考项目: `src/rcu.rs#lock`

---

## 5. 单方模块与降权项

### 5.1 仅单方存在的模块

| 模块 | 存在方 | 函数数 | 说明 |
|------|--------|--------|------|
| 网络协议栈 | 仅参考项目 | 51 | 目标项目未实现网络功能，无法比对 |

### 5.2 降权项清单

以下证据类型**不构成有效同源指纹**，予以降权或排除：

| 降权类型 | 示例 | 降权理由 |
|----------|------|----------|
| **POSIX 标准系统调用名** | `fork`、`execve`、`exit`、`wait4`、`getpid`、`getppid`、`exit_group`、`rt_sigprocmask` | Linux 系统调用标准名称，任何 Linux 兼容内核必须使用 |
| **Rust 标准库 trait 实现** | `write_str`（`fmt::Write`）、`partial_cmp`（`Ord`） | Rust 编译器强制要求的 trait 方法签名 |
| **Rust 通用范式** | `Arc::new`、`Ok/Err`、`Some`、`assert!`、`drop`、`Iterator::map/collect`、`matches!` | 任何 Rust 项目的高频基础调用 |
| **跨抽象层同名** | `anemone-rs/src/os/linux.rs#execve` vs `src/kernel/syscall/procops.rs#execve` | 用户态 libc 包装 vs 内核实现，抽象层不同 |
| **同功能不同语义** | `anemone-kernel/src/task/files.rs#fork` vs `src/kernel/syscall/procops.rs#fork` | 前者克隆文件描述符表，后者调用 `do_clone` |
| **独立第三方 crate** | `buddy-system` vs `slab_allocator` 的 `alloc`/`dealloc` | 双方使用不同的开源内存分配器 crate |
| **简单字段访问** | `bits`（`self.perm.bits`）、`size`（`self.size.load`） | 单行 getter 方法，无逻辑复杂度 |

---

## 6. 保守结论

本庭基于客观 AST 指纹比对数据（总体相似度 1.06%，共同指纹 52 个，目标项目覆盖率 1.66%）及全模块调用图谱差异分析，认定目标项目 `[2026-T2026181239910972-哈尔滨工业大学深圳]` 与参考项目 `[2025-sudo_pacman_Syu-同济大学]` 之间**不存在系统性代码复用**。

52 个共同 AST 指纹散布于 11 个模块中，经逐项审查，绝大多数对应 Rust 标准库通用范式（`Arc::new`、`Ok/Err`、`assert!`）、POSIX 强制语义调用（`fork`、`execve`、`exit`）或独立第三方 crate 接口（`buddy-system`、`slab_allocator`）。双方在**内存抽象**（`VirtPageRange` vs `VRange`、`PhysPageNum` vs `Page`）、**同步原语**（`NoIrqRwLock` + 自旋锁 vs `Locked` + RCU）、**对象管理模型**（`TOPOLOGY` vs `ProcessList`、`Inode` + `NilOpaque` vs `InodeData` + `Mode`）三个核心架构维度上使用了完全独立的类型体系与设计模式。

控方关于“进程拓扑与回收机制 AST 控制流高度同构”“Execve 加载器流水线骨架复刻”“Procfs 节点初始化模板复用”“进程退出时序复用”的四项指控，本质是将 POSIX 规范强制语义与领域共性流程误读为代码复用。本庭对上述指控**不予采信**。

**最终裁决**: 目标项目为独立开发，不构成对参考项目的代码抄袭或逻辑复用。

---

*裁决日期: 2026 年*
*仲裁依据: 客观 AST 指纹比对 + 全模块调用图谱异构性分析*

------------------------------

