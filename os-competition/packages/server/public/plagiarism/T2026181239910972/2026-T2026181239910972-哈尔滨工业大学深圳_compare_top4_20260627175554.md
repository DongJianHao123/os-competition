# 🛡️ OS 内核赛自动化代码级架构审计报告

**审计时间**: `2026-06-27 17:55:54`
**参赛作品名称**: `2026-T2026181239910972-哈尔滨工业大学深圳`
**年份**: `2026`
**学校**: `哈尔滨工业大学（深圳）`
**队伍名称**: `T2026181239910972`
**比赛名字**: `OS-Competition`
**比赛子赛道**: `Kernel-Implementation`
**仓库地址**: `https://gitlab.eduxiji.net/educg-group-43501-3132633/T2026181239910972-2383`
**推测开发语言**: `Rust / C (由AST探针自动识别)`
**特征函数量**: `3608`

## ⚔️ 底层代码同源性对比 (AST Homology)
> 本部分由专门的代码审查智能体，对该项目与符合年份条件的历届最高相似度项目进行控制流与底层调用对比。新版报告会先给出确定性的全模块覆盖矩阵，再给出 Agent 的分模块解释，避免只围绕少量抽样函数展开。

### 🏆 Top 4 底层对标: vs `2025-NexusOS-郑州大学`（郑州大学 / NexusOS）

#### 对方作品信息

**对方年份**: `2025`
**对方学校**: `郑州大学`
**对方队伍名称**: `NexusOS`
**对方比赛名字**: `OS-Competition`
**对方比赛子赛道**: `Kernel-Implementation`
**对方仓库地址**: `https://gitlab.eduxiji.net/educg-group-36002-2710490/T202510459995282-239`

> 双向覆盖率：共同 AST 指纹: 57；唯一 AST 指纹: 目标项目 3135，参考项目 3814；覆盖率: 目标项目 1.82%，参考项目 1.49%

## 全模块对比总览（确定性证据）

> 总体覆盖率：共同 AST 指纹: 57；唯一 AST 指纹: 目标项目 3135，参考项目 3814；覆盖率: 目标项目 1.82%，参考项目 1.49%

### 1. 模块覆盖矩阵
| 模块 | 目标函数数 | 参考函数数 | 共同唯一 AST 指纹 | Target 覆盖 | Base 覆盖 | 状态 |
| :--- | ---: | ---: | ---: | ---: | ---: | :--- |
| 进程管理 | 289 | 26 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |
| 任务调度/异步运行时 | 708 | 268 | 1 | 0.15% | 0.42% | 双方存在且有结构重合 |
| 内存管理 | 633 | 885 | 3 | 0.5% | 0.37% | 双方存在且有结构重合 |
| 中断/异常处理 | 169 | 132 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |
| 系统调用 | 95 | 7 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |
| 文件系统 | 692 | 447 | 3 | 0.47% | 0.76% | 双方存在且有结构重合 |
| 设备驱动 | 417 | 88 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |
| 网络协议栈 | 0 | 0 | 0 | 0.0% | 0.0% | 双方均未体现 |
| 文件系统/第三方库 | 75 | 360 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |
| 网络协议栈/第三方库 | 0 | 117 | 0 | 0.0% | 0.0% | 仅参考项目体现 |
| 第三方库 | 244 | 1781 | 3 | 1.38% | 0.21% | 双方存在且有结构重合 |
| 通用模块/其他 | 286 | 523 | 7 | 2.8% | 1.69% | 双方存在且有结构重合 |

### 2. 分模块证据清单

#### 进程管理
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `anemone-kernel/src/task/api/exit/mod.rs#kernel_exit` | `kernel/src/thread/wait.rs#do_wait4` | 同模块上下文对齐/差异观察 | SigInfoFields::Chld, Signal::new, ThreadGroupLifeCycle::Exited, Tid::new, UserWritePtr::<Tid>::try_new, assert! ...(+51) | ControlFlow::Continue, Err, Ok, Some, child.clone, child.lifecycle.wait ...(+13) |
| `anemone-kernel/src/task/topology/process_group.rs#move_to_process_group_if` | `kernel/src/thread/execve.rs#do_execve` | 同模块上下文对齐/差异观察 | Arc::new, BTreeSet::new, Err, NoIrqRwLock::new, Ok, TOPOLOGY.inner.write ...(+21) | ControlFlow::Continue, Ok, PathSlice::new, errno_with_message, load_elf_to_vm, path.is_absolute ...(+11) |
| `anemone-kernel/src/fs/proc/tgid/stat.rs#build_stat_line` | `kernel/src/syscall/signal.rs#do_rt_sigprocmask` | 同模块上下文对齐/差异观察 | Ok, VirtAddr::new, cmdline_start.get, cpu_usage.reaped_kernel, cpu_usage.reaped_user, cpu_usage.self_kernel ...(+37) | ControlFlow::Continue, Err, KernelSigset, Ok, Some, errno_with_message ...(+3) |
| `anemone-kernel/src/task/topology/process_group.rs#create_session` | `ostd/src/smp.rs#inter_processor_call` | 同模块上下文对齐/差异观察 | Arc::new, BTreeSet::from, Err, NoIrqRwLock::new, Ok, TOPOLOGY.inner.write ...(+18) | CALL_QUEUES.get_on_cpu, CALL_QUEUES.get_on_cpu(cpu_id).lock, CALL_QUEUES.get_on_cpu(cpu_id).lock().push_back, INTER_PROCESSOR_CALL_IRQ.get, INTER_PROCESSOR_CALL_IRQ.get().unwrap, INTER_PROCESSOR_CALL_IRQ.get().unwrap().num ...(+6) |

#### 任务调度/异步运行时
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `scripts/xtask/src/tasks/qemu.rs#run` | `osdk/src/bundle/mod.rs#run` | 同板块同名 | Ok, PlatformConfig::from_str, Some, anyhow::bail!, cmd.status, cmd_echo ...(+5) | Command::new, action
                    .qemu
                    .bootdev_append_options
                    .as_deref, action
                    .qemu
                    .bootdev_append_options
                    .as_deref()
                    .unwrap_or, action.boot.kcmdline.join, assert!, aster_bin.path ...(+27) |
| `scripts/xtask/src/tasks/fmt.rs#run` | `ostd/src/task/scheduler/mod.rs#run` | 同板块同名 | Ok, Path::new, Path::new(RUSTFMT_CONFIG)
        .canonicalize, Path::new(RUSTFMT_CONFIG)
        .canonicalize()
        .with_context, app_manifest_path, app_manifest_path(package).exists ...(+6) | RUNNING.with, SCHEDULER.with, Some, crate::arch::wait_for_interrupt, log::error!, log::info! ...(+5) |
| `scripts/xtask/src/tasks/mrproper.rs#run` | `ostd/src/task/mod.rs#run` | 同板块同名 | Ok, Shell::new, log_progress!, sh.cmd, sh.cmd("cargo").arg, sh.cmd("cargo").arg("clean").run_echo ...(+9) | scheduler::spawn_user_task, self.clone |
| `anemone-kernel/src/task/topology/thread_group.rs#leader` | `kernel/src/thread/thread_group.rs#leader` | 同板块同名 | TOPOLOGY.inner.read, node.task.clone, self.tgid, topology
            .tasks
            .get, topology
            .tasks
            .get(&self.tgid())
            .map | self.members.read, self.members.read().first, self.members.read().first().unwrap, self.members.read().first().unwrap().clone |

#### 内存管理
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `anemone-kernel/src/mm/paging/mapper.rs#map` | `kernel/src/thread/loader/elf_mapper.rs#map` | 同板块同名 | (0..=max_hpage_level).rev, (vpn.get() ^ ppn.get()).trailing_zeros, ManuallyDrop::new, MapTransaction::new, Ok, PhysPageNum::new ...(+12) | AuxVec::new, ElfImage::from_path, Loader::<MmapImpl>::new, Ok, Self::apply_bias_to_range, Self::calc_phdr_at_user ...(+33) |
| `anemone-kernel/src/mm/uspace/mod.rs#handle_page_fault` | `kernel/src/vm/vmar/vm_mapping.rs#handle_page_fault` | 同板块同名 | Err, Ok, Self::find_vma_raw_mut, Self::find_vma_raw_mut(vmas, fault_addr).ok_or, Some, VirtPageRange::new ...(+14) | Ok, PageProperty::new, TlbFlushOp::Address, TlbFlushOp::Address(va).perform_on_current, VmPerms::from, VmPerms::from(prop.flags).contains ...(+20) |
| `anemone-kernel/src/arch/loongarch64/exception/trap/mod.rs#try_from` | `ostd/src/mm/frame/segment.rs#try_from` | 同板块同名 | Err, Ok, Self::Address, Self::FloatingPoint, Self::GuestCsr, Self::Watchpoint | (first_frame.dyn_meta() as &dyn core::any::Any).is::<M>, Err, Frame::<dyn AnyFrameMeta>::from_raw, ManuallyDrop::new, Ok, core::mem::transmute::<Segment<dyn AnyFrameMeta>, Segment<M>> ...(+4) |
| `anemone-kernel/src/fs/api/getdents64.rs#push` | `ostd/src/mm/tlb.rs#push` | 同板块同名 | Err, Ok, dirent64_dtype, dirent64_record_len, entry.ino.get, entry.name.len ...(+12) | Some |

#### 中断/异常处理
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `anemone-kernel/src/arch/riscv64/exception/intr.rs#send_ipi` | `ostd/src/arch/riscv/irq.rs#send_ipi` | 同板块同名 | sbi_rt::HartMask::from_mask_base, sbi_rt::send_ipi, sbi_rt::send_ipi(hartmask).expect | CPU_IPI_QUEUES
        .get_on_cpu, CPU_IPI_QUEUES
        .get_on_cpu(cpu_id)
        .get, CPU_IPI_QUEUES
        .get_on_cpu(cpu_id)
        .get()
        .expect, Ok, build_hart_mask, cpu_id.as_usize ...(+7) |
| `anemone-kernel/src/exception/intr/irq.rs#exit` | `ostd/src/tracer.rs#exit` | 同板块同名 | desc.domain.ops.read_irqsave, desc.domain.ops.read_irqsave().eoi, desc.domain.ops.read_irqsave().unmask | PER_CPU_INDENTATION.load, PER_CPU_INDENTATION.store, current_val.saturating_sub, println!, trap::disable_local |
| `anemone-kernel/src/exception/intr/irq.rs#enter` | `ostd/src/tracer.rs#enter` | 同板块同名 | desc.domain.ops.read_irqsave, desc.domain.ops.read_irqsave().mask | PER_CPU_INDENTATION.load, PER_CPU_INDENTATION.store, current_val.saturating_add, trap::disable_local |
| `anemone-kernel/src/arch/loongarch64/exception/trap/mod.rs#set_sp` | `ostd/src/arch/x86/trap/mod.rs#set_sp` | 同板块同名 | 内部闭环逻辑 | 内部闭环逻辑 |

#### 系统调用
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `anemone-kernel/src/mm/uspace/shm/api/shmctl.rs#sys_shmctl` | `kernel/src/syscall/uts.rs#current` | 同模块上下文对齐/差异观察 | Ok, ShmId::from_raw, ShmSlotIndex::from_linux_stat_target, check_access, ipc_info, ipc_set_update_from_linux ...(+16) | buf[..len].copy_from_slice, bytes.len, bytes.len().min, fill, src.as_bytes |
| `anemone-kernel/src/syscall/mod.rs#register_syscall_handlers` | `kernel/src/syscall/uts.rs#fill` | 同模块上下文对齐/差异观察 | SYSCALL_TABLE.init, assert!, core::slice::from_raw_parts, handler_ptr.add, handler_ptr.add(handler.sysno).write, knoticeln! ...(+4) | buf[..len].copy_from_slice, bytes.len, bytes.len().min, src.as_bytes |
| `anemone-kernel/src/mm/uspace/api/mod.rs#try_from_syscall_arg` | `kernel/src/syscall/users.rs#do_getegid` | 同模块上下文对齐/差异观察 | AuxMmapFlags::from_bits, AuxMmapFlags::from_bits(raw_aux).ok_or_else, Err, Ok, kwarningln!, syscall_arg_flag32 | ControlFlow::Continue, Ok, Some |
| `anemone-kernel/src/syscall/user_access.rs#to_read` | `kernel/src/syscall/users.rs#do_geteuid` | 同模块上下文对齐/差异观察 | Ok, VirtAddr::new, self
                    .ptr
                    .len, self
                    .ptr
                    .len()
                    .checked_mul, self
                    .ptr
                    .len()
                    .checked_mul(size_of::<T>())
                    .expect, self.ptr.cast::<T> ...(+2) | ControlFlow::Continue, Ok, Some |

#### 文件系统
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `anemone-kernel/src/fs/mod.rs#mount` | `kernel/comps/vfs/src/impls/memfs/provider.rs#mount` | 同板块同名 | "/".to_string, Arc::new, Dentry::new, Err, Mount::new, Ok ...(+23) | Arc::downgrade, Arc::new, InMemoryFileSystem::new, InMemoryVnode::new_directory, Ok, Timestamps::now ...(+2) |
| `anemone-kernel/src/fs/mod.rs#unmount` | `kernel/comps/vfs/src/manager.rs#unmount` | 同板块同名 | Arc::ptr_eq, Err, Ok, fs.flags, fs.flags().contains, fs.kill_sb ...(+16) | KernelError::with_message, Ok, info.fs.id, info.fs.prepare_unmount, self.fs_id_pool.free, self.mount_id_pool.free ...(+2) |
| `anemone-kernel/src/fs/api/mod.rs#try_from` | `kernel/src/vm/mmap.rs#try_from` | 同板块同名 | Err, InodeMode::new, InodePerm::try_from, Ok, knoticeln! | MMapFlags::from_bits, Ok, return_errno_with_message! |
| `anemone-rs/src/os/linux.rs#mount` | `kernel/comps/vfs/src/static_dispatch/provider.rs#mount` | 同板块同名 | CString::new, CString::new(fstype).map_err, CString::new(s.to_str().ok_or(EINVAL)?).map_err, CString::new(target.to_str().ok_or(EINVAL)?).map_err, fs::mount, fs::mount(
            source_cstr.as_ref().map(|s| s.as_ptr() as u64).unwrap_or(0),
            target_cstr.as_ptr() as u64,
            fstype_cstr.as_ptr() as u64,
            0,
            0,
        )
        .map ...(+12) | Ok, SFileSystem::Dev, SFileSystem::Ext4, p.mount |

#### 设备驱动
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `anemone-kernel/src/device/bus/mod.rs#register_driver` | `ostd/src/bus/pci/bus.rs#register_driver` | 同板块同名 | BusType::base, BusType::base(self)
            .drivers
            .write_irqsave, BusType::base(self)
            .drivers
            .write_irqsave()
            .add_kobject, BusType::base(self).devices.read_irqsave, BusType::base(self).devices.read_irqsave().iter, Some ...(+11) | (0..length).rev, common_device.device_id, debug!, debug_assert!, driver.probe, error! ...(+6) |
| `anemone-kernel/src/driver/serial/ns16550a.rs#handle_irq` | `ostd/src/bus/device_manager.rs#handle_irq` | 同板块同名 | BOOKKEEPER.lock_irqsave, Ns16550ARegisters::from_raw, bookkeeper.deref, devices
            .get, devices
            .get(minor)
            .expect, devices
            .get(minor)
            .expect("invalid minor number in irq handler")
            .clone ...(+7) | dev.ack_interrupt, self.devices.get, self.irq_map.get, tracing::trace! |
| `anemone-kernel/src/device/bus/pcie/ecam.rs#bar` | `ostd/src/bus/pci/common_device.rs#bar` | 同板块同名 | Err, Ok, PciBar::try_from_u32, kwarningln!, self.read_u32 | Some, self.bars[idx as usize].clone |
| `anemone-kernel/src/driver/virtio/mod.rs#mmio_phys_to_virt` | `ostd/src/drivers/virtio/hal.rs#mmio_phys_to_virt` | 同板块同名 | NonNull::new_unchecked, PhysAddr::new, panic!, query_virt_addr, query_virt_addr(PhysAddr::new(paddr), size as u64).unwrap_or_else, vaddr.get | NonNull::new, NonNull::new(paddr_to_vaddr(paddr) as *mut u8).unwrap, paddr_to_vaddr |

#### 文件系统/第三方库
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `anemone-kernel/src/fs/ext4/mod.rs#read_blocks` | `kernel/comps/vfs/src/impls/ext4_fs/block_dev.rs#read_blocks` | 同板块同名 | LwExt4Error::new, Ok, buf.len, err.as_errno, self.dev
                .read_blocks, self.dev
                .read_blocks(block_id as usize, buf)
                .map_err | Ok, Self::blk2sec, self.inner.lock, self.inner.lock().read_blocks, self.inner.lock().read_blocks(sector, buf).unwrap |
| `anemone-kernel/src/fs/ext4/mod.rs#write_blocks` | `kernel/comps/vfs/src/impls/ext4_fs/block_dev.rs#write_blocks` | 同板块同名 | LwExt4Error::new, Ok, buf.len, err.as_errno, self.dev
                .write_blocks, self.dev
                .write_blocks(block_id as usize, buf)
                .map_err | Ok, Self::blk2sec, self.inner.lock, self.inner.lock().write_blocks, self.inner.lock().write_blocks(sector, buf).unwrap |
| `anemone-kernel/src/fs/ext4/mod.rs#flush` | `kernel/comps/another_ext4/src/ext4_defs/cache.rs#flush` | 同板块同名 | fs.flush, fs.flush().map_err, self.with_fs | cache[set_id].access, self.block_dev.write_block, self.cache.lock |
| `anemone-kernel/src/fs/ext4/file.rs#size` | `kernel/comps/another_ext4/src/ext4_defs/inode.rs#size` | 同板块同名 | self.size.load | 内部闭环逻辑 |

#### 网络协议栈/第三方库
仅参考项目中观察到该模块代表函数，目标项目没有同模块可对齐证据：
- 参考项目：`ostd/libs/virtio-drivers/src/device/socket/connectionmanager.rs#send_recv`，调用摘要："Hello from guest".as_bytes, (hello_from_guest.len() as u32).into, (hello_from_host.len() as u32).into, 0.into, 50.into, Arc::new ...(+43)
- 参考项目：`ostd/libs/virtio-drivers/src/device/socket/connectionmanager.rs#incoming_connection`，调用摘要：0.into, 50.into, Arc::new, Mutex::new, ReadOnly::new, SocketType::Stream.into ...(+24)
- 参考项目：`ostd/libs/virtio-drivers/src/device/socket/connectionmanager.rs#poll`，调用摘要：Connection::new, Err, Ok, SocketError::OutputBufferTooShort, SocketError::OutputBufferTooShort(length).into, Some ...(+16)
- 参考项目：`ostd/libs/virtio-drivers/examples/x86_64/src/tcp.rs#test_echo_server`，调用摘要：Config::new, DeviceWrapper::new, Instant::from_micros_const, Interface::new, IpAddress::from_str, IpAddress::from_str(IP).unwrap ...(+44)

#### 第三方库
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `anemone-kernel/crates/device-tree/src/parser.rs#parse` | `kernel/libs/typeflags/src/type_flag.rs#parse` | 同板块同名 | AllNodesAdapter::new, Layout::from_size_align, Layout::from_size_align(self.accumulated_bytes, align_of::<DeviceTree>()).unwrap, LinkedList::new, NonNull::dangling, NonNull::new_unchecked ...(+70) | Attribute::parse_outer, Ok, Token!, braced!, content.parse_terminated, input.parse |
| `anemone-kernel/crates/buddy-system/src/zone.rs#alloc` | `ostd/libs/id-alloc/src/lib.rs#alloc` | 同板块同名 | ((order + 1)..=o).rev, AlignedAddr::new, AlignedAddr::new(block.as_ptr() as usize)
                        .expect, Err, LinkedListLink::new, Ok ...(+13) | (id + 1..self.bitset.len())
                .find, (id + 1..self.bitset.len())
                .find(|&i| !self.bitset[i])
                .map_or, Some, self.bitset.len, self.bitset.set |
| `anemone-kernel/crates/range-allocator/src/lib.rs#free` | `ostd/libs/id-alloc/src/lib.rs#free` | 同板块同名 | Err, Ok, R::from_parts, r.clone, r.len, r.start ...(+9) | debug_assert!, self.bitset.set |
| `anemone-kernel/crates/kernel-macros/src/syscall.rs#parse` | `kernel/libs/typeflags/src/type_flag.rs#parse` | 同板块同名 | Err, Error::new_spanned, Ok, Some, Token!, input.is_empty ...(+6) | Attribute::parse_outer, Ok, Token!, input.parse |

#### 通用模块/其他
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `anemone-kernel/src/utils/writer.rs#write_str` | `ostd/src/arch/riscv/serial.rs#write_str` | 同板块同名 | Err, Ok, bytes.len, panic!, s.as_bytes, self.buf.len ...(+3) | Ok, s.as_bytes, send |
| `anemone-kernel/src/sync/spinlock.rs#lock` | `ostd/src/sync/guard_spin.rs#lock` | 同板块同名 | PreemptGuard::new, Some, core::hint::spin_loop, self.lock.try_lock | G::guard, self.acquire_lock |
| `anemone-kernel/src/utils/identity.rs#try_from` | `ostd/src/cpu/mod.rs#try_from` | 同板块同名 | Ok, Self, heapless::String::new, s.push_str, s.push_str(value).map_err | CpuId, Err, Ok, num_cpus |
| `anemone-kernel/src/utils/bitmap.rs#clear` | `kernel/src/vm/vmo/mod.rs#clear` | 同板块同名 | Self::position | Ok, VmReader::from, VmReader::from(buffer.as_slice()).to_fallible, buffer.as_slice, self.write, vec! |


### 审计结论：底层架构显著差异 / 独立开发

#### 1. 宏观判定
- **定性结论**: **独立开发**。双方项目在宏观架构、核心抽象、同步模型及错误处理范式上存在根本性差异。极低的 AST 相似度（0.83%）与覆盖率（目标 1.82%，参考 1.49%）表明，57 个共同 AST 指纹完全处于 Rust 语言标准库、POSIX 规范及硬件接口驱动的正常背景噪声范围内，不构成任何形式的代码复用或抄袭。

#### 2. 全模块覆盖矩阵解读
- **进程管理**: 双方均存在该模块。**共同唯一 AST 指纹为 0，覆盖率为 0.0%**。这是控方指控最集中的板块，但客观数据直接否定了任何结构性同源的可能。证据强度：**未形成**。
- **任务调度/异步运行时**: 双方均存在。共同指纹 1 个，覆盖率极低（目标 0.15%，参考 0.42%）。重合点仅为 Rust 生态中标准的 `xtask` 构建脚本入口函数名 `run`，其内部调用图谱完全不同。证据强度：**弱**。
- **内存管理**: 双方均存在。共同指纹 3 个，覆盖率低（目标 0.5%，参考 0.37%）。重合点源于同名函数（如 `map`、`try_from`），但底层操作对象（物理页帧 vs. ELF 映像、异常向量 vs. 帧段）完全不同。证据强度：**弱**。
- **中断/异常处理**: 双方均存在。**共同唯一 AST 指纹为 0，覆盖率为 0.0%**。尽管存在同名函数（如 `send_ipi`），但实现方式（SBI 调用 vs. 队列/IPI 线）完全不同。证据强度：**未形成**。
- **系统调用**: 双方均存在。**共同唯一 AST 指纹为 0，覆盖率为 0.0%**。双方均实现了 POSIX/Linux 标准系统调用，但用户态内存访问、错误处理等核心机制截然不同。证据强度：**未形成**。
- **文件系统**: 双方均存在。共同指纹 3 个，覆盖率低（目标 0.47%，参考 0.76%）。重合点集中在 ext4 块设备的 `read_blocks`/`write_blocks` 等标准操作，以及 `mount`/`unmount` 等 VFS 通用接口，其内部实现与锁机制完全不同。证据强度：**弱**。
- **设备驱动**: 双方均存在。**共同唯一 AST 指纹为 0，覆盖率为 0.0%**。尽管存在 `register_driver`、`handle_irq` 等同名函数，但驱动模型（总线类型 vs. 设备管理器）和硬件操作完全不同。证据强度：**未形成**。
- **网络协议栈**: **仅参考项目体现**，目标项目无此模块。证据强度：**未形成**。
- **第三方库/公共依赖**: 双方均存在。共同指纹 3 个，覆盖率低（目标 1.38%，参考 0.21%）。重合点源于 `parse`、`alloc`、`free` 等通用函数名，但解析对象（设备树 vs. 类型标志）和分配器算法（伙伴系统 vs. ID 位图）完全不同。证据强度：**弱**。

#### 3. 底层 AST 关键证据指纹表
| 所属板块 | 目标项目路径 | 参考项目路径 | 证据类型 | 技术说明 |
| :--- | :--- | :--- | :--- | :--- |
| 进程管理 | `anemone-kernel/src/task/api/exit/mod.rs#kernel_exit` | `kernel/src/thread/exit.rs#do_exit` | 同板块上下文对齐/差异观察 | 共同 AST 指纹为 0。目标使用 `ThreadGroupLifeCycle` + `UserWritePtr`，参考使用 `shared_info.lifecycle` + `process_vm`，抽象层完全不同。 |
| 进程管理 | `anemone-kernel/src/task/sig/api/rt_sigprocmask.rs#sys_rt_sigprocmask` | `kernel/src/syscall/signal.rs#do_rt_sigprocmask` | 同板块上下文对齐/差异观察 | 共同 AST 指纹为 0。目标使用 `UserReadPtr/UserWritePtr` 安全指针，参考使用 `process_vm.read_val/write_val`，用户态访问模型异构。 |
| 任务调度/异步运行时 | `scripts/xtask/src/tasks/qemu.rs#run` | `osdk/src/bundle/mod.rs#run` | 同板块同名 | 共同指纹 1。Rust 生态标准 `xtask` 入口函数名，内部调用图谱（`PlatformConfig` vs. `Command`）完全不同。 |
| 内存管理 | `anemone-kernel/src/mm/uspace/mod.rs#handle_page_fault` | `kernel/src/vm/vmar/vm_mapping.rs#handle_page_fault` | 同板块同名 | 共同指纹 3 之一。同名缺页处理函数，目标返回 `Result`，参考返回 `Ok`，错误处理范式不同。 |
| 文件系统 | `anemone-kernel/src/fs/ext4/mod.rs#read_blocks` | `kernel/comps/vfs/src/impls/ext4_fs/block_dev.rs#read_blocks` | 同板块同名 | 共同指纹 3 之一。ext4 标准块读取操作，目标直接调用设备层，参考通过内部锁进行，同步策略不同。 |
| 设备驱动 | `anemone-kernel/src/device/bus/mod.rs#register_driver` | `ostd/src/bus/pci/bus.rs#register_driver` | 同板块同名 | 共同 AST 指纹为 0。目标基于 `BusType` 的 kobject 模型，参考基于 PCI 总线扫描，驱动架构完全不同。 |
| 第三方库 | `anemone-kernel/crates/buddy-system/src/zone.rs#alloc` | `ostd/libs/id-alloc/src/lib.rs#alloc` | 同板块同名 | 共同指纹 3 之一。同名分配函数，目标实现伙伴系统算法，参考实现 ID 位图分配，算法与数据结构完全不同。 |

#### 4. 核心代码调用流对比剖析
* **【进程管理/exit 系统调用】**
* **底层技术析理**: 目标项目 `kernel_exit` 采用全局 `TOPOLOGY` 集中管理进程树，通过 `ThreadGroupLifeCycle` 状态机驱动退出，并使用 `UserWritePtr<Tid>` 将退出状态安全地写入用户态。参考项目 `do_exit` 则通过分散的 `state.shared_info.lifecycle` 管理生命周期，依赖 `process_vm` 对象进行用户态内存读写。两者在并发控制（`NoIrqRwLock` vs. 内部锁）、错误处理（`Result` + `kdebugln!` vs. `ControlFlow::Break` + `log::info!`）上存在系统性差异。
* **代码溯源链接**:
  - 目标项目: `anemone-kernel/src/task/api/exit/mod.rs#kernel_exit`
  - 参考项目: `kernel/src/thread/exit.rs#do_exit`

* **【任务调度/异步运行时/yield 机制】**
* **底层技术析理**: 目标项目 `yield_now` 通过 `with_intr_disabled` 关中断并直接调用 `schedule` 进行上下文切换，属于传统的协程调度模型。参考项目 `yield_now` 则基于 `maitake` 异步运行时，调用 `maitake::future::yield_now` 并配合 `cpu_local::clear_need_preempt` 实现协作式调度。两者在调度器底层实现上完全不同。
* **代码溯源链接**:
  - 目标项目: `anemone-kernel/src/sched/mod.rs#yield_now`
  - 参考项目: `ostd/src/task/scheduler/mod.rs#yield_now`

* **【内存管理/缺页异常处理】**
* **底层技术析理**: 目标项目 `handle_page_fault` 在 `VirtPageRange` 上操作，通过 `find_vma_raw_mut` 查找虚拟内存区域，返回 `Result` 类型。参考项目 `handle_page_fault` 则直接操作 `PageProperty` 和 `VmPerms`，并执行 `TlbFlushOp`。两者在 VMA 管理、权限检查及 TLB 刷新策略上采用不同实现路径。
* **代码溯源链接**:
  - 目标项目: `anemone-kernel/src/mm/uspace/mod.rs#handle_page_fault`
  - 参考项目: `kernel/src/vm/vmar/vm_mapping.rs#handle_page_fault`

* **【文件系统/挂载操作】**
* **底层技术析理**: 目标项目 `mount` 通过 `Mount::new` 创建挂载点并直接关联 `Dentry`，采用显式的 dentry 树管理。参考项目 `mount` 则通过 `InMemoryFileSystem::new` 创建文件系统实例，并返回 `Arc::downgrade` 弱引用。两者在 VFS 挂载管理模型上存在根本差异。
* **代码溯源链接**:
  - 目标项目: `anemone-kernel/src/fs/mod.rs#mount`
  - 参考项目: `kernel/comps/vfs/src/impls/memfs/provider.rs#mount`

* **【第三方库/内存分配器】**
* **底层技术析理**: 目标项目 `alloc` 实现伙伴系统（Buddy System），通过 `AlignedAddr` 和 `LinkedListLink` 管理不同阶的空闲块。参考项目 `alloc` 实现基于位图的 ID 分配器，通过 `bitset` 的 `set` 和 `find` 操作分配空闲 ID。两者在分配算法和数据结构上完全不同。
* **代码溯源链接**:
  - 目标项目: `anemone-kernel/crates/buddy-system/src/zone.rs#alloc`
  - 参考项目: `ostd/libs/id-alloc/src/lib.rs#alloc`

#### 5. 单方模块与降权项
- **单方模块**: **网络协议栈/第三方库** 仅存在于参考项目，目标项目无此模块，不构成任何同源嫌疑。
- **降权项**: 所有跨板块同名函数（如 `run`, `yield_now`, `claim`, `complete`, `set`, `get`, `clear`）均因属于 Rust 生态惯例、硬件规范接口或 POSIX 标准行为，且内部调用图谱完全不同，已被降级为弱信号或不形成证据。第三方库中的同名函数（如 `parse`, `alloc`, `free`）因解析对象和算法实现完全不同，同样不构成强同源证据。

#### 6. 保守结论
本次审计基于 0.83% 的极低 AST 相似度及双方模块画像的深度对比，认定目标项目 `[2026-T2026181239910972-哈尔滨工业大学深圳]` 与参考项目 `[2025-NexusOS-郑州大学]` 为**独立开发**。57 个共同 AST 指纹完全可归因于 Rust 语言标准库、Linux/POSIX 系统调用规范及 RISC-V 硬件接口驱动的正常趋同。控方在【进程管理】板块的指控被模块覆盖矩阵中 **0 共同指纹 / 0% 覆盖率** 的客观数据直接否定。双方在核心架构上存在根本性差异：目标项目采用全局 `TOPOLOGY` 集中式进程管理与 `UserReadPtr/UserWritePtr` 安全指针抽象，参考项目则采用分散的 `shared_info` 与 `process_vm` 对象模型。整体代码库无任何系统性复用或掩饰性重构的风险。

------------------------------

