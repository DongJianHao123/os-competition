# 🛡️ OS 内核赛自动化代码级架构审计报告

**审计时间**: `2026-06-27 17:55:54`
**参赛作品名称**: `2026-T2026181239910972-哈尔滨工业大学深圳`
**年份**: `2026`
**学校**: `哈尔滨工业大学（深圳）`
**队伍名称**: `T2026181239910972`
**比赛名字**: `OS-Competition`
**比赛子赛道**: `Kernel-Implementation`
**仓库地址**: `https://gitlab.eduxiji.net/educg-group-43501-3132633/T2026181239910972-2383`
**推测开发语言**: `Rust / C (由AST探针自动识别)`
**特征函数量**: `3608`

## ⚔️ 底层代码同源性对比 (AST Homology)
> 本部分由专门的代码审查智能体，对该项目与符合年份条件的历届最高相似度项目进行控制流与底层调用对比。新版报告会先给出确定性的全模块覆盖矩阵，再给出 Agent 的分模块解释，避免只围绕少量抽样函数展开。

### 🏆 Top 3 底层对标: vs `2025-StarryX-杭州电子科技大学`（杭州电子科技大学 / StarryX）

#### 对方作品信息

**对方年份**: `2025`
**对方学校**: `杭州电子科技大学`
**对方队伍名称**: `StarryX`
**对方比赛名字**: `OS-Competition`
**对方比赛子赛道**: `Kernel-Implementation`
**对方仓库地址**: `https://gitlab.eduxiji.net/educg-group-36002-2710490/T202510336995486-3037`

> 双向覆盖率：共同 AST 指纹: 53；唯一 AST 指纹: 目标项目 3135，参考项目 2897；覆盖率: 目标项目 1.69%，参考项目 1.83%

## 全模块对比总览（确定性证据）

> 总体覆盖率：共同 AST 指纹: 53；唯一 AST 指纹: 目标项目 3135，参考项目 2897；覆盖率: 目标项目 1.69%，参考项目 1.83%

### 1. 模块覆盖矩阵
| 模块 | 目标函数数 | 参考函数数 | 共同唯一 AST 指纹 | Target 覆盖 | Base 覆盖 | 状态 |
| :--- | ---: | ---: | ---: | ---: | ---: | :--- |
| 进程管理 | 289 | 172 | 1 | 0.4% | 0.63% | 双方存在且有结构重合 |
| 任务调度/异步运行时 | 708 | 480 | 1 | 0.15% | 0.28% | 双方存在且有结构重合 |
| 内存管理 | 633 | 430 | 6 | 1.0% | 1.54% | 双方存在且有结构重合 |
| 中断/异常处理 | 169 | 70 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |
| 系统调用 | 95 | 89 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |
| 文件系统 | 692 | 801 | 2 | 0.32% | 0.32% | 双方存在且有结构重合 |
| 设备驱动 | 417 | 139 | 1 | 0.28% | 0.79% | 双方存在且有结构重合 |
| 网络协议栈 | 0 | 77 | 0 | 0.0% | 0.0% | 仅参考项目体现 |
| 文件系统/第三方库 | 75 | 164 | 1 | 1.37% | 0.64% | 双方存在且有结构重合 |
| 网络协议栈/第三方库 | 0 | 166 | 0 | 0.0% | 0.0% | 仅参考项目体现 |
| 第三方库 | 244 | 349 | 2 | 0.92% | 0.68% | 双方存在且有结构重合 |
| 通用模块/其他 | 286 | 665 | 6 | 2.4% | 1.13% | 双方存在且有结构重合 |

### 2. 分模块证据清单

#### 进程管理
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `anemone-kernel/src/task/topology/process_group.rs#create_session` | `xmodules/xprocess/src/process.rs#create_session` | 同板块同名 | Arc::new, BTreeSet::from, Err, NoIrqRwLock::new, Ok, TOPOLOGY.inner.write ...(+18) | ProcessGroup::new, Session::new, Some, self.group.lock, self.group.lock().session.sid, self.set_group |
| `anemone-kernel/src/task/sig/api/rt_sigprocmask.rs#sys_rt_sigprocmask` | `xapi/src/task/signal.rs#sys_rt_sigprocmask` | 同板块同名 | Err, Ok, SigSet::new_with_mask, UserReadPtr::<linux_signal::SigSet>::try_new, UserWritePtr::<linux_signal::SigSet>::try_new, get_current_task ...(+12) | Err, Ok, XProcess::from_thread, XProcess::from_thread(thread).uspace, XThread::from_thread, check_sigset_size ...(+3) |
| `anemone-rs/src/os/linux.rs#execve` | `arceos/ulib/axlibc/c/unistd.c#execve` | 同板块同名 | CString::new, CString::new(*arg).map_err, CString::new(*env).map_err, CString::new(path).map_err, arg.as_ptr, argv
            .iter ...(+19) | unimplemented |
| `anemone-kernel/src/task/files.rs#fork` | `xmodules/xprocess/src/process.rs#fork` | 同板块同名 | Arc::new, FileDesc::new, fd.fd_flags, fd.pfile.clone, fd_opt
                    .as_ref, fd_opt
                    .as_ref()
                    .map ...(+4) | Box::new, Some, self.clone |

#### 任务调度/异步运行时
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `anemone-kernel/src/task/api/futex/futex.rs#sys_futex` | `xapi/src/task/futex.rs#sys_futex` | 同板块同名 | (user_addr.nullable()), Duration::new, Duration::new(tv_sec as u64, tv_nsec as u32)
                    .saturating_sub, Err, Instant::now, Instant::now().to_duration ...(+19) | Err, FutexKey::new, Ok, axtask::yield_now, current, current().task_ext ...(+24) |
| `anemone-kernel/src/fs/api/iomux/pselect6.rs#sys_pselect6` | `xapi/src/iomux/select.rs#sys_pselect6` | 同板块同名 | (|| -> Result<u64, SysError> {
        let nready = wait_for_iomux_ready("sys_pselect6", &task, timeout, |mode| {
            scan_pselect_fds(
                &task,
                n,
                in_interests.as_ref(),
                out_interests.as_ref(),
                exp_interests.as_ref(),
                &mut in_ready,
                &mut out_ready,
                &mut exp_ready,
                mode,
            )
        })?;

        Ok(nready as u64)
    }), Bitmap::new_with, Box::new, Duration::new, Err, FdBitmap::new ...(+51) | do_select, nullable!, nullable!(uspace.read(timeout))?
                .map, nullable!(uspace.read(timeout))?
                .map(timespec::to_time_value)
                .transpose, with_uspace |
| `anemone-kernel/src/task/sig/api/rt_sigaction.rs#sys_rt_sigaction` | `xapi/src/task/signal.rs#sys_rt_sigaction` | 同板块同名 | Err, Ok, SaFlags::from_bits, SaFlags::from_bits(sa_flags as u32 as u64).ok_or_else, SigSet::new_with_mask, SigSet::new_with_signos ...(+27) | Err, Ok, act.try_into, actions[signo].to_ctype, check_sigset_size, debug! ...(+6) |
| `anemone-kernel/src/task/sig/api/sigaltstack.rs#sys_sigaltstack` | `xapi/src/task/signal.rs#sys_sigaltstack` | 同板块同名 | Err, Ok, SigAltStack::new, SigAltStackFlags::try_from_linux_bits, Some, UserReadPtr::<linux_signal::SigStack>::try_new ...(+18) | Err, Ok, XProcess::from_thread, XProcess::from_thread(thread).uspace, XThread::from_thread, XThread::from_thread(thread).signal.with_stack_mut ...(+4) |

#### 内存管理
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `anemone-kernel/src/mm/uspace/api/mmap.rs#sys_mmap` | `xapi/src/mm/mmap.rs#sys_mmap` | 同板块同名 | Err, Fd::new, Fd::new(raw_fd as u32).ok_or, Ok, Protection::empty, a.page_down ...(+26) | (addr + length).align_up, Err, FD_TABLE.is_assigned, File::from_fd, File::from_fd(fd, FileFlags::READ, FileFlags::empty())
            .map_err, FileFlags::empty ...(+39) |
| `anemone-kernel/src/mm/paging/mapper.rs#map` | `arceos/modules/axmm/src/backend/mod.rs#map` | 同板块同名 | (0..=max_hpage_level).rev, (vpn.get() ^ ppn.get()).trailing_zeros, ManuallyDrop::new, MapTransaction::new, Ok, PhysPageNum::new ...(+12) | Self::map_alloc, Self::map_linear, Self::map_shared |
| `anemone-kernel/src/mm/uspace/mod.rs#handle_page_fault` | `src/mm.rs#handle_page_fault` | 同板块同名 | Err, Ok, Self::find_vma_raw_mut, Self::find_vma_raw_mut(vmas, fault_addr).ok_or, Some, VirtPageRange::new ...(+14) | (xcore::config::USER_STACK_TOP - xcore::config::USER_STACK_SIZE
        ..xcore::config::USER_STACK_TOP)
        .contains, SignalInfo::new, Signo::from_repr, Signo::from_repr(SIGSEGV as u8).unwrap, XTaskExt::from_task, current ...(+17) |
| `anemone-kernel/src/mm/uspace/shm/api/shmat.rs#sys_shmat` | `xapi/src/ipc/shm.rs#sys_shmat` | 同板块同名 | Err, Ok, Some, VirtAddr::new, addr.get, addr.page_down ...(+18) | Err, IPC_MANAGER.with_shm, Ok, ShmAtFlags::from_bits_truncate, VirtAddr::from, VirtAddrRange::new ...(+17) |

#### 中断/异常处理
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `anemone-kernel/src/driver/pcie_drivers/platform.rs#probe` | `arceos/ulib/axlibc/c/pow.c#exp_inline` | 同模块上下文对齐/差异观察 | (0..addr_cells_parent)
                .map, (0..addr_cells_parent)
                .map(|j| u32::from_be_bytes(mem_addr[(j as usize * 4)..][..4].try_into().unwrap()))
                .fold, (0..size_cells)
                .map, (0..size_cells)
                .map(|j| u32::from_be_bytes(size[(j as usize * 4)..][..4].try_into().unwrap()))
                .fold, Arc::new, Box::from ...(+91) | __math_oflow, __math_uflow, asdouble, asuint64, converttoint, eval_as_double ...(+4) |
| `anemone-kernel/src/driver/serial/ns16550a.rs#try_drain_irq` | `xmodules/xsignal/src/types.rs#default_action` | 同模块上下文对齐/差异观察 | self.read_reg | 内部闭环逻辑 |
| `anemone-kernel/src/arch/loongarch64/bootstrap.rs#register_debugcon` | `arceos/modules/axhal/src/arch/aarch64/trap.rs#handle_sync_exception` | 同模块上下文对齐/差异观察 | Arc::new, Dmw::from_u64, Ns16550ARegisters::from_raw, SpinLock::new, dmw2::csr_write, register_console ...(+2) | ESR_EL1.extract, crate::trap::handle_syscall, crate::trap::post_trap_callback, debug!, esr.read, esr.read_as_enum ...(+6) |
| `anemone-kernel/src/driver/pcie_drivers/bus.rs#create_and_preinit_endpoint` | `xcore/src/fs/vfs/dev/tty.rs#default_termios` | 同模块上下文对齐/差异观察 | Arc::new, KObjIdent::try_from_fmt, KObjIdent::try_from_fmt(format_args!("{}:{}", domain.id(), identifier)).unwrap, PcieDevice::new_endpoint, Some, device.set_parent ...(+10) | 内部闭环逻辑 |

#### 系统调用
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `anemone-kernel/src/mm/uspace/shm/api/shmctl.rs#sys_shmctl` | `xapi/src/ipc/shm.rs#sys_shmctl` | 同板块同名 | Ok, ShmId::from_raw, ShmSlotIndex::from_linux_stat_target, check_access, ipc_info, ipc_set_update_from_linux ...(+16) | Err, IPC_MANAGER.with_shm, Ok, buf.is_null, nullable!, segment.lock ...(+5) |
| `anemone-kernel/src/debug/api/syslog.rs#sys_syslog` | `xapi/src/sys/common.rs#sys_syslog` | 同板块同名 | Ok, knoticeln! | Ok |
| `anemone-rs/src/sys/linux.rs#dup3` | `arceos/ulib/axlibc/src/fd_ops.rs#dup3` | 同板块同名 | syscall | (LinuxError::EINVAL as c_int).wrapping_neg, e, sys_dup2, sys_fcntl |
| `anemone-rs/src/sys/anemone.rs#shutdown` | `arceos/ulib/axlibc/src/net.rs#shutdown` | 同板块同名 | syscall, syscall(SYS_POWER_SHUTDOWN, magic, 0, 0, 0, 0, 0).map | e, sys_shutdown |

#### 文件系统
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `anemone-kernel/src/fs/api/linkat.rs#sys_linkat` | `xapi/src/fs/ctl.rs#sys_linkat` | 同板块同名 | FsPermChecker::for_current_fs, Ok, Path::new, checker.check_path, kdebugln!, new_parent.mount ...(+6) | Err, Ok, fs.resolve_nonexistent, location.is_dir, new_dir.link, new_path.into ...(+7) |
| `anemone-kernel/src/fs/api/openat.rs#sys_openat` | `arceos/api/arceos_posix_api/src/imp/fs.rs#sys_openat` | 同板块同名 | FsPermChecker::for_current_fs, OpenHow::from_linux, Path::new, create_or_open_path, file_for_path, finish_open ...(+3) | char_ptr_to_str, debug!, syscall_body! |
| `anemone-rs/src/env.rs#current_dir` | `arceos/ulib/axstd/src/env.rs#current_dir` | 同板块同名 | Err, String::from_utf8, String::from_utf8(buf[..len].to_vec())
                    .map, String::from_utf8(buf[..len].to_vec())
                    .map(PathBuf::from)
                    .map_err, buf.iter, buf.iter().position ...(+6) | arceos_api::fs::ax_current_dir |
| `symtab/src/lib.rs#lookup` | `xcore/src/fs/vfs/virt_file.rs#lookup` | 同板块同名 | Some, SymbolType::from, self.entries, self.strtab, strtab.get_str, symbols.binary_search_by_key | DirEntry::new_dir, DirEntry::new_file, DirNode::new, Err, FileNode::new, Ok ...(+10) |

#### 设备驱动
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `anemone-kernel/src/driver/virtio/pcie.rs#probe` | `arceos/modules/axdriver/src/lib.rs#probe` | 同板块同名 | (*bus).into, (*dev).into, Arc::new, DeviceBase::new, Err, FuncNum::MIN.into ...(+25) | for_each_drivers!, self.probe_bus_devices |
| `anemone-kernel/src/device/bus/mod.rs#register_driver` | `arceos/tools/raspi4/chainloader/src/driver.rs#register_driver` | 同板块同名 | BusType::base, BusType::base(self)
            .drivers
            .write_irqsave, BusType::base(self)
            .drivers
            .write_irqsave()
            .add_kobject, BusType::base(self).devices.read_irqsave, BusType::base(self).devices.read_irqsave().iter, Some ...(+11) | Some, self.inner.lock |
| `anemone-kernel/src/driver/virtio/mod.rs#mmio_phys_to_virt` | `arceos/modules/axdriver/src/ixgbe.rs#mmio_phys_to_virt` | 同板块同名 | NonNull::new_unchecked, PhysAddr::new, panic!, query_virt_addr, query_virt_addr(PhysAddr::new(paddr), size as u64).unwrap_or_else, vaddr.get | NonNull::new, NonNull::new(phys_to_virt(paddr.into()).as_mut_ptr()).unwrap, paddr.into, phys_to_virt, phys_to_virt(paddr.into()).as_mut_ptr |
| `anemone-kernel/src/device/block/loop.rs#ioctl` | `arceos/tools/bwbench_client/src/device.rs#ioctl` | 同板块同名 | Err, ctx.cmd, self.clear_fd, self.get_status, self.set_fd, self.set_status | Err, Ok, libc::ioctl, std::io::Error::last_os_error |

#### 网络协议栈
仅参考项目中观察到该模块代表函数，目标项目没有同模块可对齐证据：
- 参考项目：`arceos/ulib/axlibc/c/network.c#inet_pton`，调用摘要：hexval, inet_pton, isdigit, memmove
- 参考项目：`xcore/src/net/socket.rs#sendto`，调用摘要：Err, Ipv4Addr::LOCALHOST.into, Ok, SocketAddr::new, inner
                    .bind, inner
                    .bind(SocketAddr::new(Ipv4Addr::LOCALHOST.into(), 0))
                    .ok ...(+2)
- 参考项目：`xcore/src/net/socket.rs#recvfrom`，调用摘要：Ok, Some, tcpsocket.lock, tcpsocket.lock().recv, tcpsocket.lock().recv(buf).map, udpsocket
                .lock ...(+5)
- 参考项目：`arceos/api/arceos_posix_api/src/imp/net.rs#shutdown`，调用摘要：Ok, tcpsocket.lock, tcpsocket.peer_addr, tcpsocket.shutdown, udpsocket.lock, udpsocket.peer_addr ...(+1)

#### 文件系统/第三方库
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `anemone-kernel/src/fs/ext4/mod.rs#read_blocks` | `arceos/modules/axfs-ng/src/fs/ext4/mod.rs#read_blocks` | 同板块同名 | LwExt4Error::new, Ok, buf.len, err.as_errno, self.dev
                .read_blocks, self.dev
                .read_blocks(block_id as usize, buf)
                .map_err | Ext4Error::new, Ok, block.copy_from_slice, buf.chunks_mut, buf.chunks_mut(EXT4_DEV_BSIZE).enumerate, buf.len ...(+2) |
| `anemone-kernel/src/fs/ext4/mod.rs#write_blocks` | `arceos/modules/axfs-ng/src/fs/ext4/mod.rs#write_blocks` | 同板块同名 | LwExt4Error::new, Ok, buf.len, err.as_errno, self.dev
                .write_blocks, self.dev
                .write_blocks(block_id as usize, buf)
                .map_err | Ext4Error::new, Ok, block_buf.copy_from_slice, buf.chunks, buf.chunks(EXT4_DEV_BSIZE).enumerate, buf.len ...(+2) |
| `anemone-kernel/src/fs/ext4/mod.rs#num_blocks` | `arceos/modules/axfs-ng/src/fs/ext4/mod.rs#num_blocks` | 同板块同名 | Ok, self.dev.total_blocks | Ok, self.0.num_blocks |
| `anemone-kernel/src/fs/ext4/file.rs#size` | `arceos/crates/lwext4_rust/src/inode/attr.rs#size` | 同板块同名 | self.size.load | ext4_inode_get_size, self.superblock |

#### 网络协议栈/第三方库
仅参考项目中观察到该模块代表函数，目标项目没有同模块可对齐证据：
- 参考项目：`arceos/modules/axnet/src/smoltcp_impl/tcp.rs#connect`，调用摘要：Err, Ok, Ok::<(IpEndpoint, IpEndpoint), NetError>, SOCKET_SET
                .with_socket_mut::<tcp::Socket, _, _>, SOCKET_SET.add, SocketSetWrapper::new_tcp_socket ...(+33)
- 参考项目：`arceos/modules/axnet/src/smoltcp_impl/dns.rs#query`，调用摘要：Err, IpAddr::from, Ok, SOCKET_SET
            .with_socket_mut::<dns::Socket, _, _>, SOCKET_SET
            .with_socket_mut::<dns::Socket, _, _>(handle, |socket| {
                socket.start_query(iface.lock().context(), name, query_type)
            })
            .map_err, SOCKET_SET.poll_interfaces ...(+11)
- 参考项目：`arceos/modules/axnet/src/smoltcp_impl/bench.rs#bench_transmit_bandwidth`，调用摘要：(current_time - past_time).secs, AxNetTxToken::consume, InterfaceWrapper::current_time, info!, self.transmit, tx_buf[0..12].fill ...(+2)
- 参考项目：`arceos/modules/axnet/src/smoltcp_impl/tcp.rs#recv_timeout`，调用摘要：Err, Ok, SOCKET_SET.with_socket_mut::<tcp::Socket, _, _>, current_ticks, self.block_on, self.handle.get ...(+9)

#### 第三方库
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `anemone-kernel/crates/buddy-system/src/zone.rs#from_slice` | `arceos/crates/axdriver_crates/axdriver_display/src/lib.rs#from_slice` | 同板块同名 | (&mut *hdr_ptr).inner.get_mut, (0..NORDER).rev, AlignedAddr::align_up, LinkedListLink::new, UnsafeRef::from_raw, align_of::<Self> ...(+19) | 内部闭环逻辑 |
| `anemone-kernel/crates/buddy-system/src/zone.rs#dealloc` | `arceos/crates/axdriver_crates/axdriver_net/src/net_buf.rs#dealloc` | 同板块同名 | AlignedAddr::<MIN_BLOCK_BYTES>::new, AlignedAddr::<MIN_BLOCK_BYTES>::new(addr.as_ptr() as usize)
            .ok_or, Err, FreeBlock::buddy_block_idx, LinkedListLink::new, Ok ...(+29) | debug_assert_eq!, self.free_list.lock, self.free_list.lock().push |
| `anemone-kernel/crates/buddy-system/src/zone.rs#alloc` | `arceos/crates/axdriver_crates/axdriver_net/src/net_buf.rs#alloc` | 同板块同名 | ((order + 1)..=o).rev, AlignedAddr::new, AlignedAddr::new(block.as_ptr() as usize)
                        .expect, Err, LinkedListLink::new, Ok ...(+13) | Arc::clone, NonNull::new, NonNull::new(self.pool.as_ptr().add(pool_offset) as *mut u8).unwrap, Some, self.free_list.lock, self.free_list.lock().pop ...(+2) |
| `anemone-kernel/crates/idalloc/src/stack.rs#alloc` | `arceos/crates/axdriver_crates/axdriver_pci/src/lib.rs#alloc` | 同板块同名 | Some, self
                .end_exclusive
                .is_some_and, self.free_list.pop | Some, align_up, size.is_power_of_two |

#### 通用模块/其他
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `anemone-kernel/src/utils/writer.rs#write_str` | `arceos/modules/axlog/src/lib.rs#write_str` | 同板块同名 | Err, Ok, bytes.len, panic!, s.as_bytes, self.buf.len ...(+3) | Ok, cfg_if::cfg_if! |
| `anemone-rs/src/os/linux.rs#sigaction` | `arceos/ulib/axlibc/c/signal.c#sigaction` | 同板块同名 | Some, act.map_or, oldact
                    .and_then, oldact
                    .and_then(|o| Some(o as *mut SigAction as u64))
                    .unwrap_or, sig.as_usize, signal::rt_sigaction ...(+2) | sigaction_helper |
| `anemone-kernel/src/sync/spinlock.rs#lock` | `arceos/modules/axsync/src/mutex.rs#lock` | 同板块同名 | PreemptGuard::new, Some, core::hint::spin_loop, self.lock.try_lock | assert_ne!, current, current().id, current().id().as_u64, self.is_locked, self.owner_id.compare_exchange_weak ...(+1) |
| `anemone-kernel/src/arch/mod.rs#clear_bss` | `arceos/modules/axhal/src/mem.rs#clear_bss` | 同板块同名 | bss_start.add, bss_start.add(i).write_volatile | core::slice::from_raw_parts_mut, core::slice::from_raw_parts_mut(_sbss as usize as *mut u8, _ebss as usize - _sbss as usize)
            .fill |


### 1. 宏观判定
- **定性结论**: **独立开发 / 底层架构显著差异**
- **核心依据**: 客观 AST 相似度仅为 0.89%，全项目共同 AST 指纹仅 53 个，目标项目与参考项目的覆盖率分别低至 1.69% 和 1.83%。在争议最集中的进程管理板块，覆盖率仅 0.4%–0.63%。该数据量级属于统计学噪声，无法支撑“抄袭/复用”的指控。双方在任务生命周期、进程拓扑、锁机制、用户态指针抽象及调度模型等核心架构维度上存在系统性差异。

### 2. 全模块覆盖矩阵解读
- **进程管理**: 双方均存在。存在 1 个共同 AST 指纹。覆盖率极低（目标 0.4%，参考 0.63%）。控方指控的函数调用链在状态机、锁原语和指针抽象上完全不同，证据强度为**弱**。
- **任务调度/异步运行时**: 双方均存在。存在 1 个共同 AST 指纹。覆盖率极低（目标 0.15%，参考 0.28%）。双方在超时等待机制上使用了不同的时间源和调度原语，证据强度为**弱**。
- **内存管理**: 双方均存在。存在 6 个共同 AST 指纹。覆盖率低（目标 1.0%，参考 1.54%）。同名函数如 `map` 和 `handle_page_fault` 在实现上分属不同的后端抽象和错误处理模型，证据强度为**弱**。
- **中断/异常处理**: 双方均存在。未观察到共同结构指纹。覆盖率均为 0.0%。证据强度为**未形成**。
- **系统调用**: 双方均存在。未观察到共同结构指纹。覆盖率均为 0.0%。同名函数多为 POSIX 标准接口的极简封装，证据强度为**未形成**。
- **文件系统**: 双方均存在。存在 2 个共同 AST 指纹。覆盖率极低（目标 0.32%，参考 0.32%）。同名函数在路径解析和文件操作上使用了不同的内部抽象，证据强度为**弱**。
- **设备驱动**: 双方均存在。存在 1 个共同 AST 指纹。覆盖率低（目标 0.28%，参考 0.79%）。同名函数如 `probe` 和 `mmio_phys_to_virt` 在驱动模型和物理内存映射上实现不同，证据强度为**弱**。
- **网络协议栈**: 仅参考项目体现。目标项目无此模块。覆盖率均为 0.0%。证据强度为**未形成**。
- **第三方库/公共依赖**: 双方均存在。存在 3 个共同 AST 指纹。覆盖率低（目标约 1.1%，参考约 0.66%）。同名函数如 `alloc`/`dealloc` 在分配器实现上存在本质差异，证据强度为**弱**。

### 3. 底层 AST 关键证据指纹表
| 所属板块 | 目标项目路径 | 参考项目路径 | 证据类型 | 技术说明 |
| :--- | :--- | :--- | :--- | :--- |
| 进程管理 | `anemone-kernel/src/task/topology/process_group.rs#create_session` | `xmodules/xprocess/src/process.rs#create_session` | 同板块同名 | 同名源于 POSIX 概念。目标使用集中式 `TOPOLOGY` + `NoIrqRwLock`，参考使用进程内部锁 `self.group.lock` + `SpinNoIrq`，拓扑管理模型异构。 |
| 进程管理 | `anemone-kernel/src/task/sig/api/rt_sigprocmask.rs#sys_rt_sigprocmask` | `xapi/src/task/signal.rs#sys_rt_sigprocmask` | 同板块同名 | 同名源于系统调用。目标使用类型化指针 `UserReadPtr`/`UserWritePtr`，参考使用 `XProcess`/`XThread` 上下文，用户态交互抽象不同。 |
| 进程管理 | `anemone-kernel/src/sched/wait.rs#wake_wait` | `arceos/modules/axtask/src/wait_queue.rs#cancel_events` | 同板块证据 | 目标基于 `WakeCommit` 枚举的显式状态提交模型，参考基于 `queue.lock().retain` 的队列清理模型，状态机实现路径不同。 |
| 任务调度/异步运行时 | `anemone-kernel/src/sched/mod.rs#schedule_wait_with_timeout` | `arceos/modules/axtask/src/wait_queue.rs#wait_timeout_until` | 同板块证据 | 目标使用 `Instant::now` + `schedule_local_irq_timer_event`，参考使用 `wall_time` + `set_alarm_wakeup`，时间源与调度原语异构。 |
| 内存管理 | `anemone-kernel/src/mm/uspace/mod.rs#handle_page_fault` | `src/mm.rs#handle_page_fault` | 同板块同名 | 目标通过 `VirtPageRange` 和 `find_vma_raw_mut` 处理缺页，参考通过栈范围检查和 `SignalInfo` 发送信号，错误处理模型不同。 |
| 内存管理 | `anemone-kernel/src/mm/paging/mapper.rs#map` | `arceos/modules/axmm/src/backend/mod.rs#map` | 同板块同名 | 目标实现手动页表项操作与 `MapTransaction`，参考分发至 `map_alloc`/`map_linear`/`map_shared` 等后端，分派策略不同。 |
| 文件系统 | `anemone-kernel/src/fs/api/openat.rs#sys_openat` | `arceos/api/arceos_posix_api/src/imp/fs.rs#sys_openat` | 同板块同名 | 目标使用 `OpenHow`、`Path` 和 `FsPermChecker` 构建调用链，参考通过 `syscall_body!` 宏分发，系统调用入口抽象不同。 |
| 设备驱动 | `anemone-kernel/src/driver/virtio/pcie.rs#probe` | `arceos/modules/axdriver/src/lib.rs#probe` | 同板块同名 | 目标基于 `DeviceBase` 和 `FuncNum` 构建 PCI 设备，参考通过 `for_each_drivers!` 宏和总线设备扫描，驱动模型不同。 |
| 第三方库/公共依赖 | `anemone-kernel/crates/buddy-system/src/zone.rs#alloc` | `arceos/crates/axdriver_crates/axdriver_net/src/net_buf.rs#alloc` | 同板块同名 | 目标实现基于阶数的伙伴系统分配器，参考实现基于自由链表的内存池分配，分配器算法与数据结构完全不同。 |

### 4. 核心代码调用流对比剖析
* **【进程管理/进程退出与资源回收】**
* **底层技术析理**: 目标项目的 `kernel_exit` 围绕 `ThreadGroupLifeCycle::Exited` 状态机、`cpu_usage` 统计字段和 `UserWritePtr::<Tid>` 类型化指针构建退出流。参考项目的 `do_exit` 则通过 `axtask::exit`、`IPC_MANAGER.clear` 和 `UserPtr::<Pid>` 实现。两者在任务生命周期管理、进程间通信资源清理和用户态指针抽象上存在根本性差异。
* **代码溯源链接**:
  - 目标项目: `anemone-kernel/src/task/api/exit/mod.rs#kernel_exit`
  - 参考项目: `xapi/src/task/exit.rs#do_exit`

* **【进程管理/进程组与会话拓扑】**
* **底层技术析理**: 目标项目采用全局集中式拓扑表 `TOPOLOGY`，结合 `BTreeSet` 和 `NoIrqRwLock` 管理进程组关系。参考项目则将拓扑信息分散在进程对象内部，通过 `self.group.lock` 和 `StrongMap` 管理。这是集中式与分布式状态管理策略的典型差异。
* **代码溯源链接**:
  - 目标项目: `anemone-kernel/src/task/topology/process_group.rs#create_session`
  - 参考项目: `xmodules/xprocess/src/process.rs#create_session`

* **【内存管理/缺页异常处理】**
* **底层技术析理**: 目标项目的 `handle_page_fault` 通过查找虚拟内存区域并操作页表来修复缺页。参考项目的同名函数则主要检查栈范围，并向用户态进程发送 `SIGSEGV` 信号。一个采用内核态透明修复，一个采用用户态信号通知，错误处理哲学截然不同。
* **代码溯源链接**:
  - 目标项目: `anemone-kernel/src/mm/uspace/mod.rs#handle_page_fault`
  - 参考项目: `src/mm.rs#handle_page_fault`

### 5. 单方模块与降权项
- **网络协议栈**: 仅存在于参考项目中，目标项目无此模块，无法形成同源证据。
- **第三方库/公共依赖**: 如 `buddy-system` 与 `net_buf` 中的 `alloc`/`dealloc` 函数，虽同名但实现算法（伙伴系统 vs 内存池）完全不同。`from_slice` 等函数在目标项目中是复杂的内存初始化，在参考项目中是简单的内部闭环逻辑，不构成强同源证据。
- **POSIX 标准接口**: `sys_getpid`、`sys_exit`、`fork`、`execve` 等函数同名源于 POSIX/Linux ABI 的强制性要求。其实现多为极简封装或存根，调用图谱自然趋同，属于独立开发中的合理巧合，已降权处理。
- **跨板块证据**: 控方将目标项目的文件系统函数与参考项目的进程管理函数强行对齐，此类跨板块比对违反审计指令，不构成有效证据。

### 6. 保守结论
本次审计基于 0.89% 的极低 AST 相似度及 1.69%/1.83% 的覆盖率，认定双方项目为独立开发。控方提出的“复用指控”所依赖的调用图谱相似性，可被操作系统领域的 POSIX 标准约束、Rust 语言惯用法及通用内核设计模式完全解释。双方在进程拓扑、内存管理、调度与同步原语等核心架构上存在系统性差异，远非“变量重命名”或“API 替换”所能概括。现有证据远不足以支持抄袭或核心逻辑复用的结论，整体同源性风险极低。

------------------------------

