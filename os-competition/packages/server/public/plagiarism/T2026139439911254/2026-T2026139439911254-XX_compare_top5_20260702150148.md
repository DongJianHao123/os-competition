# 🛡️ OS 内核赛自动化代码级架构审计报告

**审计时间**: `2026-07-02 15:01:48`
**参赛作品名称**: `2026-T2026139439911254-XX`
**年份**: `2026`
**学校**: `XX`
**队伍名称**: `T2026139439911254`
**比赛名字**: `OS-Competition`
**比赛子赛道**: `Kernel-Implementation`
**仓库地址**: `https://gitlab.eduxiji.net/educg-group-43501-3132633/T2026139439911254-1516`
**推测开发语言**: `Rust / C (由AST探针自动识别)`
**特征函数量**: `854`

## ⚔️ 底层代码同源性对比 (AST Homology)
> 本部分由专门的代码审查智能体，对该项目与符合年份条件的历届最高相似度项目进行控制流与底层调用对比。新版报告会先给出确定性的全模块覆盖矩阵，再给出 Agent 的分模块解释，避免只围绕少量抽样函数展开。

### 🏆 Top 5 底层对标: vs `2024-chaos-北京科技大学`（北京科技大学 / chaos）

#### 对方作品信息

**对方年份**: `2024`
**对方学校**: `北京科技大学`
**对方队伍名称**: `chaos`
**对方比赛名字**: `OS-Competition`
**对方比赛子赛道**: `Kernel-Implementation`
**对方仓库地址**: `https://gitlab.eduxiji.net/educg-group-26010-2376550/oskernel2024-chaos`

> 双向覆盖率：共同 AST 指纹: 6；唯一 AST 指纹: 目标项目 748，参考项目 798；覆盖率: 目标项目 0.8%，参考项目 0.75%

## 全模块对比总览（确定性证据）

> 总体覆盖率：共同 AST 指纹: 6；唯一 AST 指纹: 目标项目 748，参考项目 798；覆盖率: 目标项目 0.8%，参考项目 0.75%

### 1. 模块覆盖矩阵
| 模块 | 目标函数数 | 参考函数数 | 共同唯一 AST 指纹 | Target 覆盖 | Base 覆盖 | 状态 |
| :--- | ---: | ---: | ---: | ---: | ---: | :--- |
| 进程管理 | 47 | 45 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |
| 任务调度/异步运行时 | 111 | 76 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |
| 内存管理 | 161 | 178 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |
| 中断/异常处理 | 34 | 5 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |
| 系统调用 | 13 | 67 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |
| 文件系统 | 66 | 152 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |
| 设备驱动 | 53 | 19 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |
| 网络协议栈 | 0 | 0 | 0 | 0.0% | 0.0% | 双方均未体现 |
| 文件系统/第三方库 | 306 | 270 | 1 | 0.41% | 0.44% | 双方存在且有结构重合 |
| 第三方库 | 2 | 50 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |
| 通用模块/其他 | 61 | 75 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |

### 2. 分模块证据清单

#### 进程管理
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `luwu-kernel/src/syscall/task.rs#sys_execve` | `os/src/syscall/process.rs#sys_execve` | 同板块同名 | A::enter_user, Some, String::from, SyscallOutcome::Return, applet.as_str, argv.clear ...(+17) | String::from, Vec::new, all_data.as_slice, args.add, args_vec.insert, args_vec.len ...(+22) |
| `luwu-kernel/src/process.rs#current_pid` | `os/src/task/processor.rs#current_pid` | 同板块同名 | CURRENT_PID.load | Some, current_task |
| `luwu-kernel/src/process.rs#fork_current` | `os/src/syscall/signal.rs#sys_sigtimedwait` | 同模块上下文对齐/差异观察 | (*PROCESS_TABLE.inner.get()).len, A::capture_user_context, A::set_context_stack_pointer, A::set_context_thread_pointer, Box::leak, Box::new ...(+15) | trace! |
| `luwu-kernel/src/syscall/task.rs#sys_wait4` | `os/src/syscall/signal.rs#sys_sigprocmask` | 同模块上下文对齐/差异观察 | A::capture_user_context, SyscallOutcome::Return, memory.write_user_u32, process::current_pid, process::current_user_memory, process::set_waiting ...(+3) | SignalFlags::from_bits, SignalFlags::from_bits(new_set).unwrap, current_task, current_task().unwrap, file!, line! ...(+5) |

#### 任务调度/异步运行时
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `luwu-kernel/src/syscall/misc.rs#sys_ioctl` | `os/src/syscall/fs.rs#sys_ioctl` | 同板块同名 | 0x3bu32.to_le_bytes, 0x500u32.to_le_bytes, 0x5u32.to_le_bytes, 0x8bfu32.to_le_bytes, SyscallOutcome::Return, memory.write_user_bytes ...(+6) | trace! |
| `luwu-kernel/src/syscall/io_write.rs#sys_write` | `os/src/syscall/fs.rs#sys_write` | 同板块同名 | Err, Ok, Some, SyscallOutcome::Return, console::write_byte::<A>, console_fd.is_some ...(+10) | core::slice::from_raw_parts, current_task, current_task().unwrap, drop, file!, file.clone ...(+8) |
| `luwu-kernel/src/syscall/fs_ops.rs#sys_linkat` | `os/src/syscall/fs.rs#sys_linkat` | 同板块同名 | SyscallOutcome::Return, create_memory_file_with, fs::resolve_path, fs::resolve_path(root, &old_path).is_ok, memory_file_bytes, memory_file_bytes(id).unwrap_or_default ...(+8) | curdir.inode, curdir.inode().link, curdir.inode().lookup, curdir.inode().lookup(old_name.as_str()).unwrap, current_task, current_task()
        .unwrap ...(+8) |
| `luwu-kernel/src/syscall/misc.rs#sys_ppoll` | `os/src/syscall/ppoll.rs#sys_ppoll` | 同板块同名 | SyscallOutcome::Return, events_bytes[..2].try_into, events_bytes[..2].try_into().unwrap_or, fd_bytes[..4].try_into, fd_bytes[..4].try_into().unwrap_or, fd_ready ...(+7) | Box::from_raw, Box::into_raw, Box::new, SignalFlags::empty, current_task, current_task().unwrap ...(+18) |

#### 内存管理
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `luwu-kernel/src/syscall/fs_ops.rs#sys_openat` | `os/src/syscall/fs.rs#sys_openat` | 同板块同名 | String::from_utf8, SyscallOutcome::Return, build_dirents::<A>, create_memory_file, create_memory_file_with, data.clone ...(+17) | OpenFlags::from_bits, OpenFlags::from_bits(flags).unwrap, Some, cast_file_to_inode, cast_file_to_inode(dir).unwrap, cast_inode_to_file ...(+19) |
| `luwu-kernel/src/user_paging.rs#create` | `os/src/fs/fat32/inode.rs#create` | 同板块同名 | A::create_user_root, A::map_user_page, A::share_kernel_mappings, Ok, UnsafeCell::new, Vec::new ...(+5) | Arc::clone, Arc::new, Dentry::new, Some, fs
            .insert_dentry, fs
            .insert_dentry(self.start_cluster, name.to_string(), attr, 0, start_cluster)
            .unwrap ...(+7) |
| `luwu-kernel/src/syscall/fd.rs#sys_fcntl` | `os/src/syscall/fs.rs#sys_fcntl` | 同板块同名 | SyscallOutcome::Return, process::alloc_fd_from, process::get_fd | current_task, current_task().unwrap, debug!, file!, inner.alloc_fd, inner.fd_table.len ...(+6) |
| `luwu-kernel/src/syscall/fd.rs#sys_dup` | `os/src/syscall/fs.rs#sys_dup` | 同板块同名 | SyscallOutcome::Return, process::alloc_fd, process::get_fd | Arc::clone, Some, current_task, current_task().unwrap, file!, inner.alloc_fd ...(+7) |

#### 中断/异常处理
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `luwu-loongarch/src/main.rs#decode_syscall_args` | `os/src/fs/pipe.rs#read` | 同模块上下文对齐/差异观察 | core::ptr::read_volatile | assert!, buf.into_iter, buf.len, buf_iter.next, debug!, drop ...(+11) |
| `luwu-riscv/src/main.rs#decode_syscall_args` | `os/src/trap/mod.rs#set_kernel_trap_entry` | 同模块上下文对齐/差异观察 | core::ptr::read_volatile | stvec::write |
| `luwu-common/src/trap.rs#fmt` | `os/src/trap/mod.rs#set_user_trap_entry` | 同模块上下文对齐/差异观察 | f.write_str, write! | stvec::write |
| `luwu-kernel/src/trap.rs#signal_for_exception` | `os/src/trap/mod.rs#enable_timer_interrupt` | 同模块上下文对齐/差异观察 | 内部闭环逻辑 | sie::set_stimer |

#### 系统调用
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `luwu-kernel/src/syscall/io_read.rs#sys_read` | `user/src/syscall.rs#sys_read` | 同板块同名 | sys_pread_like::<A> | buffer.as_mut_ptr, buffer.len, syscall |
| `luwu-kernel/src/syscall/stat.rs#write_statx` | `user/src/lib.rs#waitpid` | 同模块上下文对齐/差异观察 | (mode as u16).to_le_bytes, 0x7ffu32.to_le_bytes, 1u32.to_le_bytes, 4096u32.to_le_bytes, SyscallOutcome::Return, blocks.to_le_bytes ...(+11) | sys_waitpid, yield_ |
| `luwu-kernel/src/syscall/stat.rs#write_stat` | `user/src/syscall.rs#syscall` | 同模块上下文对齐/差异观察 | 1u32.to_le_bytes, 4096u32.to_le_bytes, SyscallOutcome::Return, blocks.to_le_bytes, ino.to_le_bytes, memory.write_user_bytes ...(+9) | asm! |
| `luwu-kernel/src/syscall/misc.rs#sys_pselect6` | `user/src/lib.rs#wait` | 同模块上下文对齐/差异观察 | SyscallOutcome::Return, filter_fd_set | sys_waitpid, yield_ |

#### 文件系统
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `luwu-kernel/src/init.rs#discover_root_test_scripts` | `os/src/fs/fat32/fs.rs#insert_dentry` | 同模块上下文对齐/差异观察 | Err, Ok, String::from, Vec::new, console::write_fmt::<A>, console::write_str::<A> ...(+18) | Arc::clone, Fat32Dentry::new, Fat32DentryLayout::new, Fat32LDentryLayout::new, Some, get_block_cache ...(+15) |
| `luwu-kernel/src/virtio/pci.rs#assign_memory_bars` | `os/src/fs/fat32/fs.rs#get_dentry` | 同模块上下文对齐/差异观察 | align_up, bar_size, next_mmio.saturating_add, self.read32, self.write32 | Arc::clone, Fat32Dentry::new, Fat32Dentry::new_deleted, Some, get_block_cache, get_block_cache(*sector_id, Arc::clone(&self.bdev))
                    .lock ...(+9) |
| `luwu-kernel/src/syscall/io_read.rs#read_memory_file_with_pipe_wait` | `os/src/fs/fat32/dentry.rs#new` | 同模块上下文对齐/差异观察 | Err, MEMORY_FILES.inner.get, Ok, core::cmp::min, core::hint::spin_loop, file.data.len ...(+3) | attr.bits, c.is_ascii_uppercase, file_name.chars |
| `luwu-common/src/efi.rs#discover_fdt` | `os/src/fs/fat32/dentry.rs#from_short_layout` | 同模块上下文对齐/差异观察 | Some, core::cmp::min, guid_eq, read_u64, read_u64(entry + 16).map, read_u64(systab + EFI_SYSTEM_TABLE_TABLES_OFFSET).map | Some |

#### 设备驱动
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `luwu-kernel/src/virtio/mod.rs#read_block` | `os/src/drivers/block/virtio_blk.rs#read_block` | 同板块同名 | Err, Ok, async_rt::block_on::<A, _>, blk::read_sector, block_no
            .checked_mul, block_no
            .checked_mul((buf.len() / 512) as u64)
            .ok_or ...(+6) | error!, panic!, res.is_err, res.unwrap, self.0.lock, self.0.lock().read_blocks ...(+1) |
| `luwu-kernel/src/virtio/mod.rs#write_block` | `os/src/drivers/block/virtio_blk.rs#write_block` | 同板块同名 | Err, Ok, async_rt::block_on::<A, _>, blk::write_sector, block_no
            .checked_mul, block_no
            .checked_mul((buf.len() / 512) as u64)
            .ok_or ...(+6) | debug!, self.0
            .lock, self.0
            .lock()
            .write_blocks, self.0
            .lock()
            .write_blocks(block_id, buf)
            .expect |
| `luwu-kernel/src/virtio/blk.rs#submit_request` | `os/src/utils/platform_info.rs#fmt` | 同模块上下文对齐/差异观察 | A::dma_fence, A::virt_to_phys, Self::dma_storage, Self::storage, avail_idx.wrapping_add, core::mem::size_of::<BlkRequestHeader> ...(+7) | Ok, core::str::from_utf8, core::str::from_utf8(&self.model[..index]).unwrap, core::str::from_utf8(&x[..self.bootargs_len]).unwrap, self
            .bootargs
            .as_ref, self
            .bootargs
            .as_ref()
            .map ...(+11) |
| `luwu-kernel/src/virtio/mmio.rs#setup_queue_modern` | `os/src/block/block_cache.rs#get_block_cache` | 同模块上下文对齐/差异观察 | Err, Ok, self.read32, self.write32, self.write64 | Arc::clone, Arc::new, Arc::strong_count, BlockCache::new, Mutex::new, panic! ...(+8) |

#### 文件系统/第三方库
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `luwu-ext4/src/fs_core/block_group_manager.rs#load` | `os/libs/ext4_rs/src/ext4_structs/block_group.rs#load` | 同板块同名 | BlockGroupDesc::parse, Err, Ok, Vec::with_capacity, desc.verify_checksum, descriptors.push ...(+1) | Ext4BlockGroup::try_from, block_device.read_offset, size_of::<Ext4BlockGroup> |
| `luwu-ext4/src/fs_core/extent_modifier.rs#insert_extent` | `os/libs/ext4_rs/src/ext4_structs/inode.rs#insert_extent` | 同板块同名 | Err, Ext4Error::CorruptedFs, Ok, Self::collect_extents, Self::normalize_extents, Self::rebuild_tree ...(+2) | self.inner.inode.extent_depth, self.insert_leaf, self.write_back_inode_without_csum |
| `luwu-ext4/src/fs_core/dir_reader.rs#lookup` | `os/src/fs/ext4/inode.rs#lookup` | 同板块同名 | Ok, Self::htree_lookup, Self::linear_lookup, dir_inode.uses_htree | Arc::new, Dentry::new, Ext4File::new, Some, UPSafeCell::new, self.fs
            .ext4
            .ext4_open_from ...(+2) |
| `luwu-ext4/src/layout/dir_entry.rs#from_u8` | `os/libs/ext4_rs/src/ext4_structs/direntry.rs#from_u8` | 同板块同名 | 内部闭环逻辑 | core::ptr::read, data.as_mut_ptr |

#### 第三方库
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `luwu-kernel/src/virtio/pci.rs#for_each_vendor_capability` | `os/libs/visionfive2-sd/src/lib.rs#send_cmd` | 同模块上下文对齐/差异观察 | f, self.read16, self.read32, self.read8 | RawInterrupt::from, RawInterruptStatusReg::from, S::sleep_ms_until, Some, arg.into, assert! ...(+21) |
| `luwu-kernel/src/virtio/pci.rs#vendor_id` | `os/libs/visionfive2-sd/src/lib.rs#init_sdcard` | 同模块上下文对齐/差异观察 | (vendor != 0).then_some, self.read16 | CardTypeReg::from, CardTypeReg::from(0).with_card_width4_1, CmdArg::new, CmdReg::from, ControlReg::from, RawInterruptStatusReg::from ...(+18) |

#### 通用模块/其他
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `luwu-kernel/src/console.rs#write_str` | `os/src/console.rs#write_str` | 同板块同名 | Writer::<A>::new, console.write_str, console.write_str(s).ok | Ok, console_putchar, s.chars |
| `luwu-kernel/src/console.rs#write_str` | `user/src/console.rs#write_str` | 同板块同名 | Ok, s.as_bytes, self.inner.write_bytes | Ok, s.as_bytes, write |
| `luwu-kernel/src/user_paging.rs#prepare_user_stack` | `os/src/utils/platform_info.rs#walk_dt` | 同模块上下文对齐/差异观察 | Ok, Vec::new, argv.iter, argv.iter().rev, argv.len, argv_ptrs.into_iter ...(+13) | Some, bootargs.len, fdt.all_nodes, fdt.chosen, fdt.chosen().bootargs, fdt.chosen().bootargs().map ...(+25) |
| `luwu-kernel/src/shell.rs#read_line` | `user/src/bin/user_shell.rs#new` | 同模块上下文对齐/差异观察 | buffer.len, byte.is_ascii_graphic, console::read_byte_async::<A>, console::write_byte::<A>, console::write_str::<A> | String::new, arg.as_ptr, arg.as_str, arg.is_empty, args
            .iter, args
            .iter()
            .filter ...(+16) |


好的，遵循审计指令，基于客观 AST 指纹数据与底层调用图谱，现对参赛作品 `[2026-T2026139439911254-XX]` 与参考项目 `[2024-chaos-北京科技大学]` 进行最终同源性定性仲裁。

### 1. 宏观判定
- **定性结论**: **独立开发**
- **核心依据**: 客观 AST 相似度仅为 0.39%，全项目共同 AST 指纹仅 6 个，且全部集中于“文件系统/第三方库”板块。在操作系统内核的核心区域（进程管理、内存管理、任务调度等），双方共同指纹为 0。目标项目与参考项目在架构抽象、内存模型、同步策略及核心调用流上存在本质性、系统性的差异，排除了基于参考项目进行代码复用的可能性。

### 2. 全模块覆盖矩阵解读
| 模块 | 双方存在 | 共同 AST 指纹 | 覆盖率 (目标/参考) | 证据强度 | 解读 |
| :--- | :---: | :---: | :---: | :---: | :--- |
| **进程管理** | 是 | 0 | 0.0% / 0.0% | **未形成** | 双方均实现了进程管理，但底层架构完全不同。目标项目使用 `Vec` 线性进程表 + 原子 PID，参考项目使用 `HashMap` + `TaskControlBlock`。同名函数如 `sys_execve` 的调用图谱无交集，`current_pid` 的实现路径（原子变量 vs. 任务控制块）截然不同。控方指控的“状态机复用”实为标准 POSIX 语义的共性实现。 |
| **任务调度/异步运行时** | 是 | 0 | 0.0% / 0.0% | **未形成** | 调度模型存在代差。目标项目采用现代异步运行时模型 (`async_rt::block_on`)，而参考项目是传统的 `processor.rs` + `__schedule` 协作式调度。这是两种完全不同的内核架构范式，不存在复用基础。 |
| **内存管理** | 是 | 0 | 0.0% / 0.0% | **未形成** | 双方均存在内存管理模块，但无共同指纹。目标项目通过 `user_paging.rs` 和架构抽象 `A::map_user_page` 管理内存，参考项目则深度绑定 RISC-V 页表机制。内存管理是内核中架构耦合度最高的部分，零共同指纹强有力地证明了独立实现。 |
| **中断/异常处理** | 是 | 0 | 0.0% / 0.0% | **未形成** | 目标项目通过 `luwu-riscv` 和 `luwu-loongarch` 展示了多架构中断处理抽象，而参考项目仅针对 RISC-V。双方在 Trap 处理路径上无共同指纹，属于不同硬件架构下的独立实现。 |
| **系统调用** | 是 | 0 | 0.0% / 0.0% | **未形成** | 双方均提供 Linux 兼容系统调用，但实现风格迥异。目标项目使用 `SyscallOutcome::Return` 统一包装返回值，参考项目则大量使用 `trace!`、`file!`、`line!` 宏进行调试追踪。同名系统调用（如 `sys_read`）的底层调用图谱完全不同。 |
| **文件系统** | 是 | 0 | 0.0% / 0.0% | **未形成** | 目标项目实现了自研的 `luwu-ext4` 和内存文件系统，参考项目则基于 FAT32 和简单的 inode 抽象。双方在文件系统核心逻辑上无任何共同指纹，是完全不同的文件系统技术栈。 |
| **设备驱动** | 是 | 0 | 0.0% / 0.0% | **未形成** | 双方均实现了 VirtIO 块设备驱动，但调用图谱差异显著。目标项目通过 `async_rt::block_on` 异步化 I/O，参考项目使用同步锁 `self.0.lock()`。同名函数 `read_block` 的实现路径和依赖对象完全不同。 |
| **网络协议栈** | 否 | 0 | N/A | **未形成** | 双方均未体现网络协议栈模块。 |
| **文件系统/第三方库** | 是 | 1 | 0.41% / 0.44% | **弱** | 全项目唯一的共同指纹集中于此，具体位于 `luwu-ext4` 与 `os/libs/ext4_rs` 之间。这源于 ext4 文件系统磁盘格式的公共规范，或对同一开源 Rust ext4 库的合法引用，属于**第三方库/公共依赖**范畴，不能作为核心逻辑抄袭的证据。 |
| **第三方库** | 是 | 0 | 0.0% / 0.0% | **未形成** | 双方均包含第三方库，但无共同指纹，表明引用了不同的外部依赖。 |
| **通用模块/其他** | 是 | 0 | 0.0% / 0.0% | **未形成** | 同名函数如 `console::write_str` 的底层实现完全不同，目标项目使用 `Writer` 抽象，参考项目直接调用 `console_putchar`。 |

### 3. 底层 AST 关键证据指纹表
| 所属板块 | 目标项目路径 | 参考项目路径 | 证据类型 | 技术说明 |
| :--- | :--- | :--- | :--- | :--- |
| **文件系统/第三方库** | `luwu-ext4/src/fs_core/block_group_manager.rs#load` | `os/libs/ext4_rs/src/ext4_structs/block_group.rs#load` | 第三方库/公共规范 | 共同指纹源于 ext4 规范的 `BlockGroupDesc` 解析。双方可能基于相同规范或公共 Rust 库实现，属于合法复用。 |
| **文件系统/第三方库** | `luwu-ext4/src/fs_core/extent_modifier.rs#insert_extent` | `os/libs/ext4_rs/src/ext4_structs/inode.rs#insert_extent` | 第三方库/公共规范 | 共同指纹源于 ext4 规范的 extent 树操作。属于数据结构驱动的逻辑相似，非核心逻辑抄袭。 |
| **文件系统/第三方库** | `luwu-ext4/src/fs_core/dir_reader.rs#lookup` | `os/src/fs/ext4/inode.rs#lookup` | 第三方库/公共规范 | 共同指纹源于 ext4 规范的目录项查找。目标项目使用 `htree_lookup`，参考项目使用 `ext4_open_from`，实现路径仍有差异。 |
| **文件系统/第三方库** | `luwu-ext4/src/layout/dir_entry.rs#from_u8` | `os/libs/ext4_rs/src/ext4_structs/direntry.rs#from_u8` | 第三方库/公共规范 | 共同指纹源于 ext4 规范的目录项结构体解析。属于磁盘格式驱动的强制相似。 |
| **设备驱动** | `luwu-kernel/src/virtio/mod.rs#read_block` | `os/src/drivers/block/virtio_blk.rs#read_block` | 同板块同名/弱信号 | 同名函数但调用流完全不同。目标项目使用 `async_rt::block_on` 异步模型，参考项目使用同步互斥锁。无共同指纹，不构成同源证据。 |
| **通用模块/其他** | `luwu-kernel/src/console.rs#write_str` | `os/src/console.rs#write_str` | 同板块同名/弱信号 | 同名函数但实现完全不同。目标项目通过 `Writer::<A>` 泛型抽象，参考项目直接调用 `console_putchar`。无共同指纹。 |

### 4. 核心代码调用流对比剖析
*   **【进程管理/进程创建】**
    *   **底层技术析理**: 目标项目 `fork_current` 采用原子操作 (`NEXT_PID.fetch_add`) 分配 PID，并通过泛型架构抽象 `A::capture_user_context` 保存上下文，体现了对 LoongArch/RISC-V 的多架构支持。参考项目 `sys_clone` 则深度绑定 `TaskControlBlock` 和 `CloneFlags`，处理 Linux 复杂的 clone 语义。两者在内存模型（`Box::leak` vs. `inner_exclusive_access`）和同步策略（原子变量 vs. 互斥锁）上存在根本差异。
    *   **代码溯源链接**:
        - 目标项目: `luwu-kernel/src/process.rs#fork_current`
        - 参考项目: `os/src/syscall/process.rs#sys_clone`

*   **【任务调度/异步运行时】**
    *   **底层技术析理**: 目标项目在 `async_rt.rs` 中实现了完整的异步运行时，允许在 `read_block` 等 I/O 操作中使用 `async_rt::block_on` 进行异步等待。参考项目则使用传统的 `processor.rs#schedule` 函数，通过 `__schedule` 进行协作式任务切换。这是两种截然不同的调度范式，前者面向事件驱动，后者基于控制流让出，不存在代码复用可能。
    *   **代码溯源链接**:
        - 目标项目: `luwu-kernel/src/virtio/mod.rs#read_block`
        - 参考项目: `os/src/task/processor.rs#schedule`

*   **【内存管理/用户页表】**
    *   **底层技术析理**: 目标项目通过 `user_paging.rs#create` 函数，调用 `A::create_user_root` 和 `A::map_user_page` 等架构抽象接口来构建用户页表。参考项目则没有对应的统一抽象，其内存管理分散在 `sys_mmap` 等系统调用中，直接操作 RISC-V 页表。目标项目的分层抽象设计是其独立开发的显著标志。
    *   **代码溯源链接**:
        - 目标项目: `luwu-kernel/src/user_paging.rs#create`
        - 参考项目: `os/src/syscall/process.rs#sys_mmap`

### 5. 单方模块与降权项
*   **单方模块**:
    *   **目标项目独有**: `luwu-loongarch` (LoongArch 架构支持)、`luwu-common` (跨架构公共抽象)、`async_rt` (异步运行时)。这些是目标项目原创性的核心体现。
    *   **参考项目独有**: `os/src/fs/fat32` (FAT32 文件系统实现)、`os/src/drivers/block/virtio_blk.rs` (同步 VirtIO 驱动)。这些是参考项目的特定技术选型。
*   **降权项说明**:
    *   **跨板块同名函数**: 如 `sys_ioctl`、`sys_write` 等，虽在多个板块出现同名，但均为 POSIX/Linux 标准系统调用名，且底层调用图谱完全不同，**降权为弱信号，不构成同板块主证据**。
    *   **第三方库/公共依赖**: 全项目仅有的 6 个共同指纹均属此类，源于 ext4 标准或公共 Rust 库，**降权为弱信号，不代表核心逻辑抄袭**。
    *   **泛型函数名与领域术语**: 如 `new`, `init`, `read`, `write` 等，是编程语言和操作系统领域的通用术语，其出现不具有特异性，**不构成任何证据**。

### 6. 保守结论
本次审计的客观 AST 相似度极低（0.39%），共同指纹仅 6 个且全部定位于第三方库/公共规范范畴。在进程管理、内存管理、任务调度等操作系统内核的核心竞争力模块，双方共同指纹为 0，且展现出截然不同的架构范式、抽象层次和同步策略。目标项目采用的多架构抽象（`A::`）、异步运行时等现代设计，与参考项目的传统 RISC-V 单核同步模型存在代差。因此，综合判定两个项目为独立开发，不存在基于参考项目的代码抄袭或结构性复用。控方基于功能共性的指控在底层 AST 证据面前无法成立。

------------------------------

