# 🛡️ OS 内核赛自动化代码级架构审计报告

**审计时间**: `2026-07-02 15:01:48`
**参赛作品名称**: `2026-T2026139439911254-XX`
**年份**: `2026`
**学校**: `XX`
**队伍名称**: `T2026139439911254`
**比赛名字**: `OS-Competition`
**比赛子赛道**: `Kernel-Implementation`
**仓库地址**: `https://gitlab.eduxiji.net/educg-group-43501-3132633/T2026139439911254-1516`
**推测开发语言**: `Rust / C (由AST探针自动识别)`
**特征函数量**: `854`

## ⚔️ 底层代码同源性对比 (AST Homology)
> 本部分由专门的代码审查智能体，对该项目与符合年份条件的历届最高相似度项目进行控制流与底层调用对比。新版报告会先给出确定性的全模块覆盖矩阵，再给出 Agent 的分模块解释，避免只围绕少量抽样函数展开。

### 🏆 Top 4 底层对标: vs `2022-啊队队队-河南科技大学`（河南科技大学 / 啊队队队）

#### 对方作品信息

**对方年份**: `2022`
**对方学校**: `河南科技大学`
**对方队伍名称**: `啊队队队`
**对方比赛名字**: `OS-Competition`
**对方比赛子赛道**: `Kernel-Implementation`
**对方仓库地址**: `https://gitlab.eduxiji.net/alexsondes/oskernel2022-byte-os.git`

> 双向覆盖率：共同 AST 指纹: 6；唯一 AST 指纹: 目标项目 748，参考项目 759；覆盖率: 目标项目 0.8%，参考项目 0.79%

## 全模块对比总览（确定性证据）

> 总体覆盖率：共同 AST 指纹: 6；唯一 AST 指纹: 目标项目 748，参考项目 759；覆盖率: 目标项目 0.8%，参考项目 0.79%

### 1. 模块覆盖矩阵
| 模块 | 目标函数数 | 参考函数数 | 共同唯一 AST 指纹 | Target 覆盖 | Base 覆盖 | 状态 |
| :--- | ---: | ---: | ---: | ---: | ---: | :--- |
| 进程管理 | 47 | 41 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |
| 任务调度/异步运行时 | 111 | 122 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |
| 内存管理 | 161 | 190 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |
| 中断/异常处理 | 34 | 15 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |
| 系统调用 | 13 | 9 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |
| 文件系统 | 66 | 436 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |
| 设备驱动 | 53 | 44 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |
| 网络协议栈 | 0 | 0 | 0 | 0.0% | 0.0% | 双方均未体现 |
| 文件系统/第三方库 | 306 | 0 | 0 | 0.0% | 0.0% | 仅目标项目体现 |
| 第三方库 | 2 | 0 | 0 | 0.0% | 0.0% | 仅目标项目体现 |
| 通用模块/其他 | 61 | 25 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |

### 2. 分模块证据清单

#### 进程管理
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `luwu-kernel/src/syscall/task.rs#sys_wait4` | `kernel/src/sys_call/task/fork.rs#sys_wait4` | 同板块同名 | A::capture_user_context, SyscallOutcome::Return, memory.write_user_u32, process::current_pid, process::current_user_memory, process::set_waiting ...(+3) | Err, Ok, cprocess.exit_code.unwrap, cprocess_vec.len, cprocess_vec[0].borrow, debug! ...(+14) |
| `luwu-kernel/src/process.rs#kill_process` | `kernel/src/task/task_scheduler.rs#kill_process` | 同板块同名 | PROCESS_TABLE.inner.get, ProcessState::Exited, table.iter_mut, table.iter_mut().find | self.queue.clone, self.queue.clone().into_iter, self.queue.clone().into_iter().filter, self.queue.clone().into_iter().filter(|x| x.pid != pid).collect |
| `luwu-kernel/src/async_rt.rs#wait` | `kernel/src/task/process.rs#wait` | 同板块同名 | 内部闭环逻辑 | 内部闭环逻辑 |
| `luwu-kernel/src/process.rs#fork_current` | `kernel/src/sys_call/task/fork.rs#sys_clone` | 同模块上下文对齐/差异观察 | (*PROCESS_TABLE.inner.get()).len, A::capture_user_context, A::set_context_stack_pointer, A::set_context_thread_pointer, Box::leak, Box::new ...(+15) | Ok, Task::new, add_task_to_scheduler, debug!, drop, inner.process.clone ...(+12) |

#### 任务调度/异步运行时
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `luwu-kernel/src/init.rs#run` | `kernel/src/task/task.rs#run` | 同板块同名 | A::enter_user, A::init_tasks, A::poweroff, Box::leak, Box::new, Some ...(+15) | change_task, drop, inner.process.borrow_mut, self.inner.borrow_mut |
| `luwu-kernel/src/syscall/io_write.rs#sys_write` | `kernel/src/sys_call/fd/rw.rs#sys_write` | 同板块同名 | Err, Ok, Some, SyscallOutcome::Return, console::write_byte::<A>, console_fd.is_some ...(+10) | Ok, buf.len, buf_ptr.transfer_vec, debug!, drop, inner.process.borrow_mut ...(+4) |
| `luwu-kernel/src/syscall/fd.rs#sys_lseek` | `kernel/src/sys_call/fd/rw.rs#sys_lseek` | 同板块同名 | Some, SyscallOutcome::Return, memory_file_len, memory_file_len(id).map, process::get_fd, process::with_fd_mut | Ok, debug!, drop, file.lseek, inner.process.borrow_mut, process.fd_table.get ...(+1) |
| `luwu-kernel/src/syscall/misc.rs#sys_readlinkat` | `kernel/src/sys_call/fd/open.rs#sys_readlinkat` | 同板块同名 | String::from, SyscallOutcome::Return, core::cmp::min, fs::resolve_path, fs::resolve_path(root, &path)
            .ok, fs::resolve_path(root, &path)
            .ok()
            .and_then ...(+9) | "/lmbench_all".to_string, Ok, buf.transfer_vec, buf[..path.len()].copy_from_slice, debug!, path.as_bytes ...(+3) |

#### 内存管理
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `luwu-kernel/src/syscall/fs_ops.rs#sys_openat` | `kernel/src/sys_call/fd/open.rs#sys_openat` | 同板块同名 | String::from_utf8, SyscallOutcome::Return, build_dirents::<A>, create_memory_file, create_memory_file_with, data.clone ...(+17) | DevRtc::new, EtcAdjtime::new, FileDesc::new, INode::open, INode::open_or_create, Ok ...(+16) |
| `luwu-kernel/src/syscall/misc.rs#sys_mmap` | `kernel/src/sys_call/mm.rs#sys_mmap` | 同板块同名 | A::tlb_flush, Some, SyscallOutcome::Return, alloc::vec!, content.len, core::cmp::min ...(+9) | MapFlags::from_bits_truncate, MemMap::new, Ok, VirtAddr::from, VirtAddr::from(start).into, debug! ...(+18) |
| `luwu-kernel/src/heap.rs#dealloc` | `kernel/src/memory/page.rs#dealloc` | 同板块同名 | adjust_size, ptr.is_null, self.dealloc_inner | self.pages.get, usize::from |
| `luwu-common/src/lib.rs#is_valid` | `kernel/src/memory/addr.rs#is_valid` | 同板块同名 | 内部闭环逻辑 | 内部闭环逻辑 |

#### 中断/异常处理
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `luwu-loongarch/src/main.rs#decode_syscall_args` | `kernel/src/interrupt/timer.rs#now` | 同模块上下文对齐/差异观察 | core::ptr::read_volatile | time::read |
| `luwu-riscv/src/main.rs#decode_syscall_args` | `kernel/src/interrupt/mod.rs#init` | 同模块上下文对齐/差异观察 | core::ptr::read_volatile | asm!, info!, timer::init |
| `luwu-common/src/trap.rs#fmt` | `kernel/src/interrupt/timer.rs#timer_handler` | 同模块上下文对齐/差异观察 | f.write_str, write! | NEXT_TICKS.force_get, NEXT_TICKS.force_get().need_change, set_next_timeout |
| `luwu-kernel/src/trap.rs#signal_for_exception` | `kernel/src/interrupt/mod.rs#clone_from` | 同模块上下文对齐/差异观察 | 内部闭环逻辑 | 内部闭环逻辑 |

#### 系统调用
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `luwu-kernel/src/syscall/stat.rs#write_statx` | `kernel/src/sys_call/net.rs#read_at` | 同模块上下文对齐/差异观察 | (mode as u16).to_le_bytes, 0x7ffu32.to_le_bytes, 1u32.to_le_bytes, 4096u32.to_le_bytes, SyscallOutcome::Return, blocks.to_le_bytes ...(+11) | buf.len, queue.pop_front, self.0.borrow_mut |
| `luwu-kernel/src/syscall/stat.rs#write_stat` | `kernel/src/sys_call/net.rs#write_at` | 同模块上下文对齐/差异观察 | 1u32.to_le_bytes, 4096u32.to_le_bytes, SyscallOutcome::Return, blocks.to_le_bytes, ino.to_le_bytes, memory.write_user_bytes ...(+9) | buf.len, queue.push_back, self.0.borrow_mut |
| `luwu-kernel/src/syscall/misc.rs#sys_pselect6` | `kernel/src/sys_call/mod.rs#is_vfork_wait` | 同模块上下文对齐/差异观察 | SyscallOutcome::Return, filter_fd_set | VFORK_WAIT_LIST.lock, vfork_wait_list.iter, vfork_wait_list.iter().find, vfork_wait_list.iter().find(|&&x| x == pid).is_some |
| `luwu-kernel/src/syscall/stat.rs#stat_cache_put` | `kernel/src/sys_call/mod.rs#remove_vfork_wait` | 同模块上下文对齐/差异观察 | STAT_CACHE.inner.get, String::from, entries.iter, entries.iter().any, entries.len, entries.push ...(+1) | VFORK_WAIT_LIST.lock, vfork_wait_list.drain_filter |

#### 文件系统
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `luwu-kernel/src/init.rs#discover_root_test_scripts` | `fatfs/src/boot_sector.rs#format_bpb` | 同模块上下文对齐/差异观察 | Err, Ok, String::from, Vec::new, console::write_fmt::<A>, console::write_str::<A> ...(+18) | Err, FatType::from_clusters, Ok, assert!, bpb.total_clusters, debug_assert! ...(+15) |
| `luwu-kernel/src/virtio/pci.rs#assign_memory_bars` | `fatfs/src/boot_sector.rs#determine_sectors_per_fat` | 同模块上下文对齐/差异观察 | align_up, bar_size, next_mmio.saturating_add, self.read32, self.write32 | fat_type.bits_per_fat_entry, u32::from, u64::from |
| `luwu-kernel/src/syscall/io_read.rs#read_memory_file_with_pipe_wait` | `kernel/src/device/sdcard.rs#get_csdregister` | 同模块上下文对齐/差异观察 | Err, MEMORY_FILES.inner.get, Ok, core::cmp::min, core::hint::spin_loop, file.data.len ...(+3) | Err, Ok, self.end_cmd, self.get_response, self.read_data, self.send_cmd ...(+2) |
| `luwu-common/src/efi.rs#discover_fdt` | `fatfs/src/file.rs#seek` | 同模块上下文对齐/差异观察 | Some, core::cmp::min, guid_eq, read_u64, read_u64(entry + 16).map, read_u64(systab + EFI_SYSTEM_TABLE_TABLES_OFFSET).map | Err, Ok, Some, debug_assert!, error!, i64::from ...(+16) |

#### 设备驱动
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `luwu-kernel/src/virtio/mod.rs#read_block` | `kernel/src/device/sdcard.rs#read_block` | 同板块同名 | Err, Ok, async_rt::block_on::<A, _>, blk::read_sector, block_no
            .checked_mul, block_no
            .checked_mul((buf.len() / 512) as u64)
            .ok_or ...(+6) | Self::wait_for_one_sec, info!, result.is_err, sd_card.init, sd_card.read_sector, sd_card.read_sector(buf, block_id as u32).is_err ...(+1) |
| `luwu-kernel/src/virtio/mod.rs#write_block` | `kernel/src/device/sdcard.rs#write_block` | 同板块同名 | Err, Ok, async_rt::block_on::<A, _>, blk::write_sector, block_no
            .checked_mul, block_no
            .checked_mul((buf.len() / 512) as u64)
            .ok_or ...(+6) | self.0
            .borrow, self.0
            .borrow()
            .write_sector, self.0
            .borrow()
            .write_sector(buf, block_id as u32)
            .unwrap |
| `luwu-kernel/src/virtio/blk.rs#read_sector` | `kernel/src/device/sdcard.rs#read_sector` | 同板块同名 | 内部闭环逻辑 | Err, Ok, assert!, chunk.iter_mut, chunk.iter_mut().zip, data_buf.chunks_mut ...(+6) |
| `luwu-kernel/src/virtio/blk.rs#write_sector` | `kernel/src/device/sdcard.rs#write_sector` | 同板块同名 | 内部闭环逻辑 | (data_buf.len() / SEC_LEN).try_into, (data_buf.len() / SEC_LEN).try_into().unwrap, Err, Ok, assert!, chunk.iter ...(+9) |

#### 文件系统/第三方库
仅目标项目中观察到该模块代表函数，参考项目没有同模块可对齐证据：
- 目标项目：`luwu-ext4/src/journal/commit.rs#commit`，调用摘要：((blk >> 32) as u32).to_be_bytes, (blk as u32).to_be_bytes, 0u32.to_be_bytes, Err, Ext4Error::CorruptedFs, JBD2_BLOCKTYPE_COMMIT.to_be_bytes ...(+35)
- 目标项目：`luwu-ext4/src/fs/alloc_flush.rs#flush_alloc_metadata`，调用摘要：((free_blocks >> 32) as u32).to_le_bytes, (free_blocks as u32).to_le_bytes, (free_inodes as u32).to_le_bytes, BlockGroupManager::desc_table_start, BlockWriter::new, Ok ...(+41)
- 目标项目：`luwu-ext4/src/journal/recovery.rs#recover`，调用摘要：BTreeSet::<u64>::new, Err, JBD2_MAGIC_NUMBER.to_be_bytes, JournalHeader::parse, Ok, Self::needs_recovery ...(+15)
- 目标项目：`luwu-ext4/src/fs_core/extent_modifier.rs#rebuild_tree`，调用摘要：Err, Ext4Error::CorruptedFs, Ok, Self::collect_tree_blocks, Self::write_header, Self::write_header_slice ...(+20)

#### 第三方库
仅目标项目中观察到该模块代表函数，参考项目没有同模块可对齐证据：
- 目标项目：`luwu-kernel/src/virtio/pci.rs#for_each_vendor_capability`，调用摘要：f, self.read16, self.read32, self.read8
- 目标项目：`luwu-kernel/src/virtio/pci.rs#vendor_id`，调用摘要：(vendor != 0).then_some, self.read16

#### 通用模块/其他
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `luwu-kernel/src/console.rs#write_str` | `kernel/src/console.rs#write_str` | 同板块同名 | Writer::<A>::new, console.write_str, console.write_str(s).ok | Ok, c.encode_utf8, c.encode_utf8(&mut buffer).as_bytes, c.encode_utf8(&mut buffer).as_bytes().iter, console_putchar, s.chars |
| `luwu-kernel/src/user_paging.rs#prepare_user_stack` | `kernel/src/sync/mutex.rs#obtain_lock` | 同模块上下文对齐/差异观察 | Ok, Vec::new, argv.iter, argv.iter().rev, argv.len, argv_ptrs.into_iter ...(+13) | self.lock.compare_exchange, self.lock.load, spin_loop |
| `luwu-kernel/src/shell.rs#read_line` | `kernel/src/main.rs#clear_bss` | 同模块上下文对齐/差异观察 | buffer.len, byte.is_ascii_graphic, console::read_byte_async::<A>, console::write_byte::<A>, console::write_str::<A> | core::slice::from_raw_parts_mut, core::slice::from_raw_parts_mut(bss_start_addr, bss_size).fill, info! |
| `luwu-kernel/src/shell.rs#run` | `kernel/src/sbi.rs#sbi_call_3` | 同模块上下文对齐/差异观察 | A::poweroff, A::reboot, command.is_empty, console::write_str::<A>, read_line::<A> | asm! |


好的，审计官。基于提供的客观 AST 证据、全模块对比矩阵及双方论据，现出具最终定性仲裁报告。

### #### 1. 宏观判定
- **定性结论**: **独立开发 / 底层架构显著差异**
- **核心依据**: 客观 AST 相似度仅为 **0.4%**，共同 AST 指纹仅 **6** 个，而双方各自拥有超过 700 个唯一指纹。在控方重点指控的【进程管理】板块，共同指纹覆盖率为 **0.0%**，表明双方在该核心板块的实现结构、调用链、数据访问路径上无任何可被算法识别的共同表达。控方指控均基于领域通用概念和标准命名，未能提供任何底层 AST 结构层面的同源铁证。

### #### 2. 全模块覆盖矩阵解读
- **进程管理**
  1. 双方均存在该模块。
  2. 不存在共同 AST 指纹 (0)。
  3. 覆盖率均为 0.0%，说明双方在进程管理的底层实现（数据结构、同步原语、调用链）上完全不同。
  4. 证据强度：**弱**。控方基于同名函数和通用逻辑的指控已被 0% 的 AST 覆盖率证伪。

- **任务调度/异步运行时**
  1. 双方均存在该模块。
  2. 不存在共同 AST 指纹 (0)。
  3. 覆盖率均为 0.0%，表明调度模型存在本质差异（目标为异步运行时，参考为传统任务队列）。
  4. 证据强度：**未形成**。同名函数 `run` 和 `sys_write` 的底层调用图谱完全不同。

- **内存管理**
  1. 双方均存在该模块。
  2. 不存在共同 AST 指纹 (0)。
  3. 覆盖率均为 0.0%，说明内存分配、映射、管理的底层实现路径无重合。
  4. 证据强度：**弱**。同名函数 `dealloc` 和 `is_valid` 均为内部闭环逻辑或标准 trait 实现，不构成同源证据。

- **中断/异常处理**
  1. 双方均存在该模块。
  2. 不存在共同 AST 指纹 (0)。
  3. 覆盖率均为 0.0%，表明 Trap 处理、上下文切换的底层机制完全不同。
  4. 证据强度：**未形成**。所有证据均为跨模块上下文对齐，无同板块同名函数。

- **系统调用**
  1. 双方均存在该模块。
  2. 不存在共同 AST 指纹 (0)。
  3. 覆盖率均为 0.0%，说明系统调用分发、参数解码、返回机制无共同结构。
  4. 证据强度：**未形成**。所有证据均为跨模块上下文对齐，无同板块同名函数。

- **文件系统**
  1. 双方均存在该模块。
  2. 不存在共同 AST 指纹 (0)。
  3. 覆盖率均为 0.0%，表明文件系统实现（目标为自研 ext4，参考为 fatfs）完全不同。
  4. 证据强度：**未形成**。所有证据均为跨模块上下文对齐，无同板块同名函数。

- **设备驱动**
  1. 双方均存在该模块。
  2. 不存在共同 AST 指纹 (0)。
  3. 覆盖率均为 0.0%，说明驱动模型（目标为 VirtIO，参考为 SD 卡）完全不同。
  4. 证据强度：**弱**。同名函数 `read_block` 和 `write_block` 仅因功能同名，底层调用链（`async_rt::block_on` vs `sd_card.init`）完全不同。

- **网络协议栈**
  1. 双方均未体现该模块。
  2. 不存在共同 AST 指纹。
  3. 覆盖率均为 0.0%。
  4. 证据强度：**未形成**。

- **第三方库/公共依赖**
  1. 仅目标项目体现（`luwu-ext4` 等）。
  2. 不存在共同 AST 指纹。
  3. 参考项目无此模块，无法比对。
  4. 证据强度：**未形成**。属于单方自研或引入的独立模块，是独立开发的强证据。

### #### 3. 底层 AST 关键证据指纹表
| 所属板块 | 目标项目路径 | 参考项目路径 | 证据类型 | 技术说明 |
| :--- | :--- | :--- | :--- | :--- |
| 进程管理 | `luwu-kernel/src/syscall/task.rs#sys_wait4` | `kernel/src/sys_call/task/fork.rs#sys_wait4` | 同板块同名 | 同名系统调用，但底层数据结构访问路径完全不同。目标操作扁平 `PROCESS_TABLE`，参考操作 `Rc<RefCell>` 向量。 |
| 进程管理 | `luwu-kernel/src/process.rs#kill_process` | `kernel/src/task/task_scheduler.rs#kill_process` | 同板块同名 | 同名函数，但功能与位置不同。目标在进程表中设置退出态，参考在调度队列中过滤删除。 |
| 进程管理 | `luwu-kernel/src/async_rt.rs#wait` | `kernel/src/task/process.rs#wait` | 同板块同名 | 同名函数，但均为“内部闭环逻辑”，无外部调用图谱交叉，无法形成证据链。 |
| 设备驱动 | `luwu-kernel/src/virtio/mod.rs#read_block` | `kernel/src/device/sdcard.rs#read_block` | 同板块同名 | 同名函数，但驱动模型完全不同。目标基于 VirtIO 异步模型，参考基于 SD 卡同步轮询。 |
| 设备驱动 | `luwu-kernel/src/virtio/mod.rs#write_block` | `kernel/src/device/sdcard.rs#write_block` | 同板块同名 | 同上，同名写块操作，底层调用链无交集。 |
| 通用模块/其他 | `luwu-kernel/src/console.rs#write_str` | `kernel/src/console.rs#write_str` | 同板块同名 | 同名标准 trait 方法实现。目标使用 `Writer` 封装，参考逐字符 `encode_utf8` 输出。 |
| 第三方库/公共依赖 | `luwu-ext4/src/journal/commit.rs#commit` | (无) | 单方模块 | 目标项目自研的 ext4 日志系统，参考项目无此模块，是独立开发的强证据。 |
| 第三方库/公共依赖 | `luwu-kernel/src/virtio/pci.rs#for_each_vendor_capability` | (无) | 单方模块 | 目标项目 VirtIO PCI 能力遍历，参考项目无此模块。 |

### #### 4. 核心代码调用流对比剖析
* **【进程管理 / 进程退出与父进程唤醒】**
* **底层技术析理**: 两者均实现了 POSIX 规范的进程退出与父进程唤醒语义，但底层实现模型截然不同。目标项目采用扁平化全局进程表 `PROCESS_TABLE`，通过 `terminate_current_with_signal` 函数直接设置 `ProcessState::Exited` 状态，并调用独立的 `resume_waiting_parent` 辅助函数完成唤醒。参考项目则基于 `Rc<RefCell>` 的树形进程结构，在 `exit` 函数中通过调用 `kill_process` 间接处理，并调用 `self.release` 释放内存映射。两者在状态表示、资源释放路径和同步策略上存在根本性差异。
* **代码溯源**:
  - 目标项目: `luwu-kernel/src/process.rs#terminate_current_with_signal`
  - 参考项目: `kernel/src/task/process.rs#exit`

* **【设备驱动 / 块设备读取】**
* **底层技术析理**: 两者均实现了块设备读取功能，但驱动模型和同步机制完全不同。目标项目基于 VirtIO 标准，采用异步运行时 `async_rt::block_on` 等待 I/O 完成，体现了现代异步驱动设计。参考项目基于 SD 卡硬件，采用同步轮询 `Self::wait_for_one_sec` 和错误重试机制。两者在硬件抽象层、并发模型和错误处理策略上无任何共同点。
* **代码溯源**:
  - 目标项目: `luwu-kernel/src/virtio/mod.rs#read_block`
  - 参考项目: `kernel/src/device/sdcard.rs#read_block`

### #### 5. 单方模块与降权项
- **单方模块（强独立开发证据）**:
  - **文件系统/第三方库**: 目标项目包含自研的 `luwu-ext4` 文件系统（含日志、恢复、extent 树等复杂特性），参考项目无此模块。
  - **第三方库**: 目标项目包含 VirtIO PCI 设备枚举等独立模块，参考项目无此模块。
  这些模块的存在是目标项目独立开发、具备不同技术栈和功能目标的强有力证据。

- **降权项说明**:
  - **同名系统调用/函数**: `sys_wait4`, `kill_process`, `read_block` 等均为 POSIX 标准接口或通用功能描述，其同名性不能作为复用证据。底层 AST 分析已证明其实现路径完全不同。
  - **跨板块证据**: 控方及全模块对比中，将大量跨板块（如文件系统 vs 进程管理）的函数进行比对，此类证据根据审计指令必须降权，不构成同板块主证据。
  - **通用命名与惯例**: `NEXT_PID`, `PROCESS_TABLE` 等命名属于操作系统内核开发中的常见约定，不受单一项目垄断。

### #### 6. 保守结论
综合客观 AST 指纹覆盖率（0.4%）、全模块 0% 共同指纹的矩阵分析，以及双方在进程模型、同步原语、驱动框架和文件系统等核心子系统上表现出的系统性架构差异，本次审计未发现任何支持“同源性”或“代码复用”的底层结构证据。控方提出的所有指控均基于领域通用概念、标准命名或跨板块硬凑，已被 0% 的进程管理板块 AST 覆盖率证伪。目标项目 `[2026-T2026139439911254-XX]` 与参考项目 `[2022-啊队队队-河南科技大学]` 应被判定为两个独立开发的操作系统内核，整体相似度风险极低。

------------------------------

