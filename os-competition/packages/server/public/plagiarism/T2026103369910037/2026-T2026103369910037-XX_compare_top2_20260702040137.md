# 🛡️ OS 内核赛自动化代码级架构审计报告

**审计时间**: `2026-07-02 04:01:37`
**参赛作品名称**: `2026-T2026103369910037-XX`
**年份**: `2026`
**学校**: `XX`
**队伍名称**: `T2026103369910037`
**比赛名字**: `OS-Competition`
**比赛子赛道**: `Kernel-Implementation`
**仓库地址**: `https://gitlab.eduxiji.net/educg-group-43501-3132633/T2026103369910037-3065`
**推测开发语言**: `Rust / C (由AST探针自动识别)`
**特征函数量**: `2755`

## ⚔️ 底层代码同源性对比 (AST Homology)
> 本部分由专门的代码审查智能体，对该项目与符合年份条件的历届最高相似度项目进行控制流与底层调用对比。新版报告会先给出确定性的全模块覆盖矩阵，再给出 Agent 的分模块解释，避免只围绕少量抽样函数展开。

### 🏆 Top 2 底层对标: vs `2025-九莲宝灯一向听-天津理工大学`（天津理工大学 / 九莲宝灯一向听）

#### 对方作品信息

**对方年份**: `2025`
**对方学校**: `天津理工大学`
**对方队伍名称**: `九莲宝灯一向听`
**对方比赛名字**: `OS-Competition`
**对方比赛子赛道**: `Kernel-Implementation`
**对方仓库地址**: `https://gitlab.eduxiji.net/educg-group-36002-2710490/T202510060995409-3585`

> 双向覆盖率：共同 AST 指纹: 754；唯一 AST 指纹: 目标项目 2209，参考项目 2610；覆盖率: 目标项目 34.13%，参考项目 28.89%

## 全模块对比总览（确定性证据）

> 总体覆盖率：共同 AST 指纹: 754；唯一 AST 指纹: 目标项目 2209，参考项目 2610；覆盖率: 目标项目 34.13%，参考项目 28.89%

### 1. 模块覆盖矩阵
| 模块 | 目标函数数 | 参考函数数 | 共同唯一 AST 指纹 | Target 覆盖 | Base 覆盖 | 状态 |
| :--- | ---: | ---: | ---: | ---: | ---: | :--- |
| 进程管理 | 135 | 79 | 13 | 10.4% | 17.81% | 双方存在且有结构重合 |
| 任务调度/异步运行时 | 303 | 385 | 79 | 31.1% | 28.73% | 双方存在且有结构重合 |
| 内存管理 | 392 | 301 | 72 | 20.4% | 26.47% | 双方存在且有结构重合 |
| 中断/异常处理 | 39 | 60 | 12 | 35.29% | 23.53% | 双方存在且有结构重合 |
| 系统调用 | 217 | 130 | 27 | 15.17% | 25.23% | 双方存在且有结构重合 |
| 文件系统 | 780 | 616 | 117 | 19.8% | 25.77% | 双方存在且有结构重合 |
| 设备驱动 | 99 | 156 | 84 | 90.32% | 59.15% | 双方存在且有结构重合 |
| 网络协议栈 | 47 | 61 | 38 | 100.0% | 74.51% | 双方存在且有结构重合 |
| 文件系统/第三方库 | 32 | 577 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |
| 网络协议栈/第三方库 | 99 | 130 | 72 | 78.26% | 62.07% | 双方存在且有结构重合 |
| 第三方库 | 178 | 209 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |
| 通用模块/其他 | 434 | 630 | 240 | 74.07% | 49.9% | 双方存在且有结构重合 |

### 2. 分模块证据清单

#### 进程管理
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `modules/axfs/src/fs/builtin.rs#procfs` | `arceos/modules/axfs/src/mounts.rs#procfs` | 同板块同名 | Arc::new, MeminfoNode::new, MountNode::new, Ok, file_cpuinfo.write_at, file_over.write_at ...(+13) | Arc::new, Ok, ProcInterruptsFile::new, ReadOnlyRamFileSystem::new, core_dir.create_readonly_file, net_dir.create_subdir ...(+7) |
| `modules/axtask/src/run_queue.rs#exit_current` | `arceos/modules/axtask/src/run_queue.rs#exit_current` | 同板块同名 | EXITED_TASKS.lock, EXITED_TASKS.lock().clear, EXITED_TASKS.lock().push_back, WAIT_FOR_EXIT.notify_one_locked, assert!, axhal::misc::terminate ...(+12) | EXITED_TASKS.current_ref_mut_raw, EXITED_TASKS.current_ref_mut_raw().clear, EXITED_TASKS.current_ref_mut_raw().push_back, WAIT_FOR_EXIT.current_ref_mut_raw, WAIT_FOR_EXIT.current_ref_mut_raw().notify_one, assert! ...(+8) |
| `modules/axtask/src/wait_queue.rs#wait_timeout_until` | `arceos/modules/axtask/src/wait_queue.rs#wait_timeout_until` | 同板块同名 | RUN_QUEUE.lock, axhal::time::monotonic_time, condition, crate::current, crate::timers::set_alarm_wakeup, curr.clone ...(+6) | axhal::time::wall_time, condition, crate::current, crate::timers::set_alarm_wakeup, curr.clone, current_run_queue::<NoPreemptIrqSave> ...(+4) |
| `tools/raspi4/chainloader/src/main.rs#print_jtag_info_and_wait_forever` | `arceos/tools/raspi4/chainloader/src/main.rs#print_jtag_info_and_wait_forever` | 同板块同名同结构 | cpu::wait_forever, println! | cpu::wait_forever, println! |

#### 任务调度/异步运行时
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `ulib/axlibc/c/printf.c#format_string_loop` | `arceos/ulib/axlibc/c/printf.c#format_string_loop` | 同板块同名同结构 | ABS_FOR_PRINTING, ADVANCE_IN_FORMAT_STRING, atou_, is_digit_, out_rev_, parse_flags ...(+5) | ABS_FOR_PRINTING, ADVANCE_IN_FORMAT_STRING, atou_, is_digit_, out_rev_, parse_flags ...(+5) |
| `modules/axlog/src/lib.rs#log` | `arceos/modules/axlog/src/lib.rs#log` | 同板块同名同结构 | cfg_if::cfg_if!, record.level, record.line, record.line().unwrap_or, record.metadata, record.target ...(+1) | cfg_if::cfg_if!, record.level, record.line, record.line().unwrap_or, record.metadata, record.target ...(+1) |
| `modules/axtask/src/task.rs#set_state` | `arceos/modules/axtask/src/task.rs#set_state` | 同板块同名 | core::mem::size_of::<usize>, core::panic::Location::caller, core::ptr::read_volatile, debug!, self.ctx.get, self.kernel_stack_top ...(+4) | self.state.store |
| `modules/axruntime/src/lib.rs#init_interrupt` | `arceos/modules/axruntime/src/lib.rs#init_interrupt` | 同板块同名 | <TimerInterfaceImpl as axhal::time::TimerInterface>::check_timer, NEXT_DEADLINE.read_current_raw, NEXT_DEADLINE.write_current_raw, axhal::arch::enable_irqs, axhal::irq::register_handler, axhal::irq::trigger_soft ...(+8) | NEXT_DEADLINE.read_current_raw, NEXT_DEADLINE.write_current_raw, axhal::arch::enable_irqs, axhal::irq::register_handler, axhal::time::monotonic_time_nanos, axhal::time::set_oneshot_timer ...(+3) |

#### 内存管理
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `modules/axruntime/src/trap.rs#handle_page_fault` | `src/mm.rs#handle_page_fault` | 同板块同名 | (addr.as_usize()).into, AxVirtAddr::from, KERNEL_SPACE.exclusive_access, MapPermission::empty, addr.as_usize, axlog::debug! ...(+18) | curr
        .task_ext, curr
        .task_ext()
        .process_data, curr
        .task_ext()
        .process_data()
        .aspace
        .lock, curr
        .task_ext()
        .process_data()
        .aspace
        .lock()
        .handle_page_fault, current, do_exit ...(+2) |
| `ulib/axlibc/src/strftime.rs#strftime_inner` | `arceos/ulib/axlibc/src/strftime.rs#strftime_inner` | 同板块同名 | CountingWriter::new, format.offset, inner_strftime, w! | CountingWriter::new, format.offset, inner_strftime, w! |
| `ulib/axlibc/src/strftime.rs#inner_strftime` | `arceos/ulib/axlibc/src/strftime.rs#inner_strftime` | 同板块同名 | format.offset, w! | format.offset, w! |
| `modules/axhal/src/arch/riscv/trap.rs#riscv_trap_handler` | `arceos/modules/axhal/src/arch/riscv/trap.rs#riscv_trap_handler` | 同板块同名 | addr.into, axlog::error!, crate::trap::check_preempt_pending, crate::trap::handle_irq, crate::trap::handle_signal, crate::trap::handle_syscall ...(+9) | crate::trap::handle_syscall, crate::trap::post_trap_callback, handle_breakpoint, handle_page_fault, handle_trap!, mask_irqs ...(+7) |

#### 中断/异常处理
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `ulib/axlibc/c/pow.c#exp_inline` | `arceos/ulib/axlibc/c/pow.c#exp_inline` | 同板块同名同结构 | __math_oflow, __math_uflow, asdouble, asuint64, converttoint, eval_as_double ...(+4) | __math_oflow, __math_uflow, asdouble, asuint64, converttoint, eval_as_double ...(+4) |
| `modules/axhal/src/platform/loongarch64_qemu_virt/irq.rs#register_handler` | `arceos/modules/axhal/src/platform/riscv64_visionfive2/irq.rs#register_handler` | 同板块同名 | SOFT_HANDLER.init_once, SOFT_HANDLER.is_inited, TIMER_HANDLER.init_once, TIMER_HANDLER.is_inited, crate::irq::register_handler_common | debug!, with_cause! |
| `modules/axhal/src/platform/riscv64_qemu_virt/irq.rs#register_handler` | `arceos/modules/axhal/src/platform/riscv64_qemu_virt/irq.rs#register_handler` | 同板块同名 | with_cause! | with_cause! |
| `modules/axhal/src/platform/riscv64_qemu_virt/irq.rs#dispatch_irq` | `arceos/modules/axhal/src/platform/riscv64_qemu_virt/irq.rs#dispatch_irq` | 同板块同名 | with_cause! | with_cause! |

#### 系统调用
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `ulib/axlibc/src/syscall_wrapper.rs#sys_writev` | `arceos/api/arceos_posix_api/src/imp/io.rs#sys_writev` | 同板块同名 | core::slice::from_raw_parts, iovs.iter, sys_write | debug!, syscall_body! |
| `ulib/axlibc/src/net.rs#sendto` | `arceos/ulib/axlibc/src/net.rs#sendto` | 同板块同名同结构 | e, socket_addr.is_null, sys_send, sys_sendto | e, socket_addr.is_null, sys_send, sys_sendto |
| `ulib/axlibc/src/net.rs#recvfrom` | `arceos/ulib/axlibc/src/net.rs#recvfrom` | 同板块同名同结构 | e, socket_addr.is_null, sys_recv, sys_recvfrom | e, socket_addr.is_null, sys_recv, sys_recvfrom |
| `ulib/axlibc/src/fd_ops.rs#dup3` | `arceos/ulib/axlibc/src/fd_ops.rs#dup3` | 同板块同名同结构 | (LinuxError::EINVAL as c_int).wrapping_neg, e, sys_dup2, sys_fcntl | (LinuxError::EINVAL as c_int).wrapping_neg, e, sys_dup2, sys_fcntl |

#### 文件系统
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `ulib/axlibc/c/glob.c#do_glob` | `arceos/ulib/axlibc/c/glob.c#do_glob` | 同板块同名同结构 | S_ISDIR, append, closedir, do_glob, errfunc, fnmatch ...(+7) | S_ISDIR, append, closedir, do_glob, errfunc, fnmatch ...(+7) |
| `ulib/axlibc/src/setjmp.rs#longjmp` | `arceos/ulib/axlibc/src/setjmp.rs#longjmp` | 同板块同名 | core::arch::naked_asm! | core::arch::naked_asm! |
| `ulib/axlibc/src/setjmp.rs#setjmp` | `arceos/ulib/axlibc/src/setjmp.rs#setjmp` | 同板块同名 | core::arch::naked_asm! | core::arch::naked_asm! |
| `ulib/axlibc/c/pow.c#log_inline` | `arceos/ulib/axlibc/c/pow.c#log_inline` | 同板块同名同结构 | __builtin_fma, asdouble | __builtin_fma, asdouble |

#### 设备驱动
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `tools/bwbench_client/src/main.rs#transmit_benchmark` | `arceos/tools/bwbench_client/src/main.rs#transmit_benchmark` | 同板块同名同结构 | Local::now, NetDevice::new, NetDevice::new(interface).unwrap, current_time.signed_duration_since, current_time.signed_duration_since(past_time).num_seconds, dev.send ...(+2) | Local::now, NetDevice::new, NetDevice::new(interface).unwrap, current_time.signed_duration_since, current_time.signed_duration_since(past_time).num_seconds, dev.send ...(+2) |
| `tools/bwbench_client/src/device.rs#bind_interface` | `arceos/tools/bwbench_client/src/device.rs#bind_interface` | 同板块同名同结构 | ETH_P_ALL.to_be, Err, Ok, libc::bind, self.ifreq.ioctl, std::io::Error::last_os_error ...(+2) | ETH_P_ALL.to_be, Err, Ok, libc::bind, self.ifreq.ioctl, std::io::Error::last_os_error ...(+2) |
| `modules/axdriver/src/lib.rs#init_drivers` | `arceos/modules/axdriver/src/lib.rs#init_drivers` | 同板块同名 | AllDevices::default, all_devs.block.iter, all_devs.block.iter().enumerate, all_devs.display.iter, all_devs.display.iter().enumerate, all_devs.net.iter ...(+5) | AllDevices::default, all_devs.block.iter, all_devs.block.iter().enumerate, all_devs.display.iter, all_devs.display.iter().enumerate, all_devs.net.iter ...(+5) |
| `tools/bwbench_client/src/main.rs#receive_benchmark` | `arceos/tools/bwbench_client/src/main.rs#receive_benchmark` | 同板块同名同结构 | Local::now, NetDevice::new, NetDevice::new(interface).unwrap, current_time.signed_duration_since, current_time.signed_duration_since(past_time).num_seconds, dev.recv ...(+1) | Local::now, NetDevice::new, NetDevice::new(interface).unwrap, current_time.signed_duration_since, current_time.signed_duration_since(past_time).num_seconds, dev.recv ...(+1) |

#### 网络协议栈
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `ulib/axlibc/c/network.c#inet_pton` | `arceos/ulib/axlibc/c/network.c#inet_pton` | 同板块同名同结构 | hexval, inet_pton, isdigit, memmove | hexval, inet_pton, isdigit, memmove |
| `ulib/axlibc/c/socket.c#accept4` | `arceos/ulib/axlibc/c/socket.c#accept4` | 同板块同名同结构 | accept, fcntl | accept, fcntl |
| `ulib/axstd/src/net/tcp.rs#bind` | `arceos/ulib/axstd/src/net/tcp.rs#bind` | 同板块同名同结构 | Ok, TcpListener, api::ax_tcp_bind, api::ax_tcp_listen, api::ax_tcp_socket, super::each_addr | Ok, TcpListener, api::ax_tcp_bind, api::ax_tcp_listen, api::ax_tcp_socket, super::each_addr |
| `ulib/axstd/src/net/tcp.rs#connect` | `arceos/ulib/axstd/src/net/tcp.rs#connect` | 同板块同名同结构 | Ok, TcpStream, api::ax_tcp_connect, api::ax_tcp_socket, super::each_addr | Ok, TcpStream, api::ax_tcp_connect, api::ax_tcp_socket, super::each_addr |

#### 文件系统/第三方库
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `modules/axfs/src/fs/ext4fs.rs#create` | `arceos/modules/axfs/src/fs/lwext4_rust.rs#create` | 同板块同名 | Err, Ok, fs
                    .create, fs
                    .create(self.ino, path, inode_type, mode)
                    .map_err, fs.with_inode_ref, fs.with_inode_ref(child_ino, |inode| {
                    inode.update_atime();
                    inode.update_mtime();
                    inode.update_ctime();
                    Ok(())
                })
                .map_err ...(+8) | Ok, e.try_into, e.try_into().unwrap, file.check_inode_exist, file.dir_mk, file.dir_mk(fpath)
                    .map ...(+12) |
| `modules/axfs/src/fs/ext4fs.rs#read_dir` | `arceos/modules/axfs/src/fs/lwext4_rust.rs#read_dir` | 同板块同名 | Err, Ok, VfsDirEntry::new, core::str::from_utf8, core::str::from_utf8(entry.name()).unwrap_or, dirents.len ...(+12) | Ok, VfsDirEntry::new, core::str::from_utf8, core::str::from_utf8(iname.unwrap()).unwrap, dirents.iter_mut, dirents.iter_mut().enumerate ...(+13) |
| `modules/axfs/src/fs/ext4fs.rs#lookup` | `arceos/modules/axfs/src/fs/lwext4_rust.rs#lookup` | 同板块同名 | Err, Ext4FileSystem::new_inode, Ok, debug!, drop, entry.ino ...(+8) | Arc::new, Err, Ok, Self::new, file.check_inode_exist, fpath.as_str ...(+5) |
| `modules/axfs/src/fs/ext4fs.rs#rename` | `arceos/modules/axfs/src/fs/lwext4_rust.rs#rename` | 同板块同名 | Err, Ok, debug!, dst_dir.get_attr, fs.rename, fs.rename(self.ino, src_path, dst_dir_ino, dst_path)
                    .map_err ...(+6) | e.try_into, e.try_into().unwrap, file.file_rename, file.file_rename(&src_path, dst_path)
            .map, file.file_rename(&src_path, dst_path)
            .map(|_v| ())
            .map_err, file.get_path ...(+2) |

#### 网络协议栈/第三方库
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `modules/axnet/src/smoltcp_impl/tcp.rs#connect` | `arceos/modules/axnet/src/smoltcp_impl/tcp.rs#connect` | 同板块同名 | Err, Ok, SOCKET_SET
                .with_socket_mut::<tcp::Socket, _, _>, SOCKET_SET.add, SocketSetWrapper::new_tcp_socket, Some ...(+25) | Err, Ok, Ok::<(IpEndpoint, IpEndpoint), AxError>, SOCKET_SET
                .with_socket_mut::<tcp::Socket, _, _>, SOCKET_SET.add, SocketSetWrapper::new_tcp_socket ...(+32) |
| `modules/axnet/src/smoltcp_impl/dns.rs#query` | `arceos/modules/axnet/src/smoltcp_impl/dns.rs#query` | 同板块同名 | Err, Ok, SOCKET_SET
            .with_socket_mut::<dns::Socket, _, _>, SOCKET_SET
            .with_socket_mut::<dns::Socket, _, _>(handle, |socket| {
                socket.start_query(iface.lock().context(), name, query_type)
            })
            .map_err, SOCKET_SET.poll_interfaces, SOCKET_SET.with_socket_mut::<dns::Socket, _, _> ...(+12) | Err, Ok, SOCKET_SET
            .with_socket_mut::<dns::Socket, _, _>, SOCKET_SET
            .with_socket_mut::<dns::Socket, _, _>(handle, |socket| {
                socket.start_query(iface.lock().context(), name, query_type)
            })
            .map_err, SOCKET_SET.poll_interfaces, SOCKET_SET.with_socket_mut::<dns::Socket, _, _> ...(+12) |
| `modules/axnet/src/smoltcp_impl/listen_table.rs#incoming_tcp_packet` | `arceos/modules/axnet/src/smoltcp_impl/listen_table.rs#incoming_tcp_packet` | 同板块同名 | SocketSetWrapper::new_tcp_socket, debug!, entry.can_accept, entry.syn_queue.iter, entry.syn_queue.iter().any, entry.syn_queue.len ...(+9) | SocketSetWrapper::new_tcp_socket, debug!, entry.can_accept, entry.syn_queue.len, entry.syn_queue.push_back, self.tcp[dst.port as usize].lock ...(+5) |
| `modules/axnet/src/smoltcp_impl/bench.rs#bench_transmit_bandwidth` | `arceos/modules/axnet/src/smoltcp_impl/bench.rs#bench_transmit_bandwidth` | 同板块同名同结构 | (current_time - past_time).secs, AxNetTxToken::consume, InterfaceWrapper::current_time, info!, self.transmit, tx_buf[0..12].fill ...(+2) | (current_time - past_time).secs, AxNetTxToken::consume, InterfaceWrapper::current_time, info!, self.transmit, tx_buf[0..12].fill ...(+2) |

#### 第三方库
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `crates/page_table/src/bits64.rs#map_region` | `crates/axdriver_net/src/dwmac/mod.rs#init_phy` | 同模块上下文对齐/差异观察 | Err, Ok, PageSize::Size4K.into, error!, memory_addr::is_aligned, paddr.is_aligned ...(+4) | Err, H::wait_until, Ok, Yt8531cPhy::<H>::new, core::time::Duration::from_millis, log::error! ...(+18) |
| `crates/axerrno/src/lib.rs#as_str` | `crates/axdriver_net/src/dwmac/mod.rs#init` | 同模块上下文对齐/差异观察 | 内部闭环逻辑 | AtomicBool::new, DescriptorRing::<RX_DESC_COUNT, H>::new, DescriptorRing::<TX_DESC_COUNT, H>::new, H::configure_platform, Ok, log::error! ...(+20) |
| `crates/axerrno/src/lib.rs#from` | `crates/visionfive2-sd/src/lib.rs#send_cmd` | 同模块上下文对齐/差异观察 | 内部闭环逻辑 | RawInterrupt::from, RawInterruptStatusReg::from, S::sleep_ms_until, Some, arg.into, assert! ...(+22) |
| `crates/spinlock/src/base.rs#lots_and_lots` | `crates/visionfive2-sd/src/lib.rs#init_sdcard` | 同模块上下文对齐/差异观察 | M.lock, SpinMutex::<_>::new, Vec::new, assert_eq!, channel, drop ...(+10) | CardTypeReg::from, CardTypeReg::from(0).with_card_width4_1, CmdArg::new, CmdReg::from, ControlReg::from, RawInterruptStatusReg::from ...(+20) |

#### 通用模块/其他
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `ulib/axlibc/c/printf.c#print_exponential_number` | `arceos/ulib/axlibc/c/printf.c#print_exponential_number` | 同板块同名同结构 | ABS_FOR_PRINTING, PRINTF_ABS, bastardized_floor, get_components, get_normalized_components, get_sign_bit ...(+5) | ABS_FOR_PRINTING, PRINTF_ABS, bastardized_floor, get_components, get_normalized_components, get_sign_bit ...(+5) |
| `ulib/axlibc/c/pow.c#pow` | `arceos/ulib/axlibc/c/pow.c#pow` | 同板块同名同结构 | __builtin_fma, __math_invalid, __math_oflow, __math_uflow, asdouble, asuint64 ...(+8) | __builtin_fma, __math_invalid, __math_oflow, __math_uflow, asdouble, asuint64 ...(+8) |
| `ulib/axlibc/c/string.c#twoway_strstr` | `arceos/ulib/axlibc/c/string.c#twoway_strstr` | 同板块同名同结构 | BITOP, MAX, memchr, memcmp | BITOP, MAX, memchr, memcmp |
| `ulib/axlibc/c/printf.c#print_broken_up_decimal` | `arceos/ulib/axlibc/c/printf.c#print_broken_up_decimal` | 同板块同名同结构 | out_rev_ | out_rev_ |


好的，我将遵循审计指令，基于提供的所有材料，进行最终的独立同源性定性仲裁。

### 1. 宏观判定
- **定性结论**: **独立开发 / 底层架构显著差异**
  客观 AST 相似度仅为 18.55%，远低于构成实质性相似的阈值。双方共同 AST 指纹仅 754 个，而各自拥有超过 2200 个唯一指纹，表明两个项目的绝大部分代码实现是独立且异构的。观察到的相似性高度集中在源自共同上游开源项目（ArceOS）的模块、标准库调用及行业通用模板代码中，不构成对参考项目的定向抄袭或复用。

### 2. 全模块覆盖矩阵解读
| 模块 | 目标函数数 | 参考函数数 | 共同唯一 AST 指纹 | Target 覆盖 | Base 覆盖 | 证据强度 | 解读 |
| :--- | ---: | ---: | ---: | ---: | ---: | :--- | :--- |
| **进程管理** | 135 | 79 | 13 | 10.4% | 17.81% | **弱** | 双方均实现进程管理，但共同指纹极少，覆盖率低。相似点集中在 `axtask` 等待队列和 `chainloader` 等 ArceOS 通用组件，核心系统调用如 `execve` 和 `wait4` 的实现路径完全不同。 |
| **任务调度/异步运行时** | 303 | 385 | 79 | 31.1% | 28.73% | **中** | 双方均存在该模块且有结构重合。相似性主要源于 `axtask` 调度框架和 `axlog` 等基础组件，这些是 ArceOS 的核心，其结构具有领域必然性。 |
| **内存管理** | 392 | 301 | 72 | 20.4% | 26.47% | **弱** | 双方均存在该模块。尽管有共同指纹，但覆盖率不高。核心的缺页异常处理 (`handle_page_fault`) 流程差异巨大，目标项目直接操作页表，参考项目则通过进程数据结构间接处理，架构思路不同。 |
| **中断/异常处理** | 39 | 60 | 12 | 35.29% | 23.53% | **弱** | 双方均存在该模块。共同指纹源于平台级 IRQ 注册/分发和数学库函数，这些是硬件强相关或标准数学实现，不反映内核架构的独创性。 |
| **系统调用** | 217 | 130 | 27 | 15.17% | 25.23% | **弱** | 双方均存在该模块。覆盖率较低。相似点集中在 libc 层的包装函数（如 `sendto`， `dup3`），这些是对 POSIX 接口的标准实现，而核心的系统调用处理路径则显著不同。 |
| **文件系统** | 780 | 616 | 117 | 19.8% | 25.77% | **弱** | 双方均存在该模块。共同指纹多来自 `axlibc` 的 C 库函数（如 `glob`， `pow`）和第三方库的 Rust 封装（如 `lwext4_rust`），这些是成熟的外部代码，非项目原创逻辑。 |
| **设备驱动** | 99 | 156 | 84 | 90.32% | 59.15% | **中** | 双方均存在该模块。覆盖率极高，但证据指向 `bwbench_client` 等**用户态测试工具**和 `axdriver` 的**标准初始化流程**，这些是 ArceOS 生态的标准工具和框架代码。 |
| **网络协议栈** | 47 | 61 | 38 | 100.0% | 74.51% | **中** | 双方均存在该模块。覆盖率极高，但相似性完全集中在 `smoltcp_impl` 和 `axlibc` 的 socket 封装上，这是对开源网络栈 `smoltcp` 的统一封装模式，属于公共依赖。 |
| **文件系统/第三方库** | 32 | 577 | 0 | 0.0% | 0.0% | **未形成** | 双方均存在该模块，但**未观察到任何共同 AST 指纹**。这表明双方使用的第三方库或实现完全不同。 |
| **网络协议栈/第三方库** | 99 | 130 | 72 | 78.26% | 62.07% | **中** | 双方均存在该模块。高覆盖率再次印证双方都集成了 `smoltcp` 协议栈，其内部结构指纹必然高度重合，属于第三方库依赖。 |
| **第三方库** | 178 | 209 | 0 | 0.0% | 0.0% | **未形成** | 双方均存在该模块，但**未观察到任何共同 AST 指纹**。这有力地证明双方在核心第三方库的选择和集成上存在根本差异。 |
| **通用模块/其他** | 434 | 630 | 240 | 74.07% | 49.9% | **中** | 双方均存在该模块。高覆盖率源于 `axlibc` 中大量标准 C 库函数的实现（如 `printf`， `pow`， `string`），这些函数算法固定，实现方式必然趋同，属于公共领域知识。 |

### 3. 底层 AST 关键证据指纹表
| 所属板块 | 目标项目路径 | 参考项目路径 | 证据类型 | 技术说明 |
| :--- | :--- | :--- | :--- | :--- |
| **第三方库/公共依赖** | `ulib/axlibc/c/printf.c#format_string_loop` | `arceos/ulib/axlibc/c/printf.c#format_string_loop` | 同板块同名同结构 | 标准 C 库 `printf` 系列函数的内部格式化循环，算法公开且固定，实现必然趋同。 |
| **第三方库/公共依赖** | `ulib/axlibc/c/pow.c#pow` | `arceos/ulib/axlibc/c/pow.c#pow` | 同板块同名同结构 | 标准数学库 `pow` 函数的实现，基于 IEEE 754 浮点数标准，算法和调用模式高度固定。 |
| **第三方库/公共依赖** | `modules/axnet/src/smoltcp_impl/tcp.rs#connect` | `arceos/modules/axnet/src/smoltcp_impl/tcp.rs#connect` | 同板块同名 | 对开源网络栈 `smoltcp` 的 TCP socket `connect` 操作的统一封装，调用模式由 `smoltcp` API 决定。 |
| **第三方库/公共依赖** | `modules/axfs/src/fs/ext4fs.rs#create` | `arceos/modules/axfs/src/fs/lwext4_rust.rs#create` | 同板块同名 | 对开源 ext4 文件系统库 `lwext4` 的 Rust 封装，其 `create` 操作流程由底层 C 库决定。 |
| **任务调度/异步运行时** | `modules/axtask/src/wait_queue.rs#wait_timeout_until` | `arceos/modules/axtask/src/wait_queue.rs#wait_timeout_until` | 同板块同名 | ArceOS 任务等待队列的超时等待逻辑。目标项目使用 `RUN_QUEUE.lock` + `monotonic_time`，参考项目使用 `current_run_queue` + `wall_time`，体现了全局锁 vs Per-CPU 的架构差异。 |
| **进程管理** | `modules/axtask/src/run_queue.rs#exit_current` | `arceos/modules/axtask/src/run_queue.rs#exit_current` | 同板块同名 | ArceOS 任务退出逻辑。目标项目使用 `EXITED_TASKS.lock()` 全局锁，参考项目使用 `EXITED_TASKS.current_ref_mut_raw()` Per-CPU 引用，同步原语选择不同。 |
| **设备驱动** | `tools/bwbench_client/src/main.rs#transmit_benchmark` | `arceos/tools/bwbench_client/src/main.rs#transmit_benchmark` | 同板块同名同结构 | ArceOS 生态的带宽测试工具，属于用户态测试程序，非内核核心代码。 |
| **进程管理** | `tools/raspi4/chainloader/src/main.rs#print_jtag_info_and_wait_forever` | `arceos/tools/raspi4/chainloader/src/main.rs#print_jtag_info_and_wait_forever` | 同板块同名同结构 | Raspberry Pi 4 的链式加载器模板代码，仅包含 `println!` 和 `cpu::wait_forever` 两个标准调用。 |
| **进程管理** | `ulib/axlibc/c/unistd.c#fork` | `arceos/ulib/axlibc/c/unistd.c#fork` | 同板块同名 | 均为 `unimplemented` 占位符，是 libc 移植中处理未实现系统调用的行业标准做法。 |

### 4. 核心代码调用流对比剖析
* **【进程管理 / execve 系统调用】**
  * **底层技术析理**: 两者实现路径完全不同。目标项目 (`syscall_execve`) 采用文件系统优先的方式，通过 `OpenOptions::new().read(...)` 打开可执行文件，并定义了一套基于 `LinuxError` 的详细错误码体系。参考项目 (`sys_execve`) 则专注于用户空间参数的解析，通过 `arg.get_as_str()` 和 `argv.get_as_null_terminated()` 等方法从用户态指针获取数据。两者在【抽象层次、错误处理模型、参数来源】上存在根本性异构。
  * **代码溯源链接**:
    - 目标项目: `modules/axsyscall/src/task.rs#syscall_execve`
    - 参考项目: `api/src/imp/task/execve.rs#sys_execve`

* **【内存管理 / 缺页异常处理】**
  * **底层技术析理**: 两者在内核态处理缺页中断的架构截然不同。目标项目 (`handle_page_fault`) 直接与硬件页表交互，通过 `KERNEL_SPACE.exclusive_access` 获取内核地址空间，并调用 `MapPermission::empty` 等底层页表操作。参考项目 (`handle_page_fault`) 则完全通过进程抽象层处理，调用 `current().task_ext().process_data().aspace.lock().handle_page_fault(...)`，将处理逻辑封装在进程地址空间对象中。这反映了【直接内存管理 vs 进程封装管理】的架构哲学差异。
  * **代码溯源链接**:
    - 目标项目: `modules/axruntime/src/trap.rs#handle_page_fault`
    - 参考项目: `src/mm.rs#handle_page_fault`

* **【任务调度/异步运行时 / 等待队列】**
  * **底层技术析理**: 两者均实现了等待队列，但同步策略不同。目标项目 (`wait_timeout_until`) 使用全局的 `RUN_QUEUE.lock` 来管理任务阻塞，并使用 `monotonic_time` 作为时钟源。参考项目 (`wait_timeout_until`) 则使用 `current_run_queue::<NoPreemptIrqSave>` 获取当前 CPU 的运行队列，并采用 `wall_time`。这体现了【全局锁 + 单调时钟 vs Per-CPU 队列 + 墙钟】的同步模型差异。
  * **代码溯源链接**:
    - 目标项目: `modules/axtask/src/wait_queue.rs#wait_timeout_until`
    - 参考项目: `arceos/modules/axtask/src/wait_queue.rs#wait_timeout_until`

### 5. 单方模块与降权项
*   **单方模块**: 无。所有宏观模块在双方项目中均有体现。
*   **降权项说明**:
    *   **第三方库/公共依赖**: `axlibc` (C 标准库), `smoltcp` (网络协议栈), `lwext4` (文件系统), `bwbench_client` (测试工具) 等模块中的相似性，均源于对成熟开源项目的集成或标准算法实现，不代表内核原创架构的抄袭。
    *   **跨板块证据**: 控方原始证据中将 `pipe::wait_event` 与 `epoll::sys_epoll_wait` 进行配对，属于跨板块硬凑，二者处理的对象（管道 vs. epoll）和所属模块完全不同，已按指令降权，不构成有效证据。
    *   **标准库/宏调用**: `println!`, `unimplemented`, `AtomicBool::new`, `asm::wfe` 等是 Rust 语言标准库或 ARM 架构标准指令，任何开发者在此场景下的实现都必然相同，不具备代码独创性，不能作为抄袭证据。
    *   **领域通用命名**: `exit_current`, `wait_timeout`, `procfs`, `ax_exit` 等是操作系统内核领域的标准术语，同名是领域必然，不代表代码复用。

### 6. 保守结论
综合评估，目标项目与参考项目的整体 AST 相似度仅为 18.55%，处于极低水平。观察到的有限相似性，经逐项剖析，均指向双方共同依赖的 ArceOS 上游框架、第三方开源库（如 smoltcp、lwext4）、标准 C 库算法以及硬件架构强相关的模板代码。在决定内核独创性的核心子系统，如进程管理（`execve` 实现）、内存管理（缺页处理）和任务调度（同步模型）上，两者展现出截然不同的架构设计和实现路径。因此，现有证据无法支持抄袭或实质性复用的指控，应认定为基于共同开源生态的独立开发项目。

------------------------------

