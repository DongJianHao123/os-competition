# 🛡️ OS 内核赛自动化代码级架构审计报告

**审计时间**: `2026-07-02 04:32:29`
**参赛作品名称**: `2026-T2026104229910571-XX`
**年份**: `2026`
**学校**: `XX`
**队伍名称**: `T2026104229910571`
**比赛名字**: `OS-Competition`
**比赛子赛道**: `Kernel-Implementation`
**仓库地址**: `https://gitlab.eduxiji.net/educg-group-43501-3132633/T2026104229910571-2627`
**推测开发语言**: `Rust / C (由AST探针自动识别)`
**特征函数量**: `948`

## ⚔️ 底层代码同源性对比 (AST Homology)
> 本部分由专门的代码审查智能体，对该项目与符合年份条件的历届最高相似度项目进行控制流与底层调用对比。新版报告会先给出确定性的全模块覆盖矩阵，再给出 Agent 的分模块解释，避免只围绕少量抽样函数展开。

### 🏆 Top 5 底层对标: vs `2024-萌新-南开大学`（南开大学 / 萌新）

#### 对方作品信息

**对方年份**: `2024`
**对方学校**: `南开大学`
**对方队伍名称**: `萌新`
**对方比赛名字**: `OS-Competition`
**对方比赛子赛道**: `Kernel-Implementation`
**对方仓库地址**: `https://gitlab.eduxiji.net/educg-group-26010-2376550/T202410055992606-2876`

> 双向覆盖率：共同 AST 指纹: 29；唯一 AST 指纹: 目标项目 845，参考项目 2483；覆盖率: 目标项目 3.43%，参考项目 1.17%

## 全模块对比总览（确定性证据）

> 总体覆盖率：共同 AST 指纹: 29；唯一 AST 指纹: 目标项目 845，参考项目 2483；覆盖率: 目标项目 3.43%，参考项目 1.17%

### 1. 模块覆盖矩阵
| 模块 | 目标函数数 | 参考函数数 | 共同唯一 AST 指纹 | Target 覆盖 | Base 覆盖 | 状态 |
| :--- | ---: | ---: | ---: | ---: | ---: | :--- |
| 进程管理 | 97 | 39 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |
| 任务调度/异步运行时 | 165 | 272 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |
| 内存管理 | 69 | 174 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |
| 中断/异常处理 | 14 | 29 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |
| 系统调用 | 50 | 67 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |
| 文件系统 | 233 | 285 | 2 | 1.0% | 0.86% | 双方存在且有结构重合 |
| 设备驱动 | 47 | 42 | 1 | 2.56% | 2.5% | 双方存在且有结构重合 |
| 网络协议栈 | 9 | 0 | 0 | 0.0% | 0.0% | 仅目标项目体现 |
| 文件系统/第三方库 | 55 | 730 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |
| 网络协议栈/第三方库 | 0 | 118 | 0 | 0.0% | 0.0% | 仅参考项目体现 |
| 第三方库 | 182 | 1374 | 16 | 9.41% | 1.75% | 双方存在且有结构重合 |
| 通用模块/其他 | 27 | 106 | 1 | 3.85% | 1.14% | 双方存在且有结构重合 |

### 2. 分模块证据清单

#### 进程管理
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `kernel/kernel/src/tasks/task.rs#exit` | `modules/axtask/src/api.rs#exit` | 同板块同名 | self.finish_process_exit | RUN_QUEUE.lock, RUN_QUEUE.lock().exit_current |
| `kernel/kernel/src/tasks/initproc.rs#run_test_script` | `api/linux_syscall_api/src/syscall_task/imp/task.rs#syscall_exec` | 同模块上下文对齐/差异观察 | Duration::from_secs, Some, current_time, current_time()
            .checked_sub, current_time()
            .checked_sub(started)
            .unwrap_or_else, current_time()
        .checked_sub ...(+7) | Err, Ok, Some, Vec::new, args_vec.len, args_vec.push ...(+20) |
| `kernel/kernel/src/tasks/initproc.rs#command_in_dir` | `modules/axfs/src/mounts.rs#procfs` | 同模块上下文对齐/差异观察 | CommandResult::Exited, Duration::from_secs, File::open, String::from, add_user_task_with_cwd, args.drain ...(+34) | Arc::new, Ok, file_over.write_at, file_somaxconn.write_at, fs::ramfs::RamFileSystem::new, proc_root.clone ...(+3) |
| `kernel/kernel/src/syscall/task.rs#sys_wait4` | `api/linux_syscall_api/src/syscall_task/imp/task.rs#syscall_wait4` | 同模块上下文对齐/差异观察 | Err, Ok, WaitPid, child_task.cleanup_resources_after_exit, child_task.clone, child_task.clone().map_or ...(+27) | Err, Ok, WaitFlags::from_bits, WaitFlags::from_bits(args[2] as u32).unwrap, WaitFuture::new, axtask::block_task ...(+8) |

#### 任务调度/异步运行时
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `kernel/kernel/src/tasks/async_ops.rs#futex_requeue` | `api/linux_syscall_api/src/syscall_task/imp/futex.rs#futex_requeue` | 同板块同名 | futex_table
        .get_mut, futex_table
        .get_mut(&uaddr)
        .map, futex_table
        .get_mut(&uaddr)
        .map(|x| x.drain(..wake_count as usize).count())
        .unwrap_or, futex_table.contains_key, futex_table.get_mut, futex_table.get_mut(&uaddr2).unwrap ...(+7) | FUTEX_WAIT_TASK.lock, dst_wait_task.append, futex_wait_task.contains_key, futex_wait_task.entry, futex_wait_task.entry(key_new).or_default, futex_wait_task.get_mut ...(+10) |
| `kernel/kernel/src/tasks/filetable.rs#deref_mut` | `modules/axtask/src/sync/mutex.rs#deref_mut` | 同板块同名 | 内部闭环逻辑 | 内部闭环逻辑 |
| `kernel/kernel/src/tasks/filetable.rs#deref` | `modules/axtask/src/sync/mutex.rs#deref` | 同板块同名 | 内部闭环逻辑 | 内部闭环逻辑 |
| `kernel/kernel/src/tasks/memset.rs#deref` | `modules/axtask/src/task.rs#deref` | 同板块同名 | 内部闭环逻辑 | self.0.deref |

#### 内存管理
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `kernel/filesystem/fs/src/fatfs_shim.rs#as_vfs_err` | `modules/axfs/src/fs/fatfs.rs#as_vfs_err` | 同板块同名 | 内部闭环逻辑 | 内部闭环逻辑 |
| `kernel/driver/kvirtio/src/virtio_impl.rs#dma_dealloc` | `modules/axdriver/src/ixgbe.rs#dma_dealloc` | 同板块同名 | (phy_page..phy_page + pages).contains, VIRTIO_CONTAINER.lock, VIRTIO_CONTAINER.lock().retain, trace!, x.0.raw | Layout::from_size_align, Layout::from_size_align(size, 8).unwrap, global_allocator, global_allocator().dealloc |
| `kernel/driver/kvirtio/src/virtio_impl.rs#dma_alloc` | `modules/axdriver/src/virtio.rs#dma_alloc` | 同板块同名 | NonNull::new, NonNull::new(paddr.get_mut_ptr()).unwrap, VIRTIO_CONTAINER.lock, VIRTIO_CONTAINER.lock().extend, frame_alloc_much, frame_alloc_much(pages).expect ...(+3) | NonNull::dangling, NonNull::new, NonNull::new(vaddr as _).unwrap, global_allocator, global_allocator().alloc_pages, paddr.as_usize ...(+2) |
| `kernel/kernel/src/main.rs#dealloc` | `modules/axmem/src/area.rs#dealloc` | 同板块同名 | PAGE_TABLE_FRAMES.lock, drop, frame_unalloc, frames.iter, frames.iter().position, frames.swap_remove ...(+2) | page_table.unmap_region, page_table.unmap_region(self.vaddr, self.size()).unwrap, self.pages.clear, self.size |

#### 中断/异常处理
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `kernel/kernel/src/syscall/types/signal.rs#restore_ctx` | `modules/axhal/src/platform/VisionFive2/irq.rs#register_handler` | 同模块上下文对齐/差异观察 | 内部闭环逻辑 | with_cause! |
| `kernel/kernel/src/syscall/types/signal.rs#store_ctx` | `modules/axhal/src/platform/riscv64_qemu_virt/irq.rs#register_handler` | 同模块上下文对齐/差异观察 | 内部闭环逻辑 | with_cause! |
| `kernel/driver/general-plic/src/plic.rs#set_irq_enable` | `modules/axtrap/src/arch/riscv/mod.rs#riscv_kernel_trap_handler` | 同模块上下文对齐/差异观察 | read_volatile, write_volatile | handle_irq, panic!, scause.bits, scause.cause, scause::read |
| `kernel/driver/general-plic/src/plic.rs#get_irq_claim` | `modules/axhal/src/irq.rs#register_handler_common` | 同模块上下文对齐/差异观察 | read_volatile | IRQ_HANDLER_TABLE.register_handler, axlog::ax_println!, set_enable, warn! |

#### 系统调用
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `kernel/kernel/src/syscall/sys.rs#sys_uname` | `api/linux_syscall_api/src/syscall_net/socket.rs#get` | 同模块上下文对齐/差异观察 | Ok, debug!, sys_domain.len, sys_machine.len, sys_name.len, sys_nodename.len ...(+9) | copy_nonoverlapping, drop, keep_alive.to_ne_bytes, panic!, s.keep_alive, s.keep_alive().is_some ...(+10) |
| `kernel/kernel/src/syscall/types/poll.rs#ctl` | `api/linux_syscall_api/src/syscall_net/socket.rs#set` | 同模块上下文对齐/差异观察 | data_map.get_mut, data_map.get_mut(&fd).map, data_map.insert, data_map.remove, self.data.lock | <[u8; 4]>::try_from, <[u8; 4]>::try_from(&opt[0..4]).unwrap, Err, Ok, Some, axnet::Duration::from_secs ...(+14) |
| `kernel/kernel/src/syscall/time.rs#sys_gettimeofday` | `api/linux_syscall_api/src/syscall_mem/imp.rs#syscall_shmget` | 同模块上下文对齐/差异观察 | Ok, core::mem::size_of::<i32>, debug!, realtime_now, realtime_now().into, self.check_user_writable_span ...(+2) | Err, MemorySet::add_shared_mem, MemorySet::create_shared_mem, Ok, ShmFlags::from_bits, axmem::KEY_TO_SHMID.lock ...(+8) |
| `kernel/kernel/src/syscall/sys.rs#sys_capget` | `api/linux_syscall_api/src/syscall_net/socket.rs#get` | 同模块上下文对齐/差异观察 | Ok, UserCapData::default, data.fill, data.is_valid, data.slice_mut_with_len, header.is_valid ...(+1) | bytes.as_bytes, bytes.as_ptr, bytes.len, copy_nonoverlapping, panic!, raw_socket.get_congestion ...(+4) |

#### 文件系统
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `kernel/filesystem/ramfs/src/lib.rs#read_dir` | `modules/axfs/src/fs/fatfs.rs#read_dir` | 同板块同名 | Ok, dir.name.clone, file.len.lock, file.name.clone, link.name.clone, link.target.len ...(+4) | Ok, VfsDirEntry::new, dirents.iter_mut, dirents.iter_mut().enumerate, dirents.len, entry.file_name ...(+6) |
| `kernel/filesystem/devfs/src/tty.rs#ioctl` | `modules/axfs/src/api/port.rs#ioctl` | 同板块同名 | Err, TermIoctlCmd::try_from, TermIoctlCmd::try_from(command as u32).map_err | Err |
| `kernel/filesystem/fs/src/file.rs#seek` | `modules/axfs/src/fs/fatfs.rs#seek` | 同板块同名 | Ok, Stat::default, self.inner.stat, self.offset.lock | Ok, Some, match pos {
            SeekFrom::Start(pos) => Some(pos),
            SeekFrom::Current(off) => self.position().checked_add_signed(off),
            SeekFrom::End(off) => size.checked_add_signed(off),
        }
        .ok_or, self.position, self.position().checked_add_signed, self.set_position ...(+3) |
| `kernel/filesystem/ramfs/src/lib.rs#remove` | `modules/axfs/src/root.rs#remove` | 同板块同名 | Err, Ok, self
            .inner
            .children
            .lock, self
            .inner
            .children
            .lock()
            .extract_if, self
            .inner
            .children
            .lock()
            .extract_if(.., |x| match x {
                FileContainer::File(x) => x.name == name,
                FileContainer::Dir(_) => false,
                FileContainer::Link(x) => x.name == name,
                FileContainer::Symlink(x) => x.name == name,
            })
            .count | ax_err!, fs.root_dir, fs.root_dir().remove, rest_path.is_empty, self.lookup_mounted_fs |

#### 设备驱动
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `kernel/driver/kvirtio/src/virtio_impl.rs#share` | `modules/axdriver/src/virtio.rs#share` | 同板块同名 | buffer.as_ptr, virt_to_phys, virt_to_phys(raw_ptr).unwrap_or | buffer.as_ptr, vaddr.into, virt_to_phys, virt_to_phys(vaddr.into()).into |
| `kernel/driver/kvirtio/src/virtio_impl.rs#unshare` | `modules/axdriver/src/virtio.rs#unshare` | 同板块同名同结构 | 内部闭环逻辑 | 内部闭环逻辑 |
| `kernel/driver/kvirtio/src/virtio_impl.rs#mmio_phys_to_virt` | `modules/axdriver/src/ixgbe.rs#mmio_phys_to_virt` | 同板块同名 | NonNull::new, NonNull::new((paddr | VIRT_ADDR_START) as *mut u8).unwrap | NonNull::new, NonNull::new(phys_to_virt(paddr.into()).as_mut_ptr()).unwrap, paddr.into, phys_to_virt, phys_to_virt(paddr.into()).as_mut_ptr |
| `kernel/driver/kvirtio/src/lib.rs#dump_bar_contents` | `modules/axdriver/src/lib.rs#init_drivers` | 同模块上下文对齐/差异观察 | Mutex::new, PCI_RANGES.lock, assert!, buf.as_mut_ptr, buf.iter, buf.iter().any ...(+8) | AllDevices::default, all_devs.block.iter, all_devs.block.iter().enumerate, all_devs.display.iter, all_devs.display.iter().enumerate, all_devs.net.iter ...(+5) |

#### 网络协议栈
仅目标项目中观察到该模块代表函数，参考项目没有同模块可对齐证据：
- 目标项目：`kernel/kernel/src/socket.rs#reuse`，调用摘要：Mutex::new, NET_SERVER.get_tcp, NET_SERVER.get_udp, self.inner.get_protocol, self.inner.get_protocol().unwrap, self.options.lock ...(+4)
- 目标项目：`kernel/kernel/src/socket.rs#new`，调用摘要：Arc::new, Mutex::new, NET_SERVER.blank_tcp, NET_SERVER.blank_udp, panic!, vec!
- 目标项目：`kernel/kernel/src/socket.rs#drop`，调用摘要：NET_SERVER.remote_tcp, NET_SERVER.remote_udp, local.port, log::debug!, self.inner.get_local, self.inner.get_protocol
- 目标项目：`kernel/kernel/src/socket.rs#note_socket_dequeue`，调用摘要：SOCKET_QUEUED_BYTES.compare_exchange_weak, SOCKET_QUEUED_BYTES.load, current.saturating_sub

#### 文件系统/第三方库
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `kernel/filesystem/ext4rsfs/src/lib.rs#write_offset` | `crates/another_ext4/src/ext4_defs/block.rs#write_offset` | 同板块同名 | buf.len, bytes_to_write.div_ceil, data[offset_in_block..offset_in_block + bytes_to_copy]
                .copy_from_slice, device.read_blocks, device.write_blocks, get_blk_device ...(+1) | data.len, self.data[offset..offset + data.len()].copy_from_slice |
| `kernel/filesystem/ext4fs/src/lib.rs#create` | `crates/axfs_ramfs/src/dir.rs#create` | 同板块同名 | CString::new, CString::new(fpath).unwrap, Errno::from_ret, Ok, c_fpath.into_raw, ext4_mknod ...(+13) | Ok, log::debug!, name.is_empty, self
                        .children
                        .read, self
                        .children
                        .read()
                        .get, self
                        .children
                        .read()
                        .get(name)
                        .ok_or ...(+8) |
| `kernel/filesystem/ext4rsfs/src/lib.rs#lookup` | `crates/axfs_devfs/src/dir.rs#lookup` | 同板块同名 | Arc::new, Errno::new, Ok, log::debug!, name.as_bytes, self
            .ext4
            .generic_open ...(+6) | Ok, node.lookup, self
                .children
                .read, self
                .children
                .read()
                .get, self
                .children
                .read()
                .get(name)
                .cloned, self
                .children
                .read()
                .get(name)
                .cloned()
                .ok_or ...(+4) |
| `kernel/filesystem/ext4fs/src/lib.rs#lookup` | `crates/axfs_ramfs/src/dir.rs#lookup` | 同板块同名 | Arc::new, Err, Ext4FileWrapper::new, Ok, file.check_inode_exist, fpath.as_str ...(+3) | Ok, node.lookup, self
                .children
                .read, self
                .children
                .read()
                .get, self
                .children
                .read()
                .get(name)
                .cloned, self
                .children
                .read()
                .get(name)
                .cloned()
                .ok_or ...(+4) |

#### 网络协议栈/第三方库
仅参考项目中观察到该模块代表函数，目标项目没有同模块可对齐证据：
- 参考项目：`modules/axnet/src/smoltcp_impl/tcp.rs#connect`，调用摘要：Ok, Ok::<(IpEndpoint, IpEndpoint), AxError>, SOCKET_SET
                .with_socket_mut::<tcp::Socket, _, _>, SOCKET_SET.add, SocketSetWrapper::new_tcp_socket, Some ...(+23)
- 参考项目：`modules/axnet/src/smoltcp_impl/tcp.rs#recv_timeout`，调用摘要：Err, Ok, SOCKET_SET.with_socket_mut::<tcp::Socket, _, _>, ax_err!, ax_err_type!, current_ticks ...(+11)
- 参考项目：`modules/axnet/src/smoltcp_impl/mod.rs#init`，调用摘要：Config::new, ETH0.init_by, EthernetAddress, GATEWAY.parse, GATEWAY.parse().expect, IP.parse ...(+22)
- 参考项目：`modules/axnet/src/smoltcp_impl/tcp.rs#shutdown`，调用摘要：LISTEN_TABLE.unlisten, Ok, SOCKET_SET.poll_interfaces, SOCKET_SET.with_socket_mut::<tcp::Socket, _, _>, debug!, self.handle.get ...(+9)

#### 第三方库
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `kernel/crates/libc-types/src/types.rs#handle` | `crates/handler_table/src/lib.rs#handle` | 同板块同名 | 内部闭环逻辑 | core::mem::transmute, handler, self.handlers[idx].load |
| `kernel/crates/libc-types/src/types.rs#partial_cmp` | `crates/timer_list/src/lib.rs#partial_cmp` | 同板块同名 | Some, self.sec.cmp, self.usec.cmp | Some, self.cmp |
| `kernel/crates/runtime/src/frame.rs#dealloc` | `crates/allocator/src/tlsf.rs#dealloc` | 同板块同名 | frm.dealloc | layout.align, layout.size, self.inner.deallocate |
| `kernel/crates/libc-types/src/types.rs#add` | `crates/flatten_objects/src/lib.rs#add` | 同板块同名 | 内部闭环逻辑 | Some, self.id_bitmap.first_false_index, self.id_bitmap.set, self.objects[id].write |

#### 通用模块/其他
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `kernel/kernel/src/logging.rs#write_str` | `modules/axlog/src/lib.rs#write_str` | 同板块同名 | Ok, c.encode_utf8, c.encode_utf8(&mut buffer).as_bytes, puts, s.chars | Ok, cfg_if::cfg_if! |
| `kernel/kernel/src/watchdog.rs#current_time_ns` | `modules/axhal/src/time.rs#current_time_nanos` | 同板块同结构 | current_time, duration_to_ns | current_ticks, ticks_to_nanos |
| `kernel/kernel/src/watchdog.rs#check_global_deadline_from_timer` | `modules/axhal/src/mem.rs#kernel_image_regions` | 同模块上下文对齐/差异观察 | GLOBAL_DEADLINE_FIRED.swap, GLOBAL_DEADLINE_NS.load, current_time, duration_to_ns, println!, shutdown | (_sbss as usize).into, (_sdata as usize).into, (_srodata as usize).into, (_stext as usize).into, (boot_stack as usize).into, [
        MemRegion {
            paddr: virt_to_phys((_stext as usize).into()),
            size: _etext as usize - _stext as usize,
            flags: MemRegionFlags::RESERVED | MemRegionFlags::READ | MemRegionFlags::EXECUTE,
            name: ".text",
        },
        MemRegion {
            paddr: virt_to_phys((_srodata as usize).into()),
            size: _erodata as usize - _srodata as usize,
            flags: MemRegionFlags::RESERVED | MemRegionFlags::READ,
            name: ".rodata",
        },
        MemRegion {
            paddr: virt_to_phys((_sdata as usize).into()),
            size: _edata as usize - _sdata as usize,
            flags: MemRegionFlags::RESERVED | MemRegionFlags::READ | MemRegionFlags::WRITE,
            name: ".data .tdata .tbss .percpu",
        },
        MemRegion {
            paddr: virt_to_phys((boot_stack as usize).into()),
            size: boot_stack_top as usize - boot_stack as usize,
            flags: MemRegionFlags::RESERVED | MemRegionFlags::READ | MemRegionFlags::WRITE,
            name: "boot stack",
        },
        MemRegion {
            paddr: virt_to_phys((_sbss as usize).into()),
            size: _ebss as usize - _sbss as usize,
            flags: MemRegionFlags::RESERVED | MemRegionFlags::READ | MemRegionFlags::WRITE,
            name: ".bss",
        },
    ]
    .into_iter ...(+1) |
| `kernel/kernel/src/shutdown.rs#shutdown` | `modules/axmem/src/lib.rs#find_free_area` | 同模块上下文对齐/差异观察 | core::arch::asm!, ged_addr.write_volatile, log::info! | Some, axconfig::USER_MEMORY_START.into, hint.max, hint.max(axconfig::USER_MEMORY_START.into()).as_usize, last_end.into, mem.size ...(+8) |


#### 1. 宏观判定
- **定性结论**: 独立开发 (相似度: 0.88%，共同 AST 指纹: 29；唯一 AST 指纹: 目标项目 845，参考项目 2483；覆盖率: 目标项目 3.43%，参考项目 1.17%)

#### 2. 全模块覆盖矩阵解读
- **进程管理**: 双方均存在该模块。共同 AST 指纹为 0，覆盖率为 0.0%。这表明双方在进程创建、调度、退出等核心逻辑上无任何可观测的结构性复用。证据强度：未形成。
- **任务调度/异步运行时**: 双方均存在该模块。共同 AST 指纹为 0，覆盖率为 0.0%。目标项目采用 Monolithic Task 模型，参考项目采用 axtask 异步执行器模型，底层调度原语完全不同。证据强度：未形成。
- **内存管理**: 双方均存在该模块。共同 AST 指纹为 0，覆盖率为 0.0%。双方在物理页分配、虚拟地址映射、内存区域管理等核心路径上无共享调用链。证据强度：未形成。
- **中断/异常处理**: 双方均存在该模块。共同 AST 指纹为 0，覆盖率为 0.0%。双方的中断注册、分发、上下文保存与恢复机制在 AST 层面无交集。证据强度：未形成。
- **系统调用**: 双方均存在该模块。共同 AST 指纹为 0，覆盖率为 0.0%。尽管双方都实现了 POSIX 接口，但内部调用链（如 wait4、exit、epoll）无共同指纹。证据强度：未形成。
- **文件系统**: 双方均存在该模块。共同 AST 指纹为 2，目标覆盖率 1.0%，参考覆盖率 0.86%。共同指纹位于 `read_dir` 和 `seek` 等通用文件操作，属于 VFS 接口的常规实现模式。证据强度：弱。
- **设备驱动**: 双方均存在该模块。共同 AST 指纹为 1，目标覆盖率 2.56%，参考覆盖率 2.5%。共同指纹为 virtio `share`/`unshare` 函数，源于 virtio 标准协议规定的队列共享操作，属于领域强制逻辑。证据强度：弱。
- **网络协议栈**: 仅目标项目体现。参考项目无此模块，无法形成同板块证据。证据强度：未形成。
- **第三方库/公共依赖**: 双方均存在该模块。共同 AST 指纹为 16，目标覆盖率 9.41%，参考覆盖率 1.75%。共同指纹集中于 `handle`、`partial_cmp`、`dealloc` 等泛型操作，属于 Rust 标准库或通用数据结构的基础方法。证据强度：弱。

#### 3. 底层 AST 关键证据指纹表
| 所属板块 | 目标项目路径 | 参考项目路径 | 证据类型 | 技术说明 |
| :--- | :--- | :--- | :--- | :--- |
| 设备驱动 | `kernel/driver/kvirtio/src/virtio_impl.rs#share` | `modules/axdriver/src/virtio.rs#share` | 同板块同名 | 均实现 virtio 队列的 DMA 共享操作，调用 `buffer.as_ptr` 和 `virt_to_phys`，源于 virtio 标准协议。 |
| 设备驱动 | `kernel/driver/kvirtio/src/virtio_impl.rs#unshare` | `modules/axdriver/src/virtio.rs#unshare` | 同板块同名同结构 | 均为内部闭环逻辑，对应 virtio 队列的 unshare 操作，是 share 的逆操作。 |
| 文件系统 | `kernel/filesystem/ramfs/src/lib.rs#read_dir` | `modules/axfs/src/fs/fatfs.rs#read_dir` | 同板块同名 | 均实现目录读取，调用 `dir.name.clone` 或 `VfsDirEntry::new`，属于 VFS 通用模式。 |
| 文件系统 | `kernel/filesystem/fs/src/file.rs#seek` | `modules/axfs/src/fs/fatfs.rs#seek` | 同板块同名 | 均实现文件 seek 操作，处理 `SeekFrom` 枚举，属于 POSIX 标准接口的常规实现。 |
| 第三方库 | `kernel/crates/libc-types/src/types.rs#handle` | `crates/handler_table/src/lib.rs#handle` | 同板块同名 | 均为句柄/处理器表的索引或调用操作，属于通用数据结构方法。 |
| 第三方库 | `kernel/crates/libc-types/src/types.rs#partial_cmp` | `crates/timer_list/src/lib.rs#partial_cmp` | 同板块同名 | 均实现 `PartialOrd` trait 的 `partial_cmp` 方法，属于 Rust 标准 trait 实现。 |
| 第三方库 | `kernel/crates/runtime/src/frame.rs#dealloc` | `crates/allocator/src/tlsf.rs#dealloc` | 同板块同名 | 均实现内存释放，调用底层分配器的 `dealloc` 或 `deallocate`，属于通用内存管理操作。 |
| 第三方库 | `kernel/crates/libc-types/src/types.rs#add` | `crates/flatten_objects/src/lib.rs#add` | 同板块同名 | 均实现向集合添加元素的操作，属于通用数据结构方法。 |
| 通用模块/其他 | `kernel/kernel/src/logging.rs#write_str` | `modules/axlog/src/lib.rs#write_str` | 同板块同名 | 均实现 `core::fmt::Write` trait 的 `write_str` 方法，属于 Rust 标准 trait 实现。 |

#### 4. 核心代码调用流对比剖析
* **【进程管理/任务退出路径】**
* **底层技术析理**: 目标项目采用 Monolithic Task 模型，退出流程为 `exit` -> `finish_process_exit`，核心操作是向父进程 TCB 发送 POSIX 信号 (`parent.tcb.write().signal.insert`) 并清理资源。参考项目采用 axtask 异步执行器模型，退出流程为 `exit` -> `exit_current`，核心操作是更新调度器状态 (`curr.inner.lock().set_state`) 并调用 `axhal::misc::terminate` 终止执行。两者在任务抽象、退出状态管理、父子进程通信机制上完全不同。
* **代码溯源链接**:
  - 目标项目: `kernel/kernel/src/tasks/task.rs#exit`
  - 参考项目: `modules/axtask/src/api.rs#exit`

* **【进程管理/等待子进程路径】**
* **底层技术析理**: 目标项目 `sys_wait4` 通过 `WaitPid` 对象和 `child_task.final_exit_code()` 同步获取退出码。参考项目 `syscall_wait4` 通过 `WaitFuture::new` 和 `axtask::block_task` 实现异步等待。两者在等待语义（同步 vs 异步）、阻塞机制（自旋/挂起 vs Future 驱动）上存在根本差异。
* **代码溯源链接**:
  - 目标项目: `kernel/kernel/src/syscall/task.rs#sys_wait4`
  - 参考项目: `api/linux_syscall_api/src/syscall_task/imp/task.rs#syscall_wait4`

* **【文件系统/虚拟文件系统初始化】**
* **底层技术析理**: 目标项目 procfs 基于 `StaticDir`/`StaticFile` 静态节点架构，通过 `Interrupts::new`、`MemInfo::new` 等独立节点类型构建。参考项目 procfs 基于 `RamFileSystem` 动态节点，通过 `proc_root.create` 和 `file_over.write_at` 动态创建并写入文件。两者在文件系统后端、节点生命周期管理上完全不同。
* **代码溯源链接**:
  - 目标项目: `kernel/filesystem/procfs/src/lib.rs#new`
  - 参考项目: `modules/axfs/src/mounts.rs#procfs`

* **【设备驱动/virtio 队列共享】**
* **底层技术析理**: 双方 `share` 函数均实现 virtio 队列的 DMA 地址共享，调用 `buffer.as_ptr` 和 `virt_to_phys`。此逻辑由 virtio 标准协议强制规定，任何 virtio 驱动实现都必须将缓冲区物理地址写入描述符表，属于领域强制趋同，不构成原创性复用。
* **代码溯源链接**:
  - 目标项目: `kernel/driver/kvirtio/src/virtio_impl.rs#share`
  - 参考项目: `modules/axdriver/src/virtio.rs#share`

#### 5. 单方模块与降权项
- **目标项目独有模块**:
  - **网络协议栈**: `kernel/kernel/src/socket.rs` 中的 `reuse`、`new`、`drop` 等函数，使用 `NET_SERVER` 对象管理 TCP/UDP 套接字。参考项目无此模块，无法形成同板块比对。
  - **LTP 测试基础设施**: `kernel/kernel/src/tasks/initproc.rs` 中的 `run_test_script`、`prepare_ltp_rootfs`、`clear_ltp_case_scratch` 等函数，为目标项目独立设计的测试 harness。
  - **全局 deadline 看门狗**: `arm_global_deadline_after`、`disarm_global_deadline` 等机制，为目标项目独立引入的超时保护机制。
- **参考项目独有模块**:
  - **网络协议栈/第三方库**: `modules/axnet/src/smoltcp_impl/` 下的 `connect`、`recv_timeout`、`init` 等函数，基于 smoltcp 协议栈实现。目标项目无此模块。
- **降权项说明**:
  - 第三方库中的 `handle`、`partial_cmp`、`dealloc`、`add` 等共同指纹，均为 Rust 标准 trait 实现或通用数据结构方法，在任何 Rust 项目中都可能出现，不能作为同源证据。
  - 设备驱动中的 `share`/`unshare` 共同指纹，源于 virtio 标准协议，属于领域强制逻辑。
  - 文件系统中的 `read_dir`、`seek` 共同指纹，属于 POSIX VFS 接口的常规实现模式。
  - 跨板块同名函数（如 `exit`、`new`）因所在模块和调用上下文完全不同，不构成同板块主证据。

#### 6. 保守结论
本案客观 AST 相似度仅 0.88%，进程管理等核心板块共同指纹为零。29 个共同指纹集中于第三方库泛型操作、virtio 协议强制逻辑及 VFS 通用接口，均属领域常识或标准实现。目标项目独有的网络协议栈、LTP 测试 harness、全局 deadline 看门狗等模块，以及 Monolithic Task 与 axtask 异步执行器的根本架构差异，均指向独立开发。控方基于跨板块业务概念相似性提出的复用指控，缺乏 AST 结构证据支持。综合判定，目标项目不存在对参考项目的结构性代码复用，整体风险为低。

------------------------------

