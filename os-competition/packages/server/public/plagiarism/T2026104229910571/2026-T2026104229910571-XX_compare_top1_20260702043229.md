# 🛡️ OS 内核赛自动化代码级架构审计报告

**审计时间**: `2026-07-02 04:32:29`
**参赛作品名称**: `2026-T2026104229910571-XX`
**年份**: `2026`
**学校**: `XX`
**队伍名称**: `T2026104229910571`
**比赛名字**: `OS-Competition`
**比赛子赛道**: `Kernel-Implementation`
**仓库地址**: `https://gitlab.eduxiji.net/educg-group-43501-3132633/T2026104229910571-2627`
**推测开发语言**: `Rust / C (由AST探针自动识别)`
**特征函数量**: `948`

## ⚔️ 底层代码同源性对比 (AST Homology)
> 本部分由专门的代码审查智能体，对该项目与符合年份条件的历届最高相似度项目进行控制流与底层调用对比。新版报告会先给出确定性的全模块覆盖矩阵，再给出 Agent 的分模块解释，避免只围绕少量抽样函数展开。

### 🏆 Top 1 底层对标: vs `2025-Moncake-天津大学`（天津大学 / Moncake）

#### 对方作品信息

**对方年份**: `2025`
**对方学校**: `天津大学`
**对方队伍名称**: `Moncake`
**对方比赛名字**: `OS-Competition`
**对方比赛子赛道**: `Kernel-Implementation`
**对方仓库地址**: `https://gitlab.eduxiji.net/educg-group-36002-2710490/T202510056995244-24`

> 双向覆盖率：共同 AST 指纹: 330；唯一 AST 指纹: 目标项目 845，参考项目 656；覆盖率: 目标项目 39.05%，参考项目 50.3%

## 全模块对比总览（确定性证据）

> 总体覆盖率：共同 AST 指纹: 330；唯一 AST 指纹: 目标项目 845，参考项目 656；覆盖率: 目标项目 39.05%，参考项目 50.3%

### 1. 模块覆盖矩阵
| 模块 | 目标函数数 | 参考函数数 | 共同唯一 AST 指纹 | Target 覆盖 | Base 覆盖 | 状态 |
| :--- | ---: | ---: | ---: | ---: | ---: | :--- |
| 进程管理 | 97 | 50 | 10 | 11.11% | 27.78% | 双方存在且有结构重合 |
| 任务调度/异步运行时 | 165 | 144 | 32 | 20.51% | 22.54% | 双方存在且有结构重合 |
| 内存管理 | 69 | 61 | 10 | 14.49% | 16.39% | 双方存在且有结构重合 |
| 中断/异常处理 | 14 | 7 | 6 | 42.86% | 85.71% | 双方存在且有结构重合 |
| 系统调用 | 50 | 42 | 4 | 10.26% | 10.53% | 双方存在且有结构重合 |
| 文件系统 | 233 | 191 | 109 | 54.5% | 65.66% | 双方存在且有结构重合 |
| 设备驱动 | 47 | 52 | 34 | 87.18% | 75.56% | 双方存在且有结构重合 |
| 网络协议栈 | 9 | 6 | 5 | 55.56% | 83.33% | 双方存在且有结构重合 |
| 文件系统/第三方库 | 55 | 54 | 36 | 72.0% | 73.47% | 双方存在且有结构重合 |
| 第三方库 | 182 | 107 | 80 | 47.06% | 74.77% | 双方存在且有结构重合 |
| 通用模块/其他 | 27 | 16 | 10 | 38.46% | 66.67% | 双方存在且有结构重合 |

### 2. 分模块证据清单

#### 进程管理
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `kernel/kernel/src/syscall/task.rs#sys_wait4` | `kernel/src/syscall/task.rs#sys_wait4` | 同板块同名 | Err, Ok, WaitPid, child_task.cleanup_resources_after_exit, child_task.clone, child_task.clone().map_or ...(+27) | Err, Ok, WaitPid, child_task.clone, child_task.clone().map_or, child_task.pcb.lock ...(+27) |
| `kernel/kernel/src/tasks/initproc.rs#initproc` | `kernel/src/tasks/initproc.rs#initproc` | 同板块同名 | Duration::from_secs, arm_global_deadline_after, current_time, disarm_global_deadline, global_deadline_secs, kill_all_tasks ...(+7) | "/glibc/lib".to_string, "/glibc/lib/ld-linux-loongarch-lp64d.so.1".to_string, "/lib/ld-musl-riscv64.so.1".to_string, "/musl/lib/libc.so".to_string, "/usr/lib".to_string, PathBuf::from ...(+21) |
| `kernel/kernel/src/tasks/initproc.rs#kill_all_tasks` | `kernel/src/tasks/initproc.rs#kill_all_tasks` | 同板块同名 | (task.get_task_type() == TaskType::MonolithicTask && *task_id != current_id)
                    .then_some, TASK_MAP
            .lock, TASK_MAP
            .lock()
            .iter, TASK_MAP
            .lock()
            .iter()
            .filter_map, TASK_MAP
            .lock()
            .iter()
            .filter_map(|(task_id, task)| {
                let task = task.upgrade()?;
                (task.get_task_type() == TaskType::MonolithicTask && *task_id != current_id)
                    .then_some((*task_id, task))
            })
            .collect, current_task ...(+19) | TASK_MAP.lock, TASK_MAP.lock().values, TASK_MAP.lock().values().into_iter, TASK_MAP.lock().values().into_iter().for_each, task.upgrade, task.upgrade().inspect ...(+2) |
| `kernel/kernel/src/syscall/signal.rs#sys_sigprocmask` | `kernel/src/syscall/signal.rs#sys_sigprocmask` | 同板块同名 | Ok, SigMaskHow::try_from, SigMaskHow::try_from(how).map_err, debug!, drop, oldset.is_valid ...(+5) | Ok, SigMaskHow::from_usize, SigMaskHow::from_usize(how).ok_or, debug!, drop, oldset.get_mut ...(+5) |

#### 任务调度/异步运行时
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `kernel/kernel/src/syscall/fd.rs#sys_pselect` | `kernel/src/syscall/fd.rs#sys_pselect` | 同板块同名 | Ok, cmp::min, current_time, debug!, drop, efds.get_bit ...(+28) | Ok, cmp::min, current_nsec, debug!, drop, efds.get_bit ...(+24) |
| `kernel/kernel/src/syscall/task.rs#sys_futex` | `kernel/src/syscall/task.rs#sys_futex` | 同板块同名 | Err, FutexFlags::try_from, FutexFlags::try_from(op).map_err, Ok, UserRef::<TimeSpec>::from, UserRef::<TimeSpec>::from(value2).read ...(+21) | Err, Ok, UserRef::<TimeSpec>::from, UserRef::<TimeSpec>::from(value2).get_mut, WaitFutex, WaitUntilsec ...(+20) |
| `kernel/kernel/src/user/mod.rs#handle_syscall` | `kernel/src/user/mod.rs#handle_syscall` | 同板块同名 | cx_ref.args, cx_ref.syscall_ok, debug!, e.into_raw, get_ticks, matches! ...(+9) | Sysno::rt_sigreturn.id, Time::now, Time::now().raw, cx_ref.args, cx_ref.syscall_ok, e.into_raw ...(+5) |
| `kernel/kernel/src/syscall/fd.rs#sys_ioctl` | `kernel/src/syscall/fd.rs#sys_ioctl` | 同板块同名 | Err, Ok, TermIoctlCmd::try_from, UserRef::<Termios>::from, UserRef::<Termios>::from(arg1).read, UserRef::<WinSize>::from ...(+11) | debug!, self.task
            .get_fd, self.task
            .get_fd(fd)
            .ok_or, self.task
            .get_fd(fd)
            .ok_or(Errno::EINVAL)?
            .ioctl, self.task
            .get_fd(fd)
            .ok_or(Errno::EINVAL)?
            .ioctl(request, arg1)
            .map_err |

#### 内存管理
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `kernel/kernel/src/tasks/exec.rs#exec_with_process` | `kernel/src/tasks/exec.rs#exec_with_process` | 同板块同名 | "/busybox".to_string, Arc::new, Arc::new(file).clone, Err, File::open_link, Ok ...(+93) | (virt_addr + mem_size).div_ceil, File::open, File::open(path.clone(), OpenFlags::O_RDONLY)
            .map, File::open(path.clone(), OpenFlags::O_RDONLY)
            .map(Arc::new)?
            .clone, Ok, String::from ...(+84) |
| `kernel/kernel/src/syscall/mm.rs#sys_mmap` | `kernel/src/syscall/mm.rs#sys_mmap` | 同板块同名 | Err, MapFlags::from_bits_truncate, MmapProt::from_bits_truncate, Ok, Some, UserRef::<u8>::from ...(+32) | (|| -> Result<(), Errno> {
                let buffer = UserRef::<u8>::from(addr).slice_mut_with_len(len);
                file.readat(off, buffer).map(|_| ())
            }), Err, MapFlags::from_bits_truncate, MmapProt::from_bits_truncate, Ok, Some ...(+32) |
| `kernel/kernel/src/tasks/memset.rs#sub` | `kernel/src/tasks/memset.rs#sub` | 同板块同名 | Some, jrange.contains, new_area_range.contains, new_area_range.len, new_self_rang.contains, pt.unmap_page ...(+18) | Some, jrange.contains, new_area_range.contains, new_area_range.len, new_self_rang.contains, pt.unmap_page ...(+17) |
| `kernel/kernel/src/main.rs#kernel_interrupt` | `kernel/src/main.rs#kernel_interrupt` | 同板块同名 | arm_watchdog_tick, current.downcast_arc::<UserTask>, current.downcast_arc::<UserTask>().ok, current_task, current_user_task, get_int_device ...(+11) | current_task, current_task().downcast_arc::<UserTask>, current_task().downcast_arc::<UserTask>().ok, current_user_task, get_int_device, get_int_device().try_handle_interrupt ...(+8) |

#### 中断/异常处理
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `kernel/driver/general-plic/src/plic.rs#set_irq_enable` | `driver/general-plic/src/plic.rs#set_irq_enable` | 同板块同名同结构 | read_volatile, write_volatile | read_volatile, write_volatile |
| `kernel/driver/general-plic/src/plic.rs#get_irq_claim` | `driver/general-plic/src/plic.rs#get_irq_claim` | 同板块同名同结构 | read_volatile | read_volatile |
| `kernel/driver/general-plic/src/plic.rs#complete_irq_claim` | `driver/general-plic/src/plic.rs#complete_irq_claim` | 同板块同名同结构 | write_volatile | write_volatile |
| `kernel/driver/general-plic/src/lib.rs#register_irq` | `driver/general-plic/src/lib.rs#register_irq` | 同板块同名同结构 | log::info!, self.set_irq_enable, self.set_priority | log::info!, self.set_irq_enable, self.set_priority |

#### 系统调用
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `kernel/kernel/src/syscall/sys.rs#sys_uname` | `kernel/src/syscall/sys.rs#sys_uname` | 同板块同名 | Ok, debug!, sys_domain.len, sys_machine.len, sys_name.len, sys_nodename.len ...(+9) | Ok, debug!, sys_domain.len, sys_machine.len, sys_name.len, sys_nodename.len ...(+9) |
| `kernel/kernel/src/syscall/types/poll.rs#ctl` | `kernel/src/syscall/types/poll.rs#ctl` | 同板块同名 | data_map.get_mut, data_map.get_mut(&fd).map, data_map.insert, data_map.remove, self.data.lock | self.data.lock, self.data.lock().get_mut, self.data.lock().get_mut(&fd).map, self.data.lock().insert, self.data.lock().remove |
| `kernel/kernel/src/syscall/time.rs#sys_gettimeofday` | `kernel/src/syscall/time.rs#sys_gettimeofday` | 同板块同名 | Ok, core::mem::size_of::<i32>, debug!, realtime_now, realtime_now().into, self.check_user_writable_span ...(+2) | Ok, current_timeval, debug!, tv_ptr.get_mut |
| `kernel/kernel/src/syscall/shm.rs#sys_shmctl` | `kernel/src/syscall/shm.rs#sys_shmctl` | 同板块同名 | Err, Ok, SHARED_MEMORY.lock, SHARED_MEMORY.lock().get_mut, debug!, map.deleted.lock | Err, Ok, SHARED_MEMORY.lock, SHARED_MEMORY.lock().get_mut, debug!, map.deleted.lock |

#### 文件系统
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `kernel/kernel/src/socket.rs#readat` | `filesystem/fs/src/pipe.rs#readat` | 同板块同名 | DebugConsole::putchar, Err, Ok, buffer.len, buffer[..rlen].copy_from_slice, cmp::min ...(+8) | Err, Ok, Weak::strong_count, buffer.is_empty, buffer.len, cmp::min ...(+7) |
| `kernel/filesystem/ramfs/src/lib.rs#read_dir` | `filesystem/ramfs/src/lib.rs#read_dir` | 同板块同名 | Ok, dir.name.clone, file.len.lock, file.name.clone, link.name.clone, link.target.len ...(+4) | Ok, dir.name.clone, file.len.lock, file.name.clone, link.name.clone, self
            .inner
            .children
            .lock ...(+3) |
| `kernel/kernel/src/utils/mod.rs#hexdump` | `kernel/src/utils/mod.rs#hexdump` | 同板块同名同结构 | (0..data.len()).step_by, data.len, print!, println! | (0..data.len()).step_by, data.len, print!, println! |
| `kernel/filesystem/fs/src/fatfs_shim.rs#link` | `filesystem/fs/src/fatfs_shim.rs#link` | 同板块同名同结构 | unimplemented! | unimplemented! |

#### 设备驱动
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `kernel/driver/kvirtio/src/lib.rs#dump_bar_contents` | `driver/kvirtio/src/lib.rs#dump_bar_contents` | 同板块同名 | Mutex::new, PCI_RANGES.lock, assert!, buf.as_mut_ptr, buf.iter, buf.iter().any ...(+8) | buf.as_mut_ptr, buf.iter, buf.iter().any, core::ptr::copy, info!, root.bar_info ...(+3) |
| `kernel/driver/kvirtio/src/lib.rs#enumerate_pci` | `driver/kvirtio/src/lib.rs#enumerate_pci` | 同板块同名 | MmioCam::new, PciRoot::<MmioCam>::new, PciTransport::new::<HalImpl, MmioCam>, PciTransport::new::<HalImpl, MmioCam>(&mut pci_root, device_function).unwrap, dump_bar_contents, info! ...(+5) | MmioCam::new, PciRoot::<MmioCam>::new, PciTransport::new::<HalImpl, MmioCam>, assign_bars_with_pci_root, error!, info! ...(+7) |
| `kernel/driver/kvirtio/src/lib.rs#init_mmio` | `driver/kvirtio/src/lib.rs#init_mmio` | 同板块同名同结构 | Arc::new, MmioTransport::new, NonNull::new, NonNull::new(vaddr as *mut VirtIOHeader).unwrap, info!, node.reg ...(+3) | Arc::new, MmioTransport::new, NonNull::new, NonNull::new(vaddr as *mut VirtIOHeader).unwrap, info!, node.reg ...(+3) |
| `kernel/driver/ns16550a/src/lib.rs#init_driver` | `driver/ns16550a/src/lib.rs#init_driver` | 同板块同名同结构 | Arc::new, Uart::new, info!, node.reg, node.reg().unwrap, node.reg().unwrap().next ...(+5) | Arc::new, Uart::new, info!, node.reg, node.reg().unwrap, node.reg().unwrap().next ...(+5) |

#### 网络协议栈
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `kernel/kernel/src/socket.rs#reuse` | `kernel/src/socket.rs#reuse` | 同板块同名同结构 | Mutex::new, NET_SERVER.get_tcp, NET_SERVER.get_udp, self.inner.get_protocol, self.inner.get_protocol().unwrap, self.options.lock ...(+4) | Mutex::new, NET_SERVER.get_tcp, NET_SERVER.get_udp, self.inner.get_protocol, self.inner.get_protocol().unwrap, self.options.lock ...(+4) |
| `kernel/kernel/src/socket.rs#new_with_inner` | `kernel/src/socket.rs#new_with_inner` | 同板块同名同结构 | Arc::new, Mutex::new, vec! | Arc::new, Mutex::new, vec! |
| `kernel/kernel/src/socket.rs#from_usize` | `kernel/src/socket.rs#from_usize` | 同板块同名同结构 | Some | Some |
| `kernel/kernel/src/user/socket_pair.rs#create_socket_pair` | `kernel/src/user/socket_pair.rs#create_socket_pair` | 同板块同名同结构 | Arc::new, Mutex::new, VecDeque::new | Arc::new, Mutex::new, VecDeque::new |

#### 文件系统/第三方库
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `kernel/filesystem/ext4rsfs/src/lib.rs#write_offset` | `filesystem/fs/src/ext4_rs_shim.rs#write_offset` | 同板块同名 | buf.len, bytes_to_write.div_ceil, data[offset_in_block..offset_in_block + bytes_to_copy]
                .copy_from_slice, device.read_blocks, device.write_blocks, get_blk_device ...(+1) | buf.len, bytes_to_write.div_ceil, data[offset_in_block..offset_in_block + bytes_to_copy]
                .copy_from_slice, device.read_blocks, device.write_blocks, get_blk_device ...(+1) |
| `kernel/filesystem/ext4fs/src/lib.rs#create` | `filesystem/fs/src/ext4_shim.rs#create` | 同板块同名 | CString::new, CString::new(fpath).unwrap, Errno::from_ret, Ok, c_fpath.into_raw, ext4_mknod ...(+13) | CString::new, CString::new(fpath).unwrap, Ok, core::mem::MaybeUninit::uninit, ext4_type.clone, file.check_inode_exist ...(+15) |
| `kernel/filesystem/ext4rsfs/src/lib.rs#lookup` | `filesystem/fs/src/ext4_rs_shim.rs#lookup` | 同板块同名同结构 | Arc::new, Errno::new, Ok, log::debug!, name.as_bytes, self
            .ext4
            .generic_open ...(+6) | Arc::new, Errno::new, Ok, log::debug!, name.as_bytes, self
            .ext4
            .generic_open ...(+6) |
| `kernel/filesystem/ext4fs/src/lib.rs#lookup` | `filesystem/fs/src/ext4_shim.rs#lookup` | 同板块同名 | Arc::new, Err, Ext4FileWrapper::new, Ok, file.check_inode_exist, fpath.as_str ...(+3) | Arc::new, Err, Ext4FileWrapper::new, Ok, file.check_inode_exist, fpath.as_str ...(+3) |

#### 第三方库
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `kernel/crates/executor/src/executor.rs#run_ready_task` | `crates/executor/src/executor.rs#run_ready_task` | 同板块同名 | Arc::new, Arc::new(Waker {
                task_id: task.get_task_id(),
            })
            .into, Context::from_waker, Some, TASK_MAP.lock, TASK_MAP.lock().remove ...(+13) | Arc::new, Arc::new(Waker {
                task_id: task.get_task_id(),
            })
            .into, Context::from_waker, Some, TASK_QUEUE.lock, TASK_QUEUE.lock().pop_front ...(+8) |
| `kernel/crates/runtime/src/frame.rs#alloc_much` | `crates/runtime/src/frame.rs#alloc_much` | 同板块同名 | (i..j).for_each, FrameTracker::new, Some, Vec::new, ans.push, pa! ...(+4) | FrameTracker::new, Some, Vec::new, ans.push, max_consecutive.max, pa! ...(+6) |
| `kernel/crates/devices/src/device.rs#add_device` | `crates/devices/src/device.rs#add_device` | 同板块同名 | INT_DEVICE.init_by, MAIN_UART.init_by, device.clone, device.get_device_wrapper, self.blk.push, self.input.push ...(+4) | INT_DEVICE.init_by, MAIN_UART.init_by, device.clone, device.get_device_wrapper, self.blk.push, self.input.push ...(+4) |
| `kernel/crates/devices/src/lib.rs#try_to_add_device` | `crates/devices/src/lib.rs#try_to_add_device` | 同板块同名 | ALL_DEVICES.lock, ALL_DEVICES.lock().add_device, DRIVER_REGS.lock, driver_manager.get, f, info! ...(+2) | ALL_DEVICES.lock, ALL_DEVICES.lock().add_device, DRIVER_REGS.lock, driver_manager.get, f, info! ...(+2) |

#### 通用模块/其他
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `kernel/kernel/src/logging.rs#write_str` | `kernel/src/logging.rs#write_str` | 同板块同名同结构 | Ok, c.encode_utf8, c.encode_utf8(&mut buffer).as_bytes, puts, s.chars | Ok, c.encode_utf8, c.encode_utf8(&mut buffer).as_bytes, puts, s.chars |
| `kernel/kernel/src/utils/useref.rs#slice_until_valid` | `kernel/src/utils/useref.rs#slice_until_valid` | 同板块同名同结构 | self.addr.raw, self.addr.slice_until | self.addr.raw, self.addr.slice_until |
| `kernel/kernel/src/logging.rs#print` | `kernel/src/logging.rs#print` | 同板块同名同结构 | Logger
        .write_fmt, Logger
        .write_fmt(args)
        .expect | Logger
        .write_fmt, Logger
        .write_fmt(args)
        .expect |
| `kernel/kernel/src/utils/useref.rs#slice_mut_with_len` | `kernel/src/utils/useref.rs#slice_mut_with_len` | 同板块同名同结构 | self.addr.slice_mut_with_len | self.addr.slice_mut_with_len |


好的，审判长。本庭已收到控辩双方的全部陈述、证据底座及原始 AST 抽样数据。现依据审计指令，对本案进行最终同源性定性仲裁。

### 1. 宏观判定
- **定性结论**: **独立开发 / 底层架构显著差异**
  - **客观 AST 相似度**: 28.18%
  - **共同 AST 指纹**: 330
  - **唯一 AST 指纹**: 目标项目 845，参考项目 656
  - **覆盖率**: 目标项目 39.05%，参考项目 50.3%

本庭认定，28.18% 的全局 AST 相似度远低于构成“系统性抄袭”的阈值。在操作系统内核这一高度受限于硬件规范 (ISA)、标准接口 (POSIX) 和编程语言惯用法 (Rust) 的领域，此相似度水平更符合两个独立项目因遵循共同标准而产生的基线噪声。控方关于【进程管理】板块“重度抄袭”的指控，因证据强度不足且存在跨板块硬凑，不予支持。

### 2. 全模块覆盖矩阵解读
| 模块 | 目标函数数 | 参考函数数 | 共同唯一 AST 指纹 | Target 覆盖 | Base 覆盖 | 证据强度 | 解读 |
| :--- | ---: | ---: | ---: | ---: | ---: | :--- | :--- |
| **进程管理** | 97 | 50 | 10 | 11.11% | 27.78% | **弱** | 双方均存在该模块。覆盖率极低，表明核心实现逻辑差异巨大。共同指纹多集中于 `sys_getpid`、`sys_exit_group` 等短调用链的 POSIX 标准系统调用，不构成特异性同源证据。 |
| **任务调度/异步运行时** | 165 | 144 | 32 | 20.51% | 22.54% | **弱** | 双方均存在该模块。覆盖率低，且双方调度器设计存在根本差异（如目标项目的全局截止时间调度），共同指纹多为 `Arc::new`、`Mutex::new` 等 Rust 异步运行时通用原语。 |
| **内存管理** | 69 | 61 | 10 | 14.49% | 16.39% | **弱** | 双方均存在该模块。覆盖率低，共同指纹集中在 `sys_mmap` 等标准系统调用入口，其内部实现（如页面分配、映射管理）路径差异显著。 |
| **中断/异常处理** | 14 | 7 | 6 | 42.86% | 85.71% | **中** | 双方均存在该模块。覆盖率较高，但证据集中在 `general-plic` 平台级中断控制器驱动，其操作逻辑由硬件手册严格规定，属于硬件强制收敛，非软件抄袭。 |
| **系统调用** | 50 | 42 | 4 | 10.26% | 10.53% | **弱** | 双方均存在该模块。覆盖率极低，共同指纹为 `sys_uname` 等返回静态信息的系统调用，实现路径高度相似是功能需求使然。 |
| **文件系统** | 233 | 191 | 109 | 54.5% | 65.66% | **中** | 双方均存在该模块。覆盖率较高，但证据多为 `procfs`、`ramfs` 等内存文件系统的通用接口实现（如 `readat`、`read_dir`），以及 `ext4` 的第三方库封装。此类代码结构模式固定，独立开发易趋同。 |
| **设备驱动** | 47 | 52 | 34 | 87.18% | 75.56% | **中** | 双方均存在该模块。覆盖率很高，但证据几乎全部集中在 `kvirtio` 和 `ns16550a` 等遵循 VirtIO 和工业标准规范的驱动初始化流程，属于硬件驱动的标准范式。 |
| **网络协议栈** | 9 | 6 | 5 | 55.56% | 83.33% | **弱** | 双方均存在该模块。函数基数小，共同指纹为 `Socket` 结构的构造函数和 `reuse` 方法，属于 Rust 网络编程的常见抽象。 |
| **文件系统/第三方库** | 55 | 54 | 36 | 72.0% | 73.47% | **中** | 双方均存在该模块。高覆盖率源于对 `ext4_rs` 等同一第三方库的封装，调用路径必然高度一致。这是公共依赖导致的趋同，非原创代码抄袭。 |
| **第三方库** | 182 | 107 | 80 | 47.06% | 74.77% | **中** | 双方均存在该模块。共同指纹多来自 `executor`、`runtime`、`devices` 等自研或公共组件，其内部逻辑（如任务调度、帧分配）存在差异，但对外接口相似。 |
| **通用模块/其他** | 27 | 16 | 10 | 38.46% | 66.67% | **弱** | 双方均存在该模块。共同指纹为 `logging`、`useref` 等通用工具函数，其实现高度依赖 Rust 标准库惯用法，属于任何 Rust 项目的常见模式。 |

### 3. 底层 AST 关键证据指纹表
| 所属板块 | 目标项目路径 | 参考项目路径 | 证据类型 | 技术说明 |
| :--- | :--- | :--- | :--- | :--- |
| **中断/异常处理** | `kernel/driver/general-plic/src/plic.rs#set_irq_enable` | `driver/general-plic/src/plic.rs#set_irq_enable` | 硬件强制收敛 | 通过 `read_volatile`/`write_volatile` 操作 PLIC 寄存器。该逻辑由 RISC-V PLIC 硬件规范严格定义，任何驱动实现必然相同。 |
| **设备驱动** | `kernel/driver/kvirtio/src/lib.rs#init_mmio` | `driver/kvirtio/src/lib.rs#init_mmio` | 硬件标准范式 | VirtIO MMIO 传输层的初始化序列，包括 `MmioTransport::new` 和 `NonNull::new`。此流程由 VirtIO 标准规定，属于行业标准范式。 |
| **文件系统/第三方库** | `kernel/filesystem/ext4rsfs/src/lib.rs#lookup` | `filesystem/fs/src/ext4_rs_shim.rs#lookup` | 第三方库封装 | 对 `ext4_rs` 库 `generic_open` 的调用封装。双方均依赖同一第三方库，其调用路径必然一致，不构成原创性抄袭。 |
| **文件系统** | `kernel/filesystem/ramfs/src/lib.rs#read_dir` | `filesystem/ramfs/src/lib.rs#read_dir` | 通用接口实现 | 遍历 `BTreeMap` 并构造 `DirEntry` 列表。这是内存文件系统 `read_dir` 的标准实现模式，`len: 0` 等硬编码是 procfs 的常见处理。 |
| **进程管理** | `kernel/kernel/src/syscall/task.rs#sys_getppid` | `kernel/src/syscall/task.rs#sys_getppid` | 领域共性逻辑 | 通过 `parent.read().upgrade().map(...)` 查找父进程。该调用链是 `Weak<Process>` 查找的标准 Rust 模式，字段名差异 (`task_id` vs `process_id`) 表明独立建模。 |
| **任务调度/异步运行时** | `kernel/kernel/src/syscall/fd.rs#sys_epoll_wait` | `kernel/src/syscall/fd.rs#sys_epoll_wait` | POSIX 标准实现 | `epoll_wait` 系统调用的标准实现骨架。目标项目将核心逻辑封装为 `sys_epoll_wait_duration`，参考项目则内联处理，架构分层不同。 |
| **系统调用** | `kernel/kernel/src/syscall/sys.rs#sys_uname` | `kernel/src/syscall/sys.rs#sys_uname` | POSIX 标准实现 | 填充 `utsname` 结构体。调用链 `sys_name.len` 等完全一致，因为这是 `uname` 系统调用的唯一定义，无创造性空间。 |
| **第三方库** | `kernel/crates/devices/src/device.rs#add_device` | `crates/devices/src/device.rs#add_device` | 公共组件模式 | 向设备管理器注册设备，包含 `INT_DEVICE.init_by` 等调用。这是设备驱动框架的常见抽象，但内部初始化逻辑可能不同。 |

### 4. 核心代码调用流对比剖析
* **【进程管理/进程退出路径】**
* **底层技术析理**: 双方均实现了 `sys_exit_group` 系统调用，其调用流均为 `Ok -> debug! -> self.task.exit`。此路径是“终止线程组”这一 POSIX 定义语义的最简 Rust 表达，无创造性设计空间。目标项目在 `exit` 的具体实现上，通过 `final_exit_code` 管理退出状态，而参考项目则通过 `pcb.lock` 访问进程控制块，内部状态管理模型不同。
* **代码溯源链接**:
  - 目标项目: `kernel/kernel/src/syscall/task.rs#sys_exit_group`
  - 参考项目: `kernel/src/syscall/task.rs#sys_exit_group`

* **【进程管理/初始化与测试框架】**
* **底层技术析理**: 目标项目的 `initproc` 函数引入了 `arm_global_deadline_after`、`run_test_script` 等独特的调度与测试抽象，调用图谱复杂且特异。参考项目的 `initproc` 则侧重于 `command` 和 `command_iperf` 等具体应用启动，并包含 `detect_libc_type` 等特性。两者在初始化流程的设计哲学和功能集合上完全不同，体现了独立开发的核心差异。
* **代码溯源链接**:
  - 目标项目: `kernel/kernel/src/tasks/initproc.rs#initproc`
  - 参考项目: `kernel/src/tasks/initproc.rs#initproc`

* **【文件系统/伪文件系统接口】**
* **底层技术析理**: 在 `procfs` 的 `read_dir` 实现中，目标项目使用 `self.inner.map.keys().map(...)`，参考项目使用 `self.inner.map.iter().map(|(name, _)| ...)`。虽然功能相同，但迭代器选择 (`keys` vs `iter`) 和闭包参数模式 (单参数 vs 元组解构) 在 AST 层面是不同的调用节点，反映了不同的编码习惯或 Rust 版本偏好，不支持“复制后微调”的指控。
* **代码溯源链接**:
  - 目标项目: `kernel/filesystem/procfs/src/lib.rs#read_dir`
  - 参考项目: `filesystem/procfs/src/lib.rs#read_dir`

### 5. 单方模块与降权项
* **单方独有模块/功能**:
  - **目标项目独有**: `run_test_script`, `command_in_dir`, `group_budget`, `script_markers`, `scan_test_scripts_in` 等，构成了一个完整的自动化测试框架，是目标项目的显著原创特征。
  - **参考项目独有**: `command_iperf`, `detect_libc_type`, `CpuInfo`, `ProcStat`, `Version` 等，展示了其在网络性能测试和 procfs 信息展示上的不同侧重点。
  - **结论**: 这些单方独有的、成体系的功能模块，是双方独立开发、拥有不同设计目标和技术演进路径的有力证明。

* **降权项说明**:
  - **跨板块同名函数**: 控方将 `procfs` 下的 `readat`、`lookup`、`new`、`name` 等文件系统接口函数，作为【进程管理】板块的抄袭证据。这属于典型的跨板块硬凑，本庭不予采信。这些函数名是文件系统 `Inode` 或 `VfsNode` trait 的标准方法名，任何实现该接口的项目都会拥有同名函数。
  - **短调用链与标准库惯用法**: `sys_getpid` (调用链: `Ok, yield_now`)、`root_dir` (调用链: `Arc::new, self.root.clone`) 等函数，其调用链极短且完全由 Rust 标准库的惯用法构成，不具备项目特异性，不能作为抄袭证据。
  - **第三方库封装**: `ext4fs` 和 `ext4rsfs` 目录下的证据，是对同一开源库 `ext4_rs` 的封装。由此产生的 AST 趋同是公共依赖的必然结果，已从原创性抄袭证据中降权处理。

### 6. 保守结论
综合全案证据，目标项目与参考项目的全局 AST 相似度仅为 28.18%，处于独立开发项目的正常基线噪声范围内。控方对【进程管理】板块“重度抄袭”的指控，主要建立在短调用链的 POSIX 标准实现、跨板块硬凑的文件系统接口以及 Rust 语言通用惯用法之上，证据强度严重不足。辩方成功论证了双方在核心调度抽象、初始化流程、测试框架等方面存在根本性的架构差异，并提供了大量目标项目独有的原创功能作为反证。因此，本庭裁定，无充分证据表明目标项目 `[2026-T2026104229910571-XX]` 对参考项目 `[2025-Moncake-天津大学]` 构成系统性或实质性抄袭，维持“独立开发”的定性结论。

------------------------------

