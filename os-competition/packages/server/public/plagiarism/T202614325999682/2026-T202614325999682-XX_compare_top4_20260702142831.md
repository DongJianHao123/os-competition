# 🛡️ OS 内核赛自动化代码级架构审计报告

**审计时间**: `2026-07-02 14:28:31`
**参赛作品名称**: `2026-T202614325999682-XX`
**年份**: `2026`
**学校**: `XX`
**队伍名称**: `T202614325999682`
**比赛名字**: `OS-Competition`
**比赛子赛道**: `Kernel-Implementation`
**仓库地址**: `https://gitlab.eduxiji.net/educg-group-43501-3132633/T202614325999682-3734`
**推测开发语言**: `Rust / C (由AST探针自动识别)`
**特征函数量**: `203`

## ⚔️ 底层代码同源性对比 (AST Homology)
> 本部分由专门的代码审查智能体，对该项目与符合年份条件的历届最高相似度项目进行控制流与底层调用对比。新版报告会先给出确定性的全模块覆盖矩阵，再给出 Agent 的分模块解释，避免只围绕少量抽样函数展开。

### 🏆 Top 4 底层对标: vs `2021-TOO_LOW-北京航空航天大学`（北京航空航天大学 / ^^^TOO_LOW^^^）

#### 对方作品信息

**对方年份**: `2021`
**对方学校**: `北京航空航天大学`
**对方队伍名称**: `^^^TOO_LOW^^^`
**对方比赛名字**: `OS-Competition`
**对方比赛子赛道**: `Kernel-Implementation`
**对方仓库地址**: `https://gitlab.eduxiji.net/buaawyk2/oskernel2021-too_low`

> 双向覆盖率：共同 AST 指纹: 2；唯一 AST 指纹: 目标项目 158，参考项目 295；覆盖率: 目标项目 1.27%，参考项目 0.68%

## 全模块对比总览（确定性证据）

> 总体覆盖率：共同 AST 指纹: 2；唯一 AST 指纹: 目标项目 158，参考项目 295；覆盖率: 目标项目 1.27%，参考项目 0.68%

### 1. 模块覆盖矩阵
| 模块 | 目标函数数 | 参考函数数 | 共同唯一 AST 指纹 | Target 覆盖 | Base 覆盖 | 状态 |
| :--- | ---: | ---: | ---: | ---: | ---: | :--- |
| 进程管理 | 0 | 5 | 0 | 0.0% | 0.0% | 仅参考项目体现 |
| 任务调度/异步运行时 | 0 | 30 | 0 | 0.0% | 0.0% | 仅参考项目体现 |
| 内存管理 | 9 | 80 | 1 | 33.33% | 1.28% | 双方存在且有结构重合 |
| 中断/异常处理 | 0 | 9 | 0 | 0.0% | 0.0% | 仅参考项目体现 |
| 系统调用 | 20 | 94 | 1 | 33.33% | 2.94% | 双方存在且有结构重合 |
| 文件系统 | 0 | 77 | 0 | 0.0% | 0.0% | 仅参考项目体现 |
| 设备驱动 | 0 | 49 | 0 | 0.0% | 0.0% | 仅参考项目体现 |
| 网络协议栈 | 0 | 0 | 0 | 0.0% | 0.0% | 双方均未体现 |
| 第三方库 | 134 | 0 | 0 | 0.0% | 0.0% | 仅目标项目体现 |
| 通用模块/其他 | 40 | 75 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |

### 2. 分模块证据清单

#### 进程管理
仅参考项目中观察到该模块代表函数，目标项目没有同模块可对齐证据：
- 参考项目：`user/fork.c#user_bcopy`，调用摘要：内部闭环逻辑
- 参考项目：`user/syscall_lib.c#syscall_rt_sigprocmask`，调用摘要：internal_syscall
- 参考项目：`lib/syscall_all.c#sys_rt_sigprocmask`，调用摘要：内部闭环逻辑
- 参考项目：`user/fork.c#user_bzero`，调用摘要：内部闭环逻辑

#### 任务调度/异步运行时
仅参考项目中观察到该模块代表函数，目标项目没有同模块可对齐证据：
- 参考项目：`lib/print.c#lp_Print`，调用摘要：Ctod, IsDigit, OUTPUT, PrintChar, PrintNum, PrintString ...(+1)
- 参考项目：`user/print.c#user_lp_Print`，调用摘要：Ctod, IsDigit, OUTPUT, user_PrintChar, user_PrintNum, user_PrintString ...(+1)
- 参考项目：`lib/fat32.c#emake`，调用摘要：(u16), cal_checksum, generate_shortname, memset, panic, reloc_clus ...(+3)
- 参考项目：`drivers/spi.c#spi_receive_data_standard`，调用摘要：(u32), configASSERT, spi_get_frame_size, spi_set_tmod

#### 内存管理
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `libs/basecpp/string.c#memmove` | `lib/string.c#memmove` | 同板块同名 | 内部闭环逻辑 | 内部闭环逻辑 |
| `libs/sbi/sbi_remote_fence.c#sbi_remote_hfence_gvma_vmid` | `user/syscall_lib.c#syscall_mem_alloc` | 同板块同结构 | sbi_ecall | internal_syscall |
| `libs/sbi/sbi_remote_fence.c#sbi_remote_hfence_vvma_asid` | `bootloader/SBI/rustsbi-k210/src/main.rs#main` | 同模块上下文对齐/差异观察 | sbi_ecall | 115_200.bps, ALLOCATOR.lock, ALLOCATOR.lock().init, Clocks::new, boot.clear_interrupt_pending_bits, boot.trigger_on_edge ...(+46) |
| `libs/sbi/sbi_remote_fence.c#sbi_remote_sfence_vma_asid` | `bootloader/SBI/rustsbi-qemu/src/main.rs#main` | 同模块上下文对齐/差异观察 | sbi_ecall | ALLOCATOR.lock, ALLOCATOR.lock().init, enter_privileged, hal::Clint::new, hal::Ns16550a::new, init_ipi ...(+28) |

#### 中断/异常处理
仅参考项目中观察到该模块代表函数，目标项目没有同模块可对齐证据：
- 参考项目：`bootloader/SBI/rustsbi-qemu/src/main.rs#start_trap_rust`，调用摘要：clint.get_mtime, get_vaddr_u32, hal::Clint::new, llvm_asm!, mcause::read, mcause::read().cause ...(+9)
- 参考项目：`bootloader/SBI/rustsbi-k210/src/main.rs#set_rd`，调用摘要：panic!
- 参考项目：`lib/traps.c#default_handler`，调用摘要：printf
- 参考项目：`bootloader/SBI/rustsbi-k210/src/main.rs#devintr`，调用摘要：llvm_asm!

#### 系统调用
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `libs/sbi/sbi_remote_fence.c#sbi_remote_fence_i` | `user/syscall_lib.c#syscall_linkat` | 同板块同结构 | sbi_ecall | internal_syscall |
| `libs/sbi/sbi_send_ipi.c#sbi_send_ipi` | `user/syscall_lib.c#syscall_mem_map` | 同板块同结构 | sbi_ecall | internal_syscall |
| `libs/sbi/sbi_base.c#sbi_get_impl_version` | `user/syscall_lib.c#syscall_clone` | 同板块同结构 | sbi_ecall | internal_syscall |
| `libs/sbi/sbi_base.c#sbi_get_spec_version` | `user/syscall_lib.c#syscall_ipc_can_send` | 同板块同结构 | sbi_ecall | internal_syscall |

#### 文件系统
仅参考项目中观察到该模块代表函数，目标项目没有同模块可对齐证据：
- 参考项目：`lib/fat32.c#fat32_init`，调用摘要：bread, brelse, memmove, memset, panic, printf ...(+1)
- 参考项目：`lib/syscall_file.c#sys_rename`，调用摘要：dirlookup, edup, emake, ename, enameparent, eput ...(+4)
- 参考项目：`lib/fat32.c#enext`，调用摘要：memset, panic, read_entry_info, read_entry_name, reloc_clus, rw_clus
- 参考项目：`lib/fat32.c#dirlookup`，调用摘要：edup, eget, enext, eput, panic, reloc_clus ...(+2)

#### 设备驱动
仅参考项目中观察到该模块代表函数，目标项目没有同模块可对齐证据：
- 参考项目：`drivers/gpiohs.c#gpiohs_set_pin_edge`，调用摘要：set_gpio_bit
- 参考项目：`drivers/sdcard.c#sd_write_sector`，调用摘要：sd_end_cmd, sd_get_dataresponse, sd_get_response, sd_send_cmd, sd_write_data
- 参考项目：`drivers/fpioa.c#fpioa_set_function_raw`，调用摘要：内部闭环逻辑
- 参考项目：`drivers/sdcard.c#check_block_size`，调用摘要：printf, sd_end_cmd, sd_get_response_R1, sd_get_response_R3_rest, sd_send_cmd

#### 第三方库
仅目标项目中观察到该模块代表函数，参考项目没有同模块可对齐证据：
- 目标项目：`third_party/libs/libfdt/fdt_overlay.c#overlay_symbol_update`，调用摘要：fdt_add_subnode, fdt_for_each_property_offset, fdt_get_path, fdt_getprop_by_offset, fdt_overlay_target_offset, fdt_setprop_placeholder ...(+8)
- 目标项目：`third_party/libs/libfdt/fdt_rw.c#fdt_open_into`，调用摘要：FDT_ALIGN, FDT_RO_PROBE, can_assume, fdt_blocks_misordered_, fdt_boot_cpuid_phys, fdt_move ...(+14)
- 目标项目：`third_party/libs/libfdt/fdt_overlay.c#overlay_fixup_phandle`，调用摘要：fdt_get_phandle, fdt_getprop, fdt_getprop_by_offset, fdt_path_offset, memchr, overlay_fixup_one_phandle ...(+1)
- 目标项目：`third_party/libs/libfdt/fdt_check.c#fdt_check_full`，调用摘要：fdt_check_header, fdt_get_name, fdt_getprop_by_offset, fdt_header_size, fdt_next_tag, fdt_num_mem_rsv ...(+1)

#### 通用模块/其他
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `libs/basecpp/string.c#memcmp` | `lib/string.c#memcmp` | 同板块同名 | 内部闭环逻辑 | 内部闭环逻辑 |
| `libs/basecpp/string.c#strncmp` | `lib/string.c#strncmp` | 同板块同名 | 内部闭环逻辑 | 内部闭环逻辑 |
| `libs/basecpp/string.c#strncpy` | `lib/string.c#strncpy` | 同板块同名 | 内部闭环逻辑 | 内部闭环逻辑 |
| `libs/basecpp/string.c#memcpy` | `lib/string.c#memcpy` | 同板块同名 | 内部闭环逻辑 | 内部闭环逻辑 |


好的，审计官。基于提供的证据底座、控辩双方陈词及原始AST抽样数据，我将进行最终的独立同源性定性仲裁。

### 1. 宏观判定
- **定性结论**: **独立开发 (底层架构显著差异)**
- **核心依据**: 客观AST相似度仅为0.44%，共同指纹仅2个，双向覆盖率均低于1.3%。该数值处于统计噪声水平，远不足以支持“代码复用”或“同源”的指控。双方在核心子系统（进程管理、文件系统、设备驱动等）上零重合，且重合点均可被标准库规范或行业通用范式完美解释。

### 2. 全模块覆盖矩阵解读
- **进程管理**: 仅参考项目存在，目标项目缺失。无共同指纹，覆盖率为0。**未形成证据**。
- **任务调度/异步运行时**: 仅参考项目存在，目标项目缺失。无共同指纹，覆盖率为0。**未形成证据**。
- **内存管理**: 双方均存在。共同AST指纹1个，目标覆盖率33.33%，参考覆盖率1.28%。高目标覆盖率源于其模块函数基数极小(9个)，单一重合即被放大。该重合点为标准C库函数`memmove`，不构成项目间复用证据。**证据强度: 弱**。
- **中断/异常处理**: 仅参考项目存在，目标项目缺失。无共同指纹，覆盖率为0。**未形成证据**。
- **系统调用**: 双方均存在。共同AST指纹1个，目标覆盖率33.33%，参考覆盖率2.94%。与内存管理模块同理，高目标覆盖率由小基数(20个)导致。重合点`internal_syscall`与`sbi_ecall`是不同层级、不同规范下的调用封装模式，属于架构范式，非代码同源。**证据强度: 弱**。
- **文件系统**: 仅参考项目存在，目标项目缺失。无共同指纹，覆盖率为0。**未形成证据**。
- **设备驱动**: 仅参考项目存在，目标项目缺失。无共同指纹，覆盖率为0。**未形成证据**。
- **网络协议栈**: 双方均未体现。**未形成证据**。
- **第三方库/公共依赖**: 仅目标项目存在（`libfdt`等），参考项目缺失。无共同指纹，覆盖率为0。此差异凸显双方项目定位与代码来源完全不同。**未形成证据**。

### 3. 底层 AST 关键证据指纹表
| 所属板块 | 目标项目路径 | 参考项目路径 | 证据类型 | 技术说明 |
| :--- | :--- | :--- | :--- | :--- |
| 内存管理 | `libs/basecpp/string.c#memmove` | `lib/string.c#memmove` | 同板块同名 | ISO C标准库强制函数。双方均为无外部依赖的叶子函数实现，是标准语义的必然结果，非抄袭痕迹。 |
| 系统调用 | `libs/sbi/sbi_remote_fence.c#sbi_remote_fence_i` | `user/syscall_lib.c#syscall_linkat` | 同板块同结构 | 双方均采用“参数打包-触发调用”的封装模式。但目标调用`sbi_ecall`（RISC-V SBI），参考调用`internal_syscall`（OS内核），底层机制与上下文完全不同，属架构范式雷同。 |
| 通用模块/其他 | `libs/basecpp/string.c#memcmp` | `lib/string.c#memcmp` | 同板块同名 | ISO C标准库强制函数。解释同上。 |
| 通用模块/其他 | `libs/basecpp/string.c#strncmp` | `lib/string.c#strncmp` | 同板块同名 | ISO C标准库强制函数。解释同上。 |
| 通用模块/其他 | `libs/basecpp/string.c#strncpy` | `lib/string.c#strncpy` | 同板块同名 | ISO C标准库强制函数。解释同上。 |
| 通用模块/其他 | `libs/basecpp/string.c#memcpy` | `lib/string.c#memcpy` | 同板块同名 | ISO C标准库强制函数。解释同上。 |
| 通用模块/其他 | `libs/basecpp/string.c#strchr` | `lib/string.c#strchr` | 同板块同名 | ISO C标准库强制函数。解释同上。 |
| 通用模块/其他 | `libs/basecpp/string.c#memset` | `lib/string.c#memset` | 同板块同名 | ISO C标准库强制函数。解释同上。 |
| 通用模块/其他 | `libs/basecpp/string.c#strcpy` | `lib/string.c#strcpy` | 同板块同名 | 目标项目调用`memcpy, strlen`，参考项目为内部闭环。调用图谱的差异反而证明两者采用了不同的独立实现路径。 |

### 4. 核心代码调用流对比剖析
* **【内存管理/底层内存操作】**
* **底层技术析理**: 双方在`memmove`等函数上均呈现“内部闭环逻辑”，这是因为此类函数直接操作原始内存指针，不依赖任何外部模块（如分配器、页表），是典型的叶子函数。其实现遵循ISO C标准规定的公开算法，任何独立编写且符合规范的实现都会产生相似的AST结构。这并非同源性证据，而是标准收敛的结果。
* **代码溯源链接**:
  - 目标项目: `libs/basecpp/string.c#memmove`
  - 参考项目: `lib/string.c#memmove`

* **【系统调用/调用封装模式】**
* **底层技术析理**: 目标项目的系统调用模块完全围绕RISC-V SBI规范展开，其核心是`sbi_ecall`函数，通过构造`struct sbiret`并执行`ecall`指令陷入M-mode固件。参考项目的系统调用模块则围绕类Unix教学内核展开，其核心是`internal_syscall`，通过软件中断或`ecall`指令从U-mode陷入S-mode内核，并依赖`envid2env`等进程管理结构。两者在**抽象层次**（固件接口 vs 内核接口）、**内存模型**（物理地址/Hart vs 虚拟地址/进程）和**同步策略**（IPI/TLB维护 vs 进程调度/信号）上存在根本性异构。
* **代码溯源链接**:
  - 目标项目: `libs/sbi/sbi.c#sbi_ecall`
  - 参考项目: `user/syscall_lib.c#syscall_mmap`

### 5. 单方模块与降权项
- **单方模块**: 目标项目独有的**第三方库**（`libfdt`等）和参考项目独有的**进程管理、任务调度、中断/异常处理、文件系统、设备驱动**等核心OS模块，均未在对方项目中找到任何可对齐的AST证据。这强有力地证明两个项目在功能定位、设计目标和代码来源上完全不同。
- **降权项**: 控方提出的【内存管理】板块中目标项目`sbi_remote_*`函数与参考项目`rustsbi-*/main.rs`的关联，属于**跨语言（C vs Rust）、跨层级（SBI调用方 vs SBI实现方）的硬凑证据**。函数名相似源于对同一RISC-V SBI规范的遵循，而非代码复用。此关联已被降权，不构成有效同源指控。

### 6. 保守结论
本次审计的客观AST相似度极低（0.44%），共同指纹仅2个，处于统计噪声水平。所有重合点均可被ISO C标准库规范或RISC-V SBI架构范式完美解释，不构成代码复用证据。双方在进程管理、文件系统、设备驱动等核心子系统上零重合，且目标项目存在大量参考项目缺失的第三方库，进一步印证了其独立开发的本质。控方指控多基于跨板块、跨语言或跨层级的证据硬凑，缺乏底层AST结构的一致性支撑。综合判定，目标项目`[2026-T202614325999682-XX]`相对于参考项目`[2021-TOO_LOW-北京航空航天大学]`为独立开发，不存在代码抄袭或同源违规行为，整体风险极低。

------------------------------

