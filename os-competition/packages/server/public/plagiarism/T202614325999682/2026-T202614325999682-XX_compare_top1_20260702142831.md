# 🛡️ OS 内核赛自动化代码级架构审计报告

**审计时间**: `2026-07-02 14:28:31`
**参赛作品名称**: `2026-T202614325999682-XX`
**年份**: `2026`
**学校**: `XX`
**队伍名称**: `T202614325999682`
**比赛名字**: `OS-Competition`
**比赛子赛道**: `Kernel-Implementation`
**仓库地址**: `https://gitlab.eduxiji.net/educg-group-43501-3132633/T202614325999682-3734`
**推测开发语言**: `Rust / C (由AST探针自动识别)`
**特征函数量**: `203`

## ⚔️ 底层代码同源性对比 (AST Homology)
> 本部分由专门的代码审查智能体，对该项目与符合年份条件的历届最高相似度项目进行控制流与底层调用对比。新版报告会先给出确定性的全模块覆盖矩阵，再给出 Agent 的分模块解释，避免只围绕少量抽样函数展开。

### 🏆 Top 1 底层对标: vs `2024-重启之我是loader-西安邮电大学`（西安邮电大学 / 重启之我是loader）

#### 对方作品信息

**对方年份**: `2024`
**对方学校**: `西安邮电大学`
**对方队伍名称**: `重启之我是loader`
**对方比赛名字**: `OS-Competition`
**对方比赛子赛道**: `Kernel-Implementation`
**对方仓库地址**: `https://gitlab.eduxiji.net/educg-group-26011-2376549/T202411664992499-2328`

> 双向覆盖率：共同 AST 指纹: 27；唯一 AST 指纹: 目标项目 158，参考项目 1537；覆盖率: 目标项目 17.09%，参考项目 1.76%

## 全模块对比总览（确定性证据）

> 总体覆盖率：共同 AST 指纹: 27；唯一 AST 指纹: 目标项目 158，参考项目 1537；覆盖率: 目标项目 17.09%，参考项目 1.76%

### 1. 模块覆盖矩阵
| 模块 | 目标函数数 | 参考函数数 | 共同唯一 AST 指纹 | Target 覆盖 | Base 覆盖 | 状态 |
| :--- | ---: | ---: | ---: | ---: | ---: | :--- |
| 进程管理 | 0 | 49 | 0 | 0.0% | 0.0% | 仅参考项目体现 |
| 任务调度/异步运行时 | 0 | 163 | 0 | 0.0% | 0.0% | 仅参考项目体现 |
| 内存管理 | 9 | 297 | 1 | 33.33% | 0.35% | 双方存在且有结构重合 |
| 中断/异常处理 | 0 | 50 | 0 | 0.0% | 0.0% | 仅参考项目体现 |
| 系统调用 | 20 | 13 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |
| 文件系统 | 0 | 357 | 0 | 0.0% | 0.0% | 仅参考项目体现 |
| 设备驱动 | 0 | 87 | 0 | 0.0% | 0.0% | 仅参考项目体现 |
| 网络协议栈 | 0 | 0 | 0 | 0.0% | 0.0% | 双方均未体现 |
| 文件系统/第三方库 | 0 | 192 | 0 | 0.0% | 0.0% | 仅参考项目体现 |
| 第三方库 | 134 | 0 | 0 | 0.0% | 0.0% | 仅目标项目体现 |
| 通用模块/其他 | 40 | 552 | 1 | 3.12% | 0.22% | 双方存在且有结构重合 |

### 2. 分模块证据清单

#### 进程管理
仅参考项目中观察到该模块代表函数，目标项目没有同模块可对齐证据：
- 参考项目：`scripts/docproc.c#main`，调用摘要：exit, fclose, fflush, fopen, fprintf, fseek ...(+7)
- 参考项目：`scripts/docproc.c#find_export_symbols`，调用摘要：add_new_file, add_new_symbol, exit, fclose, fgets, filename_exist ...(+9)
- 参考项目：`scripts/docproc.c#parse_file`，调用摘要：defaultline, docsection, externalfunctions, fflush, fgets, findall ...(+4)
- 参考项目：`drivers/firmware/efi/libstub/efi-stub-helper.c#efi_exit_boot_services`，调用摘要：efi_bs_call, efi_get_memory_map, priv_func

#### 任务调度/异步运行时
仅参考项目中观察到该模块代表函数，目标项目没有同模块可对齐证据：
- 参考项目：`scripts/kconfig/lxdialog/inputbox.c#dialog_inputbox`，调用摘要：delwin, draw_box, draw_shadow, flash, getmaxx, getmaxy ...(+18)
- 参考项目：`scripts/kconfig/conf.c#main`，调用摘要：_, bindtextdomain, check_conf, conf, conf_get_changed, conf_get_configname ...(+23)
- 参考项目：`scripts/kconfig/lxdialog/checklist.c#dialog_checklist`，调用摘要：MAX, MIN, delwin, doupdate, draw_box, draw_shadow ...(+30)
- 参考项目：`drivers/firmware/efi/libstub/vsprintf.c#vsnprintf`，调用摘要：ARRAY_SIZE, PUTC, get_flags, get_int, get_number, get_sign ...(+8)

#### 内存管理
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `libs/sbi/sbi_remote_fence.c#sbi_remote_hfence_gvma_vmid` | `thread/thread.c#fork_pid` | 同板块同结构 | sbi_ecall | allocate_pid |
| `libs/basecpp/string.c#memmove` | `scripts/asn1_compiler.c#parse_type` | 同模块上下文对齐/差异观察 | 内部闭环逻辑 | abort, alloc_elem, bsearch, exit, fprintf, free ...(+7) |
| `libs/sbi/sbi_remote_fence.c#sbi_remote_hfence_vvma_asid` | `scripts/kconfig/menu.c#menu_finalize` | 同模块上下文对齐/差异观察 | sbi_ecall | expr_alloc_and, expr_alloc_comp, expr_alloc_one, expr_alloc_or, expr_alloc_symbol, expr_contains_symbol ...(+20) |
| `libs/sbi/sbi_remote_fence.c#sbi_remote_sfence_vma_asid` | `scripts/kconfig/lxdialog/textbox.c#dialog_textbox` | 同模块上下文对齐/差异观察 | sbi_ecall | attr_clear, back_lines, delwin, draw_box, draw_shadow, get_line ...(+21) |

#### 中断/异常处理
仅参考项目中观察到该模块代表函数，目标项目没有同模块可对齐证据：
- 参考项目：`drivers/pci/pci.c#pci_device_dump`，调用摘要：pci_device_bar_dump, printk
- 参考项目：`scripts/kconfig/gconf.c#on_introduction1_activate`，调用摘要：GTK_OBJECT, GTK_WINDOW, G_CALLBACK, _, g_signal_connect_swapped, gtk_message_dialog_new ...(+1)
- 参考项目：`kernel/softirq.c#do_softirq`，调用摘要：bh_disable, bh_enable, h->action, in_interrupt, intr_disable, intr_enable
- 参考项目：`drivers/pci/pci.c#pci_device_init`，调用摘要：内部闭环逻辑

#### 系统调用
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `libs/sbi/sbi.c#sbi_ecall` | `arch/loongarch/kernel/syscall.c#do_syscall` | 同模块上下文对齐/差异观察 | 内部闭环逻辑 | sys_fn |
| `libs/sbi/sbi_remote_fence.c#sbi_remote_fence_i` | `thread/thread.c#snap` | 同模块上下文对齐/差异观察 | sbi_ecall | sys_gettimeofday |
| `libs/sbi/sbi_send_ipi.c#sbi_send_ipi` | `userprog/exec.c#sys_exeload` | 同模块上下文对齐/差异观察 | sbi_ecall | load, printk |
| `libs/sbi/sbi_timer.c#sbi_set_timer` | `drivers/timer.c#sys_gettimeofday` | 同模块上下文对齐/差异观察 | (umb_t), sbi_ecall | CLK_TO_SEC, CLK_TO_USEC, USEC_TO_NSEC, rdtime |

#### 文件系统
仅参考项目中观察到该模块代表函数，目标项目没有同模块可对齐证据：
- 参考项目：`scripts/mod/modpost.c#parse_elf`，调用摘要：TO_NATIVE, exit, fatal, fprintf, grab_file, perror ...(+2)
- 参考项目：`scripts/mod/devicetable-offsets.c#main`，调用摘要：DEVID, DEVID_FIELD
- 参考项目：`scripts/asn1_compiler.c#render`，调用摘要：debug, exit, ferror, fprintf, perror, render_element ...(+2)
- 参考项目：`scripts/unifdef.c#parseline`，调用摘要：Eioccc, debug, fgets, findsym, ifeval, skipcomment ...(+6)

#### 设备驱动
仅参考项目中观察到该模块代表函数，目标项目没有同模块可对齐证据：
- 参考项目：`drivers/disk/ahci.c#port_rebase`，调用摘要：memset, printk, start_cmd, stop_cmd
- 参考项目：`mm/memblock.c#__next_mem_range`，调用摘要：max, min
- 参考项目：`drivers/firmware/efi/libstub/printk.c#utf8_to_utf32`，调用摘要：内部闭环逻辑
- 参考项目：`drivers/disk/ahci.c#ahci_initialize_command_table`，调用摘要：memset

#### 文件系统/第三方库
仅参考项目中观察到该模块代表函数，目标项目没有同模块可对齐证据：
- 参考项目：`fs/ext4/ext4_block.c#ext4_balloc_alloc_block`，调用摘要：EXT4_LBA2PBA, bufRead, bufRelease, bufWrite, ext4_balloc_get_bgid_of_block, ext4_balloc_get_block_of_bgid ...(+20)
- 参考项目：`fs/ext4/ext4_extent.c#ext4_extent_get_blocks`，调用摘要：IN_RANGE, ext4_ext_convert_to_initialized, ext4_ext_drop_refs, ext4_ext_find_goal, ext4_ext_free_blocks, ext4_ext_get_actual_len ...(+12)
- 参考项目：`fs/ext4/ext4_extent.c#ext4_ext_split_node`，调用摘要：ASSERT, EXT4_BLOCK_ZERO, EXT4_LBA2PBA, EXT_FIRST_EXTENT, EXT_FIRST_INDEX, EXT_MAX_EXTENT ...(+18)
- 参考项目：`fs/ext4/ext4_fs.c#ext4_fs_set_inode_data_block_index`，调用摘要：EXT4_LBA2PBA, bufRead, bufRelease, bufWrite, ext4_balloc_alloc_block, ext4_balloc_free_blocks ...(+10)

#### 第三方库
仅目标项目中观察到该模块代表函数，参考项目没有同模块可对齐证据：
- 目标项目：`third_party/libs/libfdt/fdt_overlay.c#overlay_symbol_update`，调用摘要：fdt_add_subnode, fdt_for_each_property_offset, fdt_get_path, fdt_getprop_by_offset, fdt_overlay_target_offset, fdt_setprop_placeholder ...(+8)
- 目标项目：`third_party/libs/libfdt/fdt_rw.c#fdt_open_into`，调用摘要：FDT_ALIGN, FDT_RO_PROBE, can_assume, fdt_blocks_misordered_, fdt_boot_cpuid_phys, fdt_move ...(+14)
- 目标项目：`third_party/libs/libfdt/fdt_overlay.c#overlay_fixup_phandle`，调用摘要：fdt_get_phandle, fdt_getprop, fdt_getprop_by_offset, fdt_path_offset, memchr, overlay_fixup_one_phandle ...(+1)
- 目标项目：`third_party/libs/libfdt/fdt_check.c#fdt_check_full`，调用摘要：fdt_check_header, fdt_get_name, fdt_getprop_by_offset, fdt_header_size, fdt_next_tag, fdt_num_mem_rsv ...(+1)

#### 通用模块/其他
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `libs/basecpp/string.c#strcmp` | `lib/string.c#strcmp` | 同板块同名 | 内部闭环逻辑 | ASSERT |
| `libs/basecpp/string.c#memcmp` | `lib/string.c#memcmp` | 同板块同名 | 内部闭环逻辑 | efi_puts |
| `libs/basecpp/string.c#strncmp` | `lib/string.c#strncmp` | 同板块同名 | 内部闭环逻辑 | 内部闭环逻辑 |
| `libs/basecpp/string.c#strcat` | `lib/string.c#strcat` | 同板块同名 | strlen | ASSERT |


好的，我将以审计专家的身份，基于提供的证据和辩论，对本案进行最终的定性仲裁。

### 1. 宏观判定
- **定性结论**: **独立开发**
  底层架构显著差异 (相似度: 1.62%，共同 AST 指纹: 27；唯一 AST 指纹: 目标项目 158，参考项目 1537；覆盖率: 目标项目 17.09%，参考项目 1.76%)。目标项目与参考项目在核心内核子系统上无共同指纹，其微弱的相似度完全由C标准库函数的必然同名和RISC-V架构规范强制接口导致，不构成抄袭或同源复用。

### 2. 全模块覆盖矩阵解读
- **进程管理**: 仅参考项目体现。无共同AST指纹，覆盖率均为0.0%。证据强度：**未形成**。
- **任务调度/异步运行时**: 仅参考项目体现。无共同AST指纹，覆盖率均为0.0%。证据强度：**未形成**。
- **内存管理**: 双方存在且有结构重合。共同AST指纹1个，目标覆盖率33.33%，参考覆盖率0.35%。目标项目的高覆盖率由其函数总数极少(9个)导致，1个共同指纹即占1/3。该指纹为`sbi_remote_hfence_gvma_vmid`与`fork_pid`的机械对齐，两者分属RISC-V IOMMU刷新与通用PID分配，无实际逻辑关联。证据强度：**弱**。
- **中断/异常处理**: 仅参考项目体现。无共同AST指纹，覆盖率均为0.0%。证据强度：**未形成**。
- **系统调用**: 双方存在但未观察到共同结构指纹。目标覆盖率0.0%，参考覆盖率0.0%。目标项目为RISC-V SBI规范实现，参考项目为LoongArch系统调用，两者在指令集、ABI、调用号上完全不同。证据强度：**弱**。
- **文件系统**: 仅参考项目体现。无共同AST指纹，覆盖率均为0.0%。证据强度：**未形成**。
- **设备驱动**: 仅参考项目体现。无共同AST指纹，覆盖率均为0.0%。证据强度：**未形成**。
- **网络协议栈**: 双方均未体现。证据强度：**未形成**。
- **第三方库/公共依赖**: 目标项目存在`libfdt`等第三方库，参考项目无对应模块；参考项目存在`ext4`等文件系统实现，目标项目无对应模块。无共同AST指纹。证据强度：**未形成**。

### 3. 底层 AST 关键证据指纹表
| 所属板块 | 目标项目路径 | 参考项目路径 | 证据类型 | 技术说明 |
| :--- | :--- | :--- | :--- | :--- |
| 通用模块/其他 | `libs/basecpp/string.c#strcmp` | `lib/string.c#strcmp` | 同板块同名 | C标准库强制命名。目标项目为内部闭环，参考项目调用`ASSERT`，实现环境与错误处理策略完全不同。 |
| 通用模块/其他 | `libs/basecpp/string.c#memcmp` | `lib/string.c#memcmp` | 同板块同名 | C标准库强制命名。目标项目为内部闭环，参考项目调用`efi_puts`，反映EFI固件与通用基础库的领域差异。 |
| 通用模块/其他 | `libs/basecpp/string.c#strncmp` | `lib/string.c#strncmp` | 同板块同名 | C标准库强制命名。双方均为内部闭环逻辑，是标准函数的自然实现。 |
| 通用模块/其他 | `libs/basecpp/string.c#strrchr` | `lib/string.c#strrchr` | 同板块同名 | C标准库强制命名。目标项目为内部闭环，参考项目调用`ASSERT`，实现环境与错误处理策略完全不同。 |
| 通用模块/其他 | `libs/basecpp/string.c#strcat` | `lib/string.c#strcat` | 同板块同名 | C标准库强制命名。目标项目调用`strlen`，参考项目调用`ASSERT`，实现策略与环境均不同。 |
| 通用模块/其他 | `libs/basecpp/string.c#strchr` | `lib/string.c#strchr` | 同板块同名 | C标准库强制命名。目标项目为内部闭环，参考项目调用`ASSERT`，实现环境与错误处理策略完全不同。 |
| 通用模块/其他 | `libs/basecpp/string.c#strlen` | `lib/string.c#strlen` | 同板块同名 | C标准库强制命名。目标项目为内部闭环，参考项目调用`ASSERT`，实现环境与错误处理策略完全不同。 |
| 通用模块/其他 | `libs/basecpp/string.c#memcpy` | `lib/string.c#memcpy` | 同板块同名 | C标准库强制命名。双方均为内部闭环逻辑，是标准函数的自然实现。 |
| 通用模块/其他 | `libs/basecpp/string.c#memset` | `lib/string.c#memset` | 同板块同名 | C标准库强制命名。目标项目为内部闭环，参考项目调用`efi_puts`，反映EFI固件与通用基础库的领域差异。 |
| 通用模块/其他 | `libs/basecpp/string.c#strnlen` | `lib/string.c#strnlen` | 同板块同名 | C标准库强制命名。目标项目为内部闭环，参考项目调用`efi_puts, strlen`，是EFI环境下的特定实现。 |
| 通用模块/其他 | `libs/basecpp/string.c#strcpy` | `lib/string.c#strcpy` | 同板块同名 | C标准库强制命名。目标项目调用`memcpy, strlen`，参考项目为内部闭环，实现策略完全不同，支持独立开发。 |
| 内存管理 | `libs/sbi/sbi_remote_fence.c#sbi_remote_hfence_gvma_vmid` | `thread/thread.c#fork_pid` | 同板块证据 | 机械对齐产物。前者为RISC-V IOMMU VMID刷新，后者为通用PID分配，调用对象与逻辑无任何关联。 |
| 系统调用 | `libs/sbi/sbi.c#sbi_ecall` | `arch/loongarch/kernel/syscall.c#do_syscall` | 同板块证据 | 架构规范差异。前者封装RISC-V `ecall`指令，后者为LoongArch `syscall`处理入口，底层指令集与ABI完全不同。 |

### 4. 核心代码调用流对比剖析
* **【系统调用/异常处理入口】**
* **底层技术析理**: 目标项目与参考项目的系统调用入口在抽象层次、内存模型和同步策略上存在根本性异构。目标项目基于RISC-V SBI规范，其核心是`sbi_ecall`函数，通过封装`ecall`指令从S模式陷入M模式，传递`a7`(扩展ID)和`a6`(功能ID)寄存器参数，属于固件级接口。参考项目基于LoongArch架构，其核心是`do_syscall`函数，通过`syscall`指令从用户态陷入内核态，根据`a7`寄存器传递的系统调用号分发到`sys_fn`表，属于操作系统级接口。两者在指令集、特权级切换机制、参数传递约定上完全不同，不存在AST复用的技术基础。
* **代码溯源链接**:
  - 目标项目: `libs/sbi/sbi.c#sbi_ecall`
  - 参考项目: `arch/loongarch/kernel/syscall.c#do_syscall`

* **【通用模块/其他/字符串处理】**
* **底层技术析理**: 双方均实现了C标准库中的`strcpy`函数，但采用了完全不同的控制流结构。参考项目`lib/string.c#strcpy`采用内部闭环逻辑，即通过一个`while`循环逐字节拷贝源字符串到目标地址，直到遇到空字符`\0`。目标项目`libs/basecpp/string.c#strcpy`则采用了组合调用策略，先调用`strlen`获取源字符串长度，再调用`memcpy`进行批量内存拷贝。这种从循环结构到函数组合调用的根本性差异，是独立设计选择的体现，而非对同一AST的“缝合式重构”。
* **代码溯源链接**:
  - 目标项目: `libs/basecpp/string.c#strcpy`
  - 参考项目: `lib/string.c#strcpy`

### 5. 单方模块与降权项
- **单方模块**: 进程管理、任务调度/异步运行时、中断/异常处理、文件系统、设备驱动等核心模块仅存在于参考项目中，目标项目无任何对应实现。这构成了双方项目在功能范围和设计目标上的根本差异，是独立开发的有力证据。
- **第三方库/公共依赖**: 目标项目中的`third_party/libs/libfdt/*`（设备树操作库）和参考项目中的`scripts/kconfig/*`（内核配置工具）、`fs/ext4/*`（文件系统）等均属于单方持有的第三方库或独立子系统，无任何跨项目复用痕迹。
- **降权项说明**: 所有被列为“同板块同名”的`string.c`函数（如`strcmp`, `memcpy`等）均为ISO C标准库强制命名的函数。其函数名相同是标准规范所致，不构成同源证据。调用图谱中`ASSERT`、`efi_puts`等环境特定调用的有无，进一步证明了双方代码服务于不同的运行环境（EFI固件 vs 通用内核基础库），属于领域差异而非抄袭后的“剪枝”。

### 6. 保守结论
本次审计的客观AST相似度仅为1.62%，处于极低水平。目标项目17.09%的覆盖率主要由C标准库函数的必然同名贡献，而参考项目对目标项目的覆盖率更是低至1.76%。双方在进程管理、文件系统、设备驱动等核心内核子系统上无任何共同指纹，系统调用入口更是分属RISC-V SBI与LoongArch两种完全不同的架构规范。控方基于11个标准库同名函数提出的“大规模AST级复用”指控，在缺乏AST子树同构证据、忽视领域差异和标准规范强制性的情况下，无法成立。综合判定，目标项目不存在对参考项目的违规代码复用，整体风险极低。

------------------------------

