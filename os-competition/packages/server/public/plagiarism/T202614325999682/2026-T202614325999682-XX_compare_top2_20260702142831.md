# 🛡️ OS 内核赛自动化代码级架构审计报告

**审计时间**: `2026-07-02 14:28:31`
**参赛作品名称**: `2026-T202614325999682-XX`
**年份**: `2026`
**学校**: `XX`
**队伍名称**: `T202614325999682`
**比赛名字**: `OS-Competition`
**比赛子赛道**: `Kernel-Implementation`
**仓库地址**: `https://gitlab.eduxiji.net/educg-group-43501-3132633/T202614325999682-3734`
**推测开发语言**: `Rust / C (由AST探针自动识别)`
**特征函数量**: `203`

## ⚔️ 底层代码同源性对比 (AST Homology)
> 本部分由专门的代码审查智能体，对该项目与符合年份条件的历届最高相似度项目进行控制流与底层调用对比。新版报告会先给出确定性的全模块覆盖矩阵，再给出 Agent 的分模块解释，避免只围绕少量抽样函数展开。

### 🏆 Top 2 底层对标: vs `2025-高清的KernelX-杭州电子科技大学`（杭州电子科技大学 / 高清的KernelX）

#### 对方作品信息

**对方年份**: `2025`
**对方学校**: `杭州电子科技大学`
**对方队伍名称**: `高清的KernelX`
**对方比赛名字**: `OS-Competition`
**对方比赛子赛道**: `Kernel-Implementation`
**对方仓库地址**: `https://gitlab.eduxiji.net/educg-group-36002-2710490/T202510336995651-14`

> 双向覆盖率：共同 AST 指纹: 106；唯一 AST 指纹: 目标项目 158，参考项目 11088；覆盖率: 目标项目 67.09%，参考项目 0.96%

## 全模块对比总览（确定性证据）

> 总体覆盖率：共同 AST 指纹: 106；唯一 AST 指纹: 目标项目 158，参考项目 11088；覆盖率: 目标项目 67.09%，参考项目 0.96%

### 1. 模块覆盖矩阵
| 模块 | 目标函数数 | 参考函数数 | 共同唯一 AST 指纹 | Target 覆盖 | Base 覆盖 | 状态 |
| :--- | ---: | ---: | ---: | ---: | ---: | :--- |
| 进程管理 | 0 | 386 | 0 | 0.0% | 0.0% | 仅参考项目体现 |
| 任务调度/异步运行时 | 0 | 5269 | 0 | 0.0% | 0.0% | 仅参考项目体现 |
| 内存管理 | 9 | 2752 | 1 | 33.33% | 0.05% | 双方存在且有结构重合 |
| 中断/异常处理 | 0 | 1246 | 0 | 0.0% | 0.0% | 仅参考项目体现 |
| 系统调用 | 20 | 258 | 1 | 33.33% | 0.58% | 双方存在且有结构重合 |
| 文件系统 | 0 | 2033 | 0 | 0.0% | 0.0% | 仅参考项目体现 |
| 设备驱动 | 0 | 3298 | 0 | 0.0% | 0.0% | 仅参考项目体现 |
| 网络协议栈 | 0 | 0 | 0 | 0.0% | 0.0% | 双方均未体现 |
| 文件系统/第三方库 | 0 | 536 | 0 | 0.0% | 0.0% | 仅参考项目体现 |
| 第三方库 | 134 | 3 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |
| 通用模块/其他 | 40 | 2 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |

### 2. 分模块证据清单

#### 进程管理
仅参考项目中观察到该模块代表函数，目标项目没有同模块可对齐证据：
- 参考项目：`rt-thread/components/net/lwip/lwip-1.4.1/src/core/tcp_in.c#tcp_process`，调用摘要：LWIP_ASSERT, LWIP_DEBUGF, TCP_EVENT_ACCEPT, TCP_EVENT_CONNECTED, TCP_REG, TCP_RMV_ACTIVE ...(+13)
- 参考项目：`rt-thread/src/ipc.c#_rt_mq_send_wait`，调用摘要：GET_MESSAGEBYTE_ADDR, LOG_D, RT_ASSERT, RT_DEBUG_SCHEDULER_AVAILABLE, RT_OBJECT_HOOK_CALL, RT_UNUSED ...(+12)
- 参考项目：`rt-thread/components/net/lwip/lwip-2.1.2/src/core/ipv6/nd6.c#nd6_process_autoconfig_prefix`，调用摘要：IP6_ADDR, LWIP_ASSERT, ip6_addr_assign_zone, ip6_addr_cmp, ip6_addr_isduplicated, ip6_addr_isinvalid ...(+10)
- 参考项目：`rt-thread/components/net/lwip/lwip-2.0.3/src/netif/ppp/polarssl/sha1.c#sha1_process`，调用摘要：GET_ULONG_BE, P, R

#### 任务调度/异步运行时
仅参考项目中观察到该模块代表函数，目标项目没有同模块可对齐证据：
- 参考项目：`rt-thread/components/drivers/usb/cherryusb/class/video/usbd_video.c#usbd_video_control_unit_terminal_request_handler`，调用摘要：USB_LOG_WRN, memcpy
- 参考项目：`rt-thread/components/net/lwip/lwip-2.1.2/src/netif/lowpan6_common.c#lowpan6_decompress_hdr`，调用摘要：IP6H_HOPLIM_SET, IP6H_NEXTH_SET, IP6H_PLEN_SET, IP6H_VTCFL_SET, LWIP_ASSERT, LWIP_DEBUGF ...(+5)
- 参考项目：`rt-thread/src/klibc/rt_vsscanf.c#scanf_parse`，调用摘要：__sccl, isspace, rt_memcpy, rt_strlen, rt_strncmp, strtod ...(+5)
- 参考项目：`rt-thread/components/net/sal/src/sal_socket.c#sal_ioctlsocket`，调用摘要：LOG_E, SAL_NETDEV_SOCKETOPS_VALID, SAL_SOCKET_OBJ_GET, netdev_get_by_name, netdev_set_down, netdev_set_ipaddr ...(+9)

#### 内存管理
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `libs/sbi/sbi_remote_fence.c#sbi_remote_sfence_vma_asid` | `rt-thread/libcpu/risc-v/common64/sbi.c#sbi_remote_sfence_vma_asid` | 同板块同名 | sbi_ecall | RT_ASSERT, SBI_CALL4, SBI_CALL5 |
| `libs/sbi/sbi_remote_fence.c#sbi_remote_sfence_vma` | `rt-thread/libcpu/risc-v/common64/sbi.c#sbi_remote_sfence_vma` | 同板块同名 | sbi_ecall | SBI_CALL3, SBI_CALL4 |
| `libs/sbi/sbi_remote_fence.c#sbi_remote_hfence_gvma_vmid` | `rt-thread/components/mm/mm_page.c#_early_pages_alloc` | 同板块同结构 | sbi_ecall | __pages_alloc |
| `libs/sbi/sbi_remote_fence.c#sbi_remote_hfence_vvma_asid` | `rt-thread/components/mm/mm_page.c#_pages_alloc` | 同板块同结构 | sbi_ecall | __pages_alloc |

#### 中断/异常处理
仅参考项目中观察到该模块代表函数，目标项目没有同模块可对齐证据：
- 参考项目：`rt-thread/components/drivers/sdio/sdhci/sdhci.c#rt_sdhci_setup_host`，调用摘要：DIV_ROUND_UP, DMA_BIT_MASK, FIELD_GET, FIELD_PREP, LOG_D, RT_ASSERT ...(+18)
- 参考项目：`rt-thread/components/drivers/ofw/irq.c#ofw_parse_irq_map`，调用摘要：LOG_D, RT_ASSERT, _map_walk_range, fdt32_to_cpu, ofw_interrupt_cells, rt_ofw_bus_addr_cells ...(+3)
- 参考项目：`rt-thread/components/drivers/usb/cherryusb/port/fsdev/usb_dc_fsdev.c#USBD_IRQHandler`，调用摘要：MIN, PCD_CLEAR_RX_EP_CTR, PCD_CLEAR_TX_EP_CTR, PCD_GET_ENDPOINT, PCD_GET_EP_RX_CNT, PCD_GET_EP_TX_CNT ...(+12)
- 参考项目：`rt-thread/components/drivers/usb/cherryusb/port/ch32/usb_dc_usbhs.c#USBD_IRQHandler`，调用摘要：MIN, USB_GET_RX_CTRL, USB_GET_TX_CTRL, USB_SET_RX_CTRL, USB_SET_RX_DMA, USB_SET_TX_CTRL ...(+7)

#### 系统调用
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `libs/sbi/sbi_remote_fence.c#sbi_remote_fence_i` | `rt-thread/components/lwp/syscall/device.c#sys_device_write` | 同板块同结构 | sbi_ecall | rt_device_write |
| `libs/sbi/sbi_send_ipi.c#sbi_send_ipi` | `rt-thread/components/lwp/syscall/device.c#sys_device_read` | 同板块同结构 | sbi_ecall | rt_device_read |
| `libs/sbi/sbi_base.c#sbi_get_impl_version` | `rt-thread/components/drivers/ipc/workqueue.c#rt_work_submit` | 同板块同结构 | sbi_ecall | rt_workqueue_submit_work |
| `libs/sbi/sbi_base.c#sbi_get_spec_version` | `rt-thread/components/drivers/ipc/workqueue.c#rt_work_cancel` | 同板块同结构 | sbi_ecall | rt_workqueue_cancel_work |

#### 文件系统
仅参考项目中观察到该模块代表函数，目标项目没有同模块可对齐证据：
- 参考项目：`rt-thread/examples/libc/file.c#libc_fseek`，调用摘要：asprintf, fclose, fdopen, feof, fflush, fgetc ...(+21)
- 参考项目：`rt-thread/bsp/zynqmp-r5-axu4ev/drivers/Zynq_HAL_Driver/gpiops_v3_7/xgpiops_hw.c#XGpioPs_ResetHw`，调用摘要：XGetPlatform_Info, XGpioPs_WriteReg
- 参考项目：`rt-thread/components/dfs/dfs_v2/src/dfs_fs.c#dfs_mount`，调用摘要：DLOG, LOG_D, LOG_E, LOG_W, _find_filesystem, dfs_dentry_lookup ...(+13)
- 参考项目：`rt-thread/components/net/lwip/lwip-2.1.2/src/apps/snmp/snmp_core.c#snmp_get_next_node_instance_from_oid`，调用摘要：((const struct snmp_leaf_node *)(const void *)mn)->get_next_instance, LWIP_DEBUGF, node_instance->release_instance, snmp_get_mib_between, snmp_get_mib_from_oid, snmp_get_next_mib ...(+6)

#### 设备驱动
仅参考项目中观察到该模块代表函数，目标项目没有同模块可对齐证据：
- 参考项目：`rt-thread/components/drivers/spi/dev_spi_msd.c#rt_msd_init`，调用摘要：MSD_DEBUG, MSD_take_owner, _read_block, _send_cmd, msd->spi_device->bus->ops->xfer, rt_memset ...(+7)
- 参考项目：`rt-thread/bsp/zynqmp-r5-axu4ev/drivers/Zynq_HAL_Driver/xemacpsif/xemacpsif_physpeed.c#SetUpSLCRDivisors`，调用摘要：(UINTPTR), (u64), Xil_In32, Xil_Out32, Xil_Smc, xil_printf
- 参考项目：`rt-thread/components/drivers/sensor/v2/sensor_cmd.c#sensor`，调用摘要：LOG_E, RT_SENSOR_MODE_GET_ACCURACY, RT_SENSOR_MODE_GET_FETCH, RT_SENSOR_MODE_GET_POWER, atoi, rt_device_close ...(+19)
- 参考项目：`rt-thread/components/drivers/usb/cherryusb/class/hub/usbh_hub.c#usbh_hub_events`，调用摘要：USB_LOG_DBG, USB_LOG_ERR, USB_LOG_INFO, USB_LOG_WRN, memcpy, memset ...(+10)

#### 文件系统/第三方库
仅参考项目中观察到该模块代表函数，目标项目没有同模块可对齐证据：
- 参考项目：`machines/qemu-virt-riscv64/packages/lwext4-v2.0.0-dfsv2/src/ext4_balloc.c#ext4_balloc_alloc_block`，调用摘要：ext4_balloc_get_bgid_of_block, ext4_balloc_get_block_of_bgid, ext4_balloc_set_bitmap_csum, ext4_balloc_verify_bitmap_csum, ext4_bg_get_block_bitmap, ext4_bg_get_free_blocks_count ...(+19)
- 参考项目：`machines/qemu-virt-riscv64/packages/lwext4-v2.0.0-dfsv2/src/ext4.c#ext4_dir_rm`，调用摘要：EXT4_MP_LOCK, EXT4_MP_UNLOCK, ext4_block_cache_write_back, ext4_dir_en_get_inode, ext4_dir_iterator_fini, ext4_dir_iterator_init ...(+19)
- 参考项目：`machines/qemu-virt-riscv64/packages/lwext4-v2.0.0-dfsv2/src/ext4_journal.c#jbd_journal_prepare`，调用摘要：EXT4_BLOCK_ZERO, RB_FIND, TAILQ_FOREACH_REVERSE_SAFE, TAILQ_FOREACH_SAFE, TAILQ_REMOVE, ext4_bcache_clear_dirty ...(+17)
- 参考项目：`machines/qemu-virt-riscv64/packages/lwext4-v2.0.0-dfsv2/src/ext4_dir_idx.c#ext4_dir_dx_split_index`，调用摘要：ext4_block_set, ext4_dir_dx_climit_get_count, ext4_dir_dx_climit_get_limit, ext4_dir_dx_climit_set_count, ext4_dir_dx_climit_set_limit, ext4_dir_dx_entry_get_hash ...(+10)

#### 第三方库
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `third_party/libs/libfdt/fdt_overlay.c#overlay_symbol_update` | `rt-thread/components/drivers/usb/cherryusb/core/usbd_core.c#usbd_vendor_request_handler` | 同模块上下文对齐/差异观察 | fdt_add_subnode, fdt_for_each_property_offset, fdt_get_path, fdt_getprop_by_offset, fdt_overlay_target_offset, fdt_setprop_placeholder ...(+8) | USB_LOG_ERR, intf->vendor_handler |
| `third_party/libs/libfdt/fdt_rw.c#fdt_open_into` | `rt-thread/components/legacy/usb/usbdevice/core/usbdevice_core.c#_vendor_request` | 同模块上下文对齐/差异观察 | FDT_ALIGN, FDT_RO_PROBE, can_assume, fdt_blocks_misordered_, fdt_boot_cpuid_phys, fdt_move ...(+14) | RT_ASSERT, intf->handler, rt_list_entry, rt_list_len, rt_malloc, rt_memcpy ...(+3) |
| `third_party/libs/libfdt/fdt_overlay.c#overlay_fixup_phandle` | `rt-thread/components/drivers/sensor/v2/sensor_cmd.c#sensor_get_vendor_name` | 同模块上下文对齐/差异观察 | fdt_get_phandle, fdt_getprop, fdt_getprop_by_offset, fdt_path_offset, memchr, overlay_fixup_one_phandle ...(+1) | RT_SENSOR_MACRO_GET_NAME |

#### 通用模块/其他
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `libs/basecpp/stdlib.c#strtoul` | `oscomp/rv/src/start.c#test_start_musl` | 同模块上下文对齐/差异观察 | helper_c2d | chdir |
| `libs/basecpp/string.c#strtok` | `machines/qemu-loongarch/applications/main.c#main` | 同模块上下文对齐/差异观察 | 内部闭环逻辑 | rt_kprintf |


好的，审计官。基于提供的证据底座、双方画像及控辩逻辑，我将进行最终的独立定性仲裁。

### 1. 宏观判定
- **定性结论**: **独立开发 / 底层架构显著差异**
  客观 AST 相似度仅为 **0.95%**，参考项目覆盖率低至 **0.96%**。双方在操作系统核心子系统（进程、调度、文件系统、网络、设备驱动）上零交集。所谓“同板块同名”证据，经核实均为对 RISC-V SBI 公开规范的独立实现，其外部调用图谱、内存模型及同步策略存在根本性差异。指控方提出的复用逻辑不成立。

### 2. 全模块覆盖矩阵解读
- **进程管理**: 仅参考项目体现，目标项目无此模块。共同指纹为 0。**未形成**任何证据。
- **任务调度/异步运行时**: 仅参考项目体现，目标项目无此模块。共同指纹为 0。**未形成**任何证据。
- **内存管理**: 双方均存在。共同唯一 AST 指纹仅 1 个，参考项目覆盖率 0.05%。该指纹源于 RISC-V SBI 规范强制定义的函数签名，双方调用图谱（`sbi_ecall` vs `SBI_CALLx`/`RT_ASSERT`）完全不同。证据强度为 **弱**。
- **中断/异常处理**: 仅参考项目体现，目标项目无此模块。共同指纹为 0。**未形成**任何证据。
- **系统调用**: 双方均存在。共同唯一 AST 指纹仅 1 个，参考项目覆盖率 0.58%。目标项目为 RISC-V SBI 调用，参考项目为 lwIP/RT-Thread 设备框架调用，两者分属完全不同的子系统。证据强度为 **弱**。
- **文件系统**: 仅参考项目体现，目标项目无此模块。共同指纹为 0。**未形成**任何证据。
- **设备驱动**: 仅参考项目体现，目标项目无此模块。共同指纹为 0。**未形成**任何证据。
- **网络协议栈**: 双方均未体现。**未形成**任何证据。
- **第三方库/公共依赖**: 双方均存在，但共同指纹为 0。目标项目为 `libfdt`，参考项目为 USB/Sensor 驱动，领域完全不同。证据强度为 **弱**。

### 3. 底层 AST 关键证据指纹表
| 所属板块 | 目标项目路径 | 参考项目路径 | 证据类型 | 技术说明 |
| :--- | :--- | :--- | :--- | :--- |
| 内存管理 | `libs/sbi/sbi_remote_fence.c#sbi_remote_sfence_vma_asid` | `rt-thread/libcpu/risc-v/common64/sbi.c#sbi_remote_sfence_vma_asid` | 同板块同名 | 函数名由 RISC-V SBI 规范强制规定。目标项目调用 `sbi_ecall`，参考项目调用 `SBI_CALL4`/`SBI_CALL5` 宏，底层调用图谱异构。 |
| 内存管理 | `libs/sbi/sbi_remote_fence.c#sbi_remote_sfence_vma` | `rt-thread/libcpu/risc-v/common64/sbi.c#sbi_remote_sfence_vma` | 同板块同名 | 同上。目标项目调用 `sbi_ecall`，参考项目调用 `SBI_CALL3`/`SBI_CALL4` 宏，底层调用图谱异构。 |
| 系统调用 | `libs/sbi/sbi.c#sbi_ecall` | `rt-thread/libcpu/risc-v/common64/sbi.c` (SBI_CALLx 宏体系) | 同板块证据 | 目标项目将 SBI 调用封装为统一函数，参考项目使用宏。两者均遵循 RISC-V ecall ABI，但实现方式（函数 vs 宏）和错误处理（无 vs `RT_ASSERT`）不同。 |
| 第三方库 | `third_party/libs/libfdt/fdt_overlay.c#overlay_symbol_update` | `rt-thread/components/drivers/usb/cherryusb/core/usbd_core.c#usbd_vendor_request_handler` | 同板块上下文对齐/差异观察 | 双方均归类为第三方库，但领域完全不同（设备树操作 vs USB 协议栈），调用图谱无任何交集。此为聚类假阳性。 |

### 4. 核心代码调用流对比剖析
* **【内存管理 / RISC-V SBI 远程 fence 调用】**
* **底层技术析理**: 两者均实现 RISC-V SBI 规范中的 RFENCE 扩展，但**内存模型与同步策略**截然不同。目标项目采用薄封装模式，`sbi_remote_sfence_vma` 直接通过 `sbi_ecall` 函数陷入 M 模式，无任何本地状态检查或断言。参考项目则嵌入 RT-Thread 生态，调用 `SBI_CALL4` 宏前使用 `RT_ASSERT` 进行参数合法性断言，体现了 RTOS 的防御式编程风格。两者的**抽象层次**也不同：目标项目是固件级接口，参考项目是内核 CPU 架构层接口。
* **代码溯源链接**:
  - 目标项目: `libs/sbi/sbi_remote_fence.c#sbi_remote_sfence_vma`
  - 参考项目: `rt-thread/libcpu/risc-v/common64/sbi.c#sbi_remote_sfence_vma`

* **【系统调用 / SBI 底层陷入指令封装】**
* **底层技术析理**: 目标项目的核心是 `libs/sbi/sbi.c#sbi_ecall` 函数，它是一个内部闭环逻辑，直接内联汇编执行 `ecall` 指令，作为所有 SBI 服务的唯一陷入点。参考项目则通过 `SBI_CALL0`~`SBI_CALL5` 等多个宏直接展开 `ecall` 指令，无统一封装。这体现了**对象管理模型**的差异：目标项目采用中心化的调用分发，而参考项目采用去中心化的宏展开。两者在寄存器分配上均遵循 RISC-V calling convention，这是 ISA 规范的强制要求，非代码复用。
* **代码溯源链接**:
  - 目标项目: `libs/sbi/sbi.c#sbi_ecall`
  - 参考项目: `rt-thread/libcpu/risc-v/common64/sbi.c` (SBI_CALL 宏体系)

### 5. 单方模块与降权项
- **单方模块**: 进程管理、任务调度/异步运行时、中断/异常处理、文件系统、设备驱动、网络协议栈等核心模块仅在参考项目中存在。这构成了两个项目在**架构层级与功能完备性上的根本差异**，是判定独立开发的最强证据。
- **降权项**:
  - **RISC-V SBI 同名函数**: `sbi_remote_sfence_vma` 等函数名由公开行业标准强制规定，任何合规实现必然同名，不能作为抄袭证据。
  - **ISO C 标准库函数**: `memmove`, `strtoul`, `strtok` 等函数的行为由 C 语言标准定义，其内部闭环逻辑是标准算法的必然体现，不具特异性。
  - **跨子系统聚类假阳性**: 证据底座将目标项目的 SBI 调用与参考项目的 lwIP、workqueue 等函数归为“同板块”，属于聚类算法偏差，此类证据已被降权，不构成同源指控。

### 6. 保守结论
本次审计的客观 AST 相似度极低（0.95%），参考项目覆盖率仅 0.96%，表明目标项目未对参考项目进行系统性代码复用。双方在操作系统核心子系统上零交集，架构层级完全不同。所有“同名”证据均指向 RISC-V SBI 公开规范，其底层调用图谱、同步策略和抽象层次存在显著差异，符合独立实现的典型特征。第三方库和通用 C 函数因领域不同或标准约束，亦不构成同源证据。综上，目标项目不存在对参考项目的抄袭或违规复用行为，整体风险极低。

------------------------------

