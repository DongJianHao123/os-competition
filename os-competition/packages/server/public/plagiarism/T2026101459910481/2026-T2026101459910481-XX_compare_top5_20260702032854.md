# 🛡️ OS 内核赛自动化代码级架构审计报告

**审计时间**: `2026-07-02 03:28:54`
**参赛作品名称**: `2026-T2026101459910481-XX`
**年份**: `2026`
**学校**: `XX`
**队伍名称**: `T2026101459910481`
**比赛名字**: `OS-Competition`
**比赛子赛道**: `Kernel-Implementation`
**仓库地址**: `https://gitlab.eduxiji.net/educg-group-43501-3132633/T2026101459910481-3290`
**推测开发语言**: `Rust / C (由AST探针自动识别)`
**特征函数量**: `2841`

## ⚔️ 底层代码同源性对比 (AST Homology)
> 本部分由专门的代码审查智能体，对该项目与符合年份条件的历届最高相似度项目进行控制流与底层调用对比。新版报告会先给出确定性的全模块覆盖矩阵，再给出 Agent 的分模块解释，避免只围绕少量抽样函数展开。

### 🏆 Top 5 底层对标: vs `2025-NPUcore-BLOSSOM-西北工业大学`（西北工业大学 / NPUcore-BLOSSOM）

#### 对方作品信息

**对方年份**: `2025`
**对方学校**: `西北工业大学`
**对方队伍名称**: `NPUcore-BLOSSOM`
**对方比赛名字**: `OS-Competition`
**对方比赛子赛道**: `Kernel-Implementation`
**对方仓库地址**: `https://gitlab.eduxiji.net/educg-group-36002-2710490/oskernel2025-npucore-blossom`

> 双向覆盖率：共同 AST 指纹: 159；唯一 AST 指纹: 目标项目 2241，参考项目 2354；覆盖率: 目标项目 7.1%，参考项目 6.75%

## 全模块对比总览（确定性证据）

> 总体覆盖率：共同 AST 指纹: 159；唯一 AST 指纹: 目标项目 2241，参考项目 2354；覆盖率: 目标项目 7.1%，参考项目 6.75%

### 1. 模块覆盖矩阵
| 模块 | 目标函数数 | 参考函数数 | 共同唯一 AST 指纹 | Target 覆盖 | Base 覆盖 | 状态 |
| :--- | ---: | ---: | ---: | ---: | ---: | :--- |
| 进程管理 | 155 | 75 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |
| 任务调度/异步运行时 | 270 | 143 | 2 | 0.81% | 1.54% | 双方存在且有结构重合 |
| 内存管理 | 597 | 605 | 5 | 0.94% | 1.17% | 双方存在且有结构重合 |
| 中断/异常处理 | 236 | 47 | 3 | 2.27% | 7.5% | 双方存在且有结构重合 |
| 系统调用 | 67 | 65 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |
| 文件系统 | 472 | 561 | 6 | 1.56% | 1.61% | 双方存在且有结构重合 |
| 设备驱动 | 176 | 47 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |
| 网络协议栈 | 93 | 88 | 1 | 1.37% | 2.13% | 双方存在且有结构重合 |
| 文件系统/第三方库 | 426 | 369 | 116 | 33.72% | 40.14% | 双方存在且有结构重合 |
| 网络协议栈/第三方库 | 2 | 138 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |
| 第三方库 | 0 | 1228 | 0 | 0.0% | 0.0% | 仅参考项目体现 |
| 通用模块/其他 | 347 | 241 | 7 | 2.57% | 5.43% | 双方存在且有结构重合 |

### 2. 分模块证据清单

#### 进程管理
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `os/src/kernel/syscall/task/wait_ops.rs#wait4` | `os/src/syscall/process.rs#sys_execve` | 同模块上下文对齐/差异观察 | Some, TASK_MANAGER.lock, TASK_MANAGER.lock().release_task, WaitFlags::from_bits, WaitStatus::continued_code, WaitStatus::exit_code ...(+34) | Box::<[u8; 4]>::new, DEFAULT_SHELL.to_string, Some, Vec::with_capacity, arg_ptr.is_null, argv.add ...(+22) |
| `os/src/kernel/syscall/signal.rs#wait_for_signal` | `os/src/syscall/process.rs#sys_prlimit` | 同模块上下文对齐/差异观察 | Err, Ok, TIMER_QUEUE
                        .lock, TIMER_QUEUE
                        .lock()
                        .push, TIMER_QUEUE.lock, TIMER_QUEUE.lock().remove_task ...(+20) | Resource::from_primitive, assert!, copy_from_user, copy_from_user(token, new_limit, rlimit).is_err, copy_to_user, copy_to_user(
                        token,
                        &(RLimit {
                            rlim_cur: SYSTEM_TASK_LIMIT,
                            rlim_max: SYSTEM_TASK_LIMIT,
                        }),
                        old_limit,
                    )
                    .is_err ...(+18) |
| `os/src/kernel/task/exec_loader.rs#apply_static_pie_relocs` | `os/src/syscall/process.rs#sys_futex` | 同模块上下文对齐/差异观察 | Err, Ok, classify_relocation, classify_relocation(r_type).ok_or, phdrs.iter, phdrs.iter().find ...(+4) | FutexOption::from_bits_truncate, current_task, current_task().unwrap, do_futex_wait, drop, info! ...(+15) |
| `os/src/fs/proc/inode.rs#readdir` | `os/src/syscall/process.rs#sys_clone` | 同模块上下文对齐/差异观察 | ".".to_string, "..".to_string, Err, Ok, TASK_MANAGER.lock, TASK_MANAGER.lock().list_process_pids_snapshot ...(+14) | CloneFlags::from_bits, CloneFlags::from_bits(flags & !0xff).unwrap, Signals::empty, Signals::from_signum, add_task, current_task ...(+7) |

#### 任务调度/异步运行时
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `os/src/kernel/syscall/ioctl.rs#ioctl` | `os/src/fs/dev/tty.rs#ioctl` | 同板块同名 | current_task, e.to_errno, file.ioctl, handle_fioasync, handle_fionbio, handle_fionread ...(+11) | TeletypeCommand::from_primitive, copy_from_user, copy_to_user, crate::task::current_user_token, info!, self.inner.lock ...(+2) |
| `os/src/kernel/scheduler/rr_scheduler.rs#add_task` | `os/src/task/manager.rs#add_task` | 同板块同名 | crate::pr_debug!, panic!, self.enqueue_task, task.lock | self.inner.push |
| `os/src/kernel/scheduler/task_queue.rs#contains` | `os/src/task/manager.rs#contains` | 同板块同名 | Arc::ptr_eq | Weak::as_ptr, self.inner
            .iter, self.inner
            .iter()
            .any |
| `os/src/kernel/scheduler/task_queue.rs#pop_task` | `os/src/task/manager.rs#pop_task` | 同板块同名 | Some, self.queue.is_empty, self.queue.remove | self.inner.pop_front |

#### 内存管理
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `os/src/kernel/syscall/mm.rs#mmap` | `os/src/mm/memory_set.rs#mmap` | 同板块同名 | (offset as usize).is_multiple_of, MapFlags::from_bits_truncate, ProtFlags::from_bits_truncate, Some, VA::from_usize, Vpn::from_addr_ceil ...(+29) | MMAP_BASE.into, MapArea::new, Some, USR_MMAP_BASE.into, VirtAddr::from, VirtAddr::from(MMAP_END).into ...(+31) |
| `os/src/arch/riscv/mm/page_table.rs#map` | `os/src/hal/arch/loongarch64/laflex.rs#map` | 同板块同名 | (target_level..Self::LEVELS).rev, Err, Ok, PageTableEntry::empty, PageTableEntry::new_leaf, PageTableEntry::new_table ...(+16) | LAFlexPageTableEntry::new, debug_assert!, flags.contains, self.find_pte_create, self.find_pte_create(vpn).unwrap |
| `os/src/arch/loongarch/mm/page_table.rs#activate` | `os/src/hal/arch/loongarch64/laflex.rs#activate` | 同板块同名 | Self::tlb_flush_all, core::arch::asm!, ppn.as_usize, ppn.start_addr, ppn.start_addr().as_usize, super::kernel_root_paddr ...(+1) | self.get_root_ppn, self.is_kernel_pt, super::register::PGDH::from, super::register::PGDH::from(self.get_root_ppn().0 << PAGE_SIZE_BITS).write, super::register::PGDL::from, super::register::PGDL::from(self.get_root_ppn().0 << PAGE_SIZE_BITS).write ...(+1) |
| `os/src/mm/memory_space/space/mmap_ops.rs#mprotect` | `os/src/mm/memory_set.rs#mprotect` | 同板块同名 | Err, Ok, VA::from_usize, Vpn::from_addr_ceil, Vpn::from_addr_floor, Vpn::from_usize ...(+22) | Err, MapPermission::from_bits, MapPermission::from_bits(((prot as u8) << 1) | (1 << 4)).unwrap, Ok, VirtAddr::from, area.get_end::<T> ...(+18) |

#### 中断/异常处理
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `os/src/arch/loongarch/intr/mod.rs#enable_timer_interrupt` | `os/src/hal/arch/loongarch64/trap/mod.rs#enable_timer_interrupt` | 同板块同名 | update_ecfg | ECfg::empty, ECfg::empty()
        .set_line_based_interrupt_vector, ECfg::empty()
        .set_line_based_interrupt_vector(LineBasedInterrupt::TIMER)
        .write, TCfg::read, TCfg::read()
        .set_enable, TCfg::read()
        .set_enable(true)
        .set_periodic ...(+3) |
| `os/src/net/interface.rs#is_interrupt_enabled` | `os/src/hal/arch/loongarch64/register/base/crmd.rs#is_interrupt_enabled` | 同板块同名 | self.interrupt_enabled.lock | self.bits.get_bit |
| `os/src/arch/riscv/intr/mod.rs#enable_timer_interrupt` | `os/src/hal/arch/riscv/trap/mod.rs#enable_timer_interrupt` | 同板块同名 | sie::set_stimer | sie::set_stimer |
| `os/src/arch/mod.rs#sigreturn_trampoline_address` | `os/src/hal/arch/riscv/trap/mod.rs#get_bad_instruction` | 同板块同结构 | trap::sigreturn_trampoline_address | stval::read |

#### 系统调用
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `os/src/net/unix_socket.rs#shutdown` | `user/src/usr_call.rs#shutdown` | 同板块同名 | (0..=2).contains, conn.close_read, conn.close_write, conn.lock, crate::kernel::syscall::io::wake_poll_waiters, self.shutdown_read.lock ...(+2) | sys_shutdown |
| `user/lib/src/syscall.rs#waitpid` | `user/src/usr_call.rs#waitpid` | 同板块同名 | syscall! | sys_waitpid |
| `user/lib/src/syscall.rs#exit` | `user/src/usr_call.rs#exit` | 同板块同名 | syscall!, unreachable! | sys_exit |
| `user/lib/src/syscall.rs#getpid` | `user/src/usr_call.rs#getpid` | 同板块同名 | syscall! | sys_getpid |

#### 文件系统
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `os/src/fs/tmpfs/inode.rs#rename` | `os/src/fs/directory_tree.rs#rename` | 同板块同名 | Arc::downgrade, Arc::ptr_eq, Err, Ok, Self::is_descendant_or_self, Self::validate_rename_target ...(+40) | Arc::downgrade, Arc::new, Err, Mutex::new, Ok, ROOT.cd_comp ...(+41) |
| `os/src/fs/vfat/inode.rs#rename` | `os/src/fs/file_descriptor.rs#rename` | 同板块同名 | Arc::ptr_eq, Err, Ok, new_dir.open_dir, new_dir.open_dir(new_name).is_ok, new_dir.open_file ...(+17) | DirectoryTreeNode::rename, Err, [new_inode.get_cwd(), new_path.to_string()].join, [old_inode.get_cwd(), old_path.to_string()].join, new_fd.file.get_dirtree_node, new_fd.file.is_file ...(+8) |
| `os/src/vfs/impls/reg_file.rs#lseek` | `os/src/fs/dev/interrupts.rs#lseek` | 同板块同名 | Err, Ok, self.dir_entries.lock, self.inode.metadata, self.offset.lock | Err, Ok, self.get_size, self.offset.lock |
| `os/src/fs/tmpfs/inode.rs#unlink` | `os/src/fs/fat32/fat_osinode.rs#unlink` | 同板块同名 | Err, Ok, child.metadata.lock, children.get, children.get(name).ok_or, children.remove ...(+6) | Err, Ok, self.inner.is_dir, self.inner.is_empty_dir_lock, self.inner.unlink_lock, self.inner.write |

#### 设备驱动
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `os/src/device/virtio_hal.rs#mmio_phys_to_virt` | `os/src/drivers/block/virtio_blk.rs#mmio_phys_to_virt` | 同板块同名 | NonNull::new, NonNull::new(virt.as_usize() as *mut u8).expect, PA::from_usize, crate::arch::mmio_pa_to_va, virt.as_usize | NonNull::new, NonNull::new(vaddr.0 as *mut u8)
            .expect, PhysAddr, virtio_phys_to_virt |
| `os/src/device/block/partition.rs#read_block` | `os/src/drivers/block/virtio_blk.rs#read_block` | 同板块同名 | self.inner.read_block, self.start_block.checked_add | assert!, buf.chunks_mut, buf.chunks_mut(VIRT_IO_BLOCK_SZ).enumerate, self.0
                .lock, self.0
                .lock()
                .read_blocks, self.0
                .lock()
                .read_blocks(virtio_block_id as usize, chunk)
                .expect |
| `os/src/device/block/partition.rs#write_block` | `os/src/drivers/block/virtio_blk.rs#write_block` | 同板块同名 | self.inner.write_block, self.start_block.checked_add | assert!, buf.chunks, buf.chunks(VIRT_IO_BLOCK_SZ).enumerate, self.0
                .lock, self.0
                .lock()
                .write_blocks, self.0
                .lock()
                .write_blocks(virtio_block_id as usize, chunk)
                .expect |
| `os/src/arch/loongarch/lib.rs#console_getchar` | `os/src/hal/arch/loongarch64/sbi.rs#console_getchar` | 同板块同名 | ptr.add, ptr.add(5).read_volatile, ptr.read_volatile | 1usize.wrapping_neg, UART.read |

#### 网络协议栈
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `os/src/net/unix_socket.rs#writable` | `os/src/net/unix.rs#writable` | 同板块同名 | conn.local_write_closed, conn.lock, conn.outbound, conn.outbound(*side).len, conn.peer_read_closed, self.dgram_peer.lock ...(+5) | todo! |
| `os/src/net/unix_socket.rs#readable` | `os/src/net/udp.rs#readable` | 同板块同名 | conn.inbound, conn.inbound(*side).is_empty, conn.lock, conn.peer_write_closed, pending.is_empty, self.dgram_queue.lock ...(+3) | todo! |
| `os/src/net/socket.rs#readable` | `os/src/net/unix.rs#readable` | 同板块同名 | crate::net::stack::network_stack, crate::net::stack::network_stack().socket_readable | todo! |
| `os/src/net/socket.rs#writable` | `os/src/net/tcp.rs#writable` | 同板块同名 | crate::net::stack::network_stack, crate::net::stack::network_stack().socket_writable | 内部闭环逻辑 |

#### 文件系统/第三方库
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `third_party/ext4_rs/src/ext4_impls/file.rs#write_at` | `os/src/fs/ext4/file.rs#write_at` | 同板块同名 | Block::load, Err, Ok, allocated_blocks.len, block.data.copy_from_slice, block.sync_blk_to_disk ...(+23) | Block::load_offset, Ok, block.sync_blk_to_disk, block.write_offset, drop, inode_ref.inode.set_size ...(+10) |
| `third_party/ext4_rs/src/ext4_impls/extents.rs#insert_new_extent` | `os/src/fs/ext4/extent.rs#insert_new_extent` | 同板块同名 | (insert_pos..entries_count).rev, Block::load, Ok, Self::extent_insert_pos, core::mem::size_of::<Ext4Extent>, core::mem::size_of::<Ext4ExtentHeader> ...(+17) | Block::load_offset, Ok, core::mem::size_of::<Ext4Extent>, core::mem::size_of::<Ext4ExtentHeader>, ext4block.read_offset_as_mut, ext4block.sync_blk_to_disk ...(+5) |
| `third_party/ext4_rs/src/ext4_impls/extents.rs#extent_remove_space` | `os/src/fs/ext4/extent.rs#extent_remove_space` | 同板块同名 | Block::load, Ext4Extent::default, Ok, ex.get_actual_len, ex.get_first_block, ex.get_pblock ...(+14) | Block::load_offset, Ext4Extent::default, Ok, ex.get_actual_len, ex.get_first_block, ex.get_pblock ...(+14) |
| `third_party/ext4_rs/src/ext4_impls/extents.rs#ext_grow_indepth` | `os/src/fs/ext4/extent.rs#ext_grow_indepth` | 同板块同名 | Block::load, Ext4ExtentHeader::new, Ok, core::ptr::write_bytes, core::slice::from_raw_parts, inode_ref.inode.block.as_mut_ptr ...(+21) | Block::load_offset, Ext4ExtentHeader::load_from_u8_mut, Ok, core::mem::size_of::<Ext4Extent>, core::mem::size_of::<Ext4ExtentHeader>, core::ptr::copy_nonoverlapping ...(+15) |

#### 网络协议栈/第三方库
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `os/src/net/stack/mod.rs#poll_smoltcp` | `dependency/dep_iso/src/net/ethernet/intel/ixgbe.rs#new` | 同模块上下文对齐/差异观察 | crate::arch::get_time_ms, crate::pr_debug!, dev.loopback_queue_len, iface.poll, self.device.lock, self.interface.lock ...(+2) | (IXGBE_FCRTH..IXGBE_FCRTH_END).step_by, (IXGBE_FCRTL..IXGBE_FCRTL_END).step_by, (IXGBE_FCTTV..IXGBE_FCTTV_END).step_by, EthernetAddress::from_bytes, MaybeUninit::uninit, MaybeUninit::uninit().assume_init ...(+56) |
| `os/src/net/interface.rs#create_smoltcp_interface` | `dependency/virtio-drivers/src/device/socket/connectionmanager.rs#send_recv` | 同模块上下文对齐/差异观察 | SmoltcpInterface::new, addrs.push, self.device.clone, self.ip_addresses.lock, self.ip_addresses.lock().iter, self.ipv4_gateway ...(+7) | "Hello from guest".as_bytes, (hello_from_guest.len() as u32).into, (hello_from_host.len() as u32).into, 0.into, 50.into, Arc::new ...(+43) |

#### 第三方库
仅参考项目中观察到该模块代表函数，目标项目没有同模块可对齐证据：
- 参考项目：`dependency/virtio-drivers/src/queue.rs#fake_read_write_queue`，调用摘要：(*available_ring).idx.load, (*used_ring).idx.fetch_add, (*used_ring).idx.load, Vec::new, assert!, assert_eq! ...(+18)
- 参考项目：`dependency/dep_pci/pci/src/lib.rs#probe_function`，调用摘要：BAR::decode, BridgeControl::from_bits_truncate, Command::from_bits_truncate, DeviceKind::CardbusBridge, DeviceKind::Device, DeviceKind::PciBridge ...(+6)
- 参考项目：`dependency/virtio-drivers/src/device/sound/fake.rs#handle_control_request`，调用摘要：CommandCode::SOk.into, Some, Vec::new, VirtIOSndHdr {
                            command_code: CommandCode::SOk.into(),
                        }
                        .as_bytes, VirtIOSndHdr::read_from_prefix, VirtIOSndHdr::read_from_prefix(&request)
                .expect ...(+16)
- 参考项目：`dependency/dep_iso/src/block/ahci.rs#new`，调用摘要：(0..ghc.num_ports()).find, P::alloc_dma, Some, c.set_bit, debug!, ghc.enable ...(+16)

#### 通用模块/其他
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `os/src/arch/loongarch/timer.rs#set_next_trigger` | `os/src/hal/arch/riscv/time.rs#set_next_trigger` | 同板块同名 | (clock_freq() / TICKS_PER_SEC).max, clock_freq, core::arch::asm! | get_time, set_timer |
| `os/src/arch/riscv/lib.rs#sbi_call` | `os/src/hal/arch/riscv/sbi.rs#sbi_call` | 同板块同名 | core::arch::asm! | asm! |
| `os/src/uapi/time.rs#add` | `os/src/timer.rs#add` | 同板块同名 | 内部闭环逻辑 | 内部闭环逻辑 |
| `os/src/uapi/time.rs#sub` | `os/src/timer.rs#sub` | 同板块同名 | 内部闭环逻辑 | TimeSpec::from_ns, TimeSpec::new, other.to_ns, self.to_ns |


### 审计任务：最终同源性定性仲裁

#### 1. 宏观判定
- **定性结论**: **独立开发**
- **核心依据**: 客观 AST 相似度仅为 **3.58%**，远低于构成同源性疑虑的阈值。在 159 个共同 AST 指纹中，**116 个（73%）集中于“文件系统/第三方库”板块**，属于对 `ext4_rs` 等公共第三方库的共同引用，不反映核心内核的原创性复用。在代表内核核心设计差异的**进程管理、系统调用、设备驱动**等关键板块，共同指纹数量为 **0**。双方在任务调度、内存管理、中断处理等模块虽有零星结构重合，但覆盖率极低（< 8%），且经分析均为对 POSIX/Linux 标准 ABI 的独立实现或通用算法，未发现非标准的、私有化的同源痕迹。因此，综合判定目标项目为独立开发。

#### 2. 全模块覆盖矩阵解读

- **进程管理**
  - 双方均存在该模块。
  - **不存在共同 AST 指纹 (0)**。
  - 目标覆盖率 0.0%，参考覆盖率 0.0%。这表明双方在进程创建、退出、信号处理、资源限制等核心系统调用实现上，采用了完全不同的函数调用链和数据结构，无任何结构层面的重合。
  - **证据强度: 未形成**。控方所有指控均基于跨功能硬凑或 API 同名，无 AST 证据支撑。

- **任务调度/异步运行时**
  - 双方均存在该模块。
  - 存在 2 个共同 AST 指纹。
  - 目标覆盖率 0.81%，参考覆盖率 1.54%。覆盖率极低，表明调度器的核心数据结构（如运行队列）和调度算法实现基本独立。
  - **证据强度: 弱**。共同指纹源于 `add_task`、`pop_task` 等通用调度器操作名，其内部实现（如目标项目的 `enqueue_task` 与参考项目的 `self.inner.push`）调用链不同。

- **内存管理**
  - 双方均存在该模块。
  - 存在 5 个共同 AST 指纹。
  - 目标覆盖率 0.94%，参考覆盖率 1.17%。覆盖率极低，表明页表映射、内存分配、地址空间管理等核心实现差异巨大。
  - **证据强度: 弱**。共同指纹源于 `mmap`、`mprotect`、`map` 等 POSIX 标准接口或通用页表操作名，其内部调用图谱（如目标项目的 `Vpn::from_addr_ceil` 与参考项目的 `VirtAddr::from`）完全不同。

- **中断/异常处理**
  - 双方均存在该模块。
  - 存在 3 个共同 AST 指纹。
  - 目标覆盖率 2.27%，参考覆盖率 7.5%。覆盖率低，且参考项目覆盖率偏高可能因其模块函数总数较少（47个）放大了比例。
  - **证据强度: 弱**。共同指纹源于 `enable_timer_interrupt` 等通用操作，其底层硬件操作（如目标项目的 `update_ecfg` 与参考项目的 `ECfg::empty()...write`）因架构不同而调用链迥异。

- **系统调用**
  - 双方均存在该模块。
  - **不存在共同 AST 指纹 (0)**。
  - 目标覆盖率 0.0%，参考覆盖率 0.0%。这表明双方的系统调用分发、参数处理、内核态/用户态切换机制在 AST 层面完全独立。
  - **证据强度: 未形成**。共同点仅在于都实现了 `waitpid`、`exit` 等 POSIX 标准系统调用名，这是任何类 Unix 内核的必备项。

- **文件系统**
  - 双方均存在该模块。
  - 存在 6 个共同 AST 指纹。
  - 目标覆盖率 1.56%，参考覆盖率 1.61%。覆盖率极低，表明 VFS 框架、具体文件系统实现（除第三方库外）基本独立。
  - **证据强度: 弱**。共同指纹源于 `rename`、`unlink`、`lseek` 等标准 VFS 操作名，其内部实现（如目标项目的 `validate_rename_target` 与参考项目的 `ROOT.cd_comp`）调用链完全不同。

- **设备驱动**
  - 双方均存在该模块。
  - **不存在共同 AST 指纹 (0)**。
  - 目标覆盖率 0.0%，参考覆盖率 0.0%。双方在驱动模型和具体硬件操作上无任何结构重合。
  - **证据强度: 未形成**。共同点仅在于 `read_block`、`write_block` 等通用块设备操作名。

- **网络协议栈**
  - 双方均存在该模块。
  - 存在 1 个共同 AST 指纹。
  - 目标覆盖率 1.37%，参考覆盖率 2.13%。覆盖率极低，表明协议栈实现基本独立。
  - **证据强度: 弱**。共同指纹源于 `readable`/`writable` 等通用 socket 状态检查函数名，参考项目实现为 `todo!`，目标项目则有完整逻辑，差异巨大。

- **第三方库/公共依赖**
  - 双方均存在该模块（`文件系统/第三方库`），且参考项目有独立的 `第三方库` 模块。
  - 存在 **116 个共同 AST 指纹**，高度集中。
  - 目标覆盖率 33.72%，参考覆盖率 40.14%。这是双方唯一的显著结构重合区域。
  - **证据强度: 强，但指向公共依赖而非原创性复用**。所有证据均指向 `ext4_rs` 等第三方库的文件系统实现，属于对相同开源代码的共同引用，应从核心内核原创性判定中剥离。

#### 3. 底层 AST 关键证据指纹表

| 所属板块 | 目标项目路径 | 参考项目路径 | 证据类型 | 技术说明 |
| :--- | :--- | :--- | :--- | :--- |
| 第三方库/公共依赖 | `third_party/ext4_rs/src/ext4_impls/extents.rs#insert_new_extent` | `os/src/fs/ext4/extent.rs#insert_new_extent` | 同板块同名 | 双方均使用了 `ext4_rs` 库的 extent 操作逻辑，调用链高度相似，属于对同一第三方库的引用。 |
| 第三方库/公共依赖 | `third_party/ext4_rs/src/ext4_impls/file.rs#write_at` | `os/src/fs/ext4/file.rs#write_at` | 同板块同名 | 双方的文件写入操作均基于 `ext4_rs` 库的 `Block::load` 和 `sync_blk_to_disk` 调用链，属于对同一第三方库的引用。 |
| 任务调度/异步运行时 | `os/src/kernel/scheduler/task_queue.rs#pop_task` | `os/src/task/manager.rs#pop_task` | 同板块同名 | 函数名相同，但目标项目操作 `self.queue.remove`，参考项目操作 `self.inner.pop_front`，数据结构不同。 |
| 内存管理 | `os/src/kernel/syscall/mm.rs#mmap` | `os/src/mm/memory_set.rs#mmap` | 同板块同名 | 均为 POSIX `mmap` 实现，但目标项目使用 `Vpn::from_addr_ceil` 等虚拟地址抽象，参考项目使用 `VirtAddr::from`，内存模型不同。 |
| 中断/异常处理 | `os/src/arch/riscv/intr/mod.rs#enable_timer_interrupt` | `os/src/hal/arch/riscv/trap/mod.rs#enable_timer_interrupt` | 同板块同名 | 均为 RISC-V 架构下开启时钟中断，调用链极短且均为 `sie::set_stimer`，属于对 RISC-V SBI 规范的标准调用。 |
| 文件系统 | `os/src/fs/tmpfs/inode.rs#rename` | `os/src/fs/directory_tree.rs#rename` | 同板块同名 | 均为 `rename` 操作，但目标项目实现了 `is_descendant_or_self` 等复杂校验，参考项目则通过路径拼接操作，实现逻辑差异显著。 |
| 网络协议栈 | `os/src/net/unix_socket.rs#writable` | `os/src/net/unix.rs#writable` | 同板块同名 | 函数名相同，但目标项目有完整的连接状态检查逻辑，参考项目实现为 `todo!`，完成度天差地别。 |

#### 4. 核心代码调用流对比剖析

*   **【进程管理 / 资源限制系统调用】**
*   **底层技术析理**: 双方均实现了 `prlimit` 系统调用，但采用了完全不同的内核抽象和同步策略。目标项目使用全局 `TASK_MANAGER` 进行任务查找，并通过 `rlimit_lock` 和 `fd_table` 进行细粒度的资源管理；参考项目则在系统调用入口处通过 `Resource::from_primitive` 分发，并直接硬编码 `SYSTEM_TASK_LIMIT` 等常量进行比较。目标项目的用户态内存拷贝使用 `read_from_user`/`write_to_user`，而参考项目使用 `copy_from_user`/`copy_to_user`，表明二者在用户/内核地址空间交互的底层 helper 函数实现上亦非同源。
*   **代码溯源链接**:
  - 目标项目: `os/src/kernel/syscall/task/process_ops.rs#prlimit`
  - 参考项目: `os/src/syscall/process.rs#sys_prlimit`

*   **【进程管理 / Futex 同步机制】**
*   **底层技术析理**: 这是双方内核架构差异的典型例证。目标项目采用全局的 `FUTEX_MANAGER`，以**物理地址 (paddr)** 作为键值来管理等待队列，并通过 `TIMER_QUEUE` 实现超时机制。参考项目则采用**每任务 (per-task) 的 `futex` 锁**，通过 `WaitQueue::new` 创建等待队列，并直接调用调度器函数 `block_current_and_run_next` 进行阻塞。这两种 Futex 实现模型在对象管理、地址键类型、超时与调度交互上截然不同，是独立架构设计的强有力证明。
*   **代码溯源链接**:
  - 目标项目: `os/src/kernel/syscall/task/futex_ops.rs#futex_wait_common`
  - 参考项目: `os/src/task/threads.rs#do_futex_wait`

*   **【内存管理 / 页表映射】**
*   **底层技术析理**: 双方均实现了 `map` 函数用于建立虚拟地址到物理地址的映射，但页表遍历和项构造逻辑不同。目标项目采用从目标级别向上遍历的策略 `(target_level..Self::LEVELS).rev`，并显式构造 `PageTableEntry::new_leaf` 或 `new_table`。参考项目则通过 `self.find_pte_create(vpn)` 查找或创建页表项，并直接操作 `LAFlexPageTableEntry`。这反映了二者在页表数据结构设计和映射算法上的独立选择。
*   **代码溯源链接**:
  - 目标项目: `os/src/arch/riscv/mm/page_table.rs#map`
  - 参考项目: `os/src/hal/arch/loongarch64/laflex.rs#map`

#### 5. 单方模块与降权项

- **单方模块**:
  - **第三方库** (仅参考项目): 参考项目包含 1228 个函数的独立第三方库模块，目标项目无此独立分类。这进一步证明双方项目组织结构和依赖管理方式不同。
- **降权项说明**:
  - **POSIX/Linux 标准系统调用名**: `wait4`, `clone`, `execve`, `futex`, `mmap`, `mprotect`, `sigprocmask` 等函数名是操作系统为提供标准 ABI 所必须实现的，其同名不能作为同源性证据。
  - **Rust 语言标准库与宏**: `Some`, `Err`, `Ok`, `Vec::new`, `Arc::new`, `format!`, `assert!` 等是 Rust 语言的通用基础设施，在任何 Rust 项目中都会高频出现，不具备指纹特异性。
  - **架构相关的标准操作**: `core::arch::asm!`, `sie::set_stimer` 等是特定 CPU 架构（RISC-V, LoongArch）的标准指令或寄存器操作，其使用方式由硬件规范决定，不能作为软件复用的证据。
  - **跨板块同名函数**: 如目标项目 `os/src/net/unix_socket.rs#shutdown` 与参考项目 `user/src/usr_call.rs#shutdown`，一个在内核态网络栈，一个在用户态系统调用封装，属于完全不同的层次和功能，其同名是功能描述的自然重合，不构成同源证据。

#### 6. 保守结论

综合全模块 AST 指纹比对与核心调用流分析，目标项目 [2026-T2026101459910481-XX] 与参考项目 [2025-NPUcore-BLOSSOM-西北工业大学] 的客观相似度仅为 3.58%，处于极低水平。双方 73% 的共同指纹集中于 `ext4_rs` 等第三方公共库，属于对开源代码的共同依赖，不反映内核原创部分的复用。在进程管理、系统调用、设备驱动等核心差异化板块，共同指纹为零，且 Futex、资源限制等关键机制的实现模型（全局物理地址管理器 vs. 每任务私有锁）存在根本性架构差异。因此，本次审计未发现任何支持抄袭或违规复用的有效证据，整体原创性风险极低。

------------------------------

