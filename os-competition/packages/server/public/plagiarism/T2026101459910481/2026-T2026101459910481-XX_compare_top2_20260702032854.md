# 🛡️ OS 内核赛自动化代码级架构审计报告

**审计时间**: `2026-07-02 03:28:54`
**参赛作品名称**: `2026-T2026101459910481-XX`
**年份**: `2026`
**学校**: `XX`
**队伍名称**: `T2026101459910481`
**比赛名字**: `OS-Competition`
**比赛子赛道**: `Kernel-Implementation`
**仓库地址**: `https://gitlab.eduxiji.net/educg-group-43501-3132633/T2026101459910481-3290`
**推测开发语言**: `Rust / C (由AST探针自动识别)`
**特征函数量**: `2841`

## ⚔️ 底层代码同源性对比 (AST Homology)
> 本部分由专门的代码审查智能体，对该项目与符合年份条件的历届最高相似度项目进行控制流与底层调用对比。新版报告会先给出确定性的全模块覆盖矩阵，再给出 Agent 的分模块解释，避免只围绕少量抽样函数展开。

### 🏆 Top 2 底层对标: vs `2025-KernalCraft-中山大学`（中山大学 / KernalCraft）

#### 对方作品信息

**对方年份**: `2025`
**对方学校**: `中山大学`
**对方队伍名称**: `KernalCraft`
**对方比赛名字**: `OS-Competition`
**对方比赛子赛道**: `Kernel-Implementation`
**对方仓库地址**: `https://gitlab.eduxiji.net/educg-group-36002-2710490/T202510558995240-2536`

> 双向覆盖率：共同 AST 指纹: 218；唯一 AST 指纹: 目标项目 2241，参考项目 1853；覆盖率: 目标项目 9.73%，参考项目 11.76%

## 全模块对比总览（确定性证据）

> 总体覆盖率：共同 AST 指纹: 218；唯一 AST 指纹: 目标项目 2241，参考项目 1853；覆盖率: 目标项目 9.73%，参考项目 11.76%

### 1. 模块覆盖矩阵
| 模块 | 目标函数数 | 参考函数数 | 共同唯一 AST 指纹 | Target 覆盖 | Base 覆盖 | 状态 |
| :--- | ---: | ---: | ---: | ---: | ---: | :--- |
| 进程管理 | 155 | 71 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |
| 任务调度/异步运行时 | 270 | 115 | 1 | 0.4% | 0.89% | 双方存在且有结构重合 |
| 内存管理 | 597 | 741 | 8 | 1.51% | 1.79% | 双方存在且有结构重合 |
| 中断/异常处理 | 236 | 128 | 4 | 3.03% | 4.0% | 双方存在且有结构重合 |
| 系统调用 | 67 | 21 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |
| 文件系统 | 472 | 147 | 3 | 0.78% | 2.34% | 双方存在且有结构重合 |
| 设备驱动 | 176 | 555 | 5 | 3.65% | 1.07% | 双方存在且有结构重合 |
| 网络协议栈 | 93 | 53 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |
| 文件系统/第三方库 | 426 | 340 | 186 | 54.07% | 70.99% | 双方存在且有结构重合 |
| 网络协议栈/第三方库 | 2 | 0 | 0 | 0.0% | 0.0% | 仅目标项目体现 |
| 第三方库 | 0 | 2 | 0 | 0.0% | 0.0% | 仅参考项目体现 |
| 通用模块/其他 | 347 | 669 | 9 | 3.31% | 3.4% | 双方存在且有结构重合 |

### 2. 分模块证据清单

#### 进程管理
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `os/src/kernel/syscall/task/wait_ops.rs#wait4` | `os/src/syscall/process.rs#sys_exec` | 同模块上下文对齐/差异观察 | Some, TASK_MANAGER.lock, TASK_MANAGER.lock().release_task, WaitFlags::from_bits, WaitStatus::continued_code, WaitStatus::exit_code ...(+34) | BUSYBOX_BIN.to_vec, String::from, Vec::new, all_data.as_slice, app_inode.read_all, args.add ...(+15) |
| `os/src/kernel/syscall/signal.rs#wait_for_signal` | `os/src/syscall/signal.rs#sys_sigprocmask` | 同模块上下文对齐/差异观察 | Err, Ok, TIMER_QUEUE
                        .lock, TIMER_QUEUE
                        .lock()
                        .push, TIMER_QUEUE.lock, TIMER_QUEUE.lock().remove_task ...(+20) | SignalFlags::from_bits, SignalFlags::from_bits(new_set).unwrap, curr_process.inner_exclusive_access, current_process, inner.memory_set.token, inner.memory_set.write_to_user_ptr ...(+2) |
| `os/src/kernel/task/exec_loader.rs#apply_static_pie_relocs` | `fdt/src/node.rs#children` | 同模块上下文对齐/差异观察 | Err, Ok, classify_relocation, classify_relocation(r_type).ok_or, phdrs.iter, phdrs.iter().find ...(+4) | CStr::new, CStr::new(stream.remaining()).expect, CStr::new(stream.remaining()).expect("unit name").as_str, FdtData::new, NodeProperty::parse, Self::new ...(+11) |
| `os/src/fs/proc/inode.rs#readdir` | `os/src/syscall/info.rs#write_proc_global` | 同模块上下文对齐/差异观察 | ".".to_string, "..".to_string, Err, Ok, TASK_MANAGER.lock, TASK_MANAGER.lock().list_process_pids_snapshot ...(+14) | EXT4
            .generic_open, EXT4
            .generic_open(
                filename,
                &mut parent_inode_num,
                true,
                InodeFileType::S_IFREG.bits(),
                &mut 0,
            )
            .unwrap, EXT4.ext4_dir_mk_at, Inode::new, InodeFileType::S_IFREG.bits, content.as_bytes ...(+13) |

#### 任务调度/异步运行时
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `os/src/arch/loongarch/trap/trap_handler.rs#trap_handler` | `os/src/hal/trap/mod.rs#trap_handler` | 同板块同名 | FIRST_TRAP_LOGGED.swap, check_signal, core::arch::asm!, crate::kernel::try_current_task, crate::kernel::try_current_task()
        .map, crate::kernel::try_current_task()
        .map(|t| t.lock().trap_frame_ptr.load(Ordering::SeqCst) as usize)
        .unwrap_or ...(+6) | TrapContextImpl::decode_trap_type, TrapContextImpl::set_kernel_trap_entry, TrapContextImpl::trap_return, main_trap_handler |
| `os/src/arch/loongarch/trap/trap_handler.rs#check_timer` | `os/src/timer.rs#check_timer` | 同板块同名 | TIMER.lock, TIMER.lock().pop_due_entry, TIMER.lock().push, TIMER_QUEUE.lock, TIMER_QUEUE.lock().pop_due_task, TIMER_TICKS.fetch_add ...(+12) | Arc::clone, TIMERS.exclusive_session, get_time_ms, timers.peek, timers.pop, wakeup_task |
| `os/src/kernel/scheduler/rr_scheduler.rs#add_task` | `os/src/task/manager.rs#add_task` | 同板块同名 | crate::pr_debug!, panic!, self.enqueue_task, task.lock | TASK_MANAGER.exclusive_access, TASK_MANAGER.exclusive_access().add |
| `os/src/sync/mutex.rs#lock` | `os/src/sync/mutex.rs#lock` | 同板块同名 | current_task, drop, self.data.get, self.guard.lock, self.locked.swap, self.queue.lock ...(+2) | block_current_and_run_next, current_task, current_task().unwrap, drop, mutex_inner.wait_queue.push_back, self.inner.exclusive_access |

#### 内存管理
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `os/src/mm/memory_space/space/elf_loader.rs#from_elf` | `os/src/mm/memory_set.rs#from_elf` | 同板块同名 | ElfFile::new, ElfFile::new(elf_data).map_err, Err, MemorySpace::new, Ok, PA::from_usize ...(+67) | AuxHeader::new, MapArea::new, MmapManager::new, Self::new_bare, Some, VirtAddr ...(+13) |
| `os/src/kernel/syscall/mm.rs#mmap` | `os/src/mm/memory_set.rs#mmap` | 同板块同名 | (offset as usize).is_multiple_of, MapFlags::from_bits_truncate, ProtFlags::from_bits_truncate, Some, VA::from_usize, Vpn::from_addr_ceil ...(+29) | MapArea::new, log::error!, log::trace!, mmap_manager.mmap_alloc, self.mmap_manager.as_mut, self.push |
| `os/src/mm/memory_space/space/mmap_ops.rs#brk` | `os/src/mm/memory_set.rs#brk` | 同板块同名 | Err, Ok, UniversalPTEFlag::user_rw, Vpn::from_addr_ceil, VpnRange::new, a.area_type ...(+20) | VirtAddr, current_start_heap_vpn.into, heap_area.remap, self.areas.get_mut, self.areas.get_mut(heap_area_idx).unwrap, self.areas[heap_area_idx].vpn_range.get_start ...(+1) |
| `os/src/arch/riscv/mm/page_table.rs#map` | `os/src/hal/pagetable/riscv64.rs#map` | 同板块同名 | (target_level..Self::LEVELS).rev, Err, Ok, PageTableEntry::empty, PageTableEntry::new_leaf, PageTableEntry::new_table ...(+16) | RVPageTableEntry::new, assert!, self.find_pte_create, self.find_pte_create(vpn).unwrap |

#### 中断/异常处理
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `os/src/arch/riscv/intr/mod.rs#enable_irq` | `os/src/hal/irq/loongarch64/mod.rs#enable_irq` | 同板块同名 | sie::set_sext | Eiointc::enable_irq, log::warn!, self.platic.read_w, self.platic.write_b, self.platic.write_w |
| `os/src/arch/loongarch/intr/mod.rs#enable_timer_interrupt` | `os/src/hal/instruction/loongarch.rs#enable_timer_interrupt` | 同板块同名 | update_ecfg | crmd::set_ie, ecfg::set_lie, get_timer_freq, log::info!, tcfg::set_en, tcfg::set_init_val ...(+2) |
| `os/src/arch/riscv/trap/mod.rs#restore` | `os/src/hal/trap/riscv.rs#restore` | 同板块同名 | __restore | asm! |
| `os/src/arch/loongarch/trap/trap_frame.rs#set_sp` | `os/src/hal/trap/loongarch.rs#set_sp` | 同板块同名 | TrapFrame::set_sp | 内部闭环逻辑 |

#### 系统调用
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `os/src/kernel/syscall/network/sockopt_ops.rs#set` | `os/src/syscall/ppoll.rs#set` | 同板块同名 | socket.set_socket_options | Self::fd_elt, Self::fd_mask |
| `os/src/kernel/syscall/sys.rs#syslog` | `os/src/syscall/net.rs#sys_socket` | 同模块上下文对齐/差异观察 | LogLevel::from_u8, SyslogAction::from_i32, bytes.as_ptr, bytes.len, check_syslog_permission, crate::arch::ArchImpl::copy_to_user ...(+14) | create_tcp_socket, create_udp_socket |
| `os/src/kernel/syscall/network/socket_ops.rs#is_local_bind_address` | `os/src/syscall/input.rs#sys_event_get` | 同模块上下文对齐/差异观察 | NETWORK_INTERFACE_MANAGER
                    .lock, NETWORK_INTERFACE_MANAGER
                    .lock()
                    .get_interfaces, NETWORK_INTERFACE_MANAGER
                    .lock()
                    .get_interfaces()
                    .iter, NETWORK_INTERFACE_MANAGER
                    .lock()
                    .get_interfaces()
                    .iter()
                    .any, addr.is_loopback, addr.is_unspecified ...(+5) | KEYBOARD_DEVICE.clone, MOUSE_DEVICE.clone, kb.is_empty, kb.read_event, mouse.is_empty, mouse.read_event |
| `os/src/kernel/syscall/mm.rs#get_mempolicy` | `os/src/syscall/info.rs#fmt` | 同模块上下文对齐/差异观察 | mode.is_null, nmask.is_null, validate_user_ptr_mut, write_to_user | writeln! |

#### 文件系统
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `os/src/kernel/syscall/fs/metadata_ops.rs#linkat` | `os/src/fs/inode.rs#linkat` | 同板块同名 | DENTRY_CACHE.insert, Dentry::new, FsError::AlreadyExists.to_errno, FsError::NotDirectory.to_errno, FsError::NotFound.to_errno, crate::uapi::fs::AtFlags::SYMLINK_FOLLOW.bits ...(+14) | EXT4.ext4_file_open_at, EXT4.fuse_link, dir.starts_with, get_dirinode_at, newpath.rfind, parent_inode.is_err ...(+1) |
| `os/src/kernel/syscall/fs/path_ops.rs#unlinkat` | `os/src/fs/inode.rs#unlinkat` | 同板块同名 | FsError::InvalidArgument.to_errno, FsError::IsDirectory.to_errno, FsError::NotDirectory.to_errno, FsError::NotFound.to_errno, drop_cached_child, e.to_errno ...(+7) | EXT4.fuse_unlink, dir.starts_with, get_dirinode_at, log::trace!, path.rfind |
| `os/src/fs/vfat/inode.rs#write_at` | `os/src/fs/inode.rs#write_at` | 同板块同名 | Err, Ok, buf.is_empty, buf.len, fatfs::Seek::seek, fatfs::Seek::seek(&mut file, fatfs::SeekFrom::Start(current_size as u64))
                    .map_err ...(+11) | EXT4.write_at, panic! |
| `os/src/fs/tmpfs/inode.rs#rmdir` | `os/src/fs/inode.rs#rmdir` | 同板块同名 | Err, Ok, child.children.lock, child.metadata.lock, child_children.is_empty, children.get ...(+8) | unimplemented! |

#### 设备驱动
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `os/src/device/rtc/rtc_goldfish.rs#init_dt` | `virtio-drivers/examples/riscv/src/main.rs#init_dt` | 同板块同名 | Arc::new, DRIVERS.write, DRIVERS.write().push, PA::from_usize, RTC_DRIVERS.write, RTC_DRIVERS.write().push ...(+17) | Fdt::from_ptr, Fdt::from_ptr(dtb as *const u8).unwrap, info!, walk_dt |
| `os/src/device/net/net_device.rs#receive` | `virtio-drivers/src/device/console.rs#receive` | 同板块同名 | Err, Ok, buf.len, buf[..actual_len].copy_from_slice, core::cmp::min, packet.len ...(+3) | Arc::new, Mutex::new, ReadOnly::new, State::new, VirtIOConsole::<FakeHal, FakeTransport<Config>>::new, VirtIOConsole::<FakeHal, FakeTransport<Config>>::new(transport).unwrap ...(+7) |
| `os/src/device/block/partition.rs#read_blocks` | `virtio-drivers/src/device/blk.rs#read_blocks` | 同板块同名 | buf.len, buf.len().is_multiple_of, self.block_size, self.inner.read_blocks, self.start_block.checked_add, start_block.checked_add | assert_eq!, assert_ne!, self.request_read |
| `os/src/device/block/partition.rs#write_blocks` | `virtio-drivers/src/device/blk.rs#write_blocks` | 同板块同名 | buf.len, buf.len().is_multiple_of, self.block_size, self.inner.write_blocks, self.start_block.checked_add, start_block.checked_add | Default::default, assert_eq!, assert_ne!, self.request_write |

#### 网络协议栈
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `os/src/net/unix_socket.rs#writable` | `os/src/net/port_table.rs#writable` | 同板块同名 | conn.local_write_closed, conn.lock, conn.outbound, conn.outbound(*side).len, conn.peer_read_closed, self.dgram_peer.lock ...(+5) | 内部闭环逻辑 |
| `os/src/net/unix_socket.rs#readable` | `os/src/net/port_table.rs#readable` | 同板块同名 | conn.inbound, conn.inbound(*side).is_empty, conn.lock, conn.peer_write_closed, pending.is_empty, self.dgram_queue.lock ...(+3) | 内部闭环逻辑 |
| `os/src/net/socket.rs#readable` | `os/src/net/tcp.rs#readable` | 同板块同名 | crate::net::stack::network_stack, crate::net::stack::network_stack().socket_readable | 内部闭环逻辑 |
| `os/src/net/socket.rs#writable` | `os/src/net/tcp.rs#writable` | 同板块同名 | crate::net::stack::network_stack, crate::net::stack::network_stack().socket_writable | 内部闭环逻辑 |

#### 文件系统/第三方库
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `third_party/ext4_rs/src/ext4_impls/file.rs#write_at` | `ext4_rs/src/ext4_impls/file.rs#write_at` | 同板块同名 | Block::load, Err, Ok, allocated_blocks.len, block.data.copy_from_slice, block.sync_blk_to_disk ...(+23) | Block::load, Ok, block.sync_blk_to_disk, block.write_offset, drop, inode_ref
                .inode
                .set_size ...(+9) |
| `third_party/ext4_rs/src/ext4_impls/extents.rs#insert_new_extent` | `ext4_rs/src/ext4_impls/extents.rs#insert_new_extent` | 同板块同名 | (insert_pos..entries_count).rev, Block::load, Ok, Self::extent_insert_pos, core::mem::size_of::<Ext4Extent>, core::mem::size_of::<Ext4ExtentHeader> ...(+17) | Block::load, Ok, core::mem::size_of::<Ext4Extent>, core::mem::size_of::<Ext4ExtentHeader>, ext4block.read_offset_as_mut, ext4block.sync_blk_to_disk ...(+6) |
| `os/src/fs/ext4/inode.rs#rename` | `ext4_rs/src/fuse_interface/mod.rs#rename` | 同板块同名 | Arc::ptr_eq, Err, Ok, Some, existing_ref.inode.is_dir, fs.dir_add_entry ...(+28) | Ok, self.link, self.unlink |
| `third_party/ext4_rs/src/ext4_impls/extents.rs#extent_remove_space` | `ext4_rs/src/ext4_impls/extents.rs#extent_remove_space` | 同板块同名 | Block::load, Ext4Extent::default, Ok, ex.get_actual_len, ex.get_first_block, ex.get_pblock ...(+14) | Block::load, Ext4Extent::default, Ok, ex.get_actual_len, ex.get_first_block, ex.get_pblock ...(+15) |

#### 网络协议栈/第三方库
仅目标项目中观察到该模块代表函数，参考项目没有同模块可对齐证据：
- 目标项目：`os/src/net/stack/mod.rs#poll_smoltcp`，调用摘要：crate::arch::get_time_ms, crate::pr_debug!, dev.loopback_queue_len, iface.poll, self.device.lock, self.interface.lock ...(+2)
- 目标项目：`os/src/net/interface.rs#create_smoltcp_interface`，调用摘要：SmoltcpInterface::new, addrs.push, self.device.clone, self.ip_addresses.lock, self.ip_addresses.lock().iter, self.ipv4_gateway ...(+7)

#### 第三方库
仅参考项目中观察到该模块代表函数，目标项目没有同模块可对齐证据：
- 参考项目：`virtio-drivers/src/transport/mmio.rs#vendor_id`，调用摘要：field_shared!, field_shared!(self.header, vendor_id).read
- 参考项目：`virtio-drivers-old/src/header.rs#vendor_id`，调用摘要：self.vendor_id.read

#### 通用模块/其他
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `os/src/uapi/log.rs#as_str` | `fdt/src/node.rs#as_str` | 同板块同名 | 内部闭环逻辑 | core::str::from_utf8, core::str::from_utf8(self.value).map, core::str::from_utf8(self.value).map(|s| s.trim_end_matches('\0')).ok, s.trim_end_matches |
| `os/src/kernel/boot.rs#clear_bss` | `os/src/main.rs#clear_bss` | 同板块同名 | (sbss_paddr.as_usize()..ebss_paddr.as_usize()).for_each, (va.as_usize() as *mut u8).write_volatile, crate::arch::address::PA::from_usize, crate::arch::address::VA::from_usize, crate::arch::pa_to_va, crate::arch::va_to_pa ...(+3) | core::slice::from_raw_parts_mut, core::slice::from_raw_parts_mut(sbss as usize as *mut u8, ebss as usize - sbss as usize)
            .fill |
| `os/src/arch/loongarch/timer.rs#set_next_trigger` | `os/src/timer.rs#set_next_trigger` | 同板块同名 | (clock_freq() / TICKS_PER_SEC).max, clock_freq, core::arch::asm! | TimerImpl::get_time, TimerImpl::set_timer |
| `os/src/arch/riscv/cpu_ops.rs#id` | `plic/src/lib.rs#id` | 同板块同名 | core::arch::asm! | NonZeroU32::new, NonZeroU32::new(self).expect |


好的，审计官。基于提供的证据底座、控辩双方陈词及AST指纹数据，本庭现对案件[2026-T2026101459910481-XX]涉嫌同源性抄袭一案，作出最终定性仲裁。

#### 1. 宏观判定
- **定性结论**: **独立开发 (相似度: 5.62%)**
  综合客观AST相似度（5.62%）、极低的双向覆盖率（目标9.73%，参考11.76%）以及进程管理等核心板块零共同指纹的事实，本庭认定两个项目在整体架构与核心实现上存在显著差异，不构成同源性抄袭。观察到的微量相似度主要集中于双方共同依赖的第三方库，属于合法的公共代码复用。

#### 2. 全模块覆盖矩阵解读
- **进程管理**: 双方均存在。**共同AST指纹为0**，覆盖率均为0.0%。这是控方指控的核心区域，但客观证据为零，表明双方在进程创建、调度、通信、退出等核心逻辑上无任何可识别的结构同源性。**证据强度：未形成**。
- **任务调度/异步运行时**: 双方均存在。共同指纹1个，覆盖率极低（目标0.4%，参考0.89%）。微弱的信号来自架构相关的trap handler或通用调度器接口，属于领域内常见模式，不足以构成同源证据。**证据强度：弱**。
- **内存管理**: 双方均存在。共同指纹8个，覆盖率低（目标1.51%，参考1.79%）。重合点集中在`from_elf`、`mmap`、`brk`等POSIX规范强相关的接口名和标准算法上，底层实现（如页表映射`map`函数）的调用链存在差异。**证据强度：弱**。
- **中断/异常处理**: 双方均存在。共同指纹4个，覆盖率低（目标3.03%，参考4.0%）。证据为`enable_irq`、`restore`等架构强相关的硬件操作，其实现由硬件手册决定，同名同参是必然结果。**证据强度：弱**。
- **系统调用**: 双方均存在。**共同AST指纹为0**，覆盖率均为0.0%。双方的系统调用分发与实现路径完全不同，无任何结构重合。**证据强度：未形成**。
- **文件系统**: 双方均存在。共同指纹3个，覆盖率低（目标0.78%，参考2.34%）。重合点如`linkat`、`unlinkat`、`write_at`均为POSIX标准文件操作接口，其高层逻辑相似，但底层存储后端（目标为VFS/proc/tmpfs，参考为EXT4）和具体实现调用链不同。**证据强度：弱**。
- **设备驱动**: 双方均存在。共同指纹5个，覆盖率低（目标3.65%，参考1.07%）。证据指向`init_dt`、`read_blocks`等设备树解析和块设备操作，这些是遵循硬件规范的标准流程。**证据强度：弱**。
- **网络协议栈**: 双方均存在。**共同AST指纹为0**，覆盖率均为0.0%。双方在网络栈实现上无任何结构重合。**证据强度：未形成**。
- **第三方库/公共依赖**: 双方均存在。共同指纹186个，覆盖率高（目标54.07%，参考70.99%）。这是双方相似度的**唯一主要来源**，证据明确指向`ext4_rs`、`fatfs`等第三方库的代码。这是对公共开源组件的合法复用，不构成对参考项目原创部分的抄袭。**证据强度：强（但属于合法公共依赖）**。

#### 3. 底层 AST 关键证据指纹表
| 所属板块 | 目标项目路径 | 参考项目路径 | 证据类型 | 技术说明 |
| :--- | :--- | :--- | :--- | :--- |
| 第三方库/公共依赖 | `third_party/ext4_rs/src/ext4_impls/file.rs#write_at` | `ext4_rs/src/ext4_impls/file.rs#write_at` | 同源指纹 | 完全相同的第三方库函数，调用链高度一致，是覆盖率的主要贡献者。 |
| 第三方库/公共依赖 | `third_party/ext4_rs/src/ext4_impls/extents.rs#insert_new_extent` | `ext4_rs/src/ext4_impls/extents.rs#insert_new_extent` | 同源指纹 | 完全相同的第三方库函数，涉及复杂的Ext4 extent树操作，逻辑一致。 |
| 第三方库/公共依赖 | `os/src/fs/ext4/inode.rs#rename` | `ext4_rs/src/fuse_interface/mod.rs#rename` | 同源指纹 | 目标项目对第三方库的封装函数，与库内部实现逻辑同源。 |
| 内存管理 | `os/src/mm/memory_space/space/elf_loader.rs#from_elf` | `os/src/mm/memory_set.rs#from_elf` | 同名/规范实现 | 双方均实现ELF加载，遵循ELF标准规范，但内部调用链（如页分配器、映射函数）不同。 |
| 文件系统 | `os/src/kernel/syscall/fs/metadata_ops.rs#linkat` | `os/src/fs/inode.rs#linkat` | 同名/POSIX接口 | 双方均实现POSIX `linkat`系统调用，高层逻辑相似，但底层文件系统后端完全不同。 |
| 设备驱动 | `os/src/device/block/partition.rs#read_blocks` | `virtio-drivers/src/device/blk.rs#read_blocks` | 同名/领域模式 | 双方均实现块设备读取，遵循标准的块设备驱动模型，但操作的具体硬件和抽象层不同。 |

#### 4. 核心代码调用流对比剖析
* **【进程管理/信号处理】**
* **底层技术析理**: 控方指控的核心模块。目标项目的`rt_sigprocmask`通过`read_from_user`/`write_to_user`安全原语与用户态交互，并使用`task.lock()`保护任务结构体。参考项目的`sys_sigprocmask`则直接通过`memory_set.write_to_user_ptr`和`token`操作地址空间，并使用`inner_exclusive_access()`保护进程结构。两者在**用户态数据拷贝抽象、内核同步原语、以及内存管理模型**上存在根本性差异，体现了不同的内核架构设计思想。
* **代码溯源链接**:
  - 目标项目: `os/src/kernel/syscall/signal.rs#rt_sigprocmask`
  - 参考项目: `os/src/syscall/signal.rs#sys_sigprocmask`

* **【内存管理/ELF加载】**
* **底层技术析理**: 双方都实现了`from_elf`函数，这是加载可执行文件的必要步骤。然而，目标项目的实现依赖于自定义的`MemorySpace::new`、`PA::from_usize`和复杂的权限映射逻辑，而参考项目则基于`MmapManager::new`、`MapArea::new`和`VirtAddr`构建。两者在**内存空间抽象、物理/虚拟地址管理、以及区域映射策略**上采用了不同的对象模型和内部API，表明其内存管理子系统是独立构建的。
* **代码溯源链接**:
  - 目标项目: `os/src/mm/memory_space/space/elf_loader.rs#from_elf`
  - 参考项目: `os/src/mm/memory_set.rs#from_elf`

* **【文件系统/第三方库】**
* **底层技术析理**: 这是双方相似度的主要来源。目标项目在`os/src/fs/ext4/inode.rs`中对`ext4_rs`库的`rename`操作进行了封装，而参考项目在`ext4_rs/src/fuse_interface/mod.rs`中直接实现了该逻辑。两者的调用链高度相似，因为它们都源于同一个第三方开源库`ext4_rs`。这是对公共代码的合法依赖，而非对参考项目原创部分的抄袭。
* **代码溯源链接**:
  - 目标项目: `os/src/fs/ext4/inode.rs#rename`
  - 参考项目: `ext4_rs/src/fuse_interface/mod.rs#rename`

#### 5. 单方模块与降权项
- **单方模块**:
  - **网络协议栈/第三方库**: 仅目标项目拥有（`smoltcp`相关封装），是其独立实现的网络子系统。
  - **第三方库**: 仅参考项目拥有（`virtio-drivers`旧版本），是其独立依赖的驱动库。
  这些模块的存在进一步证明双方在技术选型和架构设计上的独立性。
- **降权项说明**:
  - **跨板块同名函数**: 如`wait4`与`sys_exec`、`apply_static_pie_relocs`与`children`等，属于控方为构建证据链而进行的强行配对。它们分属不同功能领域，其调用图谱无任何可比性，不构成同源证据。
  - **POSIX/规范接口**: `wait4`、`mmap`、`linkat`、`read_at`等函数名由POSIX标准或通用编程规范定义，同名是接口兼容性的必然要求，不代表代码复用。
  - **第三方库代码**: `ext4_rs`、`fatfs`等库的代码重合是双方共同依赖开源生态的结果，已在矩阵中剥离并认定为合法公共依赖。

#### 6. 保守结论
本庭基于客观AST指纹数据裁定，参赛作品[2026-T2026101459910481-XX]与参考项目[2025-KernalCraft-中山大学]在整体代码实现上存在显著差异，5.62%的极低相似度属于独立开发的正常范畴。控方关于“进程管理”等核心板块的复用指控，因缺乏客观的AST结构同源证据（共同指纹为0）而不能成立。双方唯一的相似度集中点在于共同依赖的第三方开源库（如`ext4_rs`），这属于合法的公共代码复用，不构成对参考项目原创性的抄袭。综上，本庭认定目标项目为独立开发，驳回控方所有指控。

------------------------------

