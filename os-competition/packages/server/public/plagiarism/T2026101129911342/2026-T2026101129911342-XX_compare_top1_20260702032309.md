# 🛡️ OS 内核赛自动化代码级架构审计报告

**审计时间**: `2026-07-02 03:23:09`
**参赛作品名称**: `2026-T2026101129911342-XX`
**年份**: `2026`
**学校**: `XX`
**队伍名称**: `T2026101129911342`
**比赛名字**: `OS-Competition`
**比赛子赛道**: `Kernel-Implementation`
**仓库地址**: `https://gitlab.eduxiji.net/educg-group-43501-3132633/T2026101129911342-3041`
**推测开发语言**: `Rust / C (由AST探针自动识别)`
**特征函数量**: `193`

## ⚔️ 底层代码同源性对比 (AST Homology)
> 本部分由专门的代码审查智能体，对该项目与符合年份条件的历届最高相似度项目进行控制流与底层调用对比。新版报告会先给出确定性的全模块覆盖矩阵，再给出 Agent 的分模块解释，避免只围绕少量抽样函数展开。

### 🏆 Top 1 底层对标: vs `2024-NPUcore-重生之我是秦始皇-河南理工大学`（河南理工大学 / NPUcore-重生之我是秦始皇）

#### 对方作品信息

**对方年份**: `2024`
**对方学校**: `河南理工大学`
**对方队伍名称**: `NPUcore-重生之我是秦始皇`
**对方比赛名字**: `OS-Competition`
**对方比赛子赛道**: `Kernel-Implementation`
**对方仓库地址**: `https://gitlab.eduxiji.net/educg-group-26011-2376549/T202410460992502-2319`

> 双向覆盖率：共同 AST 指纹: 3；唯一 AST 指纹: 目标项目 186，参考项目 1037；覆盖率: 目标项目 1.61%，参考项目 0.29%

## 全模块对比总览（确定性证据）

> 总体覆盖率：共同 AST 指纹: 3；唯一 AST 指纹: 目标项目 186，参考项目 1037；覆盖率: 目标项目 1.61%，参考项目 0.29%

### 1. 模块覆盖矩阵
| 模块 | 目标函数数 | 参考函数数 | 共同唯一 AST 指纹 | Target 覆盖 | Base 覆盖 | 状态 |
| :--- | ---: | ---: | ---: | ---: | ---: | :--- |
| 进程管理 | 5 | 77 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |
| 任务调度/异步运行时 | 7 | 129 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |
| 内存管理 | 38 | 346 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |
| 中断/异常处理 | 8 | 27 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |
| 系统调用 | 16 | 46 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |
| 文件系统 | 54 | 412 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |
| 设备驱动 | 15 | 18 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |
| 网络协议栈 | 5 | 0 | 0 | 0.0% | 0.0% | 仅目标项目体现 |
| 文件系统/第三方库 | 14 | 140 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |
| 第三方库 | 0 | 10 | 0 | 0.0% | 0.0% | 仅参考项目体现 |
| 通用模块/其他 | 31 | 76 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |

### 2. 分模块证据清单

#### 进程管理
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `kernel/src/syscall.rs#sys_rt_sigprocmask` | `os/src/syscall/process.rs#sys_execve` | 同模块上下文对齐/差异观察 | (oldset as *mut u64).write_volatile, (set as *const u64).read_volatile | Box::<[u8; 4]>::new, DEFAULT_SHELL.to_string, Some, Vec::with_capacity, arg_ptr.is_null, argv.add ...(+22) |
| `khal/src/loongarch64.rs#exec_replace` | `os/src/syscall/process.rs#sys_prlimit` | 同模块上下文对齐/差异观察 | __la_enter_user, core::mem::size_of::<KStack> | Resource::from_primitive, assert!, copy_from_user, copy_from_user(token, new_limit, rlimit).is_err, copy_to_user, copy_to_user(
                        token,
                        &(RLimit {
                            rlim_cur: SYSTEM_TASK_LIMIT,
                            rlim_max: SYSTEM_TASK_LIMIT,
                        }),
                        old_limit,
                    )
                    .is_err ...(+18) |
| `khal/src/riscv64.rs#exec_replace` | `os/src/syscall/process.rs#sys_clone` | 同模块上下文对齐/差异观察 | __enter_user, core::mem::size_of::<KStack> | CloneFlags::from_bits, CloneFlags::from_bits(flags & !0xff).unwrap, Signals::empty, Signals::from_signum, add_task, child.acquire_inner_lock ...(+9) |
| `kernel/src/net.rs#cloexec` | `os/src/syscall/process.rs#sys_futex` | 同模块上下文对齐/差异观察 | socks, socks().len | FutexOption::from_bits_truncate, current_task, current_task().unwrap, do_futex_wait, drop, info! ...(+15) |

#### 任务调度/异步运行时
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `kernel/src/syscall.rs#sys_readlinkat` | `os/src/syscall/fs.rs#sys_readlinkat` | 同板块同名 | ((buf + i) as *mut u8).write_volatile, core::cmp::min, link.iter, link.iter().take, link.iter().take(n).enumerate, link.len ...(+1) | __openat, copy_to_user_string, copy_to_user_string(token, &real_path[0..len], buf).is_err, current_task, current_task().unwrap, debug! ...(+10) |
| `kernel/src/syscall.rs#kunikos_syscall` | `os/src/syscall/mod.rs#syscall` | 同模块上下文对齐/差异观察 | crate::fs::chdir, crate::fs::close, crate::fs::dup, crate::fs::dup3, crate::fs::fstat, crate::fs::ftruncate ...(+53) | Errno::try_from, [
            //black list
            SYSCALL_YIELD,
            // SYSCALL_READ,
            SYSCALL_WRITE,
            SYSCALL_GETDENTS64,
            SYSCALL_READV,
            SYSCALL_WRITEV,
            SYSCALL_PSELECT6,
            SYSCALL_SIGACTION,
            SYSCALL_SIGPROCMASK,
            // SYSCALL_WAIT4,
            // SYSCALL_GETPPID,
            SYSCALL_CLOCK_GETTIME,
        ]
        .contains, args.len, crate::task::current_task, crate::task::current_task()
                .unwrap, crate::task::current_task()
                .unwrap()
                .acquire_inner_lock ...(+99) |
| `kernel/src/syscall.rs#sys_sysinfo` | `os/src/syscall/mod.rs#syscall_name` | 同模块上下文对齐/差异观察 | ((info + 104) as *mut u32).write_volatile, ((info + 80) as *mut u16).write_volatile, ((info + off) as *mut usize).write_volatile, core::ptr::write_bytes, khal::time_ns, w | 内部闭环逻辑 |
| `khal/src/loongarch64.rs#run_user` | `os/src/syscall/fs.rs#sys_sendfile` | 同模块上下文对齐/差异观察 | KernelContext::default, __la_run_user, arm_timeout, core::mem::size_of::<KStack>, disarm_timeout, enable_timer_irq ...(+1) | Some, Vec::<u8>::with_capacity, buffer.as_mut_slice, buffer.as_slice, buffer.set_len, current_task ...(+17) |

#### 内存管理
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `kernel/src/heap.rs#alloc` | `os/src/fs/fat32/bitmap.rs#alloc` | 同板块同名 | align_up, bnext, bsize, core::hint::spin_loop, core::ptr::null_mut, layout.size ...(+7) | Vec::with_capacity, allocated_cluster.push, last.is_none, last.unwrap, log::error!, self.alloc_one ...(+2) |
| `kernel/src/heap.rs#insert` | `os/src/mm/zram.rs#insert` | 同板块同名 | bnext, bsize, self.head.get, wr | Arc::new, Err, Ok, Some, ZramTracker, self.compressed.len ...(+1) |
| `kernel/src/heap.rs#dealloc` | `os/src/mm/frame_allocator.rs#dealloc` | 同板块同名 | align_up, core::hint::spin_loop, layout.size, layout.size().max, ptr.is_null, self
            .lock
            .compare_exchange ...(+3) | Some, log::trace!, option_env!, panic!, self.recycled.iter, self.recycled.iter().find ...(+2) |
| `khal/src/riscv64.rs#frame_alloc` | `os/src/mm/frame_allocator.rs#frame_alloc` | 同板块同名 | FRAME_NEXT.fetch_add, assert!, core::ptr::write_bytes | Arc::new, FRAME_ALLOCATOR
                .write, FRAME_ALLOCATOR
                .write()
                .alloc, FRAME_ALLOCATOR
                .write()
                .alloc()
                .map, FRAME_ALLOCATOR.write, FRAME_ALLOCATOR.write().alloc ...(+2) |

#### 中断/异常处理
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `khal/src/riscv64.rs#rv_user_syscall` | `os/src/arch/la64/trap/mod.rs#trap_from_kernel` | 同模块上下文对齐/差异观察 | asm!, crate::kprintln!, exit_user, kunikos_syscall, rdtime, rdtime().wrapping_add ...(+1) | ((addr + i) as *mut u8).read_unaligned, ((addr + i) as *mut u8).write_unaligned, (0..sz).rev, BadV::read, BadV::read().get_vaddr, EStat::read ...(+14) |
| `khal/src/loongarch64.rs#la_user_syscall` | `os/src/arch/la64/trap/mem_access.rs#get_addr` | 同模块上下文对齐/差异观察 | asm!, crate::kprintln!, exit_user, kunikos_syscall, rdtime_now | self.get_op_code, self.get_op_code().map, self.get_rj_num, self.get_rk_num, self.get_si12, self.get_si14 |
| `khal/src/riscv64.rs#rv_trap_handler` | `os/src/arch/la64/trap/mem_access.rs#get_size` | 同模块上下文对齐/差异观察 | (tf.sepc as *const u16).read_volatile, asm!, crate::kprintln!, shutdown | 内部闭环逻辑 |
| `khal/src/loongarch64.rs#la_trap_handler` | `os/src/fs/dev/tty.rs#default` | 同模块上下文对齐/差异观察 | asm!, crate::kprintln!, shutdown | 内部闭环逻辑 |

#### 系统调用
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `kernel/src/syscall.rs#sys_getrusage` | `user/src/syscall.rs#sys_getrusage` | 同板块同名 | core::ptr::write_bytes | syscall |
| `kernel/src/syscall.rs#sys_wait4` | `user/src/syscall.rs#syscall` | 同模块上下文对齐/差异观察 | (0..NZOMBIE).rev, (status as *mut i32).write_volatile, Some | __syscall, asm! |
| `kernel/src/syscall.rs#sys_nanosleep` | `user/src/syscall.rs#sys_exec` | 同模块上下文对齐/差异观察 | ((req + 8) as *const u64).read_volatile, (req as *const u64).read_volatile, core::hint::spin_loop, khal::time_ns, khal::time_ns().wrapping_sub, sec.saturating_mul ...(+1) | args.as_ptr, envp.as_ptr, path.as_ptr, syscall |
| `kernel/src/syscall.rs#sys_uname` | `os/src/syscall/socket.rs#sys_sendto` | 同模块上下文对齐/差异观察 | ((buf + i * 65 + j) as *mut u8).write_volatile, bs.len, f.iter, f.iter().enumerate, s.as_bytes | 内部闭环逻辑 |

#### 文件系统
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `kernel/src/fs.rs#lseek` | `os/src/fs/fat32/inode.rs#lseek` | 同板块同名 | fds, files, files()[*idx].data.len | Err, Ok, self.inner.get_file_size_wlock, self.inner.write, self.offset.lock |
| `kernel/src/fs.rs#mkdir` | `os/src/fs/directory_tree.rs#mkdir` | 同板块同名 | Node::dir, Vec::new, files, files().iter, files().iter().any, files().push ...(+2) | (*last_comp).to_string, Arc::downgrade, Err, Ok, Self::new, Self::parse_dir_path ...(+13) |
| `kernel/src/fs.rs#unlink` | `os/src/fs/fat32/inode.rs#unlink` | 同板块同名 | files, files().iter, files().iter().position, files()[i].name.clear, path.trim_start_matches, path.trim_start_matches("./").trim_start_matches | Err, Ok, self.inner.is_dir, self.inner.is_empty_dir_lock, self.inner.unlink_lock, self.inner.write |
| `kernel/src/virtio_blk.rs#init` | `os/src/fs/directory_tree.rs#open` | 同模块上下文对齐/差异观察 | (!mask).wrapping_add, rd16, rd32, rd8, wr16, wr32 ...(+1) | (*last_comp).to_string, Arc::downgrade, Err, Ok, PATH_CACHE.lock, REDIRECT_TO_BUSYBOX.contains ...(+27) |

#### 设备驱动
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `khal/src/loongarch64.rs#console_putchar` | `os/src/arch/la64/sbi.rs#console_putchar` | 同板块同名 | core::ptr::write_volatile | UART.write |
| `kernel/src/virtio_blk.rs#read` | `os/src/drivers/block/mod.rs#block_device_test` | 同模块上下文对齐/差异观察 | ai.wrapping_add, buf.as_mut_ptr, buf.len, core::ptr::addr_of!, core::ptr::read_volatile, fence ...(+2) | BLOCK_DEVICE.clone, assert_eq!, block_device.read_block, block_device.write_block, println!, write_buffer.iter_mut |
| `khal/src/loongarch64.rs#pci_scan` | `os/src/mm/map_area.rs#force_swap_out` | 同模块上下文对齐/差异观察 | core::ptr::read_volatile, crate::kprintln! | Err, Frame::SwappedOut, Ok, SWAP_DEVICE.lock, SWAP_DEVICE.lock().write, frame_ref.ppn.get_bytes_array |
| `kernel/src/virtio_blk.rs#init` | `os/src/drivers/block/virtio_blk.rs#read_block` | 同模块上下文对齐/差异观察 | r, setup | buf.chunks_mut, self.0
                .lock, self.0
                .lock()
                .read_block, self.0
                .lock()
                .read_block(block_id, buf)
                .expect |

#### 网络协议栈
仅目标项目中观察到该模块代表函数，参考项目没有同模块可对齐证据：
- 目标项目：`kernel/src/net.rs#connect`，调用摘要：Vec::new, parse_port, socks, socks().iter, socks().iter().position, socks().len ...(+2)
- 目标项目：`kernel/src/net.rs#sendto`，调用摘要：Vec::with_capacity, d.push, parse_port, socks, socks().iter, socks().iter().position ...(+1)
- 目标项目：`kernel/src/net.rs#parse_port`，调用摘要：内部闭环逻辑
- 目标项目：`kernel/src/net.rs#socks`，调用摘要：内部闭环逻辑

#### 文件系统/第三方库
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `kernel/src/ext4.rs#mount` | `os/src/fs/ext4/lwext4_rust/examples/src/ext4fs.rs#mount` | 同板块同名 | (num_groups * desc_size).div_ceil, Some, alloc::vec!, bgd[i * BS..(i + 1) * BS].copy_from_slice, rd16, rd32 ...(+2) | Ok |
| `kernel/src/ext4.rs#extent_runs` | `os/src/fs/ext4/lwext4_rust/src/blockdev.rs#new` | 同模块上下文对齐/差异观察 | Vec::new, alloc::vec!, blk.to_vec, out.push, rd16, rd32 ...(+3) | Box::into_raw, Box::new, CString::new, CString::new("/").unwrap, CString::new("ext4_fs").expect, Ok ...(+16) |
| `kernel/src/ext4.rs#read_file` | `os/src/fs/ext4/lwext4_rust/c/ulibc.c#qsort_r` | 同模块上下文对齐/差异观察 | alloc::vec!, core::cmp::min, data.len, data.truncate, rd32, self.extent_runs ...(+2) | (*comp), ext4_assert |
| `kernel/src/ext4.rs#list_root` | `os/src/fs/ext4/lwext4_rust/examples/src/main.rs#walk_dt_node` | 同模块上下文对齐/差异观察 | String::from, Vec::new, core::str::from_utf8, out.push, rd16, rd32 ...(+3) | core::mem::size_of::<u32>, dt.children.iter, dt.prop_cells, dt.prop_cells("ranges").unwrap, dt.prop_raw, dt.prop_str ...(+12) |

#### 第三方库
仅参考项目中观察到该模块代表函数，目标项目没有同模块可对齐证据：
- 参考项目：`dependency/rlibc/src/lib.rs#memmove_overlapping`，调用摘要：assert_eq!, b"0123012345".iter, b"4567896789".iter, memmove
- 参考项目：`dependency/rlibc/src/lib.rs#memcmp_strings`，调用摘要：assert!, assert_eq!, x.len
- 参考项目：`dependency/rlibc/src/lib.rs#memmove`，调用摘要：dest.offset, src.offset
- 参考项目：`dependency/rlibc/src/lib.rs#memset_single_byte_pointers`，调用摘要：assert_eq!, memset

#### 通用模块/其他
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `khal/src/loongarch64.rs#shutdown` | `os/src/arch/la64/sbi.rs#shutdown` | 同板块同名 | asm!, core::ptr::write_volatile | Pm1Cnt::empty, pm1_cnt.set_s5, pm1_cnt.set_s5().write |
| `khal/src/lib.rs#write_str` | `os/src/console.rs#write_str` | 同板块同名 | Ok, console_write | Ok, console_flush, console_putchar, s.chars |
| `khal/src/loongarch64.rs#reset_user_space` | `user/src/lib.rs#_parameter` | 同模块上下文对齐/差异观察 | FRAME_NEXT.store, KERNEL_WATERMARK.load, asm!, dir2.add | ((argv + i * core::mem::size_of::<usize>()) as *const usize).read_volatile, ((str_start + *i) as *const u8).read_volatile, (0usize..)
            .find, (0usize..)
            .find(|i| unsafe { ((str_start + *i) as *const u8).read_volatile() == 0 })
            .unwrap, HEAP.lock, HEAP.lock()
            .init ...(+10) |
| `khal/src/riscv64.rs#is_shared` | `user/src/bin/exit.rs#main` | 同模块上下文对齐/差异观察 | base.add | assert!, exit, fork, println!, yield_ |


### 审计任务：最终同源性定性仲裁

#### 1. 宏观判定
- **定性结论**: **独立开发**
- **核心依据**: 客观 AST 相似度仅为 0.25%，双方共同 AST 指纹仅 3 个，而各自拥有 186 和 1037 个唯一指纹。在所有核心模块（进程管理、内存管理、文件系统等）中，共同唯一 AST 指纹数均为 0。该数据量级属于两个独立 Rust 项目因语言标准库和领域规范而产生的正常背景噪声，远未达到代码复用或抄袭的判定标准。

#### 2. 全模块覆盖矩阵解读
- **进程管理**: 双方均存在。共同 AST 指纹为 0，覆盖率极低。目标项目采用裸指针直接操作信号掩码，参考项目则使用 `SigMaskHow` 等安全抽象，实现路径完全不同。证据强度：**弱**。
- **任务调度/异步运行时**: 双方均存在。共同 AST 指纹为 0。目标项目 `kunikos_syscall` 直接分发至功能模块，参考项目 `syscall` 则围绕任务上下文和锁机制构建，调度模型截然不同。证据强度：**弱**。
- **内存管理**: 双方均存在。共同 AST 指纹为 0。目标项目采用 CAS 自旋锁链表堆和原子计数器线性帧分配器，参考项目则使用回收站和全局单例分配器，算法与数据结构均不同。证据强度：**弱**。
- **中断/异常处理**: 双方均存在。共同 AST 指纹为 0。目标项目在 HAL 层直接处理寄存器，参考项目有复杂的 `Instruction` 解码和 `BadV` 处理，异常处理流程差异显著。证据强度：**弱**。
- **系统调用**: 双方均存在。共同 AST 指纹为 0。路径分发、错误处理、参数传递方式均不同。证据强度：**弱**。
- **文件系统**: 双方均存在。共同 AST 指纹为 0。目标项目为自研 `ext4` 解析，参考项目包含 `fat32` 和 `lwext4_rust` 封装，文件系统实现完全不同。证据强度：**弱**。
- **设备驱动**: 双方均存在。共同 AST 指纹为 0。目标项目直接 MMIO 操作 `virtio_blk`，参考项目有分层块设备抽象，驱动模型不同。证据强度：**弱**。
- **网络协议栈**: 仅目标项目存在，拥有独立的 `connect`、`sendto` 等实现。参考项目无此模块。证据强度：**未形成**。
- **第三方库/公共依赖**: 参考项目存在 `rlibc` 等第三方库，目标项目无对应模块。双方无共同指纹。证据强度：**未形成**。

#### 3. 底层 AST 关键证据指纹表
| 所属板块 | 目标项目路径 | 参考项目路径 | 证据类型 | 技术说明 |
| :--- | :--- | :--- | :--- | :--- |
| 任务调度/异步运行时 | `kernel/src/syscall.rs#sys_readlinkat` | `os/src/syscall/fs.rs#sys_readlinkat` | 同板块同名 | 同名源于 POSIX 规范。目标项目使用 `write_volatile` 和手动迭代，参考项目使用 `copy_to_user_string` 封装，调用链无重合。 |
| 内存管理 | `khal/src/riscv64.rs#load_elf_args` | `os/src/mm/memory_set.rs#create_elf_tables` | 同板块证据 | 同为 ELF 加载。目标项目直接裸指针写入栈，参考项目使用 `AuxvEntry` 和页表翻译，外部依赖对象完全不同。 |
| 内存管理 | `kernel/src/heap.rs#alloc` | `os/src/fs/fat32/bitmap.rs#alloc` | 同板块同名 | 同名源于通用术语。目标项目为 CAS 锁链表堆分配，参考项目为 FAT32 簇位图分配，算法与数据结构均不同。 |
| 内存管理 | `khal/src/riscv64.rs#frame_alloc` | `os/src/mm/frame_allocator.rs#frame_alloc` | 同板块同名 | 同名源于通用术语。目标项目为原子计数器线性分配，参考项目为带回收机制的全局分配器，实现策略不同。 |
| 进程管理 | `kernel/src/syscall.rs#sys_rt_sigprocmask` | `os/src/task/signal.rs#sigprocmask` | 同板块证据 | 同为信号掩码操作。目标项目直接 `u64` 裸指针读写，参考项目使用 `SigMaskHow` 位掩码和任务锁，实现路径完全不同。 |

#### 4. 核心代码调用流对比剖析
* **【内存管理/物理帧分配】**
* **底层技术析理**: 两者在物理帧分配这一核心内存管理原语上展现了根本性的架构差异。目标项目采用极简的线性分配策略，通过一个全局原子计数器 `FRAME_NEXT.fetch_add` 直接递增并返回帧号，不涉及任何回收机制。参考项目则构建了复杂的带回收机制的单例分配器，通过 `FRAME_ALLOCATOR.write().alloc()` 获取帧，并维护一个 `recycled` 向量来管理回收的物理页。这两种设计在同步策略（原子操作 vs. 读写锁）、生命周期管理（无回收 vs. 显式回收）和数据结构上完全不同，不存在同源性。
* **代码溯源链接**:
  - 目标项目: `khal/src/riscv64.rs#frame_alloc`
  - 参考项目: `os/src/mm/frame_allocator.rs#frame_alloc`

* **【任务调度/异步运行时/系统调用分发】**
* **底层技术析理**: 两者的系统调用分发模型存在显著差异。目标项目的 `kunikos_syscall` 函数是一个扁平化的分发器，直接调用 `crate::fs::chdir`、`crate::fs::close` 等具体功能模块函数，逻辑简单直接。参考项目的 `syscall` 函数则是一个深度集成任务管理的中心化调度器，强依赖 `current_task().unwrap().acquire_inner_lock()` 获取任务锁，并维护一个系统调用黑名单数组进行权限检查。两者在调度模型（扁平分发 vs. 中心化任务感知）、错误处理（简单返回 vs. `Errno::try_from`）和同步策略上截然不同。
* **代码溯源链接**:
  - 目标项目: `kernel/src/syscall.rs#kunikos_syscall`
  - 参考项目: `os/src/syscall/mod.rs#syscall`

* **【文件系统/路径解析与挂载】**
* **底层技术析理**: 两者在文件系统实现上选择了完全不同的技术路线。目标项目包含一个自研的 `ext4` 文件系统解析器，其 `mount` 函数直接操作裸块设备，手动解析超级块和块组描述符（`bgd[i * BS..(i + 1) * BS].copy_from_slice`）。参考项目则采用多文件系统支持架构，既有 `fat32` 的复杂簇管理，也有对 `lwext4_rust` 外部库的封装。两者在文件系统类型、实现层次和外部依赖上均无共同点。
* **代码溯源链接**:
  - 目标项目: `kernel/src/ext4.rs#mount`
  - 参考项目: `os/src/fs/ext4/lwext4_rust/examples/src/ext4fs.rs#mount`

#### 5. 单方模块与降权项
- **网络协议栈**: 目标项目独有的 `kernel/src/net.rs` 模块，实现了 `connect`、`sendto` 等网络功能，是证明其独立开发的有力证据。
- **第三方库/公共依赖**: 参考项目包含 `dependency/rlibc/` 等第三方库，目标项目无此依赖。双方在此模块无任何共同指纹。
- **降权项说明**: 双方存在的同名函数（如 `alloc`, `frame_alloc`, `shutdown`）均为操作系统领域的通用术语或 POSIX 标准命名，其 AST 调用图谱完全不同，不能作为同源证据。跨模块的弱信号配对（如将目标项目的网络函数与参考项目的进程管理函数对比）因功能领域无关，已全部排除。

#### 6. 保守结论
综合客观 AST 证据，目标项目与参考项目的相似度（0.25%）处于独立开发的正常噪声范围。所有核心模块的共同唯一 AST 指纹均为 0，表明双方在内存管理、任务调度、文件系统等关键子系统上采用了完全不同的算法、数据结构和同步策略。目标项目独有的网络协议栈、自研 ext4 解析器以及双架构 HAL 设计，进一步印证了其独立的架构规划和实现路径。控方提出的“AST 级伪装重写”指控缺乏客观的、可量化的 AST 结构重合作为支撑，其证据多源于 POSIX 规范强制或 Rust 语言惯用法导致的表面相似性。因此，判定目标项目不存在对参考项目的实质性代码抄袭或同源性复用。

------------------------------

