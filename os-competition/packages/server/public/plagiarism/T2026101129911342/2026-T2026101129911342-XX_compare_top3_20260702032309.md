# 🛡️ OS 内核赛自动化代码级架构审计报告

**审计时间**: `2026-07-02 03:23:09`
**参赛作品名称**: `2026-T2026101129911342-XX`
**年份**: `2026`
**学校**: `XX`
**队伍名称**: `T2026101129911342`
**比赛名字**: `OS-Competition`
**比赛子赛道**: `Kernel-Implementation`
**仓库地址**: `https://gitlab.eduxiji.net/educg-group-43501-3132633/T2026101129911342-3041`
**推测开发语言**: `Rust / C (由AST探针自动识别)`
**特征函数量**: `193`

## ⚔️ 底层代码同源性对比 (AST Homology)
> 本部分由专门的代码审查智能体，对该项目与符合年份条件的历届最高相似度项目进行控制流与底层调用对比。新版报告会先给出确定性的全模块覆盖矩阵，再给出 Agent 的分模块解释，避免只围绕少量抽样函数展开。

### 🏆 Top 3 底层对标: vs `2021-啊普鲁派哒哒哒哒哒DROPDATABASEteams-哈尔滨工业大学深圳`（哈尔滨工业大学（深圳） / 啊普鲁派哒哒哒哒哒"; DROP DATABASE teams;）

#### 对方作品信息

**对方年份**: `2021`
**对方学校**: `哈尔滨工业大学（深圳）`
**对方队伍名称**: `啊普鲁派哒哒哒哒哒"; DROP DATABASE teams;`
**对方比赛名字**: `OS-Competition`
**对方比赛子赛道**: `Kernel-Implementation`
**对方仓库地址**: `https://gitlab.eduxiji.net/willson0v0/oscomp_handin`

> 双向覆盖率：共同 AST 指纹: 2；唯一 AST 指纹: 目标项目 186，参考项目 688；覆盖率: 目标项目 1.08%，参考项目 0.29%

## 全模块对比总览（确定性证据）

> 总体覆盖率：共同 AST 指纹: 2；唯一 AST 指纹: 目标项目 186，参考项目 688；覆盖率: 目标项目 1.08%，参考项目 0.29%

### 1. 模块覆盖矩阵
| 模块 | 目标函数数 | 参考函数数 | 共同唯一 AST 指纹 | Target 覆盖 | Base 覆盖 | 状态 |
| :--- | ---: | ---: | ---: | ---: | ---: | :--- |
| 进程管理 | 5 | 64 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |
| 任务调度/异步运行时 | 7 | 27 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |
| 内存管理 | 38 | 174 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |
| 中断/异常处理 | 8 | 4 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |
| 系统调用 | 16 | 16 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |
| 文件系统 | 54 | 478 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |
| 设备驱动 | 15 | 29 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |
| 网络协议栈 | 5 | 0 | 0 | 0.0% | 0.0% | 仅目标项目体现 |
| 文件系统/第三方库 | 14 | 0 | 0 | 0.0% | 0.0% | 仅目标项目体现 |
| 第三方库 | 0 | 1 | 0 | 0.0% | 0.0% | 仅参考项目体现 |
| 通用模块/其他 | 31 | 30 | 2 | 7.14% | 7.14% | 双方存在且有结构重合 |

### 2. 分模块证据清单

#### 进程管理
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `kernel/src/syscall.rs#sys_rt_sigprocmask` | `oshit_kernel/src/process/pcb.rs#default_sig_handlers` | 同模块上下文对齐/差异观察 | (oldset as *mut u64).write_volatile, (set as *const u64).read_volatile | 0.into, BTreeMap::new, SignalFlags::empty, VirtAddr::from, cont_va          .clone, dump_core_va     .clone ...(+5) |
| `khal/src/loongarch64.rs#exec_replace` | `oshit_kernel/src/syscall/process_syscall.rs#sys_waitpid` | 同模块上下文对齐/差异观察 | __la_enter_user, core::mem::size_of::<KStack> | Some, assert_eq!, child.get_inner_locked, child.get_pid, child_proc.get_inner_locked, child_proc.get_pid ...(+11) |
| `khal/src/riscv64.rs#exec_replace` | `oshit_kernel/src/syscall/process_syscall.rs#sys_kill` | 同模块上下文对齐/差异观察 | __enter_user, core::mem::size_of::<KStack> | PROCESS_MANAGER.lock, child.recv_signal, child.recv_signal(signal).is_some, current_process, current_process().unwrap, error! ...(+5) |
| `kernel/src/net.rs#cloexec` | `oshit_rootfs/proc0/src/main.rs#main` | 同模块上下文对齐/差异观察 | socks, socks().len | println!, prog.as_ptr, sys_exec, sys_exit, sys_fork, sys_waitpid ...(+1) |

#### 任务调度/异步运行时
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `kernel/src/syscall.rs#kunikos_syscall` | `oshit_kernel/src/fs/deprecated/mod.rs#fs_test` | 同模块上下文对齐/差异观察 | crate::fs::chdir, crate::fs::close, crate::fs::dup, crate::fs::dup3, crate::fs::fstat, crate::fs::ftruncate ...(+53) | "Hello, world".as_bytes, FILE::delete_dir, FILE::delete_dir("/new_dir").unwrap, FILE::delete_file, FILE::delete_file("/new_dir/file").unwrap, FILE::delete_file("/newfile").unwrap ...(+23) |
| `kernel/src/syscall.rs#sys_sysinfo` | `oshit_kernel/src/syscall/mod.rs#syscall` | 同模块上下文对齐/差异观察 | ((info + 104) as *mut u32).write_volatile, ((info + 80) as *mut u16).write_volatile, ((info + off) as *mut usize).write_volatile, core::ptr::write_bytes, khal::time_ns, w | VirtAddr, args[0].into, args[1].into, args[2].into, fatal!, sys_brk ...(+30) |
| `kernel/src/syscall.rs#sys_readlinkat` | `oshit_kernel/src/syscall/fs_syscall.rs#sys_unlink` | 同模块上下文对齐/差异观察 | ((buf + i) as *mut u8).write_volatile, core::cmp::min, link.iter, link.iter().take, link.iter().take(n).enumerate, link.len ...(+1) | arcpcb.files[dirfd as usize].clone, arcpcb.layout.get_user_cstr, arcpcb.path.clone, core::str::from_utf8, current_process, current_process().unwrap ...(+8) |
| `khal/src/loongarch64.rs#run_user` | `oshit_kernel/src/syscall/fs_syscall.rs#sys_mkdirat` | 同模块上下文对齐/差异观察 | KernelContext::default, __la_run_user, arm_timeout, core::mem::size_of::<KStack>, disarm_timeout, enable_timer_irq ...(+1) | arcpcb.files[dirfd as usize].clone, arcpcb.layout.get_user_cstr, arcpcb.path.clone, core::str::from_utf8, current_process, current_process().unwrap ...(+8) |

#### 内存管理
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `kernel/src/heap.rs#alloc` | `oshit_kernel/src/memory/frame_alloc.rs#alloc` | 同板块同名 | align_up, bnext, bsize, core::hint::spin_loop, core::ptr::null_mut, layout.size ...(+7) | Some, fatal!, self.freed.pop |
| `khal/src/riscv64.rs#map_page` | `oshit_kernel/src/memory/layout.rs#map_page` | 同板块同名 | frame_alloc, table.add | Err, Ok, PTEFlags::from_bits, PTEFlags::from_bits(self.segFlags.bits).unwrap, PhysPageNum, alloc_frame ...(+5) |
| `kernel/src/fs.rs#alloc_fd` | `oshit_kernel/src/process/pcb.rs#alloc_fd` | 同板块同名 | RLIM_NOFILE.min, f.is_none, fds, fds()[..lim].iter, fds()[..lim].iter().position | (0..self.files.len()).find, self.files.len, self.files.push, self.files[*i].is_none |
| `khal/src/riscv64.rs#load_elf_args` | `oshit_kernel/src/memory/layout.rs#new_elf` | 同模块上下文对齐/差异观察 | (random as *mut u8).add, (random as *mut u8).add(i).write_volatile, (va as *mut usize).write_volatile, argptr.iter, argptr.iter().take, argptr.iter().take(n).enumerate ...(+16) | Segment::new, Self::new, TRAMPOLINE.into, TRAP_CONTEXT.into, VMAFlags::empty, VirtAddr::from ...(+22) |

#### 中断/异常处理
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `khal/src/riscv64.rs#rv_user_syscall` | `oshit_kernel/src/trap/trap_handler.rs#init` | 同模块上下文对齐/差异观察 | asm!, crate::kprintln!, exit_user, kunikos_syscall, rdtime, rdtime().wrapping_add ...(+1) | debug!, info!, reset_timer_trigger, sie::set_stimer, stvec::write, verbose! |
| `khal/src/loongarch64.rs#la_user_syscall` | `oshit_kernel/src/trap/trap_handler.rs#kernel_trap` | 同模块上下文对齐/差异观察 | asm!, crate::kprintln!, exit_user, kunikos_syscall, rdtime_now | fatal!, panic! |
| `khal/src/riscv64.rs#rv_trap_handler` | `oshit_kernel/src/trap/trap_handler.rs#set_kernel_trap_entry` | 同模块上下文对齐/差异观察 | (tf.sepc as *const u16).read_volatile, asm!, crate::kprintln!, shutdown | stvec::write |
| `khal/src/loongarch64.rs#la_trap_handler` | `oshit_kernel/src/trap/trap_handler.rs#set_user_trap_entry` | 同模块上下文对齐/差异观察 | asm!, crate::kprintln!, shutdown | stvec::write |

#### 系统调用
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `kernel/src/syscall.rs#sys_uname` | `oshit_usrlib/src/syscalls.rs#sys_uname` | 同板块同名 | ((buf + i * 65 + j) as *mut u8).write_volatile, bs.len, f.iter, f.iter().enumerate, s.as_bytes | syscall |
| `kernel/src/syscall.rs#sys_wait4` | `oshit_usrlib/src/lib.rs#_start` | 同模块上下文对齐/差异观察 | (0..NZOMBIE).rev, (status as *mut i32).write_volatile, Some | ((argv + i * core::mem::size_of::<usize>()) as *const usize).read_volatile, ((envp + i * core::mem::size_of::<usize>()) as *const usize).read_volatile, ((str_start + *i) as *const u8).read_volatile, (0usize..).find, (0usize..).find(|i| unsafe {
            ((str_start + *i) as *const u8).read_volatile() == 0
        }).unwrap, HEAP.lock ...(+14) |
| `kernel/src/syscall.rs#sys_nanosleep` | `oshit_usrlib/src/syscalls.rs#syscall` | 同模块上下文对齐/差异观察 | ((req + 8) as *const u64).read_volatile, (req as *const u64).read_volatile, core::hint::spin_loop, khal::time_ns, khal::time_ns().wrapping_sub, sec.saturating_mul ...(+1) | asm! |
| `kernel/src/syscall.rs#sys_writev` | `oshit_usrlib/src/syscalls.rs#sys_exec` | 同模块上下文对齐/差异观察 | ((iov + i * 16 + 8) as *const usize).read_volatile, ((iov + i * 16) as *const usize).read_volatile, sys_write | args.as_ptr, envp.as_ptr, syscall |

#### 文件系统
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `kernel/src/fs.rs#write_file` | `oshit_kernel/src/fs/deprecated/file.rs#write_file` | 同板块同名 | (buf as *const u8).add, (buf as *const u8).add(i).read_volatile, (buf as *const u8).add(k).read_volatile, data.len, data.push, fds ...(+3) | Err, Ok, append_chain, buf.len, get_free_cluster, self.append_allowed ...(+6) |
| `kernel/src/fs.rs#mkdir` | `oshit_kernel/src/fs/fs_impl/fat32/file.rs#mkdir` | 同板块同名 | Node::dir, Vec::new, files, files().iter, files().iter().any, files().push ...(+2) | Err, Ok, parent.find_inode, parent.new_dir, parse_path, path.path.len ...(+7) |
| `kernel/src/virtio_blk.rs#init` | `oshit_kernel/src/drivers/sdcard.rs#get_csdregister` | 同模块上下文对齐/差异观察 | (!mask).wrapping_add, rd16, rd32, rd8, wr16, wr32 ...(+1) | Err, Ok, self.end_cmd, self.get_response, self.read_data, self.send_cmd ...(+2) |
| `kernel/src/syscall.rs#sys_clone` | `oshit_kernel/src/fs/deprecated/path.rs#read` | 同模块上下文对齐/差异观察 | (ctid as *mut u32).write_volatile, crate::fs::fork_pop, crate::fs::fork_push, khal::spawn_child, z.len | Err, Some, String::from, String::with_capacity, result.clone, self.buf.clone ...(+6) |

#### 设备驱动
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `kernel/src/virtio_blk.rs#read` | `oshit_kernel/src/drivers/sdcard.rs#read_sector` | 同模块上下文对齐/差异观察 | ai.wrapping_add, buf.as_mut_ptr, buf.len, core::ptr::addr_of!, core::ptr::read_volatile, fence ...(+2) | Err, Ok, chunk.iter_mut, chunk.iter_mut().zip, data_buf.chunks_mut, data_buf.len ...(+5) |
| `khal/src/loongarch64.rs#pci_scan` | `oshit_kernel/src/drivers/sdcard.rs#write_sector` | 同模块上下文对齐/差异观察 | core::ptr::read_volatile, crate::kprintln! | (data_buf.len() / SEC_LEN).try_into, (data_buf.len() / SEC_LEN).try_into().unwrap, Err, Ok, chunk.iter, data_buf.chunks ...(+8) |
| `kernel/src/virtio_blk.rs#init` | `oshit_kernel/src/drivers/sdcard.rs#init` | 同模块上下文对齐/差异观察 | r, setup | Err, InitError::CMDFailed, self.CS_HIGH, self.HIGH_SPEED_ENABLE, self.end_cmd, self.get_cardinfo ...(+6) |
| `kernel/src/virtio_blk.rs#w` | `oshit_kernel/src/drivers/sdcard.rs#get_cidregister` | 同模块上下文对齐/差异观察 | ((base + off) as *mut u32).write_volatile | Err, Ok, self.end_cmd, self.get_response, self.read_data, self.send_cmd ...(+2) |

#### 网络协议栈
仅目标项目中观察到该模块代表函数，参考项目没有同模块可对齐证据：
- 目标项目：`kernel/src/net.rs#connect`，调用摘要：Vec::new, parse_port, socks, socks().iter, socks().iter().position, socks().len ...(+2)
- 目标项目：`kernel/src/net.rs#sendto`，调用摘要：Vec::with_capacity, d.push, parse_port, socks, socks().iter, socks().iter().position ...(+1)
- 目标项目：`kernel/src/net.rs#parse_port`，调用摘要：内部闭环逻辑
- 目标项目：`kernel/src/net.rs#socks`，调用摘要：内部闭环逻辑

#### 文件系统/第三方库
仅目标项目中观察到该模块代表函数，参考项目没有同模块可对齐证据：
- 目标项目：`kernel/src/ext4.rs#extent_runs`，调用摘要：Vec::new, alloc::vec!, blk.to_vec, out.push, rd16, rd32 ...(+3)
- 目标项目：`kernel/src/ext4.rs#mount`，调用摘要：(num_groups * desc_size).div_ceil, Some, alloc::vec!, bgd[i * BS..(i + 1) * BS].copy_from_slice, rd16, rd32 ...(+2)
- 目标项目：`kernel/src/ext4.rs#read_file`，调用摘要：alloc::vec!, core::cmp::min, data.len, data.truncate, rd32, self.extent_runs ...(+2)
- 目标项目：`kernel/src/ext4.rs#list_root`，调用摘要：String::from, Vec::new, core::str::from_utf8, out.push, rd16, rd32 ...(+3)

#### 第三方库
仅参考项目中观察到该模块代表函数，目标项目没有同模块可对齐证据：
- 参考项目：`oshit_kernel/src/sbi/sbi_funcs.rs#get_vendor_id`，调用摘要：sbi_call_all

#### 通用模块/其他
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `khal/src/riscv64.rs#sbi_call` | `oshit_kernel/src/sbi/sbi_funcs.rs#sbi_call` | 同板块同名 | asm! | asm! |
| `khal/src/loongarch64.rs#shutdown` | `oshit_kernel/src/sbi/sbi_funcs.rs#shutdown` | 同板块同名 | asm!, core::ptr::write_volatile | sbi_call, unreachable! |
| `khal/src/lib.rs#write_str` | `oshit_kernel/src/sbi/primitive_io.rs#write_str` | 同板块同名同结构 | Ok, console_write | Ok, puts |
| `khal/src/riscv64.rs#set_timer` | `oshit_kernel/src/sbi/sbi_funcs.rs#set_timer` | 同板块同名 | sbi_call | sbi_call |


### 1. 宏观判定
- **定性结论**: 独立开发 (相似度: 0.23%，共同 AST 指纹: 2；唯一 AST 指纹: 目标项目 186，参考项目 688；覆盖率: 目标项目 1.08%，参考项目 0.29%)

### 2. 全模块覆盖矩阵解读
- **进程管理**: 双方均存在该模块。共同 AST 指纹为 0，目标覆盖率 0.0%，参考覆盖率 0.0%。双方在进程控制块、信号处理、执行替换等核心实现上无任何底层结构重合，证据强度为**未形成**。
- **任务调度/异步运行时**: 双方均存在该模块。共同 AST 指纹为 0，目标覆盖率 0.0%，参考覆盖率 0.0%。系统调用分发、上下文切换等关键路径的调用图谱完全不同，证据强度为**未形成**。
- **内存管理**: 双方均存在该模块。共同 AST 指纹为 0，目标覆盖率 0.0%，参考覆盖率 0.0%。尽管存在 `alloc`、`map_page` 等同名函数，但其底层调用、数据结构与抽象层级均显著不同，证据强度为**未形成**。
- **中断/异常处理**: 双方均存在该模块。共同 AST 指纹为 0，目标覆盖率 0.0%，参考覆盖率 0.0%。陷阱处理入口、上下文保存与恢复逻辑无共同指纹，证据强度为**未形成**。
- **系统调用**: 双方均存在该模块。共同 AST 指纹为 0，目标覆盖率 0.0%，参考覆盖率 0.0%。系统调用分发器与具体实现函数无底层结构重合，证据强度为**未形成**。
- **文件系统**: 双方均存在该模块。共同 AST 指纹为 0，目标覆盖率 0.0%，参考覆盖率 0.0%。目标项目采用 ext4，参考项目采用 FAT32，子系统选型与实现完全不同，证据强度为**未形成**。
- **设备驱动**: 双方均存在该模块。共同 AST 指纹为 0，目标覆盖率 0.0%，参考覆盖率 0.0%。目标项目为 virtio-blk 驱动，参考项目为 SD 卡驱动，硬件与驱动模型均不同，证据强度为**未形成**。
- **网络协议栈**: 仅目标项目存在，参考项目无此模块。属于单方独有特征，证据强度为**未形成**。
- **第三方库/公共依赖**: 目标项目独有 ext4 等第三方库模块，参考项目独有 sbi 封装模块。双方在通用模块/其他中存在 2 个共同 AST 指纹，对应 `sbi_call`、`shutdown` 等 RISC-V SBI 标准包装函数，属于硬件规范强制要求的共性代码，证据强度为**弱**。

### 3. 底层 AST 关键证据指纹表
| 所属板块 | 目标项目路径 | 参考项目路径 | 证据类型 | 技术说明 |
| :--- | :--- | :--- | :--- | :--- |
| 通用模块/其他 | khal/src/riscv64.rs#sbi_call | oshit_kernel/src/sbi/sbi_funcs.rs#sbi_call | 同板块同名 | 均为 RISC-V SBI 标准调用封装，使用 `asm!` 宏，属于硬件规范强制要求的共性代码。 |
| 通用模块/其他 | khal/src/loongarch64.rs#shutdown | oshit_kernel/src/sbi/sbi_funcs.rs#shutdown | 同板块同名 | 目标为 LoongArch 的 `asm!` 关机实现，参考为 RISC-V 的 `sbi_call` 关机，仅功能语义相同，底层指令集不同。 |
| 内存管理 | kernel/src/heap.rs#alloc | oshit_kernel/src/memory/frame_alloc.rs#alloc | 同板块同名 | 目标为带锁的堆分配器 (`compare_exchange`)，参考为帧分配器 (`self.freed.pop`)，算法与数据结构完全不同。 |
| 内存管理 | khal/src/riscv64.rs#map_page | oshit_kernel/src/memory/layout.rs#map_page | 同板块同名 | 目标为 HAL 层直接页表操作 (`table.add`)，参考为高层内存布局管理 (`pagetable.map`)，抽象层级不同。 |
| 内存管理 | kernel/src/fs.rs#alloc_fd | oshit_kernel/src/process/pcb.rs#alloc_fd | 同板块同名 | 目标使用 `fds()[..lim].iter().position`，参考使用 `(0..self.files.len()).find`，实现细节与数据结构不同。 |
| 任务调度/异步运行时 | kernel/src/syscall.rs#kunikos_syscall | oshit_kernel/src/syscall/mod.rs#syscall | 同模块上下文对齐/差异观察 | 均为系统调用分发器，但目标直接调用 `crate::fs::*`，参考通过 `args[]` 转换后调用 `sys_*`，调用图谱无交集。 |
| 进程管理 | khal/src/riscv64.rs#exec_replace | oshit_kernel/src/process/pcb.rs#exec | 同模块上下文对齐/差异观察 | 目标为架构相关进入用户态汇编入口，参考为高层 `exec` 流程，处于不同抽象层。 |

### 4. 核心代码调用流对比剖析
* **【内存管理/页表映射】**
* **底层技术析理**: 目标项目的 `map_page` 位于硬件抽象层 (`khal`)，直接操作页表项 (`table.add`)，调用 `frame_alloc` 分配物理帧。参考项目的 `map_page` 位于高层内存布局管理 (`MemLayout`)，通过 `pagetable.map` 进行映射，并维护一个 `frames` 集合来追踪已分配的物理页。两者在抽象层次、数据结构和调用链上存在根本差异，目标项目更贴近硬件，参考项目则封装了更复杂的管理逻辑。
* **代码溯源链接**:
  - 目标项目: `khal/src/riscv64.rs#map_page`
  - 参考项目: `oshit_kernel/src/memory/layout.rs#map_page`

* **【任务调度/异步运行时/系统调用分发】**
* **底层技术析理**: 目标项目的 `kunikos_syscall` 与参考项目的 `syscall` 均为系统调用入口。然而，目标项目的分发器直接调用文件系统等子系统的内部函数（如 `crate::fs::chdir`），呈现为扁平化的巨型 `match` 结构。参考项目的分发器则先将参数从 `args` 数组中转换出来，再调用对应的 `sys_*` 处理函数。两者的参数传递方式和内部调用路径完全不同，不存在控制流同构。
* **代码溯源链接**:
  - 目标项目: `kernel/src/syscall.rs#kunikos_syscall`
  - 参考项目: `oshit_kernel/src/syscall/mod.rs#syscall`

* **【进程管理/执行替换】**
* **底层技术析理**: 目标项目的 `exec_replace` 是架构相关的底层入口例程，直接调用 `__enter_user` (RISC-V) 或 `__la_enter_user` (LoongArch) 汇编符号进入用户态，并关注 `KStack` 的大小。参考项目的 `exec` 是高层系统调用处理函数，负责重置内存布局 (`KERNEL_MEM_LAYOUT`)、初始化陷阱上下文 (`TrapContext::init`) 等复杂流程。一个操作在 HAL 层，一个在系统调用层，调用图谱无任何交集。
* **代码溯源链接**:
  - 目标项目: `khal/src/riscv64.rs#exec_replace`
  - 参考项目: `oshit_kernel/src/process/pcb.rs#exec`

### 5. 单方模块与降权项
- **网络协议栈**: 目标项目独有 `kernel/src/net.rs` 模块，包含 `connect`、`sendto` 等函数，参考项目无此模块。此为独立开发的有力证据。
- **文件系统/第三方库**: 目标项目独有 `kernel/src/ext4.rs` 模块，实现了完整的 ext4 文件系统解析。参考项目使用 FAT32 文件系统。子系统选型完全不同。
- **第三方库**: 参考项目独有 `oshit_kernel/src/sbi/sbi_funcs.rs` 等 SBI 调用封装，目标项目无此独立模块。
- **通用模块/其他中的 2 个共同指纹**: `sbi_call`、`shutdown`、`set_timer` 等函数属于 RISC-V 特权架构规范定义的标准接口，任何运行在 RISC-V 平台上的内核都必须实现，属于硬件强制要求的共性代码，不构成可保护的独创性表达，已降权为弱信号。

### 6. 保守结论
本次审计基于 AST 指纹的客观比对，目标项目与参考项目的整体相似度仅为 0.23%，在内存管理、任务调度、进程管理等核心板块的共同指纹均为 0。控方所指控的“同构”均源于对同名函数和操作系统共性范式的误读，而非底层代码结构的重合。目标项目展现出的多架构 HAL 设计、独立的 ext4 文件系统与网络协议栈实现，以及与参考项目截然不同的编程风格和抽象层级，均为独立开发的强有力证据。通用模块中极少量重合源于 RISC-V 硬件规范，不构成抄袭。综上，目标项目不存在对参考项目的实质性代码复用，风险极低。

------------------------------

