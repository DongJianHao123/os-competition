# 🛡️ OS 内核赛自动化代码级架构审计报告

**审计时间**: `2026-07-02 11:18:51`
**参赛作品名称**: `2026-T202610486999572-XX`
**年份**: `2026`
**学校**: `XX`
**队伍名称**: `T202610486999572`
**比赛名字**: `OS-Competition`
**比赛子赛道**: `Kernel-Implementation`
**仓库地址**: `https://gitlab.eduxiji.net/educg-group-43501-3132633/T202610486999572-3522`
**推测开发语言**: `Rust / C (由AST探针自动识别)`
**特征函数量**: `4002`

## ⚔️ 底层代码同源性对比 (AST Homology)
> 本部分由专门的代码审查智能体，对该项目与符合年份条件的历届最高相似度项目进行控制流与底层调用对比。新版报告会先给出确定性的全模块覆盖矩阵，再给出 Agent 的分模块解释，避免只围绕少量抽样函数展开。

### 🏆 Top 4 底层对标: vs `2025-undefined-清华大学`（清华大学 / undefined）

#### 对方作品信息

**对方年份**: `2025`
**对方学校**: `清华大学`
**对方队伍名称**: `undefined`
**对方比赛名字**: `OS-Competition`
**对方比赛子赛道**: `Kernel-Implementation`
**对方仓库地址**: `https://gitlab.eduxiji.net/educg-group-36002-2710490/T202510003995291-2331`

> 双向覆盖率：共同 AST 指纹: 1140；唯一 AST 指纹: 目标项目 3261，参考项目 2820；覆盖率: 目标项目 34.96%，参考项目 40.43%

## 全模块对比总览（确定性证据）

> 总体覆盖率：共同 AST 指纹: 1140；唯一 AST 指纹: 目标项目 3261，参考项目 2820；覆盖率: 目标项目 34.96%，参考项目 40.43%

### 1. 模块覆盖矩阵
| 模块 | 目标函数数 | 参考函数数 | 共同唯一 AST 指纹 | Target 覆盖 | Base 覆盖 | 状态 |
| :--- | ---: | ---: | ---: | ---: | ---: | :--- |
| 进程管理 | 224 | 135 | 29 | 14.72% | 23.97% | 双方存在且有结构重合 |
| 任务调度/异步运行时 | 737 | 415 | 165 | 27.27% | 55.0% | 双方存在且有结构重合 |
| 内存管理 | 510 | 450 | 155 | 32.22% | 40.58% | 双方存在且有结构重合 |
| 中断/异常处理 | 58 | 53 | 34 | 62.96% | 72.34% | 双方存在且有结构重合 |
| 系统调用 | 242 | 109 | 60 | 29.85% | 69.77% | 双方存在且有结构重合 |
| 文件系统 | 1067 | 999 | 140 | 16.3% | 20.96% | 双方存在且有结构重合 |
| 设备驱动 | 168 | 133 | 112 | 78.32% | 91.8% | 双方存在且有结构重合 |
| 网络协议栈 | 90 | 65 | 42 | 53.85% | 76.36% | 双方存在且有结构重合 |
| 文件系统/第三方库 | 52 | 643 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |
| 网络协议栈/第三方库 | 131 | 130 | 49 | 42.98% | 42.24% | 双方存在且有结构重合 |
| 通用模块/其他 | 723 | 555 | 373 | 67.82% | 85.35% | 双方存在且有结构重合 |

### 2. 分模块证据清单

#### 进程管理
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `kernel/starry-next/src/syscall_imp/task/thread.rs#sys_wait4` | `api/src/imp/task/wait.rs#sys_wait4` | 同板块同名 | syscall_body! | Err, Ok, WaitOptions::from_bits_truncate, WaitPid::Pgid, WaitPid::Pid, child.get_exit_code ...(+19) |
| `kernel/starry-next/src/syscall_imp/task/thread.rs#sys_execve` | `api/src/interface/task/execve.rs#sys_execve` | 同板块同名 | syscall_body! | BUSYBOX.to_string, Some, abs_path.to_string, args[1..].iter, args[1..].iter().cloned, argv.clone ...(+10) |
| `kernel/starry-next/arceos/modules/axtask/src/run_queue.rs#exit_current` | `arceos/modules/axtask/src/run_queue.rs#exit_current` | 同板块同名 | EXITED_TASKS.current_ref_mut_raw, EXITED_TASKS.current_ref_mut_raw().clear, EXITED_TASKS.current_ref_mut_raw().push_back, WAIT_FOR_EXIT.current_ref_mut_raw, WAIT_FOR_EXIT.current_ref_mut_raw().notify_one, assert! ...(+9) | EXITED_TASKS.current_ref_mut_raw, EXITED_TASKS.current_ref_mut_raw().clear, EXITED_TASKS.current_ref_mut_raw().push_back, WAIT_FOR_EXIT.current_ref_mut_raw, WAIT_FOR_EXIT.current_ref_mut_raw().notify_one, assert! ...(+8) |
| `kernel/starry-next/src/syscall_imp/task/thread.rs#sys_exit_group` | `api/src/interface/task/exit.rs#sys_exit_group` | 同板块同名 | crate::task::exit_current_task, crate::task::terminate_other_threads_in_group, crate::task::wait_for_other_threads_in_group_to_exit, crate::task::wait_status_exited, curr.id, curr.id().as_u64 ...(+10) | sys_exit_impl |

#### 任务调度/异步运行时
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `kernel/starry-next/arceos/ulib/axlibc/c/printf.c#format_string_loop` | `arceos/ulib/axlibc/c/printf.c#format_string_loop` | 同板块同名同结构 | ABS_FOR_PRINTING, ADVANCE_IN_FORMAT_STRING, atou_, is_digit_, out_rev_, parse_flags ...(+5) | ABS_FOR_PRINTING, ADVANCE_IN_FORMAT_STRING, atou_, is_digit_, out_rev_, parse_flags ...(+5) |
| `kernel/starry-next/src/syscall_imp/task/thread.rs#sys_kill` | `api/src/imp/task/signal.rs#sys_kill` | 同板块同名 | syscall_body! | Err, Ok, current_process, current_process().get_group, debug!, get_all_processes ...(+8) |
| `kernel/starry-next/src/syscall_imp/task/schedule.rs#sys_clock_nanosleep` | `api/src/imp/task/schedule.rs#sys_clock_nanosleep` | 同板块同名 | syscall_body! | Err, debug!, remain.clone, requested.clone, sys_nanosleep |
| `kernel/starry-next/src/syscall_imp/task/thread.rs#sys_clone` | `api/src/interface/task/clone.rs#sys_clone` | 同板块同名 | syscall_body! | addr_child_tid.clone, addr_parent_tid.clone, sys_clone_ |

#### 内存管理
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `kernel/starry-next/src/syscall_imp/mod.rs#handle_syscall` | `src/syscall.rs#handle_syscall` | 同板块同名 | LinuxError::ENOSYS.code, Sysno::new, axtask::current, curr.id, curr.id().as_u64, curr.name ...(+239) | Err, Sysno::new, err.code, info!, result.unwrap_or_else, set_trap_frame ...(+151) |
| `kernel/starry-next/src/mm.rs#handle_page_fault` | `arceos/modules/axmm/src/aspace.rs#handle_page_fault` | 同板块同名 | (slot as *mut u64).cast::<u8>, Some, VirtAddr::from_usize, VirtAddr::from_usize(trap.get_ip()).align_down_4k, access_flags.contains, alloc::string::String::new ...(+60) | Self::handle_cow_fault, access_flags.contains, area
                    .backend, area
                    .backend()
                    .handle_page_fault, area.flags, orig_flags.contains ...(+3) |
| `kernel/starry-next/src/syscall_imp/mm/mmap.rs#sys_mmap` | `api/src/imp/mm/mmap.rs#sys_mmap` | 同板块同名 | syscall_body! | Err, File::from_fd, MmapFlags::from_bits_truncate, MmapProt::from_bits_truncate, Ok, PhysAddr::from ...(+34) |
| `kernel/starry-next/src/mm.rs#map_elf` | `core/src/mm.rs#map_elf` | 同板块同名 | ELFParser::new, ELFParser::new(
            &interp_elf,
            axconfig::plat::USER_INTERP_BASE,
            None,
            uspace_base,
        )
        .map_err, ElfFile::new, ElfFile::new(interp_data.as_slice()).map_err, Ok, auxv
        .iter_mut ...(+44) | ELFParser::new, ELFParser::new(
        elf,
        axconfig::plat::USER_INTERP_BASE,
        Some(uspace_base as isize),
        uspace_base,
    )
    .map_err, Ok, Some, assert_eq!, debug! ...(+12) |

#### 中断/异常处理
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `kernel/starry-next/arceos/ulib/axlibc/c/pow.c#exp_inline` | `arceos/ulib/axlibc/c/pow.c#exp_inline` | 同板块同名同结构 | __math_oflow, __math_uflow, asdouble, asuint64, converttoint, eval_as_double ...(+4) | __math_oflow, __math_uflow, asdouble, asuint64, converttoint, eval_as_double ...(+4) |
| `kernel/starry-next/arceos/modules/axhal/src/arch/aarch64/trap.rs#handle_sync_exception` | `arceos/modules/axhal/src/arch/aarch64/trap.rs#handle_sync_exception` | 同板块同名 | ESR_EL1.extract, crate::trap::handle_syscall, debug!, esr.read, esr.read_as_enum, handle_data_abort ...(+2) | ESR_EL1.extract, crate::trap::handle_syscall, crate::trap::post_trap_callback, debug!, esr.read, esr.read_as_enum ...(+6) |
| `kernel/starry-next/arceos/modules/axhal/src/platform/loongarch64_qemu_virt/irq.rs#register_handler` | `arceos/modules/axhal/src/platform/riscv64_qemu_virt/irq.rs#register_handler` | 同板块同名 | with_cause! | with_cause! |
| `kernel/starry-next/arceos/modules/axhal/src/platform/riscv64_qemu_virt/irq.rs#register_handler` | `arceos/modules/axhal/src/platform/loongarch64_qemu_virt/irq.rs#register_handler` | 同板块同名 | with_cause! | crate::irq::register_handler_common |

#### 系统调用
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `kernel/starry-next/arceos/api/arceos_posix_api/src/imp/net.rs#sys_getaddrinfo` | `api/src/imp/net/socket.rs#sys_getaddrinfo` | 同板块同名 | char_ptr_to_str, debug!, syscall_body! | Err, Ok, SocketAddrV4::new, SocketAddrV4::new(ip, port).into, Vec::with_capacity, axnet::dns_query ...(+21) |
| `kernel/starry-next/arceos/api/arceos_posix_api/src/imp/net.rs#sys_setsockopt` | `api/src/imp/net/socket.rs#sys_setsockopt` | 同板块同名 | debug!, syscall_body! | Err, Ok, Socket::from_fd, Socket::from_fd(sockfd)?.setsockopt, core::slice::from_raw_parts, debug! ...(+1) |
| `kernel/starry-next/src/syscall_imp/utils/misc.rs#sys_recvfrom` | `api/src/imp/net/socket.rs#sys_recvfrom` | 同板块同名 | addr.cast::<c_void>, addr.is_null, addrlen.cast::<c_void>, addrlen.cast_const, addrlen.is_null, arceos_posix_api::sys_recvfrom ...(+8) | Err, Ok, Socket::from_fd, addrlen.is_null, buf_ptr.is_null, core::slice::from_raw_parts_mut ...(+4) |
| `kernel/starry-next/apps/nimbos/rust/src/arch/riscv.rs#sys_clone` | `apps/nimbos/rust/src/arch/riscv.rs#sys_clone` | 同板块同名同结构 | naked_asm! | naked_asm! |

#### 文件系统
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `kernel/starry-next/arceos/ulib/axlibc/c/glob.c#do_glob` | `arceos/ulib/axlibc/c/glob.c#do_glob` | 同板块同名同结构 | S_ISDIR, append, closedir, do_glob, errfunc, fnmatch ...(+7) | S_ISDIR, append, closedir, do_glob, errfunc, fnmatch ...(+7) |
| `kernel/starry-next/src/syscall_imp/fs/ctl.rs#sys_utimensat` | `api/src/imp/fs/ctl.rs#sys_utimensat` | 同板块同名 | syscall_body! | Default::default, Duration::from, Ok, ResolveFlags::from_bits_truncate, Some, into_duration ...(+8) |
| `kernel/starry-next/arceos/ulib/axlibc/src/setjmp.rs#longjmp` | `arceos/ulib/axlibc/src/setjmp.rs#longjmp` | 同板块同名 | core::arch::naked_asm! | core::arch::naked_asm! |
| `kernel/starry-next/arceos/ulib/axlibc/src/setjmp.rs#setjmp` | `arceos/ulib/axlibc/src/setjmp.rs#setjmp` | 同板块同名 | core::arch::naked_asm! | core::arch::naked_asm! |

#### 设备驱动
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `kernel/starry-next/arceos/tools/bwbench_client/src/main.rs#transmit_benchmark` | `arceos/tools/bwbench_client/src/main.rs#transmit_benchmark` | 同板块同名同结构 | Local::now, NetDevice::new, NetDevice::new(interface).unwrap, current_time.signed_duration_since, current_time.signed_duration_since(past_time).num_seconds, dev.send ...(+2) | Local::now, NetDevice::new, NetDevice::new(interface).unwrap, current_time.signed_duration_since, current_time.signed_duration_since(past_time).num_seconds, dev.send ...(+2) |
| `kernel/starry-next/arceos/tools/bwbench_client/src/device.rs#bind_interface` | `arceos/tools/bwbench_client/src/device.rs#bind_interface` | 同板块同名同结构 | ETH_P_ALL.to_be, Err, Ok, libc::bind, self.ifreq.ioctl, std::io::Error::last_os_error ...(+2) | ETH_P_ALL.to_be, Err, Ok, libc::bind, self.ifreq.ioctl, std::io::Error::last_os_error ...(+2) |
| `kernel/starry-next/arceos/modules/axdriver/src/lib.rs#init_drivers` | `arceos/modules/axdriver/src/lib.rs#init_drivers` | 同板块同名同结构 | AllDevices::default, all_devs.block.iter, all_devs.block.iter().enumerate, all_devs.display.iter, all_devs.display.iter().enumerate, all_devs.net.iter ...(+5) | AllDevices::default, all_devs.block.iter, all_devs.block.iter().enumerate, all_devs.display.iter, all_devs.display.iter().enumerate, all_devs.net.iter ...(+5) |
| `kernel/starry-next/arceos/modules/axdriver/src/virtio.rs#probe_pci` | `arceos/modules/axdriver/src/virtio.rs#probe_pci` | 同板块同名同结构 | D::try_new, Some, axdriver_virtio::probe_pci_device::<VirtIoHalImpl>, warn! | D::try_new, Some, axdriver_virtio::probe_pci_device::<VirtIoHalImpl>, warn! |

#### 网络协议栈
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `kernel/starry-next/arceos/ulib/axlibc/c/network.c#inet_pton` | `arceos/ulib/axlibc/c/network.c#inet_pton` | 同板块同名同结构 | hexval, inet_pton, isdigit, memmove | hexval, inet_pton, isdigit, memmove |
| `kernel/starry-next/arceos/api/arceos_posix_api/src/imp/net.rs#shutdown` | `api/src/imp/net/socket.rs#shutdown` | 同板块同名 | Err, Ok, self.shutdown_state
                    .store, self.shutdown_state.fetch_or, socket.shutdown, tcpsocket.peer_addr ...(+4) | Ok, tcpsocket.lock, tcpsocket.peer_addr, tcpsocket.shutdown, udpsocket.lock, udpsocket.peer_addr ...(+1) |
| `kernel/starry-next/arceos/api/arceos_posix_api/src/imp/net.rs#accept` | `api/src/imp/net/socket.rs#accept` | 同板块同名 | Err, Ok, drop, pending.is_empty, pending.pop_front, self.accept_wait
                .wait_until ...(+2) | Err, Ok, tcpsocket.lock, tcpsocket.lock().accept |
| `kernel/starry-next/arceos/api/arceos_posix_api/src/imp/net.rs#send` | `api/src/imp/net/socket.rs#send` | 同板块同名 | Err, endpoint.write, self
                    .inner
                    .lock, self
                    .inner
                    .lock()
                    .peer_addr
                    .clone, self
                    .inner
                    .lock()
                    .peer_addr
                    .clone()
                    .ok_or, self.inner.lock ...(+1) | Ok, tcpsocket.lock, tcpsocket.lock().send, udpsocket.lock, udpsocket.lock().send |

#### 文件系统/第三方库
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `kernel/starry-next/arceos/modules/axfs/src/fs/lwext4_rust.rs#create` | `arceos/modules/axfs-ng/src/fs/ext4/inode.rs#create` | 同板块同名 | CString::new, CString::new(fpath).map_err, Err, Ok, Self::invalidate_shared_dir_entries, Self::invalidate_shared_nodes ...(+21) | DirEntry::new_dir, DirEntry::new_file, DirNode::new, Err, FileNode::new, Inode::new ...(+11) |
| `kernel/starry-next/arceos/modules/axfs/src/fs/lwext4_rust.rs#lookup` | `modules/lwext4_rust/src/inode/dir.rs#lookup` | 同板块同名 | Err, Ok, Self::cached_node, debug!, file.check_inode_exist, fpath.as_str ...(+4) | Ok, ext4_dir_find_entry, ext4_dir_find_entry(
                &mut result,
                self.inner.as_mut(),
                name.as_ptr() as *const _,
                name.len() as _,
            )
            .context, mem::zeroed, name.as_ptr, name.len ...(+1) |
| `kernel/starry-next/arceos/modules/axfs/src/fs/lwext4_rust.rs#get_attr` | `modules/lwext4_rust/src/inode/attr.rs#get_attr` | 同板块同名 | Ok, VfsNodeAttr::new, VfsNodePerm::from_bits_truncate, file.ensure_open_for_read, file.ensure_open_for_read(path.as_str())
                .map_err, file.file_close ...(+13) | decode_time, ext4_inode_get_blocks_count, get_block_size, self.gid, self.inode_type, self.mode ...(+6) |
| `kernel/starry-next/arceos/modules/axfs/src/fs/lwext4_rust.rs#read_dir` | `arceos/modules/axfs-ng/src/fs/ext4/inode.rs#read_dir` | 同板块同名 | Ok, VfsDirEntry::new, core::str::from_utf8, core::str::from_utf8(name).unwrap, dirents.iter_mut, dirents.iter_mut().zip ...(+9) | Ok, core::str::from_utf8, core::str::from_utf8(entry.name())
                .map_err, core::str::from_utf8(entry.name())
                .map_err(|_| VfsError::EINVAL)?
                .to_owned, entry.ino, entry.inode_type ...(+10) |

#### 网络协议栈/第三方库
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `kernel/starry-next/arceos/modules/axnet/src/smoltcp_impl/dns.rs#query` | `arceos/modules/axnet/src/smoltcp_impl/dns.rs#query` | 同板块同名 | Err, Ok, SOCKET_SET
            .with_socket_mut::<dns::Socket, _, _>, SOCKET_SET
            .with_socket_mut::<dns::Socket, _, _>(handle, |socket| {
                socket.start_query(iface.lock().context(), name, query_type)
            })
            .map_err, SOCKET_SET.poll_interfaces, SOCKET_SET.with_socket_mut::<dns::Socket, _, _> ...(+12) | Err, Ok, SOCKET_SET
            .with_socket_mut::<dns::Socket, _, _>, SOCKET_SET
            .with_socket_mut::<dns::Socket, _, _>(handle, |socket| {
                socket.start_query(iface.lock().context(), name, query_type)
            })
            .map_err, SOCKET_SET.poll_interfaces, SOCKET_SET.with_socket_mut::<dns::Socket, _, _> ...(+12) |
| `kernel/starry-next/arceos/modules/axnet/src/smoltcp_impl/tcp.rs#poll_connect` | `arceos/modules/axnet/src/smoltcp_impl/tcp.rs#poll_connect` | 同板块同名 | Ok, SOCKET_SET.with_socket::<tcp::Socket, _, _>, debug!, self.handle.get, self.handle.get().read, self.handle.get().read().unwrap ...(+7) | Ok, SOCKET_SET.with_socket::<tcp::Socket, _, _>, debug!, self.handle.get, self.handle.get().read, self.handle.get().read().unwrap ...(+6) |
| `kernel/starry-next/arceos/modules/axnet/src/smoltcp_impl/tcp.rs#shutdown` | `arceos/modules/axnet/src/smoltcp_impl/tcp.rs#shutdown` | 同板块同名 | LISTEN_TABLE.unlisten, Ok, SOCKET_SET.poll_interfaces, SOCKET_SET.with_socket_mut::<tcp::Socket, _, _>, debug!, self.handle.get ...(+10) | LISTEN_TABLE.unlisten, Ok, SOCKET_SET.poll_interfaces, SOCKET_SET.with_socket_mut::<tcp::Socket, _, _>, debug!, self.handle.get ...(+9) |
| `kernel/starry-next/arceos/modules/axnet/src/smoltcp_impl/udp.rs#send_impl` | `arceos/modules/axnet/src/smoltcp_impl/udp.rs#send_impl` | 同板块同名 | Err, Ok, SOCKET_SET.poll_interfaces, SOCKET_SET.with_socket_mut::<udp::Socket, _, _>, ax_err!, ax_err_type! ...(+12) | Err, Ok, SOCKET_SET.with_socket_mut::<udp::Socket, _, _>, ax_err!, ax_err_type!, buf.len ...(+7) |

#### 通用模块/其他
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `kernel/starry-next/arceos/ulib/axlibc/c/printf.c#print_exponential_number` | `arceos/ulib/axlibc/c/printf.c#print_exponential_number` | 同板块同名同结构 | ABS_FOR_PRINTING, PRINTF_ABS, bastardized_floor, get_components, get_normalized_components, get_sign_bit ...(+5) | ABS_FOR_PRINTING, PRINTF_ABS, bastardized_floor, get_components, get_normalized_components, get_sign_bit ...(+5) |
| `kernel/starry-next/arceos/ulib/axlibc/c/pow.c#pow` | `arceos/ulib/axlibc/c/pow.c#pow` | 同板块同名同结构 | __builtin_fma, __math_invalid, __math_oflow, __math_uflow, asdouble, asuint64 ...(+8) | __builtin_fma, __math_invalid, __math_oflow, __math_uflow, asdouble, asuint64 ...(+8) |
| `kernel/starry-next/arceos/ulib/axlibc/c/string.c#twoway_strstr` | `arceos/ulib/axlibc/c/string.c#twoway_strstr` | 同板块同名同结构 | BITOP, MAX, memchr, memcmp | BITOP, MAX, memchr, memcmp |
| `kernel/starry-next/arceos/ulib/axlibc/c/printf.c#print_broken_up_decimal` | `arceos/ulib/axlibc/c/printf.c#print_broken_up_decimal` | 同板块同名同结构 | out_rev_ | out_rev_ |


#### 1. 宏观判定
- **定性结论**: 独立开发，底层架构存在显著差异 (相似度: 23.07%，共同 AST 指纹: 1140；唯一 AST 指纹: 目标项目 3261，参考项目 2820；覆盖率: 目标项目 34.96%，参考项目 40.43%)

#### 2. 全模块覆盖矩阵解读
- **进程管理**: 双方均存在。存在共同 AST 指纹。目标覆盖率 14.72%，参考覆盖率 23.97%，表明双方在该模块重合度低，目标项目有超过 85% 的独有实现。证据强度：弱。
- **任务调度/异步运行时**: 双方均存在。存在共同 AST 指纹。目标覆盖率 27.27%，参考覆盖率 55.0%，覆盖率差异显著，说明参考项目更深度依赖共同上游，而目标项目有大量独立扩展。证据强度：中。
- **内存管理**: 双方均存在。存在共同 AST 指纹。目标覆盖率 32.22%，参考覆盖率 40.58%，双方均有大量独立实现，共同部分多为基础框架。证据强度：中。
- **中断/异常处理**: 双方均存在。存在共同 AST 指纹。目标覆盖率 62.96%，参考覆盖率 72.34%，覆盖率较高，但共同指纹多集中于硬件架构强相关的底层处理逻辑。证据强度：中。
- **系统调用**: 双方均存在。存在共同 AST 指纹。目标覆盖率 29.85%，参考覆盖率 69.77%，覆盖率差异巨大，说明双方的系统调用分发与实现架构完全不同。证据强度：强（证明异构性）。
- **文件系统**: 双方均存在。存在共同 AST 指纹。目标覆盖率 16.3%，参考覆盖率 20.96%，双方重合度极低，均为大规模独立实现。证据强度：弱。
- **设备驱动**: 双方均存在。存在共同 AST 指纹。目标覆盖率 78.32%，参考覆盖率 91.8%，覆盖率极高，共同指纹几乎全部来自共享的硬件驱动框架。证据强度：强（证明共享依赖）。
- **网络协议栈**: 双方均存在。存在共同 AST 指纹。目标覆盖率 53.85%，参考覆盖率 76.36%，共同部分主要来自 smoltcp 等第三方库的集成代码。证据强度：中。
- **第三方库/公共依赖**: 双方均存在。文件系统/第三方库模块未观察到共同指纹，网络协议栈/第三方库覆盖率约 42%，通用模块/其他覆盖率高达 67%-85%。这些高覆盖率模块的指纹几乎全部来自 `arceos`、`axlibc`、`nimbos` 等共享上游依赖。证据强度：强（证明共享依赖）。

#### 3. 底层 AST 关键证据指纹表
| 所属板块 | 目标项目路径 | 参考项目路径 | 证据类型 | 技术说明 |
| :--- | :--- | :--- | :--- | :--- |
| 进程管理 | `kernel/starry-next/src/task.rs#get_parent` | `process/src/process.rs#get_parent` | 架构异构 | 目标使用 `self.parent_id.load` (原子变量)，参考使用 `self.parent.lock().upgrade` (Mutex+Weak指针)，进程父子关系模型完全不同。 |
| 进程管理 | `kernel/starry-next/src/syscall_imp/task/thread.rs#sys_exit_group` | `api/src/interface/task/exit.rs#sys_exit_group` | 架构异构 | 目标显式调用 `exit_current_task`, `terminate_other_threads_in_group` 等复杂线程组管理逻辑；参考仅委托给 `sys_exit_impl`。 |
| 系统调用 | `kernel/starry-next/src/syscall_imp/task/thread.rs#sys_wait4` | `api/src/imp/task/wait.rs#sys_wait4` | 架构异构 | 目标使用统一的 `syscall_body!` 宏进行分发；参考直接展开 `WaitOptions::from_bits_truncate` 等具体实现，分发层设计不同。 |
| 系统调用 | `kernel/starry-next/src/syscall_imp/task/thread.rs#sys_execve` | `api/src/interface/task/execve.rs#sys_execve` | 架构异构 | 目标使用 `syscall_body!` 宏封装；参考包含 `BUSYBOX.to_string`, `get_string_array` 等具体参数处理逻辑。 |
| 任务调度/异步运行时 | `kernel/starry-next/arceos/modules/axtask/src/run_queue.rs#exit_current` | `arceos/modules/axtask/src/run_queue.rs#exit_current` | 共享依赖 | 调用图谱高度一致，但目标额外调用 `collect_exited_tasks` 进行资源回收。双方均依赖 ArceOS 上游框架。 |
| 任务调度/异步运行时 | `kernel/starry-next/arceos/modules/axtask/src/wait_queue.rs#cancel_events` | `arceos/modules/axtask/src/wait_queue.rs#cancel_events` | 共享依赖 | 调用图谱 100% 吻合，均为 ArceOS 框架中 `SpinNoIrq` 等待队列的标准操作。 |
| 内存管理 | `kernel/starry-next/src/mm.rs#handle_page_fault` | `arceos/modules/axmm/src/aspace.rs#handle_page_fault` | 架构异构 | 目标包含 `VirtAddr::from_usize(trap.get_ip()).align_down_4k` 等直接地址操作；参考调用 `area.backend().handle_page_fault`，通过后端抽象处理。 |
| 内存管理 | `kernel/starry-next/src/mm.rs#map_elf` | `core/src/mm.rs#map_elf` | 架构异构 | 目标使用 `ELFParser::new` 并传入 `axconfig::plat::USER_INTERP_BASE` 等平台相关参数；参考实现包含 `assert_eq!` 等不同的初始化逻辑。 |
| 中断/异常处理 | `kernel/starry-next/arceos/modules/axhal/src/arch/aarch64/trap.rs#handle_sync_exception` | `arceos/modules/axhal/src/arch/aarch64/trap.rs#handle_sync_exception` | 共享依赖 | 调用图谱高度一致，均为 ArceOS 框架中固定的异常分发流程，包含 `esr.read_as_enum` 等标准操作。 |
| 文件系统 | `kernel/starry-next/arceos/modules/axfs/src/fs/lwext4_rust.rs#create` | `arceos/modules/axfs-ng/src/fs/ext4/inode.rs#create` | 架构异构 | 目标基于 `lwext4_rust` 库封装，调用 `CString::new(fpath).map_err`；参考基于自研 `axfs-ng`，调用 `DirEntry::new_dir` 等不同抽象。 |
| 设备驱动 | `kernel/starry-next/arceos/modules/axdriver/src/lib.rs#init_drivers` | `arceos/modules/axdriver/src/lib.rs#init_drivers` | 共享依赖 | 调用图谱 100% 吻合，均为 ArceOS 框架的标准驱动初始化流程。 |
| 网络协议栈 | `kernel/starry-next/arceos/modules/axnet/src/smoltcp_impl/dns.rs#query` | `arceos/modules/axnet/src/smoltcp_impl/dns.rs#query` | 共享依赖 | 调用图谱 100% 吻合，均为 ArceOS 框架对 smoltcp 库的统一封装。 |
| 第三方库/公共依赖 | `kernel/starry-next/arceos/ulib/axlibc/c/printf.c#format_string_loop` | `arceos/ulib/axlibc/c/printf.c#format_string_loop` | 共享依赖 | 调用图谱 100% 吻合，均为从同一上游 axlibc 库引入的 C 标准库实现。 |
| 第三方库/公共依赖 | `kernel/starry-next/apps/nimbos/rust/src/bin/forktree.rs#fork_child` | `apps/nimbos/rust/src/bin/forktree.rs#fork_child` | 共享依赖 | 调用图谱 100% 吻合，均为从同一上游 NimbOS 教学系统引入的示例代码。 |

#### 4. 核心代码调用流对比剖析
* **【进程管理/进程退出路径】**
* **底层技术析理**: 目标项目在 `sys_exit_group` 系统调用中，显式实现了复杂的线程组同步逻辑，包括 `exit_current_task`、`terminate_other_threads_in_group` 和 `wait_for_other_threads_in_group_to_exit`，表明其在内核态维护了精细的线程组状态。而参考项目仅将请求委托给 `sys_exit_impl`，进程/线程生命周期的管理模型存在根本性差异。
* **代码溯源链接**:
  - 目标项目: `kernel/starry-next/src/syscall_imp/task/thread.rs#sys_exit_group`
  - 参考项目: `api/src/interface/task/exit.rs#sys_exit_group`

* **【进程管理/进程关系模型】**
* **底层技术析理**: 在获取父进程的函数中，目标项目通过 `self.parent_id.load` 直接加载一个原子变量，暗示其进程控制块中存储的是父进程的 ID。参考项目则通过 `self.parent.lock().upgrade` 操作一个 `Mutex<Weak<Process>>`，表明其使用带锁的弱引用指针直接指向父进程对象。两者在内存管理和同步策略上截然不同。
* **代码溯源链接**:
  - 目标项目: `kernel/starry-next/src/task.rs#get_parent`
  - 参考项目: `process/src/process.rs#get_parent`

* **【内存管理/缺页异常处理】**
* **底层技术析理**: 目标项目的缺页处理函数直接操作虚拟地址 (`VirtAddr::from_usize(trap.get_ip()).align_down_4k`) 和页表项 (`slot as *mut u64`)，属于较为底层的直接管理。参考项目则通过 `area.backend().handle_page_fault` 将处理逻辑委托给内存区域的后端对象，体现了更高级的抽象分层设计。
* **代码溯源链接**:
  - 目标项目: `kernel/starry-next/src/mm.rs#handle_page_fault`
  - 参考项目: `arceos/modules/axmm/src/aspace.rs#handle_page_fault`

#### 5. 单方模块与降权项
- **单方模块**: 无。所有宏观模块在双方项目中均存在。
- **降权项**: 所有标记为 `[🚨同板块底层逻辑一致]` 且路径包含 `arceos/`、`axlibc/`、`nimbos/` 的函数，如 `wait_queue.rs` 中的 `cancel_events`、`wait_timeout_until` 等，以及 `forktree.rs` 中的 `fork_child`。这些函数是双方共同依赖的开源上游框架或教学示例代码，其 AST 指纹一致是共享依赖的自然结果，不能作为目标项目抄袭参考项目的证据。此外，`syscall_body!`、`unimplemented` 等宏或标准库调用，属于语言层面的通用表达，不具备独创性，同样予以降权。

#### 6. 保守结论
本次审计基于 23.07% 的客观 AST 相似度，该数值本身处于较低水平。深入分析发现，双方共有的 1140 个 AST 指纹绝大多数源自对 ArceOS 内核框架、axlibc 库及 NimbOS 教学示例等上游开源项目的共同依赖。在决定项目原创性的核心系统调用层、进程管理模型和内存管理架构上，双方呈现出显著差异：目标项目采用宏封装分发、原子变量表示进程关系、直接物理地址操作；参考项目则采用函数展开分发、带锁弱引用指针表示进程关系、后端抽象处理缺页。因此，现有证据无法支持“大规模直接复用”的指控，目标项目应被认定为独立开发，整体同源性风险低。

------------------------------

