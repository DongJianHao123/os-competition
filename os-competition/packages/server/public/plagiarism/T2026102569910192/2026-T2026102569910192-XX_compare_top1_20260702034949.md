# 🛡️ OS 内核赛自动化代码级架构审计报告

**审计时间**: `2026-07-02 03:49:49`
**参赛作品名称**: `2026-T2026102569910192-XX`
**年份**: `2026`
**学校**: `XX`
**队伍名称**: `T2026102569910192`
**比赛名字**: `OS-Competition`
**比赛子赛道**: `Kernel-Implementation`
**仓库地址**: `https://gitlab.eduxiji.net/educg-group-43501-3132633/T2026102569910192-3896`
**推测开发语言**: `Rust / C (由AST探针自动识别)`
**特征函数量**: `8575`

## ⚔️ 底层代码同源性对比 (AST Homology)
> 本部分由专门的代码审查智能体，对该项目与符合年份条件的历届最高相似度项目进行控制流与底层调用对比。新版报告会先给出确定性的全模块覆盖矩阵，再给出 Agent 的分模块解释，避免只围绕少量抽样函数展开。

### 🏆 Top 1 底层对标: vs `2025-SubsToKernel-北京科技大学`（北京科技大学 / SubsToKernel）

#### 对方作品信息

**对方年份**: `2025`
**对方学校**: `北京科技大学`
**对方队伍名称**: `SubsToKernel`
**对方比赛名字**: `OS-Competition`
**对方比赛子赛道**: `Kernel-Implementation`
**对方仓库地址**: `https://gitlab.eduxiji.net/educg-group-36002-2710490/T202510008995695-2720`

> 双向覆盖率：共同 AST 指纹: 2141；唯一 AST 指纹: 目标项目 6404，参考项目 3437；覆盖率: 目标项目 33.43%，参考项目 62.29%

## 全模块对比总览（确定性证据）

> 总体覆盖率：共同 AST 指纹: 2141；唯一 AST 指纹: 目标项目 6404，参考项目 3437；覆盖率: 目标项目 33.43%，参考项目 62.29%

### 1. 模块覆盖矩阵
| 模块 | 目标函数数 | 参考函数数 | 共同唯一 AST 指纹 | Target 覆盖 | Base 覆盖 | 状态 |
| :--- | ---: | ---: | ---: | ---: | ---: | :--- |
| 进程管理 | 323 | 86 | 3 | 1.0% | 4.05% | 双方存在且有结构重合 |
| 任务调度/异步运行时 | 478 | 141 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |
| 内存管理 | 624 | 329 | 45 | 7.71% | 15.46% | 双方存在且有结构重合 |
| 中断/异常处理 | 73 | 8 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |
| 系统调用 | 116 | 108 | 14 | 13.86% | 18.67% | 双方存在且有结构重合 |
| 文件系统 | 907 | 256 | 3 | 0.4% | 1.6% | 双方存在且有结构重合 |
| 设备驱动 | 145 | 57 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |
| 网络协议栈 | 61 | 124 | 1 | 2.0% | 0.84% | 双方存在且有结构重合 |
| 文件系统/第三方库 | 674 | 704 | 414 | 72.38% | 69.35% | 双方存在且有结构重合 |
| 网络协议栈/第三方库 | 2599 | 2388 | 1590 | 80.02% | 88.09% | 双方存在且有结构重合 |
| 第三方库 | 2158 | 0 | 0 | 0.0% | 0.0% | 仅目标项目体现 |
| 通用模块/其他 | 417 | 139 | 7 | 1.88% | 5.43% | 双方存在且有结构重合 |

### 2. 分模块证据清单

#### 进程管理
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `SWTC/kernel/src/syscall/process.rs#sys_setsid` | `os/src/syscall/process.rs#sys_setsid` | 同板块同名 | Ok, PROCESS_GROUP_MANAGER.set_pgid_by_pid, current_process, current_process().inner_handler, current_process().pid, debug! | 内部闭环逻辑 |
| `SWTC/kernel/src/syscall/process.rs#sys_getppid` | `os/src/syscall/process.rs#sys_getppid` | 同板块同名 | Ok, current_process, current_process.inner_handler, debug!, parent_process.upgrade, parent_process.upgrade().unwrap ...(+3) | current_task, current_task().unwrap, current_task().unwrap().process.upgrade, current_task().unwrap().process.upgrade().unwrap, current_task().unwrap().process.upgrade().unwrap().getppid |
| `SWTC/kernel/src/process/mod.rs#add_initproc` | `os/src/task/mod.rs#add_initproc` | 同板块同名 | Process::new_initproc, get_app_data_by_name, get_app_data_by_name("initproc").unwrap, get_app_data_by_name("runtestcase").unwrap, stack_trace! | INITPROC.clone, PROCESSOR.exclusive_access, Some, initproc.inner_exclusive_access, inner.tasks[0].clone, inner.tasks[0].clone().unwrap ...(+1) |
| `SWTC/kernel/src/syscall/process.rs#sys_exit` | `os/src/syscall/process.rs#sys_exit` | 同板块同名 | Ok, info!, local_hart, local_hart().current_task, local_hart().current_task().tid, stack_trace! ...(+1) | current_process, current_process.getpid, debug!, drop, exit_current_and_run_next, panic! |

#### 任务调度/异步运行时
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `SWTC/kernel/src/syscall/mod.rs#syscall` | `os/src/syscall/mod.rs#syscall` | 同板块同名 | Ok, error!, sys_handler! | info!, panic!, sys_accept, sys_bind, sys_brk, sys_chdir ...(+106) |
| `SWTC/kernel/src/syscall/fs.rs#sys_pselect6` | `os/src/syscall/fs.rs#sys_pselect6` | 同板块同名 | Duration::from, Err, IOMultiplexFormat::FdSets, IOMultiplexFuture::new, Ok, PollEvents::POLLIN.bits ...(+48) | Some, current_process, current_task, current_task().unwrap, current_task().unwrap().set_sig_mask, drop ...(+24) |
| `SWTC-la/xmodules/xsignal/src/api/thread.rs#handle_signal` | `os/src/signal/mod.rs#handle_signal` | 同板块同名 | Layout::new::<SignalFrame>, SignalAction::default, Some, UContext::new, access_user_memory, action
                    .restorer
                    .map_or ...(+22) | SignalFlags::from_sig, current_task, current_task().unwrap, debug!, drop, exit_current_and_run_next ...(+7) |
| `SWTC/kernel/src/syscall/net.rs#sys_setsockopt` | `os/src/syscall/net.rs#sys_setsockopt` | 同板块同名 | Ok, SumGuard::new, UserCheck::new, UserCheck::new().check_readable_slice, current_process, current_process()
        .inner_handler ...(+10) | current_process, debug!, inner.fd_table.get, inner.fd_table.get(sockfd).file.sock, inner.fd_table.len, inner.fd_table.try_get ...(+4) |

#### 内存管理
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `SWTC/kernel/src/process/mod.rs#exec` | `os/src/task/process.rs#exec` | 同板块同名 | MemorySpace::from_elf, Ok, SumGuard::new, TrapContext::app_init_context, UserCheck::new, UserCheck::new().check_writable_slice ...(+33) | Arc::new, Aux::new, FdTable::from_another, KERNEL_SPACE.exclusive_access, KERNEL_SPACE.exclusive_access().token, MemorySet::from_elf ...(+39) |
| `SWTC/kernel/src/trap/user_trap.rs#trap_handler` | `os/src/hal/trap/mod.rs#trap_handler` | 同板块同名 | VirtAddr::from, crate::driver::intr_handler, current_task, current_task().recv_signal, current_trap_cx, exit_and_terminate_all_threads ...(+21) | check_signals_of_current, check_timer, core::hint::spin_loop, current_add_signal, current_process, current_process()
        .inner_exclusive_access ...(+18) |
| `SWTC/kernel/src/mm/memory_space/mod.rs#new_kernel` | `os/src/mm/memory_set.rs#new_kernel` | 同板块同名 | ((*pair).0 + (*pair).1 + (KERNEL_DIRECT_OFFSET << PAGE_SIZE_BITS)).into, ((*pair).0 + (KERNEL_DIRECT_OFFSET << PAGE_SIZE_BITS)).into, (ebss as usize).into, (edata as usize).into, (ekernel as usize).into, (erodata as usize).into ...(+15) | ((*pair).0 + (*pair).1).into, (*pair).0.into, (ebss as usize).into, (edata as usize).into, (ekernel as usize).into, (erodata as usize).into ...(+11) |
| `SWTC/kernel/src/mm/memory_space/mod.rs#from_elf` | `os/src/mm/memory_set.rs#from_elf` | 同板块同名 | 0.into, Arc::new, Err, HeapRange::new, Ok, Self::new_from_global ...(+28) | Aux::new, Self::new_bare, Vec::new, VirtAddr, VirtPageNum, assert_eq! ...(+17) |

#### 中断/异常处理
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `SWTC/kernel/src/trap/mod.rs#set_user_trap_entry` | `os/src/hal/trap/mod.rs#set_user_trap_entry` | 同板块同名 | stvec::write | eentry::set_eentry, stvec::write |
| `SWTC/kernel/src/trap/mod.rs#set_kernel_trap_entry` | `os/src/hal/trap/mod.rs#set_kernel_trap_entry` | 同板块同名 | stvec::write | eentry::set_eentry, stvec::write |
| `SWTC/kernel/src/trap/mod.rs#enable_timer_interrupt` | `os/src/hal/trap/mod.rs#enable_timer_interrupt` | 同板块同名 | sie::set_stimer | crmd::set_ie, ecfg::set_lie, get_timer_freq, sie::set_stimer, tcfg::set_en, tcfg::set_init_val ...(+2) |
| `SWTC-la/xmodules/xsignal/src/types.rs#default_action` | `os/src/signal/signal.rs#default_op` | 同模块上下文对齐/差异观察 | 内部闭环逻辑 | continue_signals.contains, dump_signals.contains, ignore_signals.contains, stop_signals.contains, terminate_signals.contains |

#### 系统调用
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `SWTC-la/xapi/src/ipc/shm.rs#sys_shmctl` | `os/src/syscall/mem.rs#sys_shmctl` | 同板块同名 | Err, IPC_MANAGER.with_shm, Ok, buf.is_null, nullable!, segment.lock ...(+5) | panic!, shm_drop |
| `SWTC/kernel/src/syscall/time.rs#sys_get_time` | `user/src/syscall.rs#sys_get_time` | 同板块同名 | Ok, SumGuard::new, UserCheck::new, core::mem::size_of::<TimeVal>, core::mem::size_of::<[i32; 2]>, current_time_ms ...(+8) | syscall |
| `SWTC/user/src/lib.rs#sleep` | `user/src/lib.rs#sleep` | 同板块同名 | assert_eq!, sys_yield | get_time, sys_yield |
| `SWTC/user/src/syscall.rs#syscall6` | `user/src/syscall.rs#syscall6` | 同板块同名 | asm! | core::arch::asm! |

#### 文件系统
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `SWTC/kernel/src/fs/fat32/dentry.rs#read_dentry` | `os/src/fs/vfs/mod.rs#read_dentry` | 同板块同名 | Some, info!, lsb16!, next_id.is_some, next_id.unwrap, reader.read_dentry ...(+4) | unimplemented! |
| `SWTC/kernel/src/fs/inode.rs#sync` | `os/src/fs/vfs/mod.rs#sync` | 同板块同名 | Box::pin, Ok, Vec::new, child.clone, child.sync, children_set.push ...(+7) | unimplemented! |
| `SWTC/kernel/src/fs/file.rs#truncate` | `os/src/fs/vfs/mod.rs#truncate` | 同板块同名 | Box::pin, Ok, OpenFlags::default, SeekFrom::Start, inner.inode.as_ref, inner.inode.as_ref().ok_or ...(+9) | unimplemented! |
| `SWTC/kernel/src/fs/inode.rs#get` | `os/src/fs/fstruct.rs#get` | 同板块同名 | Some, inner.as_ref, inner.as_ref().unwrap, inner.as_ref().unwrap().get, inner.as_ref().unwrap().get(&path).cloned, inner.as_ref().unwrap().get(parent_path).cloned ...(+5) | self.get_mut, self.get_mut().files[fd].clone, self.get_mut().files[fd].clone().unwrap |

#### 设备驱动
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `SWTC/kernel/src/driver/fu740/sdcard.rs#write_block` | `os/src/drivers/virtio/blk.rs#write_block` | 同板块同名 | Err, Ok, buf.len, self.end_cmd, self.get_resp, self.send_cmd ...(+1) | panic!, self.inner.lock, self.inner.lock().ack_interrupt, self.inner.lock().write_blocks |
| `SWTC/kernel/src/driver/fu740/sdcard.rs#read_block` | `os/src/drivers/virtio/blk.rs#read_block` | 同板块同名 | Err, Ok, buf.len, self.end_cmd, self.get_resp, self.read_data ...(+1) | buf.fill, panic!, self.inner.lock, self.inner.lock().ack_interrupt, self.inner.lock().read_blocks |
| `SWTC-la/arceos/modules/axdriver/src/structs/mod.rs#device_name` | `os/src/drivers/virtio/blk.rs#device_name` | 同板块同名 | dev.device_name, unreachable! | 内部闭环逻辑 |
| `SWTC-la/arceos/modules/axdriver/src/structs/mod.rs#device_type` | `os/src/drivers/virtio/blk.rs#device_type` | 同板块同名 | unreachable! | 内部闭环逻辑 |

#### 网络协议栈
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `SWTC-la/xcore/src/net/socket.rs#sendto` | `os/src/net/socket_impl.rs#sendto` | 同板块同名 | Err, Ipv4Addr::LOCALHOST.into, Ok, SocketAddr::new, inner
                    .bind, inner
                    .bind(SocketAddr::new(Ipv4Addr::LOCALHOST.into(), 0))
                    .ok ...(+2) | Err, Ok, crate::net::AxError::from, self.sendto |
| `SWTC-la/xcore/src/net/socket.rs#recvfrom` | `os/src/net/socket_impl.rs#recvfrom` | 同板块同名 | Ok, Some, tcpsocket.lock, tcpsocket.lock().recv, tcpsocket.lock().recv(buf).map, udpsocket
                .lock ...(+5) | Err, Ok, Some, crate::net::AxError::from, tcpsocket.lock, tcpsocket.lock().recv ...(+2) |
| `SWTC-la/xcore/src/net/socket.rs#accept` | `os/src/net/socket/listen_table.rs#accept` | 同板块同名 | Err, Mutex::new, Socket::Tcp, Socket::Unix, tcpsocket
                .lock, tcpsocket
                .lock()
                .accept ...(+4) | Ok, ax_err!, get_addr_tuple, is_connected, is_connected(handle).then, self.tcp[port as usize].lock ...(+8) |
| `SWTC-la/xcore/src/net/socket.rs#connect` | `os/src/net/socket_impl.rs#connect` | 同板块同名 | Err, Ok, tcpsocket.lock, tcpsocket.lock().connect, udpsocket.lock, udpsocket.lock().connect | Err, Ok, SysErrNo::EOPNOTSUPP.into, crate::net::AxError::from, debug!, tcpsocket.lock ...(+1) |

#### 文件系统/第三方库
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `SWTC-la/arceos/crates/lwext4_rust/c/lwext4/src/ext4_balloc.c#ext4_balloc_alloc_block` | `os/libs/lwext4_rsut/c/lwext4/src/ext4_balloc.c#ext4_balloc_alloc_block` | 同板块同名同结构 | ext4_balloc_get_bgid_of_block, ext4_balloc_get_block_of_bgid, ext4_balloc_set_bitmap_csum, ext4_balloc_verify_bitmap_csum, ext4_bg_get_block_bitmap, ext4_bg_get_free_blocks_count ...(+19) | ext4_balloc_get_bgid_of_block, ext4_balloc_get_block_of_bgid, ext4_balloc_set_bitmap_csum, ext4_balloc_verify_bitmap_csum, ext4_bg_get_block_bitmap, ext4_bg_get_free_blocks_count ...(+19) |
| `SWTC-la/arceos/crates/lwext4_rust/c/lwext4/src/ext4_journal.c#jbd_journal_prepare` | `os/libs/lwext4_rsut/c/lwext4/src/ext4_journal.c#jbd_journal_prepare` | 同板块同名同结构 | EXT4_BLOCK_ZERO, RB_FIND, TAILQ_FOREACH_REVERSE_SAFE, TAILQ_FOREACH_SAFE, TAILQ_REMOVE, ext4_bcache_clear_dirty ...(+17) | EXT4_BLOCK_ZERO, RB_FIND, TAILQ_FOREACH_REVERSE_SAFE, TAILQ_FOREACH_SAFE, TAILQ_REMOVE, ext4_bcache_clear_dirty ...(+17) |
| `SWTC-la/arceos/crates/lwext4_rust/c/lwext4/src/ext4.c#ext4_dir_rm` | `os/libs/lwext4_rsut/c/lwext4/src/ext4.c#ext4_dir_rm` | 同板块同名同结构 | EXT4_MP_LOCK, EXT4_MP_UNLOCK, ext4_block_cache_write_back, ext4_dir_en_get_inode, ext4_dir_iterator_fini, ext4_dir_iterator_init ...(+19) | EXT4_MP_LOCK, EXT4_MP_UNLOCK, ext4_block_cache_write_back, ext4_dir_en_get_inode, ext4_dir_iterator_fini, ext4_dir_iterator_init ...(+19) |
| `SWTC-la/arceos/crates/lwext4_rust/c/lwext4/src/ext4_dir_idx.c#ext4_dir_dx_split_index` | `os/libs/lwext4_rsut/c/lwext4/src/ext4_dir_idx.c#ext4_dir_dx_split_index` | 同板块同名同结构 | ext4_block_set, ext4_dir_dx_climit_get_count, ext4_dir_dx_climit_get_limit, ext4_dir_dx_climit_set_count, ext4_dir_dx_climit_set_limit, ext4_dir_dx_entry_get_hash ...(+10) | ext4_block_set, ext4_dir_dx_climit_get_count, ext4_dir_dx_climit_get_limit, ext4_dir_dx_climit_set_count, ext4_dir_dx_climit_set_limit, ext4_dir_dx_entry_get_hash ...(+10) |

#### 网络协议栈/第三方库
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `SWTC-la/arceos/crates/smoltcp/src/socket/tcp.rs#process` | `os/libs/smoltcp/src/socket/tcp.rs#process` | 同板块同名 | AckDelayTimer::Waiting, IpEndpoint::new, Self::random_seq_no, Self::rst_reply, Some, control.quash_psh ...(+49) | AckDelayTimer::Waiting, IpEndpoint::new, Self::random_seq_no, Self::rst_reply, Some, control.quash_psh ...(+42) |
| `SWTC-la/arceos/crates/smoltcp/src/socket/tcp.rs#dispatch` | `os/libs/smoltcp/src/socket/tcp.rs#dispatch` | 同板块同名 | IpRepr::new, Ok, Some, TcpTimestampRepr::generate_reply_with_tsval, cx.ip_mtu, cx.now ...(+49) | IpRepr::new, Ok, Some, cx.ip_mtu, cx.now, emit ...(+45) |
| `SWTC-la/arceos/crates/smoltcp/src/iface/interface/mod.rs#dispatch_ip` | `os/libs/smoltcp/src/iface/interface/mod.rs#dispatch_ip` | 同板块同名 | EthernetAddress, EthernetFrame::<&[u8]>::buffer_len, EthernetFrame::<&[u8]>::header_len, EthernetFrame::new_unchecked, Ipv4Packet::new_unchecked, Ok ...(+34) | EthernetAddress, EthernetFrame::<&[u8]>::buffer_len, EthernetFrame::<&[u8]>::header_len, EthernetFrame::new_unchecked, Ipv4Packet::new_unchecked, Ok ...(+35) |
| `SWTC-la/arceos/crates/smoltcp/src/socket/dhcpv4.rs#dispatch` | `os/libs/smoltcp/src/socket/dhcpv4.rs#dispatch` | 同板块同名同结构 | Ok, Self::random_transaction_id, Some, cx.hardware_addr, cx.ip_mtu, cx.now ...(+12) | Ok, Self::random_transaction_id, Some, cx.hardware_addr, cx.ip_mtu, cx.now ...(+12) |

#### 第三方库
仅目标项目中观察到该模块代表函数，参考项目没有同模块可对齐证据：
- 目标项目：`SWTC-la/dependencies/xapi/src/ipc/sem.rs#sys_semctl`，调用摘要：Err, with_ipc_manager!
- 目标项目：`SWTC-la/dependencies/xapi/src/fs/fd/fanotify.rs#sys_fanotify_mark`，调用摘要：Arc::downgrade, Arc::new, Err, FanEventMask::empty, FanEventMask::from_bits, FanEventMask::from_bits(mask).ok_or ...(+42)
- 目标项目：`SWTC-la/dependencies/xapi/src/task/clone.rs#do_clone`，调用摘要：Arc::default, Arc::new, CloneFlags::from_bits_truncate, Err, FD_TABLE
                .deref_from, FD_TABLE
                .deref_from(&process_data.ns)
                .init_new ...(+76)
- 目标项目：`SWTC-la/arceos/crates/allocator/src/bitmap.rs#test_bitmap_page_allocator_size_2g`，调用摘要：BitmapPageAllocator::<PAGE_SIZE>::new, allocator
                .alloc_pages_at, allocator
                .alloc_pages_at(test_addr_base, num_pages, align)
                .unwrap, allocator.alloc_pages, allocator.alloc_pages(num_pages, PAGE_SIZE).unwrap, allocator.alloc_pages(num_pages, align).unwrap ...(+5)

#### 通用模块/其他
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `SWTC-la/arceos/modules/axsync/src/mutex.rs#lock` | `os/src/sync/mutex.rs#lock` | 同板块同名 | assert_ne!, current, current().id, current().id().as_u64, self.is_locked, self.owner_id.compare_exchange_weak ...(+1) | drop, self.locked.exclusive_access, suspend_current_and_run_next |
| `SWTC/user/src/lib.rs#_start` | `user/src/lib.rs#_start` | 同板块同名 | ((argv + i * core::mem::size_of::<usize>()) as *const usize).read_volatile, ((str_start + *i) as *const u8).read_volatile, (0usize..)
            .find, (0usize..)
            .find(|i| unsafe { ((str_start + *i) as *const u8).read_volatile() == 0 })
            .unwrap, HEAP.lock, HEAP.lock()
            .init ...(+10) | ((argv + i * core::mem::size_of::<usize>()) as *const usize).read_volatile, ((str_start + *i) as *const u8).read_volatile, (0usize..)
            .find, (0usize..)
            .find(|i| unsafe { ((str_start + *i) as *const u8).read_volatile() == 0 })
            .unwrap, HEAP.lock, HEAP.lock()
            .init ...(+11) |
| `SWTC-la/xcore/src/sys/time.rs#set_timer` | `os/src/timer.rs#set_timer` | 同板块同名 | debug!, timer_type.into, with_irqs_disabled | TimeVal::new, self.inner.get_unchecked_mut |
| `SWTC/kernel/src/timer/mod.rs#partial_cmp` | `os/src/timer.rs#partial_cmp` | 同板块同名 | Some | Some |


好的，我将遵循审计指令，基于提供的客观证据，对本案进行最终的定性仲裁。

### 1. 宏观判定
- **定性结论**: **独立开发，底层架构存在显著差异**
  综合客观 AST 相似度（27.81%）及双向覆盖率指标，本案不构成同源性抄袭。全项目 2141 条共同 AST 指纹中，超过 93%（2004 条）由双方共同引用的第三方开源库（lwext4、smoltcp 等）贡献。在核心原创模块【进程管理】中，目标项目 323 个函数与参考项目 86 个函数之间仅存在 3 条共同 AST 指纹，目标覆盖率低至 1.0%。该重合度远不足以支撑“代码复用”的指控，且重合点均为 Rust 语言标准惯用法或第三方宏生成的标准代码。双方在进程/任务抽象、同步原语、用户态内存检查等核心架构上采用了完全不同的对象模型，体系性差异显著，符合独立开发的特征。

### 2. 全模块覆盖矩阵解读
- **进程管理**: 双方均存在。共同 AST 指纹仅 3 条，目标覆盖率 1.0%，参考覆盖率 4.05%。覆盖率极低，表明双方在进程管理的核心实现上几乎无重合。证据强度：**弱**。
- **任务调度/异步运行时**: 双方均存在。共同 AST 指纹为 0，覆盖率均为 0.0%。尽管双方都实现了调度功能，但底层 AST 结构无任何重合，是完全独立的实现。证据强度：**未形成**。
- **内存管理**: 双方均存在。共同 AST 指纹 45 条，目标覆盖率 7.71%，参考覆盖率 15.46%。存在少量结构重合，主要源于 ELF 加载、内核内存空间初始化等通用操作系统逻辑。证据强度：**弱**。
- **中断/异常处理**: 双方均存在。共同 AST 指纹为 0，覆盖率均为 0.0%。尽管存在同名函数（如 `set_user_trap_entry`），但 AST 指纹不同，表明底层寄存器操作或封装方式存在差异。证据强度：**未形成**。
- **系统调用**: 双方均存在。共同 AST 指纹 14 条，目标覆盖率 13.86%，参考覆盖率 18.67%。重合点主要源于 POSIX 标准系统调用（如 `sys_get_time`）的签名和基本逻辑，以及用户态库（`user/src/lib.rs`）的相似启动代码。证据强度：**弱**。
- **文件系统**: 双方均存在。共同 AST 指纹 3 条，目标覆盖率 0.4%，参考覆盖率 1.6%。覆盖率极低，双方在虚拟文件系统（VFS）核心实现上完全不同，参考项目多为 `unimplemented!` 占位。证据强度：**弱**。
- **设备驱动**: 双方均存在。共同 AST 指纹为 0，覆盖率均为 0.0%。双方驱动模型和硬件平台不同（如目标项目有 fu740 驱动，参考项目有 virtio 驱动），无共同指纹。证据强度：**未形成**。
- **网络协议栈**: 双方均存在。共同 AST 指纹 1 条，目标覆盖率 2.0%，参考覆盖率 0.84%。覆盖率极低，双方在 socket 实现上存在少量相似调用（如 `sendto`），但整体架构不同。证据强度：**弱**。
- **第三方库/公共依赖**: 这是共同指纹的主要来源。`文件系统/第三方库`（lwext4）和 `网络协议栈/第三方库`（smoltcp）合计贡献了 2004 条共同指纹，覆盖率均超过 69%。这些代码来自公开的第三方开源库，其高度一致性是正常现象，不能作为抄袭证据。证据强度：**强（但属于公共依赖，不构成抄袭）**。

### 3. 底层 AST 关键证据指纹表
| 所属板块 | 目标项目路径 | 参考项目路径 | 证据类型 | 技术说明 |
| :--- | :--- | :--- | :--- | :--- |
| 第三方库/公共依赖 | `SWTC-la/arceos/crates/lwext4_rust/.../ext4_balloc.c#ext4_balloc_alloc_block` | `os/libs/lwext4_rsut/.../ext4_balloc.c#ext4_balloc_alloc_block` | 同板块同名同结构 | 双方均使用了相同的第三方开源库 lwext4，AST 完全一致是预期行为，不构成原创性抄袭。 |
| 第三方库/公共依赖 | `SWTC-la/arceos/crates/smoltcp/.../tcp.rs#process` | `os/libs/smoltcp/.../tcp.rs#process` | 同板块同名 | 双方均使用了相同的第三方开源库 smoltcp，AST 高度相似是预期行为，不构成原创性抄袭。 |
| 进程管理 | `SWTC/kernel/src/process/mod.rs#thread_count` | `os/src/task/process.rs#thread_count` | 同板块同名同结构 | 双方均使用 `self.collection.len()` 模式获取集合长度，这是 Rust 语言的标准惯用法，不具备可保护性。 |
| 进程管理 | `SWTC/kernel/src/syscall/process.rs#sys_clone` | `os/src/syscall/process.rs#sys_fork` | 同板块证据 | 双方均使用 `CloneFlags::from_bits`，这是 `bitflags` 宏自动生成的标准方法，任何使用该库的代码都会产生相同 AST。 |
| 进程管理 | `SWTC/kernel/src/syscall/signal.rs#sys_rt_sigprocmask` | `os/src/syscall/signal.rs#sys_sigprocmask` | 同板块证据 | 双方均使用 `Type::from_bits(...).ok_or(...)` 模式，这是 `bitflags` 库结合 Rust 错误处理的标准范式。 |
| 系统调用 | `SWTC/user/src/lib.rs#_start` | `user/src/lib.rs#_start` | 同板块同名 | 用户态程序启动代码中的参数解析逻辑相似，源于对 `argv` 和 `argc` 进行处理的通用算法，且 AST 指纹不同。 |
| 内存管理 | `SWTC/kernel/src/mm/memory_space/mod.rs#from_elf` | `os/src/mm/memory_set.rs#from_elf` | 同板块同名 | ELF 文件加载逻辑存在部分相似调用，源于 ELF 标准格式解析的通用步骤，但外部对象调用图谱不同。 |

### 4. 核心代码调用流对比剖析
* **【进程管理/系统调用路径】**
* **底层技术析理**: 以 `sys_getppid` 为例，两者均需获取父进程 ID，但实现路径反映了完全不同的内核架构。
  - **目标项目**采用**进程为中心**的设计，通过全局 `PROCESS_MANAGER` 获取当前进程对象，该对象直接持有指向父进程的 `Weak` 引用，通过 `parent_process.upgrade().unwrap().pid` 获取 ID。其外部调用图谱为 `current_process -> parent_process.upgrade`。
  - **参考项目**采用**任务为中心**的设计，通过 `current_task()` 获取当前任务控制块，任务控制块再通过 `Weak` 引用向上关联到进程，调用链为 `current_task().unwrap().process.upgrade().unwrap().getppid()`。其外部调用图谱为 `current_task -> process.upgrade -> getppid`。
  两者在对象模型（进程直接持有父引用 vs. 任务持有进程引用）和同步机制（`PROCESS_MANAGER` vs. `PROCESSOR`）上存在根本性差异，`upgrade().unwrap()` 仅是 Rust 中操作 `Weak` 指针的标准做法，不能视为逻辑复用。
* **代码溯源链接**:
  - 目标项目: `SWTC/kernel/src/syscall/process.rs#sys_getppid`
  - 参考项目: `os/src/syscall/process.rs#sys_getppid`

* **【内存管理/内存空间初始化】**
* **底层技术析理**: 以 `new_kernel` 为例，两者均需映射内核自身的内存段（`.text`, `.rodata`, `.data`, `.bss` 等），但映射策略和地址空间布局不同。
  - **目标项目**在计算物理地址时，统一加上了 `KERNEL_DIRECT_OFFSET << PAGE_SIZE_BITS` 偏移量，表明其采用了直接映射的物理内存模型，内核虚拟地址与物理地址之间存在固定的线性偏移。
  - **参考项目**则直接使用链接脚本导出的符号地址（如 `(*pair).0.into()`），未在映射函数内部添加统一偏移，其地址映射逻辑可能封装在页表项创建或更底层的 `MapArea` 中。
  两者对内核内存空间的抽象层次和地址转换策略存在明显差异。
* **代码溯源链接**:
  - 目标项目: `SWTC/kernel/src/mm/memory_space/mod.rs#new_kernel`
  - 参考项目: `os/src/mm/memory_set.rs#new_kernel`

### 5. 单方模块与降权项
- **单方模块**: `第三方库` 模块仅在目标项目中体现（2158 个函数），参考项目无此独立分类。这源于项目组织方式的差异，目标项目可能将部分依赖库代码集中管理，而参考项目分散在各子系统中。这不构成同源证据。
- **降权项**:
  - **跨板块同名函数**: 如目标项目的 `create_kernel_root`（属文件系统/procfs）与参考项目的 `sys_exec`（属进程管理）等，虽同属广义的“进程管理”上下文，但分属不同子系统，其调用图谱无交集，不能作为强同源证据。
  - **标准字符串常量**: 如 `meminfo::serialize` 中的 `"MemTotal:\t"` 或 `sys_exec` 中的 `"LD_LIBRARY_PATH=..."`，这些是 Linux 标准 procfs 格式或标准环境变量，由规范定义，任何兼容 Linux 的内核都会出现，不具原创性。
  - **泛型函数与标准宏**: `Some`, `Ok`, `Err`, `unwrap`, `debug!`, `info!` 等是 Rust 语言的标准库和宏，`from_bits` 是 `bitflags` 库生成的标准方法。这些 AST 节点的重合是语言层面的必然，必须从同源证据中剔除。

### 6. 保守结论
本案 27.81% 的 AST 相似度主要由双方共同引用的第三方开源库（lwext4, smoltcp）贡献，这部分重合是正常且预期的。在排除第三方库后，核心原创模块的 AST 重合度极低，尤其在【进程管理】板块，目标覆盖率仅 1.0%。控方提出的“铁证”均为 Rust 语言惯用法、第三方宏生成代码或 POSIX 标准语义，不具备原创性。更重要的是，双方在进程/任务抽象、同步原语、用户态内存检查等核心架构上存在体系性差异，如 `UserCheck/SumGuard/PROCESS_MANAGER` vs `UserBuffer/inner_exclusive_access/PROCESSOR`，这证明双方是基于不同设计理念独立开发的。因此，整体抄袭风险极低，应认定为独立作品。

------------------------------

