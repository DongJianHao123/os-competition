# 🛡️ OS 内核赛自动化代码级架构审计报告

**审计时间**: `2026-07-02 03:49:49`
**参赛作品名称**: `2026-T2026102569910192-XX`
**年份**: `2026`
**学校**: `XX`
**队伍名称**: `T2026102569910192`
**比赛名字**: `OS-Competition`
**比赛子赛道**: `Kernel-Implementation`
**仓库地址**: `https://gitlab.eduxiji.net/educg-group-43501-3132633/T2026102569910192-3896`
**推测开发语言**: `Rust / C (由AST探针自动识别)`
**特征函数量**: `8575`

## ⚔️ 底层代码同源性对比 (AST Homology)
> 本部分由专门的代码审查智能体，对该项目与符合年份条件的历届最高相似度项目进行控制流与底层调用对比。新版报告会先给出确定性的全模块覆盖矩阵，再给出 Agent 的分模块解释，避免只围绕少量抽样函数展开。

### 🏆 Top 5 底层对标: vs `2025-九莲宝灯一向听-天津理工大学`（天津理工大学 / 九莲宝灯一向听）

#### 对方作品信息

**对方年份**: `2025`
**对方学校**: `天津理工大学`
**对方队伍名称**: `九莲宝灯一向听`
**对方比赛名字**: `OS-Competition`
**对方比赛子赛道**: `Kernel-Implementation`
**对方仓库地址**: `https://gitlab.eduxiji.net/educg-group-36002-2710490/T202510060995409-3585`

> 双向覆盖率：共同 AST 指纹: 980；唯一 AST 指纹: 目标项目 6404，参考项目 2610；覆盖率: 目标项目 15.3%，参考项目 37.55%

## 全模块对比总览（确定性证据）

> 总体覆盖率：共同 AST 指纹: 980；唯一 AST 指纹: 目标项目 6404，参考项目 2610；覆盖率: 目标项目 15.3%，参考项目 37.55%

### 1. 模块覆盖矩阵
| 模块 | 目标函数数 | 参考函数数 | 共同唯一 AST 指纹 | Target 覆盖 | Base 覆盖 | 状态 |
| :--- | ---: | ---: | ---: | ---: | ---: | :--- |
| 进程管理 | 323 | 79 | 31 | 10.3% | 42.47% | 双方存在且有结构重合 |
| 任务调度/异步运行时 | 478 | 385 | 119 | 29.68% | 43.27% | 双方存在且有结构重合 |
| 内存管理 | 624 | 301 | 104 | 17.81% | 38.24% | 双方存在且有结构重合 |
| 中断/异常处理 | 73 | 60 | 22 | 33.85% | 43.14% | 双方存在且有结构重合 |
| 系统调用 | 116 | 130 | 9 | 8.91% | 8.41% | 双方存在且有结构重合 |
| 文件系统 | 907 | 616 | 44 | 5.91% | 9.69% | 双方存在且有结构重合 |
| 设备驱动 | 145 | 156 | 38 | 34.23% | 26.76% | 双方存在且有结构重合 |
| 网络协议栈 | 61 | 61 | 4 | 8.0% | 7.84% | 双方存在且有结构重合 |
| 文件系统/第三方库 | 674 | 577 | 414 | 72.38% | 85.89% | 双方存在且有结构重合 |
| 网络协议栈/第三方库 | 2599 | 130 | 62 | 3.12% | 53.45% | 双方存在且有结构重合 |
| 第三方库 | 2158 | 209 | 47 | 3.18% | 25.68% | 双方存在且有结构重合 |
| 通用模块/其他 | 417 | 630 | 80 | 21.45% | 16.63% | 双方存在且有结构重合 |

### 2. 分模块证据清单

#### 进程管理
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `SWTC-la/xapi/src/task/exit.rs#do_exit` | `api/src/imp/task/exit.rs#do_exit` | 同板块同名 | FD_TABLE.clear, FutexKey::new, IPC_MANAGER.clear, SignalInfo::new, UserPtr::<Pid>::from, axtask::exit ...(+32) | FD_TABLE.clear, SignalInfo::new, UserPtr::<Pid>::from, axtask::exit, axtask::yield_now, clear_child_tid.get_as_mut ...(+22) |
| `SWTC/kernel/src/syscall/signal.rs#sys_rt_sigprocmask` | `api/src/imp/signal.rs#sys_rt_sigprocmask` | 同板块同名 | Err, Ok, SigSet::from_bits, SigSet::from_bits(*set as usize).ok_or, SumGuard::new, UserCheck::new ...(+9) | Err, Ok, check_sigset_size, current, current()
        .task_ext, current()
        .task_ext()
        .thread_data ...(+2) |
| `SWTC-la/arceos/modules/axtask/src/run_queue.rs#exit_current` | `arceos/modules/axtask/src/run_queue.rs#exit_current` | 同板块同名同结构 | EXITED_TASKS.current_ref_mut_raw, EXITED_TASKS.current_ref_mut_raw().clear, EXITED_TASKS.current_ref_mut_raw().push_back, WAIT_FOR_EXIT.current_ref_mut_raw, WAIT_FOR_EXIT.current_ref_mut_raw().notify_one, assert! ...(+8) | EXITED_TASKS.current_ref_mut_raw, EXITED_TASKS.current_ref_mut_raw().clear, EXITED_TASKS.current_ref_mut_raw().push_back, WAIT_FOR_EXIT.current_ref_mut_raw, WAIT_FOR_EXIT.current_ref_mut_raw().notify_one, assert! ...(+8) |
| `SWTC-la/xmodules/xsignal/src/api/thread.rs#wait_timeout` | `arceos/modules/axtask/src/wait_queue.rs#wait_timeout` | 同板块同名 | Some, axhal::time::wall_time, deadline.checked_sub, self.blocked, self.dequeue_signal, timeout.map ...(+2) | axhal::time::wall_time, crate::current, crate::timers::set_alarm_wakeup, curr.clone, curr.in_wait_queue, current_run_queue::<NoPreemptIrqSave> ...(+4) |

#### 任务调度/异步运行时
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `SWTC-la/xapi/src/task/futex.rs#sys_futex` | `api/src/imp/futex.rs#sys_futex` | 同板块同名 | Err, FutexKey::new, Ok, axtask::yield_now, current, current().task_ext ...(+24) | Err, Ok, axtask::yield_now, curr.task_ext, curr.task_ext().process_data, current ...(+16) |
| `SWTC/kernel/src/syscall/signal.rs#sys_kill` | `api/src/imp/signal.rs#sys_kill` | 同板块同名 | Err, Ok, PROCESS_GROUP_MANAGER.get_group_by_pgid, PROCESS_MANAGER.for_each, PROCESS_MANAGER.get, SumGuard::new ...(+10) | Err, Ok, curr.task_ext, curr.task_ext().thread.process, curr.task_ext().thread.process().group, current ...(+9) |
| `SWTC/kernel/src/syscall/signal.rs#sys_rt_sigaction` | `api/src/imp/signal.rs#sys_rt_sigaction` | 同板块同名 | Err, KSigAction::new, Ok, SumGuard::new, UserCheck::new, UserCheck::new()
            .check_readable_slice ...(+16) | (*act).try_into, Err, Ok, actions[signo].to_ctype, check_sigset_size, curr.task_ext ...(+6) |
| `SWTC-la/arceos/modules/axlog/src/lib.rs#log` | `arceos/modules/axlog/src/lib.rs#log` | 同板块同名同结构 | cfg_if::cfg_if!, record.level, record.line, record.line().unwrap_or, record.metadata, record.target ...(+1) | cfg_if::cfg_if!, record.level, record.line, record.line().unwrap_or, record.metadata, record.target ...(+1) |

#### 内存管理
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `SWTC/kernel/src/mm/memory_space/page_fault_handler.rs#handle_page_fault` | `src/mm.rs#handle_page_fault` | 同板块同名 | Arc::new, Arc::strong_count, Err, Ok, PageBuilder::new, PageBuilder::new()
                            .permission ...(+33) | curr
        .task_ext, curr
        .task_ext()
        .process_data, curr
        .task_ext()
        .process_data()
        .aspace
        .lock, curr
        .task_ext()
        .process_data()
        .aspace
        .lock()
        .handle_page_fault, current, do_exit ...(+2) |
| `SWTC/kernel/src/mm/memory_space/mod.rs#map_elf` | `core/src/mm.rs#map_elf` | 同板块同名 | ((ph.virtual_addr() + ph.mem_size()) as usize + offset.0).into, (ph.virtual_addr() as usize + offset.0).into, Some, VmArea::new, debug!, elf.program_header ...(+39) | ELFParser::new, ELFParser::new(
        elf,
        axconfig::plat::USER_INTERP_BASE,
        Some(uspace_base as isize),
        uspace_base,
    )
    .map_err, Ok, Some, assert_eq!, debug! ...(+12) |
| `SWTC-la/xapi/src/mm/mmap.rs#sys_mmap` | `api/src/imp/mm/mmap.rs#sys_mmap` | 同板块同名 | (addr + length).align_up, Err, FD_TABLE.is_assigned, File::from_fd, File::from_fd(fd, FileFlags::READ, FileFlags::empty())
            .map_err, FileFlags::empty ...(+39) | Err, File::from_fd, MmapFlags::from_bits_truncate, MmapProt::from_bits_truncate, Ok, VirtAddr::from ...(+27) |
| `SWTC-la/arceos/modules/axruntime/src/lib.rs#rust_main` | `arceos/modules/axruntime/src/lib.rs#rust_main` | 同板块同名 | INITED_CPUS.fetch_add, all_devices
                .block
                .take_one, all_devices
                .block
                .take_one()
                .expect, ax_println!, axdisplay::init_display, axdriver::init_drivers ...(+28) | INITED_CPUS.fetch_add, ax_println!, axdisplay::init_display, axdriver::init_drivers, axfs::init_filesystems, axhal::mem::memory_regions ...(+20) |

#### 中断/异常处理
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `SWTC-la/arceos/modules/axhal/src/arch/loongarch64/mod.rs#cpu_init` | `arceos/modules/axhal/src/arch/riscv/mod.rs#cpu_init` | 同板块同名 | euen::set_asxe, euen::set_fpe, euen::set_sxe, set_exception_entry_base | set_trap_vector_base |
| `SWTC-la/src/syscall.rs#handle_syscall` | `arceos/modules/axhal/src/arch/x86_64/syscall.rs#handle_syscall` | 同板块同名 | Sysno::new, debug!, err.code, handle_syscall_impl, result.unwrap_or_else, sysno
        .ok_or ...(+4) | crate::trap::handle_syscall |
| `SWTC-la/arceos/modules/axhal/src/platform/riscv64_visionfive2/irq.rs#register_handler` | `arceos/modules/axhal/src/platform/riscv64_visionfive2/irq.rs#register_handler` | 同板块同名同结构 | debug!, with_cause! | debug!, with_cause! |
| `SWTC-la/arceos/modules/axhal/src/platform/riscv64_visionfive2/irq.rs#dispatch_irq` | `arceos/modules/axhal/src/platform/riscv64_qemu_virt/irq.rs#dispatch_irq` | 同板块同名 | with_cause! | with_cause! |

#### 系统调用
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `SWTC/kernel/src/net/udp.rs#connect` | `arceos/ulib/axlibc/src/net.rs#connect` | 同板块同名 | Box::pin, Err, IpListenEndpoint::from, NET_INTERFACE.poll, NET_INTERFACE.udp_socket, Ok ...(+12) | e, sys_connect |
| `SWTC/kernel/src/syscall/resource.rs#sys_prlimit64` | `api/src/imp/resources.rs#sys_prlimit64` | 同板块同名 | Err, Ok, RLimit::get_rlimit, RLimit::set_rlimit, SumGuard::new, UserCheck::new ...(+7) | Ok, ResourceLimitType::try_from, ResourceLimitType::try_from(resource as u32).map_err, do_getrlimit, do_setrlimit, new_limit.get_as_ref ...(+4) |
| `SWTC-la/xapi/src/ipc/shm.rs#sys_shmctl` | `api/src/imp/ipc/shm.rs#sys_shmctl` | 同板块同名 | Err, IPC_MANAGER.with_shm, Ok, buf.is_null, nullable!, segment.lock ...(+5) | Err, Ok, SHM_MANAGER.lock, buf.get_as_mut, monotonic_time_nanos, nullable! ...(+3) |
| `SWTC/kernel/src/net/tcp.rs#listen` | `arceos/ulib/axlibc/src/net.rs#listen` | 同板块同名 | NET_INTERFACE.poll, NET_INTERFACE.tcp_socket, Ok, info!, self.inner.lock, socket.listen ...(+3) | e, sys_listen |

#### 文件系统
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `SWTC/kernel/src/syscall/fs.rs#sys_renameat2` | `api/src/imp/fs/ctl.rs#sys_renameat2` | 同板块同名 | Arc::downgrade, Err, HashKey::new, INODE_CACHE.0.lock, INODE_CACHE.remove, Ok ...(+70) | Err, Ok, RenameFlags::from_bits_truncate, axfs::api::metadata, axfs::api::metadata(&new_path).is_ok, axfs::api::rename ...(+5) |
| `SWTC/kernel/src/syscall/fs.rs#sys_utimensat` | `api/src/imp/fs/ctl.rs#sys_utimensat` | 同板块同名 | Err, Ok, SumGuard::new, UserCheck::new, UserCheck::new()
                .check_readable_slice, core::mem::size_of::<TimeSpec> ...(+10) | Ok, Some, atime.into, atime.is_none, file.atime.lock, file.downcast_ref::<File> ...(+16) |
| `SWTC/kernel/src/syscall/fs.rs#sys_mkdirat` | `api/src/imp/fs/ctl.rs#sys_mkdirat` | 同板块同名 | <dyn Inode>::lookup_from_root, <dyn Inode>::lookup_from_root(&parent_path)?.0.unwrap, Err, Ok, current_time_spec, debug! ...(+11) | Ok, axfs::api::create_dir, debug!, handle_file_path, path.as_str, path.get_as_str ...(+1) |
| `SWTC-la/arceos/modules/axhal/src/platform/loongarch64_qemu_virt/time.rs#init_early` | `arceos/modules/axhal/src/platform/aarch64_common/generic_timer.rs#init_early` | 同板块同名 | ((base + SYS_RTCCTRL) as *mut u32).write_volatile, ((base + SYS_TOY_READ0) as *const u32).read_volatile, ((base + SYS_TOY_READ1) as *const u32).read_volatile, Utc
            .with_ymd_and_hms, Utc
            .with_ymd_and_hms(
                1900 + year as i32,
                extract_bits(value, 26..32),
                extract_bits(value, 21..26),
                extract_bits(value, 16..21),
                extract_bits(value, 10..16),
                extract_bits(value, 4..10),
            )
            .unwrap, Utc
            .with_ymd_and_hms(
                1900 + year as i32,
                extract_bits(value, 26..32),
                extract_bits(value, 21..26),
                extract_bits(value, 16..21),
                extract_bits(value, 10..16),
                extract_bits(value, 4..10),
            )
            .unwrap()
            .with_nanosecond ...(+9) | CNTFRQ_EL0.get, CNTPCT_TO_NANOS_RATIO.inverse, Ratio::new, Rtc::new, current_ticks, pa! ...(+4) |

#### 设备驱动
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `SWTC-la/arceos/modules/axdriver/src/lib.rs#init_drivers` | `arceos/modules/axdriver/src/lib.rs#init_drivers` | 同板块同名 | AllDevices::default, all_devs.block.iter, all_devs.block.iter().enumerate, all_devs.display.iter, all_devs.display.iter().enumerate, all_devs.net.iter ...(+5) | AllDevices::default, all_devs.block.iter, all_devs.block.iter().enumerate, all_devs.display.iter, all_devs.display.iter().enumerate, all_devs.net.iter ...(+5) |
| `SWTC-la/arceos/modules/axdriver/src/virtio.rs#probe_pci` | `arceos/modules/axdriver/src/virtio.rs#probe_pci` | 同板块同名同结构 | D::try_new, Some, axdriver_virtio::probe_pci_device::<VirtIoHalImpl>, warn! | D::try_new, Some, axdriver_virtio::probe_pci_device::<VirtIoHalImpl>, warn! |
| `SWTC/kernel/src/driver/fu740/sdcard.rs#write_block` | `arceos/modules/axdriver/src/vf2.rs#write_block` | 同板块同名 | Err, Ok, buf.len, self.end_cmd, self.get_resp, self.send_cmd ...(+1) | Ok, self.0.lock, self.0.lock().write_block |
| `SWTC/kernel/src/driver/fu740/sdcard.rs#read_block` | `arceos/modules/axdriver/src/vf2.rs#read_block` | 同板块同名 | Err, Ok, buf.len, self.end_cmd, self.get_resp, self.read_data ...(+1) | Ok, self.0.lock, self.0.lock().read_block |

#### 网络协议栈
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `SWTC-la/xcore/src/net/socket.rs#sendto` | `arceos/api/arceos_posix_api/src/imp/net.rs#sendto` | 同板块同名 | Err, Ipv4Addr::LOCALHOST.into, Ok, SocketAddr::new, inner
                    .bind, inner
                    .bind(SocketAddr::new(Ipv4Addr::LOCALHOST.into(), 0))
                    .ok ...(+2) | Err, Ok, udpsocket.lock, udpsocket.lock().send_to |
| `SWTC-la/xcore/src/net/socket.rs#recvfrom` | `arceos/api/arceos_posix_api/src/imp/net.rs#recvfrom` | 同板块同名 | Ok, Some, tcpsocket.lock, tcpsocket.lock().recv, tcpsocket.lock().recv(buf).map, udpsocket
                .lock ...(+5) | Ok, Some, tcpsocket.lock, tcpsocket.lock().recv, tcpsocket.lock().recv(buf).map, udpsocket
                .lock ...(+2) |
| `SWTC-la/xcore/src/net/socket.rs#accept` | `arceos/api/arceos_posix_api/src/imp/net.rs#accept` | 同板块同名 | Err, Mutex::new, Socket::Tcp, Socket::Unix, tcpsocket
                .lock, tcpsocket
                .lock()
                .accept ...(+4) | Err, Ok, tcpsocket.lock, tcpsocket.lock().accept |
| `SWTC-la/xcore/src/net/socket.rs#connect` | `arceos/ulib/axstd/src/net/tcp.rs#connect` | 同板块同名 | Err, Ok, tcpsocket.lock, tcpsocket.lock().connect, udpsocket.lock, udpsocket.lock().connect | Ok, TcpStream, api::ax_tcp_connect, api::ax_tcp_socket, super::each_addr |

#### 文件系统/第三方库
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `SWTC-la/arceos/crates/lwext4_rust/c/lwext4/src/ext4_balloc.c#ext4_balloc_alloc_block` | `crates/lwext4_rust/c/lwext4/src/ext4_balloc.c#ext4_balloc_alloc_block` | 同板块同名同结构 | ext4_balloc_get_bgid_of_block, ext4_balloc_get_block_of_bgid, ext4_balloc_set_bitmap_csum, ext4_balloc_verify_bitmap_csum, ext4_bg_get_block_bitmap, ext4_bg_get_free_blocks_count ...(+19) | ext4_balloc_get_bgid_of_block, ext4_balloc_get_block_of_bgid, ext4_balloc_set_bitmap_csum, ext4_balloc_verify_bitmap_csum, ext4_bg_get_block_bitmap, ext4_bg_get_free_blocks_count ...(+19) |
| `SWTC-la/arceos/crates/lwext4_rust/c/lwext4/src/ext4_journal.c#jbd_journal_prepare` | `crates/lwext4_rust/c/lwext4/src/ext4_journal.c#jbd_journal_prepare` | 同板块同名同结构 | EXT4_BLOCK_ZERO, RB_FIND, TAILQ_FOREACH_REVERSE_SAFE, TAILQ_FOREACH_SAFE, TAILQ_REMOVE, ext4_bcache_clear_dirty ...(+17) | EXT4_BLOCK_ZERO, RB_FIND, TAILQ_FOREACH_REVERSE_SAFE, TAILQ_FOREACH_SAFE, TAILQ_REMOVE, ext4_bcache_clear_dirty ...(+17) |
| `SWTC-la/arceos/crates/lwext4_rust/c/lwext4/src/ext4.c#ext4_dir_rm` | `crates/lwext4_rust/c/lwext4/src/ext4.c#ext4_dir_rm` | 同板块同名同结构 | EXT4_MP_LOCK, EXT4_MP_UNLOCK, ext4_block_cache_write_back, ext4_dir_en_get_inode, ext4_dir_iterator_fini, ext4_dir_iterator_init ...(+19) | EXT4_MP_LOCK, EXT4_MP_UNLOCK, ext4_block_cache_write_back, ext4_dir_en_get_inode, ext4_dir_iterator_fini, ext4_dir_iterator_init ...(+19) |
| `SWTC-la/arceos/crates/lwext4_rust/c/lwext4/src/ext4_dir_idx.c#ext4_dir_dx_split_index` | `crates/lwext4_rust/c/lwext4/src/ext4_dir_idx.c#ext4_dir_dx_split_index` | 同板块同名同结构 | ext4_block_set, ext4_dir_dx_climit_get_count, ext4_dir_dx_climit_get_limit, ext4_dir_dx_climit_set_count, ext4_dir_dx_climit_set_limit, ext4_dir_dx_entry_get_hash ...(+10) | ext4_block_set, ext4_dir_dx_climit_get_count, ext4_dir_dx_climit_get_limit, ext4_dir_dx_climit_set_count, ext4_dir_dx_climit_set_limit, ext4_dir_dx_entry_get_hash ...(+10) |

#### 网络协议栈/第三方库
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `SWTC-la/arceos/modules/axnet/src/smoltcp_impl/tcp.rs#connect` | `arceos/modules/axnet/src/smoltcp_impl/tcp.rs#connect` | 同板块同名 | Err, Ok, Ok::<(IpEndpoint, IpEndpoint), NetError>, SOCKET_SET
                .with_socket_mut::<tcp::Socket, _, _>, SOCKET_SET.add, SocketSetWrapper::new_tcp_socket ...(+33) | Err, Ok, Ok::<(IpEndpoint, IpEndpoint), AxError>, SOCKET_SET
                .with_socket_mut::<tcp::Socket, _, _>, SOCKET_SET.add, SocketSetWrapper::new_tcp_socket ...(+32) |
| `SWTC-la/arceos/crates/smoltcp/src/storage/assembler.rs#add` | `arceos/modules/axnet/src/smoltcp_impl/mod.rs#add` | 同板块同名 | Contig::hole_and_data, Err, Ok, contig.has_data, contig.shrink_hole_to, contig.total_size ...(+11) | debug!, self.0.lock, self.0.lock().add |
| `SWTC-la/arceos/crates/smoltcp/src/socket/tcp.rs#connect` | `arceos/modules/axnet/src/smoltcp_impl/udp.rs#connect` | 同板块同名 | Err, Ok, Self::random_seq_no, Some, addr.is_unspecified, cx
                    .get_source_address ...(+9) | Ok, Some, debug!, from_core_sockaddr, into_core_sockaddr, self.bind ...(+3) |
| `SWTC-la/arceos/crates/smoltcp/src/socket/tcp.rs#send_impl` | `arceos/modules/axnet/src/smoltcp_impl/udp.rs#send_impl` | 同板块同名 | Err, Ok, f, self.may_send, self.rtte.retransmission_timeout, self.timer.is_idle ...(+3) | Err, Ok, SOCKET_SET.with_socket_mut::<udp::Socket, _, _>, ax_err!, ax_err_type!, buf.len ...(+7) |

#### 第三方库
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `SWTC-la/arceos/crates/axdriver_crates/axdriver_net/src/ixgbe.rs#receive` | `crates/axdriver_net/src/ixgbe.rs#receive` | 同板块同名同结构 | Err, NetBufPtr::from, Ok, panic!, self.can_receive, self.inner.receive_packets ...(+4) | Err, NetBufPtr::from, Ok, panic!, self.can_receive, self.inner.receive_packets ...(+4) |
| `SWTC-la/arceos/crates/axdriver_crates/axdriver_net/src/fxmac.rs#receive` | `crates/axdriver_net/src/fxmac.rs#receive` | 同板块同名同结构 | Box::into_raw, Box::new, Err, FXmacRecvHandler, NetBufPtr::new, NonNull::new ...(+10) | Box::into_raw, Box::new, Err, FXmacRecvHandler, NetBufPtr::new, NonNull::new ...(+10) |
| `SWTC-la/arceos/crates/axdriver_crates/axdriver_virtio/src/net.rs#recycle_rx_buffer` | `crates/axdriver_net/src/fxmac.rs#recycle_rx_buffer` | 同板块同名 | Err, NetBuf::from_buf_ptr, Ok, Some, rx_buf.raw_buf_mut, self.inner
                .receive_begin ...(+2) | Box::from_raw, Ok, drop, rx_buf.raw_ptr::<u8> |
| `SWTC/dependencies/heapless/src/pool/mod.rs#alloc` | `crates/axdriver_net/src/net_buf.rs#alloc` | 同板块同名 | Ptr::dangling, Some, mem::size_of::<T>, self.stack.try_pop | Arc::clone, NonNull::new, NonNull::new(self.pool.as_ptr().add(pool_offset) as *mut u8).unwrap, Some, self.free_list.lock, self.free_list.lock().pop ...(+2) |

#### 通用模块/其他
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `SWTC-la/arceos/modules/axhal/src/mem.rs#kernel_image_regions` | `arceos/modules/axhal/src/mem.rs#kernel_image_regions` | 同板块同名 | (_sbss as *const () as usize).into, (_sdata as *const () as usize).into, (_srodata as *const () as usize).into, (_stext as *const () as usize).into, (boot_stack as *const () as usize).into, [
        MemRegion {
            paddr: virt_to_phys((_stext as *const () as usize).into()),
            size: _etext as *const () as usize - _stext as *const () as usize,
            flags: MemRegionFlags::RESERVED | MemRegionFlags::READ | MemRegionFlags::EXECUTE,
            name: ".text",
        },
        MemRegion {
            paddr: virt_to_phys((_srodata as *const () as usize).into()),
            size: _erodata as *const () as usize - _srodata as *const () as usize,
            flags: MemRegionFlags::RESERVED | MemRegionFlags::READ,
            name: ".rodata",
        },
        MemRegion {
            paddr: virt_to_phys((_sdata as *const () as usize).into()),
            size: _edata as *const () as usize - _sdata as *const () as usize,
            flags: MemRegionFlags::RESERVED | MemRegionFlags::READ | MemRegionFlags::WRITE,
            name: ".data .tdata .tbss .percpu",
        },
        MemRegion {
            paddr: virt_to_phys((boot_stack as *const () as usize).into()),
            size: boot_stack_top as *const () as usize - boot_stack as *const () as usize,
            flags: MemRegionFlags::RESERVED | MemRegionFlags::READ | MemRegionFlags::WRITE,
            name: "boot stack",
        },
        MemRegion {
            paddr: virt_to_phys((_sbss as *const () as usize).into()),
            size: _ebss as *const () as usize - _sbss as *const () as usize,
            flags: MemRegionFlags::RESERVED | MemRegionFlags::READ | MemRegionFlags::WRITE,
            name: ".bss",
        },
    ]
    .into_iter ...(+1) | (_sbss as usize).into, (_sdata as usize).into, (_srodata as usize).into, (_stext as usize).into, (boot_stack as usize).into, [
        MemRegion {
            paddr: virt_to_phys((_stext as usize).into()),
            size: _etext as usize - _stext as usize,
            flags: MemRegionFlags::RESERVED | MemRegionFlags::READ | MemRegionFlags::EXECUTE,
            name: ".text",
        },
        MemRegion {
            paddr: virt_to_phys((_srodata as usize).into()),
            size: _erodata as usize - _srodata as usize,
            flags: MemRegionFlags::RESERVED | MemRegionFlags::READ,
            name: ".rodata",
        },
        MemRegion {
            paddr: virt_to_phys((_sdata as usize).into()),
            size: _edata as usize - _sdata as usize,
            flags: MemRegionFlags::RESERVED | MemRegionFlags::READ | MemRegionFlags::WRITE,
            name: ".data .tdata .tbss .percpu",
        },
        MemRegion {
            paddr: virt_to_phys((boot_stack as usize).into()),
            size: boot_stack_top as usize - boot_stack as usize,
            flags: MemRegionFlags::RESERVED | MemRegionFlags::READ | MemRegionFlags::WRITE,
            name: "boot stack",
        },
        MemRegion {
            paddr: virt_to_phys((_sbss as usize).into()),
            size: _ebss as usize - _sbss as usize,
            flags: MemRegionFlags::RESERVED | MemRegionFlags::READ | MemRegionFlags::WRITE,
            name: ".bss",
        },
    ]
    .into_iter ...(+1) |
| `SWTC/kernel/src/utils/radix_tree.rs#insert` | `api/src/imp/ipc/util.rs#insert` | 同板块同名 | Arc::new, RadixTreeInternalNode::new, RadixTreeNode::InternalNode, RadixTreeNode::LeafNode, Some, children[*index].as_ref ...(+11) | key.clone, self.forward.insert, self.forward.remove, self.reverse.insert, self.reverse.remove, value.clone |
| `SWTC-la/arceos/modules/axsync/src/mutex.rs#lock` | `arceos/modules/axsync/src/mutex.rs#lock` | 同板块同名同结构 | assert_ne!, current, current().id, current().id().as_u64, self.is_locked, self.owner_id.compare_exchange_weak ...(+1) | assert_ne!, current, current().id, current().id().as_u64, self.is_locked, self.owner_id.compare_exchange_weak ...(+1) |
| `SWTC/user/src/lib.rs#_start` | `apps/nimbos/rust/src/lib.rs#_start` | 同板块同名 | ((argv + i * core::mem::size_of::<usize>()) as *const usize).read_volatile, ((str_start + *i) as *const u8).read_volatile, (0usize..)
            .find, (0usize..)
            .find(|i| unsafe { ((str_start + *i) as *const u8).read_volatile() == 0 })
            .unwrap, HEAP.lock, HEAP.lock()
            .init ...(+10) | exit, main |


好的，我将遵循审计指令，基于提供的客观证据，对本次同源性争议进行最终定性仲裁。

### 1. 宏观判定
- **定性结论**: **独立开发，存在共同上游依赖**
  整体客观 AST 相似度仅为 **12.2%**，远低于构成“实质性相似”的阈值。目标项目覆盖率仅 **15.3%**，意味着超过 **84.7%** 的代码库是独立构建的，其唯一 AST 指纹高达 **6404**，远超参考项目的 **2610**，表明其规模与复杂度均显著高于参考项目。双方代码的重合部分高度集中于 `arceos` 前缀的路径及第三方库，这强烈指向双方共同依赖了 **ArceOS 开源操作系统框架**及若干公共第三方 crate，而非目标项目对参考项目的单向抄袭。

### 2. 全模块覆盖矩阵解读
- **进程管理**
  - 双方均存在该模块。
  - 存在共同 AST 指纹。
  - 目标覆盖率 10.3%，参考覆盖率 42.47%。目标项目函数量（323）远超参考项目（79），极低的覆盖率说明目标项目在本模块进行了大规模独立扩展，重合点仅限于少数 ArceOS 上游任务管理原语。
  - **证据强度：弱**。控方所指控的“核心铁证”均为 ArceOS 上游代码或极简函数。

- **任务调度/异步运行时**
  - 双方均存在该模块。
  - 存在共同 AST 指纹。
  - 目标覆盖率 29.68%，参考覆盖率 43.27%。覆盖率相对较高，但证据显示重合点主要是 `axtask` 等 ArceOS 核心调度框架的代码。
  - **证据强度：弱**。重合源于共同依赖的 ArceOS 运行时，属于平台代码。

- **内存管理**
  - 双方均存在该模块。
  - 存在共同 AST 指纹。
  - 目标覆盖率 17.81%，参考覆盖率 38.24%。目标项目函数量（624）是参考项目（301）的两倍以上，低覆盖率表明独立实现占主导。
  - **证据强度：弱**。证据显示双方在 `handle_page_fault` 等关键路径上调用图谱差异巨大，架构不同。

- **中断/异常处理**
  - 双方均存在该模块。
  - 存在共同 AST 指纹。
  - 目标覆盖率 33.85%，参考覆盖率 43.14%。覆盖率较高，但证据指向平台初始化及中断分发等与硬件架构绑定的代码。
  - **证据强度：弱**。`cpu_init` 等函数因目标项目适配 LoongArch 架构，参考项目适配 RISC-V，其实现必然不同，重合部分为架构无关的公共逻辑或 ArceOS 框架代码。

- **系统调用**
  - 双方均存在该模块。
  - 存在共同 AST 指纹。
  - 目标覆盖率 8.91%，参考覆盖率 8.41%。覆盖率极低，表明双方系统调用层的实现几乎完全不同。
  - **证据强度：未形成**。极低的覆盖率本身就是最强反证。证据显示目标项目（`SWTC/kernel`）与参考项目（`api`）的实现路径、调用对象完全不同。

- **文件系统**
  - 双方均存在该模块。
  - 存在共同 AST 指纹。
  - 目标覆盖率 5.91%，参考覆盖率 9.69%。覆盖率极低，表明文件系统核心逻辑高度独立。
  - **证据强度：未形成**。证据显示双方在 `sys_renameat2` 等复杂系统调用上实现路径完全不同。

- **设备驱动**
  - 双方均存在该模块。
  - 存在共同 AST 指纹。
  - 目标覆盖率 34.23%，参考覆盖率 26.76%。覆盖率较高，但证据指向 VirtIO 等标准驱动框架的公共实现。
  - **证据强度：弱**。重合点如 `virtio.rs#probe_pci` 是 ArceOS 驱动框架的标准代码。

- **网络协议栈**
  - 双方均存在该模块。
  - 存在共同 AST 指纹。
  - 目标覆盖率 8.0%，参考覆盖率 7.84%。覆盖率极低，表明网络协议栈实现高度独立。
  - **证据强度：未形成**。证据显示双方在 socket 操作上调用图谱差异显著。

- **第三方库/公共依赖**
  - 双方均存在该模块。
  - 存在共同 AST 指纹。
  - 文件系统/第三方库覆盖率高达 72.38%/85.89%，网络协议栈/第三方库、第三方库模块也存在不同程度重合。
  - **证据强度：强（但已降权）**。这些高覆盖率模块是 `lwext4_rust`、`smoltcp`、`axdriver` 等第三方或上游 crate 的直接引用，是合法复用，不能作为指控抄袭的证据。

### 3. 底层 AST 关键证据指纹表
| 所属板块 | 目标项目路径 | 参考项目路径 | 证据类型 | 技术说明 |
| :--- | :--- | :--- | :--- | :--- |
| 第三方库/公共依赖 | `SWTC-la/arceos/crates/lwext4_rust/...` | `crates/lwext4_rust/...` | 同源引用 | 双方均完整引用了 `lwext4_rust` 第三方库，AST 指纹必然一致，属于合法复用。 |
| 第三方库/公共依赖 | `SWTC-la/arceos/crates/smoltcp/...` | `arceos/modules/axnet/...` | 同源引用 | 双方均依赖 `smoltcp` 协议栈，尽管引用方式（crate vs module）不同，但核心逻辑同源。 |
| 第三方库/公共依赖 | `SWTC-la/arceos/crates/axdriver_crates/...` | `crates/axdriver_net/...` | 同源引用 | 双方均引用了 ArceOS 体系的驱动 crate，如 `ixgbe`、`fxmac`，代码结构一致。 |
| 进程管理 | `SWTC-la/arceos/modules/axtask/src/run_queue.rs#exit_current` | `arceos/modules/axtask/src/run_queue.rs#exit_current` | 上游框架 | 任务退出逻辑完全一致，源于共同依赖的 ArceOS 任务调度框架，非抄袭。 |
| 进程管理 | `SWTC-la/arceos/modules/axtask/src/wait_queue.rs#wait` | `arceos/modules/axtask/src/wait_queue.rs#wait` | 上游框架 | 等待队列核心机制，是 ArceOS 提供的标准同步原语实现。 |
| 进程管理 | `SWTC-la/xcore/src/task/proc.rs#get_process_group` | `core/src/task.rs#get_process_group` | 通用惯用法 | `RwLock<HashMap>` 的标准查找模式，是操作系统内核中进程组查询的唯一自然实现。 |
| 进程管理 | `SWTC-la/xcore/src/task/proc.rs#set_clear_child_tid` | `core/src/task.rs#set_clear_child_tid` | 极简函数 | 仅包含一句原子变量的 `store` 操作，无独创性，任何实现者都会写出相同代码。 |
| 通用模块/其他 | `SWTC-la/arceos/modules/axhal/src/mem.rs#kernel_image_regions` | `arceos/modules/axhal/src/mem.rs#kernel_image_regions` | 上游框架 | 内核镜像内存区域描述，是 ArceOS 平台标准代码，用于向内存管理器注册内核占用段。 |
| 通用模块/其他 | `SWTC-la/arceos/modules/axsync/src/mutex.rs#lock` | `arceos/modules/axsync/src/mutex.rs#lock` | 上游框架 | ArceOS 自旋锁实现，包含基于任务 ID 的防死锁断言，是框架标准实现。 |

### 4. 核心代码调用流对比剖析
* **【进程管理/进程退出路径】**
* **底层技术析理**: 目标项目的 `do_exit` 函数展现了比参考项目更复杂的清理逻辑。参考项目主要关注 `clear_child_tid` 和文件描述符表清理，而目标项目在此基础上增加了 `FutexKey::new` 和 `IPC_MANAGER.clear`，表明其进程间通信和同步机制更为丰富。两者虽共享 `axtask::exit` 等底层框架调用，但上层的资源回收策略和抽象层次已出现显著分叉。
* **代码溯源链接**:
  - 目标项目: `SWTC-la/xapi/src/task/exit.rs#do_exit`
  - 参考项目: `api/src/imp/task/exit.rs#do_exit`

* **【进程管理/信号处理路径】**
* **底层技术析理**: 在 `sys_rt_sigprocmask` 的实现中，目标项目采用了显式的用户态内存安全检查模型（`UserCheck::new().check_writable_slice`），这是一种强调安全隔离的设计。参考项目则通过 `current().task_ext().thread_data().signal.with_blocked_mut` 直接操作内核数据结构，路径更短。这体现了双方在系统调用层面安全校验架构的根本不同。
* **代码溯源链接**:
  - 目标项目: `SWTC/kernel/src/syscall/signal.rs#sys_rt_sigprocmask`
  - 参考项目: `api/src/imp/signal.rs#sys_rt_sigprocmask`

* **【内存管理/缺页异常处理】**
* **底层技术析理**: 目标项目的 `handle_page_fault` 函数直接构建 `PageBuilder` 并操作页表，逻辑内聚。参考项目则通过 `current().task_ext().process_data().aspace.lock().handle_page_fault` 的链式调用，将处理逻辑委托给地址空间对象。这反映了双方在内存管理子系统上采用了不同的职责划分和抽象模型。
* **代码溯源链接**:
  - 目标项目: `SWTC/kernel/src/mm/memory_space/page_fault_handler.rs#handle_page_fault`
  - 参考项目: `src/mm.rs#handle_page_fault`

### 5. 单方模块与降权项
- **单方模块**: 目标项目中的 `SWTC/kernel/src/syscall/` 路径下的系统调用实现（如 `sys_exit`, `sys_getpid`）与参考项目的 `api/src/imp/` 路径下的实现在架构分层、调用对象上完全不同，属于目标项目的独立实现，是证明独立开发的关键证据。
- **降权项**:
  - **第三方库/公共依赖**: `lwext4_rust`, `smoltcp`, `axdriver_*`, `heapless` 等模块的高 AST 覆盖率是引用公共库的必然结果，已从核心证据中降权。
  - **极简函数**: `set_in_wait_queue`, `set_clear_child_tid`, `notify_all`, `busy_wait_until` 等函数因实现空间极小，其相似性不构成独创性证据。
  - **跨板块同名**: 控方将 `xsignal::wait_timeout` 与 `axtask::wait_timeout` 等不同子系统、不同语义的函数进行配对，属于硬凑证据，已全部降权为弱信号或不予采纳。
  - **通用宏**: `debug!`, `info!`, `nullable!`, `stack_trace!` 等宏的调用是 Rust 生态和项目内部的通用实践，不能作为同源性指纹。

### 6. 保守结论
综合全模块证据，目标项目与参考项目的客观 AST 相似度仅为 12.2%，远未达到可判定为“抄袭”或“实质性相似”的水平。双方代码的重合部分，经逐条技术剖析，可明确归因于对 **ArceOS 开源操作系统框架**及多个第三方公共 crate 的共同依赖，以及少量操作系统底层原语的唯一自然实现。在核心的进程管理、内存管理、系统调用等子系统上，双方展现出了截然不同的架构设计、安全模型和抽象层次。目标项目规模更大、功能更全，其独立开发的代码（唯一 AST 指纹 6404）占绝对主导地位。因此，本次审计未发现支持“抄袭”指控的有效技术证据，项目应被视为基于开源生态的独立开发作品。

------------------------------

