# 🛡️ OS 内核赛自动化代码级架构审计报告

**审计时间**: `2026-07-02 03:49:49`
**参赛作品名称**: `2026-T2026102569910192-XX`
**年份**: `2026`
**学校**: `XX`
**队伍名称**: `T2026102569910192`
**比赛名字**: `OS-Competition`
**比赛子赛道**: `Kernel-Implementation`
**仓库地址**: `https://gitlab.eduxiji.net/educg-group-43501-3132633/T2026102569910192-3896`
**推测开发语言**: `Rust / C (由AST探针自动识别)`
**特征函数量**: `8575`

## ⚔️ 底层代码同源性对比 (AST Homology)
> 本部分由专门的代码审查智能体，对该项目与符合年份条件的历届最高相似度项目进行控制流与底层调用对比。新版报告会先给出确定性的全模块覆盖矩阵，再给出 Agent 的分模块解释，避免只围绕少量抽样函数展开。

### 🏆 Top 2 底层对标: vs `2025-StarryX-杭州电子科技大学`（杭州电子科技大学 / StarryX）

#### 对方作品信息

**对方年份**: `2025`
**对方学校**: `杭州电子科技大学`
**对方队伍名称**: `StarryX`
**对方比赛名字**: `OS-Competition`
**对方比赛子赛道**: `Kernel-Implementation`
**对方仓库地址**: `https://gitlab.eduxiji.net/educg-group-36002-2710490/T202510336995486-3037`

> 双向覆盖率：共同 AST 指纹: 1981；唯一 AST 指纹: 目标项目 6404，参考项目 2897；覆盖率: 目标项目 30.93%，参考项目 68.38%

## 全模块对比总览（确定性证据）

> 总体覆盖率：共同 AST 指纹: 1981；唯一 AST 指纹: 目标项目 6404，参考项目 2897；覆盖率: 目标项目 30.93%，参考项目 68.38%

### 1. 模块覆盖矩阵
| 模块 | 目标函数数 | 参考函数数 | 共同唯一 AST 指纹 | Target 覆盖 | Base 覆盖 | 状态 |
| :--- | ---: | ---: | ---: | ---: | ---: | :--- |
| 进程管理 | 323 | 172 | 144 | 47.84% | 90.57% | 双方存在且有结构重合 |
| 任务调度/异步运行时 | 478 | 480 | 251 | 62.59% | 71.51% | 双方存在且有结构重合 |
| 内存管理 | 624 | 430 | 278 | 47.6% | 71.47% | 双方存在且有结构重合 |
| 中断/异常处理 | 73 | 70 | 38 | 58.46% | 61.29% | 双方存在且有结构重合 |
| 系统调用 | 116 | 89 | 15 | 14.85% | 18.75% | 双方存在且有结构重合 |
| 文件系统 | 907 | 801 | 432 | 57.99% | 69.57% | 双方存在且有结构重合 |
| 设备驱动 | 145 | 139 | 49 | 44.14% | 38.89% | 双方存在且有结构重合 |
| 网络协议栈 | 61 | 77 | 13 | 26.0% | 21.31% | 双方存在且有结构重合 |
| 文件系统/第三方库 | 674 | 164 | 148 | 25.87% | 94.27% | 双方存在且有结构重合 |
| 网络协议栈/第三方库 | 2599 | 166 | 132 | 6.64% | 91.67% | 双方存在且有结构重合 |
| 第三方库 | 2158 | 349 | 292 | 19.77% | 99.66% | 双方存在且有结构重合 |
| 通用模块/其他 | 417 | 665 | 210 | 56.3% | 39.62% | 双方存在且有结构重合 |

### 2. 分模块证据清单

#### 进程管理
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `SWTC-la/xcore/src/fs/vfs/proc/sys.rs#create_kernel_root` | `xcore/src/fs/vfs/proc/sys.rs#create_kernel_root` | 同板块同名同结构 | "0\n".to_string, "16384\n".to_string, "2097152\n".to_string, "250	32000	32	128\n".to_string, "32000\n".to_string, "33554432\n".to_string ...(+32) | "0\n".to_string, "16384\n".to_string, "2097152\n".to_string, "250	32000	32	128\n".to_string, "32000\n".to_string, "33554432\n".to_string ...(+32) |
| `SWTC-la/xapi/src/task/exit.rs#do_exit` | `xapi/src/task/exit.rs#do_exit` | 同板块同名 | FD_TABLE.clear, FutexKey::new, IPC_MANAGER.clear, SignalInfo::new, UserPtr::<Pid>::from, axtask::exit ...(+32) | FD_TABLE.clear, FutexKey::new, IPC_MANAGER.clear, SignalInfo::new, UserPtr::<Pid>::from, axtask::exit ...(+32) |
| `SWTC-la/xapi/src/task/wait.rs#sys_wait4` | `xapi/src/task/wait.rs#sys_wait4` | 同板块同名同结构 | Err, Ok, WaitOptions::from_bits, WaitPid::Pgid, WaitPid::Pid, child.data::<XProcess> ...(+26) | Err, Ok, WaitOptions::from_bits, WaitPid::Pgid, WaitPid::Pid, child.data::<XProcess> ...(+26) |
| `SWTC/kernel/src/syscall/signal.rs#sys_rt_sigprocmask` | `xapi/src/task/signal.rs#sys_rt_sigprocmask` | 同板块同名 | Err, Ok, SigSet::from_bits, SigSet::from_bits(*set as usize).ok_or, SumGuard::new, UserCheck::new ...(+9) | Err, Ok, XProcess::from_thread, XProcess::from_thread(thread).uspace, XThread::from_thread, check_sigset_size ...(+3) |

#### 任务调度/异步运行时
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `SWTC/kernel/src/syscall/fs.rs#sys_pselect6` | `xapi/src/iomux/select.rs#sys_pselect6` | 同板块同名 | Duration::from, Err, IOMultiplexFormat::FdSets, IOMultiplexFuture::new, Ok, PollEvents::POLLIN.bits ...(+48) | do_select, nullable!, nullable!(uspace.read(timeout))?
                .map, nullable!(uspace.read(timeout))?
                .map(timespec::to_time_value)
                .transpose, with_uspace |
| `SWTC-la/xapi/src/ipc/sem.rs#sys_semctl` | `xapi/src/ipc/sem.rs#sys_semctl` | 同板块同名同结构 | Err, with_ipc_manager! | Err, with_ipc_manager! |
| `SWTC-la/xapi/src/task/futex.rs#sys_futex` | `xapi/src/task/futex.rs#sys_futex` | 同板块同名同结构 | Err, FutexKey::new, Ok, axtask::yield_now, current, current().task_ext ...(+24) | Err, FutexKey::new, Ok, axtask::yield_now, current, current().task_ext ...(+24) |
| `SWTC/kernel/src/syscall/time.rs#sys_setitimer` | `xapi/src/sys/time.rs#sys_setitimer` | 同板块同名 | (current_time_duration() + next_timeout).into, Duration::ZERO.into, Duration::from, Duration::from(timer.it_value).is_zero, Err, Ok ...(+24) | Err, Ok, XProcess::from_thread, XProcess::from_thread(thread).uspace, clear_timer, new_value.it_interval.to_nanos ...(+7) |

#### 内存管理
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `SWTC-la/src/syscall.rs#handle_syscall_impl` | `src/syscall.rs#handle_syscall_impl` | 同板块同名 | Err, Ok, sys_accept, sys_accept4, sys_access, sys_arch_prctl ...(+199) | Err, Ok, sys_accept, sys_accept4, sys_access, sys_arch_prctl ...(+203) |
| `SWTC-la/xapi/src/task/clone.rs#do_clone` | `xapi/src/task/clone.rs#do_clone` | 同板块同名同结构 | Arc::default, Arc::new, CloneFlags::from_bits_truncate, Err, FD_TABLE
                .deref_from, FD_TABLE
                .deref_from(&process_data.ns)
                .init_new ...(+76) | Arc::default, Arc::new, CloneFlags::from_bits_truncate, Err, FD_TABLE
                .deref_from, FD_TABLE
                .deref_from(&process_data.ns)
                .init_new ...(+76) |
| `SWTC/kernel/src/mm/memory_space/page_fault_handler.rs#handle_page_fault` | `src/mm.rs#handle_page_fault` | 同板块同名 | Arc::new, Arc::strong_count, Err, Ok, PageBuilder::new, PageBuilder::new()
                            .permission ...(+33) | (xcore::config::USER_STACK_TOP - xcore::config::USER_STACK_SIZE
        ..xcore::config::USER_STACK_TOP)
        .contains, SignalInfo::new, Signo::from_repr, Signo::from_repr(SIGSEGV as u8).unwrap, XTaskExt::from_task, current ...(+17) |
| `SWTC/kernel/src/mm/memory_space/mod.rs#map_elf` | `xcore/src/mm/init.rs#map_elf` | 同板块同名 | ((ph.virtual_addr() + ph.mem_size()) as usize + offset.0).into, (ph.virtual_addr() as usize + offset.0).into, Some, VmArea::new, debug!, elf.program_header ...(+39) | ELFParser::new, ELFParser::new(
        elf,
        crate::config::USER_INTERP_BASE,
        Some(uspace_base as isize),
        uspace_base,
    )
    .map_err, Ok, Some, assert_eq!, debug! ...(+12) |

#### 中断/异常处理
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `SWTC-la/xmodules/xsignal/src/types.rs#default_action` | `xmodules/xsignal/src/types.rs#default_action` | 同板块同名同结构 | 内部闭环逻辑 | 内部闭环逻辑 |
| `SWTC/kernel/src/trap/ctx.rs#restore` | `xmodules/xsignal/src/arch/x86_64.rs#restore` | 同板块同名 | asm! | 内部闭环逻辑 |
| `SWTC-la/xcore/src/fs/vfs/dev/tty.rs#default_termios` | `xcore/src/fs/vfs/dev/tty.rs#default_termios` | 同板块同名同结构 | 内部闭环逻辑 | 内部闭环逻辑 |
| `SWTC-la/xmodules/xsignal/src/arch/x86_64.rs#restore` | `xmodules/xsignal/src/arch/loongarch64.rs#restore` | 同板块同名 | 内部闭环逻辑 | mem::transmute::<[u64; 32], GeneralRegisters> |

#### 系统调用
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `SWTC/kernel/src/net/udp.rs#connect` | `arceos/ulib/axlibc/src/net.rs#connect` | 同板块同名 | Box::pin, Err, IpListenEndpoint::from, NET_INTERFACE.poll, NET_INTERFACE.udp_socket, Ok ...(+12) | e, sys_connect |
| `SWTC-la/xapi/src/ipc/sem.rs#sys_semget` | `xapi/src/ipc/sem.rs#sys_semget` | 同板块同名 | Err, IPC_MANAGER.with_sem, Ok, current_pid, sem_manager.create_semset, sem_manager.get_semid_by_key ...(+3) | Err, IPC_MANAGER.with_sem, Ok, current_pid, sem_manager.create_semset, sem_manager.get_semid_by_key ...(+3) |
| `SWTC-la/xapi/src/ipc/shm.rs#sys_shmctl` | `xapi/src/ipc/shm.rs#sys_shmctl` | 同板块同名同结构 | Err, IPC_MANAGER.with_shm, Ok, buf.is_null, nullable!, segment.lock ...(+5) | Err, IPC_MANAGER.with_shm, Ok, buf.is_null, nullable!, segment.lock ...(+5) |
| `SWTC-la/xapi/src/sys/resource.rs#sys_getrusage` | `xapi/src/sys/resource.rs#sys_getrusage` | 同板块同名同结构 | Err, Ok, __kernel_old_timeval::from_nanos, nullable!, time_stat_output, trace! ...(+1) | Err, Ok, __kernel_old_timeval::from_nanos, nullable!, time_stat_output, trace! ...(+1) |

#### 文件系统
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `SWTC/kernel/src/syscall/fs.rs#sys_renameat2` | `xapi/src/fs/ctl.rs#sys_renameat2` | 同板块同名 | Arc::downgrade, Err, HashKey::new, INODE_CACHE.0.lock, INODE_CACHE.remove, Ok ...(+70) | Ok, Path::new, fs.resolve_nonexistent, fs.resolve_parent, new_path.into, old_dir.rename ...(+4) |
| `SWTC-la/xapi/src/fs/fd/fanotify.rs#sys_fanotify_mark` | `xapi/src/fs/fd/fanotify.rs#sys_fanotify_mark` | 同板块同名同结构 | Arc::downgrade, Arc::new, Err, FanEventMask::empty, FanEventMask::from_bits, FanEventMask::from_bits(mask).ok_or ...(+42) | Arc::downgrade, Arc::new, Err, FanEventMask::empty, FanEventMask::from_bits, FanEventMask::from_bits(mask).ok_or ...(+42) |
| `SWTC-la/xapi/src/iomux/epoll.rs#sys_epoll_wait` | `xapi/src/iomux/epoll.rs#sys_epoll_wait` | 同板块同名同结构 | Err, Ok, PollFd::new, Some, TimeValue::from_millis, Vec::new ...(+23) | Err, Ok, PollFd::new, Some, TimeValue::from_millis, Vec::new ...(+23) |
| `SWTC/kernel/src/fs/devfs/tty.rs#ioctl` | `xcore/src/fs/vfs/dev/loopx.rs#ioctl` | 同板块同名 | Ok, SumGuard::new, UserCheck::new, UserCheck::new()
                    .check_readable_slice, UserCheck::new()
                    .check_writable_slice, core::mem::size_of::<Pid> ...(+6) | Err, Ok, Some, argp.address, argp.address().as_usize, argp.cast ...(+24) |

#### 设备驱动
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `SWTC-la/arceos/modules/axdriver/src/lib.rs#init_drivers` | `arceos/modules/axdriver/src/lib.rs#init_drivers` | 同板块同名同结构 | AllDevices::default, all_devs.block.iter, all_devs.block.iter().enumerate, all_devs.display.iter, all_devs.display.iter().enumerate, all_devs.net.iter ...(+5) | AllDevices::default, all_devs.block.iter, all_devs.block.iter().enumerate, all_devs.display.iter, all_devs.display.iter().enumerate, all_devs.net.iter ...(+5) |
| `SWTC-la/arceos/modules/axdriver/src/virtio.rs#probe_pci` | `arceos/modules/axdriver/src/virtio.rs#probe_pci` | 同板块同名同结构 | D::try_new, Some, axdriver_virtio::probe_pci_device::<VirtIoHalImpl>, warn! | D::try_new, Some, axdriver_virtio::probe_pci_device::<VirtIoHalImpl>, warn! |
| `SWTC-la/xcore/src/net/sockaddr.rs#read_from_user` | `xcore/src/net/sockaddr.rs#read_from_user` | 同板块同名同结构 | Err, SocketAddrV4::read_from_user, SocketAddrV4::read_from_user(addr, addrlen).map, SocketAddrV6::read_from_user, SocketAddrV6::read_from_user(addr, addrlen).map, size_of::<__kernel_sa_family_t> ...(+3) | Err, SocketAddrV4::read_from_user, SocketAddrV4::read_from_user(addr, addrlen).map, SocketAddrV6::read_from_user, SocketAddrV6::read_from_user(addr, addrlen).map, size_of::<__kernel_sa_family_t> ...(+3) |
| `SWTC-la/arceos/modules/axdriver/src/virtio.rs#probe_mmio` | `arceos/modules/axdriver/src/virtio.rs#probe_mmio` | 同板块同名 | D::try_new, Some, axdriver_virtio::probe_mmio_device, base_vaddr.as_mut_ptr, mmio_base.into, phys_to_virt ...(+1) | D::try_new, Some, axdriver_virtio::probe_mmio_device, base_vaddr.as_mut_ptr, mmio_base.into, phys_to_virt ...(+1) |

#### 网络协议栈
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `SWTC-la/xcore/src/net/socket.rs#sendto` | `xcore/src/net/socket.rs#sendto` | 同板块同名同结构 | Err, Ipv4Addr::LOCALHOST.into, Ok, SocketAddr::new, inner
                    .bind, inner
                    .bind(SocketAddr::new(Ipv4Addr::LOCALHOST.into(), 0))
                    .ok ...(+2) | Err, Ipv4Addr::LOCALHOST.into, Ok, SocketAddr::new, inner
                    .bind, inner
                    .bind(SocketAddr::new(Ipv4Addr::LOCALHOST.into(), 0))
                    .ok ...(+2) |
| `SWTC-la/xcore/src/net/socket.rs#recvfrom` | `xcore/src/net/socket.rs#recvfrom` | 同板块同名同结构 | Ok, Some, tcpsocket.lock, tcpsocket.lock().recv, tcpsocket.lock().recv(buf).map, udpsocket
                .lock ...(+5) | Ok, Some, tcpsocket.lock, tcpsocket.lock().recv, tcpsocket.lock().recv(buf).map, udpsocket
                .lock ...(+5) |
| `SWTC-la/xcore/src/net/socket.rs#accept` | `xcore/src/net/socket.rs#accept` | 同板块同名同结构 | Err, Mutex::new, Socket::Tcp, Socket::Unix, tcpsocket
                .lock, tcpsocket
                .lock()
                .accept ...(+4) | Err, Mutex::new, Socket::Tcp, Socket::Unix, tcpsocket
                .lock, tcpsocket
                .lock()
                .accept ...(+4) |
| `SWTC-la/xcore/src/net/socket.rs#connect` | `xcore/src/net/socket.rs#connect` | 同板块同名同结构 | Err, Ok, tcpsocket.lock, tcpsocket.lock().connect, udpsocket.lock, udpsocket.lock().connect | Err, Ok, tcpsocket.lock, tcpsocket.lock().connect, udpsocket.lock, udpsocket.lock().connect |

#### 文件系统/第三方库
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `SWTC-la/arceos/crates/lwext4_rust/src/inode/file.rs#read_at` | `arceos/crates/lwext4_rust/src/inode/file.rs#read_at` | 同板块同名同结构 | (inode as *const _ as *const u8).add, (pos + buf.len() as u64).min, Ok, WritebackGuard::new, assert!, buf.as_mut_ptr ...(+22) | (inode as *const _ as *const u8).add, (pos + buf.len() as u64).min, Ok, WritebackGuard::new, assert!, buf.as_mut_ptr ...(+22) |
| `SWTC-la/arceos/crates/lwext4_rust/src/inode/file.rs#write_at` | `arceos/crates/lwext4_rust/src/inode/file.rs#write_at` | 同板块同名 | Ok, assert!, assert_eq!, buf.as_ptr, buf.is_empty, buf.len ...(+15) | Ok, assert!, assert_eq!, buf.as_ptr, buf.is_empty, buf.len ...(+15) |
| `SWTC-la/arceos/crates/lwext4_rust/src/inode/file.rs#set_len` | `arceos/crates/lwext4_rust/src/inode/file.rs#set_len` | 同板块同名 | Ok, assert!, assert_eq!, cur_len.div_ceil, ext4_inode_set_size, get_block_size ...(+8) | Ok, self.size, self.truncate, todo! |
| `SWTC-la/arceos/modules/axfs-ng/src/fs/ext4/inode.rs#create` | `arceos/modules/axfs-ng/src/fs/ext4/inode.rs#create` | 同板块同名同结构 | DirEntry::new_dir, DirEntry::new_file, DirNode::new, Err, FileNode::new, Inode::new ...(+11) | DirEntry::new_dir, DirEntry::new_file, DirNode::new, Err, FileNode::new, Inode::new ...(+11) |

#### 网络协议栈/第三方库
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `SWTC-la/arceos/modules/axnet/src/smoltcp_impl/tcp.rs#connect` | `arceos/modules/axnet/src/smoltcp_impl/tcp.rs#connect` | 同板块同名 | Err, Ok, Ok::<(IpEndpoint, IpEndpoint), NetError>, SOCKET_SET
                .with_socket_mut::<tcp::Socket, _, _>, SOCKET_SET.add, SocketSetWrapper::new_tcp_socket ...(+33) | Err, Ok, Ok::<(IpEndpoint, IpEndpoint), NetError>, SOCKET_SET
                .with_socket_mut::<tcp::Socket, _, _>, SOCKET_SET.add, SocketSetWrapper::new_tcp_socket ...(+33) |
| `SWTC-la/arceos/crates/smoltcp/src/storage/assembler.rs#add` | `arceos/modules/axnet/src/smoltcp_impl/mod.rs#add` | 同板块同名 | Contig::hole_and_data, Err, Ok, contig.has_data, contig.shrink_hole_to, contig.total_size ...(+11) | debug!, self.0.lock, self.0.lock().add |
| `SWTC-la/arceos/crates/smoltcp/src/socket/tcp.rs#connect` | `arceos/modules/axnet/src/smoltcp_impl/unix_socket.rs#connect` | 同板块同名 | Err, Ok, Self::random_seq_no, Some, addr.is_unspecified, cx
                    .get_source_address ...(+9) | ConnectionPair::new, Err, LISTEN_TABLE.lock, Ok, Some, UnixSocket::new_connected ...(+19) |
| `SWTC-la/arceos/crates/smoltcp/src/socket/tcp.rs#send_impl` | `arceos/modules/axnet/src/smoltcp_impl/udp.rs#send_impl` | 同板块同名 | Err, Ok, f, self.may_send, self.rtte.retransmission_timeout, self.timer.is_idle ...(+3) | Err, Ok, SOCKET_SET.with_socket_mut::<udp::Socket, _, _>, buf.len, self.block_on, self.local_addr.read ...(+5) |

#### 第三方库
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `SWTC-la/arceos/crates/page_table_multiarch/page_table_multiarch/src/bits64.rs#map_region` | `arceos/crates/page_table_multiarch/page_table_multiarch/src/bits64.rs#map_region` | 同板块同名同结构 | Err, M::flush_tlb, Ok, PageSize::Size1G.is_aligned, PageSize::Size2M.is_aligned, PageSize::Size4K.is_aligned ...(+12) | Err, M::flush_tlb, Ok, PageSize::Size1G.is_aligned, PageSize::Size2M.is_aligned, PageSize::Size4K.is_aligned ...(+12) |
| `SWTC-la/arceos/crates/axdriver_crates/axdriver_virtio/src/net.rs#try_new` | `arceos/crates/axdriver_crates/axdriver_virtio/src/net.rs#try_new` | 同板块同名同结构 | Err, InnerDev::new, InnerDev::new(transport).map_err, NetBufPool::new, Ok, Some ...(+14) | Err, InnerDev::new, InnerDev::new(transport).map_err, NetBufPool::new, Ok, Some ...(+14) |
| `SWTC-la/arceos/crates/page_table_multiarch/page_table_multiarch/src/bits64.rs#protect_region` | `arceos/crates/page_table_multiarch/page_table_multiarch/src/bits64.rs#protect_region` | 同板块同名同结构 | Err, Ok, TlbFlushAll::new, assert!, error!, self.protect ...(+5) | Err, Ok, TlbFlushAll::new, assert!, error!, self.protect ...(+5) |
| `SWTC/dependencies/riscv/src/paging/multi_level.rs#unmap` | `arceos/crates/page_table_multiarch/page_table_multiarch/src/bits64.rs#unmap` | 同板块同名 | Err, Ok, Self::MapperFlush::new, p1_entry.flags, p1_entry.flags().contains, p1_entry.frame::<PhysAddrSv48> ...(+14) | Err, Ok, TlbFlush::new, entry.clear, entry.is_present, entry.paddr ...(+1) |

#### 通用模块/其他
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `SWTC-la/xapi/src/iomux/select.rs#do_select` | `xapi/src/iomux/select.rs#do_select` | 同板块同名同结构 | Err, FD_ISSET, FD_SET, FD_ZERO, Ok, PollFd::new ...(+17) | Err, FD_ISSET, FD_SET, FD_ZERO, Ok, PollFd::new ...(+17) |
| `SWTC-la/arceos/modules/axhal/src/mem.rs#kernel_image_regions` | `arceos/modules/axhal/src/mem.rs#kernel_image_regions` | 同板块同名 | (_sbss as *const () as usize).into, (_sdata as *const () as usize).into, (_srodata as *const () as usize).into, (_stext as *const () as usize).into, (boot_stack as *const () as usize).into, [
        MemRegion {
            paddr: virt_to_phys((_stext as *const () as usize).into()),
            size: _etext as *const () as usize - _stext as *const () as usize,
            flags: MemRegionFlags::RESERVED | MemRegionFlags::READ | MemRegionFlags::EXECUTE,
            name: ".text",
        },
        MemRegion {
            paddr: virt_to_phys((_srodata as *const () as usize).into()),
            size: _erodata as *const () as usize - _srodata as *const () as usize,
            flags: MemRegionFlags::RESERVED | MemRegionFlags::READ,
            name: ".rodata",
        },
        MemRegion {
            paddr: virt_to_phys((_sdata as *const () as usize).into()),
            size: _edata as *const () as usize - _sdata as *const () as usize,
            flags: MemRegionFlags::RESERVED | MemRegionFlags::READ | MemRegionFlags::WRITE,
            name: ".data .tdata .tbss .percpu",
        },
        MemRegion {
            paddr: virt_to_phys((boot_stack as *const () as usize).into()),
            size: boot_stack_top as *const () as usize - boot_stack as *const () as usize,
            flags: MemRegionFlags::RESERVED | MemRegionFlags::READ | MemRegionFlags::WRITE,
            name: "boot stack",
        },
        MemRegion {
            paddr: virt_to_phys((_sbss as *const () as usize).into()),
            size: _ebss as *const () as usize - _sbss as *const () as usize,
            flags: MemRegionFlags::RESERVED | MemRegionFlags::READ | MemRegionFlags::WRITE,
            name: ".bss",
        },
    ]
    .into_iter ...(+1) | (_sbss as usize).into, (_sdata as usize).into, (_srodata as usize).into, (_stext as usize).into, (boot_stack as usize).into, [
        MemRegion {
            paddr: virt_to_phys((_stext as usize).into()),
            size: _etext as usize - _stext as usize,
            flags: MemRegionFlags::RESERVED | MemRegionFlags::READ | MemRegionFlags::EXECUTE,
            name: ".text",
        },
        MemRegion {
            paddr: virt_to_phys((_srodata as usize).into()),
            size: _erodata as usize - _srodata as usize,
            flags: MemRegionFlags::RESERVED | MemRegionFlags::READ,
            name: ".rodata",
        },
        MemRegion {
            paddr: virt_to_phys((_sdata as usize).into()),
            size: _edata as usize - _sdata as usize,
            flags: MemRegionFlags::RESERVED | MemRegionFlags::READ | MemRegionFlags::WRITE,
            name: ".data .tdata .tbss .percpu",
        },
        MemRegion {
            paddr: virt_to_phys((boot_stack as usize).into()),
            size: boot_stack_top as usize - boot_stack as usize,
            flags: MemRegionFlags::RESERVED | MemRegionFlags::READ | MemRegionFlags::WRITE,
            name: "boot stack",
        },
        MemRegion {
            paddr: virt_to_phys((_sbss as usize).into()),
            size: _ebss as usize - _sbss as usize,
            flags: MemRegionFlags::RESERVED | MemRegionFlags::READ | MemRegionFlags::WRITE,
            name: ".bss",
        },
    ]
    .into_iter ...(+1) |
| `SWTC/kernel/src/utils/radix_tree.rs#insert` | `xmodules/xutils/src/collections/btreemap.rs#insert` | 同板块同名 | Arc::new, RadixTreeInternalNode::new, RadixTreeNode::InternalNode, RadixTreeNode::LeafNode, Some, children[*index].as_ref ...(+11) | key.clone, self.forward.insert, self.forward.remove, self.reverse.insert, self.reverse.remove, value.clone |
| `SWTC-la/xmodules/xsignal/src/action.rs#try_from` | `xmodules/xsignal/src/action.rs#try_from` | 同板块同名同结构 | Err, Ok, SignalActionFlags::from_bits, SignalDisposition::Handler, flags.contains, value.sa_mask.into ...(+1) | Err, Ok, SignalActionFlags::from_bits, SignalDisposition::Handler, flags.contains, value.sa_mask.into ...(+1) |


好的，合议庭。本庭已收到控辩双方的全部陈述、证据清单及原始 AST 抽样数据。现依据《审计指令》，对本案进行最终、独立的同源性定性仲裁。

### 1. 宏观判定
- **定性结论**: **独立开发，存在共享上游生态及公共依赖的显著影响** (客观 AST 相似度: 27.06%，共同 AST 指纹: 1981；唯一 AST 指纹: 目标项目 6404，参考项目 2897；覆盖率: 目标项目 30.93%，参考项目 68.38%)

本庭认为，27.06% 的客观 AST 相似度处于低位，远未达到可判定为“抄袭”或“衍生作品”的阈值。目标项目近 70% 的独立 AST 指纹（6404）是其独立开发工作的有力证明。参考项目高达 68.38% 的覆盖率，结合其模块画像中大量函数位于 `第三方库`、`文件系统/第三方库` 等目录，清晰地揭示了高相似度的根源在于双方共同依赖的 ArceOS 内核框架及其庞大的第三方 crate 生态，而非单向的代码抄袭。

### 2. 全模块覆盖矩阵解读

| 模块 | 目标函数数 | 参考函数数 | Target 覆盖 | Base 覆盖 | 证据强度 | 解读 |
| :--- | ---: | ---: | ---: | ---: | :--- | :--- |
| **进程管理** | 323 | 172 | 47.84% | 90.57% | **弱** | 双方均实现该模块。Base 覆盖率极高，表明参考项目的大部分进程管理代码与目标项目重合。然而，这主要因为双方共享了 `xcore`、`xapi`、`xmodules/xprocess` 等核心 crate。控方所指的“铁证”均位于这些共享 crate 中，是共同依赖而非抄袭。 |
| **任务调度/异步运行时** | 478 | 480 | 62.59% | 71.51% | **弱** | 双方均实现该模块。覆盖率较高，但核心证据集中在 `arceos/modules/axtask` 目录，这是 ArceOS 官方任务调度模块。任何基于 ArceOS 的项目都必然包含此代码，属于公共基础设施。 |
| **内存管理** | 624 | 430 | 47.6% | 71.47% | **弱** | 双方均实现该模块。存在共同指纹，但核心差异显著。例如 `handle_page_fault` 和 `map_elf` 的实现策略完全不同，证明了双方在内存管理核心逻辑上的独立设计。 |
| **中断/异常处理** | 73 | 70 | 58.46% | 61.29% | **弱** | 双方均实现该模块。证据多为 `default_action`、`default_termios` 等“内部闭环逻辑”或数据定义，属于功能强制性的常量或简单逻辑，不构成强证据。 |
| **系统调用** | 116 | 89 | 14.85% | 18.75% | **未形成** | 双方均实现该模块。覆盖率极低，表明双方在系统调用层的实现差异巨大。控方未就此模块提出任何有效指控，这反而是独立开发的有力佐证。 |
| **文件系统** | 907 | 801 | 57.99% | 69.57% | **弱** | 双方均实现该模块。证据如 `sys_renameat2` 显示出不同的实现路径（目标项目直接操作 inode cache，参考项目使用 fs resolver）。高度相似性主要来自共享的 VFS 框架（`VirtDir`、`VirtFile`）和第三方库（`lwext4_rust`）。 |
| **设备驱动** | 145 | 139 | 44.14% | 38.89% | **弱** | 双方均实现该模块。证据均为 `axdriver` 模块中的驱动初始化及探测逻辑，属于 ArceOS 框架的标准实现，是公共依赖。 |
| **网络协议栈** | 61 | 77 | 26.0% | 21.31% | **弱** | 双方均实现该模块。证据集中在 `smoltcp` 库的封装和 socket 实现上，属于对同一第三方库的相似用法，不构成抄袭。 |
| **第三方库/公共依赖** | 2158 | 349 | 19.77% | 99.66% | **未形成** | 此模块是本案关键。参考项目覆盖率高达 99.66%，说明其代码库几乎完全是目标项目的一个子集。这确凿无疑地证明，两项目的高度相似性源于共享了大量的第三方库和公共依赖 crate，而非代码抄袭。 |

### 3. 底层 AST 关键证据指纹表
本庭对控辩双方提交的证据进行审查后，提取以下关键指纹。所有证据均因属于共享上游生态、POSIX 规范强制或标准 Rust 惯用法而被降权或排除。

| 所属板块 | 目标项目路径 | 参考项目路径 | 证据类型 | 技术说明 |
| :--- | :--- | :--- | :--- | :--- |
| 进程管理 | `SWTC-la/xcore/src/fs/vfs/proc/mod.rs#create_proc_root` | `xcore/src/fs/vfs/proc/mod.rs#create_proc_root` | 共享上游/Mock数据 | 硬编码字符串 `"StarryX version 1.0.0\n"` 等是参考项目遗留的 mock 数据，目标项目未清理。此为数据字面量，非代码逻辑。路径指向共享的 `xcore` crate。 |
| 进程管理 | `SWTC-la/xcore/src/fs/vfs/proc/sys.rs#create_kernel_root` | `xcore/src/fs/vfs/proc/sys.rs#create_kernel_root` | 共享上游/配置数据 | `"16384\n"` 等魔数序列是 Linux 内核标准参数的硬编码，属于领域知识。路径指向共享的 `xcore` crate。 |
| 进程管理 | `SWTC-la/xcore/src/fs/vfs/proc/sys.rs#create_random_root` | `xcore/src/fs/vfs/proc/sys.rs#create_random_root` | 共享上游/示例数据 | UUID `550e8400-...` 是 RFC 4122 定义的示例 UUID，非项目独创。路径指向共享的 `xcore` crate。 |
| 任务调度 | `SWTC-la/arceos/modules/axtask/src/run_queue.rs#exit_current` | `arceos/modules/axtask/src/run_queue.rs#exit_current` | 公共依赖/上游框架 | 文件路径和调用图谱完全一致，属于 ArceOS 官方模块 `axtask` 的核心实现。 |
| 任务调度 | `SWTC-la/arceos/modules/axtask/src/wait_queue.rs#wait_until` 等 | `arceos/modules/axtask/src/wait_queue.rs#wait_until` 等 | 公共依赖/上游框架 | 等待队列系列函数，使用了 `NoPreemptIrqSave` 等 ArceOS 特有泛型，是框架强制 API。 |
| 进程管理 | `SWTC-la/xcore/src/task/proc.rs#new_user_task` | `xcore/src/task/proc.rs#new_user_task` | 共享上游/POSIX语义 | 复杂的链式调用和 panic 信息是 `clone` 系统调用 `set_tid_address` 语义的标准实现，路径指向共享的 `xcore` crate。 |
| 进程管理 | `SWTC-la/xmodules/xprocess/src/process.rs#build` | `xmodules/xprocess/src/process.rs#build` | 共享上游/标准初始化 | `Arc::new`、`SpinNoIrq::new` 等调用是 Rust 中复杂结构体的标准初始化模式，路径指向共享的 `xmodules/xprocess` crate。 |
| 进程管理 | `SWTC-la/xapi/src/task/exit.rs#do_exit` | `xapi/src/task/exit.rs#do_exit` | 共享上游/POSIX语义 | `FD_TABLE.clear`、`IPC_MANAGER.clear` 等是 POSIX 进程退出时必须执行的标准清理步骤。路径指向共享的 `xapi` crate。 |
| 第三方库 | `SWTC-la/arceos/crates/lwext4_rust/src/inode/file.rs#read_at` | `arceos/crates/lwext4_rust/src/inode/file.rs#read_at` | 第三方库 | 完全相同的第三方库代码，属于公共依赖。 |
| 第三方库 | `SWTC-la/arceos/crates/page_table_multiarch/.../bits64.rs#map_region` | `arceos/crates/page_table_multiarch/.../bits64.rs#map_region` | 第三方库 | 完全相同的第三方库代码，属于公共依赖。 |

### 4. 核心代码调用流对比剖析
本庭选取双方均存在且控方重点指控的模块，进行底层技术析理。

*   **【进程管理/进程退出路径】**
*   **底层技术析理**: 控方指控 `do_exit` 函数调用图谱高度一致。经查，双方调用流均为 `FD_TABLE.clear` -> `FutexKey::new` -> `IPC_MANAGER.clear` -> `SignalInfo::new` -> `axtask::exit`。此调用序列是 POSIX 规范定义的进程退出标准流程：清理文件描述符、释放 futex、清理 IPC 资源、向父进程发送信号、最后调用调度器退出。该流程由操作系统语义强制，而非代码抄袭。双方均使用 `xapi` crate 提供的标准 API 实现此流程，其 AST 相似是共享同一上层抽象的结果。
*   **代码溯源链接**:
  - 目标项目: `SWTC-la/xapi/src/task/exit.rs#do_exit`
  - 参考项目: `xapi/src/task/exit.rs#do_exit`

*   **【任务调度/等待队列机制】**
*   **底层技术析理**: 控方指控 `wait_until`、`requeue` 等函数存在“像素级”复用。经查，双方均使用了 `current_run_queue::<NoPreemptIrqSave>` 和 `select_run_queue::<NoOp>` 等泛型。`NoPreemptIrqSave` 和 `NoOp` 是 ArceOS 调度框架定义的、用于关中断和空操作的调度策略类型。任何在 ArceOS 框架下实现等待队列的开发者，都必须使用这些类型来与框架交互。调用流 `self.queue.lock` -> `wq.retain` -> `task.set_in_wait_queue` 是操作 `Mutex<VecDeque<Arc<Task>>>` 这一标准数据结构的 Rust 惯用法。此为框架约束与语言惯用法的结合，不构成抄袭。
*   **代码溯源链接**:
  - 目标项目: `SWTC-la/arceos/modules/axtask/src/wait_queue.rs#wait_until`
  - 参考项目: `arceos/modules/axtask/src/wait_queue.rs#wait_until`

*   **【内存管理/缺页异常处理】**
*   **底层技术析理**: 此模块体现了双方架构设计的显著差异。目标项目 (`SWTC/kernel/src/mm/memory_space/page_fault_handler.rs#handle_page_fault`) 采用 `PageBuilder::new().permission(...)` 模式，直接构建页表项进行映射。而参考项目 (`src/mm.rs#handle_page_fault`) 则通过 `SignalInfo::new` 和 `Signo::from_repr(SIGSEGV as u8)` 将缺页异常转换为 POSIX 信号进行处理。前者是底层内存管理器的直接操作，后者是更高层次的信号分发机制。这种抽象层次和处理模型的根本不同，是双方独立开发的有力证据。
*   **代码溯源链接**:
  - 目标项目: `SWTC/kernel/src/mm/memory_space/page_fault_handler.rs#handle_page_fault`
  - 参考项目: `src/mm.rs#handle_page_fault`

### 5. 单方模块与降权项
*   **第三方库/公共依赖**: 目标项目 `第三方库` 模块有 2158 个函数，`网络协议栈/第三方库` 有 2599 个函数。参考项目对应模块函数数极少（349 和 166），但覆盖率高达 99.66% 和 91.67%。这证明参考项目几乎完全被包含在目标项目的第三方库代码中。所有位于 `arceos/crates/`、`arceos/modules/`、`dependencies/` 等目录下的证据，均被降权为**公共依赖**，不构成抄袭证据。
*   **跨板块同名弱信号**: 控方已主动降级的 `sys_rt_sigprocmask`、`sys_getppid`、`sys_exit` 等函数，本庭予以确认并完全排除。这些函数分属 `SWTC/kernel` 和 `xapi` 或 `arceos/api` 等不同抽象层次，其内部调用对象完全不同，证明了实现路径的独立性。
*   **通用模块/其他**: `kernel_image_regions` 等函数虽高度相似，但其逻辑是描述内核内存布局的静态数组，由链接脚本定义的符号决定，属于事实性描述而非创造性代码。

### 6. 保守结论
综合全案证据，目标项目 `[2026-T2026102569910192-XX]` 与参考项目 `[2025-StarryX-杭州电子科技大学]` 的客观 AST 相似度为 27.06%，处于低位。双方代码的高度重合部分，经查证，几乎全部位于 `arceos`、`xcore`、`xapi`、`smoltcp` 等共享上游框架及第三方 crate 中。控方所指的“像素级复用铁证”，实为共同依赖的上游代码、POSIX/Linux 标准语义的强制约束、以及 Rust 语言的标准惯用法。目标项目拥有 6404 个独立 AST 指纹，并在内存管理、系统调用等核心模块展现出与参考项目截然不同的架构设计与实现路径。因此，本庭裁定，现有证据不足以认定目标项目构成对参考项目的代码抄袭，两项目间的相似性可合理归因于共享的开源生态与共同的兼容性目标。

------------------------------

