# 🛡️ OS 内核赛自动化代码级架构审计报告

**审计时间**: `2026-07-02 13:17:42`
**参赛作品名称**: `2026-T202610574999903-XX`
**年份**: `2026`
**学校**: `XX`
**队伍名称**: `T202610574999903`
**比赛名字**: `OS-Competition`
**比赛子赛道**: `Kernel-Implementation`
**仓库地址**: `https://gitlab.eduxiji.net/educg-group-43501-3132633/T202610574999903-2691`
**推测开发语言**: `Rust / C (由AST探针自动识别)`
**特征函数量**: `48`

## ⚔️ 底层代码同源性对比 (AST Homology)
> 本部分由专门的代码审查智能体，对该项目与符合年份条件的历届最高相似度项目进行控制流与底层调用对比。新版报告会先给出确定性的全模块覆盖矩阵，再给出 Agent 的分模块解释，避免只围绕少量抽样函数展开。

### 🏆 Top 2 底层对标: vs `2022-段错误-杭州电子科技大学`（杭州电子科技大学 / 段错误）

#### 对方作品信息

**对方年份**: `2022`
**对方学校**: `杭州电子科技大学`
**对方队伍名称**: `段错误`
**对方比赛名字**: `OS-Competition`
**对方比赛子赛道**: `Kernel-Implementation`
**对方仓库地址**: `https://gitlab.eduxiji.net/19061120/oskernel2022-segmentfault.git`

> 双向覆盖率：共同 AST 指纹: 3；唯一 AST 指纹: 目标项目 42，参考项目 517；覆盖率: 目标项目 7.14%，参考项目 0.58%

## 全模块对比总览（确定性证据）

> 总体覆盖率：共同 AST 指纹: 3；唯一 AST 指纹: 目标项目 42，参考项目 517；覆盖率: 目标项目 7.14%，参考项目 0.58%

### 1. 模块覆盖矩阵
| 模块 | 目标函数数 | 参考函数数 | 共同唯一 AST 指纹 | Target 覆盖 | Base 覆盖 | 状态 |
| :--- | ---: | ---: | ---: | ---: | ---: | :--- |
| 进程管理 | 4 | 52 | 1 | 25.0% | 2.33% | 双方存在且有结构重合 |
| 任务调度/异步运行时 | 1 | 56 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |
| 内存管理 | 3 | 183 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |
| 中断/异常处理 | 5 | 6 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |
| 系统调用 | 15 | 36 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |
| 文件系统 | 2 | 226 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |
| 设备驱动 | 0 | 37 | 0 | 0.0% | 0.0% | 仅参考项目体现 |
| 网络协议栈 | 0 | 0 | 0 | 0.0% | 0.0% | 双方均未体现 |
| 通用模块/其他 | 18 | 41 | 1 | 6.25% | 2.63% | 双方存在且有结构重合 |

### 2. 分模块证据清单

#### 进程管理
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `src/syscall/process.rs#sys_exit` | `os/src/syscall/process.rs#sys_exit` | 同板块同名 | batch::run_next_app, crate::println! | exit_current_and_run_next, panic! |
| `src/syscall/process.rs#sys_getpid` | `os/src/syscall/process.rs#sys_getpid` | 同板块同名 | crate::batch::current_pid | current_task, current_task().unwrap |
| `src/syscall/process.rs#sys_yield` | `os/src/syscall/process.rs#sys_yield` | 同板块同名 | 内部闭环逻辑 | suspend_current_and_run_next |
| `src/batch.rs#current_pid` | `os/src/syscall/process.rs#sys_prlimit64` | 同模块上下文对齐/差异观察 | 内部闭环逻辑 | UserBuffer::new, buf.as_ptr, buf.write, current_task, current_task().unwrap, current_user_token ...(+6) |

#### 任务调度/异步运行时
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `src/batch.rs#run_next_app` | `os/src/syscall/mod.rs#syscall` | 同模块上下文对齐/差异观察 | TrapContext::app_init, core::arch::asm!, core::mem::transmute, crate::println!, crate::sbi::shutdown, dst.add ...(+5) | Option::<usize>::from, panic!, sys_accept, sys_bind, sys_brk, sys_chdir ...(+69) |

#### 内存管理
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `src/trap/handler.rs#trap_handler` | `os/src/trap/mod.rs#trap_handler` | 同板块同名 | batch::run_next_app, core::arch::asm!, crate::println!, panic!, syscall::syscall | (stval as usize).into, TRAMPOLINE.into, Trap::Exception, cfg!, current_add_signal, current_task ...(+17) |
| `src/utils/memory.rs#print_memory_map` | `os/src/mm/memory_set.rs#from_copy_on_write` | 同模块上下文对齐/差异观察 | println! | (*_vpn).clone, ChunkArea::from_another, FrameTracker::from_ppn, MapArea::from_another, Self::new_bare, VirtAddr::from ...(+34) |

#### 中断/异常处理
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `src/lang_items.rs#panic` | `os/src/trap/mod.rs#trap_from_kernel` | 同模块上下文对齐/差异观察 | Registers::zeroed, asm!, error!, info.location, print_backtrace, print_registers ...(+2) | panic!, println! |
| `src/utils/panic.rs#print_registers` | `os/src/trap/mod.rs#set_kernel_trap_entry` | 同模块上下文对齐/差异观察 | println! | stvec::write |
| `src/utils/panic.rs#zeroed` | `os/src/trap/mod.rs#set_user_trap_entry` | 同模块上下文对齐/差异观察 | 内部闭环逻辑 | stvec::write |
| `src/main.rs#rust_main` | `os/src/trap/mod.rs#enable_timer_interrupt` | 同模块上下文对齐/差异观察 | batch::init, batch::run_next_app, clear_bss, info!, print_banner, print_memory_map ...(+1) | sie::set_stimer |

#### 系统调用
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `user/src/lib.rs#exit` | `user_c/lib/syscall.c#exit` | 同板块同名 | syscall::sys_exit | syscall |
| `user/src/lib.rs#getpid` | `user_c/lib/syscall.c#getpid` | 同板块同名 | syscall::sys_getpid | syscall |
| `user/src/bin/01_power.rs#_start` | `user_c/lib/syscall.c#clone` | 同模块上下文对齐/差异观察 | format_u64, msg1.as_ptr, msg1.len, result.wrapping_mul, s.as_ptr, s.len ...(+2) | __clone |
| `user/src/bin/03_fib.rs#_start` | `os/src/syscall/socket.rs#sys_recvfrom` | 同模块上下文对齐/差异观察 | a.wrapping_add, format_u64, msg.as_ptr, msg.len, s.as_ptr, s.len ...(+2) | UserBuffer::new, current_user_token, translated_byte_buffer, userbuf.write |

#### 文件系统
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `src/syscall/fs.rs#sys_write` | `simple-fat32/src/vfs.rs#find_long_name` | 同模块上下文对齐/差异观察 | buf.add, buf.add(i).read, sbi::console_putchar | LongDirEntry::new, ShortDirEntry::new, Some, String::from, VFile::new, Vec::new ...(+22) |
| `src/syscall/fs.rs#sys_read` | `os/src/drivers/block/sdcard.rs#get_csdregister` | 同模块上下文对齐/差异观察 | 内部闭环逻辑 | Err, Ok, self.end_cmd, self.get_response, self.read_data, self.send_cmd ...(+2) |

#### 设备驱动
仅参考项目中观察到该模块代表函数，目标项目没有同模块可对齐证据：
- 参考项目：`os/src/drivers/block/sdcard.rs#read_sector`，调用摘要：Err, Ok, assert!, chunk.iter_mut, chunk.iter_mut().zip, data_buf.chunks_mut ...(+6)
- 参考项目：`os/src/drivers/block/sdcard.rs#write_sector`，调用摘要：(data_buf.len() / SEC_LEN).try_into, (data_buf.len() / SEC_LEN).try_into().unwrap, Err, Ok, assert!, chunk.iter ...(+9)
- 参考项目：`os/src/drivers/block/sdcard.rs#get_cidregister`，调用摘要：Err, Ok, self.end_cmd, self.get_response, self.read_data, self.send_cmd ...(+2)
- 参考项目：`simple-fat32/src/block_cache.rs#get_block_cache`，调用摘要：Arc::clone, Arc::new, Arc::strong_count, BlockCache::new, RwLock::new, panic! ...(+7)

#### 通用模块/其他
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `src/main.rs#clear_bss` | `os/src/main.rs#clear_bss` | 同板块同名 | (a as *mut u64).write_volatile, (start..end).step_by | core::slice::from_raw_parts_mut, core::slice::from_raw_parts_mut(sbss as usize as *mut u8, ebss as usize - sbss as usize).fill |
| `src/sbi.rs#shutdown` | `os/src/sbi.rs#shutdown` | 同板块同名 | system_reset, unreachable! | panic!, sbi_call |
| `user/src/lib.rs#_start` | `user_c/lib/lib.c#_start` | 同板块同名 | exit, main | exit, main |
| `src/console.rs#write_str` | `os/src/console.rs#write_str` | 同板块同名 | Ok, console_putchar, s.chars | Ok, crate::sbi::console_putchar, s.chars |


### 1. 宏观判定
- **定性结论**: 独立开发 (相似度: 0.54%，共同 AST 指纹: 3；唯一 AST 指纹: 目标项目 42，参考项目 517；覆盖率: 目标项目 7.14%，参考项目 0.58%)

### 2. 全模块覆盖矩阵解读
- **进程管理**: 双方均存在。存在 1 个共同 AST 指纹，源于 POSIX 标准系统调用命名。目标覆盖率 25.0%，参考覆盖率 2.33%。目标项目采用批处理模型，参考项目采用多任务并发模型，底层调度对象完全不同。证据强度：**弱**。
- **任务调度/异步运行时**: 双方均存在。共同 AST 指纹为 0。目标项目为简单的批处理调度器，参考项目为复杂的多任务调度与系统调用分发中心。证据强度：**未形成**。
- **内存管理**: 双方均存在。共同 AST 指纹为 0。目标项目无页表、无 ELF 加载器，参考项目具备完整的虚拟内存管理、写时复制和 ELF 解析能力。证据强度：**未形成**。
- **中断/异常处理**: 双方均存在。共同 AST 指纹为 0。目标项目的中断处理直接耦合批处理调度，参考项目具备独立的内核/用户态陷阱入口和信号机制。证据强度：**未形成**。
- **系统调用**: 双方均存在。共同 AST 指纹为 0。目标项目仅实现 5 个基本系统调用，参考项目实现了 70+ 个系统调用，涵盖文件、网络、进程等。证据强度：**未形成**。
- **文件系统**: 双方均存在。共同 AST 指纹为 0。目标项目仅有串口读写，参考项目包含完整的 FAT32 虚拟文件系统。证据强度：**未形成**。
- **设备驱动**: 仅参考项目存在。目标项目无任何设备驱动模块。证据强度：**未形成**。
- **网络协议栈**: 双方均未体现。证据强度：**未形成**。
- **第三方库/公共依赖**: 双方均使用 Rust 标准库或 C 标准库中的通用项，如 `println!`、`asm!`、`Ok` 等，这些属于语言级公共领域，不构成项目间复用证据。

### 3. 底层 AST 关键证据指纹表
| 所属板块 | 目标项目路径 | 参考项目路径 | 证据类型 | 技术说明 |
| :--- | :--- | :--- | :--- | :--- |
| 通用模块/其他 | `[src/console.rs]` 函数 `write_str` | `[os/src/console.rs]` 函数 `write_str` | 公共模式 | 均为实现 Rust `core::fmt::Write` trait 的标准模板，调用链 `Ok, console_putchar, s.chars` 是语言级必然。 |
| 通用模块/其他 | `[user/src/lib.rs]` 函数 `_start` | `[user_c/lib/lib.c]` 函数 `_start` | 公共模式 | 用户态程序入口点调用 `main` 后 `exit` 是 C/Rust 运行时的标准模式，跨语言 AST 抽象导致同名同图谱。 |
| 进程管理 | `[src/syscall/process.rs]` 函数 `sys_exit` | `[os/src/syscall/process.rs]` 函数 `sys_exit` | 标准接口 | 同名源于 POSIX 标准，但目标调用 `batch::run_next_app`，参考调用 `exit_current_and_run_next`，调度模型完全不同。 |
| 进程管理 | `[src/syscall/process.rs]` 函数 `sys_getpid` | `[os/src/syscall/process.rs]` 函数 `sys_getpid` | 标准接口 | 同名源于 POSIX 标准，目标调用 `crate::batch::current_pid`，参考调用 `current_task`，进程抽象完全不同。 |
| 进程管理 | `[src/syscall/process.rs]` 函数 `sys_yield` | `[os/src/syscall/process.rs]` 函数 `sys_yield` | 标准接口 | 同名源于 POSIX 标准，目标为内部闭环逻辑，参考调用 `suspend_current_and_run_next`，无共同结构。 |
| 通用模块/其他 | `[src/main.rs]` 函数 `clear_bss` | `[os/src/main.rs]` 函数 `clear_bss` | 独立实现 | 目标使用 `write_volatile` 循环，参考使用 `slice::fill`，算法与 AST 结构均不同。 |
| 通用模块/其他 | `[src/sbi.rs]` 函数 `shutdown` | `[os/src/sbi.rs]` 函数 `shutdown` | 独立实现 | 目标使用 `sbi_rt` 库的 `system_reset`，参考使用手写 `sbi_call`，SBI 抽象层完全不同。 |
| 通用模块/其他 | `[src/sbi.rs]` 函数 `console_putchar` | `[os/src/sbi.rs]` 函数 `console_putchar` | 独立实现 | 目标使用 `sbi_rt::legacy::console_putchar`，参考使用手写 `sbi_call`，外部依赖对象不同。 |

### 4. 核心代码调用流对比剖析
* **【进程管理/系统调用路径】**
* **底层技术析理**: 目标项目的 `sys_exit` 直接调用 `batch::run_next_app` 切换到下一个批处理应用，无进程控制块操作。参考项目的 `sys_exit` 调用 `exit_current_and_run_next`，涉及当前任务控制块的资源回收与调度器状态更新。两者在进程抽象与调度模型上存在根本性差异。
* **代码溯源链接**:
  - 目标项目: `src/syscall/process.rs#sys_exit`
  - 参考项目: `os/src/syscall/process.rs#sys_exit`

* **【内存管理/陷阱处理路径】**
* **底层技术析理**: 目标项目的 `trap_handler` 仅做简单的系统调用分发和错误打印，无内存地址转换。参考项目的 `trap_handler` 包含 `stval` 地址提取、`TRAMPOLINE` 跳转、信号处理等复杂虚拟内存管理逻辑。两者在内存抽象层次上完全不同。
* **代码溯源链接**:
  - 目标项目: `src/trap/handler.rs#trap_handler`
  - 参考项目: `os/src/trap/mod.rs#trap_handler`

* **【中断/异常处理/初始化路径】**
* **底层技术析理**: 目标项目的 `rust_main` 完成 BSS 清零、批处理初始化后直接运行应用。参考项目的 `trap::init` 仅设置内核态陷阱入口，其主流程包含复杂的设备驱动和文件系统初始化。两者在启动流程和子系统初始化上无共同结构。
* **代码溯源链接**:
  - 目标项目: `src/main.rs#rust_main`
  - 参考项目: `os/src/trap/mod.rs#init`

* **【系统调用/分发路径】**
* **底层技术析理**: 目标项目的 `syscall` 函数仅匹配 5 个系统调用号。参考项目的 `syscall` 函数匹配 70+ 个系统调用号，涵盖进程、文件、网络、内存等。两者在系统调用表的规模和覆盖领域上存在数量级差异。
* **代码溯源链接**:
  - 目标项目: `src/syscall/mod.rs#syscall`
  - 参考项目: `os/src/syscall/mod.rs#syscall`

### 5. 单方模块与降权项
- **仅参考项目存在的模块**: 设备驱动（`os/src/drivers/block/sdcard.rs` 等）、完整的 FAT32 文件系统（`simple-fat32`）。目标项目完全缺失这些模块，证明两者在系统复杂度上存在巨大鸿沟，不存在“降级复用”的可能。
- **标准接口与公共模式**: `sys_exit`、`sys_getpid`、`sys_yield`、`write`、`read`、`exit`、`getpid` 等均为 POSIX/Linux 标准系统调用命名，任何实现系统调用的 OS 都会使用。`_start`、`write_str`、`clear_bss` 等是操作系统开发的通用模式或语言级要求，不具备项目特异性。
- **跨板块硬凑证据**: 控方将目标项目的 `src/batch.rs#run_next_app`（任务调度）与参考项目的 `os/src/syscall/mod.rs#syscall`（系统调用分发）配对，将 `src/syscall/fs.rs#sys_write`（文件系统）与 `simple-fat32/src/vfs.rs#find_long_name`（文件系统实现）配对，这些配对在功能、模块和 AST 结构上均无共同点，所在模块的共同指纹为 0，不能作为同源证据。

### 6. 保守结论
本次审计基于 0.54% 的客观 AST 相似度与 3/559 的共同指纹比例，判定目标项目与参考项目为独立开发。双方在进程管理、内存管理、文件系统等核心模块的共同 AST 指纹均为 0，架构上存在批处理与多任务并发、无 MMU 与完整虚拟内存、串口 I/O 与 FAT32 文件系统的本质差异。控方提出的“复用铁证”经分析均为 Rust/C 语言标准模式或 POSIX 标准接口命名，不具备项目特异性。整体代码复用风险极低，无证据表明存在抄袭或违规行为。

------------------------------

