# 🛡️ OS 内核赛自动化代码级架构审计报告

**审计时间**: `2026-07-02 13:17:42`
**参赛作品名称**: `2026-T202610574999903-XX`
**年份**: `2026`
**学校**: `XX`
**队伍名称**: `T202610574999903`
**比赛名字**: `OS-Competition`
**比赛子赛道**: `Kernel-Implementation`
**仓库地址**: `https://gitlab.eduxiji.net/educg-group-43501-3132633/T202610574999903-2691`
**推测开发语言**: `Rust / C (由AST探针自动识别)`
**特征函数量**: `48`

## ⚔️ 底层代码同源性对比 (AST Homology)
> 本部分由专门的代码审查智能体，对该项目与符合年份条件的历届最高相似度项目进行控制流与底层调用对比。新版报告会先给出确定性的全模块覆盖矩阵，再给出 Agent 的分模块解释，避免只围绕少量抽样函数展开。

### 🏆 Top 5 底层对标: vs `2024-NPUcore-IMPACT-西北工业大学`（西北工业大学 / NPUcore-IMPACT!!!）

#### 对方作品信息

**对方年份**: `2024`
**对方学校**: `西北工业大学`
**对方队伍名称**: `NPUcore-IMPACT!!!`
**对方比赛名字**: `OS-Competition`
**对方比赛子赛道**: `Kernel-Implementation`
**对方仓库地址**: `https://gitlab.eduxiji.net/educg-group-26011-2376549/T202410699992496-312`

> 双向覆盖率：共同 AST 指纹: 2；唯一 AST 指纹: 目标项目 42，参考项目 642；覆盖率: 目标项目 4.76%，参考项目 0.31%

## 全模块对比总览（确定性证据）

> 总体覆盖率：共同 AST 指纹: 2；唯一 AST 指纹: 目标项目 42，参考项目 642；覆盖率: 目标项目 4.76%，参考项目 0.31%

### 1. 模块覆盖矩阵
| 模块 | 目标函数数 | 参考函数数 | 共同唯一 AST 指纹 | Target 覆盖 | Base 覆盖 | 状态 |
| :--- | ---: | ---: | ---: | ---: | ---: | :--- |
| 进程管理 | 4 | 69 | 1 | 25.0% | 1.56% | 双方存在且有结构重合 |
| 任务调度/异步运行时 | 1 | 123 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |
| 内存管理 | 3 | 259 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |
| 中断/异常处理 | 5 | 1 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |
| 系统调用 | 15 | 29 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |
| 文件系统 | 2 | 274 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |
| 设备驱动 | 0 | 22 | 0 | 0.0% | 0.0% | 仅参考项目体现 |
| 网络协议栈 | 0 | 0 | 0 | 0.0% | 0.0% | 双方均未体现 |
| 文件系统/第三方库 | 0 | 31 | 0 | 0.0% | 0.0% | 仅参考项目体现 |
| 通用模块/其他 | 18 | 50 | 1 | 6.25% | 2.86% | 双方存在且有结构重合 |

### 2. 分模块证据清单

#### 进程管理
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `src/syscall/process.rs#sys_exit` | `os/src/syscall/process.rs#sys_exit` | 同板块同名 | batch::run_next_app, crate::println! | exit_current_and_run_next |
| `src/syscall/process.rs#sys_getpid` | `os/src/syscall/process.rs#sys_getpid` | 同板块同名 | crate::batch::current_pid | current_task, current_task().unwrap |
| `src/syscall/process.rs#sys_yield` | `os/src/syscall/process.rs#sys_yield` | 同板块同名 | 内部闭环逻辑 | suspend_current_and_run_next |
| `src/batch.rs#current_pid` | `user/src/bin/initproc.rs#main` | 同模块上下文对齐/差异观察 | 内部闭环逻辑 | "HOME=/root\0".as_ptr, "LANG=C.UTF-8\0".as_ptr, "LD_LIBRARY_PATH=/\0".as_ptr, "LOGNAME=root\0".as_ptr, "MOTD_SHOWN=pam\0".as_ptr, "OLDPWD=/root\0".as_ptr ...(+15) |

#### 任务调度/异步运行时
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `src/batch.rs#run_next_app` | `os/src/syscall/mod.rs#syscall` | 同模块上下文对齐/差异观察 | TrapContext::app_init, core::arch::asm!, core::mem::transmute, crate::println!, crate::sbi::shutdown, dst.add ...(+5) | Errno::try_from, [
            //black list
            SYSCALL_YIELD,
            SYSCALL_READ,
            SYSCALL_WRITE,
            SYSCALL_GETDENTS64,
            SYSCALL_READV,
            SYSCALL_WRITEV,
            SYSCALL_PSELECT6,
            SYSCALL_SIGACTION,
            SYSCALL_SIGPROCMASK,
            // SYSCALL_WAIT4,
            // SYSCALL_GETPPID,
            SYSCALL_CLOCK_GETTIME,
        ]
        .contains, args.len, crate::task::current_task, crate::task::current_task()
                .unwrap, crate::task::current_task()
                .unwrap()
                .acquire_inner_lock ...(+94) |

#### 内存管理
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `src/trap/handler.rs#trap_handler` | `os/src/task/signal.rs#do_signal` | 同模块上下文对齐/差异观察 | batch::run_next_app, core::arch::asm!, crate::println!, panic!, syscall::syscall | (trap_cx as *const TrapContext).cast::<MachineContext>, SigInfo::new, SignalStack::new, Signals::SIGSEGV.to_signum, Signals::SIGSEGV.to_signum().unwrap, Signals::from_bits_truncate ...(+40) |
| `src/utils/memory.rs#print_memory_map` | `os/src/mm/memory_set.rs#map_elf` | 同模块上下文对齐/差异观察 | println! | ((ph.virtual_addr() + ph.mem_size()) as usize + bias).into, (VirtAddr::from(elf.input.as_ptr() as usize + (ph.offset() as usize)))
                                .floor, (ph.virtual_addr() as usize + bias).into, Err, KERNEL_SPACE
                        .lock, KERNEL_SPACE
                        .lock()
                        .remove_area_with_start_vpn ...(+45) |
| `src/trap/context.rs#app_init` | `os/src/mm/memory_set.rs#create_elf_tables` | 同模块上下文对齐/差异观察 | 内部闭环逻辑 | (phys_user_sp as *mut usize).add, AuxvEntry::new, T::from_token, T::from_token(self.token())
            .translate_va, T::from_token(self.token())
            .translate_va(VirtAddr::from(user_sp))
            .unwrap, Vec::<*const u8>::new ...(+32) |

#### 中断/异常处理
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `src/lang_items.rs#panic` | `os/src/fs/dev/tty.rs#default` | 同模块上下文对齐/差异观察 | Registers::zeroed, asm!, error!, info.location, print_backtrace, print_registers ...(+2) | 内部闭环逻辑 |

#### 系统调用
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `src/syscall/mod.rs#syscall` | `user/src/syscall.rs#syscall` | 同板块同名 | crate::println!, sys_exit, sys_getpid, sys_read, sys_write, sys_yield | __syscall |
| `user/src/syscall.rs#sys_write` | `user/src/syscall.rs#sys_write` | 同板块同名 | asm! | buffer.as_ptr, buffer.len, syscall |
| `user/src/syscall.rs#sys_exit` | `user/src/syscall.rs#sys_exit` | 同板块同名 | asm! | panic!, syscall |
| `user/src/syscall.rs#sys_yield` | `user/src/syscall.rs#sys_yield` | 同板块同名 | asm! | syscall |

#### 文件系统
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `src/syscall/fs.rs#sys_write` | `os/src/fs/directory_tree.rs#open` | 同模块上下文对齐/差异观察 | buf.add, buf.add(i).read, sbi::console_putchar | (*last_comp).to_string, Arc::downgrade, DiskInodeType::from_char, Err, Ok, PATH_CACHE.lock ...(+28) |
| `src/syscall/fs.rs#sys_read` | `os/src/fs/directory_tree.rs#rename` | 同模块上下文对齐/差异观察 | 内部闭环逻辑 | Arc::downgrade, Arc::new, Err, FILE_SYSTEM.rename, FILE_SYSTEM.rename(old_path, new_path).unwrap, Mutex::new ...(+35) |

#### 设备驱动
仅参考项目中观察到该模块代表函数，目标项目没有同模块可对齐证据：
- 参考项目：`os/src/drivers/block/sata_blk.rs#pci_init`，调用摘要：AHCI::new, Some, Status::empty, dev.bars.iter, dev.bars.iter().enumerate, dev.bars.iter().enumerate().for_each ...(+3)
- 参考项目：`os/src/drivers/block/sata_blk.rs#enable`，调用摘要：am.read16, am.write32
- 参考项目：`os/src/drivers/block/mod.rs#block_device_test`，调用摘要：BLOCK_DEVICE.clone, assert_eq!, block_device.read_block, block_device.write_block, println!, write_buffer.iter_mut
- 参考项目：`os/src/mm/map_area.rs#force_swap_out`，调用摘要：Err, Frame::SwappedOut, Ok, SWAP_DEVICE.lock, SWAP_DEVICE.lock().write, frame_ref.ppn.get_bytes_array

#### 文件系统/第三方库
仅参考项目中观察到该模块代表函数，目标项目没有同模块可对齐证据：
- 参考项目：`os/src/mm/mod.rs#ext4_user_malloc`，调用摘要：HEAP_ALLOCATOR
        .lock, HEAP_ALLOCATOR
        .lock()
        .alloc, HEAP_ALLOCATOR
        .lock()
        .alloc(Layout::array::<u8>(size).unwrap())
        .unwrap, HEAP_ALLOCATOR
        .lock()
        .alloc(Layout::array::<u8>(size).unwrap())
        .unwrap()
        .as_ptr, Layout::array::<u8>, Layout::array::<u8>(size).unwrap
- 参考项目：`os/src/fs/ext4_file.rs#get_single_cache`，调用摘要：todo!
- 参考项目：`os/src/fs/ext4_file.rs#create`，调用摘要：todo!
- 参考项目：`os/src/fs/ext4_file.rs#get_all_caches`，调用摘要：todo!

#### 通用模块/其他
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `user/src/lib.rs#_start` | `user/src/lib.rs#_start` | 同板块同名 | exit, main | _parameter, asm!, unreachable! |
| `src/console.rs#write_str` | `os/src/console.rs#write_str` | 同板块同名 | Ok, console_putchar, s.chars | Ok, console_flush, console_putchar, s.chars |
| `user/src/lib.rs#write_str` | `user/src/console.rs#write_str` | 同板块同名 | Ok, s.as_bytes, write | Ok, s.as_bytes, write |
| `src/console.rs#print` | `os/src/console.rs#print` | 同板块同名同结构 | Stdout.write_fmt, Stdout.write_fmt(args).unwrap | KernelOutput.write_fmt, KernelOutput.write_fmt(args).unwrap |


好的，审计官。基于提供的证据底座、双方辩护材料及AST抽样数据，现出具最终同源性定性仲裁报告。

---

### 1. 宏观判定
- **定性结论**: **独立开发**
- **核心依据**: 客观AST相似度仅为0.29%，双方共同AST指纹仅有2个，且均位于通用模块的标准化输出层。在操作系统内核的核心竞争力领域，如内存管理、任务调度、文件系统、中断/异常处理等模块，共同AST指纹为0。目标项目是一个简化的批处理系统（Batch OS），而参考项目是一个功能完备的多任务宏内核，两者在架构层级、实现机制和代码体量上存在本质差异。控方提出的“铁证”均为Rust语言标准范式和POSIX系统调用命名规范，不构成实质性代码复用。

### 2. 全模块覆盖矩阵解读
| 模块 | 双方存在 | 共同AST指纹 | 覆盖率解读 | 证据强度 |
| :--- | :--- | :--- | :--- | :--- |
| **进程管理** | 是 | 1 | 目标覆盖率25.0%，源于同名系统调用外壳；参考覆盖率仅1.56%。 | **弱** |
| **任务调度/异步运行时** | 是 | 0 | 覆盖率均为0.0%。目标为简单批处理循环，参考为复杂调度器，实现完全不同。 | **未形成** |
| **内存管理** | 是 | 0 | 覆盖率均为0.0%。目标无虚拟内存，参考有完整页表与内存集管理，无共同结构。 | **未形成** |
| **中断/异常处理** | 是 | 0 | 覆盖率均为0.0%。目标为简单panic处理，参考为信号机制，无共同结构。 | **未形成** |
| **系统调用** | 是 | 0 | 覆盖率均为0.0%。目标用户态直接内联汇编，参考通过封装函数调用，机制不同。 | **未形成** |
| **文件系统** | 是 | 0 | 覆盖率均为0.0%。目标仅通过SBI输出字符，参考有完整的ext4/VFS，无共同结构。 | **未形成** |
| **设备驱动** | 否 | 0 | 仅参考项目存在。目标项目无此模块，是架构差异的明证。 | **未形成** |
| **网络协议栈** | 否 | 0 | 双方均未体现。 | **未形成** |
| **文件系统/第三方库** | 否 | 0 | 仅参考项目存在。目标项目无此模块。 | **未形成** |
| **通用模块/其他** | 是 | 1 | 目标覆盖率6.25%，参考覆盖率2.86%。重合点在于`print!`宏和`Write::write_str` trait的标准实现。 | **弱** |

### 3. 底层 AST 关键证据指纹表
| 所属板块 | 目标项目路径 | 参考项目路径 | 证据类型 | 技术说明 |
| :--- | :--- | :--- | :--- | :--- |
| 通用模块/其他 | `[user/src/lib.rs]` 函数 `print` | `[user/src/console.rs]` 函数 `print` | 语言标准范式 | 两者均使用`Stdout.write_fmt(args).unwrap()`，这是Rust `no_std`环境下实现`print!`宏的唯一标准AST展开，非独创代码。 |
| 通用模块/其他 | `[user/src/lib.rs]` 函数 `write_str` | `[user/src/console.rs]` 函数 `write_str` | 语言标准范式 | 两者均使用`s.as_bytes`并调用内部`write`，这是实现`core::fmt::Write` trait的固定模式，由trait语义强制。 |
| 进程管理 | `[src/syscall/process.rs]` 函数 `sys_exit` | `[os/src/syscall/process.rs]` 函数 `sys_exit` | 同名异构 | 函数名同为POSIX标准。目标调用`batch::run_next_app`，参考调用`exit_current_and_run_next`，底层调度对象完全不同。 |
| 进程管理 | `[src/syscall/process.rs]` 函数 `sys_getpid` | `[os/src/syscall/process.rs]` 函数 `sys_getpid` | 同名异构 | 函数名同为POSIX标准。目标调用`crate::batch::current_pid`，参考调用`current_task().unwrap`，进程模型完全不同。 |
| 进程管理 | `[src/syscall/process.rs]` 函数 `sys_yield` | `[os/src/syscall/process.rs]` 函数 `sys_yield` | 同名异构 | 函数名同为POSIX标准。目标为内部闭环逻辑，参考调用`suspend_current_and_run_next`，实现机制完全不同。 |
| 系统调用 | `[user/src/lib.rs]` 函数 `exit` | `[user/src/usr_call.rs]` 函数 `exit` | 同名异构 | 均为对`sys_exit`的薄封装，是用户库的标准做法。目标置于`lib.rs`，参考置于`usr_call.rs`，模块组织不同。 |
| 系统调用 | `[user/src/lib.rs]` 函数 `yield_` | `[user/src/usr_call.rs]` 函数 `yield_` | 同名异构 | 均为对`sys_yield`的薄封装，是用户库的标准做法。模块组织不同。 |

### 4. 核心代码调用流对比剖析
* **【进程管理/系统调用路径】**
* **底层技术析理**: 双方均实现了POSIX标准的`sys_exit`系统调用，但底层进程调度模型存在根本性差异。目标项目采用单任务批处理模型，`sys_exit`直接调用`batch::run_next_app`加载并执行下一个应用，无进程控制块（PCB）或任务调度器的概念。参考项目采用多任务模型，`sys_exit`调用`exit_current_and_run_next`，该函数会操作当前任务的PCB，将其状态置为退出，并从就绪队列中选择下一个任务进行切换。两者的内存管理对象和调度策略完全不同。
* **代码溯源链接**:
  - 目标项目: `src/syscall/process.rs#sys_exit`
  - 参考项目: `os/src/syscall/process.rs#sys_exit`

* **【系统调用/用户态接口】**
* **底层技术析理**: 双方用户库均对`sys_write`系统调用进行了封装，但触发系统调用的底层机制不同。目标项目在`[user/src/syscall.rs]`中直接使用`asm!`内联汇编指令执行`ecall`，陷入内核。参考项目在`[user/src/syscall.rs]`中则调用了一个名为`syscall`或`__syscall`的封装函数，由该函数统一处理系统调用号和参数的传递。这反映了不同的用户库设计分层和抽象策略。
* **代码溯源链接**:
  - 目标项目: `user/src/syscall.rs#sys_write`
  - 参考项目: `user/src/syscall.rs#sys_write`

* **【通用模块/其他/输出底层】**
* **底层技术析理**: 双方在用户态实现`print!`宏时，AST指纹完全一致，均使用了`Stdout.write_fmt(args).unwrap()`。这是Rust语言在`no_std`环境下实现格式化输出的标准范式，由`core::fmt::Write` trait定义。任何开发者遵循Rust标准库文档编写此功能，其生成的AST都必然相同。这属于语言层面的同构，而非代码层面的抄袭。目标项目在内核态也使用了`Stdout`，而参考项目使用了`KernelOutput`，这反而证明目标项目未直接复用参考的内核态输出设计。
* **代码溯源链接**:
  - 目标项目: `user/src/lib.rs#print`
  - 参考项目: `user/src/console.rs#print`

### 5. 单方模块与降权项
* **单方模块**: 参考项目独有的【设备驱动】（如SATA/AHCI驱动）、【文件系统/第三方库】（如ext4实现）等模块，在目标项目中完全缺失。这直接证明目标项目是一个功能范围远小于参考项目的独立实现，而非通过删减代码得到的“精简版”。
* **降权项说明**:
  * **同名系统调用/函数**: `sys_exit`、`sys_write`、`exit`、`yield_`等函数名均源自POSIX标准或Rust语言惯例，属于公共领域命名规范，不能作为同源性证据。
  * **跨板块比对**: 控方将目标项目的`sys_write`（文件系统/控制台输出）与参考项目的`directory_tree::open`（文件系统/路径解析）进行比对，属于跨功能硬凑证据。两者功能完全不同，且模块矩阵显示文件系统模块共同指纹为0。
  * **通用宏与语言特性**: `println!`、`assert!`、`asm!`等是Rust编译器或标准库提供的固定设施，其AST展开由编译器决定，任何项目使用它们都会产生相同的指纹。

### 6. 保守结论
综合客观AST指纹数据（相似度0.29%，共同指纹仅2个）、全模块覆盖矩阵分析（核心模块零重合）以及关键代码调用流的架构差异剖析，本次审计认定目标项目`2026-T202610574999903-XX`与参考项目`2024-NPUcore-IMPACT-西北工业大学`之间**不存在实质性代码复用或抄袭行为**。双方唯一的两个共同AST指纹均源于Rust语言实现标准输出功能的强制范式，属于公共知识领域。目标项目作为一个简化的Batch OS，与参考项目的复杂宏内核在进程模型、内存管理、文件系统等核心子系统上存在根本性架构差异，符合独立开发的典型特征。整体复用风险极低。

------------------------------

