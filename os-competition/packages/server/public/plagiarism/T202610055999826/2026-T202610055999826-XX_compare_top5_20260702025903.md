# 🛡️ OS 内核赛自动化代码级架构审计报告

**审计时间**: `2026-07-02 02:59:03`
**参赛作品名称**: `2026-T202610055999826-XX`
**年份**: `2026`
**学校**: `XX`
**队伍名称**: `T202610055999826`
**比赛名字**: `OS-Competition`
**比赛子赛道**: `Kernel-Implementation`
**仓库地址**: `https://gitlab.eduxiji.net/educg-group-43501-3132633/T202610055999826-3679`
**推测开发语言**: `Rust / C (由AST探针自动识别)`
**特征函数量**: `559`

## ⚔️ 底层代码同源性对比 (AST Homology)
> 本部分由专门的代码审查智能体，对该项目与符合年份条件的历届最高相似度项目进行控制流与底层调用对比。新版报告会先给出确定性的全模块覆盖矩阵，再给出 Agent 的分模块解释，避免只围绕少量抽样函数展开。

### 🏆 Top 5 底层对标: vs `2024-NPUcore-IMPACT-西北工业大学`（西北工业大学 / NPUcore-IMPACT!!!）

#### 对方作品信息

**对方年份**: `2024`
**对方学校**: `西北工业大学`
**对方队伍名称**: `NPUcore-IMPACT!!!`
**对方比赛名字**: `OS-Competition`
**对方比赛子赛道**: `Kernel-Implementation`
**对方仓库地址**: `https://gitlab.eduxiji.net/educg-group-26011-2376549/T202410699992496-312`

> 双向覆盖率：共同 AST 指纹: 18；唯一 AST 指纹: 目标项目 499，参考项目 642；覆盖率: 目标项目 3.61%，参考项目 2.8%

## 全模块对比总览（确定性证据）

> 总体覆盖率：共同 AST 指纹: 18；唯一 AST 指纹: 目标项目 499，参考项目 642；覆盖率: 目标项目 3.61%，参考项目 2.8%

### 1. 模块覆盖矩阵
| 模块 | 目标函数数 | 参考函数数 | 共同唯一 AST 指纹 | Target 覆盖 | Base 覆盖 | 状态 |
| :--- | ---: | ---: | ---: | ---: | ---: | :--- |
| 进程管理 | 69 | 69 | 2 | 3.03% | 3.12% | 双方存在且有结构重合 |
| 任务调度/异步运行时 | 139 | 123 | 1 | 0.77% | 0.9% | 双方存在且有结构重合 |
| 内存管理 | 184 | 259 | 14 | 8.43% | 6.11% | 双方存在且有结构重合 |
| 中断/异常处理 | 15 | 1 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |
| 系统调用 | 18 | 29 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |
| 文件系统 | 24 | 274 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |
| 设备驱动 | 41 | 22 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |
| 网络协议栈 | 0 | 0 | 0 | 0.0% | 0.0% | 双方均未体现 |
| 文件系统/第三方库 | 45 | 31 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |
| 通用模块/其他 | 24 | 50 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |

### 2. 分模块证据清单

#### 进程管理
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `src/syscall/task.rs#sys_getpid` | `os/src/syscall/process.rs#sys_getpid` | 同板块同名 | crate::kprintln!, crate::task::current, crate::task::current().pid | current_task, current_task().unwrap |
| `src/syscall/task.rs#sys_getppid` | `os/src/syscall/process.rs#sys_getppid` | 同板块同名 | crate::task::current, crate::task::current().parent | current_task, current_task().unwrap, inner.parent.as_ref, inner.parent.as_ref().unwrap, inner.parent.as_ref().unwrap().upgrade, inner.parent.as_ref().unwrap().upgrade().unwrap ...(+1) |
| `src/task/processor.rs#current` | `os/src/task/processor.rs#current` | 同板块同名 | self.current.as_mut, self.current.as_mut().expect | self.current.as_ref, self.current.as_ref().map |
| `src/task/processor.rs#take_current` | `os/src/task/processor.rs#take_current` | 同板块同名 | self.current.take, self.current.take().expect | self.current.take |

#### 任务调度/异步运行时
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `src/syscall/mod.rs#syscall` | `os/src/syscall/mod.rs#syscall` | 同板块同名 | crate::arch::shutdown, crate::kprintln!, crate::syscall::errno::err, crate::task::current, crate::task::current().pid, crate::task::current().set_clear_child_tid ...(+48) | Errno::try_from, [
            //black list
            SYSCALL_YIELD,
            SYSCALL_READ,
            SYSCALL_WRITE,
            SYSCALL_GETDENTS64,
            SYSCALL_READV,
            SYSCALL_WRITEV,
            SYSCALL_PSELECT6,
            SYSCALL_SIGACTION,
            SYSCALL_SIGPROCMASK,
            // SYSCALL_WAIT4,
            // SYSCALL_GETPPID,
            SYSCALL_CLOCK_GETTIME,
        ]
        .contains, args.len, crate::task::current_task, crate::task::current_task()
                .unwrap, crate::task::current_task()
                .unwrap()
                .acquire_inner_lock ...(+94) |
| `src/syscall/fd.rs#sys_read` | `os/src/syscall/fs.rs#sys_read` | 同板块同名 | PIPE_BUFFERS.get_mut, chunk.as_slice, chunk.is_empty, chunk.len, core::cmp::min, crate::task::current ...(+15) | UserBuffer::new, current_task, current_task().unwrap, fd_table.get_ref, file_descriptor.read_user, file_descriptor.readable ...(+3) |
| `src/syscall/fd.rs#sys_write` | `os/src/syscall/fs.rs#sys_write` | 同板块同名 | PIPE_BUFFERS.get_mut, core::cmp::min, crate::drivers::serial::Serial::putchar, crate::task::current, crate::task::with_current_memory_set, err ...(+9) | UserBuffer::new, current_task, current_task().unwrap, fd_table.get_ref, file_descriptor.writable, file_descriptor.write_user ...(+3) |
| `src/syscall/fd.rs#sys_fstatat` | `os/src/syscall/fs.rs#sys_fstatat` | 同板块同名 | ((data.len() as u64 + 511) / 512).to_le_bytes, (data.len() as u64).to_le_bytes, 0u32.to_le_bytes, 0u64.to_le_bytes, 0x81a4u32.to_le_bytes, 1u32.to_le_bytes ...(+23) | FstatatFlags::from_bits, copy_to_user, copy_to_user(token, &file_descriptor.get_stat(), buf as *mut Stat).is_err, current_task, current_task().unwrap, current_user_token ...(+12) |

#### 内存管理
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `src/mm/heap_allocator.rs#dealloc` | `os/src/mm/frame_allocator.rs#dealloc` | 同板块同名 | alloc_start.checked_add, core::cmp::max, header_ptr.read, kernel_heap_end, kernel_heap_start, layout.size ...(+6) | Some, log::trace!, option_env!, panic!, self.recycled.iter, self.recycled.iter().find ...(+2) |
| `src/mm/heap_allocator.rs#alloc` | `os/src/mm/frame_allocator.rs#alloc` | 同板块同名 | crate::kprintln!, crate::println!, curr.is_null, header_ptr.write, layout.size, min_block_size ...(+6) | (self.current - 1).into, FrameTracker::new, FrameTracker::new_uninit, Some, log::trace!, ppn.into ...(+1) |
| `src/mm/memory_set.rs#copy_from_user` | `os/src/mm/page_table.rs#copy_from_user` | 同板块同名 | VirtAddr, area.perm, crate::println!, dst.len, self.find_area, self.read_byte ...(+1) | Ok, UserBuffer::new, UserBuffer::new(translated_byte_buffer(token, src as *const u8, size)?)
            .read, VirtAddr::from, VirtAddr::from(src as usize + size - 1).floor, VirtAddr::from(src as usize).floor ...(+5) |
| `src/mm/memory_set.rs#copy_to_user` | `os/src/mm/page_table.rs#copy_to_user` | 同板块同名 | core::cmp::min, core::slice::from_raw_parts_mut, dst[page_offset..page_offset + chunk].copy_from_slice, phys_to_kvirt, remaining.is_empty, remaining.len ...(+1) | Ok, UserBuffer::new, UserBuffer::new(translated_byte_buffer(token, dst as *mut u8, size)?)
            .write, VirtAddr::from, VirtAddr::from(dst as usize + size - 1).floor, VirtAddr::from(dst as usize).floor ...(+5) |

#### 中断/异常处理
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `src/trap/mod.rs#init` | `os/src/fs/dev/tty.rs#default` | 同模块上下文对齐/差异观察 | crate::arch::read_stvec, crate::arch::set_trap_entry, crate::arch::trap_entry_addr, crate::kprintln!, crate::println! | 内部闭环逻辑 |

#### 系统调用
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `src/syscall/fd.rs#normalize_absolute_path` | `user/src/syscall.rs#sys_exec` | 同模块上下文对齐/差异观察 | String::from, Vec::new, out.push, out.push_str, part.is_empty, parts.iter ...(+4) | args.as_ptr, envp.as_ptr, path.as_ptr, syscall |
| `src/syscall/fd.rs#sys_umount2` | `os/src/syscall/socket.rs#sys_sendto` | 同模块上下文对齐/差异观察 | MOUNT_POINTS
            .iter, MOUNT_POINTS
            .iter()
            .position, MOUNT_POINTS.remove, copy_user_cstr, full_path.as_str, normalize_path ...(+1) | 内部闭环逻辑 |
| `src/syscall/fd.rs#sys_mkdirat` | `user/src/syscall.rs#sys_read` | 同模块上下文对齐/差异观察 | CREATED_DIRS.push, copy_user_cstr, dir_exists, err, full_path.as_str, normalize_path | buffer.as_mut_ptr, buffer.len, syscall |
| `src/syscall/fd.rs#join_path` | `user/src/usr_call.rs#sleep` | 同模块上下文对齐/差异观察 | String::from, out.ends_with, out.push, out.push_str | sys_get_time, sys_yield |

#### 文件系统
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `src/syscall/fd.rs#sys_mount` | `os/src/syscall/fs.rs#sys_mount` | 同板块同名 | CREATED_DIRS
                .iter, CREATED_DIRS
                .iter()
                .any, CREATED_DIRS.push, MOUNT_POINTS
            .iter, MOUNT_POINTS
            .iter()
            .any, MOUNT_POINTS.push ...(+9) | MountFlags::from_bits, MountFlags::from_bits(mountflags).unwrap, current_user_token, filesystemtype.is_null, info!, source.is_null ...(+3) |
| `src/drivers/mod.rs#block_write_bytes` | `os/src/fs/directory_tree.rs#open` | 同模块上下文对齐/差异观察 | buf.len, core::cmp::min, sec_buf[off..off + n].copy_from_slice, virtio_mmio::read_block, virtio_mmio::write_block, virtio_pci::read_block ...(+1) | (*last_comp).to_string, Arc::downgrade, DiskInodeType::from_char, Err, Ok, PATH_CACHE.lock ...(+28) |
| `src/loader/elf.rs#parse_elf_header` | `os/src/fs/directory_tree.rs#rename` | 同模块上下文对齐/差异观察 | Some, crate::arch::elf_machine, data.get, data.len, phentsize.checked_mul, phoff.checked_add ...(+2) | Arc::downgrade, Arc::new, Err, FILE_SYSTEM.rename, FILE_SYSTEM.rename(old_path, new_path).unwrap, Mutex::new ...(+35) |
| `src/drivers/virtio_mmio.rs#read_bytes` | `os/src/syscall/fs.rs#sys_utimensat` | 同模块上下文对齐/差异观察 | buf.is_empty, buf.len, buf[done..done + copy_len]
                .copy_from_slice, core::cmp::min, err, offset.checked_add ...(+2) | Some, String::new, TimeSpec::now, UtimensatFlags::from_bits, __openat, copy_from_user ...(+10) |

#### 设备驱动
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `src/drivers/virtio_mmio.rs#read_block` | `os/src/drivers/block/sata_blk.rs#read_block` | 同板块同名 | BATCH_BUF.as_ptr, BATCH_BUF[..read_len].iter_mut, DATA_BUF.as_ptr, DATA_BUF.iter_mut, avail_idx.wrapping_add, buf.copy_from_slice ...(+10) | Ok, buf.chunks_mut, self.0
                .lock, self.0
                .lock()
                .read_block |
| `src/drivers/virtio_mmio.rs#write_block` | `os/src/drivers/block/sata_blk.rs#write_block` | 同板块同名 | BATCH_BUF.as_ptr, BATCH_BUF[..write_len].copy_from_slice, DATA_BUF.as_ptr, DATA_BUF[..write_len].copy_from_slice, avail_idx.wrapping_add, buf.len ...(+9) | Ok, buf.chunks, self.0
                .lock, self.0
                .lock()
                .write_block |
| `src/drivers/virtio_pci.rs#read_block` | `os/src/drivers/block/sata_blk.rs#read_block` | 同板块同名 | BATCH_BUF.as_ptr, BATCH_BUF[..read_len].iter_mut, DATA.as_ptr, DATA.iter_mut, buf.copy_from_slice, buf.len ...(+10) | buf.chunks_mut, self.0
                .lock, self.0
                .lock()
                .read_block |
| `src/drivers/virtio_pci.rs#write_block` | `os/src/drivers/block/sata_blk.rs#write_block` | 同板块同名 | BATCH_BUF.as_ptr, BATCH_BUF[..write_len].copy_from_slice, DATA.as_ptr, DATA[..write_len].copy_from_slice, buf.len, compiler_fence ...(+9) | buf.chunks, self.0
                .lock, self.0
                .lock()
                .write_block |

#### 文件系统/第三方库
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `src/fs/ext4.rs#read_file_range` | `os/src/mm/mod.rs#ext4_user_malloc` | 同模块上下文对齐/差异观察 | Err, Ok, Some, buf.is_empty, buf.len, buf[..copy_len].copy_from_slice ...(+18) | HEAP_ALLOCATOR
        .lock, HEAP_ALLOCATOR
        .lock()
        .alloc, HEAP_ALLOCATOR
        .lock()
        .alloc(Layout::array::<u8>(size).unwrap())
        .unwrap, HEAP_ALLOCATOR
        .lock()
        .alloc(Layout::array::<u8>(size).unwrap())
        .unwrap()
        .as_ptr, Layout::array::<u8>, Layout::array::<u8>(size).unwrap |
| `src/fs/ext4.rs#read_file_from_inode` | `os/src/fs/ext4_file.rs#get_single_cache` | 同模块上下文对齐/差异观察 | Err, Ok, Vec::new, block_buf.len, core::cmp::min, crate::drivers::block_read_bytes ...(+10) | todo! |
| `src/fs/ext4.rs#read_dir_entries_from_inode` | `os/src/fs/ext4_file.rs#create` | 同模块上下文对齐/差异观察 | Err, Ok, Vec::new, buf.len, crate::drivers::block_read_bytes, crate::kprintln! ...(+8) | todo! |
| `src/fs/ext4.rs#create_file` | `os/src/fs/ext4_file.rs#get_all_caches` | 同模块上下文对齐/差异观察 | Err, Ok, Vec::new, crate::drivers::block_read_bytes, crate::drivers::block_write_bytes, crate::kprintln! ...(+17) | todo! |

#### 通用模块/其他
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `src/arch/riscv64/mod.rs#shutdown` | `user/src/lib.rs#_parameter` | 同模块上下文对齐/差异观察 | core::arch::asm!, crate::drivers::serial::Serial::putchar, crate::kprintln! | ((argv + i * core::mem::size_of::<usize>()) as *const usize).read_volatile, ((str_start + *i) as *const u8).read_volatile, (0usize..)
            .find, (0usize..)
            .find(|i| unsafe { ((str_start + *i) as *const u8).read_volatile() == 0 })
            .unwrap, HEAP.lock, HEAP.lock()
            .init ...(+10) |
| `src/loader/elf.rs#debug_parse_elf_header` | `os/src/main.rs#mem_clear` | 同模块上下文对齐/差异观察 | Some, crate::kprintln!, parse_elf_header | core::slice::from_raw_parts_mut, core::slice::from_raw_parts_mut(
            sbss as usize as *mut u8,
            crate::config::MEMORY_END - sbss as usize,
        )
        .fill, core::slice::from_raw_parts_mut(sbss as usize as *mut u8, ebss as usize - sbss as usize)
            .fill |
| `src/arch/loongarch64/mod.rs#timer_freq` | `user/src/lib.rs#_start` | 同模块上下文对齐/差异观察 | base.saturating_mul, cpucfg | _parameter, asm!, unreachable! |
| `src/arch/riscv64/mod.rs#sbi_set_timer` | `os/src/console.rs#log_init` | 同模块上下文对齐/差异观察 | core::arch::asm! | log::set_logger, log::set_logger(&LOGGER).unwrap, log::set_max_level, option_env! |


好的，审计官。基于提供的客观AST覆盖率数据、双方模块画像、原始AST抽样证据以及控辩双方的陈述，我将进行最终的独立同源性定性仲裁。

### 1. 宏观判定
- **定性结论**: **独立开发 / 底层架构显著差异**
- **核心依据**: 客观AST相似度仅为 **1.6%**，共同AST指纹仅 **18** 个，而双方各自拥有数百个唯一指纹（目标499，参考642）。双向覆盖率均低于4%（目标3.61%，参考2.8%）。此数据远未达到“实质性相似”或“抄袭/复用”的阈值，从根本上否定了系统性代码复用的可能性。控方指控主要建立在领域共性、POSIX标准接口及Rust语言惯用法之上，而非独创性代码逻辑的挪用。

### 2. 全模块覆盖矩阵解读
| 模块 | 双方存在 | 共同AST指纹 | 目标覆盖 | 参考覆盖 | 证据强度 | 解读 |
| :--- | :---: | :---: | :---: | :---: | :---: | :--- |
| **进程管理** | 是 | 2 | 3.03% | 3.12% | **弱** | 共同指纹极少，覆盖率极低。`sys_getpid`等函数同名由POSIX标准决定，`current`等函数是Rust `Option`枚举的惯用访问器。双方在任务所有权、同步模型（有无`acquire_inner_lock`）上存在根本架构差异。 |
| **任务调度/异步运行时** | 是 | 1 | 0.77% | 0.9% | **弱** | 覆盖率不足1%。`syscall`分发函数同名是操作系统必然设计，但其内部调用图谱差异巨大，目标项目为简单函数指针表，参考项目包含黑名单过滤等复杂逻辑。 |
| **内存管理** | 是 | 14 | 8.43% | 6.11% | **中** | 共同指纹数量最多，但主要集中在`alloc/dealloc`、`copy_from/to_user`等基础操作。这些是任何内存管理器必须实现的功能，其内部调用（如目标用裸指针链表，参考用`FrameTracker`）揭示了不同的分配器实现策略。 |
| **中断/异常处理** | 是 | 0 | 0.0% | 0.0% | **未形成** | 无共同AST指纹。目标项目`trap/mod.rs#init`与参考项目`fs/dev/tty.rs#default`的配对属于跨模块硬凑，不构成证据。 |
| **系统调用** | 是 | 0 | 0.0% | 0.0% | **未形成** | 无共同AST指纹。控方提供的证据均为跨模块配对（如目标`syscall/fd.rs`与参考`user/src/syscall.rs`），属于“硬凑证据”，不成立。 |
| **文件系统** | 是 | 0 | 0.0% | 0.0% | **未形成** | 无共同AST指纹。目标项目`sys_mount`与参考项目`sys_mount`虽同名，但AST调用图谱完全不同，目标操作内存中的挂载点列表，参考则处理`MountFlags`等复杂参数。 |
| **设备驱动** | 是 | 0 | 0.0% | 0.0% | **未形成** | 无共同AST指纹。`read_block/write_block`同名是块设备驱动的通用接口，但目标项目基于VirtIO协议直接操作MMIO/PIO，参考项目则通过SATA抽象层，底层硬件交互逻辑完全不同。 |
| **网络协议栈** | 否 | 0 | 0.0% | 0.0% | **未形成** | 双方均未体现该模块。 |
| **文件系统/第三方库** | 是 | 0 | 0.0% | 0.0% | **未形成** | 无共同AST指纹。目标项目实现了具体的ext4读取逻辑，参考项目则多为`todo!`占位或完全不同模块的函数，无同源性。 |
| **通用模块/其他** | 是 | 0 | 0.0% | 0.0% | **未形成** | 无共同AST指纹。配对均为跨模块、跨层次（如目标架构相关代码与参考用户库代码）的强行对齐，无任何参考价值。 |

### 3. 底层 AST 关键证据指纹表
| 所属板块 | 目标项目路径 | 参考项目路径 | 证据类型 | 技术说明 |
| :--- | :--- | :--- | :--- | :--- |
| 进程管理 | `src/task/processor.rs#current` | `os/src/task/processor.rs#current` | 弱证据 | 同名函数，均为获取当前任务引用。但目标使用`as_mut().expect`（可变借用，空时panic），参考使用`as_ref().map`（不可变借用，返回Option），语义和错误处理策略不同。 |
| 进程管理 | `src/syscall/task.rs#sys_getpid` | `os/src/syscall/process.rs#sys_getpid` | 弱证据 | POSIX标准系统调用，函数名必然相同。目标直接访问`current().pid`，参考通过`current_task().unwrap()`获取，访问路径和中间抽象层不同。 |
| 内存管理 | `src/mm/heap_allocator.rs#alloc` | `os/src/mm/frame_allocator.rs#alloc` | 弱证据 | 同名函数，功能相似。但目标基于裸指针和链表管理堆内存，参考基于`FrameTracker`和物理页号(PPN)管理物理帧，分配粒度和元数据管理方式完全不同。 |
| 内存管理 | `src/mm/memory_set.rs#copy_from_user` | `os/src/mm/page_table.rs#copy_from_user` | 弱证据 | 同名函数，实现用户态数据拷贝。目标通过`find_area`和`read_byte`逐字节处理，参考通过`UserBuffer`和`translated_byte_buffer`进行批量翻译和拷贝，抽象层次和实现路径迥异。 |
| 设备驱动 | `src/drivers/virtio_mmio.rs#read_block` | `os/src/drivers/block/sata_blk.rs#read_block` | 弱证据 | 同名函数，实现块设备读取。目标直接操作VirtIO MMIO寄存器进行批量数据传输，参考则通过`self.0.lock().read_block`调用SATA驱动抽象层，底层硬件和驱动模型均不同。 |

### 4. 核心代码调用流对比剖析
* **【进程管理/进程退出路径】**
* **底层技术析理**: 两者均实现了`exit`系统调用，但内部架构截然不同。目标项目采用“策略与机制分离”的设计，`sys_exit` -> `exit_current`负责状态清理（标记僵尸、唤醒父进程），`kernel_exit`负责底层上下文切换，直接调用`crate::arch::switch_task_context`。参考项目则将两者合并于`exit_current_and_run_next`，并通过`do_exit`、`schedule`等更高级的调度器抽象完成，且其任务结构体包含`acquire_inner_lock`等复杂的锁机制。目标项目的退出路径更直接、简化，缺少参考项目中的线程组管理、futex等复杂特性。
* **代码溯源链接**:
  - 目标项目: `src/syscall/task.rs#sys_exit` -> `src/task/mod.rs#exit_current` -> `src/task/mod.rs#kernel_exit`
  - 参考项目: `os/src/syscall/process.rs#sys_exit` -> `os/src/task/mod.rs#exit_current_and_run_next`

* **【内存管理/用户态内存拷贝】**
* **底层技术析理**: 两者均需实现用户态与内核态的数据拷贝，但抽象模型完全不同。目标项目的`copy_from_user`基于`MemorySet`，通过`find_area`查找虚拟内存区域并逐字节读取，逻辑直接且底层。参考项目则构建了`UserBuffer`和`translated_byte_buffer`抽象，将地址翻译和缓冲区管理封装起来，提供了更高级、更安全的接口。这表明两者在内存管理的分层设计和抽象哲学上存在根本差异。
* **代码溯源链接**:
  - 目标项目: `src/mm/memory_set.rs#copy_from_user`
  - 参考项目: `os/src/mm/page_table.rs#copy_from_user`

### 5. 单方模块与降权项
*   **单方模块/功能**：参考项目实现了大量目标项目缺失的复杂系统功能，如`sys_futex`、`sys_mmap`、`sys_clone`、`sys_sigreturn`、`sys_prlimit`等，以及完整的信号机制、资源限制管理和线程组支持。这证明两者在功能完整度和复杂度上不在同一层级，目标项目是一个更简化的教学/竞赛内核，不可能通过“拆分伪装”从参考项目衍生而来。
*   **降权项说明**：
    *   **POSIX标准接口**：`sys_getpid`、`sys_exit`、`sys_execve`等函数名由标准规定，任何兼容POSIX的内核都必须使用，不构成独创性证据。
    *   **Rust语言惯用法**：`Option::take`、`Vec::push/pop`、`panic!`、`println!`等是Rust标准库和核心语言提供的公共构件，其在AST中的出现是语言使用的必然结果，与业务逻辑复用无关。
    *   **跨模块/跨层次配对**：控方证据中存在大量将目标项目的系统调用层函数与参考项目的用户库函数、或完全不同模块的函数进行配对的情况（如将`sys_mkdirat`与`user/src/syscall.rs#sys_read`配对），此类证据无效。
    *   **通用领域模式**：“查找-修改-唤醒”是任何调度或事件处理系统的通用设计模式，不能作为特定代码复用的证据。

### 6. 保守结论
本次审计的核心结论为**独立开发**。客观AST覆盖率（1.6%）极低，是判定无实质性代码复用的决定性证据。控方提出的“拆分伪装”、“下沉式抄袭”等指控，在宏观覆盖率数据和微观架构差异面前无法成立。双方在进程管理、内存管理等核心模块的同步模型、抽象层次和功能复杂度上存在根本性架构差异，目标项目缺失了参考项目中大量复杂的系统调用和内核机制。观察到的少量相似性，均可被操作系统领域的标准化需求（POSIX接口）、Rust语言惯用法及通用设计模式所解释，不构成对独创性代码表达的侵权。整体风险极低，建议驳回抄袭指控。

------------------------------

