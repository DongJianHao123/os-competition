# 🛡️ OS 内核赛自动化代码级架构审计报告

**审计时间**: `2026-07-02 03:01:28`
**参赛作品名称**: `2026-T2026100109910165-XX`
**年份**: `2026`
**学校**: `XX`
**队伍名称**: `T2026100109910165`
**比赛名字**: `OS-Competition`
**比赛子赛道**: `Kernel-Implementation`
**仓库地址**: `https://gitlab.eduxiji.net/educg-group-43501-3132633/T2026100109910165-2460`
**推测开发语言**: `Rust / C (由AST探针自动识别)`
**特征函数量**: `883`

## ⚔️ 底层代码同源性对比 (AST Homology)
> 本部分由专门的代码审查智能体，对该项目与符合年份条件的历届最高相似度项目进行控制流与底层调用对比。新版报告会先给出确定性的全模块覆盖矩阵，再给出 Agent 的分模块解释，避免只围绕少量抽样函数展开。

### 🏆 Top 5 底层对标: vs `2021-广告位招租-重庆大学`（重庆大学 / 广告位招租）

#### 对方作品信息

**对方年份**: `2021`
**对方学校**: `重庆大学`
**对方队伍名称**: `广告位招租`
**对方比赛名字**: `OS-Competition`
**对方比赛子赛道**: `Kernel-Implementation`
**对方仓库地址**: `https://gitlab.eduxiji.net/deng19992008/openbhos`

> 双向覆盖率：共同 AST 指纹: 11；唯一 AST 指纹: 目标项目 556，参考项目 635；覆盖率: 目标项目 1.98%，参考项目 1.73%

## 全模块对比总览（确定性证据）

> 总体覆盖率：共同 AST 指纹: 11；唯一 AST 指纹: 目标项目 556，参考项目 635；覆盖率: 目标项目 1.98%，参考项目 1.73%

### 1. 模块覆盖矩阵
| 模块 | 目标函数数 | 参考函数数 | 共同唯一 AST 指纹 | Target 覆盖 | Base 覆盖 | 状态 |
| :--- | ---: | ---: | ---: | ---: | ---: | :--- |
| 进程管理 | 89 | 6 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |
| 任务调度/异步运行时 | 181 | 64 | 2 | 1.96% | 3.33% | 双方存在且有结构重合 |
| 内存管理 | 195 | 138 | 2 | 1.41% | 1.56% | 双方存在且有结构重合 |
| 中断/异常处理 | 168 | 83 | 1 | 1.25% | 1.54% | 双方存在且有结构重合 |
| 系统调用 | 2 | 24 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |
| 文件系统 | 91 | 75 | 3 | 3.9% | 4.62% | 双方存在且有结构重合 |
| 设备驱动 | 15 | 102 | 1 | 7.69% | 1.52% | 双方存在且有结构重合 |
| 网络协议栈 | 0 | 0 | 0 | 0.0% | 0.0% | 双方均未体现 |
| 文件系统/第三方库 | 76 | 0 | 0 | 0.0% | 0.0% | 仅目标项目体现 |
| 第三方库 | 0 | 1 | 0 | 0.0% | 0.0% | 仅参考项目体现 |
| 通用模块/其他 | 66 | 291 | 4 | 7.14% | 1.65% | 双方存在且有结构重合 |

### 2. 分模块证据清单

#### 进程管理
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `kernel/proc.c#inherit_pipe_fds` | `src/kernel/task/threads.c#threads_wait` | 同模块上下文对齐/差异观察 | 内部闭环逻辑 | GET_RUNNING_THREAD, intrLock_lock, intrLock_unlock, link_remove_by_node, sleep_on_queue |
| `kernel/vfs.c#procfs_make_child_node` | `src/kernel/task/threads.c#zombie_self` | 同模块上下文对齐/差异观察 | kstrcmp, procfs_current_proc, procfs_make_node, vfs_is_digit_string, vfs_parse_ino | GET_RUNNING_THREAD, intrLock_lock, intrLock_unlock, link_add_tail, scheduler_remove_self, threads_set_status |
| `arch/riscv/trap.c#sys_wait4` | `src/kernel/trap/syscall.c#syscall_getppid` | 同模块上下文对齐/差异观察 | proc_find_waitable_zombie, proc_reap_child, wait_to_kernel, write_user32 | GET_RUNNING_THREAD |
| `arch/riscv/trapbk.c#sys_wait4` | `src/kernel/task/threads.c#threads_exit_self` | 同模块上下文对齐/差异观察 | proc_find_waitable_zombie, proc_reap_child, wait_to_kernel, write_user32 | GET_RUNNING_THREAD, object_unref, scheduler_active, wakeup_all_on_queue, zombie_self |

#### 任务调度/异步运行时
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `arch/riscv/trap.c#pipe_get_fd` | `src/kernel/task/cpu.c#put_cpu_id` | 同板块同结构 | pipe_get_proc_fd | preempt_enable |
| `kernel/pipe.c#yield_to_kernel` | `src/kernel/task/scheduler.c#scheduler_cpu_init` | 同板块同结构 | proc_current, proc_yield, scheduler | get_cpu_var_no_preempt, link_init, put_cpu_var_no_preempt |
| `arch/riscv/trap.c#sys_write` | `src/platform/k210/driver/spi.c#spi_receive_data_standard` | 同模块上下文对齐/差异观察 | devfs_node_is_memory_file, devfs_write_memory_file, fd_can_write, fd_file_desc_checked, file->f_dentry->d_inode->i_fop->write, file->f_op->write ...(+6) | spi_get_frame_size, spi_set_tmod |
| `arch/riscv/trapbk.c#sys_write` | `src/platform/k210/driver/spi.c#spi_send_data_normal` | 同模块上下文对齐/差异观察 | devfs_node_is_memory_file, devfs_write_memory_file, fd_can_write, fd_file_desc_checked, file->f_dentry->d_inode->i_fop->write, file->f_op->write ...(+6) | memcpy, spi_get_frame_size, spi_set_tmod |

#### 内存管理
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `kernel/mm.c#align_up` | `firmware/rust_dep/linked-list-allocator-0.8.0/src/lib.rs#align_up` | 同板块同名 | 内部闭环逻辑 | align_down |
| `kernel/vfs.c#vfs_alloc_inode` | `src/lib/libc/src/libc_stub.c#libc_stub_malloc` | 同板块同结构 | vfs_alloc_generic_inode | heap_kalloc |
| `arch/loongarch/platform.c#platform_memory_end` | `src/kernel/mm/vmm/vmm.c#addr_is_user_space` | 同板块同结构 | 内部闭环逻辑 | 内部闭环逻辑 |
| `arch/riscv/trap.c#riscv_trap_handler` | `firmware/rustsbi-k210/src/main.rs#start_trap_rust` | 同模块上下文对齐/差异观察 | add_mmap_region, align_down, align_up_u64, cancel_thread, close_fd, copy_to_user ...(+116) | devintr, get_vaddr_u32, k210_hal::clint::mtime::read, llvm_asm!, mcause::read, mcause::read().cause ...(+17) |

#### 中断/异常处理
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `arch/loongarch/trap.c#loongarch_trap_placeholder` | `src/platform/k210/driver/gpiohs.c#gpiohs_irq_disable` | 同板块同结构 | 内部闭环逻辑 | 内部闭环逻辑 |
| `arch/loongarch/trap.c#fork_ret` | `src/kernel/trap/syscall.c#syscall_readv` | 同板块同结构 | 内部闭环逻辑 | 内部闭环逻辑 |
| `arch/riscv/trap.c#build_exec_stack` | `firmware/rustsbi-qemu/src/main.rs#start_trap_rust` | 同模块上下文对齐/差异观察 | align_down, copy_to_user_in_space, stack_guard_from_random, write_aux_by_index, write_user64_in_space | clint.get_mtime, get_vaddr_u32, hal::Clint::new, llvm_asm!, mcause::read, mcause::read().cause ...(+9) |
| `arch/riscv/trapbk.c#build_exec_stack` | `firmware/rust_dep/rustsbi-0.0.2/platform/qemu/src/main.rs#start_trap_rust` | 同模块上下文对齐/差异观察 | align_down, copy_to_user_in_space, stack_guard_from_random, write_aux_by_index, write_user64_in_space | clint.get_mtime, get_vaddr_u32, hal::Clint::new, llvm_asm!, mcause::read, mcause::read().cause ...(+9) |

#### 系统调用
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `arch/riscv/platform.c#platform_shutdown` | `firmware/rust_dep/rustsbi-0.0.2/rustsbi/src/ecall.rs#handle_ecall` | 同模块上下文对齐/差异观察 | 内部闭环逻辑 | SbiRet::not_supported, base::handle_ecall_base, ipi::handle_ecall_ipi, legacy::console_getchar, legacy::console_getchar().legacy_return, legacy::console_putchar ...(+10) |
| `arch/riscv/timer.c#sbi_set_timer` | `firmware/rust_dep/rustsbi-0.0.2/rustsbi/src/ecall/base.rs#handle_ecall_base` | 同模块上下文对齐/差异观察 | 内部闭环逻辑 | SbiRet::not_supported, get_marchid, get_mimpid, get_mvendorid, get_sbi_impl_id, get_sbi_impl_version ...(+2) |

#### 文件系统
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `kernel/vfs.c#sys_mount` | `src/platform/k210/driver/gpiohs.c#get_gpio_bit` | 同板块同结构 | vfs_do_mount | get_bit |
| `kernel/vfs.c#root_create` | `src/kernel/fs/fat32.c#entry_get_sub_read` | 同板块同结构 | vfs_create_memory_child | entry_sub_get |
| `kernel/vfs.c#root_mkdir` | `src/kernel/fs/fat32.c#parse_path_write` | 同板块同结构 | vfs_create_memory_child | _parse_path |
| `kernel/tmpfs.c#tmpfs_create` | `src/kernel/fs/block.c#block_get_read` | 同板块同结构 | tmpfs_create_child | _block_get |

#### 设备驱动
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `kernel/virtio_blk.c#fail_device` | `src/platform/k210/driver/sdcard.c#sd_lowlevel_init` | 同板块同结构 | set_status | gpiohs_set_drive_mode |
| `kernel/virtio_blk.c#virtio_blk_init` | `firmware/rust_dep/k210-hal/src/plic.rs#from_bits` | 同模块上下文对齐/差异观察 | device_status, fail_device, find_block_device, mmio_read32, mmio_write32, set_status ...(+2) | Some, panic! |
| `kernel/virtio_blk.c#virtio_blk_read_sector` | `src/platform/k210/driver/sdcard.c#sd_init` | 同模块上下文对齐/差异观察 | kputs, mmio_write32, platform_dma_addr, platform_fence_io, used_index | SD_CS_HIGH, SD_HIGH_SPEED_ENABLE, sd_end_cmd, sd_get_cardinfo, sd_get_response, sd_lowlevel_init ...(+3) |
| `kernel/mm.c#heap_insert_block` | `src/platform/k210/driver/sdcard.c#sd_get_cidregister` | 同模块上下文对齐/差异观察 | 内部闭环逻辑 | sd_end_cmd, sd_get_response, sd_read_data, sd_send_cmd |

#### 文件系统/第三方库
仅目标项目中观察到该模块代表函数，参考项目没有同模块可对齐证据：
- 目标项目：`kernel/ext4.c#ext4_rename_path`，调用摘要：ext4_lookup_path, find_cached_descendant_file, find_cached_dirent_entry, kstrcmp, kstrlen, parse_path_parent ...(+2)
- 目标项目：`kernel/ext4.c#ext4_get_cwd_path`，调用摘要：append_path_component
- 目标项目：`kernel/vfs.c#ext4_lookup`，调用摘要：ext4_fill_inode, ext4_iterate_dirents, ext4_lookup_child_or_descendant, kfree, vfs_alloc_generic_dentry, vfs_alloc_generic_inode ...(+4)
- 目标项目：`kernel/ext4.c#ext4_lookup_path`，调用摘要：find_cached_descendant, find_cached_dirent, find_cached_parent_dir, find_in_dir

#### 第三方库
仅参考项目中观察到该模块代表函数，目标项目没有同模块可对齐证据：
- 参考项目：`firmware/rust_dep/rustsbi-0.0.2/rustsbi/src/ecall/base.rs#get_mvendorid`，调用摘要：SbiRet::ok, mvendorid::read, mvendorid::read().map, mvendorid::read().map(|r| r.bits()).unwrap_or, r.bits

#### 通用模块/其他
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `kernel/string.c#memcmp` | `src/lib/libc/src/string/memcmp.c#memcmp` | 同板块同名 | kmemcmp | 内部闭环逻辑 |
| `kernel/print.c#kputs` | `src/lib/libc/src/stdio/stdio.c#kputs` | 同板块同名 | kputc | libc_stub_putc |
| `arch/riscv/user.c#user_space_run` | `src/lib/libc/src/string/bcopy.c#bcopy` | 同板块同结构 | user_space_run_argv | memmove |
| `kernel/string.c#memcpy` | `src/lib/libc/src/string/bcmp.c#bcmp` | 同板块同结构 | kmemcpy | memcmp |


好的，审计官。以下是基于提供的证据底座、控辩双方主张及客观 AST 数据的最终同源性定性仲裁报告。

### 1. 宏观判定
- **定性结论**: **独立开发 / 底层架构显著差异**
- **核心依据**: 客观 AST 相似度仅为 **0.93%**，双向覆盖率均低于 2%（目标项目 1.98%，参考项目 1.73%）。该数值处于操作系统内核因遵循 POSIX 规范、硬件手册及使用标准 C 库所必然产生的“背景噪声”区间内。共同 AST 指纹仅 11 个，且经分析多为跨板块假阳性匹配或标准库函数，无法构成同源性证据链。

### 2. 全模块覆盖矩阵解读
| 模块 | 双方存在 | 共同指纹 | 覆盖率 (T/B) | 证据强度 | 解读 |
| :--- | :---: | :---: | :---: | :---: | :--- |
| **进程管理** | 是 | 0 | 0.0% / 0.0% | **未形成** | 双方均实现进程/线程管理，但 AST 指纹覆盖率为 0。目标项目以 `proc` 为核心抽象，参考项目以 `thread` 为核心，架构模型完全不同，无任何结构重合。 |
| **任务调度/异步运行时** | 是 | 2 | 1.96% / 3.33% | **弱** | 共同指纹极少。目标项目围绕 `sys_*` 系统调用实现，参考项目则包含大量 K210 平台特定驱动代码。重合点 `pipe_get_fd` 与 `put_cpu_id` 等属于跨领域假阳性。 |
| **内存管理** | 是 | 2 | 1.41% / 1.56% | **弱** | 共同指纹为 `align_up` 和通用内存分配原语，这是任何 OS 内核的必备基础工具，属于标准算法实现，不构成抄袭证据。 |
| **中断/异常处理** | 是 | 1 | 1.25% / 1.54% | **弱** | 共同指纹为 `loongarch_trap_placeholder` 与 `gpiohs_irq_disable` 的配对，两者均为“内部闭环逻辑”，是 AST 匹配器对空函数或简单函数的假阳性匹配。 |
| **系统调用** | 是 | 0 | 0.0% / 0.0% | **未形成** | 双方均存在系统调用层，但无任何共同指纹。目标项目实现了丰富的 Linux 兼容系统调用，参考项目仅有少量基础调用，实现路径完全不同。 |
| **文件系统** | 是 | 3 | 3.9% / 4.62% | **弱** | 共同指纹均为跨领域假阳性，如目标项目的 `sys_mount` (VFS) 匹配到参考项目的 `get_gpio_bit` (GPIO驱动)，`root_create` (VFS) 匹配到 `entry_get_sub_read` (FAT32)。 |
| **设备驱动** | 是 | 1 | 7.69% / 1.52% | **弱** | 共同指纹 `fail_device` 与 `sd_lowlevel_init` 均为设备初始化失败/状态设置路径，属于驱动开发的通用模式，且调用图谱完全不同。 |
| **网络协议栈** | 否 | 0 | 0.0% / 0.0% | **未形成** | 双方均未实现网络协议栈。 |
| **第三方库/公共依赖** | 部分 | 0 | 0.0% / 0.0% | **未形成** | 目标项目包含 ext4 等第三方库，参考项目包含 rustsbi 等。双方独有的第三方库无任何交叉，证明项目依赖完全独立。 |

### 3. 底层 AST 关键证据指纹表
> 所有共同指纹均为**弱信号**或**假阳性**，无强同源证据。

| 所属板块 | 目标项目路径 | 参考项目路径 | 证据类型 | 技术说明 |
| :--- | :--- | :--- | :--- | :--- |
| **通用模块/其他** | `kernel/string.c#memcmp` | `src/lib/libc/src/string/memcmp.c#memcmp` | 标准库函数 | 同名标准 C 库函数实现，任何 C 项目都可能存在，不构成同源证据。 |
| **通用模块/其他** | `kernel/print.c#kputs` | `src/lib/libc/src/stdio/stdio.c#kputs` | 标准库函数 | 内核打印函数，命名和实现逻辑高度受限于硬件串口输出规范，属于通用实现。 |
| **内存管理** | `kernel/mm.c#align_up` | `firmware/rust_dep/.../lib.rs#align_up` | 通用算法 | 内存地址对齐是任何 OS 和底层库的标准宏/内联函数，算法逻辑唯一且简单。 |
| **内存管理** | `kernel/vfs.c#vfs_alloc_inode` | `src/lib/libc/src/libc_stub.c#libc_stub_malloc` | 跨领域假阳性 | VFS inode 分配 vs 通用堆分配，仅因都调用底层分配函数而被匹配，语义完全不同。 |
| **文件系统** | `kernel/vfs.c#sys_mount` | `src/platform/k210/driver/gpiohs.c#get_gpio_bit` | 跨领域假阳性 | 文件系统挂载 vs GPIO 位操作，AST 浅层结构相似，属于匹配器误判。 |
| **文件系统** | `kernel/vfs.c#root_create` | `src/kernel/fs/fat32.c#entry_get_sub_read` | 跨领域假阳性 | VFS 根节点创建 vs FAT32 目录项读取，功能完全不相关。 |
| **文件系统** | `kernel/vfs.c#root_mkdir` | `src/kernel/fs/fat32.c#parse_path_write` | 跨领域假阳性 | VFS 目录创建 vs FAT32 路径解析，功能完全不相关。 |
| **设备驱动** | `kernel/virtio_blk.c#fail_device` | `src/platform/k210/driver/sdcard.c#sd_lowlevel_init` | 跨平台假阳性 | VirtIO 块设备错误处理 vs K210 SD 卡初始化，硬件和协议完全不同。 |
| **任务调度/异步运行时** | `arch/riscv/trap.c#pipe_get_fd` | `src/kernel/task/cpu.c#put_cpu_id` | 跨领域假阳性 | 管道 fd 获取 vs CPU 引用计数释放，语义和调用链完全不同。 |
| **任务调度/异步运行时** | `kernel/pipe.c#yield_to_kernel` | `src/kernel/task/scheduler.c#scheduler_cpu_init` | 跨领域假阳性 | 管道让出 CPU vs 调度器 CPU 初始化，功能完全不相关。 |
| **中断/异常处理** | `arch/loongarch/trap.c#loongarch_trap_placeholder` | `src/platform/k210/driver/gpiohs.c#gpiohs_irq_disable` | 跨架构假阳性 | LoongArch 陷阱占位符 vs K210 GPIO 中断禁用，均为简单函数，无实质逻辑。 |

### 4. 核心代码调用流对比剖析
#### **【进程管理/系统调用路径】**
*   **底层技术析理**: 目标项目实现了完整的 Linux 兼容 `sys_wait4` 系统调用，其调用流为 `proc_find_waitable_zombie` → `proc_reap_child` → `wait_to_kernel` → `write_user32`，核心是**进程**对象的僵尸状态查找与资源回收，并向用户空间写回状态。参考项目的 `threads_wait` 调用流为 `GET_RUNNING_THREAD` → `intrLock_lock` → `link_remove_by_node` → `sleep_on_queue`，核心是**线程**对象在链表中的移除与休眠。两者在抽象层次（进程 vs 线程）、同步策略（内核态数据拷贝 vs 锁+休眠）和对象管理模型上存在根本性差异。
*   **代码溯源链接**:
    - 目标项目: `arch/riscv/trap.c#sys_wait4`
    - 参考项目: `src/kernel/task/threads.c#threads_wait`

#### **【任务调度/异步运行时】**
*   **底层技术析理**: 目标项目的 `sys_futex` 实现了 Linux 的快速用户空间互斥锁，调用流涉及 `futex_requeue_waiters`、`futex_wake_waiters` 和 `read_user32`，直接操作用户态内存地址和复杂的 futex 哈希桶队列。参考项目的 `sem_release` 是经典的内核信号量释放操作，调用流为 `spinlock_lock` → `wakeup_on_queue_no_lock` → `spinlock_unlock`，操作的是内核自旋锁保护的等待队列。两者的同步原语（futex vs semaphore）、内存模型（操作用户态地址 vs 内核对象）和并发控制策略完全不同。
*   **代码溯源链接**:
    - 目标项目: `arch/riscv/trap.c#sys_futex`
    - 参考项目: `src/kernel/sync/sem.c#sem_release`

#### **【文件系统】**
*   **底层技术析理**: 目标项目的 `sys_write` 调用流展示了其复杂的 VFS 层，路径为 `fd_can_write` → `fd_file_desc_checked` → `file->f_op->write`，并包含对 `devfs` (设备文件系统) 和 `mem_file` (内存文件) 的特殊处理分支。参考项目的 `open_fp` 调用流仅为 `parse_path_read` / `parse_path_write`，直接进行路径解析。目标项目构建了完整的 VFS 抽象层和多后端支持，而参考项目的文件系统实现更为直接和简化，架构差异显著。
*   **代码溯源链接**:
    - 目标项目: `arch/riscv/trap.c#sys_write`
    - 参考项目: `src/kernel/fs/file.c#open_fp`

### 5. 单方模块与降权项
*   **目标项目独有模块**: `文件系统/第三方库` (76个函数，如 ext4 文件系统实现)。这表明目标项目集成了 ext4 支持，是其独立架构的一部分。
*   **参考项目独有模块**: `第三方库` (1个函数，为 RustSBI 实现)。这表明参考项目基于 Rust 的 SBI 固件环境，是其独立技术栈的体现。
*   **降权项说明**:
    *   **标准库/公共接口函数**: `memcmp`、`memcpy`、`kputs`、`align_up` 等函数，其命名和实现由语言标准或硬件手册强约束，在任何独立实现中都会高度相似，**不能作为同源证据**。
    *   **跨板块/跨语言假阳性**: 证据底座中大量匹配项（如 `sys_mount` 与 `get_gpio_bit`，C 函数与 Rust 函数）属于 AST 指纹匹配器的固有噪声。这些匹配在语义、功能和领域上均不相关，**必须降权为无效证据**。
    *   **项目内部文件重复**: 目标项目的 `trap.c` 和 `trapbk.c` 存在大量同名函数，这是项目内部的代码备份或变体，与参考项目无关，**不构成多来源交叉印证**。

### 6. 保守结论
本次审计的客观 AST 相似度仅为 0.93%，远低于通常判定同源性所需的阈值。11 个共同指纹经逐条技术剖析，均为标准库函数、通用算法或跨领域的假阳性匹配，未发现任何一条位于同板块、同语义、同抽象层次的强同源证据。控方基于函数语义的“变体复用”指控，在客观 AST 证据表中均未得到支持，属于主观推断。双方在进程/线程模型、内存管理、文件系统架构和同步机制等核心子系统上展现出根本性的设计差异。综合判定，目标项目 `[2026-T2026100109910165-XX]` 相对于参考项目 `[2021-广告位招租-重庆大学]` 为**独立开发**，不存在代码同源性风险。

------------------------------

