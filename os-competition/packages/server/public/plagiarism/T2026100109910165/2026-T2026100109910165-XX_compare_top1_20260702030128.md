# 🛡️ OS 内核赛自动化代码级架构审计报告

**审计时间**: `2026-07-02 03:01:28`
**参赛作品名称**: `2026-T2026100109910165-XX`
**年份**: `2026`
**学校**: `XX`
**队伍名称**: `T2026100109910165`
**比赛名字**: `OS-Competition`
**比赛子赛道**: `Kernel-Implementation`
**仓库地址**: `https://gitlab.eduxiji.net/educg-group-43501-3132633/T2026100109910165-2460`
**推测开发语言**: `Rust / C (由AST探针自动识别)`
**特征函数量**: `883`

## ⚔️ 底层代码同源性对比 (AST Homology)
> 本部分由专门的代码审查智能体，对该项目与符合年份条件的历届最高相似度项目进行控制流与底层调用对比。新版报告会先给出确定性的全模块覆盖矩阵，再给出 Agent 的分模块解释，避免只围绕少量抽样函数展开。

### 🏆 Top 1 底层对标: vs `2022-寿春-南开大学`（南开大学 / 寿春）

#### 对方作品信息

**对方年份**: `2022`
**对方学校**: `南开大学`
**对方队伍名称**: `寿春`
**对方比赛名字**: `OS-Competition`
**对方比赛子赛道**: `Kernel-Implementation`
**对方仓库地址**: `https://gitlab.eduxiji.net/hlw2014/ucore-smp`

> 双向覆盖率：共同 AST 指纹: 11；唯一 AST 指纹: 目标项目 556，参考项目 528；覆盖率: 目标项目 1.98%，参考项目 2.08%

## 全模块对比总览（确定性证据）

> 总体覆盖率：共同 AST 指纹: 11；唯一 AST 指纹: 目标项目 556，参考项目 528；覆盖率: 目标项目 1.98%，参考项目 2.08%

### 1. 模块覆盖矩阵
| 模块 | 目标函数数 | 参考函数数 | 共同唯一 AST 指纹 | Target 覆盖 | Base 覆盖 | 状态 |
| :--- | ---: | ---: | ---: | ---: | ---: | :--- |
| 进程管理 | 89 | 47 | 2 | 3.33% | 4.76% | 双方存在且有结构重合 |
| 任务调度/异步运行时 | 181 | 48 | 1 | 0.98% | 2.13% | 双方存在且有结构重合 |
| 内存管理 | 195 | 178 | 2 | 1.41% | 1.15% | 双方存在且有结构重合 |
| 中断/异常处理 | 168 | 16 | 1 | 1.25% | 6.25% | 双方存在且有结构重合 |
| 系统调用 | 2 | 45 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |
| 文件系统 | 91 | 153 | 2 | 2.6% | 1.44% | 双方存在且有结构重合 |
| 设备驱动 | 15 | 44 | 1 | 7.69% | 3.85% | 双方存在且有结构重合 |
| 网络协议栈 | 0 | 0 | 0 | 0.0% | 0.0% | 双方均未体现 |
| 文件系统/第三方库 | 76 | 0 | 0 | 0.0% | 0.0% | 仅目标项目体现 |
| 通用模块/其他 | 66 | 114 | 5 | 8.93% | 5.56% | 双方存在且有结构重合 |

### 2. 分模块证据清单

#### 进程管理
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `kernel/proc.c#scheduler` | `os/proc/scheduler.c#scheduler` | 同板块同名 | 内部闭环逻辑 | MS_TO_CYCLE, acquire, debugcore, get_tick, mycpu, pop_off ...(+7) |
| `arch/riscv/trap.c#riscv_current_process` | `os/device/proc_device.c#proc_write` | 同板块同结构 | 内部闭环逻辑 | 内部闭环逻辑 |
| `kernel/vfs.c#procfs_make_child_node` | `os/proc/loader.c#check_script_header` | 同模块上下文对齐/差异观察 | kstrcmp, procfs_current_proc, procfs_make_node, vfs_is_digit_string, vfs_parse_ino | ilock, infof, inode_by_name, iunlockput, readi, strcpy |
| `arch/riscv/trap.c#sys_wait4` | `os/device/proc_device.c#proc_read` | 同模块上下文对齐/差异观察 | proc_find_waitable_zombie, proc_reap_child, wait_to_kernel, write_user32 | acquire, either_copyout, printf, release, strncpy |

#### 任务调度/异步运行时
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `arch/riscv/trap.c#sys_write` | `os/syscall/syscall_impl.c#sys_write` | 同板块同名 | devfs_node_is_memory_file, devfs_write_memory_file, fd_can_write, fd_file_desc_checked, file->f_dentry->d_inode->i_fop->write, file->f_op->write ...(+6) | curr_proc, filewrite |
| `arch/riscv/trap.c#sys_read` | `os/syscall/syscall_impl.c#sys_read` | 同板块同名 | copy_mem_file_sparse_to_user, copy_to_user, cstr_len, devfs_node_is_memory_file, devfs_read_memory_file, ext4_read_inode_range ...(+10) | curr_proc, fileread |
| `arch/riscv/trap.c#sys_lseek` | `os/syscall/syscall_impl.c#sys_lseek` | 同板块同名 | devfs_node_size, fd_file_desc, mem_file_for_fd | curr_proc, filelseek, get_proc_file_by_fd, infof |
| `arch/riscv/trap.c#riscv_user_alive` | `user/lib/syscall.c#sched_yield` | 同板块同结构 | proc_is_alive | syscall |

#### 内存管理
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `arch/riscv/trap.c#sys_execve` | `os/syscall/syscall_impl.c#sys_execve` | 同板块同名 | build_exec_stack, clear_all_mmap_regions, close_fd, collect_exec_args, collect_exec_envs, copy_user_cstr ...(+14) | arg_copy, copyinstr, curr_proc, exec, infof, tracecore |
| `arch/riscv/trap.c#sys_openat` | `os/syscall/syscall_impl.c#sys_openat` | 同板块同名 | IS_ERR, PTR_ERR, alloc_vfs_fd, copy_user_cstr, find_mem_file, finish_open_fd ...(+9) | copyinstr, curr_proc, debugcore, fileopenat |
| `kernel/vfs.c#vfs_alloc_inode` | `os/fatfs/ffsystem.c#ff_memalloc` | 同板块同结构 | vfs_alloc_generic_inode | malloc |
| `kernel/mm.c#mm_total_mem` | `os/syscall/syscall_impl.c#sys_unlink` | 同板块同结构 | 内部闭环逻辑 | 内部闭环逻辑 |

#### 中断/异常处理
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `arch/loongarch/trap.c#loongarch_trap_placeholder` | `os/trap/trap.c#trapinit` | 同板块同结构 | 内部闭环逻辑 | 内部闭环逻辑 |
| `arch/riscv/trap.c#build_exec_stack` | `os/trap/trap.c#kerneltrap` | 同模块上下文对齐/差异观察 | align_down, copy_to_user_in_space, stack_guard_from_random, write_aux_by_index, write_user64_in_space | KERNEL_ASSERT, intr_get, kernel_exception_handler, kernel_interrupt_handler, pushtrace, r_scause ...(+5) |
| `arch/riscv/trapbk.c#build_exec_stack` | `os/main.c#hart_bootcamp` | 同模块上下文对齐/差异观察 | align_down, copy_to_user_in_space, stack_guard_from_random, write_aux_by_index, write_user64_in_space | kvminithart, plicinithart, printf, trapinit_hart, w_tp |
| `arch/riscv/trap.c#collect_exec_envs` | `os/trap/plic.c#plicinithart` | 同模块上下文对齐/差异观察 | clear_exec_arg_block, copy_user_cstr, cstr_len, exec_env_has_path, read_user64, set_exec_arg_slot | PLIC_SENABLE, PLIC_SPRIORITY, cpuid |

#### 系统调用
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `arch/riscv/platform.c#platform_shutdown` | `os/sbi/sbi.c#a_sbi_ecall` | 同模块上下文对齐/差异观察 | 内部闭环逻辑 | 内部闭环逻辑 |
| `arch/riscv/timer.c#sbi_set_timer` | `os/sbi/sbi.c#sbi_call` | 同模块上下文对齐/差异观察 | 内部闭环逻辑 | 内部闭环逻辑 |

#### 文件系统
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `kernel/vfs.c#sys_mount` | `os/fs/inode.c#inode_parent_by_name` | 同板块同结构 | vfs_do_mount | inode_or_parent_by_name |
| `kernel/vfs.c#vfs_do_create_file` | `user/lib/syscall.c#fstat` | 同板块同结构 | vfs_do_create_file_from_dentry | syscall |
| `kernel/vfs.c#vfs_open` | `user/lib/syscall.c#pipe` | 同板块同结构 | vfs_open_from_dentry | syscall |
| `arch/riscv/user.c#user_get_cwd_inode` | `os/fatfs/ffsystem.c#ff_del_syncobj` | 同板块同结构 | 内部闭环逻辑 | 内部闭环逻辑 |

#### 设备驱动
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `kernel/virtio_blk.c#device_status` | `os/driver/sd.c#sd_txrx` | 同板块同结构 | mmio_read32 | spi_txrx |
| `kernel/virtio_blk.c#virtio_blk_init` | `os/driver/sd.c#sd_read_blocks` | 同模块上下文对齐/差异观察 | device_status, fail_device, find_block_device, mmio_read32, mmio_write32, set_status ...(+2) | SD_CMD, crc16, crc7, sd_cmd, sd_cmd_end, sd_dummy |
| `kernel/virtio_blk.c#virtio_blk_read_sector` | `os/device/cpu_device.c#cpu_read` | 同模块上下文对齐/差异观察 | kputs, mmio_write32, platform_dma_addr, platform_fence_io, used_index | either_copyout, r_time |
| `kernel/mm.c#heap_insert_block` | `os/driver/sd.c#sd_poweron` | 同模块上下文对齐/差异观察 | 内部闭环逻辑 | get_time_us, sd_dummy, spi_min_clk_divisor |

#### 文件系统/第三方库
仅目标项目中观察到该模块代表函数，参考项目没有同模块可对齐证据：
- 目标项目：`kernel/ext4.c#ext4_rename_path`，调用摘要：ext4_lookup_path, find_cached_descendant_file, find_cached_dirent_entry, kstrcmp, kstrlen, parse_path_parent ...(+2)
- 目标项目：`kernel/ext4.c#ext4_get_cwd_path`，调用摘要：append_path_component
- 目标项目：`kernel/vfs.c#ext4_lookup`，调用摘要：ext4_fill_inode, ext4_iterate_dirents, ext4_lookup_child_or_descendant, kfree, vfs_alloc_generic_dentry, vfs_alloc_generic_inode ...(+4)
- 目标项目：`kernel/ext4.c#ext4_lookup_path`，调用摘要：find_cached_descendant, find_cached_dirent, find_cached_parent_dir, find_in_dir

#### 通用模块/其他
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `kernel/string.c#memcpy` | `os/mem/string.c#memcpy` | 同板块同名同结构 | kmemcpy | memmove |
| `kernel/string.c#memcmp` | `os/mem/string.c#memcmp` | 同板块同名 | kmemcmp | 内部闭环逻辑 |
| `kernel/string.c#memset` | `os/mem/string.c#memset` | 同板块同名 | kmemset | 内部闭环逻辑 |
| `arch/riscv/user.c#user_space_run` | `os/sbi/sbi.c#sbi_console_putchar` | 同板块同结构 | user_space_run_argv | sbi_call |


好的，审计员。基于提供的证据底座、控辩双方陈述及审计指令，现出具最终同源性定性仲裁报告。

---

### 1. 宏观判定
- **定性结论**: **独立开发**
- **核心依据**: 客观 AST 相似度仅为 1.03%，双向覆盖率均低于 2.1%。双方在进程管理、内存管理等核心板块的底层调用图谱、同步原语及对象管理模型上存在根本性架构差异。控方指控主要基于跨板块、跨抽象层的语义硬凑，未能提供同板块内 AST 结构同构的实质性证据。

### 2. 全模块覆盖矩阵解读
- **进程管理**:
  - 双方均存在。存在共同 AST 指纹。目标覆盖率 3.33%，参考覆盖率 4.76%。
  - 证据强度: **弱**。同名函数（如 `scheduler`）为领域通用术语，其调用图谱差异巨大，不构成同源证据。
- **任务调度/异步运行时**:
  - 双方均存在。存在共同 AST 指纹。目标覆盖率 0.98%，参考覆盖率 2.13%。
  - 证据强度: **弱**。共同指纹源于 `sys_write` 等标准系统调用入口，其内部实现路径完全不同。
- **内存管理**:
  - 双方均存在。存在共同 AST 指纹。目标覆盖率 1.41%，参考覆盖率 1.15%。
  - 证据强度: **弱**。共同指纹 `sys_execve` 等为 POSIX 标准接口，其内部内存操作（如目标项目的 `build_exec_stack` 与参考项目的 `exec`）调用链无重合。
- **中断/异常处理**:
  - 双方均存在。存在共同 AST 指纹。目标覆盖率 1.25%，参考覆盖率 6.25%。
  - 证据强度: **弱**。配对证据（如 `build_exec_stack` 与 `kerneltrap`）功能完全不同，属于跨板块硬凑。
- **系统调用**:
  - 双方均存在。未观察到共同结构指纹。
  - 证据强度: **未形成**。目标项目仅 2 个函数，参考项目 45 个，规模与实现路径不匹配。
- **文件系统**:
  - 双方均存在。存在共同 AST 指纹。目标覆盖率 2.6%，参考覆盖率 1.44%。
  - 证据强度: **弱**。证据多为 `vfs_alloc_inode` 与 `ff_memalloc` 这类泛型内存分配操作，或 `syscall` 包装函数，不涉及文件系统核心逻辑同源。
- **设备驱动**:
  - 双方均存在。存在共同 AST 指纹。目标覆盖率 7.69%，参考覆盖率 3.85%。
  - 证据强度: **弱**。证据 `device_status` 与 `sd_txrx` 均为简单的 MMIO 读取或 SPI 收发，是硬件交互的通用模式。
- **网络协议栈**:
  - 双方均未体现。
  - 证据强度: **未形成**。
- **第三方库/公共依赖**:
  - 仅目标项目体现（`文件系统/第三方库` 模块，76 个函数）。
  - 证据强度: **未形成**。参考项目无此模块，无法对比。

### 3. 底层 AST 关键证据指纹表
| 所属板块 | 目标项目路径 | 参考项目路径 | 证据类型 | 技术说明 |
| :--- | :--- | :--- | :--- | :--- |
| 通用模块/其他 | `kernel/string.c#memcpy` | `os/mem/string.c#memcpy` | 同板块同名同结构 | 标准 C 库函数，双方均实现为调用内部 `kmemcpy` 或 `memmove`，属于公共依赖，非原创逻辑。 |
| 通用模块/其他 | `kernel/string.c#memset` | `os/mem/string.c#memset` | 同板块同名 | 同上，标准 C 库函数实现。 |
| 通用模块/其他 | `kernel/string.c#memcmp` | `os/mem/string.c#memcmp` | 同板块同名 | 同上，标准 C 库函数实现。 |
| 进程管理 | `kernel/proc.c#scheduler` | `os/proc/scheduler.c#scheduler` | 同板块同名 | 仅函数名相同。目标为“内部闭环逻辑”，参考调用 `mycpu, push_off, pop_off` 等复杂调度原语，实现完全不同。 |
| 进程管理 | `arch/riscv/trap.c#riscv_current_process` | `os/device/proc_device.c#proc_write` | 跨板块同结构 | **无效证据**。功能完全不相关（获取当前进程 vs. proc 设备写操作），属于控方硬凑。 |
| 文件系统 | `kernel/vfs.c#sys_mount` | `os/fs/inode.c#inode_parent_by_name` | 跨板块同结构 | **无效证据**。功能完全不相关（文件系统挂载 vs. 路径解析），属于控方硬凑。 |
| 文件系统 | `kernel/vfs.c#vfs_alloc_inode` | `os/fatfs/ffsystem.c#ff_memalloc` | 跨板块同结构 | **无效证据**。通用内存分配操作，且跨模块（VFS vs. FAT32/第三方库）。 |
| 设备驱动 | `kernel/virtio_blk.c#device_status` | `os/driver/sd.c#sd_txrx` | 跨板块同结构 | **无效证据**。功能完全不相关（读取设备状态寄存器 vs. SPI 收发），属于控方硬凑。 |

### 4. 核心代码调用流对比剖析
* **【进程管理/进程退出与清理路径】**
* **底层技术析理**: 目标项目采用引用计数（`proc_file_put`）管理文件对象，退出流程聚合了 futex 唤醒（`futex_wake_waiters`）、子进程信号队列（`proc_queue_child_signal`）等 Linux 兼容语义。参考项目则基于 xv6 模型，使用简单的 `fileclose` 直接关闭文件，退出时调用 `freeproc` 回收资源并通过 `reparent` 移交子进程。两者在**对象生命周期管理模型**（引用计数 vs. 直接回收）和**同步原语**（futex vs. 锁/睡眠唤醒）上存在根本性架构差异。
* **代码溯源链接**:
  - 目标项目: `kernel/proc.c#proc_exit`
  - 参考项目: `os/proc/exit.c#reparent`

* **【进程管理/Procfs 状态读取路径】**
* **底层技术析理**: 目标项目实现了完整的 Linux 风格 `/proc` 文件系统，通过 `procfs_find_proc` 查找进程控制块，并使用 `ksnprintf` 格式化输出到 `status`、`stat` 等不同文件。参考项目则通过单一的 `proc_read` 设备函数，使用 `printf` 或 `either_copyout` 直接输出信息。两者在**虚拟文件系统抽象层次**（多文件节点 vs. 单一设备文件）和**数据组织模型**上完全不同。
* **代码溯源链接**:
  - 目标项目: `kernel/vfs.c#read_pid_status`
  - 参考项目: `os/device/proc_device.c#proc_read`

* **【内存管理/程序加载路径】**
* **底层技术析理**: 目标项目的 `sys_execve` 调用链包含 `build_exec_stack`（构建栈）、`clear_all_mmap_regions`（清除内存映射区域）等，显示其采用基于 `mmap` 区域的内存管理模型。参考项目的 `sys_execve` 则调用 `exec`，其内部通过 `readi` 从 inode 加载程序段，是典型的 xv6 直接加载模型。两者在**进程地址空间管理模型**上存在本质区别。
* **代码溯源链接**:
  - 目标项目: `arch/riscv/trap.c#sys_execve`
  - 参考项目: `os/syscall/syscall_impl.c#sys_execve`

### 5. 单方模块与降权项
- **目标项目独有模块**: `文件系统/第三方库` (76 个函数)，包含 `ext4` 等复杂文件系统实现。这恰恰证明了目标项目的独立设计与功能扩展能力，而非抄袭。
- **弱信号/无效证据**: 控方提出的 `sys_rt_sigtimedwait`、`futex` 机制等，因参考项目缺失对应实现，无法形成比对闭环，已由控方自行降级为弱信号。
- **跨板块硬凑**: 证据底座中大量配对属于跨模块、跨功能硬凑（如将 `procfs_make_child_node` 与 `check_script_header` 配对），此类证据不构成同板块主证据，予以剔除。
- **公共依赖**: `memcpy`、`memset` 等标准 C 库函数实现，是任何系统软件的基础公共依赖，不能作为代码同源的证据。

### 6. 保守结论
本次审计的客观 AST 相似度（1.03%）与双向覆盖率（均低于 2.1%）极低，处于独立开发的典型区间。控方《复用指控清单》中的“铁证”经不起底层调用图谱的比对，其核心指控均建立在跨板块、跨抽象层的语义偷换和函数名巧合之上。目标项目与参考项目在进程管理、内存管理等核心板块的架构设计（如对象生命周期模型、虚拟文件系统抽象、进程地址空间管理）上存在根本性差异。目标项目独有的 ext4 文件系统、futex 及信号处理等复杂功能，进一步佐证了其独立设计与实现能力。综合判定，双方代码不构成实质性相似，无抄袭或违规风险。

------------------------------

