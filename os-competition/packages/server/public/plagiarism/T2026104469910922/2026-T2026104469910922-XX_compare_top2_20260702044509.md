# 🛡️ OS 内核赛自动化代码级架构审计报告

**审计时间**: `2026-07-02 04:45:09`
**参赛作品名称**: `2026-T2026104469910922-XX`
**年份**: `2026`
**学校**: `XX`
**队伍名称**: `T2026104469910922`
**比赛名字**: `OS-Competition`
**比赛子赛道**: `Kernel-Implementation`
**仓库地址**: `https://gitlab.eduxiji.net/educg-group-43501-3132633/T2026104469910922-2031`
**推测开发语言**: `Rust / C (由AST探针自动识别)`
**特征函数量**: `354`

## ⚔️ 底层代码同源性对比 (AST Homology)
> 本部分由专门的代码审查智能体，对该项目与符合年份条件的历届最高相似度项目进行控制流与底层调用对比。新版报告会先给出确定性的全模块覆盖矩阵，再给出 Agent 的分模块解释，避免只围绕少量抽样函数展开。

### 🏆 Top 2 底层对标: vs `2024-RuaruaOs-天津大学`（天津大学 / RuaruaOs）

#### 对方作品信息

**对方年份**: `2024`
**对方学校**: `天津大学`
**对方队伍名称**: `RuaruaOs`
**对方比赛名字**: `OS-Competition`
**对方比赛子赛道**: `Kernel-Implementation`
**对方仓库地址**: `https://gitlab.eduxiji.net/educg-group-26010-2376550/T202410056992657-49`

> 双向覆盖率：共同 AST 指纹: 20；唯一 AST 指纹: 目标项目 311，参考项目 309；覆盖率: 目标项目 6.43%，参考项目 6.47%

## 全模块对比总览（确定性证据）

> 总体覆盖率：共同 AST 指纹: 20；唯一 AST 指纹: 目标项目 311，参考项目 309；覆盖率: 目标项目 6.43%，参考项目 6.47%

### 1. 模块覆盖矩阵
| 模块 | 目标函数数 | 参考函数数 | 共同唯一 AST 指纹 | Target 覆盖 | Base 覆盖 | 状态 |
| :--- | ---: | ---: | ---: | ---: | ---: | :--- |
| 进程管理 | 24 | 53 | 4 | 20.0% | 9.09% | 双方存在且有结构重合 |
| 任务调度/异步运行时 | 26 | 29 | 1 | 4.0% | 3.85% | 双方存在且有结构重合 |
| 内存管理 | 92 | 125 | 3 | 3.26% | 2.52% | 双方存在且有结构重合 |
| 中断/异常处理 | 8 | 20 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |
| 系统调用 | 35 | 4 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |
| 文件系统 | 82 | 68 | 3 | 3.9% | 4.92% | 双方存在且有结构重合 |
| 设备驱动 | 18 | 16 | 1 | 5.56% | 7.69% | 双方存在且有结构重合 |
| 网络协议栈 | 0 | 0 | 0 | 0.0% | 0.0% | 双方均未体现 |
| 文件系统/第三方库 | 26 | 0 | 0 | 0.0% | 0.0% | 仅目标项目体现 |
| 通用模块/其他 | 43 | 40 | 5 | 11.63% | 13.51% | 双方存在且有结构重合 |

### 2. 分模块证据清单

#### 进程管理
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `kernel/core/sysproc.c#sys_wait4` | `sysfproc.c#sys_wait4` | 同板块同名 | LOG_TRACE, LOG_WARN, argaddr, argint, wait4 | argaddr, argint, waitpid |
| `kernel/core/proc.c#sched` | `proc.c#sched` | 同板块同名 | get_cpu, get_proc, holding, intr_get, panic, swtch | intr_get, mycpu, myproc, panic, swtch |
| `kernel/core/proc.c#yield` | `proc.c#yield` | 同板块同名 | acquire, get_proc, release, sched | acquire, myproc, release, sched |
| `kernel/core/sysproc.c#sys_exit` | `sysfproc.c#sys_exit` | 同板块同名 | LOG_TRACE, argint, exit | argint, exit, myproc |

#### 任务调度/异步运行时
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `kernel/core/sysfile.c#sys_chdir` | `sysfile.c#sys_chdir` | 同板块同名 | argstr, get_proc, strcmp, strncpy, vfs_lookup_at, vfs_make_absolute_path | argstr, ilock, iput, iunlock, iunlockput, myproc ...(+1) |
| `kernel/core/syscall.c#syscall` | `syscall.c#syscall` | 同板块同名 | LOG_ERROR, LOG_TRACE, NELEM, get_proc, syscalls[num] | NELEM, myproc, syscalls[num] |
| `kernel/core/sysfile.c#sys_close` | `sysfile.c#sys_close` | 同板块同名 | LOG_ERROR, acquire, argint, fileclose, get_proc, release | argfd, fileclose, myproc |
| `kernel/core/syscall.c#argraw` | `sysfile.c#argraw` | 同板块同名 | get_proc, panic | myproc, panic |

#### 内存管理
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `kernel/driver/virtio_blk.c#virtio_disk_init` | `virtio_disk.c#virtio_disk_init` | 同板块同名 | (uint32), LOG_INFO, LOG_TRACE, VA2PA, VIRTIO_READ32, VIRTIO_WRITE32 ...(+6) | R, initlock, memset, panic |
| `arch/riscv/trap/trap.c#usertrap` | `trap.c#usertrap` | 同板块同名 | LOG_ERROR, LOG_TRACE, LOG_WARN, PGROUNDUP, cpuid, get_proc ...(+18) | PA2PTE, PGROUNDDOWN, PGROUNDUP, exit, intr_on, kalloc ...(+20) |
| `kernel/driver/virtio_blk.c#virtio_disk_rw` | `virtio_disk.c#virtio_disk_rw` | 同板块同名 | VA2PA, VIRTIO_WRITE32, __sync_synchronize, acquire, alloc3_desc, free_chain ...(+3) | R, __sync_synchronize, acquire, alloc3_desc, free_chain, release ...(+1) |
| `arch/riscv/mm/vm.c#kvmmake` | `vm.c#kvmmake` | 同板块同名 | LOG_TRACE, PA2VA, ekalloc, kvmmap, memset | kalloc, kvmmap, memset, proc_mapstacks |

#### 中断/异常处理
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `kernel/core/spinlock.c#pop_off` | `spinlock.c#pop_off` | 同板块同名 | get_cpu, intr_get, intr_on, panic | intr_get, intr_on, mycpu, panic |
| `kernel/core/spinlock.c#push_off` | `spinlock.c#push_off` | 同板块同名 | get_cpu, intr_get, intr_off | intr_get, intr_off, mycpu |
| `arch/riscv/trap/trap.c#trapinit` | `trap.c#trapinit` | 同板块同名 | LOG_TRACE, w_stvec | initlock |
| `kernel/core/printf.c#_panic` | `exec.c#trapframedump` | 同模块上下文对齐/差异观察 | intr_off, kprintint, kputs, va_end, va_start, vkprintf | printf |

#### 系统调用
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `arch/riscv/driver/sbi.c#sbi_ecall` | `sys_pselect.c#clear_bit` | 同模块上下文对齐/差异观察 | 内部闭环逻辑 | BIT_WORD |
| `user/ulib.c#syscall6` | `sys_pselect.c#convert_from_timespec2` | 同模块上下文对齐/差异观察 | 内部闭环逻辑 | 内部闭环逻辑 |
| `user/ulib.c#syscall4` | `sys_pselect.c#test_bit` | 同模块上下文对齐/差异观察 | 内部闭环逻辑 | BIT_WORD |
| `arch/riscv/driver/sbi.c#sbi_shutdown` | `sys_pselect.c#sys_ppoll` | 同模块上下文对齐/差异观察 | PA2VA, sbi_ecall | 内部闭环逻辑 |

#### 文件系统
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `kernel/fs/easyfs/fs.c#dirlink` | `fs.c#dirlink` | 同板块同名 | LOG_WARN, easyfs_read_inode, easyfs_vfs_lookup, easyfs_write_inode, memset, panic ...(+2) | dirlookup, iput, panic, readi, strncpy, writei |
| `kernel/core/file.c#fileclose` | `file.c#fileclose` | 同板块同名 | acquire, panic, pipe_close, release, vfs_iput | acquire, iput, panic, pipeclose, release |
| `kernel/fs/easyfs/inode.c#iupdate` | `fs.c#iupdate` | 同板块同名 | bread, brelse, bwrite, memmove | IBLOCK, bread, brelse, memmove |
| `kernel/fs/fs.c#skipelem` | `fs.c#skipelem` | 同板块同名 | memmove | memmove |

#### 设备驱动
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `arch/riscv/driver/uart.c#uart_init` | `uart.c#uart_init` | 同板块同名 | WriteReg | initlock, uart_read_reg, uart_write_reg |
| `kernel/driver/virtio_blk.c#free_chain` | `virtio_disk.c#free_chain` | 同板块同名 | LOG_ERROR, insert_free_desc_list, wakeup | free_desc |
| `devlog/2025_12/xv6_uart.c#uartputc_sync` | `uart.c#uartputc_sync` | 同板块同名 | ReadReg, WriteReg, pop_off, push_off | pop_off, push_off, uart_read_reg, uart_write_reg |
| `devlog/2025_12/xv6_uart.c#uartgetc` | `uart.c#uart_getc` | 同板块同结构 | ReadReg | uart_read_reg |

#### 文件系统/第三方库
仅目标项目中观察到该模块代表函数，参考项目没有同模块可对齐证据：
- 目标项目：`kernel/fs/ext4fs/mount.c#ext4_mount`，调用摘要：LOG_TRACE, bread, brelse, ext4_check_features, ext4_read_le16, ext4_read_le32
- 目标项目：`kernel/fs/ext4fs/probe.c#ext4_probe_dir_block`，调用摘要：LOG_ERROR, LOG_INFO, bread, brelse, ext4_read_le16, ext4_read_le32 ...(+1)
- 目标项目：`kernel/fs/ext4fs/probe.c#ext4_probe`，调用摘要：LOG_ERROR, LOG_INFO, ext4_get_root_fs, ext4_mount_root, ext4_probe_dir_inode, ext4_read_inode ...(+1)
- 目标项目：`kernel/fs/ext4fs/read.c#ext4_read_file`，调用摘要：bread, brelse, ext4_find_extent, memcpy

#### 通用模块/其他
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `user/ulib.c#printf` | `printf.c#printf` | 同板块同名 | va_arg, va_end, va_start, write | _vprintf, va_end, va_start |
| `kernel/core/spinlock.c#acquire` | `spinlock.c#acquire` | 同板块同名 | __sync_lock_test_and_set, __sync_synchronize, get_cpu, holding, panic, push_off | __sync_lock_test_and_set, __sync_synchronize, holding, mycpu, panic, push_off |
| `kernel/core/string.c#memcpy` | `string.c#memcpy` | 同板块同名 | 内部闭环逻辑 | memmove |
| `user/ulib.c#strncmp` | `string.c#strncmp` | 同板块同名 | 内部闭环逻辑 | 内部闭环逻辑 |


好的，审判长。本庭已收到控辩双方的完整陈述、底层 AST 证据及全模块确定性对比上下文。现在，我将依据客观的 AST 相似度数据、双向覆盖率指标以及双方模块画像，对本案进行最终的独立、深度析理与定性仲裁。

---

### 1. 宏观判定

- **定性结论**: **独立开发 (底层架构显著差异)**
- **核心依据**: 客观 AST 相似度仅为 **3.33%**，双向覆盖率均低于 **6.5%**。该指标远低于通常用于认定代码抄袭的阈值。双方共有的 20 个 AST 指纹，经技术析理，绝大多数可归因于遵循 POSIX 规范、RISC-V 架构 ABI 以及 xv6 教学内核范式的**领域公共知识与标准实现模式**。目标项目在文件系统、硬件抽象、日志系统等核心子系统的架构设计上与参考项目存在根本性分歧，排除了系统性复用的可能。

### 2. 全模块覆盖矩阵解读

| 模块 | 双方存在 | 共同 AST 指纹 | 覆盖率解读 | 证据强度 |
| :--- | :---: | :---: | :--- | :--- |
| **进程管理** | 是 | 4 | 目标 20.0% vs 参考 9.09%。重合集中在 POSIX 标准系统调用包装函数（如 `sys_getpid`）和通用调度原语（如 `yield`）。覆盖率差异表明双方实现体量不同。 | **弱** |
| **任务调度/异步运行时** | 是 | 1 | 目标 4.0% vs 参考 3.85%。覆盖率极低。重合点仅为 `syscall` 分发器等通用模式，且调用图谱存在差异。 | **弱** |
| **内存管理** | 是 | 3 | 目标 3.26% vs 参考 2.52%。覆盖率极低。重合点如 `kvmmake` 是内核初始化的标准流程，具体实现（如分配器）不同。 | **弱** |
| **中断/异常处理** | 是 | 0 | 覆盖率均为 0.0%。尽管双方都有此模块，但 AST 层面未发现任何共同指纹，表明实现完全独立。 | **未形成** |
| **系统调用** | 是 | 0 | 覆盖率均为 0.0%。目标项目有 35 个函数，参考项目仅 4 个，规模和实现完全不同。 | **未形成** |
| **文件系统** | 是 | 3 | 目标 3.9% vs 参考 4.92%。覆盖率极低。重合点如 `dirlink`、`iupdate` 是文件系统 POSIX 语义下的通用操作，但目标项目采用 VFS 多文件系统架构，与参考项目有本质区别。 | **弱** |
| **设备驱动** | 是 | 1 | 目标 5.56% vs 参考 7.69%。覆盖率低。重合点 `virtio_disk_rw` 等是 VirtIO 协议的标准实现流程，且调用图谱存在差异。 | **弱** |
| **网络协议栈** | 否 | 0 | 双方均未体现此模块。 | **未形成** |
| **文件系统/第三方库** | 仅目标 | 0 | 目标项目独有模块（26 个函数），包含 `ext4fs` 等第三方库集成，是架构差异的关键证据。 | **未形成** |
| **通用模块/其他** | 是 | 5 | 目标 11.63% vs 参考 13.51%。覆盖率相对较高，但重合点集中在 `memcpy`、`strncmp`、`acquire` 等**基础库函数与通用同步原语**，这些是任何 C 语言内核的基石，不具备独创性。 | **弱** |

### 3. 底层 AST 关键证据指纹表

| 所属板块 | 目标项目路径 | 参考项目路径 | 证据类型 | 技术说明 |
| :--- | :--- | :--- | :--- | :--- |
| 进程管理 | `kernel/core/proc.c#sched` | `proc.c#sched` | 同板块同名 | 核心调度器。调用链均包含 `intr_get`, `panic`, `swtch`，这是 xv6 调度器范式的标准控制流。目标项目额外调用 `holding` 检查锁状态，并使用 `get_cpu/get_proc` 抽象，表明其安全策略与架构封装不同。 |
| 进程管理 | `kernel/core/proc.c#yield` | `proc.c#yield` | 同板块同名 | 主动让出 CPU。调用链 `acquire -> 获取当前进程 -> release -> sched` 是协作式调度的最小通用实现，无独创性。 |
| 进程管理 | `kernel/core/proc.c#cpuid` | `proc.c#cpuid` | 同板块同名同结构 | 获取 CPU ID。目标项目调用 HAL 层 `hal_get_cpu_id`，参考项目直接读取 `r_tp` 寄存器，体现了硬件抽象层设计上的根本差异。 |
| 任务调度/异步运行时 | `kernel/core/syscall.c#syscall` | `syscall.c#syscall` | 同板块同名 | 系统调用分发器。`NELEM` 边界检查与 `syscalls[num]` 函数指针调用是操作系统内核的经典模式，属于领域公共知识。 |
| 任务调度/异步运行时 | `kernel/core/sleeplock.c#acquiresleep` | `sleeplock.c#acquiresleep` | 同板块同名同结构 | 睡眠锁实现。`acquire -> 记录持有者 -> release -> sleep` 是 xv6 睡眠锁的标准算法，非参考项目独创。 |
| 通用模块/其他 | `kernel/core/spinlock.c#acquire` | `spinlock.c#acquire` | 同板块同名 | 自旋锁实现。使用 `__sync_lock_test_and_set` 等 GCC 内建原子操作是标准实践，调用链高度相似是算法必然。 |
| 通用模块/其他 | `kernel/core/string.c#memcpy` | `string.c#memcpy` | 同板块同名 | 标准 C 库函数，实现逻辑由规范定义，不具备独创性表达。 |
| 文件系统 | `kernel/fs/fs.c#skipelem` | `fs.c#skipelem` | 同板块同名 | 路径名解析辅助函数。调用 `memmove` 进行字符串处理是标准做法，逻辑简单且通用。 |
| 设备驱动 | `kernel/driver/virtio_blk.c#free_chain` | `virtio_disk.c#free_chain` | 同板块同名 | VirtIO 描述符链释放。目标项目调用 `insert_free_desc_list`，参考项目调用 `free_desc`，表明内部描述符管理算法不同。 |
| 内存管理 | `arch/riscv/mm/vm.c#kvmmake` | `vm.c#kvmmake` | 同板块同名 | 内核虚拟内存初始化。调用 `kvmmap` 进行页表映射是标准流程，但目标项目使用 `ekalloc` 分配，参考项目使用 `kalloc`，底层页分配器实现不同。 |

### 4. 核心代码调用流对比剖析

*   **【进程管理/调度器】**
*   **底层技术析理**: 双方 `sched` 函数均遵循 xv6 范式的上下文切换协议：检查中断状态 -> 获取当前进程 -> 切换到调度器线程 -> 执行 `swtch`。然而，目标项目在调用流中显式增加了 `holding` 检查，用于验证当前 CPU 是否持有进程锁，这是一个额外的安全断言。此外，目标项目通过 `get_cpu` 和 `get_proc` 函数封装了对 CPU 和进程结构的访问，而参考项目直接使用 `mycpu` 和 `myproc` 宏/函数。这反映了目标项目在架构上引入了更严格的锁检查机制和一层访问抽象，属于独立的设计决策。
*   **代码溯源链接**:
    - 目标项目: `kernel/core/proc.c#sched`
    - 参考项目: `proc.c#sched`

*   **【任务调度/异步运行时/系统调用分发】**
*   **底层技术析理**: 双方 `syscall` 函数的核心逻辑都是从当前进程的陷阱帧中读取系统调用号，进行边界检查（`NELEM`），然后通过函数指针数组 `syscalls[num]` 调用对应的处理函数。这是操作系统内核中最经典、最通用的系统调用分发模式。目标项目在标准流程之外，增加了 `LOG_TRACE` 和 `LOG_ERROR` 日志节点，并再次使用 `get_proc` 抽象获取进程上下文，而参考项目使用 `myproc`。这些差异是功能增强和架构封装，而非混淆手段。
*   **代码溯源链接**:
    - 目标项目: `kernel/core/syscall.c#syscall`
    - 参考项目: `syscall.c#syscall`

*   **【文件系统/路径解析】**
*   **底层技术析理**: 双方 `sys_chdir` 函数均实现 POSIX `chdir` 语义。但调用流揭示了文件系统架构的根本分歧。目标项目调用 `vfs_lookup_at` 和 `vfs_make_absolute_path`，表明其构建在一个 **VFS（虚拟文件系统）抽象层**之上，支持多种文件系统。参考项目则直接调用 `namei`、`ilock`、`iput` 等传统 inode 层函数，是一个单一文件系统实现。这种架构层级的差异是独立开发的有力证明。
*   **代码溯源链接**:
    - 目标项目: `kernel/core/sysfile.c#sys_chdir`
    - 参考项目: `sysfile.c#sys_chdir`

### 5. 单方模块与降权项

*   **单方模块**:
    *   **文件系统/第三方库 (目标项目独有)**: 目标项目集成了 `ext4fs` 和 `devtmpfs` 等第三方/复杂文件系统模块，这是其与参考项目最显著的架构差异之一，直接证明了其功能范围的独立性。
    *   **系统调用 (规模差异)**: 目标项目实现了 35 个系统调用相关函数，而参考项目仅有 4 个。目标项目更贴近 Linux ABI 兼容，而参考项目更精简，两者设计目标不同。
*   **降权项说明**:
    *   **跨板块同名函数**: 如控方已降级的 `sys_chdir`（分属不同板块），其调用图谱差异巨大，不能作为同源证据。
    *   **公共接口与泛型函数名**: `argint`、`argaddr`、`argstr`、`panic`、`acquire`、`release`、`memcpy` 等函数名是操作系统内核领域的通用术语和标准接口，其出现是领域必然，不能作为抄袭的强信号。
    *   **内部闭环逻辑**: 多个 `sys_setuid`、`sys_getgid` 等函数被标记为“内部闭环逻辑”，说明其实现极其简单（通常为几行代码），其相似性是功能单一和规范约束所致，不具备证据价值。

### 6. 保守结论

本次审计的核心结论是**独立开发**。客观 AST 相似度（3.33%）与双向覆盖率（约 6.4%）均处于极低水平，远不足以构成抄袭认定。控方提出的“复用铁证”，经技术析理，实为在遵循 POSIX 标准、RISC-V 架构 ABI 及 xv6 教学范式过程中产生的**领域公共知识与通用实现模式**。目标项目将 `myproc` 替换为 `get_proc` 并非低级宏替换，而是引入硬件抽象层（HAL）和统一进程访问接口的独立架构设计。其在文件系统（VFS 多文件系统支持）、日志系统、系统调用规模等方面的显著差异，进一步排除了系统性复用的可能。综上，目标项目 [2026-T2026104469910922-XX] 在底层架构上具有充分的原创性，整体抄袭风险极低。

------------------------------

