# 🛡️ OS 内核赛自动化代码级架构审计报告

**审计时间**: `2026-07-02 04:45:09`
**参赛作品名称**: `2026-T2026104469910922-XX`
**年份**: `2026`
**学校**: `XX`
**队伍名称**: `T2026104469910922`
**比赛名字**: `OS-Competition`
**比赛子赛道**: `Kernel-Implementation`
**仓库地址**: `https://gitlab.eduxiji.net/educg-group-43501-3132633/T2026104469910922-2031`
**推测开发语言**: `Rust / C (由AST探针自动识别)`
**特征函数量**: `354`

## ⚔️ 底层代码同源性对比 (AST Homology)
> 本部分由专门的代码审查智能体，对该项目与符合年份条件的历届最高相似度项目进行控制流与底层调用对比。新版报告会先给出确定性的全模块覆盖矩阵，再给出 Agent 的分模块解释，避免只围绕少量抽样函数展开。

### 🏆 Top 4 底层对标: vs `2024-刀片超车-华中科技大学`（华中科技大学 / 刀片超车）

#### 对方作品信息

**对方年份**: `2024`
**对方学校**: `华中科技大学`
**对方队伍名称**: `刀片超车`
**对方比赛名字**: `OS-Competition`
**对方比赛子赛道**: `Kernel-Implementation`
**对方仓库地址**: `https://gitlab.eduxiji.net/educg-group-26011-2376549/T202410487993009-1211`

> 双向覆盖率：共同 AST 指纹: 20；唯一 AST 指纹: 目标项目 311，参考项目 422；覆盖率: 目标项目 6.43%，参考项目 4.74%

## 全模块对比总览（确定性证据）

> 总体覆盖率：共同 AST 指纹: 20；唯一 AST 指纹: 目标项目 311，参考项目 422；覆盖率: 目标项目 6.43%，参考项目 4.74%

### 1. 模块覆盖矩阵
| 模块 | 目标函数数 | 参考函数数 | 共同唯一 AST 指纹 | Target 覆盖 | Base 覆盖 | 状态 |
| :--- | ---: | ---: | ---: | ---: | ---: | :--- |
| 进程管理 | 24 | 63 | 4 | 20.0% | 7.84% | 双方存在且有结构重合 |
| 任务调度/异步运行时 | 26 | 40 | 2 | 8.0% | 5.13% | 双方存在且有结构重合 |
| 内存管理 | 92 | 141 | 2 | 2.17% | 1.44% | 双方存在且有结构重合 |
| 中断/异常处理 | 8 | 17 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |
| 系统调用 | 35 | 8 | 1 | 6.67% | 14.29% | 双方存在且有结构重合 |
| 文件系统 | 82 | 86 | 1 | 1.3% | 1.28% | 双方存在且有结构重合 |
| 设备驱动 | 18 | 14 | 4 | 22.22% | 33.33% | 双方存在且有结构重合 |
| 网络协议栈 | 0 | 0 | 0 | 0.0% | 0.0% | 双方均未体现 |
| 文件系统/第三方库 | 26 | 0 | 0 | 0.0% | 0.0% | 仅目标项目体现 |
| 通用模块/其他 | 43 | 97 | 7 | 16.28% | 7.95% | 双方存在且有结构重合 |

### 2. 分模块证据清单

#### 进程管理
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `kernel/core/sysproc.c#sys_wait4` | `kernel/sysproc.c#sys_wait4` | 同板块同名 | LOG_TRACE, LOG_WARN, argaddr, argint, wait4 | argaddr, argint, wait4pid |
| `kernel/core/proc.c#sched` | `kernel/proc.c#sched` | 同板块同名 | get_cpu, get_proc, holding, intr_get, panic, swtch | holding, intr_get, mycpu, myproc, panic, swtch |
| `kernel/core/proc.c#yield` | `kernel/proc.c#yield` | 同板块同名 | acquire, get_proc, release, sched | acquire, myproc, release, sched |
| `kernel/core/sysproc.c#sys_exit` | `kernel/sysproc.c#sys_exit` | 同板块同名 | LOG_TRACE, argint, exit | argint, exit |

#### 任务调度/异步运行时
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `kernel/core/sysfile.c#sys_chdir` | `kernel/sysfile.c#sys_chdir` | 同板块同名 | argstr, get_proc, strcmp, strncpy, vfs_lookup_at, vfs_make_absolute_path | argstr, elock, ename, eput, eunlock, myproc |
| `kernel/core/syscall.c#syscall` | `kernel/syscall.c#syscall` | 同板块同名 | LOG_ERROR, LOG_TRACE, NELEM, get_proc, syscalls[num] | NELEM, myproc, printf, syscalls[num] |
| `kernel/core/sysfile.c#sys_close` | `kernel/sysfile.c#sys_close` | 同板块同名 | LOG_ERROR, acquire, argint, fileclose, get_proc, release | argfd, fileclose, myproc |
| `kernel/core/syscall.c#argraw` | `kernel/syscall.c#argraw` | 同板块同名 | get_proc, panic | myproc, panic |

#### 内存管理
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `kernel/driver/virtio_blk.c#virtio_disk_init` | `kernel/virtio_disk.c#virtio_disk_init` | 同板块同名 | (uint32), LOG_INFO, LOG_TRACE, VA2PA, VIRTIO_READ32, VIRTIO_WRITE32 ...(+6) | R, initlock, memset, panic, printf |
| `arch/riscv/trap/trap.c#usertrap` | `kernel/trap.c#usertrap` | 同板块同名 | LOG_ERROR, LOG_TRACE, LOG_WARN, PGROUNDUP, cpuid, get_proc ...(+18) | devintr, exit, intr_on, myproc, panic, printf ...(+11) |
| `kernel/driver/virtio_blk.c#virtio_disk_rw` | `kernel/virtio_disk.c#virtio_disk_rw` | 同板块同名 | VA2PA, VIRTIO_WRITE32, __sync_synchronize, acquire, alloc3_desc, free_chain ...(+3) | R, __sync_synchronize, acquire, alloc3_desc, free_chain, kwalkaddr ...(+2) |
| `arch/riscv/mm/vm.c#copyin` | `kernel/vm.c#copyin` | 同板块同名 | LOG_TRACE, LOG_WARN, PA2VA, PGROUNDDOWN, PTE2PA, get_proc ...(+3) | PGROUNDDOWN, memmove, panic, printf, walkaddr |

#### 中断/异常处理
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `kernel/core/spinlock.c#pop_off` | `kernel/intr.c#pop_off` | 同板块同名 | get_cpu, intr_get, intr_on, panic | intr_get, intr_on, mycpu, panic |
| `kernel/core/spinlock.c#push_off` | `kernel/intr.c#push_off` | 同板块同名 | get_cpu, intr_get, intr_off | intr_get, intr_off, mycpu |
| `arch/riscv/trap/trap.c#trapinit` | `kernel/trap.c#trapinit` | 同板块同名 | LOG_TRACE, w_stvec | countdown_timer_init, initlock, intr_on, w_csr_ecfg, w_csr_eentry, w_csr_merrentry ...(+1) |
| `kernel/core/printf.c#_panic` | `kernel/trap.c#trapframedump` | 同模块上下文对齐/差异观察 | intr_off, kprintint, kputs, va_end, va_start, vkprintf | printf |

#### 系统调用
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `kernel/core/syscall.c#argstr` | `kernel/syscall.c#argstr` | 同板块同名 | argaddr, fetchstr | argaddr, fetchstr |
| `kernel/core/syscall.c#argaddr` | `kernel/syscall.c#argaddr` | 同板块同名 | argraw | argraw |
| `kernel/core/syscall.c#argint` | `kernel/syscall.c#argint` | 同板块同名 | argraw | argraw |
| `user/ulib.c#setpriority` | `kernel/syssig.c#sys_rt_sigreturn` | 同板块同结构 | syscall | rt_sigreturn |

#### 文件系统
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `kernel/core/file.c#fileclose` | `kernel/file.c#fileclose` | 同板块同名 | acquire, panic, pipe_close, release, vfs_iput | acquire, eput, panic, pipeclose, release |
| `kernel/fs/fs.c#skipelem` | `kernel/fat32.c#skipelem` | 同板块同名 | memmove | memmove |
| `kernel/core/sysfile.c#sys_mmap` | `kernel/sysfile.c#sys_mmap` | 同板块同名 | argaddr, argint, do_mmap | argaddr, argfd, argint, mmap, printf |
| `kernel/core/sysfile.c#sys_open` | `kernel/sysfile.c#sys_open` | 同板块同名 | LOG_TRACE, argaddr, argint, argstr, open | argint, argstr, open |

#### 设备驱动
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `devlog/2025_12/xv6_uart.c#uartinit` | `kernel/uart.c#uartinit` | 同板块同名同结构 | WriteReg, initlock | WriteReg, initlock |
| `devlog/2025_12/xv6_uart.c#uartstart` | `kernel/uart.c#uartstart` | 同板块同名同结构 | ReadReg, WriteReg, wakeup | ReadReg, WriteReg, wakeup |
| `devlog/2025_12/xv6_uart.c#uartputc` | `kernel/uart.c#uartputc` | 同板块同名 | acquire, release, sleep, uartstart | acquire, release, sleep, uartstart |
| `kernel/driver/virtio_blk.c#free_chain` | `kernel/virtio_disk.c#free_chain` | 同板块同名 | LOG_ERROR, insert_free_desc_list, wakeup | free_desc |

#### 文件系统/第三方库
仅目标项目中观察到该模块代表函数，参考项目没有同模块可对齐证据：
- 目标项目：`kernel/fs/ext4fs/mount.c#ext4_mount`，调用摘要：LOG_TRACE, bread, brelse, ext4_check_features, ext4_read_le16, ext4_read_le32
- 目标项目：`kernel/fs/ext4fs/probe.c#ext4_probe_dir_block`，调用摘要：LOG_ERROR, LOG_INFO, bread, brelse, ext4_read_le16, ext4_read_le32 ...(+1)
- 目标项目：`kernel/fs/ext4fs/probe.c#ext4_probe`，调用摘要：LOG_ERROR, LOG_INFO, ext4_get_root_fs, ext4_mount_root, ext4_probe_dir_inode, ext4_read_inode ...(+1)
- 目标项目：`kernel/fs/ext4fs/read.c#ext4_read_file`，调用摘要：bread, brelse, ext4_find_extent, memcpy

#### 通用模块/其他
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `user/ulib.c#printf` | `user/printf.c#printf` | 同板块同名 | va_arg, va_end, va_start, write | va_start, vprintf |
| `kernel/core/string.c#memcpy` | `user/ulib.c#memcpy` | 同板块同名 | 内部闭环逻辑 | memmove |
| `user/ulib.c#strncmp` | `kernel/string.c#strncmp` | 同板块同名 | 内部闭环逻辑 | 内部闭环逻辑 |
| `kernel/core/spinlock.c#release` | `kernel/spinlock.c#release` | 同板块同名 | __sync_lock_release, __sync_synchronize, holding, panic, pop_off | __sync_lock_release, __sync_synchronize, holding, panic, pop_off |


好的，审判长。本庭已收到控辩双方的全部材料，并依据客观 AST 指纹数据、全模块覆盖矩阵及底层调用图谱，对本案进行了独立的、深度的技术析理。现作出最终定性仲裁。

### 1. 宏观判定

- **定性结论**: **独立开发 (底层架构显著差异)**
- **核心依据**: 客观 AST 相似度仅为 **2.81%**。双方共同 AST 指纹仅 20 个，而目标项目独有 311 个，参考项目独有 422 个。双向覆盖率均处于极低水平（目标项目 6.43%，参考项目 4.74%）。该数据从根本上否定了“系统性复用”或“批量搬运”的指控。控方所列举的“铁证”经技术析理后，均属于操作系统内核在公共领域知识（xv6/POSIX）下的惯用实现，其相似性源于功能需求的唯一性，而非代码抄袭。双方在核心架构抽象、文件系统、内存管理及设备驱动模型上存在根本性差异。

### 2. 全模块覆盖矩阵解读

| 模块 | 双方均存在 | 共同 AST 指纹 | 目标覆盖率 | 参考覆盖率 | 证据强度 | 技术解读 |
| :--- | :---: | :---: | :---: | :---: | :---: | :--- |
| **进程管理** | 是 | 4 | 20.0% | 7.84% | **弱** | 覆盖率主要由 `sched`, `yield` 等少量调度原语贡献。这些是 xv6 内核的标准骨架，其调用流（获取进程上下文、检查锁、切换）是功能唯一性决定的。目标项目使用 `get_proc`/`get_cpu` 抽象，参考项目使用 `myproc`/`mycpu`，表明进程/CPU 上下文获取模型不同。 |
| **任务调度/异步运行时** | 是 | 2 | 8.0% | 5.13% | **弱** | 共同指纹集中在 `syscall` 分发和 `uartintr` 中断处理。`syscall` 的表驱动分发是通用模式；`uartintr` 的调用序列是 xv6 UART 驱动的标准状态机。目标项目引入了结构化日志宏 `LOG_*`，参考项目使用裸 `printf`，工程实践不同。 |
| **内存管理** | 是 | 2 | 2.17% | 1.44% | **未形成** | 覆盖率极低，表明双方在内存管理核心逻辑（如页表构建、分配器算法、虚拟内存映射）上几乎完全不同。共同指纹仅来自 `virtio_disk` 初始化等与内存映射相关的边缘调用，不构成模块级同源证据。 |
| **中断/异常处理** | 是 | 0 | 0.0% | 0.0% | **未形成** | 共同指纹为 0，是双方底层架构差异的最强证据。目标项目基于 RISC-V 架构（`m_trap`, `CLINT_MTIMECMP`, `w_mepc`），参考项目基于 LoongArch 架构（`r_csr_badi`, `w_csr_ecfg`），Trap 处理入口和 CSR 操作完全不同。 |
| **系统调用** | 是 | 1 | 6.67% | 14.29% | **弱** | 共同指纹来自 `argraw`, `argint` 等参数提取函数。这些是系统调用入口的 trivial 辅助函数，其实现（从陷入帧读取寄存器）由硬件 ABI 决定，逻辑必然相似。`sys_fork`, `sys_exit` 等函数名由 POSIX 标准强制规定。 |
| **文件系统** | 是 | 1 | 1.3% | 1.28% | **未形成** | 覆盖率极低，是双方架构差异最显著的模块。目标项目实现了独立的 VFS 层及 ext4/easyfs/devtmpfs 多后端支持；参考项目则基于 FAT32 的 `e*` 对象模型。`sys_chdir` 等系统调用虽同名，但其依赖的外部对象集合（VFS vs. FAT32）完全不同。 |
| **设备驱动** | 是 | 4 | 22.22% | 33.33% | **中** | 覆盖率相对较高，但集中在 UART 和 virtio 块设备驱动。这些驱动是 xv6 的标准组件，其与硬件交互的寄存器操作序列（`WriteReg`, `ReadReg`）和中断处理流程（`uartintr`）是公共领域知识。目标项目的 virtio 驱动有独立的描述符管理实现。 |
| **网络协议栈** | 否 | 0 | 0.0% | 0.0% | **未形成** | 双方均未体现该模块。 |
| **文件系统/第三方库** | 否 | 0 | 0.0% | 0.0% | **未形成** | 仅目标项目存在该模块（ext4fs 实现），参考项目无对应模块。这是目标项目独立开发的强证据。 |
| **通用模块/其他** | 是 | 7 | 16.28% | 7.95% | **弱** | 共同指纹来自 `release`, `memcpy`, `strncmp` 等通用工具函数。这些函数逻辑简单、实现方式有限，且 `release` 等自旋锁操作是 xv6 的经典实现，属于公共领域代码片段。 |

### 3. 底层 AST 关键证据指纹表

| 所属板块 | 目标项目路径 | 参考项目路径 | 证据类型 | 技术说明 |
| :--- | :--- | :--- | :--- | :--- |
| 设备驱动 | `devlog/2025_12/xv6_uart.c#uartintr` | `kernel/uart.c#uartintr` | 同板块底层逻辑一致 | xv6 UART 驱动的标准中断处理状态机：获取锁->读字符->送入控制台->启动发送->释放锁。功能唯一性决定的公共领域惯用实现。 |
| 设备驱动 | `devlog/2025_12/xv6_uart.c#uartstart` | `kernel/uart.c#uartstart` | 同板块同名同结构 | xv6 UART 发送启动的标准实现：检查设备状态寄存器，若就绪则写入数据并唤醒等待进程。硬件操作序列固定。 |
| 任务调度/异步运行时 | `kernel/core/sleeplock.c#acquiresleep` | `kernel/sleeplock.c#acquiresleep` | 同板块同名同结构 | xv6 睡眠锁获取的标准流程：获取自旋锁->记录持有进程->释放自旋锁->睡眠。目标项目用 `get_proc`，参考项目用 `myproc`，接口命名不同。 |
| 进程管理 | `kernel/core/proc.c#cpuid` | `kernel/proc.c#cpuid` | 同板块同名同结构 | 获取当前 CPU ID 的单行函数。目标项目通过 HAL 层 `hal_get_cpu_id` 封装，参考项目直接读取 `r_tp` 寄存器，抽象层次不同。 |
| 进程管理 | `kernel/core/proc.c#sched` | `kernel/proc.c#sched` | 同板块同名 | 上下文切换的核心调度器。调用流（检查锁、关中断、切换上下文）是抢占式内核的必要安全序列。目标项目使用 `get_cpu`/`get_proc`，参考项目使用 `mycpu`/`myproc`。 |
| 进程管理 | `kernel/core/proc.c#yield` | `kernel/proc.c#yield` | 同板块同名 | 主动让出 CPU 的标准实现：`acquire(p->lock) -> 获取当前进程 -> release(p->lock) -> sched()`。逻辑唯一。 |
| 任务调度/异步运行时 | `kernel/core/syscall.c#syscall` | `kernel/syscall.c#syscall` | 同板块同名 | 系统调用表驱动分发的通用模式。目标项目使用 `LOG_*` 宏，参考项目使用 `printf`，日志子系统不同。 |
| 任务调度/异步运行时 | `kernel/core/syscall.c#argraw` | `kernel/syscall.c#argraw` | 同板块同名 | 从进程陷入帧提取参数的 trivial 函数，逻辑由硬件 ABI 决定。 |
| 系统调用 | `kernel/core/syscall.c#argstr` | `kernel/syscall.c#argstr` | 同板块同名 | 系统调用字符串参数提取的标准实现，调用 `argaddr` 和 `fetchstr`。 |
| 通用模块/其他 | `kernel/core/spinlock.c#release` | `kernel/spinlock.c#release` | 同板块同名 | xv6 自旋锁释放的标准实现：内存屏障、清除锁、恢复中断状态。调用序列固定。 |

### 4. 核心代码调用流对比剖析

**【进程管理/调度原语】**
*   **底层技术析理**: 双方均实现了经典的 xv6 风格进程调度。然而，在进程上下文获取这一核心抽象上存在显著差异。目标项目采用 `get_proc()` 和 `get_cpu()` 函数接口，这暗示了其进程和CPU管理可能通过独立的管理器或哈希表实现。参考项目则使用 `myproc()` 和 `mycpu()`，这通常是基于 per-CPU 变量或当前 CPU 结构体指针的直接访问。这种抽象层次的差异，使得上层调度函数 `sched` 和 `yield` 虽然宏观调用流相似，但其依赖的底层对象管理模型是异构的。
*   **代码溯源链接**:
    - 目标项目: `kernel/core/proc.c#sched`
    - 参考项目: `kernel/proc.c#sched`

**【文件系统/路径解析】**
*   **底层技术析理**: 这是双方架构差异最核心的体现。在 `sys_chdir` 系统调用中，目标项目调用 `vfs_lookup_at` 和 `vfs_make_absolute_path`，证明其构建了一个完整的 VFS（虚拟文件系统）层，用于抽象多种文件系统（ext4, easyfs, devtmpfs）。而参考项目直接调用 `elock`, `ename`, `eput` 等 FAT32 文件系统专用的 `e*` 对象接口。这表明双方的文件系统架构在根本设计上分道扬镳：一个是 VFS 多后端架构，一个是 FAT32 单体架构。调用流的相似性仅停留在 POSIX 系统调用入口的层面。
*   **代码溯源链接**:
    - 目标项目: `kernel/core/sysfile.c#sys_chdir`
    - 参考项目: `kernel/sysfile.c#sys_chdir`

**【中断/异常处理/Trap 入口】**
*   **底层技术析理**: 双方 Trap 处理入口的调用图谱无任何共同 AST 指纹，是架构异构的典型。目标项目 `m_trap` 函数直接操作 RISC-V 特权架构定义的 M-mode CSR（如 `CLINT_MTIMECMP`, `r_mip`, `w_mepc`），用于处理机器模式下的定时器和外部中断。参考项目 `kerneltrap` 函数则操作 LoongArch 架构的 CSR（如 `r_csr_badi`, `r_csr_badv`, `r_csr_crmd`）。硬件平台和指令集架构的根本不同，导致了 Trap 处理逻辑的完全独立。
*   **代码溯源链接**:
    - 目标项目: `arch/riscv/trap/mtrap.c#m_trap`
    - 参考项目: `kernel/trap.c#kerneltrap`

### 5. 单方模块与降权项

*   **单方模块（目标项目独有）**:
    - **文件系统/第三方库 (ext4fs)**: 目标项目包含 `kernel/fs/ext4fs/` 目录下的 `ext4_mount`, `ext4_probe`, `ext4_read_file` 等函数，实现了独立的 ext4 文件系统支持。参考项目无此模块，是目标项目独立开发的强证据。
    - **RISC-V 特定 Trap 处理**: 目标项目的 `arch/riscv/trap/mtrap.c` 中的 `m_trap` 函数，处理机器模式中断，是 RISC-V 架构特有的底层代码，参考项目基于 LoongArch，无对应逻辑。
*   **降权项（跨板块/公共知识）**:
    - 控方“证据降级区”中提到的跨板块线索（如目标 `proc.c::sched` 与参考 `trap.c::kerneltrap` 均调用 `holding`），因违反“同板块”限定原则，本庭不予采纳为复用证据。`holding`, `panic`, `NELEM` 等是内核全局通用的基础设施，跨模块调用是正常现象。
    - `sys_fork`, `sys_exit`, `sys_getpid` 等 POSIX 标准系统调用名，以及 `memcpy`, `strncmp` 等标准 C 库函数名，属于公共接口规范，不能作为抄袭证据。

### 6. 保守结论

综合客观 AST 相似度（2.81%）、极低的模块覆盖率以及底层架构的深度剖析，本庭裁定目标项目 `[2026-T2026104469910922-XX]` 与参考项目 `[2024-刀片超车-华中科技大学]` 为**独立开发**。控方提出的“复用铁证”，如 `uartintr` 的调用链重合，实为 xv6 教学操作系统在特定硬件驱动上的公共领域惯用实现，其逻辑由硬件手册和功能唯一性决定，不构成独创性表达。双方在进程/CPU 抽象模型（`get_proc` vs `myproc`）、文件系统架构（VFS/ext4 vs FAT32）以及底层硬件平台（RISC-V vs LoongArch）等核心维度上存在根本性、系统性的差异。现有证据远未达到认定代码抄袭或系统性复用的标准，整体风险极低。

------------------------------

