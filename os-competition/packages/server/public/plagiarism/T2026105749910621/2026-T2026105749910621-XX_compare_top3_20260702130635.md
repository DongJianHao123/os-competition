# 🛡️ OS 内核赛自动化代码级架构审计报告

**审计时间**: `2026-07-02 13:06:35`
**参赛作品名称**: `2026-T2026105749910621-XX`
**年份**: `2026`
**学校**: `XX`
**队伍名称**: `T2026105749910621`
**比赛名字**: `OS-Competition`
**比赛子赛道**: `Kernel-Implementation`
**仓库地址**: `https://gitlab.eduxiji.net/educg-group-43501-3132633/T2026105749910621-2669`
**推测开发语言**: `Rust / C (由AST探针自动识别)`
**特征函数量**: `449`

## ⚔️ 底层代码同源性对比 (AST Homology)
> 本部分由专门的代码审查智能体，对该项目与符合年份条件的历届最高相似度项目进行控制流与底层调用对比。新版报告会先给出确定性的全模块覆盖矩阵，再给出 Agent 的分模块解释，避免只围绕少量抽样函数展开。

### 🏆 Top 3 底层对标: vs `2024-你起吧-长春理工大学`（长春理工大学 / 你起吧）

#### 对方作品信息

**对方年份**: `2024`
**对方学校**: `长春理工大学`
**对方队伍名称**: `你起吧`
**对方比赛名字**: `OS-Competition`
**对方比赛子赛道**: `Kernel-Implementation`
**对方仓库地址**: `https://gitlab.eduxiji.net/educg-group-26010-2376550/T202410186992540-1599`

> 双向覆盖率：共同 AST 指纹: 113；唯一 AST 指纹: 目标项目 417，参考项目 573；覆盖率: 目标项目 27.1%，参考项目 19.72%

## 全模块对比总览（确定性证据）

> 总体覆盖率：共同 AST 指纹: 113；唯一 AST 指纹: 目标项目 417，参考项目 573；覆盖率: 目标项目 27.1%，参考项目 19.72%

### 1. 模块覆盖矩阵
| 模块 | 目标函数数 | 参考函数数 | 共同唯一 AST 指纹 | Target 覆盖 | Base 覆盖 | 状态 |
| :--- | ---: | ---: | ---: | ---: | ---: | :--- |
| 进程管理 | 41 | 37 | 17 | 44.74% | 51.52% | 双方存在且有结构重合 |
| 任务调度/异步运行时 | 47 | 22 | 5 | 10.87% | 22.73% | 双方存在且有结构重合 |
| 内存管理 | 141 | 140 | 31 | 22.46% | 23.48% | 双方存在且有结构重合 |
| 中断/异常处理 | 9 | 9 | 7 | 77.78% | 77.78% | 双方存在且有结构重合 |
| 系统调用 | 37 | 3 | 2 | 6.45% | 100.0% | 双方存在且有结构重合 |
| 文件系统 | 66 | 67 | 22 | 34.92% | 34.38% | 双方存在且有结构重合 |
| 设备驱动 | 17 | 14 | 5 | 29.41% | 35.71% | 双方存在且有结构重合 |
| 网络协议栈 | 0 | 0 | 0 | 0.0% | 0.0% | 双方均未体现 |
| 文件系统/第三方库 | 23 | 323 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |
| 通用模块/其他 | 68 | 71 | 24 | 38.71% | 39.34% | 双方存在且有结构重合 |

### 2. 分模块证据清单

#### 进程管理
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `kernel-riscv/kernel/proc.c#scheduler` | `kernel/proc.c#scheduler` | 同板块同名 | acquire, intr_off, intr_on, mycpu, release, swtch | acquire, intr_on, mycpu, release, swtch |
| `kernel-riscv/kernel/proc.c#procdump` | `kernel/proc.c#procdump` | 同板块同名同结构 | NELEM, printf | NELEM, printf |
| `kernel-riscv/kernel/proc.c#sleep` | `kernel/proc.c#sleep` | 同板块同名同结构 | acquire, myproc, release, sched | acquire, myproc, release, sched |
| `kernel-riscv/kernel/proc.c#sched` | `kernel/proc.c#sched` | 同板块同名 | holding, intr_get, mycpu, myproc, panic, swtch | holding, intr_get, mycpu, myproc, panic, swtch |

#### 任务调度/异步运行时
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `kernel-riscv/kernel/virtio_disk.c#virtio_disk_intr` | `kernel/virtio_disk.c#virtio_disk_intr` | 同板块同名 | R, __atomic_thread_fence, acquire, panic, release, wakeup | R, __sync_synchronize, acquire, free_chain, panic, release ...(+1) |
| `kernel-riscv/kernel/console.c#consoleintr` | `kernel/console.c#consoleintr` | 同板块同名同结构 | C, acquire, consputc, procdump, release, wakeup | C, acquire, consputc, procdump, release, wakeup |
| `kernel-riscv/kernel/console.c#consoleread` | `kernel/console.c#consoleread` | 同板块同名同结构 | C, acquire, either_copyout, killed, myproc, release ...(+1) | C, acquire, either_copyout, killed, myproc, release ...(+1) |
| `kernel-riscv/kernel/trap.c#kerneltrap` | `kernel/trap.c#kerneltrap` | 同板块同名 | devintr, intr_get, myproc, panic, printf, r_scause ...(+6) | devintr, intr_get, myproc, panic, printf, r_scause ...(+6) |

#### 内存管理
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `kernel-riscv/kernel/file.c#filewrite` | `kernel/file.c#filewrite` | 同板块同名同结构 | begin_op, devsw[f->major].write, end_op, ilock, iunlock, panic ...(+2) | begin_op, devsw[f->major].write, end_op, ilock, iunlock, panic ...(+2) |
| `kernel-riscv/kernel/proc.c#freeproc` | `kernel/proc.c#freeproc` | 同板块同名 | free_linux_vmas, kfree, memset, proc_freepagetable | kfree, memset, proc_freepagetable |
| `kernel-riscv/kernel/vm.c#kvmmake` | `kernel/vm.c#kvmmake` | 同板块同名 | kalloc, kvmmap, memset, proc_mapstacks | kalloc, kvmmap, memset, proc_mapstacks |
| `kernel-riscv/kernel/proc.c#allocproc` | `src/task.rs#allocproc` | 同板块同名 | acquire, allocpid, freeproc, kalloc, memset, proc_pagetable ...(+1) | PageTable::new, SpinLock::new, TASK_POOL.alloc, TrapFrame::new, UnsafeCell::new, allocpid ...(+4) |

#### 中断/异常处理
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `kernel-riscv/kernel/plic.c#plicinithart` | `kernel/plic.c#plicinithart` | 同板块同名同结构 | PLIC_SENABLE, PLIC_SPRIORITY, cpuid | PLIC_SENABLE, PLIC_SPRIORITY, cpuid |
| `kernel-riscv/kernel/trap.c#clockintr` | `kernel/trap.c#clockintr` | 同板块同名 | acquire, cpuid, release, timerinit, wakeup | acquire, release, wakeup |
| `kernel-riscv/kernel/spinlock.c#pop_off` | `kernel/spinlock.c#pop_off` | 同板块同名同结构 | intr_get, intr_on, mycpu, panic | intr_get, intr_on, mycpu, panic |
| `kernel-riscv/kernel/plic.c#plicinit` | `kernel/plic.c#plicinit` | 同板块同名同结构 | 内部闭环逻辑 | 内部闭环逻辑 |

#### 系统调用
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `kernel-riscv/kernel/syscall.c#argstr` | `kernel/syscall.c#argstr` | 同板块同名同结构 | argaddr, fetchstr | argaddr, fetchstr |
| `kernel-riscv/kernel/syscall.c#argaddr` | `kernel/syscall.c#argaddr` | 同板块同名同结构 | argraw | argraw |
| `kernel-riscv/kernel/syscall.c#argint` | `kernel/syscall.c#argint` | 同板块同名同结构 | argraw | argraw |

#### 文件系统
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `kernel-riscv/mkfs/mkfs.c#iappend` | `mkfs/mkfs.c#iappend` | 同板块同名同结构 | assert, bcopy, min, rinode, rsect, winode ...(+2) | assert, bcopy, min, rinode, rsect, winode ...(+2) |
| `kernel-riscv/kernel/sysfile.c#sys_unlink` | `kernel/sysfile.c#sys_unlink` | 同板块同名 | argstr, begin_op, dirlookup, end_op, ilock, isdirempty ...(+7) | argstr, unlinkat |
| `kernel-riscv/kernel/fs.c#writei` | `kernel/fs.c#writei` | 同板块同名 | bmap, bread, brelse, either_copyin, iupdate, log_write ...(+1) | rs_inode_read, rs_writei |
| `kernel-riscv/kernel/fs.c#iget` | `kernel/fs.c#iget` | 同板块同名 | acquire, panic, release | acquire, panic, release |

#### 设备驱动
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `kernel-riscv/kernel/uart.c#uartinit` | `kernel/uart.c#uartinit` | 同板块同名同结构 | WriteReg, initlock | WriteReg, initlock |
| `kernel-riscv/kernel/console.c#consolewrite` | `kernel/console.c#consolewrite` | 同板块同名 | either_copyin, uartwrite | acquiresleep, either_copyin, releasesleep, uartputc |
| `kernel-riscv/kernel/uart.c#uartputc_sync` | `kernel/uart.c#uartputc_sync` | 同板块同名 | ReadReg, WriteReg, pop_off, push_off | ReadReg, WriteReg, pop_off, push_off, sbi_console_putchar |
| `kernel-riscv/kernel/virtio_disk.c#free_desc` | `kernel/virtio_disk.c#free_desc` | 同板块同名同结构 | panic, wakeup | panic, wakeup |

#### 文件系统/第三方库
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `kernel-riscv/kernel/linux_exec.c#linux_exec_ext4` | `third/ext4_rs/src/ext4_impls/extents.rs#extent_remove_space` | 同模块上下文对齐/差异观察 | PGROUNDDOWN, PGROUNDUP, ext4_lite_read_file, linux_flags2perm, linux_free_vmas, linux_init_default_fds ...(+10) | Block::load, Ext4Extent::default, Ok, ex.get_actual_len, ex.get_first_block, ex.get_pblock ...(+16) |
| `kernel-riscv/kernel/ext4_lite.c#ext4_mount` | `third/ext4_rs/src/ext4_impls/extents.rs#ext_remove_leaf` | 同模块上下文对齐/差异观察 | bread, brelse, get16, get32, printf | Block::load, Block::load_inode_root_block, Ext4Extent::default, Ok, ex.get_actual_len, ex.get_pblock ...(+14) |
| `kernel-riscv/kernel/ext4_lite.c#ext4_lite_probe` | `third/ext4_rs/src/ext4_impls/file.rs#write_at` | 同模块上下文对齐/差异观察 | ext4_lite_lookup_path, ext4_lite_read_file, ext4_mount, ext4_require_path, printf | Block::load, Ok, block.sync_blk_to_disk, block.write_offset, drop, inode_ref
                .inode
                .set_size ...(+10) |
| `kernel-riscv/kernel/ext4_lite.c#ext4_extent_map_node` | `third/ext4_rs/src/ext4_impls/balloc.rs#balloc_alloc_block` | 同模块上下文对齐/差异观察 | ext4_extent_map_node, ext4_read_block, get16, get32, get64lohi, kalloc ...(+1) | Block::load, Ext4BlockGroup::load_new, Ok, block_group.get_block_bitmap_block, block_group.get_free_blocks_count, block_group.set_block_group_balloc_bitmap_csum ...(+16) |

#### 通用模块/其他
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `kernel-riscv/user/printf.c#vprintf` | `user/printf.c#vprintf` | 同板块同名 | printint, printptr, putc, va_arg | printint, printptr, putc, va_arg |
| `kernel-riscv/kernel/printf.c#printf` | `user/printf.c#printf` | 同板块同名 | acquire, consputc, printint, printptr, release, va_arg ...(+2) | va_start, vprintf |
| `kernel-riscv/kernel/spinlock.c#release` | `kernel/spinlock.c#release` | 同板块同名 | __atomic_store_n, holding, panic, pop_off | __sync_lock_release, __sync_synchronize, holding, panic, pop_off |
| `kernel-riscv/kernel/bio.c#bget` | `kernel/bio.c#bget` | 同板块同名同结构 | acquire, acquiresleep, panic, release | acquire, acquiresleep, panic, release |


好的，审判长。本庭已收到控辩双方的全部陈述、底层 AST 证据及全模块确定性对比上下文。现在，本庭将依据客观数据与代码逻辑，对本案进行最终的独立仲裁。

---

### 1. 宏观判定
- **定性结论**: **独立开发，底层架构存在显著差异**
  本案客观 AST 相似度仅为 **12.88%**，远低于构成“同源性”或“抄袭”的阈值。双方共同 AST 指纹仅 113 个，而目标项目独有 417 个，参考项目独有 573 个。覆盖率（目标 27.1%，参考 19.72%）表明，双方超过 70% 的代码结构是彼此唯一的。控方所指控的“整体性搬运”与“贴牌式抄袭”，在宏观数据上不成立。双方相似性主要源于对同一教学操作系统（xv6）的继承与遵循，而非目标项目对参考项目的定向复用。

### 2. 全模块覆盖矩阵解读
本庭对全模块矩阵进行逐项审查，以确定各模块的证据强度。

- **进程管理**: 双方均存在。存在 17 个共同指纹，覆盖率分别为 44.74% 和 51.52%。相似性集中于 xv6 标准内核原语（如 `scheduler`, `sleep`, `myproc`），属于公共骨架。目标项目独有的 `linux_*` 兼容层函数与参考项目独有的 `sys_clone` 等函数，体现了巨大的演进差异。**证据强度: 弱**。共同指纹是公共祖先的遗留，而非抄袭证据。

- **任务调度/异步运行时**: 双方均存在。共同指纹仅 5 个，覆盖率分别为 10.87% 和 22.73%。重合点仅为 `consoleintr`、`virtio_disk_intr` 等标准设备中断处理函数，其结构由硬件交互逻辑决定。**证据强度: 弱**。低覆盖率表明调度与异步模型实现高度独立。

- **内存管理**: 双方均存在。共同指纹 31 个，覆盖率分别为 22.46% 和 23.48%。重合点如 `kvmmake`、`filewrite` 等，均为 xv6 标准内核态内存操作与文件写入流程。目标项目的 `free_linux_vmas` 与参考项目的 Rust 内存管理（`PageTable::new`）揭示了完全不同的内存抽象层。**证据强度: 弱**。

- **中断/异常处理**: 双方均存在。共同指纹 7 个，覆盖率高达 77.78%。这是由 RISC-V 特权架构与 PLIC 中断控制器规范决定的。`plicinit`、`plicinithart` 等函数是对硬件手册的直译，`pop_off` 等是 xv6 的中断屏蔽标准原语。**证据强度: 弱**。高覆盖率是硬件约束下的必然收敛，非代码复用。

- **系统调用**: 双方均存在。共同指纹仅 2 个，目标覆盖率 6.45%，参考覆盖率 100%。重合点仅为 `argint`、`argaddr` 等参数提取函数，这是 xv6 系统调用分发路径的标准范式。目标项目 37 个函数 vs 参考项目 3 个函数，规模差异巨大，证明目标项目构建了庞大的独立系统调用层。**证据强度: 未形成**。

- **文件系统**: 双方均存在。共同指纹 22 个，覆盖率约 34%。重合点如 `iget`、`writei`、`iappend` 等，是 xv6 类 Unix 文件系统的标准 inode 操作流程。目标项目的 `ext4_lite` 与参考项目的 `ext4_rs` 是两个完全独立、语言不同（C vs Rust）的 ext4 实现。**证据强度: 弱**。

- **设备驱动**: 双方均存在。共同指纹 5 个，覆盖率约 30%。重合点如 `uartinit`、`virtio_disk` 相关函数，是对 UART/VirtIO 硬件协议的标准驱动实现。**证据强度: 弱**。

- **网络协议栈**: 双方均未体现。**证据强度: 未形成**。

- **第三方库/公共依赖**: 双方均存在，但共同指纹为 0。目标项目有 23 个函数，参考项目有 323 个函数，但 AST 结构无任何重合。这证明双方在第三方库的选型与集成上完全不同（如目标项目自研 `ext4_lite`，参考项目引入 Rust 的 `ext4_rs`）。**证据强度: 未形成**。

### 3. 底层 AST 关键证据指纹表
本庭提取双方可对齐的关键代码路径，并评估其证据价值。所有证据均指向“同源 xv6 骨架”或“领域标准实现”，而非“定向抄袭”。

| 所属板块 | 目标项目路径 | 参考项目路径 | 证据类型 | 技术说明 |
| :--- | :--- | :--- | :--- | :--- |
| 进程管理 | `kernel-riscv/kernel/proc.c#scheduler` | `kernel/proc.c#scheduler` | 同板块同名 | xv6 标准调度循环。目标项目增加了 `intr_off`/`intr_on` 中断保护，是独立的功能增强。 |
| 进程管理 | `kernel-riscv/kernel/proc.c#sleep` | `kernel/proc.c#sleep` | 同板块同名同结构 | xv6 标准条件睡眠原语。`acquire, myproc, release, sched` 调用链是此机制的最小实现。 |
| 进程管理 | `kernel-riscv/kernel/proc.c#myproc` | `kernel/proc.c#myproc` | 同板块同名同结构 | RISC-V 架构下获取当前进程的标准方法，依赖 `tp` 寄存器和 per-CPU 变量。 |
| 进程管理 | `kernel-riscv/kernel/sysproc.c#sys_exit` | `kernel/sysproc.c#sys_exit` | 同板块同名同结构 | xv6 系统调用入口。目标调用 `kexit`（含 contest runner 逻辑），参考调用 `exit`（含 sbi 关机），语义不同。 |
| 进程管理 | `kernel-riscv/kernel/sysproc.c#sys_fork` | `kernel/sysproc.c#sys_fork` | 同板块同名同结构 | xv6 系统调用入口。目标调用 `kfork`，参考调用 `clone`，进程创建模型根本不同。 |
| 内存管理 | `kernel-riscv/kernel/vm.c#kvmmake` | `kernel/vm.c#kvmmake` | 同板块同名 | xv6 标准内核页表初始化流程，调用 `kalloc`, `kvmmap` 等标准函数。 |
| 内存管理 | `kernel-riscv/kernel/proc.c#allocproc` | `src/task.rs#allocproc` | 同板块同名 | 进程分配。目标为 C 语言实现，参考为 Rust 实现（`PageTable::new`），语言和模型均不同。 |
| 中断/异常处理 | `kernel-riscv/kernel/plic.c#plicinithart` | `kernel/plic.c#plicinithart` | 同板块同名同结构 | PLIC 硬件初始化序列，由 RISC-V 中断控制器规范定义，无创造性。 |
| 中断/异常处理 | `kernel-riscv/kernel/spinlock.c#pop_off` | `kernel/spinlock.c#pop_off` | 同板块同名同结构 | xv6 中断屏蔽/恢复的标准原语，用于保护临界区。 |
| 系统调用 | `kernel-riscv/kernel/syscall.c#argint` | `kernel/syscall.c#argint` | 同板块同名同结构 | xv6 系统调用参数提取的标准封装函数。 |
| 文件系统 | `kernel-riscv/kernel/fs.c#iget` | `kernel/fs.c#iget` | 同板块同名 | xv6 inode 缓存获取的标准实现，使用自旋锁保护。 |
| 设备驱动 | `kernel-riscv/kernel/uart.c#uartinit` | `kernel/uart.c#uartinit` | 同板块同名同结构 | UART 硬件初始化序列，由硬件协议决定。 |
| 第三方库/公共依赖 | `kernel-riscv/kernel/ext4_lite.c#ext4_mount` | `third/ext4_rs/src/ext4_impls/extents.rs#ext_remove_leaf` | 同模块上下文对齐/差异观察 | 双方均实现 ext4，但目标为 C 自研，参考为 Rust 库，内部调用流完全不同。 |

### 4. 核心代码调用流对比剖析
本庭选取双方均存在的核心模块，进行底层技术析理，以论证其异构性。

* **【进程管理/进程退出路径】**
* **底层技术析理**: 控方指控 `sys_exit` 是“换名式抄袭”。然而，深入调用流即可发现本质差异。参考项目的 `exit` 函数最终调用 `sbi_shutdown`，表明其设计目标是一个直接运行在裸机上的独立内核。而目标项目的 `kexit` 函数，其调用图谱包含 `contest_runner_finish` 和 `proc_close_linux_fds`，表明其运行在一个**竞赛运行环境**中，进程退出后需要通知管理器，并清理 Linux 兼容层的文件描述符。两者的**运行时上下文和退出语义完全不同**，绝非简单的重命名。
* **代码溯源链接**:
  - 目标项目: `kernel-riscv/kernel/sysproc.c#sys_exit` -> `kernel-riscv/kernel/proc.c#kexit`
  - 参考项目: `kernel/sysproc.c#sys_exit` -> `kernel/proc.c#exit`

* **【内存管理/进程分配】**
* **底层技术析理**: 这是双方架构差异的典型例证。参考项目的 `allocproc` 位于 `src/task.rs`，是一个 **Rust 实现**，其调用图谱包含 `PageTable::new`, `SpinLock::new`, `TASK_POOL.alloc` 等 Rust 风格的内存安全抽象和对象池管理。目标项目的 `allocproc` 是传统的 **C 实现**，调用 `kalloc`, `memset`, `proc_pagetable` 等 xv6 风格的函数。两者在**编程语言、内存模型（Rust 所有权 vs C 手动管理）、对象管理模型（对象池 vs 空闲链表）**上均存在根本性差异。
* **代码溯源链接**:
  - 目标项目: `kernel-riscv/kernel/proc.c#allocproc`
  - 参考项目: `src/task.rs#allocproc`

* **【文件系统/ext4 实现】**
* **底层技术析理**: 双方都实现了 ext4 文件系统，但路径截然不同。目标项目采用自研的 `ext4_lite`（C 语言），其挂载函数 `ext4_mount` 直接调用底层 `bread`, `brelse`, `get32` 等块设备 I/O 函数。参考项目则集成了 Rust 的 `ext4_rs` 第三方库，其内部操作通过 `Block::load`, `Ext4Extent` 等高级抽象完成。这是**自研轻量级实现 vs 集成第三方成熟库**的路线差异，代码结构无任何重合。
* **代码溯源链接**:
  - 目标项目: `kernel-riscv/kernel/ext4_lite.c#ext4_mount`
  - 参考项目: `third/ext4_rs/src/ext4_impls/extents.rs#ext_remove_leaf`

### 5. 单方模块与降权项
以下模块或证据因仅存在于一方或属于公共接口，不能作为强同源证据：

- **目标项目独有模块**: Linux 兼容层（`linux_syscall.c`, `linux_exec.c` 中的大量函数）、竞赛运行环境支持（`contest_runner_finish`）。这些是目标项目的核心创新，在参考项目中无任何对应。
- **参考项目独有模块**: Rust 实现的任务管理（`src/task.rs`）、Rust 版 ext4 库（`third/ext4_rs/`）、`sys_clone`/`sys_mmap` 等 Linux 系统调用。这些是参考项目的特征，目标项目未实现或采用不同模型。
- **公共接口/泛型函数名**: `printf`, `memset`, `strlen`, `memmove`, `NELEM`, `KSTACK` 等。这些是 C 标准库或 xv6 公共宏，其调用图谱一致是功能使然，不构成抄袭证据。
- **跨板块弱信号**: 控方已降级的 `linux_proc_virtual_file` 等函数，因在参考项目中无直接 AST 同构对应，本庭不予采纳。

### 6. 保守结论
本庭裁定，目标项目 `[2026-T2026105749910621-XX]` 与参考项目 `[2024-你起吧-长春理工大学]` 的 12.88% 低 AST 相似度，是双方基于同一开源教学内核（xv6）进行独立二次开发的正常结果。控方提出的“整体性搬运”与“贴牌式抄袭”指控，混淆了“公共骨架继承”与“定向代码复用”的界限。双方在进程模型、内存管理、文件系统实现及上层应用生态上存在显著且根本的架构差异。目标项目独有的 Linux 兼容层与竞赛环境支持，是其独立开发的有力证明。综上，本庭认定目标项目为独立开发，驳回控方所有指控。

------------------------------

