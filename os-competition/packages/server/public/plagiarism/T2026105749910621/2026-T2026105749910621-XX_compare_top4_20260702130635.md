# 🛡️ OS 内核赛自动化代码级架构审计报告

**审计时间**: `2026-07-02 13:06:35`
**参赛作品名称**: `2026-T2026105749910621-XX`
**年份**: `2026`
**学校**: `XX`
**队伍名称**: `T2026105749910621`
**比赛名字**: `OS-Competition`
**比赛子赛道**: `Kernel-Implementation`
**仓库地址**: `https://gitlab.eduxiji.net/educg-group-43501-3132633/T2026105749910621-2669`
**推测开发语言**: `Rust / C (由AST探针自动识别)`
**特征函数量**: `449`

## ⚔️ 底层代码同源性对比 (AST Homology)
> 本部分由专门的代码审查智能体，对该项目与符合年份条件的历届最高相似度项目进行控制流与底层调用对比。新版报告会先给出确定性的全模块覆盖矩阵，再给出 Agent 的分模块解释，避免只围绕少量抽样函数展开。

### 🏆 Top 4 底层对标: vs `2024-月光加冕贝拉拉-南京理工大学`（南京理工大学 / 月光加冕贝拉拉）

#### 对方作品信息

**对方年份**: `2024`
**对方学校**: `南京理工大学`
**对方队伍名称**: `月光加冕贝拉拉`
**对方比赛名字**: `OS-Competition`
**对方比赛子赛道**: `Kernel-Implementation`
**对方仓库地址**: `https://gitlab.eduxiji.net/educg-group-26010-2376550/T202410288992519-2849`

> 双向覆盖率：共同 AST 指纹: 82；唯一 AST 指纹: 目标项目 417，参考项目 323；覆盖率: 目标项目 19.66%，参考项目 25.39%

## 全模块对比总览（确定性证据）

> 总体覆盖率：共同 AST 指纹: 82；唯一 AST 指纹: 目标项目 417，参考项目 323；覆盖率: 目标项目 19.66%，参考项目 25.39%

### 1. 模块覆盖矩阵
| 模块 | 目标函数数 | 参考函数数 | 共同唯一 AST 指纹 | Target 覆盖 | Base 覆盖 | 状态 |
| :--- | ---: | ---: | ---: | ---: | ---: | :--- |
| 进程管理 | 41 | 40 | 11 | 28.95% | 27.5% | 双方存在且有结构重合 |
| 任务调度/异步运行时 | 47 | 33 | 6 | 13.04% | 18.75% | 双方存在且有结构重合 |
| 内存管理 | 141 | 95 | 16 | 11.59% | 17.58% | 双方存在且有结构重合 |
| 中断/异常处理 | 9 | 9 | 1 | 11.11% | 11.11% | 双方存在且有结构重合 |
| 系统调用 | 37 | 4 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |
| 文件系统 | 66 | 73 | 8 | 12.7% | 11.43% | 双方存在且有结构重合 |
| 设备驱动 | 17 | 9 | 2 | 11.76% | 22.22% | 双方存在且有结构重合 |
| 网络协议栈 | 0 | 0 | 0 | 0.0% | 0.0% | 双方均未体现 |
| 文件系统/第三方库 | 23 | 0 | 0 | 0.0% | 0.0% | 仅目标项目体现 |
| 通用模块/其他 | 68 | 85 | 36 | 58.06% | 48.65% | 双方存在且有结构重合 |

### 2. 分模块证据清单

#### 进程管理
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `kernel-riscv/user/sh.c#parseexec` | `user/sh.c#parseexec` | 同板块同名同结构 | execcmd, gettoken, panic, parseblock, parseredirs, peek | execcmd, gettoken, panic, parseblock, parseredirs, peek |
| `kernel-riscv/kernel/proc.c#procdump` | `kernel/proc.c#procdump` | 同板块同名 | NELEM, printf | NELEM, printf |
| `kernel-riscv/kernel/proc.c#sleep` | `kernel/proc.c#sleep` | 同板块同名 | acquire, myproc, release, sched | acquire, myproc, release, sched |
| `kernel-riscv/kernel/proc.c#sched` | `kernel/proc.c#sched` | 同板块同名 | holding, intr_get, mycpu, myproc, panic, swtch | holding, intr_get, mycpu, myproc, panic, swtch |

#### 任务调度/异步运行时
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `kernel-riscv/user/sh.c#runcmd` | `user/sh.c#runcmd` | 同板块同名 | close, dup, exec, exit, fork1, fprintf ...(+5) | close, dup, exec, exit, fork1, fprintf ...(+5) |
| `kernel-riscv/kernel/console.c#consoleintr` | `kernel/console.c#consoleintr` | 同板块同名 | C, acquire, consputc, procdump, release, wakeup | C, acquire, consputc, procdump, release, wakeup |
| `kernel-riscv/kernel/console.c#consoleread` | `kernel/console.c#consoleread` | 同板块同名 | C, acquire, either_copyout, killed, myproc, release ...(+1) | C, acquire, either_copyout, myproc, release, sleep |
| `kernel-riscv/user/sh.c#nulterminate` | `user/sh.c#nulterminate` | 同板块同名同结构 | nulterminate | nulterminate |

#### 内存管理
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `kernel-riscv/kernel/sysfile.c#sys_open` | `kernel/sysfile.c#sys_open` | 同板块同名 | argint, argstr, begin_op, create, end_op, fdalloc ...(+7) | argint, argstr, create, elock, ename, eput ...(+5) |
| `kernel-riscv/kernel/sysfile.c#create` | `kernel/sysfile.c#create` | 同板块同名 | dirlink, dirlookup, ialloc, ilock, iunlockput, iupdate ...(+1) | ealloc, elock, enameparent, eput, eunlock |
| `kernel-riscv/kernel/proc.c#freeproc` | `kernel/proc.c#freeproc` | 同板块同名 | free_linux_vmas, kfree, memset, proc_freepagetable | kfree, kvmfree, proc_freepagetable |
| `kernel-riscv/kernel/proc.c#allocproc` | `kernel/proc.c#allocproc` | 同板块同名 | acquire, allocpid, freeproc, kalloc, memset, proc_pagetable ...(+1) | acquire, allocpid, freeproc, kalloc, memset, panic ...(+3) |

#### 中断/异常处理
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `kernel-riscv/kernel/plic.c#plicinithart` | `kernel/plic.c#plicinithart` | 同板块同名 | PLIC_SENABLE, PLIC_SPRIORITY, cpuid | PLIC_SENABLE, printf |
| `kernel-riscv/kernel/spinlock.c#pop_off` | `kernel/intr.c#pop_off` | 同板块同名 | intr_get, intr_on, mycpu, panic | intr_get, intr_on, mycpu, panic |
| `kernel-riscv/kernel/plic.c#plicinit` | `kernel/plic.c#plicinit` | 同板块同名 | 内部闭环逻辑 | printf, writed |
| `kernel-riscv/kernel/plic.c#plic_claim` | `kernel/plic.c#plic_claim` | 同板块同名 | PLIC_SCLAIM, cpuid | PLIC_SCLAIM, cpuid |

#### 系统调用
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `kernel-riscv/kernel/syscall.c#argstr` | `kernel/syscall.c#argstr` | 同板块同名 | argaddr, fetchstr | argaddr, fetchstr |
| `kernel-riscv/kernel/syscall.c#argaddr` | `kernel/syscall.c#argaddr` | 同板块同名 | argraw | argraw |
| `kernel-riscv/kernel/syscall.c#argint` | `kernel/syscall.c#argint` | 同板块同名 | argraw | argraw |
| `kernel-riscv/kernel/linux_syscall.c#linux_resolve_basic_exec_path` | `kernel/systime.c#sys_gettimeofday` | 同模块上下文对齐/差异观察 | linux_has_slash, safestrcpy, strlen, strncmp | argaddr, copyout2, r_time |

#### 文件系统
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `kernel-riscv/user/grind.c#go` | `user/grind.c#go` | 同板块同名 | chdir, close, dup, exec, exit, fork ...(+16) | chdir, close, dup, exec, exit, fork ...(+16) |
| `kernel-riscv/kernel/fs.c#dirlookup` | `kernel/fat32.c#dirlookup` | 同板块同名 | iget, namecmp, panic, readi | edup, eget, enext, eput, panic, reloc_clus ...(+2) |
| `kernel-riscv/kernel/file.c#fileclose` | `kernel/file.c#fileclose` | 同板块同名 | acquire, begin_op, end_op, iput, panic, pipeclose ...(+1) | acquire, eput, panic, pipeclose, release |
| `kernel-riscv/kernel/fs.c#skipelem` | `kernel/fat32.c#skipelem` | 同板块同名 | memmove | memmove |

#### 设备驱动
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `kernel-riscv/kernel/console.c#consolewrite` | `kernel/console.c#consolewrite` | 同板块同名 | either_copyin, uartwrite | acquire, either_copyin, release, sbi_console_putchar, uart_putchar |
| `kernel-riscv/user/sh.c#parseblock` | `user/sh.c#parseblock` | 同板块同名同结构 | gettoken, panic, parseline, parseredirs, peek | gettoken, panic, parseline, parseredirs, peek |
| `kernel-riscv/kernel/console.c#consputc` | `kernel/console.c#consputc` | 同板块同名 | uartputc_sync | sbi_console_putchar, uart_putchar |
| `kernel-riscv/kernel/printf.c#panic` | `kernel/printf.c#panic` | 同板块同名 | printf | backtrace, printf |

#### 文件系统/第三方库
仅目标项目中观察到该模块代表函数，参考项目没有同模块可对齐证据：
- 目标项目：`kernel-riscv/kernel/linux_exec.c#linux_exec_ext4`，调用摘要：PGROUNDDOWN, PGROUNDUP, ext4_lite_read_file, linux_flags2perm, linux_free_vmas, linux_init_default_fds ...(+10)
- 目标项目：`kernel-riscv/kernel/ext4_lite.c#ext4_mount`，调用摘要：bread, brelse, get16, get32, printf
- 目标项目：`kernel-riscv/kernel/ext4_lite.c#ext4_lite_probe`，调用摘要：ext4_lite_lookup_path, ext4_lite_read_file, ext4_mount, ext4_require_path, printf
- 目标项目：`kernel-riscv/kernel/ext4_lite.c#ext4_extent_map_node`，调用摘要：ext4_extent_map_node, ext4_read_block, get16, get32, get64lohi, kalloc ...(+1)

#### 通用模块/其他
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `kernel-riscv/user/printf.c#vprintf` | `user/printf.c#vprintf` | 同板块同名 | printint, printptr, putc, va_arg | printint, printptr, putc, va_arg |
| `kernel-riscv/kernel/printf.c#printf` | `user/printf.c#printf` | 同板块同名 | acquire, consputc, printint, printptr, release, va_arg ...(+2) | fflush, va_start, vprintf |
| `kernel-riscv/kernel/spinlock.c#release` | `kernel/spinlock.c#release` | 同板块同名 | __atomic_store_n, holding, panic, pop_off | __sync_lock_release, __sync_synchronize, holding, panic, pop_off |
| `kernel-riscv/kernel/bio.c#bget` | `kernel/bio.c#bget` | 同板块同名同结构 | acquire, acquiresleep, panic, release | acquire, acquiresleep, panic, release |


好的，审计官。以下是基于提供的证据底座、控辩双方陈述及审计指令，对本案进行的最终同源性定性仲裁。

### 1. 宏观判定
- **定性结论**: **独立开发 (底层架构显著差异)**
- **核心依据**: 客观 AST 相似度仅为 **12.46%**，远低于同源性阈值。目标项目拥有 **417** 个唯一 AST 指纹，参考项目拥有 **323** 个，双方在底层实现上展现出高度的独立性。覆盖率指标（目标项目 19.66%，参考项目 25.39%）表明，即使存在重合，也仅占各自代码库的较小部分，且这些重合点绝大多数可归因于共同的上游教学内核（xv6-riscv）及 RISC-V 硬件规范约束。

### 2. 全模块覆盖矩阵解读
| 模块 | 双方存在 | 共同指纹 | 覆盖率 (目标/参考) | 证据强度 | 解读 |
| :--- | :---: | :---: | :---: | :---: | :--- |
| **进程管理** | 是 | 11 | 28.95% / 27.5% | **中** | 重合点集中在 `proc.c` 和 `sysproc.c` 的核心调度与上下文切换函数，均为 xv6 标准模板。目标项目独有的 `linux_*` 系列函数构成显著差异。 |
| **任务调度/异步运行时** | 是 | 6 | 13.04% / 18.75% | **弱** | 重合点主要在 `sh.c` (Shell 解析器) 和 `console.c` (控制台驱动)，属于用户态交互逻辑，与内核调度核心无关。 |
| **内存管理** | 是 | 16 | 11.59% / 17.58% | **弱** | 重合点如 `allocproc`、`freeproc` 是进程生命周期管理的标准步骤。目标项目独有的 `linux_exec.c` 和 `ext4_lite.c` 引入了全新的内存映射与文件系统内存管理逻辑。 |
| **中断/异常处理** | 是 | 1 | 11.11% / 11.11% | **弱** | 重合点 `pop_off` 是 xv6 的中断关闭/开启原语，受 RISC-V 架构 `sstatus` 寄存器操作约束，实现方式唯一。 |
| **系统调用** | 是 | 0 | 0.0% / 0.0% | **未形成** | 尽管双方都有系统调用模块，但**未观察到任何共同 AST 指纹**。目标项目实现了庞大的 Linux 兼容系统调用层，与参考项目完全异构。 |
| **文件系统** | 是 | 8 | 12.7% / 11.43% | **弱** | 重合点如 `dirlookup`、`skipelem` 是路径解析的通用算法。目标项目实现了独立的 `ext4_lite` 文件系统，而参考项目基于 FAT32，底层存储管理模型完全不同。 |
| **设备驱动** | 是 | 2 | 11.76% / 22.22% | **弱** | 重合点 `consolewrite` 和 `plic_claim` 是 UART 和 PLIC 中断控制器的标准驱动逻辑，由硬件手册定义。 |
| **网络协议栈** | 否 | 0 | 0.0% / 0.0% | **未形成** | 双方均未体现该模块。 |
| **文件系统/第三方库** | 仅目标 | 0 | 0.0% / 0.0% | **未形成** | 目标项目独有的 `ext4_lite` 实现，是证明其独立开发能力的关键证据。 |

### 3. 底层 AST 关键证据指纹表
| 所属板块 | 目标项目路径 | 参考项目路径 | 证据类型 | 技术说明 |
| :--- | :--- | :--- | :--- | :--- |
| **进程管理** | `kernel-riscv/kernel/proc.c#yield` | `kernel/proc.c#yield` | 同板块同名同调用图 | 均为 `acquire -> myproc -> release -> sched`，这是 xv6 教学内核中进程自愿放弃 CPU 的标准锁保护流程。 |
| **进程管理** | `kernel-riscv/kernel/proc.c#sched` | `kernel/proc.c#sched` | 同板块同名同调用图 | 均为 `holding -> intr_get -> mycpu -> myproc -> panic -> swtch`，这是 xv6 上下文切换前的标准不变式检查序列。 |
| **进程管理** | `kernel-riscv/user/sh.c#parseexec` | `user/sh.c#parseexec` | 同板块同名同调用图 | 均为 `execcmd -> gettoken -> panic -> parseblock -> parseredirs -> peek`，这是 xv6 Shell 递归下降解析器的标准骨架。 |
| **内存管理** | `kernel-riscv/kernel/proc.c#allocproc` | `kernel/proc.c#allocproc` | 同板块同名同调用图 | 均包含 `kalloc`、`memset`、`proc_pagetable` 等，是 xv6 进程结构体与内核栈分配的通用流程。 |
| **文件系统** | `kernel-riscv/kernel/fs.c#dirlookup` | `kernel/fat32.c#dirlookup` | 同板块同名 | 均调用 `iget`/`eget` 和 `namecmp`/`enext`，是目录项查找的标准模式，但底层 inode 管理模型不同。 |
| **中断/异常处理** | `kernel-riscv/kernel/spinlock.c#pop_off` | `kernel/intr.c#pop_off` | 同板块同名同调用图 | 均为 `intr_get -> intr_on -> mycpu -> panic`，是 RISC-V 架构下恢复中断状态的标准操作。 |
| **设备驱动** | `kernel-riscv/kernel/plic.c#plic_claim` | `kernel/plic.c#plic_claim` | 同板块同名同调用图 | 均为 `PLIC_SCLAIM, cpuid`，是 RISC-V PLIC 中断控制器读取中断 ID 的标准操作。 |
| **通用模块/其他** | `kernel-riscv/kernel/spinlock.c#release` | `kernel/spinlock.c#release` | 同板块同名 | 均包含 `holding -> panic -> pop_off` 及原子操作，是自旋锁释放的标准模式，但原子指令实现有差异。 |

### 4. 核心代码调用流对比剖析
* **【进程管理/进程退出路径】**
* **底层技术析理**: 目标项目在系统调用层 (`sys_exit`) 调用 `kexit`，而参考项目调用 `exit`。`kexit` 的调用链 (`acquire, begin_op, end_op, fileclose, iput, myproc, panic, proc_close_linux_fds...`) 比参考项目的 `exit` 更复杂，明确增加了 `proc_close_linux_fds` 等 Linux 兼容层清理逻辑。这表明目标项目在复用 xv6 进程退出框架的基础上，进行了独立的、面向 Linux ABI 的架构扩展，而非简单复制。
* **代码溯源链接**:
  - 目标项目: `kernel-riscv/kernel/sysproc.c#sys_exit` -> `kernel-riscv/kernel/proc.c#kexit`
  - 参考项目: `kernel/sysproc.c#sys_exit` -> `kernel/proc.c#exit`

* **【内存管理/分配器模型】**
* **底层技术析理**: 双方在 `allocproc` 中均调用 `kalloc` 分配内核栈，但目标项目的内存管理子系统深度集成了 `linux_exec.c` 中的 `linux_free_vmas`、`PGROUNDDOWN` 等虚拟内存区域操作，服务于其 ext4 文件系统执行器。参考项目则使用 `growproc` 等更贴近原始 xv6 的堆管理方式。两者在内存抽象层次上存在根本差异。
* **代码溯源链接**:
  - 目标项目: `kernel-riscv/kernel/proc.c#allocproc` -> `kernel-riscv/kernel/linux_exec.c#linux_exec_ext4`
  - 参考项目: `kernel/proc.c#allocproc` -> `kernel/sysproc.c#sys_sbrk`

* **【文件系统/路径查找】**
* **底层技术析理**: 目标项目的 `ext4_lite_lookup_path` 调用链 (`ext4_extent_map_node -> ext4_read_block -> get32...`) 揭示了其基于 ext4 区段树的独立查找算法。参考项目的 `dirlookup` 则基于 FAT32 的簇链遍历 (`enext -> reloc_clus...`)。两者在底层存储数据结构和查找算法上完全不同，是架构异构性的典型代表。
* **代码溯源链接**:
  - 目标项目: `kernel-riscv/kernel/ext4_lite.c#ext4_lite_lookup_path`
  - 参考项目: `kernel/fat32.c#dirlookup`

### 5. 单方模块与降权项
*   **目标项目独有模块**:
    *   **Linux 兼容层 (`kernel/linux_syscall.c`, `kernel/linux_exec.c`)**: 包含 `/proc` 文件系统、Linux 系统调用转发、Busybox 角色识别等完整子系统。这是目标项目最核心的原创架构，在参考项目中无任何对应。
    *   **ext4 文件系统 (`kernel/ext4_lite.c`)**: 实现了 ext4 区段树、超级块、inode 查找等轻量级驱动，是独立于参考项目 FAT32 实现的全新模块。
    *   **竞赛框架 (`contest_runner_finish` 等)**: 目标项目特有的竞赛测试框架集成。
*   **参考项目独有模块**:
    *   **`wait4pid` / `sys_wait4` / `sys_clone`**: 参考项目实现了更复杂的进程等待与克隆变体系统调用，目标项目未包含。
*   **降权项说明**:
    *   **`cpuid` / `mycpu` / `sys_getpid` 等极小函数**: 这些函数体量极小（通常1-3行），其实现由 RISC-V 硬件规范（`r_tp` 指令）或 xv6 数据结构（`mycpu()->proc`）唯一确定，独立开发必然趋同，不能作为抄袭证据。
    *   **`zombie.c::main`**: 虽为同板块同名同结构，但目标项目使用 `pause` 而参考项目使用 `sleep`，这一差异恰恰体现了目标项目向 Linux 信号语义靠拢的独立设计意图。
    *   **`parseexec` / `fork1`**: 位于 `user/sh.c`，属于用户态 Shell 程序，其逻辑由 Shell 语法和 xv6 标准库决定，与内核进程管理核心架构无关。

### 6. 保守结论
本次审计的客观 AST 相似度仅为 12.46%，属于典型的**独立开发**范畴。控辩双方聚焦的进程管理板块，其重合点（如 `yield`, `sched`, `myproc`）均为 MIT xv6-riscv 教学内核的标准模板函数，其调用图一致是共同教学来源与 RISC-V 硬件约束下的必然结果，不构成抄袭证据。目标项目不仅拥有 417 个唯一 AST 指纹，更实现了**Linux 系统调用兼容层**和**ext4 文件系统**这两个在参考项目中完全不存在的、具有显著架构复杂度的独立模块。这些事实共同构成了证明其独立开发的有力证据链。因此，整体同源性风险极低，不应判定为抄袭或违规复用。

------------------------------

