# 🛡️ OS 内核赛自动化代码级架构审计报告

**审计时间**: `2026-07-02 03:34:00`
**参赛作品名称**: `2026-T2026102139911088-XX`
**年份**: `2026`
**学校**: `XX`
**队伍名称**: `T2026102139911088`
**比赛名字**: `OS-Competition`
**比赛子赛道**: `Kernel-Implementation`
**仓库地址**: `https://gitlab.eduxiji.net/educg-group-43501-3132633/T2026102139911088-2824`
**推测开发语言**: `Rust / C (由AST探针自动识别)`
**特征函数量**: `5003`

## ⚔️ 底层代码同源性对比 (AST Homology)
> 本部分由专门的代码审查智能体，对该项目与符合年份条件的历届最高相似度项目进行控制流与底层调用对比。新版报告会先给出确定性的全模块覆盖矩阵，再给出 Agent 的分模块解释，避免只围绕少量抽样函数展开。

### 🏆 Top 1 底层对标: vs `2022-S0S-哈尔滨工业大学`（哈尔滨工业大学 / S0S）

#### 对方作品信息

**对方年份**: `2022`
**对方学校**: `哈尔滨工业大学`
**对方队伍名称**: `S0S`
**对方比赛名字**: `OS-Competition`
**对方比赛子赛道**: `Kernel-Implementation`
**对方仓库地址**: `https://gitlab.eduxiji.net/educg-group-14158-894147/oskernel2022-s0s`

> 双向覆盖率：共同 AST 指纹: 95；唯一 AST 指纹: 目标项目 4183，参考项目 447；覆盖率: 目标项目 2.27%，参考项目 21.25%

## 全模块对比总览（确定性证据）

> 总体覆盖率：共同 AST 指纹: 95；唯一 AST 指纹: 目标项目 4183，参考项目 447；覆盖率: 目标项目 2.27%，参考项目 21.25%

### 1. 模块覆盖矩阵
| 模块 | 目标函数数 | 参考函数数 | 共同唯一 AST 指纹 | Target 覆盖 | Base 覆盖 | 状态 |
| :--- | ---: | ---: | ---: | ---: | ---: | :--- |
| 进程管理 | 528 | 51 | 4 | 0.82% | 8.51% | 双方存在且有结构重合 |
| 任务调度/异步运行时 | 642 | 41 | 6 | 0.99% | 15.0% | 双方存在且有结构重合 |
| 内存管理 | 963 | 139 | 52 | 5.91% | 40.94% | 双方存在且有结构重合 |
| 中断/异常处理 | 27 | 5 | 2 | 8.0% | 50.0% | 双方存在且有结构重合 |
| 系统调用 | 1072 | 35 | 4 | 0.48% | 16.67% | 双方存在且有结构重合 |
| 文件系统 | 1315 | 166 | 7 | 0.68% | 4.7% | 双方存在且有结构重合 |
| 设备驱动 | 39 | 42 | 1 | 2.94% | 2.63% | 双方存在且有结构重合 |
| 网络协议栈 | 25 | 0 | 0 | 0.0% | 0.0% | 仅目标项目体现 |
| 文件系统/第三方库 | 213 | 0 | 0 | 0.0% | 0.0% | 仅目标项目体现 |
| 网络协议栈/第三方库 | 3 | 0 | 0 | 0.0% | 0.0% | 仅目标项目体现 |
| 通用模块/其他 | 176 | 31 | 5 | 3.07% | 17.24% | 双方存在且有结构重合 |

### 2. 分模块证据清单

#### 进程管理
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `os/src/task/process_block.rs#exec` | `os/src/task/task.rs#exec` | 同板块同名 | MemorySet::from_elf, Ok, self.exec_with_memory_set | Aux::new, KERNEL_SPACE.lock, KERNEL_SPACE.lock().token, MemorySet::from_elf, String::from, TrapContext::app_init_context ...(+27) |
| `os/src/task/processor.rs#take_current_task` | `os/src/task/processor.rs#take_current_task` | 同板块同名 | drop, local_processor, local_processor().lock, processor.take_current_task | PROCESSORS[hartid()].lock, PROCESSORS[hartid()].lock().take_current, hartid |
| `os/src/task/processor.rs#current_task` | `os/src/task/processor.rs#current_task` | 同板块同名 | drop, local_processor, local_processor().lock, processor.current | PROCESSORS[hartid()].lock, PROCESSORS[hartid()].lock().current, hartid |
| `os/src/task/processor.rs#current` | `os/src/task/processor.rs#current` | 同板块同名 | self.now_task_block.as_ref, self.now_task_block.as_ref().cloned | self.current.as_ref, self.current.as_ref().map |

#### 任务调度/异步运行时
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `os/src/task/manager/run_queue.rs#add` | `os/src/task/manager.rs#add` | 同板块同名 | FairGroupQueue::new, Some, current_fair_entity_for_group, current_time_ns_usize, fair_group_id_and_shares, fair_nice_weight ...(+26) | self.ready_queue.push_back |
| `os/src/task/signal.rs#check_error` | `os/src/task/signal.rs#check_error` | 同板块同名 | Some, self.contains | Some, self.contains |
| `os/src/task/manager/run_queue.rs#fetch` | `os/src/task/manager.rs#fetch` | 同板块同名 | Self::pop_ready_fair_candidate, Self::pop_ready_rt_candidate, Some, log::debug!, picked.is_none, rq.rt_queues.iter_mut ...(+7) | self.ready_queue.pop_front |
| `os/src/task/manager.rs#add_task` | `os/src/task/manager.rs#add_task` | 同板块同名 | Arc::clone, Some, arch::send_ipi, crate::task::processor::current_task, crate::task::processor::hart_id, crate::task::processor::request_reschedule_for_wakeup ...(+4) | Arc::clone, PID2TCB.lock, TASK_MANAGERS[hartid()].lock, TASK_MANAGERS[hartid()].lock().add, TASK_MANAGERS[len % CORE_NUM].lock, TASK_MANAGERS[len % CORE_NUM].lock().add ...(+5) |

#### 内存管理
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `os/src/task/processor.rs#exit_current_and_run_next` | `os/src/task/mod.rs#exit_current_and_run_next` | 同板块同名 | Arc::new, Arc::ptr_eq, FilesStruct::new, MemorySet::new_bare, MmRef::new, TaskContext::new ...(+60) | Arc::downgrade, INITPROC.inner_exclusive_access, Some, TaskContext::zero_init, child.clone, child.inner_exclusive_access ...(+13) |
| `os/src/arch/riscv64/trap/handler.rs#trap_return` | `os/src/trap/mod.rs#trap_return` | 同板块同名 | FIRST_TRAP_RETURN_LOGGED.swap, PageTable::from_token, TRAP_RETURN_COUNT.fetch_add, TRAP_RETURN_COUNT.load, VirtAddr::from, VirtAddr::from(TRAMPOLINE).floor ...(+31) | asm!, current_user_token, set_user_trap_entry |
| `os/src/arch/riscv64/trap/handler.rs#trap_handler` | `os/src/trap/mod.rs#trap_handler` | 同板块同名 | TRAP_HANDLER_COUNT.fetch_add, asm!, check_if_current_signals_error, check_timer, crate::fs::cgroup_maybe_block_current, crate::syscall::misc::check_current_rlimit_cpu ...(+25) | check_signals_error_of_current, current_add_signal, current_trap_cx, exit_current_and_run_next, handle_signals, panic! ...(+9) |
| `os/src/mm/memory_set.rs#from_elf` | `os/src/mm/memory_set.rs#from_elf` | 同板块同名 | (load_bias + (ph.virtual_addr() + ph.mem_size()) as usize).into, (load_bias + ph.virtual_addr() as usize).into, Err, MapArea::new, Ok, Self::new_bare ...(+39) | ((ph.virtual_addr() + ph.mem_size()) as usize).into, (ph.virtual_addr() as usize).into, Aux::new, MapArea::new, Self::new_bare, Some ...(+33) |

#### 中断/异常处理
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `os/src/arch/loongarch64/mod.rs#enable_timer_interrupt` | `os/src/trap/mod.rs#enable_timer_interrupt` | 同板块同名 | asm!, enable_ipi_interrupt | sie::set_stimer |
| `os/src/arch/riscv64/trap/handler.rs#set_kernel_trap_entry` | `os/src/trap/mod.rs#set_kernel_trap_entry` | 同板块同名 | riscv::register::stvec::Stvec::new, riscv::register::stvec::write | stvec::write |
| `os/src/arch/riscv64/trap/handler.rs#set_user_trap_entry` | `os/src/trap/mod.rs#set_user_trap_entry` | 同板块同名 | riscv::register::stvec::Stvec::new, riscv::register::stvec::write | stvec::write |
| `os/src/arch/loongarch64/trap/handler.rs#init_trap` | `os/src/trap/mod.rs#init` | 同板块同结构 | set_kernel_trap_entry | set_kernel_trap_entry |

#### 系统调用
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `user/src/syscall/mod.rs#sigaction` | `user/src/lib.rs#sigaction` | 同板块同名 | RtSigAction::default, SignalFlags::from_bits_truncate, act.mask.bits, action.map, action_rt
        .as_ref, action_rt
        .as_ref()
        .map_or ...(+5) | sys_sigaction |
| `os/src/syscall/net/unix.rs#shutdown` | `user/src/lib.rs#shutdown` | 同板块同名 | end.shutdown, self.notify_poll_waiters, self.state.lock, st.stream_end.clone | sys_shutdown |
| `user/src/syscall/mod.rs#sleep` | `user/src/lib.rs#sleep` | 同板块同名 | syscall | sys_nanosleep |
| `user/src/syscall/mod.rs#exec` | `user/src/lib.rs#exec` | 同板块同名 | args_addr.as_ptr, args_addr.is_empty, core::ptr::null, path.as_ptr, syscall | sys_exec |

#### 文件系统
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `os/src/fs/socketpair.rs#shutdown` | `fat32/src/sbi.rs#shutdown` | 同板块同名 | Ok, inbox.wake_all, read_end.shutdown_read_end, self.opts.lock, write_end.shutdown_write_end | panic!, sbi_call |
| `os/src/fs/inode.rs#read_all` | `os/src/fs/inode.rs#read_all` | 同板块同名 | Self::flush_inner, Vec::new, ext4_lock, inner.inode.read_at, inner.inode.size, self.inner.lock ...(+1) | Vec::new, inner.inode.read, inner.inode.read().read_at, self.inner_exclusive_access, v.extend_from_slice |
| `os/src/fs/pipe.rs#make_pipe` | `os/src/fs/pipe.rs#make_pipe` | 同板块同名 | Arc::new, Mutex::new, Pipe::read_end_with_buffer, Pipe::write_end_with_buffer, PipeRingBuffer::new, buffer.clone ...(+3) | Arc::new, Mutex::new, Pipe::read_end_with_buffer, Pipe::write_end_with_buffer, PipeRingBuffer::new, buffer.clone ...(+2) |
| `os/src/fs/cgroupfs/file.rs#writable` | `os/src/fs/inode.rs#writable` | 同板块同名 | matches! | 内部闭环逻辑 |

#### 设备驱动
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `os/src/drivers/block/mod.rs#block_device_test` | `os/src/drivers/block/mod.rs#block_device_test` | 同板块同名同结构 | BLOCK_DEVICE.clone, assert_eq!, block_device.read_block, block_device.write_block, println!, write_buffer.iter_mut | BLOCK_DEVICE.clone, assert_eq!, block_device.read_block, block_device.write_block, println!, write_buffer.iter_mut |
| `os/src/drivers/block/virtio_blk.rs#read_block` | `os/src/drivers/block/virtio_blk.rs#read_block` | 同板块同名 | self.read_blocks | self.0
            .lock, self.0
            .lock()
            .read_block, self.0
            .lock()
            .read_block(block_id, buf)
            .expect |
| `os/src/drivers/block/virtio_blk.rs#read_block` | `os/src/drivers/block/sdcard.rs#read_block` | 同板块同名 | self.read_blocks | self.0.lock, self.0.lock().read_sector, self.0.lock().read_sector(buf, block_id as u32).unwrap |
| `os/src/drivers/block/virtio_blk.rs#write_block` | `os/src/drivers/block/virtio_blk.rs#write_block` | 同板块同名 | self.write_blocks | self.0
            .lock, self.0
            .lock()
            .write_block, self.0
            .lock()
            .write_block(block_id, buf)
            .expect |

#### 网络协议栈
仅目标项目中观察到该模块代表函数，参考项目没有同模块可对齐证据：
- 目标项目：`os/src/net/mod.rs#new_stack`，调用摘要：(ns_id as u64).wrapping_mul, Config::new, Interface::new, IpAddress::Ipv4, IpAddress::Ipv6, IpCidr::new ...(+10)
- 目标项目：`os/src/net/mod.rs#udp_port_bound_in`，调用摘要：NET.lock, init_in, net.get_mut, sock.endpoint, stack.sockets.iter, stack.sockets.iter().any
- 目标项目：`os/src/net/mod.rs#socket_uses_local_port`，调用摘要：sock
                    .local_endpoint, sock
                    .local_endpoint()
                    .is_some_and, sock.endpoint, sock.get_bound_endpoint
- 目标项目：`os/src/net/mod.rs#poll_busy_with_sockets_mut_in`，调用摘要：NET.lock, drain_pending_veth_ip_deliveries, f, init_in, net.get_mut, net.get_mut(&ns_id).unwrap ...(+2)

#### 文件系统/第三方库
仅目标项目中观察到该模块代表函数，参考项目没有同模块可对齐证据：
- 目标项目：`ext4-fs-packer/src/main.rs#main`，调用摘要：Args::parse, Command::new, Command::new("mke2fs")
        .arg, Command::new("mke2fs")
        .arg("-t")
        .arg, Command::new("mke2fs")
        .arg("-t")
        .arg("ext4")
        .arg, Command::new("mke2fs")
        .arg("-t")
        .arg("ext4")
        .arg("-F")
        .arg ...(+39)
- 目标项目：`ext4-fs/src/vfs.rs#write_extents_to_inode`，调用摘要：Err, Ok, Self::alloc_extent_metadata_blocks, Self::dealloc_extent_metadata_blocks, Self::extent_tree_blocks, Self::write_extent_index_block ...(+18)
- 目标项目：`os/src/syscall/filesystem/path_utils.rs#resolve_ext4_path`，调用摘要：Err, Ok, String::from_utf8_lossy, String::from_utf8_lossy(&target_bytes).into_owned, String::new, alloc::sync::Arc::clone ...(+35)
- 目标项目：`ext4-fs/src/vfs.rs#dir_add_entry`，调用摘要：Arc::clone, Err, Ok, Self::map_logical_block, Self::map_logical_block(&extents, lblock as u32).ok_or, Self::map_logical_block(&extents, new_block_index).ok_or ...(+15)

#### 网络协议栈/第三方库
仅目标项目中观察到该模块代表函数，参考项目没有同模块可对齐证据：
- 目标项目：`os/src/syscall/net/netdev.rs#ipv4_from_smoltcp`，调用摘要：ip.as_bytes
- 目标项目：`ext4-fs/src/vfs.rs#is_socket`，调用摘要：inode.is_socket, self.read_disk_inode
- 目标项目：`ext4-fs/src/layout.rs#is_socket`，调用摘要：内部闭环逻辑

#### 通用模块/其他
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `os/src/sbi/mod.rs#sbi_call` | `os/src/sbi.rs#sbi_call` | 同板块同名同结构 | asm! | asm! |
| `os/src/console/mod.rs#print` | `os/src/console.rs#print` | 同板块同名 | CONSOLE_LOCK.lock, Stdout.write_fmt, Stdout.write_fmt(args).unwrap, disable_interrupts, restore_interrupts | STDOUT.lock, STDOUT.lock().write_fmt, STDOUT.lock().write_fmt(args).unwrap |
| `os/src/arch/loongarch64/mod.rs#set_timer` | `os/src/sbi.rs#set_timer` | 同板块同名 | asm!, timer.max | sbi_call |
| `os/src/time.rs#set_next_trigger` | `os/src/timer.rs#set_next_trigger` | 同板块同名 | get_time_ns, get_time_ns().saturating_add, loongarch_program_clockevent_deadline_ns, riscv_program_clockevent_delta_ticks, riscv_tick_delta, tick_period_ns | get_time, set_timer |


好的，我将遵循审计指令，基于客观数据与证据底座，对本案进行最终同源性定性仲裁。

### 1. 宏观判定
- **定性结论**: **独立开发** (底层架构显著差异)
- **核心依据**: 客观 AST 相似度仅为 **2.09%**。目标项目覆盖率 **2.27%**，参考项目覆盖率 **21.25%**。共同 AST 指纹仅 95 个，而目标项目拥有 4183 个唯一指纹，规模是参考项目的 9.4 倍。极低的覆盖率与巨大的规模差异，从根本上排除了系统性复用的可能。控方指控的“铁证”多为操作系统领域的必然实现或 POSIX 强制命名，不构成实质性抄袭证据。

### 2. 全模块覆盖矩阵解读

| 模块 | 双方存在 | 共同指纹 | 目标覆盖 | 参考覆盖 | 证据强度 | 解读 |
| :--- | :---: | :---: | :---: | :---: | :---: | :--- |
| **进程管理** | 是 | 4 | 0.82% | 8.51% | **弱** | 目标项目函数量是参考的10倍，极低的覆盖率表明两者实现规模与复杂度不在同一量级。重合点仅为 `getpid` 等 POSIX 强制接口。 |
| **任务调度/异步运行时** | 是 | 6 | 0.99% | 15.0% | **弱** | 目标项目实现了复杂的 CFS、实时调度、cgroups 等，而参考项目为简单的 FIFO 调度。同名函数如 `fetch` 的内部调用流完全不同。 |
| **内存管理** | 是 | 52 | 5.91% | 40.94% | **中** | 覆盖率相对较高，但主要源于 `MemorySet::from_elf` 等 ELF 加载标准流程和 `MapArea` 等通用数据结构。核心内存管理架构（如分配器）存在差异。 |
| **中断/异常处理** | 是 | 2 | 8.0% | 50.0% | **弱** | 重合点 `set_kernel_trap_entry` 等是 RISC-V 架构的必选操作（写 `stvec` 寄存器），属于硬件规范决定的必然实现。 |
| **系统调用** | 是 | 4 | 0.48% | 16.67% | **弱** | 目标项目实现了 1072 个系统调用相关函数，远超参考项目的 35 个。重合仅为用户态的 `exec`、`sleep` 等 POSIX 标准封装接口。 |
| **文件系统** | 是 | 7 | 0.68% | 4.7% | **弱** | 目标项目实现了完整的 ext4、procfs、cgroupfs 等复杂文件系统，参考项目为简易 inode 文件系统。重合点 `make_pipe` 等为通用结构。 |
| **设备驱动** | 是 | 1 | 2.94% | 2.63% | **弱** | `block_device_test` 函数完全一致，但这是一个标准的块设备读写测试用例，其逻辑是固定的，属于功能验证的通用写法。 |
| **网络协议栈** | 否 | 0 | 0.0% | 0.0% | **未形成** | 目标项目拥有完整的基于 smoltcp 的网络协议栈，参考项目完全缺失此模块，是目标项目独立开发的强证据。 |
| **第三方库/公共依赖** | 否 | 0 | 0.0% | 0.0% | **未形成** | 目标项目包含 `ext4-fs` 等第三方库的集成与修改，参考项目无此模块，体现了目标项目在功能上的巨大扩展。 |

### 3. 底层 AST 关键证据指纹表

| 所属板块 | 目标项目路径 | 参考项目路径 | 证据类型 | 技术说明 |
| :--- | :--- | :--- | :--- | :--- |
| 进程管理 | `os/src/task/process_block.rs#getpid` | `os/src/task/task.rs#getpid` | 同板块底层逻辑一致 | 两者均为“内部闭环逻辑”，直接返回进程PID字段。这是获取进程标识符的唯一合理实现方式，属于领域必然实现，不具备区分度。 |
| 进程管理 | `os/src/task/processor.rs#current` | `os/src/task/processor.rs#current` | 同板块同名 | 目标项目调用 `self.now_task_block.as_ref().cloned()`，参考项目调用 `self.current.as_ref().map()`。字段名与操作均不同，AST 节点序列不重合。 |
| 进程管理 | `os/src/task/process_block.rs#exec` | `os/src/task/task.rs#exec` | 同板块同名 | 唯一共同调用是 `MemorySet::from_elf`，这是加载 ELF 文件的标准入口。目标项目后续调用 `self.exec_with_memory_set` 进行二次封装，与参考项目直接构建 `TrapContext` 的流程完全不同。 |
| 任务调度 | `os/src/task/manager/run_queue.rs#fetch` | `os/src/task/manager.rs#fetch` | 同板块同名 | 目标项目实现了 CFS/RT 双队列调度逻辑，调用 `pop_ready_fair_candidate` 等复杂函数；参考项目仅为 `self.ready_queue.pop_front()` 的 FIFO 出队。 |
| 内存管理 | `os/src/mm/memory_set.rs#from_elf` | `os/src/mm/memory_set.rs#from_elf` | 同板块同名 | 两者均解析 ELF 头并创建 `MapArea`，这是 ELF 加载的标准流程。目标项目增加了 `load_bias` 等地址随机化相关逻辑，体现了实现差异。 |
| 系统调用 | `user/src/syscall/mod.rs#sigprocmask` | `user/src/lib.rs#sigprocmask` | 同板块同名 | 目标项目使用泛型宏 `syscall` 统一封装，参考项目调用具体函数 `sys_sigprocmask`。封装策略完全不同。 |
| 文件系统 | `os/src/fs/pipe.rs#make_pipe` | `os/src/fs/pipe.rs#make_pipe` | 同板块同名 | 两者均创建 `Pipe` 结构体及其读写端，这是实现管道的标准做法。调用参数和后续配置存在差异。 |
| 设备驱动 | `os/src/drivers/block/mod.rs#block_device_test` | `os/src/drivers/block/mod.rs#block_device_test` | 同板块同名同结构 | 这是一个标准的块设备读写测试函数，其逻辑（写入模式数据 -> 读回 -> 断言校验）是固定的测试范式。 |
| 第三方库 | `ext4-fs/src/vfs.rs#write_extents_to_inode` | (无) | 仅目标项目存在 | 目标项目集成了完整的 ext4 文件系统库，实现了 extent 树管理等复杂逻辑，参考项目完全不具备。 |

### 4. 核心代码调用流对比剖析

* **【进程管理/调度器核心访问】**
* **底层技术析理**: 两者获取当前任务指针的抽象层次和同步模型不同。参考项目直接通过 hart ID 索引全局静态数组 `PROCESSORS[hartid()].lock()`，是面向硬件核的扁平设计。目标项目则通过 `local_processor()` 获取本地处理器抽象，再调用 `.lock()`，体现了更复杂的多核封装与可能的 per-CPU 变量优化。`current` 函数的内部实现，目标项目使用 `cloned()` 获取 `Arc<TaskControlBlock>` 的副本，而参考项目使用 `map()` 返回引用，反映了不同的所有权和生命周期管理策略。
* **代码溯源链接**:
  - 目标项目: `os/src/task/processor.rs#take_current_task`
  - 参考项目: `os/src/task/processor.rs#take_current_task`

* **【任务调度/核心调度逻辑】**
* **底层技术析理**: 调度器的核心 `fetch` 逻辑揭示了完全不同的复杂度。参考项目的 `fetch` 是简单的 FIFO 队列 `pop_front`，无调度策略可言。目标项目的 `fetch` 则实现了双优先级调度：优先调用 `pop_ready_rt_candidate` 查找实时任务，若无则调用 `pop_ready_fair_candidate` 进入 CFS 公平调度流程。这证明目标项目实现了一个具备实时性和公平性保证的现代调度器，而非对教学用简单调度器的复用。
* **代码溯源链接**:
  - 目标项目: `os/src/task/manager/run_queue.rs#fetch`
  - 参考项目: `os/src/task/manager.rs#fetch`

* **【内存管理/ELF加载】**
* **底层技术析理**: 两者虽都调用 `MemorySet::from_elf`，但内部逻辑已分叉。参考项目的 `exec` 函数在调用 `from_elf` 后，直接在内核空间 (`KERNEL_SPACE`) 中操作，手动构建 `TrapContext` 和 `Aux` 向量，流程较为线性。目标项目的 `exec` 则是一个薄封装，将核心逻辑下放到 `self.exec_with_memory_set`，并在 `from_elf` 中引入了 `load_bias` 计算，这是实现地址空间布局随机化（ASLR）或兼容 PIE 可执行文件的关键步骤，表明其设计目标远超参考项目。
* **代码溯源链接**:
  - 目标项目: `os/src/task/process_block.rs#exec`
  - 参考项目: `os/src/task/task.rs#exec`

### 5. 单方模块与降权项

*   **单方模块（强原创证据）**:
    *   **网络协议栈**: 目标项目 `os/src/net/mod.rs` 等文件实现了完整的网络协议栈，参考项目完全缺失。这是功能集上的根本性差异。
    *   **第三方库/公共依赖**: 目标项目集成了 `ext4-fs`、`ext4-fs-packer` 等第三方库，并进行了适配性开发，参考项目无此模块。
    *   **高级内核特性**: 目标项目独有 `cgroupfs`、`procfs`、`epoll`、`syscall_clone3`、`syscall_ptrace` 等现代 Linux 内核特性，这些大规模独立子系统是原创性的直接体现。

*   **降权项（不构成强同源证据）**:
    *   **跨板块弱信号**: 控方提出的 `syscall_wait4` (wait.rs) 与 `sys_fork` (process.rs) 等跨文件比对，已按指令降级处理。它们功能路径不同，调用图谱无实质重合。
    *   **POSIX/架构强制命名**: `getpid`、`sigprocmask`、`exec`、`set_kernel_trap_entry` 等函数名，以及 `Arc::clone`、`Vec::new` 等标准库调用，是遵循 POSIX 标准或 RISC-V 架构规范的必然结果，不能作为抄袭证据。
    *   **通用测试范式**: `block_device_test` 函数的逻辑是测试块设备读写的标准做法，其 AST 结构重合是功能需求决定的。

### 6. 保守结论

综合客观 AST 数据与深度析理，本案整体风险极低。2.09% 的 AST 相似度与 2.27% 的目标覆盖率，在代码查重领域属于典型的“背景噪声”级别，主要由操作系统领域的通用惯用语、POSIX 强制命名及标准库调用构成。控方提出的“铁证”，如 `getpid` 的闭环逻辑和 `exec` 的 ELF 加载入口，经技术剖析，均为领域必然实现或标准流程，不具备区分度。辩方提供的证据表明，目标项目在调度器（CFS/RT）、内存管理（ASLR支持）、文件系统（ext4/procfs）及网络协议栈等方面，实现了远超参考项目的复杂架构与独立功能集。因此，现有证据无法支持“实质性相似”或“代码抄袭”的指控，应认定为独立开发。

------------------------------

