# 🛡️ OS 内核赛自动化代码级架构审计报告

**审计时间**: `2026-07-02 03:34:00`
**参赛作品名称**: `2026-T2026102139911088-XX`
**年份**: `2026`
**学校**: `XX`
**队伍名称**: `T2026102139911088`
**比赛名字**: `OS-Competition`
**比赛子赛道**: `Kernel-Implementation`
**仓库地址**: `https://gitlab.eduxiji.net/educg-group-43501-3132633/T2026102139911088-2824`
**推测开发语言**: `Rust / C (由AST探针自动识别)`
**特征函数量**: `5003`

## ⚔️ 底层代码同源性对比 (AST Homology)
> 本部分由专门的代码审查智能体，对该项目与符合年份条件的历届最高相似度项目进行控制流与底层调用对比。新版报告会先给出确定性的全模块覆盖矩阵，再给出 Agent 的分模块解释，避免只围绕少量抽样函数展开。

### 🏆 Top 4 底层对标: vs `2025-KernalCraft-中山大学`（中山大学 / KernalCraft）

#### 对方作品信息

**对方年份**: `2025`
**对方学校**: `中山大学`
**对方队伍名称**: `KernalCraft`
**对方比赛名字**: `OS-Competition`
**对方比赛子赛道**: `Kernel-Implementation`
**对方仓库地址**: `https://gitlab.eduxiji.net/educg-group-36002-2710490/T202510558995240-2536`

> 双向覆盖率：共同 AST 指纹: 107；唯一 AST 指纹: 目标项目 4183，参考项目 1853；覆盖率: 目标项目 2.56%，参考项目 5.77%

## 全模块对比总览（确定性证据）

> 总体覆盖率：共同 AST 指纹: 107；唯一 AST 指纹: 目标项目 4183，参考项目 1853；覆盖率: 目标项目 2.56%，参考项目 5.77%

### 1. 模块覆盖矩阵
| 模块 | 目标函数数 | 参考函数数 | 共同唯一 AST 指纹 | Target 覆盖 | Base 覆盖 | 状态 |
| :--- | ---: | ---: | ---: | ---: | ---: | :--- |
| 进程管理 | 528 | 71 | 5 | 1.03% | 7.94% | 双方存在且有结构重合 |
| 任务调度/异步运行时 | 642 | 115 | 9 | 1.49% | 8.04% | 双方存在且有结构重合 |
| 内存管理 | 963 | 741 | 60 | 6.82% | 13.39% | 双方存在且有结构重合 |
| 中断/异常处理 | 27 | 128 | 1 | 4.0% | 1.0% | 双方存在且有结构重合 |
| 系统调用 | 1072 | 21 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |
| 文件系统 | 1315 | 147 | 5 | 0.49% | 3.91% | 双方存在且有结构重合 |
| 设备驱动 | 39 | 555 | 2 | 5.88% | 0.43% | 双方存在且有结构重合 |
| 网络协议栈 | 25 | 53 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |
| 文件系统/第三方库 | 213 | 340 | 2 | 1.06% | 0.76% | 双方存在且有结构重合 |
| 网络协议栈/第三方库 | 3 | 0 | 0 | 0.0% | 0.0% | 仅目标项目体现 |
| 第三方库 | 0 | 2 | 0 | 0.0% | 0.0% | 仅参考项目体现 |
| 通用模块/其他 | 176 | 669 | 5 | 3.07% | 1.89% | 双方存在且有结构重合 |

### 2. 分模块证据清单

#### 进程管理
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `os/src/syscall/condvar.rs#sys_condvar_wait` | `os/src/syscall/sync.rs#sys_condvar_wait` | 同板块同名 | c.as_ref, condvar.wait, current_process, drop, m.as_ref, process.borrow_mut ...(+6) | Arc::clone, condvar.wait_with_mutex, current_process, drop, process.inner_exclusive_access, process_inner.condvar_list[condvar_id].as_ref ...(+3) |
| `os/src/task/manager/pid_map.rs#remove_from_pid2process` | `os/src/task/manager.rs#remove_from_pid2process` | 同板块同名 | PID2PCB.lock, crate::println!, crate::task::unregister_pid_namespace_reaper_for_process, drop, log::warn!, map.len ...(+1) | PID2PCB.exclusive_access, map.remove, map.remove(&pid).is_none, panic! |
| `os/src/task/manager/pid_map.rs#insert_into_pid2process` | `os/src/task/manager.rs#insert_into_pid2process` | 同板块同名 | PID2PCB.lock, crate::println!, map.insert, map.len | PID2PCB.exclusive_access, PID2PCB.exclusive_access().insert |
| `os/src/task/processor.rs#current_process` | `os/src/task/processor.rs#current_process` | 同板块同名 | INITPROC.clone, current_task, current_task()
        .and_then, current_task()
        .and_then(|task| task.process.upgrade())
        .unwrap_or_else, log::warn!, task.process.upgrade | current_task, current_task().unwrap, current_task().unwrap().process.upgrade, current_task().unwrap().process.upgrade().unwrap |

#### 任务调度/异步运行时
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `os/src/arch/loongarch64/trap/handler.rs#trap_handler` | `os/src/hal/trap/mod.rs#trap_handler` | 同板块同名 | AtomicUsize::new, LAST_SLEEP_TIMER_LOG.load, LAST_SLEEP_TIMER_LOG.store, TRAP_HANDLER_COUNT.fetch_add, check_if_current_signals_error, check_timer ...(+40) | TrapContextImpl::decode_trap_type, TrapContextImpl::set_kernel_trap_entry, TrapContextImpl::trap_return, main_trap_handler |
| `os/src/task/manager/run_queue.rs#add` | `os/src/task/manager.rs#add` | 同板块同名 | FairGroupQueue::new, Some, current_fair_entity_for_group, current_time_ns_usize, fair_group_id_and_shares, fair_nice_weight ...(+26) | self.ready_queue.push_back |
| `os/src/task/signal.rs#check_error` | `os/src/task/signal.rs#check_error` | 同板块同名 | Some, self.contains | Some, self.contains |
| `os/src/syscall/thread.rs#sys_thread_create` | `os/src/syscall/thread.rs#sys_thread_create` | 同板块同名 | Arc::clone, Arc::new, Some, SyscallError::from, TaskControlBlock::try_new, TrapContext::app_init_context ...(+21) | Arc::clone, Arc::new, Some, TaskControlBlock::new, TrapContextImpl::app_init_ctx, add_task ...(+17) |

#### 内存管理
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `os/src/mm/memory_set.rs#from_existed_user_cow` | `os/src/mm/memory_set.rs#from_existed_user_cow` | 同板块同名 | (delta_cycles as u128).saturating_mul, COW_CLONE_DIAG_SEQ.fetch_add, MapArea::from_another, PageWalkCache::new, Self::new_bare, area.map_type ...(+39) | Self::new_bare, area.to_cow, log::info!, memory_set.areas.push, memory_set.map_trampoline, user_space.areas.iter_mut |
| `os/src/task/processor.rs#exit_current_and_run_next` | `os/src/task/mod.rs#exit_current_and_run_next` | 同板块同名 | Arc::new, Arc::ptr_eq, FilesStruct::new, MemorySet::new_bare, MmRef::new, TaskContext::new ...(+60) | Arc::downgrade, Arch::shutdown, INITPROC.inner_exclusive_access, Some, TaskContext::zero_init, Vec::<TaskUserRes>::new ...(+40) |
| `os/src/arch/riscv64/trap/handler.rs#trap_handler` | `virtio-drivers/examples/x86_64/src/trap.rs#trap_handler` | 同板块同名 | TRAP_HANDLER_COUNT.fetch_add, asm!, check_if_current_signals_error, check_timer, crate::fs::cgroup_maybe_block_current, crate::syscall::misc::check_current_rlimit_cpu ...(+25) | panic!, x86_64::registers::control::Cr2::read |
| `os/src/mm/memory_set.rs#from_elf` | `os/src/mm/memory_set.rs#from_elf` | 同板块同名 | (load_bias + (ph.virtual_addr() + ph.mem_size()) as usize).into, (load_bias + ph.virtual_addr() as usize).into, Err, MapArea::new, Ok, Self::new_bare ...(+39) | AuxHeader::new, MapArea::new, MmapManager::new, Self::new_bare, Some, VirtAddr ...(+13) |

#### 中断/异常处理
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `os/src/arch/loongarch64/mod.rs#enable_timer_interrupt` | `os/src/hal/instruction/loongarch.rs#enable_timer_interrupt` | 同板块同名 | asm!, enable_ipi_interrupt | crmd::set_ie, ecfg::set_lie, get_timer_freq, log::info!, tcfg::set_en, tcfg::set_init_val ...(+2) |
| `os/src/arch/riscv64/trap/handler.rs#set_kernel_trap_entry` | `os/src/hal/trap/loongarch.rs#set_kernel_trap_entry` | 同板块同名 | riscv::register::stvec::Stvec::new, riscv::register::stvec::write | eentry::set_eentry |
| `os/src/arch/riscv64/trap/handler.rs#set_user_trap_entry` | `os/src/hal/trap/riscv.rs#set_user_trap_entry` | 同板块同名 | riscv::register::stvec::Stvec::new, riscv::register::stvec::write | stvec::write |
| `os/src/arch/riscv64/trap/entry.rs#enable_timer_interrupt` | `os/src/hal/instruction/riscv.rs#enable_timer_interrupt` | 同板块同名 | sie::set_stimer | sie::set_stimer |

#### 系统调用
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `os/src/syscall/net/netdev.rs#learn_ipv4_neighbor_in_namespace_inner` | `os/src/syscall/net.rs#sys_socket` | 同模块上下文对齐/差异观察 | NET_STATE.lock, Some, dev.addrs
                    .iter, dev.addrs
                    .iter()
                    .filter, dev.addrs
                    .iter()
                    .filter(|entry| ipv4_same_prefix(entry.addr, dst, entry.prefix_len))
                    .map, dev.addrs
                    .iter()
                    .filter(|entry| ipv4_same_prefix(entry.addr, dst, entry.prefix_len))
                    .map(|entry| (idx, entry.prefix_len))
                    .max_by_key ...(+45) | create_tcp_socket, create_udp_socket |
| `os/src/syscall/net/wireguard.rs#apply_set_device` | `os/src/syscall/input.rs#sys_event_get` | 同模块上下文对齐/差异观察 | Err, Ok, Some, Vec::new, WIREGUARD_CONFIGS.lock, WIREGUARD_SESSIONS
                .lock ...(+38) | KEYBOARD_DEVICE.clone, MOUSE_DEVICE.clone, kb.is_empty, kb.read_event, mouse.is_empty, mouse.read_event |
| `os/src/syscall/net/socket.rs#materialize_rx_ring_frame` | `os/src/syscall/info.rs#fmt` | 同模块上下文对齐/差异观察 | Self::ring_slot_addr, Self::ring_slot_count, Self::ring_slot_count(ring).max, Self::ring_status_addr, Self::write_ring_block_v3, Self::write_ring_frame ...(+15) | writeln! |
| `os/src/syscall/net/wireguard.rs#build_device_msg` | `os/src/syscall/info.rs#sys_sysinfo` | 同模块上下文对齐/差异观察 | (dev.ifindex as u32).to_ne_bytes, Vec::new, allowed.is_empty, allowed_ip.family.to_ne_bytes, append_rtattr, build_genlmsg ...(+14) | Sysinfo::collect, current_user_token, translated_refmut |

#### 文件系统
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `os/src/fs/fanotify.rs#notify` | `virtio-drivers/src/transport/pci.rs#notify` | 同板块同名 | Some, inner.events.push_back, inner.marks.iter, inner.marks.iter_mut, inode.device_id, mark_event_bits ...(+4) | field!, field!(self.common_cfg, queue_select).write, field_shared!, field_shared!(self.common_cfg, queue_notify_off).read, self.notify_region.get, self.notify_region.get(index).unwrap ...(+3) |
| `os/src/fs/inode.rs#read_all` | `os/src/fs/inode.rs#read_all` | 同板块同名 | Self::flush_inner, Vec::new, ext4_lock, inner.inode.read_at, inner.inode.size, self.inner.lock ...(+1) | Vec::new, inner.inode.read_at, self.inner.exclusive_access, v.extend_from_slice |
| `os/src/fs/cgroupfs/file.rs#writable` | `os/src/fs/inode.rs#writable` | 同板块同名 | matches! | 内部闭环逻辑 |
| `os/src/syscall/filesystem/fd_utils.rs#open_file` | `os/src/fs/inode.rs#open_file` | 同板块同名 | Arc::new, FifoDuplexFile::new, Some, self.read_end.clone, self.write_end.clone | Arc::new, EXT4.ext4_file_open_at, OSInode::new, Some, flags.contains, flags.read_write |

#### 设备驱动
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `os/src/console/mod.rs#write_str` | `virtio-drivers/src/device/console.rs#write_str` | 同板块同名 | Ok, console_flush, console_putchar, crate::arch::UART_FIFO_DEPTH.saturating_sub, crate::arch::UART_FIFO_DEPTH.saturating_sub(2).max, crate::klog::append_str ...(+4) | error!, s.as_bytes, self.send_bytes, self.send_bytes(s.as_bytes()).map_err |
| `os/src/drivers/block/virtio_blk.rs#for_pci_ranges` | `virtio-drivers/examples/aarch64/src/main.rs#for_pci_ranges` | 同板块同名 | PciRangeType::from, matches!, memory_32_size.into, panic!, pci_node
                .property, pci_node
                .property("ranges")
                .expect ...(+12) | PciRangeType::from, assert_eq!, info!, matches!, memory_32_size.into, panic! ...(+5) |
| `os/src/drivers/block/virtio_blk.rs#share` | `virtio-drivers/examples/aarch64/src/hal.rs#share` | 同板块同名 | Box::into_raw, assert_ne!, buffer.as_ptr, buffer.len, core::ptr::copy_nonoverlapping, core::slice::from_raw_parts ...(+5) | buffer.as_ptr, virt_to_phys |
| `os/src/drivers/block/virtio_blk.rs#unshare` | `virtio-drivers/src/hal/fake.rs#unshare` | 同板块同名 | Box::from_raw, assert_ne!, buffer.as_ptr, buffer.len, core::ptr::slice_from_raw_parts_mut, core::slice::from_raw_parts ...(+3) | Box::from_raw, assert_ne!, buffer
                    .as_ptr, buffer
                    .as_ptr()
                    .cast::<u8>, buffer
                    .as_ptr()
                    .cast::<u8>()
                    .copy_from, buffer.len ...(+3) |

#### 网络协议栈
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `os/src/net/mod.rs#new_stack` | `lose-net-stack/src/packets/udp.rs#build_data` | 同模块上下文对齐/差异观察 | (ns_id as u64).wrapping_mul, Config::new, Interface::new, IpAddress::Ipv4, IpAddress::Ipv6, IpCidr::new ...(+10) | ((self.data_len + UDP_LEN + IP_LEN) as u16).to_be, ((self.data_len + UDP_LEN) as u16).to_be, ETH_RTYPE_IP.to_be, IP_PROTOCAL_UDP.to_be, UnsafeRefIter::new, check_sum ...(+17) |
| `os/src/net/mod.rs#udp_port_bound_in` | `lose-net-stack/src/packets/arp.rs#build_data` | 同模块上下文对齐/差异观察 | NET.lock, init_in, net.get_mut, sock.endpoint, stack.sockets.iter, stack.sockets.iter().any | ARP_HRD_ETHER.to_be, ETH_RTYPE_ARP.to_be, ETH_RTYPE_IP.to_be, UnsafeRefIter::new, data_ptr_iter.next_mut::<Arp>, data_ptr_iter.next_mut::<Eth> ...(+11) |
| `os/src/net/mod.rs#socket_uses_local_port` | `lose-net-stack/src/packets/tcp.rs#ack` | 同模块上下文对齐/差异观察 | sock
                    .local_endpoint, sock
                    .local_endpoint()
                    .is_some_and, sock.endpoint, sock.get_bound_endpoint | flags.contains, flags.remove, self.flags.contains |
| `os/src/net/mod.rs#poll_busy_with_sockets_mut_in` | `lose-net-stack/src/lib.rs#analysis_udp` | 同模块上下文对齐/差异观察 | NET.lock, drain_pending_veth_ip_deliveries, f, init_in, net.get_mut, net.get_mut(&ns_id).unwrap ...(+2) | IPv4::from_u32, MacAddress::new, Packet::UDP, data_ptr_iter.get_curr_arr, data_ptr_iter.next::<UDP>, ip_header.dst.to_be ...(+5) |

#### 文件系统/第三方库
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `ext4-fs/src/vfs.rs#dir_add_entry` | `ext4_rs/src/ext4_impls/dir.rs#dir_add_entry` | 同板块同名 | Arc::clone, Err, Ok, Self::map_logical_block, Self::map_logical_block(&extents, lblock as u32).ok_or, Self::map_logical_block(&extents, new_block_index).ok_or ...(+15) | Block::load, Ok, ext4block.sync_blk_to_disk, log::trace!, new_ext4block.sync_blk_to_disk, parent.inode.generation ...(+9) |
| `ext4-fs/src/vfs.rs#unlink` | `ext4_rs/src/ext4_impls/ext4.rs#unlink` | 同板块同名 | Err, Ok, child.free_all_blocks, child.inode_num, child.is_dir, child.ls ...(+16) | Ok, child.inode.is_dir, log::trace!, self.dir_remove_entry, self.ialloc_free_inode |
| `ext4-fs/src/vfs.rs#write_at` | `ext4_rs/src/ext4_impls/file.rs#write_at` | 同板块同名 | (buf.len() - written).min, Arc::clone, Err, Ok, Self::map_logical_block, Self::map_logical_block(&extents, lblock).ok_or ...(+18) | Block::load, Ok, block.sync_blk_to_disk, block.write_offset, drop, inode_ref
                .inode
                .set_size ...(+9) |
| `ext4-fs/src/vfs.rs#dir_remove_entry` | `ext4_rs/src/ext4_impls/dir.rs#dir_remove_entry` | 同板块同名 | Arc::clone, Err, Ok, Self::map_logical_block, cache.lock, drop ...(+9) | Block::load, Ext4DirEntry::default, Ext4DirSearchResult::new, Ok, ext4block.read_offset_as_mut, ext4block.sync_blk_to_disk ...(+6) |

#### 网络协议栈/第三方库
仅目标项目中观察到该模块代表函数，参考项目没有同模块可对齐证据：
- 目标项目：`os/src/syscall/net/netdev.rs#ipv4_from_smoltcp`，调用摘要：ip.as_bytes
- 目标项目：`ext4-fs/src/vfs.rs#is_socket`，调用摘要：inode.is_socket, self.read_disk_inode
- 目标项目：`ext4-fs/src/layout.rs#is_socket`，调用摘要：内部闭环逻辑

#### 第三方库
仅参考项目中观察到该模块代表函数，目标项目没有同模块可对齐证据：
- 参考项目：`virtio-drivers/src/transport/mmio.rs#vendor_id`，调用摘要：field_shared!, field_shared!(self.header, vendor_id).read
- 参考项目：`virtio-drivers-old/src/header.rs#vendor_id`，调用摘要：self.vendor_id.read

#### 通用模块/其他
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `os/src/log.rs#log` | `os/src/logging.rs#log` | 同板块同名同结构 | println!, record.level, record.metadata, self.enabled | println!, record.level, record.metadata, self.enabled |
| `os/src/console/mod.rs#print` | `os/src/console.rs#print` | 同板块同名 | CONSOLE_LOCK.lock, Stdout.write_fmt, Stdout.write_fmt(args).unwrap, disable_interrupts, restore_interrupts | Stdout.write_fmt, Stdout.write_fmt(args).unwrap |
| `os/src/arch/loongarch64/mod.rs#set_timer` | `os/src/hal/timer/loongarch64.rs#set_timer` | 同板块同名 | asm!, timer.max | LATimer::get_time, register::tcfg::set_en, register::tcfg::set_init_val, register::tcfg::set_periodic, register::ticlr::clear_timer_interrupt |
| `os/src/time.rs#set_next_trigger` | `os/src/timer.rs#set_next_trigger` | 同板块同名 | get_time_ns, get_time_ns().saturating_add, loongarch_program_clockevent_deadline_ns, riscv_program_clockevent_delta_ticks, riscv_tick_delta, tick_period_ns | TimerImpl::get_time, TimerImpl::set_timer |


好的，审计官。以下是基于提供的证据底座、控辩双方主张及审计指令形成的最终同源性定性仲裁报告。

---

### 1. 宏观判定
- **定性结论**: **独立开发，底层架构显著差异**
- **相似度**: 1.8%
- **共同 AST 指纹**: 107
- **唯一 AST 指纹**: 目标项目 4183，参考项目 1853
- **覆盖率**: 目标项目 2.56%，参考项目 5.77%

**判定依据**: 双方项目的整体 AST 覆盖率极低（2.56% / 5.77%），远未达到可认定“实质性相似”或“系统性复用”的阈值。参考项目较高的覆盖率（5.77%）源于其自身代码体量较小，少量公共惯用语即可占据较高比例，不能反向推定目标项目抄袭。证据底座中，控方所指控的“核心铁证”均为 Rust 语言标准用法或操作系统领域的通用语义实现，不具备独创性鉴别力。双方在进程管理、任务调度等核心板块的同步原语、内存管理模型及功能复杂度上存在根本性架构差异。

### 2. 全模块覆盖矩阵解读
| 模块 | 双方存在 | 共同指纹 | 目标覆盖 | 参考覆盖 | 证据强度 | 解读 |
| :--- | :---: | :---: | :---: | :---: | :---: | :--- |
| **进程管理** | 是 | 5 | 1.03% | 7.94% | **弱** | 覆盖率极低。共同指纹集中在 `get_task`、`getpid` 等 Rust 语言惯用法或 POSIX 语义必然的 getter 函数上。双方同步原语（`lock` vs `exclusive_access`）和功能集（ptrace, clone3 vs fork/exec）差异巨大。 |
| **任务调度/异步运行时** | 是 | 9 | 1.49% | 8.04% | **弱** | 共同指纹多为同名系统调用入口（如 `sys_thread_create`）或简单队列操作。目标项目实现了复杂的 CFS 公平调度（`FairGroupQueue`），参考项目为简单的 `ready_queue.push_back`，调度模型完全不同。 |
| **内存管理** | 是 | 60 | 6.82% | 13.39% | **中** | 覆盖率相对较高，共同指纹集中在 `from_elf`、`from_existed_user_cow` 等复杂函数。这反映了双方可能遵循了相同的教学内核（如 rCore）内存管理范式。但目标项目引入了 `PageWalkCache` 等独立优化，属于同一教学流派下的独立演进。 |
| **中断/异常处理** | 是 | 1 | 4.0% | 1.0% | **弱** | 共同指纹为架构相关的 `enable_timer_interrupt` 等函数，其实现由硬件规范决定，表达方式唯一。双方 Trap Handler 的内部逻辑（如目标项目的信号检查、cgroup 阻塞）完全不同。 |
| **系统调用** | 是 | 0 | 0.0% | 0.0% | **未形成** | 尽管双方都有大量系统调用实现，但 AST 层面未观察到任何共同结构指纹。目标项目实现了 Linux 兼容的 epoll、ptrace、clone3 等复杂调用，参考项目为简单的 `sys_write`、`sys_read` 等，功能集和复杂度不在一个量级。 |
| **文件系统** | 是 | 5 | 0.49% | 3.91% | **弱** | 共同指纹集中在 `read_all`、`open_file` 等通用文件操作。目标项目实现了 procfs、cgroupfs 等伪文件系统和 fanotify，参考项目主要依赖简单的 EXT4 封装，架构差异显著。 |
| **设备驱动** | 是 | 2 | 5.88% | 0.43% | **弱** | 共同指纹为 VirtIO 驱动的公共示例代码（`for_pci_ranges`, `share/unshare`）。这些是 VirtIO 驱动开发的标准模板，属于公共领域知识，不构成项目间的复用证据。 |
| **网络协议栈** | 是 | 0 | 0.0% | 0.0% | **未形成** | 双方网络栈实现完全不同。目标项目基于 smoltcp 构建了完整的 WireGuard、socket 快照等功能，参考项目使用简单的 lose-net-stack 用户态示例，无任何共同指纹。 |
| **第三方库/公共依赖** | 部分 | 2 | 1.06% | 0.76% | **弱** | 共同指纹来自双方均依赖的 `ext4-fs` / `ext4_rs` 第三方库，属于公共依赖的相同版本或 fork 关系，不能作为目标项目抄袭参考项目的证据。 |

### 3. 底层 AST 关键证据指纹表
| 所属板块 | 目标项目路径 | 参考项目路径 | 证据类型 | 技术说明 |
| :--- | :--- | :--- | :--- | :--- |
| 进程管理 | `os/src/task/process_block.rs#get_task` | `os/src/task/process.rs#get_task` | Rust 语言惯用法 | `self.tasks[tid].as_ref().unwrap().clone()` 是从 `Vec<Option<Arc<...>>>` 取元素的唯一标准写法。 |
| 进程管理 | `os/src/task/process_block.rs#get_user_token` | `os/src/task/process.rs#get_user_token` | 单字段访问必然 | `self.memory_set.token` 是获取页表 token 的唯一直接表达。 |
| 进程管理 | `os/src/task/process_block.rs#getpid` | `os/src/task/process.rs#getpid` | 语义闭环必然 | 返回自身 PID 的 getter 函数，逻辑必然是读取内部字段，无外部调用。 |
| 进程管理 | `os/src/task/manager/pid_map.rs#pid2process` | `os/src/task/manager.rs#pid2process` | 领域公共模式 | `map.get(&pid).map(...)` 是 HashMap/BTreeMap 查询的标准链式调用。双方锁机制不同（`lock` vs `exclusive_access`）。 |
| 任务调度 | `os/src/task/manager/run_queue.rs#add` | `os/src/task/manager.rs#add` | 同名不同构 | 虽同名，但目标项目实现了 CFS 调度逻辑，参考项目仅为 `push_back`，AST 图谱完全不同。 |
| 内存管理 | `os/src/mm/memory_set.rs#from_elf` | `os/src/mm/memory_set.rs#from_elf` | 教学内核公共范式 | 均遵循 rCore 风格的 ELF 加载流程，但目标项目增加了 `load_bias` 等独立处理。 |
| 内存管理 | `os/src/mm/memory_set.rs#from_existed_user_cow` | `os/src/mm/memory_set.rs#from_existed_user_cow` | 教学内核公共范式 | 均实现了 COW fork 的逻辑，但目标项目增加了诊断计数和 `PageWalkCache` 优化。 |
| 文件系统 | `os/src/fs/inode.rs#read_all` | `os/src/fs/inode.rs#read_all` | 通用文件操作 | 循环调用 `read_at` 读取全部数据的标准模式。 |
| 设备驱动 | `os/src/drivers/block/virtio_blk.rs#share` | `virtio-drivers/examples/aarch64/src/hal.rs#share` | 公共示例/驱动模板 | VirtIO 驱动中用于物理内存共享的标准实现模板。 |
| 第三方库 | `ext4-fs/src/vfs.rs#dir_add_entry` | `ext4_rs/src/ext4_impls/dir.rs#dir_add_entry` | 公共依赖 | 双方均依赖的 ext4 第三方库，其内部逻辑的同源性与参赛作品间的抄袭无关。 |

### 4. 核心代码调用流对比剖析
*   **【进程管理 / 条件变量等待系统调用】**
    *   **底层技术析理**: 双方均实现了 `sys_condvar_wait`，但底层同步与对象管理模型截然不同。目标项目采用 `borrow_mut` 进行内部可变性借用，并通过 `and_then` 进行 Option 链式处理；参考项目则使用 `inner_exclusive_access` 进行外部独占访问，并通过 `as_ref().unwrap()` 强制解包。这反映了 `RefCell` vs `UPSafeCell` 两种不同的 Rust 内部可变性实现策略。目标项目的条件变量等待逻辑直接调用 `condvar.wait`，而参考项目则调用封装了互斥锁操作的 `condvar.wait_with_mutex`，显示其在抽象层次上的差异。
    *   **代码溯源链接**:
        - 目标项目: `os/src/syscall/condvar.rs#sys_condvar_wait`
        - 参考项目: `os/src/syscall/sync.rs#sys_condvar_wait`

*   **【任务调度 / 任务加入就绪队列】**
    *   **底层技术析理**: 双方均有将任务加入调度器就绪队列的 `add` 函数，但内部实现天差地别。目标项目 `os/src/task/manager/run_queue.rs#add` 调用了 `FairGroupQueue::new`、`current_fair_entity_for_group` 等复杂逻辑，表明其实现了 Linux CFS 调度器。参考项目 `os/src/task/manager.rs#add` 仅执行 `self.ready_queue.push_back`，是一个简单的 FIFO 就绪队列。两者在调度算法和数据结构上无任何共同点。
    *   **代码溯源链接**:
        - 目标项目: `os/src/task/manager/run_queue.rs#add`
        - 参考项目: `os/src/task/manager.rs#add`

*   **【内存管理 / 进程退出与资源回收】**
    *   **底层技术析理**: `exit_current_and_run_next` 是内核核心流程。目标项目在回收过程中涉及 `Arc::ptr_eq` 引用诊断、`FilesStruct::new`、`cgroup_exit_process` 等大量复杂子系统交互。参考项目则调用 `INITPROC`、`TaskContext::zero_init` 等教学内核典型结构。目标项目的资源回收粒度更细，与 cgroup、诊断等子系统深度耦合，远超参考项目的简洁实现。
    *   **代码溯源链接**:
        - 目标项目: `os/src/task/processor.rs#exit_current_and_run_next`
        - 参考项目: `os/src/task/mod.rs#exit_current_and_run_next`

### 5. 单方模块与降权项
*   **单方模块**:
    *   **目标项目独有**: `网络协议栈/第三方库` (3个函数)。目标项目集成了更复杂的网络功能，如 `ipv4_from_smoltcp`，参考项目无此模块。
    *   **参考项目独有**: `第三方库` (2个函数)。参考项目包含独立的 `virtio-drivers` 相关代码，目标项目无此模块。
    *   **分析**: 单方模块的存在进一步证明两个项目具有不同的功能目标和依赖栈，是独立开发的强信号。
*   **降权项说明**:
    *   **跨板块硬凑**: 控方将参考项目的 `fdt/src/node.rs`（设备树解析）、`virtio-drivers/...`（设备驱动示例）、`lose-net-stack/...`（用户态网络栈）等证据强行归入“进程管理”板块进行比对，违反了“同板块对齐”原则。这些证据已被降权，不构成同板块主证据。
    *   **语言级与公共接口**: `Arc::clone`、`Vec::new`、`println!`、`Some`、`Ok`、`Err` 等 Rust 标准库或核心语言项，在任何 Rust 项目中都会大量出现，不具备项目间抄袭的鉴别力，已从强证据中剔除。

### 6. 保守结论
经 AST 指纹比对与架构剖析，目标项目 `[2026-T2026102139911088-XX]` 与参考项目 `[2025-KernalCraft-中山大学]` 的客观相似度仅为 1.8%，处于极低水平。双方在进程管理、任务调度、内存管理等核心板块虽存在少量共同指纹，但均属于 Rust 语言惯用法、POSIX/Linux 语义必然或教学内核公共范式，不具备独创性。双方在同步原语（`Mutex` vs `UPSafeCell`）、调度算法（CFS vs FIFO）、系统调用复杂度（Linux 兼容 vs 教学子集）及第三方依赖等关键架构层面存在根本性差异。目标项目拥有大量参考项目不具备的独立高级功能（如 ptrace, cgroup, WireGuard）。综合判定，**目标项目不存在对参考项目的系统性代码复用或洗稿行为，整体风险极低，应认定为独立开发作品。**

------------------------------

