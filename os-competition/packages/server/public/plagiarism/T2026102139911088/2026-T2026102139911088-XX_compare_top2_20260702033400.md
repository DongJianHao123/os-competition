# 🛡️ OS 内核赛自动化代码级架构审计报告

**审计时间**: `2026-07-02 03:34:00`
**参赛作品名称**: `2026-T2026102139911088-XX`
**年份**: `2026`
**学校**: `XX`
**队伍名称**: `T2026102139911088`
**比赛名字**: `OS-Competition`
**比赛子赛道**: `Kernel-Implementation`
**仓库地址**: `https://gitlab.eduxiji.net/educg-group-43501-3132633/T2026102139911088-2824`
**推测开发语言**: `Rust / C (由AST探针自动识别)`
**特征函数量**: `5003`

## ⚔️ 底层代码同源性对比 (AST Homology)
> 本部分由专门的代码审查智能体，对该项目与符合年份条件的历届最高相似度项目进行控制流与底层调用对比。新版报告会先给出确定性的全模块覆盖矩阵，再给出 Agent 的分模块解释，避免只围绕少量抽样函数展开。

### 🏆 Top 2 底层对标: vs `2024-chaos-北京科技大学`（北京科技大学 / chaos）

#### 对方作品信息

**对方年份**: `2024`
**对方学校**: `北京科技大学`
**对方队伍名称**: `chaos`
**对方比赛名字**: `OS-Competition`
**对方比赛子赛道**: `Kernel-Implementation`
**对方仓库地址**: `https://gitlab.eduxiji.net/educg-group-26010-2376550/oskernel2024-chaos`

> 双向覆盖率：共同 AST 指纹: 91；唯一 AST 指纹: 目标项目 4183，参考项目 798；覆盖率: 目标项目 2.18%，参考项目 11.4%

## 全模块对比总览（确定性证据）

> 总体覆盖率：共同 AST 指纹: 91；唯一 AST 指纹: 目标项目 4183，参考项目 798；覆盖率: 目标项目 2.18%，参考项目 11.4%

### 1. 模块覆盖矩阵
| 模块 | 目标函数数 | 参考函数数 | 共同唯一 AST 指纹 | Target 覆盖 | Base 覆盖 | 状态 |
| :--- | ---: | ---: | ---: | ---: | ---: | :--- |
| 进程管理 | 528 | 45 | 1 | 0.21% | 2.27% | 双方存在且有结构重合 |
| 任务调度/异步运行时 | 642 | 76 | 6 | 0.99% | 8.57% | 双方存在且有结构重合 |
| 内存管理 | 963 | 178 | 47 | 5.34% | 28.14% | 双方存在且有结构重合 |
| 中断/异常处理 | 27 | 5 | 2 | 8.0% | 50.0% | 双方存在且有结构重合 |
| 系统调用 | 1072 | 67 | 5 | 0.6% | 12.5% | 双方存在且有结构重合 |
| 文件系统 | 1315 | 152 | 4 | 0.39% | 3.17% | 双方存在且有结构重合 |
| 设备驱动 | 39 | 19 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |
| 网络协议栈 | 25 | 0 | 0 | 0.0% | 0.0% | 仅目标项目体现 |
| 文件系统/第三方库 | 213 | 270 | 2 | 1.06% | 0.88% | 双方存在且有结构重合 |
| 网络协议栈/第三方库 | 3 | 0 | 0 | 0.0% | 0.0% | 仅目标项目体现 |
| 第三方库 | 0 | 50 | 0 | 0.0% | 0.0% | 仅参考项目体现 |
| 通用模块/其他 | 176 | 75 | 7 | 4.29% | 10.29% | 双方存在且有结构重合 |

### 2. 分模块证据清单

#### 进程管理
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `os/src/task/manager/pid_map.rs#remove_from_pid2process` | `os/src/task/manager.rs#remove_from_pid2process` | 同板块同名 | PID2PCB.lock, crate::println!, crate::task::unregister_pid_namespace_reaper_for_process, drop, log::warn!, map.len ...(+1) | PID2PCB.exclusive_access, file!, line!, map.remove, map.remove(&pid).is_none, panic! |
| `os/src/task/manager/pid_map.rs#insert_into_pid2process` | `os/src/task/manager.rs#insert_into_pid2process` | 同板块同名 | PID2PCB.lock, crate::println!, map.insert, map.len | PID2PCB.exclusive_access, PID2PCB.exclusive_access(file!(), line!()).insert, file!, line! |
| `os/src/task/processor.rs#take_current_task` | `os/src/task/processor.rs#take_current_task` | 同板块同名 | drop, local_processor, local_processor().lock, processor.take_current_task | PROCESSOR.exclusive_access, PROCESSOR.exclusive_access(file!(), line!()).take_current, file!, line! |
| `os/src/task/processor.rs#current_task` | `os/src/task/processor.rs#current_task` | 同板块同名 | drop, local_processor, local_processor().lock, processor.current | PROCESSOR.exclusive_access, PROCESSOR.exclusive_access(file!(), line!()).current, file!, line! |

#### 任务调度/异步运行时
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `os/src/task/manager/run_queue.rs#add` | `os/src/task/manager.rs#add` | 同板块同名 | FairGroupQueue::new, Some, current_fair_entity_for_group, current_time_ns_usize, fair_group_id_and_shares, fair_nice_weight ...(+26) | self.ready_queue.push_back |
| `os/src/task/signal.rs#check_error` | `os/src/task/signal.rs#check_error` | 同板块同名 | Some, self.contains | Some, self.contains |
| `os/src/task/manager/run_queue.rs#fetch` | `os/src/task/manager.rs#fetch` | 同板块同名 | Self::pop_ready_fair_candidate, Self::pop_ready_rt_candidate, Some, log::debug!, picked.is_none, rq.rt_queues.iter_mut ...(+7) | self.ready_queue.is_empty, self.ready_queue.pop_front |
| `os/src/task/manager.rs#add_task` | `os/src/task/manager.rs#add_task` | 同板块同名 | Arc::clone, Some, arch::send_ipi, crate::task::processor::current_task, crate::task::processor::hart_id, crate::task::processor::request_reschedule_for_wakeup ...(+4) | TASK_MANAGER.exclusive_access, TASK_MANAGER.exclusive_access(file!(), line!()).add, file!, line! |

#### 内存管理
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `os/src/task/processor.rs#exit_current_and_run_next` | `os/src/task/mod.rs#exit_current_and_run_next` | 同板块同名 | Arc::new, Arc::ptr_eq, FilesStruct::new, MemorySet::new_bare, MmRef::new, TaskContext::new ...(+60) | Arc::clone, Arc::downgrade, INITPROC.inner_exclusive_access, Some, TaskContext::zero_init, child.clone ...(+25) |
| `os/src/arch/riscv64/trap/handler.rs#trap_return` | `os/src/trap/mod.rs#trap_return` | 同板块同名 | FIRST_TRAP_RETURN_LOGGED.swap, PageTable::from_token, TRAP_RETURN_COUNT.fetch_add, TRAP_RETURN_COUNT.load, VirtAddr::from, VirtAddr::from(TRAMPOLINE).floor ...(+31) | asm!, current_task, current_task()
        .unwrap, current_task()
        .unwrap()
        .inner_exclusive_access, current_task()
        .unwrap()
        .inner_exclusive_access(file!(), line!())
        .user_clock_time_start, current_trap_cx_user_va ...(+6) |
| `os/src/arch/riscv64/trap/handler.rs#trap_handler` | `os/src/trap/mod.rs#trap_handler` | 同板块同名 | TRAP_HANDLER_COUNT.fetch_add, asm!, check_if_current_signals_error, check_timer, crate::fs::cgroup_maybe_block_current, crate::syscall::misc::check_current_rlimit_cpu ...(+25) | check_signals_of_current, check_timer, current_add_signal, current_task, current_task()
                .unwrap, current_task()
                .unwrap()
                .inner_exclusive_access ...(+24) |
| `os/src/mm/memory_set.rs#from_elf` | `os/src/mm/memory_set.rs#from_elf` | 同板块同名 | (load_bias + (ph.virtual_addr() + ph.mem_size()) as usize).into, (load_bias + ph.virtual_addr() as usize).into, Err, MapArea::new, Ok, Self::new_bare ...(+39) | ((ph.virtual_addr() + ph.mem_size()) as usize).into, (ph.virtual_addr() as usize).into, AuxHeader::new, MapArea::new, Self::new_process, Some ...(+29) |

#### 中断/异常处理
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `os/src/arch/loongarch64/mod.rs#enable_timer_interrupt` | `os/src/trap/mod.rs#enable_timer_interrupt` | 同板块同名 | asm!, enable_ipi_interrupt | sie::set_stimer |
| `os/src/arch/riscv64/trap/handler.rs#set_kernel_trap_entry` | `os/src/trap/mod.rs#set_kernel_trap_entry` | 同板块同名 | riscv::register::stvec::Stvec::new, riscv::register::stvec::write | stvec::write |
| `os/src/arch/riscv64/trap/handler.rs#set_user_trap_entry` | `os/src/trap/mod.rs#set_user_trap_entry` | 同板块同名 | riscv::register::stvec::Stvec::new, riscv::register::stvec::write | stvec::write |
| `os/src/arch/loongarch64/trap/handler.rs#init_trap` | `os/src/trap/mod.rs#init` | 同板块同结构 | set_kernel_trap_entry | set_kernel_trap_entry |

#### 系统调用
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `user/src/syscall/mod.rs#sleep` | `user/src/lib.rs#sleep` | 同板块同名 | syscall | sys_sleep |
| `user/src/syscall/mod.rs#exec` | `user/src/lib.rs#exec` | 同板块同名 | args_addr.as_ptr, args_addr.is_empty, core::ptr::null, path.as_ptr, syscall | sys_exec |
| `user/src/syscall/mod.rs#syscall` | `user/src/syscall.rs#syscall` | 同板块同名 | asm! | asm! |
| `user/src/syscall/mod.rs#waitpid` | `user/src/lib.rs#waitpid` | 同板块同名 | syscall | sys_waitpid, yield_ |

#### 文件系统
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `os/src/fs/inode.rs#read_all` | `os/src/fs/inode.rs#read_all` | 同板块同名 | Self::flush_inner, Vec::new, ext4_lock, inner.inode.read_at, inner.inode.size, self.inner.lock ...(+1) | Vec::new, self.read_at, trace!, v.extend_from_slice |
| `os/src/fs/cgroupfs/registry.rs#mount` | `os/src/fs/fs.rs#mount` | 同板块同名 | CgroupMountState::new, String::from, self.hierarchies
            .entry, self.hierarchies
            .entry(spec.hierarchy_key().clone())
            .or_insert_with, self.mounts
            .insert, self.mounts.contains_key ...(+4) | Path::new, self.mounted_fs.insert |
| `os/src/fs/pipe.rs#make_pipe` | `os/src/fs/pipe.rs#make_pipe` | 同板块同名 | Arc::new, Mutex::new, Pipe::read_end_with_buffer, Pipe::write_end_with_buffer, PipeRingBuffer::new, buffer.clone ...(+3) | Arc::new, Pipe::read_end_with_buffer, Pipe::write_end_with_buffer, PipeRingBuffer::new, UPSafeCell::new, buffer
        .exclusive_access ...(+5) |
| `os/src/fs/cgroupfs/file.rs#writable` | `os/src/fs/pipe.rs#writable` | 同板块同名 | matches! | 内部闭环逻辑 |

#### 设备驱动
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `os/src/drivers/block/virtio_blk.rs#unshare` | `os/src/drivers/block/virtio_blk.rs#unshare` | 同板块同名 | Box::from_raw, assert_ne!, buffer.as_ptr, buffer.len, core::ptr::slice_from_raw_parts_mut, core::slice::from_raw_parts ...(+3) | 内部闭环逻辑 |
| `os/src/drivers/block/mod.rs#block_device_test` | `os/src/drivers/block/mod.rs#block_device_test` | 同板块同名 | BLOCK_DEVICE.clone, assert_eq!, block_device.read_block, block_device.write_block, println!, write_buffer.iter_mut | BLOCK_DEVICE.clone, assert_eq!, println!, write_buffer.iter_mut |
| `os/src/drivers/block/virtio_blk.rs#mmio_phys_to_virt` | `os/src/drivers/block/virtio_blk.rs#mmio_phys_to_virt` | 同板块同名 | NonNull::new, NonNull::new(phys_to_virt(paddr) as *mut u8).unwrap, phys_to_virt | KernelAddr::from, NonNull::new_unchecked, PhysAddr::from |
| `os/src/drivers/block/virtio_blk.rs#read_block` | `os/src/drivers/block/virtio_blk.rs#read_block` | 同板块同名 | self.read_blocks | error!, panic!, res.is_err, res.unwrap, self.0.lock, self.0.lock().read_blocks ...(+1) |

#### 网络协议栈
仅目标项目中观察到该模块代表函数，参考项目没有同模块可对齐证据：
- 目标项目：`os/src/net/mod.rs#new_stack`，调用摘要：(ns_id as u64).wrapping_mul, Config::new, Interface::new, IpAddress::Ipv4, IpAddress::Ipv6, IpCidr::new ...(+10)
- 目标项目：`os/src/net/mod.rs#udp_port_bound_in`，调用摘要：NET.lock, init_in, net.get_mut, sock.endpoint, stack.sockets.iter, stack.sockets.iter().any
- 目标项目：`os/src/net/mod.rs#socket_uses_local_port`，调用摘要：sock
                    .local_endpoint, sock
                    .local_endpoint()
                    .is_some_and, sock.endpoint, sock.get_bound_endpoint
- 目标项目：`os/src/net/mod.rs#poll_busy_with_sockets_mut_in`，调用摘要：NET.lock, drain_pending_veth_ip_deliveries, f, init_in, net.get_mut, net.get_mut(&ns_id).unwrap ...(+2)

#### 文件系统/第三方库
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `ext4-fs/src/vfs.rs#unlink` | `os/src/fs/ext4/inode.rs#unlink` | 同板块同名 | Err, Ok, child.free_all_blocks, child.inode_num, child.is_dir, child.ls ...(+16) | self.fs.ext4.ext4_file_remove, self.fs.ext4.ext4_file_remove(self.ino, name).is_ok |
| `ext4-fs/src/vfs.rs#write_at` | `os/src/fs/ext4/inode.rs#write_at` | 同板块同名 | (buf.len() - written).min, Arc::clone, Err, Ok, Self::map_logical_block, Self::map_logical_block(&extents, lblock).ok_or ...(+18) | Arc::downgrade, Ext4File::new, Ext4InodeRef::get_inode_ref, buf.len, inode_ref.inner.inode.inode_get_size, self.fs.ext4.ext4_file_write |
| `ext4-fs/src/vfs.rs#read_at` | `os/src/fs/ext4/inode.rs#read_at` | 同板块同名 | block_data.len, buf.len, buf.len().min, buf[..len].copy_from_slice, buf[..read_len].fill, data.copy_from_slice ...(+10) | Ext4File::new, buf.len, self
            .fs
            .ext4
            .ext4_file_read |
| `ext4-fs/src/vfs.rs#rename` | `os/src/fs/ext4/inode.rs#rename` | 同板块同名 | Err, Ok, String::from, child.inode_num, child.is_dir, child.is_file ...(+12) | todo! |

#### 网络协议栈/第三方库
仅目标项目中观察到该模块代表函数，参考项目没有同模块可对齐证据：
- 目标项目：`os/src/syscall/net/netdev.rs#ipv4_from_smoltcp`，调用摘要：ip.as_bytes
- 目标项目：`ext4-fs/src/vfs.rs#is_socket`，调用摘要：inode.is_socket, self.read_disk_inode
- 目标项目：`ext4-fs/src/layout.rs#is_socket`，调用摘要：内部闭环逻辑

#### 第三方库
仅参考项目中观察到该模块代表函数，目标项目没有同模块可对齐证据：
- 参考项目：`os/libs/visionfive2-sd/src/lib.rs#send_cmd`，调用摘要：RawInterrupt::from, RawInterruptStatusReg::from, S::sleep_ms_until, Some, arg.into, assert! ...(+21)
- 参考项目：`os/libs/visionfive2-sd/src/lib.rs#init_sdcard`，调用摘要：CardTypeReg::from, CardTypeReg::from(0).with_card_width4_1, CmdArg::new, CmdReg::from, ControlReg::from, RawInterruptStatusReg::from ...(+18)
- 参考项目：`os/libs/visionfive2-sd/src/register.rs#from`，调用摘要：CmdReg::with_data, CmdReg::with_data(0, value.into()).with_transfer_dir, CmdReg::with_no_data, CmdReg::with_no_data(0, value.into())
                    .with_check_response_crc, CmdReg::with_no_data(0, value.into())
                    .with_check_response_crc(false)
                    .with_response_length, CmdReg::with_no_data(0, value.into()).with_check_response_crc ...(+3)
- 参考项目：`os/libs/visionfive2-sd/src/lib.rs#reset_clock`，调用摘要：ClockDividerReg::new, ClockDividerReg::new().with_clk_divider0, ClockEnableReg::from, CmdArg::new, CmdReg::from, CmdReg::from(0)
        .with_start_cmd ...(+7)

#### 通用模块/其他
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `os/src/sbi/mod.rs#sbi_call` | `os/src/sbi.rs#sbi_call` | 同板块同名 | asm! | asm! |
| `os/src/console/mod.rs#print` | `os/src/console.rs#print` | 同板块同名 | CONSOLE_LOCK.lock, Stdout.write_fmt, Stdout.write_fmt(args).unwrap, disable_interrupts, restore_interrupts | Stdout.write_fmt, Stdout.write_fmt(args).unwrap |
| `os/src/arch/loongarch64/mod.rs#set_timer` | `os/src/sbi.rs#set_timer` | 同板块同名 | asm!, timer.max | sbi_call |
| `os/src/time.rs#set_next_trigger` | `os/src/timer.rs#set_next_trigger` | 同板块同名 | get_time_ns, get_time_ns().saturating_add, loongarch_program_clockevent_deadline_ns, riscv_program_clockevent_delta_ticks, riscv_tick_delta, tick_period_ns | get_time, set_timer |


#### 1. 宏观判定
- **定性结论**: 独立开发 (相似度: 1.86%，共同 AST 指纹: 91；唯一 AST 指纹: 目标项目 4183，参考项目 798；覆盖率: 目标项目 2.18%，参考项目 11.4%)

#### 2. 全模块覆盖矩阵解读
- **进程管理**: 双方均存在。共同 AST 指纹仅 1 个，目标覆盖率 0.21%，参考覆盖率 2.27%。极低的覆盖率表明双方在进程生命周期管理、调度、信号处理等核心逻辑上无实质性结构复用。证据强度：**弱**。
- **任务调度/异步运行时**: 双方均存在。共同 AST 指纹 6 个，目标覆盖率 0.99%，参考覆盖率 8.57%。目标项目实现了复杂的 CFS/RT 混合调度，参考项目为简单的 FIFO 调度，架构差异巨大。证据强度：**弱**。
- **内存管理**: 双方均存在。共同 AST 指纹 47 个，目标覆盖率 5.34%，参考覆盖率 28.14%。这是覆盖率最高的模块，但目标项目实现了完整的页表隔离、CoW、共享内存等，参考项目为基本的内存映射。证据强度：**中**。
- **中断/异常处理**: 双方均存在。共同 AST 指纹 2 个，目标覆盖率 8.0%，参考覆盖率 50.0%。覆盖率虽高，但证据集中在架构相关的 `set_kernel_trap_entry` 等函数，属于硬件规范决定的通用逻辑。证据强度：**弱**。
- **系统调用**: 双方均存在。共同 AST 指纹 5 个，目标覆盖率 0.6%，参考覆盖率 12.5%。证据集中在用户库的 `syscall` 封装函数，其内联汇编结构由 RISC-V ABI 决定，不构成独创性复用。证据强度：**弱**。
- **文件系统**: 双方均存在。共同 AST 指纹 4 个，目标覆盖率 0.39%，参考覆盖率 3.17%。目标项目实现了 ext4、procfs、cgroupfs 等复杂文件系统，参考项目仅为基本 inode 操作。证据强度：**弱**。
- **设备驱动**: 双方均存在。共同 AST 指纹 0 个，覆盖率均为 0.0%。双方虽都有 virtio-blk 驱动，但 AST 层面无任何共同指纹，实现完全独立。证据强度：**未形成**。
- **网络协议栈**: 仅目标项目体现。参考项目无此模块，无法形成同源证据。证据强度：**未形成**。
- **第三方库/公共依赖**: 双方均存在。共同 AST 指纹 2 个，目标覆盖率 1.06%，参考覆盖率 0.88%。证据集中在 ext4-fs 库，但目标项目使用的是独立维护的 `ext4-fs` crate，参考项目为内联实现，调用路径完全不同。证据强度：**弱**。

#### 3. 底层 AST 关键证据指纹表
| 所属板块 | 目标项目路径 | 参考项目路径 | 证据类型 | 技术说明 |
| :--- | :--- | :--- | :--- | :--- |
| 内存管理 | os/src/mm/memory_set.rs#from_elf | os/src/mm/memory_set.rs#from_elf | 同板块同名 | 双方均实现了 ELF 加载逻辑，但目标项目使用 `load_bias` 进行 PIE 支持，参考项目无此概念。 |
| 内存管理 | os/src/task/processor.rs#exit_current_and_run_next | os/src/task/mod.rs#exit_current_and_run_next | 同板块同名 | 进程退出与资源回收。目标项目涉及 `Arc`、`MemorySet`、`FilesStruct` 的复杂析构，参考项目为简单的 `Arc::downgrade` 和 `TaskContext` 重置。 |
| 中断/异常处理 | os/src/arch/riscv64/trap/handler.rs#trap_handler | os/src/trap/mod.rs#trap_handler | 同板块同名 | Trap 处理入口。目标项目集成了信号检查、cgroup 阻塞、CPU rlimit 等，参考项目仅处理信号和时钟。 |
| 中断/异常处理 | os/src/arch/riscv64/trap/handler.rs#trap_return | os/src/trap/mod.rs#trap_return | 同板块同名 | Trap 返回路径。目标项目包含 `PageTable::from_token` 和复杂的诊断计数器，参考项目直接操作 `current_trap_cx_user_va`。 |
| 系统调用 | user/src/syscall/mod.rs#syscall | user/src/syscall.rs#syscall | 同板块同名 | 用户态 `syscall` 封装函数，均使用 `asm!` 宏执行 `ecall` 指令，这是 RISC-V 架构的强制要求。 |
| 文件系统 | os/src/fs/pipe.rs#make_pipe | os/src/fs/pipe.rs#make_pipe | 同板块同名 | 管道创建。目标项目使用 `Mutex`，参考项目使用 `UPSafeCell`，同步原语选择不同。 |
| 第三方库 | ext4-fs/src/vfs.rs#read_at | os/src/fs/ext4/inode.rs#read_at | 同板块同名 | ext4 读取。目标项目调用独立的 `ext4-fs` crate 接口，参考项目调用内联的 `self.fs.ext4.ext4_file_read`。 |

#### 4. 核心代码调用流对比剖析
* **【进程管理/处理器上下文获取】**
* **底层技术析理**: 目标项目采用 per-HART 的 `local_processor()` 设计，通过 `lock()` 获取本地处理器实例后再访问 `processor.current`，体现了多核 SMP 架构下的并发隔离思想。参考项目则使用全局单例 `PROCESSOR.exclusive_access()`，是典型的单核或大锁设计。两者在处理器抽象层次和同步策略上存在根本性异构。
* **代码溯源链接**:
  - 目标项目: `os/src/task/processor.rs#current_task`
  - 参考项目: `os/src/task/processor.rs#current_task`

* **【内存管理/ELF加载】**
* **底层技术析理**: 目标项目的 `from_elf` 函数引入了 `load_bias` 概念，用于计算 PIE (Position-Independent Executable) 的加载基址，并涉及 `MapArea::new` 等复杂映射逻辑。参考项目的实现则直接使用 `ph.virtual_addr()` 作为映射地址，不支持 PIE。两者在内存模型和可执行文件加载策略上存在显著差异。
* **代码溯源链接**:
  - 目标项目: `os/src/mm/memory_set.rs#from_elf`
  - 参考项目: `os/src/mm/memory_set.rs#from_elf`

* **【中断/异常处理/Trap入口】**
* **底层技术析理**: 目标项目的 `trap_handler` 在入口处集成了 `check_if_current_signals_error`、`cgroup_maybe_block_current`、`check_current_rlimit_cpu` 等高级内核特性，调用流复杂。参考项目的 `trap_handler` 则直接调用 `check_signals_of_current` 和 `check_timer`，逻辑简洁。目标项目的异常处理路径深度耦合了进程控制组和资源限制，远超参考项目的实现范畴。
* **代码溯源链接**:
  - 目标项目: `os/src/arch/riscv64/trap/handler.rs#trap_handler`
  - 参考项目: `os/src/trap/mod.rs#trap_handler`

#### 5. 单方模块与降权项
- **网络协议栈**: 仅目标项目拥有完整的网络协议栈实现（`os/src/net/mod.rs`），参考项目无此模块。这是目标项目独立开发的显著标志。
- **高级进程管理特性**: 目标项目独有的 `syscall_ptrace`、`syscall_clone3`、`process_posix_timers`、`epoll_wait_common`、`cgroup_attach_process_to_target` 等函数，体现了远超参考项目的功能复杂度，无法构成同源证据。
- **第三方库**: 参考项目独有的 `visionfive2-sd` 库是特定硬件（昉·星光2）的 SD 卡驱动，与目标项目无关。目标项目使用的 `ext4-fs` 是独立的 Rust crate，与参考项目内联的 ext4 实现调用路径完全不同，属于对公共标准的独立实现。
- **通用惯用法**: 控方证据中大量出现的 `Arc::clone`、`Vec::new`、`String::from`、`BTreeMap::get`、`asm!` 等均为 Rust 语言标准库或领域通用宏，任何 Rust 内核都会使用，不能作为抄袭指纹。

#### 6. 保守结论
本次审计的客观 AST 相似度仅为 1.86%，目标项目覆盖率 2.18%，参考项目覆盖率 11.4%，数据层面已明确指向独立开发。控方提出的“底层骨架复用”指控，其证据高度依赖 `BTreeMap`、`Arc`、`asm!` 等语言级或领域通用惯用法，缺乏区分度。双方在进程管理、内存管理、中断处理等核心子系统上，于抽象层次、同步策略、内存模型和功能复杂度方面均存在根本性架构差异。目标项目实现的网络协议栈、cgroup、epoll 等高级特性，是参考项目完全不具备的独立工作。综合判定，目标项目不存在对参考项目的违规代码复用，原创性成立。

------------------------------

