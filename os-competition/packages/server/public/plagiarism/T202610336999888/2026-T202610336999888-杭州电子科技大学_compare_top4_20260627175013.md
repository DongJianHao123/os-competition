# 🛡️ OS 内核赛自动化代码级架构审计报告

**审计时间**: `2026-06-27 17:50:13`
**参赛作品名称**: `2026-T202610336999888-杭州电子科技大学`
**年份**: `2026`
**学校**: `杭州电子科技大学`
**队伍名称**: `T202610336999888`
**比赛名字**: `OS-Competition`
**比赛子赛道**: `Kernel-Implementation`
**仓库地址**: `https://gitlab.eduxiji.net/educg-group-43501-3132633/T202610336999888-2558`
**推测开发语言**: `Rust / C (由AST探针自动识别)`
**特征函数量**: `2420`

## ⚔️ 底层代码同源性对比 (AST Homology)
> 本部分由专门的代码审查智能体，对该项目与符合年份条件的历届最高相似度项目进行控制流与底层调用对比。新版报告会先给出确定性的全模块覆盖矩阵，再给出 Agent 的分模块解释，避免只围绕少量抽样函数展开。

### 🏆 Top 4 底层对标: vs `2025-StarryX-杭州电子科技大学`（杭州电子科技大学 / StarryX）

#### 对方作品信息

**对方年份**: `2025`
**对方学校**: `杭州电子科技大学`
**对方队伍名称**: `StarryX`
**对方比赛名字**: `OS-Competition`
**对方比赛子赛道**: `Kernel-Implementation`
**对方仓库地址**: `https://gitlab.eduxiji.net/educg-group-36002-2710490/T202510336995486-3037`

> 双向覆盖率：共同 AST 指纹: 811；唯一 AST 指纹: 目标项目 2042，参考项目 2897；覆盖率: 目标项目 39.72%，参考项目 27.99%

## 全模块对比总览（确定性证据）

> 总体覆盖率：共同 AST 指纹: 811；唯一 AST 指纹: 目标项目 2042，参考项目 2897；覆盖率: 目标项目 39.72%，参考项目 27.99%

### 1. 模块覆盖矩阵
| 模块 | 目标函数数 | 参考函数数 | 共同唯一 AST 指纹 | Target 覆盖 | Base 覆盖 | 状态 |
| :--- | ---: | ---: | ---: | ---: | ---: | :--- |
| 进程管理 | 157 | 172 | 25 | 18.66% | 15.72% | 双方存在且有结构重合 |
| 任务调度/异步运行时 | 464 | 480 | 115 | 26.81% | 32.76% | 双方存在且有结构重合 |
| 内存管理 | 253 | 430 | 90 | 39.3% | 23.14% | 双方存在且有结构重合 |
| 中断/异常处理 | 2 | 70 | 2 | 100.0% | 3.23% | 双方存在且有结构重合 |
| 系统调用 | 70 | 89 | 21 | 34.43% | 26.25% | 双方存在且有结构重合 |
| 文件系统 | 506 | 801 | 133 | 32.92% | 21.42% | 双方存在且有结构重合 |
| 设备驱动 | 113 | 139 | 82 | 78.1% | 65.08% | 双方存在且有结构重合 |
| 网络协议栈 | 97 | 77 | 50 | 60.24% | 81.97% | 双方存在且有结构重合 |
| 文件系统/第三方库 | 29 | 164 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |
| 网络协议栈/第三方库 | 139 | 166 | 44 | 35.48% | 30.56% | 双方存在且有结构重合 |
| 第三方库 | 0 | 349 | 0 | 0.0% | 0.0% | 仅参考项目体现 |
| 通用模块/其他 | 590 | 665 | 262 | 54.7% | 49.43% | 双方存在且有结构重合 |

### 2. 分模块证据清单

#### 进程管理
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `examples/shell/src/uspace/process_lifecycle.rs#fork` | `xmodules/xprocess/src/process.rs#fork` | 同板块同名 | Arc::new, AtomicBool::new, AtomicI32::new, AtomicU32::new, AtomicU64::new, AtomicUsize::new ...(+58) | Box::new, Some, self.clone |
| `examples/shell/src/uspace/signal_abi.rs#sys_rt_sigprocmask` | `xapi/src/task/signal.rs#sys_rt_sigprocmask` | 同板块同名 | all_application_signal_mask, clear_user_bytes, current_mask.to_ne_bytes, current_task_ext, ext.fork_signal_mask_restore
                .store, ext.process.as_ref ...(+14) | Err, Ok, XProcess::from_thread, XProcess::from_thread(thread).uspace, XThread::from_thread, check_sigset_size ...(+3) |
| `api/arceos_posix_api/src/imp/io_mpx/epoll.rs#sys_epoll_wait` | `arceos/api/arceos_posix_api/src/imp/io_mpx/epoll.rs#sys_epoll_wait` | 同板块同名 | debug!, syscall_body! | debug!, syscall_body! |
| `kernel/task/axtask/src/run_queue.rs#exit_current` | `arceos/modules/axtask/src/run_queue.rs#exit_current` | 同板块同名同结构 | EXITED_TASKS.current_ref_mut_raw, EXITED_TASKS.current_ref_mut_raw().clear, EXITED_TASKS.current_ref_mut_raw().push_back, WAIT_FOR_EXIT.current_ref_mut_raw, WAIT_FOR_EXIT.current_ref_mut_raw().notify_one, assert! ...(+8) | EXITED_TASKS.current_ref_mut_raw, EXITED_TASKS.current_ref_mut_raw().clear, EXITED_TASKS.current_ref_mut_raw().push_back, WAIT_FOR_EXIT.current_ref_mut_raw, WAIT_FOR_EXIT.current_ref_mut_raw().notify_one, assert! ...(+8) |

#### 任务调度/异步运行时
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `ulib/axlibc/c/printf.c#format_string_loop` | `arceos/ulib/axlibc/c/printf.c#format_string_loop` | 同板块同名同结构 | ABS_FOR_PRINTING, ADVANCE_IN_FORMAT_STRING, atou_, is_digit_, out_rev_, parse_flags ...(+5) | ABS_FOR_PRINTING, ADVANCE_IN_FORMAT_STRING, atou_, is_digit_, out_rev_, parse_flags ...(+5) |
| `examples/shell/src/uspace/futex.rs#sys_futex` | `xapi/src/task/futex.rs#sys_futex` | 同板块同名 | core::time::Duration::new, current_sigcancel_pending, current_task_ext, ext.futex_wait.store, futex_key, neg_errno ...(+19) | Err, FutexKey::new, Ok, axtask::yield_now, current, current().task_ext ...(+24) |
| `examples/shell/src/uspace/select_fdset.rs#sys_pselect6` | `xapi/src/iomux/select.rs#sys_pselect6` | 同板块同名 | (nfds as usize).min, axhal::time::wall_time, axtask::yield_now, current_unblocked_signal_pending, deadline.is_some_and, neg_errno ...(+6) | do_select, nullable!, nullable!(uspace.read(timeout))?
                .map, nullable!(uspace.read(timeout))?
                .map(timespec::to_time_value)
                .transpose, with_uspace |
| `kernel/diagnostics/axlog/src/lib.rs#log` | `arceos/modules/axlog/src/lib.rs#log` | 同板块同名 | cfg_if::cfg_if!, record.level, record.line, record.line().unwrap_or, record.metadata, record.target ...(+1) | cfg_if::cfg_if!, record.level, record.line, record.line().unwrap_or, record.metadata, record.target ...(+1) |

#### 内存管理
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `ulib/axlibc/src/strftime.rs#strftime_inner` | `arceos/ulib/axlibc/src/strftime.rs#strftime_inner` | 同板块同名同结构 | CountingWriter::new, format.offset, inner_strftime, w! | CountingWriter::new, format.offset, inner_strftime, w! |
| `examples/shell/src/uspace/memory_map.rs#sys_mmap` | `xapi/src/mm/mmap.rs#sys_mmap` | 同板块同名 | (offset as u64).checked_add, LinuxError::from, Some, VirtAddr::from, VirtAddrRange::from_start_size, addr.as_usize ...(+30) | (addr + length).align_up, Err, FD_TABLE.is_assigned, File::from_fd, File::from_fd(fd, FileFlags::READ, FileFlags::empty())
            .map_err, FileFlags::empty ...(+39) |
| `ulib/axlibc/src/strftime.rs#inner_strftime` | `arceos/ulib/axlibc/src/strftime.rs#inner_strftime` | 同板块同名同结构 | format.offset, w! | format.offset, w! |
| `kernel/runtime/axruntime/src/lib.rs#rust_main` | `arceos/modules/axruntime/src/lib.rs#rust_main` | 同板块同名 | INITED_CPUS.fetch_add, ax_println!, axdisplay::init_display, axdriver::init_drivers, axfs::init_filesystems, axhal::init_early ...(+24) | INITED_CPUS.fetch_add, all_devices
                .block
                .take_one, all_devices
                .block
                .take_one()
                .expect, ax_println!, axdisplay::init_display, axdriver::init_drivers ...(+28) |

#### 中断/异常处理
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `ulib/axlibc/c/pow.c#exp_inline` | `arceos/ulib/axlibc/c/pow.c#exp_inline` | 同板块同名同结构 | __math_oflow, __math_uflow, asdouble, asuint64, converttoint, eval_as_double ...(+4) | __math_oflow, __math_uflow, asdouble, asuint64, converttoint, eval_as_double ...(+4) |
| `kernel/sync/axsync/src/mutex.rs#may_interrupt` | `arceos/modules/axsync/src/mutex.rs#may_interrupt` | 同板块同名同结构 | rand::random::<u32>, thread::yield_now | rand::random::<u32>, thread::yield_now |

#### 系统调用
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `api/arceos_posix_api/src/imp/net.rs#sys_getaddrinfo` | `arceos/api/arceos_posix_api/src/imp/net.rs#sys_getaddrinfo` | 同板块同名 | char_ptr_to_str, debug!, syscall_body! | char_ptr_to_str, debug!, syscall_body! |
| `api/arceos_posix_api/src/imp/io.rs#sys_writev` | `arceos/api/arceos_posix_api/src/imp/io.rs#sys_writev` | 同板块同名 | debug!, syscall_body! | debug!, syscall_body! |
| `api/arceos_posix_api/src/imp/net.rs#sys_recvfrom` | `arceos/api/arceos_posix_api/src/imp/net.rs#sys_recvfrom` | 同板块同名 | debug!, syscall_body! | debug!, syscall_body! |
| `api/arceos_posix_api/src/imp/net.rs#sys_socket` | `arceos/api/arceos_posix_api/src/imp/net.rs#sys_socket` | 同板块同名同结构 | debug!, syscall_body! | debug!, syscall_body! |

#### 文件系统
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `ulib/axlibc/c/glob.c#do_glob` | `arceos/ulib/axlibc/c/glob.c#do_glob` | 同板块同名同结构 | S_ISDIR, append, closedir, do_glob, errfunc, fnmatch ...(+7) | S_ISDIR, append, closedir, do_glob, errfunc, fnmatch ...(+7) |
| `ulib/axlibc/src/setjmp.rs#longjmp` | `arceos/ulib/axlibc/src/setjmp.rs#longjmp` | 同板块同名 | core::arch::naked_asm! | core::arch::naked_asm! |
| `ulib/axlibc/src/setjmp.rs#setjmp` | `arceos/ulib/axlibc/src/setjmp.rs#setjmp` | 同板块同名 | core::arch::naked_asm! | core::arch::naked_asm! |
| `ulib/axlibc/c/pow.c#log_inline` | `arceos/ulib/axlibc/c/pow.c#log_inline` | 同板块同名同结构 | __builtin_fma, asdouble | __builtin_fma, asdouble |

#### 设备驱动
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `tools/bwbench_client/src/main.rs#transmit_benchmark` | `arceos/tools/bwbench_client/src/main.rs#transmit_benchmark` | 同板块同名同结构 | Local::now, NetDevice::new, NetDevice::new(interface).unwrap, current_time.signed_duration_since, current_time.signed_duration_since(past_time).num_seconds, dev.send ...(+2) | Local::now, NetDevice::new, NetDevice::new(interface).unwrap, current_time.signed_duration_since, current_time.signed_duration_since(past_time).num_seconds, dev.send ...(+2) |
| `tools/bwbench_client/src/device.rs#bind_interface` | `arceos/tools/bwbench_client/src/device.rs#bind_interface` | 同板块同名同结构 | ETH_P_ALL.to_be, Err, Ok, libc::bind, self.ifreq.ioctl, std::io::Error::last_os_error ...(+2) | ETH_P_ALL.to_be, Err, Ok, libc::bind, self.ifreq.ioctl, std::io::Error::last_os_error ...(+2) |
| `kernel/drivers/axdriver/src/lib.rs#init_drivers` | `arceos/modules/axdriver/src/lib.rs#init_drivers` | 同板块同名同结构 | AllDevices::default, all_devs.block.iter, all_devs.block.iter().enumerate, all_devs.display.iter, all_devs.display.iter().enumerate, all_devs.net.iter ...(+5) | AllDevices::default, all_devs.block.iter, all_devs.block.iter().enumerate, all_devs.display.iter, all_devs.display.iter().enumerate, all_devs.net.iter ...(+5) |
| `tools/bwbench_client/src/main.rs#receive_benchmark` | `arceos/tools/bwbench_client/src/main.rs#receive_benchmark` | 同板块同名同结构 | Local::now, NetDevice::new, NetDevice::new(interface).unwrap, current_time.signed_duration_since, current_time.signed_duration_since(past_time).num_seconds, dev.recv ...(+1) | Local::now, NetDevice::new, NetDevice::new(interface).unwrap, current_time.signed_duration_since, current_time.signed_duration_since(past_time).num_seconds, dev.recv ...(+1) |

#### 网络协议栈
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `ulib/axlibc/c/network.c#inet_pton` | `arceos/ulib/axlibc/c/network.c#inet_pton` | 同板块同名同结构 | hexval, inet_pton, isdigit, memmove | hexval, inet_pton, isdigit, memmove |
| `api/arceos_posix_api/src/imp/net.rs#shutdown` | `arceos/api/arceos_posix_api/src/imp/net.rs#shutdown` | 同板块同名 | Err, Ok, matches!, tcpsocket.lock, tcpsocket.peer_addr, tcpsocket.shutdown ...(+6) | Ok, tcpsocket.lock, tcpsocket.peer_addr, tcpsocket.shutdown, udpsocket.lock, udpsocket.peer_addr ...(+1) |
| `ulib/axlibc/c/socket.c#accept4` | `arceos/ulib/axlibc/c/socket.c#accept4` | 同板块同名同结构 | accept, fcntl | accept, fcntl |
| `api/arceos_posix_api/src/imp/net.rs#recvfrom` | `arceos/api/arceos_posix_api/src/imp/net.rs#recvfrom` | 同板块同名同结构 | Ok, Some, tcpsocket.lock, tcpsocket.lock().recv, tcpsocket.lock().recv(buf).map, udpsocket
                .lock ...(+2) | Ok, Some, tcpsocket.lock, tcpsocket.lock().recv, tcpsocket.lock().recv(buf).map, udpsocket
                .lock ...(+2) |

#### 文件系统/第三方库
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `kernel/fs/axfs/src/fs/ext4fs.rs#read_dir` | `arceos/modules/axfs-ng/src/fs/ext4/inode.rs#read_dir` | 同板块同名 | Ok, VfsDirEntry::new, dirents.iter_mut, dirents.iter_mut().enumerate, dirents.len, entry.file_name ...(+12) | Ok, core::str::from_utf8, core::str::from_utf8(entry.name())
                .map_err, core::str::from_utf8(entry.name())
                .map_err(|_| VfsError::EINVAL)?
                .to_owned, entry.ino, entry.inode_type ...(+10) |
| `kernel/fs/axfs/src/fs/ext4fs.rs#lookup` | `arceos/crates/lwext4_rust/src/inode/dir.rs#lookup` | 同板块同名 | Arc::new, Ext4FileNode::new, Ok, Self::new, file_type.is_dir, fs.metadata ...(+12) | Ok, ext4_dir_find_entry, ext4_dir_find_entry(
                &mut result,
                self.inner.as_mut(),
                name.as_ptr() as *const _,
                name.len() as _,
            )
            .context, mem::zeroed, name.as_ptr, name.len ...(+1) |
| `kernel/fs/axfs/src/fs/ext4fs.rs#read_at` | `arceos/crates/lwext4_rust/src/inode/file.rs#read_at` | 同板块同名 | file.read_bytes, file.read_bytes(buf).map_err, file.seek_to, file.seek_to(offset).map_err, fs.open, fs.open(self.path.as_str()).map_err ...(+2) | (inode as *const _ as *const u8).add, (pos + buf.len() as u64).min, Ok, WritebackGuard::new, assert!, buf.as_mut_ptr ...(+22) |
| `kernel/fs/axfs/src/fs/ext4fs.rs#metadata` | `arceos/modules/axfs-ng/src/fs/ext4/inode.rs#metadata` | 同板块同名 | fs.metadata, fs.metadata(self.path.as_str()).map_err, self.fs
            .with, self.path.as_str | DeviceId::default, FileAttr::default, NodePermission::from_bits_truncate, Ok, into_vfs_type, self.fs
            .lock ...(+2) |

#### 网络协议栈/第三方库
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `kernel/net/axnet/src/smoltcp_impl/tcp.rs#connect` | `arceos/modules/axnet/src/smoltcp_impl/tcp.rs#connect` | 同板块同名 | Err, Ok, SOCKET_SET
                .with_socket_mut::<tcp::Socket, _, _>, SOCKET_SET.add, SocketSetWrapper::new_tcp_socket, Some ...(+27) | Err, Ok, Ok::<(IpEndpoint, IpEndpoint), NetError>, SOCKET_SET
                .with_socket_mut::<tcp::Socket, _, _>, SOCKET_SET.add, SocketSetWrapper::new_tcp_socket ...(+33) |
| `kernel/net/axnet/src/smoltcp_impl/dns.rs#query` | `arceos/modules/axnet/src/smoltcp_impl/dns.rs#query` | 同板块同名 | Err, IpAddr::from, Ok, SOCKET_SET
            .with_socket_mut::<dns::Socket, _, _>, SOCKET_SET
            .with_socket_mut::<dns::Socket, _, _>(handle, |socket| {
                socket.start_query(iface.lock().context(), name, query_type)
            })
            .map_err, SOCKET_SET.poll_interfaces ...(+12) | Err, IpAddr::from, Ok, SOCKET_SET
            .with_socket_mut::<dns::Socket, _, _>, SOCKET_SET
            .with_socket_mut::<dns::Socket, _, _>(handle, |socket| {
                socket.start_query(iface.lock().context(), name, query_type)
            })
            .map_err, SOCKET_SET.poll_interfaces ...(+11) |
| `kernel/net/axnet/src/smoltcp_impl/tcp.rs#shutdown` | `arceos/modules/axnet/src/smoltcp_impl/tcp.rs#shutdown` | 同板块同名 | (&mut *self.loopback.get()).take, LISTEN_TABLE.unlisten, Ok, SOCKET_SET.poll_interfaces, SOCKET_SET.with_socket_mut::<tcp::Socket, _, _>, debug! ...(+16) | LISTEN_TABLE.unlisten, Ok, SOCKET_SET.poll_interfaces, SOCKET_SET.with_socket_mut::<tcp::Socket, _, _>, debug!, self.handle.get ...(+9) |
| `kernel/net/axnet/src/smoltcp_impl/tcp.rs#recv` | `arceos/modules/axnet/src/smoltcp_impl/tcp.rs#recv` | 同板块同名 | Err, Ok, SOCKET_SET.with_socket_mut::<tcp::Socket, _, _>, ax_err!, ax_err_type!, endpoint.recv ...(+14) | Err, Ok, SOCKET_SET.with_socket_mut::<tcp::Socket, _, _>, self.block_on, self.handle.get, self.handle.get().read ...(+8) |

#### 第三方库
仅参考项目中观察到该模块代表函数，目标项目没有同模块可对齐证据：
- 参考项目：`arceos/crates/page_table_multiarch/page_table_multiarch/src/bits64.rs#map_region`，调用摘要：Err, M::flush_tlb, Ok, PageSize::Size1G.is_aligned, PageSize::Size2M.is_aligned, PageSize::Size4K.is_aligned ...(+12)
- 参考项目：`arceos/crates/axdriver_crates/axdriver_virtio/src/net.rs#try_new`，调用摘要：Err, InnerDev::new, InnerDev::new(transport).map_err, NetBufPool::new, Ok, Some ...(+14)
- 参考项目：`arceos/crates/page_table_multiarch/page_table_multiarch/src/bits64.rs#protect_region`，调用摘要：Err, Ok, TlbFlushAll::new, assert!, error!, self.protect ...(+5)
- 参考项目：`arceos/crates/page_table_multiarch/page_table_multiarch/src/arch/loongarch64.rs#flush_tlb`，调用摘要：asm!

#### 通用模块/其他
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `ulib/axlibc/c/printf.c#print_exponential_number` | `arceos/ulib/axlibc/c/printf.c#print_exponential_number` | 同板块同名同结构 | ABS_FOR_PRINTING, PRINTF_ABS, bastardized_floor, get_components, get_normalized_components, get_sign_bit ...(+5) | ABS_FOR_PRINTING, PRINTF_ABS, bastardized_floor, get_components, get_normalized_components, get_sign_bit ...(+5) |
| `ulib/axlibc/c/pow.c#pow` | `arceos/ulib/axlibc/c/pow.c#pow` | 同板块同名同结构 | __builtin_fma, __math_invalid, __math_oflow, __math_uflow, asdouble, asuint64 ...(+8) | __builtin_fma, __math_invalid, __math_oflow, __math_uflow, asdouble, asuint64 ...(+8) |
| `ulib/axlibc/c/string.c#twoway_strstr` | `arceos/ulib/axlibc/c/string.c#twoway_strstr` | 同板块同名同结构 | BITOP, MAX, memchr, memcmp | BITOP, MAX, memchr, memcmp |
| `ulib/axlibc/c/printf.c#print_broken_up_decimal` | `arceos/ulib/axlibc/c/printf.c#print_broken_up_decimal` | 同板块同名同结构 | out_rev_ | out_rev_ |


# 最终同源性定性仲裁报告

## 1. 宏观判定

- **定性结论**: **独立开发 / 底层架构显著差异**
- **客观 AST 相似度**: 19.65%
- **双向覆盖率**: 共同 AST 指纹 811；唯一 AST 指纹: 目标项目 2042，参考项目 2897；覆盖率: 目标项目 39.72%，参考项目 27.99%

经对双方项目全模块 AST 指纹、外部调用图谱及架构设计的深度析理，本庭认定：目标项目与参考项目在进程管理、任务调度、内存管理等核心板块的相似性，主要归因于**共同依赖的 ArceOS 开源框架及操作系统领域的标准实现范式**。双方在业务逻辑层（如进程生命周期管理、信号处理、内存映射）的抽象层次、内存模型及同步策略上存在显著架构差异。控方所列 19 项“同板块底层逻辑一致”证据，经逐条核验，均属框架公共代码、标准内核原语、trivial 访问器或 `unimplemented` 占位符，不构成对参考项目原创性表达的抄袭。整体相似度 19.65% 远低于可认定复用的阈值，支持独立开发结论。

## 2. 全模块覆盖矩阵解读

| 模块 | 目标函数数 | 参考函数数 | 共同 AST 指纹 | Target 覆盖 | Base 覆盖 | 双方均存在 | 证据强度 | 解读 |
| :--- | ---: | ---: | ---: | ---: | ---: | :---: | :---: | :--- |
| 进程管理 | 157 | 172 | 25 | 18.66% | 15.72% | 是 | 弱 | 共同指纹集中于 ArceOS 公共模块（axtask、axlibc）及 trivial 访问器；业务逻辑函数（fork、sys_wait4 等）调用图谱差异显著，属独立实现。 |
| 任务调度/异步运行时 | 464 | 480 | 115 | 26.81% | 32.76% | 是 | 中 | 共同指纹数量较多，但高度集中于 ArceOS 框架的 axtask 调度器、axlog 等公共基础设施；双方在 futex、select 等上层系统调用实现上存在明显架构差异。 |
| 内存管理 | 253 | 430 | 90 | 39.3% | 23.14% | 是 | 中 | 共同指纹包含 ArceOS 公共库（axlibc strftime）及框架初始化流程（axruntime::rust_main）；mmap 等核心系统调用的内存抽象与对象管理模型不同。 |
| 中断/异常处理 | 2 | 70 | 2 | 100.0% | 3.23% | 是 | 弱 | 目标项目仅 2 个函数落入该板块，覆盖率 100% 系样本过小导致的统计假象；共同指纹为数学库函数（pow.c）及同步原语（mutex），属公共依赖。 |
| 系统调用 | 70 | 89 | 21 | 34.43% | 26.25% | 是 | 弱 | 共同指纹集中于 ArceOS POSIX API 层的宏包装（syscall_body!）及网络系统调用；双方在核心系统调用路径上存在差异。 |
| 文件系统 | 506 | 801 | 133 | 32.92% | 21.42% | 是 | 中 | 共同指纹包含 axlibc 公共库（glob、setjmp、pow）及 ext4 文件系统接口；双方在 ext4 底层实现（read_at、lookup）上调用图谱差异显著，属不同封装层次。 |
| 设备驱动 | 113 | 139 | 82 | 78.1% | 65.08% | 是 | 中 | 覆盖率较高，但共同指纹集中于 ArceOS 官方工具（bwbench_client）及驱动框架初始化（axdriver::init_drivers），属公共示例与框架代码。 |
| 网络协议栈 | 97 | 77 | 50 | 60.24% | 81.97% | 是 | 中 | 共同指纹集中于 ArceOS 网络栈（axnet smoltcp_impl）及 axlibc 公共库（inet_pton、socket）；双方在 TCP 连接管理、DNS 查询等实现上调用图谱高度一致，属框架公共代码。 |
| 文件系统/第三方库 | 29 | 164 | 0 | 0.0% | 0.0% | 是 | 未形成 | 双方虽均有该板块函数，但未观察到任何共同 AST 指纹，表明该板块完全独立实现。 |
| 网络协议栈/第三方库 | 139 | 166 | 44 | 35.48% | 30.56% | 是 | 弱 | 共同指纹集中于 ArceOS 网络栈实现，属框架公共代码。 |
| 第三方库 | 0 | 349 | 0 | 0.0% | 0.0% | 仅参考项目 | 未形成 | 参考项目独有板块（page_table_multiarch、axdriver_virtio 等），目标项目无对应模块，不构成同源证据。 |
| 通用模块/其他 | 590 | 665 | 262 | 54.7% | 49.43% | 是 | 中 | 共同指纹数量最多，但集中于 axlibc 公共 C 库（printf、pow、string）及 Rust 标准库惯用模式；此类代码属公共依赖或有限表达。 |

## 3. 底层 AST 关键证据指纹表

| 所属板块 | 目标项目路径 | 参考项目路径 | 证据类型 | 技术说明 |
| :--- | :--- | :--- | :--- | :--- |
| 进程管理 | `kernel/task/axtask/src/wait_queue.rs#cancel_events` | `arceos/modules/axtask/src/wait_queue.rs#cancel_events` | 框架公共代码 | ArceOS 等待队列标准实现；调用图谱 `curr.in_wait_queue, curr.ptr_eq, curr.set_in_wait_queue, curr.timer_ticket_expired, self.queue.lock().retain` 完全一致，属框架公共逻辑。 |
| 进程管理 | `kernel/task/axtask/src/wait_queue.rs#wait_timeout` | `arceos/modules/axtask/src/wait_queue.rs#wait_timeout` | 框架公共代码 | ArceOS 超时等待原语；调用 `axhal::time::wall_time, crate::timers::set_alarm_wakeup, rq.blocked_resched` 为框架标准调度流。 |
| 进程管理 | `kernel/task/axtask/src/wait_queue.rs#unblock_one_task` | `arceos/modules/axtask/src/wait_queue.rs#unblock_one_task` | trivial 包装 | 单行委托函数：`select_run_queue::<NoOp>(&task).unblock_task`，无独创性表达。 |
| 进程管理 | `kernel/task/axtask/src/wait_queue.rs#requeue` | `arceos/modules/axtask/src/wait_queue.rs#requeue` | 标准算法 | futex requeue 标准操作；`drain(..count).collect` 为 VecDeque 惯用 API，属有限表达。 |
| 进程管理 | `kernel/task/axtask/src/task.rs#notify_exit` | `arceos/modules/axtask/src/task.rs#notify_exit` | 框架公共代码 | ArceOS 任务退出通知；`self.exit_code.store` + `self.wait_for_exit.notify_all` 为框架标准实现。 |
| 进程管理 | `kernel/task/axtask/src/task.rs#set_in_wait_queue` | `arceos/modules/axtask/src/task.rs#set_in_wait_queue` | trivial 访问器 | 原子标志位存储：`self.in_wait_queue.store`，无独创性。 |
| 进程管理 | `kernel/task/axtask/src/task.rs#in_wait_queue` | `arceos/modules/axtask/src/task.rs#in_wait_queue` | trivial 访问器 | 原子标志位加载：`self.in_wait_queue.load`，无独创性。 |
| 进程管理 | `kernel/task/axtask/src/api.rs#exit` | `arceos/modules/axtask/src/api.rs#exit` | 框架公共代码 | ArceOS 退出 API 转发：`current_run_queue().exit_current()`，属框架标准接口。 |
| 进程管理 | `tools/raspi4/chainloader/src/main.rs#print_jtag_info_and_wait_forever` | `arceos/tools/raspi4/chainloader/src/main.rs#print_jtag_info_and_wait_forever` | 官方示例代码 | ArceOS 官方 Raspberry Pi chainloader 示例；调用 `cpu::wait_forever, println!` 为固定模板。 |
| 进程管理 | `tools/raspi4/chainloader/src/panic_wait.rs#panic_prevent_reenter` | `arceos/tools/raspi4/chainloader/src/panic_wait.rs#panic_prevent_reenter` | 标准惯用模式 | 单核 panic 重入保护；`AtomicBool` + `compile_error!` 为 Rust 嵌入式标准做法。 |
| 进程管理 | `api/arceos_posix_api/src/imp/task.rs#sys_getpid` | `arceos/api/arceos_posix_api/src/imp/task.rs#sys_getpid` | 宏包装 | 单行 `syscall_body!` 宏调用，属 POSIX API 标准封装。 |
| 进程管理 | `api/arceos_posix_api/src/imp/pthread/mod.rs#exit_current` | `arceos/api/arceos_posix_api/src/imp/pthread/mod.rs#exit_current` | POSIX 标准实现 | pthread 退出标准流程：获取当前线程、读取返回值、调用 `axtask::exit`，表达空间有限。 |
| 进程管理 | `ulib/axlibc/c/pthread.c#pthread_cond_wait` | `arceos/ulib/axlibc/c/pthread.c#pthread_cond_wait` | 空占位符 | 函数体为单一 `unimplemented`，无实质性表达，不构成复用。 |
| 进程管理 | `ulib/axlibc/c/unistd.c#execve` | `arceos/ulib/axlibc/c/unistd.c#execve` | 空占位符 | 函数体为单一 `unimplemented`，无实质性表达，不构成复用。 |
| 进程管理 | `ulib/axlibc/c/wait.c#wait3` | `arceos/ulib/axlibc/c/wait.c#wait3` | 空占位符 | 函数体为单一 `unimplemented`，无实质性表达，不构成复用。 |
| 进程管理 | `ulib/axlibc/c/wait.c#waitpid` | `arceos/ulib/axlibc/c/wait.c#waitpid` | 空占位符 | 函数体为单一 `unimplemented`，无实质性表达，不构成复用。 |
| 任务调度/异步运行时 | `ulib/axlibc/c/printf.c#format_string_loop` | `arceos/ulib/axlibc/c/printf.c#format_string_loop` | 公共 C 库 | axlibc 格式化字符串核心循环；调用 `ABS_FOR_PRINTING, parse_flags` 等内部宏完全一致，属公共库代码。 |
| 任务调度/异步运行时 | `kernel/diagnostics/axlog/src/lib.rs#log` | `arceos/modules/axlog/src/lib.rs#log` | 框架公共代码 | ArceOS 日志宏实现；调用 `cfg_if::cfg_if!, record.level, record.target` 完全一致，属框架公共模块。 |
| 内存管理 | `ulib/axlibc/src/strftime.rs#strftime_inner` | `arceos/ulib/axlibc/src/strftime.rs#strftime_inner` | 公共 C 库 | axlibc 时间格式化内部函数；调用 `CountingWriter::new, format.offset, w!` 完全一致，属公共库代码。 |
| 内存管理 | `kernel/runtime/axruntime/src/lib.rs#rust_main` | `arceos/modules/axruntime/src/lib.rs#rust_main` | 框架公共代码 | ArceOS 内核入口；调用 `axdriver::init_drivers, axfs::init_filesystems` 等初始化序列高度一致，属框架标准启动流。 |
| 系统调用 | `api/arceos_posix_api/src/imp/net.rs#sys_socket` | `arceos/api/arceos_posix_api/src/imp/net.rs#sys_socket` | 框架公共代码 | ArceOS POSIX socket 系统调用；调用 `debug!, syscall_body!` 完全一致，属框架标准封装。 |
| 文件系统 | `ulib/axlibc/c/glob.c#do_glob` | `arceos/ulib/axlibc/c/glob.c#do_glob` | 公共 C 库 | axlibc glob 模式匹配；调用 `S_ISDIR, append, closedir, fnmatch` 等完全一致，属公共库代码。 |
| 文件系统 | `ulib/axlibc/src/setjmp.rs#longjmp` | `arceos/ulib/axlibc/src/setjmp.rs#longjmp` | 公共 C 库 | axlibc 非本地跳转；调用 `core::arch::naked_asm!` 完全一致，属架构相关公共实现。 |
| 设备驱动 | `tools/bwbench_client/src/main.rs#transmit_benchmark` | `arceos/tools/bwbench_client/src/main.rs#transmit_benchmark` | 官方示例代码 | ArceOS 带宽基准测试工具；调用 `NetDevice::new, dev.send` 等完全一致，属官方示例。 |
| 设备驱动 | `kernel/drivers/axdriver/src/lib.rs#init_drivers` | `arceos/modules/axdriver/src/lib.rs#init_drivers` | 框架公共代码 | ArceOS 驱动初始化；调用 `AllDevices::default, all_devs.block.iter().enumerate` 等完全一致，属框架标准实现。 |
| 网络协议栈 | `ulib/axlibc/c/network.c#inet_pton` | `arceos/ulib/axlibc/c/network.c#inet_pton` | 公共 C 库 | axlibc IP 地址转换；调用 `hexval, isdigit, memmove` 完全一致，属公共库代码。 |
| 网络协议栈 | `kernel/net/axnet/src/smoltcp_impl/tcp.rs#connect` | `arceos/modules/axnet/src/smoltcp_impl/tcp.rs#connect` | 框架公共代码 | ArceOS TCP 连接管理；调用 `SOCKET_SET.add, SocketSetWrapper::new_tcp_socket` 等高度一致，属框架公共实现。 |
| 网络协议栈/第三方库 | `kernel/net/axnet/src/smoltcp_impl/dns.rs#query` | `arceos/modules/axnet/src/smoltcp_impl/dns.rs#query` | 框架公共代码 | ArceOS DNS 查询；调用 `socket.start_query(iface.lock().context(), name, query_type)` 完全一致，属框架公共实现。 |
| 通用模块/其他 | `ulib/axlibc/c/printf.c#print_exponential_number` | `arceos/ulib/axlibc/c/printf.c#print_exponential_number` | 公共 C 库 | axlibc 浮点指数打印；调用 `bastardized_floor, get_components` 等完全一致，属公共库代码。 |
| 通用模块/其他 | `ulib/axlibc/c/pow.c#pow` | `arceos/ulib/axlibc/c/pow.c#pow` | 公共 C 库 | axlibc 幂函数；调用 `__builtin_fma, __math_invalid, asdouble` 等完全一致，属公共数学库。 |
| 通用模块/其他 | `ulib/axlibc/c/string.c#twoway_strstr` | `arceos/ulib/axlibc/c/string.c#twoway_strstr` | 公共 C 库 | axlibc 双向字符串搜索；调用 `BITOP, MAX, memchr, memcmp` 完全一致，属公共库算法。 |

## 4. 核心代码调用流对比剖析

### 【进程管理 / fork 系统调用】
* **底层技术析理**: 目标项目 `fork` 使用 `Arc::new` + 多种原子类型（`AtomicBool::new, AtomicI32::new, AtomicU32::new, AtomicU64::new, AtomicUsize::new`）构建进程控制块，体现基于引用计数的共享内存模型；参考项目 `fork` 使用 `Box::new, Some, self.clone`，体现基于所有权转移的复制模型。二者在内存抽象（`Arc` vs `Box`）与对象管理策略上存在根本性架构差异，属独立设计。
* **代码溯源链接**:
  - 目标项目: `examples/shell/src/uspace/process_lifecycle.rs#fork`
  - 参考项目: `xmodules/xprocess/src/process.rs#fork`

### 【进程管理 / sys_wait4 系统调用】
* **底层技术析理**: 目标项目调用 `neg_errno, process.wait_child, user_trace!, write_user_value`，采用进程对象内聚的等待模型，并通过 `write_user_value` 直接操作用户态内存；参考项目调用 `WaitOptions::from_bits, WaitPid::Pgid, WaitPid::Pid, child.data::<XProcess>`，采用选项位掩码与泛型数据提取的分离式等待模型。二者在同步策略与用户态交互路径上完全不同。
* **代码溯源链接**:
  - 目标项目: `examples/shell/src/uspace/process_lifecycle.rs#sys_wait4`
  - 参考项目: `xapi/src/task/wait.rs#sys_wait4`

### 【进程管理 / sys_rt_sigprocmask 系统调用】
* **底层技术析理**: 目标项目调用 `all_application_signal_mask, clear_user_bytes, current_mask.to_ne_bytes, current_task_ext, ext.process.as_ref`，基于 `current_task_ext` 扩展结构直接操作进程信号掩码；参考项目调用 `XProcess::from_thread, XThread::from_thread, check_sigset_size, nullable!, with_thread`，通过 `XProcess`/`XThread` 抽象层间接操作。二者在信号管理的抽象层次与对象模型上存在显著差异。
* **代码溯源链接**:
  - 目标项目: `examples/shell/src/uspace/signal_abi.rs#sys_rt_sigprocmask`
  - 参考项目: `xapi/src/task/signal.rs#sys_rt_sigprocmask`

### 【内存管理 / sys_mmap 系统调用】
* **底层技术析理**: 目标项目调用 `VirtAddr::from, VirtAddrRange::from_start_size, addr.as_usize`，直接使用虚拟地址与地址范围抽象进行映射操作；参考项目调用 `File::from_fd, FileFlags::READ, FD_TABLE.is_assigned`，通过文件描述符表与文件对象进行映射。二者在内存映射的抽象层次（地址驱动 vs 文件驱动）上存在根本性架构差异。
* **代码溯源链接**:
  - 目标项目: `examples/shell/src/uspace/memory_map.rs#sys_mmap`
  - 参考项目: `xapi/src/mm/mmap.rs#sys_mmap`

### 【任务调度/异步运行时 / sys_futex 系统调用】
* **底层技术析理**: 目标项目调用 `core::time::Duration::new, current_sigcancel_pending, current_task_ext, ext.futex_wait.store, futex_key`，基于任务扩展结构内嵌的 futex 等待标志位实现；参考项目调用 `FutexKey::new, axtask::yield_now, current().task_ext`，通过独立的 `FutexKey` 对象管理等待队列。二者在 futex 的键管理策略与等待机制上存在架构差异。
* **代码溯源链接**:
  - 目标项目: `examples/shell/src/uspace/futex.rs#sys_futex`
  - 参考项目: `xapi/src/task/futex.rs#sys_futex`

### 【文件系统 / ext4 read_at 操作】
* **底层技术析理**: 目标项目调用 `file.read_bytes, file.seek_to, fs.open`，通过文件句柄的高级抽象进行读取；参考项目调用 `ext4_dir_find_entry, mem::zeroed, WritebackGuard::new, buf.as_mut_ptr`，直接操作 ext4 底层目录项与写回守卫。二者在文件系统抽象层次（高级文件接口 vs 底层 inode 操作）上存在显著差异。
* **代码溯源链接**:
  - 目标项目: `kernel/fs/axfs/src/fs/ext4fs.rs#read_at`
  - 参考项目: `arceos/crates/lwext4_rust/src/inode/file.rs#read_at`

## 5. 单方模块与降权项

### 5.1 仅参考项目存在的模块
- **第三方库**（349 个函数）：包含 `page_table_multiarch`、`axdriver_virtio` 等独立 crate。目标项目无对应模块，不构成同源证据。此类模块的存在反而说明参考项目具有更广泛的第三方依赖，与目标项目的模块组织策略不同。

### 5.2 跨板块同名/同 hash 降权项
以下函数虽同名，但因跨板块对比或调用图谱差异显著，依法降级为弱信号，不纳入主证据：
- `fork`：目标位于 `examples/shell/src/uspace/`，参考位于 `xmodules/xprocess/src/`；调用图谱从 `Arc` 多原子类型变为 `Box` + `self.clone`，内存模型根本不同。
- `sys_rt_sigprocmask`：目标位于 `examples/shell/src/uspace/`，参考位于 `xapi/src/task/`；外部对象从 `current_task_ext` 变为 `XProcess`/`XThread`。
- `sys_wait4`：目标位于 `examples/shell/src/uspace/`，参考位于 `xapi/src/task/`；等待模型从 `process.wait_child` 变为 `WaitOptions` + `WaitPid`。
- `sys_rt_sigtimedwait`：目标位于 `examples/shell/src/uspace/`，参考位于 `xapi/src/task/`；信号等待实现路径不同。
- `sys_exit_group`：目标位于 `examples/shell/src/uspace/`，参考位于 `xapi/src/task/`；目标调用 `process.request_exit_group`，参考直接调用 `do_exit`。
- `sys_exit`（examples/shell vs xapi）：目标调用 `terminate_current_thread`，参考调用 `do_exit`。
- `pgid` / `pid` / `sid`：目标位于 `examples/shell/src/uspace/process_lifecycle.rs` 为结构体字段访问器；参考位于 `xmodules/xprocess/` 为独立模块内部逻辑。领域模型不同。

### 5.3 公共接口与泛型函数名降权说明
- `syscall_body!`、`debug!`、`unimplemented` 等宏或占位符，属 Rust 生态通用模式，不具备项目特异性。
- `AtomicBool::new`、`VecDeque::with_capacity`、`SpinNoIrq::new` 等为标准库/框架通用类型构造，调用顺序由框架设计决定，非独立创作表达。
- `sys_epoll_wait`、`sys_getaddrinfo`、`sys_writev` 等 POSIX 系统调用名称为行业标准，同名不构成同源证据。

## 6. 保守结论

经全模块 AST 指纹比对与架构差异深度析理，本庭认定：目标项目 `[2026-T202610336999888-杭州电子科技大学]` 与参考项目 `[2025-StarryX-杭州电子科技大学]` 的客观相似度为 19.65%，远低于可认定代码复用的阈值。双方在进程管理、内存管理、文件系统等核心板块的共同 AST 指纹，经逐条核验，均集中于 ArceOS 开源框架的公共模块（axtask、axlibc、axnet、axdriver）、官方示例代码（raspi4 chainloader、bwbench_client）及标准库惯用模式。在真正体现业务逻辑原创性的系统调用层（fork、sys_wait4、sys_mmap、sys_futex 等），双方在内存抽象、同步策略、对象管理模型上存在根本性架构差异。控方所列 19 项“同板块底层逻辑一致”证据，实质为框架公共代码、trivial 访问器或空占位符，不构成对参考项目原创性表达的抄袭。综上，目标项目具有合理的原创性基础，其与参考项目的相似性归因于共同上游框架依赖及操作系统领域的标准实现范式，不构成代码复用或抄袭。

------------------------------

