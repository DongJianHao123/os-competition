# 🛡️ OS 内核赛自动化代码级架构审计报告

**审计时间**: `2026-06-27 17:50:13`
**参赛作品名称**: `2026-T202610336999888-杭州电子科技大学`
**年份**: `2026`
**学校**: `杭州电子科技大学`
**队伍名称**: `T202610336999888`
**比赛名字**: `OS-Competition`
**比赛子赛道**: `Kernel-Implementation`
**仓库地址**: `https://gitlab.eduxiji.net/educg-group-43501-3132633/T202610336999888-2558`
**推测开发语言**: `Rust / C (由AST探针自动识别)`
**特征函数量**: `2420`

## ⚔️ 底层代码同源性对比 (AST Homology)
> 本部分由专门的代码审查智能体，对该项目与符合年份条件的历届最高相似度项目进行控制流与底层调用对比。新版报告会先给出确定性的全模块覆盖矩阵，再给出 Agent 的分模块解释，避免只围绕少量抽样函数展开。

### 🏆 Top 3 底层对标: vs `2025-狗剩-海南大学`（海南大学 / 狗剩）

#### 对方作品信息

**对方年份**: `2025`
**对方学校**: `海南大学`
**对方队伍名称**: `狗剩`
**对方比赛名字**: `OS-Competition`
**对方比赛子赛道**: `Kernel-Implementation`
**对方仓库地址**: `https://gitlab.eduxiji.net/educg-group-36002-2710490/T202510589995148-2209`

> 双向覆盖率：共同 AST 指纹: 900；唯一 AST 指纹: 目标项目 2042，参考项目 3020；覆盖率: 目标项目 44.07%，参考项目 29.8%

## 全模块对比总览（确定性证据）

> 总体覆盖率：共同 AST 指纹: 900；唯一 AST 指纹: 目标项目 2042，参考项目 3020；覆盖率: 目标项目 44.07%，参考项目 29.8%

### 1. 模块覆盖矩阵
| 模块 | 目标函数数 | 参考函数数 | 共同唯一 AST 指纹 | Target 覆盖 | Base 覆盖 | 状态 |
| :--- | ---: | ---: | ---: | ---: | ---: | :--- |
| 进程管理 | 157 | 93 | 26 | 19.4% | 30.95% | 双方存在且有结构重合 |
| 任务调度/异步运行时 | 464 | 399 | 120 | 27.97% | 41.67% | 双方存在且有结构重合 |
| 内存管理 | 253 | 326 | 94 | 41.05% | 32.3% | 双方存在且有结构重合 |
| 中断/异常处理 | 2 | 62 | 2 | 100.0% | 4.08% | 双方存在且有结构重合 |
| 系统调用 | 70 | 153 | 21 | 34.43% | 16.28% | 双方存在且有结构重合 |
| 文件系统 | 506 | 747 | 204 | 50.5% | 36.04% | 双方存在且有结构重合 |
| 设备驱动 | 113 | 142 | 84 | 80.0% | 68.29% | 双方存在且有结构重合 |
| 网络协议栈 | 97 | 77 | 50 | 60.24% | 94.34% | 双方存在且有结构重合 |
| 文件系统/第三方库 | 29 | 689 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |
| 网络协议栈/第三方库 | 139 | 107 | 53 | 42.74% | 53.0% | 双方存在且有结构重合 |
| 第三方库 | 0 | 386 | 0 | 0.0% | 0.0% | 仅参考项目体现 |
| 通用模块/其他 | 590 | 665 | 260 | 54.28% | 52.95% | 双方存在且有结构重合 |

### 2. 分模块证据清单

#### 进程管理
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `examples/shell/src/uspace/process_lifecycle.rs#fork` | `arceos/ulib/axlibc/c/unistd.c#fork` | 同板块同名 | Arc::new, AtomicBool::new, AtomicI32::new, AtomicU32::new, AtomicU64::new, AtomicUsize::new ...(+58) | unimplemented |
| `kernel/fs/axfs/src/mounts.rs#procfs` | `arceos/modules/axfs/src/mounts.rs#procfs` | 同板块同名 | Arc::new, Ok, file_cpuinfo.write_at, file_lo_tag.write_at, file_meminfo.write_at, file_mounts.write_at ...(+19) | Arc::new, Ok, file_over.write_at, file_somaxconn.write_at, fs::ramfs::RamFileSystem::new, proc_root.clone ...(+3) |
| `examples/shell/src/uspace/signal_abi.rs#sys_rt_sigprocmask` | `api/src/imp/signal.rs#sys_rt_sigprocmask` | 同板块同名 | all_application_signal_mask, clear_user_bytes, current_mask.to_ne_bytes, current_task_ext, ext.fork_signal_mask_restore
                .store, ext.process.as_ref ...(+14) | Err, Ok, check_sigset, current, current()
        .task_ext, current()
        .task_ext()
        .thread
        .signal
        .with_blocked_mut::<LinuxResult<_>> ...(+2) |
| `api/arceos_posix_api/src/imp/io_mpx/epoll.rs#sys_epoll_wait` | `arceos/api/arceos_posix_api/src/imp/io_mpx/epoll.rs#sys_epoll_wait` | 同板块同名 | debug!, syscall_body! | debug!, syscall_body! |

#### 任务调度/异步运行时
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `ulib/axlibc/c/printf.c#format_string_loop` | `arceos/ulib/axlibc/c/printf.c#format_string_loop` | 同板块同名同结构 | ABS_FOR_PRINTING, ADVANCE_IN_FORMAT_STRING, atou_, is_digit_, out_rev_, parse_flags ...(+5) | ABS_FOR_PRINTING, ADVANCE_IN_FORMAT_STRING, atou_, is_digit_, out_rev_, parse_flags ...(+5) |
| `examples/shell/src/uspace/futex.rs#sys_futex` | `api/src/imp/futex.rs#sys_futex` | 同板块同名 | core::time::Duration::new, current_sigcancel_pending, current_task_ext, ext.futex_wait.store, futex_key, neg_errno ...(+19) | Err, Ok, TimeValue::new, axtask::yield_now, futex_table.get, futex_table.get_or_insert ...(+14) |
| `kernel/diagnostics/axlog/src/lib.rs#log` | `arceos/modules/axlog/src/lib.rs#log` | 同板块同名 | cfg_if::cfg_if!, record.level, record.line, record.line().unwrap_or, record.metadata, record.target ...(+1) | cfg_if::cfg_if!, record.level, record.line, record.line().unwrap_or, record.metadata, record.target ...(+1) |
| `examples/shell/src/uspace/signal_abi.rs#sys_rt_sigreturn` | `api/src/imp/signal.rs#sys_rt_sigreturn` | 同板块同名 | Some, apply_loongarch_sigcontext, apply_riscv_sigcontext, current_task_ext, ext.pending_signal.load, ext.pending_sigreturn.lock ...(+8) | Ok, current, current().task_ext, current().task_ext().thread.signal.restore, tf.retval |

#### 内存管理
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `examples/shell/src/uspace/process_lifecycle.rs#sys_clone` | `src/syscall_imp/task/thread.rs#sys_clone` | 同板块同名 | "user:fork".into, "user:thread".into, TaskInner::new, UserTaskExt::new, UspaceContext::from, all_application_signal_mask ...(+35) | Arc::default, Arc::downgrade, Arc::new, CloneFlags::from_bits, CloneFlags::from_bits((flags & !FLAG_MASK) as u32).unwrap, Err ...(+57) |
| `ulib/axlibc/src/strftime.rs#strftime_inner` | `arceos/ulib/axlibc/src/strftime.rs#strftime_inner` | 同板块同名同结构 | CountingWriter::new, format.offset, inner_strftime, w! | CountingWriter::new, format.offset, inner_strftime, w! |
| `examples/shell/src/uspace/memory_map.rs#sys_mmap` | `src/syscall_imp/mm/mmap.rs#sys_mmap` | 同板块同名 | (offset as u64).checked_add, LinuxError::from, Some, VirtAddr::from, VirtAddrRange::from_start_size, addr.as_usize ...(+30) | (addr + length).align_up, Err, MmapFlags::MAP_PRIVATE.bits, MmapFlags::MAP_SHARED.bits, MmapFlags::MAP_SHARED_VALIDATE.bits, MmapFlags::from_bits_truncate ...(+32) |
| `ulib/axlibc/src/strftime.rs#inner_strftime` | `arceos/ulib/axlibc/src/strftime.rs#inner_strftime` | 同板块同名同结构 | format.offset, w! | format.offset, w! |

#### 中断/异常处理
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `ulib/axlibc/c/pow.c#exp_inline` | `arceos/ulib/axlibc/c/pow.c#exp_inline` | 同板块同名同结构 | __math_oflow, __math_uflow, asdouble, asuint64, converttoint, eval_as_double ...(+4) | __math_oflow, __math_uflow, asdouble, asuint64, converttoint, eval_as_double ...(+4) |
| `kernel/sync/axsync/src/mutex.rs#may_interrupt` | `arceos/modules/axsync/src/mutex.rs#may_interrupt` | 同板块同名同结构 | rand::random::<u32>, thread::yield_now | rand::random::<u32>, thread::yield_now |

#### 系统调用
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `api/arceos_posix_api/src/imp/net.rs#sys_getaddrinfo` | `arceos/api/arceos_posix_api/src/imp/net.rs#sys_getaddrinfo` | 同板块同名 | char_ptr_to_str, debug!, syscall_body! | char_ptr_to_str, debug!, syscall_body! |
| `api/arceos_posix_api/src/imp/io.rs#sys_writev` | `api/src/imp/io.rs#sys_writev` | 同板块同名 | debug!, syscall_body! | (0..=1024).contains, Err, Ok, core::slice::from_raw_parts, debug!, iov.get_as_bytes ...(+2) |
| `api/arceos_posix_api/src/imp/net.rs#sys_recvfrom` | `arceos/api/arceos_posix_api/src/imp/net.rs#sys_recvfrom` | 同板块同名 | debug!, syscall_body! | debug!, syscall_body! |
| `api/arceos_posix_api/src/imp/net.rs#sys_socket` | `arceos/api/arceos_posix_api/src/imp/net.rs#sys_socket` | 同板块同名同结构 | debug!, syscall_body! | debug!, syscall_body! |

#### 文件系统
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `ulib/axlibc/c/glob.c#do_glob` | `arceos/ulib/axlibc/c/glob.c#do_glob` | 同板块同名同结构 | S_ISDIR, append, closedir, do_glob, errfunc, fnmatch ...(+7) | S_ISDIR, append, closedir, do_glob, errfunc, fnmatch ...(+7) |
| `ulib/axlibc/src/setjmp.rs#longjmp` | `arceos/ulib/axlibc/src/setjmp.rs#longjmp` | 同板块同名 | core::arch::naked_asm! | core::arch::naked_asm! |
| `ulib/axlibc/src/setjmp.rs#setjmp` | `arceos/ulib/axlibc/src/setjmp.rs#setjmp` | 同板块同名 | core::arch::naked_asm! | core::arch::naked_asm! |
| `ulib/axlibc/c/pow.c#log_inline` | `arceos/ulib/axlibc/c/pow.c#log_inline` | 同板块同名同结构 | __builtin_fma, asdouble | __builtin_fma, asdouble |

#### 设备驱动
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `tools/bwbench_client/src/main.rs#transmit_benchmark` | `arceos/tools/bwbench_client/src/main.rs#transmit_benchmark` | 同板块同名同结构 | Local::now, NetDevice::new, NetDevice::new(interface).unwrap, current_time.signed_duration_since, current_time.signed_duration_since(past_time).num_seconds, dev.send ...(+2) | Local::now, NetDevice::new, NetDevice::new(interface).unwrap, current_time.signed_duration_since, current_time.signed_duration_since(past_time).num_seconds, dev.send ...(+2) |
| `tools/bwbench_client/src/device.rs#bind_interface` | `arceos/tools/bwbench_client/src/device.rs#bind_interface` | 同板块同名同结构 | ETH_P_ALL.to_be, Err, Ok, libc::bind, self.ifreq.ioctl, std::io::Error::last_os_error ...(+2) | ETH_P_ALL.to_be, Err, Ok, libc::bind, self.ifreq.ioctl, std::io::Error::last_os_error ...(+2) |
| `kernel/drivers/axdriver/src/lib.rs#init_drivers` | `arceos/modules/axdriver/src/lib.rs#init_drivers` | 同板块同名同结构 | AllDevices::default, all_devs.block.iter, all_devs.block.iter().enumerate, all_devs.display.iter, all_devs.display.iter().enumerate, all_devs.net.iter ...(+5) | AllDevices::default, all_devs.block.iter, all_devs.block.iter().enumerate, all_devs.display.iter, all_devs.display.iter().enumerate, all_devs.net.iter ...(+5) |
| `tools/bwbench_client/src/main.rs#receive_benchmark` | `arceos/tools/bwbench_client/src/main.rs#receive_benchmark` | 同板块同名同结构 | Local::now, NetDevice::new, NetDevice::new(interface).unwrap, current_time.signed_duration_since, current_time.signed_duration_since(past_time).num_seconds, dev.recv ...(+1) | Local::now, NetDevice::new, NetDevice::new(interface).unwrap, current_time.signed_duration_since, current_time.signed_duration_since(past_time).num_seconds, dev.recv ...(+1) |

#### 网络协议栈
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `ulib/axlibc/c/network.c#inet_pton` | `arceos/ulib/axlibc/c/network.c#inet_pton` | 同板块同名同结构 | hexval, inet_pton, isdigit, memmove | hexval, inet_pton, isdigit, memmove |
| `api/arceos_posix_api/src/imp/net.rs#shutdown` | `api/src/fd/net.rs#shutdown` | 同板块同名 | Err, Ok, matches!, tcpsocket.lock, tcpsocket.peer_addr, tcpsocket.shutdown ...(+6) | Ok, tcpsocket.lock, tcpsocket.peer_addr, tcpsocket.shutdown, udpsocket.lock, udpsocket.peer_addr ...(+1) |
| `ulib/axlibc/c/socket.c#accept4` | `arceos/ulib/axlibc/c/socket.c#accept4` | 同板块同名同结构 | accept, fcntl | accept, fcntl |
| `api/arceos_posix_api/src/imp/net.rs#recvfrom` | `api/src/fd/net.rs#recvfrom` | 同板块同名同结构 | Ok, Some, tcpsocket.lock, tcpsocket.lock().recv, tcpsocket.lock().recv(buf).map, udpsocket
                .lock ...(+2) | Ok, Some, tcpsocket.lock, tcpsocket.lock().recv, tcpsocket.lock().recv(buf).map, udpsocket
                .lock ...(+2) |

#### 文件系统/第三方库
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `kernel/fs/axfs/src/fs/ext4fs.rs#read_dir` | `arceos/modules/axfs/src/fs/lwext4_rust.rs#read_dir` | 同板块同名 | Ok, VfsDirEntry::new, dirents.iter_mut, dirents.iter_mut().enumerate, dirents.len, entry.file_name ...(+12) | Ok, VfsDirEntry::new, core::str::from_utf8, core::str::from_utf8(iname.unwrap()).unwrap, dirents.iter_mut, dirents.iter_mut().enumerate ...(+13) |
| `kernel/fs/axfs/src/fs/ext4fs.rs#lookup` | `arceos/modules/axfs/src/fs/lwext4_rust.rs#lookup` | 同板块同名 | Arc::new, Ext4FileNode::new, Ok, Self::new, file_type.is_dir, fs.metadata ...(+12) | Arc::new, Err, Ok, Self::new, file.check_inode_exist, fpath.as_str ...(+5) |
| `kernel/fs/axfs/src/fs/ext4fs.rs#read_at` | `crates/lwext4_rust/src/inode/file.rs#read_at` | 同板块同名 | file.read_bytes, file.read_bytes(buf).map_err, file.seek_to, file.seek_to(offset).map_err, fs.open, fs.open(self.path.as_str()).map_err ...(+2) | (inode as *const _ as *const u8).add, (pos + buf.len() as u64).min, Ok, WritebackGuard::new, assert!, buf.as_mut_ptr ...(+22) |
| `kernel/fs/axfs/src/fs/ext4fs.rs#parent` | `arceos/modules/axfs/src/fs/lwext4_rust.rs#parent` | 同板块同名 | Arc::new, Self::new, self.fs.clone, self.parent_path, self.parent_path()
            .map | Arc::new, Self::new, Some, file.get_path, file.get_type, info! ...(+6) |

#### 网络协议栈/第三方库
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `kernel/net/axnet/src/smoltcp_impl/tcp.rs#connect` | `arceos/modules/axnet/src/smoltcp_impl/tcp.rs#connect` | 同板块同名 | Err, Ok, SOCKET_SET
                .with_socket_mut::<tcp::Socket, _, _>, SOCKET_SET.add, SocketSetWrapper::new_tcp_socket, Some ...(+27) | Err, Ok, SOCKET_SET
                .with_socket_mut::<tcp::Socket, _, _>, SOCKET_SET.add, SocketSetWrapper::new_tcp_socket, Some ...(+29) |
| `kernel/net/axnet/src/smoltcp_impl/dns.rs#query` | `arceos/modules/axnet/src/smoltcp_impl/dns.rs#query` | 同板块同名 | Err, IpAddr::from, Ok, SOCKET_SET
            .with_socket_mut::<dns::Socket, _, _>, SOCKET_SET
            .with_socket_mut::<dns::Socket, _, _>(handle, |socket| {
                socket.start_query(iface.lock().context(), name, query_type)
            })
            .map_err, SOCKET_SET.poll_interfaces ...(+12) | Err, Ok, SOCKET_SET
            .with_socket_mut::<dns::Socket, _, _>, SOCKET_SET
            .with_socket_mut::<dns::Socket, _, _>(handle, |socket| {
                socket.start_query(iface.lock().context(), name, query_type)
            })
            .map_err, SOCKET_SET.poll_interfaces, SOCKET_SET.with_socket_mut::<dns::Socket, _, _> ...(+12) |
| `kernel/net/axnet/src/smoltcp_impl/tcp.rs#shutdown` | `arceos/modules/axnet/src/smoltcp_impl/tcp.rs#shutdown` | 同板块同名 | (&mut *self.loopback.get()).take, LISTEN_TABLE.unlisten, Ok, SOCKET_SET.poll_interfaces, SOCKET_SET.with_socket_mut::<tcp::Socket, _, _>, debug! ...(+16) | LISTEN_TABLE.unlisten, Ok, SOCKET_SET.poll_interfaces, SOCKET_SET.with_socket_mut::<tcp::Socket, _, _>, debug!, self.handle.get ...(+9) |
| `kernel/net/axnet/src/smoltcp_impl/tcp.rs#recv` | `arceos/modules/axnet/src/smoltcp_impl/tcp.rs#recv` | 同板块同名 | Err, Ok, SOCKET_SET.with_socket_mut::<tcp::Socket, _, _>, ax_err!, ax_err_type!, endpoint.recv ...(+14) | Err, Ok, SOCKET_SET.with_socket_mut::<tcp::Socket, _, _>, ax_err!, ax_err_type!, self.block_on ...(+10) |

#### 第三方库
仅参考项目中观察到该模块代表函数，目标项目没有同模块可对齐证据：
- 参考项目：`crates/visionfive2-sd/src/lib.rs#send_cmd`，调用摘要：RawInterrupt::from, RawInterruptStatusReg::from, S::sleep_ms_until, Some, arg.into, assert! ...(+24)
- 参考项目：`crates/visionfive2-sd/src/lib.rs#init_sdcard`，调用摘要：CardTypeReg::from, CardTypeReg::from(0).with_card_width4_1, CmdArg::new, CmdReg::from, ControlReg::from, RawInterruptStatusReg::from ...(+20)
- 参考项目：`crates/page_table_multiarch/page_table_multiarch/src/bits64.rs#map_region`，调用摘要：Err, M::flush_tlb, Ok, PageSize::Size1G.is_aligned, PageSize::Size2M.is_aligned, PageSize::Size4K.is_aligned ...(+12)
- 参考项目：`crates/axdriver_crates/axdriver_virtio/src/net.rs#try_new`，调用摘要：Err, InnerDev::new, InnerDev::new(transport).map_err, NetBufPool::new, Ok, Some ...(+14)

#### 通用模块/其他
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `ulib/axlibc/c/printf.c#print_exponential_number` | `arceos/ulib/axlibc/c/printf.c#print_exponential_number` | 同板块同名同结构 | ABS_FOR_PRINTING, PRINTF_ABS, bastardized_floor, get_components, get_normalized_components, get_sign_bit ...(+5) | ABS_FOR_PRINTING, PRINTF_ABS, bastardized_floor, get_components, get_normalized_components, get_sign_bit ...(+5) |
| `ulib/axlibc/c/pow.c#pow` | `arceos/ulib/axlibc/c/pow.c#pow` | 同板块同名同结构 | __builtin_fma, __math_invalid, __math_oflow, __math_uflow, asdouble, asuint64 ...(+8) | __builtin_fma, __math_invalid, __math_oflow, __math_uflow, asdouble, asuint64 ...(+8) |
| `ulib/axlibc/c/string.c#twoway_strstr` | `arceos/ulib/axlibc/c/string.c#twoway_strstr` | 同板块同名同结构 | BITOP, MAX, memchr, memcmp | BITOP, MAX, memchr, memcmp |
| `ulib/axlibc/c/printf.c#print_broken_up_decimal` | `arceos/ulib/axlibc/c/printf.c#print_broken_up_decimal` | 同板块同名同结构 | out_rev_ | out_rev_ |


#### 1. 宏观判定
- **定性结论**: 独立开发 (相似度: 21.62%，共同 AST 指纹: 900；唯一 AST 指纹: 目标项目 2042，参考项目 3020；覆盖率: 目标项目 44.07%，参考项目 29.8%)

#### 2. 全模块覆盖矩阵解读
- **进程管理**
  1. 双方均存在该模块。
  2. 存在共同 AST 指纹。
  3. 目标覆盖率 19.4%，参考覆盖率 30.95%。覆盖率较低，表明双方在该模块的实现细节上存在显著差异。
  4. 证据强度：**弱**。控方将大量属于任务调度、文件系统、C库的代码硬凑入此板块，核心指控证据实为公共上游代码。

- **任务调度/异步运行时**
  1. 双方均存在该模块。
  2. 存在共同 AST 指纹。
  3. 目标覆盖率 27.97%，参考覆盖率 41.67%。覆盖率中等，共同指纹多集中在 `axtask` 等公共 crate 的基础设施上。
  4. 证据强度：**弱**。共同部分如 `wait_queue`、`run_queue` 的实现，是 ArceOS 官方 `axtask` 模块的标准范式，非参考项目独创。

- **内存管理**
  1. 双方均存在该模块。
  2. 存在共同 AST 指纹。
  3. 目标覆盖率 41.05%，参考覆盖率 32.3%。覆盖率中等，共同指纹多源于 `axlibc` 等公共库。
  4. 证据强度：**弱**。例如 `strftime_inner` 等函数是标准 C 库的 Rust 移植，其实现逻辑由规范决定，非抄袭证据。

- **中断/异常处理**
  1. 双方均存在该模块。
  2. 存在共同 AST 指纹。
  3. 目标覆盖率 100.0%，参考覆盖率 4.08%。目标项目函数极少，覆盖率数值失真。
  4. 证据强度：**未形成**。目标项目仅2个函数，与参考项目62个函数体量悬殊，无法进行有效对比。

- **系统调用**
  1. 双方均存在该模块。
  2. 存在共同 AST 指纹。
  3. 目标覆盖率 34.43%，参考覆盖率 16.28%。覆盖率较低。
  4. 证据强度：**弱**。共同部分多为 `syscall_body!` 宏或 `debug!` 等日志宏调用，无法证明内部实现逻辑一致。

- **文件系统**
  1. 双方均存在该模块。
  2. 存在共同 AST 指纹。
  3. 目标覆盖率 50.5%，参考覆盖率 36.04%。覆盖率中等。
  4. 证据强度：**中**。双方在 ext4 文件系统实现上存在结构重合，但均基于 `lwext4_rust` 公共库，且调用流存在差异。

- **设备驱动**
  1. 双方均存在该模块。
  2. 存在共同 AST 指纹。
  3. 目标覆盖率 80.0%，参考覆盖率 68.29%。覆盖率较高。
  4. 证据强度：**弱**。高覆盖率源于 `bwbench_client` 等工具和 `axdriver` 初始化代码，这些是 ArceOS 官方提供的标准示例和驱动框架。

- **网络协议栈**
  1. 双方均存在该模块。
  2. 存在共同 AST 指纹。
  3. 目标覆盖率 60.24%，参考覆盖率 94.34%。覆盖率较高。
  4. 证据强度：**弱**。共同部分集中在 `smoltcp_impl` 和 `axlibc` 的 socket 实现，这是对 `smoltcp` 协议栈和 POSIX 接口的标准封装。

- **第三方库/公共依赖**
  1. 目标项目无独立“第三方库”模块，参考项目有 386 个函数。
  2. 无共同 AST 指纹。
  3. 覆盖率均为 0.0%。
  4. 证据强度：**未形成**。此模块仅存在于参考项目，是目标项目未包含的独立第三方代码，不构成同源证据。

#### 3. 底层 AST 关键证据指纹表
| 所属板块 | 目标项目路径 | 参考项目路径 | 证据类型 | 技术说明 |
| :--- | :--- | :--- | :--- | :--- |
| 任务调度/异步运行时 | `kernel/task/axtask/src/wait_queue.rs#cancel_events` | `arceos/modules/axtask/src/wait_queue.rs#cancel_events` | 公共上游框架 | 等待队列事件取消的标准流程，调用链 `curr.in_wait_queue, curr.ptr_eq, self.queue.lock().retain` 是 `axtask` crate 的通用实现。 |
| 任务调度/异步运行时 | `kernel/task/axtask/src/wait_queue.rs#wait_timeout_until` | `arceos/modules/axtask/src/wait_queue.rs#wait_timeout_until` | 公共上游框架 | 带超时的阻塞等待逻辑，调用 `axhal::time::wall_time, crate::timers::set_alarm_wakeup, current_run_queue::<NoPreemptIrqSave>` 是 ArceOS 调度器的标准范式。 |
| 任务调度/异步运行时 | `kernel/task/axtask/src/run_queue.rs#exit_current` | `arceos/modules/axtask/src/run_queue.rs#exit_current` | 公共上游框架 | 任务退出时的队列清理和等待唤醒逻辑，是 `axtask` 运行队列的核心功能。 |
| 进程管理 | `api/arceos_posix_api/src/imp/task.rs#sys_exit` | `arceos/api/arceos_posix_api/src/imp/task.rs#sys_exit` | 公共上游框架 | POSIX `exit` 系统调用的封装，调用 `axtask::exit` 是标准做法。差异在于目标调用 `axhal::power::system_off`，参考调用 `axhal::misc::terminate`。 |
| 进程管理 | `ulib/axlibc/c/unistd.c#execve` | `arceos/ulib/axlibc/c/unistd.c#execve` | POSIX 标准桩函数 | 未实现的 POSIX 接口，函数体均为 `unimplemented`，由接口规范决定，无独创性。 |
| 文件系统 | `kernel/fs/axfs/src/fs/ext4fs.rs#read_at` | `crates/lwext4_rust/src/inode/file.rs#read_at` | 公共第三方库 | 双方均依赖 `lwext4_rust` 库实现 ext4 读取，但调用路径和封装方式不同。 |
| 设备驱动 | `tools/bwbench_client/src/main.rs#transmit_benchmark` | `arceos/tools/bwbench_client/src/main.rs#transmit_benchmark` | 公共示例代码 | 网络带宽测试工具，是 ArceOS 官方提供的标准示例，非项目核心逻辑。 |
| 网络协议栈 | `kernel/net/axnet/src/smoltcp_impl/tcp.rs#connect` | `arceos/modules/axnet/src/smoltcp_impl/tcp.rs#connect` | 公共上游框架 | 对 `smoltcp` 库 TCP socket 连接操作的标准封装，调用 `SOCKET_SET.add, SocketSetWrapper::new_tcp_socket` 是固定模式。 |

#### 4. 核心代码调用流对比剖析
* **【进程管理/任务退出路径】**
* **底层技术析理**: 双方均实现了 `sys_exit` 系统调用，并最终调用 `axtask::exit` 退出当前任务。然而，在最终的硬件操作上，目标项目调用 `axhal::power::system_off` 执行系统关机，而参考项目调用 `axhal::misc::terminate` 终止当前硬件线程。这反映了双方在硬件抽象层（HAL）选择和退出语义上的根本差异，目标项目倾向于更彻底的电源管理，而参考项目则停留在 CPU 级别终止。
* **代码溯源链接**:
  - 目标项目: `api/arceos_posix_api/src/imp/task.rs#sys_exit`
  - 参考项目: `arceos/api/arceos_posix_api/src/imp/task.rs#sys_exit`

* **【任务调度/等待队列机制】**
* **底层技术析理**: 双方在 `wait_queue.rs` 中的实现高度相似，均基于 `SpinNoIrq` 和 `VecDeque` 构建。这是因为它们都直接继承自 ArceOS 的 `axtask` crate。该 crate 提供了完整的等待队列抽象，包括 `wait_until`、`notify_one`、`cancel_events` 等标准方法。任何基于 ArceOS 的项目都会复用此基础设施，其调用流 `current_run_queue::<NoPreemptIrqSave>().blocked_resched` 是框架规定的调度入口，不具备项目间的独创性。
* **代码溯源链接**:
  - 目标项目: `kernel/task/axtask/src/wait_queue.rs#wait_until`
  - 参考项目: `arceos/modules/axtask/src/wait_queue.rs#wait_until`

* **【内存管理/标准库实现】**
* **底层技术析理**: 在 `strftime.rs` 等 C 标准库函数的 Rust 移植中，双方代码结构一致。这是因为 `strftime` 的格式化逻辑由 POSIX 标准严格定义，其内部解析状态机（如 `inner_strftime` 函数）的实现方式高度受限，导致不同实现者在遵循同一规范时产生相似的 AST 结构。这属于算法与规范的必然相似，而非代码抄袭。
* **代码溯源链接**:
  - 目标项目: `ulib/axlibc/src/strftime.rs#strftime_inner`
  - 参考项目: `arceos/ulib/axlibc/src/strftime.rs#strftime_inner`

#### 5. 单方模块与降权项
- **单方模块**：参考项目中的 `第三方库` 模块（386个函数）及其下的 `crates/visionfive2-sd`、`crates/page_table_multiarch` 等子模块，在目标项目中无对应实现。这表明双方在硬件支持和底层架构上存在显著差异，是独立开发的强有力证据。
- **降权项**：
  - **跨板块同名函数**：控方将 `kernel/fs/axfs/src/mounts.rs#procfs`（文件系统）归入【进程管理】板块进行指控，属于跨板块硬凑，已降权为弱信号。
  - **公共接口与宏**：`syscall_body!`、`debug!`、`println!`、`unimplemented` 等泛型宏或标准桩函数，因其通用性和非特异性，不能作为同源证据。
  - **C 标准库桩函数**：`execve`、`waitpid`、`pthread_cond_wait` 等函数体仅为 `unimplemented`，其相似性由 POSIX 标准接口签名决定，无代码复用价值。

#### 6. 保守结论
综合评估，目标项目与参考项目的客观 AST 相似度仅为 21.62%，目标项目拥有 2042 个唯一 AST 指纹，远超 900 个共同指纹，表明其绝大部分代码为独立实现。双方在 `axtask`、`axhal`、`axlibc` 等模块的相似性，可明确归因于共同依赖的 ArceOS 开源上游框架及 POSIX 标准规范。在核心系统调用、内存管理、硬件抽象层等关键路径上，双方展现出不同的设计选择与实现细节。控方指控主要基于跨板块同名硬凑和对公共框架代码的误判，缺乏项目间直接抄袭的实锤。因此，整体原创性风险低，无充分证据支持抄袭结论。

------------------------------

