# 🛡️ OS 内核赛自动化代码级架构审计报告

**审计时间**: `2026-07-02 04:54:38`
**参赛作品名称**: `2026-T2026104609910022-XX`
**年份**: `2026`
**学校**: `XX`
**队伍名称**: `T2026104609910022`
**比赛名字**: `OS-Competition`
**比赛子赛道**: `Kernel-Implementation`
**仓库地址**: `https://gitlab.eduxiji.net/educg-group-43501-3132633/T2026104609910022-73`
**推测开发语言**: `Rust / C (由AST探针自动识别)`
**特征函数量**: `1159`

## ⚔️ 底层代码同源性对比 (AST Homology)
> 本部分由专门的代码审查智能体，对该项目与符合年份条件的历届最高相似度项目进行控制流与底层调用对比。新版报告会先给出确定性的全模块覆盖矩阵，再给出 Agent 的分模块解释，避免只围绕少量抽样函数展开。

### 🏆 Top 5 底层对标: vs `2024-chaos-北京科技大学`（北京科技大学 / chaos）

#### 对方作品信息

**对方年份**: `2024`
**对方学校**: `北京科技大学`
**对方队伍名称**: `chaos`
**对方比赛名字**: `OS-Competition`
**对方比赛子赛道**: `Kernel-Implementation`
**对方仓库地址**: `https://gitlab.eduxiji.net/educg-group-26010-2376550/oskernel2024-chaos`

> 双向覆盖率：共同 AST 指纹: 27；唯一 AST 指纹: 目标项目 613，参考项目 798；覆盖率: 目标项目 4.4%，参考项目 3.38%

## 全模块对比总览（确定性证据）

> 总体覆盖率：共同 AST 指纹: 27；唯一 AST 指纹: 目标项目 613，参考项目 798；覆盖率: 目标项目 4.4%，参考项目 3.38%

### 1. 模块覆盖矩阵
| 模块 | 目标函数数 | 参考函数数 | 共同唯一 AST 指纹 | Target 覆盖 | Base 覆盖 | 状态 |
| :--- | ---: | ---: | ---: | ---: | ---: | :--- |
| 进程管理 | 280 | 45 | 2 | 1.49% | 4.55% | 双方存在且有结构重合 |
| 任务调度/异步运行时 | 135 | 76 | 1 | 1.54% | 1.43% | 双方存在且有结构重合 |
| 内存管理 | 265 | 178 | 17 | 11.41% | 10.18% | 双方存在且有结构重合 |
| 中断/异常处理 | 10 | 5 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |
| 系统调用 | 12 | 67 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |
| 文件系统 | 295 | 152 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |
| 设备驱动 | 53 | 19 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |
| 网络协议栈 | 0 | 0 | 0 | 0.0% | 0.0% | 双方均未体现 |
| 文件系统/第三方库 | 48 | 270 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |
| 第三方库 | 0 | 50 | 0 | 0.0% | 0.0% | 仅参考项目体现 |
| 通用模块/其他 | 61 | 75 | 5 | 11.63% | 7.35% | 双方存在且有结构重合 |

### 2. 分模块证据清单

#### 进程管理
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `src-la/syscall/process.rs#sys_execve` | `os/src/syscall/process.rs#sys_execve` | 同板块同名 | Some, String::from, Vec::new, arg.as_str, arg.clone, arg_refs.push ...(+34) | String::from, Vec::new, all_data.as_slice, args.add, args_vec.insert, args_vec.len ...(+22) |
| `src-la/syscall/process.rs#sys_clone` | `os/src/syscall/process.rs#sys_clone` | 同板块同名 | TaskControlBlock::clone_thread, TaskControlBlock::fork, add_task, child.acquire_inner_lock, crate::fs::manager::FS_MANAGER.lock, crate::fs::manager::FS_MANAGER.lock().as_mut ...(+8) | CloneFlags::from_bits, CloneFlags::from_bits((flags & !CSIGNAL) as u32).unwrap, SignalFlags::from_bits, SignalFlags::from_bits(1 << ((flags & CSIGNAL) - 1)).unwrap, clone_signals.contains, ctid.is_null ...(+13) |
| `src-la/syscall/process.rs#sys_uname` | `os/src/syscall/process.rs#sys_uname` | 同板块同名 | UserBuffer::new, arr[..len].copy_from_slice, bytes.len, bytes.len().min, core::mem::size_of::<UTSName>, core::slice::from_raw_parts ...(+5) | "Machine: riscv64".as_bytes, "None".as_bytes, SYS_NAME.as_bytes, SYS_NODENAME.as_bytes, SYS_RELEASE.as_bytes, SYS_VERSION.as_bytes ...(+15) |
| `src-la/syscall/process.rs#sys_kill` | `os/src/syscall/process.rs#sys_kill` | 同板块同名 | crate::task::signal_task, crate::task::terminate_ready_task, current.acquire_inner_lock, current_task, current_task().unwrap, signal_bit | SignalFlags::from_bits, file!, line!, pid2process, process.inner_exclusive_access, trace! |

#### 任务调度/异步运行时
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `src-la/syscall/fs.rs#sys_fstat` | `os/src/syscall/fs.rs#sys_fstat` | 同板块同名 | FS_MANAGER.lock, crate::task::current_user_token, mgr.as_ref, mgr.stat, translated_refmut | current_task, current_task().unwrap, file!, file.clone, file.fstat, inner.fd_table.len ...(+8) |
| `src-la/syscall/fs.rs#sys_linkat` | `os/src/syscall/fs.rs#sys_linkat` | 同板块同名 | FS_MANAGER.lock, crate::task::current_user_token, mgr.as_mut, mgr.link, mgr.resolve_at_path, newpath.as_bytes ...(+2) | curdir.inode, curdir.inode().link, curdir.inode().lookup, curdir.inode().lookup(old_name.as_str()).unwrap, current_task, current_task()
        .unwrap ...(+8) |
| `src-la/syscall/fs.rs#sys_getcwd` | `os/src/syscall/fs.rs#sys_getcwd` | 同板块同名 | FS_MANAGER.lock, UserBuffer::new, Vec::new, crate::task::current_user_token, cwd.len, mgr.as_ref ...(+5) | core::cmp::min, current_task, current_task()
        .unwrap, current_task()
        .unwrap()
        .inner_exclusive_access, current_task()
        .unwrap()
        .inner_exclusive_access(file!(), line!())
        .work_dir
        .clone, current_task()
        .unwrap()
        .inner_exclusive_access(file!(), line!())
        .work_dir
        .clone()
        .name ...(+13) |
| `src-la/syscall/fs.rs#sys_writev` | `os/src/syscall/fs.rs#sys_writev` | 同板块同名 | crate::task::current_user_token, read_iovecs, sys_write | core::mem::size_of::<Iovec>, core::slice::from_raw_parts, current_task, current_task().unwrap, file!, file.clone ...(+10) |

#### 内存管理
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `src/trap/mod.rs#trap_handler` | `os/src/trap/mod.rs#trap_handler` | 同板块同名 | crate::task::check_per_task_timeout, crate::task::exit_current_and_run_next, current_task, current_trap_cx, do_wake_expired, drop ...(+12) | check_signals_of_current, check_timer, current_add_signal, current_task, current_task()
                .unwrap, current_task()
                .unwrap()
                .inner_exclusive_access ...(+24) |
| `src/task/task.rs#fork` | `os/src/task/task.rs#fork` | 同板块同名 | Arc::clone, Arc::downgrade, Arc::new, KernelStack::alloc, MemorySet::from_existed_user, Mutex::new ...(+13) | (MapPermission::R | MapPermission::W).bits, Arc::clone, Arc::downgrade, Arc::new, MemorySet::from_existed_user, PTEFlags::from_bits ...(+42) |
| `src-la/task/task.rs#exec` | `os/src/task/task.rs#exec` | 同板块同名 | Err, KERNEL_SPACE.lock, KERNEL_SPACE.lock().token, MemorySet::new_bare, Ok, TrapContext::app_init_context ...(+13) | KERNEL_SPACE.exclusive_access, KERNEL_SPACE.exclusive_access(file!(), line!()).token, MemorySet::from_elf, TaskContext::goto_user_entry, TrapContext::app_init_context, assert_eq! ...(+22) |
| `src-la/mm/memory_set.rs#new_kernel` | `os/src/mm/memory_set.rs#new_kernel` | 同板块同名 | PageTable::new, PageTable::new().expect, PhysAddr, PhysAddr(trampoline_pa).floor, PhysPageNum, Vec::new ...(+4) | ((*pair).0 + (*pair).1 + (KERNEL_SPACE_OFFSET << PAGE_SIZE_BITS)).into, ((*pair).0 + (KERNEL_SPACE_OFFSET << PAGE_SIZE_BITS)).into, (ebss as usize).into, (edata as usize).into, (ekernel as usize).into, (erodata as usize).into ...(+10) |

#### 中断/异常处理
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `src-la/loongarch_csr.rs#enable_timer_interrupt` | `os/src/trap/mod.rs#enable_timer_interrupt` | 同板块同名 | read_csr::<CSR_CRMD>, read_csr::<CSR_ECFG>, write_csr::<CSR_CRMD>, write_csr::<CSR_ECFG> | sie::set_stimer |
| `src/trap/mod.rs#set_user_trap_entry` | `os/src/trap/mod.rs#set_user_trap_entry` | 同板块同名 | stvec::write | stvec::write |
| `src/trap/mod.rs#set_kernel_trap_entry` | `os/src/trap/mod.rs#set_kernel_trap_entry` | 同板块同名 | stvec::write | stvec::write |
| `src-la/trap/mod.rs#trap_from_kernel` | `os/src/fs/pipe.rs#read` | 同模块上下文对齐/差异观察 | panic! | assert!, buf.into_iter, buf.len, buf_iter.next, debug!, drop ...(+11) |

#### 系统调用
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `user/src/main.rs#sys_write` | `user/src/syscall.rs#sys_write` | 同板块同名 | asm! | buffer.as_ptr, buffer.len, syscall |
| `user/src/main.rs#sys_getpid` | `user/src/syscall.rs#sys_getpid` | 同板块同名 | asm! | syscall |
| `user/src/lib.rs#sys_sleep` | `user/src/syscall.rs#sys_sleep` | 同板块同名 | syscall3 | syscall |
| `user/src/main.rs#sys_exit` | `user/src/syscall.rs#sys_exit` | 同板块同名 | asm! | panic!, syscall |

#### 文件系统
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `src-la/fs/manager.rs#write_at` | `os/src/fs/fat32/inode.rs#write_at` | 同板块同名 | buf.len, buf.to_vec, crate::sbi::console_putchar, node.data.len, node.data.resize, node.data[offset..end].copy_from_slice ...(+8) | buf.len, cluster_buf[pos % CLUSTER_SIZE..pos % CLUSTER_SIZE + copy_size]
                .copy_from_slice, fs.cluster_chain, fs.read_cluster, fs.write_cluster, min ...(+2) |
| `src-la/fs/manager.rs#read_at` | `os/src/fs/fat32/inode.rs#read_at` | 同板块同名 | available.min, buf.fill, buf.len, buf[..n].copy_from_slice, clone.offset.set, clone.read ...(+8) | buf.len, buf[read_size..read_size + copy_size]
                .copy_from_slice, dentry.file_size, fs.cluster_chain, fs.read_cluster, min ...(+3) |
| `src-la/fs/manager.rs#rename` | `os/src/fs/fat32/inode.rs#rename` | 同板块同名 | child.path.extend_from_slice, child.path.push, child.path.starts_with, child.path[prefix.len()..].to_vec, new.clone, old.clone ...(+5) | todo! |
| `src-la/lang_items.rs#panic` | `os/src/lang_items.rs#panic` | 同板块同名 | crate::console::print, format_args!, info.location, info.message, shutdown | info.location, println!, shutdown |

#### 设备驱动
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `src-la/drivers/block.rs#probe_first` | `os/src/utils/platform_info.rs#fmt` | 同模块上下文对齐/差异观察 | Some, crate::println!, init_legacy_io_bar, pci_cfg_read16, pci_cfg_read8 | Ok, core::str::from_utf8, core::str::from_utf8(&self.model[..index]).unwrap, core::str::from_utf8(&x[..self.bootargs_len]).unwrap, self
            .bootargs
            .as_ref, self
            .bootargs
            .as_ref()
            .map ...(+11) |
| `src-la/drivers/block.rs#transfer_sector` | `os/src/block/block_cache.rs#get_block_cache` | 同模块上下文对齐/差异观察 | Err, Ok, addr_of!, addr_of_mut!, avail_idx.wrapping_add, buf.as_mut_ptr ...(+8) | Arc::clone, Arc::new, Arc::strong_count, BlockCache::new, Mutex::new, panic! ...(+8) |
| `src-la/drivers/block.rs#read_sector` | `os/src/drivers/block/virtio_blk.rs#read_block` | 同模块上下文对齐/差异观察 | Err, Ok, addr_of!, addr_of_mut!, buf.as_mut_ptr, compiler_fence ...(+7) | error!, panic!, res.is_err, res.unwrap, self.0.lock, self.0.lock().read_blocks ...(+1) |
| `src/drivers/block.rs#read_sector` | `os/src/drivers/block/mod.rs#block_device_test` | 同模块上下文对齐/差异观察 | Err, Ok, addr_of!, addr_of_mut!, buf.as_mut_ptr, compiler_fence ...(+7) | BLOCK_DEVICE.clone, assert_eq!, println!, write_buffer.iter_mut |

#### 文件系统/第三方库
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `src-la/fs/ext4.rs#lookup` | `os/src/fs/ext4/inode.rs#lookup` | 同板块同名 | Err, Some, entries.min, i_block.copy_from_slice, len.min, parse_dir_block ...(+6) | Arc::new, Dentry::new, Ext4File::new, Some, UPSafeCell::new, self.fs
            .ext4
            .ext4_open_from ...(+2) |
| `src-la/drivers/block.rs#scan_ext4_directory` | `os/libs/ext4_rs/src/ext4_interface.rs#ext4_generic_open` | 同模块上下文对齐/差异观察 | Err, Ok, crate::println!, i_block.copy_from_slice, merge_dir_scan, parse_ext4_dir_block ...(+4) | DirEntryType::EXT4_DE_DIR.bits, Errnum::ENOENT.into, Ext4DirEntry::default, Ext4DirSearchResult::new, Ext4InodeRef::get_inode_ref, Ext4InodeRef::new ...(+17) |
| `src/drivers/block.rs#scan_ext4_directory` | `os/libs/ext4_rs/src/ext4_interface.rs#ext4_file_read` | 同模块上下文对齐/差异观察 | Err, Ok, crate::println!, i_block.copy_from_slice, merge_dir_scan, parse_ext4_dir_block ...(+4) | Ext4InodeRef::get_inode_ref, Ok, core::cmp::min, inode_ref.get_inode_dblk_idx, inode_ref.inner.inode.ext4_inode_type, inode_ref.inner.inode.inode_get_size ...(+8) |
| `src-la/drivers/block.rs#scan_ext4_root` | `os/libs/ext4_rs/src/ext4_interface.rs#ext4_generic_open_from` | 同模块上下文对齐/差异观察 | Err, Ok, Some, crate::println!, load_elf64_segments, print_ext4_file_prefix ...(+3) | DirEntryType::EXT4_DE_DIR.bits, Errnum::ENOENT.into, Ext4DirEntry::default, Ext4DirSearchResult::new, Ext4InodeRef::get_inode_ref, Ext4InodeRef::new ...(+17) |

#### 第三方库
仅参考项目中观察到该模块代表函数，目标项目没有同模块可对齐证据：
- 参考项目：`os/libs/visionfive2-sd/src/lib.rs#send_cmd`，调用摘要：RawInterrupt::from, RawInterruptStatusReg::from, S::sleep_ms_until, Some, arg.into, assert! ...(+21)
- 参考项目：`os/libs/visionfive2-sd/src/lib.rs#init_sdcard`，调用摘要：CardTypeReg::from, CardTypeReg::from(0).with_card_width4_1, CmdArg::new, CmdReg::from, ControlReg::from, RawInterruptStatusReg::from ...(+18)
- 参考项目：`os/libs/visionfive2-sd/src/register.rs#from`，调用摘要：CmdReg::with_data, CmdReg::with_data(0, value.into()).with_transfer_dir, CmdReg::with_no_data, CmdReg::with_no_data(0, value.into())
                    .with_check_response_crc, CmdReg::with_no_data(0, value.into())
                    .with_check_response_crc(false)
                    .with_response_length, CmdReg::with_no_data(0, value.into()).with_check_response_crc ...(+3)
- 参考项目：`os/libs/visionfive2-sd/src/lib.rs#reset_clock`，调用摘要：ClockDividerReg::new, ClockDividerReg::new().with_clk_divider0, ClockEnableReg::from, CmdArg::new, CmdReg::from, CmdReg::from(0)
        .with_start_cmd ...(+7)

#### 通用模块/其他
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `src/timer.rs#sub` | `os/src/timer.rs#sub` | 同板块同名 | 内部闭环逻辑 | TimeSpec::from_ns, TimeSpec::new, other.to_ns, self.to_ns |
| `user/src/lib.rs#write_str` | `os/src/console.rs#write_str` | 同板块同名 | Ok, bytes.len, bytes.len().min, s.as_bytes, self.buf[self.len..self.len + copy_len].copy_from_slice | Ok, console_putchar, s.chars |
| `src/sbi.rs#console_getchar` | `os/src/sbi.rs#console_getchar` | 同板块同名 | Some, sbi_rt::legacy::console_getchar | sbi_call |
| `src-la/timer.rs#add` | `os/src/timer.rs#add` | 同板块同名 | 内部闭环逻辑 | 内部闭环逻辑 |


好的，仲裁官。基于提供的证据底座、控辩双方陈述及原始AST抽样数据，本庭现对“[2026-T2026104609910022-XX] vs [2024-chaos-北京科技大学]”同源性争议进行最终定性仲裁。

### 1. 宏观判定
- **定性结论**: **独立开发，底层架构显著差异**
- **核心依据**: 客观AST相似度仅为1.95%，双向覆盖率均低于5%。该数值远低于认定“抄袭”或“直接复用”的阈值。在操作系统内核这种高度模块化且存在大量公共范式（如POSIX接口、Rust标准库惯用法、rCore教学骨架）的领域，此相似度水平完全可以用独立开发、遵循共同规范和参考公共教学资源来解释。控方提出的“铁证”在统计意义上不成立，且被辩方从技术层面有效反驳。

### 2. 全模块覆盖矩阵解读
| 模块 | 双方存在 | 共同指纹 | 覆盖率解读 | 证据强度 |
| :--- | :---: | :---: | :--- | :---: |
| **进程管理** | 是 | 2 | 目标1.49%，参考4.55%。重合度极低，仅有的两个指纹为Rust标准惯用法。 | **弱** |
| **任务调度/异步运行时** | 是 | 1 | 目标1.54%，参考1.43%。覆盖率极低，表明调度框架实现路径不同。 | **弱** |
| **内存管理** | 是 | 17 | 目标11.41%，参考10.18%。覆盖率相对最高，但指纹多为`Arc::new`, `Mutex::new`等Rust标准库和`MemorySet::from_existed_user`等rCore教学框架通用API。 | **中（可解释）** |
| **中断/异常处理** | 是 | 0 | 覆盖率均为0.0%。双方存在但未观察到共同结构指纹，说明底层架构（LoongArch vs RISC-V）差异导致实现完全不同。 | **未形成** |
| **系统调用** | 是 | 0 | 覆盖率均为0.0%。同名函数由POSIX规范决定，但内部调用图谱无重合，实现独立。 | **未形成** |
| **文件系统** | 是 | 0 | 覆盖率均为0.0%。双方存在但未观察到共同结构指纹，文件系统实现（如ext4 vs FAT32）和抽象层完全不同。 | **未形成** |
| **设备驱动** | 是 | 0 | 覆盖率均为0.0%。双方存在但未观察到共同结构指纹，驱动模型和硬件平台不同。 | **未形成** |
| **网络协议栈** | 否 | 0 | 双方均未体现。 | **未形成** |
| **文件系统/第三方库** | 是 | 0 | 覆盖率均为0.0%。双方存在但未观察到共同结构指纹。 | **未形成** |
| **第三方库** | 仅参考 | 0 | 仅参考项目体现，目标项目无对应模块。 | **未形成** |
| **通用模块/其他** | 是 | 5 | 目标11.63%，参考7.35%。指纹多为`console_getchar`等SBI调用或`write_str`等基础函数，属于公共接口或简单逻辑。 | **弱** |

### 3. 底层 AST 关键证据指纹表
| 所属板块 | 目标项目路径 | 参考项目路径 | 证据类型 | 技术说明 |
| :--- | :--- | :--- | :--- | :--- |
| 进程管理 | `src-la/task/processor.rs#new` | `os/src/task/processor.rs#new` | 同板块同名 | 均调用 `TaskContext::zero_init`。此为rCore教学框架中任务上下文的标准初始化方式，属于公共知识，非独创逻辑。 |
| 进程管理 | `src-la/task/processor.rs#take_current` | `os/src/task/processor.rs#take_current` | 同板块同名 | 均调用 `self.current.take`。此为 `Option<T>` 类型的标准方法，是Rust语言解决此问题的唯一惯用写法，不构成独创性表达。 |
| 内存管理 | `src-la/mm/memory_set.rs#new_kernel` | `os/src/mm/memory_set.rs#new_kernel` | 同板块同名 | 均调用 `PageTable::new`。此为任何分页式内存管理系统的必要初始化步骤，且函数签名和逻辑受硬件MMU机制约束，属于有限表达。 |
| 内存管理 | `src/task/task.rs#fork` | `os/src/task/task.rs#fork` | 同板块同名 | 均调用 `Arc::new`, `MemorySet::from_existed_user`。`Arc`是Rust标准智能指针，`from_existed_user`是rCore教程中经典的进程内存空间复制方法，属于公共教学框架。 |
| 通用模块/其他 | `src/sbi.rs#console_getchar` | `os/src/sbi.rs#console_getchar` | 同板块同名 | 均调用SBI的`console_getchar`接口。这是RISC-V SBI规范定义的标准接口，任何遵循该规范的内核实现都必然相同。 |

### 4. 核心代码调用流对比剖析
* **【进程管理/`sys_execve`系统调用】**
* **底层技术析理**: 两者均实现了POSIX标准的`execve`调用，但内部架构完全不同。目标项目采用全局文件系统管理器（`FS_MANAGER.lock`）和独立的ELF解释器加载路径（`load_interpreter`, `elf_interpreter`），并硬编码了busybox路径。参考项目则通过指针遍历参数（`c_ptr_to_string`）和`args_vec`进行参数处理，未见独立的解释器加载逻辑。这表明两者在文件系统抽象、进程加载流程上存在根本性设计差异。
* **代码溯源链接**:
  - 目标项目: `src-la/syscall/process.rs#sys_execve`
  - 参考项目: `os/src/syscall/process.rs#sys_execve`

* **【进程管理/`sys_clone`系统调用】**
* **底层技术析理**: 两者均实现`clone`系统调用，但底层对象模型截然不同。目标项目直接操作 `TaskControlBlock` 对象，调用其 `clone_thread` 或 `fork` 方法，并通过 `FS_MANAGER` 处理文件描述符。参考项目则基于位标志（`CloneFlags::from_bits`）和信号集（`SignalFlags::from_bits`）进行条件判断，再执行后续逻辑。这体现了面向对象封装与面向过程标志位判断两种不同的设计哲学。
* **代码溯源链接**:
  - 目标项目: `src-la/syscall/process.rs#sys_clone`
  - 参考项目: `os/src/syscall/process.rs#sys_clone`

* **【内存管理/`trap_handler`中断处理】**
* **底层技术析理**: 这是内核的核心路径，两者差异巨大。目标项目的处理流程涉及 `check_per_task_timeout`、`do_wake_expired` 等超时和唤醒机制。参考项目则侧重于信号处理（`check_signals_of_current`、`current_add_signal`）和定时器检查（`check_timer`）。两者在中断处理的核心关注点和控制流上完全不同，反映了不同的调度和信号处理模型。
* **代码溯源链接**:
  - 目标项目: `src/trap/mod.rs#trap_handler`
  - 参考项目: `os/src/trap/mod.rs#trap_handler`

### 5. 单方模块与降权项
*   **第三方库（仅参考项目）**: 参考项目包含 `os/libs/visionfive2-sd` 等第三方SD卡驱动库，目标项目中无任何对应证据。若存在抄袭，这部分代码理应被复用或移植，其缺失是独立开发的有力佐证。
*   **跨板块配对证据**: 控方证据中存在大量跨模块配对，如将目标项目的 `trap_from_kernel` 与参考项目的 `pipe.rs#read` 配对，将 `probe_first` 与 `platform_info.rs#fmt` 配对。这些配对功能领域完全不同，仅因个别通用调用（如`panic!`）而关联，属于**跨板块硬凑**，本庭不予采信，并在此降权为无效证据。
*   **公共接口与泛型函数名**: 所有以 `sys_` 开头的函数名均由Linux系统调用规范定义，`new`、`clone`、`fork`、`exec` 等是操作系统领域的通用术语。这些同名现象不能作为同源性证据。

### 6. 保守结论
经对全模块覆盖矩阵、底层AST指纹及核心调用流的深度析理，本庭裁定指控不成立。目标项目与参考项目的客观AST相似度（1.95%）和双向覆盖率（均<5%）极低，处于独立开发的正常统计波动范围内。控方提出的两个“铁证”（`TaskContext::zero_init` 与 `self.current.take`）经技术剖析，实为Rust语言标准惯用法和rCore教学框架的通用范式，不具备独创性。双方在进程管理、内存管理等核心板块的调用流上展现出截然不同的设计哲学、对象模型和同步策略，且存在LoongArch/RISC-V的底层架构鸿沟。综上，现有证据无法证明目标项目对参考项目存在直接代码复用或抄袭行为，其相似度可被公共知识、规范约束和独立开发所完全解释。

------------------------------

