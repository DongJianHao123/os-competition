# 🛡️ OS 内核赛自动化代码级架构审计报告

**审计时间**: `2026-07-02 04:54:38`
**参赛作品名称**: `2026-T2026104609910022-XX`
**年份**: `2026`
**学校**: `XX`
**队伍名称**: `T2026104609910022`
**比赛名字**: `OS-Competition`
**比赛子赛道**: `Kernel-Implementation`
**仓库地址**: `https://gitlab.eduxiji.net/educg-group-43501-3132633/T2026104609910022-73`
**推测开发语言**: `Rust / C (由AST探针自动识别)`
**特征函数量**: `1159`

## ⚔️ 底层代码同源性对比 (AST Homology)
> 本部分由专门的代码审查智能体，对该项目与符合年份条件的历届最高相似度项目进行控制流与底层调用对比。新版报告会先给出确定性的全模块覆盖矩阵，再给出 Agent 的分模块解释，避免只围绕少量抽样函数展开。

### 🏆 Top 1 底层对标: vs `2022-S0S-哈尔滨工业大学`（哈尔滨工业大学 / S0S）

#### 对方作品信息

**对方年份**: `2022`
**对方学校**: `哈尔滨工业大学`
**对方队伍名称**: `S0S`
**对方比赛名字**: `OS-Competition`
**对方比赛子赛道**: `Kernel-Implementation`
**对方仓库地址**: `https://gitlab.eduxiji.net/educg-group-14158-894147/oskernel2022-s0s`

> 双向覆盖率：共同 AST 指纹: 33；唯一 AST 指纹: 目标项目 613，参考项目 447；覆盖率: 目标项目 5.38%，参考项目 7.38%

## 全模块对比总览（确定性证据）

> 总体覆盖率：共同 AST 指纹: 33；唯一 AST 指纹: 目标项目 613，参考项目 447；覆盖率: 目标项目 5.38%，参考项目 7.38%

### 1. 模块覆盖矩阵
| 模块 | 目标函数数 | 参考函数数 | 共同唯一 AST 指纹 | Target 覆盖 | Base 覆盖 | 状态 |
| :--- | ---: | ---: | ---: | ---: | ---: | :--- |
| 进程管理 | 280 | 51 | 5 | 3.73% | 10.64% | 双方存在且有结构重合 |
| 任务调度/异步运行时 | 135 | 41 | 2 | 3.08% | 5.0% | 双方存在且有结构重合 |
| 内存管理 | 265 | 139 | 18 | 12.08% | 14.17% | 双方存在且有结构重合 |
| 中断/异常处理 | 10 | 5 | 1 | 10.0% | 25.0% | 双方存在且有结构重合 |
| 系统调用 | 12 | 35 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |
| 文件系统 | 295 | 166 | 2 | 1.32% | 1.34% | 双方存在且有结构重合 |
| 设备驱动 | 53 | 42 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |
| 网络协议栈 | 0 | 0 | 0 | 0.0% | 0.0% | 双方均未体现 |
| 文件系统/第三方库 | 48 | 0 | 0 | 0.0% | 0.0% | 仅目标项目体现 |
| 通用模块/其他 | 61 | 31 | 3 | 6.98% | 10.34% | 双方存在且有结构重合 |

### 2. 分模块证据清单

#### 进程管理
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `src-la/syscall/process.rs#sys_uname` | `os/src/syscall/process.rs#sys_uname` | 同板块同名 | UserBuffer::new, arr[..len].copy_from_slice, bytes.len, bytes.len().min, core::mem::size_of::<UTSName>, core::slice::from_raw_parts ...(+5) | UserBuffer::new, Utsname::new, buffer.read, current_user_token, size_of::<Utsname>, translated_byte_buffer ...(+1) |
| `src-la/task/processor.rs#run_tasks` | `os/src/task/processor.rs#run_tasks` | 同板块同名 | PROCESSOR.lock, Some, __switch, crate::fs::manager::FS_MANAGER.lock, crate::fs::manager::FS_MANAGER.lock().as_mut, crate::sbi::shutdown ...(+7) | PROCESSORS[hartid()].lock, Some, __switch, drop, fetch_task, hartid ...(+2) |
| `src-la/syscall/process.rs#sys_kill` | `os/src/syscall/process.rs#sys_kill` | 同板块同名 | crate::task::signal_task, crate::task::terminate_ready_task, current.acquire_inner_lock, current_task, current_task().unwrap, signal_bit | SignalFlags::from_bits, pid2task, task.inner_exclusive_access, task_ref.signals.contains, task_ref.signals.insert |
| `src-la/syscall/process.rs#sys_times` | `os/src/syscall/process.rs#sys_times` | 同板块同名 | TimeSpec::now, current_user_token, now.sec.saturating_mul, translated_refmut | current_user_token, get_time, translated_refmut |

#### 任务调度/异步运行时
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `src-la/syscall/fs.rs#sys_getdents64` | `os/src/syscall/fs.rs#sys_getdents64` | 同板块同名 | ((start_offset + emitted + 1) as u64).to_le_bytes, (e.ino as u64).to_le_bytes, (reclen as u16).to_le_bytes, FS_MANAGER.lock, UserBuffer::new, Vec::new ...(+21) | UserBuffer::new, buf.read, current_task, current_task().unwrap, current_user_token, inner.fd_table.len ...(+4) |
| `src-la/syscall/fs.rs#sys_fstat` | `os/src/syscall/fs.rs#sys_fstat` | 同板块同名 | FS_MANAGER.lock, crate::task::current_user_token, mgr.as_ref, mgr.stat, translated_refmut | UserBuffer::new, buf.read, core::mem::size_of::<Kstat>, current_task, current_task().unwrap, current_user_token ...(+5) |
| `src/main.rs#rust_main` | `os/src/main.rs#rust_main` | 同板块同名 | clear_bss, console::log_init, crate::println!, crate::sbi::shutdown, drivers::init, fs::init ...(+6) | LOCK.load, LOCK.store, clear_bss, fs::add_initproc_shell, fs::list_apps, hartid ...(+11) |
| `src-la/syscall/fs.rs#sys_readlinkat` | `os/src/syscall/fs.rs#sys_readlinkat` | 同板块同名 | FS_MANAGER.lock, UserBuffer::new, UserBuffer::new(buffers).write, crate::task::current_user_token, mgr.as_ref, mgr.readlink_path ...(+6) | "/".as_bytes, UserBuffer::new, buf.read, current_user_token, println!, translated_byte_buffer ...(+2) |

#### 内存管理
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `src/trap/mod.rs#trap_handler` | `os/src/trap/mod.rs#trap_handler` | 同板块同名 | crate::task::check_per_task_timeout, crate::task::exit_current_and_run_next, current_task, current_trap_cx, do_wake_expired, drop ...(+12) | check_signals_error_of_current, current_add_signal, current_trap_cx, exit_current_and_run_next, handle_signals, panic! ...(+9) |
| `src/task/task.rs#fork` | `os/src/task/task.rs#fork` | 同板块同名 | Arc::clone, Arc::downgrade, Arc::new, KernelStack::alloc, MemorySet::from_existed_user, Mutex::new ...(+13) | Arc::downgrade, Arc::new, KernelStack::new, MemorySet::from_existed_user, Mutex::new, SignalFlags::empty ...(+20) |
| `src-la/syscall/process.rs#sys_mmap` | `os/src/syscall/process.rs#sys_mmap` | 同板块同名 | alloc::vec!, base.checked_add, base.checked_add(length).map_or, copy_to_current_user, crate::fs::manager::FS_MANAGER.lock, current_task ...(+9) | MapFlags::from_bits, MapFlags::from_bits(flags).unwrap, ProtFlags::from_bits, ProtFlags::from_bits(prot).unwrap, UserBuffer::new, Vec::new ...(+20) |
| `src-la/mm/memory_set.rs#new_kernel` | `os/src/mm/memory_set.rs#new_kernel` | 同板块同名 | PageTable::new, PageTable::new().expect, PhysAddr, PhysAddr(trampoline_pa).floor, PhysPageNum, Vec::new ...(+4) | ((*pair).0 + (*pair).1).into, (*pair).0.into, (ebss as usize).into, (edata as usize).into, (ekernel as usize).into, (erodata as usize).into ...(+11) |

#### 中断/异常处理
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `src-la/trap/mod.rs#trap_from_kernel` | `os/src/trap/mod.rs#trap_from_kernel` | 同板块同名 | panic! | panic!, println! |
| `src-la/loongarch_csr.rs#enable_timer_interrupt` | `os/src/trap/mod.rs#enable_timer_interrupt` | 同板块同名 | read_csr::<CSR_CRMD>, read_csr::<CSR_ECFG>, write_csr::<CSR_CRMD>, write_csr::<CSR_ECFG> | sie::set_stimer |
| `src/trap/mod.rs#set_user_trap_entry` | `os/src/trap/mod.rs#set_user_trap_entry` | 同板块同名 | stvec::write | stvec::write |
| `src/trap/mod.rs#set_kernel_trap_entry` | `os/src/trap/mod.rs#set_kernel_trap_entry` | 同板块同名同结构 | stvec::write | stvec::write |

#### 系统调用
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `user/src/main.rs#sys_write` | `user/src/syscall.rs#sys_write` | 同板块同名 | asm! | buffer.as_ptr, buffer.len, syscall |
| `user/src/main.rs#sys_getpid` | `user/src/syscall.rs#sys_getpid` | 同板块同名 | asm! | syscall |
| `user/src/main.rs#sys_exit` | `user/src/syscall.rs#sys_exit` | 同板块同名 | asm! | panic!, syscall |
| `user_bin/write.c#_start` | `user/src/syscall.rs#syscall` | 同模块上下文对齐/差异观察 | 内部闭环逻辑 | asm! |

#### 文件系统
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `src-la/fs/manager.rs#write_at` | `fat32/src/vfs.rs#write_at` | 同板块同名 | buf.len, buf.to_vec, crate::sbi::console_putchar, node.data.len, node.data.resize, node.data[offset..end].copy_from_slice ...(+8) | (offset + buf.len()).min, Arc::clone, buf.len, buf.len().min, dst.copy_from_slice, end_current_block.min ...(+17) |
| `src-la/fs/manager.rs#read_at` | `fat32/src/vfs.rs#read_at` | 同板块同名 | available.min, buf.fill, buf.len, buf[..n].copy_from_slice, clone.offset.set, clone.read ...(+8) | (offset + buf.len()).min, Arc::clone, buf.len, buf.len().min, dst.copy_from_slice, end_current_block.min ...(+12) |
| `src-la/fs/manager.rs#rename` | `os/src/fs/inode.rs#rename` | 同板块同名 | child.path.extend_from_slice, child.path.push, child.path.starts_with, child.path[prefix.len()..].to_vec, new.clone, old.clone ...(+5) | UserBuffer::new, drop, inner.inode.write, inode.is_dir, inode.remove, mkdir_at ...(+6) |
| `src-la/fs/manager.rs#seek` | `os/src/fs/inode.rs#seek` | 同板块同名 | file.offset.get, file.offset.set, self.files.get, self.files.get(fd).and_then, self.files.get_mut, self.files.get_mut(fd).and_then ...(+1) | self.inner_exclusive_access |

#### 设备驱动
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `src-la/drivers/block.rs#read_sector` | `os/src/drivers/block/sdcard.rs#read_sector` | 同板块同名 | Err, Ok, addr_of!, addr_of_mut!, buf.as_mut_ptr, compiler_fence ...(+7) | Err, Ok, assert!, chunk.iter_mut, chunk.iter_mut().zip, data_buf.chunks_mut ...(+6) |
| `src-la/drivers/block.rs#write_sector` | `os/src/drivers/block/sdcard.rs#write_sector` | 同板块同名 | self.transfer_sector | (data_buf.len() / SEC_LEN).try_into, (data_buf.len() / SEC_LEN).try_into().unwrap, Err, Ok, assert!, chunk.iter ...(+9) |
| `src/drivers/block.rs#read_sector` | `fat32/src/block_cache.rs#get_block_cache` | 同模块上下文对齐/差异观察 | Err, Ok, addr_of!, addr_of_mut!, buf.as_mut_ptr, compiler_fence ...(+7) | Arc::clone, Arc::new, Arc::strong_count, BlockCache::new, RwLock::new, panic! ...(+8) |
| `src-la/drivers/block.rs#init_legacy_io_bar` | `os/src/drivers/block/sdcard.rs#init_sdcard` | 同模块上下文对齐/差异观察 | crate::println!, pci_cfg_read32, pci_cfg_write32, pci_io_to_cpu | 115_200.bps, Peripherals::steal, SDCard::new, assert!, io_init, k210_hal::clock::Clocks::new ...(+10) |

#### 文件系统/第三方库
仅目标项目中观察到该模块代表函数，参考项目没有同模块可对齐证据：
- 目标项目：`src-la/drivers/block.rs#scan_ext4_directory`，调用摘要：Err, Ok, crate::println!, i_block.copy_from_slice, merge_dir_scan, parse_ext4_dir_block ...(+4)
- 目标项目：`src/drivers/block.rs#scan_ext4_directory`，调用摘要：Err, Ok, crate::println!, i_block.copy_from_slice, merge_dir_scan, parse_ext4_dir_block ...(+4)
- 目标项目：`src-la/drivers/block.rs#scan_ext4_root`，调用摘要：Err, Ok, Some, crate::println!, load_elf64_segments, print_ext4_file_prefix ...(+3)
- 目标项目：`src/drivers/block.rs#scan_ext4_root`，调用摘要：Err, Ok, Some, crate::println!, load_elf64_segments, print_ext4_file_prefix ...(+3)

#### 通用模块/其他
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `user/src/lib.rs#write_str` | `os/src/console.rs#write_str` | 同板块同名 | Ok, bytes.len, bytes.len().min, s.as_bytes, self.buf[self.len..self.len + copy_len].copy_from_slice | Ok, console_putchar, s.chars |
| `src/sbi.rs#console_getchar` | `os/src/sbi.rs#console_getchar` | 同板块同名 | Some, sbi_rt::legacy::console_getchar | sbi_call |
| `src-la/timer.rs#add` | `os/src/timer.rs#add` | 同板块同名 | 内部闭环逻辑 | 内部闭环逻辑 |
| `src-la/main.rs#clear_bss` | `os/src/main.rs#clear_bss` | 同板块同名同结构 | core::slice::from_raw_parts_mut, core::slice::from_raw_parts_mut(
            sbss as usize as *mut u8,
            ebss as usize - sbss as usize,
        )
        .fill | core::slice::from_raw_parts_mut, core::slice::from_raw_parts_mut(sbss as usize as *mut u8, ebss as usize - sbss as usize)
            .fill |


好的，我将遵循审计指令，以仲裁专家身份，基于提供的证据底座、控辩双方论点及原始AST抽样数据，对本案进行最终定性分析。

### 1. 宏观判定
- **定性结论**: **独立开发 (底层架构显著差异)**
- **核心依据**: 客观AST相似度仅为 **3.21%**，远低于认定实质性相似的阈值。双向覆盖率（目标项目5.38%，参考项目7.38%）表明，两项目在抽象语法树层面的重合度极低，属于采用相同编程语言（Rust）和相似教学框架（rCore范式）下的背景噪声级相似。控方提出的4项“绝对同构”指控，经技术析理，均为Rust语言标准库惯用法或操作系统课程设计的必然、极简表达，不构成可保护的创造性表达复用。

### 2. 全模块覆盖矩阵解读
| 模块 | 双方存在 | 共同AST指纹 | 目标覆盖率 | 参考覆盖率 | 证据强度 | 解读 |
| :--- | :---: | :---: | :---: | :---: | :---: | :--- |
| **进程管理** | 是 | 5 | 3.73% | 10.64% | **弱** | 尽管函数数量多且存在同名函数，但覆盖率极低。控方主证据均为单调用链的极简函数，属语言惯用法。`sys_execve`与`sys_exec`等核心实现调用图谱差异显著，表明独立实现。 |
| **任务调度/异步运行时** | 是 | 2 | 3.08% | 5.0% | **弱** | 覆盖率极低。`run_tasks`等核心调度函数因架构差异（单核 `PROCESSOR` vs 多核 `PROCESSORS[hartid()]`）导致AST访问路径不同，未被列为强证据。 |
| **内存管理** | 是 | 18 | 12.08% | 14.17% | **中** | 覆盖率相对最高，但证据清单显示双方在`fork`、`sys_mmap`等核心函数中，内存分配、映射标志处理、锁机制等具体实现存在显著差异，非简单复制。 |
| **中断/异常处理** | 是 | 1 | 10.0% | 25.0% | **弱** | 函数基数小，覆盖率波动大。`trap_handler`等核心处理逻辑的调用图谱（如信号处理、超时检查）完全不同，表明独立设计。`set_kernel_trap_entry`等同名函数仅因调用RISC-V/LoongArch CSR指令而相似，属硬件规范决定。 |
| **系统调用** | 是 | 0 | 0.0% | 0.0% | **未形成** | 双方均存在该模块，但未观察到任何共同AST指纹。用户态`sys_write`等函数因调用`asm!`宏或`syscall`指令而相似，属系统调用ABI的必然实现。 |
| **文件系统** | 是 | 2 | 1.32% | 1.34% | **弱** | 覆盖率极低。目标项目采用全局`FS_MANAGER`锁管理器模式，参考项目采用`inode`直接操作模式，底层设计哲学不同。`write_at`/`read_at`等函数调用图谱差异巨大。 |
| **设备驱动** | 是 | 0 | 0.0% | 0.0% | **未形成** | 双方均存在该模块，但未观察到共同AST指纹。目标项目基于LoongArch PCI，参考项目基于K210 SDCard，硬件平台完全不同，驱动实现必然异构。 |
| **网络协议栈** | 否 | 0 | 0.0% | 0.0% | **未形成** | 双方均未体现该模块。 |
| **文件系统/第三方库** | 仅目标 | 0 | 0.0% | 0.0% | **未形成** | 目标项目独有的`scan_ext4_directory`等EXT4解析逻辑，是其独立开发的强有力证据。 |
| **通用模块/其他** | 是 | 3 | 6.98% | 10.34% | **弱** | `clear_bss`函数因实现逻辑极简（调用`from_raw_parts_mut`和`fill`）而相似，属嵌入式系统标准启动流程，不具原创性。 |

### 3. 底层 AST 关键证据指纹表
| 所属板块 | 目标项目路径 | 参考项目路径 | 证据类型 | 技术说明 |
| :--- | :--- | :--- | :--- | :--- |
| 进程管理 | `[src-la/task/processor.rs]` `new` | `[os/src/task/processor.rs]` `new` | 弱信号 | 均调用`TaskContext::zero_init`。这是对任务上下文结构体进行零初始化的标准构造函数调用，属于功能必然表达。 |
| 进程管理 | `[src-la/task/processor.rs]` `current` | `[os/src/task/processor.rs]` `current` | 弱信号 | 均使用`self.current.as_ref().map(...)`链式调用。这是访问`Option<Arc<T>>`内部值的Rust语言标准惯用法，非创意表达。 |
| 进程管理 | `[src-la/task/processor.rs]` `take_current` | `[os/src/task/processor.rs]` `take_current` | 弱信号 | 均调用`self.current.take()`。这是`Option::take`标准库方法，用于取出所有权并留下`None`，无可替代的原创写法。 |
| 进程管理 | `[src-la/syscall/process.rs]` `sys_yield` | `[os/src/syscall/process.rs]` `sys_yield` | 弱信号 | 均调用`suspend_current_and_run_next`。`yield`系统调用的语义就是主动放弃CPU，此调用是rCore教学范式的标准实现模板。 |
| 内存管理 | `[src/task/task.rs]` `fork` | `[os/src/task/task.rs]` `fork` | 差异证据 | 目标项目使用`Arc::new`、`Mutex::new`、`KernelStack::alloc`；参考项目使用`KernelStack::new`、`SignalFlags::empty`。内存分配与初始化流程不同。 |
| 内存管理 | `[src-la/syscall/process.rs]` `sys_mmap` | `[os/src/syscall/process.rs]` `sys_mmap` | 差异证据 | 目标项目使用`base.checked_add`、`copy_to_current_user`；参考项目使用`MapFlags::from_bits`、`UserBuffer::new`。参数解析与内存操作路径异构。 |
| 文件系统 | `[src-la/fs/manager.rs]` `write_at` | `[fat32/src/vfs.rs]` `write_at` | 差异证据 | 目标项目通过`node.data.resize`和`copy_from_slice`直接操作内存；参考项目通过`end_current_block.min`等块缓存逻辑操作。抽象层次不同。 |
| 中断/异常处理 | `[src/trap/mod.rs]` `trap_handler` | `[os/src/trap/mod.rs]` `trap_handler` | 差异证据 | 目标项目调用`check_per_task_timeout`、`do_wake_expired`；参考项目调用`check_signals_error_of_current`、`handle_signals`。异常分发与处理策略完全不同。 |

### 4. 核心代码调用流对比剖析
* **【进程管理/exec系统调用】**
* **底层技术析理**: 目标项目的`sys_execve`与参考项目的`sys_exec`虽然功能相同，但底层实现路径完全不同。目标项目围绕busybox解释器构建，调用流为`load_interpreter` -> `argv.push` -> `b"./busybox".as_slice`，体现了对脚本解释器的支持。参考项目则直接加载ELF应用，调用流为`app_inode.read_all` -> `args.add` -> `MemorySet::from_elf`。两者在文件加载、参数处理和内存映射的AST节点上无重合，表明是面向不同应用场景的独立实现。
* **代码溯源链接**:
  - 目标项目: `src-la/syscall/process.rs#sys_execve`
  - 参考项目: `os/src/syscall/process.rs#sys_exec`

* **【内存管理/fork实现】**
* **底层技术析理**: 在进程复制的核心逻辑上，目标项目`fork`函数调用`Arc::new`、`Mutex::new`、`KernelStack::alloc`，显示其采用“分配新栈”的模型。参考项目`fork`函数调用`KernelStack::new`、`SignalFlags::empty`，显示其采用“创建新栈结构并初始化信号”的模型。两者在内核栈对象的创建、任务控制块字段的初始化序列上存在明显差异，非同一代码的简单修改。
* **代码溯源链接**:
  - 目标项目: `src/task/task.rs#fork`
  - 参考项目: `os/src/task/task.rs#fork`

* **【中断/异常处理/trap_handler】**
* **底层技术析理**: 这是内核最关键的控制流之一。目标项目的`trap_handler`调用`check_per_task_timeout`和`do_wake_expired`，表明其集成了基于时间片的抢占式调度逻辑。参考项目的`trap_handler`调用`check_signals_error_of_current`和`handle_signals`，表明其侧重于POSIX信号处理。两者在trap分发后的核心处理路径上完全不同，是内核设计哲学的根本性差异。
* **代码溯源链接**:
  - 目标项目: `src/trap/mod.rs#trap_handler`
  - 参考项目: `os/src/trap/mod.rs#trap_handler`

### 5. 单方模块与降权项
* **单方模块（目标项目独有）**:
  - **文件系统/第三方库**: 目标项目包含`scan_ext4_directory`、`scan_ext4_root`等EXT4文件系统解析函数，参考项目无此模块。这是目标项目独立开发的强有力证据，表明其具备超出参考项目范畴的原创工作。
* **降权项说明**:
  - **跨板块同名/同Hash**: 如用户态的`sys_write`与内核态的`sys_write`，或不同模块中均出现的`Vec::new`、`String::from`等。这些属于系统调用ABI规定或Rust标准库的通用接口，其AST指纹相同是必然的，不构成跨模块复用的证据。
  - **架构差异导致的弱信号**: 所有涉及`PROCESSOR.lock`（单核）与`PROCESSORS[hartid()].lock`（多核）的同名函数（如`run_tasks`、`schedule`），其底层AST访问路径已因架构抽象不同而改变，控方已主动降级，本庭予以确认。
  - **极简函数**: 函数体仅包含一个标准库调用或一个调度原语的函数（如`sys_yield`、`take_current`），其相似性源于语言规范或教学模板，不构成可保护的表达。

### 6. 保守结论
综合全模块证据，目标项目与参考项目的客观AST相似度仅为3.21%，处于极低水平。控方提出的4项“绝对同构”指控，经技术审查，均为Rust语言标准库惯用法或操作系统课程设计的必然、极简表达，不构成代码复用铁证。相反，双方在进程模型（busybox vs 原生ELF）、内存管理（`KernelStack::alloc` vs `KernelStack::new`）、中断处理（时间片调度 vs 信号处理）、文件系统（全局管理器 vs inode操作）以及底层ISA架构（LoongArch vs RISC-V）等核心子系统上，均表现出实质性、根本性的设计差异。目标项目独有的EXT4支持模块进一步佐证了其独立开发能力。因此，整体风险极低，应认定为基于相同教学框架和编程语言的独立开发作品，不构成对参考项目的代码抄袭。

------------------------------

