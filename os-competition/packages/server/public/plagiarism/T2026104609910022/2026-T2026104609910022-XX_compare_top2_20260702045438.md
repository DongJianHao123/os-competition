# 🛡️ OS 内核赛自动化代码级架构审计报告

**审计时间**: `2026-07-02 04:54:38`
**参赛作品名称**: `2026-T2026104609910022-XX`
**年份**: `2026`
**学校**: `XX`
**队伍名称**: `T2026104609910022`
**比赛名字**: `OS-Competition`
**比赛子赛道**: `Kernel-Implementation`
**仓库地址**: `https://gitlab.eduxiji.net/educg-group-43501-3132633/T2026104609910022-73`
**推测开发语言**: `Rust / C (由AST探针自动识别)`
**特征函数量**: `1159`

## ⚔️ 底层代码同源性对比 (AST Homology)
> 本部分由专门的代码审查智能体，对该项目与符合年份条件的历届最高相似度项目进行控制流与底层调用对比。新版报告会先给出确定性的全模块覆盖矩阵，再给出 Agent 的分模块解释，避免只围绕少量抽样函数展开。

### 🏆 Top 2 底层对标: vs `2022-段错误-杭州电子科技大学`（杭州电子科技大学 / 段错误）

#### 对方作品信息

**对方年份**: `2022`
**对方学校**: `杭州电子科技大学`
**对方队伍名称**: `段错误`
**对方比赛名字**: `OS-Competition`
**对方比赛子赛道**: `Kernel-Implementation`
**对方仓库地址**: `https://gitlab.eduxiji.net/19061120/oskernel2022-segmentfault.git`

> 双向覆盖率：共同 AST 指纹: 33；唯一 AST 指纹: 目标项目 613，参考项目 517；覆盖率: 目标项目 5.38%，参考项目 6.38%

## 全模块对比总览（确定性证据）

> 总体覆盖率：共同 AST 指纹: 33；唯一 AST 指纹: 目标项目 613，参考项目 517；覆盖率: 目标项目 5.38%，参考项目 6.38%

### 1. 模块覆盖矩阵
| 模块 | 目标函数数 | 参考函数数 | 共同唯一 AST 指纹 | Target 覆盖 | Base 覆盖 | 状态 |
| :--- | ---: | ---: | ---: | ---: | ---: | :--- |
| 进程管理 | 280 | 52 | 7 | 5.22% | 16.28% | 双方存在且有结构重合 |
| 任务调度/异步运行时 | 135 | 56 | 1 | 1.54% | 1.85% | 双方存在且有结构重合 |
| 内存管理 | 265 | 183 | 19 | 12.75% | 11.31% | 双方存在且有结构重合 |
| 中断/异常处理 | 10 | 6 | 1 | 10.0% | 20.0% | 双方存在且有结构重合 |
| 系统调用 | 12 | 36 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |
| 文件系统 | 295 | 226 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |
| 设备驱动 | 53 | 37 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |
| 网络协议栈 | 0 | 0 | 0 | 0.0% | 0.0% | 双方均未体现 |
| 文件系统/第三方库 | 48 | 0 | 0 | 0.0% | 0.0% | 仅目标项目体现 |
| 通用模块/其他 | 61 | 41 | 3 | 6.98% | 7.89% | 双方存在且有结构重合 |

### 2. 分模块证据清单

#### 进程管理
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `src-la/syscall/process.rs#sys_uname` | `os/src/syscall/process.rs#sys_uname` | 同板块同名 | UserBuffer::new, arr[..len].copy_from_slice, bytes.len, bytes.len().min, core::mem::size_of::<UTSName>, core::slice::from_raw_parts ...(+5) | UTSNAME.lock, UserBuffer::new, core::mem::size_of::<Utsname>, current_user_token, translated_byte_buffer, uname.as_bytes ...(+1) |
| `src-la/syscall/process.rs#sys_ppoll` | `os/src/syscall/process.rs#sys_ppoll` | 同板块同名 | core::mem::size_of::<PollFd>, crate::fs::manager::FS_MANAGER.lock, current_user_token, guard.as_ref, mgr.poll_events, suspend_current_and_run_next ...(+1) | 内部闭环逻辑 |
| `src-la/syscall/process.rs#sys_sysinfo` | `os/src/syscall/process.rs#sys_sysinfo` | 同板块同名 | TimeSpec::now, current_user_token, translated_refmut | 内部闭环逻辑 |
| `src-la/task/processor.rs#run_tasks` | `os/src/task/processor.rs#run_tasks` | 同板块同名 | PROCESSOR.lock, Some, __switch, crate::fs::manager::FS_MANAGER.lock, crate::fs::manager::FS_MANAGER.lock().as_mut, crate::sbi::shutdown ...(+7) | PROCESSOR.lock, Some, __switch, drop, fetch_task, processor.get_idle_task_cx_ptr ...(+1) |

#### 任务调度/异步运行时
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `src-la/syscall/fs.rs#sys_getdents64` | `os/src/syscall/fs.rs#sys_getdents64` | 同板块同名 | ((start_offset + emitted + 1) as u64).to_le_bytes, (e.ino as u64).to_le_bytes, (reclen as u16).to_le_bytes, FS_MANAGER.lock, UserBuffer::new, Vec::new ...(+21) | Dirent::new, UserBuffer::new, current_task, current_task().unwrap, current_user_token, dirent.as_bytes ...(+8) |
| `src-la/syscall/fs.rs#sys_newfstatat` | `os/src/syscall/fs.rs#sys_newfstatat` | 同板块同名 | FS_MANAGER.lock, crate::task::current_user_token, mgr.as_ref, mgr.resolve_at_path, mgr.stat, mgr.stat_path ...(+4) | Kstat::new, UserBuffer::new, current_task, current_task().unwrap, current_user_token, file.get_name ...(+11) |
| `src-la/syscall/fs.rs#sys_fstat` | `os/src/syscall/fs.rs#sys_fstat` | 同板块同名 | FS_MANAGER.lock, crate::task::current_user_token, mgr.as_ref, mgr.stat, translated_refmut | Kstat::new, UserBuffer::new, current_task, current_task().unwrap, current_user_token, file.get_fstat ...(+6) |
| `src-la/syscall/fs.rs#sys_utimensat` | `os/src/syscall/fs.rs#sys_utimensat` | 同板块同名 | FS_MANAGER.lock, TimeSpec::now, core::mem::size_of::<TimeSpec>, crate::task::current_user_token, mgr.as_mut, mgr.resolve_at_path ...(+6) | current_task, current_task().unwrap, current_user_token, file.set_time, inner.fd_table.len, inner.get_work_path ...(+10) |

#### 内存管理
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `src/trap/mod.rs#trap_handler` | `os/src/trap/mod.rs#trap_handler` | 同板块同名 | crate::task::check_per_task_timeout, crate::task::exit_current_and_run_next, current_task, current_trap_cx, do_wake_expired, drop ...(+12) | (stval as usize).into, TRAMPOLINE.into, Trap::Exception, cfg!, current_add_signal, current_task ...(+17) |
| `src-la/task/elf.rs#load_elf` | `os/src/mm/memory_set.rs#load_elf` | 同板块同名 | (0x1000 - page_off).min, ElfFile::new, ElfFile::new(aligned_bytes).expect, VirtAddr, aligned_elf_image, aligned_image.as_ptr ...(+25) | ((ph.virtual_addr() + ph.mem_size()) as usize).into, (ph.virtual_addr() as usize + base_address).into, (ph.virtual_addr() as usize + ph.mem_size() as usize + base_address).into, (ph.virtual_addr() as usize).into, AuxEntry, ChunkArea::new ...(+47) |
| `src/task/task.rs#fork` | `os/src/task/task.rs#fork` | 同板块同名 | Arc::clone, Arc::downgrade, Arc::new, KernelStack::alloc, MemorySet::from_existed_user, Mutex::new ...(+13) | Arc::downgrade, Arc::new, KernelStack::new, MemorySet::from_copy_on_write, Mutex::new, SigSet::new ...(+21) |
| `src-la/mm/memory_set.rs#new_kernel` | `os/src/mm/memory_set.rs#new_kernel` | 同板块同名 | PageTable::new, PageTable::new().expect, PhysAddr, PhysAddr(trampoline_pa).floor, PhysPageNum, Vec::new ...(+4) | ((*pair).0 + (*pair).1).into, (*pair).0.into, (ebss as usize).into, (edata as usize).into, (ekernel as usize).into, (erodata as usize).into ...(+10) |

#### 中断/异常处理
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `src-la/trap/mod.rs#trap_from_kernel` | `os/src/trap/mod.rs#trap_from_kernel` | 同板块同名 | panic! | panic!, println! |
| `src-la/loongarch_csr.rs#enable_timer_interrupt` | `os/src/trap/mod.rs#enable_timer_interrupt` | 同板块同名 | read_csr::<CSR_CRMD>, read_csr::<CSR_ECFG>, write_csr::<CSR_CRMD>, write_csr::<CSR_ECFG> | sie::set_stimer |
| `src/trap/mod.rs#set_user_trap_entry` | `os/src/trap/mod.rs#set_user_trap_entry` | 同板块同名 | stvec::write | stvec::write |
| `src/trap/mod.rs#set_kernel_trap_entry` | `os/src/trap/mod.rs#set_kernel_trap_entry` | 同板块同名同结构 | stvec::write | stvec::write |

#### 系统调用
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `user_bin/write.c#_start` | `user_c/lib/syscall.c#clone` | 同模块上下文对齐/差异观察 | 内部闭环逻辑 | __clone |
| `src-la/mm/memory_set.rs#translated_refmut` | `os/src/syscall/socket.rs#sys_recvfrom` | 同模块上下文对齐/差异观察 | Err, Ok, ptr.is_null, virt_to_phys, virt_to_phys(token, va).ok_or | UserBuffer::new, current_user_token, translated_byte_buffer, userbuf.write |
| `src/mm/memory_set.rs#translated_refmut` | `user_c/lib/syscall.c#get_time` | 同模块上下文对齐/差异观察 | Err, Ok, ptr.is_null, virt_to_phys, virt_to_phys(token, va).ok_or | sys_get_time |
| `user/src/lib.rs#syscall3` | `user_c/lib/syscall.c#exec` | 同模块上下文对齐/差异观察 | asm! | syscall |

#### 文件系统
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `src-la/fs/manager.rs#write_at` | `simple-fat32/src/layout.rs#write_at` | 同板块同名 | buf.len, buf.to_vec, crate::sbi::console_putchar, node.data.len, node.data.resize, node.data[offset..end].copy_from_slice ...(+8) | Arc::clone, buf.len, dst.copy_from_slice, fat.read, fat_reader.get_next_cluster, get_block_cache ...(+9) |
| `src/fs/manager.rs#write_at` | `simple-fat32/src/vfs.rs#write_at` | 同板块同名 | buf.len, buf.to_vec, crate::sbi::console_putchar, node.data.len, node.data.resize, node.data[offset..end].copy_from_slice ...(+8) | buf.len, self.fs.read, self.fs.read().get_fat, self.increase_size, self.modify_short_dirent, short_entry.write_at |
| `src-la/fs/manager.rs#read_at` | `simple-fat32/src/layout.rs#read_at` | 同板块同名 | available.min, buf.fill, buf.len, buf[..n].copy_from_slice, clone.offset.set, clone.read ...(+8) | Arc::clone, buf.len, dst.copy_from_slice, fat.read, fat_reader.get_next_cluster, get_block_cache ...(+9) |
| `src/fs/manager.rs#read_at` | `simple-fat32/src/vfs.rs#read_at` | 同板块同名 | available.min, buf.fill, buf.len, buf[..n].copy_from_slice, clone.offset.set, clone.read ...(+8) | self.fs.read, self.fs.read().get_fat, self.read_short_dirent, short_entry.read_at |

#### 设备驱动
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `src-la/drivers/block.rs#read_sector` | `os/src/drivers/block/sdcard.rs#read_sector` | 同板块同名 | Err, Ok, addr_of!, addr_of_mut!, buf.as_mut_ptr, compiler_fence ...(+7) | Err, Ok, assert!, chunk.iter_mut, chunk.iter_mut().zip, data_buf.chunks_mut ...(+6) |
| `src-la/drivers/block.rs#write_sector` | `os/src/drivers/block/sdcard.rs#write_sector` | 同板块同名 | self.transfer_sector | (data_buf.len() / SEC_LEN).try_into, (data_buf.len() / SEC_LEN).try_into().unwrap, Err, Ok, assert!, chunk.iter ...(+9) |
| `src/drivers/block.rs#read_sector` | `simple-fat32/src/block_cache.rs#get_block_cache` | 同模块上下文对齐/差异观察 | Err, Ok, addr_of!, addr_of_mut!, buf.as_mut_ptr, compiler_fence ...(+7) | Arc::clone, Arc::new, Arc::strong_count, BlockCache::new, RwLock::new, panic! ...(+7) |
| `src-la/drivers/block.rs#init_legacy_io_bar` | `os/src/drivers/block/sdcard.rs#init_sdcard` | 同模块上下文对齐/差异观察 | crate::println!, pci_cfg_read32, pci_cfg_write32, pci_io_to_cpu | 115_200.bps, Peripherals::steal, SDCard::new, assert!, io_init, k210_hal::clock::Clocks::new ...(+10) |

#### 文件系统/第三方库
仅目标项目中观察到该模块代表函数，参考项目没有同模块可对齐证据：
- 目标项目：`src-la/drivers/block.rs#scan_ext4_directory`，调用摘要：Err, Ok, crate::println!, i_block.copy_from_slice, merge_dir_scan, parse_ext4_dir_block ...(+4)
- 目标项目：`src/drivers/block.rs#scan_ext4_directory`，调用摘要：Err, Ok, crate::println!, i_block.copy_from_slice, merge_dir_scan, parse_ext4_dir_block ...(+4)
- 目标项目：`src-la/drivers/block.rs#scan_ext4_root`，调用摘要：Err, Ok, Some, crate::println!, load_elf64_segments, print_ext4_file_prefix ...(+3)
- 目标项目：`src/drivers/block.rs#scan_ext4_root`，调用摘要：Err, Ok, Some, crate::println!, load_elf64_segments, print_ext4_file_prefix ...(+3)

#### 通用模块/其他
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `src/timer.rs#sub` | `os/src/timer.rs#sub` | 同板块同名 | 内部闭环逻辑 | 内部闭环逻辑 |
| `user/src/lib.rs#write_str` | `os/src/console.rs#write_str` | 同板块同名 | Ok, bytes.len, bytes.len().min, s.as_bytes, self.buf[self.len..self.len + copy_len].copy_from_slice | Ok, crate::sbi::console_putchar, s.chars |
| `src/sbi.rs#console_getchar` | `os/src/sbi.rs#console_getchar` | 同板块同名 | Some, sbi_rt::legacy::console_getchar | sbi_call |
| `src-la/timer.rs#add` | `os/src/timer.rs#add` | 同板块同名 | 内部闭环逻辑 | 内部闭环逻辑 |


好的，仲裁庭已收到控辩双方的全部陈述及底层AST证据。现依据客观数据、代码指纹及架构逻辑，做出最终同源性定性仲裁。

---

#### 1. 宏观判定
- **定性结论**: **独立开发，底层架构显著差异**
  客观 AST 相似度仅为 3.01%，目标项目覆盖率 5.38%，参考项目覆盖率 6.38%。双方在核心模块（如文件系统、设备驱动）的共同 AST 指纹为 0。现有证据表明，两项目是基于同一开源教学生态（rCore 风格）的独立演进作品，存在因遵循相同 POSIX 标准、Rust 语言惯用法及 OS 教学范式导致的局部、非实质性调用图谱重合，不构成代码抄袭或非法复用。

#### 2. 全模块覆盖矩阵解读
- **进程管理**: 双方均存在。共同 AST 指纹 7 个，目标覆盖率 5.22%，参考覆盖率 16.28%。重合集中在单行访问器（如 `current_task`）和 POSIX 标准系统调用入口。证据强度：**弱**。
- **任务调度/异步运行时**: 双方均存在。共同 AST 指纹 1 个，目标覆盖率 1.54%，参考覆盖率 1.85%。调度器骨架（`run_tasks`）相似，但目标项目额外集成了文件系统管理器和关机逻辑，差异明显。证据强度：**弱**。
- **内存管理**: 双方均存在。共同 AST 指纹 19 个，目标覆盖率 12.75%，参考覆盖率 11.31%。重合主要源于 ELF 加载、页表创建等通用内存操作，但具体实现（如 `ElfFile::new` vs `ChunkArea`）完全不同。证据强度：**中**。
- **中断/异常处理**: 双方均存在。共同 AST 指纹 1 个，目标覆盖率 10.0%，参考覆盖率 20.0%。重合仅为硬件指令级操作（`stvec::write`），架构差异（LoongArch CSR vs RISC-V SIE）显著。证据强度：**弱**。
- **系统调用**: 双方均存在。共同 AST 指纹 0 个，覆盖率均为 0.0%。未观察到任何共同结构指纹。证据强度：**未形成**。
- **文件系统**: 双方均存在。共同 AST 指纹 0 个，覆盖率均为 0.0%。目标项目采用 `FS_MANAGER` 统一管理并支持 ext4 扫描，参考项目使用 `simple-fat32` 库，设计迥异。证据强度：**未形成**。
- **设备驱动**: 双方均存在。共同 AST 指纹 0 个，覆盖率均为 0.0%。目标项目为 LoongArch PCI 设备，参考项目为 K210 SD 卡，硬件平台完全不同。证据强度：**未形成**。
- **网络协议栈**: 双方均未体现。证据强度：**未形成**。
- **第三方库/公共依赖**: 仅目标项目体现（`文件系统/第三方库` 模块，48 个函数），包含 ext4 扫描等独立功能。参考项目无此模块。证据强度：**未形成**。

#### 3. 底层 AST 关键证据指纹表
| 所属板块 | 目标项目路径 | 参考项目路径 | 证据类型 | 技术说明 |
| :--- | :--- | :--- | :--- | :--- |
| 进程管理 | `src-la/task/processor.rs#run_tasks` | `os/src/task/processor.rs#run_tasks` | 同板块同名 | 主调度循环骨架相似（`PROCESSOR.lock` -> `fetch_task` -> `__switch`），但目标项目额外调用 `FS_MANAGER.lock` 和 `crate::sbi::shutdown`，体现了独立的功能扩展。 |
| 进程管理 | `src-la/syscall/process.rs#sys_execve` | `os/src/syscall/process.rs#sys_exec` | 同板块证据 | 系统调用入口层均出现 `b"./busybox"` 硬编码路径，此为 OS 竞赛/教学中测试 busybox 的通用功能性常量，非独创表达。 |
| 进程管理 | `src-la/syscall/process.rs#sys_waitid` | `os/src/syscall/process.rs#sys_waitpid` | 同板块证据 | 均遍历 `inner.children.iter` 查找子进程，这是 `wait` 系列系统调用的必然实现路径。目标使用 `block_current_and_run_next` 阻塞，参考使用 `inner_exclusive_access` 锁，同步策略不同。 |
| 内存管理 | `src-la/task/elf.rs#load_elf` | `os/src/mm/memory_set.rs#load_elf` | 同板块同名 | ELF 加载入口同名，但实现路径完全不同。目标使用 `ElfFile::new` 解析，参考使用 `ChunkArea` 和手动基址计算，无共同底层指纹。 |
| 内存管理 | `src-la/mm/memory_set.rs#new_kernel` | `os/src/mm/memory_set.rs#new_kernel` | 同板块同名 | 内核页表创建同名，但目标使用 `PageTable::new()` 和 `PhysAddr` 抽象，参考使用 `(ekernel as usize).into()` 等硬编码地址，内存模型不同。 |
| 中断/异常处理 | `src-la/trap/mod.rs#trap_from_kernel` | `os/src/trap/mod.rs#trap_from_kernel` | 同板块同名 | 均调用 `panic!`，这是 Rust 语言处理不可恢复错误的标准宏，无独创性。 |
| 中断/异常处理 | `src/trap/mod.rs#set_kernel_trap_entry` | `os/src/trap/mod.rs#set_kernel_trap_entry` | 同板块同名同结构 | 均调用 `stvec::write`，这是 RISC-V 架构设置陷阱向量的唯一特权指令，属于硬件接口决定的必然写法。 |
| 通用模块/其他 | `src/timer.rs#sub` | `os/src/timer.rs#sub` | 同板块同名 | 均为“内部闭环逻辑”，表明是简单的数据结构操作，无外部依赖，不构成复用证据。 |

#### 4. 核心代码调用流对比剖析
* **【进程管理/调度器核心】**
* **底层技术析理**: 两者均实现了基于 `PROCESSOR` 全局单例的协作式调度器。`run_tasks` 函数都遵循“获取锁 -> 取出任务 -> 上下文切换”的 rCore 经典模式。然而，目标项目在调度循环中集成了 `FS_MANAGER` 的周期性检查和关机逻辑，而参考项目仅包含 `drop` 释放锁。这表明目标项目在参考了通用设计模式的基础上，进行了独立的、面向特定需求的深度修改与功能增强。
* **代码溯源链接**:
  - 目标项目: `src-la/task/processor.rs#run_tasks`
  - 参考项目: `os/src/task/processor.rs#run_tasks`

* **【内存管理/ELF加载】**
* **底层技术析理**: 两者均需将 ELF 文件加载到用户内存空间，但采用了完全不同的技术路径。目标项目使用 `ElfFile::new` 创建一个高级解析对象，通过 `VirtAddr` 进行地址对齐和映射。参考项目则直接操作 ELF 程序头，手动计算 `base_address` 偏移，并使用 `ChunkArea` 进行内存区域管理。这种从抽象层次到具体实现算法的根本性差异，是独立开发的强有力证据。
* **代码溯源链接**:
  - 目标项目: `src-la/task/elf.rs#load_elf`
  - 参考项目: `os/src/mm/memory_set.rs#load_elf`

* **【文件系统/数据读写】**
* **底层技术析理**: 在 `write_at` 和 `read_at` 这两个核心 I/O 操作上，双方实现完全不同。目标项目操作的是 `node.data`（字节数组），逻辑是简单的内存 `copy_from_slice` 和 `resize`，甚至包含向控制台输出的调试代码。参考项目则基于 FAT32 文件系统，通过 `fat_reader` 获取簇链，并使用 `get_block_cache` 进行块设备交互。两者面向的存储模型（内存文件系统 vs 磁盘文件系统）和操作对象截然不同。
* **代码溯源链接**:
  - 目标项目: `src-la/fs/manager.rs#write_at`
  - 参考项目: `simple-fat32/src/layout.rs#write_at`

#### 5. 单方模块与降权项
- **单方模块**: `文件系统/第三方库` 模块仅存在于目标项目，包含 `scan_ext4_directory`、`scan_ext4_root` 等 ext4 文件系统解析函数。这是目标项目独有的功能实现，参考项目完全不具备，是独立开发的强证据。
- **降权项**: 控辩双方均提及的 `sys_getpid`、`sys_yield`、`current_task`、`take_current` 等单行访问器函数，以及 `sys_ppoll`、`sys_sysinfo` 等“内部闭环逻辑”函数，因其逻辑过于简单或属于通用标准范式，表达形式有限，根据审计原则，**降级为弱信号**，不作为判定同源性的主证据。跨板块出现的同名函数（如 `write_at` 在文件系统和第三方库中）已按指令要求，不作为同板块主证据。

#### 6. 保守结论
综合全模块证据，目标项目与参考项目的客观 AST 相似度极低（3.01%），核心重合点仅限于 POSIX 系统调用命名、Rust 标准库惯用法及 rCore 风格的调度器骨架。这些重合在操作系统教学与竞赛语境下，属于遵循相同接口标准和设计模式的正常现象，不构成独创性表达的抄袭。目标项目在架构支持（LoongArch）、文件系统设计（FS_MANAGER + ext4）、设备驱动模型等方面展现出显著的独立开发特征。整体判定为**独立开发，无实质性代码抄袭风险**。

------------------------------

