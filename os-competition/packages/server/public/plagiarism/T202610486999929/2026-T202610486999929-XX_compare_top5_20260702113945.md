# 🛡️ OS 内核赛自动化代码级架构审计报告

**审计时间**: `2026-07-02 11:39:45`
**参赛作品名称**: `2026-T202610486999929-XX`
**年份**: `2026`
**学校**: `XX`
**队伍名称**: `T202610486999929`
**比赛名字**: `OS-Competition`
**比赛子赛道**: `Kernel-Implementation`
**仓库地址**: `https://gitlab.eduxiji.net/educg-group-43501-3132633/T202610486999929-1714`
**推测开发语言**: `Rust / C (由AST探针自动识别)`
**特征函数量**: `2585`

## ⚔️ 底层代码同源性对比 (AST Homology)
> 本部分由专门的代码审查智能体，对该项目与符合年份条件的历届最高相似度项目进行控制流与底层调用对比。新版报告会先给出确定性的全模块覆盖矩阵，再给出 Agent 的分模块解释，避免只围绕少量抽样函数展开。

### 🏆 Top 5 底层对标: vs `2025-NPUcore-BLOSSOM-西北工业大学`（西北工业大学 / NPUcore-BLOSSOM）

#### 对方作品信息

**对方年份**: `2025`
**对方学校**: `西北工业大学`
**对方队伍名称**: `NPUcore-BLOSSOM`
**对方比赛名字**: `OS-Competition`
**对方比赛子赛道**: `Kernel-Implementation`
**对方仓库地址**: `https://gitlab.eduxiji.net/educg-group-36002-2710490/oskernel2025-npucore-blossom`

> 双向覆盖率：共同 AST 指纹: 136；唯一 AST 指纹: 目标项目 2134，参考项目 2354；覆盖率: 目标项目 6.37%，参考项目 5.78%

## 全模块对比总览（确定性证据）

> 总体覆盖率：共同 AST 指纹: 136；唯一 AST 指纹: 目标项目 2134，参考项目 2354；覆盖率: 目标项目 6.37%，参考项目 5.78%

### 1. 模块覆盖矩阵
| 模块 | 目标函数数 | 参考函数数 | 共同唯一 AST 指纹 | Target 覆盖 | Base 覆盖 | 状态 |
| :--- | ---: | ---: | ---: | ---: | ---: | :--- |
| 进程管理 | 0 | 75 | 0 | 0.0% | 0.0% | 仅参考项目体现 |
| 任务调度/异步运行时 | 0 | 143 | 0 | 0.0% | 0.0% | 仅参考项目体现 |
| 内存管理 | 0 | 605 | 0 | 0.0% | 0.0% | 仅参考项目体现 |
| 中断/异常处理 | 0 | 47 | 0 | 0.0% | 0.0% | 仅参考项目体现 |
| 系统调用 | 0 | 65 | 0 | 0.0% | 0.0% | 仅参考项目体现 |
| 文件系统 | 0 | 561 | 0 | 0.0% | 0.0% | 仅参考项目体现 |
| 设备驱动 | 0 | 47 | 0 | 0.0% | 0.0% | 仅参考项目体现 |
| 网络协议栈 | 0 | 88 | 0 | 0.0% | 0.0% | 仅参考项目体现 |
| 文件系统/第三方库 | 489 | 369 | 119 | 30.2% | 41.18% | 双方存在且有结构重合 |
| 网络协议栈/第三方库 | 109 | 138 | 1 | 0.95% | 0.81% | 双方存在且有结构重合 |
| 第三方库 | 1987 | 1228 | 8 | 0.48% | 1.13% | 双方存在且有结构重合 |
| 通用模块/其他 | 0 | 241 | 0 | 0.0% | 0.0% | 仅参考项目体现 |

### 2. 分模块证据清单

#### 进程管理
仅参考项目中观察到该模块代表函数，目标项目没有同模块可对齐证据：
- 参考项目：`os/src/syscall/process.rs#sys_execve`，调用摘要：Box::<[u8; 4]>::new, DEFAULT_SHELL.to_string, Some, Vec::with_capacity, arg_ptr.is_null, argv.add ...(+22)
- 参考项目：`os/src/syscall/process.rs#sys_prlimit`，调用摘要：Resource::from_primitive, assert!, copy_from_user, copy_from_user(token, new_limit, rlimit).is_err, copy_to_user, copy_to_user(
                        token,
                        &(RLimit {
                            rlim_cur: SYSTEM_TASK_LIMIT,
                            rlim_max: SYSTEM_TASK_LIMIT,
                        }),
                        old_limit,
                    )
                    .is_err ...(+18)
- 参考项目：`os/src/syscall/process.rs#sys_futex`，调用摘要：FutexOption::from_bits_truncate, current_task, current_task().unwrap, do_futex_wait, drop, info! ...(+15)
- 参考项目：`os/src/syscall/process.rs#sys_clone`，调用摘要：CloneFlags::from_bits, CloneFlags::from_bits(flags & !0xff).unwrap, Signals::empty, Signals::from_signum, add_task, current_task ...(+7)

#### 任务调度/异步运行时
仅参考项目中观察到该模块代表函数，目标项目没有同模块可对齐证据：
- 参考项目：`os/src/syscall/mod.rs#syscall`，调用摘要：Errno::try_from, [
            //black list
            SYSCALL_YIELD,
            // SYSCALL_READ,
            SYSCALL_WRITE,
            SYSCALL_GETDENTS64,
            SYSCALL_READV,
            SYSCALL_WRITEV,
            SYSCALL_PSELECT6,
            SYSCALL_SIGACTION,
            SYSCALL_SIGPROCMASK,
            // SYSCALL_WAIT4,
            // SYSCALL_GETPPID,
            SYSCALL_CLOCK_GETTIME,
        ]
        .contains, args.len, crate::task::current_task, crate::task::current_task()
                .unwrap, crate::task::current_task()
                .unwrap()
                .acquire_inner_lock ...(+107)
- 参考项目：`os/src/syscall/fs.rs#sys_copy_file_range`，调用摘要：Some, Vec::new, Vec::with_capacity, buf.set_len, current_task, current_task().unwrap ...(+18)
- 参考项目：`os/src/syscall/mod.rs#syscall_name`，调用摘要：内部闭环逻辑
- 参考项目：`os/src/syscall/fs.rs#sys_splice`，调用摘要：Some, Vec::<u8>::with_capacity, buffer.as_mut_slice, buffer.as_slice, buffer.set_len, current_task ...(+17)

#### 内存管理
仅参考项目中观察到该模块代表函数，目标项目没有同模块可对齐证据：
- 参考项目：`user/src/bin/initproc.rs#main`，调用摘要："-c\0".as_ptr, "./basic_testcode.sh\0".as_ptr, "./busybox cp hello /tmp\0".as_ptr, "./busybox echo \"#### OS COMP TEST GROUP END libctest-musl ####\"\0".as_ptr, "./busybox echo \"#### OS COMP TEST GROUP END lmbench-glibc ####\"\0".as_ptr, "./busybox echo \"#### OS COMP TEST GROUP END lmbench-musl ####\"\0".as_ptr ...(+237)
- 参考项目：`os/src/task/signal.rs#do_signal`，调用摘要：(trap_cx as *const TrapContext).cast::<MachineContext>, SigInfo::new, SignalStack::new, Signals::SIGSEGV.to_signum, Signals::SIGSEGV.to_signum().unwrap, Signals::from_bits_truncate ...(+40)
- 参考项目：`os/src/hal/arch/loongarch64/trap/mod.rs#trap_handler`，调用摘要：(0..sz).rev, BadV::read, BadV::read().get_vaddr, ERA::read, ERA::read().next_ins, ERA::read().next_ins().write ...(+55)
- 参考项目：`os/src/mm/memory_set.rs#do_page_fault`，调用摘要：Err, Ok, PhysAddr::from, VirtAddr::from, addr.floor, addr.page_offset ...(+34)

#### 中断/异常处理
仅参考项目中观察到该模块代表函数，目标项目没有同模块可对齐证据：
- 参考项目：`os/src/hal/arch/loongarch64/trap/mod.rs#trap_from_kernel`，调用摘要：((addr + i) as *mut u8).read_unaligned, ((addr + i) as *mut u8).write_unaligned, (0..sz).rev, BadV::read, BadV::read().get_vaddr, EStat::read ...(+14)
- 参考项目：`os/src/hal/arch/loongarch64/trap/mem_access.rs#get_addr`，调用摘要：self.get_op_code, self.get_op_code().map, self.get_rj_num, self.get_rk_num, self.get_si12, self.get_si14
- 参考项目：`os/src/hal/arch/loongarch64/trap/mem_access.rs#get_size`，调用摘要：内部闭环逻辑
- 参考项目：`os/src/hal/arch/loongarch64/trap/mem_access.rs#is_store`，调用摘要：内部闭环逻辑

#### 系统调用
仅参考项目中观察到该模块代表函数，目标项目没有同模块可对齐证据：
- 参考项目：`os/src/net/tcp.rs#_read`，调用摘要：Err, GeneralRet::Err, GeneralRet::Ok, NET_INTERFACE.poll, NET_INTERFACE.tcp_socket, Ok ...(+7)
- 参考项目：`os/src/net/tcp.rs#_accept`，调用摘要：Err, GeneralRet::Err, GeneralRet::Ok, NET_INTERFACE.poll, NET_INTERFACE.tcp_socket, Ok ...(+7)
- 参考项目：`os/src/net/udp.rs#connect`，调用摘要：Err, IpListenEndpoint::from, NET_INTERFACE.poll, NET_INTERFACE.udp_socket, Ok, SocketAddrv4::new ...(+11)
- 参考项目：`os/src/net/udp.rs#_read`，调用摘要：Err, GeneralRet::Err, GeneralRet::Ok, NET_INTERFACE.poll, NET_INTERFACE.udp_socket, Ok ...(+10)

#### 文件系统
仅参考项目中观察到该模块代表函数，目标项目没有同模块可对齐证据：
- 参考项目：`os/src/fs/directory_tree.rs#open`，调用摘要：(*last_comp).to_string, Arc::downgrade, Err, Ok, PATH_CACHE.lock, REDIRECT_TO_BUSYBOX.contains ...(+26)
- 参考项目：`os/src/fs/directory_tree.rs#rename`，调用摘要：Arc::downgrade, Arc::new, Err, Mutex::new, Ok, ROOT.cd_comp ...(+41)
- 参考项目：`apps/kilo/kilo.c#editorRefreshScreen`，调用摘要：abAppend, abFree, editorSyntaxToColor, snprintf, strlen, time ...(+1)
- 参考项目：`os/src/fs/directory_tree.rs#init_device_directory`，调用摘要："null".to_string, "rtc".to_string, "tty".to_string, "urandom".to_string, "zero".to_string, Arc::downgrade ...(+21)

#### 设备驱动
仅参考项目中观察到该模块代表函数，目标项目没有同模块可对齐证据：
- 参考项目：`os/src/drivers/block/sata_blk.rs#pci_init`，调用摘要：AHCI::new, Some, Status::empty, dev.bars.iter, dev.bars.iter().enumerate, dev.bars.iter().enumerate().for_each ...(+3)
- 参考项目：`os/src/net/config.rs#new`，调用摘要：Config::new, EthernetAddress, EthernetAddress([0x02, 0x00, 0x00, 0x00, 0x00, 0x01]).into, Instant::from_millis, Interface::new, IpAddress::v4 ...(+12)
- 参考项目：`os/src/drivers/block/virtio_blk.rs#read_block`，调用摘要：assert!, buf.chunks_mut, buf.chunks_mut(VIRT_IO_BLOCK_SZ).enumerate, self.0
                .lock, self.0
                .lock()
                .read_blocks, self.0
                .lock()
                .read_blocks(virtio_block_id as usize, chunk)
                .expect
- 参考项目：`os/src/drivers/block/virtio_blk.rs#write_block`，调用摘要：assert!, buf.chunks, buf.chunks(VIRT_IO_BLOCK_SZ).enumerate, self.0
                .lock, self.0
                .lock()
                .write_blocks, self.0
                .lock()
                .write_blocks(virtio_block_id as usize, chunk)
                .expect

#### 网络协议栈
仅参考项目中观察到该模块代表函数，目标项目没有同模块可对齐证据：
- 参考项目：`os/src/net/tcp.rs#new`，调用摘要：Mutex::new, NET_INTERFACE.add_socket, NET_INTERFACE.poll, RNG.positive_u32, info!, socket::tcp::Socket::new ...(+2)
- 参考项目：`os/src/net/udp.rs#new`，调用摘要：Mutex::new, NET_INTERFACE.add_socket, NET_INTERFACE.poll, log::info!, socket::udp::PacketBuffer::new, socket::udp::Socket::new ...(+1)
- 参考项目：`os/src/net/tcp.rs#drop`，调用摘要：NET_INTERFACE.poll, NET_INTERFACE.remove, NET_INTERFACE.tcp_socket, info!, socket.close, socket.is_open
- 参考项目：`os/src/net/mod.rs#can_bind`，调用摘要：Some, log::info!, self.0.clone, socket.loacl_endpoint, socket.loacl_endpoint().eq, socket.socket_type ...(+1)

#### 文件系统/第三方库
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `crates/scarlet/ext4_rs_fixed/src/ext4_impls/file.rs#write_at` | `os/src/fs/ext4/file.rs#write_at` | 同板块同名 | Block::load, Err, Ok, allocated_blocks.len, block.data.copy_from_slice, block.sync_blk_to_disk ...(+18) | Block::load_offset, Ok, block.sync_blk_to_disk, block.write_offset, drop, inode_ref.inode.set_size ...(+10) |
| `crates/scarlet/ext4_rs_fixed/src/ext4_impls/extents.rs#ext_remove_leaf` | `os/src/fs/ext4/extent.rs#ext_remove_leaf` | 同板块同名 | Block::load, Block::load_inode_root_block, Ext4Extent::default, Ok, core::slice::from_raw_parts, ex.get_actual_len ...(+21) | Block::load_inode_root_block, Block::load_offset, Ext4Extent::default, Ok, ex.get_actual_len, ex.get_pblock ...(+14) |
| `crates/scarlet/ext4_rs_fixed/src/ext4_impls/extents.rs#insert_new_extent` | `os/src/fs/ext4/extent.rs#insert_new_extent` | 同板块同名 | Block::load, Ok, core::mem::size_of::<Ext4Extent>, core::mem::size_of::<Ext4ExtentHeader>, ext4block.read_offset_as_mut, ext4block.sync_blk_to_disk ...(+10) | Block::load_offset, Ok, core::mem::size_of::<Ext4Extent>, core::mem::size_of::<Ext4ExtentHeader>, ext4block.read_offset_as_mut, ext4block.sync_blk_to_disk ...(+5) |
| `crates/scarlet/ext4_rs_fixed/src/ext4_impls/extents.rs#extent_remove_space` | `os/src/fs/ext4/extent.rs#extent_remove_space` | 同板块同名 | Block::load, Ext4Extent::default, Ok, ex.get_actual_len, ex.get_first_block, ex.get_pblock ...(+13) | Block::load_offset, Ext4Extent::default, Ok, ex.get_actual_len, ex.get_first_block, ex.get_pblock ...(+14) |

#### 网络协议栈/第三方库
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `crates/scarlet/src/net/tcp.rs#receive` | `dependency/dep_iso/src/net/ethernet/intel/e1000.rs#receive` | 同板块同名 | TCP_CONNECTIONS
        .lock, TCP_CONNECTIONS
        .lock()
        .get, TCP_CONNECTIONS
        .lock()
        .get(&(dst_port, src_port, src_ip))
        .cloned, TCP_CONNECTIONS
        .lock()
        .get(&(dst_port, src_port, src_ip))
        .cloned()
        .or_else, buf.payload, data.is_empty ...(+12) | Some, buffer.to_vec, recv_desc.status.get_bit, recv_desc.status.set_bit, self.recv_queue.len, self.registers[E1000_RDT].read ...(+5) |
| `crates/scarlet/src/net/arp.rs#receive` | `dependency/virtio-drivers/src/device/net/dev.rs#receive` | 同板块同名 | EthernetAddr, buf.payload, flush_pending, insert, our_ip, payload.len ...(+2) | Err, Ok, rx_buf.as_bytes_mut, rx_buf.set_packet_len, self.inner.poll_receive, self.inner.receive_complete ...(+2) |
| `crates/scarlet/src/net/ipv4.rs#receive` | `dependency/virtio-drivers/examples/riscv/src/tcp.rs#receive` | 同板块同名 | NetworkBuffer::new, arp::our_ip, buf.payload, ip_checksum, payload.len, payload[ihl..total_len].to_vec ...(+3) | Some, VirtioRxToken, VirtioTxToken, panic!, self.inner.borrow_mut, self.inner.borrow_mut().receive ...(+1) |
| `crates/scarlet/src/net/ipv4.rs#send` | `dependency/dep_iso/src/net/ethernet/intel/e1000.rs#send` | 同板块同名 | LOOPBACK_QUEUE.lock, LOOPBACK_QUEUE.lock().push, arp::lookup, arp::our_ip, arp::queue_pending, arp::send_request ...(+9) | assert!, buffer.len, fence, self.registers[E1000_TDT].read, self.registers[E1000_TDT].write, self.send_queue.len ...(+2) |

#### 第三方库
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `crates/devil/src/header.rs#parse` | `dependency/riscv/descriptor/generator.rs#parse` | 同板块同名 | Err, Ok, Self::read_from_prefix, blob.len, header
            .off_dt_strings
            .checked_add, header
            .off_dt_strings
            .checked_add(header.size_dt_strings)
            .ok_or ...(+6) | EnumerationDescriptor::parse, Some, as_str_polyfill!, desc.split, parts.next, parts.next().unwrap ...(+2) |
| `crates/scarlet/src/fs/procfs/status.rs#generate` | `dependency/riscv/descriptor/generator.rs#generate` | 同板块同名 | String::new, area.end_va, area.start_va, info.is_none, info.unwrap, mm.areas ...(+13) | String::new, bf.generate_enum, format!, self.bfs.iter, write!, write!(&mut bit_sets, "{}", bf.generate_bitops()).unwrap ...(+2) |
| `crates/scarlet/src/arch/shared/virtio_net.rs#send` | `dependency/virtio-drivers/src/device/console.rs#send` | 同板块同名 | avail.idx.wrapping_add, buf.payload, core::hint::spin_loop, fence, last.wrapping_add, mmio.add ...(+9) | Arc::new, Mutex::new, ReadOnly::new, State::new, State::wait_until_queue_notified, VirtIOConsole::<FakeHal, FakeTransport<Config>>::new ...(+12) |
| `crates/scarlet/src/arch/shared/virtio_blk.rs#read_block` | `dependency/dep_iso/src/block/ahci.rs#read_block` | 同板块同名 | Err, Ok, buf.as_mut_ptr, buf.as_mut_ptr().cast, buf.len, core::ptr::addr_of! ...(+11) | buf.len, buf.len().min, buf[..len].clone_from_slice, fis.set_lba, self.port.issue_command, self.port.spin_on_slot |

#### 通用模块/其他
仅参考项目中观察到该模块代表函数，目标项目没有同模块可对齐证据：
- 参考项目：`os/src/hal/arch/loongarch64/register/base/misc.rs#fmt`，调用摘要：f.debug_struct, f.debug_struct("Misc")
            .field, f.debug_struct("Misc")
            .field(
                "32-bit addr plv(1,2,3):",
                &format_args!(
                    "{},{},{}",
                    self.is_va32l1(),
                    self.is_va32l2(),
                    self.is_va32l3()
                ),
            )
            .field, f.debug_struct("Misc")
            .field(
                "32-bit addr plv(1,2,3):",
                &format_args!(
                    "{},{},{}",
                    self.is_va32l1(),
                    self.is_va32l2(),
                    self.is_va32l3()
                ),
            )
            .field(
                "rdtime allowed for plv(1,2,3):",
                &format_args!(
                    "{},{},{}",
                    self.is_drdtl1(),
                    self.is_drdtl2(),
                    self.is_drdtl3()
                ),
            )
            .field, f.debug_struct("Misc")
            .field(
                "32-bit addr plv(1,2,3):",
                &format_args!(
                    "{},{},{}",
                    self.is_va32l1(),
                    self.is_va32l2(),
                    self.is_va32l3()
                ),
            )
            .field(
                "rdtime allowed for plv(1,2,3):",
                &format_args!(
                    "{},{},{}",
                    self.is_drdtl1(),
                    self.is_drdtl2(),
                    self.is_drdtl3()
                ),
            )
            .field(
                "Disable dirty bit check for plv(0,1,2):",
                &format_args!(
                    "{},{},{}",
                    self.is_dwpl0(),
                    self.is_dwpl1(),
                    self.is_dwpl2(),
                ),
            )
            .field, f.debug_struct("Misc")
            .field(
                "32-bit addr plv(1,2,3):",
                &format_args!(
                    "{},{},{}",
                    self.is_va32l1(),
                    self.is_va32l2(),
                    self.is_va32l3()
                ),
            )
            .field(
                "rdtime allowed for plv(1,2,3):",
                &format_args!(
                    "{},{},{}",
                    self.is_drdtl1(),
                    self.is_drdtl2(),
                    self.is_drdtl3()
                ),
            )
            .field(
                "Disable dirty bit check for plv(0,1,2):",
                &format_args!(
                    "{},{},{}",
                    self.is_dwpl0(),
                    self.is_dwpl1(),
                    self.is_dwpl2(),
                ),
            )
            .field(
                "Misalignment check for plv(0,1,2,4):",
                &format_args!(
                    "{},{},{},{}",
                    self.is_alcl0(),
                    self.is_alcl1(),
                    self.is_alcl2(),
                    self.is_alcl3(),
                ),
            )
            .finish ...(+1)
- 参考项目：`apps/kilo/kilo.c#getWindowSize`，调用摘要：getCursorPosition, ioctl, snprintf, strlen, write
- 参考项目：`user/src/lib.rs#_start`，调用摘要：_parameter, asm!, core::arch::asm!, unreachable!
- 参考项目：`user/src/lib.rs#_parameter`，调用摘要：((argv + i * core::mem::size_of::<usize>()) as *const usize).read_volatile, ((str_start + *i) as *const u8).read_volatile, (0usize..)
            .find, (0usize..)
            .find(|i| unsafe { ((str_start + *i) as *const u8).read_volatile() == 0 })
            .unwrap, HEAP.lock, HEAP.lock()
            .init ...(+10)


好的，审计官。基于提供的全量证据，我将执行最终同源性定性仲裁。

### 1. 宏观判定
- **定性结论**: **独立开发**
- **核心依据**: 客观 AST 相似度仅为 3.12%，双向覆盖率均低于 7%。在操作系统内核的核心子系统（进程、内存、调度、中断、驱动、网络协议栈）上，双方共同 AST 指纹为 0，呈现出根本性的架构差异。唯一的相似性集中在 `文件系统/第三方库` 板块的 Ext4 实现上，这完全符合“同一公开规范下的独立实现收敛”特征，不构成代码抄袭或同源性证据。

### 2. 全模块覆盖矩阵解读
- **进程管理**: 仅参考项目存在，目标项目无此模块。共同 AST 指纹为 0。**证据强度: 未形成**。
- **任务调度/异步运行时**: 仅参考项目存在，目标项目无此模块。共同 AST 指纹为 0。**证据强度: 未形成**。
- **内存管理**: 仅参考项目存在，目标项目无此模块。共同 AST 指纹为 0。**证据强度: 未形成**。
- **中断/异常处理**: 仅参考项目存在，目标项目无此模块。共同 AST 指纹为 0。**证据强度: 未形成**。
- **系统调用**: 仅参考项目存在，目标项目无此模块。共同 AST 指纹为 0。**证据强度: 未形成**。
- **文件系统**: 仅参考项目存在（非第三方库部分），目标项目无此模块。共同 AST 指纹为 0。**证据强度: 未形成**。
- **设备驱动**: 仅参考项目存在，目标项目无此模块。共同 AST 指纹为 0。**证据强度: 未形成**。
- **网络协议栈**: 仅参考项目存在（非第三方库部分），目标项目无此模块。共同 AST 指纹为 0。**证据强度: 未形成**。
- **文件系统/第三方库**: 双方均存在，共同 AST 指纹 119。目标覆盖率 30.2%，参考覆盖率 41.18%。相似性集中在 Ext4 规范实现上，但双方在块设备抽象、错误处理、日志风格上存在差异。**证据强度: 弱**。
- **网络协议栈/第三方库**: 双方均存在，共同 AST 指纹 1。覆盖率极低（<1%），同名函数（如 `receive`, `send`）分属不同驱动或协议层，调用图谱完全不同。**证据强度: 未形成**。
- **第三方库**: 双方均存在，共同 AST 指纹 8。覆盖率极低（<1.2%），同名函数（如 `parse`, `generate`）分属完全不同的库和上下文。**证据强度: 未形成**。
- **通用模块/其他**: 仅参考项目存在，目标项目无此模块。共同 AST 指纹为 0。**证据强度: 未形成**。

### 3. 底层 AST 关键证据指纹表
| 所属板块 | 目标项目路径 | 参考项目路径 | 证据类型 | 技术说明 |
| :--- | :--- | :--- | :--- | :--- |
| 文件系统/第三方库 | `crates/scarlet/ext4_rs_fixed/src/ext4_defs/extents.rs#binsearch_extent` | `os/src/fs/ext4/extent.rs#binsearch_extent` | 规范收敛 | Ext4 extent 二分查找的标准实现，调用 `load_from_u32` 等规范定义的访问器。 |
| 文件系统/第三方库 | `crates/scarlet/ext4_rs_fixed/src/ext4_defs/inode.rs#get_inode_checksum` | `os/src/fs/ext4/ext4_inode.rs#get_inode_checksum` | 规范收敛 | Ext4 inode 校验和的标准算法实现，调用 `ext4_crc32c` 及固定字段的字节转换。 |
| 文件系统/第三方库 | `crates/scarlet/ext4_rs_fixed/src/ext4_impls/extents.rs#find_extent` | `os/src/fs/ext4/extent.rs#find_extent` | 规范收敛 | 均使用 `core::mem::transmute::<&[u32; 15], &[u8; 60]>` 进行 Ext4 inode block 的固定尺寸类型转换。 |
| 文件系统/第三方库 | `crates/scarlet/ext4_rs_fixed/src/ext4_impls/file.rs#write_at` | `os/src/fs/ext4/file.rs#write_at` | 弱信号 | 文件写入的通用流程相似，但目标项目使用 `Block::load`，参考项目使用 `Block::load_offset`，块设备抽象不同。 |
| 文件系统/第三方库 | `crates/scarlet/ext4_rs_fixed/src/ext4_impls/balloc.rs#balloc_alloc_block` | `os/src/fs/ext4/balloc.rs#balloc_alloc_block` | 弱信号 | Ext4 块分配的标准流程，调用链高度相似，但目标项目缺少参考项目中的 `Err` 路径处理。 |
| 文件系统/第三方库 | `crates/scarlet/ext4_rs_fixed/src/ext4_impls/dir.rs#dir_add_entry` | `os/src/fs/ext4/direntry.rs#dir_add_entry` | 弱信号 | 目录项添加的标准流程，但目标项目调用 `Self::dir_blocks_for_size`，参考项目调用 `self.append_inode_pblk`，内部实现路径不同。 |
| 文件系统/第三方库 | `crates/scarlet/src/fs/ext4.rs#unlink` | `os/src/fs/ext4/layout.rs#unlink` | 降权/跨层 | 同名函数但处于不同抽象层。目标项目使用迭代器查找，参考项目使用 `Arc::downcast` 和弱引用，调用图谱完全不同。 |
| 文件系统/第三方库 | `crates/scarlet/src/fs/ext4_adaptor.rs#write_offset` | `os/src/fs/ext4/block_group.rs#write_offset` | 降权/跨层 | 同名函数但操作对象不同。目标项目操作设备扇区缓冲 `sector_buf`，参考项目操作内存中的 `self.data`。 |
| 网络协议栈/第三方库 | `crates/scarlet/src/net/tcp.rs#receive` | `dependency/dep_iso/src/net/ethernet/intel/e1000.rs#receive` | 降权/跨层 | 同名函数但分属 TCP 协议层和 Intel e1000 网卡驱动，调用图谱完全不同。 |
| 第三方库 | `crates/devil/src/header.rs#parse` | `dependency/riscv/descriptor/generator.rs#parse` | 降权/跨库 | 同名函数但分属设备树解析和 RISC-V 描述符生成，是完全不同的第三方库。 |

### 4. 核心代码调用流对比剖析
* **【文件系统/第三方库 - Ext4 块分配】**
* **底层技术析理**: 两者均遵循 Ext4 规范的块分配流程：加载块组描述符 -> 检查空闲块计数 -> 在位图中查找空闲位 -> 设置位图 -> 更新校验和。然而，目标项目 (`balloc_alloc_block`) 的调用图谱中未出现 `Err` 处理分支，而参考项目明确包含 `Err` 路径，表明两者在错误处理策略上存在设计差异。此外，底层块加载 API 分别为 `Block::load` 和 `Block::load_offset`，揭示了不同的块缓存抽象层。
* **代码溯源链接**:
  - 目标项目: `crates/scarlet/ext4_rs_fixed/src/ext4_impls/balloc.rs#balloc_alloc_block`
  - 参考项目: `os/src/fs/ext4/balloc.rs#balloc_alloc_block`

* **【文件系统/第三方库 - Ext4 目录项插入】**
* **底层技术析理**: 两者均实现了向现有目录块插入新目录项的通用逻辑：尝试从块偏移处解析 `Ext4DirEntry` -> 检查空间 -> 写入新条目 -> 同步块。但目标项目 (`try_insert_to_existing_block`) 的调用链中缺少参考项目中的 `Err` 处理分支，且两者在计算目录块数量的辅助函数上存在差异（`Self::dir_blocks_for_size` vs `self.append_inode_pblk`），表明内部实现路径不完全一致。
* **代码溯源链接**:
  - 目标项目: `crates/scarlet/ext4_rs_fixed/src/ext4_impls/dir.rs#try_insert_to_existing_block`
  - 参考项目: `os/src/fs/ext4/direntry.rs#try_insert_to_existing_block`

### 5. 单方模块与降权项
- **单方模块**: 进程管理、任务调度/异步运行时、内存管理、中断/异常处理、系统调用、文件系统（非第三方库）、设备驱动、网络协议栈（非第三方库）、通用模块/其他。这些模块仅在参考项目中存在，是构成完整操作系统内核的核心部分。目标项目完全缺失这些模块，构成了两者最根本的架构差异，是判定“独立开发”的基石。
- **降权项说明**:
  - **跨板块/跨抽象层同名函数**: `unlink`、`write_offset` 等函数虽同名，但分属不同文件、不同抽象层次，其调用图谱完全不同，不能作为同源证据。
  - **跨库同名函数**: `receive`、`send`、`parse`、`generate` 等函数名在第三方库中极为常见，且分属完全不同的库（如 TCP 协议栈 vs. 网卡驱动，设备树解析 vs. 描述符生成），其相似性纯属巧合。
  - **信息量为零的函数**: `ext_correct_indexes` 的调用图谱仅剩 `Ok`，无法进行任何有效的比对分析。

### 6. 保守结论
综合全量 AST 证据，目标项目与参考项目在操作系统内核的核心子系统上存在根本性架构差异，共同指纹为零。唯一的相似性集中在 `文件系统/第三方库` 板块的 Ext4 实现上，但该相似性完全可由 Ext4 公开规范的约束和领域内命名收敛来解释，且双方在块设备抽象、错误处理、日志风格及模块组织上存在明显差异。整体 3.12% 的极低 AST 相似度和不足 7% 的双向覆盖率，远不足以支持代码抄袭或同源性指控。因此，本庭裁定目标项目为独立开发，驳回控方关于代码复用的指控。

------------------------------

