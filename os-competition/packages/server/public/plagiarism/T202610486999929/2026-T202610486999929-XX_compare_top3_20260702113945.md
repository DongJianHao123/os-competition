# 🛡️ OS 内核赛自动化代码级架构审计报告

**审计时间**: `2026-07-02 11:39:45`
**参赛作品名称**: `2026-T202610486999929-XX`
**年份**: `2026`
**学校**: `XX`
**队伍名称**: `T202610486999929`
**比赛名字**: `OS-Competition`
**比赛子赛道**: `Kernel-Implementation`
**仓库地址**: `https://gitlab.eduxiji.net/educg-group-43501-3132633/T202610486999929-1714`
**推测开发语言**: `Rust / C (由AST探针自动识别)`
**特征函数量**: `2585`

## ⚔️ 底层代码同源性对比 (AST Homology)
> 本部分由专门的代码审查智能体，对该项目与符合年份条件的历届最高相似度项目进行控制流与底层调用对比。新版报告会先给出确定性的全模块覆盖矩阵，再给出 Agent 的分模块解释，避免只围绕少量抽样函数展开。

### 🏆 Top 3 底层对标: vs `2025-广告位招租-西安电子科技大学`（西安电子科技大学 / 广告位招租）

#### 对方作品信息

**对方年份**: `2025`
**对方学校**: `西安电子科技大学`
**对方队伍名称**: `广告位招租`
**对方比赛名字**: `OS-Competition`
**对方比赛子赛道**: `Kernel-Implementation`
**对方仓库地址**: `https://gitlab.eduxiji.net/educg-group-36002-2710490/T202510701995284-398`

> 双向覆盖率：共同 AST 指纹: 133；唯一 AST 指纹: 目标项目 2134，参考项目 1946；覆盖率: 目标项目 6.23%，参考项目 6.83%

## 全模块对比总览（确定性证据）

> 总体覆盖率：共同 AST 指纹: 133；唯一 AST 指纹: 目标项目 2134，参考项目 1946；覆盖率: 目标项目 6.23%，参考项目 6.83%

### 1. 模块覆盖矩阵
| 模块 | 目标函数数 | 参考函数数 | 共同唯一 AST 指纹 | Target 覆盖 | Base 覆盖 | 状态 |
| :--- | ---: | ---: | ---: | ---: | ---: | :--- |
| 进程管理 | 0 | 131 | 0 | 0.0% | 0.0% | 仅参考项目体现 |
| 任务调度/异步运行时 | 0 | 139 | 0 | 0.0% | 0.0% | 仅参考项目体现 |
| 内存管理 | 0 | 630 | 0 | 0.0% | 0.0% | 仅参考项目体现 |
| 中断/异常处理 | 0 | 47 | 0 | 0.0% | 0.0% | 仅参考项目体现 |
| 系统调用 | 0 | 64 | 0 | 0.0% | 0.0% | 仅参考项目体现 |
| 文件系统 | 0 | 530 | 0 | 0.0% | 0.0% | 仅参考项目体现 |
| 设备驱动 | 0 | 47 | 0 | 0.0% | 0.0% | 仅参考项目体现 |
| 网络协议栈 | 0 | 91 | 0 | 0.0% | 0.0% | 仅参考项目体现 |
| 文件系统/第三方库 | 489 | 366 | 119 | 30.2% | 41.75% | 双方存在且有结构重合 |
| 网络协议栈/第三方库 | 109 | 21 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |
| 第三方库 | 1987 | 869 | 5 | 0.3% | 1.26% | 双方存在且有结构重合 |
| 通用模块/其他 | 0 | 226 | 0 | 0.0% | 0.0% | 仅参考项目体现 |

### 2. 分模块证据清单

#### 进程管理
仅参考项目中观察到该模块代表函数，目标项目没有同模块可对齐证据：
- 参考项目：`os/src/syscall/process.rs#sys_prlimit`，调用摘要：Resource::from_primitive, assert!, copy_from_user, copy_from_user(token, new_limit, rlimit).is_err, copy_to_user, copy_to_user(
                        token,
                        &(RLimit {
                            rlim_cur: MAX_FILE_SIZE,
                            rlim_max: MAX_FILE_SIZE,
                        }),
                        old_limit,
                    )
                    .is_err ...(+19)
- 参考项目：`os/src/syscall/process.rs#sys_execve`，调用摘要：Box::<[u8; 4]>::new, DEFAULT_SHELL.to_string, Some, Vec::with_capacity, arg_ptr.is_null, argv.add ...(+22)
- 参考项目：`os/src/syscall/process.rs#sys_shmctl`，调用摘要：buf.is_null, info!, warn!
- 参考项目：`os/src/syscall/process.rs#sys_futex`，调用摘要：FutexOption::from_bits_truncate, current_task, current_task().unwrap, do_futex_wait, drop, info! ...(+15)

#### 任务调度/异步运行时
仅参考项目中观察到该模块代表函数，目标项目没有同模块可对齐证据：
- 参考项目：`os/src/syscall/fs.rs#sys_copy_file_range`，调用摘要：Some, Vec::new, Vec::with_capacity, buf.set_len, current_task, current_task().unwrap ...(+18)
- 参考项目：`os/src/syscall/fs.rs#sys_splice`，调用摘要：Some, Vec::<u8>::with_capacity, buffer.as_mut_slice, buffer.as_slice, buffer.set_len, current_task ...(+17)
- 参考项目：`os/src/syscall/fs.rs#sys_sendfile`，调用摘要：Some, Vec::<u8>::with_capacity, buffer.as_mut_slice, buffer.as_slice, buffer.set_len, current_task ...(+17)
- 参考项目：`os/src/syscall/fs.rs#sys_fcntl`，调用摘要：Fcntl_Command::from_primitive, OpenFlags::O_NONBLOCK.bits, OpenFlags::O_RDWR.bits, current_task, current_task().unwrap, fd_table.get_ref ...(+10)

#### 内存管理
仅参考项目中观察到该模块代表函数，目标项目没有同模块可对齐证据：
- 参考项目：`user/src/bin/initproc-comp.rs#main`，调用摘要："-c\0".as_ptr, "./basic_testcode.sh\0".as_ptr, "./busybox echo Bandwidth measurements\0".as_ptr, "./busybox echo context switch overhead\0".as_ptr, "./busybox echo file system latency\0".as_ptr, "./busybox echo latency measurements\0".as_ptr ...(+51)
- 参考项目：`user/src/bin/initproc.rs#main`，调用摘要："-c\0".as_ptr, "./basic_testcode.sh\0".as_ptr, "./busybox echo Bandwidth measurements\0".as_ptr, "./busybox echo context switch overhead\0".as_ptr, "./busybox echo file system latency\0".as_ptr, "./busybox echo latency measurements\0".as_ptr ...(+53)
- 参考项目：`os/src/syscall/mod.rs#syscall`，调用摘要：Errno::try_from, [
            //black list
            SYSCALL_YIELD,
            // SYSCALL_READ,
            SYSCALL_WRITE,
            SYSCALL_GETDENTS64,
            SYSCALL_READV,
            SYSCALL_WRITEV,
            SYSCALL_PSELECT6,
            SYSCALL_SIGACTION,
            SYSCALL_SIGPROCMASK,
            // SYSCALL_WAIT4,
            // SYSCALL_GETPPID,
            SYSCALL_CLOCK_GETTIME,
        ]
        .contains, args.len, crate::task::current_task, crate::task::current_task()
                .unwrap, crate::task::current_task()
                .unwrap()
                .acquire_inner_lock ...(+113)
- 参考项目：`os/src/task/signal.rs#do_signal`，调用摘要：(trap_cx as *const TrapContext).cast::<MachineContext>, SigInfo::new, SignalStack::new, Signals::SIGSEGV.to_signum, Signals::SIGSEGV.to_signum().unwrap, Signals::from_bits_truncate ...(+40)

#### 中断/异常处理
仅参考项目中观察到该模块代表函数，目标项目没有同模块可对齐证据：
- 参考项目：`os/src/hal/arch/loongarch64/trap/mod.rs#trap_from_kernel`，调用摘要：((addr + i) as *mut u8).read_unaligned, ((addr + i) as *mut u8).write_unaligned, (0..sz).rev, BadV::read, BadV::read().get_vaddr, EStat::read ...(+14)
- 参考项目：`os/src/hal/arch/loongarch64/trap/mem_access.rs#get_addr`，调用摘要：self.get_op_code, self.get_op_code().map, self.get_rj_num, self.get_rk_num, self.get_si12, self.get_si14
- 参考项目：`os/src/hal/arch/loongarch64/trap/mem_access.rs#get_size`，调用摘要：内部闭环逻辑
- 参考项目：`os/src/fs/dev/tty.rs#default`，调用摘要：内部闭环逻辑

#### 系统调用
仅参考项目中观察到该模块代表函数，目标项目没有同模块可对齐证据：
- 参考项目：`os/src/net/tcp.rs#_read`，调用摘要：Err, GeneralRet::Err, GeneralRet::Ok, NET_INTERFACE.poll, NET_INTERFACE.tcp_socket, Ok ...(+7)
- 参考项目：`os/src/net/tcp.rs#_accept`，调用摘要：Err, GeneralRet::Err, GeneralRet::Ok, NET_INTERFACE.poll, NET_INTERFACE.tcp_socket, Ok ...(+7)
- 参考项目：`os/src/net/udp.rs#connect`，调用摘要：Err, IpListenEndpoint::from, NET_INTERFACE.poll, NET_INTERFACE.udp_socket, Ok, SocketAddrv4::new ...(+11)
- 参考项目：`os/src/net/udp.rs#_read`，调用摘要：Err, GeneralRet::Err, GeneralRet::Ok, NET_INTERFACE.poll, NET_INTERFACE.udp_socket, Ok ...(+10)

#### 文件系统
仅参考项目中观察到该模块代表函数，目标项目没有同模块可对齐证据：
- 参考项目：`os/src/fs/directory_tree.rs#open`，调用摘要：(*last_comp).to_string, Arc::downgrade, Err, Ok, PATH_CACHE.lock, REDIRECT_TO_BUSYBOX.contains ...(+26)
- 参考项目：`os/src/fs/directory_tree.rs#rename`，调用摘要：Arc::downgrade, Arc::new, Err, Mutex::new, Ok, ROOT.cd_comp ...(+41)
- 参考项目：`os/src/fs/directory_tree.rs#init_device_directory`，调用摘要："null".to_string, "rtc".to_string, "tty".to_string, "urandom".to_string, "zero".to_string, Arc::downgrade ...(+21)
- 参考项目：`os/src/fs/fat32/fat_inode.rs#delete_dir_ent`，调用摘要：Err, FATDirEnt::unused_and_last_entry, FATDirEnt::unused_not_last_entry, Ok, Some, debug_assert! ...(+14)

#### 设备驱动
仅参考项目中观察到该模块代表函数，目标项目没有同模块可对齐证据：
- 参考项目：`os/src/drivers/block/sata_blk.rs#pci_init`，调用摘要：AHCI::new, Some, Status::empty, dev.bars.iter, dev.bars.iter().enumerate, dev.bars.iter().enumerate().for_each ...(+3)
- 参考项目：`os/src/net/config.rs#new`，调用摘要：Config::new, EthernetAddress, EthernetAddress([0x02, 0x00, 0x00, 0x00, 0x00, 0x01]).into, Instant::from_millis, Interface::new, IpAddress::v4 ...(+12)
- 参考项目：`os/src/net/address.rs#from`，调用摘要：IpAddress::Ipv6, Ipv6Address, RNG.positive_u32, Some, addr.is_unspecified, u16::from_be_bytes
- 参考项目：`os/src/net/address.rs#from`，调用摘要：IpAddress::Ipv4, Ipv4Address, RNG.positive_u32, Some, addr.is_unspecified, u16::from_be_bytes

#### 网络协议栈
仅参考项目中观察到该模块代表函数，目标项目没有同模块可对齐证据：
- 参考项目：`os/src/net/tcp.rs#new`，调用摘要：Mutex::new, NET_INTERFACE.add_socket, NET_INTERFACE.poll, RNG.positive_u32, info!, socket::tcp::Socket::new ...(+2)
- 参考项目：`os/src/net/udp.rs#new`，调用摘要：Mutex::new, NET_INTERFACE.add_socket, NET_INTERFACE.poll, log::info!, socket::udp::PacketBuffer::new, socket::udp::Socket::new ...(+1)
- 参考项目：`os/src/net/tcp.rs#drop`，调用摘要：NET_INTERFACE.poll, NET_INTERFACE.remove, NET_INTERFACE.tcp_socket, info!, socket.close, socket.is_open
- 参考项目：`os/src/net/mod.rs#can_bind`，调用摘要：Some, log::info!, self.0.clone, socket.loacl_endpoint, socket.loacl_endpoint().eq, socket.socket_type ...(+1)

#### 文件系统/第三方库
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `crates/scarlet/ext4_rs_fixed/src/ext4_impls/file.rs#write_at` | `os/src/fs/ext4/file.rs#write_at` | 同板块同名 | Block::load, Err, Ok, allocated_blocks.len, block.data.copy_from_slice, block.sync_blk_to_disk ...(+18) | Block::load_offset, Ok, block.sync_blk_to_disk, block.write_offset, drop, inode_ref.inode.set_size ...(+10) |
| `crates/scarlet/ext4_rs_fixed/src/ext4_impls/extents.rs#ext_remove_leaf` | `os/src/fs/ext4/extent.rs#ext_remove_leaf` | 同板块同名 | Block::load, Block::load_inode_root_block, Ext4Extent::default, Ok, core::slice::from_raw_parts, ex.get_actual_len ...(+21) | Block::load_inode_root_block, Block::load_offset, Ext4Extent::default, Ok, ex.get_actual_len, ex.get_pblock ...(+14) |
| `crates/scarlet/ext4_rs_fixed/src/ext4_impls/extents.rs#insert_new_extent` | `os/src/fs/ext4/extent.rs#insert_new_extent` | 同板块同名 | Block::load, Ok, core::mem::size_of::<Ext4Extent>, core::mem::size_of::<Ext4ExtentHeader>, ext4block.read_offset_as_mut, ext4block.sync_blk_to_disk ...(+10) | Block::load_offset, Ok, core::mem::size_of::<Ext4Extent>, core::mem::size_of::<Ext4ExtentHeader>, ext4block.read_offset_as_mut, ext4block.sync_blk_to_disk ...(+5) |
| `crates/scarlet/ext4_rs_fixed/src/ext4_impls/extents.rs#extent_remove_space` | `os/src/fs/ext4/extent.rs#extent_remove_space` | 同板块同名 | Block::load, Ext4Extent::default, Ok, ex.get_actual_len, ex.get_first_block, ex.get_pblock ...(+13) | Block::load_offset, Ext4Extent::default, Ok, ex.get_actual_len, ex.get_first_block, ex.get_pblock ...(+14) |

#### 网络协议栈/第三方库
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `crates/scarlet/src/net/tcp.rs#receive` | `dependency/dep_iso/isomorphic_drivers/src/net/ethernet/intel/e1000.rs#receive` | 同板块同名 | TCP_CONNECTIONS
        .lock, TCP_CONNECTIONS
        .lock()
        .get, TCP_CONNECTIONS
        .lock()
        .get(&(dst_port, src_port, src_ip))
        .cloned, TCP_CONNECTIONS
        .lock()
        .get(&(dst_port, src_port, src_ip))
        .cloned()
        .or_else, buf.payload, data.is_empty ...(+12) | Some, buffer.to_vec, recv_desc.status.get_bit, recv_desc.status.set_bit, self.recv_queue.len, self.registers[E1000_RDT].read ...(+5) |
| `crates/scarlet/src/net/ipv4.rs#send` | `dependency/dep_iso/isomorphic_drivers/src/net/ethernet/intel/e1000.rs#send` | 同板块同名 | LOOPBACK_QUEUE.lock, LOOPBACK_QUEUE.lock().push, arp::lookup, arp::our_ip, arp::queue_pending, arp::send_request ...(+9) | assert!, buffer.len, fence, self.registers[E1000_TDT].read, self.registers[E1000_TDT].write, self.send_queue.len ...(+2) |
| `crates/scarlet/src/net/tcp.rs#send` | `dependency/dep_iso/isomorphic_drivers/src/net/ethernet/intel/ixgbe.rs#send` | 同板块同名 | Err, Errno::EDESTADDRREQ.as_ret, Errno::ENOTCONN.as_ret, NetworkBuffer::new, Ok, Vec::with_capacity ...(+10) | self.msend |
| `crates/scarlet/src/net/tcp.rs#recv` | `dependency/dep_iso/isomorphic_drivers/src/net/ethernet/intel/ixgbe.rs#recv` | 同板块同名 | Err, Errno::EAGAIN.as_ret, Errno::ENOTCONN.as_ret, Ok, buf.len, buf[..n].copy_from_slice ...(+7) | Some, buffer.to_vec, ixgbe[IXGBE_RDT].read, ixgbe[IXGBE_RDT].write, recv_desc.status_error.get_bit, recv_desc.status_error.set_bit ...(+1) |

#### 第三方库
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `crates/devil/src/header.rs#parse` | `dependency/riscv/descriptor/generator.rs#parse` | 同板块同名 | Err, Ok, Self::read_from_prefix, blob.len, header
            .off_dt_strings
            .checked_add, header
            .off_dt_strings
            .checked_add(header.size_dt_strings)
            .ok_or ...(+6) | EnumerationDescriptor::parse, Some, as_str_polyfill!, desc.split, parts.next, parts.next().unwrap ...(+2) |
| `crates/scarlet/src/fs/procfs/status.rs#generate` | `dependency/riscv/descriptor/generator.rs#generate` | 同板块同名 | String::new, area.end_va, area.start_va, info.is_none, info.unwrap, mm.areas ...(+13) | String::new, bf.generate_enum, format!, self.bfs.iter, write!, write!(&mut bit_sets, "{}", bf.generate_bitops()).unwrap ...(+2) |
| `crates/scarlet/src/arch/shared/virtio_net.rs#send` | `dependency/virtio-drivers/src/net.rs#send` | 同板块同名 | avail.idx.wrapping_add, buf.payload, core::hint::spin_loop, fence, last.wrapping_add, mmio.add ...(+9) | MaybeUninit::<Header>::zeroed, MaybeUninit::<Header>::zeroed().assume_init, Ok, header.as_buf, self.header.notify, self.send_queue.add ...(+3) |
| `crates/scarlet/src/arch/shared/virtio_blk.rs#read_block` | `dependency/virtio-drivers/src/blk.rs#read_block` | 同板块同名 | Err, Ok, buf.as_mut_ptr, buf.as_mut_ptr().cast, buf.len, core::ptr::addr_of! ...(+11) | BlkResp::default, Err, Ok, Self::get_bufs_mut, Self::get_bufs_ref, Vec::<&[u8]>::new ...(+14) |

#### 通用模块/其他
仅参考项目中观察到该模块代表函数，目标项目没有同模块可对齐证据：
- 参考项目：`os/src/hal/arch/loongarch64/register/base/misc.rs#fmt`，调用摘要：f.debug_struct, f.debug_struct("Misc")
            .field, f.debug_struct("Misc")
            .field(
                "32-bit addr plv(1,2,3):",
                &format_args!(
                    "{},{},{}",
                    self.is_va32l1(),
                    self.is_va32l2(),
                    self.is_va32l3()
                ),
            )
            .field, f.debug_struct("Misc")
            .field(
                "32-bit addr plv(1,2,3):",
                &format_args!(
                    "{},{},{}",
                    self.is_va32l1(),
                    self.is_va32l2(),
                    self.is_va32l3()
                ),
            )
            .field(
                "rdtime allowed for plv(1,2,3):",
                &format_args!(
                    "{},{},{}",
                    self.is_drdtl1(),
                    self.is_drdtl2(),
                    self.is_drdtl3()
                ),
            )
            .field, f.debug_struct("Misc")
            .field(
                "32-bit addr plv(1,2,3):",
                &format_args!(
                    "{},{},{}",
                    self.is_va32l1(),
                    self.is_va32l2(),
                    self.is_va32l3()
                ),
            )
            .field(
                "rdtime allowed for plv(1,2,3):",
                &format_args!(
                    "{},{},{}",
                    self.is_drdtl1(),
                    self.is_drdtl2(),
                    self.is_drdtl3()
                ),
            )
            .field(
                "Disable dirty bit check for plv(0,1,2):",
                &format_args!(
                    "{},{},{}",
                    self.is_dwpl0(),
                    self.is_dwpl1(),
                    self.is_dwpl2(),
                ),
            )
            .field, f.debug_struct("Misc")
            .field(
                "32-bit addr plv(1,2,3):",
                &format_args!(
                    "{},{},{}",
                    self.is_va32l1(),
                    self.is_va32l2(),
                    self.is_va32l3()
                ),
            )
            .field(
                "rdtime allowed for plv(1,2,3):",
                &format_args!(
                    "{},{},{}",
                    self.is_drdtl1(),
                    self.is_drdtl2(),
                    self.is_drdtl3()
                ),
            )
            .field(
                "Disable dirty bit check for plv(0,1,2):",
                &format_args!(
                    "{},{},{}",
                    self.is_dwpl0(),
                    self.is_dwpl1(),
                    self.is_dwpl2(),
                ),
            )
            .field(
                "Misalignment check for plv(0,1,2,4):",
                &format_args!(
                    "{},{},{},{}",
                    self.is_alcl0(),
                    self.is_alcl1(),
                    self.is_alcl2(),
                    self.is_alcl3(),
                ),
            )
            .finish ...(+1)
- 参考项目：`user/src/lib.rs#_start`，调用摘要：_parameter, asm!, core::arch::asm!, unreachable!
- 参考项目：`user/src/lib.rs#_parameter`，调用摘要：((argv + i * core::mem::size_of::<usize>()) as *const usize).read_volatile, ((str_start + *i) as *const u8).read_volatile, (0usize..)
            .find, (0usize..)
            .find(|i| unsafe { ((str_start + *i) as *const u8).read_volatile() == 0 })
            .unwrap, HEAP.lock, HEAP.lock()
            .init ...(+10)
- 参考项目：`os/src/hal/arch/loongarch64/time.rs#get_timer_freq_first_time`，调用摘要：cfg5.get_bits, config::CPUCfg4::read, config::CPUCfg4::read().get_bits, config::CPUCfg5::read, println!


好的，审计员。我将基于提供的客观数据、证据底座及双方论点，进行最终的独立同源性定性仲裁。

### 📝 审计结论

#### 1. 宏观判定
- **定性结论**: **独立开发 / 底层架构显著差异**
  整体客观 AST 相似度仅为 **3.37%**，双向覆盖率均低于 7%。所有重合点均集中在 `文件系统/第三方库` 这一非原创内核板块，而构成操作系统核心的进程管理、内存管理、任务调度、中断/异常、系统调用等模块的 AST 指纹重合率为 **0%**。这表明两个项目在原创性内核架构上完全异构，不存在大规模、系统性抄袭行为。`文件系统/第三方库` 板块的相似性源于对同一公开技术规范（Ext4）的独立实现，属于规范约束下的算法收敛。

#### 2. 全模块覆盖矩阵解读
- **进程管理**: 仅参考项目存在，目标项目无此模块。共同 AST 指纹为 0。**未形成证据**。
- **任务调度/异步运行时**: 仅参考项目存在，目标项目无此模块。共同 AST 指纹为 0。**未形成证据**。
- **内存管理**: 仅参考项目存在，目标项目无此模块。共同 AST 指纹为 0。**未形成证据**。
- **中断/异常处理**: 仅参考项目存在，目标项目无此模块。共同 AST 指纹为 0。**未形成证据**。
- **系统调用**: 仅参考项目存在，目标项目无此模块。共同 AST 指纹为 0。**未形成证据**。
- **文件系统**: 仅参考项目存在原创文件系统模块，目标项目无此模块。共同 AST 指纹为 0。**未形成证据**。
- **设备驱动**: 仅参考项目存在，目标项目无此模块。共同 AST 指纹为 0。**未形成证据**。
- **网络协议栈**: 仅参考项目存在原创网络协议栈模块，目标项目无此模块。共同 AST 指纹为 0。**未形成证据**。
- **文件系统/第三方库**: 双方均存在，共同 AST 指纹 119。目标覆盖率 30.2%，参考覆盖率 41.75%。重合点均为 Ext4 文件系统实现，属于对同一公开标准的实现。**证据强度：弱**。
- **网络协议栈/第三方库**: 双方均存在，共同 AST 指纹为 0。表明双方在该板块的第三方库选型或实现完全不同。**未形成证据**。
- **第三方库**: 双方均存在，共同 AST 指纹仅 5，覆盖率均低于 1.3%，属于噪声级。**证据强度：弱**。
- **通用模块/其他**: 仅参考项目存在，目标项目无此模块。共同 AST 指纹为 0。**未形成证据**。

#### 3. 底层 AST 关键证据指纹表
| 所属板块 | 目标项目路径 | 参考项目路径 | 证据类型 | 技术说明 |
| :--- | :--- | :--- | :--- | :--- |
| 文件系统/第三方库 | `crates/scarlet/ext4_rs_fixed/src/ext4_defs/extents.rs#binsearch_extent` | `os/src/fs/ext4/extent.rs#binsearch_extent` | 弱信号 / 规范收敛 | Ext4 extent 树二分查找算法的标准实现。调用序列由 Ext4 磁盘格式规范强制决定，任何独立实现必然遵循 `load header -> sizeof -> load entry` 的顺序。 |
| 文件系统/第三方库 | `crates/scarlet/ext4_rs_fixed/src/ext4_defs/extents.rs#binsearch_idx` | `os/src/fs/ext4/extent.rs#binsearch_idx` | 弱信号 / 规范收敛 | 同上，为 Ext4 extent 索引节点的二分查找标准实现。 |
| 文件系统/第三方库 | `crates/scarlet/ext4_rs_fixed/src/ext4_defs/inode.rs#get_inode_checksum` | `os/src/fs/ext4/ext4_inode.rs#get_inode_checksum` | 弱信号 / 规范收敛 | Ext4 inode 校验和计算的标准流程。输入（inode号、generation、UUID）和算法（CRC32C）由 Linux 内核规范定义，调用顺序是算法实现的唯一自然路径。 |
| 文件系统/第三方库 | `crates/scarlet/ext4_rs_fixed/src/ext4_impls/extents.rs#ext_remove_leaf` | `os/src/fs/ext4/extent.rs#ext_remove_leaf` | 弱信号 / 规范收敛 | Ext4 extent 树节点移除操作。`ex.get_actual_len`、`ex.get_pblock` 等是 `Ext4Extent` 结构体的标准访问器，其调用顺序由操作逻辑决定。目标项目使用 `Block::load`，参考项目使用 `Block::load_offset`，底层块接口封装不同。 |
| 文件系统/第三方库 | `crates/scarlet/ext4_rs_fixed/src/ext4_impls/dir.rs#dir_add_entry` | `os/src/fs/ext4/direntry.rs#dir_add_entry` | 弱信号 / 规范收敛 | Ext4 目录项添加操作。`parent.inode.generation` 和 `parent.inode.size` 是目录项写入时必须检查和更新的字段，读取序列由 Ext4 目录结构定义。 |
| 第三方库 | `crates/scarlet/src/arch/shared/virtio_blk.rs#read_block` | `dependency/virtio-drivers/src/blk.rs#read_block` | 弱信号 / 公共接口 | 双方均使用 `virtio-drivers` 库，`read_block` 是 VirtIO 块设备驱动的标准读接口。AST 相似源于对同一公共 crate 的依赖和调用，而非代码抄袭。 |

#### 4. 核心代码调用流对比剖析
* **【文件系统/第三方库 / Ext4 文件系统】**
* **底层技术析理**: 双方均实现了 Ext4 文件系统，这是相似性的唯一来源。然而，其底层架构集成方式存在显著差异。目标项目将 Ext4 实现置于 `crates/scarlet/ext4_rs_fixed`，作为一个独立的 crate 进行修复和适配，并通过 `crates/scarlet/src/fs/ext4_adaptor.rs` 提供的 `write_offset` 等函数与上层虚拟文件系统对接，其操作对象是 `self.dev`（块设备抽象）。参考项目则将 Ext4 实现直接内嵌于 `os/src/fs/ext4/` 目录下，其 `write_offset` 函数位于 `block_group.rs`，直接操作 `self.data`（内存中的块组数据）。这种在**模块化分层、抽象层级和对象所有权模型**上的根本差异，证明两者是独立架构设计，而非简单代码搬运。
* **代码溯源链接**:
  - 目标项目: `crates/scarlet/src/fs/ext4_adaptor.rs#write_offset`
  - 参考项目: `os/src/fs/ext4/block_group.rs#write_offset`

#### 5. 单方模块与降权项
- **单方模块**: 进程管理、任务调度/异步运行时、内存管理、中断/异常处理、系统调用、文件系统（原创）、设备驱动、网络协议栈（原创）、通用模块/其他等模块仅在参考项目中存在，目标项目无任何对应 AST 指纹。这构成了两个项目在操作系统内核核心架构上完全异构的决定性证据，直接否定了“系统性抄袭”的指控。
- **降权项**:
  - **跨板块同名函数**: 如目标项目 `crates/scarlet/src/net/tcp.rs#send` 与参考项目 `dependency/.../ixgbe.rs#send`，虽同名但分属 TCP 协议栈和网卡驱动，属于跨板块硬凑，不构成同源证据。
  - **第三方库/公共依赖**: `virtio-drivers` 等第三方库中的同名函数（如 `read_block`）是公共接口调用的必然结果，不能作为抄袭证据。
  - **泛型函数名**: `write_at`, `read_at`, `send`, `recv`, `parse`, `generate` 等均为通用编程动词，其同名不具备特异性。

#### 6. 保守结论
本次审计的客观 AST 相似度仅为 3.37%，且所有重合点均局限于 `文件系统/第三方库` 板块。该板块的相似性可被 Ext4 文件系统公开的磁盘格式规范、标准算法（二分查找、CRC32C校验）以及 Rust 语言惯用法（切片、`copy_from_slice`）所充分解释，属于**规范约束下的独立实现收敛**，而非代码抄袭。两个项目在构成操作系统原创性核心的进程、内存、调度、中断、系统调用等模块上**零 AST 重合**，架构设计（如 Ext4 的集成方式）存在根本差异。因此，综合判定目标项目不存在对参考项目的系统性代码复用或抄袭行为，整体风险极低。

------------------------------

