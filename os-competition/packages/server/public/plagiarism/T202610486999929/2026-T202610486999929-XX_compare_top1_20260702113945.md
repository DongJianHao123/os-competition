# 🛡️ OS 内核赛自动化代码级架构审计报告

**审计时间**: `2026-07-02 11:39:45`
**参赛作品名称**: `2026-T202610486999929-XX`
**年份**: `2026`
**学校**: `XX`
**队伍名称**: `T202610486999929`
**比赛名字**: `OS-Competition`
**比赛子赛道**: `Kernel-Implementation`
**仓库地址**: `https://gitlab.eduxiji.net/educg-group-43501-3132633/T202610486999929-1714`
**推测开发语言**: `Rust / C (由AST探针自动识别)`
**特征函数量**: `2585`

## ⚔️ 底层代码同源性对比 (AST Homology)
> 本部分由专门的代码审查智能体，对该项目与符合年份条件的历届最高相似度项目进行控制流与底层调用对比。新版报告会先给出确定性的全模块覆盖矩阵，再给出 Agent 的分模块解释，避免只围绕少量抽样函数展开。

### 🏆 Top 1 底层对标: vs `2024-你起吧-长春理工大学`（长春理工大学 / 你起吧）

#### 对方作品信息

**对方年份**: `2024`
**对方学校**: `长春理工大学`
**对方队伍名称**: `你起吧`
**对方比赛名字**: `OS-Competition`
**对方比赛子赛道**: `Kernel-Implementation`
**对方仓库地址**: `https://gitlab.eduxiji.net/educg-group-26010-2376550/T202410186992540-1599`

> 双向覆盖率：共同 AST 指纹: 165；唯一 AST 指纹: 目标项目 2134，参考项目 573；覆盖率: 目标项目 7.73%，参考项目 28.8%

## 全模块对比总览（确定性证据）

> 总体覆盖率：共同 AST 指纹: 165；唯一 AST 指纹: 目标项目 2134，参考项目 573；覆盖率: 目标项目 7.73%，参考项目 28.8%

### 1. 模块覆盖矩阵
| 模块 | 目标函数数 | 参考函数数 | 共同唯一 AST 指纹 | Target 覆盖 | Base 覆盖 | 状态 |
| :--- | ---: | ---: | ---: | ---: | ---: | :--- |
| 进程管理 | 0 | 37 | 0 | 0.0% | 0.0% | 仅参考项目体现 |
| 任务调度/异步运行时 | 0 | 22 | 0 | 0.0% | 0.0% | 仅参考项目体现 |
| 内存管理 | 0 | 140 | 0 | 0.0% | 0.0% | 仅参考项目体现 |
| 中断/异常处理 | 0 | 9 | 0 | 0.0% | 0.0% | 仅参考项目体现 |
| 系统调用 | 0 | 3 | 0 | 0.0% | 0.0% | 仅参考项目体现 |
| 文件系统 | 0 | 67 | 0 | 0.0% | 0.0% | 仅参考项目体现 |
| 设备驱动 | 0 | 14 | 0 | 0.0% | 0.0% | 仅参考项目体现 |
| 网络协议栈 | 0 | 0 | 0 | 0.0% | 0.0% | 双方均未体现 |
| 文件系统/第三方库 | 489 | 323 | 164 | 41.62% | 67.21% | 双方存在且有结构重合 |
| 网络协议栈/第三方库 | 109 | 0 | 0 | 0.0% | 0.0% | 仅目标项目体现 |
| 第三方库 | 1987 | 0 | 0 | 0.0% | 0.0% | 仅目标项目体现 |
| 通用模块/其他 | 0 | 71 | 0 | 0.0% | 0.0% | 仅参考项目体现 |

### 2. 分模块证据清单

#### 进程管理
仅参考项目中观察到该模块代表函数，目标项目没有同模块可对齐证据：
- 参考项目：`kernel/proc.c#exit`，调用摘要：acquire, fileclose, myproc, panic, printf, release ...(+4)
- 参考项目：`kernel/proc.c#scheduler`，调用摘要：acquire, intr_on, mycpu, release, swtch
- 参考项目：`kernel/proc.c#procdump`，调用摘要：NELEM, printf
- 参考项目：`kernel/proc.c#sleep`，调用摘要：acquire, myproc, release, sched

#### 任务调度/异步运行时
仅参考项目中观察到该模块代表函数，目标项目没有同模块可对齐证据：
- 参考项目：`kernel/virtio_disk.c#virtio_disk_intr`，调用摘要：R, __sync_synchronize, acquire, free_chain, panic, release ...(+1)
- 参考项目：`kernel/trap.c#usertrap`，调用摘要：devintr, exit, intr_on, killed, myproc, panic ...(+10)
- 参考项目：`kernel/console.c#consoleintr`，调用摘要：C, acquire, consputc, procdump, release, wakeup
- 参考项目：`kernel/console.c#consoleread`，调用摘要：C, acquire, either_copyout, killed, myproc, release ...(+1)

#### 内存管理
仅参考项目中观察到该模块代表函数，目标项目没有同模块可对齐证据：
- 参考项目：`kernel/exec.c#execve`，调用摘要：PGROUNDUP, alloc_aux, begin_op, copyout, copyoutstr, end_op ...(+15)
- 参考项目：`kernel/virtio_disk.c#virtio_disk_rw_multiple`，调用摘要：R, __sync_synchronize, acquire, alloc_descs, free_chain, release ...(+1)
- 参考项目：`mkfs/mkfs.c#main`，调用摘要：assert, balloc, bzero, close, die, exit ...(+17)
- 参考项目：`kernel/virtio_disk.c#virtio_disk_init`，调用摘要：R, initlock, memset, panic

#### 中断/异常处理
仅参考项目中观察到该模块代表函数，目标项目没有同模块可对齐证据：
- 参考项目：`kernel/plic.c#plicinithart`，调用摘要：PLIC_SENABLE, PLIC_SPRIORITY, cpuid
- 参考项目：`kernel/spinlock.c#pop_off`，调用摘要：intr_get, intr_on, mycpu, panic
- 参考项目：`kernel/plic.c#plicinit`，调用摘要：内部闭环逻辑
- 参考项目：`kernel/spinlock.c#push_off`，调用摘要：intr_get, intr_off, mycpu

#### 系统调用
仅参考项目中观察到该模块代表函数，目标项目没有同模块可对齐证据：
- 参考项目：`kernel/syscall.c#argstr`，调用摘要：argaddr, fetchstr
- 参考项目：`kernel/syscall.c#argaddr`，调用摘要：argraw
- 参考项目：`kernel/syscall.c#argint`，调用摘要：argraw

#### 文件系统
仅参考项目中观察到该模块代表函数，目标项目没有同模块可对齐证据：
- 参考项目：`mkfs/mkfs.c#iappend`，调用摘要：assert, bcopy, min, rinode, rsect, winode ...(+2)
- 参考项目：`src/fs.rs#do_create`，调用摘要：EXT4FS.assume_init_ref, Ext4DirEntry::default, Ext4DirSearchResult::new, fs.create, fs.dir_find_entry, fs.dir_find_entry(idir, name, &mut res).is_ok ...(+2)
- 参考项目：`src/fs.rs#do_getdents`，调用摘要：(*de).d_name.as_mut_ptr, EXT4FS.assume_init_ref, addr_of!, buf.as_mut_ptr, buf.as_ptr, either_copyout ...(+4)
- 参考项目：`kernel/fs.c#safepathcat`，调用摘要：skipelem, strlen, strncmp

#### 设备驱动
仅参考项目中观察到该模块代表函数，目标项目没有同模块可对齐证据：
- 参考项目：`kernel/uart.c#uartinit`，调用摘要：WriteReg, initlock
- 参考项目：`kernel/uart.c#uartstart`，调用摘要：ReadReg, WriteReg, wakeup
- 参考项目：`kernel/uart.c#uartputc`，调用摘要：acquire, release, sbi_console_putchar, sleep, uartstart
- 参考项目：`kernel/console.c#consoleinit`，调用摘要：initlock, initsleeplock, uartinit

#### 文件系统/第三方库
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `crates/scarlet/ext4_rs_fixed/src/ext4_impls/file.rs#write_at` | `third/ext4_rs/src/ext4_impls/file.rs#write_at` | 同板块同名 | Block::load, Err, Ok, allocated_blocks.len, block.data.copy_from_slice, block.sync_blk_to_disk ...(+18) | Block::load, Ok, block.sync_blk_to_disk, block.write_offset, drop, inode_ref
                .inode
                .set_size ...(+10) |
| `crates/scarlet/ext4_rs_fixed/src/ext4_impls/extents.rs#ext_remove_leaf` | `third/ext4_rs/src/ext4_impls/extents.rs#ext_remove_leaf` | 同板块同名 | Block::load, Block::load_inode_root_block, Ext4Extent::default, Ok, core::slice::from_raw_parts, ex.get_actual_len ...(+21) | Block::load, Block::load_inode_root_block, Ext4Extent::default, Ok, ex.get_actual_len, ex.get_pblock ...(+14) |
| `crates/scarlet/ext4_rs_fixed/src/ext4_impls/extents.rs#insert_new_extent` | `third/ext4_rs/src/ext4_impls/extents.rs#insert_new_extent` | 同板块同名 | Block::load, Ok, core::mem::size_of::<Ext4Extent>, core::mem::size_of::<Ext4ExtentHeader>, ext4block.read_offset_as_mut, ext4block.sync_blk_to_disk ...(+10) | Ok, inode_ref.inode.root_extent_header_mut, inode_ref.inode.root_extent_mut_at, log::trace!, node.header.clone, return_errno_with_message! ...(+1) |
| `crates/scarlet/ext4_rs_fixed/src/ext4_impls/extents.rs#extent_remove_space` | `third/ext4_rs/src/ext4_impls/extents.rs#extent_remove_space` | 同板块同名 | Block::load, Ext4Extent::default, Ok, ex.get_actual_len, ex.get_first_block, ex.get_pblock ...(+13) | Block::load, Ext4Extent::default, Ok, ex.get_actual_len, ex.get_first_block, ex.get_pblock ...(+16) |

#### 网络协议栈/第三方库
仅目标项目中观察到该模块代表函数，参考项目没有同模块可对齐证据：
- 目标项目：`crates/scarlet/src/net/tcp.rs#receive`，调用摘要：TCP_CONNECTIONS
        .lock, TCP_CONNECTIONS
        .lock()
        .get, TCP_CONNECTIONS
        .lock()
        .get(&(dst_port, src_port, src_ip))
        .cloned, TCP_CONNECTIONS
        .lock()
        .get(&(dst_port, src_port, src_ip))
        .cloned()
        .or_else, buf.payload, data.is_empty ...(+12)
- 目标项目：`crates/scarlet/src/syscall/socket.rs#sys_recvmsg`，调用摘要：(cur + len).min, Errno::EAGAIN.as_ret, Errno::EFAULT.as_ret, Errno::ENOTSOCK.as_ret, alloc::vec!, alloc::vec::Vec::new ...(+21)
- 目标项目：`crates/scarlet/src/syscall/socket.rs#sys_setsockopt`，调用摘要：Errno::EFAULT.as_ret, Errno::EINVAL.as_ret, Errno::ENOPROTOOPT.as_ret, Errno::ENOTSOCK.as_ret, crate::net::socket::socket_by_fd, current_tid ...(+3)
- 目标项目：`crates/scarlet/src/syscall/socket.rs#sys_recvfrom`，调用摘要：Err, Errno::EAGAIN.as_ret, Errno::ENOTSOCK.as_ret, Ok, addr_len.to_le_bytes, alloc::vec! ...(+11)

#### 第三方库
仅目标项目中观察到该模块代表函数，参考项目没有同模块可对齐证据：
- 目标项目：`crates/scarlet/src/sched/mod.rs#schedule`，调用摘要：Arc::ptr_eq, CurrentThreadCache::from_locked, CurrentThreadCache::from_thread, Some, ZOMBIE_CLEANUP_QUEUES[current_hart_id].lock, ZOMBIE_CLEANUP_QUEUES[current_hart_id].lock().pop ...(+79)
- 目标项目：`crates/scarlet/src/competition.rs#competition_runner`，调用摘要：(load_info.tls_align as usize).max, Arc::clone, DEBUG_TEST_FILTER.lock, DEBUG_TEST_FILTER.lock().clone, Some, String::from ...(+88)
- 目标项目：`crates/scarlet/src/main.rs#main`，调用摘要：alloc::format!, alloc::sync::Arc::clone, alloc::sync::Arc::get_mut, alloc::sync::Arc::get_mut(&mut proc)
                        .expect, alloc::sync::Arc::get_mut(&mut proc)
                        .expect("init proc has refcount 1")
                        .lock, alloc::sync::Arc::get_mut(&mut proc)
                        .expect("init proc has refcount 1")
                        .lock()
                        .mm
                        .as_mut ...(+96)
- 目标项目：`crates/sakuya/src/main.rs#main`，调用摘要："build.sh not found".to_string, "missing initramfs field".to_string, Command::new, Command::new("sh")
                    .arg, Command::new("sh")
                    .arg(&build_script)
                    .env, Command::new("sh")
                    .arg(&build_script)
                    .env("SAKUYA_OUT", &out_dir)
                    .env ...(+66)

#### 通用模块/其他
仅参考项目中观察到该模块代表函数，目标项目没有同模块可对齐证据：
- 参考项目：`user/printf.c#vprintf`，调用摘要：printint, printptr, putc, va_arg
- 参考项目：`user/init.c#main`，调用摘要：execve, fork, printf, wait
- 参考项目：`kernel/spinlock.c#release`，调用摘要：__sync_lock_release, __sync_synchronize, holding, panic, pop_off
- 参考项目：`kernel/bio.c#bget`，调用摘要：acquire, acquiresleep, panic, release


好的，我将遵循审计指令，基于提供的证据进行最终同源性定性仲裁。

### 1. 宏观判定
- **定性结论**: **独立开发 (相似度: 6.49%)**
  共同 AST 指纹仅 165 个，目标项目覆盖率 7.73%，参考项目覆盖率 28.8%。所有重合点高度集中于双方共同依赖的第三方 `ext4_rs` 库，而操作系统核心（进程、内存、中断、驱动等）模块零重合。这表明两个项目在核心架构上存在根本性差异，不存在整体衍生关系。

### 2. 全模块覆盖矩阵解读
- **进程管理**: 仅参考项目存在（37个函数），目标项目无此模块。无共同 AST 指纹。证据强度：**未形成**。
- **任务调度/异步运行时**: 仅参考项目存在（22个函数），目标项目无此模块。无共同 AST 指纹。证据强度：**未形成**。
- **内存管理**: 仅参考项目存在（140个函数），目标项目无此模块。无共同 AST 指纹。证据强度：**未形成**。
- **中断/异常处理**: 仅参考项目存在（9个函数），目标项目无此模块。无共同 AST 指纹。证据强度：**未形成**。
- **系统调用**: 仅参考项目存在（3个函数），目标项目无此模块。无共同 AST 指纹。证据强度：**未形成**。
- **文件系统**: 仅参考项目存在（67个函数），目标项目无此模块。无共同 AST 指纹。证据强度：**未形成**。
- **设备驱动**: 仅参考项目存在（14个函数），目标项目无此模块。无共同 AST 指纹。证据强度：**未形成**。
- **网络协议栈**: 双方均未体现。证据强度：**未形成**。
- **文件系统/第三方库**: 双方均存在且有结构重合。目标项目 489 个函数，参考项目 323 个函数，共同指纹 164 个。目标覆盖率 41.62%，参考覆盖率 67.21%。这是唯一的重合板块，但源于共同依赖的 `ext4_rs` 上游库。证据强度：**中**。
- **网络协议栈/第三方库**: 仅目标项目存在（109个函数），参考项目无此模块。无共同 AST 指纹。证据强度：**未形成**。
- **第三方库**: 仅目标项目存在（1987个函数），参考项目无此模块。无共同 AST 指纹。证据强度：**未形成**。
- **通用模块/其他**: 仅参考项目存在（71个函数），目标项目无此模块。无共同 AST 指纹。证据强度：**未形成**。

### 3. 底层 AST 关键证据指纹表
| 所属板块 | 目标项目路径 | 参考项目路径 | 证据类型 | 技术说明 |
| :--- | :--- | :--- | :--- | :--- |
| 文件系统/第三方库 | `crates/scarlet/ext4_rs_fixed/src/ext4_impls/extents.rs#find_extent` | `third/ext4_rs/src/ext4_impls/extents.rs#find_extent` | 同板块同名，调用流高度相似 | 均使用 `core::mem::transmute::<&[u32; 15], &[u8; 60]>` 进行类型强转，这是解析 ext4 extent 树根节点的特定惯用法，源于对同一磁盘格式的处理。 |
| 文件系统/第三方库 | `crates/scarlet/ext4_rs_fixed/src/ext4_defs/extents.rs#binsearch_idx` | `third/ext4_rs/src/ext4_defs/extents.rs#binsearch_idx` | 同板块同名，底层逻辑一致 | 二分查找索引的调用链 `Ext4ExtentIndex::load_from_u32 -> Ext4ExtentIndex::load_from_u8 -> size_of::<Ext4ExtentHeader> -> size_of::<Ext4ExtentIndex>` 完全一致，由 ext4 规范决定。 |
| 文件系统/第三方库 | `crates/scarlet/ext4_rs_fixed/src/fuse_interface/mod.rs#fuse_setattr` | `third/ext4_rs/src/fuse_interface/mod.rs#fuse_setattr` | 同板块同名，底层逻辑一致 | FUSE 属性设置逻辑中，对 `mode` 字段的位掩码解析与调用顺序 `InodeFileType::from_bits(...) -> InodePerm::from_bits(...) -> inode_ref.set_attr` 完全一致。 |
| 文件系统/第三方库 | `crates/scarlet/ext4_rs_fixed/src/ext4_impls/balloc.rs#balloc_alloc_block_from` | `third/ext4_rs/src/ext4_impls/balloc.rs#balloc_alloc_block_from` | 同板块同名，调用流高度相似 | 块分配的核心位图操作流 `Ext4BlockGroup::load_new -> block_group.get_block_bitmap_block -> ext4_bmap_bit_find_clr -> ext4_bmap_bit_set` 完全一致。 |
| 文件系统/第三方库 | `crates/scarlet/ext4_rs_fixed/src/ext4_impls/dir.rs#try_insert_to_existing_block` | `third/ext4_rs/src/ext4_impls/dir.rs#try_insert_to_existing_block` | 同板块同名，调用流高度相似 | 目录项插入逻辑 `Ext4DirEntry::try_from(&block.data[offset..]).unwrap -> core::mem::size_of::<Ext4FakeDirEntry> -> de.copy_to_slice` 完全一致。 |
| 文件系统/第三方库 | `crates/scarlet/ext4_rs_fixed/src/ext4_impls/extents.rs#create_new_leaf` | `third/ext4_rs/src/ext4_impls/extents.rs#create_new_leaf` | 同板块同名，实现差异 | 参考项目为 `unimplemented!`，目标项目独立实现了 `log::info! -> self.ext_grow_indepth -> self.insert_extent` 的完整逻辑，是独立开发的强证据。 |

### 4. 核心代码调用流对比剖析
* **【文件系统/第三方库 - Extent 树查找】**
* **底层技术析理**: 两者均遵循 ext4 文件系统标准，通过 `ExtentNode::load_from_data` 解析磁盘数据，并使用 `core::mem::transmute` 将根节点数据块从 `[u32; 15]` 强转为 `[u8; 60]` 以便进行字节级操作。这是由 ext4 磁盘布局（60字节的 extent header）决定的通用做法。然而，目标项目在查找时使用了 `extent.get_first_block` 和 `ExtentNode::load_from_data_mut`，而参考项目使用了 `extent.get_pblock` 和 `node.binsearch_extent`，显示出在具体查找策略和可变性处理上的实现差异。
* **代码溯源链接**:
  - 目标项目: `crates/scarlet/ext4_rs_fixed/src/ext4_impls/extents.rs#find_extent`
  - 参考项目: `third/ext4_rs/src/ext4_impls/extents.rs#find_extent`

* **【文件系统/第三方库 - 块分配】**
* **底层技术析理**: 两者均实现了标准的 ext4 块分配流程：加载块组描述符 -> 获取块位图块号 -> 在位图中查找空闲位 -> 设置位图占用位 -> 更新块组校验和。此调用流是 ext4 块分配算法的标准实现步骤，其相似性源于算法规范本身。目标项目在 `balloc_alloc_block` 和 `balloc_free_blocks` 等函数中展现了与参考项目相同的调用拓扑，但这是实现同一文件系统功能的必然结果。
* **代码溯源链接**:
  - 目标项目: `crates/scarlet/ext4_rs_fixed/src/ext4_impls/balloc.rs#balloc_alloc_block_from`
  - 参考项目: `third/ext4_rs/src/ext4_impls/balloc.rs#balloc_alloc_block_from`

### 5. 单方模块与降权项
- **单方模块**: 参考项目的进程管理、任务调度、内存管理、中断/异常处理、系统调用、文件系统、设备驱动等核心模块在目标项目中完全不存在。反之，目标项目独有的网络协议栈/第三方库、第三方库等模块在参考项目中也不存在。这些单方模块构成了两个项目架构的根本差异，是独立开发的最有力证据。
- **降权项**:
  - **跨板块同名函数 `unlink`**: 目标项目位于 `crates/scarlet/src/fs/ext4.rs`，属于上层 VFS 封装，采用函数式迭代；参考项目位于 `third/ext4_rs/src/ext4_impls/ext4.rs`，属于底层库实现。两者架构层级和调用风格不同，已降权为弱信号。
  - **Rust 泛型与标准库调用**: `Ok`, `Err`, `Some`, `Vec::new`, `assert!`, `Default::default`, `Arc::clone` 等 AST 指纹是 Rust 语言的通用惯用法，其相似性源于语言本身，不构成同源证据。
  - **第三方公共依赖**: 所有重合证据均位于 `ext4_rs` 系列 crate 中，该 crate 是公开的第三方库。双方共同依赖此库，其模块结构、函数名和核心算法由上游决定，不能作为一方抄袭另一方的证据。

### 6. 保守结论
本次审计的客观 AST 相似度仅为 6.49%，且所有重合点均集中于双方共同依赖的第三方 `ext4_rs` 库。操作系统内核的核心子系统（进程、内存、中断、驱动等）在 AST 层面零重合，表明两个项目在架构设计、技术栈和实现路径上存在根本性差异。目标项目在 `create_new_leaf` 等函数中补全了参考项目未实现的逻辑，进一步支持了独立开发的结论。因此，整体同源性风险极低。现有证据无法支持“抄袭”或“直接衍生”的指控，观察到的局部相似性可合理解释为对同一开源上游库的共同继承和对同一技术规范（ext4）的独立实现。

------------------------------

