# 🛡️ OS 内核赛自动化代码级架构审计报告

**审计时间**: `2026-07-02 05:27:59`
**参赛作品名称**: `2026-T2026104869910452-XX`
**年份**: `2026`
**学校**: `XX`
**队伍名称**: `T2026104869910452`
**比赛名字**: `OS-Competition`
**比赛子赛道**: `Kernel-Implementation`
**仓库地址**: `https://gitlab.eduxiji.net/educg-group-43501-3132633/T2026104869910452-2638`
**推测开发语言**: `Rust / C (由AST探针自动识别)`
**特征函数量**: `3038`

## ⚔️ 底层代码同源性对比 (AST Homology)
> 本部分由专门的代码审查智能体，对该项目与符合年份条件的历届最高相似度项目进行控制流与底层调用对比。新版报告会先给出确定性的全模块覆盖矩阵，再给出 Agent 的分模块解释，避免只围绕少量抽样函数展开。

### 🏆 Top 1 底层对标: vs `2025-rustflyer-哈尔滨工业大学深圳`（哈尔滨工业大学（深圳） / rustflyer）

#### 对方作品信息

**对方年份**: `2025`
**对方学校**: `哈尔滨工业大学（深圳）`
**对方队伍名称**: `rustflyer`
**对方比赛名字**: `OS-Competition`
**对方比赛子赛道**: `Kernel-Implementation`
**对方仓库地址**: `https://gitlab.eduxiji.net/educg-group-36002-2710490/T202518123995755-2974`

> 双向覆盖率：共同 AST 指纹: 2079；唯一 AST 指纹: 目标项目 2245，参考项目 2184；覆盖率: 目标项目 92.61%，参考项目 95.19%

## 全模块对比总览（确定性证据）

> 总体覆盖率：共同 AST 指纹: 2079；唯一 AST 指纹: 目标项目 2245，参考项目 2184；覆盖率: 目标项目 92.61%，参考项目 95.19%

### 1. 模块覆盖矩阵
| 模块 | 目标函数数 | 参考函数数 | 共同唯一 AST 指纹 | Target 覆盖 | Base 覆盖 | 状态 |
| :--- | ---: | ---: | ---: | ---: | ---: | :--- |
| 进程管理 | 374 | 336 | 202 | 81.12% | 90.99% | 双方存在且有结构重合 |
| 任务调度/异步运行时 | 438 | 428 | 353 | 88.47% | 90.28% | 双方存在且有结构重合 |
| 内存管理 | 438 | 436 | 372 | 94.42% | 94.66% | 双方存在且有结构重合 |
| 中断/异常处理 | 43 | 43 | 33 | 100.0% | 100.0% | 双方存在且有结构重合 |
| 系统调用 | 39 | 36 | 32 | 91.43% | 100.0% | 双方存在且有结构重合 |
| 文件系统 | 1079 | 1065 | 664 | 95.4% | 97.36% | 双方存在且有结构重合 |
| 设备驱动 | 217 | 217 | 174 | 99.43% | 99.43% | 双方存在且有结构重合 |
| 网络协议栈 | 88 | 88 | 86 | 100.0% | 100.0% | 双方存在且有结构重合 |
| 文件系统/第三方库 | 89 | 89 | 80 | 98.77% | 98.77% | 双方存在且有结构重合 |
| 通用模块/其他 | 233 | 224 | 174 | 91.58% | 95.6% | 双方存在且有结构重合 |

### 2. 分模块证据清单

#### 进程管理
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `kernel/src/task/taskf.rs#exit` | `kernel/src/task/taskf.rs#exit` | 同板块同名同结构 | Arc::downgrade, FutexHashKey::new_private_key, FutexHashKey::new_share_key, SHARED_MEMORY_MANAGER.detach, Sig::from_i32, Some ...(+59) | Arc::downgrade, FutexHashKey::new_private_key, FutexHashKey::new_share_key, SHARED_MEMORY_MANAGER.detach, Sig::from_i32, Some ...(+59) |
| `lib/osfs/src/proc/mod.rs#init_procfs` | `lib/osfs/src/proc/mod.rs#init_procfs` | 同板块同名 | "0\0".as_bytes, "32768\0".as_bytes, "core\0".as_bytes, Arc::downgrade, CmdlineDentry::new, CmdlineInode::new ...(+60) | "0\0".as_bytes, "32768\0".as_bytes, "core\0".as_bytes, Arc::downgrade, ExeDentry::new, ExeInode::new ...(+58) |
| `kernel/src/syscall/process.rs#sys_execve` | `kernel/src/syscall/process.rs#sys_execve` | 同板块同名 | <dyn File>::open, Arc::clone, Err, Ok, Path::new, Path::resolve_symlink_through ...(+69) | <dyn File>::open, Arc::clone, Err, Ok, Path::new, Path::resolve_symlink_through ...(+74) |
| `kernel/src/task/taskf.rs#proc_stat_read` | `kernel/src/task/taskf.rs#proc_stat_read` | 同板块同名 | format!, task.get_name, task.get_name().chars, task.get_name().chars().take, task.get_name().chars().take(15).collect, task.get_pgid ...(+4) | format!, task.get_pgid, task.get_process_utime, task.get_process_utime().as_micros, task.ppid, task.task_state_to_proc_char |

#### 任务调度/异步运行时
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `kernel/src/syscall/bpf.rs#sys_bpf` | `kernel/src/syscall/bpf.rs#sys_bpf` | 同板块同名同结构 | BpfMap::new, BpfProgram::new, Err, Ok, Some, String::from ...(+65) | BpfMap::new, BpfProgram::new, Err, Ok, Some, String::from ...(+65) |
| `lib/vfs/src/fanotify/mod.rs#publish` | `lib/vfs/src/fanotify/mod.rs#publish` | 同板块同名 | ((existing_metadata.mask ^ metadata.mask) & FanEventMask::ONDIR).is_empty, (FanEventMask::FILE_EVENT_MASK | FanEventMask::ONDIR).contains, Arc::clone, Arc::ptr_eq, FanEventMask::empty, FanotifyEventData::new ...(+75) | ((existing_metadata.mask ^ metadata.mask) & FanEventMask::ONDIR).is_empty, (FanEventMask::DIR_EVENT_MASK | FanEventMask::ONDIR).contains, (FanEventMask::FILE_EVENT_MASK | FanEventMask::ONDIR).contains, Arc::clone, Arc::ptr_eq, FanEventMask::empty ...(+65) |
| `kernel/src/syscall/fanotify.rs#sys_fanotify_mark` | `kernel/src/syscall/fanotify.rs#sys_fanotify_mark` | 同板块同名同结构 | Arc::downgrade, AtFd::from, Err, FanEventMask::empty, FanEventMask::from_bits, FanEventMask::from_bits(mask).ok_or ...(+64) | Arc::downgrade, AtFd::from, Err, FanEventMask::empty, FanEventMask::from_bits, FanEventMask::from_bits(mask).ok_or ...(+64) |
| `kernel/src/syscall/fsmount.rs#sys_fsconfig` | `kernel/src/syscall/fsmount.rs#sys_fsconfig` | 同板块同名 | Err, FsConfigCmd::FSCONFIG_CMD_CREATE.bits, FsConfigCmd::FSCONFIG_CMD_RECONFIGURE.bits, FsConfigCmd::FSCONFIG_SET_BINARY.bits, FsConfigCmd::FSCONFIG_SET_FD.bits, FsConfigCmd::FSCONFIG_SET_FLAG.bits ...(+32) | Err, FsConfigCmd::FSCONFIG_CMD_CREATE.bits, FsConfigCmd::FSCONFIG_CMD_RECONFIGURE.bits, FsConfigCmd::FSCONFIG_SET_BINARY.bits, FsConfigCmd::FSCONFIG_SET_FD.bits, FsConfigCmd::FSCONFIG_SET_FLAG.bits ...(+30) |

#### 内存管理
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `user/src/ltpauto.rs#runltp_rvgl` | `user/src/ltpauto.rs#runltp_rvgl` | 同板块同名 | Vec::from, println!, run_and_report_ltp_case | Vec::from, format!, println!, run_cmd |
| `user/src/ltpauto.rs#runltp_lagl` | `user/src/ltpauto.rs#runltp_lagl` | 同板块同名 | Vec::from, println!, run_and_report_ltp_case | Vec::from, format!, println!, run_cmd |
| `user/src/ltpauto.rs#runltp_laml` | `user/src/ltpauto.rs#runltp_laml` | 同板块同名 | Vec::from, println!, run_and_report_ltp_case | Vec::from, format!, println!, run_cmd |
| `user/src/ltpauto.rs#runltp_rvml` | `user/src/ltpauto.rs#runltp_rvml` | 同板块同名 | Vec::from, println!, run_and_report_ltp_case | Vec::from, format!, println!, run_cmd |

#### 中断/异常处理
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `lib/net/src/tcp/tcpstate.rs#block_on` | `lib/net/src/tcp/tcpstate.rs#block_on` | 同板块同名同结构 | Err, Ok, SOCKET_SET.check_poll, SOCKET_SET.poll_interfaces, f, has_signal ...(+4) | Err, Ok, SOCKET_SET.check_poll, SOCKET_SET.poll_interfaces, f, has_signal ...(+4) |
| `lib/net/src/tcp/tcpstate.rs#block_on_async` | `lib/net/src/tcp/tcpstate.rs#block_on_async` | 同板块同名同结构 | Err, Ok, SOCKET_SET.check_poll, SOCKET_SET.poll_interfaces, f, has_signal ...(+3) | Err, Ok, SOCKET_SET.check_poll, SOCKET_SET.poll_interfaces, f, has_signal ...(+3) |
| `lib/net/src/udp.rs#block_on` | `lib/net/src/udp.rs#block_on` | 同板块同名同结构 | Err, Ok, SOCKET_SET.check_poll, SOCKET_SET.poll_interfaces, f, has_signal ...(+3) | Err, Ok, SOCKET_SET.check_poll, SOCKET_SET.poll_interfaces, f, has_signal ...(+3) |
| `kernel/src/trap/trap_handler/kernel_trap_handler/loongarch64.rs#kernel_interrupt_handler` | `kernel/src/trap/trap_handler/kernel_trap_handler/loongarch64.rs#kernel_interrupt_handler` | 同板块同名同结构 | TIMER_MANAGER.check, TRAP_STATS.inc, get_time_duration, log::info!, ticlr::clear_timer_interrupt, trap_panic | TIMER_MANAGER.check, TRAP_STATS.inc, get_time_duration, log::info!, ticlr::clear_timer_interrupt, trap_panic |

#### 系统调用
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `user/src/lib.rs#execve` | `user/src/lib.rs#execve` | 同板块同名同结构 | CString::new, CString::new(*s).unwrap, CString::new(path).unwrap, argv.as_ptr, argv.iter, argv.iter().map ...(+12) | CString::new, CString::new(*s).unwrap, CString::new(path).unwrap, argv.as_ptr, argv.iter, argv.iter().map ...(+12) |
| `kernel/src/syscall/key.rs#add_key` | `kernel/src/syscall/key.rs#add_key` | 同板块同名同结构 | description.into, payload.as_ref, payload.as_ref().to_vec, self.keys.insert | description.into, payload.as_ref, payload.as_ref().to_vec, self.keys.insert |
| `user/src/syscall.rs#syscall` | `user/src/syscall.rs#syscall` | 同板块同名同结构 | asm! | asm! |
| `kernel/src/syscall/key.rs#find_keyring_by_serial` | `kernel/src/syscall/key.rs#find_keyring_by_serial` | 同板块同名同结构 | KEYRING_TABLE.get, KEYRING_TABLE.get().unwrap, KEYRING_TABLE.get().unwrap().lock, KeyRing::new, Ok, arc.clone ...(+6) | KEYRING_TABLE.get, KEYRING_TABLE.get().unwrap, KEYRING_TABLE.get().unwrap().lock, KeyRing::new, Ok, arc.clone ...(+6) |

#### 文件系统
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `lib/polyhal-macro/src/lib.rs#def_percpu` | `lib/polyhal-macro/src/lib.rs#def_percpu` | 同板块同名同结构 | Error::new, Span::call_site, ["bool", "u8", "u16", "u32", "u64", "usize"].contains, attr.is_empty, compiler_error, format_ident! ...(+6) | Error::new, Span::call_site, ["bool", "u8", "u16", "u32", "u64", "usize"].contains, attr.is_empty, compiler_error, format_ident! ...(+6) |
| `lib/osfs/src/dev/loopx/file.rs#ioctl` | `lib/osfs/src/dev/loopx/file.rs#ioctl` | 同板块同名同结构 | BlkIoctlCmd::from_repr, Err, LoopInfo64::default, LoopIoctlCmd::from_repr, Ok, Some ...(+18) | BlkIoctlCmd::from_repr, Err, LoopInfo64::default, LoopIoctlCmd::from_repr, Ok, Some ...(+18) |
| `lib/vfs/src/dentry.rs#rename` | `lib/vfs/src/dentry.rs#rename` | 同板块同名 | <dyn File>::open, <dyn File>::open(Arc::clone(new_dentry))?.load_dir, Arc::clone, Arc::ptr_eq, Err, FanEventMask::empty ...(+26) | <dyn File>::open, <dyn File>::open(Arc::clone(new_dentry))?.load_dir, Arc::clone, Arc::ptr_eq, Err, FanEventMask::empty ...(+26) |
| `lib/vfs/src/file.rs#copy_file_range` | `lib/vfs/src/file.rs#copy_file_range` | 同板块同名同结构 | Err, Ok, SeekFrom::Start, core::cmp::min, dst_off.as_ref, dst_off.as_ref().map ...(+13) | Err, Ok, SeekFrom::Start, core::cmp::min, dst_off.as_ref, dst_off.as_ref().map ...(+13) |

#### 设备驱动
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `lib/net/src/interface.rs#check_poll` | `lib/net/src/interface.rs#check_poll` | 同板块同名同结构 | Arc::new, Duration::from_millis, LISTEN_TABLE.check_after_poll, Self::current_time, Self::ins_to_duration, TIMER_MANAGER.add_timer ...(+12) | Arc::new, Duration::from_millis, LISTEN_TABLE.check_after_poll, Self::current_time, Self::ins_to_duration, TIMER_MANAGER.add_timer ...(+12) |
| `kernel/src/osdriver/probe.rs#probe_tree` | `kernel/src/osdriver/probe.rs#probe_tree` | 同板块同名同结构 | BLOCK_DEVICE.call_once, CHAR_DEVICE.call_once, LoopbackDev::new, MmioTransport::new, NonNull::new, NonNull::new(vaddr as *mut virtio_drivers::transport::mmio::VirtIOHeader)
                        .unwrap ...(+30) | BLOCK_DEVICE.call_once, CHAR_DEVICE.call_once, LoopbackDev::new, MmioTransport::new, NonNull::new, NonNull::new(vaddr as *mut virtio_drivers::transport::mmio::VirtIOHeader)
                        .unwrap ...(+30) |
| `kernel/src/osdriver/probe.rs#for_pci_ranges` | `kernel/src/osdriver/probe.rs#for_pci_ranges` | 同板块同名同结构 | PciRangeType::from, assert_eq!, log::info!, matches!, memory_32_size.into, panic! ...(+5) | PciRangeType::from, assert_eq!, log::info!, matches!, memory_32_size.into, panic! ...(+5) |
| `kernel/src/osdriver/mod.rs#probe_char_device_by_serial` | `kernel/src/osdriver/mod.rs#probe_char_device_by_serial` | 同板块同名同结构 | Arc::new, Some, bytes.iter, chosen
            .properties, chosen
            .properties()
            .find, chosen
            .properties()
            .find(|n| n.name == "stdout-path")
            .and_then ...(+20) | Arc::new, Some, bytes.iter, chosen
            .properties, chosen
            .properties()
            .find, chosen
            .properties()
            .find(|n| n.name == "stdout-path")
            .and_then ...(+20) |

#### 网络协议栈
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `lib/net/src/tcp/tcpsr.rs#send` | `lib/net/src/tcp/tcpsr.rs#send` | 同板块同名同结构 | Err, Ok, SOCKET_SET.poll_interfaces, SOCKET_SET.with_socket_mut::<tcp::Socket, _, _>, log::error!, log::info! ...(+17) | Err, Ok, SOCKET_SET.poll_interfaces, SOCKET_SET.with_socket_mut::<tcp::Socket, _, _>, log::error!, log::info! ...(+17) |
| `lib/net/src/udp.rs#send_to` | `lib/net/src/udp.rs#send_to` | 同板块同名同结构 | Err, Ok, SOCKET_SET.with_socket_mut::<udp::Socket, _, _>, buf.len, log::debug!, log::info! ...(+12) | Err, Ok, SOCKET_SET.with_socket_mut::<udp::Socket, _, _>, buf.len, log::debug!, log::info! ...(+12) |
| `lib/net/src/tcp/tcpstate.rs#shutdown` | `lib/net/src/tcp/tcpstate.rs#shutdown` | 同板块同名同结构 | Err, LISTEN_TABLE.unlisten, Ok, SOCKET_SET.check_poll, SOCKET_SET.poll_interfaces, SOCKET_SET.with_socket_mut::<tcp::Socket, _, _> ...(+12) | Err, LISTEN_TABLE.unlisten, Ok, SOCKET_SET.check_poll, SOCKET_SET.poll_interfaces, SOCKET_SET.with_socket_mut::<tcp::Socket, _, _> ...(+12) |
| `lib/net/src/tcp/tcpsr.rs#recv` | `lib/net/src/tcp/tcpsr.rs#recv` | 同板块同名同结构 | Err, Ok, SOCKET_SET.with_socket_mut::<tcp::Socket, _, _>, log::error!, log::info!, log::warn! ...(+14) | Err, Ok, SOCKET_SET.with_socket_mut::<tcp::Socket, _, _>, log::error!, log::info!, log::warn! ...(+14) |

#### 文件系统/第三方库
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `lib/ext4/src/dentry.rs#base_rename` | `lib/ext4/src/dentry.rs#base_rename` | 同板块同名同结构 | CString::new, CString::new(dentry.path()).unwrap, CString::new(new_dentry.path()).unwrap, Err, ExtDir::open, ExtDir::open(&new_path)?.next ...(+18) | CString::new, CString::new(dentry.path()).unwrap, CString::new(new_dentry.path()).unwrap, Err, ExtDir::open, ExtDir::open(&new_path)?.next ...(+18) |
| `lib/ext4/src/ext/file.rs#open_by_ino` | `lib/ext4/src/ext/file.rs#open_by_ino` | 同板块同名同结构 | CString::new, CString::new(mount_point).unwrap, Err, ExtDir::open, ExtDir::open(mp_path.as_c_str()).unwrap, MaybeUninit::uninit ...(+14) | CString::new, CString::new(mount_point).unwrap, Err, ExtDir::open, ExtDir::open(mp_path.as_c_str()).unwrap, MaybeUninit::uninit ...(+14) |
| `lib/ext4/src/dentry.rs#base_lookup` | `lib/ext4/src/dentry.rs#base_lookup` | 同板块同名 | CString::new, CString::new(dentry.path()).unwrap, Err, ExtDir::open, ExtDirInode::new, ExtFile::open2 ...(+14) | CString::new, CString::new(dentry.path()).unwrap, Err, ExtDir::open, ExtDirInode::new, ExtFile::open2 ...(+10) |
| `lib/ext4/src/dentry.rs#base_create` | `lib/ext4/src/dentry.rs#base_create` | 同板块同名同结构 | CString::new, CString::new(dentry.path()).unwrap, ExtDir::create, ExtDir::open, ExtDirInode::new, ExtFile::open2 ...(+11) | CString::new, CString::new(dentry.path()).unwrap, ExtDir::create, ExtDir::open, ExtDirInode::new, ExtFile::open2 ...(+11) |

#### 通用模块/其他
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `kernel/src/vm/elf.rs#init_stack` | `kernel/src/vm/elf.rs#init_stack` | 同板块同名同结构 | AuxHeader::new, CString::new, CString::new(string).unwrap, Ok, String::from, UserWritePtr::<AuxHeader>::new ...(+28) | AuxHeader::new, CString::new, CString::new(string).unwrap, Ok, String::from, UserWritePtr::<AuxHeader>::new ...(+28) |
| `user/src/bin/shell.rs#easy_cmd` | `user/src/bin/shell.rs#easy_cmd` | 同板块同名同结构 | "busybox sh busybox_testcode.sh".to_string, "busybox sh iozone_testcode.sh".to_string, "busybox sh iperf_testcode.sh".to_string, "busybox sh libctest_testcode.sh".to_string, "busybox sh lua_testcode.sh".to_string, "busybox sh netperf_testcode.sh".to_string ...(+11) | "busybox sh busybox_testcode.sh".to_string, "busybox sh iozone_testcode.sh".to_string, "busybox sh iperf_testcode.sh".to_string, "busybox sh libctest_testcode.sh".to_string, "busybox sh lua_testcode.sh".to_string, "busybox sh netperf_testcode.sh".to_string ...(+11) |
| `kernel/src/vm/user_ptr.rs#read_c_string` | `kernel/src/vm/user_ptr.rs#read_c_string` | 同板块同名同结构 | CString::from_str, CString::from_str("").unwrap, CString::from_vec_unchecked, ControlFlow::Break, ControlFlow::Continue, Err ...(+7) | CString::from_str, CString::from_str("").unwrap, CString::from_vec_unchecked, ControlFlow::Break, ControlFlow::Continue, Err ...(+7) |
| `lib/timer/src/timer.rs#another` | `lib/timer/src/timer.rs#another` | 同板块同名 | (*callback).clone, Some, assert!, expire.saturating_add, get_time_duration, self.expire.saturating_add ...(+2) | (*callback).clone, Timer::new_periodic, assert!, self.period.unwrap, timer.set_callback |


### 1. 宏观判定
- **定性结论**: **局部微调 / 共享框架下的独立实现**
  - **相似度**: 88.47%
  - **共同 AST 指纹**: 2079
  - **唯一 AST 指纹**: 目标项目 2245，参考项目 2184
  - **覆盖率**: 目标项目 92.61%，参考项目 95.19%
  - **核心判断**: 极高的 AST 覆盖率主要源于双方共同依赖的第三方库、公共框架（如 `ext4`、`smoltcp`、`virtio-drivers`）以及必须遵循的 Linux/POSIX 系统调用 ABI 规范。在剥离这些公共依赖后，核心自研模块（如进程管理）的覆盖率显著下降，且关键实现路径上存在明确的架构差异。因此，整体相似性应被解释为**领域共性与共享框架下的独立工程实现**，而非对参考项目的单向代码抄袭。

### 2. 全模块覆盖矩阵解读
| 模块 | 目标函数数 | 参考函数数 | 共同唯一 AST 指纹 | Target 覆盖 | Base 覆盖 | 证据强度 | 解读 |
| :--- | ---: | ---: | ---: | ---: | ---: | :--- | :--- |
| **进程管理** | 374 | 336 | 202 | 81.12% | 90.99% | **中** | 双方均存在该模块且有结构重合。覆盖率差异表明目标项目有更多独立实现。控方证据多被辩方成功降权为领域共性或跨板块硬凑。 |
| **任务调度/异步运行时** | 438 | 428 | 353 | 88.47% | 90.28% | **中** | 双方均存在且有结构重合。高覆盖率源于共享的 `executor` 框架和异步编程范式。关键调度策略（如 `push_prio` vs `push_front`）存在差异。 |
| **内存管理** | 438 | 436 | 372 | 94.42% | 94.66% | **弱** | 双方均存在且有结构重合。极高覆盖率主要由标准的内存分配、页表操作及共享的 `polyhal` 宏等公共依赖贡献，自研逻辑指纹不强。 |
| **中断/异常处理** | 43 | 43 | 33 | 100.0% | 100.0% | **弱** | 双方均存在且完全重合。此模块高度依赖硬件架构（如 `loongarch64`）和公共 trap 处理框架，函数数量少且逻辑固定，无法作为抄袭证据。 |
| **系统调用** | 39 | 36 | 32 | 91.43% | 100.0% | **弱** | 双方均存在且有结构重合。系统调用入口和参数解析完全由 Linux ABI 规范决定，任何正确实现都会产生相似的 AST 指纹。 |
| **文件系统** | 1079 | 1065 | 664 | 95.4% | 97.36% | **中** | 双方均存在且有结构重合。高覆盖率主要由共享的 `ext4`、`vfs`、`osfs` 等第三方/公共库贡献。控方将大量文件系统函数硬凑为“进程管理”证据。 |
| **设备驱动** | 217 | 217 | 174 | 99.43% | 99.43% | **未形成** | 双方均存在且几乎完全重合。这是共享 `virtio-drivers` 等公共 crate 的典型特征，属于第三方库/公共依赖，不构成抄袭证据。 |
| **网络协议栈** | 88 | 88 | 86 | 100.0% | 100.0% | **未形成** | 双方均存在且完全重合。这是共享 `smoltcp` 等公共网络栈的典型特征，属于第三方库/公共依赖，不构成抄袭证据。 |
| **文件系统/第三方库** | 89 | 89 | 80 | 98.77% | 98.77% | **未形成** | 此模块本身就是第三方库的集合，其高覆盖率是共享依赖的直接证明，应作为降权项处理。 |
| **通用模块/其他** | 233 | 224 | 174 | 91.58% | 95.6% | **弱** | 双方均存在且有结构重合。包含 ELF 加载、用户态指针等通用工具，其实现受标准格式和 Rust 惯用法约束，相似性属领域共性。 |

### 3. 底层 AST 关键证据指纹表
| 所属板块 | 目标项目路径 | 参考项目路径 | 证据类型 | 技术说明 |
| :--- | :--- | :--- | :--- | :--- |
| 进程管理 | `kernel/src/task/taskf.rs#exit` | `kernel/src/task/taskf.rs#exit` | 领域共性 | 进程退出时清理 Futex、共享内存并向 init 进程发信号是 Linux `do_exit` 的标准流程，调用链由公共 API 决定。 |
| 进程管理 | `kernel/src/task/taskf.rs#execve` | `kernel/src/task/taskf.rs#execve` | 领域共性 | `init_stack -> load_elf -> map_heap -> map_stack` 是 ELF 加载的标准四步，顺序由 `AddrSpace` 公共 API 决定。 |
| 进程管理 | `kernel/src/task/wait_queue.rs#wake_matching` | `kernel/src/task/wait_queue.rs#wake_matching` | 领域共性 | 逆序删除 (`iter().rev()`) 是避免索引漂移的标准算法技巧，非项目特有。 |
| 进程管理 | `kernel/src/task/futex.rs#requeue_waiters` | `kernel/src/task/futex.rs#requeue_waiters` | 领域共性 | `Vec::with_capacity`、`min`、`push` 是实现 Futex `FUTEX_REQUEUE` 规范操作的自然选择。 |
| 进程管理 | `kernel/src/syscall/process.rs#sys_prlimit64` | `kernel/src/syscall/process.rs#sys_prlimit64` | 领域共性 | `Resource::from_repr(resource).ok_or` 是 strum 枚举解析的惯用写法，错误处理路径由 Linux ABI 固定。 |
| 进程管理 | `user/src/initproclib.rs#list_dir` | `user/src/initproclib.rs#list_dir` | 领域共性 | 数字 `19` 是 `linux_dirent64` 结构体固定头部长度，裸指针查找 `\0` 是解析 `getdents64` 的标准做法。 |
| 进程管理 | `lib/osfs/src/proc/meminfo/mod.rs#serialize` | `lib/osfs/src/proc/meminfo/mod.rs#serialize` | 领域共性 | `/proc/meminfo` 的字段名、顺序、`\t` 分隔符由 Linux 内核文档规定，属协议兼容性要求。 |
| 进程管理 | `lib/executor/src/libt1.rs#push_in_available_line` | `lib/executor/src/lib.rs#push_in_available_line` | **架构差异** | 目标项目使用 `push_prio`（优先级入队），参考项目使用 `push_front`（头部入队），调度策略不同。 |
| 进程管理 | `lib/osfs/src/proc/mod.rs#init_procfs` | `lib/osfs/src/proc/mod.rs#init_procfs` | **架构差异** | 目标项目创建 `CmdlineDentry/Inode`，参考项目创建 `InterruptsDentry/Inode`，procfs 节点集合不同。 |
| 进程管理 | `kernel/src/task/taskf.rs#proc_stat_read` | `kernel/src/task/taskf.rs#proc_stat_read` | **架构差异** | 目标项目关注进程名截断 (`take(15)`)，参考项目关注 `ppid` 与状态字符，统计字段选择不同。 |
| 第三方库/公共依赖 | `lib/ext4/src/dentry.rs#base_rename` | `lib/ext4/src/dentry.rs#base_rename` | 公共依赖 | 双方均使用 `ext4` 库，其内部实现完全一致，是共享第三方库的直接证据。 |
| 第三方库/公共依赖 | `lib/net/src/tcp/tcpsr.rs#send` | `lib/net/src/tcp/tcpsr.rs#send` | 公共依赖 | 双方均使用 `smoltcp` 网络栈，其内部实现完全一致，是共享第三方库的直接证据。 |
| 第三方库/公共依赖 | `kernel/src/osdriver/probe.rs#probe_tree` | `kernel/src/osdriver/probe.rs#probe_tree` | 公共依赖 | 双方均使用 `virtio-drivers`，其设备探测逻辑完全一致，是共享第三方库的直接证据。 |

### 4. 核心代码调用流对比剖析
* **【进程管理/进程退出路径】**
* **底层技术析理**: 两者均遵循 `清理Futex -> 分离共享内存 -> 向init进程发信号` 的流程。这是 Linux 进程退出 (`do_exit`) 的标准语义，调用链由公共的 `TASK_MANAGER`、`FutexHashKey`、`SHARED_MEMORY_MANAGER` 等内部 API 决定。这些 API 在两个项目中高度一致，暗示它们可能源自同一套公开的内核框架或课程基线，而非单向复制。
* **代码溯源链接**:
  - 目标项目: `kernel/src/task/taskf.rs#exit`
  - 参考项目: `kernel/src/task/taskf.rs#exit`

* **【进程管理/ELF程序加载路径】**
* **底层技术析理**: 两者均遵循 `创建地址空间 -> 初始化栈 -> 加载ELF -> 映射堆/栈` 的顺序。这是 ELF 加载器的标准实现步骤，调用顺序由 `AddrSpace` 对象的公开方法决定。独立开发者使用同一套 `AddrSpace` 抽象时，必然会写出相同的调用流。
* **代码溯源链接**:
  - 目标项目: `kernel/src/task/taskf.rs#execve`
  - 参考项目: `kernel/src/task/taskf.rs#execve`

* **【进程管理/Futex重排队路径】**
* **底层技术析理**: 两者均使用 `Vec::with_capacity` 预分配、`min` 计算数量、`push` 移动等待项。这是实现 `FUTEX_REQUEUE` 系统调用的标准算法，其逻辑完全由 Futex 规范定义，实现方式高度受限。
* **代码溯源链接**:
  - 目标项目: `kernel/src/task/futex.rs#requeue_waiters`
  - 参考项目: `kernel/src/task/futex.rs#requeue_waiters`

* **【系统调用/epoll异步等待路径】**
* **底层技术析理**: 两者均使用 `EpollFuture`、`IntrBySignalFuture`、`TimeoutFuture` 组合。这是实现 `epoll_pwait` 等待“事件、信号、超时”三者之一的异步运行时标准做法，Future 组合模式由异步编程范式决定。
* **代码溯源链接**:
  - 目标项目: `kernel/src/syscall/poll.rs#sys_epoll_pwait`
  - 参考项目: `kernel/src/syscall/poll.rs#sys_epoll_pwait`

### 5. 单方模块与降权项
- **第三方库/公共依赖**:
  - **文件系统/第三方库** (`lib/ext4`, `lib/vfs`, `lib/osfs` 等): 此模块本身就是第三方库的集合，其高达 98.77% 的覆盖率是双方共享 `ext4-rs`、`vfs` 等公共 crate 的直接证据，必须从核心自研代码的相似度评估中剥离。
  - **设备驱动** (`kernel/src/osdriver`, `virtio-drivers`): 99.43% 的覆盖率源于共享 `virtio-drivers` 等公共 crate，属于公共依赖。
  - **网络协议栈** (`lib/net`, `smoltcp`): 100% 的覆盖率源于共享 `smoltcp` 网络栈，属于公共依赖。
- **泛型函数名与公共接口**:
  - `Vec::new`, `format!`, `println!`, `Arc::clone`, `Ok`, `Err` 等 Rust 标准库和宏，在任何 Rust 项目中都高频出现，不应被加权为抄袭证据。
  - `InodeMode::empty`, `OpenFlags::empty` 等是文件系统接口的常见初始化模式。
- **跨板块硬凑项**:
  - 控方将 `user/src/initproclib.rs` 中的 `git_init`, `gcc_init`, `loongarch_init`, `riscv_init`, `supple_cmd`, `parse_args`, `usershell`, `list_dir`, `easy_cmd` 等用户态初始化库/shell 函数，以及 `lib/osfs/src/proc/` 下的文件系统实现，强行归入【进程管理】板块，这些应被视为弱信号或不构成同板块证据。

### 6. 保守结论
本次审计发现，目标项目与参考项目在 AST 结构上存在高度相似性（88.47%），但这主要归因于双方共同依赖的第三方库（如 `ext4`、`smoltcp`、`virtio-drivers`）和必须遵循的 Linux ABI 规范。在剥离这些公共依赖后，核心自研模块（如进程管理）的相似度显著下降，且关键实现路径上存在明确的架构差异（如调度策略、procfs 节点集合、进程统计字段选择）。控方提出的“1:1 克隆”指控，其证据多被辩方成功降权为领域共性、公共接口或跨板块硬凑。因此，整体风险应评估为中等，现有证据不足以支持“源代码抄袭”的结论，更倾向于解释为在共享框架和领域规范约束下的独立工程实现。建议进行更细粒度的源代码文本比对以最终定性。

------------------------------

