# 🛡️ OS 内核赛自动化代码级架构审计报告

**审计时间**: `2026-07-02 05:27:59`
**参赛作品名称**: `2026-T2026104869910452-XX`
**年份**: `2026`
**学校**: `XX`
**队伍名称**: `T2026104869910452`
**比赛名字**: `OS-Competition`
**比赛子赛道**: `Kernel-Implementation`
**仓库地址**: `https://gitlab.eduxiji.net/educg-group-43501-3132633/T2026104869910452-2638`
**推测开发语言**: `Rust / C (由AST探针自动识别)`
**特征函数量**: `3038`

## ⚔️ 底层代码同源性对比 (AST Homology)
> 本部分由专门的代码审查智能体，对该项目与符合年份条件的历届最高相似度项目进行控制流与底层调用对比。新版报告会先给出确定性的全模块覆盖矩阵，再给出 Agent 的分模块解释，避免只围绕少量抽样函数展开。

### 🏆 Top 4 底层对标: vs `2025-Del0n1x-哈尔滨工业大学深圳`（哈尔滨工业大学（深圳） / Del0n1x）

#### 对方作品信息

**对方年份**: `2025`
**对方学校**: `哈尔滨工业大学（深圳）`
**对方队伍名称**: `Del0n1x`
**对方比赛名字**: `OS-Competition`
**对方比赛子赛道**: `Kernel-Implementation`
**对方仓库地址**: `https://gitlab.eduxiji.net/educg-group-36002-2710490/T202518123995600-2523`

> 双向覆盖率：共同 AST 指纹: 120；唯一 AST 指纹: 目标项目 2245，参考项目 1681；覆盖率: 目标项目 5.35%，参考项目 7.14%

## 全模块对比总览（确定性证据）

> 总体覆盖率：共同 AST 指纹: 120；唯一 AST 指纹: 目标项目 2245，参考项目 1681；覆盖率: 目标项目 5.35%，参考项目 7.14%

### 1. 模块覆盖矩阵
| 模块 | 目标函数数 | 参考函数数 | 共同唯一 AST 指纹 | Target 覆盖 | Base 覆盖 | 状态 |
| :--- | ---: | ---: | ---: | ---: | ---: | :--- |
| 进程管理 | 374 | 239 | 7 | 2.81% | 3.85% | 双方存在且有结构重合 |
| 任务调度/异步运行时 | 438 | 254 | 5 | 1.25% | 2.22% | 双方存在且有结构重合 |
| 内存管理 | 438 | 396 | 3 | 0.76% | 0.9% | 双方存在且有结构重合 |
| 中断/异常处理 | 43 | 113 | 2 | 6.06% | 2.44% | 双方存在且有结构重合 |
| 系统调用 | 39 | 97 | 4 | 11.43% | 5.33% | 双方存在且有结构重合 |
| 文件系统 | 1079 | 496 | 24 | 3.45% | 6.52% | 双方存在且有结构重合 |
| 设备驱动 | 217 | 155 | 31 | 17.71% | 23.48% | 双方存在且有结构重合 |
| 网络协议栈 | 88 | 88 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |
| 文件系统/第三方库 | 89 | 44 | 2 | 2.47% | 4.65% | 双方存在且有结构重合 |
| 通用模块/其他 | 233 | 265 | 26 | 13.68% | 11.3% | 双方存在且有结构重合 |

### 2. 分模块证据清单

#### 进程管理
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `kernel/src/syscall/process.rs#sys_clone3` | `os/src/syscall/process.rs#sys_clone3` | 同板块同名 | CloneFlags::from_bits, CloneFlags::from_bits(args.flags & !0xff).ok_or, Err, Ok, PF_TABLE.get, PF_TABLE.get().unwrap ...(+25) | Err, info!, sys_clone, unlikely |
| `kernel/src/syscall/process.rs#sys_prlimit64` | `os/src/syscall/process.rs#sys_prlimit64` | 同板块同名 | Ok, RLimit::one, Resource::from_repr, Resource::from_repr(resource).ok_or, TASK_MANAGER.get_task, TASK_MANAGER.get_task(pid).ok_or ...(+14) | Err, Ok, RLimit64::new, RLimit64::new_bare, RlimResource::try_from_primitive, RlimResource::try_from_primitive(resource).map_err ...(+10) |
| `kernel/src/syscall/process.rs#sys_getrusage` | `os/src/syscall/process.rs#sys_getrusage` | 同板块同名 | Ok, Rusage::default, RusageType::from_repr, RusageType::from_repr(who).ok_or, UserWritePtr::<Rusage>::new, current_task ...(+8) | Err, Ok, Rusage::new, core::ptr::write, current_task, current_task().unwrap ...(+5) |
| `lib/executor/src/libt1.rs#spawn` | `os/src/task/executor.rs#spawn` | 同板块同名 | WithInfo, async_task::spawn, push_in_available_line | TASK_QUEUE.push_normal, TASK_QUEUE.push_prior, WithInfo, async_task::spawn, runnable.schedule, task.detach |

#### 任务调度/异步运行时
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `kernel/src/syscall/fs.rs#sys_splice` | `os/src/syscall/fs.rs#sys_splice` | 同板块同名 | Err, Ok, PF_TABLE
            .get, PF_TABLE
            .get()
            .is_some_and, SeekFrom::Start, Some ...(+94) | Err, Ok, core::ptr::write, current_task, current_task().unwrap, debug_point! ...(+24) |
| `kernel/src/syscall/fs.rs#sys_statx` | `os/src/syscall/fs.rs#sys_statx` | 同板块同名 | AtFd::from, Err, Ok, Path::resolve_symlink_through, StatxFlags::from_bits_truncate, StatxTimestamp::default ...(+25) | Err, Kstat::new, Ok, String::from, StxMask::from_bits, StxMask::from_bits(mask).ok_or ...(+21) |
| `kernel/src/syscall/signal.rs#sys_futex` | `os/src/syscall/sync.rs#sys_futex` | 同板块同名 | (!ts.is_valid()).then_some, Err, FutexAddr::new_with_check, FutexHashKey::new_key, FutexOp::exstract_futex_flags, FutexOp::exstract_main_futex_flags ...(+35) | Err, FutexHashKey::get_futex_key, FutexOp::from_bits, FutexOp::from_bits(futex_op).ok_or, Ok, atomic_load_acquire ...(+12) |
| `kernel/src/syscall/fs.rs#sys_copy_file_range` | `os/src/syscall/fs.rs#sys_copy_file_range` | 同板块同名 | Err, Ok, Some, UserReadPtr::<Offset>::new, UserReadPtr::<Offset>::new(off_in_ptr, &addr_space).read, UserReadPtr::<Offset>::new(off_out_ptr, &addr_space).read ...(+32) | (off_in != 0)
        .then, (off_in != 0)
        .then(|| unsafe { *(off_in as *const usize) })
        .unwrap_or, (off_out != 0)
        .then, (off_out != 0)
        .then(|| unsafe { *(off_out as *const usize) })
        .unwrap_or, Ok, buffer.truncate ...(+20) |

#### 内存管理
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `kernel/src/syscall/mod.rs#syscall` | `os/src/syscall/mod.rs#syscall` | 同板块同名 | SysError::ENOSYS.code, SyscallNo::from_repr, log::debug!, log::error!, log::warn!, print! ...(+193) | Ok, SysCode::from, sys_accept, sys_accept4, sys_bind, sys_brk ...(+121) |
| `kernel/src/syscall/fs.rs#sys_openat` | `os/src/syscall/fs.rs#sys_openat` | 同板块同名 | <dyn File>::open, AtFd::from, Err, InodeMode::from_bits_truncate, OpenFlags::from_bits, OpenFlags::from_bits(flags).ok_or ...(+58) | Err, FdInfo::new, Ok, OpenFlags::from_bits, OpenFlags::from_bits(flags as i32).ok_or, SYS_OPENAT_MODE.store ...(+19) |
| `lib/driver/src/block/dw_mshc/mmc.rs#send_cmd` | `os/src/drivers/vf2/dw/vf2_sdio.rs#send_cmd` | 同板块同名 | CMD::offset, CMDARG::offset, RESP::offset, RINSTS::offset, Some, arg.into ...(+27) | CMD::offset, CMDARG::offset, RESP::offset, RINSTS::offset, Some, arg.into ...(+27) |
| `kernel/src/main.rs#rust_main` | `os/src/main.rs#rust_main` | 同板块同名 | arch::time::init_timer, arch::trap::init, boot::clear_bss, disable_log, executor::init, executor::task_run_always_alone ...(+20) | FIRST_HART
        .compare_exchange, FIRST_HART
        .compare_exchange(true, false, Ordering::SeqCst, Ordering::SeqCst)
        .is_ok, INIT_FINISHED.store, START_HART_ID.store, arch_init, block_on ...(+20) |

#### 中断/异常处理
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `lib/arch/src/trap/loongarch64.rs#set_trap_handler` | `os/src/hal/la64/trap/mod.rs#set_trap_handler` | 同板块同名 | crmd::read, crmd::read().ie, disable_interrupt, ecfg::set_vs, eentry::set_eentry, enable_interrupt | ecfg::set_vs, eentry::set_eentry |
| `kernel/src/trap/trap_handler/kernel_trap_handler/riscv64.rs#kernel_trap_handler` | `os/src/hal/rv64/trap/kernel_trap.rs#kernel_trap_handler` | 同板块同名 | Exception::from_number, Exception::from_number(e).unwrap, Interrupt::from_number, Interrupt::from_number(i).unwrap, exception_handler, interrupt_handler ...(+2) | unimplemented! |
| `lib/arch/src/trap/riscv64.rs#set_trap_handler` | `os/src/hal/rv64/trap/mod.rs#set_trap_handler` | 同板块同名 | Stvec::from_bits, mode.into, stvec.set_address, stvec.set_trap_mode, stvec::write | ecfg::set_vs, eentry::set_eentry |
| `lib/arch/src/trap/riscv64.rs#enable_interrupt` | `os/src/sync/interrupt.rs#enable_interrupt` | 同板块同名同结构 | interrupt::enable | sstatus::set_sie |

#### 系统调用
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `user/src/lib.rs#execve` | `user/src/lib.rs#execve` | 同板块同名 | CString::new, CString::new(*s).unwrap, CString::new(path).unwrap, argv.as_ptr, argv.iter, argv.iter().map ...(+12) | (*s).as_ptr, argv.as_ptr, argv.iter, argv.iter().map, argv.iter().map(|s| (*s).as_ptr() as usize).collect, argv.push ...(+6) |
| `user/src/syscall.rs#syscall` | `user/src/syscall.rs#syscall` | 同板块同名 | asm! | asm! |
| `user/src/syscall.rs#syscall` | `user/src/syscall.rs#syscall` | 同板块同名 | asm! | asm! |
| `kernel/src/syscall/mm.rs#sys_membarrier` | `os/src/syscall/mm.rs#sys_membarrier` | 同板块同名 | Ok, log::error! | Ok, info! |

#### 文件系统
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `lib/osfs/src/dev/loopx/file.rs#ioctl` | `os/src/fs/devfs/tty.rs#ioctl` | 同板块同名 | BlkIoctlCmd::from_repr, Err, LoopInfo64::default, LoopIoctlCmd::from_repr, Ok, Some ...(+18) | Err, Ok, TtyIoctlCmd::try_from, TtyIoctlCmd::try_from(op).map_err, arg.into, info! ...(+7) |
| `lib/vfs/src/dentry.rs#rename` | `os/src/fs/tmp/file.rs#rename` | 同板块同名 | <dyn File>::open, <dyn File>::open(Arc::clone(new_dentry))?.load_dir, Arc::clone, Arc::ptr_eq, Err, FanEventMask::empty ...(+26) | Err, Ok, ext4file.file_rename, ext4file.file_rename(&old_path, &new_path).unwrap, flags.contains, new_path.len ...(+4) |
| `lib/osfs/src/dev/tty/file.rs#ioctl` | `os/src/fs/devfs/char.rs#ioctl` | 同板块同名 | Ok, TtyIoctlCmd::from_repr, log::error!, log::info!, self.inner.lock, todo! ...(+1) | TtyIoctlCmd::try_from, TtyIoctlCmd::try_from(op).map_err, block_on, self.dev.ioctl |
| `lib/driver/src/block/dw_mshc/mmc.rs#reset_clock` | `os/src/drivers/vf2/dw/vf2_sdio.rs#reset_clock` | 同板块同名 | CLKDIV::new, CLKDIV::new().with_clk_divider0, CLKDIV::offset, CLKENA::new, CLKENA::new().with_cclk_enable, CLKENA::offset ...(+8) | CLKDIV::new, CLKDIV::new().with_clk_divider0, CLKDIV::offset, CLKENA::new, CLKENA::new().with_cclk_enable, CLKENA::offset ...(+8) |

#### 设备驱动
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `kernel/src/osdriver/probe.rs#for_pci_ranges` | `os/src/drivers/virtio_driver/pci.rs#for_pci_ranges` | 同板块同名 | PciRangeType::from, assert_eq!, log::info!, matches!, memory_32_size.into, panic! ...(+5) | PciRangeType::from, assert_eq!, info!, matches!, memory_32_size.into, panic! ...(+5) |
| `kernel/src/net/sockopt.rs#try_from` | `os/src/drivers/tty/tty_core.rs#try_from` | 同板块同名 | Ok, log::warn! | Err, Ok |
| `lib/driver/src/serial/uart8250.rs#init_u32` | `os/src/drivers/tty/serial/ns16550a.rs#init_u32` | 同板块同名 | log::info!, reg.byte_add, reg.byte_add(0 << self.reg_shift)
                .write_volatile, reg.byte_add(1 << self.reg_shift)
                .write_volatile, reg.byte_add(FCR << self.reg_shift).write_volatile, reg.byte_add(IER << self.reg_shift).write_volatile ...(+2) | info!, reg.byte_add, reg.byte_add(0 << self.reg_shift)
                    .write_volatile, reg.byte_add(1 << self.reg_shift)
                    .write_volatile, reg.byte_add(FCR << self.reg_shift).write_volatile, reg.byte_add(IER << self.reg_shift).write_volatile ...(+2) |
| `lib/driver/src/serial/uart8250.rs#init_u8` | `os/src/drivers/tty/serial/ns16550a.rs#init_u8` | 同板块同名 | reg.byte_add, reg.byte_add(0 << self.reg_shift)
                .write_volatile, reg.byte_add(1 << self.reg_shift)
                .write_volatile, reg.byte_add(IER << self.reg_shift).write_volatile, reg.byte_add(LCR << self.reg_shift).write_volatile, reg.byte_add(MCR << self.reg_shift).write_volatile | reg.byte_add, reg.byte_add(0 << self.reg_shift)
                    .write_volatile, reg.byte_add(1 << self.reg_shift)
                    .write_volatile, reg.byte_add(IER << self.reg_shift).write_volatile, reg.byte_add(LCR << self.reg_shift).write_volatile, reg.byte_add(MCR << self.reg_shift).write_volatile |

#### 网络协议栈
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `lib/net/src/tcp/tcpstate.rs#shutdown` | `os/src/net/tcp.rs#shutdown` | 同板块同名 | Err, LISTEN_TABLE.unlisten, Ok, SOCKET_SET.check_poll, SOCKET_SET.poll_interfaces, SOCKET_SET.with_socket_mut::<tcp::Socket, _, _> ...(+12) | NET_DEV.lock, NET_DEV.lock().poll, Ok, SOCKET_SET.lock, Some, binding.get_mut::<tcp::Socket> ...(+5) |
| `lib/net/src/tcp/listentable.rs#accept` | `os/src/net/udp.rs#accept` | 同板块同名 | Err, Ok, get_addr_tuple, is_connected, is_connected(handle).then, log::debug! ...(+11) | Err |
| `lib/net/src/tcp/tcpstate.rs#bind` | `os/src/net/tcp.rs#bind` | 同板块同名 | Err, Ok, get_ephemeral_port, log::info!, log::warn!, self.local_addr.get ...(+5) | Err, IpEndpoint::try_from, Ok, addr.clone, info!, self.do_bind ...(+2) |
| `lib/net/src/tcp/tcpstate.rs#listen` | `os/src/net/tcp.rs#listen` | 同板块同名 | LISTEN_TABLE.listen, Ok, SOCKET_SET.add, SOCKET_SET.with_socket_mut::<tcp::Socket, _, _>, SocketSetWrapper::new_tcp_socket, log::debug! ...(+9) | Err, Ok, info!, self.set_state, self.sockmeta.lock, self.with_socket ...(+5) |

#### 文件系统/第三方库
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `lib/ext4/src/ext/file.rs#rename` | `os/src/fs/ext4/inode.rs#rename` | 同板块同名 | Err, Ok, SysError::from_i32, ext4_frename, log::warn!, new_path.as_ptr ...(+1) | Err, Ext4Inode::new, Ok, block_on, ext4file.file_rename, new_dentry.bind ...(+18) |
| `lib/ext4/src/ext/file.rs#link` | `os/src/fs/ext4/inode.rs#link` | 同板块同名 | Err, Ok, SysError::from_i32, ext4_flink, log::warn!, path.as_ptr ...(+1) | Err, Ext4Inode::new, Ok, bare_dentry.bind, bare_dentry.get_abs_path, bare_dentry.is_valid ...(+9) |
| `lib/ext4/src/ext/file.rs#truncate` | `os/src/fs/ext4/inode.rs#truncate` | 同板块同名 | Err, Ok, SysError::from_i32, ext4_ftruncate, log::warn!, self.0.get_mut | Errno::EIO.into, file.file_close, file.file_open, file.file_truncate, file.get_path, info! ...(+11) |
| `lib/ext4/src/ext/file.rs#unlink` | `os/src/fs/ext4/inode.rs#unlink` | 同板块同名 | Err, Ok, SysError::from_i32, ext4_fremove, log::warn!, path.as_ptr | Err, Ok, debug_point!, info!, lock_file.dir_rm, lock_file.file_remove ...(+6) |

#### 通用模块/其他
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `lib/mutex/src/mutex/spin_mutex.rs#lock` | `os/src/sync/mutex/spin.rs#lock` | 同板块同名 | S::before_lock, self
                .lock
                .compare_exchange, self
                .lock
                .compare_exchange(false, true, Ordering::SeqCst, Ordering::Relaxed)
                .is_ok, self.wait_unlock | S::before_lock, self
                .lock
                .compare_exchange, self
                .lock
                .compare_exchange(false, true, Ordering::Acquire, Ordering::Relaxed)
                .is_ok, self.wait_unlock |
| `lib/arch/src/console/loongarch64.rs#getchar` | `user/src/console.rs#getchar` | 同板块同名 | Some, ptr.add, ptr.add(0).read_volatile, ptr.add(5).read_volatile | read |
| `kernel_snippet.rs#lock` | `os/src/sync/mutex/sleep.rs#lock` | 同板块同名 | core::hint::spin_loop, self
            .locked
            .compare_exchange, self
            .locked
            .compare_exchange(false, true, Ordering::Acquire, Ordering::Relaxed)
            .is_err | Pin::new_unchecked, Pin::new_unchecked(future).init, SleepMutexFuture::new |
| `lib/arch/src/hart/loongarch64.rs#hart_start` | `os/src/hal/rv64/arch/sbi.rs#hart_start` | 同板块同名 | 内部闭环逻辑 | sbi_call! |


#### 1. 宏观判定
- **定性结论**: **独立开发 / 底层架构显著差异**
  基于客观 AST 相似度 3.15%，以及目标项目覆盖率 5.35%、参考项目覆盖率 7.14% 的量化指标，双方代码库在整体结构上不存在系统性复用关系。共同指纹主要集中在由 Linux ABI 和 Rust 生态强约束的领域，属于典型的领域共性与标准范式，而非代码抄袭。

#### 2. 全模块覆盖矩阵解读
- **进程管理**: 双方均存在。共同 AST 指纹 7 个，目标覆盖率 2.81%，参考覆盖率 3.85%。证据强度 **弱**。重合点均为 Linux 标准系统调用名及通用数据结构操作，外部调用图谱揭示了两者在任务管理、并发模型上的根本差异。
- **任务调度/异步运行时**: 双方均存在。共同 AST 指纹 5 个，目标覆盖率 1.25%，参考覆盖率 2.22%。证据强度 **弱**。调度器架构（队列数量、回退策略、取任务方向）完全不同，仅共享了 `async_task::spawn` 等第三方库 API。
- **内存管理**: 双方均存在。共同 AST 指纹 3 个，目标覆盖率 0.76%，参考覆盖率 0.9%。证据强度 **未形成**。极低的覆盖率表明双方在内存管理子系统上采用了完全不同的实现路径。
- **中断/异常处理**: 双方均存在。共同 AST 指纹 2 个，目标覆盖率 6.06%，参考覆盖率 2.44%。证据强度 **弱**。重合点 `set_trap_handler` 和 `enable_interrupt` 是硬件架构强相关的标准操作，其内部调用流差异显著。
- **系统调用**: 双方均存在。共同 AST 指纹 4 个，目标覆盖率 11.43%，参考覆盖率 5.33%。证据强度 **弱**。重合点 `syscall` 分发函数是内核的必经路径，其内部调用图谱（分发的系统调用数量与类型）完全不同。
- **文件系统**: 双方均存在。共同 AST 指纹 24 个，目标覆盖率 3.45%，参考覆盖率 6.52%。证据强度 **弱**。重合点多为 `ioctl`、`rename` 等通用 VFS 操作，但底层文件系统实现（如 ext4 操作方式）和驱动交互模型差异巨大。
- **设备驱动**: 双方均存在。共同 AST 指纹 31 个，目标覆盖率 17.71%，参考覆盖率 23.48%。证据强度 **中**。此模块覆盖率最高，但证据集中在 `dw_mshc`、`uart8250` 等特定硬件驱动上，这通常源于对硬件手册和厂商 SDK 的共同依赖，属于硬件驱动开发的常见现象。
- **网络协议栈**: 双方均存在。共同 AST 指纹 0 个，覆盖率 0.0%。证据强度 **未形成**。双方虽都有网络模块，但 AST 层面无任何重合，是完全独立的实现。
- **第三方库/公共依赖**: 双方均存在。共同 AST 指纹 2 个，目标覆盖率 2.47%，参考覆盖率 4.65%。证据强度 **弱**。重合点是对 `ext4` 库的封装调用，属于对同一开源库的正常使用。

#### 3. 底层 AST 关键证据指纹表
| 所属板块 | 目标项目路径 | 参考项目路径 | 证据类型 | 技术说明 |
| :--- | :--- | :--- | :--- | :--- |
| 设备驱动 | `lib/driver/src/block/dw_mshc/mmc.rs#send_cmd` | `os/src/drivers/vf2/dw/vf2_sdio.rs#send_cmd` | 同板块同名同调用流 | 对 DW MMC 控制器的寄存器操作序列（CMD, CMDARG, RESP, RINSTS）完全一致，源于硬件手册的强约束。 |
| 设备驱动 | `lib/driver/src/serial/uart8250.rs#init_u32` | `os/src/drivers/tty/serial/ns16550a.rs#init_u32` | 同板块同名同调用流 | 对 UART 8250/16550 兼容控制器的初始化序列（FCR, IER, LCR, MCR 等寄存器）完全一致，源于硬件标准。 |
| 设备驱动 | `lib/driver/src/serial/uart8250.rs#init_u8` | `os/src/drivers/tty/serial/ns16550a.rs#init_u8` | 同板块同名同调用流 | 同上，为 UART 初始化的另一部分，同样受硬件标准约束。 |
| 设备驱动 | `kernel/src/osdriver/probe.rs#for_pci_ranges` | `os/src/drivers/virtio_driver/pci.rs#for_pci_ranges` | 同板块同名同调用流 | PCI 配置空间解析逻辑一致，包括 `PciRangeType::from` 和 `assert_eq!`，源于 PCI 总线标准。 |
| 进程管理 | `lib/mutex/src/mutex/spin_mutex.rs#wait_unlock` | `os/src/sync/mutex/spin.rs#wait_unlock` | 同板块同名同调用流 | 自旋锁的 `spin_loop` + `load` 等待模式，是 Rust 中实现自旋锁的最小通用范式。 |
| 进程管理 | `kernel/src/syscall/process.rs#sys_getpid` | `os/src/syscall/process.rs#sys_getpid` | 同板块同名 | Linux ABI 强制的系统调用名。内部实现一个用字段访问，一个用 getter 方法，差异明显。 |
| 进程管理 | `kernel/src/task/task.rs#add_child` | `os/src/task/task.rs#add_child` | 同板块同名 | 通用的“加锁-插入”集合操作模式。键值来源（`child.tid` vs `child.get_pid`）不同。 |
| 任务调度 | `lib/executor/src/libt1.rs#spawn` | `os/src/task/executor.rs#spawn` | 同板块同名 | 共享 `async_task::spawn` 和 `WithInfo`，源于对 `async-task` 第三方库的标准用法。 |
| 文件系统 | `lib/vfs/src/dentry.rs#rename` | `os/src/fs/tmp/file.rs#rename` | 同板块同名 | VFS 层通用的重命名操作，但底层调用（`load_dir` vs `file_rename`）和实现完全不同。 |
| 第三方库 | `lib/ext4/src/ext/file.rs#rename` | `os/src/fs/ext4/inode.rs#rename` | 同板块同名 | 对 Rust ext4 库的封装调用，属于对同一开源库的正常使用。 |

#### 4. 核心代码调用流对比剖析
* **【进程管理/系统调用】**
* **底层技术析理**: 以 `sys_getpid` 为例，两者虽为同名系统调用，但底层抽象与错误处理模型截然不同。目标项目采用直接字段访问 `current_task().pid`，无错误处理，暗示其 `current_task()` 是绝对可靠的。参考项目则采用 `current_task().unwrap().get_tgid()`，包含 `Option` 解包和 getter 方法调用，表明其任务获取可能失败，且内部实现被封装。这反映了双方在“当前任务”这一核心概念上的不同安全假设和封装哲学。
* **代码溯源链接**:
  - 目标项目: `kernel/src/syscall/process.rs#sys_getpid`
  - 参考项目: `os/src/syscall/process.rs#sys_getpid`

* **【任务调度/异步运行时】**
* **底层技术析理**: 在任务 `spawn` 操作上，目标项目调用 `push_in_available_line`，暗示其调度器可能只有一个或自动选择的任务队列。参考项目则显式调用 `TASK_QUEUE.push_normal` 和 `TASK_QUEUE.push_prior`，明确区分了普通和优先级两个队列。这揭示了双方调度器在队列架构设计上的根本差异，并非简单的函数重命名。
* **代码溯源链接**:
  - 目标项目: `lib/executor/src/libt1.rs#spawn`
  - 参考项目: `os/src/task/executor.rs#spawn`

* **【内存管理/系统调用分发】**
* **底层技术析理**: 在系统调用总入口 `syscall` 上，目标项目使用 `SyscallNo::from_repr` 解析调用号，并通过庞大的 `match` 分支分发，包含大量 `log::debug!` 等调试日志。参考项目则使用 `SysCode::from` 解析，并直接调用 `sys_accept`、`sys_brk` 等具体函数。两者在分发机制、日志策略和所支持的系统调用集合上均表现出独立的设计。
* **代码溯源链接**:
  - 目标项目: `kernel/src/syscall/mod.rs#syscall`
  - 参考项目: `os/src/syscall/mod.rs#syscall`

#### 5. 单方模块与降权项
- **单方模块**: 无。所有宏观模块双方均存在。
- **降权项**:
  - **第三方库/公共接口**: `async_task::spawn`、`WithInfo`、`core::hint::spin_loop`、`CloneFlags::from_bits` 等调用，属于 Rust 生态和操作系统开发的通用范式，不构成同源证据。
  - **内部闭环逻辑**: `child_user_system_time`、`get_meta`、`metadata` 等函数因无外部调用图谱，无法进行有效的 AST 比对，其证据强度降为零。
  - **跨板块硬凑**: 控方将目标项目 `lib/osfs/src/proc/` 下的 `get_meta` 与参考项目 `os/src/fs/procfs/` 下完全不同节点的 `metadata` 函数强行配对，属于典型的跨板块硬凑，不构成有效证据。

#### 6. 保守结论
综合评估，双方项目整体 AST 相似度极低（3.15%），在进程管理、任务调度、内存管理等核心子系统上均表现出显著的架构差异。控方所指控的“系统性 AST 照搬”缺乏客观数据支持，其列举的证据多为 Linux ABI 强制、Rust 标准范式或硬件手册驱动的必然共性。唯一覆盖率较高的设备驱动模块，其重合点也完全可归因于对相同硬件标准的遵循。因此，本次审计未发现足以判定代码抄袭或违规复用的同源性证据，项目 `[2026-T2026104869910452-XX]` 应被视为独立开发作品。

------------------------------

