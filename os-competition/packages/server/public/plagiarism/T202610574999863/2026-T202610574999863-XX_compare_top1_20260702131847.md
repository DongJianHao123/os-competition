# 🛡️ OS 内核赛自动化代码级架构审计报告

**审计时间**: `2026-07-02 13:18:47`
**参赛作品名称**: `2026-T202610574999863-XX`
**年份**: `2026`
**学校**: `XX`
**队伍名称**: `T202610574999863`
**比赛名字**: `OS-Competition`
**比赛子赛道**: `Kernel-Implementation`
**仓库地址**: `https://gitlab.eduxiji.net/educg-group-43501-3132633/T202610574999863-772`
**推测开发语言**: `Rust / C (由AST探针自动识别)`
**特征函数量**: `159`

## ⚔️ 底层代码同源性对比 (AST Homology)
> 本部分由专门的代码审查智能体，对该项目与符合年份条件的历届最高相似度项目进行控制流与底层调用对比。新版报告会先给出确定性的全模块覆盖矩阵，再给出 Agent 的分模块解释，避免只围绕少量抽样函数展开。

### 🏆 Top 1 底层对标: vs `2021-TeamNameNotFound-重庆邮电大学`（重庆邮电大学 / TeamNameNotFound）

#### 对方作品信息

**对方年份**: `2021`
**对方学校**: `重庆邮电大学`
**对方队伍名称**: `TeamNameNotFound`
**对方比赛名字**: `OS-Competition`
**对方比赛子赛道**: `Kernel-Implementation`
**对方仓库地址**: `https://gitlab.eduxiji.net/qq976243638/xos`

> 双向覆盖率：共同 AST 指纹: 3；唯一 AST 指纹: 目标项目 135，参考项目 78；覆盖率: 目标项目 2.22%，参考项目 3.85%

## 全模块对比总览（确定性证据）

> 总体覆盖率：共同 AST 指纹: 3；唯一 AST 指纹: 目标项目 135，参考项目 78；覆盖率: 目标项目 2.22%，参考项目 3.85%

### 1. 模块覆盖矩阵
| 模块 | 目标函数数 | 参考函数数 | 共同唯一 AST 指纹 | Target 覆盖 | Base 覆盖 | 状态 |
| :--- | ---: | ---: | ---: | ---: | ---: | :--- |
| 进程管理 | 28 | 14 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |
| 任务调度/异步运行时 | 38 | 4 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |
| 内存管理 | 52 | 34 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |
| 中断/异常处理 | 2 | 2 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |
| 系统调用 | 6 | 0 | 0 | 0.0% | 0.0% | 仅目标项目体现 |
| 文件系统 | 32 | 9 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |
| 设备驱动 | 0 | 4 | 0 | 0.0% | 0.0% | 仅参考项目体现 |
| 网络协议栈 | 0 | 0 | 0 | 0.0% | 0.0% | 双方均未体现 |
| 第三方库 | 0 | 18 | 0 | 0.0% | 0.0% | 仅参考项目体现 |
| 通用模块/其他 | 1 | 0 | 0 | 0.0% | 0.0% | 仅目标项目体现 |

### 2. 分模块证据清单

#### 进程管理
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `kernel/src/proc.c#proc_wait` | `kernel/proc/proc.c#proc_wait` | 同板块同名 | schedule | free_proc, kfree, list_rm, proc_switch |
| `kernel/src/proc.c#proc_init` | `kernel/proc/proc.c#proc_init` | 同板块同名 | uart_puts | init_fd_table, sche_ptr->init |
| `kernel/src/proc.c#proc_dump` | `kernel/proc/sche_fifo.c#dequeue` | 同模块上下文对齐/差异观察 | uart_put_dec, uart_puts | isempty |
| `kernel/src/proc.c#proc_waitpid` | `kernel/proc/sche_fifo.c#queue_init` | 同模块上下文对齐/差异观察 | find_by_pid, schedule | 内部闭环逻辑 |

#### 任务调度/异步运行时
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `kernel/src/trap.c#trap_handler` | `kernel/trap/syscall.c#syscall` | 同模块上下文对齐/差异观察 | csr_read, loongarch_exc_name, riscv_exc_name, sched_tick, syscall_handler, uart_put_hex ...(+1) | fs_close, fs_openat, fs_read, proc_clone, proc_execve, proc_exit ...(+2) |
| `kernel/src/syscall.c#syscall_handler` | `kernel/trap/trap.c#trap_ret_user` | 同模块上下文对齐/差异观察 | sys_brk, sys_chdir, sys_clone, sys_close, sys_dup, sys_dup2 ...(+31) | csrr, csrw, trap_end |
| `kernel/src/sched.c#schedule` | `kernel/fs/fat32.c#fat32_read` | 同模块上下文对齐/差异观察 | proc_current, proc_set_current, proc_table_ptr, switch_to | kfree, load_file, memcpy |
| `kernel/src/syscall.c#sys_open` | `kernel/fs/fs.c#fs_close` | 同模块上下文对齐/差异观察 | proc_current, proc_get_cwd, vfs_close, vfs_get_path, vfs_open | 内部闭环逻辑 |

#### 内存管理
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `kernel/src/pmm.c#pmm_init` | `kernel/mem/pmm.c#pmm_init` | 同板块同名 | PAGE_ALIGN_DOWN, PAGE_ALIGN_UP, addr_to_index, bitmap_clear, uart_put_hex, uart_puts | ROUNDUP, free_a_page |
| `kernel/arch/loongarch64/vmm.c#vmm_arch_enable` | `kernel/fs/elf.c#load_elf` | 同模块上下文对齐/差异观察 | csr_read, csr_write, tlb_flush_all, uart_put_hex, uart_puts | (uint64), ROUNDDOWN, flags_trans, kalloc, list_add_back, magic_test ...(+4) |
| `kernel/src/syscall.c#sys_openat` | `kernel/batch/app.c#app_init` | 同模块上下文对齐/差异观察 | proc_current, proc_get_cwd, vfs_add_dentry, vfs_alloc_file, vfs_alloc_inode, vfs_close ...(+3) | alloc_proc, fs_close, fs_openat, fs_read, init_fd_table, kalloc ...(+6) |
| `kernel/src/elf_loader.c#elf_load` | `kernel/proc/proc.c#proc_clone` | 同模块上下文对齐/差异观察 | elf_check_magic, elf_ensure_user_page_mapped, elf_load_flat, elf_set_user_perms | ROUNDDOWN, alloc_proc, copy_segments, copy_trapframe, init_fd_table, kalloc ...(+3) |

#### 中断/异常处理
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `kernel/arch/loongarch64/trap.c#trap_init` | `kernel/trap/trap.c#trap_init` | 同板块同名 | csr_write, uart_put_hex, uart_puts | csrr, csrw |

#### 系统调用
仅目标项目中观察到该模块代表函数，参考项目没有同模块可对齐证据：
- 目标项目：`kernel/src/syscall.c#sys_uname`，调用摘要：内部闭环逻辑
- 目标项目：`kernel/src/syscall.c#sys_gettimeofday`，调用摘要：timer_get_ticks
- 目标项目：`kernel/src/syscall.c#syscall_handler6`，调用摘要：sys_mmap6, syscall_handler
- 目标项目：`kernel/src/syscall.c#ksyscall6`，调用摘要：syscall_handler6

#### 文件系统
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `kernel/src/fs.c#vfs_getdents` | `kernel/fs/fat32.c#fname2short` | 同模块上下文对齐/差异观察 | getdents_append | strcpy, strlen, toupper |
| `kernel/src/fs.c#vfs_get_path` | `kernel/fs/elf.c#magic_test` | 同模块上下文对齐/差异观察 | my_strlen | panic |
| `kernel/src/fs.c#vfs_lookup` | `kernel/fs/fat32.c#dentry2file_info` | 同模块上下文对齐/差异观察 | my_strcmp | 内部闭环逻辑 |
| `kernel/src/fs.c#vfs_list` | `kernel/fs/fs.c#dev_init` | 同模块上下文对齐/差异观察 | uart_put_dec, uart_puts, vfs_find_inode, vfs_lookup | strcpy |

#### 设备驱动
仅参考项目中观察到该模块代表函数，目标项目没有同模块可对齐证据：
- 参考项目：`kernel/driver/virt_disk.c#disk_read_sector`，调用摘要：memcpy
- 参考项目：`kernel/driver/clock.c#clock_init`，调用摘要：csrr, csrw, set_next_timer
- 参考项目：`kernel/driver/clock.c#set_next_timer`，调用摘要：csrr, set_timer
- 参考项目：`kernel/driver/virt_disk.c#disk_write_sector`，调用摘要：内部闭环逻辑

#### 第三方库
仅参考项目中观察到该模块代表函数，目标项目没有同模块可对齐证据：
- 参考项目：`kernel/libs/sbi.c#sbi_ecall`，调用摘要：内部闭环逻辑
- 参考项目：`kernel/libs/list.c#list_add_back`，调用摘要：内部闭环逻辑
- 参考项目：`kernel/libs/list.c#list_rm_front`，调用摘要：panic
- 参考项目：`kernel/libs/list.c#list_rm`，调用摘要：list_rm_front

#### 通用模块/其他
仅目标项目中观察到该模块代表函数，参考项目没有同模块可对齐证据：
- 目标项目：`kernel/src/elf_loader.c#elf_set_user_perms`，调用摘要：vmm_arch_set_user_bit


好的，审计官。基于提供的客观AST数据、模块画像、证据底座及控辩双方论点，现出具最终同源性定性仲裁报告。

---

### 1. 宏观判定
- **定性结论**: **独立开发 / 底层架构显著差异**
- **核心依据**: 客观AST相似度仅为 **1.43%**，共同AST指纹仅 **3** 个，远低于可判定复用的阈值。目标项目与参考项目的双向覆盖率（2.22% / 3.85%）处于噪声水平。所有核心模块（进程管理、内存管理、任务调度等）的**共同唯一AST指纹均为0**，表明双方在代码结构层面不存在实质性重合。控方指控主要基于通用命名和主观调用链映射，缺乏客观AST证据支持。

### 2. 全模块覆盖矩阵解读
| 模块 | 双方存在 | 共同唯一AST指纹 | 覆盖率解读 | 证据强度 |
| :--- | :---: | :---: | :--- | :--- |
| **进程管理** | 是 | 0 | 双方均有实现但无结构重合。目标项目函数数（28）远超参考项目（14），表明其功能复杂度更高。 | **弱** |
| **任务调度/异步运行时** | 是 | 0 | 目标项目函数数（38）远超参考项目（4），表明其调度与异步机制实现规模完全不同。 | **弱** |
| **内存管理** | 是 | 0 | 双方均有大量实现（52 vs 34），但零共同指纹。目标项目使用位图（`bitmap_clear`）管理，参考项目使用`kalloc`，内存模型根本不同。 | **弱** |
| **中断/异常处理** | 是 | 0 | 双方函数数极少（2 vs 2），但无共同指纹。目标项目`trap_init`调用`csr_write`，参考项目调用`csrr/csrw`，架构适配差异明显。 | **未形成** |
| **系统调用** | 仅目标 | 0 | 目标项目独有6个函数，参考项目无此独立模块。此为架构性差异。 | **未形成** |
| **文件系统** | 是 | 0 | 目标项目函数数（32）远超参考项目（9），且实现VFS抽象层，参考项目为FAT32具体实现。 | **弱** |
| **设备驱动** | 仅参考 | 0 | 参考项目独有4个函数，目标项目无此独立模块。此为架构性差异。 | **未形成** |
| **网络协议栈** | 否 | 0 | 双方均未实现。 | **未形成** |
| **第三方库/公共依赖** | 仅参考 | 0 | 参考项目包含18个函数（如`sbi_ecall`、链表操作），目标项目无此类依赖。 | **未形成** |

### 3. 底层 AST 关键证据指纹表
> 所有模块的共同唯一AST指纹均为0，因此不存在可认定为“同源指纹”的强证据。下表列出控方指控的同板块同名/同功能函数，但其调用图谱完全不同，属于通用命名或领域常识，不构成同源证据。

| 所属板块 | 目标项目路径 | 参考项目路径 | 证据类型 | 技术说明 |
| :--- | :--- | :--- | :--- | :--- |
| 进程管理 | `kernel/src/proc.c#proc_wait` | `kernel/proc/proc.c#proc_wait` | 同名不同构 | 目标仅调用`schedule`让出CPU；参考调用`free_proc, kfree, list_rm, proc_switch`执行资源回收与链表操作。语义与调用流完全不同。 |
| 进程管理 | `kernel/src/proc.c#proc_init` | `kernel/proc/proc.c#proc_init` | 同名不同构 | 目标仅调用`uart_puts`输出；参考调用`init_fd_table, sche_ptr->init`初始化文件描述符表和调度器。初始化内容完全不同。 |
| 内存管理 | `kernel/src/pmm.c#pmm_init` | `kernel/mem/pmm.c#pmm_init` | 同名不同构 | 目标使用`PAGE_ALIGN_DOWN/UP, addr_to_index, bitmap_clear`实现位图管理；参考仅使用`ROUNDUP, free_a_page`。物理内存管理模型根本不同。 |
| 任务调度 | `kernel/src/sched.c#schedule` | `kernel/proc/proc.c#proc_switch` | 功能相似不同构 | 目标使用`proc_table_ptr, switch_to`操作全局进程表；参考使用`sche_ptr->next_proc, context_switch`操作FIFO调度器对象。调度架构不同。 |
| 内存管理 | `kernel/src/elf_loader.c#elf_load` | `kernel/fs/elf.c#load_elf` | 功能相似不同构 | 目标调用`elf_load_flat, elf_ensure_user_page_mapped`等自有函数；参考调用`kalloc, list_add_back, memcpy`。ELF加载的资源分配与段映射方式完全不同。 |

### 4. 核心代码调用流对比剖析
* **【进程管理/调度路径】**
* **底层技术析理**: 目标项目的`schedule`函数通过全局进程表`proc_table_ptr`和`switch_to`执行上下文切换，是一种直接的表驱动调度。参考项目的`proc_switch`则依赖一个抽象的调度器对象`sche_ptr`，通过调用其`next_proc`方法从FIFO队列中获取下一个进程，是典型的策略模式。两者在调度策略的抽象层次和实现机制上存在根本差异。
* **代码溯源链接**:
  - 目标项目: `kernel/src/sched.c#schedule`
  - 参考项目: `kernel/proc/proc.c#proc_switch`

* **【内存管理/物理页分配路径】**
* **底层技术析理**: 目标项目的`pmm_init`采用位图（bitmap）管理物理内存，通过`addr_to_index`和`bitmap_clear`操作位图结构。参考项目的`pmm_init`则仅是对页进行对齐后调用`free_a_page`，缺乏复杂的管理结构。这代表了两套完全不同的物理内存分配器设计，从数据结构到分配算法均无重合。
* **代码溯源链接**:
  - 目标项目: `kernel/src/pmm.c#pmm_init`
  - 参考项目: `kernel/mem/pmm.c#pmm_init`

* **【文件系统/文件打开路径】**
* **底层技术析理**: 目标项目的`sys_openat`构建在完整的VFS抽象层之上，调用`vfs_find_inode, vfs_get_path, vfs_alloc_file`等通用接口。参考项目的`fat32_openat`则是FAT32文件系统的具体实现，直接操作`dir_search, dentry2file_info`等FAT32特有结构。一个是通用框架，一个是具体实现，抽象层次完全不同。
* **代码溯源链接**:
  - 目标项目: `kernel/src/syscall.c#sys_openat`
  - 参考项目: `kernel/fs/fat32.c#fat32_openat`

### 5. 单方模块与降权项
* **单方模块**:
  - **目标项目独有**: `系统调用`模块（`sys_uname`, `sys_gettimeofday`等）、`通用模块/其他`（`elf_set_user_perms`）。这表明目标项目有独立的系统调用扩展和ELF处理逻辑。
  - **参考项目独有**: `设备驱动`模块（`virt_disk.c`, `clock.c`）、`第三方库`模块（`sbi.c`, `list.c`）。这表明参考项目依赖特定的RISC-V SBI接口和自定义链表库。
  - **结论**: 这些单方模块是两者架构独立演化的直接证据，不能作为同源证据。

* **降权项**:
  - **通用命名函数**: `proc_wait`, `proc_init`, `pmm_init`, `trap_init`等函数名是操作系统内核开发中的通用术语，其存在同名不能作为抄袭证据。
  - **跨板块指控**: 控方将参考项目内存管理板块的`proc_switch`、`trap`、`proc_execve`等函数，与目标项目进程管理/调度板块的函数进行对比，属于跨板块硬凑证据，已全部降权为无效指控。
  - **领域常识流程**: ELF加载的“校验-映射-设置权限”流程、页表遍历的多级指针操作，是操作系统领域的标准技术流程，不同项目遵循此流程是领域合理性的体现，不构成同源。

### 6. 保守结论
本次审计基于客观AST指纹分析，目标项目与参考项目的代码相似度极低（1.43%），所有核心模块的共同唯一AST指纹均为零。控方提出的“复用指控”主要建立在通用函数命名和主观调用链映射之上，缺乏底层AST结构同源的直接证据。辩方成功论证了双方在内存管理模型（位图 vs `kalloc`）、调度架构（全局表 vs FIFO队列）、文件系统抽象（VFS vs FAT32）以及架构支持（双架构 vs 单RISC-V）等核心层面存在根本性设计差异。因此，现有证据无法支持代码复用或抄袭的指控，应认定为两个独立开发的操作系统内核项目。整体同源性风险极低。

------------------------------

