# 🛡️ OS 内核赛自动化代码级架构审计报告

**审计时间**: `2026-07-02 13:18:47`
**参赛作品名称**: `2026-T202610574999863-XX`
**年份**: `2026`
**学校**: `XX`
**队伍名称**: `T202610574999863`
**比赛名字**: `OS-Competition`
**比赛子赛道**: `Kernel-Implementation`
**仓库地址**: `https://gitlab.eduxiji.net/educg-group-43501-3132633/T202610574999863-772`
**推测开发语言**: `Rust / C (由AST探针自动识别)`
**特征函数量**: `159`

## ⚔️ 底层代码同源性对比 (AST Homology)
> 本部分由专门的代码审查智能体，对该项目与符合年份条件的历届最高相似度项目进行控制流与底层调用对比。新版报告会先给出确定性的全模块覆盖矩阵，再给出 Agent 的分模块解释，避免只围绕少量抽样函数展开。

### 🏆 Top 5 底层对标: vs `2021-TOO_LOW-北京航空航天大学`（北京航空航天大学 / ^^^TOO_LOW^^^）

#### 对方作品信息

**对方年份**: `2021`
**对方学校**: `北京航空航天大学`
**对方队伍名称**: `^^^TOO_LOW^^^`
**对方比赛名字**: `OS-Competition`
**对方比赛子赛道**: `Kernel-Implementation`
**对方仓库地址**: `https://gitlab.eduxiji.net/buaawyk2/oskernel2021-too_low`

> 双向覆盖率：共同 AST 指纹: 4；唯一 AST 指纹: 目标项目 135，参考项目 295；覆盖率: 目标项目 2.96%，参考项目 1.36%

## 全模块对比总览（确定性证据）

> 总体覆盖率：共同 AST 指纹: 4；唯一 AST 指纹: 目标项目 135，参考项目 295；覆盖率: 目标项目 2.96%，参考项目 1.36%

### 1. 模块覆盖矩阵
| 模块 | 目标函数数 | 参考函数数 | 共同唯一 AST 指纹 | Target 覆盖 | Base 覆盖 | 状态 |
| :--- | ---: | ---: | ---: | ---: | ---: | :--- |
| 进程管理 | 28 | 5 | 1 | 4.76% | 20.0% | 双方存在且有结构重合 |
| 任务调度/异步运行时 | 38 | 30 | 2 | 5.88% | 6.9% | 双方存在且有结构重合 |
| 内存管理 | 52 | 80 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |
| 中断/异常处理 | 2 | 9 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |
| 系统调用 | 6 | 94 | 1 | 20.0% | 2.94% | 双方存在且有结构重合 |
| 文件系统 | 32 | 77 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |
| 设备驱动 | 0 | 49 | 0 | 0.0% | 0.0% | 仅参考项目体现 |
| 网络协议栈 | 0 | 0 | 0 | 0.0% | 0.0% | 双方均未体现 |
| 通用模块/其他 | 1 | 75 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |

### 2. 分模块证据清单

#### 进程管理
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `kernel/src/proc.c#proc_current` | `lib/syscall_all.c#sys_rt_sigprocmask` | 同板块同结构 | 内部闭环逻辑 | 内部闭环逻辑 |
| `kernel/src/syscall.c#sys_execve` | `user/fork.c#user_bcopy` | 同模块上下文对齐/差异观察 | elf_load, proc_create_user, vfs_find_inode, vfs_lookup | 内部闭环逻辑 |
| `kernel/src/proc.c#proc_wait` | `user/syscall_lib.c#syscall_rt_sigprocmask` | 同模块上下文对齐/差异观察 | schedule | internal_syscall |
| `kernel/src/proc.c#proc_dump` | `user/fork.c#user_bzero` | 同模块上下文对齐/差异观察 | uart_put_dec, uart_puts | 内部闭环逻辑 |

#### 任务调度/异步运行时
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `kernel/src/syscall.c#sys_yield` | `lib/syscall_all.c#sys_yield` | 同板块同名 | proc_yield | sched_yield |
| `kernel/src/trap.c#trap_handler` | `lib/print.c#lp_Print` | 同模块上下文对齐/差异观察 | csr_read, loongarch_exc_name, riscv_exc_name, sched_tick, syscall_handler, uart_put_hex ...(+1) | Ctod, IsDigit, OUTPUT, PrintChar, PrintNum, PrintString ...(+1) |
| `kernel/src/syscall.c#syscall_handler` | `user/print.c#user_lp_Print` | 同模块上下文对齐/差异观察 | sys_brk, sys_chdir, sys_clone, sys_close, sys_dup, sys_dup2 ...(+31) | Ctod, IsDigit, OUTPUT, user_PrintChar, user_PrintNum, user_PrintString ...(+1) |
| `kernel/src/sched.c#schedule` | `lib/fat32.c#emake` | 同模块上下文对齐/差异观察 | proc_current, proc_set_current, proc_table_ptr, switch_to | (u16), cal_checksum, generate_shortname, memset, panic, reloc_clus ...(+3) |

#### 内存管理
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `kernel/src/syscall.c#sys_mprotect` | `lib/syscall_all.c#sys_mprotect` | 同板块同名 | vmm_arch_protect_page | bzero, page2kva, page_alloc, page_lookup, printf |
| `kernel/arch/loongarch64/vmm.c#vmm_arch_enable` | `lib/traps.c#syscall_handler` | 同模块上下文对齐/差异观察 | csr_read, csr_write, tlb_flush_all, uart_put_hex, uart_puts | bcopy, get_sstatus, set_sstatus, sys_brk, sys_chdir, sys_clock_gettime ...(+62) |
| `kernel/src/syscall.c#sys_openat` | `bootloader/SBI/rustsbi-k210/src/main.rs#start_trap_rust` | 同模块上下文对齐/差异观察 | proc_current, proc_get_cwd, vfs_add_dentry, vfs_alloc_file, vfs_alloc_inode, vfs_close ...(+3) | devintr, get_vaddr_u32, k210_hal::clint::mtime::read, llvm_asm!, mcause::read, mcause::read().cause ...(+17) |
| `kernel/src/elf_loader.c#elf_load` | `bootloader/SBI/rustsbi-k210/src/main.rs#main` | 同模块上下文对齐/差异观察 | elf_check_magic, elf_ensure_user_page_mapped, elf_load_flat, elf_set_user_perms | 115_200.bps, ALLOCATOR.lock, ALLOCATOR.lock().init, Clocks::new, boot.clear_interrupt_pending_bits, boot.trigger_on_edge ...(+46) |

#### 中断/异常处理
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `kernel/arch/loongarch64/trap.c#trap_init` | `lib/traps.c#trap_init` | 同板块同名 | csr_write, uart_put_hex, uart_puts | get_sie, get_sstatus, printf, set_sie, set_sscratch, set_sstatus ...(+1) |
| `kernel/arch/riscv64/trap.c#trap_init` | `bootloader/SBI/rustsbi-k210/src/main.rs#set_rd` | 同模块上下文对齐/差异观察 | csr_write, uart_put_hex, uart_puts | panic! |

#### 系统调用
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `kernel/src/syscall.c#sys_uname` | `lib/syscall_all.c#sys_uname` | 同板块同名 | 内部闭环逻辑 | 内部闭环逻辑 |
| `kernel/src/syscall.c#sys_gettimeofday` | `lib/syscall_all.c#sys_gettimeofday` | 同板块同名 | timer_get_ticks | 内部闭环逻辑 |
| `kernel/src/syscall.c#ksyscall6` | `user/syscall_lib.c#syscall_linkat` | 同板块同结构 | syscall_handler6 | internal_syscall |
| `kernel/src/syscall.c#ksyscall` | `user/syscall_lib.c#syscall_mem_map` | 同板块同结构 | syscall_handler | internal_syscall |

#### 文件系统
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `kernel/src/syscall.c#sys_mkdir` | `lib/syscall_file.c#sys_mkdir` | 同板块同名 | vfs_mkdir | eput, sys_create |
| `kernel/src/syscall.c#sys_unlink` | `lib/syscall_file.c#sys_unlink` | 同板块同名 | vfs_unlink | ename, eput, eremove, isdirempty, strlen |
| `kernel/src/fs.c#vfs_getdents` | `lib/fat32.c#fat32_init` | 同模块上下文对齐/差异观察 | getdents_append | bread, brelse, memmove, memset, panic, printf ...(+1) |
| `kernel/src/fs.c#vfs_get_path` | `lib/syscall_file.c#sys_rename` | 同模块上下文对齐/差异观察 | my_strlen | dirlookup, edup, emake, ename, enameparent, eput ...(+4) |

#### 设备驱动
仅参考项目中观察到该模块代表函数，目标项目没有同模块可对齐证据：
- 参考项目：`drivers/gpiohs.c#gpiohs_set_pin_edge`，调用摘要：set_gpio_bit
- 参考项目：`drivers/sdcard.c#sd_write_sector`，调用摘要：sd_end_cmd, sd_get_dataresponse, sd_get_response, sd_send_cmd, sd_write_data
- 参考项目：`drivers/fpioa.c#fpioa_set_function_raw`，调用摘要：内部闭环逻辑
- 参考项目：`drivers/sdcard.c#check_block_size`，调用摘要：printf, sd_end_cmd, sd_get_response_R1, sd_get_response_R3_rest, sd_send_cmd

#### 通用模块/其他
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `kernel/src/elf_loader.c#elf_set_user_perms` | `lib/print.c#PrintNum` | 同模块上下文对齐/差异观察 | vmm_arch_set_user_bit | 内部闭环逻辑 |


好的，我将遵循审计指令，基于提供的证据底座、控辩双方陈述及原始AST抽样证据，进行最终同源性定性仲裁。

### 1. 宏观判定
- **定性结论**: **独立开发 (相似度: 0.94%，共同 AST 指纹: 4；唯一 AST 指纹: 目标项目 135，参考项目 295；覆盖率: 目标项目 2.96%，参考项目 1.36%)**
    - **裁决摘要**: 双方项目的客观AST相似度极低（0.94%），共同指纹仅4个，远未达到可判定为“代码复用”或“洗稿”的阈值。控方提出的“接口伪装式抄袭”等指控，在宏观数据上不成立，且其证据配对存在严重的跨板块硬凑和领域常识误判。辩方关于“标准接口命名”、“领域通用控制流”及“架构设计差异”的辩护逻辑成立。目标项目与参考项目在核心架构、硬件抽象、数据结构和实现范式上存在根本性差异，应认定为独立开发。

### 2. 全模块覆盖矩阵解读
| 模块 | 双方存在 | 共同AST指纹 | 覆盖率解读 | 证据强度 |
| :--- | :---: | :---: | :--- | :--- |
| **进程管理** | 是 | 1 | 目标覆盖率4.76%，参考20.0%。唯一的共同指纹源于`proc_current`这类极简的“内部闭环逻辑”，不构成实质性结构复用。双方函数规模差异巨大（28 vs 5），表明实现复杂度完全不同。 | **弱** |
| **任务调度/异步运行时** | 是 | 2 | 目标覆盖率5.88%，参考6.9%。共同指纹极少，且其中一个为`sys_yield`，其“单叶子节点委托”结构是系统调用入口的通用模式。控方将大量驱动、文件系统、打印函数强行归入此板块进行配对，证据无效。 | **弱** |
| **内存管理** | 是 | **0** | 双方覆盖率均为0.0%。**这是驳回控方“内存保护系统调用复用”指控的决定性证据。** 双方在此核心模块无任何共同AST指纹，证明其内部实现（页表操作、分配器、映射逻辑）完全不同。 | **未形成** |
| **中断/异常处理** | 是 | **0** | 双方覆盖率均为0.0%。尽管存在同名函数`trap_init`，但AST指纹无重合，说明其初始化逻辑和中断向量表设置等底层实现细节不同。 | **未形成** |
| **系统调用** | 是 | 1 | 目标覆盖率20.0%，参考2.94%。共同指纹源于`sys_uname`等内部闭环逻辑，以及`ksyscall`/`syscall_*`这类系统调用分发入口的通用结构。这是任何支持系统调用的内核都具备的，不构成特定代码复用。 | **弱** |
| **文件系统** | 是 | **0** | 双方覆盖率均为0.0%。目标项目采用集中式VFS抽象（`vfs_*`），参考项目采用分散式设计（`file.c`、`fat32.c`），AST结构无重合，证实了独立实现。 | **未形成** |
| **设备驱动** | 否 | 0 | 仅参考项目存在（49个函数），目标项目无此模块。这直接表明双方面向的硬件平台和驱动架构完全不同，是独立开发的有力佐证。 | **未形成** |
| **网络协议栈** | 否 | 0 | 双方均未体现。 | **未形成** |
| **通用模块/其他** | 是 | **0** | 双方覆盖率均为0.0%。参考项目有75个函数，目标项目仅1个，规模差异巨大，且无共同指纹。 | **未形成** |

### 3. 底层 AST 关键证据指纹表
| 所属板块 | 目标项目路径 | 参考项目路径 | 证据类型 | 技术说明 |
| :--- | :--- | :--- | :--- | :--- |
| 任务调度/异步运行时 | `[kernel/src/syscall.c#sys_yield]` | `[lib/syscall_all.c#sys_yield]` | 同板块同名 | 双方均实现POSIX标准的`sys_yield`系统调用，AST结构均为“单叶子节点委托”。但目标委托给`proc_yield`（进程层），参考委托给`sched_yield`（调度层），内部实现路径不同。 |
| 系统调用 | `[kernel/src/syscall.c#sys_uname]` | `[lib/syscall_all.c#sys_uname]` | 同板块同名 | 双方均实现`uname`系统调用，且AST均为“内部闭环逻辑”。这是填充固定字符串的标准操作，不构成代码复用。 |
| 系统调用 | `[kernel/src/syscall.c#ksyscall6]` | `[user/syscall_lib.c#syscall_linkat]` | 同板块同结构 | 系统调用分发入口函数，AST结构相似（参数打包与转发）。这是所有系统调用机制的通用模式。 |
| 进程管理 | `[kernel/src/proc.c#proc_current]` | `[lib/syscall_all.c#sys_rt_sigprocmask]` | 同板块同结构 | 双方函数均表现为“内部闭环逻辑”，但功能完全不同（获取当前进程指针 vs 信号掩码操作）。此指纹匹配为低信息量的偶然重合。 |

### 4. 核心代码调用流对比剖析
* **【任务调度/异步运行时 / 调度核心】**
* **底层技术析理**: 目标项目的`schedule`函数与参考项目的`sched_yield`函数是控方的主要指控点。然而，两者的调用流揭示了截然不同的对象管理模型。目标项目采用**进程表（Process Table）模型**，通过`proc_table_ptr`操作进程数组，并使用`switch_to`进行显式上下文切换。参考项目则采用**链表（Linked List）模型**，直接使用`LIST_*`宏操作`env`结构体链表，并通过`env_run`切换环境。这是两种互斥的调度器实现范式，其数据结构和遍历算法在AST层面不可能一致。
* **代码溯源链接**:
  - 目标项目: `kernel/src/sched.c#schedule`
  - 参考项目: `lib/sched.c#sched_yield`

* **【内存管理 / 内存保护系统调用】**
* **底层技术析理**: 控方指控目标项目的`sys_mprotect`将参考项目的实现“黑盒化”为`vmm_arch_protect_page`。此指控被模块覆盖矩阵证伪（内存管理模块共同AST指纹为0）。目标项目调用`vmm_arch_protect_page`，这是一个架构相关的抽象接口，用于处理多ISA（riscv64/loongarch64）下的页表项修改。参考项目则直接调用`page2kva`、`page_alloc`、`bzero`等底层函数，是单一架构下的内联实现。两者的**抽象层次**和**实现策略**截然相反，不存在子树搬迁的可能。
* **代码溯源链接**:
  - 目标项目: `kernel/src/syscall.c#sys_mprotect`
  - 参考项目: `lib/syscall_all.c#sys_mprotect`

* **【中断/异常处理 / 陷阱初始化】**
* **底层技术析理**: 双方均有`trap_init`函数，但调用图谱无共同指纹。目标项目的实现（以loongarch64为例）直接通过`csr_write`设置架构特定的异常入口地址，并调用`uart_puts`输出调试信息。参考项目的实现则涉及`get_sie`、`set_sie`、`set_sscratch`等多个控制状态寄存器操作，并调用`printf`。两者操作的寄存器集合和初始化流程不同，反映了不同ISA（LoongArch vs. RISC-V）和不同内核设计下的必然差异。
* **代码溯源链接**:
  - 目标项目: `kernel/arch/loongarch64/trap.c#trap_init`
  - 参考项目: `lib/traps.c#trap_init`

### 5. 单方模块与降权项
- **单方模块**:
    - **设备驱动**: 仅参考项目拥有此模块（`drivers/`），包含`gpiohs`、`sdcard`、`spi`、`virtio_blk`等K210平台特定驱动。目标项目无任何设备驱动代码，表明其运行环境（QEMU通用设备）和硬件抽象层与参考项目完全不同，是独立开发的有力证据。
- **降权项（弱信号/不构成同源证据）**:
    - **跨板块同名函数**: 如`sys_execve`（进程管理）与`user_bcopy`（进程管理）的配对，`trap_handler`（任务调度）与`lp_Print`（任务调度）的配对，均属于功能完全不相关的函数被算法强行关联，不构成同源证据。
    - **标准POSIX接口同名**: `sys_yield`、`sys_mprotect`、`sys_mkdir`、`sys_unlink`、`sys_gettimeofday`等函数名是操作系统领域的标准命名，其同名是接口规范使然，与代码来源无关。
    - **第三方库/公共依赖**: 双方均使用了标准C库函数（如`memset`、`memcpy`、`strlen`、`va_arg`）和编译器内置函数（如`__sync_synchronize`），这些属于公共依赖，不计入项目原创性评估。

### 6. 保守结论
本次审计的目标项目与参考项目之间，客观AST相似度仅为0.94%，这是一个极低的数值，在软件同源性鉴定中通常被视为“无实质性相似”。4个共同AST指纹均指向系统调用分发、标准POSIX接口或内部闭环逻辑等通用模式，而非特定的、非标准的代码实现。控方提出的“调度器复用”、“内存保护洗稿”等指控，在宏观数据（内存管理模块零重合）和微观调用流（进程表模型 vs. 链表模型）的双重检验下均不成立。双方在硬件平台、驱动架构、文件系统抽象和内存管理策略上存在根本性分歧，符合独立开发的全部特征。综合裁定，目标项目不存在对参考项目的代码复用或抄袭行为，整体风险极低。

------------------------------

