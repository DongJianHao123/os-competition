# 🛡️ OS 内核赛自动化代码级架构审计报告

**审计时间**: `2026-07-02 12:44:20`
**参赛作品名称**: `2026-T2026104889910576-XX`
**年份**: `2026`
**学校**: `XX`
**队伍名称**: `T2026104889910576`
**比赛名字**: `OS-Competition`
**比赛子赛道**: `Kernel-Implementation`
**仓库地址**: `https://gitlab.eduxiji.net/educg-group-43501-3132633/T2026104889910576-1616`
**推测开发语言**: `Rust / C (由AST探针自动识别)`
**特征函数量**: `654`

## ⚔️ 底层代码同源性对比 (AST Homology)
> 本部分由专门的代码审查智能体，对该项目与符合年份条件的历届最高相似度项目进行控制流与底层调用对比。新版报告会先给出确定性的全模块覆盖矩阵，再给出 Agent 的分模块解释，避免只围绕少量抽样函数展开。

### 🏆 Top 3 底层对标: vs `2024-蕾姆赛高队-扬州大学`（扬州大学 / 蕾姆赛高队）

#### 对方作品信息

**对方年份**: `2024`
**对方学校**: `扬州大学`
**对方队伍名称**: `蕾姆赛高队`
**对方比赛名字**: `OS-Competition`
**对方比赛子赛道**: `Kernel-Implementation`
**对方仓库地址**: `https://gitlab.eduxiji.net/educg-group-26010-2376550/T202411117993110-198`

> 双向覆盖率：共同 AST 指纹: 15；唯一 AST 指纹: 目标项目 561，参考项目 754；覆盖率: 目标项目 2.67%，参考项目 1.99%

## 全模块对比总览（确定性证据）

> 总体覆盖率：共同 AST 指纹: 15；唯一 AST 指纹: 目标项目 561，参考项目 754；覆盖率: 目标项目 2.67%，参考项目 1.99%

### 1. 模块覆盖矩阵
| 模块 | 目标函数数 | 参考函数数 | 共同唯一 AST 指纹 | Target 覆盖 | Base 覆盖 | 状态 |
| :--- | ---: | ---: | ---: | ---: | ---: | :--- |
| 进程管理 | 37 | 45 | 2 | 6.45% | 4.65% | 双方存在且有结构重合 |
| 任务调度/异步运行时 | 50 | 43 | 1 | 2.33% | 2.44% | 双方存在且有结构重合 |
| 内存管理 | 223 | 125 | 2 | 0.93% | 1.68% | 双方存在且有结构重合 |
| 中断/异常处理 | 17 | 17 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |
| 系统调用 | 60 | 6 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |
| 文件系统 | 99 | 68 | 2 | 2.27% | 3.57% | 双方存在且有结构重合 |
| 设备驱动 | 29 | 43 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |
| 网络协议栈 | 0 | 0 | 0 | 0.0% | 0.0% | 双方均未体现 |
| 文件系统/第三方库 | 36 | 503 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |
| 通用模块/其他 | 103 | 51 | 1 | 1.08% | 2.44% | 双方存在且有结构重合 |

### 2. 分模块证据清单

#### 进程管理
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `src/syscall.c#sys_exit` | `src/kernel/sysproc.c#sys_exit` | 同板块同名 | curproc, panic, sched_yield, wait_queue_wakeup_one | argint, exit |
| `src/syscall.c#sys_getppid` | `src/kernel/sysproc.c#sys_getppid` | 同板块同名 | curproc | myproc |
| `src/syscall/syscall.c#sys_getpid` | `src/kernel/sysproc.c#sys_getpid` | 同板块同名同结构 | curproc | myproc |
| `src/trap/trap.c#forkret` | `src/proc/proc.c#forkret` | 同板块同名 | thiscpu, usertrapret | dir_open, ext4_init, myproc, r_time, release, usertrapret |

#### 任务调度/异步运行时
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `src/trap/trap.c#usertrap` | `src/kernel/trap.c#usertrap` | 同板块同名 | errorf, handle_external, handle_timer, intr_off, intr_on, r_scause ...(+7) | devintr, exit, intr_on, myproc, panic, printf ...(+10) |
| `src/syscall/syscall.c#syscall` | `src/kernel/syscall.c#syscall` | 同板块同名 | curproc, fn, infof, syscall_lookup, warnf | NELEM, myproc, printf, syscalls[num] |
| `src/syscall.c#sys_close` | `src/kernel/sysfile.c#sys_close` | 同板块同名 | curproc, fd_free | argfd, fileclose, myproc |
| `src/syscall/syscall.c#sys_sched_yield` | `src/kernel/sysothers.c#sys_sched_yield` | 同板块同名同结构 | sched_yield | yield |

#### 内存管理
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `src/syscall/syscall.c#sys_openat` | `src/kernel/sysfile.c#sys_openat` | 同板块同名 | IS_ERR, PTR_ERR, copyinstr, curproc, file_put, install_file_for_proc ...(+9) | EXT4open, argfd, argint, argstr, dir_open, fdalloc2 ...(+8) |
| `src/mm/vm.c#kvminit` | `src/mm/vm.c#kvminit` | 同板块同名 | KVM, MAKE_SATP, devminit, kmappages, pagetable_create, panic ...(+2) | kalloc, kvmmap, memset, printf |
| `src/mm/pagetable.c#mappages` | `src/mm/vm.c#mappages` | 同板块同名 | PA2PTE, PAGE_ALIGNED, va2pte, warnf | PA2PTE, PGROUNDDOWN, panic, walk |
| `src/syscall/syscall.c#sys_pipe2` | `src/kernel/sysfile.c#sys_pipe2` | 同板块同名 | copyout, curproc, fd_alloc, fd_free, fd_install, file_alloc ...(+3) | argaddr, argint, copyout2, fdalloc, fileclose, myproc ...(+1) |

#### 中断/异常处理
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `src/hal/virtio/virtio_blk.c#virtio_blk_init_all` | `src/kernel/trap.c#trapframedump` | 同模块上下文对齐/差异观察 | mmio_read32, virtio_blk_init_one | printf |
| `src/hal/virtio/virtio_net.c#virtio_net_init` | `src/kernel/plic.c#plicinithart` | 同模块上下文对齐/差异观察 | mmio_read32, virtio_net_init_one, warnf | PLIC_MENABLE, PLIC_SENABLE, PLIC_SPRIORITY, cpuid, printf, readd |
| `src/hal/virtio/virtio_mmio.c#virtio_mmio_probe` | `src/mm/dmac.c#dmac_enable_common_interrupt_signal` | 同模块上下文对齐/差异观察 | debugf, mmio_read32, warnf | readq, writeq |
| `src/sync/spinlock.c#spinlock_acquire_bare` | `src/mm/dmac.c#dmac_enable_common_interrupt_status` | 同模块上下文对齐/差异观察 | __atomic_exchange_n, assert, intr_off, panic, r_tp | readq, writeq |

#### 系统调用
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `src/syscall/syscall.c#fill_stat_dev_special` | `src/kernel/sysothers.c#sys_clock_gettime` | 同模块上下文对齐/差异观察 | current_time_sec, memset, strcmp | argaddr, argint, copyout2, r_time |
| `src/syscall/syscall.c#parse_dev_path` | `src/kernel/sysothers.c#sys_gettimeofday` | 同模块上下文对齐/差异观察 | MKDEV, strncmp | argaddr, copyout2, r_time |
| `src/user/init.c#syscall4` | `src/kernel/syscall.c#argstr` | 同模块上下文对齐/差异观察 | 内部闭环逻辑 | argaddr, fetchstr |
| `src/syscall.c#syscall_register` | `src/kernel/sysmem.c#sys_munmap` | 同模块上下文对齐/差异观察 | panic, sc_hash | argaddr, do_munmap |

#### 文件系统
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `src/fs/vfs.c#vfs_write` | `src/lib/utils.c#get_gpio_bit` | 同板块同结构 | file_write | get_bit |
| `src/fs/vfs.c#vfs_lseek` | `src/fs/fat32/fat32.c#enameparent` | 同板块同结构 | file_lseek | lookup_path |
| `src/fs/vfs.c#vfs_sync` | `src/fs/file.c#filekstat` | 同板块同结构 | 内部闭环逻辑 | 内部闭环逻辑 |
| `src/misc/lz4.c#lz4_compress_generic` | `src/fs/fat32/fat32.c#fat32_init` | 同模块上下文对齐/差异观察 | (u32), (u8), LZ4_HASH_FUNCTION, lz4_read32, lz4_write_literal, memcpy ...(+1) | bread, brelse, initlock, initsleeplock, memmove, memset ...(+3) |

#### 设备驱动
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `src/hal/virtio/virtio_mmio.c#virtio_negotiate_features` | `src/driver/spi.c#spi_receive_data_normal_dma` | 同模块上下文对齐/差异观察 | errorf, mb, virtio_get_status, virtio_read_reg, virtio_set_status, virtio_write_reg | (uint32), dmac_set_single_mode, dmac_wait_done, spi_get_frame_format, spi_set_tmod, sysctl_dma_select |
| `src/hal/virtio/virtio_mmio.c#virtio_mmio_init` | `src/driver/sdcard.c#sdcard_write_sector` | 同模块上下文对齐/差异观察 | errorf, mb, virtio_get_status, virtio_read_reg, virtio_set_status | acquiresleep, panic, printf, releasesleep, sd_end_cmd, sd_get_response_R1 ...(+4) |
| `src/hal/blk.c#blk_register` | `src/driver/fpioa.c#fpioa_set_function_raw` | 同模块上下文对齐/差异观察 | MAJOR, MINOR, errorf, spinlock_acquire, spinlock_release, warnf | 内部闭环逻辑 |
| `src/hal/virtio/virtq.c#virtq_get_buf` | `src/driver/sdcard.c#check_block_size` | 同模块上下文对齐/差异观察 | rmb, spinlock_acquire, spinlock_release, virtq_free_desc | printf, sd_end_cmd, sd_get_response_R1, sd_get_response_R3_rest, sd_send_cmd |

#### 文件系统/第三方库
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `src/fs/ext4/ext4.c#ext4_unlink` | `src/fs/ext4/ext4.c#ext4_unlink` | 同板块同名 | (u32), ext4_get_file_block, ext4_get_sbi, ext4_read_data, ext4_write_at, inode_dirty ...(+3) | ext4_dir_remove_entry, ext4_fs_inode_links_count_dec, ext4_has_children, ext4_inode_get_links_cnt, ext4_inode_is_type |
| `src/fs/ext4/ext4.c#ext4_mount` | `src/fs/ext4/ext4.c#ext4_mount` | 同板块同名 | IS_ERR, PTR, PTR_ERR, dentry_root, ext4_fill_super, ext4_put_super ...(+5) | ext4_assert, ext4_bcache_cleanup, ext4_bcache_fini_dynamic, ext4_bcache_init_dynamic, ext4_block_bind_bcache, ext4_block_fini ...(+7) |
| `src/fs/ext4/ext4.c#ext4_add_dir_entry` | `src/fs/ext4/ext4_balloc.c#ext4_balloc_alloc_block` | 同模块上下文对齐/差异观察 | (u16), (u32), ext4_alloc_block, ext4_append_block_to_inode, ext4_get_file_block, ext4_get_sbi ...(+7) | ext4_balloc_get_bgid_of_block, ext4_balloc_get_block_of_bgid, ext4_balloc_set_bitmap_csum, ext4_balloc_verify_bitmap_csum, ext4_bg_get_block_bitmap, ext4_bg_get_free_blocks_count ...(+19) |
| `src/fs/ext4/ext4.c#ext4_mkdir` | `src/fs/ext4/ext4_journal.c#jbd_journal_prepare` | 同模块上下文对齐/差异观察 | (u32), ext4_add_dir_entry, ext4_alloc_block, ext4_alloc_inode_from_disk, ext4_get_sbi, ext4_read_group_desc ...(+8) | EXT4_BLOCK_ZERO, RB_FIND, TAILQ_FOREACH_REVERSE_SAFE, TAILQ_FOREACH_SAFE, TAILQ_REMOVE, ext4_bcache_clear_dirty ...(+17) |

#### 通用模块/其他
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `src/misc/string.c#memcmp` | `src/lib/string.c#memcmp` | 同板块同名 | 内部闭环逻辑 | 内部闭环逻辑 |
| `src/misc/string.c#strncpy` | `src/lib/string.c#strncpy` | 同板块同名 | 内部闭环逻辑 | 内部闭环逻辑 |
| `src/misc/string.c#strncmp` | `src/lib/string.c#strncmp` | 同板块同名 | 内部闭环逻辑 | 内部闭环逻辑 |
| `src/misc/string.c#strcat` | `src/lib/string.c#strcat` | 同板块同名 | strlen | 内部闭环逻辑 |


#### 1. 宏观判定
- **定性结论**: 独立开发 (相似度: 1.15%，共同 AST 指纹: 15；唯一 AST 指纹: 目标项目 561，参考项目 754；覆盖率: 目标项目 2.67%，参考项目 1.99%)

#### 2. 全模块覆盖矩阵解读
- **进程管理**: 双方均存在该模块。存在 2 个共同 AST 指纹，目标覆盖率 6.45%，参考覆盖率 4.65%。重合点集中在 `sys_getpid`、`sys_getppid` 等 POSIX 强制接口的最小实现上，其余 35/43 个函数无结构重合。证据强度：**弱**。
- **任务调度/异步运行时**: 双方均存在该模块。存在 1 个共同 AST 指纹，目标覆盖率 2.33%，参考覆盖率 2.44%。`sys_sched_yield` 的同构源于其“立即让出CPU”的原子语义，调度器核心逻辑（如目标 `dequeue_proc` 与参考 `sched`）在数据结构与状态管理上完全不同。证据强度：**弱**。
- **内存管理**: 双方均存在该模块。存在 2 个共同 AST 指纹，目标覆盖率 0.93%，参考覆盖率 1.68%。`kvminit` 和 `mappages` 的同名源于 RISC-V 架构下页表建立的硬件强制流程，但目标采用独立 `pagetable_create` 抽象，参考使用 `kalloc` 直接分配，内存模型异构。证据强度：**弱**。
- **中断/异常处理**: 双方均存在该模块。共同 AST 指纹为 0，覆盖率均为 0.0%。目标基于 virtio 设备中断处理，参考基于 PLIC/DMAC 硬件，上下文完全不可对齐。证据强度：**未形成**。
- **系统调用**: 双方均存在该模块。共同 AST 指纹为 0，覆盖率均为 0.0%。目标拥有 60 个系统调用函数，参考仅 6 个，功能集与分发机制差异巨大。证据强度：**未形成**。
- **文件系统**: 双方均存在该模块。存在 2 个共同 AST 指纹，目标覆盖率 2.27%，参考覆盖率 3.57%。重合点为 `vfs_write`/`vfs_lseek` 等 VFS 通用接口的空壳或简单转发，底层 ext4 实现调用链差异显著。证据强度：**弱**。
- **设备驱动**: 双方均存在该模块。共同 AST 指纹为 0，覆盖率均为 0.0%。目标为 virtio 设备模型，参考为 SPI/SDCard/FPIOA 等嵌入式外设，硬件抽象层完全不同。证据强度：**未形成**。
- **网络协议栈**: 双方均未体现此模块。证据强度：**未形成**。
- **第三方库/公共依赖**: 双方均存在该模块。共同 AST 指纹为 0，覆盖率均为 0.0%。目标包含 lz4 等压缩库，参考包含 fat32 等完整文件系统实现，依赖库选型完全不同。证据强度：**未形成**。

#### 3. 底层 AST 关键证据指纹表
| 所属板块 | 目标项目路径 | 参考项目路径 | 证据类型 | 技术说明 |
| :--- | :--- | :--- | :--- | :--- |
| 进程管理 | `[src/syscall/syscall.c#sys_getpid]` | `[src/kernel/sysproc.c#sys_getpid]` | 同板块同名同结构 | POSIX 标准接口，AST 均为单节点调用当前进程指针（`curproc` vs `myproc`），属领域强制收敛。 |
| 进程管理 | `[src/trap/trap.c#forkret]` | `[src/proc/proc.c#forkret]` | 同板块同名 | 均调用 `usertrapret` 返回用户态，这是 RISC-V trap 架构的强制路径。外部调用差异巨大（目标无文件系统初始化）。 |
| 任务调度/异步运行时 | `[src/syscall/syscall.c#sys_sched_yield]` | `[src/kernel/sysothers.c#sys_sched_yield]` | 同板块同名同结构 | 均为直接调用调度让出函数（`sched_yield` vs `yield`），是让出 CPU 的最小实现。 |
| 内存管理 | `[src/mm/vm.c#kvminit]` | `[src/mm/vm.c#kvminit]` | 同板块同名 | 内核虚拟内存初始化，均涉及页表创建和映射，但目标使用 `pagetable_create`，参考使用 `kalloc`。 |
| 内存管理 | `[src/mm/pagetable.c#mappages]` | `[src/mm/vm.c#mappages]` | 同板块同名 | 页表映射函数，均使用 `PA2PTE` 宏，但内部实现（`va2pte` vs `walk`）不同。 |
| 文件系统 | `[src/fs/vfs.c#vfs_write]` | `[src/lib/utils.c#get_gpio_bit]` | 同板块同结构 | 极简转发函数与位操作函数产生巧合性 AST 碰撞，无实际同源意义。 |
| 文件系统 | `[src/fs/vfs.c#vfs_lseek]` | `[src/fs/fat32/fat32.c#enameparent]` | 同板块同结构 | 极简转发函数与路径查找函数产生巧合性 AST 碰撞，无实际同源意义。 |
| 通用模块/其他 | `[src/misc/string.c#memcmp]` | `[src/lib/string.c#memcmp]` | 同板块同名 | 标准 C 库函数，实现逻辑由算法规范决定，属公共领域代码。 |

#### 4. 核心代码调用流对比剖析
* **【进程管理/进程退出路径】**
* **底层技术析理**: 目标 `sys_exit` 调用 `curproc, panic, sched_yield, wait_queue_wakeup_one`，采用显式等待队列唤醒机制；参考 `sys_exit` 调用 `argint, exit`，采用进程状态标记与调度器轮询机制。两者在同步原语（等待队列 vs 进程状态）和资源回收策略上存在根本性架构差异。
* **代码溯源链接**:
  - 目标项目: `src/syscall.c#sys_exit`
  - 参考项目: `src/kernel/sysproc.c#sys_exit`

* **【内存管理/内核页表初始化】**
* **底层技术析理**: 目标 `kvminit` 调用 `KVM, MAKE_SATP, devminit, kmappages, pagetable_create, panic`，使用独立的页表创建抽象并包含设备内存映射；参考 `kvminit` 调用 `kalloc, kvmmap, memset, printf`，直接基于物理页分配器构建映射。两者在内存对象管理模型（页表对象 vs 裸页）上异构。
* **代码溯源链接**:
  - 目标项目: `src/mm/vm.c#kvminit`
  - 参考项目: `src/mm/vm.c#kvminit`

* **【文件系统/Ext4 挂载】**
* **底层技术析理**: 目标 `ext4_mount` 调用 `IS_ERR, PTR, PTR_ERR, dentry_root, ext4_fill_super, ext4_put_super`，采用基于 VFS dentry 和超级块的标准挂载流程；参考 `ext4_mount` 调用 `ext4_assert, ext4_bcache_cleanup, ext4_bcache_fini_dynamic, ext4_bcache_init_dynamic, ext4_block_bind_bcache`，深度依赖自定义块缓存层。两者在存储栈抽象层次上完全不同。
* **代码溯源链接**:
  - 目标项目: `src/fs/ext4/ext4.c#ext4_mount`
  - 参考项目: `src/fs/ext4/ext4.c#ext4_mount`

#### 5. 单方模块与降权项
- **单方独有模块/功能**：目标项目独有 futex 机制（`futex_bucket`）、资源限制（`rlimit`）、共享内存子进程同步（`sync_shared_mmap_base_children`）；参考项目独有管道（`pipe`）、睡眠锁（`sleeplock`）、DMAC 等待、`clone` 系统调用。这些功能集合的根本差异无法用“剥离”或“重命名”解释。
- **跨板块同名降权**：`ext4_unlink` 等同名函数分属不同项目，但调用链差异巨大，且 ext4 本身是公共文件系统标准实现，同名属领域常态。
- **公共接口与泛型函数**：`memcmp`、`strncpy`、`strncmp`、`strcat` 等 C 标准库函数，以及 `INIT_LIST_HEAD`、`list_add_tail` 等内核通用链表宏，其实现由语言规范或经典算法决定，不构成同源证据。

#### 6. 保守结论
本次审计的客观 AST 相似度仅为 1.15%，全项目仅 15 个共同指纹，远低于可判定抄袭的阈值。控方提出的四项指控均基于功能语义类比和领域通用模式，而非 AST 结构重合。双方在进程管理、内存管理、文件系统等核心板块的同步原语、内存模型、存储栈抽象上存在根本性架构差异，且各自拥有对方不具备的显著功能集合。现有证据链充分支持独立开发结论，整体抄袭风险极低。

------------------------------

