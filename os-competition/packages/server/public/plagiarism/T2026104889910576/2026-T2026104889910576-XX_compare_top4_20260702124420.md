# 🛡️ OS 内核赛自动化代码级架构审计报告

**审计时间**: `2026-07-02 12:44:20`
**参赛作品名称**: `2026-T2026104889910576-XX`
**年份**: `2026`
**学校**: `XX`
**队伍名称**: `T2026104889910576`
**比赛名字**: `OS-Competition`
**比赛子赛道**: `Kernel-Implementation`
**仓库地址**: `https://gitlab.eduxiji.net/educg-group-43501-3132633/T2026104889910576-1616`
**推测开发语言**: `Rust / C (由AST探针自动识别)`
**特征函数量**: `654`

## ⚔️ 底层代码同源性对比 (AST Homology)
> 本部分由专门的代码审查智能体，对该项目与符合年份条件的历届最高相似度项目进行控制流与底层调用对比。新版报告会先给出确定性的全模块覆盖矩阵，再给出 Agent 的分模块解释，避免只围绕少量抽样函数展开。

### 🏆 Top 4 底层对标: vs `2024-SCUCCS-CST-SRA-四川大学`（四川大学 / SCUCCS-CST-SRA）

#### 对方作品信息

**对方年份**: `2024`
**对方学校**: `四川大学`
**对方队伍名称**: `SCUCCS-CST-SRA`
**对方比赛名字**: `OS-Competition`
**对方比赛子赛道**: `Kernel-Implementation`
**对方仓库地址**: `https://gitlab.eduxiji.net/educg-group-26011-2376549/T202410610993313-2141`

> 双向覆盖率：共同 AST 指纹: 14；唯一 AST 指纹: 目标项目 561，参考项目 667；覆盖率: 目标项目 2.5%，参考项目 2.1%

## 全模块对比总览（确定性证据）

> 总体覆盖率：共同 AST 指纹: 14；唯一 AST 指纹: 目标项目 561，参考项目 667；覆盖率: 目标项目 2.5%，参考项目 2.1%

### 1. 模块覆盖矩阵
| 模块 | 目标函数数 | 参考函数数 | 共同唯一 AST 指纹 | Target 覆盖 | Base 覆盖 | 状态 |
| :--- | ---: | ---: | ---: | ---: | ---: | :--- |
| 进程管理 | 37 | 15 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |
| 任务调度/异步运行时 | 50 | 35 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |
| 内存管理 | 223 | 97 | 2 | 0.93% | 2.22% | 双方存在且有结构重合 |
| 中断/异常处理 | 17 | 17 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |
| 系统调用 | 60 | 26 | 3 | 11.54% | 15.79% | 双方存在且有结构重合 |
| 文件系统 | 99 | 54 | 2 | 2.27% | 4.35% | 双方存在且有结构重合 |
| 设备驱动 | 29 | 9 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |
| 网络协议栈 | 0 | 0 | 0 | 0.0% | 0.0% | 双方均未体现 |
| 文件系统/第三方库 | 36 | 501 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |
| 通用模块/其他 | 103 | 49 | 3 | 3.23% | 6.38% | 双方存在且有结构重合 |

### 2. 分模块证据清单

#### 进程管理
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `src/user/init.c#run_exec_group` | `src/core/proc.c#switch_to` | 同模块上下文对齐/差异观察 | my_chdir, my_execve, my_exit, my_fork, my_strcat, my_strcpy ...(+6) | invtlb_all, swtch, w_csr_pgdl |
| `src/sync/wait.c#wait_queue_wakeup_addr` | `src/core/syscall.c#sys_wait4` | 同模块上下文对齐/差异观察 | enqueue_proc, list_del, list_entry, list_for_each_safe, r_tp, spinlock_acquire ...(+2) | argraw, memset, my_proc, proc_find, yield |
| `src/syscall/syscall.c#reap_zombie_threads` | `src/core/syscall.c#sys_execve` | 同模块上下文对齐/差异观察 | INIT_LIST_HEAD, free_proc, list_del, list_entry, list_for_each_safe, spinlock_acquire ...(+1) | argraw, exec, get_path, kill, my_proc, strcpy |
| `src/syscall/syscall.c#sync_shared_mmap_base_children` | `src/core/proc.c#in_kernel_memory` | 同模块上下文对齐/差异观察 | list_entry, list_for_each, shared_vm_owner, spinlock_acquire, spinlock_release, sync_shared_mmap_base_children | 内部闭环逻辑 |

#### 任务调度/异步运行时
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `src/misc/printf.c#_vsnprintf` | `src/core/kdb.c#cmd_handler` | 同模块上下文对齐/差异观察 | _atoi, _etoa, _ftoa, _is_digit, _ntoa_long, _ntoa_long_long ...(+3) | breakpoint_add, cmd_parse, expr, get_trapframe_reg, getstr, hexdump ...(+3) |
| `src/syscall/syscall.c#syscall_init` | `src/driver/ahci.c#do_ahci_write` | 同模块上下文对齐/差异观察 | syscall_register | Log, PHYS_TO_UNCACHED, find_cmdslot, memset |
| `src/syscall.c#syscall_init` | `src/driver/ahci.c#do_ahci_read` | 同模块上下文对齐/差异观察 | syscall_register | Log, PHYS_TO_UNCACHED, find_cmdslot, memset |
| `src/syscall/syscall.c#sys_fcntl` | `src/lolibc/stdio.c#vsprintf` | 同模块上下文对齐/差异观察 | curproc, fd_get, fd_install, file_get, file_put, spinlock_acquire ...(+1) | _itoa, _uatoi, _uitoa, copy_fmt_str, get_fmt_flag, panic_on ...(+3) |

#### 内存管理
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `src/syscall/syscall.c#sys_mmap` | `src/core/syscall.c#sys_mmap` | 同板块同名 | PAGE_ALIGN_UP, curproc, fd_get, file_put, file_read, find_free_user_range ...(+12) | argraw, kmalloc |
| `src/syscall/syscall.c#sys_brk` | `src/core/syscall.c#sys_brk` | 同板块同名 | PAGE_ALIGN_UP, curproc, infof, kfree, kzalloc, shared_map_page ...(+6) | argraw, assert, my_proc |
| `src/ipc/pipe.c#pipe_create` | `src/fs/pipe.c#pipe_create` | 同板块同名 | SPINLOCK_INITIALIZER, kzalloc, wait_queue_init | pipe_alloc |
| `src/mm/kalloc.c#kfree` | `src/mm/mm.c#kfree` | 同板块同名 | PAGE_ALIGN_DOWN, addr2page, buddy_free, kmem_cache_free, unlikely, warnf | PADDR_TO_PFN, kfree_bytes_slab, kfree_pages_buddy |

#### 中断/异常处理
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `src/hal/timer.c#timer_init` | `src/core/trap.c#timer_init` | 同板块同名 | r_time, sbi_set_timer, us_to_cputime | w_csr_tcfg |
| `src/trap/trap.c#trap_init` | `src/core/trap.c#trap_init` | 同板块同名 | r_sie, r_sstatus, set_kerneltrap, w_sie, w_sstatus | timer_init, w_csr_ecfg, w_csr_eentry, w_csr_tlbrentry |
| `src/hal/virtio/virtio_blk.c#virtio_blk_init_all` | `src/core/kdb.c#get_trapframe_reg` | 同模块上下文对齐/差异观察 | mmio_read32, virtio_blk_init_one | assert, memcmp |
| `src/hal/virtio/virtio_net.c#virtio_net_init` | `src/core/kdb.c#info_registers` | 同模块上下文对齐/差异观察 | mmio_read32, virtio_net_init_one, warnf | info_csrs, printf |

#### 系统调用
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `src/syscall/syscall.c#sys_faccessat` | `src/core/syscall.c#sys_faccessat` | 同板块同名 | 内部闭环逻辑 | 内部闭环逻辑 |
| `src/syscall.c#sys_fcntl` | `src/core/syscall.c#sys_fcntl` | 同板块同名 | 内部闭环逻辑 | argraw, fcntl |
| `src/syscall.c#sys_unlinkat` | `src/core/syscall.c#sys_unlinkat` | 同板块同名 | 内部闭环逻辑 | argraw, unlinkat |
| `src/user/init.c#my_dup3` | `src/core/syscall.c#sys_return_random` | 同板块同结构 | syscall3 | rand |

#### 文件系统
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `src/fs/vfs.c#vfs_write` | `src/fs/fs2.c#openat` | 同板块同结构 | file_write | open |
| `src/fs/vfs.c#vfs_sync` | `src/fs/fs2.c#statx` | 同板块同结构 | 内部闭环逻辑 | 内部闭环逻辑 |
| `src/misc/lz4.c#lz4_compress_generic` | `src/fs/fat32.c#find_offset_by_pathname` | 同模块上下文对齐/差异观察 | (u32), (u8), LZ4_HASH_FUNCTION, lz4_read32, lz4_write_literal, memcpy ...(+1) | strcmp |
| `src/user/init.c#run_busybox_cmd_list` | `src/fs/fat32.c#find_size_by_pathname` | 同模块上下文对齐/差异观察 | line_needs_shell, my_chdir, my_close, my_execve, my_exit, my_fork ...(+11) | strcmp |

#### 设备驱动
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `src/hal/virtio/virtio_mmio.c#virtio_negotiate_features` | `src/driver/ahci.c#ahic_probe_port` | 同模块上下文对齐/差异观察 | errorf, mb, virtio_get_status, virtio_read_reg, virtio_set_status, virtio_write_reg | printf |
| `src/hal/virtio/virtio_mmio.c#virtio_mmio_init` | `src/driver/ahci.c#find_cmdslot` | 同模块上下文对齐/差异观察 | errorf, mb, virtio_get_status, virtio_read_reg, virtio_set_status | Log, get_bits_range |
| `src/hal/blk.c#blk_register` | `src/driver/ahci.c#ahci_write` | 同模块上下文对齐/差异观察 | MAJOR, MINOR, errorf, spinlock_acquire, spinlock_release, warnf | do_ahci_write, memcpy |
| `src/hal/virtio/virtq.c#virtq_get_buf` | `src/driver/ahci.c#ahci_read` | 同模块上下文对齐/差异观察 | rmb, spinlock_acquire, spinlock_release, virtq_free_desc | do_ahci_read, memcpy |

#### 文件系统/第三方库
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `src/fs/ext4/ext4.c#ext4_unlink` | `src/fs/lwext4/src/ext4.c#ext4_unlink` | 同板块同名 | (u32), ext4_get_file_block, ext4_get_sbi, ext4_read_data, ext4_write_at, inode_dirty ...(+3) | ext4_dir_remove_entry, ext4_fs_inode_links_count_dec, ext4_has_children, ext4_inode_get_links_cnt, ext4_inode_is_type |
| `src/fs/ext4/ext4.c#ext4_mount` | `src/fs/lwext4/src/ext4.c#ext4_mount` | 同板块同名 | IS_ERR, PTR, PTR_ERR, dentry_root, ext4_fill_super, ext4_put_super ...(+5) | ext4_assert, ext4_bcache_cleanup, ext4_bcache_fini_dynamic, ext4_bcache_init_dynamic, ext4_block_bind_bcache, ext4_block_fini ...(+7) |
| `src/fs/ext4/ext4.c#ext4_add_dir_entry` | `src/fs/lwext4/src/ext4_balloc.c#ext4_balloc_alloc_block` | 同模块上下文对齐/差异观察 | (u16), (u32), ext4_alloc_block, ext4_append_block_to_inode, ext4_get_file_block, ext4_get_sbi ...(+7) | ext4_balloc_get_bgid_of_block, ext4_balloc_get_block_of_bgid, ext4_balloc_set_bitmap_csum, ext4_balloc_verify_bitmap_csum, ext4_bg_get_block_bitmap, ext4_bg_get_free_blocks_count ...(+19) |
| `src/fs/ext4/ext4.c#ext4_mkdir` | `src/fs/lwext4/src/ext4_journal.c#jbd_journal_prepare` | 同模块上下文对齐/差异观察 | (u32), ext4_add_dir_entry, ext4_alloc_block, ext4_alloc_inode_from_disk, ext4_get_sbi, ext4_read_group_desc ...(+8) | EXT4_BLOCK_ZERO, RB_FIND, TAILQ_FOREACH_REVERSE_SAFE, TAILQ_FOREACH_SAFE, TAILQ_REMOVE, ext4_bcache_clear_dirty ...(+17) |

#### 通用模块/其他
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `src/misc/string.c#strtok` | `src/lolibc/string.c#strtok` | 同板块同名 | strchr | strcspn, strspn |
| `src/misc/string.c#memcmp` | `src/lolibc/string.c#memcmp` | 同板块同名 | 内部闭环逻辑 | 内部闭环逻辑 |
| `src/misc/string.c#strncpy` | `src/lolibc/string.c#strncpy` | 同板块同名 | 内部闭环逻辑 | 内部闭环逻辑 |
| `src/misc/string.c#strncmp` | `src/lolibc/string.c#strncmp` | 同板块同名 | 内部闭环逻辑 | 内部闭环逻辑 |


好的，审判长。基于客观 AST 指纹证据、全模块覆盖矩阵及双方架构画像，本庭对 `[2026-T2026104889910576-XX]`（目标项目）与 `[2024-SCUCCS-CST-SRA-四川大学]`（参考项目）的同源性争议作出最终定性仲裁。

### 1. 宏观判定
- **定性结论**: **独立开发**
- **核心依据**: 客观 AST 相似度仅为 **1.15%**，共同 AST 指纹仅 14 个，远低于构成代码抄袭或结构性复用的阈值。目标项目与参考项目在**进程管理**、**任务调度/异步运行时**等核心指控板块的共同唯一 AST 指纹均为 **0**，直接否定了控方关于“同板块底层逻辑一致”的指控。双方在指令集架构、文件系统抽象、并发模型等底层设计上存在根本性差异。

### 2. 全模块覆盖矩阵解读
| 模块 | 双方存在 | 共同指纹 | 覆盖率 (目标/参考) | 证据强度 | 解读 |
| :--- | :---: | :---: | :---: | :---: | :--- |
| **进程管理** | 是 | **0** | 0.0% / 0.0% | **未形成** | 控方重点指控板块，但客观 AST 指纹无任何重合。双方均实现了进程调度、等待、退出等 POSIX 语义，但调用图谱（如 `spinlock_acquire` vs `assert`）和外部依赖完全不同，属独立实现。 |
| **任务调度/异步运行时** | 是 | **0** | 0.0% / 0.0% | **未形成** | 控方另一重点指控板块，共同指纹为零。目标项目采用显式自旋锁和 VFS 函数指针派发，参考项目使用直接 ext4 调用和日志宏，架构风格迥异。`main` 函数同名但职责完全不同（内核入口 vs 用户态初始化）。 |
| **内存管理** | 是 | 2 | 0.93% / 2.22% | **弱** | 共同指纹源于 `sys_mmap`、`sys_brk` 等 POSIX 标准系统调用入口和 `kfree` 等通用内核概念。这些顶层函数名由规范决定，但内部实现（如目标项目的 `buddy_free` vs 参考项目的 `kfree_bytes_slab`）完全不同。 |
| **中断/异常处理** | 是 | **0** | 0.0% / 0.0% | **未形成** | 双方均有 `timer_init`、`trap_init` 等标准初始化函数，但操作的特权寄存器完全不同（RISC-V `r_sie`/`w_sie` vs LoongArch `w_csr_tcfg`/`w_csr_eentry`），无复用可能。 |
| **系统调用** | 是 | 3 | 11.54% / 15.79% | **弱** | 共同指纹来自 `sys_faccessat`、`sys_fcntl`、`sys_unlinkat` 等 POSIX 标准系统调用。这些接口名和参数结构由规范强制规定，必然产生相似顶层 AST 节点，但内部实现路径（如目标项目的 VFS 派发 vs 参考项目的直接调用）不同。 |
| **文件系统** | 是 | 2 | 2.27% / 4.35% | **弱** | 共同指纹源于 `vfs_write`/`openat`、`vfs_sync`/`statx` 等文件操作语义对齐。目标项目构建了完整的 VFS 抽象层，参考项目是直接耦合 ext4 的实现，架构差异显著。 |
| **设备驱动** | 是 | **0** | 0.0% / 0.0% | **未形成** | 双方驱动模型完全不同。目标项目基于 virtio-mmio 总线，参考项目基于 AHCI 控制器，外部调用图谱无任何交集。 |
| **网络协议栈** | 否 | 0 | 0.0% / 0.0% | **未形成** | 双方均未体现网络协议栈模块。 |
| **第三方库/公共依赖** | 是 | **0** | 0.0% / 0.0% | **未形成** | 参考项目包含 501 个函数的 `lwext4` 库，目标项目有独立的 ext4 实现。尽管存在 `ext4_unlink` 等同名函数，但这是文件系统规范决定的，且模块矩阵显示共同指纹为 0，表明无代码级复用。 |
| **通用模块/其他** | 是 | 3 | 3.23% / 6.38% | **弱** | 共同指纹全部来自 `strtok`、`memcmp`、`strncpy` 等 C 标准库函数。这些是任何 C 项目都可能独立实现的通用算法，属于公共知识领域，不构成原创性争议。 |

### 3. 底层 AST 关键证据指纹表
| 所属板块 | 目标项目路径 | 参考项目路径 | 证据类型 | 技术说明 |
| :--- | :--- | :--- | :--- | :--- |
| 通用模块/其他 | `[src/misc/string.c]` `strtok` | `[src/lolibc/string.c]` `strtok` | 同板块同名 | C 标准库函数，双方独立实现。目标项目调用 `strchr`，参考项目调用 `strcspn`/`strspn`，内部算法不同。 |
| 通用模块/其他 | `[src/misc/string.c]` `memcmp` | `[src/lolibc/string.c]` `memcmp` | 同板块同名 | C 标准库函数，双方均为“内部闭环逻辑”，属通用算法独立实现。 |
| 通用模块/其他 | `[src/misc/string.c]` `strncpy` | `[src/lolibc/string.c]` `strncpy` | 同板块同名 | C 标准库函数，双方均为“内部闭环逻辑”，属通用算法独立实现。 |
| 内存管理 | `[src/syscall/syscall.c]` `sys_mmap` | `[src/core/syscall.c]` `sys_mmap` | 同板块同名 | POSIX 标准系统调用入口。目标项目调用 `PAGE_ALIGN_UP`、`find_free_user_range` 等复杂 VMA 管理函数，参考项目仅调用 `argraw`、`kmalloc`，实现深度完全不同。 |
| 内存管理 | `[src/syscall/syscall.c]` `sys_brk` | `[src/core/syscall.c]` `sys_brk` | 同板块同名 | POSIX 标准系统调用入口。目标项目调用 `shared_map_page`、`kzalloc` 等，参考项目调用 `argraw`、`assert`，内部逻辑无重合。 |
| 系统调用 | `[src/syscall/syscall.c]` `sys_faccessat` | `[src/core/syscall.c]` `sys_faccessat` | 同板块同名 | POSIX 标准系统调用，双方均为“内部闭环逻辑”，但实现细节未知，顶层 AST 节点因接口规范而相似。 |
| 系统调用 | `[src/syscall.c]` `sys_fcntl` | `[src/core/syscall.c]` `sys_fcntl` | 同板块同名 | POSIX 标准系统调用。目标项目为“内部闭环逻辑”，参考项目调用 `argraw`、`fcntl`，实现路径不同。 |
| 系统调用 | `[src/syscall.c]` `sys_unlinkat` | `[src/core/syscall.c]` `sys_unlinkat` | 同板块同名 | POSIX 标准系统调用。目标项目为“内部闭环逻辑”，参考项目调用 `argraw`、`unlinkat`，实现路径不同。 |
| 文件系统 | `[src/fs/vfs.c]` `vfs_write` | `[src/fs/fs2.c]` `openat` | 同板块同结构 | 文件操作语义对齐。目标项目通过 VFS 层调用 `file_write`，参考项目直接调用 `open`，抽象层次不同。 |
| 文件系统 | `[src/fs/vfs.c]` `vfs_sync` | `[src/fs/fs2.c]` `statx` | 同板块同结构 | 文件系统元数据操作。双方均为“内部闭环逻辑”，但功能语义相似，属文件系统基本操作。 |
| 第三方库/公共依赖 | `[src/fs/ext4/ext4.c]` `ext4_unlink` | `[src/fs/lwext4/src/ext4.c]` `ext4_unlink` | 同板块同名 | 第三方 ext4 文件系统实现。函数名由 ext4 规范决定，但目标项目为独立实现，参考项目使用 `lwext4` 库，调用图谱完全不同。 |
| 第三方库/公共依赖 | `[src/fs/ext4/ext4.c]` `ext4_mount` | `[src/fs/lwext4/src/ext4.c]` `ext4_mount` | 同板块同名 | 同上，ext4 挂载操作。目标项目调用 `ext4_fill_super` 等自研函数，参考项目调用 `ext4_bcache_init_dynamic` 等 `lwext4` 库函数，无代码重合。 |

### 4. 核心代码调用流对比剖析
* **【进程管理/execve 执行流】**
* **底层技术析理**: 目标项目的 `execve` 路径（`run_exec_group` -> `my_execve` -> `execve_die`）是用户态测试框架通过系统调用封装实现，异常清理时显式回收文件描述符表并唤醒等待队列。参考项目的路径（`sys_execve` -> `exec` -> `kill`）是内核系统调用直接实现，失败时通过信号机制终止进程。两者在**抽象层次**（用户态封装 vs 内核态）、**资源回收模型**（显式回收 vs 信号驱动）和**同步策略**（等待队列 vs 无显式同步）上完全不同。
* **代码溯源链接**:
  - 目标项目: `src/user/init.c#run_exec_group` / `src/syscall/syscall.c#execve_die`
  - 参考项目: `src/core/syscall.c#sys_execve`

* **【任务调度/文件读取流】**
* **底层技术析理**: 目标项目采用 VFS 抽象层，`file_read` 通过函数指针 `file->f_op->read` 实现多文件系统支持，并使用 `spinlock_acquire/release` 保证并发安全。参考项目 `read` 直接调用 `ext4_fread`，耦合于单一文件系统，并发控制依赖 `my_proc` 和 `Log` 宏。两者在**对象管理模型**（VFS 指针派发 vs 直接调用）和**并发原语**（显式自旋锁 vs 隐式日志/断言）上存在根本性架构差异。
* **代码溯源链接**:
  - 目标项目: `src/fs/file.c#file_read`
  - 参考项目: `src/fs/fs2.c#read`

* **【中断/异常处理/定时器初始化】**
* **底层技术析理**: 目标项目 `timer_init` 基于 RISC-V 架构，通过 `sbi_set_timer` 设置定时器。参考项目 `timer_init` 基于 LoongArch 架构，通过 `w_csr_tcfg` 写入控制状态寄存器。两者操作的**硬件抽象层**和**指令集架构**完全不同，不存在指令级或逻辑级复用的基础。
* **代码溯源链接**:
  - 目标项目: `src/hal/timer.c#timer_init`
  - 参考项目: `src/core/trap.c#timer_init`

### 5. 单方模块与降权项
- **单方模块**: 参考项目包含 501 个函数的 `文件系统/第三方库` 模块（主要为 `lwext4`），目标项目无此依赖，表明双方在文件系统实现策略上完全不同。
- **降权项说明**:
  - **跨板块同名函数**: 如目标项目 `sys_kill`（任务调度）与参考项目 `kill`（进程管理），虽功能相关但分属不同模块，且各自板块内共同指纹为 0，不构成同板块主证据。
  - **标准 POSIX 接口/通用内核概念**: `sys_mmap`、`sys_fcntl`、`pipe_create`、`kfree` 等函数名由行业规范或通用知识决定，其顶层 AST 节点相似是功能需求所致，不能作为代码复用的证据。
  - **C 标准库函数**: `strtok`、`memcmp` 等属于公共领域算法实现，任何 C 项目都可能包含，必须从原创性争议中排除。

### 6. 保守结论
本庭裁定，目标项目 `[2026-T2026104889910576-XX]` 与参考项目 `[2024-SCUCCS-CST-SRA-四川大学]` 不存在代码抄袭或结构性复用关系。客观 AST 相似度仅 1.15%，14 个共同指纹全部可归因于 POSIX 标准接口、通用内核概念或 C 标准库函数等公共知识领域。控方重点指控的进程管理与任务调度板块，共同唯一 AST 指纹均为零，其“底层逻辑一致”的论断是将功能语义的必然相似曲解为代码结构复用。双方在指令集架构（RISC-V vs LoongArch）、文件系统抽象（VFS vs 直接 ext4）、并发模型（显式自旋锁 vs 日志断言）等核心设计上存在根本性差异，进一步支持了独立开发的结论。综合评估，**同源性风险极低**。

------------------------------

