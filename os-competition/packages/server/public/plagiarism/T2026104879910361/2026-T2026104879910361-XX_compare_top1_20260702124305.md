# 🛡️ OS 内核赛自动化代码级架构审计报告

**审计时间**: `2026-07-02 12:43:05`
**参赛作品名称**: `2026-T2026104879910361-XX`
**年份**: `2026`
**学校**: `XX`
**队伍名称**: `T2026104879910361`
**比赛名字**: `OS-Competition`
**比赛子赛道**: `Kernel-Implementation`
**仓库地址**: `https://gitlab.eduxiji.net/educg-group-43501-3132633/T2026104879910361-2728`
**推测开发语言**: `Rust / C (由AST探针自动识别)`
**特征函数量**: `4310`

## ⚔️ 底层代码同源性对比 (AST Homology)
> 本部分由专门的代码审查智能体，对该项目与符合年份条件的历届最高相似度项目进行控制流与底层调用对比。新版报告会先给出确定性的全模块覆盖矩阵，再给出 Agent 的分模块解释，避免只围绕少量抽样函数展开。

### 🏆 Top 1 底层对标: vs `2025-火箭队-哈尔滨工业大学`（哈尔滨工业大学 / 火箭队）

#### 对方作品信息

**对方年份**: `2025`
**对方学校**: `哈尔滨工业大学`
**对方队伍名称**: `火箭队`
**对方比赛名字**: `OS-Competition`
**对方比赛子赛道**: `Kernel-Implementation`
**对方仓库地址**: `https://gitlab.eduxiji.net/educg-group-36002-2710490/T202510213995926-2475`

> 双向覆盖率：共同 AST 指纹: 2159；唯一 AST 指纹: 目标项目 2767，参考项目 2341；覆盖率: 目标项目 78.03%，参考项目 92.23%

## 全模块对比总览（确定性证据）

> 总体覆盖率：共同 AST 指纹: 2159；唯一 AST 指纹: 目标项目 2767，参考项目 2341；覆盖率: 目标项目 78.03%，参考项目 92.23%

### 1. 模块覆盖矩阵
| 模块 | 目标函数数 | 参考函数数 | 共同唯一 AST 指纹 | Target 覆盖 | Base 覆盖 | 状态 |
| :--- | ---: | ---: | ---: | ---: | ---: | :--- |
| 进程管理 | 600 | 534 | 135 | 67.84% | 90.6% | 双方存在且有结构重合 |
| 任务调度/异步运行时 | 637 | 552 | 409 | 77.32% | 87.21% | 双方存在且有结构重合 |
| 内存管理 | 748 | 666 | 408 | 76.26% | 88.89% | 双方存在且有结构重合 |
| 中断/异常处理 | 42 | 41 | 34 | 94.44% | 97.14% | 双方存在且有结构重合 |
| 系统调用 | 104 | 85 | 74 | 78.72% | 97.37% | 双方存在且有结构重合 |
| 文件系统 | 1043 | 813 | 452 | 66.86% | 93.39% | 双方存在且有结构重合 |
| 设备驱动 | 185 | 185 | 144 | 100.0% | 100.0% | 双方存在且有结构重合 |
| 网络协议栈 | 159 | 156 | 136 | 97.84% | 100.0% | 双方存在且有结构重合 |
| 文件系统/第三方库 | 336 | 313 | 171 | 77.03% | 90.0% | 双方存在且有结构重合 |
| 通用模块/其他 | 456 | 454 | 287 | 98.29% | 98.63% | 双方存在且有结构重合 |

### 2. 分模块证据清单

#### 进程管理
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `os/src/fs/proc/cpuinfo.rs#serialize` | `os/src/fs/proc/cpuinfo.rs#serialize` | 同板块同名同结构 | String::new, result.push, result.push_str, self.apicid.to_string, self.bogomips.to_string, self.bugs.iter ...(+19) | String::new, result.push, result.push_str, self.apicid.to_string, self.bogomips.to_string, self.bugs.iter ...(+19) |
| `os/src/fs/proc_la2000/cpuinfo.rs#serialize` | `os/src/fs/proc_la2000/cpuinfo.rs#serialize` | 同板块同名同结构 | String::new, result.push, result.push_str, self.apicid.to_string, self.bogomips.to_string, self.bugs.iter ...(+19) | String::new, result.push, result.push_str, self.apicid.to_string, self.bogomips.to_string, self.bugs.iter ...(+19) |
| `os/src/futex/futex.rs#futex_wait` | `os/src/futex/futex.rs#futex_wait` | 同板块同名 | Err, FUTEXQUEUES.buckets[futex_hash(&key)].lock, FutexQ::new, Ok, TimeSpec::new_wall_time, current_task ...(+17) | Err, FUTEXQUEUES.buckets[futex_hash(&key)].lock, FutexQ::new, Ok, TimeSpec::new_wall_time, current_task ...(+17) |
| `os/src/syscall/task.rs#sys_execveat` | `os/src/syscall/task.rs#sys_execveat` | 同板块同名 | "/".to_string, Err, Nameidata::new, Ok, OpenFlags::empty, c_str_to_string ...(+20) | "/".to_string, Err, Nameidata::new, Ok, OpenFlags::empty, c_str_to_string ...(+17) |

#### 任务调度/异步运行时
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `os/src/syscall/fs.rs#sys_renameat2` | `os/src/syscall/fs.rs#sys_renameat2` | 同板块同名 | Arc::ptr_eq, Err, Nameidata::new, Ok, RenameFlags::from_bits, RenameFlags::from_bits(flags).unwrap ...(+52) | Arc::ptr_eq, Err, Nameidata::new, Ok, RenameFlags::from_bits, RenameFlags::from_bits(flags).unwrap ...(+38) |
| `os/src/syscall/util.rs#sys_timer_settimer` | `os/src/syscall/util.rs#sys_timer_settimer` | 同板块同名同结构 | Err, ITimerSpec::default, Ok, TimeSpec::new_machine_time, TimeSpec::new_wall_time, add_posix_timer ...(+16) | Err, ITimerSpec::default, Ok, TimeSpec::new_machine_time, TimeSpec::new_wall_time, add_posix_timer ...(+16) |
| `os/src/bpf/insn.rs#interpret` | `os/src/bpf/insn.rs#interpret` | 同板块同名同结构 | (addr as *const u64).read, BpfInsnClass::from, bpf_call, insn.dst_reg, insn.src_reg, insns.len ...(+10) | (addr as *const u64).read, BpfInsnClass::from, bpf_call, insn.dst_reg, insn.src_reg, insns.len ...(+10) |
| `os/src/syscall/task.rs#sys_prctl` | `os/src/syscall/task.rs#sys_prctl` | 同板块同名 | CapFlags::CAP_SETPCAP.bits, CapFlags::CAP_SYS_ADMIN.bits, Err, Ok, Sig::from, copy_from_user ...(+14) | CapFlags::CAP_SETPCAP.bits, CapFlags::CAP_SYS_ADMIN.bits, Err, Ok, Sig::from, copy_from_user ...(+11) |

#### 内存管理
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `os/src/fs/proc/mod.rs#init_procfs` | `os/src/fs/proc/mod.rs#init_procfs` | 同板块同名 | CPUINFO.call_once, CPUInfoFile::new, EXE.call_once, ExeFile::new, ExeInode::new, Ext4InodeDisk::default ...(+80) | CPUINFO.call_once, CPUInfoFile::new, EXE.call_once, ExeFile::new, ExeInode::new, Ext4InodeDisk::default ...(+77) |
| `os/src/mm/memory_set.rs#from_elf_lazily` | `os/src/mm/memory_set.rs#from_elf_lazily` | 同板块同名 | (p_vaddr as usize + DL_INTERP_OFFSET + p_memsz as usize).into, (p_vaddr as usize + DL_INTERP_OFFSET).into, (ph_vaddr as usize + ph_memsz as usize).into, (ph_vaddr as usize).into, Err, MapArea::new ...(+119) | (p_vaddr as usize + DL_INTERP_OFFSET + p_memsz as usize).into, (p_vaddr as usize + DL_INTERP_OFFSET).into, (ph_vaddr as usize + ph_memsz as usize).into, (ph_vaddr as usize).into, Err, MapArea::new ...(+117) |
| `os/src/fs/proc_la2000/mod.rs#init_procfs` | `os/src/fs/proc_la2000/mod.rs#init_procfs` | 同板块同名同结构 | CPUINFO.call_once, CPUInfoFile::new, EXE.call_once, ExeFile::new, ExeInode::new, Ext4InodeDisk::new_bare ...(+73) | CPUINFO.call_once, CPUInfoFile::new, EXE.call_once, ExeFile::new, ExeInode::new, Ext4InodeDisk::new_bare ...(+73) |
| `os/src/syscall/mod.rs#syscall` | `os/src/syscall/mod.rs#syscall` | 同板块同名 | Err, flush, log::trace!, log::warn!, sys_acct, sys_adjtimex ...(+239) | Err, flush, log::trace!, log::warn!, sys_acct, sys_adjtimex ...(+205) |

#### 中断/异常处理
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `os/src/net/tcp.rs#accept` | `os/src/net/tcp.rs#accept` | 同板块同名同结构 | Err, LISTEN_TABLE.lock, LISTEN_TABLE.lock().get, LISTEN_TABLE.lock().get().unwrap, LISTEN_TABLE.lock().get().unwrap().accept, Ok ...(+7) | Err, LISTEN_TABLE.lock, LISTEN_TABLE.lock().get, LISTEN_TABLE.lock().get().unwrap, LISTEN_TABLE.lock().get().unwrap().accept, Ok ...(+7) |
| `os/src/signal/sig_struct copy.rs#name` | `os/src/signal/sig_struct copy.rs#name` | 同板块同名同结构 | 内部闭环逻辑 | 内部闭环逻辑 |
| `os/src/signal/sig_struct.rs#name` | `os/src/signal/sig_struct.rs#name` | 同板块同名同结构 | 内部闭环逻辑 | 内部闭环逻辑 |
| `os/src/arch/la64/trap/mem_access.rs#get_size` | `os/src/arch/la64/trap/mem_access.rs#get_size` | 同板块同名同结构 | 内部闭环逻辑 | 内部闭环逻辑 |

#### 系统调用
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `os/src/net/socket.rs#set` | `os/src/net/socket.rs#set` | 同板块同名同结构 | <[u8; 4]>::try_from, <[u8; 4]>::try_from(&opt[0..4]).unwrap, Ok, Some, i32::from_ne_bytes, log::error! ...(+15) | <[u8; 4]>::try_from, <[u8; 4]>::try_from(&opt[0..4]).unwrap, Ok, Some, i32::from_ne_bytes, log::error! ...(+15) |
| `os/src/futex/mod.rs#do_futex` | `os/src/futex/mod.rs#do_futex` | 同板块同名同结构 | Some, TimeSpec::default, TimeSpec::new_machine_time, copy_from_user, futex_cmp_requeue, futex_op_to_flag ...(+7) | Some, TimeSpec::default, TimeSpec::new_machine_time, copy_from_user, futex_cmp_requeue, futex_op_to_flag ...(+7) |
| `os/src/syscall/net.rs#syscall_recvmmsg` | `os/src/syscall/net.rs#syscall_recvmmsg` | 同板块同名同结构 | (mmsghdr_ptr as *mut u8).add, (this_ptr as *mut u8).add, Err, MMsgHdrRaw::default, Ok, TimeSpec::default ...(+4) | (mmsghdr_ptr as *mut u8).add, (this_ptr as *mut u8).add, Err, MMsgHdrRaw::default, Ok, TimeSpec::default ...(+4) |
| `os/src/net/socket.rs#set` | `os/src/net/socket.rs#set` | 同板块同名同结构 | <[u8; 4]>::try_from, <[u8; 4]>::try_from(&opt[0..4]).unwrap, Ok, String::from_utf8, String::from_utf8(Vec::from(opt)).unwrap, Vec::from ...(+12) | <[u8; 4]>::try_from, <[u8; 4]>::try_from(&opt[0..4]).unwrap, Ok, String::from_utf8, String::from_utf8(Vec::from(opt)).unwrap, Vec::from ...(+12) |

#### 文件系统
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `os/src/fs/dev/mod.rs#init_devfs` | `os/src/fs/dev/mod.rs#init_devfs` | 同板块同名同结构 | DevT::loop_control_devt, DevT::loopx_devt, DevT::null_devt, DevT::rtc_devt, DevT::tty_devt, DevT::urandom_devt ...(+36) | DevT::loop_control_devt, DevT::loopx_devt, DevT::null_devt, DevT::rtc_devt, DevT::tty_devt, DevT::urandom_devt ...(+36) |
| `os/src/fs/dev_la2000/mod.rs#init_devfs` | `os/src/fs/dev_la2000/mod.rs#init_devfs` | 同板块同名同结构 | DevT::loop_control_devt, DevT::loopx_devt, DevT::null_devt, DevT::rtc_devt, DevT::tty_devt, DevT::urandom_devt ...(+36) | DevT::loop_control_devt, DevT::loopx_devt, DevT::null_devt, DevT::rtc_devt, DevT::tty_devt, DevT::urandom_devt ...(+36) |
| `os/src/net/unix.rs#passwd_lookup` | `os/src/net/unix.rs#passwd_lookup` | 同板块同名同结构 | Err, GroupEntry::parse_group_line, LAST_DB.lock, Ok, PasswdEntry::parse_passwd_line, Some ...(+22) | Err, GroupEntry::parse_group_line, LAST_DB.lock, Ok, PasswdEntry::parse_passwd_line, Some ...(+22) |
| `os/src/fat32/dentry.rs#read_dentry` | `os/src/fat32/dentry.rs#read_dentry` | 同板块同名同结构 | Some, log::info!, lsb16!, next_ord.is_some, next_ord.unwrap, reader.read_dentry ...(+4) | Some, log::info!, lsb16!, next_ord.is_some, next_ord.unwrap, reader.read_dentry ...(+4) |

#### 设备驱动
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `os/src/drivers/net/starfive/eth_dev.rs#eth_set_mtl` | `os/src/drivers/net/starfive/eth_dev.rs#eth_set_mtl` | 同板块同名同结构 | eth_mac_clrset_bits, eth_mac_read_reg, eth_mac_set_bits, eth_mac_write_reg, log::info! | eth_mac_clrset_bits, eth_mac_read_reg, eth_mac_set_bits, eth_mac_write_reg, log::info! |
| `os/src/drivers/net/starfive/eth_phy.rs#gmac_dev_genphy_config_aneg` | `os/src/drivers/net/starfive/eth_phy.rs#gmac_dev_genphy_config_aneg` | 同板块同名同结构 | gmac_mdio_read, gmac_mdio_write, log::info! | gmac_mdio_read, gmac_mdio_write, log::info! |
| `os/src/net/mod.rs#init_la` | `os/src/net/mod.rs#init_la` | 同板块同名同结构 | Box::new, Config::new, ETH0.init_once, GATEWAY.parse, GATEWAY.parse().expect, GATEWAY_V6.parse ...(+30) | Box::new, Config::new, ETH0.init_once, GATEWAY.parse, GATEWAY.parse().expect, GATEWAY_V6.parse ...(+30) |
| `os/src/drivers/net/starfive/drv_eth.rs#eth_rx` | `os/src/drivers/net/starfive/drv_eth.rs#eth_rx` | 同板块同名同结构 | GMAC_DESC_NUM.try_into, GMAC_DESC_NUM.try_into().unwrap, eth_get_desc_owner, eth_is_desc_empty, eth_mac_write_reg, gmacdev.RxDesc[desc_idx as usize].read ...(+6) | GMAC_DESC_NUM.try_into, GMAC_DESC_NUM.try_into().unwrap, eth_get_desc_owner, eth_is_desc_empty, eth_mac_write_reg, gmacdev.RxDesc[desc_idx as usize].read ...(+6) |

#### 网络协议栈
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `os/src/net/socket.rs#get` | `os/src/net/socket.rs#get` | 同板块同名同结构 | copy_nonoverlapping, copy_to_user, core::mem::size_of::<UCred>, gid_bytes.as_ptr, keep_alive.to_ne_bytes, opt_value.add ...(+17) | copy_nonoverlapping, copy_to_user, core::mem::size_of::<UCred>, gid_bytes.as_ptr, keep_alive.to_ne_bytes, opt_value.add ...(+17) |
| `os/src/net/socket.rs#get` | `os/src/net/socket.rs#get` | 同板块同名同结构 | bytes.as_bytes, bytes.as_ptr, bytes.len, copy_nonoverlapping, copy_to_user, log::error! ...(+8) | bytes.as_bytes, bytes.as_ptr, bytes.len, copy_nonoverlapping, copy_to_user, log::error! ...(+8) |
| `os/src/net/unix.rs#to_bytes` | `os/src/net/unix.rs#to_bytes` | 同板块同名同结构 | (off_pw_dir as u32).to_le_bytes, (off_pw_gecos as u32).to_le_bytes, (off_pw_name as u32).to_le_bytes, (off_pw_passwd as u32).to_le_bytes, (off_pw_shell as u32).to_le_bytes, (total_len as u32).to_le_bytes ...(+15) | (off_pw_dir as u32).to_le_bytes, (off_pw_gecos as u32).to_le_bytes, (off_pw_name as u32).to_le_bytes, (off_pw_passwd as u32).to_le_bytes, (off_pw_shell as u32).to_le_bytes, (total_len as u32).to_le_bytes ...(+15) |
| `os/src/net/listentable.rs#push_incoming_packet` | `os/src/net/listentable.rs#push_incoming_packet` | 同板块同名同结构 | SocketSetWrapper::new_tcp_socket, entry.can_accept, entry.syn_queue.len, entry.syn_queue.push_back, log::error!, self.table[dst.port as usize].lock ...(+4) | SocketSetWrapper::new_tcp_socket, entry.can_accept, entry.syn_queue.len, entry.syn_queue.push_back, log::error!, self.table[dst.port as usize].lock ...(+4) |

#### 文件系统/第三方库
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `os/src/ext4/mod.rs#mknod` | `os/src/ext4/mod.rs#mknod` | 同板块同名 | Ext4InodeDisk::new_blk, InvalidInode::new, LoopDevice::new, LoopInode::new, NullInode::new, OpenFlags::empty ...(+31) | InvalidInode::new, LoopDevice::new, LoopInode::new, NullInode::new, OpenFlags::empty, PipeInode::new ...(+29) |
| `os/src/ext4/inode.rs#insert_extent` | `os/src/ext4/inode.rs#insert_extent` | 同板块同名 | Err, Ext4Extent::new, Ext4ExtentBlock::new, Ext4ExtentBlock::new(
                    get_block_cache(child_block_num, block_device.clone(), ext4_block_size)
                        .lock()
                        .get_mut(0),
                )
                .insert_extent, Ok, block_device.clone ...(+19) | Err, Ext4Extent::new, Ext4ExtentBlock::new, Ext4ExtentBlock::new(
                    get_block_cache(child_block_num, block_device, ext4_block_size)
                        .lock()
                        .get_mut(0),
                )
                .insert_extent, Ext4ExtentBlock::new(
                    get_block_cache(child_block_num, block_device.clone(), ext4_block_size)
                        .lock()
                        .get_mut(0),
                )
                .insert_extent, Ok ...(+22) |
| `os/src/ext4/inode_la2000.rs#insert_extent` | `os/src/ext4/inode_la2000.rs#insert_extent` | 同板块同名 | Err, Ext4Extent::new, Ext4ExtentBlock::new, Ext4ExtentBlock::new(
                    get_block_cache(child_block_num, block_device.clone(), ext4_block_size)
                        .lock()
                        .get_mut(0),
                )
                .insert_extent, Ok, block_device.clone ...(+20) | Err, Ext4Extent::new, Ext4ExtentBlock::new, Ext4ExtentBlock::new(
                    get_block_cache(child_block_num, block_device, ext4_block_size)
                        .lock()
                        .get_mut(0),
                )
                .insert_extent, Ext4ExtentBlock::new(
                    get_block_cache(child_block_num, block_device.clone(), ext4_block_size)
                        .lock()
                        .get_mut(0),
                )
                .insert_extent, Ok ...(+22) |
| `os/src/ext4/inode.rs#write_direct` | `os/src/ext4/inode.rs#write_direct` | 同板块同名 | Ext4Extent::new, Ext4ExtentHeader::new_root, TimeSpec::new_wall_time, buf.len, data[0..EXT4_MAX_INLINE_DATA].copy_from_slice, data[start..end].copy_from_slice ...(+22) | Ext4Extent::new, Ext4ExtentHeader::new_root, TimeSpec::new_wall_time, buf.len, data[0..EXT4_MAX_INLINE_DATA].copy_from_slice, data[start..end].copy_from_slice ...(+22) |

#### 通用模块/其他
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `user/src/bin/shell/environment.rs#expand_variables` | `user/src/bin/shell/environment.rs#expand_variables` | 同板块同名同结构 | String::new, ch.is_alphanumeric, chars.next, chars.next().unwrap, chars.peek, input.chars ...(+6) | String::new, ch.is_alphanumeric, chars.next, chars.next().unwrap, chars.peek, input.chars ...(+6) |
| `os/src/utils.rs#seconds_to_beijing_datetime` | `os/src/utils.rs#seconds_to_beijing_datetime` | 同板块同名同结构 | days_in_month, days_in_year | days_in_month, days_in_year |
| `os/src/futex/jhash.rs#jhash2` | `os/src/futex/jhash.rs#jhash2` | 同板块同名同结构 | JHASH_INITVAL
        .wrapping_add, JHASH_INITVAL
        .wrapping_add(key.len() as u32)
        .wrapping_add, a.wrapping_add, b.wrapping_add, c.wrapping_add, jhash_final ...(+3) | JHASH_INITVAL
        .wrapping_add, JHASH_INITVAL
        .wrapping_add(key.len() as u32)
        .wrapping_add, a.wrapping_add, b.wrapping_add, c.wrapping_add, jhash_final ...(+3) |
| `os/src/index_list/mod.rs#trim_swap` | `os/src/index_list/mod.rs#trim_swap` | 同板块同名同结构 | ListIndex::new, debug_assert_eq!, dst.iter, e.is_none, e.is_some, self.elems.truncate ...(+16) | ListIndex::new, debug_assert_eq!, dst.iter, e.is_none, e.is_some, self.elems.truncate ...(+16) |


好的，审判长。本庭已收到控辩双方的全部陈述、证据底座及原始 AST 抽样数据。现依据审计指令，对本案进行最终定性仲裁。

### 1. 宏观判定
- **定性结论**: **部分逻辑借鉴 / 存在架构改动** (相似度: 73.21%，共同 AST 指纹: 2159；唯一 AST 指纹: 目标项目 2767，参考项目 2341；覆盖率: 目标项目 78.03%，参考项目 92.23%)

本庭认为，目标项目并非对参考项目的像素级复刻。高达 2767 的唯一 AST 指纹与 78.03% 的覆盖率表明，目标项目在参考项目所覆盖的领域之外，进行了大量的独立开发与功能扩展。然而，92.23% 的参考项目覆盖率与控方列举的诸多“同板块底层逻辑一致”证据，也清晰地揭示了目标项目在核心子系统上对参考项目存在深度的逻辑借鉴与代码骨架复用。因此，本案性质介于“完全独立开发”与“恶意抄袭”之间，属于在已有成熟实现基础上的二次开发与架构演进。

### 2. 全模块覆盖矩阵解读
| 模块 | 双方存在 | 共同指纹 | 覆盖率 (T/B) | 证据强度 | 解读 |
| :--- | :---: | :---: | :---: | :---: | :--- |
| **进程管理** | 是 | 是 | 67.84% / 90.6% | **强** | 参考项目绝大部分代码被目标项目覆盖，存在大量同板块同名且底层逻辑一致的函数，是本案相似性的核心来源。 |
| **任务调度/异步运行时** | 是 | 是 | 77.32% / 87.21% | **强** | 覆盖率双高，调度器核心逻辑（如 `run_tasks`）呈现高度一致的 AST 指纹，表明调度框架存在同源借鉴。 |
| **内存管理** | 是 | 是 | 76.26% / 88.89% | **强** | 核心的内存映射与 ELF 加载逻辑（如 `from_elf_lazily`）高度相似，是构成高相似度的重要部分。 |
| **中断/异常处理** | 是 | 是 | 94.44% / 97.14% | **弱** | 覆盖率极高，但证据多为内部闭环逻辑或简单函数，且此模块受硬件规范强约束，独创性表达空间小。 |
| **系统调用** | 是 | 是 | 78.72% / 97.37% | **强** | 参考项目几乎被完全覆盖。系统调用分发、Futex、信号处理等核心路径的 AST 结构高度一致，是抄袭指控的关键区域。 |
| **文件系统** | 是 | 是 | 66.86% / 93.39% | **强** | 参考项目绝大部分被覆盖。procfs、ext4、FAT32 等多个子系统的实现骨架高度雷同，是主要相似性来源之一。 |
| **设备驱动** | 是 | 是 | 100.0% / 100.0% | **中** | 覆盖率 100% 看似极高，但证据集中在 StarFive GMAC 网卡驱动，此类 BSP 代码由硬件手册决定，独立实现也必然趋同。 |
| **网络协议栈** | 是 | 是 | 97.84% / 100.0% | **中** | 覆盖率极高，但证据多为 Socket 选项处理、监听表管理等，受 POSIX/Linux ABI 与 smoltcp 等公开库影响大。 |
| **文件系统/第三方库** | 是 | 是 | 77.03% / 90.0% | **中** | 此模块本身被归类为第三方库，其相似性可能源于共同依赖的上游代码或公开教学框架，需降权处理。 |
| **通用模块/其他** | 是 | 是 | 98.29% / 98.63% | **弱** | 覆盖率极高，但证据多为语言惯用法（`jhash2`）、通用工具函数（`seconds_to_beijing_datetime`）或用户态程序，独创性低。 |

### 3. 底层 AST 关键证据指纹表
| 所属板块 | 目标项目路径 | 参考项目路径 | 证据类型 | 技术说明 |
| :--- | :--- | :--- | :--- | :--- |
| **进程管理** | `os/src/task/processor.rs#run_tasks` | `os/src/task/processor.rs#run_tasks` | 同板块底层逻辑一致 | 多核调度器启动的核心调用流完全一致：`BOOT_TASK.clone` → `PROCESSOR[hart_id].write` → `asm!` → `fetch_task`。 |
| **进程管理** | `os/src/syscall/signal.rs#sys_rt_sigprocmask` | `os/src/syscall/signal.rs#sys_rt_sigprocmask` | 同板块底层逻辑一致 | 信号掩码的用户态-内核态拷贝与校验逻辑完全一致：`copy_from_user` → `size_of::<SigSet>` → `copy_to_user`。 |
| **进程管理** | `os/src/fs/proc/meminfo.rs#serialize` | `os/src/fs/proc/meminfo.rs#serialize` | 同板块底层逻辑一致 | `/proc/meminfo` 的序列化字符串常量与拼接逻辑完全一致，包含 33 个相同的外部调用。 |
| **进程管理** | `os/src/fs/proc/fd.rs#lookup` | `os/src/fs/proc/fd.rs#lookup` | 同板块底层逻辑一致 | `/proc/self/fd` 的 Dentry 负载创建逻辑完全一致：`Dentry::negative` → `FdInode::new` → `update_type_from_negative`。 |
| **系统调用** | `os/src/syscall/task.rs#sys_execve` | `os/src/syscall/task.rs#sys_execve` | 同板块同名 | `execve` 的核心路径解析逻辑雷同，但目标项目将 `file.get_path` 替换为 `fanotify::notify_open_exec`，存在功能扩展。 |
| **系统调用** | `os/src/syscall/task.rs#deal_zombie_task` | `os/src/syscall/task.rs#deal_zombie_task` | 同板块同名 | 僵尸进程的 `SigInfo` 构造逻辑完全一致，但目标项目增加了 `child.release_zombie_resources` 调用，存在设计差异。 |
| **内存管理** | `os/src/mm/memory_set.rs#from_elf_lazily` | `os/src/mm/memory_set.rs#from_elf_lazily` | 同板块同名 | ELF 懒加载的内存区域创建逻辑高度一致，包含 119 个相同的外部调用，是内存管理模块相似性的核心。 |
| **文件系统** | `os/src/fs/proc/mod.rs#init_procfs` | `os/src/fs/proc/mod.rs#init_procfs` | 同板块同名 | procfs 初始化逻辑高度一致，包含 80 个相同的外部调用，用于注册 `cpuinfo`、`meminfo` 等文件。 |
| **文件系统** | `os/src/ext4/inode.rs#insert_extent` | `os/src/ext4/inode.rs#insert_extent` | 同板块同名 | ext4 文件系统的 extent 插入逻辑高度一致，包含对 `Ext4ExtentBlock::new` 和 `get_block_cache` 的相同调用模式。 |
| **设备驱动** | `os/src/drivers/net/starfive/eth_phy.rs#gmac_dev_genphy_process_aneg_result` | `os/src/drivers/net/starfive/eth_phy.rs#gmac_dev_genphy_process_aneg_result` | 同板块底层逻辑一致 | StarFive GMAC PHY 自协商处理逻辑完全一致，仅包含 `gmac_mdio_read/write` 和 `log::info!` 三个调用。 |
| **第三方库/公共依赖** | `os/src/futex/jhash.rs#jhash2` | `os/src/futex/jhash.rs#jhash2` | 同板块同名同结构 | Jenkins hash 算法的标准实现，属于公共领域算法，独创性极低。 |
| **第三方库/公共依赖** | `os/src/utils.rs#seconds_to_beijing_datetime` | `os/src/utils.rs#seconds_to_beijing_datetime` | 同板块同名同结构 | 时间戳转换工具函数，逻辑简单且为通用功能，独创性低。 |

### 4. 核心代码调用流对比剖析
* **【进程管理 / 调度器启动路径】**
* **底层技术析理**: 两者在多核调度器启动的核心路径上呈现出高度一致的调用流。目标项目在 `run_tasks` 中使用了 `crate::task::scheduler::fetch_task`，而参考项目在 `other_run_tasks` 中使用了 `crate::task::scheduler::init_fetch_task`，表明两者在调度入口上已产生分化。然而，其围绕 `BOOT_TASK.clone`、`PROCESSOR[hart_id].write` 和 `asm!` 宏进行上下文切换的底层逻辑骨架完全一致，这超出了硬件规范所能解释的范畴，指向了对参考项目调度框架的直接借鉴。
* **代码溯源链接**:
  - 目标项目: `os/src/task/processor.rs#run_tasks`
  - 参考项目: `os/src/task/processor.rs#other_run_tasks`

* **【系统调用 / 进程执行路径】**
* **底层技术析理**: 在 `sys_execve` 的实现中，两者共享了从用户态字符串提取、路径名解析到文件打开标志检查的完整前置流程。目标项目将参考项目中的 `file.get_path` 调用替换为 `fanotify::notify_open_exec`，这是一个显著的**架构差异**，表明目标项目集成并扩展了文件访问通知机制。这并非简单的“篡改伪装”，而是引入了新的功能特性。尽管如此，其主体框架的同源性依然清晰。
* **代码溯源链接**:
  - 目标项目: `os/src/syscall/task.rs#sys_execve`
  - 参考项目: `os/src/syscall/task.rs#sys_execve`

* **【文件系统 / procfs 初始化路径】**
* **底层技术析理**: 两者在初始化 procfs 时，对 `cpuinfo`、`meminfo`、`exe`、`fd` 等文件的注册流程和 Inode 构造逻辑高度一致。目标项目在 `os/src/fs/proc_la2000/mod.rs` 中存在一个几乎完全相同的副本，这被辩方解释为多架构兼容。然而，这种代码复制行为本身是软件工程中的反模式，增加了相似代码的总量，是构成高 AST 覆盖率的重要原因。
* **代码溯源链接**:
  - 目标项目: `os/src/fs/proc/mod.rs#init_procfs`
  - 参考项目: `os/src/fs/proc/mod.rs#init_procfs`

### 5. 单方模块与降权项
* **单方模块**: 目标项目独有的 2767 个 AST 指纹是其独立开发工作的直接证据，这部分代码体量甚至超过了与参考项目的共同部分，是辩方最有力的论据。
* **第三方库/公共依赖**: `os/src/futex/jhash.rs`、`os/src/utils.rs` 等文件中的函数，属于通用算法或工具函数，其相似性不能作为抄袭证据。
* **跨板块弱信号**: 控方关于 `proc/` 与 `proc_la2000/` 的“跨板块”指控，本庭不予采信。此二者属于同一子系统在不同架构下的实现，其内部函数的相似是架构设计导致的结果，而非跨模块抄袭。
* **硬件/规范约束项**: 设备驱动（StarFive GMAC）和部分系统调用（如 `sys_rt_sigprocmask`）的实现，受硬件手册和 POSIX/Linux ABI 的强约束，其 AST 相似性在原创性判定中应被降权。

### 6. 保守结论
本庭裁定，目标项目 `[2026-T2026104879910361-XX]` 并非对参考项目 `[2025-火箭队-哈尔滨工业大学]` 的简单抄袭，而是在其基础上进行了显著的功能扩展与架构演进的二次开发。73.21% 的 AST 相似度与 92.23% 的参考覆盖率，主要源于对进程管理、内存管理、文件系统等核心子系统实现骨架的深度借鉴，这部分构成了“部分逻辑借鉴”的事实基础。然而，目标项目拥有超过共同指纹数量的独立代码，并在系统调用、挂载管理等路径上引入了 `fanotify`、`USER_MOUNTS` 等差异化设计，证明了其具备独立的开发能力与设计决策。因此，本案不构成“像素级抄袭”，但存在明确的“同源性代码骨架复用”，整体风险等级为中等。建议目标项目在后续迭代中，对借鉴部分进行更彻底的重构，以强化其技术独立性与原创辨识度。

------------------------------

