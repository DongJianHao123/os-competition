# 🛡️ OS 内核赛自动化代码级架构审计报告

**审计时间**: `2026-07-02 02:39:11`
**参赛作品名称**: `2026-T202610006999602-XX`
**年份**: `2026`
**学校**: `XX`
**队伍名称**: `T202610006999602`
**比赛名字**: `OS-Competition`
**比赛子赛道**: `Kernel-Implementation`
**仓库地址**: `https://gitlab.eduxiji.net/educg-group-43501-3132633/T202610006999602-2239`
**推测开发语言**: `Rust / C (由AST探针自动识别)`
**特征函数量**: `3711`

## ⚔️ 底层代码同源性对比 (AST Homology)
> 本部分由专门的代码审查智能体，对该项目与符合年份条件的历届最高相似度项目进行控制流与底层调用对比。新版报告会先给出确定性的全模块覆盖矩阵，再给出 Agent 的分模块解释，避免只围绕少量抽样函数展开。

### 🏆 Top 4 底层对标: vs `2021-ATOM-北京工商大学`（北京工商大学 / ATOM）

#### 对方作品信息

**对方年份**: `2021`
**对方学校**: `北京工商大学`
**对方队伍名称**: `ATOM`
**对方比赛名字**: `OS-Competition`
**对方比赛子赛道**: `Kernel-Implementation`
**对方仓库地址**: `https://gitlab.eduxiji.net/chenzhiy/atom`

> 双向覆盖率：共同 AST 指纹: 121；唯一 AST 指纹: 目标项目 3141，参考项目 2288；覆盖率: 目标项目 3.85%，参考项目 5.29%

## 全模块对比总览（确定性证据）

> 总体覆盖率：共同 AST 指纹: 121；唯一 AST 指纹: 目标项目 3141，参考项目 2288；覆盖率: 目标项目 3.85%，参考项目 5.29%

### 1. 模块覆盖矩阵
| 模块 | 目标函数数 | 参考函数数 | 共同唯一 AST 指纹 | Target 覆盖 | Base 覆盖 | 状态 |
| :--- | ---: | ---: | ---: | ---: | ---: | :--- |
| 进程管理 | 388 | 45 | 6 | 1.88% | 14.63% | 双方存在且有结构重合 |
| 任务调度/异步运行时 | 368 | 49 | 5 | 1.59% | 10.42% | 双方存在且有结构重合 |
| 内存管理 | 1008 | 175 | 46 | 4.9% | 29.11% | 双方存在且有结构重合 |
| 中断/异常处理 | 63 | 6 | 1 | 1.85% | 20.0% | 双方存在且有结构重合 |
| 系统调用 | 206 | 27 | 3 | 1.95% | 15.79% | 双方存在且有结构重合 |
| 文件系统 | 982 | 281 | 36 | 4.21% | 16.07% | 双方存在且有结构重合 |
| 设备驱动 | 265 | 44 | 2 | 0.86% | 5.13% | 双方存在且有结构重合 |
| 网络协议栈 | 54 | 0 | 0 | 0.0% | 0.0% | 仅目标项目体现 |
| 文件系统/第三方库 | 132 | 25 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |
| 第三方库 | 0 | 6418 | 0 | 0.0% | 0.0% | 仅参考项目体现 |
| 通用模块/其他 | 245 | 32 | 5 | 2.35% | 17.86% | 双方存在且有结构重合 |

### 2. 分模块证据清单

#### 进程管理
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `os/src/task/processor.rs#run_tasks` | `os/src/task/processor.rs#run_tasks` | 同板块同名 | KernelWorkerBudget::default, Some, __switch, add_task, arch::cpu::wait_for_interrupt, fetch_task ...(+9) | PROCESSOR_LIST[core_id].run, get_core_id |
| `os/src/syscall/process.rs#sys_times` | `os/src/syscall/process.rs#sys_times` | 同板块同名 | copy_to_user, copy_to_user(tms as *mut Tms, &value).map_or, current_task, drop, get_time, inner.time_snapshot ...(+1) | current_user_token, get_time_us, time.add, translated_refmut |
| `os/src/syscall/process.rs#sys_uname` | `os/src/syscall/process.rs#sys_uname` | 同板块同名 | copy_to_user, copy_to_user(uts as *mut UtsName, &value).map_or, uts_machine, write_uts_field | UserBuffer::new, current_user_token, size_of::<utsname>, translated_byte_buffer, uname.as_bytes, userbuf.write ...(+1) |
| `os/src/syscall/process.rs#sys_exit` | `os/src/syscall/process.rs#sys_exit` | 同板块同名 | exit_current_and_run_next, klog_debug!, panic!, with_current_task_ref | exit_current_and_run_next, panic! |

#### 任务调度/异步运行时
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `os/src/syscall/fs.rs#sys_chdir` | `os/src/syscall/fs.rs#sys_chdir` | 同板块同名 | ResolveOptions::follow_all, component.len, copy_cstr_from_user, current_task, current_task().unwrap, fs_error_to_errno ...(+9) | String::new, Vec::new, ch_dir, current_task, current_task().unwrap, current_user_token ...(+20) |
| `os/src/syscall/fs.rs#sys_getdents64` | `os/src/syscall/fs.rs#sys_getdents64` | 同板块同名 | current_task, current_task().unwrap, descriptor.as_ref, descriptor.file, fd_table.exclusive_access, fd_table.get ...(+7) | Dirent::empty, UserBuffer::new, current_task, current_task().unwrap, current_user_token, dirent.as_bytes ...(+9) |
| `os/src/task.rs#suspend_current_and_run_next` | `os/src/task/mod.rs#suspend_current_and_run_next` | 同板块同名 | add_task, crate::drivers::flush_block_requests, drop, klog_debug!, schedule, take_current_task ...(+5) | current_task, current_task().unwrap, drop, schedule, task.acquire_inner_lock, task_inner.get_task_cx_ptr2 |
| `os/src/syscall/fs.rs#sys_getcwd` | `os/src/syscall/fs.rs#sys_getcwd` | 同板块同名 | Vec::from, absolute_path, buf.is_null, bytes.push, checked_copy_bytes_to_user, checked_copy_bytes_to_user(buf, &bytes).map_or ...(+9) | UserBuffer::new, current_task, current_task().unwrap, current_user_token, inner.current_path.as_bytes, task.acquire_inner_lock ...(+2) |

#### 内存管理
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `os/src/syscall/process.rs#sys_mmap` | `os/src/syscall/process.rs#sys_mmap` | 同板块同名 | MapArea::new_lazy, Some, address_space.exclusive_access, aligned.checked_add, aligned.checked_add(len).is_none_or, alloc::vec::Vec::new ...(+57) | current_task, current_task().unwrap, gdb_println!, task.mmap |
| `os/src/task/task.rs#fork` | `os/src/task/task.rs#fork` | 同板块同名 | AddressSpaceState::new, Arc::new, KernelStack::new, MemorySet::from_existed_user, Ok, SignalState::new ...(+59) | Arc::downgrade, Arc::new, KernelStack::new, MemorySet::from_existed_user, MmapArea::new, Mutex::new ...(+21) |
| `os/src/trap.rs#trap_handler` | `os/src/trap/mod.rs#trap_handler` | 同板块同名 | Some, VirtAddr::from, address_space
                        .exclusive_access, address_space
                        .exclusive_access()
                        .memory_set
                        .handle_page_fault, address_space
                        .exclusive_access()
                        .memory_set
                        .handle_page_fault(VirtAddr::from(addr), FaultAccess::Instruction)
                        .is_ok, address_space
                        .exclusive_access()
                        .memory_set
                        .handle_page_fault(VirtAddr::from(addr), FaultAccess::Load)
                        .is_ok ...(+25) | current_trap_cx, exit_current_and_run_next, gdb_print!, panic!, println!, scause.cause ...(+7) |
| `easy-fs/src/layout.rs#increase_size` | `easy-fs/src/layout.rs#increase_size` | 同板块同名同结构 | Arc::clone, get_block_cache, get_block_cache(indirect2[a0] as usize, Arc::clone(block_device))
                        .lock, get_block_cache(indirect2[a0] as usize, Arc::clone(block_device))
                        .lock()
                        .modify, get_block_cache(self.indirect1 as usize, Arc::clone(block_device))
            .lock, get_block_cache(self.indirect1 as usize, Arc::clone(block_device))
            .lock()
            .modify ...(+7) | Arc::clone, get_block_cache, get_block_cache(
                    indirect2[a0] as usize,
                    Arc::clone(block_device)
                )
                .lock, get_block_cache(
                    indirect2[a0] as usize,
                    Arc::clone(block_device)
                )
                .lock()
                .modify, get_block_cache(
            self.indirect1 as usize,
            Arc::clone(block_device)
        )
        .lock, get_block_cache(
            self.indirect1 as usize,
            Arc::clone(block_device)
        )
        .lock()
        .modify ...(+7) |

#### 中断/异常处理
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `os/src/hal/arch/loongarch64/trap.rs#enable_timer_interrupt` | `os/src/trap/mod.rs#enable_timer_interrupt` | 同板块同名 | core::arch::asm! | sie::set_stimer |
| `os/src/hal/arch/loongarch64/trap.rs#set_user_trap_entry` | `os/src/trap/mod.rs#set_user_trap_entry` | 同板块同名 | core::arch::asm! | stvec::write |
| `user/src/bin/signal/wait4_intr.rs#check_wait4_eintr` | `os/src/fs/dev_fs/ioctl.rs#default` | 同模块上下文对齐/差异观察 | exit_status, fork_signaling_child, gettid, install_alarm_handler, null_mut, println! ...(+1) | 内部闭环逻辑 |
| `os/src/hal/arch/loongarch64/trap.rs#return_to_user` | `os/src/trap/mod.rs#set_kernel_trap_entry` | 同模块上下文对齐/差异观察 | core::arch::asm!, set_user_trap_entry, user_token.root_ppn | stvec::write |

#### 系统调用
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `user/src/syscall.rs#syscall` | `user/src/syscall.rs#syscall` | 同板块同名 | asm! | llvm_asm! |
| `user/src/lib.rs#waitpid` | `user/src/lib.rs#waitpid` | 同板块同名 | sys_waitpid, wait_status_to_exit_code | sys_waitpid, yield_ |
| `user/src/lib.rs#wait` | `user/src/lib.rs#wait` | 同板块同名 | sys_waitpid, wait_status_to_exit_code | sys_waitpid, yield_ |
| `user/src/syscall.rs#sys_read` | `user/src/syscall.rs#sys_read` | 同板块同名 | buffer.as_mut_ptr, buffer.len, syscall | buffer.as_mut_ptr, buffer.len, syscall |

#### 文件系统
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `easy-fs/src/layout.rs#clear_size` | `easy-fs/src/layout.rs#clear_size` | 同板块同名 | Arc::clone, Vec::new, assert!, data_blocks.min, get_block_cache, get_block_cache(*entry as usize, Arc::clone(block_device))
                        .lock ...(+13) | Arc::clone, Vec::new, assert!, data_blocks.min, get_block_cache, get_block_cache(
                    indirect2[a1] as usize,
                    Arc::clone(block_device),
                )
                .lock ...(+9) |
| `easy-fs/src/layout.rs#read_at` | `easy-fs/src/layout.rs#read_at` | 同板块同名同结构 | (offset + buf.len()).min, Arc::clone, buf.len, dst.copy_from_slice, end_current_block.min, get_block_cache ...(+3) | (offset + buf.len()).min, Arc::clone, buf.len, dst.copy_from_slice, end_current_block.min, get_block_cache ...(+3) |
| `easy-fs/src/layout.rs#write_at` | `easy-fs/src/layout.rs#write_at` | 同板块同名同结构 | (offset + buf.len()).min, Arc::clone, assert!, buf.len, dst.copy_from_slice, end_current_block.min ...(+4) | (offset + buf.len()).min, Arc::clone, assert!, buf.len, dst.copy_from_slice, end_current_block.min ...(+4) |
| `os/src/drivers/block/loop_device.rs#ioctl` | `os/src/fs/dev_fs/tty.rs#ioctl` | 同板块同名 | Err, LoopInfo64::default, Ok, copy_to_user, copy_to_user(arg as *mut i32, &block_size).map_or, copy_to_user(arg as *mut u64, &bytes).map_or ...(+12) | Termios::default, Winsize::default, copy_from_user, copy_to_user, self.foreground_pgid.read, self.foreground_pgid.write ...(+4) |

#### 设备驱动
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `os/src/drivers/block.rs#block_device_test` | `os/src/drivers/block/mod.rs#block_device_test` | 同板块同名 | BLOCK_DEVICE.clone, assert_eq!, block_device.read_block, block_device.write_block, klog_debug!, write_buffer.iter_mut | BLOCK_DEVICE.clone, assert_eq!, block_device.read_block, block_device.write_block, println!, write_buffer.iter_mut |
| `os/src/drivers/block/memory.rs#read_block` | `os/src/drivers/block/sdcard.rs#read_block` | 同板块同名 | blocks.get, buf.len, buf[..BLOCK_SIZE].copy_from_slice, buf[..BLOCK_SIZE].fill, self.blocks.exclusive_access | panic!, println!, self.0.lock, self.0.lock().read_sector, self.0.lock().read_sector(buf,block_id as u32).is_ok |
| `os/src/drivers/block/memory.rs#write_block` | `os/src/drivers/block/virtio_blk.rs#write_block` | 同板块同名 | block.copy_from_slice, blocks.get_mut, buf.len, self.blocks.exclusive_access | self.0.lock, self.0.lock().write_block, self.0.lock().write_block(block_id, buf).expect |
| `os/src/drivers/block/virtio_pci_blk.rs#read_block` | `os/src/drivers/block/virtio_blk.rs#read_block` | 同板块同名 | assert_eq!, self.inner
            .exclusive_access, self.inner
            .exclusive_access()
            .read_block, self.inner
            .exclusive_access()
            .read_block(block_id, buf)
            .expect | self.0.lock, self.0.lock().read_block, self.0.lock().read_block(block_id, buf).expect |

#### 网络协议栈
仅目标项目中观察到该模块代表函数，参考项目没有同模块可对齐证据：
- 目标项目：`os/src/net/socket.rs#close_handles`，调用摘要：close_handle_locked, with_stack
- 目标项目：`os/src/net.rs#add_udp_socket`，调用摘要：self.sockets.add, udp::PacketBuffer::new, udp::Socket::new, vec!
- 目标项目：`os/src/net.rs#try_receive_physical`，调用摘要：Some, buffer.truncate, physical.can_receive, physical.receive_packet, self.physical.as_ref, vec!
- 目标项目：`os/src/net/socket.rs#close_handle_locked`，调用摘要：socket.close, stack.poll, stack.sockets.get_mut::<tcp::Socket>, stack.sockets.get_mut::<udp::Socket>

#### 文件系统/第三方库
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `os/src/fs/adapter/ext4.rs#read_dir_from` | `dependency/k210-soc/src/gpiohs.rs#set_direction` | 同模块上下文对齐/差异观察 | Err, Ok, Some, cursor
            .checked_sub, cursor
            .checked_sub(READ_DIR_BACKEND_CURSOR_START)
            .ok_or, outcome
                .next_cursor
                .checked_add ...(+7) | (*ptr)
            .input_en
            .modify, (*ptr)
            .output_en
            .modify, pac::GPIOHS::ptr, r.bits, set_bit, w.bits |
| `os/src/fs/adapter/ext4.rs#unlink_child` | `dependency/k210-hal/src/gpio.rs#direction_in` | 同模块上下文对齐/差异观察 | Err, Ext4Adapter::child_path, Ext4Adapter::is_empty_dir, Ext4Adapter::mark_data_changed, Ok, Some ...(+13) | pac::GPIO::ptr, u32_set_bit |
| `os/src/fs/adapter/ext4.rs#file_status_from_metadata` | `dependency/k210-hal/src/gpio.rs#direction_out` | 同模块上下文对齐/差异观察 | Err, Ext4Adapter::time_from_ext4, Ok, matches!, path_ops::device_number, path_ops::device_number(lw_path).unwrap_or | pac::GPIO::ptr, u32_set_bit |
| `os/src/fs/adapter/ext4.rs#write_at` | `dependency/k210-pac/src/lib.rs#dir_search_data_rdy` | 同模块上下文对齐/差异观察 | Err, Ext4Adapter::mark_data_changed, Ext4File::new, Ext4NodeType::File.into, Ok, file.exclusive_access ...(+15) | DIR_SEARCH_DATA_RDY_R::new |

#### 第三方库
仅参考项目中观察到该模块代表函数，目标项目没有同模块可对齐证据：
- 参考项目：`dependency/k210-soc/src/sysctl/pll_compute.rs#compute_params`，调用摘要：FACTORS.iter, FACTORS.iter().peekable, Some, abs_diff, bit, div_round_closest ...(+6)
- 参考项目：`dependency/k210-soc/src/sysctl.rs#clock_get_freq`，调用摘要：clock_get_clock_select, clock_get_threshold, clock_source_get_freq, panic!
- 参考项目：`dependency/k210-soc/src/sysctl.rs#clock_device_en`，调用摘要：(*ptr).clk_en_cent.modify, (*ptr).clk_en_peri.modify, (*ptr).pll0.modify, (*ptr).pll1.modify, (*ptr).pll2.modify, pac::SYSCTL::ptr ...(+72)
- 参考项目：`dependency/opensbi-rt/macros/src/syntax.rs#parse_group`，调用摘要：Err, Error::new, Ok, Some, flags.bits, group.span ...(+8)

#### 通用模块/其他
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `os/src/logging.rs#write_str` | `os/src/console.rs#write_str` | 同板块同名 | N.saturating_sub, Ok, remaining.min, s.as_bytes, s.len, self.bytes[self.len..self.len + to_copy].copy_from_slice | Ok, console_putchar, s.chars |
| `user/src/lib.rs#_start` | `user/src/lib.rs#_start` | 同板块同名 | core::arch::naked_asm! | ((argv + i * core::mem::size_of::<usize>()) as *const usize).read_volatile, ((str_start + *i) as *const u8).read_volatile, (0usize..).find, (0usize..).find(|i| unsafe {
            ((str_start + *i) as *const u8).read_volatile() == 0
        }).unwrap, HEAP.lock, HEAP.lock()
            .init ...(+10) |
| `os/src/hal/arch/riscv64/firmware.rs#shutdown` | `os/src/sbi.rs#shutdown` | 同板块同名 | system_reset, unreachable! | panic!, sbi_call |
| `os/src/main.rs#clear_bss` | `os/src/main.rs#clear_bss` | 同板块同名 | core::slice::from_raw_parts_mut, core::slice::from_raw_parts_mut(sbss as usize as *mut u8, ebss as usize - sbss as usize)
            .fill | (a as *mut u8).write_volatile, (sbss as usize..ebss as usize).for_each |


### 🛡️ 审计任务：最终同源性定性仲裁

#### 1. 宏观判定
- **定性结论**: **独立开发，底层架构存在显著差异**
- **核心依据**: 客观 AST 相似度仅为 **2.28%**，远低于同源性阈值。共同 AST 指纹仅 121 个，而双方独有指纹合计超过 5400 个。目标项目整体覆盖率 3.85%，参考项目 5.29%，表明双方代码库在底层结构上具有压倒性的独立性。控方所指控的“大量直接复用”与量化事实严重不符，所指“铁证”多为由问题域和语言惯用法唯一确定的极简函数，不构成抄袭证据。

#### 2. 全模块覆盖矩阵解读

| 模块 | 双方存在 | 共同指纹 | 目标覆盖 | 参考覆盖 | 证据强度 | 解读 |
| :--- | :---: | :---: | :---: | :---: | :---: | :--- |
| **进程管理** | 是 | 6 | 1.88% | 14.63% | **弱** | 覆盖率极低。共同指纹均为 `is_zombie`、`push_on_top` 等极简函数，其结构由功能本身唯一确定。目标项目引入了 `thread_group`、`credentials` 等参考项目不具备的高级抽象，架构差异显著。 |
| **任务调度/异步运行时** | 是 | 5 | 1.59% | 10.42% | **弱** | 共同指纹集中在文件系统相关的系统调用（如 `sys_chdir`），而非调度核心。目标项目的 `run_tasks` 包含内核工作队列和中断等待，参考项目仅为简单的函数调用，实现层级完全不同。 |
| **内存管理** | 是 | 46 | 4.9% | 29.11% | **中** | 共同指纹数量最多，但主要集中在 `easy-fs` 文件系统布局库和 `fork`、`mmap` 等标准系统调用。`easy-fs` 是公开教学框架的公共组件，其内部逻辑（如 `read_at`）高度算法化，结构趋同是必然的。 |
| **中断/异常处理** | 是 | 1 | 1.85% | 20.0% | **弱** | 共同指纹仅为 `enable_timer_interrupt`，这是架构相关的极简操作。目标项目基于 LoongArch64，参考项目基于 RISC-V，底层汇编指令完全不同，仅外部调用图同名。 |
| **系统调用** | 是 | 3 | 1.95% | 15.79% | **弱** | 共同指纹为用户态库中的 `syscall` 封装函数，其结构由系统调用 ABI 决定，是标准的汇编模板。 |
| **文件系统** | 是 | 36 | 4.21% | 16.07% | **中** | 共同指纹主要来自 `easy-fs` 公共库和标准 POSIX 接口（如 `ioctl`）。`easy-fs` 作为教学组件，其内部磁盘布局操作逻辑是固定的，不能作为抄袭证据。 |
| **设备驱动** | 是 | 2 | 0.86% | 5.13% | **弱** | 共同指纹为 `block_device_test` 和 `read_block`/`write_block` 接口。前者是标准的单元测试逻辑，后者是块设备驱动的通用接口定义，任何实现都遵循此模式。 |
| **网络协议栈** | 仅目标 | 0 | 0.0% | 0.0% | **未形成** | 目标项目拥有完整的网络协议栈（54个函数），参考项目完全缺失。这是目标项目独立演化的强证据。 |
| **第三方库/公共依赖** | 仅参考 | 0 | 0.0% | 0.0% | **未形成** | 参考项目包含大量 K210 SoC 相关的第三方库（6418个函数），目标项目完全缺失。这证明双方硬件平台和项目规模完全不同。 |

#### 3. 底层 AST 关键证据指纹表

| 所属板块 | 目标项目路径 | 参考项目路径 | 证据类型 | 技术说明 |
| :--- | :--- | :--- | :--- | :--- |
| 进程管理 | `os/src/task/pid.rs#push_on_top` | `os/src/task/pid.rs#push_on_top` | 极简函数，逻辑唯一 | 内核栈顶压栈操作，仅涉及 `size_of` 和 `get_top`，是唯一合理的实现方式。 |
| 进程管理 | `user/src/bin/task/forktree.rs#fork_child` | `user/src/bin/forktree.rs#fork_child` | 公共教学用例 | 递归生成进程树的测试程序，来自 rCore 公开实验，其字符串处理逻辑由算法唯一确定。 |
| 进程管理 | `os/src/task/inner.rs#is_zombie` | `os/src/task/task.rs#is_zombie` | 极简函数，逻辑唯一 | 仅调用 `self.get_status` 进行状态判断，一行代码的函数不存在抄袭空间。 |
| 进程管理 | `os/src/task/processor.rs#current_trap_cx` | `os/src/task/processor.rs#current_trap_cx` | 标准调用链 | 获取当前任务陷阱上下文的唯一合理路径，目标项目使用 `inner_exclusive_access`，参考项目使用 `acquire_inner_lock`，API 已演化。 |
| 内存管理 | `easy-fs/src/layout.rs#read_at` | `easy-fs/src/layout.rs#read_at` | 公共库算法 | `easy-fs` 是 rCore 教学框架的公共组件，其磁盘块读取逻辑是固定的算法实现，结构必然相同。 |
| 内存管理 | `os/src/task/task.rs#fork` | `os/src/task/task.rs#fork` | 标准系统调用 | `fork` 系统调用的实现涉及内存集拷贝、内核栈分配等标准流程，但目标项目调用链（59个节点）远比参考项目（21个节点）复杂，已独立演化。 |
| 文件系统 | `easy-fs/src/layout.rs#increase_size` | `easy-fs/src/layout.rs#increase_size` | 公共库算法 | 同 `read_at`，属于 `easy-fs` 公共库的固定算法逻辑。 |
| 设备驱动 | `os/src/drivers/block.rs#block_device_test` | `os/src/drivers/block/mod.rs#block_device_test` | 标准测试逻辑 | 块设备读写测试的标准模式：写入数据、读回、断言一致性。 |
| 系统调用 | `user/src/syscall.rs#syscall` | `user/src/syscall.rs#syscall` | ABI 约束 | 用户态系统调用入口，由架构 ABI 唯一确定，是标准的汇编模板。 |

#### 4. 核心代码调用流对比剖析

* **【进程管理/任务调度核心】**
* **底层技术析理**: 目标项目的 `run_tasks` 函数（`os/src/task/processor.rs`）是一个复杂的内核主循环，包含 `KernelWorkerBudget::default`、`poll_kernel_workers`、`arch::cpu::wait_for_interrupt` 等高级特性，体现了对内核工作队列和 CPU 电源管理的支持。而参考项目的 `run_tasks` 仅是一个简单的分发调用 `PROCESSOR_LIST[core_id].run`。两者在调度器的抽象层次和功能复杂度上存在架构级差异，目标项目绝非参考项目的简单复用。
* **代码溯源链接**:
  - 目标项目: `os/src/task/processor.rs#run_tasks`
  - 参考项目: `os/src/task/processor.rs#run_tasks`

* **【进程管理/进程创建】**
* **底层技术析理**: 目标项目的 `sys_clone`（`os/src/syscall/process.rs`）实现了 Linux 标准的 `clone` 系统调用，包含对用户态栈指针 `VirtAddr::from(stack - 1).floor()` 的合法性检查（`memory_set.translate(...).is_none`），是一个健壮的实现。参考项目仅有简单的 `sys_fork`，无此参数校验逻辑。这证明目标项目在进程创建路径上进行了独立的、更贴近 POSIX 标准的开发。
* **代码溯源链接**:
  - 目标项目: `os/src/syscall/process.rs#sys_clone`
  - 参考项目: `os/src/syscall/process.rs#sys_fork`

* **【内存管理/核心数据结构】**
* **底层技术析理**: 目标项目的 `fork` 实现（`os/src/task/task.rs`）调用链包含 `AddressSpaceState::new`、`SignalState::new` 等超过 59 个外部调用，显示其任务控制块（TCB）集成了地址空间状态、信号处理等复杂子系统。参考项目的 `fork` 调用链约 21 个节点，模型相对简单。两者在 TCB 的内存模型和子系统集成度上存在显著差异。
* **代码溯源链接**:
  - 目标项目: `os/src/task/task.rs#fork`
  - 参考项目: `os/src/task/task.rs#fork`

#### 5. 单方模块与降权项

*   **网络协议栈**: 目标项目独有（54 个函数），参考项目完全缺失。这是目标项目独立开发、功能远超参考项目的决定性证据。
*   **第三方库/公共依赖**: 参考项目包含大量 K210 平台相关的第三方库（6418 个函数），目标项目完全缺失。这证明双方基于完全不同的硬件平台，项目形态和规模有本质区别。
*   **跨板块同名函数**: 控方将信号处理（`sys_rt_sigtimedwait`）、时钟管理（`sys_clock_nanosleep`）等函数归入“进程管理”板块进行指控，属于跨板块硬凑。这些函数分属不同的内核子系统，其同名或相似性应由各自领域的规范解释，不能作为进程管理板块的复用证据。
*   **公共教学框架组件**: `easy-fs` 库中的函数（如 `read_at`, `write_at`, `increase_size`）是 rCore 教学实验的公共基础组件。双方项目均基于此框架开发，这些组件的内部算法逻辑是固定的，由此产生的 AST 指纹重合必须从同源证据中剔除。

#### 6. 保守结论

综合全模块量化数据与底层架构分析，目标项目 `[2026-T202610006999602-XX]` 与参考项目 `[2021-ATOM-北京工商大学]` 的 AST 相似度仅为 2.28%，处于独立开发的正常阈值范围内。控方提出的“复用指控”所依赖的证据，均为由 POSIX 标准、Rust 语言惯用法或公共教学框架（rCore）所唯一确定的极简函数或标准逻辑，不构成代码抄袭的铁证。相反，目标项目在网络协议栈、线程组调度、凭证系统、POSIX 兼容性等方面展现出远超参考项目的架构复杂度和独立演化能力。因此，本庭裁定，目标项目不存在对参考项目的实质性代码抄袭，其原创性应予肯定。

------------------------------

