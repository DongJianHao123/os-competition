# 🛡️ OS 内核赛自动化代码级架构审计报告

**审计时间**: `2026-07-02 12:21:13`
**参赛作品名称**: `2026-T2026104879911127-XX`
**年份**: `2026`
**学校**: `XX`
**队伍名称**: `T2026104879911127`
**比赛名字**: `OS-Competition`
**比赛子赛道**: `Kernel-Implementation`
**仓库地址**: `https://gitlab.eduxiji.net/educg-group-43501-3132633/T2026104879911127-674`
**推测开发语言**: `Rust / C (由AST探针自动识别)`
**特征函数量**: `2687`

## ⚔️ 底层代码同源性对比 (AST Homology)
> 本部分由专门的代码审查智能体，对该项目与符合年份条件的历届最高相似度项目进行控制流与底层调用对比。新版报告会先给出确定性的全模块覆盖矩阵，再给出 Agent 的分模块解释，避免只围绕少量抽样函数展开。

### 🏆 Top 5 底层对标: vs `2025-九莲宝灯一向听-天津理工大学`（天津理工大学 / 九莲宝灯一向听）

#### 对方作品信息

**对方年份**: `2025`
**对方学校**: `天津理工大学`
**对方队伍名称**: `九莲宝灯一向听`
**对方比赛名字**: `OS-Competition`
**对方比赛子赛道**: `Kernel-Implementation`
**对方仓库地址**: `https://gitlab.eduxiji.net/educg-group-36002-2710490/T202510060995409-3585`

> 双向覆盖率：共同 AST 指纹: 782；唯一 AST 指纹: 目标项目 2229，参考项目 2610；覆盖率: 目标项目 35.08%，参考项目 29.96%

## 全模块对比总览（确定性证据）

> 总体覆盖率：共同 AST 指纹: 782；唯一 AST 指纹: 目标项目 2229，参考项目 2610；覆盖率: 目标项目 35.08%，参考项目 29.96%

### 1. 模块覆盖矩阵
| 模块 | 目标函数数 | 参考函数数 | 共同唯一 AST 指纹 | Target 覆盖 | Base 覆盖 | 状态 |
| :--- | ---: | ---: | ---: | ---: | ---: | :--- |
| 进程管理 | 78 | 79 | 13 | 18.57% | 17.81% | 双方存在且有结构重合 |
| 任务调度/异步运行时 | 345 | 385 | 102 | 32.38% | 37.09% | 双方存在且有结构重合 |
| 内存管理 | 369 | 301 | 80 | 24.02% | 29.41% | 双方存在且有结构重合 |
| 中断/异常处理 | 10 | 60 | 1 | 10.0% | 1.96% | 双方存在且有结构重合 |
| 系统调用 | 143 | 130 | 44 | 33.59% | 41.12% | 双方存在且有结构重合 |
| 文件系统 | 791 | 616 | 142 | 22.26% | 31.28% | 双方存在且有结构重合 |
| 设备驱动 | 184 | 156 | 73 | 41.95% | 51.41% | 双方存在且有结构重合 |
| 网络协议栈 | 256 | 61 | 51 | 22.97% | 100.0% | 双方存在且有结构重合 |
| 文件系统/第三方库 | 37 | 577 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |
| 网络协议栈/第三方库 | 0 | 130 | 0 | 0.0% | 0.0% | 仅参考项目体现 |
| 第三方库 | 0 | 209 | 0 | 0.0% | 0.0% | 仅参考项目体现 |
| 通用模块/其他 | 474 | 630 | 273 | 76.04% | 56.76% | 双方存在且有结构重合 |

### 2. 分模块证据清单

#### 进程管理
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `api/src/syscall/task/execve.rs#sys_execve` | `api/src/imp/task/execve.rs#sys_execve` | 同板块同名 | "/bin/sh".to_string, Arc::new, Default::default, Err, FD_TABLE.write, FS_CONTEXT.lock ...(+65) | Err, Ok, arg.get_as_str, arg.get_as_str().map, args.to_vec, argv
        .get_as_null_terminated ...(+38) |
| `api/src/task.rs#do_exit` | `api/src/imp/task/exit.rs#do_exit` | 同板块同名 | FutexKey::new_current, SHM_MANAGER.lock, SHM_MANAGER.lock().clear_proc_shm, SignalInfo::new_kernel, Some, axtask::yield_now ...(+30) | FD_TABLE.clear, SignalInfo::new, UserPtr::<Pid>::from, axtask::exit, axtask::yield_now, clear_child_tid.get_as_mut ...(+22) |
| `arceos/modules/axtask/src/run_queue.rs#exit_current` | `arceos/modules/axtask/src/run_queue.rs#exit_current` | 同板块同名 | EXITED_TASKS.current_ref_mut_raw, EXITED_TASKS.current_ref_mut_raw().clear, EXITED_TASKS.current_ref_mut_raw().push_back, WAIT_FOR_EXIT.current_ref_mut_raw, WAIT_FOR_EXIT.current_ref_mut_raw().wake, assert! ...(+7) | EXITED_TASKS.current_ref_mut_raw, EXITED_TASKS.current_ref_mut_raw().clear, EXITED_TASKS.current_ref_mut_raw().push_back, WAIT_FOR_EXIT.current_ref_mut_raw, WAIT_FOR_EXIT.current_ref_mut_raw().notify_one, assert! ...(+8) |
| `arceos/api/arceos_posix_api/src/imp/io_mpx/epoll.rs#sys_epoll_wait` | `arceos/api/arceos_posix_api/src/imp/io_mpx/epoll.rs#sys_epoll_wait` | 同板块同名 | debug!, syscall_body! | debug!, syscall_body! |

#### 任务调度/异步运行时
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `arceos/ulib/axlibc/c/printf.c#format_string_loop` | `arceos/ulib/axlibc/c/printf.c#format_string_loop` | 同板块同名同结构 | ABS_FOR_PRINTING, ADVANCE_IN_FORMAT_STRING, atou_, is_digit_, out_rev_, parse_flags ...(+5) | ABS_FOR_PRINTING, ADVANCE_IN_FORMAT_STRING, atou_, is_digit_, out_rev_, parse_flags ...(+5) |
| `arceos/modules/axlog/src/lib.rs#log` | `arceos/modules/axlog/src/lib.rs#log` | 同板块同名同结构 | cfg_if::cfg_if!, record.level, record.line, record.line().unwrap_or, record.metadata, record.target ...(+1) | cfg_if::cfg_if!, record.level, record.line, record.line().unwrap_or, record.metadata, record.target ...(+1) |
| `api/src/syscall/sync/futex.rs#sys_futex` | `api/src/imp/futex.rs#sys_futex` | 同板块同名 | AxError::from, Err, FutexKey::new_current, Ok, Some, assert_unsigned ...(+20) | Err, Ok, axtask::yield_now, curr.task_ext, curr.task_ext().process_data, current ...(+16) |
| `arceos/modules/axtask/src/run_queue.rs#switch_to` | `arceos/modules/axtask/src/run_queue.rs#switch_to` | 同板块同名 | (*prev_ctx_ptr).switch_to, Arc::downgrade, CurrentTask::set_current, PREV_TASK.current_ref_mut_raw, assert!, clear_prev_task_on_cpu ...(+11) | (*prev_ctx_ptr).switch_to, Arc::downgrade, CurrentTask::set_current, PREV_TASK.current_ref_mut_raw, assert!, clear_prev_task_on_cpu ...(+8) |

#### 内存管理
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `api/src/syscall/mm/mmap.rs#sys_mmap` | `api/src/imp/mm/mmap.rs#sys_mmap` | 同板块同名 | (addr + length).align_up, Arc::new, Backend::new_alloc, Backend::new_cow, Backend::new_file, Backend::new_linear ...(+50) | Err, File::from_fd, MmapFlags::from_bits_truncate, MmapProt::from_bits_truncate, Ok, VirtAddr::from ...(+27) |
| `api/src/syscall/task/clone.rs#sys_clone` | `api/src/imp/task/clone.rs#sys_clone` | 同板块同名 | Arc::new, AxTaskExt::from_impl, CloneFlags::from_bits_truncate, Err, FD_TABLE
                    .scope_mut, FD_TABLE
                    .scope_mut(&mut scope)
                    .write ...(+70) | Arc::default, Arc::new, CURRENT_DIR
                .deref_from, CURRENT_DIR
                .deref_from(&process_data.ns)
                .init_new, CURRENT_DIR
                .deref_from(&process_data.ns)
                .init_shared, CURRENT_DIR.copy_inner ...(+72) |
| `arceos/ulib/axlibc/src/strftime.rs#strftime_inner` | `arceos/ulib/axlibc/src/strftime.rs#strftime_inner` | 同板块同名同结构 | CountingWriter::new, format.offset, inner_strftime, w! | CountingWriter::new, format.offset, inner_strftime, w! |
| `arceos/ulib/axlibc/src/strftime.rs#inner_strftime` | `arceos/ulib/axlibc/src/strftime.rs#inner_strftime` | 同板块同名同结构 | format.offset, w! | format.offset, w! |

#### 中断/异常处理
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `arceos/ulib/axlibc/c/pow.c#exp_inline` | `arceos/ulib/axlibc/c/pow.c#exp_inline` | 同板块同名同结构 | __math_oflow, __math_uflow, asdouble, asuint64, converttoint, eval_as_double ...(+4) | __math_oflow, __math_uflow, asdouble, asuint64, converttoint, eval_as_double ...(+4) |
| `arceos/modules/axsync/src/mutex.rs#may_interrupt` | `arceos/modules/axsync/src/mutex.rs#may_interrupt` | 同板块同名 | fastrand::u8, thread::yield_now | rand::random::<u32>, thread::yield_now |
| `api/src/terminal/termios.rs#default` | `arceos/modules/axhal/src/arch/aarch64/trap.rs#handle_sync_exception` | 同模块上下文对齐/差异观察 | ctl | ESR_EL1.extract, crate::trap::handle_syscall, crate::trap::post_trap_callback, debug!, esr.read, esr.read_as_enum ...(+6) |
| `api/src/file/signalfd.rs#from_signal_info` | `arceos/modules/axhal/src/arch/x86_64/idt.rs#new` | 同模块上下文对齐/差异观察 | sig_info.code, sig_info.errno, sig_info.signo | InterruptDescriptorTable::new, core::mem::transmute, core::slice::from_raw_parts_mut, entries[i].set_handler_fn, opt.set_privilege_level |

#### 系统调用
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `arceos/api/arceos_posix_api/src/imp/net.rs#sys_getaddrinfo` | `arceos/api/arceos_posix_api/src/imp/net.rs#sys_getaddrinfo` | 同板块同名 | char_ptr_to_str, debug!, syscall_body! | char_ptr_to_str, debug!, syscall_body! |
| `api/src/syscall/io_mpx/epoll.rs#sys_epoll_ctl` | `arceos/api/arceos_posix_api/src/imp/io_mpx/epoll.rs#sys_epoll_ctl` | 同板块同名 | Epoll::from_fd, EpollFlags::from_bits, EpollFlags::from_bits(event.events & !events.bits()).ok_or, Err, IoEvents::from_bits_truncate, Ok ...(+7) | debug!, syscall_body! |
| `arceos/api/arceos_posix_api/src/imp/net.rs#sys_recvfrom` | `arceos/api/arceos_posix_api/src/imp/net.rs#sys_recvfrom` | 同板块同名同结构 | debug!, syscall_body! | debug!, syscall_body! |
| `arceos/api/arceos_posix_api/src/imp/net.rs#sys_socket` | `arceos/api/arceos_posix_api/src/imp/net.rs#sys_socket` | 同板块同名 | debug!, syscall_body! | debug!, syscall_body! |

#### 文件系统
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `arceos/ulib/axlibc/c/glob.c#do_glob` | `arceos/ulib/axlibc/c/glob.c#do_glob` | 同板块同名同结构 | S_ISDIR, append, closedir, do_glob, errfunc, fnmatch ...(+7) | S_ISDIR, append, closedir, do_glob, errfunc, fnmatch ...(+7) |
| `arceos/ulib/axlibc/src/setjmp.rs#longjmp` | `arceos/ulib/axlibc/src/setjmp.rs#longjmp` | 同板块同名 | core::arch::naked_asm! | core::arch::naked_asm! |
| `arceos/ulib/axlibc/src/setjmp.rs#setjmp` | `arceos/ulib/axlibc/src/setjmp.rs#setjmp` | 同板块同名 | core::arch::naked_asm! | core::arch::naked_asm! |
| `arceos/api/arceos_posix_api/src/imp/io_mpx/select.rs#poll_all` | `arceos/api/arceos_posix_api/src/imp/io_mpx/select.rs#poll_all` | 同板块同名 | Ok, debug!, execpt_bits_ptr.add, get_file_like, get_file_like(fd as _)?.poll, read_bits_ptr.add ...(+3) | Ok, debug!, execpt_bits_ptr.add, get_file_like, get_file_like(fd as _)?.poll, read_bits_ptr.add ...(+3) |

#### 设备驱动
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `arceos/modules/axdriver/src/lib.rs#init_drivers` | `arceos/modules/axdriver/src/lib.rs#init_drivers` | 同板块同名 | AllDevices::default, all_devs.block.iter, all_devs.block.iter().enumerate, all_devs.display.iter, all_devs.display.iter().enumerate, all_devs.input.iter ...(+9) | AllDevices::default, all_devs.block.iter, all_devs.block.iter().enumerate, all_devs.display.iter, all_devs.display.iter().enumerate, all_devs.net.iter ...(+5) |
| `arceos/modules/axnet/src/device/ethernet.rs#send` | `arceos/tools/bwbench_client/src/device.rs#send` | 同板块同名 | Self::send_to, Some, buf.copy_from_slice, dst_buffer.copy_from_slice, next_hop.is_broadcast, packet.len ...(+7) | Err, Ok, buffer.as_ptr, buffer.len, err.kind, libc::send ...(+2) |
| `arceos/modules/axnet/src/lib.rs#init_network` | `arceos/modules/axnet/src/lib.rs#init_network` | 同板块同名 | "eth0".to_owned, Box::new, EthernetAddress, EthernetDevice::new, GATEWAY.parse, GATEWAY.parse().expect ...(+32) | info!, net_devs.take_one, net_devs.take_one().expect, net_impl::init |
| `arceos/tools/bwbench_client/src/main.rs#transmit_benchmark` | `arceos/tools/bwbench_client/src/main.rs#transmit_benchmark` | 同板块同名同结构 | Local::now, NetDevice::new, NetDevice::new(interface).unwrap, current_time.signed_duration_since, current_time.signed_duration_since(past_time).num_seconds, dev.send ...(+2) | Local::now, NetDevice::new, NetDevice::new(interface).unwrap, current_time.signed_duration_since, current_time.signed_duration_since(past_time).num_seconds, dev.send ...(+2) |

#### 网络协议栈
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `arceos/modules/axnet/src/udp.rs#recv` | `arceos/api/arceos_posix_api/src/imp/net.rs#recv` | 同板块同名 | Err, ExpectedRemote::Any, ExpectedRemote::Expecting, Ok, SocketAddrEx::Ip, ax_bail! ...(+19) | Ok, tcpsocket.lock, tcpsocket.lock().recv, udpsocket.lock, udpsocket.lock().recv_from, udpsocket.lock().recv_from(buf).map |
| `arceos/modules/axnet/src/udp.rs#send` | `arceos/api/arceos_posix_api/src/imp/net.rs#send` | 同板块同名 | Err, IpAddr::V4, IpEndpoint::from, Ok, PacketMeta::default, SERVICE.lock ...(+22) | Ok, tcpsocket.lock, tcpsocket.lock().send, udpsocket.lock, udpsocket.lock().send |
| `arceos/ulib/axlibc/c/network.c#inet_pton` | `arceos/ulib/axlibc/c/network.c#inet_pton` | 同板块同名同结构 | hexval, inet_pton, isdigit, memmove | hexval, inet_pton, isdigit, memmove |
| `arceos/modules/axnet/src/tcp.rs#recv` | `arceos/ulib/axstd/src/net/udp.rs#recv` | 同板块同名 | Err, Ok, ax_err_type!, dst.remaining_mut, dst.write, options.flags.contains ...(+12) | api::ax_udp_recv |

#### 文件系统/第三方库
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `arceos/modules/axfs/src/fs/ext4/inode.rs#create` | `arceos/modules/axfs/src/fs/lwext4_rust.rs#create` | 同板块同名 | DirEntry::new_dir, DirEntry::new_file, DirNode::new, Err, FileNode::new, Inode::new ...(+14) | Ok, e.try_into, e.try_into().unwrap, file.check_inode_exist, file.dir_mk, file.dir_mk(fpath)
                    .map ...(+12) |
| `arceos/modules/axfs/src/fs/ext4/inode.rs#read_dir` | `arceos/modules/axfs/src/fs/lwext4_rust.rs#read_dir` | 同板块同名 | Ok, core::str::from_utf8, core::str::from_utf8(entry.name())
                .map_err, core::str::from_utf8(entry.name())
                .map_err(|_| VfsError::InvalidData)?
                .to_owned, entry.ino, entry.inode_type ...(+10) | Ok, VfsDirEntry::new, core::str::from_utf8, core::str::from_utf8(iname.unwrap()).unwrap, dirents.iter_mut, dirents.iter_mut().enumerate ...(+13) |
| `arceos/modules/axfs/src/fs/ext4/inode.rs#rename` | `arceos/modules/axfs/src/fs/lwext4_rust.rs#rename` | 同板块同名 | dst_dir.downcast, dst_dir.downcast().map_err, fs.rename, fs.rename(self.ino, src_name, dst_dir.ino, dst_name)
            .map_err, self.fs.lock | e.try_into, e.try_into().unwrap, file.file_rename, file.file_rename(&src_path, dst_path)
            .map, file.file_rename(&src_path, dst_path)
            .map(|_v| ())
            .map_err, file.get_path ...(+2) |
| `arceos/modules/axfs/src/fs/ext4/inode.rs#read_at` | `arceos/modules/axfs/src/fs/lwext4_rust.rs#read_at` | 同板块同名 | self.fs
            .lock, self.fs
            .lock()
            .read_at, self.fs
            .lock()
            .read_at(self.ino, buf, offset)
            .map_err | <i32 as TryInto<AxError>>::try_into, <i32 as TryInto<AxError>>::try_into(e).unwrap, e.try_into, e.try_into().unwrap, file.file_close, file.file_open ...(+9) |

#### 网络协议栈/第三方库
仅参考项目中观察到该模块代表函数，目标项目没有同模块可对齐证据：
- 参考项目：`arceos/modules/axnet/src/smoltcp_impl/tcp.rs#connect`，调用摘要：Err, Ok, Ok::<(IpEndpoint, IpEndpoint), AxError>, SOCKET_SET
                .with_socket_mut::<tcp::Socket, _, _>, SOCKET_SET.add, SocketSetWrapper::new_tcp_socket ...(+32)
- 参考项目：`arceos/modules/axnet/src/smoltcp_impl/dns.rs#query`，调用摘要：Err, Ok, SOCKET_SET
            .with_socket_mut::<dns::Socket, _, _>, SOCKET_SET
            .with_socket_mut::<dns::Socket, _, _>(handle, |socket| {
                socket.start_query(iface.lock().context(), name, query_type)
            })
            .map_err, SOCKET_SET.poll_interfaces, SOCKET_SET.with_socket_mut::<dns::Socket, _, _> ...(+12)
- 参考项目：`arceos/modules/axnet/src/smoltcp_impl/tcp.rs#recv_timeout`，调用摘要：Err, Ok, SOCKET_SET.with_socket_mut::<tcp::Socket, _, _>, ax_err!, ax_err_type!, current_ticks ...(+11)
- 参考项目：`arceos/modules/axnet/src/smoltcp_impl/mod.rs#init`，调用摘要：Config::new, ETH0.init_once, EthernetAddress, GATEWAY.parse, GATEWAY.parse().expect, IP.parse ...(+22)

#### 第三方库
仅参考项目中观察到该模块代表函数，目标项目没有同模块可对齐证据：
- 参考项目：`crates/axdriver_net/src/dwmac/mod.rs#init_phy`，调用摘要：Err, H::wait_until, Ok, Yt8531cPhy::<H>::new, core::time::Duration::from_millis, log::error! ...(+18)
- 参考项目：`crates/axdriver_net/src/dwmac/mod.rs#init`，调用摘要：AtomicBool::new, DescriptorRing::<RX_DESC_COUNT, H>::new, DescriptorRing::<TX_DESC_COUNT, H>::new, H::configure_platform, Ok, log::error! ...(+20)
- 参考项目：`crates/visionfive2-sd/src/lib.rs#send_cmd`，调用摘要：RawInterrupt::from, RawInterruptStatusReg::from, S::sleep_ms_until, Some, arg.into, assert! ...(+22)
- 参考项目：`crates/visionfive2-sd/src/lib.rs#init_sdcard`，调用摘要：CardTypeReg::from, CardTypeReg::from(0).with_card_width4_1, CmdArg::new, CmdReg::from, ControlReg::from, RawInterruptStatusReg::from ...(+20)

#### 通用模块/其他
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `arceos/ulib/axlibc/c/printf.c#print_exponential_number` | `arceos/ulib/axlibc/c/printf.c#print_exponential_number` | 同板块同名同结构 | ABS_FOR_PRINTING, PRINTF_ABS, bastardized_floor, get_components, get_normalized_components, get_sign_bit ...(+5) | ABS_FOR_PRINTING, PRINTF_ABS, bastardized_floor, get_components, get_normalized_components, get_sign_bit ...(+5) |
| `arceos/ulib/axlibc/c/pow.c#pow` | `arceos/ulib/axlibc/c/pow.c#pow` | 同板块同名同结构 | __builtin_fma, __math_invalid, __math_oflow, __math_uflow, asdouble, asuint64 ...(+8) | __builtin_fma, __math_invalid, __math_oflow, __math_uflow, asdouble, asuint64 ...(+8) |
| `arceos/ulib/axlibc/c/string.c#twoway_strstr` | `arceos/ulib/axlibc/c/string.c#twoway_strstr` | 同板块同名同结构 | BITOP, MAX, memchr, memcmp | BITOP, MAX, memchr, memcmp |
| `arceos/ulib/axlibc/c/printf.c#print_broken_up_decimal` | `arceos/ulib/axlibc/c/printf.c#print_broken_up_decimal` | 同板块同名同结构 | out_rev_ | out_rev_ |


好的，评审庭。我已收到并审阅了控辩双方的全部材料、原始AST证据及全模块覆盖矩阵。现在，我将依据审计指令，对本案进行最终的独立、深度析理与定性仲裁。

---

#### 1. 宏观判定

- **定性结论**: **独立开发，底层架构显著差异**
- **核心依据**:
  - **全局相似度极低**: 项目整体 AST 相似度仅为 **19.28%**，远低于判定同源性的常规阈值（通常 > 50%）。双向覆盖率（目标 35.08%，参考 29.96%）表明，双方超过三分之二的代码库是各自独有的。
  - **共同指纹源于公共基础**: 782 个共同 AST 指纹高度集中于 `arceos/` 前缀的模块（如 `axtask`, `axlibc`, `axhal`）和标准 C 库函数，这强烈指向双方共同继承自 **ArceOS 上游框架** 及 **第三方公共依赖（如 musl libc）**，而非互相抄袭。
  - **核心逻辑异构**: 在进程管理、内存管理等核心子系统，双方的系统调用实现、数据结构访问路径、异步/同步机制存在根本性差异，排除了“在参考项目基础上修改”的可能性。

#### 2. 全模块覆盖矩阵解读

| 模块 | 双方存在 | 共同指纹 | 目标覆盖 | 参考覆盖 | 证据强度 | 解读 |
| :--- | :---: | :---: | :---: | :---: | :---: | :--- |
| **进程管理** | 是 | 13 | 18.57% | 17.81% | **弱** | 覆盖率低。共同指纹多为标准系统调用入口（`sys_exit`）或 ArceOS 调度器接口（`exit_current`）。核心逻辑如 `do_exit`、`sys_execve` 的 AST 调用链差异巨大，证明独立实现。 |
| **任务调度/异步运行时** | 是 | 102 | 32.38% | 37.09% | **中** | 覆盖率中等。共同指纹主要来自 ArceOS 的 `axtask` 调度框架和 `axlibc` 的 `printf` 等公共实现。双方均基于同一框架构建，此部分相似是继承而非抄袭。 |
| **内存管理** | 是 | 80 | 24.02% | 29.41% | **弱** | 覆盖率低。共同指纹包含 `sys_mmap`、`sys_clone` 等标准 POSIX 接口。但深入分析显示，其内部 VMA 管理、文件后端映射等核心实现路径不同，架构差异显著。 |
| **中断/异常处理** | 是 | 1 | 10.0% | 1.96% | **未形成** | 覆盖率极低。唯一的共同指纹是数学库函数 `exp_inline`，与中断处理无关。双方 Trap 处理、GIC/APIC 驱动等核心逻辑完全不同，无同源嫌疑。 |
| **系统调用** | 是 | 44 | 33.59% | 41.12% | **弱** | 覆盖率中等偏低。共同指纹多为 `arceos_posix_api` 下的薄封装函数（如 `sys_getaddrinfo`），其内部直接调用 `syscall_body!` 宏。这是框架的标准用法，不构成独创性复用。 |
| **文件系统** | 是 | 142 | 22.26% | 31.28% | **弱** | 覆盖率低。共同指纹集中在 `axlibc` 的 C 库函数（`glob`, `setjmp`）和 `arceos_posix_api` 的 `select/poll` 封装。双方底层文件系统实现（ext4 vs. lwext4_rust）的 AST 指纹完全不同。 |
| **设备驱动** | 是 | 73 | 41.95% | 51.41% | **中** | 覆盖率中等。共同指纹主要来自 ArceOS 的设备驱动框架 `axdriver` 和 `axnet` 初始化逻辑。这是对同一框架的标准使用，其初始化设备枚举流程必然相似。 |
| **网络协议栈** | 是 | 51 | 22.97% | 100.0% | **弱** | 目标覆盖率低，参考覆盖率高是因为参考项目网络栈模块较小。共同指纹为 `inet_pton` 等标准 C 函数。核心协议实现（目标自研 vs. 参考 smoltcp）完全不同，架构异构。 |
| **第三方库/公共依赖** | 部分 | 0 | 0.0% | 0.0% | **未形成** | 参考项目包含 `dwmac`、`visionfive2-sd` 等独立第三方驱动，目标项目无。目标项目的 `文件系统/第三方库` 与参考项目无共同指纹。这证明双方依赖了不同的第三方库，是独立开发的强证据。 |

#### 3. 底层 AST 关键证据指纹表

| 所属板块 | 目标项目路径 | 参考项目路径 | 证据类型 | 技术说明 |
| :--- | :--- | :--- | :--- | :--- |
| **第三方库/公共依赖** | `arceos/ulib/axlibc/c/printf.c#format_string_loop` | `arceos/ulib/axlibc/c/printf.c#format_string_loop` | 同源指纹 | 完全一致的内部宏和函数调用序列（`ABS_FOR_PRINTING`, `out_rev_` 等），是共同继承自 musl libc 的确定性证据。 |
| **第三方库/公共依赖** | `arceos/ulib/axlibc/c/pow.c#pow` | `arceos/ulib/axlibc/c/pow.c#pow` | 同源指纹 | 完全一致的数学库内部调用（`asdouble`, `asuint64`, `__math_oflow` 等），共同继承自 musl libc。 |
| **任务调度/异步运行时** | `arceos/modules/axtask/src/run_queue.rs#switch_to` | `arceos/modules/axtask/src/run_queue.rs#switch_to` | 同源指纹 | 上下文切换的底层操作序列（`(*prev_ctx_ptr).switch_to`, `CurrentTask::set_current`）完全一致，是共同继承自 ArceOS 内核框架的核心证据。 |
| **进程管理** | `arceos/modules/axtask/src/run_queue.rs#exit_current` | `arceos/modules/axtask/src/run_queue.rs#exit_current` | 同源指纹 | 任务退出时操作 `EXITED_TASKS` 和 `WAIT_FOR_EXIT` 的序列一致，是 ArceOS 调度器内部机制，非进程管理业务逻辑。 |
| **进程管理** | `core/src/shm.rs#get_shmids_by_pid` | `api/src/imp/ipc/shm.rs#get_shmids_by_pid` | 领域惯用写法 | `Vec::new`, `map.forward.keys`, `res.push` 是从 `BiMap` 收集键值的 Rust 标准模式，不具独创性。 |
| **进程管理** | `arceos/tools/raspi4/chainloader/src/panic_wait.rs#panic_prevent_reenter` | `arceos/tools/raspi4/chainloader/src/panic_wait.rs#panic_prevent_reenter` | 同源指纹 | 嵌入式 panic 重入防护的标准模式，且路径属于 ArceOS 上游板级支持包，双方共同继承。 |
| **进程管理** | `arceos/ulib/axlibc/c/pthread.c#pthread_cond_wait` | `arceos/ulib/axlibc/c/pthread.c#pthread_cond_wait` | 同源指纹 | 均为 `unimplemented` 桩函数，是共同继承自不完整的 axlibc 的证据。 |

#### 4. 核心代码调用流对比剖析

* **【进程管理/进程退出路径】**
* **底层技术析理**: 双方 `do_exit` 函数虽同名，但内部资源清理的**对象管理模型**和**同步策略**完全不同。目标项目采用全局锁直接操作共享内存管理器（`SHM_MANAGER.lock().clear_proc_shm`），并通过 `vm_write` 写入 `clear_child_tid`。参考项目则通过 `FD_TABLE.clear` 清理文件描述符，并使用 `UserPtr` 和 `get_as_mut` 来操作 `clear_child_tid`。目标项目使用 `FutexKey` 处理健壮 futex，参考项目则无。这表明两者在进程退出这一核心路径上，对资源抽象和清理顺序的设计是独立且异构的。
* **代码溯源链接**:
  - 目标项目: `api/src/task.rs#do_exit`
  - 参考项目: `api/src/imp/task/exit.rs#do_exit`

* **【进程管理/程序执行路径】**
* **底层技术析理**: 双方 `sys_execve` 的实现揭示了完全不同的**文件解析与参数处理架构**。目标项目直接硬编码了 `”/bin/sh”.to_string` 作为后备，并通过 `FS_CONTEXT.lock().resolve` 进行路径解析，参数处理细节被折叠。参考项目则实现了完整的参数解析逻辑，通过 `arg.get_as_str` 和 `argv.get_as_null_terminated` 细致地处理用户态字符串数组。这种从参数处理到文件系统交互的顶层设计差异，是独立开发的强有力证明。
* **代码溯源链接**:
  - 目标项目: `api/src/syscall/task/execve.rs#sys_execve`
  - 参考项目: `api/src/imp/task/execve.rs#sys_execve`

* **【任务调度/异步运行时/等待队列机制】**
* **底层技术析理**: 双方在等待队列（`wait_queue`）的实现上采用了**截然不同的同步原语**。目标项目使用基于事件的异步模型，核心操作是 `block_on`, `listener!` 和 `self.event.notify`，这是典型的 `async/await` 风格。参考项目则直接操作调度器的运行队列，使用 `current_run_queue`, `blocked_resched` 和 `self.queue.lock`，是传统的基于锁和调度器操作的同步模型。这一根本性差异证明双方在异步运行时这一核心基础设施上走了完全不同的技术路线。
* **代码溯源链接**:
  - 目标项目: `arceos/modules/axtask/src/wait_queue.rs#wait_timeout_until`
  - 参考项目: `arceos/modules/axtask/src/wait_queue.rs#wait_timeout_until`

#### 5. 单方模块与降权项

- **单方模块**:
  - **网络协议栈/第三方库** 与 **第三方库**: 仅存在于参考项目中（如 `smoltcp_impl`, `dwmac`, `visionfive2-sd`）。目标项目无对应模块，这是双方采用不同第三方依赖和硬件平台的直接证据，强烈支持独立开发。
  - **文件系统/第三方库**: 仅存在于目标项目中，且与参考项目无共同指纹，同样指向独立的第三方库选型。
- **降权项**:
  - **所有 `unimplemented` 桩函数**: 如 `pthread_cond_wait`, `execve`, `wait3`。这些是未实现功能的占位符，在任何独立项目中都会存在，不能作为抄袭证据。
  - **`arceos/tools/raspi4/chainloader/` 下的函数**: 属于板级启动和调试代码，与操作系统内核核心逻辑无关，且是 ArceOS 上游代码，应予以排除。
  - **跨板块同名函数**: 如将目标项目的文件系统函数与参考项目的进程管理函数对比，此类证据不构成同板块主证据，已全部降权或忽略。

#### 6. 保守结论

综合全模块 AST 指纹分析，目标项目与参考项目的整体相似度仅为 19.28%，远低于同源性判定阈值。双方共有的 782 个 AST 指纹，经深度析理，绝大部分可归因于对 **ArceOS 微内核框架、musl libc 等公共第三方库的共同继承**，以及 **POSIX/Linux 标准 ABI 对函数名和调用序列的强制约束**。在进程管理、内存管理、异步运行时等核心自研领域，双方的内部实现架构、同步机制、对象管理模型呈现出根本性、系统性的差异，例如一方采用事件驱动异步模型而另一方采用传统调度器锁模型。这些决定性证据共同表明，目标项目是独立设计和实现的，不存在对参考项目的整体或局部抄袭。整体风险评级为：**无风险**。

------------------------------

