# 🛡️ OS 内核赛自动化代码级架构审计报告

**审计时间**: `2026-07-02 11:31:24`
**参赛作品名称**: `2026-T202610486999845-XX`
**年份**: `2026`
**学校**: `XX`
**队伍名称**: `T202610486999845`
**比赛名字**: `OS-Competition`
**比赛子赛道**: `Kernel-Implementation`
**仓库地址**: `https://gitlab.eduxiji.net/educg-group-43501-3132633/T202610486999845-779`
**推测开发语言**: `Rust / C (由AST探针自动识别)`
**特征函数量**: `2812`

## ⚔️ 底层代码同源性对比 (AST Homology)
> 本部分由专门的代码审查智能体，对该项目与符合年份条件的历届最高相似度项目进行控制流与底层调用对比。新版报告会先给出确定性的全模块覆盖矩阵，再给出 Agent 的分模块解释，避免只围绕少量抽样函数展开。

### 🏆 Top 3 底层对标: vs `2022-RunOS-电子科技大学`（电子科技大学 / RunOS）

#### 对方作品信息

**对方年份**: `2022`
**对方学校**: `电子科技大学`
**对方队伍名称**: `RunOS`
**对方比赛名字**: `OS-Competition`
**对方比赛子赛道**: `Kernel-Implementation`
**对方仓库地址**: `https://gitlab.eduxiji.net/weihuan_tang/runos`

> 双向覆盖率：共同 AST 指纹: 13；唯一 AST 指纹: 目标项目 1449，参考项目 709；覆盖率: 目标项目 0.9%，参考项目 1.83%

## 全模块对比总览（确定性证据）

> 总体覆盖率：共同 AST 指纹: 13；唯一 AST 指纹: 目标项目 1449，参考项目 709；覆盖率: 目标项目 0.9%，参考项目 1.83%

### 1. 模块覆盖矩阵
| 模块 | 目标函数数 | 参考函数数 | 共同唯一 AST 指纹 | Target 覆盖 | Base 覆盖 | 状态 |
| :--- | ---: | ---: | ---: | ---: | ---: | :--- |
| 进程管理 | 896 | 45 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |
| 任务调度/异步运行时 | 407 | 88 | 1 | 0.44% | 1.15% | 双方存在且有结构重合 |
| 内存管理 | 321 | 222 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |
| 中断/异常处理 | 13 | 8 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |
| 系统调用 | 11 | 79 | 1 | 11.11% | 2.38% | 双方存在且有结构重合 |
| 文件系统 | 1039 | 305 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |
| 设备驱动 | 8 | 25 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |
| 网络协议栈 | 0 | 0 | 0 | 0.0% | 0.0% | 双方均未体现 |
| 文件系统/第三方库 | 50 | 0 | 0 | 0.0% | 0.0% | 仅目标项目体现 |
| 通用模块/其他 | 67 | 130 | 5 | 10.0% | 5.56% | 双方存在且有结构重合 |

### 2. 分模块证据清单

#### 进程管理
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `os-la/src/syscall/process.rs#sys_setpgid` | `kernel/src/syscall/process.rs#sys_setpgid` | 同板块同名 | LTP_SETPGID03_REMOTE_CALLS.saturating_add | log::debug! |
| `os-la/src/syscall/process.rs#sys_times` | `kernel/src/syscall/process.rs#sys_times` | 同板块同名 | 0u64.to_le_bytes, data[0..8].copy_from_slice, data[16..24].copy_from_slice, data[24..32].copy_from_slice, data[8..16].copy_from_slice, monotonic_millis ...(+3) | current_user_token, get_time_us, translated_refmut |
| `os-la/src/syscall/process.rs#sys_getpgid` | `kernel/src/syscall/process.rs#sys_getpgid` | 同板块同名 | sys_getpid | log::debug! |
| `os-la/src/syscall/process.rs#sys_clock_gettime` | `kernel/src/syscall/process.rs#sys_clock_gettime` | 同板块同名 | clock_now_nanos, is_supported_clock_id, task::is_user_range_writable, write_timespec_i128 | current_user_token, get_time_sec_nsec, tp.add, translated_refmut |

#### 任务调度/异步运行时
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `os-la/src/syscall/fs.rs#sys_fstatat` | `kernel/src/syscall/fs.rs#sys_fstatat` | 同板块同名 | content.len, dangling_symlink_or_prefix_error, find_dummy_mounted_mem_file, find_hardlink_alias, find_mem_file, find_mem_file(&stat_path[..stat_len])
        .or_else ...(+25) | Stat::empty, String::from, UserBuffer::new, current_task, current_task().unwrap, fstat_inner ...(+11) |
| `os-la/src/syscall/fs.rs#sys_readlinkat` | `kernel/src/syscall/fs.rs#sys_readlinkat` | 同板块同名 | DEFAULT_EXE_LINK.len, dangling_symlink_or_prefix_error, find_mem_file, has_test_eloop_chain, is_fd_symlink, is_runtime_libgcc_s_path ...(+24) | UserBuffer::new, current_user_token, linkpath_str.as_bytes, log::debug!, panic!, translated_byte_buffer ...(+2) |
| `os-la/src/syscall/fs.rs#sys_write` | `kernel/src/syscall/fs.rs#sys_write` | 同板块同名 | check_direct_io_user_buffer, entry.offset.saturating_add, fd_allows_write, is_proc_pid_oom_score_adj_path, matches!, notify_inotify_modify ...(+19) | UserBuffer::new, current_task, current_task().unwrap, drop, f.clone, file.writable ...(+6) |
| `os-la/src/syscall/fs.rs#sys_ppoll` | `kernel/src/syscall/fs.rs#sys_ppoll` | 同板块同名 | data[0..4].try_into, data[0..4].try_into().unwrap, data[4..6].try_into, data[4..6].try_into().unwrap, data[6..8].copy_from_slice, earliest_timerfd_deadline ...(+22) | current_task, current_task().unwrap, f.is_some, fds.add, inner.fd_table.get, inner.get_user_token ...(+3) |

#### 内存管理
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `os/src/syscall/fs.rs#sys_fcntl` | `kernel/src/syscall/fs.rs#sys_fcntl` | 同板块同名 | add_pipe_endpoint_ref, alloc_fd_from, arg.max, ensure_shared_offset_slot, fcntl_get_lock, fcntl_get_owner_ex ...(+8) | current_task, current_task().unwrap, inner.alloc_fd, inner.fd_table.len, inner.fd_table[fd].clone, log::debug! ...(+2) |
| `os-la/src/syscall/process.rs#sys_brk` | `kernel/src/syscall/process.rs#sys_brk` | 同板块同名 | addr.saturating_sub, task::reserve_user_range, task::reserve_user_range(PROGRAM_BRK, addr, true, false).is_err | VirtAddr::from, VirtAddr::from(brk_addr).floor, current_task, current_task().unwrap, current_task.acquire_inner_lock, inner
                .addrspace
                .alloc_heap_section ...(+4) |
| `os-la/src/syscall/fs.rs#sys_mprotect` | `kernel/src/syscall/process.rs#sys_mprotect` | 同板块同名 | mprotect_would_upgrade_readonly_shared_file, mprotect_would_violate_write_seal, task::protect_user_range, update_file_mapping_prot | PTEFlags::from_bits, PTEFlags::from_bits((prot << 1) as u8).unwrap, VirtAddr::from, VirtAddr::from(address + length).ceil, VirtAddr::from(address).floor, current_task ...(+4) |
| `os-la/src/syscall/fs.rs#sys_dup` | `kernel/src/syscall/fs.rs#sys_dup` | 同板块同名 | add_pipe_endpoint_ref, alloc_fd_any, ensure_shared_offset_slot | Some, current_task, current_task().unwrap, inner.alloc_fd, inner.fd_table.len, inner.fd_table[fd].as_ref ...(+5) |

#### 中断/异常处理
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `os/src/drivers/virtio.rs#read_sector` | `kernel/src/trap/trap.rs#set_kernel_trap_entry` | 同模块上下文对齐/差异观察 | Err, Ok, avail.idx.wrapping_add, avail_ring_mut, buffer.as_mut_ptr, core::mem::size_of::<VirtioBlkReqHeader> ...(+8) | stvec::write |
| `os-la/src/drivers/virtio.rs#read_sector` | `kernel/src/trap/trap.rs#set_user_trap_entry` | 同模块上下文对齐/差异观察 | (self.pci.isr as *const u8).read_volatile, (self.pci.notify_addr as *mut u16).write_volatile, Err, Ok, avail.idx.wrapping_add, avail_ring_mut ...(+9) | stvec::write |
| `os-la/src/arch.rs#write_stvec` | `kernel/src/timer.rs#disable_timer_interrupt` | 同模块上下文对齐/差异观察 | asm! | sie::clear_stimer |
| `os-la/src/trap/mod.rs#fatal_trap_signal` | `kernel/src/timer.rs#enable_timer_interrupt` | 同模块上下文对齐/差异观察 | 内部闭环逻辑 | sie::set_stimer |

#### 系统调用
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `user/src/syscall.rs#syscall` | `user/src/syscall.rs#syscall` | 同板块同名 | asm! | asm! |
| `user/src/syscall.rs#sys_write` | `user/src/syscall.rs#sys_write` | 同板块同名 | buffer.as_ptr, buffer.len, syscall | buffer.as_ptr, buffer.len, syscall |
| `user/src/syscall.rs#sys_exit` | `user/src/syscall.rs#sys_exit` | 同板块同名 | syscall | panic!, syscall |
| `user/src/lib.rs#exit` | `user/src/lib.rs#exit` | 同板块同名 | sys_exit | sys_exit |

#### 文件系统
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `os-la/src/syscall/fs.rs#sys_renameat2` | `kernel/src/syscall/fs.rs#sys_renameat2` | 同板块同名 | MEM_FILES[existing].path.fill, MEM_FILES[existing].path[..old_len].copy_from_slice, MEM_FILES[index].path.fill, MEM_FILES[index].path[..new_len].copy_from_slice, Some, directory_has_children ...(+14) | current_user_token, log::debug!, translated_str |
| `os-la/src/syscall/fs.rs#sys_mount` | `kernel/src/syscall/fs.rs#sys_mount` | 同板块同名 | RO_MOUNT_PATH.fill, RO_MOUNT_PATH[..target_len].copy_from_slice, bind_dummy_mount, crate::syscall::current_euid_for_fs, dummy_mount_has_open_file, dummy_mount_path_matches ...(+11) | MNT_TABLE.lock, MNT_TABLE.lock().mount, current_user_token, log::debug!, translated_str |
| `os-la/src/syscall/fs.rs#sys_statfs` | `kernel/src/syscall/fs.rs#sys_statfs` | 同板块同名 | dangling_symlink_or_prefix_error, dummy_mount_flags_for_path, dummy_mount_statfs_for_path, dummy_mount_statfs_for_path(&statfs_path[..statfs_len]).unwrap_or, find_dummy_mounted_mem_file, find_mem_file ...(+21) | StatFS::empty, UserBuffer::new, current_user_token, kstat.as_bytes, log::debug!, size_of::<StatFS> ...(+3) |
| `os/src/syscall/fs.rs#empty` | `kernel/src/fs/finfo.rs#empty` | 同板块同名 | 内部闭环逻辑 | Self::default |

#### 设备驱动
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `os-la/src/drivers/virtio.rs#pci_cfg_addr` | `kernel/src/drivers/block/sdcard.rs#read_sector` | 同模块上下文对齐/差异观察 | 内部闭环逻辑 | Err, Ok, assert!, chunk.iter_mut, chunk.iter_mut().zip, data_buf.chunks_mut ...(+6) |
| `os-la/src/drivers/virtio.rs#new` | `kernel/src/drivers/block/sdcard.rs#write_sector` | 同模块上下文对齐/差异观察 | 内部闭环逻辑 | (data_buf.len() / SEC_LEN).try_into, (data_buf.len() / SEC_LEN).try_into().unwrap, Err, Ok, assert!, chunk.iter ...(+9) |
| `os-la/src/sbi.rs#console_putchar` | `kernel/src/drivers/block/sdcard.rs#get_cidregister` | 同模块上下文对齐/差异观察 | (UART0_BASE as *mut u8).write_volatile | Err, Ok, self.end_cmd, self.get_response, self.read_data, self.send_cmd ...(+2) |
| `os-la/src/drivers/virtio.rs#queue_base_addr` | `kernel/src/drivers/block/sdcard.rs#init_sdcard` | 同模块上下文对齐/差异观察 | core::ptr::addr_of! | 115_200.bps, Peripherals::steal, SDCard::new, assert!, io_init, k210_hal::clock::Clocks::new ...(+10) |

#### 文件系统/第三方库
仅目标项目中观察到该模块代表函数，参考项目没有同模块可对齐证据：
- 目标项目：`os-la/src/fs/ext4.rs#lookup_child`，调用摘要：Err, Ok, Some, core::str::from_utf8, div_ceil_u64, node_kind ...(+6)
- 目标项目：`os/src/fs/ext4.rs#lookup_child`，调用摘要：Err, Ok, Some, core::str::from_utf8, div_ceil_u64, node_kind ...(+6)
- 目标项目：`os-la/src/fs/ext4.rs#for_each_child`，调用摘要：Err, Ok, core::str::from_utf8, div_ceil_u64, node_kind, read_u16 ...(+6)
- 目标项目：`os/src/fs/ext4.rs#for_each_child`，调用摘要：Err, Ok, core::str::from_utf8, div_ceil_u64, node_kind, read_u16 ...(+6)

#### 通用模块/其他
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `os-la/src/logging.rs#log` | `kernel/src/logger.rs#log` | 同板块同名 | println!, record.level, record.metadata, self.enabled | get_time, get_time_sec_msec, hart_id, mod_path.trim_start_matches, println!, record
            .module_path_static ...(+6) |
| `os/src/sbi.rs#shutdown` | `kernel/src/opensbi/legacy.rs#shutdown` | 同板块同名 | asm!, sbi_call | log::debug!, opensbi_call, panic! |
| `os-la/src/main.rs#clear_bss` | `kernel/src/main.rs#clear_bss` | 同板块同名 | core::slice::from_raw_parts_mut, core::slice::from_raw_parts_mut(start as *mut u8, end - start).fill | core::slice::from_raw_parts_mut, core::slice::from_raw_parts_mut(sbss as usize as *mut u8, ebss as usize - sbss as usize)
            .fill |
| `os-la/src/sbi.rs#shutdown` | `kernel/src/rustsbi/legacy.rs#shutdown` | 同板块同名 | (GED_SLEEP_CTL as *mut u8).write_volatile, core::arch::asm! | panic!, println!, rustsbi_call |


# 最终同源性定性仲裁报告

**案号**：2026-T202610486999845-XX vs 2022-RunOS-电子科技大学  
**审计方法**：底层 AST 调用图谱比对 + 架构差异深度析理  
**审计日期**：2026 年

---

## 1. 宏观判定

| 判定维度 | 数值/结论 |
| :--- | :--- |
| **定性结论** | **独立开发 / 底层架构显著差异** |
| **客观 AST 相似度** | **0.61%** |
| **共同 AST 指纹** | **13**（全项目级） |
| **唯一 AST 指纹** | 目标项目 1449，参考项目 709 |
| **覆盖率** | 目标项目 0.9%，参考项目 1.83% |
| **核心争议板块（进程管理）共同指纹** | **0** |
| **核心争议板块覆盖率** | 目标项目 0.0%，参考项目 0.0% |

**判定依据摘要**：
- 全项目 AST 相似度仅 0.61%，远低于通常认定同源性的阈值（>50%）。
- 控方指控的“100% 同构”证据链，经核实全部为**目标项目内部两个架构变体（`os-la` 与 `os`）之间的自洽性**，而非目标项目与参考项目（2022-RunOS）之间的复用。
- 在【进程管理】这一核心争议板块，双方**共同唯一 AST 指纹为 0**，覆盖率均为 0.0%，直接否定控方“逐字级复用”的核心论断。
- 双方在任务抽象、地址空间模型、文件系统架构、同步原语等底层设计上存在**根本性差异**，符合独立演化的技术特征。

---

## 2. 全模块覆盖矩阵解读

### 进程管理
- **双方是否均存在**：是。目标项目 896 个函数，参考项目 45 个函数。
- **共同 AST 指纹**：0。
- **覆盖率**：目标项目 0.0%，参考项目 0.0%。
- **证据强度**：**未形成**。双方虽均有进程管理模块，但函数体量悬殊（896 vs 45），且未观察到任何共同结构指纹。控方所列“铁证”均为目标项目内部 `os-la` 与 `os` 之间的一致性，参考项目证据中无 `sys_prctl`、`sys_futex`、`sys_semctl`、`sys_shmctl`、`sys_msgctl`、`sys_epoll_pwait`、`sys_capset` 等对应实现，无法形成有效比对。

### 任务调度/异步运行时
- **双方是否均存在**：是。目标项目 407 个函数，参考项目 88 个函数。
- **共同 AST 指纹**：1。
- **覆盖率**：目标项目 0.44%，参考项目 1.15%。
- **证据强度**：**弱**。共同指纹涉及 `sys_fstatat`、`sys_readlinkat`、`sys_write`、`sys_ppoll` 等 POSIX 标准系统调用名，但双方底层调用对象完全不同（目标项目使用 `find_mem_file`、`lookup_ext4_path`、`check_direct_io_user_buffer`；参考项目使用 `UserBuffer::new`、`current_task().unwrap()`、`translated_byte_buffer`）。同名函数源于 POSIX 标准，不构成同源证据。

### 内存管理
- **双方是否均存在**：是。目标项目 321 个函数，参考项目 222 个函数。
- **共同 AST 指纹**：0。
- **覆盖率**：目标项目 0.0%，参考项目 0.0%。
- **证据强度**：**未形成**。双方内存管理模块函数数量相当，但未观察到任何共同结构指纹。目标项目使用 `task::reserve_user_range`、`task::protect_user_range` 等自有抽象；参考项目使用 `AddrSpace::alloc_heap_section`、`PTEFlags::from_bits`、`VirtAddr::from`。地址空间管理模型根本不同。

### 中断/异常处理
- **双方是否均存在**：是。目标项目 13 个函数，参考项目 8 个函数。
- **共同 AST 指纹**：0。
- **覆盖率**：目标项目 0.0%，参考项目 0.0%。
- **证据强度**：**未形成**。双方 Trap 处理机制完全不同：目标项目使用 `fatal_trap_signal` 内部闭环逻辑；参考项目使用 `stvec::write`、`sie::set_stimer` 等 RISC-V 特权架构直接操作。无共同调用对象。

### 系统调用
- **双方是否均存在**：是。目标项目 11 个函数，参考项目 79 个函数。
- **共同 AST 指纹**：1。
- **覆盖率**：目标项目 11.11%，参考项目 2.38%。
- **证据强度**：**弱**。共同指纹为用户态 `syscall` 函数中的 `asm!` 宏调用，这是 RISC-V 架构 `ecall` 指令的标准内联汇编写法，属于**架构强制实现**，不构成独创性表达。`sys_write`、`sys_exit` 等均为 POSIX 标准接口的最小封装。

### 文件系统
- **双方是否均存在**：是。目标项目 1039 个函数，参考项目 305 个函数。
- **共同 AST 指纹**：0。
- **覆盖率**：目标项目 0.0%，参考项目 0.0%。
- **证据强度**：**未形成**。目标项目实现了完整的 ext4 文件系统（`lookup_child`、`for_each_child`、`div_ceil_u64`、`node_kind` 等 ext4 特有操作）；参考项目为简单教学型文件系统（`MNT_TABLE.lock().mount`、`app_inode.get_size`）。架构复杂度与实现路径完全不同。

### 设备驱动
- **双方是否均存在**：是。目标项目 8 个函数，参考项目 25 个函数。
- **共同 AST 指纹**：0。
- **覆盖率**：目标项目 0.0%，参考项目 0.0%。
- **证据强度**：**未形成**。目标项目使用 VirtIO 块设备驱动（`VirtioBlkReqHeader`、`avail_ring_mut`）；参考项目使用 SD 卡驱动（`k210_hal::clock::Clocks::new`、`SDCard::new`）。硬件平台与驱动模型完全不同。

### 网络协议栈
- **双方是否均存在**：否。双方均未体现该模块。
- **共同 AST 指纹**：0。
- **覆盖率**：N/A。
- **证据强度**：**未形成**。

### 文件系统/第三方库
- **双方是否均存在**：仅目标项目体现（50 个函数），参考项目无。
- **共同 AST 指纹**：0。
- **覆盖率**：目标项目 0.0%，参考项目 N/A。
- **证据强度**：**未形成**。目标项目的 ext4 实现（`os-la/src/fs/ext4.rs`、`os/src/fs/ext4.rs`）为自研模块，参考项目无对应实现，无法比对。

### 通用模块/其他
- **双方是否均存在**：是。目标项目 67 个函数，参考项目 130 个函数。
- **共同 AST 指纹**：5。
- **覆盖率**：目标项目 10.0%，参考项目 5.56%。
- **证据强度**：**弱**。共同指纹涉及 `log`、`shutdown`、`clear_bss` 等基础系统函数。`clear_bss` 中的 `core::slice::from_raw_parts_mut(...).fill(0)` 是 Rust 裸机开发的标准 BSS 清零模式；`shutdown` 中的 `asm!` / `sbi_call` 是 RISC-V SBI 规范的标准关机路径。这些属于**领域常规实现**，不构成同源证据。

---

## 3. 底层 AST 关键证据指纹表

| 所属板块 | 目标项目路径 | 参考项目路径 | 证据类型 | 技术说明 |
| :--- | :--- | :--- | :--- | :--- |
| 系统调用 | `user/src/syscall.rs#syscall` | `user/src/syscall.rs#syscall` | 弱信号 | 双方均使用 `asm!` 宏执行 `ecall` 指令，这是 RISC-V 系统调用的**架构强制实现**，不构成独创性表达。 |
| 系统调用 | `user/src/syscall.rs#sys_write` | `user/src/syscall.rs#sys_write` | 弱信号 | POSIX 标准接口的最小封装，调用 `buffer.as_ptr`、`buffer.len`、`syscall`，属于常规用户态封装模式。 |
| 系统调用 | `user/src/syscall.rs#sys_exit` | `user/src/syscall.rs#sys_exit` | 弱信号 | POSIX 标准接口的最小封装，调用 `syscall`，参考项目额外调用 `panic!`，实现细节不同。 |
| 系统调用 | `user/src/lib.rs#exit` | `user/src/lib.rs#exit` | 弱信号 | 对 `sys_exit` 的简单包装，属于标准库常见模式。 |
| 通用模块/其他 | `os-la/src/logging.rs#log` | `kernel/src/logger.rs#log` | 弱信号 | 双方均实现日志接口，但目标项目使用 `println!` + `record.level`；参考项目使用 `get_time` + `hart_id` + `mod_path.trim_start_matches`，实现细节不同。 |
| 通用模块/其他 | `os/src/sbi.rs#shutdown` | `kernel/src/opensbi/legacy.rs#shutdown` | 弱信号 | 双方均通过 SBI 调用关机，目标项目使用 `asm!` + `sbi_call`；参考项目使用 `opensbi_call` + `log::debug!`，SBI 规范强制调用模式。 |
| 通用模块/其他 | `os-la/src/main.rs#clear_bss` | `kernel/src/main.rs#clear_bss` | 弱信号 | 双方均使用 `core::slice::from_raw_parts_mut(...).fill(0)` 清零 BSS 段，这是 Rust 裸机开发的**标准模式**，不构成独创性表达。 |
| 通用模块/其他 | `os-la/src/sbi.rs#shutdown` | `kernel/src/rustsbi/legacy.rs#shutdown` | 弱信号 | 目标项目使用 `GED_SLEEP_CTL` 写 volatile + `asm!`；参考项目使用 `rustsbi_call` + `println!`，实现路径不同。 |
| 通用模块/其他 | `os-la/src/sbi.rs#console_putchar` | `kernel/src/drivers/block/sdcard.rs#get_cidregister` | 跨板块对齐 | 目标项目为 UART 输出（`UART0_BASE` 写 volatile）；参考项目为 SD 卡 CID 读取，**跨板块硬凑**，不构成同源证据。 |
| 任务调度/异步运行时 | `os-la/src/syscall/fs.rs#sys_fstatat` | `kernel/src/syscall/fs.rs#sys_fstatat` | 弱信号 | POSIX 标准接口同名，目标项目调用 `find_mem_file`、`lookup_ext4_path`；参考项目调用 `UserBuffer::new`、`fstat_inner`，底层对象完全不同。 |
| 任务调度/异步运行时 | `os-la/src/syscall/fs.rs#sys_readlinkat` | `kernel/src/syscall/fs.rs#sys_readlinkat` | 弱信号 | POSIX 标准接口同名，目标项目调用 `find_mem_file`、`has_test_eloop_chain`；参考项目调用 `UserBuffer::new`、`translated_byte_buffer`，底层对象完全不同。 |
| 任务调度/异步运行时 | `os-la/src/syscall/fs.rs#sys_write` | `kernel/src/syscall/fs.rs#sys_write` | 弱信号 | POSIX 标准接口同名，目标项目调用 `check_direct_io_user_buffer`、`notify_inotify_modify`；参考项目调用 `UserBuffer::new`、`file.writable`，底层对象完全不同。 |
| 任务调度/异步运行时 | `os-la/src/syscall/fs.rs#sys_ppoll` | `kernel/src/syscall/fs.rs#sys_ppoll` | 弱信号 | POSIX 标准接口同名，目标项目调用 `earliest_timerfd_deadline`、`data[0..4].try_into`；参考项目调用 `fds.add`、`inner.fd_table.get`，底层对象完全不同。 |

> **注**：控方指控的 10 条“铁证”（`sys_prctl`、`sys_futex`、`exec_resolved_inode`、`sys_semctl`、`sys_shmctl`、`sys_msgctl`、`sys_prlimit64`、`set_current_exec_policy`、`sys_epoll_pwait`、`sys_capset`）**均未列入本表**，因其在参考项目中无对应实现，无法形成有效比对。控方所列“一致”仅为目标项目内部 `os-la` 与 `os` 之间的自洽性。

---

## 4. 核心代码调用流对比剖析

### 【进程管理 / `sys_prctl` 路径】
- **底层技术析理**：
  - **目标项目**（`os-la` / `os`）：使用自有的 `get_process_comm` / `set_process_comm` 管理进程名，通过 `task::is_user_range_readable` 验证用户态地址可读性，使用 `write_user_i32` 写回结果，并通过 `write_user_i32(arg2, CURRENT_PDEATHSIG).map_or_else` 处理 `PR_SET_PDEATHSIG` 操作。任务抽象为 `task::` 前缀的模块级函数。
  - **参考项目**（2022-RunOS）：**无 `sys_prctl` 实现**。其进程管理使用 `current_task().unwrap()` 获取 `TaskControlBlock`，通过 `acquire_inner_lock` 访问内部状态，使用 `translated_refmut` 进行地址转换。任务抽象为 `TaskControlBlock` 对象方法。
  - **结论**：参考项目无对应实现，无法比对。目标项目内部 `os-la` 与 `os` 的一致性是同一项目跨架构共享代码的正常结果。

- **代码溯源链接**：
  - 目标项目：`os-la/src/syscall/process.rs#sys_prctl`
  - 参考项目：无对应实现

---

### 【进程管理 / `sys_futex` 路径】
- **底层技术析理**：
  - **目标项目**（`os-la` / `os`）：实现了完整的 futex 系统调用，包含线程取消标志（`arm_current_thread_cancel_flag`）、检查点唤醒（`consume_checkpoint_wake`）、futex 等待信用（`consume_futex_wait_credit`）、鲁棒列表处理（`finish_robust_owner_for_wait`）以及 LTP 测试兼容钩子（`ltp_tgkill02_checkpoint_futex_active`、`force_tgkill02_checkpoint_futex`）。
  - **参考项目**（2022-RunOS）：**无 `sys_futex` 实现**。其同步机制仅包含基础的 `sys_sleep`（调用 `suspend_current_and_run_next`）。
  - **结论**：参考项目无 futex 实现，目标项目的 futex 复杂度远超参考项目的同步能力范畴。LTP 兼容钩子是目标项目为通过测试而自研的补丁层。

- **代码溯源链接**：
  - 目标项目：`os-la/src/syscall/process.rs#sys_futex`
  - 参考项目：无对应实现

---

### 【进程管理 / `exec_resolved_inode` 路径】
- **底层技术析理**：
  - **目标项目**（`os-la` / `os`）：使用 `TrapContext::app_init_context` 初始化用户态上下文，通过 `exec_arg_bytes` / `exec_basename` 解析参数，使用 `core::str::from_utf8(...).map_err` 进行 UTF-8 验证。地址空间初始化通过 `TrapContext` 完成。
  - **参考项目**（2022-RunOS）：exec 逻辑位于 `kernel/src/task/task.rs#exec`，使用 `AddrSpace::create_user_space` 创建地址空间，通过 `KERNEL_SPACE.lock().token` 获取页表令牌，使用 `Section::new` 加载段。地址空间模型为 `AddrSpace` + `KERNEL_SPACE` 双空间模型。
  - **结论**：双方 exec 实现的核心抽象完全不同（`TrapContext::app_init_context` vs `AddrSpace::create_user_space`），内存模型异构。

- **代码溯源链接**：
  - 目标项目：`os-la/src/syscall/process.rs#exec_resolved_inode`
  - 参考项目：`kernel/src/task/task.rs#exec`

---

### 【内存管理 / `sys_brk` 路径】
- **底层技术析理**：
  - **目标项目**（`os-la` / `os`）：使用 `task::reserve_user_range(PROGRAM_BRK, addr, true, false).is_err` 管理堆边界，通过 `addr.saturating_sub` 计算偏移。内存管理抽象为 `task::reserve_user_range` 模块级函数。
  - **参考项目**（2022-RunOS）：使用 `VirtAddr::from(brk_addr).floor` 对齐地址，通过 `inner.addrspace.alloc_heap_section` 分配堆段。内存管理抽象为 `AddrSpace` 对象方法。
  - **结论**：双方堆管理模型不同（`reserve_user_range` vs `alloc_heap_section`），地址抽象不同（直接数值计算 vs `VirtAddr` 类型封装）。

- **代码溯源链接**：
  - 目标项目：`os-la/src/syscall/process.rs#sys_brk`
  - 参考项目：`kernel/src/syscall/process.rs#sys_brk`

---

### 【文件系统 / `sys_renameat2` 路径】
- **底层技术析理**：
  - **目标项目**（`os-la` / `os`）：使用 `MEM_FILES` 全局数组管理内存文件，通过 `MEM_FILES[index].path[..new_len].copy_from_slice` 更新路径，调用 `directory_has_children` 检查目录状态。文件系统为自研内存文件系统 + ext4。
  - **参考项目**（2022-RunOS）：使用 `current_user_token` + `translated_str` 进行地址转换，通过 `log::debug!` 记录日志。文件系统为简单教学型实现。
  - **结论**：双方文件系统架构复杂度悬殊（`MEM_FILES` 全局数组 + ext4 vs 简单教学型 FS），实现路径完全不同。

- **代码溯源链接**：
  - 目标项目：`os-la/src/syscall/fs.rs#sys_renameat2`
  - 参考项目：`kernel/src/syscall/fs.rs#sys_renameat2`

---

## 5. 单方模块与降权项

### 仅目标项目存在的模块
| 模块 | 函数数 | 说明 | 降权理由 |
| :--- | :--- | :--- | :--- |
| 文件系统/第三方库（ext4） | 50 | `os-la/src/fs/ext4.rs`、`os/src/fs/ext4.rs` 中的 `lookup_child`、`for_each_child`、`div_ceil_u64`、`node_kind` 等 ext4 特有实现 | 参考项目无 ext4 实现，无法比对。目标项目自研 ext4 文件系统是独立开发的强证据。 |

### 仅参考项目存在的模块
| 模块 | 函数数 | 说明 | 降权理由 |
| :--- | :--- | :--- | :--- |
| oscomp 测试框架 | 多个 C 测试文件 | `oscomp/src/oscomp/waitpid.c`、`fork.c`、`execve.c` 等 | 目标项目无对应 C 测试框架，测试体系不同。 |

### 跨板块硬凑证据（降权）
控方证据中存在多处跨板块标签错误，根据审计指令“跨板块同名/同 hash 只能作为弱信号”：
- `resolve_exec_inode_at`（实际位于 `syscall/fs.rs`）被标签为【进程管理】→ 降权。
- `sys_epoll_pwait`（实际位于 `syscall/fs.rs`）被标签为【进程管理】→ 降权。
- `sys_fstatat`、`sys_readlinkat`、`sys_write`、`sys_ppoll`（实际位于 `syscall/fs.rs`）被标签为【任务调度/异步运行时】→ 降权。
- `sys_fcntl`、`sys_dup`（实际位于 `syscall/fs.rs`）被标签为【内存管理】→ 降权。

### 泛型宏/标准方法（降权）
以下调用属于 Rust 标准库或 POSIX/Linux ABI 强制规范，不构成独创性表达：
- `matches!`、`println!`、`assert!`、`panic!`：Rust 标准宏。
- `copy_from_slice`、`try_into().unwrap()`、`saturating_add`、`saturating_sub`、`saturating_mul`：Rust 标准库方法。
- `core::str::from_utf8`、`core::slice::from_raw_parts_mut`：Rust 核心库。
- `asm!`：RISC-V 内联汇编标准写法。
- `SEMVMX`、`CAP_USER_VERSION_3`、`CURRENT_PDEATHSIG`：Linux ABI 标准常量。
- `sys_prctl`、`sys_futex`、`sys_semctl`、`sys_shmctl`、`sys_msgctl`、`sys_epoll_pwait` 等：POSIX/SUS 标准系统调用名。

---

## 6. 保守结论

基于全项目 AST 相似度 0.61%、共同指纹仅 13 个、覆盖率均低于 2% 的客观数据，结合对【进程管理】核心争议板块 0 共同指纹的深度析理，本审计认定：**目标项目 [2026-T202610486999845-XX] 与参考项目 [2022-RunOS-电子科技大学] 之间不存在可证实的代码同源性**。

控方《复用指控清单》所列 10 条“铁证”经核实全部为目标项目内部 `os-la` 与 `os` 两个架构变体之间的自洽性，而非与参考项目的复用。双方在任务抽象（`task::` 模块函数 vs `TaskControlBlock` 对象方法）、地址空间模型（`TrapContext::app_init_context` vs `AddrSpace::create_user_space`）、文件系统架构（自研 ext4 vs 教学型 FS）、同步机制（完整 futex vs 基础 sleep）等底层设计上存在根本性差异，符合独立演化的技术特征。13 个共同指纹均为 POSIX 标准接口名、RISC-V 架构强制实现或 Rust 裸机开发标准模式，不构成独创性表达。**综合判定：独立开发，排除抄袭嫌疑。**

------------------------------

