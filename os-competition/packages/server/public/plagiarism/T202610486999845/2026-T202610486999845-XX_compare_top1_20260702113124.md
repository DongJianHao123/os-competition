# 🛡️ OS 内核赛自动化代码级架构审计报告

**审计时间**: `2026-07-02 11:31:24`
**参赛作品名称**: `2026-T202610486999845-XX`
**年份**: `2026`
**学校**: `XX`
**队伍名称**: `T202610486999845`
**比赛名字**: `OS-Competition`
**比赛子赛道**: `Kernel-Implementation`
**仓库地址**: `https://gitlab.eduxiji.net/educg-group-43501-3132633/T202610486999845-779`
**推测开发语言**: `Rust / C (由AST探针自动识别)`
**特征函数量**: `2812`

## ⚔️ 底层代码同源性对比 (AST Homology)
> 本部分由专门的代码审查智能体，对该项目与符合年份条件的历届最高相似度项目进行控制流与底层调用对比。新版报告会先给出确定性的全模块覆盖矩阵，再给出 Agent 的分模块解释，避免只围绕少量抽样函数展开。

### 🏆 Top 1 底层对标: vs `2024-chaos-北京科技大学`（北京科技大学 / chaos）

#### 对方作品信息

**对方年份**: `2024`
**对方学校**: `北京科技大学`
**对方队伍名称**: `chaos`
**对方比赛名字**: `OS-Competition`
**对方比赛子赛道**: `Kernel-Implementation`
**对方仓库地址**: `https://gitlab.eduxiji.net/educg-group-26010-2376550/oskernel2024-chaos`

> 双向覆盖率：共同 AST 指纹: 17；唯一 AST 指纹: 目标项目 1449，参考项目 798；覆盖率: 目标项目 1.17%，参考项目 2.13%

## 全模块对比总览（确定性证据）

> 总体覆盖率：共同 AST 指纹: 17；唯一 AST 指纹: 目标项目 1449，参考项目 798；覆盖率: 目标项目 1.17%，参考项目 2.13%

### 1. 模块覆盖矩阵
| 模块 | 目标函数数 | 参考函数数 | 共同唯一 AST 指纹 | Target 覆盖 | Base 覆盖 | 状态 |
| :--- | ---: | ---: | ---: | ---: | ---: | :--- |
| 进程管理 | 896 | 45 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |
| 任务调度/异步运行时 | 407 | 76 | 1 | 0.44% | 1.43% | 双方存在且有结构重合 |
| 内存管理 | 321 | 178 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |
| 中断/异常处理 | 13 | 5 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |
| 系统调用 | 11 | 67 | 3 | 33.33% | 7.5% | 双方存在且有结构重合 |
| 文件系统 | 1039 | 152 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |
| 设备驱动 | 8 | 19 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |
| 网络协议栈 | 0 | 0 | 0 | 0.0% | 0.0% | 双方均未体现 |
| 文件系统/第三方库 | 50 | 270 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |
| 第三方库 | 0 | 50 | 0 | 0.0% | 0.0% | 仅参考项目体现 |
| 通用模块/其他 | 67 | 75 | 8 | 16.0% | 11.76% | 双方存在且有结构重合 |

### 2. 分模块证据清单

#### 进程管理
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `os-la/src/syscall/process.rs#sys_kill` | `os/src/syscall/process.rs#sys_kill` | 同板块同名 | child_record_exists, mark_child_killed, record_pending_signal, signal_has_user_handler, signal_is_ignored, task::request_exit | SignalFlags::from_bits, file!, line!, pid2process, process.inner_exclusive_access, trace! |
| `os-la/src/syscall/process.rs#sys_uname` | `os/src/syscall/process.rs#sys_uname` | 同板块同名 | put_uts_field, task::copy_to_user, task::copy_to_user(buf, &data).is_err, task::is_user_range_writable | "Machine: riscv64".as_bytes, "None".as_bytes, SYS_NAME.as_bytes, SYS_NODENAME.as_bytes, SYS_RELEASE.as_bytes, SYS_VERSION.as_bytes ...(+15) |
| `os-la/src/syscall/process.rs#sys_gettimeofday` | `os/src/syscall/process.rs#sys_gettimeofday` | 同板块同名 | ((us % 1_000_000) as u64).to_le_bytes, ((us / 1_000_000) as u64).to_le_bytes, data[0..8].copy_from_slice, data[8..16].copy_from_slice, realtime_nanos, realtime_nanos().div_euclid ...(+5) | get_time_us, sstatus::clear_sum, sstatus::set_sum, trace! |
| `os-la/src/syscall/process.rs#sys_times` | `os/src/syscall/process.rs#sys_times` | 同板块同名 | 0u64.to_le_bytes, data[0..8].copy_from_slice, data[16..24].copy_from_slice, data[24..32].copy_from_slice, data[8..16].copy_from_slice, monotonic_millis ...(+3) | current_task, current_task()
        .unwrap, current_task()
        .unwrap()
        .inner_exclusive_access, current_task()
        .unwrap()
        .inner_exclusive_access(file!(), line!())
        .get_children_process_clock_time, current_task()
        .unwrap()
        .inner_exclusive_access(file!(), line!())
        .get_process_clock_time, file! ...(+4) |

#### 任务调度/异步运行时
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `os-la/src/syscall/fs.rs#sys_linkat` | `os/src/syscall/fs.rs#sys_linkat` | 同板块同名 | Some, create_hardlink_alias, fd_to_mem_index, find_hardlink_alias, find_hardlink_alias(&new_path[..new_len]).is_some, find_hardlink_alias(&old_lookup_path[..old_lookup_len])
                .map ...(+19) | curdir.inode, curdir.inode().link, curdir.inode().lookup, curdir.inode().lookup(old_name.as_str()).unwrap, current_task, current_task()
        .unwrap ...(+8) |
| `os-la/src/syscall/fs.rs#sys_write` | `os/src/syscall/fs.rs#sys_write` | 同板块同名 | check_direct_io_user_buffer, entry.offset.saturating_add, fd_allows_write, is_proc_pid_oom_score_adj_path, matches!, notify_inotify_modify ...(+19) | core::slice::from_raw_parts, current_task, current_task().unwrap, drop, file!, file.clone ...(+8) |
| `os-la/src/syscall/fs.rs#sys_ppoll` | `os/src/syscall/ppoll.rs#sys_ppoll` | 同板块同名 | data[0..4].try_into, data[0..4].try_into().unwrap, data[4..6].try_into, data[4..6].try_into().unwrap, data[6..8].copy_from_slice, earliest_timerfd_deadline ...(+22) | Box::from_raw, Box::into_raw, Box::new, SignalFlags::empty, current_task, current_task().unwrap ...(+18) |
| `os-la/src/syscall/fs.rs#sys_sendfile` | `os/src/syscall/fs.rs#sys_sendfile` | 同板块同名 | Some, explicit_offset.as_mut, fast_sparse_sendfile, fd_allows_read, fd_allows_write, matches! ...(+9) | current_task, current_task().unwrap, drop, error!, file!, in_file.read ...(+14) |

#### 内存管理
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `os-la/src/syscall/fs.rs#sys_openat` | `os/src/syscall/fs.rs#sys_openat` | 同板块同名 | Ok, alloc_fd, alloc_fd_checked, alloc_fd_checked(FdEntry {
                kind: HANDLE_DEV_NULL,
                inode: 0,
                mem_index: usize::MAX,
                shared_offset: usize::MAX,
                offset: 0,
                path: effective_path,
                path_len: effective_len,
                fd_flags: open_fd_flags(flags),
                status_flags: open_status_flags(flags),
            })
            .unwrap_or_else, alloc_fd_checked(FdEntry {
                kind: HANDLE_LOOP_DEVICE,
                inode: 0,
                mem_index: index,
                shared_offset: usize::MAX,
                offset: 0,
                path: effective_path,
                path_len: effective_len,
                fd_flags: open_fd_flags(flags),
                status_flags: open_status_flags(flags),
            })
            .unwrap_or_else, build_tmpfile_path ...(+68) | OpenFlags::from_bits, OpenFlags::from_bits(flags).unwrap, Some, cast_file_to_inode, cast_file_to_inode(dir).unwrap, cast_inode_to_file ...(+19) |
| `os/src/syscall/fs.rs#sys_fcntl` | `os/src/syscall/fs.rs#sys_fcntl` | 同板块同名 | add_pipe_endpoint_ref, alloc_fd_from, arg.max, ensure_shared_offset_slot, fcntl_get_lock, fcntl_get_owner_ex ...(+8) | current_task, current_task().unwrap, debug!, file!, inner.alloc_fd, inner.fd_table.len ...(+6) |
| `os/src/trap/mod.rs#trap_handler` | `os/src/trap/mod.rs#trap_handler` | 同板块同名 | crate::syscall::should_treat_low_exec_fault_as_exit, deliver_trap_signal, fatal_trap_signal, finish_child_after_exit, maybe_deliver_signal, panic! ...(+7) | check_signals_of_current, check_timer, current_add_signal, current_task, current_task()
                .unwrap, current_task()
                .unwrap()
                .inner_exclusive_access ...(+24) |
| `os-la/src/syscall/process.rs#sys_brk` | `os/src/syscall/process.rs#sys_brk` | 同板块同名 | addr.saturating_sub, task::reserve_user_range, task::reserve_user_range(PROGRAM_BRK, addr, true, false).is_err | addr.into, align_addr.into, current_task, current_task().unwrap, file!, inner.memory_set.map_heap ...(+3) |

#### 中断/异常处理
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `os/src/drivers/virtio.rs#read_sector` | `os/src/fs/pipe.rs#read` | 同模块上下文对齐/差异观察 | Err, Ok, avail.idx.wrapping_add, avail_ring_mut, buffer.as_mut_ptr, core::mem::size_of::<VirtioBlkReqHeader> ...(+8) | assert!, buf.into_iter, buf.len, buf_iter.next, debug!, drop ...(+11) |
| `os-la/src/drivers/virtio.rs#read_sector` | `os/src/trap/mod.rs#set_kernel_trap_entry` | 同模块上下文对齐/差异观察 | (self.pci.isr as *const u8).read_volatile, (self.pci.notify_addr as *mut u16).write_volatile, Err, Ok, avail.idx.wrapping_add, avail_ring_mut ...(+9) | stvec::write |
| `os-la/src/arch.rs#write_stvec` | `os/src/trap/mod.rs#set_user_trap_entry` | 同模块上下文对齐/差异观察 | asm! | stvec::write |
| `os-la/src/trap/mod.rs#fatal_trap_signal` | `os/src/trap/mod.rs#enable_timer_interrupt` | 同模块上下文对齐/差异观察 | 内部闭环逻辑 | sie::set_stimer |

#### 系统调用
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `user/src/syscall.rs#syscall` | `user/src/syscall.rs#syscall` | 同板块同名同结构 | asm! | asm! |
| `user/src/syscall.rs#sys_write` | `user/src/syscall.rs#sys_write` | 同板块同名同结构 | buffer.as_ptr, buffer.len, syscall | buffer.as_ptr, buffer.len, syscall |
| `user/src/syscall.rs#sys_exit` | `user/src/syscall.rs#sys_exit` | 同板块同名 | syscall | panic!, syscall |
| `user/src/lib.rs#exit` | `user/src/lib.rs#exit` | 同板块同名 | sys_exit | sys_exit |

#### 文件系统
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `user/src/lang_items.rs#panic_handler` | `user/src/lang_items.rs#panic_handler` | 同板块同名 | panic_info.location, panic_info.message, println! | SignalFlags::SIGABRT.bits, getpid, kill, panic_info.location, panic_info.message, panic_info.message().unwrap ...(+2) |
| `os-la/src/lang_items.rs#panic` | `os/src/lang_items.rs#panic` | 同板块同名 | info.location, println!, shutdown | info.location, println!, shutdown |
| `os-la/src/syscall/fs.rs#sys_renameat2` | `os/src/fs/fat32/fs.rs#insert_dentry` | 同模块上下文对齐/差异观察 | MEM_FILES[existing].path.fill, MEM_FILES[existing].path[..old_len].copy_from_slice, MEM_FILES[index].path.fill, MEM_FILES[index].path[..new_len].copy_from_slice, Some, directory_has_children ...(+14) | Arc::clone, Fat32Dentry::new, Fat32DentryLayout::new, Fat32LDentryLayout::new, Some, get_block_cache ...(+15) |
| `os/src/syscall/fs.rs#sys_renameat2` | `os/src/fs/fat32/fs.rs#get_dentry` | 同模块上下文对齐/差异观察 | MEM_FILES[existing].path.fill, MEM_FILES[existing].path[..old_len].copy_from_slice, MEM_FILES[index].path.fill, MEM_FILES[index].path[..new_len].copy_from_slice, Some, directory_has_children ...(+14) | Arc::clone, Fat32Dentry::new, Fat32Dentry::new_deleted, Some, get_block_cache, get_block_cache(*sector_id, Arc::clone(&self.bdev))
                    .lock ...(+9) |

#### 设备驱动
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `os-la/src/drivers/virtio.rs#pci_cfg_addr` | `os/src/utils/platform_info.rs#fmt` | 同模块上下文对齐/差异观察 | 内部闭环逻辑 | Ok, core::str::from_utf8, core::str::from_utf8(&self.model[..index]).unwrap, core::str::from_utf8(&x[..self.bootargs_len]).unwrap, self
            .bootargs
            .as_ref, self
            .bootargs
            .as_ref()
            .map ...(+11) |
| `os-la/src/drivers/virtio.rs#new` | `os/src/block/block_cache.rs#get_block_cache` | 同模块上下文对齐/差异观察 | 内部闭环逻辑 | Arc::clone, Arc::new, Arc::strong_count, BlockCache::new, Mutex::new, panic! ...(+8) |
| `os-la/src/sbi.rs#console_putchar` | `os/src/drivers/block/virtio_blk.rs#read_block` | 同模块上下文对齐/差异观察 | (UART0_BASE as *mut u8).write_volatile | error!, panic!, res.is_err, res.unwrap, self.0.lock, self.0.lock().read_blocks ...(+1) |
| `os-la/src/drivers/virtio.rs#queue_base_addr` | `os/src/drivers/block/mod.rs#block_device_test` | 同模块上下文对齐/差异观察 | core::ptr::addr_of! | BLOCK_DEVICE.clone, assert_eq!, println!, write_buffer.iter_mut |

#### 文件系统/第三方库
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `os-la/src/fs/ext4.rs#lookup_child` | `os/libs/ext4_rs/src/ext4_interface.rs#ext4_generic_open` | 同模块上下文对齐/差异观察 | Err, Ok, Some, core::str::from_utf8, div_ceil_u64, node_kind ...(+6) | DirEntryType::EXT4_DE_DIR.bits, Errnum::ENOENT.into, Ext4DirEntry::default, Ext4DirSearchResult::new, Ext4InodeRef::get_inode_ref, Ext4InodeRef::new ...(+17) |
| `os/src/fs/ext4.rs#lookup_child` | `os/libs/ext4_rs/src/ext4_interface.rs#ext4_file_read` | 同模块上下文对齐/差异观察 | Err, Ok, Some, core::str::from_utf8, div_ceil_u64, node_kind ...(+6) | Ext4InodeRef::get_inode_ref, Ok, core::cmp::min, inode_ref.get_inode_dblk_idx, inode_ref.inner.inode.ext4_inode_type, inode_ref.inner.inode.inode_get_size ...(+8) |
| `os-la/src/fs/ext4.rs#for_each_child` | `os/libs/ext4_rs/src/ext4_interface.rs#ext4_generic_open_from` | 同模块上下文对齐/差异观察 | Err, Ok, core::str::from_utf8, div_ceil_u64, node_kind, read_u16 ...(+6) | DirEntryType::EXT4_DE_DIR.bits, Errnum::ENOENT.into, Ext4DirEntry::default, Ext4DirSearchResult::new, Ext4InodeRef::get_inode_ref, Ext4InodeRef::new ...(+17) |
| `os/src/fs/ext4.rs#for_each_child` | `os/libs/ext4_rs/src/ext4_interface.rs#ext4_generic_open2` | 同模块上下文对齐/差异观察 | Err, Ok, core::str::from_utf8, div_ceil_u64, node_kind, read_u16 ...(+6) | DirEntryType::EXT4_DE_DIR.bits, Errnum::ENOENT.into, Ext4DirEntry::default, Ext4DirSearchResult::new, Ext4InodeRef::get_inode_ref, Ext4InodeRef::new ...(+16) |

#### 第三方库
仅参考项目中观察到该模块代表函数，目标项目没有同模块可对齐证据：
- 参考项目：`os/libs/visionfive2-sd/src/lib.rs#send_cmd`，调用摘要：RawInterrupt::from, RawInterruptStatusReg::from, S::sleep_ms_until, Some, arg.into, assert! ...(+21)
- 参考项目：`os/libs/visionfive2-sd/src/lib.rs#init_sdcard`，调用摘要：CardTypeReg::from, CardTypeReg::from(0).with_card_width4_1, CmdArg::new, CmdReg::from, ControlReg::from, RawInterruptStatusReg::from ...(+18)
- 参考项目：`os/libs/visionfive2-sd/src/register.rs#from`，调用摘要：CmdReg::with_data, CmdReg::with_data(0, value.into()).with_transfer_dir, CmdReg::with_no_data, CmdReg::with_no_data(0, value.into())
                    .with_check_response_crc, CmdReg::with_no_data(0, value.into())
                    .with_check_response_crc(false)
                    .with_response_length, CmdReg::with_no_data(0, value.into()).with_check_response_crc ...(+3)
- 参考项目：`os/libs/visionfive2-sd/src/lib.rs#reset_clock`，调用摘要：ClockDividerReg::new, ClockDividerReg::new().with_clk_divider0, ClockEnableReg::from, CmdArg::new, CmdReg::from, CmdReg::from(0)
        .with_start_cmd ...(+7)

#### 通用模块/其他
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `os/src/sbi.rs#shutdown` | `os/src/sbi.rs#shutdown` | 同板块同名 | asm!, sbi_call | panic!, sbi_call |
| `os/src/sbi.rs#sbi_call` | `os/src/sbi.rs#sbi_call` | 同板块同名 | asm! | asm! |
| `os-la/src/main.rs#clear_bss` | `os/src/main.rs#clear_bss` | 同板块同名 | core::slice::from_raw_parts_mut, core::slice::from_raw_parts_mut(start as *mut u8, end - start).fill | core::slice::from_raw_parts_mut, core::slice::from_raw_parts_mut(sbss as usize as *mut u8, ebss as usize - sbss as usize)
            .fill |
| `os-la/src/sbi.rs#shutdown` | `os/src/boards/qemu.rs#shutdown` | 同板块同名 | (GED_SLEEP_CTL as *mut u8).write_volatile, core::arch::asm! | QEMU_EXIT_HANDLE.exit_success |


好的，我将遵循审计指令，基于提供的证据材料，对本案进行最终的定性仲裁。

### 1. 宏观判定
- **定性结论**: **独立开发 (底层架构显著差异)**
- **核心依据**: 客观 AST 相似度仅为 0.76%，共同 AST 指纹仅 17 个，在双方庞大的代码库中覆盖率极低（目标项目 1.17%，参考项目 2.13%）。该量化指标远未达到“代码抄袭”或“系统性复用”的阈值。更关键的是，在双方函数体量最大的【进程管理】模块，共同唯一 AST 指纹为 0，揭示了内核架构的本质差异。

### 2. 全模块覆盖矩阵解读
- **进程管理**
  1. 双方均存在该模块。
  2. 共同唯一 AST 指纹为 0。
  3. 目标项目 0.0%，参考项目 0.0%。覆盖率说明双方在进程管理的底层实现上无任何可观测的结构性重合。
  4. **证据强度: 未形成**。控方指控的“核心铁证”在参考项目中无对应 AST 证据，无法形成有效比对。

- **任务调度/异步运行时**
  1. 双方均存在该模块。
  2. 存在 1 个共同 AST 指纹。
  3. 目标项目 0.44%，参考项目 1.43%。覆盖率极低，表明调度机制的核心实现路径不同。
  4. **证据强度: 弱**。仅有的证据为跨板块对齐（如 `sys_linkat` 被归入此模块），不构成同板块主证据。

- **内存管理**
  1. 双方均存在该模块。
  2. 共同唯一 AST 指纹为 0。
  3. 目标项目 0.0%，参考项目 0.0%。覆盖率说明内存分配、映射等核心逻辑无结构重合。
  4. **证据强度: 未形成**。证据为跨板块对齐（如 `sys_openat` 被归入此模块），不构成同板块主证据。

- **中断/异常处理**
  1. 双方均存在该模块。
  2. 共同唯一 AST 指纹为 0。
  3. 目标项目 0.0%，参考项目 0.0%。覆盖率说明 Trap 处理、中断分发等底层机制实现不同。
  4. **证据强度: 未形成**。证据为跨板块硬凑（如 VirtIO 驱动 vs 管道读取），无实际比对意义。

- **系统调用**
  1. 双方均存在该模块。
  2. 存在 3 个共同 AST 指纹。
  3. 目标项目 33.33%，参考项目 7.5%。覆盖率较高，但证据集中在用户态标准封装。
  4. **证据强度: 弱**。证据为 `asm!` 宏和标准系统调用封装，属于 Rust 内联汇编和 Linux ABI 的领域约束，非原创逻辑。

- **文件系统**
  1. 双方均存在该模块。
  2. 共同唯一 AST 指纹为 0。
  3. 目标项目 0.0%，参考项目 0.0%。覆盖率说明文件系统核心逻辑无结构重合。
  4. **证据强度: 未形成**。证据为跨模块对齐（如 `sys_renameat2` vs FAT32 实现），或与第三方库的差异对比，不构成同板块主证据。

- **设备驱动**
  1. 双方均存在该模块。
  2. 共同唯一 AST 指纹为 0。
  3. 目标项目 0.0%，参考项目 0.0%。覆盖率说明驱动模型和实现完全不同。
  4. **证据强度: 未形成**。证据为跨板块硬凑（如 SBI 串口 vs 块设备读取），无实际比对意义。

- **网络协议栈**
  1. 双方均未体现该模块。
  2. 无共同 AST 指纹。
  3. 覆盖率均为 0.0%。
  4. **证据强度: 未形成**。

- **第三方库/公共依赖**
  1. 仅参考项目体现。
  2. 无共同 AST 指纹。
  3. 目标项目 0.0%，参考项目 0.0%。
  4. **证据强度: 未形成**。目标项目未使用该第三方库，体现了不同的技术选型。

### 3. 底层 AST 关键证据指纹表
| 所属板块 | 目标项目路径 | 参考项目路径 | 证据类型 | 技术说明 |
| :--- | :--- | :--- | :--- | :--- |
| 系统调用 | `user/src/syscall.rs#syscall` | `user/src/syscall.rs#syscall` | 同板块同名同结构 | 均为 `asm!` 宏实现系统调用指令，属于 RISC-V 架构和 Rust 语言的领域约束，非原创逻辑。 |
| 系统调用 | `user/src/syscall.rs#sys_write` | `user/src/syscall.rs#sys_write` | 同板块同名同结构 | 标准的系统调用封装，调用 `syscall` 函数，参数传递模式由 ABI 决定。 |
| 系统调用 | `user/src/lib.rs#exit` | `user/src/lib.rs#exit` | 同板块同名 | 对 `sys_exit` 的简单包装，属于用户库的标准做法。 |
| 通用模块/其他 | `os/src/sbi.rs#sbi_call` | `os/src/sbi.rs#sbi_call` | 同板块同名 | 均为 `asm!` 宏实现 SBI 调用，属于 RISC-V 架构的领域约束。 |
| 通用模块/其他 | `os-la/src/main.rs#clear_bss` | `os/src/main.rs#clear_bss` | 同板块同名 | 均为 `core::slice::from_raw_parts_mut(...).fill(0)` 的标准 Rust 写法，用于初始化 BSS 段。 |
| 任务调度/异步运行时 | `os-la/src/syscall/fs.rs#sys_ppoll` | `os/src/syscall/ppoll.rs#sys_ppoll` | 同板块同名 | 跨文件对齐，且调用流差异巨大。目标项目使用 `data[0..4].try_into()` 等手动解析，参考项目使用 `Box::from_raw` 等智能指针。 |
| 内存管理 | `os-la/src/syscall/fs.rs#sys_openat` | `os/src/syscall/fs.rs#sys_openat` | 同板块同名 | 跨模块对齐。目标项目调用 `alloc_fd_checked` 等自定义分配器，参考项目调用 `OpenFlags::from_bits` 等标志位解析。 |
| 中断/异常处理 | `os-la/src/drivers/virtio.rs#read_sector` | `os/src/fs/pipe.rs#read` | 同模块上下文对齐/差异观察 | 跨板块硬凑。目标为块设备驱动，参考为管道读取，外部对象完全不同。 |
| 文件系统 | `os-la/src/fs/ext4.rs#lookup_child` | `os/libs/ext4_rs/src/ext4_interface.rs#ext4_generic_open` | 同模块上下文对齐/差异观察 | 目标项目自行封装 ext4 操作，参考项目使用第三方库 `ext4_rs`，实现路径完全不同。 |
| 设备驱动 | `os-la/src/sbi.rs#console_putchar` | `os/src/drivers/block/virtio_blk.rs#read_block` | 同模块上下文对齐/差异观察 | 跨板块硬凑。目标为 SBI 串口输出，参考为块设备读取，无任何语义关联。 |

### 4. 核心代码调用流对比剖析
* **【进程管理/进程退出路径】**
* **底层技术析理**: 两者在进程退出的核心路径上展现了截然不同的内核对象管理模型。目标项目 `os-la` 的 `sys_exit` 直接调用 `task::request_exit`，表明其采用一种基于请求的异步或解耦退出机制。而参考项目 `os` 的 `sys_exit` 则直接调用 `exit_current_and_run_next`，并伴随 `panic!`，表明其采用在当前上下文中立即执行调度切换的同步退出模型。这种在核心控制流上的根本差异，是两者独立架构的有力证明。
* **代码溯源链接**:
  - 目标项目: `os-la/src/syscall/process.rs#sys_exit`
  - 参考项目: `os/src/syscall/process.rs#sys_exit`

* **【进程管理/时间获取路径】**
* **底层技术析理**: 在 `sys_gettimeofday` 的实现中，两者使用了完全不同的时间源和权限控制模型。目标项目 `os-la` 基于 `realtime_nanos()` 获取纳秒级时间，并通过 `div_euclid` 和 `to_le_bytes` 进行纯软件层面的数学转换，最后使用 `task::copy_to_user` 写回用户态。参考项目 `os` 则调用 `get_time_us` 获取微秒级时间，并显式地使用 `sstatus::clear_sum` 和 `sstatus::set_sum` 操作 RISC-V 的 SUM 位来控制内核访问用户态内存的权限。这体现了在时间子系统和内存访问策略上的独立设计。
* **代码溯源链接**:
  - 目标项目: `os-la/src/syscall/process.rs#sys_gettimeofday`
  - 参考项目: `os/src/syscall/process.rs#sys_gettimeofday`

* **【进程管理/信号发送路径】**
* **底层技术析理**: `sys_kill` 的实现揭示了完全不同的信号分发架构。目标项目 `os-la` 调用 `child_record_exists`、`mark_child_killed` 等函数，表明其维护了一套自定义的子进程记录和标记系统。参考项目 `os` 则通过 `pid2process` 查找进程控制块，然后调用 `SignalFlags::from_bits` 进行信号集操作。一个是基于子进程记录的间接管理，一个是基于 PID 查找和标志位操作的直接管理，架构差异显著。
* **代码溯源链接**:
  - 目标项目: `os-la/src/syscall/process.rs#sys_kill`
  - 参考项目: `os/src/syscall/process.rs#sys_kill`

### 5. 单方模块与降权项
- **单方模块**: 参考项目独有的【第三方库】模块（如 `visionfive2-sd`），表明两者在硬件平台支持和外部依赖选择上存在差异，目标项目未采用此技术路线。
- **降权项**:
  - **Linux ABI 强制的同名函数**: `sys_futex`, `sys_execve`, `sys_prctl` 等系统调用入口名由 Linux 标准强制规定，同名是领域约束，不能作为抄袭证据。
  - **Rust 标准库与核心库惯用法**: `core::str::from_utf8`, `to_le_bytes`, `copy_from_slice`, `try_into().unwrap()`, `saturating_add` 等是 Rust 语言处理字节、切片和算术运算的标准写法，任何 Rust OS 都会出现。
  - **编译器内置与日志宏**: `asm!`, `panic!`, `println!`, `trace!`, `file!`, `line!` 等是编译器或框架提供的通用机制，调用名必然相同。
  - **跨板块硬凑证据**: 控方及全模块对比中，将大量文件系统、设备驱动的函数与进程管理、内存管理模块的函数强行对齐，这些证据因外部对象和所属领域完全不同，必须降权为无效证据。

### 6. 保守结论
本案客观 AST 相似度仅 0.76%，共同指纹 17 个，覆盖率极低，量化层面完全不支持抄袭指控。控方提出的“核心铁证”在参考项目中缺乏对应的 AST 证据，证据链断裂。技术层面，双方在内核架构上存在本质差异：目标项目倾向于使用 `task::copy_to_user` 等抽象接口和自定义兼容层，而参考项目则大量使用 `current_task().unwrap().inner_exclusive_access` 直接操作任务内部结构及 RISC-V SUM 位控制。这些差异体现在进程退出、时间获取、信号分发等多个核心路径上，是独立设计的显著标志。综上，目标项目 `os-la` 不存在对参考项目 `os` 的代码抄袭或系统性复用行为，其原创性应予以肯定。

------------------------------

