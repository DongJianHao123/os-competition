# 🛡️ OS 内核赛自动化代码级架构审计报告

**审计时间**: `2026-07-02 11:31:24`
**参赛作品名称**: `2026-T202610486999845-XX`
**年份**: `2026`
**学校**: `XX`
**队伍名称**: `T202610486999845`
**比赛名字**: `OS-Competition`
**比赛子赛道**: `Kernel-Implementation`
**仓库地址**: `https://gitlab.eduxiji.net/educg-group-43501-3132633/T202610486999845-779`
**推测开发语言**: `Rust / C (由AST探针自动识别)`
**特征函数量**: `2812`

## ⚔️ 底层代码同源性对比 (AST Homology)
> 本部分由专门的代码审查智能体，对该项目与符合年份条件的历届最高相似度项目进行控制流与底层调用对比。新版报告会先给出确定性的全模块覆盖矩阵，再给出 Agent 的分模块解释，避免只围绕少量抽样函数展开。

### 🏆 Top 4 底层对标: vs `2022-S0S-哈尔滨工业大学`（哈尔滨工业大学 / S0S）

#### 对方作品信息

**对方年份**: `2022`
**对方学校**: `哈尔滨工业大学`
**对方队伍名称**: `S0S`
**对方比赛名字**: `OS-Competition`
**对方比赛子赛道**: `Kernel-Implementation`
**对方仓库地址**: `https://gitlab.eduxiji.net/educg-group-14158-894147/oskernel2022-s0s`

> 双向覆盖率：共同 AST 指纹: 10；唯一 AST 指纹: 目标项目 1449，参考项目 447；覆盖率: 目标项目 0.69%，参考项目 2.24%

## 全模块对比总览（确定性证据）

> 总体覆盖率：共同 AST 指纹: 10；唯一 AST 指纹: 目标项目 1449，参考项目 447；覆盖率: 目标项目 0.69%，参考项目 2.24%

### 1. 模块覆盖矩阵
| 模块 | 目标函数数 | 参考函数数 | 共同唯一 AST 指纹 | Target 覆盖 | Base 覆盖 | 状态 |
| :--- | ---: | ---: | ---: | ---: | ---: | :--- |
| 进程管理 | 896 | 51 | 1 | 0.23% | 2.13% | 双方存在且有结构重合 |
| 任务调度/异步运行时 | 407 | 41 | 1 | 0.44% | 2.5% | 双方存在且有结构重合 |
| 内存管理 | 321 | 139 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |
| 中断/异常处理 | 13 | 5 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |
| 系统调用 | 11 | 35 | 1 | 11.11% | 4.17% | 双方存在且有结构重合 |
| 文件系统 | 1039 | 166 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |
| 设备驱动 | 8 | 42 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |
| 网络协议栈 | 0 | 0 | 0 | 0.0% | 0.0% | 双方均未体现 |
| 文件系统/第三方库 | 50 | 0 | 0 | 0.0% | 0.0% | 仅目标项目体现 |
| 通用模块/其他 | 67 | 31 | 4 | 8.0% | 13.79% | 双方存在且有结构重合 |

### 2. 分模块证据清单

#### 进程管理
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `os-la/src/syscall/process.rs#sys_kill` | `os/src/syscall/process.rs#sys_kill` | 同板块同名 | child_record_exists, mark_child_killed, record_pending_signal, signal_has_user_handler, signal_is_ignored, task::request_exit | SignalFlags::from_bits, pid2task, task.inner_exclusive_access, task_ref.signals.contains, task_ref.signals.insert |
| `os-la/src/syscall/process.rs#sys_uname` | `os/src/syscall/process.rs#sys_uname` | 同板块同名 | put_uts_field, task::copy_to_user, task::copy_to_user(buf, &data).is_err, task::is_user_range_writable | UserBuffer::new, Utsname::new, buffer.read, current_user_token, size_of::<Utsname>, translated_byte_buffer ...(+1) |
| `os-la/src/syscall/process.rs#sys_nanosleep` | `os/src/syscall/process.rs#sys_nanosleep` | 同板块同名 | arm_current_thread_cancel_flag, current_thread_cancel_pending, maybe_interrupt_long_sleep, read_sleep_timespec, sleep_relative_timespec | current_user_token, get_time_ns, get_time_s, suspend_current_and_run_next, translated_refmut |
| `os-la/src/syscall/process.rs#sys_times` | `os/src/syscall/process.rs#sys_times` | 同板块同名 | 0u64.to_le_bytes, data[0..8].copy_from_slice, data[16..24].copy_from_slice, data[24..32].copy_from_slice, data[8..16].copy_from_slice, monotonic_millis ...(+3) | current_user_token, get_time, translated_refmut |

#### 任务调度/异步运行时
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `os-la/src/syscall/fs.rs#sys_readlinkat` | `os/src/syscall/fs.rs#sys_readlinkat` | 同板块同名 | DEFAULT_EXE_LINK.len, dangling_symlink_or_prefix_error, find_mem_file, has_test_eloop_chain, is_fd_symlink, is_runtime_libgcc_s_path ...(+24) | "/".as_bytes, UserBuffer::new, buf.read, current_user_token, println!, translated_byte_buffer ...(+2) |
| `os-la/src/syscall/fs.rs#sys_write` | `os/src/syscall/fs.rs#sys_write` | 同板块同名 | check_direct_io_user_buffer, entry.offset.saturating_add, fd_allows_write, is_proc_pid_oom_score_adj_path, matches!, notify_inotify_modify ...(+19) | UserBuffer::new, current_task, current_task().unwrap, current_user_token, drop, f.clone ...(+5) |
| `os-la/src/syscall/fs.rs#sys_sendfile` | `os/src/syscall/fs.rs#sys_sendfile` | 同板块同名 | Some, explicit_offset.as_mut, fast_sparse_sendfile, fd_allows_read, fd_allows_write, matches! ...(+9) | UserBuffer::new, current_task, current_task().unwrap, current_user_token, f.clone, fin.offset ...(+7) |
| `os-la/src/syscall/fs.rs#sys_fstat` | `os/src/syscall/fs.rs#sys_fstat` | 同板块同名 | fs.metadata, fs.metadata(entry.inode).map_err, fs_mut, fs_mut().and_then, loop_visible_size_bytes, mem_file_mode ...(+4) | UserBuffer::new, buf.read, core::mem::size_of::<Kstat>, current_task, current_task().unwrap, current_user_token ...(+5) |

#### 内存管理
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `os-la/src/syscall/fs.rs#sys_openat` | `os/src/syscall/fs.rs#sys_openat` | 同板块同名 | Ok, alloc_fd, alloc_fd_checked, alloc_fd_checked(FdEntry {
                kind: HANDLE_DEV_NULL,
                inode: 0,
                mem_index: usize::MAX,
                shared_offset: usize::MAX,
                offset: 0,
                path: effective_path,
                path_len: effective_len,
                fd_flags: open_fd_flags(flags),
                status_flags: open_status_flags(flags),
            })
            .unwrap_or_else, alloc_fd_checked(FdEntry {
                kind: HANDLE_LOOP_DEVICE,
                inode: 0,
                mem_index: index,
                shared_offset: usize::MAX,
                offset: 0,
                path: effective_path,
                path_len: effective_len,
                fd_flags: open_fd_flags(flags),
                status_flags: open_status_flags(flags),
            })
            .unwrap_or_else, build_tmpfile_path ...(+68) | FileClass::OSInode, OpenFlags::from_bits, OpenFlags::from_bits(flags).unwrap, Some, current_task, current_task().unwrap ...(+6) |
| `os/src/syscall/fs.rs#sys_fcntl` | `os/src/syscall/fs.rs#sys_fcntl` | 同板块同名 | add_pipe_endpoint_ref, alloc_fd_from, arg.max, ensure_shared_offset_slot, fcntl_get_lock, fcntl_get_owner_ex ...(+8) | current_task, current_task().unwrap, inner.alloc_fd, inner.fd_table.len, inner.fd_table[fd].clone, inner.fd_table[fd].is_some ...(+1) |
| `os-la/src/syscall/fs.rs#sys_mmap` | `os/src/syscall/process.rs#sys_mmap` | 同板块同名 | Some, copy_file_to_mapping, fd_allows_read, fd_allows_write, is_ltp_result_mapping_path, is_sparse_dirty_freeblocks_fd ...(+15) | MapFlags::from_bits, MapFlags::from_bits(flags).unwrap, ProtFlags::from_bits, ProtFlags::from_bits(prot).unwrap, UserBuffer::new, Vec::new ...(+20) |
| `os/src/trap/mod.rs#trap_handler` | `os/src/trap/mod.rs#trap_handler` | 同板块同名 | crate::syscall::should_treat_low_exec_fault_as_exit, deliver_trap_signal, fatal_trap_signal, finish_child_after_exit, maybe_deliver_signal, panic! ...(+7) | check_signals_error_of_current, current_add_signal, current_trap_cx, exit_current_and_run_next, handle_signals, panic! ...(+9) |

#### 中断/异常处理
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `os/src/drivers/virtio.rs#read_sector` | `os/src/trap/mod.rs#trap_from_kernel` | 同模块上下文对齐/差异观察 | Err, Ok, avail.idx.wrapping_add, avail_ring_mut, buffer.as_mut_ptr, core::mem::size_of::<VirtioBlkReqHeader> ...(+8) | panic!, println! |
| `os-la/src/drivers/virtio.rs#read_sector` | `os/src/trap/mod.rs#set_kernel_trap_entry` | 同模块上下文对齐/差异观察 | (self.pci.isr as *const u8).read_volatile, (self.pci.notify_addr as *mut u16).write_volatile, Err, Ok, avail.idx.wrapping_add, avail_ring_mut ...(+9) | stvec::write |
| `os-la/src/arch.rs#write_stvec` | `os/src/trap/mod.rs#set_user_trap_entry` | 同模块上下文对齐/差异观察 | asm! | stvec::write |
| `os-la/src/trap/mod.rs#fatal_trap_signal` | `os/src/trap/mod.rs#enable_timer_interrupt` | 同模块上下文对齐/差异观察 | 内部闭环逻辑 | sie::set_stimer |

#### 系统调用
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `user/src/syscall.rs#syscall` | `user/src/syscall.rs#syscall` | 同板块同名 | asm! | asm! |
| `user/src/syscall.rs#sys_write` | `user/src/syscall.rs#sys_write` | 同板块同名 | buffer.as_ptr, buffer.len, syscall | buffer.as_ptr, buffer.len, syscall |
| `user/src/syscall.rs#sys_exit` | `user/src/syscall.rs#sys_exit` | 同板块同名 | syscall | panic!, syscall |
| `user/src/lib.rs#exit` | `user/src/lib.rs#exit` | 同板块同名 | sys_exit | sys_exit |

#### 文件系统
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `os-la/src/syscall/fs.rs#sys_renameat2` | `os/src/syscall/fs.rs#sys_renameat2` | 同板块同名 | MEM_FILES[existing].path.fill, MEM_FILES[existing].path[..old_len].copy_from_slice, MEM_FILES[index].path.fill, MEM_FILES[index].path[..new_len].copy_from_slice, Some, directory_has_children ...(+14) | current_user_token, rename, translated_str |
| `os-la/src/syscall/fs.rs#sys_faccessat` | `os/src/syscall/fs.rs#sys_faccessat` | 同板块同名 | find_mem_file, is_ltp_noop_tool, is_runtime_libgcc_s_path, is_virtual_busybox_applet, is_virtual_directory_path, long_path_or_eloop_error ...(+13) | current_user_token, open_file_at, open_file_at(dirfd, path.as_str(), OpenFlags::RDWR).is_some, path.as_str, translated_str |
| `os-la/src/syscall/fs.rs#sys_mount` | `os/src/syscall/fs.rs#sys_mount` | 同板块同名 | RO_MOUNT_PATH.fill, RO_MOUNT_PATH[..target_len].copy_from_slice, bind_dummy_mount, crate::syscall::current_euid_for_fs, dummy_mount_has_open_file, dummy_mount_path_matches ...(+11) | 内部闭环逻辑 |
| `os-la/src/syscall/fs.rs#sys_mkdirat` | `os/src/syscall/fs.rs#sys_mkdirat` | 同板块同名 | create_mem_file, create_mem_file(&path[..path_len], true)
        .map, create_mem_file(&path[..path_len], true)
        .map(|index| {
            set_mem_mode(index, mode);
            inherit_parent_sgid_for_new_dir(index, &path[..path_len]);
            notify_inotify_create(&path[..path_len], true);
            0
        })
        .unwrap_or_else, find_mem_file, find_mem_file(&path[..path_len]).is_some, has_test_eloop_chain ...(+8) | current_user_token, mkdir_at, path.as_str, translated_str |

#### 设备驱动
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `os-la/src/drivers/virtio.rs#pci_cfg_addr` | `os/src/drivers/block/sdcard.rs#read_sector` | 同模块上下文对齐/差异观察 | 内部闭环逻辑 | Err, Ok, assert!, chunk.iter_mut, chunk.iter_mut().zip, data_buf.chunks_mut ...(+6) |
| `os-la/src/drivers/virtio.rs#new` | `os/src/drivers/block/sdcard.rs#write_sector` | 同模块上下文对齐/差异观察 | 内部闭环逻辑 | (data_buf.len() / SEC_LEN).try_into, (data_buf.len() / SEC_LEN).try_into().unwrap, Err, Ok, assert!, chunk.iter ...(+9) |
| `os-la/src/sbi.rs#console_putchar` | `os/src/drivers/block/sdcard.rs#get_cidregister` | 同模块上下文对齐/差异观察 | (UART0_BASE as *mut u8).write_volatile | Err, Ok, self.end_cmd, self.get_response, self.read_data, self.send_cmd ...(+2) |
| `os-la/src/drivers/virtio.rs#queue_base_addr` | `fat32/src/block_cache.rs#get_block_cache` | 同模块上下文对齐/差异观察 | core::ptr::addr_of! | Arc::clone, Arc::new, Arc::strong_count, BlockCache::new, RwLock::new, panic! ...(+8) |

#### 文件系统/第三方库
仅目标项目中观察到该模块代表函数，参考项目没有同模块可对齐证据：
- 目标项目：`os-la/src/fs/ext4.rs#lookup_child`，调用摘要：Err, Ok, Some, core::str::from_utf8, div_ceil_u64, node_kind ...(+6)
- 目标项目：`os/src/fs/ext4.rs#lookup_child`，调用摘要：Err, Ok, Some, core::str::from_utf8, div_ceil_u64, node_kind ...(+6)
- 目标项目：`os-la/src/fs/ext4.rs#for_each_child`，调用摘要：Err, Ok, core::str::from_utf8, div_ceil_u64, node_kind, read_u16 ...(+6)
- 目标项目：`os/src/fs/ext4.rs#for_each_child`，调用摘要：Err, Ok, core::str::from_utf8, div_ceil_u64, node_kind, read_u16 ...(+6)

#### 通用模块/其他
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `os/src/sbi.rs#shutdown` | `os/src/sbi.rs#shutdown` | 同板块同名 | asm!, sbi_call | panic!, sbi_call |
| `os/src/sbi.rs#sbi_call` | `os/src/sbi.rs#sbi_call` | 同板块同名 | asm! | asm! |
| `os-la/src/main.rs#clear_bss` | `os/src/main.rs#clear_bss` | 同板块同名 | core::slice::from_raw_parts_mut, core::slice::from_raw_parts_mut(start as *mut u8, end - start).fill | core::slice::from_raw_parts_mut, core::slice::from_raw_parts_mut(sbss as usize as *mut u8, ebss as usize - sbss as usize)
            .fill |
| `os-la/src/fallback.rs#_start` | `user/src/lib.rs#_start` | 同板块同名 | 内部闭环逻辑 | ((argv + i * core::mem::size_of::<usize>()) as *const usize).read_volatile, ((str_start + *i) as *const u8).read_volatile, (0usize..)
            .find, (0usize..)
            .find(|i| unsafe { ((str_start + *i) as *const u8).read_volatile() == 0 })
            .unwrap, HEAP.lock, HEAP.lock()
            .init ...(+10) |


好的，我将遵循审计指令，基于提供的证据材料，进行最终的定性仲裁。

### 1. 宏观判定
- **定性结论**: **独立开发，底层架构显著差异**
  - **相似度**: 0.53%
  - **共同 AST 指纹**: 10
  - **唯一 AST 指纹**: 目标项目 1449，参考项目 447
  - **覆盖率**: 目标项目 0.69%，参考项目 2.24%
  - **判定依据**: 客观 AST 覆盖率处于背景噪声水平。双方在内存管理、文件系统、设备驱动等核心子系统中的共同 AST 指纹均为 0，表明底层实现架构完全不同。控方所指控的“复用铁证”实为 Linux/POSIX 系统调用规范、公开测试套件（LTP/lmbench）兼容层命名及 Rust 语言惯用法导致的必然收敛，不构成代码抄袭。

### 2. 全模块覆盖矩阵解读
- **进程管理**:
  - 双方均存在该模块。
  - 存在 1 个共同 AST 指纹。
  - 目标覆盖率 0.23%，参考覆盖率 2.13%。覆盖率极低，表明在 896 vs 51 的巨大函数体量差异下，仅存在 1 个通用结构模式重合。
  - 证据强度: **弱**。控方列举的多数同名函数（如 `sys_kill`, `sys_uname`）调用图谱已发生变异，而 `sys_futex` 等函数的相似性源于 Linux 规范与 LTP 兼容性需求。

- **任务调度/异步运行时**:
  - 双方均存在该模块。
  - 存在 1 个共同 AST 指纹。
  - 目标覆盖率 0.44%，参考覆盖率 2.5%。覆盖率极低，说明调度框架设计基本独立。
  - 证据强度: **弱**。共同指纹未指向核心调度逻辑，同名函数如 `sys_readlinkat` 等属于文件系统范畴，其调用图谱差异巨大。

- **内存管理**:
  - 双方均存在该模块。
  - 共同 AST 指纹为 0。
  - 目标覆盖率 0.0%，参考覆盖率 0.0%。这是核心子系统完全异构的铁证。
  - 证据强度: **未形成**。双方在内存抽象（如 `os-la` 的独立模型 vs `os` 的 `MemorySet`/`VirtAddr`）上无任何 AST 层面重合。

- **中断/异常处理**:
  - 双方均存在该模块。
  - 共同 AST 指纹为 0。
  - 目标覆盖率 0.0%，参考覆盖率 0.0%。Trap 处理逻辑无共同结构。
  - 证据强度: **未形成**。证据显示双方 Trap 处理路径（`trap_handler`）调用的内部函数完全不同。

- **系统调用**:
  - 双方均存在该模块。
  - 存在 1 个共同 AST 指纹。
  - 目标覆盖率 11.11%，参考覆盖率 4.17%。覆盖率主要由用户态 `asm!` 宏调用贡献。
  - 证据强度: **弱**。`asm!` 是 RISC-V 架构下触发系统调用的唯一方式，属于硬件规范决定的必然写法。

- **文件系统**:
  - 双方均存在该模块。
  - 共同 AST 指纹为 0。
  - 目标覆盖率 0.0%，参考覆盖率 0.0%。这是核心子系统完全异构的铁证。
  - 证据强度: **未形成**。`os-la` 基于 `ext4`/`mem_file`，而 `os` 基于 rCore 的 `inode`/`UserBuffer`，架构截然不同。

- **设备驱动**:
  - 双方均存在该模块。
  - 共同 AST 指纹为 0。
  - 目标覆盖率 0.0%，参考覆盖率 0.0%。驱动模型完全不同。
  - 证据强度: **未形成**。`os-la` 使用 `virtio` 驱动，`os` 使用 `sdcard` 驱动，无任何共同结构。

- **网络协议栈**:
  - 双方均未体现该模块。
  - 证据强度: **未形成**。

- **文件系统/第三方库**:
  - 仅目标项目体现。
  - 共同 AST 指纹为 0。
  - 证据强度: **未形成**。此为单方模块，无法构成同源证据。

- **通用模块/其他**:
  - 双方均存在该模块。
  - 存在 4 个共同 AST 指纹。
  - 目标覆盖率 8.0%，参考覆盖率 13.79%。覆盖率相对较高，但均为启动样板代码。
  - 证据强度: **弱**。共同指纹指向 `clear_bss`、`sbi_call` 等 RISC-V 内核启动的必要步骤，属于硬件平台初始化的标准模板。

### 3. 底层 AST 关键证据指纹表
| 所属板块 | 目标项目路径 | 参考项目路径 | 证据类型 | 技术说明 |
| :--- | :--- | :--- | :--- | :--- |
| 通用模块/其他 | `os-la/src/main.rs#clear_bss` | `os/src/main.rs#clear_bss` | 同板块同名 | RISC-V 内核启动清零 BSS 段的标准操作，使用 `core::slice::from_raw_parts_mut` 和 `fill`。 |
| 通用模块/其他 | `os-la/src/sbi.rs#sbi_call` | `os/src/sbi.rs#sbi_call` | 同板块同名 | RISC-V SBI 规范定义的 ecall 调用，必须使用内联汇编 `asm!` 实现。 |
| 系统调用 | `user/src/syscall.rs#syscall` | `user/src/syscall.rs#syscall` | 同板块同名 | 用户态触发系统调用的 `asm!` 指令，由 RISC-V 指令集架构决定。 |
| 进程管理 | `os-la/src/syscall/process.rs#sys_futex` | `os/src/syscall/process.rs#sys_futex` | 同板块证据 | 调用 `ltp_tgkill02_checkpoint_futex` 等 LTP 测试兼容函数，属于面向公开测试套件的描述性命名收敛。 |
| 进程管理 | `os-la/src/syscall/process.rs#sys_capset` | `os/src/syscall/process.rs#sys_capset` | 同板块证据 | 按 Linux `capset` ABI 解析 `data[base+4..base+8]` 等固定偏移量，是解析标准结构的唯一合理写法。 |
| 进程管理 | `os-la/src/syscall/process.rs#sys_semtimedop` | `os/src/syscall/process.rs#sys_semtimedop` | 同板块证据 | 实现 System V 信号量 `saturating_add/sub` 语义，由 POSIX 标准强制规定。 |
| 进程管理 | `os-la/src/syscall/process.rs#sys_prlimit64` | `os/src/syscall/process.rs#sys_prlimit64` | 同板块证据 | 解析 `struct rlimit64` 的 `data[0..8]` 和 `data[8..16]` 字段，由 Linux ABI 强制规定。 |
| 进程管理 | `os-la/src/syscall/process.rs#sys_epoll_pwait` | `os/src/syscall/fs.rs#sys_epoll_pwait` | 同板块证据 | 实现 `epoll_wait` 标准流程：遍历兴趣列表、检查就绪状态、处理挂起信号。 |
| 进程管理 | `os-la/src/syscall/process.rs#sys_semctl` | `os/src/syscall/process.rs#sys_semctl` | 同板块证据 | 实现 System V IPC `*ctl` 操作的标准流程：查找对象、检查权限、拷贝数据、填充时间戳。 |
| 进程管理 | `os-la/src/syscall/process.rs#sys_prctl` | `os/src/syscall/process.rs#sys_prctl` | 同板块证据 | 实现 Linux `prctl` 的 `PR_SET_NAME`/`PR_GET_NAME` 等标准选项，调用 `write_user_i32` 等惯用模式。 |

### 4. 核心代码调用流对比剖析
* **【进程管理/sys_futex 路径】**
* **底层技术析理**: 双方均实现了 `futex` 系统调用，并包含针对 LTP 测试套件的兼容性处理。然而，`os-la` 的调用流包含 `finish_robust_owner_for_wait`，而 `os` 的调用流包含 `read_user_i32`，表明内部实现存在差异。`ltp_tgkill02_checkpoint_futex` 等函数名是面向公开测试用例的描述性命名，任何旨在通过该测试的内核都可能采用类似命名，属于测试驱动的自然收敛，而非代码抄袭。
* **代码溯源链接**:
  - 目标项目: `os-la/src/syscall/process.rs#sys_futex`
  - 参考项目: `os/src/syscall/process.rs#sys_futex`

* **【文件系统/sys_openat 路径】**
* **底层技术析理**: 双方均实现了 `openat` 系统调用，但底层架构完全不同。`os-la` 的调用流围绕 `alloc_fd_checked`、`HANDLE_DEV_NULL`、`lookup_ext4_path` 等自有的 ext4 文件系统抽象构建。而 `os` 的调用流则基于 `FileClass::OSInode`、`OpenFlags::from_bits` 等 rCore 风格的 inode 抽象。两者在对象模型、路径解析和资源管理上无任何共同 AST 指纹，是独立实现的典型案例。
* **代码溯源链接**:
  - 目标项目: `os-la/src/syscall/fs.rs#sys_openat`
  - 参考项目: `os/src/syscall/fs.rs#sys_openat`

* **【内存管理/sys_mmap 路径】**
* **底层技术析理**: 双方均实现了 `mmap` 系统调用，但内存管理模型截然不同。`os-la` 的调用流涉及 `copy_file_to_mapping`、`is_ltp_result_mapping_path` 等自定义逻辑。而 `os` 的调用流则基于 `MapFlags::from_bits`、`ProtFlags::from_bits`、`UserBuffer::new` 等 rCore 框架的标准抽象。两者在内存区域创建、标志位解析和用户态数据拷贝方式上无任何共同 AST 指纹，证明了独立的内存管理子系统设计。
* **代码溯源链接**:
  - 目标项目: `os-la/src/syscall/fs.rs#sys_mmap`
  - 参考项目: `os/src/syscall/process.rs#sys_mmap`

### 5. 单方模块与降权项
- **单方模块**: `文件系统/第三方库` 模块仅存在于目标项目，包含 `ext4.rs` 等文件系统实现。参考项目无此模块，无法形成同源对比，不构成证据。
- **降权项**:
  - **同名异构函数**: `sys_kill`, `sys_uname`, `sys_nanosleep`, `sys_times`, `sys_clock_gettime` 等函数虽同名同板块，但其内部调用图谱已发生显著变异，证明并非直接复用。
  - **内部闭环逻辑**: `sys_getpid`, `sys_exit`, `sys_sched_yield` 等函数逻辑过于简单，缺乏特化的外部调用链，不能作为抄袭证据。
  - **公共接口与泛型函数名**: `matches!`, `Ok`, `Err`, `asm!`, `Vec::new` 等是 Rust 语言标准库或核心库的公共接口，在任何 Rust 项目中都会出现，必须从证据中剔除。

### 6. 保守结论
本次审计的核心结论是**独立开发**。客观 AST 覆盖率（0.53%）处于背景噪声水平，双方在内存管理、文件系统、设备驱动等核心子系统上的共同指纹均为 0，揭示了完全不同的底层架构设计。控方提出的“复用铁证”均能被合理解释为 Linux/POSIX 系统调用规范、公开测试套件兼容性需求或 Rust 语言惯用法所导致的必然收敛。特别是针对 LTP 测试的兼容函数，其命名和调用序列的相似性是测试驱动开发下的自然结果，而非代码抄袭。综上，目标项目 `2026-T202610486999845-XX` 与参考项目 `2022-S0S-哈尔滨工业大学` 不存在可认定的代码复用或抄袭行为。

------------------------------

