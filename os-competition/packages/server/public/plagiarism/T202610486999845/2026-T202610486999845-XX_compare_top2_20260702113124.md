# 🛡️ OS 内核赛自动化代码级架构审计报告

**审计时间**: `2026-07-02 11:31:24`
**参赛作品名称**: `2026-T202610486999845-XX`
**年份**: `2026`
**学校**: `XX`
**队伍名称**: `T202610486999845`
**比赛名字**: `OS-Competition`
**比赛子赛道**: `Kernel-Implementation`
**仓库地址**: `https://gitlab.eduxiji.net/educg-group-43501-3132633/T202610486999845-779`
**推测开发语言**: `Rust / C (由AST探针自动识别)`
**特征函数量**: `2812`

## ⚔️ 底层代码同源性对比 (AST Homology)
> 本部分由专门的代码审查智能体，对该项目与符合年份条件的历届最高相似度项目进行控制流与底层调用对比。新版报告会先给出确定性的全模块覆盖矩阵，再给出 Agent 的分模块解释，避免只围绕少量抽样函数展开。

### 🏆 Top 2 底层对标: vs `2024-ᕕڼᕗ旺仔-哈尔滨工业大学`（哈尔滨工业大学 / ᕕ(◠ڼ◠)ᕗ旺仔）

#### 对方作品信息

**对方年份**: `2024`
**对方学校**: `哈尔滨工业大学`
**对方队伍名称**: `ᕕ(◠ڼ◠)ᕗ旺仔`
**对方比赛名字**: `OS-Competition`
**对方比赛子赛道**: `Kernel-Implementation`
**对方仓库地址**: `https://gitlab.eduxiji.net/educg-group-26010-2376550/T202410213992561-2280`

> 双向覆盖率：共同 AST 指纹: 14；唯一 AST 指纹: 目标项目 1449，参考项目 834；覆盖率: 目标项目 0.97%，参考项目 1.68%

## 全模块对比总览（确定性证据）

> 总体覆盖率：共同 AST 指纹: 14；唯一 AST 指纹: 目标项目 1449，参考项目 834；覆盖率: 目标项目 0.97%，参考项目 1.68%

### 1. 模块覆盖矩阵
| 模块 | 目标函数数 | 参考函数数 | 共同唯一 AST 指纹 | Target 覆盖 | Base 覆盖 | 状态 |
| :--- | ---: | ---: | ---: | ---: | ---: | :--- |
| 进程管理 | 896 | 68 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |
| 任务调度/异步运行时 | 407 | 55 | 1 | 0.44% | 1.92% | 双方存在且有结构重合 |
| 内存管理 | 321 | 180 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |
| 中断/异常处理 | 13 | 13 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |
| 系统调用 | 11 | 56 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |
| 文件系统 | 1039 | 200 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |
| 设备驱动 | 8 | 7 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |
| 网络协议栈 | 0 | 0 | 0 | 0.0% | 0.0% | 双方均未体现 |
| 文件系统/第三方库 | 50 | 273 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |
| 通用模块/其他 | 67 | 86 | 10 | 20.0% | 12.05% | 双方存在且有结构重合 |

### 2. 分模块证据清单

#### 进程管理
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `os-la/src/syscall/process.rs#sys_wait4` | `os/src/syscall/process.rs#sys_wait4` | 同板块同名 | (status as i32).to_le_bytes, ChildRecord::empty, task::copy_to_user, task::copy_to_user(status_ptr, &(status as i32).to_le_bytes()).is_err | WaitFuture::new, WaitOption::from_bits, WaitOption::from_bits(options).unwrap, trace! |
| `os-la/src/syscall/process.rs#sys_nanosleep` | `os/src/syscall/process.rs#sys_nanosleep` | 同板块同名 | arm_current_thread_cancel_flag, current_thread_cancel_pending, maybe_interrupt_long_sleep, read_sleep_timespec, sleep_relative_timespec | Duration::from_millis, TimeoutFuture::new, trace! |
| `os-la/src/syscall/process.rs#sys_getpgid` | `os/src/syscall/process.rs#sys_getpgid` | 同板块同名 | sys_getpid | sys_getpid, trace!, warn! |
| `os-la/src/syscall/process.rs#sys_execve` | `os/src/syscall/process.rs#sys_execve` | 同板块同名 | exec_resolved_inode, fs::resolve_exec_inode | Err, Ok, Path::from, Vec::new, all_data.as_slice, app_inode.read_all ...(+15) |

#### 任务调度/异步运行时
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `os-la/src/syscall/fs.rs#sys_write` | `os/src/syscall/fs.rs#sys_write` | 同板块同名 | check_direct_io_user_buffer, entry.offset.saturating_add, fd_allows_write, is_proc_pid_oom_score_adj_path, matches!, notify_inotify_modify ...(+19) | Err, Ok, core::slice::from_raw_parts, current_process, fdinfo
            .file
            .write, inner.fd_table.get ...(+1) |
| `os-la/src/trap/context.rs#set_sp` | `os/src/trap/context.rs#set_sp` | 同板块同名同结构 | 内部闭环逻辑 | 内部闭环逻辑 |
| `os/src/trap/context.rs#set_sp` | `os/src/trap/context.rs#set_global_pointer` | 同板块同结构 | 内部闭环逻辑 | 内部闭环逻辑 |
| `os-la/src/syscall/fs.rs#sys_connect` | `os/src/syscall/mod.rs#syscall` | 同模块上下文对齐/差异观察 | crate::syscall::current_pid_for_fs, find_bound_socket, find_bound_unix_socket, find_listening_socket, find_listening_socket(port, SOCKETS[index].domain).or_else, normalize_unix_socket_path ...(+7) | dummy, sys_brk, sys_chdir, sys_clock_getres, sys_clock_gettime, sys_clone ...(+46) |

#### 内存管理
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `os-la/src/syscall/fs.rs#sys_openat` | `os/src/syscall/fs.rs#sys_openat` | 同板块同名 | Ok, alloc_fd, alloc_fd_checked, alloc_fd_checked(FdEntry {
                kind: HANDLE_DEV_NULL,
                inode: 0,
                mem_index: usize::MAX,
                shared_offset: usize::MAX,
                offset: 0,
                path: effective_path,
                path_len: effective_len,
                fd_flags: open_fd_flags(flags),
                status_flags: open_status_flags(flags),
            })
            .unwrap_or_else, alloc_fd_checked(FdEntry {
                kind: HANDLE_LOOP_DEVICE,
                inode: 0,
                mem_index: index,
                shared_offset: usize::MAX,
                offset: 0,
                path: effective_path,
                path_len: effective_len,
                fd_flags: open_fd_flags(flags),
                status_flags: open_status_flags(flags),
            })
            .unwrap_or_else, build_tmpfile_path ...(+68) | Err, FdInfo::default_flags, Ok, OpenFlags::from_bits, OpenFlags::from_bits(flags).ok_or, Path::from ...(+15) |
| `os/src/syscall/fs.rs#sys_fcntl` | `os/src/syscall/fs.rs#sys_fcntl` | 同板块同名 | add_pipe_endpoint_ref, alloc_fd_from, arg.max, ensure_shared_offset_slot, fcntl_get_lock, fcntl_get_owner_ex ...(+8) | Err, FcntlFlags::FD_CLOEXEC.bits, FcntlFlags::from_bits, FcntlFlags::from_bits(arg as u32).ok_or, Ok, OpenFlags::from_bits ...(+17) |
| `os-la/src/syscall/fs.rs#sys_mmap` | `os/src/syscall/mm.rs#sys_mmap` | 同板块同名 | Some, copy_file_to_mapping, fd_allows_read, fd_allows_write, is_ltp_result_mapping_path, is_sparse_dirty_freeblocks_fd ...(+15) | Err, MMAPPROT::from_bits, MMAPPROT::from_bits(prot as u32).ok_or, MmapFlags::from_bits, MmapFlags::from_bits(flags as u32).ok_or, Ok ...(+26) |
| `os/src/trap/mod.rs#trap_handler` | `os/src/trap/mod.rs#trap_handler` | 同板块同名 | crate::syscall::should_treat_low_exec_fault_as_exit, deliver_trap_signal, fatal_trap_signal, finish_child_after_exit, maybe_deliver_signal, panic! ...(+7) | PageTable::from_token, VirtAddr::from, VirtAddr::from(stval).floor, current_trap_cx, cx.set_entry_point, error! ...(+17) |

#### 中断/异常处理
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `os/src/drivers/virtio.rs#read_sector` | `os/src/syscall/fs.rs#sys_sendfile` | 同模块上下文对齐/差异观察 | Err, Ok, avail.idx.wrapping_add, avail_ring_mut, buffer.as_mut_ptr, core::mem::size_of::<VirtioBlkReqHeader> ...(+8) | Err, Ok, in_file.read, in_file.seek, in_file.seek(*offset as usize).ok_or, offset.is_null ...(+6) |
| `os-la/src/drivers/virtio.rs#read_sector` | `os/src/fs/tty.rs#new` | 同模块上下文对齐/差异观察 | (self.pci.isr as *const u8).read_volatile, (self.pci.notify_addr as *mut u16).write_volatile, Err, Ok, avail.idx.wrapping_add, avail_ring_mut ...(+9) | 内部闭环逻辑 |
| `os-la/src/arch.rs#write_stvec` | `user/src/lib.rs#nanosleep` | 同模块上下文对齐/差异观察 | asm! | Err, Ok, Some, ret.into, sys_nanosleep |
| `os-la/src/trap/mod.rs#fatal_trap_signal` | `os/src/mm/user_check/mod.rs#try_write_user` | 同模块上下文对齐/差异观察 | 内部闭环逻辑 | Some, __try_write_user_u8 |

#### 系统调用
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `user/src/syscall.rs#syscall` | `user/src/syscall.rs#syscall` | 同板块同名 | asm! | asm! |
| `user/src/console.rs#write_str` | `user/src/console.rs#write_str` | 同板块同名 | Ok, s.as_bytes, write | Ok, s.as_bytes, sys_write |
| `user/src/syscall.rs#sys_write` | `user/src/syscall.rs#sys_write` | 同板块同名 | buffer.as_ptr, buffer.len, syscall | buf.as_ptr, buf.len, syscall |
| `user/src/syscall.rs#sys_exit` | `user/src/syscall.rs#sys_exit` | 同板块同名 | syscall | syscall, unreachable! |

#### 文件系统
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `os-la/src/syscall/fs.rs#sys_faccessat` | `os/src/syscall/fs.rs#sys_faccessat` | 同板块同名 | find_mem_file, is_ltp_noop_tool, is_runtime_libgcc_s_path, is_virtual_busybox_applet, is_virtual_directory_path, long_path_or_eloop_error ...(+13) | FaccessatFlags::from_bits, FaccessatFlags::from_bits(amode).ok_or, FcntlFlags::from_bits, FcntlFlags::from_bits(flag).ok_or, Ok, OpenFlags::empty ...(+5) |
| `os-la/src/syscall/fs.rs#sys_getdents64` | `os/src/syscall/fs.rs#sys_getdents64` | 同板块同名 | Err, Ok, Some, len.saturating_sub, read_next_ext4_dirent, read_next_mem_dirent | Err, Ok, core::slice::from_raw_parts_mut, dirents_dst[..src_len].copy_from_slice, dirents_src.len, file.get_meta ...(+9) |
| `os-la/src/syscall/fs.rs#sys_mkdirat` | `os/src/syscall/fs.rs#sys_mkdirat` | 同板块同名 | create_mem_file, create_mem_file(&path[..path_len], true)
        .map, create_mem_file(&path[..path_len], true)
        .map(|index| {
            set_mem_mode(index, mode);
            inherit_parent_sgid_for_new_dir(index, &path[..path_len]);
            notify_inotify_create(&path[..path_len], true);
            0
        })
        .unwrap_or_else, find_mem_file, find_mem_file(&path[..path_len]).is_some, has_test_eloop_chain ...(+8) | Path::from, c_str_to_string, create_dir, trace! |
| `user/src/lang_items.rs#panic_handler` | `user/src/lang_items.rs#panic_handler` | 同板块同名 | panic_info.location, panic_info.message, println! | exit, panic_info.location, panic_info.message, panic_info.message().unwrap, println! |

#### 设备驱动
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `os-la/src/drivers/virtio.rs#pci_cfg_addr` | `os/src/drivers/block/mod.rs#block_device_test` | 同模块上下文对齐/差异观察 | 内部闭环逻辑 | BLOCK_DEVICE.clone, assert_eq!, block_device.read_block, block_device.write_block, println!, write_buffer.iter_mut |
| `os-la/src/drivers/virtio.rs#new` | `os/src/drivers/block/virtio_blk.rs#read_block` | 同模块上下文对齐/差异观察 | 内部闭环逻辑 | self.0
            .exclusive_access, self.0
            .exclusive_access()
            .read_block, self.0
            .exclusive_access()
            .read_block(block_id, buf)
            .expect |
| `os-la/src/sbi.rs#console_putchar` | `os/src/drivers/block/virtio_blk.rs#write_block` | 同模块上下文对齐/差异观察 | (UART0_BASE as *mut u8).write_volatile | self.0
            .exclusive_access, self.0
            .exclusive_access()
            .write_block, self.0
            .exclusive_access()
            .write_block(block_id, buf)
            .expect |
| `os-la/src/drivers/virtio.rs#queue_base_addr` | `os/src/drivers/block/virtio_blk.rs#new` | 同模块上下文对齐/差异观察 | core::ptr::addr_of! | Self, UPSafeCell::new, VirtIOBlk::<VirtioHal>::new, VirtIOBlk::<VirtioHal>::new(&mut *(VIRTIO0 as *mut VirtIOHeader)).unwrap |

#### 文件系统/第三方库
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `os-la/src/fs/ext4.rs#lookup_child` | `ext4/src/ext4_interface.rs#ext4_generic_open` | 同模块上下文对齐/差异观察 | Err, Ok, Some, core::str::from_utf8, div_ceil_u64, node_kind ...(+6) | DirEntryType::EXT4_DE_DIR.bits, Errnum::ENOENT.into, Ext4DirEntry::default, Ext4DirSearchResult::new, Ext4InodeRef::get_inode_ref, Ext4InodeRef::new ...(+17) |
| `os/src/fs/ext4.rs#lookup_child` | `ext4/src/ext4_interface.rs#ext4_file_read` | 同模块上下文对齐/差异观察 | Err, Ok, Some, core::str::from_utf8, div_ceil_u64, node_kind ...(+6) | Ext4InodeRef::get_inode_ref, Ok, core::cmp::min, inode_ref.get_inode_dblk_idx, inode_ref.inner.inode.ext4_inode_type, inode_ref.inner.inode.inode_get_size ...(+8) |
| `os-la/src/fs/ext4.rs#for_each_child` | `ext4/src/ext4_interface.rs#ext4_generic_open_from` | 同模块上下文对齐/差异观察 | Err, Ok, core::str::from_utf8, div_ceil_u64, node_kind, read_u16 ...(+6) | DirEntryType::EXT4_DE_DIR.bits, Errnum::ENOENT.into, Ext4DirEntry::default, Ext4DirSearchResult::new, Ext4InodeRef::get_inode_ref, Ext4InodeRef::new ...(+17) |
| `os/src/fs/ext4.rs#for_each_child` | `ext4/src/ext4_interface.rs#ext4_generic_open2` | 同模块上下文对齐/差异观察 | Err, Ok, core::str::from_utf8, div_ceil_u64, node_kind, read_u16 ...(+6) | DirEntryType::EXT4_DE_DIR.bits, Errnum::ENOENT.into, Ext4DirEntry::default, Ext4DirSearchResult::new, Ext4InodeRef::get_inode_ref, Ext4InodeRef::new ...(+16) |

#### 通用模块/其他
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `os-la/src/logging.rs#log` | `os/src/logging.rs#log` | 同板块同名同结构 | println!, record.level, record.metadata, self.enabled | println!, record.level, record.metadata, self.enabled |
| `os/src/sbi.rs#shutdown` | `os/src/sbi.rs#shutdown` | 同板块同名 | asm!, sbi_call | panic!, sbi_call |
| `os/src/sbi.rs#sbi_call` | `os/src/sbi.rs#sbi_call` | 同板块同名 | asm! | asm! |
| `os-la/src/main.rs#clear_bss` | `os/src/main.rs#clear_bss` | 同板块同名 | core::slice::from_raw_parts_mut, core::slice::from_raw_parts_mut(start as *mut u8, end - start).fill | core::slice::from_raw_parts_mut, core::slice::from_raw_parts_mut(sbss as usize as *mut u8, ebss as usize - sbss as usize)
            .fill |


好的，审计官。基于提供的证据底座、控辩双方陈述及AST抽样数据，现出具最终同源性定性仲裁报告。

---

### 1. 宏观判定
- **定性结论**: **独立开发 / 底层架构显著差异**
- **核心依据**: 客观AST相似度仅为 **0.62%**，共同AST指纹仅14个，远低于常规复用阈值。目标项目与参考项目在【进程管理】这一核心板块的共同唯一AST指纹为 **0**，且双方在任务调度模型、内存管理策略、系统调用实现深度上存在根本性架构分歧。控方未能提供任何同板块底层逻辑一致的主证据，其指控主要基于POSIX标准接口的同名现象，已被辩方有效驳斥。

### 2. 全模块覆盖矩阵解读

| 模块 | 双方存在 | 共同AST指纹 | 覆盖率 (目标/参考) | 证据强度 | 解读 |
| :--- | :--- | :--- | :--- | :--- | :--- |
| **进程管理** | 是 | **0** | 0.0% / 0.0% | **未形成** | 双方均实现进程管理，但目标项目包含SysV IPC、capabilities、robust futex等复杂子系统，参考项目则无。同名系统调用（如`sys_execve`）的内部调用图谱完全不同，证明实现逻辑异构。 |
| **任务调度/异步运行时** | 是 | 1 | 0.44% / 1.92% | **弱** | 覆盖率极低。唯一的共同指纹源于`set_sp`这类极小粒度函数。目标项目采用传统同步内核线程模型，参考项目则基于`async-task`的异步协程模型，架构本质不同。 |
| **内存管理** | 是 | 0 | 0.0% / 0.0% | **未形成** | 双方均存在该模块，但未发现任何共同结构指纹。目标项目的`sys_mmap`调用链涉及LTP兼容、文件映射等复杂处理，与参考项目的教学型实现差异显著。 |
| **中断/异常处理** | 是 | 0 | 0.0% / 0.0% | **未形成** | 双方均存在该模块，但未发现共同结构指纹。证据清单中的配对均为跨板块硬凑（如目标`virtio`驱动对比参考`sys_sendfile`），不构成有效证据。 |
| **系统调用** | 是 | 0 | 0.0% / 0.0% | **未形成** | 用户态`syscall`函数均使用`asm!`宏，这是RISC-V架构下陷入内核的标准模式，属平台强制规范，非代码复用。 |
| **文件系统** | 是 | 0 | 0.0% / 0.0% | **未形成** | 双方均实现文件系统，但未发现共同结构指纹。目标项目实现了完整的ext4只读支持、内存文件系统及复杂的路径解析兼容层，远超参考项目的教学范畴。 |
| **设备驱动** | 是 | 0 | 0.0% / 0.0% | **未形成** | 双方均存在该模块，但未发现共同结构指纹。证据清单中的配对均为跨板块硬凑，不构成有效证据。 |
| **网络协议栈** | 否 | 0 | 0.0% / 0.0% | **未形成** | 双方均未体现该模块。 |
| **文件系统/第三方库** | 是 | 0 | 0.0% / 0.0% | **未形成** | 目标项目疑似内嵌了ext4解析库，参考项目则依赖独立的`ext4` crate。证据清单中的配对均为跨库上下文对齐，非结构重合。 |
| **通用模块/其他** | 是 | 10 | 20.0% / 12.05% | **弱** | 覆盖率相对较高，但证据集中在`logging.rs#log`、`sbi.rs#sbi_call`、`main.rs#clear_bss`等函数。这些相似性源于Rust标准log trait实现、SBI标准调用规范及链接脚本驱动的BSS清零模式，属于标准范式或极小粒度代码片段。 |

### 3. 底层 AST 关键证据指纹表
> 表格按模块优先级排序，仅列出可对齐的同名/同板块证据，并说明其不构成复用的技术原因。

| 所属板块 | 目标项目路径 | 参考项目路径 | 证据类型 | 技术说明 |
| :--- | :--- | :--- | :--- | :--- |
| 进程管理 | `os-la/src/syscall/process.rs#sys_execve` | `os/src/syscall/process.rs#sys_execve` | 同板块同名 | 函数名由POSIX标准强制规定。目标项目调用`exec_resolved_inode`，参考项目直接`app_inode.read_all`，内部调用图谱完全不同，表明执行逻辑异构。 |
| 进程管理 | `os-la/src/syscall/process.rs#sys_wait4` | `os/src/syscall/process.rs#sys_wait4` | 同板块同名 | 函数名由POSIX标准强制规定。目标项目使用`ChildRecord`+`copy_to_user`（同步模型），参考项目使用`WaitFuture`（异步模型），同步原语和进程状态管理模型截然不同。 |
| 进程管理 | `os-la/src/syscall/process.rs#sys_nanosleep` | `os/src/syscall/process.rs#sys_nanosleep` | 同板块同名 | 函数名由POSIX标准强制规定。目标项目包含`arm_current_thread_cancel_flag`等线程取消逻辑，参考项目仅使用`TimeoutFuture`，功能复杂度和实现路径完全不同。 |
| 任务调度 | `os-la/src/trap/context.rs#set_sp` | `os/src/trap/context.rs#set_sp` | 同板块同名同结构 | 极小粒度函数，通常为单行寄存器赋值操作，属于RISC-V架构下上下文切换的必要操作，逻辑简单到几乎只有一种写法。 |
| 系统调用 | `user/src/syscall.rs#syscall` | `user/src/syscall.rs#syscall` | 同板块同名 | 用户态陷入内核的汇编指令封装，由RISC-V `ecall` 指令及ABI规范强制决定，无原创设计空间。 |
| 通用模块 | `os-la/src/logging.rs#log` | `os/src/logging.rs#log` | 同板块同名同结构 | 双方均实现了Rust标准库`log` crate的`Log` trait。`enabled`、`record.level`等调用是该trait接口的强制要求，属接口规范驱动的必然相似。 |
| 通用模块 | `os/src/sbi.rs#sbi_call` | `os/src/sbi.rs#sbi_call` | 同板块同名 | RISC-V SBI标准调用规范（`ecall`指令封装）的必然实现，参数传递和返回值处理由标准定义。 |

### 4. 核心代码调用流对比剖析
* **【进程管理 / sys_execve 执行路径】**
* **底层技术析理**: 此路径揭示了双方在程序加载和执行上的根本性架构差异。参考项目采用简单的教学模型，直接从inode读取全部数据到内存后跳转执行。目标项目则实现了一个复杂的、面向兼容性的两阶段解析-执行模型：首先通过`resolve_exec_inode_at`进行路径解析，该函数内部包含对ext4文件系统、内存文件系统、虚拟busybox工具、LTP noop工具等多种后端的判断与查找；随后调用`exec_resolved_inode`，该函数进一步处理参数/环境变量字符串的解析、TrapContext初始化等。这种设计差异源于两者完全不同的目标定位，无法通过简单重构实现转化。
* **代码溯源链接**:
  - 目标项目: `os-la/src/syscall/process.rs#sys_execve`
  - 参考项目: `os/src/syscall/process.rs#sys_execve`

* **【进程管理 / sys_wait4 等待子进程路径】**
* **底层技术析理**: 此路径揭示了双方在进程同步原语上的根本性差异。参考项目基于异步运行时，`sys_wait4`通过创建一个`WaitFuture`并等待其完成来实现，这是典型的协程/异步同步模式。目标项目则采用传统的同步内核线程模型，通过`ChildRecord`结构体管理子进程退出状态，并使用`task::copy_to_user`将状态数据主动拷贝到用户空间。这两种同步模型在调度、阻塞、唤醒机制上完全不同，是内核架构层面的核心差异。
* **代码溯源链接**:
  - 目标项目: `os-la/src/syscall/process.rs#sys_wait4`
  - 参考项目: `os/src/syscall/process.rs#sys_wait4`

### 5. 单方模块与降权项
* **目标项目独有功能域**: 目标项目在【进程管理】板块存在大量参考项目完全缺失的功能域，包括但不限于：
  * **SysV IPC 全套实现** (`sys_semctl`, `sys_shmctl`, `sys_msgctl` 等)
  * **Linux Capabilities 安全模型** (`sys_capset`)
  * **Robust Futex 及 LTP 测试兼容层** (`force_tgkill02_checkpoint_futex` 等)
  * **资源限制** (`sys_prlimit64`)
  * **执行兼容层** (`is_ltp_noop_tool`, `is_virtual_busybox_applet`)
  这些功能域是目标项目追求真实Linux环境兼容性的独立开发成果，是证明其原创性的强有力证据。

* **降权项说明**:
  * **POSIX 标准系统调用同名**: `sys_exit`, `sys_getpid`, `sys_gettid` 等函数名由POSIX/Linux ABI标准强制规定，任何实现该接口的操作系统都必须使用这些名称，不能作为复用证据。
  * **语言级标准宏/库调用**: `println!`, `panic!`, `asm!`, `Ok/Err`, `Some`, `Arc::new` 等是Rust语言的标准库或核心语法，其出现是语言使用的必然结果，与代码抄袭无关。
  * **跨板块硬凑证据**: 控方全模块对比中出现了大量跨子系统配对（如 `virtio.rs#read_sector` vs `syscall/fs.rs#sys_sendfile`），这些配对在功能和语义上毫无关联，仅共享极泛型的AST节点，证据强度为零。

### 6. 保守结论
综合全模块AST证据、控辩双方陈述及核心调用流剖析，本次审计认定目标项目 `[2026-T202610486999845-XX]` 与参考项目 `[2024-ᕕڼᕗ旺仔-哈尔滨工业大学]` 在代码实现层面构成 **独立开发**。客观AST相似度（0.62%）和覆盖率（<2%）处于独立实现区间。双方在进程管理、任务调度等核心板块展现出根本性的架构差异（同步 vs 异步、教学微内核 vs Linux兼容宏内核），且目标项目包含大量参考项目不具备的独立功能域。控方提出的“同名重构”指控，本质上是将POSIX标准接口的强制同名和内核架构的必然差异，错误地解读为规避查重的行为。现有证据远不足以支持代码复用或抄袭的结论。

------------------------------

