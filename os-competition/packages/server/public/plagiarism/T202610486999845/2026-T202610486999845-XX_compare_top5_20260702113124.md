# 🛡️ OS 内核赛自动化代码级架构审计报告

**审计时间**: `2026-07-02 11:31:24`
**参赛作品名称**: `2026-T202610486999845-XX`
**年份**: `2026`
**学校**: `XX`
**队伍名称**: `T202610486999845`
**比赛名字**: `OS-Competition`
**比赛子赛道**: `Kernel-Implementation`
**仓库地址**: `https://gitlab.eduxiji.net/educg-group-43501-3132633/T202610486999845-779`
**推测开发语言**: `Rust / C (由AST探针自动识别)`
**特征函数量**: `2812`

## ⚔️ 底层代码同源性对比 (AST Homology)
> 本部分由专门的代码审查智能体，对该项目与符合年份条件的历届最高相似度项目进行控制流与底层调用对比。新版报告会先给出确定性的全模块覆盖矩阵，再给出 Agent 的分模块解释，避免只围绕少量抽样函数展开。

### 🏆 Top 5 底层对标: vs `2022-段错误-杭州电子科技大学`（杭州电子科技大学 / 段错误）

#### 对方作品信息

**对方年份**: `2022`
**对方学校**: `杭州电子科技大学`
**对方队伍名称**: `段错误`
**对方比赛名字**: `OS-Competition`
**对方比赛子赛道**: `Kernel-Implementation`
**对方仓库地址**: `https://gitlab.eduxiji.net/19061120/oskernel2022-segmentfault.git`

> 双向覆盖率：共同 AST 指纹: 10；唯一 AST 指纹: 目标项目 1449，参考项目 517；覆盖率: 目标项目 0.69%，参考项目 1.93%

## 全模块对比总览（确定性证据）

> 总体覆盖率：共同 AST 指纹: 10；唯一 AST 指纹: 目标项目 1449，参考项目 517；覆盖率: 目标项目 0.69%，参考项目 1.93%

### 1. 模块覆盖矩阵
| 模块 | 目标函数数 | 参考函数数 | 共同唯一 AST 指纹 | Target 覆盖 | Base 覆盖 | 状态 |
| :--- | ---: | ---: | ---: | ---: | ---: | :--- |
| 进程管理 | 896 | 52 | 1 | 0.23% | 2.33% | 双方存在且有结构重合 |
| 任务调度/异步运行时 | 407 | 56 | 1 | 0.44% | 1.85% | 双方存在且有结构重合 |
| 内存管理 | 321 | 183 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |
| 中断/异常处理 | 13 | 6 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |
| 系统调用 | 11 | 36 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |
| 文件系统 | 1039 | 226 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |
| 设备驱动 | 8 | 37 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |
| 网络协议栈 | 0 | 0 | 0 | 0.0% | 0.0% | 双方均未体现 |
| 文件系统/第三方库 | 50 | 0 | 0 | 0.0% | 0.0% | 仅目标项目体现 |
| 通用模块/其他 | 67 | 41 | 3 | 6.0% | 7.89% | 双方存在且有结构重合 |

### 2. 分模块证据清单

#### 进程管理
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `os-la/src/syscall/process.rs#sys_prlimit64` | `os/src/syscall/process.rs#sys_prlimit64` | 同板块同名 | data[0..8].copy_from_slice, data[0..8].try_into, data[0..8].try_into().unwrap, data[8..16].copy_from_slice, data[8..16].try_into, data[8..16].try_into().unwrap ...(+9) | UserBuffer::new, buf.as_ptr, buf.write, current_task, current_task().unwrap, current_user_token ...(+6) |
| `os-la/src/syscall/process.rs#sys_kill` | `os/src/syscall/process.rs#sys_kill` | 同板块同名 | child_record_exists, mark_child_killed, record_pending_signal, signal_has_user_handler, signal_is_ignored, task::request_exit | SignalFlags::from_bits, panic!, pid2task, task.inner_exclusive_access |
| `os-la/src/syscall/process.rs#sys_rt_sigprocmask` | `os/src/syscall/sigset.rs#sys_rt_sigprocmask` | 同板块同名 | bytes.len, current.to_le_bytes, is_current_thread, read_user_sigmask, sigset_size.min, task::copy_to_user ...(+2) | UserBuffer::new, buf_vec.pop, buf_vec.pop().unwrap, current_task, current_task().unwrap, current_user_token ...(+7) |
| `os-la/src/syscall/process.rs#sys_uname` | `os/src/syscall/process.rs#sys_uname` | 同板块同名 | put_uts_field, task::copy_to_user, task::copy_to_user(buf, &data).is_err, task::is_user_range_writable | UTSNAME.lock, UserBuffer::new, core::mem::size_of::<Utsname>, current_user_token, translated_byte_buffer, uname.as_bytes ...(+1) |

#### 任务调度/异步运行时
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `os-la/src/syscall/fs.rs#sys_readlinkat` | `os/src/syscall/fs.rs#sys_readlinkat` | 同板块同名 | DEFAULT_EXE_LINK.len, dangling_symlink_or_prefix_error, find_mem_file, has_test_eloop_chain, is_fd_symlink, is_runtime_libgcc_s_path ...(+24) | UserBuffer::new, current_user_token, panic!, path.as_str, procinfo.as_bytes, procinfo.len ...(+3) |
| `os-la/src/syscall/fs.rs#sys_write` | `os/src/syscall/fs.rs#sys_write` | 同板块同名 | check_direct_io_user_buffer, entry.offset.saturating_add, fd_allows_write, is_proc_pid_oom_score_adj_path, matches!, notify_inotify_modify ...(+19) | UserBuffer::new, VirtAddr::from, current_task, current_task().unwrap, current_user_token, drop ...(+8) |
| `os-la/src/syscall/fs.rs#sys_sendfile` | `os/src/syscall/fs.rs#sys_sendfile` | 同板块同名 | Some, explicit_offset.as_mut, fast_sparse_sendfile, fd_allows_read, fd_allows_write, matches! ...(+9) | current_task, current_task().unwrap, data_buffer.len, drop, fd_table[in_fd].as_ref, fd_table[in_fd].as_ref().unwrap ...(+7) |
| `os-la/src/syscall/fs.rs#sys_fstat` | `os/src/syscall/fs.rs#sys_fstat` | 同板块同名 | fs.metadata, fs.metadata(entry.inode).map_err, fs_mut, fs_mut().and_then, loop_visible_size_bytes, mem_file_mode ...(+4) | Kstat::new, UserBuffer::new, current_task, current_task().unwrap, current_user_token, file.get_fstat ...(+6) |

#### 内存管理
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `os-la/src/syscall/fs.rs#sys_openat` | `os/src/syscall/fs.rs#sys_openat` | 同板块同名 | Ok, alloc_fd, alloc_fd_checked, alloc_fd_checked(FdEntry {
                kind: HANDLE_DEV_NULL,
                inode: 0,
                mem_index: usize::MAX,
                shared_offset: usize::MAX,
                offset: 0,
                path: effective_path,
                path_len: effective_len,
                fd_flags: open_fd_flags(flags),
                status_flags: open_status_flags(flags),
            })
            .unwrap_or_else, alloc_fd_checked(FdEntry {
                kind: HANDLE_LOOP_DEVICE,
                inode: 0,
                mem_index: index,
                shared_offset: usize::MAX,
                offset: 0,
                path: effective_path,
                path_len: effective_len,
                fd_flags: open_fd_flags(flags),
                status_flags: open_status_flags(flags),
            })
            .unwrap_or_else, build_tmpfile_path ...(+68) | OpenFlags::from_bits, OpenFlags::from_bits(flags).expect, Some, current_task, current_task().unwrap, current_user_token ...(+9) |
| `os/src/syscall/fs.rs#sys_fcntl` | `os/src/syscall/fs.rs#sys_fcntl` | 同板块同名 | add_pipe_endpoint_ref, alloc_fd_from, arg.max, ensure_shared_offset_slot, fcntl_get_lock, fcntl_get_owner_ex ...(+8) | Arc::clone, Arc::new, FcntlFlags::from_bits, FcntlFlags::from_bits(cmd).unwrap, OpenFlags::from_bits, OpenFlags::from_bits(arg.unwrap() as u32).unwrap ...(+18) |
| `os/src/trap/mod.rs#trap_handler` | `os/src/trap/mod.rs#trap_handler` | 同板块同名 | crate::syscall::should_treat_low_exec_fault_as_exit, deliver_trap_signal, fatal_trap_signal, finish_child_after_exit, maybe_deliver_signal, panic! ...(+7) | (stval as usize).into, TRAMPOLINE.into, Trap::Exception, cfg!, current_add_signal, current_task ...(+17) |
| `os-la/src/syscall/fs.rs#sys_dup` | `os/src/syscall/fs.rs#sys_dup` | 同板块同名 | add_pipe_endpoint_ref, alloc_fd_any, ensure_shared_offset_slot | Arc::clone, Some, current_task, current_task().unwrap, inner.alloc_fd, inner.fd_table.len ...(+4) |

#### 中断/异常处理
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `os/src/drivers/virtio.rs#read_sector` | `os/src/trap/mod.rs#trap_from_kernel` | 同模块上下文对齐/差异观察 | Err, Ok, avail.idx.wrapping_add, avail_ring_mut, buffer.as_mut_ptr, core::mem::size_of::<VirtioBlkReqHeader> ...(+8) | panic!, println! |
| `os-la/src/drivers/virtio.rs#read_sector` | `os/src/trap/mod.rs#set_kernel_trap_entry` | 同模块上下文对齐/差异观察 | (self.pci.isr as *const u8).read_volatile, (self.pci.notify_addr as *mut u16).write_volatile, Err, Ok, avail.idx.wrapping_add, avail_ring_mut ...(+9) | stvec::write |
| `os-la/src/arch.rs#write_stvec` | `os/src/trap/mod.rs#set_user_trap_entry` | 同模块上下文对齐/差异观察 | asm! | stvec::write |
| `os-la/src/trap/mod.rs#fatal_trap_signal` | `os/src/trap/mod.rs#enable_timer_interrupt` | 同模块上下文对齐/差异观察 | 内部闭环逻辑 | sie::set_stimer |

#### 系统调用
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `user/src/lib.rs#exit` | `user_c/lib/syscall.c#exit` | 同板块同名 | sys_exit | syscall |
| `user/src/syscall.rs#syscall` | `user_c/lib/syscall.c#clone` | 同模块上下文对齐/差异观察 | asm! | __clone |
| `os-la/src/syscall/mod.rs#ltp_pause_compat_mode` | `os/src/syscall/socket.rs#sys_recvfrom` | 同模块上下文对齐/差异观察 | matches! | UserBuffer::new, current_user_token, translated_byte_buffer, userbuf.write |
| `os/src/syscall/mod.rs#ltp_pause_compat_mode` | `user_c/lib/syscall.c#get_time` | 同模块上下文对齐/差异观察 | matches! | sys_get_time |

#### 文件系统
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `os-la/src/syscall/fs.rs#sys_mount` | `os/src/syscall/fs.rs#sys_mount` | 同板块同名 | RO_MOUNT_PATH.fill, RO_MOUNT_PATH[..target_len].copy_from_slice, bind_dummy_mount, crate::syscall::current_euid_for_fs, dummy_mount_has_open_file, dummy_mount_path_matches ...(+11) | MNT_TABLE.lock, MNT_TABLE.lock().mount, current_user_token, translated_str |
| `os-la/src/syscall/fs.rs#sys_statfs` | `os/src/syscall/fs.rs#sys_statfs` | 同板块同名 | dangling_symlink_or_prefix_error, dummy_mount_flags_for_path, dummy_mount_statfs_for_path, dummy_mount_statfs_for_path(&statfs_path[..statfs_len]).unwrap_or, find_dummy_mounted_mem_file, find_mem_file ...(+21) | Statfs::new, Statfs::new().as_bytes, UserBuffer::new, current_user_token, size_of::<Statfs>, translated_byte_buffer ...(+1) |
| `os-la/src/lang_items.rs#panic` | `os/src/lang_items.rs#panic` | 同板块同名 | info.location, println!, shutdown | info.location, println!, shutdown |
| `os-la/src/syscall/fs.rs#sys_umask` | `os/src/syscall/fs.rs#sys_umask` | 同板块同名 | 内部闭环逻辑 | 内部闭环逻辑 |

#### 设备驱动
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `os-la/src/drivers/virtio.rs#pci_cfg_addr` | `os/src/drivers/block/sdcard.rs#read_sector` | 同模块上下文对齐/差异观察 | 内部闭环逻辑 | Err, Ok, assert!, chunk.iter_mut, chunk.iter_mut().zip, data_buf.chunks_mut ...(+6) |
| `os-la/src/drivers/virtio.rs#new` | `os/src/drivers/block/sdcard.rs#write_sector` | 同模块上下文对齐/差异观察 | 内部闭环逻辑 | (data_buf.len() / SEC_LEN).try_into, (data_buf.len() / SEC_LEN).try_into().unwrap, Err, Ok, assert!, chunk.iter ...(+9) |
| `os-la/src/sbi.rs#console_putchar` | `os/src/drivers/block/sdcard.rs#get_cidregister` | 同模块上下文对齐/差异观察 | (UART0_BASE as *mut u8).write_volatile | Err, Ok, self.end_cmd, self.get_response, self.read_data, self.send_cmd ...(+2) |
| `os-la/src/drivers/virtio.rs#queue_base_addr` | `simple-fat32/src/block_cache.rs#get_block_cache` | 同模块上下文对齐/差异观察 | core::ptr::addr_of! | Arc::clone, Arc::new, Arc::strong_count, BlockCache::new, RwLock::new, panic! ...(+7) |

#### 文件系统/第三方库
仅目标项目中观察到该模块代表函数，参考项目没有同模块可对齐证据：
- 目标项目：`os-la/src/fs/ext4.rs#lookup_child`，调用摘要：Err, Ok, Some, core::str::from_utf8, div_ceil_u64, node_kind ...(+6)
- 目标项目：`os/src/fs/ext4.rs#lookup_child`，调用摘要：Err, Ok, Some, core::str::from_utf8, div_ceil_u64, node_kind ...(+6)
- 目标项目：`os-la/src/fs/ext4.rs#for_each_child`，调用摘要：Err, Ok, core::str::from_utf8, div_ceil_u64, node_kind, read_u16 ...(+6)
- 目标项目：`os/src/fs/ext4.rs#for_each_child`，调用摘要：Err, Ok, core::str::from_utf8, div_ceil_u64, node_kind, read_u16 ...(+6)

#### 通用模块/其他
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `os/src/sbi.rs#shutdown` | `os/src/sbi.rs#shutdown` | 同板块同名 | asm!, sbi_call | panic!, sbi_call |
| `os/src/sbi.rs#sbi_call` | `os/src/sbi.rs#sbi_call` | 同板块同名 | asm! | asm! |
| `os-la/src/main.rs#clear_bss` | `os/src/main.rs#clear_bss` | 同板块同名 | core::slice::from_raw_parts_mut, core::slice::from_raw_parts_mut(start as *mut u8, end - start).fill | core::slice::from_raw_parts_mut, core::slice::from_raw_parts_mut(sbss as usize as *mut u8, ebss as usize - sbss as usize).fill |
| `os-la/src/fallback.rs#_start` | `user_c/lib/lib.c#_start` | 同板块同名 | 内部闭环逻辑 | exit, main |


#### 1. 宏观判定
- **定性结论**: 独立开发 (相似度: 0.51%，共同 AST 指纹: 10；唯一 AST 指纹: 目标项目 1449，参考项目 517；覆盖率: 目标项目 0.69%，参考项目 1.93%)

#### 2. 全模块覆盖矩阵解读
- **进程管理**: 双方均存在。共同 AST 指纹 1 个，目标覆盖率 0.23%，参考覆盖率 2.33%。证据强度：**弱**。极低的覆盖率表明双方在进程管理的核心实现上完全不同，参考项目的核心调度与任务管理函数在目标项目中无对应指纹。
- **任务调度/异步运行时**: 双方均存在。共同 AST 指纹 1 个，目标覆盖率 0.44%，参考覆盖率 1.85%。证据强度：**弱**。覆盖率处于背景噪声级别，无法证明存在系统性复用。
- **内存管理**: 双方均存在。共同 AST 指纹 0 个。证据强度：**未形成**。双方虽均有内存管理模块，但 AST 层面未发现任何共同结构指纹，表明实现方案完全独立。
- **中断/异常处理**: 双方均存在。共同 AST 指纹 0 个。证据强度：**未形成**。无共同指纹，说明 Trap 处理、中断向量设置等底层机制实现各异。
- **系统调用**: 双方均存在。共同 AST 指纹 0 个。证据强度：**未形成**。系统调用分发与实现路径无共同结构，符合独立开发特征。
- **文件系统**: 双方均存在。共同 AST 指纹 0 个。证据强度：**未形成**。目标项目实现了自研的 ext4 模块，参考项目使用 simple-fat32，两者在文件系统层面无任何 AST 同源性。
- **设备驱动**: 双方均存在。共同 AST 指纹 0 个。证据强度：**未形成**。目标项目使用 virtio 块设备驱动，参考项目使用 sdcard 驱动，硬件目标与实现均不同。
- **网络协议栈**: 双方均未体现。证据强度：**未形成**。
- **第三方库/公共依赖**: 仅目标项目体现。证据强度：**未形成**。目标项目包含 ext4 等第三方/自研库，参考项目无对应模块，属于单方特征。

#### 3. 底层 AST 关键证据指纹表
| 所属板块 | 目标项目路径 | 参考项目路径 | 证据类型 | 技术说明 |
| :--- | :--- | :--- | :--- | :--- |
| 进程管理 | os-la/src/syscall/process.rs#sys_futex | os/src/syscall/process.rs#sys_futex | 同板块同名 | 调用图谱均包含 `arm_current_thread_cancel_flag`、`ltp_tgkill02_checkpoint_futex_active` 等 LTP 兼容性辅助函数。此类命名源于 Linux Test Project 的公共测试用例，是任何试图通过 LTP 验证的内核的领域公共知识，非私有逻辑。 |
| 进程管理 | os-la/src/syscall/process.rs#sys_prctl | os/src/syscall/process.rs#sys_prctl | 同板块同名 | 调用图谱均包含 `get_process_comm`、`CURRENT_PDEATHSIG` 等。`prctl` 是 POSIX 标准系统调用，其子命令处理流程（如设置进程名、死亡信号）由规范定义，函数命名与调用顺序具有必然性。 |
| 进程管理 | os-la/src/syscall/process.rs#sys_semctl | os/src/syscall/process.rs#sys_semctl | 同板块同名 | 调用图谱均包含 `SemArray::empty`、`find_sem_array_by_id` 等。System V 信号量控制接口的语义由 Linux ABI 严格约束，`SEMVMX` 等常量及饱和运算 (`saturating_add`) 是 Rust 实现该标准接口的惯用法。 |
| 进程管理 | os-la/src/syscall/process.rs#sys_shmctl | os/src/syscall/process.rs#sys_shmctl | 同板块同名 | 调用图谱均包含 `ShmSegment::empty`、`find_shm_segment` 等。与 `sys_semctl` 同理，共享内存控制接口的命名与实现受 System V IPC 标准约束，属于领域公共知识。 |
| 进程管理 | os-la/src/syscall/process.rs#sys_semtimedop | os/src/syscall/process.rs#sys_semtimedop | 同板块同名 | 调用图谱均包含 `SEM_ARRAYS[index].values[slot].saturating_add` 等。信号量操作的饱和运算与 `i16::from_le_bytes` 字节序转换是 Rust 实现 System V IPC 的必要安全措施，非独创性逻辑。 |
| 进程管理 | os-la/src/syscall/fs.rs#resolve_exec_inode_at | os/src/syscall/fs.rs#resolve_exec_inode_at | 同板块同名 | 调用图谱均包含 `is_virtual_busybox_applet`、`is_ltp_noop_tool` 等。busybox 和 LTP 是 OS 教学/测试领域的标准用户态组件，为其提供兼容性路径是课程项目的常见需求，函数命名直接反映了其功能意图。 |
| 通用模块/其他 | os/src/sbi.rs#sbi_call | os/src/sbi.rs#sbi_call | 同板块同名 | 调用图谱均为 `asm!` 宏。RISC-V 的 SBI 调用必须通过 `ecall` 指令实现，在 Rust 中封装为内联汇编宏是唯一且标准的做法，不构成源码复用证据。 |
| 通用模块/其他 | os-la/src/main.rs#clear_bss | os/src/main.rs#clear_bss | 同板块同名 | 调用图谱均为 `core::slice::from_raw_parts_mut(...).fill(0)`。清零 BSS 段是操作系统启动的必经步骤，此 Rust 一行式写法是社区公认的最佳实践，属于语言惯用法。 |

#### 4. 核心代码调用流对比剖析
* **【进程管理 / sys_prlimit64】**
* **底层技术析理**: 目标项目采用裸指针与字节切片 (`data[0..8].copy_from_slice`) 直接操作用户态内存，并通过 `task::copy_to_user` 进行验证与拷贝。参考项目则封装了 `UserBuffer` 抽象层，通过 `buf.write` 写入数据，并依赖 `current_user_token` 进行地址转换。两者在用户态内存访问模型上存在根本性架构差异，前者是内核直拷原语，后者是缓冲抽象层，并非简单的 API 替换。
* **代码溯源链接**:
  - 目标项目: `os-la/src/syscall/process.rs#sys_prlimit64`
  - 参考项目: `os/src/syscall/process.rs#sys_prlimit64`

* **【进程管理 / sys_kill】**
* **底层技术析理**: 目标项目的信号发送逻辑基于子进程记录 (`child_record_exists`、`mark_child_killed`) 和信号处理器查询 (`signal_has_user_handler`)，呈现出一个以进程关系为中心的管理模型。参考项目则直接操作任务结构体的信号标志位 (`SignalFlags::from_bits`、`task.inner_exclusive_access`)，模型更偏向底层任务控制。两者的进程/任务抽象层次和信号路由策略完全不同。
* **代码溯源链接**:
  - 目标项目: `os-la/src/syscall/process.rs#sys_kill`
  - 参考项目: `os/src/syscall/process.rs#sys_kill`

* **【进程管理 / sys_rt_sigprocmask】**
* **底层技术析理**: 目标项目通过 `read_user_sigmask` 和 `is_current_thread` 等辅助函数操作信号掩码，并将结果通过 `task::copy_to_user` 写回。参考项目则使用 `UserBuffer` 和 `buf_vec.pop` 从用户态读取数据，并直接操作 `inner.sigset.bits`。两者在信号集的读取、修改和写回路径上采用了完全不同的内部接口和数据流。
* **代码溯源链接**:
  - 目标项目: `os-la/src/syscall/process.rs#sys_rt_sigprocmask`
  - 参考项目: `os/src/syscall/sigset.rs#sys_rt_sigprocmask`

#### 5. 单方模块与降权项
- **单方模块**: 目标项目独有的 `文件系统/第三方库` 模块（含 `os-la/src/fs/ext4.rs` 等）是参考项目完全不具备的独立实现，属于目标项目的原创性工作，不构成同源证据。
- **降权项**: 控辩双方列举的 `sys_getpid`、`sys_exit` 等大量“内部闭环逻辑”或仅调用一两个标准函数的系统调用，其 AST 结构过于简单，任何独立实现都可能产生相同指纹，证据力严重不足，已从主证据表中移除。所有跨板块的相似性（如参考项目 `task/task.rs#exec` 与目标项目 `syscall/process.rs` 中的函数对比）均不被采纳为同板块主证据。

#### 6. 保守结论
本次审计基于 0.51% 的客观 AST 相似度与 10 个共同指纹，判定双方项目在整体架构上存在显著差异，属于独立开发。控方所指控的“大规模系统性复用”无法被极低的覆盖率数据支持。双方在 `sys_futex`、`sys_prctl` 等函数上的调用图谱相似性，均可被解释为对 POSIX/System V 标准、LTP 测试套件兼容性需求以及 Rust 语言惯用法的共同遵循。尤其在 `sys_prlimit64`、`sys_kill` 等关键系统调用中，双方在内存访问模型、任务抽象和信号处理路径上展现出根本性的架构分歧，排除了“洗稿式复用”的可能。综上，现有证据不足以认定目标项目抄袭。

------------------------------

