# 🛡️ OS 内核赛自动化代码级架构审计报告

**审计时间**: `2026-07-02 03:56:28`
**参赛作品名称**: `2026-T2026103369910012-XX`
**年份**: `2026`
**学校**: `XX`
**队伍名称**: `T2026103369910012`
**比赛名字**: `OS-Competition`
**比赛子赛道**: `Kernel-Implementation`
**仓库地址**: `https://gitlab.eduxiji.net/educg-group-43501-3132633/T2026103369910012-831`
**推测开发语言**: `Rust / C (由AST探针自动识别)`
**特征函数量**: `1270`

## ⚔️ 底层代码同源性对比 (AST Homology)
> 本部分由专门的代码审查智能体，对该项目与符合年份条件的历届最高相似度项目进行控制流与底层调用对比。新版报告会先给出确定性的全模块覆盖矩阵，再给出 Agent 的分模块解释，避免只围绕少量抽样函数展开。

### 🏆 Top 3 底层对标: vs `2025-狗剩-海南大学`（海南大学 / 狗剩）

#### 对方作品信息

**对方年份**: `2025`
**对方学校**: `海南大学`
**对方队伍名称**: `狗剩`
**对方比赛名字**: `OS-Competition`
**对方比赛子赛道**: `Kernel-Implementation`
**对方仓库地址**: `https://gitlab.eduxiji.net/educg-group-36002-2710490/T202510589995148-2209`

> 双向覆盖率：共同 AST 指纹: 153；唯一 AST 指纹: 目标项目 1097，参考项目 3020；覆盖率: 目标项目 13.95%，参考项目 5.07%

## 全模块对比总览（确定性证据）

> 总体覆盖率：共同 AST 指纹: 153；唯一 AST 指纹: 目标项目 1097，参考项目 3020；覆盖率: 目标项目 13.95%，参考项目 5.07%

### 1. 模块覆盖矩阵
| 模块 | 目标函数数 | 参考函数数 | 共同唯一 AST 指纹 | Target 覆盖 | Base 覆盖 | 状态 |
| :--- | ---: | ---: | ---: | ---: | ---: | :--- |
| 进程管理 | 54 | 93 | 3 | 6.0% | 3.57% | 双方存在且有结构重合 |
| 任务调度/异步运行时 | 243 | 399 | 2 | 0.91% | 0.69% | 双方存在且有结构重合 |
| 内存管理 | 188 | 326 | 11 | 6.21% | 3.78% | 双方存在且有结构重合 |
| 中断/异常处理 | 3 | 62 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |
| 系统调用 | 150 | 153 | 1 | 0.71% | 0.78% | 双方存在且有结构重合 |
| 文件系统 | 579 | 747 | 91 | 18.72% | 16.08% | 双方存在且有结构重合 |
| 设备驱动 | 0 | 142 | 0 | 0.0% | 0.0% | 仅参考项目体现 |
| 网络协议栈 | 0 | 77 | 0 | 0.0% | 0.0% | 仅参考项目体现 |
| 文件系统/第三方库 | 38 | 689 | 16 | 42.11% | 2.73% | 双方存在且有结构重合 |
| 网络协议栈/第三方库 | 0 | 107 | 0 | 0.0% | 0.0% | 仅参考项目体现 |
| 第三方库 | 0 | 386 | 0 | 0.0% | 0.0% | 仅参考项目体现 |
| 通用模块/其他 | 15 | 665 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |

### 2. 分模块证据清单

#### 进程管理
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `kernel/src/task/ops.rs#do_exit` | `api/src/imp/task.rs#do_exit` | 同板块同名 | FutexKey::new_current, SignalInfo::new_kernel, Some, axtask::yield_now, clear_child_tid.vm_write, clear_child_tid.vm_write(0).is_ok ...(+31) | Layout::new::<Tid>, SignalInfo::new, UserPtr::<Tid>::from, axtask::exit, axtask::yield_now, clear_child_tid.get_as ...(+18) |
| `kernel/src/syscall/signal.rs#sys_rt_sigprocmask` | `api/src/imp/signal.rs#sys_rt_sigprocmask` | 同板块同名 | Err, Ok, check_sigset_size, curr.as_thread, current, debug! ...(+7) | Err, Ok, check_sigset, current, current()
        .task_ext, current()
        .task_ext()
        .thread
        .signal
        .with_blocked_mut::<LinuxResult<_>> ...(+2) |
| `kernel/src/syscall/ipc/shm.rs#clear_proc_shm` | `api/src/imp/ipc/shm.rs#clear_proc_shm` | 同板块同名 | self.get_inner_by_shmid, self.get_shmids_by_pid, self.remove_pid, self.remove_shmid, shm_inner.attach_count, shm_inner.detach_process ...(+1) | SHM_MANAGER.lock, shm_inner.detach_process, shm_inner.lock, shm_manager.get_inner_by_shmid, shm_manager.get_inner_by_shmid(shmid).unwrap, shm_manager.get_shmids_by_pid ...(+1) |
| `kernel/src/syscall/ipc/shm.rs#attach_process` | `api/src/imp/ipc/shm.rs#attach_process` | 同板块同名 | assert!, monotonic_time_nanos, self.va_range.insert | assert!, self.va_range.insert |

#### 任务调度/异步运行时
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `kernel/src/syscall/sync/futex.rs#sys_futex` | `api/src/imp/futex.rs#sys_futex` | 同板块同名 | Err, FutexKey::new_current, Ok, Some, assert_unsigned, axtask::yield_now ...(+17) | Err, Ok, TimeValue::new, axtask::yield_now, futex_table.get, futex_table.get_or_insert ...(+14) |
| `kernel/src/syscall/signal.rs#sys_kill` | `api/src/imp/signal.rs#sys_kill` | 同板块同名 | Err, Ok, Some, current, current().as_thread, current().as_thread().proc_data.proc.group ...(+10) | Err, Ok, current, find_process, get_all_processes, make_siginfo ...(+3) |
| `kernel/src/pseudofs/mod.rs#mount_all` | `core/src/vfs/mod.rs#mount_all` | 同板块同名 | FS_CONTEXT.lock, Ok, Path::new, Path::new("/sys/class/graphics/fb0/device").components, PathBuf::new, axfs::new_default ...(+18) | Ok, dev::new_devfs, mount_at, proc::new_procfs, tmp::MemoryFs::new |
| `kernel/src/syscall/ipc/shm.rs#sys_shmdt` | `api/src/imp/ipc/shm.rs#sys_shmdt` | 同板块同名 | Ok, SHM_MANAGER.lock, VirtAddr::from, aspace.unmap, curr.as_thread, current ...(+14) | Ok, SHM_MANAGER.lock, VirtAddr::from, aspace.unmap, axhal::arch::flush_tlb, curr.task_ext ...(+17) |

#### 内存管理
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `kernel/src/syscall/mm/mmap.rs#sys_mmap` | `src/syscall_imp/mm/mmap.rs#sys_mmap` | 同板块同名 | (addr + length).align_up, Arc::new, Backend::new_alloc, Backend::new_cow, Backend::new_file, Backend::new_linear ...(+50) | (addr + length).align_up, Err, MmapFlags::MAP_PRIVATE.bits, MmapFlags::MAP_SHARED.bits, MmapFlags::MAP_SHARED_VALIDATE.bits, MmapFlags::from_bits_truncate ...(+32) |
| `kernel/src/mm/loader.rs#load_user_app` | `core/src/mm.rs#load_user_app` | 同板块同名 | "sh".to_owned, (ustack_top - user_sp_aligned).align_up_4k, Backend::new_alloc, ELF_LOADER.lock, ELF_LOADER.lock().load, Err ...(+62) | CStr::from_bytes_with_nul, CStr::from_bytes_with_nul(interp)
                    .ok, CStr::from_bytes_with_nul(interp)
                    .ok()
                    .and_then, CStr::from_bytes_with_nul(interp)
                    .ok()
                    .and_then(|it| it.to_str().ok())
                    .ok_or, ElfFile::new, ElfFile::new(&file_data).map_err ...(+53) |
| `kernel/src/syscall/ipc/shm.rs#sys_shmat` | `api/src/imp/ipc/shm.rs#sys_shmat` | 同板块同名 | Arc::new, Backend::new_shared, Ok, SHM_MANAGER.lock, SharedPages::new, ShmAtFlags::from_bits_truncate ...(+27) | Err, MappingFlags::all, Ok, SHM_MANAGER.lock, ShmAtFlags::from_bits_truncate, VirtAddr::from ...(+26) |
| `axfs-ng-local/src/fs/fat/dir.rs#create` | `arceos/modules/axfs-ng/src/fs/fat/dir.rs#create` | 同板块同名 | Default::default, DirEntry::new_dir, DirEntry::new_file, Err, FatDirNode::new, FatFileNode::new ...(+23) | DirEntry::new_dir, DirEntry::new_file, Err, FatDirNode::new, FatFileNode::new, Reference::new ...(+12) |

#### 中断/异常处理
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `kernel/src/pseudofs/dev/tty/terminal/termios.rs#default` | `arceos/ulib/axlibc/c/pow.c#exp_inline` | 同模块上下文对齐/差异观察 | ctl | __math_oflow, __math_uflow, asdouble, asuint64, converttoint, eval_as_double ...(+4) |
| `kernel/src/file/signalfd.rs#from_signal_info` | `arceos/modules/axhal/src/arch/aarch64/trap.rs#handle_sync_exception` | 同模块上下文对齐/差异观察 | sig_info.code, sig_info.errno, sig_info.signo | ESR_EL1.extract, crate::trap::handle_syscall, crate::trap::post_trap_callback, debug!, esr.read, esr.read_as_enum ...(+6) |
| `kernel/src/pseudofs/dev/tty/terminal/termios.rs#signo_for` | `arceos/modules/axhal/src/arch/x86_64/idt.rs#new` | 同模块上下文对齐/差异观察 | Some, self.special_char | InterruptDescriptorTable::new, core::mem::transmute, core::slice::from_raw_parts_mut, entries[i].set_handler_fn, opt.set_privilege_level |

#### 系统调用
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `kernel/src/syscall/io_mpx/epoll.rs#sys_epoll_ctl` | `api/src/imp/io_mpx/epoll.rs#sys_epoll_ctl` | 同板块同名 | Epoll::from_fd, EpollFlags::from_bits, EpollFlags::from_bits(event.events & !events.bits()).ok_or, Err, IoEvents::from_bits_truncate, Ok ...(+7) | EpollInstance::from_fd, EpollInstance::from_fd(epfd)?.control, Err, Ok, debug!, event.get_as_mut |
| `kernel/src/syscall/ipc/shm.rs#sys_shmctl` | `api/src/imp/ipc/shm.rs#sys_shmctl` | 同板块同名 | Err, Ok, SHM_MANAGER.lock, buf.get_as_mut, monotonic_time_nanos, nullable! ...(+3) | Err, Ok, SHM_MANAGER.lock, shm_inner.lock, shm_manager
            .get_inner_by_shmid, shm_manager
            .get_inner_by_shmid(shmid)
            .ok_or |
| `kernel/src/syscall/io_mpx/poll.rs#sys_ppoll` | `api/src/imp/io_mpx/poll.rs#sys_ppoll` | 同板块同名 | check_sigset_size, do_poll, fds.get_as_mut_slice, nfds.try_into, nfds.try_into().map_err, nullable! ...(+4) | Err, core::slice::from_raw_parts_mut, fds.get_as_array, poll_impl, timeout.is_none, timeout.nullable ...(+2) |
| `kernel/src/syscall/io_mpx/select.rs#sys_pselect6` | `api/src/imp/io_mpx/poll.rs#sys_pselect6` | 同板块同名 | do_select, nullable!, nullable!(timeout.get_as_ref())?
            .map, nullable!(timeout.get_as_ref())?
            .map(|ts| ts.try_into_time_value())
            .transpose, ts.try_into_time_value | Err, do_select, timeout.is_none, timeout.nullable, timeout.unwrap |

#### 文件系统
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `kernel/src/syscall/fs/fd_ops.rs#sys_fcntl` | `api/src/imp/fd/fd_ops.rs#sys_fcntl` | 同板块同名 | Err, FD_TABLE
                .read, FD_TABLE
                .read()
                .get, FD_TABLE
                .read()
                .get(fd as _)
                .ok_or, FD_TABLE
                .read()
                .get(fd as _)
                .ok_or(AxError::BadFileDescriptor)?
                .status_flags, FD_TABLE
                .write ...(+23) | Ok, debug!, dup_fd, get_file_like, get_file_like(fd)?.set_nonblocking, warn! |
| `kernel/src/syscall/fs/ctl.rs#sys_linkat` | `api/src/imp/fd/ctl.rs#sys_linkat` | 同板块同名 | Err, Ok, Path::new, crate::syscall::sys::sys_getegid, crate::syscall::sys::sys_geteuid, debug! ...(+18) | Err, Ok, debug!, fs.resolve_nonexistent, new_dir.link, new_path.get_as_str ...(+9) |
| `kernel/src/syscall/fs/io.rs#sys_splice` | `api/src/imp/io.rs#sys_splice` | 同板块同名 | DummyFd::from_fd, DummyFd::from_fd(fd_in).is_ok, DummyFd::from_fd(fd_out).is_ok, Err, File::from_fd, Pipe::from_fd ...(+20) | (len - total_copied).min, Err, Ok, SeekFrom::Current, SeekFrom::Start, buf.len ...(+43) |
| `kernel/src/syscall/fs/stat.rs#sys_faccessat2` | `api/src/imp/fd/stat.rs#sys_faccessat2` | 同板块同名 | Err, Ok, debug!, file.stat, path.as_deref, path.nullable ...(+5) | Err, NodePermission::empty, Ok, file.stat, path.nullable, required_mode.bits ...(+2) |

#### 设备驱动
仅参考项目中观察到该模块代表函数，目标项目没有同模块可对齐证据：
- 参考项目：`arceos/tools/raspi4/chainloader/src/bsp/device_driver/bcm/bcm2xxx_pl011_uart.rs#init`，调用摘要：FBRD::BAUD_DIVFRAC.val, IBRD::BAUD_DIVINT.val, self.flush, self.registers
            .CR
            .write, self.registers
            .LCR_H
            .write, self.registers.CR.set ...(+3)
- 参考项目：`arceos/tools/bwbench_client/src/device.rs#new`，调用摘要：ETH_P_ALL.to_be, Err, Ok, dev.bind_interface, dev.interface_mtu, ifreq.ioctl ...(+5)
- 参考项目：`arceos/tools/bwbench_client/src/main.rs#transmit_benchmark`，调用摘要：Local::now, NetDevice::new, NetDevice::new(interface).unwrap, current_time.signed_duration_since, current_time.signed_duration_since(past_time).num_seconds, dev.send ...(+2)
- 参考项目：`arceos/tools/bwbench_client/src/device.rs#bind_interface`，调用摘要：ETH_P_ALL.to_be, Err, Ok, libc::bind, self.ifreq.ioctl, std::io::Error::last_os_error ...(+2)

#### 网络协议栈
仅参考项目中观察到该模块代表函数，目标项目没有同模块可对齐证据：
- 参考项目：`arceos/ulib/axlibc/c/network.c#inet_pton`，调用摘要：hexval, inet_pton, isdigit, memmove
- 参考项目：`api/src/fd/net.rs#sendto`，调用摘要：Err, IpAddr::V4, Ipv4Addr::new, Ok, SocketAddr::new, socket
                    .bind ...(+3)
- 参考项目：`api/src/fd/net.rs#shutdown`，调用摘要：Ok, tcpsocket.lock, tcpsocket.peer_addr, tcpsocket.shutdown, udpsocket.lock, udpsocket.peer_addr ...(+1)
- 参考项目：`arceos/api/arceos_posix_api/src/imp/net.rs#shutdown`，调用摘要：Ok, tcpsocket.lock, tcpsocket.peer_addr, tcpsocket.shutdown, udpsocket.lock, udpsocket.peer_addr ...(+1)

#### 文件系统/第三方库
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `axfs-ng-local/src/fs/ext4/inode.rs#create` | `arceos/modules/axfs-ng/src/fs/ext4/inode.rs#create` | 同板块同名 | DirEntry::new_dir, DirEntry::new_file, DirNode::new, Err, FileNode::new, Inode::new ...(+14) | DirEntry::new_dir, DirEntry::new_file, DirNode::new, Err, FileNode::new, Inode::new ...(+11) |
| `axfs-ng-local/src/fs/ext4/inode.rs#metadata` | `arceos/modules/axfs-ng/src/fs/ext4/inode.rs#metadata` | 同板块同名同结构 | DeviceId::default, FileAttr::default, NodePermission::from_bits_truncate, Ok, into_vfs_type, self.fs
            .lock ...(+2) | DeviceId::default, FileAttr::default, NodePermission::from_bits_truncate, Ok, into_vfs_type, self.fs
            .lock ...(+2) |
| `axfs-ng-local/src/fs/ext4/inode.rs#read_dir` | `arceos/modules/axfs-ng/src/fs/ext4/inode.rs#read_dir` | 同板块同名同结构 | Ok, core::str::from_utf8, core::str::from_utf8(entry.name())
                .map_err, core::str::from_utf8(entry.name())
                .map_err(|_| VfsError::InvalidData)?
                .to_owned, entry.ino, entry.inode_type ...(+10) | Ok, core::str::from_utf8, core::str::from_utf8(entry.name())
                .map_err, core::str::from_utf8(entry.name())
                .map_err(|_| VfsError::EINVAL)?
                .to_owned, entry.ino, entry.inode_type ...(+10) |
| `axfs-ng-local/src/fs/ext4/inode.rs#update_metadata` | `arceos/modules/axfs-ng/src/fs/ext4/inode.rs#update_metadata` | 同板块同名 | Ok, fs.with_inode_ref, fs.with_inode_ref(self.ino, |inode| {
            if let Some(mode) = update.mode {
                inode.set_mode((inode.mode() & !0xfff) | (mode.bits() as u32));
            }
            if let Some((uid, gid)) = update.owner {
                inode.set_owner(uid as _, gid as _);
            }
            if let Some(atime) = update.atime {
                inode.set_atime(&atime);
            }
            if let Some(mtime) = update.mtime {
                inode.set_mtime(&mtime);
            }
            inode.update_ctime();
            Ok(())
        })
        .map_err, inode.mode, inode.set_atime, inode.set_mode ...(+5) | Ok, fs.with_inode_ref, fs.with_inode_ref(self.ino, |inode| {
            if let Some(mode) = update.mode {
                inode.set_mode((inode.mode() & !0xfff) | (mode.bits() as u32));
            }
            if let Some((uid, gid)) = update.owner {
                inode.set_owner(uid as _, gid as _);
            }
            if let Some(atime) = update.atime {
                inode.set_atime(&atime);
            }
            if let Some(mtime) = update.mtime {
                inode.set_mtime(&mtime);
            }
            Ok(())
        })
        .map_err, inode.mode, inode.set_atime, inode.set_mode ...(+4) |

#### 网络协议栈/第三方库
仅参考项目中观察到该模块代表函数，目标项目没有同模块可对齐证据：
- 参考项目：`arceos/modules/axnet/src/smoltcp_impl/tcp.rs#connect`，调用摘要：Err, Ok, SOCKET_SET
                .with_socket_mut::<tcp::Socket, _, _>, SOCKET_SET.add, SocketSetWrapper::new_tcp_socket, Some ...(+29)
- 参考项目：`arceos/modules/axnet/src/smoltcp_impl/dns.rs#query`，调用摘要：Err, Ok, SOCKET_SET
            .with_socket_mut::<dns::Socket, _, _>, SOCKET_SET
            .with_socket_mut::<dns::Socket, _, _>(handle, |socket| {
                socket.start_query(iface.lock().context(), name, query_type)
            })
            .map_err, SOCKET_SET.poll_interfaces, SOCKET_SET.with_socket_mut::<dns::Socket, _, _> ...(+12)
- 参考项目：`arceos/modules/axnet/src/smoltcp_impl/bench.rs#bench_transmit_bandwidth`，调用摘要：(current_time - past_time).secs, AxNetTxToken::consume, InterfaceWrapper::current_time, info!, self.transmit, tx_buf[0..12].fill ...(+2)
- 参考项目：`arceos/modules/axnet/src/smoltcp_impl/tcp.rs#shutdown`，调用摘要：LISTEN_TABLE.unlisten, Ok, SOCKET_SET.poll_interfaces, SOCKET_SET.with_socket_mut::<tcp::Socket, _, _>, debug!, self.handle.get ...(+9)

#### 第三方库
仅参考项目中观察到该模块代表函数，目标项目没有同模块可对齐证据：
- 参考项目：`crates/visionfive2-sd/src/lib.rs#send_cmd`，调用摘要：RawInterrupt::from, RawInterruptStatusReg::from, S::sleep_ms_until, Some, arg.into, assert! ...(+24)
- 参考项目：`crates/visionfive2-sd/src/lib.rs#init_sdcard`，调用摘要：CardTypeReg::from, CardTypeReg::from(0).with_card_width4_1, CmdArg::new, CmdReg::from, ControlReg::from, RawInterruptStatusReg::from ...(+20)
- 参考项目：`crates/page_table_multiarch/page_table_multiarch/src/bits64.rs#map_region`，调用摘要：Err, M::flush_tlb, Ok, PageSize::Size1G.is_aligned, PageSize::Size2M.is_aligned, PageSize::Size4K.is_aligned ...(+12)
- 参考项目：`crates/axdriver_crates/axdriver_virtio/src/net.rs#try_new`，调用摘要：Err, InnerDev::new, InnerDev::new(transport).map_err, NetBufPool::new, Ok, Some ...(+14)

#### 通用模块/其他
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `kernel/src/time.rs#try_into_time_value` | `arceos/ulib/axlibc/c/printf.c#print_exponential_number` | 同模块上下文对齐/差异观察 | Err, Ok, TimeValue::new | ABS_FOR_PRINTING, PRINTF_ABS, bastardized_floor, get_components, get_normalized_components, get_sign_bit ...(+5) |
| `kernel/src/time.rs#try_into_time_value` | `arceos/ulib/axlibc/c/pow.c#pow` | 同模块上下文对齐/差异观察 | Err, Ok, TimeValue::new | __builtin_fma, __math_invalid, __math_oflow, __math_uflow, asdouble, asuint64 ...(+8) |
| `kernel/src/time.rs#try_into_time_value` | `arceos/ulib/axlibc/c/string.c#twoway_strstr` | 同模块上下文对齐/差异观察 | Err, Ok, TimeValue::new | BITOP, MAX, memchr, memcmp |
| `kernel/src/time.rs#try_into_time_value` | `arceos/ulib/axlibc/c/printf.c#print_broken_up_decimal` | 同模块上下文对齐/差异观察 | Err, Ok, TimeValue::new | out_rev_ |


好的，审计官。以下是基于客观 AST 证据、控辩双方主张及审计指令形成的最终同源性定性仲裁报告。

---

### 1. 宏观判定
- **定性结论**: **独立开发**
- **核心依据**: 客观 AST 相似度仅为 **3.86%**，双向覆盖率均处于极低水平（目标项目 13.95%，参考项目 5.07%）。该数据表明，双方项目在整体代码实现上存在根本性差异。控方提出的“复用指控”所依赖的证据，经核查，多为由 POSIX 标准、Linux ABI 及 Rust 语言惯用法所决定的领域共性，或是对双方共用第三方库/框架代码的误判，未能形成证明抄袭的完整证据链。辩方关于独立开发的辩护与客观 AST 证据更为吻合。

### 2. 全模块覆盖矩阵解读
| 模块 | 双方存在 | 共同指纹 | 覆盖率 (目标/参考) | 证据强度 | 解读 |
| :--- | :---: | :---: | :---: | :---: | :--- |
| **进程管理** | 是 | 3 | 6.0% / 3.57% | **弱** | 共同指纹极少，覆盖率极低。相似性集中在 `sys_getpid`、`sys_exit` 等 POSIX 强制接口及 `clear_child_tid` 等 Linux ABI 原子操作，属于领域共性，不构成抄袭。 |
| **任务调度/异步运行时** | 是 | 2 | 0.91% / 0.69% | **弱** | 覆盖率可忽略。共同指纹为 `sys_futex` 和 `sys_kill` 等系统调用入口，其内部实现（如目标项目用 `block_on`，参考项目用 `wall_time`）完全不同。 |
| **内存管理** | 是 | 11 | 6.21% / 3.78% | **弱** | 覆盖率低。相似性集中在 `sys_mmap`、`sys_shmat` 等标准系统调用，以及 `load_user_app` 这类 ELF 加载流程。这些流程的顶层逻辑由 ABI 和 ELF 格式决定，但底层内存抽象和错误处理路径存在差异。 |
| **中断/异常处理** | 是 | 0 | 0.0% / 0.0% | **未形成** | 目标项目仅有 3 个函数，参考项目有 62 个，且无任何共同指纹。双方在此核心模块的实现完全不同。 |
| **系统调用** | 是 | 1 | 0.71% / 0.78% | **弱** | 覆盖率极低。共同指纹为 `sys_epoll_ctl` 等标准系统调用，其内部实现依赖各自不同的 I/O 多路复用抽象。 |
| **文件系统** | 是 | 91 | 18.72% / 16.08% | **中** | 覆盖率相对较高，但经核查，大量共同指纹源于双方共用的 `axfs-ng` 第三方库（见下文）。去除该库后，原生文件系统实现（如 procfs）的相似度显著降低。 |
| **设备驱动** | 否 | 0 | 0.0% / 0.0% | **未形成** | 仅参考项目拥有此模块，目标项目未实现。 |
| **网络协议栈** | 否 | 0 | 0.0% / 0.0% | **未形成** | 仅参考项目拥有此模块，目标项目未实现。 |
| **文件系统/第三方库** | 是 | 16 | 42.11% / 2.73% | **强（第三方库）** | 目标项目覆盖率高达 42.11%，但所有证据均指向 `axfs-ng` 库。这是双方共用的第三方代码，其相似性是共享依赖的结果，**不能作为抄袭证据**。 |
| **通用模块/其他** | 是 | 0 | 0.0% / 0.0% | **未形成** | 双方均有此模块，但无任何共同指纹，实现完全不同。 |

### 3. 底层 AST 关键证据指纹表
| 所属板块 | 目标项目路径 | 参考项目路径 | 证据类型 | 技术说明 |
| :--- | :--- | :--- | :--- | :--- |
| **第三方库/公共依赖** | `axfs-ng-local/src/fs/ext4/inode.rs#metadata` | `arceos/modules/axfs-ng/src/fs/ext4/inode.rs#metadata` | 同板块同名同结构 | **双方共用 `axfs-ng` 第三方库**。AST 图谱几乎一致，是共享依赖的直接体现，非抄袭。 |
| **第三方库/公共依赖** | `axfs-ng-local/src/fs/ext4/inode.rs#read_dir` | `arceos/modules/axfs-ng/src/fs/ext4/inode.rs#read_dir` | 同板块同名同结构 | **双方共用 `axfs-ng` 第三方库**。AST 图谱几乎一致，是共享依赖的直接体现，非抄袭。 |
| **第三方库/公共依赖** | `axfs-ng-local/src/fs/ext4/inode.rs#update_metadata` | `arceos/modules/axfs-ng/src/fs/ext4/inode.rs#update_metadata` | 同板块同名同结构 | **双方共用 `axfs-ng` 第三方库**。AST 图谱几乎一致，是共享依赖的直接体现，非抄袭。 |
| **进程管理** | `kernel/src/task/mod.rs#set_clear_child_tid` | `core/src/task/thread.rs#set_clear_child_tid` | 同板块底层逻辑一致 | **Linux ABI 共性**。`set_tid_address` 系统调用要求线程控制块存储用户态地址，`AtomicUsize::store` 是唯一实现方式。 |
| **进程管理** | `kernel/src/task/mod.rs#clear_child_tid` | `core/src/task/thread.rs#clear_child_tid` | 同板块底层逻辑一致 | **Linux ABI 共性**。`clear_child_tid` 机制要求退出时读取该地址，`AtomicUsize::load` 是唯一实现方式。 |
| **进程管理** | `kernel/src/syscall/ipc/shm.rs#attach_process` | `api/src/imp/ipc/shm.rs#attach_process` | 同板块同名 | **POSIX 接口共性**。`assert!` 和 `va_range.insert` 是任何共享内存实现的标准操作。目标项目额外调用 `monotonic_time_nanos` 记录时间戳，存在差异。 |
| **进程管理** | `kernel/src/syscall/ipc/shm.rs#detach_process` | `api/src/imp/ipc/shm.rs#detach_process` | 同板块同名 | **POSIX 接口共性**。`assert!` 和 `va_range.remove` 是任何共享内存实现的标准操作。目标项目额外调用 `monotonic_time_nanos` 记录时间戳，存在差异。 |
| **进程管理** | `kernel/src/syscall/task/thread.rs#sys_getppid` | `src/syscall_imp/task/thread.rs#sys_getppid` | 同板块同名 | **POSIX 接口共性**。访问父进程 PID 的调用链由 `getppid` 语义决定。目标项目使用 `ok_or().map()`，参考项目使用 `map_or()`，错误处理策略不同。 |
| **进程管理** | `kernel/src/task/ops.rs#processes` | `core/src/task/process_group.rs#processes` | 同板块同名 | **Rust 语言惯用法**。`.values().collect()` 是从 `Map` 收集值的标准链式调用。目标项目用 `PROCESS_TABLE.read()`，参考项目用 `self.processes.lock()`，同步原语不同。 |

### 4. 核心代码调用流对比剖析
* **【进程管理/进程退出路径】**
* **底层技术析理**: 双方 `do_exit` 函数均遵循“清理资源 -> 通知父进程 -> 让出 CPU”的通用模式，但**清理的具体对象和内存管理方式存在根本差异**。目标项目侧重于清理 System V 共享内存（`SHM_MANAGER.lock().clear_proc_shm`），而参考项目侧重于关闭所有文件描述符（`close_all_filelike`）。在关键的 `clear_child_tid` 处理上，目标项目直接通过 `vm_write(0)` 操作用户态内存，而参考项目则构建 `UserPtr<Tid>` 对象，通过 `get_as` 和 `query_paddr` 进行内核-用户指针转换。这表明两者在用户态内存访问的安全抽象上采用了完全不同的模型。
* **代码溯源链接**:
  - 目标项目: `kernel/src/task/ops.rs#do_exit`
  - 参考项目: `api/src/imp/task.rs#do_exit`

* **【进程管理/信号处理】**
* **底层技术析理**: 双方 `sys_rt_sigprocmask` 函数均实现对线程信号屏蔽字的操作，但**数据访问路径和核心操作完全不同**。目标项目通过 `curr.as_thread()` 获取线程数据后，直接操作用户态内存（`oldset.vm_write`）来返回旧的信号集。参考项目则通过 `current().task_ext().thread.signal.with_blocked_mut` 路径，直接在内核信号管理器的内部状态上进行修改。这体现了两种不同的架构设计：一个倾向于在系统调用层处理用户态数据拷贝，另一个则封装在信号子系统内部。
* **代码溯源链接**:
  - 目标项目: `kernel/src/syscall/signal.rs#sys_rt_sigprocmask`
  - 参考项目: `api/src/imp/signal.rs#sys_rt_sigprocmask`

* **【内存管理/共享内存挂载】**
* **底层技术析理**: 双方 `sys_shmat` 系统调用的顶层流程相似，但**底层内存映射模型不同**。目标项目通过创建 `Backend::new_shared` 后端来管理共享内存页，而参考项目则直接使用 `MappingFlags::all` 进行映射。目标项目引入了 `SharedPages` 这一中间抽象层来管理物理页面的共享，而参考项目则无此抽象。这反映了在内存管理子系统设计上的独立决策。
* **代码溯源链接**:
  - 目标项目: `kernel/src/syscall/ipc/shm.rs#sys_shmat`
  - 参考项目: `api/src/imp/ipc/shm.rs#sys_shmat`

### 5. 单方模块与降权项
- **单方模块**: **设备驱动**、**网络协议栈**、**第三方库**（非文件系统部分）仅存在于参考项目中。目标项目在这些模块的函数数为 0。如果存在系统性抄袭，目标项目理应包含这些模块的实现，事实恰恰相反，这支持了独立开发的结论。
- **第三方库/公共依赖**: 文件系统模块中覆盖率最高的证据均来自 `axfs-ng` 库。该库是双方共用的第三方代码，其内部函数（如 `ext4/inode.rs` 中的 `metadata`、`read_dir` 等）的高度相似性是共享依赖的必然结果，**必须从抄袭证据中排除**。
- **跨板块/跨语言硬凑**: 控方及全模块对比中，将目标项目的 `time.rs::try_into_time_value` 与参考项目的 C 语言数学库函数（`pow.c::pow`）、格式化打印函数（`printf.c`）等进行配对，这些配对在逻辑上毫无关联，属于无效证据，**必须排除**。
- **POSIX/Linux ABI 与语言惯用法**: `sys_getpid`、`sys_exit`、`sys_exit_group` 等函数名及核心调用（如 `current().as_thread().proc_data.proc.pid`）由 POSIX 标准或 Linux ABI 强制规定。`Ok/Err/Some`、`assert!`、`.lock()`、`.values().collect()` 等是 Rust 语言的通用惯用法或标准库 API。这些均不能作为抄袭证据。

### 6. 保守结论
综合评估，目标项目 [2026-T2026103369910012-XX] 与参考项目 [2025-狗剩-海南大学] 的客观 AST 相似度仅为 3.86%，远低于构成抄袭嫌疑的阈值。双方在进程管理、内存管理等核心板块的相似性，绝大部分可归因于 POSIX/Linux ABI 的强制性要求、Rust 语言惯用法以及共用第三方库 `axfs-ng`。在关键的实现细节上，如进程退出时的资源清理对象、信号处理的数据流、用户态内存访问的安全抽象以及共享内存的映射模型，双方均表现出明确的架构级差异。参考项目独有的设备驱动、网络协议栈等模块在目标项目中完全缺失，进一步否定了系统性抄袭的可能。因此，本次审计判定目标项目为独立开发，整体抄袭风险极低。

------------------------------

