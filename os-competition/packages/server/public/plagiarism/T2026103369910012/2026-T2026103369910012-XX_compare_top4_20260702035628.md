# 🛡️ OS 内核赛自动化代码级架构审计报告

**审计时间**: `2026-07-02 03:56:28`
**参赛作品名称**: `2026-T2026103369910012-XX`
**年份**: `2026`
**学校**: `XX`
**队伍名称**: `T2026103369910012`
**比赛名字**: `OS-Competition`
**比赛子赛道**: `Kernel-Implementation`
**仓库地址**: `https://gitlab.eduxiji.net/educg-group-43501-3132633/T2026103369910012-831`
**推测开发语言**: `Rust / C (由AST探针自动识别)`
**特征函数量**: `1270`

## ⚔️ 底层代码同源性对比 (AST Homology)
> 本部分由专门的代码审查智能体，对该项目与符合年份条件的历届最高相似度项目进行控制流与底层调用对比。新版报告会先给出确定性的全模块覆盖矩阵，再给出 Agent 的分模块解释，避免只围绕少量抽样函数展开。

### 🏆 Top 4 底层对标: vs `2025-undefined-清华大学`（清华大学 / undefined）

#### 对方作品信息

**对方年份**: `2025`
**对方学校**: `清华大学`
**对方队伍名称**: `undefined`
**对方比赛名字**: `OS-Competition`
**对方比赛子赛道**: `Kernel-Implementation`
**对方仓库地址**: `https://gitlab.eduxiji.net/educg-group-36002-2710490/T202510003995291-2331`

> 双向覆盖率：共同 AST 指纹: 139；唯一 AST 指纹: 目标项目 1097，参考项目 2820；覆盖率: 目标项目 12.67%，参考项目 4.93%

## 全模块对比总览（确定性证据）

> 总体覆盖率：共同 AST 指纹: 139；唯一 AST 指纹: 目标项目 1097，参考项目 2820；覆盖率: 目标项目 12.67%，参考项目 4.93%

### 1. 模块覆盖矩阵
| 模块 | 目标函数数 | 参考函数数 | 共同唯一 AST 指纹 | Target 覆盖 | Base 覆盖 | 状态 |
| :--- | ---: | ---: | ---: | ---: | ---: | :--- |
| 进程管理 | 54 | 135 | 3 | 6.0% | 2.48% | 双方存在且有结构重合 |
| 任务调度/异步运行时 | 243 | 415 | 2 | 0.91% | 0.67% | 双方存在且有结构重合 |
| 内存管理 | 188 | 450 | 12 | 6.78% | 3.14% | 双方存在且有结构重合 |
| 中断/异常处理 | 3 | 53 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |
| 系统调用 | 150 | 109 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |
| 文件系统 | 579 | 999 | 90 | 18.52% | 13.47% | 双方存在且有结构重合 |
| 设备驱动 | 0 | 133 | 0 | 0.0% | 0.0% | 仅参考项目体现 |
| 网络协议栈 | 0 | 65 | 0 | 0.0% | 0.0% | 仅参考项目体现 |
| 文件系统/第三方库 | 38 | 643 | 16 | 42.11% | 2.96% | 双方存在且有结构重合 |
| 网络协议栈/第三方库 | 0 | 130 | 0 | 0.0% | 0.0% | 仅参考项目体现 |
| 通用模块/其他 | 15 | 555 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |

### 2. 分模块证据清单

#### 进程管理
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `kernel/src/syscall/task/execve.rs#sys_execve` | `api/src/interface/task/execve.rs#sys_execve` | 同板块同名 | Arc::new, Default::default, Err, FD_TABLE.write, FS_CONTEXT.lock, FS_CONTEXT.lock().resolve ...(+43) | BUSYBOX.to_string, Some, abs_path.to_string, args[1..].iter, args[1..].iter().cloned, argv.clone ...(+10) |
| `kernel/src/task/ops.rs#do_exit` | `arceos/examples/shell/src/cmd.rs#do_exit` | 同板块同名 | FutexKey::new_current, SignalInfo::new_kernel, Some, axtask::yield_now, clear_child_tid.vm_write, clear_child_tid.vm_write(0).is_ok ...(+31) | println!, std::process::exit |
| `kernel/src/syscall/signal.rs#sys_rt_sigprocmask` | `api/src/imp/task/signal.rs#sys_rt_sigprocmask` | 同板块同名 | Err, Ok, check_sigset_size, curr.as_thread, current, debug! ...(+7) | Err, Ok, check_sigset_size, current_thread_data, current_thread_data()
        .signal
        .with_blocked_mut::<LinuxResult<_>>, oldset.nullable ...(+1) |
| `kernel/src/syscall/task/wait.rs#apply` | `api/src/imp/task/wait.rs#apply` | 同板块同名 | child.group, child.group().pgid, child.pid | child.get_group, child.get_group().get_pgid, child.get_pid |

#### 任务调度/异步运行时
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `kernel/src/syscall/sync/futex.rs#sys_futex` | `api/src/imp/task/futex.rs#sys_futex` | 同板块同名 | Err, FutexKey::new_current, Ok, Some, assert_unsigned, axtask::yield_now ...(+17) | Err, Ok, axtask::yield_now, current_process_data, drop, futex_table
                .entry ...(+27) |
| `kernel/src/syscall/signal.rs#sys_kill` | `api/src/imp/task/signal.rs#sys_kill` | 同板块同名 | Err, Ok, Some, current, current().as_thread, current().as_thread().proc_data.proc.group ...(+10) | Err, Ok, current_process, current_process().get_group, debug!, get_all_processes ...(+8) |
| `kernel/src/pseudofs/mod.rs#mount_all` | `api/src/core/fs/mount.rs#mount_all` | 同板块同名 | FS_CONTEXT.lock, Ok, Path::new, Path::new("/sys/class/graphics/fb0/device").components, PathBuf::new, axfs::new_default ...(+18) | Ok, dev::new_devfs, mount_at, proc::new_procfs, tmp::MemoryFs::new |
| `kernel/src/syscall/task/clone.rs#sys_clone` | `api/src/interface/task/clone.rs#sys_clone` | 同板块同名 | CloneFlags::from_bits_truncate, Err, args.do_clone, clone_flags.contains | addr_child_tid.clone, addr_parent_tid.clone, sys_clone_ |

#### 内存管理
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `kernel/src/syscall/mod.rs#handle_syscall` | `src/syscall.rs#handle_syscall` | 同板块同名 | Err, LinuxError::ENOSYS.code, LinuxError::from, LinuxError::from(err).code, Ok, Sysno::new ...(+242) | Err, Sysno::new, err.code, info!, result.unwrap_or_else, set_trap_frame ...(+151) |
| `kernel/src/syscall/mm/mmap.rs#sys_mmap` | `api/src/imp/mm/mmap.rs#sys_mmap` | 同板块同名 | (addr + length).align_up, Arc::new, Backend::new_alloc, Backend::new_cow, Backend::new_file, Backend::new_linear ...(+50) | Err, File::from_fd, MmapFlags::from_bits_truncate, MmapProt::from_bits_truncate, Ok, PhysAddr::from ...(+34) |
| `kernel/src/mm/loader.rs#load_user_app` | `core/src/mm.rs#load_user_app` | 同板块同名 | "sh".to_owned, (ustack_top - user_sp_aligned).align_up_4k, Backend::new_alloc, ELF_LOADER.lock, ELF_LOADER.lock().load, Err ...(+62) | CStr::from_bytes_with_nul, CStr::from_bytes_with_nul(interp)
            .map_err, CStr::from_bytes_with_nul(interp)
            .map_err(|_| AxError::InvalidData)?
            .to_str, CStr::from_bytes_with_nul(interp)
            .map_err(|_| AxError::InvalidData)?
            .to_str()
            .map_err, CStr::from_bytes_with_nul(interp)
            .map_err(|_| AxError::InvalidData)?
            .to_str()
            .map_err(|_| AxError::InvalidData)?
            .to_string, ElfFile::new ...(+34) |
| `kernel/src/syscall/ipc/shm.rs#sys_shmat` | `api/src/interface/mm/shm.rs#sys_shmat` | 同板块同名 | Arc::new, Backend::new_shared, Ok, SHM_MANAGER.lock, SharedPages::new, ShmAtFlags::from_bits_truncate ...(+27) | Err, Ok, SHARED_MEMORY_MANAGER.get, SHARED_MEMORY_MANAGER.get(key).ok_or, ShmFlags::from_bits_truncate, Some ...(+16) |

#### 中断/异常处理
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `kernel/src/pseudofs/dev/tty/terminal/termios.rs#default` | `arceos/ulib/axlibc/c/pow.c#exp_inline` | 同模块上下文对齐/差异观察 | ctl | __math_oflow, __math_uflow, asdouble, asuint64, converttoint, eval_as_double ...(+4) |
| `kernel/src/file/signalfd.rs#from_signal_info` | `arceos/modules/axhal/src/arch/aarch64/trap.rs#handle_sync_exception` | 同模块上下文对齐/差异观察 | sig_info.code, sig_info.errno, sig_info.signo | ESR_EL1.extract, crate::trap::handle_syscall, crate::trap::post_trap_callback, debug!, esr.read, esr.read_as_enum ...(+6) |
| `kernel/src/pseudofs/dev/tty/terminal/termios.rs#signo_for` | `arceos/modules/axhal/src/arch/x86_64/idt.rs#new` | 同模块上下文对齐/差异观察 | Some, self.special_char | InterruptDescriptorTable::new, core::mem::transmute, core::slice::from_raw_parts_mut, entries[i].set_handler_fn, opt.set_privilege_level |

#### 系统调用
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `kernel/src/syscall/net/opt.rs#sys_setsockopt` | `api/src/imp/net/socket.rs#sys_setsockopt` | 同板块同名 | Err, Ok, Socket::from_fd, call_dispatch!, debug!, matches! ...(+3) | Err, Ok, Socket::from_fd, Socket::from_fd(sockfd)?.setsockopt, core::slice::from_raw_parts, debug! ...(+1) |
| `kernel/src/syscall/time.rs#sys_times` | `api/src/imp/utils/time.rs#sys_times` | 同板块同名 | Ok, current, current().as_thread, current().as_thread().time.borrow, current().as_thread().time.borrow().output, monotonic_time_nanos ...(+4) | Ok, monotonic_time_nanos, nanos_to_ticks, time_stat_output, tms.get |
| `kernel/src/syscall/net/socket.rs#sys_connect` | `api/src/imp/net/socket.rs#sys_connect` | 同板块同名 | Ok, Socket::from_fd, Socket::from_fd(fd)?.connect, Socket::from_fd(fd)?.connect(addr).map_err, SocketAddrEx::read_from_user, debug! | Ok, Socket::from_fd, Socket::from_fd(socket_fd)?.connect, debug!, from_sockaddr |
| `kernel/src/syscall/net/socket.rs#sys_shutdown` | `api/src/imp/net/socket.rs#sys_shutdown` | 同板块同名 | Err, Socket::from_fd, debug!, socket.shutdown, socket.shutdown(how).map | Ok, Socket::from_fd, Socket::from_fd(socket_fd)?.shutdown, debug! |

#### 文件系统
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `kernel/src/pseudofs/dev/fb.rs#ioctl` | `api/src/core/fs/imp/dev/framebuffer.rs#ioctl` | 同板块同名 | (arg as *mut FixScreenInfo).vm_write, (arg as *mut VarScreenInfo).vm_write, Err, Ok, axdisplay::framebuffer_info | Default::default, Err, Ok, arg.into, framebuffer_flush, user_info.get_as_mut_ref |
| `kernel/src/pseudofs/dev/loop.rs#ioctl` | `api/src/core/fs/pseudo/file.rs#ioctl` | 同板块同名 | (arg as *const loop_info).vm_read_uninit, (arg as *const loop_info).vm_read_uninit()?.assume_init, (arg as *const u32).vm_read, (arg as *mut loop_info).vm_write, (arg as *mut u32).vm_write, (arg as *mut u64).vm_write ...(+22) | Err, warn! |
| `kernel/src/syscall/fs/ctl.rs#sys_linkat` | `api/src/interface/fs/path.rs#sys_linkat` | 同板块同名 | Err, Ok, Path::new, crate::syscall::sys::sys_getegid, crate::syscall::sys::sys_geteuid, debug! ...(+18) | new_path.get_as_str, nullable!, sys_link_impl |
| `kernel/src/syscall/fs/io.rs#sys_splice` | `api/src/interface/fs/io.rs#sys_splice` | 同板块同名 | DummyFd::from_fd, DummyFd::from_fd(fd_in).is_ok, DummyFd::from_fd(fd_out).is_ok, Err, File::from_fd, Pipe::from_fd ...(+20) | Err, Ok, fd_lookup, file_like_as::<Pipe>, file_like_as::<Pipe>(in_file.clone()).is_none, file_like_as::<Pipe>(out_file.clone()).is_none ...(+6) |

#### 设备驱动
仅参考项目中观察到该模块代表函数，目标项目没有同模块可对齐证据：
- 参考项目：`arceos/tools/raspi4/chainloader/src/bsp/device_driver/bcm/bcm2xxx_pl011_uart.rs#init`，调用摘要：FBRD::BAUD_DIVFRAC.val, IBRD::BAUD_DIVINT.val, self.flush, self.registers
            .CR
            .write, self.registers
            .LCR_H
            .write, self.registers.CR.set ...(+3)
- 参考项目：`arceos/tools/bwbench_client/src/device.rs#new`，调用摘要：ETH_P_ALL.to_be, Err, Ok, dev.bind_interface, dev.interface_mtu, ifreq.ioctl ...(+5)
- 参考项目：`arceos/tools/bwbench_client/src/main.rs#transmit_benchmark`，调用摘要：Local::now, NetDevice::new, NetDevice::new(interface).unwrap, current_time.signed_duration_since, current_time.signed_duration_since(past_time).num_seconds, dev.send ...(+2)
- 参考项目：`arceos/tools/bwbench_client/src/device.rs#bind_interface`，调用摘要：ETH_P_ALL.to_be, Err, Ok, libc::bind, self.ifreq.ioctl, std::io::Error::last_os_error ...(+2)

#### 网络协议栈
仅参考项目中观察到该模块代表函数，目标项目没有同模块可对齐证据：
- 参考项目：`arceos/ulib/axlibc/c/network.c#inet_pton`，调用摘要：hexval, inet_pton, isdigit, memmove
- 参考项目：`api/src/imp/net/socket.rs#setsockopt`，调用摘要：Ok, SocketOptionLevel::try_from, error!, udpsocket.lock, unimplemented!, warn!
- 参考项目：`api/src/imp/net/socket.rs#sendto`，调用摘要：Err, IpAddr::V4, Ipv4Addr::new, SocketAddr::new, udpsocket
                    .bind, udpsocket
                    .bind(SocketAddr::new(IpAddr::V4(Ipv4Addr::new(127, 0, 0, 1)), 0))
                    .map_err ...(+3)
- 参考项目：`api/src/imp/net/socket.rs#shutdown`，调用摘要：Ok, tcpsocket.lock, tcpsocket.peer_addr, tcpsocket.shutdown, udpsocket.lock, udpsocket.peer_addr ...(+1)

#### 文件系统/第三方库
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `axfs-ng-local/src/fs/ext4/inode.rs#create` | `arceos/modules/axfs-ng/src/fs/ext4/inode.rs#create` | 同板块同名 | DirEntry::new_dir, DirEntry::new_file, DirNode::new, Err, FileNode::new, Inode::new ...(+14) | DirEntry::new_dir, DirEntry::new_file, DirNode::new, Err, FileNode::new, Inode::new ...(+11) |
| `axfs-ng-local/src/fs/ext4/inode.rs#metadata` | `arceos/modules/axfs-ng/src/fs/ext4/inode.rs#metadata` | 同板块同名同结构 | DeviceId::default, FileAttr::default, NodePermission::from_bits_truncate, Ok, into_vfs_type, self.fs
            .lock ...(+2) | DeviceId::default, FileAttr::default, NodePermission::from_bits_truncate, Ok, into_vfs_type, self.fs
            .lock ...(+2) |
| `axfs-ng-local/src/fs/ext4/inode.rs#read_dir` | `arceos/modules/axfs-ng/src/fs/ext4/inode.rs#read_dir` | 同板块同名同结构 | Ok, core::str::from_utf8, core::str::from_utf8(entry.name())
                .map_err, core::str::from_utf8(entry.name())
                .map_err(|_| VfsError::InvalidData)?
                .to_owned, entry.ino, entry.inode_type ...(+10) | Ok, core::str::from_utf8, core::str::from_utf8(entry.name())
                .map_err, core::str::from_utf8(entry.name())
                .map_err(|_| VfsError::EINVAL)?
                .to_owned, entry.ino, entry.inode_type ...(+10) |
| `axfs-ng-local/src/fs/ext4/inode.rs#update_metadata` | `arceos/modules/axfs-ng/src/fs/ext4/inode.rs#update_metadata` | 同板块同名 | Ok, fs.with_inode_ref, fs.with_inode_ref(self.ino, |inode| {
            if let Some(mode) = update.mode {
                inode.set_mode((inode.mode() & !0xfff) | (mode.bits() as u32));
            }
            if let Some((uid, gid)) = update.owner {
                inode.set_owner(uid as _, gid as _);
            }
            if let Some(atime) = update.atime {
                inode.set_atime(&atime);
            }
            if let Some(mtime) = update.mtime {
                inode.set_mtime(&mtime);
            }
            inode.update_ctime();
            Ok(())
        })
        .map_err, inode.mode, inode.set_atime, inode.set_mode ...(+5) | Ok, fs.with_inode_ref, fs.with_inode_ref(self.ino, |inode| {
            if let Some(mode) = update.mode {
                inode.set_mode((inode.mode() & !0xfff) | (mode.bits() as u32));
            }
            if let Some((uid, gid)) = update.owner {
                inode.set_owner(uid as _, gid as _);
            }
            if let Some(atime) = update.atime {
                inode.set_atime(&atime);
            }
            if let Some(mtime) = update.mtime {
                inode.set_mtime(&mtime);
            }
            Ok(())
        })
        .map_err, inode.mode, inode.set_atime, inode.set_mode ...(+4) |

#### 网络协议栈/第三方库
仅参考项目中观察到该模块代表函数，目标项目没有同模块可对齐证据：
- 参考项目：`arceos/modules/axnet/src/smoltcp_impl/tcp.rs#connect`，调用摘要：Err, Ok, Ok::<(IpEndpoint, IpEndpoint), AxError>, SOCKET_SET
                .with_socket_mut::<tcp::Socket, _, _>, SOCKET_SET.add, SocketSetWrapper::new_tcp_socket ...(+32)
- 参考项目：`arceos/modules/axnet/src/smoltcp_impl/dns.rs#query`，调用摘要：Err, Ok, SOCKET_SET
            .with_socket_mut::<dns::Socket, _, _>, SOCKET_SET
            .with_socket_mut::<dns::Socket, _, _>(handle, |socket| {
                socket.start_query(iface.lock().context(), name, query_type)
            })
            .map_err, SOCKET_SET.poll_interfaces, SOCKET_SET.with_socket_mut::<dns::Socket, _, _> ...(+12)
- 参考项目：`arceos/modules/axnet/src/smoltcp_impl/tcp.rs#recv_timeout`，调用摘要：Err, Ok, SOCKET_SET.with_socket_mut::<tcp::Socket, _, _>, ax_err!, ax_err_type!, current_ticks ...(+11)
- 参考项目：`arceos/modules/axnet/src/smoltcp_impl/bench.rs#bench_transmit_bandwidth`，调用摘要：(current_time - past_time).secs, AxNetTxToken::consume, InterfaceWrapper::current_time, info!, self.transmit, tx_buf[0..12].fill ...(+2)

#### 通用模块/其他
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `kernel/src/time.rs#try_into_time_value` | `arceos/ulib/axlibc/c/printf.c#print_exponential_number` | 同模块上下文对齐/差异观察 | Err, Ok, TimeValue::new | ABS_FOR_PRINTING, PRINTF_ABS, bastardized_floor, get_components, get_normalized_components, get_sign_bit ...(+5) |
| `kernel/src/time.rs#try_into_time_value` | `arceos/ulib/axlibc/c/pow.c#pow` | 同模块上下文对齐/差异观察 | Err, Ok, TimeValue::new | __builtin_fma, __math_invalid, __math_oflow, __math_uflow, asdouble, asuint64 ...(+8) |
| `kernel/src/time.rs#try_into_time_value` | `arceos/ulib/axlibc/c/string.c#twoway_strstr` | 同模块上下文对齐/差异观察 | Err, Ok, TimeValue::new | BITOP, MAX, memchr, memcmp |
| `kernel/src/time.rs#try_into_time_value` | `arceos/ulib/axlibc/c/printf.c#print_broken_up_decimal` | 同模块上下文对齐/差异观察 | Err, Ok, TimeValue::new | out_rev_ |


好的，我将遵循审计指令，基于提供的证据材料，进行最终的定性仲裁。

### 1. 宏观判定
- **定性结论**: **独立开发 / 底层架构显著差异**
  客观 AST 相似度仅为 3.68%，双向覆盖率均处于极低水平（目标项目 12.67%，参考项目 4.93%）。该数据表明，双方项目在整体代码实现上存在根本性差异。控方所指控的“复用”行为，经分析，其证据多源于 Linux/POSIX 标准 ABI 的强制命名、共享上游框架（ArceOS）的公共调用以及内核开发的通用范式，不足以证明存在系统性的抄袭或逻辑平移。

### 2. 全模块覆盖矩阵解读
- **进程管理**: 双方均存在该模块。存在 3 个共同 AST 指纹，目标覆盖率 6.0%，参考覆盖率 2.48%。覆盖率极低，共同指纹源于 `is_clone_child` 等极简通用逻辑。证据强度：**弱**。
- **任务调度/异步运行时**: 双方均存在该模块。存在 2 个共同 AST 指纹，目标覆盖率 0.91%，参考覆盖率 0.67%。覆盖率极低，共同指纹源于 `sys_futex` 等系统调用入口。证据强度：**弱**。
- **内存管理**: 双方均存在该模块。存在 12 个共同 AST 指纹，目标覆盖率 6.78%，参考覆盖率 3.14%。覆盖率低，共同指纹源于 `sys_mmap` 等标准系统调用。证据强度：**弱**。
- **中断/异常处理**: 双方均存在该模块。共同 AST 指纹为 0，覆盖率均为 0.0%。双方在此模块无任何可观测的结构重合。证据强度：**未形成**。
- **系统调用**: 双方均存在该模块。共同 AST 指纹为 0，覆盖率均为 0.0%。尽管存在同名系统调用函数，但 AST 层面无共同指纹，表明实现路径完全不同。证据强度：**未形成**。
- **文件系统**: 双方均存在该模块。存在 90 个共同 AST 指纹，目标覆盖率 18.52%，参考覆盖率 13.47%。覆盖率相对较高，但证据主要集中在 `axfs-ng` 等第三方库和 procfs 的标准实现上。证据强度：**中**（主要由第三方库贡献）。
- **设备驱动**: 仅参考项目体现。目标项目无此模块，覆盖率 0.0%。证据强度：**未形成**。
- **网络协议栈**: 仅参考项目体现。目标项目无此模块，覆盖率 0.0%。证据强度：**未形成**。
- **第三方库/公共依赖**: 双方均存在。存在 16 个共同 AST 指纹，目标覆盖率 42.11%，参考覆盖率 2.96%。目标项目在该模块覆盖率极高，但这是因其直接 fork 或引用了与参考项目同源的 `axfs-ng` 等上游 crate，属于共享依赖，不能作为抄袭证据。证据强度：**强**（但性质为共享上游依赖）。

### 3. 底层 AST 关键证据指纹表
| 所属板块 | 目标项目路径 | 参考项目路径 | 证据类型 | 技术说明 |
| :--- | :--- | :--- | :--- | :--- |
| 第三方库/公共依赖 | `axfs-ng-local/src/fs/ext4/inode.rs#create` | `arceos/modules/axfs-ng/src/fs/ext4/inode.rs#create` | 同板块同名同结构 | 双方均使用来自 ArceOS 上游的 `axfs-ng` crate，AST 高度一致是共享依赖的必然结果，非项目间抄袭。 |
| 第三方库/公共依赖 | `axfs-ng-local/src/fs/ext4/inode.rs#metadata` | `arceos/modules/axfs-ng/src/fs/ext4/inode.rs#metadata` | 同板块同名同结构 | 同上，属于共享上游 `axfs-ng` 库的代码。 |
| 第三方库/公共依赖 | `axfs-ng-local/src/fs/ext4/inode.rs#read_dir` | `arceos/modules/axfs-ng/src/fs/ext4/inode.rs#read_dir` | 同板块同名同结构 | 同上，属于共享上游 `axfs-ng` 库的代码。 |
| 进程管理 | `kernel/src/task/mod.rs#is_clone_child` | `core/src/process.rs#is_clone_child` | 同板块底层逻辑一致 | 双方均实现对 `Option` 字段的判空，AST 坍缩为 `Some` 是 Rust 类型系统下的最简表达，属通用范式。 |
| 进程管理 | `kernel/src/syscall/task/wait.rs#apply` | `api/src/imp/task/wait.rs#apply` | 同板块同名 | 实现 POSIX `waitpid` 的进程组匹配语义，数据流（子进程->进程组->pgid）由 ABI 强制定义，非代码复用。 |
| 进程管理 | `kernel/src/syscall/signal.rs#sys_rt_sigprocmask` | `api/src/imp/task/signal.rs#sys_rt_sigprocmask` | 同板块同名 | 实现 Linux 标准信号掩码操作，调用 `check_sigset_size` 和 `nullable` 是 ABI 合规的必然步骤。 |
| 进程管理 | `kernel/src/pseudofs/proc.rs#child_names` | 无直接对应 | 同板块证据 | 硬编码的字符串数组是 Linux `/proc/[pid]/` 目录的标准条目，由 `proc(5)` 手册规定，非原告独创。 |
| 内存管理 | `kernel/src/syscall/mm/mmap.rs#sys_mmap` | `api/src/imp/mm/mmap.rs#sys_mmap` | 同板块同名 | 实现 POSIX `mmap` 语义，双方均需处理地址、长度、标志位等，流程相似源于接口规范。 |
| 任务调度/异步运行时 | `kernel/src/syscall/sync/futex.rs#sys_futex` | `api/src/imp/task/futex.rs#sys_futex` | 同板块同名 | 实现 Linux `futex` 系统调用，均依赖 `axtask::yield_now` 等共享框架调度原语。 |

### 4. 核心代码调用流对比剖析
* **【进程管理/进程退出】**
* **底层技术析理**: 目标项目的 `do_exit` 与参考项目的 `sys_exit_impl` 虽同为进程退出核心，但清理路径差异显著。目标项目显式调用 `FutexKey::new_current`、`SHM_MANAGER.lock().clear_proc_shm` 和 `handle_futex_death`，表明其自行实现了 Futex 健壮列表和共享内存的清理逻辑。而参考项目的调用流则侧重于 `close_all_file_like` 和 `axtask::exit`。两者在资源回收的策略和依赖的内部子系统上完全不同，体现了异构的架构设计。
* **代码溯源链接**:
  - 目标项目: `kernel/src/task/ops.rs#do_exit`
  - 参考项目: `api/src/imp/task/exit.rs#sys_exit_impl`

* **【内存管理/内存映射】**
* **底层技术析理**: 在 `sys_mmap` 实现中，目标项目使用 `Backend::new_alloc`、`Backend::new_cow`、`Backend::new_file` 等多种后端策略进行内存抽象，而参考项目则直接使用 `PhysAddr::from` 等更底层的物理地址操作。这揭示了两者在内存管理模型上的根本分歧：目标项目采用了更复杂的后端抽象层来统一管理不同类型的内存映射，而参考项目的实现更贴近底层硬件。
* **代码溯源链接**:
  - 目标项目: `kernel/src/syscall/mm/mmap.rs#sys_mmap`
  - 参考项目: `api/src/imp/mm/mmap.rs#sys_mmap`

* **【文件系统/伪文件系统挂载】**
* **底层技术析理**: 两者都挂载 `proc` 文件系统，但使用了完全不同的文件系统抽象。目标项目使用 `SimpleFs::new_with`，暗示其 procfs 实现是静态或结构简单的；而参考项目使用 `DynamicFs::new_with`，表明其采用了动态节点生成机制。`"proc".into()` 是 Linux 标准的挂载点名，不能作为抄袭证据，而不同的文件系统类型选择恰恰证明了独立的设计决策。
* **代码溯源链接**:
  - 目标项目: `kernel/src/pseudofs/proc.rs#new_procfs`
  - 参考项目: `api/src/core/fs/imp/proc.rs#new_procfs`

### 5. 单方模块与降权项
- **单方模块**: **设备驱动**、**网络协议栈**及其**第三方库**仅存在于参考项目中。目标项目完全缺失这些模块，这是两者架构范围和目标定位不同的直接体现，是“独立开发”的有力反证。
- **降权项**:
  - **跨板块证据**: 控方将参考项目中用户态 shell (`arceos/examples/shell/src/cmd.rs#do_exit`)、启动加载器、测试程序等与目标项目内核函数进行对比，属于跨抽象层、跨模块的强行关联，不构成有效证据。
  - **通用操作**: `pending_exit` 与 `is_zombie` 中对原子变量的 `.load()` 操作，`is_clone_child` 中的 `Some` 判空，均为 Rust 语言的基础惯用法，不具备任何独创性，不能作为同源证据。
  - **共享框架调用**: 双方对 `PROCESS_GROUP_TABLE`、`FD_TABLE`、`axtask::yield_now` 等 ArceOS 框架公共 API 的调用，是使用同一框架开发的必然结果，其产生的 AST 重合应归因于上游依赖，而非项目间抄袭。

### 6. 保守结论
综合全模块证据，目标项目与参考项目的客观 AST 相似度仅为 3.68%，双向覆盖率均低于 13%。所谓“抄袭”指控的核心证据，经技术析理，均指向 Linux/POSIX 标准 ABI 的强制性接口规范、ArceOS 共享框架的公共依赖以及 Rust 语言内核开发的通用范式。双方在进程退出、内存管理、文件系统抽象等核心子系统上展现出截然不同的设计策略和实现路径，且目标项目缺失设备驱动、网络协议栈等参考项目的关键模块。因此，整体风险极低，无证据表明存在系统性的代码抄袭或架构复用，应认定为基于同一操作系统规范（Linux ABI）和共享上游生态（ArceOS）的独立开发作品。

------------------------------

