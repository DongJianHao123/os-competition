# 🛡️ OS 内核赛自动化代码级架构审计报告

**审计时间**: `2026-07-02 14:58:37`
**参赛作品名称**: `2026-T2026105749910169-XX`
**年份**: `2026`
**学校**: `XX`
**队伍名称**: `T2026105749910169`
**比赛名字**: `OS-Competition`
**比赛子赛道**: `Kernel-Implementation`
**仓库地址**: `https://gitlab.eduxiji.net/educg-group-43501-3132633/26QS`
**推测开发语言**: `Rust / C (由AST探针自动识别)`
**特征函数量**: `1818`

## ⚔️ 底层代码同源性对比 (AST Homology)
> 本部分由专门的代码审查智能体，对该项目与符合年份条件的历届最高相似度项目进行控制流与底层调用对比。新版报告会先给出确定性的全模块覆盖矩阵，再给出 Agent 的分模块解释，避免只围绕少量抽样函数展开。

### 🏆 Top 1 底层对标: vs `2024-萌新-南开大学`（南开大学 / 萌新）

#### 对方作品信息

**对方年份**: `2024`
**对方学校**: `南开大学`
**对方队伍名称**: `萌新`
**对方比赛名字**: `OS-Competition`
**对方比赛子赛道**: `Kernel-Implementation`
**对方仓库地址**: `https://gitlab.eduxiji.net/educg-group-26010-2376550/T202410055992606-2876`

> 双向覆盖率：共同 AST 指纹: 28；唯一 AST 指纹: 目标项目 1626，参考项目 2483；覆盖率: 目标项目 1.72%，参考项目 1.13%

## 全模块对比总览（确定性证据）

> 总体覆盖率：共同 AST 指纹: 28；唯一 AST 指纹: 目标项目 1626，参考项目 2483；覆盖率: 目标项目 1.72%，参考项目 1.13%

### 1. 模块覆盖矩阵
| 模块 | 目标函数数 | 参考函数数 | 共同唯一 AST 指纹 | Target 覆盖 | Base 覆盖 | 状态 |
| :--- | ---: | ---: | ---: | ---: | ---: | :--- |
| 进程管理 | 255 | 39 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |
| 任务调度/异步运行时 | 190 | 272 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |
| 内存管理 | 291 | 174 | 1 | 0.36% | 0.6% | 双方存在且有结构重合 |
| 中断/异常处理 | 35 | 29 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |
| 系统调用 | 143 | 67 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |
| 文件系统 | 219 | 285 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |
| 设备驱动 | 108 | 42 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |
| 网络协议栈 | 2 | 0 | 0 | 0.0% | 0.0% | 仅目标项目体现 |
| 文件系统/第三方库 | 95 | 730 | 3 | 3.49% | 0.47% | 双方存在且有结构重合 |
| 网络协议栈/第三方库 | 0 | 118 | 0 | 0.0% | 0.0% | 仅参考项目体现 |
| 第三方库 | 0 | 1374 | 0 | 0.0% | 0.0% | 仅参考项目体现 |
| 通用模块/其他 | 480 | 106 | 1 | 0.24% | 1.14% | 双方存在且有结构重合 |

### 2. 分模块证据清单

#### 进程管理
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `src/process.rs#exit_current` | `modules/axtask/src/run_queue.rs#exit_current` | 同板块同名 | ProcessAction::ExitToKernel, ProcessSlot::empty, RuntimeProcessState::Zombie, self.current_index, self.find_pid_index, self.find_runnable_sibling ...(+6) | EXITED_TASKS.lock, EXITED_TASKS.lock().clear, assert!, axhal::misc::terminate, axlog::warn!, crate::current ...(+4) |
| `src/script_exec.rs#push` | `modules/axtask/src/executor.rs#push` | 同板块同名 | Some | lock.as_mut, lock.as_mut().unwrap, lock.as_mut().unwrap().push_back, self.queue.lock |
| `src/vm.rs#executable` | `modules/axtask/src/stdio.rs#executable` | 同板块同名 | self.contains | 内部闭环逻辑 |
| `src/process.rs#pselect6_current` | `modules/axfs/src/mounts.rs#procfs` | 同模块上下文对齐/差异观察 | PROCESS_FD_LIMIT.div_ceil, PselectAction::Ready, PselectAction::Switched, RuntimeProcessState::WaitingPipeRead, Some, byte.count_ones ...(+17) | Arc::new, Ok, file_over.write_at, file_somaxconn.write_at, fs::ramfs::RamFileSystem::new, proc_root.clone ...(+3) |

#### 任务调度/异步运行时
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `src/user.rs#deref` | `modules/axtask/src/sync/mutex.rs#deref` | 同板块同名 | core::slice::from_raw_parts | 内部闭环逻辑 |
| `src/main.rs#run_on_block_device` | `api/linux_syscall_api/src/syscall_fs/ctype/mount.rs#get_stat_in_fs` | 同模块上下文对齐/差异观察 | DiscoveredScripts::<MAX_DISCOVERED_SCRIPTS>::new, Ext4Reader::open, Ok, arch::monotonic_nanos, arch::monotonic_nanos().saturating_add, core::ptr::addr_of_mut! ...(+31) | 0.into, Err, Kstat::default, Ok, axfs::api::metadata, axfs::api::metadata(path.path()).unwrap ...(+34) |
| `src/main.rs#addrspace_try_run_la` | `modules/axtask/src/api.rs#load_app` | 同模块上下文对齐/差异观察 | (*core::ptr::addr_of_mut!(LA_ADDRSPACE_SLOTS))[0].as_mut, (*core::ptr::addr_of_mut!(LA_ADDRSPACE_SLOTS))[0].as_mut().unwrap, (*core::ptr::addr_of_mut!(PROCESS_RUNTIME)).reset_root, (*core::ptr::addr_of_mut!(SYSCALL_CONTEXT))
        .reset_exec_memory_range, (LA_AS_RUN_SECONDS as u64).saturating_mul, AddrSpaceUserMemory::new ...(+30) | "busybox".to_string, Err, Ok, Some, VirtAddr::from, [0_u8].repeat ...(+35) |
| `src/main.rs#addrspace_do_clone_la` | `api/linux_syscall_api/src/syscall_fs/imp/io.rs#syscall_splice` | 同模块上下文对齐/差异观察 | (child_pid as i32).to_le_bytes, Some, addrspace_switch_to_la, arch::flush_user_tlb, core::ptr::addr_of!, core::ptr::addr_of_mut! ...(+14) | Err, Ok, SFuture::new, SeekFrom::Current, SeekFrom::Start, axtask::block_task ...(+28) |

#### 内存管理
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `src/syscall/mod.rs#alloc_fd` | `modules/axtask/src/task.rs#alloc_fd` | 同板块同名 | self.nofile_limit.min | Err, Ok, debug!, fd.is_none, fd_table.iter, fd_table.iter().enumerate ...(+3) |
| `src/vm.rs#alloc` | `modules/axalloc/src/lib.rs#alloc` | 同板块同名 | Some, frame_index, ref_set_one, self.free.alloc_zeroed, self.slice | Ok, balloc.add_memory, balloc.alloc, balloc.total_bytes, debug!, layout.size ...(+5) |
| `src/user.rs#materialized_page_count` | `modules/axmem/src/shared.rs#size` | 同板块同结构 | self.materialized.count | self.pages.size |
| `src/main.rs#handle_user_syscall` | `modules/axtask/src/run_queue.rs#switch_to` | 同模块上下文对齐/差异观察 | (*core::ptr::addr_of!(RUN_TICK_BUDGET)).remaining, (*counter).wrapping_add, (result as i32).to_le_bytes, (sig as i32).to_le_bytes, 0i32.to_le_bytes, <[u8; 8]>::try_from ...(+126) | CurrentTask::set_current, asm!, assert!, axhal::arch::write_page_table_root0, axhal::time::current_time_nanos, axlog::info! ...(+23) |

#### 中断/异常处理
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `src/arch/riscv64.rs#prepare_user_return` | `modules/axhal/src/platform/VisionFive2/irq.rs#register_handler` | 同模块上下文对齐/差异观察 | 内部闭环逻辑 | with_cause! |
| `src/arch/loongarch64.rs#prepare_user_return` | `modules/axhal/src/platform/riscv64_qemu_virt/irq.rs#register_handler` | 同模块上下文对齐/差异观察 | 内部闭环逻辑 | with_cause! |
| `src/arch/riscv64.rs#trap_frame_layout_matches_assembly_contract` | `modules/axtrap/src/arch/riscv/mod.rs#riscv_kernel_trap_handler` | 同模块上下文对齐/差异观察 | assert_eq! | handle_irq, panic!, scause.bits, scause.cause, scause::read |
| `src/arch/loongarch64.rs#trap_frame_layout_matches_assembly_contract` | `modules/axhal/src/irq.rs#register_handler_common` | 同模块上下文对齐/差异观察 | assert_eq! | IRQ_HANDLER_TABLE.register_handler, axlog::ax_println!, set_enable, warn! |

#### 系统调用
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `src/main.rs#run_busybox_script` | `api/linux_syscall_api/src/syscall_net/socket.rs#get` | 同模块上下文对齐/差异观察 | busybox::render_busybox_script, crate::println!, read_busybox_commands, runner::render_unsupported | copy_nonoverlapping, drop, keep_alive.to_ne_bytes, panic!, s.keep_alive, s.keep_alive().is_some ...(+10) |
| `src/syscall/mod.rs#sys_msgrcv` | `api/linux_syscall_api/src/syscall_net/socket.rs#set` | 同模块上下文对齐/差异观察 | (0..reg[idx].count).find, core::cmp::min, core::ptr::addr_of_mut!, memory.write_bytes, msg_index, mtype.to_le_bytes ...(+1) | <[u8; 4]>::try_from, <[u8; 4]>::try_from(&opt[0..4]).unwrap, Err, Ok, Some, axnet::Duration::from_secs ...(+14) |
| `src/syscall/mod.rs#sys_msgsnd` | `api/linux_syscall_api/src/syscall_mem/imp.rs#syscall_shmget` | 同模块上下文对齐/差异观察 | core::ptr::addr_of_mut!, i64::from_le_bytes, m.data[..msgsz].copy_from_slice, memory.read_bytes, msg_index, sem_now_seconds ...(+2) | Err, MemorySet::add_shared_mem, MemorySet::create_shared_mem, Ok, ShmFlags::from_bits, axmem::KEY_TO_SHMID.lock ...(+8) |
| `src/syscall/mod.rs#sys_setxattr` | `api/linux_syscall_api/src/syscall_net/socket.rs#get` | 同模块上下文对齐/差异观察 | core::ptr::addr_of_mut!, memory.read_bytes, read_cstr, t.iter, t.iter().position, value[..size].copy_from_slice ...(+2) | bytes.as_bytes, bytes.as_ptr, bytes.len, copy_nonoverlapping, panic!, raw_socket.get_congestion ...(+4) |

#### 文件系统
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `src/runner.rs#basic_subtest_elf_path` | `api/linux_syscall_api/src/syscall_fs/mod.rs#fs_syscall` | 同模块上下文对齐/差异观察 | Some | Ok, panic!, sys_linkat, syscall_chdir, syscall_close, syscall_dup ...(+31) |
| `src/syscall/mod.rs#ftruncate_statfs_sync_and_metadata_probe_boundaries_are_real` | `ulib/src/file.rs#fs_init` | 同模块上下文对齐/差异观察 | MockMemory::new, assert_eq!, ctx_with_file, dispatch, memory.write_cstr | "/bin/".to_string, "/lib/".to_string, "/usr/bin/".to_string, "/usr/sbin/".to_string, ("/lib/".to_string() + libc_so).as_str, ("/lib/".to_string() + libc_so2).as_str ...(+17) |
| `src/user.rs#prepare_initial_stack_args_inner` | `api/linux_syscall_api/src/syscall_fs/ctype/file.rs#get_stat` | 同模块上下文对齐/差异观察 | DEFAULT_ENV.len, Ok, arg.checked_add_nul_len, arg.len, args
            .len, args
            .len()
            .checked_add ...(+42) | INODE_NAME_MAP.lock, Ok, assert!, attr.blocks, attr.size, drop ...(+12) |
| `src/syscall/mod.rs#renameat2_moves_runtime_files_and_rmdir_requires_empty_directory` | `modules/axfs/src/fops.rs#_open_at` | 同模块上下文对齐/差异观察 | MockMemory::new, assert!, assert_eq!, ctx_with_file, dispatch, memory.write_cstr | Err, Ok, WithCap::new, attr.is_dir, attr.perm, ax_err! ...(+10) |

#### 设备驱动
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `src/virtio/mmio.rs#probe` | `modules/axdriver/src/lib.rs#probe` | 同板块同名 | Err, FailureBreaker::new, Ok, device.add_status, device.negotiate_features, device.read_reg ...(+3) | for_each_drivers!, self.probe_bus_devices |
| `src/main.rs#collect_basic_lines` | `modules/axdriver/src/lib.rs#init_drivers` | 同模块上下文对齐/差异观察 | Err, Ok, PathBuf::from_slice, ScriptLines::new, ScriptRuntimeState::new_from_cwd, collect_basic_lines ...(+17) | AllDevices::default, all_devs.block.iter, all_devs.block.iter().enumerate, all_devs.display.iter, all_devs.display.iter().enumerate, all_devs.net.iter ...(+5) |
| `src/main.rs#emulate_runtest` | `modules/axdriver/src/virtio.rs#probe_pci` | 同模块上下文对齐/差异观察 | Err, ExpandedArgs::from_slice, Ok, args.len, command.args.as_slice, name.is_empty ...(+8) | D::try_new, Some, driver_virtio::probe_pci_device::<VirtIoHalImpl>, warn! |
| `src/virtio/pci.rs#submit_read` | `modules/axdriver/src/virtio.rs#probe_mmio` | 同模块上下文对齐/差异观察 | Err, Ok, RequestHeader::read, addr_of!, addr_of_mut!, avail_idx.wrapping_add ...(+10) | D::try_new, Some, base_vaddr.as_mut_ptr, driver_virtio::probe_mmio_device, mmio_base.into, phys_to_virt ...(+1) |

#### 网络协议栈
仅目标项目中观察到该模块代表函数，参考项目没有同模块可对齐证据：
- 目标项目：`src/netperf.rs#render_netperf_script`，调用摘要：crate::runner::render_basic_end, crate::runner::render_basic_start, sink.write_str, write_u32
- 目标项目：`src/netperf.rs#write_u32`，调用摘要：core::str::from_utf8, core::str::from_utf8(&ch).unwrap_or, sink.write_str

#### 文件系统/第三方库
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `src/fs/ext4.rs#read_inode` | `crates/another_ext4/src/ext4/rw.rs#read_inode` | 同板块同名 | Err, Ok, block.copy_from_slice, min, read_u16, read_u32 ...(+6) | InodeRef::new, block.read_offset_as, self.inode_disk_pos, self.read_block |
| `src/fs/ext4.rs#read_at` | `modules/axfs/src/fs/another_ext4.rs#read_at` | 同板块同名 | Ok, device.read_sector, min, offset
            .checked_add, offset
            .checked_add(done as u64)
            .ok_or, out.len ...(+1) | self.fs
            .read, self.fs
            .read(self.id, offset as usize, buf)
            .map_err |
| `src/fs/ext4.rs#read_block` | `crates/another_ext4/src/ext4_defs/cache.rs#read_block` | 同板块同名 | Err, out.len, self.block_byte_offset, self.read_at | block.clone, cache[set_id].access, debug!, self.block_dev.read_block, self.block_dev.write_block, self.cache.lock ...(+1) |
| `src/fs/ext4.rs#blocks_per_group` | `crates/another_ext4/src/ext4_defs/super_block.rs#blocks_per_group` | 同板块同名 | 内部闭环逻辑 | 内部闭环逻辑 |

#### 网络协议栈/第三方库
仅参考项目中观察到该模块代表函数，目标项目没有同模块可对齐证据：
- 参考项目：`modules/axnet/src/smoltcp_impl/tcp.rs#connect`，调用摘要：Ok, Ok::<(IpEndpoint, IpEndpoint), AxError>, SOCKET_SET
                .with_socket_mut::<tcp::Socket, _, _>, SOCKET_SET.add, SocketSetWrapper::new_tcp_socket, Some ...(+23)
- 参考项目：`modules/axnet/src/smoltcp_impl/tcp.rs#recv_timeout`，调用摘要：Err, Ok, SOCKET_SET.with_socket_mut::<tcp::Socket, _, _>, ax_err!, ax_err_type!, current_ticks ...(+11)
- 参考项目：`modules/axnet/src/smoltcp_impl/mod.rs#init`，调用摘要：Config::new, ETH0.init_by, EthernetAddress, GATEWAY.parse, GATEWAY.parse().expect, IP.parse ...(+22)
- 参考项目：`modules/axnet/src/smoltcp_impl/tcp.rs#shutdown`，调用摘要：LISTEN_TABLE.unlisten, Ok, SOCKET_SET.poll_interfaces, SOCKET_SET.with_socket_mut::<tcp::Socket, _, _>, debug!, self.handle.get ...(+9)

#### 第三方库
仅参考项目中观察到该模块代表函数，目标项目没有同模块可对齐证据：
- 参考项目：`crates/bcm2835-sdhci/src/Bcm2835SDhci.rs#sd_card_init`，调用摘要：ACMD!, byte_swap, debug!, info!, self.cmd_timeout, self.dumpregs ...(+26)
- 参考项目：`crates/elf_parser/src/arch/x86_64.rs#get_relocate_pairs`，调用摘要：Ok, Vec::new, VirtAddr::from, assert_eq!, dyn_sym.get_name, dyn_sym.get_name(elf).unwrap ...(+23)
- 参考项目：`crates/elf_parser/src/arch/aarch64.rs#get_relocate_pairs`，调用摘要：Ok, Vec::new, VirtAddr::from, assert_eq!, dyn_sym.get_name, dyn_sym.get_name(elf).unwrap ...(+22)
- 参考项目：`crates/elf_parser/src/arch/riscv.rs#get_relocate_pairs`，调用摘要：Ok, Vec::new, VirtAddr::from, assert_eq!, dyn_sym.get_name, dyn_sym.get_name(elf).unwrap ...(+23)

#### 通用模块/其他
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `src/vm.rs#write_bytes` | `modules/axhal/src/lib.rs#write_bytes` | 同板块同名 | bytes.is_empty, self.space
            .get_mut, self.space
            .get_mut()
            .copy_to_user, self.space
            .get_mut()
            .copy_to_user(addr, bytes, frames)
            .is_ok | putchar |
| `src/signal.rs#set_action` | `modules/axsignal/src/lib.rs#set_action` | 同板块同名 | catchable, self.disposition_ignores, sigmask | Some |
| `src/main.rs#clear` | `modules/axsignal/src/lib.rs#clear` | 同板块同名 | 内部闭环逻辑 | self.handlers.iter_mut |
| `src/main.rs#write_str` | `modules/axlog/src/lib.rs#write_str` | 同板块同名 | capture_user_output, crate::print!, value.as_bytes | Ok, cfg_if::cfg_if! |


#### 1. 宏观判定
- **定性结论**: 独立开发 (相似度: 0.69%，共同 AST 指纹: 28；唯一 AST 指纹: 目标项目 1626，参考项目 2483；覆盖率: 目标项目 1.72%，参考项目 1.13%)

#### 2. 全模块覆盖矩阵解读
- **进程管理**: 双方均存在该模块。共同 AST 指纹为 0，目标覆盖率 0.0%，参考覆盖率 0.0%。该模块证据强度为**未形成**。控方提出的所有指控均基于跨板块配对或功能语义相似，而非同板块内的结构同源。
- **任务调度/异步运行时**: 双方均存在该模块。共同 AST 指纹为 0，目标覆盖率 0.0%，参考覆盖率 0.0%。该模块证据强度为**未形成**。目标项目采用基于 `ProcessRuntime` 的协作式调度，参考项目采用基于 `axtask` 的异步抢占式调度，运行时模型根本不同。
- **内存管理**: 双方均存在该模块。共同 AST 指纹为 1，目标覆盖率 0.36%，参考覆盖率 0.6%。该模块证据强度为**弱**。唯一的共同指纹来自 `alloc_fd` 函数，这是文件描述符分配逻辑，属于 POSIX 标准语义下的必然实现，且双方外部调用对象完全不同。
- **中断/异常处理**: 双方均存在该模块。共同 AST 指纹为 0，目标覆盖率 0.0%，参考覆盖率 0.0%。该模块证据强度为**未形成**。目标项目包含针对 LoongArch 的专有处理逻辑，与参考项目的多平台抽象路径无结构重合。
- **系统调用**: 双方均存在该模块。共同 AST 指纹为 0，目标覆盖率 0.0%，参考覆盖率 0.0%。该模块证据强度为**未形成**。目标项目的系统调用分发与参考项目的 `linux_syscall_api` 在实现范式上无共同结构。
- **文件系统**: 双方均存在该模块。共同 AST 指纹为 0，目标覆盖率 0.0%，参考覆盖率 0.0%。该模块证据强度为**未形成**。目标项目使用自定义的 `RuntimeNode` 构建 VFS，参考项目使用 `RamFileSystem`，两者抽象层次与实现路径完全不同。
- **设备驱动**: 双方均存在该模块。共同 AST 指纹为 0，目标覆盖率 0.0%，参考覆盖率 0.0%。该模块证据强度为**未形成**。目标项目包含独立的 virtio 驱动实现，参考项目使用 `axdriver` 框架，无结构复用痕迹。
- **网络协议栈**: 仅目标项目体现该模块。共同 AST 指纹为 0，目标覆盖率 0.0%，参考覆盖率 0.0%。该模块证据强度为**未形成**。属于单方特有模块，不构成同源证据。
- **第三方库/公共依赖**: 双方均存在该模块。共同 AST 指纹为 3，目标覆盖率 3.49%，参考覆盖率 0.47%。该模块证据强度为**弱**。共同指纹集中在 `ext4.rs` 文件系统库中，如 `read_inode`、`read_at` 等函数。这些函数操作的是标准的 ext4 磁盘数据结构，其读取逻辑由 ext4 规范严格定义，属于对公共标准的独立实现，不构成原创性表达的复用。

#### 3. 底层 AST 关键证据指纹表
| 所属板块 | 目标项目路径 | 参考项目路径 | 证据类型 | 技术说明 |
| :--- | :--- | :--- | :--- | :--- |
| 内存管理 | `src/syscall/mod.rs#alloc_fd` | `modules/axtask/src/task.rs#alloc_fd` | 同板块同名 | 双方均实现文件描述符分配，但目标使用 `self.nofile_limit.min`，参考使用 `fd_table.iter().enumerate`，外部调用对象与内部逻辑完全不同。 |
| 文件系统/第三方库 | `src/fs/ext4.rs#read_inode` | `crates/another_ext4/src/ext4/rw.rs#read_inode` | 同板块同名 | 双方均实现对 ext4 inode 的读取，但目标直接操作 `block` 字节切片，参考通过 `InodeRef` 和 `self.read_block` 进行封装，调用链不同。 |
| 文件系统/第三方库 | `src/fs/ext4.rs#read_at` | `modules/axfs/src/fs/another_ext4.rs#read_at` | 同板块同名 | 双方均实现文件读取，但目标直接调用 `device.read_sector`，参考通过 `self.fs.read` 进行间接调用，抽象层次不同。 |
| 文件系统/第三方库 | `src/fs/ext4.rs#read_block` | `crates/another_ext4/src/ext4_defs/cache.rs#read_block` | 同板块同名 | 双方均实现块读取，但目标直接计算偏移并调用 `read_at`，参考通过 `self.block_dev.read_block` 和缓存机制，实现路径不同。 |
| 通用模块/其他 | `src/vm.rs#write_bytes` | `modules/axhal/src/lib.rs#write_bytes` | 同板块同名 | 目标实现向用户空间写数据，调用 `copy_to_user`；参考实现向控制台输出字符，调用 `putchar`。功能与上下文完全不同。 |
| 通用模块/其他 | `src/signal.rs#set_action` | `modules/axsignal/src/lib.rs#set_action` | 同板块同名 | 双方均实现信号处理函数注册，但目标操作 `disposition_ignores` 和 `sigmask`，参考仅返回 `Some`，内部状态管理模型不同。 |
| 进程管理 | `src/process.rs#exit_current` | `modules/axtask/src/run_queue.rs#exit_current` | 同板块同名 | 双方均实现进程退出，但目标操作 `ProcessSlot` 和 `pipe_fds`，参考操作 `EXITED_TASKS` 和 `TaskInner`，外部调用对象无交集。 |
| 进程管理 | `src/script_exec.rs#push` | `modules/axtask/src/executor.rs#push` | 同板块同名 | 目标为脚本参数入栈，调用 `Some`；参考为任务队列操作，调用 `lock.as_mut().unwrap().push_back`。AST 结构完全不同，属于巧合同名。 |
| 进程管理 | `src/vm.rs#executable` | `modules/axtask/src/stdio.rs#executable` | 同板块同名 | 目标判断地址区域包含性，调用 `self.contains`；参考为 stdio 内部闭环逻辑。无复用痕迹，予以排除。 |

#### 4. 核心代码调用流对比剖析
* **【进程管理/进程退出】**
* **底层技术析理**: 目标项目的 `exit_current` 函数操作的是自定义的 `ProcessSlot` 和 `RuntimeProcessState::Zombie` 状态，退出时需要调用 `release_all_pipe_fds` 清理管道资源，这体现了其进程模型与管道引用计数的紧耦合。参考项目的 `exit_current` 函数则操作全局链表 `EXITED_TASKS` 和 `TaskInner` 锁，并最终调用 `axhal::misc::terminate` 终止平台。两者的内存抽象（`ProcessSlot` vs `TaskInner`）、同步原语（隐式借用检查 vs 显式锁）和对象管理模型（管道引用计数 vs 全局退出链表）完全不同，无结构复用痕迹。
* **代码溯源链接**:
  - 目标项目: `src/process.rs#exit_current`
  - 参考项目: `modules/axtask/src/run_queue.rs#exit_current`

* **【内存管理/文件描述符分配】**
* **底层技术析理**: 目标项目的 `alloc_fd` 函数通过 `self.nofile_limit.min` 限制最大文件描述符，并在 `ProcessRuntime` 的上下文中操作。参考项目的 `alloc_fd` 函数则通过遍历 `fd_table` 寻找空闲位，并调用 `debug!` 宏输出日志。两者的调用流反映了不同的资源管理策略：目标项目依赖运行时内部的限制字段，参考项目依赖独立的文件描述符表和日志系统。这是对 POSIX 标准语义的两种独立实现。
* **代码溯源链接**:
  - 目标项目: `src/syscall/mod.rs#alloc_fd`
  - 参考项目: `modules/axtask/src/task.rs#alloc_fd`

* **【文件系统/ext4 公共依赖】**
* **底层技术析理**: 目标项目的 `read_inode` 函数直接对 `block` 字节切片进行偏移读取和解析。参考项目的 `read_inode` 函数则通过 `InodeRef::new` 和 `self.read_block` 进行封装。尽管两者都服务于读取 ext4 inode 这一公共标准，但目标项目采用过程式字节操作，参考项目采用面向对象的封装，调用流和抽象层次存在显著差异，属于对同一底层数据结构的独立解析实现。
* **代码溯源链接**:
  - 目标项目: `src/fs/ext4.rs#read_inode`
  - 参考项目: `crates/another_ext4/src/ext4/rw.rs#read_inode`

#### 5. 单方模块与降权项
- **单方模块**:
  - **网络协议栈**: 仅目标项目拥有 `src/netperf.rs` 等网络性能测试模块，参考项目无对应模块。
  - **第三方库**: 参考项目拥有大量目标项目未体现的第三方库模块，如 `bcm2835-sdhci`、`elf_parser` 等。
  这些模块仅在一方出现，无法形成同源比对，不构成任何复用证据。
- **降权项**:
  - 所有跨板块的同名函数（如目标 `src/script_exec.rs#push` 与参考 `modules/axtask/src/executor.rs#push`）均降级为弱信号。`push`、`executable` 等属于 Rust 或计算机科学领域的通用动词/形容词，其同名不具有特异性，且 AST 结构完全不同，不构成同板块主证据。
  - 第三方库（ext4）中的同名函数，因其操作的数据结构由外部公共标准（ext4 文件系统规范）严格定义，其实现逻辑的相似性源于规范而非代码复用，证据强度降级为弱。

#### 6. 保守结论
本次审计的客观 AST 相似度仅为 0.69%，全模块覆盖率极低，尤其在核心的进程管理、任务调度、系统调用等板块，共同结构指纹均为零。控方提出的复用指控均建立在跨板块配对或对通用操作系统语义（如进程退出、wait4）的曲解之上，缺乏同板块内的底层 AST 结构支撑。目标项目在运行时模型、内存抽象、文件系统实现和架构支持上与参考项目存在系统性差异，符合独立开发的特征。第三方库中的微弱重合源于对公共标准（ext4）的独立实现。综合判断，目标项目不存在对参考项目的实质性代码复用，整体风险极低。

------------------------------

