# 🛡️ OS 内核赛自动化代码级架构审计报告

**审计时间**: `2026-07-02 14:58:37`
**参赛作品名称**: `2026-T2026105749910169-XX`
**年份**: `2026`
**学校**: `XX`
**队伍名称**: `T2026105749910169`
**比赛名字**: `OS-Competition`
**比赛子赛道**: `Kernel-Implementation`
**仓库地址**: `https://gitlab.eduxiji.net/educg-group-43501-3132633/26QS`
**推测开发语言**: `Rust / C (由AST探针自动识别)`
**特征函数量**: `1818`

## ⚔️ 底层代码同源性对比 (AST Homology)
> 本部分由专门的代码审查智能体，对该项目与符合年份条件的历届最高相似度项目进行控制流与底层调用对比。新版报告会先给出确定性的全模块覆盖矩阵，再给出 Agent 的分模块解释，避免只围绕少量抽样函数展开。

### 🏆 Top 5 底层对标: vs `2025-sudo_pacman_Syu-同济大学`（同济大学 / sudo_pacman_Syu）

#### 对方作品信息

**对方年份**: `2025`
**对方学校**: `同济大学`
**对方队伍名称**: `sudo_pacman_Syu`
**对方比赛名字**: `OS-Competition`
**对方比赛子赛道**: `Kernel-Implementation`
**对方仓库地址**: `https://gitlab.eduxiji.net/educg-group-36002-2710490/T202510247995532-2817`

> 双向覆盖率：共同 AST 指纹: 20；唯一 AST 指纹: 目标项目 1626，参考项目 1840；覆盖率: 目标项目 1.23%，参考项目 1.09%

## 全模块对比总览（确定性证据）

> 总体覆盖率：共同 AST 指纹: 20；唯一 AST 指纹: 目标项目 1626，参考项目 1840；覆盖率: 目标项目 1.23%，参考项目 1.09%

### 1. 模块覆盖矩阵
| 模块 | 目标函数数 | 参考函数数 | 共同唯一 AST 指纹 | Target 覆盖 | Base 覆盖 | 状态 |
| :--- | ---: | ---: | ---: | ---: | ---: | :--- |
| 进程管理 | 255 | 154 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |
| 任务调度/异步运行时 | 190 | 164 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |
| 内存管理 | 291 | 261 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |
| 中断/异常处理 | 35 | 28 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |
| 系统调用 | 143 | 58 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |
| 文件系统 | 219 | 318 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |
| 设备驱动 | 108 | 162 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |
| 网络协议栈 | 2 | 51 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |
| 文件系统/第三方库 | 95 | 48 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |
| 第三方库 | 0 | 740 | 0 | 0.0% | 0.0% | 仅参考项目体现 |
| 通用模块/其他 | 480 | 188 | 2 | 0.48% | 1.28% | 双方存在且有结构重合 |

### 2. 分模块证据清单

#### 进程管理
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `src/exec_cache.rs#lookup` | `src/fs/procfs.rs#lookup` | 同板块同名 | Some, self.path.as_slice | (name == &***dentry.name()).then, Ok, block_on, dentry.name, lock.prove, node.unwrap ...(+4) |
| `src/process.rs#exit_current` | `src/kernel/task.rs#execute` | 同模块上下文对齐/差异观察 | ProcessAction::ExitToKernel, ProcessSlot::empty, RuntimeProcessState::Zombie, self.current_index, self.find_pid_index, self.find_runnable_sibling ...(+6) | Arc::new, AtomicBool::new, Context::from_waker, Some, Task::current, Task::current().clone ...(+14) |
| `src/process.rs#pselect6_current` | `src/kernel/task/process.rs#build` | 同模块上下文对齐/差异观察 | PROCESS_FD_LIMIT.div_ceil, PselectAction::Ready, PselectAction::Switched, RuntimeProcessState::WaitingPipeRead, Some, byte.count_ones ...(+17) | Arc::new, BTreeMap::new, Locked::new, MMList::new, ProcessGroupBuilder::new, ProcessGroupBuilder::new()
                .leader ...(+22) |
| `src/syscall/mod.rs#seeded_procfs_supports_busybox_proc_consumers` | `src/kernel/task/process.rs#wait` | 同模块上下文对齐/差异观察 | MockMemory::new, SyscallContext::new, assert!, assert_eq!, b"meminfo".len, b"mounts".len ...(+6) | Err, Ok, ProcessList::get, ProcessList::get().write, Some, assert! ...(+10) |

#### 任务调度/异步运行时
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `src/main.rs#run_on_block_device` | `src/kernel/task/signal.rs#handle` | 同模块上下文对齐/差异观察 | DiscoveredScripts::<MAX_DISCOVERED_SCRIPTS>::new, Ext4Reader::open, Ok, arch::monotonic_nanos, arch::monotonic_nanos().saturating_add, core::ptr::addr_of_mut! ...(+31) | ProcessList::get, ProcessList::get().read, ProcessList::get().read().await.prove, Runtime::block_till_woken, SIGNAL_IGNORE!, SIGNAL_STOP! ...(+19) |
| `src/main.rs#addrspace_try_run_la` | `src/kernel/syscall/file_rw.rs#pselect6` | 同模块上下文对齐/差异观察 | (*core::ptr::addr_of_mut!(LA_ADDRSPACE_SLOTS))[0].as_mut, (*core::ptr::addr_of_mut!(LA_ADDRSPACE_SLOTS))[0].as_mut().unwrap, (*core::ptr::addr_of_mut!(PROCESS_RUNTIME)).reset_root, (*core::ptr::addr_of_mut!(SYSCALL_CONTEXT))
        .reset_exec_memory_range, (LA_AS_RUN_SECONDS as u64).saturating_mul, AddrSpaceUserMemory::new ...(+30) | Duration::from_millis, FD::from, Ok, PollEvent::empty, Some, UserPointer::new ...(+33) |
| `src/main.rs#addrspace_do_clone_la` | `src/kernel/task/signal/signal_action.rs#handle` | 同模块上下文对齐/差异观察 | (child_pid as i32).to_le_bytes, Some, addrspace_switch_to_la, arch::flush_user_tlb, core::ptr::addr_of!, core::ptr::addr_of_mut! ...(+14) | (VDSO_RT_SIGRETURN_ADDR as *const _ as *const usize).read_volatile, (VDSO_SIGRETURN_ADDR as *const _ as *const usize).read_volatile, (current_sp - 128 - SAVED_DATA_SIZE).floor_to, (current_sp - SAVED_DATA_SIZE).floor_to, Long::new_val, Long::new_val(signal.into_raw() as _).get ...(+23) |
| `src/main.rs#run_script_command` | `src/kernel/task/loader/mod.rs#parse` | 同模块上下文对齐/差异观察 | Err, Ok, ScriptRuntimeState::new_from_cwd, ShellRuntimeState::from_expanded_args, command.args.as_slice, command.program.as_bytes ...(+17) | ByteBuffer::new, CString::new, CString::new(&line[..blank_pos]).unwrap, CString::new(&line[blank_pos + 1..]).unwrap, CString::new(line).unwrap, Dentry::open ...(+19) |

#### 内存管理
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `src/vm.rs#load_segment` | `src/kernel/task/loader/elf.rs#load_segment` | 同板块同名 | Err, MapArea::new, Ok, alloc.alloc, bias.wrapping_add, core::cmp::max ...(+15) | (program_header.offset()).align_down, FileMapping::new, Mapping::File, Ok, load_vaddr_end.ceil, mm_list
                .mmap_fixed ...(+11) |
| `src/vm.rs#unmap` | `src/kernel/mem/mm_list.rs#unmap` | 同板块同名 | Some, core::ptr::read, core::ptr::write, pte_is_leaf, pte_to_table, vpn | (start + len).ceil, Err, Ok, Page::from_raw, Some, VRange::new ...(+18) |
| `src/vm.rs#unmap` | `src/kernel/mem/mm_list.rs#unmap` | 同板块同名 | Some, core::ptr::read, core::ptr::write, la_pte_is_leaf, la_pte_pa, la_vpn | Ok, drop, self.flush_user_tlbs, self.inner.borrow, self.inner.borrow().unmap |
| `src/user.rs#protect` | `src/kernel/mem/mm_list.rs#protect` | 同板块同名 | Err, Ok, range.end.div_ceil, self.checked_translated_range, u8::try_from, u8::try_from(prot).map_err | (start + len).ceil, Err, Ok, VRange::new, area.range, area.range().mask_with_checked ...(+19) |

#### 中断/异常处理
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `src/arch/riscv64.rs#prepare_user_return` | `src/kernel/terminal.rs#commit_char` | 同模块上下文对齐/差异观察 | 内部闭环逻辑 | inner.termio.echok, inner.termio.echoke, inner.termio.icanon, inner.termio.icrnl, inner.termio.iexten, inner.termio.igncr ...(+16) |
| `src/arch/loongarch64.rs#prepare_user_return` | `src/kernel/terminal.rs#read` | 同模块上下文对齐/差异观察 | 内部闭环逻辑 | Err, Thread::current, Thread::current().signal_list.has_pending_signal, buffer.available, buffer.available().min, buffer.fill ...(+17) |
| `src/arch/riscv64.rs#trap_frame_layout_matches_assembly_contract` | `src/kernel/timer.rs#timer_interrupt` | 同模块上下文对齐/差异观察 | assert_eq! | CPU::local, CPU::local().cpuid, SLEEPERS_LIST.lock_irq, TICKS.fetch_add, WAKEUP_TICK.load, WAKEUP_TICK.store ...(+6) |
| `src/arch/loongarch64.rs#trap_frame_layout_matches_assembly_contract` | `src/kernel/terminal.rs#new_standard` | 同模块上下文对齐/差异观察 | assert_eq! | CTRL!, core::array::from_fn |

#### 系统调用
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `src/main.rs#run_busybox_script` | `src/kernel/mem/mm_list.rs#set_break` | 同模块上下文对齐/差异观察 | busybox::render_busybox_script, crate::println!, read_busybox_commands, runner::render_unsupported | Some, VRange::new, assert!, drop, inner.locked.lock, locked.areas.check_overlapping_range ...(+5) |
| `src/syscall/mod.rs#sys_msgrcv` | `src/kernel/syscall/sysinfo.rs#newuname` | 同模块上下文对齐/差异观察 | (0..reg[idx].count).find, core::cmp::min, core::ptr::addr_of_mut!, memory.write_bytes, msg_index, mtype.to_le_bytes ...(+1) | UserPointerMut::new, buffer.write, copy_cstr_to_array |
| `src/syscall/mod.rs#sys_msgsnd` | `src/kernel/syscall/net.rs#get_sockopt` | 同模块上下文对齐/差异观察 | core::ptr::addr_of_mut!, i64::from_le_bytes, m.data[..msgsz].copy_from_slice, memory.read_bytes, msg_index, sem_now_seconds ...(+2) | Ok, UserPointerMut::new, UserPointerMut::new(optlen)?.write, UserPointerMut::new(optval.cast())?.write, optval.cast, size_of::<u32> |
| `src/syscall/mod.rs#sys_setxattr` | `src/kernel/syscall/net.rs#read_socket_addr` | 同模块上下文对齐/差异观察 | core::ptr::addr_of_mut!, memory.read_bytes, read_cstr, t.iter, t.iter().position, value[..size].copy_from_slice ...(+2) | Err, IpAddr::V4, Ipv4Addr::from_bits, Ok, SockDomain::try_from, SocketAddr::new ...(+6) |

#### 文件系统
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `src/syscall/mod.rs#offset` | `src/kernel/mem/mm_list/mapping.rs#offset` | 同板块同名 | Some | Self::new, self.file.clone |
| `src/runner.rs#basic_subtest_elf_path` | `src/fs/tmpfs.rs#rename` | 同模块上下文对齐/差异观察 | Some | (new_parent_inode.as_ref() as &dyn Any)
                .downcast_ref::<DirectoryInode>, (new_parent_inode.as_ref() as &dyn Any)
                .downcast_ref::<DirectoryInode>()
                .expect, Arc::as_ptr, Err, Instant::now, Ok ...(+53) |
| `src/syscall/mod.rs#ftruncate_statfs_sync_and_metadata_probe_boundaries_are_real` | `src/kernel/syscall/file_rw.rs#do_copy_or_splice` | 同模块上下文对齐/差异观察 | MockMemory::new, assert_eq!, ctx_with_file, dispatch, memory.write_cstr | (&buffer[..read_bytes]).into_stream, (offset + total_copied as isize).to_le_bytes, ByteBuffer::new, Ok, Some, UserBuffer::new ...(+19) |
| `src/user.rs#prepare_initial_stack_args_inner` | `src/kernel/vfs/inode.rs#statx` | 同模块上下文对齐/差异观察 | DEFAULT_ENV.len, Ok, arg.checked_add_nul_len, arg.len, args
            .len, args
            .len()
            .checked_add ...(+42) | Ok, atime.into, ctime.into, mode.format_bits, mode.is_blk, mode.is_chr ...(+15) |

#### 设备驱动
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `src/virtio/mmio.rs#probe` | `src/driver/goldfish_rtc.rs#probe` | 同板块同名 | Err, FailureBreaker::new, Ok, device.add_status, device.negotiate_features, device.read_reg ...(+3) | ArchPhysAccess::as_ptr, register_rtc |
| `src/main.rs#collect_basic_lines` | `src/driver/virtio/riscv64.rs#init` | 同模块上下文对齐/差异观察 | Err, Ok, PathBuf::from_slice, ScriptLines::new, ScriptRuntimeState::new_from_cwd, collect_basic_lines ...(+17) | Arc::new, ArchPhysAccess::as_ptr, BlockDevice::register_disk, BlockDevice::register_disk(
                        make_device(8, 16 * disk_id),
                        2147483647,
                        Arc::new(Spin::new(block_device)),
                    )
                    .expect, MmioTransport::new, Spin::new ...(+14) |
| `src/main.rs#emulate_runtest` | `src/driver/virtio/loongarch64.rs#handle_device` | 同模块上下文对齐/差异观察 | Err, ExpandedArgs::from_slice, Ok, args.len, command.args.as_slice, name.is_empty ...(+8) | Arc::new, BlockDevice::register_disk, Err, Ok, PciRoot::new, PciTransport::new::<HAL, _> ...(+13) |
| `src/virtio/pci.rs#submit_read` | `src/driver/serial.rs#init` | 同模块上下文对齐/差异观察 | Err, Ok, RequestHeader::read, addr_of!, addr_of_mut!, avail_idx.wrapping_add ...(+10) | Ok, PAddr::from, Serial::new, SerialIO::new, port.register_as_char_device, serial.register_as_char_device |

#### 网络协议栈
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `src/netperf.rs#render_netperf_script` | `src/net/socket/tcp.rs#poll_impl` | 同模块上下文对齐/差异观察 | crate::runner::render_basic_end, crate::runner::render_basic_start, sink.write_str, write_u32 | Ok, PollEvent::empty, Some, block_on, events.contains, iface.lock ...(+12) |
| `src/netperf.rs#write_u32` | `src/net/socket/udp.rs#bind` | 同模块上下文对齐/差异观察 | core::str::from_utf8, core::str::from_utf8(&ch).unwrap_or, sink.write_str | BoundSocket::new_bind_all, BoundSocket::new_bind_single, Err, Ok, Self::bind_impl, Some ...(+23) |

#### 文件系统/第三方库
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `src/fs/ext4.rs#read_block` | `src/fs/ext4.rs#read_block` | 同板块同名 | Err, out.len, self.block_byte_offset, self.read_at | ByteBuffer::new, buffer.as_mut_slice, self
            .device
            .read_some, self
            .device
            .read_some((block_id as usize) * 4096, &mut byte_buffer)
            .expect |
| `src/fs/ext4.rs#build_fixture` | `src/fs/ext4.rs#lookup` | 同模块上下文对齐/差异观察 | MockBlockDevice::new, disk.add_dir_entry, disk.write, disk.write_u16, disk.write_u32, write_extent_inode | AtomicMode::new, AtomicNlink::new, AtomicU32::new, AtomicU64::new, Err, InodeMode::BLOCKDEV.bits ...(+30) |
| `src/fs/ext4.rs#parse_directory_block` | `src/fs/ext4.rs#rename` | 同模块上下文对齐/差异观察 | FileName::from_slice, FileType::from_dir_entry, Ok, block
                .get, block
                .get(offset + 8..offset + 8 + name_len)
                .ok_or, block.len ...(+4) | Arc::as_ptr, Err, Instant::now, Ok, block_on, core::str::from_utf8 ...(+27) |
| `src/fs/ext4.rs#read_regular_file_bounded` | `src/fs/ext4.rs#create` | 同模块上下文对齐/差异观察 | Err, FixedList::<Extent, MAX_EXTENTS>::new, Ok, blocks_for_len, clock, extents.as_slice ...(+12) | Arc::downgrade, Arc::new, AtomicMode::new, AtomicNlink::new, AtomicU32::new, AtomicU64::new ...(+25) |

#### 第三方库
仅参考项目中观察到该模块代表函数，目标项目没有同模块可对齐证据：
- 参考项目：`crates/eonix_hal/src/arch/riscv64/trap/default.rs#_default_trap_entry`，调用摘要：naked_asm!
- 参考项目：`crates/eonix_hal/src/arch/loongarch64/trap/mod.rs#_raw_trap_entry`，调用摘要：naked_asm!
- 参考项目：`crates/eonix_percpu/eonix_percpu_macros/src/lib.rs#define_percpu_impl`，调用摘要：["u8", "u16", "u32", "u64", "usize"].contains, attrs.is_empty, format_ident!, get_percpu_pointer, panic!, parse2::<ItemStatic> ...(+6)
- 参考项目：`crates/eonix_hal/src/arch/riscv64/trap/captured.rs#_captured_trap_entry`，调用摘要：naked_asm!

#### 通用模块/其他
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `src/main.rs#clear` | `src/kernel/pcie/header.rs#clear` | 同板块同名 | 内部闭环逻辑 | current.and, memory_barrier, self.read, self.write, value.not |
| `src/main.rs#write_str` | `src/io.rs#write_str` | 同板块同名 | capture_user_output, crate::print!, value.as_bytes | Err, Ok, s.as_bytes, self.0.fill |
| `src/busybox.rs#set` | `src/kernel/pcie/header.rs#set` | 同板块同名 | self.entries.iter_mut, self.entries.iter_mut().find | base.map_or, memory_barrier, panic!, reg2.load, reg2.store, self.reg1
                    .store ...(+3) |
| `src/main.rs#get` | `src/kernel/pcie/header.rs#get` | 同板块同名 | Some | NonZero::new, NonZero::new(size).map, bar.unwrap_or, memory_barrier, reg2.load, reg2.store ...(+5) |


#### 1. 宏观判定
- **定性结论**: **底层架构显著差异 / 独立开发**
- **核心依据**: 客观 AST 相似度仅为 0.58%，全项目共同 AST 指纹仅 20 个，且均属于 Rust 语言惯用法或通用工具函数。在进程管理、内存管理、系统调用等核心板块，共同唯一 AST 指纹均为 0，覆盖率均为 0.0%。双方采用了完全不同的外部对象图谱与架构范式，未发现任何可保护的表达层面复用。

#### 2. 全模块覆盖矩阵解读
- **进程管理**: 双方均存在该模块。共同 AST 指纹为 0，覆盖率均为 0.0%。证据强度：**未形成**。控方指控均基于 POSIX 公共语义与通用函数命名，缺乏 AST 结构层面的重合。
- **任务调度/异步运行时**: 双方均存在该模块。共同 AST 指纹为 0，覆盖率均为 0.0%。证据强度：**未形成**。目标项目采用状态机枚举与静态槽位调度，参考项目采用条件变量与全局任务链表，架构异构。
- **内存管理**: 双方均存在该模块。共同 AST 指纹为 0，覆盖率均为 0.0%。证据强度：**未形成**。同名函数 `load_segment`、`unmap`、`protect` 的外部调用图谱完全不同，目标项目使用自定义 `MapArea`，参考项目使用 `FileMapping`、`VRange` 等。
- **中断/异常处理**: 双方均存在该模块。共同 AST 指纹为 0，覆盖率均为 0.0%。证据强度：**未形成**。目标项目证据为架构相关的 `prepare_user_return` 与 trap frame 布局测试，参考项目为终端与定时器中断处理，无对应关系。
- **系统调用**: 双方均存在该模块。共同 AST 指纹为 0，覆盖率均为 0.0%。证据强度：**未形成**。目标项目使用 `MockMemory` 与 `SyscallContext` 进行测试，参考项目使用 `UserPointer`、`ProcessList` 等真实内核抽象。
- **文件系统**: 双方均存在该模块。共同 AST 指纹为 0，覆盖率均为 0.0%。证据强度：**未形成**。同名函数 `offset` 的外部对象完全不同；`ext4.rs` 中的同名函数 `read_block` 调用链异构。
- **设备驱动**: 双方均存在该模块。共同 AST 指纹为 0，覆盖率均为 0.0%。证据强度：**未形成**。同名函数 `probe` 的外部调用图谱无交集。
- **网络协议栈**: 双方均存在该模块。共同 AST 指纹为 0，覆盖率均为 0.0%。证据强度：**未形成**。目标项目仅有 2 个函数，参考项目有 51 个，规模与实现完全不同。
- **第三方库/公共依赖**: 仅参考项目存在该模块（740 个函数）。共同 AST 指纹为 0。证据强度：**未形成**。目标项目未包含该模块，属于架构性差异。

#### 3. 底层 AST 关键证据指纹表
| 所属板块 | 目标项目路径 | 参考项目路径 | 证据类型 | 技术说明 |
| :--- | :--- | :--- | :--- | :--- |
| 通用模块/其他 | `src/main.rs#clear` | `src/kernel/pcie/header.rs#clear` | 同名函数 | 均为寄存器位清除操作，但目标项目为内部闭环逻辑，参考项目涉及 `memory_barrier` 与 `self.read/write`，外部对象零交集。 |
| 通用模块/其他 | `src/main.rs#write_str` | `src/io.rs#write_str` | 同名函数 | 目标项目调用 `capture_user_output` 与 `crate::print!`，参考项目调用 `self.0.fill`，实现路径完全不同。 |
| 通用模块/其他 | `src/busybox.rs#set` | `src/kernel/pcie/header.rs#set` | 同名函数 | 目标项目为 busybox 条目迭代查找，参考项目为 PCIe 寄存器位设置，功能领域不同。 |
| 通用模块/其他 | `src/main.rs#get` | `src/kernel/pcie/header.rs#get` | 同名函数 | 目标项目返回 `Some`，参考项目涉及 `NonZero::new`、`memory_barrier` 与 BAR 寄存器操作，无结构重合。 |
| 进程管理 | `src/exec_cache.rs#lookup` | `src/fs/procfs.rs#lookup` | 跨板块同名 | 目标项目为可执行文件缓存查找，参考项目为 procfs 目录项查找，外部对象图谱完全不同。 |
| 内存管理 | `src/vm.rs#load_segment` | `src/kernel/task/loader/elf.rs#load_segment` | 同名函数 | 目标项目使用 `MapArea::new` 与 `alloc.alloc`，参考项目使用 `FileMapping::new` 与 `mmap_fixed`，内存抽象异构。 |
| 内存管理 | `src/vm.rs#unmap` | `src/kernel/mem/mm_list.rs#unmap` | 同名函数 | 目标项目操作裸指针 `core::ptr::read/write` 与自定义页表，参考项目使用 `VRange::new` 与 `Page::from_raw`。 |
| 内存管理 | `src/user.rs#protect` | `src/kernel/mem/mm_list.rs#protect` | 同名函数 | 目标项目使用 `checked_translated_range` 与 `u8::try_from`，参考项目使用 `VRange::new` 与 `mask_with_checked`。 |
| 文件系统 | `src/syscall/mod.rs#offset` | `src/kernel/mem/mm_list/mapping.rs#offset` | 跨板块同名 | 目标项目仅返回 `Some`，参考项目构造 `Self::new` 并克隆文件对象，语义完全不同。 |
| 文件系统 | `src/fs/ext4.rs#read_block` | `src/fs/ext4.rs#read_block` | 同名函数 | 目标项目调用 `self.block_byte_offset` 与 `self.read_at`，参考项目调用 `self.device.read_some` 与 `ByteBuffer::new`。 |
| 设备驱动 | `src/virtio/mmio.rs#probe` | `src/driver/goldfish_rtc.rs#probe` | 同名函数 | 目标项目为 MMIO 传输层探测，参考项目为 Goldfish RTC 驱动探测，设备类型与外部对象均不同。 |

#### 4. 核心代码调用流对比剖析
* **【进程管理/进程退出与资源回收】**
* **底层技术析理**: 目标项目 `exit_current` 的操作对象是静态槽位数组 `self.slots` 与状态机枚举 `RuntimeProcessState::Zombie`，资源回收通过 `release_all_pipe_fds` 清理管道描述符。参考项目 `remove_process` 的操作对象是全局进程表 `self.processes`、进程组 `self.pgroups` 与会话 `self.sessions`，资源回收依赖 `Arc::strong_count` 与 `rcu_sync`。前者是单地址空间运行时模拟器的确定性状态迁移，后者是真实多进程宏内核的引用计数与 RCU 并发回收，内存模型与同步策略根本不同。
* **代码溯源链接**:
  - 目标项目: `src/process.rs#exit_current`
  - 参考项目: `src/kernel/task/process_list.rs#remove_process`

* **【内存管理/段加载】**
* **底层技术析理**: 目标项目 `load_segment` 使用自定义 `MapArea::new` 与 `alloc.alloc` 进行物理页分配，偏移计算基于 `bias.wrapping_add`。参考项目 `load_segment` 使用 `FileMapping::new` 与 `mm_list.mmap_fixed` 进行文件映射，偏移对齐使用 `program_header.offset().align_down`。前者是自包含的物理内存分配器模型，后者是 VFS 文件映射模型，内存抽象层次完全不同。
* **代码溯源链接**:
  - 目标项目: `src/vm.rs#load_segment`
  - 参考项目: `src/kernel/task/loader/elf.rs#load_segment`

* **【系统调用/信号处理】**
* **底层技术析理**: 目标项目 `signal_pid` 通过 `find_pid_index` 直接索引静态槽位后调用 `raise_signal`，`tick_itimer_real` 通过 `read_volatile` 检查定时器状态。参考项目 `kill` 通过 `ProcessList::get().read()` 获取全局进程表，再通过 `try_find_pgroup` 或 `try_find_process` 定位目标后调用 `.raise`。前者是无锁的静态数组直接访问，后者是带锁的全局进程表遍历与进程组查找，同步与对象管理模型异构。
* **代码溯源链接**:
  - 目标项目: `src/process.rs#signal_pid`
  - 参考项目: `src/kernel/syscall/procops.rs#kill`

#### 5. 单方模块与降权项
- **第三方库/公共依赖**: 仅参考项目存在该模块（740 个函数），包含 `eonix_hal`、`eonix_percpu` 等 HAL 层与宏定义。目标项目无对应模块，属于架构性差异，不构成同源证据。
- **跨板块同名函数**: `lookup`（进程管理 vs 文件系统）、`offset`（文件系统 vs 内存管理）等跨板块同名函数，因所属子系统与外部对象完全不同，仅能作为弱信号，不构成同板块主证据。
- **Rust 语言惯用法**: `Some`、`Ok`、`Err`、`assert!`、`assert_eq!`、`to_le_bytes`、`from_le_bytes`、`core::ptr::addr_of!` 等 20 个共同 AST 指纹属于 Rust 标准库与语言级惯用结构，在任何 Rust 项目中均大量出现，不能作为业务逻辑复用的证据。
- **POSIX 公共语义**: `wait4`、`exit`、`kill`、`futex`、`procfs` 等系统调用语义由 POSIX/Linux 标准定义，其宏观步骤（查找→操作→返回）是规范强制，不是参考项目的独创表达。

#### 6. 保守结论
基于 0.58% 的客观 AST 相似度与核心板块 0.0% 的共同指纹覆盖率，目标项目 `[2026-T2026105749910169-XX]` 与参考项目 `[2025-sudo_pacman_Syu-同济大学]` 在底层架构上存在显著差异。目标项目采用“确定性运行时模拟器 + 静态槽位 + 状态机调度”范式，参考项目采用“宏内核 + 全局链表/RCU + 条件变量”范式，外部对象图谱零交集。20 个共同 AST 指纹均为 Rust 语言惯用法，不构成业务逻辑复用证据。控方指控的“语义等价混淆”实质是对正当架构差异的误读。综合判定，双方为独立开发，未发现可保护的表达层面抄袭或违规复用。

------------------------------

