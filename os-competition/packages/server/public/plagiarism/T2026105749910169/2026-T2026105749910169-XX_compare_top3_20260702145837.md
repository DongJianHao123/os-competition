# 🛡️ OS 内核赛自动化代码级架构审计报告

**审计时间**: `2026-07-02 14:58:37`
**参赛作品名称**: `2026-T2026105749910169-XX`
**年份**: `2026`
**学校**: `XX`
**队伍名称**: `T2026105749910169`
**比赛名字**: `OS-Competition`
**比赛子赛道**: `Kernel-Implementation`
**仓库地址**: `https://gitlab.eduxiji.net/educg-group-43501-3132633/26QS`
**推测开发语言**: `Rust / C (由AST探针自动识别)`
**特征函数量**: `1818`

## ⚔️ 底层代码同源性对比 (AST Homology)
> 本部分由专门的代码审查智能体，对该项目与符合年份条件的历届最高相似度项目进行控制流与底层调用对比。新版报告会先给出确定性的全模块覆盖矩阵，再给出 Agent 的分模块解释，避免只围绕少量抽样函数展开。

### 🏆 Top 3 底层对标: vs `2022-caporustos-浙江大学`（浙江大学 / caporustos）

#### 对方作品信息

**对方年份**: `2022`
**对方学校**: `浙江大学`
**对方队伍名称**: `caporustos`
**对方比赛名字**: `OS-Competition`
**对方比赛子赛道**: `Kernel-Implementation`
**对方仓库地址**: `https://gitlab.eduxiji.net/yhwu_is/oskernel2022-caporustos`

> 双向覆盖率：共同 AST 指纹: 21；唯一 AST 指纹: 目标项目 1626，参考项目 1959；覆盖率: 目标项目 1.29%，参考项目 1.07%

## 全模块对比总览（确定性证据）

> 总体覆盖率：共同 AST 指纹: 21；唯一 AST 指纹: 目标项目 1626，参考项目 1959；覆盖率: 目标项目 1.29%，参考项目 1.07%

### 1. 模块覆盖矩阵
| 模块 | 目标函数数 | 参考函数数 | 共同唯一 AST 指纹 | Target 覆盖 | Base 覆盖 | 状态 |
| :--- | ---: | ---: | ---: | ---: | ---: | :--- |
| 进程管理 | 255 | 161 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |
| 任务调度/异步运行时 | 190 | 34 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |
| 内存管理 | 291 | 232 | 1 | 0.36% | 0.47% | 双方存在且有结构重合 |
| 中断/异常处理 | 35 | 10 | 2 | 7.14% | 22.22% | 双方存在且有结构重合 |
| 系统调用 | 143 | 49 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |
| 文件系统 | 219 | 259 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |
| 设备驱动 | 108 | 36 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |
| 网络协议栈 | 2 | 0 | 0 | 0.0% | 0.0% | 仅目标项目体现 |
| 文件系统/第三方库 | 95 | 2 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |
| 第三方库 | 0 | 2951 | 0 | 0.0% | 0.0% | 仅参考项目体现 |
| 通用模块/其他 | 480 | 41 | 1 | 0.24% | 2.7% | 双方存在且有结构重合 |

### 2. 分模块证据清单

#### 进程管理
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `src/process.rs#empty` | `codes/os/src/process/process/signal/sigaction.rs#empty` | 同板块同名 | 内部闭环逻辑 | Sigset |
| `src/process.rs#exit_current` | `codes/os/src/process/process/core/pcb.rs#execve` | 同模块上下文对齐/差异观察 | ProcessAction::ExitToKernel, ProcessSlot::empty, RuntimeProcessState::Zombie, self.current_index, self.find_pid_index, self.find_runnable_sibling ...(+6) | (0..=args.len()).collect, (0..=env.len()).collect, KERNEL_SPACE.read, KERNEL_SPACE.read().token, MemorySet::from_elf, String::from ...(+35) |
| `src/process.rs#pselect6_current` | `codes/os/src/syscall/process.rs#sys_prlimit` | 同模块上下文对齐/差异观察 | PROCESS_FD_LIMIT.div_ceil, PselectAction::Ready, PselectAction::Switched, RuntimeProcessState::WaitingPipeRead, Some, byte.count_ones ...(+17) | UserBuffer::empty, UserBuffer::new, current_task, current_task().unwrap, current_user_token, find_task ...(+10) |
| `src/syscall/mod.rs#seeded_procfs_supports_busybox_proc_consumers` | `codes/os/src/syscall/signal.rs#sys_sigtimedwait` | 同模块上下文对齐/差异观察 | MockMemory::new, SyscallContext::new, assert!, assert_eq!, b"meminfo".len, b"mounts".len ...(+6) | current_task, current_task().unwrap, current_user_token, get_time, thread.inner.lock, thread.inner.lock().pending.mask.remove_set ...(+1) |

#### 任务调度/异步运行时
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `src/syscall/file.rs#sys_utimensat` | `codes/os/src/syscall/fs.rs#sys_utimensat` | 同板块同名 | buf.copy_from_slice, decode, memory.read_bytes, now_monotonic_nanos, path.as_slice, path_parent_is_nondir ...(+3) | OpenFlags::from_bits, OpenFlags::from_bits(flags as u32).unwrap, current_task, current_task().unwrap, current_user_token, f.find ...(+9) |
| `src/syscall/file.rs#sys_renameat2` | `codes/os/src/syscall/fs.rs#sys_renameat2` | 同板块同名 | child.path, ctx.find_node, ctx.mark_node_removed, ctx.nodes[..ctx.node_len].iter, ctx.nodes[..ctx.node_len].iter().any, ctx.nodes[old_node].path[..new_resolved.len].copy_from_slice ...(+10) | current_task, current_task().unwrap, current_user_token, get_file_discpt, newfile.set_head_cluster, oldfile.delete ...(+7) |
| `src/syscall/file.rs#sys_readlinkat` | `codes/os/src/syscall/fs.rs#sys_readlinkat` | 同板块同名 | ctx.cwd, ctx.find_node, ctx.nodes[node]
        .read_data, ctx.nodes[node]
        .read_data(0, copy_len, |target| memory.write_bytes(buf_addr, target))
        .unwrap_or, ctx.nodes[node].data_len, cwd.len ...(+11) | UserBuffer::new, current_user_token, gdb_println!, panic!, path.as_str, procinfo.as_bytes ...(+4) |
| `src/syscall/file.rs#sys_getdents64` | `codes/os/src/syscall/fs.rs#sys_getdents64` | 同板块同名 | align_up, basename, ctx.find_next_directory_entry, ctx.nodes[entry_node].path, memory.write_bytes, name.len ...(+4) | UserBuffer::new, current_task, current_task().unwrap, current_user_token, getdents64_inner, inner.current_path.clone ...(+9) |

#### 内存管理
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `src/syscall/memory.rs#sys_mmap` | `codes/os/src/syscall/process.rs#sys_mmap` | 同板块同名 | Some, align_up, ctx
            .read_file_data, ctx
            .read_file_data(node, offset, count, |data| {
                memory.write_bytes(map_addr, data)
            })
            .unwrap_or, ctx.file_data_len, ctx.read_file_data ...(+12) | current_task, current_task().unwrap, gdb_println!, task.mmap, thread.get_parent, thread.get_parent().unwrap |
| `src/vm.rs#map` | `codes/os/src/mm/page_table.rs#map` | 同板块同名 | Err, Ok, alloc.alloc, core::ptr::read, core::ptr::write, leaf_pte ...(+5) | PageTableEntry::new, pte.is_valid, self.find_pte_create, self.find_pte_create(vpn).unwrap |
| `src/vm.rs#map` | `codes/os/src/mm/memory_set.rs#map` | 同板块同名 | Err, Ok, alloc.alloc, core::ptr::read, core::ptr::write, la_leaf_pte ...(+6) | self.map_one |
| `src/paging.rs#map_one` | `codes/os/src/mm/memory_set.rs#map_one` | 同板块同名 | Err, Ok, alloc.alloc_zeroed, alloc.alloc_zeroed().ok_or, core::ptr::read, core::ptr::write ...(+4) | PTEFlags::from_bits, PTEFlags::from_bits(self.map_perm.bits).unwrap, PhysPageNum, frame_alloc, page_table.map, panic! ...(+2) |

#### 中断/异常处理
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `src/arch/riscv64.rs#set_program_counter` | `codes/os/src/trap/mod.rs#set_syscall` | 同板块同结构 | 内部闭环逻辑 | 内部闭环逻辑 |
| `src/arch/riscv64.rs#program_counter` | `codes/os/src/trap/mod.rs#get_syscall` | 同板块同结构 | 内部闭环逻辑 | 内部闭环逻辑 |
| `src/arch/riscv64.rs#prepare_user_return` | `codes/os/src/trap/mod.rs#trap_from_kernel` | 同模块上下文对齐/差异观察 | 内部闭环逻辑 | panic! |
| `src/arch/loongarch64.rs#prepare_user_return` | `codes/os/src/syscall/mod.rs#reset_time_int` | 同模块上下文对齐/差异观察 | 内部闭环逻辑 | crate::timer::set_next_trigger, crate::trap::enable_timer_interrupt |

#### 系统调用
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `src/main.rs#run_busybox_script` | `codes/user/src/syscall.rs#syscall` | 同模块上下文对齐/差异观察 | busybox::render_busybox_script, crate::println!, read_busybox_commands, runner::render_unsupported | core::arch::asm! |
| `src/syscall/mod.rs#sys_msgrcv` | `codes/user/src/lib.rs#waitpid` | 同模块上下文对齐/差异观察 | (0..reg[idx].count).find, core::cmp::min, core::ptr::addr_of_mut!, memory.write_bytes, msg_index, mtype.to_le_bytes ...(+1) | sys_waitpid, yield_ |
| `src/syscall/mod.rs#sys_msgsnd` | `codes/os/src/main.rs#test` | 同模块上下文对齐/差异观察 | core::ptr::addr_of_mut!, i64::from_le_bytes, m.data[..msgsz].copy_from_slice, memory.read_bytes, msg_index, sem_now_seconds ...(+2) | 内部闭环逻辑 |
| `src/syscall/mod.rs#sys_setxattr` | `codes/user/src/lib.rs#wait` | 同模块上下文对齐/差异观察 | core::ptr::addr_of_mut!, memory.read_bytes, read_cstr, t.iter, t.iter().position, value[..size].copy_from_slice ...(+2) | sys_waitpid, yield_ |

#### 文件系统
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `src/syscall/mod.rs#empty` | `codes/fat32_fs/src/layout.rs#empty` | 同板块同名 | 内部闭环逻辑 | 内部闭环逻辑 |
| `src/runner.rs#basic_subtest_elf_path` | `codes/user/src/lib.rs#busybox_test` | 同模块上下文对齐/差异观察 | Some | (*programname_op).contains, String::from, String::new, Vec::new, args.len, args[i].as_ptr ...(+28) |
| `src/syscall/mod.rs#ftruncate_statfs_sync_and_metadata_probe_boundaries_are_real` | `codes/fat32_fs/src/vfs.rs#find_long_name` | 同模块上下文对齐/差异观察 | MockMemory::new, assert_eq!, ctx_with_file, dispatch, memory.write_cstr | LongDirEntry::empty, ShortDirEntry::empty, Some, String::from, VFile::new, Vec::new ...(+18) |
| `src/user.rs#prepare_initial_stack_args_inner` | `codes/os/src/drivers/block/sdcard.rs#get_csdregister` | 同模块上下文对齐/差异观察 | DEFAULT_ENV.len, Ok, arg.checked_add_nul_len, arg.len, args
            .len, args
            .len()
            .checked_add ...(+42) | Err, Ok, self.end_cmd, self.get_response, self.read_data, self.send_cmd ...(+2) |

#### 设备驱动
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `src/virtio/mmio.rs#read_sector` | `codes/os/src/drivers/block/sdcard.rs#read_sector` | 同板块同名 | Err, Ok, self.breaker.is_dead, self.breaker.record_failure, self.breaker.record_success, self.capture_error_diag ...(+3) | Err, Ok, assert!, chunk.iter_mut, chunk.iter_mut().zip, data_buf.chunks_mut ...(+7) |
| `src/main.rs#collect_basic_lines` | `codes/os/src/drivers/block/sdcard.rs#write_sector` | 同模块上下文对齐/差异观察 | Err, Ok, PathBuf::from_slice, ScriptLines::new, ScriptRuntimeState::new_from_cwd, collect_basic_lines ...(+17) | (data_buf.len() / SEC_LEN).try_into, (data_buf.len() / SEC_LEN).try_into().unwrap, Err, Ok, assert!, chunk.iter ...(+10) |
| `src/virtio/pci.rs#submit_read` | `codes/os/src/drivers/block/spi.rs#configure` | 同模块上下文对齐/差异观察 | Err, Ok, RequestHeader::read, addr_of!, addr_of_mut!, avail_idx.wrapping_add ...(+10) | self.spi.csdef.modify, self.spi.csid.modify, self.spi.fmt.modify, w.bits, w.dir, w.dir().clear_bit ...(+4) |
| `src/virtio/mmio.rs#submit_read` | `codes/os/src/drivers/block/spi.rs#recv_data` | 同模块上下文对齐/差异观察 | Err, Ok, RequestHeader::read, addr_of!, addr_of_mut!, avail_idx.wrapping_add ...(+10) | rx.len, self.spi.csid.modify, self.spi.csmode.modify, self.spi.fmt.modify, self.spi.ip.read, self.spi.ip.read().rxwm ...(+11) |

#### 网络协议栈
仅目标项目中观察到该模块代表函数，参考项目没有同模块可对齐证据：
- 目标项目：`src/netperf.rs#render_netperf_script`，调用摘要：crate::runner::render_basic_end, crate::runner::render_basic_start, sink.write_str, write_u32
- 目标项目：`src/netperf.rs#write_u32`，调用摘要：core::str::from_utf8, core::str::from_utf8(&ch).unwrap_or, sink.write_str

#### 文件系统/第三方库
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `src/fs/ext4.rs#build_fixture` | `codes/dependency/fu740-pac/src/spi0/fmt.rs#dir` | 同模块上下文对齐/差异观察 | MockBlockDevice::new, disk.add_dir_entry, disk.write, disk.write_u16, disk.write_u32, write_extent_inode | DIR_R::new |
| `src/fs/ext4.rs#parse_directory_block` | `codes/dependency/fu740-pac/src/spi0/fmt.rs#dir` | 同模块上下文对齐/差异观察 | FileName::from_slice, FileType::from_dir_entry, Ok, block
                .get, block
                .get(offset + 8..offset + 8 + name_len)
                .ok_or, block.len ...(+4) | 内部闭环逻辑 |

#### 第三方库
仅参考项目中观察到该模块代表函数，目标项目没有同模块可对齐证据：
- 参考项目：`codes/dependency/num-derive/src/lib.rs#float`，调用摘要：NumTraits::new, import.wrap, import.wrap("Float", &name, impl_).into, newtype_inner, newtype_inner(&ast.data).expect, parse! ...(+1)
- 参考项目：`codes/dependency/num-derive/src/lib.rs#to_primitive`，调用摘要：NumTraits::new, import.wrap, import.wrap("ToPrimitive", &name, impl_).into, newtype_inner, panic!, parse! ...(+5)
- 参考项目：`codes/dependency/num-derive/src/lib.rs#from_primitive`，调用摘要：NumTraits::new, clauses.is_empty, import.wrap, import.wrap("FromPrimitive", &name, impl_).into, newtype_inner, panic! ...(+5)
- 参考项目：`codes/dependency/log/src/kv/value.rs#visit`，调用摘要：Visitor, self.0
                    .visit_any, self.0
                    .visit_any(Value { inner: value })
                    .map_err, self.0.visit_bool, self.0.visit_bool(value).map_err, self.0.visit_borrowed_error ...(+21)

#### 通用模块/其他
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `src/main.rs#write_str` | `codes/os/src/console.rs#write_str` | 同板块同名 | capture_user_output, crate::print!, value.as_bytes | Ok, console_putchar, s.chars |
| `src/runner.rs#write_str` | `codes/user/src/console.rs#write_str` | 同板块同名 | assert!, self.bytes[self.len..end].copy_from_slice, value.as_bytes, value.len | Ok, s.as_bytes, write |
| `src/main.rs#as_bytes` | `codes/os/src/lang_items.rs#as_bytes` | 同板块同名 | 内部闭环逻辑 | core::mem::size_of::<T>, core::slice::from_raw_parts |
| `src/ltp.rs#overflowed` | `codes/os/src/main.rs#is_in` | 同板块同结构 | 内部闭环逻辑 | 内部闭环逻辑 |


#### 1. 宏观判定
- **定性结论**: 底层架构显著差异 / 独立开发 (相似度: 0.59%，共同 AST 指纹: 21；唯一 AST 指纹: 目标项目 1626，参考项目 1959；覆盖率: 目标项目 1.29%，参考项目 1.07%)

#### 2. 全模块覆盖矩阵解读
- **进程管理**
  - 双方存在情况：双方均存在。
  - 共同 AST 指纹：0。
  - 覆盖率说明：目标覆盖率 0.0%，参考覆盖率 0.0%。表明在进程生命周期、调度、信号处理等核心逻辑上，未发现任何结构级同源证据。
  - 证据强度：未形成。

- **任务调度/异步运行时**
  - 双方存在情况：双方均存在。
  - 共同 AST 指纹：0。
  - 覆盖率说明：目标覆盖率 0.0%，参考覆盖率 0.0%。双方调度器实现路径无 AST 层面交集。
  - 证据强度：未形成。

- **内存管理**
  - 双方存在情况：双方均存在。
  - 共同 AST 指纹：1。
  - 覆盖率说明：目标覆盖率 0.36%，参考覆盖率 0.47%。唯一的共同指纹源于 `map` 等泛化接口，属于操作系统内存管理的通用范式。
  - 证据强度：弱。

- **中断/异常处理**
  - 双方存在情况：双方均存在。
  - 共同 AST 指纹：2。
  - 覆盖率说明：目标覆盖率 7.14%，参考覆盖率 22.22%。共同指纹为 `set_program_counter`/`program_counter` 等与硬件架构绑定的极简 getter/setter，是 RISC-V 特权级编程的必然表达。
  - 证据强度：弱。

- **系统调用**
  - 双方存在情况：双方均存在。
  - 共同 AST 指纹：0。
  - 覆盖率说明：目标覆盖率 0.0%，参考覆盖率 0.0%。系统调用分发与实现路径无结构重合。
  - 证据强度：未形成。

- **文件系统**
  - 双方存在情况：双方均存在。
  - 共同 AST 指纹：0。
  - 覆盖率说明：目标覆盖率 0.0%，参考覆盖率 0.0%。文件系统实现（如 Ext4 vs. FAT32）及 VFS 抽象层无共同指纹。
  - 证据强度：未形成。

- **设备驱动**
  - 双方存在情况：双方均存在。
  - 共同 AST 指纹：0。
  - 覆盖率说明：目标覆盖率 0.0%，参考覆盖率 0.0%。驱动模型（VirtIO vs. SDCard/SPI）完全不同。
  - 证据强度：未形成。

- **网络协议栈**
  - 双方存在情况：仅目标项目体现。
  - 共同 AST 指纹：0。
  - 覆盖率说明：目标项目独有模块，无法比对。
  - 证据强度：未形成。

- **第三方库/公共依赖**
  - 双方存在情况：参考项目大量存在，目标项目少量存在。
  - 共同 AST 指纹：0。
  - 覆盖率说明：参考项目包含 2951 个第三方库函数，目标项目仅 95 个，且无共同指纹，表明依赖库完全异构。
  - 证据强度：未形成。

#### 3. 底层 AST 关键证据指纹表
| 所属板块 | 目标项目路径 | 参考项目路径 | 证据类型 | 技术说明 |
| :--- | :--- | :--- | :--- | :--- |
| 中断/异常处理 | `src/arch/riscv64.rs#set_program_counter` | `codes/os/src/trap/mod.rs#set_syscall` | 同板块同结构 | 均为设置系统调用号的极简函数，是 RISC-V 特权级规范下的标准操作，不构成独创性表达。 |
| 中断/异常处理 | `src/arch/riscv64.rs#program_counter` | `codes/os/src/trap/mod.rs#get_syscall` | 同板块同结构 | 均为读取系统调用号的极简函数，是 RISC-V 特权级规范下的标准操作，不构成独创性表达。 |
| 内存管理 | `src/vm.rs#map` | `codes/os/src/mm/page_table.rs#map` | 同板块同名 | 页表映射的通用接口，双方均调用 `find_pte_create` 等内部方法，但外部依赖对象（`alloc` vs `frame_alloc`）完全不同。 |
| 通用模块/其他 | `src/main.rs#write_str` | `codes/os/src/console.rs#write_str` | 同板块同名 | 控制台输出的标准 Rust trait 实现，目标调用 `capture_user_output`，参考调用 `console_putchar`，底层输出机制异构。 |
| 通用模块/其他 | `src/main.rs#as_bytes` | `codes/os/src/lang_items.rs#as_bytes` | 同板块同名 | Rust 语言项 `AsBytes` trait 的强制实现，属于编译器驱动的泛型约束，无用户态独创性。 |

#### 4. 核心代码调用流对比剖析
* **【进程管理/进程退出与调度】**
* **底层技术析理**: 目标项目采用基于 `ProcessRuntime` 和 `ProcessSlot` 的确定性状态机模型，通过 `RuntimeProcessState::Zombie` 枚举标记状态，并由 `find_runnable_sibling` 等内部方法驱动调度。参考项目则基于 `TaskControlBlock` 和 `child_queue`/`ready_queue` 双队列模型，通过 `check_finished` 轮询僵尸状态并调用全局 `schedule` 函数。两者在进程控制块（PCB）抽象、调度队列数据结构以及状态转移的驱动方式上存在根本性架构差异。
* **代码溯源链接**:
  - 目标项目: `src/process.rs#exit_current`
  - 参考项目: `codes/os/src/process/process/manager/manager.rs#check_finished`

* **【内存管理/页表映射】**
* **底层技术析理**: 目标项目的 `map` 函数直接操作 `alloc` 接口进行物理页分配，并通过 `core::ptr::read/write` 直接操作页表项。参考项目的 `map` 函数则通过 `frame_alloc` 分配物理帧，并调用 `PageTableEntry::new` 构造页表项，其内存模型强依赖于 `PhysPageNum` 和 `PTEFlags` 的位操作。两者在物理内存分配器接口、页表项构造方式上完全不同。
* **代码溯源链接**:
  - 目标项目: `src/vm.rs#map`
  - 参考项目: `codes/os/src/mm/page_table.rs#map`

* **【系统调用/文件系统】**
* **底层技术析理**: 目标项目的 `sys_getdents64` 直接操作内部的 `RuntimeNode` 树，通过 `ctx.find_next_directory_entry` 遍历节点并调用 `memory.write_bytes` 写入用户缓冲区。参考项目的 `sys_getdents64` 则通过 `UserBuffer` 和 `translated_byte_buffer` 进行用户态地址翻译，并调用 `getdents64_inner` 操作 FAT32 文件系统的目录项。两者在文件系统抽象层（内存节点树 vs. 磁盘文件系统）和用户态内存访问机制上截然不同。
* **代码溯源链接**:
  - 目标项目: `src/syscall/file.rs#sys_getdents64`
  - 参考项目: `codes/os/src/syscall/fs.rs#sys_getdents64`

#### 5. 单方模块与降权项
- **单方模块**：
  - **网络协议栈**：仅存在于目标项目 (`src/netperf.rs`)，参考项目无此模块，无法形成同源比对。
  - **第三方库**：参考项目包含大量第三方库（2951 个函数，如 `num-derive`, `log`, `fu740-pac`），目标项目极少（95 个），且无共同指纹。这证明双方依赖的生态和构建的底层驱动完全不同。
- **降权项**：
  - **同名函数 `empty`**：目标 `src/process.rs#empty` 与参考 `sigaction.rs#empty` 虽同名，但前者为内部闭环逻辑，后者调用 `Sigset`，功能与调用上下文均不同，不构成同源证据。
  - **泛型/公共接口**：`write_str`, `as_bytes` 等为 Rust 标准库 trait 的强制实现，`assert!`, `assert_eq!` 为语言内置测试宏，均为语言级通用范式，不具备代码指纹特性。

#### 6. 保守结论
综合全量 AST 证据，目标项目与参考项目的整体相似度极低（0.59%），在进程管理、系统调用、文件系统等核心板块未发现任何共同结构指纹。双方采用了完全异构的架构设计：目标项目是基于 `MockMemory`/`ProcessRuntime` 的确定性仿真/运行时框架，而参考项目是基于 `TaskControlBlock`/`KERNEL_SPACE` 的传统单体内核。观察到的极少数共同指纹均为硬件规范、语言标准或通用编程范式所强制要求的表达，不构成独创性代码的复用。现有证据不足以支持抄袭或深度复用的指控，应认定为独立开发。

------------------------------

