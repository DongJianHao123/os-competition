# 🛡️ OS 内核赛自动化代码级架构审计报告

**审计时间**: `2026-07-02 14:58:37`
**参赛作品名称**: `2026-T2026105749910169-XX`
**年份**: `2026`
**学校**: `XX`
**队伍名称**: `T2026105749910169`
**比赛名字**: `OS-Competition`
**比赛子赛道**: `Kernel-Implementation`
**仓库地址**: `https://gitlab.eduxiji.net/educg-group-43501-3132633/26QS`
**推测开发语言**: `Rust / C (由AST探针自动识别)`
**特征函数量**: `1818`

## ⚔️ 底层代码同源性对比 (AST Homology)
> 本部分由专门的代码审查智能体，对该项目与符合年份条件的历届最高相似度项目进行控制流与底层调用对比。新版报告会先给出确定性的全模块覆盖矩阵，再给出 Agent 的分模块解释，避免只围绕少量抽样函数展开。

### 🏆 Top 2 底层对标: vs `2022-Maturin-清华大学`（清华大学 / Maturin）

#### 对方作品信息

**对方年份**: `2022`
**对方学校**: `清华大学`
**对方队伍名称**: `Maturin`
**对方比赛名字**: `OS-Competition`
**对方比赛子赛道**: `Kernel-Implementation`
**对方仓库地址**: `https://gitlab.eduxiji.net/scPointer/maturin.git`

> 双向覆盖率：共同 AST 指纹: 20；唯一 AST 指纹: 目标项目 1626，参考项目 1491；覆盖率: 目标项目 1.23%，参考项目 1.34%

## 全模块对比总览（确定性证据）

> 总体覆盖率：共同 AST 指纹: 20；唯一 AST 指纹: 目标项目 1626，参考项目 1491；覆盖率: 目标项目 1.23%，参考项目 1.34%

### 1. 模块覆盖矩阵
| 模块 | 目标函数数 | 参考函数数 | 共同唯一 AST 指纹 | Target 覆盖 | Base 覆盖 | 状态 |
| :--- | ---: | ---: | ---: | ---: | ---: | :--- |
| 进程管理 | 255 | 60 | 1 | 0.41% | 1.82% | 双方存在且有结构重合 |
| 任务调度/异步运行时 | 190 | 160 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |
| 内存管理 | 291 | 275 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |
| 中断/异常处理 | 35 | 4 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |
| 系统调用 | 143 | 8 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |
| 文件系统 | 219 | 593 | 1 | 0.47% | 0.19% | 双方存在且有结构重合 |
| 设备驱动 | 108 | 15 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |
| 网络协议栈 | 2 | 8 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |
| 文件系统/第三方库 | 95 | 0 | 0 | 0.0% | 0.0% | 仅目标项目体现 |
| 第三方库 | 0 | 335 | 0 | 0.0% | 0.0% | 仅参考项目体现 |
| 通用模块/其他 | 480 | 218 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |

### 2. 分模块证据清单

#### 进程管理
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `src/script_exec.rs#len` | `kernel/src/task/task.rs#get_pid_num` | 同板块同结构 | 内部闭环逻辑 | 内部闭环逻辑 |
| `src/process.rs#exit_current` | `kernel/src/syscall/process.rs#waitpid` | 同模块上下文对齐/差异观察 | ProcessAction::ExitToKernel, ProcessSlot::empty, RuntimeProcessState::Zombie, self.current_index, self.find_pid_index, self.find_runnable_sibling ...(+6) | child.get_code_if_exit, child.get_pid_num, get_current_task, get_current_task().unwrap, task.inner.lock, tcb_inner.children.iter ...(+2) |
| `src/process.rs#pselect6_current` | `libc/src/regression/pthread_cancel-sem_wait.c#main` | 同模块上下文对齐/差异观察 | PROCESS_FD_LIMIT.div_ceil, PselectAction::Ready, PselectAction::Switched, RuntimeProcessState::WaitingPipeRead, Some, byte.count_ones ...(+17) | TESTC, TESTR, pthread_cancel, pthread_create, pthread_join, sem_init ...(+2) |
| `src/syscall/mod.rs#seeded_procfs_supports_busybox_proc_consumers` | `kernel/src/task/cpu_local.rs#handle_zombie_task` | 同模块上下文对齐/差异观察 | MockMemory::new, SyscallContext::new, assert!, assert_eq!, b"meminfo".len, b"mounts".len ...(+6) | Arc::downgrade, ORIGIN_USER_PROC.clone, ORIGIN_USER_PROC.clone().inner.try_lock, Some, child.clone, child.inner.try_lock ...(+9) |

#### 任务调度/异步运行时
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `src/syscall/file.rs#sys_linkat` | `kernel/src/syscall/fs.rs#sys_linkat` | 同板块同名 | RuntimeNode::hard_link, ctx.add_runtime_node, ctx.file_data_node, ctx.find_node, ctx.find_node(new_resolved.as_slice()).is_some, ctx.validate_parent_dir ...(+6) | Err, Ok, get_current_task, get_current_task().unwrap, resolve_path_from_fd, try_add_link |
| `src/syscall/file.rs#sys_unlinkat` | `kernel/src/syscall/fs.rs#sys_unlinkat` | 同板块同名 | child.path, ctx.find_node, ctx.mark_node_removed, ctx.nodes[..ctx.node_len]
            .iter, ctx.nodes[..ctx.node_len]
            .iter()
            .any, is_direct_child ...(+4) | Err, Ok, get_current_task, get_current_task().unwrap, resolve_path_from_fd, try_remove_link |
| `src/syscall/file.rs#sys_lseek` | `kernel/src/syscall/fs.rs#sys_lseek` | 同板块同名 | base.checked_add, ctx.file_data_len | Err, Ok, SeekFrom::Current, SeekFrom::End, SeekFrom::Start, file.seek ...(+5) |
| `src/syscall/file.rs#sys_fstat` | `kernel/src/syscall/fs.rs#sys_fstat` | 同板块同名 | stat_mode, write_stat_for_node, write_stat_record | Err, Ok, file.get_stat, get_current_task, get_current_task().unwrap, task.fd_manager.lock ...(+1) |

#### 内存管理
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `src/syscall/memory.rs#sys_mmap` | `kernel/src/syscall/process.rs#sys_mmap` | 同板块同名 | Some, align_up, ctx
            .read_file_data, ctx
            .read_file_data(node, offset, count, |data| {
                memory.write_bytes(map_addr, data)
            })
            .unwrap_or, ctx.file_data_len, ctx.read_file_data ...(+12) | BackEndFile::new, Err, Ok, SeekFrom::Start, Some, drop ...(+11) |
| `src/vm.rs#map` | `kernel/src/memory/page_table.rs#map` | 同板块同名 | Err, Ok, alloc.alloc, core::ptr::read, core::ptr::write, leaf_pte ...(+5) | Err, Ok, flags.is_empty, println!, pte.is_valid, pte.set_all ...(+1) |
| `src/vm.rs#unmap` | `kernel/src/memory/page_table.rs#unmap` | 同板块同名 | Some, core::ptr::read, core::ptr::write, pte_is_leaf, pte_to_table, vpn | (*pte).clear, (*pte).is_valid, Err, Ok, println!, self.find_pte |
| `src/user.rs#clear` | `kernel/src/memory/page_table.rs#clear` | 同板块同名 | self.bytes.fill, self.materialized.clear_all, self.protections.fill | 内部闭环逻辑 |

#### 中断/异常处理
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `src/arch/riscv64.rs#prepare_user_return` | `libc/src/functional/fcntl.c#main` | 同模块上下文对齐/差异观察 | 内部闭环逻辑 | TEST, TESTE, _exit, fclose, fcntl, fileno ...(+4) |
| `src/arch/loongarch64.rs#prepare_user_return` | `kernel/src/signal/sig_action.rs#get_restorer` | 同模块上下文对齐/差异观察 | 内部闭环逻辑 | self.flags.contains |
| `src/arch/riscv64.rs#trap_frame_layout_matches_assembly_contract` | `kernel/src/trap/mod.rs#init` | 同模块上下文对齐/差异观察 | assert_eq! | stvec::write |
| `src/arch/loongarch64.rs#trap_frame_layout_matches_assembly_contract` | `kernel/src/trap/mod.rs#enable_timer_interrupt` | 同模块上下文对齐/差异观察 | assert_eq! | sie::set_stimer |

#### 系统调用
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `src/main.rs#run_busybox_script` | `kernel/src/syscall/flags.rs#default` | 同模块上下文对齐/差异观察 | busybox::render_busybox_script, crate::println!, read_busybox_commands, runner::render_unsupported | Self::from_str |
| `src/syscall/mod.rs#sys_msgrcv` | `libc/src/regression/syscall-sign-extend.c#main` | 同模块上下文对齐/差异观察 | (0..reg[idx].count).find, core::cmp::min, core::ptr::addr_of_mut!, memory.write_bytes, msg_index, mtype.to_le_bytes ...(+1) | T, open, syscall, t_error |
| `src/syscall/mod.rs#sys_msgsnd` | `kernel/src/syscall/flags.rs#set_info` | 同模块上下文对齐/差异观察 | core::ptr::addr_of_mut!, i64::from_le_bytes, m.data[..msgsz].copy_from_slice, memory.read_bytes, msg_index, sem_now_seconds ...(+2) | 内部闭环逻辑 |
| `src/syscall/mod.rs#sys_setxattr` | `kernel/src/syscall/flags.rs#resolve_clone_flags_and_signal` | 同模块上下文对齐/差异观察 | core::ptr::addr_of_mut!, memory.read_bytes, read_cstr, t.iter, t.iter().position, value[..size].copy_from_slice ...(+2) | CloneFlags::from_bits, CloneFlags::from_bits(flag as u32 & (!0x3f)).unwrap, SignalNo::try_from, SignalNo::try_from(flag as u8 & 0x3f).unwrap |

#### 文件系统
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `src/syscall/mod.rs#nofile_limit` | `rust-fatfs/src/dir.rs#len` | 同板块同结构 | 内部闭环逻辑 | 内部闭环逻辑 |
| `src/runner.rs#basic_subtest_elf_path` | `rust-fatfs/src/boot_sector.rs#format_bpb` | 同模块上下文对齐/差异观察 | Some | Err, FatType::from_clusters, Ok, assert!, bpb.total_clusters, debug_assert! ...(+15) |
| `src/syscall/mod.rs#ftruncate_statfs_sync_and_metadata_probe_boundaries_are_real` | `rust-fatfs/src/boot_sector.rs#determine_sectors_per_fat` | 同模块上下文对齐/差异观察 | MockMemory::new, assert_eq!, ctx_with_file, dispatch, memory.write_cstr | fat_type.bits_per_fat_entry, u32::from, u64::from |
| `src/user.rs#prepare_initial_stack_args_inner` | `kernel/src/file/device/mod.rs#open_file` | 同模块上下文对齐/差异观察 | DEFAULT_ENV.len, Ok, arg.checked_add_nul_len, arg.len, args
            .len, args
            .len()
            .checked_add ...(+42) | Arc::new, FatFile::new, FdDir::new, MEMORY_FS.root_dir, Ok, Some ...(+18) |

#### 设备驱动
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `src/main.rs#collect_basic_lines` | `kernel/src/drivers/block/mod.rs#block_device_test` | 同模块上下文对齐/差异观察 | Err, Ok, PathBuf::from_slice, ScriptLines::new, ScriptRuntimeState::new_from_cwd, collect_basic_lines ...(+17) | BLOCK_DEVICE.clone, assert_eq!, block_device.read_block, block_device.write_block, println!, write_buffer.iter_mut |
| `src/main.rs#emulate_runtest` | `kernel/src/drivers/block/virtio_block.rs#read_block` | 同模块上下文对齐/差异观察 | Err, ExpandedArgs::from_slice, Ok, args.len, command.args.as_slice, name.is_empty ...(+8) | self.0
            .lock, self.0
            .lock()
            .read_block, self.0
            .lock()
            .read_block(block_id, buf)
            .expect |
| `src/virtio/pci.rs#submit_read` | `kernel/src/drivers/block/virtio_block.rs#write_block` | 同模块上下文对齐/差异观察 | Err, Ok, RequestHeader::read, addr_of!, addr_of_mut!, avail_idx.wrapping_add ...(+10) | self.0
            .lock, self.0
            .lock()
            .write_block, self.0
            .lock()
            .write_block(block_id, buf)
            .expect |
| `src/virtio/mmio.rs#submit_read` | `kernel/src/drivers/block/virtio_block.rs#new` | 同模块上下文对齐/差异观察 | Err, Ok, RequestHeader::read, addr_of!, addr_of_mut!, avail_idx.wrapping_add ...(+10) | Mutex::new, Self, VirtIOBlk::new, VirtIOBlk::new(&mut *(VIRTIO0 as *mut VirtIOHeader)).unwrap |

#### 网络协议栈
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `src/netperf.rs#render_netperf_script` | `libc/src/functional/inet_pton.c#main` | 同模块上下文对齐/差异观察 | crate::runner::render_basic_end, crate::runner::render_basic_start, sink.write_str, write_u32 | V4, V6, inet_ntop, inet_pton, strerror, t_error |
| `src/netperf.rs#write_u32` | `libc/src/functional/socket.c#main` | 同模块上下文对齐/差异观察 | core::str::from_utf8, core::str::from_utf8(&ch).unwrap_or, sink.write_str | TEST, TESTE, accept, bind, close, connect ...(+9) |

#### 文件系统/第三方库
仅目标项目中观察到该模块代表函数，参考项目没有同模块可对齐证据：
- 目标项目：`src/fs/ext4.rs#build_fixture`，调用摘要：MockBlockDevice::new, disk.add_dir_entry, disk.write, disk.write_u16, disk.write_u32, write_extent_inode
- 目标项目：`src/fs/ext4.rs#parse_directory_block`，调用摘要：FileName::from_slice, FileType::from_dir_entry, Ok, block
                .get, block
                .get(offset + 8..offset + 8 + name_len)
                .ok_or, block.len ...(+4)
- 目标项目：`src/fs/ext4.rs#read_regular_file_bounded`，调用摘要：Err, FixedList::<Extent, MAX_EXTENTS>::new, Ok, blocks_for_len, clock, extents.as_slice ...(+12)
- 目标项目：`src/fs/ext4.rs#collect_extents_for_blocks`，调用摘要：Err, Ok, inode.block.len, parse_extent_entries, parse_extent_header, read_u16 ...(+2)

#### 第三方库
仅参考项目中观察到该模块代表函数，目标项目没有同模块可对齐证据：
- 参考项目：`dependencies/core2/src/io/buffered.rs#write`，调用摘要：Ok, memchr::memrchr, self.buffer.capacity, self.buffer.flush_buf, self.buffer.write, self.buffer.write_to_buf ...(+3)
- 参考项目：`dependencies/core2/src/io/buffered.rs#flush_buf`，调用摘要：BufGuard::new, Err, Error::new, Ok, e.kind, guard.consume ...(+7)
- 参考项目：`dependencies/core2/src/io/traits.rs#read_to_end_with_reservation`，调用摘要：Err, Ok, assert!, buf.len, e.kind, g.buf.capacity ...(+7)
- 参考项目：`dependencies/bitmap-allocator/src/lib.rs#bitalloc_contiguous`，调用摘要：BitAlloc16::default, BitAlloc4K::default, assert_eq!, ba.dealloc, ba.insert, ba.remove ...(+2)

#### 通用模块/其他
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `src/signal.rs#set_action` | `kernel/src/signal/mod.rs#set_action` | 同板块同名 | catchable, self.disposition_ignores, sigmask | Some |
| `src/main.rs#clear` | `kernel/src/signal/shadow_bitset.rs#clear` | 同板块同名 | 内部闭环逻辑 | self.addr.add |
| `src/main.rs#write_str` | `kernel/src/arch/riscv/stdout.rs#write_str` | 同板块同名 | capture_user_output, crate::print!, value.as_bytes | Ok, putchar_raw, s.bytes |
| `src/busybox.rs#set` | `libc/src/functional/ipc_shm.c#set` | 同板块同名 | self.entries.iter_mut, self.entries.iter_mut().find | EQ, T, exit, ftok, getegid, geteuid ...(+9) |


#### 1. 宏观判定
- **定性结论**: 底层架构显著差异 / 独立开发 (相似度: 0.65%，共同 AST 指纹: 20；唯一 AST 指纹: 目标项目 1626，参考项目 1491；覆盖率: 目标项目 1.23%，参考项目 1.34%)

#### 2. 全模块覆盖矩阵解读

- **进程管理**
  - 双方是否都存在：是。目标项目 255 个函数，参考项目 60 个函数。
  - 是否存在共同 AST 指纹：是，仅 1 个共同指纹。
  - Target/Base 覆盖率说明：目标覆盖率 0.41%，参考覆盖率 1.82%。覆盖率极低，且唯一共同指纹为 `len`/`get_pid_num` 这类内部闭环的 trivial getter，不构成实质性结构重合。
  - 证据强度：弱。控方指控的“退出/克隆/信号/exec”四大控制流在 AST 层面无共同指纹支撑，调用图谱对象完全不同。

- **任务调度/异步运行时**
  - 双方是否都存在：是。目标项目 190 个函数，参考项目 160 个函数。
  - 是否存在共同 AST 指纹：否。
  - Target/Base 覆盖率说明：双方覆盖率均为 0.0%。目标项目使用协作式 `find_runnable_sibling` 等确定性调度，参考项目使用 `push_task_to_scheduler` 等抢占式内核调度，调度模型完全不同。
  - 证据强度：未形成。

- **内存管理**
  - 双方是否都存在：是。目标项目 291 个函数，参考项目 275 个函数。
  - 是否存在共同 AST 指纹：否。
  - Target/Base 覆盖率说明：双方覆盖率均为 0.0%。目标项目使用 `MockMemory`、`PROCESS_MEMORIES[slot].copy_from` 等用户态模拟内存快照；参考项目使用 `area.copy_to_new_area_with_data`、页表操作等真实内核内存管理。内存模型异构。
  - 证据强度：未形成。

- **中断/异常处理**
  - 双方是否都存在：是。目标项目 35 个函数，参考项目 4 个函数。
  - 是否存在共同 AST 指纹：否。
  - Target/Base 覆盖率说明：双方覆盖率均为 0.0%。目标项目为多架构（RISC-V64、LoongArch64）的模拟 trap frame 布局验证；参考项目为真实硬件 `stvec::write`、`sie::set_stimer` 等寄存器操作。抽象层次完全不同。
  - 证据强度：未形成。

- **系统调用**
  - 双方是否都存在：是。目标项目 143 个函数，参考项目 8 个函数。
  - 是否存在共同 AST 指纹：否。
  - Target/Base 覆盖率说明：双方覆盖率均为 0.0%。同名函数（如 `sys_linkat`、`sys_mmap`）源于 POSIX 标准接口名，但调用图谱完全不同：目标使用 `RuntimeNode`、`ctx.find_node`，参考使用 `get_current_task`、`resolve_path_from_fd`。
  - 证据强度：弱。同名不构成同构证据。

- **文件系统**
  - 双方是否都存在：是。目标项目 219 个函数，参考项目 593 个函数。
  - 是否存在共同 AST 指纹：是，仅 1 个共同指纹。
  - Target/Base 覆盖率说明：目标覆盖率 0.47%，参考覆盖率 0.19%。共同指纹为 `nofile_limit`/`len` 这类 trivial 函数。目标项目原创了 `fs/ext4.rs` 等 EXT4 解析模块，参考项目依赖 `rust-fatfs` 第三方库，文件系统实现路径完全不同。
  - 证据强度：弱。

- **设备驱动**
  - 双方是否都存在：是。目标项目 108 个函数，参考项目 15 个函数。
  - 是否存在共同 AST 指纹：否。
  - Target/Base 覆盖率说明：双方覆盖率均为 0.0%。目标项目实现了 virtio PCI/MMIO 的模拟层（`submit_read`），参考项目为真实 virtio 块设备驱动（`VirtIOBlk::new`）。驱动模型异构。
  - 证据强度：未形成。

- **网络协议栈**
  - 双方是否都存在：是。目标项目 2 个函数，参考项目 8 个函数。
  - 是否存在共同 AST 指纹：否。
  - Target/Base 覆盖率说明：双方覆盖率均为 0.0%。目标项目仅有 `netperf.rs` 中的脚本渲染函数，参考项目为 libc 的 `inet_pton`、`socket` 等测试用例，无内核网络栈实现对比。
  - 证据强度：未形成。

- **第三方库/公共依赖**
  - 双方是否都存在：否。目标项目有 `文件系统/第三方库` 模块（95 个函数），参考项目有 `第三方库` 模块（335 个函数）。
  - 是否存在共同 AST 指纹：否。
  - Target/Base 覆盖率说明：目标项目原创了 EXT4 解析等模块，参考项目依赖 `rust-fatfs`、`bitmap-allocator`、`core2` 等第三方库。双方第三方依赖无交集。
  - 证据强度：未形成。

#### 3. 底层 AST 关键证据指纹表

| 所属板块 | 目标项目路径 | 参考项目路径 | 证据类型 | 技术说明 |
| :--- | :--- | :--- | :--- | :--- |
| 进程管理 | `src/script_exec.rs#len` | `kernel/src/task/task.rs#get_pid_num` | 同板块同结构 | 唯一共同 AST 指纹，均为内部闭环的 trivial getter，不涉及任何控制流或算法逻辑。 |
| 任务调度/异步运行时 | `src/syscall/file.rs#sys_linkat` | `kernel/src/syscall/fs.rs#sys_linkat` | 同板块同名 | POSIX 标准接口名。目标调用 `RuntimeNode::hard_link`、`ctx.find_node`；参考调用 `get_current_task`、`resolve_path_from_fd`。实现路径完全不同。 |
| 任务调度/异步运行时 | `src/syscall/file.rs#sys_unlinkat` | `kernel/src/syscall/fs.rs#sys_unlinkat` | 同板块同名 | POSIX 标准接口名。目标调用 `ctx.mark_node_removed`、`is_direct_child`；参考调用 `try_remove_link`。实现路径完全不同。 |
| 内存管理 | `src/syscall/memory.rs#sys_mmap` | `kernel/src/syscall/process.rs#sys_mmap` | 同板块同名 | POSIX 标准接口名。目标调用 `ctx.read_file_data`、`align_up`；参考调用 `BackEndFile::new`、`SeekFrom::Start`。内存模型异构。 |
| 内存管理 | `src/vm.rs#map` | `kernel/src/memory/page_table.rs#map` | 同板块同名 | 通用动词名。目标调用 `alloc.alloc`、`leaf_pte`；参考调用 `println!`、`pte.set_all`。页表操作模型完全不同。 |
| 系统调用 | `src/syscall/mod.rs#sys_msgrcv` | `libc/src/regression/syscall-sign-extend.c#main` | 跨板块硬凑 | 目标为内核系统调用实现，参考为 libc 用户态测试用例。领域不同，不构成同板块证据。 |
| 文件系统 | `src/syscall/mod.rs#nofile_limit` | `rust-fatfs/src/dir.rs#len` | 同板块同结构 | 唯一共同 AST 指纹，均为内部闭环的 trivial getter。 |
| 设备驱动 | `src/virtio/pci.rs#submit_read` | `kernel/src/drivers/block/virtio_block.rs#write_block` | 同模块上下文对齐 | 目标为 virtio PCI 模拟层，参考为真实 virtio 块设备驱动。调用对象完全不同。 |
| 通用模块/其他 | `src/signal.rs#set_action` | `kernel/src/signal/mod.rs#set_action` | 同板块同名 | 通用动词名。目标调用 `catchable`、`sigmask`；参考调用 `Some`。实现完全不同。 |
| 通用模块/其他 | `src/main.rs#write_str` | `kernel/src/arch/riscv/stdout.rs#write_str` | 同板块同名 | Rust 标准 trait `fmt::Write` 的通用方法名。目标调用 `capture_user_output`，参考调用 `putchar_raw`。实现完全不同。 |

#### 4. 核心代码调用流对比剖析

* **【进程管理/进程退出与回收路径】**
* **底层技术析理**: 目标项目 `exit_current` 基于固定槽位表 `ProcessRuntime`，通过 `RuntimeProcessState::Zombie` 状态标记、`find_runnable_sibling` 协作式查找下一个可运行进程、`release_all_pipe_fds` 清理管道资源。参考项目 `sys_exit` 调用 `exit_current_task`，`waitpid` 通过 `task.inner.lock` 获取锁、`tcb_inner.children.iter().enumerate()` 遍历链表、`tcb_inner.children.remove` 移除子进程。两者在进程表示（固定槽位数组 vs Arc<Task> 链表）、同步机制（无锁协作式 vs 互斥锁）、资源回收粒度（显式管道清理 vs 通用信号发送）上完全不同。唯一共性“遍历→检查→移除”是 POSIX `waitpid` 的标准语义，不构成代码复用。
* **代码溯源链接**:
  - 目标项目: `src/process.rs#exit_current`
  - 参考项目: `kernel/src/syscall/process.rs#sys_exit`

* **【内存管理/页表映射路径】**
* **底层技术析理**: 目标项目 `vm.rs#map` 是用户态模拟的页表操作，调用 `alloc.alloc` 分配物理页帧、`core::ptr::read/write` 直接读写页表项、`leaf_pte` 判断叶节点。参考项目 `page_table.rs#map` 是真实内核页表操作，调用 `pte.is_valid` 检查有效性、`pte.set_all` 设置标志位、包含 `println!` 调试输出。两者虽然都处理页表映射，但目标项目是模拟器的扁平化页表操作，参考项目是真实硬件多级页表的标志位操作，内存抽象层次和实现路径完全不同。
* **代码溯源链接**:
  - 目标项目: `src/vm.rs#map`
  - 参考项目: `kernel/src/memory/page_table.rs#map`

* **【文件系统/链接操作路径】**
* **底层技术析理**: 目标项目 `sys_linkat` 基于内存中的 `RuntimeNode` 树，调用 `ctx.find_node` 查找节点、`RuntimeNode::hard_link` 创建硬链接、`ctx.add_runtime_node` 添加到运行时树。参考项目 `sys_linkat` 基于真实文件系统，调用 `get_current_task` 获取当前任务、`resolve_path_from_fd` 解析文件描述符路径、`try_add_link` 尝试添加链接。两者文件系统模型（内存运行时树 vs 真实文件系统抽象）、路径解析方式（直接节点查找 vs fd 解析）完全不同。
* **代码溯源链接**:
  - 目标项目: `src/syscall/file.rs#sys_linkat`
  - 参考项目: `kernel/src/syscall/fs.rs#sys_linkat`

#### 5. 单方模块与降权项

- **目标项目独有模块**：`文件系统/第三方库`（95 个函数），包含原创的 `fs/ext4.rs` EXT4 文件系统解析模块（`build_fixture`、`parse_directory_block`、`read_regular_file_bounded` 等）。参考项目无对应实现，证明目标项目存在独立开发的子系统。
- **参考项目独有模块**：`第三方库`（335 个函数），包含 `rust-fatfs`、`bitmap-allocator`、`core2` 等第三方依赖。目标项目无对应依赖，双方第三方库选型无交集。
- **降权项说明**：
  - **POSIX 标准接口名**：`sys_linkat`、`sys_unlinkat`、`sys_lseek`、`sys_fstat`、`sys_mmap`、`sys_clone`、`sys_kill` 等函数名来自 Linux/POSIX 系统调用规范，任何实现 Linux 兼容层的项目都会使用。同名不构成同构证据。
  - **Rust 标准 trait 方法名**：`write_str`（`fmt::Write`）、`map`/`unmap`（通用动词）、`clear`（通用动词）、`len`（通用 getter）、`set`（通用 setter）等属于 Rust 语言惯用法或标准库 trait 实现，跨项目出现是正常现象。
  - **libc 测试用例**：参考项目中 `libc/src/regression/`、`libc/src/functional/` 下的 C 语言测试用例属于 musl/libc 的公开 POSIX 符合性测试，与目标项目的 Rust 内核运行时实现处于完全不同的领域，不得作为同板块抄袭证据。
  - **跨板块配对**：控方将目标项目的 `sys_msgrcv`（系统调用实现）与参考项目的 `syscall-sign-extend.c#main`（libc 测试用例）配对，属于跨板块硬凑，不构成有效证据。

#### 6. 保守结论

基于客观 AST 指纹分析，目标项目 `[2026-T2026105749910169-XX]` 与参考项目 `[2022-Maturin-清华大学]` 的全局 AST 相似度仅为 0.65%，共同指纹仅 20 个，覆盖率不足 1.4%。进程管理模块作为控方指控的核心区域，仅存在 1 个 trivial getter 的共同指纹，覆盖率 0.41%。双方在架构定位上存在根本性差异：目标项目是用户态确定性测试运行时（`MockMemory`、`ProcessRuntime`、`SyscallContext`），参考项目是真实操作系统内核（`TaskControlBlock`、`CPU_CONTEXTS`、`memory_set`）。控方指控的“退出/克隆/信号/exec”四大控制流在 AST 层面无共同指纹支撑，调用图谱对象完全不同。同名函数源于 POSIX 标准接口名和 Rust 语言惯用法，不能作为代码复用证据。综合判定：双方为独立开发，不存在实质性代码复用或抄袭。

------------------------------

