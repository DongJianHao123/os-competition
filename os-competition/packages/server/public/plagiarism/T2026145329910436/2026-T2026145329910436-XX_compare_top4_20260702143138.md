# 🛡️ OS 内核赛自动化代码级架构审计报告

**审计时间**: `2026-07-02 14:31:38`
**参赛作品名称**: `2026-T2026145329910436-XX`
**年份**: `2026`
**学校**: `XX`
**队伍名称**: `T2026145329910436`
**比赛名字**: `OS-Competition`
**比赛子赛道**: `Kernel-Implementation`
**仓库地址**: `https://gitlab.eduxiji.net/educg-group-43501-3132633/T2026145329910436-1554`
**推测开发语言**: `Rust / C (由AST探针自动识别)`
**特征函数量**: `1294`

## ⚔️ 底层代码同源性对比 (AST Homology)
> 本部分由专门的代码审查智能体，对该项目与符合年份条件的历届最高相似度项目进行控制流与底层调用对比。新版报告会先给出确定性的全模块覆盖矩阵，再给出 Agent 的分模块解释，避免只围绕少量抽样函数展开。

### 🏆 Top 4 底层对标: vs `2025-undefined-清华大学`（清华大学 / undefined）

#### 对方作品信息

**对方年份**: `2025`
**对方学校**: `清华大学`
**对方队伍名称**: `undefined`
**对方比赛名字**: `OS-Competition`
**对方比赛子赛道**: `Kernel-Implementation`
**对方仓库地址**: `https://gitlab.eduxiji.net/educg-group-36002-2710490/T202510003995291-2331`

> 双向覆盖率：共同 AST 指纹: 146；唯一 AST 指纹: 目标项目 1117，参考项目 2820；覆盖率: 目标项目 13.07%，参考项目 5.18%

## 全模块对比总览（确定性证据）

> 总体覆盖率：共同 AST 指纹: 146；唯一 AST 指纹: 目标项目 1117，参考项目 2820；覆盖率: 目标项目 13.07%，参考项目 5.18%

### 1. 模块覆盖矩阵
| 模块 | 目标函数数 | 参考函数数 | 共同唯一 AST 指纹 | Target 覆盖 | Base 覆盖 | 状态 |
| :--- | ---: | ---: | ---: | ---: | ---: | :--- |
| 进程管理 | 46 | 135 | 3 | 7.14% | 2.48% | 双方存在且有结构重合 |
| 任务调度/异步运行时 | 205 | 415 | 2 | 1.08% | 0.67% | 双方存在且有结构重合 |
| 内存管理 | 185 | 450 | 12 | 6.94% | 3.14% | 双方存在且有结构重合 |
| 中断/异常处理 | 13 | 53 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |
| 系统调用 | 136 | 109 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |
| 文件系统 | 570 | 999 | 95 | 19.92% | 14.22% | 双方存在且有结构重合 |
| 设备驱动 | 0 | 133 | 0 | 0.0% | 0.0% | 仅参考项目体现 |
| 网络协议栈 | 0 | 65 | 0 | 0.0% | 0.0% | 仅参考项目体现 |
| 文件系统/第三方库 | 37 | 643 | 16 | 43.24% | 2.96% | 双方存在且有结构重合 |
| 网络协议栈/第三方库 | 0 | 130 | 0 | 0.0% | 0.0% | 仅参考项目体现 |
| 通用模块/其他 | 102 | 555 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |

### 2. 分模块证据清单

#### 进程管理
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `kernel/src/syscall/task/execve.rs#sys_execve` | `api/src/interface/task/execve.rs#sys_execve` | 同板块同名 | Arc::new, Default::default, Err, FD_TABLE.write, FS_CONTEXT.lock, FS_CONTEXT.lock().resolve ...(+42) | BUSYBOX.to_string, Some, abs_path.to_string, args[1..].iter, args[1..].iter().cloned, argv.clone ...(+10) |
| `kernel/src/task/ops.rs#do_exit` | `arceos/examples/shell/src/cmd.rs#do_exit` | 同板块同名 | FutexKey::new_current, SignalInfo::new_kernel, Some, axtask::yield_now, clear_child_tid.vm_write, clear_child_tid.vm_write(0).is_ok ...(+31) | println!, std::process::exit |
| `kernel/src/syscall/signal.rs#sys_rt_sigprocmask` | `api/src/imp/task/signal.rs#sys_rt_sigprocmask` | 同板块同名 | Err, Ok, check_sigset_size, curr.as_thread, current, debug! ...(+7) | Err, Ok, check_sigset_size, current_thread_data, current_thread_data()
        .signal
        .with_blocked_mut::<LinuxResult<_>>, oldset.nullable ...(+1) |
| `kernel/src/syscall/task/wait.rs#apply` | `api/src/imp/task/wait.rs#apply` | 同板块同名 | child.group, child.group().pgid, child.pid | child.get_group, child.get_group().get_pgid, child.get_pid |

#### 任务调度/异步运行时
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `kernel/src/syscall/sync/futex.rs#sys_futex` | `api/src/imp/task/futex.rs#sys_futex` | 同板块同名 | AxError::from, Err, FutexKey::new_current, Ok, Some, assert_unsigned ...(+18) | Err, Ok, axtask::yield_now, current_process_data, drop, futex_table
                .entry ...(+27) |
| `kernel/src/syscall/signal.rs#sys_kill` | `api/src/imp/task/signal.rs#sys_kill` | 同板块同名 | Ok, Some, current, current().as_thread, current().as_thread().proc_data.proc.group, current().as_thread().proc_data.proc.group().pgid ...(+9) | Err, Ok, current_process, current_process().get_group, debug!, get_all_processes ...(+8) |
| `kernel/src/syscall/resources.rs#sys_prlimit64` | `api/src/interface/task/resource.rs#sys_prlimit64` | 同板块同名 | Err, Ok, get_process_data, new_limit.nullable, new_limit.vm_read_uninit, new_limit.vm_read_uninit()?.assume_init ...(+4) | Ok, ResourceLimitType::try_from, ResourceLimitType::try_from(resource as u32).map_err, new_limit.get_as_ref, new_limit.is_null, old_limit.get ...(+4) |
| `kernel/src/syscall/task/clone.rs#sys_clone` | `api/src/interface/task/clone.rs#sys_clone` | 同板块同名 | CloneFlags::from_bits_truncate, Err, args.do_clone, clone_flags.contains | addr_child_tid.clone, addr_parent_tid.clone, sys_clone_ |

#### 内存管理
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `kernel/src/syscall/mod.rs#handle_syscall` | `src/syscall.rs#handle_syscall` | 同板块同名 | Err, LinuxError::ENOSYS.code, LinuxError::from, LinuxError::from(err).code, Ok, Sysno::new ...(+215) | Err, Sysno::new, err.code, info!, result.unwrap_or_else, set_trap_frame ...(+151) |
| `kernel/src/syscall/mm/mmap.rs#sys_mmap` | `api/src/imp/mm/mmap.rs#sys_mmap` | 同板块同名 | (addr + length).align_up, Arc::new, Backend::new_alloc, Backend::new_cow, Backend::new_file, Backend::new_linear ...(+50) | Err, File::from_fd, MmapFlags::from_bits_truncate, MmapProt::from_bits_truncate, Ok, PhysAddr::from ...(+34) |
| `kernel/src/mm/loader.rs#load_user_app` | `core/src/mm.rs#load_user_app` | 同板块同名 | "/bin/sh".to_owned, (ustack_top - user_sp_aligned).align_up_4k, Backend::new_alloc, ELF_LOADER.lock, ELF_LOADER.lock().load, Err ...(+43) | CStr::from_bytes_with_nul, CStr::from_bytes_with_nul(interp)
            .map_err, CStr::from_bytes_with_nul(interp)
            .map_err(|_| AxError::InvalidData)?
            .to_str, CStr::from_bytes_with_nul(interp)
            .map_err(|_| AxError::InvalidData)?
            .to_str()
            .map_err, CStr::from_bytes_with_nul(interp)
            .map_err(|_| AxError::InvalidData)?
            .to_str()
            .map_err(|_| AxError::InvalidData)?
            .to_string, ElfFile::new ...(+34) |
| `kernel/src/syscall/ipc/shm.rs#sys_shmat` | `api/src/interface/mm/shm.rs#sys_shmat` | 同板块同名 | Arc::new, Backend::new_shared, Ok, SHM_MANAGER.lock, SharedPages::new, ShmAtFlags::from_bits_truncate ...(+27) | Err, Ok, SHARED_MEMORY_MANAGER.get, SHARED_MEMORY_MANAGER.get(key).ok_or, ShmFlags::from_bits_truncate, Some ...(+16) |

#### 中断/异常处理
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `tools/cargo-axplat/template/src/irq.rs#set_enable` | `arceos/modules/axhal/src/platform/loongarch64_qemu_virt/irq.rs#set_enable` | 同板块同名 | todo! | ecfg::read, ecfg::read().lie, ecfg::set_lie |
| `kernel/src/pseudofs/dev/tty/terminal/termios.rs#default` | `arceos/ulib/axlibc/c/pow.c#exp_inline` | 同模块上下文对齐/差异观察 | ctl | __math_oflow, __math_uflow, asdouble, asuint64, converttoint, eval_as_double ...(+4) |
| `kernel/src/file/signalfd.rs#from_signal_info` | `arceos/modules/axhal/src/arch/aarch64/trap.rs#handle_sync_exception` | 同模块上下文对齐/差异观察 | sig_info.code, sig_info.errno, sig_info.signo | ESR_EL1.extract, crate::trap::handle_syscall, crate::trap::post_trap_callback, debug!, esr.read, esr.read_as_enum ...(+6) |
| `kernel/src/pseudofs/dev/tty/terminal/termios.rs#signo_for` | `arceos/modules/axhal/src/arch/x86_64/idt.rs#new` | 同模块上下文对齐/差异观察 | Some, self.special_char | InterruptDescriptorTable::new, core::mem::transmute, core::slice::from_raw_parts_mut, entries[i].set_handler_fn, opt.set_privilege_level |

#### 系统调用
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `kernel/src/syscall/net/opt.rs#sys_setsockopt` | `api/src/imp/net/socket.rs#sys_setsockopt` | 同板块同名 | Err, Ok, Socket::from_fd, call_dispatch!, debug!, size_of::<T> ...(+2) | Err, Ok, Socket::from_fd, Socket::from_fd(sockfd)?.setsockopt, core::slice::from_raw_parts, debug! ...(+1) |
| `kernel/src/syscall/time.rs#sys_times` | `api/src/imp/utils/time.rs#sys_times` | 同板块同名 | Ok, current, current().as_thread, current().as_thread().time.borrow, current().as_thread().time.borrow().output, monotonic_time_nanos ...(+4) | Ok, monotonic_time_nanos, nanos_to_ticks, time_stat_output, tms.get |
| `kernel/src/syscall/net/socket.rs#sys_connect` | `api/src/imp/net/socket.rs#sys_connect` | 同板块同名 | Ok, Socket::from_fd, Socket::from_fd(fd)?.connect, Socket::from_fd(fd)?.connect(addr).map_err, SocketAddrEx::read_from_user, debug! | Ok, Socket::from_fd, Socket::from_fd(socket_fd)?.connect, debug!, from_sockaddr |
| `kernel/src/syscall/time.rs#sys_clock_getres` | `api/src/imp/utils/time.rs#sys_clock_getres` | 同板块同名 | Ok, TimeValue::from_micros, res.nullable, res.vm_write, timespec::from_time_value, warn! | Err, Ok, nullable! |

#### 文件系统
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `kernel/src/pseudofs/dev/fb.rs#ioctl` | `api/src/core/fs/imp/dev/framebuffer.rs#ioctl` | 同板块同名 | (arg as *mut FixScreenInfo).vm_write, (arg as *mut VarScreenInfo).vm_write, Err, Ok, axdisplay::framebuffer_info | Default::default, Err, Ok, arg.into, framebuffer_flush, user_info.get_as_mut_ref |
| `kernel/src/pseudofs/dev/loop.rs#ioctl` | `api/src/core/fs/pseudo/file.rs#ioctl` | 同板块同名 | (arg as *const loop_info).vm_read_uninit, (arg as *const loop_info).vm_read_uninit()?.assume_init, (arg as *const u32).vm_read, (arg as *mut loop_info).vm_write, (arg as *mut u32).vm_write, (arg as *mut u64).vm_write ...(+22) | Err, warn! |
| `kernel/src/syscall/fs/io.rs#sys_splice` | `api/src/interface/fs/io.rs#sys_splice` | 同板块同名 | DummyFd::from_fd, DummyFd::from_fd(fd_in).is_ok, DummyFd::from_fd(fd_out).is_ok, Err, File::from_fd, Pipe::from_fd ...(+20) | Err, Ok, fd_lookup, file_like_as::<Pipe>, file_like_as::<Pipe>(in_file.clone()).is_none, file_like_as::<Pipe>(out_file.clone()).is_none ...(+6) |
| `kernel/src/pseudofs/dev/event.rs#read_at` | `arceos/modules/axfs-ng/src/fs/fat/file.rs#read_at` | 同板块同名 | Err, Ok, buf.chunks_exact_mut, buf.is_empty, buf.len, inner.has_event ...(+8) | Ok, SeekFrom::Start, file.read, file.read(buf).map_err, file.seek, file.seek(SeekFrom::Start(offset)).map_err ...(+2) |

#### 设备驱动
仅参考项目中观察到该模块代表函数，目标项目没有同模块可对齐证据：
- 参考项目：`arceos/tools/raspi4/chainloader/src/bsp/device_driver/bcm/bcm2xxx_pl011_uart.rs#init`，调用摘要：FBRD::BAUD_DIVFRAC.val, IBRD::BAUD_DIVINT.val, self.flush, self.registers
            .CR
            .write, self.registers
            .LCR_H
            .write, self.registers.CR.set ...(+3)
- 参考项目：`arceos/tools/bwbench_client/src/device.rs#new`，调用摘要：ETH_P_ALL.to_be, Err, Ok, dev.bind_interface, dev.interface_mtu, ifreq.ioctl ...(+5)
- 参考项目：`arceos/tools/bwbench_client/src/main.rs#transmit_benchmark`，调用摘要：Local::now, NetDevice::new, NetDevice::new(interface).unwrap, current_time.signed_duration_since, current_time.signed_duration_since(past_time).num_seconds, dev.send ...(+2)
- 参考项目：`arceos/tools/bwbench_client/src/device.rs#bind_interface`，调用摘要：ETH_P_ALL.to_be, Err, Ok, libc::bind, self.ifreq.ioctl, std::io::Error::last_os_error ...(+2)

#### 网络协议栈
仅参考项目中观察到该模块代表函数，目标项目没有同模块可对齐证据：
- 参考项目：`arceos/ulib/axlibc/c/network.c#inet_pton`，调用摘要：hexval, inet_pton, isdigit, memmove
- 参考项目：`api/src/imp/net/socket.rs#setsockopt`，调用摘要：Ok, SocketOptionLevel::try_from, error!, udpsocket.lock, unimplemented!, warn!
- 参考项目：`api/src/imp/net/socket.rs#sendto`，调用摘要：Err, IpAddr::V4, Ipv4Addr::new, SocketAddr::new, udpsocket
                    .bind, udpsocket
                    .bind(SocketAddr::new(IpAddr::V4(Ipv4Addr::new(127, 0, 0, 1)), 0))
                    .map_err ...(+3)
- 参考项目：`api/src/imp/net/socket.rs#shutdown`，调用摘要：Ok, tcpsocket.lock, tcpsocket.peer_addr, tcpsocket.shutdown, udpsocket.lock, udpsocket.peer_addr ...(+1)

#### 文件系统/第三方库
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `patches/axfs-ng/src/fs/ext4/inode.rs#create` | `arceos/modules/axfs-ng/src/fs/ext4/inode.rs#create` | 同板块同名 | DirEntry::new_dir, DirEntry::new_file, DirNode::new, Err, FileNode::new, Inode::new ...(+14) | DirEntry::new_dir, DirEntry::new_file, DirNode::new, Err, FileNode::new, Inode::new ...(+11) |
| `patches/axfs-ng/src/fs/ext4/inode.rs#metadata` | `arceos/modules/axfs-ng/src/fs/ext4/inode.rs#metadata` | 同板块同名同结构 | DeviceId::default, FileAttr::default, NodePermission::from_bits_truncate, Ok, into_vfs_type, self.fs
            .lock ...(+2) | DeviceId::default, FileAttr::default, NodePermission::from_bits_truncate, Ok, into_vfs_type, self.fs
            .lock ...(+2) |
| `patches/axfs-ng/src/fs/ext4/inode.rs#read_dir` | `arceos/modules/axfs-ng/src/fs/ext4/inode.rs#read_dir` | 同板块同名同结构 | Ok, core::str::from_utf8, core::str::from_utf8(entry.name())
                .map_err, core::str::from_utf8(entry.name())
                .map_err(|_| VfsError::InvalidData)?
                .to_owned, entry.ino, entry.inode_type ...(+10) | Ok, core::str::from_utf8, core::str::from_utf8(entry.name())
                .map_err, core::str::from_utf8(entry.name())
                .map_err(|_| VfsError::EINVAL)?
                .to_owned, entry.ino, entry.inode_type ...(+10) |
| `patches/axfs-ng/src/fs/ext4/inode.rs#update_metadata` | `arceos/modules/axfs-ng/src/fs/ext4/inode.rs#update_metadata` | 同板块同名 | Ok, fs.with_inode_ref, fs.with_inode_ref(self.ino, |inode| {
            if let Some(mode) = update.mode {
                inode.set_mode((inode.mode() & !0xfff) | (mode.bits() as u32));
            }
            if let Some((uid, gid)) = update.owner {
                inode.set_owner(uid as _, gid as _);
            }
            if let Some(atime) = update.atime {
                inode.set_atime(&atime);
            }
            if let Some(mtime) = update.mtime {
                inode.set_mtime(&mtime);
            }
            inode.update_ctime();
            Ok(())
        })
        .map_err, inode.mode, inode.set_atime, inode.set_mode ...(+5) | Ok, fs.with_inode_ref, fs.with_inode_ref(self.ino, |inode| {
            if let Some(mode) = update.mode {
                inode.set_mode((inode.mode() & !0xfff) | (mode.bits() as u32));
            }
            if let Some((uid, gid)) = update.owner {
                inode.set_owner(uid as _, gid as _);
            }
            if let Some(atime) = update.atime {
                inode.set_atime(&atime);
            }
            if let Some(mtime) = update.mtime {
                inode.set_mtime(&mtime);
            }
            Ok(())
        })
        .map_err, inode.mode, inode.set_atime, inode.set_mode ...(+4) |

#### 网络协议栈/第三方库
仅参考项目中观察到该模块代表函数，目标项目没有同模块可对齐证据：
- 参考项目：`arceos/modules/axnet/src/smoltcp_impl/tcp.rs#connect`，调用摘要：Err, Ok, Ok::<(IpEndpoint, IpEndpoint), AxError>, SOCKET_SET
                .with_socket_mut::<tcp::Socket, _, _>, SOCKET_SET.add, SocketSetWrapper::new_tcp_socket ...(+32)
- 参考项目：`arceos/modules/axnet/src/smoltcp_impl/dns.rs#query`，调用摘要：Err, Ok, SOCKET_SET
            .with_socket_mut::<dns::Socket, _, _>, SOCKET_SET
            .with_socket_mut::<dns::Socket, _, _>(handle, |socket| {
                socket.start_query(iface.lock().context(), name, query_type)
            })
            .map_err, SOCKET_SET.poll_interfaces, SOCKET_SET.with_socket_mut::<dns::Socket, _, _> ...(+12)
- 参考项目：`arceos/modules/axnet/src/smoltcp_impl/tcp.rs#recv_timeout`，调用摘要：Err, Ok, SOCKET_SET.with_socket_mut::<tcp::Socket, _, _>, ax_err!, ax_err_type!, current_ticks ...(+11)
- 参考项目：`arceos/modules/axnet/src/smoltcp_impl/bench.rs#bench_transmit_bandwidth`，调用摘要：(current_time - past_time).secs, AxNetTxToken::consume, InterfaceWrapper::current_time, info!, self.transmit, tx_buf[0..12].fill ...(+2)

#### 通用模块/其他
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `tools/axconfig-gen/src/output.rs#from_str` | `api/src/imp/sys.rs#from_str` | 同板块同名 | Err, Ok, s.into | data[..info.len()].copy_from_slice, info.as_bytes, info.len |
| `tools/cargo-axplat/template/src/mem.rs#phys_to_virt` | `arceos/modules/axhal/src/paging.rs#phys_to_virt` | 同板块同名 | todo! | phys_to_virt |
| `tools/cargo-axplat/template/src/console.rs#read_bytes` | `arceos/modules/axhal/src/platform/riscv64_qemu_virt/console.rs#read_bytes` | 同板块同名 | todo! | VirtAddr::from_mut_ptr_of, bytes.as_mut_ptr, bytes.len, bytes.len().min, sbi_rt::Physical::new, sbi_rt::console_read ...(+2) |
| `tools/cargo-axplat/template/src/time.rs#set_oneshot_timer` | `arceos/modules/axhal/src/platform/x86_pc/time.rs#set_oneshot_timer` | 同板块同名 | todo! | NANOS_TO_LAPIC_TICKS_RATIO.mul_trunc, apic_ticks.max, assert!, crate::time::monotonic_time_nanos, lapic.set_timer_initial, super::apic::local_apic |


好的，审计员。基于提供的证据底座、控辩双方主张及客观量化指标，现出具最终同源性定性仲裁报告。

### 1. 宏观判定
- **定性结论**: **独立开发，底层架构显著差异**
  客观 AST 相似度仅为 3.85%，远低于同源性阈值。目标项目与参考项目虽共享部分 POSIX/Linux 兼容层语义及上游 ArceOS 生态的公共依赖，但在核心数据结构、对象管理模型、同步策略及系统调用实现路径上表现出根本性的架构异构性。控方提出的“复用指控”主要基于函数同名、通用语言范式及领域必然收敛性，缺乏特异性 AST 结构指纹支撑，不予采纳。

### 2. 全模块覆盖矩阵解读
- **进程管理**: 双方均存在。共同 AST 指纹仅 3 个，目标覆盖率 7.14%。重合点均为极简 getter 或标准系统调用入口，属领域共性。证据强度：**弱**。
- **任务调度/异步运行时**: 双方均存在。共同 AST 指纹仅 2 个，目标覆盖率 1.08%。双方均依赖 `axtask` 库，但调度与等待逻辑的实现细节（如 `wait_queue` 操作）不同。证据强度：**弱**。
- **内存管理**: 双方均存在。共同 AST 指纹 12 个，目标覆盖率 6.94%。`sys_mmap` 等系统调用虽同名，但后端内存对象（`Backend`）及映射流程存在差异。证据强度：**弱**。
- **中断/异常处理**: 双方均存在。共同 AST 指纹为 0。目标项目大量使用 `todo!` 占位，与参考项目成熟实现无结构重合。证据强度：**未形成**。
- **系统调用**: 双方均存在。共同 AST 指纹为 0。尽管存在同名系统调用，但内部调用图谱完全不同，如 `sys_connect` 等。证据强度：**未形成**。
- **文件系统**: 双方均存在。共同 AST 指纹 95 个，目标覆盖率 19.92%，为所有模块中最高。重合主要集中在 `axfs-ng` 第三方库的 ext4 实现及伪文件系统框架中，属于公共依赖或领域标准范式。证据强度：**中**（主要来自第三方库）。
- **设备驱动**: 仅参考项目体现。目标项目无此模块。证据强度：**未形成**。
- **网络协议栈**: 仅参考项目体现。目标项目无此模块。证据强度：**未形成**。
- **第三方库/公共依赖**: 双方均存在。共同 AST 指纹 16 个，目标覆盖率 43.24%。高覆盖率明确指向双方共同依赖的 `axfs-ng` 等上游开源组件，是相似度的主要来源，不构成抄袭。证据强度：**强**（作为公共依赖的证明）。

### 3. 底层 AST 关键证据指纹表
| 所属板块 | 目标项目路径 | 参考项目路径 | 证据类型 | 技术说明 |
| :--- | :--- | :--- | :--- | :--- |
| 第三方库/公共依赖 | `patches/axfs-ng/src/fs/ext4/inode.rs#metadata` | `arceos/modules/axfs-ng/src/fs/ext4/inode.rs#metadata` | 同板块同名同结构 | 双方均直接使用或轻度封装上游 `axfs-ng` 库的 ext4 元数据读取逻辑，调用流完全一致。此为公共依赖，非抄袭。 |
| 第三方库/公共依赖 | `patches/axfs-ng/src/fs/ext4/inode.rs#read_dir` | `arceos/modules/axfs-ng/src/fs/ext4/inode.rs#read_dir` | 同板块同名同结构 | 同上，ext4 目录读取逻辑完全一致，源于公共依赖 `axfs-ng`。 |
| 进程管理 | `kernel/src/task/mod.rs#is_clone_child` | `core/src/process.rs#is_clone_child` | 同板块底层逻辑一致 | 双方均返回 `Some`，是极简的 `Option` 构造，属 Rust 语言通用范式，不具备特异性。 |
| 进程管理 | `kernel/src/syscall/task/wait.rs#apply` | `api/src/imp/task/wait.rs#apply` | 同板块同名 | 调用链 `child.group().pgid` vs `child.get_group().get_pgid()`，是 getter 命名风格差异，非结构复用。 |
| 进程管理 | `kernel/src/syscall/task/exit.rs#sys_exit` | `api/src/interface/task/exit.rs#sys_exit` | 同板块同名 | 均委托给内部实现，但目标项目 `do_exit` 与参考项目 `sys_exit_impl` 的内部调用图谱差异巨大。 |
| 内存管理 | `kernel/src/syscall/mm/mmap.rs#sys_mmap` | `api/src/imp/mm/mmap.rs#sys_mmap` | 同板块同名 | 同名系统调用，但目标项目使用 `Backend::new_*` 系列构造器，参考项目使用 `MmapFlags`，后端抽象不同。 |
| 文件系统 | `kernel/src/pseudofs/proc.rs#new_procfs` | `api/src/core/fs/imp/proc.rs#new_procfs` | 同板块同名 | 均使用 `”proc”.into()` 和 `*Fs::new_with` 模式，是 Rust 构造器惯用法与 procfs 标准命名的巧合。 |
| 任务调度 | `kernel/src/syscall/sync/futex.rs#sys_futex` | `api/src/imp/task/futex.rs#sys_futex` | 同板块同名 | 同名系统调用，但目标项目使用 `FutexKey::new_current`，参考项目使用 `futex_table.entry`，内部数据结构不同。 |

### 4. 核心代码调用流对比剖析
* **【进程管理/进程退出路径】**
* **底层技术析理**: 双方均将 `sys_exit` 系统调用路由至一个统一的内部函数，但该函数的实现揭示了完全不同的资源清理模型。目标项目的 `do_exit` 深度集成 `FutexKey` 健壮列表处理和 `SHM_MANAGER` 共享内存清理，而参考项目的 `sys_exit_impl` 则侧重于 `UserPtr` 用户态指针操作和 `close_all_file_like` 文件描述符回收。两者在内核对象管理上的关注点截然不同。
* **代码溯源链接**:
  - 目标项目: `kernel/src/task/ops.rs#do_exit`
  - 参考项目: `api/src/imp/task/exit.rs#sys_exit_impl`

* **【内存管理/内存映射】**
* **底层技术析理**: `sys_mmap` 的实现体现了内存后端抽象的差异。目标项目通过 `Backend::new_alloc`、`Backend::new_file` 等构造器创建不同类型的内存区域，而参考项目则通过 `MmapFlags` 和 `PhysAddr` 进行操作。目标项目的 `Backend` 枚举提供了更丰富的内存来源抽象，与参考项目的实现路径不同。
* **代码溯源链接**:
  - 目标项目: `kernel/src/syscall/mm/mmap.rs#sys_mmap`
  - 参考项目: `api/src/imp/mm/mmap.rs#sys_mmap`

* **【文件系统/伪文件系统根节点初始化】**
* **底层技术析理**: 两者在创建 procfs 根节点时都使用了 `”proc”.into()` 和 `new_with` 构造器，但这属于 Rust 类型转换惯用法和 proc 文件系统的标准命名。目标项目使用 `SimpleFs` 作为后端，而参考项目使用 `DynamicFs`，表明其内部目录树管理和节点操作采用了不同的抽象。
* **代码溯源链接**:
  - 目标项目: `kernel/src/pseudofs/proc.rs#new_procfs`
  - 参考项目: `api/src/core/fs/imp/proc.rs#new_procfs`

### 5. 单方模块与降权项
*   **单方模块**: **设备驱动**、**网络协议栈**及其**第三方库**仅存在于参考项目中。**pidfd 进程文件描述符**、**共享内存(SHM)管理器**的完整实现仅存在于目标项目中。这些单方存在的模块是独立开发的有力证明。
*   **降权项说明**:
    *   **第三方库/公共依赖**: `axfs-ng` (ext4 文件系统) 等模块的高相似度是双方共同依赖上游开源组件的结果，不构成抄袭。
    *   **通用宏/语言范式**: `Some`、`Ok`、`Err`、`debug!`、`assert!`、`.load()`、`.lock()` 等是 Rust 语言标准库或通用生态中的惯用法，不具备可保护性。
    *   **POSIX 语义收敛**: `sys_fork`、`sys_exit`、`sys_getpid` 等系统调用函数名、参数和基本行为由 Linux/POSIX 标准定义，任何兼容内核的实现都必然表现出高层逻辑的相似性。
    *   **跨板块/跨运行域证据**: 控方将目标项目内核代码与参考项目的用户态示例程序 (`arceos/examples/shell`) 或上游调度模块 (`axtask`) 进行比对，违反了同板块比对原则，相关指控无效。

### 6. 保守结论
综合全模块证据，目标项目 `[2026-T2026145329910436-XX]` 与参考项目 `[2025-undefined-清华大学]` 的客观 AST 相似度仅为 3.85%，远低于构成同源性或抄袭的阈值。观察到的少量相似性主要源于双方共同遵循的 POSIX/Linux 系统调用规范、Rust 语言通用范式以及对 `axfs-ng` 等上游开源组件的共同依赖。在进程管理、内存管理等核心子系统上，双方在数据结构设计、资源管理策略和底层调用流上表现出显著架构差异。目标项目还独立实现了 `pidfd`、共享内存管理等参考项目未包含的功能模块。因此，不存在足以认定代码抄袭或非独立开发的证据链，应判定为独立开发作品。

------------------------------

