# 🛡️ OS 内核赛自动化代码级架构审计报告

**审计时间**: `2026-07-02 14:31:38`
**参赛作品名称**: `2026-T2026145329910436-XX`
**年份**: `2026`
**学校**: `XX`
**队伍名称**: `T2026145329910436`
**比赛名字**: `OS-Competition`
**比赛子赛道**: `Kernel-Implementation`
**仓库地址**: `https://gitlab.eduxiji.net/educg-group-43501-3132633/T2026145329910436-1554`
**推测开发语言**: `Rust / C (由AST探针自动识别)`
**特征函数量**: `1294`

## ⚔️ 底层代码同源性对比 (AST Homology)
> 本部分由专门的代码审查智能体，对该项目与符合年份条件的历届最高相似度项目进行控制流与底层调用对比。新版报告会先给出确定性的全模块覆盖矩阵，再给出 Agent 的分模块解释，避免只围绕少量抽样函数展开。

### 🏆 Top 5 底层对标: vs `2025-九莲宝灯一向听-天津理工大学`（天津理工大学 / 九莲宝灯一向听）

#### 对方作品信息

**对方年份**: `2025`
**对方学校**: `天津理工大学`
**对方队伍名称**: `九莲宝灯一向听`
**对方比赛名字**: `OS-Competition`
**对方比赛子赛道**: `Kernel-Implementation`
**对方仓库地址**: `https://gitlab.eduxiji.net/educg-group-36002-2710490/T202510060995409-3585`

> 双向覆盖率：共同 AST 指纹: 63；唯一 AST 指纹: 目标项目 1117，参考项目 2610；覆盖率: 目标项目 5.64%，参考项目 2.41%

## 全模块对比总览（确定性证据）

> 总体覆盖率：共同 AST 指纹: 63；唯一 AST 指纹: 目标项目 1117，参考项目 2610；覆盖率: 目标项目 5.64%，参考项目 2.41%

### 1. 模块覆盖矩阵
| 模块 | 目标函数数 | 参考函数数 | 共同唯一 AST 指纹 | Target 覆盖 | Base 覆盖 | 状态 |
| :--- | ---: | ---: | ---: | ---: | ---: | :--- |
| 进程管理 | 46 | 79 | 3 | 7.14% | 4.11% | 双方存在且有结构重合 |
| 任务调度/异步运行时 | 205 | 385 | 6 | 3.24% | 2.18% | 双方存在且有结构重合 |
| 内存管理 | 185 | 301 | 11 | 6.36% | 4.04% | 双方存在且有结构重合 |
| 中断/异常处理 | 13 | 60 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |
| 系统调用 | 136 | 130 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |
| 文件系统 | 570 | 616 | 8 | 1.68% | 1.76% | 双方存在且有结构重合 |
| 设备驱动 | 0 | 156 | 0 | 0.0% | 0.0% | 仅参考项目体现 |
| 网络协议栈 | 0 | 61 | 0 | 0.0% | 0.0% | 仅参考项目体现 |
| 文件系统/第三方库 | 37 | 577 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |
| 网络协议栈/第三方库 | 0 | 130 | 0 | 0.0% | 0.0% | 仅参考项目体现 |
| 第三方库 | 0 | 209 | 0 | 0.0% | 0.0% | 仅参考项目体现 |
| 通用模块/其他 | 102 | 630 | 2 | 2.35% | 0.42% | 双方存在且有结构重合 |

### 2. 分模块证据清单

#### 进程管理
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `kernel/src/syscall/task/execve.rs#sys_execve` | `api/src/imp/task/execve.rs#sys_execve` | 同板块同名 | Arc::new, Default::default, Err, FD_TABLE.write, FS_CONTEXT.lock, FS_CONTEXT.lock().resolve ...(+42) | Err, Ok, arg.get_as_str, arg.get_as_str().map, args.to_vec, argv
        .get_as_null_terminated ...(+38) |
| `kernel/src/task/ops.rs#do_exit` | `api/src/imp/task/exit.rs#do_exit` | 同板块同名 | FutexKey::new_current, SignalInfo::new_kernel, Some, axtask::yield_now, clear_child_tid.vm_write, clear_child_tid.vm_write(0).is_ok ...(+31) | FD_TABLE.clear, SignalInfo::new, UserPtr::<Pid>::from, axtask::exit, axtask::yield_now, clear_child_tid.get_as_mut ...(+22) |
| `kernel/src/syscall/signal.rs#sys_rt_sigprocmask` | `api/src/imp/signal.rs#sys_rt_sigprocmask` | 同板块同名 | Err, Ok, check_sigset_size, curr.as_thread, current, debug! ...(+7) | Err, Ok, check_sigset_size, current, current()
        .task_ext, current()
        .task_ext()
        .thread_data ...(+2) |
| `kernel/src/syscall/ipc/shm.rs#clear_proc_shm` | `api/src/imp/ipc/shm.rs#clear_proc_shm` | 同板块同名 | self.get_inner_by_shmid, self.get_shmids_by_pid, self.remove_pid, self.remove_shmid, shm_inner.attach_count, shm_inner.detach_process ...(+1) | SHM_MANAGER.lock, shm_inner.attach_count, shm_inner.detach_process, shm_inner.lock, shm_manager.get_inner_by_shmid, shm_manager.get_inner_by_shmid(shmid).unwrap ...(+3) |

#### 任务调度/异步运行时
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `kernel/src/syscall/sync/futex.rs#sys_futex` | `api/src/imp/futex.rs#sys_futex` | 同板块同名 | AxError::from, Err, FutexKey::new_current, Ok, Some, assert_unsigned ...(+18) | Err, Ok, axtask::yield_now, curr.task_ext, curr.task_ext().process_data, current ...(+16) |
| `kernel/src/syscall/fs/stat.rs#sys_statx` | `api/src/imp/fs/stat.rs#sys_statx` | 同板块同名 | Ok, debug!, path.as_deref, path.nullable, path.nullable().map, path.nullable().map(vm_load_string).transpose ...(+4) | Err, Ok, debug!, f.stat, f.stat()?.into, get_file_like ...(+9) |
| `kernel/src/syscall/signal.rs#sys_kill` | `api/src/imp/signal.rs#sys_kill` | 同板块同名 | Ok, Some, current, current().as_thread, current().as_thread().proc_data.proc.group, current().as_thread().proc_data.proc.group().pgid ...(+9) | Err, Ok, curr.task_ext, curr.task_ext().thread.process, curr.task_ext().thread.process().group, current ...(+9) |
| `kernel/src/syscall/ipc/shm.rs#sys_shmdt` | `api/src/imp/ipc/shm.rs#sys_shmdt` | 同板块同名 | Ok, SHM_MANAGER.lock, VirtAddr::from, aspace.unmap, curr.as_thread, current ...(+14) | Ok, SHM_MANAGER.lock, VirtAddr::from, aspace.unmap, axhal::arch::flush_tlb, curr.task_ext ...(+17) |

#### 内存管理
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `kernel/src/syscall/mm/mmap.rs#sys_mmap` | `api/src/imp/mm/mmap.rs#sys_mmap` | 同板块同名 | (addr + length).align_up, Arc::new, Backend::new_alloc, Backend::new_cow, Backend::new_file, Backend::new_linear ...(+50) | Err, File::from_fd, MmapFlags::from_bits_truncate, MmapProt::from_bits_truncate, Ok, VirtAddr::from ...(+27) |
| `kernel/src/mm/loader.rs#load_user_app` | `core/src/mm.rs#load_user_app` | 同板块同名 | "/bin/sh".to_owned, (ustack_top - user_sp_aligned).align_up_4k, Backend::new_alloc, ELF_LOADER.lock, ELF_LOADER.lock().load, Err ...(+43) | CStr::from_bytes_with_nul, CStr::from_bytes_with_nul(interp)
                .map_err, CStr::from_bytes_with_nul(interp)
                .map_err(|_| AxError::InvalidData)?
                .to_str, CStr::from_bytes_with_nul(interp)
                .map_err(|_| AxError::InvalidData)?
                .to_str()
                .map_err, ElfFile::new, ElfFile::new(&file_data).map_err ...(+41) |
| `kernel/src/syscall/ipc/shm.rs#sys_shmat` | `api/src/imp/ipc/shm.rs#sys_shmat` | 同板块同名 | Arc::new, Backend::new_shared, Ok, SHM_MANAGER.lock, SharedPages::new, ShmAtFlags::from_bits_truncate ...(+27) | Err, Ok, SHM_MANAGER.lock, ShmAtFlags::from_bits_truncate, Some, VirtAddr::from ...(+28) |
| `kernel/src/mm/access.rs#check_null_terminated` | `api/src/ptr.rs#check_null_terminated` | 同板块同名 | Err, Layout::new::<T>, Layout::new::<T>().align, Ok, T::default, access_user_memory ...(+10) | Err, Layout::new::<T>, Layout::new::<T>().align, Ok, T::default, VirtAddrRange::from_start_size ...(+12) |

#### 中断/异常处理
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `tools/cargo-axplat/template/src/irq.rs#set_enable` | `arceos/modules/axhal/src/platform/loongarch64_qemu_virt/irq.rs#set_enable` | 同板块同名 | todo! | ecfg::read, ecfg::read().lie, ecfg::set_lie |
| `kernel/src/pseudofs/dev/tty/terminal/termios.rs#default` | `arceos/ulib/axlibc/c/pow.c#exp_inline` | 同模块上下文对齐/差异观察 | ctl | __math_oflow, __math_uflow, asdouble, asuint64, converttoint, eval_as_double ...(+4) |
| `kernel/src/file/signalfd.rs#from_signal_info` | `arceos/modules/axhal/src/arch/aarch64/trap.rs#handle_sync_exception` | 同模块上下文对齐/差异观察 | sig_info.code, sig_info.errno, sig_info.signo | ESR_EL1.extract, crate::trap::handle_syscall, crate::trap::post_trap_callback, debug!, esr.read, esr.read_as_enum ...(+6) |
| `kernel/src/pseudofs/dev/tty/terminal/termios.rs#signo_for` | `arceos/modules/axhal/src/arch/x86_64/idt.rs#new` | 同模块上下文对齐/差异观察 | Some, self.special_char | InterruptDescriptorTable::new, core::mem::transmute, core::slice::from_raw_parts_mut, entries[i].set_handler_fn, opt.set_privilege_level |

#### 系统调用
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `kernel/src/syscall/io_mpx/epoll.rs#sys_epoll_ctl` | `arceos/api/arceos_posix_api/src/imp/io_mpx/epoll.rs#sys_epoll_ctl` | 同板块同名 | Epoll::from_fd, EpollFlags::from_bits, EpollFlags::from_bits(event.events & !events.bits()).ok_or, Err, IoEvents::from_bits_truncate, Ok ...(+7) | debug!, syscall_body! |
| `kernel/src/syscall/ipc/shm.rs#sys_shmctl` | `api/src/imp/ipc/shm.rs#sys_shmctl` | 同板块同名 | Err, Ok, SHM_MANAGER.lock, buf.get_as_mut, monotonic_time_nanos, nullable! ...(+3) | Err, Ok, SHM_MANAGER.lock, buf.get_as_mut, monotonic_time_nanos, nullable! ...(+3) |
| `kernel/src/syscall/time.rs#sys_times` | `api/src/imp/time.rs#sys_times` | 同板块同名 | Ok, current, current().as_thread, current().as_thread().time.borrow, current().as_thread().time.borrow().output, monotonic_time_nanos ...(+4) | Ok, monotonic_time_nanos, nanos_to_ticks, time_stat_output, tms.get_as_mut |
| `kernel/src/syscall/net/socket.rs#sys_connect` | `arceos/api/arceos_posix_api/src/imp/net.rs#sys_connect` | 同板块同名 | Ok, Socket::from_fd, Socket::from_fd(fd)?.connect, Socket::from_fd(fd)?.connect(addr).map_err, SocketAddrEx::read_from_user, debug! | debug!, syscall_body! |

#### 文件系统
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `kernel/src/syscall/fs/fd_ops.rs#sys_fcntl` | `api/src/imp/fs/fd_ops.rs#sys_fcntl` | 同板块同名 | FD_TABLE
                .read, FD_TABLE
                .read()
                .get, FD_TABLE
                .read()
                .get(fd as _)
                .ok_or, FD_TABLE
                .write, FD_TABLE
                .write()
                .get_mut, FD_TABLE
                .write()
                .get_mut(fd as _)
                .ok_or ...(+15) | Ok, debug!, dup_fd, get_file_like, get_file_like(fd)?.set_nonblocking, warn! |
| `kernel/src/pseudofs/dev/event.rs#read_at` | `arceos/modules/axfs/src/readonly_ramfs.rs#read_at` | 同板块同名 | Err, Ok, buf.chunks_exact_mut, buf.is_empty, buf.len, inner.has_event ...(+8) | Ok, buf.len, buf[..src.len()].copy_from_slice, self.content.len, self.content.len().min, src.len |
| `kernel/src/syscall/fs/ctl.rs#sys_utimensat` | `api/src/imp/fs/ctl.rs#sys_utimensat` | 同板块同名 | Ok, Some, atime.is_none, mtime.is_none, path.is_null, time.try_into_time_value ...(+8) | Ok, Some, atime.into, atime.is_none, file.atime.lock, file.downcast_ref::<File> ...(+16) |
| `kernel/src/pseudofs/dir.rs#read_dir` | `arceos/modules/axfs/src/readonly_ramfs.rs#read_dir` | 同板块同名 | Ok, [DOT, DOTDOT]
            .into_iter, [DOT, DOTDOT]
            .into_iter()
            .map, [DOT, DOTDOT]
            .into_iter()
            .map(Cow::Borrowed)
            .chain, children.enumerate, children.enumerate().skip ...(+12) | Ok, VfsDirEntry::new, children.iter, children.iter().skip, children.next, dirents.iter_mut ...(+7) |

#### 设备驱动
仅参考项目中观察到该模块代表函数，目标项目没有同模块可对齐证据：
- 参考项目：`arceos/modules/axdriver/src/dwmac.rs#set_clocks_uboot`，调用摘要：<Self as DwmacHal>::mmio_phys_to_virt, <Self as DwmacHal>::mmio_phys_to_virt(pac::AONCRG::ptr() as usize, 0x1000).as_ptr, <Self as DwmacHal>::mmio_phys_to_virt(pac::SYSCRG::ptr() as usize, 0x1000).as_ptr, DwmacHalImpl::wait_until, Self::check_diff, aoncrg.clk_ahb_gmac5 ...(+67)
- 参考项目：`arceos/modules/axdriver/src/dwmac.rs#print_preserved_status`，调用摘要：<Self as DwmacHal>::mmio_phys_to_virt, <Self as DwmacHal>::mmio_phys_to_virt(pac::AONCRG::ptr() as usize, 0x1000).as_ptr, <Self as DwmacHal>::mmio_phys_to_virt(pac::SYSCRG::ptr() as usize, 0x1000).as_ptr, log::info!, pac::AONCRG::ptr, pac::SYSCRG::ptr ...(+24)
- 参考项目：`arceos/modules/axdriver/src/dwmac.rs#print_clocks`，调用摘要：<Self as DwmacHal>::mmio_phys_to_virt, <Self as DwmacHal>::mmio_phys_to_virt(pac::AONCRG::ptr() as usize, 0x1000).as_ptr, <Self as DwmacHal>::mmio_phys_to_virt(pac::SYSCRG::ptr() as usize, 0x1000).as_ptr, aoncrg.clk_axi_gmac5, aoncrg.clk_axi_gmac5().read, aoncrg.clk_axi_gmac5().read().clk_icg ...(+27)
- 参考项目：`arceos/tools/raspi4/chainloader/src/bsp/device_driver/bcm/bcm2xxx_pl011_uart.rs#init`，调用摘要：FBRD::BAUD_DIVFRAC.val, IBRD::BAUD_DIVINT.val, self.flush, self.registers
            .CR
            .write, self.registers
            .LCR_H
            .write, self.registers.CR.set ...(+3)

#### 网络协议栈
仅参考项目中观察到该模块代表函数，目标项目没有同模块可对齐证据：
- 参考项目：`arceos/ulib/axlibc/c/network.c#inet_pton`，调用摘要：hexval, inet_pton, isdigit, memmove
- 参考项目：`arceos/api/arceos_posix_api/src/imp/net.rs#shutdown`，调用摘要：Ok, tcpsocket.lock, tcpsocket.peer_addr, tcpsocket.shutdown, udpsocket.lock, udpsocket.peer_addr ...(+1)
- 参考项目：`arceos/ulib/axlibc/c/socket.c#accept4`，调用摘要：accept, fcntl
- 参考项目：`arceos/api/arceos_posix_api/src/imp/net.rs#recvfrom`，调用摘要：Ok, Some, tcpsocket.lock, tcpsocket.lock().recv, tcpsocket.lock().recv(buf).map, udpsocket
                .lock ...(+2)

#### 文件系统/第三方库
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `patches/axfs-ng/src/fs/ext4/inode.rs#create` | `arceos/modules/axfs/src/fs/lwext4_rust.rs#create` | 同板块同名 | DirEntry::new_dir, DirEntry::new_file, DirNode::new, Err, FileNode::new, Inode::new ...(+14) | Ok, e.try_into, e.try_into().unwrap, file.check_inode_exist, file.dir_mk, file.dir_mk(fpath)
                    .map ...(+12) |
| `patches/axfs-ng/src/fs/ext4/inode.rs#read_dir` | `arceos/modules/axfs/src/fs/lwext4_rust.rs#read_dir` | 同板块同名 | Ok, core::str::from_utf8, core::str::from_utf8(entry.name())
                .map_err, core::str::from_utf8(entry.name())
                .map_err(|_| VfsError::InvalidData)?
                .to_owned, entry.ino, entry.inode_type ...(+10) | Ok, VfsDirEntry::new, core::str::from_utf8, core::str::from_utf8(iname.unwrap()).unwrap, dirents.iter_mut, dirents.iter_mut().enumerate ...(+13) |
| `patches/axfs-ng/src/fs/ext4/inode.rs#rename` | `arceos/modules/axfs/src/fs/lwext4_rust.rs#rename` | 同板块同名 | dst_dir.downcast, dst_dir.downcast().map_err, fs.rename, fs.rename(self.ino, src_name, dst_dir.ino, dst_name)
            .map_err, self.fs.lock | e.try_into, e.try_into().unwrap, file.file_rename, file.file_rename(&src_path, dst_path)
            .map, file.file_rename(&src_path, dst_path)
            .map(|_v| ())
            .map_err, file.get_path ...(+2) |
| `patches/axfs-ng/src/fs/ext4/inode.rs#read_at` | `arceos/modules/axfs/src/fs/lwext4_rust.rs#read_at` | 同板块同名 | self.fs
            .lock, self.fs
            .lock()
            .read_at, self.fs
            .lock()
            .read_at(self.ino, buf, offset)
            .map_err | <i32 as TryInto<AxError>>::try_into, <i32 as TryInto<AxError>>::try_into(e).unwrap, e.try_into, e.try_into().unwrap, file.file_close, file.file_open ...(+9) |

#### 网络协议栈/第三方库
仅参考项目中观察到该模块代表函数，目标项目没有同模块可对齐证据：
- 参考项目：`arceos/modules/axnet/src/smoltcp_impl/tcp.rs#connect`，调用摘要：Err, Ok, Ok::<(IpEndpoint, IpEndpoint), AxError>, SOCKET_SET
                .with_socket_mut::<tcp::Socket, _, _>, SOCKET_SET.add, SocketSetWrapper::new_tcp_socket ...(+32)
- 参考项目：`arceos/modules/axnet/src/smoltcp_impl/dns.rs#query`，调用摘要：Err, Ok, SOCKET_SET
            .with_socket_mut::<dns::Socket, _, _>, SOCKET_SET
            .with_socket_mut::<dns::Socket, _, _>(handle, |socket| {
                socket.start_query(iface.lock().context(), name, query_type)
            })
            .map_err, SOCKET_SET.poll_interfaces, SOCKET_SET.with_socket_mut::<dns::Socket, _, _> ...(+12)
- 参考项目：`arceos/modules/axnet/src/smoltcp_impl/tcp.rs#recv_timeout`，调用摘要：Err, Ok, SOCKET_SET.with_socket_mut::<tcp::Socket, _, _>, ax_err!, ax_err_type!, current_ticks ...(+11)
- 参考项目：`arceos/modules/axnet/src/smoltcp_impl/mod.rs#init`，调用摘要：Config::new, ETH0.init_once, EthernetAddress, GATEWAY.parse, GATEWAY.parse().expect, IP.parse ...(+22)

#### 第三方库
仅参考项目中观察到该模块代表函数，目标项目没有同模块可对齐证据：
- 参考项目：`crates/axdriver_net/src/dwmac/mod.rs#init_phy`，调用摘要：Err, H::wait_until, Ok, Yt8531cPhy::<H>::new, core::time::Duration::from_millis, log::error! ...(+18)
- 参考项目：`crates/axdriver_net/src/dwmac/mod.rs#init`，调用摘要：AtomicBool::new, DescriptorRing::<RX_DESC_COUNT, H>::new, DescriptorRing::<TX_DESC_COUNT, H>::new, H::configure_platform, Ok, log::error! ...(+20)
- 参考项目：`crates/visionfive2-sd/src/lib.rs#send_cmd`，调用摘要：RawInterrupt::from, RawInterruptStatusReg::from, S::sleep_ms_until, Some, arg.into, assert! ...(+22)
- 参考项目：`crates/visionfive2-sd/src/lib.rs#init_sdcard`，调用摘要：CardTypeReg::from, CardTypeReg::from(0).with_card_width4_1, CmdArg::new, CmdReg::from, ControlReg::from, RawInterruptStatusReg::from ...(+20)

#### 通用模块/其他
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `kernel/src/time.rs#from_time_value` | `api/src/time.rs#from_time_value` | 同板块同名同结构 | tv.as_secs, tv.subsec_micros | tv.as_secs, tv.subsec_micros |
| `kernel/src/time.rs#from_time_value` | `api/src/time.rs#from_time_value` | 同板块同名同结构 | tv.as_secs, tv.subsec_micros | tv.as_secs, tv.subsec_micros |
| `kernel/src/time.rs#from_time_value` | `api/src/time.rs#from_time_value` | 同板块同名同结构 | tv.as_secs, tv.subsec_micros | tv.as_secs, tv.subsec_micros |
| `kernel/src/time.rs#from_time_value` | `api/src/time.rs#from_time_value` | 同板块同名同结构 | tv.as_secs, tv.subsec_nanos | tv.as_secs, tv.subsec_nanos |


### 🛡️ 审计裁决：最终同源性定性仲裁

#### 1. 宏观判定
- **定性结论**: **独立开发 / 底层架构显著差异**
- **核心依据**: 客观 AST 相似度仅为 **1.72%**。在总计 3790 个唯一 AST 指纹中，双方仅共享 63 个。目标项目对参考项目的覆盖率仅为 5.64%，参考项目对目标项目的覆盖率仅为 2.41%。此量级的重合度在操作系统内核这一高度规范化的领域中，属于由公共领域知识、POSIX/Linux 标准语义及 Rust 语言惯用法所导致的**领域共性噪声**，远未达到认定代码抄袭或系统性复用所需的结构一致性阈值。

#### 2. 全模块覆盖矩阵解读

| 模块 | 双方存在 | 共同 AST 指纹 | Target 覆盖 | Base 覆盖 | 证据强度 | 解读 |
| :--- | :---: | :---: | :---: | :---: | :---: | :--- |
| **进程管理** | 是 | 3 | 7.14% | 4.11% | **弱** | 重合点均为 POSIX/Linux 标准语义下的极简封装（如 `sys_fork` 调用 `sys_clone`）或原子操作（`set_clear_child_tid`），不构成独创性表达。 |
| **任务调度/异步运行时** | 是 | 6 | 3.24% | 2.18% | **弱** | 双方均实现调度功能，但目标项目采用基于 `async/await` 的协作式调度模型，参考项目采用传统的 `run_queue` 抢占式模型，架构根本不同。重合指纹多为 `debug!` 等通用宏。 |
| **内存管理** | 是 | 11 | 6.36% | 4.04% | **弱** | 重合点集中在 `sys_mmap`、`check_null_terminated` 等标准系统调用或通用内存检查函数。在核心的内存加载器（`load_user_app`）上，双方展现出完全不同的文件解析与段映射策略。 |
| **中断/异常处理** | 是 | 0 | 0.0% | 0.0% | **未形成** | 双方虽均有此模块，但未观察到任何共同 AST 指纹。目标项目的中断处理与伪文件系统深度耦合，参考项目则按平台（aarch64, x86_64）独立实现，架构截然不同。 |
| **系统调用** | 是 | 0 | 0.0% | 0.0% | **未形成** | 双方均实现系统调用分发，但未观察到共同 AST 指纹。目标项目使用 `FD_TABLE.scope` 等特有抽象，参考项目大量使用 `syscall_body!` 宏，分发与参数解析模型完全不同。 |
| **文件系统** | 是 | 8 | 1.68% | 1.76% | **弱** | 重合点多为 `read_dir`、`read_at` 等通用 VFS 接口的标准实现模式。目标项目采用 `pseudofs` 架构，参考项目采用 `readonly_ramfs`，文件系统架构根本不同。 |
| **设备驱动** | 否 | 0 | 0.0% | 0.0% | **未形成** | 仅参考项目包含此模块（如 `dwmac` 网卡驱动），目标项目无对应实现，无法形成同板块比对。 |
| **网络协议栈** | 否 | 0 | 0.0% | 0.0% | **未形成** | 仅参考项目包含此模块（如 `smoltcp` 集成），目标项目无对应实现，无法形成同板块比对。 |
| **第三方库/公共依赖** | 部分 | 0 | 0.0% | 0.0% | **未形成** | 参考项目包含大量第三方库（如 `lwext4_rust`），目标项目虽有少量文件系统第三方库，但未观察到共同指纹，表明双方依赖或封装方式不同。 |

#### 3. 底层 AST 关键证据指纹表
> 表格按模块优先级排序，仅列出双方均存在且可对齐的模块。第三方库证据后置。

| 所属板块 | 目标项目路径 | 参考项目路径 | 证据类型 | 技术说明 |
| :--- | :--- | :--- | :--- | :--- |
| 进程管理 | `kernel/src/task/mod.rs#set_clear_child_tid` | `core/src/task.rs#set_clear_child_tid` | 同板块底层逻辑一致 | 均为 Linux `set_tid_address` 机制的原子存储操作，是标准语义下的必然实现。 |
| 进程管理 | `kernel/src/task/mod.rs#clear_child_tid` | `core/src/task.rs#clear_child_tid` | 同板块底层逻辑一致 | 与上条配套的原子读取操作，用于 `futex` 唤醒，属公共领域知识。 |
| 进程管理 | `kernel/src/task/mod.rs#is_clone_child` | `core/src/task.rs#is_clone_child` | 同板块底层逻辑一致 | 极简 getter 函数，AST 退化为 `Some` 变体，无法作为抄袭证据。 |
| 进程管理 | `kernel/src/syscall/ipc/shm.rs#attach_process` | `api/src/imp/ipc/shm.rs#attach_process` | 同板块同名 | POSIX 共享内存挂载的标准三步流程（断言、时间戳、插入范围），属语义强制。 |
| 进程管理 | `kernel/src/syscall/ipc/shm.rs#detach_process` | `api/src/imp/ipc/shm.rs#detach_process` | 同板块同名 | 与挂载对称的卸载标准流程，属语义强制。 |
| 进程管理 | `kernel/src/syscall/task/wait.rs#apply` | `api/src/imp/task/wait.rs#apply` | 同板块同名 | `wait` 系统调用过滤子进程的标准属性访问链路，属语义强制。 |
| 进程管理 | `kernel/src/task/ops.rs#get_process_group` | `core/src/task.rs#get_process_group` | 同板块同名 | 对全局 `RwLock<HashMap>` 的标准查找与错误处理模式，属 Rust 惯用法。 |
| 进程管理 | `kernel/src/task/ops.rs#processes` | `core/src/task.rs#processes` | 同板块同名 | 对全局进程表的标准遍历与收集模式，属 Rust 惯用法。 |
| 进程管理 | `kernel/src/syscall/task/clone.rs#sys_fork` | `api/src/imp/task/clone.rs#sys_fork` | 同板块同名 | Linux `fork` 对 `clone` 的封装，是 POSIX 规范的强制要求。 |
| 内存管理 | `kernel/src/syscall/mm/mmap.rs#sys_mmap` | `api/src/imp/mm/mmap.rs#sys_mmap` | 同板块同名 | 标准 `mmap` 系统调用实现，但目标项目使用 `Backend::new_*` 系列构造函数，参考项目使用 `MmapFlags`，内部策略不同。 |
| 内存管理 | `kernel/src/mm/access.rs#check_null_terminated` | `api/src/ptr.rs#check_null_terminated` | 同板块同名 | 通用的用户态字符串安全检查函数，属任何内核都会实现的公共逻辑。 |
| 文件系统 | `kernel/src/pseudofs/dir.rs#read_dir` | `arceos/modules/axfs/src/readonly_ramfs.rs#read_dir` | 同板块同名 | 通用 VFS `read_dir` 接口实现，均需处理 `.` 和 `..` 条目并遍历子项，属标准模式。 |
| 文件系统/第三方库 | `patches/axfs-ng/src/fs/ext4/inode.rs#read_at` | `arceos/modules/axfs/src/fs/lwext4_rust.rs#read_at` | 同板块同名 | 对底层 C 库（可能是同一 `lwext4` 库）的 Rust 封装调用，属第三方库绑定的共性。 |

#### 4. 核心代码调用流对比剖析
* **【进程管理 / `do_exit` 核心退出路径】**
* **底层技术析理**: 两者均实现了进程退出的核心逻辑，但**同步策略与资源清理模型截然不同**。目标项目采用基于 `async` 的协作式调度，在退出路径中显式调用 `axtask::yield_now` 主动让出 CPU，并集成 `FutexKey::new_current` 处理 robust list  futex。参考项目则调用 `axtask::exit` 直接终止任务，其资源清理（`FD_TABLE.clear`）更为集中。目标项目的清理逻辑分散于 `SHM_MANAGER.lock().clear_proc_shm` 等独立子系统中，展现了不同的模块化设计思路。
* **代码溯源链接**:
  - 目标项目: `kernel/src/task/ops.rs#do_exit`
  - 参考项目: `api/src/imp/task/exit.rs#do_exit`

* **【内存管理 / `load_user_app` 程序加载器】**
* **底层技术析理**: 两者均负责加载 ELF 可执行文件，但**文件解析与内存映射策略存在根本差异**。目标项目使用独立的 `ELF_LOADER` 锁保护加载过程，并通过 `Backend::new_alloc` 等后端抽象来管理内存。参考项目则直接使用 `ElfFile::new` 解析文件，并显式处理动态链接器（interp）路径。目标项目的加载流程与文件系统后端（`Backend`）耦合更深，而参考项目的流程更侧重于 ELF 文件格式本身的解析。
* **代码溯源链接**:
  - 目标项目: `kernel/src/mm/loader.rs#load_user_app`
  - 参考项目: `core/src/mm.rs#load_user_app`

* **【系统调用 / `sys_rt_sigprocmask` 信号处理】**
* **底层技术析理**: 两者均实现信号掩码操作，但**任务扩展模型与数据访问路径完全不同**。目标项目通过 `curr.as_thread().proc_data` 路径直接访问进程数据，而参考项目通过 `current().task_ext().thread_data().signal` 路径，使用了一个显式的 `task_ext` 扩展层。这揭示了两者在任务控制块（TCB）和进程控制块（PCB）的数据组织上采用了完全不同的结构设计。
* **代码溯源链接**:
  - 目标项目: `kernel/src/syscall/signal.rs#sys_rt_sigprocmask`
  - 参考项目: `api/src/imp/signal.rs#sys_rt_sigprocmask`

#### 5. 单方模块与降权项
- **单方模块**: **设备驱动**、**网络协议栈**及大量**第三方库**仅存在于参考项目中。目标项目未实现这些模块，因此无法形成同板块比对，任何跨模块的相似性指控均不成立。
- **降权项**: 控方指控中的 `sys_execve`、`sys_rt_sigprocmask`、`clear_proc_shm` 等函数，虽为同板块同名，但其 AST 调用图谱存在显著差异，已被控方自行降级为“弱信号”。本庭认定这些差异恰恰是独立架构设计的直接证据。
- **跨板块硬凑**: 控方证据中混入了大量与进程管理无关的函数，如任务调度原语（`wait_queue.rs`）、文件系统挂载（`mounts.rs#procfs`）、裸机固件（`chainloader`）及用户态测试程序（`signal.c`）。这些证据操作的对象与目标项目的进程管理函数完全不同，属于**跨板块硬凑**，依法予以排除，不作为同板块主证据。

#### 6. 保守结论
综合客观 AST 数据与深度架构剖析，本庭裁定目标项目 `[2026-T2026145329910436-XX]` 与参考项目 `[2025-九莲宝灯一向听-天津理工大学]` 为**独立开发**。1.72% 的极低相似度源于操作系统内核的公共领域知识、POSIX/Linux 标准语义及 Rust 语言惯用法，属于正常的领域共性。双方在任务调度模型（异步协作式 vs. 传统抢占式）、内存管理后端抽象、系统调用分发机制及文件系统架构等核心子系统上存在根本性差异。控方基于碎片化 AST 调用图谱提出的“复用指控”，因无法区分标准实现与独创表达，且包含大量跨板块硬凑证据，**不予采信**。

------------------------------

