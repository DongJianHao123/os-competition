# 🛡️ OS 内核赛自动化代码级架构审计报告

**审计时间**: `2026-07-02 14:04:59`
**参赛作品名称**: `2026-T2026106479910700-XX`
**年份**: `2026`
**学校**: `XX`
**队伍名称**: `T2026106479910700`
**比赛名字**: `OS-Competition`
**比赛子赛道**: `Kernel-Implementation`
**仓库地址**: `https://gitlab.eduxiji.net/educg-group-43501-3132633/T2026106479910700-2564`
**推测开发语言**: `Rust / C (由AST探针自动识别)`
**特征函数量**: `1216`

## ⚔️ 底层代码同源性对比 (AST Homology)
> 本部分由专门的代码审查智能体，对该项目与符合年份条件的历届最高相似度项目进行控制流与底层调用对比。新版报告会先给出确定性的全模块覆盖矩阵，再给出 Agent 的分模块解释，避免只围绕少量抽样函数展开。

### 🏆 Top 1 底层对标: vs `2025-智核速启队-武汉大学`（武汉大学 / 智核速启队）

#### 对方作品信息

**对方年份**: `2025`
**对方学校**: `武汉大学`
**对方队伍名称**: `智核速启队`
**对方比赛名字**: `OS-Competition`
**对方比赛子赛道**: `Kernel-Implementation`
**对方仓库地址**: `https://gitlab.eduxiji.net/educg-group-36002-2710490/T202510486995194-334`

> 双向覆盖率：共同 AST 指纹: 1031；唯一 AST 指纹: 目标项目 1043，参考项目 1038；覆盖率: 目标项目 98.85%，参考项目 99.33%

## 全模块对比总览（确定性证据）

> 总体覆盖率：共同 AST 指纹: 1031；唯一 AST 指纹: 目标项目 1043，参考项目 1038；覆盖率: 目标项目 98.85%，参考项目 99.33%

### 1. 模块覆盖矩阵
| 模块 | 目标函数数 | 参考函数数 | 共同唯一 AST 指纹 | Target 覆盖 | Base 覆盖 | 状态 |
| :--- | ---: | ---: | ---: | ---: | ---: | :--- |
| 进程管理 | 56 | 56 | 55 | 100.0% | 100.0% | 双方存在且有结构重合 |
| 任务调度/异步运行时 | 77 | 76 | 72 | 97.3% | 98.63% | 双方存在且有结构重合 |
| 内存管理 | 242 | 240 | 228 | 97.85% | 98.28% | 双方存在且有结构重合 |
| 中断/异常处理 | 26 | 26 | 23 | 100.0% | 100.0% | 双方存在且有结构重合 |
| 系统调用 | 51 | 51 | 40 | 97.56% | 97.56% | 双方存在且有结构重合 |
| 文件系统 | 103 | 103 | 76 | 97.44% | 97.44% | 双方存在且有结构重合 |
| 设备驱动 | 33 | 33 | 29 | 100.0% | 100.0% | 双方存在且有结构重合 |
| 网络协议栈 | 1 | 1 | 1 | 100.0% | 100.0% | 双方存在且有结构重合 |
| 文件系统/第三方库 | 523 | 523 | 442 | 100.0% | 100.0% | 双方存在且有结构重合 |
| 通用模块/其他 | 104 | 96 | 86 | 96.63% | 100.0% | 双方存在且有结构重合 |

### 2. 分模块证据清单

#### 进程管理
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `kernel/process.c#scheduler` | `kernel/process.c#scheduler` | 同板块同名同结构 | acquire, copycontext, copytrapframe, futex_clear, hsai_swtch, intr_on ...(+9) | acquire, copycontext, copytrapframe, futex_clear, hsai_swtch, intr_on ...(+9) |
| `kernel/procfs.c#generate_proc_status_content` | `kernel/procfs.c#generate_proc_status_content` | 同板块同名同结构 | acquire, getproc, release, snprintf | acquire, getproc, release, snprintf |
| `kernel/futex.c#futex_wait` | `kernel/futex.c#futex_wait` | 同板块同名同结构 | DEBUG_LOG_LEVEL, acquire, myproc, panic, release, sched ...(+1) | DEBUG_LOG_LEVEL, acquire, myproc, panic, release, sched ...(+1) |
| `kernel/futex.c#futex_wait_bitset` | `kernel/futex.c#futex_wait_bitset` | 同板块同名同结构 | DEBUG_LOG_LEVEL, acquire, myproc, panic, release, sched | DEBUG_LOG_LEVEL, acquire, myproc, panic, release, sched |

#### 任务调度/异步运行时
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `hsai/print.c#vsnprintf` | `hsai/print.c#vsnprintf` | 同板块同名同结构 | format_int, format_uint, va_arg | format_int, format_uint, va_arg |
| `kernel/signal.c#handle_signal` | `kernel/signal.c#handle_signal` | 同板块同名同结构 | DEBUG_LOG_LEVEL, futex_wake | DEBUG_LOG_LEVEL, futex_wake |
| `user/riscv/user.c#test_shm` | `user/riscv/user.c#test_shm` | 同板块同名同结构 | exit, fork, getpid, printf, sleep, strcpy ...(+7) | exit, fork, getpid, printf, sleep, strcpy ...(+7) |
| `hsai/hsai_trap.c#kerneltrap` | `hsai/hsai_trap.c#kerneltrap` | 同板块同名同结构 | LOG_LEVEL, assert, devintr, exit, intr_get, kill ...(+15) | LOG_LEVEL, assert, devintr, exit, intr_get, kill ...(+15) |

#### 内存管理
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `kernel/syscall.c#syscall` | `kernel/syscall.c#syscall` | 同板块同名同结构 | DEBUG_LOG_LEVEL, LOG_LEVEL, get_syscall_name, hsai_get_arg, myproc, sleep ...(+136) | DEBUG_LOG_LEVEL, LOG_LEVEL, get_syscall_name, hsai_get_arg, myproc, sleep ...(+136) |
| `kernel/exec.c#exec` | `kernel/exec.c#exec` | 同板块同名 | DEBUG_LOG_LEVEL, LOG_LEVEL, PGROUNDDOWN, PGROUNDUP, acquire, alloc_aux ...(+42) | DEBUG_LOG_LEVEL, LOG_LEVEL, PGROUNDDOWN, PGROUNDUP, acquire, alloc_aux ...(+42) |
| `kernel/fs/fs.c#dir_init` | `kernel/fs/fs.c#dir_init` | 同板块同名同结构 | create_file, ext4_fsymlink, free_inode, namei, snprintf, vfs_ext4_mkdir ...(+2) | create_file, ext4_fsymlink, free_inode, namei, snprintf, vfs_ext4_mkdir ...(+2) |
| `hsai/hsai_trap.c#usertrap` | `hsai/hsai_trap.c#usertrap` | 同板块同名同结构 | LOG_LEVEL, check_and_handle_signals, devintr, exit, handle_exception, hsai_usertrapret ...(+21) | LOG_LEVEL, check_and_handle_signals, devintr, exit, handle_exception, hsai_usertrapret ...(+21) |

#### 中断/异常处理
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `kernel/driver/riscv/virt.c#virtio_disk_intr` | `kernel/driver/riscv/virt.c#virtio_disk_intr` | 同板块同名同结构 | R, __sync_synchronize, acquire, panic, printf, release ...(+1) | R, __sync_synchronize, acquire, panic, printf, release ...(+1) |
| `user/loongarch/user.c#test_pselect6_signal` | `user/loongarch/user.c#test_pselect6_signal` | 同板块同名同结构 | exit, fork, getpid, getppid, kill, printf ...(+4) | exit, fork, getpid, getppid, kill, printf ...(+4) |
| `user/riscv/user.c#test_pselect6_signal` | `user/riscv/user.c#test_pselect6_signal` | 同板块同名同结构 | exit, fork, getpid, getppid, kill, printf ...(+4) | exit, fork, getpid, getppid, kill, printf ...(+4) |
| `hsai/hsai_trap.c#hsai_trap_init` | `hsai/hsai_trap.c#hsai_trap_init` | 同板块同名同结构 | timer_init, w_csr_ecfg, w_csr_eentry, w_csr_euen, w_csr_merrentry, w_csr_tlbrentry ...(+1) | timer_init, w_csr_ecfg, w_csr_eentry, w_csr_euen, w_csr_merrentry, w_csr_tlbrentry ...(+1) |

#### 系统调用
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `user/loongarch/user.c#test_iozone` | `user/loongarch/user.c#test_iozone` | 同板块同名同结构 | exit, fork, printf, sys_chdir, sys_execve, waitpid | exit, fork, printf, sys_chdir, sys_execve, waitpid |
| `user/riscv/user.c#test_iozone` | `user/riscv/user.c#test_iozone` | 同板块同名同结构 | exit, fork, printf, sys_chdir, sys_execve, waitpid | exit, fork, printf, sys_chdir, sys_execve, waitpid |
| `user/loongarch/user.c#test_busybox` | `user/loongarch/user.c#test_busybox` | 同板块同名同结构 | exit, fork, printf, sys_chdir, sys_execve, waitpid | exit, fork, printf, sys_chdir, sys_execve, waitpid |
| `user/riscv/user.c#test_busybox` | `user/riscv/user.c#test_busybox` | 同板块同名同结构 | exit, fork, printf, sys_chdir, sys_execve, waitpid | exit, fork, printf, sys_chdir, sys_execve, waitpid |

#### 文件系统
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `kernel/fs/inode.c#cal_absolute_path` | `kernel/fs/inode.c#cal_absolute_path` | 同板块同名同结构 | memmove, strcat, strcpy, strlen | memmove, strcat, strcpy, strlen |
| `kernel/fs/qsort.c#_quicksort` | `kernel/fs/qsort.c#_quicksort` | 同板块同名同结构 | (*cmp), POP, PUSH, SWAP, min | (*cmp), POP, PUSH, SWAP, min |
| `user/loongarch/user.c#test_final` | `user/loongarch/user.c#test_final` | 同板块同名同结构 | exit, fork, printf, sys_execve, waitpid | exit, fork, printf, sys_execve, waitpid |
| `user/riscv/user.c#test_final` | `user/riscv/user.c#test_final` | 同板块同名同结构 | exit, fork, printf, sys_execve, waitpid | exit, fork, printf, sys_execve, waitpid |

#### 设备驱动
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `kernel/console.c#chardev_init` | `kernel/console.c#chardev_init` | 同板块同名同结构 | initlock, uart_init | initlock, uart_init |
| `hal/loongarch/uart.c#uart_init` | `hal/loongarch/uart.c#uart_init` | 同板块同名同结构 | _write_reg, initlock | _write_reg, initlock |
| `hal/riscv/uart.c#uart_init` | `hal/riscv/uart.c#uart_init` | 同板块同名同结构 | WriteReg, initlock | WriteReg, initlock |
| `kernel/driver/loongarch/virtio_pci.c#virtio_pci_set_queue_addr2` | `kernel/driver/loongarch/virtio_pci.c#virtio_pci_set_queue_addr2` | 同板块同名同结构 | PCI_REG16, PCI_REG64, dsb | PCI_REG16, PCI_REG64, dsb |

#### 网络协议栈
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `kernel/socket.c#sock_bind` | `kernel/socket.c#sock_bind` | 同板块同名同结构 | DEBUG_LOG_LEVEL, memmove | DEBUG_LOG_LEVEL, memmove |

#### 文件系统/第三方库
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `kernel/fs/ext4_balloc.c#ext4_balloc_alloc_block` | `kernel/fs/ext4_balloc.c#ext4_balloc_alloc_block` | 同板块同名同结构 | ext4_balloc_get_bgid_of_block, ext4_balloc_get_block_of_bgid, ext4_balloc_set_bitmap_csum, ext4_balloc_verify_bitmap_csum, ext4_bg_get_block_bitmap, ext4_bg_get_free_blocks_count ...(+19) | ext4_balloc_get_bgid_of_block, ext4_balloc_get_block_of_bgid, ext4_balloc_set_bitmap_csum, ext4_balloc_verify_bitmap_csum, ext4_bg_get_block_bitmap, ext4_bg_get_free_blocks_count ...(+19) |
| `kernel/fs/ext4.c#ext4_dir_rm` | `kernel/fs/ext4.c#ext4_dir_rm` | 同板块同名同结构 | EXT4_MP_LOCK, EXT4_MP_UNLOCK, ext4_block_cache_write_back, ext4_dir_en_get_inode, ext4_dir_iterator_fini, ext4_dir_iterator_init ...(+19) | EXT4_MP_LOCK, EXT4_MP_UNLOCK, ext4_block_cache_write_back, ext4_dir_en_get_inode, ext4_dir_iterator_fini, ext4_dir_iterator_init ...(+19) |
| `kernel/fs/ext4.c#ext4_generic_open2` | `kernel/fs/ext4.c#ext4_generic_open2` | 同板块同名同结构 | ext4_dir_destroy_result, ext4_dir_en_get_inode, ext4_dir_en_get_inode_type, ext4_dir_find_entry, ext4_fs_alloc_inode, ext4_fs_correspond_inode_mode ...(+16) | ext4_dir_destroy_result, ext4_dir_en_get_inode, ext4_dir_en_get_inode_type, ext4_dir_find_entry, ext4_fs_alloc_inode, ext4_fs_correspond_inode_mode ...(+16) |
| `kernel/fs/ext4_journal.c#jbd_journal_prepare` | `kernel/fs/ext4_journal.c#jbd_journal_prepare` | 同板块同名同结构 | EXT4_BLOCK_ZERO, RB_FIND, TAILQ_FOREACH_REVERSE_SAFE, TAILQ_FOREACH_SAFE, TAILQ_REMOVE, ext4_bcache_clear_dirty ...(+17) | EXT4_BLOCK_ZERO, RB_FIND, TAILQ_FOREACH_REVERSE_SAFE, TAILQ_FOREACH_SAFE, TAILQ_REMOVE, ext4_bcache_clear_dirty ...(+17) |

#### 通用模块/其他
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `hsai/print.c#printf` | `hsai/print.c#printf` | 同板块同名同结构 | acquire, consputc, printint, printptr, release, va_arg ...(+2) | acquire, consputc, printint, printptr, release, va_arg ...(+2) |
| `kernel/string.c#strtok` | `kernel/string.c#strtok` | 同板块同名同结构 | 内部闭环逻辑 | 内部闭环逻辑 |
| `kernel/pmem.c#buddy_safe_check` | `kernel/pmem.c#buddy_safe_check` | 同板块同名同结构 | list_begin, list_end, list_entry, list_next, printf | list_begin, list_end, list_entry, list_next, printf |
| `kernel/futex.c#futex_wake_bitset` | `kernel/futex.c#futex_wake_bitset` | 同板块同名同结构 | DEBUG_LOG_LEVEL, acquire, release | DEBUG_LOG_LEVEL, acquire, release |


#### 1. 宏观判定
- **定性结论**: 架构高度同构 (相似度: 98.19%，共同 AST 指纹: 1031；唯一 AST 指纹: 目标项目 1043，参考项目 1038；覆盖率: 目标项目 98.85%，参考项目 99.33%)

#### 2. 全模块覆盖矩阵解读
- **进程管理**: 双方均存在该模块。共同 AST 指纹 55，目标覆盖率 100%，参考覆盖率 100%。该模块函数名、调用图谱高度重合，但受限于 xv6 等公开教学基线及 Linux ABI 规范，证据强度为**中**。
- **任务调度/异步运行时**: 双方均存在该模块。共同 AST 指纹 72，目标覆盖率 97.3%，参考覆盖率 98.63%。存在少量独有指纹，表明双方在调度细节上存在独立实现空间，证据强度为**中**。
- **内存管理**: 双方均存在该模块。共同 AST 指纹 228，目标覆盖率 97.85%，参考覆盖率 98.28%。内存分配器、页表操作等核心逻辑高度相似，但双方均存在独有指纹，证据强度为**中**。
- **中断/异常处理**: 双方均存在该模块。共同 AST 指纹 23，目标覆盖率 100%，参考覆盖率 100%。中断向量表、trap 入口/出口等由硬件架构强制规范，证据强度为**弱**。
- **系统调用**: 双方均存在该模块。共同 AST 指纹 40，目标覆盖率 97.56%，参考覆盖率 97.56%。系统调用表及处理函数受限于 POSIX 标准，证据强度为**弱**。
- **文件系统**: 双方均存在该模块。共同 AST 指纹 76，目标覆盖率 97.44%，参考覆盖率 97.44%。VFS 接口与 ext4 等具体实现高度重合，但 ext4 部分属于第三方库，证据强度为**中**。
- **设备驱动**: 双方均存在该模块。共同 AST 指纹 29，目标覆盖率 100%，参考覆盖率 100%。virtio 等设备驱动由硬件规范与厂商驱动模板决定，证据强度为**弱**。
- **网络协议栈**: 双方均存在该模块。共同 AST 指纹 1，目标覆盖率 100%，参考覆盖率 100%。模块极小，仅包含 socket 绑定等基础操作，证据强度为**弱**。
- **第三方库/公共依赖**: 双方均存在该模块。共同 AST 指纹 442，目标覆盖率 100%，参考覆盖率 100%。此模块函数数量庞大且覆盖率 100%，强烈表明双方引用了完全相同的第三方开源库（如 lwext4），这是导致整体 AST 相似度极高的主要原因，证据强度为**强（作为公共依赖）**。

#### 3. 底层 AST 关键证据指纹表
| 所属板块 | 目标项目路径 | 参考项目路径 | 证据类型 | 技术说明 |
| :--- | :--- | :--- | :--- | :--- |
| 进程管理 | `kernel/process.c#scheduler` | `kernel/process.c#scheduler` | 调用流同构 | 调度器核心循环中，`futex_clear` 与 `hsai_swtch` 的调用顺序及上下文完全一致，`hsai_swtch` 为项目特有的硬件抽象层切换函数。 |
| 进程管理 | `kernel/process.c#sched` | `kernel/process.c#sched` | 调用流同构 | 调度前置检查逻辑中，`copytrapframe`、`holding`、`hsai_swtch` 的调用序列与锁检查模式完全一致。 |
| 进程管理 | `kernel/procfs.c#check_proc_path` | `kernel/procfs.c#check_proc_path` | 调用流同构 | `/proc` 路径路由器，其内部调用的 8 个 `is_proc_...` 校验函数命名与分发逻辑完全一致，构成一个高度特化的状态机。 |
| 进程管理 | `kernel/futex.c#futex_wait` | `kernel/futex.c#futex_wait` | 调用流同构 | futex 等待路径中，`timer_get_ntime` 的调用位置与 `DEBUG_LOG_LEVEL` 的埋点模式完全一致。 |
| 进程管理 | `kernel/process.c#tgkill` | `kernel/process.c#tgkill` | 调用流同构 | 线程组信号发送函数中，`list_begin/end/entry/next` 链表遍历宏与 `acquire/release` 锁的配对使用模式完全一致。 |
| 内存管理 | `kernel/exec.c#exec` | `kernel/exec.c#exec` | 调用流同构 | `exec` 系统调用实现中，`PGROUNDDOWN/UP`、`alloc_aux` 等内存操作与 ELF 加载辅助函数的调用图谱高度重合。 |
| 内存管理 | `hsai/hsai_trap.c#usertrap` | `hsai/hsai_trap.c#usertrap` | 调用流同构 | 用户态 trap 处理中，`check_and_handle_signals`、`handle_exception` 等信号/异常分发逻辑的调用链高度一致。 |
| 文件系统 | `kernel/fs/inode.c#cal_absolute_path` | `kernel/fs/inode.c#cal_absolute_path` | 调用流同构 | 路径规范化函数中，`memmove`、`strcat`、`strcpy`、`strlen` 的组合使用模式完全一致。 |
| 第三方库/公共依赖 | `kernel/fs/ext4_balloc.c#ext4_balloc_alloc_block` | `kernel/fs/ext4_balloc.c#ext4_balloc_alloc_block` | 公共库同源 | ext4 块分配器，其内部调用的 `ext4_balloc_get_bgid_of_block`、`ext4_bg_get_free_blocks_count` 等 20 余个函数完全一致，指向同一开源库。 |
| 第三方库/公共依赖 | `kernel/fs/ext4_journal.c#jbd_journal_prepare` | `kernel/fs/ext4_journal.c#jbd_journal_prepare` | 公共库同源 | ext4 日志准备事务，其内部 `RB_FIND`、`TAILQ_FOREACH_REVERSE_SAFE` 等复杂宏和数据结构的操作序列完全一致。 |

#### 4. 核心代码调用流对比剖析
* **【进程管理/调度路径】**
* **底层技术析理**: 双方在 `scheduler` 函数的实现中，均遵循“遍历进程表 -> 获取进程锁 -> 检查进程状态 -> 执行上下文切换”的流程。关键在于，上下文切换均通过调用非标准的 `hsai_swtch` 函数完成，并在切换前显式调用 `futex_clear` 清理 futex 状态。这种将 futex 清理与硬件抽象层切换函数耦合在调度主循环特定位置的模式，构成了超越公开教学基线（如 xv6）的强同源指纹。
* **代码溯源链接**:
  - 目标项目: `kernel/process.c#scheduler`
  - 参考项目: `kernel/process.c#scheduler`

* **【进程管理/procfs 路径解析】**
* **底层技术析理**: 双方在实现 `/proc` 伪文件系统的路径解析时，均采用了相同的路由分发模式。`check_proc_path` 函数作为总入口，通过一系列 `is_proc_...` 函数（如 `is_proc_pid_status`、`is_proc_pid_task_dir`）判断具体路径。每个 `is_proc_...` 函数内部均使用 `atoi`、`strchr`、`strncmp`、`strncpy` 的组合进行字符串解析，并调用 `myproc` 获取当前进程信息。这种特定的函数拆分方式和字符串解析模板，是独立于 Linux ABI 规范的具体实现选择。
* **代码溯源链接**:
  - 目标项目: `kernel/procfs.c#check_proc_path`
  - 参考项目: `kernel/procfs.c#check_proc_path`

* **【内存管理/用户态 Trap 处理】**
* **底层技术析理**: 在用户态 trap 处理函数 `usertrap` 中，双方的处理流程高度一致：从 `devintr` 处理设备中断，到 `handle_exception` 处理异常，再到 `check_and_handle_signals` 处理挂起的信号，最后通过 `hsai_usertrapret` 返回。这种将信号检查与异常/中断处理交织在 trap 返回路径特定位置的模式，是操作系统内核设计中的一个具体实现决策。
* **代码溯源链接**:
  - 目标项目: `hsai/hsai_trap.c#usertrap`
  - 参考项目: `hsai/hsai_trap.c#usertrap`

#### 5. 单方模块与降权项
- **第三方库/公共依赖**: `文件系统/第三方库` 模块包含 523 个函数，双方覆盖率均为 100%，共同指纹 442 个。这明确指向双方引用了相同的 ext4 或其他第三方开源库。此部分的高相似度是整体 AST 覆盖率超过 98% 的主要贡献者，不能作为抄袭证据，应作为公共依赖降权处理。
- **网络协议栈**: 双方均仅有 1 个函数，且为 `sock_bind` 这类基础操作。模块规模极小，其相似性无法构成系统性抄袭的证据，证据强度降为弱。
- **通用模块/其他**: 目标项目有 104 个函数，参考项目有 96 个，目标覆盖率 96.63%。函数数量的差异和未达到 100% 的覆盖率表明此模块存在独立开发或差异化实现的空间，削弱了“完全抄袭”的指控。
- **证据抽取失败**: 控方证据底座出现 `401 Unauthorized` 错误，表明部分证据生成过程不完整，其可靠性存疑，应作为降权因素考虑。

#### 6. 保守结论
本次审计发现，目标项目与参考项目在 AST 层面存在极高相似度（98.19%），主要源于双方引用了完全相同的第三方开源库（如 ext4 文件系统实现），这部分贡献了最大的代码重合体量。在排除第三方库后，核心内核模块（如进程管理、内存管理）仍存在高度结构同构，具体表现为调度器、procfs 路径解析、trap 处理等关键路径上的调用流和函数拆分模式一致。然而，鉴于操作系统内核开发受限于公开教学基线（xv6）、硬件架构规范（RISC-V/LoongArch）及 POSIX/Linux ABI 标准，且双方均存在一定数量的独有 AST 指纹，当前证据无法排除双方基于共同规范和公共基线独立开发并产生结构性收敛的可能性。因此，整体风险等级为**高**，但尚不能唯一确定为代码直接复制，建议结合源码级 diff、注释、变量命名及开发历史进行最终裁决。

------------------------------

