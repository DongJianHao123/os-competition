# 🛡️ OS 内核赛自动化代码级架构审计报告

**审计时间**: `2026-07-02 14:04:59`
**参赛作品名称**: `2026-T2026106479910700-XX`
**年份**: `2026`
**学校**: `XX`
**队伍名称**: `T2026106479910700`
**比赛名字**: `OS-Competition`
**比赛子赛道**: `Kernel-Implementation`
**仓库地址**: `https://gitlab.eduxiji.net/educg-group-43501-3132633/T2026106479910700-2564`
**推测开发语言**: `Rust / C (由AST探针自动识别)`
**特征函数量**: `1216`

## ⚔️ 底层代码同源性对比 (AST Homology)
> 本部分由专门的代码审查智能体，对该项目与符合年份条件的历届最高相似度项目进行控制流与底层调用对比。新版报告会先给出确定性的全模块覆盖矩阵，再给出 Agent 的分模块解释，避免只围绕少量抽样函数展开。

### 🏆 Top 2 底层对标: vs `2024-undefinedType-南京航空航天大学`（南京航空航天大学 / undefinedType）

#### 对方作品信息

**对方年份**: `2024`
**对方学校**: `南京航空航天大学`
**对方队伍名称**: `undefinedType`
**对方比赛名字**: `OS-Competition`
**对方比赛子赛道**: `Kernel-Implementation`
**对方仓库地址**: `https://gitlab.eduxiji.net/educg-group-26010-2376550/T202410287992755-2176`

> 双向覆盖率：共同 AST 指纹: 369；唯一 AST 指纹: 目标项目 1043，参考项目 398；覆盖率: 目标项目 35.38%，参考项目 92.71%

## 全模块对比总览（确定性证据）

> 总体覆盖率：共同 AST 指纹: 369；唯一 AST 指纹: 目标项目 1043，参考项目 398；覆盖率: 目标项目 35.38%，参考项目 92.71%

### 1. 模块覆盖矩阵
| 模块 | 目标函数数 | 参考函数数 | 共同唯一 AST 指纹 | Target 覆盖 | Base 覆盖 | 状态 |
| :--- | ---: | ---: | ---: | ---: | ---: | :--- |
| 进程管理 | 56 | 0 | 0 | 0.0% | 0.0% | 仅目标项目体现 |
| 任务调度/异步运行时 | 77 | 0 | 0 | 0.0% | 0.0% | 仅目标项目体现 |
| 内存管理 | 242 | 0 | 0 | 0.0% | 0.0% | 仅目标项目体现 |
| 中断/异常处理 | 26 | 0 | 0 | 0.0% | 0.0% | 仅目标项目体现 |
| 系统调用 | 51 | 0 | 0 | 0.0% | 0.0% | 仅目标项目体现 |
| 文件系统 | 103 | 0 | 0 | 0.0% | 0.0% | 仅目标项目体现 |
| 设备驱动 | 33 | 0 | 0 | 0.0% | 0.0% | 仅目标项目体现 |
| 网络协议栈 | 1 | 0 | 0 | 0.0% | 0.0% | 仅目标项目体现 |
| 文件系统/第三方库 | 523 | 478 | 369 | 83.48% | 92.71% | 双方存在且有结构重合 |
| 通用模块/其他 | 104 | 0 | 0 | 0.0% | 0.0% | 仅目标项目体现 |

### 2. 分模块证据清单

#### 进程管理
仅目标项目中观察到该模块代表函数，参考项目没有同模块可对齐证据：
- 目标项目：`kernel/process.c#scheduler`，调用摘要：acquire, copycontext, copytrapframe, futex_clear, hsai_swtch, intr_on ...(+9)
- 目标项目：`kernel/procfs.c#generate_proc_status_content`，调用摘要：acquire, getproc, release, snprintf
- 目标项目：`kernel/futex.c#futex_wait`，调用摘要：DEBUG_LOG_LEVEL, acquire, myproc, panic, release, sched ...(+1)
- 目标项目：`kernel/futex.c#futex_wait_bitset`，调用摘要：DEBUG_LOG_LEVEL, acquire, myproc, panic, release, sched

#### 任务调度/异步运行时
仅目标项目中观察到该模块代表函数，参考项目没有同模块可对齐证据：
- 目标项目：`hsai/print.c#vsnprintf`，调用摘要：format_int, format_uint, va_arg
- 目标项目：`kernel/signal.c#handle_signal`，调用摘要：DEBUG_LOG_LEVEL, futex_wake
- 目标项目：`user/riscv/user.c#test_shm`，调用摘要：exit, fork, getpid, printf, sleep, strcpy ...(+7)
- 目标项目：`hsai/hsai_trap.c#kerneltrap`，调用摘要：LOG_LEVEL, assert, devintr, exit, intr_get, kill ...(+15)

#### 内存管理
仅目标项目中观察到该模块代表函数，参考项目没有同模块可对齐证据：
- 目标项目：`kernel/syscall.c#syscall`，调用摘要：DEBUG_LOG_LEVEL, LOG_LEVEL, get_syscall_name, hsai_get_arg, myproc, sleep ...(+136)
- 目标项目：`kernel/exec.c#exec`，调用摘要：DEBUG_LOG_LEVEL, LOG_LEVEL, PGROUNDDOWN, PGROUNDUP, acquire, alloc_aux ...(+42)
- 目标项目：`kernel/fs/fs.c#dir_init`，调用摘要：create_file, ext4_fsymlink, free_inode, namei, snprintf, vfs_ext4_mkdir ...(+2)
- 目标项目：`hsai/hsai_trap.c#usertrap`，调用摘要：LOG_LEVEL, check_and_handle_signals, devintr, exit, handle_exception, hsai_usertrapret ...(+21)

#### 中断/异常处理
仅目标项目中观察到该模块代表函数，参考项目没有同模块可对齐证据：
- 目标项目：`kernel/driver/riscv/virt.c#virtio_disk_intr`，调用摘要：R, __sync_synchronize, acquire, panic, printf, release ...(+1)
- 目标项目：`user/loongarch/user.c#test_pselect6_signal`，调用摘要：exit, fork, getpid, getppid, kill, printf ...(+4)
- 目标项目：`user/riscv/user.c#test_pselect6_signal`，调用摘要：exit, fork, getpid, getppid, kill, printf ...(+4)
- 目标项目：`hsai/hsai_trap.c#hsai_trap_init`，调用摘要：timer_init, w_csr_ecfg, w_csr_eentry, w_csr_euen, w_csr_merrentry, w_csr_tlbrentry ...(+1)

#### 系统调用
仅目标项目中观察到该模块代表函数，参考项目没有同模块可对齐证据：
- 目标项目：`user/loongarch/user.c#test_iozone`，调用摘要：exit, fork, printf, sys_chdir, sys_execve, waitpid
- 目标项目：`user/riscv/user.c#test_iozone`，调用摘要：exit, fork, printf, sys_chdir, sys_execve, waitpid
- 目标项目：`user/loongarch/user.c#test_busybox`，调用摘要：exit, fork, printf, sys_chdir, sys_execve, waitpid
- 目标项目：`user/riscv/user.c#test_busybox`，调用摘要：exit, fork, printf, sys_chdir, sys_execve, waitpid

#### 文件系统
仅目标项目中观察到该模块代表函数，参考项目没有同模块可对齐证据：
- 目标项目：`kernel/fs/inode.c#cal_absolute_path`，调用摘要：memmove, strcat, strcpy, strlen
- 目标项目：`kernel/fs/qsort.c#_quicksort`，调用摘要：(*cmp), POP, PUSH, SWAP, min
- 目标项目：`user/loongarch/user.c#test_final`，调用摘要：exit, fork, printf, sys_execve, waitpid
- 目标项目：`user/riscv/user.c#test_final`，调用摘要：exit, fork, printf, sys_execve, waitpid

#### 设备驱动
仅目标项目中观察到该模块代表函数，参考项目没有同模块可对齐证据：
- 目标项目：`kernel/console.c#chardev_init`，调用摘要：initlock, uart_init
- 目标项目：`hal/loongarch/uart.c#uart_init`，调用摘要：_write_reg, initlock
- 目标项目：`hal/riscv/uart.c#uart_init`，调用摘要：WriteReg, initlock
- 目标项目：`kernel/driver/loongarch/virtio_pci.c#virtio_pci_set_queue_addr2`，调用摘要：PCI_REG16, PCI_REG64, dsb

#### 网络协议栈
仅目标项目中观察到该模块代表函数，参考项目没有同模块可对齐证据：
- 目标项目：`kernel/socket.c#sock_bind`，调用摘要：DEBUG_LOG_LEVEL, memmove

#### 文件系统/第三方库
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `kernel/fs/ext4_balloc.c#ext4_balloc_alloc_block` | `Kernel/File/lwext4/ext4_balloc.c#ext4_balloc_alloc_block` | 同板块同名同结构 | ext4_balloc_get_bgid_of_block, ext4_balloc_get_block_of_bgid, ext4_balloc_set_bitmap_csum, ext4_balloc_verify_bitmap_csum, ext4_bg_get_block_bitmap, ext4_bg_get_free_blocks_count ...(+19) | EXT4_Debug_u32, ext4_balloc_get_bgid_of_block, ext4_balloc_get_block_of_bgid, ext4_balloc_set_bitmap_csum, ext4_balloc_verify_bitmap_csum, ext4_bg_get_block_bitmap ...(+19) |
| `kernel/fs/ext4.c#ext4_dir_rm` | `Kernel/File/lwext4/ext4.c#ext4_dir_rm` | 同板块同名同结构 | EXT4_MP_LOCK, EXT4_MP_UNLOCK, ext4_block_cache_write_back, ext4_dir_en_get_inode, ext4_dir_iterator_fini, ext4_dir_iterator_init ...(+19) | EXT4_MP_LOCK, EXT4_MP_UNLOCK, ext4_block_cache_write_back, ext4_dir_en_get_inode, ext4_dir_iterator_fini, ext4_dir_iterator_init ...(+19) |
| `kernel/fs/ext4.c#ext4_generic_open2` | `Kernel/File/lwext4/ext4.c#ext4_generic_open2` | 同板块同名 | ext4_dir_destroy_result, ext4_dir_en_get_inode, ext4_dir_en_get_inode_type, ext4_dir_find_entry, ext4_fs_alloc_inode, ext4_fs_correspond_inode_mode ...(+16) | ext4_dir_destroy_result, ext4_dir_en_get_inode, ext4_dir_en_get_inode_type, ext4_dir_find_entry, ext4_fs_alloc_inode, ext4_fs_correspond_inode_mode ...(+12) |
| `kernel/fs/ext4_journal.c#jbd_journal_prepare` | `Kernel/File/lwext4/ext4_journal.c#jbd_journal_prepare` | 同板块同名 | EXT4_BLOCK_ZERO, RB_FIND, TAILQ_FOREACH_REVERSE_SAFE, TAILQ_FOREACH_SAFE, TAILQ_REMOVE, ext4_bcache_clear_dirty ...(+17) | EXT4_BLOCK_ZERO, RB_FIND, TAILQ_FOREACH_REVERSE_SAFE, TAILQ_FOREACH_SAFE, TAILQ_REMOVE, ext4_bcache_clear_dirty ...(+17) |

#### 通用模块/其他
仅目标项目中观察到该模块代表函数，参考项目没有同模块可对齐证据：
- 目标项目：`hsai/print.c#printf`，调用摘要：acquire, consputc, printint, printptr, release, va_arg ...(+2)
- 目标项目：`kernel/string.c#strtok`，调用摘要：内部闭环逻辑
- 目标项目：`kernel/pmem.c#buddy_safe_check`，调用摘要：list_begin, list_end, list_entry, list_next, printf
- 目标项目：`kernel/futex.c#futex_wake_bitset`，调用摘要：DEBUG_LOG_LEVEL, acquire, release


好的，审计官。我将基于提供的证据底座、控辩双方陈述及客观数据，对本案进行最终同源性定性仲裁。

### 1. 宏观判定
- **定性结论**: **底层架构显著差异 / 独立开发**
- **核心依据**: 客观 AST 相似度仅为 34.42%。目标项目拥有 1043 个唯一 AST 指纹，而参考项目仅有 398 个。所有重合指纹（369个）均被严格锁定在“文件系统/第三方库”这一单一板块内，该板块占参考项目总指纹的 92.71%，但在目标项目中仅占 35.38%。目标项目在进程管理、内存管理、任务调度、中断/异常处理、系统调用、设备驱动、网络协议栈等操作系统核心模块上，与参考项目的共同 AST 指纹为 **0**。这表明双方在操作系统内核的核心架构与设计上存在根本性差异，重合部分源于对同一公开第三方库（lwext4）的共同依赖，而非系统性抄袭。

### 2. 全模块覆盖矩阵解读
- **进程管理**: 仅目标项目存在（56个函数）。共同 AST 指纹为 0。目标项目拥有完整的进程调度、futex 及 procfs 实现，参考项目无此模块。**证据强度: 未形成**。
- **任务调度/异步运行时**: 仅目标项目存在（77个函数）。共同 AST 指纹为 0。目标项目包含独立的 trap 处理、信号机制及用户态测试框架，参考项目无此模块。**证据强度: 未形成**。
- **内存管理**: 仅目标项目存在（242个函数）。共同 AST 指纹为 0。目标项目实现了包含 `exec`、`syscall` 分发、伙伴系统在内的完整内存管理体系，参考项目无此模块。**证据强度: 未形成**。
- **中断/异常处理**: 仅目标项目存在（26个函数）。共同 AST 指纹为 0。目标项目包含针对 RISC-V 和 LoongArch 的 trap 初始化及 virtio 磁盘中断处理，参考项目无此模块。**证据强度: 未形成**。
- **系统调用**: 仅目标项目存在（51个函数）。共同 AST 指纹为 0。目标项目实现了丰富的系统调用接口及用户态测试用例（如 `test_iozone`），参考项目无此模块。**证据强度: 未形成**。
- **文件系统**: 仅目标项目存在（103个函数）。共同 AST 指纹为 0。目标项目拥有独立的 VFS 层、inode 管理及路径解析逻辑（如 `cal_absolute_path`），与参考项目无重合。**证据强度: 未形成**。
- **设备驱动**: 仅目标项目存在（33个函数）。共同 AST 指纹为 0。目标项目包含针对 RISC-V 和 LoongArch 的 UART 及 VirtIO PCI 驱动，参考项目无此模块。**证据强度: 未形成**。
- **网络协议栈**: 仅目标项目存在（1个函数）。共同 AST 指纹为 0。目标项目包含 socket 绑定等初步网络功能，参考项目无此模块。**证据强度: 未形成**。
- **第三方库/公共依赖**: 双方均存在（目标 523，参考 478 个函数）。共同 AST 指纹为 369。目标项目覆盖率 83.48%，参考项目覆盖率 92.71%。所有重合证据均集中于此，且均为 lwext4 库的标准实现。**证据强度: 强（但限于第三方库）**。

### 3. 底层 AST 关键证据指纹表
| 所属板块 | 目标项目路径 | 参考项目路径 | 证据类型 | 技术说明 |
| :--- | :--- | :--- | :--- | :--- |
| 第三方库/公共依赖 | `kernel/fs/ext4_balloc.c#ext4_balloc_alloc_block` | `Kernel/File/lwext4/ext4_balloc.c#ext4_balloc_alloc_block` | 同板块同名同结构 | 均为 lwext4 标准块分配函数，调用链高度一致，但目标项目缺少 `EXT4_Debug_u32` 调试宏。 |
| 第三方库/公共依赖 | `kernel/fs/ext4.c#ext4_dir_rm` | `Kernel/File/lwext4/ext4.c#ext4_dir_rm` | 同板块同名同结构 | 均为 lwext4 标准目录删除函数，调用链完全一致，属于 ext4 规范的标准实现。 |
| 第三方库/公共依赖 | `kernel/fs/ext4_journal.c#jbd_iterate_log` | `Kernel/File/lwext4/ext4_journal.c#jbd_iterate_log` | 同板块同名同结构 | 日志回放逻辑同构，但目标项目将多个调试宏统一为 `ext4_dbg`，AST 调用节点数不同（+3 vs +5）。 |
| 第三方库/公共依赖 | `kernel/fs/ext4_dir_idx.c#ext4_dir_dx_add_entry` | `Kernel/File/lwext4/ext4_dir_idx.c#ext4_dir_dx_add_entry` | 同板块同名同结构 | HTree 索引插入的标准实现，目标项目使用 `ext4_dbg` 替代 `EXT4_Debug_u32_u32`。 |
| 第三方库/公共依赖 | `kernel/fs/ext4_mkfs.c#ext4_mkfs` | `Kernel/File/lwext4/ext4_mkfs.c#ext4_mkfs` | 同板块同名 | 格式化流程一致，但目标项目调用节点数（+12）显著少于参考项目（+17），体现了不同的调试策略。 |
| 第三方库/公共依赖 | `kernel/fs/ext4_extent.c#ext4_ext_split_node` | `Kernel/File/lwext4/ext4_extent.c#ext4_ext_split_node` | 同板块底层逻辑一致 | 区段树分裂算法由 ext4 规范决定，调用链高度相似，属于算法收敛。 |
| 第三方库/公共依赖 | `kernel/fs/ext4_xattr.c#ext4_xattr_set_entry` | `Kernel/File/lwext4/ext4_xattr.c#ext4_xattr_set_entry` | 同板块底层逻辑一致 | 扩展属性操作逻辑一致，使用了 `memcpy`、`memmove` 等标准 C 库函数。 |
| 第三方库/公共依赖 | `kernel/fs/ext4_fs.c#ext4_fs_get_inode_dblk_idx_internal` | `Kernel/File/lwext4/ext4_fs.c#ext4_fs_get_inode_dblk_idx_internal` | 同板块底层逻辑一致 | 数据块索引解析逻辑同构，包含对 `ext4_inode_has_flag` 的标准判断分支。 |

### 4. 核心代码调用流对比剖析
* **【第三方库/公共依赖 / ext4 块分配器】**
* **底层技术析理**: 双方在 `ext4_balloc_alloc_block` 函数中实现了完全一致的 ext4 块分配流程。调用流均始于 `ext4_balloc_get_bgid_of_block` 定位块组，通过 `ext4_bg_get_block_bitmap` 获取位图，经 `ext4_balloc_verify_bitmap_csum` 校验后执行分配，最后更新位图校验和及空闲块计数。核心差异在于目标项目移除了所有 `EXT4_Debug_*` 宏调用，统一为内核日志接口，这改变了函数的 AST 节点类型和数量，属于工程化适配而非核心逻辑抄袭。
* **代码溯源链接**:
  - 目标项目: `kernel/fs/ext4_balloc.c#ext4_balloc_alloc_block`
  - 参考项目: `Kernel/File/lwext4/ext4_balloc.c#ext4_balloc_alloc_block`

* **【第三方库/公共依赖 / ext4 目录索引】**
* **底层技术析理**: 在 `ext4_dir_dx_add_entry` 目录项插入操作中，双方遵循相同的 HTree 算法流程：计算哈希 -> `ext4_dir_dx_get_leaf` 查找叶子节点 -> `ext4_dir_dx_csum_verify` 校验 -> 尝试插入，失败则调用 `ext4_dir_dx_split_data` 分裂。此调用流由 ext4 文件系统规范严格定义，任何正确实现都必然呈现此拓扑结构。目标项目将调试宏 `EXT4_Debug_u32_u32` 替换为 `ext4_dbg`，表明其进行了独立的接口抽象。
* **代码溯源链接**:
  - 目标项目: `kernel/fs/ext4_dir_idx.c#ext4_dir_dx_add_entry`
  - 参考项目: `Kernel/File/lwext4/ext4_dir_idx.c#ext4_dir_dx_add_entry`

### 5. 单方模块与降权项
- **单方模块**: 进程管理、任务调度/异步运行时、内存管理、中断/异常处理、系统调用、文件系统（原生VFS层）、设备驱动、网络协议栈、通用模块/其他。这九大模块仅在目标项目中存在，构成了一个完整的、独立于参考项目的操作系统内核骨架，是排除系统性抄袭的最有力证据。
- **降权项（第三方库/公共依赖）**: 所有重合证据均属于 `文件系统/第三方库` 板块。双方代码路径（`kernel/fs/` vs `Kernel/File/lwext4/`）和函数名均指向公开的 lwext4 库。`EXT4_MP_LOCK`、`RB_FIND`、`TAILQ_FOREACH_SAFE` 等是标准的同步原语和数据结构宏。这些基于同一开源库或公共规范的实现，其相似性具有合理来源，不能作为判定抄袭的强证据。

### 6. 保守结论
本案客观 AST 相似度为 34.42%，低于通常判定抄袭的阈值。所有重合指纹均源于双方对同一开源第三方库（lwext4）的集成，属于“第三方库/公共依赖”板块内的规范收敛。目标项目在进程、内存、驱动等九大核心模块上与参考项目零重合，展现了完全独立的操作系统内核架构设计。控方关于“蓄意混淆”的指控，实质上是目标项目将调试宏统一抽象为 `ext4_dbg` 的独立工程化改造。综上，现有证据无法支持“系统性代码搬运”的指控，应判定为独立开发，整体风险为低。

------------------------------

