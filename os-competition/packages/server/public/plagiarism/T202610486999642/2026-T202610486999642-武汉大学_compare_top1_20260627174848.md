# 🛡️ OS 内核赛自动化代码级架构审计报告

**审计时间**: `2026-06-27 17:48:48`
**参赛作品名称**: `2026-T202610486999642-武汉大学`
**年份**: `2026`
**学校**: `武汉大学`
**队伍名称**: `T202610486999642`
**比赛名字**: `OS-Competition`
**比赛子赛道**: `Kernel-Implementation`
**仓库地址**: `https://gitlab.eduxiji.net/educg-group-43501-3132633/T202610486999642-1267`
**推测开发语言**: `Rust / C (由AST探针自动识别)`
**特征函数量**: `4393`

## ⚔️ 底层代码同源性对比 (AST Homology)
> 本部分由专门的代码审查智能体，对该项目与符合年份条件的历届最高相似度项目进行控制流与底层调用对比。新版报告会先给出确定性的全模块覆盖矩阵，再给出 Agent 的分模块解释，避免只围绕少量抽样函数展开。

### 🏆 Top 1 底层对标: vs `2025-KernalCraft-中山大学`（中山大学 / KernalCraft）

#### 对方作品信息

**对方年份**: `2025`
**对方学校**: `中山大学`
**对方队伍名称**: `KernalCraft`
**对方比赛名字**: `OS-Competition`
**对方比赛子赛道**: `Kernel-Implementation`
**对方仓库地址**: `https://gitlab.eduxiji.net/educg-group-36002-2710490/T202510558995240-2536`

> 双向覆盖率：共同 AST 指纹: 114；唯一 AST 指纹: 目标项目 3483，参考项目 1853；覆盖率: 目标项目 3.27%，参考项目 6.15%

## 全模块对比总览（确定性证据）

> 总体覆盖率：共同 AST 指纹: 114；唯一 AST 指纹: 目标项目 3483，参考项目 1853；覆盖率: 目标项目 3.27%，参考项目 6.15%

### 1. 模块覆盖矩阵
| 模块 | 目标函数数 | 参考函数数 | 共同唯一 AST 指纹 | Target 覆盖 | Base 覆盖 | 状态 |
| :--- | ---: | ---: | ---: | ---: | ---: | :--- |
| 进程管理 | 629 | 71 | 11 | 1.96% | 17.46% | 双方存在且有结构重合 |
| 任务调度/异步运行时 | 627 | 115 | 8 | 1.45% | 7.14% | 双方存在且有结构重合 |
| 内存管理 | 806 | 741 | 50 | 6.93% | 11.16% | 双方存在且有结构重合 |
| 中断/异常处理 | 77 | 128 | 9 | 12.68% | 9.0% | 双方存在且有结构重合 |
| 系统调用 | 200 | 21 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |
| 文件系统 | 1636 | 147 | 4 | 0.31% | 3.12% | 双方存在且有结构重合 |
| 设备驱动 | 144 | 555 | 4 | 4.44% | 0.85% | 双方存在且有结构重合 |
| 网络协议栈 | 16 | 53 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |
| 文件系统/第三方库 | 38 | 340 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |
| 第三方库 | 0 | 2 | 0 | 0.0% | 0.0% | 仅参考项目体现 |
| 通用模块/其他 | 220 | 669 | 15 | 14.15% | 5.66% | 双方存在且有结构重合 |

### 2. 分模块证据清单

#### 进程管理
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `os/src/task/processor.rs#run_tasks` | `os/src/task/processor.rs#run_tasks` | 同板块同名 | PROCESSOR.exclusive_access, __switch, crate::fs::console_tty_drain_uart, crate::timer::get_time_us, drop, fetch_task ...(+6) | PROCESSOR.exclusive_access, Some, __switch, drop, fetch_task, println! ...(+2) |
| `os/src/syscall/process/id.rs#sys_kill` | `os/src/syscall/process.rs#sys_kill` | 同板块同名 | Err, Ok, SignalFlags::from_signum, SignalFlags::from_signum(signal).ok_or, SignalInfo::user, caller_can_signal_target ...(+8) | SignalFlags::from_bits, log::error!, pid2process, process.inner_exclusive_access |
| `os/src/syscall/process/id.rs#sys_exit_group` | `os/src/syscall/process.rs#sys_exit_group` | 同板块同名 | current_task, current_task()
        .map, current_task()
        .map(|task| task.inner_exclusive_access().clone_vm_process_helper)
        .unwrap_or, exit_current_and_run_next, exit_current_group_and_run_next, panic! ...(+1) | exit_current_and_run_next, panic! |
| `os/src/syscall/process/clone.rs#sys_clone` | `os/src/syscall/process.rs#sys_clone` | 同板块同名 | CloneArgs::parse, Err, args.is_thread, clone_tls_and_ctid_args, is_vm_newnet_process_clone, is_vm_vfork_process_clone ...(+4) | current_process, current_process.fork, new_process.getpid, new_process.inner_exclusive_access, new_process_inner.tasks[0].as_ref, new_process_inner.tasks[0].as_ref().unwrap ...(+3) |

#### 任务调度/异步运行时
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `os/src/task/signal.rs#check_error` | `os/src/task/signal.rs#check_error` | 同板块同名 | Some, self.contains | Some, self.contains |
| `os/src/syscall/fs/poll.rs#sys_ppoll` | `os/src/syscall/ppoll.rs#sys_ppoll` | 同板块同名 | Arc::clone, Err, Ok, PollWaiter::new, Some, block_signal_only_waiter ...(+16) | Box::from_raw, Box::into_raw, Box::new, SignalFlags::empty, curr_process.inner_exclusive_access, current_process ...(+16) |
| `os/src/syscall/fs/path.rs#sys_renameat2` | `os/src/syscall/fs.rs#sys_renameat2` | 同板块同名 | AccessSubject::from_fs_credentials, Err, Ok, check_rename_access, current_process, current_process().credentials ...(+14) | current_process, current_user_token, file.getid, inner.fd_table.len, inner.fd_table[fd].as_ref, inner.fd_table[fd].as_ref().unwrap ...(+6) |
| `os/src/task/manager.rs#fetch` | `os/src/task/manager.rs#fetch` | 同板块同名 | Self::rt_priority, Some, perf::record_scheduler_fetch, perf::time_scope, self.clear_rt_priority_if_empty, self.decrement_ready_count ...(+7) | self.ready_queue.pop_front |

#### 内存管理
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `os/src/task/process_lifecycle.rs#fork` | `os/src/task/process.rs#fork` | 同板块同名 | Arc::clone, Arc::downgrade, Arc::new, Default::default, MemorySet::from_existed_user, ProcessCpuTimes::with_inherited_self_maxrss ...(+32) | Arc::clone, Arc::downgrade, Arc::new, MemorySet::from_existed_user, RLimit::new, RecycleAllocator::new ...(+29) |
| `os/src/arch/riscv64/trap.rs#trap_handler` | `virtio-drivers/examples/x86_64/src/trap.rs#trap_handler` | 同板块同名 | Arc::clone, Some, account_task_user_time_until, check_signals_of_task, check_timer, crate::arch::signal::deliver_pending_signal ...(+33) | panic!, x86_64::registers::control::Cr2::read |
| `os/src/task/exec.rs#exec` | `os/src/task/process.rs#exec` | 同板块同名 | KERNEL_SPACE.exclusive_access, KERNEL_SPACE.exclusive_access().token, MemorySet::from_elf_lazy, MemorySet::from_elf_lazy(elf, executable_file, executable_file_size, interpreter)
            .ok_or, Ok, SigAltStack::disabled ...(+41) | KERNEL_SPACE.exclusive_access, KERNEL_SPACE.exclusive_access().token, MemorySet::from_elf, TrapContextImpl::app_init_ctx, assert_eq!, core::mem::size_of::<usize> ...(+12) |
| `os/src/mm/elf_loader.rs#from_elf` | `os/src/mm/memory_set.rs#from_elf` | 同板块同名 | MapArea::new, Self::new_bare, assert_eq!, bias_nonzero_addr, crate::vdso::map_into, crate::vdso::map_into(&mut memory_set).unwrap_or ...(+12) | AuxHeader::new, MapArea::new, MmapManager::new, Self::new_bare, Some, VirtAddr ...(+13) |

#### 中断/异常处理
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `os/src/arch/loongarch64/board.rs#irq_handler` | `os/src/boards/qemu.rs#irq_handler` | 同板块同名 | KEYBOARD_DEVICE.as_ref, MOUSE_DEVICE.as_ref, Some, UART.handle_irq, board_config, crate::drivers::block::handle_irq ...(+8) | BLOCK_DEVICE.handle_irq, IRQ_CTRL.claim_irq, IRQ_CTRL.claim_irq().unwrap, IRQ_CTRL.complete_irq, KEYBOARD_DEVICE.handle_irq, MOUSE_DEVICE.handle_irq ...(+3) |
| `os/src/arch/loongarch64/board.rs#device_init` | `os/src/boards/qemu.rs#device_init` | 同板块同名 | block_devices, board_config, crate::trap::enable_external_interrupt, eiointc.init, enable_external_irq, info! ...(+4) | Arch::enable_supervisor_interrupt, IRQ_CTRL.enable_irq |
| `os/src/fs/pipe.rs#make_pipe` | `os/src/fs/pipe.rs#make_pipe` | 同板块同名 | Arc::new, Pipe::read_end_with_buffer, Pipe::write_end_with_buffer, PipeRingBuffer::new, UPIntrFreeCell::new, buffer.clone ...(+3) | Arc::new, Pipe::read_end_with_buffer, Pipe::write_end_with_buffer, PipeRingBuffer::new, UPIntrFreeCell::new, buffer.clone ...(+2) |
| `os/src/drivers/plic.rs#set_threshold` | `os/src/drivers/plic.rs#set_threshold` | 同板块同名同结构 | assert!, self.threshold_ptr_of_hart_with_priority, threshold_ptr.write_volatile | assert!, self.threshold_ptr_of_hart_with_priority, threshold_ptr.write_volatile |

#### 系统调用
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `os/src/syscall/sem.rs#try_semop` | `os/src/syscall/net.rs#sys_socket` | 同模块上下文对齐/差异观察 | Err, Ok, i32::from, now_sec, ops.is_empty, ops.len ...(+11) | create_tcp_socket, create_udp_socket |
| `os/src/syscall/keyring.rs#sys_request_key` | `os/src/syscall/input.rs#sys_event_get` | 同模块上下文对齐/差异观察 | Err, KEY_MANAGER
                    .exclusive_session, KEY_MANAGER.exclusive_session, Ok, callout_info.is_null, current_owner_uid ...(+10) | KEYBOARD_DEVICE.clone, MOUSE_DEVICE.clone, kb.is_empty, kb.read_event, mouse.is_empty, mouse.read_event |
| `os/src/syscall/msg.rs#receive` | `os/src/syscall/info.rs#fmt` | 同模块上下文对齐/差异观察 | Err, Ok, find_message_index, message.text.len, now_sec, pid_to_i32 ...(+9) | writeln! |
| `os/src/syscall/aio.rs#sys_io_getevents` | `os/src/syscall/info.rs#sys_sysinfo` | 同模块上下文对齐/差异观察 | AIO_MANAGER
        .exclusive_access, AIO_MANAGER
        .exclusive_access()
        .pop_events, AIO_MANAGER.exclusive_access, AIO_MANAGER.exclusive_access().contains, Err, Ok ...(+9) | Sysinfo::collect, current_user_token, translated_refmut |

#### 文件系统
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `os/src/fs/staticfs.rs#seek` | `os/src/fs/mod.rs#seek` | 同板块同名 | Err, Ok, base.checked_add, base.checked_add(offset).ok_or, content, content(self.node).map_or ...(+2) | EXT4.get_inode_ref, ext4_inode_ref.inode.size, log::trace!, off.abs, self.get_inner_refmut, self.getid |
| `os/src/fs/tmpfs.rs#read_at` | `os/src/fs/inode.rs#read_at` | 同板块同名 | FileTimestamp::now, buf.is_empty, buf.len, buf.len().min, inode.copy_sparse_to, inode.data.len ...(+5) | EXT4.read_at, panic! |
| `os/src/fs/fat.rs#write_at` | `os/src/fs/inode.rs#write_at` | 同板块同名 | SeekFrom::Start, Self::grow_file, Self::grow_file(&mut file, offset).is_err, buf.is_empty, file.seek, file.seek(SeekFrom::Start(offset)).is_err ...(+5) | EXT4.write_at, panic! |
| `os/src/fs/vfs/file.rs#read_all` | `os/src/fs/inode.rs#read_all` | 同板块同名 | Vec::new, data.extend_from_slice, perf::record_vfs_read_all_backend_read, perf::record_vfs_read_all_call, self.offset.lock, self.read_backend_at_profiled ...(+1) | Vec::new, inner.inode.read_at, self.inner.exclusive_access, v.extend_from_slice |

#### 设备驱动
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `os/src/drivers/virtio.rs#unshare` | `virtio-drivers/src/hal/fake.rs#unshare` | 同板块同名 | SHARED_BOUNCES.exclusive_access, SHARED_BOUNCES.exclusive_access().remove, buffer.as_mut, buffer.len, core::slice::from_raw_parts, drop ...(+2) | Box::from_raw, assert_ne!, buffer
                    .as_ptr, buffer
                    .as_ptr()
                    .cast::<u8>, buffer
                    .as_ptr()
                    .cast::<u8>()
                    .copy_from, buffer.len ...(+3) |
| `os/src/drivers/block.rs#handle_irq` | `os/src/drivers/input/mod.rs#handle_irq` | 同板块同名 | BLOCK_DEVICES.iter, BLOCK_DEVICES.iter().find, device.handle_irq, device.irq | inner.events.push_back, inner.virtio_input.ack_interrupt, inner.virtio_input.pop_pending_event, self.condvar.signal, self.inner.exclusive_session |
| `os/src/arch/riscv64/board.rs#virtio_device_type` | `virtio-drivers/src/transport/pci.rs#virtio_device_type` | 同板块同名 | MmioTransport::new, NonNull::new, NonNull::new(device.base as *mut VirtIOHeader).unwrap, transport.device_type, unsafe { MmioTransport::new(header, device.size) }
        .ok, unsafe { MmioTransport::new(header, device.size) }
        .ok()
        .map | device_type |
| `os/src/drivers/block.rs#read_blocks` | `virtio-drivers/src/device/blk.rs#read_blocks` | 同板块同名 | block_cache::read_with_cache, self.cache_key, self.read_blocks_uncached | assert_eq!, assert_ne!, self.request_read |

#### 网络协议栈
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `os/src/perf.rs#record_local_socket_write_user_buffer` | `lose-net-stack/src/packets/udp.rs#build_data` | 同模块上下文对齐/差异观察 | LOCAL_SOCKET_STREAM_DIRECT_BYTES.fetch_add, LOCAL_SOCKET_WRITE_TO_VEC_BYTES.fetch_add, LOCAL_SOCKET_WRITE_USER_BUFFER_BYTES.fetch_add | ((self.data_len + UDP_LEN + IP_LEN) as u16).to_be, ((self.data_len + UDP_LEN) as u16).to_be, ETH_RTYPE_IP.to_be, IP_PROTOCAL_UDP.to_be, UnsafeRefIter::new, check_sum ...(+17) |
| `os/src/perf.rs#record_local_socket_stream_recv` | `lose-net-stack/src/packets/arp.rs#build_data` | 同模块上下文对齐/差异观察 | LOCAL_SOCKET_STREAM_RECV_CONTIGUOUS_BYTES.fetch_add, LOCAL_SOCKET_STREAM_RECV_SLICE_BYTES.fetch_add | ARP_HRD_ETHER.to_be, ETH_RTYPE_ARP.to_be, ETH_RTYPE_IP.to_be, UnsafeRefIter::new, data_ptr_iter.next_mut::<Arp>, data_ptr_iter.next_mut::<Eth> ...(+11) |
| `os/src/perf.rs#record_local_socket_write_user_buffer` | `lose-net-stack/src/packets/tcp.rs#ack` | 同模块上下文对齐/差异观察 | 内部闭环逻辑 | flags.contains, flags.remove, self.flags.contains |
| `os/src/perf.rs#record_local_socket_reader_wakeup` | `lose-net-stack/src/lib.rs#analysis_udp` | 同模块上下文对齐/差异观察 | LOCAL_SOCKET_READER_WAKEUPS.fetch_add | IPv4::from_u32, MacAddress::new, Packet::UDP, data_ptr_iter.get_curr_arr, data_ptr_iter.next::<UDP>, ip_header.dst.to_be ...(+5) |

#### 文件系统/第三方库
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `os/src/fs/ext4.rs#unlink` | `ext4_rs/src/ext4_impls/ext4.rs#unlink` | 同板块同名 | Ok, drop, lookup.entry, lookup.entry().ino, lwext4_rust::FileAttr::default, self
            .fs
            .lookup ...(+12) | Ok, child.inode.is_dir, log::trace!, self.dir_remove_entry, self.ialloc_free_inode |
| `os/src/fs/ext4.rs#read_at` | `ext4_rs/src/ext4_impls/file.rs#read_at` | 同板块同名 | perf::time_scope, self.fs.read_at, self.fs.read_at(ino, buf, offset).expect | Ok, core::cmp::min, inode_ref.inode.size, log::trace!, min, read_buf.len ...(+5) |
| `os/src/fs/ext4.rs#set_mode` | `ext4_rs/src/ext4_defs/inode.rs#set_mode` | 同板块同名 | self.fs.set_mode, self.fs.set_mode(ino, mode).map_err, self.stat | 内部闭环逻辑 |
| `os/src/fs/ext4.rs#rename` | `ext4_rs/src/fuse_interface/mod.rs#rename` | 同板块同名 | self.fs
            .rename, self.fs
            .rename(src_dir, src_name, dst_dir, dst_name)
            .map_err | Ok, self.link, self.unlink |

#### 第三方库
仅参考项目中观察到该模块代表函数，目标项目没有同模块可对齐证据：
- 参考项目：`virtio-drivers/src/transport/mmio.rs#vendor_id`，调用摘要：field_shared!, field_shared!(self.header, vendor_id).read
- 参考项目：`virtio-drivers-old/src/header.rs#vendor_id`，调用摘要：self.vendor_id.read

#### 通用模块/其他
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `os/src/lang_items.rs#backtrace` | `os/src/lang_items.rs#backtrace` | 同板块同名 | crate::arch::backtrace::frame_pointer, current_kstack_bounds, frame_pointer_in_current_stack, println! | asm!, current_kstack_top, println! |
| `os/src/logging.rs#log` | `os/src/logging.rs#log` | 同板块同名同结构 | println!, record.level, record.metadata, self.enabled | println!, record.level, record.metadata, self.enabled |
| `os/src/arch/loongarch64/sbi.rs#set_timer` | `os/src/hal/timer/loongarch64.rs#set_timer` | 同板块同名 | loongArch64::register::tcfg::set_en, loongArch64::register::tcfg::set_init_val, loongArch64::register::tcfg::set_periodic, loongArch64::time::Time::read, timer.saturating_sub, timer.saturating_sub(now).max | LATimer::get_time, register::tcfg::set_en, register::tcfg::set_init_val, register::tcfg::set_periodic, register::ticlr::clear_timer_interrupt |
| `os/src/sync/up.rs#enter` | `os/src/hal/up/loongarch.rs#enter` | 同板块同名 | interrupt::disable_supervisor_interrupt, interrupt::supervisor_interrupt_enabled | crmd.ie, crmd::read, crmd::set_ie |


#### 1. 宏观判定
- **定性结论**: 独立开发 (相似度: 2.18%，共同 AST 指纹: 114；唯一 AST 指纹: 目标项目 3483，参考项目 1853；覆盖率: 目标项目 3.27%，参考项目 6.15%)

#### 2. 全模块覆盖矩阵解读
- **进程管理**: 双方均存在。存在共同 AST 指纹。目标覆盖率 1.96%，参考覆盖率 17.46%。参考覆盖率因分母较小（71个函数）而被放大，目标项目 629 个函数中仅 11 个指纹重合，重合点多为 `getpid`、`getppid` 等 POSIX 标准系统调用转发或 `inner_exclusive_access` 等 Rust 惯用访问器。该模块证据强度为**弱**。
- **任务调度/异步运行时**: 双方均存在。存在共同 AST 指纹。目标覆盖率 1.45%，参考覆盖率 7.14%。重合点集中在 `run_tasks`、`schedule` 等调度入口的骨架调用（如 `__switch`、`fetch_task`），但目标项目额外引入了多核 idle 等待、控制台同步、内核时间记账等独立外部调用。该模块证据强度为**弱**。
- **内存管理**: 双方均存在。存在共同 AST 指纹。目标覆盖率 6.93%，参考覆盖率 11.16%。重合点包括 `fork`、`exec`、`from_elf` 等核心流程，均基于 rCore 的 `MemorySet`、`MapArea` 等公开框架 API。目标项目在 `exec` 中实现了 `from_elf_lazy` 延迟加载策略，与参考项目的 `from_elf` 存在实质性架构差异。该模块证据强度为**弱**。
- **中断/异常处理**: 双方均存在。存在共同 AST 指纹。目标覆盖率 12.68%，参考覆盖率 9.0%。重合点包括 `irq_handler`、`device_init` 等板级初始化与中断分发逻辑，以及 `make_pipe` 等基于 `UPIntrFreeCell` 的标准管道创建。`plic.rs#set_threshold` 为直接硬件寄存器操作，无独创表达空间。该模块证据强度为**弱**。
- **系统调用**: 双方均存在。未观察到共同结构指纹。目标项目实现了完整的 Linux ABI（如 `semop`、`request_key`、`io_getevents`），参考项目仅有基础的网络/输入系统调用，两者在系统调用功能范围和实现上无重合。该模块证据强度为**未形成**。
- **文件系统**: 双方均存在。存在共同 AST 指纹。目标覆盖率 0.31%，参考覆盖率 3.12%。重合点仅为 `seek`、`read_at`、`write_at` 等 VFS 通用接口签名，底层实现分别对接 `staticfs`/`tmpfs`/`fat` 与 `ext4_rs`，外部调用对象完全不同。该模块证据强度为**弱**。
- **设备驱动**: 双方均存在。存在共同 AST 指纹。目标覆盖率 4.44%，参考覆盖率 0.85%。重合点包括 `virtio.rs#unshare` 的共享缓冲区移除逻辑和 `plic.rs#set_threshold` 的寄存器操作，均为 VirtIO/PLIC 硬件规范的标准实现。该模块证据强度为**弱**。
- **网络协议栈**: 双方均存在。未观察到共同结构指纹。目标项目仅含 16 个函数（主要为 `perf.rs` 中的性能统计埋点），参考项目包含完整的 `lose-net-stack` 第三方库。两者无任何 AST 重合。该模块证据强度为**未形成**。
- **第三方库/公共依赖**: 参考项目包含 `virtio-drivers`、`ext4_rs`、`lose-net-stack` 等独立第三方 crate，目标项目未直接引用这些 crate。跨项目的第三方库同名函数（如 `ext4.rs#unlink`）属于上游库的公共 API，不构成项目间的同源证据。该模块证据强度为**未形成**。

#### 3. 底层 AST 关键证据指纹表
| 所属板块 | 目标项目路径 | 参考项目路径 | 证据类型 | 技术说明 |
| :--- | :--- | :--- | :--- | :--- |
| 进程管理 | os/src/task/manager.rs#pid2process | os/src/task/manager.rs#pid2process | 领域收敛/标准模板 | 全局 `HashMap` 加锁查询，rCore 教学框架标准设计。 |
| 进程管理 | os/src/task/process.rs#inner_exclusive_access | os/src/task/process.rs#inner_exclusive_access | 领域收敛/标准模板 | `UPIntrFreeCell`/`Mutex` 的一行访问器封装。 |
| 进程管理 | os/src/task/processor.rs#take_current_task | os/src/task/processor.rs#take_current_task | 领域收敛/标准模板 | `PROCESSOR` 全局实例的 `Option::take` 操作。 |
| 进程管理 | os/src/syscall/process/id.rs#sys_exit | os/src/syscall/process.rs#sys_exit | 领域收敛/标准模板 | `exit_current_and_run_next` 不返回，后接 `panic!` 满足 Rust 类型系统。 |
| 进程管理 | os/src/syscall/process/id.rs#sys_getppid | os/src/syscall/process.rs#sys_getppid | 领域收敛/标准模板 | Linux `getppid` 语义的标准转发。 |
| 进程管理 | os/src/task/processor.rs#get_idle_task_cx_ptr | os/src/task/processor.rs#get_idle_task_cx_ptr | 领域收敛/标准模板 | 空闲任务上下文指针的纯返回，无外部调用。 |
| 进程管理 | os/src/task/process.rs#get_user_token | os/src/task/process.rs#get_user_token | 领域收敛/标准模板 | rCore `MemorySet::token()` 标准接口调用。 |
| 进程管理 | os/src/task.rs#add_initproc | os/src/task/mod.rs#add_initproc | 领域收敛/标准模板 | rCore `lazy_static!` 初始化进程的克隆入队模式。 |
| 进程管理 | os/src/task/process.rs#getpid | os/src/task/process.rs#getpid | 领域收敛/标准模板 | 返回 `self.pid`，无其他实现可能。 |
| 任务调度 | os/src/task/processor.rs#run_tasks | os/src/task/processor.rs#run_tasks | 领域收敛/标准模板 | rCore 调度循环骨架，目标项目扩展了多核 idle 与控制台同步。 |
| 任务调度 | os/src/task/processor.rs#schedule | os/src/task/processor.rs#schedule | 领域收敛/标准模板 | rCore 上下文切换骨架，目标项目扩展了内核时间记账。 |
| 任务调度 | os/src/sync/condvar.rs#wait_no_sched | os/src/sync/condvar.rs#wait_no_sched | 领域收敛/标准模板 | 条件变量等待队列操作，目标项目使用不同的阻塞原语。 |
| 内存管理 | os/src/task/process_lifecycle.rs#fork | os/src/task/process.rs#fork | 领域收敛/标准模板 | rCore `MemorySet::from_existed_user` 等标准 API 调用。 |
| 内存管理 | os/src/task/exec.rs#exec | os/src/task/process.rs#exec | 领域收敛/标准模板 | rCore ELF 加载流程，目标项目采用 `from_elf_lazy` 延迟加载。 |
| 中断/异常处理 | os/src/arch/loongarch64/board.rs#irq_handler | os/src/boards/qemu.rs#irq_handler | 领域收敛/标准模板 | 板级中断分发，调用不同硬件设备。 |
| 中断/异常处理 | os/src/fs/pipe.rs#make_pipe | os/src/fs/pipe.rs#make_pipe | 领域收敛/标准模板 | `PipeRingBuffer` + `UPIntrFreeCell` 标准管道创建。 |
| 设备驱动 | os/src/drivers/plic.rs#set_threshold | os/src/drivers/plic.rs#set_threshold | 领域收敛/标准模板 | PLIC 阈值寄存器直接写入，硬件规范决定。 |
| 设备驱动 | os/src/drivers/virtio.rs#unshare | virtio-drivers/src/hal/fake.rs#unshare | 领域收敛/标准模板 | VirtIO 共享缓冲区移除，硬件交互标准模式。 |

#### 4. 核心代码调用流对比剖析
* **【进程管理/调度入口】**
* **底层技术析理**: 两者均遵循 rCore 的 `PROCESSOR` 全局处理器实例 + `fetch_task` + `__switch` 调度范式。目标项目在循环体内额外调用 `crate::fs::console_tty_drain_uart`（多核控制台同步）和 `hart::enable_interrupt_and_wait`（多核 idle 等待），参考项目仅使用 `println!` 和 `task.inner.exclusive_session`。目标项目的调度器面向多核环境实现了独立的中断等待与 I/O 同步逻辑。
* **代码溯源链接**:
  - 目标项目: `os/src/task/processor.rs#run_tasks`
  - 参考项目: `os/src/task/processor.rs#run_tasks`

* **【进程管理/进程退出】**
* **底层技术析理**: 参考项目的 `sys_exit_group` 仅调用 `exit_current_and_run_next` 后 `panic!`。目标项目实现了完整的线程组退出语义：先通过 `current_task().map(...clone_vm_process_helper)` 获取 VM 辅助结构，再调用 `exit_current_group_and_run_next`。两者在退出逻辑的抽象层次和功能完整度上存在显著差异。
* **代码溯源链接**:
  - 目标项目: `os/src/syscall/process/id.rs#sys_exit_group`
  - 参考项目: `os/src/syscall/process.rs#sys_exit_group`

* **【内存管理/ELF 加载】**
* **底层技术析理**: 参考项目在 `exec` 中调用 `MemorySet::from_elf` 一次性完成 ELF 加载。目标项目调用 `MemorySet::from_elf_lazy` 实现延迟加载策略，并额外处理 `SigAltStack::disabled`、`vdso::map_into` 等高级特性。两者在内存映射策略上存在根本性架构差异。
* **代码溯源链接**:
  - 目标项目: `os/src/task/exec.rs#exec`
  - 参考项目: `os/src/task/process.rs#exec`

* **【中断/异常处理/板级初始化】**
* **底层技术析理**: 两者均实现了板级中断分发和设备初始化。目标项目面向 LoongArch64 架构，调用 `eiointc.init`、`enable_external_irq` 等架构特定接口；参考项目面向 RISC-V/QEMU，调用 `IRQ_CTRL.enable_irq`。两者在硬件抽象层和中断控制器驱动上完全不同。
* **代码溯源链接**:
  - 目标项目: `os/src/arch/loongarch64/board.rs#device_init`
  - 参考项目: `os/src/boards/qemu.rs#device_init`

#### 5. 单方模块与降权项
- **第三方库/公共依赖**: 参考项目包含 `virtio-drivers`、`ext4_rs`、`lose-net-stack` 等独立第三方 crate。目标项目未直接引用这些 crate，其文件系统（`staticfs`、`tmpfs`、`fat`）和设备驱动（自研 `virtio.rs`）为独立实现。跨项目的第三方库同名函数（如 `ext4.rs#unlink`）属于上游库的公共 API，不构成项目间的同源证据。
- **网络协议栈**: 目标项目仅含 16 个函数（主要为 `perf.rs` 中的性能统计埋点），参考项目包含完整的 `lose-net-stack` 第三方库。两者无任何 AST 重合，该模块为单方实现。
- **系统调用**: 目标项目实现了 200 个系统调用函数（涵盖 `semop`、`request_key`、`io_getevents` 等完整 Linux ABI），参考项目仅 21 个。两者在系统调用功能范围和实现上无重合，该模块为单方实现。
- **跨板块同名函数**: 控方证据中 `fdt/src/node.rs#children`（设备树解析）、`os/src/syscall/info.rs#write_proc_global`（ext4 proc 写入）等被归入“进程管理”板块，属于跨板块硬凑，不构成同板块主证据。

#### 6. 保守结论
双方项目整体 AST 相似度仅 2.18%，远低于可认定“实质性相似”的阈值。进程管理板块中控方所列“逐字一致”的证据，均为 `getpid`、`inner_exclusive_access` 等 POSIX 标准系统调用转发或 Rust 惯用访问器，属于同一教学框架（rCore）和 Linux ABI 约束下的领域收敛，不具有独创性表达。目标项目在调度器、内存管理、进程退出等核心路径上实现了多核支持、延迟加载、线程组语义等参考项目不具备的高级功能，外部调用对象和架构抽象呈现清晰的独立演化路径。综合量化指标与技术实质，双方项目为独立开发，不存在违规复用。

------------------------------

