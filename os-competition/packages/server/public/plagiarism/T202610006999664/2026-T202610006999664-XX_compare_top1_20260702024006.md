# 🛡️ OS 内核赛自动化代码级架构审计报告

**审计时间**: `2026-07-02 02:40:06`
**参赛作品名称**: `2026-T202610006999664-XX`
**年份**: `2026`
**学校**: `XX`
**队伍名称**: `T202610006999664`
**比赛名字**: `OS-Competition`
**比赛子赛道**: `Kernel-Implementation`
**仓库地址**: `https://gitlab.eduxiji.net/educg-group-43501-3132633/T202610006999664-263`
**推测开发语言**: `Rust / C (由AST探针自动识别)`
**特征函数量**: `90`

## ⚔️ 底层代码同源性对比 (AST Homology)
> 本部分由专门的代码审查智能体，对该项目与符合年份条件的历届最高相似度项目进行控制流与底层调用对比。新版报告会先给出确定性的全模块覆盖矩阵，再给出 Agent 的分模块解释，避免只围绕少量抽样函数展开。

### 🏆 Top 1 底层对标: vs `2023-你宁可空转也不愿被我抢占-西安邮电大学`（西安邮电大学 / 你宁可空转也不愿被我抢占）

#### 对方作品信息

**对方年份**: `2023`
**对方学校**: `西安邮电大学`
**对方队伍名称**: `你宁可空转也不愿被我抢占`
**对方比赛名字**: `OS-Competition`
**对方比赛子赛道**: `Kernel-Implementation`
**对方仓库地址**: `https://gitlab.eduxiji.net/202311664101068/oskernel2023-xupt-orz`

> 双向覆盖率：共同 AST 指纹: 2；唯一 AST 指纹: 目标项目 85，参考项目 52；覆盖率: 目标项目 2.35%，参考项目 3.85%

## 全模块对比总览（确定性证据）

> 总体覆盖率：共同 AST 指纹: 2；唯一 AST 指纹: 目标项目 85，参考项目 52；覆盖率: 目标项目 2.35%，参考项目 3.85%

### 1. 模块覆盖矩阵
| 模块 | 目标函数数 | 参考函数数 | 共同唯一 AST 指纹 | Target 覆盖 | Base 覆盖 | 状态 |
| :--- | ---: | ---: | ---: | ---: | ---: | :--- |
| 进程管理 | 8 | 0 | 0 | 0.0% | 0.0% | 仅目标项目体现 |
| 任务调度/异步运行时 | 5 | 12 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |
| 内存管理 | 52 | 15 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |
| 中断/异常处理 | 2 | 9 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |
| 系统调用 | 10 | 0 | 0 | 0.0% | 0.0% | 仅目标项目体现 |
| 文件系统 | 1 | 1 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |
| 设备驱动 | 0 | 4 | 0 | 0.0% | 0.0% | 仅参考项目体现 |
| 网络协议栈 | 0 | 0 | 0 | 0.0% | 0.0% | 双方均未体现 |
| 通用模块/其他 | 12 | 11 | 2 | 16.67% | 18.18% | 双方存在且有结构重合 |

### 2. 分模块证据清单

#### 进程管理
仅目标项目中观察到该模块代表函数，参考项目没有同模块可对齐证据：
- 目标项目：`kernel/process.c#load_elf`，调用摘要：load_segment, user_range_valid
- 目标项目：`kernel/process.c#process_init_first`，调用摘要：(usize), memset, process_load
- 目标项目：`kernel/process.c#process_set_status`，调用摘要：printk, process_find
- 目标项目：`kernel/process.c#process_destroy`，调用摘要：printk, process_find, process_release_resources

#### 任务调度/异步运行时
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `kernel/console.c#vprintk` | `kernel/diver/printf.c#_vsnprintf` | 同模块上下文对齐/差异观察 | console_putchar, console_puts, print_signed, print_unsigned, va_arg | va_arg |
| `kernel/syscall.c#sys_write_dev` | `kernel/trap/trap.c#trap_handler` | 同模块上下文对齐/差异观察 | IS_ALIGNED, device_range_valid, is_power_of_two, process_current, user_read_u8, user_va_range_valid | r_sip, timer_handler, uart_puts, w_sip |
| `kernel/syscall.c#sys_read_dev` | `kernel/trap/timer.c#timer_create` | 同模块上下文对齐/差异观察 | (u8), IS_ALIGNED, device_range_valid, is_power_of_two, process_current, user_va_range_valid ...(+1) | spin_lock, spin_unlock |
| `kernel/init.c#main` | `kernel/sched/co_sched.c#task_create` | 同模块上下文对齐/差异观察 | pmm_init, pmm_selftest, printk, process_init_first, process_run_first, runtime_selftest ...(+4) | 内部闭环逻辑 |

#### 内存管理
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `kernel/pmm.c#page_free` | `kernel/memory/page.c#page_free` | 同板块同名 | panic | list_add |
| `kernel/pmm.c#page_alloc` | `kernel/memory/page.c#page_alloc` | 同板块同名 | memset, page2kva | __list_del_entry, list_entry, memset, page2PA, printf, r_hartid |
| `kernel/process.c#process_exofork` | `kernel/memory/memory.c#vm_init` | 同模块上下文对齐/差异观察 | ROUND_DOWN, alloc_process_slot, memcpy, memset, pa2page, page_ref_dec ...(+8) | page_insert, printf |
| `kernel/syscall.c#syscall_dispatch` | `kernel/memory/page.c#page_walk` | 同模块上下文对齐/差异观察 | process_current, process_destroy, process_exofork, process_set_status, sys_cgetc, sys_ipc_recv ...(+8) | GET_PAGE_TABLE_INDEX, PTE2PA, page2PTE, page_alloc |

#### 中断/异常处理
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `kernel/trap.c#trap_init` | `kernel/trap/trap.c#trap_init` | 同板块同名 | csr_write_stvec, printk | w_stvec |
| `kernel/timer.c#timer_handle_interrupt` | `kernel/trap/timer.c#timer_load` | 同模块上下文对齐/差异观察 | printk, timer_set_next | r_mtime |

#### 系统调用
仅目标项目中观察到该模块代表函数，参考项目没有同模块可对齐证据：
- 目标项目：`kernel/sbi.c#sbi_ecall`，调用摘要：内部闭环逻辑
- 目标项目：`kernel/syscall.c#device_range_valid`，调用摘要：内部闭环逻辑
- 目标项目：`kernel/sbi.c#sbi_console_getchar`，调用摘要：内部闭环逻辑
- 目标项目：`kernel/syscall.c#user_va_range_valid`，调用摘要：range_overlaps

#### 文件系统
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `kernel/console.c#panic_impl` | `kernel/diver/printf.c#__panic` | 同模块上下文对齐/差异观察 | console_putchar, printk, va_end, va_start, vprintk | __printf_nolock, _vprintf, r_hartid, va_end, va_start |

#### 设备驱动
仅参考项目中观察到该模块代表函数，目标项目没有同模块可对齐证据：
- 参考项目：`kernel/diver/uart.c#getchar`，调用摘要：readl
- 参考项目：`kernel/diver/uart.c#putchar`，调用摘要：readl, writel
- 参考项目：`kernel/diver/uart.c#__raw_readl`，调用摘要：内部闭环逻辑
- 参考项目：`kernel/diver/uart.c#__raw_writel`，调用摘要：内部闭环逻辑

#### 通用模块/其他
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `lib/string.c#memcpy` | `kernel/lib/memlib.c#memcpy` | 同板块同名 | 内部闭环逻辑 | 内部闭环逻辑 |
| `lib/string.c#memset` | `kernel/lib/memlib.c#memset` | 同板块同名 | 内部闭环逻辑 | 内部闭环逻辑 |
| `kernel/console.c#printk` | `kernel/diver/printf.c#__printf_nolock` | 同板块同结构 | va_end, va_start, vprintk | _vprintf, va_end, va_start |
| `kernel/console.c#console_puts` | `kernel/diver/printf.c#putstring` | 同板块同结构 | console_putchar | putchar |


#### 1. 宏观判定
- **定性结论**: 底层架构显著差异 / 独立开发 (相似度: 1.48%，共同 AST 指纹: 2；唯一 AST 指纹: 目标项目 85，参考项目 52；覆盖率: 目标项目 2.35%，参考项目 3.85%)

#### 2. 全模块覆盖矩阵解读
- **进程管理**: 仅目标项目存在（8个函数），参考项目无此模块。共同AST指纹为0。此模块为目标项目独有，体现了其基于进程的宏内核设计，与参考项目的协程模型形成根本性架构差异。证据强度：未形成。
- **任务调度/异步运行时**: 双方均存在（目标5个，参考12个）。共同AST指纹为0。目标项目函数（如`sys_write_dev`）与参考项目函数（如`trap_handler`）虽被归入同板块，但调用图谱无任何重合，表明两者调度与运行时机制完全不同。证据强度：未形成。
- **内存管理**: 双方均存在（目标52个，参考15个）。共同AST指纹为0。尽管存在`page_alloc`等同名函数，但外部调用图谱揭示了截然不同的实现：目标项目采用引用计数（`page_ref_inc/dec`）和写时复制（`process_handle_cow_fault`），参考项目则基于链表（`list_entry`）和直接物理地址操作。证据强度：未形成。
- **中断/异常处理**: 双方均存在（目标2个，参考9个）。共同AST指纹为0。`trap_init`同名，但这是RISC-V架构规范强制的标准步骤，且目标项目使用SBI封装（`csr_write_stvec`），参考项目直接写寄存器（`w_stvec`），抽象层次不同。证据强度：未形成。
- **系统调用**: 仅目标项目存在（10个函数），参考项目无此模块。共同AST指纹为0。这进一步印证了目标项目是提供用户态接口的宏内核，而参考项目可能是运行在机器态的裸机或协程内核。证据强度：未形成。
- **文件系统**: 双方均存在（各1个函数）。共同AST指纹为0。目标项目的`panic_impl`与参考项目的`__panic`均使用C标准变参处理，但输出后端（`console_putchar` vs `_vprintf`）完全不同，属于通用模式的不同实现。证据强度：未形成。
- **设备驱动**: 仅参考项目存在（4个函数），目标项目无此模块。共同AST指纹为0。参考项目包含独立的UART驱动，而目标项目通过SBI抽象硬件，这是两者硬件抽象层的核心差异。证据强度：未形成。
- **网络协议栈**: 双方均未体现。
- **第三方库/公共依赖**: 双方均未体现。

#### 3. 底层 AST 关键证据指纹表
| 所属板块 | 目标项目路径 | 参考项目路径 | 证据类型 | 技术说明 |
| :--- | :--- | :--- | :--- | :--- |
| 通用模块/其他 | `lib/string.c#memcpy` | `kernel/lib/memlib.c#memcpy` | 同板块同名 | 均为C标准库函数的内部闭环实现，属于公开领域知识，不构成特有指纹。 |
| 通用模块/其他 | `lib/string.c#memset` | `kernel/lib/memlib.c#memset` | 同板块同名 | 均为C标准库函数的内部闭环实现，属于公开领域知识，不构成特有指纹。 |

#### 4. 核心代码调用流对比剖析
* **【内存管理/页表遍历】**
* **底层技术析理**: 目标项目的`walk`函数与参考项目的`page_walk`函数均实现RISC-V页表遍历。然而，目标项目在遍历中集成了引用计数（`page_ref_inc`）和叶子节点判断（`pte_is_leaf`），暗示其对巨页和物理页共享的管理。参考项目则更纯粹地聚焦于页表项的查找与分配（`page2PTE`）。两者在内存管理模型上存在根本差异：一个是引用计数模型，一个是直接操作模型。
* **代码溯源链接**:
  - 目标项目: `kernel/vmm.c#walk`
  - 参考项目: `kernel/memory/page.c#page_walk`

* **【中断/异常处理/定时器】**
* **底层技术析理**: 目标项目的`timer_set_next`通过SBI接口`csr_read_time`和`sbi_set_timer`设置下一次中断，这是RISC-V标准的S-mode调用规范。参考项目的`timer_load`则直接读取`r_mtime`寄存器，属于M-mode或裸机直接硬件操作。两者处于不同的特权级和硬件抽象层，调用流完全不同。
* **代码溯源链接**:
  - 目标项目: `kernel/timer.c#timer_set_next`
  - 参考项目: `kernel/trap/timer.c#timer_load`

* **【通用模块/其他/格式化输出】**
* **底层技术析理**: 目标项目的`printk`和参考项目的`printf`均采用`va_start -> 核心格式化 -> va_end`的C语言变参标准结构。但核心格式化引擎不同，目标项目调用`vprintk`，参考项目调用`_vprintf`。这属于同一通用模式下的不同实现，而非代码复用。
* **代码溯源链接**:
  - 目标项目: `kernel/console.c#printk`
  - 参考项目: `kernel/diver/printf.c#printf`

#### 5. 单方模块与降权项
- **单方模块**:
  - **进程管理** (目标项目独有): 包含`process_exofork`、`process_load`等完整进程生命周期管理，是宏内核的核心特征，参考项目完全缺失。
  - **系统调用** (目标项目独有): 包含`syscall_dispatch`、`sys_mem_map`等用户态接口，进一步证实其宏内核定位。
  - **设备驱动** (参考项目独有): 包含`uart.c`等裸机驱动，表明其直接管理硬件，与目标项目依赖SBI形成对比。
  这些单方模块是两者架构异构性的铁证，不能作为同源证据。
- **降权项**:
  - **跨板块同名/同语义**: 如目标项目的`vprintk`（任务调度）与参考项目的`_vsnprintf`（任务调度），虽在同一板块对齐，但功能（内核打印 vs 字符串格式化）和调用上下文完全不同，属于弱信号。
  - **通用术语/标准库函数**: `page_alloc`、`page_free`、`trap_init`、`memset`、`memcpy`等命名和实现，属于操作系统领域的通用术语或C标准库的公开知识，不构成特有指纹，已作降权处理。

#### 6. 保守结论
本次审计基于极低的AST相似度（1.48%）和覆盖率（2.35%/3.85%），判定目标项目与参考项目为独立开发。仅有的2个共同指纹落在`memcpy`和`memset`这类标准C库函数上，属于不可保护的公共领域知识。双方在核心架构上存在根本性分歧：目标项目是基于进程、系统调用、SBI抽象和COW的宏内核；参考项目是基于协程、自旋锁和裸机UART驱动的内核。内存管理、中断处理等关键模块的调用图谱揭示了完全不同的抽象层次与实现策略。控方所指控的复用，实为将硬件强制步骤、C语言惯用法和通用OS术语误判为独创表达。综合所有证据，目标项目不存在违规复用行为，原创性成立。

------------------------------

