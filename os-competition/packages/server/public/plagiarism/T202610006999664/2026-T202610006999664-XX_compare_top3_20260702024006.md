# 🛡️ OS 内核赛自动化代码级架构审计报告

**审计时间**: `2026-07-02 02:40:06`
**参赛作品名称**: `2026-T202610006999664-XX`
**年份**: `2026`
**学校**: `XX`
**队伍名称**: `T202610006999664`
**比赛名字**: `OS-Competition`
**比赛子赛道**: `Kernel-Implementation`
**仓库地址**: `https://gitlab.eduxiji.net/educg-group-43501-3132633/T202610006999664-263`
**推测开发语言**: `Rust / C (由AST探针自动识别)`
**特征函数量**: `90`

## ⚔️ 底层代码同源性对比 (AST Homology)
> 本部分由专门的代码审查智能体，对该项目与符合年份条件的历届最高相似度项目进行控制流与底层调用对比。新版报告会先给出确定性的全模块覆盖矩阵，再给出 Agent 的分模块解释，避免只围绕少量抽样函数展开。

### 🏆 Top 3 底层对标: vs `2021-TeamNameNotFound-重庆邮电大学`（重庆邮电大学 / TeamNameNotFound）

#### 对方作品信息

**对方年份**: `2021`
**对方学校**: `重庆邮电大学`
**对方队伍名称**: `TeamNameNotFound`
**对方比赛名字**: `OS-Competition`
**对方比赛子赛道**: `Kernel-Implementation`
**对方仓库地址**: `https://gitlab.eduxiji.net/qq976243638/xos`

> 双向覆盖率：共同 AST 指纹: 2；唯一 AST 指纹: 目标项目 85，参考项目 78；覆盖率: 目标项目 2.35%，参考项目 2.56%

## 全模块对比总览（确定性证据）

> 总体覆盖率：共同 AST 指纹: 2；唯一 AST 指纹: 目标项目 85，参考项目 78；覆盖率: 目标项目 2.35%，参考项目 2.56%

### 1. 模块覆盖矩阵
| 模块 | 目标函数数 | 参考函数数 | 共同唯一 AST 指纹 | Target 覆盖 | Base 覆盖 | 状态 |
| :--- | ---: | ---: | ---: | ---: | ---: | :--- |
| 进程管理 | 8 | 14 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |
| 任务调度/异步运行时 | 5 | 4 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |
| 内存管理 | 52 | 34 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |
| 中断/异常处理 | 2 | 2 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |
| 系统调用 | 10 | 0 | 0 | 0.0% | 0.0% | 仅目标项目体现 |
| 文件系统 | 1 | 9 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |
| 设备驱动 | 0 | 4 | 0 | 0.0% | 0.0% | 仅参考项目体现 |
| 网络协议栈 | 0 | 0 | 0 | 0.0% | 0.0% | 双方均未体现 |
| 第三方库 | 0 | 18 | 0 | 0.0% | 0.0% | 仅参考项目体现 |
| 通用模块/其他 | 12 | 0 | 0 | 0.0% | 0.0% | 仅目标项目体现 |

### 2. 分模块证据清单

#### 进程管理
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `kernel/process.c#load_elf` | `kernel/proc/proc.c#proc_wait` | 同模块上下文对齐/差异观察 | load_segment, user_range_valid | free_proc, kfree, list_rm, proc_switch |
| `kernel/process.c#process_init_first` | `kernel/proc/proc.c#proc_init` | 同模块上下文对齐/差异观察 | (usize), memset, process_load | init_fd_table, sche_ptr->init |
| `kernel/process.c#process_set_status` | `kernel/proc/proc.c#init_fd_table` | 同模块上下文对齐/差异观察 | printk, process_find | 内部闭环逻辑 |
| `kernel/process.c#process_destroy` | `kernel/proc/sche_fifo.c#dequeue` | 同模块上下文对齐/差异观察 | printk, process_find, process_release_resources | isempty |

#### 任务调度/异步运行时
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `kernel/console.c#vprintk` | `kernel/trap/syscall.c#syscall` | 同模块上下文对齐/差异观察 | console_putchar, console_puts, print_signed, print_unsigned, va_arg | fs_close, fs_openat, fs_read, proc_clone, proc_execve, proc_exit ...(+2) |
| `kernel/syscall.c#sys_write_dev` | `kernel/trap/trap.c#trap_ret_user` | 同模块上下文对齐/差异观察 | IS_ALIGNED, device_range_valid, is_power_of_two, process_current, user_read_u8, user_va_range_valid | csrr, csrw, trap_end |
| `kernel/syscall.c#sys_read_dev` | `kernel/fs/fat32.c#fat32_read` | 同模块上下文对齐/差异观察 | (u8), IS_ALIGNED, device_range_valid, is_power_of_two, process_current, user_va_range_valid ...(+1) | kfree, load_file, memcpy |
| `kernel/init.c#main` | `kernel/fs/fs.c#fs_close` | 同模块上下文对齐/差异观察 | pmm_init, pmm_selftest, printk, process_init_first, process_run_first, runtime_selftest ...(+4) | 内部闭环逻辑 |

#### 内存管理
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `kernel/pmm.c#pmm_init` | `kernel/mem/pmm.c#pmm_init` | 同板块同名 | ROUND_UP, free_range, panic, printk | ROUNDUP, free_a_page |
| `kernel/process.c#process_exofork` | `kernel/fs/elf.c#load_elf` | 同模块上下文对齐/差异观察 | ROUND_DOWN, alloc_process_slot, memcpy, memset, pa2page, page_ref_dec ...(+8) | (uint64), ROUNDDOWN, flags_trans, kalloc, list_add_back, magic_test ...(+4) |
| `kernel/syscall.c#syscall_dispatch` | `kernel/batch/app.c#app_init` | 同模块上下文对齐/差异观察 | process_current, process_destroy, process_exofork, process_set_status, sys_cgetc, sys_ipc_recv ...(+8) | alloc_proc, fs_close, fs_openat, fs_read, init_fd_table, kalloc ...(+6) |
| `kernel/syscall.c#sys_ipc_try_send` | `kernel/proc/proc.c#proc_clone` | 同模块上下文对齐/差异观察 | IS_ALIGNED, ROUND_DOWN, pa2page, page_ref_dec, page_ref_inc, process_current ...(+7) | ROUNDDOWN, alloc_proc, copy_segments, copy_trapframe, init_fd_table, kalloc ...(+3) |

#### 中断/异常处理
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `kernel/trap.c#trap_init` | `kernel/trap/trap.c#trap_init` | 同板块同名 | csr_write_stvec, printk | csrr, csrw |
| `kernel/timer.c#timer_handle_interrupt` | `kernel/trap/syscall.c#sys_write` | 同模块上下文对齐/差异观察 | printk, timer_set_next | console_putchar |

#### 系统调用
仅目标项目中观察到该模块代表函数，参考项目没有同模块可对齐证据：
- 目标项目：`kernel/sbi.c#sbi_ecall`，调用摘要：内部闭环逻辑
- 目标项目：`kernel/syscall.c#device_range_valid`，调用摘要：内部闭环逻辑
- 目标项目：`kernel/sbi.c#sbi_console_getchar`，调用摘要：内部闭环逻辑
- 目标项目：`kernel/syscall.c#user_va_range_valid`，调用摘要：range_overlaps

#### 文件系统
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `kernel/console.c#panic_impl` | `kernel/fs/fat32.c#fname2short` | 同模块上下文对齐/差异观察 | console_putchar, printk, va_end, va_start, vprintk | strcpy, strlen, toupper |

#### 设备驱动
仅参考项目中观察到该模块代表函数，目标项目没有同模块可对齐证据：
- 参考项目：`kernel/driver/virt_disk.c#disk_read_sector`，调用摘要：memcpy
- 参考项目：`kernel/driver/clock.c#clock_init`，调用摘要：csrr, csrw, set_next_timer
- 参考项目：`kernel/driver/clock.c#set_next_timer`，调用摘要：csrr, set_timer
- 参考项目：`kernel/driver/virt_disk.c#disk_write_sector`，调用摘要：内部闭环逻辑

#### 第三方库
仅参考项目中观察到该模块代表函数，目标项目没有同模块可对齐证据：
- 参考项目：`kernel/libs/sbi.c#sbi_ecall`，调用摘要：内部闭环逻辑
- 参考项目：`kernel/libs/list.c#list_add_back`，调用摘要：内部闭环逻辑
- 参考项目：`kernel/libs/list.c#list_rm_front`，调用摘要：panic
- 参考项目：`kernel/libs/list.c#list_rm`，调用摘要：list_rm_front

#### 通用模块/其他
仅目标项目中观察到该模块代表函数，参考项目没有同模块可对齐证据：
- 目标项目：`kernel/console.c#print_unsigned`，调用摘要：console_putchar
- 目标项目：`lib/string.c#memcmp`，调用摘要：内部闭环逻辑
- 目标项目：`kernel/console.c#print_signed`，调用摘要：(u64), console_putchar, print_unsigned
- 目标项目：`kernel/timer.c#timer_set_next`，调用摘要：csr_read_time, panic, sbi_set_timer


好的，审计官。基于提供的证据底座、控辩双方陈述及客观 AST 数据，我将依据审计指令进行最终定性仲裁。

### 1. 宏观判定
- **定性结论**: **独立开发**
  底层架构存在显著差异。客观 AST 相似度仅为 1.24%，共同 AST 指纹仅有 2 个，双向覆盖率均低于 3%，处于统计噪声水平。控方提出的“复用指控”主要建立在跨板块硬凑配对和对操作系统领域共性范式的误读之上，缺乏排他性的同源证据。辩方对架构差异的辩护逻辑清晰，与客观数据相符。

### 2. 全模块覆盖矩阵解读
| 模块 | 双方存在性 | 共同 AST 指纹 | 覆盖率解读 | 证据强度 |
| :--- | :--- | :--- | :--- | :--- |
| **进程管理** | 双方均存在 | 0 | 目标 0.0%，参考 0.0%。双方在进程创建、销毁、调度等核心路径上未发现任何共同结构指纹。 | **未形成** |
| **任务调度/异步运行时** | 双方均存在 | 0 | 目标 0.0%，参考 0.0%。调度与运行时初始化逻辑无共同指纹，控方将 `vprintk` 与 `syscall` 硬凑配对无效。 | **未形成** |
| **内存管理** | 双方均存在 | 0 | 目标 0.0%，参考 0.0%。尽管存在同名函数 `pmm_init`，但未产生共同 AST 指纹，表明其内部实现逻辑不同。 | **未形成** |
| **中断/异常处理** | 双方均存在 | 0 | 目标 0.0%，参考 0.0%。Trap 处理入口与分发逻辑无共同指纹。 | **未形成** |
| **系统调用** | 仅目标项目 | 0 | 目标 0.0%，参考 0.0%。目标项目有独立的系统调用模块，参考项目无此独立分类，架构不同。 | **未形成** |
| **文件系统** | 双方均存在 | 0 | 目标 0.0%，参考 0.0%。目标项目文件系统极简（仅1个函数），参考项目有完整的 FAT32 实现，规模与复杂度差异巨大。 | **未形成** |
| **设备驱动** | 仅参考项目 | 0 | 目标 0.0%，参考 0.0%。参考项目包含磁盘和时钟驱动，目标项目无此模块。 | **未形成** |
| **网络协议栈** | 双方均无 | 0 | 双方均未体现。 | **未形成** |
| **第三方库/公共依赖** | 仅参考项目 | 0 | 目标 0.0%，参考 0.0%。参考项目使用了链表等公共库，目标项目未体现。 | **未形成** |

### 3. 底层 AST 关键证据指纹表
> 所有配对均为“差异观察”，未发现可认定为同源的强证据指纹。

| 所属板块 | 目标项目路径 | 参考项目路径 | 证据类型 | 技术说明 |
| :--- | :--- | :--- | :--- | :--- |
| 内存管理 | `kernel/pmm.c#pmm_init` | `kernel/mem/pmm.c#pmm_init` | 同名函数差异观察 | 同名但无共同 AST 指纹。目标调用 `free_range` 按区域释放，参考调用 `free_a_page` 按单页释放，核心算法粒度不同。 |
| 内存管理 | `kernel/vmm.c#walk` | `kernel/mem/page.c#map_a_page` | 核心机制差异观察 | 目标将页表遍历（`walk`）与映射（`vmm_map`）解耦；参考将映射内联在 `map_a_page` 中。两者控制流与抽象层次不同。 |
| 内存管理 | `kernel/process.c#load_elf` | `kernel/fs/elf.c#load_elf` | 跨模块差异观察 | 目标 ELF 加载位于进程管理模块，依赖 `page_alloc`；参考 ELF 加载位于文件系统模块，依赖 `kalloc` 和 `fs_read`。架构归属与依赖完全不同。 |
| 进程管理 | `kernel/process.c#process_exofork` | `kernel/proc/proc.c#proc_clone` | 算法差异观察 | 目标显式操作 `page_ref_inc/dec` 实现页框级 COW；参考通过 `copy_segments` 实现，未暴露引用计数。内存管理策略不同。 |
| 进程管理 | `kernel/process.c#process_destroy` | `kernel/proc/proc.c#proc_exit` | 控制流差异观察 | 目标聚合资源释放为 `process_release_resources`；参考分散释放 `free_fd_table`、`free_segments` 并调用 `proc_switch`。进程模型不同。 |
| 中断/异常处理 | `kernel/trap.c#trap_init` | `kernel/trap/trap.c#trap_init` | 同名函数差异观察 | 同名但无共同 AST 指纹。目标调用 `csr_write_stvec`，参考调用 `csrr/csrw`，初始化序列不同。 |

### 4. 核心代码调用流对比剖析
*   **【内存管理/页表映射路径】**
    *   **底层技术析理**: 两者均实现了 RISC-V 页表遍历，但抽象层次与内存模型截然不同。目标项目采用显式的 `walk` 函数进行多级页表遍历，返回页表项指针，再由 `vmm_map` 负责填充和 TLB 刷新，实现了遍历与映射的解耦。其依赖 `page_alloc` 和 `page_ref_inc`，表明使用了基于页框结构体的引用计数管理。参考项目则将遍历、叶子节点检查（`check_pde`）和映射逻辑内联在 `map_a_page` 函数中，依赖 `kalloc` 进行页分配，未显式暴露引用计数机制。两者在同步原语（目标使用 `sfence_vma` 显式刷新，参考未在映射路径体现）和对象管理模型上存在根本性差异。
    *   **代码溯源链接**:
        - 目标项目: `kernel/vmm.c#walk` -> `kernel/vmm.c#vmm_map`
        - 参考项目: `kernel/mem/page.c#map_a_page`

*   **【进程管理/进程创建路径】**
    *   **底层技术析理**: 目标项目的进程创建通过 `process_exofork` 实现，其核心调用流为 `alloc_process_slot` -> `memcpy`/`memset` -> `pa2page`/`page_ref_inc`，显式地操作物理页框引用计数来构建 COW 机制。参考项目的进程创建通过 `proc_clone` 实现，其调用流为 `alloc_proc` -> `copy_trapframe`/`copy_segments` -> `init_fd_table` -> `run_proc`，包含了文件描述符表的初始化，且未在克隆路径暴露底层页引用计数。这表明两者在进程模型（有无 fd 表）和内存克隆策略（页框级 vs 段拷贝）上存在显著架构差异。
    *   **代码溯源链接**:
        - 目标项目: `kernel/process.c#process_exofork`
        - 参考项目: `kernel/proc/proc.c#proc_clone`

### 5. 单方模块与降权项
*   **仅目标项目存在**:
    *   **系统调用模块**: 目标项目有独立的 `syscall_dispatch` 及 `sys_ipc_try_send`、`sys_mem_map` 等 IPC/内存系统调用，体现了其微内核/IPC 倾向的架构设计。参考项目无此独立模块，系统调用分发内联在 `trap/syscall.c` 中，且主要围绕文件操作。此为架构差异，非复用证据。
    *   **通用模块/其他**: 目标项目包含 `console.c` 等独立打印模块，属于常规基础设施，不构成同源证据。
*   **仅参考项目存在**:
    *   **设备驱动**: 参考项目包含 `virt_disk.c`、`clock.c` 等驱动，目标项目无此模块。
    *   **第三方库/公共依赖**: 参考项目包含 `list.c` 链表库，属于通用数据结构，其存在与否不构成抄袭或原创的判据。
*   **降权项说明**: 控方提出的所有跨板块配对（如 `vprintk` vs `syscall`， `main` vs `fs_close`）均因功能模块归属不同、调用对象无关，根据审计指令第4条，降级为弱信号，不构成同板块主证据。

### 6. 保守结论
本次审计基于客观 AST 数据与严格的同板块对比原则。两项目在 1.24% 的极低相似度下，所有核心模块的共同 AST 指纹均为 0，表明不存在可观测的代码结构复用。控方指控主要源于对 `memcpy`、`ROUND_DOWN` 等标准范式及 `pmm_init` 等通用命名的误读，并将跨模块证据强行配对。辩方成功论证了两者在内存管理（页框引用计数 vs 内核堆分配）、进程模型（有无 fd 表）、系统调用架构（IPC 导向 vs 文件导向）等核心子系统上存在根本性设计差异。综合判定，目标项目为独立开发，无代码抄袭风险。

------------------------------

