# 🛡️ OS 内核赛自动化代码级架构审计报告

**审计时间**: `2026-07-02 11:18:05`
**参赛作品名称**: `2026-T202610486999573-XX`
**年份**: `2026`
**学校**: `XX`
**队伍名称**: `T202610486999573`
**比赛名字**: `OS-Competition`
**比赛子赛道**: `Kernel-Implementation`
**仓库地址**: `https://gitlab.eduxiji.net/educg-group-43501-3132633/T202610486999573-2679`
**推测开发语言**: `Rust / C (由AST探针自动识别)`
**特征函数量**: `311`

## ⚔️ 底层代码同源性对比 (AST Homology)
> 本部分由专门的代码审查智能体，对该项目与符合年份条件的历届最高相似度项目进行控制流与底层调用对比。新版报告会先给出确定性的全模块覆盖矩阵，再给出 Agent 的分模块解释，避免只围绕少量抽样函数展开。

### 🏆 Top 3 底层对标: vs `2024-小混子队-合肥工业大学`（合肥工业大学 / 小混子队）

#### 对方作品信息

**对方年份**: `2024`
**对方学校**: `合肥工业大学`
**对方队伍名称**: `小混子队`
**对方比赛名字**: `OS-Competition`
**对方比赛子赛道**: `Kernel-Implementation`
**对方仓库地址**: `https://gitlab.eduxiji.net/educg-group-26010-2376550/oskernel2024-xiaohunzi`

> 双向覆盖率：共同 AST 指纹: 19；唯一 AST 指纹: 目标项目 262，参考项目 298；覆盖率: 目标项目 7.25%，参考项目 6.38%

## 全模块对比总览（确定性证据）

> 总体覆盖率：共同 AST 指纹: 19；唯一 AST 指纹: 目标项目 262，参考项目 298；覆盖率: 目标项目 7.25%，参考项目 6.38%

### 1. 模块覆盖矩阵
| 模块 | 目标函数数 | 参考函数数 | 共同唯一 AST 指纹 | Target 覆盖 | Base 覆盖 | 状态 |
| :--- | ---: | ---: | ---: | ---: | ---: | :--- |
| 进程管理 | 20 | 50 | 2 | 10.53% | 4.76% | 双方存在且有结构重合 |
| 任务调度/异步运行时 | 38 | 29 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |
| 内存管理 | 81 | 119 | 3 | 3.75% | 2.65% | 双方存在且有结构重合 |
| 中断/异常处理 | 11 | 20 | 1 | 9.09% | 6.25% | 双方存在且有结构重合 |
| 系统调用 | 34 | 4 | 1 | 9.09% | 25.0% | 双方存在且有结构重合 |
| 文件系统 | 60 | 65 | 3 | 5.36% | 5.17% | 双方存在且有结构重合 |
| 设备驱动 | 8 | 16 | 2 | 25.0% | 15.38% | 双方存在且有结构重合 |
| 网络协议栈 | 0 | 0 | 0 | 0.0% | 0.0% | 双方均未体现 |
| 文件系统/第三方库 | 17 | 0 | 0 | 0.0% | 0.0% | 仅目标项目体现 |
| 通用模块/其他 | 42 | 40 | 6 | 18.75% | 16.22% | 双方存在且有结构重合 |

### 2. 分模块证据清单

#### 进程管理
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `kernel/syscall/sysfunc.c#sys_wait4` | `sysfproc.c#sys_wait4` | 同板块同名 | arg_int64, arg_uint64, proc_wait4 | argaddr, argint, waitpid |
| `kernel/syscall/sysfunc.c#sys_getppid` | `sysfproc.c#sys_getppid` | 同板块同名 | myproc | myproc |
| `kernel/syscall/sysfunc.c#sys_exit` | `sysfproc.c#sys_exit` | 同板块同名 | arg_uint32, proc_exit | argint, exit, myproc |
| `kernel/proc/cpu.c#mycpu` | `proc.c#mycpu` | 同板块同名 | r_tp | cpuid |

#### 任务调度/异步运行时
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `kernel/syscall/syscall.c#syscall` | `syscall.c#syscall` | 同板块同名 | myproc, printf, sys_brk, sys_chdir, sys_clone, sys_close ...(+30) | NELEM, myproc, syscalls[num] |
| `kernel/syscall/sysfunc.c#sys_chdir` | `sysfile.c#sys_chdir` | 同板块同名 | arg_uint64, myproc, safestrcpy, strlen, uvm_copyin_str | argstr, ilock, iput, iunlock, iunlockput, myproc ...(+1) |
| `kernel/trap/trap_kernel.c#trap_kernel_handler` | `sys_pselect.c#sys_pselect6` | 同模块上下文对齐/差异观察 | assert, external_interrupt_handler, intr_get, myproc, panic, printf ...(+10) | argaddr, argint, clear_bit, convert_from_timespec2, either_copyin, either_copyout ...(+5) |
| `kernel/trap/trap_user.c#trap_user_handler` | `syscall.c#sys_sysinfo` | 同模块上下文对齐/差异观察 | assert, external_interrupt_handler, intr_on, myproc, panic, printf ...(+10) | argaddr, either_copyout, freemem_amount, memset, procnum, r_time |

#### 内存管理
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `kernel/dev/virtio.c#virtio_disk_rw` | `virtio_disk.c#virtio_disk_rw` | 同板块同名 | ALIGN_DOWN, PTE_TO_PA, R, __sync_synchronize, alloc3_desc, free_chain ...(+3) | R, __sync_synchronize, acquire, alloc3_desc, free_chain, release ...(+1) |
| `kernel/dev/virtio.c#virtio_disk_init` | `virtio_disk.c#virtio_disk_init` | 同板块同名 | R, memset, panic, spinlock_init | R, initlock, memset, panic |
| `kernel/syscall/sysfunc.c#sys_mmap` | `sysfproc.c#sys_mmap` | 同板块同名 | ALIGN_UP, arg_uint64, fd_get, file_read, memset, myproc ...(+2) | argaddr, argfd, argraw, myproc, readi, uvmalloc |
| `kernel/lib/str.c#memmove` | `string.c#memmove` | 同板块同名 | 内部闭环逻辑 | 内部闭环逻辑 |

#### 中断/异常处理
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `kernel/lib/spinlock.c#pop_off` | `spinlock.c#pop_off` | 同板块同名 | intr_get, intr_on, mycpu, panic | intr_get, intr_on, mycpu, panic |
| `kernel/lib/spinlock.c#push_off` | `spinlock.c#push_off` | 同板块同名 | intr_get, intr_off, mycpu | intr_get, intr_off, mycpu |
| `kernel/dev/uart.c#uart_intr` | `uart.c#uart_intr` | 同板块同名 | uart_getc_sync, uart_putc_sync | acquire, consoleintr, release, uart_getc, uart_start |
| `kernel/trap/trap_kernel.c#trap_kernel_init` | `disk.c#disk_intr` | 同板块同结构 | timer_create | virtio_disk_intr |

#### 系统调用
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `kernel/syscall/sysfunc.c#sys_getegid` | `sys_pselect.c#sys_ppoll` | 同板块同结构 | 内部闭环逻辑 | 内部闭环逻辑 |
| `user/initcode.c#main` | `sys_pselect.c#clear_bit` | 同模块上下文对齐/差异观察 | syscall | BIT_WORD |
| `kernel/syscall/sysfunc.c#ramdir_init` | `sys_pselect.c#convert_from_timespec2` | 同模块上下文对齐/差异观察 | memset, safestrcpy | 内部闭环逻辑 |
| `kernel/syscall/sysfunc.c#ramdir_add` | `sys_pselect.c#test_bit` | 同模块上下文对齐/差异观察 | ramdir_exists, safestrcpy | BIT_WORD |

#### 文件系统
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `mkfs/mkfs.c#xint` | `mkfs.c#xint` | 同板块同名 | 内部闭环逻辑 | 内部闭环逻辑 |
| `mkfs/mkfs.c#xshort` | `mkfs.c#xshort` | 同板块同名 | 内部闭环逻辑 | 内部闭环逻辑 |
| `user/user_syscall.c#sys_lseek` | `sysfile.c#sys_lseek` | 同板块同名 | syscall | argaddr, argfd, argint |
| `kernel/syscall/sysfunc.c#sys_umount2` | `sysfile.c#sys_umount2` | 同板块同名 | 内部闭环逻辑 | acquire, argstr, devput, get_loop_devi, iput, namei ...(+1) |

#### 设备驱动
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `kernel/dev/uart.c#uart_init` | `uart.c#uart_init` | 同板块同名 | WriteReg | initlock, uart_read_reg, uart_write_reg |
| `kernel/dev/virtio.c#free_chain` | `virtio_disk.c#free_chain` | 同板块同名同结构 | free_desc | free_desc |
| `kernel/dev/uart.c#uart_getc_sync` | `uart.c#uart_getc` | 同板块同结构 | ReadReg | uart_read_reg |
| `loongarch/main.c#putc` | `uart.c#uart_putc` | 同模块上下文对齐/差异观察 | 内部闭环逻辑 | acquire, release, sleep, uart_start |

#### 文件系统/第三方库
仅目标项目中观察到该模块代表函数，参考项目没有同模块可对齐证据：
- 目标项目：`kernel/fs/ext4.c#ext4_init`，调用摘要：cache_basic_tests, find_in_dir, memset, pmem_alloc, pmem_free, printf ...(+1)
- 目标项目：`kernel/fs/ext4.c#cache_basic_tests`，调用摘要：inode_size_bytes, memmove, memset, pmem_alloc, pmem_free, read_inode ...(+1)
- 目标项目：`kernel/fs/ext4.c#find_in_dir`，调用摘要：inode_size_bytes, memset, name_eq, pmem_alloc, pmem_free, read_inode ...(+1)
- 目标项目：`kernel/fs/ext4.c#read_extent_tree`，调用摘要：copy_extent_blocks, idx_leaf, pmem_alloc, pmem_free, read_block, read_extent_tree

#### 通用模块/其他
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `user/user_lib.c#printint` | `first.c#printint` | 同板块同名 | stdout | putc |
| `user/user_lib.c#printptr` | `first.c#printptr` | 同板块同名 | stdout | putc |
| `kernel/lib/str.c#memcmp` | `string.c#memcmp` | 同板块同名 | 内部闭环逻辑 | 内部闭环逻辑 |
| `kernel/lib/str.c#safestrcpy` | `string.c#safestrcpy` | 同板块同名同结构 | 内部闭环逻辑 | 内部闭环逻辑 |


好的，审计官。基于提供的证据底座、控辩双方陈述及原始AST抽样数据，我将进行最终的独立同源性定性仲裁。

### 1. 宏观判定
- **定性结论**: **独立开发 / 底层架构显著差异**
  - **客观 AST 相似度**: 3.51%
  - **双向覆盖率**: 目标项目 7.25%，参考项目 6.38%
  - **核心依据**: 极低的整体相似度与双向覆盖率表明，两个项目在代码实现层面存在根本性差异。19个共同AST指纹几乎全部对应操作系统内核领域的标准范式（如POSIX系统调用、xv6风格调度原语、通用字符串/锁操作），属于领域强制设计，而非独创性代码表达的复用。控方所指控的“复用”实为对通用内核设计模式的误读。

### 2. 全模块覆盖矩阵解读
| 模块 | 双方存在 | 共同指纹 | 目标覆盖 | 参考覆盖 | 证据强度 | 解读 |
| :--- | :---: | :---: | :---: | :---: | :---: | :--- |
| **进程管理** | 是 | 2 | 10.53% | 4.76% | **弱** | 共同指纹极少，覆盖率低。重合点仅限于`sys_getpid`等POSIX标准接口，其单节点调用图谱是语义强制的结果。核心调度与退出逻辑的调用链存在显著差异。 |
| **任务调度/异步运行时** | 是 | 0 | 0.0% | 0.0% | **未形成** | 该模块是控方指控的重点，但客观AST指纹重合数为零，彻底否定了系统性复用的可能。控方证据多为跨板块硬凑或对通用模式的曲解。 |
| **内存管理** | 是 | 3 | 3.75% | 2.65% | **弱** | 重合点在于`memmove`等通用基础库函数和`virtio_disk_rw`等设备驱动接口，这些是硬件交互的强制逻辑，不构成独创性复用证据。 |
| **中断/异常处理** | 是 | 1 | 9.09% | 6.25% | **弱** | 共同指纹`pop_off`是自旋锁的标准实现模式。`trap_kernel_handler`与`kerneltrap`的调用图谱差异巨大，分别侧重进程调度与寄存器操作，架构思路不同。 |
| **系统调用** | 是 | 1 | 9.09% | 25.0% | **弱** | 参考项目覆盖率较高因其函数总数少。共同指纹`sys_getegid`为内部闭环逻辑，属于简单功能函数。核心分发函数`syscall`的AST拓扑完全不同（静态展开 vs 函数指针表）。 |
| **文件系统** | 是 | 3 | 5.36% | 5.17% | **弱** | 重合点集中在`mkfs`工具函数和`sys_lseek`等标准系统调用封装。目标项目独有的`ext4`支持与参考项目的传统xv6文件系统在架构上存在本质区别。 |
| **设备驱动** | 是 | 2 | 25.0% | 15.38% | **中** | 覆盖率相对较高，但重合点`virtio_disk_rw`和`free_chain`是遵循virtio硬件协议的标准实现，其描述符链管理逻辑具有高度确定性，属于硬件强制设计。 |
| **网络协议栈** | 否 | 0 | 0.0% | 0.0% | **未形成** | 双方均未实现，无可比性。 |
| **文件系统/第三方库** | 仅目标 | 0 | 0.0% | 0.0% | **未形成** | 目标项目独有的`ext4`文件系统模块，是其独立开发的有力证据。 |
| **通用模块/其他** | 是 | 6 | 18.75% | 16.22% | **弱** | 覆盖率最高，但重合点均为`safestrcpy`、`memcmp`、`printint`等通用基础库函数，在任何C语言项目中都普遍存在，不具备独创性。 |

### 3. 底层 AST 关键证据指纹表
| 所属板块 | 目标项目路径 | 参考项目路径 | 证据类型 | 技术说明 |
| :--- | :--- | :--- | :--- | :--- |
| 进程管理 | `kernel/syscall/sysfunc.c#sys_getpid` | `sysfproc.c#sys_getpid` | 同板块同名 | POSIX标准接口，单节点调用`myproc`是语义强制的最简实现，不具独创性。 |
| 进程管理 | `kernel/proc/proc.c#proc_sched` | `proc.c#sched` | 同板块证据 | 上下文切换的标准安全检查序列（关中断、持锁、获取上下文），是操作系统课程的标准范式。 |
| 进程管理 | `kernel/proc/proc.c#proc_exit` | `proc.c#exit` | 同板块证据 | 进程退出标准流程。目标项目缺少`fileclose`/`iput`调用，资源清理路径存在实质性差异。 |
| 任务调度/异步运行时 | `kernel/syscall/syscall.c#syscall` | `syscall.c#syscall` | 同板块同名 | 系统调用分发器。AST拓扑根本不同：目标为Switch-Case静态展开，参考为函数指针表动态索引。 |
| 任务调度/异步运行时 | `kernel/trap/trap_kernel.c#trap_kernel_handler` | `trap.c#kerneltrap` | 同板块证据 | 内核态陷阱处理。目标耦合了`proc_sched`/`proc_yield`调度逻辑，参考侧重`r_sepc`/`w_sepc`寄存器操作，架构抽象不同。 |
| 内存管理 | `kernel/dev/virtio.c#virtio_disk_rw` | `virtio_disk.c#virtio_disk_rw` | 同板块同名 | VirtIO块设备驱动读写。调用图谱高度相似是遵循Virtio硬件协议进行描述符链操作的必然结果。 |
| 中断/异常处理 | `kernel/lib/spinlock.c#pop_off` | `spinlock.c#pop_off` | 同板块同名 | 自旋锁中断状态恢复。调用`intr_get`/`intr_on`是标准实现模式，且目标项目使用`panic`，参考使用`assert`。 |
| 文件系统 | `mkfs/mkfs.c#xint` | `mkfs.c#xint` | 同板块同名 | 文件系统制作工具函数，内部闭环逻辑，属于通用工具代码片段。 |
| 设备驱动 | `kernel/dev/virtio.c#free_chain` | `virtio_disk.c#free_chain` | 同板块同名同结构 | VirtIO描述符链释放。调用`free_desc`是操作VirtIO环形队列的标准步骤，属于硬件强制逻辑。 |
| 通用模块/其他 | `kernel/lib/str.c#safestrcpy` | `string.c#safestrcpy` | 同板块同名同结构 | 安全字符串拷贝，属于任何C语言项目都可能出现的通用基础工具函数。 |

### 4. 核心代码调用流对比剖析
*   **【进程管理/调度原语】**
    *   **底层技术析理**: 控方指控`proc_sleep`与`sleep`存在完美映射。然而，两者的同步原语抽象层次不同。目标项目直接操作自旋锁（`spinlock_acquire/release`），而参考项目使用通用锁（`acquire/release`）。更重要的是，目标项目的`sys_nanosleep`通过`r_time`和`uvm_copyin`实现，而参考项目的`sys_clock_nanosleep`则引入了`killed`状态检查，其睡眠中断响应模型存在本质差异。
    *   **代码溯源链接**:
        - 目标项目: `kernel/proc/proc.c#proc_sleep`
        - 参考项目: `proc.c#sleep`

*   **【任务调度/异步运行时/系统调用分发】**
    *   **底层技术析理**: 这是揭露双方架构差异的关键。目标项目的`syscall`函数采用静态的`switch-case`结构，直接调用30余个`sys_*`函数，这是一种编译时确定的控制流。参考项目的`syscall`则通过`syscalls[num]`函数指针表进行动态索引分发，是一种运行时多态。这两种AST拓扑结构在编译原理层面截然不同，是根本性的设计决策差异，绝非简单改名可以达成。
    *   **代码溯源链接**:
        - 目标项目: `kernel/syscall/syscall.c#syscall`
        - 参考项目: `syscall.c#syscall`

*   **【文件系统/架构差异】**
    *   **底层技术析理**: 目标项目实现了完整的`ext4`文件系统模块（`ext4_init`, `read_extent_tree`等），这是一个复杂的、独立的功能子系统。参考项目则基于传统的xv6文件系统（`iget`, `ilock`, `iput`等）。两者在文件系统架构、磁盘布局、inode管理模型上完全不同。目标项目独有的`ext4`支持是其独立开发的强有力证据。
    *   **代码溯源链接**:
        - 目标项目: `kernel/fs/ext4.c#ext4_init`
        - 参考项目: `fs.c#namex`

### 5. 单方模块与降权项
*   **单方模块**:
    *   **目标项目独有**: `文件系统/ext4` (kernel/fs/ext4.c)，`文件系统/ramdir` (kernel/syscall/sysfunc.c)。这些模块在参考项目中无任何对应实现，是目标项目独立开发的直接证据。
    *   **参考项目独有**: `信号处理` (signal.c)，`共享内存` (sysshm.c)，`线程` (thread.c)。这些模块在目标项目中不存在，体现了双方功能集的不同。
*   **降权项说明**:
    *   **跨板块硬凑**: 控方将目标项目的`ramdir_init`（文件系统）与参考项目的`convert_from_timespec2`（时间转换）进行对比，功能语义完全无关，此类证据无效。
    *   **通用接口/泛型函数名**: `printf`, `memmove`, `memcmp`, `safestrcpy`, `mycpu`, `myproc`等均为操作系统内核或C标准库的通用接口/函数名，其存在本身不构成同源证据，必须深入其内部AST实现进行比对。
    *   **硬件强制逻辑**: `virtio_disk_rw`, `free_chain`, `uart_intr`等与特定硬件交互的函数，其控制流受硬件协议强约束，实现上的相似性是工程必然，不能作为抄袭指控的依据。

### 6. 保守结论
综合客观AST指纹数据与深度架构剖析，本次审计裁定目标项目`[2026-T202610486999573-XX]`与参考项目`[2024-小混子队-合肥工业大学]`为**独立开发**。3.51%的极低相似度及不足8%的双向覆盖率，已从宏观层面排除了系统性代码复用的可能。控方基于少数同名函数和通用内核原语提出的“复用指控”，在底层AST拓扑、同步策略、内存模型及核心子系统（如系统调用分发、文件系统架构）的显著差异面前无法成立。19个共同指纹均可被解释为领域强制设计、硬件约束或通用基础库的合理使用。目标项目独有的`ext4`文件系统等模块更是其独立开发的有力佐证。因此，本次审计未发现足以判定代码抄袭的实质性证据。

------------------------------

