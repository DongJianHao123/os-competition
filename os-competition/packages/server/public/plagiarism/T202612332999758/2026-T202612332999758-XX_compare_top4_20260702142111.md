# 🛡️ OS 内核赛自动化代码级架构审计报告

**审计时间**: `2026-07-02 14:21:11`
**参赛作品名称**: `2026-T202612332999758-XX`
**年份**: `2026`
**学校**: `XX`
**队伍名称**: `T202612332999758`
**比赛名字**: `OS-Competition`
**比赛子赛道**: `Kernel-Implementation`
**仓库地址**: `https://gitlab.eduxiji.net/educg-group-43501-3132633/T202612332999758-1958`
**推测开发语言**: `Rust / C (由AST探针自动识别)`
**特征函数量**: `3316`

## ⚔️ 底层代码同源性对比 (AST Homology)
> 本部分由专门的代码审查智能体，对该项目与符合年份条件的历届最高相似度项目进行控制流与底层调用对比。新版报告会先给出确定性的全模块覆盖矩阵，再给出 Agent 的分模块解释，避免只围绕少量抽样函数展开。

### 🏆 Top 4 底层对标: vs `2025-Del0n1x-哈尔滨工业大学深圳`（哈尔滨工业大学（深圳） / Del0n1x）

#### 对方作品信息

**对方年份**: `2025`
**对方学校**: `哈尔滨工业大学（深圳）`
**对方队伍名称**: `Del0n1x`
**对方比赛名字**: `OS-Competition`
**对方比赛子赛道**: `Kernel-Implementation`
**对方仓库地址**: `https://gitlab.eduxiji.net/educg-group-36002-2710490/T202518123995600-2523`

> 双向覆盖率：共同 AST 指纹: 111；唯一 AST 指纹: 目标项目 2741，参考项目 1681；覆盖率: 目标项目 4.05%，参考项目 6.6%

## 全模块对比总览（确定性证据）

> 总体覆盖率：共同 AST 指纹: 111；唯一 AST 指纹: 目标项目 2741，参考项目 1681；覆盖率: 目标项目 4.05%，参考项目 6.6%

### 1. 模块覆盖矩阵
| 模块 | 目标函数数 | 参考函数数 | 共同唯一 AST 指纹 | Target 覆盖 | Base 覆盖 | 状态 |
| :--- | ---: | ---: | ---: | ---: | ---: | :--- |
| 进程管理 | 266 | 239 | 3 | 1.28% | 1.65% | 双方存在且有结构重合 |
| 任务调度/异步运行时 | 372 | 254 | 5 | 1.41% | 2.22% | 双方存在且有结构重合 |
| 内存管理 | 763 | 396 | 5 | 0.75% | 1.51% | 双方存在且有结构重合 |
| 中断/异常处理 | 99 | 113 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |
| 系统调用 | 196 | 97 | 6 | 3.61% | 8.0% | 双方存在且有结构重合 |
| 文件系统 | 560 | 496 | 17 | 3.78% | 4.62% | 双方存在且有结构重合 |
| 设备驱动 | 205 | 155 | 19 | 11.24% | 14.39% | 双方存在且有结构重合 |
| 网络协议栈 | 351 | 88 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |
| 文件系统/第三方库 | 171 | 44 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |
| 通用模块/其他 | 333 | 265 | 35 | 12.41% | 15.22% | 双方存在且有结构重合 |

### 2. 分模块证据清单

#### 进程管理
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `os/src/executor/mod.rs#spawn` | `os/src/task/executor.rs#spawn` | 同板块同名 | AtomicUsize::new, Instruction::get_tp, PROCESSORS[cpu_mask_id]
                                .unwrap_with_mut_task_queue, PROCESSORS[cpu_mask_id].rt_hint.store, PROCESSORS[target]
                                .unwrap_with_mut_task_queue, SCHEDULE_COUNT.fetch_add ...(+22) | TASK_QUEUE.push_normal, TASK_QUEUE.push_prior, WithInfo, async_task::spawn, runnable.schedule, task.detach |
| `os/src/sync/mutex/spin_mutex.rs#wait_unlock` | `os/src/sync/mutex/spin.rs#wait_unlock` | 同板块同名 | core::hint::spin_loop, panic!, self.owner.load | core::hint::spin_loop, panic!, self.lock.load |
| `os/src/task/task.rs#do_group_exit` | `os/src/signal/do_signal.rs#do_group_exit` | 同板块同名 | Some, drop, group.iter, member.is_zombie, member.recv_sigs, member.tid ...(+4) | info!, task.kill_all_thread, task.set_exit_code |
| `os/src/syscall/process.rs#sys_setpgid` | `os/src/syscall/process.rs#sys_setpgid` | 同板块同名 | Ok, PROCESS_GROUP_MANAGER.add_group, PROCESS_GROUP_MANAGER.add_task_to_group, PROCESS_GROUP_MANAGER.get_group, PROCESS_GROUP_MANAGER.get_group(existing).is_some, TASK_MANAGER.get_task ...(+4) | Err, Ok, current_task, current_task().unwrap, extract_proc_to_new_group, get_target_proc_group ...(+9) |

#### 任务调度/异步运行时
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `os/src/syscall/futex.rs#sys_futex` | `os/src/syscall/sync.rs#sys_futex` | 同板块同名 | (op_code & FUTEX_OP_ARG_SHIFT != 0)
                .then, (op_code & FUTEX_OP_ARG_SHIFT != 0)
                .then(|| 1u32 << op_operand)
                .unwrap_or, Err, FutexOp::from, Ok, SumGuard::new ...(+37) | Err, FutexHashKey::get_futex_key, FutexOp::from_bits, FutexOp::from_bits(futex_op).ok_or, Ok, atomic_load_acquire ...(+12) |
| `os/src/syscall/fs.rs#sys_utimensat` | `os/src/syscall/fs.rs#sys_utimensat` | 同板块同名 | AtFlags::AT_FDCWD.bits, AtFlags::from_bits, AtFlags::from_bits(flags).ok_or, Err, Ok, TimeSpec::from ...(+32) | CLOCK_MANAGER.lock, CLOCK_MANAGER.lock().get, CLOCK_MANAGER.lock().get(0).unwrap, Ok, OpenFlags::from_bits, OpenFlags::from_bits(flags).ok_or ...(+24) |
| `os/src/syscall/io.rs#sys_ppoll` | `os/src/syscall/io.rs#sys_ppoll` | 同板块同名 | DualSelect::new, Err, Ok, PollEvents::empty, Some, TimedTaskFut::new ...(+33) | Duration::from, Err, IoFutrue::new, Ok, SigMask::from_bits, SigMask::from_bits(unsafe { *sigmask }).ok_or ...(+9) |
| `os/src/fs/devfs/loop_dev.rs#ioctl` | `os/src/fs/devfs/dev_loop.rs#ioctl` | 同板块同名 | Err, LoopIoctlCmd::from_repr, LoopIoctlCmd::from_repr(cmd).ok_or, Ok, Some, UserMutPtr::new ...(+37) | Err, LoopFlags::LO_FLAGS_DIRECT_IO.bits, LoopFlags::LO_FLAGS_READ_ONLY.bits, LoopInfo64::new, LoopIoctl::from_bits, LoopIoctl::from_bits(op as u32).ok_or ...(+33) |

#### 内存管理
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `os/src/syscall/mod.rs#syscall` | `os/src/syscall/mod.rs#syscall` | 同板块同名 | ForceSend, Ok, SysError::EACCES.code, SysError::ENOSYS.code, SyscallNum::from_repr, UserMutPtr::new ...(+239) | Ok, SysCode::from, sys_accept, sys_accept4, sys_bind, sys_brk ...(+121) |
| `os/src/trap/mod.rs#user_trap_handler` | `os/src/hal/la64/trap/user_trap.rs#user_trap_handler` | 同板块同名 | Instruction::clear_software_interrupt, Instruction::enable_interrupt, SumGuard::new, TrapType::get_debug, VirtAddr::from, bump_timer_irq ...(+32) | DEVICE_MANAGER.read, DEVICE_MANAGER.read().handle_irq, TIMER_QUEUE.handle_expired, badv::read, badv::read().vaddr, crmd.plv ...(+23) |
| `os/src/syscall/fs.rs#sys_openat` | `os/src/syscall/fs.rs#sys_openat` | 同板块同名 | Err, InodeMode::from_bits_truncate, Ok, OpenFlags::empty, OpenFlags::from_bits, OpenFlags::from_bits(flags as i32).ok_or ...(+53) | Err, FdInfo::new, Ok, OpenFlags::from_bits, OpenFlags::from_bits(flags as i32).ok_or, SYS_OPENAT_MODE.store ...(+19) |
| `os/src/mm/vm/uvm.rs#handle_page_fault` | `os/src/mm/memory_space/vm_area.rs#handle_page_fault` | 同板块同名 | Err, FileBackedFiller::handle_lazy_page_fault, HeapFiller::handle_lazy_page_fault, Instruction::tlb_flush_addr, MmapFiller::handle_lazy_page_fault, Ok ...(+27) | Arc::strong_count, Err, Ok, Page::new, access_type.can_access, access_type.contains ...(+39) |

#### 中断/异常处理
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `hal/src/component/irq/loongarch64/eiointc.rs#claim_irq` | `os/src/drivers/irqchip/la64/loongson_eiointc.rs#claim_irq` | 同板块同名 | Some, flags0.leading_zeros, flags1.leading_zeros, flags2.leading_zeros, flags3.leading_zeros, iocsr_read_d | Some, pendings.trailing_zeros, self.get_core_pendings, self.get_core_pendings(hart_id as u8)
            .expect |
| `hal/src/component/irq/loongarch64/mod.rs#enable_irq` | `os/src/drivers/irqchip/la64/loongson_liointc.rs#enable_irq` | 同板块同名 | Eiointc::enable_irq, log::warn!, self.platic.read_w, self.platic.write_b, self.platic.write_w | ConfigVector::new, self.enable, self.route |
| `hal/src/component/irq/loongarch64/mod.rs#disable_irq` | `os/src/drivers/irqchip/la64/loongson_eiointc.rs#disable_irq` | 同板块同名 | Eiointc::disable_irq, log::warn!, self.platic.read_w, self.platic.write_w | self.next_icu.disable_irq |
| `hal/src/component/irq/loongarch64/eiointc.rs#enable_irq` | `os/src/drivers/irqchip/la64/loongson_pch_pic.rs#enable_irq` | 同板块同名 | iocsr_read_d, iocsr_write_d | self.enable, self.route, self.set_ht_msi_vector |

#### 系统调用
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `os/src/syscall/fd.rs#update` | `os/src/syscall/io_async.rs#update` | 同板块同名 | Err, Ok, map.contains_key, map.insert, map.len, self.array_index ...(+4) | Ok, core::slice::from_raw_parts_mut, fdset.setfd, flash, now_fds.len, pollfd.revents.contains ...(+1) |
| `os/src/syscall/net.rs#sys_sethostname` | `os/src/syscall/net.rs#sys_sethostname` | 同板块同名 | (0..=64).contains, Err, Ok, String::from_utf8, String::from_utf8(name_src.to_ref().to_vec()).map_err, UTS.lock ...(+5) | Err, HOST_NAME[..size].copy_from_slice, Ok, check_readable, check_readable(name.into(), size).is_err, core::slice::from_raw_parts ...(+3) |
| `user/src/lib.rs#execve` | `user/src/lib.rs#execve` | 同板块同名 | CString::new, CString::new(path).unwrap, argv.as_ptr, cstr_ptr_table, envp.as_ptr, path.as_ptr ...(+1) | (*s).as_ptr, argv.as_ptr, argv.iter, argv.iter().map, argv.iter().map(|s| (*s).as_ptr() as usize).collect, argv.push ...(+6) |
| `user/src/syscall.rs#syscall` | `user/src/syscall.rs#syscall` | 同板块同名同结构 | asm! | asm! |

#### 文件系统
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `os/src/fs/devfs/tty.rs#ioctl` | `os/src/fs/devfs/tty.rs#ioctl` | 同板块同名 | Err, Ok, TtyIoctlCmd::from_repr, log::debug!, log::error!, self.meta.lock ...(+1) | Err, Ok, TtyIoctlCmd::try_from, TtyIoctlCmd::try_from(op).map_err, arg.into, info! ...(+7) |
| `os/src/syscall/fs.rs#sys_preadv2` | `os/src/syscall/fs.rs#sys_preadv2` | 同板块同名 | Err, Ok, RwfFlags::from_bits_truncate, dst.to_mut, fd_file, file.inode ...(+14) | info!, sys_pread64 |
| `os/src/syscall/fs.rs#sys_pwritev2` | `os/src/syscall/fs.rs#sys_pwritev2` | 同板块同名 | Err, Ok, RwfFlags::from_bits_truncate, fd_file, file.inode, file.inode()?.inode_type ...(+13) | info!, sys_pwrite64 |
| `os/src/fs/devfs/urandom.rs#fill_buf` | `os/src/utils/random.rs#fill_buf` | 同板块同名 | (Timer::read() as u64).wrapping_add, Salsa20::new, Timer::read, buf.iter_mut, buf.len, cipher.apply_keystream ...(+7) | (buf.len() - offset).min, buf.len, buf[offset..chunk_size + offset].copy_from_slice, rand.to_le_bytes, self.next |

#### 设备驱动
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `os/src/drivers/serial/uart.rs#init_u32` | `os/src/drivers/tty/serial/ns16550a.rs#init_u32` | 同板块同名 | log::info!, regs.byte_add, regs.byte_add(self.reg_off(0)).write_volatile, regs.byte_add(self.reg_off(1)).write_volatile, regs.byte_add(self.reg_off(FCR)).write_volatile, regs.byte_add(self.reg_off(IER)).write_volatile ...(+4) | info!, reg.byte_add, reg.byte_add(0 << self.reg_shift)
                    .write_volatile, reg.byte_add(1 << self.reg_shift)
                    .write_volatile, reg.byte_add(FCR << self.reg_shift).write_volatile, reg.byte_add(IER << self.reg_shift).write_volatile ...(+2) |
| `os/src/drivers/serial/mod.rs#poll_in` | `os/src/drivers/tty/serial/mod.rs#poll_in` | 同板块同名 | get_waker, inner.pollin_queue.push_back, inner.read_buf.is_empty, self.uart, self.uart_with, self.with_mut_inner ...(+1) | get_waker, self.icbuffer.lock, self.icbuffer.lock().is_empty, self.read_queue.lock, self.read_queue.lock().push_back, self.uart.poll_in |
| `os/src/drivers/serial/uart.rs#init_u8` | `os/src/drivers/tty/serial/ns16550a.rs#init_u8` | 同板块同名 | regs.byte_add, regs.byte_add(self.reg_off(0)).write_volatile, regs.byte_add(self.reg_off(1)).write_volatile, regs.byte_add(self.reg_off(IER)).write_volatile, regs.byte_add(self.reg_off(LCR)).write_volatile, regs.byte_add(self.reg_off(MCR)).write_volatile ...(+2) | reg.byte_add, reg.byte_add(0 << self.reg_shift)
                    .write_volatile, reg.byte_add(1 << self.reg_shift)
                    .write_volatile, reg.byte_add(IER << self.reg_shift).write_volatile, reg.byte_add(LCR << self.reg_shift).write_volatile, reg.byte_add(MCR << self.reg_shift).write_volatile |
| `os/src/drivers/block/virtio_blk/loongarch64.rs#dump_bar_contents` | `os/src/drivers/virtio_driver/pci.rs#dump_bar_contents` | 同板块同名 | base.add, core::ptr::copy, println!, root.bar_info, root.bar_info(device_function, bar_index).unwrap, window.as_mut_ptr ...(+2) | buf.as_mut_ptr, buf.iter, buf.iter().any, info!, ptr::copy, root.bar_info ...(+2) |

#### 网络协议栈
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `os/src/net/tcp_socket.rs#shutdown` | `os/src/net/tcp.rs#shutdown` | 同板块同名 | Err, Ok, crate::net::get_backend, crate::net::get_backend().tcp_shutdown, log::info!, self.handle.lock ...(+2) | NET_DEV.lock, NET_DEV.lock().poll, Ok, SOCKET_SET.lock, Some, binding.get_mut::<tcp::Socket> ...(+5) |
| `os/src/net/socket.rs#shutdown` | `os/src/net/udp.rs#shutdown` | 同板块同名 | Err, Ok, log::info!, pair.close, tcp.shutdown, udp_socket.shutdown | NET_DEV.lock, NET_DEV.lock().poll, Ok, SOCKET_SET.lock, binding.get_mut::<udp::Socket>, socket.close |
| `os/src/net/netlink_socket.rs#bind` | `os/src/net/tcp.rs#bind` | 同板块同名 | Ok, Some, log::info!, self.local_addr.lock, self.next_port_id.fetch_add | Err, IpEndpoint::try_from, Ok, addr.clone, info!, self.do_bind ...(+2) |
| `os/src/net/tcp/socket_api.rs#bind` | `os/src/net/unix.rs#bind` | 同板块同名 | Err, Ok, Some, self.local_endpoint.lock, self.state.load, self.state.store | unimplemented! |

#### 文件系统/第三方库
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `os/src/fs/ext4_native/native/inode.rs#write_at` | `os/src/fs/ext4/inode.rs#write_at` | 同板块同名 | (*disk).clone, Err, Ext4InodeDisk::clone, Ok, alloc::vec!, bitmap::alloc_block ...(+25) | cache.write, info!, self.get_size, self.set_size, self.write_directly |
| `os/src/fs/ext4_native/native/inode.rs#unlink` | `os/src/fs/ext4/inode.rs#unlink` | 同板块同名 | (*disk).clone, Ok, Some, core::slice::from_ref, crate::timer::get_current_time, directory::remove_entry ...(+13) | Err, Ok, debug_point!, info!, lock_file.dir_rm, lock_file.file_remove ...(+6) |
| `os/src/fs/ext4_native/native/inode.rs#rename` | `os/src/fs/ext4/inode.rs#rename` | 同板块同名 | Ok, Some, String::from, core::slice::from_ref, directory::add_entry, directory::mode_to_dirent_type ...(+11) | Err, Ext4Inode::new, Ok, block_on, ext4file.file_rename, new_dentry.bind ...(+18) |
| `os/src/fs/ext4_native/native/inode.rs#link` | `os/src/fs/ext4/inode.rs#link` | 同板块同名 | (*disk).clone, Err, Ok, core::slice::from_ref, crate::timer::get_current_time, directory::add_entry ...(+14) | Err, Ext4Inode::new, Ok, bare_dentry.bind, bare_dentry.get_abs_path, bare_dentry.is_valid ...(+9) |

#### 通用模块/其他
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `utils/range-map/src/lib.rs#find_free_range` | `os/src/utils/container/range_map.rs#find_free_range` | 同板块同名 | Some, debug_assert!, self.0.range, self.0.range(..=start).next_back, start.min | Some, debug_assert!, self.0.range, self.0.range(..=start).next_back, start.min |
| `utils/range-map/src/lib.rs#try_insert` | `os/src/utils/container/range_map.rs#try_insert` | 同板块同名同结构 | Err, Ok, debug_assert!, self.0.range, self.0.range(..range.end).next_back, self.0.try_insert ...(+2) | Err, Ok, debug_assert!, self.0.range, self.0.range(..range.end).next_back, self.0.try_insert ...(+2) |
| `hal/src/util/mutex.rs#lock` | `os/src/sync/mutex/spin.rs#lock` | 同板块同名 | SieGuard::new, core::hint::spin_loop, panic!, self.mutex.compare_exchange, self.mutex.compare_exchange(false, true, Ordering::Acquire, Ordering::Relaxed).is_ok | S::before_lock, self
                .lock
                .compare_exchange, self
                .lock
                .compare_exchange(false, true, Ordering::Acquire, Ordering::Relaxed)
                .is_ok, self.wait_unlock |
| `os/src/sync/async_mutex.rs#lock` | `os/src/sync/mutex/sleep.rs#lock` | 同板块同名 | crate::utils::async_utils::get_waker, crate::utils::async_utils::suspend_now, self.try_lock, self.waker.cancel, self.waker.register | Pin::new_unchecked, Pin::new_unchecked(future).init, SleepMutexFuture::new |


#### 1. 宏观判定
- **定性结论**: **独立开发** (相似度: 2.57%，共同 AST 指纹: 111；唯一 AST 指纹: 目标项目 2741，参考项目 1681；覆盖率: 目标项目 4.05%，参考项目 6.6%)

#### 2. 全模块覆盖矩阵解读
- **进程管理**: 双方均存在该模块。存在 3 个共同 AST 指纹，目标覆盖率 1.28%，参考覆盖率 1.65%。该覆盖率处于操作系统领域公共设计模式的背景噪音区间，证据强度为**弱**。
- **任务调度/异步运行时**: 双方均存在该模块。存在 5 个共同 AST 指纹，目标覆盖率 1.41%，参考覆盖率 2.22%。双方在调度器核心架构上存在根本性差异（自定义 per-CPU executor vs 基于 `async-task` crate），证据强度为**弱**。
- **内存管理**: 双方均存在该模块。存在 5 个共同 AST 指纹，目标覆盖率 0.75%，参考覆盖率 1.51%。极低的覆盖率表明内存管理子系统实现路径完全不同，证据强度为**未形成**。
- **中断/异常处理**: 双方均存在该模块。共同 AST 指纹为 0，覆盖率为 0.0%。虽然存在同名函数，但 AST 结构无交集，证据强度为**未形成**。
- **系统调用**: 双方均存在该模块。存在 6 个共同 AST 指纹，目标覆盖率 3.61%，参考覆盖率 8.0%。相似性集中在 POSIX/Linux ABI 规定的标准系统调用入口，证据强度为**弱**。
- **文件系统**: 双方均存在该模块。存在 17 个共同 AST 指纹，目标覆盖率 3.78%，参考覆盖率 4.62%。共同指纹多源于 ext4 等标准磁盘数据结构的解析逻辑，证据强度为**弱**。
- **设备驱动**: 双方均存在该模块。存在 19 个共同 AST 指纹，目标覆盖率 11.24%，参考覆盖率 14.39%。相似性集中于 NS16550A UART 等标准硬件初始化序列，证据强度为**中**。
- **网络协议栈**: 双方均存在该模块。共同 AST 指纹为 0，覆盖率为 0.0%。目标项目为自研 TCP 状态机，参考项目基于 `smoltcp`，架构完全不同，证据强度为**未形成**。
- **第三方库/公共依赖**: 双方均存在该模块。共同 AST 指纹为 0，覆盖率为 0.0%。双方均依赖 Rust 标准库及部分公共 crate，但未观察到共同的结构指纹，证据强度为**未形成**。

#### 3. 底层 AST 关键证据指纹表
| 所属板块 | 目标项目路径 | 参考项目路径 | 证据类型 | 技术说明 |
| :--- | :--- | :--- | :--- | :--- |
| 设备驱动 | `os/src/drivers/serial/uart.rs#init_u32` | `os/src/drivers/tty/serial/ns16550a.rs#init_u32` | 标准硬件初始化序列 | 两者均为 NS16550A UART 的标准初始化流程，涉及 IER、FCR、LCR 等标准寄存器操作。目标使用 `reg_off` 偏移，参考使用 `reg_shift` 位移，属于对同一硬件手册的独立实现。 |
| 设备驱动 | `os/src/drivers/serial/uart.rs#init_u8` | `os/src/drivers/tty/serial/ns16550a.rs#init_u8` | 标准硬件初始化序列 | 同上，为 UART 8位模式的标准初始化序列。 |
| 设备驱动 | `os/src/drivers/serial/mod.rs#poll_in` | `os/src/drivers/tty/serial/mod.rs#poll_in` | 通用设备驱动模式 | 两者均实现了串口设备的 `poll_in` 方法，包含检查读缓冲区、操作唤醒队列等通用逻辑。目标使用 `with_mut_inner` 封装，参考直接操作 `icbuffer`，实现细节不同。 |
| 设备驱动 | `os/src/drivers/block/virtio_blk/loongarch64.rs#dump_bar_contents` | `os/src/drivers/virtio_driver/pci.rs#dump_bar_contents` | 标准 PCI 调试功能 | 两者均为 VirtIO PCI BAR 空间内容的调试转储函数，涉及 `root.bar_info` 和 `ptr::copy` 等标准操作。 |
| 通用模块/其他 | `utils/range-map/src/lib.rs#find_free_range` | `os/src/utils/container/range_map.rs#find_free_range` | 同结构算法实现 | 两者实现了完全相同的区间映射查找算法，AST 结构高度一致。但该算法为通用数据结构，可能源自相同的算法描述。 |
| 通用模块/其他 | `utils/range-map/src/lib.rs#try_insert` | `os/src/utils/container/range_map.rs#try_insert` | 同结构算法实现 | 同上，为区间映射的插入算法，AST 结构高度一致。 |
| 进程管理 | `os/src/sync/mutex/spin_mutex.rs#wait_unlock` | `os/src/sync/mutex/spin.rs#wait_unlock` | 标准自旋锁模式 | 两者均为自旋锁的等待解锁实现，包含 `spin_loop`、`panic!`、`load` 等标准操作。目标使用 `self.owner.load`，参考使用 `self.lock.load`，字段语义不同。 |
| 进程管理 | `os/src/task/task.rs#add_child` | `os/src/task/task.rs#add_child` | 通用进程树管理 | 两者均实现了向父任务添加子任务的操作，核心为 `self.children.lock().insert`。目标使用 `child.gettid`，参考使用 `child.get_pid`，键值选择不同。 |
| 进程管理 | `os/src/task/task.rs#remove_child` | `os/src/task/task.rs#remove_child` | 通用进程树管理 | 两者均实现了从父任务移除子任务的操作，核心为 `self.children.lock().remove`。参考方增加了 `.expect` 错误处理。 |
| 任务调度/异步运行时 | `os/src/syscall/futex.rs#sys_futex` | `os/src/syscall/sync.rs#sys_futex` | 标准 Linux ABI | 两者均为 Linux `futex` 系统调用的实现，包含 `FutexOp` 解析、原子操作等标准流程。目标实现更复杂，包含 `FUTEX_OP_ARG_SHIFT` 等操作码处理。 |
| 任务调度/异步运行时 | `os/src/syscall/fs.rs#sys_utimensat` | `os/src/syscall/fs.rs#sys_utimensat` | 标准 Linux ABI | 两者均为 Linux `utimensat` 系统调用的实现，包含 `AtFlags` 解析、时间戳更新等标准流程。 |
| 内存管理 | `os/src/syscall/mod.rs#syscall` | `os/src/syscall/mod.rs#syscall` | 标准系统调用分发器 | 两者均为系统调用总入口，包含 `SyscallNum` 解析和分发逻辑。目标使用 `ForceSend` 等自定义类型，参考直接匹配。 |
| 内存管理 | `os/src/trap/mod.rs#user_trap_handler` | `os/src/hal/la64/trap/user_trap.rs#user_trap_handler` | 标准 Trap 处理流程 | 两者均为用户态 Trap 处理入口，包含中断使能、计时器更新等标准操作。目标使用 `SumGuard` 等自定义抽象。 |
| 文件系统 | `os/src/fs/devfs/tty.rs#ioctl` | `os/src/fs/devfs/tty.rs#ioctl` | 标准 Linux ABI | 两者均为 TTY 设备的 `ioctl` 实现，包含 `TtyIoctlCmd` 解析和分发。 |
| 文件系统 | `os/src/syscall/fs.rs#sys_openat` | `os/src/syscall/fs.rs#sys_openat` | 标准 Linux ABI | 两者均为 Linux `openat` 系统调用的实现，包含 `OpenFlags` 解析和文件打开流程。 |
| 系统调用 | `user/src/lib.rs#execve` | `user/src/lib.rs#execve` | 标准 POSIX 接口 | 两者均为用户态 `execve` 函数的实现，包含参数指针表的构建。 |
| 系统调用 | `user/src/syscall.rs#syscall` | `user/src/syscall.rs#syscall` | 标准系统调用指令 | 两者均为用户态系统调用入口，使用 `asm!` 宏执行 `syscall` 指令。这是 LoongArch 架构的标准操作。 |

#### 4. 核心代码调用流对比剖析
* **【进程管理/系统调用路径】**
* **底层技术析理**: 在 `sys_getpid`/`sys_getuid` 等属性读取系统调用中，两者均遵循 `current_task().unwrap() -> 获取属性` 的标准模式。但目标项目通过 `.clone().ruid` 直接访问字段，参考项目通过 `.get_euid()` 调用 getter 方法。这反映了双方在 TaskControlBlock 内部封装策略上的根本差异：目标项目将 uid/gid 设计为可直接访问的公开字段，而参考项目采用 getter/setter 封装。这种 API 设计层面的差异无法通过简单的“重命名”或“内联展开”实现，是独立架构设计的有力证据。
* **代码溯源链接**:
  - 目标项目: `os/src/syscall/process.rs#sys_getuid`
  - 参考项目: `os/src/syscall/process.rs#sys_getuid`

* **【任务调度/异步运行时】**
* **底层技术析理**: 在任务 spawn 的核心路径上，两者存在架构级差异。目标项目使用自定义的 per-CPU executor，通过 `PROCESSORS[cpu].unwrap_with_mut_task_queue` 获取特定 CPU 的任务队列，并调用 `rt_hint.store` 进行运行时提示。参考项目则基于 `async-task` 通用 crate，调用 `async_task::spawn` 和 `runnable.schedule`。目标项目实现了 `round_robin` 和 `woken` 双队列，参考项目实现了 `normal` 和 `prior` 优先级队列。两者在调度器设计、队列语义、运行时抽象上完全不同。
* **代码溯源链接**:
  - 目标项目: `os/src/executor/mod.rs#spawn`
  - 参考项目: `os/src/task/executor.rs#spawn`

* **【内存管理/缺页异常处理】**
* **底层技术析理**: 在 `handle_page_fault` 的核心路径上，两者采用了不同的填充器（Filler）抽象。目标项目使用 `FileBackedFiller`、`HeapFiller`、`MmapFiller` 三种策略处理不同类型的缺页，参考项目则通过 `access_type.can_access` 和 `Page::new` 进行权限检查和页面分配。目标项目的缺页处理与文件系统（`FileBackedFiller`）和内存映射（`MmapFiller`）深度耦合，而参考项目的实现相对独立。这种架构分层上的差异是独立开发的典型特征。
* **代码溯源链接**:
  - 目标项目: `os/src/mm/vm/uvm.rs#handle_page_fault`
  - 参考项目: `os/src/mm/memory_space/vm_area.rs#handle_page_fault`

#### 5. 单方模块与降权项
- **网络协议栈**: 目标项目拥有 351 个函数的自研 TCP/IP 协议栈，参考项目仅有 88 个函数且基于 `smoltcp`。双方在该模块的共同 AST 指纹为 0，属于完全独立的实现。
- **第三方库/公共依赖**: 目标项目拥有 171 个函数，参考项目拥有 44 个函数。双方均依赖 Rust 标准库及部分公共 crate，但未观察到共同的结构指纹。`range-map` 等通用数据结构虽然 AST 结构高度一致，但属于标准算法实现，不能作为强同源证据。
- **跨板块同名函数**: 目标项目的 `sys_sigprocmask` 位于用户态 `user/src/syscall.rs`，参考项目位于内核态 `os/src/syscall/process.rs`。这种跨板块同名属于 Linux ABI 标准名称的必然结果，不构成同板块主证据。
- **内部闭环逻辑**: `child_time_pair`、`get_child_ustime`、`id`、`timer_irq_cnt` 等函数标记为内部闭环逻辑，缺乏外部调用图谱支撑，无法形成有效指控。

#### 6. 保守结论
本次审计基于客观 AST 指纹比对，目标项目与参考项目的整体相似度仅为 2.57%，双向覆盖率均低于 7%。在进程管理、任务调度、内存管理等核心模块中，双方展现出系统性的架构差异：目标项目采用自定义 per-CPU executor、`PROCESS_GROUP_MANAGER` 独立管理器、自研 TCP 协议栈；参考项目则基于 `async-task` crate、集中式 `MANAGER`、`smoltcp` 网络栈。观察到的少量 AST 相似片段（如 `spin_loop`、`lock().push_back`、`current_task().unwrap()`）均属于 Rust 语言惯用法、Linux ABI 标准接口或操作系统通用设计模式，不具备唯一性。综合判定，目标项目 `[2026-T202612332999758-XX]` 为独立开发，不存在对参考项目 `[2025-Del0n1x-哈尔滨工业大学深圳]` 的系统性代码复用。

------------------------------

