# 🛡️ OS 内核赛自动化代码级架构审计报告

**审计时间**: `2026-07-02 14:21:11`
**参赛作品名称**: `2026-T202612332999758-XX`
**年份**: `2026`
**学校**: `XX`
**队伍名称**: `T202612332999758`
**比赛名字**: `OS-Competition`
**比赛子赛道**: `Kernel-Implementation`
**仓库地址**: `https://gitlab.eduxiji.net/educg-group-43501-3132633/T202612332999758-1958`
**推测开发语言**: `Rust / C (由AST探针自动识别)`
**特征函数量**: `3316`

## ⚔️ 底层代码同源性对比 (AST Homology)
> 本部分由专门的代码审查智能体，对该项目与符合年份条件的历届最高相似度项目进行控制流与底层调用对比。新版报告会先给出确定性的全模块覆盖矩阵，再给出 Agent 的分模块解释，避免只围绕少量抽样函数展开。

### 🏆 Top 5 底层对标: vs `2023-MankorOS-哈尔滨工业大学深圳`（哈尔滨工业大学（深圳） / MankorOS）

#### 对方作品信息

**对方年份**: `2023`
**对方学校**: `哈尔滨工业大学（深圳）`
**对方队伍名称**: `MankorOS`
**对方比赛名字**: `OS-Competition`
**对方比赛子赛道**: `Kernel-Implementation`
**对方仓库地址**: `https://gitlab.eduxiji.net/MankorOS/OSKernel2023-MankorOS`

> 双向覆盖率：共同 AST 指纹: 84；唯一 AST 指纹: 目标项目 2741，参考项目 1233；覆盖率: 目标项目 3.06%，参考项目 6.81%

## 全模块对比总览（确定性证据）

> 总体覆盖率：共同 AST 指纹: 84；唯一 AST 指纹: 目标项目 2741，参考项目 1233；覆盖率: 目标项目 3.06%，参考项目 6.81%

### 1. 模块覆盖矩阵
| 模块 | 目标函数数 | 参考函数数 | 共同唯一 AST 指纹 | Target 覆盖 | Base 覆盖 | 状态 |
| :--- | ---: | ---: | ---: | ---: | ---: | :--- |
| 进程管理 | 266 | 264 | 7 | 2.98% | 2.92% | 双方存在且有结构重合 |
| 任务调度/异步运行时 | 372 | 79 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |
| 内存管理 | 763 | 320 | 2 | 0.3% | 0.71% | 双方存在且有结构重合 |
| 中断/异常处理 | 99 | 28 | 1 | 1.52% | 3.85% | 双方存在且有结构重合 |
| 系统调用 | 196 | 12 | 1 | 0.6% | 8.33% | 双方存在且有结构重合 |
| 文件系统 | 560 | 475 | 12 | 2.67% | 3.12% | 双方存在且有结构重合 |
| 设备驱动 | 205 | 123 | 18 | 10.65% | 16.67% | 双方存在且有结构重合 |
| 网络协议栈 | 351 | 0 | 0 | 0.0% | 0.0% | 仅目标项目体现 |
| 文件系统/第三方库 | 171 | 0 | 0 | 0.0% | 0.0% | 仅目标项目体现 |
| 通用模块/其他 | 333 | 159 | 10 | 3.55% | 7.35% | 双方存在且有结构重合 |

### 2. 分模块证据清单

#### 进程管理
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `os/src/executor/mod.rs#spawn` | `src/executor/mod.rs#spawn` | 同板块同名 | AtomicUsize::new, Instruction::get_tp, PROCESSORS[cpu_mask_id]
                                .unwrap_with_mut_task_queue, PROCESSORS[cpu_mask_id].rt_hint.store, PROCESSORS[target]
                                .unwrap_with_mut_task_queue, SCHEDULE_COUNT.fetch_add ...(+22) | TASK_QUEUE.push, async_task::spawn |
| `os/src/executor/mod.rs#run_until_idle` | `src/executor/mod.rs#run_until_idle` | 同板块同名 | AtomicUsize::new, IDLE_CHECK_COUNT.fetch_add, PROCESSORS[i].try_unwrap_with_mut_task_queue, PROCESSORS[i].try_unwrap_with_mut_task_queue(|q| (q.local_len(), q.rt_len(), q.len() - q.local_len() - q.rt_len())).unwrap_or, PROCESSORS[migrate_id].unwrap_with_mut_task_queue, TASK_QUEUE.dequeue ...(+19) | TASK_QUEUE.fetch, task.run |
| `os/src/syscall/process.rs#sys_execve` | `src/syscall/process.rs#sys_execve` | 同板块同名 | "/loongarch/musl/busybox".to_string, "/riscv/musl/busybox".to_string, "busybox".to_string, "sh".to_string, AtFlags::AT_FDCWD.bits, AtFlags::empty ...(+27) | Ok, Path::from_string, Some, String::from, UserReadPtr::<u8>::from, UserReadPtr::<usize>::from ...(+24) |
| `os/src/syscall/process.rs#sys_setpgid` | `src/syscall/process.rs#sys_setpgid` | 同板块同名 | Ok, PROCESS_GROUP_MANAGER.add_group, PROCESS_GROUP_MANAGER.add_task_to_group, PROCESS_GROUP_MANAGER.get_group, PROCESS_GROUP_MANAGER.get_group(existing).is_some, TASK_MANAGER.get_task ...(+4) | GlobalLProcManager::get, GlobalLProcManager::get(target_lproc_pid).ok_or, Ok, Pid::from, info!, self.cx.syscall_args ...(+3) |

#### 任务调度/异步运行时
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `os/src/syscall/fs.rs#sys_utimensat` | `src/syscall/fs.rs#sys_utimensat` | 同板块同名 | AtFlags::AT_FDCWD.bits, AtFlags::from_bits, AtFlags::from_bits(flags).ok_or, Err, Ok, TimeSpec::from ...(+32) | Err, Ok, TimeChange::new_now, TimeChange::new_omit, TimeChange::new_time, TimeInfoChange::new ...(+18) |
| `os/src/syscall/io.rs#sys_ppoll` | `src/syscall/io.rs#sys_ppoll` | 同板块同名 | DualSelect::new, Err, Ok, PollEvents::empty, Some, TimedTaskFut::new ...(+33) | AnyFuture::new_with, BTreeMap::<usize, (usize, i16)>::new, Ok, UserInOutPtr::<PollFd>::from_usize, Vec::<Async<SysResult<usize>>>::new, add_func_call ...(+24) |
| `os/src/fs/devfs/loop_dev.rs#ioctl` | `src/fs/memfs/tty.rs#ioctl` | 同板块同名 | Err, LoopIoctlCmd::from_repr, LoopIoctlCmd::from_repr(cmd).ok_or, Ok, Some, UserMutPtr::new ...(+37) | Ok, UserReadPtr::<pid_t>::from, UserWritePtr::<pid_t>::from, argp.read, argp.write, dyn_future ...(+5) |
| `os/src/syscall/fs.rs#sys_unlinkat` | `src/syscall/fs.rs#sys_unlinkat` | 同板块同名 | Err, Ok, SysError::from_repr, SysError::from_repr(raw_errno).unwrap_or, UserMutPtr::new, abs_path_to_name ...(+28) | Err, UserReadPtr::<u8>::from, dir.lookup, dir.lookup(&file_name).await?.kind, dir.remove, dir.remove(&file_name).await.map ...(+4) |

#### 内存管理
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `os/src/drivers/block/mmc/mod.rs#card_init` | `src/drivers/blk/dw_mshc/mmc.rs#card_init` | 同板块同名 | (self.virt_base_addr() as *mut CLKDIV)
                .byte_add, (self.virt_base_addr() as *mut CLKDIV)
                .byte_add(CLKDIV::offset())
                .read_volatile, CID::from, CLKDIV::offset, CMD::data_cmd, CMD::no_data_cmd ...(+42) | CID::from, CLKDIV::offset, CMD::data_cmd, CMD::no_data_cmd, CMD::no_data_cmd_no_crc, CMD::no_data_cmd_no_crc(card_idx, 2).with_response_length ...(+38) |
| `os/src/syscall/fs.rs#sys_openat` | `src/syscall/io.rs#sys_openat` | 同板块同名 | Err, InodeMode::from_bits_truncate, Ok, OpenFlags::empty, OpenFlags::from_bits, OpenFlags::from_bits(flags as i32).ok_or ...(+53) | Box::new, Err, Ok, OpenFlags::from_bits_truncate, Path::from_str, Path::from_string ...(+27) |
| `os/src/fs/fat32/native/inode.rs#write_at` | `src/fs/new_vfs/page_cache.rs#write_at` | 同板块同名 | Ok, Vec::new, alloc.alloc_cluster, bpb.cluster_to_sector, buf.is_empty, buf.len ...(+18) | Ok, PhysAddr::from, PhysAddr::from(offset).floor, PhysAddr::from(offset).floor().bits, buf.len, dyn_future ...(+4) |
| `os/src/devices/manager.rs#map_devices` | `src/drivers/manager.rs#map_devices` | 同板块同名 | "virtio-blk".to_string, Arc::new, IrqCtrl::from_dt, MmioManager::scan_mmio_root, PciManager::scan_pcie_root, Some ...(+23) | PTEFlags::rw, VirtAddr::from, boot::boot_pagetable_paddr, boot::boot_pagetable_paddr().into, core::mem::forget, dev.mmio_base ...(+11) |

#### 中断/异常处理
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `hal/src/component/irq/loongarch64/eiointc.rs#claim_irq` | `src/drivers/plic.rs#claim_irq` | 同板块同名 | Some, flags0.leading_zeros, flags1.leading_zeros, flags2.leading_zeros, flags3.leading_zeros, iocsr_read_d | (*plic).claim, PLICCtxWrapper::new, irq.get, irq.map, kernel_phys_dev_to_virt |
| `hal/src/component/irq/loongarch64/mod.rs#enable_irq` | `src/drivers/plic.rs#enable_irq` | 同板块同名 | Eiointc::enable_irq, log::warn!, self.platic.read_w, self.platic.write_b, self.platic.write_w | (*plic).enable, (*plic).set_priority, (*plic).set_threshold, PLICCtxWrapper::new, PLICSrcWrapper::new, kernel_phys_dev_to_virt |
| `hal/src/component/irq/loongarch64/eiointc.rs#enable_irq` | `src/trap/trap.rs#enable_irq` | 同板块同名 | iocsr_read_d, iocsr_write_d | sstatus::set_sie |
| `hal/src/component/irq/riscv64.rs#complete_irq` | `src/drivers/plic.rs#complete_irq` | 同板块同名 | (*plic).complete, PLICCtxWrapper::new, PLICSrcWrapper::new | (*plic).complete, PLICCtxWrapper::new, PLICSrcWrapper::new, kernel_phys_dev_to_virt |

#### 系统调用
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `os/src/syscall/net.rs#set` | `src/syscall/io.rs#set` | 同板块同名 | (probe != 0).then, <[u8; 4]>::try_from, <[u8; 4]>::try_from(s).ok, Duration::from_secs, Err, Ok ...(+22) | 内部闭环逻辑 |
| `os/src/syscall/misc.rs#from_str` | `src/syscall/misc.rs#from_str` | 同板块同名同结构 | field[..text.len()].copy_from_slice, text.as_bytes, text.len | data[..info.len()].copy_from_slice, info.as_bytes, info.len |
| `os/src/syscall/io.rs#is_set` | `src/syscall/io.rs#is_set` | 同板块同名 | 内部闭环逻辑 | 内部闭环逻辑 |
| `os/src/syscall/fd.rs#readable` | `src/syscall/io.rs#readable` | 同板块同名 | matches! | self.contains |

#### 文件系统
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `os/src/fs/devfs/tty.rs#ioctl` | `src/fs/new_vfs/top.rs#ioctl` | 同板块同名 | Err, Ok, TtyIoctlCmd::from_repr, log::debug!, log::error!, self.meta.lock ...(+1) | Ok, dyn_future |
| `os/src/fs/vfs/dentry.rs#find` | `src/fs/nfat32/fs.rs#find` | 同板块同名 | DCACHE.rlock, Ok, Some, Vec::new, cache.get, cache.get(&(node.path() + &normalize_path)).cloned ...(+16) | Some, self.next |
| `hal/src/util/backtrace.rs#backtrace` | `src/xdebug/backtrace.rs#backtrace` | 同板块同名 | (current_fp as *const usize).offset, core::arch::asm!, println! | (current_fp as *const usize).offset, arch::fp, arch::lr, error! |
| `os/src/drivers/block/mmc/mod.rs#reset_clock` | `src/drivers/blk/dw_mshc/mmc.rs#reset_clock` | 同板块同名 | (self.virt_base_addr() as *mut CLKDIV)
                .byte_add, (self.virt_base_addr() as *mut CLKDIV)
                .byte_add(CLKDIV::offset())
                .write_volatile, (self.virt_base_addr() as *mut CLKENA)
                    .byte_add, (self.virt_base_addr() as *mut CLKENA)
                    .byte_add(CLKENA::offset())
                    .write_volatile, CLKDIV::new, CLKDIV::new().with_clk_divider0 ...(+10) | CLKDIV::new, CLKDIV::new().with_clk_divider0, CLKDIV::offset, CLKENA::new, CLKENA::new().with_cclk_enable, CLKENA::offset ...(+8) |

#### 设备驱动
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `os/src/drivers/serial/uart.rs#init_u32` | `src/drivers/serial/uart8250.rs#init_u32` | 同板块同名 | log::info!, regs.byte_add, regs.byte_add(self.reg_off(0)).write_volatile, regs.byte_add(self.reg_off(1)).write_volatile, regs.byte_add(self.reg_off(FCR)).write_volatile, regs.byte_add(self.reg_off(IER)).write_volatile ...(+4) | info!, reg.byte_add, reg.byte_add(0 << self.reg_shift).write_volatile, reg.byte_add(1 << self.reg_shift).write_volatile, reg.byte_add(FCR << self.reg_shift).write_volatile, reg.byte_add(IER << self.reg_shift).write_volatile ...(+2) |
| `os/src/drivers/serial/uart.rs#init_u8` | `src/drivers/serial/uart8250.rs#init_u8` | 同板块同名 | regs.byte_add, regs.byte_add(self.reg_off(0)).write_volatile, regs.byte_add(self.reg_off(1)).write_volatile, regs.byte_add(self.reg_off(IER)).write_volatile, regs.byte_add(self.reg_off(LCR)).write_volatile, regs.byte_add(self.reg_off(MCR)).write_volatile ...(+2) | reg.byte_add, reg.byte_add(0 << self.reg_shift).write_volatile, reg.byte_add(1 << self.reg_shift).write_volatile, reg.byte_add(FCR << self.reg_shift).write_volatile, reg.byte_add(IER << self.reg_shift).write_volatile, reg.byte_add(LCR << self.reg_shift).write_volatile ...(+1) |
| `os/src/drivers/serial/uart.rs#send_u32` | `src/drivers/serial/uart8250.rs#send_u32` | 同板块同名 | b' '.into, c.into, matches!, regs.byte_add, regs.byte_add(thr).write_volatile, self.reg_off ...(+1) | b' '.into, c.into, ptr.byte_add, ptr.byte_add(THR << self.reg_shift).write_volatile, wait_for! |
| `os/src/drivers/serial/uart.rs#send_u8` | `src/drivers/serial/uart8250.rs#send_u8` | 同板块同名 | matches!, regs.byte_add, regs.byte_add(thr).write_volatile, self.reg_off, wait_for! | ptr.byte_add, ptr.byte_add(THR << self.reg_shift).write_volatile, wait_for! |

#### 网络协议栈
仅目标项目中观察到该模块代表函数，参考项目没有同模块可对齐证据：
- 目标项目：`os/src/net/crypto.rs#encode`，调用摘要：AlgType::from_bytes, CmsgLevel::from, CmsgType::parse, Err, Ok, Vec::new ...(+20)
- 目标项目：`os/src/net/socketpair.rs#poll`，调用摘要：PollEvents::empty, events.contains, get_waker, inbound.is_empty, my_waker.clone, outbound.is_full ...(+13)
- 目标项目：`os/src/net/crypto.rs#cmsghdr_from_bytes`，调用摘要：CmsgLevel::from, CmsgType::parse, Err, Ok, bytes.len, i32::from_ne_bytes ...(+6)
- 目标项目：`user/src/bin/tcp.rs#server`，调用摘要：(0..100).for_each, accept, bind, buf.as_mut_slice, close, core::mem::size_of::<SockaddrIn> ...(+11)

#### 文件系统/第三方库
仅目标项目中观察到该模块代表函数，参考项目没有同模块可对齐证据：
- 目标项目：`os/src/fs/ext4_native/native/inode.rs#create`，调用摘要：Ok, Some, bitmap::alloc_block, bitmap::alloc_inode, core::slice::from_raw_parts_mut, core::slice::from_ref ...(+21)
- 目标项目：`os/src/fs/ext4_native/native/inode.rs#write_at`，调用摘要：(*disk).clone, Err, Ext4InodeDisk::clone, Ok, alloc::vec!, bitmap::alloc_block ...(+25)
- 目标项目：`os/src/fs/ext4_native/native/superblock.rs#read`，调用摘要：assert_eq!, bdev.direct_read_block, log::info!, u16::from_le_bytes, u32::from_le_bytes
- 目标项目：`os/src/fs/ext4_native/native/inode.rs#unlink`，调用摘要：(*disk).clone, Ok, Some, core::slice::from_ref, crate::timer::get_current_time, directory::remove_entry ...(+13)

#### 通用模块/其他
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `hal/src/util/mutex.rs#lock` | `src/sync/mutex.rs#lock` | 同板块同名 | SieGuard::new, core::hint::spin_loop, panic!, self.mutex.compare_exchange, self.mutex.compare_exchange(false, true, Ordering::Acquire, Ordering::Relaxed).is_ok | S::before_lock, self.ensure_support, self.obtain_lock |
| `os/src/sync/async_mutex.rs#lock` | `src/sync/sleep.rs#lock` | 同板块同名 | crate::utils::async_utils::get_waker, crate::utils::async_utils::suspend_now, self.try_lock, self.waker.cancel, self.waker.register | 内部闭环逻辑 |
| `os/src/diag/heartbeat.rs#write_str` | `src/utils.rs#write_str` | 同板块同名 | Ok, s.as_bytes, self.buf.len | Ok, s.as_bytes, self.inner.write, self.inner.write(s.as_bytes()).unwrap |
| `utils/range-map/src/lib.rs#get_mut` | `src/lazy_init.rs#get_mut` | 同板块同名 | Some, self.0.range_mut, self.0.range_mut(..=key).next_back | self.check_init, self.get_mut_unchecked |


### 1. 宏观判定
- **定性结论**: **独立开发** (相似度: 2.16%，共同 AST 指纹: 84；唯一 AST 指纹: 目标项目 2741，参考项目 1233；覆盖率: 目标项目 3.06%，参考项目 6.81%)

### 2. 全模块覆盖矩阵解读
- **进程管理**: 双方均存在该模块。存在 7 个共同 AST 指纹，目标覆盖率 2.98%，参考覆盖率 2.92%。极低的覆盖率表明双方在进程管理的底层实现上高度独立。控方指控的函数相似性源于 POSIX 系统调用标准与 Rust 同步原语惯用法，证据强度为**弱**。
- **任务调度/异步运行时**: 双方均存在该模块。共同 AST 指纹为 0，覆盖率均为 0.0%。目标项目实现了复杂的多核 SMP 调度（`PROCESSORS`、`rt_hint`），而参考项目为简单的单队列 `async_task` 模型，两者在架构上完全不同，**未形成**有效证据。
- **内存管理**: 双方均存在该模块。存在 2 个共同 AST 指纹，目标覆盖率 0.3%，参考覆盖率 0.71%。覆盖率极低，且共同指纹主要出现在设备驱动初始化等非核心内存管理逻辑中，证据强度为**弱**。
- **中断/异常处理**: 双方均存在该模块。存在 1 个共同 AST 指纹，目标覆盖率 1.52%，参考覆盖率 3.85%。共同指纹源于不同架构（LoongArch vs RISC-V）中断控制器驱动中通用的 `claim_irq`/`complete_irq` 模式，属于硬件操作规范，证据强度为**弱**。
- **系统调用**: 双方均存在该模块。存在 1 个共同 AST 指纹，目标覆盖率 0.6%，参考覆盖率 8.33%。共同指纹源于 `bitflags!` 宏生成的标准方法或简单的内部闭环逻辑，属于 Rust 生态惯用法，证据强度为**弱**。
- **文件系统**: 双方均存在该模块。存在 12 个共同 AST 指纹，目标覆盖率 2.67%，参考覆盖率 3.12%。共同指纹分散在设备驱动、procfs、backtrace 等不同子系统中，未形成集中的同源证据链，证据强度为**弱**。
- **设备驱动**: 双方均存在该模块。存在 18 个共同 AST 指纹，目标覆盖率 10.65%，参考覆盖率 16.67%。共同指纹主要集中在 UART 和 MMC 等硬件的标准初始化与操作序列上，这些序列由硬件手册规定，属于领域内通用知识，证据强度为**中**。
- **网络协议栈**: 仅目标项目存在该模块。共同 AST 指纹为 0，覆盖率均为 0.0%。目标项目拥有完整的自研网络协议栈，而参考项目完全缺失此模块，**未形成**有效证据。
- **第三方库/公共依赖**: 仅目标项目存在该模块（如 ext4 原生实现）。共同 AST 指纹为 0，覆盖率均为 0.0%。目标项目包含大量参考项目所没有的第三方库或独立子系统，**未形成**有效证据。

### 3. 底层 AST 关键证据指纹表
| 所属板块 | 目标项目路径 | 参考项目路径 | 证据类型 | 技术说明 |
| :--- | :--- | :--- | :--- | :--- |
| 设备驱动 | `os/src/drivers/serial/uart.rs#init_u32` | `src/drivers/serial/uart8250.rs#init_u32` | 同板块同名 | 两者均遵循 UART 8250 标准初始化序列（FCR、IER、LCR 等寄存器操作），调用图谱高度相似，但这是由硬件手册规定的通用流程。 |
| 设备驱动 | `os/src/drivers/serial/uart.rs#send_u32` | `src/drivers/serial/uart8250.rs#send_u32` | 同板块同名 | 两者均实现了通过检查状态寄存器后向 THR 写入数据的标准 UART 发送流程，属于硬件驱动的标准范式。 |
| 设备驱动 | `os/src/drivers/block/mmc/mod.rs#card_init` | `src/drivers/blk/dw_mshc/mmc.rs#card_init` | 同板块同名 | 两者均包含 MMC/SD 卡初始化命令序列（CMD0, CMD8, CMD55, ACMD41 等），这是由 MMC 规范定义的，非代码抄袭。 |
| 设备驱动 | `os/src/drivers/block/mmc/mod.rs#reset_clock` | `src/drivers/blk/dw_mshc/mmc.rs#reset_clock` | 同板块同名 | 两者均操作 DW MSHC 控制器的 CLKDIV 和 CLKENA 寄存器，调用图谱相似源于对同一款硬件 IP 的驱动开发。 |
| 中断/异常处理 | `hal/src/component/irq/riscv64.rs#complete_irq` | `src/drivers/plic.rs#complete_irq` | 同板块同名 | 两者均调用 `(*plic).complete` 来完成 PLIC 中断，这是 RISC-V PLIC 硬件标准操作，且目标项目同时支持 LoongArch。 |
| 通用模块/其他 | `hal/src/util/backtrace.rs#backtrace` | `src/xdebug/backtrace.rs#backtrace` | 同板块同名 | 两者均通过帧指针（`current_fp`）回溯调用栈，这是操作系统内核调试的通用技术，实现方式相似但属于领域常识。 |
| 通用模块/其他 | `hal/src/util/mutex.rs#lock` | `src/sync/mutex.rs#lock` | 同板块同名 | 目标项目使用 `compare_exchange` 实现自旋锁，参考项目使用 `obtain_lock` 封装，底层同步原语实现路径不同。 |
| 文件系统 | `os/src/fs/vfs/dentry.rs#find` | `src/fs/nfat32/fs.rs#find` | 同板块同名 | 目标项目在全局 `DCACHE` 中查找，参考项目在链表 `self.next` 中迭代，缓存与查找算法完全不同。 |
| 系统调用 | `os/src/syscall/misc.rs#from_str` | `src/syscall/misc.rs#from_str` | 同板块同名同结构 | 两者均使用 `copy_from_slice` 从字节切片构造字符串，这是 Rust 标准惯用法，不构成独创性表达。 |
| 内存管理 | `os/src/devices/manager.rs#map_devices` | `src/drivers/manager.rs#map_devices` | 同板块同名 | 目标项目通过 `PciManager`/`MmioManager` 扫描设备树，参考项目通过 `boot_pagetable` 手动映射，设备发现机制完全不同。 |

### 4. 核心代码调用流对比剖析
* **【进程管理/进程调度与生命周期】**
* **底层技术析理**: 目标项目采用 `Task` 对象模型，通过全局 `TASK_MANAGER` 和 `PROCESSORS` 数组进行多核 SMP 调度，其 `spawn` 和 `run_until_idle` 函数涉及复杂的 CPU 亲和性、实时任务提示（`rt_hint`）及 TLB 刷新（`quiescent_if_needed`）逻辑。参考项目采用 `LightProcess` 对象模型，通过 `GlobalLProcManager` 进行管理，其调度器基于 `async_task` 库，`spawn` 和 `run_until_idle` 仅为简单的任务入队与执行。两者在进程抽象、调度算法和并发控制上存在根本性架构差异。
* **代码溯源链接**:
  - 目标项目: `os/src/executor/mod.rs#spawn` `os/src/executor/mod.rs#run_until_idle`
  - 参考项目: `src/executor/mod.rs#spawn` `src/executor/mod.rs#run_until_idle`

* **【设备驱动/UART 串口驱动】**
* **底层技术析理**: 这是双方相似度最高的板块。两者在 `init_u32` 和 `send_u32` 等函数中表现出相似的寄存器操作序列。然而，这源于双方均需驱动标准的 8250/16550 兼容 UART 硬件。对 FCR（FIFO 控制）、IER（中断使能）、LCR（线路控制）等寄存器的初始化顺序，以及通过检查 LSR（线路状态）寄存器后向 THR（发送保持）寄存器写入数据的流程，是由硬件数据手册严格定义的，属于该领域的通用知识，而非代码抄袭。
* **代码溯源链接**:
  - 目标项目: `os/src/drivers/serial/uart.rs#init_u32` `os/src/drivers/serial/uart.rs#send_u32`
  - 参考项目: `src/drivers/serial/uart8250.rs#init_u32` `src/drivers/serial/uart8250.rs#send_u32`

* **【中断/异常处理/PLIC 中断控制器驱动】**
* **底层技术析理**: 在 RISC-V 架构部分，两者的 `complete_irq` 函数均调用了 `(*plic).complete`。这是 RISC-V 平台级中断控制器（PLIC）的标准操作，用于通知硬件中断处理完成。该操作是 RISC-V 特权架构规范的一部分，任何 RISC-V 操作系统的实现都必然包含此调用。此外，目标项目同时支持 LoongArch 的 EIOINTC，展现了更广泛的架构适配能力。
* **代码溯源链接**:
  - 目标项目: `hal/src/component/irq/riscv64.rs#complete_irq`
  - 参考项目: `src/drivers/plic.rs#complete_irq`

### 5. 单方模块与降权项
- **网络协议栈**: 目标项目拥有完整的自研网络协议栈（`os/src/net/`），包含 TCP 状态机、UDP 处理、加密等复杂实现，而参考项目完全缺失此模块。这是目标项目独立开发的强有力证据。
- **第三方库/公共依赖**: 目标项目集成了 ext4 原生文件系统（`os/src/fs/ext4_native/`）等第三方库或复杂子系统，参考项目未包含。这进一步证明了两项目在功能范围和实现深度上的巨大差异。
- **跨板块证据**: 控方将目标项目中属于网络协议栈（`process_ack`）、文件系统（`init_procfs`）等模块的函数强行归入【进程管理】板块进行指控，违反了“跨板块不得作为强证据”的原则。这些函数在参考项目中无对应实现，反而证明了目标项目的独立性。
- **POSIX 接口与 Rust 惯用法**: 控方指控的 `sys_clone`、`sys_wait`、`sys_exit` 等函数的相似性，源于它们都必须遵循 POSIX/Linux 系统调用标准。`CloneFlags::from_bits`、`WaitOptions::from_bits_truncate` 等方法由 `bitflags!` 宏自动生成，`lock().clone()` 是 Rust 的惯用模式。这些属于共享的领域规范与语言生态，不能作为抄袭证据。

### 6. 保守结论
综合全模块覆盖矩阵、AST 指纹分析和核心调用流对比，目标项目与参考项目的客观 AST 相似度仅为 2.16%，处于极低水平。双方在进程调度模型（SMP vs 单队列）、内存管理、文件系统构成及网络协议栈等核心子系统上存在根本性架构差异。少量相似指纹高度集中在由硬件手册或行业标准严格定义的设备驱动（UART、MMC、PLIC）和 POSIX 系统调用接口上，属于领域通用知识与标准规范的必然体现，而非代码复用。目标项目包含大量参考项目完全缺失的自研模块（如网络协议栈、ext4 文件系统），进一步佐证了其独立开发的本质。因此，现有证据无法支持同源性或抄袭指控。

------------------------------

