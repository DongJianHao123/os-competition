# 🛡️ OS 内核赛自动化代码级架构审计报告

**审计时间**: `2026-07-02 11:27:18`
**参赛作品名称**: `2026-T202610486999648-XX`
**年份**: `2026`
**学校**: `XX`
**队伍名称**: `T202610486999648`
**比赛名字**: `OS-Competition`
**比赛子赛道**: `Kernel-Implementation`
**仓库地址**: `https://gitlab.eduxiji.net/educg-group-43501-3132633/T202610486999648-1756`
**推测开发语言**: `Rust / C (由AST探针自动识别)`
**特征函数量**: `3187`

## ⚔️ 底层代码同源性对比 (AST Homology)
> 本部分由专门的代码审查智能体，对该项目与符合年份条件的历届最高相似度项目进行控制流与底层调用对比。新版报告会先给出确定性的全模块覆盖矩阵，再给出 Agent 的分模块解释，避免只围绕少量抽样函数展开。

### 🏆 Top 5 底层对标: vs `2024-cabbageOS-华中科技大学`（华中科技大学 / cabbageOS）

#### 对方作品信息

**对方年份**: `2024`
**对方学校**: `华中科技大学`
**对方队伍名称**: `cabbageOS`
**对方比赛名字**: `OS-Competition`
**对方比赛子赛道**: `Kernel-Implementation`
**对方仓库地址**: `https://gitlab.eduxiji.net/educg-group-26010-2376550/T202410487992456-422`

> 双向覆盖率：共同 AST 指纹: 365；唯一 AST 指纹: 目标项目 2421，参考项目 1579；覆盖率: 目标项目 15.08%，参考项目 23.12%

## 全模块对比总览（确定性证据）

> 总体覆盖率：共同 AST 指纹: 365；唯一 AST 指纹: 目标项目 2421，参考项目 1579；覆盖率: 目标项目 15.08%，参考项目 23.12%

### 1. 模块覆盖矩阵
| 模块 | 目标函数数 | 参考函数数 | 共同唯一 AST 指纹 | Target 覆盖 | Base 覆盖 | 状态 |
| :--- | ---: | ---: | ---: | ---: | ---: | :--- |
| 进程管理 | 389 | 95 | 2 | 0.92% | 2.7% | 双方存在且有结构重合 |
| 任务调度/异步运行时 | 294 | 56 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |
| 内存管理 | 528 | 290 | 47 | 10.22% | 16.49% | 双方存在且有结构重合 |
| 中断/异常处理 | 77 | 28 | 2 | 3.23% | 8.7% | 双方存在且有结构重合 |
| 系统调用 | 94 | 76 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |
| 文件系统 | 599 | 176 | 26 | 6.68% | 17.69% | 双方存在且有结构重合 |
| 设备驱动 | 480 | 446 | 253 | 60.67% | 66.75% | 双方存在且有结构重合 |
| 网络协议栈 | 63 | 0 | 0 | 0.0% | 0.0% | 仅目标项目体现 |
| 文件系统/第三方库 | 384 | 522 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |
| 第三方库 | 1 | 1 | 1 | 100.0% | 100.0% | 双方存在且有结构重合 |
| 通用模块/其他 | 278 | 228 | 31 | 11.83% | 16.85% | 双方存在且有结构重合 |

### 2. 分模块证据清单

#### 进程管理
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `os/src/syscall/process.rs#sys_clone` | `kernel/src/syscall/sysproc.c#sys_clone` | 同板块同名 | Arc::downgrade, CloneFlags::from_bits, Err, Ok, Some, _tls.is_null ...(+31) | arg_addr, arg_int, do_clone |
| `os/src/syscall/process.rs#sys_sched_setscheduler` | `kernel/src/syscall/sysproc.c#sys_sched_setscheduler` | 同板块同名 | Err, Ok, current_task, current_task()
                .unwrap, current_task()
                .unwrap()
                .inner_exclusive_access, current_task()
                .unwrap()
                .inner_exclusive_access()
                .memory_set
                .clone ...(+9) | 内部闭环逻辑 |
| `os/src/syscall/process.rs#sys_exit_group` | `kernel/src/syscall/sysproc.c#sys_exit_group` | 同板块同名 | current_task, current_task().unwrap, drop, exit_current_and_run_next, inner.thread_tcb.iter, inner.thread_tcb.iter().cloned ...(+10) | atomic_read, do_exit_group, proc_current |
| `os/src/syscall/process.rs#sys_sched_getparam` | `kernel/src/syscall/sysproc.c#sys_sched_getparam` | 同板块同名 | Err, Ok, current_task, current_task()
                .unwrap, current_task()
                .unwrap()
                .inner_exclusive_access, current_task()
                .unwrap()
                .inner_exclusive_access()
                .memory_set
                .clone ...(+10) | 内部闭环逻辑 |

#### 任务调度/异步运行时
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `os/src/syscall/fs.rs#sys_lseek` | `kernel/src/syscall/sysfile.c#sys_lseek` | 同板块同名 | Err, Ok, _file.get_dentry, _file.get_dentry().get_inode, _file.get_dentry().get_inode()?.get_size, _file.get_dentry().is_fifo ...(+18) | MAX, arg_addr, arg_fd, arg_int, panic, strncmp ...(+1) |
| `os/src/syscall/fs.rs#sys_faccessat` | `kernel/src/syscall/sysfile.c#sys_faccessat` | 同板块同名 | AtFlags::from_bits_truncate, Err, FaccessatMode::from_bits_truncate, Ok, at_path_to_dentry, current_task ...(+11) | arg_int, arg_str, find_inode, get_absolute_path, get_fs_from_path, ip->i_op->lock ...(+3) |
| `os/src/syscall/fs.rs#sys_mkdirat` | `kernel/src/syscall/sysfile.c#sys_mkdirat` | 同板块同名 | Err, InodeMode::DIR.bits, InodeMode::TYPE_MASK.bits, InodeMode::from_bits_truncate, Ok, OpenFlags::empty ...(+21) | arg_int, arg_str, assist_icreate, get_absolute_path, get_fs_from_path, ip->i_op->unlock_put ...(+2) |
| `os/src/task/futex.rs#futex_requeue` | `kernel/src/atomic/futex.c#futex_requeue` | 同板块同名 | FUTEX_Q.lock, Vec::new, VecDeque::new, claim_waiter, drop, futex_q
            .entry ...(+11) | Queue_isempty_atomic, Queue_provide_atomic, Queue_push_back_atomic, acquire, futex_free, futex_wakeup ...(+5) |

#### 内存管理
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `virtio-drivers/src/device/socket/connectionmanager.rs#send_recv` | `kernel/dep/virtio-drivers/src/device/socket/connectionmanager.rs#send_recv` | 同板块同名 | "Hello from guest".as_bytes, (hello_from_guest.len() as u32).into, (hello_from_host.len() as u32).into, 0.into, 50.into, Arc::new ...(+43) | "Hello from guest".as_bytes, (hello_from_guest.len() as u32).into, (hello_from_host.len() as u32).into, 0.into, 50.into, Arc::new ...(+43) |
| `virtio-drivers/src/device/socket/connectionmanager.rs#incoming_connection` | `kernel/dep/virtio-drivers/src/device/socket/connectionmanager.rs#incoming_connection` | 同板块同名同结构 | 0.into, 50.into, Arc::new, Default::default, Mutex::new, NonNull::from ...(+25) | 0.into, 50.into, Arc::new, Default::default, Mutex::new, NonNull::from ...(+25) |
| `os/src/syscall/process.rs#sys_futex` | `kernel/src/syscall/sysproc.c#sys_futex` | 同板块同名 | Err, FutexCmd::from_bits_truncate, FutexKey::new, FutexOpt::from_bits_truncate, Ok, Some ...(+24) | arg_addr, arg_int, arg_uint, copy_in, do_futex, proc_current |
| `os/src/syscall/memory.rs#sys_mmap` | `kernel/src/mm/mmap.c#sys_mmap` | 同板块同名 | Err, MapArea::new, MmapFlags::from_bits_truncate, Ok, Some, VirtAddr::new ...(+24) | acquire, arg_addr, arg_fd, arg_int, arg_long, arg_ulong ...(+3) |

#### 中断/异常处理
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `os/src/sync/up.rs#push_off` | `kernel/src/atomic/spinlock.c#push_off` | 同板块同名 | intr_off | intr_get, intr_off, mycpu |
| `os/src/sync/up.rs#pop_off` | `kernel/src/atomic/spinlock.c#pop_off` | 同板块同名 | intr_on | intr_get, intr_on, mycpu, panic |
| `virtio-drivers/examples/x86_64/src/main.rs#main` | `kernel/dep/virtio-drivers/examples/x86_64/src/main.rs#main` | 同板块同名同结构 | enumerate_pci, heap::init_heap, info!, logger::init, logger::init(log::LevelFilter::Info).unwrap, system_off ...(+1) | enumerate_pci, heap::init_heap, info!, logger::init, logger::init(log::LevelFilter::Info).unwrap, system_off ...(+1) |
| `virtio-drivers/examples/x86_64/src/trap.rs#init` | `kernel/dep/virtio-drivers/examples/x86_64/src/trap.rs#init` | 同板块同名同结构 | IDT.load, set_general_handler! | IDT.load, set_general_handler! |

#### 系统调用
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `os/src/syscall/memory.rs#sys_shmat` | `kernel/src/syscall/sysipc.c#sys_shmat` | 同板块同名 | Err, KernIpcPerm::from_bits, KernIpcPerm::from_bits(shmflag).unwrap, shm_at, shmflg.contains | arg_addr, arg_int, do_shmat |
| `user/src/lib.rs#waitpid` | `user/deps/syscall.c#waitpid` | 同板块同名 | sys_waitpid, yield_ | syscall |
| `user/src/lib.rs#wait` | `user/deps/syscall.c#wait` | 同板块同名 | sys_waitpid, yield_ | waitpid |
| `user/src/lib.rs#sleep` | `user/deps/syscall.c#sleep` | 同板块同名 | sys_get_time, sys_yield | syscall |

#### 文件系统
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `os/src/syscall/fs.rs#sys_mount` | `kernel/src/syscall/syscallnew.c#sys_mount` | 同板块同名 | BLOCK_DEVICE.get, BLOCK_DEVICE.get().cloned, Err, FILE_SYSTEMS.lock, MountFlags::from_bits_truncate, Ok ...(+24) | 内部闭环逻辑 |
| `virtio-drivers/src/device/blk.rs#device_id` | `kernel/dep/virtio-drivers/src/device/blk.rs#device_id` | 同板块同名 | Arc::new, BlkFeature::RING_INDIRECT_DESC.bits, BlkResp {
                            status: RespStatus::OK,
                        }
                        .as_bytes, Default::default, Mutex::new, NonNull::from ...(+19) | Arc::new, BlkFeature::RING_INDIRECT_DESC.bits, Default::default, Mutex::new, NonNull::from, QUEUE_SIZE.into ...(+14) |
| `virtio-drivers/src/device/blk.rs#flush` | `kernel/dep/virtio-drivers/src/device/blk.rs#flush` | 同板块同名 | (BlkFeature::RING_INDIRECT_DESC | BlkFeature::FLUSH).bits, Arc::new, BlkResp {
                            status: RespStatus::OK,
                        }
                        .as_bytes, Default::default, Mutex::new, NonNull::from ...(+19) | (BlkFeature::RING_INDIRECT_DESC | BlkFeature::FLUSH).bits, Arc::new, Default::default, Mutex::new, NonNull::from, QUEUE_SIZE.into ...(+13) |
| `virtio-drivers/src/transport/pci/bus.rs#bar_info` | `kernel/dep/virtio-drivers/src/transport/pci/bus.rs#bar_info` | 同板块同名同结构 | (!(size_mask & 0xfffffff0)).wrapping_add, Err, MemoryBarType::try_from, Ok, self.config_read_word, self.config_write_word ...(+1) | (!(size_mask & 0xfffffff0)).wrapping_add, Err, MemoryBarType::try_from, Ok, self.config_read_word, self.config_write_word ...(+1) |

#### 设备驱动
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `virtio-drivers/src/queue.rs#fake_read_write_queue` | `kernel/dep/virtio-drivers/src/queue.rs#fake_read_write_queue` | 同板块同名 | Vec::new, assert!, assert_eq!, assert_ne!, descriptor.flags.contains, descriptor.next ...(+15) | (*available_ring).idx.load, (*used_ring).idx.fetch_add, (*used_ring).idx.load, Vec::new, assert!, assert_eq! ...(+18) |
| `virtio-drivers/examples/x86_64/src/tcp.rs#test_echo_server` | `kernel/dep/virtio-drivers/examples/x86_64/src/tcp.rs#test_echo_server` | 同板块同名同结构 | Config::new, DeviceWrapper::new, Instant::from_micros_const, Interface::new, IpAddress::from_str, IpAddress::from_str(IP).unwrap ...(+44) | Config::new, DeviceWrapper::new, Instant::from_micros_const, Interface::new, IpAddress::from_str, IpAddress::from_str(IP).unwrap ...(+44) |
| `virtio-drivers/examples/riscv/src/tcp.rs#test_echo_server` | `kernel/dep/virtio-drivers/examples/riscv/src/tcp.rs#test_echo_server` | 同板块同名同结构 | Config::new, DeviceWrapper::new, Instant::from_micros_const, Interface::new, IpAddress::from_str, IpAddress::from_str(IP).unwrap ...(+44) | Config::new, DeviceWrapper::new, Instant::from_micros_const, Interface::new, IpAddress::from_str, IpAddress::from_str(IP).unwrap ...(+44) |
| `virtio-drivers/src/queue.rs#add_buffers` | `kernel/dep/virtio-drivers/src/queue.rs#add_buffers` | 同板块同名同结构 | MmioTransport::new, NonNull::from, VirtIOHeader::make_fake_header, VirtQueue::<FakeHal, 4>::new, VirtQueue::<FakeHal, 4>::new(&mut transport, 0, false, false).unwrap, assert! ...(+6) | MmioTransport::new, NonNull::from, VirtIOHeader::make_fake_header, VirtQueue::<FakeHal, 4>::new, VirtQueue::<FakeHal, 4>::new(&mut transport, 0, false, false).unwrap, assert! ...(+6) |

#### 网络协议栈
仅目标项目中观察到该模块代表函数，参考项目没有同模块可对齐证据：
- 目标项目：`lose-net-stack/src/packets/arp.rs#build_data`，调用摘要：ARP_HRD_ETHER.to_be, EthRtype::ARP.into, EthRtype::IP.into, UnsafeRefIter::new, data_ptr_iter.next_mut::<Arp>, data_ptr_iter.next_mut::<Eth> ...(+5)
- 目标项目：`lose-net-stack/src/connection/tcp.rs#accept`，调用摘要：Err, Ok, conn.clone, conn.syn_ack, self.clients.lock, self.clients.lock().push ...(+7)
- 目标项目：`lose-net-stack/src/connection/mod.rs#analysis_ip`，调用摘要：cache_arp_entry, data_ptr_iter.next::<Ip>, self.analysis_icmp, self.analysis_tcp, self.analysis_udp, unsafe { data_ptr_iter.next::<Ip>() }.unwrap
- 目标项目：`lose-net-stack/src/connection/mod.rs#analysis_arp`，调用摘要：ArpPacket::new, ArpType::form_u16, T::send, arp
                .reply_packet, arp
                .reply_packet(self.local_ip, self.local_mac)
                .expect, arp
                .reply_packet(self.local_ip, self.local_mac)
                .expect("can't build reply")
                .build_data ...(+4)

#### 文件系统/第三方库
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `ext4_rs-1.3.1/src/utils/bitmap.rs#ext4_bmap_bit_find_clr` | `kernel/src/fs/ext4/lwext4/ext4_bitmap.c#ext4_bmap_bit_find_clr` | 同板块同名 | ext4_bmap_is_bit_clr | ext4_bmap_is_bit_clr |
| `ext4_rs-1.3.1/src/simple_interface/mod.rs#ext4_parse_flags` | `kernel/src/fs/ext4/lwext4/ext4.c#ext4_parse_flags` | 同板块同名 | Err, Ext4Error::new, Ok | strcmp |
| `ext4_rs-1.3.1/src/simple_interface/mod.rs#ext4_dir_mk` | `kernel/src/fs/ext4/lwext4/ext4.c#ext4_dir_mk` | 同板块同名 | Ext4DirEntry::default, Ext4DirSearchResult::new, filetype.bits, r.is_ok, return_errno!, self.dir_find_entry ...(+1) | EXT4_MP_LOCK, EXT4_MP_UNLOCK, ext4_generic_open, ext4_get_mount |
| `ext4_rs-1.3.1/src/ext4_defs/super_block.rs#ext4_ialloc_bitmap_csum` | `kernel/src/fs/ext4/lwext4/ext4_ialloc.c#ext4_ialloc_bitmap_csum` | 同板块同名 | ext4_crc32c, uuid.len | ext4_crc32c, ext4_get32, ext4_sb_feature_ro_com |

#### 第三方库
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `virtio-drivers/src/transport/mmio.rs#vendor_id` | `kernel/dep/virtio-drivers/src/transport/mmio.rs#vendor_id` | 同板块同名同结构 | volread! | volread! |

#### 通用模块/其他
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `visionfive2-sd/src/lib.rs#init_sdcard` | `kernel/dep/sdcard/src/visionfive2_sd/sd.rs#init_sdcard` | 同板块同名 | CardTypeReg::from, CardTypeReg::from(0).with_card_width4_1, CmdArg::new, CmdReg::from, ControlReg::from, RawInterruptStatusReg::from ...(+20) | CardTypeReg::from, CardTypeReg::from(0).with_card_width4_1, CmdArg::new, CmdReg::from, ControlReg::from, RawInterruptStatusReg::from ...(+20) |
| `visionfive2-sd/src/lib.rs#reset_clock` | `kernel/dep/sdcard/src/visionfive2_sd/sd.rs#reset_clock` | 同板块同名同结构 | ClockDividerReg::new, ClockDividerReg::new().with_clk_divider0, ClockEnableReg::from, CmdArg::new, CmdReg::from, CmdReg::from(0)
        .with_start_cmd ...(+9) | ClockDividerReg::new, ClockDividerReg::new().with_clk_divider0, ClockEnableReg::from, CmdArg::new, CmdReg::from, CmdReg::from(0)
        .with_start_cmd ...(+9) |
| `visionfive2-sd/src/lib.rs#test_write_read` | `kernel/dep/sdcard/src/visionfive2_sd/sd.rs#test_write_read` | 同板块同名同结构 | CmdArg::new, CmdReg::from, DataTransType::Read, DataTransType::Write, buffer.as_slice, buffer.fill ...(+6) | CmdArg::new, CmdReg::from, DataTransType::Read, DataTransType::Write, buffer.as_slice, buffer.fill ...(+6) |
| `visionfive2-sd/src/lib.rs#check_bus_width` | `kernel/dep/sdcard/src/visionfive2_sd/sd.rs#check_bus_width` | 同板块同名 | CmdArg::new, CmdReg::from, DataTransType::Read, pprintln!, read_fifo, send_cmd::<_, S> ...(+3) | CmdArg::new, CmdReg::from, DataTransType::Read, info!, pprintln!, read_fifo ...(+4) |


好的，审判长。本庭已收到控辩双方的全部陈述、证据底座及原始 AST 抽样数据。现在，本庭将依据《最终同源性定性仲裁》程序，对本案进行独立、客观的深度析理，并作出最终裁决。

### 1. 宏观判定
- **定性结论**: **独立开发，底层架构显著差异**
  本案客观 AST 相似度仅为 **10.04%**。目标项目拥有 **2421** 个唯一 AST 指纹（占比 84.92%），参考项目拥有 **1579** 个唯一指纹（占比 76.88%）。双方在核心操作系统逻辑上的实现路径、内存模型和同步策略存在根本性异构。控方所指控的“抄袭铁证”实为双方共同依赖的第三方开源库，不构成对参考项目的代码抄袭。

### 2. 全模块覆盖矩阵解读
本庭对全模块覆盖矩阵进行逐项审查，以确定各板块的证据强度：

- **进程管理**: 双方均存在该模块。共同唯一 AST 指纹仅 **2** 个，目标覆盖率 0.92%，参考覆盖率 2.7%。极低的覆盖率表明，尽管存在大量同名系统调用，但底层实现结构完全不同。**证据强度：弱**。
- **任务调度/异步运行时**: 双方均存在该模块。共同唯一 AST 指纹为 **0**。双方调度器的核心数据结构与调度算法在 AST 层面无任何重合。**证据强度：未形成**。
- **内存管理**: 双方均存在该模块。共同唯一 AST 指纹 **47** 个，目标覆盖率 10.22%，参考覆盖率 16.49%。重合指纹主要来自 `virtio-drivers` 等共享第三方库，核心内存管理逻辑（如页表、分配器）异构。**证据强度：中（指向第三方库共享）**。
- **中断/异常处理**: 双方均存在该模块。共同唯一 AST 指纹 **2** 个，目标覆盖率 3.23%，参考覆盖率 8.7%。重合点 `push_off`/`pop_off` 为常见关中断模式，且 AST 图谱存在差异。**证据强度：弱**。
- **系统调用**: 双方均存在该模块。共同唯一 AST 指纹为 **0**。尽管存在大量同名函数，但 Rust 与 C 的实现路径在 AST 层面无重合。**证据强度：未形成**。
- **文件系统**: 双方均存在该模块。共同唯一 AST 指纹 **26** 个，目标覆盖率 6.68%，参考覆盖率 17.69%。重合主要来自 `ext4_rs`/`lwext4` 和 `virtio-drivers` 等第三方库。**证据强度：中（指向第三方库共享）**。
- **设备驱动**: 双方均存在该模块。共同唯一 AST 指纹 **253** 个，目标覆盖率 60.67%，参考覆盖率 66.75%。高覆盖率完全由 `virtio-drivers` 和 `visionfive2-sd` 这两个共享的第三方 crate 贡献。**证据强度：中（指向第三方库共享）**。
- **网络协议栈**: **仅目标项目体现**。参考项目无此模块。目标项目独立实现了 `lose-net-stack`，这是独立开发的有力佐证。**证据强度：未形成**。
- **第三方库/公共依赖**: 双方均存在。共同唯一 AST 指纹 **1** 个，覆盖率均为 100%。这直接证实了双方引用了完全相同的第三方库代码。**证据强度：中（指向第三方库共享）**。

### 3. 底层 AST 关键证据指纹表
本庭提取以下关键 AST 指纹作为裁决依据。表格按模块优先级排序，第三方库证据后置。

| 所属板块 | 目标项目路径 | 参考项目路径 | 证据类型 | 技术说明 |
| :--- | :--- | :--- | :--- | :--- |
| 进程管理 | `os/src/syscall/process.rs#sys_clone` | `kernel/src/syscall/sysproc.c#sys_clone` | 同板块同名 | 函数名相同，但目标项目调用 `Arc::downgrade`, `add_task` 等 Rust 特有原语，参考项目调用 `arg_addr`, `do_clone` 等 C 风格原语。AST 结构完全不同，属 POSIX 标准接口的跨语言实现。 |
| 进程管理 | `os/src/syscall/process.rs#sys_exit_group` | `kernel/src/syscall/sysproc.c#sys_exit_group` | 同板块同名 | 目标项目调用 `exit_current_and_run_next`, `inner.thread_tcb.iter()`，参考项目调用 `atomic_read`, `do_exit_group`。实现骨骼异构。 |
| 内存管理 | `os/src/syscall/memory.rs#sys_mmap` | `kernel/src/mm/mmap.c#sys_mmap` | 同板块同名 | 目标项目调用 `MapArea::new`, `MmapFlags::from_bits_truncate`，参考项目调用 `arg_addr`, `arg_fd` 等。内存抽象模型完全不同。 |
| 中断/异常处理 | `os/src/sync/up.rs#push_off` | `kernel/src/atomic/spinlock.c#push_off` | 同板块同名 | 目标项目直接调用 `intr_off`，参考项目调用 `intr_get`, `intr_off`, `mycpu`。后者包含 CPU 状态检查，逻辑更复杂，非同构。 |
| 设备驱动 | `visionfive2-sd/src/lib.rs#wait_ms_util_response` | `kernel/dep/sdcard/src/visionfive2_sd/sd.rs#wait_ms_util_response` | 第三方库/公共依赖 | **AST 调用图谱完全一致**。双方均引用了相同的 `visionfive2-sd` 第三方 crate。参考项目路径中的 `dep/` 目录证实其亦为依赖项，非原创。 |
| 设备驱动 | `visionfive2-sd/src/lib.rs#wait_ms_util_can_send_data` | `kernel/dep/sdcard/src/visionfive2_sd/sd.rs#wait_ms_util_can_send_data` | 第三方库/公共依赖 | **AST 调用图谱完全一致**。同上，属共同依赖第三方库。 |
| 设备驱动 | `visionfive2-sd/src/lib.rs#wait_ms_util_can_send_cmd` | `kernel/dep/sdcard/src/visionfive2_sd/sd.rs#wait_ms_util_can_send_cmd` | 第三方库/公共依赖 | **AST 调用图谱完全一致**。同上，属共同依赖第三方库。 |
| 第三方库 | `virtio-drivers/src/transport/mmio.rs#vendor_id` | `kernel/dep/virtio-drivers/src/transport/mmio.rs#vendor_id` | 第三方库/公共依赖 | **AST 调用图谱完全一致**。双方均使用了 `virtio-drivers` 公共 crate。 |

### 4. 核心代码调用流对比剖析
本庭选取双方均存在的核心模块，进行底层技术析理，以论证其架构异构性。

* **【进程管理/系统调用路径】**
* **底层技术析理**: 以 `sys_clone` 为例，目标项目（Rust）的实现基于 `Arc` 智能指针和 `TaskControlBlock` 的内部可变性模式，通过 `add_task` 和 `block_current_and_run_next` 管理任务生命周期。参考项目（C）的实现则基于裸指针和显式锁（`acquire`/`release`），通过 `do_clone` 函数操作进程控制块。两者在内存管理、所有权模型和并发控制策略上存在根本性差异，AST 调用图谱无重合。
* **代码溯源链接**:
  - 目标项目: `os/src/syscall/process.rs#sys_clone`
  - 参考项目: `kernel/src/syscall/sysproc.c#sys_clone`

* **【内存管理/系统调用路径】**
* **底层技术析理**: 以 `sys_mmap` 为例，目标项目通过 `MapArea::new` 和 `MmapFlags::from_bits_truncate` 构建内存区域对象，体现了 Rust 的类型安全和所有权模型。参考项目则通过 `arg_addr`, `arg_fd` 等参数解析函数获取参数，并直接操作内核内存结构。两者的抽象层次完全不同，目标项目使用了更高阶的抽象，而参考项目更接近底层 C 语言操作。
* **代码溯源链接**:
  - 目标项目: `os/src/syscall/memory.rs#sys_mmap`
  - 参考项目: `kernel/src/mm/mmap.c#sys_mmap`

* **【中断/异常处理/同步原语】**
* **底层技术析理**: 以 `push_off` 为例，目标项目的实现是直接调用架构相关的 `intr_off` 函数。参考项目的实现则更复杂，它首先调用 `intr_get` 判断当前中断状态，再调用 `intr_off` 关中断，并将之前的状态保存到 CPU 本地变量中。这种“保存-关闭”的模式是经典的自旋锁实现的一部分，与目标项目的简单关闭逻辑在 AST 结构上存在显著差异。
* **代码溯源链接**:
  - 目标项目: `os/src/sync/up.rs#push_off`
  - 参考项目: `kernel/src/atomic/spinlock.c#push_off`

### 5. 单方模块与降权项
本庭对以下情况进行说明，并明确其不能作为强同源证据：
- **单方模块**: **网络协议栈** (`lose-net-stack`) 仅存在于目标项目中，是目标项目独立开发的有力证据。
- **第三方库/公共依赖**: `virtio-drivers`、`visionfive2-sd`、`ext4_rs`/`lwext4` 等 crate 在双方项目中均有出现，并贡献了设备驱动、文件系统等模块的大部分共同指纹。这些属于对开源生态的共同依赖，不构成抄袭。
- **公共接口/泛型函数名**: 所有 POSIX 标准系统调用名（如 `sys_clone`, `sys_mmap`, `sys_getpid` 等）以及 Rust 泛型函数（如 `Default::default`, `Mutex::new`）均属于语言标准或公共接口，其同名不能作为抄袭证据。本庭已将其全部降权。

### 6. 保守结论
综合全案证据，目标项目 `[2026-T202610486999648-XX]` 与参考项目 `[2024-cabbageOS-华中科技大学]` 的客观 AST 相似度仅为 10.04%，远低于构成抄袭的阈值。84.92% 的唯一 AST 指纹表明其核心逻辑为独立实现。控方提出的“SD卡驱动铁证”经查属于双方共同依赖的第三方开源 crate `visionfive2-sd`，参考项目自身也将其置于 `dep/` 目录下，证明其非原创。双方在进程管理、内存管理等核心模块的实现上，采用了完全不同的语言范式、内存模型和同步策略，架构差异显著。目标项目独立实现的网络协议栈进一步佐证了其原创性。因此，本庭裁定，**抄袭指控不成立**。

------------------------------

