# 🛡️ OS 内核赛自动化代码级架构审计报告

**审计时间**: `2026-07-02 05:02:33`
**参赛作品名称**: `2026-T2026104869910054-XX`
**年份**: `2026`
**学校**: `XX`
**队伍名称**: `T2026104869910054`
**比赛名字**: `OS-Competition`
**比赛子赛道**: `Kernel-Implementation`
**仓库地址**: `https://gitlab.eduxiji.net/educg-group-43501-3132633/T2026104869910054-3130`
**推测开发语言**: `Rust / C (由AST探针自动识别)`
**特征函数量**: `2260`

## ⚔️ 底层代码同源性对比 (AST Homology)
> 本部分由专门的代码审查智能体，对该项目与符合年份条件的历届最高相似度项目进行控制流与底层调用对比。新版报告会先给出确定性的全模块覆盖矩阵，再给出 Agent 的分模块解释，避免只围绕少量抽样函数展开。

### 🏆 Top 1 底层对标: vs `2025-NoAxiom-杭州电子科技大学`（杭州电子科技大学 / NoAxiom）

#### 对方作品信息

**对方年份**: `2025`
**对方学校**: `杭州电子科技大学`
**对方队伍名称**: `NoAxiom`
**对方比赛名字**: `OS-Competition`
**对方比赛子赛道**: `Kernel-Implementation`
**对方仓库地址**: `https://gitlab.eduxiji.net/educg-group-36002-2710490/T202510336995214-148`

> 双向覆盖率：共同 AST 指纹: 32；唯一 AST 指纹: 目标项目 1950，参考项目 2349；覆盖率: 目标项目 1.64%，参考项目 1.36%

## 全模块对比总览（确定性证据）

> 总体覆盖率：共同 AST 指纹: 32；唯一 AST 指纹: 目标项目 1950，参考项目 2349；覆盖率: 目标项目 1.64%，参考项目 1.36%

### 1. 模块覆盖矩阵
| 模块 | 目标函数数 | 参考函数数 | 共同唯一 AST 指纹 | Target 覆盖 | Base 覆盖 | 状态 |
| :--- | ---: | ---: | ---: | ---: | ---: | :--- |
| 进程管理 | 2 | 207 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |
| 任务调度/异步运行时 | 2 | 434 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |
| 内存管理 | 3 | 382 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |
| 中断/异常处理 | 0 | 107 | 0 | 0.0% | 0.0% | 仅参考项目体现 |
| 系统调用 | 27 | 48 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |
| 文件系统 | 2 | 877 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |
| 设备驱动 | 0 | 227 | 0 | 0.0% | 0.0% | 仅参考项目体现 |
| 网络协议栈 | 1 | 53 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |
| 文件系统/第三方库 | 254 | 49 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |
| 网络协议栈/第三方库 | 137 | 0 | 0 | 0.0% | 0.0% | 仅目标项目体现 |
| 第三方库 | 1805 | 0 | 0 | 0.0% | 0.0% | 仅目标项目体现 |
| 通用模块/其他 | 27 | 552 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |

### 2. 分模块证据清单

#### 进程管理
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `user/apps/user_init/src/main.rs#wait_exit_code` | `NoAxiom/kernel/src/syscall/process.rs#sys_futex` | 同模块上下文对齐/差异观察 | Some | Duration::from, Err, FUTEX_SHARED_QUEUE
                            .lock, FUTEX_SHARED_QUEUE
                            .lock()
                            .requeue, FUTEX_SHARED_QUEUE.lock, FUTEX_SHARED_QUEUE.lock().wake_waiter ...(+34) |
| `user/libc/src/lib.rs#fork` | `NoAxiom/kernel/src/fs/vfs/impls/proc/mod.rs#init` | 同模块上下文对齐/差异观察 | clone | "32768\0".as_bytes, Arc::new, Arc::new(RamFsDentry::new(
        Some(fs_root.clone()),
        "self",
        fs_root.super_block(),
    ))
    .into_dyn, Arc::new(RamFsDentry::new(
        Some(fs_root.clone()),
        "sys",
        fs_root.super_block(),
    ))
    .into_dyn, ExeDentry::new, ExeInode::new ...(+51) |

#### 任务调度/异步运行时
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `user/apps/user_init/src/main.rs#run_lmbench_cases` | `NoAxiom/kernel/src/fs/vfs/basic/dentry.rs#__walk_path` | 同模块上下文对齐/差异观察 | cstr_eq, run_busybox, run_program, write_group_end, write_group_start, write_str | Err, FileFlags::empty, Ok, assert_no_lock!, block_on, block_on(file.load_dir()).expect ...(+23) |
| `user/apps/user_init/src/main.rs#init_etc_files` | `NoAxiom/kernel/src/syscall/signal.rs#sys_kill` | 同模块上下文对齐/差异观察 | run_sh_command | Err, Ok, PROCESS_GROUP_MANAGER
                    .lock, PROCESS_GROUP_MANAGER
                    .lock()
                    .get_group, PROCESS_GROUP_MANAGER
                    .lock()
                    .get_group(pgid)
                    .unwrap, PROCESS_GROUP_MANAGER
                    .lock()
                    .get_group(pgid)
                    .unwrap()
                    .into_iter ...(+16) |

#### 内存管理
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `user/apps/user_init/src/main.rs#run_script` | `NoAxiom/kernel/src/syscall/syscall.rs#syscall_inner` | 同模块上下文对齐/差异观察 | chdir, cstr_eq, reap_adopted_zombies, run_cyclictest_cases, run_iperf_cases, run_libctest_scripts ...(+6) | Ok, Self::empty_syscall, Self::sys_gettimeofday, Self::sys_madvise, Self::sys_syslog, Self::sys_systemshutdown ...(+156) |
| `user/apps/user_init/src/main.rs#run_sh_command` | `NoAxiom/kernel/src/fs/vfs/impls/proc/status/file.rs#base_read` | 同模块上下文对齐/差异观察 | "-c\0".as_ptr, "busybox\0".as_ptr, "sh\0".as_ptr, busybox_path, command.as_ptr, core::ptr::null::<u8> ...(+6) | "0".to_string, "conditional enabled".to_string, "thread vulnerable".to_string, Ok, String::new, TASK_MANAGER.get ...(+31) |
| `user/apps/user_init/src/main.rs#reap_adopted_zombies` | `NoAxiom/kernel/src/mm/memory_set.rs#map_elf` | 同模块上下文对齐/差异观察 | wait4 | ((ph.virtual_addr() + ph.mem_size()) as usize + base_offset).into, (ph.virtual_addr() as usize + base_offset).into, ElfFile::new, ElfFile::new(elf_buf.as_slice()).map_err, MapArea::new, Ok ...(+43) |

#### 中断/异常处理
仅参考项目中观察到该模块代表函数，目标项目没有同模块可对齐证据：
- 参考项目：`NoAxiom/kernel/src/include/fs.rs#new`，调用摘要：内部闭环逻辑
- 参考项目：`NoAxiom/kernel/src/signal/sig_action.rs#new_default`，调用摘要：内部闭环逻辑
- 参考项目：`NoAxiom/kernel/src/trap/soft_int.rs#ipi_handler`，调用摘要：Arch::clear_ipi, Arch::tlb_flush, current_ipi_info, info!, trace!
- 参考项目：`NoAxiom/kernel/src/trap/ktrap_handler.rs#kernel_interrupt_handler`，调用摘要：ext_int_handler, inc_interrupts_count, kernel_timer_trap_handler, soft_int_handler

#### 系统调用
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `user/libc/src/lib.rs#execve` | `user/libd/src/syscall/wrapper.rs#execve` | 同板块同名 | argv.as_ptr, envp.as_ptr, path.as_ptr, syscall::syscall3 | args.as_ptr, envp.as_ptr, path.as_ptr, sys_execve |
| `user/libc/src/lib.rs#exit` | `user/libd/src/syscall/wrapper.rs#exit` | 同板块同名 | core::hint::spin_loop, syscall::syscall1 | sys_exit |
| `user/libc/src/lib.rs#chdir` | `user/libd/src/syscall/wrapper.rs#chdir` | 同板块同名 | path.as_ptr, syscall::syscall1 | path.as_ptr, sys_chdir |
| `user/libc/src/lib.rs#clone` | `NoAxiom/kernel/src/syscall/net.rs#sys_getsockopt` | 同模块上下文对齐/差异观察 | syscall::syscall5 | CONGESTION.as_bytes, CONGESTION.len, Ok, PosixSocketOption::from_repr, PosixTcpSocketOptions::from_repr, SocketLevel::try_from ...(+12) |

#### 文件系统
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `user/apps/user_init/src/main.rs#test_env` | `user/libd/src/lib_widget.rs#on_event` | 同模块上下文对齐/差异观察 | ENV_LIBRARY_PATH.as_ptr, ENV_LMBENCH_ENOUGH.as_ptr, ENV_LTP_DEV.as_ptr, ENV_LTP_DEV_FS_TYPE.as_ptr, ENV_PATH.as_ptr, ENV_TERM.as_ptr ...(+1) | "input text".to_string, Box::new, Decoder::convert_key, PaintElement::Line, PaintElement::Rect, PaintElement::Text ...(+24) |
| `user/apps/user_init/src/main.rs#init_environment` | `NoAxiom/kernel/src/fs/vfs/basic/dentry.rs#mknodat_son` | 同模块上下文对齐/差异观察 | init_arch_environment, init_etc_files | Arc::new, Arc::new(LoopControlDentry::new(
                            Some(self.clone()),
                            name,
                            super_block.clone(),
                        ))
                        .into_dyn, Arc::new(LoopDevDentry::new(
                            Some(self.clone()),
                            &format!("loop{}", y),
                            super_block.clone(),
                        ))
                        .into_dyn, Arc::new(NullDentry::new(
                            Some(self.clone()),
                            name,
                            super_block.clone(),
                        ))
                        .into_dyn, Arc::new(PipeDentry::new(name)).into_dyn, Arc::new(RtcDentry::new(
                            Some(self.clone()),
                            name,
                            super_block.clone(),
                        ))
                        .into_dyn ...(+42) |

#### 设备驱动
仅参考项目中观察到该模块代表函数，目标项目没有同模块可对齐证据：
- 参考项目：`NoAxiom/lib/driver_ahci/src/drv_ahci.rs#ahci_host_init`，调用摘要：ahci_mdelay, ahci_port_base, ahci_readl, ahci_writel, log::debug!
- 参考项目：`NoAxiom/lib/driver/src/impls/block/vf2_sdcard/mod.rs#init_sdcard`，调用摘要：CardTypeReg::from, CardTypeReg::from(0).with_card_width4_1, CmdArg::new, CmdReg::from, ControlReg::from, RawInterruptStatusReg::from ...(+20)
- 参考项目：`NoAxiom/lib/driver/src/probe/realize.rs#of_realize`，调用摘要：DEV_BUS.add_block_device, LsAhciDevice::new, PlicDevice::new, VF2SdcardDevice::new, log::info!, log::warn! ...(+1)
- 参考项目：`NoAxiom/lib/driver_ahci/src/drv_ahci.rs#ahci_print_info`，调用摘要：log::debug!

#### 网络协议栈
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `user/apps/user_init/src/main.rs#run_netperf_cases` | `NoAxiom/kernel/src/net/mod.rs#net_udp_test` | 同模块上下文对齐/差异观察 | core::ptr::null::<u8>, cstr_eq, kill_pid, push_arg_ptrs, push_arg_ptrs_at, run_busybox ...(+7) | Instant::from_millis, LoopBackDev::new, SocketSet::new, SpinLock::new, client.bind, client.bind(233).unwrap ...(+26) |

#### 文件系统/第三方库
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `crates/fs/src/impls/ext4/file.rs#symlink` | `NoAxiom/kernel/src/fs/vfs/impls/ext4/dentry.rs#symlink` | 同板块同名 | Ok, add_dir_entry, alloc_inode, sb.supports_extents, target.as_bytes, write_file ...(+1) | Ok, assert!, assert_no_lock!, debug!, downcast_inode.get_inode, downcast_inode.get_inode().lock ...(+19) |
| `crates/task/src/execve.rs#check_execve_redirect` | `NoAxiom/kernel/src/fs/vfs/impls/ext4/dentry.rs#create` | 同模块上下文对齐/差异观察 | EXEC_STACK_PREMAP_PAGES.min, LTP_FILE_X_CHILD_PID.store, LTP_FILE_X_PARENT_PID.store, LTP_FILE_X_SHELL_PID.store, PENDING_EXECVE.lock, PENDING_EXECVE.lock().take ...(+67) | Arc::new, Err, Ext4DirInode::new, Ext4FileInode::new, Ok, assert! ...(+42) |
| `crates/fs/src/impls/procfs/procfs.rs#proc_dir_entries` | `NoAxiom/kernel/src/fs/vfs/impls/ext4/file.rs#load_dir` | 同模块上下文对齐/差异观察 | Some | Arc::new, Ext4DirInode::new, Ext4FileInode::new, InodeMode::from_bits, InodeMode::from_bits(mode as u32).unwrap, Ok ...(+23) |
| `crates/fs/src/impls/ext4/adapter.rs#flush_file_pages_locked` | `NoAxiom/kernel/src/fs/vfs/impls/ext4/mod.rs#ext4_rs_test` | 同模块上下文对齐/差异观察 | EXT4_FILE_PAGE_CACHE.lock, Ok, Some, cache.slots.len, inos.len, inos[..ino_count].contains ...(+13) | (InodePerm::S_IREAD | InodePerm::S_IWRITE).bits, Arc::new, DiskCursor::new, Ext4::open, InodeFileType::S_IFREG.bits, block_on ...(+8) |

#### 网络协议栈/第三方库
仅目标项目中观察到该模块代表函数，参考项目没有同模块可对齐证据：
- 目标项目：`crates/syscall/src/imp/net.rs#sys_socketpair`，调用摘要：Some, core::slice::from_raw_parts, current_token, entry0.clone, entry1.clone, fd_alloc ...(+13)
- 目标项目：`crates/syscall/src/imp/net.rs#udp_recv_iov_queued`，调用摘要：bind_lookup, bind_lookup(local_port).is_none, bound_set_read_waiter, cancel_udp_wait, crate::imp::signal::check_itimers, crate::imp::signal::has_pending_current ...(+12)
- 目标项目：`crates/syscall/src/imp/net.rs#pipewrite_socket`，调用摘要：cancel_socket_wait, crate::imp::signal::check_itimers, crate::imp::signal::has_pending_current, fs::pipe::pipe_get, fs::pipe::pipe_set_write_waiter, pipe.available_write ...(+9)
- 目标项目：`crates/syscall/src/imp/net.rs#udp_recv_queued`，调用摘要：bind_lookup, bind_lookup(local_port).is_none, bound_set_read_waiter, cancel_udp_wait, crate::imp::signal::check_itimers, crate::imp::signal::has_pending_current ...(+10)

#### 第三方库
仅目标项目中观察到该模块代表函数，参考项目没有同模块可对齐证据：
- 目标项目：`crates/syscall/src/dispatch.rs#handle_syscall`，调用摘要：imp::fs::sys_bpf, imp::fs::sys_chdir, imp::fs::sys_close, imp::fs::sys_dup, imp::fs::sys_dup3_flags, imp::fs::sys_epoll_create1 ...(+162)
- 目标项目：`crates/task/src/fork.rs#sys_clone`，调用摘要：(child_tid as u32).to_le_bytes, Process::new, Some, Vec::new, alloc_pid, alloc_tid ...(+52)
- 目标项目：`crates/syscall/src/imp/fs.rs#sys_read`，调用摘要：Ok, alloc_io_buffer, block_device_read, buf_ptr.wrapping_add, console_getchar, copy_to_user_iter ...(+42)
- 目标项目：`crates/syscall/src/imp/fs.rs#sys_openat`，调用摘要：Err, chown_new_ramfs_inode, core::str::from_utf8, core::str::from_utf8(&path_buf[..(path_len as usize - 1)]).unwrap_or, create_file, ext4fs.chmod ...(+42)

#### 通用模块/其他
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `user/libc/src/lib.rs#write_str` | `user/libd/src/console.rs#write_str` | 同板块同名 | s.as_bytes, write | Ok, s.as_bytes, write |
| `user/apps/user_init/src/main.rs#init_arch_environment` | `user/libd/src/lib_client.rs#resize_widgets` | 同模块上下文对齐/差异观察 | busybox_path, mkdirat_path, run_busybox, sym_link | Size::new, child_margin_size.x_axis, child_margin_size.y_axis, child_size.x_axis, child_size.y_axis, child_widget.get_margin ...(+23) |
| `user/apps/user_init/src/main.rs#run_cyclictest_cases` | `user/libd/src/lib_client.rs#resize_widget_subtree` | 同模块上下文对齐/差异观察 | kill_pid, run_busybox, run_program, spawn_program, wait4, write_group_end ...(+4) | Size::new, child_margin_size.x_axis, child_margin_size.y_axis, child_size.x_axis, child_size.y_axis, child_widget.get_margin ...(+18) |
| `user/apps/user_init/src/main.rs#run_iperf_cases` | `user/libd/src/lib_client.rs#on_event` | 同模块上下文对齐/差异观察 | core::ptr::null::<u8>, kill_pid, push_arg_ptrs, push_arg_ptrs_at, run_busybox, run_program_ptrs ...(+7) | Some, event.args[0].clone, event.args[0].clone().try_into, event.args[0].clone().try_into().unwrap, exit, handler ...(+13) |


好的，我将遵循审计指令，基于提供的证据材料，进行最终的同源性定性仲裁。

### 1. 宏观判定
- **定性结论**: **独立开发 / 底层架构显著差异**
- **核心依据**: 客观 AST 相似度仅为 0.75%，双方共同 AST 指纹仅 32 个，而各自拥有 1950 和 2349 个唯一指纹。目标项目与参考项目的覆盖率分别低至 1.64% 和 1.36%。在【系统调用】等关键同板块中，共同唯一 AST 指纹为 0。这些指标远低于构成实质性相似的阈值，表明两个项目在代码实现层面具有根本性的不同。

### 2. 全模块覆盖矩阵解读
- **进程管理**
  1. 双方均存在该模块。
  2. 共同唯一 AST 指纹为 0。
  3. 覆盖率均为 0.0%，说明双方在该模块的实现上无任何可识别的结构重合。
  4. 证据强度: **未形成**。目标项目仅有 2 个函数，参考项目有 207 个，体量差异巨大，且无共同指纹。

- **任务调度/异步运行时**
  1. 双方均存在该模块。
  2. 共同唯一 AST 指纹为 0。
  3. 覆盖率均为 0.0%，表明调度与运行时机制在 AST 层面完全不同。
  4. 证据强度: **未形成**。目标项目为用户态脚本调度，参考项目为内核态路径解析与信号处理，领域不同。

- **内存管理**
  1. 双方均存在该模块。
  2. 共同唯一 AST 指纹为 0。
  3. 覆盖率均为 0.0%，说明内存管理策略与实现无结构相似性。
  4. 证据强度: **未形成**。目标项目是用户态环境初始化与进程回收，参考项目是内核 ELF 加载与内存映射，抽象层次完全不同。

- **中断/异常处理**
  1. 仅参考项目体现，目标项目无此模块。
  2. 无共同 AST 指纹。
  3. 覆盖率均为 0.0%。
  4. 证据强度: **未形成**。模块缺失是架构差异的强信号。

- **系统调用**
  1. 双方均存在该模块。
  2. 共同唯一 AST 指纹为 0。
  3. 覆盖率均为 0.0%。尽管存在同名函数（如 `execve`, `chdir`），但未形成任何唯一的、非通用的结构指纹。
  4. 证据强度: **弱**。同名及相似调用图谱源于 POSIX 标准接口和 Rust 语言惯用法，不构成特异性同源证据。

- **文件系统**
  1. 双方均存在该模块。
  2. 共同唯一 AST 指纹为 0。
  3. 覆盖率均为 0.0%，文件系统实现无结构重合。
  4. 证据强度: **未形成**。目标项目是用户态环境变量设置，参考项目是内核 VFS 层的设备节点创建，功能与层级完全不同。

- **设备驱动**
  1. 仅参考项目体现，目标项目无此模块。
  2. 无共同 AST 指纹。
  3. 覆盖率均为 0.0%。
  4. 证据强度: **未形成**。模块缺失是架构差异的强信号。

- **网络协议栈**
  1. 双方均存在该模块。
  2. 共同唯一 AST 指纹为 0。
  3. 覆盖率均为 0.0%，网络协议栈实现无结构重合。
  4. 证据强度: **未形成**。目标项目是用户态网络测试用例执行器，参考项目是内核态 UDP 协议栈测试，功能与层级完全不同。

- **第三方库/公共依赖**
  1. 目标项目大量存在（`文件系统/第三方库`、`网络协议栈/第三方库`、`第三方库`），参考项目极少。
  2. 共同唯一 AST 指纹为 0。
  3. 覆盖率均为 0.0%。
  4. 证据强度: **未形成**。目标项目重度依赖第三方库，而参考项目几乎无此依赖，这是架构设计哲学上的根本差异。

### 3. 底层 AST 关键证据指纹表
| 所属板块 | 目标项目路径 | 参考项目路径 | 证据类型 | 技术说明 |
| :--- | :--- | :--- | :--- | :--- |
| 系统调用 | `user/libc/src/lib.rs#execve` | `user/libd/src/syscall/wrapper.rs#execve` | 同板块同名 | 均为 POSIX `execve` 的标准封装。参数 `path.as_ptr`, `argv.as_ptr`, `envp.as_ptr` 由 ABI 定义，`.as_ptr()` 是 Rust 与 C 接口交互的通用惯用法。底层分发调用不同（`syscall::syscall3` vs `sys_execve`）。 |
| 系统调用 | `user/libc/src/lib.rs#exit` | `user/libd/src/syscall/wrapper.rs#exit` | 同板块同名 | 均为 POSIX `exit` 的标准封装。目标项目包含 `core::hint::spin_loop` 自旋逻辑，参考项目直接调用 `sys_exit`，控制流存在显著差异。 |
| 系统调用 | `user/libc/src/lib.rs#chdir` | `user/libd/src/syscall/wrapper.rs#chdir` | 同板块同名 | 均为 POSIX `chdir` 的标准封装。`path.as_ptr` 是传递路径字符串给系统调用的唯一标准方式。底层分发调用不同（`syscall::syscall1` vs `sys_chdir`）。 |
| 系统调用 | `user/libc/src/syscall.rs#syscall0..6` | `user/libd/src/arch/rv64/syscall.rs#syscall` | 同板块证据 | 均为 RISC-V 架构下使用 `asm!` 宏实现裸系统调用。这是由硬件 ABI 决定的唯一实现方式，属于架构约束，非项目特异性代码。目标项目按参数个数拆分为 7 个函数，参考项目为统一入口，设计策略不同。 |
| 系统调用 | `user/libc/src/lib.rs#linkat` | `user/libd/src/syscall/wrapper.rs#hard_link` | 同板块证据 | `linkat` 是直接系统调用封装，`hard_link` 是包含 `open`, `close` 等复杂逻辑的高层辅助函数。两者功能层级和调用图谱完全不同。`new.as_ptr`/`old.as_ptr` 是路径指针传递的通用写法。 |
| 文件系统/第三方库 | `crates/fs/src/impls/ext4/file.rs#symlink` | `NoAxiom/kernel/src/fs/vfs/impls/ext4/dentry.rs#symlink` | 同板块同名 | 均为 ext4 文件系统符号链接功能的实现。目标项目位于 `crates/fs` 第三方库路径，参考项目位于内核源码树。同名源于实现相同的文件系统功能，但 AST 指纹不同，表明是独立实现。 |

### 4. 核心代码调用流对比剖析
* **【系统调用/execve 路径】**
* **底层技术析理**: 两者均封装了 POSIX `execve` 系统调用，但体现了不同的抽象层次和错误处理策略。目标项目 (`libc`) 是薄封装，直接通过 `syscall::syscall3` 分发，将错误码直接返回。参考项目 (`libd`) 的 `execve` 调用 `sys_execve`，可能在其内部进行了更复杂的架构相关处理或错误码转换。目标项目的 `libc` 更接近传统 C 库的最小化封装，而参考项目的 `libd` 看起来是其自有运行时库的一部分，封装层次更深。两者在参数准备上的相似性完全由 POSIX 标准接口定义。
* **代码溯源链接**:
  - 目标项目: `user/libc/src/lib.rs#execve`
  - 参考项目: `user/libd/src/syscall/wrapper.rs#execve`

* **【系统调用/底层分发路径】**
* **底层技术析理**: 目标项目在 `user/libc/src/syscall.rs` 中定义了 `syscall0` 到 `syscall6` 共 7 个函数，每个函数直接内联 `asm!` 宏，将 Rust 变量绑定到 RISC-V 的 `a0-a6` 寄存器并执行 `ecall`。这是一种直接、扁平的分发策略。参考项目在 `user/libd/src/arch/rv64/syscall.rs` 中定义了一个统一的 `syscall` 函数，同样使用 `asm!`，但可能通过宏或分发逻辑处理不同参数数量的系统调用。两者都使用了 `asm!`，这是 RISC-V 架构下实现系统调用的唯一途径，属于不受保护的“场景原则”范畴。两者在函数拆分策略上的不同，反映了不同的软件工程选择。
* **代码溯源链接**:
  - 目标项目: `user/libc/src/syscall.rs#syscall3`
  - 参考项目: `user/libd/src/arch/rv64/syscall.rs#syscall`

* **【内存管理/进程回收 vs ELF加载】**
* **底层技术析理**: 目标项目的 `reap_adopted_zombies` 函数位于用户态初始化程序，通过调用 `wait4` 系统调用来回收子进程，这是一个典型的用户态 shell/init 进程行为。参考项目的 `map_elf` 函数位于内核内存管理子系统，负责解析 ELF 文件头并将各个段映射到进程地址空间，涉及复杂的页表操作和权限设置。两者虽然同属广义的“内存管理”范畴，但一个在用户态处理进程生命周期，一个在内核态处理程序加载，其操作的对象、使用的原语和所处的抽象层级完全不同，无任何同源性。
* **代码溯源链接**:
  - 目标项目: `user/apps/user_init/src/main.rs#reap_adopted_zombies`
  - 参考项目: `NoAxiom/kernel/src/mm/memory_set.rs#map_elf`

### 5. 单方模块与降权项
- **单方模块**:
  - **中断/异常处理**、**设备驱动**: 仅存在于参考项目。这表明参考项目是一个完整的内核实现，而目标项目可能依赖外部内核或在这些领域尚未实现，是架构完整性上的重大差异。
  - **网络协议栈/第三方库**、**第三方库**: 几乎仅存在于目标项目。目标项目大量使用第三方库（共 2196 个函数），而参考项目极少（49 个）。这揭示了双方在开发哲学上的根本不同：目标项目倾向于复用现有生态，参考项目倾向于从零构建。
- **降权项**:
  - **POSIX 同名函数** (`execve`, `exit`, `chdir` 等): 函数名和参数结构由行业标准强制规定，不能作为抄袭证据。
  - **Rust 语言惯用法** (`.as_ptr()`, `asm!`, `Some`, `Ok`, `Err`): 这些是 Rust 编程中实现特定功能（如 FFI、错误处理）的标准模式，属于通用表达。
  - **跨板块证据**: 控方将用户态初始化函数与内核态核心函数（如 `run_lmbench_cases` vs `__walk_path`）强行配对，因功能、层级完全不同，此类证据无效。
  - **证据重复计数**: 原始证据中 `syscall0` 至 `syscall6` 被重复列出，人为放大了共同指纹数量，需予以纠正。

### 6. 保守结论
综合客观 AST 覆盖率（0.75%）和全模块矩阵分析，目标项目与参考项目在代码实现层面存在根本性、系统性的差异。32 个共同 AST 指纹经分析均为 POSIX 标准接口、Rust 语言惯用法或硬件架构约束等“场景原则”所涵盖的通用表达，不具备项目特异性。双方在【中断/异常处理】、【设备驱动】等核心内核模块的存在与否，以及在【第三方库】依赖上的天壤之别，清晰地指向了两个独立、异构的软件架构。控方基于少数同名函数和通用语法的复用指控，在如此低的相似度背景下无法成立。因此，判定两项目为独立开发，不存在同源性抄袭风险。

------------------------------

