# 🛡️ OS 内核赛自动化代码级架构审计报告

**审计时间**: `2026-07-02 05:02:33`
**参赛作品名称**: `2026-T2026104869910054-XX`
**年份**: `2026`
**学校**: `XX`
**队伍名称**: `T2026104869910054`
**比赛名字**: `OS-Competition`
**比赛子赛道**: `Kernel-Implementation`
**仓库地址**: `https://gitlab.eduxiji.net/educg-group-43501-3132633/T2026104869910054-3130`
**推测开发语言**: `Rust / C (由AST探针自动识别)`
**特征函数量**: `2260`

## ⚔️ 底层代码同源性对比 (AST Homology)
> 本部分由专门的代码审查智能体，对该项目与符合年份条件的历届最高相似度项目进行控制流与底层调用对比。新版报告会先给出确定性的全模块覆盖矩阵，再给出 Agent 的分模块解释，避免只围绕少量抽样函数展开。

### 🏆 Top 5 底层对标: vs `2022-啊队队队-河南科技大学`（河南科技大学 / 啊队队队）

#### 对方作品信息

**对方年份**: `2022`
**对方学校**: `河南科技大学`
**对方队伍名称**: `啊队队队`
**对方比赛名字**: `OS-Competition`
**对方比赛子赛道**: `Kernel-Implementation`
**对方仓库地址**: `https://gitlab.eduxiji.net/alexsondes/oskernel2022-byte-os.git`

> 双向覆盖率：共同 AST 指纹: 19；唯一 AST 指纹: 目标项目 1950，参考项目 759；覆盖率: 目标项目 0.97%，参考项目 2.5%

## 全模块对比总览（确定性证据）

> 总体覆盖率：共同 AST 指纹: 19；唯一 AST 指纹: 目标项目 1950，参考项目 759；覆盖率: 目标项目 0.97%，参考项目 2.5%

### 1. 模块覆盖矩阵
| 模块 | 目标函数数 | 参考函数数 | 共同唯一 AST 指纹 | Target 覆盖 | Base 覆盖 | 状态 |
| :--- | ---: | ---: | ---: | ---: | ---: | :--- |
| 进程管理 | 2 | 41 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |
| 任务调度/异步运行时 | 2 | 122 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |
| 内存管理 | 3 | 190 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |
| 中断/异常处理 | 0 | 15 | 0 | 0.0% | 0.0% | 仅参考项目体现 |
| 系统调用 | 27 | 9 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |
| 文件系统 | 2 | 436 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |
| 设备驱动 | 0 | 44 | 0 | 0.0% | 0.0% | 仅参考项目体现 |
| 网络协议栈 | 1 | 0 | 0 | 0.0% | 0.0% | 仅目标项目体现 |
| 文件系统/第三方库 | 254 | 0 | 0 | 0.0% | 0.0% | 仅目标项目体现 |
| 网络协议栈/第三方库 | 137 | 0 | 0 | 0.0% | 0.0% | 仅目标项目体现 |
| 第三方库 | 1805 | 0 | 0 | 0.0% | 0.0% | 仅目标项目体现 |
| 通用模块/其他 | 27 | 25 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |

### 2. 分模块证据清单

#### 进程管理
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `user/apps/user_init/src/main.rs#wait_exit_code` | `kernel/src/sys_call/task/fork.rs#sys_wait4` | 同模块上下文对齐/差异观察 | Some | Err, Ok, cprocess.exit_code.unwrap, cprocess_vec.len, cprocess_vec[0].borrow, debug! ...(+14) |
| `user/libc/src/lib.rs#fork` | `kernel/src/sys_call/task/fork.rs#sys_clone` | 同模块上下文对齐/差异观察 | clone | Ok, Task::new, add_task_to_scheduler, debug!, drop, inner.process.clone ...(+12) |

#### 任务调度/异步运行时
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `user/apps/user_init/src/main.rs#run_lmbench_cases` | `kernel/src/sys_call/mod.rs#sys_call` | 同模块上下文对齐/差异观察 | cstr_eq, run_busybox, run_program, write_group_end, write_group_start, write_str | Ok, args[0].into, args[1].into, args[2].into, args[4].into, self.get_cwd ...(+60) |
| `user/apps/user_init/src/main.rs#init_etc_files` | `kernel/src/device/sdcard.rs#init` | 同模块上下文对齐/差异观察 | run_sh_command | Err, InitError::CMDFailed, InitError::CardCapacityStatusNotSet, self.CS_HIGH, self.HIGH_SPEED_ENABLE, self.end_cmd ...(+7) |

#### 内存管理
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `user/apps/user_init/src/main.rs#run_script` | `kernel/src/task/mod.rs#exec_with_process` | 同模块上下文对齐/差异观察 | chdir, cstr_eq, reap_adopted_zombies, run_cyclictest_cases, run_iperf_cases, run_libctest_scripts ...(+6) | (ph.virtual_addr() as usize + base).into, BTreeMap::new, INode::open, MemMap::exists_page, Ok, PhysAddr::from ...(+48) |
| `user/apps/user_init/src/main.rs#run_sh_command` | `fatfs/src/fs.rs#format_volume` | 同模块上下文对齐/差异观察 | "-c\0".as_ptr, "busybox\0".as_ptr, "sh\0".as_ptr, busybox_path, command.as_ptr, core::ptr::null::<u8> ...(+6) | DirFileEntryData::new, Err, Ok, SeekFrom::End, SeekFrom::Start, alloc_cluster ...(+27) |
| `user/apps/user_init/src/main.rs#reap_adopted_zombies` | `kernel/src/sys_call/mod.rs#interrupt` | 同模块上下文对齐/差异观察 | wait4 | Err, Ok, args.copy_from_slice, debug!, drop, error! ...(+12) |

#### 中断/异常处理
仅参考项目中观察到该模块代表函数，目标项目没有同模块可对齐证据：
- 参考项目：`kernel/src/interrupt/timer.rs#now`，调用摘要：time::read
- 参考项目：`kernel/src/interrupt/mod.rs#init`，调用摘要：asm!, info!, timer::init
- 参考项目：`kernel/src/interrupt/timer.rs#timer_handler`，调用摘要：NEXT_TICKS.force_get, NEXT_TICKS.force_get().need_change, set_next_timeout
- 参考项目：`kernel/src/interrupt/mod.rs#clone_from`，调用摘要：内部闭环逻辑

#### 系统调用
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `user/libc/src/lib.rs#clone` | `kernel/src/sys_call/net.rs#read_at` | 同模块上下文对齐/差异观察 | syscall::syscall5 | buf.len, queue.pop_front, self.0.borrow_mut |
| `user/libc/src/syscall.rs#syscall6` | `kernel/src/sys_call/net.rs#write_at` | 同模块上下文对齐/差异观察 | asm! | buf.len, queue.push_back, self.0.borrow_mut |
| `user/libc/src/syscall.rs#syscall6` | `kernel/src/sys_call/mod.rs#is_vfork_wait` | 同模块上下文对齐/差异观察 | asm! | VFORK_WAIT_LIST.lock, vfork_wait_list.iter, vfork_wait_list.iter().find, vfork_wait_list.iter().find(|&&x| x == pid).is_some |
| `user/libc/src/syscall.rs#syscall5` | `kernel/src/sys_call/mod.rs#remove_vfork_wait` | 同模块上下文对齐/差异观察 | asm! | VFORK_WAIT_LIST.lock, vfork_wait_list.drain_filter |

#### 文件系统
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `user/apps/user_init/src/main.rs#test_env` | `fatfs/src/boot_sector.rs#format_bpb` | 同模块上下文对齐/差异观察 | ENV_LIBRARY_PATH.as_ptr, ENV_LMBENCH_ENOUGH.as_ptr, ENV_LTP_DEV.as_ptr, ENV_LTP_DEV_FS_TYPE.as_ptr, ENV_PATH.as_ptr, ENV_TERM.as_ptr ...(+1) | Err, FatType::from_clusters, Ok, assert!, bpb.total_clusters, debug_assert! ...(+15) |
| `user/apps/user_init/src/main.rs#init_environment` | `fatfs/src/boot_sector.rs#determine_sectors_per_fat` | 同模块上下文对齐/差异观察 | init_arch_environment, init_etc_files | fat_type.bits_per_fat_entry, u32::from, u64::from |

#### 设备驱动
仅参考项目中观察到该模块代表函数，目标项目没有同模块可对齐证据：
- 参考项目：`kernel/src/device/sdcard.rs#read_sector`，调用摘要：Err, Ok, assert!, chunk.iter_mut, chunk.iter_mut().zip, data_buf.chunks_mut ...(+6)
- 参考项目：`kernel/src/device/sdcard.rs#write_sector`，调用摘要：(data_buf.len() / SEC_LEN).try_into, (data_buf.len() / SEC_LEN).try_into().unwrap, Err, Ok, assert!, chunk.iter ...(+9)
- 参考项目：`kernel/src/device/sdcard.rs#get_cidregister`，调用摘要：Err, Ok, self.end_cmd, self.get_response, self.read_data, self.send_cmd ...(+2)
- 参考项目：`kernel/src/device/sdcard.rs#read_block`，调用摘要：Self::wait_for_one_sec, info!, result.is_err, sd_card.init, sd_card.read_sector, sd_card.read_sector(buf, block_id as u32).is_err ...(+1)

#### 网络协议栈
仅目标项目中观察到该模块代表函数，参考项目没有同模块可对齐证据：
- 目标项目：`user/apps/user_init/src/main.rs#run_netperf_cases`，调用摘要：core::ptr::null::<u8>, cstr_eq, kill_pid, push_arg_ptrs, push_arg_ptrs_at, run_busybox ...(+7)

#### 文件系统/第三方库
仅目标项目中观察到该模块代表函数，参考项目没有同模块可对齐证据：
- 目标项目：`crates/task/src/execve.rs#check_execve_redirect`，调用摘要：EXEC_STACK_PREMAP_PAGES.min, LTP_FILE_X_CHILD_PID.store, LTP_FILE_X_PARENT_PID.store, LTP_FILE_X_SHELL_PID.store, PENDING_EXECVE.lock, PENDING_EXECVE.lock().take ...(+67)
- 目标项目：`crates/fs/src/impls/procfs/procfs.rs#proc_dir_entries`，调用摘要：Some
- 目标项目：`crates/fs/src/impls/ext4/inode.rs#add_dir_entry`，调用摘要：Block::load, Err, Ok, add_data_block_to_inode, alloc_block, blk_sz.min ...(+20)
- 目标项目：`crates/fs/src/impls/ext4/adapter.rs#flush_file_pages_locked`，调用摘要：EXT4_FILE_PAGE_CACHE.lock, Ok, Some, cache.slots.len, inos.len, inos[..ino_count].contains ...(+13)

#### 网络协议栈/第三方库
仅目标项目中观察到该模块代表函数，参考项目没有同模块可对齐证据：
- 目标项目：`crates/syscall/src/imp/net.rs#sys_socketpair`，调用摘要：Some, core::slice::from_raw_parts, current_token, entry0.clone, entry1.clone, fd_alloc ...(+13)
- 目标项目：`crates/syscall/src/imp/net.rs#udp_recv_iov_queued`，调用摘要：bind_lookup, bind_lookup(local_port).is_none, bound_set_read_waiter, cancel_udp_wait, crate::imp::signal::check_itimers, crate::imp::signal::has_pending_current ...(+12)
- 目标项目：`crates/syscall/src/imp/net.rs#pipewrite_socket`，调用摘要：cancel_socket_wait, crate::imp::signal::check_itimers, crate::imp::signal::has_pending_current, fs::pipe::pipe_get, fs::pipe::pipe_set_write_waiter, pipe.available_write ...(+9)
- 目标项目：`crates/syscall/src/imp/net.rs#udp_recv_queued`，调用摘要：bind_lookup, bind_lookup(local_port).is_none, bound_set_read_waiter, cancel_udp_wait, crate::imp::signal::check_itimers, crate::imp::signal::has_pending_current ...(+10)

#### 第三方库
仅目标项目中观察到该模块代表函数，参考项目没有同模块可对齐证据：
- 目标项目：`crates/syscall/src/dispatch.rs#handle_syscall`，调用摘要：imp::fs::sys_bpf, imp::fs::sys_chdir, imp::fs::sys_close, imp::fs::sys_dup, imp::fs::sys_dup3_flags, imp::fs::sys_epoll_create1 ...(+162)
- 目标项目：`crates/task/src/fork.rs#sys_clone`，调用摘要：(child_tid as u32).to_le_bytes, Process::new, Some, Vec::new, alloc_pid, alloc_tid ...(+52)
- 目标项目：`crates/syscall/src/imp/fs.rs#sys_read`，调用摘要：Ok, alloc_io_buffer, block_device_read, buf_ptr.wrapping_add, console_getchar, copy_to_user_iter ...(+42)
- 目标项目：`crates/syscall/src/imp/fs.rs#sys_openat`，调用摘要：Err, chown_new_ramfs_inode, core::str::from_utf8, core::str::from_utf8(&path_buf[..(path_len as usize - 1)]).unwrap_or, create_file, ext4fs.chmod ...(+42)

#### 通用模块/其他
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `user/libc/src/lib.rs#write_str` | `kernel/src/console.rs#write_str` | 同板块同名 | s.as_bytes, write | Ok, c.encode_utf8, c.encode_utf8(&mut buffer).as_bytes, c.encode_utf8(&mut buffer).as_bytes().iter, console_putchar, s.chars |
| `user/apps/user_init/src/main.rs#init_arch_environment` | `kernel/src/sync/mutex.rs#obtain_lock` | 同模块上下文对齐/差异观察 | busybox_path, mkdirat_path, run_busybox, sym_link | self.lock.compare_exchange, self.lock.load, spin_loop |
| `user/apps/user_init/src/main.rs#run_cyclictest_cases` | `kernel/src/main.rs#clear_bss` | 同模块上下文对齐/差异观察 | kill_pid, run_busybox, run_program, spawn_program, wait4, write_group_end ...(+4) | core::slice::from_raw_parts_mut, core::slice::from_raw_parts_mut(bss_start_addr, bss_size).fill, info! |
| `user/apps/user_init/src/main.rs#run_iperf_cases` | `kernel/src/sbi.rs#sbi_call_3` | 同模块上下文对齐/差异观察 | core::ptr::null::<u8>, kill_pid, push_arg_ptrs, push_arg_ptrs_at, run_busybox, run_program_ptrs ...(+7) | asm! |


#### 1. 宏观判定
- **定性结论**: **独立开发，底层架构显著差异**
  - **客观相似度**: 0.71%
  - **共同 AST 指纹**: 19
  - **唯一 AST 指纹**: 目标项目 1950，参考项目 759
  - **覆盖率**: 目标项目 0.97%，参考项目 2.5%
  - **核心判定**: 双方在所有可比模块中的共同唯一 AST 指纹均为 0。19 个共同指纹均为 `asm!`、`Some`、`Ok`、`Err`、`core::ptr::null` 等 Rust 语言级或 ABI 级通用构造，不具备项目间排他性识别能力。目标项目与参考项目在架构定位、核心职责、模块分布上存在根本性差异，无证据表明存在代码复用。

#### 2. 全模块覆盖矩阵解读

| 模块 | 双方存在 | 共同唯一 AST 指纹 | 覆盖率解读 | 证据强度 |
| :--- | :---: | :---: | :--- | :---: |
| **进程管理** | 是 | 0 | 目标 0.0%，参考 0.0%。双方均实现进程创建与回收，但目标为用户态 libc 封装，参考为内核态任务管理，无结构同源。 | 未形成 |
| **任务调度/异步运行时** | 是 | 0 | 目标 0.0%，参考 0.0%。目标为用户态测试调度脚本，参考为内核系统调用分发与设备初始化，功能层级完全不同。 | 未形成 |
| **内存管理** | 是 | 0 | 目标 0.0%，参考 0.0%。目标为用户态 `execve` 参数构造，参考为内核 ELF 加载与 FAT 卷格式化，内存模型与操作对象无交集。 | 未形成 |
| **中断/异常处理** | 仅参考 | 0 | 目标无此模块。参考拥有独立的中断处理、定时器模块，目标项目无对应实现。 | 未形成 |
| **系统调用** | 是 | 0 | 目标 0.0%，参考 0.0%。目标为用户态 `asm!` 陷入封装，参考为内核网络 socket 与 vfork 管理，调用对象无重叠。 | 未形成 |
| **文件系统** | 是 | 0 | 目标 0.0%，参考 0.0%。目标为用户态环境变量设置，参考为 FAT 引导扇区格式化，文件系统类型与操作层级均不同。 | 未形成 |
| **设备驱动** | 仅参考 | 0 | 目标无此模块。参考拥有独立的 SD 卡驱动实现，目标项目无对应代码。 | 未形成 |
| **网络协议栈** | 仅目标 | 0 | 参考无此模块。目标拥有独立的网络性能测试及第三方网络协议栈实现。 | 未形成 |
| **第三方库/公共依赖** | 仅目标 | 0 | 目标拥有 2196 个函数分布于文件系统/第三方库、网络协议栈/第三方库、第三方库模块，参考无对应实现。 | 未形成 |
| **通用模块/其他** | 是 | 0 | 目标 0.0%，参考 0.0%。同名 `write_str` 实现路径完全不同（用户态 write 系统调用 vs 内核 SBI 字符输出），其余配对均为跨子系统硬凑。 | 未形成 |

#### 3. 底层 AST 关键证据指纹表
| 所属板块 | 目标项目路径 | 参考项目路径 | 证据类型 | 技术说明 |
| :--- | :--- | :--- | :--- | :--- |
| 通用模块/其他 | `user/libc/src/lib.rs#write_str` | `kernel/src/console.rs#write_str` | 同名函数，实现异构 | 同名但实现完全不同。目标：`s.as_bytes` → `write` 系统调用；参考：`c.encode_utf8` → `console_putchar` SBI 调用。无共同调用对象。 |
| 系统调用 | `user/libc/src/syscall.rs#syscall3-6` | `kernel/src/sbi.rs#sbi_call_3` | 跨板块 `asm!` 使用 | 目标为用户态 `ecall` 陷入，参考为内核态 SBI 调用。`asm!` 是 RISC-V 底层编程唯一手段，属架构共性。 |
| 进程管理 | `user/libc/src/lib.rs#fork` | `kernel/src/sys_call/task/fork.rs#sys_clone` | 接口两侧标准分层 | 目标 `fork` → `clone` → `syscall5`；参考 `sys_clone` → `Task::new` → `add_task_to_scheduler`。调用对象无重叠。 |
| 内存管理 | `user/apps/user_init/src/main.rs#run_sh_command` | `kernel/src/task/mod.rs#exec_with_process` | 跨层级功能类比 | 目标构造 `execve` 参数阵列；参考处理 ELF 内存映射。无共同 AST 节点。 |
| 第三方库 | `crates/syscall/src/dispatch.rs#handle_syscall` | — | 单方存在 | 目标拥有 162+ 系统调用分发实现，参考无对应模块。 |
| 第三方库 | `crates/fs/src/impls/ext4/inode.rs#add_dir_entry` | — | 单方存在 | 目标拥有独立 ext4 实现，参考使用 FAT 文件系统。 |
| 设备驱动 | — | `kernel/src/device/sdcard.rs#read_sector` | 单方存在 | 参考拥有独立 SD 卡驱动，目标无此模块。 |
| 中断/异常处理 | — | `kernel/src/interrupt/timer.rs#timer_handler` | 单方存在 | 参考拥有独立时钟中断处理，目标无此模块。 |

#### 4. 核心代码调用流对比剖析

* **【进程管理】`fork` / `sys_clone` 调用流**
* **底层技术析理**: 目标项目 `fork` 位于用户态 libc，调用 `clone` 后经 `syscall5` 通过 `asm!` 触发 `ecall` 陷入内核，这是 POSIX 标准进程创建的用户态封装范式。参考项目 `sys_clone` 位于内核态，直接操作 `Task::new` 创建任务控制块、`add_task_to_scheduler` 加入调度队列、`borrow_mut` 修改内部状态。两者是系统调用接口两侧的正常分层实现，调用对象从“系统调用号/寄存器约定”变为“任务控制块/调度器”，无任何 AST 节点重叠。
* **代码溯源链接**:
  - 目标项目: `user/libc/src/lib.rs#fork`
  - 参考项目: `kernel/src/sys_call/task/fork.rs#sys_clone`

* **【内存管理】`run_sh_command` / `exec_with_process` 调用流**
* **底层技术析理**: 目标项目 `run_sh_command` 构造 `["busybox", "sh", "-c", command, NULL]` 裸指针数组后调用 `execve`，这是嵌入式 Linux 用户态执行 shell 命令的标准模式。参考项目 `exec_with_process` 处理 ELF 程序头解析（`VirtAddr::from(ph.virtual_addr() as usize + base).into`）、内存页存在性检查（`MemMap::exists_page`）、文件节点打开（`INode::open`）。两者分别处于用户态参数准备与内核态内存映射加载的不同层级，调用对象无任何重叠。
* **代码溯源链接**:
  - 目标项目: `user/apps/user_init/src/main.rs#run_sh_command`
  - 参考项目: `kernel/src/task/mod.rs#exec_with_process`

* **【系统调用】`syscall5` / `sys_sigreturn` 调用流**
* **底层技术析理**: 目标项目 `syscall5` 使用 `asm!` 宏执行 RISC-V `ecall` 指令，传递 5 个参数的系统调用号与参数，这是用户态发起系统调用的唯一标准方式。参考项目 `sys_sigreturn` 是内核信号处理函数，调用 `debug!` 宏进行日志输出。两者一个是用户态陷入入口，一个是内核态信号返回，功能、层级、调用对象均无关联。
* **代码溯源链接**:
  - 目标项目: `user/libc/src/syscall.rs#syscall5`
  - 参考项目: `kernel/src/sys_call/signal.rs#sys_sigreturn`

* **【通用模块/其他】`write_str` 同名函数调用流**
* **底层技术析理**: 目标项目 `write_str` 将字符串转为字节切片后调用 `write` 系统调用输出，属于用户态 libc 标准输出封装。参考项目 `write_str` 逐字符进行 UTF-8 编码后调用 `console_putchar` 通过 SBI 直接输出到串口，属于内核态早期控制台输出原语。两者实现路径、底层依赖、调用对象完全不同，仅函数名相同。
* **代码溯源链接**:
  - 目标项目: `user/libc/src/lib.rs#write_str`
  - 参考项目: `kernel/src/console.rs#write_str`

#### 5. 单方模块与降权项

* **仅目标项目存在的模块**:
  - **网络协议栈**: 包含 `run_netperf_cases` 等网络性能测试函数，以及 `crates/syscall/src/imp/net.rs` 下的 socket、UDP 收发等独立实现。
  - **第三方库/公共依赖**: 包含 2196 个函数，涵盖 ext4 文件系统（`crates/fs/src/impls/ext4/`）、系统调用分发（`crates/syscall/src/dispatch.rs`）、任务管理（`crates/task/src/fork.rs`）等大量独立 crate。
  - 这些模块在参考项目中无任何对应实现，属于目标项目的独立领域扩展，不能作为同源证据。

* **仅参考项目存在的模块**:
  - **中断/异常处理**: 包含 `kernel/src/interrupt/timer.rs#timer_handler`、`kernel/src/interrupt/mod.rs#init` 等独立中断处理逻辑。
  - **设备驱动**: 包含 `kernel/src/device/sdcard.rs` 下完整的 SD 卡初始化、读写扇区、获取 CID 寄存器等驱动实现。
  - 这些模块在目标项目中无任何对应实现，说明双方在硬件抽象层存在根本性架构差异。

* **降权项说明**:
  - 跨板块 `asm!` 使用（目标系统调用 vs 参考通用模块 SBI）：`asm!` 是 Rust 进行底层硬件操作的唯一语法途径，任何 RISC-V 内核或用户态程序均须使用，属于架构级共性，不构成同源证据。
  - 同名函数 `write_str`：函数名属于通用词汇，且双方实现路径完全不同，不构成同源证据。
  - 通用构造 `Some`、`Ok`、`Err`、`core::ptr::null`、`as_ptr`、`buf.len`：均为 Rust 标准库或 C ABI 兼容的必备构造，不具备项目间排他性。

#### 6. 保守结论
本次审计基于 AST 指纹比对与调用图谱分析，目标项目与参考项目的客观相似度仅为 0.71%，所有可比模块的共同唯一 AST 指纹均为 0。19 个共同指纹均为 Rust 系统编程中不可规避的语言级或 ABI 级通用构造。双方在架构定位上存在根本性差异：目标项目以用户态 init/libc 及大量第三方 crate（ext4、网络协议栈）为主，参考项目以内核任务调度、FAT 文件系统、SD 卡驱动为主。控方指控的“用户态-内核态咬合”实质是将 POSIX 标准分层设计与 OS 通用功能进行概念类比，而非 AST 结构同构。综合客观数据与代码溯源分析，目标项目属于独立开发，未发现代码复用或抄袭证据。

------------------------------

