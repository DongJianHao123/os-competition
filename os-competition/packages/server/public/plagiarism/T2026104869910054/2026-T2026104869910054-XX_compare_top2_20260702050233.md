# 🛡️ OS 内核赛自动化代码级架构审计报告

**审计时间**: `2026-07-02 05:02:33`
**参赛作品名称**: `2026-T2026104869910054-XX`
**年份**: `2026`
**学校**: `XX`
**队伍名称**: `T2026104869910054`
**比赛名字**: `OS-Competition`
**比赛子赛道**: `Kernel-Implementation`
**仓库地址**: `https://gitlab.eduxiji.net/educg-group-43501-3132633/T2026104869910054-3130`
**推测开发语言**: `Rust / C (由AST探针自动识别)`
**特征函数量**: `2260`

## ⚔️ 底层代码同源性对比 (AST Homology)
> 本部分由专门的代码审查智能体，对该项目与符合年份条件的历届最高相似度项目进行控制流与底层调用对比。新版报告会先给出确定性的全模块覆盖矩阵，再给出 Agent 的分模块解释，避免只围绕少量抽样函数展开。

### 🏆 Top 2 底层对标: vs `2024-萌新-南开大学`（南开大学 / 萌新）

#### 对方作品信息

**对方年份**: `2024`
**对方学校**: `南开大学`
**对方队伍名称**: `萌新`
**对方比赛名字**: `OS-Competition`
**对方比赛子赛道**: `Kernel-Implementation`
**对方仓库地址**: `https://gitlab.eduxiji.net/educg-group-26010-2376550/T202410055992606-2876`

> 双向覆盖率：共同 AST 指纹: 33；唯一 AST 指纹: 目标项目 1950，参考项目 2483；覆盖率: 目标项目 1.69%，参考项目 1.33%

## 全模块对比总览（确定性证据）

> 总体覆盖率：共同 AST 指纹: 33；唯一 AST 指纹: 目标项目 1950，参考项目 2483；覆盖率: 目标项目 1.69%，参考项目 1.33%

### 1. 模块覆盖矩阵
| 模块 | 目标函数数 | 参考函数数 | 共同唯一 AST 指纹 | Target 覆盖 | Base 覆盖 | 状态 |
| :--- | ---: | ---: | ---: | ---: | ---: | :--- |
| 进程管理 | 2 | 39 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |
| 任务调度/异步运行时 | 2 | 272 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |
| 内存管理 | 3 | 174 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |
| 中断/异常处理 | 0 | 29 | 0 | 0.0% | 0.0% | 仅参考项目体现 |
| 系统调用 | 27 | 67 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |
| 文件系统 | 2 | 285 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |
| 设备驱动 | 0 | 42 | 0 | 0.0% | 0.0% | 仅参考项目体现 |
| 网络协议栈 | 1 | 0 | 0 | 0.0% | 0.0% | 仅目标项目体现 |
| 文件系统/第三方库 | 254 | 730 | 4 | 1.72% | 0.62% | 双方存在且有结构重合 |
| 网络协议栈/第三方库 | 137 | 118 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |
| 第三方库 | 1805 | 1374 | 17 | 1.1% | 1.86% | 双方存在且有结构重合 |
| 通用模块/其他 | 27 | 106 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |

### 2. 分模块证据清单

#### 进程管理
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `user/apps/user_init/src/main.rs#wait_exit_code` | `api/linux_syscall_api/src/syscall_task/imp/task.rs#syscall_exec` | 同模块上下文对齐/差异观察 | Some | Err, Ok, Some, Vec::new, args_vec.len, args_vec.push ...(+20) |
| `user/libc/src/lib.rs#fork` | `modules/axfs/src/mounts.rs#procfs` | 同模块上下文对齐/差异观察 | clone | Arc::new, Ok, file_over.write_at, file_somaxconn.write_at, fs::ramfs::RamFileSystem::new, proc_root.clone ...(+3) |

#### 任务调度/异步运行时
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `user/apps/user_init/src/main.rs#run_lmbench_cases` | `api/linux_syscall_api/src/syscall_fs/ctype/mount.rs#get_stat_in_fs` | 同模块上下文对齐/差异观察 | cstr_eq, run_busybox, run_program, write_group_end, write_group_start, write_str | 0.into, Err, Kstat::default, Ok, axfs::api::metadata, axfs::api::metadata(path.path()).unwrap ...(+34) |
| `user/apps/user_init/src/main.rs#init_etc_files` | `modules/axtask/src/api.rs#load_app` | 同模块上下文对齐/差异观察 | run_sh_command | "busybox".to_string, Err, Ok, Some, VirtAddr::from, [0_u8].repeat ...(+35) |

#### 内存管理
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `user/apps/user_init/src/main.rs#run_script` | `modules/axtask/src/run_queue.rs#switch_to` | 同模块上下文对齐/差异观察 | chdir, cstr_eq, reap_adopted_zombies, run_cyclictest_cases, run_iperf_cases, run_libctest_scripts ...(+6) | CurrentTask::set_current, asm!, assert!, axhal::arch::write_page_table_root0, axhal::time::current_time_nanos, axlog::info! ...(+23) |
| `user/apps/user_init/src/main.rs#run_sh_command` | `modules/axtask/src/task.rs#clone_task` | 同模块上下文对齐/差异观察 | "-c\0".as_ptr, "busybox\0".as_ptr, "sh\0".as_ptr, busybox_path, command.as_ptr, core::ptr::null::<u8> ...(+6) | (axconfig::SIGNAL_TRAMPOLINE).into, (start_signal_trampoline as usize).into, Arc::clone, Arc::new, AxTask::new, Err ...(+73) |
| `user/apps/user_init/src/main.rs#reap_adopted_zombies` | `api/linux_syscall_api/src/syscall_net/socket.rs#poll` | 同模块上下文对齐/差异观察 | wait4 | Poll::Ready, Some, UDPFuture::new, addr.is_null, addr_buf.is_null, addr_len.is_null ...(+34) |

#### 中断/异常处理
仅参考项目中观察到该模块代表函数，目标项目没有同模块可对齐证据：
- 参考项目：`modules/axhal/src/platform/VisionFive2/irq.rs#register_handler`，调用摘要：with_cause!
- 参考项目：`modules/axhal/src/platform/riscv64_qemu_virt/irq.rs#register_handler`，调用摘要：with_cause!
- 参考项目：`modules/axtrap/src/arch/riscv/mod.rs#riscv_kernel_trap_handler`，调用摘要：handle_irq, panic!, scause.bits, scause.cause, scause::read
- 参考项目：`modules/axhal/src/irq.rs#register_handler_common`，调用摘要：IRQ_HANDLER_TABLE.register_handler, axlog::ax_println!, set_enable, warn!

#### 系统调用
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `user/libc/src/lib.rs#clone` | `api/linux_syscall_api/src/syscall_net/socket.rs#get` | 同模块上下文对齐/差异观察 | syscall::syscall5 | copy_nonoverlapping, drop, keep_alive.to_ne_bytes, panic!, s.keep_alive, s.keep_alive().is_some ...(+10) |
| `user/libc/src/syscall.rs#syscall6` | `api/linux_syscall_api/src/syscall_net/socket.rs#set` | 同模块上下文对齐/差异观察 | asm! | <[u8; 4]>::try_from, <[u8; 4]>::try_from(&opt[0..4]).unwrap, Err, Ok, Some, axnet::Duration::from_secs ...(+14) |
| `user/libc/src/syscall.rs#syscall6` | `api/linux_syscall_api/src/syscall_mem/imp.rs#syscall_shmget` | 同模块上下文对齐/差异观察 | asm! | Err, MemorySet::add_shared_mem, MemorySet::create_shared_mem, Ok, ShmFlags::from_bits, axmem::KEY_TO_SHMID.lock ...(+8) |
| `user/libc/src/syscall.rs#syscall5` | `api/linux_syscall_api/src/syscall_net/socket.rs#get` | 同模块上下文对齐/差异观察 | asm! | bytes.as_bytes, bytes.as_ptr, bytes.len, copy_nonoverlapping, panic!, raw_socket.get_congestion ...(+4) |

#### 文件系统
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `user/apps/user_init/src/main.rs#test_env` | `api/linux_syscall_api/src/syscall_fs/mod.rs#fs_syscall` | 同模块上下文对齐/差异观察 | ENV_LIBRARY_PATH.as_ptr, ENV_LMBENCH_ENOUGH.as_ptr, ENV_LTP_DEV.as_ptr, ENV_LTP_DEV_FS_TYPE.as_ptr, ENV_PATH.as_ptr, ENV_TERM.as_ptr ...(+1) | Ok, panic!, sys_linkat, syscall_chdir, syscall_close, syscall_dup ...(+31) |
| `user/apps/user_init/src/main.rs#init_environment` | `ulib/src/file.rs#fs_init` | 同模块上下文对齐/差异观察 | init_arch_environment, init_etc_files | "/bin/".to_string, "/lib/".to_string, "/usr/bin/".to_string, "/usr/sbin/".to_string, ("/lib/".to_string() + libc_so).as_str, ("/lib/".to_string() + libc_so2).as_str ...(+17) |

#### 设备驱动
仅参考项目中观察到该模块代表函数，目标项目没有同模块可对齐证据：
- 参考项目：`modules/axdriver/src/lib.rs#init_drivers`，调用摘要：AllDevices::default, all_devs.block.iter, all_devs.block.iter().enumerate, all_devs.display.iter, all_devs.display.iter().enumerate, all_devs.net.iter ...(+5)
- 参考项目：`modules/axdriver/src/virtio.rs#probe_pci`，调用摘要：D::try_new, Some, driver_virtio::probe_pci_device::<VirtIoHalImpl>, warn!
- 参考项目：`modules/axdriver/src/virtio.rs#probe_mmio`，调用摘要：D::try_new, Some, base_vaddr.as_mut_ptr, driver_virtio::probe_mmio_device, mmio_base.into, phys_to_virt ...(+1)
- 参考项目：`modules/axdriver/src/bus/mmio.rs#probe_bus_devices`，调用摘要：for_each_drivers!

#### 网络协议栈
仅目标项目中观察到该模块代表函数，参考项目没有同模块可对齐证据：
- 目标项目：`user/apps/user_init/src/main.rs#run_netperf_cases`，调用摘要：core::ptr::null::<u8>, cstr_eq, kill_pid, push_arg_ptrs, push_arg_ptrs_at, run_busybox ...(+7)

#### 文件系统/第三方库
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `crates/fs/src/impls/procfs/procfs.rs#read_inode` | `crates/another_ext4/src/ext4/rw.rs#read_inode` | 同板块同名 | Err, Ok, ProcFs::make_dir_inode, ProcFs::make_file_inode, build_cpuinfo_content, build_cpuinfo_content().len ...(+9) | InodeRef::new, block.read_offset_as, self.inode_disk_pos, self.read_block |
| `crates/fs/src/impls/ext4/inode.rs#alloc_block` | `crates/another_ext4/src/ext4/alloc.rs#alloc_block` | 同板块同名 | Err, Ok, SB_BUF.as_mut_ptr, Some, bg.free_blocks_count.saturating_sub, bg_desc_offset ...(+14) | Bitmap::new, Ok, bg.desc
            .set_free_blocks_count, bg.desc.block_bitmap_block, bg.desc.get_free_blocks_count, bg.desc.set_block_bitmap_csum ...(+14) |
| `crates/fs/src/impls/ext4/inode.rs#alloc_inode` | `crates/another_ext4/src/ext4/alloc.rs#alloc_inode` | 同板块同名 | Err, Ok, SB_BUF.as_mut_ptr, Some, bg.free_inodes_count.saturating_sub, bg_desc_offset ...(+9) | Bitmap::new, Ok, bg.desc
                .set_free_inodes_count, bg.desc.free_inodes_count, bg.desc.inode_bitmap_block, bg.desc.itable_unused ...(+21) |
| `crates/fs/src/impls/ramfs/super_block.rs#link_inode` | `crates/another_ext4/src/ext4/link.rs#link_inode` | 同板块同名 | Err, Ok, RAMFS_STATE.lock, Vec::new, is_dir_mode, name_buf[..new_name.len()].copy_from_slice ...(+12) | Ok, child.inode.is_dir, child.inode.link_count, child.inode.set_link_count, parent.inode.link_count, parent.inode.set_link_count ...(+2) |

#### 网络协议栈/第三方库
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `crates/net/src/udp.rs#recv_from` | `modules/axnet/src/smoltcp_impl/udp.rs#recv_from` | 同板块同名 | Err, Ok, SocketAddr::V4, buf.len, buf[..len].copy_from_slice, self.rx_len.min ...(+1) | Ok, ax_err!, into_core_sockaddr, self.recv_impl, socket.recv_slice |
| `crates/net/src/tcp.rs#accept` | `modules/axnet/src/smoltcp_impl/listen_table.rs#accept` | 同板块同名 | Err, Ok, TcpSocket::new | Ok, ax_err!, get_addr_tuple, is_connected, is_connected(handle).then, self.tcp[port as usize].lock ...(+8) |
| `crates/net/src/raw.rs#recv` | `modules/axnet/src/smoltcp_impl/tcp.rs#recv` | 同板块同名 | Ok, SocketAddr::V4, buf.len, buf[..len].copy_from_slice, self.rx_len.min | Err, Ok, SOCKET_SET.with_socket_mut::<tcp::Socket, _, _>, ax_err!, ax_err_type!, self.block_on ...(+10) |
| `crates/net/src/udp.rs#send_to` | `modules/axnet/src/smoltcp_impl/udp.rs#send_to` | 同板块同名 | Err, Ok, buf.len | ax_err!, from_core_sockaddr, remote_addr.ip, remote_addr.ip().is_unspecified, remote_addr.port, self.send_impl |

#### 第三方库
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `crates/fs/src/impls/procfs/procfs.rs#lookup` | `crates/axfs_devfs/src/null.rs#lookup` | 同板块同名 | Err, is_pid_dir_path, is_pid_stat_path, parse_pid_dir_path, parse_pid_dir_path(normalized).ok_or, parse_pid_stat_path ...(+6) | VfsResult::Err |
| `crates/fs/src/vfs/mount.rs#lookup` | `crates/axfs_devfs/src/random.rs#lookup` | 同板块同名 | DEVFS_MOUNT.as_ref, EXT4_MOUNTS[idx].as_ref, Err, FsKind::Ext4, Ok, PROCFS_MOUNT.as_ref ...(+6) | VfsResult::Err |
| `crates/hal/src/la64/memory.rs#map` | `crates/page_table/src/bits64.rs#map` | 同板块同名 | Ok, Self::map_flags_to_pte, mm_alloc_page, mm_zero_page | Err, GenericPTE::new_page, Ok, entry.is_unused, page_size.is_huge, self.get_entry_mut_or_create ...(+1) |
| `crates/fs/src/impls/devfs/devfs.rs#lookup` | `crates/axfs_devfs/src/zero.rs#lookup` | 同板块同名 | Err, Ok, crate::init::scratch_block_device, crate::init::scratch_block_device(0).is_some, devfs_block_inode, devfs_char_inode ...(+2) | VfsResult::Err |

#### 通用模块/其他
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `user/apps/user_init/src/main.rs#panic` | `modules/axruntime/src/lang_items.rs#panic` | 同板块同名 | exit, write_str | axbacktrace::backtrace, axhal::misc::terminate, error! |
| `user/libc/src/lib.rs#write_str` | `modules/axlog/src/lib.rs#write_str` | 同板块同名 | s.as_bytes, write | Ok, cfg_if::cfg_if! |
| `user/apps/user_init/src/main.rs#init_arch_environment` | `modules/axhal/src/mem.rs#kernel_image_regions` | 同模块上下文对齐/差异观察 | busybox_path, mkdirat_path, run_busybox, sym_link | (_sbss as usize).into, (_sdata as usize).into, (_srodata as usize).into, (_stext as usize).into, (boot_stack as usize).into, [
        MemRegion {
            paddr: virt_to_phys((_stext as usize).into()),
            size: _etext as usize - _stext as usize,
            flags: MemRegionFlags::RESERVED | MemRegionFlags::READ | MemRegionFlags::EXECUTE,
            name: ".text",
        },
        MemRegion {
            paddr: virt_to_phys((_srodata as usize).into()),
            size: _erodata as usize - _srodata as usize,
            flags: MemRegionFlags::RESERVED | MemRegionFlags::READ,
            name: ".rodata",
        },
        MemRegion {
            paddr: virt_to_phys((_sdata as usize).into()),
            size: _edata as usize - _sdata as usize,
            flags: MemRegionFlags::RESERVED | MemRegionFlags::READ | MemRegionFlags::WRITE,
            name: ".data .tdata .tbss .percpu",
        },
        MemRegion {
            paddr: virt_to_phys((boot_stack as usize).into()),
            size: boot_stack_top as usize - boot_stack as usize,
            flags: MemRegionFlags::RESERVED | MemRegionFlags::READ | MemRegionFlags::WRITE,
            name: "boot stack",
        },
        MemRegion {
            paddr: virt_to_phys((_sbss as usize).into()),
            size: _ebss as usize - _sbss as usize,
            flags: MemRegionFlags::RESERVED | MemRegionFlags::READ | MemRegionFlags::WRITE,
            name: ".bss",
        },
    ]
    .into_iter ...(+1) |
| `user/apps/user_init/src/main.rs#run_cyclictest_cases` | `modules/axmem/src/lib.rs#find_free_area` | 同模块上下文对齐/差异观察 | kill_pid, run_busybox, run_program, spawn_program, wait4, write_group_end ...(+4) | Some, axconfig::USER_MEMORY_START.into, hint.max, hint.max(axconfig::USER_MEMORY_START.into()).as_usize, last_end.into, mem.size ...(+8) |


### 审计裁定：最终同源性定性仲裁

#### 1. 宏观判定
- **定性结论**: **独立开发** (底层架构显著差异)
- **核心依据**: 客观 AST 相似度仅为 **0.75%**。在总计 4483 个唯一 AST 指纹中，双方仅共享 33 个指纹，且均集中于第三方库/公共依赖。在操作系统核心模块（进程管理、内存管理、系统调用等），共同指纹数量为 **0**，覆盖率均为 **0.0%**。这表明双方在核心逻辑的实现上不存在可观测的代码结构复用。

#### 2. 全模块覆盖矩阵解读

| 模块 | 双方存在 | 共同指纹 | 覆盖率解读 | 证据强度 |
| :--- | :---: | :---: | :--- | :--- |
| **进程管理** | 是 | 0 | 目标 0.0% / 参考 0.0%。双方虽均有进程相关函数，但 AST 结构无任何重合。 | **未形成** |
| **任务调度/异步运行时** | 是 | 0 | 目标 0.0% / 参考 0.0%。目标侧重用户态测试编排，参考侧重内核态调度与加载，无结构重合。 | **未形成** |
| **内存管理** | 是 | 0 | 目标 0.0% / 参考 0.0%。目标为用户态环境初始化与脚本执行，参考为内核态页表与任务克隆，无结构重合。 | **未形成** |
| **中断/异常处理** | 仅参考 | 0 | 目标项目无此模块，无法比对。 | **未形成** |
| **系统调用** | 是 | 0 | 目标 0.0% / 参考 0.0%。目标为 `asm!` 宏包装的用户态调用入口，参考为内核态具体实现，无结构重合。 | **未形成** |
| **文件系统** | 是 | 0 | 目标 0.0% / 参考 0.0%。目标为环境变量注入，参考为内核挂载点初始化，无结构重合。 | **未形成** |
| **设备驱动** | 仅参考 | 0 | 目标项目无此模块，无法比对。 | **未形成** |
| **网络协议栈** | 仅目标 | 0 | 参考项目无此模块，无法比对。 | **未形成** |
| **文件系统/第三方库** | 是 | 4 | 目标 1.72% / 参考 0.62%。重合指纹为 `read_inode`、`alloc_block` 等标准文件系统算法名，属公共知识。 | **弱** |
| **网络协议栈/第三方库** | 是 | 0 | 目标 0.0% / 参考 0.0%。虽存在 `recv_from`、`accept` 等同名函数，但均为 BSD Socket 标准接口名，AST 结构无重合。 | **未形成** |
| **第三方库** | 是 | 17 | 目标 1.1% / 参考 1.86%。重合指纹为 `lookup`、`map` 等通用函数名，可能源于公共 crate 或标准算法。 | **弱** |
| **通用模块/其他** | 是 | 0 | 目标 0.0% / 参考 0.0%。虽存在 `panic`、`write_str` 等同名函数，但均为 Rust 语言标准接口实现，AST 结构无重合。 | **未形成** |

#### 3. 底层 AST 关键证据指纹表
> 所有共同指纹均位于第三方库/公共依赖，无核心模块强证据。

| 所属板块 | 目标项目路径 | 参考项目路径 | 证据类型 | 技术说明 |
| :--- | :--- | :--- | :--- | :--- |
| **第三方库/公共依赖** | `crates/fs/src/impls/procfs/procfs.rs#read_inode` | `crates/another_ext4/src/ext4/rw.rs#read_inode` | 同名函数 | 均为读取 inode 的标准文件系统操作，函数名是公开的领域术语。 |
| **第三方库/公共依赖** | `crates/fs/src/impls/ext4/inode.rs#alloc_block` | `crates/another_ext4/src/ext4/alloc.rs#alloc_block` | 同名函数 | 均为 Ext4 文件系统的 block 分配函数，算法源于公开标准。 |
| **第三方库/公共依赖** | `crates/fs/src/impls/ext4/inode.rs#alloc_inode` | `crates/another_ext4/src/ext4/alloc.rs#alloc_inode` | 同名函数 | 均为 Ext4 文件系统的 inode 分配函数，算法源于公开标准。 |
| **第三方库/公共依赖** | `crates/fs/src/impls/ramfs/super_block.rs#link_inode` | `crates/another_ext4/src/ext4/link.rs#link_inode` | 同名函数 | 均为文件系统 inode 链接操作，是 VFS 层的通用概念。 |
| **第三方库/公共依赖** | `crates/net/src/udp.rs#recv_from` | `modules/axnet/src/smoltcp_impl/udp.rs#recv_from` | 同名函数 | 均为 UDP 接收操作，函数签名源于 BSD Socket 标准 API。 |
| **第三方库/公共依赖** | `crates/net/src/tcp.rs#accept` | `modules/axnet/src/smoltcp_impl/listen_table.rs#accept` | 同名函数 | 均为 TCP 接受连接操作，函数签名源于 BSD Socket 标准 API。 |
| **第三方库/公共依赖** | `crates/net/src/raw.rs#recv` | `modules/axnet/src/smoltcp_impl/tcp.rs#recv` | 同名函数 | 均为 socket 接收操作，是 BSD Socket API 的标准组成部分。 |
| **第三方库/公共依赖** | `crates/net/src/udp.rs#send_to` | `modules/axnet/src/smoltcp_impl/udp.rs#send_to` | 同名函数 | 均为 UDP 发送操作，函数签名源于 BSD Socket 标准 API。 |
| **第三方库/公共依赖** | `crates/fs/src/impls/procfs/procfs.rs#lookup` | `crates/axfs_devfs/src/null.rs#lookup` | 同名函数 | 均为文件系统 `lookup` 操作，是 VFS 层的标准接口。 |
| **第三方库/公共依赖** | `crates/fs/src/vfs/mount.rs#lookup` | `crates/axfs_devfs/src/random.rs#lookup` | 同名函数 | 同上，均为 VFS 标准 `lookup` 接口的不同实现。 |
| **第三方库/公共依赖** | `crates/hal/src/la64/memory.rs#map` | `crates/page_table/src/bits64.rs#map` | 同名函数 | 均为内存映射函数，是 MMU 编程的通用操作。 |
| **第三方库/公共依赖** | `crates/fs/src/impls/devfs/devfs.rs#lookup` | `crates/axfs_devfs/src/zero.rs#lookup` | 同名函数 | 均为设备文件系统的 `lookup` 操作，是 VFS 标准接口。 |

#### 4. 核心代码调用流对比剖析

* **【系统调用】**
* **底层技术析理**: 目标项目与参考项目在系统调用路径上存在根本性的架构差异，体现了用户态包装与内核态实现的明确分界。
  * **目标项目**：采用典型的用户态 libc 模式。其调用流为 `用户函数 (如 fork)` -> `syscallN 包装函数` -> `asm! 宏 (内联汇编)`。核心是通过 `asm!` 宏直接执行 `syscall` 指令，将参数传递给内核。其操作对象是 C 风格的裸指针 (`path.as_ptr`, `buf.as_ptr`) 和系统调用号。
  * **参考项目**：采用内核态系统调用实现模式。其调用流为 `syscall_* 分发函数` -> `具体实现函数 (如 syscall_exec)` -> `内核数据结构操作 (如 args_vec.push, argv.add)`。核心是解析用户态传入的参数，并操作内核对象（如 `MemorySet`, `AxTask`）。其操作对象是 Rust 容器 (`Vec`, `String`) 和内核同步原语 (`Mutex`, `.lock()`)。
  * **结论**：两者处于操作系统的不同抽象层级，一个是调用者，一个是实现者。其调用图谱中，目标项目围绕 `asm!` 和裸指针展开，参考项目围绕内核对象和锁机制展开，无任何结构重合。
* **代码溯源链接**:
  - 目标项目: `user/libc/src/lib.rs#fork` -> `user/libc/src/lib.rs#clone` -> `user/libc/src/syscall.rs#syscall5`
  - 参考项目: `api/linux_syscall_api/src/syscall_task/imp/task.rs#syscall_exec`

* **【进程管理】**
* **底层技术析理**: 双方在进程管理上的实现模型完全不同。
  * **目标项目**：进程管理逻辑位于用户态初始化程序 `user_init` 中，表现为对子进程的简单等待与回收。其调用流为 `wait_exit_code` -> `wait4` 系统调用，核心是获取子进程退出状态。
  * **参考项目**：进程管理逻辑位于内核态，表现为复杂的任务创建、克隆和上下文切换。其调用流为 `clone_task` -> `Arc::clone`, `AxTask::new`，核心是复制内核任务控制块、地址空间和信号处理结构。
  * **结论**：目标项目仅通过系统调用使用内核的进程服务，而参考项目实现了这些服务的核心逻辑。两者的调用图谱在深度、广度和操作对象上均无交集。
* **代码溯源链接**:
  - 目标项目: `user/apps/user_init/src/main.rs#wait_exit_code` -> `user/libc/src/lib.rs#wait4`
  - 参考项目: `modules/axtask/src/task.rs#clone_task`

* **【文件系统】**
* **底层技术析理**: 双方的文件系统初始化路径服务于完全不同的目的。
  * **目标项目**：`init_environment` 函数的核心是准备用户态进程的环境变量，通过 `test_env` 构造 `ENV_PATH`, `ENV_TERM` 等字符串指针，为后续通过 `execve` 执行测试程序做准备。
  * **参考项目**：`fs_init` 函数的核心是初始化内核的虚拟文件系统结构，通过硬编码路径字符串 (`"/bin/"`, `"/lib/"`) 和 `FilePath::new` 来创建目录和挂载点。
  * **结论**：一个是用户态环境变量的字符串准备，一个是内核态文件系统目录树的构造。两者的操作对象、系统层级和最终目标均不同，AST 图谱无重合。
* **代码溯源链接**:
  - 目标项目: `user/apps/user_init/src/main.rs#init_environment` -> `user/apps/user_init/src/main.rs#test_env`
  - 参考项目: `ulib/src/file.rs#fs_init`

#### 5. 单方模块与降权项
*   **单方模块**：
    *   **中断/异常处理**、**设备驱动**：仅在参考项目中存在。这反映了参考项目是一个完整的内核实现，而目标项目可能依赖某个未在参赛作品中体现的内核或运行时环境。这是架构上的根本差异，不构成抄袭证据。
    *   **网络协议栈**：仅在目标项目中存在。这表明目标项目可能独立实现或集成了用户态网络功能，而参考项目未包含此部分。这是功能集上的差异。
*   **降权项（通用语言特性与公共接口）**：
    *   **Rust 语言原语**：`Some`, `Err`, `Ok`, `Vec::new`, `panic!`, `asm!` 等。这些是 Rust 编程语言的标准组成部分，任何 Rust 项目都会高频使用，不能作为代码复用的证据。
    *   **操作系统公共接口/概念**：`fork`, `clone`, `execve`, `wait4`, `chdir`, `recv_from`, `accept`, `lookup`, `map` 等。这些是 POSIX 标准、BSD Socket 标准或操作系统领域的通用概念和函数名。不同项目实现同名函数是遵循标准的必然结果，不代表代码抄袭。
    *   **第三方库/公共依赖**：所有 33 个共同 AST 指纹均落在此范畴。它们源于对公开标准（如 Ext4 文件系统格式、VFS 模型）的独立实现，或对公共 crates 的依赖，不构成对参考项目核心逻辑的复用。

#### 6. 保守结论
本次审计基于 0.75% 的极低 AST 相似度，裁定目标项目 `[2026-T2026104869910054-XX]` 与参考项目 `[2024-萌新-南开大学]` 为独立开发。双方在进程管理、内存管理、系统调用等操作系统核心模块的共同 AST 指纹为零，表明其底层实现逻辑完全不同。所有观测到的 33 个共同指纹均位于第三方库/公共依赖，源于对公开标准或通用接口的独立实现，不构成核心逻辑复用。目标项目侧重用户态测试框架与 libc 包装，参考项目侧重内核态完整实现，二者在架构层级和功能集上存在显著差异。综上，该项目原创性风险极低。

------------------------------

