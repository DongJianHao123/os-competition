# 🛡️ OS 内核赛自动化代码级架构审计报告

**审计时间**: `2026-07-02 05:02:33`
**参赛作品名称**: `2026-T2026104869910054-XX`
**年份**: `2026`
**学校**: `XX`
**队伍名称**: `T2026104869910054`
**比赛名字**: `OS-Competition`
**比赛子赛道**: `Kernel-Implementation`
**仓库地址**: `https://gitlab.eduxiji.net/educg-group-43501-3132633/T2026104869910054-3130`
**推测开发语言**: `Rust / C (由AST探针自动识别)`
**特征函数量**: `2260`

## ⚔️ 底层代码同源性对比 (AST Homology)
> 本部分由专门的代码审查智能体，对该项目与符合年份条件的历届最高相似度项目进行控制流与底层调用对比。新版报告会先给出确定性的全模块覆盖矩阵，再给出 Agent 的分模块解释，避免只围绕少量抽样函数展开。

### 🏆 Top 4 底层对标: vs `2025-Chronix-哈尔滨工业大学深圳`（哈尔滨工业大学（深圳） / Chronix）

#### 对方作品信息

**对方年份**: `2025`
**对方学校**: `哈尔滨工业大学（深圳）`
**对方队伍名称**: `Chronix`
**对方比赛名字**: `OS-Competition`
**对方比赛子赛道**: `Kernel-Implementation`
**对方仓库地址**: `https://gitlab.eduxiji.net/educg-group-36002-2710490/T202518123995568-675`

> 双向覆盖率：共同 AST 指纹: 27；唯一 AST 指纹: 目标项目 1950，参考项目 1873；覆盖率: 目标项目 1.38%，参考项目 1.44%

## 全模块对比总览（确定性证据）

> 总体覆盖率：共同 AST 指纹: 27；唯一 AST 指纹: 目标项目 1950，参考项目 1873；覆盖率: 目标项目 1.38%，参考项目 1.44%

### 1. 模块覆盖矩阵
| 模块 | 目标函数数 | 参考函数数 | 共同唯一 AST 指纹 | Target 覆盖 | Base 覆盖 | 状态 |
| :--- | ---: | ---: | ---: | ---: | ---: | :--- |
| 进程管理 | 2 | 176 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |
| 任务调度/异步运行时 | 2 | 275 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |
| 内存管理 | 3 | 562 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |
| 中断/异常处理 | 0 | 89 | 0 | 0.0% | 0.0% | 仅参考项目体现 |
| 系统调用 | 27 | 115 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |
| 文件系统 | 2 | 430 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |
| 设备驱动 | 0 | 200 | 0 | 0.0% | 0.0% | 仅参考项目体现 |
| 网络协议栈 | 1 | 144 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |
| 文件系统/第三方库 | 254 | 60 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |
| 网络协议栈/第三方库 | 137 | 0 | 0 | 0.0% | 0.0% | 仅目标项目体现 |
| 第三方库 | 1805 | 0 | 0 | 0.0% | 0.0% | 仅目标项目体现 |
| 通用模块/其他 | 27 | 228 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |

### 2. 分模块证据清单

#### 进程管理
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `user/apps/user_init/src/main.rs#wait_exit_code` | `os/src/syscall/process.rs#sys_waitpid` | 同模块上下文对齐/差异观察 | Some | Err, Ok, PROCESS_GROUP_MANAGER.remove, Some, TASK_MANAGER.remove_task, UserPtrRaw::new ...(+45) |
| `user/libc/src/lib.rs#fork` | `os/src/syscall/io.rs#sys_epoll_pwait2` | 同模块上下文对齐/差异观察 | clone | Err, Ok, Select2Futures::new, Some, TimedTaskFuture::new, UserPtrRaw::new ...(+29) |

#### 任务调度/异步运行时
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `user/apps/user_init/src/main.rs#run_lmbench_cases` | `user/src/bin/user_shell.rs#main` | 同模块上下文对齐/差异观察 | cstr_eq, run_busybox, run_program, write_group_end, write_group_start, write_str | ProcessArguments::new, String::new, Vec::new, args_addr.as_slice, args_copy[0].as_str, assert_eq! ...(+37) |
| `user/apps/user_init/src/main.rs#init_etc_files` | `os/src/syscall/io.rs#sys_pselect6` | 同模块上下文对齐/差异观察 | run_sh_command | (*timeout.to_ref()).into, (*timeout.to_ref()).is_valid, Err, Ok, PollEvents::empty, Select2Futures::new ...(+52) |

#### 内存管理
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `user/apps/user_init/src/main.rs#run_script` | `os/src/syscall/mod.rs#syscall` | 同模块上下文对齐/差异观察 | chdir, cstr_eq, reap_adopted_zombies, run_cyclictest_cases, run_iperf_cases, run_libctest_scripts ...(+6) | SendWrapper, SysError::ENOSYS.code, SyscallId::from_repr, UserPtrRaw::new, VirtAddr::from, args[0].into ...(+171) |
| `user/apps/user_init/src/main.rs#run_sh_command` | `os/src/syscall/futex.rs#sys_futex` | 同模块上下文对齐/差异观察 | "-c\0".as_ptr, "busybox\0".as_ptr, "sh\0".as_ptr, busybox_path, command.as_ptr, core::ptr::null::<u8> ...(+6) | (uaddr2 as *const _ as usize).into, Err, FutexOp::from, Ok, SumGuard::new, VirtAddr::from ...(+37) |
| `user/apps/user_init/src/main.rs#reap_adopted_zombies` | `os/src/drivers/block/mmc/mod.rs#card_init` | 同模块上下文对齐/差异观察 | wait4 | CID::from, CLKDIV::offset, CMD::data_cmd, CMD::no_data_cmd, CMD::no_data_cmd_no_crc, CMD::no_data_cmd_no_crc(card_idx, 2).with_response_length ...(+38) |

#### 中断/异常处理
仅参考项目中观察到该模块代表函数，目标项目没有同模块可对齐证据：
- 参考项目：`hal/src/component/trap/loongarch64/mod.rs#restore`，调用摘要：core::arch::asm!, register::euen::read, register::euen::read().fpe, register::euen::set_fpe
- 参考项目：`hal/src/component/trap/loongarch64/mod.rs#save`，调用摘要：core::arch::asm!, register::euen::read, register::euen::read().fpe, register::euen::set_fpe
- 参考项目：`hal/src/component/trap/riscv64/mod.rs#restore`，调用摘要：asm!
- 参考项目：`hal/src/component/trap/riscv64/mod.rs#save`，调用摘要：asm!

#### 系统调用
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `user/libc/src/lib.rs#execve` | `user/src/lib.rs#execve` | 同板块同名 | argv.as_ptr, envp.as_ptr, path.as_ptr, syscall::syscall3 | CString::new, CString::new(*s).unwrap, CString::new(path).unwrap, argv.as_ptr, argv.iter, argv.iter().map ...(+12) |
| `user/libc/src/lib.rs#exit` | `user/src/lib.rs#exit` | 同板块同名 | core::hint::spin_loop, syscall::syscall1 | sys_exit |
| `user/libc/src/lib.rs#chdir` | `user/src/lib.rs#chdir` | 同板块同名 | path.as_ptr, syscall::syscall1 | path.as_ptr, sys_chdir |
| `user/libc/src/lib.rs#clone` | `os/src/syscall/net.rs#set` | 同模块上下文对齐/差异观察 | syscall::syscall5 | <[u8; 4]>::try_from, <[u8; 4]>::try_from(&opt[0..4]).unwrap, Duration::from_secs, Err, Ok, SOCKET_SET.with_socket_mut::<smoltcp::socket::tcp::Socket,_,_> ...(+15) |

#### 文件系统
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `user/apps/user_init/src/main.rs#test_env` | `os/src/net/raw.rs#send` | 同模块上下文对齐/差异观察 | ENV_LIBRARY_PATH.as_ptr, ENV_LMBENCH_ENOUGH.as_ptr, ENV_LTP_DEV.as_ptr, ENV_LTP_DEV_FS_TYPE.as_ptr, ENV_PATH.as_ptr, ENV_TERM.as_ptr ...(+1) | 0u16.to_be_bytes, Err, Ok, SOCKET_SET.poll_interfaces, SOCKET_SET.with_socket_mut::<smoltcp::socket::raw::Socket, _, _>, [ipv4_header.as_ref(), data].concat ...(+25) |
| `user/apps/user_init/src/main.rs#init_environment` | `os/src/fs/devfs/mod.rs#init_devfs` | 同模块上下文对齐/差异观察 | init_arch_environment, init_etc_files | CpuDmaLatencyInode::new, DCACHE.lock, DCACHE.lock().insert, LoopDevDentry::new, LoopDevInode::new, NullDentry::new ...(+43) |

#### 设备驱动
仅参考项目中观察到该模块代表函数，目标项目没有同模块可对齐证据：
- 参考项目：`os/src/devices/serial.rs#get_serial`，调用摘要：Box::new, Serial::new, Uart::new, log::info!, panic!, reg.size.unwrap ...(+21)
- 参考项目：`os/src/drivers/serial/uart.rs#init_u32`，调用摘要：log::info!, reg.byte_add, reg.byte_add(0 << self.reg_shift)
                .write_volatile, reg.byte_add(1 << self.reg_shift)
                .write_volatile, reg.byte_add(FCR << self.reg_shift).write_volatile, reg.byte_add(IER << self.reg_shift).write_volatile ...(+2)
- 参考项目：`os/src/net/mod.rs#init_network`，调用摘要："".parse, "".parse().unwrap, "127.0.0.1".parse, "127.0.0.1".parse().unwrap, ETH0.call_once, EthernetAddress ...(+15)
- 参考项目：`os/src/devices/serial.rs#scan_char_device`，调用摘要：Arc::new, bytes.iter, chosen
        .properties, chosen
        .properties()
        .find, chosen
        .properties()
        .find(|n| n.name == "stdout-path")
        .and_then, chosen
        .properties()
        .find(|n| n.name == "stdout-path")
        .and_then(|n| {
            let bytes = unsafe {
                core::slice::from_raw_parts_mut((n.value.as_ptr()) as *mut u8, n.value.len())
            };
            let mut len = 0;
            for byte in bytes.iter() {
                if *byte == b':' {
                    return core::str::from_utf8(&n.value[..len]).ok();
                }
                len += 1;
            }
            core::str::from_utf8(&n.value[..n.value.len() - 1]).ok()
        })
        .unwrap ...(+13)

#### 网络协议栈
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `user/apps/user_init/src/main.rs#run_netperf_cases` | `os/src/net/tcp.rs#recv` | 同模块上下文对齐/差异观察 | core::ptr::null::<u8>, cstr_eq, kill_pid, push_arg_ptrs, push_arg_ptrs_at, run_busybox ...(+7) | Err, Ok, SOCKET_SET.with_socket_mut::<tcp::Socket,_,_>, get_current_time_duration, get_waker, info! ...(+15) |

#### 文件系统/第三方库
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `crates/fs/src/impls/ext4/adapter.rs#mount` | `os/src/fs/ext4/fstype.rs#mount` | 同板块同名 | BgDesc::parse, InodeCache::new, Mutex::new, Ok, PathCache::new, SB_BUF.as_mut_ptr ...(+9) | Arc::downgrade, Arc::from_raw, Arc::new, DCACHE.lock, DCACHE.lock().insert, Ext4Dentry::new ...(+14) |
| `crates/fs/src/impls/ext4/file.rs#rename` | `os/src/fs/ext4/inode.rs#rename` | 同板块同名 | Block::load, Err, Ok, blk_sz.min, block.le16, block.le32 ...(+8) | Err, InodeMode::from_inode_type, InodeMode::from_inode_type(ty).get_type, Ok, file.dir_mv, file.dir_rm ...(+11) |
| `crates/fs/src/impls/ext4/file.rs#symlink` | `os/src/fs/ext4/inode.rs#symlink` | 同板块同名 | Ok, add_dir_entry, alloc_inode, sb.supports_extents, target.as_bytes, write_file ...(+1) | Arc::new, Ext4Inode::new, Ok, SysError::from_i32, file.symlink_create, file.symlink_create(target_path, link_path).map_err ...(+4) |
| `crates/fs/src/impls/ext4/file.rs#link` | `os/src/fs/ext4/inode.rs#link` | 同板块同名 | Ok, add_dir_entry, inode.links_count.saturating_add, read_inode, write_inode | Ok, file.link_create, file.link_create(target_path).expect, self.file.lock |

#### 网络协议栈/第三方库
仅目标项目中观察到该模块代表函数，参考项目没有同模块可对齐证据：
- 目标项目：`crates/syscall/src/imp/net.rs#sys_socketpair`，调用摘要：Some, core::slice::from_raw_parts, current_token, entry0.clone, entry1.clone, fd_alloc ...(+13)
- 目标项目：`crates/syscall/src/imp/net.rs#udp_recv_iov_queued`，调用摘要：bind_lookup, bind_lookup(local_port).is_none, bound_set_read_waiter, cancel_udp_wait, crate::imp::signal::check_itimers, crate::imp::signal::has_pending_current ...(+12)
- 目标项目：`crates/syscall/src/imp/net.rs#pipewrite_socket`，调用摘要：cancel_socket_wait, crate::imp::signal::check_itimers, crate::imp::signal::has_pending_current, fs::pipe::pipe_get, fs::pipe::pipe_set_write_waiter, pipe.available_write ...(+9)
- 目标项目：`crates/syscall/src/imp/net.rs#udp_recv_queued`，调用摘要：bind_lookup, bind_lookup(local_port).is_none, bound_set_read_waiter, cancel_udp_wait, crate::imp::signal::check_itimers, crate::imp::signal::has_pending_current ...(+10)

#### 第三方库
仅目标项目中观察到该模块代表函数，参考项目没有同模块可对齐证据：
- 目标项目：`crates/syscall/src/dispatch.rs#handle_syscall`，调用摘要：imp::fs::sys_bpf, imp::fs::sys_chdir, imp::fs::sys_close, imp::fs::sys_dup, imp::fs::sys_dup3_flags, imp::fs::sys_epoll_create1 ...(+162)
- 目标项目：`crates/task/src/fork.rs#sys_clone`，调用摘要：(child_tid as u32).to_le_bytes, Process::new, Some, Vec::new, alloc_pid, alloc_tid ...(+52)
- 目标项目：`crates/syscall/src/imp/fs.rs#sys_read`，调用摘要：Ok, alloc_io_buffer, block_device_read, buf_ptr.wrapping_add, console_getchar, copy_to_user_iter ...(+42)
- 目标项目：`crates/syscall/src/imp/fs.rs#sys_openat`，调用摘要：Err, chown_new_ramfs_inode, core::str::from_utf8, core::str::from_utf8(&path_buf[..(path_len as usize - 1)]).unwrap_or, create_file, ext4fs.chmod ...(+42)

#### 通用模块/其他
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `user/libc/src/lib.rs#write_str` | `hal/src/component/console/loongarch64.rs#write_str` | 同板块同名 | s.as_bytes, write | Ok, console_putchar, s.chars |
| `user/apps/user_init/src/main.rs#init_arch_environment` | `os/src/main.rs#pre_main` | 同模块上下文对齐/差异观察 | busybox_path, mkdirat_path, run_busybox, sym_link | naked_asm! |
| `user/apps/user_init/src/main.rs#run_cyclictest_cases` | `user/src/bin/user_shell.rs#new` | 同模块上下文对齐/差异观察 | kill_pid, run_busybox, run_program, spawn_program, wait4, write_group_end ...(+4) | String::new, arg.as_ptr, arg.as_str, arg.is_empty, args
            .iter, args
            .iter()
            .filter ...(+16) |
| `user/apps/user_init/src/main.rs#run_iperf_cases` | `os/src/utils/ring_buffer.rs#read` | 同模块上下文对齐/差异观察 | core::ptr::null::<u8>, kill_pid, push_arg_ptrs, push_arg_ptrs_at, run_busybox, run_program_ptrs ...(+7) | buf.is_empty, buf.len, buf[..ret_len].copy_from_slice, buf[..right_len].copy_from_slice, buf[right_len..ret_len].copy_from_slice, cmp::min ...(+1) |


好的，作为操作系统内核架构审计专家，我将基于提供的客观AST证据、双方模块画像及控辩双方陈述，进行最终的独立、定性仲裁。

### 1. 宏观判定
- **定性结论**: **底层架构显著差异 / 独立开发**
- **核心依据**: 客观AST相似度仅为 **0.71%**，共同AST指纹仅 **27** 个，而双方各自拥有近2000个唯一指纹，覆盖率均不足1.5%。在所有核心系统模块（进程、内存、系统调用等）中，**共同唯一AST指纹数均为0**，表明不存在任何函数级别的结构性代码复用。控方指控主要基于`asm!`、`.as_ptr()`等Rust系统编程通用惯用法及POSIX强制接口名，不构成独创性表达。

### 2. 全模块覆盖矩阵解读
| 模块 | 双方存在 | 共同AST指纹 | 覆盖率解读 | 证据强度 |
| :--- | :---: | :---: | :--- | :--- |
| **进程管理** | 是 | 0 | 目标项目仅2个函数，参考项目176个，规模差异巨大。零共同指纹表明无复用。 | **未形成** |
| **任务调度/异步运行时** | 是 | 0 | 目标项目为用户态测试编排，参考项目包含内核异步运行时，功能定位完全不同。零共同指纹。 | **未形成** |
| **内存管理** | 是 | 0 | 目标项目为用户态脚本执行器，参考项目为内核内存管理，抽象层次迥异。零共同指纹。 | **未形成** |
| **中断/异常处理** | 否 | 0 | 仅参考项目存在，属于其独有架构实现。 | **未形成** |
| **系统调用** | 是 | 0 | 控方指控核心区。双方均有用户态封装，但设计范式（元数分派 vs 泛型封装）和实现细节不同。零共同指纹。 | **弱** |
| **文件系统** | 是 | 0 | 目标项目为用户态环境初始化，参考项目为内核devfs构建，层级和目的不同。零共同指纹。 | **未形成** |
| **设备驱动** | 否 | 0 | 仅参考项目存在，属于其独有硬件适配层。 | **未形成** |
| **网络协议栈** | 是 | 0 | 目标项目为用户态网络测试，参考项目为内核协议栈，层级不同。零共同指纹。 | **未形成** |
| **第三方库/公共依赖** | 部分 | 0 | 目标项目包含大量自研crate（1805个函数），参考项目无。双方共有的ext4等文件系统逻辑，在同名函数上调用链差异显著。 | **弱** |

### 3. 底层 AST 关键证据指纹表
| 所属板块 | 目标项目路径 | 参考项目路径 | 证据类型 | 技术说明 |
| :--- | :--- | :--- | :--- | :--- |
| **系统调用** | `user/libc/src/syscall.rs` # `syscall0`..`syscall6` | `user/src/syscall.rs` # `syscall` | 通用惯用法 | 均调用`asm!`宏，这是Rust `no_std`环境下触发系统陷阱的唯一标准手段，非原告独创。目标项目按参数个数拆分为多个函数，与原告的单一泛型函数设计范式不同。 |
| **系统调用** | `user/libc/src/lib.rs` # `execve` | `user/src/lib.rs` # `execve` | 接口强制/通用惯用法 | 同名源于POSIX标准。双方最终都提取`argv.as_ptr`等裸指针，这是满足`execve`系统调用ABI的强制要求。目标项目为裸指针直传，原告项目则经过`CString`和`Vec`构建，数据流结构显著不同。 |
| **系统调用** | `user/libc/src/lib.rs` # `chdir` | `user/src/lib.rs` # `chdir` | 接口强制/通用惯用法 | 同名源于POSIX标准。`path.as_ptr()`是将Rust字符串转为C风格指针的通用做法。目标项目调用自研的`syscall::syscall1`，原告调用`sys_chdir`，底层封装路径不同。 |
| **系统调用** | `user/libc/src/lib.rs` # `exit` | `user/src/lib.rs` # `exit` | 接口强制/结构差异 | 同名源于POSIX标准。目标项目在调用`syscall1`前包含`core::hint::spin_loop`自旋逻辑，原告项目直接调用`sys_exit`，AST结构已产生变异。 |
| **文件系统/第三方库** | `crates/fs/src/impls/ext4/adapter.rs` # `mount` | `os/src/fs/ext4/fstype.rs` # `mount` | 公共领域逻辑 | 同名源于ext4标准操作。双方调用链差异巨大：目标项目依赖`BgDesc::parse`、`InodeCache::new`等，原告项目依赖`Arc::new`、`DCACHE.lock`等，表明底层实现和内存管理模型完全不同。 |

### 4. 核心代码调用流对比剖析
*   **【系统调用/用户态封装】**
    *   **底层技术析理**: 双方都实现了用户态到内核态的切换机制，但**抽象层次与设计策略**截然不同。目标项目采用**元数分派**模式，将系统调用按参数数量拆分为`syscall0`到`syscall6`七个独立函数，在编译期进行类型安全检查。参考项目则采用**泛型封装**模式，通过一个统一的`syscall`函数处理所有调用。尽管两者最终都依赖`asm!`宏执行特权指令，但`asm!`是Rust裸机编程中实现此功能的唯一标准路径，其内部寄存器分配与指令序列由目标架构ABI决定，属于领域公共知识。两者的差异体现了不同的软件工程设计哲学，而非抄袭。
    *   **代码溯源链接**:
        *   目标项目: `user/libc/src/syscall.rs#syscall3`
        *   参考项目: `user/src/syscall.rs#syscall`

*   **【文件系统/第三方库-ext4挂载】**
    *   **底层技术析理**: 双方都实现了ext4文件系统的挂载操作，但**对象管理模型**存在本质区别。目标项目使用`Mutex::new`进行同步，并依赖`BgDesc::parse`、`InodeCache::new`等自研组件，呈现出自包含的、基于结构体的设计。参考项目则大量使用`Arc`进行原子引用计数，并通过`DCACHE.lock()`操作全局目录项缓存，呈现出更复杂的共享所有权和全局状态管理模式。这种在**同步原语和内存管理**上的根本性差异，是架构级异构的铁证。
    *   **代码溯源链接**:
        *   目标项目: `crates/fs/src/impls/ext4/adapter.rs#mount`
        *   参考项目: `os/src/fs/ext4/fstype.rs#mount`

### 5. 单方模块与降权项
*   **单方独有模块**:
    *   **中断/异常处理**、**设备驱动**: 仅存在于参考项目，是其硬件抽象层（HAL）和具体硬件适配的体现。
    *   **第三方库 (crates)** : 目标项目包含大量自研crate（如`crates/syscall`、`crates/task`、`crates/fs`），函数量达1805个，是其核心逻辑所在，在参考项目中无对应物。
    *   **结论**: 这些单方模块是双方项目架构差异和独立演进的直接证据。
*   **降权项说明**:
    *   **跨板块/跨层级比对**: 控方及证据底座中大量将用户态应用（`user_init`）与内核态子系统（`sys_futex`、`card_init`、`sys_pselect6`）进行比对，此类比对违反同域原则，已全部降权为无效证据。
    *   **公共接口/惯用法**: `execve`、`chdir`、`exit`等函数同名是遵循POSIX标准的结果。`asm!`、`.as_ptr()`、`Some`、`Ok`、`Err`等是Rust语言的通用语法和标准库类型，不具备可保护性，不能作为同源证据。

### 6. 保守结论
本次审计基于极低的AST覆盖率（0.71%）和所有核心模块零共同唯一指纹的客观事实，判定双方项目在底层架构上存在显著差异，属于独立开发。控方提出的“系统调用封装层抄袭”指控，其依据`asm!`、`.as_ptr()`等均为Rust系统编程领域的标准惯用法或POSIX强制接口要求，不构成独创性表达。双方在系统调用封装的设计范式（元数分派 vs 泛型）、关键数据流处理（裸指针直传 vs CString构建）以及文件系统等复杂模块的同步与内存管理模型上均存在根本性不同。综合来看，`[2026-T2026104869910054-XX]`项目不存在对`[2025-Chronix-哈尔滨工业大学深圳]`项目的代码抄袭或结构性复用，整体原创性风险极低。

------------------------------

