# 🛡️ OS 内核赛自动化代码级架构审计报告

**审计时间**: `2026-07-02 13:11:08`
**参赛作品名称**: `2026-T2026105749910760-XX`
**年份**: `2026`
**学校**: `XX`
**队伍名称**: `T2026105749910760`
**比赛名字**: `OS-Competition`
**比赛子赛道**: `Kernel-Implementation`
**仓库地址**: `https://gitlab.eduxiji.net/educg-group-43501-3132633/T2026105749910760-3579`
**推测开发语言**: `Rust / C (由AST探针自动识别)`
**特征函数量**: `1248`

## ⚔️ 底层代码同源性对比 (AST Homology)
> 本部分由专门的代码审查智能体，对该项目与符合年份条件的历届最高相似度项目进行控制流与底层调用对比。新版报告会先给出确定性的全模块覆盖矩阵，再给出 Agent 的分模块解释，避免只围绕少量抽样函数展开。

### 🏆 Top 2 底层对标: vs `2024-undefinedType-南京航空航天大学`（南京航空航天大学 / undefinedType）

#### 对方作品信息

**对方年份**: `2024`
**对方学校**: `南京航空航天大学`
**对方队伍名称**: `undefinedType`
**对方比赛名字**: `OS-Competition`
**对方比赛子赛道**: `Kernel-Implementation`
**对方仓库地址**: `https://gitlab.eduxiji.net/educg-group-26010-2376550/T202410287992755-2176`

> 双向覆盖率：共同 AST 指纹: 375；唯一 AST 指纹: 目标项目 1068，参考项目 398；覆盖率: 目标项目 35.11%，参考项目 94.22%

## 全模块对比总览（确定性证据）

> 总体覆盖率：共同 AST 指纹: 375；唯一 AST 指纹: 目标项目 1068，参考项目 398；覆盖率: 目标项目 35.11%，参考项目 94.22%

### 1. 模块覆盖矩阵
| 模块 | 目标函数数 | 参考函数数 | 共同唯一 AST 指纹 | Target 覆盖 | Base 覆盖 | 状态 |
| :--- | ---: | ---: | ---: | ---: | ---: | :--- |
| 进程管理 | 72 | 0 | 0 | 0.0% | 0.0% | 仅目标项目体现 |
| 任务调度/异步运行时 | 67 | 0 | 0 | 0.0% | 0.0% | 仅目标项目体现 |
| 内存管理 | 209 | 0 | 0 | 0.0% | 0.0% | 仅目标项目体现 |
| 中断/异常处理 | 15 | 0 | 0 | 0.0% | 0.0% | 仅目标项目体现 |
| 系统调用 | 15 | 0 | 0 | 0.0% | 0.0% | 仅目标项目体现 |
| 文件系统 | 151 | 0 | 0 | 0.0% | 0.0% | 仅目标项目体现 |
| 设备驱动 | 61 | 0 | 0 | 0.0% | 0.0% | 仅目标项目体现 |
| 网络协议栈 | 0 | 0 | 0 | 0.0% | 0.0% | 双方均未体现 |
| 文件系统/第三方库 | 519 | 478 | 375 | 86.61% | 94.22% | 双方存在且有结构重合 |
| 通用模块/其他 | 139 | 0 | 0 | 0.0% | 0.0% | 仅目标项目体现 |

### 2. 分模块证据清单

#### 进程管理
仅目标项目中观察到该模块代表函数，参考项目没有同模块可对齐证据：
- 目标项目：`kernel/fs/procfs.c#procfs_render_file`，调用摘要：TIME2SEC, freemem_bytes, r_time, snprintf, totalram_bytes
- 目标项目：`kernel/ipc/signal.c#do_sigtimedwait`，调用摘要：Warn, acquire, holding, mythread, release, sig_existed ...(+4)
- 目标项目：`kernel/ipc/signal.c#do_sigprocmask`，调用摘要：Log, Warn, acquire, mythread, release, sig_del_set_mask ...(+1)
- 目标项目：`kernel/sched/proc.c#proc_exit`，调用摘要：acquire, fileclose, freeprocvm, memset, myproc, panic ...(+6)

#### 任务调度/异步运行时
仅目标项目中观察到该模块代表函数，参考项目没有同模块可对齐证据：
- 目标项目：`kernel/lib/snprintf.c#vsnprintf`，调用摘要：is_digit, number, skip_atoi, strnlen, va_arg, vsnprintf_writechar
- 目标项目：`kernel/lib/snprintf.c#vsprintf`，调用摘要：is_digit, number, skip_atoi, strnlen, va_arg
- 目标项目：`kernel/ipc/signal.c#signal_handle`，调用摘要：Log, Warn, acquire, do_handle_signal, kfree, list_del_reinit ...(+6)
- 目标项目：`kernel/fs/sysfile.c#generic_utimensat`，调用摘要：Log, file_exist, fs->fsops->utimens, generic_fstat, get_abpath_from_dirfd, printf ...(+1)

#### 内存管理
仅目标项目中观察到该模块代表函数，参考项目没有同模块可对齐证据：
- 目标项目：`kernel/fs/exec.c#execve`，调用摘要：ADD_AUXV, Log, PGROUNDDOWN, PGROUNDUP, Warn, copyout ...(+19)
- 目标项目：`kernel/sched/proc.c#do_clone`，调用摘要：Log, acquire, alloc_thread, append_child, atomic_add_return, copyout ...(+14)
- 目标项目：`kernel/fs/virtio_disk.c#virtio_disk_init`，调用摘要：R, initlock, memset, panic
- 目标项目：`kernel/fs/virtio_disk.c#virtio_disk_rw_multiple`，调用摘要：R, __sync_synchronize, acquire, alloc_descs, free_chain, release ...(+1)

#### 中断/异常处理
仅目标项目中观察到该模块代表函数，参考项目没有同模块可对齐证据：
- 目标项目：`kernel/arch/loongarch/drv_ahci.c#ahci_host_init`，调用摘要：ahci_mdelay, ahci_port_base, ahci_printf, ahci_readl, ahci_writel
- 目标项目：`kernel/arch/loongarch/pci.c#pci_device_dump`，调用摘要：pci_device_bar_dump, printf
- 目标项目：`kernel/arch/loongarch/pci.c#pci_device_init`，调用摘要：内部闭环逻辑
- 目标项目：`kernel/arch/qemu/plic.c#plicinithart`，调用摘要：PLIC_SENABLE, PLIC_SPRIORITY, cpuid

#### 系统调用
仅目标项目中观察到该模块代表函数，参考项目没有同模块可对齐证据：
- 目标项目：`kernel/mm/mmap.c#sys_munmap`，调用摘要：Log, argaddr, argint, do_munmap
- 目标项目：`kernel/mm/mmap.c#sys_mprotect`，调用摘要：Log, argaddr, argint, do_mprotect
- 目标项目：`kernel/sysother.c#sys_poweroff`，调用摘要：argint, opensbi_poweroff, qemu_raw_poweroff
- 目标项目：`kernel/syscall.c#argstr`，调用摘要：argaddr, fetchstr

#### 文件系统
仅目标项目中观察到该模块代表函数，参考项目没有同模块可对齐证据：
- 目标项目：`user/init/init.c#main`，调用摘要：chdir, dev, dup, execve, exit, fork ...(+6)
- 目标项目：`kernel/arch/loongarch/smoldtb.c#dtb_finalise_to_buffer`，调用摘要：be32, be64, do_foreach_sibling
- 目标项目：`kernel/fs/vfs/vfs.c#get_absolute_path`，调用摘要：strcat, strcpy, strlen, strncpy
- 目标项目：`kernel/fs/sysfile.c#sys_mount`，调用摘要：内部闭环逻辑

#### 设备驱动
仅目标项目中观察到该模块代表函数，参考项目没有同模块可对齐证据：
- 目标项目：`kernel/arch/loongarch/drv_ahci.c#ahci_print_info`，调用摘要：ahci_printf
- 目标项目：`kernel/arch/loongarch/drv_ahci.c#ahci_exec_ata_cmd`，调用摘要：ahci_fill_cmd_slot, ahci_fill_sg, ahci_get_cmd_slot, ahci_memcpy, ahci_printf, ahci_readl ...(+2)
- 目标项目：`kernel/arch/loongarch/drv_ahci.c#ahci_sata_rw_cmd_ext`，调用摘要：ahci_exec_ata_cmd
- 目标项目：`kernel/arch/loongarch/drv_ahci.c#ahci_fill_sg`，调用摘要：ahci_printf, ahci_virt_to_phys

#### 文件系统/第三方库
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `kernel/fs/ext4_balloc.c#ext4_balloc_alloc_block` | `Kernel/File/lwext4/ext4_balloc.c#ext4_balloc_alloc_block` | 同板块同名同结构 | ext4_balloc_get_bgid_of_block, ext4_balloc_get_block_of_bgid, ext4_balloc_set_bitmap_csum, ext4_balloc_verify_bitmap_csum, ext4_bg_get_block_bitmap, ext4_bg_get_free_blocks_count ...(+19) | EXT4_Debug_u32, ext4_balloc_get_bgid_of_block, ext4_balloc_get_block_of_bgid, ext4_balloc_set_bitmap_csum, ext4_balloc_verify_bitmap_csum, ext4_bg_get_block_bitmap ...(+19) |
| `kernel/fs/ext4.c#ext4_fwrite` | `Kernel/File/lwext4/ext4.c#ext4_fwrite` | 同板块同名 | EXT4_MP_LOCK, EXT4_MP_UNLOCK, Log, ext4_assert, ext4_block_cache_write_back, ext4_block_writebytes ...(+11) | EXT4_MP_LOCK, EXT4_MP_UNLOCK, ext4_assert, ext4_block_cache_write_back, ext4_block_writebytes, ext4_blocks_set_direct ...(+10) |
| `kernel/fs/ext4_journal.c#jbd_journal_prepare` | `Kernel/File/lwext4/ext4_journal.c#jbd_journal_prepare` | 同板块同名同结构 | EXT4_BLOCK_ZERO, RB_FIND, TAILQ_FOREACH_REVERSE_SAFE, TAILQ_FOREACH_SAFE, TAILQ_REMOVE, ext4_bcache_clear_dirty ...(+17) | EXT4_BLOCK_ZERO, RB_FIND, TAILQ_FOREACH_REVERSE_SAFE, TAILQ_FOREACH_SAFE, TAILQ_REMOVE, ext4_bcache_clear_dirty ...(+17) |
| `kernel/fs/ext4.c#ext4_dir_rm` | `Kernel/File/lwext4/ext4.c#ext4_dir_rm` | 同板块同名同结构 | EXT4_MP_LOCK, EXT4_MP_UNLOCK, ext4_block_cache_write_back, ext4_dir_en_get_inode, ext4_dir_iterator_fini, ext4_dir_iterator_init ...(+19) | EXT4_MP_LOCK, EXT4_MP_UNLOCK, ext4_block_cache_write_back, ext4_dir_en_get_inode, ext4_dir_iterator_fini, ext4_dir_iterator_init ...(+19) |

#### 通用模块/其他
仅目标项目中观察到该模块代表函数，参考项目没有同模块可对齐证据：
- 目标项目：`kernel/lib/qsort.c#qsort`，调用摘要：(*cmp), POP, PUSH, SWAP, min_t
- 目标项目：`kernel/lib/snprintf.c#number`，调用摘要：do_div
- 目标项目：`kernel/arch/loongarch/smoldtb.c#print_node`，调用摘要：be32, do_foreach_prop, do_foreach_sibling, dtb_align_up, memcpy, string_len
- 目标项目：`kernel/ipc/futex.c#futex_wake`，调用摘要：ASSERT, Log, Warn, acquire, futex_free, get_futex ...(+5)


# 最终同源性定性仲裁裁决书

## 1. 宏观判定

**定性结论**: 独立开发，存在第三方公共库集成重合 (相似度: 34.37%，共同 AST 指纹: 375；唯一 AST 指纹: 目标项目 1068，参考项目 398；覆盖率: 目标项目 35.11%，参考项目 94.22%)

**核心裁决**: 目标项目 `[2026-T2026105749910760-XX]` 与参考项目 `[2024-undefinedType-南京航空航天大学]` 在操作系统内核主体架构上呈现**底层架构显著差异**。目标项目拥有进程管理、任务调度、内存管理、中断/异常处理、系统调用、设备驱动、原创文件系统等完整子系统，与参考项目零重合。双方唯一重合板块为“文件系统/第三方库”，该重合源于双方均集成了开源 `lwext4` 库，且 ext4 文件系统规范对函数命名与调用序列存在强约束。参考项目覆盖率高达 94.22% 说明其本身几乎全部由 `lwext4` 构成，任何集成该库的项目均会与之产生高覆盖率。目标项目整体独立 AST 指纹占比 64.89%，原创性显著。

---

## 2. 全模块覆盖矩阵解读

| 模块 | 目标函数数 | 参考函数数 | 共同 AST 指纹 | Target 覆盖 | Base 覆盖 | 证据强度 | 解读 |
| :--- | ---: | ---: | ---: | ---: | ---: | :--- | :--- |
| **进程管理** | 72 | 0 | 0 | 0.0% | 0.0% | **未形成** | 仅目标项目存在。包含 `proc_exit`、`do_sigtimedwait`、`do_sigprocmask` 等完整进程生命周期管理，参考项目无任何对应模块。 |
| **任务调度/异步运行时** | 67 | 0 | 0 | 0.0% | 0.0% | **未形成** | 仅目标项目存在。包含 `signal_handle`、`vsnprintf`、`generic_utimensat` 等调度与运行时支持，参考项目无对应模块。 |
| **内存管理** | 209 | 0 | 0 | 0.0% | 0.0% | **未形成** | 仅目标项目存在。包含 `execve`、`do_clone`、`virtio_disk_rw_multiple` 等内存分配与页表管理，参考项目无对应模块。 |
| **中断/异常处理** | 15 | 0 | 0 | 0.0% | 0.0% | **未形成** | 仅目标项目存在。包含 `ahci_host_init`、`pci_device_init`、`plicinithart` 等 LoongArch/QEMU 平台中断处理，参考项目无对应模块。 |
| **系统调用** | 15 | 0 | 0 | 0.0% | 0.0% | **未形成** | 仅目标项目存在。包含 `sys_munmap`、`sys_mprotect`、`sys_poweroff`、`argstr` 等系统调用入口，参考项目无对应模块。 |
| **文件系统** | 151 | 0 | 0 | 0.0% | 0.0% | **未形成** | 仅目标项目存在。包含 `get_absolute_path`、`sys_mount`、`init.c#main` 等 VFS 与路径解析原创实现，参考项目无对应模块。 |
| **设备驱动** | 61 | 0 | 0 | 0.0% | 0.0% | **未形成** | 仅目标项目存在。包含 `ahci_exec_ata_cmd`、`ahci_fill_sg` 等 AHCI SATA 驱动，参考项目无对应模块。 |
| **网络协议栈** | 0 | 0 | 0 | 0.0% | 0.0% | **未形成** | 双方均未体现网络协议栈模块。 |
| **文件系统/第三方库** | 519 | 478 | 375 | 86.61% | 94.22% | **强（第三方库）** | 双方均存在且高度重合。目标项目路径 `kernel/fs/ext4*`，参考项目路径 `Kernel/File/lwext4/ext4*`，均为开源 `lwext4` 库的集成。375 个共同指纹对应 `lwext4` 公共 API 调用面。 |
| **通用模块/其他** | 139 | 0 | 0 | 0.0% | 0.0% | **未形成** | 仅目标项目存在。包含 `qsort`、`futex_wake`、`print_node` 等通用工具与算法，参考项目无对应模块。 |

**矩阵总结**: 除“文件系统/第三方库”外，目标项目在 7 个核心子系统模块中与参考项目零重合。参考项目仅存在“文件系统/第三方库”单一模块，其 94.22% 的覆盖率说明其本身即为 `lwext4` 库的封装。目标项目 35.11% 的整体覆盖率意味着 64.89% 的 AST 指纹为独立原创。

---

## 3. 底层 AST 关键证据指纹表

| 所属板块 | 目标项目路径 | 参考项目路径 | 证据类型 | 技术说明 |
| :--- | :--- | :--- | :--- | :--- |
| 第三方库/公共依赖 | `kernel/fs/ext4_balloc.c#ext4_balloc_alloc_block` | `Kernel/File/lwext4/ext4_balloc.c#ext4_balloc_alloc_block` | 同板块同名同结构 | ext4 块分配标准流程，调用 `ext4_balloc_get_bgid_of_block`、`ext4_bg_get_block_bitmap` 等 lwext4 公共 API。目标项目使用 `Log` 替代 `EXT4_Debug_u32`。 |
| 第三方库/公共依赖 | `kernel/fs/ext4.c#ext4_fwrite` | `Kernel/File/lwext4/ext4.c#ext4_fwrite` | 同板块同名 | ext4 文件写入标准流程，调用 `EXT4_MP_LOCK`、`ext4_block_writebytes`、`ext4_blocks_set_direct` 等。目标项目额外调用 `Log`。 |
| 第三方库/公共依赖 | `kernel/fs/ext4_journal.c#jbd_journal_prepare` | `Kernel/File/lwext4/ext4_journal.c#jbd_journal_prepare` | 同板块底层逻辑一致 | JBD 日志准备，必须遍历 `RB_FIND`、`TAILQ_FOREACH_*`、`ext4_bcache_*` 等 JBD 标准数据结构。 |
| 第三方库/公共依赖 | `kernel/fs/ext4.c#ext4_dir_rm` | `Kernel/File/lwext4/ext4.c#ext4_dir_rm` | 同板块底层逻辑一致 | 目录删除必须加锁、迭代目录项、释放 inode，调用 `ext4_dir_iterator_*` 与 `ext4_fs_free_inode`。 |
| 第三方库/公共依赖 | `kernel/fs/ext4_dir_idx.c#ext4_dir_dx_split_index` | `Kernel/File/lwext4/ext4_dir_idx.c#ext4_dir_dx_split_index` | 同板块底层逻辑一致 | 目录索引分裂必须操作 `climit` 的 count/limit 与 entry 的 hash/block。 |
| 第三方库/公共依赖 | `kernel/fs/ext4_dir_idx.c#ext4_dir_dx_split_data` | `Kernel/File/lwext4/ext4_dir_idx.c#ext4_dir_dx_split_data` | 同板块底层逻辑一致 | 数据节点分裂必然涉及 `EXT4_DIRENT_TAIL`、`ext4_dir_en_get_entry_len` 等 dirent 字段操作。 |
| 第三方库/公共依赖 | `kernel/fs/ext4_mkfs.c#ext4_mkfs` | `Kernel/File/lwext4/ext4_mkfs.c#ext4_mkfs` | 同板块同名 | mkfs 主流程，调用 `alloc_inodes`、`compute_blocks_per_group` 等。目标项目含 `ext4_bcache_fini_dynamic`，参考项目含 `EXT4_Debug*` 系列。 |
| 第三方库/公共依赖 | `kernel/fs/ext4_fs.c#ext4_fs_set_inode_data_block_index` | `Kernel/File/lwext4/ext4_fs.c#ext4_fs_set_inode_data_block_index` | 同板块底层逻辑一致 | 设置 inode 数据块索引必须操作直接/间接块指针，调用 `ext4_inode_set_direct_block` 等固定 API。 |
| 第三方库/公共依赖 | `kernel/fs/ext4_journal.c#jbd_iterate_log` | `Kernel/File/lwext4/ext4_journal.c#jbd_iterate_log` | 同板块同名同结构 | 日志迭代回放，调用 `jbd_block_get`、`jbd_build_revoke_tree` 等。目标项目使用 `ext4_dbg` 替代 `EXT4_Debug*`。 |
| 第三方库/公共依赖 | `kernel/fs/ext4.c#ext4_fread` | `Kernel/File/lwext4/ext4.c#ext4_fread` | 同板块同名 | ext4 文件读取标准流程，调用 `ext4_block_readbytes`、`ext4_blocks_get_direct` 等。目标项目额外调用 `Log`。 |
| 第三方库/公共依赖 | `kernel/fs/ext4_extent.c#ext4_ext_split_node` | `Kernel/File/lwext4/ext4_extent.c#ext4_ext_split_node` | 同板块底层逻辑一致 | Extent 树节点分裂必须区分 index/extent，使用 `EXT_FIRST_*`、`EXT_MAX_*` 宏。 |
| 第三方库/公共依赖 | `kernel/fs/ext4_xattr.c#ext4_xattr_set_entry` | `Kernel/File/lwext4/ext4_xattr.c#ext4_xattr_set_entry` | 同板块底层逻辑一致 | xattr 项设置必然使用 `EXT4_XATTR_*` 头宏及 `memcpy/memmove/memset`。 |
| 第三方库/公共依赖 | `kernel/fs/ext4_extent.c#ext4_extent_remove_space` | `Kernel/File/lwext4/ext4_extent.c#ext4_extent_remove_space` | 同板块底层逻辑一致 | 删除 extent 空间必须遍历 `EXT_FIRST/EXT_LAST_EXTENT` 并使用 `IN_RANGE` 判断范围。 |
| 第三方库/公共依赖 | `kernel/fs/ext4_fs.c#ext4_fs_free_inode` | `Kernel/File/lwext4/ext4_fs.c#ext4_fs_free_inode` | 同板块底层逻辑一致 | 释放 inode 必须释放数据块、inode 位图、处理 ACL，调用 `ext4_ialloc_free_inode` 等固定步骤。 |
| 第三方库/公共依赖 | `kernel/fs/ext4_dir_idx.c#ext4_dir_dx_init` | `Kernel/File/lwext4/ext4_dir_idx.c#ext4_dir_dx_init` | 同板块底层逻辑一致 | 初始化 dx 根节点必须设置 hash 版本、间接层数、limit/count 等字段。 |
| 第三方库/公共依赖 | `kernel/fs/ext4.c#ext4_generic_open2` | `Kernel/File/lwext4/ext4.c#ext4_generic_open2` | 同板块底层逻辑一致 | 打开文件必须查找目录项、分配/获取 inode、校验模式，调用链由 VFS-open 语义决定。 |
| 第三方库/公共依赖 | `kernel/fs/ext4_xattr.c#ext4_xattr_list` | `Kernel/File/lwext4/ext4_xattr.c#ext4_xattr_list` | 同板块底层逻辑一致 | 列出 xattr 必须遍历 block/inline 项，使用 `EXT4_XATTR_BFIRST/IFIRST/IHDR/IS_LAST_ENTRY`。 |
| 第三方库/公共依赖 | `kernel/fs/ext4_ialloc.c#ext4_ialloc_alloc_inode` | `Kernel/File/lwext4/ext4_ialloc.c#ext4_ialloc_alloc_inode` | 同板块同名同结构 | inode 分配标准流程，调用 `ext4_bg_get_free_inodes_count`、`ext4_bg_get_inode_bitmap` 等。目标项目无调试宏。 |
| 第三方库/公共依赖 | `kernel/fs/ext4_dir_idx.c#ext4_dir_dx_add_entry` | `Kernel/File/lwext4/ext4_dir_idx.c#ext4_dir_dx_add_entry` | 同板块同名同结构 | 目录索引添加条目，调用 `ext4_dir_dx_get_leaf`、`ext4_dir_dx_split_data` 等。目标项目使用 `ext4_dbg`。 |
| 第三方库/公共依赖 | `kernel/fs/ext4_balloc.c#ext4_balloc_free_blocks` | `Kernel/File/lwext4/ext4_balloc.c#ext4_balloc_free_blocks` | 同板块同名同结构 | 块释放标准流程，调用 `ext4_balloc_set_bitmap_csum`、`ext4_bcache_invalidate_lba` 等。目标项目无调试宏。 |
| 第三方库/公共依赖 | `kernel/fs/ext4_fs.c#ext4_fs_init_block_bitmap` | `Kernel/File/lwext4/ext4_fs.c#ext4_fs_init_block_bitmap` | 同板块底层逻辑一致 | 初始化块位图必须按块组遍历，设置位图、校验和、保留块。 |
| 第三方库/公共依赖 | `kernel/fs/ext4_xattr.c#ext4_xattr_block_set` | `Kernel/File/lwext4/ext4_xattr.c#ext4_xattr_block_set` | 同板块底层逻辑一致 | 块级 xattr 更新必须通过事务获取块、查找条目、设置脏位。 |
| 第三方库/公共依赖 | `kernel/fs/ext4_extent.c#ext4_extent_get_blocks` | `Kernel/File/lwext4/ext4_extent.c#ext4_extent_get_blocks` | 同板块底层逻辑一致 | 块映射必须查找/插入/转换 extent，调用 `ext4_ext_insert_extent` 等。 |
| 第三方库/公共依赖 | `kernel/fs/ext4_dir_idx.c#ext4_dir_dx_find_entry` | `Kernel/File/lwext4/ext4_dir_idx.c#ext4_dir_dx_find_entry` | 同板块同名同结构 | 目录索引查找，调用 `ext4_dir_dx_get_leaf`、`ext4_dir_find_in_block` 等。目标项目使用 `ext4_dbg`。 |
| 第三方库/公共依赖 | `kernel/fs/ext4_extent.c#ext4_ext_remove_leaf` | `Kernel/File/lwext4/ext4_extent.c#ext4_ext_remove_leaf` | 同板块底层逻辑一致 | 叶子节点删除必须修正索引、标记未初始化、释放块。 |
| 第三方库/公共依赖 | `kernel/fs/ext4_mkfs.c#fill_sb` | `Kernel/File/lwext4/ext4_mkfs.c#fill_sb` | 同板块底层逻辑一致 | 填充超级块必然调用 `ext4_sb_set_blocks_cnt`、字节序转换 `to_le16/32`、内存拷贝。 |
| 第三方库/公共依赖 | `kernel/fs/ext4_journal.c#__jbd_journal_commit_trans` | `Kernel/File/lwext4/ext4_journal.c#__jbd_journal_commit_trans` | 同板块底层逻辑一致 | 事务提交必须操作红黑树、TAILQ、bcache，调用序列由 JBD 算法决定。 |
| 第三方库/公共依赖 | `kernel/fs/ext4_fs.c#ext4_fs_get_inode_dblk_idx_internal` | `Kernel/File/lwext4/ext4_fs.c#ext4_fs_get_inode_dblk_idx_internal` | 同板块底层逻辑一致 | 获取 inode 数据块索引必须读取直接/间接块，支持 extent 时回退到 `ext4_extent_get_blocks`。 |
| 第三方库/公共依赖 | `kernel/fs/ext4_dir.c#ext4_dir_add_entry` | `Kernel/File/lwext4/ext4_dir.c#ext4_dir_add_entry` | 同板块同名同结构 | 目录添加条目，调用 `ext4_dir_try_insert_entry`、`ext4_dir_dx_add_entry` 等。目标项目使用 `ext4_dbg`。 |
| 第三方库/公共依赖 | `kernel/fs/ext4_dir_idx.c#ext4_dir_dx_get_leaf` | `Kernel/File/lwext4/ext4_dir_idx.c#ext4_dir_dx_get_leaf` | 同板块同名同结构 | 获取目录索引叶子节点，调用 `ext4_dir_dx_climit_get_count` 等。目标项目使用 `ext4_dbg`。 |
| 第三方库/公共依赖 | `kernel/fs/ext4_fs.c#ext4_fs_release_inode_block` | `Kernel/File/lwext4/ext4_fs.c#ext4_fs_release_inode_block` | 同板块底层逻辑一致 | 释放 inode 数据块必须释放直接/间接块并清空指针。 |
| 第三方库/公共依赖 | `kernel/fs/ext4_mkfs.c#write_bgroups` | `Kernel/File/lwext4/ext4_mkfs.c#write_bgroups` | 同板块底层逻辑一致 | 写块组描述符必须设置各类位图、计数、标志，调用 `ext4_bg_set_*` 固定 API。 |
| 第三方库/公共依赖 | `kernel/fs/ext4_xattr.c#ext4_xattr_remove` | `Kernel/File/lwext4/ext4_xattr.c#ext4_xattr_remove` | 同板块底层逻辑一致 | 删除 xattr 必须查找条目、复制新块、更新 inode ACL。 |
| 第三方库/公共依赖 | `kernel/fs/ext4_mkfs.c#create_fs_aux_info` | `Kernel/File/lwext4/ext4_mkfs.c#create_fs_aux_info` | 同板块同名同结构 | mkfs 辅助信息创建，调用 `EXT4_DIV_ROUND_UP`、`ext4_calloc`、`has_superblock`。目标项目使用 `ext4_dbg`。 |
| 第三方库/公共依赖 | `kernel/fs/ext4_extent.c#ext4_ext_insert_leaf` | `Kernel/File/lwext4/ext4_extent.c#ext4_ext_insert_leaf` | 同板块底层逻辑一致 | 插入叶子必须判断 append/prepend、修正索引、dirty 节点。 |
| 第三方库/公共依赖 | `kernel/fs/ext4_fs.c#ext4_fs_truncate_inode` | `Kernel/File/lwext4/ext4_fs.c#ext4_fs_truncate_inode` | 同板块底层逻辑一致 | 截断必须移除 extent 空间、释放间接块、更新 inode 大小。 |

**表格说明**: 全部 36 条证据均属于“第三方库/公共依赖”板块。证据类型中“同板块同名同结构”与“同板块底层逻辑一致”反映的是 ext4 规范与 lwext4 公共 API 的必然约束，非源代码行级复制证据。

---

## 4. 核心代码调用流对比剖析

### 【第三方库/公共依赖 — ext4 块分配路径】

**底层技术析理**: 双方 `ext4_balloc_alloc_block` 函数均遵循 ext4 块分配规范：通过 `ext4_balloc_get_bgid_of_block` 定位块组，通过 `ext4_bg_get_block_bitmap` 获取位图，通过 `ext4_balloc_set_bitmap_csum` / `ext4_balloc_verify_bitmap_csum` 操作校验和。该调用序列由 ext4 on-disk 格式强制规定，任何正确实现均需遵循。目标项目使用 `Log` 进行调试输出，参考项目使用 `EXT4_Debug_u32`，反映两者集成到不同日志框架的适配差异。

**代码溯源链接**:
- 目标项目: `kernel/fs/ext4_balloc.c#ext4_balloc_alloc_block`
- 参考项目: `Kernel/File/lwext4/ext4_balloc.c#ext4_balloc_alloc_block`

---

### 【第三方库/公共依赖 — ext4 文件写入路径】

**底层技术析理**: 双方 `ext4_fwrite` 函数均遵循 ext4 写入流程：`EXT4_MP_LOCK` 加锁，`ext4_block_writebytes` 写入数据，`ext4_blocks_set_direct` 设置直接块映射，`ext4_fs_append_inode_dblk` 追加数据块，`ext4_block_cache_write_back` 写回缓存。目标项目额外调用 `Log` 进行日志记录，且外部调用数量存在差异（目标 +9/+11 vs 参考 +8/+10），说明内部实现存在差异。

**代码溯源链接**:
- 目标项目: `kernel/fs/ext4.c#ext4_fwrite`
- 参考项目: `Kernel/File/lwext4/ext4.c#ext4_fwrite`

---

### 【第三方库/公共依赖 — JBD 日志准备路径】

**底层技术析理**: 双方 `jbd_journal_prepare` 函数均操作 JBD 日志标准数据结构：`RB_FIND` 在红黑树中查找事务，`TAILQ_FOREACH_*` 遍历描述符队列，`ext4_bcache_clear_dirty` / `ext4_bcache_set_dirty` 管理块缓存脏标志。该调用序列由 JBD 算法规范决定，调用图谱完全一致是领域必然性，非代码复制证据。

**代码溯源链接**:
- 目标项目: `kernel/fs/ext4_journal.c#jbd_journal_prepare`
- 参考项目: `Kernel/File/lwext4/ext4_journal.c#jbd_journal_prepare`

---

### 【进程管理 — 仅目标项目存在】

**底层技术析理**: 目标项目 `proc_exit` 函数实现完整的进程退出逻辑：`acquire` 获取锁，`fileclose` 关闭文件，`freeprocvm` 释放虚拟内存，`memset` 清零结构，`myproc` 获取当前进程，`panic` 异常处理。参考项目无任何进程管理模块，该调用流体现目标项目独立的内核架构设计。

**代码溯源链接**:
- 目标项目: `kernel/sched/proc.c#proc_exit`
- 参考项目: 无对应模块

---

### 【内存管理 — 仅目标项目存在】

**底层技术析理**: 目标项目 `execve` 函数实现完整的 exec 系统调用：`PGROUNDDOWN`/`PGROUNDUP` 页对齐，`copyout` 拷贝数据到用户空间，`ADD_AUXV` 添加辅助向量，`Log`/`Warn` 日志记录。该调用流体现目标项目独立的内存管理与进程创建机制，参考项目无任何对应模块。

**代码溯源链接**:
- 目标项目: `kernel/fs/exec.c#execve`
- 参考项目: 无对应模块

---

### 【设备驱动 — 仅目标项目存在】

**底层技术析理**: 目标项目 `ahci_exec_ata_cmd` 函数实现 AHCI SATA 命令执行：`ahci_get_cmd_slot` 获取命令槽，`ahci_fill_cmd_slot` 填充命令，`ahci_fill_sg` 填充散聚表，`ahci_readl`/`ahci_writel` 操作寄存器。该调用流体现目标项目对 LoongArch 平台的独立驱动开发，参考项目无任何设备驱动模块。

**代码溯源链接**:
- 目标项目: `kernel/arch/loongarch/drv_ahci.c#ahci_exec_ata_cmd`
- 参考项目: 无对应模块

---

## 5. 单方模块与降权项

### 仅目标项目存在的模块（强原创证据）

| 模块 | 函数数 | 代表性函数 | 原创性说明 |
| :--- | ---: | :--- | :--- |
| 进程管理 | 72 | `proc_exit`、`do_sigtimedwait`、`do_sigprocmask` | 完整进程生命周期、信号处理，与参考项目零重合 |
| 任务调度/异步运行时 | 67 | `signal_handle`、`vsnprintf`、`generic_utimensat` | 调度与运行时支持，与参考项目零重合 |
| 内存管理 | 209 | `execve`、`do_clone`、`virtio_disk_rw_multiple` | 内存分配、页表管理、进程创建，与参考项目零重合 |
| 中断/异常处理 | 15 | `ahci_host_init`、`pci_device_init`、`plicinithart` | LoongArch/QEMU 平台中断处理，与参考项目零重合 |
| 系统调用 | 15 | `sys_munmap`、`sys_mprotect`、`sys_poweroff` | 系统调用入口实现，与参考项目零重合 |
| 文件系统（原创） | 151 | `get_absolute_path`、`sys_mount`、`init.c#main` | VFS 层、路径解析、用户态 init，与参考项目零重合 |
| 设备驱动 | 61 | `ahci_exec_ata_cmd`、`ahci_fill_sg` | AHCI SATA 驱动，与参考项目零重合 |
| 通用模块/其他 | 139 | `qsort`、`futex_wake`、`print_node` | 算法库、futex、设备树解析，与参考项目零重合 |

### 第三方库/公共依赖降权说明

双方唯一重合板块“文件系统/第三方库”中，目标项目 519 个函数与参考项目 478 个函数均基于开源 `lwext4` 库。375 个共同 AST 指纹对应的是 `lwext4` 公共 API（如 `ext4_bg_get_block_bitmap`、`ext4_ext_dirty`、`EXT4_XATTR_IS_LAST_ENTRY` 等），这些函数名与调用序列由 ext4 文件系统规范与 `lwext4` 库接口强制约束，不能作为跨项目抄袭的强证据。

目标项目使用 `Log`/`ext4_dbg` 替代参考项目的 `EXT4_Debug*` 系列宏，反映的是集成到不同日志框架的正常适配，非“机械全局替换”。目标项目在 `ext4_mkfs` 中额外调用 `ext4_bcache_fini_dynamic`，在 `ext4_balloc_free_blocks` 中删除调试宏，说明存在独立修改。

---

## 6. 保守结论

目标项目 `[2026-T2026105749910760-XX]` 与参考项目 `[2024-undefinedType-南京航空航天大学]` 的整体 AST 相似度为 34.37%，该相似度**完全集中于“文件系统/第三方库”单一板块**，源于双方均集成了开源 `lwext4` 库。目标项目在进程管理（72 函数）、内存管理（209 函数）、设备驱动（61 函数）、系统调用（15 函数）等 7 个核心子系统上与参考项目零重合，独立 AST 指纹占比 64.89%，原创性显著。参考项目覆盖率 94.22% 说明其本身即为 `lwext4` 封装，任何集成该库的项目均会与之产生高覆盖率。控方所列 36 个函数的调用图谱相似性可归因于 ext4 规范约束与 `lwext4` 公共 API，不构成对参考项目的代码抄袭。**综合裁定：独立开发，第三方库集成重合不构成违规。**

------------------------------

