# 🛡️ OS 内核赛自动化代码级架构审计报告

**审计时间**: `2026-07-02 13:11:08`
**参赛作品名称**: `2026-T2026105749910760-XX`
**年份**: `2026`
**学校**: `XX`
**队伍名称**: `T2026105749910760`
**比赛名字**: `OS-Competition`
**比赛子赛道**: `Kernel-Implementation`
**仓库地址**: `https://gitlab.eduxiji.net/educg-group-43501-3132633/T2026105749910760-3579`
**推测开发语言**: `Rust / C (由AST探针自动识别)`
**特征函数量**: `1248`

## ⚔️ 底层代码同源性对比 (AST Homology)
> 本部分由专门的代码审查智能体，对该项目与符合年份条件的历届最高相似度项目进行控制流与底层调用对比。新版报告会先给出确定性的全模块覆盖矩阵，再给出 Agent 的分模块解释，避免只围绕少量抽样函数展开。

### 🏆 Top 1 底层对标: vs `2025-reXvapor-吉林大学`（吉林大学 / reXvapor）

#### 对方作品信息

**对方年份**: `2025`
**对方学校**: `吉林大学`
**对方队伍名称**: `reXvapor`
**对方比赛名字**: `OS-Competition`
**对方比赛子赛道**: `Kernel-Implementation`
**对方仓库地址**: `https://gitlab.eduxiji.net/educg-group-36002-2710490/T202510183995907-273`

> 双向覆盖率：共同 AST 指纹: 906；唯一 AST 指纹: 目标项目 1068，参考项目 966；覆盖率: 目标项目 84.83%，参考项目 93.79%

## 全模块对比总览（确定性证据）

> 总体覆盖率：共同 AST 指纹: 906；唯一 AST 指纹: 目标项目 1068，参考项目 966；覆盖率: 目标项目 84.83%，参考项目 93.79%

### 1. 模块覆盖矩阵
| 模块 | 目标函数数 | 参考函数数 | 共同唯一 AST 指纹 | Target 覆盖 | Base 覆盖 | 状态 |
| :--- | ---: | ---: | ---: | ---: | ---: | :--- |
| 进程管理 | 72 | 62 | 47 | 68.12% | 87.04% | 双方存在且有结构重合 |
| 任务调度/异步运行时 | 67 | 70 | 61 | 93.85% | 89.71% | 双方存在且有结构重合 |
| 内存管理 | 209 | 190 | 158 | 81.03% | 89.77% | 双方存在且有结构重合 |
| 中断/异常处理 | 15 | 15 | 14 | 93.33% | 93.33% | 双方存在且有结构重合 |
| 系统调用 | 15 | 16 | 10 | 100.0% | 90.91% | 双方存在且有结构重合 |
| 文件系统 | 151 | 147 | 112 | 88.19% | 91.06% | 双方存在且有结构重合 |
| 设备驱动 | 61 | 38 | 29 | 50.88% | 87.88% | 双方存在且有结构重合 |
| 网络协议栈 | 0 | 0 | 0 | 0.0% | 0.0% | 双方均未体现 |
| 文件系统/第三方库 | 519 | 519 | 432 | 99.77% | 99.77% | 双方存在且有结构重合 |
| 通用模块/其他 | 139 | 89 | 67 | 54.03% | 85.9% | 双方存在且有结构重合 |

### 2. 分模块证据清单

#### 进程管理
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `kernel/ipc/signal.c#do_sigtimedwait` | `kernel/ipc/signal.c#do_sigtimedwait` | 同板块同名同结构 | Warn, acquire, holding, mythread, release, sig_existed ...(+4) | Warn, acquire, holding, mythread, release, sig_existed ...(+4) |
| `kernel/ipc/signal.c#do_sigprocmask` | `kernel/ipc/signal.c#do_sigprocmask` | 同板块同名同结构 | Log, Warn, acquire, mythread, release, sig_del_set_mask ...(+1) | Log, Warn, acquire, mythread, release, sig_del_set_mask ...(+1) |
| `kernel/sched/proc.c#proc_exit` | `kernel/sched/proc.c#proc_exit` | 同板块同名同结构 | acquire, fileclose, freeprocvm, memset, myproc, panic ...(+6) | acquire, fileclose, freeprocvm, memset, myproc, panic ...(+6) |
| `kernel/sysproc.c#sys_setpgid` | `kernel/sysproc.c#sys_setpgid` | 同板块同名 | acquire, argint, myproc, release | acquire, argint, myproc, release |

#### 任务调度/异步运行时
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `kernel/lib/snprintf.c#vsnprintf` | `kernel/lib/snprintf.c#vsnprintf` | 同板块同名同结构 | is_digit, number, skip_atoi, strnlen, va_arg, vsnprintf_writechar | is_digit, number, skip_atoi, strnlen, va_arg, vsnprintf_writechar |
| `kernel/lib/snprintf.c#vsprintf` | `kernel/lib/snprintf.c#vsprintf` | 同板块同名同结构 | is_digit, number, skip_atoi, strnlen, va_arg | is_digit, number, skip_atoi, strnlen, va_arg |
| `kernel/ipc/signal.c#signal_handle` | `kernel/ipc/signal.c#signal_handle` | 同板块同名 | Log, Warn, acquire, do_handle_signal, kfree, list_del_reinit ...(+6) | Log, Warn, acquire, do_handle_signal, kfree, list_del_reinit ...(+7) |
| `kernel/fs/sysfile.c#generic_utimensat` | `kernel/fs/sysfile.c#generic_utimensat` | 同板块同名 | Log, file_exist, fs->fsops->utimens, generic_fstat, get_abpath_from_dirfd, printf ...(+1) | Log, file_exist, fs->fsops->utimens, get_abpath_from_dirfd, printf, vfs_resolve_fs |

#### 内存管理
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `kernel/fs/exec.c#execve` | `kernel/fs/exec.c#execve` | 同板块同名同结构 | ADD_AUXV, Log, PGROUNDDOWN, PGROUNDUP, Warn, copyout ...(+19) | ADD_AUXV, Log, PGROUNDDOWN, PGROUNDUP, Warn, copyout ...(+19) |
| `kernel/sched/proc.c#do_clone` | `kernel/sched/proc.c#do_clone` | 同板块同名同结构 | Log, acquire, alloc_thread, append_child, atomic_add_return, copyout ...(+14) | Log, acquire, alloc_thread, append_child, atomic_add_return, copyout ...(+14) |
| `kernel/fs/virtio_disk.c#virtio_disk_init` | `kernel/fs/virtio_disk.c#virtio_disk_init` | 同板块同名同结构 | R, initlock, memset, panic | R, initlock, memset, panic |
| `kernel/fs/virtio_disk.c#virtio_disk_rw_multiple` | `kernel/fs/virtio_disk.c#virtio_disk_rw_multiple` | 同板块同名同结构 | R, __sync_synchronize, acquire, alloc_descs, free_chain, release ...(+1) | R, __sync_synchronize, acquire, alloc_descs, free_chain, release ...(+1) |

#### 中断/异常处理
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `kernel/arch/loongarch/pci.c#pci_device_dump` | `kernel/arch/loongarch/pci.c#pci_device_dump` | 同板块同名同结构 | pci_device_bar_dump, printf | pci_device_bar_dump, printf |
| `kernel/arch/loongarch/pci.c#pci_device_init` | `kernel/arch/loongarch/pci.c#pci_device_init` | 同板块同名同结构 | 内部闭环逻辑 | 内部闭环逻辑 |
| `kernel/arch/qemu/plic.c#plicinithart` | `kernel/arch/qemu/plic.c#plicinithart` | 同板块同名同结构 | PLIC_SENABLE, PLIC_SPRIORITY, cpuid | PLIC_SENABLE, PLIC_SPRIORITY, cpuid |
| `kernel/arch/loongarch/extioi.c#extioi_init` | `kernel/arch/loongarch/extioi.c#extioi_init` | 同板块同名同结构 | iocsr_writeq | iocsr_writeq |

#### 系统调用
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `kernel/mm/mmap.c#sys_munmap` | `kernel/mm/mmap.c#sys_munmap` | 同板块同名同结构 | Log, argaddr, argint, do_munmap | Log, argaddr, argint, do_munmap |
| `kernel/mm/mmap.c#sys_mprotect` | `kernel/mm/mmap.c#sys_mprotect` | 同板块同名同结构 | Log, argaddr, argint, do_mprotect | Log, argaddr, argint, do_mprotect |
| `kernel/sysother.c#sys_poweroff` | `kernel/sysother.c#sys_poweroff` | 同板块同名同结构 | argint, opensbi_poweroff, qemu_raw_poweroff | argint, opensbi_poweroff, qemu_raw_poweroff |
| `kernel/syscall.c#argstr` | `kernel/syscall.c#argstr` | 同板块同名同结构 | argaddr, fetchstr | argaddr, fetchstr |

#### 文件系统
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `kernel/fs/vfs/vfs.c#get_absolute_path` | `kernel/fs/vfs/vfs.c#get_absolute_path` | 同板块同名同结构 | strcat, strcpy, strlen, strncpy | strcat, strcpy, strlen, strncpy |
| `kernel/fs/sysfile.c#sys_mount` | `kernel/fs/sysfile.c#sys_mount` | 同板块同名同结构 | 内部闭环逻辑 | 内部闭环逻辑 |
| `kernel/fs/sysfile.c#sys_renameat2` | `kernel/fs/sysfile.c#sys_renameat2` | 同板块同名 | Log, S_ISDIR, argint, argstr, fs->fsops->rename, generic_fstat ...(+5) | Log, argint, argstr, fs->fsops->rename, get_abpath_from_dirfd, printf ...(+1) |
| `kernel/fs/vfs/vfs.c#iget` | `kernel/fs/vfs/vfs.c#iget` | 同板块同名同结构 | acquire, fill_inode, get_vfs_entry, mnt_rtinode, panic, release | acquire, fill_inode, get_vfs_entry, mnt_rtinode, panic, release |

#### 设备驱动
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `kernel/arch/loongarch/ns16550a.c#real_serial_ns16550a_init` | `kernel/arch/loongarch/ns16550a.c#real_serial_ns16550a_init` | 同板块同名同结构 | inb, outb | inb, outb |
| `kernel/arch/loongarch/pci.c#pci_device_bar_init` | `kernel/arch/loongarch/pci.c#pci_device_bar_init` | 同板块同名同结构 | 内部闭环逻辑 | 内部闭环逻辑 |
| `kernel/arch/loongarch/pci.c#pci_enable_bus_mastering` | `kernel/arch/loongarch/pci.c#pci_enable_bus_mastering` | 同板块同名同结构 | pci_read_config, pci_write_config, printf | pci_read_config, pci_write_config, printf |
| `kernel/arch/qemu/uart.c#uartinit` | `kernel/arch/qemu/uart.c#uartinit` | 同板块同名同结构 | WriteReg, initlock | WriteReg, initlock |

#### 文件系统/第三方库
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `kernel/fs/ext4_balloc.c#ext4_balloc_alloc_block` | `kernel/fs/ext4_balloc.c#ext4_balloc_alloc_block` | 同板块同名同结构 | ext4_balloc_get_bgid_of_block, ext4_balloc_get_block_of_bgid, ext4_balloc_set_bitmap_csum, ext4_balloc_verify_bitmap_csum, ext4_bg_get_block_bitmap, ext4_bg_get_free_blocks_count ...(+19) | ext4_balloc_get_bgid_of_block, ext4_balloc_get_block_of_bgid, ext4_balloc_set_bitmap_csum, ext4_balloc_verify_bitmap_csum, ext4_bg_get_block_bitmap, ext4_bg_get_free_blocks_count ...(+19) |
| `kernel/fs/ext4.c#ext4_fwrite` | `kernel/fs/ext4.c#ext4_fwrite` | 同板块同名同结构 | EXT4_MP_LOCK, EXT4_MP_UNLOCK, Log, ext4_assert, ext4_block_cache_write_back, ext4_block_writebytes ...(+11) | EXT4_MP_LOCK, EXT4_MP_UNLOCK, Log, ext4_assert, ext4_block_cache_write_back, ext4_block_writebytes ...(+11) |
| `kernel/fs/ext4_journal.c#jbd_journal_prepare` | `kernel/fs/ext4_journal.c#jbd_journal_prepare` | 同板块同名同结构 | EXT4_BLOCK_ZERO, RB_FIND, TAILQ_FOREACH_REVERSE_SAFE, TAILQ_FOREACH_SAFE, TAILQ_REMOVE, ext4_bcache_clear_dirty ...(+17) | EXT4_BLOCK_ZERO, RB_FIND, TAILQ_FOREACH_REVERSE_SAFE, TAILQ_FOREACH_SAFE, TAILQ_REMOVE, ext4_bcache_clear_dirty ...(+17) |
| `kernel/fs/ext4.c#ext4_dir_rm` | `kernel/fs/ext4.c#ext4_dir_rm` | 同板块同名同结构 | EXT4_MP_LOCK, EXT4_MP_UNLOCK, ext4_block_cache_write_back, ext4_dir_en_get_inode, ext4_dir_iterator_fini, ext4_dir_iterator_init ...(+19) | EXT4_MP_LOCK, EXT4_MP_UNLOCK, ext4_block_cache_write_back, ext4_dir_en_get_inode, ext4_dir_iterator_fini, ext4_dir_iterator_init ...(+19) |

#### 通用模块/其他
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `kernel/lib/qsort.c#qsort` | `kernel/lib/qsort.c#qsort` | 同板块同名同结构 | (*cmp), POP, PUSH, SWAP, min_t | (*cmp), POP, PUSH, SWAP, min_t |
| `kernel/lib/snprintf.c#number` | `kernel/lib/snprintf.c#number` | 同板块同名同结构 | do_div | do_div |
| `kernel/ipc/futex.c#futex_wake` | `kernel/ipc/futex.c#futex_wake` | 同板块同名 | ASSERT, Log, Warn, acquire, futex_free, get_futex ...(+5) | ASSERT, Log, Warn, acquire, futex_free, get_futex ...(+6) |
| `user/lib/printf.c#vprintf` | `user/lib/printf.c#vprintf` | 同板块同名同结构 | printint, printptr, putc, va_arg | printint, printptr, putc, va_arg |


好的，审计官。基于提供的证据底座、双方陈述及AST抽样数据，现出具最终同源性定性仲裁报告。

---

#### 1. 宏观判定
- **定性结论**: **部分逻辑借鉴，存在架构改动** (相似度: 80.32%，共同 AST 指纹: 906；唯一 AST 指纹: 目标项目 1068，参考项目 966；覆盖率: 目标项目 84.83%，参考项目 93.79%)
- **判定依据**: 目标项目与参考项目在AST层面存在高度结构重合，尤其在进程管理、任务调度、内存管理等核心模块，大量函数的底层调用图谱完全一致，远超独立开发可能出现的偶然相似。然而，目标项目拥有更多的唯一AST指纹（1068 vs 966），且在`procfs`等子系统中表现出与参考项目不同的架构设计（如统一分发层），表明其并非全盘照搬，而是在参考项目基础上进行了二次开发、重构和功能扩展。证据抽取环节的失败（401 Unauthorized）导致部分自动化证据缺失，但现有手动标注的调用图谱已足以支撑“部分逻辑借鉴”的判定。

#### 2. 全模块覆盖矩阵解读
- **进程管理**: 双方均存在。共同AST指纹47个，目标覆盖率68.12%，参考覆盖率87.04%。参考覆盖率显著高于目标，说明目标项目在进程管理模块有更多独立扩展的函数。但核心函数调用图谱高度一致，证据强度为**强**。
- **任务调度/异步运行时**: 双方均存在。共同AST指纹61个，目标覆盖率93.85%，参考覆盖率89.71%。覆盖率极高且双向均衡，表明调度器核心逻辑高度同源，证据强度为**强**。
- **内存管理**: 双方均存在。共同AST指纹158个，目标覆盖率81.03%，参考覆盖率89.77%。与进程管理类似，参考覆盖率更高，目标有独立实现。但核心分配与映射逻辑重合度高，证据强度为**强**。
- **中断/异常处理**: 双方均存在。共同AST指纹14个，目标覆盖率93.33%，参考覆盖率93.33%。覆盖率完全一致且极高，说明此底层架构相关代码几乎完全同源，证据强度为**强**。
- **系统调用**: 双方均存在。共同AST指纹10个，目标覆盖率100.0%，参考覆盖率90.91%。目标项目所有系统调用入口在参考项目中均有迹可循，复用痕迹明显，证据强度为**强**。
- **文件系统**: 双方均存在。共同AST指纹112个，目标覆盖率88.19%，参考覆盖率91.06%。核心VFS与Ext4逻辑高度重合，证据强度为**强**。
- **设备驱动**: 双方均存在。共同AST指纹29个，目标覆盖率50.88%，参考覆盖率87.88%。目标覆盖率远低于参考，说明目标项目移植或新增了大量独立驱动代码，证据强度为**弱**。
- **网络协议栈**: 双方均未体现，证据强度为**未形成**。
- **第三方库/公共依赖**: 双方均存在。共同AST指纹432个，覆盖率均为99.77%。这证实双方引用了完全相同的第三方库（如lwext4），此部分不构成抄袭证据，但解释了整体高相似度的部分原因，证据强度为**未形成（公共代码）**。

#### 3. 底层 AST 关键证据指纹表
| 所属板块 | 目标项目路径 | 参考项目路径 | 证据类型 | 技术说明 |
| :--- | :--- | :--- | :--- | :--- |
| 进程管理 | `kernel/ipc/signal.c#do_sigtimedwait` | `kernel/ipc/signal.c#do_sigtimedwait` | 调用图谱完全一致 | 信号等待的完整逻辑链，包括锁获取、信号集检查、线程休眠等非标准宏调用序列完全一致。 |
| 进程管理 | `kernel/sched/proc.c#proc_exit` | `kernel/sched/proc.c#proc_exit` | 调用图谱完全一致 | 进程退出的复杂流程，涉及文件关闭、内存释放、状态迁移、父进程唤醒等，调用序列和隐含节点(+4)均匹配。 |
| 进程管理 | `kernel/sched/proc.c#procinit` | `kernel/sched/proc.c#procinit` | 调用图谱完全一致 | 进程子系统初始化，包含特定的PCB队列初始化宏`PCB_Q_ALL_INIT`和原子操作设置，序列一致。 |
| 任务调度/异步运行时 | `kernel/atomic/cond.c#cond_wait` | `kernel/atomic/cond.c#cond_wait` | 调用图谱完全一致 | 条件变量等待的标准模式，但其中`panic`、`tcb_q_change_state`等特定辅助函数的调用序列完全匹配，构成强证据。 |
| 内存管理 | `kernel/fs/exec.c#execve` | `kernel/fs/exec.c#execve` | 调用图谱完全一致 | `execve`系统调用的核心实现，涉及页对齐、辅助向量构建、内存拷贝等，调用图谱(+19)完全一致。 |
| 内存管理 | `kernel/sched/proc.c#do_clone` | `kernel/sched/proc.c#do_clone` | 调用图谱完全一致 | `clone`系统调用的底层实现，包含线程分配、父子关系维护、内存拷贝等复杂流程，调用图谱(+14)完全一致。 |
| 系统调用 | `kernel/sysproc.c#sys_sleep` | `kernel/sysproc.c#sys_sleep` | 调用图谱完全一致 | `sleep`系统调用实现，包含锁管理、进程/线程状态检查与切换的特定调用序列完全一致。 |
| 文件系统 | `kernel/fs/vfs/vfs.c#iget` | `kernel/fs/vfs/vfs.c#iget` | 调用图谱完全一致 | VFS层的核心inode获取函数，包含锁、填充、挂载点查找等特定调用序列完全一致。 |
| 第三方库/公共依赖 | `kernel/fs/ext4_balloc.c#ext4_balloc_alloc_block` | `kernel/fs/ext4_balloc.c#ext4_balloc_alloc_block` | 同源第三方库 | 双方均使用相同的ext4文件系统库，内部调用图谱(+19)完全一致，属于公共依赖，不构成抄袭。 |

#### 4. 核心代码调用流对比剖析
* **【进程管理/信号处理路径】**
* **底层技术析理**: 两者均采用“获取当前线程 -> 加锁 -> 操作信号集 -> 解锁”的同步模型。在`do_sigprocmask`中，调用流均为`Log/Warn -> acquire -> mythread -> sig_del_set_mask/sig_gen_mask -> release`，表明其信号集操作函数命名和加锁粒度完全同源。
* **代码溯源链接**:
  - 目标项目: `kernel/ipc/signal.c#do_sigprocmask`
  - 参考项目: `kernel/ipc/signal.c#do_sigprocmask`

* **【任务调度/进程生命周期路径】**
* **底层技术析理**: 在`proc_exit`中，两者均遵循“获取当前进程 -> 加锁 -> 清理文件描述符 -> 释放虚拟内存 -> 唤醒父进程 -> 改变自身状态 -> 解锁 -> 调度”的严格顺序。调用流`acquire -> fileclose -> freeprocvm -> ... -> pcb_q_change_state -> release`完全一致，揭示了相同的进程控制块管理模型和资源回收策略。
* **代码溯源链接**:
  - 目标项目: `kernel/sched/proc.c#proc_exit`
  - 参考项目: `kernel/sched/proc.c#proc_exit`

* **【内存管理/用户空间内存分配路径】**
* **底层技术析理**: `sys_brk`系统调用实现中，两者均通过`argaddr`解析参数，调用`growproc`调整进程内存空间，并使用`myproc`获取当前进程上下文。调用流`Log/Warn -> argaddr -> growproc -> myproc`完全一致，表明其进程地址空间管理模型和系统调用参数传递机制同源。
* **代码溯源链接**:
  - 目标项目: `kernel/sysproc.c#sys_brk`
  - 参考项目: `kernel/sysproc.c#sys_brk`

#### 5. 单方模块与降权项
- **第三方库/公共依赖**: `kernel/fs/ext4*.c`等519个函数被明确识别为第三方库，双方覆盖率99.77%。此部分为公共代码，**不构成同源证据**，在判定中已予排除。
- **设备驱动模块**: 目标项目（61个函数）与参考项目（38个函数）规模差异显著，目标覆盖率仅50.88%。这表明目标项目独立开发或移植了大量新驱动，该模块的相似性主要源于硬件操作的标准接口（如PCI配置空间读写），**证据强度弱**。
- **procfs子系统**: 目标项目采用`procfs_lookup`统一分发，而参考项目直接调用`proc_interrupts_*`。此架构差异是目标项目独立设计的体现，**不能作为抄袭证据**，反而证明了二次开发的深度。
- **泛型函数名与公共接口**: `sys_*`、`do_*`、`acquire`、`release`等命名是操作系统内核的通用惯例，`argint`、`argaddr`等是教学内核框架的标准辅助函数。仅凭同名或调用这些公共接口，**不构成强同源证据**。

#### 6. 保守结论
本次审计基于AST指纹与调用图谱比对，确认目标项目与参考项目在进程管理、内存管理、任务调度等核心模块存在高度结构相似性，底层逻辑复用痕迹明显，定性为“部分逻辑借鉴”。然而，目标项目并非简单抄袭，证据显示其在设备驱动、procfs等子系统进行了独立的架构重构与功能扩展，拥有更多唯一AST指纹。整体相似度（80.32%）由核心逻辑复用、公共第三方库引用及教学框架共同基线共同推高。建议合议庭将风险等级定为**中高级**，要求作者对核心模块（如`proc_exit`、`do_clone`）的代码来源与设计决策做出合理解释，但不宜直接判定为完全抄袭。

------------------------------

