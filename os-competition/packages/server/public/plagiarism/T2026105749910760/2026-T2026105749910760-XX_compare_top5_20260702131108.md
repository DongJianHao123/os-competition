# 🛡️ OS 内核赛自动化代码级架构审计报告

**审计时间**: `2026-07-02 13:11:08`
**参赛作品名称**: `2026-T2026105749910760-XX`
**年份**: `2026`
**学校**: `XX`
**队伍名称**: `T2026105749910760`
**比赛名字**: `OS-Competition`
**比赛子赛道**: `Kernel-Implementation`
**仓库地址**: `https://gitlab.eduxiji.net/educg-group-43501-3132633/T2026105749910760-3579`
**推测开发语言**: `Rust / C (由AST探针自动识别)`
**特征函数量**: `1248`

## ⚔️ 底层代码同源性对比 (AST Homology)
> 本部分由专门的代码审查智能体，对该项目与符合年份条件的历届最高相似度项目进行控制流与底层调用对比。新版报告会先给出确定性的全模块覆盖矩阵，再给出 Agent 的分模块解释，避免只围绕少量抽样函数展开。

### 🏆 Top 5 底层对标: vs `2025-啊对的对的嗷不对不对-华中科技大学`（华中科技大学 / 啊对的对的,嗷不对不对）

#### 对方作品信息

**对方年份**: `2025`
**对方学校**: `华中科技大学`
**对方队伍名称**: `啊对的对的,嗷不对不对`
**对方比赛名字**: `OS-Competition`
**对方比赛子赛道**: `Kernel-Implementation`
**对方仓库地址**: `https://gitlab.eduxiji.net/educg-group-36002-2710490/T202510487995136-2371`

> 双向覆盖率：共同 AST 指纹: 463；唯一 AST 指纹: 目标项目 1068，参考项目 873；覆盖率: 目标项目 43.35%，参考项目 53.04%

## 全模块对比总览（确定性证据）

> 总体覆盖率：共同 AST 指纹: 463；唯一 AST 指纹: 目标项目 1068，参考项目 873；覆盖率: 目标项目 43.35%，参考项目 53.04%

### 1. 模块覆盖矩阵
| 模块 | 目标函数数 | 参考函数数 | 共同唯一 AST 指纹 | Target 覆盖 | Base 覆盖 | 状态 |
| :--- | ---: | ---: | ---: | ---: | ---: | :--- |
| 进程管理 | 72 | 87 | 12 | 17.39% | 15.79% | 双方存在且有结构重合 |
| 任务调度/异步运行时 | 67 | 33 | 5 | 7.69% | 15.62% | 双方存在且有结构重合 |
| 内存管理 | 209 | 164 | 15 | 7.69% | 10.0% | 双方存在且有结构重合 |
| 中断/异常处理 | 15 | 28 | 5 | 33.33% | 20.83% | 双方存在且有结构重合 |
| 系统调用 | 15 | 10 | 3 | 30.0% | 42.86% | 双方存在且有结构重合 |
| 文件系统 | 151 | 66 | 5 | 3.94% | 9.43% | 双方存在且有结构重合 |
| 设备驱动 | 61 | 50 | 9 | 15.79% | 22.5% | 双方存在且有结构重合 |
| 网络协议栈 | 0 | 0 | 0 | 0.0% | 0.0% | 双方均未体现 |
| 文件系统/第三方库 | 519 | 537 | 381 | 87.99% | 85.04% | 双方存在且有结构重合 |
| 通用模块/其他 | 139 | 68 | 24 | 19.35% | 40.68% | 双方存在且有结构重合 |

### 2. 分模块证据清单

#### 进程管理
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `kernel/sysproc.c#sys_setpgid` | `kernel/sys/sysproc.c#sys_setpgid` | 同板块同名 | acquire, argint, myproc, release | 内部闭环逻辑 |
| `kernel/sched/proc.c#procinit` | `kernel/proc/proc.c#procinit` | 同板块同名 | INIT_LIST_HEAD, KSTACK, PCB_Q_ALL_INIT, atomic_set, initlock, queue_push_back_atomic | KSTACK, initlock |
| `kernel/sysproc.c#sys_getpgid` | `kernel/sys/sysproc.c#sys_getpgid` | 同板块同名 | acquire, argint, myproc, release | 内部闭环逻辑 |
| `kernel/sched/proc.c#procdump` | `kernel/proc/proc.c#procdump` | 同板块同名 | NELEM, printf | NELEM, printf |

#### 任务调度/异步运行时
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `kernel/fs/vfs/vfs.c#namex` | `kernel/fs/vfs/ops.c#namex` | 同板块同名 | Log, idup, ip->iops->dirlookup, ip->iops->ilock, ip->iops->iunlock, iput ...(+4) | 内部闭环逻辑 |
| `kernel/arch/qemu/console.c#consoleintr` | `kernel/lib/console.c#consoleintr` | 同板块同名同结构 | C, acquire, consputc, procdump, release, thread_wakeup_chan | C, acquire, consputc, procdump, release, wakeup |
| `kernel/arch/qemu/console.c#consoleread` | `kernel/lib/console.c#consoleread` | 同板块同名 | C, acquire, either_copyout, myproc, proc_killed, release ...(+1) | C, acquire, either_copyout, killed, myproc, release ...(+1) |
| `kernel/lib/printf.c#printf` | `kernel/lib/printf.c#printf` | 同板块同名同结构 | acquire, consputc, panic, printint, printptr, release ...(+3) | acquire, consputc, panic, printint, printptr, release ...(+3) |

#### 内存管理
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `kernel/fs/exec.c#execve` | `kernel/proc/exec.c#execve` | 同板块同名 | ADD_AUXV, Log, PGROUNDDOWN, PGROUNDUP, Warn, copyout ...(+19) | PGROUNDDOWN, PGROUNDUP, alloc_aux, copyout, ip->i_op->lock, ip->i_op->read ...(+13) |
| `kernel/fs/virtio_disk.c#virtio_disk_init` | `kernel/driver/loongarch/virtio_disk.c#virtio_disk_init` | 同板块同名 | R, initlock, memset, panic | initlock, memset, pci_device_init, printf, virtio_pci_get_device_features, virtio_pci_get_queue_enable ...(+8) |
| `kernel/arch/loongarch/pci.c#pci_scan_device` | `kernel/driver/loongarch/pci.c#pci_scan_device` | 同板块同名 | pci_alloc_device, pci_device_bar_init, pci_device_dump, pci_device_init, pci_read_config, pci_write_config ...(+1) | pci_alloc_device, pci_device_bar_init, pci_device_init, pci_read_config, pci_write_config, printf |
| `kernel/fs/virtio_disk.c#virtio_disk_init` | `kernel/driver/riscv/virtio_disk.c#virtio_disk_init` | 同板块同名 | R, initlock, kalloc, memset, panic | R, initlock, memset, panic, printf |

#### 中断/异常处理
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `kernel/arch/loongarch/pci.c#pci_device_init` | `kernel/driver/loongarch/pci.c#pci_device_init` | 同板块同名同结构 | 内部闭环逻辑 | 内部闭环逻辑 |
| `kernel/arch/qemu/plic.c#plicinithart` | `kernel/sys/plic.c#plicinithart` | 同板块同名 | PLIC_SENABLE, PLIC_SPRIORITY, cpuid | PLIC_SENABLE, PLIC_SPRIORITY, cpuid |
| `kernel/arch/loongarch/extioi.c#extioi_init` | `kernel/trap/loongarch/extioi.c#extioi_init` | 同板块同名 | iocsr_writeq | iocsr_writeq |
| `kernel/arch/loongarch/apic.c#apic_init` | `kernel/trap/loongarch/apic.c#apic_init` | 同板块同名 | 内部闭环逻辑 | 内部闭环逻辑 |

#### 系统调用
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `kernel/syscall.c#argstr` | `kernel/sys/syscall.c#argstr` | 同板块同名同结构 | argaddr, fetchstr | argaddr, fetchstr |
| `kernel/syscall.c#argint` | `kernel/sys/syscall.c#argint` | 同板块同名同结构 | argraw | argraw |
| `kernel/sysother.c#sys_geteuid` | `kernel/sys/syssig.c#sys_rt_sigtimedwait` | 同板块同结构 | 内部闭环逻辑 | 内部闭环逻辑 |
| `kernel/sysother.c#sys_setuid` | `kernel/sys/sysmem.c#sys_madvise` | 同板块同结构 | 内部闭环逻辑 | 内部闭环逻辑 |

#### 文件系统
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `kernel/fs/vfs/vfs.c#get_absolute_path` | `kernel/fs/vfs/ops.c#get_absolute_path` | 同板块同名同结构 | strcat, strcpy, strlen, strncpy | strcat, strcpy, strlen, strncpy |
| `kernel/fs/sysfile.c#sys_mount` | `kernel/sys/sysfile.c#sys_mount` | 同板块同名同结构 | 内部闭环逻辑 | 内部闭环逻辑 |
| `kernel/fs/sysfile.c#sys_lseek` | `kernel/sys/sysfile.c#sys_lseek` | 同板块同名 | Log, argfd, argint, arglong, f->fops->lseek, printf | argaddr, argfd, argint, vfs_ext_lseek |
| `kernel/fs/file.c#filewrite` | `kernel/fs/vfs/file.c#filewrite` | 同板块同名 | IS_WRITABLE, devsw[f->major].write, f->fops->write, panic, pipewrite | devsw[f->f_major].write, get_fops, get_fops()->writable, panic, pipewrite, vfs_ext_write |

#### 设备驱动
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `kernel/arch/loongarch/pci.c#pci_device_bar_init` | `kernel/driver/loongarch/pci.c#pci_device_bar_init` | 同板块同名同结构 | 内部闭环逻辑 | 内部闭环逻辑 |
| `kernel/arch/qemu/uart.c#uartinit` | `kernel/mem/uart.c#uartinit` | 同板块同名同结构 | WriteReg, initlock | WriteReg, initlock |
| `kernel/arch/qemu/uart.c#uartstart` | `kernel/mem/uart.c#uartstart` | 同板块同名同结构 | ReadReg, WriteReg, thread_wakeup_chan | ReadReg, WriteReg, wakeup |
| `kernel/arch/loongarch/pci.c#pci_init` | `kernel/driver/loongarch/pci.c#pci_init` | 同板块同名同结构 | pci_scan_buses, printf | pci_scan_buses, printf |

#### 文件系统/第三方库
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `kernel/fs/ext4_balloc.c#ext4_balloc_alloc_block` | `kernel/fs/ext4/lwext4/ext4_balloc.c#ext4_balloc_alloc_block` | 同板块同名同结构 | ext4_balloc_get_bgid_of_block, ext4_balloc_get_block_of_bgid, ext4_balloc_set_bitmap_csum, ext4_balloc_verify_bitmap_csum, ext4_bg_get_block_bitmap, ext4_bg_get_free_blocks_count ...(+19) | ext4_balloc_get_bgid_of_block, ext4_balloc_get_block_of_bgid, ext4_balloc_set_bitmap_csum, ext4_balloc_verify_bitmap_csum, ext4_bg_get_block_bitmap, ext4_bg_get_free_blocks_count ...(+19) |
| `kernel/fs/ext4.c#ext4_fwrite` | `kernel/fs/ext4/lwext4/ext4.c#ext4_fwrite` | 同板块同名 | EXT4_MP_LOCK, EXT4_MP_UNLOCK, Log, ext4_assert, ext4_block_cache_write_back, ext4_block_writebytes ...(+11) | EXT4_MP_LOCK, EXT4_MP_UNLOCK, ext4_assert, ext4_block_cache_write_back, ext4_block_writebytes, ext4_blocks_set_direct ...(+10) |
| `kernel/fs/ext4_journal.c#jbd_journal_prepare` | `kernel/fs/ext4/lwext4/ext4_journal.c#jbd_journal_prepare` | 同板块同名 | EXT4_BLOCK_ZERO, RB_FIND, TAILQ_FOREACH_REVERSE_SAFE, TAILQ_FOREACH_SAFE, TAILQ_REMOVE, ext4_bcache_clear_dirty ...(+17) | EXT4_BLOCK_ZERO, RB_FIND, TAILQ_FOREACH_REVERSE_SAFE, TAILQ_FOREACH_SAFE, TAILQ_REMOVE, ext4_bcache_clear_dirty ...(+17) |
| `kernel/fs/ext4.c#ext4_dir_rm` | `kernel/fs/ext4/lwext4/ext4.c#ext4_dir_rm` | 同板块同名同结构 | EXT4_MP_LOCK, EXT4_MP_UNLOCK, ext4_block_cache_write_back, ext4_dir_en_get_inode, ext4_dir_iterator_fini, ext4_dir_iterator_init ...(+19) | EXT4_MP_LOCK, EXT4_MP_UNLOCK, ext4_block_cache_write_back, ext4_dir_en_get_inode, ext4_dir_iterator_fini, ext4_dir_iterator_init ...(+19) |

#### 通用模块/其他
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `kernel/lib/qsort.c#qsort` | `kernel/lib/qsort.c#qsort` | 同板块同名 | (*cmp), POP, PUSH, SWAP, min_t | _quicksort |
| `user/lib/printf.c#vprintf` | `user/deps/printf.c#vprintf` | 同板块同名同结构 | printint, printptr, putc, va_arg | printint, printptr, putc, va_arg |
| `user/lib/printf.c#printint` | `user/deps/printf.c#printint` | 同板块同名同结构 | putc | putc |
| `kernel/lib/printf.c#printint` | `kernel/lib/printf.c#printint` | 同板块同名同结构 | consputc | consputc |


好的，审计官。基于提供的客观 AST 数据、模块画像、控辩双方陈述及原始抽样证据，我将进行最终的独立同源性定性仲裁。

### 1. 宏观判定
- **定性结论**: **独立开发，底层架构显著差异**
- **核心依据**: 客观 AST 相似度仅为 **31.33%**，处于低水平。该相似度主要由 **“文件系统/第三方库”** 模块（共同指纹 381 个，覆盖率均超 85%）贡献，剥离此公共依赖后，核心内核模块的相似度极低。目标项目拥有 **1068** 个唯一 AST 指纹，远超共同指纹数，显示出大规模的独立代码体量。控方所指控的【进程管理】板块“铁证”，经技术剖析，均为受硬件 ABI、POSIX 标准或教学范式约束的**功能性必然相似**，而非代码复用。

### 2. 全模块覆盖矩阵解读
- **进程管理**: 双方均存在。共同指纹仅 12 个，覆盖率均低于 20%。控方核心指控区，但证据强度为 **弱**。相似点集中于 POSIX 系统调用接口和 xv6 式调度原语，而目标项目显式的 `thread_*` 模型与参考项目的 `proc_*` 模型构成根本性架构差异。
- **任务调度/异步运行时**: 双方均存在。共同指纹 5 个，覆盖率低。证据强度为 **弱**。相似点如 `consoleintr`、`printf` 等属于通用内核工具函数或标准库实现，不构成调度器核心算法的同源性。
- **内存管理**: 双方均存在。共同指纹 15 个，覆盖率低于 10%。证据强度为 **弱**。相似点如 `execve` 等复杂函数，其调用图谱差异巨大（目标 24+ 调用，参考 18+ 调用），表明内部实现逻辑不同。
- **中断/异常处理**: 双方均存在。共同指纹 5 个，覆盖率中等。证据强度为 **弱**。相似点如 `plicinithart`、`extioi_init` 等，其逻辑完全由硬件手册规定的寄存器地址和初始化序列决定，属于硬件强制相似。
- **系统调用**: 双方均存在。共同指纹 3 个，覆盖率中等。证据强度为 **弱**。相似点 `argint`、`argstr` 是 xv6 内核中经典的参数获取范式，属于公共架构遗产。
- **文件系统**: 双方均存在。共同指纹 5 个，覆盖率极低（<10%）。证据强度为 **未形成**。除 `sys_mount` 等 POSIX 接口同名外，核心 VFS 与具体文件系统实现（目标项目含大量 ext4 操作）差异显著。
- **设备驱动**: 双方均存在。共同指纹 9 个，覆盖率中等。证据强度为 **弱**。相似点如 `uartinit`、`pci_init` 等，其初始化流程由硬件规范决定，且调用图谱存在差异（如参考项目 `virtio_disk_init` 包含特定 PCI 函数调用）。
- **网络协议栈**: **双方均未体现**。无证据。
- **第三方库/公共依赖**: 双方均存在。共同指纹 **381** 个，覆盖率 **>85%**。证据强度为 **强**，但**不构成抄袭**。这是双方共同集成了 `lwext4` 等开源第三方库的铁证，是相似度的主要来源，应在审计中剥离。

### 3. 底层 AST 关键证据指纹表
| 所属板块 | 目标项目路径 | 参考项目路径 | 证据类型 | 技术说明 |
| :--- | :--- | :--- | :--- | :--- |
| **第三方库/公共依赖** | `kernel/fs/ext4_balloc.c#ext4_balloc_alloc_block` | `kernel/fs/ext4/lwext4/ext4_balloc.c#ext4_balloc_alloc_block` | **强-同源** | **第三方库 lwext4**。调用图谱完全一致（20+ 内部函数），是双方共同引入的开源库，非竞赛代码。 |
| **第三方库/公共依赖** | `kernel/fs/ext4_journal.c#jbd_journal_prepare` | `kernel/fs/ext4/lwext4/ext4_journal.c#jbd_journal_prepare` | **强-同源** | **第三方库 lwext4**。调用图谱完全一致（20+ 内部函数），是双方共同引入的开源库，非竞赛代码。 |
| **第三方库/公共依赖** | `kernel/fs/ext4.c#ext4_dir_rm` | `kernel/fs/ext4/lwext4/ext4.c#ext4_dir_rm` | **强-同源** | **第三方库 lwext4**。调用图谱完全一致（20+ 内部函数），是双方共同引入的开源库，非竞赛代码。 |
| 进程管理 | `kernel/sysproc.c#sys_uptime` | `kernel/sys/sysproc.c#sys_uptime` | 弱-必然相似 | 锁保护下的原子读取操作，是获取全局变量的最简实现，无独创性。 |
| 进程管理 | `user/src/zombie.c#main` | `user/app/zombie.c#main` | 弱-公共范式 | xv6 教科书标准僵尸进程测试用例，调用序列由测试目的决定。 |
| 进程管理 | `kernel/sched/proc.c#mycpu` | `kernel/proc/proc.c#mycpu` | 弱-硬件约束 | RISC-V 架构下通过 `tp` 寄存器获取 CPU ID 的标准方法，源自硬件 ABI。 |
| 进程管理 | `kernel/sysproc.c#sys_fork` | `kernel/sys/sysproc.c#sys_fork` | 弱-架构差异 | 目标调用 `do_clone`，参考调用 `fork`，暴露了线程模型与进程模型的根本差异。 |
| 系统调用 | `kernel/syscall.c#argint` | `kernel/sys/syscall.c#argint` | 弱-公共遗产 | 从 trapframe 获取参数的 xv6 经典范式，属于公共教学内核遗产。 |
| 中断/异常处理 | `kernel/arch/qemu/plic.c#plicinithart` | `kernel/sys/plic.c#plicinithart` | 弱-硬件强制 | PLIC 中断控制器初始化，操作寄存器地址和流程由 RISC-V 特权规范定义。 |
| 设备驱动 | `kernel/arch/qemu/uart.c#uartinit` | `kernel/mem/uart.c#uartinit` | 弱-硬件强制 | UART 16550 兼容设备的初始化序列，由硬件手册规定。 |

### 4. 核心代码调用流对比剖析
* **【进程管理/系统调用路径】**
* **底层技术析理**: 控方核心指控点。以 `sys_fork` 为例，目标项目调用 `do_clone`，参考项目调用 `fork`。这不仅是函数名差异，更揭示了**根本性的对象模型差异**。目标项目采用 `do_clone` 这一 Linux 风格的线程创建原语，其内部必然涉及线程控制块（TCB）与进程控制块（PCB）的分离与资源共享逻辑；而参考项目的 `fork` 是传统的进程复制模型。结合 `sys_exit`（目标 `thread_exit` vs 参考 `exit`）和 `sys_wait`（目标 `wait_one` vs 参考 `wait`）等证据，整个进程/线程生命周期管理的底层抽象完全不同，这是独立架构设计的铁证，而非抄袭的掩饰。
* **代码溯源链接**:
  - 目标项目: `kernel/sysproc.c#sys_fork`
  - 参考项目: `kernel/sys/sysproc.c#sys_fork`

* **【内存管理/可执行文件加载】**
* **底层技术析理**: `execve` 是内核中最复杂的系统调用之一。目标项目的调用图谱包含 `ADD_AUXV`, `Log`, `Warn` 等辅助向量和日志宏，而参考项目包含 `alloc_aux` 和 `ip->i_op->lock/read` 等直接操作 inode 的函数。这表明两者在**辅助向量构建、错误处理机制、文件系统访问层次**上存在不同实现路径。目标项目似乎有更完善的日志和调试体系，而参考项目更贴近传统的 xv6 直接访问方式。
* **代码溯源链接**:
  - 目标项目: `kernel/fs/exec.c#execve`
  - 参考项目: `kernel/proc/exec.c#execve`

* **【设备驱动/VirtIO 磁盘初始化】**
* **底层技术析理**: 在 `virtio_disk_init` 中，参考项目的调用图谱包含 `pci_device_init`, `virtio_pci_get_device_features` 等特定于 PCI 配置空间的函数，表明其初始化流程是通过 PCI 总线枚举和配置进行的。目标项目的调用图谱则缺少这些 PCI 特定函数，暗示其可能采用了 MMIO 直接访问或其他非标准 PCI 枚举方式。这构成了**硬件抽象层与驱动模型的显著差异**。
* **代码溯源链接**:
  - 目标项目: `kernel/fs/virtio_disk.c#virtio_disk_init`
  - 参考项目: `kernel/driver/riscv/virtio_disk.c#virtio_disk_init`

### 5. 单方模块与降权项
- **单方模块/子系统**:
  - **目标项目独有**: `kernel/ipc/signal.c` 中的 `do_sigtimedwait`、`do_sigprocmask` 等函数，以及 `kernel/atomic/cond.c` 中的 `cond_wait`，表明其实现了独立的、更复杂的信号处理和条件变量同步机制。
  - **参考项目独有**: `kernel/proc/socket.c` 中的 `recvfrom`、`shutdown` 等函数，表明其包含一个初步的网络协议栈实现。
  - 这些单方存在的子系统是独立开发的强有力证据。
- **降权项说明**:
  - **第三方库/公共依赖**: `lwext4` 文件系统库、`printf` 系列函数、`qsort` 等标准 C 库函数。这些代码量大，是 AST 相似度的主要贡献者，但属于公共资源，**必须从原创性审计中剥离**。
  - **跨板块硬凑**: 控方将目标项目的 `kernel/fs/procfs.c`（文件系统）与参考项目的 `kernel/proc/socket.c`（网络/进程间通信）放在【进程管理】下对比，属于跨模块错误关联，其证据无效。
  - **硬件强制相似**: `cpuid`、`plicinithart`、`uartinit` 等由硬件手册或 ABI 决定的代码，其相似性不反映设计思想或代码复用。

### 6. 保守结论
综合审计，目标项目与参考项目的客观 AST 相似度为 31.33%，处于低位。该相似度主要由双方共同引入的 `lwext4` 等第三方开源库贡献，剥离后核心内核的相似度极低。控方关于【进程管理】板块抄袭的指控，所依赖的证据均为受 POSIX 标准、RISC-V 硬件 ABI 或 xv6 教学范式强约束的**功能性必然相似**，不具备代码复用的指纹特征。相反，双方在进程/线程模型（`do_clone` vs `fork`）、内存管理、驱动模型及独有子系统上存在**根本性的架构差异**。因此，现有证据无法支持抄袭指控，应认定为两个基于公共知识和第三方库的独立开发作品。

------------------------------

