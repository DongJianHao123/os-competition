# 🛡️ OS 内核赛自动化代码级架构审计报告

**审计时间**: `2026-07-02 11:07:12`
**参赛作品名称**: `2026-T202610486999563-XX`
**年份**: `2026`
**学校**: `XX`
**队伍名称**: `T202610486999563`
**比赛名字**: `OS-Competition`
**比赛子赛道**: `Kernel-Implementation`
**仓库地址**: `https://gitlab.eduxiji.net/educg-group-43501-3132633/T202610486999563-1502`
**推测开发语言**: `Rust / C (由AST探针自动识别)`
**特征函数量**: `2087`

## ⚔️ 底层代码同源性对比 (AST Homology)
> 本部分由专门的代码审查智能体，对该项目与符合年份条件的历届最高相似度项目进行控制流与底层调用对比。新版报告会先给出确定性的全模块覆盖矩阵，再给出 Agent 的分模块解释，避免只围绕少量抽样函数展开。

### 🏆 Top 4 底层对标: vs `2025-如有名字队-南开大学`（南开大学 / 如有名字队）

#### 对方作品信息

**对方年份**: `2025`
**对方学校**: `南开大学`
**对方队伍名称**: `如有名字队`
**对方比赛名字**: `OS-Competition`
**对方比赛子赛道**: `Kernel-Implementation`
**对方仓库地址**: `https://gitlab.eduxiji.net/educg-group-36002-2710490/nonix`

> 双向覆盖率：共同 AST 指纹: 266；唯一 AST 指纹: 目标项目 1823，参考项目 1745；覆盖率: 目标项目 14.59%，参考项目 15.24%

## 全模块对比总览（确定性证据）

> 总体覆盖率：共同 AST 指纹: 266；唯一 AST 指纹: 目标项目 1823，参考项目 1745；覆盖率: 目标项目 14.59%，参考项目 15.24%

### 1. 模块覆盖矩阵
| 模块 | 目标函数数 | 参考函数数 | 共同唯一 AST 指纹 | Target 覆盖 | Base 覆盖 | 状态 |
| :--- | ---: | ---: | ---: | ---: | ---: | :--- |
| 进程管理 | 0 | 36 | 0 | 0.0% | 0.0% | 仅参考项目体现 |
| 任务调度/异步运行时 | 87 | 92 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |
| 内存管理 | 10 | 297 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |
| 中断/异常处理 | 2 | 43 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |
| 系统调用 | 0 | 63 | 0 | 0.0% | 0.0% | 仅参考项目体现 |
| 文件系统 | 0 | 252 | 0 | 0.0% | 0.0% | 仅参考项目体现 |
| 设备驱动 | 1 | 372 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |
| 网络协议栈 | 0 | 0 | 0 | 0.0% | 0.0% | 双方均未体现 |
| 文件系统/第三方库 | 57 | 696 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |
| 网络协议栈/第三方库 | 125 | 0 | 0 | 0.0% | 0.0% | 仅目标项目体现 |
| 第三方库 | 1785 | 1 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |
| 通用模块/其他 | 20 | 155 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |

### 2. 分模块证据清单

#### 进程管理
仅参考项目中观察到该模块代表函数，目标项目没有同模块可对齐证据：
- 参考项目：`os/src/syscall/process.rs#sys_exec`，调用摘要："LD_LIBRARY_PATH=/:/lib:/usr/lib:".to_string, "PATH=/:/bin:".to_string, Ok, String::from, Vec::<String>::new, Vec::new ...(+28)
- 参考项目：`os/src/syscall/signal.rs#sys_sig_proc_mask`，调用摘要：Err, Ok, SignalMaskFlag::from_bits, SignalMaskFlag::from_bits(how).unwrap, current_task, current_task().unwrap ...(+7)
- 参考项目：`patch/polyhal/src/components/timer/x86_64.rs#timer_wait`，调用摘要：PC_SPEAKER.read, PC_SPEAKER.write, PIT_CH2.write, PIT_CMD.write, duration.as_millis, spin_loop
- 参考项目：`os/src/syscall/process.rs#sys_clone`，调用摘要：CloneFlags::from_bits, CloneFlags::from_bits(flags as u32).ok_or_else, Ok, add_task, current_task, current_task().unwrap ...(+5)

#### 任务调度/异步运行时
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `tools/xtask/src/main.rs#contest_selfcheck` | `user/src/bin/user_shell.rs#main` | 同模块上下文对齐/差异观察 | ContestRuntime::detect, ExitCode::from, Path::new, PathBuf::from, Some, branch.trim ...(+35) | ProcessArguments::new, String::new, Vec::new, args_addr.as_slice, args_copy[0].as_str, assert_eq! ...(+39) |
| `tools/xtask/src/main.rs#validate_oscomp_full_image` | `os/src/syscall/mod.rs#syscall` | 同模块上下文对齐/差异观察 | "/glibc/test_all.sh(or /glibc/basic/run-all.sh)".to_string, "/musl/test_all.sh(or /musl/basic/run-all.sh)".to_string, Vec::new, basic_listing.contains, debugfs_exists, debugfs_list ...(+14) | Err, error!, sys_brk, sys_chdir, sys_clock_gettime, sys_clock_nanosleep ...(+72) |
| `tools/xtask/src/main.rs#qemu_loongarch_with_disk_and_mode` | `os/src/syscall/fs.rs#sys_pselect6` | 同模块上下文对齐/差异观察 | ExitCode::from, PathBuf::from, PathBuf::from("target")
        .join, PathBuf::from("target")
        .join(LOONGARCH_TARGET)
        .join, PathBuf::from("target")
        .join(LOONGARCH_TARGET)
        .join("debug")
        .join, Some ...(+19) | Ok, Some, current_task, current_task().unwrap, current_user_token, drop ...(+21) |
| `tools/xtask/src/main.rs#prepare_oscomp_images` | `os/src/syscall/fs.rs#sys_copy_file_range` | 同模块上下文对齐/差异观察 | ExitCode::from, build_oscomp_sdcard, debugfs_write_text, ensure_oscomp_source_image, env::var, env::var("WHUSE_OSCOMP_SKIP_BUILD").is_err ...(+13) | Err, Ok, UserBuffer::new, Vec::new, buf.as_mut_slice, buf.as_mut_slice().as_mut_ptr ...(+27) |

#### 内存管理
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `platform/riscv64-virt/src/main.rs#dealloc` | `os/src/task/pid.rs#dealloc` | 同板块同名 | buddy.saturating_add, current.min, layout
                .size, layout
                .size()
                .max, layout
                .size()
                .max(layout.align())
                .max, layout
                .size()
                .max(layout.align())
                .max(size_of::<usize>())
                .next_power_of_two ...(+6) | assert!, self.recycled.push |
| `platform/riscv64-virt/src/main.rs#alloc` | `patch/polyhal/src/mem.rs#alloc` | 同板块同名 | core::arch::asm!, core::fmt::Write::write_fmt, core::ptr::null_mut, cpu.disable_interrupts, cpu.enable_interrupts, cpu.interrupts_enabled ...(+9) | MEM_AREA.iter_mut, unreachable! |
| `platform/loongarch64-virt/src/main.rs#alloc` | `os/src/task/pid.rs#alloc` | 同板块同名 | (start_order + 1..order + 1).rev, Err, NonNull::new, NonNull::new(self.pop(start_order).unwrap() as *mut u8).unwrap, Ok, layout
                .size ...(+11) | PidHandle, self.recycled.pop |
| `platform/riscv64-virt/src/main.rs#alloc` | `patch/polyhal/src/pagetable/mod.rs#alloc` | 同板块同名 | (start_order + 1..order + 1).rev, Err, NonNull::new, NonNull::new(self.pop(start_order).unwrap() as *mut u8).unwrap, Ok, layout
                .size ...(+11) | PageTable, Self, frame_alloc, pt.restore |

#### 中断/异常处理
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `platform/loongarch64-virt/src/main.rs#main` | `os/src/signal/sigflags.rs#default_op` | 同模块上下文对齐/差异观察 | hal_loongarch64_virt::bootstrap, kernel_core::Kernel::bootstrap, println! | continue_signals.contains, dump_signals.contains, ignore_signals.contains, stop_signals.contains, terminate_signals.contains |
| `platform/riscv64-virt/src/main.rs#main` | `patch/polyhal/src/arch/x86_64/idt.rs#new` | 同模块上下文对齐/差异观察 | hal_riscv64_virt::bootstrap, kernel_core::Kernel::bootstrap, println! | InterruptDescriptorTable::new, Self, core::slice::from_raw_parts_mut, entries[i].set_handler_fn, transmute::<extern "C" fn(), extern "x86-interrupt" fn(InterruptStackFrame)> |

#### 系统调用
仅参考项目中观察到该模块代表函数，目标项目没有同模块可对齐证据：
- 参考项目：`os/src/syscall/other.rs#sys_uname`，调用摘要：Ok, UserBuffer::new, buf[..len].copy_from_slice, bytes.len, bytes.len().min, copy_str_to_buf ...(+8)
- 参考项目：`os/src/syscall/other.rs#sys_getrandom`，调用摘要：Err, Ok, UserBuffer::new, Vec::new, all_bytes.extend_from_slice, buf_ptr.is_null ...(+7)
- 参考项目：`os/src/syscall/mm.rs#sys_shmget`，调用摘要：Err, Ok, ShmFlags::from_bits, ShmFlags::from_bits(flags & !0x1ff).unwrap, flags.contains, shm_create ...(+2)
- 参考项目：`os/src/syscall/other.rs#sys_clock_nanosleep`，调用摘要：Ok, current_user_token, get_data, get_time_ms, suspend_current_and_run_next, trace!

#### 文件系统
仅参考项目中观察到该模块代表函数，目标项目没有同模块可对齐证据：
- 参考项目：`os/src/mm/memory_set.rs#split_area_by_range`，调用摘要：BTreeMap::new, MapArea::from_another, VAddrRange::new, Vec::new, area.clone, area.data_frames.retain ...(+19)
- 参考项目：`patch/polyhal/src/debug_console/x86_64/graphic.rs#put_char`，调用摘要：BIT_FONTS.len, self.backspace, self.scroll_up, self.write_pixel
- 参考项目：`patch/virtio-drivers/src/transport/pci.rs#new`，调用摘要：Err, Ok, Some, VirtioPciError::InvalidNotifyOffMultiplier, VirtioPciError::InvalidVendorId, common_cfg.is_none ...(+12)
- 参考项目：`os/src/mm/memory_set.rs#load_dl_interp_if_needed`，调用摘要："/glibc/lib/libc.so.6".to_string, "ld-musl-riscv64.so.1".to_string, DL_INTERP_OFFSET.into, Some, String::from_utf8, String::from_utf8(section.raw_data(&elf).to_vec()).unwrap ...(+29)

#### 设备驱动
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `platform/riscv64-virt/src/main.rs#panic` | `patch/virtio-drivers/src/queue.rs#fake_read_write_queue` | 同模块上下文对齐/差异观察 | core::arch::asm!, core::fmt::Write::write_fmt, core::ptr::write_volatile, format_args! | (*available_ring).idx.load, (*used_ring).idx.fetch_add, (*used_ring).idx.load, Vec::new, assert!, assert_eq! ...(+18) |

#### 文件系统/第三方库
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `crates/fs-ext4/src/lib.rs#read_dir` | `lwext4_rust/examples/src/vfs_ops.rs#read_dir` | 同板块同名 | Ok, Vec::new, entry.file_type, entry.file_type().map_err, entry.map_err, entry.metadata ...(+7) | unimplemented! |
| `crates/fs-ext4/src/lib.rs#read_link` | `os/src/fs/ext4_lw/inode.rs#read_link` | 同板块同名 | Ok, format!, self.fs.read_link, self.fs.read_link(path).map_err | SysErrNo::from, file.file_readlink, file.file_readlink(buf, bufsize)
            .map_err, self.inner.exclusive_access |
| `third_party/virtio-drivers/src/queue.rs#add_buffers_indirect` | `lwext4_rust/c/lwext4/src/ext4_balloc.c#ext4_balloc_alloc_block` | 同模块上下文对齐/差异观察 | MmioTransport::new_from_unique, MmioTransport::new_from_unique(
            UniqueMmioPointer::from(&mut header),
            UniqueMmioPointer::from([].as_mut_slice()),
        )
        .unwrap, UniqueMmioPointer::from, VirtIOHeader::make_fake_header, VirtQueue::<FakeHal, 4>::new, VirtQueue::<FakeHal, 4>::new(&mut transport, 0, true, false).unwrap ...(+8) | ext4_balloc_get_bgid_of_block, ext4_balloc_get_block_of_bgid, ext4_balloc_set_bitmap_csum, ext4_balloc_verify_bitmap_csum, ext4_bg_get_block_bitmap, ext4_bg_get_free_blocks_count ...(+19) |
| `crates/fs-ext4/src/lib.rs#read` | `lwext4_rust/c/lwext4/src/ext4_journal.c#jbd_journal_prepare` | 同模块上下文对齐/差异观察 | Box::new, Err, Ok, dst.len, dst[dst_start..dst_start + len]
                .copy_from_slice, end.saturating_sub ...(+12) | EXT4_BLOCK_ZERO, RB_FIND, TAILQ_FOREACH_REVERSE_SAFE, TAILQ_FOREACH_SAFE, TAILQ_REMOVE, ext4_bcache_clear_dirty ...(+17) |

#### 网络协议栈/第三方库
仅目标项目中观察到该模块代表函数，参考项目没有同模块可对齐证据：
- 目标项目：`third_party/virtio-drivers/src/device/socket/connectionmanager.rs#send_recv`，调用摘要："Hello from guest".as_bytes, (hello_from_guest.len() as u32).into, (hello_from_host.len() as u32).into, 0.into, 50.into, Arc::new ...(+43)
- 目标项目：`third_party/virtio-drivers/src/device/socket/connectionmanager.rs#incoming_connection`，调用摘要：0.into, 50.into, Arc::new, Mutex::new, ReadOnly::new, SocketType::Stream.into ...(+24)
- 目标项目：`crates/syscall/src/lib.rs#eventfd_epoll_and_socketpair_smoke`，调用摘要：(eventfd as u64).to_le_bytes, 1u32.to_le_bytes, 1u64.to_le_bytes, KernelVfs::new, ProcessTable::new, Scheduler::new ...(+24)
- 目标项目：`crates/syscall/src/lib.rs#inet_loopback_socket_smoke`，调用摘要：16u32.to_le_bytes, 2u16.to_le_bytes, 7000u16.to_be_bytes, KernelVfs::new, ProcessTable::new, Scheduler::new ...(+21)

#### 第三方库
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `third_party/virtio-drivers/src/transport/mmio.rs#vendor_id` | `patch/virtio-drivers/src/transport/mmio.rs#vendor_id` | 同板块同名 | field_shared!, field_shared!(self.header, vendor_id).read | volread! |

#### 通用模块/其他
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `platform/riscv64-virt/src/main.rs#init` | `user/src/bin/user_shell.rs#new` | 同模块上下文对齐/差异观察 | core::fmt::Write::write_fmt, format_args!, min, prev_power_of_two, self.push, size.trailing_zeros ...(+1) | String::new, arg.as_ptr, arg.as_str, arg.is_empty, args
            .iter, args
            .iter()
            .filter ...(+16) |
| `platform/loongarch64-virt/src/main.rs#init` | `patch/polyhal/src/mem.rs#parse_system_info` | 同模块上下文对齐/差异观察 | min, prev_power_of_two, self.push, size.trailing_zeros, size_of::<usize> | CPU_NUM.init_once, display_info!, fdt.chosen, fdt.chosen().inspect, fdt.find_nodes, fdt.find_nodes("/cpus/cpu").count ...(+8) |
| `platform/loongarch64-virt/src/main.rs#pop` | `os/src/signal/sigflags.rs#check_error` | 同模块上下文对齐/差异观察 | Some, res.is_null | Some, self.contains |
| `platform/riscv64-virt/src/main.rs#pop` | `user/src/lib.rs#_start` | 同模块上下文对齐/差异观察 | Some, res.is_null | ((argv + i * core::mem::size_of::<usize>()) as *const usize).read_volatile, ((str_start + *i) as *const u8).read_volatile, (0usize..)
            .find, (0usize..)
            .find(|i| unsafe { ((str_start + *i) as *const u8).read_volatile() == 0 })
            .unwrap, HEAP.lock, HEAP.lock()
            .init ...(+10) |


好的，审计官。基于提供的证据底座、控辩双方陈述及客观AST指标，现出具最终同源性定性仲裁报告。

---

### 1. 宏观判定
- **定性结论**: **独立开发 / 底层架构显著差异**
- **核心依据**: 客观AST相似度仅为 **8.06%**，双向覆盖率均低于16%。在所有核心系统模块（进程管理、内存管理、系统调用等）中，**共同唯一AST指纹均为0**。控方指控所依赖的证据存在严重的“跨板块硬凑”问题，将目标项目的构建工具链代码与参考项目的操作系统内核代码进行比对，其结论不成立。双方项目在软件栈层级、核心抽象与外部依赖上展现出根本性差异。

### 2. 全模块覆盖矩阵解读
| 模块 | 双方存在 | 共同AST指纹 | 覆盖率解读 | 证据强度 |
| :--- | :--- | :--- | :--- | :--- |
| **进程管理** | 仅参考项目 | 0 | 目标项目无此模块，无法比对。 | **未形成** |
| **任务调度/异步运行时** | 是 | 0 | 双方虽均有此模块，但目标项目为构建调度工具，参考项目为内核任务管理，领域完全隔离，无共同指纹。 | **弱** |
| **内存管理** | 是 | 0 | 双方均存在，但目标项目为早期堆分配器，参考项目涵盖页表、PID分配等，实现层面无共同指纹。 | **弱** |
| **中断/异常处理** | 是 | 0 | 双方均存在，但目标为平台启动，参考为x86_64 IDT设置，架构与实现完全不同。 | **弱** |
| **系统调用** | 仅参考项目 | 0 | 目标项目无此模块，无法比对。 | **未形成** |
| **文件系统** | 仅参考项目 | 0 | 目标项目无独立的内核文件系统模块，其文件操作属于宿主机工具链行为。 | **未形成** |
| **设备驱动** | 是 | 0 | 双方均存在，但目标为virtio驱动测试，参考为驱动实现，且共同指纹为0。 | **弱** |
| **网络协议栈** | 仅目标项目 | 0 | 参考项目无此模块，无法比对。 | **未形成** |
| **第三方库/公共依赖** | 是 | 0 | 双方均使用第三方库（如virtio-drivers, lwext4），但共同指纹为0，表明使用方式和封装层次不同。 | **弱** |

### 3. 底层 AST 关键证据指纹表
> 经审查，**未发现任何满足“同板块底层逻辑一致”的强证据指纹**。控方提供的所有证据均因跨板块比对、通用语言模式或规范驱动接口而被降级。下表中仅列出经严格筛选后仍不构成复用证据的弱信号项。

| 所属板块 | 目标项目路径 | 参考项目路径 | 证据类型 | 技术说明 |
| :--- | :--- | :--- | :--- | :--- |
| 任务调度/异步运行时 | `tools/xtask/src/main.rs#main` | `user/src/bin/user_shell.rs#main` | 弱信号 (同名函数) | 同为Rust程序入口`main`，但目标为构建命令分发器，参考为用户态Shell主循环，AST调用链完全不同。 |
| 内存管理 | `platform/riscv64-virt/src/main.rs#alloc` | `os/src/task/pid.rs#alloc` | 弱信号 (同名函数) | 同名`alloc`，但目标实现的是堆内存分配，参考实现的是PID分配，操作对象和逻辑完全不同。 |
| 第三方库/公共依赖 | `third_party/virtio-drivers/src/transport/mmio.rs#vendor_id` | `patch/virtio-drivers/src/transport/mmio.rs#vendor_id` | 弱信号 (规范接口) | 均读取virtio MMIO `vendor_id`寄存器，这是**硬件规范定义**的操作，且双方使用了不同的宏封装。 |

### 4. 核心代码调用流对比剖析
#### **【任务调度/异步运行时】**
*   **底层技术析理**: 此模块是控方指控的核心区域，但双方代码在**抽象层次与执行环境**上存在根本性异构。目标项目的“任务”是宿主机上的构建与测试任务（如编译内核、启动QEMU），其核心调用流为`main -> cargo/build_artifact -> Command::new`，依赖宿主机进程模型。参考项目的“任务”是内核管理的线程/进程，其核心调用流为`syscall -> sys_clone/sys_exec -> current_task().unwrap`，依赖内核任务控制块与调度器。两者在内存模型（宿主机虚拟内存 vs 内核物理/虚拟内存）、同步策略（进程退出码 vs 内核信号量/锁）上毫无共同点。
*   **代码溯源链接**:
    *   目标项目: `tools/xtask/src/main.rs#main`
    *   参考项目: `os/src/syscall/mod.rs#syscall`

#### **【内存管理】**
*   **底层技术析理**: 目标项目的内存管理局限于早期平台启动阶段的简易堆分配器（`platform/.../main.rs#alloc`），其核心是`buddy.saturating_add`等伙伴系统操作。参考项目则包含完整的内核内存管理子系统，涵盖页帧分配、页表操作、内存区域映射等（`os/src/mm/memory_set.rs`）。目标项目的实现是自包含的、用于引导的简单分配器；参考项目则是与进程、文件系统紧密耦合的复杂VMM。两者在规模、抽象层次和与系统其他部分的交互上完全不在一个量级。
*   **代码溯源链接**:
    *   目标项目: `platform/riscv64-virt/src/main.rs#alloc`
    *   参考项目: `os/src/task/pid.rs#alloc`

### 5. 单方模块与降权项
*   **单方模块**: **进程管理、系统调用、文件系统**仅存在于参考项目；**网络协议栈**仅存在于目标项目。这些模块的完全缺失或独有，是双方项目设计目标与实现阶段根本不同的有力证明，直接否定了系统性抄袭的可能。
*   **降权项说明**: 审计过程中发现的同名函数（如`main`, `alloc`, `dealloc`, `read`, `write`）均被降权。原因如下：
    1.  **语言/领域通用性**: 这些名称是Rust语言、操作系统领域的标准术语或强制入口，不具备独创性。
    2.  **底层AST异构**: 模块覆盖矩阵已证实，即使函数同名，其所在模块的共同AST指纹也为0，表明具体实现逻辑完全不同。
    3.  **规范驱动**: 如`vendor_id`等函数，其行为由外部硬件规范定义，不同实现必然趋同。

### 6. 保守结论
基于8.06%的极低客观AST相似度、所有核心模块共同指纹为0的事实，以及控方证据链中严重的“跨板块硬凑”缺陷，本次审计的最终结论为**独立开发**。目标项目`[2026-T202610486999563-XX]`的`tools/xtask`构建工具链与参考项目`[2025-如有名字队-南开大学]`的操作系统内核在软件栈层级、核心抽象、外部依赖和具体实现上存在根本性、系统性的差异。当前证据完全不足以支持任何形式的代码抄袭或架构复用指控。双方约15%的AST覆盖率重合，可合理地解释为共同使用Rust标准库、第三方库接口及遵循相同硬件规范所带来的正常技术巧合。

------------------------------

