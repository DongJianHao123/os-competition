# 🛡️ OS 内核赛自动化代码级架构审计报告

**审计时间**: `2026-07-02 11:07:12`
**参赛作品名称**: `2026-T202610486999563-XX`
**年份**: `2026`
**学校**: `XX`
**队伍名称**: `T202610486999563`
**比赛名字**: `OS-Competition`
**比赛子赛道**: `Kernel-Implementation`
**仓库地址**: `https://gitlab.eduxiji.net/educg-group-43501-3132633/T202610486999563-1502`
**推测开发语言**: `Rust / C (由AST探针自动识别)`
**特征函数量**: `2087`

## ⚔️ 底层代码同源性对比 (AST Homology)
> 本部分由专门的代码审查智能体，对该项目与符合年份条件的历届最高相似度项目进行控制流与底层调用对比。新版报告会先给出确定性的全模块覆盖矩阵，再给出 Agent 的分模块解释，避免只围绕少量抽样函数展开。

### 🏆 Top 5 底层对标: vs `2025-AstranciA-哈尔滨工业大学深圳`（哈尔滨工业大学（深圳） / AstranciA）

#### 对方作品信息

**对方年份**: `2025`
**对方学校**: `哈尔滨工业大学（深圳）`
**对方队伍名称**: `AstranciA`
**对方比赛名字**: `OS-Competition`
**对方比赛子赛道**: `Kernel-Implementation`
**对方仓库地址**: `https://gitlab.eduxiji.net/educg-group-36002-2710490/T202518123995667-1436`

> 双向覆盖率：共同 AST 指纹: 403；唯一 AST 指纹: 目标项目 1823，参考项目 3750；覆盖率: 目标项目 22.11%，参考项目 10.75%

## 全模块对比总览（确定性证据）

> 总体覆盖率：共同 AST 指纹: 403；唯一 AST 指纹: 目标项目 1823，参考项目 3750；覆盖率: 目标项目 22.11%，参考项目 10.75%

### 1. 模块覆盖矩阵
| 模块 | 目标函数数 | 参考函数数 | 共同唯一 AST 指纹 | Target 覆盖 | Base 覆盖 | 状态 |
| :--- | ---: | ---: | ---: | ---: | ---: | :--- |
| 进程管理 | 0 | 112 | 0 | 0.0% | 0.0% | 仅参考项目体现 |
| 任务调度/异步运行时 | 87 | 455 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |
| 内存管理 | 10 | 343 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |
| 中断/异常处理 | 2 | 71 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |
| 系统调用 | 0 | 136 | 0 | 0.0% | 0.0% | 仅参考项目体现 |
| 文件系统 | 0 | 620 | 0 | 0.0% | 0.0% | 仅参考项目体现 |
| 设备驱动 | 1 | 142 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |
| 网络协议栈 | 0 | 61 | 0 | 0.0% | 0.0% | 仅参考项目体现 |
| 文件系统/第三方库 | 57 | 775 | 3 | 5.26% | 0.47% | 双方存在且有结构重合 |
| 网络协议栈/第三方库 | 125 | 217 | 80 | 67.8% | 41.45% | 双方存在且有结构重合 |
| 第三方库 | 1785 | 1155 | 318 | 20.45% | 34.16% | 双方存在且有结构重合 |
| 通用模块/其他 | 20 | 673 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |

### 2. 分模块证据清单

#### 进程管理
仅参考项目中观察到该模块代表函数，目标项目没有同模块可对齐证据：
- 参考项目：`AstrancE/ulib/axmono/src/task/process.rs#exec_current`，调用摘要："/usr/bin/busybox".to_string, "sh".to_string, Arc::strong_count, Err, Some, UspaceContext::new ...(+37)
- 参考项目：`AstrancE/ulib/axmono/src/task/exit.rs#do_exit`，调用摘要：FD_TABLE.clear, SigInfo_::Child, SigStatus::ExitCode, UserPtr::<Pid>::from, axtask::exit, clear_child_tid.get ...(+21)
- 参考项目：`AstrancE/ulib/axmono/src/task/wait.rs#sys_waitpid`，调用摘要：Err, Ok, WaitOptions::from_bits_truncate, WaitPid::Pgid, WaitPid::Pid, child.data::<ProcessData> ...(+27)
- 参考项目：`AstrancE/modules/axfs/src/mounts.rs#procfs`，调用摘要：Arc::new, Ok, ProcFileSystem::new, format!, proc_root.create_static_file, proc_version_string.as_bytes ...(+2)

#### 任务调度/异步运行时
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `tools/xtask/src/main.rs#contest_selfcheck` | `AstrancE/ulib/axlibc/c/printf.c#format_string_loop` | 同模块上下文对齐/差异观察 | ContestRuntime::detect, ExitCode::from, Path::new, PathBuf::from, Some, branch.trim ...(+35) | ABS_FOR_PRINTING, ADVANCE_IN_FORMAT_STRING, atou_, is_digit_, out_rev_, parse_flags ...(+5) |
| `tools/xtask/src/main.rs#validate_oscomp_full_image` | `AstrancE/modules/axsignal/src/lib.rs#handle_pending_signals` | 同模块上下文对齐/差异观察 | "/glibc/test_all.sh(or /glibc/basic/run-all.sh)".to_string, "/musl/test_all.sh(or /musl/basic/run-all.sh)".to_string, Vec::new, basic_listing.contains, debugfs_exists, debugfs_list ...(+14) | Ok, SigFlags::empty, SigHandler::Default, SignalSet::empty, SignalSet::from, Some ...(+26) |
| `tools/xtask/src/main.rs#qemu_loongarch_with_disk_and_mode` | `AstrancE/ulib/axmono/src/syscall/io.rs#sys_pselect` | 同模块上下文对齐/差异观察 | ExitCode::from, PathBuf::from, PathBuf::from("target")
        .join, PathBuf::from("target")
        .join(LOONGARCH_TARGET)
        .join, PathBuf::from("target")
        .join(LOONGARCH_TARGET)
        .join("debug")
        .join, Some ...(+19) | (nfds as usize).min, Duration::new, Err, FdSets::from, FdSets::from(nfds, readfds, writefds, exceptfds)
                    .poll_all, Ok ...(+18) |
| `tools/xtask/src/main.rs#prepare_oscomp_images` | `AstrancE/modules/axlog/src/lib.rs#log` | 同模块上下文对齐/差异观察 | ExitCode::from, build_oscomp_sdcard, debugfs_write_text, ensure_oscomp_source_image, env::var, env::var("WHUSE_OSCOMP_SKIP_BUILD").is_err ...(+13) | cfg_if::cfg_if!, record.level, record.line, record.line().unwrap_or, record.metadata, record.target ...(+1) |

#### 内存管理
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `platform/riscv64-virt/src/main.rs#dealloc` | `AstrancE/modules/axalloc/src/lib.rs#dealloc` | 同板块同名 | buddy.saturating_add, current.min, layout
                .size, layout
                .size()
                .max, layout
                .size()
                .max(layout.align())
                .max, layout
                .size()
                .max(layout.align())
                .max(size_of::<usize>())
                .next_power_of_two ...(+6) | GlobalAllocator::dealloc, NonNull::new, NonNull::new(ptr).expect |
| `platform/riscv64-virt/src/main.rs#alloc` | `AstrancE/modules/axalloc/src/lib.rs#alloc` | 同板块同名 | core::arch::asm!, core::fmt::Write::write_fmt, core::ptr::null_mut, cpu.disable_interrupts, cpu.enable_interrupts, cpu.interrupts_enabled ...(+9) | Ok, balloc.add_memory, balloc.alloc, balloc.total_bytes, debug!, layout.size ...(+5) |
| `platform/loongarch64-virt/src/main.rs#alloc` | `AstrancE/modules/axhal/src/tls.rs#alloc` | 同板块同名 | (start_order + 1..order + 1).rev, Err, NonNull::new, NonNull::new(self.pop(start_order).unwrap() as *mut u8).unwrap, Ok, layout
                .size ...(+11) | Layout::from_size_align, Layout::from_size_align(tls_area_size(), TLS_ALIGN).unwrap, NonNull::new, NonNull::new(area_base).unwrap, alloc::alloc::alloc_zeroed, area_base.add ...(+4) |
| `platform/riscv64-virt/src/main.rs#alloc` | `AstrancE/modules/axalloc/src/page.rs#alloc` | 同板块同名 | (start_order + 1..order + 1).rev, Err, NonNull::new, NonNull::new(self.pop(start_order).unwrap() as *mut u8).unwrap, Ok, layout
                .size ...(+11) | global_allocator, global_allocator()
            .alloc_pages, global_allocator()
            .alloc_pages(1, PAGE_SIZE)
            .map, global_allocator()
            .alloc_pages(1, PAGE_SIZE)
            .map(|vaddr| Self {
                start_vaddr: vaddr.into(),
                num_pages: 1,
            })
            .map_err, vaddr.into |

#### 中断/异常处理
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `platform/loongarch64-virt/src/main.rs#main` | `AstrancE/ulib/axsyscall/src/syscall_imp/errno.rs#try_from` | 同模块上下文对齐/差异观察 | hal_loongarch64_virt::bootstrap, kernel_core::Kernel::bootstrap, println! | Err, Ok |
| `platform/riscv64-virt/src/main.rs#main` | `AstrancE/modules/axhal/src/arch/loongarch64/unaligned.rs#emulate_load_store_insn` | 同模块上下文对齐/差异观察 | hal_riscv64_virt::bootstrap, kernel_core::Kernel::bootstrap, println! | asm!, badv::read, badv::read().raw, core::slice::from_raw_parts_mut, panic!, read_fpr ...(+5) |

#### 系统调用
仅参考项目中观察到该模块代表函数，目标项目没有同模块可对齐证据：
- 参考项目：`AstrancE/modules/axsignal/src/siginfo.rs#fill_raw_siginfo`，调用摘要：addr.as_ptr, call_addr.as_ptr, core::mem::size_of::<siginfo_t>, i32::from, ptr.as_ptr, ptr::write_bytes ...(+1)
- 参考项目：`AstrancE/api/arceos_posix_api/src/imp/net.rs#sys_getaddrinfo`，调用摘要：char_ptr_to_str, debug!, syscall_body!
- 参考项目：`AstrancE/ulib/axmono/src/syscall/time.rs#sys_setitimer`，调用摘要：Err, ItimerConfig::default, Ok, Some, new_value.is_null, old_value.is_null ...(+1)
- 参考项目：`AstrancE/api/arceos_posix_api/src/imp/fd_ops.rs#sys_ppoll`，调用摘要：syscall_body!

#### 文件系统
仅参考项目中观察到该模块代表函数，目标项目没有同模块可对齐证据：
- 参考项目：`AstrancE/ulib/axlibc/c/glob.c#do_glob`，调用摘要：S_ISDIR, append, closedir, do_glob, errfunc, fnmatch ...(+7)
- 参考项目：`AstrancE/api/arceos_posix_api/src/imp/fd_ops.rs#sys_copy_file_range`，调用摘要：Err, Ok, SeekFrom::Current, SeekFrom::Start, core::cmp::min, debug! ...(+11)
- 参考项目：`AstrancE/ulib/axmono/src/mm/mod.rs#map_elf_sections_with_auxv`，调用摘要：Err, Ok, Some, Vec::from, Vec::new, VirtAddr::from_usize ...(+23)
- 参考项目：`AstrancE/ulib/axlibc/src/setjmp.rs#longjmp`，调用摘要：core::arch::naked_asm!

#### 设备驱动
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `platform/riscv64-virt/src/main.rs#panic` | `AstrancE/modules/axdriver/src/lib.rs#init_drivers` | 同模块上下文对齐/差异观察 | core::arch::asm!, core::fmt::Write::write_fmt, core::ptr::write_volatile, format_args! | AllDevices::default, all_devs.block.iter_mut, all_devs.block.iter_mut().enumerate, all_devs.display.iter_mut, all_devs.display.iter_mut().enumerate, all_devs.net.iter_mut ...(+14) |

#### 网络协议栈
仅参考项目中观察到该模块代表函数，目标项目没有同模块可对齐证据：
- 参考项目：`AstrancE/ulib/axlibc/c/network.c#inet_pton`，调用摘要：hexval, inet_pton, isdigit, memmove
- 参考项目：`AstrancE/api/arceos_posix_api/src/imp/net.rs#shutdown`，调用摘要：Ok, tcpsocket.lock, tcpsocket.peer_addr, tcpsocket.shutdown, udpsocket.lock, udpsocket.peer_addr ...(+1)
- 参考项目：`AstrancE/ulib/axlibc/c/socket.c#accept4`，调用摘要：accept, fcntl
- 参考项目：`AstrancE/api/arceos_posix_api/src/imp/net.rs#recvfrom`，调用摘要：Ok, Some, tcpsocket.lock, tcpsocket.lock().recv, tcpsocket.lock().recv(buf).map, udpsocket
                .lock ...(+2)

#### 文件系统/第三方库
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `third_party/virtio-drivers/src/queue.rs#add_buffers_indirect` | `AstrancE/crates/virtio-drivers/src/queue.rs#add_buffers_indirect` | 同板块同名同结构 | MmioTransport::new_from_unique, MmioTransport::new_from_unique(
            UniqueMmioPointer::from(&mut header),
            UniqueMmioPointer::from([].as_mut_slice()),
        )
        .unwrap, UniqueMmioPointer::from, VirtIOHeader::make_fake_header, VirtQueue::<FakeHal, 4>::new, VirtQueue::<FakeHal, 4>::new(&mut transport, 0, true, false).unwrap ...(+8) | MmioTransport::new_from_unique, MmioTransport::new_from_unique(
            UniqueMmioPointer::from(&mut header),
            UniqueMmioPointer::from([].as_mut_slice()),
        )
        .unwrap, UniqueMmioPointer::from, VirtIOHeader::make_fake_header, VirtQueue::<FakeHal, 4>::new, VirtQueue::<FakeHal, 4>::new(&mut transport, 0, true, false).unwrap ...(+8) |
| `third_party/virtio-drivers/src/queue.rs#add_indirect` | `AstrancE/crates/virtio-drivers/src/queue.rs#add_indirect` | 同板块同名同结构 | <[Descriptor]>::new_box_zeroed_with_elems, <[Descriptor]>::new_box_zeroed_with_elems(inputs.len() + outputs.len()).unwrap, Box::leak, Box::leak(indirect_list).as_bytes, Box::leak(indirect_list).as_bytes().into, InputOutputIter::new ...(+14) | <[Descriptor]>::new_box_zeroed_with_elems, <[Descriptor]>::new_box_zeroed_with_elems(inputs.len() + outputs.len()).unwrap, Box::leak, Box::leak(indirect_list).as_bytes, Box::leak(indirect_list).as_bytes().into, InputOutputIter::new ...(+14) |
| `third_party/virtio-drivers/src/queue.rs#add_direct` | `AstrancE/crates/virtio-drivers/src/queue.rs#add_direct` | 同板块同名同结构 | InputOutputIter::new, assert_ne!, desc.set_buf::<H>, inputs.len, outputs.len, self.desc_shadow[usize::from(last)]
            .flags
            .remove ...(+2) | InputOutputIter::new, assert_ne!, desc.set_buf::<H>, inputs.len, outputs.len, self.desc_shadow[usize::from(last)]
            .flags
            .remove ...(+2) |
| `crates/fs-ext4/src/lib.rs#read_dir` | `AstrancE/crates/axfs_crates/axfs_procfs/src/dir.rs#read_dir` | 同板块同名 | Ok, Vec::new, entry.file_type, entry.file_type().map_err, entry.map_err, entry.metadata ...(+7) | BTreeMap::new, Ok, VfsDirEntry::new, all_children.insert, all_children.iter, all_children.iter().skip ...(+10) |

#### 网络协议栈/第三方库
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `third_party/virtio-drivers/src/device/socket/connectionmanager.rs#send_recv` | `AstrancE/crates/virtio-drivers/src/device/socket/connectionmanager.rs#send_recv` | 同板块同名同结构 | "Hello from guest".as_bytes, (hello_from_guest.len() as u32).into, (hello_from_host.len() as u32).into, 0.into, 50.into, Arc::new ...(+43) | "Hello from guest".as_bytes, (hello_from_guest.len() as u32).into, (hello_from_host.len() as u32).into, 0.into, 50.into, Arc::new ...(+43) |
| `third_party/virtio-drivers/src/device/socket/connectionmanager.rs#incoming_connection` | `AstrancE/crates/virtio-drivers/src/device/socket/connectionmanager.rs#incoming_connection` | 同板块同名同结构 | 0.into, 50.into, Arc::new, Mutex::new, ReadOnly::new, SocketType::Stream.into ...(+24) | 0.into, 50.into, Arc::new, Mutex::new, ReadOnly::new, SocketType::Stream.into ...(+24) |
| `third_party/virtio-drivers/src/device/socket/vsock.rs#from_header` | `AstrancE/crates/virtio-drivers/src/device/socket/vsock.rs#from_header` | 同板块同名同结构 | Err, Ok, SocketError::InvalidOperation.into, debug!, header.buf_alloc.into, header.check_data_is_empty ...(+5) | Err, Ok, SocketError::InvalidOperation.into, debug!, header.buf_alloc.into, header.check_data_is_empty ...(+5) |
| `third_party/virtio-drivers/src/device/net/dev_raw.rs#send` | `AstrancE/modules/axnet/src/smoltcp_impl/tcp.rs#send` | 同板块同名 | Ok, send! | Err, Ok, SOCKET_SET.with_socket_mut::<tcp::Socket, _, _>, ax_err!, ax_err_type!, self.block_on ...(+10) |

#### 第三方库
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `third_party/virtio-drivers/src/queue.rs#fake_read_write_queue` | `AstrancE/crates/virtio-drivers/src/queue.rs#fake_read_write_queue` | 同板块同名同结构 | (*available_ring).idx.load, (*used_ring).idx.fetch_add, (*used_ring).idx.load, Vec::new, assert!, assert_eq! ...(+18) | (*available_ring).idx.load, (*used_ring).idx.fetch_add, (*used_ring).idx.load, Vec::new, assert!, assert_eq! ...(+18) |
| `third_party/virtio-drivers/src/device/sound/fake.rs#handle_control_request` | `AstrancE/crates/virtio-drivers/src/device/sound/fake.rs#handle_control_request` | 同板块同名同结构 | CommandCode::SOk.into, Some, Vec::new, VirtIOSndHdr {
                            command_code: CommandCode::SOk.into(),
                        }
                        .as_bytes, VirtIOSndHdr::read_from_prefix, VirtIOSndHdr::read_from_prefix(&request)
                .expect ...(+16) | CommandCode::SOk.into, Some, Vec::new, VirtIOSndHdr {
                            command_code: CommandCode::SOk.into(),
                        }
                        .as_bytes, VirtIOSndHdr::read_from_prefix, VirtIOSndHdr::read_from_prefix(&request)
                .expect ...(+16) |
| `third_party/virtio-drivers/src/transport/pci/bus.rs#bar_info` | `AstrancE/crates/virtio-drivers/src/transport/pci/bus.rs#bar_info` | 同板块同名同结构 | (!(size_mask & !flag_bits)).wrapping_add, Err, MemoryBarType::try_from, Ok, Some, self
                .configuration_access
                .read_word ...(+6) | (!(size_mask & !flag_bits)).wrapping_add, Err, MemoryBarType::try_from, Ok, Some, self
                .configuration_access
                .read_word ...(+6) |
| `third_party/virtio-drivers/src/queue.rs#recycle_descriptors` | `AstrancE/crates/virtio-drivers/src/queue.rs#recycle_descriptors` | 同板块同名 | Box::from_raw, H::unshare, InputOutputIter::new, InputOutputIter::new(inputs, outputs).enumerate, Some, assert_eq! ...(+17) | Box::from_raw, H::unshare, InputOutputIter::new, InputOutputIter::new(inputs, outputs).enumerate, Some, assert_eq! ...(+17) |

#### 通用模块/其他
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `platform/loongarch64-virt/src/main.rs#panic` | `AstrancE/modules/axruntime/src/lang_items.rs#panic` | 同板块同名 | core::fmt::Write::write_fmt, core::hint::spin_loop, format_args! | axhal::misc::terminate, error! |
| `platform/riscv64-virt/src/main.rs#init` | `AstrancE/ulib/axlibc/c/printf.c#print_exponential_number` | 同模块上下文对齐/差异观察 | core::fmt::Write::write_fmt, format_args!, min, prev_power_of_two, self.push, size.trailing_zeros ...(+1) | ABS_FOR_PRINTING, PRINTF_ABS, bastardized_floor, get_components, get_normalized_components, get_sign_bit ...(+5) |
| `platform/loongarch64-virt/src/main.rs#init` | `AstrancE/ulib/axlibc/c/pow.c#pow` | 同模块上下文对齐/差异观察 | min, prev_power_of_two, self.push, size.trailing_zeros, size_of::<usize> | __builtin_fma, __math_invalid, __math_oflow, __math_uflow, asdouble, asuint64 ...(+8) |
| `platform/loongarch64-virt/src/main.rs#pop` | `AstrancE/ulib/axlibc/c/string.c#twoway_strstr` | 同模块上下文对齐/差异观察 | Some, res.is_null | BITOP, MAX, memchr, memcmp |


好的，我将遵循审计指令，以最终同源性定性仲裁者的身份，基于提供的客观证据和双方论点，给出最终裁决。

### 1. 宏观判定
- **定性结论**: **独立开发，无实质性同源复用**
  基于客观 AST 相似度 7.79%，以及核心模块“任务调度/异步运行时”共同指纹为 0 的事实，认定双方项目在核心技术实现上存在根本性差异。目标项目 `[2026-T202610486999563-XX]` 的主体是宿主机构建与测试脚手架，而参考项目 `[2025-AstranciA-哈尔滨工业大学深圳]` 是操作系统内核运行时，二者在抽象层次、调用对象和底层逻辑上完全隔离。所有重合点均可归因于对公开第三方库的共同依赖或通用编程范式，不构成对参考项目的实质性代码复用。

### 2. 全模块覆盖矩阵解读
- **进程管理**
  - 双方存在性: 仅参考项目存在。
  - 共同 AST 指纹: 0。
  - 覆盖率解读: 目标项目无此模块，覆盖率 0.0%，表明目标项目未实现传统意义上的进程管理。
  - 证据强度: **未形成**。

- **任务调度/异步运行时**
  - 双方存在性: 双方均存在。
  - 共同 AST 指纹: 0。
  - 覆盖率解读: 目标项目 87 个函数，参考项目 455 个函数，覆盖率均为 0.0%。这定量地证明了双方在此核心板块的 AST 结构上无任何重合。
  - 证据强度: **未形成**。

- **内存管理**
  - 双方存在性: 双方均存在。
  - 共同 AST 指纹: 0。
  - 覆盖率解读: 目标项目 10 个函数，参考项目 343 个函数，覆盖率均为 0.0%。双方虽都有内存管理，但实现路径完全不同。
  - 证据强度: **未形成**。

- **中断/异常处理**
  - 双方存在性: 双方均存在。
  - 共同 AST 指纹: 0。
  - 覆盖率解读: 目标项目仅 2 个函数，参考项目 71 个函数，覆盖率均为 0.0%。目标项目的中断处理极为简化，与参考项目复杂的异常处理体系无结构重合。
  - 证据强度: **未形成**。

- **系统调用**
  - 双方存在性: 仅参考项目存在。
  - 共同 AST 指纹: 0。
  - 覆盖率解读: 目标项目无此模块，覆盖率 0.0%，符合其作为宿主机构建工具而非内核的定位。
  - 证据强度: **未形成**。

- **文件系统**
  - 双方存在性: 仅参考项目存在。
  - 共同 AST 指纹: 0。
  - 覆盖率解读: 目标项目无此模块，覆盖率 0.0%。目标项目对文件系统的操作完全通过宿主机标准库 `std::fs` 进行，而非实现一个内核文件系统。
  - 证据强度: **未形成**。

- **设备驱动**
  - 双方存在性: 双方均存在。
  - 共同 AST 指纹: 0。
  - 覆盖率解读: 目标项目仅 1 个函数，参考项目 142 个函数，覆盖率均为 0.0%。目标项目的设备驱动概念与参考项目自研的驱动框架无任何 AST 层面的联系。
  - 证据强度: **未形成**。

- **网络协议栈**
  - 双方存在性: 仅参考项目存在。
  - 共同 AST 指纹: 0。
  - 覆盖率解读: 目标项目无此模块，覆盖率 0.0%。
  - 证据强度: **未形成**。

- **第三方库/公共依赖**
  - 双方存在性: 双方均存在。
  - 共同 AST 指纹: 401。
  - 覆盖率解读: 共同指纹几乎全部集中于此（`文件系统/第三方库`、`网络协议栈/第三方库`、`第三方库`）。这是双方项目 AST 相似度的唯一来源，源于对 `virtio-drivers` 等上游开源 crate 的共同依赖。
  - 证据强度: **弱**。该重合是依赖收敛，不能作为目标项目对参考项目原创代码复用的证据。

### 3. 底层 AST 关键证据指纹表
| 所属板块 | 目标项目路径 | 参考项目路径 | 证据类型 | 技术说明 |
| :--- | :--- | :--- | :--- | :--- |
| 第三方库/公共依赖 | `third_party/virtio-drivers/src/queue.rs#add_buffers_indirect` | `AstrancE/crates/virtio-drivers/src/queue.rs#add_buffers_indirect` | 同板块同名同结构 | 双方均使用了相同版本的 `virtio-drivers` 公共库，该函数及其内部调用流（如 `MmioTransport::new_from_unique`）完全一致，属于公开依赖。 |
| 第三方库/公共依赖 | `third_party/virtio-drivers/src/device/socket/connectionmanager.rs#send_recv` | `AstrancE/crates/virtio-drivers/src/device/socket/connectionmanager.rs#send_recv` | 同板块同名同结构 | 同上，双方均依赖相同的公开 virtio socket 驱动库，AST 结构必然一致。 |
| 第三方库/公共依赖 | `third_party/virtio-drivers/src/transport/pci/bus.rs#bar_info` | `AstrancE/crates/virtio-drivers/src/transport/pci/bus.rs#bar_info` | 同板块同名同结构 | 同上，PCI 总线配置的底层实现完全来自公共库，非任何一方原创。 |
| 任务调度/异步运行时 | `tools/xtask/src/main.rs#main` | `App_oscomp/src/main.rs#main` | 同板块同名 | 双方均有程序入口 `main` 函数，但目标项目是构建工具入口，参考项目是内核测试入口，AST 调用图谱无交集，属于语言强制范式。 |
| 内存管理 | `platform/riscv64-virt/src/main.rs#alloc` | `AstrancE/modules/axalloc/src/lib.rs#alloc` | 同板块同名 | 函数同名，但目标项目实现的是简单的栈式分配器，参考项目调用的是全局分配器接口，内部逻辑和调用对象完全不同。 |

### 4. 核心代码调用流对比剖析
* **【任务调度/异步运行时】**
* **底层技术析理**: 目标项目的 `main` 函数是一个典型的宿主机构建工具入口，其调用流围绕 `Command::new` 展开，通过创建子进程来调用 `cargo`、`docker`、`qemu-system-*` 等外部工具，完成编译、打包、模拟测试等任务。这是一种**进程级调度**，依赖宿主机操作系统能力。参考项目的 `main` 函数是一个运行在内核之上的用户态测试程序入口，其调用流围绕内核抽象 `axmono::init`、`init_fs` 展开，初始化内核模块并挂载文件系统。这是一种**内核态/运行时初始化**。两者在抽象层次（用户态工具 vs. 内核态运行时）、调度对象（外部进程 vs. 内核模块）、依赖基础（`std::process::Command` vs. `axmono`）上存在根本性异构。
* **代码溯源链接**:
  - 目标项目: `tools/xtask/src/main.rs#main`
  - 参考项目: `App_oscomp/src/main.rs#main`

* **【内存管理】**
* **底层技术析理**: 目标项目在 `platform/riscv64-virt/src/main.rs#alloc` 中实现了一个基于栈的简易分配器，通过位运算和栈操作 (`self.pop`, `self.push`) 管理固定大小的内存块。这是一种**无运行时依赖的静态分配策略**。参考项目在 `AstrancE/modules/axalloc/src/lib.rs#alloc` 中则通过 `GlobalAllocator::alloc` 接口进行分配，背后是一个复杂的、支持多内存区域的全局分配器。两者在内存模型（静态栈 vs. 全局多区域分配器）和同步策略（关中断无锁 vs. 全局分配器内部锁机制）上完全不同。
* **代码溯源链接**:
  - 目标项目: `platform/riscv64-virt/src/main.rs#alloc`
  - 参考项目: `AstrancE/modules/axalloc/src/lib.rs#alloc`

### 5. 单方模块与降权项
- **单方模块**: `进程管理`、`系统调用`、`文件系统`、`网络协议栈` 等模块仅在参考项目中存在，是构成一个完整宏内核的关键组件。目标项目作为构建工具，不包含这些模块，这从根本上决定了两者技术栈的差异。
- **第三方库重合**: `文件系统/第三方库`、`网络协议栈/第三方库`、`第三方库` 中的 AST 重合是双方项目相似度的唯一来源。这些重合点均指向 `virtio-drivers` 等公开的、广泛使用的上游 crate。引用相同的第三方库是正常且合法的软件开发行为，其导致的 AST 指纹重合不能作为抄袭或违规的证据。
- **降权项**: 控方提出的所有指控均已被依法降级为弱信号。`main` 函数同名是语言强制范式；文件系统操作 (`fs::copy` vs `init_rootfs`) 和外部状态读取 (`env::var` vs `sys_sysinfo`) 的对比属于跨领域、跨抽象层次的硬凑，其底层 AST 调用图谱无任何交集，不构成同板块主证据。

### 6. 保守结论
综合全模块 AST 覆盖矩阵与底层调用流分析，本案 7.79% 的 AST 相似度完全由双方共同依赖的公开第三方库贡献。在操作系统核心板块（任务调度、内存管理、中断处理等），双方共同 AST 指纹均为 0，表明不存在任何实质性代码复用。目标项目是一个面向宿主机环境的构建与测试脚手架，而参考项目是一个完整宏内核的运行时，二者在技术定位、抽象层次和实现路径上存在根本性差异。所有指控点均因跨板块硬凑或依赖公开库而被消解。因此，本庭裁定目标项目 `[2026-T202610486999563-XX]` 相对于参考项目 `[2025-AstranciA-哈尔滨工业大学深圳]` 为独立开发，无同源性违规。

------------------------------

