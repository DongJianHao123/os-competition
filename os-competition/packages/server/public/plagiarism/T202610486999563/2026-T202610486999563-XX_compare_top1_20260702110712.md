# 🛡️ OS 内核赛自动化代码级架构审计报告

**审计时间**: `2026-07-02 11:07:12`
**参赛作品名称**: `2026-T202610486999563-XX`
**年份**: `2026`
**学校**: `XX`
**队伍名称**: `T202610486999563`
**比赛名字**: `OS-Competition`
**比赛子赛道**: `Kernel-Implementation`
**仓库地址**: `https://gitlab.eduxiji.net/educg-group-43501-3132633/T202610486999563-1502`
**推测开发语言**: `Rust / C (由AST探针自动识别)`
**特征函数量**: `2087`

## ⚔️ 底层代码同源性对比 (AST Homology)
> 本部分由专门的代码审查智能体，对该项目与符合年份条件的历届最高相似度项目进行控制流与底层调用对比。新版报告会先给出确定性的全模块覆盖矩阵，再给出 Agent 的分模块解释，避免只围绕少量抽样函数展开。

### 🏆 Top 1 底层对标: vs `2025-KernalCraft-中山大学`（中山大学 / KernalCraft）

#### 对方作品信息

**对方年份**: `2025`
**对方学校**: `中山大学`
**对方队伍名称**: `KernalCraft`
**对方比赛名字**: `OS-Competition`
**对方比赛子赛道**: `Kernel-Implementation`
**对方仓库地址**: `https://gitlab.eduxiji.net/educg-group-36002-2710490/T202510558995240-2536`

> 双向覆盖率：共同 AST 指纹: 399；唯一 AST 指纹: 目标项目 1823，参考项目 1853；覆盖率: 目标项目 21.89%，参考项目 21.53%

## 全模块对比总览（确定性证据）

> 总体覆盖率：共同 AST 指纹: 399；唯一 AST 指纹: 目标项目 1823，参考项目 1853；覆盖率: 目标项目 21.89%，参考项目 21.53%

### 1. 模块覆盖矩阵
| 模块 | 目标函数数 | 参考函数数 | 共同唯一 AST 指纹 | Target 覆盖 | Base 覆盖 | 状态 |
| :--- | ---: | ---: | ---: | ---: | ---: | :--- |
| 进程管理 | 0 | 71 | 0 | 0.0% | 0.0% | 仅参考项目体现 |
| 任务调度/异步运行时 | 87 | 115 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |
| 内存管理 | 10 | 741 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |
| 中断/异常处理 | 2 | 128 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |
| 系统调用 | 0 | 21 | 0 | 0.0% | 0.0% | 仅参考项目体现 |
| 文件系统 | 0 | 147 | 0 | 0.0% | 0.0% | 仅参考项目体现 |
| 设备驱动 | 1 | 555 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |
| 网络协议栈 | 0 | 53 | 0 | 0.0% | 0.0% | 仅参考项目体现 |
| 文件系统/第三方库 | 57 | 340 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |
| 网络协议栈/第三方库 | 125 | 0 | 0 | 0.0% | 0.0% | 仅目标项目体现 |
| 第三方库 | 1785 | 2 | 1 | 0.06% | 50.0% | 双方存在且有结构重合 |
| 通用模块/其他 | 20 | 669 | 0 | 0.0% | 0.0% | 双方存在但未观察到共同结构指纹 |

### 2. 分模块证据清单

#### 进程管理
仅参考项目中观察到该模块代表函数，目标项目没有同模块可对齐证据：
- 参考项目：`os/src/syscall/process.rs#sys_exec`，调用摘要：BUSYBOX_BIN.to_vec, String::from, Vec::new, all_data.as_slice, app_inode.read_all, args.add ...(+15)
- 参考项目：`os/src/syscall/signal.rs#sys_sigprocmask`，调用摘要：SignalFlags::from_bits, SignalFlags::from_bits(new_set).unwrap, curr_process.inner_exclusive_access, current_process, inner.memory_set.token, inner.memory_set.write_to_user_ptr ...(+2)
- 参考项目：`fdt/src/node.rs#children`，调用摘要：CStr::new, CStr::new(stream.remaining()).expect, CStr::new(stream.remaining()).expect("unit name").as_str, FdtData::new, NodeProperty::parse, Self::new ...(+11)
- 参考项目：`os/src/syscall/info.rs#write_proc_global`，调用摘要：EXT4
            .generic_open, EXT4
            .generic_open(
                filename,
                &mut parent_inode_num,
                true,
                InodeFileType::S_IFREG.bits(),
                &mut 0,
            )
            .unwrap, EXT4.ext4_dir_mk_at, Inode::new, InodeFileType::S_IFREG.bits, content.as_bytes ...(+13)

#### 任务调度/异步运行时
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `tools/xtask/src/main.rs#contest_selfcheck` | `os/src/syscall/mod.rs#syscall` | 同模块上下文对齐/差异观察 | ContestRuntime::detect, ExitCode::from, Path::new, PathBuf::from, Some, branch.trim ...(+35) | MmapFlags::from_bits_truncate, exit_current_and_run_next, log::error!, sys_accept, sys_brk, sys_chdir ...(+82) |
| `tools/xtask/src/main.rs#validate_oscomp_full_image` | `os/src/syscall/fs.rs#sys_sendfile` | 同模块上下文对齐/差异观察 | "/glibc/test_all.sh(or /glibc/basic/run-all.sh)".to_string, "/musl/test_all.sh(or /musl/basic/run-all.sh)".to_string, Vec::new, basic_listing.contains, debugfs_exists, debugfs_list ...(+14) | (count - total_written).min, Inode::new, UserBuffer::new, curr_process.inner_exclusive_access, current_process, drop ...(+12) |
| `tools/xtask/src/main.rs#qemu_loongarch_with_disk_and_mode` | `os/src/syscall/fs.rs#sys_rename` | 同模块上下文对齐/差异观察 | ExitCode::from, PathBuf::from, PathBuf::from("target")
        .join, PathBuf::from("target")
        .join(LOONGARCH_TARGET)
        .join, PathBuf::from("target")
        .join(LOONGARCH_TARGET)
        .join("debug")
        .join, Some ...(+19) | OpenFlags::RDONLY.bits, current_process, current_user_token, drop, inner.fd_table.len, inner.fd_table[new_parent_fd as usize].clone ...(+18) |
| `tools/xtask/src/main.rs#prepare_oscomp_images` | `os/src/syscall/ppoll.rs#sys_ppoll` | 同模块上下文对齐/差异观察 | ExitCode::from, build_oscomp_sdcard, debugfs_write_text, ensure_oscomp_source_image, env::var, env::var("WHUSE_OSCOMP_SKIP_BUILD").is_err ...(+13) | Box::from_raw, Box::into_raw, Box::new, SignalFlags::empty, curr_process.inner_exclusive_access, current_process ...(+16) |

#### 内存管理
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `platform/riscv64-virt/src/main.rs#dealloc` | `os/src/mm/frame_allocator.rs#dealloc` | 同板块同名 | buddy.saturating_add, current.min, layout
                .size, layout
                .size()
                .max, layout
                .size()
                .max(layout.align())
                .max, layout
                .size()
                .max(layout.align())
                .max(size_of::<usize>())
                .next_power_of_two ...(+6) | panic!, self.recycled.iter, self.recycled.iter().any, self.recycled.push |
| `platform/riscv64-virt/src/main.rs#alloc` | `os/src/utils.rs#alloc` | 同板块同名 | core::arch::asm!, core::fmt::Write::write_fmt, core::ptr::null_mut, cpu.disable_interrupts, cpu.enable_interrupts, cpu.interrupts_enabled ...(+9) | Some, self.get, self.set |
| `platform/loongarch64-virt/src/main.rs#alloc` | `os/src/mm/frame_allocator.rs#alloc` | 同板块同名 | (start_order + 1..order + 1).rev, Err, NonNull::new, NonNull::new(self.pop(start_order).unwrap() as *mut u8).unwrap, Ok, layout
                .size ...(+11) | (self.current - 1).into, Some, ppn.into, self.recycled.pop |
| `platform/riscv64-virt/src/main.rs#alloc` | `os/src/task/id.rs#alloc` | 同板块同名 | (start_order + 1..order + 1).rev, Err, NonNull::new, NonNull::new(self.pop(start_order).unwrap() as *mut u8).unwrap, Ok, layout
                .size ...(+11) | self.recycled.pop |

#### 中断/异常处理
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `platform/loongarch64-virt/src/main.rs#main` | `os/src/hal/trap/riscv.rs#restore` | 同模块上下文对齐/差异观察 | hal_loongarch64_virt::bootstrap, kernel_core::Kernel::bootstrap, println! | asm! |
| `platform/riscv64-virt/src/main.rs#main` | `os/src/hal/trap/riscv.rs#save` | 同模块上下文对齐/差异观察 | hal_riscv64_virt::bootstrap, kernel_core::Kernel::bootstrap, println! | asm! |

#### 系统调用
仅参考项目中观察到该模块代表函数，目标项目没有同模块可对齐证据：
- 参考项目：`os/src/syscall/net.rs#sys_socket`，调用摘要：create_tcp_socket, create_udp_socket
- 参考项目：`os/src/syscall/input.rs#sys_event_get`，调用摘要：KEYBOARD_DEVICE.clone, MOUSE_DEVICE.clone, kb.is_empty, kb.read_event, mouse.is_empty, mouse.read_event
- 参考项目：`os/src/syscall/info.rs#fmt`，调用摘要：writeln!
- 参考项目：`os/src/syscall/info.rs#sys_sysinfo`，调用摘要：Sysinfo::collect, current_user_token, translated_refmut

#### 文件系统
仅参考项目中观察到该模块代表函数，目标项目没有同模块可对齐证据：
- 参考项目：`virtio-drivers/src/transport/pci.rs#new`，调用摘要：Err, Ok, Some, UniqueMmioPointer::new, VirtioPciError::InvalidDeviceId, VirtioPciError::InvalidNotifyOffMultiplier ...(+15)
- 参考项目：`virtio-drivers/src/transport/x86_64.rs#new`，调用摘要：Err, Ok, Some, VirtioPciError::InvalidDeviceId, VirtioPciError::InvalidNotifyOffMultiplier, VirtioPciError::InvalidVendorId ...(+16)
- 参考项目：`virtio-drivers/src/transport/pci/bus.rs#bar_info`，调用摘要：(!(size_mask & !flag_bits)).wrapping_add, Err, MemoryBarType::try_from, Ok, Some, self
                .configuration_access
                .read_word ...(+6)
- 参考项目：`os/src/mm/memory_set.rs#load_dl_interp_if_needed`，调用摘要："/lib/libc.so".to_string, "/libc.so".to_string, EXT4.generic_open, EXT4.generic_open(&interp, &mut 2, false, InodeFileType::S_IFREG.bits(), &mut 0).ok, Inode::new, InodeFileType::S_IFREG.bits ...(+28)

#### 设备驱动
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `platform/riscv64-virt/src/main.rs#panic` | `virtio-drivers/examples/aarch64/src/main.rs#panic` | 同板块同名 | core::arch::asm!, core::fmt::Write::write_fmt, core::ptr::write_volatile, format_args! | error!, system_off::<Hvc>, system_off::<Hvc>().unwrap |

#### 网络协议栈
仅参考项目中观察到该模块代表函数，目标项目没有同模块可对齐证据：
- 参考项目：`lose-net-stack/src/packets/udp.rs#build_data`，调用摘要：((self.data_len + UDP_LEN + IP_LEN) as u16).to_be, ((self.data_len + UDP_LEN) as u16).to_be, ETH_RTYPE_IP.to_be, IP_PROTOCAL_UDP.to_be, UnsafeRefIter::new, check_sum ...(+17)
- 参考项目：`lose-net-stack/src/packets/arp.rs#build_data`，调用摘要：ARP_HRD_ETHER.to_be, ETH_RTYPE_ARP.to_be, ETH_RTYPE_IP.to_be, UnsafeRefIter::new, data_ptr_iter.next_mut::<Arp>, data_ptr_iter.next_mut::<Eth> ...(+11)
- 参考项目：`lose-net-stack/src/packets/tcp.rs#ack`，调用摘要：flags.contains, flags.remove, self.flags.contains
- 参考项目：`lose-net-stack/src/lib.rs#analysis_udp`，调用摘要：IPv4::from_u32, MacAddress::new, Packet::UDP, data_ptr_iter.get_curr_arr, data_ptr_iter.next::<UDP>, ip_header.dst.to_be ...(+5)

#### 文件系统/第三方库
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `crates/fs-ext4/src/lib.rs#is_dir` | `ext4_rs/src/ext4_defs/inode.rs#is_dir` | 同板块同名 | Ok, metadata.is_dir, self.fs.metadata, self.fs.metadata(path).map_err | log::trace!, self.file_type |
| `third_party/virtio-drivers/src/queue.rs#add_buffers_indirect` | `ext4_rs/src/ext4_impls/extents.rs#extent_remove_space` | 同模块上下文对齐/差异观察 | MmioTransport::new_from_unique, MmioTransport::new_from_unique(
            UniqueMmioPointer::from(&mut header),
            UniqueMmioPointer::from([].as_mut_slice()),
        )
        .unwrap, UniqueMmioPointer::from, VirtIOHeader::make_fake_header, VirtQueue::<FakeHal, 4>::new, VirtQueue::<FakeHal, 4>::new(&mut transport, 0, true, false).unwrap ...(+8) | Block::load, Ext4Extent::default, Ok, ex.get_actual_len, ex.get_first_block, ex.get_pblock ...(+15) |
| `crates/fs-ext4/src/lib.rs#read` | `ext4_rs/src/ext4_impls/extents.rs#ext_remove_leaf` | 同模块上下文对齐/差异观察 | Box::new, Err, Ok, dst.len, dst[dst_start..dst_start + len]
                .copy_from_slice, end.saturating_sub ...(+12) | Block::load, Block::load_inode_root_block, Ext4Extent::default, Ok, ex.get_actual_len, ex.get_pblock ...(+14) |
| `third_party/virtio-drivers/src/queue.rs#add_indirect` | `ext4_rs/src/fuse_interface/mod.rs#fuse_rename` | 同模块上下文对齐/差异观察 | <[Descriptor]>::new_box_zeroed_with_elems, <[Descriptor]>::new_box_zeroed_with_elems(inputs.len() + outputs.len()).unwrap, Box::leak, Box::leak(indirect_list).as_bytes, Box::leak(indirect_list).as_bytes().into, InputOutputIter::new ...(+14) | Ext4DirEntry::default, Ext4DirSearchResult::new, Ok, dst_inode_ref.inode.links_count, r.is_err, r.is_ok ...(+11) |

#### 网络协议栈/第三方库
仅目标项目中观察到该模块代表函数，参考项目没有同模块可对齐证据：
- 目标项目：`third_party/virtio-drivers/src/device/socket/connectionmanager.rs#send_recv`，调用摘要："Hello from guest".as_bytes, (hello_from_guest.len() as u32).into, (hello_from_host.len() as u32).into, 0.into, 50.into, Arc::new ...(+43)
- 目标项目：`third_party/virtio-drivers/src/device/socket/connectionmanager.rs#incoming_connection`，调用摘要：0.into, 50.into, Arc::new, Mutex::new, ReadOnly::new, SocketType::Stream.into ...(+24)
- 目标项目：`crates/syscall/src/lib.rs#eventfd_epoll_and_socketpair_smoke`，调用摘要：(eventfd as u64).to_le_bytes, 1u32.to_le_bytes, 1u64.to_le_bytes, KernelVfs::new, ProcessTable::new, Scheduler::new ...(+24)
- 目标项目：`crates/syscall/src/lib.rs#inet_loopback_socket_smoke`，调用摘要：16u32.to_le_bytes, 2u16.to_le_bytes, 7000u16.to_be_bytes, KernelVfs::new, ProcessTable::new, Scheduler::new ...(+21)

#### 第三方库
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `third_party/virtio-drivers/src/transport/mmio.rs#vendor_id` | `virtio-drivers/src/transport/mmio.rs#vendor_id` | 同板块同名同结构 | field_shared!, field_shared!(self.header, vendor_id).read | field_shared!, field_shared!(self.header, vendor_id).read |
| `crates/syscall/src/lib.rs#sys_futex` | `virtio-drivers-old/src/header.rs#vendor_id` | 同模块上下文对齐/差异观察 | Err, Ok, Some, cancel_debug, format!, hal ...(+36) | self.vendor_id.read |

#### 通用模块/其他
| 目标证据 | 参考证据 | 关系 | 目标调用摘要 | 参考调用摘要 |
| :--- | :--- | :--- | :--- | :--- |
| `platform/riscv64-virt/src/main.rs#init` | `fdt/src/pretty_print.rs#print_node` | 同模块上下文对齐/差异观察 | core::fmt::Write::write_fmt, format_args!, min, prev_power_of_two, self.push, size.trailing_zeros ...(+1) | Ok, name.contains, node.children, node.children().next, node.children().next().is_some, node.properties ...(+9) |
| `platform/loongarch64-virt/src/main.rs#init` | `fdt/src/node.rs#ranges` | 同模块上下文对齐/差异观察 | min, prev_power_of_two, self.push, size.trailing_zeros, size_of::<usize> | FdtData::new, Some, core::iter::from_fn, self.cell_sizes, self.parent_cell_sizes, self.properties ...(+4) |
| `platform/loongarch64-virt/src/main.rs#pop` | `fdt/src/node.rs#find_node` | 同模块上下文对齐/差异观察 | Some, res.is_null | CStr::new, CStr::new(stream.remaining()).expect, CStr::new(stream.remaining()).expect("unit name C str").as_str, FdtData::new, FdtNode::new, NodeProperty::parse ...(+16) |
| `platform/riscv64-virt/src/main.rs#pop` | `fdt/src/node.rs#all_nodes` | 同模块上下文对齐/差异观察 | Some, res.is_null | CStr::new, CStr::new(stream.remaining()).expect, CStr::new(stream.remaining()).expect("unit name C str").as_str, CStr::new(stream.remaining()).expect("unit name C str").as_str().unwrap, FdtData::new, NodeProperty::parse ...(+13) |


好的，我将遵循审计指令，基于提供的证据材料，进行最终的定性仲裁。

### 1. 宏观判定
- **定性结论**: **独立开发**
- **核心依据**: 客观 AST 相似度仅为 12.18%，双向覆盖率均低于 22%。在作为指控核心的“任务调度/异步运行时”板块，双方共同 AST 指纹为 0。目标项目证据集中于宿主机端的构建与测试工具链，而参考项目证据集中于操作系统内核运行时，两者在代码实现层面处于完全不同的抽象层次与业务域，不存在可被认定为同源复用的底层逻辑一致性。

### 2. 全模块覆盖矩阵解读
- **进程管理**
  - 双方存在性: 仅参考项目体现。
  - 共同 AST 指纹: 0。
  - 覆盖率解读: 目标项目无此模块，覆盖率 0.0%。
  - 证据强度: **未形成**。

- **任务调度/异步运行时**
  - 双方存在性: 双方均存在。
  - 共同 AST 指纹: 0。
  - 覆盖率解读: 双方覆盖率均为 0.0%。尽管双方均有大量函数，但 AST 指纹无任何重合，表明实现逻辑完全不同。
  - 证据强度: **未形成**。控方已承认此板块无强证据，所有指控均为跨域误判。

- **内存管理**
  - 双方存在性: 双方均存在。
  - 共同 AST 指纹: 0。
  - 覆盖率解读: 双方覆盖率均为 0.0%。目标项目仅有 10 个函数，参考项目有 741 个，规模差异巨大且无结构重合。
  - 证据强度: **未形成**。

- **中断/异常处理**
  - 双方存在性: 双方均存在。
  - 共同 AST 指纹: 0。
  - 覆盖率解读: 双方覆盖率均为 0.0%。
  - 证据强度: **未形成**。

- **系统调用**
  - 双方存在性: 仅参考项目体现。
  - 共同 AST 指纹: 0。
  - 覆盖率解读: 目标项目无此模块，覆盖率 0.0%。
  - 证据强度: **未形成**。

- **文件系统**
  - 双方存在性: 仅参考项目体现。
  - 共同 AST 指纹: 0。
  - 覆盖率解读: 目标项目无此模块，覆盖率 0.0%。
  - 证据强度: **未形成**。

- **设备驱动**
  - 双方存在性: 双方均存在。
  - 共同 AST 指纹: 0。
  - 覆盖率解读: 双方覆盖率均为 0.0%。
  - 证据强度: **未形成**。

- **网络协议栈**
  - 双方存在性: 仅参考项目体现。
  - 共同 AST 指纹: 0。
  - 覆盖率解读: 目标项目无此模块，覆盖率 0.0%。
  - 证据强度: **未形成**。

- **第三方库/公共依赖**
  - 双方存在性: 双方均存在。
  - 共同 AST 指纹: 1。
  - 覆盖率解读: 目标项目覆盖率 0.06%，参考项目覆盖率 50.0%。唯一的共同指纹源于双方均合法依赖的第三方开源库 `virtio-drivers`。
  - 证据强度: **弱**。该证据属于对公共上游依赖的合法复用，不构成项目间的抄袭。

### 3. 底层 AST 关键证据指纹表
| 所属板块 | 目标项目路径 | 参考项目路径 | 证据类型 | 技术说明 |
| :--- | :--- | :--- | :--- | :--- |
| 第三方库 | `third_party/virtio-drivers/src/transport/mmio.rs#vendor_id` | `virtio-drivers/src/transport/mmio.rs#vendor_id` | 同板块同名同结构 | 双方均使用了开源库 `virtio-drivers` 的 `VendorID` 字段访问器，这是对公共上游代码的合法依赖，不构成项目间的原创性抄袭。 |
| 任务调度/异步运行时 | `tools/xtask/src/main.rs#contest_selfcheck` | `os/src/syscall/mod.rs#syscall` | 跨板块概念误植 | 目标项目为宿主机测试调度器，调用 `Command::new` 等；参考项目为内核系统调用分发，调用 `sys_accept` 等。AST 调用图谱无交集。 |
| 内存管理 | `platform/riscv64-virt/src/main.rs#alloc` | `os/src/mm/frame_allocator.rs#alloc` | 跨板块同名 | 函数名相同但实现完全不同。目标项目为伙伴分配器，参考项目为基于回收栈的分配器，AST 调用图谱无交集。 |
| 设备驱动 | `platform/riscv64-virt/src/main.rs#panic` | `virtio-drivers/examples/aarch64/src/main.rs#panic` | 跨板块同名 | 函数名相同但实现完全不同。目标项目使用 `asm!` 停机，参考项目调用 `system_off`，AST 调用图谱无交集。 |

### 4. 核心代码调用流对比剖析
* **【任务调度/异步运行时】**
* **底层技术析理**: 目标项目的“调度”对象是宿主机上的外部进程（QEMU、Docker），通过 `std::process::Command` 启动、`timeout` 控制生命周期、`docker` 实现环境隔离。参考项目的“调度”对象是内核线程，通过 `TaskControlBlock` 管理上下文、`schedule` 函数切换执行流、`inner_exclusive_access` 进行同步。两者在抽象层次（用户态进程 vs 内核态线程）、同步策略（外部命令 vs 内核锁）、内存模型（宿主机内存 vs 内核页表）上存在根本性架构差异。
* **代码溯源链接**:
  - 目标项目: `tools/xtask/src/main.rs#contest_riscv_boot_smoke`
  - 参考项目: `os/src/task/mod.rs#suspend_current_and_run_next`

* **【内存管理】**
* **底层技术析理**: 目标项目的 `alloc` 函数实现了一个基于伙伴系统（Buddy System）的分配器，通过 `layout.size().max(layout.align()).max(size_of::<usize>()).next_power_of_two()` 进行尺寸对齐和伙伴分裂。参考项目的 `alloc` 函数实现了一个基于回收栈（Recycle Stack）的简单帧分配器，通过 `self.recycled.pop()` 获取空闲帧。两者的内存管理算法、数据结构和对齐策略完全不同。
* **代码溯源链接**:
  - 目标项目: `platform/riscv64-virt/src/main.rs#alloc`
  - 参考项目: `os/src/mm/frame_allocator.rs#alloc`

### 5. 单方模块与降权项
- **单方模块**: 进程管理、系统调用、文件系统、网络协议栈等模块仅在参考项目中存在，目标项目无对应实现。这体现了双方在操作系统功能完整度上的巨大差异，而非抄袭。
- **第三方库/公共依赖**: 唯一的共同 AST 指纹 `vendor_id` 来自 `virtio-drivers` 库，这是双方均合法依赖的第三方开源代码，属于公共接口，必须降权，不能作为抄袭证据。
- **跨板块同名函数**: `alloc`, `dealloc`, `panic`, `init`, `pop` 等函数名在系统编程中具有高度通用性。双方同名函数位于不同板块，且 AST 调用图谱完全不同，属于语言惯用法或通用语义的自然重合，必须降权为弱信号。

### 6. 保守结论
本案客观 AST 相似度仅 12.18%，且在核心指控板块“任务调度/异步运行时”中共同指纹为 0。所有被标记为“同板块”的证据均存在根本性分类错误，将目标项目的宿主机端构建工具链与参考项目的操作系统内核源码强行对齐。唯一的共同指纹源于对第三方开源库 `virtio-drivers` 的合法依赖。双方在内存管理、任务调度等核心子系统上展现出完全不同的算法选择与架构设计。综合判定，目标项目为独立开发，不存在基于当前证据的代码抄袭或同源性违规风险。

------------------------------

